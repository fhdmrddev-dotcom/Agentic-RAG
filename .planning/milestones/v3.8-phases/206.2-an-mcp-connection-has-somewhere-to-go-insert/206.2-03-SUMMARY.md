---
phase: 206.2-an-mcp-connection-has-somewhere-to-go-insert
plan: 03
subsystem: workflow-builder / connectors
tags: [mcp, shape-axis, multi-key-clear, serialization, readiness, negative-controls, driven-counterfactual]
requires:
  - "frontend/src/components/workflows/ConnectionPicker.tsx — the `shape` prop wave 2 shipped, MOUNTED here for the first time"
  - "frontend/src/components/workflows/externalShapeVocabulary.ts — the `ExternalActionShape` type wave 2 created"
  - "frontend/src/components/workflows/definitionOps.ts — patchPhaseConfig, READ not edited (the pure spread whose behaviour A1 is about)"
provides:
  - "a way for an author to say *this step calls a tool on an MCP server* — SEED-200's authoring half"
  - "the frontend's FIRST config-key clear, at exactly one site, in both directions"
  - "RESEARCH assumption A1 falsified at T1 against a real store, both halves asserted"
  - "one permanently-unsatisfiable readiness row that stops appearing on MCP steps"
affects:
  - "frontend/src/components/workflows/externalShapeVocabulary.ts"
  - "frontend/src/components/workflows/ExternalActionSection.tsx"
  - "frontend/src/components/workflows/PhaseFormPanel.tsx"
  - "frontend/src/components/workflows/stepReadinessContext.ts"
  - "frontend/src/components/workflows/ExternalActionSection.test.tsx"
  - "frontend/src/components/workflows/StepPanelPort.test.tsx"
  - "scripts/vitest-count-gate.cjs"
tech-stack:
  added: []
  patterns:
    - "a SECOND AXIS on a new type, never a fourth member of a closed cross-language set"
    - "SET EQUALITY over Object.keys for a clearing patch — toMatchObject passes a partial clear"
    - "both halves of a serialization assumption asserted, because the difference IS the assumption"
    - "non-vacuity controls that were ALREADY GREEN at base, so a suppression cannot read as a fix"
    - "driven counterfactual plant restored md5-identical"
    - "correction-beside-the-original on a claim this plan's own first draft got wrong"
key-files:
  created: []
  modified:
    - "frontend/src/components/workflows/externalShapeVocabulary.ts (+54/-0)"
    - "frontend/src/components/workflows/ExternalActionSection.tsx (+264/-4)"
    - "frontend/src/components/workflows/PhaseFormPanel.tsx (+2/-1)"
    - "frontend/src/components/workflows/stepReadinessContext.ts (+29/-2)"
    - "frontend/src/components/workflows/ExternalActionSection.test.tsx (+466/-0)"
    - "frontend/src/components/workflows/StepPanelPort.test.tsx (+73/-0)"
    - "scripts/vitest-count-gate.cjs (+7/-1)"
decisions:
  - "D-206.2-01 executed: the MCP option is a SHAPE on a new type with its own radiogroup, testid and accessible name — never a fourth capability"
  - "D-206.2-21 PROVED BY DRIVING, not asserted: planting the capability testid onto a segment turned 20+ cases RED, including all five ===3 pins; restored md5-identical"
  - "D-206.2-15 / D-206.2-22 executed: the multi-key write goes through a NEW onChangeShape prop; onChange is unwidened and all eight string assertions are untouched"
  - "AR-05 executed: BOTH directions clear connection_id to null, the addition no upstream document named"
  - "AR-03 executed: unset leaves BOTH segments unchecked and renders byte-identically below the control"
  - "A1 FALSIFIED-AT-T1: an undefined value leaves the KEY present in memory and is dropped by JSON.stringify — both halves asserted; T4 browser confirmation still owed to 206.2-04"
  - "DECLINED: ExternalActionSection.test.tsx's gate pin stays at 25 against an actual of 59 — eleven consecutive plans have now declined it for the same reason"
  - "DEVIATION (declared): the panel's three JSX attributes landed in task 1 rather than task 2, so every commit typechecks"
metrics:
  duration: "~35 min (10:02 → 10:37 +0400)"
  completed: 2026-08-25
  tasks: 3
  commits: 3
  files: 7
---

# Phase 206.2 Plan 03: The shape choice, and the frontend's first config-key clear — Summary

An `external_action` step can now say *this step calls a tool on an MCP server*, through a
two-position segmented control that sits BESIDE the capability radiogroup and never inside
it — and switching shape emits ONE multi-key patch that clears the other shape's keys **and**
`connection_id`, which is the first time any code in `frontend/src` has cleared a key inside
`config`.

## What shipped

| Task | Commit | Files | Verdict |
|---|---|---|---|
| 1 · the three strings, the control, the three states, the two clearing patches | `128dcde7` | `externalShapeVocabulary.ts`, `ExternalActionSection.tsx`, `PhaseFormPanel.tsx`, (+2 helper lines in the suite) | RED observed on **5 of 5** before any source edit |
| 2 · the readiness lie, stopped at its source | `664ba306` | `stepReadinessContext.ts`, `PhaseFormPanel.tsx`, `StepPanelPort.test.tsx`, `vitest-count-gate.cjs` | RED observed on **exactly 1 of 4** — the other three are controls that were already green |
| 3 · the proofs, and A1 | `8498b9ae` | `ExternalActionSection.test.tsx` | 59 passed; D-206.2-21 driven RED against a plant, restored md5-identical |

## The measured baselines (D-206.2-09 — re-derived at this plan's own base, never quoted)

Verdict lines read VERBATIM from the repo root with `GSD_VITEST_MAX_WORKERS=2`:

| | At this plan's base (`a3227e76`) | At its close (`8498b9ae`) |
|---|---|---|
| count gate | `total 5643  ·  failed 0  ·  pinned total 5081`<br>`count gate OK — 111/111 pinned files present, no per-file decrease, 0 failing.` | `total 5672  ·  failed 0  ·  pinned total 5085`<br>`count gate OK — 111/111 pinned files present, no per-file decrease, 0 failing.` |
| `ExternalActionSection.test.tsx` row | `25      34      +9` | `25      59     +34` |
| `StepPanelPort.test.tsx` row | `25      25       0` | `29      29       0` |
| `ConnectionPicker.test.tsx` row | `58      58       0` | `58      58       0` — unmoved, and a MEASURED non-touch is a stronger statement than silence |
| `tsc --noEmit -p tsconfig.app.json` | **34** errors, **0** in any touched file | **34** errors, **0** in any touched file |

**The `+29` on the grand total is fully attributed and leaves no residual:** `+25` new cases in
`ExternalActionSection.test.tsx` and `+4` in `StepPanelPort.test.tsx`. The `pinned total`
moved `+4` because only the second of those two files had its pin raised.

⚠ **The gate was never red at any point in this plan**, so SEED-171's triage procedure was not
entered and the worker cap stayed at `2` throughout. Recorded as an observation, not as proof
of innocence.

**Scope fences, asserted rather than assumed** (all against base `a3227e76`):
`frontend/src/lib/api.ts` **0 files** · `backend` + `supabase/migrations` **0 files** ·
`frontend/package.json` / `package-lock.json` **0 files** (the Package Legitimacy Gate never
fired; no package was installed) · `ExternalActionSection.test.tsx` deletions **0**.

## D-206.2-21 — the five pins, and the counterfactual that proves the claim is structural

The five shipped assertions pinning `external-action-option` / `[role="radio"]`-in-that-group
at exactly **3** pass **UNEDITED**, and the file's diff against base contains **zero deletion
lines**. That is the assertion. But *passing* is not the same as *passing for the stated
reason*, so the reason was driven:

**PLANT** — one attribute on real production source, changing the shape segment's testid from
`external-action-shape-option` to `external-action-option`:

```
× nothing chosen: three unselected rows under the heading, plus the honest note
× send_email chosen: that ONE row is selected, and the no-capability note is gone
× create_ticket chosen: … × post_message chosen: …
× an UNRECOGNISED stored value selects NO row and fabricates nothing (T-189-39)
× an INHERITED key selects no row either — `constructor` is not a capability
× the section RENDERS in every state it can reach — never an empty branch
× ONE tab stop, and it is the chosen row — not three
   … 20+ cases red in total, including every one of the five ===3 pins
```

The plant was removed and the file verified **md5-identical** to its committed form
(`md5sum -c` → `OK`, `git diff --numstat` → 0 lines).

⚠ **The refuted first draft is recorded as refuted, not rediscovered:** *hide the capability
rows until a shape is chosen* turns all five red and looks like the more honest design. The
capability radiogroup therefore renders under BOTH `unset` and `capability`, and only the
`mcp` shape withholds it — which is exactly why the shipped suite (which supplies no
`toolName` anywhere) never leaves the non-MCP branch.

## RESEARCH assumption A1 — FALSIFIED AT T1, both halves

Driven against a real `createBuilderStore` in the drafted view, wired to the shipped
`patchConfig`:

| Half | Assertion | Result |
|---|---|---|
| in memory | `Object.keys(config)` still CONTAINS `tool_name` / `tool_args`, and both read `undefined` | ✅ |
| on the wire | `JSON.parse(JSON.stringify(phases))[0].config` contains NEITHER key | ✅ |
| non-vacuity | the serialized config is not merely empty (`phase_type` survives) and `connection_id` survives as an explicit `null` | ✅ |

⚠ **BOTH halves are asserted because the difference between them IS the assumption.** If only
the serialized half were checked, a merge that had started `delete`-ing keys would pass
identically and nobody would learn which mechanism is load-bearing. The other direction (press
B, a stranded `capability`) lands the same way and has its own case.

**A1 moves UNFALSIFIED → FALSIFIED-AT-T1. The T4 browser confirmation is still owed to
`206.2-04`.**

## Task 2 — the RED, and why three of four cases were green before the fix

```
× (b) a step that names an MCP tool is owed no capability row
✓ (a) NON-VACUITY — neither a capability nor a tool STILL yields the row
✓ (c) a WHITESPACE-ONLY tool name behaves as absent — a blank is not an answer
✓ (d) the other arms are untouched by the new field
1 failed | 28 passed
```

**That three of four were already green is the point, not a weakness.** (a) is the half a
careless suppression would silently delete: a `stepGaps` that stopped pushing the row
altogether would turn (b) green and read as a fix, while removing a row that is TRUE for a
step with neither answer. The row is **suppressed, never replaced.**

The lie itself: `outsideSentence` is the caller's lookup into the CLOSED capability table, so
it is `undefined` for **every MCP step forever, however completely configured** — a *still
missing* row that could never be satisfied, on a card whose premise is that a row is something
you can go and fix.

## Deviations from Plan

**Three, all declared. One is a task re-attribution, one is a corrected acceptance criterion
that was unsatisfiable at its own base, and one is a claim this plan's own first draft got
wrong and measured.**

---

**1. [Rule 3 — blocking] The panel's three JSX attributes landed in task 1, not task 2, so
that every commit typechecks.**

- **Found during:** Task 1
- **Issue:** the plan makes `toolName` and `onChangeShape` REQUIRED props (correctly — an
  optional `onChangeShape` would let a mount silently drop the shape write, which is the
  failure mode this phase exists to close), and simultaneously requires
  `git diff -- ExternalActionSection.test.tsx` to be EMPTY for task 1 and `tsc` to equal base
  at task 1's close. Those three cannot all hold: a required prop makes the panel mount and
  the suite's `renderSection` helper into TS2741s the moment the interface changes, so task 1
  would have committed a tree with **36** tsc errors against a base of 34, resolved only two
  commits later.
- **Fix:** the panel's three attributes (`key={phase.slug}`, `toolName=`, `onChangeShape=`)
  and two default entries in the suite's `renderSection` HELPER landed in task 1. Task 2 then
  owns the readiness work and the `stepGaps` call site alone.
- **What is preserved:** every acceptance criterion's PURPOSE. `grep -c "onChangeShape"` == 1
  and `grep -c "key={phase.slug}"` == 1 in `PhaseFormPanel.tsx`; its whole-plan numstat is
  **`+2 / -1`**, inside the plan's `+3 / -2` budget; the suite's diff carries **zero
  deletions** and **no case moved** — the edit is two lines inside a props-default object, the
  same shape wave 2 recorded when it extended this file's import list.
- **Files modified:** `frontend/src/components/workflows/PhaseFormPanel.tsx`,
  `frontend/src/components/workflows/ExternalActionSection.test.tsx`
- **Commit:** `128dcde7`

---

**2. [Rule 1 — two acceptance greps were UNSATISFIABLE AT THEIR OWN BASE, and my first draft
made a third one wrong.]** The 187-24 trap, twelfth recorded firing, and it fired in **both
directions in one plan**.

- **Found during:** Task 1
- **Issue:** the plan asks for `grep -o 'mt-1\.5' … | wc -l` **== 1** and
  `grep -c 'role="radiogroup"' … ` **== 2**. Measured at this plan's base, BEFORE any edit:
  `mt-1.5` reads **2** (`OPTION_LIST_CLASSES` and `NOTE_CLASSES`) and `role="radiogroup"`
  reads **2** (one JSX attribute at `:183` plus **one mention inside a shipped comment**).
  Neither criterion could ever have been satisfied.
- **And my own first draft made it worse:** the docblock I wrote for the new spacing spelled
  `` `mt-1.5` `` twice in prose and the shape control's comment spelled `` `useId` `` twice —
  taking the raw counts to **5** and **4**. A guard whose own prose spells the token it counts
  counts itself.
- **Fix:** the prose was rewritten to name the values without spelling them (*"the 6px top
  gap"*, *"generates no React id at all"*), and the criteria are restated in their satisfiable
  form with both readings recorded:

  | Criterion | plan's form | base | close | corrected form |
  |---|---|---|---|---|
  | 6px spent once on a NEW node | `mt-1.5` == 1 | **2** | **3** | **+1 against base** |
  | a second radiogroup | `role="radiogroup"` == 2 | 2 (1 JSX + 1 comment) | 3 | **JSX-scoped** `grep -cE '^\s*role="radiogroup"$'`: base **1** → close **2** |
  | AR-07 no new React id | `useId` unchanged | **2** | **2** | ✅ unchanged, as written |

- **Files modified:** `frontend/src/components/workflows/ExternalActionSection.tsx`
- **Commit:** `128dcde7`

---

**3. [Rule 1 — a claim in my own new test was OBSERVED RED and is corrected beside the
original.]**

- **Found during:** Task 3
- **Issue:** the byte-identity case's first docblock asserted, in writing, that *"both sides
  are produced by the SAME component in the SAME run, so no id normalization is needed"* — the
  plan says the same thing. It is **false**, and it went red immediately:
  `AssertionError: expected '<div role="radiogroup" aria-labelledb…' to be '<div role="radiogroup" aria-labelledb…'`.
  React's `useId` counter advances **per MOUNT**, not per commit, so two sequential mounts in
  one run carry different ids.
- **Fix:** a COUNTED normalization, with the needle read off a real render (`_r_N_` —
  underscores, no guillemets, confirming wave 2's correction of D-206.2-18) and the count
  asserted per capture: the options subtree carries **exactly 1** generated id, the
  nothing-chosen note carries **0** and therefore compares raw. The original claim is kept in
  the case's docblock above its correction, because the useful part is *why* it was wrong.
- **Files modified:** `frontend/src/components/workflows/ExternalActionSection.test.tsx`
- **Commit:** `8498b9ae`

---

**A declined change, recorded so it is a decision rather than an omission:**
`"ExternalActionSection.test.tsx"` in `scripts/vitest-count-gate.cjs` **stays at 25** against
an actual of **59** (declared drift now `+34`, was `+9`). The gate's contract is *no per-file
DECREASE* and *zero failing*; adding cases moves the ACTUAL upward, which is the gate working.
**Eleven consecutive plans have now declined this pin for the same reason.**
`"StepPanelPort.test.tsx"` WAS raised, `25 → 29`, because it had no declared drift to inherit.

## ⚠ FLAGGED FOR UAT — two deliberate non-changes the first round will otherwise report as bugs

| What a tester will see | Why it is deliberate | Re-open trigger |
|---|---|---|
| **An MCP step shows NO consequence sentence** under the shape control, where a capability step shows one | `OutsideChangeLine` renders **nothing** for a step with no capability, and its own docblock argues why: *"A card that printed a blank consequence row would be byte-indistinguishable from one whose lookup failed, and on a governance surface those two readings are opposite."* Adding an MCP arm re-opens a decision that file made deliberately | *the wire gaining a read/write hint for a discovered tool (an `annotations` key)* — the only thing that would let such a sentence be true rather than generic |
| **The canvas card for an MCP step reads *"Reach outside"*, and its effect banner reads `CHANGES SOMETHING OUTSIDE` even for a read-only tool** | `derivedFace` falls through to the type sentence — honest, and identical for every MCP step regardless of tool. `nodeEffectBanner` keys on the phase TYPE, not on the tool. Register threat `T-206.2-03-R1`, disposition **accept, flagged** | *`SEED-199`'s canvas-grammar work starting* |

## What this plan does NOT claim

- ⚠ **Nothing here proves the MCP shape is reachable in production.** Every case in this suite
  constructs the section's props by hand, and *a test that constructs its own props is unable
  to see that nothing constructs them in production* — SEED-200's own finding, which wave 2
  recorded about itself. **The two-legged guard (D-206.2-07 / D-206.2-12) is `206.2-04`'s**,
  and leg (b) must run through the panel and construct no component prop at all. What this
  plan *does* add toward it is the missing wire: `PhaseFormPanel.tsx` now hands the section a
  `toolName` and a shape writer, so there is finally something for that guard to find.
- **No live MCP connection was bound through a browser, and no run was driven.** The shape's
  effect on a real `workflow_definitions.definition` row is asserted through the shipped store
  and `JSON.stringify`, not observed in Postgres. T4 belongs to wave 4.
- **`ConnectionPicker.tsx` and `frontend/src/lib/api.ts` are not edited** — 0 files each,
  measured. The picker's `shape` prop is CONSUMED here for the first time and unchanged.
- **The backend is untouched.** `_exec_external_action`'s two-shape branch was READ, and its
  line is cited at the clear's own site; nothing in `backend/` moved.

## ⚠ Hot-file ledger — four rows move or are absent. Plan 04 owns the commit (D-206.2-11)

Re-derived with the CLAUDE.md recipe at this plan's close (dated quick-task buckets
subtracted):

| File | ledger row says | **measured at `8498b9ae`** | G-5 |
|---|---|---|---|
| `frontend/src/components/workflows/PhaseFormPanel.tsx` | `24 / 11 / 1375` | **`29 / 13 / 1561`** | **FIRES** |
| `frontend/src/components/workflows/ConnectionPicker.tsx` | ⚠ **NO ROW AT ALL** | **`4 / 3 / 616`** — unchanged by this plan | ⚠ **FIRES — EXACTLY AT THRESHOLD** |
| `scripts/vitest-count-gate.cjs` | `114 / 19 / 3797` | **`117 / 21 / 3871`** | ⚠ **FIRES** |
| `frontend/src/components/workflows/phaseVocabulary.ts` | ⚠ **NO ROW AT ALL** | **`16 / 6 / 990`** — READ not edited | **FIRES** |
| `frontend/src/components/workflows/ExternalActionSection.tsx` | — (never had a row) | `4 / 3 / 498` | ⚠ **FIRES — EXACTLY AT THRESHOLD** |
| `frontend/src/components/workflows/stepReadinessContext.ts` | — | `3 / 2 / 209` | no (2 phases) |
| `frontend/src/components/workflows/externalShapeVocabulary.ts` | — (created wave 2) | `2 / 1 / 109` | no (1 phase) |

⚠ **`ExternalActionSection.tsx` CROSSED THE THRESHOLD IN THIS VERY PLAN and has never had a
row** — the `FlowEdge.tsx` / `libraryRow.ts` / `ConnectionPicker.tsx` state, which is where a
missing row costs most: at 2 phases it was legitimately below threshold and nobody was obliged
to write it down; at 3 it fires, and a file with no row is permanently invisible to its own
guardrail at any count. Its phase buckets are `189 · 206 · 206.2`. **Honoured by construction
here:** it gained a SECOND AXIS, in its own group, with its own words in a leaf module — no
second copy of the capability mirror, and the shipped capability branch's DOM is asserted
byte-identical below the new control. **Seam to name when the row is written:** the shape
derivation plus the two clearing patches (~40 L) are the only DECISION in the file; everything
else is render.

⚠ **`PhaseFormPanel.tsx`'s row was stale by 5 commits / 2 phases / 186 lines BEFORE this plan
ran**, and this plan moved it again by `+2 / -1`. **Honoured by construction:** three JSX
attributes on one existing line, one key on an existing call, **no hook** — the ABSOLUTE-ZERO
pin passes and was independently re-checked with needles assembled at runtime
(`"use"+"State("` etc., each reading **0**) so this summary's own prose cannot redden it.

⚠ **`phaseVocabulary.ts` has never had a row at 6 phases** and is READ by this plan's blast
radius (it is the ONE home of `EXTERNAL_CAPABILITY_SENTENCES`, the map both languages' fences
compare). Named here so its absence is deliberate rather than invisible.

Test files are not ledger-tracked: `ExternalActionSection.test.tsx` measures `3 / 3 / 926`,
`StepPanelPort.test.tsx` `3 / 2 / 581`. Recorded so the absence is a decision.

## Threat Flags

None. No new network endpoint, auth path, file access pattern or schema change was introduced.
This plan's own register is discharged:

- **`T-206.2-03-T1`** (stranded `tool_name`) **mitigated** — press A clears `tool_name` and
  `tool_args` as a SET, asserted by set equality over the emitted patch's keys AND by a
  serialized-definition read. This is the SILENT direction and it has its own case.
- **`T-206.2-03-T2`** (stranded `connection_id`) **mitigated** — both presses clear it to
  `null`, asserted in both directions in one case (AR-05).
- **`T-206.2-03-E1`** (a fake capability) **mitigated** — the shape is a NEW type; the
  cross-language mirror fence and the client-map fences are unedited and green, and a
  glob-based case asserts `externalShapeVocabulary.ts` spells no capability id and imports
  nothing, with positive controls.
- **`T-206.2-03-I1`** **mitigated** — the shape patch carries only the four named keys,
  asserted by set equality. ⚠ `tool_args` is AUTHOR-TYPED CONTENT and is **not** covered by
  the picker's T6 promise; here it is only ever CLEARED, never written.
- **`T-206.2-03-R1`** **accepted, FLAGGED** — see the UAT table above.
- **`T-206.2-03-D1`** (a leaked session press) **mitigated** — `key={phase.slug}` remounts the
  section per step, and a case drives the remount and asserts the shape returns to what the
  props say.
- **`T-206.2-03-SC`** never fired — no package was installed, and both manifests measure 0.

## Self-Check: PASSED

- `frontend/src/components/workflows/externalShapeVocabulary.ts` — FOUND
- `frontend/src/components/workflows/ExternalActionSection.tsx` — FOUND
- `frontend/src/components/workflows/PhaseFormPanel.tsx` — FOUND
- `frontend/src/components/workflows/stepReadinessContext.ts` — FOUND
- `frontend/src/components/workflows/ExternalActionSection.test.tsx` — FOUND
- `frontend/src/components/workflows/StepPanelPort.test.tsx` — FOUND
- `scripts/vitest-count-gate.cjs` — FOUND
- commit `128dcde7` — FOUND
- commit `664ba306` — FOUND
- commit `8498b9ae` — FOUND
