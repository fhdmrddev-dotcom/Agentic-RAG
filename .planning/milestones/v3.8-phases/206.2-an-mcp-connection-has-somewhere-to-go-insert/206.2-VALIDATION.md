---
phase: 206.2
slug: an-mcp-connection-has-somewhere-to-go-insert
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-25
---

# Phase 206.2 — Validation Strategy

> Extracted from `206.2-RESEARCH.md` § Validation Architecture, which is its source of truth.
> ⚠ **One criterion is DELIBERATELY STRONGER here than in RESEARCH.md** — see SC#3b below. Research
> recommended not claiming publish; `D-206.2-19` overrode that and widened scope to fix it. Where the
> two disagree, **this file and CONTEXT.md win**, and the disagreement is written down rather than
> resolved silently.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Frontend** | vitest + `@testing-library/react`; `frontend/vitest.config.ts` |
| **Backend** | pytest; `backend/tests/unit` |
| **Typecheck** | `npx tsc --noEmit -p tsconfig.app.json` — **project-scoped or it measures nothing** |
| **Browser** | **Playwright + real Chromium** (`chromium-1223`). ⚠ **Chrome DevTools MCP is NOT available here** — measured, and the 206.1 executors hit the same wall. |
| **Quick run** | `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/` |
| **Full gate** | *(REPO ROOT)* `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` |

### Baselines, re-derived at this phase's base

| Gate | Base reading |
|---|---|
| backend unit | `68 failed, 2605 passed, 2 xfailed, 2 xpassed` |
| tsc `-p tsconfig.app.json` | **34** errors — **zero** in any file this phase touches |
| count gate verdict (verbatim) | `total 5605 · failed 0 · pinned total 5043` / `count gate OK — 111/111 pinned files present, no per-file decrease, 0 failing.` |
| CLAUDE.md size | `124,357 chars · 82.9% · headroom 25,643 · [WARN]` — **already past the 120,000 warn band** |

⚠ A **larger** grand total is the gate WORKING. Contract: **no per-file DECREASE, zero failing** —
never a fixed total.

---

## ⚠ Tier discipline — this phase exists because a green component test hid an unreachable feature

| Tier | CAN prove | CANNOT prove |
|---|---|---|
| **T1** component test, constructed props | the component's own contract | **that anything constructs those props. This is the tier that shipped the defect.** |
| **T2** source / import-graph fence | an import edge exists; exactly N importers | that the edge carries usable props. **Green at this defect's base** — see SC#4. |
| **T3** data-path integration (production parent, only `@/lib/api` mocked) | that the production path PRODUCES the props; that the read carries the right argument | that the server filters as expected; that a run calls the tool |
| **T4** driven browser round trip (Playwright + real backend + real MCP server) | the whole chain — grant enforcement, receipt, refusal | nothing repeatable; it is manual |

**Every criterion below names the tier that OWNS its proof.**

---

## Per-Criterion Verification Map

| SC | PROVES it | DISPROVES it | Tier | Auto? |
|----|-----------|--------------|------|-------|
| **SC#1** step points at an MCP connection; picker lists MCP rows | **T4:** `connection-picker-select` contains an `<option>` reading `DeepWiki (206.1 UAT)` after choosing the MCP shape, on a step the browser created | the list is empty, or `connection-picker-empty` renders | T4 + T3 | ⚠ T4 manual |
| SC#1a the MCP read is unfiltered | **T3:** `expect(listMock).toHaveBeenCalledWith()` — **zero arguments** | called with a capability string | T3 | ✅ |
| SC#1b the shapes don't list each other's rows | **T3:** with a `post_message` row AND an MCP row mocked — each shape lists exactly 1 option (+ `— none —`), and they differ | either list contains the other's row | T3 | ✅ |
| SC#1c a DISABLED MCP row is not listed | **T3:** `is_enabled: false` ⇒ `connection-picker-empty` | it appears as an option | T3 | ✅ |
| **SC#2** a grant is flipped through the UI; `updateConnectorGrants` gains its first production caller | **T4:** network shows `PATCH …/grants` whose body is the **FULL merged map**; the column afterwards reads `{"read_wiki_structure": true}`. **T2:** the caller census names ≥1 non-test, non-`api.ts` file | zero production callers remain; or the body carries only the toggled key (**the REPLACE lost-update**) | T4 + T2 | partly |
| SC#2b a denied tool is still refused, with its receipt | **T4:** grant OFF ⇒ `harness_audit` row `event_type='tool_refused'`, `metadata->>'connection_id'` = the MCP row, `reason='permission_denied'`; step text begins `Tool execution refused:` | no audit row, or it sends anyway | T4 | ❌ |
| SC#2c the control disturbs neither badge nor deny predicate | **T1:** the two shipped grant-label cases pass **UNEDITED** beside the new control | either had to be re-baselined | T1 | ✅ |
| SC#2d the non-admin path is honest | **T3:** a member sees the control **REMOVED** with one sentence in real DOM text; **no `title=`** (two suites fence it); a 403 renders a named reason | a silent failure, a tooltip, or a Discover button that 403s | T3 | ✅ |
| **SC#3** the run calls the granted tool and records a send receipt (**206.1's owed SC#1b**) | **T4:** `harness_audit` `external_action_sent` with `capability:"mcp"`, the MCP row's `connection_id`, `raw_status:200`; the step's output is the remote server's answer | the step lands `recorded_not_sent`, or `capability` is anything but `"mcp"` | T4 | ❌ — **the phase's headline** |
| **SC#3b** ⚠ **a workflow containing an MCP step PUBLISHES** | **T4/backend:** the publish gauntlet's stage 2.6 returns no `unregistered_tool` for an MCP step, and a real publish succeeds. **Backend:** both negative controls still block — (A) an `llm_agent` step carrying `available_tools:["send_email"]`, (B) an `external_action` step whose list disagrees with its own `tool_name` | either negative control stops blocking (**a loosening, not a fix**); or publish still refuses | T4 + pytest | ✅ for the controls |
| **SC#4** a REACHABILITY assertion, not another component test | **T2 (leg a):** the sweep names exactly `["ConnectionPicker.tsx"]`, **with a positive control and a non-vacuity check**. **T3 (leg b):** a test that constructs **NO** `McpToolPicker` prop (`grep -c "connection={" == 0`) and still reaches `mcp-tool-picker` through the production path | the sweep passes vacuously; or the only new test builds a `connection` prop | **T2 + T3** | ✅ |
| **SC#5** the three shipped capability shapes untouched | all five `external-action-option` `=== 3` sites pass **UNEDITED**; the cross-language fence passes unedited; a `send_email` step's picker render is byte-identical to a baseline **captured from the BASE commit's component** | any of the five moved; or the baseline was re-captured from new code | T1 | ✅ |
| **SC#6** baselines unchanged | backend `68 failed`; tsc `34`, zero in any touched file; gate `failed 0`, **no per-file decrease**, `McpToolPicker.test.tsx` now PINNED | a per-file decrease; a tsc error in a touched file | gate | ✅ |

⚠ **SC#4's leg (a) is GREEN AT THIS PHASE'S BASE** — `McpToolPicker` already has exactly one
production importer, so an inverted `≥1` sweep passes on the broken tree. **Leg (b) is the
load-bearing one**, and the plan requires observing it RED against the pre-phase tree before
trusting it.

⚠ **SC#3 is reachable via ▶ Test Run, a draft path with NO verdict gate.** A plan could therefore
score SC#3 green while publish stayed broken — which is precisely why SC#3b exists as a separate
criterion rather than being folded into SC#3.

---

## Sampling Rate

- **Per task commit:** `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/` + `npx tsc --noEmit -p tsconfig.app.json`
- **Per wave merge:** *(repo root)* `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs`
- **Phase gate:** all of SC#6, plus the T4 drive, before `/gsd:verify-work`

---

## Wave 0 Requirements

⚠ **`wave_0_complete: true` means there is no test-INFRASTRUCTURE gap** — no framework install is
needed; vitest, pytest, Playwright and Chromium are all present. **The unchecked boxes below are
ARTIFACTS the plans create as they run**, not preconditions blocking them.

- [ ] **PIN `McpToolPicker.test.tsx`** — it RUNS via a directory `TARGETS` entry but carries no per-file baseline, so its 12 cases are deletable with the gate green. ⚠ The script's own comment refuses a redundant `TARGETS` line as *"a dependency that is not real"* — the pin is a BASELINE entry only.
- [ ] A **T3 data-path integration** suite (a NEW file — widening `ConnectionPicker.test.tsx` would drag its T6 sweep, which is scoped to the interactions it drives)
- [ ] A **T2 reachability sweep** with a positive control
- [ ] Mock-budget edits: `McpToolPicker.test.tsx` (+`updateConnectorGrants`) and the **four** measured-incomplete `WorkflowBuilderPage.*` suites
- [ ] A **byte-identical capability-shape baseline** for SC#5 — captured from the BASE commit's component **before any edit**, never re-captured after
- [ ] **First-ever backend coverage** of `PATCH /grants` / `update_connection_grants`, including its REPLACE semantics (measured: zero today)
- [ ] ⚠ **The T4 browser tier** for SC#1, SC#2, SC#2b, SC#3 and SC#3b. The one gap no code closes.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Instructions |
|----------|-------------|------------|--------------|
| The bind → discover → grant → call round trip (SC#1/2/3) | CONN-02, CONN-03 | needs a live backend, a live remote MCP server and a real run | Playwright + real Chromium. ⚠ **The 206.1 `localStorage` trick does NOT transfer** — `activeView` is unpersisted `useState` with no router, so the rail must be CLICKED (`[aria-label="Workflows"]`). Fixture: MCP row `7ca5e114…`, `tool_grants = {}`, 3 cached tools. |
| The DENY case and its receipt (SC#2b) | CONN-03 | the refusal only exists at run time | Grant OFF, Test Run, then read `harness_audit`. ⚠ `tool_refused` is a **SHARED** `event_type` — filter on `connection_id`/`reason`, **never the name alone**. |
| A real publish of an MCP workflow (SC#3b) | CONN-02 | the gauntlet runs server-side against a real definition | Publish after wave 1 lands; before it, the measured block is `unregistered_tool` at stage 2.6. |

---

## Validation Sign-Off

- [x] Every task has an automated verify, or an explicit browser-tier reference
- [x] Sampling continuity: no window of 3 consecutive implementation tasks without an automated verify
- [x] Wave 0 covers all MISSING references — no `<automated>MISSING</automated>` anywhere
- [x] No watch-mode flags
- [x] `nyquist_compliant: true` — ⚠ this records that the STRATEGY is compliant. It does **not**
      assert the T4 evidence has been gathered; that is the open box above.
- [ ] ⚠ T4 evidence captured and recorded per step

**Approval:** approved 2026-08-25 — authored from `206.2-RESEARCH.md § Validation Architecture` after
`gsd-plan-checker` blocked on its absence (the artifact was owed and had been skipped; the content
already existed).
