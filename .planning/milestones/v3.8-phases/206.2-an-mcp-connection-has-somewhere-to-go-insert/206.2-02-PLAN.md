---
phase: 206.2-an-mcp-connection-has-somewhere-to-go-insert
plan: 02
type: execute
wave: 2
depends_on: ["206.2-01"]
files_modified:
  - frontend/src/components/workflows/externalShapeVocabulary.ts
  - frontend/src/components/workflows/ConnectionPicker.tsx
  - frontend/src/components/settings/connectionsCopy.ts
  - frontend/src/components/workflows/ConnectionPicker.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [CONN-02, CONN-03]
worktrees: forbidden
gap_closure: false

must_haves:
  truths:
    - "⚠ DECLARED DEVIATION FROM THE ORCHESTRATOR'S SUGGESTED SPLIT, AND THE REASON IS A CROSS-PLAN SEAM. The orchestrator suggested `206.2-02 = the shape choice` and `206.2-03 = the picker's MCP branch`. **The dependency runs the other way.** `ExternalActionSection`'s MCP arm mounts `<ConnectionPicker shape=\"mcp\" />`; if the shape choice landed first it would either reference a prop that does not exist (a typecheck failure at the wave boundary) or mount the capability-scoped read under a false name — a half-built seam of exactly the kind that reached the operator twice in Phase 204, each side green. **The consumed contract is built FIRST.** It costs nothing: `ConnectionPicker.test.tsx` already renders the picker standalone, so this plan is fully provable on its own. The shape choice is `206.2-03`, wave 3."
    - "D-206.2-03 — the MCP read is `listConnectorConnections()` with **NO ARGUMENT**. A CALL-SITE change, not an API change: `frontend/src/lib/api.ts:6469-6483` already builds an empty query string for a falsy argument. **`frontend/src/lib/api.ts` IS NOT EDITED** (Phase 207 owns it) and that is asserted with `git diff --numstat`, not assumed."
    - "D-206.2-03 — ⚠ THE `is_enabled` FILTER APPLIES TO THE MCP READ TOO, IDENTICALLY. *A DISABLED connection is not a choice.* An MCP row must obey the same rule rather than inheriting a laxer one."
    - "D-206.2-03 — ⚠ AN UNFILTERED READ RETURNS CAPABILITY ROWS AS WELL, so the MCP shape lists **only** rows carrying `mcp_server_url`, filtered client-side with the SAME predicate `McpToolPicker.tsx:131` uses — so the picker cannot offer a row the child will refuse to render. **The negative half is as binding as the positive half and is asserted in the SAME reading**: with one `post_message` row and one MCP row in the response, each shape lists exactly ONE option and they are DIFFERENT rows."
    - "D-206.2-08 — ⚠ THE CAPABILITY BRANCH IS BYTE-IDENTICAL, AND THAT IS AN ASSERTION, NOT AN INTENTION. The three capability rows' `connection-picker` `outerHTML` is captured FROM THE BASE COMMIT'S COMPONENT BEFORE ANY EDIT and never re-captured afterwards. Its filter chain (`rows.filter((row) => row.is_enabled)`) is asserted byte-identical in source. The capability shape gains NO `data-empty-reason` attribute — adding one would change the very bytes this criterion pins (AR-04)."
    - "D-206.2-18 — ⚠ THE BYTE-IDENTITY PIN NEEDS A **COUNTED** `useId` NORMALIZATION. `ConnectionPicker` calls `useId()` TWICE (`selectId`, `refusalId`) and both reach the DOM (`<Label htmlFor>` + `<select id>`; `<p id>` + `aria-describedby`), so a case added above moves the ids. React 19 emits the `«r0»` form — **NOT `radix-…`** — and the needle is READ OFF A REAL RENDER, never assumed. Expected replacement count **2** for a plain bound render and **4** when the refusal shows. **A normalization without a count is how a pin stops watching the thing it names.**"
    - "D-206.2-20 — ⚠ `destinationPartsOf` HAS AN UN-REPAIRED POSITIONAL TAIL AND THIS PHASE'S OWN BINDING FLOW DISPLAYS IT. Bind an MCP row today and the footer reads `🔒 slack.com/api` — byte-for-byte the defect 206.1 repaired one file over in `connectionsCopy.destinationFactsOf` (its verified SC#4). It survived here only because nothing could reach it. Repaired with an explicit `mcp_server_url` arm ABOVE the tail, an EXPLICIT `post_message` arm, a NEUTRAL `return []` tail, and a **synthetic-fifth-shape negative control** — the shape 206.1 used."
    - "D-206.2-20 — ⚠ `destinationPartsOf` (workflows) and `destinationFactsOf` (settings) are TWO SPELLINGS OF ONE RULE and this phase does **not** merge them. Recording the drift is the honest first step; merging crosses two component subtrees and is a refactor this phase was not scoped for. **BOTH files carry the note in the SAME COMMIT** — the correction-beside-the-original discipline — with a named re-open trigger: *the first phase whose `files_modified` names BOTH files, or a third surface needing the same footer.*"
    - "⚠ THE NINTH LIVE WR-04 SINK IS IN THIS FILE AND THE MCP SHAPE WIDENS WHAT REACHES IT. `noConnectionYetNote` reads `CONNECTION_CAPABILITY_WORDS[capability] ?? \"external\"` — a bare bracket read with a `??` fallback on a plain object literal, so `[\"constructor\"]` returns the `Object` FUNCTION, which is not nullish, so the fallback never fires and a function stringifies into the sentence. Converted to `own(CONNECTION_CAPABILITY_WORDS, capability) ?? \"external\"`. `own()` is a zero-import leaf; importing it is free, adding an import TO it is forbidden."
    - "⚠ THIS PLAN MAKES THE `McpToolPicker` MOUNT REACHABLE FOR THE FIRST TIME — and its own test case asserting that mount is **NOT** SC#4's evidence. That case hands `ConnectionPicker` a `shape` prop by hand, and *a test that constructs its own props cannot see that nothing constructs them in production* — the exact finding SEED-200 is about. The two-legged guard (D-206.2-07 / D-206.2-12) is `206.2-04`'s, because leg (b) must run through `ExternalActionSection` and construct NO component prop at all."
    - "D-206.2-09 — baselines RE-DERIVED at this plan's own base, never quoted: `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the REPO ROOT (verdict line read VERBATIM) and `npx tsc --noEmit -p tsconfig.app.json` (the bare `--noEmit` checks ZERO files). A LARGER gate total is the gate WORKING; the contract is **no per-file DECREASE and zero failing**. A red gate is triaged from its persisted JSON BEFORE any re-run (SEED-171) — and a red gate is sometimes REAL."
  artifacts:
    - path: "frontend/src/components/workflows/externalShapeVocabulary.ts"
      provides: "the `ExternalActionShape` union — the ONE contract both `ConnectionPicker` (this plan) and `ExternalActionSection` (206.2-03) consume. A TRUE leaf: it imports nothing."
      exports: ["ExternalActionShape"]
    - path: "frontend/src/components/workflows/ConnectionPicker.tsx"
      provides: "the `shape` prop, the unfiltered MCP read, both filters, the shape term in `requestKey`, the two MCP empty sentences with `data-empty-reason`, the `own()` conversion, and the repaired `destinationPartsOf`"
      exports: ["CONNECTION_PICKER_NO_MCP_NOTE", "CONNECTION_PICKER_MCP_ALL_DISABLED", "destinationPartsOf", "noConnectionYetNote"]
    - path: "frontend/src/components/settings/connectionsCopy.ts"
      provides: "the two-spellings-of-one-rule drift note, landed in the SAME COMMIT as its twin"
    - path: "frontend/src/components/workflows/ConnectionPicker.test.tsx"
      provides: "the capability branch's byte-identity capture, the counted `useId` normalization, the MCP state ladder, the cross-shape exclusion pair, and the synthetic-fifth-shape footer control"
  key_links:
    - from: "frontend/src/components/workflows/ConnectionPicker.tsx"
      to: "frontend/src/lib/api.ts (listConnectorConnections)"
      via: "the MCP branch calls it with NO argument; the capability branch is unchanged"
      pattern: "listConnectorConnections\\(\\)"
    - from: "frontend/src/components/workflows/ConnectionPicker.tsx"
      to: "frontend/src/components/workflows/McpToolPicker.tsx"
      via: "`bound?.mcp_server_url && <McpToolPicker connection={bound} …/>` — a mount that was dead at this plan's base and is live at its close"
      pattern: "McpToolPicker"
---

<objective>
Give the workflow builder's `ConnectionPicker` a second shape. Today `ConnectionPicker.tsx:271`
unconditionally calls `listConnectorConnections(capability)`; an MCP row has `capability = null`, so
it is filtered out of every read the picker ever performs, `bound?.mcp_server_url` is unsatisfiable,
and the `McpToolPicker` mount at `:425` is dead. **That single line is what SEED-200 is about.**

**Purpose:** this is the CONSUMED CONTRACT for wave 3's shape control. Building it first is the
interface-first rule and it removes the only cross-plan seam in this phase.

**It also repairs a live lie.** `destinationPartsOf`'s trailing positional `return` means an MCP row's
footer reads `🔒 slack.com/api` — on the one surface whose entire job is answering *where does this
send?*. It is byte-for-byte the defect 206.1 repaired one file over, and it survived here only
because nothing could reach it. **This plan is what makes it reachable, so this plan fixes it.**

**Output:** a shape-aware picker whose capability branch is proved byte-identical, an MCP branch with
its own three-fact empty vocabulary, a repaired destination footer with a synthetic-fifth-shape
control, and the ninth WR-04 sink in this tree closed.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/206.2-an-mcp-connection-has-somewhere-to-go-insert/206.2-CONTEXT.md
@.planning/phases/206.2-an-mcp-connection-has-somewhere-to-go-insert/206.2-UI-SPEC.md
@.planning/phases/206.2-an-mcp-connection-has-somewhere-to-go-insert/206.2-PATTERNS.md
@.planning/phases/206.2-an-mcp-connection-has-somewhere-to-go-insert/206.2-RESEARCH.md
@.planning/seeds/SEED-200-mcp-connection-cannot-be-bound-to-any-step.md
@.planning/phases/206.2-an-mcp-connection-has-somewhere-to-go-insert/206.2-01-SUMMARY.md
</context>

<interfaces>
<!-- Read at HEAD by the research + pattern passes and re-read by this planner. No exploration needed. -->

**`ConnectionPickerProps`** — `ConnectionPicker.tsx:191-195`: exactly one member, `capability: string`,
documented as *"already narrowed to a recognised one by the section"*.

**`ReadState`** — `:186-189`: `{kind:"loading";key} | {kind:"ready";key;connections} | {kind:"error";key}`.
Every settled read carries the KEY it answered, so a stale result reads `loading` **by derivation**,
with no synchronous `setState` in the effect body.

**`requestKey`** — `:202`: `` `${slug ?? ""}::${capability}` ``. Internal only; it reaches no DOM node
(verified — `grep -n "requestKey" ConnectionPicker.tsx` shows comparisons and state keys only).

**The read** — `:266-282`. Guarded on `store === null || slug === null` so a provider-less render
opens NO request; then `listConnectorConnections(capability).then(rows => setSettled({kind:"ready",
key:requestKey, connections: rows.filter((row) => row.is_enabled)}))` with a `.catch` to
`{kind:"error"}` and a `cancelled` latch in the cleanup.

**The seven states**, each with its own `data-state` AND `data-testid` — `:316-433`:
`disconnected` / `loading` (`connection-picker-loading`) / `error` (`connection-picker-error`,
`role="alert"`) / `empty` (`connection-picker-empty`) / `unbound` / `bound` / `bound-failing`.
Their charter, `:84-88`: *"Populated, empty, loading and error are four different facts, and a read
failure that renders as 'none exist' would tell an author to create something they already have."*

**The defect** — `:151-170`, `destinationPartsOf`. Two explicit arms (`send_email`, `create_ticket`)
then a **positional tail** that returns `[SLACK_FIXED_DESTINATION, …]`. `capability === null` lands
there.

**The repaired twin, to PORT rather than reinvent** — `frontend/src/components/settings/connectionsCopy.ts:205-233`.
Its MCP arm identifies itself by `connection.mcp_server_url`, renders **HOST ONLY** via
`url.replace(/^https?:\/\//, "").split("/")[0]` with a fallback to the raw string (*"a malformed value
must still render SOMETHING true rather than throw"*), and its tail is a NEUTRAL `return []`. Its
docblock names the original defect and the live UAT reading that found it.

**The WR-04 sink** — `:112-113`, `noConnectionYetNote`, and its table `CONNECTION_CAPABILITY_WORDS`
at `:103-107`. **`own()`** is `frontend/src/components/workflows/ownProperty.ts:82-86` — a zero-import
leaf; `stepCardSectionContext.ts:87-89` is the shipped call site to copy.

**The mount that is dead at this plan's base** — `:425-433`,
`{bound?.mcp_server_url && <McpToolPicker connection={bound} toolName={toolName} toolArgs={toolArgs}
onSelectTool={handleSelectTool} onChangeArgs={handleChangeArgs} />}`, against
`McpToolPicker.tsx:131-133`'s `if (!connection?.mcp_server_url) return null`.

**The suite's harness** — `ConnectionPicker.test.tsx:110-149`: `draftedStore(config)` (already
`builderPhase === "drafted"`, which `patchConfig` requires), `renderPicker(opts)` returning
`{store, patch, unmount, container}`, and a hoisted three-key `vi.mock("@/lib/api")` carrying
`listConnectorConnections`, `discoverConnectorTools` and `updateConnectorGrants` — **so this suite can
render the real `McpToolPicker` without a new mock.** `beforeEach` resets `listMock` only.

**The pins that constrain every edit here** — `ConnectionPicker.test.tsx`:
`:161-176` the disconnected render (`listMock` NOT called);
`:187-204` `noConnectionYetNote` character-identity for all three capability words;
`:217-253` the footer character-identity, including `slack.com/api`;
`:289-298` a DISABLED connection is not listed;
`:301-338` Gate 1 (`aria-disabled`, writes nothing, positive control, unbind writes `{connection_id: null}`);
`:342-372` **T6** — `expect(Object.keys(patch)).toEqual(["connection_id"])` over EVERY driven call,
`toHaveBeenCalledTimes(3)`, with an in-line positive control;
`:382-405` one `<select>` with a real associated `<label>`, refusal wired by `aria-describedby`;
`:407-429` source discipline: no `title=`, and **copy rule 5** — no absolute verb for a guarantee only
this client gate provides (no `always`, `never fails`, `guaranteed`, `secure`, `safe`).

**The byte-identity idiom to copy** — `frontend/src/components/settings/__tests__/ConnectionsTab.test.tsx:872-1075`:
(a) the COUNTED normalization and its *"normalizing without counting is how a pin stops watching the
thing it names"* docblock; (b) `wideCapture()` — ONE render shared by capture and assertion, then
`unmount()`; (c) the capture's status banner (*"THESE LITERALS ARE A CAPTURE, NOT AN EXPECTATION …
A DIFF AGAINST THIS RECORD IS A BEHAVIOUR CHANGE … and NOT A TEST TO UPDATE"*); (d) the declared-delta
helper (`not.toContain` + single-anchor + one `replace`); (e) the MARKER rows that say WHAT each
capture contains, read from the COMMITTED baseline strings and never from a fresh render.

**The gate's two knobs** — `scripts/vitest-count-gate.cjs`: `TARGETS` (`:3009-3011`) already carries
the DIRECTORY `src/components/workflows`, so everything in it RUNS; `BASELINE` (`:1619`)
`"ConnectionPicker.test.tsx": 20` is what makes it GUARDED. The script's own comment at `:1596-1603`
refuses a redundant file-level `TARGETS` entry beside a directory that already covers it.

**READ, NEVER EDIT:** `frontend/src/lib/api.ts` (Phase 207). `listConnectorConnections(capability?: string)`
at `:6469-6483` already does `const qs = capability ? \`?capability=…\` : ""`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `externalShapeVocabulary.ts` (the type) + `ConnectionPicker`'s shape prop, the unfiltered MCP read, both filters, and the three-fact empty vocabulary</name>
  <files>frontend/src/components/workflows/externalShapeVocabulary.ts, frontend/src/components/workflows/ConnectionPicker.tsx</files>

  <read_first>
    - `frontend/src/components/workflows/ConnectionPicker.tsx` **in full** (439 L). Especially
      `:1-53` (the docblock, including ⭐ *"WHY THIS IS A CHILD AND NOT THREE MORE PROPS ON THE
      SECTION"* at `:5-16` and the degrade-never-throw charter at `:42-47`), `:60-146` (every exported
      string — the Pattern-2 rule that **every user-visible string is an exported identifier**),
      `:103-113` (⚠ the WR-04 sink), `:186-204` (`ReadState`, props, `requestKey`), `:266-282` (the
      read), `:316-433` (the seven states)
    - `frontend/src/components/workflows/McpToolPicker.tsx:131-133` — the guard whose predicate the
      new filter must MATCH exactly
    - `frontend/src/components/workflows/ownProperty.ts:9-16` (⚠ its zero-import contract) and
      `:58-86` (⭐ the inherited-key argument), plus the shipped call site
      `frontend/src/components/workflows/stepCardSectionContext.ts:87-89`
    - `frontend/src/components/workflows/doorVocabulary.ts` and
      `frontend/src/components/workflows/receiptVocabulary.ts` — ⭐ the leaf-vocabulary house rules:
      a true leaf imports nothing, exports no component, **declares every scalar ABOVE any array or
      object that reads it** (206.1 measured a temporal-dead-zone `ReferenceError` at module load
      *"which no type checker reports"*), and **spells no forbidden token in its own prose**
    - `frontend/src/lib/api.ts:6440-6500` — `ConnectorConnection` and `listConnectorConnections`.
      **READ ONLY.**
    - `.planning/phases/206.2-an-mcp-connection-has-somewhere-to-go-insert/206.2-UI-SPEC.md` §
      "Surface 2" and § "Copywriting Contract"
  </read_first>

  <behavior>
    - `shape` defaults to `"capability"`, so every shipped call site and every shipped test renders
      EXACTLY as it does today with no edit.
    - Capability shape: `listConnectorConnections(capability)` — argument unchanged; filter chain
      unchanged; DOM unchanged; **no `data-empty-reason` attribute**.
    - MCP shape: `listConnectorConnections()` called with **zero arguments** —
      `expect(listMock).toHaveBeenCalledWith()`. This single assertion is the line SEED-200 is about.
    - MCP shape lists ONLY rows with a truthy `mcp_server_url`, and ONLY those that are `is_enabled`.
    - **Three absences stay three sentences**: no MCP row at all → `CONNECTION_PICKER_NO_MCP_NOTE`;
      MCP rows exist but every one is disabled → `CONNECTION_PICKER_MCP_ALL_DISABLED`; the read failed
      → the shipped `CONNECTION_PICKER_READ_FAILED`, unchanged. Folding the first two is the defect
      this tree has now recorded five times (`runFacts.ts` CR-01, `DecisionsList` D-20,
      `transcriptVocabulary.ts`, 206.1's AR-05).
    - A shape switch makes a settled answer stale **by derivation**, not by an effect — the shipped
      `requestKey` rule, extended by one term.
    - Provider-less render still opens NO request, in EITHER shape.
  </behavior>

  <action>
    Create `frontend/src/components/workflows/externalShapeVocabulary.ts` exporting the union type
    `ExternalActionShape = "capability" | "mcp"` and nothing else yet. Its docblock states: (a) why
    the extra member is a NEW type rather than a fourth key in `EXTERNAL_CAPABILITY_SENTENCES` —
    citing that the closed set is mirrored by a server `Literal`, a migration-116 `CHECK`, a
    module-scope `assert` in `backend/app/models/connector.py` that fails at IMPORT, and an adapter
    registry assert, so a fourth key is not a fence to update but four separate failures; (b) that
    this module is a TRUE LEAF and imports nothing; (c) the DECLARATION-ORDER rule for the string
    constants `206.2-03` will add here, naming the temporal-dead-zone `ReferenceError` 206.1
    measured; (d) that it exports **no component**, because a runtime const beside a component is a
    `react-refresh/only-export-components` error — the recorded reason
    `EXTERNAL_ACTION_CAPABILITIES` is not exported from `ExternalActionSection.tsx`.
    ⚠ Do NOT spell any capability id in this file's prose or code.

    Widen `ConnectionPickerProps`: make `capability` optional (`capability?: string`) and add
    `shape?: ExternalActionShape` defaulting to `"capability"` at the destructure. Document on the
    props that the two questions are ORDERED — the section answers the shape first, then the
    capability — and that the default exists so no shipped call site moves.

    Extend `requestKey` with the shape term. Keep the capability term. It reaches no DOM node, so
    this changes nothing observable — assert that with the byte-identity pin in task 3 rather than
    stating it.

    Branch the read on the shape: MCP calls `listConnectorConnections()` with NO argument; capability
    calls it exactly as today. ⚠ **The capability branch's `.then` body must remain
    BYTE-IDENTICAL** — do not "unify" the two filter chains into one expression that both share,
    because that rewrites the line the byte-identity criterion protects. Two arms, one of them
    untouched.

    For the MCP arm, derive in this order: filter by `Boolean(row.mcp_server_url)` FIRST, remember
    how many rows that produced, THEN filter by `is_enabled`. **The order is load-bearing** — the
    "all disabled" fact is about MCP-shaped rows specifically, and counting before the shape filter
    would report "all disabled" when the only disabled row was a Slack connection. Carry the count
    (or a derived reason) on the `ready` state; a new field on `ReadState` reaches no DOM.

    Add the two new exported sentences beside the shipped ones, verbatim from the UI-SPEC contract
    table: `CONNECTION_PICKER_NO_MCP_NOTE` = `No MCP server connection yet — add one in Settings →
    Connections.` and `CONNECTION_PICKER_MCP_ALL_DISABLED` = `Every MCP server connection is switched
    off. Turn one on in Settings → Connections.` **TEXT ONLY, NO LINK** — there is no router on this
    surface and a leaf inside the Builder cannot switch `ActiveView`; naming the destination is
    honest, a dead link is not. ⚠ Do NOT reuse `noConnectionYetNote` for the MCP shape: it prints
    *"No external connection yet"*, which is `[PATTERNS]`'s named anti-pattern.
    ⚠ Neither sentence may use an absolute verb for a guarantee only this client gate provides —
    copy rule 5 is swept over this file's own source at `ConnectionPicker.test.tsx:407-429`.

    In the empty state, render `data-empty-reason` ONLY on the MCP shape (`"none"` / `"all-disabled"`).
    ⚠ The element, its `data-testid` and its `data-state` do not change; the fifth arm is a SECOND
    SENTENCE INSIDE THE SAME NODE plus one attribute. Adding the attribute to the capability shape
    would change the very bytes D-206.2-08 pins (AR-04) — the asymmetry is DELIBERATE and is recorded
    at the site with its re-open trigger: *the first phase permitted to re-baseline the capability
    shape's empty render, at which point the same arm is owed there.*

    Convert `noConnectionYetNote`'s bracket read to `own(CONNECTION_CAPABILITY_WORDS, capability) ??
    "external"`, importing `own` from `./ownProperty`. Note at the site that this is the ninth live
    instance of a class the tree has closed eight times, and that the MCP shape is what widened what
    reaches this file. ⚠ The rendered OUTPUT for all three capability words must be unchanged — three
    character-identity assertions already pin it.

    ⚠ **DO NOT EDIT `frontend/src/lib/api.ts`.** No new export is needed: the no-argument path already
    ships. A task that believes otherwise has mis-read the file.
  </action>

  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/ConnectionPicker.test.tsx 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `git diff --numstat -- frontend/src/lib/api.ts | wc -l` == `0`. Record the command and its
      output verbatim.
    - `grep -c "listConnectorConnections()" frontend/src/components/workflows/ConnectionPicker.tsx`
      >= 1 (the no-argument call exists) and
      `grep -c "listConnectorConnections(capability)" frontend/src/components/workflows/ConnectionPicker.tsx`
      == 1 (the shipped call is still there, once).
    - The capability arm's filter line is byte-identical to base:
      `git show <base>:frontend/src/components/workflows/ConnectionPicker.tsx | grep -n 'rows.filter((row) => row.is_enabled)'`
      and the same grep on the working tree return the SAME text. Record both.
    - `grep -c "data-empty-reason" frontend/src/components/workflows/ConnectionPicker.tsx` >= 1, and
      the attribute appears only inside the MCP arm — assert by rendering the capability shape with
      zero rows and checking `screen.getByTestId("connection-picker-empty")` has NO
      `data-empty-reason` attribute, with a non-vacuity control that the MCP shape's node HAS one.
    - `grep -n "own(CONNECTION_CAPABILITY_WORDS" frontend/src/components/workflows/ConnectionPicker.tsx`
      resolves, and `grep -c "CONNECTION_CAPABILITY_WORDS\[" frontend/src/components/workflows/ConnectionPicker.tsx`
      == `0`.
    - `frontend/src/components/workflows/externalShapeVocabulary.ts` contains **zero** import
      statements (`grep -c '^import' ` == 0) and zero occurrences of any of the three capability ids
      (`grep -cE 'send_email|create_ticket|post_message'` == 0).
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — error count equals the figure
      re-derived at this plan's base, with **zero** errors in any `ConnectionPicker*`,
      `externalShapeVocabulary*` or `McpToolPicker*` file. ⚠ The project flag is load-bearing; bare
      `npx tsc --noEmit` checks ZERO files.
    - Every shipped case in `ConnectionPicker.test.tsx` passes with `git diff -- frontend/src/components/workflows/ConnectionPicker.test.tsx`
      **empty for this task** — the props default is what makes that true, and it is asserted rather
      than hoped.
  </acceptance_criteria>

  <done>
    The picker has two shapes. The MCP shape reads unfiltered and filters client-side by
    `mcp_server_url` and `is_enabled`; three absences render three sentences; the capability branch's
    source and DOM are unchanged; the WR-04 sink is closed; `api.ts` is untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repair `destinationPartsOf`'s positional tail — explicit MCP arm, explicit Slack arm, neutral tail — and record the two-spellings drift in BOTH files, same commit</name>
  <files>frontend/src/components/workflows/ConnectionPicker.tsx, frontend/src/components/settings/connectionsCopy.ts</files>

  <read_first>
    - `frontend/src/components/workflows/ConnectionPicker.tsx:143-170` — `SLACK_FIXED_DESTINATION` and
      `destinationPartsOf` in full, including its *"deliberately total over the union"* docblock
    - ⭐ `frontend/src/components/settings/connectionsCopy.ts:183-240` — the REPAIRED twin. Read its
      MCP arm, its host-only derivation, its *"a malformed value must still render SOMETHING true
      rather than throw"* comment, its neutral tail, and the UAT reading recorded in its prose. **Port
      the shape; do not reinvent it.**
    - `frontend/src/components/workflows/ConnectionPicker.test.tsx:217-253` — the three footer
      character-identity assertions, including the `slack.com/api` one. **They must pass UNEDITED.**
    - `.planning/phases/206.1-settings-connections-finishes-the-mcp-story-insert/206.1-03-SUMMARY.md`
      — how 206.1 proved its four repaired ladders with synthetic-fifth-shape controls
    - `.planning/phases/206.2-an-mcp-connection-has-somewhere-to-go-insert/206.2-UI-SPEC.md`
      § "⚠ THE FOOTER"
  </read_first>

  <behavior>
    - An MCP row's footer renders `🔒 <host>` — HOST ONLY. For the live fixture that is
      `🔒 mcp.deepwiki.com`, not `mcp.deepwiki.com/mcp` and never `slack.com/api`.
    - A malformed `mcp_server_url` renders the raw string rather than throwing.
    - A `post_message` row's footer is UNCHANGED, character for character, and it now arrives through
      an arm that NAMES its own condition.
    - **The synthetic fifth shape**: a row that is none of `send_email` / `create_ticket` /
      `post_message` / MCP renders an EMPTY destination list — never Slack's host. This is the control
      that proves the tail became neutral rather than merely reordered.
    - The `send_email` and `create_ticket` arms are byte-identical.
  </behavior>

  <action>
    Give `destinationPartsOf` an explicit `if (connection.mcp_server_url)` arm placed ABOVE the
    existing capability arms — above, because an MCP row has `capability === null` and the shape is
    resolved by the URL, never by a missing capability. Derive the host with the same
    `replace(/^https?:\/\//, "").split("/")[0]` form the settings twin uses, falling back to the raw
    string, and return it as the single part. ⚠ No `URL` parser: a malformed value must still render
    something true rather than throw, and the raw string is the truest thing available when it cannot
    be split.

    Turn the positional tail into an explicit `if (connection.capability === "post_message")` arm
    carrying the existing Slack body verbatim, and make the new tail a NEUTRAL `return []`. State at
    the tail, in a comment, that a fifth shape must get an empty destination list rather than silently
    inheriting Slack's host, and that this is the defect the arm above it was repaired from.

    Write the correction beside the original: keep `destinationPartsOf`'s existing docblock and append
    a `⚠ REPAIRED 2026-08-25 (Phase 206.2 / D-206.2-20)` paragraph recording that the tail was
    POSITIONAL, that every MCP row therefore claimed `slack.com/api`, that this is byte-for-byte the
    defect `connectionsCopy.destinationFactsOf` shipped and 206.1 repaired as its own SC#4, and that
    it survived HERE only because nothing in production could produce an MCP row for this component.

    Add the two-spellings note to BOTH files in the SAME COMMIT: in `ConnectionPicker.tsx` at
    `destinationPartsOf`, and in `connectionsCopy.ts` at `destinationFactsOf`. Each note says that the
    two functions are two spellings of ONE rule across two component subtrees, that this phase
    deliberately does NOT merge them because that is a refactor it was not scoped for, and it carries
    the re-open trigger verbatim: *the first phase whose `files_modified` names BOTH files, or a third
    surface needing the same footer.* ⚠ A note in only one file is the drift the same-commit rule
    exists to forbid.

    ⚠ Do NOT change `SLACK_FIXED_DESTINATION`'s value or its docblock — the server's constant in
    `backend/app/security/egress.py` is the authority and this string is a reading of it.
    ⚠ Do NOT touch `connectionsCopy.destinationFactsOf`'s BODY. Only its prose gains the note; the
    settings surface is 206.1's and is verified.
  </action>

  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/ConnectionPicker.test.tsx src/components/settings/__tests__/ConnectionsTab.test.tsx 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `git diff -U0 -- frontend/src/components/settings/connectionsCopy.ts | grep '^-[^-]' | wc -l`
      == `0` — the settings twin gains prose and loses nothing.
    - Both files carry the drift note in this task's single commit:
      `git show --stat HEAD` names both, and
      `grep -c "two spellings of ONE rule" -i frontend/src/components/workflows/ConnectionPicker.tsx frontend/src/components/settings/connectionsCopy.ts`
      returns `1` for each.
    - `grep -c 'connection.capability === "post_message"' frontend/src/components/workflows/ConnectionPicker.tsx`
      == 1, and the function's final statement is `return []` — assert by reading the last three lines
      of the function body.
    - The three shipped footer character-identity cases (`ConnectionPicker.test.tsx:217-253`) pass
      with an EMPTY diff for those line ranges.
    - The synthetic-fifth-shape control (added in task 3) is named in this task's commit message as
      the control that will prove the tail.
  </acceptance_criteria>

  <done>
    Every arm of `destinationPartsOf` names its own condition, the tail is neutral, the repair's
    argument stands beside the original docblock, and both spellings of the rule carry the same drift
    note from the same commit.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The picker's proofs — the capability branch's byte-identity capture with a COUNTED `useId` normalization, the MCP state ladder, the cross-shape exclusion pair, the fifth-shape control, and the gate pin</name>
  <files>frontend/src/components/workflows/ConnectionPicker.test.tsx, scripts/vitest-count-gate.cjs</files>

  <read_first>
    - `frontend/src/components/workflows/ConnectionPicker.test.tsx` **in full** — the header's five
      named threats, `:26-38` (the hoisted three-key api mock — ⚠ **it already carries
      `discoverConnectorTools` and `updateConnectorGrants`, so the real `McpToolPicker` renders with
      no new mock**), `:63-108` (the three capability fixtures `mailbox` / JIRA / SLACK),
      `:110-149` (`draftedStore` + `renderPicker`, which RETURNS `container`), and every pin listed in
      this plan's `<interfaces>`
    - ⭐ `frontend/src/components/settings/__tests__/ConnectionsTab.test.tsx:872-1075` — the whole
      byte-identity idiom: the counted normalization and its *"normalizing without counting"*
      docblock, `wideCapture`'s ONE-render-shared-by-capture-and-assertion rule, the capture's status
      banner, the declared-delta helper, and the MARKER rows
    - `frontend/src/components/workflows/RunReceipt.test.tsx:249-287` — the non-vacuity-before-contents
      discipline this suite's new cases inherit
    - `scripts/vitest-count-gate.cjs:1596-1625` — ⭐ the two-knob rule, and the shipped refusal of a
      redundant file-level `TARGETS` entry beside a directory that already covers the file
    - `.planning/phases/206.2-an-mcp-connection-has-somewhere-to-go-insert/206.2-CONTEXT.md` D-206.2-18
  </read_first>

  <behavior>
    - **The byte-identity pin.** For the three capability fixtures, across `unbound` / `bound` /
      `bound-failing` / `empty`, the `connection-picker` node's `outerHTML` equals a literal captured
      FROM THE BASE COMMIT'S COMPONENT. ⚠ The capture is taken by rendering the BASE commit's
      component **before any edit from tasks 1-2 is applied**, and it is OBSERVED TWICE on the
      unchanged tree with the two runs agreeing byte for byte. **It is never re-captured afterwards.**
    - **The counted normalization.** React's `useId` values are a function of how many components
      rendered before them, so an unrelated case added above must not redden this pin. The needle is
      READ OFF A REAL RENDER (React 19 emits the `«r0»` form, **not** `radix-…`) and the substitution
      COUNT is returned and asserted: **2** for a plain bound render, **4** when the refusal shows. A
      render that stopped emitting an id yields a lower count and goes RED rather than quietly passing
      against a shorter string.
    - **Non-vacuity FIRST.** Each captured literal's `.length` is asserted `> 0` before it is compared.
      Byte-identity against an empty capture passes forever.
    - **MARKER rows.** Say WHAT each capture contains — the label text, the `— none —` option, the
      footer's `🔒` — read from the COMMITTED baseline strings, never from a fresh render.
    - **The MCP read's argument:** `expect(listMock).toHaveBeenCalledWith()` with ZERO arguments in
      the MCP shape, and `toHaveBeenCalledWith("send_email")` in the capability shape, in the same
      file.
    - **The cross-shape exclusion pair, in ONE reading:** with a `post_message` row and an MCP row in
      the mocked response, the MCP shape renders exactly one `connection-picker-option` and it is the
      MCP row; the capability shape renders exactly one and it is the other row.
    - **The disabled filter, in the MCP shape:** a single MCP row with `is_enabled: false` yields
      `connection-picker-empty` with `data-empty-reason="all-disabled"`, and **zero** MCP rows yields
      `data-empty-reason="none"` — two different sentences, asserted by character identity against the
      two exported constants.
    - **The synthetic fifth shape:** a row with `capability: "wire_transfer"` (none of the four
      shapes, no `mcp_server_url`) renders a footer with NO destination parts and, specifically,
      NOT `slack.com/api`. Assert the absence of the Slack constant by name.
    - **The MCP footer:** a bound MCP row's footer contains the host and NOT the path segment.
    - **The mount becomes reachable:** binding an MCP row through the picker's own `<select>` renders
      `mcp-tool-picker`. ⚠ This case is labelled IN THE SOURCE as **not SC#4's evidence** — it hands
      the component a `shape` prop by hand, and *a test that constructs its own props cannot see that
      nothing constructs them in production.*
    - **The provider-less render, in BOTH shapes,** still opens no request.
  </behavior>

  <action>
    Add every new case BELOW every shipped case, under a banner reading `⚠ ADDED, NEVER RE-BASELINED —
    every case above this banner is untouched by 206.2`, and carry the ConnectionsTab capture-status
    paragraph verbatim in spirit: these literals are a CAPTURE, not an EXPECTATION; a diff against
    this record is a behaviour change in the picker and NOT a test to update.

    ⚠ **CAPTURE BEFORE YOU EDIT.** The first action of this task is to produce the baseline literals
    from the BASE commit's component — check the base file out to a scratch path outside the watched
    tree, render it through the same harness, print the normalized `outerHTML`, run it twice and
    confirm the two runs agree, then paste the literals in. If tasks 1-2 are already committed, obtain
    the base component with `git show <base>:frontend/src/components/workflows/ConnectionPicker.tsx`
    rather than by reasoning about what it used to render. **Do not type a literal by hand and do not
    compute one.**

    Build the normalization by first rendering ONE bound capability row and printing the raw
    `outerHTML`, then reading the actual id shape out of that output. Write the regex against what you
    read. Return `{html, replaced}` and assert `replaced` per case. If the id form your local React
    emits is not `«r0»`, record the real one in the docblock — the point is a measurement, not an
    inherited assumption.

    If tasks 1-2 introduced any attribute on the capability branch's root (they should not — verify
    with the diff), use the DECLARED DELTA route: `not.toContain` the new term against the capture,
    assert its anchor is SINGULAR, and apply one `replace`. **Do not re-capture.**

    Write the MCP cases, the cross-shape pair, the two empty reasons, the fifth-shape control, the MCP
    footer case and the reachability-caveat case. Every fence gets a POSITIVE CONTROL in the same
    file, and every glob/`?raw` read asserts non-vacuity before contents.

    Raise `"ConnectionPicker.test.tsx"` in `scripts/vitest-count-gate.cjs`'s `BASELINE` to the number
    read out of **the gate's own `actual` column**, not counted by hand. Write the pin's comment in the
    house style: what the new cases pin, and the measured reason no `TARGETS` edit accompanies it
    (`src/components/workflows` is already a DIRECTORY entry, so the file RUNS; the script's own
    `:1596-1603` comment refuses a redundant entry that would state a dependency that is not real).
    ⚠ Raising a pin necessarily deletes one line, so a `grep -c '^-[^-]'` expecting 0 on this file is
    wrong; expect exactly the pin line.

    ⚠ Do NOT "fix" `ExternalActionSection.test.tsx`'s declared `+9` pin drift (25 pinned / 34 actual).
    Ten consecutive plans have declined it for the same reason and this is not the phase for it.
  </action>

  <verify>
    <automated>GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs 2>&1 | tail -25</automated>
  </verify>

  <acceptance_criteria>
    - The gate's verdict line is read **VERBATIM** from the repo root and recorded, together with the
      line re-derived at this plan's base. Contract: **`failed 0` and NO per-file DECREASE.** A larger
      grand total is the gate WORKING.
    - `"ConnectionPicker.test.tsx"` in `BASELINE` equals the gate's own `actual` reading for that file,
      and the SUMMARY records where the number was read from.
    - The byte-identity block asserts, for each of the twelve (3 fixtures × 4 states) captures:
      `.length > 0` FIRST, then the normalized equality, then the replacement count.
    - The replacement counts asserted are the ones MEASURED on a real render, and the docblock names
      the id form actually observed.
    - `expect(listMock).toHaveBeenCalledWith()` appears in the MCP case with **zero** arguments, and
      `toHaveBeenCalledWith("send_email")` in the capability case.
    - The fifth-shape control asserts `expect(footerText).not.toContain(SLACK_FIXED_DESTINATION)`
      **and** that the destination list is empty — both halves, because "not Slack" alone would pass
      on a footer that named some other wrong host.
    - The reachability-caveat case's source contains the sentence naming it as NOT SC#4's evidence:
      `grep -c "not SC#4" frontend/src/components/workflows/ConnectionPicker.test.tsx` >= 1.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — count equals this plan's re-derived
      base with zero errors in any touched file.
    - ⚠ If the gate comes back RED: capture the failing filenames from the gate's own PERSISTED JSON
      report **before re-running anything**, check each against `git diff --numstat` and
      `git status --short`, and only then decide. A byte-unchanged file among SEED-171's five named
      flaky suites is recorded as an observation; anything else is a real defect. **Do not reach for
      the worker cap** — adjusting it is measured NOT to fix these.
  </acceptance_criteria>

  <done>
    The capability branch is proved byte-identical against a capture taken from the base commit and
    never re-captured. The MCP branch's read argument, both filters, all three absences, the repaired
    footer and the newly-live `McpToolPicker` mount are all asserted, each with a control. The suite
    is pinned at the gate's own reading.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `GET /connectors/connections` | an ungated org-wide read; the response is untrusted display data |
| remote MCP server → `discovered_tools` → the browser | remote-authored text rendered as UI |
| the picker → `workflow_definitions.definition` (org-readable JSONB) | the T6 boundary — only `connection_id` may cross |
| the picker's footer → the person approving a governed send | the one surface that answers *where does this send?* |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-206.2-02-S1 | Spoofing | `destinationPartsOf` naming the wrong host | mitigate | The MCP arm is EXPLICIT and above the tail; the Slack arm names its own condition; the tail is a neutral `return []`. Proved by a synthetic-fifth-shape control asserting BOTH an empty list and the absence of `SLACK_FIXED_DESTINATION`. ⚠ A row naming the wrong host is worse than a row naming none — this column is the fact a governed send is approved on. |
| T-206.2-02-I1 | Information disclosure | a credential or destination fact reaching the definition JSONB | mitigate | The T6 sweep (`ConnectionPicker.test.tsx:342-372`) is untouched and still passes: `bind()` stays one key. **This plan adds no write of any kind.** The multi-key shape patch is `206.2-03`'s and lives at the shape-choice site, never here. |
| T-206.2-02-E1 | Elevation of privilege | a DISABLED connection becoming bindable through the new shape | mitigate | The `is_enabled` filter is applied to the MCP arm identically, and the "all disabled" empty reason proves the rows were seen and rejected rather than never fetched. The server's GATE 5 (`ConnectorDisabled` — records, never sends) is the second layer and is not simulated. |
| T-206.2-02-T1 | Tampering | remote tool metadata rendered as UI | mitigate | `discovered_tools` reaches the DOM only through the shipped `McpToolPicker`, as escaped React children in `<option>` and `<p>`. ⚠ **Never `dangerouslySetInnerHTML` for a tool name or description** — T-206-03. This plan introduces no new render of remote text. |
| T-206.2-02-D1 | Denial of service | the unfiltered read returning every org connection | accept | The list is org-scoped and small (measured: 2 rows locally); the read is already performed on every capability shape today and the MCP arm adds no second request. The shape term on `requestKey` prevents a stale answer being displayed, which is the honesty risk rather than a load one. |
| T-206.2-02-R1 | Repudiation | a footer whose host cannot be traced to a source of truth | mitigate | `SLACK_FIXED_DESTINATION` is unchanged and still documented as a READING of `backend/app/security/egress.py`'s constant, never a second source. The MCP host is read from the row's own `mcp_server_url`. |
| T-206.2-02-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed.** `frontend/package.json` and `package-lock.json` are not modified; no `npx shadcn add` runs. The Package Legitimacy Gate does not fire; a proposed dependency is a scope deviation and the gate runs first. |
</threat_model>

<verification>
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` **from the repo root** — verdict line
  VERBATIM, at this plan's base and at its close. `failed 0`, no per-file DECREASE.
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — count unchanged from this plan's own base,
  zero in any touched file.
- `git diff --numstat -- frontend/src/lib/api.ts | wc -l` == 0.
- `git diff --numstat -- supabase/migrations | wc -l` == 0.
- `git diff --numstat -- backend | wc -l` == 0 — this plan is frontend-only.
- `grep -rn "external-action-option" frontend/src/components/workflows/ExternalActionSection.tsx | wc -l`
  unchanged — this plan does not touch the section at all.
</verification>

<success_criteria>
- The MCP shape calls `listConnectorConnections()` with **no argument**, lists only enabled rows
  carrying `mcp_server_url`, and neither shape lists the other's rows — all asserted in the same file.
- The capability branch renders byte-for-byte what it rendered at this plan's base, against a capture
  taken from the base commit's own component, with a COUNTED `useId` normalization whose count is
  asserted and whose needle was read off a real render.
- An MCP row's footer names its own host. A synthetic fifth shape names nothing — and specifically not
  `slack.com/api`.
- `ConnectionPicker.test.tsx` is pinned at the gate's own `actual` reading, in the same commit as the
  cases it counts.
- `frontend/src/lib/api.ts` is not edited, proved by `git diff --numstat`.
</success_criteria>

<output>
Create `.planning/phases/206.2-an-mcp-connection-has-somewhere-to-go-insert/206.2-02-SUMMARY.md` when
done. Record: the base and close gate verdict lines VERBATIM; the base and close tsc counts; the id
form actually observed for the `useId` normalization and the replacement counts measured; where the
new `ConnectionPicker.test.tsx` pin number was read from; and the footer reading for the live MCP
fixture.
</output>
