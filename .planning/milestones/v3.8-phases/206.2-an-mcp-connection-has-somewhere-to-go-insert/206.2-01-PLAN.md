---
phase: 206.2-an-mcp-connection-has-somewhere-to-go-insert
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/harness/grounding.py
  - backend/tests/unit/test_103_grounding_fidelity.py
  - backend/tests/unit/test_mcp_connector_client.py
autonomous: true
requirements: [CONN-02, CONN-03]
worktrees: forbidden
gap_closure: false

must_haves:
  truths:
    - "D-206.2-19 — ⚠ A WORKFLOW CONTAINING AN MCP STEP CANNOT BE PUBLISHED TODAY, AND THE FIX IS IN SCOPE. This reverses CONTEXT.md's own earlier `no backend change` fence, deliberately and in writing. Driven at HEAD: `ExternalActionPhaseConfig._available_tools_is_the_capability` (`backend/app/models/harness.py:324-352`) derives `available_tools = [tool_name]` for an MCP step; `_unregistered_tools` (`backend/app/services/harness/grounding.py:816-866`) admits, for an `external_action` phase, only `EXTERNAL_ACTION_CAPABILITIES`; so the tool name is a rule-2 offender, `api/workflows.py`'s subtraction taxonomy resolves it to severity `error`, and `publish_service.py:252-268` blocks at stage 2.6 `grounding_fidelity` BEFORE the golden run."
    - "D-206.2-19 — ⚠ THE FIX IS NOT A LOOSENING AND THE PLAN PROVES IT WITH TWO NEGATIVE CONTROLS, NEVER WITH REASONING. When an `external_action` phase carries a `tool_name`, the admissible set gains THAT ONE NAME — which `available_tools` is already DERIVED from by the model validator, so the two are equal by construction and nothing new becomes representable. Control A: CR-01's measured wire-around — an `llm_agent` step carrying `{\"available_tools\": [\"send_email\"]}` — is STILL BLOCKED (`test_a_capability_on_an_llm_agent_step_still_blocks_publish` passes UNEDITED). Control B: an `external_action` phase whose `available_tools` DISAGREES with its own `tool_name` is STILL FLAGGED."
    - "D-206.2-19 — ⚠ THE MCP TOOL IS CLOSED BY A DIFFERENT AND STRONGER MECHANISM THAN `_TOOL_REGISTRY`: the per-tool grant, enforced at run time as `tool_grants.get(tool_name) is True` (**a missing key DENIES**) with a `tool_refused` audit event. **Publishing grants nothing.** That sentence goes in the source, at the widening's own site, so a future reader meets the argument rather than re-deriving it."
    - "D-206.2-19 — ⚠ `_unregistered_tools`'s OWN DOCSTRING ASSERTS TWO THINGS THAT STOPPED BEING TRUE THE DAY PHASE 206 LANDED: *\"IT COSTS `external_action` NOTHING\"* and *\"D-06 (a workflow containing the external-action node PUBLISHES) stays true by construction\"*. Both are corrected BESIDE their originals, never over them — the correction-beside-the-original discipline this repository has recorded a dozen times. A deleted stale claim leaves no evidence that anyone measured it."
    - "D-206.2-19 — ⚠ ZERO BACKEND TEST COVERAGE EXISTS FOR `PATCH /connectors/connections/{id}/grants` OR FOR `connector_service.update_connection_grants` — measured (`grep -rn 'update_connection_grants|/grants' backend/tests` returns nothing). The write this phase's UI is about to make has never been exercised in any tier. This plan adds it rather than inheriting the gap."
    - "D-206.2-19 — ⚠ SC#3 IS REACHABLE WITHOUT THIS FIX, VIA ▶ Test Run, WHICH IS A DRAFT PATH WITH NO VERDICT GATE. Recorded here so nobody scores SC#3 green on the draft path and leaves publish broken. Wave 4's driven round trip runs through Test Run; THIS plan is what makes the published path true as well."
    - "D-206.2-16 — the grant endpoint is a whole-column REPLACE (`connector_service.py:786-793`: `.update({\"tool_grants\": sanitized_grants})`). That is asserted HERE, at the tier that owns it, so wave 4's UI obligation to send the FULL MERGED MAP rests on a measurement rather than on a reading of the source."
    - "D-206.2-05 — SEED-146's standing rule (*never add an outbound capability before the approval model exists*) is ANSWERED, not assumed: this plan adds NO outbound capability. It widens a PUBLISH-time admissibility check by exactly the one name the model already derived, and it adds test coverage. The egress, the SSRF guard and the dispatch all shipped in Phase 206."
    - "D-206.2-08 — the three shipped capability shapes are untouched. `EXTERNAL_ACTION_CAPABILITIES` keeps exactly its three members; migration 116's CHECK is not touched; `backend/app/models/connector.py:101`'s module-scope `assert` (an IMPORT failure, not a test failure) still holds."
    - "D-206.2-09 — baselines RE-DERIVED at this plan's own base, never quoted. Backend `pytest tests/unit -q` read at this plan's base and again at its close; the DELTA is the claim, not the absolute."
  artifacts:
    - path: "backend/app/services/harness/grounding.py"
      provides: "the `external_action` arm of `_unregistered_tools` admits the phase's own `tool_name`, plus the two corrections-beside-their-originals"
      contains: "tool_name"
    - path: "backend/tests/unit/test_103_grounding_fidelity.py"
      provides: "the MCP publish case + BOTH negative controls, driven through the SAME collector and the SAME production `tool_names` set the publish gate uses"
    - path: "backend/tests/unit/test_mcp_connector_client.py"
      provides: "the first test coverage `PATCH /grants` and `update_connection_grants` have ever had — REPLACE semantics, boolean sanitization, the not-found arm, and the route's `require_org_manage` dependency"
  key_links:
    - from: "backend/app/services/harness/grounding.py"
      to: "backend/app/models/harness.py"
      via: "the admissible set is the SAME derivation the model validator performs — `tool_name` first, then `capability`"
      pattern: "tool_name"
    - from: "backend/tests/unit/test_103_grounding_fidelity.py"
      to: "backend/app/services/harness/grounding.py"
      via: "`grounding_verdicts` — the ONE collector both `POST /workflows/validate` and publish stage 2.6 call"
      pattern: "grounding_verdicts"
---

<objective>
Close the measured publish blocker: a workflow containing an MCP-shaped `external_action` step is
refused at publish stage 2.6 with an `unregistered_tool` finding at severity `error`, and it is
painted as a hard error on the builder canvas the moment a tool is chosen.

**Purpose:** without this, waves 2-4 ship an authoring door whose result the publish gauntlet
refuses — the **FOURTH consecutive built-but-unreachable door** in this feature's history (206
shipped the engine with no create door; 206.1 the create door with no bind door; 206.2 would ship
the bind door with no publish). Breaking that pattern is the phase's whole reason for existing.

**Why this plan is FIRST and standalone:** it is the only work in the phase that touches no
frontend file, so it can be verified entirely by the backend suite, and it unblocks the one success
criterion that the ▶ Test Run path would otherwise let a later wave score green while leaving
publish broken.

**Output:** one widened membership arm with its argument in the source, two corrected docstring
claims recorded beside their originals, three grounding cases (one positive, two negative controls),
and the first backend coverage `PATCH /grants` has ever had.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/206.2-an-mcp-connection-has-somewhere-to-go-insert/206.2-CONTEXT.md
@.planning/phases/206.2-an-mcp-connection-has-somewhere-to-go-insert/206.2-RESEARCH.md
@.planning/phases/206.2-an-mcp-connection-has-somewhere-to-go-insert/206.2-PATTERNS.md
@.planning/seeds/SEED-200-mcp-connection-cannot-be-bound-to-any-step.md
@.planning/phases/206-mcp-connector-client-workflow-scoped/206-01-SUMMARY.md
</context>

<interfaces>
<!-- Read at HEAD by the research pass and re-read by this planner. No exploration needed. -->

**The membership site** — `backend/app/services/harness/grounding.py:816-866`. Its body today is
three statements: `allowed = set(tool_names) - EXTERNAL_ACTION_CAPABILITIES` (`:857`), then
`if getattr(phase.config, "phase_type", None) == "external_action": allowed |= EXTERNAL_ACTION_CAPABILITIES`
(`:858-859`), then a list comprehension over `getattr(phase.config, "available_tools", None) or []`
keeping every `tool` NOT in `allowed`. **The subtraction-first shape at `:857` is load-bearing** and
its own comment says why: it makes the answer independent of what the caller passed, so a caller
handing in a set that already contains the capabilities cannot widen a non-`external_action` type by
accident.

**The derivation the widening must mirror** — `backend/app/models/harness.py:324-352`,
`_available_tools_is_the_capability`, a `@model_validator(mode="after")`: `if self.tool_name:
derived = [self.tool_name]` / `elif self.capability: derived = [self.capability]` / `else: derived =
[]`, and then `self.available_tools = derived` whenever it disagrees. **It TOTALLY REPLACES whatever
the client sent** — which is why control B below cannot be built from a real
`ExternalActionPhaseConfig` and must use a stub.

**The fidelity set** — `grounding.py:477`: `fidelity_tool_names = schema_tool_names |
EXTERNAL_ACTION_CAPABILITIES`, threaded to `grounding_verdicts` at `:523` as `tool_names=`. The 28
`get_tools(None)` schema names are `analyze_document … write_todos`; no MCP tool name is among them.

**The severity taxonomy** — `backend/app/api/workflows.py:864-907`: `_ERROR_CODES = _KNOWN_CODES -
_INCOMPLETE_CODES - _DUAL_SOURCE_CODES`. `unregistered_tool` is in neither soft bucket, so it is
**`error`**.

**The publish stage** — `backend/app/services/harness/publish_service.py:252-268`: stage 2.6 calls
`_grounding_fidelity_failures(...)` and, if it returns anything, `return await _block(..., stage="grounding_fidelity", ...)`
**before** the golden run.

**The test helpers that already exist and must be reused, not re-invented** —
`backend/tests/unit/test_103_grounding_fidelity.py`:
`_definition_with_tools(tools)` (`:213`, an **`llm_agent`** phase),
`_definition_with_external_actions(capabilities, *, extra_phases=())` (`:224`, ONE
`external_action` phase per capability, slugs `act-0…act-N`),
`_production_tool_names()` (`:260`, reads the REAL `assemble_grounding_bundle` — load-bearing: a
test that re-typed `get_tools(None)` would measure the patch instead of the property),
`_unregistered_tool_findings(tools)` (`:290`) and
`_unregistered_tool_findings_for(definition)` (`:301`, the arbitrary-definition drive).
`_UID` is `"00000000-0000-0000-0000-0000000000a1"` — a real UUID, because `_skill_registry` parses it.

**The two shipped cases that must pass UNEDITED** —
`test_a_capability_on_an_llm_agent_step_still_blocks_publish` (`:391`) and
`test_a_genuinely_unknown_tool_still_produces_the_finding` (`:429`).

**The grant write** — `backend/app/services/connector_service.py:778-803`,
`update_connection_grants(connection_id, org_id, tool_grants, supabase=None)`:
`sanitized_grants = {str(k): bool(v) for k, v in tool_grants.items()}`, then
`client.table(_TABLE).update({"tool_grants": sanitized_grants}).eq("id", ...).eq("org_id", ...)`
wrapped in `_project(...)` and `aexec(...)`; `updated = (result.data or [None])[0]`;
`if updated is None: raise ConnectorNotFound(...)`; returns `_to_response(updated)`.

**The route** — `backend/app/api/connectors.py:604-626`,
`@router.patch("/connections/{connection_id}/grants", response_model=ConnectorConnectionResponse,
dependencies=[Depends(require_org_manage)])`, binding `tool_grants: dict[str, bool]` as the WHOLE
body (a bare JSON object, not `{"tool_grants": {...}}`). `require_org_manage` raises **403** at
`backend/app/dependencies.py:894-911`. ⚠ It does NOT carry `require_visible("live_connectors")`,
unlike the CRUD routes.

**The existing MCP test file's idiom** — `backend/tests/unit/test_mcp_connector_client.py` already
imports `SimpleNamespace`, `AsyncMock`, `patch`, and drives `_exec_external_action` with monkeypatched
seams (`test_exec_external_action_mcp_granted` `:378`,
`test_exec_external_action_mcp_permission_denied_emits_audit` `:440`). New cases belong there.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED first — the three grounding cases that pin the publish boundary, driven against the UNCHANGED validator</name>
  <files>backend/tests/unit/test_103_grounding_fidelity.py</files>

  <read_first>
    - `backend/tests/unit/test_103_grounding_fidelity.py:160-210` (the file's own header on what
      stage 2.6 is and why `EXTERNAL_ACTION_CAPABILITIES` is a closed frozenset here), `:213-222`
      (`_definition_with_tools`), `:224-257` (⭐ `_definition_with_external_actions` and the
      ONE-PHASE-PER-NAME reasoning forced by the model), `:260-289` (⭐ `_production_tool_names` and
      the two reasons it must read the real assembler), `:290-320` (`_unregistered_tool_findings` /
      `_unregistered_tool_findings_for`), `:326-339` (the `len(...) == 3` D-15 fence), `:340-388`
      (the D-06 positive), `:391-428` (⭐ **CONTROL A**, `test_a_capability_on_an_llm_agent_step_still_blocks_publish`
      — read its docstring in full: it is the exact argument this plan must not weaken), `:429-455`
      (the hallucinated-tool control)
    - `backend/app/services/harness/grounding.py:816-866` — the function under test, in full
    - `backend/app/models/harness.py:324-352` — the validator whose derivation the widening mirrors,
      and the reason control B needs a STUB
    - `backend/app/services/harness/publish_service.py:248-268` — where the block happens
    - `.planning/phases/206.2-an-mcp-connection-has-somewhere-to-go-insert/206.2-RESEARCH.md` §OQ#3
  </read_first>

  <behavior>
    - **The MCP positive (RED at this task's base):** a definition carrying ONE `external_action`
      phase whose config is `{phase_type, tool_name: "ask_question", tool_args: {...}}` and NO
      `capability`, driven through `_unregistered_tool_findings_for`, produces **zero**
      `unregistered_tool` findings. Measured at base it produces exactly one, naming `'ask_question'`.
    - **Control A (GREEN at base and must STAY green, UNEDITED):** the shipped
      `test_a_capability_on_an_llm_agent_step_still_blocks_publish` — an `llm_agent` phase whitelisting
      all three capabilities is still blocked on all three. The plan MAY NOT edit this case.
    - **Control A′ (NEW, the MCP mirror of A):** an **`llm_agent`** phase whose `available_tools`
      names `"ask_question"` is STILL FLAGGED after the widening. This is the control that proves the
      widening is scoped by the `phase_type` test rather than by the name.
    - **Control B (NEW):** an `external_action` phase whose `available_tools` DISAGREES with its own
      `tool_name` is STILL FLAGGED, and the flagged name is the disagreeing one. ⚠ Not representable
      through `ExternalActionPhaseConfig` — the validator replaces the list — so this case calls
      `_unregistered_tools` DIRECTLY with a `SimpleNamespace` stub, which the function's own `getattr`
      reads make legitimate. Say so in the case's docstring.
    - **Non-vacuity:** every case asserts the findings list it reasons about is non-empty where it
      expects offenders, and `_production_tool_names()`'s own `assert bundle.tool_names` guard is
      inherited.
  </behavior>

  <action>
    Add a new `describe`-equivalent block of cases to `backend/tests/unit/test_103_grounding_fidelity.py`,
    BELOW every shipped case, under a banner reading `⚠ ADDED, NEVER RE-BASELINED — every case above
    this banner is untouched by 206.2`. Do not move, re-word or re-order any shipped case.

    Add a helper `_definition_with_mcp_external_action(tool_name, *, available_tools=None)` beside
    `_definition_with_external_actions`, producing a one-phase definition with slug `act-mcp`,
    `phase_index` 0, config `{phase_type: "external_action", tool_name: <name>, tool_args: {"repoName": "facebook/react"}}`
    and `available_tools` only when explicitly passed. Its docstring states that the model validator
    will derive `available_tools` from `tool_name`, so the omission is the shipped shape and not an
    oversight.

    Write the MCP positive case using `_unregistered_tool_findings_for`, asserting
    `findings == []` with a message naming stage 2.6, `publish_service.py`'s
    `stage="grounding_fidelity"` block, and the fact that the fix is a widening by ONE name the model
    already derived — never a disabling of rule 2.

    Write control A′ using `_unregistered_tool_findings(["ask_question"])` (the `llm_agent` fixture),
    asserting exactly one finding whose `code` is `unregistered_tool`, whose `phase` is `"research"`
    and whose `message` contains `repr("ask_question")`. Its docstring names CR-01 by id and states
    that an `llm_agent` step must never reach an MCP tool by whitelisting its name, because that step
    has `action_risk_armed: false` and no D-04 checkpoint.

    Write control B by importing `_unregistered_tools` directly from
    `app.services.harness.grounding` and calling it with a `SimpleNamespace(config=SimpleNamespace(
    phase_type="external_action", tool_name="ask_question", available_tools=["read_wiki_contents"]))`
    and a `tool_names` set obtained from `_production_tool_names()`. Assert the returned list is
    exactly `["read_wiki_contents"]`. In the same case, assert the AGREEING stub
    (`available_tools=["ask_question"]`) returns `[]` — the two halves in one case, so the pair is a
    measurement of the boundary rather than two unrelated drives.

    ⚠ **RUN THE THREE NEW CASES AND RECORD THE OUTPUT BEFORE TASK 2 EDITS ANY SOURCE.** Expected at
    this task's base: the MCP positive FAILS naming `'ask_question'`; control A′ PASSES; control B's
    disagreeing half PASSES and its agreeing half FAILS. **A guard first observed GREEN on a broken
    tree is not a guard** (S-4). Paste the raw pytest output into the task's commit message.
  </action>

  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_103_grounding_fidelity.py -q 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `git diff --numstat` for this task names `backend/tests/unit/test_103_grounding_fidelity.py`
      and NOTHING under `backend/app/`. A source edit in this task means RED was never observed.
    - The pytest run at this task's close reports the MCP positive case as **FAILED** and its failure
      message contains `ask_question`. Record the verbatim line in the commit message.
    - `test_a_capability_on_an_llm_agent_step_still_blocks_publish` and
      `test_a_genuinely_unknown_tool_still_produces_the_finding` both PASS, and
      `git diff -- backend/tests/unit/test_103_grounding_fidelity.py` shows **zero** deletions inside
      either case's line range (`git diff -U0 ... | grep '^-[^-]'` scoped to `:391-455` returns
      nothing).
    - The new banner string `⚠ ADDED, NEVER RE-BASELINED` appears exactly once in the file
      (`grep -c 'ADDED, NEVER RE-BASELINED' backend/tests/unit/test_103_grounding_fidelity.py` == 1).
    - `grep -n "assert len(EXTERNAL_ACTION_CAPABILITIES) == 3" backend/tests/unit/test_103_grounding_fidelity.py`
      still resolves — the D-15 fence is untouched.
  </acceptance_criteria>

  <done>
    Three new cases exist. The MCP positive is RED against the unchanged validator and its failure
    text names the offending tool. Both controls are green. No file under `backend/app/` was touched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — widen `_unregistered_tools`'s `external_action` arm by the phase's own `tool_name`, and correct the two stale docstring claims BESIDE their originals</name>
  <files>backend/app/services/harness/grounding.py</files>

  <read_first>
    - `backend/app/services/harness/grounding.py:816-866` in full — especially the CR-01 paragraph
      (*"THE RULE IS STILL ONE RULE — the WIDENING is what became conditional"*), the two claims to be
      corrected (*"IT COSTS `external_action` NOTHING"* and *"D-06 … stays true by construction"*),
      and the `:853-856` comment explaining why `allowed` SUBTRACTS first
    - `backend/app/services/harness/grounding.py:436-480` — the assembler's own two-set block and the
      `fidelity_tool_names` line at `:477`, so the widening is understood as HERE and not there
    - `backend/app/models/harness.py:324-352` — the derivation being mirrored
    - `backend/app/services/harness/phase_types.py:2274-2299` — the executor's two-shape branch, and
      `:2435-2470` — GATE 6, `grants.get(tool_name) is True`, the mechanism that actually closes an
      MCP tool. **Read, never edit.**
    - `.planning/phases/206.2-an-mcp-connection-has-somewhere-to-go-insert/206.2-CONTEXT.md` D-206.2-19
      and D-206.2-05
  </read_first>

  <action>
    In `_unregistered_tools`, inside the existing `if ... == "external_action":` arm and immediately
    after `allowed |= EXTERNAL_ACTION_CAPABILITIES`, read the phase's own `tool_name` with the same
    `getattr(phase.config, "tool_name", None)` defensive form the rest of the function uses, and when
    it is a non-empty string add THAT ONE NAME to `allowed`. Nothing else changes — `allowed` still
    starts from the SUBTRACTION at `:857`, and the comprehension is untouched.

    ⚠ Add ONE name, from the phase being checked. Do NOT add `phase.config.available_tools`, do NOT
    add a set of names, and do NOT introduce a second membership set: `available_tools` is DERIVED
    from `tool_name` by the model validator, so admitting the derived list instead of its source
    would make the check tautological and control B would go green for the wrong reason.

    ⚠ Guard on a non-empty STRING, not on truthiness of an arbitrary object. `phase.config` may be a
    stub in tests and a `str()` coercion of a non-string would admit a name nobody authored.

    Write the widening's argument in the source, immediately above it, as a `── D-206.2-19 ──` block
    stating: (a) what was measured (the derivation makes `available_tools == [tool_name]`, so the two
    are equal by construction and nothing new becomes representable); (b) that the type test already
    scopes this to `external_action`, so CR-01's `llm_agent` wire-around stays blocked and is
    ASSERTED rather than assumed; (c) that the MCP tool is closed by a **different and stronger**
    mechanism than `_TOOL_REGISTRY` — the per-tool grant at GATE 6, where a missing key DENIES and a
    refusal writes a `tool_refused` audit event — so **publishing grants nothing**; and (d) that this
    is a defect on shipped code introduced by Phase 206 in the same commit that made `tool_name`
    outrank `capability`, not a new capability.

    Correct the two stale claims BESIDE their originals, never over them. Leave the sentences
    *"IT COSTS `external_action` NOTHING…"* and *"D-06 … stays true by construction"* exactly where
    they are, and append a `⚠ CORRECTED 2026-08-25 (Phase 206.2)` paragraph directly beneath the
    block that contains them, stating that both stopped being true the day Phase 206 added `tool_name`
    and made it outrank `capability` in that same validator, that nothing caught it because nothing
    in production could reach the shape, and what the corrected invariant is: on this type the list is
    always exactly one member of {the closed capability set} ∪ {this phase's own `tool_name`}.

    ⚠ Do NOT touch `assemble_grounding_bundle`'s `fidelity_tool_names` (`:477`). Widening THERE would
    widen the set for EVERY phase type and would re-create CR-01's wire-around one level up — the
    exact shape the function's own prose refuses. The narrowing/widening belongs at the ONE membership
    site, which is this function.
  </action>

  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_103_grounding_fidelity.py tests/unit/test_182_publish_grounding_stage.py tests/unit/test_193_1_grounding_wire.py tests/unit/test_182_grounding_degradation.py -q 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - Every case in `tests/unit/test_103_grounding_fidelity.py` passes, including the MCP positive
      that was RED at task 1's close. Paste the before/after pytest one-liners in the commit message.
    - Control A (`test_a_capability_on_an_llm_agent_step_still_blocks_publish`), control A′ and
      control B all pass, and `git diff -- backend/tests/` for THIS task is empty
      (`git diff --numstat HEAD~1 -- backend/tests | wc -l` == 0). **A source fix that also edits its
      own tests has not been proved by them.**
    - `git diff --numstat` names exactly one file under `backend/app/`:
      `backend/app/services/harness/grounding.py`.
    - `grep -c "fidelity_tool_names = schema_tool_names | EXTERNAL_ACTION_CAPABILITIES" backend/app/services/harness/grounding.py`
      returns `1` and the line is byte-identical to its base
      (`git diff -U0 -- backend/app/services/harness/grounding.py | grep -c 'fidelity_tool_names'` == 0).
    - `git diff -U0 -- backend/app/services/harness/grounding.py | grep '^-[^-]'` contains **no**
      deletion of either stale sentence — verify by
      `git show HEAD:backend/app/services/harness/grounding.py | grep -c "IT COSTS"` equals
      `grep -c "IT COSTS" backend/app/services/harness/grounding.py`, and likewise for
      `"stays true by construction"`.
    - `grep -c "CORRECTED 2026-08-25" backend/app/services/harness/grounding.py` >= 1 and
      `grep -c "D-206.2-19" backend/app/services/harness/grounding.py` >= 1.
    - `grep -c "publishing grants nothing" -i backend/app/services/harness/grounding.py` >= 1 —
      D-206.2-05's answer is at the widening's own site, in the source, as the decision requires.
    - The full backend suite delta is measured: `cd backend && venv/Scripts/python -m pytest tests/unit -q`
      and the `failed` count is recorded against the figure read at this plan's base. **A LARGER
      passed count is the suite working; a larger failed count is a stop.**
  </acceptance_criteria>

  <done>
    An MCP-shaped `external_action` step produces zero rule-2 findings and therefore passes publish
    stage 2.6. Both negative controls still block. The two stale docstring claims stand beside their
    corrections. No test file moved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The first backend coverage `PATCH /grants` has ever had — REPLACE semantics, boolean sanitization, the not-found arm, and the route's org-admin dependency</name>
  <files>backend/tests/unit/test_mcp_connector_client.py</files>

  <read_first>
    - `backend/app/services/connector_service.py:778-803` — `update_connection_grants`, in full
    - `backend/app/api/connectors.py:276-289` (the picker's UNGATED read, for the contrast),
      `:574-602` (`discover_tools`, `require_org_manage`), `:604-630` (`update_grants`,
      `require_org_manage`, `tool_grants: dict[str, bool]` bound as the WHOLE body)
    - `backend/app/dependencies.py:894-911` — `require_org_manage` and its 403
    - `backend/tests/unit/test_mcp_connector_client.py:1-40` (the file's imports and posture),
      `:346-377` (the two model cases), `:378-500` (⭐ the two `_exec_external_action` drives — the
      monkeypatch idiom to copy)
    - `backend/tests/unit/test_190_connectors_api.py` — the shipped connectors-API test posture, to
      match how routes are exercised in this repository rather than inventing a second style
    - `.planning/phases/206.2-an-mcp-connection-has-somewhere-to-go-insert/206.2-RESEARCH.md` §OQ#4
  </read_first>

  <behavior>
    - **REPLACE, not merge — the headline.** Calling `update_connection_grants` with `{"b": True}`
      against a row whose stored `tool_grants` is `{"a": True}` issues an `.update()` whose payload's
      `tool_grants` is exactly `{"b": True}` — **`"a"` is GONE**. Assert the payload dict handed to
      `.update()`, captured off a fake client, by SET EQUALITY of its `tool_grants` keys. This is the
      measurement wave 4's "send the FULL MERGED MAP" obligation rests on.
    - **Sanitization:** a payload of `{"t": 1}` / `{"t": "yes"}` / `{"t": 0}` reaches the column as
      real booleans (`True`, `True`, `False`) — `bool(v)` at `:786`. This matters because GATE 6
      compares with `is True`, and the CLIENT's `isToolGranted` accepts any truthy value: the
      sanitizer is what makes the two agree.
    - **Scoping:** the update is filtered by BOTH `id` and `org_id`. Assert both `.eq()` calls, by
      column NAME, off the fake. A grant write scoped by id alone is a cross-org write.
    - **Not found:** an empty `result.data` raises `ConnectorNotFound`, and the error names the id.
    - **The route is org-admin-only:** `require_org_manage` is among the dependencies of BOTH
      `update_grants` and `discover_tools`, asserted by INSPECTING the registered route's
      dependencies rather than by reading the decorator's source text — a source grep would pass on a
      commented-out decorator.
    - ⚠ **Non-vacuity for the fake:** the fake client's `.update()` must RECORD its argument and the
      test must assert the recording is non-empty before asserting its contents. A fake whose
      `update` silently discards its payload is exactly the trap
      `backend/app/api/workflow_runs.py`'s ledger section records — a forgotten projection passed
      GREEN under an old fake.
  </behavior>

  <action>
    Add the cases to `backend/tests/unit/test_mcp_connector_client.py`, below every shipped case,
    under the same `⚠ ADDED, NEVER RE-BASELINED` banner discipline used in task 1.

    Build a small recording fake for the Supabase client shaped to the exact chain
    `client.table(_TABLE).update(payload).eq("id", ...).eq("org_id", ...)`, returning an object with a
    `.data` list. Record the payload and the ordered `(column, value)` pairs. Assert the recording is
    non-empty FIRST (the non-vacuity control), then its contents. Do not monkeypatch
    `update_connection_grants` itself — the function under test must really run.

    Drive the REPLACE case, the three sanitization cases (in ONE case, as a table, so a partial fix
    cannot pass), the two-`eq` scoping case and the `ConnectorNotFound` case.

    For the authorization case, import the FastAPI app's router and locate the two routes by path
    (`/connections/{connection_id}/grants` and `/connections/{connection_id}/discover`), then assert
    `require_org_manage` appears among each route's dependency callables. Assert in the SAME case that
    the list route `/connections` does NOT carry it — the asymmetry is the fact wave 4's three-arm
    audience render is built on, and asserting only the positive half would not measure it.

    Add one prose paragraph at the top of the new block recording that this file previously had
    **zero** coverage of the grant write, that the endpoint is a whole-column REPLACE, and that the
    UI's obligation to send the full merged map (D-206.2-16) is a consequence of THIS measurement.

    ⚠ Do NOT add a version column, an optimistic-locking scheme, or any migration. `tool_grants`
    carries no version and adding one is a migration this phase forbids. The honest mitigation is the
    UI's scope sentence in wave 4, not a lock.
  </action>

  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_mcp_connector_client.py -q 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rn "update_connection_grants" backend/tests | wc -l` is **>= 1** where it was `0` at this
      plan's base. Record both readings.
    - The REPLACE case asserts `set(payload["tool_grants"].keys()) == {"b"}` against a row seeded with
      `{"a": True}`, and its assertion message says in words that a `{[tool]: next}` payload from the
      UI would wipe every other grant.
    - The sanitization case asserts all three coercions in one drive and the assertion is on the
      payload's VALUES being `is True` / `is False`, not merely truthy.
    - The scoping case asserts both `("id", ...)` and `("org_id", ...)` were applied, by column name.
    - The authorization case asserts `require_org_manage` is a dependency of BOTH `/grants` and
      `/discover`, AND that it is NOT a dependency of the `GET /connections` list route. All three
      assertions in one case.
    - The non-vacuity control is present and precedes every content assertion: the recorded payload
      list is asserted non-empty before its contents are read.
    - `cd backend && venv/Scripts/python -m pytest tests/unit -q` — the `failed` count equals the
      figure recorded at this plan's base and the `passed` count is strictly larger. Record the
      verbatim summary line.
  </acceptance_criteria>

  <done>
    `PATCH /grants` and `update_connection_grants` have test coverage for the first time. The REPLACE
    semantics, the boolean sanitization, the org scoping, the not-found arm and the org-admin
    dependency asymmetry are all asserted, with a non-vacuity control on the fake.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → `POST /workflows` / draft `PATCH` / the NL generator → `WorkflowDefinition` | untrusted definition JSONB reaches the publish validator; none of these three write paths is the author-facing rail |
| author (any org member) → `GET /connectors/connections` | ungated org-wide read |
| org admin → `PATCH /connectors/connections/{id}/grants` | `require_org_manage` + an RLS policy in both `USING` and `WITH CHECK` |
| harness executor → remote MCP server | egress, guarded by `validate_mcp_destination` before any socket |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-206.2-01-E1 | Elevation of privilege | `grounding._unregistered_tools` — the widening | mitigate | The widening is scoped by the existing `phase_type == "external_action"` test and admits exactly ONE name, the phase's own `tool_name`, which `available_tools` is already derived from. Asserted by control A (`llm_agent` + capability still blocked, UNEDITED), control A′ (`llm_agent` + MCP tool name still blocked) and control B (`external_action` whose `available_tools` disagrees with its `tool_name` still flagged). |
| T-206.2-01-E2 | Elevation of privilege | publish as an authorization decision | accept, with the reason recorded in source | Publishing admits a NAME to a fidelity check; it grants no permission. The tool is closed at run time by GATE 6 (`tool_grants.get(tool_name) is True` — a missing key DENIES) plus `tool_refused` audit. Task 2 writes this argument at the widening's own site so it cannot be lost. |
| T-206.2-01-T1 | Tampering | `update_connection_grants` — whole-column REPLACE | mitigate (assert here, fix in wave 4) | Task 3 asserts REPLACE semantics by SET EQUALITY on the payload's keys, so the UI's full-merged-map obligation (D-206.2-16) rests on a measurement. No version column is added — that is a migration this phase forbids. |
| T-206.2-01-I1 | Information disclosure | cross-org grant write | mitigate | Task 3 asserts the update is filtered by BOTH `id` and `org_id`, by column name. RLS is the second layer and is not simulated by any test. |
| T-206.2-01-S1 | Spoofing | a hand-built `phase.config` stub reaching the validator | accept | `_unregistered_tools` reads through `getattr` by design (both presentations share it). Control B EXPLOITS that to construct the disagreement case, and says so in its docstring rather than pretending the shape is representable. |
| T-206.2-01-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed by this plan.** `frontend/package.json`, `package-lock.json`, `backend/requirements*.txt` are not modified. The Package Legitimacy Gate does not fire; if a task believes it needs a dependency, that is a scope deviation and the gate runs first. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/unit -q` — re-derived at this plan's base AND at
  its close. The DELTA is the claim: `failed` unchanged, `passed` strictly larger. ⚠ Do not quote the
  figure recorded in RESEARCH.md; re-derive it (D-206.2-09).
- `git diff --numstat` over the whole plan names exactly three files, all under `backend/`.
- `git diff --numstat -- frontend/src/lib/api.ts | wc -l` == 0 — this plan does not touch the
  frontend at all, so the Phase-207 fence is satisfied trivially and is asserted rather than assumed.
- `git diff --numstat -- supabase/migrations | wc -l` == 0 — **no migration. If one appears, the
  scope has broken.**
- `grep -c "IT COSTS" backend/app/services/harness/grounding.py` equals the same count at
  `git show HEAD~N:...` for this plan's base — the stale claims were corrected beside, never deleted.
</verification>

<success_criteria>
- A definition containing an MCP-shaped `external_action` step produces **zero** `unregistered_tool`
  findings from `grounding_verdicts`, driven through the same collector and the same production
  `tool_names` set publish stage 2.6 uses.
- **Both negative controls hold**, and one of them (`test_a_capability_on_an_llm_agent_step_still_blocks_publish`)
  is a SHIPPED case that passes UNEDITED — proved by an empty deletion diff inside its line range.
- `_unregistered_tools`' two stale docstring claims stand beside a dated correction, and the
  widening's argument — including D-206.2-05's "this phase adds no outbound capability" answer — is
  in the source at its own site.
- `PATCH /grants` and `update_connection_grants` have backend coverage where they had none: REPLACE
  semantics, boolean sanitization, `id`+`org_id` scoping, the not-found arm, and the org-admin
  dependency asymmetry against the ungated list route.
- Backend `failed` count unchanged from this plan's own re-derived base; `passed` strictly larger.
</success_criteria>

<output>
Create `.planning/phases/206.2-an-mcp-connection-has-somewhere-to-go-insert/206.2-01-SUMMARY.md` when
done. Record: the base and close backend suite lines VERBATIM; the RED output from task 1 before any
source edit; the exact widening as it landed; and the two grant-write readings (`0` callers/tests at
base, the new count at close).
</output>
