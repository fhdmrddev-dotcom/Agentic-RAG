---
phase: 206.2-an-mcp-connection-has-somewhere-to-go-insert
plan: 04
subsystem: workflow-builder / connectors / governance
tags: [mcp, grants, approval-model, reachability-guard, two-legged, driven-round-trip, hot-file-ledger, jsonb-string-scalar]
requires:
  - "backend `_unregistered_tools` admitting an external_action phase's own tool_name (206.2-01) — the stage-2.6 fix this plan proved end to end"
  - "ConnectionPicker's MCP shape + the no-argument read (206.2-02) — the data path leg (b) walks"
  - "ExternalActionSection's shape control + PhaseFormPanel's toolName/onChangeShape wire (206.2-03) — without it leg (b) has nothing to find"
provides:
  - "the grant control — updateConnectorGrants' FIRST production caller, with three audience arms"
  - "a two-legged reachability guard whose leg (b) was OBSERVED RED against the pre-phase tree"
  - "the driven round trip: bind -> discover -> refuse -> grant -> send, against a live MCP server"
  - "206.1's owed SC#1b, CLOSED in the same commit as the evidence that closed it"
  - "six hot-file rows with six matching sections, four corrected cells, one written decline"
affects:
  - "frontend/src/components/workflows/McpToolPicker.tsx"
  - "frontend/src/components/workflows/ConnectionPicker.tsx"
  - "frontend/src/components/workflows/McpToolPicker.test.tsx"
  - "frontend/src/components/workflows/McpToolPicker.reachability.test.tsx"
  - "frontend/src/pages/WorkflowBuilderPage.{test,describe.test,preDraft.baseline.test,session.test}.tsx"
  - "scripts/vitest-count-gate.cjs"
  - "CLAUDE.md · docs/HOT-FILE-LEDGER.md · .planning/ROADMAP.md · SEED-146 · SEED-201"
tech-stack:
  added: []
  patterns:
    - "a two-legged guard where the cheap leg is declared VACUOUS in its own source, and the claim is MEASURED"
    - "a whole-collection write derived from the SERVER-OWNED prop, never a useState copy"
    - "no optimistic flip — the control's position is the server's truth"
    - "four audience arms, splitting *unmeasured* into no-provider (byte-identical) and probing (hides the 403 button)"
    - "a needle assembled at runtime so a fence's own prose cannot redden it (187-24, 13th firing)"
    - "every T4 reading taken from the source of truth, never from the UI agreeing with itself"
key-files:
  created:
    - "frontend/src/components/workflows/McpToolPicker.reachability.test.tsx (316 L)"
    - ".planning/seeds/SEED-201-external-action-workflow-cannot-publish-golden-run-waits-forever.md"
  modified:
    - "frontend/src/components/workflows/McpToolPicker.tsx (+288/-2)"
    - "frontend/src/components/workflows/ConnectionPicker.tsx (+35/-0)"
    - "frontend/src/components/workflows/McpToolPicker.test.tsx (+338/-0 — ZERO deletions, measured)"
    - "scripts/vitest-count-gate.cjs (+38/-0 — two pins ADDED, none RAISED, so no line was deleted)"
    - "CLAUDE.md (+6 rows, 4 corrections) · docs/HOT-FILE-LEDGER.md (+6 sections, 1 decline)"
decisions:
  - "D-206.2-04 / D-206.2-05 executed: the per-tool grant IS the approval model for this surface, answered in source AND in SEED-146's frontmatter"
  - "D-206.2-16 executed: the FULL MERGED MAP from the server-owned prop, proved at T4 by granting a SECOND tool"
  - "AR-01 executed with a DECLARED DEVIATION: FOUR audience arms, not three — `unknown` splits into `no-provider` and `probing`"
  - "D-206.2-07 / D-206.2-12 executed: leg (a) 3/3 GREEN and leg (b) 5/11 RED against the pre-phase tree, in ONE run"
  - "D-206.2-10 executed: 206.1's owed SC#1b marked closed in the same commit as the drive"
  - "D-206.2-11 executed: six rows + six sections, four re-derived corrections, CLAUDE.md's split ESCALATED as ROADMAP 208 rather than shaved"
  - "MEASURED CORRECTION: harness_audit.metadata is a jsonb STRING SCALAR on 2906/2906 rows — the plan's own prescribed audit query returns ZERO rows"
  - "MEASURED CORRECTION: RESEARCH A1 is falsified at T4 in the OPPOSITE direction from T1 — the cleared keys SURVIVE to the row, normalised"
  - "SC#3b NOT MET, named not absorbed: stage 2.6 PASSES, stage 3's golden run cannot complete for ANY external_action workflow -> SEED-201"
metrics:
  duration: "~2h20m (10:26 → 12:45 +0400)"
  completed: 2026-08-25
  tasks: 4
  commits: 4
  files: 12
---

# Phase 206.2 Plan 04: The grant control, the guard that would have failed, and the round trip — Summary

`updateConnectorGrants` has its first production caller since Phase 206 shipped it; the door
SEED-200 reported is open and was **walked through in a real browser** — bind → discover → refuse →
grant → send, against the live `mcp.deepwiki.com` server — and the guard that now watches it was
observed RED against the pre-phase tree in the same run that showed the cheap leg passing green.

## What shipped

| Task | Commit | Files | Verdict |
|---|---|---|---|
| 1 · the grant control — three arms rendered, FOUR arms computed, the full merged map | `58260911` | `McpToolPicker.tsx`, `ConnectionPicker.tsx` | RED observed on **15 of 17** new cases before any source edit; all **12 shipped cases pass UNEDITED** |
| 2 · the two-legged guard, the mock budget, two BASELINE pins | `c99dcc4a` | the guard suite (new), `McpToolPicker.test.tsx`, 4 × `WorkflowBuilderPage.*`, `vitest-count-gate.cjs` | leg (b) **5 of 11 RED** against the pre-phase tree; leg (a) **3 of 3 GREEN in the same run** |
| 3 · the driven round trip (evidence only) | — | none | `auto-approved-with-evidence` — see the scoreboard below |
| 4 · the ledger debt, 206.1's SC#1b, SEED-146, SEED-201 | `cdd31e4f` | `CLAUDE.md`, `docs/HOT-FILE-LEDGER.md`, `ROADMAP.md`, 2 seeds | six rows + six sections in ONE commit |

## The measured baselines (re-derived at this plan's own base, never quoted)

| | base (`42af355d`) | close (`cdd31e4f`) |
|---|---|---|
| count gate | `total 5672 · failed 0 · pinned total 5085`<br>`OK — 111/111` | `total 5700 · failed 0 · pinned total 5125`<br>`OK — 113/113` |
| `tsc --noEmit -p tsconfig.app.json` | **34**, zero in any touched file | **34**, zero in any touched file |
| backend `pytest tests/unit -q` | `68 failed, 2613 passed` (wave 1's close) | `68 failed, 2613 passed, 2 xfailed, 2 xpassed` — **unchanged** |
| `check-claude-md-size.cjs` | `124357 chars · 82.9% · WARN` | `133006 chars · 88.7% · WARN` (exit 0) |

**The `+28` on the grand total is fully attributed and leaves no residual:** `McpToolPicker.test.tsx`
`12 → 29` (`+17`) and the net-new reachability suite (`+11`). The **pinned total** moved `+40` and the
pinned FILE count `+2`, because both figures were net-new pins rather than raised ones.

⚠ **The gate was never red at any point in this plan**, so SEED-171's triage procedure was not
entered and the worker cap stayed at `2` throughout. Recorded as an observation, not as proof of
innocence.

**Scope fences, asserted rather than assumed** (all against base `42af355d`):
`frontend/src/lib/api.ts` **0 files** · `supabase/migrations` **0 files** ·
`frontend/package.json` / `package-lock.json` **0 files** (the Package Legitimacy Gate never fired;
no package was installed, no `npx shadcn add` ran) · `McpToolPicker.test.tsx` deletions **0**.

**SC#2's caller census, both readings:** `grep -rln "updateConnectorGrants(" frontend/src` minus tests
and `lib/api.ts` read **0 files at base** and **1 file at close**.

---

## ⚠ THE LEG-(b) RED — observed before the guard was trusted

Both counterfactuals were driven. **The first is the one that matters**, because it reproduces the
world the guard is about.

### Counterfactual 1 — the PRE-PHASE TREE (base `4c9b9a7f`'s `ConnectionPicker.tsx`, `ExternalActionSection.tsx` and `PhaseFormPanel.tsx` checked out over the working copy)

```
 × LEG (b) … ⭐ the MCP read is issued with NO ARGUMENT — the single line SEED-200 is about
   → Unable to find an element by: [data-testid="external-action-shape-option"]
 × LEG (b) … ⭐ …and the tool picker REACHES THE DOM, mounted by nothing this file wrote
 × LEG (b) … POSITIVE CONTROL — strip the server URL from the WIRE ROW …
 × LEG (b) … CROSS-SHAPE NEGATIVE — the MCP shape lists no capability row
 × LEG (b) … ⚠ the capability shape's exclusion of an MCP row is the SERVER's …
 ✓ LEG (b) … POSITIVE CONTROL 2 — on the CAPABILITY shape a bound row mounts NO tool picker
 ✓ LEG (b) … the CAPABILITY shape still calls the read WITH its capability
 ✓ LEG (b) … ⚠ NO COMPONENT PROP IS CONSTRUCTED IN THIS FILE
 ✓ LEG (a) · names its single importer by equality
 ✓ LEG (a) · POSITIVE CONTROL — both regexes really can find a mount
 ✓ LEG (a) · NON-VACUITY — the sweep really can see the file it excludes
      Tests  5 failed | 6 passed (11)
```

⚠ **ALL THREE LEG-(a) CASES WERE GREEN IN THAT SAME RUN.** That is the vacuity claim MEASURED rather
than argued: an import sweep is satisfied by the very defect SEED-200 reported, because
`ConnectionPicker.tsx` already imported and already mounted the component while the feature was
unreachable. *A guard satisfied by the defect it was written to catch is worse than no guard, because
it reads as coverage.*

**Restoration proof:** `md5sum -c` → `frontend/src/components/workflows/ConnectionPicker.tsx: OK`,
and `git diff --numstat` over all three restored files → **0 lines**.

### Counterfactual 2 — the isolated SEED-200 line (`listConnectorConnections()` re-planted as `listConnectorConnections(capability)`)

```
 × ⭐ the MCP read is issued with NO ARGUMENT — the single line SEED-200 is about
 × ⚠ the capability shape's exclusion of an MCP row is the SERVER's, and that is stated not assumed
      Tests  2 failed | 9 passed (11)
```
Restored: `md5sum -c … OK`, `git diff --numstat` → 0 lines.

⚠ **THE NARROWER COUNTERFACTUAL EXPOSED SOMETHING WORTH RECORDING: THE DOM-REACHING CASE STAYED
GREEN.** The `@/lib/api` mock returns the same rows whatever argument it is handed, so a test that
only asserts *the picker mounted* cannot see a wrong read argument. **Only the argument assertion
can** — which is exactly why leg (b)'s headline is `toHaveBeenCalledWith()` with zero arguments and
not a `getByTestId`.

---

## TASK 3 — THE DRIVEN ROUND TRIP · `auto-approved-with-evidence`

**Tool: Playwright 1.60.0 + real Chromium (`chromium-1223`), headless.** ⚠ Chrome DevTools MCP is not
present in this executor's toolset (the known upstream bug) — a deviation in **method, not in
evidence**, and the same one both 206.1 executors recorded. Scripts live in the scratchpad;
`git status -- frontend` is clean.

**Dev server: `http://localhost:5180`** — a FRESH Vite, because 5173/5174 were already bound to an
IPv6 `localhost` (the 206.1 trap, reproduced). **HEAD was asserted, not assumed:**
`curl http://localhost:5180/src/components/workflows/McpToolPicker.tsx | grep -c "Allow this tool on this connection"` → **1**.

**Session: local-only, minted for the org admin** (`fhdmrd@gmail.com`) via Supabase admin
`generate_link` + `verify`, behind a guard that **refuses any non-loopback `SUPABASE_URL`**.
⚠ **NO DIRECT DATABASE WRITE APPEARS ANYWHERE IN THIS EVIDENCE.** Every mutation was made by pressing
a control. `psycopg2` reads were used to *observe*, never to change.

⚠ **206.1's `localStorage` trick did NOT transfer, exactly as D-206.2-23 warned** — `activeView` is
unpersisted `useState` with no URL router, so the workflows home was **CLICKED**
(`[aria-label="Workflows"]`). ⚠ Radix opens on `pointerdown`; every menu/segment interaction is a real
pointer sequence. ⚠ **A canvas node at position 0 needs a pointer click on the CARD BODY** — a centred
click lands on the insert handle that floats above it, and the panel silently stayed on *"select a
step to refine it"* until the click moved to the card's lower-left.

### The scoreboard — a step that could not be driven is ⛔, never omitted

| # | Step | Verdict | Raw reading |
|---|---|---|---|
| 0 | HEAD + dev server | ✅ | `c99dcc4a…`; port **5180**; marker string present in the served module |
| 3 | Workflows home reached by CLICKING the rail | ✅ | `heading: "Workflows"` |
| 4-6 | draft `Northwind QBR` → canvas → `external_action` step added at position 0 → selected | ✅ | `removableSlugs: ["act","gather-renewals","write-brief"]`; `sectionShape: "capability"` |
| 7 | MCP shape segment pressed | ✅ | `data-shape: "mcp"`; segments `[{"One of these three actions", checked:false},{"A tool on an MCP server", checked:true}]` |
| **8** | **the picker lists the MCP row and NOT the capability rows — BOTH halves** | ✅ | `options: [{value:"7ca5e114-c5cd-4f7f-8c08-8885278bf42d", label:"DeepWiki (206.1 UAT)  ◌ not checked"}]` · `mcpRowPresent: true` · `slackRowPresent: false` · `jiraRowPresent: false` |
| 8b | the read's ARGUMENT, over the wire | ✅ | `GET /connectors/connections` (MCP shape) beside `GET /connectors/connections?capability=send_email` (capability shape) — **SEED-200's line, in a browser** |
| **9** | the footer, verbatim | ✅ | **`🔒 mcp.deepwiki.com`** · contains `mcp.deepwiki.com` ✔ · contains `slack.com/api` ✘ |
| 9b | the grant switch before a tool is chosen | ✅ | absent — the control is scoped to the SELECTED tool |
| **10** | the ORG-ADMIN discover path | ✅ | `POST /connectors/connections/7ca5e114…/discover`, `discoveryError: null`, tools `["", "ask_question","read_wiki_contents","read_wiki_structure"]` |
| 11 | tool chosen (`read_wiki_structure` — one required string arg) | ✅ | badge `data-granted="false"`, `"Permission Not Granted…"`; toggle present, `aria-checked="false"`, label `Allow this tool on this connection`; scope note rendered in full; `adminOnlyNote: null` |
| **12** | **THE DENY CASE, DRIVEN FIRST** | ✅ | see below |
| **13** | the grant flipped ON | ✅ | see below |
| **14** | the granted run | ✅ | see below |
| **15** | A1 at T4 — the shape switch read from the SAVED definition | ⚠ **falsified in the OPPOSITE direction from T1** | see below |
| **16** | the publish path | ⛔ **BLOCKED — cause identified, not this phase's** | see below |
| 17 | teardown, through the UI | ✅ | see below |

⚠ **UAT scoreboard scope, stated rather than skipped:** this phase touches neither streaming, the
agent loop, provider routing nor chat UI state, so the **4-axis cross-provider roster does NOT apply**
and no provider rows are owed. Said out loud rather than silently omitted.

⚠ **The live fixture differed from the plan's:** `connector_connections` holds **3** rows, not 2 — a
`Jira — KAN` (`create_ticket`) row exists beside `Slack-rag-test` and the DeepWiki row. That made
step 8's negative half **stronger**, and it is recorded because a plan's stated fixture is a claim
like any other.

### Step 12 — THE DENY CASE (SC#2b)

⚠ **`▶ Test Run` NAVIGATES AWAY FROM THE BUILDER TO THE RUN PAGE, AND AN `external_action` STEP
PAUSES FOR APPROVAL BEFORE IT ACTS.** The plan did not predict either; both were driven.

```
Step 1 of 3, "act", is about to run. This step is marked as needing your approval first.
The run is waiting here and will not continue until you answer.
[Approve this step] [Do not run it]  or  Reason  [Send Answer]
```

After approving: `✕ Reach outside · Failed — this step did not finish`. The step's own row:

```sql
select slug, status, output from workflow_phases where workflow_run_id='c2d3d41b-…';
```
```
act | failed | {"text": "Tool execution refused: Tool 'read_wiki_structure' is not granted
                        permission on connection 'DeepWiki (206.1 UAT)'.",
               "failure": "tool 'read_wiki_structure' refused: permission not granted"}
```

⚠ **THE PLAN'S OWN PRESCRIBED AUDIT QUERY RETURNED ZERO ROWS WHILE THE RECEIPT WAS THERE ALL ALONG —
AND THIS IS THE MOST TRANSFERABLE FINDING OF THE PLAN.**

```sql
-- the plan's form. Returns 0 rows. NOT because the receipt is missing.
select … from harness_audit
 where event_type='tool_refused' and metadata->>'connection_id' = '7ca5e114-…';
```
```sql
select jsonb_typeof(metadata), count(*) from harness_audit group by 1;
--  string | 2906        ← 2,906 of 2,906. There is no other value.
```

`harness_audit.metadata` is a **jsonb STRING SCALAR** — the same double-encoding trap already recorded
for `workflow_definitions.definition`, `workflow_phases.output` and two others. `->>'key'` on a string
scalar cannot match, silently. **The working query, which is what future work should use:**

```sql
select created_at, event_type, metadata from harness_audit
 where event_type='tool_refused'
   and (metadata #>> '{}')::jsonb ->> 'connection_id' = '7ca5e114-c5cd-4f7f-8c08-8885278bf42d'
 order by created_at desc limit 3;
```
```
2026-08-25T07:19:30.139769+00:00  tool_refused
  {"phase": "act", "connection_id": "7ca5e114-c5cd-4f7f-8c08-8885278bf42d",
   "tool_name": "read_wiki_structure", "reason": "permission_denied"}
```

⚠ **AND THE FILTER MATTERS EXACTLY AS D-206.2-06 WARNED.** `tool_refused` is a SHARED `event_type`:
**8 rows** carry it and only **1** carries a `connection_id`; the other 7 are the agent tool-whitelist
path with a completely different shape (`{"tool": "workspace_write", "allowed": ["search_documents"]}`).
Filtering on the name alone would have "found" seven rows about something else.

### Step 13 — THE GRANT FLIP, and step 13b — THE MERGE PROVED

The first flip's PATCH body, captured from the network log:

```
PATCH http://localhost:8000/connectors/connections/7ca5e114-…/grants
body: {"read_wiki_structure":true}
200  {"id":"7ca5e114-…","tool_grants":{"read_wiki_structure":true},…}
```
```sql
select tool_grants from connector_connections where id='7ca5e114-…';
--  {'read_wiki_structure': True}
```
UI: `✎ Allowed`, `data-grant-write="idle"`, badge `data-granted="true"`.

⚠ **THAT READING DOES NOT DEMONSTRATE A MERGE, AND SAYING SO IS THE POINT.** The connection's
`tool_grants` was `{}`, so the payload carries exactly one key — **byte-identical to what the naive
`{[tool]: next}` lost-update bug would have sent.** So a second reading was driven, granting a SECOND
tool:

```
body: {"read_wiki_structure":true,"read_wiki_contents":true}     ← ✎ Allowed
--  DB: {'read_wiki_contents': True, 'read_wiki_structure': True}
```

and then revoking it again, which is the destructive direction:

```
body: {"read_wiki_contents":false,"read_wiki_structure":true}    ← ✎ Not allowed
```

**Under the naive bug the second body would have been `{"read_wiki_contents":true}` and
`read_wiki_structure` would have been WIPED.** T-206.2-04-T1 is mitigated and **measured**, in both
directions, with the receipt naming its direction each time.

⚠ **THE NO-OPTIMISTIC-FLIP PROPERTY WAS NOT OBSERVABLE AT T4, AND THAT IS RECORDED RATHER THAN
CLAIMED.** Sampled 120 ms after the press, the local backend had already answered: `aria-checked`
read `true`, `data-grant-write` read `idle`. The property is asserted **deterministically at T1**
(a never-resolving promise: `aria-checked` unchanged, `data-grant-write="pending"`, `aria-busy="true"`,
`Saving…` in a `role="status"`). A green browser reading here would have been luck, not evidence.

### Step 14 — THE SEND (SC#3)

`✓ Reach outside · wrote an answer`, 22s.

```sql
select created_at, metadata from harness_audit
 where event_type='external_action_sent' order by created_at desc limit 1;
```
```
2026-08-25T07:25:21.122466+00:00
{"capability": "mcp", "connection_id": "7ca5e114-c5cd-4f7f-8c08-8885278bf42d",
 "destination_host": "https://mcp.deepwiki.com/mcp", "raw_status": 200,
 "phase": "act", "phase_index": 0}
```

⚠ **THE RECEIPT DOES NOT CARRY THE TOOL NAME, AND THIS SUMMARY DOES NOT CLAIM IT DOES.**
⚠ **`capability: "mcp"` is a metadata STRING in an audit row, not a `connector_connections.capability`
value.** It was not "fixed".

The step's own output is the remote server's real answer, truncated here only for length:

```
act | completed | {"text": "Available pages for facebook/react:\n\n- 1 React Repository Overview\n
  - 1.1 Repository Structure and Packages\n  - 1.2 Feature Flags System\n- 2 Core Reconciler
  Architecture\n  - 2.1 Fiber Work Loop and Scheduling\n  - 2.2 Hooks Implementation\n …"}
```

⚠ **A REAL REMOTE CALL LEFT THE MACHINE.** `https://mcp.deepwiki.com/mcp` answers unauthenticated and
`read_wiki_structure` on a public repository is a READ. One call, against the fixture 206.1
deliberately left in place. Said out loud, not glossed.

### Step 15 — RESEARCH ASSUMPTION A1 AT T4 · ⚠ FALSIFIED IN THE OPPOSITE DIRECTION FROM T1

`206.2-03` proved at T1 that `{k: undefined}` leaves the key present in memory and `JSON.stringify`
**drops it from the wire**. Read from the SAVED row after pressing the capability segment in a real
browser and saving:

```sql
select (definition #>> '{}')::jsonb -> 'phases' -> 0 -> 'config'
  from workflow_definitions where slug='vendor-renewal-brief-a49f6398';
```
```json
{"tool_args": {}, "tool_name": null, "capability": null,
 "phase_type": "external_action", "connection_id": null, "available_tools": []}
```

**Both keys SURVIVE, normalised to empty values**, and so does `available_tools`. Something between
the store and the row re-inflates the defaults — almost certainly the phase-config model. The MCP
direction reads symmetrically before the switch:

```json
{"tool_args": {"repoName": "facebook/react"}, "tool_name": "read_wiki_structure",
 "capability": null, "phase_type": "external_action",
 "connection_id": "7ca5e114-…", "available_tools": ["read_wiki_structure"]}
```

⚠ **IT IS HARMLESS ONLY BECAUSE THE EXECUTOR BRANCHES ON TRUTHINESS.** `phase_types.py:2287` reads
`if mcp_tool_name:`, and `None` is falsy, so a cleared step correctly falls through to the capability
branch. **A future refactor to `if "tool_name" in config:` would silently resurrect the MCP branch on
a capability-shaped step, and nothing in either language currently watches for that.** Recorded as an
invariant in `ExternalActionSection.tsx`'s ledger section.

### Step 16 — THE PUBLISH PATH · ⛔ **SC#3b IS NOT MET, AND IT IS NAMED RATHER THAN ABSORBED**

**Which path was driven, stated plainly, because the plan requires it:**

- **SC#3 (the run) was driven through ▶ Test Run — BOTH directions, deny first.** A green Test Run is
  **not** reported here as "MCP steps work end to end".
- **SC#3b (publish) was driven through the REAL GAUNTLET**, not inferred from the run. It ran, and
  **stage 2.6 — the exact stage `206.2-01` fixed — PASSED with the MCP step present:**

```
✓Owner  ✓Valid  ✓Goal  ✓Structure  ✓Pause  ✓Grounding   Golden run  Citations  Judge  Commit
⛔ Publish was blocked — nothing was published.
published false · version null · golden_run_id null
blocked_stage  golden_run_error
named_failures ["golden run could not complete: None"]
```

- **Corroborated independently through the validate route** — the single source of validation truth
  (`POST /workflows/validate`), on the real saved definition, with a **non-vacuity control**:

| | request | response |
|---|---|---|
| A | the definition AS SAVED (MCP tool `read_wiki_structure`) | `{"ok": true, "verdicts": []}` |
| B | ⚠ **NOT A CONTROL** — a bogus name in BOTH `tool_name` and `available_tools` | `{"ok": true, "verdicts": []}` — because `ExternalActionPhaseConfig`'s D-03 validator REPLACES `available_tools` with `[tool_name]` on parse, so the injected value never survives. **Recorded as a failed control, not as a result.** |
| C | a name in `available_tools` that is not the phase's own `tool_name` | `{"ok": true, "verdicts": []}` — same normalisation |
| **D** | **the SAME MCP name on an `llm_agent` phase** | **`{"ok": false, "verdicts":[{"code":"unregistered_tool","phase":"gather-renewals","message":"phase 'gather-renewals' references a non-registered tool 'read_wiki_structure'","severity":"error"}]}` — THE FENCE IS NOT VACUOUS, and CR-01's `external_action`-only scope holds** |

- The builder's live static-check tray, with the MCP step present, read
  **`▸ Nothing to fix — the static checks pass · checked by the server`**.

**The cause of the block, and it is not this phase's:** an `external_action` step ALWAYS stops for
approval — the panel says *"This step reaches outside your workspace, so it always stops and asks you
first. That cannot be switched off."* The golden run is UNATTENDED, so it waits forever. Golden run
`5d67d004-acc6-44b4-95f3-35528868433d` was created, started `act`, and sat `active`; the publish
service gave up. **`published: false` — nothing was published and nothing was mutated.**

⚠ **A SECOND DEFECT FOUND BY THE SAME DRIVE:** the verdict reports `golden_run_id: null` and explains
*"this blocked before stage 3, so there is no run to open. (A pre-run block never has a run link.)"* —
**both halves are false here.** It blocked INSIDE stage 3 and the run exists. And `named_failures`
reads `"golden run could not complete: None"`, so the named failure names nothing.

**Both are planted as `SEED-201`** with the three candidate fixes and their trade-offs.

### Step 17 — TEARDOWN, through the UI where a UI path exists

| What | How | Result |
|---|---|---|
| the grant | reverted through the **same switch**, both tools | `✎ Not allowed` ×2; DB `{'read_wiki_contents': False, 'read_wiki_structure': False}` |
| the added step | removed through the canvas `✕`, then **Save draft** | draft back to `['gather-renewals', 'write-brief']` — its original two phases |
| the orphaned golden run | cancelled through the run page's **Stop** | `⊘ Cancelled` |
| the run started to prove the deny case | cancelled through **Stop** | `⊘ Cancelled` |

**Deliberately left behind, and named:**
- the **DeepWiki fixture row itself** — 206.1 left it on purpose and this plan does not remove it.
- `tool_grants` reads `{'read_wiki_contents': False, 'read_wiki_structure': False}` rather than the
  original `{}`. ⚠ **Semantically identical** (`grants.get(t) is True` denies a missing key and a
  `false` key alike) but **NOT byte-identical**, and the difference is stated rather than rounded off.
- **3 run rows** (`c2d3d41b` cancelled, `d2b4ad72` completed, `5d67d004` cancelled) and their threads.
- `discovered_tools` was refreshed by the Discover press — same three tools.

---

## Deviations from Plan

**Four, all declared. Two are corrections to the plan's own text, one is a design deviation with a
measured reason, and one is a criterion that was unsatisfiable at its own base.**

---

**1. [Rule 1 — the plan's `unknown` arm removes a node the same plan requires to stay] FOUR audience
arms, not three: `unknown` splits into `no-provider` and `probing`.**

- **Found during:** Task 1, before any code was written.
- **Issue:** the plan's `<behavior>` says the unknown arm shows *"nothing at all — no switch, no
  button, no sentence"*, with `unknown` defined as `org === null` **or** `org.loading === true`.
  That is internally inconsistent with **three** of the plan's own acceptance criteria and with
  **two shipped test cases**: the UI-SPEC's T1 contract row requires *"the unknown arm adds **zero**
  new nodes in a provider-less render — assert a node COUNT or an `innerHTML` identity"*, and
  `McpToolPicker.test.tsx:90-95` and `:135-156` both assert `mcp-discover-btn` is present with **no
  `OrgProvider` mounted**. Removing the Discover button when `org === null` is a **removal**, not an
  addition of nothing — identity is then impossible and both shipped cases go red, against a
  criterion demanding a zero-deletion diff.
- **Fix:** `audienceOf()` returns four values. `admin` gets everything. A **measured** `member` loses
  both write affordances and gains the sentence. `probing` (a real caller whose org probe is in
  flight) **also loses the Discover button** — which is what actually honours AR-01's hard rule that
  *a non-admin must never see a Discover button that 403s*, since during the probe we do not yet know
  they are not one. `no-provider` (a null context) renders **exactly what it rendered before this
  phase**.
- **Why this is stronger, not weaker, than the plan's three arms:** in production `OrgProvider` wraps
  the whole app, so `org === null` is **not a production state at all** — it is tests and storybook.
  The only unmeasured state a real person can be in is `probing`, and that arm hides the button. The
  plan's wording would have paid a shipped-node removal in six suites to protect a state no user can
  reach.
- **Asserted:** a provider-less render's element count is **33 = 33** against the pre-phase component
  (captured before any source edit), with all five new testids absent, both shipped nodes present, and
  a **non-vacuity** case proving the admin render has strictly more nodes.
- **Files modified:** `frontend/src/components/workflows/McpToolPicker.tsx`
- **Commit:** `58260911`

---

**2. [Rule 1 — TWO acceptance greps were UNSATISFIABLE AT THEIR OWN BASE.]** The 187-24 trap, and this
is its **thirteenth** recorded firing on this codebase.

- **Found during:** Task 1's acceptance pass.
- **Issue:** the plan asks for `grep -c "useOrgOptional" McpToolPicker.tsx` **== 1**. A file that
  IMPORTS and CALLS a symbol contains it at least **twice**; with the docblock explaining *why*
  `useOrg` is forbidden it reads **4**. Separately, the first draft's own docblock spelled
  `role="switch"` twice while explaining AR-06, taking that count to **3** against a criterion of `1`.
- **Fix:** the prose was rewritten to name the concepts without spelling the tokens (*"its switch
  role"*, *"a NATIVE button carrying the switch role"*), which is the discipline the trap exists to
  teach — and the criteria are restated in satisfiable form with both readings recorded:

  | Criterion | plan's form | measured | corrected form |
  |---|---|---|---|
  | the org seam is the non-throwing one | `useOrgOptional` == 1 | **4** (import + call + 2 prose) | **import-scoped**: `grep -cE '^import \{ useOrgOptional'` == **1**, and `grep -c "useOrg("` == **0** ✅ |
  | one native switch | `role="switch"` == 1 | 3 → **1** after the rewrite | ✅ as written, once the file stopped spelling its own needle |

- **Files modified:** `frontend/src/components/workflows/McpToolPicker.tsx`
- **Commit:** `58260911`

---

**3. [Rule 1 — leg (b)'s first positive control was WRONG and was corrected by observing it fail.]**

- **Found during:** Task 2.
- **Issue:** the plan's positive control is *"run the same render with the row's `mcp_server_url`
  stripped and assert `queryByTestId("mcp-tool-picker")` is `null`"*. Driven, it went red at the
  BINDING step, not the assertion: the MCP arm's own shape filter (`Boolean(row.mcp_server_url)`)
  drops the row **before the list is built**, so there is no option to select and the field renders
  its empty state.
- **Fix:** the control asserts the observable consequence one step earlier — `connection-picker-empty`
  with `data-empty-reason="none"`, no select at all, no tool picker — and a **second** positive
  control was added on the CAPABILITY shape, where a row really is bound and the tool picker still
  does not mount. Both are recorded in the file with the wrong first draft kept above the correction,
  because the useful part is *why* it was wrong.
- **Files modified:** `frontend/src/components/workflows/McpToolPicker.reachability.test.tsx`
- **Commit:** `c99dcc4a`

---

**4. [Rule 3 — a measured Vite behaviour forced the self-source read to change shape.]**

- **Found during:** Task 2.
- **Issue:** leg (b)'s *"no component prop is constructed in this file"* case read its own source via
  `import.meta.glob`. It failed with `AssertionError: expected undefined to be defined` — **Vite
  EXCLUDES THE IMPORTING FILE FROM ITS OWN GLOB.**
- **Fix:** a static `import selfSource from "./McpToolPicker.reachability.test.tsx?raw"`, with the
  measurement recorded at the site. ⚠ The needle is **assembled at runtime** (`"connection" + "={"`)
  with its own positive control, because spelling it whole would make the file contain the token it
  counts — the same trap as deviation 2, one file over.
- **Files modified:** `frontend/src/components/workflows/McpToolPicker.reachability.test.tsx`
- **Commit:** `c99dcc4a`

---

**A declined change, recorded so it is a decision rather than an omission:** the plan's task-2
criterion *"the pinned FILE count is `+2`"* is met (`111/111 → 113/113`), and **no `TARGETS` edit
accompanies the two pins.** That is the two-knob rule satisfied by MEASUREMENT rather than by adding
an entry: `grep -n "src/components/workflows" scripts/vitest-count-gate.cjs` shows the DIRECTORY entry
at `:3036` that already makes both files RUN, and the script's own comment refuses a redundant
file-level entry beside it because it would *"state a dependency that is not real"*. The proof both
files ran before being pinned is the gate's own printed column: `McpToolPicker.test.tsx — 12 new`.

---

## ⚠ THE TWO NON-CHANGES `206.2-03` FLAGGED — both CONFIRMED, neither closed

`206.2-03` flagged two deliberate omissions so the first UAT round would not report them as bugs.
**Both were seen in the driven session and both are confirmed as deliberate.**

| What a tester sees | Confirmed in the drive | Verdict |
|---|---|---|
| **An MCP step shows NO consequence sentence** under the shape control | ✅ Seen: the panel's *"What it changes outside this workflow"* region reads **`NEEDS ARMING`** and no capability sentence. The step face reads *"Reach outside"* with no consequence line | **CONFIRMED DELIBERATE.** `OutsideChangeLine` renders nothing for a step with no capability, and its own docblock argues why: *"A card that printed a blank consequence row would be byte-indistinguishable from one whose lookup failed, and on a governance surface those two readings are opposite."* ⚠ **Re-open trigger UNCHANGED and still un-fired:** *the wire gaining a read/write hint for a discovered tool (an `annotations` key)*. **Re-measured 2026-08-25 against the LIVE DeepWiki `tools/list` payload: still no `annotations` key on any of the three tools.** So the trigger is not merely unfired — it is measurably still false |
| **The canvas card reads *"Reach outside"* and the effect banner reads `CHANGES SOMETHING OUTSIDE`, even for a READ-ONLY tool** | ✅ Seen verbatim in the canvas: `Reach outside · Stops for your approval before it acts outside · CHANGES SOMETHING OUTSIDE`, on a step bound to `read_wiki_structure` — which is a read | **CONFIRMED DELIBERATE, and now with a live example of the overstatement.** `derivedFace` falls through to the type sentence and `nodeEffectBanner` keys on the phase TYPE, not the tool. Threat `T-206.2-03-R1`, disposition **accept, flagged**. ⚠ Its re-open trigger (*`SEED-199`'s canvas-grammar work starting*) is unchanged — but note it now has the SAME root cause as the row above: **the wire carries no read/write hint**, so both would be answered by one `annotations` change rather than two |

⚠ **A THIRD non-change is added to the list by this drive, because the first UAT round will otherwise
report it too:** the approval checkpoint's own text says *"Step 1 of 3, \"act\", is about to run…"* and
offers *Approve this step / Do not run it* — **it never names the TOOL or the DESTINATION.** A person
approving an outbound call is not told which tool on which host. That is not this phase's to change
(the checkpoint is D-04 / Phase 185 surface), but it is worth someone deciding on rather than
discovering.

---

## What this plan does NOT claim

- ⚠ **It does not claim an MCP workflow can be PUBLISHED.** It cannot — see step 16 and `SEED-201`.
  What is proved is that the stage `206.2-01` fixed now passes, and that the block is one stage later
  and shape-independent.
- ⚠ **It does not claim the `external_action_sent` receipt names the tool.** It does not.
- ⚠ **It does not claim the no-optimistic-flip property was observed in a browser.** It was not; it is
  asserted at T1 and the browser sample is recorded as inconclusive.
- ⚠ **It does not claim a member really receives 403 from `PATCH /grants`.** RESEARCH assumption A6
  is still **UNFALSIFIED** — the member arm is proved at T1 with a mocked provider, and no
  non-admin identity was driven. The UI does not key its error branch on a status nobody drove.
- ⚠ **It does not claim the lost-update hazard is eliminated.** It is a read-modify-write over a client
  snapshot; two admins in two browsers can still clobber each other. The mitigation is the SENTENCE,
  and it is rendered permanently beside the control.
- **`frontend/src/lib/api.ts` is not edited** — 0 files across all four waves, measured. Phase 207 owns it.
- **The backend is untouched by this plan** — 0 files. Wave 1 owns the only backend change in the phase.

## Threat Flags

None new. This plan's register is discharged:

- **`T-206.2-04-T1`** (lost update) **mitigated and MEASURED** — the full merged map from the
  server-owned prop, `if (busy) return`, no optimistic flip, the response row adopted upstream.
  Asserted at T1 by SET EQUALITY and **proved at T4 in both directions**.
- **`T-206.2-04-E1`** (a member reaching an admin write) **mitigated (render) / accepted
  (enforcement)** — both affordances REMOVED for a measured member and for a caller mid-probe. ⚠ The
  wall is `require_org_manage` plus RLS; no test simulates either layer, and none claims to.
- **`T-206.2-04-E2`** (a workflow calling an ungranted tool) **mitigated and DRIVEN FIRST** — the deny
  case was observed before any grant existed, with its receipt filtered by `connection_id`.
- **`T-206.2-04-I1`** (a credential reaching the browser) **mitigated (inherited)** — no new field
  reaches `ConnectorConnection`; the PATCH response body was inspected and carries no secret.
- **`T-206.2-04-I2`** (an existence oracle) **accepted (inherited)**.
- **`T-206.2-04-T2`** (remote text as UI) **mitigated** — tool names and descriptions render as escaped
  React children; no `dangerouslySetInnerHTML` was added. The live DeepWiki description
  (*"Get a list of documentation topics for a GitHub repository."*) rendered as text.
- **`T-206.2-04-R1`** (a grant change with no receipt) **mitigated** — two directions, two receipts,
  both observed in the browser.
- **`T-206.2-04-D1`** (a real remote call) **accepted, DISCLOSED** — one `read_wiki_structure` call on a
  public repository, unauthenticated, said out loud above.
- **`T-206.2-04-SC`** never fired — no package installed, both manifests measure **0**.

## Self-Check: PASSED

- `frontend/src/components/workflows/McpToolPicker.tsx` — FOUND
- `frontend/src/components/workflows/ConnectionPicker.tsx` — FOUND
- `frontend/src/components/workflows/McpToolPicker.test.tsx` — FOUND
- `frontend/src/components/workflows/McpToolPicker.reachability.test.tsx` — FOUND
- `frontend/src/pages/WorkflowBuilderPage.test.tsx` — FOUND
- `frontend/src/pages/WorkflowBuilderPage.describe.test.tsx` — FOUND
- `frontend/src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx` — FOUND
- `frontend/src/pages/WorkflowBuilderPage.session.test.tsx` — FOUND
- `scripts/vitest-count-gate.cjs` — FOUND
- `CLAUDE.md` — FOUND · `docs/HOT-FILE-LEDGER.md` — FOUND · `.planning/ROADMAP.md` — FOUND
- `.planning/seeds/SEED-201-external-action-workflow-cannot-publish-golden-run-waits-forever.md` — FOUND
- commit `58260911` — FOUND
- commit `c99dcc4a` — FOUND
- commit `cdd31e4f` — FOUND
