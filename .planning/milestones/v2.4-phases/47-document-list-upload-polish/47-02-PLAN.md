---
phase: 47-document-list-upload-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/documents.py
autonomous: true
requirements:
  - DOC-05
must_haves:
  truths:
    - Uploading a file larger than 50 MB returns a 422 error with message "File too large. Maximum size is 50 MB."
    - The error propagates to the frontend and displays as red text in the upload area
    - Files at or below 50 MB continue to upload normally
  artifacts:
    - path: backend/app/api/documents.py
      provides: 50 MB size validation
      contains: "len(raw) > 50 * 1024 * 1024"
      exports: []
  key_links:
    - from: backend/app/api/documents.py
      to: frontend/src/components/ingestion/DocumentUpload.tsx
      via: HTTP 422 detail → api.ts Error → batch.errors
      pattern: "File too large"
---

<objective>
Add a 50 MB per-file size limit to the document upload endpoint, with a specific error message that propagates to the frontend through the existing error chain.

Purpose: Currently there is no explicit file size limit check. Supabase Storage has a default limit, but the backend should validate size early and return a clear, specific error message so users know why their upload failed.
Output: One new validation block in documents.py upload endpoint.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/47-document-list-upload-polish/47-CONTEXT.md
@backend/app/api/documents.py

## Decisions to implement
- **D-04:** Add 50 MB size check in `upload_document()` immediately after `raw = await file.read()` and before duplicate/storage logic.
- **D-05:** No frontend-only validation needed — backend check is sufficient; error propagates via existing `err.detail` → `batch.errors` path.

## Existing error propagation chain
1. Backend raises `HTTPException(status_code=422, detail="...")`
2. `frontend/src/lib/api.ts` catches and throws `new Error(err.detail)`
3. `useDocuments.ts` `upload()` propagates the Error
4. `DocumentUpload.tsx` pushes to `batch.errors` and displays as `<p className="text-sm text-destructive">{err}</p>`

## Insertion point
After line 154 (`raw = await file.read()`) and before line 155 (`if len(raw) == 0:`).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 50 MB file size validation to upload endpoint</name>
  <files>backend/app/api/documents.py</files>
  <action>
1. Open `backend/app/api/documents.py`.
2. Locate the `upload_document()` function (starts at line 130).
3. Find the line `raw = await file.read()` (around line 154).
4. Immediately AFTER that line and BEFORE the empty-file check (`if len(raw) == 0:`), insert:
   ```python
   MAX_FILE_SIZE = 50 * 1024 * 1024  # 50 MB
   if len(raw) > MAX_FILE_SIZE:
       raise HTTPException(
           status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
           detail="File too large. Maximum size is 50 MB.",
       )
   ```
   - Use the exact error message: `"File too large. Maximum size is 50 MB."`
   - Use `status.HTTP_422_UNPROCESSABLE_ENTITY` (already imported at line 8).
   - Keep the constant local to the function (no module-level constant needed for a single check).
5. Verify the empty-file check (`if len(raw) == 0:`) still comes AFTER the size check, so empty files get the existing "File is empty" message, not the size message.
  </action>
  <verify>
    <automated>grep -n "File too large" backend/app/api/documents.py</automated>
  </verify>
  <done>
    - documents.py contains size check after `raw = await file.read()` and before empty-file check
    - Exact error message: "File too large. Maximum size is 50 MB."
    - Uses HTTP 422 status code
    - Empty-file check remains in place and fires before size check for empty files
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify backend error propagation to frontend</name>
  <files>frontend/src/lib/api.ts, frontend/src/components/ingestion/DocumentUpload.tsx</files>
  <action>
1. Confirm the existing error chain will surface the new message:
   - Open `frontend/src/lib/api.ts` and verify that upload errors throw `new Error(err.detail)` (should already exist around line 233-235).
   - Open `frontend/src/components/ingestion/DocumentUpload.tsx` and verify `batch.errors` renders as red text (already exists at lines 119-121).
2. No code changes needed in these files — the propagation chain is already wired. This task is verification-only.
3. If the chain is NOT as expected (e.g., api.ts uses `err.message` instead of `err.detail`), note the discrepancy and update the plan, but do NOT change working code.
  </action>
  <verify>
    <automated>grep -n "err.detail" frontend/src/lib/api.ts | head -3</automated>
  </verify>
  <done>
    - api.ts extracts `err.detail` from backend errors
    - DocumentUpload.tsx displays `batch.errors` as red text
    - New "File too large" message will surface automatically
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→API | Untrusted file upload crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-02 | Denial of Service | Upload endpoint | mitigate | 50 MB limit prevents memory exhaustion from oversized uploads |
</threat_model>

<verification>
- Backend starts without syntax errors (`python -m py_compile backend/app/api/documents.py`)
- Uploading a file >50 MB returns 422 with exact detail message
- Uploading a file <=50 MB proceeds normally
- Frontend displays "File too large. Maximum size is 50 MB." in red text when a large file is rejected
</verification>

<success_criteria>
- [ ] 50 MB size check exists in upload_document() after file read
- [ ] Files >50 MB return HTTP 422 with message "File too large. Maximum size is 50 MB."
- [ ] Files <=50 MB upload normally
- [ ] Empty files still return "File is empty" (size check does not interfere)
- [ ] Frontend displays the error message without code changes in the UI layer
</success_criteria>

<output>
After completion, create `.planning/phases/47-document-list-upload-polish/47-02-SUMMARY.md`
</output>
