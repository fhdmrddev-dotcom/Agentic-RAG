---
phase: 112-metadata-enrichment-document-detail-panel-manual-edit
plan: 04
type: execute
wave: 3
depends_on: [112-01, 112-03]
files_modified:
  - frontend/src/components/metadata/DocumentDetailPanel.tsx
  - frontend/src/components/metadata/InlineEdit.tsx
  - frontend/src/components/metadata/InlineEdit.test.tsx
  - frontend/src/components/metadata/DocumentDetailPanel.a11y.test.tsx
  - frontend/src/pages/IngestionPage.tsx
  - frontend/src/components/ingestion/DocumentList.tsx
autonomous: true
requirements: [META-02, META-05]
user_setup: []

must_haves:
  truths:
    - "Clicking a document row opens a right-side push/split detail panel (desktop, list still visible); mobile (<768px) renders a bottom-sheet"
    - "The Metadata section renders inside a PanelSection accordion (open by default) showing each field's value + ConfidenceChip"
    - "A field is editable inline (click → type-appropriate control; Enter saves, Esc cancels; editing an empty field adds a value); a 'Saved · audit logged' receipt appears ONLY after a successful PATCH"
    - "The panel renders the union of (7 built-ins) + (enabled custom field defs from /metadata-fields), ignoring _-prefixed keys; custom fields edit via the same PATCH"
    - "The inline MetadataPanel in DocumentList is retired (one honest surface); version-history inline-expand still works"
    - "An automated aXe check reports no WCAG 2.1 AA failures; panel-scoped AA tokens only; the panel + accordion + edit are keyboard-operable; reduced-motion still shows the save receipt"
  artifacts:
    - path: "frontend/src/components/metadata/DocumentDetailPanel.tsx"
      provides: "Push/split detail panel: PanelSection Metadata accordion, union field render, ConfidenceChip per field, mobile bottom-sheet"
      contains: "PanelSection"
    - path: "frontend/src/components/metadata/InlineEdit.tsx"
      provides: "Type-appropriate inline edit control (FolderNode pattern) with Enter/Esc/blur + Saved receipt"
      contains: "onKeyDown"
    - path: "frontend/src/pages/IngestionPage.tsx"
      provides: "selectedDocId state + push/split grid minmax(0,1fr) 430px + panel mount"
      contains: "selectedDocId"
    - path: "frontend/src/components/ingestion/DocumentList.tsx"
      provides: "row-click onSelect; inline MetadataPanel retired; version expand kept"
      contains: "onSelect"
  key_links:
    - from: "DocumentDetailPanel inline edit commit"
      to: "updateDocumentMetadata(id, field, value)"
      via: "PATCH on Enter/blur, then loadDocuments() reconcile"
      pattern: "updateDocumentMetadata"
    - from: "DocumentDetailPanel custom-field render"
      to: "listMetadataFields() filtered enabled===true"
      via: "GET /metadata-fields on mount"
      pattern: "listMetadataFields"
    - from: "IngestionPage row click"
      to: "DocumentDetailPanel docId"
      via: "selectedDocId state + onSelect prop into DocumentList"
      pattern: "onSelect|setSelectedDocId"
---

<objective>
Assemble the net-new document detail panel — the push/split shell, the Metadata `PanelSection` accordion, honest inline edit for built-in AND custom fields, the `IngestionPage` push/split wiring, and the `DocumentList` retire-inline-metadata + row-click — and prove WCAG 2.1 AA. This is the user-facing surface that makes META-02 (see confidence) + META-05 (edit, audited) real, and the shared shell Phases 117/118 will inhabit.

Purpose: Deliver the lived-experience panel on top of Plan 01's PATCH endpoint + Plan 03's ConfidenceChip/client methods. The honesty contract (Edited/Extracted/empty states, no signposts to unbuilt features) and the "Saved only after the write succeeds" receipt are load-bearing.
Output: `DocumentDetailPanel.tsx` + `InlineEdit.tsx` + their tests `InlineEdit.test.tsx`/`DocumentDetailPanel.a11y.test.tsx`; modified `IngestionPage.tsx` + `DocumentList.tsx`. (The InlineEdit + a11y Wave-0 tests are co-located here, with the components they exercise; Plan 01 owns only the backend stubs.)
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-metadata-enrichment-document-detail-panel-manual-edit/112-SPEC.md
@.planning/phases/112-metadata-enrichment-document-detail-panel-manual-edit/112-RESEARCH.md
@.planning/phases/112-metadata-enrichment-document-detail-panel-manual-edit/112-PATTERNS.md
@.planning/phases/112-metadata-enrichment-document-detail-panel-manual-edit/112-01-SUMMARY.md
@.planning/phases/112-metadata-enrichment-document-detail-panel-manual-edit/112-03-SUMMARY.md
@.claude/skills/sketch-findings-agentic-rag/sources/028-confidence-and-edit/GROUNDING.md
@.claude/skills/sketch-findings-agentic-rag/references/document-detail-panel.md

<interfaces>
<!-- Reuse targets + the mobile breakpoint, verified at file:line. Reuse DIRECTLY — no new primitives. -->

PanelSection (panel/PanelSection.tsx) — reuse DIRECTLY, no new accordion:
Props: { title, count?: PanelSectionCount, warn?: boolean, defaultOpen?: boolean, children }
A11y: head is a real <button id aria-expanded aria-controls>; body is <div role="region" aria-labelledby={headId}>; collapse = display:none (instant).
Metadata section: <PanelSection title="Metadata" warn={lowPlusEmpty>0} count={lowPlusEmpty} defaultOpen>{...fields}</PanelSection>

Inline edit pattern (ingestion/FolderNode.tsx:151-170):
```tsx
<input ref={editInputRef} value={editValue} onChange={...}
  onBlur={() => commit()}
  onKeyDown={(e) => {
    if (e.key === "Enter") { e.preventDefault(); commit() }
    if (e.key === "Escape") { e.preventDefault(); cancel() }
  }}
  className="px-1.5 py-0.5 text-sm bg-background border border-primary/30 rounded-md outline-none focus:ring-1 focus:ring-primary/40"
  autoFocus onClick={(e) => e.stopPropagation()} />
```

Mobile breakpoint — the project convention (WorkspacePanel.tsx:59-71): MOBILE_BREAKPOINT = 768, inline useIsMobile() hook (window.innerWidth < 768 + resize listener); WorkspacePanel.tsx:233 is the bottom-sheet branch analog. Reuse this exact pattern for the panel's mobile fallback.

IngestionPage current right column (IngestionPage.tsx:79,96,128-134):
```tsx
<div className="flex flex-row gap-6 flex-1 min-h-0">
  <div className="w-72 shrink-0">{/* FolderTree */}</div>
  <div className="flex-1 flex flex-col overflow-y-auto space-y-6">
    {/* DocumentUpload */}
    <DocumentList documents={documents} onDelete={deleteDoc} onRefresh={loadDocuments}
      folderId={selectedFolderId} currentUserId={user?.id ?? ""} />
  </div>
</div>
```
useDocuments() (hooks/useDocuments.ts) exposes documents + loadDocuments(). Realtime is best-effort (D-v2.5-03) → call loadDocuments() after an edit to reconcile.

DocumentList retire target — MetadataPanel function (DocumentList.tsx:32-86) + its expand branch (:408-414). KEEP the chevron/expanded/toggle machinery + VersionHistoryPanel branch (:415-426) for versions. isExpandable (:310) currently = hasMetadata || hasVersions → make it hasVersions only.

Plan 03 contracts (already built): ConfidenceChip {score?, source?}; updateDocumentMetadata(id, field, value); listMetadataFields(); DocumentMetadata._confidence/_source/[key]; MetadataFieldDef{field_key, field_type, options?, enabled, ...}.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build DocumentDetailPanel + InlineEdit + InlineEdit.test (Metadata section, honest states, custom fields)</name>
  <read_first>
    - frontend/src/components/panel/PanelSection.tsx (the accordion shell to reuse directly — props + APG a11y)
    - frontend/src/components/ingestion/FolderNode.tsx:151-170 (the inline-edit toggle/autoFocus/Enter-Esc-blur pattern to mirror)
    - frontend/src/components/panel/WorkspacePanel.tsx:59-71,233 (MOBILE_BREAKPOINT=768 + useIsMobile + bottom-sheet branch to reuse)
    - frontend/src/components/metadata/ConfidenceChip.tsx (the Plan-03 primitive to render per field)
    - .claude/skills/sketch-findings-agentic-rag/sources/028-confidence-and-edit/GROUNDING.md (the build spec: 5 field states, the trust-gutter spine, the "Saved · audit logged" receipt, the a11y checklist, default sort = schema order)
    - .claude/skills/sketch-findings-agentic-rag/references/document-detail-panel.md (the shared-shell design + what-to-avoid: no inert REL/CLASS/Versions stubs)
    - frontend/src/lib/api.ts (updateDocumentMetadata + listMetadataFields from Plan 03)
  </read_first>
  <action>
    Create `frontend/src/components/metadata/InlineEdit.tsx` — a type-appropriate inline edit control mirroring the FolderNode pattern. Props: `{ field, fieldType, value, onCommit }`. Click/Enter on the value `<button>` swaps to a control in place per `field_type` (Claude's-discretion mapping, LOCKED here per RESEARCH Open Q1/Q2):
    - `string` / `title` / `author` → `<input>` (h-8, `focus:ring-1 ring-primary/40`)
    - `summary` → `<Textarea>` (Cmd/Ctrl+Enter commits; plain Enter inserts newline)
    - `date` → `<input type="date">`
    - `enum` → `<Select>` over `options`
    - `topics` / array → comma-separated text input in v1 (RESEARCH Open Q2 recommendation: render as chips read-only, edit as comma text; upgrade to a pill editor later — do NOT block v1 on it)
    - `number` → `<input type="number">`; `boolean` → a two-option select
    Commit semantics: Enter commits if dirty; Esc cancels + restores focus to the trigger (useRef); guarded blur-commit (only if changed AND not Esc AND focus didn't move to a Save/Cancel control). Editing an EMPTY field adds a value (the empty state is a focusable dashed "Not extracted — add" button that opens the same control). Clearing to empty: pop the top-level key but the field stays a user override (per RESEARCH Open Q3 — the merge guard's else-branch already honors a cleared user field). On a successful commit, call `onCommit(field, value)`.

    Create `frontend/src/components/metadata/DocumentDetailPanel.tsx`. Props: `{ doc, onClose }` (doc is the selected `Document` with `.metadata`). On mount, `listMetadataFields()` → filter `enabled === true` client-side (matches `read_enabled_field_defs` semantics; RESEARCH Open Q5). Field set = union of the 7 built-ins + enabled custom defs — NEVER iterate raw `metadata` keys; ALWAYS ignore `_`-prefixed keys (`_confidence`, `_source`).

    Render the Metadata section as `<PanelSection title="Metadata" warn={lowPlusEmpty > 0} count={lowPlusEmpty} defaultOpen>` — NO new accordion primitive, and NO inert Relationships/Classification/Versions stubs (the shell is architecturally ready but ships ONLY the Metadata section — honesty: no signposts to unbuilt features; 117/118 add their sections later). Per-field row (grid `grid-cols-[2px_1fr]`, col 1 = decorative `aria-hidden` trust-gutter spine):
    - value + `<ConfidenceChip score={metadata._confidence?.[field]} source={metadata._source?.[field]} />`
    - `_source[field]==='user'` → InlineEdit shows the value, ConfidenceChip renders neutral "Edited" (Plan 03 handles this from `source`)
    - no `_confidence[field]` entry but a value present → ConfidenceChip renders neutral "Extracted"
    - absent field → the "Not extracted — add" affordance (InlineEdit empty state)
    - a Low value's TEXT reads tentative (italic + `text-panel-muted-foreground` + leading ⚠) — survives greyscale triage

    Edit commit flow: `onCommit` → `await updateDocumentMetadata(doc.id, field, value)`; on 200 → show the "🛡 Saved · audit logged" receipt (`role=status aria-live=polite`, `fadeSlideUp ~4s` gated by `prefers-reduced-motion` → instant render when motion off) AND `loadDocuments()` to reconcile (Realtime best-effort, D-v2.5-03). On error → keep edit mode, value preserved, `role=alert` "Couldn't save — your change wasn't recorded" (the honest inverse). The receipt's "audit logged" claim is honest ONLY because Plan 01's route writes the audit row — never show it on a failed/optimistic write.

    A11y: panel-scoped AA tokens ONLY (`--panel-status-done/-active`, `--panel-muted-foreground` 7.21:1, lightened red `hsl(0 80% 80%)`) — NEVER global `--muted-foreground` (3.59:1). Closing the panel restores focus to the trigger row. Mobile (`useIsMobile()` < 768) → render as a bottom-sheet (reuse the WorkspacePanel:233 branch shape) instead of the split track.

    Then create `frontend/src/components/metadata/InlineEdit.test.tsx` (the frontend Wave-0 home for AC10 — co-located with InlineEdit). Assert with @testing-library/user-event: click-to-edit swaps the value to a control, Enter saves (calls onCommit with the new value), Esc cancels (value unchanged + focus restored), and editing an empty/absent field adds a value.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/metadata/InlineEdit.test.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `DocumentDetailPanel.tsx` contains `PanelSection` with `title="Metadata"` and `defaultOpen`
    - `DocumentDetailPanel.tsx` calls `listMetadataFields()` and filters `enabled` (grep: `listMetadataFields` and `enabled` both present)
    - `DocumentDetailPanel.tsx` renders the union of builtins + custom defs and ignores `_`-prefixed keys (grep: a guard like `startsWith("_")` or an explicit builtins/custom list; NO `Object.keys(metadata)` raw iteration for the field set)
    - `DocumentDetailPanel.tsx` does NOT render any "Relationships"/"Classification"/"Versions" section literal (grep returns 0 — no inert stubs)
    - `DocumentDetailPanel.tsx` shows the "Saved · audit logged" receipt only inside the `await updateDocumentMetadata(...)` success branch (grep: the receipt string is not in an optimistic/pre-await position)
    - `InlineEdit.tsx` contains `onKeyDown` handling Enter (commit) + Escape (cancel) and a `type="date"`/`Textarea`/`Select` branch per field_type
    - `InlineEdit.test.tsx` exists and asserts click-to-edit, Enter, Esc, and add-empty
    - `vitest run src/components/metadata/InlineEdit.test.tsx` passes (AC10); `tsc --noEmit` clean
  </acceptance_criteria>
  <done>DocumentDetailPanel + InlineEdit + InlineEdit.test land: Metadata-only PanelSection accordion, union field render, per-field ConfidenceChip + honest states, type-appropriate inline edit with Enter/Esc/blur, custom fields via the same PATCH, "Saved · audit logged" only after a successful write, mobile bottom-sheet; InlineEdit test green + tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire IngestionPage push/split + retire DocumentList inline metadata + row-click</name>
  <read_first>
    - frontend/src/pages/IngestionPage.tsx:49,79,96,128-134 (the TooltipProvider + right-column layout to wrap in the push/split grid)
    - frontend/src/components/ingestion/DocumentList.tsx:32-86 (the MetadataPanel function to REMOVE), :230,266,310,334-343,408-426 (the expanded/toggle/isExpandable machinery + the metadata vs versions expand branches)
    - frontend/src/hooks/useDocuments.ts (loadDocuments() for post-edit reconcile)
    - .planning/phases/112-metadata-enrichment-document-detail-panel-manual-edit/112-CONTEXT.md (D-01: retire inline expand; mount at IngestionPage; mobile bottom-sheet)
  </read_first>
  <action>
    In `frontend/src/pages/IngestionPage.tsx`:
    - Add `const [selectedDocId, setSelectedDocId] = useState<string | null>(null)` at the page level.
    - Wrap the right column in a push/split grid `minmax(0,1fr) 430px` (desktop) so the list shrinks but stays visible; the 430px track mounts `<DocumentDetailPanel doc={selectedDoc} onClose={() => setSelectedDocId(null)} />` where `selectedDoc = documents.find(d => d.id === selectedDocId)`. When `selectedDocId === null`, render only the list (full width). On mobile (`useIsMobile()` < 768) the panel renders as a bottom-sheet (the panel owns that internally — pass the same props).
    - Pass `selectedDocId` + `onSelect={setSelectedDocId}` into `DocumentList`. Reuse the existing `<TooltipProvider>` (:49) — no new provider. Call `loadDocuments()` after an edit (the panel triggers it; ensure it's threaded).

    In `frontend/src/components/ingestion/DocumentList.tsx`:
    - REMOVE the `MetadataPanel` function (:32-86) AND its inline-expand branch (:408-414).
    - KEEP the chevron / `expanded` Set / `toggle(id)` machinery AND the `VersionHistoryPanel` branch (:415-426) — version history stays inline this phase (deferred to a later panel section, NOT 112).
    - Change `isExpandable` (:310) so the chevron shows for `hasVersions` ONLY (metadata no longer drives inline expand).
    - Add `onSelect?: (id: string) => void` + `selectedDocId?: string | null` to the props; wire the filename cell (:346) `onClick` → `onSelect?.(doc.id)` to open the detail panel (the chevron keeps toggling version history — distinct click targets per RESEARCH Open Q4). Add a `data-selected`/aria affordance on the selected row.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `IngestionPage.tsx` contains `selectedDocId` state and a grid with `minmax(0,1fr)` and `430px` track
    - `IngestionPage.tsx` mounts `DocumentDetailPanel` and passes `onSelect`/`selectedDocId` to `DocumentList`
    - `DocumentList.tsx` no longer contains a `MetadataPanel` function definition (grep `function MetadataPanel` / `const MetadataPanel` returns 0)
    - `DocumentList.tsx` STILL contains `VersionHistoryPanel` and the `expanded`/`toggle` machinery (versions inline-expand preserved)
    - `DocumentList.tsx` `isExpandable` references `hasVersions` and no longer ORs in `hasMetadata`
    - `DocumentList.tsx` props include `onSelect` and the filename cell calls `onSelect?.(doc.id)`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>IngestionPage gains selectedDocId + the push/split grid + the panel mount; DocumentList's inline MetadataPanel is retired while version-history inline-expand survives; row-click opens the panel, chevron toggles versions; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Prove WCAG 2.1 AA + the honest-states a11y contract (vitest-axe)</name>
  <read_first>
    - .claude/skills/sketch-findings-agentic-rag/sources/028-confidence-and-edit/GROUNDING.md (the a11y checklist: never-colour-alone, polite/assertive ARIA split, panel-scoped tokens, the "unscored field NEVER renders High" assertion, reduced-motion receipt-still-renders)
    - frontend/src/components/metadata/ConfidenceChip.tsx + DocumentDetailPanel.tsx + InlineEdit.tsx (the components under test)
    - any existing frontend *.a11y.test.tsx or vitest-axe usage in the repo (the import + setup convention)
  </read_first>
  <action>
    Create `frontend/src/components/metadata/DocumentDetailPanel.a11y.test.tsx` (the frontend Wave-0 home for AC11 — co-located with the panel) using `vitest-axe` (already a dep). Assert:
    - Rendering the panel with a representative doc (a High field, a Low field, an Edited field, an unscored Extracted field, and an absent field) produces NO aXe AA violations (`expect(await axe(container)).toHaveNoViolations()`).
    - The save receipt region is `role=status aria-live=polite`; the error region is `role=alert` (assertive). (Render the success/error paths via a mocked `updateDocumentMetadata`.)
    - Every ConfidenceChip state renders a visible WORD + an aria-hidden glyph (never colour-alone).
    - An unscored field (no `_confidence` entry) NEVER renders the text "High".
    - With `prefers-reduced-motion` simulated (matchMedia mock), the save receipt STILL renders (instantly, no animation gate hiding it).
    - The accordion head is a `<button aria-expanded aria-controls>` and the body is `role=region` (PanelSection reuse).
    Keep keyboard-reachability + greyscale-triage as documented manual-only items (VALIDATION.md Manual-Only) — the axe + role assertions are the automatable AC11 core.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/metadata/DocumentDetailPanel.a11y.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `DocumentDetailPanel.a11y.test.tsx` imports `vitest-axe` and asserts `toHaveNoViolations()` on the rendered panel
    - The test asserts the receipt region is `aria-live="polite"` and the error region is `role="alert"`
    - The test asserts an unscored field never renders "High" and every chip state has a visible word
    - The test asserts the receipt renders under a simulated `prefers-reduced-motion: reduce`
    - `vitest run src/components/metadata/DocumentDetailPanel.a11y.test.tsx` passes (AC11 automatable core green)
  </acceptance_criteria>
  <done>The a11y test proves no aXe AA violations on the panel, the polite/assertive ARIA split, never-colour-alone, never-"High"-unscored, and the reduced-motion receipt render; AC11's automatable core is green (keyboard sweep + greyscale stay manual per VALIDATION.md).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| panel inline edit → updateDocumentMetadata (PATCH) | The client sends `{field, value}` only; provenance + the audit row are server concerns (Plan 01). The panel must not claim "audit logged" before the write returns 200. |
| panel render of confidence/source | Display-only; the UX-integrity threat is fabricating authority or signposting unbuilt features. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-04-01 | Repudiation (UX integrity) | "Saved · audit logged" receipt shown before/without the write succeeding | mitigate | The receipt renders ONLY inside the `await updateDocumentMetadata(...)` 200 branch; the error path shows an honest `role=alert` "wasn't recorded". Verified by the a11y test's success/error path assertions + the grep AC in Task 1. |
| T-112-04-02 | Tampering (UX integrity) | Panel fabricating authority (green on override, "High" unscored) or signposting unbuilt 117/118 sections | mitigate | ConfidenceChip honest states (Plan 03) + the panel ships ONLY the Metadata section (no inert REL/CLASS/Versions stubs — grep AC). Verified by the never-"High" a11y assertion. |
| T-112-04-03 | Information disclosure | Panel rendering raw metadata keys (could surface internal `_source`/`_confidence` or stale keys) | mitigate | Field set = union of builtins + enabled custom defs; `_`-prefixed keys ignored; never `Object.keys(metadata)` for the field set. Verified by the grep AC in Task 1. |
| T-112-04-04 | DoS / accessibility regression | Panel failing WCAG AA (global `--muted-foreground` 3.59:1, colour-only chips, keyboard traps) | mitigate | Panel-scoped AA tokens only; vitest-axe `toHaveNoViolations`; APG accordion reuse; closing restores focus. Verified by Task 3. |
</threat_model>

<verification>
- `vitest run src/components/metadata` green (ConfidenceChip from Plan 03 + InlineEdit + a11y).
- `npx tsc --noEmit` clean; full frontend `npm test` net-new failures = 0 (SEED-056 baseline-checkout if counts shift).
- Manual-only (per VALIDATION.md, the G-4 lived-experience gate — NOT this plan's automated scope): desktop split-push + mobile bottom-sheet feel; greyscale Low distinguishability; full keyboard sweep; the end-to-end edit→PATCH→re-extract-preserve via the UI cross-checked against the `metadata.update` audit row on :54322.
</verification>

<success_criteria>
- Clicking a row opens the push/split panel (desktop) / bottom-sheet (mobile); the Metadata section renders inside a PanelSection accordion with per-field ConfidenceChips + honest states.
- Inline edit works for built-in AND custom fields (Enter/Esc/blur, empty-field add); "Saved · audit logged" appears only after a successful PATCH.
- The inline MetadataPanel is retired; version-history inline-expand survives.
- aXe reports no AA failures; the ARIA split + never-colour-alone + never-"High"-unscored + reduced-motion receipt are asserted.
</success_criteria>

<output>
After completion, create `.planning/phases/112-metadata-enrichment-document-detail-panel-manual-edit/112-04-SUMMARY.md`
</output>
