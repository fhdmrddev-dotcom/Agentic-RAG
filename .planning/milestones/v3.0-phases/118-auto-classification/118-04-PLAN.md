---
phase: 118-auto-classification
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/index.ts
  - frontend/src/lib/api.ts
  - frontend/src/App.tsx
autonomous: true
requirements: [CLASS-01, CLASS-03]
must_haves:
  truths:
    - "ClassificationRule + ClassificationSuggestion types exist; DocumentMetadata gains a _classification? field so doc.metadata._classification type-checks"
    - "Rule CRUD client fns (listRules/createRule/updateRule/deleteRule) exist; createRule's body OMITS is_global (server hard-sets it)"
    - "acceptClassification(docId) / dismissClassification(docId) client fns exist; Undo reuses the existing moveDocument(id, priorFolderId)"
    - "The 'would match N' preview reuses the EXISTING resolveAdHoc/resolveFilterCount client fn — NO new client fn, NO new backend endpoint"
    - "ActiveView union includes 'classification-rules'"
  artifacts:
    - path: "frontend/src/types/index.ts"
      provides: "ClassificationRule + ClassificationSuggestion + _classification on DocumentMetadata"
      contains: "ClassificationSuggestion"
    - path: "frontend/src/lib/api.ts"
      provides: "rule CRUD + accept/dismiss client fns"
      contains: "acceptClassification"
    - path: "frontend/src/App.tsx"
      provides: "ActiveView union extension"
      contains: "classification-rules"
  key_links:
    - from: "frontend api.ts createRule"
      to: "POST /classification-rules"
      via: "fetch (body omits is_global)"
      pattern: "classification-rules"
    - from: "frontend api.ts acceptClassification"
      to: "PATCH /documents/{id}/classification/accept"
      via: "fetch"
      pattern: "classification/accept"
---

<objective>
Ship the frontend interface seam Plans 05 + 06 implement against: the TypeScript types
(`ClassificationRule`, `ClassificationSuggestion`, and `_classification?` on `DocumentMetadata`),
the api client family (rule CRUD + accept/dismiss, mirroring the views/relationships
conventions), and the `ActiveView` union extension. Interface-first: defining these contracts
in one wave-1 plan means the two UI plans receive the types/client directly — no scavenger hunt.

Purpose: One leak-safe, convention-matching client surface so the on-doc plan (05) and the
rules-page plan (06) build against a fixed contract. Output: edited `types/index.ts`,
`api.ts`, `App.tsx`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-auto-classification/118-RESEARCH.md
@.planning/phases/118-auto-classification/118-PATTERNS.md

<interfaces>
<!-- Clone-targets + the backend contract from Plans 02/03. -->

Clone-targets (verbatim conventions — getAuthHeaders + throw-on-non-ok + 404-tolerant DELETE):
  - frontend/src/lib/api.ts:2038-2112 — the views client family (listViews/createView/updateView/deleteView).
    createView's body OMITS is_global (server hard-sets) — mirror that for createRule.
  - frontend/src/lib/api.ts:2125-2146 — resolveAdHoc / resolveFilterCount (the "would match N" count, count_only:true). REUSE — no new fn.
  - frontend/src/lib/api.ts:1971,1997 — moveDocument / updateDocumentMetadata (the PATCH shape for accept/dismiss; Undo = moveDocument).
  - frontend/src/lib/api.ts:2172-2222 — the relationships client family (the 404-tolerant DELETE conventions).
  - frontend/src/types/index.ts:288-295 — SavedView (clone → ClassificationRule; filter_expr→match_expr, add suggest_folder_id/enabled).
  - frontend/src/types/index.ts:391 — Document.metadata (DocumentMetadata) carries _source/_confidence; add _classification? the SAME way.
  - frontend/src/App.tsx:9 — the ActiveView string union to extend.

Backend contract (from Plans 02/03):
  - GET/POST/PATCH/DELETE /classification-rules ; POST body { name, match_expr, suggest_folder_id } (NO is_global).
  - PATCH /documents/{id}/classification/accept ; PATCH /documents/{id}/classification/dismiss.
  - The _classification object (D-118-5): { rule_id, rule_name, condition_summary, suggested_folder_id, suggested_folder_name, status, prior_folder_id? }.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types — ClassificationRule, ClassificationSuggestion, _classification on DocumentMetadata</name>
  <read_first>
    - .planning/phases/118-auto-classification/118-PATTERNS.md §"api.ts + types/index.ts" + §"DocumentList.tsx" (the type-addition note for _classification)
    - frontend/src/types/index.ts:288-295 (SavedView clone-target) + :391 (DocumentMetadata with _source/_confidence)
    - .planning/phases/118-auto-classification/118-RESEARCH.md §"The suggestion object shape (D-118-5)"
  </read_first>
  <files>frontend/src/types/index.ts</files>
  <action>
    Add to `types/index.ts`: `ClassificationRule` (clone `SavedView` — swap `filter_expr`→`match_expr`, add `suggest_folder_id:
    string | null`, `enabled: boolean`, keep `id/user_id?/name/is_global`). `ClassificationSuggestion` mirroring the D-118-5
    object exactly: `{ rule_id, rule_name, condition_summary, suggested_folder_id, suggested_folder_name: string | null, status:
    "suggested" | "accepted", prior_folder_id?: string | null }`. Add `_classification?: ClassificationSuggestion` to
    `DocumentMetadata` the SAME way `_source`/`_confidence` are typed there (so `doc.metadata?._classification` type-checks for the
    Plan-05 row chip + panel). Respect `verbatimModuleSyntax`/`noUnusedLocals` (export types; do not import unused).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` EXIT 0.
    - `grep -n "ClassificationSuggestion" types/index.ts` and `grep -n "_classification" types/index.ts` both match.
    - `ClassificationSuggestion.status` is the `"suggested" | "accepted"` union; `suggested_folder_name` is nullable (Pitfall 5).
  </acceptance_criteria>
  <done>The three type additions exist, `_classification?` is on DocumentMetadata, and tsc is clean.</done>
</task>

<task type="auto">
  <name>Task 2: api.ts client fns (rule CRUD + accept/dismiss) + ActiveView union</name>
  <read_first>
    - .planning/phases/118-auto-classification/118-PATTERNS.md §"api.ts + types/index.ts" + §"App.tsx (ActiveView union)"
    - frontend/src/lib/api.ts:2038-2112 (views CRUD clone-target — createView omits is_global) + :2125-2146 (resolveAdHoc REUSE) + :1971,1997 (moveDocument/updateDocumentMetadata PATCH shape)
    - frontend/src/App.tsx:9 (ActiveView union)
  </read_first>
  <files>frontend/src/lib/api.ts, frontend/src/App.tsx</files>
  <action>
    Add to `api.ts`, adjacent to the views family, cloning its `getAuthHeaders()` + throw-on-non-ok + 404-tolerant-DELETE
    conventions: `listRules()` → GET /classification-rules; `createRule(name, match_expr, suggest_folder_id)` → POST with body
    `{name, match_expr, suggest_folder_id}` (OMIT is_global — server hard-sets, mirror createView); `updateRule(id, body)` → PATCH;
    `deleteRule(id)` → DELETE (404-tolerant). `acceptClassification(docId)` → PATCH /documents/{id}/classification/accept;
    `dismissClassification(docId)` → PATCH /documents/{id}/classification/dismiss. Do NOT add a "would match N" fn — the builder
    reuses the EXISTING `resolveAdHoc`/`resolveFilterCount` (count_only:true). Undo reuses the EXISTING `moveDocument(id,
    priorFolderId)` — no new fn. Add `"classification-rules"` to the `ActiveView` union at `App.tsx:9`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` EXIT 0.
    - `grep -n "classification-rules" api.ts` matches (rule CRUD endpoints); `grep -n "classification/accept" api.ts` and `grep -n "classification/dismiss" api.ts` both match.
    - `createRule`'s POST body does NOT include `is_global` (grep the fn — server hard-sets it).
    - NO new "would match N"/count client fn is added (the builder reuses resolveAdHoc) — grep confirms reuse, not a new count fn.
    - `grep -n "classification-rules" App.tsx` matches (ActiveView union extended).
  </acceptance_criteria>
  <done>Rule CRUD + accept/dismiss client fns exist (createRule omits is_global), Undo + preview reuse existing fns, the ActiveView union is extended, and tsc is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser fetch → /classification-rules + /documents/{id}/classification/* | The client sends JWT via getAuthHeaders; the backend (Plans 02/03) is the access-control gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-04-01 | Elevation | createRule body | mitigate | The client body OMITS `is_global` — even if a caller forged it, the server hard-sets `is_global=false` (Plan 02 T-118-02-01). The client never offers a scope-forge surface for is_global on create. |
| T-118-04-02 | Information Disclosure | client error handling | accept | Client surfaces backend 404s as honest not-found states; the backend's uniform-404 contract (Plans 02/03) prevents the existence-leak oracle — the client cannot widen it. |
| T-118-04-SC | Tampering | npm installs | accept | No new package (RESEARCH audit N/A); slopcheck moot. |
</threat_model>

<verification>
- `npx tsc --noEmit` EXIT 0 on the final state.
- Net-new failures = 0 via base-checkout of the 3 changed files (type/client/union additions only).
- `grep classification-rules api.ts App.tsx` present; createRule omits is_global.
- threads.py byte-untouched (G-5).
</verification>

<success_criteria>
The types (ClassificationRule + ClassificationSuggestion + _classification on DocumentMetadata),
the api client (rule CRUD + accept/dismiss, createRule omits is_global, preview+Undo reuse
existing fns), and the ActiveView union are in place; tsc is clean.
</success_criteria>

<output>
Create `.planning/phases/118-auto-classification/118-04-SUMMARY.md` when done.
</output>
