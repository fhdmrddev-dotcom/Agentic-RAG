---
phase: 118-auto-classification
plan: 05
type: execute
wave: 3
depends_on: ["118-04", "118-03"]
files_modified:
  - frontend/src/components/classification/ClassificationSection.tsx
  - frontend/src/components/classification/ClassificationSection.test.tsx
  - frontend/src/components/metadata/DocumentDetailPanel.tsx
  - frontend/src/components/ingestion/DocumentList.tsx
  - frontend/src/components/ingestion/DocumentList.test.tsx
autonomous: true
requirements: [CLASS-03, UX-01]
must_haves:
  truths:
    - "ClassificationSection (third PanelSection in DocumentDetailPanel) shows the matched-rule provenance (rule name + condition_summary) for a 'suggested' doc — never a fabricated confidence %"
    - "'Suggested' reads instantly as NOT yet moved; accepted renders the audit receipt + Undo; no-match is a calm empty state (states: loading ≠ error ≠ calm)"
    - "Accept/dismiss/Undo re-fetch (not optimistic) after a 200; the count lifts to the PanelSection badge (1 when a suggested exists, else 0/undefined)"
    - "DocumentList row chip (→ folder ✓ ✕) renders ONLY for a status==='suggested' doc; ✓ calls acceptClassification, ✕ calls dismissClassification"
    - "a11y: accept/dismiss/Undo keyboard-operable AND always-visible on coarse-pointer/touch; icon-only controls carry aria-label; panel-AA tokens (not global muted)"
  artifacts:
    - path: "frontend/src/components/classification/ClassificationSection.tsx"
      provides: "On-doc suggestion provenance + accept/dismiss/Undo"
      min_lines: 60
    - path: "frontend/src/components/metadata/DocumentDetailPanel.tsx"
      provides: "Third PanelSection title=Classification"
      contains: "Classification"
    - path: "frontend/src/components/ingestion/DocumentList.tsx"
      provides: "Row chip for a suggested doc"
      contains: "_classification"
  key_links:
    - from: "ClassificationSection"
      to: "acceptClassification / dismissClassification"
      via: "re-fetch-not-optimistic on 200"
      pattern: "(accept|dismiss)Classification"
    - from: "DocumentList row chip"
      to: "doc.metadata._classification"
      via: "render only when status===suggested"
      pattern: "_classification"
    - from: "DocumentDetailPanel"
      to: "ClassificationSection"
      via: "third PanelSection + count lift"
      pattern: "ClassificationSection"
---

<objective>
Ship the on-doc surfaces of UX-01 + CLASS-03 (sketch 036-A): the `ClassificationSection`
(a third `PanelSection` in `DocumentDetailPanel`, cloned from `RelationshipsSection`) that
shows the matched-rule provenance and the accept/dismiss/Undo controls, plus the compact
`DocumentList` row chip (`→ folder ✓ ✕`) for one-glance accept/dismiss. Load-bearing honesty:
a match is deterministic → show the matched rule + `condition_summary` as provenance, NEVER a
confidence %; "suggested" must read instantly as NOT yet moved; accept shows the audit receipt
+ Undo. Mutations re-fetch (never optimistic). a11y is non-negotiable (the bottom-sheet has no
hover → coarse-pointer always-on controls).

Purpose: The user accepts/dismisses a suggestion from the row or the panel, reversibly (CLASS-03),
in the Deep Midnight / Aether shell (UX-01). Output: ClassificationSection + its test, the
panel 3rd section, the row chip + its test.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-auto-classification/118-RESEARCH.md
@.planning/phases/118-auto-classification/118-PATTERNS.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md

<interfaces>
<!-- Clone-targets + the Plan-04 client contract. -->

From Plan 04 (frontend/src/lib/api.ts + types/index.ts):
  - acceptClassification(docId) / dismissClassification(docId) ; moveDocument(id, priorFolderId) for Undo.
  - ClassificationSuggestion { rule_id, rule_name, condition_summary, suggested_folder_id, suggested_folder_name, status, prior_folder_id? }.
  - DocumentMetadata._classification?: ClassificationSuggestion (on the list-row type — zero new fetch needed).

Clone-targets (read verbatim):
  - frontend/src/components/relationships/RelationshipsSection.tsx — the own-fetch state machine (:57-99), re-fetch-not-optimistic
    handleRemove (:104-123), honest loading/error/calm states (:151-198), the a11y always-on coarse-pointer pattern (:23-30,:292-309),
    onTotalChange count-lift to PanelSection.
  - frontend/src/components/metadata/DocumentDetailPanel.tsx — the Relationships <PanelSection> at :244-253 (clone for the 3rd section);
    the file's :221 header comment reserves the Classification slot; the count-lift state pattern (relTotal → classCount).
  - frontend/src/components/ingestion/DocumentList.tsx:182+ — the row render (already reads doc.metadata).
  - frontend/src/index.css — the .rel-x-touch @media(pointer:coarse) always-on rule + the panel-AA tokens (text-panel-muted-foreground[-dim]).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ClassificationSection.tsx + mount as the third PanelSection</name>
  <read_first>
    - .planning/phases/118-auto-classification/118-PATTERNS.md §"ClassificationSection.tsx" + §"DocumentDetailPanel.tsx (3rd PanelSection)"
    - frontend/src/components/relationships/RelationshipsSection.tsx (the FULL clone-target: state machine, re-fetch-not-optimistic, honest states, a11y, onTotalChange)
    - frontend/src/components/metadata/DocumentDetailPanel.tsx:221,244-254 (the reserved slot + the Relationships section to clone + the count-lift)
    - .planning/sketches/036-classification-suggestion/README.md (Winner A: provenance card, honesty rules, audit receipt + reversible Undo) — the locked visual contract
  </read_first>
  <files>frontend/src/components/classification/ClassificationSection.tsx, frontend/src/components/classification/ClassificationSection.test.tsx, frontend/src/components/metadata/DocumentDetailPanel.tsx</files>
  <behavior>
    - Test: a "suggested" doc renders rule_name + condition_summary (provenance) and a "→ {suggested_folder_name}" target; NO confidence % anywhere.
    - Test: copy reads as not-yet-moved (e.g. "Suggested" / "would move to" language), distinct from an accepted receipt.
    - Test: an "accepted" suggestion renders the audit-receipt language + an Undo control.
    - Test: no _classification → a calm empty/no-match state (role NOT alert), distinct from loading (role=status) and error (role=alert + Try again).
    - Test: clicking Accept calls acceptClassification then re-fetches (not an optimistic splice); Dismiss calls dismissClassification then re-fetches; Undo calls moveDocument(id, prior_folder_id).
    - Test: onTotalChange fires 1 when a "suggested" exists, else 0/undefined.
    - Test (a11y): accept/dismiss/Undo are buttons with aria-label; the section uses panel-AA tokens (not the global muted).
  </behavior>
  <action>
    Create `ClassificationSection.tsx` cloning `RelationshipsSection.tsx`'s own-fetch state machine
    (`type LoadState = "loading" | "ready" | "error"`, load(silent) keyed on docId, fetch-on-mount, onTotalChange count-lift).
    Render the matched-rule provenance (rule_name + condition_summary + "→ suggested_folder_name") for `status==="suggested"`,
    reading instantly as NOT yet moved (the 028/036 honesty principle — NEVER a confidence %). The `status==="accepted"` state
    renders the audit receipt + an Undo button (calls `moveDocument(docId, prior_folder_id)`). The no-suggestion state is a calm
    empty (NOT role=alert). Accept (`acceptClassification`) / Dismiss (`dismissClassification`) → on 200 `void load(true)` (silent
    re-fetch under a role=status beat) — re-fetch-NOT-optimistic (clone handleRemove). a11y: accept/dismiss/Undo keyboard-operable
    (`:focus-visible`) AND always-visible on coarse-pointer (the `.rel-x-touch` always-on pattern — the bottom-sheet has no hover);
    icon-only controls carry `aria-label`; all copy uses `text-panel-muted-foreground[-dim]`, NEVER the global muted. Mount a THIRD
    `<PanelSection title="Classification" count={classCount ?? undefined}>` in `DocumentDetailPanel.tsx` AFTER the Relationships
    section (after :253), wiring a new `classCount` state via `onTotalChange={setClassCount}` (mirror relTotal; reset on doc.id
    change). Add `ClassificationSection.test.tsx` covering the behavior block.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run ClassificationSection</automated>
  </verify>
  <acceptance_criteria>
    - `ClassificationSection.test.tsx` GREEN (provenance render, suggested-not-moved copy, accepted receipt + Undo, calm/loading/error states, re-fetch-not-optimistic, onTotalChange, a11y).
    - `grep -nE "confidence|%[^s]" ClassificationSection.tsx` shows NO confidence-% rendering (provenance only).
    - `grep -n "load(true)" ClassificationSection.tsx` present (re-fetch-not-optimistic after mutation).
    - `grep -n "ClassificationSection" DocumentDetailPanel.tsx` matches (3rd PanelSection mounted after Relationships).
    - `tsc --noEmit` EXIT 0.
  </acceptance_criteria>
  <done>ClassificationSection shows provenance (no %), honest states, reversible accept/dismiss/Undo (re-fetch-not-optimistic), a11y always-on; it is mounted as the 3rd PanelSection; tests GREEN, tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DocumentList row chip (→ folder ✓ ✕) for a suggested doc</name>
  <read_first>
    - .planning/phases/118-auto-classification/118-PATTERNS.md §"DocumentList.tsx (row chip)" (the chip shape, the status==="suggested" gate, the zero-new-fetch note)
    - frontend/src/components/ingestion/DocumentList.tsx:182+ (the row render reading doc.metadata)
    - frontend/src/components/ingestion/DocumentList.test.tsx (extend, not replace)
    - .planning/sketches/036-classification-suggestion/README.md (the 036-A row chip anatomy)
  </read_first>
  <files>frontend/src/components/ingestion/DocumentList.tsx, frontend/src/components/ingestion/DocumentList.test.tsx</files>
  <behavior>
    - Test: a row whose doc.metadata._classification.status === "suggested" renders a compact chip with the suggested folder name + a ✓ and a ✕ control.
    - Test: a row with no _classification, or status === "accepted", renders NO chip.
    - Test: ✓ calls acceptClassification(doc.id); ✕ calls dismissClassification(doc.id).
    - Test (a11y): ✓ and ✕ carry aria-label and are coarse-pointer always-on (no hover-only).
  </behavior>
  <action>
    Edit `DocumentList.tsx` row render to add the compact `→ {suggested_folder_name} ✓ ✕` chip (sketch 036-A) ONLY when
    `doc.metadata?._classification?.status === "suggested"` (reads the existing `doc.metadata` — zero new fetch, Open Question 3
    resolved). `✓` calls `acceptClassification(doc.id)`, `✕` calls `dismissClassification(doc.id)`; on success, trigger the list's
    existing reconcile/refresh (re-fetch-not-optimistic; do NOT optimistically splice). The full provenance card lives in the
    panel (Task 1) — the chip is one-glance only. a11y: ✓/✕ carry `aria-label` and are coarse-pointer always-on. Extend
    `DocumentList.test.tsx` with the behavior-block cases (do not replace existing tests).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run DocumentList</automated>
  </verify>
  <acceptance_criteria>
    - `DocumentList.test.tsx` GREEN: chip renders only for status==="suggested"; absent for no-suggestion AND for "accepted"; ✓/✕ call accept/dismiss.
    - `grep -n "_classification" DocumentList.tsx` matches; `grep -n 'status === "suggested"' DocumentList.tsx` (or equivalent) gates the chip.
    - The chip controls carry `aria-label` (grep).
    - `tsc --noEmit` EXIT 0; the existing DocumentList tests still pass (extend-not-replace).
  </acceptance_criteria>
  <done>The row chip renders only for a suggested doc, wires ✓/✕ to accept/dismiss with re-fetch, is a11y-correct; DocumentList tests GREEN, tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered _classification → DOM | A suggestion object (incl. a folder name) is rendered; must not leak across users |
| accept/dismiss button → backend PATCH | The mutation is gated by the backend's owner-scope (Plan 03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-05-01 | Information Disclosure | rendered suggestion | accept | The suggestion is written only into the uploader's OWN doc metadata (Plan 03 user-scoped eval); the list/detail already only fetch the caller's own docs. The UI renders what the backend returns for the caller — no cross-user render path. |
| T-118-05-02 | Tampering / honesty | accept/dismiss UI | mitigate | Mutations re-fetch (not optimistic) — the UI claims "moved"/"audit logged" only after a 200; an accepted receipt reflects the actual backend state. Verified by the re-fetch-not-optimistic test. |
| T-118-05-03 | (a11y, not security) | accept/dismiss/Undo controls | mitigate | Keyboard-operable + coarse-pointer always-on + aria-label (UX-01 / WCAG 2.1 AA) — the bottom-sheet has no hover. Verified by the a11y test. |
| T-118-05-SC | Tampering | npm installs | accept | No new package; slopcheck moot. |
</threat_model>

<verification>
- `vitest run ClassificationSection DocumentList` GREEN; `tsc --noEmit` EXIT 0.
- `vitest run DocumentDetailPanel` GREEN (the 3rd section mount did not break the panel — mock the new fetch as 117 did to avoid stray role=status/alert collisions).
- Net-new failures = 0 via base-checkout of the changed files.
- threads.py byte-untouched (G-5).
</verification>

<success_criteria>
The ClassificationSection shows matched-rule provenance (no %), honest states, and reversible
accept/dismiss/Undo (re-fetch-not-optimistic) as the 3rd PanelSection; the DocumentList row chip
renders only for a suggested doc and wires ✓/✕; a11y is correct; all front-end tests this plan
owns are GREEN and tsc is clean.
</success_criteria>

<output>
Create `.planning/phases/118-auto-classification/118-05-SUMMARY.md` when done.
</output>
