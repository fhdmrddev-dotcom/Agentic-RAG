---
phase: 118-auto-classification
plan: 02
type: execute
wave: 2
depends_on: ["118-01"]
files_modified:
  - backend/app/services/classification_rule_service.py
  - backend/app/api/classification_rules.py
  - backend/app/main.py
autonomous: true
requirements: [CLASS-01]
must_haves:
  truths:
    - "User can create a classification rule; create HARD-SETS is_global=false server-side (never trusts a caller arg)"
    - "list_rules returns own + global via the .or_(user_id.eq.<uuid>,is_global.eq.true) leak-safe predicate with the _uid() UUID-coercion guard"
    - "A cross-user / unseeable / absent rule returns a uniform 404, NEVER 403 (no existence-leak oracle)"
    - "match_expr is validated by validate_fields + validate_operands at BOTH create and update → 422 on a _-prefixed/unknown field or malformed operand"
    - "A classification.rule.create audit row lands live in the DB after create"
    - "update_rule / delete_rule are own-scoped (.eq user_id caller) — a global rule the caller does not own is not updatable/deletable; the enabled toggle rides the UPDATE path"
  artifacts:
    - path: "backend/app/services/classification_rule_service.py"
      provides: "Own+global leak-safe CRUD with is_global hard-false"
      contains: "is_global"
    - path: "backend/app/api/classification_rules.py"
      provides: "/classification-rules CRUD router + audit"
      exports: ["router"]
    - path: "backend/app/main.py"
      provides: "Router mount"
      contains: "classification_rules.router"
  key_links:
    - from: "classification_rules.py router"
      to: "view_filter_compiler.validate_fields / validate_operands"
      via: "match_expr validation at create + update"
      pattern: "validate_(fields|operands)"
    - from: "classification_rule_service.list_rules"
      to: "classification_rules table"
      via: ".or_ own+global read"
      pattern: "is_global\\.eq\\.true"
    - from: "classification_rules.py create"
      to: "audit_log"
      via: "write_audit_entry classification.rule.create"
      pattern: "classification\\.rule\\.create"
---

<objective>
Ship CLASS-01: the `/classification-rules` CRUD surface — a near-verbatim clone of the
Phase 113 `document_view_service.py` + `document_views.py` (CRUD half only; skip the resolve
routes). It inherits the battle-tested leak-safe contract: `is_global` hard-set false on
create, own+global reads via `.or_()` with the `_uid()` UUID-coercion guard, own-scoped
update/delete, uniform 404-not-403 on every cross-user miss, and `match_expr` validation via
the same two validators the views router runs. The `enabled` toggle rides the UPDATE path
(no new endpoint). A `classification.rule.create` audit row (enum already LIVE) is written
after create.

Purpose: Give users the surface to define rules (CLASS-01) without re-inventing the leak-safe
library-object pattern. Output: the service, the router, and the main.py mount.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-auto-classification/118-RESEARCH.md
@.planning/phases/118-auto-classification/118-PATTERNS.md

<interfaces>
<!-- Contracts this plan implements against. From Plan 01 + the clone-targets. -->

From Plan 01 (backend/app/models/classification_rule.py):
  - RuleCreate { name, match_expr: ViewFilter, suggest_folder_id: UUID|None }
  - RuleUpdate { name?, match_expr?: ViewFilter, suggest_folder_id?, enabled?: bool }
  - RuleResponse { id, user_id?, name, match_expr: dict, suggest_folder_id?, is_global, enabled }

Clone-targets (read verbatim; swap table/model):
  - backend/app/services/document_view_service.py — _client/_uid (:44-62), create_view is_global hard-false (:86),
    list_views .or_ own+global (:101), get_view own-OR-global→None (:113), update/delete own-scoped (:166-191).
  - backend/app/api/document_views.py — CRUD routes :85-200; validate at :106-114 + :164-174; audit at :129-134;
    ownership-before-validation on UPDATE (:154-162); uniform 404 (:162,184,199). SKIP the resolve routes (:263+).
  - backend/app/services/document_view_resolver.py:139 — _build_field_meta(user_id, supabase) -> (whitelist, number_fields).
  - backend/app/services/audit_service.py:23 — VALID_ACTION_TYPES has classification.rule.create LIVE.
  - backend/app/main.py:405 (the `from app.api import ...` line) + :423-424 (the include_router mounts).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: classification_rule_service.py — leak-safe CRUD clone</name>
  <read_first>
    - .planning/phases/118-auto-classification/118-PATTERNS.md §"classification_rule_service.py" (the verbatim helper + create/list/get/update/delete clones)
    - backend/app/services/document_view_service.py:44-191 (the FULL clone-target: _client, _uid, create_view, list_views, get_view, update_view, delete_view)
    - backend/tests/integration/test_118_rule_crud.py (the Plan-01 scaffold this task flips GREEN)
  </read_first>
  <files>backend/app/services/classification_rule_service.py</files>
  <action>
    Create `classification_rule_service.py` cloning `document_view_service.py` verbatim, setting `_TABLE = "classification_rules"`.
    Copy `_client` and `_uid` (the canonical-UUID-string coercion that guards the sole owner-scoping `.or_()` grammar) unchanged.
    `create_rule(user_id, name, match_expr: dict, suggest_folder_id, supabase=None)` inserts a payload that HARD-SETS
    `is_global: False` and `enabled: True` (never from the caller — RLS WITH CHECK forces it too, defense-in-depth). `list_rules`
    returns own+global via `.or_(f"user_id.eq.{_uid(user_id)},is_global.eq.true")` (preserve the dedupe-by-id loop). `get_rule`
    is own-OR-global → None (router maps to 404). `update_rule` / `delete_rule` are STRICTLY own-scoped (`.eq("user_id", caller)`)
    → None/False on a cross-user miss → router 404. The `enabled` field is a plain UPDATE column (toggle rides update_rule).
    All DB access via `aexec` (async service). Make `test_118_rule_crud.py` GREEN live on :54322.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/integration/test_118_rule_crud.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"is_global": False' classification_rule_service.py` matches in create_rule (hard-set).
    - `grep -n "is_global.eq.true" classification_rule_service.py` matches in list_rules + get_rule (own+global read).
    - `grep -n '.eq("user_id"' classification_rule_service.py` present in update_rule AND delete_rule (own-scoped writes).
    - `test_118_rule_crud.py` GREEN live: create hard-sets is_global=false; own+global list; cross-user get → None→404; update/delete own-scoped.
  </acceptance_criteria>
  <done>The service clones the leak-safe CRUD contract (is_global hard-false, _uid guard, own+global 404-not-403) and `test_118_rule_crud.py` is GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: classification_rules.py router + match_expr validation + audit + main.py mount</name>
  <read_first>
    - .planning/phases/118-auto-classification/118-PATTERNS.md §"classification_rules.py (route)" (router prefix, validate-before-write, ownership-before-validation, audit-after-create, uniform 404, mount)
    - backend/app/api/document_views.py:74-200 (the CRUD-router clone-target; validate :106-114 + :164-174, audit :129-134, ownership-first-on-update :154-162)
    - backend/app/services/view_filter_compiler.py:214,263 (validate_operands / validate_fields)
    - backend/app/main.py:405,423 (the import line + the include_router mounts to clone)
    - backend/tests/integration/test_118_rule_crud.py + tests/unit/test_118_rule_validation.py (the router-level reject + audit assertions to flip GREEN)
  </read_first>
  <files>backend/app/api/classification_rules.py, backend/app/main.py</files>
  <action>
    Create `classification_rules.py` with `router = APIRouter(prefix="/classification-rules", tags=["classification-rules"])`,
    cloning the `document_views.py` CRUD half (skip the resolve routes). On CREATE and UPDATE, BEFORE the write, build the
    whitelist via `_build_field_meta(current_user["id"], supabase)` and run `view_filter_compiler.validate_fields(...)` +
    `validate_operands(...)`, mapping `ValueError → HTTPException(422)`. On UPDATE, SELECT ownership FIRST (clone
    document_views.py:154-162) so an unowned/absent id uniformly 404s regardless of whether the submitted match_expr is valid
    (no 422-vs-404 ordering oracle); require STRICT ownership for update (a global rule the caller does not own is not updatable).
    Every cross-user/unseeable/absent miss → uniform 404, never 403. After a successful create, fire
    `write_audit_entry(action_type="classification.rule.create", metadata={rule_id, name}, supabase=supabase)` (fire-and-forget;
    the live round-trip is the verification). Mount: add `classification_rules` to the `from app.api import ...` line (main.py:405)
    and `app.include_router(classification_rules.router)` after main.py:424. Do NOT add a response_model that strips nested keys
    (IN-03) — RuleResponse is safe (no doc metadata). Make the router-level reject + audit assertions GREEN.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/integration/test_118_rule_crud.py tests/unit/test_118_rule_validation.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "validate_fields" classification_rules.py` and `grep -n "validate_operands" classification_rules.py` BOTH match (validation at create + update).
    - `grep -c "403" classification_rules.py` is 0 (no 403 — uniform 404 contract).
    - `grep -n "classification.rule.create" classification_rules.py` matches (audit after create).
    - `grep -n "classification_rules.router" main.py` matches (mounted).
    - A `_`-prefixed / unknown field in match_expr → 422 (not 500); an unowned rule id on UPDATE → 404 even with a valid body (ownership-before-validation); a created rule yields a live `classification.rule.create` audit row.
  </acceptance_criteria>
  <done>The router validates match_expr at create+update (422), is uniform-404-not-403, writes the create audit live, and is mounted; the CRUD + validation tests are GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `/classification-rules` | Authenticated caller submits rule definitions (untrusted name/match_expr/scope) |
| service-role client → classification_rules table | RLS is bypassed; app-code predicates are the SOLE owner-scoping gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-02-01 | Elevation / Tampering | create_rule is_global | mitigate | Service HARD-SETS `is_global=false`; never reads a caller arg; RLS WITH CHECK forces it (defense-in-depth). Verified by `test_118_rule_crud.py`. |
| T-118-02-02 | Information Disclosure | get_rule / cross-user reads | mitigate | Own+global `.or_(user_id.eq.<uuid>,is_global.eq.true)` with `_uid()` UUID-coercion; cross-user/unseeable/absent → uniform 404, never 403 (`grep -c 403`=0). |
| T-118-02-03 | Tampering | `.or_()` grammar via non-UUID user_id | mitigate | `_uid()` coerces to a canonical UUID string (raises on malformed) — the one runtime-value-into-DSL spot is safe by construction. |
| T-118-02-04 | Tampering | match_expr injection / SSTI | mitigate | `validate_fields` + `validate_operands` at create+update; `ViewFilter` Literal op-reject at parse; the matcher (Plan 01) never eval()s. |
| T-118-02-05 | Repudiation | classification.rule.create audit | mitigate | Audit row written after create; live DB round-trip is the verification (enum already in VALID_ACTION_TYPES). |
| T-118-02-SC | Tampering | npm/pip installs | accept | No new package (RESEARCH audit N/A); slopcheck moot. |
</threat_model>

<verification>
- `pytest tests/integration/test_118_rule_crud.py tests/unit/test_118_rule_validation.py -x` GREEN live on :54322.
- `pytest -k "118 or 113"` regression spot GREEN (shared validators untouched in behavior).
- `grep -c 403 classification_rules.py` == 0; `grep classification_rules.router main.py` present.
- Net-new failures = 0 via base-checkout of the 3 changed files (main.py additively touched only).
- threads.py byte-untouched (G-5).
</verification>

<success_criteria>
The `/classification-rules` CRUD surface is leak-safe (is_global hard-false, own+global
404-not-403), validates match_expr at create+update, writes the create audit live, mounts in
main.py, and the CRUD + validation tests are GREEN.
</success_criteria>

<output>
Create `.planning/phases/118-auto-classification/118-02-SUMMARY.md` when done.
</output>
