---
phase: 114-virtual-folders-range-date-filters-view-builder-sidebar
plan: 03
type: execute
wave: 3
depends_on: ["114-01", "114-02"]
files_modified:
  - supabase/full-schema.sql
  - backend/tests/integration/test_114_typed_columns.py
  - backend/tests/integration/test_114_explain_index.py
autonomous: false
requirements: [VIEW-03]

must_haves:
  truths:
    - "Migration 074 is applied to the live local DB (:54322): document_type_norm + date_typed GENERATED STORED columns + their btree indexes exist"
    - "The two generated columns auto-backfilled every existing row at ALTER TABLE time (no backfill job)"
    - "A deliberately-bad/non-ISO stored date yields NULL via the CASE guard and breaks neither the ALTER nor a subsequent insert (R-114-B), verified against the full dataset"
    - "EXPLAIN shows a btree index scan (not a seq scan) on date_typed and document_type_norm at a ~10k-row seed (SC#3)"
    - "supabase/full-schema.sql is regenerated (no --reset) to reflect both new columns + indexes"
  artifacts:
    - path: "supabase/full-schema.sql"
      provides: "Regenerated bootstrap artifact carrying the two typed columns + indexes"
      contains: "document_type_norm"
    - path: "backend/tests/integration/test_114_typed_columns.py"
      provides: "Live auto-backfill + bad-date-yields-NULL + lower() correctness tests"
      exports: []
    - path: "backend/tests/integration/test_114_explain_index.py"
      provides: "~10k-seed EXPLAIN index-use proof (SC#3)"
      exports: []
  key_links:
    - from: "supabase/migrations/074_view_typed_columns.sql"
      to: "live DB :54322 public.documents"
      via: "SQL-editor paste / psycopg2-direct apply (NEVER db push)"
      pattern: "document_type_norm|date_typed"
---

<objective>
Cross the Plan-01-authored migration 074 into the live local DB (:54322) — adding the two GENERATED STORED typed columns + btree indexes to the existing `documents` table — verify the auto-backfill and the bad-date safety against the full dataset, prove SC#3 index-use via EXPLAIN at a ~10k seed, and regenerate the `full-schema.sql` bootstrap artifact.

Purpose: The phase CANNOT pass verification without the migration actually applied — backend build/type checks pass without it (the false-positive trap). This [BLOCKING] step also un-marks the typed-column-dependent live assertions xfail'd in Plan 02.
Output: Migration 074 live on :54322, the regenerated `full-schema.sql`, and the live bad-date + auto-backfill + EXPLAIN-index integration tests GREEN.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-virtual-folders-range-date-filters-view-builder-sidebar/114-RESEARCH.md
@.planning/phases/114-virtual-folders-range-date-filters-view-builder-sidebar/114-PATTERNS.md
@.planning/phases/114-virtual-folders-range-date-filters-view-builder-sidebar/114-01-SUMMARY.md
@.planning/phases/114-virtual-folders-range-date-filters-view-builder-sidebar/114-02-SUMMARY.md
@supabase/SETUP.md

<interfaces>
<!-- The migration apply convention (project-specific — NOT supabase db push). -->

Migration apply protocol (CLAUDE.md + the 071/072/073/100/102/110/111.1 precedent):
1. Paste supabase/migrations/074_view_typed_columns.sql into the Supabase SQL editor against the live local DB (:54322), OR psycopg2-direct against :54322 if the local stack is up and operator-authorized. NEVER `supabase db push` / `db reset` (preserves dev data — ~19 docs / threads).
2. Read back to assert the two columns + two indexes exist.
3. Run `bash scripts/regenerate-full-schema.sh` (no --reset) to rebuild supabase/full-schema.sql.
4. Commit migration (already authored in Plan 01) + regenerated full-schema.sql + the test files together.

Auto-backfill + EXPLAIN verification SQL: 114-RESEARCH.md §"Migration Design (DELIVERABLE 2)" → "Auto-backfill verification" + "EXPLAIN proof (SC#3, ~10k docs)".
Bad-date safety: 114-RESEARCH.md §"Common Pitfalls → Pitfall 2" + the Assumptions Log A1/A2 (test a regex-passing-but-invalid value against the full dataset).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Apply migration 074 to the live local DB (:54322) + regenerate full-schema.sql</name>
  <read_first>
    - supabase/migrations/074_view_typed_columns.sql (the file to apply — authored in Plan 01)
    - supabase/SETUP.md (the apply-via-SQL-editor + regenerate convention)
    - 114-RESEARCH.md §"Migration Design (DELIVERABLE 2)" (the auto-backfill verification + EXPLAIN SQL)
  </read_first>
  <what-built>
    Plan 01 authored `supabase/migrations/074_view_typed_columns.sql` (two GENERATED ALWAYS AS STORED columns — `document_type_norm` = lower(metadata->>'document_type'), `date_typed` = ISO-regex-guarded (metadata->>'date')::date — plus two btree indexes). Plan 02 wired the resolve route to query these columns. This step crosses the migration into the live DB; it CANNOT be auto-applied (SQL-editor paste is a manual operator action).
  </what-built>
  <how-to-verify>
    1. Apply: paste the whole `supabase/migrations/074_view_typed_columns.sql` into the Supabase SQL editor against the live local DB on :54322 (or run it via psycopg2-direct if the local stack is up and you authorize it). Do NOT run `supabase db push` or `db reset`.
    2. Read back (assert all four objects exist):
       `SELECT column_name, is_generated FROM information_schema.columns WHERE table_name='documents' AND column_name IN ('document_type_norm','date_typed');` → both rows, is_generated = ALWAYS.
       `SELECT indexname FROM pg_indexes WHERE tablename='documents' AND indexname IN ('idx_documents_document_type_norm','idx_documents_date_typed');` → both present.
    3. Auto-backfill: run the §"Auto-backfill verification" count query — `typed_dt` ≈ rows with a document_type, `typed_date` ≈ rows with an ISO date; no error on existing ~19 docs.
    4. Regenerate: run `bash scripts/regenerate-full-schema.sh` (no --reset); confirm `supabase/full-schema.sql` now contains `document_type_norm` and `date_typed`.
  </how-to-verify>
  <resume-signal>Type "applied" once the two columns + two indexes read back present on :54322, the auto-backfill count query runs without error, and full-schema.sql is regenerated; or describe the failure (e.g. an ALTER error on a specific row → see Task 2's bad-date handling).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Live bad-date safety + auto-backfill + EXPLAIN-index verification (un-mark Plan 02 xfails)</name>
  <read_first>
    - 114-RESEARCH.md §"Common Pitfalls → Pitfall 2" (the regex-passing-but-invalid date edge) + §"Migration Design → EXPLAIN proof"
    - 114-VALIDATION.md §"Manual-Only Verifications" (the ~10k-seed EXPLAIN requirement — dev DB is ~19 docs)
    - backend/tests/integration/test_114_resolve_range_date.py + test_114_count_only.py (the Plan-02 suites whose typed-column assertions un-mark here)
    - backend/tests/integration/test_113_view_resolve.py (the live :54322 seeding + skip-clean-when-down harness analog)
  </read_first>
  <action>
    Write `backend/tests/integration/test_114_typed_columns.py` (live :54322, skip-clean when the DB is unreachable per the test_113_view_resolve harness): (a) assert the auto-backfill — after the migration, `document_type_norm` is the lowercased `document_type` for rows that have one, and `date_typed` is the parsed date for ISO-dated rows; (b) the bad-date safety test — INSERT a `documents` row whose `metadata->>'date'` is a non-ISO string AND a regex-passing-but-calendar-invalid string (e.g. `2026-13-99`) and assert the insert succeeds with `date_typed` NULL (the CASE/regex guard yields NULL, never raises — R-114-B); if a regex-passing-invalid value reaches `::date` and errors, document the finding and tighten per Open Q2 (stricter month/day regex). Then write `backend/tests/integration/test_114_explain_index.py` — seed ~10k synthetic `documents` rows (owner-scoped, varied document_type + ISO dates), run the §"EXPLAIN proof" queries (`EXPLAIN ANALYZE ... WHERE date_typed >= CURRENT_DATE AND date_typed <= CURRENT_DATE + 90` and the `document_type_norm = 'invoice'` query), and assert the plan text contains an Index/Bitmap Index Scan on `idx_documents_date_typed` / `idx_documents_document_type_norm` and NOT a Seq Scan (SC#3) — note the test MUST seed ~10k because the planner picks a seq scan on a tiny table regardless of indexes; tear the seed down after. Finally, un-mark the typed-column-dependent xfails in `test_114_resolve_range_date.py` and `test_114_count_only.py` (from Plan 02) to GREEN now that the columns exist live, and re-run them. Verify `supabase/full-schema.sql` (regenerated in Task 1) contains both columns + both indexes.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/integration/test_114_typed_columns.py tests/integration/test_114_explain_index.py tests/integration/test_114_resolve_range_date.py tests/integration/test_114_count_only.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && venv/Scripts/python -m pytest tests/integration/test_114_typed_columns.py tests/integration/test_114_explain_index.py tests/integration/test_114_resolve_range_date.py tests/integration/test_114_count_only.py -q` is GREEN live on :54322 (or skips cleanly only if :54322 is genuinely down — the migration-apply gate above must have run first)
    - The bad-date test proves a non-ISO and a regex-passing-invalid stored date yield `date_typed` NULL with the insert succeeding (no ALTER/insert break — R-114-B)
    - The EXPLAIN test asserts a btree Index/Bitmap Index Scan (NOT Seq Scan) on both `idx_documents_date_typed` and `idx_documents_document_type_norm` at the ~10k seed (SC#3)
    - `grep -c 'document_type_norm' supabase/full-schema.sql` ≥ 1 AND `grep -c 'date_typed' supabase/full-schema.sql` ≥ 1 (artifact regenerated)
    - The Plan-02 typed-column xfails are un-marked and now pass live (operator/case-insensitive/relative-date/count-only resolve against the real columns)
  </acceptance_criteria>
  <done>Migration 074 is live on :54322 with auto-backfill verified, the bad-date guard proven against the full dataset, SC#3 EXPLAIN index-use proven at a ~10k seed, full-schema.sql regenerated, and the Plan-02 live suites un-marked GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration DDL → live documents table | The generated date column evaluates a stored string at ALTER + every future insert; a malformed value must not break writes |
| operator apply step → live dev data | The apply must preserve dev data (never db reset) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-03-01 | Denial of Service | `date_typed` GENERATED column on a malformed stored date | mitigate | The ISO-regex `CASE` guard yields NULL on a non-ISO value, breaking neither the ALTER nor a future insert. Verified LIVE against the full dataset by the bad-date test (Task 2) incl. a regex-passing-but-invalid edge (Pitfall 2) — the "static would false-green" lesson: the migration is proven applied + safe live, not assumed. |
| T-114-03-02 | Denial of Service | the apply step itself | mitigate | Apply via SQL-editor paste / psycopg2-direct against :54322 — NEVER `db push`/`db reset` (preserves dev data; CLAUDE.md hard rule). Read-back asserts the columns + indexes before done. Atomic `BEGIN; ... COMMIT;` rolls back a partial failure. |
| T-114-03-03 | Tampering | schema drift between code and live DB | mitigate | `full-schema.sql` regenerated (no --reset) and committed with the migration + tests, so the bootstrap artifact matches the live schema; the EXPLAIN/auto-backfill tests gate against a phantom apply. |
| T-114-03-SC | Tampering | npm/pip installs | accept | This plan installs NO packages. No supply-chain surface. |
</threat_model>

<verification>
- Migration 074 read-back confirms `document_type_norm` + `date_typed` (is_generated = ALWAYS) and both btree indexes exist on :54322.
- The bad-date dataset test proves NULL-on-malformed against the full dataset; the EXPLAIN test proves index-use at ~10k (SC#3).
- `full-schema.sql` regenerated; the Plan-02 typed-column live suites un-marked GREEN.
</verification>

<success_criteria>
- Migration 074 is live on :54322 with the two GENERATED STORED columns + btree indexes, auto-backfilled, bad-date-safe.
- SC#3 EXPLAIN index-use proven at a ~10k seed; `full-schema.sql` regenerated.
- The phase is no longer in the build-passes-without-apply false-positive state.
</success_criteria>

<output>
Create `.planning/phases/114-virtual-folders-range-date-filters-view-builder-sidebar/114-03-SUMMARY.md` when done.
</output>
