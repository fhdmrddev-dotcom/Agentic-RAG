---
phase: 114-virtual-folders-range-date-filters-view-builder-sidebar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/document_view.py
  - backend/app/services/view_filter_compiler.py
  - backend/app/services/view_operators_extra.py
  - supabase/migrations/074_view_typed_columns.sql
  - backend/tests/unit/test_113_view_filter_compiler.py
  - backend/tests/unit/test_114_view_filter_compiler.py
autonomous: true
requirements: [VIEW-03]

must_haves:
  truths:
    - "Each new operator (gte/lte/one_of/contains/is_empty/within_next/older_than/before/after/between) compiles to a correct bound WHERE-fragment descriptor"
    - "A SQL/SSTI payload in a filter value rides as a bound literal in the fragment — never interpolated (SC#4)"
    - "An unknown/smuggled operator still fails closed (KeyError) after the widening (Pitfall 5)"
    - "Case-insensitive text matching is encoded (document_type/language → lowercased value; free-text → ILIKE)"
    - "The migration file authored adds two GENERATED STORED typed columns + btree indexes, with the date cast ISO-regex-guarded"
  artifacts:
    - path: "backend/app/services/view_operators_extra.py"
      provides: "Additive @register_operator fns returning Fragment descriptors"
      contains: "register_operator"
    - path: "backend/app/services/view_filter_compiler.py"
      provides: "Widened compile_filter returning list[Fragment] + the Fragment dataclass + the seam import"
      contains: "Fragment"
    - path: "backend/app/models/document_view.py"
      provides: "Additively-widened ViewCondition.op Literal + optional value2/values/unit"
      contains: "within_next"
    - path: "supabase/migrations/074_view_typed_columns.sql"
      provides: "GENERATED STORED document_type_norm + date_typed columns + btree indexes (file authored, not applied here)"
      contains: "GENERATED ALWAYS AS"
    - path: "backend/tests/unit/test_114_view_filter_compiler.py"
      provides: "Per-operator fragment + case-insensitive + injection-stays-green unit tests"
      exports: []
  key_links:
    - from: "backend/app/services/view_filter_compiler.py"
      to: "backend/app/services/view_operators_extra.py"
      via: "side-effect import at the line-79 Phase 114 SEAM"
      pattern: "from . import view_operators_extra"
    - from: "backend/app/models/document_view.py"
      to: "backend/app/services/view_filter_compiler.py"
      via: "compile_filter consumes the widened ViewCondition.op Literal"
      pattern: "compile_filter"
---

<objective>
Widen the Phase 113 filter compiler from a single `metadata @> $1::jsonb` containment dict to an ordered list of bound WHERE-fragment descriptors, register the full VIEW-03 operator set additively at the documented `view_filter_compiler.py:79` seam, and author (do not apply) the one new migration that promotes `date` + `document_type` to typed, btree-indexed GENERATED STORED columns.

Purpose: This is the backend foundation. The resolve route (Plan 02) and the migration apply (Plan 03) both build on the Fragment contract and the migration file this plan produces. It is the one place Phase 113's "purely additive, eq untouched" promise deliberately bends (R-114-A) — treated as a tested contract change, not a silent regression.
Output: Widened AST model, the Fragment-returning compiler + extra-operators module, the `074_view_typed_columns.sql` migration FILE (un-applied), the rewritten three 113 containment-dict tests, and the net-new `test_114_view_filter_compiler.py` Wave-0 unit suite.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-virtual-folders-range-date-filters-view-builder-sidebar/114-CONTEXT.md
@.planning/phases/114-virtual-folders-range-date-filters-view-builder-sidebar/114-RESEARCH.md
@.planning/phases/114-virtual-folders-range-date-filters-view-builder-sidebar/114-PATTERNS.md

<interfaces>
<!-- The existing compiler contract this plan widens. Extracted from the codebase — executor uses these directly, no exploration needed. -->

From backend/app/services/view_filter_compiler.py (CURRENT — to be widened):
```python
OPERATOR_REGISTRY: dict[str, Callable[[str, object], dict]] = {}
def register_operator(op: str) -> Callable[...]: ...   # decorator → OPERATOR_REGISTRY[op] = fn
@register_operator("eq")
def _op_eq(field, value) -> dict: return {field: value}
# Phase 114 SEAM comment at :79 — import view_operators_extra ONCE here.
def compile_filter(flt: ViewFilter) -> dict:           # CURRENT: folds to one dict; raises KeyError on registry miss (:97-99 — KEEP)
def validate_fields(flt, whitelist) -> None:           # UNCHANGED — _-prefix reject + whitelist check at save
```

From backend/app/models/document_view.py (CURRENT — to be widened additively):
```python
class ViewCondition(BaseModel):
    field: str
    op: Literal["eq"]                       # 114 widens additively
    value: str | int | float | bool
class ViewFilter(BaseModel):
    op: Literal["and"]                      # stays "and" — flat AND only
    conditions: list[ViewCondition] = []
```

From backend/app/models/metadata_field.py (the field-type source of truth):
```python
_BUILTINS = {"title","author","date","document_type","topics","language","summary"}
# field_type ∈ {"string","date","number","boolean","enum"}
```

Operator → Fragment mapping (authoritative): 114-RESEARCH.md §"Operator → Fragment Mapping (DELIVERABLE 1)" — the per-operator builder/leg table.
Migration DDL (authoritative): 114-RESEARCH.md §"Migration Design (DELIVERABLE 2)" + 114-PATTERNS.md §migration analog (071 header conventions).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen the AST model + write the Wave-0 unit test scaffolds</name>
  <read_first>
    - backend/app/models/document_view.py (the file being modified — the ViewCondition/ViewFilter AST at :34-42 and the IN-03 "no ViewResolveResponse" rule at :17-24)
    - backend/tests/unit/test_113_view_filter_compiler.py (the test contract + the in-body-import / xfail-unmark convention at :9-24)
    - 114-PATTERNS.md §"backend/app/models/document_view.py (MODIFY)" (the additive-Literal + optional value2/values/unit guidance)
    - 114-RESEARCH.md §"Operator → Fragment Mapping" (the AST shapes per operator)
  </read_first>
  <behavior>
    - ViewCondition.op accepts each of: eq, gte, lte, one_of, contains, is_empty, within_next, older_than, before, after, between; rejects an unknown op at parse (ValidationError)
    - ViewFilter.op still accepts only "and"; "or" still rejected at parse
    - A pre-114 row {"op":"and","conditions":[{"field":"document_type","op":"eq","value":"invoice"}]} still validates unchanged (additive Literal, no shape break)
    - one_of carries an optional values list; between carries an optional value2; relative ops carry an optional unit ∈ {days,weeks,months} — all default-absent so existing rows parse
    - test_114_view_filter_compiler.py collects with all symbol imports INSIDE test bodies; not-yet-built behaviors xfail(strict=False) so the suite exits 0
  </behavior>
  <action>
    Widen `ViewCondition.op` from `Literal["eq"]` to the additive Literal listing all eleven operators (eq, gte, lte, one_of, contains, is_empty, within_next, older_than, before, after, between) — names exactly per the RESEARCH mapping table. Keep `ViewFilter.op = Literal["and"]` (OR/NOT deferred). Add optional `value2: str | int | float | None = None` (between), `values: list[str | int | float] | None = None` (one_of), and `unit: Literal["days","weeks","months"] | None = None` (relative-date) to `ViewCondition`, all default-absent so existing `{op:and, eq}` rows in `document_views.filter_expr` parse unchanged (the D-113-6 payoff). Preserve the IN-03 / 112-CR-01 docstring rule verbatim (NO `ViewResolveResponse` model). Then author `backend/tests/unit/test_114_view_filter_compiler.py` as the Wave-0 RED scaffold mirroring the `test_113_view_filter_compiler.py` convention (imports inside each test body; `@pytest.mark.xfail(strict=False)` on not-yet-built behaviors): one test per new operator asserting the produced Fragment (leg/field/builder/value/value2 per the mapping table), a case-insensitive test (document_type lowercased value → `.eq` on `document_type_norm`; free-text `title` → `.ilike` fragment), a relative-date test stub, and an SC#4 injection-rides-as-bound-literal test in the new fragment shape. Do NOT register operators or change the compiler in this task — fragments are asserted against the Plan code landing in Tasks 2-3 (xfail until then).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_114_view_filter_compiler.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_114_view_filter_compiler.py -q` exits 0 (xfailed/xpassed, never collection-errored)
    - `grep -c within_next backend/app/models/document_view.py` ≥ 1 (the widened Literal is present)
    - A Python repl `ViewFilter.model_validate({"op":"and","conditions":[{"field":"document_type","op":"eq","value":"invoice"}]})` succeeds (pre-114 row parses)
    - A Python repl `ViewFilter.model_validate({"op":"or","conditions":[]})` raises ValidationError (OR still rejected)
    - The "no ViewResolveResponse model" docstring rule survives in document_view.py
  </acceptance_criteria>
  <done>The AST accepts all eleven operators additively, pre-114 rows still parse, and the Wave-0 114 unit suite collects + exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen the compiler to Fragment output + register the additive operators</name>
  <read_first>
    - backend/app/services/view_filter_compiler.py (the file being modified — the registry seam at :56-83, the fold loop at :86-101, the KeyError fail-closed at :97-99, the purity docstring at :1-32)
    - 114-PATTERNS.md §"view_operators_extra.py (NEW)" + §"view_filter_compiler.py (MODIFY)" (the seam + purity invariants)
    - 114-RESEARCH.md §"Operator → Fragment Mapping (DELIVERABLE 1)" + §"Code Examples → Registering a new operator additively" (the Fragment dataclass + per-operator leg/builder)
    - backend/app/models/metadata_field.py (the _BUILTINS set + field_type vocabulary — to decide typed vs custom leg)
  </read_first>
  <behavior>
    - eq on document_type → Fragment(leg="typed", field="document_type", builder="eq", value=lower(v)) targeting document_type_norm
    - eq on a boolean/number custom field → containment-leg Fragment (the @> fast path survives)
    - eq on free-text (title/author/summary) → Fragment(builder="ilike", value=v) for case-insensitive exact
    - gte/lte on date → typed-leg Fragment on date_typed; on a custom number → custom-leg cast Fragment
    - one_of → an OR-group Fragment (or list) over one field with lowercased values
    - contains → ilike `%v%` Fragment (D-114-11); is_empty → null-or-empty OR-group Fragment (D-114-12)
    - between → a Fragment carrying value + value2
    - compile_filter returns list[Fragment] (data only — no SQL, no f-string); a registry miss still raises KeyError
  </behavior>
  <action>
    Define a `Fragment` dataclass in `view_filter_compiler.py` per the RESEARCH §"Operator → Fragment Mapping" contract: `leg ∈ {"typed","custom","containment"}`, `field` (whitelisted name, never interpolated into raw SQL), `builder ∈ {"eq","gte","lte","ilike","is_","or_","contains"}`, `value`, `value2=None`. Change `compile_filter` to return `list[Fragment]` (one or more per condition; `one_of` may yield an OR-group fragment) instead of folding into one dict — keep it a PURE module (no I/O, no SQL, no `.format()`). KEEP the registry-miss `KeyError` (Pitfall 5) and `validate_fields` byte-unchanged. Create `backend/app/services/view_operators_extra.py` registering `gte`/`lte`/`one_of`/`contains`/`is_empty`/`within_next`/`older_than`/`before`/`after`/`between` via `@register_operator`, each returning the Fragment(s) per the mapping table — choosing the `typed` leg for the promoted `date`/`document_type` fields (targeting `document_type_norm`/`date_typed`), the `containment` leg for bool/number `eq`, and the `custom` leg for everything else. Apply the case-insensitivity rule from Pitfall 3: lowercase the query VALUE for `document_type`/`language` (they are already stored lowercase — do NOT ILIKE them, it defeats the index); reserve `ilike`/lower-on-both-sides for the genuinely un-normalized free-text fields (`title`/`author`/`summary`). Wire the side-effect import at the documented Phase 114 SEAM (`view_filter_compiler.py:79`): `from . import view_operators_extra  # noqa: F401`. Relative-date operators (`within_next`/`older_than`) emit fragments that DEFER the actual today→today+N window math to the resolve route (Plan 02, server clock per D-114-16) — carry `value`=N + `unit`, do NOT bake dates here. Un-mark the corresponding xfails in `test_114_view_filter_compiler.py` to GREEN.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_114_view_filter_compiler.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_114_view_filter_compiler.py -q` is GREEN (the per-operator + case-insensitive + injection tests pass; relative-date window math stays deferred/xfail until Plan 02)
    - `grep -v '^#' backend/app/services/view_filter_compiler.py | grep -c 'view_operators_extra'` ≥ 1 (the seam import is wired)
    - The smuggled-op `KeyError` proof still passes: `cd backend && venv/Scripts/python -m pytest tests/unit/test_113_view_filter_compiler.py::test_unknown_op_rejected -q` GREEN
    - A repl `compile_filter(ViewFilter(op="and", conditions=[ViewCondition(field="document_type", op="eq", value="Invoice")]))` returns a list whose Fragment has a lowercased value ("invoice") on the typed leg (case-insensitivity, indexed path)
    - No f-string SQL anywhere in `view_filter_compiler.py` or `view_operators_extra.py` (compiler purity — values ride as Fragment fields, bound later)
  </acceptance_criteria>
  <done>compile_filter returns bound Fragment descriptors, all VIEW-03 operators register additively at the seam, the fail-closed KeyError survives, and the 114 unit suite (minus the deferred relative-date window) is GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the three 113 containment-dict tests + author the migration FILE (un-applied)</name>
  <read_first>
    - backend/tests/unit/test_113_view_filter_compiler.py (the file being modified — test_eq_compiles :31-41, test_and_folds_conditions :44-58, test_empty_filter_no_narrowing :61-69 to rewrite; test_injection_value_neutralized :150-165 to KEEP green; test_unknown_op_rejected :72-99 to KEEP)
    - supabase/migrations/071_dm_foundations.sql (the header/atomic-wrapper/index-naming conventions to copy — :1-13)
    - 114-RESEARCH.md §"Migration Design (DELIVERABLE 2)" (the exact GENERATED STORED DDL + ISO regex + immutability facts)
    - 114-PATTERNS.md §"supabase/migrations/NNN_view_typed_columns.sql (NEW)" (the "adds NO RLS" note + apply-via-SQL-editor convention)
  </read_first>
  <action>
    Rewrite the three Phase-113 containment-dict assertions in `test_113_view_filter_compiler.py` to the new `list[Fragment]` contract (per R-114-A): `test_eq_compiles` asserts a single typed-leg Fragment (document_type → `document_type_norm`, lowercased value); `test_and_folds_conditions` asserts an ordered list of two Fragments (one per AND condition — flat-AND preserved); `test_empty_filter_no_narrowing` asserts an empty list (no narrowing). KEEP `test_injection_value_neutralized` BYTE-FOR-BYTE green in spirit — assert the SQL/SSTI payload rides as the exact bound literal in the Fragment value (same byte-for-byte value check, only the wrapper shape updated). KEEP `test_unknown_op_rejected`, `test_unknown_field_rejected_at_save`, `test_underscore_field_excluded` UNCHANGED. Then author `supabase/migrations/074_view_typed_columns.sql` (next free number is 074, verified — current max 073) per the RESEARCH §"Migration Design" DDL: copy the 071 header + `BEGIN; ... COMMIT;` atomic wrapper + the apply-via-Supabase-SQL-editor / never-`db push` instruction; `ADD COLUMN document_type_norm text GENERATED ALWAYS AS (lower(metadata->>'document_type')) STORED`; `ADD COLUMN date_typed date GENERATED ALWAYS AS (CASE WHEN metadata->>'date' ~ '^\d{4}-\d{2}-\d{2}$' THEN (metadata->>'date')::date ELSE NULL END) STORED` (the ISO-regex CASE guard is load-bearing — a malformed stored date must yield NULL, never break the ALTER or a future insert, R-114-B); plus `CREATE INDEX idx_documents_document_type_norm ... USING btree (document_type_norm)` and `CREATE INDEX idx_documents_date_typed ... USING btree (date_typed)`. Add NO RLS (it modifies the already-RLS'd `documents` table — no new table, no new policy). DO NOT apply the migration; the file is authored only — Plan 03 (autonomous:false) applies it. Note the `::date` immutability fact in a comment (text::date is immutable → valid in a generation expr; text::timestamptz is NOT → would be rejected).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_113_view_filter_compiler.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_113_view_filter_compiler.py -q` is fully GREEN (rewritten containment tests + injection test all pass)
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_113_view_filter_compiler.py::test_injection_value_neutralized -q` GREEN (SC#4 invariant held through the widening)
    - `supabase/migrations/074_view_typed_columns.sql` exists; `grep -c 'GENERATED ALWAYS AS' supabase/migrations/074_view_typed_columns.sql` == 2
    - `grep -c 'CREATE INDEX' supabase/migrations/074_view_typed_columns.sql` == 2 and `grep -c 'btree' supabase/migrations/074_view_typed_columns.sql` == 2
    - The date column regex guard is present: `grep -c 'metadata->>'"'"'date'"'"'' supabase/migrations/074_view_typed_columns.sql` ≥ 1 AND a `CASE WHEN ... ~ '` guard wraps the `::date` cast
    - The migration is NOT applied (no `db push` task, `supabase/full-schema.sql` untouched in this plan)
  </acceptance_criteria>
  <done>The three 113 containment tests assert the Fragment contract, the SC#4 injection test stays green, and the 074 migration file is authored (un-applied) with both GENERATED STORED columns + btree indexes + the ISO-regex date guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (filter value) → compiler | Attacker-controlled filter VALUES enter `compile_filter`; must ride as bound Fragment literals, never SQL |
| client (filter field) → compiler | Field NAMES are constrained to the live whitelist by `validate_fields` (unchanged) before compile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-01-01 | Tampering | `compile_filter` / `view_operators_extra.py` (widened output) | mitigate | Every operator returns a `Fragment` whose `value` is a plain bound literal; NO f-string SQL / `.format()` in either module. `test_113::test_injection_value_neutralized` stays byte-for-byte green in the new fragment shape (verified Task 3). |
| T-114-01-02 | Tampering | field-name leg selection | mitigate | Fragment `field` is sourced from the validated AST whose fields passed `validate_fields` (whitelist ∪ enabled custom defs); `_`-prefixed fields rejected unconditionally (unchanged `validate_fields`). The typed leg targets CONSTANT column names (`document_type_norm`/`date_typed`), never user input. |
| T-114-01-03 | Elevation of Privilege | smuggled/forged operator | mitigate | The registry-miss `KeyError` (Pitfall 5) and the Pydantic `Literal` discriminator both survive the widening; `test_unknown_op_rejected` proves the closed dispatch stays closed (verified Task 2). |
| T-114-01-04 | Denial of Service | `074` migration date generation expr | mitigate | The `date_typed` generated column guards `(metadata->>'date')::date` with an ISO-regex `CASE` → NULL on non-ISO, so a malformed stored date can never break the ALTER or a future insert (R-114-B). Live verification gated by Plan 03's bad-date dataset test. |
| T-114-01-SC | Tampering | npm/pip installs | accept | This plan installs NO packages (RESEARCH Package Legitimacy Audit: none). No supply-chain surface. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/unit/test_113_view_filter_compiler.py tests/unit/test_114_view_filter_compiler.py -q` — both GREEN (the SC#4 injection test in 113 stays green; 114 per-operator + case-insensitive pass; relative-date window math deferred to Plan 02).
- `supabase/migrations/074_view_typed_columns.sql` authored with two GENERATED STORED columns + two btree indexes + the ISO-regex date guard; NOT applied.
- The Phase 114 SEAM import (`from . import view_operators_extra`) populates the registry; an unknown op still fails closed.
</verification>

<success_criteria>
- The compiler emits bound `list[Fragment]` descriptors; all VIEW-03 operators registered additively at the seam; SC#4 injection invariant preserved.
- The AST widens additively (pre-114 rows parse unchanged); case-insensitivity encoded query-value-side for normalized fields, ILIKE for free-text.
- The 074 migration file is authored (un-applied) following the 071 conventions, with the load-bearing ISO-regex date cast guard.
</success_criteria>

<output>
Create `.planning/phases/114-virtual-folders-range-date-filters-view-builder-sidebar/114-01-SUMMARY.md` when done.
</output>
