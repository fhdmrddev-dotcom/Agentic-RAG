---
phase: 119-document-governance-health
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/document_governance.py
  - backend/app/main.py
  - backend/tests/integration/test_119_broken.py
  - backend/tests/integration/test_119_unclassified.py
  - backend/tests/integration/test_119_low_conf.py
  - backend/tests/unit/test_119_low_conf_scan.py
  - backend/tests/integration/test_119_leak.py
  - backend/tests/test_119_governance.py
autonomous: true
requirements: [DGOV-01]
must_haves:
  truths:
    - "GET /document-governance/broken-relationships returns the caller's own edges whose far endpoint resolves to no readable latest (D-119-3)"
    - "GET /document-governance/unclassified returns the caller's latest docs where metadata._classification.status == 'suggested' (D-119-4)"
    - "GET /document-governance/low-confidence returns the caller's latest docs with any _confidence[field] < 0.5 (D-119-5)"
    - "All three routes are owner-scoped — user A never sees user B's broken/unclassified/low-conf docs"
    - "A masked-but-present relationship target is NOT reported as broken (masking != deletion)"
    - "A clean library returns {items:[], total:0} for each route (positive-empty-state-ready)"
  artifacts:
    - path: "backend/app/api/document_governance.py"
      provides: "Read-only governance aggregation router with 3 paginated signal routes"
      contains: "prefix=\"/document-governance\""
      min_lines: 90
    - path: "backend/app/main.py"
      provides: "Router mount"
      contains: "document_governance.router"
    - path: "backend/tests/integration/test_119_leak.py"
      provides: "Two-user live leak proof (clone of test_117_route_leak.py)"
  key_links:
    - from: "backend/app/api/document_governance.py"
      to: "document_relationship_service._resolve_readable_latest"
      via: "broken-edge classification (None == broken)"
      pattern: "_resolve_readable_latest"
    - from: "backend/app/api/document_governance.py"
      to: ".eq(\"user_id\", _uid(caller))"
      via: "owner-scoping (sole gate under service-role client)"
      pattern: "_uid"
    - from: "backend/app/main.py"
      to: "document_governance.router"
      via: "app.include_router"
      pattern: "include_router\\(document_governance"
---

<objective>
Build the read-only governance aggregation backend: a NEW `backend/app/api/document_governance.py`
router (cloned from `knowledge_health.py`) exposing THREE paginated signal routes —
`/broken-relationships`, `/unclassified`, `/low-confidence` — over the already-shipped DM tables,
under existing per-user RLS, with the app-code `.eq("user_id", _uid(caller))` predicate as the SOLE
owner-scoping gate (the service-role client bypasses RLS). Ship the Wave-0 test scaffolds (including
the two-user live leak harness) FIRST so the router is built against red tests.

Purpose: DGOV-01 — surface document-structure health signals (broken/dangling relationships,
unclassified docs, low-confidence metadata) so the upstream v3.0 work becomes legible. Pure consumer:
NO migration, NO new write path, NO new package.
Output: `document_governance.py` (3 routes + 3 `_fetch_*`), `main.py` mount, 6 Wave-0 test files.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-document-governance-health/119-CONTEXT.md
@.planning/phases/119-document-governance-health/119-RESEARCH.md
@.planning/phases/119-document-governance-health/119-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from live code this session — use directly, no exploration. -->

From backend/app/api/knowledge_health.py (THE clone target):
- `router = APIRouter(prefix="/knowledge-health", tags=["knowledge-health"])`
- `DEFAULT_LIMIT = 20`, `MAX_LIMIT = 100`
- `def _pagination_params(offset: int = Query(0, ge=0), limit: int = Query(DEFAULT_LIMIT, ge=1, le=MAX_LIMIT)) -> tuple[int, int]` — copy verbatim
- Sync `_fetch_*(supabase, user_id, offset, limit) -> {"items":[...], "total": N, "offset": O, "limit": L}` with bare `.execute()`
- `@router.get` handlers pull `user_id = current_user["id"]`, call `_fetch_*`, wrap exceptions in `HTTPException(status_code=502, detail="...temporarily unavailable")`
- `/summary` route is DEPRECATED (knowledge_health.py:668-674) — DO NOT clone it.

From backend/app/services/document_relationship_service.py:
- `def _uid(user_id) -> str` — returns `str(UUID(str(user_id)))`; raises ValueError on malformed input. The UUID-coerce-before-predicate hardening.
- `async def _resolve_readable_latest(doc_id_or_filename: str, caller, *, by_filename: bool = False, supabase: Client | None = None) -> dict | None` — returns the caller's LATEST ACCESSIBLE version row (own is_latest ∪ global-folder is_latest, follow-to-latest, post-follow visibility re-check) OR `None`. **`None` is exactly the broken signal.** This is ASYNC and uses aexec internally.
- Both `_uid` and `_resolve_readable_latest` are module-private (`_`-prefixed) but the 116/117 precedent freely cross-imports `_resolve_readable_latest` (one-core-no-fork) — direct import is consistent.

From backend/app/dependencies (used by knowledge_health.py):
- `get_current_user` (JWT → dict with "id"), `get_supabase` (SERVICE-ROLE Client — RLS bypassed)

From backend/app/api/documents.py (the _classification write site):
- `metadata._classification` shape: `{rule_id, rule_name, condition_summary, suggested_folder_id, suggested_folder_name, status}`
- status values: `"suggested"` (ingest), `"accepted"` (accept), and dismiss POPS the whole `_classification` key (NO `"dismissed"` status). So `.eq(...->>status, "suggested")` naturally excludes accepted+dismissed.

From frontend/src/components/metadata/ConfidenceChip.tsx (the 0.5 cutoff source of truth):
- `TIER = { HIGH: 0.75, MED: 0.5 }`; low = `score < 0.5`. The backend low-conf scan uses `0.5` to AGREE with this — do NOT introduce a second constant; reference it as "the ConfidenceChip TIER.MED low cutoff" in a comment.

From backend/tests/integration/test_117_route_leak.py (the leak-harness template):
- asyncpg pool against `postgresql://postgres:postgres@127.0.0.1:54322/postgres` (`POSTGRES_DSN` override), `_pg_reachable` skip-guard, FK-safe seed/teardown, OWN-scoped per-user rows, imports inside test bodies.
- Drive the REAL route via FastAPI `TestClient` + `get_current_user` override (inject A vs B) + `get_supabase` override (REAL service-role client).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — author the 6 test scaffolds (leak harness + signal + edge-case stubs)</name>
  <files>backend/tests/integration/test_119_broken.py, backend/tests/integration/test_119_unclassified.py, backend/tests/integration/test_119_low_conf.py, backend/tests/unit/test_119_low_conf_scan.py, backend/tests/integration/test_119_leak.py, backend/tests/test_119_governance.py</files>
  <read_first>
    - backend/tests/integration/test_117_route_leak.py (THE two-user leak harness template — asyncpg seed/teardown, TestClient dep-override, _pg_reachable skip-guard, non-vacuity discipline)
    - backend/tests/test_knowledge_health.py (the MagicMock-builder UNIT pattern for empty-shape + scan edge cases)
    - .planning/phases/119-document-governance-health/119-RESEARCH.md (§Validation Architecture — the Wave 0 Gaps list + the leak-safety properties + non-vacuity rules)
    - .planning/phases/119-document-governance-health/119-VALIDATION.md (the Per-Task Verification Map this plan populates)
  </read_first>
  <action>
    Create the six Wave-0 test files listed in RESEARCH §Validation Architecture, matching those exact
    filenames so VALIDATION.md references resolve. Author them as RED/skip-tolerant scaffolds (xfail
    strict=False OR skip-if-route-absent) so the suite exits 0 before Task 2 lands the router — the
    Phase 111/116 Wave-0 convention.

    (1) backend/tests/integration/test_119_leak.py — clone the test_117_route_leak.py harness
    (asyncpg pool, _pg_reachable skip-guard, FK-safe seed/teardown, TestClient with get_current_user
    overridden A vs B + get_supabase overridden to the REAL service-role client). Assert user A's
    governance lists (all 3 routes) NEVER contain user B's docs/edges. NON-VACUITY twin: seed at least
    one TRUE positive PER user PER signal so an "all clear" pass cannot false-green (broken: an edge
    whose target is superseded-out/deleted; unclassified: a status:"suggested" doc; low-conf: a doc
    with a 0.4 confidence field). Add the masked-not-broken case: a 117-masked target is NOT reported
    as broken (masking != deletion), with the non-vacuity twin that the doc's owner does not see it as
    broken either (it resolves).

    (2) backend/tests/integration/test_119_broken.py — a fully-deleted/superseded edge target surfaces
    in /broken-relationships; a resolvable target does NOT. Per A1, ALSO pin the real deletion
    semantics live: seed an edge, then delete/supersede the target, and assert whether the broken card
    surfaces it OR CASCADE removed the edge (document the observed behavior in a test comment — this is
    the A1 open-question resolution). Include `test_masked_not_broken`.

    (3) backend/tests/integration/test_119_unclassified.py — a doc with _classification.status=="suggested"
    surfaces; an accepted doc (status "accepted") and a dismissed doc (no _classification key at all)
    do NOT. A3: pin the EXACT PostgREST deep-jsonb path filter live against :54322 — start with
    `.eq("metadata->_classification->>status", "suggested")`; if it returns nothing, the quoted arrow
    form `metadata->'_classification'->>'status'` — assert the chosen form actually returns the seeded
    suggested doc (this test is the live verification of A3).

    (4) backend/tests/integration/test_119_low_conf.py — a doc with any _confidence[field] < 0.5
    surfaces; a doc with all fields >= 0.5 does NOT.

    (5) backend/tests/unit/test_119_low_conf_scan.py — MagicMock-builder UNIT edge cases for the
    Python scan: `0.0` is a LEGITIMATE low value (counts; never truthiness-test), a missing
    `_confidence` key does NOT error (skipped), non-numeric / None / bool values are guarded
    (isinstance int|float and not isinstance bool).

    (6) backend/tests/test_119_governance.py — MagicMock empty-shape unit tests: each `_fetch_*`
    returns `{"items":[], "total":0, "offset":0, "limit":20}` for a clean library (the positive-empty
    contract the frontend cards consume).

    Reference DGOV-01 in each test module docstring.
  </action>
  <verify>
    <automated>cd backend && source venv/Scripts/activate && python -m pytest tests/test_119_governance.py tests/unit/test_119_low_conf_scan.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All 6 files exist at the RESEARCH §Validation Architecture paths.
    - `pytest tests/test_119_governance.py tests/unit/test_119_low_conf_scan.py -q` exits 0 (scaffolds pass or xfail/skip cleanly).
    - The full collection does not error: `pytest tests/ --collect-only -q` exits 0.
    - test_119_leak.py contains a two-user assertion AND at least one seeded true-positive per signal (grep: `def test_` count >= 3; `_pg_reachable` skip-guard present).
    - test_119_unclassified.py asserts the chosen PostgREST jsonb-path filter returns the seeded suggested doc (A3 live-pinned).
    - test_119_low_conf_scan.py asserts `0.0` counts as low AND a missing `_confidence` does not raise.
  </acceptance_criteria>
  <done>Six Wave-0 test files exist matching VALIDATION.md filenames; the suite collects + the unit subset exits 0; the leak + edge-case + A3 assertions are present (non-vacuous).</done>
</task>

<task type="auto">
  <name>Task 2: Build the document_governance.py router (3 signal routes) + mount in main.py</name>
  <files>backend/app/api/document_governance.py, backend/app/main.py</files>
  <read_first>
    - backend/app/api/knowledge_health.py (THE clone target — router prefix, _pagination_params, _fetch_* shape, paginated @router.get handlers, 502 error wrap, the never-retrieved SQL-filter pattern + the low-confidence cap+Python-filter+sort+slice pattern)
    - backend/app/services/document_relationship_service.py (_uid:75-87, _resolve_readable_latest:162-266 — import both; the broken signal is the ONE async deviation from the analog)
    - backend/app/main.py (the existing `from app.api import ...` line + the include_router mounts — add document_governance to both)
    - .planning/phases/119-document-governance-health/119-PATTERNS.md (§ document_governance.py — the verbatim _pagination_params copy, the unclassified .eq-on-jsonb-path, the low-conf Python-scan guards, the async-deviation + run_in_threadpool note)
  </read_first>
  <action>
    Create backend/app/api/document_governance.py cloning the knowledge_health.py router shape:
    `router = APIRouter(prefix="/document-governance", tags=["document-governance"])`; carry
    `DEFAULT_LIMIT = 20` and `MAX_LIMIT = 100` and copy `_pagination_params` verbatim. DO NOT clone
    the deprecated `/summary` route — the frontend counter header derives its 3 counts from each
    list's `total`.

    Owner-scoping is the SOLE gate (get_supabase is service-role, RLS bypassed): EVERY query carries
    `.eq("user_id", _uid(caller))`. Import `_uid` and `_resolve_readable_latest` from
    document_relationship_service (the 116/117 one-core-no-fork precedent — do NOT re-derive the
    resolver; re-rolling it re-opens leaks).

    Route 1 — `GET /broken-relationships` (D-119-3, the ONE async route). Make `_fetch_broken_relationships`
    async: enumerate the caller's OWN edges (`document_relationships.select("id, source_doc_id,
    target_doc_id, rel_type").eq("user_id", _uid(caller))`); for each edge, for each endpoint id,
    `await _resolve_readable_latest(end_id, caller, supabase=supabase)` — a `None` return marks that
    endpoint BROKEN (target fully deleted / superseded-out, no readable latest); a non-None return
    (including a masked-but-present row) is NOT broken. Per D-119-3 a masked target is never a break;
    document in a comment that for OWN-scoped edges the cross-user existence-leak concern is structurally
    moot (the resolver can't distinguish deleted from now-unreadable, both → None, which for the user's
    own edge is acceptable — the far endpoint is no longer actionable). Build each broken item carrying
    the READABLE end's doc id (`readable_doc_id`) so the frontend link-out can open the openable end,
    plus `relationship_id`, `rel_type`, `broken_doc_id`. Paginate in Python (`broken[offset:offset+limit]`,
    `total = len(broken)`). The async route handler awaits this fetch directly. Honor D-v2.5-01: this is
    async via aexec already — no bare `.execute()` inside the async path.

    Route 2 — `GET /unclassified` (D-119-4, SQL-expressible). `_fetch_unclassified` mirrors
    `_fetch_never_retrieved`: `documents.select("id, filename, folder_id, metadata", count="exact")
    .eq("user_id", _uid(caller)).eq("is_latest", True)` plus the deep-jsonb path equality on
    `_classification.status == "suggested"` (use the exact PostgREST path form pinned GREEN by Task 1's
    test_119_unclassified.py — `.eq("metadata->_classification->>status", "suggested")` or the quoted
    arrow form). `.range(offset, offset+limit-1)`. Return `{items, total: res.count or 0, offset, limit}`.
    Each item: document_id, filename, folder_id (+ the suggestion's suggested_folder_name for the row, read
    from metadata._classification). dismiss POPs the key and accept sets status "accepted", so this query
    naturally excludes both — do NOT add `"dismissed"` handling.

    Route 3 — `GET /low-confidence` (D-119-5, NOT PostgREST-expressible → Python scan). `_fetch_low_confidence`
    mirrors the knowledge_health low-confidence cap+Python-filter+sort+slice: fetch the caller's latest
    docs capped (~2000, like the analog's .in_() cap), then in Python keep docs where ANY
    `metadata["_confidence"]` value is `< 0.5` (the ConfidenceChip TIER.MED low cutoff — reference it in
    a comment, do NOT introduce a second constant). Guard: skip docs with no `_confidence` dict; only
    count values that are `isinstance(v, (int, float)) and not isinstance(v, bool)`; `0.0` is a
    LEGITIMATE low value (never truthiness-test). Each item: document_id, filename, folder_id, `low_fields`
    (the {field: score} below-cutoff map for the row chip), `min_confidence` (= min of low_fields). Sort
    ascending by min_confidence; paginate (`low[offset:offset+limit]`, `total = len(low)`).

    Wrap the two SYNC fetches (unclassified/low-conf) called from their async route handlers in
    `run_in_threadpool` per CLAUDE.md D-v2.5-01 (do NOT block the event loop with bare supabase-py calls).

    Each `@router.get` handler: `user_id = current_user["id"]`, `offset, limit = offset_limit`, call the
    fetch inside a try/except wrapping exceptions in `HTTPException(status_code=502, detail="Governance
    metrics temporarily unavailable")`. A malformed doc id must collapse to a calm response, not a 500
    (the resolver returns None; _uid raises ValueError on a malformed user_id, caught by the 502 wrap).

    Mount in backend/app/main.py: add `document_governance` to the existing `from app.api import ...`
    import line AND `app.include_router(document_governance.router)` next to the other DM-router mounts,
    with a comment "Phase 119 DGOV-01/02 — read-only governance aggregation".

    DMF-03 (non-action, document in a module comment): do NOT gate this router behind
    `document_management_enabled` — 111/112/113/116 explicitly chose not to gate at the router surface
    (the flag is dormant at every DM surface, SEED-080/v3.2 entitlement seam). Gating Governance alone
    would make it the lone inconsistent surface.
  </action>
  <verify>
    <automated>cd backend && source venv/Scripts/activate && python -m pytest tests/test_119_governance.py tests/unit/test_119_low_conf_scan.py tests/integration/test_119_broken.py tests/integration/test_119_unclassified.py tests/integration/test_119_low_conf.py tests/integration/test_119_leak.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/api/document_governance.py` exists with `prefix="/document-governance"` and exactly 3 `@router.get` routes (broken-relationships, unclassified, low-confidence); NO `/summary` route.
    - `_fetch_broken_relationships` is `async def` and contains `_resolve_readable_latest` (grep: `async def _fetch_broken` AND `_resolve_readable_latest`).
    - Every query carries owner-scoping: `grep -c "_uid(" document_governance.py` >= 3 (one per signal) AND no governance `.table("documents")`/`.table("document_relationships")` query omits `.eq("user_id"`.
    - The low-conf scan uses the `0.5` cutoff with the int|float-and-not-bool guard and counts `0.0` (grep: `not isinstance` + `0.5`); no second threshold constant introduced.
    - `backend/app/main.py` contains `document_governance` in its import line AND `include_router(document_governance.router)`.
    - The two SYNC fetches are wrapped in `run_in_threadpool` (grep: `run_in_threadpool` present).
    - All 6 Wave-0 test files now pass live against :54322 (the leak test is NON-VACUOUS — seeded true-positives present): `pytest tests/integration/test_119_*.py tests/test_119_governance.py tests/unit/test_119_low_conf_scan.py -q` exits 0.
    - DMF-03 non-gating documented in a module comment (grep: `document_management_enabled` referenced in a comment explaining the non-gate, NOT as an actual gate call).
    - `threads.py` byte-untouched (G-5): `git diff --stat` shows no change to backend/app/api/threads.py.
  </acceptance_criteria>
  <done>The 3-route governance router is live + mounted; all 6 Wave-0 tests pass against :54322 (incl. the non-vacuous two-user leak proof + the masked-not-broken case + the A3-pinned unclassified filter); owner-scoping is on every query; no migration, no new package, threads.py untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /document-governance routes | JWT-authenticated caller; `current_user["id"]` is the owner identity |
| router → service-role Supabase | `get_supabase()` BYPASSES RLS — the app-code `.eq("user_id", _uid(caller))` is the SOLE owner gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-01-01 | Information Disclosure | All 3 `_fetch_*` queries | mitigate | Every query carries `.eq("user_id", _uid(caller))`; the broken signal additionally rides `_resolve_readable_latest` (already owner-scoped). Proven by the NON-VACUOUS two-user live leak test (`test_119_leak.py`) — A never sees B's docs/edges. |
| T-119-01-02 | Information Disclosure | broken-relationships (existence oracle) | mitigate | D-119-3: a masked-but-present target is NEVER reported as broken (masking != deletion). Own-scoped edges only; never surface a cross-user existence signal. Tested by `test_119_broken.py::test_masked_not_broken` (non-vacuity twin: the owner does not see it as broken either). |
| T-119-01-03 | DoS / Information Disclosure | malformed doc/user id → 500 | mitigate | `_uid()` raises ValueError on a malformed user_id (caught by the 502 wrap); `_resolve_readable_latest` returns None on an unresolvable id. No string interpolation into SQL — PostgREST bound predicates + `_uid`-coerced filters only. Route wraps all exceptions in a calm 502. |
| T-119-01-04 | Tampering | low-confidence row provenance | mitigate | The row renders the RAW `metadata._confidence` value (never a fabricated score); the `0.5` cutoff is the single `ConfidenceChip.TIER.MED` source-of-truth (no second threshold). |
| T-119-01-05 | Repudiation | audit gap on reads | accept | Reads are NOT audited (mirrors `resolve_view`/`get_relationships`; `VALID_ACTION_TYPES` has no read action type). This phase introduces NO write path — confirmed: zero migration, zero new write endpoint. |
| T-119-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (verified: zero new dependency, RESEARCH §Package Legitimacy Audit satisfied vacuously). |
</threat_model>

<verification>
- All 6 Wave-0 test files pass live against :54322 (the two-user leak test is non-vacuous; the masked-not-broken case holds; the A3 PostgREST jsonb-path filter is live-pinned).
- `cd backend && source venv/Scripts/activate && python -m pytest tests/integration/test_119_*.py tests/test_119_governance.py tests/unit/test_119_low_conf_scan.py -q` exits 0.
- Net-new failures = 0 (base-checkout: the changed-file diff at HEAD vs base produces the same pre-existing failure set; document in SUMMARY per the 116/117 convention).
- `threads.py` byte-untouched (G-5).
- No migration file added; no `pip install`.
</verification>

<success_criteria>
- `document_governance.py` exists with 3 owner-scoped paginated routes (broken/unclassified/low-conf), mounted in main.py.
- Each signal definition matches its locked decision: broken = `_resolve_readable_latest` None (D-119-3), unclassified = `_classification.status == "suggested"` (D-119-4), low-conf = any `_confidence[field] < 0.5` reusing the ConfidenceChip TIER.MED cutoff (D-119-5).
- Two-user leak proof GREEN (non-vacuous); masked-not-broken GREEN.
- No migration, no new package, no new write path, threads.py untouched.
</success_criteria>

<output>
Create `.planning/phases/119-document-governance-health/119-01-SUMMARY.md` when done.
</output>
