---
phase: 119-document-governance-health
plan: 02
type: execute
wave: 2
depends_on: ["119-01"]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/components/health/GovernanceRow.tsx
  - frontend/src/pages/GovernancePage.tsx
  - frontend/src/App.tsx
  - frontend/src/lib/nav-items.ts
  - frontend/src/components/layout/ChatLayout.tsx
  - frontend/src/pages/__tests__/GovernancePage.test.tsx
autonomous: true
requirements: [DGOV-01, DGOV-02]
must_haves:
  truths:
    - "The Governance nav item is reachable: clicking it renders GovernancePage (NOT KnowledgeHealthPage)"
    - "GovernancePage shows a counter header + 3 stacked cards (broken / unclassified / low-confidence) each with a positive 'all clear' empty state"
    - "Each governance signal row links/navigates to the document's DocumentDetailPanel (no inline write actions)"
    - "An empty signal response does NOT trigger an infinite re-fetch loop; Refresh clears the init guard"
    - "The page reuses the HealthPanel-styled card shell + ConfidenceChip + DocumentDetailPanel — Deep Midnight/Aether, mobile-responsive, WCAG AA"
  artifacts:
    - path: "frontend/src/pages/GovernancePage.tsx"
      provides: "The Governance top-level home — counter header + 3 stacked cards + own DocumentDetailPanel mount"
      contains: "initializedTabsRef"
      min_lines: 120
    - path: "frontend/src/components/health/GovernanceRow.tsx"
      provides: "Link-out-only row (no inline mutate actions — D-119-6)"
      min_lines: 20
    - path: "frontend/src/App.tsx"
      provides: "ActiveView union extended with 'governance'"
      contains: "governance"
    - path: "frontend/src/lib/nav-items.ts"
      provides: "Governance NAV_ITEMS entry with a distinct lucide glyph"
      contains: "governance"
    - path: "frontend/src/components/layout/ChatLayout.tsx"
      provides: "The governance render branch before the trailing KnowledgeHealthPage else"
      contains: "GovernancePage"
  key_links:
    - from: "frontend/src/lib/nav-items.ts"
      to: "App.tsx ActiveView 'governance'"
      via: "NAV_ITEMS entry { view: 'governance' }"
      pattern: "view: \"governance\""
    - from: "frontend/src/components/layout/ChatLayout.tsx"
      to: "GovernancePage"
      via: "activeView === 'governance' render branch"
      pattern: "activeView === \"governance\""
    - from: "frontend/src/pages/GovernancePage.tsx"
      to: "DocumentDetailPanel"
      via: "row click → setSelectedDocId → panel mount (DGOV-02 link-out)"
      pattern: "DocumentDetailPanel"
    - from: "frontend/src/pages/GovernancePage.tsx"
      to: "/document-governance/* routes"
      via: "api.ts fetch helpers"
      pattern: "getGov(Broken|Unclassified|LowConfidence)"
---

<objective>
Build the Governance top-level home and own ALL THREE navigation edits IN-PHASE (the D-119-2
load-bearing requirement — the Phase 118 lesson). The page is "Library Health, three different lists,
read-only, link-out instead of inline-remove": a counter header + 3 stacked HealthPanel-styled cards
(broken / unclassified / low-confidence), each fetched independently behind the `initializedTabsRef`
infinite-loop guard, each row a pure link-out to the document's `DocumentDetailPanel`.

Purpose: DGOV-01 (the governance view + 3 signals), DGOV-02 (each row links to its fix), UX-01
(Deep Midnight/Aether, mobile-responsive, WCAG AA via HealthPanel reuse).
Output: 3 api helpers, a new `GovernanceRow`, a new `GovernancePage`, the App.tsx union + nav-items
entry + ChatLayout branch (the D-119-2 triad), and the Vitest reachability/link-out/no-loop tests.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-document-governance-health/119-CONTEXT.md
@.planning/phases/119-document-governance-health/119-RESEARCH.md
@.planning/phases/119-document-governance-health/119-PATTERNS.md
@.planning/phases/119-document-governance-health/119-01-SUMMARY.md

<interfaces>
<!-- Key contracts extracted from live code this session — use directly, no exploration. -->

Backend routes shipped by Plan 119-01 (the api.ts helpers wrap these):
- `GET /document-governance/broken-relationships?offset=&limit=` → `{items:[{relationship_id, rel_type, broken_doc_id, readable_doc_id}], total, offset, limit}` — the row link-out opens `readable_doc_id` (the openable end).
- `GET /document-governance/unclassified?offset=&limit=` → `{items:[{document_id, filename, folder_id, suggested_folder_name?}], total, offset, limit}`
- `GET /document-governance/low-confidence?offset=&limit=` → `{items:[{document_id, filename, folder_id, low_fields:{field:score}, min_confidence}], total, offset, limit}`

From frontend/src/App.tsx:9 (CURRENT — extend, do not rewrite):
`export type ActiveView = "chat" | "documents" | "skills" | "settings" | "library-health" | "workflows" | "classification-rules"`
ADD ` | "governance"`.

From frontend/src/lib/nav-items.ts (CURRENT NAV_ITEMS — add an entry; glyphs taken: MessageSquare/Workflow/FileText/Wand2/Activity/Zap/Settings):
Import a DISTINCT glyph — `ShieldCheck` OR `ClipboardCheck` (NOT Wand2=Classification, NOT Activity=Library Health). Add `{ view: "governance", icon: <glyph>, label: "Governance" }` near the library-health entry.

From frontend/src/components/layout/ChatLayout.tsx:294-301 (the classification-rules branch is the EXACT template — additive BEFORE the trailing `: ( <KnowledgeHealthPage /> )` else):
```
) : activeView === "classification-rules" ? (
  <ClassificationRulesPage />
) : (
  <KnowledgeHealthPage />
)}
```
Add a `) : activeView === "governance" ? ( <GovernancePage /> ) :` branch BEFORE the final else. NOTE: the classification-rules branch self-fetches with NO props (three-homes, no router) — GovernancePage follows the same no-props pattern.

From frontend/src/components/health/HealthPanel.tsx (the card shell — REUSE its <Card> styling, NOT its body):
- `<Card className="ghost-border bg-card/50 shadow-sm">` + `<CardHeader>` (Icon + title + count) + `<CardContent className="p-0">`.
- It hardcodes `HealthDocumentRow` (inline delete/reingest/move) and has NO PaginationControls — so it CANNOT be reused verbatim for read-only link-out rows (A6). Build a HealthPanel-styled Card that maps `GovernanceRow`s instead.
- `HealthEmptyState` (`variant="positive"`) is the "all clear" state — reuse it directly for total===0.

From frontend/src/components/metadata/ConfidenceChip.tsx:
- `import { ConfidenceChip, TIER } from "@/components/metadata/ConfidenceChip"` (the 112 metadata one — NOT the local retrieval-similarity ConfidenceChip inside KnowledgeHealthPage.tsx). Render the worst field: `<ConfidenceChip score={min_confidence} />` (honest raw score).

From frontend/src/components/metadata/DocumentDetailPanel.tsx:121-131 (the link-out target — props {doc, onClose, onReconcile}):
- It needs a FULL Document object (carries .metadata), not just an id. It has NO `openSection` prop (A7) — v1 opens the panel by doc id; sections default-open independently. The page must fetch/hold the Document for the clicked row.

From frontend/src/pages/KnowledgeHealthPage.tsx:243-251, 385-390 (the D-119-9 infinite-loop guard — carry verbatim):
- `const initializedTabsRef = useRef<Set<key>>(new Set())`; the fetch effect only fires for keys not yet in the set; Refresh does `initializedTabsRef.current.clear()` then re-triggers.

From frontend/src/pages/IngestionPage.tsx:88, 136-138, 564-570 (the open-by-id pattern):
- `const [selectedDocId, setSelectedDocId] = useState<string|null>(null)`; `selectedDoc = useMemo(...documents.find(d=>d.id===selectedDocId)...)`; `{selectedDoc && <DocumentDetailPanel doc={selectedDoc} onClose={()=>setSelectedDocId(null)} onReconcile={...} />}`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: api.ts helpers + GovernanceRow (link-out-only row) + GovernancePage (3 cards + init guard + panel mount)</name>
  <files>frontend/src/lib/api.ts, frontend/src/components/health/GovernanceRow.tsx, frontend/src/pages/GovernancePage.tsx</files>
  <read_first>
    - frontend/src/pages/KnowledgeHealthPage.tsx (the FETCH + initializedTabsRef guard + Refresh-clears-ref pattern — lines 243-251, 385-390; carry the guard verbatim, REJECT the Tabs layout per D-119-7)
    - frontend/src/components/health/HealthPanel.tsx (the <Card> shell styling to mirror; note it hardcodes HealthDocumentRow + has no pager → build a HealthPanel-styled Card mapping GovernanceRow instead, A5/A6)
    - frontend/src/components/health/HealthDocumentRow.tsx (clone ONLY the row chrome — file icon + truncated filename + chip; STRIP every inline mutate button/dialog per D-119-6)
    - frontend/src/components/health/HealthEmptyState.tsx (variant="positive" "all clear" empty state — reuse directly when total===0)
    - frontend/src/components/metadata/ConfidenceChip.tsx (the 112 metadata ConfidenceChip + TIER — the low-conf row chip; NOT the local KnowledgeHealthPage one)
    - frontend/src/components/metadata/DocumentDetailPanel.tsx (the link-out target — props {doc, onClose, onReconcile}; needs a full Document object; no openSection prop, A7)
    - frontend/src/pages/IngestionPage.tsx (the open-by-id selectedDocId/selectedDoc/panel-mount pattern, lines 88/136-138/564-570)
    - frontend/src/lib/api.ts (the existing knowledge-health fetch helpers — mirror their auth/error handling for the 3 new getGov* helpers)
  </read_first>
  <behavior>
    - GovernanceRow renders the file icon + truncated filename + a metric chip; the WHOLE row is a link-out (onClick → onOpen(docId)); it ships NO Delete/Re-ingest/Move buttons and imports neither deleteDocument nor reingestDocument.
    - GovernancePage fetches all 3 signals on mount EXACTLY ONCE per card (the initializedTabsRef guard) — an empty (total:0) response does NOT re-fire the fetch.
    - Refresh clears initializedTabsRef and re-triggers all 3 fetches.
    - total:0 for a card renders HealthEmptyState variant="positive" ("all clear").
    - Clicking a row opens DocumentDetailPanel for that doc (broken card opens readable_doc_id); closing it clears selectedDocId.
    - The counter header shows the 3 totals derived from each card's `total`.
  </behavior>
  <action>
    (1) frontend/src/lib/api.ts — add 3 fetch helpers `getGovBroken(offset, limit)`,
    `getGovUnclassified(offset, limit)`, `getGovLowConfidence(offset, limit)` hitting
    `GET /document-governance/{broken-relationships,unclassified,low-confidence}`, returning the
    `{items, total, offset, limit}` shape. Mirror the existing knowledge-health helpers' auth-header +
    throw-on-non-ok handling verbatim.

    (2) frontend/src/components/health/GovernanceRow.tsx — a NEW link-out-only row (A6/Pitfall 5).
    Clone ONLY the row chrome from HealthDocumentRow (`getFileIcon(filename)` + truncated filename +
    a chip slot); the whole row is a button/clickable that calls `onOpen(docId)`. Props:
    `{ docId, filename, chip?: React.ReactNode, onOpen: (id: string) => void }`. NO inline mutate
    actions, NO import of deleteDocument/reingestDocument/MoveToFolderDialog (D-119-6 read-only). a11y:
    the row is keyboard-operable (role/button + focus-visible).

    (3) frontend/src/pages/GovernancePage.tsx — the Governance top-level home (no props; self-fetching,
    three-homes/no-router, exactly like ClassificationRulesPage). Compose:
    - A counter header (h1 distinct from "Library Health" — e.g. "Document Governance" / "Governance
      Health"; Claude's discretion within "distinct from Library Health") showing the 3 card totals.
    - 3 STACKED HealthPanel-styled <Card>s (D-119-7 — NOT Tabs): "Broken relationships", "Unclassified
      documents", "Low-confidence metadata". Each card maps its `items` to GovernanceRow; total===0
      renders HealthEmptyState variant="positive" ("all clear"). Per A5 use the lighter default —
      HealthPanel-style top-N + a "show more" (or HealthPanel's cap behavior) and surface the TRUE
      `total` in the header count; do NOT block on full PaginationControls (document the A5 choice in a
      comment). The low-conf row's chip is `<ConfidenceChip score={item.min_confidence} />` (the 112
      metadata chip — honest raw score). The unclassified row may show `suggested_folder_name`.
    - The D-119-9 LANDMINE: a `const initializedTabsRef = useRef<Set<CardKey>>(new Set())` where
      `CardKey = "broken" | "unclassified" | "low-confidence"`; a fetch effect iterates the card keys
      and only fires for keys NOT yet in the set; on success/empty it stays initialized (a total:0
      response must NOT re-trigger). A Refresh button does `initializedTabsRef.current.clear()` then
      re-triggers all 3 fetches. Carry verbatim from KnowledgeHealthPage.tsx:243-251/385-390.
    - DGOV-02 link-out: this page OWNS its own `selectedDocId` state + `DocumentDetailPanel` mount (it
      is a top-level home, NOT inside IngestionPage). On row click, set the selected doc and render
      `<DocumentDetailPanel doc={selectedDoc} onClose={...} onReconcile={refresh} />`. DocumentDetailPanel
      needs a FULL Document (with .metadata): the unclassified/low-conf items don't carry full metadata,
      so fetch the document by id on click (reuse the existing single-doc fetch helper in api.ts, or
      fetch the doc list and find it) — open the READABLE end for the broken card (`readable_doc_id`).
      Per A7 there is NO openSection prop: open the panel by doc id; section-deep-link is optional polish
      (do NOT add a new prop to DocumentDetailPanel in v1).
    - Honest states: loading (role=status) != error (role=alert + Try again) != empty (positive). Deep
      Midnight/Aether tokens; the page mobile-stacks the 3 cards naturally (< 768px single column).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/api.ts` exports `getGovBroken`, `getGovUnclassified`, `getGovLowConfidence` hitting `/document-governance/...`.
    - `GovernanceRow.tsx` does NOT import `deleteDocument` or `reingestDocument` or `MoveToFolderDialog` (grep: zero matches) — read-only link-out (D-119-6).
    - `GovernancePage.tsx` contains `initializedTabsRef` AND a Refresh handler calling `.clear()` (D-119-9).
    - `GovernancePage.tsx` mounts `DocumentDetailPanel` and holds its own `selectedDocId` state (DGOV-02).
    - The 3 cards use `HealthEmptyState` with `variant="positive"` for total===0.
    - The low-conf chip imports `ConfidenceChip` from `@/components/metadata/ConfidenceChip` (NOT the local KnowledgeHealthPage one).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>api.ts has the 3 governance helpers; GovernanceRow is link-out-only; GovernancePage renders the counter header + 3 positive-empty-state cards with the init-guard + own DocumentDetailPanel mount; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Own the D-119-2 navigation triad — App.tsx union + nav-items entry + ChatLayout branch (ALL THREE)</name>
  <files>frontend/src/App.tsx, frontend/src/lib/nav-items.ts, frontend/src/components/layout/ChatLayout.tsx</files>
  <read_first>
    - frontend/src/App.tsx (line 9 — the ActiveView union to extend)
    - frontend/src/lib/nav-items.ts (the NAV_ITEMS array + the taken-glyphs import line + the three-homes contract header comment)
    - frontend/src/components/layout/ChatLayout.tsx (lines 294-301 — the classification-rules branch is the EXACT additive template, BEFORE the trailing KnowledgeHealthPage else)
    - .planning/phases/119-document-governance-health/119-CONTEXT.md (D-119-1 glyph guidance + D-119-2 the load-bearing all-three-in-one-plan rule)
  </read_first>
  <action>
    Make ALL THREE edits in this one task (the D-119-2 LOCKED requirement — Phase 118 shipped its rules
    page built-but-unreachable because no single plan owned the mount+nav; a 119 plan MUST own all three
    or the surface is dead):

    (a) frontend/src/App.tsx:9 — extend the `ActiveView` union with ` | "governance"` (do not rewrite
    the line; append the new member).

    (b) frontend/src/lib/nav-items.ts — import a DISTINCT non-reused lucide glyph (`ShieldCheck` is the
    recommended pick — NOT Wand2=Classification, NOT Activity=Library Health) and add
    `{ view: "governance", icon: ShieldCheck, label: "Governance" }` to NAV_ITEMS, placed adjacent to
    the `library-health` entry (a governance peer-home). Keep the label distinct from "Library Health"
    (D-119-1). Update the file's header comment if it enumerates the homes.

    (c) frontend/src/components/layout/ChatLayout.tsx — add the render branch
    `) : activeView === "governance" ? ( <GovernancePage /> )` BEFORE the trailing
    `: ( <KnowledgeHealthPage /> )` else (mirror the classification-rules branch at :294-301 exactly);
    import GovernancePage at the top. No props (three-homes, no router) — matches ClassificationRulesPage.

    Do NOT gate the nav entry behind DMF-03 (the dormant flag — A8/research verdict; 113-118 ungated;
    gating Governance alone is the lone inconsistent surface). Add a one-line comment near the nav entry
    noting the deliberate non-gate.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `App.tsx` ActiveView union contains `"governance"` (grep: `ActiveView` line includes `governance`).
    - `nav-items.ts` NAV_ITEMS contains `view: "governance"` AND imports a distinct glyph (ShieldCheck/ClipboardCheck) not already used by another entry.
    - `ChatLayout.tsx` renders `<GovernancePage` when `activeView === "governance"` AND imports GovernancePage; the branch is BEFORE the trailing `<KnowledgeHealthPage />` else.
    - `npx tsc --noEmit` exits 0 (the union member is now exhaustively handled).
    - All THREE edits present in this single task's diff (D-119-2 — the surface is reachable).
  </acceptance_criteria>
  <done>The ActiveView union, the NAV_ITEMS entry (distinct glyph), and the ChatLayout governance branch are all wired in one task — clicking Governance renders GovernancePage, not KnowledgeHealthPage; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: GovernancePage Vitest — reachability, link-out, no-refetch-loop, positive empty state</name>
  <files>frontend/src/pages/__tests__/GovernancePage.test.tsx</files>
  <read_first>
    - frontend/src/pages/__tests__ (or the nearest existing page test — e.g. ClassificationRulesPage.test.tsx / KnowledgeHealthPage test — for the render+mock-api harness shape, TooltipProvider wrapping, and the no-loop assertion pattern)
    - frontend/src/pages/GovernancePage.tsx (the component under test — built in Task 1)
    - .planning/phases/119-document-governance-health/119-RESEARCH.md (§Validation Architecture — the frontend test rows: nav reachability, link-out, no-refetch-loop, empty state)
  </read_first>
  <behavior>
    - With all 3 endpoints mocked to total:0, each card renders the positive "all clear" empty state and the fetch fires EXACTLY ONCE per card (no infinite loop — assert the mock call count is 1 per endpoint after settle).
    - With a seeded item per card, the rows render; clicking a row opens DocumentDetailPanel (assert the panel mounts / the doc fetch fires) — DGOV-02 link-out, with NO write-endpoint call.
    - Refresh re-fires the 3 fetches (call count increments) — proving the init-ref clears.
    - The rows expose no inline delete/reingest/move controls (D-119-6).
  </behavior>
  <action>
    Create frontend/src/pages/__tests__/GovernancePage.test.tsx. Mock the 3 api helpers
    (getGovBroken/getGovUnclassified/getGovLowConfidence) + the single-doc fetch helper. Cover:
    (1) empty-state render with the NO-REFETCH-LOOP assertion — after the component settles, each mocked
    endpoint was called exactly once (the D-119-9 guard; the regression that BUG-260516-02 caused);
    (2) populated render → click a row → DocumentDetailPanel mounts / the doc fetch fires (DGOV-02),
    and assert NO write endpoint (deleteDocument/reingestDocument/move) was called;
    (3) Refresh → the 3 fetches fire again (init-ref cleared);
    (4) rows expose no inline mutate controls.
    Wrap the render in TooltipProvider if the component tree needs it (the 117/118 test-harness shim).
    Reference DGOV-01/DGOV-02 in the test file docstring.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run GovernancePage</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/__tests__/GovernancePage.test.tsx` exists.
    - `npx vitest run GovernancePage` exits 0.
    - The no-refetch-loop test asserts each mocked governance endpoint was called exactly once on empty (mock.calls length === 1 per endpoint).
    - The link-out test asserts a row click mounts DocumentDetailPanel / fires the doc fetch AND that no write endpoint was called.
    - The Refresh test asserts the fetches re-fire (call count increments).
  </acceptance_criteria>
  <done>GovernancePage Vitest covers reachability/empty-state/no-loop/link-out/Refresh; all pass; net-new failures = 0 (the pre-existing streaming/chat/provider failing-file roster is unchanged — document in SUMMARY).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nav click → ActiveView state | The Governance home must be reachable (D-119-2); an unhandled view falls through to KnowledgeHealthPage |
| governance row → DocumentDetailPanel | Read-only navigation only; NO inline write actions (D-119-6) |
| browser → backend governance routes | Owner-scoping enforced server-side (Plan 01); the frontend renders only the caller's own data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-02-01 | Elevation of Privilege (built-but-unreachable / wrong surface) | navigation triad | mitigate | D-119-2: ONE task owns all three edits (App.tsx union + nav-items entry + ChatLayout branch); the Vitest reachability test asserts clicking Governance renders GovernancePage, not KnowledgeHealthPage. The 118 reachability BLOCKER cannot recur. |
| T-119-02-02 | Tampering (unintended write from a read-only surface) | GovernanceRow / GovernancePage | mitigate | D-119-6: GovernanceRow imports no write helper (deleteDocument/reingestDocument/MoveToFolderDialog); the Vitest link-out test asserts no write endpoint is called on row interaction. Rows only navigate. |
| T-119-02-03 | DoS (infinite re-fetch loop on empty response) | GovernancePage fetch effect | mitigate | D-119-9: the `initializedTabsRef` guard (carried verbatim from KnowledgeHealthPage) fires each card's fetch exactly once; Refresh clears it. The Vitest no-loop test asserts a single call per endpoint on empty. |
| T-119-02-04 | Tampering (fabricated confidence) | low-confidence row chip | mitigate | The row renders the RAW `min_confidence` via the 112 `ConfidenceChip` (single 0.5-cutoff source-of-truth); never a fabricated score. |
| T-119-02-05 | Information Disclosure | client-side data | accept | The frontend renders only what the owner-scoped backend (Plan 01, T-119-01-01) returns; no client-side trust boundary beyond the JWT-authenticated fetch. |
| T-119-02-SC | Tampering | npm installs | accept | No package installs in this phase (verified: zero new dependency). |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 after Tasks 1+2.
- `npx vitest run GovernancePage` exits 0 (reachability + empty-state + no-loop + link-out + Refresh).
- The navigation triad is complete in ONE plan (D-119-2): App.tsx union + nav-items entry + ChatLayout branch all present.
- Net-new frontend test failures = 0 (the pre-existing streaming/chat/provider/model-info failing-file roster is unchanged — base-checkout diff documented in SUMMARY per the 117/118 convention).
- Manual (verify-phase): mobile-responsive 3-stacked-cards layout + WCAG AA + the actual click-through into the correct detail-panel section across viewports (G-4 lived-experience).
</verification>

<success_criteria>
- Governance is a reachable top-level home (the D-119-2 triad owned in one plan).
- The page shows a counter header + 3 stacked positive-empty-state cards (broken/unclassified/low-conf), each behind the no-refetch-loop guard, each row a pure link-out to DocumentDetailPanel.
- DGOV-02: every signal row navigates to the document's detail panel (no inline write).
- UX-01: Deep Midnight/Aether via HealthPanel reuse, mobile-responsive, AA.
- No new package; threads.py untouched (frontend-only).
</success_criteria>

<output>
Create `.planning/phases/119-document-governance-health/119-02-SUMMARY.md` when done.
</output>
