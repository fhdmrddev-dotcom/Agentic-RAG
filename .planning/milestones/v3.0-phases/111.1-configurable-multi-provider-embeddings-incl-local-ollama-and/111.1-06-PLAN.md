---
phase: 111.1-configurable-multi-provider-embeddings-incl-local-ollama-and
plan: 06
type: execute
wave: 5
depends_on: ["111.1-01", "111.1-04", "111.1-05"]
files_modified:
  - frontend/src/components/settings/ProviderPicker.tsx
  - frontend/src/components/settings/ReembedConfirmModal.tsx
  - frontend/src/components/settings/ReembedStatusCard.tsx
  - frontend/src/pages/SettingsPage.tsx
  - frontend/src/lib/api.ts
autonomous: false
requirements: [EMBED-01, EMBED-02, EMBED-03, EMBED-06]
must_haves:
  truths:
    - "An admin picks an embedding provider (incl. Ollama + LM Studio local) from a preset <select>; selecting a preset auto-fills base_url + default model + default dims + recommended threshold; local presets relax the key to a dummy + show a green local tag"
    - "An always-on lock-icon endpoint footer shows WHERE the model runs (endpoint + dims + threshold + cloud/local tag) — the legible BUG-260616-01 cure (sketch 024 winner)"
    - "The SAME reusable picker drives both the embedding model and the extraction model (D-09)"
    - "Changing the embedding model/dimension + Save fires a serious destructive-change confirm gate (sketch 025) naming chunk count, target model, recall dip, ETA, and irreversibility BEFORE any commit (EMBED-06)"
    - "A re-embed status card (sketch 026) shows running/partial-failed/complete with a Re-embed-now re-kick, reconciled on fetch; a slim search-catching-up pointer appears where search happens"
  artifacts:
    - path: "frontend/src/components/settings/ProviderPicker.tsx"
      provides: "Reusable preset picker + always-on endpoint footer, used for embedding + extraction"
      contains: "11434"
    - path: "frontend/src/components/settings/ReembedConfirmModal.tsx"
      provides: "D-03 serious destructive-change confirm gate (sketch 025)"
      contains: "re-embed"
    - path: "frontend/src/components/settings/ReembedStatusCard.tsx"
      provides: "Graceful-dip status card + Re-embed-now (sketch 026)"
      contains: "Re-embed"
  key_links:
    - from: "frontend/src/pages/SettingsPage.tsx Save"
      to: "frontend/src/components/settings/ReembedConfirmModal.tsx"
      via: "confirm-on-save fires the gate when embedding model/dims changed, BEFORE the PUT"
      pattern: "ReembedConfirmModal"
    - from: "frontend/src/components/settings/ReembedStatusCard.tsx"
      to: "/settings/reembed-progress + /settings/reembed"
      via: "reconcile-on-fetch progress read + Re-embed-now re-kick"
      pattern: "reembed"
---

<objective>
Ship the Settings UI: ONE reusable provider picker (used for both the embedding model and the
extraction model), the serious destructive-change confirm gate, and the graceful-dip re-embed
status card — all built to the operator-approved locked sketches 024/025/026 (G-2 satisfied), in
the Deep Midnight / Aether design system, mobile-responsive, WCAG 2.1 AA (UX-01).

Purpose: the picker is the everyday control AND the legible structural cure for BUG-260616-01 (you
cannot pick a model without seeing where it runs); the confirm gate is the D-03 load-bearing
attention contract before any re-embed; the status card tells the truth about the recall dip and
offers a re-kick. Build to the sketches — do NOT redesign.
Output: ProviderPicker, ReembedConfirmModal, ReembedStatusCard, SettingsPage wiring, api.ts fns.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/111.1-configurable-multi-provider-embeddings-incl-local-ollama-and/111.1-CONTEXT.md
@.planning/phases/111.1-configurable-multi-provider-embeddings-incl-local-ollama-and/111.1-PATTERNS.md
@.planning/phases/111.1-configurable-multi-provider-embeddings-incl-local-ollama-and/111.1-PROVIDER-PRESETS.md
@.planning/phases/111.1-configurable-multi-provider-embeddings-incl-local-ollama-and/111.1-05-SUMMARY.md
@.planning/sketches/024-embedding-provider-picker/README.md
@.planning/sketches/025-reembed-confirm-gate/README.md
@.planning/sketches/026-reembed-in-progress/README.md

<design_contract>
The three sketches are LOCKED operator-approved design contracts. Open the index.html in each
sketch dir and build to it — do NOT redesign:
- .planning/sketches/024-embedding-provider-picker/index.html — winner: A's calm preset `<select>`
  + C's ALWAYS-ON 🔒 endpoint footer (endpoint · dims · threshold · cloud/local tag). The everyday
  control stays quiet; where-it-runs is never a click away. SAME component for embedding + extraction.
- .planning/sketches/025-reembed-confirm-gate/index.html — winner: C's weight-not-friction frame
  (danger rail, alert icon, big chunk count, forceful copy, deliberate two-step) + A's 4-fact grid
  (chunks · ETA · target model · runs-in-background). All 5 D-03 facts unmissable; NOT a toast,
  NOT type-to-confirm theatre. Deep Midnight danger/warning tokens (not generic-alert-red).
- .planning/sketches/026-reembed-in-progress/index.html — winner: C rich status card as the home
  in Settings (running / partial-failed / complete, "nothing was lost" D-05 promise, Re-embed-now)
  + a slim "search is catching up" pointer that appears only where search happens, deep-links into
  the card, auto-hides on completion.
</design_contract>

<interfaces>
<!-- Verified file:line (PATTERNS.md § ProviderPicker). -->

Rerank `<select>` to generalize — frontend/src/pages/SettingsPage.tsx:959-967:
```tsx
<FieldRow label="Provider">
  <select value={rerankProvider} onChange={(e) => setRerankProvider(e.target.value)}
    className="h-8 w-full text-sm rounded-md bg-muted/30 ghost-border px-2 focus:outline-none focus:ring-2 focus:ring-primary/30">
    <option value="api">API (Cohere)</option>
    <option value="local">Local (sentence-transformers)</option>
  </select>
</FieldRow>
```

Advanced-override inputs that STAY (the picker auto-fills these; remain editable) — SettingsPage.tsx:937-948:
embeddingModel (TextInput), embeddingDimensions (NumberInput min=64 max=4096), embeddingBaseUrl (TextInput), embeddingApiKey (ApiKeyInput).

Preset map source of truth: 111.1-PROVIDER-PRESETS.md (Plan 04 curated). Local: Ollama
http://localhost:11434/v1 (dummy key), LM Studio http://localhost:1234/v1 (dummy key, NOT
double-appended). OpenAI default text-embedding-3-small / 1536 (D-08).

Save state lives around SettingsPage.tsx:925 (savingAI / savedAI). Progress + re-kick endpoints
from Plan 05: GET /settings/reembed-progress, POST /settings/reembed. New settings fields
(embedding_provider / extraction_provider) added to the API in Plan 01 — add them to api.ts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reusable ProviderPicker (sketch 024) + api.ts wiring</name>
  <read_first>
    - .planning/sketches/024-embedding-provider-picker/index.html (the locked design — build to it)
    - frontend/src/pages/SettingsPage.tsx:933-967 (rerank <select> to generalize + the embedding inputs that stay)
    - 111.1-PROVIDER-PRESETS.md (the preset map: base_url / default model / dims / threshold per provider)
    - frontend/src/lib/api.ts (the settings fns + the embedding fields shape)
  </read_first>
  <files>frontend/src/components/settings/ProviderPicker.tsx, frontend/src/lib/api.ts</files>
  <action>
    Build `ProviderPicker.tsx` — ONE reusable component generalizing the rerank `<select>`, driven
    by a `presets` map (sourced from 111.1-PROVIDER-PRESETS.md) and reused for BOTH the embedding
    model and the extraction model (props: current provider/model/dims/base_url/key + onChange).
    - Preset `<option>` set: OpenAI, Google, Ollama (local), LM Studio (local), Cohere, Jina,
      Mistral, Custom / OpenAI-compatible.
    - On preset select → auto-fill base_url + default model + default dims + recommended
      `retrieval_match_threshold` (D-11) into the still-editable advanced inputs. Local presets relax
      the key to the dummy (`ollama` / `lm-studio`) and show a green "local" tag; LM Studio base_url
      NOT double-appended.
    - ALWAYS-ON 🔒 endpoint footer (sketch 024 winner): shows the resolved endpoint · dims ·
      threshold · a cloud/local tag — never behind "Advanced". This is the legible BUG-260616-01
      cure: you cannot read the model without seeing where it runs.
    - Reuse the existing `FieldRow` / `TextInput` / `NumberInput` / `ApiKeyInput` primitives.
    - Do NOT hard-pin model IDs in the component — read them from the preset map (D-07).
    `api.ts`: add `embedding_provider` / `extraction_provider` to the settings type + the
    update/read fns; add `getReembedProgress()` (GET /settings/reembed-progress) and
    `kickReembed()` (POST /settings/reembed) client fns.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "ProviderPicker\|api.ts" | head -5; test -f src/components/settings/ProviderPicker.tsx && grep -q "11434" src/components/settings/ProviderPicker.tsx && grep -qi "1234" src/components/settings/ProviderPicker.tsx && echo "PICKER_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/settings/ProviderPicker.tsx` exists; contains `11434` and `1234` (local base_urls) and the preset `<option>` set (OpenAI/Google/Ollama/LM Studio/Cohere/Jina/Mistral/Custom).
    - The component renders an always-on endpoint footer (grep for a lock/endpoint footer marker) — endpoint + dims + threshold + cloud/local tag visible without expanding Advanced.
    - `grep -E "embedding_provider|extraction_provider" frontend/src/lib/api.ts` matches; `getReembedProgress` + `kickReembed` exist in api.ts.
    - `cd frontend && npx tsc --noEmit` reports NO new errors in ProviderPicker.tsx / api.ts.
  </acceptance_criteria>
  <done>A reusable preset picker with an always-on endpoint footer exists, driven by the curated preset map, reused for embedding + extraction; api.ts wired for the new fields + progress/re-kick.</done>
</task>

<task type="auto">
  <name>Task 2: ReembedConfirmModal (sketch 025) + ReembedStatusCard (sketch 026) + SettingsPage wiring</name>
  <read_first>
    - .planning/sketches/025-reembed-confirm-gate/index.html + .planning/sketches/026-reembed-in-progress/index.html (the locked designs)
    - frontend/src/pages/SettingsPage.tsx (the embedding settings region ~:933-994 + the Save state ~:925)
    - frontend/src/components/settings/ProviderPicker.tsx (Task 1)
    - frontend/src/lib/api.ts (getReembedProgress / kickReembed from Task 1)
  </read_first>
  <files>frontend/src/components/settings/ReembedConfirmModal.tsx, frontend/src/components/settings/ReembedStatusCard.tsx, frontend/src/pages/SettingsPage.tsx</files>
  <action>
    `ReembedConfirmModal.tsx` (D-03, build to sketch 025): a SERIOUS, attention-demanding gate
    (weight-not-friction: danger rail, alert icon, forceful copy, deliberate two-step Confirm — NOT
    a toast, NOT type-to-confirm). The 4-fact grid + recall line MUST name all five facts: how many
    chunks will re-embed (read the LIVE count), the target model, that search quality dips until the
    job finishes, a rough ETA, and that it's reversible only by switching back + re-embedding. Use
    Deep Midnight danger/warning tokens. Cancel = no change; Confirm = save proceeds + job kicks.

    `ReembedStatusCard.tsx` (D-04/D-05, build to sketch 026): the rich status card is the HOME in
    Settings — carries running / partial-failed / complete with the D-05 "nothing was lost" promise,
    re-embedded/total/ETA, and a "Re-embed now" re-kick (calls `kickReembed`). Progress via
    `getReembedProgress` reconciled ON FETCH (Realtime is a hint — D-v2.5-03; poll lightly, never
    trust a stale push over the fetched record). Also add the slim "search is catching up" pointer
    that appears where search happens (Documents/search surface) and deep-links into the card,
    auto-hiding on completion.

    `SettingsPage.tsx` wiring:
    - Replace the embedding text inputs (~:933-950) with the `ProviderPicker` (embedding instance);
      add a second `ProviderPicker` instance for the extraction model (D-09 reuse).
    - Confirm-on-save (D-02): when the embedding model OR dimensions changed and the user hits Save,
      fire `ReembedConfirmModal` BEFORE the PUT. Only on Confirm does the save commit (which the Plan
      05 backend turns into the job kickoff). A no-change save does NOT fire the gate.
    - Mount `ReembedStatusCard` under the picker.
    - UX-01: Deep Midnight / Aether tokens, mobile-responsive, WCAG 2.1 AA (labels, focus order,
      Escape-to-cancel on the modal, aria on the danger gate).
  </action>
  <verify>
    <automated>cd frontend && test -f src/components/settings/ReembedConfirmModal.tsx && test -f src/components/settings/ReembedStatusCard.tsx && grep -q "ReembedConfirmModal" src/pages/SettingsPage.tsx && grep -q "ProviderPicker" src/pages/SettingsPage.tsx && npx tsc --noEmit 2>&1 | grep -i "Reembed\|SettingsPage\|ProviderPicker" | head -5; echo "WIRED"</automated>
  </verify>
  <acceptance_criteria>
    - Both `ReembedConfirmModal.tsx` and `ReembedStatusCard.tsx` exist; SettingsPage imports + mounts both + two ProviderPicker instances (embedding + extraction).
    - The modal names all five D-03 facts (grep the copy for chunk-count, target-model, recall-dip, ETA, reversibility language).
    - The status card calls `getReembedProgress` (reconcile-on-fetch) and `kickReembed` (Re-embed now), and carries running/partial-failed/complete states.
    - Confirm-on-save fires the gate only on a model/dim change (a no-change save path does not import/trigger the modal).
    - `cd frontend && npx tsc --noEmit` reports NO new errors in the three touched/created files.
  </acceptance_criteria>
  <done>The D-03 confirm gate + graceful-dip status card are built to sketches 025/026 and wired into SettingsPage; the picker drives both embedding + extraction; confirm-on-save gates the re-embed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [CHECKPOINT] Live UI verification (sketches 024/025/026 + UX-01)</name>
  <action>
    This is a human-verify checkpoint — no code is written. The operator (with backend + dev app
    running) walks the three locked-sketch surfaces live and confirms the picker auto-fill +
    always-on endpoint footer + reuse, the serious 5-fact confirm gate, the status-card three states
    + Re-embed-now + search-catching-up pointer, and UX-01 accessibility. See how-to-verify for the
    exact steps; record pass/fail per item + any visual/interaction defect.
  </action>
  <what-built>
    The reusable embedding/extraction ProviderPicker with an always-on endpoint footer (sketch 024),
    the serious destructive-change confirm gate (sketch 025), and the graceful-dip re-embed status
    card + search-catching-up pointer (sketch 026), wired into SettingsPage.
  </what-built>
  <how-to-verify>
    With the backend running (operator starts uvicorn) and the dev app at http://localhost:5173/
    (login fhdmrd@gmail.com / 123456), in Settings → AI/embedding:
    1. PICKER (024): selecting each preset auto-fills base_url + default model + dims + threshold;
       the 🔒 endpoint footer ALWAYS shows endpoint · dims · threshold · cloud/local tag (never a
       click away). Local presets (Ollama/LM Studio) show the green "local" tag + a dummy key.
       Confirm the SAME picker drives the extraction model too.
    2. CONFIRM GATE (025): change the embedding model, hit Save → the gate reads as SERIOUS (not a
       toast); all five facts legible (chunk count · target model · recall dip · ETA · irreversibility).
       Cancel = no change. Confirm = save commits + the job kicks.
    3. STATUS CARD (026): after Confirm, the card shows running → (force a failure to see)
       partial-failed with "nothing was lost" + a working "Re-embed now" → complete; the slim
       "search is catching up" pointer appears on the Documents/search surface and auto-hides on
       completion.
    4. UX-01: Deep Midnight tokens; mobile-responsive (narrow viewport); keyboard — Escape cancels
       the modal, focus order sane, danger gate has aria.
    Report which of 1-4 pass and any visual/interaction defects.
  </how-to-verify>
  <verify>
    <automated>echo "human-verify checkpoint — operator-driven; no automated gate"</automated>
  </verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <acceptance_criteria>
    - All four checks (picker auto-fill + always-on footer + reuse / serious 5-fact gate / status-card 3 states + re-kick + pointer / UX-01 a11y) pass live.
    - The endpoint footer makes cloud-vs-local unmissable (the BUG-260616-01 legibility cure).
    - The confirm gate reads as serious and names all five facts; Cancel truly cancels.
  </acceptance_criteria>
  <done>The three locked sketches are faithfully built + wired; operator approves the live UI (G-2 acceptance bar met).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings UI → settings API | The picker edits provider/model/base_url/key/dims; the modal is the consent gate before a re-embed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111.1-06-01 | (UX-integrity) | re-embed without consent | mitigate | Confirm-on-save fires the D-03 gate BEFORE the PUT; only Confirm commits the change that the backend turns into the job. Verified live (Task 3) |
| T-111.1-06-02 | Information Disclosure | api key echoed in the picker | mitigate | Reuse the existing `ApiKeyInput` sentinel ("***" = keep) — never render a real key back; local presets use dummy keys |
| T-111.1-06-03 | Information Disclosure (data egress legibility) | hidden endpoint | mitigate | The always-on 🔒 endpoint footer makes cloud-vs-local unmissable — the legible half of the BUG-260616-01 cure (the structural half is Plan 02) |
| T-111.1-06-04 | Spoofing | stale progress shown as truth | accept | Status card reconciles on fetch (D-v2.5-03); a stale push never overrides the fetched record |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` — no new errors in the 5 touched files.
- The three sketches faithfully built (picker + always-on footer, serious 5-fact gate, status card 3 states + re-kick + pointer).
- Live UI checkpoint (Task 3) approved: picker auto-fill + reuse, confirm gate seriousness + 5 facts, status card states + re-kick, UX-01 a11y.
- The cross-embedder / local-embedder / switch+re-embed / local-extraction-stays-local live UAT axes are the VALIDATION.md manual matrix (run at `/gsd:verify-work`), NOT faked here.
</verification>

<success_criteria>
- A reusable preset picker (embedding + extraction) with an always-on endpoint footer ships to sketch 024 (EMBED-01/02/03).
- The D-03 destructive-change confirm gate ships to sketch 025, naming all five facts before any commit (EMBED-06).
- The graceful-dip status card + search-catching-up pointer ship to sketch 026, reconcile-on-fetch, with a Re-embed-now re-kick.
- UX-01 (Deep Midnight, mobile, WCAG 2.1 AA) met; live UI approved.
</success_criteria>

<output>
After completion, create `.planning/phases/111.1-configurable-multi-provider-embeddings-incl-local-ollama-and/111.1-06-SUMMARY.md`
</output>
