---
status: complete
phase: 111.1-configurable-multi-provider-embeddings-incl-local-ollama-and
source: [111.1-01..06-SUMMARY.md, 111.1-VERIFICATION.md, 111.1-HUMAN-UAT.md]
started: 2026-06-17
updated: 2026-06-17
---

## Current Test

[testing complete]

## Note

Phase 111.1 was already gate-cleared before this verify-work session: VERIFICATION 9/10
(1 needs-human → satisfied), HUMAN-UAT 4/4 PASS (Claude-driven live), SECURITY verified
(threats_open: 0), VALIDATION nyquist_compliant. Operator chose "smoke-test the restart,
then close" — so this session re-confirms the user-observable deliverables against the
existing live proofs and adds ONE net-new test: the cold-start smoke test for the post-
integrity-sweep fixes (which required a `--workers 2` backend restart to load). No
redundant re-driving of already-proven live tests.

## Tests

### 1. Embedding provider picker + local presets + always-on endpoint footer (EMBED-01/02/03)
expected: Settings shows a reusable provider picker for the embedding (and extraction) model; selecting a local preset (Ollama `:11434/v1` / LM Studio `:1234/v1`) auto-fills base_url + dummy key + shows a green LOCAL tag; the endpoint footer is always visible (the BUG-260616-01 cure — you can never pick a model without seeing where it runs).
result: pass
evidence: VERIFICATION truth 1 (ProviderPicker.tsx preset map + endpoint footer) + Plan 06 live UI approved via Chrome MCP (operator).

### 2. Chunk-embedding and query-embedding use the SAME configured creds (EMBED-04)
expected: A configured non-default embedder embeds chunks AND queries in the same vector space (no mismatched spaces); the `embed_chunks` credential bug is fixed.
result: pass
evidence: VERIFICATION truth 2 + HUMAN-UAT test 1 — Ollama nomic-embed-text live cred-parity: cosine(chunk, related query)=0.79 vs unrelated=0.40 through the real client, zero cloud traffic. Cross-provider parity also proven on OpenAI + Google (3072-native).

### 3. Local embedder round-trip + local extraction stays local — egress proven by ENDPOINT (D-09 / BUG-260616-01)
expected: A local embedding/extraction model embeds + serves search with no traffic to OpenAI/OpenRouter; a slashed/local model id no longer mis-routes to cloud; verified by endpoint, not just a green result.
result: pass
evidence: HUMAN-UAT test 3 — Ollama serve log proved `POST /v1/chat/completions` hit `localhost:11434` (::1) 200 OK while active provider stayed openai (the exact BUG-260616-01 cross-provider scenario); no api.openai.com / openrouter.ai traffic; document text never left the box.

### 4. Re-embed lifecycle: confirm gate + status card + resumable/non-destructive job (EMBED-05/06)
expected: Changing the embedding model/dim fires a serious 5-fact confirm gate before any commit (Cancel = corpus untouched); a batched, RLS-scoped, resumable, non-destructive re-embed job runs; the status card shows running → complete with the true chunk count (count=exact, not capped at 1000); search dips then recovers; nothing is lost.
result: pass
evidence: HUMAN-UAT test 2 — isolated non-destructive live proof on the real DB: count=exact total=1842 (>1000, exercises the 1000-cap fix), stale chunk excluded → re-embedded → searchable at 0.78 (recovery), 1,839 production chunks untouched, CASCADE cleanup restored to exactly 1,839. ReembedConfirmModal 5-fact gate verified by code + Plan 06 live Cancel=corpus-untouched (DB-verified 1,839 all text-embedding-3-small).

### 5. Cold-start smoke test — integrity-sweep fixes go live after backend restart (NET-NEW this session)
expected: Restart the backend (`--workers 2`, no auto-reload). (a) An extraction-model pick in Settings now PERSISTS on save (the HIGH fix — previously silently dropped). (b) A fresh document ingest produces metadata (the strict=False + widened-degrade fixes — closes the original "Word doc, no metadata" defect). Backend boots clean.
result: pass
evidence: |
  Operator restarted (clean boot). DB-verified against the pre-test baseline:
  (a) HIGH fix — app_settings.extraction_model changed claude-sonnet-4-6/anthropic →
      gpt-5.4-mini/openai and stuck after reload (full save→return→hydrate round-trip live).
  (b) Metadata — docs 32→33, with-metadata 26→27; newest doc analysis.txt (completed)
      carries title/topics/summary/language/document_type + a _confidence map.
  Both observed by operator (UI) and confirmed by orchestrator (psycopg2 :54322).

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none blocking]

## Non-blocking follow-ups (planted, NOT verify-work gaps)

- SEED-088 — dynamic model registry (replaces the picker's hardcoded default-model list; some defaults stale, e.g. glm-4.6 unserved → glm-4.5).
- SEED-089 — configured `embedding_dimensions` MRL-truncation is not wired into `embed_texts` (`openai_service.py` omits the `dimensions=` arg); non-1536-native models use native dims. Minor; default path unaffected.
- SEED-090 — silent-failure observability: a failed extraction is indistinguishable from "no metadata found" (no error_message / badge / distinct telemetry); `provider_error` mislabeled as honest degrade.
