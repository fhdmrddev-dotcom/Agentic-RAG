---
phase: 111-metadata-enrichment-extraction-backend
plan: 05
type: execute
wave: 4
depends_on: [01, 03, 04]
files_modified:
  - supabase/full-schema.sql
autonomous: false
requirements: [META-01, META-03]
user_setup: []

must_haves:
  truths:
    - "Migration 072 is applied to the live local DB (:54322) via the PROJECT method (SQL editor OR psycopg2-direct) — NEVER db push/db reset; the 19 docs/221 threads of dev data are preserved"
    - "A read-back assertion proves the 3 app_settings columns + metadata_field_definitions.options exist with the right defaults"
    - "supabase/full-schema.sql is regenerated (no --reset) and reflects all 4 new columns"
    - "After apply, the settings-readback + flat-filter + CRUD + audit + scoping integration tests pass live"
  artifacts:
    - path: "supabase/full-schema.sql"
      provides: "regenerated bootstrap artifact reflecting migration 072"
      contains: "extraction_model"
  key_links:
    - from: "migration 072 (Plan 01 file)"
      to: "live :54322 app_settings + metadata_field_definitions schema"
      via: "psycopg2-direct or SQL editor apply (NOT db push)"
      pattern: "extraction_window_cap"
    - from: "live schema"
      to: "supabase/full-schema.sql"
      via: "bash scripts/regenerate-full-schema.sh (no --reset)"
      pattern: "metadata_enrichment_mode"
---

<objective>
[BLOCKING] Cross migration 072 (authored in Plan 01) into the live local DB and regenerate the bootstrap artifact. This is the gate the phase CANNOT pass without: build/type checks pass without the apply (the `_build_settings_from_row` reads come from app code, not the live DB), creating a false-positive verification state. Applying the migration + a read-back assertion + regenerating `full-schema.sql` is what makes the settings actually resolvable on the live DB and the integration tests truly GREEN. Single-file plan (owns `full-schema.sql`); Wave 4, after the migration file (01), the CRUD router (03), and the wiring (04).

Purpose: Without this, an admin setting `extraction_model` would read back the column-default forever (or the row read would error on the missing column on a fresh DB), and the audit/CRUD/settings integration tests would skip-or-false-green. Applies the 3 app_settings columns + the metadata_field_definitions.options carrier.
Output: migration 072 live on :54322; full-schema.sql regenerated + committed.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-metadata-enrichment-extraction-backend/111-RESEARCH.md
@.planning/phases/111-metadata-enrichment-extraction-backend/111-PATTERNS.md

<interfaces>
<!-- The CLAUDE.md migration discipline (the 100/102/110 operator-authorized precedent): -->
<!-- Apply via the Supabase SQL editor OR psycopg2-direct to :54322 — NEVER `supabase db push` / `supabase db reset` -->
<!-- (preserves the 19 docs / 221 threads of dev data). Then `bash scripts/regenerate-full-schema.sh` (NO --reset). Commit BOTH. -->

<!-- Migration 072 (Plan 01 file): supabase/migrations/072_app_settings_extraction_model.sql -->
ALTER TABLE public.app_settings ADD COLUMN IF NOT EXISTS extraction_model text DEFAULT NULL,
  extraction_window_cap integer DEFAULT 32000, metadata_enrichment_mode text DEFAULT 'enriched';
ALTER TABLE public.metadata_field_definitions ADD COLUMN IF NOT EXISTS options jsonb DEFAULT NULL;

<!-- local DSN (the test harness default): postgresql://postgres:postgres@127.0.0.1:54322/postgres -->
<!-- read-back helper (write a throwaway script under scripts/, NOT into the uvicorn-watched backend/ tree): -->
<!-- scripts/apply_migration_072.py — connect, exec the 072 SQL, then SELECT information_schema.columns for the 4 cols. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply migration 072 to :54322 + read-back assertion</name>
  <read_first>
    - supabase/migrations/072_app_settings_extraction_model.sql (the Plan-01-authored file — the exact statements to apply)
    - .planning/phases/111-metadata-enrichment-extraction-backend/111-RESEARCH.md §Q9 ("Apply migration by pasting into the SQL editor OR psycopg2-direct — NEVER db push/db reset")
    - .planning/STATE.md (the 110-02 + 102-02 psycopg2-direct apply precedent — the exact pattern + the "no wipe, dev data preserved" discipline)
    - CLAUDE.md (the migration apply rule — never db push/db reset; regenerate full-schema with no --reset)
  </read_first>
  <action>
    If the local Supabase stack is up, apply migration 072 via psycopg2-direct to :54322 in ONE transaction (the 110-02/102-02 autonomous precedent). Write a throwaway apply script `scripts/apply_migration_072.py` (NOT into the uvicorn-watched `backend/` tree — the no-scratch-in-watched-tree rule) that: connects to `postgresql://postgres:postgres@127.0.0.1:54322/postgres`, executes the SQL from `supabase/migrations/072_app_settings_extraction_model.sql` verbatim (idempotent — `ADD COLUMN IF NOT EXISTS`), commits, then runs the read-back SELECT against `information_schema.columns` and asserts all 4 columns exist with their defaults. Run it via the Bash tool.

    If the stack is NOT up (or psycopg2 apply errors), STOP and defer the apply to the Task 2 checkpoint — present the SQL for the operator to paste into the Supabase SQL editor. NEVER run `supabase db push` or `supabase db reset` (wipes the 19 docs / 221 threads — the CLAUDE.md hard rule).

    Read-back asserts on the live DB: `app_settings` has `extraction_model` (default NULL), `extraction_window_cap` (default 32000), `metadata_enrichment_mode` (default 'enriched'); `metadata_field_definitions` has `options` (jsonb, default NULL). Also assert `select count(*) from documents` is unchanged (> 0 — proves no reset). Print `OK migration 072 live`.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && backend/venv/Scripts/python scripts/apply_migration_072.py</automated>
  </verify>
  <acceptance_criteria>
    - The read-back returns all 4 columns (3 on app_settings + options on metadata_field_definitions); the script prints `OK migration 072 live`
    - The dev data is intact (`select count(*) from documents` returns the pre-existing count, NOT 0 — proves no db reset happened)
    - No `supabase db push` / `db reset` was run (the apply was psycopg2-direct or, if deferred, SQL-editor)
  </acceptance_criteria>
  <done>Migration 072 is live on :54322; the 4 columns read back with the right defaults; dev data preserved. (If the stack was down, this task reports the deferral and Task 2 carries the SQL-editor paste.)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Operator confirms the live apply + regenerate full-schema.sql</name>
  <read_first>
    - scripts/regenerate-full-schema.sh (confirm the default no-reset live-dump behavior)
    - CLAUDE.md (the regenerate + commit-both discipline)
  </read_first>
  <action>
OPERATOR-CONFIRM gate (autonomous:false). After Task 1's apply (or, if the stack was down, after the operator pastes the migration into the SQL editor), the executor PAUSES, presents the `<how-to-verify>` steps, then WAITS for the "approved" resume-signal. The executor's job on this task: (1) ensure migration 072 is live (Task 1 applied it psycopg2-direct, OR the operator pastes it now); (2) regenerate `supabase/full-schema.sql` via `bash scripts/regenerate-full-schema.sh` (NO --reset — never hand-edit the artifact); (3) run the test_111 integration suite against the live DB and confirm GREEN; (4) commit BOTH the migration file (from Plan 01) and the regenerated full-schema.sql. Do NOT run `supabase db push`/`db reset` (the CLAUDE.md hard rule). Exact steps in `<how-to-verify>`.
  </action>
  <what-built>
    Migration 072 (3 app_settings columns + metadata_field_definitions.options) crossed into the live local DB :54322 via psycopg2-direct (or, if the stack was down, the SQL presented for the operator to paste into the Supabase SQL editor). A read-back assertion confirmed all 4 columns with their defaults and that dev data is preserved (no reset).
  </what-built>
  <how-to-verify>
    1. If Task 1 reported the stack was DOWN: paste the contents of `supabase/migrations/072_app_settings_extraction_model.sql` into the Supabase SQL editor and run it (idempotent — safe to re-run). Confirm success.
    2. Confirm the read-back: the 4 columns exist (`app_settings.extraction_model`, `extraction_window_cap`, `metadata_enrichment_mode`; `metadata_field_definitions.options`) with defaults NULL / 32000 / 'enriched' / NULL.
    3. Confirm dev data intact (documents count unchanged — no reset happened).
    4. Regenerate the bootstrap artifact: `bash scripts/regenerate-full-schema.sh` (NO --reset). Confirm `supabase/full-schema.sql` now contains `extraction_model`, `extraction_window_cap`, `metadata_enrichment_mode`, and the `options jsonb` column on metadata_field_definitions.
    5. Run the now-live integration tests: `cd backend && venv\Scripts\python -m pytest tests/integration/test_111_settings_readback.py tests/integration/test_111_flat_filter_compat.py tests/integration/test_111_metadata_fields_crud.py tests/integration/test_111_audit_field_create.py tests/integration/test_111_field_def_scoping.py -q` — all GREEN against the live DB.
    6. Commit BOTH the migration file (from Plan 01) and the regenerated `supabase/full-schema.sql`.
  </how-to-verify>
  <verify>Operator confirmation ("approved") + `grep extraction_model supabase/full-schema.sql` matches + `cd backend && venv\Scripts\python -m pytest tests/integration/test_111_*.py -q` exits 0 against the live DB. (Manual gate — the executor verifies the regenerated artifact + the live GREEN suite, not by re-applying.)</verify>
  <acceptance_criteria>
    - `grep "extraction_model" supabase/full-schema.sql` matches; `grep "metadata_enrichment_mode" supabase/full-schema.sql` matches; `grep "options jsonb" supabase/full-schema.sql` (on metadata_field_definitions) matches
    - `cd backend && venv\Scripts\python -m pytest tests/integration/test_111_*.py -q` exits 0 (all GREEN live)
    - `supabase/full-schema.sql` was regenerated by the script (NOT hand-edited)
  </acceptance_criteria>
  <resume-signal>Type "approved" once migration 072 is live, full-schema.sql is regenerated + committed, and the test_111 integration suite is GREEN — or describe issues.</resume-signal>
  <done>Migration 072 is live on :54322; operator-confirmed; full-schema.sql regenerated (script-generated, not hand-edited) + committed; the test_111 integration suite is GREEN against the migrated DB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration apply → live dev DB | The apply path must not destroy the 19 docs / 221 threads of dev data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-05-01 | Denial of Service | A partial/failed migration apply leaving the schema half-altered | mitigate | One transaction + idempotent `ADD COLUMN IF NOT EXISTS` (re-runnable) + a read-back assertion BEFORE marking done (the 102/110 precedent). A failed apply leaves the existing columns untouched. |
| T-111-05-02 | Denial of Service | `db push`/`db reset` wiping dev data | mitigate | Apply ONLY via psycopg2-direct or the SQL editor; `regenerate-full-schema.sh` runs with NO `--reset`. The acceptance criteria assert the documents count is unchanged (proof no reset happened). The CLAUDE.md hard rule + the 100/102/110 precedent. |
| T-111-05-03 | Tampering | Hand-editing full-schema.sql (drift from the live DB) | mitigate | full-schema.sql is regenerated by the script from the live dump, never hand-edited (CLAUDE.md). The acceptance criteria assert it was script-regenerated. |
</threat_model>

<verification>
- Read-back: all 4 columns present on the live DB with the right defaults (Task 1 verify prints `OK migration 072 live`).
- full-schema.sql regenerated (no --reset) and contains all 4 columns.
- The test_111 integration suite is GREEN against the live DB.
- Dev data preserved (documents count unchanged).
</verification>

<success_criteria>
- Migration 072 is live on :54322 (3 app_settings columns + metadata_field_definitions.options) — applied via the PROJECT method, never db push/reset.
- A read-back assertion proves the columns + defaults; dev data preserved.
- supabase/full-schema.sql regenerated + committed (script-generated, not hand-edited).
- The full test_111 integration suite passes live — the phase is no longer in a false-positive (build-passes-without-apply) state.
</success_criteria>

<output>
After completion, create `.planning/phases/111-metadata-enrichment-extraction-backend/111-05-SUMMARY.md`
</output>
