---
phase: 110-dm-foundations
plan: 02
type: execute
wave: 2
depends_on: ["110-01"]
files_modified:
  - supabase/full-schema.sql
  - backend/tests/integration/test_110_dm_audit_live.py
  - backend/tests/integration/test_110_audit_drift_guard.py
  - backend/tests/integration/test_110_dm_schema.py
  - backend/tests/integration/test_110_flag.py
autonomous: false

requirements: [DMF-01, DMF-02, DMF-03]

must_haves:
  truths:
    - "Migration 071 is applied to the live local DB on :54322 (operator-confirmed) — the 4 tables exist and audit_log CHECK carries 19 types"
    - "supabase/full-schema.sql is regenerated from the live DB (no --reset) and carries the 4 CREATE TABLE blocks + the 19-type CHECK + the flag column"
    - "071_dm_foundations.sql + the regenerated full-schema.sql + the Plan-01 code edits are committed together"
    - "SC#4: a real audit row INSERTs and SELECTs back live for each of the 8 new action types"
    - "SC#1: 4 tables have rowsecurity=true, nullable no-FK org_id; a 2-user cross-user RLS test returns correct row sets; the nullable-user_id global field is reachable"
    - "SC#2/SC#3: VALID_ACTION_TYPES is a subset of the live CHECK (CI test green); the boot guard raises on a simulated drift (unit test already green)"
    - "SC#5: document_management_enabled = true in app_settings and reads True through UserEffectiveSettings"
  artifacts:
    - path: "supabase/full-schema.sql"
      provides: "regenerated single-file deploy artifact reflecting migration 071"
      contains: "document_management_enabled"
    - path: "backend/tests/integration/test_110_dm_audit_live.py"
      provides: "SC#4 GREEN against the migrated live DB (8 types)"
      contains: "NEW_DM_ACTION_TYPES"
  key_links:
    - from: "operator (SQL editor)"
      to: "live Postgres :54322"
      via: "paste 071_dm_foundations.sql + run (NEVER db push/reset)"
      pattern: "audit_log_action_type_check"
    - from: "scripts/regenerate-full-schema.sh"
      to: "supabase/full-schema.sql"
      via: "live-DB schema dump (no --reset)"
      pattern: "document_management_enabled"
    - from: "test_110_audit_drift_guard.py"
      to: "audit_service.assert_action_types_synced"
      via: "shared helper call against the live pool"
      pattern: "assert_action_types_synced"
---

<objective>
Cross the Plan-01 migration into the live local DB (operator-manual — the project HARD RULE forbids `db push`/`db reset`), regenerate the `full-schema.sql` bootstrap artifact, commit migration + artifact + code together, and bring the four live verification tests GREEN against the migrated DB.

This is the phase's acceptance gate. The live INSERT+SELECT (SC#4) and the 2-user RLS test (SC#1) depend on the live DB carrying the new CHECK + tables — build/import checks alone would false-green (the D-102 lesson). The live tests SKIP (not fail) when :54322 is down, so a green run on a DB-down machine is NOT a pass; the gate requires them green against the live, migrated DB.

Purpose: Make the substrate real and verified live. Closes the live half of DMF-01/02/03.
Output: An applied migration, a regenerated full-schema.sql, one commit, and four green live tests.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-dm-foundations/110-CONTEXT.md
@.planning/phases/110-dm-foundations/110-RESEARCH.md
@.planning/phases/110-dm-foundations/110-PATTERNS.md
@.planning/phases/110-dm-foundations/110-VALIDATION.md
@.planning/phases/110-dm-foundations/110-01-PLAN.md

<interfaces>
<!-- The Plan-01 artifacts this plan applies + verifies. -->
Migration file (authored in Plan 01): supabase/migrations/071_dm_foundations.sql
Shared helper (Plan 01): audit_service.assert_action_types_synced(pool) — raises on frozenset⊄live-CHECK
The 8 new action types (the SC#4 population): view.create, view.delete, relationship.create, relationship.delete,
  classification.apply, classification.rule.create, metadata.update, metadata.field.create

Regen command (CLAUDE.md HARD RULE — NO --reset, dumps the live DB as-is):
  bash scripts/regenerate-full-schema.sh
Apply path: paste supabase/migrations/071_dm_foundations.sql into the Supabase SQL editor, run vs :54322.
  NEVER `supabase db push` / `supabase db reset` (would wipe dev data).

Live verification commands:
  cd backend && venv/Scripts/python -m pytest tests/integration/test_110_dm_audit_live.py tests/integration/test_110_audit_drift_guard.py tests/integration/test_110_dm_schema.py tests/integration/test_110_flag.py -x
  cd backend && venv/Scripts/python -m pytest -x   (full suite — pre-verify-work gate)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Operator applies migration 071 to the live local DB via the Supabase SQL editor</name>
  <files>supabase/migrations/071_dm_foundations.sql (operator pastes this into the SQL editor — read-only here; no file is edited by this task)</files>
  <read_first>
    - supabase/migrations/071_dm_foundations.sql (the file to paste)
    - CLAUDE.md §"Schema changes ship as numbered SQL migrations" (the apply discipline — paste into the SQL editor; NEVER db push/db reset)
    - .planning/phases/110-dm-foundations/110-PATTERNS.md Shared Pattern D (the apply+regen sequence)
    - .planning/phases/110-dm-foundations/110-VALIDATION.md §"Manual-Only Verifications" (why this is autonomous:false)
  </read_first>
  <action>
OPERATOR-MANUAL (autonomous:false). The executor MUST NOT apply the migration — the project HARD RULE (CLAUDE.md) forbids `supabase db push`/`db reset` and requires a SQL-editor paste (preserves dev data). The executor's role on this task is to PAUSE, present the operator steps from `<how-to-verify>` below, and WAIT for the "applied" resume-signal before starting Task 2. Do NOT regenerate full-schema.sql or run the live tests until the operator confirms the apply (those depend on the live DB carrying the migration). The exact operator steps + verification queries are in `<how-to-verify>`.
  </action>
  <what-built>
The executor cannot apply the migration — the project HARD RULE (CLAUDE.md) requires the operator to paste it into the Supabase SQL editor and run it (this preserves dev data; `supabase db push`/`db reset` are forbidden). Migration `071_dm_foundations.sql` (authored in Plan 01) creates the 4 DM tables (RLS + nullable org_id), extends the audit CHECK 11->19, and adds the `app_settings.document_management_enabled` flag column — all in one `BEGIN;...COMMIT;`.
  </what-built>
  <how-to-verify>
1. Open the Supabase SQL editor for the LOCAL stack (the project on :54322 — `supabase start` must be running).
2. Open `supabase/migrations/071_dm_foundations.sql`, copy its ENTIRE contents, paste into a new SQL editor query, and Run it. It is wrapped in `BEGIN;...COMMIT;` so a partial failure rolls back cleanly. Do NOT run `supabase db push` or `supabase db reset`.
3. Confirm success (no error) and verify the live state with these queries:
   - Tables exist (expect 4 rows):
     `SELECT tablename FROM pg_tables WHERE tablename IN ('document_views','document_relationships','classification_rules','metadata_field_definitions') ORDER BY tablename;`
   - CHECK has 19 types (expect a def listing all 19, incl. the 8 new ones):
     `SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname = 'audit_log_action_type_check';`
   - Flag column exists default true (expect one row, value `t`):
     `SELECT document_management_enabled FROM app_settings WHERE id='global';`
4. Reply "applied" (and paste the CHECK def + the 4 table names) or describe any error.
  </how-to-verify>
  <verify>Operator confirmation ("applied") + a live `pg_get_constraintdef` showing 19 types + `SELECT tablename ...` returning all 4 tables + `SELECT document_management_enabled FROM app_settings WHERE id='global'` returning true. (Manual gate — the executor verifies the operator's pasted-back evidence, not by running the apply itself.)</verify>
  <acceptance_criteria>
    - Operator confirms the migration ran with no error
    - `SELECT tablename FROM pg_tables WHERE tablename IN (...)` returns all 4 table names
    - `pg_get_constraintdef` for `audit_log_action_type_check` lists 19 quoted types including `view.create`, `relationship.create`, `classification.apply`, `metadata.update`, `metadata.field.create`, `view.delete`, `relationship.delete`, `classification.rule.create`
    - `SELECT document_management_enabled FROM app_settings WHERE id='global'` returns true
  </acceptance_criteria>
  <resume-signal>Type "applied" with the 4 table names + the 19-type CHECK def, or describe the error.</resume-signal>
  <done>Migration 071 is applied to the live :54322 DB; operator-confirmed; the 4 tables, the 19-type CHECK, and the flag column are present on the live DB.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate full-schema.sql (no --reset), run the 4 live verification tests GREEN, commit everything together</name>
  <files>supabase/full-schema.sql, backend/tests/integration/test_110_dm_audit_live.py, backend/tests/integration/test_110_audit_drift_guard.py, backend/tests/integration/test_110_dm_schema.py, backend/tests/integration/test_110_flag.py</files>
  <read_first>
    - scripts/regenerate-full-schema.sh (header — default mode is a live-DB dump, NO reset; NEVER pass --reset)
    - CLAUDE.md §"Schema changes ship as numbered SQL migrations" (commit migration + regenerated full-schema.sql together; never hand-edit full-schema.sql)
    - backend/tests/integration/test_110_dm_audit_live.py, test_110_audit_drift_guard.py, test_110_dm_schema.py, test_110_flag.py (the Plan-01 Wave-0 tests that now run against the migrated DB)
    - .planning/phases/110-dm-foundations/110-VALIDATION.md §"The exact observables that prove PASS" + §"Sampling Rate"
    - .planning/phases/110-dm-foundations/110-RESEARCH.md §4 (A4 — pin the pg_get_constraintdef parse against the live rendering) + §"Open Questions" #2 (subset-only direction)
  </read_first>
  <action>
**Precondition:** Task 1 is confirmed applied. The local Supabase stack is up on :54322. The backend `venv` is active for pytest.

**Step 1 — regenerate the bootstrap artifact.** Run `bash scripts/regenerate-full-schema.sh` with NO `--reset` flag (the default mode dumps the live DB schema as-is and preserves dev data; `--reset` would wipe local data — the F9 trap). This rebuilds `supabase/full-schema.sql` to reflect migration 071. Never hand-edit `full-schema.sql`.

**Step 2 — pin the live-DB-dependent test details (A1/A4).**
- A4 (constraint parse): run `SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname='audit_log_action_type_check'` against :54322 and confirm the rendering is `'string'::text` quoted literals so the `re.findall(r"'([^']+)'", ...)` parse in `assert_action_types_synced` matches. If the live rendering differs, adjust the regex in `audit_service.py` (and re-run the boot-guard unit test) — but the standard Postgres rendering should match as-is.
- A1 (RLS role-switch in `test_110_dm_schema.py`): confirm the `SET LOCAL ROLE authenticated` + `set_config('request.jwt.claim.sub', <uid>, true)` mechanism actually engages RLS on the live DB (a superuser pool bypasses RLS). If the project's local roles differ, adjust to the role the project's RLS predicates evaluate against. The test must demonstrably return 0 rows for user B reading user A's private row (if it returns A's row, RLS is being bypassed — fix the role switch, do NOT weaken the assertion).

**Step 3 — run the 4 live integration tests GREEN against the migrated DB:**
```
cd backend && venv/Scripts/python -m pytest tests/integration/test_110_dm_audit_live.py tests/integration/test_110_audit_drift_guard.py tests/integration/test_110_dm_schema.py tests/integration/test_110_flag.py -x
```
Expected: all GREEN (NOT skipped — :54322 is up and migration applied). Specifically:
- `test_110_dm_audit_live.py`: 8 parametrized cases pass (a real row INSERTs+SELECTs back for each of the 8 types).
- `test_110_audit_drift_guard.py`: the subset assertion passes (VALID_ACTION_TYPES ⊆ live CHECK) + the 19-string expected-set assertion passes.
- `test_110_dm_schema.py`: 4 tables `rowsecurity=true`, each `org_id` nullable + no FK; the 2-user cross-user isolation + global-visibility + the nullable-user_id global-field reachability all pass.
- `test_110_flag.py`: `document_management_enabled` = true on the live row and reads True through `UserEffectiveSettings`.

If any live test reveals a migration or code defect (e.g. a missing policy, a parse mismatch, a CASCADE that should be SET NULL), fix the migration FILE and/or the code, then have the operator RE-APPLY the corrected migration via the SQL editor (re-trigger Task 1's apply for the changed statements — DROP/ADD CONSTRAINT and CREATE TABLE IF NOT EXISTS are re-runnable; for an already-created table, author a corrective `ALTER`), regenerate full-schema.sql again, and re-run the tests. Do NOT weaken a test to make it pass — the live test IS the gate.

**Step 4 — run the full suite (the pre-verify-work backstop):**
```
cd backend && venv/Scripts/python -m pytest -x
```
Prove net-new failures = 0 against the wave base (SEED-056 discipline): if pre-existing rot fails, confirm via base-checkout that the same tests fail identically without this plan's changes. Record the net-new=0 proof in the SUMMARY.

**Step 5 — commit migration + regenerated artifact + the Plan-01 code edits together** (one logical change, CLAUDE.md rule). The commit MUST include: `supabase/migrations/071_dm_foundations.sql`, the regenerated `supabase/full-schema.sql`, `backend/app/services/audit_service.py`, `backend/app/main.py`, `backend/app/models/user_settings.py`, and the 6 test files. Commit message: `feat(110): DM Foundations substrate — 4 RLS tables + audit enum 11->19 + DM capability flag (DMF-01/02/03)`.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/integration/test_110_dm_audit_live.py tests/integration/test_110_audit_drift_guard.py tests/integration/test_110_dm_schema.py tests/integration/test_110_flag.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/full-schema.sql` was regenerated (NOT hand-edited) and contains all 4 `CREATE TABLE` blocks (`document_views`, `document_relationships`, `classification_rules`, `metadata_field_definitions`), the 19-type `audit_log_action_type_check` (grep finds `metadata.field.create`), and `document_management_enabled boolean`
    - The regen used NO `--reset` (confirm dev data preserved — e.g. existing threads/documents still present)
    - `pytest tests/integration/test_110_dm_audit_live.py -x` → 8 passed (NOT skipped) — a row round-trips for each of the 8 types
    - `pytest tests/integration/test_110_audit_drift_guard.py -x` → passed (subset holds + 19-string set)
    - `pytest tests/integration/test_110_dm_schema.py -x` → passed; the 2-user test demonstrably returns 0 rows for user B reading user A's private row AND returns the global row (RLS engaged, not bypassed)
    - `pytest tests/integration/test_110_flag.py -x` → passed (live flag = true, reads True via settings)
    - `pytest -x` full suite green (net-new failures = 0 vs base — proof recorded in SUMMARY)
    - One commit contains `071_dm_foundations.sql` + the regenerated `full-schema.sql` + all 3 modified source files + the 6 test files
  </acceptance_criteria>
  <done>full-schema.sql is regenerated and committed alongside the migration and code; all 4 live tests are GREEN (not skipped) against the migrated DB; the full suite passes with net-new=0; the substrate is verified live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live Postgres :54322 | The migration crosses into the live DB via a manual SQL-editor paste; a partial apply or a `db reset` slip would corrupt/wipe the dev DB. |
| live DB ↔ verification tests | The live INSERT+SELECT and 2-user RLS tests are the only proof that the CHECK/tables/RLS are actually correct — a mock or a bypassed RLS role would false-green. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan (bound to task) |
|-----------|----------|-----------|----------|-------------|----------------------------------|
| T-110-09 | Tampering / DoS (data loss) | the migration apply step | MEDIUM | mitigate | The migration is wrapped in one `BEGIN;...COMMIT;` so a partial failure rolls back atomically — **Plan 01 Task 1** (file shape) + **Task 1** here (apply). The apply path is SQL-editor-paste only; `db push`/`db reset` are explicitly forbidden (would wipe dev data) — **Task 1** + **Task 2 Step 1** (regen with NO `--reset`, F9 trap). |
| T-110-10 | Repudiation (false-green) | the verification tests | HIGH (gate integrity) | mitigate | SC#4 INSERTs+SELECTs over the LIVE CHECK via raw asyncpg (mock-proof — a drift raises `CheckViolationError`); the 2-user RLS test must engage RLS via the authenticated role (a superuser bypass is caught by the A1 step — if user B reads A's row, the role switch is fixed, the assertion is NOT weakened) — **Task 2**. This is the D-102 "static would false-green" control made into the phase gate. |
| T-110-11 | Information Disclosure (verified live) | RLS correctness on the 4 tables | HIGH (class) | mitigate | T-110-01/02 (Plan 01 policy shape) are proven live here: the 2-user cross-user SELECT returns 0 rows for another user's private row; the forced-global INSERT is rejected — **Task 2** (`test_110_dm_schema.py`). The leak class is verified, not trusted by label. |

**Block-on-high check:** No HIGH-severity threat is left unmitigated. The gate-integrity HIGH (T-110-10, false-green) is mitigated by the live raw-asyncpg round-trip + the RLS-engaged-not-bypassed assertion — both of which fail RED on a real defect and cannot be satisfied by a mock. The RLS-leak HIGH (T-110-11) is the live verification of Plan 01's policy shape.
</threat_model>

<verification>
- Migration 071 applied to :54322 (operator-confirmed): 4 tables, 19-type CHECK, flag column present on the live DB.
- `supabase/full-schema.sql` regenerated (no `--reset`) and reflects all of migration 071.
- The 4 live integration tests GREEN (not skipped): SC#4 (8 types round-trip), SC#2/3 (subset), SC#1 (schema + 2-user RLS + global reachability), SC#5 (flag).
- Full suite green; net-new failures = 0 (base-checkout proof in SUMMARY).
- One commit contains the migration + regenerated full-schema.sql + the 3 source files + the 6 test files.
</verification>

<success_criteria>
- The substrate is REAL and verified LIVE — not merely code-complete. The SC#4 live round-trip over all 8 action types is GREEN against the migrated DB.
- RLS is proven engaged (2-user isolation), not bypassed.
- The flag defaults on and reads True live.
- The migration, regenerated artifact, and code ship in one commit per the project discipline.
- A green run on a DB-down machine is explicitly NOT accepted as a pass (the tests must run, not skip).
</success_criteria>

<output>
After completion, create `.planning/phases/110-dm-foundations/110-02-SUMMARY.md`.
</output>
