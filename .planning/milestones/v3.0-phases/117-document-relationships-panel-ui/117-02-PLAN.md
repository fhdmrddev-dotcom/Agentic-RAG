---
phase: 117-document-relationships-panel-ui
plan: 02
type: execute
wave: 2
depends_on: ["117-01"]
files_modified:
  - backend/app/api/document_relationships.py
  - backend/tests/integration/test_117_route_leak.py
  - backend/tests/unit/test_117_no_fork.py
autonomous: true
requirements: [REL-02]
user_setup: []

must_haves:
  truths:
    - "A document's outgoing + incoming typed links are fetchable over an authenticated GET endpoint the panel can call."
    - "The GET route calls the shared leak-safe read core (Plan 01) — it never re-implements the edge queries or the masking (no fork)."
    - "An unreadable/unknown subject returns a uniform 404 — no existence-probe oracle, no id/title leak."
    - "The GET writes NO audit row (no relationship.read enum; mirrors resolve_view)."
  artifacts:
    - path: "backend/app/api/document_relationships.py"
      provides: "GET /document-relationships?document_id={id} — thin auth + threadpool wrapper over the shared read core"
      contains: "@router.get"
    - path: "backend/tests/integration/test_117_route_leak.py"
      provides: "LIVE two-user ROUTE leak proof driving the HTTP route (the net-new caller the secure-phase must confirm)"
    - path: "backend/tests/unit/test_117_no_fork.py"
      provides: "Source-grep guard going GREEN — mask string + .in_(...) + _resolve_readable_latest absent from the route file"
  key_links:
    - from: "backend/app/api/document_relationships.py"
      to: "document_relationship_service.get_related_documents"
      via: "GET handler calls the shared fn and returns its plain dict"
      pattern: "get_related_documents"
    - from: "GET /document-relationships"
      to: "Depends(get_current_user)"
      via: "JWT auth dependency (caller-scoped, never subject/edge-owner-scoped)"
      pattern: "get_current_user"
---

<objective>
Add the net-new authenticated `GET /document-relationships?document_id={id}` route to the existing `document_relationships.py` router. It is a THIN wrapper: auth → call the shared `get_related_documents` (Plan 01) → return the plain dict; uniform 404 on an unreadable/unknown subject; NO audit write. This is the REST read seam the panel (Plan 04) consumes — and the net-new caller of the shared leak-safe core the secure-phase must prove leak-safe LIVE.

Purpose: Surface the relationship read to the frontend without forking the leak-safe traversal (D-117-7). The route boundary is net-new — the 116 suite only proved the TOOL boundary, so this plan adds the LIVE two-user ROUTE leak proof.

Output: The GET route; the `test_117_no_fork.py` grep guard going GREEN (no fork in the route); and `test_117_route_leak.py` driving the HTTP route end-to-end on :54322.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-document-relationships-panel-ui/117-CONTEXT.md
@.planning/phases/117-document-relationships-panel-ui/117-RESEARCH.md
@.planning/phases/117-document-relationships-panel-ui/117-PATTERNS.md
@.planning/phases/117-document-relationships-panel-ui/117-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. -->

From backend/app/services/document_relationship_service.py (Plan 01 output — the call target):
- async def get_related_documents(caller, *, document_id=None, filename=None, supabase=None) -> dict | None
  # returns {subject:{document_id,filename}, total, documents:[...rows w/ relationship_id...], source_refs:[...]} OR None (unreadable/unknown subject)

From backend/app/api/document_relationships.py (the host router — mirror the existing POST/DELETE auth shape):
- @router.post("", response_model=RelationshipResponse, status_code=201)   # line 69 — auth + threadpool pattern to mirror
- @router.delete("/{relationship_id}", status_code=204)                     # line 161
- Both use: current_user: dict = Depends(get_current_user), supabase: Client = Depends(get_supabase); caller = current_user["id"]
- Router mounted in main.py:424

From backend/app/api/document_views.py (the no-audit-on-read + 404-not-403 PRECEDENT):
- async def resolve_view(...)  # line 203 — readability gate → HTTPException(404) on unseeable → call the shared resolve core; writes NO audit on a read

From backend/app/services/audit_service.py:
- VALID_ACTION_TYPES has ONLY relationship.create / relationship.delete — NO relationship.read enum (confirmed). Do NOT write an audit on this GET.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the thin GET route (auth + threadpool, no audit, uniform 404, no fork)</name>
  <read_first>
    - backend/app/api/document_relationships.py (the full file — the existing POST at :69 + DELETE at :161 for the auth/Depends shape; the docstring conventions; where to add the GET)
    - backend/app/api/document_views.py (resolve_view at :203 — the no-audit-on-read + 404-not-403 precedent + plain-dict return, no tight response_model)
    - backend/app/services/document_relationship_service.py (the get_related_documents signature from Plan 01)
    - backend/app/services/audit_service.py (VALID_ACTION_TYPES — confirm no relationship.read enum)
  </read_first>
  <files>backend/app/api/document_relationships.py</files>
  <action>
    Add `@router.get("")` → `async def get_relationships(document_id: str, current_user: dict = Depends(get_current_user), supabase: Client = Depends(get_supabase))` to `document_relationships.py`. Body: `caller = current_user["id"]`; `result = await document_relationship_service.get_related_documents(caller, document_id=document_id, supabase=supabase)`; if `result is None` → `raise HTTPException(status_code=404, detail="Document not found")` (uniform 404 — no existence leak, mirrors resolve_view); else `return result` (plain dict, NO response_model — the 112 CR-01 lesson). The shared fn is already aexec-wrapped (D-v2.5-01) — the route adds no bare supabase call. Write NO audit (no relationship.read enum; mirrors resolve_view). The route MUST NOT contain the literal `"linked document (no access)"`, any `_resolve_readable_latest` call, or any `.in_(` edge query — those live ONLY in the service (the no-fork invariant). Do NOT touch the existing POST/DELETE. Do NOT touch threads.py (G-5).
  </action>
  <acceptance_criteria>
    - `grep -c "@router.get" backend/app/api/document_relationships.py` returns 1.
    - The route is a no-fork thin wrapper — these must all return 0 against the route file: `grep -c "linked document (no access)" backend/app/api/document_relationships.py`, `grep -c "_resolve_readable_latest" backend/app/api/document_relationships.py`, `grep -c "\.in_(" backend/app/api/document_relationships.py`.
    - No audit on the read: `grep -c "relationship.read" backend/app/api/document_relationships.py` returns 0; the GET handler body does not call `write_audit_entry`.
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_117_no_fork.py -x` passes GREEN (the grep guard now reads the real route file and confirms it is fork-free).
    - `git diff --stat backend/app/api/threads.py` shows 0 changes.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_117_no_fork.py -x</automated>
  </verify>
  <done>GET /document-relationships exists as a thin auth-+-shared-fn wrapper with uniform 404 and no audit; the no-fork grep guard is GREEN; threads.py untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drive the LIVE two-user ROUTE leak + read tests through the HTTP endpoint</name>
  <read_first>
    - backend/tests/integration/test_117_route_leak.py (the Plan 01 scaffold — extend it to drive the HTTP ROUTE, not just the shared fn)
    - backend/tests/integration/test_117_get_read.py (the read-shape live tests — confirm they still pass via the route)
    - backend/tests/integration/test_116_tool_leak.py (the two-user asyncpg + auth-token harness to clone for hitting the route as A and as B)
    - backend/app/api/document_relationships.py (the route added in Task 1 — the endpoint under test)
  </read_first>
  <files>backend/tests/integration/test_117_route_leak.py, backend/tests/integration/test_117_get_read.py</files>
  <behavior>
    - GET as user B over a subject both can read returns the far-endpoint masked (document_id null + "linked document (no access)") when the target is private to A.
    - GET as user A over the same subject returns the real far-endpoint filename + id.
    - GET for an unknown/unreadable subject returns HTTP 404 (uniform — no 403, no existence oracle).
    - The route's payload carries direction + label + relationship_id per row, over the subject's full version set (follow-to-latest).
  </behavior>
  <action>
    Extend `test_117_route_leak.py` to drive the HTTP route end-to-end (clone the test_116_tool_leak two-user auth-token + asyncpg seeding harness): authenticate as A and as B, GET `/document-relationships?document_id={subject}` for each, and assert B sees the masked far endpoint (`document_id is None`, the mask string, no private id/filename/metadata) while A sees the real filename. Add a uniform-404 assertion: GET for a subject the caller can't read/that doesn't exist → 404 (never 403, never a 200 with a partial leak). Confirm `test_117_get_read.py` rows carry `relationship_id` + correct `direction`/`label` via the route. These run LIVE against :54322 (not mocked) — the D-102/D-110-5 "static would false-green" discipline; mark the two-user route leak assertion for secure-phase confirmation as non-vacuous.
  </action>
  <acceptance_criteria>
    - `cd backend && venv/Scripts/python -m pytest tests/integration/test_117_route_leak.py tests/integration/test_117_get_read.py -x` passes live on :54322.
    - test_117_route_leak.py drives the HTTP route (a real GET request as A and as B), not just the shared fn in-process; B's masked row asserts `document_id is None` AND the mask string AND the ABSENCE of A's private filename/id.
    - The 404 path asserts status 404 (not 403, not 200) for an unreadable/unknown subject.
    - Non-vacuity: A's result over the SAME subject shows the real far-endpoint filename (proves the mask is access-driven, not a blanket null).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/integration/test_117_route_leak.py tests/integration/test_117_get_read.py -x</automated>
  </verify>
  <done>The LIVE two-user ROUTE leak proof passes on :54322 (B masked, A real, 404 uniform); the read-shape tests confirm relationship_id/direction/label via the route; the route-leak assertion is marked non-vacuous for secure-phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) → GET route | Untrusted `document_id` query param crosses here; the route is JWT-authed and caller-scoped. |
| GET route → shared read core | The route never re-implements access logic; it delegates to the one leak-safe fn. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-02-01 | Information Disclosure | cross-user leak via the new GET (the SC#2 invariant) | mitigate | The route scopes from the CALLER (`current_user["id"]`), never the subject/edge owner; all masking happens in the shared core. Proven by the LIVE two-user ROUTE leak test (Task 2), confirmed non-vacuous in secure-phase — NEVER via the RLS label (D-102/D-110-5). |
| T-117-02-02 | Information Disclosure | fork drift re-opening the leak | mitigate | `test_117_no_fork.py` grep guard: the mask string + `.in_(` + `_resolve_readable_latest` are absent from the route file; the route only calls `get_related_documents`. |
| T-117-02-03 | Information Disclosure | existence-probe oracle (404 vs 200 distinguishing private docs) | mitigate | Uniform 404 on unreadable/unknown subject (EXACT-match resolver in the shared core; no partial ilike — the D-116-3 probe-surface anti-pattern avoided). |
| T-117-02-04 | Denial of Service | blocking I/O in the async route | mitigate | The route adds no bare supabase call; the shared `get_related_documents` rides `aexec`/run_in_threadpool for every query (D-v2.5-01). |
| T-117-02-05 | Spoofing | unauthenticated read | mitigate | `Depends(get_current_user)` (JWT) on the route — same as every other endpoint; no net-new auth. |
| T-117-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (zero new deps). |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/ -k "116 or 117"` green live on :54322 (route + 116 regression preserved).
- The route file contains no fork tokens (mask string, `.in_(`, `_resolve_readable_latest`); `test_117_no_fork.py` GREEN.
- The LIVE two-user ROUTE leak proof is non-vacuous (A real vs B masked over the same subject); uniform 404 on unreadable subject.
- `git diff backend/app/api/threads.py` empty (G-5); no new package, no new migration.
</verification>

<success_criteria>
- GET /document-relationships?document_id={id} returns outgoing+incoming rows (direction/label/relationship_id) over the version set, uniform 404 on unreadable subject, no audit, no fork.
- The LIVE two-user ROUTE leak test passes on :54322 and is marked for secure-phase non-vacuity confirmation.
- threads.py untouched; zero new packages; zero migrations.
</success_criteria>

<output>
Create `.planning/phases/117-document-relationships-panel-ui/117-02-SUMMARY.md` when done.
</output>
