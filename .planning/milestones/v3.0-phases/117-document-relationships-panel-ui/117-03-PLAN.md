---
phase: 117-document-relationships-panel-ui
plan: 03
type: execute
wave: 3
depends_on: ["117-02"]
files_modified:
  - frontend/src/types/index.ts
  - frontend/src/lib/api.ts
autonomous: true
requirements: [REL-02]
user_setup: []

must_haves:
  truths:
    - "The frontend has typed contracts mirroring the GET payload (RelationshipRow with nullable document_id for masked rows) and the POST body."
    - "Three client fns exist — read (list), create (POST), delete (404-tolerant) — following the established getAuthHeaders + fetch + res.ok throw convention."
  artifacts:
    - path: "frontend/src/types/index.ts"
      provides: "RelType, RelationshipRow, RelatedDocumentsResponse, Relationship types"
      contains: "RelatedDocumentsResponse"
    - path: "frontend/src/lib/api.ts"
      provides: "listRelationships / createRelationship / deleteRelationship client fns"
      contains: "listRelationships"
  key_links:
    - from: "frontend/src/lib/api.ts listRelationships"
      to: "GET /document-relationships?document_id="
      via: "fetch with getAuthHeaders"
      pattern: "document-relationships\\?document_id="
    - from: "frontend/src/lib/api.ts createRelationship"
      to: "POST /document-relationships"
      via: "fetch body {source_doc_id, target_doc_id, rel_type}"
      pattern: "source_doc_id"
---

<objective>
Add the frontend typed contracts and the 3 API client functions the Relationships UI (Plan 04) consumes: `RelType`/`RelationshipRow`/`RelatedDocumentsResponse`/`Relationship` types mirroring the backend payloads, and `listRelationships`/`createRelationship`/`deleteRelationship`. This is the interface-first foundation — defining the contracts before the components implement against them (no scavenger hunt for the executor of Plan 04).

Purpose: A thin, well-typed seam between the net-new GET route (Plan 02) and the components (Plan 04). The masked-row nullable `document_id` is encoded in the type so the UI cannot accidentally render a leaked id.

Output: Types in `types/index.ts`; 3 client fns in `api.ts` cloning the `updateDocumentMetadata` / `document-views` family conventions.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-document-relationships-panel-ui/117-CONTEXT.md
@.planning/phases/117-document-relationships-panel-ui/117-RESEARCH.md
@.planning/phases/117-document-relationships-panel-ui/117-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. -->

Backend payload shapes to mirror (from Plan 01/02 + models/document_relationship.py:38-63):
- GET /document-relationships?document_id={id} → { subject:{document_id, filename}, total, documents: RelationshipRow[] }
- RelationshipRow row keys: document_id (string|null when masked), filename (string, real OR "linked document (no access)"), rel_type, direction ("outgoing"|"incoming"), label (raw snake_case), relationship_id (string — for the remove DELETE)
- POST /document-relationships body: { source_doc_id, target_doc_id, rel_type }; rel_type is Literal["supersedes","amends","references","attached_to"]
- POST 201 returns RelationshipResponse: { id, user_id?, source_doc_id, target_doc_id, rel_type, created_at? }
- DELETE /document-relationships/{id} → 204 (own-scoped; 404 on cross-user/absent — tolerate 404 client-side)

Existing api.ts conventions to clone:
- frontend/src/lib/api.ts:1997 updateDocumentMetadata — getAuthHeaders() + fetch + `if (!res.ok) throw` + `res.json()`
- frontend/src/lib/api.ts:2090 deleteView — 404-tolerant DELETE (`if (!res.ok && res.status !== 404) throw`)
- frontend/src/lib/api.ts:2103 resolveView — GET returning a plain dict
- API_BASE + getAuthHeaders() are already in-module

Existing types.ts conventions to clone:
- frontend/src/types/index.ts:216 MetadataFieldDef (mirrors a backend model)
- frontend/src/types/index.ts:288 SavedView (mirrors ViewResponse)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add relationship types to types/index.ts</name>
  <read_first>
    - frontend/src/types/index.ts (lines 210-339 — the MetadataFieldDef + SavedView + Document analogs; the mirror-the-backend-model convention; where to add the new types)
    - .planning/phases/117-document-relationships-panel-ui/117-PATTERNS.md (the exact type shapes under "frontend/src/types/index.ts")
  </read_first>
  <files>frontend/src/types/index.ts</files>
  <behavior>
    - RelationshipRow.document_id is typed `string | null` (null encodes the masked "no access" row — the type prevents rendering a leaked id).
    - RelType is the closed union "supersedes" | "amends" | "references" | "attached_to".
    - RelatedDocumentsResponse mirrors the GET payload (subject + total + documents[]).
  </behavior>
  <action>
    Add to `types/index.ts`: `export type RelType = "supersedes" | "amends" | "references" | "attached_to"`; `export interface RelationshipRow { document_id: string | null; filename: string; rel_type: RelType; direction: "outgoing" | "incoming"; label: string; relationship_id?: string }`; `export interface RelatedDocumentsResponse { subject: { document_id: string; filename: string }; total: number; documents: RelationshipRow[] }`; `export interface Relationship { id: string; user_id?: string | null; source_doc_id: string; target_doc_id: string; rel_type: string; created_at?: string | null }` (mirrors the backend RelationshipResponse, models/document_relationship.py:50-63). Add a comment on `document_id` noting null = masked (D-117-8, never leaks id/title). Do not modify existing types.
  </action>
  <acceptance_criteria>
    - `grep -c "RelatedDocumentsResponse\|RelationshipRow\|export type RelType" frontend/src/types/index.ts` >= 3.
    - `cd frontend && npx tsc --noEmit` passes (the new types compile; document_id is `string | null`).
    - RelationshipRow.document_id is `string | null` (verifiable: `grep -n "document_id: string | null" frontend/src/types/index.ts` returns a match).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>RelType/RelationshipRow/RelatedDocumentsResponse/Relationship exist in types/index.ts and compile; document_id is nullable (masked-row safety).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the 3 client fns to api.ts (list / create / delete)</name>
  <read_first>
    - frontend/src/lib/api.ts (lines 1195-1206 listDocuments, 1990-2112 updateDocumentMetadata + the document-views family createView/deleteView/resolveView — the getAuthHeaders + fetch + throw conventions, the 404-tolerant DELETE)
    - frontend/src/types/index.ts (the types added in Task 1 — import RelType/RelationshipRow/RelatedDocumentsResponse/Relationship)
    - .planning/phases/117-document-relationships-panel-ui/117-PATTERNS.md (the exact 3-fn bodies under "frontend/src/lib/api.ts")
  </read_first>
  <files>frontend/src/lib/api.ts</files>
  <behavior>
    - listRelationships(documentId) GETs the route and returns RelatedDocumentsResponse; throws on non-ok.
    - createRelationship(source, target, rel_type) POSTs the body and returns Relationship; throws on non-ok (422 = unseeable/self/forged).
    - deleteRelationship(id) DELETEs and is 404-tolerant (own-scoped; a 404 is a no-op, not an error).
  </behavior>
  <action>
    Add three exported async fns to `api.ts` cloning the in-module conventions. `listRelationships(documentId: string): Promise<RelatedDocumentsResponse>` → `fetch(\`${API_BASE}/document-relationships?document_id=${documentId}\`, { headers })`, `if (!res.ok) throw new Error("Failed to load relationships")`, `return res.json()`. `createRelationship(source_doc_id: string, target_doc_id: string, rel_type: RelType): Promise<Relationship>` → POST `${API_BASE}/document-relationships` with body `JSON.stringify({ source_doc_id, target_doc_id, rel_type })`, `if (!res.ok) throw new Error("Failed to create link")`, `return res.json()`. `deleteRelationship(id: string): Promise<void>` → DELETE `${API_BASE}/document-relationships/${id}`, `if (!res.ok && res.status !== 404) throw new Error("Failed to remove link")` (404-tolerant, the deleteView pattern). Use `getAuthHeaders()` for all three (it already sets content-type for the POST in the existing pattern — match createView). Import the types from `@/types` (or the project's types path).
  </action>
  <acceptance_criteria>
    - `grep -c "export async function listRelationships\|export async function createRelationship\|export async function deleteRelationship" frontend/src/lib/api.ts` returns 3.
    - deleteRelationship is 404-tolerant: `grep -c "status !== 404" frontend/src/lib/api.ts` increased by 1 (the new delete fn carries the tolerance).
    - createRelationship posts the exact body keys: `grep -c "source_doc_id" frontend/src/lib/api.ts` >= 1 in the new fn.
    - `cd frontend && npx tsc --noEmit` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>listRelationships/createRelationship/deleteRelationship exist in api.ts, typed against Task 1's types, 404-tolerant delete, exact POST body; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client → backend routes | The client fns carry the JWT (getAuthHeaders); the backend is the access gate. The client is NOT a trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-03-01 | Information Disclosure | masked-row id leak in the type contract | mitigate | `RelationshipRow.document_id` is typed `string | null`; the masked row carries null (the backend never sends an id) — the type makes a leaked-id render a type error. The actual gate is server-side (Plan 01/02). |
| T-117-03-02 | Spoofing | unauthenticated client call | accept | The client fns use `getAuthHeaders()` (the app-wide convention); auth is enforced server-side by `Depends(get_current_user)`. The client is not a trust boundary. |
| T-117-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (zero new deps — reuses existing fetch/getAuthHeaders). |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean (types + client fns compile).
- The 3 client fns exist and follow the established conventions (getAuthHeaders, throw-on-non-ok, 404-tolerant delete).
- No new package; no backend change in this plan.
</verification>

<success_criteria>
- RelType/RelationshipRow/RelatedDocumentsResponse/Relationship types exist and compile; document_id is nullable.
- listRelationships/createRelationship/deleteRelationship exist, typed, 404-tolerant delete.
- tsc clean; zero new packages.
</success_criteria>

<output>
Create `.planning/phases/117-document-relationships-panel-ui/117-03-SUMMARY.md` when done.
</output>
