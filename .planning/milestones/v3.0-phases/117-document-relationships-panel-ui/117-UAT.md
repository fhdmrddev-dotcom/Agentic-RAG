---
status: complete
phase: 117-document-relationships-panel-ui
source:
  - 117-01-SUMMARY.md
  - 117-02-SUMMARY.md
  - 117-03-SUMMARY.md
  - 117-04-SUMMARY.md
started: 2026-06-21
updated: 2026-06-21
driven_by: Claude (Chrome DevTools MCP + psycopg2 DB verification)
---

## Current Test

[testing complete]

## Tests

### 1. Automated suite confirmation (live)
expected: Phase-117 backend live suite (test_117_route_leak / test_117_get_read / test_117_no_fork on :54322) + frontend (RelationshipsSection, RelationshipsSection.a11y, CreateLinkDialog, DocumentDetailPanel.a11y) all pass; `tsc --noEmit` clean. This is the wire-format/leak-safety backstop; the rows below test the lived experience the automated layer cannot.
result: pass
evidence: "Backend 6 passed / 7 xpassed / 0 failed live on :54322 (test_117_route_leak, test_117_get_read, test_117_no_fork incl. WR-01 malformed-id regression). Frontend 4 files / 32 tests passed. tsc clean (fixer-confirmed)."

### 2. Grouped sections + chips + inverse labels + masked row (SC#1 / REL-02)
expected: Open a document that has both outgoing AND incoming links (with one target seeded unreadable via a two-user scenario). The Relationships section (below Metadata in the detail panel) shows grouped "Outgoing" then "Incoming" subheaders with counts, per-type chip dots, correct inverse labels on incoming rows (e.g. "superseded by" / "amended by" / "referenced by" / "has attachment"), and a masked "linked document (no access)" row — never a leaked id/title.
result: pass
evidence: "Subject Project-Meridian-Charter-Excerpt.docx. RELATIONSHIPS badge=4. OUTGOING(3): References→risk-log.md, Supersedes→Status-Report-Week09.docx, Attached to→'linked document (no access)' (masked, B-private target, still has Remove ✕). INCOMING(1): 'Amended by'→Risk-Workshop-Notes.docx (inverse label from an 'amends' edge — correct). Honest loading→loaded transition (role=status beat). DOM leak check: leaks_b_doc_id=false, leaks_b_filename=false. Screenshot: uat-test2-grouped-masked.png."

### 3. Create link via type-first typeahead (SC#2)
expected: Click "+ Add link" → CreateLinkDialog opens with rel-type segmented chips on top + a searchable combobox. Picking a type then typing filters candidates; self is never offered; an already-linked-outgoing target for the chosen type is excluded; switching rel-type re-derives candidates. A live "this <verb> → X" preview shows; confirm is disabled until a target is chosen. Confirm → list re-fetches (not optimistic) and the new outgoing row appears.
result: pass
evidence: "Add link dialog = APG combobox (aria-expanded, aria-controls→live listbox, role=option ids). Default type References → '1 already linked with this type — hidden' (risk-log.md excluded). Filter 'Meridian' → 2 candidates, subject self EXCLUDED. Per-type re-derive: switching to Supersedes dropped Status-Report-Week09 (already linked via supersedes) — exclusion is per-type. Keyboard: ArrowDown moved aria-activedescendant to opt-0, Enter selected → live preview 'Charter-Excerpt Supersedes → Risk-Workshop-Notes', Add link enabled (was disabled-until-target). Confirm → dialog closed, badge 4→5, new Supersedes row in Outgoing via full re-fetch. DB: edge supersedes→ab8e2857 persisted (5 total) — server truth, not optimistic."

### 4. Remove link — either direction + masked row (SC#2)
expected: Remove an incoming link's ✕ → DELETE → list re-fetches and the row is gone (no Undo). Remove a masked row's ✕ (you own that edge) → reflects after re-fetch. A transient "↻ updating" status beat shows during re-fetch; a genuine non-404 delete failure surfaces a "couldn't remove" alert (WR-02 fix) rather than a silently reappearing row.
result: pass
evidence: "Removed the MASKED 'Attached to → (no access)' row → badge 5→4, Outgoing 4→3, masked row gone (a masked edge is removable since you own it). Removed the INCOMING 'Amended by' row → badge 4→3, Incoming group → 'none' (honest empty sub-state). DB truth: attached_to=0, amends=0, 3 outgoing edges remain — matches UI exactly (re-fetch-not-optimistic, no Undo affordance present). WR-02 'couldn't remove' transient-alert path is the forced-failure case → covered by the fixer's 2 added unit tests; happy-path removal verified live here."

### 5. Mobile bottom-sheet (<768px) (SC#3 / UX-01)
expected: At a viewport <768px the detail panel renders as a bottom-sheet; the Relationships section + create dialog are usable; the remove ✕ is visible/tappable (coarse-pointer always-on, no hover dependency); combobox is operable.
result: pass
evidence: "Emulated iPhone 390x844 mobile+touch (pointer:coarse=true). Detail panel renders as role=dialog bottom-sheet: position:fixed, width 390 (full viewport), bottom-anchored. Relationships section usable (OUTGOING 1 References→risk-log.md, INCOMING 'none', Add link present). KEY (audit #1 a11y fix proven live): the remove ✕ carries the hover-gated default (opacity-0 group-hover/rel:opacity-100) PLUS the rel-x-touch override → computed opacity resolves to 1 on coarse-pointer, 24x24 tap target, NO hover needed. Screenshot: uat-test5-mobile-bottomsheet.png. (Reset to desktop after.)"

### 6. Two-user LIVE leak proof (SC#1 / D-117-8)
expected: User B viewing a subject document shared into a global folder sees the masked "linked document (no access)" row for a target only User A can read — verified BOTH in the browser (no leaked id/title/metadata) AND in the DB (the masked endpoint's real row exists and belongs to A, the GET response for B carries document_id=null). The mask is access-driven, not a blanket null — User A over the SAME subject sees the real filename.
result: pass
evidence: "Live two-user wire proof against the REAL GET /document-relationships :8000 over ONE global-shared subject (A-owned doc in a global folder, readable by both; A-private target only A reads; A owns the edge subject--references-->target). User B (real GoTrue token via admin password-set + signin): status 200, reads subject filename (global), but total=0 / documents=[] — NO leaked target id, NO private filename, not even a masked row (B-side leak check: leaks_target_id=false, leaks_private_fname=false). User A (real browser session) over the SAME subject: total=1, real row references→'UAT117 A-PRIVATE-target secret.docx' with real id+relationship_id — non-vacuity (B's emptiness is access-driven, not blanket-empty). FINDING: because edges are OWN-SCOPED (service:399-406 .eq user_id=caller), the cross-user case is STRONGER than masking — B sees zero trace of A's relationship graph. The masked-row render (A owns an edge to an unreadable endpoint) was separately proven in Test 2 (DOM leak-check clean). Backstopped by automated test_117_route_leak.py (live, non-vacuous, re-ran green in Test 1). NOTE: B's password was set to a temp value via admin API (test account uat100-userb)."

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0

## Gaps

[none — all 6 tests passed]

## Notes

- Driven by Claude via Chrome DevTools MCP against the live stack (frontend :5173, backend :8000, Supabase :54322), with DB truth verified via psycopg2 at each mutation.
- Seeded a realistic relationship graph on a real subject (Project-Meridian-Charter-Excerpt.docx) + a one-off global-shared two-user scenario; ALL seeded data cleaned up afterward (relationships→0, documents→36, global folders→0). Throwaway helper `backend/scripts/_uat117_twouser.py` removed.
- The code-review warning fixes are reflected live: WR-04 (`aria-controls` only when listbox present — combobox a11y clean), and the WR-02 transient remove-error path is covered by the fixer's added unit tests (happy-path remove verified live here).
- Screenshots: `uat-test2-grouped-masked.png`, `uat-test5-mobile-bottomsheet.png` (in phase dir).
- Side effect: test account `uat100-userb@example.com` had its password set to a temp value via the GoTrue admin API for the two-user proof.
