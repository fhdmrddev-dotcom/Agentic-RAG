---
phase: 117-document-relationships-panel-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/document_relationship_service.py
  - backend/app/services/tool_dispatcher.py
  - backend/tests/integration/test_117_route_leak.py
  - backend/tests/integration/test_117_get_read.py
  - backend/tests/unit/test_117_no_fork.py
autonomous: true
requirements: [REL-02]
user_setup: []

must_haves:
  truths:
    - "The leak-safe outgoing+incoming relationship read traversal exists as ONE shared, FastAPI-free function callable in-process (no HTTPException raised into the agent loop)."
    - "The agent tool's relationship-read output (masked rows, source_refs, direction/label, calm strings) is byte-identical after the refactor — the 116 tool suite stays green."
    - "Each related-document row carries the edge relationship_id through (the panel's remove ✕ needs it) without breaking the 116 strict-shape tests."
    - "An unreadable/unknown subject returns calmly (None), never a raise; a masked far-endpoint shows 'linked document (no access)' with document_id null."
  artifacts:
    - path: "backend/app/services/document_relationship_service.py"
      provides: "Shared get_related_documents(caller, *, document_id, filename, supabase) read core + relocated _INVERSE_LABEL / _NO_ACCESS_MASK"
      contains: "def get_related_documents"
    - path: "backend/app/services/tool_dispatcher.py"
      provides: "_handle_get_related_documents refactored to a thin caller of the shared fn (behavior-preserving)"
      contains: "get_related_documents"
    - path: "backend/tests/integration/test_117_route_leak.py"
      provides: "Wave-0 scaffold — LIVE two-user route/shared-fn leak proof (cloned from test_116_tool_leak harness)"
    - path: "backend/tests/integration/test_117_get_read.py"
      provides: "Wave-0 scaffold — GET-shape read over the version set + follow-to-latest + create-appears/remove-reflects"
    - path: "backend/tests/unit/test_117_no_fork.py"
      provides: "Wave-0 scaffold — source-grep guard: mask string + .in_(...) edge query live ONLY in the service, never the route"
  key_links:
    - from: "backend/app/services/tool_dispatcher.py"
      to: "document_relationship_service.get_related_documents"
      via: "in-process call inside _handle_get_related_documents"
      pattern: "get_related_documents"
    - from: "backend/app/services/document_relationship_service.get_related_documents"
      to: "document_relationship_service._resolve_readable_latest"
      via: "per-edge other-endpoint readability re-check (the sole access gate)"
      pattern: "_resolve_readable_latest"
---

<objective>
Extract the canonical leak-safe relationship read traversal — currently living ONLY inside the agent-tool handler `tool_dispatcher._handle_get_related_documents` (lines 492-649) — into a new shared `document_relationship_service.get_related_documents(...)`, then refactor the agent handler into a thin caller of it. This is the D-117-7 "share, do NOT fork" core: one implementation of the access-gating traversal, consumed by the agent tool now and the net-new GET route (Plan 02) next.

Purpose: Eliminate by construction the single highest risk in this phase — forking the leak-safe read into the route, which would re-open the SC#2 cross-user leak. Mirrors the Phase 115 `resolve_filter` → `document_view_resolver.py` extraction.

Output: A FastAPI-free `get_related_documents` fn returning a plain dict (carrying `relationship_id` per row, additively); a behavior-preserving agent handler; and the three Wave-0 backend test scaffolds the rest of the backend work (Plan 02) verifies against.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-document-relationships-panel-ui/117-CONTEXT.md
@.planning/phases/117-document-relationships-panel-ui/117-RESEARCH.md
@.planning/phases/117-document-relationships-panel-ui/117-PATTERNS.md
@.planning/phases/117-document-relationships-panel-ui/117-VALIDATION.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase this session. Use directly — no exploration needed. -->

From backend/app/services/document_relationship_service.py (primitives the new fn REUSES — do NOT re-implement):
- def _uid(user_id) -> str                              # line 56 — UUID-coerce every owner-scoping predicate
- async def _subject_version_ids(subject_row, *, supabase=None)  # line 111 — full (user_id, filename) lineage set for the .in_() edge queries (CR-02)
- async def _resolve_readable_latest(doc_id_or_filename, caller, *, by_filename=False, supabase=None)  # line 143 — THE sole access gate (CR-01 is_latest-gate + post-follow folder re-check); returns the readable latest row or None
- async def create_relationship(...)                    # line 250 — unchanged, do not touch
- async def delete_relationship(...)                    # line 296 — unchanged, do not touch
- `aexec` is imported at line 46; every query in this module rides aexec — ZERO bare .execute() (D-v2.5-01)

From backend/app/services/tool_dispatcher.py (the extraction SOURCE + constants to relocate):
- _INVERSE_LABEL = { "supersedes":"superseded_by", "amends":"amended_by", "references":"referenced_by", "attached_to":"has_attachment" }   # line 478
- _NO_ACCESS_MASK = "linked document (no access)"        # line 489
- async def _handle_get_related_documents(args, ctx)     # line 492 — the handler to refactor; calm-string contract at ~528-559, ToolResult packaging at ~639-648
- async def _append_edge(edge, *, direction)             # line 597 — the per-edge masking/label logic; NOTE: edge["id"] is select-ed at :575/:581 but DROPPED here (the panel needs it)
- subject edge queries .select("id, source_doc_id, target_doc_id, rel_type") with .in_("source_doc_id", version_ids) / .in_("target_doc_id", version_ids)  # :573-584
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 backend test scaffolds (route leak, GET read, no-fork guard)</name>
  <read_first>
    - backend/tests/integration/test_116_tool_leak.py (the LIVE two-user asyncpg leak harness to clone — own-scoped per-user edges, GLOBAL-folder subject both read, PRIVATE target only owner reads)
    - backend/tests/integration/test_116_tool_read.py (the strict-shape read assertions — confirm what the agent rows expose; the route-leak/get-read tests reuse the seeding shape)
    - backend/tests/integration/test_116_version_stable.py (the follow-to-latest re-upload seeding pattern)
    - backend/tests/unit/test_115_resolver_extraction.py (the extraction-guard pattern: imports inside test bodies, xfail(strict=False) so collection never errors while RED)
    - .planning/phases/117-document-relationships-panel-ui/117-VALIDATION.md (Wave 0 Requirements — the exact filenames + behaviors each scaffold must cover)
  </read_first>
  <files>backend/tests/integration/test_117_route_leak.py, backend/tests/integration/test_117_get_read.py, backend/tests/unit/test_117_no_fork.py</files>
  <action>
    Create three RED/xfail scaffolds matching the VALIDATION.md Wave-0 filenames. Use xfail(strict=False) (or imports inside test bodies) so the suite still exits 0 while the shared fn / route are unbuilt — the 116 precedent.

    (a) `backend/tests/integration/test_117_route_leak.py` — clone the test_116_tool_leak asyncpg harness, but drive the SHARED fn `document_relationship_service.get_related_documents` directly (Plan 02 adds the test that drives the HTTP route; this file proves the shared-core boundary the route will inherit). Seed two users A and B with their OWN edges: subject = A's GLOBAL-folder doc both can read; target = A's PRIVATE doc only A reads. Assert B's result masks the far endpoint as `_NO_ACCESS_MASK` with `document_id is None` (never the private id/filename/metadata), while A's result shows the real filename. Mark this file's leak assertion for secure-phase confirmation (the LIVE two-user proof — D-117-8).

    (b) `backend/tests/integration/test_117_get_read.py` — assert `get_related_documents` returns a dict with `subject`, `total`, and `documents[]` carrying `direction` ("outgoing"|"incoming"), `label`, and `relationship_id` per row, over the subject's FULL `(user_id, filename)` version set (follow-to-latest: an edge keyed on v1 surfaces under v2 after re-upload — seed via the test_116_version_stable pattern). Add `test_created_link_appears` and a remove-reflects assertion: create via `create_relationship` → the row appears in the next read; delete via `delete_relationship` → it disappears.

    (c) `backend/tests/unit/test_117_no_fork.py` — a source-grep guard (clone the spirit of test_115_resolver_extraction): read `backend/app/api/document_relationships.py` as text and assert the literal `"linked document (no access)"`, the substring `_resolve_readable_latest`, and the substring `.in_(` do NOT appear in it (they must live ONLY in the service). Also assert `document_relationship_service.get_related_documents` is importable and callable. The route file does not exist yet (Plan 02) — guard the read with a skip-if-missing so collection passes while RED, OR xfail; the assertion goes GREEN once Plan 02's route stays fork-free.
  </action>
  <acceptance_criteria>
    - `cd backend && venv/Scripts/python -m pytest tests/integration/test_117_route_leak.py tests/integration/test_117_get_read.py tests/unit/test_117_no_fork.py --collect-only` exits 0 (no collection errors).
    - The full suite still exits 0 with the new scaffolds present (they are xfail/skip while RED): `cd backend && venv/Scripts/python -m pytest tests/unit/test_117_no_fork.py` reports no errors.
    - test_117_route_leak.py asserts `document_id is None` AND the `_NO_ACCESS_MASK` string for B's masked row, and the real filename for A — never the private id/filename in B's result.
    - test_117_get_read.py asserts `relationship_id` is present on each row and that follow-to-latest surfaces a v1-keyed edge under v2.
    - test_117_no_fork.py reads document_relationships.py source text and asserts the 3 fork tokens are absent from it.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/integration/test_117_route_leak.py tests/integration/test_117_get_read.py tests/unit/test_117_no_fork.py --collect-only</automated>
  </verify>
  <done>Three Wave-0 backend scaffolds exist with the VALIDATION.md filenames; suite still exits 0; the leak/shape/no-fork assertions are authored (RED/xfail until Tasks 2-3 + Plan 02 land them).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract get_related_documents into the shared service + relocate the constants</name>
  <read_first>
    - backend/app/services/tool_dispatcher.py (lines 470-649 — the extraction SOURCE: _INVERSE_LABEL/_NO_ACCESS_MASK, the subject resolve, the .in_(version_ids) edge queries, _append_edge masking, the result dict at :639-648)
    - backend/app/services/document_relationship_service.py (the destination module — _resolve_readable_latest, _subject_version_ids, _uid, _client, the aexec import; place the new fn here)
    - backend/app/services/document_view_resolver.py (the Phase 115 extraction PRECEDENT — FastAPI-free, returns a plain dict, no HTTPException; the shape to follow)
    - backend/tests/integration/test_117_get_read.py (the Task-1 scaffold this task turns GREEN)
  </read_first>
  <files>backend/app/services/document_relationship_service.py</files>
  <behavior>
    - get_related_documents returns `{subject: {document_id, filename}, total, documents: [...rows...], source_refs: [...]}` for a readable subject.
    - Returns None when the subject is unreadable/unknown (calm — no raise).
    - Each row carries `document_id` (or None when masked), `filename` (real or `_NO_ACCESS_MASK`), `rel_type`, `direction`, `label` (rel_type for outgoing, `_INVERSE_LABEL[rel_type]` for incoming), AND `relationship_id` (= the edge `id`).
    - A far endpoint the caller can't read → `document_id: None` + `filename: _NO_ACCESS_MASK` (never id/title/metadata).
    - The fn imports NO FastAPI symbol (no HTTPException/status on the module surface).
  </behavior>
  <action>
    Lift `tool_dispatcher.py:561-649` (the subject resolve via `_resolve_readable_latest`, `_subject_version_ids`, the two own-scoped `.in_(version_ids)` edge queries on `source_doc_id`/`target_doc_id` via `aexec`, and the per-edge `_append_edge` masking) into a new `async def get_related_documents(caller, *, document_id=None, filename=None, supabase=None)` in `document_relationship_service.py`. It accepts the subject by `document_id` (preferred) or exact `filename`. RELOCATE `_INVERSE_LABEL` and `_NO_ACCESS_MASK` into this service module (they are referenced only by tool_dispatcher.py today — verified). THE ONE SHAPE CHANGE (A6): carry `edge["id"]` through to each compact row as `relationship_id` (the agent handler currently drops it at `_append_edge` — `id` is already select-ed at the edge queries). Return the plain dict (mirror tool_dispatcher.py:639-648); return None for an unreadable/unknown subject. Reuse the existing `_resolve_readable_latest`/`_subject_version_ids`/`_uid`/`aexec` — do NOT re-implement the access gate. No bare `.execute()`. The new fn must NOT import FastAPI.
  </action>
  <acceptance_criteria>
    - `grep -c "def get_related_documents" backend/app/services/document_relationship_service.py` returns 1.
    - `grep -c "relationship_id" backend/app/services/document_relationship_service.py` >= 1 (the edge id is carried through).
    - `grep -n "HTTPException\|fastapi" backend/app/services/document_relationship_service.py` returns nothing (FastAPI-free).
    - `_INVERSE_LABEL` and `_NO_ACCESS_MASK` are defined in document_relationship_service.py: `grep -c "_NO_ACCESS_MASK\s*=" backend/app/services/document_relationship_service.py` returns 1.
    - `cd backend && venv/Scripts/python -m pytest tests/integration/test_117_get_read.py tests/integration/test_117_route_leak.py -x` passes live on :54322 (the masked far endpoint shows document_id None + the mask string; follow-to-latest surfaces the v1 edge under v2; relationship_id present).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/integration/test_117_get_read.py tests/integration/test_117_route_leak.py -x</automated>
  </verify>
  <done>Shared `get_related_documents` lives in document_relationship_service.py (FastAPI-free, plain dict, relationship_id carried, _INVERSE_LABEL/_NO_ACCESS_MASK relocated); the GET-read + route-leak live tests pass on :54322.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refactor _handle_get_related_documents into a thin caller (behavior-preserving)</name>
  <read_first>
    - backend/app/services/tool_dispatcher.py (lines 492-649 — the full handler; the calm-string contract at ~528-559: no_subject / unavailable / not_found; the ToolResult packaging at ~639-648; the import sites for the relocated _INVERSE_LABEL/_NO_ACCESS_MASK)
    - backend/app/services/document_relationship_service.py (the new get_related_documents from Task 2 — the call target)
    - backend/tests/integration/test_116_tool_leak.py + test_116_tool_read.py + backend/tests/unit/test_116_handler.py + backend/tests/integration/test_116_version_stable.py (the byte-identical-behavior regression backstop — 35/35 live on :54322)
  </read_first>
  <files>backend/app/services/tool_dispatcher.py</files>
  <behavior>
    - The agent tool output (masked rows, source_refs, direction/label fields, calm "not_found"/"no_subject"/"unavailable" strings) is byte-identical to before the refactor.
    - A no-subject / unreadable-subject call still returns the existing calm ToolResult string — NO raise into the agent loop.
    - The handler imports _INVERSE_LABEL/_NO_ACCESS_MASK from document_relationship_service now (the relocation), not its own module-level copies.
  </behavior>
  <action>
    Replace the traversal body in `_handle_get_related_documents` with a call to `document_relationship_service.get_related_documents(caller, document_id=doc_id, filename=filename, supabase=ctx.supabase)`. KEEP the handler's existing input parsing, the calm-string contract for no-subject/not-found (map a None return to the existing `not_found` string), and the `ToolResult` JSON packaging + `source_refs`. Update the import sites for `_INVERSE_LABEL`/`_NO_ACCESS_MASK` (now in the service) — or import them from the service. Delete the now-dead in-handler edge-query/`_append_edge` code that moved to the service. Do NOT change the ToolResult string shape, the masked-row JSON, or the source_refs — the agent's output must stay byte-identical (the 116 suite is the proof). Do NOT touch `dispatch_tool`, the whitelist, or any provider service (G-5; no cross-provider regression).
  </action>
  <acceptance_criteria>
    - `grep -c "get_related_documents" backend/app/services/tool_dispatcher.py` >= 1 (the handler calls the shared fn).
    - The dead in-handler edge-query body is gone: `grep -c "_append_edge" backend/app/services/tool_dispatcher.py` returns 0 (the masking moved to the service).
    - `cd backend && venv/Scripts/python -m pytest tests/integration/test_116_tool_leak.py tests/integration/test_116_tool_read.py tests/integration/test_116_version_stable.py tests/unit/test_116_handler.py` stays green live on :54322 (byte-identical agent behavior — the regression backstop). If test_116_tool_read.py strict-shape asserts EXACT keys and the new relationship_id breaks it, honestly assess: the field is additive and the agent ignores it — adjust the strict-shape test to allow the additive key (a faithful test update, NOT a weakened test), and document the assessment in the SUMMARY.
    - `git diff --stat backend/app/api/threads.py` shows 0 changes (G-5).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/ -k "116 or 117_get_read or 117_route_leak" -x</automated>
  </verify>
  <done>The agent handler is a thin caller of the shared fn; the 116 tool suite stays green (byte-identical behavior, or a faithful strict-shape adjustment for the additive relationship_id, documented); threads.py untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller (JWT user) → service-role supabase | The service-role client bypasses RLS; app-code predicates (`_uid`-scoped) are the actual access gate. |
| edge owner → far-endpoint readability | An edge the caller owns may point at a document the caller cannot read; the per-viewer re-check is the sole gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-01-01 | Information Disclosure | `get_related_documents` far-endpoint masking | mitigate | Each edge's other endpoint is re-checked via `_resolve_readable_latest` (the CR-01 is_latest-gate + post-follow folder re-check); an unreadable endpoint masks as `_NO_ACCESS_MASK` + `document_id:None` — never id/title/metadata. Proven by test_117_route_leak.py LIVE two-user (marked for secure-phase). |
| T-117-01-02 | Tampering / Elevation | owner-scoping predicates on the edge queries | mitigate | Edge queries are `.eq("user_id", _uid(caller))`; `_uid` UUID-coerces (the service-role client bypasses RLS, so the app predicate is the gate). Reused verbatim from the hardened service — no new query construction. |
| T-117-01-03 | Information Disclosure | extraction drift (fork risk) | mitigate | The traversal has exactly ONE implementation (this fn); the agent handler CALLS it. `test_117_no_fork.py` (Task 1) is the grep guard; behavior-preservation is proven by the 116 regression suite. |
| T-117-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (zero new deps — RESEARCH §Package Legitimacy Audit vacuously clean). |
</threat_model>

<verification>
- Full backend `test_117_*` set + 116 regression: `cd backend && venv/Scripts/python -m pytest tests/ -k "116 or 117"` green live on :54322.
- `_NO_ACCESS_MASK` literal + `_resolve_readable_latest` call + `.in_(` edge query appear ONLY in document_relationship_service.py (and tool_dispatcher.py before extraction is reduced) — never in any route file.
- `git diff backend/app/api/threads.py` is empty (G-5).
- No new package, no new migration.
</verification>

<success_criteria>
- `get_related_documents` exists in document_relationship_service.py, FastAPI-free, returns a plain dict carrying `relationship_id` per row, masks unreadable endpoints.
- The agent handler is a thin caller; the 116 tool suite stays green (byte-identical behavior or a faithful additive-key adjustment, documented).
- The three Wave-0 backend scaffolds exist and the GET-read + route-leak live tests pass on :54322.
- threads.py untouched; zero new packages; zero migrations.
</success_criteria>

<output>
Create `.planning/phases/117-document-relationships-panel-ui/117-01-SUMMARY.md` when done.
</output>
