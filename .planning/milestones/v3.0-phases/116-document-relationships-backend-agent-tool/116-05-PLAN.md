---
phase: 116-document-relationships-backend-agent-tool
plan: 05
type: execute
wave: 4
depends_on: [116-01, 116-03]
files_modified:
  - backend/app/services/document_relationship_service.py
  - backend/app/services/tool_dispatcher.py
  - backend/app/api/document_relationships.py
  - backend/tests/integration/test_116_tool_leak.py
  - backend/tests/integration/test_116_version_stable.py
autonomous: true
gap_closure: true
requirements: [REL-01, REL-04]
user_setup: []

must_haves:
  truths:
    - "A non-owner who replays an OLD version's id that is still in a global folder, while the owner's LATEST version has moved to a private folder, gets None from _resolve_readable_latest — never the private latest's id/filename/metadata (SC#2 / REL-04 leak-safety)"
    - "An edge created against a document's version-N id still surfaces in get_related_documents after the subject is re-uploaded to version N+1 (a new uuid row, old is_latest=False) — the link is not orphaned (SC#1 / REL-01 LOCKED D-116-1 follow-to-latest)"
    - "Both new regression tests FAIL against the pre-fix code and PASS after the fix (non-vacuous, the IN-03 'static would false-green' gap closed)"
  artifacts:
    - path: "backend/app/services/document_relationship_service.py"
      provides: "CR-01 fix: is_latest gate on the global-by-id leg + post-follow folder/owner visibility re-check; _subject_version_ids helper for CR-02; WR-02 _uid() on write paths"
      contains: "_subject_version_ids"
    - path: "backend/app/services/tool_dispatcher.py"
      provides: "CR-02 fix: edge queries use .in_() over the subject's full version-id set"
      contains: ".in_("
    - path: "backend/tests/integration/test_116_tool_leak.py"
      provides: "non-vacuous CR-01 regression: old-global / new-private version pair → resolver returns None for a non-owner"
      contains: "is_latest"
    - path: "backend/tests/integration/test_116_version_stable.py"
      provides: "non-vacuous CR-02 regression: edge survives re-upload, still surfaces in get_related_documents"
      contains: "get_related_documents"
  key_links:
    - from: "backend/app/services/document_relationship_service.py:_resolve_readable_latest (global by-id leg)"
      to: "backend/app/api/documents.py:list_documents (the canonical access model, :540-561)"
      via: "mirrored is_latest=True gate + global-folder containment re-check"
      pattern: "is_latest"
    - from: "backend/app/services/tool_dispatcher.py:_handle_get_related_documents"
      to: "backend/app/services/document_relationship_service._subject_version_ids"
      via: "edge enumeration over the full version-id set"
      pattern: "_subject_version_ids"
---

<objective>
Close the two confirmed BLOCKERs from 116-VERIFICATION.md (4/6 must-haves) — both
read-side defects, no migration.

- **CR-01 (SC#2 / REL-04 leak-safety):** `_resolve_readable_latest`'s global-by-id leg
  lacks `.eq("is_latest", True)` and the follow-to-latest step has no folder re-check.
  A non-owner replaying an old version's id (still in a global folder) follows forward
  to the owner's PRIVATE latest version and gets its real id/filename/metadata — strictly
  more permissive than `list_documents`. The D-116-9 mask never fires (resolver returns
  non-None).
- **CR-02 (SC#1 / REL-01 LOCKED D-116-1 follow-to-latest):** edges store creation-time
  ids; the handler queries edges by the resolved-LATEST subject id. After a re-upload the
  edge keyed on version N is never found → the link silently vanishes from
  `get_related_documents`.

Also fold the small WR-02 (`_uid()` on the create/delete write paths), WR-03 (the
mislabeled "fire-and-forget" audit comment) and WR-04 (the overstated self-link ordering
comment) nits. WR-01 is a verified non-defect today — explicitly NOT worked here.

Purpose: the phase cannot honestly pass verification with a live cross-user disclosure
(CR-01) and a violated LOCKED follow-to-latest guarantee (CR-02). Both are the exact
"static would false-green" cases the existing suite missed.
Output: the resolver + handler fixes, two NON-VACUOUS live regression tests (each written
to FAIL against the current code and PASS after the fix), and the three nit fold-ins.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-document-relationships-backend-agent-tool/116-VERIFICATION.md
@.planning/phases/116-document-relationships-backend-agent-tool/116-REVIEW.md
@.planning/phases/116-document-relationships-backend-agent-tool/116-CONTEXT.md

<interfaces>
<!-- The access-model source of truth the CR-01 fix must mirror. Extracted from the codebase. -->
<!-- Use these directly — no codebase exploration needed. -->

THE CANONICAL ACCESS MODEL — backend/app/api/documents.py:540-561 (list_documents):
  - Own docs:    documents WHERE user_id = caller AND is_latest = True
  - Global docs: documents WHERE folder_id IN get_globally_visible_folder_ids(caller) AND is_latest = True
  - "readable" = own-latest ∪ global-latest. An OLD version in a global folder is NOT
    independently readable (the global leg is is_latest-gated).

THE VERSIONING MODEL — backend/app/api/documents.py:441-486 (re-upload):
  - Re-upload of the same filename INSERTs a NEW row with a fresh uuid (gen via uuid4),
    bumps version_number, sets is_latest=True, and flips ALL prior versions of
    (user_id, filename) to is_latest=False. Old rows are KEPT (not deleted). Old rows
    retain their original folder_id. Rename DOES NOT EXIST (RESEARCH grep-proof, HIGH
    confidence), so (user_id, filename) is a stable lineage handle.

GLOBAL VISIBILITY — backend/app/utils/folder_utils.py:48-56
  (get_globally_visible_folder_ids): returns folder ids NOT owned by the caller but in a
  global subtree → CROSS-USER readable. This is why an old-global row can be read by a
  non-owner.

THE RESOLVER UNDER REPAIR — backend/app/services/document_relationship_service.py:
  async def _resolve_readable_latest(doc_id_or_filename, caller, *, by_filename=False, supabase=None) -> dict | None
  async def _latest_by_filename(filename, caller, *, supabase=None) -> dict | None   # already is_latest-gated on BOTH legs (the correct pattern to mirror)
  def _uid(user_id) -> str                                                            # UUID-coercion guard
  async def create_relationship(user_id, source_doc_id, target_doc_id, rel_type, supabase=None) -> dict
  async def delete_relationship(user_id, rel_id, supabase=None) -> bool

THE HANDLER UNDER REPAIR — backend/app/services/tool_dispatcher.py:492-639
  async def _handle_get_related_documents(args, ctx) -> ToolResult
  - subject_id = subject["id"]  (line 559) — the resolved LATEST id
  - outgoing edges: .eq("source_doc_id", subject_id)  (line 567)   ← CR-02: single id, must be .in_(version_ids)
  - incoming edges: .eq("target_doc_id", subject_id)  (line 573)   ← CR-02: single id, must be .in_(version_ids)
  - ctx.supabase is the service-role client; document_relationship_service._uid(caller) scopes the query
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the two NON-VACUOUS regression tests (RED — must FAIL against current code)</name>
  <files>backend/tests/integration/test_116_tool_leak.py, backend/tests/integration/test_116_version_stable.py</files>
  <read_first>
    - backend/tests/integration/test_116_tool_leak.py (the existing live two-user harness: pg_pool fixture, _seed_user, _seed_global_folder, _seed_doc, _seed_relationship, _supabase_or_skip, _make_ctx, the PG_AVAILABLE skipif)
    - backend/tests/integration/test_116_version_stable.py (the existing version harness: _seed_doc_version with shared filename, user_with_versioned_doc fixture, the re-upload UPDATE-is_latest-false + INSERT-new-version mechanism)
    - backend/app/api/documents.py:540-561 (list_documents — what "readable" means: own-latest ∪ global-latest)
    - backend/app/api/documents.py:441-486 (re-upload: new uuid row, old is_latest=False, old folder_id retained)
    - backend/app/utils/folder_utils.py:48-56 (get_globally_visible_folder_ids — global = cross-user readable)
    - backend/app/services/document_relationship_service.py (the resolver these tests drive)
  </read_first>
  <behavior>
    - CR-01 test (in test_116_tool_leak.py): seed owner A with TWO versions of one filename — v1 (is_latest=False) in A's GLOBAL folder, v2 (is_latest=True) in A's PRIVATE folder (folder_id=NULL). Seed a non-owner B (no edge needed). Drive `document_relationship_service._resolve_readable_latest(v1_id, str(B), supabase=sb)` and assert it returns None (B must NOT receive v2's private id/filename/metadata). Non-vacuity guard: assert that the SAME call for owner A (who owns both versions) returns v2 (A legitimately follows forward to its own latest) — proving the test is not just "always None".
    - CR-02 test (in test_116_version_stable.py): seed user U with v1 (is_latest=True) of a filename; create a document_relationships edge keyed on v1's id (source_doc_id=v1, target=some other readable doc U owns, rel_type="references"). Re-upload: flip v1 to is_latest=False, INSERT v2 (new uuid, is_latest=True). Drive the REAL handler `_handle_get_related_documents({"document_id": v2_id}, ctx_for_U)` and assert the edge to the target still appears in the parsed result (total >= 1 and the target id/filename present in documents/source_refs). Non-vacuity guard: assert the pre-re-upload call ALSO surfaces the edge (so the test proves survival across the re-upload, not a trivially-always-present row).
  </behavior>
  <action>
    Add ONE new test to each file, reusing each file's EXISTING harness (pg_pool, the env-reading `_supabase_or_skip`, the `_seed_*` helpers, the PG_AVAILABLE skipif) — do NOT introduce a new harness or a new DSN.

    CR-01 test (test_116_tool_leak.py): add a fixture that seeds the old-global / new-private version pair for owner A (two `documents` rows sharing one filename: v1 is_latest=False folder_id=A's global folder, v2 is_latest=True folder_id=NULL), plus a non-owner B. Add `async def test_global_old_version_does_not_leak_private_latest(...)` that asserts `await document_relationship_service._resolve_readable_latest(v1_id, str(user_b), supabase=sb)` is None, AND (non-vacuity) the same call as `str(user_a)` returns the row whose id == v2_id. Teardown must be FK-safe (delete relationships → documents → folders → audit_log → auth.users per user, matching the existing fixture).

    CR-02 test (test_116_version_stable.py): extend the harness to seed a SECOND readable target doc U owns and a `document_relationships` row (use a `_seed_relationship`-style INSERT, or add one mirroring test_116_tool_leak.py's). Add `async def test_edge_survives_reupload_in_handler(...)` that: (1) before re-upload, drives `from app.services.tool_dispatcher import _handle_get_related_documents` with `{"document_id": v1_id}` and a `_make_ctx`-style ctx for U and asserts the target surfaces; (2) re-uploads (UPDATE is_latest=false WHERE user_id+filename, then INSERT v2 via `_seed_doc_version`); (3) drives the handler again with `{"document_id": v2_id}` and asserts the SAME edge still surfaces (total >= 1, target id in documents/source_refs).

    Both tests MUST fail against the current (pre-fix) code: CR-01 currently returns v2's row to B (leak), CR-02 currently returns total 0 after re-upload (orphaned). Capture that RED state — run them BEFORE Task 2/3 land and confirm the failure, per the verify command below. Do NOT weaken the assertions to make them pass; they pass only after the fixes.

    Imports stay inside the test bodies (the existing convention) so collection never errors. Keep the `_pg_reachable` 2s guard / PG_AVAILABLE skipif intact so the tests skip cleanly off-DB.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/integration/test_116_tool_leak.py::test_global_old_version_does_not_leak_private_latest tests/integration/test_116_version_stable.py::test_edge_survives_reupload_in_handler -x</automated>
    Expected at this step (BEFORE Task 2/3): both FAIL (CR-01 leaks v2 to B; CR-02 returns 0 edges after re-upload) when :54322 is reachable. If :54322 is unreachable they SKIP — note this and proceed; the RED proof is then re-confirmed in Task 4's full run after the fixes flip them GREEN.
  </verify>
  <done>Two new live regression tests exist, each with a non-vacuity guard, each demonstrably RED against the current code (or cleanly skipped off-DB). The IN-03 "static would false-green" gap is structurally closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CR-01 — gate the global-by-id leg on is_latest + add the post-follow visibility re-check</name>
  <files>backend/app/services/document_relationship_service.py</files>
  <read_first>
    - backend/app/services/document_relationship_service.py:141-186 (the `_resolve_readable_latest` by-id path — Step 1 global leg :158-164, Step 2 follow-to-latest :172-186)
    - backend/app/services/document_relationship_service.py:95-108 (the CORRECT pattern: `_latest_by_filename`'s global leg IS already `.eq("is_latest", True)`-gated — mirror it)
    - backend/app/api/documents.py:540-561 (list_documents — the canonical own-latest ∪ global-latest access model to mirror)
    - backend/app/utils/folder_utils.py:48-56 (get_globally_visible_folder_ids — global = cross-user readable)
  </read_first>
  <action>
    Two changes to `_resolve_readable_latest` (by-id path only; the own leg and `_latest_by_filename` are already correct):

    1. Global-by-id leg (lines ~158-164): add `.eq("is_latest", True)` to the `.eq("id", ...).in_("folder_id", global_folder_ids).limit(1)` query — so an OLD version sitting in a global folder is NOT independently readable, exactly mirroring `_latest_by_filename:102` and `list_documents:558`. Track whether the matched `row` came from the GLOBAL leg vs the OWN leg (e.g. a local boolean `from_global`), because the Step-2 re-check below depends on which leg matched.

    2. Step-2 follow-to-latest post-check (lines ~172-186): after following forward to the latest row (the `(user_id, filename, is_latest=True)` lookup), if the source row was matched via the GLOBAL leg, re-verify the RETURNED latest row is STILL caller-visible before returning it — i.e. its `folder_id` is still in `global_folder_ids` (reuse the `global_folder_ids` already fetched for the Step-1 global leg; do not refetch). If the latest moved OUT of the caller's global-visible set (e.g. into a private folder), return None. The OWN leg needs no re-check (the caller always owns its own follow-to-latest target — `user_id == caller` holds across versions). Note: after change 1, the global leg matches an is_latest row directly, so the `row.get("is_latest")` early-return at :172-173 will usually fire — but the re-check must STILL guard the case where the matched global is_latest row's followed-to-latest target differs; implement the re-check on the returned latest row regardless of whether Step 2 ran, scoped to the `from_global` path. Wrap any new query in `aexec` (no bare `.execute()`).

    Update the function docstring to state the invariant: "readable = own-latest ∪ global-latest; a global-leg match never launders an old version into a private latest." Fold IN-01 (the symmetric is_latest gate) into change 1 — no separate task.

    This is an additive predicate tightening on a service-role (RLS-bypassed) gate — the sole owner-scoping gate. Do NOT touch the own leg, `_latest_by_filename`, create/delete, or any other module.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/integration/test_116_tool_leak.py::test_global_old_version_does_not_leak_private_latest tests/integration/test_116_version_stable.py -x</automated>
    Expected: the CR-01 test now PASSES (B gets None; A still gets v2 — non-vacuity holds). The existing test_116_version_stable.py rows (own-leg follow-to-latest after re-upload/restore) STILL PASS (no own-leg regression).
  </verify>
  <done>The global-by-id leg is is_latest-gated; a follow-to-latest from a global match is re-verified against the caller's global-visible folder set before returning; a non-owner replaying an old-global id whose latest moved private gets None. The own-leg follow-to-latest is byte-equivalent in behavior (existing version-stable tests green).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CR-02 — enumerate edges over the subject's full version-id set + WR-02/03/04 fold-ins</name>
  <files>backend/app/services/document_relationship_service.py, backend/app/services/tool_dispatcher.py, backend/app/api/document_relationships.py</files>
  <read_first>
    - backend/app/services/tool_dispatcher.py:492-639 (`_handle_get_related_documents` — subject_id at :559, the outgoing `.eq("source_doc_id", subject_id)` :567 and incoming `.eq("target_doc_id", subject_id)` :573 to be widened to `.in_(...)`)
    - backend/app/services/document_relationship_service.py:189-243 (`create_relationship` write payload :207-212, the 23505 re-fetch, `delete_relationship` :232-243 — the WR-02 `_uid()` fold-in sites)
    - backend/app/api/document_relationships.py (the router — WR-03 "fire-and-forget" comment on the in-band `await write_audit_entry`, WR-04 self-link ordering comment)
    - backend/app/api/documents.py:441-486 (re-upload: versions share (user_id, filename); rename does not exist → (user_id, filename) is the stable lineage handle)
  </read_first>
  <action>
    CR-02 (read-side, NO migration):

    1. In `document_relationship_service.py`, add a small shared helper
       `async def _subject_version_ids(subject_row: dict, *, supabase=None) -> list[str]` that returns
       ALL `documents.id` values sharing the subject's `(user_id, filename)` — i.e.
       `documents WHERE user_id = subject["user_id"] AND filename = subject["filename"]` (every version,
       latest or not), via `aexec`. If the lookup yields nothing, fall back to `[subject["id"]]` so the
       query is never empty. This keeps the version-set logic testable and reusable.

    2. In `tool_dispatcher.py` `_handle_get_related_documents`, after resolving `subject`, call
       `version_ids = await document_relationship_service._subject_version_ids(subject, supabase=ctx.supabase)`
       and change the two edge queries from `.eq("source_doc_id", subject_id)` / `.eq("target_doc_id", subject_id)`
       to `.in_("source_doc_id", version_ids)` / `.in_("target_doc_id", version_ids)`. Keep the
       `.eq("user_id", document_relationship_service._uid(caller))` owner scope and the `select(...)` columns
       unchanged. The per-endpoint OTHER-side readability re-check (the `_append_edge` `_resolve_readable_latest`
       call) is UNCHANGED — the leak-safe mask still applies per OTHER endpoint, and CR-01's fix now hardens
       that resolver. Do NOT widen by filename across DIFFERENT documents — `version_ids` is scoped strictly to
       the subject's own (user_id, filename) lineage.

    WR-02 (defense-in-depth fold-in, document_relationship_service.py): apply `_uid(user_id)` to the
    `user_id` value in the `create_relationship` insert payload (replace `str(user_id)` at :208) and in
    `delete_relationship`'s `.eq("user_id", str(user_id))` (:241) — matching the re-fetch (:221) and the
    resolver. This adds a free malformed-uuid `ValueError` guard and makes the owner-scoping invariant
    uniform on the service-role gate.

    WR-03 (comment honesty fold-in, document_relationships.py): the router `await write_audit_entry(...)`
    is in-band (it blocks the response), not fire-and-forget. Pick the LIGHTER honest option: reword the
    misleading "fire-and-forget" comment to state it is a blocking (error-swallowing) audit write on the
    response path. Do NOT restructure to BackgroundTasks/create_task (out of scope; the inherited pattern
    matches document_views.py).

    WR-04 (comment tightening fold-in, document_relationships.py): tighten the overstated self-link
    ordering comment to: both branches return identical status+detail (no response ordering oracle); step 2
    specifically handles the readable-self-link case (it is NOT dead code). Doc-only, no code change.

    WR-01 is a verified non-defect today — do NOT change the `"23505" in str(exc)` sniff. (Explicit non-goal.)

    threads.py and any shared SSE/agent-loop path stay byte-untouched (G-5, cross-provider no-regression).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/integration/test_116_version_stable.py::test_edge_survives_reupload_in_handler tests/integration/test_116_tool_leak.py -x</automated>
    Expected: the CR-02 test now PASSES (the edge survives the re-upload and surfaces under the v2 subject); the existing tool-leak masking tests STILL PASS (per-OTHER-endpoint mask intact under the widened enumeration).
  </verify>
  <done>Edge enumeration matches against the subject's full version-id set via `.in_()` (a re-upload no longer orphans the link); `_uid()` is applied uniformly on both write paths; the WR-03/WR-04 comments are honest. CR-02's regression test is green; CR-01's masking tests remain green.</done>
</task>

<task type="auto">
  <name>Task 4: Full-suite regression gate — net-new failures = 0 + both BLOCKER tests GREEN live</name>
  <files>backend/tests/integration/test_116_tool_leak.py, backend/tests/integration/test_116_version_stable.py, backend/tests/integration/test_116_relationship_crud.py, backend/tests/integration/test_116_idempotency.py</files>
  <read_first>
    - backend/tests/integration/test_116_relationship_crud.py (the create/delete surface — must stay green after the WR-02 _uid() write-path change)
    - backend/tests/integration/test_116_idempotency.py (the 23505 re-fetch — must stay green; WR-01 untouched)
  </read_first>
  <action>
    Run the full Phase-116 test surface against live :54322 and prove no net-new failures.

    1. Confirm BOTH new BLOCKER regression tests are GREEN live (they were RED at base in Task 1):
       the CR-01 leak test returns None for the non-owner, the CR-02 re-upload test surfaces the edge.
    2. Run the whole `test_116_*` set (unit + integration) and the create/delete/idempotency/audit rows
       to confirm the WR-02 `_uid()` write-path change and the CR-02 `.in_()` widening introduced no
       regressions (idempotency re-fetch still returns the existing edge; create/delete still own-scoped).
    3. Establish the net-new-failures-from-base baseline per the project's stash-and-rerun convention
       (SEED-056): the only deltas allowed are the two BLOCKER tests flipping RED→GREEN. Any OTHER
       newly-red test is a regression to fix before completing the plan.

    If :54322 is unreachable, the live rows SKIP — record that the DB-gated proof is pending an operator
    run and DO NOT claim the BLOCKERs closed without the live GREEN. The unit rows (schema, handler stub)
    must pass regardless.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/integration/test_116_tool_leak.py tests/integration/test_116_version_stable.py tests/integration/test_116_relationship_crud.py tests/integration/test_116_idempotency.py tests/unit/test_116_tool_schema.py tests/unit/test_116_handler.py -q</automated>
    Expected: all green live on :54322 (or cleanly skipped off-DB for the integration rows). The two new BLOCKER tests PASS. No net-new failures vs base.
  </verify>
  <done>Both CR-01 and CR-02 regression tests pass live; the full Phase-116 surface is green with zero net-new failures; SC#1b (version-stable links) and SC#2b (leak-safe masking) are now observably true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent/model → `get_related_documents` tool | The model passes a `document_id`/`filename`; an attacker-aligned prompt could replay an OLD version's id to probe an owner's PRIVATE latest version. |
| non-owner caller → `_resolve_readable_latest` (service-role client) | `get_supabase()` is service-role (RLS bypassed); the app predicate is the SOLE owner-scoping gate. A global-folder old-version id is cross-user readable (folder_utils.py:48-56). |
| re-upload event → edge enumeration | Versioning inserts a new uuid row; the read-side join must still find edges keyed on prior version ids without leaking across documents. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-05-01 | Information Disclosure | `_resolve_readable_latest` global-by-id leg (`document_relationship_service.py:158-164`) | mitigate | Add `.eq("is_latest", True)` so an old version in a global folder is not independently readable — mirrors `list_documents:558`. This is the central CR-01 threat. |
| T-116-05-02 | Information Disclosure | follow-to-latest step (`document_relationship_service.py:172-186`) | mitigate | After following from a GLOBAL-leg match, re-verify the returned latest row's `folder_id` is still in `global_folder_ids` (or owner==caller); return None if the latest moved private. Prevents laundering a private latest via an old global id. |
| T-116-05-03 | Information Disclosure | the mitigation's non-vacuity (the 113/115 "live leak proof, not RLS-label" lesson) | mitigate | Ship a LIVE two-version regression test seeding old-global/new-private and asserting the non-owner gets None while the owner gets the latest — proves the mitigation is non-vacuous, not a static label. |
| T-116-05-04 | Tampering / Integrity | edge enumeration over a single id (`tool_dispatcher.py:567,573`) | mitigate | Widen to `.in_(version_ids)` over the subject's full `(user_id, filename)` version set; scoped strictly to the subject's lineage (no cross-document widening), so a re-upload does not orphan a link AND cannot enumerate another document's edges. |
| T-116-05-05 | Spoofing / Elevation | `user_id` interpolated into PostgREST `.eq()` on the service-role gate (`document_relationship_service.py:208,241`) | mitigate | Apply `_uid(user_id)` uniformly on both write paths (WR-02) — a malformed value raises `ValueError` instead of risking a broken `user_id` predicate; makes the owner-scoping invariant auditable. |
| T-116-05-06 | Denial of Service | in-band audit `await` on the create/delete response path (`document_relationships.py`) | accept | Inherited pattern (document_views.py shares it); not a 116 regression. WR-03 reduces it to a comment-honesty fix; full decoupling is out of scope. |
| T-116-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed (read-side fixes + tests reuse asyncpg/supabase/pytest already in the venv). No package-legitimacy gate required. |
</threat_model>

<verification>
- CR-01 closed: `_resolve_readable_latest` global-by-id leg is `is_latest`-gated and the follow-to-latest is folder/owner re-checked; the live two-user old-global/new-private test returns None for the non-owner (and the latest for the owner — non-vacuous).
- CR-02 closed: edge enumeration uses `.in_()` over the subject's full version-id set; the live re-upload test surfaces the edge under the new latest subject id.
- Both regression tests demonstrably FAIL against the pre-fix code and PASS after (the IN-03 non-vacuity gap closed).
- WR-02 (`_uid()` on write paths), WR-03 (honest audit comment), WR-04 (tightened self-link comment) folded; WR-01 explicitly untouched.
- `threads.py` and shared SSE/agent-loop paths byte-untouched (G-5). No new migration. No new package installs.
- Full Phase-116 suite green live on :54322 with zero net-new failures.
</verification>

<success_criteria>
- SC#1b (version-stable links — links reference identity via latest-resolved/is_latest so a re-upload/restore does not orphan them; REL-01 / LOCKED D-116-1) observably TRUE: the edge survives a re-upload in `get_related_documents`.
- SC#2b (a relationship pointing at a document the caller can't see renders as "linked document (no access)" — never leaking title/metadata; REL-04) observably TRUE: the resolver returns None across the global→private follow-to-latest boundary, so the D-116-9 mask fires.
- Both BLOCKER regression tests pass live; no net-new failures; the three nit fold-ins applied; WR-01 not worked.
</success_criteria>

<output>
Create `.planning/phases/116-document-relationships-backend-agent-tool/116-05-SUMMARY.md` when done.
</output>
