---
phase: 116-document-relationships-backend-agent-tool
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/document_relationship.py
  - backend/app/services/document_relationship_service.py
  - supabase/migrations/075_document_relationships_idempotency_index.sql
  - backend/tests/unit/test_116_tool_schema.py
  - backend/tests/unit/test_116_tool_wiring.py
  - backend/tests/unit/test_116_handler.py
  - backend/tests/unit/test_116_whitelist_guard.py
  - backend/tests/integration/test_116_relationship_crud.py
  - backend/tests/integration/test_116_idempotency.py
  - backend/tests/integration/test_116_audit_live.py
  - backend/tests/integration/test_116_version_stable.py
  - backend/tests/integration/test_116_tool_read.py
  - backend/tests/integration/test_116_tool_leak.py
autonomous: true
requirements: [REL-01, REL-03, REL-04]

must_haves:
  truths:
    - "The 10 Wave-0 test files exist and the suite exits 0 (not-yet-built behaviors xfail-guarded)"
    - "A shared FastAPI-free `_resolve_readable_latest(doc_id_or_filename, caller)` resolves a document to its latest accessible version (own-or-global, exact filename) — callable in-process by both the create gate and the read tool"
    - "`RelationshipCreate.rel_type` is a Pydantic `Literal` over the 4 types — a forged type fails at parse (422)"
    - "Migration 075 file exists with the additive partial unique index on (user_id, source_doc_id, target_doc_id, rel_type) — NOT applied (apply is Plan 04 BLOCKING)"
    - "The service's `create_relationship` catches the 23505 unique-violation and returns the existing edge (idempotent)"
    - "`delete_relationship` is own-scoped (`.eq(\"user_id\", caller)`) returning False on a cross-user miss"
  artifacts:
    - path: "backend/app/models/document_relationship.py"
      provides: "RelationshipCreate (Literal rel_type) + RelationshipResponse"
      contains: "Literal"
    - path: "backend/app/services/document_relationship_service.py"
      provides: "create/delete + _resolve_readable_latest + _uid guard + 23505-catch idempotency"
      contains: "_resolve_readable_latest"
    - path: "supabase/migrations/075_document_relationships_idempotency_index.sql"
      provides: "Additive partial unique index for idempotency (D-116-6)"
      contains: "CREATE UNIQUE INDEX"
  key_links:
    - from: "backend/app/services/document_relationship_service.py"
      to: "document_relationships table"
      via: "aexec(client.table('document_relationships')...)"
      pattern: "document_relationships"
    - from: "backend/app/services/document_relationship_service.py"
      to: "documents (own-or-global latest)"
      via: "is_latest=True own + global-folder query"
      pattern: "is_latest"
---

<objective>
Land the Phase 116 substrate: the request/response models, the data-access service (including the shared leak-safe latest-version resolver consumed by BOTH the write gate and the read tool), the additive idempotency index migration FILE, and the 10 Wave-0 test scaffolds.

Purpose: Define the contracts and shared core first (interface-first ordering) so Plan 02 (REST surface) and Plan 03 (agent tool) build against a single resolver with no fork — the 115 "one core, no fork" precedent (RESEARCH Open Question 2).
Output: `models/document_relationship.py`, `services/document_relationship_service.py`, `supabase/migrations/075_*.sql` (file only), 10 `test_116_*` files.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-document-relationships-backend-agent-tool/116-CONTEXT.md
@.planning/phases/116-document-relationships-backend-agent-tool/116-RESEARCH.md
@.planning/phases/116-document-relationships-backend-agent-tool/116-PATTERNS.md
@.planning/phases/116-document-relationships-backend-agent-tool/116-VALIDATION.md

<interfaces>
<!-- Extracted from the codebase this session. Build against these directly — no exploration needed. -->

The clone target for the service — `backend/app/services/document_view_service.py:47-192`:
- `_client(supabase)` → returns the passed client or `get_supabase()` (the service-role client; RLS bypassed → app predicates are the SOLE owner-scoping gate).
- `_uid(user_id) -> str` → `return str(UUID(str(user_id)))`. The PostgREST `.or_()`-injection guard (WR-02). Copy VERBATIM (`from uuid import UUID`).
- `aexec(client.table(...)...)` from `app.utils.db` wraps every supabase-py `.execute()` (D-v2.5-01). NEVER bare `.execute()` in an async function.
- `delete_view(user_id, view_id, supabase)` → `aexec(client.table(_TABLE).delete().eq("id", view_id).eq("user_id", str(user_id)))`; `return bool(result.data)` (False on cross-user miss → router 404).

The own-or-global latest pattern to mirror — `backend/app/api/documents.py:535-561`:
- own leg: `documents WHERE user_id = caller AND is_latest = True`
- global leg: `folder_id IN get_globally_visible_folder_ids(supabase, caller) AND is_latest = True`
- `from app.<...> import get_globally_visible_folder_ids` (the live `GET /documents` access model).

The 23505-catch precedent — `backend/app/api/documents.py:491-505`: supabase-py surfaces the unique-violation as an exception whose `str(exc)` contains `"23505"`.

The model clone target — `backend/app/models/document_view.py:34-91`: `op: Literal[...]` discriminator → ValidationError at parse → 422.

The audit writer — `backend/app/services/audit_service.py:57` `write_audit_entry(user_id, action_type, metadata, supabase)` (swallows errors). `relationship.create`/`relationship.delete` already in `VALID_ACTION_TYPES` (`:22`) + the live CHECK (migration 071:33) — NO migration, NO frozenset-sync (D-116-12).

The table (live, migration 071:57-69): `document_relationships(id, user_id, org_id, source_doc_id FK CASCADE, target_doc_id FK CASCADE, rel_type, created_at)`; `no_self_rel` CHECK; rel_type CHECK over the 4 types; NO unique constraint today; RLS user-scoped only (no is_global, :142-150).

The leak-proof clone target — `backend/tests/integration/test_115_tool_global_leak.py`: `_POSTGRES_TEST_DSN`, `_pg_reachable()` 2s-timeout guard, `_check_pg_available_sync()`, imports INSIDE test bodies so collection never errors on the not-yet-built handler.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 10 Wave-0 test scaffolds + migration 075 file</name>
  <files>backend/tests/unit/test_116_tool_schema.py, backend/tests/unit/test_116_tool_wiring.py, backend/tests/unit/test_116_handler.py, backend/tests/unit/test_116_whitelist_guard.py, backend/tests/integration/test_116_relationship_crud.py, backend/tests/integration/test_116_idempotency.py, backend/tests/integration/test_116_audit_live.py, backend/tests/integration/test_116_version_stable.py, backend/tests/integration/test_116_tool_read.py, backend/tests/integration/test_116_tool_leak.py, supabase/migrations/075_document_relationships_idempotency_index.sql</files>
  <read_first>
    - backend/tests/integration/test_115_tool_global_leak.py (the leak-proof + _pg_reachable guard template to clone)
    - backend/tests/unit/test_116_*.py (none exist yet — these are the files to create)
    - supabase/migrations/074_view_typed_columns.sql (confirms 074 = highest; 075 is next)
    - supabase/migrations/071_dm_foundations.sql:57-69 (the table the index targets)
    - .planning/phases/116-document-relationships-backend-agent-tool/116-VALIDATION.md (§"Wave 0 Requirements" — the exact 10 filenames + each file's assertion)
  </read_first>
  <action>
    Create all 10 Wave-0 test files named EXACTLY per 116-VALIDATION.md §"Wave 0 Requirements":
    4 unit (`test_116_tool_schema.py`, `test_116_tool_wiring.py`, `test_116_handler.py`, `test_116_whitelist_guard.py`)
    + 6 integration (`test_116_relationship_crud.py`, `test_116_idempotency.py`, `test_116_audit_live.py`, `test_116_version_stable.py`, `test_116_tool_read.py`, `test_116_tool_leak.py`).
    Mark every not-yet-built behavior with `@pytest.mark.xfail(strict=False)` so the suite exits 0 (the 111-01/102-01 scaffold convention). Put all imports of not-yet-built symbols INSIDE the test bodies (the 115 collection-safety pattern). Integration tests gate on a cloned `_pg_reachable()` 2s-timeout guard and skip cleanly when `:54322` is unreachable.
    `test_116_tool_schema.py` MUST assert the schema-shape invariants now (they become GREEN in Plan 03): the `get_related_documents` params contain NO `anyOf`, NO `oneOf`, and NO property whose `type` is a list (multi-type array) — recurse the params dict; each of `document_id`/`filename` has `type == "string"`.
    `test_116_tool_leak.py` is the NON-vacuous two-user proof scaffold (clone `test_115_tool_global_leak.py`): seed a target visible to A, link it, make it unseeable to B, assert B sees `"linked document (no access)"` and never the target's real filename/metadata; assert A sees the real filename. Encode the seed/link/unsee/assert structure as xfail until the handler ships in Plan 03.
    Create `supabase/migrations/075_document_relationships_idempotency_index.sql` (FILE ONLY — do NOT apply; Plan 04 applies it): a header comment naming Phase 116 / REL-01 / D-116-6 and the manual-apply rule, then `CREATE UNIQUE INDEX IF NOT EXISTS document_relationships_idempotency_idx ON public.document_relationships (user_id, source_doc_id, target_doc_id, rel_type);`. Do NOT touch `supabase/full-schema.sql` (Plan 04 regenerates it).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_116_*.py tests/integration/test_116_*.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All 10 files exist (`ls backend/tests/unit/test_116_*.py backend/tests/integration/test_116_*.py` → 10 files); filenames match 116-VALIDATION.md verbatim.
    - The combined `test_116_*` suite exits 0 (xfail/xpass/skip only — no hard failures, no collection errors).
    - `test_116_tool_schema.py` contains an assertion that recursively rejects `anyOf`/`oneOf`/list-valued `type` in the tool params (the a5b0b917 Gemini trap).
    - `supabase/migrations/075_document_relationships_idempotency_index.sql` exists and contains `CREATE UNIQUE INDEX` over `(user_id, source_doc_id, target_doc_id, rel_type)`; `git status` shows `supabase/full-schema.sql` UNCHANGED.
  </acceptance_criteria>
  <done>10 Wave-0 test files created (suite exits 0, no collection errors), migration 075 file authored (un-applied), full-schema.sql untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Models + service (clone document_view_service) with shared resolver + idempotent create + own-scoped delete</name>
  <files>backend/app/models/document_relationship.py, backend/app/services/document_relationship_service.py, backend/tests/unit/test_116_handler.py</files>
  <read_first>
    - backend/app/services/document_view_service.py:47-192 (the EXACT clone target: _client, _uid guard, create_view, get_view own-or-global, delete_view own-scoped)
    - backend/app/models/document_view.py:34-91 (the Literal-discriminator model pattern)
    - backend/app/api/documents.py:441-505 (version-create cascade + the 23505-catch precedent), :535-561 (own ∪ global latest)
    - backend/app/services/retrieval_service.py:189-213 (the ANTI-PATTERN — own-only + partial ilike; do NOT reuse)
    - supabase/migrations/071_dm_foundations.sql:57-69,142-150 (table + user-scoped RLS)
    - backend/app/utils/db.py (the aexec wrapper)
  </read_first>
  <behavior>
    - `RelationshipCreate(source_doc_id, target_doc_id, rel_type)` with `rel_type: Literal["supersedes","amends","references","attached_to"]` → a 5th value raises ValidationError at parse.
    - `_resolve_readable_latest("<bad-id>", caller)` → None (unresolvable); a real own doc id → the latest-version row readable by caller; an exact own-or-global filename → its latest accessible version; a partial/superstring filename → None (EXACT match only, per D-116-3).
    - `create_relationship(...)` first call → inserts and returns the row; second identical `(source,target,rel_type)` call → returns the SAME existing row (23505 caught), never a duplicate, never an error.
    - `delete_relationship(caller, rel_id)` → True when the caller owns the row; False on a cross-user/absent id (own-scoped).
  </behavior>
  <action>
    Create `backend/app/models/document_relationship.py`: `RelationshipCreate` (`source_doc_id: str`, `target_doc_id: str`, `rel_type: Literal["supersedes","amends","references","attached_to"]` — mirrors the DB CHECK at migration 071:68; the DB CHECK is defense-in-depth) and `RelationshipResponse` (`id, user_id, source_doc_id, target_doc_id, rel_type, created_at` — nullable where the view-response analog is). Model the inverse-label map intent in a docstring (source=the attachment, target=the parent for `attached_to`) so Phase 117 + the agent agree on directionality.
    Create `backend/app/services/document_relationship_service.py` cloning `document_view_service.py`: copy `_client`, `_uid` (VERBATIM, `from uuid import UUID`), `_TABLE = "document_relationships"`. Implement:
    1. `_resolve_readable_latest(doc_id_or_filename, caller, *, by_filename=False, supabase=None)` — the SHARED FastAPI-free helper (RESEARCH Open Question 2): given a `document_id`, verify the caller can read it (own `is_latest=True` ∪ global-folder `is_latest=True`, mirroring documents.py:535-561) and map it to its latest version via `(user_id, filename, is_latest=True)`; given an EXACT filename (case-insensitive), resolve caller-scoped → latest accessible. Return the latest doc row or None. Use `get_globally_visible_folder_ids`. Do NOT reuse `resolve_document_id` (own-only + partial-match — the documented anti-pattern). Wrap every `.execute()` in `aexec`.
    2. `create_relationship(user_id, source_doc_id, target_doc_id, rel_type, supabase=None)` — SELECT-then-INSERT; on the INSERT catch any exception whose `str(exc)` contains `"23505"`, RE-FETCH the existing edge (`.eq("user_id", _uid(uid)).eq("source_doc_id", src).eq("target_doc_id", tgt).eq("rel_type", rt)`) and return it (D-116-6 idempotent — the documents.py:491-505 pattern, but returning existing instead of raising 409). Re-raise non-23505 exceptions.
    3. `delete_relationship(user_id, rel_id, supabase=None)` → `aexec(client.table(_TABLE).delete().eq("id", rel_id).eq("user_id", str(user_id)))`; `return bool(result.data)` (own-scoped; False → router 404).
    Un-xfail the model/service-level assertions in `test_116_handler.py` that cover the resolver + idempotency + delete shape (the schema/wiring/leak rows stay xfail until Plan 03).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_116_handler.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from app.models.document_relationship import RelationshipCreate; RelationshipCreate(source_doc_id='a',target_doc_id='b',rel_type='nope')"` raises `ValidationError` (forged rel_type rejected at parse).
    - `grep -n "_resolve_readable_latest" backend/app/services/document_relationship_service.py` → present; `grep -n "resolve_document_id" backend/app/services/document_relationship_service.py` → absent (anti-pattern not reused).
    - `grep -n "23505" backend/app/services/document_relationship_service.py` → present in `create_relationship` (idempotency catch).
    - `grep -c '\.execute()' backend/app/services/document_relationship_service.py` → 0 bare `.execute()` outside `aexec` (D-v2.5-01); `_uid` is present verbatim.
    - `test_116_handler.py` resolver/idempotency/delete assertions pass (no longer xfail); net-new failures in the wider unit suite = 0 (base-checkout proof).
  </acceptance_criteria>
  <done>Models + service shipped; `_resolve_readable_latest` shared helper, 23505-catch idempotency, own-scoped delete all behavior-tested; `_uid` guard verbatim; no bare `.execute()`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 117 panel / API client → REST router → service | Untrusted `source_doc_id`/`target_doc_id`/`rel_type` cross here |
| service-role supabase client | RLS bypassed — the service's app predicates are the SOLE owner-scoping gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-01-01 | Tampering | `rel_type` from the create body | mitigate | Pydantic `Literal["supersedes","amends","references","attached_to"]` → parse-time 422; DB CHECK (migration 071:68) defense-in-depth |
| T-116-01-02 | Tampering / Elevation | `user_id` interpolated into PostgREST `.or_()`/`.eq()` in the service | mitigate | `_uid()` UUID-coercion guard copied verbatim from `document_view_service.py:51` |
| T-116-01-03 | Tampering (data integrity) | duplicate edges under a TOCTOU race | mitigate | additive partial unique index (migration 075) + the 23505-catch upsert that returns the existing edge; the index is the only race-immune guarantee |
| T-116-01-04 | Information Disclosure | exact-filename resolver as a probe surface | mitigate | EXACT (not partial `ilike`) own-or-global match; `resolve_document_id` (own-only + partial) explicitly NOT reused |
| T-116-01-SC | Tampering | npm/pip installs | accept | No package installs this phase (zero net-new deps — RESEARCH §"Package Legitimacy Audit": N/A); nothing to slopcheck |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/unit/test_116_*.py tests/integration/test_116_*.py -q` → suite exits 0 (xfail/skip-guarded for not-yet-built behavior).
- Migration 075 file present, un-applied; `full-schema.sql` untouched.
- Net-new failures in the wider unit suite = 0 (SEED-056 base-checkout proof).
</verification>

<success_criteria>
- 10 Wave-0 test files created (suite exits 0).
- `RelationshipCreate.rel_type` is a `Literal`; forged types rejected at parse.
- `_resolve_readable_latest` shared helper exists (own-or-global, exact filename, latest-version).
- `create_relationship` is idempotent via 23505-catch; `delete_relationship` is own-scoped.
- Migration 075 authored (un-applied).
</success_criteria>

<output>
Create `.planning/phases/116-document-relationships-backend-agent-tool/116-01-SUMMARY.md` when done.
</output>
