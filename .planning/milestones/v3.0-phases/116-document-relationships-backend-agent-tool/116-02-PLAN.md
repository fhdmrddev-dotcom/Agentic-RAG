---
phase: 116-document-relationships-backend-agent-tool
plan: 02
type: execute
wave: 2
depends_on: ["116-01"]
files_modified:
  - backend/app/api/document_relationships.py
  - backend/app/main.py
  - backend/tests/integration/test_116_relationship_crud.py
  - backend/tests/integration/test_116_idempotency.py
  - backend/tests/integration/test_116_audit_live.py
  - backend/tests/integration/test_116_version_stable.py
autonomous: true
requirements: [REL-01, REL-03]

must_haves:
  truths:
    - "POST /document-relationships creates a typed link between two caller-readable documents and returns it (201)"
    - "An unseeable source OR target endpoint is rejected with a uniform 422 (no probe-by-link existence oracle); the readability check runs BEFORE the self-link/CHECK gate (no ordering oracle)"
    - "A self-link is rejected (422)"
    - "Creating the same (source,target,rel_type) twice returns the existing edge (200/201) — one row, no 409"
    - "DELETE /document-relationships/{id} removes an owned edge; a cross-user/absent id returns 404 (never 403)"
    - "A relationship.create audit row lands LIVE on create; a relationship.delete row lands LIVE on remove"
    - "After a re-upload (new version) OR a restore, the link still resolves to the LATEST version (read-time resolution)"
  artifacts:
    - path: "backend/app/api/document_relationships.py"
      provides: "POST + DELETE typed-link CRUD router with visible-both gate + audit"
      exports: ["router"]
    - path: "backend/app/main.py"
      provides: "document_relationships.router mounted"
      contains: "document_relationships"
  key_links:
    - from: "backend/app/api/document_relationships.py"
      to: "document_relationship_service (resolver + create/delete)"
      via: "service import"
      pattern: "document_relationship_service"
    - from: "backend/app/api/document_relationships.py"
      to: "audit_log"
      via: "write_audit_entry(relationship.create / relationship.delete)"
      pattern: "relationship\\.(create|delete)"
    - from: "backend/app/main.py"
      to: "document_relationships.router"
      via: "include_router"
      pattern: "include_router\\(document_relationships"
---

<objective>
Ship the REST write surface (REL-01 create, REL-03 remove) the Phase 117 panel consumes: `POST /document-relationships` (visible-both gate → idempotent persist → `relationship.create` audit) and `DELETE /document-relationships/{id}` (own-scoped → `relationship.delete` audit). Mount the router in `main.py`.

Purpose: Humans curate the relationship graph via the API (D-116-8); SC#1 (create/remove + live audit + version-stable) lands here.
Output: `api/document_relationships.py`, `main.py` mount, 4 live integration tests un-marked.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-document-relationships-backend-agent-tool/116-CONTEXT.md
@.planning/phases/116-document-relationships-backend-agent-tool/116-RESEARCH.md
@.planning/phases/116-document-relationships-backend-agent-tool/116-PATTERNS.md

<interfaces>
<!-- From Plan 01 (this plan's dependency) + the codebase. -->

From `backend/app/services/document_relationship_service.py` (Plan 01):
- `_resolve_readable_latest(doc_id_or_filename, caller, *, by_filename=False, supabase=None)` → latest readable doc row or None.
- `create_relationship(user_id, source_doc_id, target_doc_id, rel_type, supabase=None)` → dict (existing-or-new; idempotent via 23505-catch).
- `delete_relationship(user_id, rel_id, supabase=None)` → bool (own-scoped; False → 404).

From `backend/app/models/document_relationship.py` (Plan 01): `RelationshipCreate`, `RelationshipResponse`.

The CRUD route clone target — `backend/app/api/document_views.py:74-199`:
- `router = APIRouter(prefix="/document-views", tags=["document-views"])` → use `prefix="/document-relationships"`.
- `create_view` (`:94-135`): validation gate → `service.create_view(...)` → `await write_audit_entry(user_id, action_type="view.create", metadata={...}, supabase)` → return.
- `update_view` (`:154-162`): the EXPLICIT "ownership BEFORE validation, no 422-vs-404 ordering oracle" pattern — mirror this ordering for the visible-both gate.
- `delete_view` (`:188-199`): `if not removed: raise HTTPException(status_code=404, detail="...")  # NEVER 403`.
- DI: `current_user: dict = Depends(get_current_user)`, `supabase: Client = Depends(get_supabase)`.

The audit writer — `backend/app/services/audit_service.py:57` `write_audit_entry(user_id, action_type, metadata, supabase)` (swallows errors). `relationship.create`/`relationship.delete` already in the live enum (D-116-12) — NO migration.

The router-mount site — `backend/app/main.py`: `from app.api import (..., document_views, ...)` then `app.include_router(document_views.router)` (the `document_views` mount is the exact placement analog).

The version model for the version-stable test — `backend/app/api/documents.py:441-486` (re-upload INSERTs a new id, flips old `is_latest=False`), `:628-669` (restore).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: POST + DELETE router with visible-both gate, idempotency, and relationship audit; mount in main.py</name>
  <files>backend/app/api/document_relationships.py, backend/app/main.py, backend/tests/integration/test_116_relationship_crud.py, backend/tests/integration/test_116_idempotency.py, backend/tests/integration/test_116_audit_live.py, backend/tests/integration/test_116_version_stable.py</files>
  <read_first>
    - backend/app/api/document_views.py:74-199 (the CRUD clone target: create_view, the ownership-before-validation ordering at :154-162, delete_view 404-not-403)
    - backend/app/services/document_relationship_service.py (Plan 01 — _resolve_readable_latest, create_relationship, delete_relationship)
    - backend/app/models/document_relationship.py (Plan 01 — RelationshipCreate/Response)
    - backend/app/services/audit_service.py:13-26,57-75 (VALID_ACTION_TYPES has relationship.create/delete; write_audit_entry swallows)
    - backend/app/main.py (the document_views.router import + include_router placement)
    - supabase/migrations/071_dm_foundations.sql:57-69,142-150 (no_self_rel CHECK; user-scoped RLS)
  </read_first>
  <behavior>
    - POST with two caller-readable doc ids + a valid rel_type → 201 with the RelationshipResponse.
    - POST where the source OR target is a doc the caller cannot see → 422 (uniform; the readability check fires BEFORE the self-link/CHECK gate so an unseeable id and a self-link collapse to the same code — no ordering oracle).
    - POST source == target → 422 (self-link).
    - POST the same (source,target,rel_type) twice → second returns the existing edge (one row, no 409, no duplicate).
    - DELETE an owned relationship id → 204; DELETE a cross-user/absent id → 404 (never 403).
    - A relationship.create row lands live on create; a relationship.delete row lands live on delete.
  </behavior>
  <action>
    Create `backend/app/api/document_relationships.py` cloning `document_views.py` structure: `router = APIRouter(prefix="/document-relationships", tags=["document-relationships"])`, importing `RelationshipCreate`/`RelationshipResponse`, `document_relationship_service`, and `write_audit_entry`.
    `POST ""` (`response_model=RelationshipResponse`, `status_code=201`): (1) the VISIBLE-BOTH gate (D-116-5) — call `_resolve_readable_latest` on BOTH `body.source_doc_id` and `body.target_doc_id`; if EITHER returns None raise `HTTPException(422)` with a uniform detail (no distinguishing which endpoint, no 404-vs-422 oracle — mirror the `document_views.py:154-162` ownership-before-validation ordering, run this gate BEFORE any self-link/CHECK consideration — Pitfall 4). (2) Persist via `document_relationship_service.create_relationship(...)`; a `no_self_rel` CHECK violation that surfaces (self-link) maps to 422. (3) `await write_audit_entry(user_id=current_user["id"], action_type="relationship.create", metadata={"relationship_id": created["id"], "source_doc_id": ..., "target_doc_id": ..., "rel_type": ...}, supabase=supabase)`. Return the created/existing edge.
    `DELETE "/{relationship_id}"` (`status_code=204`): `removed = await document_relationship_service.delete_relationship(current_user["id"], relationship_id, supabase=supabase)`; `if not removed: raise HTTPException(status_code=404, detail="Relationship not found")  # NEVER 403`. ADD the `relationship.delete` audit-spawn AFTER a successful delete (D-116-12 — document_views DELETE has no audit; 116 must add it).
    Store creation-time ids (per D-116-1a mechanism (a) read-time resolution — the stored `source_doc_id`/`target_doc_id` are the creation-time ids; follow-to-latest happens at READ time in the resolver, not on write). Do NOT touch `documents.py` (no re-point hook — mechanism (a)).
    Mount in `main.py`: add `document_relationships` to the `from app.api import (...)` line and `app.include_router(document_relationships.router)` immediately after the `document_views.router` mount, with a Phase 116 REL-01/03 comment.
    Un-xfail the relevant rows in `test_116_relationship_crud.py`, `test_116_idempotency.py`, `test_116_audit_live.py`, `test_116_version_stable.py` (these exercise the live route against `:54322`; the idempotency UNIQUENESS assertion stays xfail/skip-guarded until the index is applied in Plan 04 — but the app-code-only "returns existing" behavior un-marks here).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/integration/test_116_relationship_crud.py tests/integration/test_116_audit_live.py tests/integration/test_116_version_stable.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "include_router(document_relationships" backend/app/main.py` → present; `document_relationships` in the `from app.api import` line.
    - `grep -n "relationship.create" backend/app/api/document_relationships.py` AND `grep -n "relationship.delete" backend/app/api/document_relationships.py` → both present.
    - `grep -n "status_code=404" backend/app/api/document_relationships.py` → present on DELETE (404-not-403, no `403` literal anywhere in the file: `grep -c "403" backend/app/api/document_relationships.py` → 0).
    - The visible-both gate calls `_resolve_readable_latest` on BOTH endpoints before persisting (`grep -c "_resolve_readable_latest" backend/app/api/document_relationships.py` → ≥ 2, or one call per endpoint).
    - `backend/app/api/threads.py` is NOT modified (`git status` shows it absent from the diff — G-5).
    - `test_116_relationship_crud.py` (create + visible-both 422 + self-link 422 + DELETE 404), `test_116_audit_live.py` (relationship.create row lands), `test_116_version_stable.py` (re-upload/restore → follows latest) pass live on `:54322` (or skip cleanly when unreachable).
  </acceptance_criteria>
  <done>POST/DELETE router shipped + mounted; visible-both gate (uniform 422, no ordering oracle), own-scoped DELETE 404-not-403, relationship.create/delete audits landing live, version-stable read-time resolution proven; threads.py untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 117 panel / API client → POST/DELETE router | Untrusted endpoint ids + rel_type cross here |
| router → service-role supabase | RLS bypassed; the visible-both gate + own-scoped delete are the owner-scoping gates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-02-01 | Information Disclosure | probe-by-link (create a link to an id you can't see → existence oracle) | mitigate | Visible-both gate (D-116-5): `_resolve_readable_latest` on BOTH endpoints; an unseeable endpoint → uniform 422; readability check BEFORE the self-link/CHECK gate (no ordering oracle — mirrors `document_views.py:154-162`) |
| T-116-02-02 | Tampering | cross-user delete (remove another user's edge) | mitigate | own-scoped `delete_relationship` (`.eq("user_id", caller)`) → False → 404-not-403 (no existence leak, no 403 literal) |
| T-116-02-03 | Tampering | forged/unknown `rel_type` | mitigate | Pydantic `Literal` (Plan 01) → 422 at parse; DB `rel_type` CHECK defense-in-depth |
| T-116-02-04 | Repudiation | a create/remove with no governance trail | mitigate | fire-and-forget `relationship.create`/`relationship.delete` (already in the live enum + boot drift-guard); SC#1 requires the create row to land LIVE (the swallow-on-failure means the live round-trip is the proof) |
| T-116-02-05 | Tampering (data integrity) | duplicate edges under double-click | mitigate | app-code SELECT-then-INSERT returns existing; the race-immune guarantee is the additive index (Plan 01 file / Plan 04 apply) |
| T-116-02-SC | Tampering | npm/pip installs | accept | No package installs (zero net-new deps — RESEARCH §"Package Legitimacy Audit": N/A) |
</threat_model>

<verification>
- POST creates between two readable docs; rejects an unseeable endpoint (422); rejects self-link (422); idempotent on duplicate.
- DELETE own-scoped; 404-not-403.
- `relationship.create` + `relationship.delete` rows land live.
- Re-upload + restore → link follows latest.
- `threads.py` untouched (G-5).
</verification>

<success_criteria>
- `POST /document-relationships` + `DELETE /document-relationships/{id}` shipped and mounted.
- Visible-both gate is a uniform 422 with no ordering oracle; DELETE is 404-not-403.
- Both audit rows land live; version-stable read-time resolution proven.
</success_criteria>

<output>
Create `.planning/phases/116-document-relationships-backend-agent-tool/116-02-SUMMARY.md` when done.
</output>
