---
phase: 116-document-relationships-backend-agent-tool
plan: 04
type: execute
wave: 3
depends_on: ["116-01", "116-02"]
files_modified:
  - supabase/full-schema.sql
  - backend/tests/integration/test_116_idempotency.py
autonomous: false
requirements: [REL-01]

must_haves:
  truths:
    - "Migration 075 (the additive partial unique index) is applied to the live local DB :54322 — NEVER via `supabase db push`/`db reset` (psycopg2-direct or SQL-editor paste; dev data preserved)"
    - "The unique index document_relationships_idempotency_idx exists on the live DB over (user_id, source_doc_id, target_doc_id, rel_type)"
    - "supabase/full-schema.sql is regenerated via the no-reset live dump and reflects the new index"
    - "The race-immune idempotency assertion in test_116_idempotency.py is un-marked and green LIVE (a duplicate insert hits the 23505 and returns the existing edge — exactly one row)"
  artifacts:
    - path: "supabase/full-schema.sql"
      provides: "Regenerated bootstrap artifact reflecting the 075 index"
      contains: "document_relationships_idempotency_idx"
  key_links:
    - from: "supabase/migrations/075_document_relationships_idempotency_index.sql"
      to: "live DB :54322"
      via: "manual apply (psycopg2-direct / SQL editor)"
      pattern: "document_relationships_idempotency_idx"
---

<objective>
Cross migration 075 (the additive idempotency unique index, authored in Plan 01) into the live local DB, regenerate `full-schema.sql`, and un-mark the race-immune half of the idempotency test. This is the BLOCKING operator gate — the index is the only race-immune idempotency guarantee (the app-code 23505-catch needs the constraint to throw).

Purpose: Without the index, the app-code SELECT-then-INSERT has a TOCTOU race (Pitfall 3) → duplicate edges under concurrency. SC#1 idempotency is only proven once the index is live.
Output: migration 075 applied live, `full-schema.sql` regenerated, `test_116_idempotency.py` race-immune assertion green.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-document-relationships-backend-agent-tool/116-CONTEXT.md
@.planning/phases/116-document-relationships-backend-agent-tool/116-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- The migration-apply discipline (CLAUDE.md, MANDATORY) -->

The migration file (Plan 01): `supabase/migrations/075_document_relationships_idempotency_index.sql` — `CREATE UNIQUE INDEX IF NOT EXISTS document_relationships_idempotency_idx ON public.document_relationships (user_id, source_doc_id, target_doc_id, rel_type);` (idempotent — re-apply is a no-op).

The apply precedent (Phase 111 migration 073 / Phase 100 migration 068 / Phase 102 migration 070): apply by pasting into the Supabase SQL editor OR psycopg2-direct (e.g. a `scripts/apply_migration_075.py` mirroring `scripts/apply_migration_072.py`). NEVER `supabase db push` / `db reset` — preserves dev data (the CLAUDE.md hard rule). The local stack is up (`:54322`), so the autonomous psycopg2-direct path is available.

Regenerate the bootstrap artifact: `bash scripts/regenerate-full-schema.sh` (no `--reset` — the default no-reset live dump). Never hand-edit `full-schema.sql`.

The idempotency test (Plan 02 authored it; the app-code "returns existing" behavior un-marked in Plan 02; the RACE-IMMUNE / unique-violation assertion un-marks HERE once the index is live).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply migration 075 to :54322 (psycopg2-direct, no reset) + regenerate full-schema.sql + un-mark the race-immune idempotency assertion</name>
  <files>supabase/full-schema.sql, backend/tests/integration/test_116_idempotency.py</files>
  <read_first>
    - supabase/migrations/075_document_relationships_idempotency_index.sql (Plan 01 — the index to apply)
    - scripts/apply_migration_072.py (the psycopg2-direct apply precedent — idempotent, read-back assertion, no reset)
    - scripts/regenerate-full-schema.sh (the no-reset dump)
    - backend/tests/integration/test_116_idempotency.py (Plan 02 — the race-immune assertion to un-mark)
    - CLAUDE.md §"Local dev infrastructure" + the migration rule (NEVER db push/reset)
  </read_first>
  <action>
    Apply migration 075 to the live local DB `:54322` via psycopg2-direct (mirror `scripts/apply_migration_072.py` — a small idempotent apply script or an inline psycopg2 call), in one transaction. NEVER `supabase db push` / `db reset`. After apply, read back from `pg_indexes` that `document_relationships_idempotency_idx` exists; assert existing `document_relationships` and `documents` rows are preserved (no wipe). Then regenerate the bootstrap artifact with `bash scripts/regenerate-full-schema.sh` (NO `--reset`) so `supabase/full-schema.sql` reflects the new index. Do NOT hand-edit `full-schema.sql`. Finally, un-xfail the race-immune assertion in `test_116_idempotency.py` (a duplicate `(source,target,rel_type)` insert now hits the live 23505 unique-violation → the service returns the existing edge → exactly one row in the DB).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/integration/test_116_idempotency.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `document_relationships_idempotency_idx` exists on the live DB (`SELECT 1 FROM pg_indexes WHERE indexname='document_relationships_idempotency_idx'` returns a row).
    - `supabase/full-schema.sql` contains `document_relationships_idempotency_idx` (regenerated, not hand-edited — `git diff` shows it produced by the dump script).
    - Existing `document_relationships`/`documents` rows preserved (read-back count unchanged — no `db reset` wipe).
    - `test_116_idempotency.py` passes LIVE on `:54322`: a duplicate create returns the existing edge and the table holds exactly one matching row.
  </acceptance_criteria>
  <done>Migration 075 applied live (no reset, dev data preserved), full-schema.sql regenerated, race-immune idempotency test green on :54322.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator confirms migration 075 is live (DB evidence)</name>
  <files>supabase/full-schema.sql</files>
  <action>Operator-gated verification only — Task 1 already performed the apply autonomously (the local stack is up). If Task 1 reported the stack was DOWN, the operator pastes `supabase/migrations/075_document_relationships_idempotency_index.sql` into the Supabase SQL editor and runs it (idempotent — safe to re-run), then confirms the index + regenerates `full-schema.sql` per CLAUDE.md. Otherwise the operator confirms the DB evidence below and resumes.</action>
  <what-built>Migration 075 (the `document_relationships_idempotency_idx` additive partial unique index) crossed into the live local DB `:54322` via psycopg2-direct (no `db push`/`db reset`), `supabase/full-schema.sql` regenerated via the no-reset dump, and the race-immune half of `test_116_idempotency.py` un-marked + green.</what-built>
  <how-to-verify>
    1. Confirm the index is live: `cd backend && venv/Scripts/python -c "import psycopg2; c=psycopg2.connect('postgresql://postgres:postgres@127.0.0.1:54322/postgres'); cur=c.cursor(); cur.execute(\"SELECT indexdef FROM pg_indexes WHERE indexname='document_relationships_idempotency_idx'\"); print(cur.fetchone()); c.close()"` → expect the UNIQUE index over `(user_id, source_doc_id, target_doc_id, rel_type)`.
    2. Confirm dev data preserved (no wipe): the document/relationship counts match pre-apply (the apply script's read-back assertion logged them).
    3. Confirm the artifact: `grep document_relationships_idempotency_idx supabase/full-schema.sql` → present.
    4. Confirm the test: `cd backend && venv/Scripts/python -m pytest tests/integration/test_116_idempotency.py -x` → green.
  </how-to-verify>
  <resume-signal>Type "approved" (or describe the DB-evidence discrepancy).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer/operator → live local DB :54322 | A destructive apply path (`db reset`) would wipe dev data |
| migration file → live schema | A partial apply could leave the index half-built |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-04-01 | Denial of Service (data loss) | `supabase db reset`/`db push` wiping dev data | mitigate | psycopg2-direct / SQL-editor apply ONLY (CLAUDE.md hard rule); read-back asserts existing rows preserved; the blocking human-verify gate confirms on DB evidence |
| T-116-04-02 | Tampering (data integrity) | a partial index apply | mitigate | single-transaction `CREATE UNIQUE INDEX IF NOT EXISTS` (idempotent — re-apply no-ops); read-back from `pg_indexes` before done |
| T-116-04-03 | Tampering (data integrity) | duplicate edges before the index lands | mitigate | the index is the race-immune guarantee the app-code 23505-catch relies on; the un-marked race-immune test proves it live |
| T-116-04-SC | Tampering | npm/pip installs | accept | No package installs (zero net-new deps — RESEARCH §"Package Legitimacy Audit": N/A) |
</threat_model>

<verification>
- Migration 075 applied live (no reset; dev data preserved).
- `document_relationships_idempotency_idx` exists on `:54322`.
- `full-schema.sql` regenerated and contains the index.
- Race-immune idempotency test green live.
</verification>

<success_criteria>
- The additive idempotency index is live on `:54322` (applied without a reset).
- `full-schema.sql` reflects it (regenerated, not hand-edited).
- `test_116_idempotency.py` race-immune assertion green.
</success_criteria>

<output>
Create `.planning/phases/116-document-relationships-backend-agent-tool/116-04-SUMMARY.md` when done.
</output>
