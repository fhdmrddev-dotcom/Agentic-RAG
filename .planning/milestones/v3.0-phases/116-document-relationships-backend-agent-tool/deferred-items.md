# Deferred Items — Phase 116

Out-of-scope discoveries logged during execution. Do NOT fix in-phase.

- **[Plan 03] `backend/README.md` deleted in the working tree (pre-existing, NOT this plan).** Observed as `D README.md` in `git status` at Plan 03 start; last committed at 075.4-era (`11b7a0ab`). Unrelated to relationships work — left untouched (SCOPE BOUNDARY). Re-add or confirm-intentional in a cleanup pass.
- **[Plan 05] Untracked pre-existing 115-era artifacts in the working tree (NOT this plan).** `backend/RUN-BACKEND.md`, `backend/scripts/115_axes234_results.json`, `backend/scripts/115_xprovider_results.json`, `backend/settings_override.json.migrated` — all present at Plan 05 start, unrelated to relationships. Left untouched (SCOPE BOUNDARY); commit or .gitignore in a cleanup pass.
