---
phase: 246-the-recall-cliff-and-the-screen-that-describes-it
plan: 02
type: execute
wave: 2
depends_on: [246-01]
files_modified:
  - frontend/src/pages/SettingsPage.tsx
  - frontend/src/pages/__tests__/SettingsPage.changedFields.test.tsx
autonomous: true
requirements: [RECALL-02]

must_haves:
  truths:
    - "D-246-06 & Finding 1: `hnswEfSearch` is initialized to `null` in `SettingsPage.tsx`. The screen renders a neutral loading placeholder/skeleton before data arrives and NEVER renders a compiled-in guess (40 or 200) on first paint."
    - "Finding 1 dead code removed: `setHnswEfSearch(data.hnsw_ef_search)` assigns the server value directly without an unreachable `??` fallback."
    - "D-246-07: The help text lead phrase is updated from `200 is a good starting point` to `Default is 200`, aligning displayed copy with the active backend default while preserving the empirical evidence and the 1000-worse-than-400 warning."
    - "D-246-08 (Finding 5): G-2 sketch is formally waived in writing — the change is a 5-word lead phrase calibration and pre-fetch null rendering on an existing card."
    - "D-246-09 (Finding 6): SC#2 is asserted against the local database under test. Production deploy verification remains deferred per D-242-08."
  artifacts:
    - path: "frontend/src/pages/SettingsPage.tsx"
      provides: "honest pre-fetch loading state and updated guidance copy"
      contains: "useState<number | null>(null)"
    - path: "frontend/src/pages/__tests__/SettingsPage.changedFields.test.tsx"
      provides: "regression suite verifying pre-fetch loading state, server hydration, and search breadth save"
      contains: "hnsw_ef_search"
  key_links:
    - from: "frontend/src/pages/SettingsPage.tsx"
      to: "backend/app/api/settings.py"
      via: "GET /settings hydration into hnswEfSearch state"
      pattern: "setHnswEfSearch\\(data\\.hnsw_ef_search\\)"
---

<objective>
**Settings UI Pre-fetch Loading Honesty & Copy Calibration (RECALL-02).**

Eliminate compiled-in frontend defaults by initializing `hnswEfSearch` as `null` with a neutral loading state, remove the dead `??` fallback, calibrate the Retrieval card help text to "Default is 200", and verify that Search breadth saves locally against the database under test.
</objective>

<threat_model>
### Security & Reliability Invariants (ASVS L1)
1. **Visual Honesty & False Pre-fetch Assertions (Finding 1):**
   - Invariant: The UI must not render a hardcoded search breadth before asking the server what it holds. During the initial fetch window, the control displays a loading indicator or disabled skeleton rather than a fabricated default.
2. **Payload Integrity on Unchanged Saves (D-242-02):**
   - Invariant: When the Search tab is saved without touching Search breadth, `handleSaveSearch` must not send `hnsw_ef_search` as an unintended change.
3. **Out-of-Bounds Rejection (D-242-03):**
   - Invariant: The control continues to enforce `min={hnswEfSearchFloor}` and `max={hnswEfSearchCeiling}` as served by the backend.
</threat_model>

<context>
- `SettingsPage.tsx:729` currently initializes `useState(40)`.
- `SettingsPage.tsx:826` currently has `setHnswEfSearch(data.hnsw_ef_search ?? 40)`.
- `SettingsPage.tsx:1653-1658` currently reads "200 is a good starting point — on our 100,000-passage test library it returned every result that should have been found, for small and large teams alike. Higher is not better: 1000 measured worse than 400."
</context>

<tasks>

<task id="246-02-01" type="execute">
  <title>Update SettingsPage.tsx state initialization, loading render, and copy</title>
  <action>
    1. In `frontend/src/pages/SettingsPage.tsx`:
       - Change `const [hnswEfSearch, setHnswEfSearch] = useState(40)` to:
         `const [hnswEfSearch, setHnswEfSearch] = useState<number | null>(null)`
       - In `hydrate` (around line 826):
         Change `setHnswEfSearch(data.hnsw_ef_search ?? 40)` to:
         `setHnswEfSearch(data.hnsw_ef_search)`
         (delete the dead `?? 40` fallback).
       - In `searchPayloadFrom` / baseline construction (if applicable):
         Ensure baseline tracks `data.hnsw_ef_search` directly.
       - In the Retrieval card JSX (around line 1628):
         When `hnswEfSearch === null`, render a skeleton or disabled placeholder input instead of guessing a number:
         ```tsx
         <FieldRow label="Search breadth">
           {hnswEfSearch === null ? (
             <div className="h-8 w-full rounded-md bg-muted/40 animate-pulse" aria-label="Loading search breadth" />
           ) : (
             <NumberInput
               value={hnswEfSearch}
               onChange={setHnswEfSearch}
               min={hnswEfSearchFloor ?? undefined}
               max={hnswEfSearchCeiling ?? undefined}
               ariaLabel="Search breadth"
             />
           )}
         </FieldRow>
         ```
       - In the help text (line 1654):
         Change:
         `<span className="font-medium text-foreground">200 is a good starting point</span>`
         to:
         `<span className="font-medium text-foreground">Default is 200</span>`
  </action>
  <verify>
    <command>npm run typecheck</command>
  </verify>
  <done>SettingsPage.tsx renders loading state when null, hydrates cleanly from server, and displays 'Default is 200'.</done>
</task>

<task id="246-02-02" type="execute">
  <title>Update and execute SettingsPage test suites</title>
  <action>
    1. In `frontend/src/pages/__tests__/SettingsPage.changedFields.test.tsx`:
       - Add test verifying that pre-hydration renders the skeleton loading state without guessing 40 or 200.
       - Verify that when `data.hnsw_ef_search` is 200, the loaded input renders 200.
       - Verify that editing `hnsw_ef_search` to 50 sends `{"hnsw_ef_search": 50}` on save (SC#2 local assert).
       - Verify that leaving `hnsw_ef_search` unchanged sends nothing in the diff payload.
    2. Run all SettingsPage test suites.
  </action>
  <verify>
    <command>npx vitest run frontend/src/pages/__tests__/SettingsPage.changedFields.test.tsx</command>
  </verify>
  <done>SettingsPage tests verify null pre-fetch state, exact server hydration, and clean change-only saving.</done>
</task>

</tasks>
