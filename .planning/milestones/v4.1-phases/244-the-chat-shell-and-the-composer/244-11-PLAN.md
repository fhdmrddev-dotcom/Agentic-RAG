---
phase: 244-the-chat-shell-and-the-composer
plan: 11
type: tdd
wave: 2
# G-7's detector (scripts/check-gap-closure-rounds.cjs) keys on `gap_closure`, NOT on
# `gap_closure_round`. Measured while authoring this plan set: with only the round field
# present the gate read `0 gap-closure` and a future round 2 would never have been capped.
gap_closure: true
gap_closure_round: 1
# ⚠ A SCHEDULING dependency, not a semantic one — stated so no reader infers a
# data dependency that does not exist. Neither 244-09 nor 244-10 produces anything
# this plan reads. The wave exists because CLAUDE.md measures the shared vitest gate
# going NON-DETERMINISTIC at three concurrent test-running agents, and wave 1 already
# runs one. Two per wave is the measured ceiling.
depends_on: ["244-09", "244-10"]
files_modified:
  - frontend/src/providers/StreamsProvider.tsx
  - frontend/src/components/chat/ChatArea.tsx
  - frontend/src/__tests__/providers/streamsProvider_244_snapshot_failure.test.tsx
  - frontend/src/components/chat/__tests__/ChatAreaBanner.test.tsx
  - scripts/vitest-count-gate.cjs
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md
  - .planning/phases/244-the-chat-shell-and-the-composer/244-01-BUG-260911-02-TRACE.md
  - .planning/reported-bugs/BUG-260911-02.md
  - .planning/phases/244-the-chat-shell-and-the-composer/244-11-UAT-ROW.md
autonomous: true
requirements: [SHELL-01]

must_haves:
  truths:
    - "When GET /threads/{id}/snapshot fails, the chat surface SAYS SO — an error is rendered with a Retry control, instead of a frame that looks perfect around a silently incomplete transcript."
    - "An EMPTY transcript after a failed snapshot does not claim it is 'showing cached version' — the sentence matches what is actually on screen."
    - "A snapshot that succeeds renders nothing new: the happy path is unchanged and no banner appears."
    - "An aborted snapshot (a thread switch) does NOT raise a banner — a navigation is not a failure."
    - "BUG-260911-02 carries a WRITTEN RULING on whether the snapshot-503 mechanism is its cause, its second cause, or unrelated — and it is never closed on an un-reproduced reading."
  artifacts:
    - path: "frontend/src/providers/StreamsProvider.tsx"
      provides: "a reconcile catch that records the failure instead of swallowing it"
      contains: "reconcileErrors"
    - path: "frontend/src/components/chat/ChatArea.tsx"
      provides: "a banner sentence that is true when the transcript is empty"
    - path: "frontend/src/__tests__/providers/streamsProvider_244_snapshot_failure.test.tsx"
      provides: "RED-driven proof that a failing snapshot reaches the per-thread error slice"
    - path: ".planning/phases/244-the-chat-shell-and-the-composer/244-01-BUG-260911-02-TRACE.md"
      provides: "the G-4 ruling, appended beside the original trace rather than overwriting it"
  key_links:
    - from: "frontend/src/providers/StreamsProvider.tsx reconcile catch"
      to: "frontend/src/components/chat/ChatArea.tsx reconcile-error-banner"
      via: "the per-thread reconcileErrors Map — a SHIPPED slice with a SHIPPED renderer"
      pattern: "reconcileErrors"
---

<objective>
Close **G-3**: `GET /threads/{id}/snapshot` returns **503** intermittently (2 of 4 observed calls)
and the UI renders a **silently incomplete transcript** — header fine, composer fine, and a sweep
of EVERY leaf element for `/unavailable|error|failed|retry|try again|something went wrong/i`
returned **ZERO** matches. The backend is explicit: it answers
`{"detail": "Streaming infrastructure unavailable"}` with `Retry-After: 10`
(`threads.py:517` and `:526`). **The UI discards both.**

The swallow is one clause, in `StreamsProvider.tsx`'s `reconcile` action:

    try { snapshot = await getSnapshot(threadId) }
    catch (err) { console.error("reconcile failed:", err); return }

⭐ **Everything needed to show this is already shipped, so this introduces no new capability**
(G-7): the per-thread `reconcileErrors` Map, its selector `useReconcileErrorForThread`, and
`ChatArea.tsx:648`'s amber `reconcile-error-banner` with **Retry** and **dismiss** — all live, all
already driven by the `loadMessages` failure path (`StreamsProvider.tsx:3209-3215`), and reached by
nothing on the snapshot path. This plan connects a shipped producer to a shipped renderer.
⛔ **No new banner, no new endpoint, no new store slice, no new retry mechanism.**

And close **G-4**: rule, in writing, on whether this mechanism is `BUG-260911-02`'s cause.

Why this belongs to SHELL-01: its goal sentence is *"the chrome around a conversation stops getting
in the way of it."* **A thread that renders its frame perfectly and silently omits the conversation
is that failure in its purest form** — and the observed symptom (first click empty, second click
loads all 78 messages) is `BUG-260911-02`'s reported symptom exactly.

⛔ **This round does not re-open SHELL-05** (L-7 passed in both directions) **and does not re-open
the parts of SHELL-01 that passed** — four of L-1's six samples, the rail's zero-drift reading, the
zero `scrollIntoView` calls.

**Solo run (D-244-21 / OV-SOLO-01):** no independent reviewer exists. SELF-verification only; say
so in the SUMMARY.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⛔ **FIRST ACTION:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`, then `git rev-parse HEAD` —
MUST be a descendant of `760c758fc` on `develop`, **including wave 1's merges**. ⚠ A fresh worktree
starts on the DEFAULT branch (`master`, at the v3.9 ship commit); five consecutive executors had to
reset. Assert the SHA before reading anything else.
⛔ NEVER `rm -rf` a worktree — `bash scripts/teardown-worktree.sh <path>`.

**⚠ Gate state, inherited:**
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs`, **from the repo root**. Contract:
  *no per-file DECREASE* + *zero failing*. A bigger grand total is the gate WORKING.
- ⛔ Capture failing FILENAMES from the gate's own persisted JSON **BEFORE** any re-run, and check
  each against `git diff --numstat`. `SEED-171`'s five flaky suites are cap-independent and three
  of them fail with a plain `AssertionError`, so `STACK_TRACE_ERROR` is **not** a reliable tell for
  *"not a real defect"*. ⛔ Never touch the cap to make a red go away.
- `sourceComposition.test.tsx` is a standing red in NEITHER knob — invisible to the verdict line.
- ⛔ `npx tsc --noEmit` in `frontend/` type-checks ZERO files (solution-style config) and is
  vacuous. Use `npx tsc -p tsconfig.app.json --noEmit` and measure a **SET DIFF** against the
  ~67-error base. "Zero errors" is not reachable here.

**⚠ TWO PLANS RUN IN THIS WAVE** (`244-11` and `244-12`) — the measured maximum, because at three
concurrent test-running agents the shared gate goes non-deterministic regardless of cap. Source
sets are disjoint: this plan owns `StreamsProvider.tsx` + `ChatArea.tsx`; `244-12` owns
`MessageItem.tsx` + `MessageList.tsx`.

**⛔ REGISTRY MERGE ORDER.** Both plans append into `scripts/vitest-count-gate.cjs`,
`docs/HOT-FILE-LEDGER.md` and `CLAUDE.md`. *"Take both sides"* is a policy, not a mechanism:
1. Merge `244-11` first, whole.
2. Rebase `244-12` onto that merge and **re-apply its `TARGETS` line, its `BASELINE` pin, its
   ledger rows and its ledger sections BY HAND.** ⛔ No automatic resolution on those three files.
3. Re-run from the repo root AFTER the second merge: `node scripts/check-hot-file-ledger.cjs 244`,
   `node scripts/check-claude-md-size.cjs`, then the wave's SINGLE full count gate.
⛔ A row lost to a bad auto-merge is SILENT (`[no-row]` fires only for files a plan NAMES); a row
added twice fails `[duplicate-row]`.

**⚠ G-2 does not fire.** Bug fix on a shipped surface, cause named at `file:line`, and the banner
itself is already designed and shipped. No sketch is owed — say so explicitly rather than sketching
everything or nothing (`D-244-18`).
</execution_context>

<context>
@.planning/phases/244-the-chat-shell-and-the-composer/244-UAT.md
@.planning/phases/244-the-chat-shell-and-the-composer/244-01-BUG-260911-02-TRACE.md
@.planning/reported-bugs/BUG-260911-02.md
@.planning/phases/244-the-chat-shell-and-the-composer/244-CONTEXT.md
@CLAUDE.md

⛔ **Read the `G-3` and `G-4` YAML blocks at the bottom of `244-UAT.md` in full**, including
`root_cause_partial` and its own honesty rule: *"⚠ a cap_paused run is NON-TERMINAL, so it keeps
being probed on every thread open; the seeded cap_paused thread is the one that produced both 503s.
**Whether that is causal was NOT established and must not be written down as though it were.**"*
⛔ Do not upgrade that correlation to a cause anywhere in this plan's output.

⛔ **Read `244-01-BUG-260911-02-TRACE.md` end to end** — §3 refutes candidates (b) and (c) on driven
observations, §4 names a FOURTH candidate (the `opacity-0` actions overlay that won the hit test,
fixed as a defect in its own right and explicitly **not** claimed as the closure), and §5 sets the
`re_open_trigger` and lists what stays owed.

@frontend/src/providers/StreamsProvider.tsx
@frontend/src/components/chat/ChatArea.tsx
@frontend/src/components/chat/__tests__/ChatAreaBanner.test.tsx
@backend/app/api/threads.py
@docs/HOT-FILE-LEDGER.md

<interfaces>
The swallow (`StreamsProvider.tsx`, inside `actions.reconcile`, ~`:1928-1936`):

    let snapshot: ThreadSnapshot
    try {
      snapshot = await getSnapshot(threadId)
    } catch (err) {
      console.error("reconcile failed:", err)
      return
    }

The SHIPPED write to copy, from the `loadMessages` failure path (`StreamsProvider.tsx:3209-3215`):

    useStreamsStore.setState((s) => ({
      reconcileErrors: new Map(s.reconcileErrors).set(
        threadId,
        err instanceof Error ? err : new Error(String(err)),
      ),
    }))

The SHIPPED renderer (`ChatArea.tsx:648-690`) — complete, accessible, already carrying Retry:

    {reconcileError && (
      <div data-testid={... === 409 ? "workflow-lock-error-banner" : "reconcile-error-banner"}
           role="status" aria-live="polite">
        <span>{reconcileError instanceof ApiError
                 ? reconcileError.message
                 : "Couldn't load latest messages. Showing cached version."}</span>
        {!hideRetry && <button onClick={handleRetryReconcile} aria-label="Retry loading messages">Retry</button>}
        <button onClick={dismissReconcileError} aria-label="Dismiss">x</button>
      </div>
    )}

    const NON_RETRYABLE = new Set([400, 403, 404, 409, 422])   // ChatArea.tsx:104
    // 503 is NOT in that set, so Retry already renders for this failure.

`messages` is already a PROP of ChatArea (destructured at `:62`), so `messages.length === 0` needs
no new read, no new hook and no new prop.

The client (`frontend/src/lib/api/threads.ts:1063-1074`) throws a BARE
`Error("Failed to fetch snapshot (status ${res.status})")` — **not** an `ApiError` — so the banner
takes its non-ApiError arm. That is the arm whose sentence this plan makes honest.

The server side, for the record (`threads.py:495-530`): a `ResponseError('no such key')` is
correctly DEGRADED (`:508`, `continue` — a GC'd buffer just means that run is terminal); a TIMEOUT
(2.0 s) or a real `RedisError` takes the whole-snapshot 503 path with `Retry-After: 10`.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — a snapshot that fails reaches nothing, and the transcript says nothing</name>
  <files>frontend/src/__tests__/providers/streamsProvider_244_snapshot_failure.test.tsx, frontend/src/components/chat/__tests__/ChatAreaBanner.test.tsx, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `.planning/phases/244-the-chat-shell-and-the-composer/244-UAT.md` — the `G-3` YAML block
    - `frontend/src/__tests__/providers/streamsProvider_243_cadence.test.tsx` — the house shape for a provider-level suite (it is gated in both knobs; copy its mocking and store-reading style)
    - `frontend/src/components/chat/__tests__/ChatAreaBanner.test.tsx` — the whole file; it already mounts ChatArea inside the REAL StreamsProvider, seeds `reconcileErrors` directly, and re-exports the REAL `ApiError` so production `instanceof` matches
    - `frontend/src/providers/StreamsProvider.tsx` — ONLY the `reconcile` action (~`:1924-1940`) and the `loadMessages` failure write (`:3205-3220`)
  </read_first>
  <behavior>
    - **Test 1 (the gap — MUST be RED):** drive `actions.reconcile(threadId)` with `getSnapshot`
      mocked to REJECT with the shape the client actually throws —
      `new Error("Failed to fetch snapshot (status 503)")`. Assert
      `useStreamsStore.getState().reconcileErrors.has(threadId)` is `true` and the stored value is
      that same Error. ⛔ Assert on the STORE, never on a `console.error` spy — a console call is
      not a user-visible state, and treating it as one is what made this gap invisible.
    - **Test 2 (the happy path is untouched):** `getSnapshot` resolves normally =>
      `reconcileErrors.has(threadId)` is `false` and the messages bucket hydrates exactly as today.
      The anti-regression half, written BEFORE the fix rather than after it.
    - **Test 3 (a navigation is not a failure):** `getSnapshot` rejects with
      `DOMException("aborted", "AbortError")` => **no** entry in `reconcileErrors`. ⛔ Without this,
      every thread switch that cancels an in-flight snapshot raises a banner on arrival.
    - **Test 4 (thread scoping):** a failure on thread A leaves `reconcileErrors.has(B)` false. The
      slice is per-thread (`D-075.4-A1`); a bleed here puts a banner on a conversation that is fine.
    - **Test 5 (ChatArea — the sentence is TRUE; lives in `ChatAreaBanner.test.tsx`):** with a plain
      (non-`ApiError`) reconcile error seeded AND `messages` **empty**, the banner renders, the
      Retry control renders, and the rendered sentence does **NOT** contain `Showing cached version`
      — because nothing cached is on screen. ⛔ Assert the rendered CONTENT, never a `data-testid`:
      *"presence assertions cannot see content drift"* is this project's own recorded lesson and is
      exactly how G-6 shipped green.
    - **Test 6 (the shipped sentence survives where it is true):** the same plain error with a
      NON-empty `messages` array => the sentence is still byte-exactly
      `"Couldn't load latest messages. Showing cached version."` ⛔ This plan changes copy in ONE
      state only; changing it everywhere would be a redesign, not a gap fix.
  </behavior>
  <action>
Create `frontend/src/__tests__/providers/streamsProvider_244_snapshot_failure.test.tsx` holding
Tests 1-4, modelled on `streamsProvider_243_cadence.test.tsx`'s mounting and mocking style.

Add Tests 5-6 to the existing `ChatAreaBanner.test.tsx`. ⛔ Do not create a second banner suite:
that file already solves the hard part (ChatArea mounted inside the REAL provider, network deps
stubbed, the REAL `ApiError` re-exported).

Register the NEW suite in **BOTH** knobs of `scripts/vitest-count-gate.cjs` — `TARGETS` gets
`"src/__tests__/providers/streamsProvider_244_snapshot_failure.test.tsx"`, and `BASELINE` gets a
pin with its case count. ⚠ **A suite in one knob and not the other runs while guarding nothing**
(the Phase 214 lesson, measured on `WorkflowScheduleModal.test.tsx`). Bump
`ChatAreaBanner.test.tsx`'s pin from `7` to its new count and attribute the `+N` with no residual.

⛔ **Run and SEE Tests 1 and 5 RED**, quoting both verbatim failures in the SUMMARY. Tests 2, 3, 4
and 6 describe shipped behaviour and should be GREEN at RED time — **record their measured colour
rather than assuming it**; a control that is already red is a finding, not a nuisance.

⛔ `git diff -- frontend/src/providers/StreamsProvider.tsx frontend/src/components/chat/ChatArea.tsx`
must be **EMPTY** at the end of this task: RED is driven against unmoved production code.
  </action>
  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/__tests__/providers/streamsProvider_244_snapshot_failure.test.tsx src/components/chat/__tests__/ChatAreaBanner.test.tsx` runs; Tests 1 and 5 FAIL, with both failure messages quoted verbatim in the SUMMARY.
    - The SUMMARY states the measured colour of Tests 2/3/4/6 at RED time, per case.
    - `git diff -- frontend/src/providers/StreamsProvider.tsx frontend/src/components/chat/ChatArea.tsx` is EMPTY.
    - `grep -c "streamsProvider_244_snapshot_failure.test.tsx" scripts/vitest-count-gate.cjs` returns exactly `2` — one `TARGETS` entry and one `BASELINE` pin.
    - `grep -c "Failed to fetch snapshot (status 503)" frontend/src/__tests__/providers/streamsProvider_244_snapshot_failure.test.tsx` returns `>= 1` — the fixture uses the client's REAL throw shape. ⛔ A fixture inventing its own error shape is the "the test constructs the shape it then asserts" failure that let G-6 ship green.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/__tests__/providers/streamsProvider_244_snapshot_failure.test.tsx src/components/chat/__tests__/ChatAreaBanner.test.tsx</automated>
  </verify>
  <done>Tests 1 and 5 are RED against unmoved production code with their failure text quoted; the new suite sits in both gate knobs.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — the failure is recorded, and the sentence matches what is on screen</name>
  <files>frontend/src/providers/StreamsProvider.tsx, frontend/src/components/chat/ChatArea.tsx</files>
  <read_first>
    - `frontend/src/providers/StreamsProvider.tsx` — ONLY `:1924-1940` (the reconcile catch) and `:3205-3220` (the shipped write it copies)
    - `frontend/src/components/chat/ChatArea.tsx` — ONLY `:96-112` (the selectors and `NON_RETRYABLE`) and `:648-690` (the banner)
    - Task 1's six cases
  </read_first>
  <action>
**(a) `StreamsProvider.tsx` — record instead of swallow.** In `reconcile`'s catch:
- keep the `console.error` (diagnostic, not a substitute for state);
- **return early WITHOUT writing** when the error is an abort —
  `err instanceof DOMException && err.name === "AbortError"`. ⚠ A navigation is not a failure, and
  `reconcile` is fired from `setViewingThread` on every thread switch;
- otherwise write the per-thread entry using the **exact shipped expression** from `:3209-3215`.
  ⛔ Reuse it rather than inventing a second shape: two writers of one slice that differ is how
  slices in this codebase drift, and `loadMessages` is the precedent that already exists;
- `return` after the write, exactly as today — ⛔ the function must still not proceed to hydrate
  from an undefined snapshot.

⛔ **Do not add a retry loop.** `loadMessages` already has a two-attempt `tryFetch`, and the
banner's Retry button is the shipped user-driven retry. A third retry mechanism is a new behaviour,
not a gap fix (G-7).
⛔ **Do not read `Retry-After`.** The header is cited in the gap as evidence that the server is
explicit; CONSUMING it is an automatic-retry feature and belongs in `deferred-items.md`, not here.

**(b) `ChatArea.tsx` — one sentence, one new state.** In the banner's non-`ApiError` arm, select
between two strings on `messages.length === 0`:
- transcript EMPTY: a sentence saying the conversation could not be loaded, inviting the retry that
  is already beside it. ⛔ It must not contain `cached version`, and must not name a status code, an
  exception type, or "Redis" — the vocabulary rule this project holds (*"say what is true OF THE
  THING, never what the code experienced"*), the same rule L-7 measured holding in the
  stopped-source popover.
- transcript NON-EMPTY: the shipped string, **byte-unchanged**.

⛔ **Change nothing else in the banner**: not the `data-testid` selection, not the 409 arm, not
`NON_RETRYABLE`, not `hideRetry`, not the dismiss control, not `role` / `aria-live`. The ApiError
arm (`099-08`'s server-detail copy) stays exactly as shipped.

Run both suites plus `ChatArea.capPausedComposer.test.tsx` (the shipped suite most likely to be
disturbed by a ChatArea edit) and report their counts.
  </action>
  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/__tests__/providers/streamsProvider_244_snapshot_failure.test.tsx src/components/chat/__tests__/ChatAreaBanner.test.tsx src/components/chat/__tests__/ChatArea.capPausedComposer.test.tsx` — `failed 0`.
    - `grep -c "AbortError" frontend/src/providers/StreamsProvider.tsx` increases by exactly `1` relative to the pre-edit value (record both numbers).
    - `grep -c "Showing cached version" frontend/src/components/chat/ChatArea.tsx` returns exactly `1` — the shipped sentence survives in exactly one place.
    - The new empty-transcript literal contains none of `cached`, `503`, `Redis`, `Streaming infrastructure`, `Error` — assert with a grep over the literal and quote the grep in the SUMMARY.
    - `git diff --stat -- frontend/src/providers/StreamsProvider.tsx` shows a change confined to the `reconcile` catch (name the hunk header); `git diff --stat -- frontend/src/components/chat/ChatArea.tsx` shows a change confined to the banner's non-ApiError arm.
    - `npx tsc -p tsconfig.app.json --noEmit` — error SET diff against the base capture EMPTY in both directions.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/__tests__/providers/streamsProvider_244_snapshot_failure.test.tsx src/components/chat/__tests__/ChatAreaBanner.test.tsx src/components/chat/__tests__/ChatArea.capPausedComposer.test.tsx</automated>
  </verify>
  <done>A failing snapshot raises the shipped banner with Retry; an empty transcript gets a sentence that is true; the happy path and the abort path raise nothing.</done>
</task>

<task type="execute">
  <name>Task 3: G-4 — rule on BUG-260911-02 against this mechanism, in writing</name>
  <files>.planning/phases/244-the-chat-shell-and-the-composer/244-01-BUG-260911-02-TRACE.md, .planning/reported-bugs/BUG-260911-02.md, docs/HOT-FILE-LEDGER.md, CLAUDE.md, .planning/phases/244-the-chat-shell-and-the-composer/244-11-UAT-ROW.md</files>
  <read_first>
    - `.planning/phases/244-the-chat-shell-and-the-composer/244-01-BUG-260911-02-TRACE.md` — all five sections
    - `.planning/reported-bugs/BUG-260911-02.md` — frontmatter and body
    - `.planning/phases/244-the-chat-shell-and-the-composer/244-UAT.md` — the `G-4` block and G-3's `why_it_belongs_to_this_phase`
    - `docs/HOT-FILE-LEDGER.md` §§ `frontend/src/providers/StreamsProvider.tsx` and `frontend/src/components/chat/ChatArea.tsx`
    - `CLAUDE.md` — the scan-list rows for both files
  </read_first>
  <action>
**(a) The ruling.** APPEND a dated section `## 6. The G-4 re-check against the snapshot-503
mechanism (round 1, YYYY-MM-DD)` to `244-01-BUG-260911-02-TRACE.md`. ⛔ **Append — never edit or
overwrite §§1-5.** That trace's own value is that its refutations are preserved with their reasons.

The ruling is a comparison of the two mechanisms against the REPORTED symptom, and it must land on
exactly one of three verdicts, each already reachable from evidence in hand:

| Verdict | What it would require |
|---|---|
| SAME defect | the snapshot-503 path explains a HIGHLIGHTED row with an unrendered transcript |
| SECOND cause | both mechanisms can produce the report's appearance, by different routes |
| UNRELATED | the 503 path cannot produce the reported appearance at all |

Two facts already in the record bear directly on it and must be weighed explicitly rather than
gestured at:
- the trace's §3(c) established that the report's *"highlighted row + 'How can I help you?'"*
  reading **cannot both come from selection state**, because the welcome branch is gated on
  `if (!thread)` and the highlight is `selectedThread?.id === thread.id`. **A failed snapshot does
  NOT produce the welcome pane** — the thread is non-null, so the THREAD branch renders with an
  empty transcript. ⚠ That is a materially different appearance from the one the report describes,
  and it is the discriminator.
- the UAT's own G-3 observation was *"a 78-message thread opening completely EMPTY; a second click
  on the same row loaded it"* — a **thread-branch** appearance, which matches the 503 mechanism and
  not the trace's §4 overlay candidate.

⛔ **Whatever the verdict, `status:` stays `folded` and the report is NOT closed** — nothing here
reproduces it in a browser, and the plan's standing rule is *never close a bug on an un-reproduced
reading*. Update `BUG-260911-02.md`'s `re_open_trigger` so it now carries **both** discriminators
in one browser visit: (i) the trace §4 right-hand-third click, and (ii) whether the empty pane on a
failed first click shows the **welcome** state or the **thread frame with an empty transcript** —
because after this plan the second case raises a visible banner, which itself becomes the tell.
⚠ Record in the report that the banner is a NEW observable that did not exist when the report was
filed, so a future reader does not read its absence in the original as evidence.

**(b) The ledger.** Both `StreamsProvider.tsx` and `ChatArea.tsx` FIRE G-5 and both have rows.
Re-derive each triple from the repo root with CLAUDE.md's recipe, **subtracting six-digit dated
quick-task buckets**:

  git log --oneline -- <file> | wc -l
  git log --format=%s -- <file> | sed -E 's/^[a-z]+\(([^)]+)\).*/\1/' | sed -E 's/-.*//' | grep -E '^[0-9]+(\.[0-9]+)?$' | grep -vE '^[0-9]{6}$' | sort -u | wc -l
  wc -l <file>

Measured at planning time: `StreamsProvider.tsx` **95 / 37 / 4584** against a row reading
`94 / 38 / 4528`; `ChatArea.tsx` **73 / 36 / 731** against `72 / 36 / 710`. ⚠ **Re-derive anyway** —
`StreamsProvider.tsx`'s row has now been found STALE four times, twice within one day, and *a row
that is present and WRONG answers the auditor with "satisfied" and stops the audit.*

Expect **honoured by construction** for both, and say why in each cell (<= 200 chars): the
provider's change is confined to one existing catch and adds no new action, no new slice and no new
subscription; ChatArea's is one string selection inside an already-rendered arm, adding no state,
no effect and no prop. Reasons go in the sections; row and section move **in the same commit**.

**(c) The browser row.** Create `244-11-UAT-ROW.md` with ONE row, unfilled:
- **Method — force the failure, do not wait for it.** The UAT proved this is reproducible by
  patching `window.fetch` to answer every `/snapshot` with the endpoint's own 503 body and header.
  ⛔ Do not seed a real Redis outage on the operator's machine.
- **Arm 1 (the gap closed):** with the patch armed, open the 78-message fixture thread
  `261d5f57-36fb-40ec-bb0b-1c72b7550350`. Assert a visible failure sentence is present, that the
  **Retry** control is present and focusable, and re-run the UAT's own sweep — every leaf element
  against `/unavailable|error|failed|retry|try again|something went wrong/i` — which returned
  **ZERO** matches before and must now return a non-zero match **inside the chat column**.
- **Arm 2 (Retry actually recovers):** unpatch `fetch`, click **Retry**, assert the full transcript
  renders and the banner is gone.
- **Arm 3 (the negative — no false signal):** a normal thread open with no patch shows **no**
  banner. ⛔ This is the ROADMAP's named failure mode in its general form: *a signal nobody will
  trust after the first false one*.
- **Arm 4 (G-4's live discriminator):** with the patch armed, note whether the pane shows the
  WELCOME state or the thread frame with an empty transcript — and record it, whichever it is, as
  the reading that settles §6's verdict against a live browser.
⛔ Every verdict field reads `pending`; the row is driven at `/gsd:verify-work`, not here.
  </action>
  <acceptance_criteria>
    - `244-01-BUG-260911-02-TRACE.md` gained a `## 6.` section; `git diff` shows lines 1-170 of that file are otherwise **unchanged** (append-only, asserted by the diff hunk list).
    - §6 states exactly one of SAME / SECOND CAUSE / UNRELATED, and weighs the welcome-vs-thread-branch discriminator explicitly.
    - `grep -c "^status: folded" .planning/reported-bugs/BUG-260911-02.md` returns `1` — the report is NOT closed.
    - `BUG-260911-02.md`'s `re_open_trigger` contains both discriminators and names the new banner as an observable that did not exist at filing time.
    - `node scripts/check-hot-file-ledger.cjs 244` exits `0` **and** reports a non-zero `subject:` file count — ⛔ a run reading `subject: 0 files` passed vacuously (measured at Phase 242 on a CRLF plan).
    - `grep -c "frontend/src/providers/StreamsProvider.tsx" CLAUDE.md` and `grep -c "frontend/src/components/chat/ChatArea.tsx" CLAUDE.md` each return exactly `1`.
    - Both disposition cells are <= 200 characters; `node scripts/check-claude-md-size.cjs` exits `0`.
    - `244-11-UAT-ROW.md` exists with four arms, names the `window.fetch` patch method, re-uses the UAT's own leaf-element sweep regex, and contains no `pass` verdict.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-hot-file-ledger.cjs 244 &amp;&amp; node scripts/check-claude-md-size.cjs</automated>
  </verify>
  <done>BUG-260911-02 carries a written ruling and stays `folded`; both ledger rows are re-derived in the same commit; the four-arm browser row exists and is unfilled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server error payload -> rendered UI text | A failure sentence is rendered in the chat column. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-244-11-01 | Information Disclosure | the new empty-transcript sentence | mitigate | The sentence is a CLIENT-AUTHORED literal. ⛔ It must not interpolate the server's `detail`, the status code, an exception name or "Redis" — asserted by grep in Task 2. The `ApiError` arm that DOES render a server string is byte-unchanged and was already reviewed under `099-08` / `T-099-08-01` (React text children only, never `dangerouslySetInnerHTML`). |
| T-244-11-02 | Spoofing | a false failure signal | mitigate | Test 2 (happy path) and Test 3 (abort) both assert **no** entry is written, and UAT Arm 3 drives the negative in a browser. A banner that fires on a healthy thread is the ROADMAP's own named failure mode. |
| T-244-11-03 | Denial of Service | retry amplification | accept | ⛔ No automatic retry is added and `Retry-After` is deliberately not consumed. Recovery stays a single user-initiated click on the shipped Retry control, so a 503 storm cannot be amplified by this change. |
| T-244-11-04 | Repudiation | `BUG-260911-02`'s disposition | mitigate | The ruling is APPENDED beside the original trace; `status:` stays `folded`; the `re_open_trigger` carries both discriminators. A report closed on an un-reproduced reading is the failure this guards. |
| T-244-11-SC | Tampering | npm/pip/cargo installs | n/a | **No package is installed.** `git diff -- frontend/package.json frontend/package-lock.json` MUST be empty; no legitimacy audit is owed. ⚠ If a dependency turns out to be needed, STOP — the audit table comes first. |
</threat_model>

<verification>
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the **repo root**: `failed 0`,
  no per-file DECREASE, and every `+N` attributed with **no residual** — the new provider suite's
  pin plus `ChatAreaBanner.test.tsx`'s increment must exactly account for the delta.
  ⛔ Capture failing filenames from the gate's own JSON BEFORE any re-run; check each against
  `git diff --numstat` before calling it inherited. ⛔ Never adjust the cap.
- `npx tsc -p tsconfig.app.json --noEmit` — SET diff against the base capture empty both ways.
- `node scripts/check-hot-file-ledger.cjs 244` (non-vacuous parse) and
  `node scripts/check-claude-md-size.cjs` — both exit 0, from the repo root, AFTER the wave merge.
- ⛔ **The backend is untouched:** `git diff --stat -- backend/ supabase/` is EMPTY, so the
  zero-headroom backend baseline (71) is not at risk and is not re-run for this plan.
- ⛔ **NOT verified here, stated as owed:** that the banner appears in a real browser on a real 503.
  `244-11-UAT-ROW.md` is driven at `/gsd:verify-work`. This plan may not report SHELL-01 as closed.
</verification>

<success_criteria>
- A failing snapshot writes the per-thread error and the shipped banner renders with Retry.
- An abort and a successful snapshot each write nothing — both driven.
- The empty-transcript sentence does not claim a cached transcript; the shipped sentence survives
  byte-exactly where it is true.
- Tests 1 and 5 were seen RED with their failure text quoted; the four controls' colours recorded.
- `BUG-260911-02` carries a written G-4 ruling, stays `folded`, and its trigger names both
  discriminators.
- Both ledger rows re-derived, rows + sections in one commit, both registry gates exit 0.
- The SUMMARY says **built, drive owed** for SHELL-01 — never closed — and says SELF-verified.
</success_criteria>

<output>
Create `.planning/phases/244-the-chat-shell-and-the-composer/244-11-SUMMARY.md` when done.
</output>
