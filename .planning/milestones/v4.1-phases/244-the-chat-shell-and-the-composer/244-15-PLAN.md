---
phase: 244-the-chat-shell-and-the-composer
plan: 15
type: execute
wave: 8
depends_on: []
gap_closure: true
gap_closure_round: 2
autonomous: true
requirements: [SHELL-03]
files_modified:
  - frontend/src/stores/streamsStore.ts
  - frontend/src/providers/StreamsProvider.tsx
  - frontend/src/components/panel/PendingAskCard.tsx
  - frontend/src/__tests__/providers/streamsProvider_244_settle_ask.test.tsx
  - frontend/src/components/panel/__tests__/PendingAskCard.retired.baseline.test.tsx
  - scripts/vitest-count-gate.cjs
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md
  - .planning/phases/244-the-chat-shell-and-the-composer/244-15-UAT-ROW.md
  - .planning/STATE.md

must_haves:
  truths:
    - "Answering an approval in the CHAT column clears the PANEL's card with no manual refresh"
    - "Answering an approval in the PANEL clears the CHAT column's controls with no manual refresh"
    - "Once the server reports the thread's workflow anchor dropped, the run line stops reading `live` and the composer is usable again"
    - "While the server still reports a live (or cap-paused) run, NOTHING is unlocked — the settle path releases, it never sets"
    - "WorkflowRunPage's PendingAskCard renders exactly as it did before (the third home is byte-unchanged in behaviour)"
  artifacts:
    - path: "frontend/src/stores/streamsStore.ts"
      provides: "`releaseSettledWorkflowLock` action type + its synchronous no-op default"
      contains: "releaseSettledWorkflowLock"
    - path: "frontend/src/providers/StreamsProvider.tsx"
      provides: "The ONE settle path: one authoritative `getThreadWorkflow` read that releases a lock the server has already dropped, clears the kickoff mark, refreshes the phase spine, and re-attaches the live producer stream when the run is still going"
      contains: "releaseSettledWorkflowLock"
    - path: "frontend/src/components/panel/PendingAskCard.tsx"
      provides: "`onAnswered` optional callback fired after a SUCCESSFUL answer; `PendingAskStack` composes the shipped ask `reconcile()` with the lock release"
      contains: "onAnswered"
    - path: "frontend/src/__tests__/providers/streamsProvider_244_settle_ask.test.tsx"
      provides: "The settle fences — one store-level pass and one two-homes render pass, both explicitly labelled as jsdom"
    - path: ".planning/phases/244-the-chat-shell-and-the-composer/244-15-UAT-ROW.md"
      provides: "The browser row `/gsd:verify-work` drives — both directions, scored by position against the panel edge and by the rendered sentence"
  key_links:
    - from: "frontend/src/components/panel/PendingAskCard.tsx (handleSubmit success arm)"
      to: "onAnswered"
      via: "one call, AFTER the optimistic answered flip, only on success"
      pattern: "onAnswered\\?\\.\\(\\)"
    - from: "frontend/src/components/panel/PendingAskCard.tsx (PendingAskStack)"
      to: "useStreamsStore.getState().actions.releaseSettledWorkflowLock"
      via: "the stack's settle callback, composed with the shipped ask `reconcile()`"
      pattern: "releaseSettledWorkflowLock"
    - from: "frontend/src/providers/StreamsProvider.tsx (releaseSettledWorkflowLock)"
      to: "clearWorkflowLockForThread + clearStopStateForThread + refreshPhaseSpineAfterStop"
      via: "the release arm, taken ONLY when neither shipped set-arm's guard holds"
      pattern: "clearStopStateForThread"
---

<objective>
Close gap **G-8** — the sole unmet ROADMAP success criterion for Phase 244. **SC#3**'s second clause,
*"and answering it in either home settles it in both"*, is FALSE on the workflow path: each home
settles its own component-local state, neither reaches the other, and nothing re-reads the thread
lock — so a run the server has already finished leaves a live run line, a climbing clock and a
disabled composer until the person guesses that a refresh is needed.

Purpose: make the ANSWER transition do what `D-v2.5-03` already requires of every other transition —
*"Realtime is a hint, not truth — always reconcile via fetch on (re)connect"*. One settle path,
fetch-based, that invalidates both homes and re-reads the lock. **Not** a second optimistic update:
a second mechanism that can drift from the first IS the defect.

Output: one new provider action, one wiring change in the shared `PendingAskStack`, one new fence
file, a re-baselined (and finally GATED) card baseline suite, and `244-15-UAT-ROW.md`.

⛔ **THIS PLAN DOES NOT CLOSE SHELL-03.** It reports **built, drive owed**. `D-244-14` binds:
*"`BUG-260828-07` is severity HIGH and closes on a DRIVEN row, not a fence."* `244-03` shipped a
green mount fence over this exact blocker and the operator found it live nineteen plans later;
`244-12` then shipped fences proving the approval MOUNTS, and the half that broke was whether it
ANSWERS. Every assertion in this plan is a jsdom fence and is labelled as one.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⚠ **Worktree first.** Before the HEAD assertion and before reading anything else:
`bash scripts/bootstrap-worktree.sh "$(pwd)"`. A worktree that skipped it reports green typechecks
and red tests for reasons that look like this plan's fault.

⚠ **Base SHA.** Agent worktrees start on `origin/HEAD` = `master`, which is stuck at the v3.9 ship
commit. Assert you are on the `develop` tip that carries `244-14` (`ace8ba6df` or later) before
touching a file. Five consecutive executors have had to reset.

⚠ **`GSD_VITEST_MAX_WORKERS=2` on every vitest invocation.** Single plan, single agent — but the cap
also matters on a single run since the gated total passed ~3600.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/244-the-chat-shell-and-the-composer/244-UAT.md
@.planning/phases/244-the-chat-shell-and-the-composer/244-12-UAT-ROW.md
@.planning/phases/244-the-chat-shell-and-the-composer/244-CONTEXT.md

Read these three ledger sections BEFORE editing their files (`G-5` / `D-244-20`, same-commit sync):
@docs/HOT-FILE-LEDGER.md → `frontend/src/providers/StreamsProvider.tsx` (`244-05`, `244-11`, `244-14`)
@docs/HOT-FILE-LEDGER.md → `frontend/src/components/panel/PendingAskCard.tsx` (`244-03`)
@docs/HOT-FILE-LEDGER.md → `frontend/src/stores/streamsStore.ts` (`244-13`)

Source you will edit:
@frontend/src/providers/StreamsProvider.tsx
@frontend/src/stores/streamsStore.ts
@frontend/src/components/panel/PendingAskCard.tsx

Harness precedent to copy, do not re-invent:
@frontend/src/__tests__/providers/streamsProvider_244_snapshot_failure.test.tsx

<interfaces>
<!-- Everything below was READ out of the codebase at planning time, at file:line. -->
<!-- The executor needs no exploration to write against these. -->

The wire type the settle reads — `frontend/src/lib/api/threads.ts:1276-1310`:
```typescript
export interface ThreadWorkflowState {
  locked: boolean
  lock_is_stale: boolean
  active_workflow_run_id: string | null     // harness iff NOT NULL (threads.py:1195)
  latest_producer_run_id?: string | null    // the live producer shell to re-attach to
  cap_paused?: boolean
  mode?: string                             // "harness" | …, computed server-side
  continues_remaining?: number
  last_run_status?: string | null
  phases?: unknown[]
}
export async function getThreadWorkflow(threadId: string, signal?: AbortSignal): Promise<ThreadWorkflowState>
export async function getThreadPendingAsks(threadId: string, signal?: AbortSignal): Promise<PendingAsk[]>
export async function answerAskUser(runId: string, body: AskUserAnswerBody, signal?: AbortSignal): Promise<void>
```

The shipped store writers this plan REUSES (never duplicates) — `StreamsProvider.tsx`:
```typescript
// module-level, :4355 — already called by the streamingThreads-transition effect
function refreshPhaseSpineAfterStop(threadId: string): void
// component-scope, :1594 — clears stoppingThreads ∪ stopNotConfirmed ∪ harnessKickoffThreads + disarms the timer
const clearStopStateForThread = (threadId: string) => void
// actions, :3497
clearWorkflowLockForThread: (threadId: string) => void
// component-scope ref used by the mount reconcile's re-attach arm, :2440
subscribeProducerStreamRef.current?.(threadId, producerRunId)
```

The two readers that make the run line and the composer wrong — `StreamsProvider.tsx:4711`,
`ChatArea.tsx:167`:
```typescript
export const useHarnessLiveForThread = (threadId: string | null): boolean =>
  useStreamsStore((s) =>
    threadId
      ? s.harnessKickoffThreads.has(threadId) ||
        s.workflowLockByThread.get(threadId)?.mode === "harness"
      : false,
  )
// ChatArea:
const workflowLocked = workflowLock !== null && workflowLock.mode === "harness"
```
⚠ **BOTH DISJUNCTS OF `useHarnessLiveForThread` MUST BE RELEASED, or the run line stays `live` with
its 1s `setInterval` clock on a dead run.** `ThreadRunLine.tsx:219-241` re-fetches its frame on
`[threadId, isLive]`, so releasing the lock is also what makes the line re-read the truth.

The shipped ask reconcile the card ALREADY holds as a prop — `hooks/usePanelReconcile.ts:105-111`:
```typescript
const reconcile = useCallback(async (): Promise<void> => {
  if (!threadId) return
  const controller = new AbortController()
  await runReconcile(threadId, controller.signal)   // fetch → replacePendingAsksForThread → SHARED store
}, [threadId, runReconcile])
```
⭐ **This is why the ask half needs no new fetch code.** Both homes mount the same
`PendingAskStack`, which reads `useAskUserPrompt(threadId)` off **one** store key
(`pendingAsksByThread`). One `reconcile()` call replaces that key and BOTH homes drop the card.
</interfaces>
</context>

<diagnosis_do_not_re_derive>
The gap was **driven in a real browser on two real workflow runs** (`244-UAT.md` § *R2-4*, gap
`G-8`). Do not re-measure it; build against it.

**What is broken, mechanically:**

1. `PendingAskCard.handleSubmit` flips **component-local** `useState` to `"answered"`
   (`PendingAskCard.tsx:359-360`). The sibling home is a **different component instance** with its
   own `useState`, so it never hears.
2. The designed cross-home settle is the `ask_user_response` **SSE**, which routes to
   `removePendingAskForThread` (`StreamsProvider.tsx:1168`). On the workflow path the client is not
   subscribed to that run's stream, so the SSE never lands and the **store is never written**.
   `D-244-11`'s claim — *"one store slice … criterion 3 becomes STRUCTURAL"* — is true **of the
   store** and false **of the path that reaches it**. This plan makes the answer write through the
   store, which is what makes the claim true.
3. Nothing re-reads `GET /threads/{id}/workflow` after an answer, so `workflowLockByThread` (and
   possibly `harnessKickoffThreads`) keep a lock the server has already dropped → run line `live`,
   clock climbing, composer disabled on *"Workflow running — Cancel to switch back"*.

**Already established — do NOT spend context re-proving:**

- **The server is correct throughout.** `messages` gains `role='system'` with
  `tool_calls=[{kind:'ask_user_response', choice_index:1}]`; `workflow_runs.status → 'failed'`;
  `threads.active_workflow_run_id → NULL`.
- **The answer is persisted BEFORE the POST returns** (`backend/app/api/runs.py` Step 2, *"persist
  messages row FIRST (durability)"*). So a `getThreadPendingAsks` issued the instant the POST
  resolves is guaranteed to exclude the answered prompt — `GET /threads/{id}/ask_user/pending`
  filters `NOT EXISTS` a matching `ask_user_response` row (`backend/app/api/panel.py:190-205`).
  **There is no race on the ask half.**
- **This is NOT a data hazard and the plan must not treat it as one.** The stale *"Approve this
  step"* cannot re-send the declined action: `submit_ask_user_response`'s anchor confirm
  (`runs.py:775-785`) requires `threads.active_workflow_run_id == run_id`, and that column is NULL
  once the run settles, so a second answer 404s. The harm is **a lie on screen plus an unusable
  composer**.
- ⛔ **FRONTEND ONLY. Do not plan or make a backend change.** No file under `backend/` is in
  `files_modified`, and nothing here needs one.
</diagnosis_do_not_re_derive>

<scope_fence>
⛔ **ONE gap. `G-8`. Do not widen.**

- **`G-7` (attachment hydration) is OUT OF SCOPE by operator ruling**, deferred to `SEED-272`.
  Do not touch `tool_dispatcher.py` or `sandbox_service.py`. Do not read them.
- ⛔ **A gap-closure round may never introduce a new user-facing capability** (guardrail G-7).
  This is a reconcile fix over surfaces that already exist. **If you find yourself adding a control,
  a route, a screen or a piece of copy, you have left the scope.** The only new strings permitted in
  this plan are test names and comments.
- ⛔ **Do not refactor the six workflow-lock write sites.** `244-13` and `244-14` just finished that
  work and `__tests__/providers/workflowLockWriters.lockstep.test.ts` fences two of them by reading
  the source. **The settle path adds NO seventh derivation — it only RELEASES.** That is the single
  most important design constraint in this plan.
- ⛔ **`WorkflowRunPage`'s home was measured CORRECT during the drive.** It mounts `PendingAskCard`
  **directly** (`WorkflowRunPage.tsx:1629`), not the stack. The new callback is **optional and
  defaults to absent**, so that home is behaviourally byte-unchanged. Do not wire it there.
- ⚠ **`G-7` FIRES ON THIS PLAN AND IS OVERRIDDEN ON THE OPERATOR'S OWN RECORDED RULING — stated
  here rather than discovered at execution.** `node scripts/check-gap-closure-rounds.cjs 244` reads
  `rounds completed: 2 (cap is 2)` the moment this plan file exists (it counts the plan, not the
  run), and prints `G-7 fires`. The gate's own worded escape hatch applies, because `G-8` **is** an
  unmet ROADMAP success criterion — which is exactly the one condition G-7 says justifies a further
  round, and it is the operator's recorded ruling at the close of round 2 (`244-UAT.md` §
  *Operator rulings*). Re-run it as:

  `node scripts/check-gap-closure-rounds.cjs 244 --unmet-criterion "SC#3: an approval answered in one home does not settle it in the other, and the run line + composer keep a lock the server has already dropped"`

  → `G-7 passed WITH OVERRIDES`. ⛔ **An overridden gate is not a clear one**: record it under
  `STATE.md → Guardrail overrides` (Task 3), with the reason, so a waved-through finding can never
  later read as an absent one. **This is the last round available** — a third would need the
  operator, not a flag.
- ⛔ **The `[new-capability-in-closure]` arm must stay clear on its own merits.** This plan adds no
  control, no route, no screen and no new user-facing string; every source file it touches existed
  long before it. If the gate ever names one of this plan's files there, stop and re-read the scope
  fence rather than reaching for `--capability-approved`.
</scope_fence>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The settle path — one authoritative read that RELEASES a lock the server has dropped</name>
  <files>
    frontend/src/stores/streamsStore.ts
    frontend/src/providers/StreamsProvider.tsx
    frontend/src/__tests__/providers/streamsProvider_244_settle_ask.test.tsx
    scripts/vitest-count-gate.cjs
  </files>
  <behavior>
    Drive these RED first, against the unmodified provider, in the new fence file. Every case
    asserts the STORE, never a `console` spy — the `244-11` rule, for the same reason.

    - Test 1 (the fence can see the file at all): the provider renders and the action exists on
      `useStreamsStore.getState().actions`; calling it with a thread id does not throw.
    - Test 2 (RELEASE): the wire reports `locked: false, lock_is_stale: false,
      active_workflow_run_id: null, cap_paused: false` → after the settle,
      `workflowLockByThread.has(tid)` is `false` AND `harnessKickoffThreads.has(tid)` is `false`.
      Seed BOTH before the call, so the case proves both disjuncts of `useHarnessLiveForThread` were
      released rather than one.
    - Test 3 (FAIL-CLOSED, live anchor): the wire reports `locked: true, lock_is_stale: false,
      active_workflow_run_id: "wr-1"` → the lock map entry afterwards is the **same object
      identity** it was before (`toBe`, not `toEqual`), and the kickoff mark is untouched. This is
      the negative control that proves the settle path never SETS and never unlocks a live run.
    - Test 4 (FAIL-CLOSED, cap-paused): the wire reports `locked: false, cap_paused: true` → same
      assertion as Test 3. A cap-paused lock is a lock the person still needs.
    - Test 5 (STALE anchor releases): `locked: true, lock_is_stale: true` → released, exactly like
      Test 2. This is the shipped F2 self-heal reading, not a new rule.
    - Test 6 (a failed read changes NOTHING): `getThreadWorkflow` rejects → the lock map and the
      kickoff set are identical afterwards, and the rejection does not propagate (the caller is a
      click handler in a card with no error boundary — `BUG-260529-03`'s lesson).
    - Test 7 (RE-ATTACH, not a poll): when the wire reports a live run AND a
      `latest_producer_run_id`, the provider's producer-stream subscribe is invoked once with
      `(threadId, latest_producer_run_id)`. Assert through the mocked `subscribeToRun` call args —
      the same seam the snapshot-failure suite already drives.
    - Test 8 (no timer): a raw source sweep of the new action's body for `setInterval` /
      `setTimeout` returns zero. `ThreadRunLine.tsx`'s D-11 rule binds here — *"`WorkflowRunPage`
      already owns the polling concern; a second poller in the chat transcript would be a second
      home for it."*
  </behavior>
  <action>
Add ONE new action, `releaseSettledWorkflowLock(threadId: string): void`, and nothing else.

**(a) `frontend/src/stores/streamsStore.ts`** — declare it in the `actions` interface, directly
beneath `clearWorkflowLockForThread`, and add its synchronous no-op default beside
`clearWorkflowLockForThread: () => {}`. Type it as returning `void`, not `Promise<void>`: it is
fire-and-forget from a click handler, and a `void` return is what lets the shipped no-op stub stay a
bare arrow (the stub exists because a store consumer can fire before the provider's mount effect
registers real bodies). Give it a one-line doc comment that says what it does and, more importantly,
what it does NOT do: **it releases a lock the server has already dropped; it never sets one.**

**(b) `frontend/src/providers/StreamsProvider.tsx`** — implement it in the actions object, placed
immediately after `clearWorkflowLockForThread` so the two live together. It must:

  1. Issue exactly ONE `getThreadWorkflow(threadId)` read, in its own `try/catch`. On failure:
     `console.warn` and return, writing nothing. Mirror `refreshPhaseSpineAfterStop`'s swallow and
     its stated reason — the surface was already stale, and a rejected background read must never
     surface as an error nobody can act on.
  2. Compute the two SHIPPED guards, by reading them off the wire and NOT re-deriving anything:
     a live anchor is `wf.locked && !wf.lock_is_stale && !!wf.active_workflow_run_id`; a cap pause is
     `wf.cap_paused`. These are the exact conditions of the two `setWorkflowLockForThread` arms in
     this file's own mount reconcile — quote them by reference in the comment, do not re-derive them
     and do not copy their BODIES.
  3. If EITHER holds → **release nothing**. Then, when `wf.latest_producer_run_id` is present, call
     `subscribeProducerStreamRef.current?.(threadId, wf.latest_producer_run_id)` — the identical
     re-attach arm the mount reconcile already owns, and idempotent per its own docblock. This is
     what lets the SHIPPED terminal handler clear the lock when the resumed run actually ends,
     instead of this plan inventing a second terminal path.
  4. Otherwise → release, in this order: `actions.clearWorkflowLockForThread(threadId)`, then
     `clearStopStateForThread(threadId)`, then `refreshPhaseSpineAfterStop(threadId)`.

⚠ **Why `clearStopStateForThread` and not a hand-written Set delete:** `useHarnessLiveForThread` has
TWO disjuncts and clearing only the lock leaves the run line `live` with its 1s clock. That function
is the SHIPPED writer of `harnessKickoffThreads` (it also clears `stoppingThreads` /
`stopNotConfirmed` and disarms the stop timer, which is correct here — the server has just told us
the run is over). **Two writers of one slice that differ is how slices drift in this file**, which
`244-14`'s CR-01 already paid for once.

⚠ **Widen `refreshPhaseSpineAfterStop`'s docblock by one sentence** naming this second caller and
stating that the contract is unchanged (viewed-thread-scoped, best-effort, swallow-on-failure). Do
NOT rename it and do NOT write a second phase-spine refresher.

⛔ **THE ONE-PARAGRAPH RULE FOR THE COMMENT ON THIS ACTION, and it is the plan's central claim:**
state in the source that this path **RELEASES ONLY**, that it is deliberately not a seventh
`setWorkflowLockForThread` writer, and that the six existing write sites and their lockstep fence
(`__tests__/providers/workflowLockWriters.lockstep.test.ts`) are untouched by it. A reader who later
adds a `setWorkflowLockForThread` call here must be made to see that they are re-opening `G-1`.

**(c) The fence file** — `frontend/src/__tests__/providers/streamsProvider_244_settle_ask.test.tsx`.
Copy the harness from `streamsProvider_244_snapshot_failure.test.tsx`: `vi.hoisted` mock handles, an
ALLOW-LIST `vi.mock("@/lib/api", …)` (an omitted export throws rather than silently falling back to
the real module — keep that property), the `@/lib/supabase` auth mock, and
`renderHook(() => useStreamActions(), { wrapper: StreamsProvider })`. Reset the store slices this
plan reads in `beforeEach`, including `workflowLockByThread` and `harnessKickoffThreads`.

⛔ **Head the file with what these fences do NOT prove**, in the `244-12` register: they are jsdom
mounts over a mocked API; they prove the SETTLE PATH does what it claims when handed a wire shape.
**They cannot prove the product hands it that shape, and they cannot observe two sibling surfaces
disagreeing after a real network round trip.** That is `244-15-UAT-ROW.md`'s job.

**(d) `scripts/vitest-count-gate.cjs` — BOTH KNOBS, in this same commit.** `src/__tests__` is not a
bare-directory `TARGETS` entry anywhere in that file, so the new suite runs in NO gate until it is
named. Add the file-level `TARGETS` entry beside
`src/__tests__/providers/workflowLockWriters.lockstep.test.ts` and a `BASELINE` pin at the count you
measure. Carry the neighbours' comment verbatim in spirit: **TARGETS decides what RUNS, BASELINE
decides what is GUARDED**, and ⛔ do NOT "simplify" this to a `src/__tests__/providers` directory
entry — the fourteen inherited failures in that folder would turn the shared gate red for a reason
no plan here owns.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/__tests__/providers/streamsProvider_244_settle_ask.test.tsx</automated>
  </verify>
  <done>
    All eight cases pass; each was observed RED against the unmodified provider before the action
    existed (record the RED output in the SUMMARY — a fence nobody has seen fire is not a fence).
    The new suite is named in BOTH count-gate knobs. No `setWorkflowLockForThread` call was added
    anywhere.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: One answer, both homes — wire the stack's settle and repair the card's ungated baseline</name>
  <files>
    frontend/src/components/panel/PendingAskCard.tsx
    frontend/src/__tests__/providers/streamsProvider_244_settle_ask.test.tsx
    frontend/src/components/panel/__tests__/PendingAskCard.retired.baseline.test.tsx
    scripts/vitest-count-gate.cjs
  </files>
  <behavior>
    Added to the same fence file as Task 1. The two-homes case is the one that matters; drive it RED
    first, with the wiring absent.

    - Test 9 (THE DEFECT, both homes): under ONE `<StreamsProvider>`, mount TWO `<PendingAskStack/>`
      instances over one seeded pending ask. Assert both render a `Send Answer` control. Answer in
      the FIRST (pick a choice, click Send Answer). With `getThreadPendingAsks` now resolving `[]`,
      assert **the SECOND stack's controls are gone**. ⚠ Before the wiring exists this case is RED
      on the second stack while the first goes green — capture that asymmetry in the SUMMARY,
      because it IS the measured defect reproduced in jsdom.
    - Test 10 (the reverse direction is the same code): repeat Test 9 answering in the SECOND stack
      and asserting the FIRST clears. Two cases, not one — the UAT measured both directions
      separately and they failed for one cause; a fence over one direction is passed by a fix that
      only works one way.
    - Test 11 (a FAILED answer settles NOTHING): `answerAskUser` rejects with a 500-shaped
      `ApiError` → neither `getThreadPendingAsks` nor `getThreadWorkflow` is called, and the ask is
      still in the store. The settle runs on SUCCESS only; an answer that was refused must leave the
      card answerable.
    - Test 12 (the third home is untouched): a bare `<PendingAskCard>` with no `onAnswered` (the
      `WorkflowRunPage` shape) submits successfully and fires neither reconcile. Assert by call
      count on the mocked API, not by render.
  </behavior>
  <action>
**(a) `PendingAskCard`** — add ONE optional prop, `onAnswered?: () => void`, defaulting to
`undefined`. Document it in the same register as `runIsOver` / `action` / `service`: optional so that
**every existing caller renders exactly the card it rendered before this prop existed**, and
`PendingAskStack` is the only production caller that passes it. Call it exactly once, in
`handleSubmit`, **after** `setAnsweredValue(...)` and `setState("answered")` and **only on the
success path** — never in any `catch` arm. Wrap the call so a throwing callback cannot break the
card.

**(b) `PendingAskStack`** — build the settle callback there, because the stack is the only one of the
two that knows the thread id. It composes exactly two things and creates no third mechanism:

  1. `reconcile()` — the ask reconcile it ALREADY holds from `useAskUserPrompt(threadId)`. This is
     the whole cross-home settle: one fetch, one `replacePendingAsksForThread`, one shared store
     key, both homes drop the card. **No new fetch code is written for the asks.**
  2. `useStreamsStore.getState().actions.releaseSettledWorkflowLock(threadId)` — Task 1's action.

⛔ **Reach the action through `useStreamsStore.getState().actions`, NOT through `useStreamActions()`,
and the reason is measured rather than stylistic.** Eight suites mock `@/providers/StreamsProvider`
with an ALLOW-LIST factory and render this stack directly or through `WorkspacePanel` /
`MessageList` (`ChatArea.approval`, `MessageItem.continueButton`, `StopControl.baseline`,
`PendingAskCard`, `PendingAskCard.retired.baseline`, `WorkspacePanel`, `WorkspacePanel.derived`,
`WorkflowRunPage`). A new provider-hook import would make every one of them throw on an omitted
export. The store module is not mocked in those suites, and its no-op default action is exactly the
right behaviour for a stack mounted without a provider. Say this in the comment so nobody "tidies"
it into the hook later.

⚠ **Rule on the receipt rather than discovering it.** Once the settle lands, the answering card
UNMOUNTS instead of resting on its green *"Answered · agent resumed"* state. That is not a
regression invented here — it is the SHIPPED behaviour of the `ask_user_response` SSE path
(`PendingAskCard.tsx:22`, *"the SSE then removes the prompt from the store, reactively clearing the
card"*), which this change extends to the workflow path that never receives that SSE. Record it as a
decision in the SUMMARY, and make `244-15-UAT-ROW.md` READ what the answering home shows afterwards,
so the operator judges it on evidence rather than on our argument.

**(c) Repair the card's baseline suite — a RED fence, measured at planning time, in NEITHER
count-gate knob.** `PendingAskCard.retired.baseline.test.tsx:207` pins
`src.split("\n").length` at **737**. The file is **766** segments today, so the suite reads
**1 failed | 8 passed** — and it has been red since `d58fa43a0` (*"a dead run refuses the answer"*)
because no gate runs it. Re-derive after your edit and re-baseline **in place**, in that file's own
established style: **supersede, never overwrite** — keep the 487 → 630 → 650 → 737 chain visible and
append yours with the same one-line explanation of what grew and why. The pin stays EXACT rather
than a range, for the reason its own comment gives: its only job is to prove the `?raw` sweep is
reading the file it thinks it is.

Then **adopt that suite into both count-gate knobs** — it is in this plan's blast radius, it would
otherwise stay invisible, and its other eight cases are green (measured). ⛔ Adopt it only AFTER
re-baselining the pin: adopting a red suite turns the shared gate red. If any case other than the
line-count pin is red after your edit, do NOT adopt — name it in the SUMMARY instead, the way
`243-03` named the fourteen it declined.

⚠ `PendingAskCard.test.tsx` (the main suite, already in both knobs, pinned at **45**) mocks
`@/providers/StreamsProvider` as an allow-list and mounts the STACK. Confirm it still passes; if its
count grew, raise its pin to the measured actual. A pin is a floor, never a census.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/__tests__/providers/streamsProvider_244_settle_ask.test.tsx src/components/panel/__tests__/PendingAskCard.test.tsx src/components/panel/__tests__/PendingAskCard.retired.baseline.test.tsx src/components/panel/__tests__/WorkspacePanel.test.tsx src/pages/WorkflowRunPage.test.tsx</automated>
  </verify>
  <done>
    Tests 9-12 pass, and 9 and 10 were each observed RED on the sibling stack before the wiring.
    All five suites above are green. `PendingAskCard.retired.baseline.test.tsx` is re-baselined and
    (if fully green) named in both count-gate knobs. The card gained exactly one optional prop and
    no new copy.
  </done>
</task>

<task type="auto">
  <name>Task 3: The browser row, the ledger, and the gates — reported as BUILT, DRIVE OWED</name>
  <files>
    .planning/phases/244-the-chat-shell-and-the-composer/244-15-UAT-ROW.md
    docs/HOT-FILE-LEDGER.md
    CLAUDE.md
    .planning/STATE.md
  </files>
  <action>
**(a) Write `244-15-UAT-ROW.md`**, in the exact shape of `244-12-UAT-ROW.md` (read it first): a
`yaml` header (`plan: 244-15`, `gap: G-8`, `closes: SHELL-03`, `severity: major`, `status: pending`,
`driven_by: pending`, `driven_on: pending`), the fixture, the operator-safety rule, one `arm_` block
per arm with `pending` fields, and a closing *"what a PASS does and does not mean"*.

⛔ **Carry the operator-safety rule forward verbatim in force:** the fixture step (200-Word Essay
Writer, step 2 `"act"`, `send_email` over SMTP) is **outward-facing and irreversible**. `L-4` and
`R2-4` both settled with **"Do not run it" + Send Answer** and never clicked *"Approve this step"*.
Hold that discipline. A row that sends a real email to prove a button works has bought its evidence
at the wrong price.

The arms, with their scoring rules stated in the row itself:

  - **Arm 1 — THREAD → PANEL.** Arm a real approval, answer in the CHAT column, then read the
    PANEL with **no manual refresh**, at the moment and again at ~30s. ⛔ **Score by the rendered
    SENTENCE and by POSITION against the panel edge** — read `aside.left`, then each control's
    `left`, exactly as `R2-4` did (`aside.left = 1136.8`; chat-column controls measured 546.8 /
    988.4; panel copies 1158.4 / 1298.8). ⛔ **Never by `data-testid` presence** — presence
    assertions cannot see content drift, and `R2-4`'s panel was still rendering *"NEEDS YOU — No
    deadline — the run is waiting for your answer and will not continue on its own"* three minutes
    after the answer was accepted. Quote the sentence you read.
  - **Arm 2 — PANEL → THREAD.** The mirror. Arm a second approval, settle it in the PANEL, then
    read the chat column. `R2-4` measured all three controls still at 546.8 / 988.4 at +6s and +20s.
  - **Arm 3 — the run line and the composer, in BOTH directions.** Read
    `data-run-line-state` (it must stop being `live`, and the clock must stop climbing) and the
    composer (it must become usable; it read disabled on *"Workflow running — Cancel to switch
    back"*). Record the time between the answer and the reading — the release is gated on the
    server having dropped the anchor, so a few seconds is expected and a stuck reading at +60s is a
    fail. ⚠ Include the honest limit as a field: if the wire reported the run still live at settle
    time and carried no `latest_producer_run_id`, the release waits for the next thread switch —
    record which arm you were in rather than scoring it silently.
  - **Arm 4 — the receipt.** What does the ANSWERING home show after it settles? The card now
    unmounts rather than resting on green. Record what the person actually sees, verbatim. This is
    the arm that tells the operator whether the decision in Task 2 was right.
  - **Arm 5 — the third home.** Open `WorkflowRunPage` for the same run: its own `PendingAskCard`
    must render exactly as it did at `R2-4` (which measured it PASS at left 1144 / 1144 / 1288.4,
    with an advancing spine). This is a regression check on a cross-surface shell with THREE homes
    (`D-244-13`).

⛔ **Head the file with the same warning `244-12-UAT-ROW.md` carries:** this plan's fences prove the
settle path settles when handed a wire shape; **not one of them proves the product hands it that
shape**, and a jsdom mount cannot observe two sibling surfaces disagreeing after a real network
round trip. ⚠ And carry `D-244-21` / `OV-SOLO-01`: Gemini is unavailable, so this build is
**self-verified** and driving this row is the first genuinely independent check it receives.

**(b) Ledger — same-commit sync rule, both registers.** Re-derive the triple for every non-test
source file this plan modified, with CLAUDE.md's recipe (and subtract the six-digit dated-quick-task
buckets). At planning time all three rows were ACCURATE — `StreamsProvider.tsx` **100 / 37 / 4727**,
`streamsStore.ts` **20 / 13 / 525**, `PendingAskCard.tsx` **14 / 7 / 765** — so your commits are
what stale them. Update the CLAUDE.md row (verdict only, **≤ 200 chars**, enforced by
`check-claude-md-size.cjs`) and append the file's section in `docs/HOT-FILE-LEDGER.md` **in the same
commit**, headed `— 244-15`. Each section states the honoured-by-construction measurement rather
than asserting it: for `PendingAskCard.tsx`, one optional prop and one composed callback — count
`useState[(<]` before and after (it must not grow; the card already carries nine pieces of state and
a tenth would be new state ownership on a cross-surface shell); for `StreamsProvider.tsx`, one
action that RELEASES and zero new `setWorkflowLockForThread` call sites (`grep -c` it and publish
the figure); for `streamsStore.ts`, one type and one no-op stub. Also update
`scripts/vitest-count-gate.cjs`'s row if its triple moved.

**(c) Gates — run all four from the repo root, read the verdict line VERBATIM, never a summary.**

  - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` — the contract is *no per-file
    DECREASE* and *zero failing*, **never a fixed total**. ⛔ Do not trust any number written in
    CLAUDE.md, STATE.md or the ROADMAP — that constant has rotted six times; re-derive it.
    ⚠ If it reds, capture the failing FILENAMES from the gate's own persisted JSON **before**
    re-running anything, check each against `git diff --numstat`, and if a red file is
    byte-unchanged and one of `SEED-171`'s five, record it as an observation. **Do not reach for the
    worker cap** — that causal claim is refuted (`SEED-171`). And say *"provably unmodified"*, never
    *"fine"*: one green sample of a flaky suite proves nothing.
  - `node scripts/check-hot-file-ledger.cjs 244` — must exit 0. ⚠ Phase 242 measured this gate
    passing VACUOUSLY over a CRLF plan file (0 parsed files, exit 0), and **your worktree will
    very likely have this plan in CRLF** — `core.autocrlf` converts on checkout, and the file was
    authored LF (0 CR bytes, measured). So an exit code alone is NOT evidence here. **Measured at
    planning time, on the LF original, with this plan present: `subject: 66 files · watched: 30`.**
    If your run prints a materially smaller `subject:` count, the gate parsed fewer plans than
    exist and its green is vacuous — fix the parse, do not accept the exit code.
  - `node scripts/check-claude-md-size.cjs` — must exit 0, including the 200-char disposition cap.
  - `cd frontend && npx tsc -p tsconfig.app.json --noEmit` — ⛔ **NOT `npx tsc --noEmit`**, which
    type-checks ZERO files and exits 0 no matter what is broken (`frontend/tsconfig.json` is
    solution-style, `{"files": [], "references": […]}`; it reported green over 24 real `TS6133`
    errors in Phase 239). Measure a **SET DIFF**: capture the base error set BEFORE your first edit
    and compare. The app config reports errors at base, so *"zero errors"* is not a reachable
    criterion and must not be written as one. **Zero NEW errors is the criterion.**

**(d) Record the `G-7` override in `STATE.md`.** The gate waved this round through on a worded
reason, not on the absence of a finding, and it says so itself: `G-7 passed WITH OVERRIDES`. Add
an entry under `STATE.md → Guardrail overrides` naming the rule (`G-7 round-cap`), the phase, the
criterion (`SC#3`, second clause) and the operator ruling it rests on (`244-UAT.md` § *Operator
rulings — 2026-09-12*). ⛔ Hand-edit `STATE.md`; do NOT call the `state.*` SDK verbs — seven of
them write false records and corrupted that file five times during Phase 190 alone while
reporting success.

**(e) Report the outcome as BUILT, DRIVE OWED.** In the SUMMARY and in any ROADMAP/STATE update:
`SHELL-03` does **not** close here. `G-8` is *built, `244-15-UAT-ROW.md` owed*. `G-7` remains
deferred to `SEED-272`. State plainly that this is a **self-verification, not a review**
(`D-244-21`).
  </action>
  <verify>
    <automated>node scripts/check-hot-file-ledger.cjs 244 && node scripts/check-claude-md-size.cjs && GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>
    `244-15-UAT-ROW.md` exists with five arms, every verdict `pending`, both directions scored by
    position and by sentence. Every modified source file's CLAUDE.md row and
    `docs/HOT-FILE-LEDGER.md` section were updated in the same commit. All four gates read green
    (or a red is recorded with its filenames captured before any re-run). The SUMMARY says
    **built, drive owed**.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `GET /threads/{id}/workflow` | owner-scoped read; the client decides what to believe from it |
| browser → `GET /threads/{id}/ask_user/pending` | owner-scoped read behind `_verify_thread_ownership` + explicit `thread_id` filter + RLS |
| client store → composer / run-line render | the lock is the ONLY thing standing between a live harness run and a composer that will 409 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-244-15-01 | Elevation of Privilege | `releaseSettledWorkflowLock` in `StreamsProvider.tsx` | mitigate | ⛔ The elevation `T-244-03-01` named: unlocking the composer during a LIVE harness run. The release arm is taken ONLY when neither shipped set-arm's guard holds; a live anchor OR `cap_paused` releases nothing. Fail-closed by construction and driven by Tests 3, 4 and 5 (including a cap-paused case, so the cheap direction is the default) |
| T-244-15-02 | Tampering | the six `setWorkflowLockForThread` write sites | mitigate | The settle path adds no seventh derivation — it writes no lock. `workflowLockWriters.lockstep.test.ts` stays green and untouched; the plan's source comment names the re-open hazard so a later editor sees they would be re-opening `G-1` |
| T-244-15-03 | Information Disclosure | the two GETs the settle issues | accept | Both are owner-scoped server-side (`_verify_thread_ownership`, `.eq("user_id", …)`, RLS on the per-request user JWT) and 404 rather than 403, so no existence leak. The client adds no new parameter and reaches no new route |
| T-244-15-04 | Denial of Service | the settle fetches | accept | ≤ 3 GETs, fired by an explicit human answer, bounded by the number of answers. **No timer and no poller** — fenced by Test 8. The re-attach arm is idempotent per its own shipped docblock |
| T-244-15-05 | Repudiation | the answering home's receipt | accept | The answer is persisted server-side as a `role='system'` message before the POST returns; the card's green state was always transient on the SSE path. Arm 4 of the UAT row READS what the person is left with rather than assuming it is enough |
| T-244-15-SC | Tampering | npm/pip/cargo installs | accept | **No package-manager install task exists in this plan.** No dependency is added, upgraded or pinned; `package.json` is not in `files_modified`. Nothing to audit, so no legitimacy checkpoint is owed |
</threat_model>

<verification>
1. `frontend/src/__tests__/providers/streamsProvider_244_settle_ask.test.tsx` passes, and Tests 2,
   3, 9 and 10 were each observed RED before their fix — quoted in the SUMMARY.
2. `grep -c "setWorkflowLockForThread(" frontend/src/providers/StreamsProvider.tsx` is unchanged
   from base. Publish both figures.
3. `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root: no per-file
   decrease, zero failing. Publish the verdict line verbatim.
4. `node scripts/check-hot-file-ledger.cjs 244` exits 0 AND its `subject:` / `watched:` counts
   include this plan's files.
5. `cd frontend && npx tsc -p tsconfig.app.json --noEmit`: zero NEW errors against the base set.
6. `node scripts/check-gap-closure-rounds.cjs 244` still reports no new capability built inside a
   closure round (this plan adds no control, route or screen).
</verification>

<success_criteria>
- One settle path exists, it is fetch-based, and it RELEASES only. Zero new lock writers.
- Both homes drop the card from ONE store write, because both read one slice — `D-244-11`'s
  structural claim is true of the path as well as of the store.
- The run line and the composer release on the server's word, and refuse to release without it.
- `WorkflowRunPage`'s home is behaviourally byte-unchanged.
- A red, ungated card baseline fence is re-baselined and brought inside the gate.
- `244-15-UAT-ROW.md` exists, unrun, with both directions and the run-line/composer readings.
- ⛔ The phase reports **G-8 built, drive owed**. `SHELL-03` does not close here.
</success_criteria>

<output>
Create `.planning/phases/244-the-chat-shell-and-the-composer/244-15-SUMMARY.md` when done.

It must record, at minimum: the RED output for each fence before its fix; the before/after
`setWorkflowLockForThread` count; the four gate verdict lines verbatim; the re-derived ledger
triples; the decision about the answering home's receipt; and the sentence **built, drive owed —
self-verified, not reviewed**.
</output>
