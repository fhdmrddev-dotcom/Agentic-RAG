---
phase: 244-the-chat-shell-and-the-composer
plan: 09
type: execute
wave: 1
# G-7's detector (scripts/check-gap-closure-rounds.cjs) keys on `gap_closure`, NOT on
# `gap_closure_round`. Measured while authoring this plan set: with only the round field
# present the gate read `0 gap-closure` and a future round 2 would never have been capped.
gap_closure: true
gap_closure_round: 1
depends_on: []
files_modified:
  - frontend/src/components/layout/NavPanel.tsx
  - frontend/src/components/layout/__tests__/ChatLayout.scrollFrame.test.tsx
  - scripts/vitest-count-gate.cjs
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md
  - .planning/phases/244-the-chat-shell-and-the-composer/244-09-UAT-ROW.md
autonomous: true
requirements: [SHELL-01]

must_haves:
  truths:
    - "The page ROOT does not overflow at a viewport height BELOW 540px — h=436 and h=516, the two heights the UAT measured failing, both read scrollHeight <= clientHeight on #root."
    - "The nav rail scrolls its OWN content when it cannot fit, instead of pushing the document."
    - "The three heights that already passed (576, 696) still pass, panel OPEN and CLOSED — the fix must not trade one height for another, which is the ROADMAP's named failure mode."
    - "The rail's bounding rect is still unchanged after the message list scrolls (the L-1 property that already held must keep holding)."
    - "NavPanel.tsx's ledger row carries a RE-DERIVED triple, and the row's own claim about itself is corrected rather than repeated."
  artifacts:
    - path: "frontend/src/components/layout/NavPanel.tsx"
      provides: "a rail root that bounds and scrolls its own content"
      contains: "overflow-y-auto"
    - path: "frontend/src/components/layout/__tests__/ChatLayout.scrollFrame.test.tsx"
      provides: "link 6 — the rail column's source pin, driven RED by deleting the token"
    - path: ".planning/phases/244-the-chat-shell-and-the-composer/244-09-UAT-ROW.md"
      provides: "the browser-driven re-drive row that is the ACTUAL acceptance for this gap"
  key_links:
    - from: "frontend/src/components/layout/NavPanel.tsx"
      to: "the document root"
      via: "the rail root's own overflow, so the mt-auto footer block cannot escape to the page scrollbar"
      pattern: "overflow-y-auto"
---

<objective>
Close **G-5**: below a viewport height of ~540px the page ROOT overflows and the whole page
scrolls — `BUG-260828-08`'s exact complaint and the ROADMAP's exact named failure mode
(*"fixed at one window height and the dead space returns at another"*).

The cause is measured to the element and it is **not** the transcript and **not** the panel:
`NavPanel.tsx:202-208`'s rail root is `hidden md:flex flex-col h-full shrink-0 … py-3` with
computed `overflow-y: visible` and `min-height: auto`, so its `mt-auto` footer block
(`NavPanel.tsx:290`, 128px tall) measures `bottom = 540px` and escapes to the document root.
244-01's `min-h-0` chain landed at five sites, **all in the message column**; the sibling RAIL
column got neither treatment.

Purpose: SHELL-01's first success criterion is *"no dead space opens under the composer **at any
window height**"*. It is currently false at two of six measured samples.
Output: a bounded rail, a source pin on it, a re-derived ledger row, and the browser row that is
the only thing that can actually score this criterion.

⛔ **THIS ROUND DOES NOT RE-OPEN SHELL-05** (its L-7 row passed in both directions on 2026-09-12)
**and does not re-open the parts of SHELL-01 that passed** — four of L-1's six samples, the rail's
zero-drift reading across four real trusted scrolls, and the zero `scrollIntoView` calls. This plan
touches ONE column of the shell.

**Solo run (D-244-21 / OV-SOLO-01):** Gemini is unavailable. Everything this plan verifies is a
**SELF-verification**, never a review. Say so in the SUMMARY.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⛔ **FIRST ACTION:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`, then `git rev-parse HEAD` —
MUST be `760c758fc` or a descendant on `develop`. ⚠ A fresh worktree starts on the DEFAULT branch
(`master`, stuck at the v3.9 ship commit); five consecutive executors had to reset. Assert the SHA
before reading anything else.
⛔ NEVER `rm -rf` a worktree — `bash scripts/teardown-worktree.sh <path>`.

**⚠ Gate state, inherited and non-negotiable:**
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs`, **run from the repo root**.
  Contract: *no per-file DECREASE* + *zero failing*. A bigger grand total is the gate WORKING.
- ⛔ Capture failing FILENAMES from the gate's own persisted JSON **BEFORE** re-running anything.
  `SEED-171`'s five cap-independent flaky suites exist; `sourceComposition.test.tsx` is a standing
  red in NEITHER knob. ⛔ Never touch the cap to "fix" a red.
- ⛔ `npx tsc --noEmit` in `frontend/` checks **ZERO files** (solution-style config) and is vacuous.
  Use `npx tsc -p tsconfig.app.json --noEmit` and measure a **SET DIFF** against the ~67-error base.
  "Zero errors" is not a reachable criterion here.

**⛔ REGISTRY MERGE ORDER.** This wave runs `244-09` and `244-10` concurrently. Both append into
`docs/HOT-FILE-LEDGER.md` and `CLAUDE.md` (`244-09` also into `scripts/vitest-count-gate.cjs`;
`244-10` does not). *"Take both sides"* is a policy, not a mechanism:
1. Merge `244-09` first, whole.
2. Rebase `244-10` onto that merge and **re-apply its ledger row + section by hand**. ⛔ Do not
   accept an automatic resolution on the registry files.
3. Re-run from the repo root AFTER the second merge: `node scripts/check-hot-file-ledger.cjs 244`,
   `node scripts/check-claude-md-size.cjs`, then the wave's single full count gate.
⛔ A row lost to a bad auto-merge is SILENT (`[no-row]` fires only for files a plan NAMES).
⛔ A row added twice fails `[duplicate-row]` — one file → exactly one task.

**⚠ G-2 (sketch-before-plan) does NOT fire.** This is a bug fix on a shipped surface with a cause
named at `file:line`, exactly as `D-244-18` requires plans to say explicitly rather than sketching
everything or nothing. No sketch is owed and none should be produced.
</execution_context>

<context>
@.planning/phases/244-the-chat-shell-and-the-composer/244-UAT.md
@.planning/phases/244-the-chat-shell-and-the-composer/244-CONTEXT.md
@.planning/ROADMAP.md
@CLAUDE.md

⛔ **Read the `G-5` YAML block at the bottom of `244-UAT.md` and test row 2 (L-1) in full** — the
sample table, the `root_cause` block, the TWO recorded measurement corrections
(`correction_recorded_not_overwritten`, `instrument_correction_recorded_not_overwritten`) and
`fix_shape`. Both corrections are load-bearing for the re-drive:
- the composer gap is **37.0px at all six samples** and is NOT the failing measure;
- ⛔ `document.scrollingElement.scrollHeight` is the **WRONG instrument** in this browser — the
  automation extension injects nodes into `<body>` and it measured 1522 at a viewport of 696. The
  correct instrument is `document.getElementById('root').scrollHeight` vs its own rect height.

@frontend/src/components/layout/NavPanel.tsx
@frontend/src/components/layout/__tests__/ChatLayout.scrollFrame.test.tsx
@docs/HOT-FILE-LEDGER.md

<interfaces>
The rail root as shipped (`NavPanel.tsx:202-208`) — the exact expression this plan edits:

  <div className={cn(
    "hidden md:flex flex-col h-full shrink-0 bg-sidebar border-r border-border/20 py-3 motion-safe:transition-[width] motion-safe:duration-300",
    expanded ? "w-[210px] items-stretch px-2" : "w-[58px] items-center",
  )}>

The escaping child (`NavPanel.tsx:290`) — the `mt-auto` footer block, measured bottom 540px:

  <div className={cn("flex flex-col gap-1 mt-auto", expanded ? "items-stretch" : "items-center")}>

The sibling that already got the treatment, for contrast (measured on the live surface):
the workspace `<aside>` carries `flex h-screen min-h-0 min-w-0 flex-col overflow-hidden`.

The existing fence this plan extends — `ChatLayout.scrollFrame.test.tsx` — already owns links 2-5
of the `min-h-0` chain, imports source via `?raw`, normalises CRLF with its own `lf()` helper and
states in its own docblock that **a fence alone is a presence assertion and does not satisfy
SHELL-01**. Link 6 belongs in that file, under that same disclaimer.
</interfaces>
</context>

<tasks>

<task type="execute">
  <name>Task 1: Bound the rail — the one column of the shell that got neither treatment</name>
  <files>frontend/src/components/layout/NavPanel.tsx, frontend/src/components/layout/__tests__/ChatLayout.scrollFrame.test.tsx, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `.planning/phases/244-the-chat-shell-and-the-composer/244-UAT.md` — test row 2 (L-1) end to end, and the `G-5` YAML block
    - `frontend/src/components/layout/NavPanel.tsx` — the whole file, once
    - `frontend/src/components/layout/__tests__/ChatLayout.scrollFrame.test.tsx` — the whole file, once; links 2-5 are the shape link 6 must match
    - `docs/HOT-FILE-LEDGER.md` § `frontend/src/components/layout/NavPanel.tsx`
  </read_first>
  <action>
Add `min-h-0 overflow-y-auto` to the FIXED half of the rail root's class list at
`NavPanel.tsx:205` — the string that already reads
`"hidden md:flex flex-col h-full shrink-0 bg-sidebar border-r border-border/20 py-3 motion-safe:transition-[width] motion-safe:duration-300"`.
⛔ Do NOT touch the `expanded ? … : …` half; width and padding are not this defect.

⚠ Name which token is load-bearing and which is defensive, in the code comment, because a comment
that claims more than it can is how this ledger's own rows go wrong. **`overflow-y-auto` is the
fix**: the rail's box is ALREADY bounded at the viewport (`h-full` inside `div.flex.h-screen`,
measured `height = viewport`), so the excess escapes purely because the computed overflow is
`visible`. `min-h-0` is carried for symmetry with the five sites 244-01 established and because
the automatic-minimum-size rule is direction-dependent — it costs nothing and removes a future
question.

⛔ **Do NOT add a `min-height` to the page, to `#root`, or to any ancestor.** That makes the page
scroll deliberately, which IS the bug (`fix_shape`, stated).
⛔ **Do NOT delete, shorten or re-order the `mt-auto` footer block** (`:290`) or any RailItem. The
footer is not too big; the rail could not scroll.
⛔ **Do NOT touch the mobile drawer.** L-1 measured the DESKTOP rail (`hidden md:flex`); nothing
here has observed the drawer, and a change nobody measured is not a fix.

Then add **link 6** to `ChatLayout.scrollFrame.test.tsx`, in the shape links 2-5 already use:
import `NavPanel.tsx?raw`, pass it through the file's own `lf()` normaliser, `.match()` the rail
root's fixed class string, and assert it contains `h-full`, `min-h-0` AND `overflow-y-auto`.
Include the file's existing not-found guard message style so a class-list rewrite reds the fence
rather than silently matching nothing.

⛔ **Drive link 6 RED first** — delete `overflow-y-auto` from the source, run the single suite,
record the failure text verbatim in the SUMMARY, restore, run green. A fence nobody has seen fire
is not a fence.

Extend link 6's docblock with the one sentence this suite's existing disclaimer does not yet
carry: **jsdom performs no layout, so link 6 pins that the token cannot be deleted silently and
proves NOTHING about whether the page overflows.** The pixels are in `244-09-UAT-ROW.md`.

Bump `ChatLayout.scrollFrame.test.tsx`'s `BASELINE` entry in `scripts/vitest-count-gate.cjs` from
`5` to its new case count, and attribute the `+N` in the SUMMARY. The suite is already in
`TARGETS` (`src/components/layout/__tests__/ChatLayout.scrollFrame.test.tsx`) — ⚠ confirm it, do
not assume: a suite in one knob and not the other runs while guarding nothing.
  </action>
  <acceptance_criteria>
    - `frontend/src/components/layout/NavPanel.tsx` line 205's fixed class string contains all three of `h-full`, `min-h-0`, `overflow-y-auto`; `grep -c "overflow-y-auto" frontend/src/components/layout/NavPanel.tsx` returns exactly `1`.
    - `grep -c "mt-auto" frontend/src/components/layout/NavPanel.tsx` is UNCHANGED from its pre-edit value (the footer block was not altered).
    - `git diff --stat -- frontend/src/components/layout/NavPanel.tsx` shows a ONE-line source change (plus comment lines).
    - `git diff -- frontend/src/components/layout/ChatLayout.tsx frontend/src/components/chat/ChatArea.tsx frontend/src/components/chat/MessageList.tsx` is **EMPTY** — the five 244-01 sites are untouched.
    - Link 6 was seen RED: the SUMMARY quotes the verbatim assertion failure produced by deleting `overflow-y-auto`, and the file is restored (`git diff` of the source after restore is limited to the intended one-line change).
    - `GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/layout/__tests__/ChatLayout.scrollFrame.test.tsx` — `failed 0`, case count equals the new `BASELINE` value.
    - `npx tsc -p tsconfig.app.json --noEmit` — error SET diff against the base capture is EMPTY in both directions.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/layout/__tests__/ChatLayout.scrollFrame.test.tsx</automated>
  </verify>
  <done>The rail root carries `min-h-0 overflow-y-auto`; link 6 pins it, was driven RED, and is registered in BOTH gate knobs.</done>
</task>

<task type="execute">
  <name>Task 2: The ledger row NavPanel.tsx has been wrong about, and the browser row that is the real acceptance</name>
  <files>docs/HOT-FILE-LEDGER.md, CLAUDE.md, .planning/phases/244-the-chat-shell-and-the-composer/244-09-UAT-ROW.md</files>
  <read_first>
    - `docs/HOT-FILE-LEDGER.md` § `frontend/src/components/layout/NavPanel.tsx` — its whole section
    - `CLAUDE.md` § the G-5-FIRING scan-list table row for `frontend/src/components/layout/NavPanel.tsx` (reads `20 / 11 / 329`)
    - `.planning/phases/244-the-chat-shell-and-the-composer/244-UAT.md` — L-1's `samples`, `rail_and_scroll_properties_all_pass`, and BOTH `*_correction_recorded_not_overwritten` blocks
    - `.planning/phases/244-the-chat-shell-and-the-composer/244-CONTEXT.md` — D-244-19 (SHELL-01 closes on a MEASURED bound, not a screenshot) and D-244-20
  </read_first>
  <action>
**(a) Correct the ledger, and correct a claim ABOUT the ledger.**

⚠ **`244-UAT.md`'s G-5 block says NavPanel.tsx has no ledger row. That claim is STALE and must not
be acted on** — the row was added at Phase 235 and `node scripts/check-hot-file-ledger.cjs --files
frontend/src/components/layout/NavPanel.tsx` exits `0`. ⛔ **Do not add a second row**;
`[duplicate-row]` is a gate failure.

What IS true is that the row's TRIPLE is stale. Re-derive it with CLAUDE.md's own recipe, from the
repo root, **subtracting six-digit dated quick-task buckets** (`260814` etc. are not phases):

  git log --oneline -- frontend/src/components/layout/NavPanel.tsx | wc -l
  git log --format=%s -- frontend/src/components/layout/NavPanel.tsx | sed -E 's/^[a-z]+\(([^)]+)\).*/\1/' | sed -E 's/-.*//' | grep -E '^[0-9]+(\.[0-9]+)?$' | grep -vE '^[0-9]{6}$' | sort -u | wc -l
  wc -l frontend/src/components/layout/NavPanel.tsx

Measured at planning time: **21 / 11 / 344** against a row reading `20 / 11 / 329`. Re-derive
anyway — this file's own recurring finding is that a figure written at a close goes stale on the
next commit, and this plan adds one.

Update the row in `CLAUDE.md`'s scan-list table **and** the section in `docs/HOT-FILE-LEDGER.md`
**in the SAME COMMIT** (the same-commit sync rule). ⛔ The disposition cell is capped at **200
characters** by `scripts/check-claude-md-size.cjs` — put the VERDICT in the cell and every reason
in the section. Record the new triple BESIDE the old, never over it.

The section must carry the finding, not just the number: **this is the file this phase's own first
criterion turned on, and it sat outside the G-5 audit for eleven phases** before Phase 235 added
its row — so when 244-01 swept the shell for unbounded flex children it swept the column the
ledger pointed at and not the one that was broken. Name the seam the next refactor should take
(the rail's three regions — logo/toggle, the nav list, the `mt-auto` footer — are one flat flex
column with no scroll owner; the nav list is the part that should own the scroll if the rail ever
grows again).

**(b) Write the browser row that is the actual acceptance.**

⛔ **G-5 CAN NEVER BE A VITEST FENCE** — jsdom performs no layout. Per `D-244-19`, SHELL-01 closes
on a MEASURED bound. Create `244-09-UAT-ROW.md` holding ONE row, ready to drive, specifying:

- **Instrument:** `document.getElementById('root').scrollHeight` vs its own
  `getBoundingClientRect().height`; cross-check `document.body.scrollHeight`.
  ⛔ **NOT `document.scrollingElement`** — the automation extension injects nodes into `<body>`
  and that instrument measured `1522` at a viewport of `696`. The UAT records catching itself on
  exactly this; repeating it would publish a defect that does not exist.
- **Samples:** 8 — viewport heights **436, 516, 576, 696** × panel **CLOSED** and **OPEN**.
  ⚠ `436` and `516` are the two the UAT measured FAILING (+104px and +24px); `576` and `696`
  already passed and are carried as the anti-regression half, because the ROADMAP's failure mode
  is *"fixed at one window height and the dead space returns at another"*.
- **Pass condition per sample:** `rootScrollHeight <= rootClientHeight`; composer gap `37.0px`
  (± 1px) — the value measured at all six prior samples, carried as a control, ⛔ NOT as the
  failing measure; footer gap `12.0px` (± 1px).
- **The rail-still-holds half:** on the 436 sample, scroll the message list with a **REAL trusted
  scroll** (a synthetic `WheelEvent` moved the list 0px and was discarded — the UAT records this),
  assert the list actually moved BEFORE reading the rail, then assert a LEAF element inside the
  rail has `dTop 0 / dLeft 0`. ⚠ `scrollTop` is NOT the reader's position — measure a leaf
  element's bounding rect. Instrument `Element.prototype.scrollIntoView` for the whole run and
  assert **0** app calls.
- **The new-behaviour half:** at `h=436`, assert the RAIL element itself now has
  `scrollHeight > clientHeight` — i.e. the overflow moved INTO the rail rather than disappearing.
  ⛔ Without this the row cannot distinguish "the rail scrolls" from "content was deleted".
- **Fixture:** a long transcript. `261d5f57-36fb-40ec-bb0b-1c72b7550350` ("UAT 243 L-2 — long
  thread", 78 messages) is the one L-1 used and is still present in the local DB.

⛔ **The row is authored here and DRIVEN at `/gsd:verify-work`** — this plan does not claim it.
Write the verdict fields as `pending` with the driver named, never pre-filled.
  </action>
  <acceptance_criteria>
    - `node scripts/check-hot-file-ledger.cjs 244` exits `0` and its output reads `subject: N files · watched: N` with N > 0 — ⛔ a run reporting `subject: 0 files` passed VACUOUSLY (it happened on a CRLF plan at Phase 242); assert the parsed count, not just the exit code.
    - `grep -c "frontend/src/components/layout/NavPanel.tsx" CLAUDE.md` returns exactly `1` (no duplicate row).
    - `CLAUDE.md`'s NavPanel row carries the re-derived triple and its disposition cell is <= 200 characters.
    - `docs/HOT-FILE-LEDGER.md` has exactly one `NavPanel.tsx` section and it names the eleven-phase invisibility, the new triple beside the old, and the named seam.
    - `node scripts/check-claude-md-size.cjs` exits `0` (no `[disposition-too-long]`, `[duplicate-row]`, `[malformed-row]`).
    - `.planning/.../244-09-UAT-ROW.md` exists, specifies 8 samples at the four named heights × panel open/closed, names `#root` as the instrument, explicitly forbids `document.scrollingElement` with the 1522-at-696 reason, and carries the rail-overflows-internally assertion.
    - Every verdict field in the row file reads `pending` — the file contains no `pass` verdict.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-hot-file-ledger.cjs 244 && node scripts/check-claude-md-size.cjs</automated>
  </verify>
  <done>NavPanel.tsx's row and section carry a re-derived triple and the real finding; the browser row that scores this criterion exists and is unfilled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | This plan changes one CSS class list, one `?raw` source fence and three documentation files. No input parsing, no network call, no persistence, no auth decision, no new dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-244-09-01 | Denial of Service | `NavPanel.tsx` rail root | mitigate | `overflow-y-auto` bounds the rail; a future footer item cannot push the document. The browser row asserts the rail's OWN `scrollHeight > clientHeight` at h=436, so "no overflow" cannot be achieved by losing content. |
| T-244-09-02 | Tampering | `ChatLayout.scrollFrame.test.tsx` | mitigate | The link-6 pin makes silent deletion of `overflow-y-auto` a gate failure; driven RED against the deleted token before it is trusted. |
| T-244-09-03 | Repudiation | `docs/HOT-FILE-LEDGER.md` / `CLAUDE.md` | mitigate | Same-commit sync rule + `check-claude-md-size.cjs` (`[duplicate-row]`, `[disposition-too-long]`) + `check-hot-file-ledger.cjs` with the parsed-count assertion, so a vacuous pass cannot read as clear. |
| T-244-09-SC | Tampering | npm/pip/cargo installs | n/a | **No package is installed by this plan.** `git diff -- frontend/package.json frontend/package-lock.json backend/requirements.txt` MUST be empty; no legitimacy audit is owed because no dependency is added. |
</threat_model>

<verification>
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the **repo root**: `failed 0`,
  no per-file DECREASE, and the `+N` on `ChatLayout.scrollFrame.test.tsx` fully attributed to this
  plan's new cases with **no residual**. ⛔ Capture failing filenames from the gate's own JSON
  before any re-run; check each named file against `git diff --numstat` before calling it inherited.
- `npx tsc -p tsconfig.app.json --noEmit` — SET diff against the base capture empty both ways.
- `node scripts/check-hot-file-ledger.cjs 244` — exit 0 AND a non-zero parsed subject count.
- `node scripts/check-claude-md-size.cjs` — exit 0.
- ⛔ **The backend is untouched**: `git diff --stat -- backend/ supabase/` is EMPTY, so the
  zero-headroom backend baseline (71) is not at risk from this plan and is not re-run for it.
- ⛔ **NOT verified here and stated as owed:** the pixels. `244-09-UAT-ROW.md` is driven at
  `/gsd:verify-work`. This plan may not report SHELL-01 as closed.
</verification>

<success_criteria>
- The rail root carries `min-h-0 overflow-y-auto`; no other source file changed.
- Link 6 exists, was seen RED with its failure text quoted, and is in BOTH gate knobs.
- NavPanel.tsx's ledger row + section carry a re-derived triple and the eleven-phase finding, in
  one commit; both registry gates exit 0 with a non-vacuous parse.
- `244-09-UAT-ROW.md` exists with 8 unfilled samples and the corrected instrument.
- The SUMMARY states plainly that SHELL-01 is **built, drive owed** — never closed.
</success_criteria>

<output>
Create `.planning/phases/244-the-chat-shell-and-the-composer/244-09-SUMMARY.md` when done.
</output>
