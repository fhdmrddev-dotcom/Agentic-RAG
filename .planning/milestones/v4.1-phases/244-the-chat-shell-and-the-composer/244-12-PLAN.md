---
phase: 244-the-chat-shell-and-the-composer
plan: 12
type: tdd
wave: 2
# G-7's detector (scripts/check-gap-closure-rounds.cjs) keys on `gap_closure`, NOT on
# `gap_closure_round`. Measured while authoring this plan set: with only the round field
# present the gate read `0 gap-closure` and a future round 2 would never have been capped.
gap_closure: true
gap_closure_round: 1
# ⚠ A SCHEDULING dependency, not a semantic one — stated so no reader infers a
# data dependency that does not exist. Neither 244-09 nor 244-10 produces anything
# this plan reads. The wave exists because CLAUDE.md measures the shared vitest gate
# going NON-DETERMINISTIC at three concurrent test-running agents, and wave 1 already
# runs one. Two per wave is the measured ceiling.
depends_on: ["244-09", "244-10"]
files_modified:
  - frontend/src/components/chat/MessageItem.tsx
  - frontend/src/components/chat/MessageList.tsx
  - frontend/src/components/chat/__tests__/MessageItem.inlineApproval.test.tsx
  - frontend/src/components/chat/__tests__/ThinkingBlock.characterization.test.tsx
  - scripts/vitest-count-gate.cjs
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md
  - .planning/phases/244-the-chat-shell-and-the-composer/244-12-UAT-ROW.md
autonomous: true
requirements: [SHELL-03]

must_haves:
  truths:
    - "A WORKFLOW-raised approval renders its actions IN THE CHAT COLUMN — the shape the harness actually writes reaches a mount, not only the shape a test constructs."
    - "A DEEP ask_user approval still renders its actions in the chat column — the path 244-03 shipped is not traded away for the one it missed."
    - "Answering in either home still settles both, structurally — one store slice, one reconcile, nothing kept in sync by hand."
    - "The chat column mounts the approval surface EXACTLY ONCE per thread, whatever the row count — the C-3 cost property survives the move."
    - "The panel's and WorkflowRunPage's homes still render their shipped copy (D-244-13: PendingAskCard is a cross-surface shell, and a change here lands in three places)."
    - "The thinking badge sits BELOW the tool-call container and still above the answer — the operator's override of the Phase 243 order, stated as an override."
  artifacts:
    - path: "frontend/src/components/chat/MessageList.tsx"
      provides: "the LIST-level approval mount, a sibling of ThreadRunLine"
      contains: "PendingAskStack"
    - path: "frontend/src/components/chat/MessageItem.tsx"
      provides: "the paused cue kept inline; the unreachable per-row stack mount removed; ThinkingBlock below RunCard"
    - path: "frontend/src/components/chat/__tests__/MessageItem.inlineApproval.test.tsx"
      provides: "cases driven against the shape the PRODUCT writes for a workflow pause"
  key_links:
    - from: "frontend/src/components/chat/MessageList.tsx"
      to: "frontend/src/components/panel/PendingAskCard.tsx (PendingAskStack)"
      via: "a zero-prop, self-resolving mount at list level"
      pattern: "PendingAskStack"
    - from: "PendingAskStack"
      to: "useAskUserPrompt(useViewingThread())"
      via: "the SAME store slice WorkspacePanel reads — which is what makes the settle structural"
      pattern: "useAskUserPrompt"
---

<objective>
Close **G-6**, this phase's **blocker** and its headline criterion. A real workflow-raised approval
(200-Word Essay Writer, step 2 "act") renders **NO approval controls and NO paused cue in the chat
thread**. All three controls measured at `left >= 1103` against the panel's left edge of `1082` —
every one inside the panel. `anyApprovalControlInChatColumn = FALSE`. That is
`BUG-260828-07`'s original complaint (*"it looked right and did nothing"*) still true on this path.

⭐ **The cause is measured against the live rows, and it makes the current mount unreachable
TWICE OVER for a workflow pause:**

    MessageItem.tsx:92-98   hasPendingAsk = tool_calls.some(tc => tc.name === "ask_user"
                                              && (tc.status === "running" || tc.status === "interrupted"))
    MessageItem.tsx:545     the mount sits inside the `isMessageStreaming` (assistant-row) arm

…while the row the harness WRITES is `role="system"` with `tool_calls: [{kind: "ask_user_prompt"}]`
— no `name`, no `status`. **The harness speaks `kind`; the predicate reads `name`/`status`.**

⛔⛔ **AND THERE IS A THIRD REASON THE PREDICATE CANNOT BE THE FIX, measured at planning and it
changes the shape of this plan: that row NEVER REACHES THE FRONTEND AT ALL.**
`backend/app/api/threads.py:427-438` and `:682-691` both apply `.neq("role", "system")` —
`BUG-260528-01`, because `MessageResponse.role` is `Literal["user","assistant"]` and serializing a
system row 500s the whole thread. So **no message in the transcript carries the harness carrier
shape**, and widening `hasPendingAsk` alone would close nothing. ⛔ **Do not widen the predicate and
declare victory** — that is precisely the move that would ship a second green fence over the same
blocker.

⭐ **The pause is already in the chat surface by another route, and that is the fix.**
`PendingAskStack` is **zero-prop and self-resolving** (`PendingAskCard.tsx:685-714`): it reads
`useViewingThread()` + `useAskUserPrompt(threadId)`, the SAME store slice `WorkspacePanel.tsx:438`
reads — which is exactly why the panel CAN show the pause and the chat cannot. **What is missing is
a reachable MOUNT, not data.**

And close **G-2**, which is an **OPERATOR OVERRIDE, not a defect fix** — see its task.

⛔ **Scope, stated as the UAT states it:** only the WORKFLOW path is proven broken. The Deep-mode
`ask_user` path was NOT driven and may well work. Say *"the workflow-raised approval has no chat
controls"*, never *"the inline approval is broken"*.

**Solo run (D-244-21 / OV-SOLO-01):** no independent reviewer. SELF-verification only.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⛔ **FIRST ACTION:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`, then `git rev-parse HEAD` —
MUST be a descendant of `760c758fc` on `develop`, **including wave 1's merges**. ⚠ A fresh worktree
starts on the DEFAULT branch (`master`, at the v3.9 ship commit). Assert the SHA first.
⛔ NEVER `rm -rf` a worktree — `bash scripts/teardown-worktree.sh <path>`.

**⚠ Gate state, inherited:**
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs`, **from the repo root**. Contract:
  *no per-file DECREASE* + *zero failing*. ⚠⚠ **This plan is at real risk of a per-file DECREASE** —
  see Task 2's rule: cases are RE-AIMED in place, never removed.
- ⛔ Capture failing FILENAMES from the gate's own persisted JSON **BEFORE** any re-run, and check
  each against `git diff --numstat`. `SEED-171`'s five flaky suites are cap-independent; three fail
  with a plain `AssertionError`, so `STACK_TRACE_ERROR` is **not** a reliable tell for "not real".
  ⛔ Never touch the cap.
- ⛔ `npx tsc --noEmit` in `frontend/` checks ZERO files. Use
  `npx tsc -p tsconfig.app.json --noEmit` with a **SET DIFF** against the ~67-error base.

**⚠ TWO PLANS RUN IN THIS WAVE** (`244-11` and `244-12`) — the measured maximum. Source sets are
disjoint: `244-11` owns `StreamsProvider.tsx` + `ChatArea.tsx`; this plan owns `MessageItem.tsx` +
`MessageList.tsx`.

**⛔ REGISTRY MERGE ORDER — this plan is the SECOND of the wave.** Both plans append into
`scripts/vitest-count-gate.cjs`, `docs/HOT-FILE-LEDGER.md` and `CLAUDE.md`. `244-11` merges FIRST,
whole. **Rebase this plan onto that merge and RE-APPLY ITS REGISTRY HUNKS BY HAND** — its `TARGETS`
line, its `BASELINE` pins, its ledger rows and its ledger sections. ⛔ Do not accept an automatic
resolution on those three files. Then re-run from the repo root:
`node scripts/check-hot-file-ledger.cjs 244`, `node scripts/check-claude-md-size.cjs`, and the
wave's SINGLE full count gate.
⛔ A row lost to a bad auto-merge is SILENT; a row added twice fails `[duplicate-row]`.

**⚠ G-2 (sketch-before-plan) does not fire on the approval half** — a bug fix on a shipped surface
with a cause named at `file:line` (`D-244-18`). ⚠ **The thinking-badge half is a re-ordering of two
already-approved blocks on a surface whose sketch (234) the operator already approved**, changed by
that same operator's live direction. No new sketch is owed — say so rather than sketching.
</execution_context>

<context>
@.planning/phases/244-the-chat-shell-and-the-composer/244-UAT.md
@.planning/phases/244-the-chat-shell-and-the-composer/244-CONTEXT.md
@.planning/phases/244-the-chat-shell-and-the-composer/244-PATTERNS.md
@CLAUDE.md

⛔ **Read the `G-6` and `G-2` YAML blocks at the bottom of `244-UAT.md`, and test row 4 (L-4) in
full** — including `why_the_green_fence_missed_it`, `scope_limit` and `operator_safety_note` (the
pending step was `send_email` over SMTP; the approval was settled with *"Do not run it"*, never
approved — hold that same discipline in the re-drive row).

⛔ **Read `244-CONTEXT.md` D-244-11 / D-244-12 / D-244-13 / D-244-14**, and `244-PATTERNS.md`
C-3 / C-4 (the measured fetch costs) before deciding anything about the mount.

Project skill — chat-surface design contracts: `.claude/skills/sketch-findings-agentic-rag/SKILL.md`.
Load it before moving a render block: this phase's surface is covered by its Phase 095 build-once
inventory rule and its Phase 243/234 thinking-block findings.

@frontend/src/components/chat/MessageItem.tsx
@frontend/src/components/chat/MessageList.tsx
@frontend/src/components/panel/PendingAskCard.tsx
@frontend/src/components/chat/__tests__/MessageItem.inlineApproval.test.tsx
@frontend/src/components/chat/__tests__/ThinkingBlock.characterization.test.tsx
@docs/HOT-FILE-LEDGER.md

<interfaces>
`PendingAskStack` — zero-prop, self-resolving (`PendingAskCard.tsx:685-714`):

    export function PendingAskStack() {
      const threadId = useViewingThread()
      const { data: asks, reconcile } = useAskUserPrompt(threadId)
      const workflowLock = useWorkflowLockForThread(threadId)
      const { data: phases } = usePhases(threadId)
      const runIsOver = workflowLock == null && phases.length > 0
      ...
      if (asks.length === 0) return null      // self-guarding: renders nothing when there is no pause
      ...
    }

The measured cost (C-3, and the docblock at `MessageItem.tsx:515-543`): `useAskUserPrompt` mounts
`usePanelReconcile` => one `getThreadPendingAsks` FETCH per mount, and `PendingAskStack` also calls
`usePhases` => a SECOND. `MessageList` renders one `MessageItem` per message with **no
virtualisation**, so a top-level per-row mount is **2 fetches x N ROWS** — measured at 6 rows:
6 ask fetches versus 1.

The LIST-LEVEL precedent, already shipped and already argued in its own comment
(`MessageList.tsx:245-261`):

    {/* Phase 194.1 Plan 06 (RUN-01 / R4 + R5 - D-15): the run-anchored line.
        AFTER the messages map and BEFORE the auto-scroll anchor, so it is at
        LIST level - not inside the transcript - and `bottomRef` still lands at
        the true bottom of the scrollable content.
        ...
        LIST LEVEL RATHER THAN MESSAGE LEVEL IS LOAD-BEARING. After plan 03 a
        harness kickoff inserts NO assistant node, so there is no message for a
        run reading to anchor to ... */}
    <ThreadRunLine threadId={threadId ?? null} />

    <div ref={bottomRef} />

⭐ **That argument is THIS gap's argument, verbatim, one surface over:** a harness pause has no
assistant row to anchor to either — its carrier row is `role="system"` and is filtered off the wire.

The current (unreachable-for-harness) mount, `MessageItem.tsx:545-548`:

    {hasPendingAsk(message.tool_calls) && (
      <>
        <PausedRunCue />
        <PendingAskStack />
      </>
    )}

The G-2 site, `MessageItem.tsx:478-486` — the two blocks whose order the operator overrode:

    <ThinkingBlock reasoningContent={...} isStreaming={isMessageStreaming} reasoningMs={...} />
    {message.tool_calls && message.tool_calls.length > 0 && (
      <RunCard message={message} isStreaming={isStreaming} />
    )}

The Phase 243 rationale it contradicts, `MessageItem.tsx:477`:

    ORDER IS THE ORDER IN TIME - above the run card's tool rows and above the
    answer. A sibling of WorkingBadge and RunCard, in the style those two use.

The fence that pins it, `ThinkingBlock.characterization.test.tsx:632` (§11):

    expect(thinkingAt).toBeLessThan(firstToolRowAt)
    expect(thinkingAt).toBeLessThan(bodyAt)
    // plus the same two relations restated via compareDocumentPosition
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — drive the approval against the shape the PRODUCT writes, not one a test builds</name>
  <files>frontend/src/components/chat/__tests__/MessageItem.inlineApproval.test.tsx</files>
  <read_first>
    - `.planning/phases/244-the-chat-shell-and-the-composer/244-UAT.md` — the `G-6` YAML block and test row 4 (L-4)
    - `frontend/src/components/chat/__tests__/MessageItem.inlineApproval.test.tsx` — the whole file, once. Cases 1-5 are the chat-side claims; 6a/6b/6c are `D-244-13`'s other-homes claims
    - `frontend/src/components/panel/PendingAskCard.tsx:685-760` — `PendingAskStack`
    - `frontend/src/components/chat/MessageList.tsx` — the render body and the `ThreadRunLine` mount
  </read_first>
  <behavior>
    - **Test W1 (THE GAP — MUST be RED):** render **`MessageList`** for a thread whose messages are
      an ordinary user row and an ordinary assistant row — ⛔ **no message carrying an `ask_user`
      tool call at all**, because the harness's carrier row is `role="system"` and is filtered off
      the wire by `threads.py`'s `.neq("role","system")`. Populate the pending-ask store slice the
      way the product populates it (via `useAskUserPrompt`'s slice / the provider action the SSE and
      `getThreadPendingAsks` write through) — ⛔ **never by constructing a message**. Assert the
      panel's two action LABELS are present in the rendered chat column.
      ⚠ This is the case whose absence let G-6 ship green. The shipped fence CONSTRUCTS
      `tool_calls:[{name:"ask_user",status:"running"}]` — the Deep-agent shape — and so proves the
      component renders when handed that shape, never that the product emits it.
    - **Test W2 (the Deep path is not traded away):** the same `MessageList` render with a
      streaming assistant row carrying `tool_calls:[{name:"ask_user",status:"running"}]` still
      shows the two actions **and** the paused cue. ⛔ Both must hold; a fix that swaps one
      unreachable path for another is not a fix.
    - **Test W3 (EXACTLY ONE mount, whatever the row count):** with **six** assistant rows and a
      pending ask, the ask-fetch count is the SAME as with one row. Re-drive C-3's measurement at
      the new mount site. ⛔ This is the property that made the original mount narrow, and it must
      be proven at the new site rather than assumed by it.
    - **Test W4 (no pause => nothing mounts and nothing is bought):** an ordinary thread with no
      pending ask renders no approval controls, no cue, and — ⚠ **state the measured truth rather
      than the convenient one** — record the ask-fetch count for that thread. If the list-level
      mount buys a fetch on every thread open, the case ASSERTS that number as the accepted cost
      and the SUMMARY names it. ⛔ Do not write an assertion that hides a regression you measured.
    - **Test W5 (the settle is structural):** a second reader of the same slice sees the same asks
      at no second fetch — the shipped case 5's claim, re-driven at the new site.
    - **Tests 6a / 6b / 6c UNCHANGED:** `PendingAskStack` (the panel's mount), `PendingAskCard`
      mounted directly (WorkflowRunPage's shape) and the source sweep for both page mount sites.
      `D-244-13`: this is a CROSS-SURFACE SHELL and a change lands in three homes.
  </behavior>
  <action>
Add W1-W5 to `MessageItem.inlineApproval.test.tsx` and **RE-AIM cases 1-5 in place** so they render
through `MessageList` instead of a bare `MessageItem`.

⛔⛔ **DO NOT DELETE ANY CASE.** The count gate's contract is *no per-file DECREASE*; removing cases
1-5 and re-homing them to a new suite would DECREASE this file's pin and fail the gate — and, worse,
would drop the only written record of why the narrow mount existed. Each re-aimed case keeps its
number and gains a docblock line saying what its anchor used to be and why it moved.

⛔ **The store must be seeded the way the product seeds it.** Find the writer `useAskUserPrompt`
reads through (the `pendingAsks` slice and the `usePanelReconcile` path in `StreamsProvider.tsx`)
and drive that. A fixture that hand-writes a message object is the exact failure this gap is about,
and reproducing it here would make the new fence as vacuous as the old one.

⛔ **Run and SEE W1 RED**, quoting the verbatim failure in the SUMMARY. Also record the measured
colour of W2-W5 at RED time — W3's and W4's numbers are data, not guesses.

⛔ `git diff -- frontend/src/components/chat/MessageItem.tsx frontend/src/components/chat/MessageList.tsx`
must be **EMPTY** at the end of this task.
  </action>
  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/chat/__tests__/MessageItem.inlineApproval.test.tsx` runs; **W1 FAILS**, with the verbatim failure quoted in the SUMMARY.
    - The SUMMARY records the measured colour of W2-W5, and the two numbers from W3/W4 (fetch counts at 1 row, 6 rows, and no-pause).
    - `grep -c "  it(" frontend/src/components/chat/__tests__/MessageItem.inlineApproval.test.tsx` is `>= 8` — no case was deleted.
    - W1's fixture contains **no** `name: "ask_user"` and **no** `status: "running"` message construction — assert with a grep scoped to W1's body and quote it. The pending state comes from the store.
    - `git diff -- frontend/src/components/chat/MessageItem.tsx frontend/src/components/chat/MessageList.tsx` is EMPTY.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/chat/__tests__/MessageItem.inlineApproval.test.tsx</automated>
  </verify>
  <done>W1 is RED against unmoved production code, driven on the product's own shape; no case was deleted; the cost numbers are measured rather than assumed.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — mount the approval where a harness pause can actually reach it</name>
  <files>frontend/src/components/chat/MessageList.tsx, frontend/src/components/chat/MessageItem.tsx</files>
  <read_first>
    - `frontend/src/components/chat/MessageList.tsx` — the render body, especially `:245-265` (the `ThreadRunLine` mount, `bottomRef`, the floating chip)
    - `frontend/src/components/chat/MessageItem.tsx` — ONLY `:88-100` (`hasPendingAsk`), `:504-552` (the live seam arm and the mount), and `:60-73` (the import docblock)
    - `frontend/src/components/panel/PendingAskCard.tsx:685-714`
    - `.planning/phases/244-the-chat-shell-and-the-composer/244-PATTERNS.md` C-3 / C-4
  </read_first>
  <action>
**Mount `PendingAskStack` at LIST level in `MessageList.tsx`**, as a sibling of `ThreadRunLine`,
**after** the messages map and **before** `<div ref={bottomRef} />` so the auto-scroll anchor still
lands at the true bottom of the scrollable content. It is zero-prop and self-guarding
(`if (asks.length === 0) return null`), so it needs nothing threaded to it.

Write the mount's docblock so the next reader inherits the argument rather than the conclusion:
- **Why list level and not message level:** the harness's carrier row is `role="system"` and is
  filtered off the wire at `threads.py:427-438` / `:682-691` (`BUG-260528-01`), so **there is no
  message to anchor to** — the identical argument `ThreadRunLine`'s own comment already makes for a
  harness kickoff that inserts no assistant node.
- **Why UNCONDITIONAL and not gated**, recorded as the REJECTED ARM with its reason: a cheaper gate
  was available (mount only when the thread holds a harness lock, or when a message carries a
  pending ask). ⛔ It was rejected because **every such gate is a narrowing mount condition, and a
  narrowing mount condition is exactly what made SHELL-03 unreachable twice over.** Buying back two
  fetches per thread open by re-introducing the failure mode, on this phase's blocker, is the wrong
  trade. The cost is bounded (ONE mount per thread, never per row) and is measured by W3/W4.
- **What it does NOT change:** one store slice, one `reconcile` — so *"answering in either home
  settles it in both"* stays STRUCTURAL (`D-244-11`), with nothing to keep in sync.

**In `MessageItem.tsx`:**
- **REMOVE** `<PendingAskStack />` from the `hasPendingAsk` arm at `:545-548`, and remove its now
  unused import. ⛔ There must be exactly ONE chat-column mount; two homes for one pause is the
  *"actionable twice and agreed in neither"* failure the ROADMAP names by name.
- **KEEP** `<PausedRunCue />` exactly where it is, and keep `hasPendingAsk` — the cue is
  `D-244-12`'s in-transcript marker for the Deep path and is not this gap.
- Update the import docblock at `:68-72` and the mount docblock at `:515-543`: they currently argue
  for a narrow per-row mount. ⛔ **Do not delete that reasoning** — it is C-3's measurement and it
  is still true about a per-row mount. Record BESIDE it that the mount moved to list level, and why
  the cost argument survives the move (one mount per thread is strictly cheaper than one per
  matching row, and is now reachable for both pause shapes).

⚠ **`MessageItem.tsx` was G-5-DISCHARGED at Phase 227 and must not be re-hollowed.** Report
`useState` / `useEffect` / prop counts before and after; this task should move all three by **zero**
and should DELETE lines rather than add state.

Run: all of W1-W5 and 1-5 and 6a-6c green. Then run the shipped suites most likely to be disturbed
by a `MessageList` edit — `MessageList.test.tsx`, `MessageList.scroll.test.tsx`,
`MessageList.dedup.test.tsx`, `MessageList.runline.baseline.test.tsx` — and report each count.
⚠ `MessageList.runline.baseline.test.tsx` exists precisely to notice a change beside
`ThreadRunLine`; treat a red there as a real finding about this mount's placement, not as noise.

Update `MessageItem.inlineApproval.test.tsx`'s `BASELINE` pin from `8` to its new count and
attribute the `+N` with no residual.
  </action>
  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/chat/__tests__/MessageItem.inlineApproval.test.tsx src/__tests__/components/chat/MessageList.test.tsx src/__tests__/components/chat/MessageList.scroll.test.tsx src/__tests__/components/chat/MessageList.dedup.test.tsx src/__tests__/components/chat/MessageList.runline.baseline.test.tsx` — `failed 0`, and each suite's count reported.
    - `grep -c "PendingAskStack" frontend/src/components/chat/MessageItem.tsx` returns `0`; `grep -c "PendingAskStack" frontend/src/components/chat/MessageList.tsx` returns `>= 2` (import + mount).
    - `grep -c "PausedRunCue" frontend/src/components/chat/MessageItem.tsx` is UNCHANGED from its pre-edit value.
    - `grep -c "useState" frontend/src/components/chat/MessageItem.tsx` and `grep -c "useEffect" frontend/src/components/chat/MessageItem.tsx` are byte-identical before and after — both numbers quoted in the SUMMARY (the Phase 227 discharge is not re-hollowed).
    - In `MessageList.tsx`, `<PendingAskStack` appears AFTER the `renderMessages.map` block and BEFORE `<div ref={bottomRef} />` — assert with `grep -n` line numbers quoted in the SUMMARY.
    - Exactly one chat-column mount exists: `grep -rn "<PendingAskStack" frontend/src/components frontend/src/pages | grep -v "__tests__"` returns exactly TWO lines — `MessageList.tsx` and `WorkspacePanel.tsx`.
    - `npx tsc -p tsconfig.app.json --noEmit` — SET diff against the base capture EMPTY both ways.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/chat/__tests__/MessageItem.inlineApproval.test.tsx src/__tests__/components/chat/MessageList.test.tsx src/__tests__/components/chat/MessageList.runline.baseline.test.tsx</automated>
  </verify>
  <done>The chat column mounts the approval surface once, at list level, reachable for both the workflow and the Deep pause; the cue stays inline; MessageItem is not re-hollowed.</done>
</task>

<task type="execute">
  <name>Task 3: G-2 — the operator's override of a locked Phase 243 rationale, and its fence re-driven</name>
  <files>frontend/src/components/chat/MessageItem.tsx, frontend/src/components/chat/__tests__/ThinkingBlock.characterization.test.tsx, scripts/vitest-count-gate.cjs, docs/HOT-FILE-LEDGER.md, CLAUDE.md, .planning/phases/244-the-chat-shell-and-the-composer/244-12-UAT-ROW.md</files>
  <read_first>
    - `.planning/phases/244-the-chat-shell-and-the-composer/244-UAT.md` — the `G-2` YAML block, including `conflicts_with` and `fence_at_risk`
    - `frontend/src/components/chat/MessageItem.tsx:464-490` — the ThinkingBlock / RunCard pair and the Phase 243 rationale at `:477`
    - `frontend/src/components/chat/__tests__/ThinkingBlock.characterization.test.tsx:620-700` — §11 (ORDER ON SCREEN) and §12 (NO SECOND GATE)
    - `docs/HOT-FILE-LEDGER.md` §§ `MessageItem.tsx` and `MessageList.tsx`
    - `.claude/skills/sketch-findings-agentic-rag/SKILL.md` — the chat-surface design contracts
  </read_first>
  <action>
**(a) The move.** Render `<ThinkingBlock>` **after** the `RunCard` block and before the answer, so
the order on screen becomes **tool-call container -> thinking badge -> answer**. The operator, live
during UAT: *"the thinking badge it's recommended to be below the container of the tools not above"*.

⛔⛔ **LABEL IT AS AN OVERRIDE, NOT AS A DEFECT FIX, IN THE CODE ITSELF.** `MessageItem.tsx:477`
carries a LOCKED Phase 243 rationale — *"ORDER IS THE ORDER IN TIME — above the run card's tool
rows and above the answer"* — and this contradicts it. **Preserve that sentence beside the new
one**, in this project's own recorded style, rather than deleting it: a reader must be able to see
that the order was chosen deliberately in 243, overridden deliberately by the operator in 244, and
on whose instruction. ⛔ Deleting the 243 rationale would make the next reader believe the old
order was an accident.

⛔ **Change NOTHING ELSE about the mount.** §12 pins, on source, that the mount carries **no tool
test** and **no `key=`**, and that `ThinkingBlock.tsx` never spells `tool_calls`. All three are
still required after the move — a `key` would close an open fold on every temp-id -> DB-id
reconcile (`243-PATTERNS §F.8`), and a tool test would restore the CHAT-04 defect 243-02 closed.
⚠ §12 reads the mount via `soleMountExpression(chatSource("MessageItem.tsx"), "ThinkingBlock")` —
confirm it still resolves after the move and does not silently match nothing.

**(b) Re-drive the fence, never delete it.** §11 asserts `thinkingAt < firstToolRowAt`. Invert that
ONE relation to `firstToolRowAt < thinkingAt`, keep `thinkingAt < bodyAt`, and keep **both**
statements of each relation — the index comparison AND the `compareDocumentPosition` restatement —
because §11's own comment says the second exists so *"a future container restructure cannot make
the indices agree by accident"*. Keep all three positive controls (the thinking trigger, the first
tool row, and the rendered answer body must each be found on the page, or the comparison is a claim
about nothing).

⛔ Record in §11's docblock, beside its existing 243-05 correction note, that the order was
**overridden by the operator on 2026-09-12** and that the case now pins the NEW order. ⛔ The case
count must not decrease.

⛔ **Drive it RED first**: with the fence inverted and the source NOT yet moved, §11 must fail.
Quote that failure verbatim. Then move the source and see it green.

**(c) The ledger.** `MessageItem.tsx` and `MessageList.tsx` both FIRE G-5 and both have rows.
Re-derive both triples from the repo root, **subtracting six-digit dated quick-task buckets**:

  git log --oneline -- <file> | wc -l
  git log --format=%s -- <file> | sed -E 's/^[a-z]+\(([^)]+)\).*/\1/' | sed -E 's/-.*//' | grep -E '^[0-9]+(\.[0-9]+)?$' | grep -vE '^[0-9]{6}$' | sort -u | wc -l
  wc -l <file>

Measured at planning time: `MessageItem.tsx` **71 / 34 / 904** (its row reads `71 / 37 / 904` — the
phase figure differs by the dated-bucket subtraction, so **say which convention the cell uses**);
`MessageList.tsx` **22 / 10 / 307** against a row reading `21 / 9 / 307`. ⚠ Re-derive anyway.

`MessageItem.tsx`'s cell must stay honest about the 227 discharge: it is **still not re-hollowed**
(quote the before/after `useState` / `useEffect` / prop counts) and this plan **deleted** a mount
rather than adding one. `MessageList.tsx`'s cell records that it gained its SECOND list-level
mount, beside `ThreadRunLine`, for the same measured reason. Row + section in the SAME commit;
disposition cells <= 200 chars.

**(d) The browser row.** Create `244-12-UAT-ROW.md`, unfilled, with three arms:
- **Arm 1 — the blocker, re-driven.** Arm a REAL workflow approval (the L-4 fixture: a published
  workflow with a step marked needs-approval). Measure the approval controls' `left` against the
  panel `<aside>`'s own `left`, exactly as L-4 did — the chat column must now carry them, and
  `anyApprovalControlInChatColumn` must be TRUE. ⛔ Score by POSITION against the panel edge, not
  by `data-testid` presence: L-4's finding was that the controls existed and were all in the panel.
- **Arm 2 — both directions settle.** Answer from the THREAD, read the panel's rendered SENTENCE
  (not a testid) with no manual refresh; then arm a second approval, answer it in the PANEL, and
  confirm the thread's controls are gone and the row reads settled. Then confirm
  `WorkflowRunPage` still renders its own `PendingAskCard` (the third home, `D-244-13`).
  ⛔ **OPERATOR SAFETY, carried forward from L-4:** if the pending step is outward-facing and
  irreversible (`send_email` over SMTP was L-4's), settle with **"Do not run it"**, never "Approve
  this step". That still exercises the answer path.
- **Arm 3 — the G-2 order, on screen.** On a live tool-bearing turn, read the three blocks'
  document order and confirm **tools -> thinking -> answer**. ⚠ Present it to the operator as an
  override for confirmation — it contradicts a rationale they approved at Phase 243, and a
  re-reversal later is cheaper than a silent disagreement.
⛔ Every verdict field reads `pending`.
  </action>
  <acceptance_criteria>
    - §11 was seen RED with the fence inverted and the source unmoved — the verbatim failure is quoted in the SUMMARY.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/chat/__tests__/ThinkingBlock.characterization.test.tsx src/components/chat/__tests__/ThinkingBlock.clamp.test.tsx` — `failed 0`, and the case count is `>= 33` (no decrease).
    - In `MessageItem.tsx`, the `<ThinkingBlock` line number is GREATER than the `<RunCard` line number — both quoted via `grep -n` in the SUMMARY.
    - `grep -c "ORDER IS THE ORDER IN TIME" frontend/src/components/chat/MessageItem.tsx` returns `1` — the Phase 243 rationale is preserved, not deleted, and the new comment names the operator override beside it.
    - The mount still carries no tool test and no `key=`: §12 passes unchanged, and `soleMountExpression` resolves (a §12 that matched nothing would pass vacuously — assert the matched expression is non-empty).
    - `node scripts/check-hot-file-ledger.cjs 244` exits `0` AND reports a non-zero `subject:` count; `node scripts/check-claude-md-size.cjs` exits `0`.
    - `grep -c "frontend/src/components/chat/MessageItem.tsx" CLAUDE.md` and `grep -c "frontend/src/components/chat/MessageList.tsx" CLAUDE.md` each return exactly `1`.
    - `244-12-UAT-ROW.md` exists with three arms, scores Arm 1 by POSITION against the panel edge, carries the "Do not run it" safety rule, and contains no `pass` verdict.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/chat/__tests__/ThinkingBlock.characterization.test.tsx</automated>
  </verify>
  <done>The thinking badge sits below the tools with the override recorded beside the rationale it overrides; §11 was re-driven not deleted; both ledger rows are re-derived; the three-arm browser row exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow-raised approval -> an actionable control in chat | A control that can APPROVE an outward-facing, irreversible step (L-4's pending step was `send_email` over SMTP) now renders in a second place. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-244-12-01 | Elevation of Privilege | the chat-column approval mount | mitigate | `PendingAskStack` is zero-prop and resolves its own thread via `useViewingThread()`; it renders only the asks the SAME store slice the panel reads already holds, fed by the owner-scoped `getThreadPendingAsks`. ⛔ No new endpoint, no new authorisation path, no prop that could point it at another thread. |
| T-244-12-02 | Spoofing | two homes for one pause | mitigate | Exactly ONE chat-column mount is asserted by grep (`MessageList.tsx` + `WorkspacePanel.tsx`, two lines total). *"Actionable twice and agreed in neither"* is the ROADMAP's own named failure mode; the shared slice makes agreement structural. |
| T-244-12-03 | Denial of Service | fetch amplification | mitigate | W3 proves 6 rows cost what 1 row costs; W4 measures and ASSERTS the no-pause cost. The rejected per-row mount was measured at 2 fetches x N rows (C-3). ⛔ If W4 shows a fetch bought on every thread open, that number is published, not hidden. |
| T-244-12-04 | Repudiation | the G-2 order override | mitigate | The Phase 243 rationale is preserved beside the override with its date and its source (the operator, live in UAT); §11 is re-driven rather than deleted, so the new order is pinned rather than merely current. |
| T-244-12-05 | Tampering | `ThinkingBlock` mount invariants | mitigate | §12 still pins no-tool-test / no-`key=` / no `tool_calls` in `ThinkingBlock.tsx`, and this plan asserts `soleMountExpression` still resolves — a §12 matching nothing would pass vacuously, which is the Phase-242 `check-hot-file-ledger` failure in a different file. |
| T-244-12-SC | Tampering | npm/pip/cargo installs | n/a | **No package is installed.** `git diff -- frontend/package.json frontend/package-lock.json` MUST be empty; no legitimacy audit is owed. ⚠ If one becomes necessary, STOP — the audit table comes first. |
</threat_model>

<verification>
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the **repo root**: `failed 0`,
  **no per-file DECREASE** (⚠ the live risk in this plan), and every `+N` attributed with no
  residual. ⛔ Capture failing filenames from the gate's own JSON BEFORE any re-run and check each
  against `git diff --numstat`. ⛔ Never adjust the cap.
- `npx tsc -p tsconfig.app.json --noEmit` — SET diff against the base capture empty both ways.
- `node scripts/check-hot-file-ledger.cjs 244` (non-vacuous parse) and
  `node scripts/check-claude-md-size.cjs` — both exit 0, from the repo root, AFTER this plan's
  merge (it is the SECOND of the wave; its registry hunks are re-applied by hand).
- ⛔ **The backend is untouched:** `git diff --stat -- backend/ supabase/` EMPTY.
- ⛔ **NOT verified here, stated as owed — and this is the criterion's whole point.** `D-244-14`:
  *"`BUG-260828-07` is severity HIGH and closes on a DRIVEN row, not a fence."* A synthetic test
  that the stack mounts proves mounting, not answering. `244-12-UAT-ROW.md` is driven at
  `/gsd:verify-work`. **This plan may not report SHELL-03 as closed.**
</verification>

<success_criteria>
- W1 was seen RED on the product's own shape and is green after the move; W2-W5 green with their
  numbers published.
- Exactly one chat-column mount of `PendingAskStack`, at list level, before `bottomRef`.
- `PausedRunCue` stays inline; `MessageItem` loses a mount and gains no state.
- §11 was re-driven (inverted, seen RED, then green) and §12 still resolves non-vacuously.
- The Phase 243 order rationale survives beside the operator override.
- Both ledger rows re-derived, rows + sections in one commit; both registry gates exit 0.
- The SUMMARY says **built, drive owed** for SHELL-03 — never closed — and says SELF-verified.
</success_criteria>

<output>
Create `.planning/phases/244-the-chat-shell-and-the-composer/244-12-SUMMARY.md` when done.
</output>
