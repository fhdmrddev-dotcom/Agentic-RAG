---
phase: 245-the-verification-debt-discharged-or-retired-in-writing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v4.0-phases/238-microsoft-graph-onedrive/238-VERIFICATION.md
  - .planning/milestones/v4.0-phases/240-mail-is-a-shape-not-a-fourth-adapter/240-VERIFICATION.md
  - .planning/milestones/v4.0-phases/241-recall-at-corpus-scale/241-VERIFICATION.md
  - .planning/phases/242-ship-it-and-prove-what-already-shipped/242-VERIFICATION.md
  - .planning/phases/244-the-chat-shell-and-the-composer/244-VERIFICATION.md
  - scripts/check-verification-honesty.cjs
  - .claude/hooks/verification-honesty-guard.js
  - .claude/settings.json
  - .planning/phases/245-the-verification-debt-discharged-or-retired-in-writing/245-VERDICT.md
  - .planning/STATE.md
autonomous: true
requirements: [DEBT-03]

must_haves:
  truths:
    - "SC#3 — a person opening 238-VERIFICATION.md, 240-VERIFICATION.md and 241-VERIFICATION.md finds the literal token `verification_mode: self-verified` in machine-readable frontmatter. ⚠ The second clause of SC#3 — *can tell from each file which gate did and did not run* — was measured ALREADY TRUE at HEAD by the CONTEXT (`238:3-9`, `240:9-13`, `241:25-29`); this plan does not observe it afresh, it preserves it byte-unchanged and cites the measurement."
    - "SC#3's honesty is preserved, not traded: every sentence in those three files naming the owed §6.3 review still exists, and the pre-edit `git diff` shows ZERO deletions in all three."
    - "The marker's absence is now LOUD rather than silent: `node scripts/check-verification-honesty.cjs` exits 1 and names the file when the field is removed, and exits 1 when a frontmatter value CLAIMS `reviewed` while OV-SOLO-01 is live."
    - "The guard has been SEEN firing on both arms against a planted defect, and the victim file was restored md5-identical — a guard nobody has seen fire is not a guard."
    - "The guard does NOT fire on the three correct files, whose bodies carry `review` 13 / 11 / 11 times and the word `reviewed` 1 / 0 / 2 times in honest prose — body prose is never inspected, by design and by measured justification."
    - "SC#4 — `OV-SOLO-01` is confirmed complete at `.planning/STATE.md` with all four elements and its re-arm trigger, cited verbatim in 245's verdict, and NOT rewritten."
  artifacts:
    - path: "scripts/check-verification-honesty.cjs"
      provides: "the executable honesty gate — exit 0 clear / 1 violation / 2 harness error, with a printed derivation and a non-vacuity floor"
      contains: "MIN_PINNED_FILES"
    - path: ".claude/hooks/verification-honesty-guard.js"
      provides: "PostToolUse hook — fires in the turn a VERIFICATION.md is authored, never denies the write"
      contains: "hookSpecificOutput"
    - path: ".planning/phases/245-the-verification-debt-discharged-or-retired-in-writing/245-VERDICT.md"
      provides: "245's written verdict file — SC#3 and SC#4 discharged here; SC#1 / SC#2 sections named and owned by 245-02 / 245-03"
      contains: "OV-SOLO-01"
  key_links:
    - from: ".claude/settings.json"
      to: ".claude/hooks/verification-honesty-guard.js"
      via: "a fifth PostToolUse `Write|Edit` entry, same shape as hot-file-ledger-guard.js"
      pattern: "verification-honesty-guard"
    - from: ".claude/hooks/verification-honesty-guard.js"
      to: "scripts/check-verification-honesty.cjs"
      via: "execFileSync with the single written path, after a path-regex early exit"
      pattern: "check-verification-honesty"
---

<objective>
**DEBT-03, both halves — SC#3 and SC#4.**

Add the machine-readable marker `verification_mode: self-verified` to the VERIFICATION.md files of
every phase that closed without an independent AGENTS.md §6.3 review, ship an executable guard so
its absence can never again be silent, and discharge SC#4 by **citing** the `OV-SOLO-01` ruling that
is already complete.

⛔⛔ **THIS PLAN IS NOT A RE-REVIEW, AND MUST NOT BECOME ONE.** The ROADMAP's Flags block is blunt:
*"`DEBT-03` IS A HONESTY REQUIREMENT AND MUST NOT BECOME A RE-REVIEW. Its entire content is the word
in three files and the ruling in one. A phase that re-reviews 238 / 240 / 241 has silently changed
the requirement into a different and much larger one."* **Read no verdict in those files. Judge no
finding. Change no prose.** The only bytes this plan adds to them are frontmatter.

⛔ **SC#3's literal wording — "does not find 'reviewed'" — is UNSATISFIABLE and is not the deliverable
(D-01).** Measured at HEAD by the orchestrator, word-boundary `\breviewed\b`: **238 = 1 · 240 = 0 ·
241 = 2**; substring `review`: **13 / 11 / 11**. Every one of those occurrences is an honest sentence
*naming the review that is owed*. Deleting them would destroy exactly the honesty SC#3 exists to
protect. What *"cannot lapse unnoticed"* actually needs is **machine-checkability**, which prose does
not give — hence the marker and the guard.

Purpose: 238, 240 and 241 already say "self-verified" in their opening paragraphs. **None carries a
token any scan can see**, and 243/244 carry the field (or not) only because those phases remembered.
Output: five marked files, one gate, one hook, one settings entry, one verdict file, one STATE.md
line.

⭐ **This plan needs no credential, no infra, no browser and no operator.** The ROADMAP names the
phase stalling on the Azure registration as a failure mode; this plan is the arm that cannot stall.

**Solo run (`OV-SOLO-01`):** Gemini is unavailable. Everything here is a **SELF-verification**, never
a review. Say so in the SUMMARY — and note the recursion out loud: this is the plan that ships the
marker for that fact.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⛔ **FIRST ACTION:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`, then `git rev-parse HEAD` — MUST
be `d4cd5d7d7` or a descendant on `develop`. ⚠ A fresh worktree starts on the DEFAULT branch
(`master`, stuck at the v3.9 ship commit); five consecutive executors had to reset. Assert the SHA
before reading anything else.
⛔ NEVER `rm -rf` a worktree — `bash scripts/teardown-worktree.sh <path>`.

**⚠ Gate state relevant to this plan, and the parts of it that are NOT:**
- ⛔ **This plan touches NO application source.** `git diff --stat -- backend/ frontend/ supabase/`
  MUST be empty at the end. The backend unit ceiling (71, zero headroom) and the vitest count gate
  are therefore **not at risk and are not re-run for this plan** — running them would consume context
  to prove something no diff can have broken.
- ⛔ **No hot-file-ledger row is owed, and this is SETTLED rather than assumed.** Measured by the
  orchestrator at HEAD:
  `node scripts/check-hot-file-ledger.cjs --files scripts/check-verification-honesty.cjs .claude/hooks/verification-honesty-guard.js`
  → `scan list: 268 rows · subject: 2 files · watched: 0 · ledger gate OK`. `WATCHED` is only
  `^backend/app/` and `^frontend/src/`, and `^scripts/` sits in `EXEMPT`; `.claude/hooks/` matches no
  WATCHED pattern. **Do NOT add ledger rows for these two files** — a spurious row risks
  `[duplicate-row]` in `check-claude-md-size.cjs` and pads a table whose size already forced two
  splits.
- ⛔ **CLAUDE.md is NOT edited by this plan.** Its `SEED-177` correction belongs to `245-03`.

**⚠ STATE.md discipline:** hand-edit it with Edit. ⛔ **NEVER call a `state.*` SDK verb** — seven of
them write false records and corrupted that file five times in Phase 190 alone while reporting
success.

**⚠ G-2 does not fire** (no UI). **G-3 does not fire**: this is not a one-word edit — it is five
frontmatter inserts *plus a new gate, a new hook and a settings registration*, which is a plan.
</execution_context>

<context>
@.planning/phases/245-the-verification-debt-discharged-or-retired-in-writing/245-CONTEXT.md
@.planning/ROADMAP.md

⛔ **Read only these regions of the three v4.0 files — their OPENING FRONTMATTER AREA, nothing else.**
Reading their verdicts is the re-review this plan is forbidden to perform:
- `.planning/milestones/v4.0-phases/238-microsoft-graph-onedrive/238-VERIFICATION.md` lines **1-10**
- `.planning/milestones/v4.0-phases/240-mail-is-a-shape-not-a-fourth-adapter/240-VERIFICATION.md` lines **1-16**
- `.planning/milestones/v4.0-phases/241-recall-at-corpus-scale/241-VERIFICATION.md` lines **1-20**

@.planning/phases/243-the-thinking-block-and-the-follow-scroll-seam/243-VERIFICATION.md
@.claude/hooks/hot-file-ledger-guard.js
@scripts/check-hot-file-ledger.cjs
@scripts/check-gap-closure-rounds.cjs

<interfaces>
**The line to replicate verbatim (`243-VERIFICATION.md:4`) — the pattern, comment included:**

  verification_mode: self-verified   # ⛔ OV-SOLO-01 / D-243-12 — NEVER "reviewed". No independent §6.3 reviewer exists.

**The five subject files and what each already has, measured at HEAD — do not re-derive:**

| file | frontmatter today | `verification_mode` | `\breviewed\b` in body | substring `review` |
|---|---|---|---|---|
| `238-VERIFICATION.md` | ⛔ **NONE** — line 1 is `# Phase 238 — Verification` | absent | 1 | 13 |
| `240-VERIFICATION.md` | ⛔ **NONE** — line 1 is `# Phase 240 — Verification` | absent | 0 | 11 |
| `241-VERIFICATION.md` | ✅ has one (`phase`, `verified`, `status`, `score`, `overrides_applied`, `human_verification`) | absent | 2 | 11 |
| `242-VERIFICATION.md` | ✅ has one — already carries `solo_run: true` / `independent_review: false` | absent | 0 | — |
| `244-VERIFICATION.md` | ✅ has one (`phase`, `verified`, `status`, `score`, `re_verification`, …) | absent | 4 | — |

**The scan set the gate binds to, and the only file in the repository that carries the field today:**

  total *-VERIFICATION.md under .planning/  = 218
  carrying verification_mode                =   1   (243-VERIFICATION.md)
  under .planning/phases/ (active milestone) =  3   (242, 243, 244)
  under .planning/milestones/ (archive)      = 215

**The hook contract to copy** (`hot-file-ledger-guard.js`, read it in full): stdin JSON →
`input.tool_input.file_path` → normalise `path.sep` to `/` → ONE regex test and `process.exit(0)` on
any non-match → `execFileSync(process.execPath, [gate, target], {cwd: root, timeout})` →
`process.exit(0)` silently when the gate is clear → on failure write
`{hookSpecificOutput: {hookEventName: 'PostToolUse', additionalContext, …}}` and **still exit 0**.
It is deliberately **NOT** a PreToolUse blocker; its header says why and the new hook's header must
say the same in its own words.

**The settings.json shape to match** — `hooks.PostToolUse` currently holds **six** entries, four of
them matcher `"Write|Edit"` (`gsd-phase-boundary.sh`, `claude-md-size-guard.js`,
`hot-file-ledger-guard.js`, `landing-drift-guard.js`). The new entry is a **fifth `Write|Edit`
entry**, appended, in exactly this shape:

  {
    "matcher": "Write|Edit",
    "hooks": [
      {
        "type": "command",
        "command": "\"C:/Program Files/nodejs/node.exe\" \"$CLAUDE_PROJECT_DIR\"/.claude/hooks/verification-honesty-guard.js",
        "timeout": 10
      }
    ]
  }

**The worded-escape-hatch precedent** (`check-gap-closure-rounds.cjs`): an override takes a STRING,
never a boolean, and changes the verdict line to `… passed WITH OVERRIDES` so a waved-through finding
can never read as an absent one.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The marker — five frontmatter inserts, zero prose changed</name>
  <files>.planning/milestones/v4.0-phases/238-microsoft-graph-onedrive/238-VERIFICATION.md, .planning/milestones/v4.0-phases/240-mail-is-a-shape-not-a-fourth-adapter/240-VERIFICATION.md, .planning/milestones/v4.0-phases/241-recall-at-corpus-scale/241-VERIFICATION.md, .planning/phases/242-ship-it-and-prove-what-already-shipped/242-VERIFICATION.md, .planning/phases/244-the-chat-shell-and-the-composer/244-VERIFICATION.md</files>
  <read_first>
    - `.planning/phases/243-the-thinking-block-and-the-follow-scroll-seam/243-VERIFICATION.md` lines 1-6 — the verbatim pattern
    - each subject file's **first 20 lines only** (⛔ not its verdicts — that is the forbidden re-review)
  </read_first>
  <action>
**Before editing anything, capture the pre-edit facts you will assert against afterwards** — do not
trust the table in `<interfaces>`, re-derive from the files you are about to touch:

  for f in <the five paths>; do
    echo "$f md5=$(md5sum "$f" | cut -d' ' -f1) reviewed_word=$(grep -owc 'reviewed' "$f") review_sub=$(grep -oc 'review' "$f") lines=$(wc -l < "$f")"
  done

(Git Bash has `md5sum`. On PowerShell use `certutil -hashfile <f> MD5`.)

**(a) 241, 242, 244 — insert ONE line into the existing frontmatter**, immediately after the
`verified:` key (241, 242, 244 all have one), matching 243's placement exactly:

  verification_mode: self-verified   # ⛔ OV-SOLO-01 / D-245-01 — NEVER "reviewed". No independent §6.3 reviewer exists.

⛔ Cite **`D-245-01`**, not `D-243-12` — the comment must point at the decision that put it there.
⛔ Do not reorder, reword or reflow any existing key. `git diff --numstat` for each of these three
MUST read exactly `1  0  <path>` — one insertion, **zero deletions**.

**(b) 238 and 240 — prepend a frontmatter block** above line 1 (`# Phase 238 — Verification` /
`# Phase 240 — Verification`). Neither file has any frontmatter today, so the block is new. Use
exactly these four keys and nothing more — ⛔ **do NOT invent a `status:` or a `score:`**, because
this plan is not permitted to read those files' verdicts and therefore cannot honestly assert one:

  ---
  phase: 238-microsoft-graph-onedrive
  verified: 2026-09-07                # the date this file's own header already carries
  verification_mode: self-verified   # ⛔ OV-SOLO-01 / D-245-01 — NEVER "reviewed". No independent §6.3 reviewer exists.
  independent_review: owed            # AGENTS.md §6.3 — nobody who did not shape this build has looked at it
  ---

240's block is the same with `phase: 240-mail-is-a-shape-not-a-fourth-adapter` and
`verified: 2026-09-09` (both dates are already stated in each file's own header — ⚠ read the header
line, do not guess).

⛔⛔ **CHANGE NOTHING ELSE IN ANY OF THE FIVE FILES.** Not a heading, not a verdict, not a table
cell, not one of the `review`/`reviewed` occurrences. Every one of them is an honest sentence naming
an owed review, and **D-01 is explicit that auditing those occurrences is the REJECTED option and is
the re-review the ROADMAP forbids.**

**(c) Record the DEVIATION, because 242 and 244 are not in D-01's list.** D-01 names three files;
this task marks five. Write the reason into the SUMMARY as `DEVIATION-245-01-A`:

> The guard's boundary is *every `*-VERIFICATION.md` under `.planning/phases/`* plus the three
> archived files D-01 names. 242 and 244 sit inside that boundary, in the same directory as 243, and
> were both solo-verified — 242's own frontmatter already says `solo_run: true` /
> `independent_review: false`. **Excluding two siblings from a boundary is the hole that goes
> silent**, which is the exact failure mode this requirement family exists to prevent; including them
> costs two inserted lines. ⛔ It is a scope deviation and is recorded as one, not smuggled.
  </action>
  <acceptance_criteria>
    - `grep -c "^verification_mode: self-verified" <each of the five files>` returns exactly `1`.
    - `git diff --numstat` shows `1  0` for 241, 242 and 244 (one insertion, ZERO deletions) and `5  0` for 238 and 240 (the four-key block plus its closing `---`; ⚠ assert deletions are `0`, not the insertion count, which depends on your exact block).
    - `git diff --numstat` lists **exactly five** paths; no sixth file appears.
    - For every one of the five files, the post-edit `grep -owc 'reviewed'` and `grep -oc 'review'` counts EQUAL the pre-edit counts captured at the top of this task. ⛔ A single changed count means prose was touched.
    - `python -c "import sys,yaml;[yaml.safe_load(open(p,encoding='utf-8').read().split('---')[1]) for p in sys.argv[1:]]" <the five paths>` parses without error — the frontmatter of all five is valid YAML (use `backend/venv/Scripts/python.exe`; `pyyaml` is present).
    - 238 and 240 contain no `status:` or `score:` key — this plan asserted no verdict it was not allowed to read.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'n=0; for f in ".planning/milestones/v4.0-phases/238-microsoft-graph-onedrive/238-VERIFICATION.md" ".planning/milestones/v4.0-phases/240-mail-is-a-shape-not-a-fourth-adapter/240-VERIFICATION.md" ".planning/milestones/v4.0-phases/241-recall-at-corpus-scale/241-VERIFICATION.md" ".planning/phases/242-ship-it-and-prove-what-already-shipped/242-VERIFICATION.md" ".planning/phases/244-the-chat-shell-and-the-composer/244-VERIFICATION.md"; do c=$(grep -c "^verification_mode: self-verified" "$f"); echo "$f -> $c"; [ "$c" = "1" ] || n=1; done; d=$(git diff --numstat -- ".planning" | awk "{s+=\$2} END {print s+0}"); echo "total deletions across .planning = $d"; [ "$d" = "0" ] || n=1; exit $n'</automated>
  </verify>
  <done>All five files carry the literal token in valid frontmatter; the whole `.planning/` diff has ZERO deletions; the `review`/`reviewed` counts are byte-for-byte what they were.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The guard (D-02) and its PostToolUse hook (D-03) — RED on both arms against a planted defect, GREEN on the five real files</name>
  <files>scripts/check-verification-honesty.cjs, .claude/hooks/verification-honesty-guard.js, .claude/settings.json, .planning/STATE.md</files>
  <read_first>
    - `scripts/check-hot-file-ledger.cjs` — the whole file, once: the exit convention, the `MIN_SCAN_ROWS` non-vacuity floor, the EXACT-header-never-a-prefix habit, and the "print the derivation" habit
    - `.claude/hooks/hot-file-ledger-guard.js` — the whole file, once: the stdin/regex/execFileSync/exit-0 contract and the header explaining why it is not a PreToolUse blocker
    - `scripts/check-gap-closure-rounds.cjs` — its worded-override handling and `passed WITH OVERRIDES` verdict line, only
  </read_first>
  <behavior>
    Each arm MUST be seen with its own eyes before the gate is trusted. The planted defects go into
    `243-VERIFICATION.md` — chosen deliberately: it is NOT one of the five files Task 1 edits, so its
    md5 restoration is unambiguous and cannot be confused with this plan's own diff.

    ⛔ **RUN THEM IN THIS ORDER. B-0 GATES B.**

    - **Arm B-0 — the COMMENT-STRIP PRECONDITION, and the arm that makes Arm B mean anything.** With
      nothing planted, the six marked files — **whose `verification_mode` comments every one contain
      the word `reviewed`** (*`NEVER "reviewed"`*) — exit `0` with **zero**
      `[frontmatter-claims-review]` findings. ⛔ If this is red, the value/comment split is broken and
      **Arm B below is unattributable**: the gate would be firing on the same code path for a reason
      that is not the plant, which is half-driven, not driven RED.
    - **Arm A — `[no-verification-mode]`:** delete the `verification_mode` line from
      `243-VERIFICATION.md` → gate exits `1`, output NAMES the file and the finding code. Restore →
      md5 identical to the value captured before planting.
    - **Arm B — `[frontmatter-claims-review]`:** set 243's `verification_mode` **value** (left of the
      `#`) to `reviewed` → gate exits `1` with that code, **and B-0 has already proved the same code
      path was silent a moment earlier**. Restore → md5 identical.
    - **Arm C — the FALSE-POSITIVE control on BODY prose:** with nothing planted, the scan exits `0`
      **while 238 / 240 / 241 appear in the printed subject list** — proving their bodies' `review`
      × 13 / 11 / 11 and `reviewed` × 1 / 0 / 2 do NOT fire. A substring-keyed predicate would fire on
      all three correct files; this one must not. ⚠ Arm B-0 and Arm C are different controls: B-0 is
      about the **comment on the marker line**, C is about the **body below the frontmatter**.
    - **Arm D — the hook wiring, FIVE cases:** (1) a `.planning/phases/**` VERIFICATION.md path with
      Arm A planted → stdout contains `hookSpecificOutput` and the finding; (2) the same path clean →
      **empty stdout**, exit 0; (3) a `*-PLAN.md` path → **empty stdout**, exit 0 (early exit, one
      regex test); (4) malformed stdin (`not json`) → exit 0, empty stdout — never break the turn;
      (5) ⛔ **an ARCHIVED out-of-boundary path** (any `*-VERIFICATION.md` under
      `.planning/milestones/` that is NOT one of the three pinned — e.g. a v3.x phase) → **empty
      stdout, exit 0**. Case 5 is the one that proves the hook cannot red the 212 files the header
      declares out of bounds.
    - **Arm E — non-vacuity, on the INVARIANT axis:** temporarily point the pinned list at a
      nonexistent path (or run the gate from a directory where the pinned paths do not resolve) →
      scan mode exits `2`, not `0`. Separately, `--files` with **zero arguments** → exits `2`.
      ⛔ `--files <one real file>` must exit `0` or `1`, **never `2`** — that is the hook's own call
      path. A gate that passes over zero files is worse than absent:
      `check-hot-file-ledger.cjs` was measured exiting `0` over `subject: 0 files` on a CRLF plan at
      Phase 242.
    - **Arm F — the glob catches an UNPINNED file, proved live rather than assumed.** Create a
      throwaway `.planning/phases/245-.../ZZ-TEST-VERIFICATION.md` with no frontmatter → scan mode
      subject count rises by one **and** the gate exits `1` naming it → delete the file → the count
      returns to its prior value and the gate exits `0`. ⛔ This is the only honest way to show the
      active-milestone half of the boundary works; do NOT try to prove it with `245-VERDICT.md`,
      which is not a `*-VERIFICATION.md` and is deliberately outside the subject set.
  </behavior>
  <action>
**(a) `scripts/check-verification-honesty.cjs` — D-02's executable guard.**

Usage, mirroring `check-hot-file-ledger.cjs`:

  node scripts/check-verification-honesty.cjs                 # the pinned scan set
  node scripts/check-verification-honesty.cjs --files a.md b.md
  node scripts/check-verification-honesty.cjs --review-by "<who reviewed it, and why they did not shape the build>"

EXIT `0` clear · `1` violation · `2` harness error.

⛔⛔ **RESOLVE THE BOUNDARY EXPLICITLY IN THE HEADER — this is the design decision that makes the
gate runnable at all, and it was measured, not guessed.** A repo-wide *"every `*-VERIFICATION.md`
must carry the field"* gate **fails 217 of 218 files on its first run**, back to
`01-VERIFICATION.md`. A gate that reds 217 files on day one is a gate everyone learns to ignore. So:

  SUBJECT SET = every `*-VERIFICATION.md` under `.planning/phases/`   (the ACTIVE milestone)
              + these three PINNED archived paths, which DEBT-03 names by requirement:
                  .planning/milestones/v4.0-phases/238-microsoft-graph-onedrive/238-VERIFICATION.md
                  .planning/milestones/v4.0-phases/240-mail-is-a-shape-not-a-fourth-adapter/240-VERIFICATION.md
                  .planning/milestones/v4.0-phases/241-recall-at-corpus-scale/241-VERIFICATION.md

  OUTSIDE THE BOUNDARY, and say so in the header rather than leaving it to be discovered:
  the other 212 archived `*-VERIFICATION.md` under `.planning/milestones/`. Retrospectively
  re-marking closed milestones back to v1.0 is a 212-file audit — **the exact "a two-line honesty fix
  becomes the largest phase in the milestone" failure mode the ROADMAP names.** DEBT-03 names three
  files; the active milestone is added because a live directory with two unmarked siblings is a hole
  that goes silent. ⚠ When a milestone is archived, its phases leave the boundary **carrying** the
  marker, which is the direction that costs nothing.

  ⛔⛔ **NON-VACUITY — FLOOR ON THE INVARIANT PART, NEVER ON THE TRANSIENT PART.**
  `const MIN_PINNED_FILES = 3;` — asserted against the three PINNED paths **resolving on disk**.
  That is the true analog of `check-hot-file-ledger.cjs:46`'s `MIN_SCAN_ROWS = 150`, which floors the
  **reference corpus** and never the subject count (measured: `--files` there legitimately resolves
  `subject: 2 files · watched: 0 · OK`).
  ⛔ **DO NOT floor on the total subject count.** `.planning/phases/` is a directory *designed to
  empty*: at the v4.1 close, 242-246 archive to `.planning/milestones/v4.1-phases/` exactly as 238 and
  241 already did, the active set drops from 3 to 0, and a `MIN_SUBJECT_FILES = 6` floor would exit `2`
  **forever, on the gate and on every hook invocation**. ⚠ A guard reporting `harness error` on every
  run within weeks of shipping is precisely the lapse DEBT-03 exists to prevent
  (*"so it cannot lapse unnoticed a second time"*).
  Print `subject: N files (3 pinned + M active)` on every run, so **a shrinking active set is VISIBLE
  without being FATAL**.

  MODES, and the floor applies to exactly one of them:
  · **scan mode** (no `--files`): fail `2` if fewer than `MIN_PINNED_FILES` pinned paths resolve.
  · **`--files` mode**: fail `2` only on **zero arguments**. ⛔ One file is a legitimate resolution —
    this is the mode the hook uses, and a floor here would make the hook exit `2` on every
    VERIFICATION.md write, contradicting Arm D case 2 in the same task.

**The predicate — three findings, and ONE deliberate non-check.** Write the measured justification
table into the header verbatim, because it is what makes the scoping defensible:

  | file | \breviewed\b | substring `review` | code-review |
  |---|---|---|---|
  | 238 |  1 | 13 | 0 |
  | 240 |  0 | 11 | 3 |
  | 241 |  2 | 11 | 1 |

  · `[no-frontmatter]`            — the file has no `---` fenced block starting at line 1.
  · `[no-verification-mode]`      — the frontmatter has no `verification_mode` key.
  · `[frontmatter-claims-review]` — a frontmatter key's **VALUE, taken left of the first unquoted
                                    `#`**, matches /\breviewed\b/i while OV-SOLO-01 is live. (Also
                                    fires on `verification_mode` values other than `self-verified`
                                    that claim a review.)

  ⛔⛔ **THE COMMENT MUST BE STRIPPED, AND THIS IS NOT AN OPTIMISATION — WITHOUT IT THE GATE REDS
  EVERY FILE IT IS BUILT TO BLESS.** The mandated marker is, verbatim:

      verification_mode: self-verified   # ⛔ OV-SOLO-01 / D-245-01 — NEVER "reviewed". No independent §6.3 reviewer exists.

  **The word `reviewed` sits inside the comment of the very line the predicate reads.** A naive
  value-match fires `[frontmatter-claims-review]` on all six subject files, every one correct. So:
  split each frontmatter line at the first `#` that is **not inside a quoted string**, take the left
  part, trim it, and match only that. No YAML dependency is needed or permitted (`fs`, `path`,
  `child_process` only) — this is a `split`, not a parser.

  ⚠ **The second-order consequence is why this is a blocker and not a nit:** if the gate were already
  firing `[frontmatter-claims-review]` on every file, then **Arm B's planted defect would be
  UNATTRIBUTABLE** — the gate would fire for a reason other than the plant, on the same code path,
  which is *half-driven*, not driven RED. **Arm B-0 exists to prove the strip works BEFORE Arm B is
  believed.**

  ⛔ **THE NON-CHECK, and it is load-bearing: BODY PROSE IS NEVER INSPECTED.** Not one line below the
  closing `---`. The three correct files carry 35 `review` substrings and 3 `reviewed` words between
  them, every one an honest sentence naming an owed review. **A predicate that read the body would
  fire on all three CORRECT files and would pressure a future agent to delete the honesty this gate
  exists to protect.** State that reasoning in the header; a future maintainer who "improves" the
  gate by scanning the body must find the reason it was refused.

**OV-SOLO-01 liveness — key on an INDEXED MARKER, never on prose.** Read `.planning/STATE.md` for a
line matching `^OV-SOLO-01-status:\s*(live|retired-\S+)`; print the matched line **verbatim** as part
of the derivation. `live` → the `[frontmatter-claims-review]` arm runs. `retired-<date>` → the arm is
skipped and the run prints `OV-SOLO-01-status: retired-<date> — claims-review arm SKIPPED`. **Marker
absent** → the arm is skipped and the run prints `OV-SOLO-01-status: NOT FOUND — claims-review arm
SKIPPED (this is itself a finding: the index line is missing)`. ⛔ A skip is ALWAYS printed; a silent
skip is a gate that stopped guarding without saying so.

⛔ **A prose-scanning predicate was CONSIDERED AND REFUSED, and the reason belongs in the header.**
Keying on *"no line containing `OV-SOLO-01` also contains `RETIRED`"* is disabled by this project's
own house style: **D-08 requires corrections to sit BESIDE their originals**, so the eventual
retirement line will contain both tokens forever — and so would any note quoting it. One sentence of
the form *"OV-SOLO-01 is not RETIRED"* would silently disable the arm **today**. The indexed marker
mirrors the rule this project already knows: **`status:` frontmatter IS the index; body prose is
invisible to every scan.**

⚠ **The marker line does not exist yet — THIS TASK ADDS IT**, one line in `.planning/STATE.md`
directly under the `⭐ OV-SOLO-01` heading:

  OV-SOLO-01-status: live   # machine-readable index for scripts/check-verification-honesty.cjs. Flip to `retired-<YYYY-MM-DD>` when the ruling is re-armed; ⛔ do not delete it — an absent marker SKIPS the claims-review arm.

⛔ Add only that line here. Task 3 adds the human-readable confirmation block; ⛔ neither touches an
operator-authored sentence, and ⛔ neither calls a `state.*` SDK verb.

**`--review-by "<string>"`** — the worded escape hatch, a STRING and never a boolean, exactly as
`check-gap-closure-rounds.cjs` does it. It suppresses `[frontmatter-claims-review]` for that run and
changes the verdict line to `honesty gate passed WITH OVERRIDES`, never `OK`. ⛔ It does NOT suppress
`[no-frontmatter]` or `[no-verification-mode]`.

Print the derivation per file — resolved path · frontmatter found y/n · `verification_mode` value
(**comment stripped**) · findings — then the subject line and the verdict line, in the shape this
project reads:

  subject: 6 files (3 pinned + 3 active)
  OV-SOLO-01-status: live
  honesty gate OK — 6/6 subject files carry verification_mode, 0 frontmatter review claims.

⚠ **Normalise CRLF before parsing.** A CRLF planning doc is exactly how
`check-hot-file-ledger.cjs` was measured passing over `subject: 0 files` at Phase 242.

**(b) `.claude/hooks/verification-honesty-guard.js`** — copy `hot-file-ledger-guard.js`'s structure
line for line, changing only the path regex, the gate path and the `additionalContext` text.

⛔⛔ **THE REGEX MUST NOT BE WIDER THAN THE GATE'S OWN BOUNDARY.** A naive
`/\.planning\/.*-VERIFICATION\.md$/` matches **all 218** files including the **212 the header
declares OUT OF BOUNDS**; each would be handed to `--files`, found to lack `verification_mode`, and
red — a loud finding on a file the design deliberately excludes. Use:

  /\.planning\/phases\/[^/]+\/.*-VERIFICATION\.md$/     on the `/`-normalised path
  OR the path is one of the THREE PINNED archived paths (exact string compare)

**Belt and braces, because a regex and a header can drift apart:** `--files` mode in the gate itself
prints `outside boundary — skipped` and contributes **no finding** for any path that is neither under
`.planning/phases/` nor in the pinned list. The boundary is then defined in ONE place semantically and
enforced in two mechanically, and neither can quietly widen the other.

Keep every one of these:
- ONE regex test then `process.exit(0)` for every other write in the repo;
- `try { JSON.parse } catch { process.exit(0) }` — never break the turn on a parse failure;
- `execFileSync(process.execPath, [gate, '--files', target], {cwd: root, encoding:'utf8', timeout: 15000})`;
- gate exit `0` → say nothing, cost nothing;
- on failure, write `{hookSpecificOutput:{hookEventName:'PostToolUse', additionalContext, honesty_gate_exit, file_path}}` and **still `process.exit(0)`**.

Its header must state in its own words **why it is not a PreToolUse blocker**: a verifier writing a
VERIFICATION.md is doing the right thing and simply owes a field; a hook must never deny that write.
It reports loudly, in-turn, while the author still holds the reason. And **why a hook rather than
CI**: `develop` was measured running **634 commits over 8 days without a push**, so an `on: push`
gate could not have fired once in that window — and a verification written and committed in one
session would slip past it entirely. ⛔ **No CI workflow is added by this plan** (D-03's rejected
third option: a second file to keep in sync over a planning-doc rule).

**(c) `.claude/settings.json`** — append the fifth `Write|Edit` PostToolUse entry exactly as given in
`<interfaces>`. ⛔ Do not reorder or reindent the existing six entries; `git diff` must be a pure
addition. Re-parse the file afterwards (`node -e "require('./.claude/settings.json')"`) — a broken
settings.json silently disables **every** hook, including the four already there.

**(d) DRIVE IT RED — arms B-0, A, B, C, D(1-5), E and F, in the order given in `<behavior>`. This is
D-02's binding clause** and this project's standing rule. Capture
`md5sum .planning/phases/243-.../243-VERIFICATION.md` **before** planting, after each restore, and
quote every value plus each verbatim failure line in the SUMMARY. ⛔ A guard nobody has seen fire is
not a guard, and the victim file must come back md5-identical.

⛔ **Arm B-0 runs FIRST and gates Arm B.** If B-0 is not green, Arm B's red is unattributable and the
gate is **not** driven — ⛔ **do not weaken, reword or drop Arm B to get past it.** Fix the
comment-strip.
  </action>
  <acceptance_criteria>
    - `node scripts/check-verification-honesty.cjs` exits `0`, prints `subject: 6 files (3 pinned + 3 active)` and `OV-SOLO-01-status: live`, and its per-file derivation NAMES 238, 240, 241, 242, 243 and 244 each with `verification_mode: self-verified`.
    - **Arm B-0 green, and named as the precondition it is:** the clear run reports **zero** `[frontmatter-claims-review]` findings across six files whose marker comments all contain the word `reviewed`. ⛔ If this is not green, Arm B is not driven and the task is not done — ⛔ fix the strip, never the arm.
    - Arm A seen RED: the SUMMARY quotes the verbatim `[no-verification-mode]` line and exit code `1`.
    - Arm B seen RED: the SUMMARY quotes the verbatim `[frontmatter-claims-review]` line and exit code `1`, **and cites Arm B-0's green run immediately above it as the attribution**.
    - Arm C green with the control asserted: the clear run's subject list includes 238/240/241 — the gate INSPECTED the three files whose bodies carry 35 `review` substrings and passed them.
    - Arm D **all five** cases as specified; cases 2, 3, 4 and **5** produce **empty stdout** (`[ -z "$out" ]`), not merely exit 0.
    - Arm E: scan mode with the pinned list unresolvable exits `2`; `--files` with **zero** arguments exits `2`; ⛔ `--files <one real subject file>` exits `0` or `1` and **never `2`** (this is the hook's own call path — a `2` here means every VERIFICATION.md write reports a harness error).
    - Arm F: the throwaway `ZZ-TEST-VERIFICATION.md` raised the subject count by one and reded the gate; after deletion the count and the verdict returned to their prior values. `git status --short` is clean of it.
    - `md5sum` of `243-VERIFICATION.md` is identical before planting and after the final restore; `git diff -- .planning/phases/243-the-thinking-block-and-the-follow-scroll-seam/` is EMPTY.
    - `.planning/STATE.md` carries exactly one `^OV-SOLO-01-status: live` line; `git diff --numstat -- .planning/STATE.md` second column is `0` (insertions only).
    - `node -e "require('./.claude/settings.json')"` exits 0; the PostToolUse array length equals **its pre-edit length + 1** (capture the pre-edit length first — ⛔ do not hardcode a total), and the new entry's command string contains `verification-honesty-guard.js`.
    - `grep -c "verification-honesty-guard" .claude/settings.json` returns exactly `1`.
    - ⛔ `ls .github/workflows/ | grep -i honesty` returns nothing — no CI backstop was added.
    - `git diff --stat -- backend/ frontend/ supabase/ docs/ CLAUDE.md` is EMPTY.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-verification-honesty.cjs && node -e "const p=require('./.claude/settings.json').hooks.PostToolUse;const hit=p.filter(e=>JSON.stringify(e).includes('verification-honesty-guard'));if(hit.length!==1)throw new Error('expected exactly 1 honesty-guard entry, got '+hit.length);if(hit[0].matcher!=='Write|Edit')throw new Error('wrong matcher: '+hit[0].matcher);console.log('settings OK: '+p.length+' PostToolUse entries, honesty guard registered once on Write|Edit')"</automated>
  </verify>
  <done>The gate exists, has a stated boundary and a non-vacuity floor, was seen failing on two planted defects and passing on the five real files without firing on their honest prose; the hook is registered and silent on everything else.</done>
</task>

<task type="auto">
  <name>Task 3: SC#4 discharged by citation — 245-VERDICT.md created, STATE.md gets one line</name>
  <files>.planning/phases/245-the-verification-debt-discharged-or-retired-in-writing/245-VERDICT.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/STATE.md` lines **468-512** — the `OV-SOLO-01` block in full. ⛔ This is operator-authored text: read it to QUOTE it, never to improve it.
    - `.planning/ROADMAP.md` → Phase 245 block — SC#1..SC#4 verbatim, and the `## How we'd know this failed` list
  </read_first>
  <action>
**(a) Create `245-VERDICT.md`** — the phase's written-verdict file, distinct from `245-UAT-RESULTS.md`
(which `245-02` owns for driven rows, per D-12). Frontmatter:

  ---
  phase: 245
  kind: verdict
  written: <date>
  author: claude (solo — OV-SOLO-01)
  verification_mode: self-verified   # ⛔ OV-SOLO-01 — this file is not exempt from its own rule
  sc_status: { "1": owed-245-03, "2": owed-245-02, "3": discharged, "4": discharged }
  ---

⚠ Give it the `verification_mode` field too — it would be absurd for the file that ships the honesty
marker to be the one document without it.

⛔ **BUT SAY PLAINLY, IN THE FILE, THAT IT IS OUTSIDE THE GATE'S SUBJECT SET.** `245-VERDICT.md` is
not a `*-VERIFICATION.md`; it matches neither the boundary glob nor the hook regex, and the gate's
`subject` count **stays 6**. ⛔⛔ **DO NOT "fix" this by widening the gate's glob to `-VERDICT.md`** —
that changes the guard's contract mid-flight, with no review, and re-breaks the floor Task 2 just
got right. The live proof that the glob catches an unpinned file is **Arm F** (a throwaway
`ZZ-TEST-VERIFICATION.md`, created, seen, deleted), and it belongs to Task 2, not here.

**§ SC#3 — discharged.** State: the deliverable was a greppable marker plus untouched prose (D-01).
Record the measured counts that make the reasoning auditable — `\breviewed\b` = 1 / 0 / 2 and
substring `review` = 13 / 11 / 11 across 238 / 240 / 241 — and state plainly that **SC#3's literal
clause ("does not find 'reviewed'") was measured unsatisfiable**, because every occurrence is an
honest sentence naming an owed review, and satisfying it literally would have deleted the record it
protects. Name the five files marked and record `DEVIATION-245-01-A` (242 and 244 included beyond
D-01's three, with its reason). Name the gate and the hook, and say exactly what the gate does NOT
check and why (body prose).

⚠ **State the limit of the marker honestly, because the ROADMAP names the opposite as a failure
mode** — *"a VERIFICATION.md gets the word 'self-verified' added while its verdict section still
reads as though a reviewer signed it."* Record: all three files **already** declared themselves
self-verifications in their opening paragraphs (238:3-9, 240:9-13, 241:25-29), which is *why* no
prose needed changing; the marker adds machine-checkability, not a new claim. ⛔ And record what was
NOT done: **no verdict inside those files was re-read or re-judged** — that is the re-review the
ROADMAP forbids, and this phase did not perform it.

**§ SC#4 — discharged by CITATION (D-04).** ⛔ **Do not re-derive, rewrite, summarise-over or
reorder the operator's ruling.** Quote it verbatim from `.planning/STATE.md` (the `⭐ OV-SOLO-01 —
RULED ON 2026-09-11 BY THE OPERATOR` block), with its line range, and tick off all four elements SC#4
demands, each with the quoted sentence that satisfies it:

  1. solo running continues;
  2. the dispatched code-review subagent is **MANDATORY** on any phase touching a trust boundary and
     is **NOT** an independent gate;
  3. `/code-review ultra` stays **ruled out on cost**;
  4. the re-arm trigger — *"Gemini's quota returns, **or the v4.1 close, whichever is first.**"*

⭐ Record the finding that matters for the register, not just the tick: **SC#4 was ALREADY SATISFIED
at HEAD before this phase began.** The ROADMAP, REQUIREMENTS and STATE.md all described it as
pending. That is the same class of error as the stale Azure claim `245-03` corrects, **in the
opposite direction** — three registers said a thing was owed when the artifact said it was done — and
the CONTEXT's `<specifics>` names both directions as in scope to prevent. Cite the precedent:
`feedback_a_review_is_a_claim_about_code_not_the_code` — *each register only knows the one below it;
the artifact is the bottom.*

⚠ Record as **deferred, not silent**: making the re-arm trigger itself executable was considered
under D-04 and **not taken**. Trigger: the v4.1 close, where it fires and can be watched working or
failing.

**§ SC#1 / SC#2 — placeholders, named and owned.** Two stub sections stating which plan owns each
(`245-03` and `245-02`) and what it must contain. ⛔ Leave them EMPTY of verdicts. Silence is how the
rows this phase exists to discharge got here in the first place; an unfilled section with an owner is
not silence, a missing section is.

**(b) `.planning/STATE.md` — ONE added block under `OV-SOLO-01`, and nothing else.** Hand-edit with
Edit. ⛔ **NEVER a `state.*` SDK verb.** ⚠ Task 2 has already inserted the machine-readable
`OV-SOLO-01-status: live` index line under that heading — ⛔ do not duplicate it, and do not move it;
this block is the **human-readable** half and sits beneath it.

  **Confirmed by Phase 245 (DEBT-03 / SC#4), <date>:** measured **complete at HEAD before the phase
  began** — all four elements and the re-arm trigger present. Cited verbatim in
  `245-VERDICT.md` §SC#4. ⛔ Nothing here was re-derived, reordered or rewritten. ⚠ The register
  said `pending`; the artifact said `done`. **Three registers can be wrong in the direction of
  "still owed" too.**

⛔ Do not touch STATE.md's item 3 or item 4 — item 4's stale Azure claim belongs to `245-03`, and
splitting one correction across two plans is how a register ends up half-corrected.
  </action>
  <acceptance_criteria>
    - `.planning/phases/245-.../245-VERDICT.md` exists, its frontmatter parses as YAML, and it carries `verification_mode: self-verified`.
    - `node scripts/check-verification-honesty.cjs` still exits `0` and still prints `subject: 6 files (3 pinned + 3 active)`. ⛔ **The count must NOT change** — `245-VERDICT.md` is deliberately outside the subject set, and a `7` here would mean somebody widened the gate's glob to chase a criterion.
    - §SC#4 quotes all four `OV-SOLO-01` elements verbatim, each traceable to a line in STATE.md, and names the re-arm trigger literally including *"whichever is first"*.
    - §SC#4 states that SC#4 was already satisfied before the phase began, and cites where the registers disagreed.
    - §SC#1 and §SC#2 exist, name their owning plan, and contain **no verdict** — `grep -c "PASS\|✅" ` inside those two sections returns `0`.
    - `git diff -- .planning/STATE.md` shows insertions only (`git diff --numstat -- .planning/STATE.md` second column is `0`), and the only region touched is under the `OV-SOLO-01` heading.
    - ⛔ No `state.*` SDK verb appears anywhere in the executor's transcript for this task.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-verification-honesty.cjs && bash -c 'd=$(git diff --numstat -- .planning/STATE.md | awk "{print \$2}"); echo "STATE.md deletions=$d"; [ "$d" = "0" ]'</automated>
  </verify>
  <done>SC#4 is discharged by verbatim citation with the register-vs-artifact disagreement recorded; SC#1/SC#2 are named placeholders with owners; STATE.md gained one block and lost nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| **local tool input → hook process** | The PostToolUse hook parses JSON arriving on stdin from the harness and derives a filesystem path from it. That path is the only untrusted value in this plan. |
| none else crossed | No application source, no network call, no database access, no auth decision, no dependency, no migration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-245-01-01 | Elevation of Privilege | `.claude/hooks/verification-honesty-guard.js` — stdin `file_path` | mitigate | The path is used ONLY as (a) a regex subject and (b) an `execFileSync` **argv element** — never concatenated into a shell string, never `exec`'d. `execFileSync(process.execPath, [gate, '--files', target])` cannot be argument-injected by a crafted path, matching `hot-file-ledger-guard.js` exactly. ⛔ No `child_process.exec`, no template-string command. |
| T-245-01-02 | Denial of Service | the hook, on every `Write`/`Edit` in the repo | mitigate | ONE regex test then `process.exit(0)` for any non-`*-VERIFICATION.md` path, so the common case costs one string comparison. `timeout: 15000` on the `execFileSync` and `timeout: 10` on the settings entry bound the worst case. The hook exits `0` on every path including failure, so it can never wedge a turn. |
| T-245-01-03 | Tampering | `.claude/settings.json` | mitigate | A malformed settings.json silently disables **all** hooks including the four already registered — so the entry is appended without reindenting the existing six, and re-parsed (`node -e "require(...)"`) plus length-asserted (`=== 7`) as an acceptance criterion. |
| T-245-01-04 | Repudiation | `scripts/check-verification-honesty.cjs` | mitigate | The whole point of the plan: a self-verification that reads as a review is a repudiation risk on every verdict downstream of it. The gate makes the marker's absence loud; `MIN_PINNED_FILES` (floored on the INVARIANT pinned set, never on the transient active set) makes a vacuous pass impossible without self-disabling at the next milestone archive; `--review-by` is a worded override that prints `passed WITH OVERRIDES`, so a waved-through claim can never read as an absent one. |
| T-245-01-05 | Information Disclosure | the three archived VERIFICATION.md files | accept | They are already committed planning documents; this plan adds four frontmatter keys and reads only their first 20 lines. No secret is introduced or surfaced. |
| T-245-01-SC | Tampering | npm/pip/cargo installs | n/a | **No package is installed.** `git diff -- package.json package-lock.json frontend/package.json frontend/package-lock.json backend/requirements.txt` MUST be empty; both new files are zero-dependency Node using only `fs`, `path` and `child_process`. No legitimacy audit is owed because no dependency is added. |
</threat_model>

<verification>
⛔ **Read this section as a list of OBSERVATIONS and GATE RUNS, not of test suites. Nothing in this
plan closes on a unit test — this phase's entire subject is that distinction.**

**SC#3 closes on an artifact observation a person can repeat in one command:**
```
grep -n "^verification_mode: self-verified" \
  .planning/milestones/v4.0-phases/238-microsoft-graph-onedrive/238-VERIFICATION.md \
  .planning/milestones/v4.0-phases/240-mail-is-a-shape-not-a-fourth-adapter/240-VERIFICATION.md \
  .planning/milestones/v4.0-phases/241-recall-at-corpus-scale/241-VERIFICATION.md
```
Three hits = SC#3's marker half is true. ⛔ **The gate passing is NOT what closes SC#3** — the gate is
a separate deliverable that makes future absence loud. Do not report SC#3 as closed *because the
gate is green*; report it closed because the token is in the three files and their prose is
byte-unchanged (`git diff --numstat` deletions = 0).

**SC#4 closes on a citation:** `245-VERDICT.md` §SC#4 quotes all four elements plus the re-arm
trigger from `.planning/STATE.md`, and the quoted text still exists at that path.

**The gate's own evidence (a deliverable, not a criterion):**
- `node scripts/check-verification-honesty.cjs` → exit 0, `subject: 6 files (3 pinned + 3 active)`,
  `OV-SOLO-01-status: live`, derivation printed. ⛔ The count must be **6**, not 7 — `245-VERDICT.md`
  is deliberately outside the subject set and widening the glob to include it is forbidden.
- Both RED arms quoted verbatim in the SUMMARY, plus the three md5 values proving
  `243-VERIFICATION.md` came back identical.
- Arm C named explicitly in the SUMMARY as the false-positive control: **the gate inspected the three
  files carrying 35 `review` substrings and passed them.**
- `node -e "require('./.claude/settings.json')"` → exit 0; PostToolUse length `7`.

**Negative fences — assert these, do not assume them:**
- `git diff --stat -- backend/ frontend/ supabase/ docs/ CLAUDE.md` is **EMPTY**. No application
  source, no ledger, no CLAUDE.md. The backend ceiling (71, zero headroom) and the vitest count gate
  are consequently **not run** for this plan, and the SUMMARY must say that is a deliberate omission
  with the empty diff as its justification — not an oversight.
- Total deletions across the whole `.planning/` diff = **0**.
- `.github/workflows/` gained nothing (D-03: no CI backstop).
- No `docs/HOT-FILE-LEDGER.md` row was added (measured exempt: `watched: 0`).

**⛔ NOT verified here, and stated as owed rather than left silent:**
- **SC#1** — 238's eleven-or-nine rows. Owned by `245-02` (M-9's drive) and `245-03` (M-8's flip,
  S-1/S-2's retirement). This plan makes no claim about any of them.
- **SC#2** — 233's five G-4 rows. Owned by `245-02`. **This plan has not opened a browser**, and
  nothing in it may be read as evidence about a driven row.
</verification>

<success_criteria>
- Five `*-VERIFICATION.md` files carry `verification_mode: self-verified` in valid frontmatter; the
  `.planning/` diff has ZERO deletions and every `review`/`reviewed` count is unchanged.
- `scripts/check-verification-honesty.cjs` exists with a **stated boundary**, a stated **non-check**
  (body prose, with the measured 13/11/11 justification), a `MIN_PINNED_FILES` floor on the invariant
  pinned set, comment-stripped value matching, an indexed `OV-SOLO-01-status:` liveness read, and a
  worded `--review-by` override.
- The gate was seen RED on `[no-verification-mode]` and `[frontmatter-claims-review]`, GREEN on all
  six subject files (**Arm B-0 first, so Arm B's red is attributable**), exit `2` below the pinned
  floor and on `--files` with zero arguments, silent on an out-of-boundary archived path (Arm D case
  5), correct on an unpinned active file (Arm F), and the victim file restored md5-identical.
- `.claude/hooks/verification-honesty-guard.js` is registered as the fifth `Write|Edit` PostToolUse
  entry, silent on non-matching paths, and never denies a write.
- `245-VERDICT.md` exists with SC#3 and SC#4 discharged and SC#1/SC#2 as owner-named placeholders.
- STATE.md carries one confirmation block under `OV-SOLO-01`, hand-edited, insertions only.
- The SUMMARY states plainly: **SC#3 and SC#4 discharged; SC#1 and SC#2 untouched and owed.** ⛔ It
  does not describe this plan as a review of anything.
</success_criteria>

<output>
Create `.planning/phases/245-the-verification-debt-discharged-or-retired-in-writing/245-01-SUMMARY.md` when done.
</output>
