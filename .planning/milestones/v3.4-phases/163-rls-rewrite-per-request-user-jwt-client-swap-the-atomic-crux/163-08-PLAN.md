---
phase: 163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux
plan: 08
type: execute
wave: 4
depends_on: ["163-01", "163-05"]
files_modified:
  - backend/app/api/skills.py
  - backend/app/api/skill_test_cases.py
  - backend/app/api/skill_tuner.py
  - backend/app/api/workflows.py
  - backend/app/api/evals.py
  - backend/app/api/settings.py
  - backend/app/api/audit.py
  - backend/app/db/workflows.py
  - backend/app/models/user_settings.py
autonomous: true
requirements: [TEN-02]
must_haves:
  truths:
    - "D-03: request-scoped Depends(get_supabase) sites across the skills, workflow, eval, settings, and audit routers are swapped to the per-request user-JWT client get_user_supabase"
    - "D-02: request-scoped connectionless pool.*() calls in db/workflows.py + models/user_settings.py become explicit get_user_pg_connection blocks carrying SET LOCAL"
    - "The skills is_global / is_system global branch still works: a global/system skill (e.g. the seeded skill-creator) remains visible to a non-owner through the user-JWT client"
    - "D-14: .eq('user_id') filters KEPT; D-v2.5-01: run_in_threadpool preserved on blocking supabase-py calls"
    - "This plan swaps request-scoped ROUTER paths only; the eval-runner / harness / re-embed ASYNC WRITERS stay service-role and are widened in plan 09 (D-05)"
  artifacts:
    - path: "backend/app/api/skills.py"
      provides: "skills CRUD request handlers on the user-JWT client"
      contains: "get_user_supabase"
  key_links:
    - from: "skills/evals/workflow request handlers"
      to: "get_user_supabase / get_user_pg_connection"
      via: "Depends swap + async-with connection"
      pattern: "get_user_supabase|get_user_pg_connection"
    - from: "skills is_global/is_system branch"
      to: "RLS global-read under role authenticated"
      via: "user-JWT client"
      pattern: "is_global|is_system"
---

<objective>
Wave 4, part 3 — the flip on the SKILLS + WORKFLOW-EVAL + IDENTITY (settings/audit) router clusters. Swap request-scoped handlers to the user-JWT clients so RLS (with the preserved is_global/is_system skill branch) becomes the real gate. This plan handles the request-scoped ROUTER paths only; the eval-runner / harness / re-embed async writers stay service-role and are widened separately in plan 09.

Purpose: skills carries the is_global/is_system global branch (the seeded skill-creator must stay cross-org visible), and evals.py is the highest-Depends-count router — swapping them completes RLS coverage of the standard request surface.

Output: the skills/workflow-eval/identity routers running on the user-JWT clients on both DB paths.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-CONTEXT.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-PATTERNS.md

<interfaces>
<!-- Consumed from plan 01 (dependencies.py). -->
get_user_supabase(request, current_user, token) -> Client         # per-request anon-key+Bearer client (keep run_in_threadpool)
get_user_pg_connection(request, current_user) -> async CM         # SET LOCAL ROLE authenticated + both GUC forms
get_service_role_supabase(org_id) -> Client                       # for background/cross-tenant only (rare here)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap the skills cluster routers (skills, skill_test_cases, skill_tuner)</name>
  <files>backend/app/api/skills.py, backend/app/api/skill_test_cases.py, backend/app/api/skill_tuner.py</files>
  <read_first>
    - backend/app/api/skills.py (12 Depends(get_supabase) — skills CRUD + the is_global/is_system global-skill surface + the skill-files/import paths)
    - backend/app/api/skill_test_cases.py (5 Depends), backend/app/api/skill_tuner.py (6 Depends)
    - backend/app/dependencies.py (the 3 factories)
    - .planning/reference_render_template_workflow_only.md + memory note SEED-102 (load_skill built-in/global precedence — confirm the global-skill visibility path)
  </read_first>
  <action>
    Swap request-scoped Depends(get_supabase) → Depends(get_user_supabase) across skills.py, skill_test_cases.py, skill_tuner.py; convert any request-scoped connectionless pool.* calls to get_user_pg_connection. Confirm a global/system skill (is_global/is_system — e.g. the seeded skill-creator) still lists/loads for a NON-owner under the user-JWT client (the RLS global branch fires). Leave the skill re-embed writer (reembed_service / skill_embedding_service) alone — plan 09 widens it. KEEP .eq("user_id") + run_in_threadpool.
  </action>
  <acceptance_criteria>
    - request-scoped Depends(get_supabase) across the 3 skills routers == 0 remaining (commented exceptions only)
    - A global/system skill remains visible to a non-owner (existing skills suite passes)
    - .eq("user_id") + run_in_threadpool intact; the re-embed writer untouched (plan 09)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/test_skills.py tests/test_skill_test_cases.py -x</automated>
  </verify>
  <done>The skills cluster routers enforce RLS via the user-JWT clients; is_global/is_system visibility preserved for non-owners.</done>
</task>

<task type="auto">
  <name>Task 2: Swap the workflow-eval routers (workflows, evals) + convert db/workflows.py pool calls</name>
  <files>backend/app/api/workflows.py, backend/app/api/evals.py, backend/app/db/workflows.py</files>
  <read_first>
    - backend/app/api/evals.py (20 Depends(get_supabase) — the highest count; eval runs/results/ratings request paths — note the eval RUNNER service is a separate async writer handled in plan 09)
    - backend/app/api/workflows.py (2 Depends + 2 pool) + backend/app/db/workflows.py (20 pool.* calls — classify request-scoped vs background/harness)
    - backend/app/dependencies.py (the 3 factories)
  </read_first>
  <action>
    Swap request-scoped Depends(get_supabase) → Depends(get_user_supabase) across evals.py + workflows.py; convert request-scoped connectionless pool.* calls in db/workflows.py + workflows.py to get_user_pg_connection. Any db/workflows.py call invoked from the harness/background path (not a request) stays on the service-role path via get_service_role_supabase(org_id) — do NOT force a request context where there is none. Do NOT edit eval_runner_service / harness_engine here (plan 09). KEEP .eq("user_id") + run_in_threadpool.
  </action>
  <acceptance_criteria>
    - request-scoped Depends(get_supabase) in evals.py + workflows.py == 0 remaining (commented exceptions only)
    - db/workflows.py request-scoped pool.* calls use get_user_pg_connection; background/harness calls use get_service_role_supabase(org_id)
    - eval_runner_service.py + harness_engine.py NOT in this plan's diff (plan 09 owns them)
    - existing workflow/eval read suites pass
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/test_workflows.py tests/test_evals.py -x</automated>
  </verify>
  <done>The workflow-eval request routers + their request-scoped db layer run on the user-JWT clients; the eval/harness async writers are left for plan 09.</done>
</task>

<task type="auto">
  <name>Task 3: Swap settings + audit routers + models/user_settings.py pool calls</name>
  <files>backend/app/api/settings.py, backend/app/api/audit.py, backend/app/models/user_settings.py</files>
  <read_first>
    - backend/app/api/settings.py (3 Depends — user_settings/app_settings request paths)
    - backend/app/api/audit.py (2 Depends — audit_log reads; recall audit_log has the D-10 nullable-org branch)
    - backend/app/models/user_settings.py (5 pool.* calls)
    - backend/app/dependencies.py (the 3 factories)
  </read_first>
  <action>
    Swap request-scoped Depends(get_supabase) → Depends(get_user_supabase) in settings.py + audit.py; convert request-scoped connectionless pool.* calls in models/user_settings.py to get_user_pg_connection. audit_log reads under the user-JWT client see the caller's own org rows + the explicit org_id IS NULL operator rows per the D-10 policy branch. KEEP .eq("user_id") + run_in_threadpool. Note: app-level/operator settings that legitimately need cross-user scope stay service-role via get_service_role_supabase(org_id).
  </action>
  <acceptance_criteria>
    - request-scoped Depends(get_supabase) in settings.py + audit.py == 0 remaining (commented operator/app-settings exceptions only)
    - models/user_settings.py request-scoped pool.* calls use get_user_pg_connection
    - existing settings/audit suites pass; user sees own settings + own-org audit rows
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/test_settings.py tests/test_audit.py -x</automated>
  </verify>
  <done>settings + audit routers run on the user-JWT clients; user_settings pool calls carry SET LOCAL; operator/app-scope exceptions kept explicit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skills/eval/workflow/settings request → DB | now the user-JWT client (RLS-enforced, with is_global/is_system + NULL-org-audit branches) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-08a | Information Disclosure | a router left on service-role | mitigate | grep gate: request-scoped Depends(get_supabase) across the 7 routers == 0 (commented exceptions only) |
| T-163-08b | Availability | is_global/is_system skill regression | mitigate | non-owner global/system skill visibility test passes under the user-JWT client |
| T-163-06b | Information Disclosure | connectionless pool.* stays BYPASSRLS | mitigate | request-scoped pool.* in db/workflows.py + user_settings.py converted to get_user_pg_connection |
| T-163-08c | Information Disclosure | forcing a request context on a background call | mitigate | harness/background db/workflows calls stay service-role via get_service_role_supabase(org_id) — not converted |
| T-163-SC | Tampering | dependency installs | accept | no new package |
</threat_model>

<verification>
- request-scoped Depends(get_supabase) across the 7 routers == 0 remaining.
- is_global/is_system skill visibility preserved for non-owners.
- eval_runner_service / harness_engine NOT in this plan's diff (plan 09 boundary).
- skills/evals/workflows/settings/audit suites pass.
</verification>

<success_criteria>
The skills + workflow-eval + identity/settings/audit request routers enforce RLS via the user-JWT clients on both DB paths; the is_global/is_system + NULL-org-audit branches are preserved; the async writers are cleanly left to plan 09; belt-and-suspenders + threadpool intact.
</success_criteria>

<output>
Create `.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-08-SUMMARY.md` when done.
</output>
