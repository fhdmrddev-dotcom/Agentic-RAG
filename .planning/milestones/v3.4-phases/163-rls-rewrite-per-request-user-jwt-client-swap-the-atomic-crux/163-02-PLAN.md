---
phase: 163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux
plan: 02
type: execute
wave: 2
depends_on: ["163-01"]
files_modified:
  - supabase/migrations/107_ten04_chunk_embedding_org_id.sql
  - backend/tests/integration/test_163_ten04_backfill.py
autonomous: true
requirements: [TEN-04]
must_haves:
  truths:
    - "D-07: org_id is denormalized onto document_chunks AND skill_embeddings, backfilled from the parent FK (documents.org_id via document_id; skills.org_id via skill_id), then flipped NOT NULL only after a self-guarded zero-NULL check"
    - "D-07: document_chunks gets a btree on org_id (or composite (org_id, user_id)) alongside the UNTOUCHED HNSW + GIN indexes — never a per-row join to documents (that join over the pgvector scan is the CONCUR-01 killer)"
    - "D-07: the RLS on document_chunks will filter the DENORMALIZED org_id directly — this migration lays that column so 163's RLS rewrite (plan 03) and Phase 164's SECDEF in-body predicate both have their substrate"
    - "Migration 107 (TEN-04) is authored to apply BEFORE migration 108 (RLS) — the document_chunks/skill_embeddings membership predicates in 108 reference the org_id column this migration adds; Postgres applies migrations in integer order so the column must land first"
    - "document_chunks + skill_embeddings are added to the mig-106 BEFORE-INSERT autofill net (parent-FK resolver) so future chunk/embedding INSERTs auto-fill org_id and pass RLS WITH CHECK without the app threading org_id"
    - "163 does NOT modify the SECDEF retrieval functions match_document_chunks / keyword_search_chunks — their org-scoping + search_path pin is Phase 164 (out of scope); 163 only lays the column"
  artifacts:
    - path: "supabase/migrations/107_ten04_chunk_embedding_org_id.sql"
      provides: "org_id column + batched backfill + self-guarded NOT-NULL flip + btree index + autofill trigger attach on document_chunks & skill_embeddings"
      contains: "document_chunks"
    - path: "backend/tests/integration/test_163_ten04_backfill.py"
      provides: "zero-NULL + NOT-NULL + btree(org_id)-present + HNSW-untouched assertions (RED until plan 05 applies 107)"
  key_links:
    - from: "document_chunks.org_id"
      to: "documents.org_id"
      via: "backfill UPDATE ... FROM documents WHERE documents.id = document_chunks.document_id"
      pattern: "document_chunks.*documents"
    - from: "skill_embeddings.org_id"
      to: "skills.org_id"
      via: "backfill UPDATE ... FROM skills WHERE skills.id = skill_embeddings.skill_id"
      pattern: "skill_embeddings.*skills"
---

<objective>
Author migration **107** (TEN-04): denormalize `org_id` onto the two pgvector hot tables `document_chunks` and `skill_embeddings` (both have NO org_id today — mig 104 explicitly deferred them to 163), backfill from the parent FK, self-guarded NOT-NULL flip, add the benchmark-driven btree alongside the untouched vector indexes, and attach both tables to the mig-106 BEFORE-INSERT autofill net. This is the SUBSTRATE the RLS rewrite (plan 03) references for these two tables — it MUST apply before 108.

Purpose: without a denormalized `org_id` column, the only way to org-scope `document_chunks` RLS is a per-row join to `documents` over the pgvector seq-scan — the single largest CONCUR-01 threat (D-07). Denormalizing kills that join.

Output: `supabase/migrations/107_ten04_chunk_embedding_org_id.sql` + `test_163_ten04_backfill.py` (RED until applied in plan 05).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-CONTEXT.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-RESEARCH.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-PATTERNS.md

<migration_order_note>
The pre-allocated slot intent was "107=RLS / 108=TEN-04". This plan set INVERTS it to **107=TEN-04, 108=RLS** because of a hard data-dependency: the RLS rewrite (plan 03 / migration 108) writes membership predicates on document_chunks + skill_embeddings that reference `org_id`, and Postgres applies migrations in integer order. The org_id column MUST exist before the RLS that references it. This is a plan-time correction forced by the dependency, not a preference. The [BLOCKING] apply (plan 05) pastes 107 THEN 108.
</migration_order_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 107 — org_id denormalize + backfill + NOT-NULL + index + autofill trigger on document_chunks & skill_embeddings</name>
  <files>supabase/migrations/107_ten04_chunk_embedding_org_id.sql</files>
  <read_first>
    - supabase/migrations/104_org_dept_role_schema.sql (the ADD COLUMN IF NOT EXISTS org_id + CREATE INDEX IF NOT EXISTS btree idiom :448-450; the :442-443 comment that DEFERS document_chunks/skill_embeddings to 163=TEN-04)
    - supabase/migrations/105_personal_org_backfill.sql (the _mig105_backfill(p_sql, p_batch DEFAULT 10000) procedure :127-141 with per-batch COMMIT; the parent-FK child-resolves-from-parent backfill shape :330-351; the self-guarded NOT-NULL flip :398-403)
    - supabase/migrations/106_org_id_autofill_trigger.sql (autofill_org_id_from_parent + the forward-compat "IF NEW.org_id IS NOT NULL THEN RETURN NEW" no-op guard; the todos-via-threads attach precedent)
    - supabase/full-schema.sql:759-770 (document_chunks columns — user_id NOT NULL, document_id NOT NULL, NO org_id), :2572 (HNSW), :2579 (GIN); :1458-1466 (skill_embeddings — PK skill_id, user_id NOT NULL, NO org_id), :3013 (idx_skill_embeddings_user_id btree)
    - CLAUDE.md (numbered-migration rules — sequential integers only, letter suffixes silently skipped; re-paste-safe idempotent idioms; apply via SQL editor)
  </read_first>
  <action>
    Author `107_ten04_chunk_embedding_org_id.sql`, re-paste-safe idempotent (mig 104/105 style), in this internal order:
    1. `ALTER TABLE public.document_chunks ADD COLUMN IF NOT EXISTS org_id uuid;` and the same for `public.skill_embeddings`.
    2. Batched backfill (reuse or re-declare a `_mig163_backfill(p_sql, p_batch DEFAULT 10000)` procedure with per-batch COMMIT — document_chunks is the LARGE table, batch ~10k windows): `document_chunks.org_id <- documents.org_id` via `document_id` FK (only WHERE org_id IS NULL AND parent org_id IS NOT NULL); `skill_embeddings.org_id <- skills.org_id` via `skill_id` FK.
    3. Self-guarded NOT-NULL flip for BOTH tables: a `DO $$ ... RAISE EXCEPTION IF EXISTS (... WHERE org_id IS NULL) ... $$` guard, then `ALTER TABLE ... ALTER COLUMN org_id SET NOT NULL` — the DB, not a human, enforces zero-NULL.
    4. Index: `CREATE INDEX IF NOT EXISTS idx_document_chunks_org_id ON public.document_chunks USING btree (org_id)` (benchmark-driven shape — a plain btree(org_id) is the recommended default; the composite `(org_id, user_id)` is the alternative since RLS + the belt-and-suspenders `.eq("user_id")` both filter these; the plan-09 CONCUR-01 benchmark decides if the composite is needed). Same btree(org_id) for skill_embeddings. **LEAVE the HNSW + GIN indexes on document_chunks UNTOUCHED** — no per-tenant partial vector index (unbounded org count).
    5. Attach both tables to the autofill net: a `BEFORE INSERT ... EXECUTE FUNCTION public.autofill_org_id_from_parent('document_id','documents','id')` trigger on document_chunks and the skill_id/skills equivalent on skill_embeddings (mirror the mig-106 todos-via-threads attach). Use `DROP TRIGGER IF EXISTS ... ; CREATE TRIGGER ...` for re-paste safety.
    Add a header comment: this is TEN-04, applies BEFORE 108 (RLS references org_id here); the SECDEF retrieval fns are NOT touched (Phase 164). Do NOT run `scripts/regenerate-full-schema.sh` here — that happens in the [BLOCKING] apply plan (05).
  </action>
  <acceptance_criteria>
    - Filename is `107_ten04_chunk_embedding_org_id.sql` — sequential integer, no letter suffix (letter suffixes are silently skipped by the Supabase CLI)
    - The file contains BOTH `document_chunks` and `skill_embeddings` org_id ADD COLUMN, backfill, NOT-NULL flip, and btree index sections
    - The self-guarded NOT-NULL flip RAISEs on any residual NULL (grep for `RAISE EXCEPTION` near an `org_id IS NULL` guard)
    - The HNSW/GIN indexes on document_chunks are NOT dropped or recreated (grep the file: no `DROP INDEX` on the vector/GIN index; no `hnsw` DDL)
    - Idempotent idioms only (`ADD COLUMN IF NOT EXISTS`, `CREATE INDEX IF NOT EXISTS`, `DROP TRIGGER IF EXISTS` + `CREATE TRIGGER`); no bare `CREATE` that fails on re-paste
    - The migration does NOT contain `CREATE OR REPLACE FUNCTION match_document_chunks` or `keyword_search_chunks` (SECDEF fns are Phase 164)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/test_163_ten04_backfill.py -x --collect-only</automated>
  </verify>
  <done>Migration 107 authored, idempotent, adds org_id + backfill + guarded NOT-NULL + btree + autofill trigger on both pgvector tables, leaves HNSW/GIN untouched, does not touch the SECDEF retrieval functions.</done>
</task>

<task type="auto">
  <name>Task 2: Author test_163_ten04_backfill.py — zero-NULL / NOT-NULL / index-present / HNSW-untouched assertions</name>
  <files>backend/tests/integration/test_163_ten04_backfill.py</files>
  <read_first>
    - backend/tests/integration/test_110_dm_schema.py:294-299 (_table_exists SQL-introspection style)
    - backend/tests/integration/_rls_harness.py (the shared DSN override + skip-guard from plan 01)
    - supabase/full-schema.sql:2572 (HNSW index name) + :2579 (GIN) + :3013 (skill_embeddings user_id btree) — assert these still exist post-migration
  </read_first>
  <action>
    Author `test_163_ten04_backfill.py` (RED until plan 05 applies migration 107) asserting against the live local DB via the shared harness: (a) `document_chunks.org_id` and `skill_embeddings.org_id` columns exist and are `NOT NULL` (query information_schema.columns); (b) zero rows have `org_id IS NULL` in either table; (c) a btree index on `org_id` exists for each table (query pg_indexes); (d) the HNSW index and the GIN(search_vector) index on document_chunks STILL exist (regression guard — the vector index must not be dropped); (e) the mig-106-style autofill trigger exists on both tables (query pg_trigger). Skip-guard if Postgres :54322 unreachable.
  </action>
  <acceptance_criteria>
    - Test asserts org_id NOT NULL + zero-NULL + btree(org_id) present for BOTH tables
    - Test asserts the HNSW + GIN indexes on document_chunks are still present (regression guard)
    - `pytest tests/integration/test_163_ten04_backfill.py --collect-only` succeeds (imports resolve); the test is RED now, goes GREEN in plan 05 after apply
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/test_163_ten04_backfill.py -x --collect-only</automated>
  </verify>
  <done>The TEN-04 backfill test exists, imports cleanly, and encodes the zero-NULL / NOT-NULL / index-present / HNSW-untouched contract that plan 05's apply must satisfy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backfill data migration | Mis-resolved parent FK could assign a wrong org_id to a chunk → cross-org leak once RLS is live |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-BF (bad backfill) | Information Disclosure | document_chunks/skill_embeddings org_id backfill | mitigate | resolve org_id strictly from the parent documents/skills FK (same owner ⇒ same org); self-guarded NOT-NULL flip RAISEs on any residual NULL so no chunk ships org-less |
| T-163-PERF (join cliff) | Denial of Service | document_chunks RLS perf | mitigate | denormalized org_id + btree; HNSW untouched; NO per-row join to documents; plan-09 CONCUR-01 benchmark is the gate |
| T-163-05 (autofill gap) | Tampering | future chunk INSERTs | mitigate | BEFORE-INSERT autofill trigger fills org_id before RLS WITH CHECK evaluates, so new inserts cannot bypass org scoping |
| T-163-SC | Tampering | dependency installs | accept | no new package (schema-only migration) |
</threat_model>

<verification>
- Migration 107 is idempotent (re-paste-safe) and touches only document_chunks + skill_embeddings + their triggers/indexes.
- `test_163_ten04_backfill.py` collects cleanly (RED until plan 05).
- No SECDEF retrieval function is modified in this migration (Phase 164 boundary held).
</verification>

<success_criteria>
Migration 107 lays a NOT-NULL, parent-FK-backfilled, btree-indexed `org_id` on both pgvector hot tables + wires their autofill triggers, with the HNSW/GIN vector indexes untouched — the substrate the RLS rewrite (plan 03) and Phase 164 both need. Its verification test encodes the applied-state contract.
</success_criteria>

<output>
Create `.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-02-SUMMARY.md` when done.
</output>
