---
phase: 163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux
plan: 03
type: execute
wave: 2
depends_on: ["163-01"]
files_modified:
  - supabase/migrations/108_rls_membership_rewrite.sql
  - backend/tests/integration/test_163_rls_documents.py
  - backend/tests/integration/test_163_rls_dm.py
  - backend/tests/integration/test_163_rls_chat.py
  - backend/tests/integration/test_163_rls_skills.py
  - backend/tests/integration/test_163_rls_workflow_eval.py
  - backend/tests/integration/test_163_rls_identity_audit.py
autonomous: true
requirements: [TEN-01]
must_haves:
  truths:
    - "D-06: every RLS predicate on all 37 target user-facing tables is rewritten from auth.uid()=user_id to org_id IN (SELECT public.current_user_org_ids()) AND (owner-within-org OR global-branch OR is_system OR dept-scope), shipped across 6 clearly-sectioned per-cluster bundles in ONE migration file"
    - "D-06: today's global OR-branches are PRESERVED verbatim — folder_is_globally_visible(id) on documents/folders, is_global on document_views/classification_rules/metadata_field_definitions/skills/workflow_definitions, EXISTS-on-skills on skill_files/tuner_runs — using the LIVE column names is_global/is_system (162-D-05; the rename is Phase 165)"
    - "D-12: predicates enforce membership via current_user_org_ids() (reads org_members live off auth.uid()) — NO custom-JWT-membership hook, NO X-Org-Id header (both Phase 166); post-162 every user has exactly one personal org so membership-set isolation == today's per-user behavior"
    - "D-10: genuinely org-agnostic operator/system/audit rows are handled with an explicit org_id IS NULL branch (reached via the operator path, not the membership predicate) — audit_log's nullable rows do not vanish"
    - "D-06 (inert-first): all bundles use the proven live org-table template and are INERT under the current BYPASSRLS connections — applying them changes zero behavior until the Wave-4 client swap flips the connection to role authenticated"
    - "The rewrite uses IN (SELECT public.current_user_org_ids()) — NOT = ANY(...) — because the helper RETURNS SETOF uuid (the live, proven form on the 8 org tables); and DROP POLICY IF EXISTS \"<exact quoted name>\" then CREATE POLICY (Postgres has no CREATE OR REPLACE POLICY)"
    - "Special ownership columns preserved: workflow_definitions uses created_by (not user_id); profiles uses id; todos/workflow_phases/workflow_runs/workspace_files/workspace_file_versions resolve ownership through the parent thread"
  artifacts:
    - path: "supabase/migrations/108_rls_membership_rewrite.sql"
      provides: "6-section membership-RLS rewrite of all 37 target tables"
      contains: "current_user_org_ids"
    - path: "backend/tests/integration/test_163_rls_documents.py"
      provides: "per-cluster SELECT/INSERT/UPDATE/DELETE membership + global-branch-preservation assertions (RED until plan 05 applies)"
  key_links:
    - from: "108 policies"
      to: "public.current_user_org_ids()"
      via: "org_id IN (SELECT ...) membership predicate"
      pattern: "org_id IN \\( SELECT public.current_user_org_ids"
    - from: "documents/folders policies"
      to: "public.folder_is_globally_visible(...)"
      via: "preserved global OR-branch"
      pattern: "folder_is_globally_visible"
---

<objective>
Author migration **108**: the TEN-01 predicate rewrite — transform every RLS policy on the **37 target user-facing tables** from `auth.uid() = user_id` to the membership form `org_id IN (SELECT public.current_user_org_ids()) AND (<owner-within-org> OR <preserved global branch> OR is_system OR <dept-scope>)`, using the proven template already live on the 8 org tables. Shipped as ONE migration file with 6 clearly-sectioned per-cluster bundles (documents / DM / chat / skills / workflow-eval / identity-audit) — the reviewability comes from the sections (letter-suffixed files are silently skipped). The bundles are INERT under the current BYPASSRLS connections, so this migration changes zero behavior until the Wave-4 client swap.

Purpose: this is Front A of the atomic crux. It is authored FIRST and applied while inert; the client swap (Wave 4) is what turns it on. There must be no window where the client is swapped but predicates are incomplete — so ALL 37 tables land in this one migration.

Output: `supabase/migrations/108_rls_membership_rewrite.sql` + 6 `test_163_rls_<cluster>.py` files (RED until plan 05 applies).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-CONTEXT.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-RESEARCH.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-PATTERNS.md

<cluster_map>
<!-- The 37 target tables (live-probed at schema head 106), grouped into the 6 D-06 bundles. -->
<!-- EXCLUDED from the rewrite (do NOT touch): the 8 org tables (161-correct), org_members's locked non-recursive self-rows policy (161-D-10), model_capabilities_overrides/roles/role_permissions (global-read USING(true) catalogs), operator_audit_log/operator_users (deny-all operator path). -->

documents (4 tables): documents, folders, document_views, document_chunks
DM / doc-management (6): document_images, document_tables, document_relationships, classification_rules, metadata_field_definitions, pdf_extraction_runs
chat (10): threads, messages, message_feedback, runs, code_executions, sandbox_files, todos, user_memory, workspace_files, workspace_file_versions
skills (7): skills, skill_files, skill_versions, skill_test_cases, skill_proposals, skill_publish_overrides, skill_embeddings
workflow-eval (8): workflow_definitions, workflow_phases, workflow_runs, eval_runs, eval_results, eval_ratings, tuner_runs, harness_audit
identity-audit (2): profiles, audit_log
</cluster_map>

<special_cases>
- ORDER: this migration (108) applies AFTER 107 (TEN-04) — document_chunks + skill_embeddings org_id columns are added by 107; their membership predicates here reference that column.
- Ownership column is NOT always user_id: workflow_definitions uses `created_by`; profiles uses `id` (= auth.uid()). Use the correct owner column per table.
- Parent-thread-resolved tables (keep the parent resolution, ADD the membership prefix using the now-backfilled org_id): todos, workflow_phases, workflow_runs, workspace_files, workspace_file_versions — these already use terse `TO authenticated` names (todos_select_own, workflow_phases_select_own, workflow_runs_select_own, workspace_files_select_own, workspace_versions_select_own, and the _insert/_update/_delete variants).
- PRESERVE global branches verbatim: folder_is_globally_visible(id) on folders + folder_is_globally_visible(folder_id) on documents; is_global on document_views/classification_rules/metadata_field_definitions/skills/workflow_definitions; EXISTS-on-skills subquery on skill_files + tuner_runs.
- INSERT WITH CHECK that today carries `is_global = false` (classification_rules/document_views/metadata_field_definitions/workflow_definitions): keep that clause, add the membership WITH CHECK.
- NULL org_id (D-10): audit_log rows may be genuinely org-agnostic — its policy handles `org_id IS NULL` explicitly (operator path), so nullable audit rows are not hidden. pdf_extraction_runs has only a SELECT policy today (pdf_extraction_runs_select_own).
- The autofill triggers (mig 106 + plan 02) mean org_id is populated on INSERT — WITH CHECK can safely require `org_id IN (SELECT current_user_org_ids())`.
</special_cases>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 108 sections 1-3 — documents, DM, chat clusters</name>
  <files>supabase/migrations/108_rls_membership_rewrite.sql</files>
  <read_first>
    - supabase/full-schema.sql:4360-4943 (the EXACT quoted policy names + USING/WITH CHECK bodies for every documents/DM/chat table — enumerate the COMPLETE policy set per table; the grep in this plan lists the headline ones but confirm SELECT+INSERT+UPDATE+DELETE per table)
    - supabase/full-schema.sql:4976-5031 (the LIVE org-table template departments_select :4990 + departments_insert :4983 — the exact predicate shape to copy) + :193-217 (current_user_org_ids / current_user_has_permission / folder_is_globally_visible bodies)
    - supabase/migrations/104_org_dept_role_schema.sql (re-paste-safe idempotent DROP/CREATE idioms)
    - .planning/phases/163-.../163-PATTERNS.md § "supabase/migrations/107_*.sql — RLS predicate rewrite" (Shape A / Shape B rewrite rules + the IN (SELECT ...) not = ANY(...) anti-pattern)
  </read_first>
  <action>
    Open a `BEGIN;`-wrapped migration file `108_rls_membership_rewrite.sql`. Author section 1 (documents: documents, folders, document_views, document_chunks), section 2 (DM: document_images, document_tables, document_relationships, classification_rules, metadata_field_definitions, pdf_extraction_runs), section 3 (chat: threads, messages, message_feedback, runs, code_executions, sandbox_files, todos, user_memory, workspace_files, workspace_file_versions). For EACH policy on EACH table: `DROP POLICY IF EXISTS "<exact quoted live name>" ON public.<table>;` then `CREATE POLICY <name> ON public.<table> FOR <cmd> TO authenticated USING (org_id IN (SELECT public.current_user_org_ids()) AND (<owner=auth.uid() branch> [OR <preserved global branch>]))` and the matching `WITH CHECK` for INSERT/UPDATE. Copy the departments template exactly. PRESERVE folder_is_globally_visible on documents/folders, is_global on document_views/classification_rules/metadata_field_definitions. For the parent-thread-resolved chat tables (todos/workspace_files/workspace_file_versions), keep the parent `t.user_id`/`threads.user_id` subquery ownership check AND add the `org_id IN (SELECT current_user_org_ids())` membership prefix (org_id is now backfilled + autofilled). document_chunks predicates reference the org_id added by migration 107. Each policy `TO authenticated`. No `CREATE OR REPLACE POLICY`. End the section group; the file `COMMIT;` is added in Task 2 after section 6.
  </action>
  <acceptance_criteria>
    - Every documents/DM/chat target table has each of its live policies DROP-IF-EXISTS'd by its EXACT quoted name then re-CREATEd with the membership prefix
    - Every rewritten predicate contains `org_id IN ( SELECT public.current_user_org_ids()` (grep count == number of rewritten policies in sections 1-3); zero occurrences of `= ANY(` on current_user_org_ids
    - folder_is_globally_visible preserved on documents + folders; is_global preserved on document_views/classification_rules/metadata_field_definitions (grep confirms each survives)
    - Parent-thread ownership subqueries preserved on todos/workspace_files/workspace_file_versions AND prefixed with the membership clause
    - No `CREATE OR REPLACE POLICY` anywhere (grep == 0)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && grep -c "current_user_org_ids" ../supabase/migrations/108_rls_membership_rewrite.sql</automated>
  </verify>
  <done>Sections 1-3 rewrite all documents/DM/chat table policies to membership form, preserving every global OR-branch and parent-thread ownership, using exact quoted DROP names + the live template.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 108 sections 4-6 — skills, workflow-eval, identity-audit clusters (+ COMMIT)</name>
  <files>supabase/migrations/108_rls_membership_rewrite.sql</files>
  <read_first>
    - supabase/full-schema.sql:4416-4943 + :5248-5599 (exact quoted policy names + bodies for skills/skill_*, eval_*, tuner_runs, harness_audit, workflow_definitions/workflow_phases/workflow_runs, profiles, audit_log, pdf_extraction_runs — enumerate the COMPLETE per-table policy set)
    - supabase/full-schema.sql:4761 (skills is_global SELECT) + :4724 (skill_files EXISTS-on-skills) + :4943 (tuner_runs EXISTS-on-skills) + :4768/:4556/:4682 (workflow_definitions uses created_by + is_global) + :4929/:4710/:4584 (profiles uses id) + :4458 (audit_log insert-only)
    - the departments/organizations template again for the write-gated shape (current_user_has_permission where a permission applies)
  </read_first>
  <action>
    Continue the same `108_rls_membership_rewrite.sql`. Author section 4 (skills: skills, skill_files, skill_versions, skill_test_cases, skill_proposals, skill_publish_overrides, skill_embeddings), section 5 (workflow-eval: workflow_definitions, workflow_phases, workflow_runs, eval_runs, eval_results, eval_ratings, tuner_runs, harness_audit), section 6 (identity-audit: profiles, audit_log). Same DROP-IF-EXISTS-by-exact-name + CREATE-with-membership-prefix discipline. Special cases: skills PRESERVE is_global + is_system in the owner branch; skill_files + tuner_runs PRESERVE the EXISTS-on-skills global subquery; skill_embeddings references the org_id added by migration 107. workflow_definitions uses `created_by` (not user_id) as the owner column and PRESERVES is_global; workflow_phases/workflow_runs keep parent-thread `t.user_id` ownership + add the membership prefix. profiles uses `id = auth.uid()` as the owner branch + `org_id IN (SELECT current_user_org_ids())`. audit_log: add an explicit `org_id IS NULL` branch so genuinely org-agnostic audit rows remain reachable via the operator path (D-10). Close the file with `COMMIT;` and a trailing comment: apply via Supabase SQL editor AFTER 107; then regenerate full-schema (done in plan 05).
  </action>
  <acceptance_criteria>
    - skills is_global + is_system preserved; skill_files + tuner_runs EXISTS-on-skills subquery preserved
    - workflow_definitions predicates use `created_by` (not user_id) as owner + preserve is_global; profiles uses `id`
    - audit_log policy contains an explicit `org_id IS NULL` handling branch (D-10)
    - The file ends with a single `COMMIT;`; total rewritten policies across all 6 sections covers all 37 target tables (grep `CREATE POLICY` count matches the enumerated live policy count for the 37 tables)
    - zero `= ANY(` on current_user_org_ids; zero `CREATE OR REPLACE POLICY`
  </acceptance_criteria>
  <verify>
    <automated>cd backend && grep -c "CREATE POLICY" ../supabase/migrations/108_rls_membership_rewrite.sql</automated>
  </verify>
  <done>Sections 4-6 complete the 37-table rewrite with created_by/id/EXISTS-on-skills/NULL-audit special cases handled; the migration file is BEGIN/COMMIT-wrapped and re-paste-safe.</done>
</task>

<task type="auto">
  <name>Task 3: Author the 6 test_163_rls_&lt;cluster&gt;.py membership + global-branch tests</name>
  <files>backend/tests/integration/test_163_rls_documents.py, backend/tests/integration/test_163_rls_dm.py, backend/tests/integration/test_163_rls_chat.py, backend/tests/integration/test_163_rls_skills.py, backend/tests/integration/test_163_rls_workflow_eval.py, backend/tests/integration/test_163_rls_identity_audit.py</files>
  <read_first>
    - backend/tests/integration/_rls_harness.py (as_user_asyncpg + assert_auth_uid preflight from plan 01)
    - backend/tests/integration/conftest.py (two_orgs_two_users fixture from plan 01)
    - backend/tests/integration/test_110_dm_schema.py (per-table RLS harness shape) + test_111_1_reembed_rls.py (RLS-scoped read+write assertion shape)
  </read_first>
  <action>
    Author one `test_163_rls_<cluster>.py` per bundle (RED until plan 05 applies 108). Each: for a representative table in the cluster, using the two_orgs_two_users fixture + the fail-loud `assert_auth_uid` preflight, assert as user B (in Org Y): (a) SELECT sees 0 of user A's rows; (b) UPDATE/DELETE of A's rows affects 0; (c) a structural assertion that the policy USING clause now references `current_user_org_ids` (query pg_policies.qual — this is the RED→GREEN signal that the rewrite landed vs the old auth.uid()-only policy); (d) for the global-branch tables (folders/documents/document_views/skills/classification_rules/metadata_field_definitions/workflow_definitions/skill_files/tuner_runs) a REGRESSION assertion that a global/system row STILL renders for a non-owner (the OR-branch was preserved). Skip-guard on Postgres :54322.
  </action>
  <acceptance_criteria>
    - Each cluster test asserts cross-org isolation (user B reads 0 of A's rows) for ≥1 representative table
    - Each cluster test has a structural assertion that at least one rewritten policy's `qual` references current_user_org_ids (proves the rewrite, not the old policy)
    - The global-branch clusters include a regression assertion that a global/system row still renders for a non-owner
    - All 6 files `pytest ... --collect-only` cleanly; RED now, GREEN after plan 05 apply
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/test_163_rls_documents.py tests/integration/test_163_rls_dm.py tests/integration/test_163_rls_chat.py tests/integration/test_163_rls_skills.py tests/integration/test_163_rls_workflow_eval.py tests/integration/test_163_rls_identity_audit.py --collect-only</automated>
  </verify>
  <done>Six per-cluster RLS tests exist encoding membership isolation + global-branch preservation + the structural current_user_org_ids proof; RED until apply.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RLS policy predicate | The membership predicate IS the isolation boundary once the client swap flips the role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-02 | Information Disclosure | predicate incompleteness / dropped global branch | mitigate | ALL 37 tables in ONE inert migration (no half-flip window); global OR-branches preserved verbatim; per-cluster SELECT/INSERT/UPDATE/DELETE + regression tests |
| T-163-02b | Information Disclosure | wrong owner column (created_by/id) | mitigate | special-case list + full-schema.sql read enforces the correct owner column per table; structural qual assertion catches a botched rewrite |
| T-163-10 | Information Disclosure | NULL org_id audit/operator rows | mitigate | audit_log gets an explicit `org_id IS NULL` branch (D-10); operator/system catalogs excluded from the rewrite |
| T-163-42P17 | Denial of Service | org_members recursion | mitigate | predicates call the SECDEF current_user_org_ids() helper, never inline an org_members subquery (161 broke the 42P17 recursion) |
| T-163-SC | Tampering | dependency installs | accept | schema-only migration; no new package |
</threat_model>

<verification>
- Migration 108 rewrites exactly the 37 target tables; the 8 org tables + operator/catalog tables are untouched (grep the file for those table names == 0 rewrites).
- `IN (SELECT public.current_user_org_ids())` on every rewritten policy; zero `= ANY(`; zero `CREATE OR REPLACE POLICY`.
- All 6 cluster tests collect cleanly (RED until plan 05).
- 108 is authored to apply AFTER 107 (document_chunks/skill_embeddings org_id dependency).
</verification>

<success_criteria>
All 37 user-facing tables carry membership-based RLS predicates in one inert, re-paste-safe migration; every global OR-branch and special owner column is preserved; NULL-org audit rows handled explicitly; 6 per-cluster tests encode isolation + global-branch preservation, RED until the apply gate.
</success_criteria>

<output>
Create `.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-03-SUMMARY.md` when done.
</output>
