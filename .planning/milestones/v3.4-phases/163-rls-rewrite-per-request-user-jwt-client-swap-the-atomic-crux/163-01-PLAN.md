---
phase: 163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/dependencies.py
  - backend/tests/integration/conftest.py
  - backend/tests/integration/_rls_harness.py
autonomous: true
requirements: [TEN-02]
must_haves:
  truths:
    - "D-01 (verified precondition): the threads.py producer extraction landed as Phase 162.5, proven Deep-Mode byte-identical (verify-work 7/7, threats_open 0) — the send_message seam is CLEAN before any org_id/client-swap work begins. This plan does NOT re-plan the extraction."
    - "D-02: get_user_pg_connection acquires the EXISTING pool, opens a transaction, runs SET LOCAL ROLE authenticated FIRST, then sets BOTH GUC forms (legacy request.jwt.claim.sub + JSON request.jwt.claims) from current_user['id']"
    - "D-03: get_user_supabase builds a NEW per-request client (anon key + Bearer header, shared httpx) and never mutates the get_supabase singleton"
    - "D-04: the SET LOCAL claims come from the already-validated current_user['id'] — local JWKS/PyJWT is an OPTIONAL latency optimization, not a blocker; GoTrue validation stays the fallback"
    - "D-05: get_service_role_supabase(org_id) RAISES when org_id is falsy — it refuses to construct a BYPASSRLS client without an explicit org"
    - "D-11: the three factories are pure env-var (SUPABASE_URL / SUPABASE_ANON_KEY / SUPABASE_SERVICE_ROLE_KEY / POSTGRES_DSN) with no hardcoded URL/key/role — identical behavior in an isolated single-org deploy and a co-tenant deploy"
    - "Two seeded users in two orgs exist as a reusable fixture; an auth.uid() live-variant probe fixture records which GUC form THIS local DB reads"
  artifacts:
    - path: "backend/app/dependencies.py"
      provides: "get_user_pg_connection(request, current_user) async CM + get_user_supabase(request, current_user, token) factory + get_service_role_supabase(org_id) hardened factory"
      contains: "SET LOCAL ROLE authenticated"
    - path: "backend/tests/integration/conftest.py"
      provides: "two-user/two-org fixtures + auth.uid() variant probe fixture"
      contains: "current_user_org_ids"
    - path: "backend/tests/integration/_rls_harness.py"
      provides: "shared SET-LOCAL-as-user asyncpg helper + supabase-py-over-asyncpg-txn adapter reused by every test_163_* file"
  key_links:
    - from: "backend/app/dependencies.py::get_user_pg_connection"
      to: "get_pg_pool() (existing singleton pool, no new pool)"
      via: "pool.acquire() inside conn.transaction() with SET LOCAL"
      pattern: "SET LOCAL ROLE authenticated"
    - from: "backend/app/dependencies.py::get_service_role_supabase"
      to: "raise on missing org_id"
      via: "explicit guard"
      pattern: "org_id"
---

<objective>
Author the three Front-B DB-context factories in `dependencies.py` and the reusable two-user/two-org test scaffold — the load-bearing seam and the contract that every later plan in this phase consumes. The factories are ADDITIVE and dead-until-wired (Wave-4 plans wire them); this plan changes NO app behavior. It also seeds the two-user/two-org fixtures + the `auth.uid()` live-variant probe that the leak tests, cluster tests, and TEN-04 tests all import.

Purpose: establishing the interface first (contracts before consumers) prevents every downstream plan from re-deriving the SET-LOCAL / anon-key-Bearer / refuse-without-org shapes. This is the "hand the contractor the blueprints" step.

Output: `get_user_pg_connection`, `get_user_supabase`, `get_service_role_supabase` in `dependencies.py`; two-user/two-org + `auth.uid()`-probe fixtures in `conftest.py`; the shared `_rls_harness.py` helpers.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-CONTEXT.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-RESEARCH.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-PATTERNS.md

<interfaces>
<!-- Contracts THIS plan creates. Wave-4 swap plans (163-06/07/08) and the leak tests (163-04) consume these directly — no re-derivation. -->

dependencies.py (existing seams to build off — do NOT delete):
  get_supabase() -> Client                     # service-role singleton (:21-25) — STAYS (used by get_service_role_supabase internals + background)
  get_pg_pool() -> asyncpg.Pool                # existing singleton pool (:79-105), DSN role = postgres/BYPASSRLS — REUSE, no new pool
  get_current_user(...) -> {"id","email"}      # (:130-151) validated identity; the "id" is the claims sub

NEW factories this plan adds:
  @asynccontextmanager
  async def get_user_pg_connection(request, current_user: dict) -> AsyncIterator[asyncpg.Connection]
      # acquire existing pool -> conn.transaction() -> SET LOCAL ROLE authenticated
      #   -> set_config('request.jwt.claim.sub', uid, true)  (legacy, local-safe)
      #   -> set_config('request.jwt.claims', {"sub":uid,"role":"authenticated"} json, true)  (cloud)
      #   -> yield conn   (COMMIT auto-reverts every SET LOCAL)
  def get_user_supabase(request, current_user: dict, token: str) -> Client
      # create_client(url, ANON_KEY, ClientOptions(headers={Authorization: Bearer token}, httpx_client=_shared_httpx))
      # NEVER mutate the get_supabase() singleton
  def get_service_role_supabase(org_id: str) -> Client
      # if not org_id: raise ValueError — refuse BYPASSRLS without an explicit org
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the three Front-B DB-context factories in dependencies.py</name>
  <files>backend/app/dependencies.py</files>
  <read_first>
    - backend/app/dependencies.py (the FULL file — the get_supabase singleton :21-25, get_pg_pool :79-105, _init_pg_connection :58-76, get_current_user :130-151; replicate their conventions)
    - backend/tests/integration/test_110_dm_schema.py:302-314 (the LIVE SET-LOCAL prototype `_select_count_as_user` — productionize it verbatim, adding the JSON GUC form)
    - .planning/phases/163-.../163-RESEARCH.md § "Pattern 1" + "Pattern 2" + "Code Examples" (the both-GUC-forms rationale; the non-mutating anon-key+Bearer client)
    - .planning/phases/163-.../163-PATTERNS.md (get_user_pg_connection / get_user_supabase / get_service_role_supabase analog rows + load-bearing invariants)
    - backend/app/config.py (confirm settings.supabase_anon_key exists — default "" per config.py:755; settings.supabase_url; settings.supabase_service_role_key)
  </read_first>
  <action>
    Add three factories to `dependencies.py`, additive (no existing function deleted or its behavior changed):
    (a) `get_user_pg_connection(request, current_user)` — an `@asynccontextmanager`. Acquire the EXISTING `get_pg_pool()` (no new pool). Inside `async with conn.transaction()`: execute the literal `SET LOCAL ROLE authenticated` FIRST (this is the load-bearing line — the pool DSN role is postgres/BYPASSRLS, so claims alone are a silent no-op — Pitfall 1), then set BOTH GUC forms via parameterized `SELECT set_config($1,$2,true)` — legacy `request.jwt.claim.sub` = str(uid) AND JSON `request.jwt.claims` = json.dumps({"sub":uid,"role":"authenticated"}). `is_local=true` (3rd arg) is mandatory on the shared pool (auto-reverts at COMMIT — Pitfall 3). NEVER string-interpolate claims (SQLi — parameterize). `uid = current_user["id"]` (no re-decode; decoupled from D-04 JWKS).
    (b) `get_user_supabase(request, current_user, token)` — build a NEW `create_client(settings.supabase_url, settings.supabase_anon_key, ClientOptions(headers={"Authorization": f"Bearer {token}"}, httpx_client=_shared_httpx))`. Use the ANON key, NOT service_role (service_role bypasses RLS). Do NOT call `.postgrest.auth()` on the shared singleton (Pitfall 2 — cross-request identity bleed). Add a module-level `_shared_httpx` transport to avoid connection fanout. supabase-py calls still block → callers keep `run_in_threadpool` (D-v2.5-01); do not remove it.
    (c) `get_service_role_supabase(org_id)` — mirror get_supabase construction but `if not org_id: raise ValueError("get_service_role_supabase requires an explicit org_id")`. Mirror the refuse-without-scope posture of `require_operator` (:247-264). This is retained ONLY for the four async writers + legitimate cross-tenant ops (D-05).
    Add a short module docstring note that D-04 local JWKS is an OPTIONAL future optimization on get_current_user's GoTrue round-trip — NOT wired here, and NOT a blocker (claims come from current_user["id"]).
  </action>
  <acceptance_criteria>
    - `grep -c "SET LOCAL ROLE authenticated" backend/app/dependencies.py` >= 1 (the load-bearing literal present)
    - Both GUC forms present: `grep` finds both `request.jwt.claim.sub` AND `request.jwt.claims` in dependencies.py
    - get_service_role_supabase raises on falsy org_id: a unit assertion `pytest.raises(ValueError)` when called with `""`/`None`
    - get_user_supabase references `supabase_anon_key` (NOT service_role) and does not call `.postgrest.auth(` on the module singleton (`grep -v` confirms no singleton mutation)
    - `python -c "import app.dependencies"` imports cleanly (no syntax/import error); existing get_supabase/get_pg_pool/get_current_user unchanged (git diff shows only additions)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/test_163_factories.py -x</automated>
  </verify>
  <done>The three factories exist, additive, import-clean; get_service_role_supabase refuses a missing org; the SET-LOCAL block sets role-first + both GUC forms parameterized.</done>
</task>

<task type="auto">
  <name>Task 2: Two-user/two-org fixtures + auth.uid() variant probe + shared RLS harness helpers</name>
  <files>backend/tests/integration/conftest.py, backend/tests/integration/_rls_harness.py, backend/tests/integration/test_163_factories.py</files>
  <read_first>
    - backend/tests/integration/conftest.py (the existing autouse pool-reset fixture, :22 — extend, do not break it)
    - backend/tests/integration/test_119_leak.py:43-75 (the _POSTGRES_TEST_DSN override + _pg_reachable() skip-guard + PG_AVAILABLE/pytestmark pattern — copy verbatim) and its two-user non-vacuity seed (:11-19)
    - backend/tests/integration/test_110_dm_schema.py:294-314 (_table_exists :294-299 + _select_count_as_user SET-LOCAL-as-user :302-314)
    - backend/tests/integration/_reembed_adapter.py (SupabaseTxnAdapter — routes supabase-py surface through an asyncpg txn so real RLS WHERE clauses hit real rows)
    - supabase/full-schema.sql:212-217 (current_user_org_ids body — org_members WHERE user_id = auth.uid()) so fixtures seed org_members rows the helper will read
  </read_first>
  <action>
    Add to `conftest.py`: a `two_orgs_two_users` fixture seeding Org X + Org Y (insert `organizations` + one `org_members` row each, OR reuse the two existing dev users' mig-105 personal orgs) with user A in Org X and user B in Org Y, each seeded ≥1 true-positive row in a representative table set (documents, folders, threads, messages, skills, user_memory) so an "all clear" can never false-green; FK-safe teardown. Add an `auth_uid_variant` probe fixture that runs `SELECT pg_get_functiondef('auth.uid()'::regprocedure)` and records whether THIS local DB's auth.uid() reads the legacy `request.jwt.claim.sub`, the JSON `request.jwt.claims`, or both.
    Create `_rls_harness.py` with the shared helpers every test_163_* imports: (a) `as_user_asyncpg(pool, uid, sql, *args)` — the productionized SET-LOCAL-as-user (role-first + both GUC forms + is_local=true), (b) `as_user_supabase_txn(...)` — the SupabaseTxnAdapter-style bridge for the supabase-py path, (c) `assert_auth_uid(conn, expected_uid)` — the fail-loud preflight that asserts `SELECT auth.uid()` == expected BEFORE any isolation assertion. Move the DSN override + skip-guard here so all test_163_* share it.
    Create `test_163_factories.py` — a minimal smoke test importing the three factories: get_service_role_supabase("") raises ValueError; get_service_role_supabase(<uuid>) returns a client; get_user_pg_connection yields a connection whose `SELECT current_user` == 'authenticated' and whose `SELECT auth.uid()` == the passed uid (proves role-first + claims resolve on THIS DB).
  </action>
  <acceptance_criteria>
    - `two_orgs_two_users` fixture seeds ≥1 row for BOTH users in ≥1 shared table type (non-vacuity) and tears down FK-safe (no residual rows after the test session)
    - `assert_auth_uid` raises/fails loudly when auth.uid() is NULL (so a wrong GUC variant cannot false-pass at "0 rows")
    - `test_163_factories.py` proves get_user_pg_connection yields a connection reporting `current_user = authenticated` AND `auth.uid() = <passed uid>` on the live local DB
    - `pytest tests/integration/test_163_factories.py -x` is GREEN (factories + fixtures wired), skip-guarded if Postgres :54322 is unreachable
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/test_163_factories.py -x</automated>
  </verify>
  <done>Two-user/two-org + auth.uid()-probe fixtures exist; `_rls_harness.py` exports the SET-LOCAL-as-user helper + supabase-txn bridge + fail-loud auth.uid() preflight; the factories smoke test is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FastAPI request → DB | The user JWT crosses here; get_user_pg_connection / get_user_supabase turn it into an RLS-enforced DB identity |
| pooled asyncpg connection → next borrower | A leaked SET (is_local=false) would leak claims across users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-01 | Elevation of Privilege | get_user_pg_connection (role-swap no-op) | mitigate | `SET LOCAL ROLE authenticated` FIRST inside the txn; test_163_factories asserts `current_user = authenticated` + `auth.uid() = uid` (a bare-claims connection would report `postgres`) |
| T-163-03 (GUC false-pass) | Spoofing | claims variant | mitigate | set BOTH GUC forms; `assert_auth_uid` fail-loud preflight; auth.uid() probe fixture records the live variant |
| T-163-IB (identity bleed) | Information Disclosure | get_user_supabase / pooled conn | mitigate | per-request client (no singleton `.postgrest.auth()` mutation); `is_local=true` transaction-scoped SET LOCAL auto-reverts at COMMIT |
| T-163-04 | Information Disclosure | get_service_role_supabase | mitigate | raises on missing org_id — cannot construct a BYPASSRLS client without an explicit org |
| T-163-SC | Tampering | dependency installs | accept | 163 installs NO new package (RESEARCH Package Legitimacy Audit — all libs already in venv); no slopcheck surface |
</threat_model>

<verification>
- `pytest tests/integration/test_163_factories.py -x` GREEN on the live local DB (:54322).
- `git diff backend/app/dependencies.py` shows ONLY additions (existing get_supabase/get_pg_pool/get_current_user byte-unchanged).
- Factories are dead-until-wired: `grep -rn "get_user_pg_connection\|get_user_supabase" backend/app/api backend/app/services` returns 0 call sites yet (wired in Wave 4).
</verification>

<success_criteria>
Three factories exist and are import-clean; get_service_role_supabase refuses a missing org; get_user_pg_connection proves role-first + both-GUC-forms resolve auth.uid() on the live DB; two-user/two-org + auth.uid()-probe fixtures + shared harness are ready for every downstream test_163_* file. No app behavior changed.
</success_criteria>

<output>
Create `.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-01-SUMMARY.md` when done.
</output>
