---
phase: 163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux
plan: 05
type: execute
wave: 3
depends_on: ["163-02", "163-03", "163-04"]
files_modified:
  - supabase/full-schema.sql
autonomous: false
requirements: [TEN-01, TEN-04]
must_haves:
  truths:
    - "Migrations 107 (TEN-04) then 108 (RLS) are applied to the live local DB via the Supabase SQL editor IN THAT ORDER (never db push / db reset — that wipes dev data), then supabase/full-schema.sql is regenerated via scripts/regenerate-full-schema.sh (no --reset)"
    - "The migration files + the regenerated full-schema.sql are committed in the SAME commit (CLAUDE.md same-commit rule)"
    - "Applied state is asserted by LIVE DB query (not merely that the SQL files exist): the 37 target tables' policies now reference current_user_org_ids; document_chunks + skill_embeddings have a NOT-NULL org_id column + a btree(org_id) index"
    - "The mechanism is proven BEFORE any router swap: the automated test_163_* suite (leak_asyncpg, leak_supabase, role_swap, rls_<cluster> x6, ten04_backfill) is GREEN post-apply — RLS isolates at the DB/factory level while the app still runs BYPASSRLS (inert-first, atomic order held)"
    - "D-06 atomic order preserved: predicates are now applied but INERT (the app's service-role / postgres connections still bypass them) — no client is swapped yet"
  artifacts:
    - path: "supabase/full-schema.sql"
      provides: "regenerated single-file bootstrap artifact reflecting the applied 107+108 state"
      contains: "current_user_org_ids"
  key_links:
    - from: "live local DB @ 127.0.0.1:54322"
      to: "pg_policies / pg_indexes / information_schema"
      via: "psycopg2 assertions after apply"
      pattern: "current_user_org_ids"
---

<objective>
The [BLOCKING] apply gate. The operator pastes migration 107 then 108 into the Supabase SQL editor against the live local DB (order matters — TEN-04 substrate before the RLS that references it), regenerates `full-schema.sql` (no reset), and commits both migrations + the regenerated schema in one commit. Then live-DB assertions confirm the applied state, and the full automated `test_163_*` suite runs GREEN — proving the membership predicates + the SET-LOCAL mechanism isolate correctly BEFORE any router swap. This is the "predicates land, inert" checkpoint of the atomic flip.

Purpose: a build/type check passes without the apply (false positive) — the ONLY real gate is the live-DB assertion + the tests going green against the running database. Proving the mechanism now (pre-swap) de-risks the Wave-4 flip.

Output: applied migrations, regenerated `full-schema.sql`, same-commit, live-DB-verified.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-CONTEXT.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Operator applies migrations 107 then 108 via the Supabase SQL editor</name>
  <what-built>
    Migration 107 (TEN-04: org_id on document_chunks/skill_embeddings) and 108 (the 37-table membership-RLS rewrite) are authored and re-paste-safe. Claude CANNOT apply them — this project forbids `supabase db push`/`db reset` (wipes dev data); migrations are applied by pasting into the Supabase SQL editor against the live local DB. Order is load-bearing: 107 before 108 (108's document_chunks/skill_embeddings predicates reference the org_id column 107 adds).
  </what-built>
  <read_first>
    - supabase/migrations/107_ten04_chunk_embedding_org_id.sql
    - supabase/migrations/108_rls_membership_rewrite.sql
    - CLAUDE.md (the migration-apply rule: SQL editor only, never db push/db reset; regenerate-full-schema.sh no --reset; same-commit)
  </read_first>
  <how-to-verify>
    1. Open the Supabase SQL editor for the local DB.
    2. Paste and run the FULL contents of `107_ten04_chunk_embedding_org_id.sql`. Confirm it completes without error (the self-guarded NOT-NULL flip RAISEs if any org_id is still NULL — if it raises, the backfill missed rows; report before continuing).
    3. Paste and run the FULL contents of `108_rls_membership_rewrite.sql`. Confirm it completes without error.
    4. Reply "applied" (or paste any error).
  </how-to-verify>
  <action>
    Operator-only manual step — Claude presents the two files and waits. Claude does NOT run db push/db reset. After the operator replies "applied", Claude proceeds to Task 2 (regenerate + assert).
  </action>
  <acceptance_criteria>
    - Operator confirms both migrations applied cleanly, 107 before 108
    - No NOT-NULL-flip RAISE fired (or, if it did, the backfill gap is reported and fixed before proceeding)
  </acceptance_criteria>
  <verify>
    <automated>MISSING — operator-gated SQL-editor apply; verified by Task 2's live-DB assertions</automated>
  </verify>
  <resume-signal>Type "applied" or paste the SQL error</resume-signal>
  <done>Both migrations are live on the local DB in the correct order.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate full-schema.sql + live-DB assertions + automated test_163_* suite GREEN</name>
  <files>supabase/full-schema.sql</files>
  <read_first>
    - scripts/regenerate-full-schema.sh (the live-DB dump, no --reset by default)
    - backend/tests/integration/test_163_ten04_backfill.py + the 6 test_163_rls_*.py + test_163_leak_asyncpg.py + test_163_leak_supabase.py + test_163_role_swap.py (all authored in plans 02/03/04 — now expected GREEN)
    - .planning/reference_evidence_tools_inventory.md (psycopg2 @ :54322 for live-DB assertions)
  </read_first>
  <action>
    Run `bash scripts/regenerate-full-schema.sh` (no --reset) to rebuild `supabase/full-schema.sql` from the live DB. Then run LIVE-DB assertions via psycopg2 @ 127.0.0.1:54322: (a) count pg_policies where `qual`/`with_check` references `current_user_org_ids` across the 37 target tables — assert it covers all rewritten policies; (b) assert `document_chunks.org_id` and `skill_embeddings.org_id` are `is_nullable = NO` (information_schema.columns); (c) assert a btree index on org_id exists on both (pg_indexes); (d) assert the HNSW + GIN indexes on document_chunks still exist. Then run the full automated `test_163_*` suite and confirm GREEN. Commit migrations 107+108 + the regenerated full-schema.sql in ONE commit (CLAUDE.md same-commit rule). Do NOT swap any client yet — predicates are applied but inert; the flip is Wave 4.
  </action>
  <acceptance_criteria>
    - `supabase/full-schema.sql` regenerated (git diff shows the new policies + org_id columns/indexes) and committed WITH 107+108 in one commit
    - Live-DB: all 37 target tables' rewritten policies reference current_user_org_ids (psycopg2 assertion prints the covered-table count == 37)
    - Live-DB: document_chunks.org_id + skill_embeddings.org_id are NOT NULL with a btree(org_id); HNSW + GIN still present
    - `pytest tests/integration/test_163_*.py -x` is GREEN (leak, role-swap, 6 cluster, ten04 backfill all pass against the applied DB)
    - App still runs BYPASSRLS (no router swapped yet) — `grep -rn "get_user_pg_connection\|get_user_supabase" backend/app/api backend/app/services` still returns 0 call sites
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/test_163_ten04_backfill.py tests/integration/test_163_rls_documents.py tests/integration/test_163_rls_dm.py tests/integration/test_163_rls_chat.py tests/integration/test_163_rls_skills.py tests/integration/test_163_rls_workflow_eval.py tests/integration/test_163_rls_identity_audit.py tests/integration/test_163_leak_asyncpg.py tests/integration/test_163_leak_supabase.py tests/integration/test_163_role_swap.py -x</automated>
  </verify>
  <done>full-schema.sql regenerated + same-commit; live-DB confirms the 37 tables reference current_user_org_ids and the two pgvector tables have NOT-NULL org_id + btree; the full test_163_* suite is GREEN; no client swapped yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored SQL → live DB | The apply is the only point where the predicates become real; a skipped/mis-ordered apply is a silent no-op |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-APPLY | Repudiation | migration apply | mitigate | live-DB assertion (not file-exists) confirms the applied state; same-commit keeps full-schema.sql in sync |
| T-163-ORDER | Tampering | 108-before-107 misorder | mitigate | operator applies 107 then 108; 108's document_chunks predicate would error if org_id were absent — apply-time fail-loud |
| T-163-02 | Information Disclosure | half-flip window | mitigate | predicates applied inert (no client swapped) — the atomic order holds; the test suite proves isolation at the mechanism level before Wave 4 |
| T-163-SC | Tampering | dependency installs | accept | no new package |
</threat_model>

<verification>
- Live-DB (psycopg2 @ :54322): 37 target tables reference current_user_org_ids; document_chunks/skill_embeddings org_id NOT NULL + btree; HNSW/GIN intact.
- `pytest tests/integration/test_163_*.py -x` GREEN post-apply.
- `git show --stat HEAD` shows 107 + 108 + full-schema.sql in one commit.
- No `get_user_pg_connection` / `get_user_supabase` call sites in app code yet (inert-first held).
</verification>

<success_criteria>
Both migrations applied in order via the SQL editor, full-schema.sql regenerated + same-commit, applied state proven by live-DB query, and the whole automated test_163_* suite GREEN — the membership predicates + SET-LOCAL mechanism isolate correctly while the app is still inert. The flip is ready.
</success_criteria>

<output>
Create `.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-05-SUMMARY.md` when done.
</output>
