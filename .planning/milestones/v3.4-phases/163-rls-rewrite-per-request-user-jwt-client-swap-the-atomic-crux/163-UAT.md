---
status: complete
phase: 163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux
source: [163-01-SUMMARY.md, 163-02-SUMMARY.md, 163-03-SUMMARY.md, 163-04-SUMMARY.md, 163-05-SUMMARY.md, 163-06-SUMMARY.md, 163-07-SUMMARY.md, 163-08-SUMMARY.md, 163-09-SUMMARY.md, 163-10-SUMMARY.md, 163-11-SUMMARY.md]
started: 2026-07-19T19:41:58Z
updated: 2026-07-20T00:00:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

[testing complete — 8/8 pass, 0 issues. Test 7 regression closed by 163-11 FIX-A (mig 109); re-verified autonomously 2026-07-20 against real data.]

## Tests

### 1. Cold Start Smoke Test
expected: With migrations 107+108 applied to the local DB, start the backend fresh (uvicorn) and open the app signed in. Server boots with no errors and the first authenticated surface loads YOUR real data. (163-10: fresh uvicorn :8000 boot, 19/20 endpoints live, 0 breaks.)
result: pass
evidence: "Live this session: :8000/health 200, /docs 200, frontend :5173 200; backend serving the flipped (mig 107+108) schema."

### 2. You see only your own data (RLS is now the enforced gate)
expected: Signed in as your user, every surface — Documents, Chat threads, Skills, Folders, Workflows — shows only YOUR rows. Membership-based RLS (mig 108, 37 tables) is the enforced gate on every request now, not app-code filters. Nothing from another tenant is ever visible.
result: pass
evidence: "Live psycopg2 real-data probe @ :54322 replicating the app seam (SET LOCAL ROLE authenticated + both JWT-claim GUCs). Real user A=d8a54002: role-swap-noop detector = as bare postgres sees ALL 38 docs (RLS inert without swap); as A → current_user=authenticated, auth.uid()=A, sees exactly own 36 docs/617 threads/6 skills/7 folders/46 workflows, foreign-non-global=0 on every table; random non-member sub → 0 everywhere; cross-tenant B's docs invisible to A (0)."

### 3. Chat works end-to-end through the swapped hot path
expected: Send a message in a chat thread. It streams a response, the run reaches "completed", and the assistant message persists. Full swapped path: your JWT → user-scoped client → workflow preflight → producer → persist.
result: pass
evidence: "163-10 live go/no-go (this same work session, real GoTrue JWT, user d8a54002): POST /threads→201, POST message→201, run reached completed, persisted assistant message 'RLS-OK' through the swapped send path + workflow_kickoff preflight fix. Backend confirmed still live-serving this session."

### 4. Cross-provider chat (OpenAI / Anthropic / Google / DeepSeek)
expected: The same chat works across providers — each completes a message under the new per-request client, no provider throws a 500 from the swap.
result: pass
evidence: "163-10 live: native-4 (OpenAI gpt-4o, Anthropic claude-haiku-4-5, Google gemini-2.5-flash, DeepSeek deepseek-v4-flash) all completed a chat under the swap; OpenRouter 404 = known external BUG-260714-02, not this phase."

### 5. Deep Mode / agent tools unchanged
expected: The agent still runs its tools (search documents, execute code, etc.) exactly as before — the swap never touched the agent loop or provider path.
result: pass
evidence: "Live this session: git diff 5a0e3cc8..HEAD on agent_loop.py / run_producer.py / provider_gateway/ is EMPTY. Red line held."

### 6. Documents / folders / skills / workflows clusters still load
expected: Browsing Documents, opening folders, viewing Skills, and the Workflows library all load and let you read/create as before — the client swap across ~45 handlers in 8+ routers didn't break any cluster.
result: pass
evidence: "Live probe: all clusters return user A's own rows under the user-JWT/RLS path (36 docs, 7 folders, 6 skills, 46 workflows, 617 threads). 163-10: workflows drafts 29/published 32/starters 3; settings, knowledge-health, evals engine-health all HTTP 200."

### 7. Platform/system content universal; user-shared global stays org-scoped (FIX-A contract)
expected: (Re-scoped to the operator-locked FIX-A decision, resolving the original Test-7 regression.) PLATFORM/system-seeded content — the seeded skill-creator (is_system) and the seeded published starter workflows (server-hard-set is_global) — is visible to ANY authenticated user regardless of org (universal). USER-self-served global content — a folder or skill a user toggled is_global themselves — CORRECTLY stays org-scoped and is invisible cross-org until orgs gain members (Phases 166/167). (FIX B — full pre-163 restore, which would re-broadcast any user's is_global content to every tenant — was REJECTED as contradicting v3.4 org-isolation.)
result: pass
evidence: "Re-verified autonomously 2026-07-20 via live psycopg2 @ :54322 replicating the app seam (SET LOCAL ROLE authenticated + both JWT-claim GUCs), against REAL data — plus the 8/8 test_163_rls_platform_universal suite. Platform half FIXED: the real seeded skill-creator (is_system, org 430bffc6) is now visible to 8/8 real users (7/7 genuine non-co-members see it) — the exact 1-of-8 → all-users flip the regression named. User-shared half CORRECT by design: the real user-toggled global folder 'A-shared' (owner fce5f4b8, org 7a0057f1) is seen by its owner but 0/7 non-co-members — stays org-scoped per FIX A until 166/167. Mechanism: mig 109 lifts the is_system / hard-set-false is_global branch OUTSIDE the org-gate while keeping user-self-served is_global inside it (109:70-73 skills SELECT); documents/folders SELECT policies deliberately untouched."

### 8. Performance not degraded
expected: The app feels as fast as before — the extra per-request "SET LOCAL ROLE authenticated" round-trip the RLS swap adds is negligible.
result: pass
evidence: "Live this session: pytest tests/integration/test_058_concurrency.py (CONCUR-01) → 1 passed, 0.39s < 1.0s gate."

## Summary

total: 8
passed: 8
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Global folders and global/system skills (e.g. the seeded skill-creator) remain visible to non-owner users after the RLS rewrite"
  status: resolved
  resolved_by: "163-11 FIX-A (mig 109) — shipped + secured + re-verified 2026-07-20. Platform/system content (is_system skill-creator + seeded is_global workflows) lifted OUT of the org-gate → universal (real skill-creator 1/8 → 8/8 users, live-confirmed). User-self-served is_global (folders/user skills) intentionally KEPT org-scoped per the operator-locked FIX-A decision (FIX B rejected) → becomes cross-visible when orgs gain members in 166/167. secure-phase re-audit registered T-163-11 (badge-spoof is_system self-set) + T-163-11b (over-widening guard); 28/28 STRIDE closed, threats_open:0."
  reason: "Live probe: mig 108 gates the global/is_system OR-branch behind `org_id IN (current_user_org_ids())`. In the solo-org topology every user is their own org, so global/system content owned in another org is invisible. System skill-creator visible to 1 of 8 users (was all 8 pre-163); global folder 'A-shared' invisible to non-owner. test_163_rls_skills passed only because it used a same-org co-member fixture, not the real cross-org topology."
  severity: major
  test: 7
  operator_decision: "REAL REGRESSION — FIX IT (2026-07-19). FORK RESOLVED (2026-07-20): FIX A — 'Only platform content universal'. Lift ONLY is_system/platform content out of the org-gate (universally visible to any authenticated user); user-self-served is_global (folders/skills owner-toggle at folders.py:196 / skills.py:628 — owner-scoped, NOT operator-gated) STAYS org-scoped, becoming shareable via org membership in 166/167. Rejected FIX B (full pre-163 restore) because self-serve is_global would let any user broadcast content to every tenant — contradicts v3.4 org-isolation. NOTE for planner: on tables where is_global is server-hard-set false for users (classification_rules/document_views/metadata_field_definitions — global rows are system-seeded), those is_global rows are effectively platform content and should also be universal; on tables where users self-serve is_global (folders, skills, workflow_definitions/starters — verify each), keep USER is_global org-scoped but ensure SEEDED is_system/platform rows are universal."
  root_cause: "mig 108 rewrote every user-facing SELECT policy to `org_id IN (SELECT current_user_org_ids()) AND (<owner> OR <global-branch>)`. The AND-gate is correct for OWNED rows (org-isolation) but WRONG for the preserved global/is_system OR-branch: it conflates two distinct concepts the Phase-165 rename will split — is_org_shared (share within an org — org-gating correct) vs is_system_global (platform-universal — MUST NOT be org-gated). Pre-163 the policy was `auth.uid()=user_id OR is_global=true` (no org gate), so is_global/is_system content was universally visible. Post-163 the outer org-gate removes any global/system row whose org_id is not one of the viewer's orgs. With the 162 backfill placing every user (and the seeded system content) in its own solo org, universal content is now owner/org-only. is_system content rides the `is_global=true` branch (skill-creator has both flags true), so fixing the is_global branch's org-gating covers is_system too, PROVIDED the fix distinguishes platform-universal (is_system) from org-shared."
  artifacts:
    - path: "supabase/migrations/108_rls_membership_rewrite.sql"
      issue: "9 SELECT policies AND-gate the global OR-branch behind org membership: documents(:116 folder_is_globally_visible), folders(:135 folder_is_globally_visible), document_views(:153 is_global), classification_rules(:225 is_global), metadata_field_definitions(:243 is_global), skills(:421 is_global), skill_files(:433 EXISTS-on-skills is_global), workflow_definitions(:500 is_global, owner=created_by), tuner_runs(:575 EXISTS-on-skills is_global). These are the exact rows to ALTER."
    - path: "backend/tests/integration/test_163_rls_skills.py"
      issue: "Same-org co-member fixture proved the global branch fires WITHIN an org but never tested the real cross-org / solo-org topology, so the regression passed CI. Fix must add a cross-org (non-co-member) global/system-visibility assertion."
  missing:
    - "A new migration 109 that lifts the platform-universal branch OUT of the org-gate: at minimum `is_system = true` (and system-owned global content) must be visible to any authenticated user regardless of org membership; keep OWNED rows org-gated. DESIGN FORK for the planner to resolve (informed by the 165 is_org_shared/is_system_global split + who may set is_global): (A) minimal/security-safe — only is_system escapes the gate, user is_global stays org-scoped (org-shared, becomes visible once orgs gain members in 166/167); (B) full pre-163 restore — is_global OR is_system escape the gate entirely (matches 'shared visible again' but re-broadcasts any user's is_global content across all orgs — a cross-tenant concern in multi-org deployments)."
    - "Cross-org visibility regression test (non-co-member sees is_system content; asserts the pre-163 universal-visibility contract) so this can never silently regress again."
    - "**WITH-CHECK hardening (NEW — surfaced by the dying planner + confirmed 2026-07-20):** because Fix A makes is_system=true a UNIVERSAL read escape and Phase 163 makes RLS the ENFORCED gate, the migration MUST also forbid a user SETTING is_system=true (else badge-spoof → cross-tenant broadcast). App layer is safe today (SkillCreate/SkillUpdate OMIT is_system — output-only badge, T-137.2-02; is_global hard-set false at skills.py:248), but mig 108's skills INSERT/UPDATE WITH CHECK is only `org_id∈orgs AND auth.uid()=user_id` — it does NOT restrict is_system. Add `AND (is_system = false)` (or `is_system IS NOT TRUE`) to skills INSERT + UPDATE WITH CHECK (and any other table where is_system becomes a universal-read escape), mirroring the existing `is_global = false` WITH CHECKs on document_views(:144)/classification_rules(:216)/metadata_field_definitions(:234). Seeded/system rows are written by migration/service-role (BYPASSRLS), so they are unaffected."
    - "workflow_definitions is_global evidence (planner-confirmed before it died): all 15 is_global=true rows are status='published' from a SINGLE seed created_by; the publish path (db/workflows.py:318) only sets status='published', never is_global=true → is_global there is curated/platform, so universal is correct for that table."
    - "Re-run /gsd:secure-phase 163 after the fix — the fix reopens the just-secured RLS surface (T-163-07b / T-163-08b were marked CLOSED on the same-org test that missed this); add a new threat T-163-11 (badge-spoof is_system self-set) to the register."
  debug_session: "In-session live psycopg2 diagnosis (scratchpad uat163_* probes); root cause deterministic — no separate debug agent required."
