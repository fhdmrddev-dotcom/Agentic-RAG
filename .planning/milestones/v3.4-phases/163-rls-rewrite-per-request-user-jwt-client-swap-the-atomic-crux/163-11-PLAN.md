---
phase: 163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux
plan: 11
type: execute
wave: 6
depends_on: ["163-05", "163-10"]
gap_closure: true
source_gap: "163-UAT.md Test 7 — global/system content org-locked after the RLS rewrite (operator-classified regression; fork resolved = FIX A)"
files_modified:
  - supabase/migrations/109_platform_universal_rls_fix.sql
  - supabase/full-schema.sql
  - backend/tests/integration/test_163_rls_platform_universal.py
autonomous: false
requirements: [TEN-01]
must_haves:
  truths:
    - "FIX A: platform/system-seeded content escapes the org-gate — the migration-108 SELECT policies whose global branch is currently trapped inside `org_id IN (SELECT public.current_user_org_ids()) AND (...)` are corrected so the PLATFORM-universal branch sits OUTSIDE the org-gate: `(<platform branch>) OR (org_id IN (SELECT public.current_user_org_ids()) AND (<owner> OR <user-org-shared branch>))`"
    - "The platform branch is is_system=true on skills (skill-creator; owner seed 00000000-0000-0000-0000-000000000001) and its EXISTS-on-skills(is_system) children skill_files + tuner_runs; and is_global=true on the four hard-set-false-for-users tables workflow_definitions / document_views / classification_rules / metadata_field_definitions (a user CANNOT self-set is_global there — mig 108 WITH CHECK hard-sets is_global=false — so is_global=true there is only ever seed/service-role platform content)"
    - "USER-self-served is_global STAYS org-scoped (operator FIX-A decision): skills.is_global (gated owner toggle skills.py:628) and folders.is_global (owner toggle folders.py:196) remain INSIDE the org-gate; documents' folder_is_globally_visible branch (derives from folders.is_global) is therefore UNCHANGED. These become cross-user-visible again only when orgs gain members (Phases 166/167)"
    - "WITH-CHECK badge-spoof hardening (T-163-11): because Fix A makes is_system=true a UNIVERSAL read escape and Phase 163 makes RLS the ENFORCED gate, skills INSERT + UPDATE WITH CHECK gain `AND (is_system = false)` so an authenticated user cannot self-set is_system=true (badge-spoof → cross-tenant broadcast). Mirrors the existing is_global=false WITH CHECKs on document_views(108:144)/classification_rules(108:216)/metadata_field_definitions(108:234). Seed/system rows are written by migration/service-role (BYPASSRLS) so they are unaffected"
    - "42P17-safe + inert-mechanism preserved: every corrected predicate still calls the SECDEF public.current_user_org_ids() helper (never an inline org_members subquery); DROP POLICY IF EXISTS \"<exact quoted name>\" then CREATE POLICY (no CREATE OR REPLACE POLICY); the READ branch is only WIDENED for platform rows — no owner/INSERT/UPDATE/DELETE predicate is loosened beyond the explicit is_system hardening"
    - "Deep byte-identical red line HELD: this is a DB-migration + test-only change; git diff on agent_loop.py / run_producer.py / provider_gateway is EMPTY; no app handler or provider file is touched (the RLS policy is the single source of truth — no app-code .or_() filter is widened)"
    - "Migration discipline: NEW file 109_platform_universal_rls_fix.sql (108 is NEVER edited); applied to the live local DB by the OPERATOR pasting into the Supabase SQL editor (NEVER db push / db reset, NEVER a Claude-driven psycopg2 DDL apply); then supabase/full-schema.sql is regenerated via scripts/regenerate-full-schema.sh (no --reset), migration + regenerated schema in the SAME commit"
  artifacts:
    - path: "supabase/migrations/109_platform_universal_rls_fix.sql"
      provides: "the 7-policy SELECT correction (skills/skill_files/tuner_runs via is_system; workflow_definitions/document_views/classification_rules/metadata_field_definitions via hard-set-false is_global) + the 2 skills WITH-CHECK is_system hardening rewrites"
      contains: "current_user_org_ids"
    - path: "backend/tests/integration/test_163_rls_platform_universal.py"
      provides: "cross-org (non-co-member) visibility regression asserts: a non-member sees is_system/platform content; still does NOT see another user's org-scoped is_global content (incl. a doc inside a global folder); a user cannot INSERT/UPDATE is_system=true"
  key_links:
    - from: "109 corrected policies"
      to: "public.current_user_org_ids()"
      via: "org-gate retained on the owner + user-org-shared branch"
      pattern: "org_id IN \\( SELECT public.current_user_org_ids"
    - from: "skills SELECT platform branch"
      to: "is_system = true"
      via: "universal escape lifted outside the org-gate"
      pattern: "is_system = true"
---

<objective>
Close the Test-7 regression from `163-UAT.md`: migration 108 org-gated the preserved global/`is_system` OR-branch, so in the solo-org topology the built-in **`skill-creator`** (`is_system=true`) is visible to only 1 of 8 users (was all 8) and the seeded published-starter workflows lost cross-user visibility. Author migration **109** implementing the operator-locked **FIX A** — lift ONLY platform/system-seeded content out of the org-gate; keep user-self-served `is_global` org-scoped — and harden the `is_system` write path so the new universal-read escape cannot be badge-spoofed.

Output: `supabase/migrations/109_platform_universal_rls_fix.sql` + `test_163_rls_platform_universal.py` (RED until Task 2 applies) + regenerated `full-schema.sql`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-UAT.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-05-PLAN.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-05-SUMMARY.md

<live_scope_evidence>
<!-- Live psycopg2 @ 127.0.0.1:54322, 2026-07-20 — the scope sweep that decided WHICH policies to touch. -->
Platform/system content that MUST escape the org-gate (owner = seed 00000000-0000-0000-0000-000000000001):
  - skills: 1 row is_system=true + is_global=true  → skill-creator (universal escape = is_system=true)
  - workflow_definitions: 15 rows is_global=true, all seed-owned published starters (is_global hard-set-false-for-users → platform-only)
User-created is_global that CORRECTLY stays org-scoped under Fix A (owner = REAL user):
  - folders: 1 row is_global=true owned by real user fce5f4b8 (→ documents' folder_is_globally_visible branch also stays org-scoped; NO change)
Hard-set-false-for-users tables with 0 global rows today (correct the policy for future seed content; is_global=true there is only ever platform):
  - document_views: 0 ; classification_rules: 0 ; metadata_field_definitions: 0
skill_files / tuner_runs: 0 rows on global/system skills today; they have NO is_global/is_system column of their own (SELECT reads skills.is_system via EXISTS; skill_files has no UPDATE policy, tuner_runs is SELECT-only) → correct their EXISTS-on-skills branch to key on is_system; NO write-side lock needed there (checker-confirmed).
</live_scope_evidence>

<policies_to_correct>
<!-- Exact migration-108 policy names + current predicates (verified). The new migration DROPs each by its exact quoted name then re-CREATEs it. -->

SELECT — lift the platform branch OUTSIDE the org-gate:
  1. skills — "Users can view own and global skills" (108:419-421)
       current: USING (org_id IN (SELECT public.current_user_org_ids()) AND ((auth.uid() = user_id) OR (is_global = true)))
       corrected: USING ((is_system = true) OR (org_id IN (SELECT public.current_user_org_ids()) AND ((auth.uid() = user_id) OR (is_global = true))))
  2. skill_files — "Users can view files on own or global skills" (108:433-437)
       corrected: universal branch EXISTS(skills WHERE id=skill_files.skill_id AND is_system=true) OUTSIDE the gate; keep the is_global EXISTS inside.
  3. tuner_runs — "Users can view tuner runs on own or global skills" (108:575-579)
       corrected: same EXISTS-on-skills(is_system) universal branch OUTSIDE the gate.
  4. workflow_definitions — "Users can view own and global workflow definitions" (108:499-500) [owner col = created_by]
       corrected: USING ((is_global = true) OR (org_id IN (SELECT public.current_user_org_ids()) AND (auth.uid() = created_by)))
  5. document_views — "Users can view own and global document_views" (108:152-153)
       corrected: USING ((is_global = true) OR (org_id IN (SELECT public.current_user_org_ids()) AND (auth.uid() = user_id)))
  6. classification_rules — "Users can view own and global classification_rules" (108:224-225) — same shape.
  7. metadata_field_definitions — "Users can view own and global metadata_field_definitions" (108:242-243) — same shape.

WITH CHECK — badge-spoof hardening (T-163-11), skills only:
  8. skills — "Users can insert own skills" (108:411-413)
       current: WITH CHECK (org_id IN (SELECT public.current_user_org_ids()) AND (auth.uid() = user_id))
       corrected: WITH CHECK (org_id IN (SELECT public.current_user_org_ids()) AND (auth.uid() = user_id) AND (is_system = false))
  9. skills — "Users can update own skills" (108:415-417) — FOR UPDATE has ONLY a USING clause today (verified); the migration ADDS a WITH CHECK (does not edit one):
       USING (unchanged): (org_id IN (SELECT public.current_user_org_ids()) AND (auth.uid() = user_id))
       ADD WITH CHECK: (org_id IN (SELECT public.current_user_org_ids()) AND (auth.uid() = user_id) AND (is_system = false))

DO NOT TOUCH: documents ("...own and globally-visible documents", folder_is_globally_visible — user-shared, org-scoped) and folders ("...own and globally-visible folders" — user-shared, org-scoped). Their global branch is user-self-served → stays gated by Fix A. Leaving them unchanged is the point.
</policies_to_correct>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 109 + the RED cross-org regression test + structural self-check</name>
  <files>
    - backend/tests/integration/test_163_rls_platform_universal.py
    - supabase/migrations/109_platform_universal_rls_fix.sql
  </files>
  <read_first>
    - supabase/migrations/108_rls_membership_rewrite.sql (the exact policy names + predicates in `<policies_to_correct>`; do NOT edit this file)
    - backend/tests/integration/_rls_harness.py (as_user_asyncpg + assert_auth_uid) + backend/tests/integration/conftest.py (the two_orgs_two_users + auth_uid_variant fixtures)
    - backend/tests/integration/test_163_rls_skills.py (the same-org test that MISSED this — the new test is the cross-org complement)
  </read_first>
  <action>
    **Write the RED regression test** `backend/tests/integration/test_163_rls_platform_universal.py` (reuse `_rls_harness.as_user_asyncpg` + the `two_orgs_two_users` conftest fixtures; skip-guard on Postgres :54322). Assertions (RED until Task 2 applies 109):
      - **Platform-universal READ:** seed an `is_system=true` skill owned by the system seed in org X; a user who is a member ONLY of a DIFFERENT org Y (a genuine non-co-member) SELECTs skills under the user-JWT seam and SEES the is_system skill (pre-109: 0 rows — the regression; post-109: 1).
      - **workflow_definitions platform:** an `is_global=true` seed-owned workflow_definition is visible to a non-co-member user.
      - **org-scoped is_global still isolated:** user A marks their OWN skill is_global=true (org-shared); a non-co-member user B does NOT see it. Also A's is_global FOLDER is not visible to B — AND a document INSIDE that global folder is ALSO not visible to B (the composed folder_is_globally_visible path — the second symptom named in 163-UAT Test 7).
      - **badge-spoof blocked (T-163-11):** as user A (role authenticated, A's claims), an INSERT of a skill with is_system=true is REJECTED by the WITH CHECK, and an UPDATE setting is_system=true on A's own skill is REJECTED; a plain is_system=false insert/update SUCCEEDS (positive control).
      - **non-vacuity control:** the same non-co-member sees their OWN skills (the gate isn't returning everything/nothing).

    **Author** `supabase/migrations/109_platform_universal_rls_fix.sql` implementing the 9 rewrites in `<policies_to_correct>`. Header comment: FIX-A gap closure for the 163-UAT Test-7 regression; corrects mig-108 SELECT policies so platform/is_system content escapes the org-gate while user is_global stays org-scoped; adds is_system=false WITH-CHECK hardening (T-163-11); applies AFTER 108; SECDEF retrieval fns NOT touched (Phase 164); do NOT run regenerate-full-schema.sh here (Task 3 does). Wrap in a single BEGIN/COMMIT so all 9 DROP/CREATEs are atomic. Do NOT edit 108.

    **Structural self-check** (still RED, pre-apply — record the output in the task note):
      - `grep -c "current_user_org_ids" supabase/migrations/109_*.sql` ≥ 7 (every corrected policy still calls the SECDEF helper on its org-gated branch).
      - 0 inline `FROM org_members` in 109 (42P17-safe).
      - `grep -c "CREATE POLICY" == grep -c "DROP POLICY IF EXISTS"` == 9; every DROP name matches a name present in 108 verbatim.
      - No `documents`/`folders` SELECT policy name appears in 109 (the user-shared-stays-gated invariant).
  </action>
  <acceptance_criteria>
    - Both files authored; 108 untouched (`git diff --name-only` does not list 108).
    - Migration is a single atomic BEGIN/COMMIT with 9 DROP+CREATE pairs.
    - Structural self-check passes (≥7 helper refs, 0 inline org_members, 9==9, no documents/folders).
    - Test imports cleanly and is RED-by-design (collect-only succeeds; the assertions fail only because 109 is unapplied — not an import error).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/test_163_rls_platform_universal.py --collect-only</automated>
  </verify>
  <done>Migration 109 + the cross-org regression test are authored and committed RED; the structural self-check confirms the SECDEF helper is retained, the apply is atomic, and documents/folders are untouched.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Operator applies migration 109 via the Supabase SQL editor</name>
  <what-built>
    `109_platform_universal_rls_fix.sql` is authored and re-paste-safe (single BEGIN/COMMIT, all-or-nothing). Claude CANNOT apply it — this project forbids `supabase db push`/`db reset` (wipes dev data) AND a Claude-driven psycopg2 DDL apply is out of scope for this gate; migrations are applied by the OPERATOR pasting into the Supabase SQL editor against the live local DB. It applies AFTER 108 (integer order).
  </what-built>
  <read_first>
    - supabase/migrations/109_platform_universal_rls_fix.sql
    - CLAUDE.md (the migration-apply rule: SQL editor only, never db push/db reset; regenerate-full-schema.sh no --reset; same-commit)
  </read_first>
  <how-to-verify>
    1. Open the Supabase SQL editor for the local DB (127.0.0.1:54322).
    2. Paste and run the FULL contents of `109_platform_universal_rls_fix.sql`. Confirm the transaction completes without error (all 9 DROP/CREATEs commit together).
    3. Reply "applied" (or paste any error).
  </how-to-verify>
  <action>
    Operator-only manual step — Claude presents the file and WAITS. Claude does NOT apply the migration directly (no psycopg2 DDL, no db push/db reset). After the operator replies "applied", Claude proceeds to Task 3 (regenerate + verify + commit).
  </action>
  <acceptance_criteria>
    - Operator confirms migration 109 applied cleanly (single transaction, no error).
  </acceptance_criteria>
  <verify>
    <automated>MISSING — operator-gated SQL-editor apply; verified by Task 3's live-DB cross-org assertions after the operator confirms</automated>
  </verify>
  <resume-signal>Type "applied" or paste the SQL error</resume-signal>
  <done>Migration 109 is live on the local DB (applied after 108).</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate full-schema.sql + live cross-org verification + full suite GREEN + same-commit</name>
  <files>supabase/full-schema.sql</files>
  <read_first>
    - scripts/regenerate-full-schema.sh (the live-DB dump, no --reset by default)
    - backend/tests/integration/test_163_rls_platform_universal.py (now expected GREEN)
    - .planning/reference_evidence_tools_inventory.md (psycopg2 @ :54322 for live-DB assertions)
  </read_first>
  <action>
    Run `bash scripts/regenerate-full-schema.sh` (no --reset) to rebuild `supabase/full-schema.sql` from the applied DB. Then run LIVE-DB assertions via psycopg2 @ 127.0.0.1:54322 replicating the app seam (SET LOCAL ROLE authenticated + both JWT-claim GUC forms):
      - the real cross-org probe that FOUND the bug now passes — a non-co-member user SEES the is_system skill-creator (the 1-of-8 → all-users flip);
      - a user's own is_global skill is STILL invisible to a non-co-member (org-scoped preserved); a document inside a user's global folder is still invisible cross-org;
      - a claims-set INSERT and UPDATE of is_system=true is REJECTED.
    Then run `pytest tests/integration/test_163_rls_platform_universal.py` (GREEN) and the FULL `test_163_*` suite (95 + new, all GREEN — no regression to the isolation/leak/role-swap proofs), and `test_058_concurrency.py` (CONCUR-01 still < 1s — policy-shape change only, no new join). Confirm the Deep red-line diff is still EMPTY (`git diff` on agent_loop.py / run_producer.py / provider_gateway). Commit migration 109 + regenerated full-schema.sql + the test in ONE commit (CLAUDE.md same-commit rule).
    Deviations (partial apply, any 163 test flips RED, CONCUR-01 regresses) → STOP + checkpoint.
  </action>
  <acceptance_criteria>
    - `supabase/full-schema.sql` regenerated (git diff shows the 9 corrected policies) and committed WITH 109 + the test in one commit.
    - Live cross-org probe: non-co-member SEES skill-creator (universal); own is_global skill + global-folder document still isolated; is_system self-set REJECTED (INSERT and UPDATE).
    - `pytest tests/integration/test_163_*.py` GREEN (95 + new); CONCUR-01 < 1s; Deep red-line diff EMPTY.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/test_163_rls_platform_universal.py tests/integration/test_163_rls_skills.py tests/integration/test_163_leak_asyncpg.py tests/integration/test_163_leak_supabase.py tests/integration/test_163_role_swap.py tests/integration/test_058_concurrency.py -x</automated>
  </verify>
  <done>full-schema.sql regenerated + same-commit; live cross-org probe confirms platform content is universal AND user is_global stays isolated AND is_system self-set is rejected; the full test_163_* suite is GREEN and CONCUR-01 is under 1s.</done>
</task>

</tasks>

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|---|---|---|---|---|
| T-163-11 | Elevation of Privilege / Spoofing | badge-spoof: user self-sets is_system=true to broadcast cross-tenant | mitigate | skills INSERT + UPDATE WITH CHECK gain `AND (is_system = false)`; the cross-org test asserts a claims-set is_system=true write RAISES; app layer already omits is_system from SkillCreate/SkillUpdate (T-137.2-02) — RLS is now the enforced backstop |
| T-163-11b | Information Disclosure | over-widening: a user's ORG-scoped is_global leaks cross-org via the fix | mitigate | ONLY is_system (skills/skill_files/tuner_runs) and hard-set-false-for-users is_global (workflow_definitions/document_views/classification_rules/metadata_field_definitions) escape the gate; skills.is_global + folders.is_global (user-self-served) stay INSIDE the org-gate; test asserts a non-co-member does NOT see another user's is_global skill/folder/global-folder-document |
| T-163-02 | Information Disclosure | dropped/half-corrected policy during the rewrite | mitigate | single BEGIN/COMMIT atomic apply; 9 DROP == 9 CREATE structural assert; full test_163_* isolation suite must stay GREEN post-apply (no owner/leak regression) |
| T-163-42P17 | Denial of Service | org_members recursion reintroduced | mitigate | corrected predicates keep calling the SECDEF current_user_org_ids() helper; 0 inline org_members subqueries (grep-asserted) |
| T-163-APPLY | Repudiation | file-exists ≠ applied | mitigate | live-DB psycopg2 verification (the real cross-org probe flips 1-of-8 → universal), not file presence; full-schema.sql regenerated in the same commit |
| T-163-09 | Tampering | shared-path fork breaks Deep | mitigate | DB-migration + test-only; agent_loop.py / run_producer.py / provider_gateway byte-unchanged; CONCUR-01 re-checked < 1s |
| T-163-SC | Tampering | dependency installs | accept | no new package (schema + test only) |

## Verification

- [ ] `109_platform_universal_rls_fix.sql` authored (108 unedited); 9 policies rewritten per `<policies_to_correct>`; single atomic BEGIN/COMMIT.
- [ ] `test_163_rls_platform_universal.py` asserts: platform-universal read (non-co-member sees is_system), org-scoped is_global still isolated (skill + folder + global-folder document), badge-spoof write rejected (INSERT + UPDATE), non-vacuity control.
- [ ] Structural: ≥7 current_user_org_ids refs, 0 inline org_members, 9 DROP == 9 CREATE, no documents/folders policy present.
- [ ] [BLOCKING checkpoint] operator applies 109 via the SQL editor (non-destructive); full-schema.sql regenerated (no --reset); same commit.
- [ ] Live cross-org probe: non-co-member SEES skill-creator (1-of-8 → universal); own is_global still isolated; is_system self-set REJECTED.
- [ ] `test_163_*` full suite GREEN (95 + new); CONCUR-01 < 1s; Deep red-line diff EMPTY.
- [ ] Re-run `/gsd:secure-phase 163` flagged (adds T-163-11; re-verifies T-163-07b/08b under the corrected policies).
