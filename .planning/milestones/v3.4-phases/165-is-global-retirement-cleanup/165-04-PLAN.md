---
phase: 165-is-global-retirement-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/agent_loop.py
  - backend/app/services/openai_service.py
  - backend/app/services/document_view_service.py
  - backend/app/services/document_relationship_service.py
  - backend/app/services/classification_matcher.py
  - backend/app/services/classification_rule_service.py
  - backend/app/services/metadata_field_service.py
  - backend/app/services/embedding_service.py
  - backend/app/services/workflow_authoring.py
  - backend/app/services/workflow_kickoff.py
  - backend/app/services/skill_tuner_service.py
  - backend/app/services/publish_gate_service.py
  - backend/app/services/harness/skill_snapshot.py
  - backend/app/db/workflows.py
  - backend/app/main.py
autonomous: true
requirements: [MIG-02]

must_haves:
  truths:
    - "D-165-01: each service/db/main is_global is renamed to the target VERIFIED against its actual supabase.table()/raw-SQL context (per the explicit per-file mapping) — NOT inventory-assumed"
    - "D-165-03: the 15 seeded workflows are NOT seeded by app code — they live in already-applied migrations 061/066/094 and their is_global=true values are auto-preserved by Plan 01's RENAME COLUMN; main.py has ONE classification_rules comment reference -> is_system_global (no seed task exists)"
    - "D-165-02: skills.is_system references in the snapshot/skill-catalog path are NOT renamed"
    - "D-14: agent_loop.py + openai_service.py diffs are TOKEN-ONLY (no structural change to the shared Deep-mode path)"
    - "Wrong-rename guard: each file's post-rename token(s) match the table(s) it actually queries"
  artifacts:
    - path: "backend/app/db/workflows.py"
      provides: "workflow_definitions raw-SQL using is_system_global"
      contains: "is_system_global"
    - path: "backend/app/services/classification_matcher.py"
      provides: "_resolve_folder_name folders query using is_org_shared"
      contains: "is_org_shared"
  key_links:
    - from: "backend/app/services/embedding_service.py"
      to: "metadata_field_definitions.is_system_global (mig 111)"
      via: "read_enabled_field_defs .or_(...is_system_global.eq.true) ingest scoping"
      pattern: "is_system_global"
---

<objective>
Rename `is_global` -> its D-165-01 split target across the backend SERVICE, DB, and app layers, with each file's target VERIFIED against the actual `supabase.table("…")` / raw-SQL it queries (the checker found 4 inventory-based misclassifications here — this plan fixes them and applies the per-occurrence discipline).

Purpose: MIG-02 / SC#1 / SC#3 — complete the backend rename so no runtime path reads the old column, and no path renames a token to the WRONG table's column (which would silently drift visibility or break a query post-migration).

Output: The service/db/app layer using the correct per-table split target; token-only agent-loop diff; `skills.is_system` preserved.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-is-global-retirement-cleanup/165-CONTEXT.md

<explicit_per_file_mapping>
Target VERIFIED against live source (the `supabase.table("X")` / raw SQL / model at each occurrence). is_system (skills) is NEVER renamed. The 4 checker-corrected files are marked [CORRECTED].

is_system_global (the four write-locked platform tables):
| File | verified table + occurrence |
|---|---|
| document_view_service.py | .table(document_views) — L13/14/16/17/76/86/101/112/131/153 |
| classification_rule_service.py | .table(classification_rules) — L16/17/20/68/69/79/95/118 |
| metadata_field_service.py | .table(metadata_field_definitions) — L8/43/62/67/68/77 |
| workflow_kickoff.py | .table("workflow_definitions") — L211 select, L213 .or_ |
| db/workflows.py | raw SQL FROM/INTO workflow_definitions — L180/202/206/213/224/227/241/248/258/277/278/293/336/350/506/509/565/569/571/583 |
| embedding_service.py [CORRECTED: was skills] | .table("metadata_field_definitions") — read_enabled_field_defs L276/277/283/284/295 |
| main.py [FOLDED — no seed task] | classification_rules.router include COMMENT — L677 only (15 workflows are seeded in migs 061/066/094, NOT here) |

is_org_shared (folders/skills):
| File | verified table + occurrence |
|---|---|
| skill_tuner_service.py | .table("skills") — L26/370/482/491 |
| publish_gate_service.py | .table("skills") — L76 |
| harness/skill_snapshot.py | .table("skills") — L25/64/72 |
| agent_loop.py | .table("skills") — L1273 query, L1311 comment (D-14 token-only) |
| openai_service.py | text-to-SQL PROMPT string describing the folders table schema — L78 "user_id (uuid), is_global (boolean)" (D-14 token-only) |
| classification_matcher.py [CORRECTED: was platform] | .table("folders") — _resolve_folder_name L270 .or_(...is_global.eq.true) |
| workflow_authoring.py [CORRECTED: was platform] | .table("skills") — _skill_registry L143/154/155/169/170/180 .or_(...is_global.eq.true) |

COMMENT-ONLY NO-OP (no column rename — table has no such column):
| document_relationship_service.py | module docstring L9 states document_relationships RLS is user-scoped "no is_global" — no query/column; update wording to the retired term for consistency OR leave (developer discretion), NOT a column rename |
</explicit_per_file_mapping>

<dev_server_note>
W1 (dev-server / this project's convention): this plan commits code that reads the RENAMED columns BEFORE the Wave-2 migration apply (plan 165-10). A running backend (`uvicorn --reload`) or frontend (`vite`) dev server would 500 in the window between this commit and the apply. Operator must STOP any running backend/frontend dev server before Wave 1 begins and restart only AFTER plan 165-10 applies the migration + regenerates full-schema.
</dev_server_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the platform-table files -> is_system_global (incl. embedding_service + main.py)</name>
  <files>backend/app/services/document_view_service.py, backend/app/services/document_relationship_service.py, backend/app/services/classification_rule_service.py, backend/app/services/metadata_field_service.py, backend/app/services/embedding_service.py, backend/app/services/workflow_kickoff.py, backend/app/db/workflows.py, backend/app/main.py</files>
  <read_first>
    - Each file's is_global occurrences WITH the nearest `.table("…")` / SQL context (grep -n -B4 each first)
    - The explicit_per_file_mapping is_system_global table above — ESPECIALLY embedding_service.py (metadata_field_definitions, NOT skills) and main.py (classification_rules comment, NO seed code)
    - supabase/migrations/108_rls_membership_rewrite.sql lines 137-243, 484-500 (the is_global=false write-lock these services honor)
  </read_first>
  <action>
    Rename is_global -> is_system_global across all eight files (each verified to query one of the four write-locked platform tables). Update field access, .select("…") field strings, .or_/.eq/.order filter literals, response dict keys, and any server hard-set of the flag false (is_global=false -> is_system_global=false, keep it server-owned).
    - embedding_service.py: rename the `read_enabled_field_defs` metadata_field_definitions query (.select "…is_global…", .or_ "…is_global.eq.true", the Python fail-closed `r.get("is_global")`, the signature/docstrings) -> is_system_global. (Was mislabeled skills; renaming it is_org_shared would break metadata-field extraction during ingest.)
    - main.py: rename the SINGLE classification_rules.router include comment (~L677 "is_global hard-false") -> is_system_global. There is NO workflow-seeding code in main.py — do NOT add any seed task; the 15 workflows are seeded in migrations 061/066/094 and auto-preserved by Plan 01's RENAME COLUMN.
    - document_relationship_service.py: comment-only (L9 says the table has no such column) — update the wording to the retired term OR leave; no column rename.
  </action>
  <verify>
    <automated>cd backend && python -c "import app.services.document_view_service, app.services.document_relationship_service, app.services.classification_rule_service, app.services.metadata_field_service, app.services.embedding_service, app.services.workflow_kickoff, app.db.workflows, app.main; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -v '^#' across the 8 files returns 0 bare is_global (except the document_relationship_service.py comment if left as a conceptual reference — documented).
    - Wrong-rename guard: is_system_global present in each renamed file; `grep -c is_org_shared` == 0 in all 8 (none of these query folders/skills).
    - embedding_service.py: `grep -c "is_system_global" backend/app/services/embedding_service.py` >= 3 (select + or_ + python filter), 0 is_org_shared.
    - main.py: exactly the classification_rules comment renamed; no seed code added (`git diff --stat backend/app/main.py` shows a ~1-line comment change, not a new seed block).
    - Any preserved write-lock hard-set now reads is_system_global=false.
    - All 8 modules import.
  </acceptance_criteria>
  <done>The platform-table files (incl. embedding_service + main.py) use is_system_global; embedding_service metadata scoping intact; no seed task; write-locks preserved; imports clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rename the skills/folders files -> is_org_shared (incl. classification_matcher + workflow_authoring; D-14 token-only)</name>
  <files>backend/app/services/skill_tuner_service.py, backend/app/services/publish_gate_service.py, backend/app/services/harness/skill_snapshot.py, backend/app/services/classification_matcher.py, backend/app/services/workflow_authoring.py, backend/app/services/agent_loop.py, backend/app/services/openai_service.py</files>
  <read_first>
    - Each file's is_global + is_system occurrences WITH the nearest `.table("…")` context (grep -n -B4 each first)
    - The explicit_per_file_mapping is_org_shared table above — ESPECIALLY classification_matcher.py (.table("folders"), _resolve_folder_name) and workflow_authoring.py (.table("skills"), _skill_registry)
    - backend/app/models/skill.py (plan 03 — the renamed is_org_shared field + preserved is_system)
    - CLAUDE.md D-14 (Deep Mode byte-identical; provider differences only at the gateway/adapter boundary)
  </read_first>
  <action>
    Rename is_global -> is_org_shared across all seven files (each verified to query folders or skills). KEEP is_system verbatim.
    - classification_matcher.py: rename the `_resolve_folder_name` folders query (~L270 `.or_(user_id.eq…,is_global.eq.true)`) -> is_org_shared. (Was mislabeled platform; renaming it is_system_global would break _resolve_folder_name.)
    - workflow_authoring.py: rename the `_skill_registry` skills query (~L143/154/155/169/170/180 select + .or_ + python filter + docstring) -> is_org_shared. (Was mislabeled platform; renaming it is_system_global would break the workflow-authoring skill-catalog lookup.)
    - openai_service.py: rename the text-to-SQL prompt string describing the folders schema (~L78 "user_id (uuid), is_global (boolean)") -> is_org_shared so the LLM is told the correct post-migration column name. TOKEN-ONLY (D-14).
    - agent_loop.py: rename the skills catalog query (~L1273) + the match_skills comment (~L1311, is_global -> is_org_shared, keep is_system). TOKEN-ONLY (D-14).
    Make NO structural edit to agent_loop.py / openai_service.py beyond the token substitution.
  </action>
  <verify>
    <automated>cd backend && python -c "import app.services.skill_tuner_service, app.services.publish_gate_service, app.services.harness.skill_snapshot, app.services.classification_matcher, app.services.workflow_authoring, app.services.agent_loop, app.services.openai_service; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -v '^#' across the 7 files returns 0 bare is_global (is_system preserved).
    - Wrong-rename guard: is_org_shared present in each; `grep -c is_system_global` == 0 in all 7 (none query the platform tables).
    - classification_matcher.py + workflow_authoring.py carry is_org_shared (the corrected target): `grep -c is_org_shared` >= 1 and `grep -c is_system_global` == 0 in each.
    - D-14 token-only (agent_loop.py + openai_service.py): per file, `git diff --numstat -- backend/app/services/agent_loop.py backend/app/services/openai_service.py` shows added == removed == (HEAD count of lines containing is_global) — no net insert/delete. Record the numbers.
    - is_system usages unchanged.
    - All 7 modules import.
  </acceptance_criteria>
  <done>The skills/folders files (incl. the corrected classification_matcher + workflow_authoring) use is_org_shared; agent_loop.py + openai_service.py are provably token-only; is_system preserved; imports clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| per-occurrence table context -> target name | Each is_global must map to the column of the table THAT occurrence queries; a wrong target silently drifts visibility or breaks the query post-migration |
| shared Deep-mode agent path | Provider-uniform path must stay byte-identical except the mechanical token (D-14) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-14 | Info-disclosure / DoS | wrong-table rename (classification_matcher/workflow_authoring/embedding_service) breaks a query or drifts visibility yet passes a bare-is_global grep | mitigate | Per-occurrence `supabase.table()` verification + the wrong-rename guard (each file carries ONLY its table's target; `grep is_org_shared` XOR `grep is_system_global`); plan-11 suite is the runtime arbiter. |
| T-165-15 | Tampering | agent_loop.py shared path restructured during rename | mitigate | Task 2 D-14 check requires added==removed==is_global-line-count. |
| T-165-16 | Tampering | skills.is_system misrenamed in snapshot/skill-catalog | mitigate | is_system excluded (D-165-02); grep confirms is_system count unchanged. |
| T-165-34 | Info-disclosure | a phantom "main.py seed" task org-scopes the 15 workflows | mitigate | No seed task exists — main.py has one classification_rules comment; the 15 workflows are migration-seeded and auto-preserved by RENAME COLUMN (Plan 01). |
| T-165-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed. |
</threat_model>

<verification>
- Zero bare `is_global` (non-comment) across the 15 service/db/main files.
- Wrong-rename guard: each file carries ONLY its verified target (is_org_shared XOR is_system_global); embedding_service->is_system_global, classification_matcher/workflow_authoring->is_org_shared.
- main.py: single classification_rules comment renamed, no seed code.
- agent_loop.py + openai_service.py: token-only diff.
- All modules import (static; assertion-level green is plan 11 post-migration).
</verification>

<success_criteria>
The backend service/db/app layer renames `is_global` to the target verified against each occurrence's actual table (the 4 checker-corrected files fixed), keeps `main.py` a comment-only classification_rules rename with no seed task, preserves `skills.is_system`, keeps `agent_loop.py`/`openai_service.py` token-only, passes the wrong-rename guard, and imports clean.
</success_criteria>

<output>
Create `.planning/phases/165-is-global-retirement-cleanup/165-04-SUMMARY.md` when done.
</output>
