---
phase: 165-is-global-retirement-cleanup
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/integration/test_163_rls_documents.py
  - backend/tests/integration/test_163_rls_platform_universal.py
  - backend/tests/integration/test_163_rls_skills.py
  - backend/tests/integration/test_163_rls_workflow_eval.py
  - backend/tests/integration/test_163_rls_dm.py
  - backend/tests/test_seed091_owner_nulling.py
  - backend/tests/integration/test_113_view_global_leak.py
  - backend/tests/integration/test_114_count_only.py
  - backend/tests/integration/test_115_tool_global_leak.py
  - backend/tests/integration/test_116_tool_leak.py
  - backend/tests/integration/test_117_route_leak.py
  - backend/tests/integration/test_119_leak.py
  - backend/tests/integration/test_119_broken.py
  - backend/tests/integration/test_seed_pm_pack.py
  - backend/tests/test_099_skill_composition.py
autonomous: true
requirements: [MIG-02]

must_haves:
  truths:
    - "D-165-01: each RLS/isolation test's is_global literal is renamed to its OWNING TABLE's split target per the explicit per-file mapping below — a folders/skills literal to is_org_shared, a workflow/view/rule/metadata literal to is_system_global (never the wrong target)"
    - "D-165-06: folder_is_globally_visible references in these tests are renamed to folder_is_org_shared"
    - "D-165-02: skills.is_system literals (platform-universal guard, badge-spoof) are NOT renamed"
    - "These mixed-table RLS tests are the mig-109 Test-7 regression-class guard — a wrong-but-present rename would still pass a bare-is_global grep, so the explicit per-file target mapping is load-bearing"
  artifacts:
    - path: "backend/tests/integration/test_163_rls_platform_universal.py"
      provides: "platform-universal + over-widening RLS guard using the split targets + folder_is_org_shared"
      contains: "is_system_global"
  key_links:
    - from: "backend/tests/integration/test_163_rls_*.py"
      to: "supabase renamed columns (mig 111)"
      via: "raw-SQL INSERT/SELECT literals match the migration-111 per-table column names"
      pattern: "is_org_shared|is_system_global"
---

<objective>
Rename `is_global` in the RLS / cross-org-isolation / leak / owner-nulling test files — the raw-SQL, MULTI-TABLE, security-sensitive subset where a mis-targeted rename would pass a "no is_global remaining" grep yet silently mis-seed the wrong column (the exact mig-109 Test-7 regression class this phase exists to prevent). Each file gets an EXPLICIT per-table target below; no "grep and infer".

Purpose: MIG-02 rename-regression coverage for the highest-risk test files, keeping every raw-SQL literal aligned to the migration-111 per-table column names.

Output: The RLS/isolation test set referencing the correct split target per table + `folder_is_org_shared`; `--collect-only` clean. (EXCLUDES the exit gate `test_v3_4_org_isolation.py` — plan 11.)
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-is-global-retirement-cleanup/165-CONTEXT.md

<explicit_per_file_mapping>
Apply EXACTLY this target per file (read each is_global line's surrounding SQL/table to confirm, then rename). is_system (skills) is NEVER renamed. folder_is_globally_visible -> folder_is_org_shared everywhere it appears.

| File | folders/skills literal -> | workflow/view/rule/metadata literal -> | notes |
|---|---|---|---|
| test_163_rls_documents.py | folders->is_org_shared | document_views->is_system_global | + folder_is_globally_visible->folder_is_org_shared |
| test_163_rls_platform_universal.py | folders/skills->is_org_shared | workflow_definitions->is_system_global | + folder_is_globally_visible->folder_is_org_shared; KEEP is_system (the universal-escape guard) |
| test_163_rls_skills.py | skills + skill_files/tuner_runs EXISTS-on-skills->is_org_shared | (none) | KEEP is_system |
| test_163_rls_workflow_eval.py | tuner_runs EXISTS-on-skills.is_global->is_org_shared | workflow_definitions->is_system_global | |
| test_163_rls_dm.py | (none) | classification_rules + metadata_field_definitions + document_views->is_system_global | |
| test_seed091_owner_nulling.py | folders/skills->is_org_shared | document_views->is_system_global | KEEP is_system; broadened owner-null in plan 02 |
| test_113_view_global_leak.py | any folders/skills->is_org_shared | document_views->is_system_global | |
| test_114_count_only.py | folders->is_org_shared | document_views->is_system_global | |
| test_115_tool_global_leak.py | folders/skills->is_org_shared | document_views->is_system_global | |
| test_116_tool_leak.py | folders->is_org_shared | (none) | |
| test_117_route_leak.py | folders->is_org_shared | (none) | |
| test_119_leak.py | folders->is_org_shared | (none) | |
| test_119_broken.py | folders->is_org_shared | (none) | |
| test_seed_pm_pack.py | folders->is_org_shared | workflow_definitions->is_system_global | |
| test_099_skill_composition.py | skills->is_org_shared | workflow_definitions + metadata->is_system_global | |

EXCLUDE test_v3_4_org_isolation.py (plan 11). test_163_factories.py has NO is_global (no edit).
</explicit_per_file_mapping>

<dev_server_note>
W1 (dev-server / this project's convention): test files here reference the RENAMED columns; they run green only AFTER the Wave-2 migration apply (plan 165-10). Operator must STOP any running backend/frontend dev server before Wave 1 begins and restart only AFTER plan 165-10 applies the migration + regenerates full-schema. The assertion-level green run for this plan is plan 11 (post-apply); this plan's gate is grep + `--collect-only`.
</dev_server_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the test_163_rls_*.py cluster + platform-universal guard (5 files)</name>
  <files>backend/tests/integration/test_163_rls_documents.py, backend/tests/integration/test_163_rls_platform_universal.py, backend/tests/integration/test_163_rls_skills.py, backend/tests/integration/test_163_rls_workflow_eval.py, backend/tests/integration/test_163_rls_dm.py</files>
  <read_first>
    - Each file's is_global + folder_is_globally_visible + is_system lines (grep -n each first)
    - The explicit_per_file_mapping table above (authoritative target per file)
    - supabase/migrations/108_rls_membership_rewrite.sql + 109_platform_universal_rls_fix.sql (the policy shapes these tests assert — confirm which table each raw-SQL literal seeds)
  </read_first>
  <action>
    Apply the explicit_per_file_mapping per file. For each is_global occurrence, confirm the owning table from the surrounding SQL (the INSERT/SELECT target table), then rename to that table's split target. Rename folder_is_globally_visible -> folder_is_org_shared (call sites + pg_proc proname assertions + docstrings). KEEP every is_system literal (the platform-universal-escape + badge-spoof guards). Token rename only — no assertion value/topology change.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/test_163_rls_documents.py tests/integration/test_163_rls_platform_universal.py tests/integration/test_163_rls_skills.py tests/integration/test_163_rls_workflow_eval.py tests/integration/test_163_rls_dm.py --collect-only -q 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 5 files: `grep -c "is_global" <file>` == 0 and `grep -c "folder_is_globally_visible" <file>` == 0.
    - is_system counts unchanged vs HEAD in test_163_rls_platform_universal.py + test_163_rls_skills.py.
    - Spot-check per the mapping: the workflow_definitions seeds in test_163_rls_workflow_eval.py + platform_universal use is_system_global; the folders seeds use is_org_shared (no cross-target).
    - `--collect-only` exits 0.
  </acceptance_criteria>
  <done>The 5 RLS-cluster files use the correct split target per table + folder_is_org_shared; is_system preserved; collection clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rename the leak / owner-nulling / mixed-seed tests (10 files)</name>
  <files>backend/tests/test_seed091_owner_nulling.py, backend/tests/integration/test_113_view_global_leak.py, backend/tests/integration/test_114_count_only.py, backend/tests/integration/test_115_tool_global_leak.py, backend/tests/integration/test_116_tool_leak.py, backend/tests/integration/test_117_route_leak.py, backend/tests/integration/test_119_leak.py, backend/tests/integration/test_119_broken.py, backend/tests/integration/test_seed_pm_pack.py, backend/tests/test_099_skill_composition.py</files>
  <read_first>
    - Each file's is_global + is_system lines (grep -n each first)
    - The explicit_per_file_mapping table above (authoritative target per file)
    - backend/app/utils/folder_utils.py (plan 02 — test_seed091_owner_nulling.py asserts the owner-nulling this broadens; confirm it references is_org_shared post-rename)
  </read_first>
  <action>
    Apply the explicit_per_file_mapping per file. Confirm each is_global occurrence's owning table from the surrounding SQL/model before renaming. The folders/skills literals -> is_org_shared; the document_views/classification_rules/metadata/workflow_definitions literals -> is_system_global. KEEP is_system. Token rename only.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_seed091_owner_nulling.py tests/integration/test_113_view_global_leak.py tests/integration/test_115_tool_global_leak.py tests/test_099_skill_composition.py --collect-only -q 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 10 files: `grep -c "is_global" <file>` == 0; is_system preserved where present.
    - Mixed files (test_seed091_owner_nulling.py, test_115_tool_global_leak.py, test_seed_pm_pack.py, test_099_skill_composition.py) apply BOTH targets correctly per the mapping (spot-check the folders vs view/workflow seeds).
    - `--collect-only` exits 0 for the collected subset.
    - Final: `grep -rln "is_global\\|folder_is_globally_visible" backend/tests/integration backend/tests/test_seed091_owner_nulling.py backend/tests/test_099_skill_composition.py` shows none of THIS plan's 15 files remain (only files owned by plans 06/07 + the exit gate).
  </acceptance_criteria>
  <done>The leak/owner-nulling/mixed-seed tests use the correct split target per table; is_system preserved; collection clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test raw-SQL literal -> DB column | A mis-targeted rename mis-seeds the wrong column and silently weakens an isolation assertion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-17 | Tampering | mis-targeted rename (is_org_shared vs is_system_global) in an RLS regression test | mitigate | Explicit per-file target mapping (not inference) + per-occurrence owning-table read; the plan-11 green run of test_163_rls_* + platform-universal is the arbiter that seeded columns match the migration. |
| T-165-18 | Tampering | is_system renamed in the platform-universal / badge-spoof guard, masking a real regression | mitigate | is_system excluded; grep asserts is_system counts unchanged in the two universal-guard files. |
| T-165-SC | Tampering | npm/pip/cargo installs | accept | No packages installed. |
</threat_model>

<verification>
- All 15 files: 0 bare `is_global`, 0 `folder_is_globally_visible`; `is_system` preserved.
- Per-file split target matches the explicit mapping (spot-checked on the mixed files).
- `--collect-only` clean; assertion-level green is plan 11 post-migration.
</verification>

<success_criteria>
Every RLS/isolation/leak/owner-nulling test renames `is_global` to its correct per-table split target (per the explicit mapping) plus `folder_is_org_shared`, preserves `is_system`, and collects clean.
</success_criteria>

<output>
Create `.planning/phases/165-is-global-retirement-cleanup/165-05-SUMMARY.md` when done.
</output>
