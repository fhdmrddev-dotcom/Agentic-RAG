---
phase: 165-is-global-retirement-cleanup
plan: 10
type: execute
wave: 2
depends_on: ["165-01", "165-02", "165-03", "165-04", "165-05", "165-06", "165-07", "165-08", "165-09"]
files_modified:
  - supabase/full-schema.sql
autonomous: false
requirements: [MIG-02]

must_haves:
  truths:
    - "Migration 111 is APPLIED to the live local DB by the OPERATOR pasting it into the Supabase SQL editor (never db push / db reset — preserves dev data)"
    - "After apply, the six columns are renamed live and every RLS policy / CHECK / DEFINER fn / trigger / storage policy references the new names (auto-propagated policies + explicit fn rewrites)"
    - "supabase/full-schema.sql is regenerated no-reset from the live DB and reflects the renamed columns/functions"
    - "The migration + regenerated full-schema.sql commit together (same-commit rule); all Wave-1 code plans (01-09) are already committed BEFORE apply so there is no partial-deploy window (D-165-06)"
  artifacts:
    - path: "supabase/full-schema.sql"
      provides: "regenerated single-file schema reflecting the rename"
      contains: "is_org_shared"
  key_links:
    - from: "supabase/migrations/111_is_global_retirement_rename.sql"
      to: "live local DB @ :54322"
      via: "operator pastes into Supabase SQL editor as ONE execution"
      pattern: "BEGIN"
---

<objective>
Apply migration 111 to the live local Supabase DB (operator-gated), regenerate `full-schema.sql`, and post-apply-verify that the rename landed correctly in the live catalog. This is the atomic apply gate: DB + already-committed code (plans 01-09) converge here with no partial-deploy window.

Purpose: The phase CANNOT pass verification without the applied migration — the rename touches live columns, RLS policies, a DEFINER function, triggers, and the storage bucket policy that backend + frontend now read by their new names; an unapplied migration = runtime failure that build/type checks alone will NOT catch.

Output: A renamed live DB, a regenerated `full-schema.sql`, and a same-commit landing of migration + schema. Unblocks the exit-gate arbitration (plan 11).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-is-global-retirement-cleanup/165-CONTEXT.md
@CLAUDE.md
@supabase/migrations/111_is_global_retirement_rename.sql
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Operator applies migration 111 via the Supabase SQL editor</name>
  <files>N/A — the operator applies the migration to the live local DB; this task changes no repo file (full-schema regen + commit is Task 2)</files>
  <action>PAUSE for the operator. Claude MUST NOT apply this migration (CLAUDE.md forbids Claude-driven DDL apply / db push / db reset for this gate). Present the how-to-verify steps below and wait for the operator's "applied" signal before Task 2 runs the catalog verification.</action>
  <what-built>
    Migration `supabase/migrations/111_is_global_retirement_rename.sql` (plan 01) — the atomic value-preserving `is_global` rename + DEFINER/trigger rewrites + storage-policy reconciliation. All Wave-1 code plans (02-09) are authored + committed; the DB is the last piece to land.
  </what-built>
  <how-to-verify>
    Claude CANNOT apply this (CLAUDE.md forbids Claude-driven DDL apply for this gate; migrations apply via the SQL editor only, never `supabase db push`/`db reset` — that would wipe dev data). Operator steps:

    0. W1 (dev-server) — FIRST: STOP any running backend (`uvicorn --reload`) and frontend (`vite`) dev server now if not already stopped. The Wave-1 code (plans 02-09) already reads the RENAMED columns, so a server running against the pre-apply DB would 500 on folder/skill/view/workflow endpoints. Restart both ONLY after step 4 (migration applied) + Task 2 (full-schema regenerated).
    1. Open the local Supabase Studio SQL editor (Postgres @ 127.0.0.1:54322 / Studio @ :54323).
    2. Paste the ENTIRE contents of `supabase/migrations/111_is_global_retirement_rename.sql` as ONE execution (it is one `BEGIN…COMMIT`; pure DDL).
    3. Confirm it runs with no error (all six RENAME COLUMN + the function/trigger CREATE OR REPLACE + the ALTER FUNCTION RENAME + the storage policy DROP/CREATE succeed atomically).
    4. Reply "applied" (or paste any error).
  </how-to-verify>
  <acceptance_criteria>
    - Operator has stopped any running backend/frontend dev server (W1) before the apply window.
    - The migration executes atomically with no error against the live local DB.
    - Post-apply the six columns exist under their new names (folders/skills.is_org_shared; the four platform tables.is_system_global) — Claude confirms via a read-only catalog query in Task 2.
  </acceptance_criteria>
  <verify>
    <human-check>Operator confirms the migration executed atomically with no error in the Supabase SQL editor; Task 2 then catalog-verifies the rename via a read-only psycopg2 query.</human-check>
  </verify>
  <done>Operator has stopped dev servers and pasted migration 111 as one execution with no error; the six columns are renamed live (confirmed in Task 2).</done>
  <resume-signal>Operator replies "applied" (or reports the error to fix in plan 01).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Post-apply catalog verification + regenerate full-schema + same-commit landing</name>
  <files>supabase/full-schema.sql</files>
  <read_first>
    - CLAUDE.md (regenerate-full-schema.sh no-reset rule; same-commit rule; deploy-artifact parity — this rename is schema-only, seeds no reference data / touches no env var, so it owes NO docs/OPERATOR.md or check-deploy-drift.sh change, like migs 108/109/110)
    - scripts/regenerate-full-schema.sh (the no-reset live-DB dump)
    - supabase/migrations/111_is_global_retirement_rename.sql (the applied contract to verify against)
  </read_first>
  <action>
    1. Post-apply catalog verification (read-only, via psycopg2 @ :54322 — allowed for verification, NOT for DDL apply). Assert against the LIVE catalog:
       - The six renamed columns exist: `information_schema.columns` shows folders.is_org_shared + skills.is_org_shared + workflow_definitions/document_views/classification_rules/metadata_field_definitions.is_system_global, and NO `is_global` column remains on any of the six tables.
       - skills.is_system STILL exists (not renamed).
       - The four write-locked tables' SELECT policy `qual` (from `pg_policies`) shows the universal branch as `is_system_global = true` (auto-propagated), NOT a bare or widened predicate — CRITICAL over-widening check (T-165-01). The folders/skills SELECT policy shows `is_org_shared`/`folder_is_org_shared`. The INSERT/UPDATE WITH CHECK on the four tables shows `is_system_global = false` (write-lock intact).
       - `folder_is_org_shared(uuid)` exists as SECURITY DEFINER with a pinned search_path; `folder_is_globally_visible` no longer exists.
       - The storage "Users can read own skill files" policy references `s.is_system = true OR s.is_org_shared = true`.
       Record the query results in the summary. If ANY assertion fails (e.g. a policy shows a widened universal branch), STOP and route back to plan 01 — do NOT proceed.
    2. Run `bash scripts/regenerate-full-schema.sh` (NO `--reset` — live-DB dump). Confirm `supabase/full-schema.sql` now shows the renamed columns/functions and zero bare `is_global`.
    3. Same-commit landing: commit `supabase/migrations/111_is_global_retirement_rename.sql` + the regenerated `supabase/full-schema.sql` together (same-commit rule). Record in the summary that cloud-parity migrations 099->110 + this 111 + `SECRETS_ENCRYPTION_KEY` are OWED at the next operator-gated production push (standing item — do NOT apply to cloud now).
    4. Tell the operator they may now restart the backend/frontend dev servers (W1) — the DB + code are consistent.
  </action>
  <verify>
    <automated>grep -Ec "\\bis_global\\b" supabase/full-schema.sql | grep -qx 0 && echo "full-schema clean" || echo "REVIEW: is_global still present in full-schema"</automated>
  </verify>
  <acceptance_criteria>
    - Live catalog: 0 `is_global` columns on the six tables; skills.is_system present; the four tables' universal SELECT branch = `is_system_global = true` (not widened); write-lock `is_system_global = false` intact; folder_is_org_shared exists (folder_is_globally_visible gone); storage policy reconciled.
    - `supabase/full-schema.sql` regenerated no-reset; `grep -E "\\bis_global\\b" supabase/full-schema.sql` returns 0.
    - Migration + full-schema committed in the SAME commit; cloud-parity note recorded (099->111 + SECRETS_ENCRYPTION_KEY owed, not applied).
    - Operator cleared to restart dev servers (W1 window closed).
  </acceptance_criteria>
  <done>The migration is applied and catalog-verified (no over-widening), full-schema is regenerated no-reset, migration + schema land in one commit with the cloud-parity note recorded, and the dev-server restart is cleared.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator SQL-editor apply -> live DB | The only sanctioned apply path; a Claude-driven DDL apply or db push/reset is forbidden (data-loss / policy-drift risk) |
| auto-propagated policy -> universal branch | Post-apply the four tables' universal SELECT branch must read is_system_global=true, never a widened predicate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-22 | Info-disclosure | policy over-widening after auto-propagation | mitigate | Task 2 dumps live `pg_policies.qual` for the four write-locked tables and asserts the universal branch is exactly `is_system_global = true` (T-165-01 arbiter); STOP-and-fix on any drift. |
| T-165-23 | Tampering/DoS | db push/reset wipes dev data or replays out of order | mitigate | Apply is SQL-editor-only per CLAUDE.md; NEVER db push/reset; single atomic BEGIN…COMMIT. |
| T-165-24 | Tampering | full-schema hand-edited or regenerated with --reset (destroys dev data) | mitigate | Regenerate no-reset (live dump); never hand-edit full-schema. |
| T-165-25 | Repudiation | cloud drifts silently (migration not tracked as owed) | accept→mitigate | Cloud-parity note recorded (099->111 + SECRETS_ENCRYPTION_KEY owed at next push); schema-only, no deploy-artifact change owed. |
| T-165-SC | Tampering | npm/pip/cargo installs | accept | No packages installed. |
</threat_model>

<verification>
- Operator stops dev servers (W1), then confirms the migration applied atomically with no error.
- Live catalog verification passes (no is_global columns; write-lock + universal branch correct; folder_is_org_shared present; storage policy reconciled).
- `full-schema.sql` regenerated no-reset with zero bare `is_global`; committed same-commit with the migration.
- Cloud-parity note recorded; dev-server restart cleared.
</verification>

<success_criteria>
Migration 111 is operator-applied to the live local DB (with dev servers stopped for the window), the live catalog verifies the rename landed with no over-widening and the write-lock intact, `full-schema.sql` is regenerated no-reset, and migration + schema land in one commit — unblocking the exit-gate arbitration.
</success_criteria>

<output>
Create `.planning/phases/165-is-global-retirement-cleanup/165-10-SUMMARY.md` when done.
</output>
