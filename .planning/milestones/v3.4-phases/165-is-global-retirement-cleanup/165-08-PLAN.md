---
phase: 165-is-global-retirement-cleanup
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/index.ts
  - frontend/src/lib/api.ts
  - frontend/src/hooks/useFolders.ts
  - frontend/src/hooks/useSkills.ts
  - frontend/src/pages/IngestionPage.tsx
  - frontend/src/pages/SkillsPage.tsx
  - frontend/src/components/ingestion/FolderCreateInput.tsx
  - frontend/src/components/ingestion/FolderDetail.tsx
  - frontend/src/components/ingestion/FolderNode.tsx
  - frontend/src/components/ingestion/FolderTree.tsx
  - frontend/src/components/ingestion/NavRow.tsx
  - frontend/src/components/ingestion/ViewsGroup.tsx
  - frontend/src/components/ingestion/AutomationGroup.tsx
  - frontend/src/components/skills/SkillCard.tsx
  - frontend/src/components/skills/SkillFormDialog.tsx
  - frontend/src/components/skills/tuner/CaseEditor.tsx
  - frontend/src/components/classification/RuleBuilderPanel.tsx
  - frontend/src/components/chat/tool-bodies/TreeBody.tsx
  - frontend/src/components/chat/tool-bodies/LsBody.tsx
  - frontend/src/pages/WorkflowsPage.tsx
autonomous: true
requirements: [MIG-02]

must_haves:
  truths:
    - "D-165-01 + D-165-06: frontend interface fields + component props mirror the wire — folders/skills become is_org_shared (FUNCTIONAL isOrgShared toggle); document_views/classification_rules/metadata_field_definitions/workflow_definitions become is_system_global (DISPLAY-ONLY isSystemGlobal badge)"
    - "W2 (retirement completeness): the compound toggle identifiers (toggleGlobal / toggleFolderGlobal / toggleSkillGlobal / apiToggleFolderGlobal / apiToggleSkillGlobal / onToggleGlobal / handleToggleGlobal) are renamed to their OrgShared equivalents WITH all call sites — no half-renamed toggleFolderGlobal calling an is_org_shared column"
    - "D-165-07 (SC#2): the folder + skill share toggles are relabeled 'Global' -> 'Shared with org' and stay FUNCTIONAL (no silent un-share, no solo-org special-casing)"
    - "D-165-02: Skill.is_system frontend field is NOT renamed (the 'Built-in' pill)"
    - "The whole frontend type-checks (tsc --noEmit clean) — the static consistency proof (a missed compound-identifier call site fails the build)"
  artifacts:
    - path: "frontend/src/types/index.ts"
      provides: "the client contract mirroring the renamed wire fields"
      contains: "is_org_shared"
    - path: "frontend/src/lib/api.ts"
      provides: "toggleFolderOrgShared / toggleSkillOrgShared exports"
      contains: "OrgShared"
  key_links:
    - from: "frontend/src/hooks/useFolders.ts"
      to: "frontend/src/lib/api.ts"
      via: "toggleGlobal->toggleOrgShared callback wraps apiToggleFolderOrgShared"
      pattern: "OrgShared"
---

<objective>
Mirror the DB/backend rename in the frontend CONTRACT + COMPONENTS, relabel the folder + skill share toggles "Global" -> "Shared with org", AND fully retire the `toggle*Global` compound identifiers (W2 — a "retirement cleanup" phase leaves no half-renamed `toggleFolderGlobal` calling an `is_org_shared` column). Per B2, each component is annotated FUNCTIONAL (`isOrgShared` toggle) vs DISPLAY-ONLY (`isSystemGlobal` badge).

Purpose: MIG-02 / SC#1 / SC#2 — the frontend contract follows the wire; the folder + skill toggles read "Shared with org" and stay functional; the display-only platform badges relabel to system/built-in wording; the toggle identifiers are consistently OrgShared.

Output: A type-clean frontend (tsc passes) with renamed fields/props/identifiers and relabeled toggles. (Frontend test files are plan 09.)
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-is-global-retirement-cleanup/165-CONTEXT.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md

<interfaces>
frontend/src/types/index.ts current is_global fields (from Read):
- MetadataFieldDef.is_global (L248) -> is_system_global (DISPLAY-ONLY)
- SavedView.is_global (L319) -> is_system_global (DISPLAY-ONLY)
- ClassificationRule.is_global (L426) -> is_system_global (DISPLAY-ONLY)
- Folder.is_global (L454) -> is_org_shared (FUNCTIONAL)
- Skill.is_global (L500) -> is_org_shared (FUNCTIONAL);  Skill.is_system (L501) UNCHANGED
- SkillCreate.is_global (L518) -> is_org_shared (FUNCTIONAL)

frontend/src/lib/api.ts: createFolder(name, parentId, isGlobal=false) sends body { is_global: isGlobal } -> param isGlobal->isOrgShared, body key -> is_org_shared.
</interfaces>

<compound_identifier_map>
W2 — rename these function/prop identifiers WITH all call sites (folders/skills domain, all -> OrgShared). tsc is the miss-detector (a missed reference fails the build).
| old | new | defined in | call sites |
|---|---|---|---|
| toggleFolderGlobal | toggleFolderOrgShared | api.ts:1610 | useFolders.ts:8 (import alias apiToggleFolderGlobal->apiToggleFolderOrgShared) |
| toggleSkillGlobal | toggleSkillOrgShared | api.ts:1684 | useSkills.ts:8 (import alias apiToggleSkillGlobal->apiToggleSkillOrgShared) |
| apiToggleFolderGlobal | apiToggleFolderOrgShared | useFolders.ts:8 alias | useFolders.ts:108 |
| apiToggleSkillGlobal | apiToggleSkillOrgShared | useSkills.ts:8 alias | useSkills.ts:67 |
| toggleGlobal | toggleOrgShared | useFolders.ts:17/107/112, useSkills.ts:20/63/71 | IngestionPage.tsx:78/315, SkillsPage.tsx:56/174 (destructure + pass as onToggleGlobal) |
| onToggleGlobal | onToggleOrgShared | FolderNode.tsx:49/73/167/253, FolderTree.tsx:22/35/196, SkillCard.tsx:18/34/70/308 | IngestionPage.tsx:315, SkillsPage.tsx:174 |
| handleToggleGlobal | handleToggleOrgShared | SkillCard.tsx:68 | SkillCard.tsx:261 |
NOTE: the matching test mocks (onToggleGlobal / toggleSkillGlobal in FolderTree.test.tsx, FolderNode.test.tsx, SkillCard.test.tsx, SkillTunerPage.test.tsx, SkillStudioPage.test.tsx) are renamed in PLAN 09.
</compound_identifier_map>

<per_component_functional_vs_display>
FUNCTIONAL isOrgShared toggle (folders/skills) — incl. "Global"->"Shared with org" copy (SC#2):
- FolderCreateInput.tsx, FolderDetail.tsx, FolderNode.tsx, FolderTree.tsx (folder toggle/badge)
- SkillCard.tsx (skill toggle; KEEP the is_system "Built-in" pill separate)
- IngestionPage.tsx, SkillsPage.tsx (page wiring of toggleGlobal/onToggleGlobal — compound-identifier rename, field pass-through)
- TreeBody.tsx, LsBody.tsx (render folder is_org_shared on the ls/tree cards)

DISPLAY-ONLY isSystemGlobal badge (platform tables):
- ViewsGroup.tsx (saved-views pill), WorkflowsPage.tsx (workflow badge), RuleBuilderPanel.tsx (rules)

MIXED (both — split per-line): NavRow.tsx, AutomationGroup.tsx (folder branch is_org_shared; view/rule branch is_system_global).

W3 EXPECTED NO-OPS: SkillFormDialog.tsx and tuner/CaseEditor.tsx have ZERO is_global/isGlobal/is_system tokens today (their .test.tsx counterparts in Plan 09 do). They are expected no-ops for the field rename — verify (grep) and skip; only touch them if a stray "Global" toggle-label copy is present (relabel to "Shared with org").
</per_component_functional_vs_display>

<rename_map>
- folders/skills: is_global -> is_org_shared ; isGlobal -> isOrgShared
- document_views/classification_rules/metadata_field_definitions/workflow_definitions: is_global -> is_system_global ; isGlobal -> isSystemGlobal
- Skill.is_system / isSystem: UNCHANGED (D-165-02, the 'Built-in' pill)
- UI copy on folder + skill share toggles: "Global" -> "Shared with org" (D-165-07); display-only platform badges -> system/built-in wording
</rename_map>

<dev_server_note>
W1 (dev-server): this plan reads renamed wire fields; a running vite dev server pointed at a still-old backend (pre-apply) will show broken folder/skill/view data until plan 165-10 applies the migration. Operator: stop dev servers before Wave 1, restart after 165-10.
</dev_server_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the contract layer (types + api + hooks) + the api/hook toggle identifiers</name>
  <files>frontend/src/types/index.ts, frontend/src/lib/api.ts, frontend/src/hooks/useFolders.ts, frontend/src/hooks/useSkills.ts</files>
  <read_first>
    - frontend/src/types/index.ts (the 6 is_global fields at L248/319/426/454/500/518 + L501 is_system)
    - frontend/src/lib/api.ts (createFolder ~1579-1584 + toggleFolderGlobal:1610 + toggleSkillGlobal:1684)
    - frontend/src/hooks/useFolders.ts + useSkills.ts (is_global/isGlobal + the toggleGlobal/apiToggle* identifiers)
    - The compound_identifier_map above
  </read_first>
  <action>
    types/index.ts: Folder/Skill/SkillCreate.is_global -> is_org_shared (Skill.is_system stays); MetadataFieldDef/SavedView/ClassificationRule.is_global -> is_system_global; update the doc comments.
    api.ts: createFolder param isGlobal -> isOrgShared, body key is_global -> is_org_shared; rename the exports `toggleFolderGlobal`->`toggleFolderOrgShared` and `toggleSkillGlobal`->`toggleSkillOrgShared`; update the server-owned comments (classification rules/workflows -> is_system_global).
    hooks: useFolders.ts + useSkills.ts — rename is_global/isGlobal folder/skill fields -> is_org_shared (keep is_system in useSkills); rename the import aliases apiToggleFolderGlobal->apiToggleFolderOrgShared / apiToggleSkillGlobal->apiToggleSkillOrgShared and the exported callback `toggleGlobal`->`toggleOrgShared` (+ its internal uses).
  </action>
  <verify>
    <automated>cd frontend && grep -rn "toggleFolderGlobal\\|toggleSkillGlobal\\|apiToggleFolderGlobal\\|apiToggleSkillGlobal" src/lib/api.ts src/hooks/useFolders.ts src/hooks/useSkills.ts | grep -c "" | sed 's/^/old-identifier-refs-remaining: /'</automated>
  </verify>
  <acceptance_criteria>
    - The four files carry 0 six-table `is_global`/`isGlobal`; types split correct (Folder/Skill/SkillCreate=is_org_shared, SavedView/ClassificationRule/MetadataFieldDef=is_system_global, Skill.is_system unchanged).
    - api.ts exports `toggleFolderOrgShared` + `toggleSkillOrgShared` (0 remaining toggleFolderGlobal/toggleSkillGlobal); hooks export `toggleOrgShared` and import the renamed aliases (0 remaining apiToggleFolderGlobal/apiToggleSkillGlobal/toggleGlobal in these files).
    - `old-identifier-refs-remaining: 0`.
  </acceptance_criteria>
  <done>The contract layer mirrors the renamed wire; the api/hook toggle identifiers are OrgShared; is_system preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Rename components + pages (fields, props, compound identifiers) + relabel toggles "Global" -> "Shared with org"</name>
  <files>frontend/src/pages/IngestionPage.tsx, frontend/src/pages/SkillsPage.tsx, frontend/src/components/ingestion/FolderCreateInput.tsx, frontend/src/components/ingestion/FolderDetail.tsx, frontend/src/components/ingestion/FolderNode.tsx, frontend/src/components/ingestion/FolderTree.tsx, frontend/src/components/ingestion/NavRow.tsx, frontend/src/components/ingestion/ViewsGroup.tsx, frontend/src/components/ingestion/AutomationGroup.tsx, frontend/src/components/skills/SkillCard.tsx, frontend/src/components/skills/SkillFormDialog.tsx, frontend/src/components/skills/tuner/CaseEditor.tsx, frontend/src/components/classification/RuleBuilderPanel.tsx, frontend/src/components/chat/tool-bodies/TreeBody.tsx, frontend/src/components/chat/tool-bodies/LsBody.tsx, frontend/src/pages/WorkflowsPage.tsx</files>
  <read_first>
    - grep -rn "is_global|isGlobal|Global|onToggleGlobal|handleToggleGlobal" across the listed files (field usage + the "Global" toggle label + the compound identifiers)
    - The per_component_functional_vs_display block (functional vs display; NavRow + AutomationGroup MIXED; SkillFormDialog + CaseEditor expected no-ops)
    - The compound_identifier_map (onToggleGlobal->onToggleOrgShared prop across FolderNode/FolderTree/SkillCard + IngestionPage/SkillsPage call sites; handleToggleGlobal->handleToggleOrgShared in SkillCard; toggleGlobal destructure in the two pages)
    - frontend/src/types/index.ts (the renamed field names from Task 1)
  </read_first>
  <action>
    Field/prop renames per the per_component annotation: folder/skill functional files + tool-bodies -> is_org_shared/isOrgShared (Skill.is_system stays); ViewsGroup/WorkflowsPage/RuleBuilderPanel display badges -> is_system_global/isSystemGlobal. MIXED files (NavRow, AutomationGroup): folder branch -> is_org_shared, view/rule branch -> is_system_global (per-line).

    Compound identifiers (W2): rename the `onToggleGlobal` prop -> `onToggleOrgShared` on FolderNode.tsx / FolderTree.tsx / SkillCard.tsx (interface + destructure + internal calls) and update the pass-through in IngestionPage.tsx (`onToggleGlobal={toggleGlobal}` -> `onToggleOrgShared={toggleOrgShared}`) and SkillsPage.tsx; rename `handleToggleGlobal` -> `handleToggleOrgShared` in SkillCard.tsx; update the `toggleGlobal` destructure in both pages -> `toggleOrgShared`.

    UI copy (D-165-07 / SC#2): on the FOLDER and SKILL share toggles/labels, change "Global" -> "Shared with org" (functional, no solo-org special-casing). Display-only is_system_global badges (ViewsGroup, WorkflowsPage) -> system/built-in wording.

    W3: SkillFormDialog.tsx + tuner/CaseEditor.tsx are expected no-ops (no is_global tokens) — verify via grep; only edit if a stray "Global" toggle label is present.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (the whole app type-checks — the load-bearing proof the field + compound-identifier renames are consistent; a missed call site fails here).
    - `grep -rn "ToggleGlobal\\|toggleGlobal" frontend/src --include=*.tsx --include=*.ts | grep -v "\\.test\\."` returns 0 (all non-test compound identifiers retired; test mocks are plan 09).
    - The six-table is_global/isGlobal fields across the components return 0.
    - The folder + skill toggle label reads "Shared with org" (`grep -rn "Shared with org" frontend/src/components` shows the folder + skill sites); no lingering user-facing "Global" toggle label on folders/skills.
    - Skill.is_system ("Built-in" pill) usages unchanged; SkillFormDialog.tsx + CaseEditor.tsx recorded as no-ops (or the stray-label fix noted).
    - MIXED files (NavRow, AutomationGroup): folder branch is_org_shared, view/rule branch is_system_global.
  </acceptance_criteria>
  <done>Components + pages consume the renamed fields with functional/display split correct, the compound toggle identifiers are OrgShared with all call sites updated, the folder/skill toggles read "Shared with org", and the whole frontend type-checks clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client field name -> wire field name | The frontend interface must mirror the renamed wire or reads silently return undefined |
| toggle label + identifier -> user mental model | "Shared with org" must remain a functional share toggle (no silent un-share); a half-renamed toggle*Global identifier is the exact half-renamed state this phase eliminates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-19 | Info-disclosure (UX) | a folder/skill silently un-shares because a renamed identifier drops the write | mitigate | Value-preserving rename; tsc gate + plan-09 toggle test assert the toggle still POSTs is_org_shared; W2 compound-identifier rename keeps the handler wired end-to-end; no solo-org special-casing (D-165-07). |
| T-165-20 | Tampering | a display-only is_system_global badge implies a settable control | accept | is_system_global is display-only in the frontend; the server owns the write-lock (plan 03). |
| T-165-21 | Tampering | a MIXED-file (NavRow/AutomationGroup) or half-renamed compound identifier | mitigate | Per-occurrence table read + the tsc gate (a missed call site fails the build); the `grep ToggleGlobal == 0` acceptance closes the half-rename. |
| T-165-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (whole-app type consistency).
- `grep -rn "ToggleGlobal\\|toggleGlobal" frontend/src` (excluding tests) == 0; six-table is_global/isGlobal fields == 0 in source.
- Folder + skill toggles read "Shared with org"; Skill.is_system preserved; SkillFormDialog/CaseEditor no-ops recorded.
- MIXED files split correctly (folder->is_org_shared, view/rule->is_system_global).
</verification>

<success_criteria>
The frontend contract + components + pages mirror the renamed wire, fully retire the `toggle*Global` compound identifiers with all call sites (tsc-verified), relabel the folder + skill toggles to "Shared with org" while keeping them functional, preserve `Skill.is_system`, and type-check clean.
</success_criteria>

<output>
Create `.planning/phases/165-is-global-retirement-cleanup/165-08-SUMMARY.md` when done.
</output>
