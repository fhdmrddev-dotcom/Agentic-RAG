---
phase: 161-org-dept-role-schema
plan: 02
type: execute
wave: 2
depends_on: [161-01]
files_modified:
  - supabase/full-schema.sql
autonomous: false
requirements: [ORG-01, ORG-02]

must_haves:
  truths:
    - "SC#2 / ORG-02 / D-10: a LIVE authenticated query against `org_members` returns WITHOUT SQLSTATE `42P17` — the recursion-break is proven against the running DB, not just authored."
    - "SC#1 / ORG-01: all 8 org tables exist live with RLS enabled (`to_regclass` non-null + `pg_class.relrowsecurity = true`)."
    - "ORG-02 / D-10: `current_user_org_ids()` exists live as `SECURITY DEFINER` (`pg_proc.prosecdef = true`)."
    - "D-02: the permission catalog is seeded live — `roles` has the 4 fixed tiers and `role_permissions` has the milestone-known grants."
    - "SC#3: the swept user-facing tables carry `org_id` live (information_schema.columns), while document_chunks/skill_embeddings and the 4 system/identity tables do NOT — proving the exclusions held."
    - "D-01 (CLAUDE.md workflow): `supabase/full-schema.sql` is regenerated via `scripts/regenerate-full-schema.sh` (NO `--reset`, NO `db push`) and committed together with the migration in one commit; full-schema.sql is never hand-edited."
    - "ADR SC#4: the local setup still boots unchanged and no deployment tier is made harder — verified additive (empty tables + nullable columns only)."
  artifacts:
    - path: "supabase/full-schema.sql"
      provides: "The regenerated single-file bootstrap dump containing the 8 org tables + current_user_org_ids() at head 104"
      contains: "public.organizations"
  key_links:
    - from: "operator SQL-editor apply"
      to: "live local DB (port 54322)"
      via: "paste + run 104_org_dept_role_schema.sql"
      pattern: "organizations"
    - from: "scripts/regenerate-full-schema.sh"
      to: "supabase/full-schema.sql"
      via: "live-DB dump (no reset)"
      pattern: "regenerate-full-schema"
---

<objective>
Apply the authored `104_org_dept_role_schema.sql` migration to the live local Supabase DB (operator-driven, via the SQL editor — NEVER `db push`/`db reset`), regenerate the `supabase/full-schema.sql` bootstrap artifact, prove the schema landed with LIVE-COLUMN EVIDENCE (not build/type checks), and commit the migration + regenerated schema together.

Purpose: A migration that is authored but not applied is not done, and "types compile" is NOT proof — the phase CANNOT pass verification on build/type checks alone (the schema-application gate is mandatory). This plan closes the loop with running-DB evidence, headlined by the live `42P17` non-recursion test that IS SC#2 / ORG-02.

Output: The migration applied to the live local DB; `full-schema.sql` regenerated; live verification captured; both files committed together; deployment-artifact parity checked.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/161-org-dept-role-schema/161-CONTEXT.md
@.planning/phases/161-org-dept-role-schema/161-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Project-specific migration workflow — STRICT (CLAUDE.md + confirmed lesson). -->
- Apply = paste + run in the LOCAL Supabase SQL editor. NEVER `supabase db push` / `supabase db reset` (they wipe dev data — forbidden).
- Regenerate = `bash scripts/regenerate-full-schema.sh` (default: live-DB dump, NO reset → rebuilds supabase/full-schema.sql). Pass `--reset` ONLY for clean-slate CI verification — do NOT use it here (destructive).
- Live DB evidence = psycopg2 against localhost port 54322 (the evidence-tools inventory) OR a query pasted into the SQL editor.
- Deploy-artifact parity = `bash scripts/check-deploy-drift.sh` (CI enforcement; env-var-focused). CLAUDE.md D-16 same-commit rule for seed-bearing migrations / touched deploy artifacts.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Operator applies migration 104 via the Supabase SQL editor</name>
  <files>supabase/migrations/104_org_dept_role_schema.sql</files>
  <what-built>
    Plan 01 authored `supabase/migrations/104_org_dept_role_schema.sql` — the 8 org tables + 3 SECDEF helpers + membership RLS + permission-catalog seed + the 23-table org_id sweep. It is idempotent / re-paste-safe. It has NOT been applied. Claude cannot open the Supabase SQL editor GUI, so the operator applies it.
  </what-built>
  <action>
    OPERATOR-ONLY manual step (Claude cannot drive the Supabase Studio GUI). The operator pastes the full contents of `supabase/migrations/104_org_dept_role_schema.sql` into the LOCAL Supabase SQL editor and runs it, confirming a clean result. NEVER `supabase db push` / `supabase db reset` (they wipe dev data). Because the migration is idempotent, a re-paste is a safe recovery action if it errors. Claude does NOT proceed to Task 2 until the operator confirms "applied".
  </action>
  <how-to-verify>
    1. Open the LOCAL Supabase SQL editor (the local stack on Docker; SQL editor at the local Studio URL).
    2. Open `supabase/migrations/104_org_dept_role_schema.sql`, copy its ENTIRE contents, paste into a new SQL editor query, and Run.
    3. Confirm it completes with NO error. (If it errors: the migration is idempotent — Claude fixes the file in Plan 01's migration, you re-paste and re-run. The `IF NOT EXISTS` / `CREATE OR REPLACE` / `DROP POLICY IF EXISTS` / `ON CONFLICT DO NOTHING` guards make re-running safe.)
    4. Do NOT run `supabase db push` or `supabase db reset` — pasting into the SQL editor is the ONLY sanctioned apply path (preserves your dev data).
    5. Reply "applied" (or paste any error text).
  </how-to-verify>
  <verify>
    <human-check>Operator confirms migration 104 ran clean in the local Supabase SQL editor (no error). Automated live assertions run in Task 2 after this gate clears.</human-check>
  </verify>
  <resume-signal>Type "applied" once migration 104 ran clean in the SQL editor, or paste the error for Claude to fix.</resume-signal>
  <done>Migration 104 is applied to the live local DB via the SQL editor (operator-confirmed), with no db push / db reset used.</done>
</task>

<task type="auto" wave="2">
  <name>Task 2: Regenerate full-schema.sql, prove it live, check deploy parity, commit both</name>
  <files>supabase/full-schema.sql</files>
  <read_first>
    - CLAUDE.md — the numbered-migration workflow (regenerate-full-schema.sh no-reset; never hand-edit full-schema.sql; deployment-artifact same-commit parity / D-16).
    - .planning/phases/161-org-dept-role-schema/161-01-SUMMARY.md — the final migration filename, the exact swept table list, the 3 helper signatures, the policy names, and the seeded grant matrix (so verification asserts the right object names).
    - scripts/regenerate-full-schema.sh — confirm it defaults to a live-DB dump with NO reset before running it.
    - scripts/check-deploy-drift.sh — the CI drift check + the OMITTED_FROM_ONEBOX allowlist mechanism.
  </read_first>
  <action>
    Runs only AFTER the operator confirms "applied".

    (1) Regenerate the bootstrap artifact: `bash scripts/regenerate-full-schema.sh` (NO `--reset` — it must be a live-DB dump that preserves data). Confirm `supabase/full-schema.sql` now contains the 8 new tables + `current_user_org_ids()`. Never hand-edit full-schema.sql.

    (2) Prove it LIVE with psycopg2 against localhost:54322 (or SQL-editor queries) — capture the outputs as evidence (do NOT accept "types compile / build passes" as verification; types come from config, not the live DB):
    - Existence + RLS: `SELECT to_regclass('public.'||t) FROM (VALUES ('organizations'),('departments'),('org_members'),('dept_members'),('roles'),('role_permissions'),('org_invitations'),('sso_configs')) x(t)` all non-null; `SELECT relname, relrowsecurity FROM pg_class WHERE relname IN (...)` shows relrowsecurity = true for all 8.
    - SECDEF helper: `SELECT proname, prosecdef FROM pg_proc WHERE proname IN ('current_user_org_ids','current_user_has_permission','create_org_with_default_dept')` shows prosecdef = true for all three.
    - THE 42P17 LIVE TEST (SC#2): in one transaction, `SET LOCAL ROLE authenticated; SET LOCAL request.jwt.claims = '{"sub":"00000000-0000-0000-0000-000000000000","role":"authenticated"}'; SELECT count(*) FROM public.org_members;` — MUST return (0 rows, empty table) WITHOUT raising SQLSTATE `42P17`. This is the headline proof; if it raises 42P17 the org_members policy is recursive and Plan 01 must be fixed + re-applied.
    - Seed: `SELECT count(*) FROM public.roles` = 4; `SELECT role, count(*) FROM public.role_permissions GROUP BY role` matches the D-02 matrix (super-admin→5, org-admin→3, dept-admin→1, member→0 or absent).
    - Sweep held: `SELECT table_name FROM information_schema.columns WHERE table_schema='public' AND column_name='org_id' AND table_name IN ('messages','todos','user_settings','code_executions')` returns all 4 (swept); the SAME query for `('document_chunks','skill_embeddings','app_settings','operator_users','profiles','model_capabilities_overrides')` returns NONE (exclusions held). Confirm a swept btree index exists, e.g. `idx_messages_org_id` in pg_indexes.

    (3) Deployment-artifact parity (CLAUDE.md D-16): run `bash scripts/check-deploy-drift.sh` — it must exit 0. Separately assess whether the permission-catalog seed needs a `docs/OPERATOR.md` Step-3 line: it is migration-embedded reference data (roles/role_permissions travel inside 104 and full-schema.sql, applied for free on any greenfield deploy) — unlike an operator-inserted `app_settings` row — so it most likely needs NO OPERATOR.md touch. If the drift check flags anything, either update the flagged artifact in THIS commit or register the omission with a reason per the script's allowlist. Surface the assessment to the operator for confirmation (checkpoint:decision if a real same-commit artifact change is required).

    (4) Commit the migration + regenerated schema TOGETHER in one commit (e.g. `feat(161): org/dept/role schema — 8 tables + current_user_org_ids helper + permission catalog + org_id sweep (mig 104)`), staging BOTH `supabase/migrations/104_org_dept_role_schema.sql` and `supabase/full-schema.sql` (plus any parity-artifact touch from step 3). Note in the commit body that migration 104 is now owed at the next cloud push (adds to the 099-103 pending set).
  </action>
  <acceptance_criteria>
    - `supabase/full-schema.sql` regenerated (no `--reset` used) and contains `public.organizations` + `current_user_org_ids` (grep the regenerated dump).
    - LIVE: `to_regclass` non-null for all 8 tables AND `pg_class.relrowsecurity = true` for all 8.
    - LIVE: `pg_proc.prosecdef = true` for `current_user_org_ids` (and the other 2 helpers).
    - LIVE 42P17 PROOF: the `SET LOCAL ROLE authenticated` + `SELECT ... FROM public.org_members` query returns 0 rows and does NOT raise SQLSTATE `42P17` (capture the successful output).
    - LIVE: `roles` count = 4; `role_permissions` grant distribution matches the D-02 matrix.
    - LIVE: information_schema.columns shows org_id on the swept sample (messages/todos/user_settings/code_executions) and shows NO org_id on document_chunks/skill_embeddings/app_settings/operator_users/profiles/model_capabilities_overrides.
    - `bash scripts/check-deploy-drift.sh` exits 0 (or the flagged artifact is updated in the same commit / registered in the allowlist with a reason).
    - `git show --stat HEAD` shows BOTH `supabase/migrations/104_org_dept_role_schema.sql` and `supabase/full-schema.sql` in the one commit; full-schema.sql was not hand-edited (only the regen script touched it).
  </acceptance_criteria>
  <verify>
    <automated>python -c "import psycopg2" against localhost:54322: assert to_regclass non-null for all 8 org tables, pg_proc.prosecdef=true for current_user_org_ids, and the SET LOCAL ROLE authenticated + SELECT count(*) FROM public.org_members returns 0 WITHOUT SQLSTATE 42P17; then `bash scripts/check-deploy-drift.sh` exits 0</automated>
  </verify>
  <done>full-schema.sql is regenerated (no reset) and committed with the migration in one commit; all live assertions pass — headlined by the 42P17 non-recursion proof; deploy-drift is clean; migration 104 recorded as owed at next cloud push.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored migration → live local DB | The unapplied SQL crosses into the running Postgres via the SQL editor. Risk if applied via `db push`/`db reset`: dev-data loss (forbidden path). |
| live RLS policy → `org_members` | The `42P17` recursion is only PROVABLE against the running DB — a purely-authored policy could still recurse live. This plan is where that proof happens. |

## STRIDE Threat Register (verification of Plan-01 mitigations, proven live)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-161-01 | Denial of Service | `org_members` policy (42P17) | mitigate | **HIGH — proven live here.** The `SET LOCAL ROLE authenticated` + `SELECT FROM org_members` test must return without `42P17`; failure blocks the phase and forces a Plan-01 fix + idempotent re-apply. |
| T-161-05 | Tampering (search-path) | SECDEF helpers | mitigate | **HIGH — proven live.** `pg_proc.prosecdef = true` confirmed; the pinned `search_path` was authored in Plan 01 (Phase 164 TEN-03 audits the bodies). |
| T-161-07 | Tampering / Availability (destructive apply) | migration application path | mitigate | **HIGH.** The BLOCKING operator task explicitly forbids `db push`/`db reset` and uses the SQL-editor paste (dev-data-preserving). Regeneration uses the no-reset dump. |
| T-161-03 | Elevation of Privilege | `roles`/`role_permissions` | accept | **Verified by construction** — reference tables ship read-all + zero write policy (Plan 01); live seed counts confirm the catalog is present and unchanged. |
| T-161-SC | Tampering (supply chain) | package installs | accept | **LOW.** No package installs — SQL apply + a schema-dump script only. |

All `high` threats verified/mitigated live. No `open` high remains.
</threat_model>

<verification>
- Migration 104 applied clean to the live local DB via the SQL editor (operator-confirmed) — NO `db push`/`db reset`.
- `full-schema.sql` regenerated (no reset) and committed WITH the migration in one commit.
- All LIVE acceptance assertions pass — headlined by the 42P17 non-recursion proof (SC#2 / ORG-02).
- Deploy-drift check clean (or flagged artifact handled same-commit).
- Local setup boots unchanged; additive-only change confirmed (ADR SC#4 — no tier made harder).
</verification>

<success_criteria>
- ORG-01 + ORG-02 proven LIVE (not on build/type checks): 8 tables + RLS + the SECDEF helper + the 42P17-safe org_members policy all confirmed against the running DB.
- The bootstrap artifact is consistent (regenerated, committed with the migration, never hand-edited).
- The phase's schema-application gate is satisfied — the only remaining cloud action is the standing "owed at next production push" parity for migration 104.
</success_criteria>

<output>
Create `.planning/phases/161-org-dept-role-schema/161-02-SUMMARY.md` when done — record the live verification evidence (the 42P17 test output, the to_regclass/pg_proc results, the seed counts, the swept-column sample), the commit SHA, and the cloud-parity note (migration 104 owed at next push), so Phase 162 (backfill) can build on a confirmed-live foundation.
</output>
