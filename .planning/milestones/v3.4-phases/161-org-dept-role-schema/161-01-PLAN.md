---
phase: 161-org-dept-role-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/104_org_dept_role_schema.sql
autonomous: true
requirements: [ORG-01, ORG-02]

must_haves:
  truths:
    - "SC#1 / D-01: `organizations` ships with `subscription_tier`, `add_ons jsonb`, and a SEPARATE `settings jsonb NOT NULL DEFAULT '{}'` — the two jsonb homes are distinct (add_ons = entitlements; settings = SEED-120 forward-compat config)."
    - "SC#1: all 8 org tables are authored — organizations, departments, org_members, dept_members, roles, role_permissions, org_invitations, sso_configs — each with a PK, timestamps, and (org-scoped ones) an FK to organizations."
    - "ORG-02 / D-10: `current_user_org_ids()` is authored `SECURITY DEFINER` with `SET search_path TO 'public'`, reading `org_members WHERE user_id = auth.uid()` — the recursion-break primitive."
    - "ORG-02 / D-10: the `org_members` SELECT policy is self-rows-only (`user_id = auth.uid()`) with NO `org_members` subquery; the org-admin read path routes through `current_user_org_ids()`. No other table inlines an `org_members` subquery."
    - "D-04: `roles` + `role_permissions` are GLOBAL reference data — a read-all `USING (true)` SELECT policy and NO write policy (INSERT/UPDATE/DELETE denied to every JWT; migration-only seeding)."
    - "D-08 / D-09: membership-correct RLS is authored on all 8 NEW tables in THIS phase (correct-from-birth — no legacy behavior to preserve); these 8 new tables are EXCLUDED from Phase 163's 38-existing-table RLS-rewrite scope (163 rewrites only the existing content/identity tables)."
    - "D-02 / D-03: the permission catalog is seeded — the 4 fixed tiers plus default grants for the milestone-known OPEN-STRING keys (org:manage, org:audit_view, dept:manage, org:invite, sso:manage); super-admin→all, org-admin→org:*, dept-admin→dept:*, member→none."
    - "D-11: `departments.is_default boolean` + a partial-unique index (one `is_default=true` per `org_id`) + a `SECURITY DEFINER` `create_org_with_default_dept()` helper — one default department per org BY CONSTRUCTION."
    - "SC#3 / D-05 discretion: nullable `org_id uuid` (NO FK, NO NOT NULL) plus a btree index is added additively to the 23 remaining user-facing tables; document_chunks/skill_embeddings and the 4 system/identity tables are EXCLUDED."
    - "SC#4: the same schema serves a 5-person team and a 2,000-person org — small orgs never touch dept_members; large orgs nest via `departments.parent_id` (nullable self-FK) — with zero schema change either way."
    - "D-06 / D-07: `org_invitations` ships near-complete storing `token_hash` (never a raw token); `sso_configs` ships deliberately thin (org_id, email_domain, nullable Supabase-provider pointer, attribute_mapping jsonb) with NO invented SAML columns."
  artifacts:
    - path: "supabase/migrations/104_org_dept_role_schema.sql"
      provides: "The single additive migration: 8 org tables + 3 SECDEF helpers + membership-correct RLS + permission-catalog seed + the 23-table org_id sweep"
      contains: "CREATE TABLE IF NOT EXISTS public.organizations"
  key_links:
    - from: "public.current_user_org_ids()"
      to: "public.org_members"
      via: "SECURITY DEFINER body reads org_members WHERE user_id = auth.uid()"
      pattern: "org_members.*auth\\.uid\\(\\)"
    - from: "org-scoped table RLS policies"
      to: "public.current_user_org_ids()"
      via: "read predicate org_id IN (SELECT public.current_user_org_ids())"
      pattern: "current_user_org_ids"
    - from: "departments.is_default"
      to: "partial-unique index"
      via: "one is_default=true per org_id"
      pattern: "WHERE .*is_default"
---

<objective>
Author the single additive Supabase migration `supabase/migrations/104_org_dept_role_schema.sql` that ships the v3.4 org/dept/role/membership schema foundation (ORG-01, ORG-02): the 8 new org tables with RLS + FKs + indexes from creation, the `current_user_org_ids()` recursion-break helper (breaks `42P17`), the default-department construction guarantee, the seeded core permission catalog, and the nullable `org_id` sweep across the remaining user-facing tables.

Purpose: This is the membership-keyed authorization substrate the personal-org backfill (Phase 162) and the atomic RLS + user-JWT crux (Phase 163) reference. It is **additive and zero-behavior-change on creation** — the new tables are empty and existing tables only gain a nullable column, so Deep Mode / the agent loop / retrieval / every current query stay byte-identical.

Output: One authored (not-yet-applied) numbered SQL migration file. Application + live verification is Plan 02 (a separate BLOCKING plan). This plan produces the file only — do NOT apply it here.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/161-org-dept-role-schema/161-CONTEXT.md
@.planning/phases/161-org-dept-role-schema/161-PATTERNS.md
@.planning/phases/160-tenancy-model-adr/160-ADR.md
@CLAUDE.md

<interfaces>
<!-- The load-bearing analog SHAPES to copy verbatim. All line numbers are into supabase/full-schema.sql at head 103 (RE-VERIFY — a dump regenerates). PATTERNS.md carries the full excerpts. -->

- `current_user_org_ids()` shape = `folder_is_globally_visible()` at full-schema.sql:97-113 — `LANGUAGE sql STABLE SECURITY DEFINER` + `SET search_path TO 'public'`. This is THE recursion-break precedent this codebase already trusts.
- Global reference-data policy = `model_overrides_read_all` at full-schema.sql:3944 — `FOR SELECT TO authenticated USING (true)`, NO write policy.
- Self-referential tree = `folders.parent_id` self-FK at full-schema.sql:843 + folders_parent_id_fkey:2833-2837 (`ON DELETE CASCADE`) + folders_parent_id_idx:2086-2089.
- Partial-unique index = documents_completed_hash_unique_idx at full-schema.sql:2040 (`... WHERE (...)`).
- Enum-via-CHECK = documents_status_check at full-schema.sql:686 (`CHECK ((col = ANY (ARRAY[...]::text[]))))`) — the repo has ZERO native `CREATE TYPE` enums.
- New-tables-with-RLS-in-one-migration = supabase/migrations/095_operator_foundation.sql (CREATE TABLE IF NOT EXISTS + ENABLE ROW LEVEL SECURITY + `metadata jsonb NOT NULL DEFAULT '{}'`).
- org_id stub column shape = supabase/migrations/096_org_id_stub_sweep.sql:18-26 (`ADD COLUMN IF NOT EXISTS org_id uuid` + forward-compat COMMENT). btree index shape = idx_classification_rules_org_id at full-schema.sql:2103.
- Idempotent seed = 094_starter_workflows.sql (`INSERT ... ON CONFLICT (...) DO NOTHING`).
- RLS-policy idempotency idiom (CREATE POLICY has no IF NOT EXISTS) = `DROP POLICY IF EXISTS <name> ON <table>; CREATE POLICY <name> ...` (015/019/029/030 migrations).
- `set_updated_at()` trigger fn = full-schema.sql:273-280, wired per-table (e.g. folders_set_updated_at) — reuse for any org table carrying updated_at.
</interfaces>
</context>

<tasks>

<task type="auto" wave="1">
  <name>Task 1: Author the 8 org tables + the 3 SECURITY DEFINER helpers</name>
  <files>supabase/migrations/104_org_dept_role_schema.sql</files>
  <read_first>
    - .planning/phases/161-org-dept-role-schema/161-PATTERNS.md — the analog map with exact line refs + code excerpts (your primary cheat sheet; do NOT re-derive shapes).
    - .planning/phases/161-org-dept-role-schema/161-CONTEXT.md — D-01, D-03, D-06, D-07, D-11 (the exact table/column obligations).
    - supabase/migrations/095_operator_foundation.sql — the "create net-new tables + ENABLE RLS in one migration" house style + the migration-header apply-discipline comment block to copy.
    - supabase/full-schema.sql lines 97-113 (folder_is_globally_visible — the SECDEF helper shape), 273-280 (set_updated_at), 683-694 (documents CHECK + org_id column + comment), 835-851 (folders self-FK + org_id), 2833-2837 (folders_parent_id_fkey) — the shapes to copy.
    - .planning/phases/160-tenancy-model-adr/160-ADR.md — the slot-104+ renumbering lock + the 4-tier deployment-flexibility contract (SC#4 binds this phase).
  </read_first>
  <action>
    Create the file `supabase/migrations/104_org_dept_role_schema.sql`. Open with the standard apply-discipline comment header (copy 095's shape): apply by pasting into the LOCAL Supabase SQL editor (NEVER `db push`/`db reset`), idempotent/re-paste-safe, then `bash scripts/regenerate-full-schema.sh` (no `--reset`) + commit the migration AND full-schema.sql together, never hand-edit full-schema.sql, digits-only filename, and a CLOUD-PARITY line (099-103 already pending on cloud — do not touch cloud now).

    Author the 8 tables FIRST (a SQL helper referencing a table needs the table to exist at CREATE time), all with `CREATE TABLE IF NOT EXISTS`, `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`, and `created_at timestamptz NOT NULL DEFAULT now()`:
    - `organizations` — `name text NOT NULL`; `subscription_tier text` (the ENT-01/SC#1 foothold, from day one); `add_ons jsonb NOT NULL DEFAULT '{}'` (entitlements); and — per D-01, kept STRICTLY SEPARATE — `settings jsonb NOT NULL DEFAULT '{}'` (the SEED-120 forward-compat home). Both jsonb columns copy the `metadata jsonb NOT NULL DEFAULT '{}'` shape at 095:47. Optionally `updated_at` + slug (executor discretion).
    - `departments` — `org_id uuid NOT NULL REFERENCES public.organizations(id) ON DELETE CASCADE`; `name text NOT NULL`; `parent_id uuid REFERENCES public.departments(id) ON DELETE CASCADE` (nullable self-FK, SC#4/D-11); `is_default boolean NOT NULL DEFAULT false` (D-11); optional `CHECK (parent_id IS NULL OR parent_id <> id)` self-parent guard. Add `departments_parent_id_idx` btree, a btree index on `org_id`, and the D-11 partial-unique index `CREATE UNIQUE INDEX IF NOT EXISTS <name> ON public.departments (org_id) WHERE (is_default = true)`.
    - `org_members` — `org_id uuid NOT NULL REFERENCES public.organizations(id) ON DELETE CASCADE`; `user_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE`; `role text NOT NULL CHECK (role = ANY (ARRAY['super-admin','org-admin','dept-admin','member']::text[]))` (D-03 fixed 4-tier via CHECK, NOT a native enum); `UNIQUE (org_id, user_id)`; btree indexes on org_id and user_id.
    - `dept_members` — `dept_id uuid NOT NULL REFERENCES public.departments(id) ON DELETE CASCADE`; a denormalized `org_id uuid NOT NULL REFERENCES public.organizations(id) ON DELETE CASCADE` (so the helper gates it without a join); `user_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE`; `role text NOT NULL CHECK (...)` (same 4-tier vocabulary); `UNIQUE (dept_id, user_id)`; btree indexes.
    - `roles` — GLOBAL reference data: `role text PRIMARY KEY` (the 4 fixed tiers) + `description text` + `created_at`. No org_id.
    - `role_permissions` — GLOBAL reference data: `role text NOT NULL REFERENCES public.roles(role)`; `permission_key text NOT NULL` (OPEN STRING, NO enum/FK — D-03 so 167/169 INSERT new keys with no schema change); composite `PRIMARY KEY (role, permission_key)` (analog user_memory_user_key_unique:1919). No org_id.
    - `org_invitations` — near-complete (D-06): `org_id` FK-cascade NOT NULL; `email text NOT NULL`; `role text NOT NULL CHECK (4-tier)`; `token_hash text NOT NULL` (hash, NEVER a raw token — T-161-04); `status text NOT NULL DEFAULT 'pending' CHECK (status = ANY (ARRAY['pending','accepted','expired','revoked']::text[]))`; `expires_at timestamptz NOT NULL`; `invited_by uuid` (nullable); `created_at`/`updated_at`; btree index on org_id.
    - `sso_configs` — deliberately THIN (D-07): `org_id` FK-cascade NOT NULL; `email_domain text`; a nullable pointer to the Supabase SSO provider id (e.g. `provider_id text` or `uuid`, NO FK into the Supabase-owned auth schema); `attribute_mapping jsonb NOT NULL DEFAULT '{}'`; `created_at`/`updated_at`; btree index on org_id. Invent NO SAML cert/metadata/XML columns — Supabase owns `auth.saml_providers`.

    Then author the 3 helpers with `CREATE OR REPLACE FUNCTION` (re-paste-safe), each `SECURITY DEFINER` + `SET search_path TO 'public'` (search-path pinning is mandatory — T-161-05):
    - `current_user_org_ids()` — `RETURNS setof uuid` (or `uuid[]`), `LANGUAGE sql STABLE SECURITY DEFINER`, body `SELECT org_id FROM public.org_members WHERE user_id = auth.uid()`. This is ORG-02's recursion-break: because it is SECDEF, its internal read of org_members bypasses org_members' RLS, so a policy that CALLS it never re-enters itself.
    - `current_user_has_permission(p_org_id uuid, p_permission_key text)` — `RETURNS boolean`, `LANGUAGE sql STABLE SECURITY DEFINER`, body an `EXISTS` over `org_members` joined to `role_permissions` on `role` where `org_id = p_org_id AND user_id = auth.uid() AND permission_key = p_permission_key`. This is the write-side role gate D-02 exists to enable ("so 164/166 have a real permission to check the day they land") — and it keeps the "no inlined org_members subquery in a policy" rule intact because the join lives inside a SECDEF body, not in a policy predicate.
    - `create_org_with_default_dept(...)` — `RETURNS uuid` (the new org id), `LANGUAGE plpgsql SECURITY DEFINER`, body inserts one `organizations` row + one `departments` row with `is_default = true` atomically. D-11 ships ONLY this schema support; do NOT wire any creation trigger here (the trigger-vs-app-layer seam is deferred to 162/167).

    Do NOT author any RLS policies or seed rows in this task — that is Task 2. Do NOT include fenced implementation you copied blindly; adapt the analog shapes named in read_first.
  </action>
  <acceptance_criteria>
    - `test -f supabase/migrations/104_org_dept_role_schema.sql` succeeds.
    - All 8 tables authored: `grep -c 'CREATE TABLE IF NOT EXISTS public\.\(organizations\|departments\|org_members\|dept_members\|roles\|role_permissions\|org_invitations\|sso_configs\)' 104_*.sql` returns 8.
    - `organizations` carries all three commercial/config columns distinctly: `grep` confirms `subscription_tier`, `add_ons jsonb`, AND `settings jsonb NOT NULL DEFAULT '{}'` all present (add_ons and settings are SEPARATE columns — D-01).
    - `departments` has `is_default boolean`, a `parent_id uuid REFERENCES public.departments`, and a partial-unique index whose definition contains `WHERE (is_default = true)`.
    - The 4-tier CHECK appears on org_members.role with exactly the values super-admin/org-admin/dept-admin/member; org_invitations.status CHECK has pending/accepted/expired/revoked.
    - 3 SECDEF helpers present, each with BOTH `SECURITY DEFINER` and `SET search_path TO 'public'`: `grep -c 'SECURITY DEFINER' 104_*.sql` >= 3 AND each of current_user_org_ids / current_user_has_permission / create_org_with_default_dept is followed within its definition by `SET search_path TO 'public'` (inspect).
    - `current_user_org_ids` body reads `org_members` filtered by `user_id = auth.uid()` (grep both tokens inside the function body).
    - Idempotency guards present: every table uses `CREATE TABLE IF NOT EXISTS`, every function uses `CREATE OR REPLACE FUNCTION`, indexes use `CREATE ... INDEX IF NOT EXISTS`.
    - `sso_configs` has NO SAML cert/xml/metadata column: `grep -i 'cert\|saml_xml\|idp_metadata\|x509' 104_*.sql` returns nothing inside the sso_configs block.
    - org_invitations stores `token_hash` and has NO plaintext `token text` column.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'CREATE TABLE IF NOT EXISTS public\.\(organizations\|departments\|org_members\|dept_members\|roles\|role_permissions\|org_invitations\|sso_configs\)' supabase/migrations/104_org_dept_role_schema.sql # expect 8; then confirm grep -c 'SECURITY DEFINER' >= 3 and each helper pins SET search_path TO 'public'</automated>
  </verify>
  <done>All 8 tables + the 3 SECDEF helpers (each SECURITY DEFINER + pinned search_path) are authored with idempotency guards; organizations carries subscription_tier + add_ons jsonb + a SEPARATE settings jsonb; departments has is_default + the partial-unique index; org_invitations stores token_hash; sso_configs is thin.</done>
</task>

<task type="auto" wave="1">
  <name>Task 2: Author membership-correct RLS on all 8 tables + seed the permission catalog</name>
  <files>supabase/migrations/104_org_dept_role_schema.sql</files>
  <read_first>
    - .planning/phases/161-org-dept-role-schema/161-PATTERNS.md — the org_members non-recursive policy section + the reference-data read-all section + the seed-idempotency section (exact policy shapes).
    - .planning/phases/161-org-dept-role-schema/161-CONTEXT.md — D-02, D-04, D-08, D-09, D-10 (the RLS + seed obligations; the LOCKED org_members policy).
    - supabase/full-schema.sql lines 3944 (model_overrides_read_all — the read-all reference-data policy), 3300/3433/3559/3645 (skills owned-table CRUD policy set — the shape to adapt), 4055-4084 (todos FK-through-parent policy shape).
    - supabase/migrations/095_operator_foundation.sql lines 57-64 (ENABLE ROW LEVEL SECURITY placement).
    - supabase/migrations/094_starter_workflows.sql lines 55-60 (INSERT ... ON CONFLICT DO NOTHING seed idiom).
  </read_first>
  <action>
    Append to `104_org_dept_role_schema.sql`, AFTER the tables + helpers from Task 1.

    (a) Enable RLS on all 8 tables: `ALTER TABLE public.<t> ENABLE ROW LEVEL SECURITY;` for each (095:62-63 shape).

    (b) Author membership-correct policies (D-08 — correct-from-birth; these 8 tables are EXCLUDED from Phase 163's rewrite per D-09). Use snake_case policy names. EVERY `CREATE POLICY` MUST be preceded by `DROP POLICY IF EXISTS <name> ON public.<t>;` (CREATE POLICY has no IF NOT EXISTS — re-paste safety). Table-specific shapes (D-10):
    - `org_members` — the LOCKED non-recursive pair: `org_members_self_select` = `FOR SELECT TO authenticated USING (user_id = auth.uid())` (self-rows-only, direct column compare, NO subquery — this is what breaks 42P17 / SC#2 / ORG-02); PLUS `org_members_admin_select` = `FOR SELECT TO authenticated USING (org_id IN (SELECT public.current_user_org_ids()))` for the org-admin read path (routes through the SECDEF helper, never an inline org_members subquery). Write policies gate via `current_user_has_permission(org_id, 'org:manage')` with a WITH CHECK on INSERT/UPDATE.
    - org-scoped tables (`organizations`, `departments`, `dept_members`, `org_invitations`, `sso_configs`) — READ policy `USING (org_id IN (SELECT public.current_user_org_ids()))` (member of the org). WRITE policies (INSERT/UPDATE/DELETE) gate on membership via the helper AND role via `current_user_has_permission(org_id, '<key>')`, and EVERY INSERT/UPDATE policy carries a `WITH CHECK` that pins `org_id IN (SELECT public.current_user_org_ids())` (T-161-02 — no cross-org write). Per-table write key: organizations→`org:manage`, departments→`org:manage`, dept_members→`org:manage`, org_invitations→`org:invite`, sso_configs→`sso:manage`. For `organizations` INSERT: author conservatively — creation runs through the `create_org_with_default_dept()` SECDEF helper (bypasses RLS); a user-JWT INSERT path is a Phase 167 concern, so no permissive INSERT policy here (note this seam in a comment).
    - `roles` + `role_permissions` — GLOBAL reference data (D-04): a single read-all policy each, byte-identical to model_overrides_read_all — `FOR SELECT TO authenticated USING (true)` — and NO INSERT/UPDATE/DELETE policy at all (writes denied to every JWT; only the migration/service-role seeds them). This is the T-161-03 privilege-escalation mitigation: an org-admin cannot grant themselves super-admin perms.

    (c) Seed the permission catalog (D-02/D-03), all with `ON CONFLICT DO NOTHING` (re-paste safe):
    - INSERT the 4 fixed `roles`: super-admin, org-admin, dept-admin, member.
    - INSERT the default `role_permissions` grants over the 5 milestone-known OPEN-STRING keys — `org:manage`, `org:audit_view`, `dept:manage`, `org:invite`, `sso:manage` — with defaults: super-admin → all 5; org-admin → the `org:`-prefixed keys (org:manage, org:audit_view, org:invite); dept-admin → the `dept:`-prefixed key (dept:manage); member → none. (168 may later additively INSERT an org-admin→sso:manage grant if it decides org-admins manage SSO — that is the whole point of open-string keys; do NOT seed it now.)

    Cite the D-IDs in inline comments so a downstream checker can trace intent.
  </action>
  <acceptance_criteria>
    - RLS enabled on all 8: `grep -c 'ENABLE ROW LEVEL SECURITY' 104_*.sql` >= 8.
    - The org_members self policy is self-rows-only: an `org_members_self_select` (or equivalently named) policy has `USING (user_id = auth.uid())` and NO occurrence of `FROM public.org_members` or an `org_members` subquery appears inside ANY org_members policy USING/WITH CHECK clause (inspect — this is the 42P17 contract).
    - No other table's policy inlines an org_members subquery: every membership predicate reads `current_user_org_ids()` — `grep -c 'current_user_org_ids' 104_*.sql` covers every org-scoped read policy.
    - Idempotency: count of `DROP POLICY IF EXISTS` == count of `CREATE POLICY` (every policy is drop-guarded).
    - Reference data is write-locked: `roles` and `role_permissions` each have exactly one `FOR SELECT ... USING (true)` policy and ZERO INSERT/UPDATE/DELETE policies (grep for any `ON public.roles`/`ON public.role_permissions` policy that is not SELECT → must be none).
    - Every write policy carries a guard: for each `FOR INSERT`/`FOR UPDATE` policy on the 5 org-scoped tables there is a corresponding `WITH CHECK` clause (inspect count).
    - Seed present + idempotent: 4 role rows + the tiered grants inserted with `ON CONFLICT ... DO NOTHING`; all 5 keys appear — `grep -c "org:manage\|org:audit_view\|dept:manage\|org:invite\|sso:manage" 104_*.sql` shows all five present.
    - The seeded grant matrix matches D-02 (super-admin→5, org-admin→3 org:* keys, dept-admin→1 dept:* key, member→0) — inspect the INSERT rows.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'ENABLE ROW LEVEL SECURITY' supabase/migrations/104_org_dept_role_schema.sql # expect >=8; then confirm DROP POLICY IF EXISTS count == CREATE POLICY count, and no 'FROM public.org_members' inside any org_members policy clause</automated>
  </verify>
  <done>RLS enabled on all 8 tables; the org_members SELECT policy is self-rows-only with no org_members subquery (42P17 contract); every other membership predicate calls current_user_org_ids(); roles/role_permissions are read-all + write-locked; the D-02 permission catalog is seeded idempotently with the 5 keys and the tiered grant matrix.</done>
</task>

<task type="auto" wave="1">
  <name>Task 3: Sweep nullable org_id + btree index onto the 23 remaining user-facing tables</name>
  <files>supabase/migrations/104_org_dept_role_schema.sql</files>
  <read_first>
    - supabase/migrations/096_org_id_stub_sweep.sql — the exact `ADD COLUMN IF NOT EXISTS org_id uuid` + forward-compat COMMENT precedent (lines 18-26); note 096 deliberately added NO index — THIS phase differs (adds a btree index per the CONTEXT Discretion).
    - .planning/phases/161-org-dept-role-schema/161-PATTERNS.md — the "org_id reconciliation" section (the authoritative 13-already-have list + the exclusion rules) + the idx_classification_rules_org_id btree shape (full-schema.sql:2103).
    - supabase/full-schema.sql — RE-VERIFY the sweep set live: search for `org_id uuid` to confirm which tables already have it (nothing should have landed since head 103, but the CONTEXT mandates re-verification against the live dump, not the stale research counts).
  </read_first>
  <action>
    Append to `104_org_dept_role_schema.sql`, after the RLS + seed. For EACH of the 23 derived sweep-target tables, add three statements (096 shape + a btree index): `ALTER TABLE public.<t> ADD COLUMN IF NOT EXISTS org_id uuid;`, `CREATE INDEX IF NOT EXISTS idx_<t>_org_id ON public.<t> USING btree (org_id);`, and a forward-compat `COMMENT ON COLUMN public.<t>.org_id IS 'Forward-compat (D-PRD-02/D-11): org-level multi-tenancy. NULL in v3.4; no FK until backfill/RLS (Phase 162/163).';`.

    The 23 sweep targets (derived from the live head-103 dump: 42 tables − 13 already carrying org_id − 4 system/identity − 2 Phase-163-deferred = 23). RE-VERIFY against the live schema before writing; the list is: audit_log, code_executions, document_images, document_tables, eval_ratings, eval_results, eval_runs, message_feedback, messages, pdf_extraction_runs, runs, sandbox_files, skill_files, skill_proposals, skill_publish_overrides, skill_test_cases, skill_versions, todos, tuner_runs, user_memory, user_settings, workspace_file_versions, workspace_files.

    HARD exclusions (do NOT stub these):
    - `document_chunks` + `skill_embeddings` — their `org_id` denormalize + composite index is Phase 163 / TEN-04 (the perf-gated crux). Out of scope here.
    - `app_settings`, `model_capabilities_overrides`, `operator_users`, `profiles` — system/identity, not user-facing (stubbing operator_users' org_id would poison the one-way door per 095:5-8).

    Every swept `org_id` stays NULLABLE with NO foreign key and NO NOT NULL constraint — existing behavior stays byte-identical (the FK + NOT NULL flip is Phase 162/163). Do NOT touch the 13 tables that already carry org_id. (Backfilling btree indexes on the 9 existing un-indexed org_id columns for uniformity is OPTIONAL executor discretion — default: skip, to keep scope tight; if you do it, note it in a comment.)
  </action>
  <acceptance_criteria>
    - Exactly 23 sweep ALTERs: `grep -c 'ADD COLUMN IF NOT EXISTS org_id uuid' 104_*.sql` returns 23 (matching the derived set — re-verify each name against the live dump).
    - Each swept table gets a btree index: count of `CREATE INDEX IF NOT EXISTS idx_.*_org_id .* USING btree (org_id)` == 23.
    - The 2 Phase-163-deferred tables are absent from the sweep: `grep 'ALTER TABLE public.document_chunks\|ALTER TABLE public.skill_embeddings' 104_*.sql` returns nothing.
    - The 4 system/identity tables are absent: `grep 'ALTER TABLE public.app_settings\|ALTER TABLE public.model_capabilities_overrides\|ALTER TABLE public.operator_users\|ALTER TABLE public.profiles' 104_*.sql` returns nothing.
    - No swept org_id carries a FK or NOT NULL: the sweep block contains no `REFERENCES` and no `SET NOT NULL` / `NOT NULL` on org_id (inspect — byte-identical guarantee).
    - Each sweep column has the forward-compat COMMENT (count of `COMMENT ON COLUMN public.<t>.org_id` == 23).
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'ADD COLUMN IF NOT EXISTS org_id uuid' supabase/migrations/104_org_dept_role_schema.sql # expect 23; then confirm zero matches for document_chunks/skill_embeddings/app_settings/model_capabilities_overrides/operator_users/profiles in the sweep block</automated>
  </verify>
  <done>Exactly 23 user-facing tables gain a nullable org_id (no FK, no NOT NULL) + a btree index + a forward-compat comment; the 2 Phase-163-deferred embedding/chunk tables and the 4 system/identity tables are excluded; existing behavior stays byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user JWT → org-scoped RLS policies | A logged-in user's `auth.uid()` crosses into org table reads/writes. Untrusted intent: a member of org A attempting to read or write org B rows. (No live traffic this phase — tables are empty until the 162 backfill + 163 client swap; policies must still be correct-from-birth per D-08.) |
| RLS policy → `org_members` self-reference | An `org_members` policy that reads `org_members` is the `42P17` infinite-recursion / lockout surface. |
| migration/service-role → `roles`/`role_permissions` | Only the migration (bypasses RLS) may write the permission catalog; a user JWT must never write role tiers or grants. |
| SECURITY DEFINER function body → `search_path` | A SECDEF function without a pinned search_path is hijackable (attacker-controlled schema resolution). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-161-01 | Denial of Service / Tampering | `org_members` RLS policy (`42P17` recursion) | mitigate | **HIGH.** D-10 self-rows-only policy `USING (user_id = auth.uid())` — direct column compare, NO org_members subquery; the org-admin read path + all other tables route membership checks through the `current_user_org_ids()` SECDEF helper (its internal org_members read bypasses RLS → no re-entry). Verified live in Plan 02 (a `SELECT` on org_members must NOT raise `42P17`). |
| T-161-02 | Elevation / Information Disclosure (cross-org) | The 8 new-table read + write policies | mitigate | **HIGH.** Read = `org_id IN (SELECT public.current_user_org_ids())`; EVERY INSERT/UPDATE policy carries a `WITH CHECK` pinning `org_id` to the caller's orgs (no cross-org write) + a role gate via `current_user_has_permission()`. Full two-org proof is Phase 164/TEN-05; policies authored HERE are correct-from-birth. |
| T-161-03 | Elevation of Privilege | `roles` / `role_permissions` | mitigate | **HIGH.** Global reference data: read-all `USING (true)` + ZERO write policy → INSERT/UPDATE/DELETE denied to every JWT; migration-only seeding (D-04). An org-admin cannot grant themselves super-admin perms. |
| T-161-05 | Tampering (search-path hijack) | `current_user_org_ids` / `current_user_has_permission` / `create_org_with_default_dept` | mitigate | **HIGH.** `SET search_path TO 'public'` pinned on all three SECDEF bodies (folder_is_globally_visible precedent). Phase 164's SECDEF audit (TEN-03) will exercise these. |
| T-161-04 | Information Disclosure | `org_invitations` | mitigate | **MEDIUM.** Stores `token_hash`, never a raw token (issuance/redemption is Phase 167). No plaintext token column exists. |
| T-161-06 | (zero-behavior-change safety) | 23 swept tables + Deep/agent-loop/retrieval path | accept | **LOW — mitigated by construction.** Additive-only: new empty tables + nullable `org_id` (no FK, no NOT NULL); no existing query path changes; the user-JWT client swap is Phase 163. Deep Mode stays byte-identical. Honors the ADR SC#4 clause — no deployment tier is made harder (empty tables + nullable columns; `settings jsonb` keeps SEED-120 a no-schema-rewrite v3.5 add). |
| T-161-SC | Tampering (supply chain) | package installs | accept | **LOW.** No npm/pip/cargo installs this phase — a pure SQL migration. No new dependency / supply-chain surface. |

All `high` threats carry a concrete in-plan mitigation (block-on-high satisfied). No threat is left `open`.
</threat_model>

<verification>
- The migration file `supabase/migrations/104_org_dept_role_schema.sql` exists and is internally ordered (tables → helpers → RLS → seed → sweep) so a single top-to-bottom paste succeeds.
- Static/structural acceptance criteria on all three tasks pass (object presence, the 42P17 self-rows-only contract, reference-data write-lock, idempotency guards, the 23-table sweep set + exclusions).
- The file is NOT applied in this plan — application + live verification (to_regclass, pg_proc.prosecdef, the live 42P17 test, seed counts, swept columns) is Plan 02 (the BLOCKING apply/verify plan).
</verification>

<success_criteria>
- One authored, re-paste-safe, additive migration delivering: the 8 org tables (ORG-01 / SC#1) with RLS from creation; the `current_user_org_ids()` recursion-break helper + the non-recursive org_members policy (ORG-02 / SC#2); the default-department construction guarantee (SC#4 / D-11); the seeded core permission catalog (D-02/D-03/D-04); and the nullable `org_id` sweep on the 23 remaining user-facing tables (SC#3).
- Every D-01…D-11 obligation is encoded and traceable to a table/column/policy/seed/index in the file.
- Zero behavior change on creation (T-161-06) — no deployment tier made harder (ADR SC#4).
</success_criteria>

<output>
Create `.planning/phases/161-org-dept-role-schema/161-01-SUMMARY.md` when done — record the final migration filename, the exact 23-table sweep set as written (with any live-schema reconciliation notes), the 3 helper signatures, the policy names, and the seeded grant matrix, so Plan 02 (apply/verify) and Phase 162/163 can reference them.
</output>
