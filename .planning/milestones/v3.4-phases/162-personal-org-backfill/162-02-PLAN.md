---
phase: 162-personal-org-backfill
plan: 02
type: execute
wave: 2
depends_on: ["162-01"]
files_modified:
  - supabase/migrations/105_personal_org_backfill.sql
  - supabase/full-schema.sql
  - .planning/phases/162-personal-org-backfill/162-BASELINE.md
autonomous: false
requirements: [MIG-01]

must_haves:
  truths:
    - "MIG-01 / SC#1 / D-01: after apply, count(organizations) == count(auth.users) and every user has exactly one org-admin membership + one default dept (0 duplicate-membership rows, 0 multi-default orgs)"
    - "MIG-01 / SC#2 / D-08 / D-11: org_id is non-NULL across all 35 targets; the only org_id columns still nullable are operator_audit_log (+ metadata_field_definitions iff the self-guard kept it nullable)"
    - "MIG-01 / SC#3 / D-04: per-target row counts are invariant before vs after and the is_global/is_system census is unchanged (folders=1, skills global=1, skills system=1, workflow_definitions=15) — nothing disappeared"
    - "MIG-01 / SC#4 / D-06: re-pasting migration 105 leaves org/member counts identical (idempotent, no lock-storm) and full-schema.sql is regenerated (no --reset) + committed SAME-COMMIT with 105"
  artifacts:
    - path: "supabase/full-schema.sql"
      provides: "The regenerated single-file bootstrap artifact reflecting the applied 105 (extended handle_new_user + backfilled/NOT-NULL org_id)"
      contains: "handle_new_user"
    - path: ".planning/phases/162-personal-org-backfill/162-BASELINE.md"
      provides: "The pre-apply per-target row-count + is_global/is_system census — the machine-parseable SC#3 before-image"
      contains: "org_id"
  key_links:
    - from: "supabase/full-schema.sql"
      to: "public.handle_new_user"
      via: "regenerated definer body after apply"
      pattern: "EXCEPTION WHEN OTHERS"
    - from: "migration 105 (applied)"
      to: "public.organizations / public.org_members"
      via: "personal-org provisioning populates the empty org tables"
      pattern: "organizations"
---

<objective>
Apply migration **105_personal_org_backfill.sql** (authored in Plan 01) to the LOCAL Supabase DB, prove it against the SC#1–4 verification-query pack, prove idempotency by re-pasting, regenerate `supabase/full-schema.sql`, and commit the migration + regenerated schema (+ the baseline snapshot) in ONE commit per D-06.

Purpose: the phase is NOT verifiable by file authoring alone — there are no build/type checks for a pure-SQL migration. Only an applied migration whose SC#1–4 pack passes against live data proves MIG-01. This plan turns the authored file into applied, verified, committed state.

Output: applied local DB (personal orgs + backfilled/NOT-NULL org_id), `162-BASELINE.md` (machine-parseable SC#3 before-image), regenerated `full-schema.sql`, and one same-commit. **BLOCKING / operator-gated** — the apply is a live-DB mutation. **Cloud is NOT touched**: 105 joins the pending-cloud set (099–104 + `SECRETS_ENCRYPTION_KEY`) applied only at the next operator-gated production push.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-personal-org-backfill/162-CONTEXT.md
@.planning/phases/162-personal-org-backfill/162-RESEARCH.md
@.planning/phases/162-personal-org-backfill/162-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Apply mechanics + the SC#1–4 pack, live-verified at plan time. -->

Apply path (RESEARCH §Batched Backfill + Pitfall 1 — the #1 apply trap):
  -- The batched COMMIT is ILLEGAL inside a wrapping transaction ("invalid transaction termination").
  -- REQUIRED: a NON-ATOMIC context. Two safe paths:
  --   (A) psycopg2 autocommit (proven for mig 104/161):
  --       backend/venv/Scripts/python.exe with psycopg2.connect(host='127.0.0.1', port=54322,
  --       dbname='postgres', user='postgres', password='postgres'); conn.autocommit = True;
  --       then execute the whole 105 file (or run each CALL as its own execute()).
  --   (B) Supabase SQL editor: run each `CALL public._mig105_backfill(...)` as a SEPARATE execution
  --       (highlight + run), not the whole file as one multi-statement paste.
  -- Do NOT `supabase db push` / `db reset` (CLAUDE.md). Local only.

Baseline format (Task 1 writes it; Task 2's SC#3 automated check parses it):
  -- 162-BASELINE.md MUST contain a machine-parseable markdown table with pipe-delimited rows
  --   `| <key> | <count> |`, one row per key:
  --     * the 35 target table names -> each table's count(*)   (SC#3 row-count invariance)
  --     * folders_is_global, skills_is_global, skills_is_system, workflow_definitions_is_global
  --       -> the is_global/is_system census                    (SC#3 census invariance)
  --   The SC#3 check in Task 2 splits each line on '|', keeps rows whose 2nd field is a known key
  --   and 3rd field is an integer, and compares live values to these captured baselines.

The SC#1–4 verification pack (paste after apply; VALIDATION.md + RESEARCH §Validation Architecture):
  -- SC#1(a) coverage:
  SELECT (SELECT count(*) FROM auth.users)                         AS users,
         (SELECT count(*) FROM public.organizations)               AS orgs,
         (SELECT count(*) FROM public.org_members WHERE role='org-admin') AS admin_members;
         -- expect: orgs == users (8 local); admin_members >= users
  -- SC#1(b) no duplicate orgs per user:
  SELECT user_id, count(*) FROM public.org_members GROUP BY user_id HAVING count(*) <> 1;  -- 0 rows
  -- SC#1(c) exactly one default dept per org:
  SELECT org_id FROM public.departments GROUP BY org_id
    HAVING count(*) FILTER (WHERE is_default) <> 1;                                         -- 0 rows
  -- SC#2 nullable set (post-flip):
  SELECT table_name FROM information_schema.columns
    WHERE table_schema='public' AND column_name='org_id' AND is_nullable='YES';
    -- expect ONLY: operator_audit_log  (+ metadata_field_definitions iff kept nullable)
  -- SC#3(b) is_global/is_system census (must equal the baseline captured in Task 1):
  SELECT 'folders' t, count(*) FILTER (WHERE is_global) g FROM public.folders
    UNION ALL SELECT 'skills', count(*) FILTER (WHERE is_global) FROM public.skills
    UNION ALL SELECT 'skills_sys', count(*) FILTER (WHERE is_system) FROM public.skills
    UNION ALL SELECT 'workflow_definitions', count(*) FILTER (WHERE is_global) FROM public.workflow_definitions;
    -- expect (== baseline): folders=1, skills=1, skills_sys=1, workflow_definitions=15
  -- SC#4(a) idempotency: re-run the whole 105 apply, then re-run SC#1(a)/(b) -> counts identical.
  -- SC#4(b) batching: the procedure's `RAISE NOTICE 'batch N -> M rows'` shows M <= 10000 + COMMIT between.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the pre-apply baseline snapshot (machine-parseable SC#3 before-image)</name>
  <files>.planning/phases/162-personal-org-backfill/162-BASELINE.md</files>
  <read_first>
    - .planning/phases/162-personal-org-backfill/162-VALIDATION.md — §Wave 0 Requirements (baseline is the one hard Wave-0 prerequisite) + §Sampling Rate
    - .planning/phases/162-personal-org-backfill/162-RESEARCH.md — §Validation Architecture SC#3 (row-count invariance + is_global census)
    - supabase/migrations/105_personal_org_backfill.sql — to enumerate the exact 35 targets to snapshot
    - The `<interfaces>` "Baseline format" note above (the EXACT parseable shape Task 2 depends on)
  </read_first>
  <action>
    BEFORE applying anything, snapshot the CURRENT live DB via psycopg2 (`autocommit=True` @ 127.0.0.1:54322) and write `.planning/phases/162-personal-org-backfill/162-BASELINE.md`. It MUST contain a machine-parseable markdown table of `| <key> | <count> |` rows (this exact format is what Task 2's SC#3 check parses — see the `<interfaces>` baseline-format note):
      1. one row per each of the 35 backfill targets -> that table's `count(*)` (SC#3 row-count invariance baseline);
      2. four census rows keyed EXACTLY `folders_is_global`, `skills_is_global`, `skills_is_system`, `workflow_definitions_is_global` -> the corresponding `count(*) FILTER (WHERE is_global/is_system)` (expect 1 / 1 / 1 / 15) (SC#3 census invariance baseline).
    Also include, for human context (not parsed), the org-table census (`organizations`, `org_members`, `departments` — expect 0 / 0 / 0 pristine pre-apply) and `count(auth.users)` (expect 8). This is a READ-ONLY snapshot — it MUST run before Task 2's apply so the "nothing disappeared" diff has a before-image. Do not apply the migration in this task.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && f=.planning/phases/162-personal-org-backfill/162-BASELINE.md && test -f $f && grep -q "folders_is_global" $f && grep -q "workflow_definitions_is_global" $f && for t in documents folders threads messages runs audit_log harness_audit todos workflow_phases workflow_runs workspace_file_versions metadata_field_definitions; do grep -q "$t" $f || { echo "MISSING baseline $t"; exit 1; }; done && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `162-BASELINE.md` exists and contains a `| <key> | <count> |` row for each of the 35 backfill targets.
    - The four census keys are present with values: `folders_is_global`=1, `skills_is_global`=1, `skills_is_system`=1, `workflow_definitions_is_global`=15.
    - The org-table census is recorded as pristine (organizations=0, org_members=0, departments=0) with auth.users=8.
    - No migration was applied in this task (DB still pristine — organizations still 0).
  </acceptance_criteria>
  <done>The machine-parseable pre-apply before-image is captured and committable; Task 2's SC#3 check can diff live counts against it after apply.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Apply migration 105 (non-atomic) + run the SC#1–4 verification pack + prove idempotency</name>
  <files>supabase/migrations/105_personal_org_backfill.sql</files>
  <read_first>
    - .planning/phases/162-personal-org-backfill/162-RESEARCH.md — §Batched Backfill (Pitfall 1: "invalid transaction termination"; the psycopg2-autocommit apply path), §Validation Architecture (the SC#1–4 pack)
    - .planning/phases/162-personal-org-backfill/162-VALIDATION.md — the SC -> verification-query map
    - supabase/migrations/105_personal_org_backfill.sql — the file to apply
    - CLAUDE.md — apply via SQL editor / psycopg2, NEVER db push / db reset
  </read_first>
  <action>
    What was built (Plan 01): migration 105 = personal-org provisioning loop + defensive `handle_new_user` trigger + the batched `_mig105_backfill` procedure + 35 resolver CALLs + 35 self-guarded NOT-NULL flips (operator_audit_log excluded/nullable). This BLOCKING task applies + proves it; the apply is an operator-gated live-DB mutation.

    1. Apply 105 in a NON-ATOMIC context (Pitfall 1 — the batched COMMIT fails with "invalid transaction termination" if pasted as one wrapping transaction). Preferred (automation-first, proven mig-104/161 path): Claude runs `backend/venv/Scripts/python.exe` with psycopg2 `conn.autocommit = True` @ 127.0.0.1:54322 and executes the whole 105 file. Alternative: the operator pastes into the Supabase SQL editor and runs each `CALL` as a SEPARATE execution. Watch the `RAISE NOTICE 'batch N -> M rows'` lines (SC#4b — M <= 10000 with a COMMIT between batches). NEVER `db push` / `db reset`; LOCAL only.
    2. Run the consolidated `<verify>` command below — it machine-proves SC#1 (orgs==users, members>=users, no duplicate memberships, one default dept), SC#2 (only operator_audit_log — and possibly metadata_field_definitions — remains org_id-nullable), and SC#3 (all 35 target row-counts + the 4 is_global/is_system census values equal `162-BASELINE.md`) and prints one consolidated `PASS`/`FAIL`. Also eyeball the raw SC#1–4 pack in `<interfaces>` for the operator record.
    3. Prove SC#4(a): RE-APPLY the entire 105 file (second autocommit run / re-paste), then re-run the consolidated check — it MUST still print `PASS` (organizations / org_members counts identical; no duplicate orgs, no lock-storm).
    4. If the `metadata_field_definitions` guard raises (only possible with cloud-scale NULL-owner rows — cannot happen locally), STOP and decide per D-11 (keep nullable or assign an org) — do NOT force it.
    Then pause for operator sign-off before Task 3 commits.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && ./venv/Scripts/python.exe -c "import psycopg2,os; P=r'C:/Vibe Apps/Agentic RAG/.planning/phases/162-personal-org-backfill/162-BASELINE.md'; T=['audit_log','classification_rules','code_executions','document_images','document_relationships','document_tables','document_views','documents','eval_ratings','eval_results','eval_runs','folders','harness_audit','message_feedback','messages','metadata_field_definitions','pdf_extraction_runs','runs','sandbox_files','skill_files','skill_proposals','skill_publish_overrides','skill_test_cases','skill_versions','skills','threads','tuner_runs','user_memory','user_settings','workflow_definitions','workspace_files','workflow_runs','todos','workspace_file_versions','workflow_phases']; C={'folders_is_global':'select count(*) filter (where is_global) from public.folders','skills_is_global':'select count(*) filter (where is_global) from public.skills','skills_is_system':'select count(*) filter (where is_system) from public.skills','workflow_definitions_is_global':'select count(*) filter (where is_global) from public.workflow_definitions'}; k=psycopg2.connect(host='127.0.0.1',port=54322,dbname='postgres',user='postgres',password='postgres'); k.autocommit=True; d=k.cursor(); g=lambda s:(d.execute(s),d.fetchone()[0])[1]; u=g('select count(*) from auth.users'); o=g('select count(*) from public.organizations'); m=g('select count(*) from public.org_members'); dup=g('select count(*) from (select user_id from public.org_members group by user_id having count(*)<>1) x'); md=g('select count(*) from (select org_id from public.departments group by org_id having count(*) filter (where is_default)<>1) y'); sc1=(o==u and m>=u and dup==0 and md==0); d.execute(\"select table_name from information_schema.columns where table_schema='public' and column_name='org_id' and is_nullable='YES'\"); nul=sorted(r[0] for r in d.fetchall()); sc2=('operator_audit_log' in nul and set(nul)<=set(['operator_audit_log','metadata_field_definitions'])); base={p[1].strip():int(p[2].strip()) for p in (ln.split('|') for ln in (open(P,encoding='utf-8') if os.path.exists(P) else [])) if len(p)>=4 and p[2].strip().isdigit() and (p[1].strip() in T or p[1].strip() in C)}; sc3r=all(t in base and g('select count(*) from public.'+t)==base[t] for t in T); sc3c=all(kk in base and g(C[kk])==base[kk] for kk in C); print('SC1',sc1,'| SC2',sc2,'nullable=',nul,'| SC3rows',sc3r,'| SC3census',sc3c,'| base=%d/39'%len(base)); print('PASS' if (sc1 and sc2 and sc3r and sc3c) else 'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - The consolidated automated check prints `PASS`, machine-proving: SC#1 (orgs==users=8, members>=users, dupmembers=0, multidefault=0); SC#2 (nullable org_id set ⊆ {operator_audit_log, metadata_field_definitions} AND operator_audit_log ∈ it — NO other table nullable); SC#3 (all 35 target row-counts AND the 4 is_global/is_system census values equal `162-BASELINE.md`; base parsed = 39/39).
    - SC#4(a): a full re-apply leaves the consolidated check at `PASS` (org/member counts unchanged); SC#4(b) batch NOTICEs observed with M<=10000 + per-batch COMMIT.
    - Applied LOCAL only; no `db push`/`db reset` used; cloud untouched.
  </acceptance_criteria>
  <done>Migration 105 applied non-atomically; the consolidated SC#1+SC#2+SC#3 check prints PASS; a re-apply is idempotent (still PASS — no duplicate orgs, no lock-storm); operator signed off.</done>
  <resume-signal>Type "approved" if the consolidated check prints PASS (SC#1+SC#2+SC#3) and the re-apply is still PASS (SC#4), or describe which assertion failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Regenerate full-schema.sql + commit migration + schema + baseline SAME-COMMIT</name>
  <files>supabase/full-schema.sql, supabase/migrations/105_personal_org_backfill.sql, .planning/phases/162-personal-org-backfill/162-BASELINE.md</files>
  <read_first>
    - CLAUDE.md — the numbered-migration + same-commit discipline (regenerate full-schema.sql with NO --reset; commit migration AND regenerated schema together; never hand-edit full-schema.sql)
    - .planning/phases/162-personal-org-backfill/162-RESEARCH.md — §Runtime State Inventory (full-schema.sql is the one artifact to regenerate; 162 is NOT seed-bearing)
  </read_first>
  <action>
    Run `bash scripts/regenerate-full-schema.sh` (NO `--reset` — a live-DB dump that preserves dev data) to rebuild `supabase/full-schema.sql` from the applied local DB. Then stage and commit, in ONE commit (D-06 same-commit): `supabase/migrations/105_personal_org_backfill.sql`, `supabase/full-schema.sql`, and `.planning/phases/162-personal-org-backfill/162-BASELINE.md`. Commit message notes: MIG-01 personal-org backfill applied local; 105 joins the pending-cloud set (099–104 + `SECRETS_ENCRYPTION_KEY`) — apply to cloud ONLY at the next operator-gated production push (do NOT touch cloud now). 162 is NOT seed-bearing (it writes user-owned rows, no reference data) -> no `docs/OPERATOR.md` Step-3 seed-list change and `scripts/check-deploy-drift.sh` needs no new entry. Do NOT run `supabase db push` / `db reset`.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && grep -q "handle_new_user" supabase/full-schema.sql && grep -q "EXCEPTION WHEN OTHERS" supabase/full-schema.sql && git show --stat HEAD | grep -q "105_personal_org_backfill.sql" && git show --stat HEAD | grep -q "full-schema.sql" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/full-schema.sql` is regenerated and its `handle_new_user` body now contains the `EXCEPTION WHEN OTHERS` defensive block (proves the applied trigger extension is captured).
    - `git show --stat HEAD` lists `105_personal_org_backfill.sql` AND `supabase/full-schema.sql` (AND `162-BASELINE.md`) in ONE commit (D-06 same-commit).
    - No `db push` / `db reset` was invoked; cloud was not touched; the commit body records that 105 joins the pending-cloud set.
  </acceptance_criteria>
  <done>full-schema.sql regenerated from the applied DB; migration + schema + baseline committed same-commit; cloud parity deferred to the next operator-gated push.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Apply session (autocommit / SQL-editor) -> live DB | A live-DB mutation gated on the operator (BLOCKING). Correctness is proven by the SC#1–4 pack, not by RLS (still service-role-bypassed until 163). |
| Wrapping-transaction vs non-atomic apply | The batched `COMMIT` is only legal in a non-atomic context — the apply MUST NOT run inside a single wrapping transaction (Pitfall 1). |
| Local DB vs cloud | 105 is applied LOCAL only; cloud parity is a separate operator-gated push. No cloud credential is touched in this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-07 | Availability | Apply inside a wrapping transaction | mitigate | Apply via psycopg2 `autocommit=True` OR per-`CALL` SQL-editor execution (Task 2) — the batched `COMMIT` fails loud ("invalid transaction termination") if misapplied, which is safe; the mitigation is the non-atomic apply path. |
| T-162-01 | Tampering / integrity | Idempotent re-run at apply time | mitigate | SC#4(a) re-applies the whole file and the consolidated check re-asserts org/member counts unchanged (the SQL's NOT-EXISTS gate + ON CONFLICT hold in practice, verified live) (Task 2). |
| T-162-02 | Denial of Service | Lock storm at cloud scale | accept (reason-only) | The bounded ~10k batch + per-batch COMMIT releases the lock window by construction; the local DB (largest target 5080 rows) completes in one batch and cannot exercise the window. Argued by construction + the live PG17.6 COMMIT-releases-lock proof (RESEARCH). |
| T-162-03 | Availability / integrity | NOT-NULL flip on dirty data | mitigate | The in-file RAISE-guard makes a dirty flip structurally impossible; the consolidated SC#2 nullable-set check confirms only operator_audit_log stays nullable (Task 2). |
| T-162-SC | Supply chain | package installs | accept (N/A) | Zero package installs in this phase — no legitimacy gate applies. |

Block-on severity = HIGH: no HIGH threat is left unmitigated. T-162-02 is a MEDIUM `accept` (reason-only, unobservable locally, safe by construction), explicitly justified.
</threat_model>

<verification>
The phase's SC#1–4 acceptance is the Task-2 consolidated automated check (SC#1+SC#2+SC#3), run against the applied DB and again after a re-paste (SC#4). Task 3's automated gate confirms the regenerated `full-schema.sql` captured the extended trigger and that the migration + schema landed in one commit. Nothing here touches Deep Mode / the agent loop / retrieval — RLS stays service-role-bypassed until 163 (D-14 byte-identical satisfied by construction).
</verification>

<success_criteria>
- Migration 105 applied to the local DB in a non-atomic context; the consolidated SC#1+SC#2+SC#3 check prints PASS.
- Re-applying 105 is idempotent (SC#4a — consolidated check still PASS; SC#4b — batched NOTICEs with per-batch COMMIT).
- Only `operator_audit_log` (+ `metadata_field_definitions` iff kept nullable) remains org_id-nullable; all 35 targets are NOT NULL; is_global/is_system census unchanged vs `162-BASELINE.md` (SC#2/SC#3).
- `supabase/full-schema.sql` regenerated (no --reset) and committed SAME-COMMIT with `105_personal_org_backfill.sql` + `162-BASELINE.md`; cloud NOT touched (105 joins the pending-cloud set).
</success_criteria>

<output>
Create `.planning/phases/162-personal-org-backfill/162-02-SUMMARY.md` when done. Record: the applied-DB SC#1–4 results (users/orgs/admin_members counts; the final nullable-org_id set; the SC#3 before/after diff = zero delta), which apply path was used (psycopg2 autocommit vs SQL editor), the `metadata_field_definitions` flip outcome, the same-commit SHA, and an explicit reminder that migration 105 is owed on cloud at the next operator-gated production push (099–104 + SECRETS_ENCRYPTION_KEY + 105).
</output>
