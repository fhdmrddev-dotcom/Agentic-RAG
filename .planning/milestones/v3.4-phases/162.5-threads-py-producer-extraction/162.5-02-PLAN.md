---
phase: 162.5-threads-py-producer-extraction
plan: 02
type: execute
wave: 2
depends_on: [162.5-01]
files_modified:
  - backend/app/api/threads.py
  - backend/app/services/workflow_kickoff.py
autonomous: true
requirements: []          # G-5 refactor — no own REQ-ID (enables TEN-02). Intentionally empty.

must_haves:
  truths:
    - "D-A2: workflow_kickoff.py owns the kickoff-preflight (skill-snapshot gate + published-definition resolve + folder-scope assert + template pin) AND the harness run-context/scope build; both blocks leave threads.py behavior-identical."
    - "D-A4: additive-then-repoint with late imports — `_ensure_skill_snapshots` is re-imported into threads.py so `app.api.threads._ensure_skill_snapshots` still resolves for test_099; the fail-closed HTTPException 400/500 shapes and the WR-03 bound-workflow fail-closed-to-avoid-whole-KB behavior are preserved verbatim."
    - "Deep-Mode is untouched (D-A6): a plain Deep send (workflow_definition_id is None, no active anchor) never enters either extracted block — the harness-only kickoff/ctx code moves without touching the Deep path; threads.py shrinks."
  artifacts:
    - path: "backend/app/services/workflow_kickoff.py"
      provides: "Workflow kickoff preflight + harness run-context build + _ensure_skill_snapshots"
      contains: "def preflight_workflow_kickoff"
    - path: "backend/app/api/threads.py"
      provides: "send_message preflight + agent_runner harness branch repointed; _ensure_skill_snapshots re-import"
      contains: "from app.services.workflow_kickoff import"
  key_links:
    - from: "backend/app/api/threads.py"
      to: "backend/app/services/workflow_kickoff.py"
      via: "preflight_workflow_kickoff(...) in send_message"
      pattern: "preflight_workflow_kickoff"
    - from: "backend/app/api/threads.py"
      to: "backend/app/services/workflow_kickoff.py"
      via: "build_harness_run_context(...) inside agent_runner harness branch"
      pattern: "build_harness_run_context"
---

<objective>
Extract the workflow-kickoff machinery out of `backend/app/api/threads.py` into a new
`workflow_kickoff.py` service module, behavior-preserving:

1. **Kickoff preflight** — `_ensure_skill_snapshots` plus the `send_message` preflight block
   (workflows kill-switch check, published-definition RLS resolve/parse, `assert_folder_scopes_subset`,
   skill-snapshot materialize, `pin_templates_for_run`).
2. **Harness run-context build** — the harness ctx/scope block currently inside `agent_runner`
   (thread-folder / project-subtree scope resolution, the WR-03 bound-workflow fail-closed
   guard, and the `SimpleNamespace` `wf_ctx` assembly consumed by `run_workflow`).

Purpose: land the harness-only surfaces before the producer heart moves in Plan 03, so Plan 03's
producer extraction is smaller and calls an already-extracted kickoff seam (D-A5 sequencing;
D-A4 discipline). Deep Mode is not on either code path.

Output: `backend/app/services/workflow_kickoff.py`, `send_message` + the `agent_runner` harness
branch repointed, and the full suite (incl. the harness/dual-mode contracts) green.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/162.5-threads-py-producer-extraction/162.5-CONTEXT.md
@.planning/phases/163-rls-rewrite-per-request-user-jwt-client-swap-the-atomic-crux/163-PATTERNS.md
@backend/app/services/run_lifecycle.py

<interfaces>
<!-- RE-VERIFY every line anchor live — threads.py drifts as plans land. -->

KICKOFF PREFLIGHT sources in backend/app/api/threads.py:
  - `async def _ensure_skill_snapshots(*, definition, run_id, supabase, user_id, definition_id=None, skill_snapshots=None)`  (~:956-1015)
      delegates to `_skill_snapshot.*`; ValueError -> HTTPException 400; unexpected -> HTTPException 500.
  - the active-anchor 409 lock check inside send_message                (~:1036-1069)
  - the `if body.workflow_definition_id is not None:` preflight block    (~:1071-1166)
      (workflows_enabled() 403; RLS-scoped definition resolve `.or_(is_global.eq.true,created_by.eq.{uid})`
       -> 404/400; WorkflowDefinition.model_validate; skill_snapshots load; assert_folder_scopes_subset
       -> 400; _ensure_skill_snapshots; template_service.pin_templates_for_run)

HARNESS RUN-CONTEXT source (currently INSIDE the agent_runner closure, harness branch):
  - the `if _active_workflow_run_id is not None:` block                  (~:1502-1657)
      (lazy imports run_workflow/_load_run_definition/_harness_emit/resolve_workflow_ctx_model;
       resolve_run_scope_root -> resolve_project_subtree -> fetch_visible_folders path walk;
       WR-03 bound-workflow fail-closed emit + RuntimeError; SimpleNamespace wf_ctx assembly)
      The `await run_workflow(_active_workflow_run_id, _wf_definition, wf_ctx, ..., stream_run_id=run_id)`
      call at ~:1658-1667 STAYS in the producer (Plan 03 owns it) — only the ctx/scope BUILD moves.

MUST stay patchable at app.api.threads.*:
  - workflows_enabled            (patched by test_147_workflows_flag.py as app.api.threads.workflows_enabled)
  - _ensure_skill_snapshots      (imported by test_099_skill_composition.py from app.api.threads)
Callers to keep green: test_dual_mode_wiring.py (harness kickoff mode-branch), test_098_scope_governance.py,
test_099_skill_composition.py, test_threads_skills.py, test_152_folder_override.py, test_141_run_scope.py.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the kickoff-preflight into workflow_kickoff.py</name>
  <files>backend/app/services/workflow_kickoff.py, backend/app/api/threads.py</files>
  <read_first>
    - backend/app/api/threads.py — `_ensure_skill_snapshots` `~:956-1015`, the anchor-lock 409 check `~:1036-1069`, and the `body.workflow_definition_id is not None` preflight block `~:1071-1166` (RE-VERIFY anchors).
    - backend/app/services/run_lifecycle.py — the late-import discipline (`from app.api.threads import ...` inside functions).
  </read_first>
  <action>
    Create `backend/app/services/workflow_kickoff.py` with a docstring naming the Phase-162.5
    G-5 kickoff extraction (behavior-preserving; fail-closed-before-user-message-insert ordering
    unchanged). Move `_ensure_skill_snapshots` verbatim (bring `_skill_snapshot`, `HTTPException`,
    `status`, `logger`).

    Add `async def preflight_workflow_kickoff(*, body, thread_id, thread_row, supabase,
    current_user)` returning `(kickoff_definition, kickoff_definition_id)`. It holds the anchor
    409 lock check (`~:1036-1069`) AND the `body.workflow_definition_id is not None` block
    (`~:1071-1166`): `workflows_enabled()` 403, the RLS-scoped `.or_(is_global.eq.true,
    created_by.eq.{uid})` definition resolve + 404/400 guards, `WorkflowDefinition.model_validate`,
    the `skill_snapshots` load, `assert_folder_scopes_subset` -> 400, `_ensure_skill_snapshots`,
    and `pin_templates_for_run`. Preserve EVERY `HTTPException` status/detail verbatim and the
    fail-closed ordering. For a plain Deep send (no anchor, `workflow_definition_id is None`)
    it returns `(None, None)` after the lock check — byte-identical no-op.

    In `threads.py`: DELETE the moved `_ensure_skill_snapshots` + the two inline blocks; add
    `from app.services.workflow_kickoff import _ensure_skill_snapshots, preflight_workflow_kickoff`
    at module scope (re-import keeps `app.api.threads._ensure_skill_snapshots` resolvable for
    test_099). Replace the inline blocks with `_kickoff_definition, _kickoff_definition_id =
    await preflight_workflow_kickoff(body=body, thread_id=thread_id, thread_row=thread_resp.data,
    supabase=supabase, current_user=current_user)`. If `workflows_enabled` must stay patchable at
    `app.api.threads.workflows_enabled`, have `preflight_workflow_kickoff` late-import it from
    `app.api.threads` (D-A4 late-import) OR retarget test_147_workflows_flag.py's patch target to
    `app.services.workflow_kickoff.workflows_enabled` (Phase-089 Pitfall-1 precedent) — verify the
    chosen path keeps test_147 green.
  </action>
  <acceptance_criteria>
    - `grep -c "def preflight_workflow_kickoff" backend/app/services/workflow_kickoff.py` returns 1; `grep -c "async def _ensure_skill_snapshots" backend/app/services/workflow_kickoff.py` returns 1.
    - `grep -c "from app.services.workflow_kickoff import" backend/app/api/threads.py` returns 1.
    - `cd backend && python -m pytest tests/test_099_skill_composition.py tests/test_147_workflows_flag.py tests/test_098_scope_governance.py -q` exits 0.
    - `wc -l backend/app/api/threads.py` is strictly less than its post-Plan-01 size.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/test_099_skill_composition.py tests/test_147_workflows_flag.py -q</automated>
  </verify>
  <done>preflight_workflow_kickoff + _ensure_skill_snapshots live in workflow_kickoff.py; send_message calls the seam; skill-snapshot/kill-switch/scope-assert 4xx shapes are byte-identical; test_099/test_147/test_098 green.</done>
</task>

<task type="auto">
  <name>Task 2: Extract the harness run-context build into workflow_kickoff.py</name>
  <files>backend/app/services/workflow_kickoff.py, backend/app/api/threads.py</files>
  <read_first>
    - backend/app/api/threads.py — the harness-branch ctx/scope block inside `agent_runner` `~:1502-1657` and the `run_workflow(...)` call that FOLLOWS it `~:1658-1667` (RE-VERIFY; only the BUILD moves, the run_workflow call stays).
    - backend/app/services/harness/scope.py imports already used (resolve_run_scope_root / resolve_project_subtree) + fetch_visible_folders in threads.py.
  </read_first>
  <action>
    Add `async def build_harness_run_context(*, active_workflow_run_id, producer_run_id,
    kickoff_definition, thread_id, body, current_user, user_settings, supabase, redis, pool,
    harness_emit)` to `workflow_kickoff.py`. Move the ctx/scope BUILD (`~:1502-1657`) into it:
    the `resolve_run_scope_root` -> `resolve_project_subtree` -> `fetch_visible_folders`
    path-walk that produces `folder_subtree_ids` + `scoped_folder_path`; the WR-03
    bound-workflow fail-closed guard (the `scope_resolution_failed` emit + `RuntimeError` for a
    bound workflow; unbound falls through to unscoped) VERBATIM; and the `SimpleNamespace` `wf_ctx`
    assembly (all fields: run_id, producer_run_id, thread_id, current_user, user_settings, model
    via `resolve_workflow_ctx_model`, inputs mirror of kickoff_prompt/folder_id, redis, pool,
    emit, retry_feedback, supabase, folder_subtree_ids, scoped_folder_path, spawn, the fresh
    per_run_task_semaphore). Return `wf_ctx`. Keep the harness lazy imports LOCAL to this function
    (dodges the import cycle, matches the existing inline lazy imports). `spawn` is passed in
    (threads.py `_spawn`) so the module need not import it.

    In `threads.py` (inside `agent_runner`'s harness branch): replace the moved BUILD with
    `wf_ctx = await build_harness_run_context(active_workflow_run_id=_active_workflow_run_id,
    producer_run_id=run_id, kickoff_definition=_kickoff_definition, thread_id=thread_id, body=body,
    current_user=current_user, user_settings=user_settings, supabase=supabase, redis=redis,
    pool=await get_pg_pool(), harness_emit=_harness_emit, spawn=_spawn)` (thread `_spawn` through),
    then keep the existing `await run_workflow(_active_workflow_run_id, _wf_definition, wf_ctx,
    pool=..., redis=redis, stream_run_id=run_id)` call UNCHANGED. NOTE: this block still physically
    lives inside `agent_runner` until Plan 03 relocates the producer shell — that is expected;
    this task only shrinks the block to a single call.
  </action>
  <acceptance_criteria>
    - `grep -c "def build_harness_run_context" backend/app/services/workflow_kickoff.py` returns 1.
    - `grep -c "build_harness_run_context" backend/app/api/threads.py` returns 1 (call site present).
    - The inline `SimpleNamespace(` ctx-build no longer lives in threads.py's harness branch: `grep -v '^[[:space:]]*#' backend/app/api/threads.py | grep -c "producer_run_id=run_id"` returns 0.
    - The harness kickoff mode-branch + scope contracts pass: `cd backend && python -m pytest tests/test_dual_mode_wiring.py tests/test_141_run_scope.py tests/test_152_folder_override.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/test_dual_mode_wiring.py tests/test_141_run_scope.py -q</automated>
  </verify>
  <done>build_harness_run_context owns the ctx/scope assembly; the agent_runner harness branch is a single build+run_workflow call; the WR-03 fail-closed guard is byte-identical; dual-mode/scope tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → send_message (kickoff) | Unchanged — the RLS-scoped definition resolve, workflows kill-switch, and folder-scope assert move verbatim; no new boundary, no relaxed check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162.5-02-NNS | (none) | workflow_kickoff.py | accept | No new threat surface — behavior-preserving extraction (D-A7). The T-092-05 IDOR mitigation (RLS-scoped `.or_` definition resolve), the workflows kill-switch 403, the 098 fail-loud scope assert, and the WR-03 bound-workflow fail-closed guard all move verbatim. Correctness control = full suite green + Plan-04 Deep byte-identical gate. |
| T-162.5-SC | Tampering | package installs | accept | N/A — no package installs (D-A7). |
</threat_model>

<verification>
- Full backend suite green: `cd backend && python -m pytest -q`.
- The harness preflight 4xx contract, the scope-assert, and the dual-mode branch are byte-identical (test_099 / test_147 / test_098 / test_dual_mode_wiring / test_141 / test_152 green).
- Deep path untouched: a `workflow_definition_id is None` send takes the `(None, None)` preflight no-op and never builds a harness ctx.
</verification>

<success_criteria>
workflow_kickoff.py owns the kickoff preflight, `_ensure_skill_snapshots`, and the harness
run-context build; `send_message` + the `agent_runner` harness branch each reduce to a single
call into the seam; every harness/scope/skill-snapshot contract stays green; threads.py shrinks.
</success_criteria>

<output>
Create `.planning/phases/162.5-threads-py-producer-extraction/162.5-02-SUMMARY.md` when done.
</output>
