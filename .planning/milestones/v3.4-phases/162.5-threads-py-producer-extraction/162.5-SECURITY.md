---
phase: 162.5
slug: threads-py-producer-extraction
status: verified
threats_open: 0
asvs_level: 1
created: 2026-07-19
---

# Phase 162.5 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.
> Scope: a **pure behavior-preserving refactor** — `backend/app/api/threads.py`
> 2444 → 1204 LOC, with the auto-title / model-resolution / workflow-kickoff /
> Deep-producer concerns extracted verbatim into four `backend/app/services/`
> modules and a single unified 8-invariant finalizer (`_finalize_producer_run`).
> **Zero `org_id` / RLS / client-swap content** (this phase is the G-5 Wave-0
> refactor that unblocks the Phase 163 RLS crux — it deliberately introduces no
> tenancy logic of its own). The security posture rests on ONE claim: every
> pre-existing mitigation moved **byte-identical**. That claim is proven by the
> D-A6 Deep byte-identical gate (operator-approved 2026-07-19) and re-confirmed
> here by direct file:line grep of each named control in its new home. "No new
> attack surface" is the documented, verified disposition — NOT an unexamined
> rubber-stamp.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| client → `send_message` handler | **Unchanged.** The extracted leaves (auto-title, model resolution) run inside the same authenticated handler with the same inputs; no new boundary, no relaxed check. | Authenticated chat request (RLS-scoped by `current_user["id"]`) |
| client → workflow kickoff seam (`workflow_kickoff.py`) | **Unchanged.** The RLS-scoped published-definition resolve, workflows kill-switch, folder-scope ⊆ assert, and bound-workflow fail-closed guard all move verbatim; the seam calls `workflows_enabled` / `get_pg_pool` LATE off `app.api.threads` so every test patch-surface is preserved. | Workflow launch request; run scope (folder subtree) |
| producer task ↔ Redis / Postgres (`run_producer.py`) | **Unchanged.** The shielded terminal co-write, SSE sentinel ordering, best-effort framing, and run-owner scoping move verbatim into the shared `_finalize_producer_run`; the 8 finalize-ordering invariants are byte-identical on both the producer and continuation paths. | Run events (Redis stream), terminal status (Postgres), run-owner scope |
| package / dependency set | **Unchanged.** No `pip` / `npm` / `cargo` install, no new dependency, no new runtime. | (none) |

---

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation / Verification | Status |
|-----------|----------|-----------|-------------|---------------------------|--------|
| T-162.5-01-NNS | (no new surface) | `thread_title.py` · `run_model_resolution.py` | accept | Pure behavior-preserving extraction (D-A7). **Zero `org_id`/RLS/client-swap** — confirmed by empty grep across all 4 extracted modules. Correctness control = full backend suite green + the Plan-04 Deep byte-identical gate. | closed |
| T-162.5-02-NNS | (no new surface) | `workflow_kickoff.py` | accept | Behavior-preserving extraction; the four security controls move **verbatim** and were grep-verified in their new home: T-092-05 IDOR RLS-scoped resolve (`workflow_kickoff.py:203` `.or_(is_global.eq.true,created_by.eq.{id})`), workflows kill-switch 403 (`:190→192`), 098 folder-scope ⊆ assert (`:236→242` `assert_folder_scopes_subset` → 400), WR-03 bound-workflow fail-closed guard (`:299` `scope_resolution_failed` emit + `RuntimeError`). | closed |
| T-162.5-03-NNS | (no new surface) | `run_producer.py` (unified `_finalize_producer_run`) | accept | Pure extraction; **zero `org_id`/RLS**. The finalize-ordering invariants preserved byte-identical and grep-verified: 075.4-03 sentinel-after-finalize race fix (`run_producer.py:23` sentinel XADD AFTER finalize / BEFORE expire) and 096-09 resume-safety F2 gate (`:239,:285` F2 terminalize gate + graceful-shutdown leave-active for the boot resume sweep). Correctness control = the Plan-04 Deep byte-identical gate (Phase-089 harness + full suite). | closed |
| T-162.5-04-NNS | (no new surface) | verification gate (Plan 04) | accept | Verification-only plan — runs tests + a live Deep smoke and authors VALIDATION.md; no source change, no new endpoint/boundary/schema. It **is** the correctness control for the whole phase. SUMMARY `## Threat Flags`: **None**. | closed |
| T-162.5-SC | Tampering (supply-chain) | package installs | accept | N/A — no package installs; no new dependency, no new runtime (D-A7 / D-14). Declared identically in Plans 01–04; no package-legitimacy gate required. | closed |

*Status: open · closed*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

**Threat-flag reconciliation:** the only SUMMARY carrying a `## Threat Flags`
section (162.5-04) records **None** — no new attack surface appeared during
implementation. No unregistered flags.

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| AR-162.5-1 | T-162.5-01/02/03/04-NNS | No new threat surface — a behavior-preserving extraction whose every pre-existing security control (IDOR resolve, kill-switch 403, folder-scope assert, bound-workflow fail-closed, sentinel ordering, resume F2 gate) was proven to move byte-identical. Verified two ways: (1) the D-A6 Deep byte-identical gate — **operator-recorded "approved" 2026-07-19**, all 4 axes + full native-7 cross-provider observed byte-identical, old-vs-new differential = zero net-new failures; (2) direct file:line grep of each named control in its new module (this audit). | Operator (fhdmrd) | 2026-07-19 |
| AR-162.5-2 | T-162.5-SC | No `pip`/`npm`/`cargo` install and no new dependency in this phase, so no software-supply-chain surface is introduced; no package-legitimacy gate applies. | Operator (fhdmrd) | 2026-07-19 |

*Accepted risks do not resurface in future audit runs.*

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-07-19 | 5 | 5 | 0 | /gsd:secure-phase (Claude, opus) |

**Method notes (this audit):**
- Register origin: `register_authored_at_plan_time: true` — all 4 PLAN files
  carried parseable `<threat_model>` blocks. Short-circuit path (threats_open:0 +
  authored-at-plan-time) taken, but **not** as a rubber-stamp: every `accept`
  rationale that asserted a preserved mitigation was independently grep-verified
  in the extracted module before closing.
- 6/6 named mitigations confirmed present in their new homes (see Threat Register
  file:line refs). `org_id` grep across all 4 extracted modules: **empty (clean)**.
- Cross-checked upstream evidence: VALIDATION.md (D-A6 gate ✅ approved),
  VERIFICATION.md (`passed`, 7/7 must-haves, verifier independently reproduced the
  zero-net-new-failures result and confirmed 8/8 late-import patch-surface identity),
  REVIEW.md (0 Critical, 1 Warning WR-01 dead-imports — already fixed `7ec7140f`).
- OpenRouter 404s observed during the live gate were **external** (both models
  unavailable upstream), not caused by the refactor; they incidentally exercised
  the FAILURE finalize path of the unified `_finalize_producer_run` correctly.

---

## Sign-Off

- [x] All threats have a disposition (accept — documented in Accepted Risks Log)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-07-19
