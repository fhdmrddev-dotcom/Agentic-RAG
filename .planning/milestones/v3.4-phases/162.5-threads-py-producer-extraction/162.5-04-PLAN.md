---
phase: 162.5-threads-py-producer-extraction
plan: 04
type: execute
wave: 4
depends_on: [162.5-03]
files_modified:
  - .planning/phases/162.5-threads-py-producer-extraction/162.5-VALIDATION.md
# [BLOCKING] operator starts the backend for the live Deep byte-identical gate:
autonomous: false
# G-5 refactor — no own REQ-ID (enables TEN-02). Intentionally empty:
requirements: []

must_haves:
  truths:
    - "D-A6: Deep-Mode byte-identical is THE gate — the full backend suite is green LIVE (integration rows execute, not skip) and a native-provider Deep smoke shows no SSE-vocabulary / streaming / terminal / title / continue difference vs pre-extraction; the operator starts the backend (autonomous:false)."
    - "D-A1: this is the phase boundary that unblocks the crux — Phase 163 (org_id/RLS/client-swap through send_message) does NOT start until this verify-work passes; a red suite or a smoke diff BLOCKS 163 and routes back to Plan 03."
    - "The extraction goal is met (goal-backward): threads.py has shrunk from 2,444 LOC into 4 cohesive modules (thread_title / run_model_resolution / workflow_kickoff / run_producer), `agent_loop.py::run_agent_loop` is byte-unchanged, and no org_id/RLS content entered any file (D-A7)."
  artifacts:
    - path: ".planning/phases/162.5-threads-py-producer-extraction/162.5-VALIDATION.md"
      provides: "The 4-axis Deep byte-identical UAT scoreboard the operator drives"
      contains: "Cross-provider"
  key_links:
    - from: "162.5-VALIDATION.md"
      to: "the live backend"
      via: "operator-driven native-provider Deep smoke"
      pattern: "byte-identical"
---

<objective>
The D-A6 hard gate. Prove the whole extraction is Deep-Mode byte-identical and the full
integration suite is green LIVE, then hand the phase boundary to Phase 163.

This is the [BLOCKING] plan: Phase 163 (the RLS + user-JWT client-swap crux) MUST NOT start
until this verify-work passes (D-A1). It runs the Phase-089 producer/finalize seam tests + the
full suite against a live backend, plus an operator-driven native-provider Deep smoke covering
the CLAUDE.md 4-axis bandwidth (cross-provider × multi-tool × parallel-thread × long-message).

`autonomous: false` — the operator starts the backend + Supabase + Redis (feedback_user_starts_backend);
Claude never runs uvicorn in this repo. Claude authors the scoreboard, runs the structural + mock
checks, and drives the live gate with the operator.

Output: `162.5-VALIDATION.md` (the 4-axis scoreboard) + a green live suite + an operator "approved"
confirming byte-identical Deep Mode.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/162.5-threads-py-producer-extraction/162.5-CONTEXT.md
@backend/tests/integration/test_089_agent_loop_result_seam.py

# Live-verify tooling + conventions:
#   - CLAUDE.md "UAT scoreboard recipe" (4-axis) + "local-dev-app" (http://localhost:5173,
#     login fhdmrd@gmail.com / 123456) + "user-starts-backend" + "evidence-tools" (psycopg2 :54322).
#   - pytest: `cd backend && python -m pytest -q` (venv active; asyncio_mode=auto; testpaths=tests).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 4-axis scoreboard + run structural and mock-backed proofs</name>
  <files>.planning/phases/162.5-threads-py-producer-extraction/162.5-VALIDATION.md</files>
  <read_first>
    - .planning/phases/162.5-threads-py-producer-extraction/162.5-CONTEXT.md — D-A6 gate definition.
    - The three prior SUMMARYs (162.5-01/02/03-SUMMARY.md) — the exact modules + any test patch retargets landed.
  </read_first>
  <action>
    Author `162.5-VALIDATION.md` with a Deep byte-identical 4-axis UAT scoreboard (CLAUDE.md recipe)
    the operator will drive in Task 2. Rows MUST exercise: Cross-provider (OpenAI, Anthropic, Google,
    OpenRouter — one representative model each, a plain Deep send each), Multi-tool (one Deep prompt
    using 2+ tools, e.g. `search_documents` + `execute_code`), Parallel-thread (Thread A streaming
    while Thread B accepts a new Deep prompt), and Long-message (a thread with >=50 prior messages
    OR a >=5 KB user prompt). Each row states the expected byte-identical observation (streaming
    deltas render, tool cards expand, terminal sentinel arrives, auto-title persists, no
    "unexpected error"). Add a Continue-affordance row (a cap_paused Deep run -> Continue re-drives
    the same run_id) since Plan 03 unified that path.

    Then run the structural + mock-backed proofs (these do NOT need live infra — the producer tests
    use httpx ASGITransport + AsyncMock pools):
      - `cd backend && python -m pytest tests/ -q -k "not integration"` (unit + service tests) and
        the mock-backed producer families under tests/integration that self-run without DB
        (test_089_agent_loop_result_seam, test_dual_mode_wiring is under tests/, test_066_*).
      - Record `wc -l backend/app/api/threads.py` (must be far below 2444) and confirm the 4 new
        modules exist and `git diff --stat backend/app/services/agent_loop.py` shows no change.
    Capture the pass/fail + line-count evidence into `162.5-VALIDATION.md` under a "Static + mock
    proof" section. Do NOT start uvicorn.
  </action>
  <acceptance_criteria>
    - `162.5-VALIDATION.md` exists and contains rows for all four axes (grep the file for `Cross-provider`, `Multi-tool`, `Parallel-thread`, `Long-message`) plus a Continue row.
    - All four modules exist: `ls backend/app/services/thread_title.py backend/app/services/run_model_resolution.py backend/app/services/workflow_kickoff.py backend/app/services/run_producer.py` all present.
    - `git diff --stat backend/app/services/agent_loop.py` shows no change (Deep red line intact).
    - `wc -l backend/app/api/threads.py` is far below 2444 (record the number).
    - The mock-backed producer/SSE tests pass: `cd backend && python -m pytest tests/test_dual_mode_wiring.py tests/integration/test_089_agent_loop_result_seam.py tests/test_continue.py tests/test_provider_router.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/test_dual_mode_wiring.py tests/integration/test_089_agent_loop_result_seam.py tests/test_continue.py -q</automated>
  </verify>
  <done>162.5-VALIDATION.md holds the 4-axis + Continue scoreboard; the 4 modules exist; agent_loop.py is unchanged; threads.py is far smaller; the mock-backed producer/finalize/continue suite is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Operator live Deep byte-identical gate</name>
  <files>.planning/phases/162.5-threads-py-producer-extraction/162.5-VALIDATION.md</files>
  <action>
    Operator-driven live gate (Claude does not start uvicorn). The operator starts Supabase + Redis
    + the backend, then Claude + operator together run the FULL suite LIVE and drive every
    162.5-VALIDATION.md row, confirming Deep Mode is byte-identical. On any diff or red suite the
    gate FAILS and routes back to Plan 03 (or the owning leaf plan); Phase 163 stays blocked (D-A1).
    See <how-to-verify> for the exact steps.
  </action>
  <what-built>
    The 2,444-LOC threads.py producer/finalize/kickoff/title/model-resolution logic has been
    extracted into 4 cohesive service modules (thread_title.py, run_model_resolution.py,
    workflow_kickoff.py, run_producer.py) with the 8 finalize-ordering invariants preserved and
    agent_runner + spawn_continuation_run unified onto one shared finalizer — behavior-preserving,
    zero org_id/RLS content. `162.5-VALIDATION.md` holds the 4-axis scoreboard.
  </what-built>
  <how-to-verify>
    1. Start infra + backend: ensure Supabase (`supabase start`) + Redis (docker compose dev) are up,
       then start the backend (`cd backend && <venv> uvicorn app.main:app --reload`) yourself and
       confirm it booted clean (no import errors — the extraction's late-import cycle-break is
       exercised at first request).
    2. Run the FULL suite LIVE (integration rows execute, not skip):
       `cd backend && <venv> python -m pytest -q` — expect exit 0 / all green (report any red).
    3. Drive `162.5-VALIDATION.md` at http://localhost:5173 (login fhdmrd@gmail.com / 123456):
       - Cross-provider: send a plain Deep message on OpenAI, Anthropic, Google, and OpenRouter —
         each streams tokens, shows model/provider, reaches a terminal state, and auto-titles the thread.
       - Multi-tool: one Deep prompt that uses 2+ tools (e.g. search the KB then run code) — both
         tool cards render + expand, the run completes.
       - Parallel-thread: while Thread A is streaming, open Thread B and send a Deep prompt — both
         stream independently; neither wedges.
       - Long-message: send on a thread with >=50 prior messages OR a >=5 KB prompt — streams + completes.
       - Continue: drive a cap_paused Deep run to the Continue affordance and confirm it re-drives
         the SAME run_id and finalizes.
       Observation bar: no SSE-vocabulary/streaming/terminal/title/continue DIFFERENCE vs before the
       extraction, and no "An unexpected error occurred".
    4. If anything diffs or the suite is red: this gate FAILS — do NOT approve; route back to Plan 03
       (the producer/finalizer) or the relevant leaf plan. Phase 163 stays blocked (D-A1).
  </how-to-verify>
  <verify>
    <human-check>Full live suite green AND every scoreboard row observed byte-identical by the operator.</human-check>
  </verify>
  <done>Operator confirms a green live suite + byte-identical Deep Mode across all 4 axes + Continue; approval recorded; Phase 163 unblocked.</done>
  <resume-signal>Type "approved" if the full live suite is green AND all scoreboard rows are byte-identical; otherwise describe the diff/failure so it routes back to the owning plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | This is a verification-only plan — no source changes, no new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162.5-04-NNS | (none) | verification gate | accept | No new threat surface — the gate only runs tests + a live smoke and authors a VALIDATION.md (D-A7). It IS the correctness control for the whole phase. |
| T-162.5-SC | Tampering | package installs | accept | N/A — no installs. |
</threat_model>

<verification>
- The FULL backend suite is green LIVE (operator-run): `cd backend && python -m pytest -q`.
- All `162.5-VALIDATION.md` rows observed byte-identical by the operator (cross-provider ×
  multi-tool × parallel-thread × long-message + Continue).
- `git diff --stat backend/app/services/agent_loop.py` shows no change.
- Operator "approved" recorded — Phase 163 is unblocked (D-A1 phase boundary satisfied).
</verification>

<success_criteria>
Deep Mode is proven byte-identical on the native providers with a green live suite; the operator
has approved; the 4-module extraction is complete with `agent_loop.py::run_agent_loop` untouched
and zero org_id/RLS content — the D-A6 gate passes and Phase 163 may begin.
</success_criteria>

<output>
Create `.planning/phases/162.5-threads-py-producer-extraction/162.5-04-SUMMARY.md` when done
(record the live suite result, the operator approval, and the final threads.py line count).
</output>
