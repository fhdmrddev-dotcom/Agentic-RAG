---
phase: 164-secdef-audit-cross-org-isolation-test-suite
plan: 03
type: execute
wave: 2
depends_on: ["164-01"]
files_modified:
  - supabase/migrations/110_secdef_org_scope_audit.sql
  - supabase/full-schema.sql
autonomous: true
requirements: [TEN-03, PRAG-01]
must_haves:
  truths:
    - "D-164-01: match_document_chunks / keyword_search_chunks / match_skills each filter `org_id = ANY(SELECT public.current_user_org_ids())` in-body; NO match_org_id parameter is added (org is never caller-supplied); the owner branch keys on `auth.uid()`, not `match_user_id`"
    - "D-164-03: all four functions REMAIN SECURITY DEFINER with a pinned, minimal search_path (prosecdef=true AND proconfig contains search_path); signatures are byte-identical (CREATE OR REPLACE, no DROP)"
    - "D-164-03: the pgvector distance operator is written OPERATOR(public.<=>) so it resolves under a pinned search_path and still matches the HNSW vector_cosine_ops index (no perf regression)"
    - "D-164-03: match_skills keeps the is_system branch OUTSIDE the org gate (mirrors mig 109:70-73 FIX-A) so the built-in skill-creator stays universal — not trapped in the org gate (the 163-UAT Test-7 regression)"
    - "PRAG-01 / D-164-07: the document_chunks SELECT RLS is WIDENED (additive on the mig-108:168-170 owner-only baseline) to mirror the documents folder-visibility EXISTS branch"
    - "D-164-08: migration 110 is applied to the live :54322 DB via psycopg2/SQL-editor (NEVER db reset / db push); full-schema.sql is regenerated no-reset and committed in the same commit"
    - "PRAG-01: CONCUR-01 stays <1s (test_058_concurrency.py) after the widening — the perf gate is re-benchmarked before merge (163 baseline 0.41s)"
  artifacts:
    - path: "supabase/migrations/110_secdef_org_scope_audit.sql"
      provides: "4 DEFINER re-CREATE (org predicate + pinned search_path) + document_chunks PRAG-01 RLS widening"
      contains: "current_user_org_ids"
    - path: "supabase/full-schema.sql"
      provides: "regenerated no-reset deploy artifact reflecting the applied 110"
  key_links:
    - from: "supabase/migrations/110_secdef_org_scope_audit.sql"
      to: "public.current_user_org_ids()"
      via: "in-body org predicate on the three retrieval functions"
      pattern: "org_id = ANY \\(SELECT public.current_user_org_ids\\(\\)\\)"
    - from: "public.document_chunks SELECT policy"
      to: "public.folder_is_globally_visible"
      via: "EXISTS on the parent document"
      pattern: "folder_is_globally_visible"
---

<objective>
Author and apply migration `110_secdef_org_scope_audit.sql` — the SQL half of the two-halves-that-must-land-together phase. It re-CREATEs the four `SECURITY DEFINER` functions with an in-body org predicate + a pinned `search_path`, and widens the `document_chunks` SELECT RLS to mirror the folder-visibility predicate (PRAG-01). It mirrors SHIPPED policy shape (mig 108 documents SELECT + mig 109 FIX-A), not new invention.

Purpose: Move org-scope enforcement into the DEFINER bodies (server-derived from `current_user_org_ids()` / `auth.uid()`, never a spoofable caller arg — D-164-01) and close the search-path-hijack CVE class (D-164-03). This SQL is INERT until Plan 04 routes the producer's retrieval calls onto the asyncpg user-context — but the migration must apply FIRST so Plan 04's swap can be tested against the org-scoped bodies.

Output: `supabase/migrations/110_secdef_org_scope_audit.sql` applied to :54322 + regenerated `full-schema.sql`, both committed same-commit.

Depends on 164-01: the exit-gate suite's red-anchor must be committed and proven RED against the pre-164 DB BEFORE this migration applies (RED-then-GREEN determinism; mirrors the 163 leak-test-first sequencing). After apply, the suite's DB-level legs (definer, search_path, table_matrix, prag01) turn GREEN.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/164-secdef-audit-cross-org-isolation-test-suite/164-CONTEXT.md
@.planning/phases/164-secdef-audit-cross-org-isolation-test-suite/164-RESEARCH.md
@.planning/phases/164-secdef-audit-cross-org-isolation-test-suite/164-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- The four CURRENT live DEFINER bodies to re-CREATE + the shipped policy shapes to mirror. -->

Current bodies (re-CREATE with byte-identical signatures — CREATE OR REPLACE, no DROP; Pitfall 6):
- match_document_chunks — supabase/migrations/073_embedding_provider_and_chunk_tags.sql:34-60 (DEFINER, NO search_path, WHERE dc.user_id = match_user_id)
- keyword_search_chunks — supabase/migrations/025_document_versioning.sql:50-76 (DEFINER, NO search_path; returns (id, document_id, content, chunk_index, rank) — PRESERVE byte-for-byte)
- match_skills — supabase/migrations/091_skill_embeddings.sql:87-108 (DEFINER, SET search_path = public, pg_temp — DROP pg_temp; returns (id, name, description, similarity))
- folder_is_globally_visible — supabase/migrations/019_global_folder_subtree_visibility.sql:12-31 (DEFINER, LANGUAGE sql STABLE, SET search_path = public — pure ancestor-walk, NO org predicate; audit = pin + qualify + justify)

Shipped shapes to MIRROR (do not re-invent):
- documents SELECT policy — migrations/108_rls_membership_rewrite.sql:114-117 (org gate + `(auth.uid()=user_id OR (folder_id IS NOT NULL AND folder_is_globally_visible(folder_id)))`)
- FIX-A is_system-outside-gate — migrations/109_platform_universal_rls_fix.sql:70-73 (`(is_system=true) OR (org_id IN (...) AND (auth.uid()=user_id OR is_global=true))`)
- document_chunks owner-only SELECT (widen this) — migrations/108_rls_membership_rewrite.sql:168-170
- the org resolver — public.current_user_org_ids() (mig 104): SECURITY DEFINER STABLE, reads `SELECT org_id FROM public.org_members WHERE user_id = auth.uid()`
- header/apply-discipline + cloud-parity block to clone in intent — migrations/109_platform_universal_rls_fix.sql:46-57 (BUT the apply command differs — see Task 2 override)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 110 — 4 DEFINER re-CREATE (org predicate + pinned search_path) + document_chunks PRAG-01 RLS widening</name>
  <files>supabase/migrations/110_secdef_org_scope_audit.sql</files>
  <read_first>
    - supabase/migrations/073_embedding_provider_and_chunk_tags.sql:34-60 + 025_document_versioning.sql:50-76 + 091_skill_embeddings.sql:87-108 + 019_global_folder_subtree_visibility.sql:12-31 (the four CURRENT bodies)
    - supabase/migrations/108_rls_membership_rewrite.sql:114-117,168-170 (documents SELECT shape + document_chunks owner-only baseline)
    - supabase/migrations/109_platform_universal_rls_fix.sql:1-73 (FIX-A is_system-outside-gate shape + the header/cloud-parity comment block)
    - 164-PATTERNS.md §110 migration (the exact org-gate transforms + the match_skills is_system hoist + the document_chunks widening)
    - 164-RESEARCH.md §Architecture Pattern 1 (org-predicate body), §Pattern 3 (search_path form + OPERATOR(public.<=>)), §Pattern 4 (document_chunks widening), §Pitfall 2/5/6
  </read_first>
  <action>
    Write `110_secdef_org_scope_audit.sql` as ONE `BEGIN; … COMMIT;` atomic migration. Clone the mig-109:46-57 header block in INTENT (CLAUDE.md apply-discipline + cloud-parity note) BUT replace the apply-command line to reflect the 164 override: applied via psycopg2/SQL-editor this session (see Task 2), NEVER `db push`/`db reset`. Note 110 joins the pending cloud set (099→110) + `SECRETS_ENCRYPTION_KEY`, applied to production only at the next operator-gated push; schema-only, seeds no reference data → owes no `docs/OPERATOR.md`/`check-deploy-drift.sh` change (D-16 by exclusion).

    Re-CREATE the four functions with `CREATE OR REPLACE` (byte-identical signatures — no DROP; Pitfall 6):
    - `match_document_chunks` and `keyword_search_chunks`: add `SET search_path = ''` to the DEFINER clause; schema-qualify every relation (`public.document_chunks`, `public.documents`); write the distance/order operators as `OPERATOR(public.<=>)` (Pitfall 2 — `''` removes public so `<=>` won't resolve otherwise; still matches the HNSW index). REPLACE `WHERE dc.user_id = match_user_id` with the org gate + within-org visibility branch: `WHERE dc.org_id = ANY (SELECT public.current_user_org_ids()) AND (dc.user_id = auth.uid() OR (d.folder_id IS NOT NULL AND public.folder_is_globally_visible(d.folder_id)))`. Keep `match_user_id` in the signature (index/backward-compat) but it is NO LONGER the scoping key. Preserve every other predicate (threshold, is_latest, metadata_filter, p_folder_ids, p_embedding_model) and the keyword fn's `(id, document_id, content, chunk_index, rank)` return shape byte-for-byte.
    - `match_skills`: pin `SET search_path = ''` (drop `pg_temp`), schema-qualify. REPLACE the WHERE with the FIX-A shape keeping is_system OUTSIDE the org gate (mirror mig 109:70-73): `WHERE ((s.is_system = true) OR (s.org_id = ANY (SELECT public.current_user_org_ids()) AND (s.user_id = auth.uid() OR s.is_global = true))) AND s.is_enabled = true`. (Pitfall 5 — else the built-in skill-creator vanishes cross-org.)
    - `folder_is_globally_visible`: NO org predicate (pure ancestor-walk; the org gate lives in the calling body). Audit = pin `search_path = ''`, schema-qualify `public.folders`, and add an in-file justification comment recording WHY DEFINER is retained per function (pgvector index access on the hot path / avoids per-row RLS re-check under CONCUR-01 <1s / stable planning) and that the in-body predicate is now the isolation guard + the pinned search_path closes CVE-2018-1058.

    Widen the `document_chunks` SELECT RLS (PRAG-01 / D-164-07) additively on the mig-108:168-170 owner-only baseline: `DROP POLICY IF EXISTS "Users can view their own chunks" ON public.document_chunks;` then re-CREATE `FOR SELECT TO authenticated USING (org_id IN (SELECT public.current_user_org_ids()) AND ((auth.uid() = user_id) OR EXISTS (SELECT 1 FROM public.documents d WHERE d.id = document_chunks.document_id AND d.folder_id IS NOT NULL AND public.folder_is_globally_visible(d.folder_id))))`. This EXISTS is a point-lookup on the documents PK (direct-read path only — retrieval is DEFINER and never evaluates this policy). Keep the live column names `is_global`/`is_system`/`folder_is_globally_visible` (renames are Phase 165). Do NOT touch the INSERT/UPDATE/DELETE document_chunks policies.
  </action>
  <acceptance_criteria>
    - File `supabase/migrations/110_secdef_org_scope_audit.sql` exists, filename matches `<digits>_name.sql`, wrapped in one `BEGIN;`/`COMMIT;`.
    - `grep -c "CREATE OR REPLACE FUNCTION" 110_...sql` == 4; grep shows NO `DROP FUNCTION` (signatures unchanged).
    - All four functions carry `SECURITY DEFINER` AND a `SET search_path = ''` clause; `grep -v '^--' | grep -c "OPERATOR(public.<=>)"` ≥ 2 (the two chunk fns' distance + order operators qualified).
    - The three retrieval fns contain `org_id = ANY (SELECT public.current_user_org_ids())`; `match_skills` contains the `is_system = true` branch OUTSIDE the org gate (grep: `(s.is_system = true)\n *OR` shape); `folder_is_globally_visible` has NO `current_user_org_ids` call.
    - The document_chunks SELECT policy re-CREATE contains the `EXISTS (SELECT 1 FROM public.documents` folder-visibility branch; no INSERT/UPDATE/DELETE document_chunks policy is present in the file.
    - A per-function DEFINER-retention justification comment is present (grep the header/body for the CVE-2018-1058 + pgvector-index rationale).
    - NOT YET APPLIED in this task (authoring only) — do not run the apply here.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && grep -c "CREATE OR REPLACE FUNCTION" supabase/migrations/110_secdef_org_scope_audit.sql</automated>
  </verify>
  <done>110 authored: 4 CREATE OR REPLACE (org predicate + pinned search_path + qualified operator), is_system outside the gate, document_chunks widened, justification recorded — not yet applied.</done>
</task>

<task type="auto" gate="blocking-apply">
  <name>Task 2: [BLOCKING] Apply migration 110 to :54322 (psycopg2/SQL-editor, NEVER reset/push) + regenerate full-schema + CONCUR-01 gate</name>
  <files>supabase/full-schema.sql</files>
  <read_first>
    - CLAUDE.md §"Schema changes ship as numbered SQL migrations" (apply-via-SQL-editor rule) + 164-CONTEXT.md D-164-08 + <specifics> (operator autonomy directive: direct psycopg2/SQL-editor apply authorized THIS session; NEVER db reset/push)
    - scripts/regenerate-full-schema.sh (no-reset live dump)
    - backend/tests/integration/test_058_concurrency.py (CONCUR-01 <1s gate; 163 baseline 0.41s)
    - backend/tests/integration/test_v3_4_org_isolation.py (authored in 164-01 — the GREEN target for the DB-level legs)
  </read_first>
  <action>
    [BLOCKING] apply gate — COMMAND OVERRIDE (operator-authorized this session, D-164-08 + CONTEXT <specifics>): apply `110_secdef_org_scope_audit.sql` directly to the LIVE local DB at `postgresql://postgres:postgres@127.0.0.1:54322/postgres` via psycopg2, OR by pasting the whole file into the Supabase SQL editor as one execution. It is `autonomous: true` — but it MUST NEVER run `supabase db push` or `supabase db reset` (destroys dev data). If applying via psycopg2, run it as an INLINE one-liner or a script placed in the session scratchpad dir — NEVER write a scratch `.py` into `backend/` (the uvicorn `--reload` watcher wedges on Windows; memory: no-scratch-in-watched-tree).

    After a clean apply, regenerate the deploy artifact: `bash scripts/regenerate-full-schema.sh` (NO `--reset` — live-DB dump, preserves dev data). Commit `110_secdef_org_scope_audit.sql` + the regenerated `supabase/full-schema.sql` in the SAME commit. Never hand-edit full-schema.sql.

    Then run the DB-level verification: (1) the pg_proc audit — all four functions `prosecdef=true` + `proconfig` contains `search_path`; (2) the 164-01 suite's DB-level legs (`-k "definer or search_path or table_matrix or prag01_retrieval"`) turn GREEN, and the red-anchor `test_definer_ignores_spoofed_match_user_id` now PASSES (org gate keys on auth.uid()); (3) the CONCUR-01 perf gate `test_058_concurrency.py` stays <1s (re-benchmark; abort/investigate if it regresses past 1s — the widening's EXISTS is direct-read-only, retrieval cost is the in-body predicate on the indexed org_id). Do NOT apply to cloud (099→110 owed at the next operator-gated push).
  </action>
  <acceptance_criteria>
    - 110 is applied to :54322 with no error (the `OPERATOR(public.<=>)` fix means match_document_chunks does not raise "operator does not exist"); a direct `SELECT * FROM public.match_document_chunks(...)` smoke returns without operator error.
    - `bash scripts/regenerate-full-schema.sh` ran no-reset; `git status` shows both `110_secdef_org_scope_audit.sql` and `supabase/full-schema.sql` staged together.
    - psycopg2 introspection: `SELECT proname, prosecdef, proconfig FROM pg_proc WHERE proname IN ('match_document_chunks','keyword_search_chunks','match_skills','folder_is_globally_visible') AND pronamespace='public'::regnamespace` → all `prosecdef=true` AND every `proconfig` contains a `search_path=` entry.
    - `cd backend && venv/Scripts/python.exe -m pytest tests/integration/test_v3_4_org_isolation.py -k "definer or search_path or table_matrix or prag01_retrieval" -x` PASSES; `::test_definer_ignores_spoofed_match_user_id` now PASSES (GREEN).
    - `venv/Scripts/python.exe -m pytest tests/integration/test_058_concurrency.py -x` passes and the CONCUR-01 timing stays <1s (record the measured value in the SUMMARY).
    - No `supabase db push` / `db reset` was run (verify shell history / description); no scratch `.py` was written under `backend/`.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/integration/test_v3_4_org_isolation.py -k "search_path or definer" -x && venv/Scripts/python.exe -m pytest tests/integration/test_058_concurrency.py -x</automated>
  </verify>
  <done>110 applied live (never reset/push); full-schema regenerated no-reset + committed same-commit; DB-level suite legs GREEN, red-anchor now passes; CONCUR-01 <1s.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated caller → DEFINER function body | the caller-supplied `match_user_id` param is untrusted; org must be server-derived, not taken from the arg |
| postgres-owned DEFINER execution → resolved objects (tables/operators) | an attacker-controlled `search_path` could shadow a called object (CVE-2018-1058) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-EoP-01 | Elevation | match_document_chunks / keyword_search_chunks / match_skills trusting `match_user_id` | mitigate | in-body `org_id = ANY(SELECT public.current_user_org_ids())` + owner branch on `auth.uid()`; `match_org_id` param deliberately NOT added (D-164-01) |
| T-164-EoP-02 | Elevation / Tampering | search_path hijack inside a postgres-owned DEFINER fn | mitigate | `SET search_path = ''` + schema-qualified refs + `OPERATOR(public.<=>)` on all four fns — closes CVE-2018-1058 |
| T-164-Avail-06 | Availability | `is_system` platform content trapped in the org gate | mitigate | is_system branch hoisted OUTSIDE the org gate in match_skills (mig 109 FIX-A shape) |
| T-164-ID-08 | Information Disclosure | shared-folder document read returning empty chunks (owner-only chunk RLS) | mitigate | document_chunks SELECT RLS widened to mirror the documents folder-visibility EXISTS (PRAG-01 / D-164-07) |
| T-164-SC | Tampering | npm/pip/cargo installs | accept (N/A) | No package installs this phase (RESEARCH §Package Legitimacy Audit = N/A) |
</threat_model>

<verification>
- All four functions: `prosecdef=true` + pinned `search_path` (psycopg2 pg_proc probe).
- 164-01 DB-level legs GREEN; red-anchor now passes.
- CONCUR-01 <1s (test_058_concurrency.py) — perf gate green.
- `full-schema.sql` regenerated no-reset + committed with the migration.
</verification>

<success_criteria>
- Migration 110 org-scopes the three retrieval fns in-body (D-164-01) + pins all four search_paths (D-164-03), keeping is_system universal (FIX-A) and widening document_chunks for PRAG-01 (D-164-07).
- Applied live to :54322 (never reset/push, D-164-08); full-schema regenerated no-reset; DB-level exit-gate legs GREEN; CONCUR-01 <1s.
</success_criteria>

<output>
Create `.planning/phases/164-secdef-audit-cross-org-isolation-test-suite/164-03-SUMMARY.md` when done.
</output>
