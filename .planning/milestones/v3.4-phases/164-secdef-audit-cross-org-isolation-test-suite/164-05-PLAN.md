---
plan: 164-05
phase: 164
wave: 4
depends_on: ["164-04"]
gap_closure: true
autonomous: true
requirements: []
files_modified:
  - backend/app/services/sql_service.py
  - backend/tests/integration/test_v3_4_org_isolation.py
source: code-review 164-REVIEW.md (WR-02 fix in-phase; CR-01/WR-01 folded to Phase 165 per operator, tracked via xfail marker)
---

# Plan 164-05 — Code-review gap closure (WR-02 fix + CR-01 known-open marker)

<objective>
Close the two 164-REVIEW.md findings that stay in Phase 164:
1. **WR-02 (fix in-phase):** `_inject_folder_scope` (`sql_service.py`) appends `WHERE`/`AND` blindly at the END of the LLM SQL, producing invalid SQL when the query has a trailing `ORDER BY` / `GROUP BY` / `LIMIT` / `OFFSET` (a regression introduced when `_inject_user_id`'s WHERE-placement normalization was deleted in 164-04). RLS still isolates (not a leak), but folder-scoped `query_documents` breaks. Fix the clause placement.
2. **CR-01 known-open marker (operator: fold to Phase 165):** add an `xfail(strict=True)` test to the exit-gate suite that exercises the KB browse/read path cross-org and demonstrates the leak, referencing [[SEED-124]]. The suite stays green; when Phase 165 org-scopes the folder helpers, the xfail flips to XPASS-strict → forcing removal.

CR-01 + WR-01 CODE fixes are deferred to Phase 165 (SEED-124) per operator decision — NOT in this plan.
</objective>

<task id="1" name="Fix WR-02 — folder-scope clause placement">
<action>
In `backend/app/services/sql_service.py::_inject_folder_scope`, insert the `folder_id IN (...)` / `folders.id IN (...)` condition BEFORE any trailing `ORDER BY` / `GROUP BY` / `HAVING` / `LIMIT` / `OFFSET` tail instead of appending at the end. Split the SQL at the first occurrence (case-insensitive, outside string literals is out of scope — a simple regex on the first ` ORDER BY|GROUP BY|HAVING|LIMIT|OFFSET ` keyword boundary is sufficient for the LLM-generated SELECT shape) into head + tail; apply the existing "has WHERE → ` AND cond`, else ` WHERE cond`" logic to the HEAD; re-append the tail. Preserve the existing `_detect_alias` behavior and the documents-vs-folders ref selection verbatim. Keep `query_documents`'s SELECT-only + no-`;` guards untouched.
</action>
<read_first>
- backend/app/services/sql_service.py (the current `_inject_folder_scope` at lines 30-46 + `query_documents`)
- .planning/phases/164-secdef-audit-cross-org-isolation-test-suite/164-REVIEW.md (WR-02 detail)
</read_first>
<acceptance_criteria>
- `_inject_folder_scope("SELECT * FROM documents ORDER BY created_at LIMIT 5", ["<uuid>"])` returns SQL where `folder_id IN (...)` appears BEFORE `ORDER BY` (valid SQL — assert the substring order).
- `_inject_folder_scope("SELECT * FROM documents WHERE title = 'x' LIMIT 3", [...])` yields `... WHERE title = 'x' AND documents.folder_id IN (...) LIMIT 3`.
- A no-tail query (`SELECT * FROM documents`) still appends ` WHERE documents.folder_id IN (...)` (no regression).
- Add ≥2 unit assertions to `backend/tests/integration/test_v3_4_org_isolation.py` (or a small pytest in the same file) covering the ORDER BY/LIMIT-tail and the WHERE+LIMIT cases; `venv/Scripts/python.exe -m pytest tests/integration/test_v3_4_org_isolation.py -k folder_scope -x` exits 0.
</acceptance_criteria>
</task>

<task id="2" name="Add CR-01 known-open xfail marker to the exit gate">
<action>
In `backend/tests/integration/test_v3_4_org_isolation.py`, add `test_browse_tools_cross_org_leak_KNOWN_OPEN_seed124`, marked `@pytest.mark.xfail(strict=True, reason="SEED-124 / CR-01: KB browse-tool service-role folder helpers are org-blind; closed by Phase 165 is_global retirement")`. Using the existing two-orgs/two-users fixtures, seed an `is_global` folder + a document owned by user A (org A), then call the browse/read path as user B (org B) on the SERVICE-ROLE client — e.g. `get_globally_visible_folder_ids(service_role_client, user_B)` and assert user B does NOT see A's folder id / `read_path` does NOT return A's document. Because the leak is live, the assertion FAILS → xfail (expected). This makes the exit gate cover the browse-tool surface while documenting the known-open state; when Phase 165 fixes it, the test XPASSes → strict xfail fails → forces marker removal.
</action>
<read_first>
- backend/tests/integration/test_v3_4_org_isolation.py (existing fixtures: two_orgs_two_users, the leak-conn helpers, seed helpers)
- backend/app/utils/folder_utils.py (get_globally_visible_folder_ids / fetch_visible_folders — the leaking helpers)
- backend/app/api/kb.py (read_path / ls_path signatures)
- .planning/seeds/SEED-124-browse-tool-service-role-cross-org-folder-leak.md (the fold contract)
</read_first>
<acceptance_criteria>
- `test_browse_tools_cross_org_leak_KNOWN_OPEN_seed124` exists with `xfail(strict=True)` + the SEED-124 reason string.
- `venv/Scripts/python.exe -m pytest tests/integration/test_v3_4_org_isolation.py -q` → all pass/xfail, exit 0 (the new test is XFAIL, not FAIL; the suite stays green).
- The test genuinely exercises the service-role folder-helper path (grep-verifiable: references `get_globally_visible_folder_ids` or `fetch_visible_folders` or `read_path`).
</acceptance_criteria>
</task>

<must_haves>
truths:
  - "WR-02: `_inject_folder_scope` places the folder condition before ORDER BY/GROUP BY/LIMIT/OFFSET; folder-scoped query_documents produces valid SQL."
  - "CR-01/SEED-124: the exit gate has a browse-tool cross-org test marked xfail(strict) referencing SEED-124; suite stays green; Phase 165 flips it."
</must_haves>

<verify>
<automated>cd backend && venv/Scripts/python.exe -m pytest tests/integration/test_v3_4_org_isolation.py -q</automated>
</verify>
