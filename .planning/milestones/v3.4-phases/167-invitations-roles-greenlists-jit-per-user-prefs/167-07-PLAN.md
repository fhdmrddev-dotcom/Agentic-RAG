---
phase: 167-invitations-roles-greenlists-jit-per-user-prefs
plan: 07
type: execute
wave: 4
depends_on: [167-04, 167-03, 167-05]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/components/settings/ModelDefaultPreference.tsx
  - frontend/src/components/settings/ModelDefaultPreference.test.tsx
  - frontend/src/components/admin/ControlRoomPage.tsx
autonomous: true
requirements: [VIS-01, VIS-02]
must_haves:
  truths:
    - "VIS-02 / D-167-04: a user picks their default model from the operator/org-allowed set via a registry-only picker; the ALWAYS-ON 🔒 footer surfaces the EFFECTIVE model and, when the operator lock is on, disables the select and names the governed default (SEED-116 two-layer)"
    - "VIS-02: the picker never lets a user pick outside the allowed set — the select offers only enabled/allowed models and a persisted unknown value stays selectable as '(current)' so a round-trip never drops it"
    - "VIS-01: the operator greenlist admin surface (minimal) extends the shipped ControlRoomPage feature-visibility toggle to set a role audience (roles[]) — rich group management stays deferred"
    - "the model-default picker clones the shipped JudgeModelPicker idiom (registry-only select + save-on-select + 🔒 footer); no new design"
  artifacts:
    - path: "frontend/src/components/settings/ModelDefaultPreference.tsx"
      provides: "VIS-02 per-user model-default picker (registry-only + 🔒 lock footer)"
      contains: "preferences"
    - path: "frontend/src/lib/api.ts"
      provides: "getModelDefault/setModelDefault + setFeatureAudience(role) client fns"
      contains: "me/preferences"
    - path: "frontend/src/components/admin/ControlRoomPage.tsx"
      provides: "minimal greenlist audience editor (role audiences on the existing visibility toggle)"
  key_links:
    - from: "frontend/src/components/settings/ModelDefaultPreference.tsx"
      to: "GET/PUT /me/preferences (Plan 04)"
      via: "getModelDefault/setModelDefault (save-on-select, server-derived)"
      pattern: "me/preferences"
    - from: "frontend/src/components/admin/ControlRoomPage.tsx"
      to: "PUT /admin/visibility (Plan 03)"
      via: "setFeatureAudience with a role audience + roles[]"
      pattern: "setFeatureAudience"
---

<objective>
Ship the two access-projection pickers: the VIS-02 per-user model-default picker (registry-only select + an always-on 🔒 footer that surfaces the operator lock) under Settings, and the minimal VIS-01 greenlist admin surface (extend the shipped Control Room feature-visibility toggle to set a role audience). Both clone shipped idioms — no new design.

Purpose: VIS-02 (D-167-04) proves the SEED-116 two-layer pattern end-to-end in the UI — the user picks within the operator/org-allowed set, the lock is surfaced honestly. VIS-01's minimal admin surface lets an operator set a role greenlist (rich group management deferred). Both touch api.ts (added AFTER Plan 05 — Wave 4, no conflict).
Output: `api.ts` (prefs + audience fns), `ModelDefaultPreference.tsx`, the ControlRoomPage greenlist toggle, tests.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/167-invitations-roles-greenlists-jit-per-user-prefs/167-CONTEXT.md
@.planning/phases/167-invitations-roles-greenlists-jit-per-user-prefs/167-PATTERNS.md
@frontend/src/components/settings/JudgeModelPicker.tsx

<interfaces>
<!-- Shipped picker idiom + backend contracts. -->

frontend/src/components/settings/JudgeModelPicker.tsx (whole file, 119 lines) — the near-1:1 analog:
  registry-only <select> (89-104): dedupe+sort, keep an unknown persisted value selectable as "(current)" (74, 97-98) so a round-trip never drops it.
  save-on-select (54-69): onChange → persist → re-read from server (server-derived, never a separate optimistic store).
  load effect with a cancelled guard (37-52).
  ALWAYS-ON 🔒 footer (108-114): shows the EFFECTIVE model, never blank — for VIS-02 this is where the operator LOCK surfaces (disable the select + name the governed default when locked; SEED-116). See ProviderPicker.tsx:202-210 for the same footer.

Plan 04 backend: GET /me/preferences → {default_model, effective_model, locked, allowed_models}; PUT /me/preferences {default_model|null}.
Plan 03 backend: PUT /admin/visibility {feature, audience:'role', roles:[...]} (allowlist-validated).

frontend/src/lib/api.ts: getAuthHeaders (108-116) auto-carries X-Org-Id; ApiError + defensive unwrap; the getJudgeModel/setJudgeModel client shape (the analog for the prefs fns).
frontend/src/components/admin/ControlRoomPage.tsx — the operator surface hosting the existing binary feature-visibility toggle; extend that control to select a role audience (minimal — rich group UI deferred).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: api.ts — model-default prefs fns + greenlist audience write fn</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts:108-116 (getAuthHeaders), the getJudgeModel/setJudgeModel client shape (the prefs analog)
    - Plan 04 (/me/preferences) + Plan 03 (PUT /admin/visibility role audience) contracts
  </read_first>
  <action>
    Add to api.ts (mirroring getJudgeModel/setJudgeModel; getAuthHeaders → X-Org-Id auto-carried; ApiError + defensive `?? fallback`):
    - `getModelDefault() -> {default_model, effective_model, locked, allowed_models}` (GET /me/preferences).
    - `setModelDefault(model: string | null) -> {default_model, effective_model, locked, allowed_models}` (PUT /me/preferences; null clears the override) — re-reads from the server so the knob stays server-derived.
    - `setFeatureAudience(feature: string, audience: string, roles?: string[]) -> void` (PUT /admin/visibility) — the greenlist write extended to a role audience + roles[].
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && npx tsc --noEmit</automated>
  </verify>
  <done>getModelDefault/setModelDefault + setFeatureAudience exist with typed payloads; tsc clean.</done>
  <acceptance_criteria>
    - `grep -n "getModelDefault\|setModelDefault\|setFeatureAudience" frontend/src/lib/api.ts` shows all three.
    - `grep -n "me/preferences" frontend/src/lib/api.ts` matches; each fn uses getAuthHeaders().
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ModelDefaultPreference picker (registry-only + 🔒 lock footer) + Settings mount + test</name>
  <files>frontend/src/components/settings/ModelDefaultPreference.tsx, frontend/src/components/settings/ModelDefaultPreference.test.tsx</files>
  <read_first>
    - frontend/src/components/settings/JudgeModelPicker.tsx (whole file — the registry-only select + save-on-select + load-with-cancelled-guard + ALWAYS-ON 🔒 footer to clone)
    - frontend/src/components/settings/ProviderPicker.tsx:202-210 (the same always-on 🔒 footer with a local/cloud tint)
    - frontend/src/lib/api.ts (Task 1 fns)
  </read_first>
  <behavior>
    - The select offers only the allowed/enabled models (`allowed_models`); a persisted unknown value stays selectable as "(current)".
    - Save-on-select: choosing a model persists via setModelDefault and re-reads the effective pair from the server.
    - When `locked` is true, the select is DISABLED and the 🔒 footer names the governed default (the user override is inert — SEED-116).
    - When unlocked and unset, the footer shows the operator/org effective default (never blank).
  </behavior>
  <action>
    Create `ModelDefaultPreference.tsx` cloning JudgeModelPicker: a registry-only `<select>` bound to `getModelDefault().default_model`, offering `allowed_models` (dedupe+sort; keep an unknown persisted value as "(current)"). Save-on-select → `setModelDefault(model)` → re-read (server-derived). The ALWAYS-ON 🔒 footer shows `effective_model`; when `locked`, disable the select and surface "Set by your administrator" naming the governed default (the operator lock — VIS-02's two-layer proof). Mount it in the personal-preferences area of Settings (the SEED-116 personal-preferences home — the profile/Settings surface, NOT the operator Control Room). Add `ModelDefaultPreference.test.tsx` covering the four behaviors (allowed-only options, save-on-select re-read, locked→disabled+footer, unset→effective footer).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && npx vitest run src/components/settings/ModelDefaultPreference.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>The model-default picker offers only allowed models, saves-on-select + re-reads, and disables + names the governed default when locked; mounted in Settings; test green.</done>
  <acceptance_criteria>
    - `grep -n "locked" frontend/src/components/settings/ModelDefaultPreference.tsx` shows the select disabling + the footer lock copy.
    - The select options come from `allowed_models` only; an unknown persisted value renders as "(current)" (round-trip safe).
    - `npx vitest run src/components/settings/ModelDefaultPreference.test.tsx` passes; `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Minimal greenlist admin surface (role audience on the Control Room visibility toggle)</name>
  <files>frontend/src/components/admin/ControlRoomPage.tsx</files>
  <read_first>
    - frontend/src/components/admin/ControlRoomPage.tsx (the existing binary feature-visibility toggle — the control to extend)
    - frontend/src/lib/api.ts (setFeatureAudience from Task 1)
    - RESEARCH.md § Pattern 3 (greenlist admin shape — minimal; groups deferred)
  </read_first>
  <action>
    Extend the shipped feature-visibility control in ControlRoomPage.tsx to a MINIMAL greenlist editor: alongside the existing Everyone/Operators choices, allow selecting a "role" audience and picking one or more of the 4-tier roles (member / dept-admin / org-admin / super-admin) to greenlist, wired to `setFeatureAudience(feature, 'role', roles)`. Keep it minimal (a small multi-select or role chips) — rich group management is explicitly deferred (CONTEXT). The write is API-enforced (Plan 03 validates roles ⊆ the 4-tier set); this control is render-only. Reuse the shipped toggle styling; no new package.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && npx tsc --noEmit</automated>
  </verify>
  <done>The Control Room feature-visibility control can set a role audience with a roles[] selection via setFeatureAudience; minimal surface (groups deferred); tsc clean.</done>
  <acceptance_criteria>
    - `grep -n "setFeatureAudience" frontend/src/components/admin/ControlRoomPage.tsx` matches the role-audience write.
    - The role selection is constrained to the 4-tier set; the write goes through setFeatureAudience (server-validated, Plan 03).
    - No new dependency import; `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → PUT /me/preferences | Client model choice is validated ∈ the allowed set server-side; the lock is server-enforced |
| operator → greenlist toggle | Role selection is client render; the API allowlist-validates roles (Plan 03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-167-13b | Elevation | user picks a disallowed model in the picker | mitigate | The select offers only allowed_models; the server (Plan 04) re-validates on PUT (400) and the overlay re-checks at read time — the client cannot widen the allowed set. |
| T-167-14b | Tampering | user edits a locked default via the client | mitigate | The 🔒 footer disables the select when locked; the server honors the lock regardless of the client (Plan 04). The disable is courtesy; the server is the wall. |
| T-167-10b | Elevation | operator sets an invalid role greenlist | mitigate | setFeatureAudience roles are allowlist-validated server-side (Plan 03, 400 on a bad role); the client control is render-only. |
| T-167-SC | Tampering | npm installs | mitigate | No new package — clones JudgeModelPicker + extends the shipped Control Room toggle. `grep` acceptance asserts no new dep import. |
</threat_model>

<verification>
- `npx vitest run src/components/settings/ModelDefaultPreference.test.tsx` + `npx tsc --noEmit` green.
- The picker is registry-only with a lock-honoring 🔒 footer; the greenlist toggle writes role audiences; no new package.
</verification>

<success_criteria>
- The per-user model-default picker proves the SEED-116 two-layer pattern (VIS-02 / D-167-04) — allowed-set only + honest operator lock.
- The minimal greenlist admin surface sets role audiences (VIS-01); rich group management stays deferred.
</success_criteria>

<output>
Create `.planning/phases/167-invitations-roles-greenlists-jit-per-user-prefs/167-07-SUMMARY.md` when done.
</output>
