---
phase: 167-invitations-roles-greenlists-jit-per-user-prefs
plan: 03
type: execute
wave: 2
depends_on: [167-01]
files_modified:
  - backend/app/models/user_settings.py
  - backend/app/dependencies.py
  - backend/app/api/features.py
  - backend/app/api/admin.py
  - backend/tests/test_167_greenlist.py
autonomous: true
requirements: [VIS-01]
must_haves:
  truths:
    - "D-167-06: the feature_visibility JSONB map extends from {audience: everyone|operators} to {audience: role, roles:[...]} with ZERO migration — the shape mig 098 was built for; NO new table"
    - "D-167-06: require_visible is EXTENDED in place, never forked — it stays a no-op for operators AND for everyone-audience features (Deep/chat carve-outs byte-identical); the role branch is added to the SAME resolver"
    - "VIS-01: a user whose org role is NOT greenlisted for a role-audience feature is denied (fail-closed); a greenlisted role is allowed"
    - "D-167-06: precedence-merge (Glean) — highest role wins the primary tier, union for secondary group grants; an unknown/malformed audience record falls back to the hardcoded safe-deny default (_GOVERNED_FEATURES), never raises"
    - "VIS-01: the GET /features effective map and the require_visible gate resolve the SAME role greenlist so the UI hide and the API refusal can never disagree"
  artifacts:
    - path: "backend/app/models/user_settings.py"
      provides: "feature_audience extended to 'role'; set_feature_visibility accepts a role record; resolve_feature_access precedence-merge helper"
      contains: "role"
    - path: "backend/app/dependencies.py"
      provides: "require_visible role-greenlist branch (fail-closed, operator/everyone no-op preserved)"
      contains: "feature_audience"
    - path: "backend/app/api/features.py"
      provides: "get_effective_features role-greenlist resolution"
    - path: "backend/app/api/admin.py"
      provides: "PUT /admin/visibility accepts the 'role' audience + roles[] (allowlist-validated)"
    - path: "backend/tests/test_167_greenlist.py"
      provides: "granted/ungranted role, operator+everyone no-op, precedence merge, fail-closed default tests"
  key_links:
    - from: "backend/app/dependencies.py::require_visible"
      to: "backend/app/models/user_settings.py::resolve_feature_access"
      via: "the one swappable audience boundary (SEED-115), extended not forked"
      pattern: "resolve_feature_access"
    - from: "backend/app/api/admin.py::set_visibility"
      to: "backend/app/models/user_settings.py::set_feature_visibility"
      via: "allowlist-validated role record → atomic JSONB || merge"
      pattern: "set_feature_visibility"
---

<objective>
Generalize the shipped binary feature-visibility (`{audience: everyone|operators}`) into per-feature role greenlists (`{audience: role, roles:[...]}`) by EXTENDING the ONE swappable resolver — `feature_audience()` + `require_visible` + the effective-features map + the `set_feature_visibility` writer + the `PUT /admin/visibility` write endpoint — with a Glean precedence-merge. ZERO migration.

Purpose: VIS-01. The `feature_visibility` JSONB (mig 098) was deliberately shaped to extend to roles with no DDL (D-167-06). The RED LINE: `require_visible` is the ONE audience boundary (its own docstring names SEED-115) — GROW the role branch in place, never fork it; operators and everyone-audience features stay byte-identical no-ops.
Output: `user_settings.py` (resolver + writer), `dependencies.py` (require_visible role branch), `features.py` (map), `admin.py` (write endpoint), `test_167_greenlist.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/167-invitations-roles-greenlists-jit-per-user-prefs/167-CONTEXT.md
@.planning/phases/167-invitations-roles-greenlists-jit-per-user-prefs/167-RESEARCH.md
@.planning/phases/167-invitations-roles-greenlists-jit-per-user-prefs/167-PATTERNS.md
@backend/app/api/features.py

<interfaces>
<!-- Extracted from the codebase. Extend these in place — do NOT fork. -->

backend/app/models/user_settings.py:
  _GOVERNED_FEATURES (989-994): {"skill_studio":"operators","model_management":"operators","workflow_authoring":"everyone","governance_health":"everyone"} — the cold-default safe-deny map.
  feature_audience(feature) -> str (997-1015): reads load_app_settings().feature_visibility[feature]["audience"]; returns it ONLY if in ("everyone","operators"); else _GOVERNED_FEATURES.get(feature,"operators"). NEVER raises. Guards isinstance(rec, dict) (Pitfall 5 double-serialization already handled by _build_settings_from_row).
  set_feature_visibility(feature, audience) -> bool (1018-1038): atomic `coalesce(feature_visibility,'{}'::jsonb) || $1::jsonb` merge of {feature: {"audience": audience}}; invalidate_settings_cache().

backend/app/dependencies.py:
  require_visible(feature) (450-476): factory → async _dep(current_user=Depends(get_current_user)); is_operator no-op FIRST; then `if feature_audience(feature)=="everyone": return`; else 403. feature_audience is lazy-imported inside the closure (avoids the user_settings→deps cycle).
  get_active_org_id stashes request.state.org_role (525-577) — the caller's role in the active org (when the request went through it). _has_org_permission (504-522) runs current_user_has_permission AS THE CALLER.

backend/app/api/features.py::get_effective_features (34-48): {f: (op or feature_audience(f)=="everyone") for f in _GOVERNED_FEATURES}. Extend the bool derivation to also resolve the role greenlist so hide==refuse.

backend/app/api/admin.py::set_visibility (953-982): PUT /admin/visibility; validates feature ∈ _VISIBILITY_FEATURES and audience ∈ _VISIBILITY_AUDIENCES (400 on unknown), then set_feature_visibility(feature, audience). operator_audit_floor. Extend the audience allowlist to include 'role' + accept + validate a roles[] payload (roles ⊆ the 4-tier set).

The 4-tier role set: {'super-admin','org-admin','dept-admin','member'} (mig 104 CHECK).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend feature_audience + set_feature_visibility to the 'role' audience + precedence-merge resolver</name>
  <files>backend/app/models/user_settings.py</files>
  <read_first>
    - backend/app/models/user_settings.py:983-1038 (_GOVERNED_FEATURES + feature_audience + set_feature_visibility — the exact functions to extend)
    - backend/app/models/user_settings.py:8-18 (the header: global app_settings writers stay service-role — keep this for the greenlist writer which is a GLOBAL feature-visibility map, NOT per-user)
    - RESEARCH.md § Pattern 3 (the resolve_feature_access example + the highest-role-wins/union precedence)
  </read_first>
  <action>
    Extend `feature_audience(feature)`: accept `"role"` as a recognized audience value in the branch that currently returns only `("everyone","operators")` — return `"role"` when the stored record's audience is `"role"`; the fail-closed fallback to `_GOVERNED_FEATURES` (unknown → "operators") and the no-raise posture stay unchanged. The `roles:[...]`/`groups:[...]` lists are read by the resolver below, not by feature_audience (which returns only the enum).

    Add `resolve_feature_access(feature: str, caller_role: str, caller_groups: set[str]) -> bool` (the pure in-memory decision, the Glean precedence-merge): read the stored record; `everyone → True`; `operators → False` (the operator no-op is handled upstream by is_operator); `role → True iff caller_role ∈ record.roles OR (caller_groups ∩ record.groups)` — highest-role-wins for the primary tier, union for the secondary group grants; unknown/malformed record → safe-deny False (matches the cold default). Reuse the existing dict-or-str guard (Pitfall 5) — do NOT add a second parse path. NEVER raise.

    Extend `set_feature_visibility` to accept the role record: change its signature to accept an audience value plus optional `roles`/`groups` (e.g. `set_feature_visibility(feature, audience, roles=None, groups=None)`), serialize `{feature: {"audience": audience, "roles": roles or [], "groups": groups or []}}` through the SAME atomic `||` merge, and keep `invalidate_settings_cache()`. The caller (admin.py) validates feature/roles against code allowlists BEFORE calling — this function still only serializes the validated record ($1::jsonb bind, SQLi-safe). This GLOBAL feature-visibility writer STAYS service-role/pool (it has no per-user scope — the user_settings.py:8-18 rule; contrast the VIS-02 per-user writer in Plan 04).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && venv/Scripts/python -c "from app.models.user_settings import feature_audience, resolve_feature_access, set_feature_visibility; assert resolve_feature_access.__code__.co_argcount>=3"</automated>
  </verify>
  <done>feature_audience recognizes 'role'; resolve_feature_access applies highest-role-wins + union, fail-closed; set_feature_visibility serializes the role record via the atomic || merge. No fork of the existing functions.</done>
  <acceptance_criteria>
    - `grep -n "\"role\"\|'role'" backend/app/models/user_settings.py` matches inside feature_audience AND resolve_feature_access.
    - resolve_feature_access returns False for an unknown/malformed record and for a caller_role not in roles with no group intersection (fail-closed).
    - set_feature_visibility still uses `coalesce(feature_visibility, '{}'::jsonb) || $1::jsonb` (atomic merge preserved, no whole-column SET).
    - `grep -c "def feature_audience\|def set_feature_visibility" backend/app/models/user_settings.py` == 2 (extended, not duplicated).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: require_visible role branch + effective-features map + PUT /admin/visibility role write + tests</name>
  <files>backend/app/dependencies.py, backend/app/api/features.py, backend/app/api/admin.py, backend/tests/test_167_greenlist.py</files>
  <read_first>
    - backend/app/dependencies.py:450-476 (require_visible — the ONE swappable boundary; extend the closure, keep is_operator + everyone no-ops)
    - backend/app/api/features.py:34-48 (get_effective_features)
    - backend/app/api/admin.py:945-982 (VisibilityUpdate model + set_visibility endpoint + the _VISIBILITY_FEATURES/_VISIBILITY_AUDIENCES allowlists)
    - backend/tests/test_148_require_visible.py + test_148_visibility_cold_default.py (the VIS-01 test precedent to extend)
  </read_first>
  <behavior>
    - require_visible(feature) stays a no-op for an operator and for an everyone-audience feature (byte-identical carve-out).
    - For a role-audience feature: a caller whose org role IS greenlisted passes; a caller whose role is NOT greenlisted gets 403.
    - Precedence-merge: a caller holding the highest greenlisted role passes the primary tier; a secondary group grant is a union (either grants access).
    - A cold cache / unknown feature / malformed record → safe-deny (fail-closed), never a raise.
    - GET /features returns the SAME per-feature bool the gate would enforce (hide == refuse).
    - PUT /admin/visibility with audience='role' + roles=['org-admin'] is accepted; roles outside the 4-tier set → 400.
  </behavior>
  <action>
    Extend `require_visible`'s closure (dependencies.py): after the `is_operator` no-op and the `everyone` no-op, add — when `feature_audience(feature) == "role"` — a resolution of the caller's org role(s) and a call to `resolve_feature_access(feature, caller_role, caller_groups)`; pass → return, else the existing 403. Resolve the caller's role fail-closed: prefer `request.state.org_role` when the request already went through get_active_org_id; otherwise resolve the caller's highest role across their memberships via a lightweight read (a helper that queries org_members for `user_id = auth.uid()` on a user-JWT connection, or reuses the resolved active-org role). On ANY resolution failure → deny (fail-closed). caller_groups is an empty/extensible set this phase (RESEARCH OQ2 — groups table deferred; the resolver handles roles + an empty groups set). Keep feature_audience/resolve_feature_access lazy-imported inside the closure (the deps↔user_settings cycle).

    Extend `get_effective_features` (features.py): derive each governed feature's bool as `op or feature_audience(f)=="everyone" or (feature_audience(f)=="role" and resolve_feature_access(f, caller_role, caller_groups))` so the map matches the gate exactly.

    Extend `PUT /admin/visibility` (admin.py): add `"role"` to the audience allowlist and a `roles: list[str] = []` field on VisibilityUpdate; validate `roles ⊆ {'super-admin','org-admin','dept-admin','member'}` (400 on a bad role) BEFORE calling `set_feature_visibility(feature, audience, roles=roles)`. Keep the operator_audit_floor + the plain audit label.

    Author `test_167_greenlist.py` (extend the test_148 precedent) covering all six behaviors.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && venv/Scripts/python -m pytest tests/test_167_greenlist.py -x -q</automated>
  </verify>
  <done>require_visible grants greenlisted roles + denies ungranted (fail-closed) while keeping operator/everyone byte-identical; the /features map matches the gate; PUT /admin/visibility accepts allowlist-validated role audiences. Tests green.</done>
  <acceptance_criteria>
    - `grep -n "resolve_feature_access" backend/app/dependencies.py` matches inside the require_visible closure (extended, not a new function).
    - `grep -c "def require_visible" backend/app/dependencies.py` == 1 (not forked).
    - require_visible still returns early for is_operator AND for `feature_audience(feature)=="everyone"` (the carve-out is unchanged).
    - admin.py validates roles ⊆ the 4-tier set with a 400 on a bad value; `grep -n "'role'\|\"role\"" backend/app/api/admin.py` matches the audience allowlist extension.
    - `pytest tests/test_167_greenlist.py -q` exits 0 covering granted/ungranted/operator-no-op/everyone-no-op/precedence/fail-closed.
    - `pytest tests/test_148_require_visible.py tests/test_148_visibility_cold_default.py -q` still green (no VIS-01 regression).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → governed feature endpoint | The client feature map is render-only; require_visible is the sole authority |
| operator → PUT /admin/visibility | Free-text feature/role must never reach SQL; code allowlists gate before the JSONB write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-167-10 | Elevation | greenlist bypass via forged client flag | mitigate | require_visible/resolve_feature_access are the sole authority; the GET /features map is render-only (T-166-07 precedent); hide==refuse by sharing the resolver. |
| T-167-11 | Elevation | resolver accidentally widens (fail-open) | mitigate | resolve_feature_access is fail-closed: unknown/malformed record → False; role resolution failure → deny; operators/everyone no-op unchanged (byte-identical). Precedence-merge grants ONLY the greenlisted roles/groups. |
| T-167-12 | Tampering | SQLi via role/feature in the visibility write | mitigate | admin.py validates feature ∈ _VISIBILITY_FEATURES and roles ⊆ the 4-tier set (400 on unknown); set_feature_visibility serializes only the validated record via a $1::jsonb bind. |
| T-167-SC | Tampering | npm/pip installs | accept | No new packages — resolver + writer extension only. No install task. |
</threat_model>

<verification>
- `pytest tests/test_167_greenlist.py -x -q` green; `pytest tests/test_148_*` green (no regression).
- require_visible is extended, not forked (grep count == 1); ZERO migration.
</verification>

<success_criteria>
- Greenlists resolve through the SAME extended require_visible with a fail-closed precedence-merge (VIS-01 / D-167-06); operator + everyone carve-outs byte-identical.
- The write endpoint accepts allowlist-validated role audiences; the /features map matches the gate.
</success_criteria>

<output>
Create `.planning/phases/167-invitations-roles-greenlists-jit-per-user-prefs/167-03-SUMMARY.md` when done.
</output>
