---
phase: 167-invitations-roles-greenlists-jit-per-user-prefs
plan: 05
type: execute
wave: 3
depends_on: [167-02]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/components/org/InviteMemberDialog.tsx
  - frontend/src/components/org/InvitationsTab.tsx
  - frontend/src/components/org/OrgAdminShell.tsx
  - frontend/src/components/org/OrgMembersTab.tsx
  - frontend/src/components/org/InvitationsTab.test.tsx
  - frontend/src/components/org/OrgMembersTab.test.tsx
autonomous: true
requirements: [INV-01, INV-02]
must_haves:
  truths:
    - "D-167-07: the 166 OrgAdminShell 'Invitations & Roles' LockedTab flips locked→live and mounts InvitationsTab — the invitations home is the org shell (NOT the operator Phase-148 roster)"
    - "D-167-03: the invite modal role picker offers Member (default) + Org-admin; Dept-admin is greyed/disabled until Phase 169"
    - "D-167-02: on send success the raw invite LINK is surfaced for copy/share (link-first) — no email service required by default"
    - "INV-01: adoption-state chips (not-yet-invited / pending / active) render on the roster reusing the 166 chip vocabulary (org-indigo primary token; amber reserved)"
    - "the invitations UI reuses the 166 shell-owns-fetch / pure-leaf split — InvitationsTab is a pure leaf; api client fns auto-carry X-Org-Id via getAuthHeaders"
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "sendInvitation/listInvitations/resendInvitation/revokeInvitation/acceptInvitation + Invitation/AdoptionState types"
      contains: "acceptInvitation"
    - path: "frontend/src/components/org/InviteMemberDialog.tsx"
      provides: "invite modal (email + role Member/Org-admin, Dept-admin greyed) + copy-link on success"
    - path: "frontend/src/components/org/InvitationsTab.tsx"
      provides: "pure-leaf invitations home — pending list + resend/revoke + send affordance"
    - path: "frontend/src/components/org/OrgAdminShell.tsx"
      provides: "Invitations & Roles tab locked→live (TABS + body switch)"
    - path: "frontend/src/components/org/OrgMembersTab.tsx"
      provides: "adoption-state chips on the roster rows"
  key_links:
    - from: "frontend/src/components/org/OrgAdminShell.tsx"
      to: "frontend/src/components/org/InvitationsTab.tsx"
      via: "activeTab === 'invitations' body branch (locked→live)"
      pattern: "InvitationsTab"
    - from: "frontend/src/lib/api.ts"
      to: "POST /org/invitations"
      via: "getAuthHeaders() auto-injects X-Org-Id"
      pattern: "org/invitations"
---

<objective>
Make the 166 "Invitations & Roles" LockedTab LIVE: an InviteMemberDialog (email + role picker, link-first copy), a pure-leaf InvitationsTab (pending list + resend/revoke), adoption-state chips on the Members roster, and the api-client fns — reusing the shipped 166 design system (band/roster/chip/dialog patterns) with no new design.

Purpose: INV-01 UI (D-167-07 — the org shell is the invitations home, not the operator roster). Extends the shipped 166 components; the invite modal clones CreateLinkDialog, InvitationsTab clones OrgMembersTab (pure leaf), adoption chips reuse the 166 chip vocabulary. acceptInvitation is added here too (consumed by Plan 06 — no api.ts conflict).
Output: `api.ts` (5 fns + types), `InviteMemberDialog.tsx`, `InvitationsTab.tsx`, `OrgAdminShell.tsx` (flip), `OrgMembersTab.tsx` (chips), tests.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/167-invitations-roles-greenlists-jit-per-user-prefs/167-CONTEXT.md
@.planning/phases/167-invitations-roles-greenlists-jit-per-user-prefs/167-PATTERNS.md
@frontend/src/components/org/OrgAdminShell.tsx
@frontend/src/components/org/OrgMembersTab.tsx

<interfaces>
<!-- Shipped 166 seams to clone/extend. -->

frontend/src/lib/api.ts:
  getAuthHeaders() (108-116): auto-injects `X-Org-Id` (D-166-06) — every new fn gets the active-org header for free.
  ApiError on non-OK (4583); defensive envelope unwrap `?? fallback` (4585-4590).
  getOrgMembers/getOrgAudit + OrgMember/OrgMembersPage types (4484-4626) — the exact org-client shape to mirror.

frontend/src/components/org/OrgAdminShell.tsx:
  TABS (73-101): the `invitations` entry is `locked: true` (77-82) with a lockedDescription. Flip to `locked: false`; drop lockedDescription.
  Body switch (269-287): add an `activeTab === 'invitations'` branch BEFORE the final `<LockedTab/>` fallthrough (285-286), mounting <InvitationsTab/>. Mirror the lazy per-tab fetch (fetchMembers/fetchAudit at 138-160, the alive.current guard at 134, the honest-degrade .catch, the per-tab effect at 174-178). The shell owns the fetch; the leaf is pure.
  useOrg() gives activeOrgId/orgs/role/canManage; OrgBand.recordingPulse is optional (may pulse on a successful invite).

frontend/src/components/org/OrgMembersTab.tsx:
  roleBadge (44-48) + chip markup (146-158): the admin chip (◆ + `border-primary/30 bg-primary/10 text-primary`) vs the muted chip (`border-border bg-muted/40 text-muted-foreground`) — reuse for adoption states. The read-only banner (81-87) points at the locked tab — update/remove it once invites are live.
  Pure leaf: props in, DOM out; the shell owns fetch + search state.

frontend/src/components/relationships/CreateLinkDialog.tsx (the invite-modal analog):
  shadcn Dialog imports (26-33) — NO new package (166 established @radix-ui/react-popover is NOT installed; use the shipped Dialog/DropdownMenu).
  reset-on-open effect (71-82); segmented role chips (180-203) → reuse for Member/Org-admin with Dept-admin greyed; confirm handler + 422-vs-transient discrimination (134-157); onCreated → parent re-fetch (50-51, re-fetch-not-optimistic).

Plan 02 backend contracts: POST /org/invitations {email,role} → {link, invitation}; GET /org/invitations → {invitations:[{id,email,role,status,expires_at,...}]}; POST /org/invitations/{id}/resend → {link}; DELETE /org/invitations/{id}; POST /org/invitations/accept {token} → {org_id,role,joined}. /org/members returns adoption state per row.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: api.ts invitation client fns + types (send/list/resend/revoke/accept)</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts:108-116 (getAuthHeaders — X-Org-Id auto-inject), :4484-4626 (getOrgMembers/getOrgAudit + OrgMember/OrgMembersPage types — the exact shape to mirror)
    - Plan 02's org.py endpoints (the request/response contracts)
  </read_first>
  <action>
    Add typed client fns to api.ts, mirroring getOrgMembers exactly (getAuthHeaders → X-Org-Id auto-carried; ApiError on non-OK; defensive `?? fallback` envelope unwrap):
    - `Invitation` interface `{id, email, role, status, expires_at, invited_by?, created_at}` and an `AdoptionState = 'not-yet-invited' | 'pending' | 'active'` type; extend `OrgMember` with an optional adoption `state` field (server-derived).
    - `sendInvitation(email, role) -> {link, invitation}` (POST /org/invitations).
    - `listInvitations(status?) -> Invitation[]` (GET /org/invitations).
    - `resendInvitation(id) -> {link}` (POST /org/invitations/{id}/resend).
    - `revokeInvitation(id) -> void` (DELETE /org/invitations/{id}).
    - `acceptInvitation(token) -> {org_id, role, joined}` (POST /org/invitations/accept) — consumed by Plan 06 (added here to avoid an api.ts merge conflict; note: accept sends NO X-Org-Id dependency — the org comes from the token).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && npx tsc --noEmit</automated>
  </verify>
  <done>Five invitation client fns + Invitation/AdoptionState types exist; each uses getAuthHeaders + ApiError + defensive unwrap; tsc clean.</done>
  <acceptance_criteria>
    - `grep -n "sendInvitation\|listInvitations\|resendInvitation\|revokeInvitation\|acceptInvitation" frontend/src/lib/api.ts` shows all five.
    - `grep -n "org/invitations" frontend/src/lib/api.ts` matches; each fn calls getAuthHeaders().
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: InviteMemberDialog (email + role picker, link-first) + InvitationsTab (pure leaf)</name>
  <files>frontend/src/components/org/InviteMemberDialog.tsx, frontend/src/components/org/InvitationsTab.tsx</files>
  <read_first>
    - frontend/src/components/relationships/CreateLinkDialog.tsx (whole file — Dialog shell, reset-on-open, segmented chips, confirm + error discrimination, onCreated re-fetch)
    - frontend/src/components/org/OrgMembersTab.tsx (whole file — the pure-leaf posture, section wrapper, loading/empty states, row shape)
    - frontend/src/lib/api.ts (Task 1 fns)
  </read_first>
  <action>
    `InviteMemberDialog.tsx` — clone CreateLinkDialog's shipped shadcn Dialog shell (no new package). Fields: an email input + a segmented role picker offering **Member (default)** and **Org-admin**, with **Dept-admin greyed/disabled** (a plain "available once departments ship" hint — never a lying enabled control; D-167-03). Reset-on-open. Confirm → `sendInvitation(email, role)`; on success surface the returned `link` with a copy affordance (link-first, D-167-02) and call `onCreated` so the parent re-fetches (re-fetch-not-optimistic). 422-vs-transient error discrimination + a loading state on the footer button.

    `InvitationsTab.tsx` — clone OrgMembersTab's pure-leaf posture (props in, DOM out; the shell owns fetch). Render: a "Send invite" affordance (opens InviteMemberDialog), the pending/accepted/expired/revoked invitation list (from the shell's fetched invitations), and per-row resend/revoke actions (`resendInvitation`/`revokeInvitation` via callbacks). Use the org-indigo `primary` accent (never amber — reserved for operator). Loading/empty states mirror OrgMembersTab.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && npx tsc --noEmit</automated>
  </verify>
  <done>InviteMemberDialog (email + Member/Org-admin picker, Dept-admin greyed, copy-link on success) and InvitationsTab (pure leaf: pending list + resend/revoke + send) exist; no new package; tsc clean.</done>
  <acceptance_criteria>
    - `grep -n "Dept-admin\|dept-admin" frontend/src/components/org/InviteMemberDialog.tsx` shows the greyed/disabled option (not an active choice).
    - `grep -n "link" frontend/src/components/org/InviteMemberDialog.tsx` shows the copy-link surfacing on success (link-first).
    - `grep -ci "amber" frontend/src/components/org/InvitationsTab.tsx frontend/src/components/org/InviteMemberDialog.tsx` == 0 (org-indigo only; amber reserved).
    - No new dependency import (`@radix-ui/react-popover` etc.); uses the shipped `@/components/ui/dialog`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: OrgAdminShell locked→live flip + OrgMembersTab adoption chips + tests</name>
  <files>frontend/src/components/org/OrgAdminShell.tsx, frontend/src/components/org/OrgMembersTab.tsx, frontend/src/components/org/InvitationsTab.test.tsx, frontend/src/components/org/OrgMembersTab.test.tsx</files>
  <read_first>
    - frontend/src/components/org/OrgAdminShell.tsx:73-101 (TABS — flip the invitations entry), :138-160 (the guarded fetchers to mirror for invitations), :174-178 (the lazy per-tab effect), :265-287 (the body switch)
    - frontend/src/components/org/OrgMembersTab.tsx:44-48 (roleBadge), :81-87 (the stale read-only banner), :146-158 (the chip markup to reuse for adoption states)
    - frontend/src/components/org/OrgAdminShell.test.tsx (the shell test precedent — locked-tab + live-tab assertions)
  </read_first>
  <behavior>
    - The Invitations & Roles tab is LIVE (not a LockedTab) and mounts InvitationsTab.
    - Opening the Invitations tab lazily fetches invitations (shell-owned fetch, alive.current guard, honest-degrade .catch).
    - The Members roster shows an adoption-state chip per row (pending vs active) reusing the 166 chip vocabulary.
    - The old "this roster is read-only / coming soon" banner is gone (invites are live).
  </behavior>
  <action>
    In OrgAdminShell.tsx: flip the `invitations` TABS entry `locked: true` → `locked: false` and drop its `lockedDescription`. Add shell-owned invitations state + a guarded lazy fetcher (`listInvitations`, mirroring fetchMembers with the alive.current guard + honest-degrade .catch + the per-tab effect keyed on `activeTab === 'invitations'`). Add an `activeTab === 'invitations'` branch to the body switch (before the LockedTab fallthrough) mounting `<InvitationsTab .../>` with the fetched invitations + resend/revoke/send callbacks that re-fetch on success. Optionally wire `OrgBand.recordingPulse` on a successful invite (a write now happens).

    In OrgMembersTab.tsx: add adoption-state chips to the roster rows (map the server-derived `state` to a chip via the existing chip markup — pending = muted/indigo chip, active = the existing role chip); remove/update the stale read-only banner (81-87). Keep the pure-leaf posture.

    Add `InvitationsTab.test.tsx` (renders the send affordance + pending list + resend/revoke; Dept-admin greyed) and extend `OrgMembersTab.test.tsx` (adoption chip renders pending/active).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && npx vitest run src/components/org && npx tsc --noEmit</automated>
  </verify>
  <done>The Invitations & Roles tab is live and mounts InvitationsTab with shell-owned lazy fetch; the roster shows adoption chips; the stale banner is gone; org vitest + tsc green.</done>
  <acceptance_criteria>
    - `grep -n "invitations" frontend/src/components/org/OrgAdminShell.tsx` shows the TABS entry `locked: false` AND the body-switch InvitationsTab branch.
    - `grep -c "locked: true" frontend/src/components/org/OrgAdminShell.tsx` == 3 (was 4 — invitations is now live; SSO/Subscription/Retention stay locked).
    - The Members roster maps a server-derived adoption `state` to a chip (grep the chip markup reuse); the old read-only "coming soon" banner text is removed.
    - `npx vitest run src/components/org` passes (incl. the new InvitationsTab test + the OrgMembersTab adoption-chip test); `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser UI → /org/invitations | The UI is render-only; the org:invite gate + RLS (Plan 01/02) are the wall; client role/canManage flags never authorize |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-167-17 | Elevation | client renders invite affordances without permission | mitigate | InvitationsTab is gated behind the canManage-gated OrgAdminShell (render courtesy); every write is server-gated on org:invite (Plan 02). The client is not the boundary (T-166-12 precedent). |
| T-167-18 | Information Disclosure | raw token displayed/persisted client-side beyond the link | mitigate | The UI only surfaces the server-returned `link` (which contains the raw token) for copy; it never stores or logs the token separately. |
| T-167-19 | Spoofing | Dept-admin selectable before departments ship | mitigate | Dept-admin is a greyed/disabled option (not an active choice); the server also refuses non-{member,org-admin} roles (Plan 02). |
| T-167-SC | Tampering | npm installs | mitigate | No new package — reuses the shipped shadcn Dialog/DropdownMenu (166 established @radix-ui/react-popover is NOT installed). `grep` acceptance asserts no new dep import. |
</threat_model>

<verification>
- `npx vitest run src/components/org` + `npx tsc --noEmit` green.
- The Invitations & Roles tab is live; adoption chips render; link-first copy on send; no new package.
</verification>

<success_criteria>
- The 166 LockedTab is live (D-167-07); the invite modal offers Member/Org-admin (Dept-admin greyed, D-167-03); link-first copy (D-167-02); adoption chips on the roster (INV-01) in the 166 chip vocabulary.
</success_criteria>

<output>
Create `.planning/phases/167-invitations-roles-greenlists-jit-per-user-prefs/167-05-SUMMARY.md` when done.
</output>
