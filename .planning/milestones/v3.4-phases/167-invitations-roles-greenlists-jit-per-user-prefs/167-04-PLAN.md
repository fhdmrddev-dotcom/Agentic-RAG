---
phase: 167-invitations-roles-greenlists-jit-per-user-prefs
plan: 04
type: execute
wave: 3
depends_on: [167-03]
files_modified:
  - backend/app/models/user_settings.py
  - backend/app/services/run_model_resolution.py
  - backend/app/api/me_preferences.py
  - backend/app/api/threads.py
  - backend/app/main.py
  - backend/tests/test_167_prefs.py
autonomous: true
requirements: [VIS-02]
must_haves:
  truths:
    - "D-167-04: a user picks a default model WITHIN the operator/org-allowed enabled set — the revived user_settings.preferences layer (dead since mig 011); ZERO migration"
    - "D-167-04 (SEED-116 two-layer): the effective default resolves as user-preference (if set AND ∈ enabled allowed-set AND NOT operator-locked) → else the operator/org default llm_model; the operator lock is honored SERVER-SIDE"
    - "D-14 red line: the per-user overlay is a STRICT no-op when unset — Deep Mode / the shared send path stays byte-identical (an unset-preference send is byte-identical to today)"
    - "D-167-09 (SC#10): the chosen default routes through the SAME resolve_run_model provider resolution across OpenAI/Anthropic/Google/OpenRouter — VIS-02 changes WHICH model id is the default, not the routing (no per-provider fork)"
    - "D-v2.5-01: the per-user preference DB read never runs as a blocking supabase-py call inside the async handler — it uses an async seam (asyncpg / run_in_threadpool)"
    - "the per-user preferences write is RLS-scoped (user_settings row keyed on the caller) — NOT the global service-role app_settings writer path"
  artifacts:
    - path: "backend/app/models/user_settings.py"
      provides: "per-user preference read + the two-layer compose (allowed-set + operator lock)"
      contains: "preferences"
    - path: "backend/app/services/run_model_resolution.py"
      provides: "apply_user_model_default overlay helper (identity-preserving no-op when unset)"
      contains: "apply_user_model_default"
    - path: "backend/app/api/me_preferences.py"
      provides: "GET/PUT /me/preferences (per-user RLS read/write of the model default)"
      exports: ["router"]
    - path: "backend/app/api/threads.py"
      provides: "the ONE-line overlay call at the load_user_settings send seam (minimal G-5 in-place guard)"
    - path: "backend/tests/test_167_prefs.py"
      provides: "overlay-only-when-set-valid-unlocked, unset byte-identical, operator-lock-wins tests"
  key_links:
    - from: "backend/app/api/threads.py"
      to: "backend/app/services/run_model_resolution.py::apply_user_model_default"
      via: "one-line async overlay after load_user_settings (no-op when unset)"
      pattern: "apply_user_model_default"
    - from: "backend/app/api/me_preferences.py"
      to: "public.user_settings.preferences"
      via: "get_user_pg_connection RLS JSONB || merge keyed on auth.uid()"
      pattern: "preferences"
---

<objective>
Revive the dead `user_settings.preferences` column under the SEED-116 two-layer pattern: a user picks a default model WITHIN the operator/org-allowed enabled set, honoring an operator lock, and that default overlays the chat send path as a STRICT no-op when unset (Deep byte-identical). ZERO migration.

Purpose: VIS-02 — the first concrete instance of the two-layer preference pattern (D-167-04). The overlay seam is `load_user_settings(user_id)` at the send path (threads.py:837) which today IGNORES user_id and returns the global row. The RED LINE (D-14): the overlay must not touch the shared path when no preference is set. The read must not block an async handler (D-v2.5-01). The write is per-user RLS-scoped (NOT the global service-role settings writer).
Output: `user_settings.py` (read + compose), `run_model_resolution.py` (overlay helper), `me_preferences.py` (endpoint), a one-line `threads.py` guard, `main.py` registration, `test_167_prefs.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/167-invitations-roles-greenlists-jit-per-user-prefs/167-CONTEXT.md
@.planning/phases/167-invitations-roles-greenlists-jit-per-user-prefs/167-RESEARCH.md
@.planning/phases/167-invitations-roles-greenlists-jit-per-user-prefs/167-PATTERNS.md

<interfaces>
<!-- The exact VIS-02 seams, verified in-tree. -->

backend/app/models/user_settings.py:
  load_user_settings(user_id, supabase=None) -> UserEffectiveSettings (861-862): TODAY `return load_app_settings()` — ignores user_id (the two-layer overlay point).
  load_app_settings() (828-835) / load_app_settings_async() (838-845): the global effective settings; .llm_model is the default model, .available_models the enabled set.
  load_all_model_overrides() (462): enabled model_capabilities_overrides rows {model_id: row} — the operator/registry ENABLED allowed-set (149 registry `enabled` flag = shows-in-picker = offerable).
  user_settings.preferences jsonb DEFAULT '{}' (mig 011:27-28) — dead-but-present; the per-user store to revive.
  Header (8-18): GLOBAL app_settings writers stay service-role BECAUSE they have no per-user scope. VIS-02 is the INVERSE — per-user data → RLS user-JWT write.

backend/app/services/run_model_resolution.py::resolve_run_model (167-241): `resolved_model = body.model if body.model else user_settings.llm_model`. VIS-02 overlays user_settings.llm_model (the fallback) BEFORE this line. resolve_run_model routes the resolved model through get_model_capability_async → provider re-derivation (cross-provider, no fork).

backend/app/api/threads.py:832-849 (send_message): `_user_settings = load_user_settings(current_user["id"])` (:837) then `resolve_run_model(...)` (:849). This is the ONE-line overlay seam. threads.py is a G-5 hot file — the guard is a SINGLE additive call (no new endpoint here, no growth, no per-provider fork), mirroring the operator-approved 147/149 in-place-guard overrides.

backend/app/dependencies.py: get_user_pg_connection (user-JWT RLS write), get_current_user, run_in_threadpool (starlette.concurrency) for any supabase-py read.
backend/app/main.py:657-685 — router import + include block (register me_preferences.router).
frontend note: ChatArea sends body.model when the user picks in-composer, else falls back to the server default — the per-user default sets BOTH the picker initial value (Plan 07) AND this server-side fallback.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-user preference read + two-layer compose + GET/PUT /me/preferences endpoint</name>
  <files>backend/app/models/user_settings.py, backend/app/api/me_preferences.py, backend/app/main.py</files>
  <read_first>
    - backend/app/models/user_settings.py:8-18 (the service-role-vs-per-user header rule), :828-862 (load_app_settings / load_user_settings), :462 (load_all_model_overrides — the enabled allowed-set), :1018-1038 (set_feature_visibility — the JSONB || merge SHAPE to copy, but scope it PER-USER)
    - supabase/migrations/011_cleanup_user_settings.sql:27-28 (preferences column)
    - backend/app/api/settings.py::update_settings (the map-fields-to-updates flow to mirror shape)
    - backend/app/dependencies.py:158-176 (get_user_pg_connection — the RLS write connection)
  </read_first>
  <action>
    Add to `user_settings.py` a per-user read `async def load_user_model_default(user_id) -> str | None` that reads `user_settings.preferences->>'default_model'` for the caller (exact JSON key is discretion, D-167-04 — use `default_model`). Use an ASYNC seam (asyncpg pool with a `WHERE user_id = $1` filter — the belt-and-suspenders `.eq(user_id)` pattern the milestone keeps; or wrap a supabase-py read in run_in_threadpool). Fail-open on any read error → return None (no overlay → byte-identical).

    Add the two-layer compose `def compose_effective_model_default(user_pref: str | None, effective: UserEffectiveSettings, enabled_models: set[str], locked: bool) -> str`: return the operator/org default (`effective.llm_model`) UNLESS `user_pref` is set AND `user_pref in enabled_models` AND NOT `locked` — in which case return `user_pref`. This is the SEED-116 rule: operator governs the allowed-set (the 149 registry enabled flag = offerable) + the lock; the user picks within it. Read the operator lock from the EXISTING effective settings / app_settings JSONB (zero migration; the per-org organizations.settings JSONB is the natural later home — note it but do NOT add a migration).

    Create `backend/app/api/me_preferences.py` — `router = APIRouter(tags=["preferences"])`. `GET /me/preferences` returns `{default_model, effective_model, locked, allowed_models}` (the picker + 🔒 footer data for Plan 07). `PUT /me/preferences` body `{default_model: str | None}`: validate `default_model` ∈ the enabled allowed-set (400 otherwise) unless clearing (null), then write via `get_user_pg_connection` (RLS user-JWT) with a JSONB `||` merge keyed on `auth.uid()`: `UPDATE public.user_settings SET preferences = coalesce(preferences,'{}'::jsonb) || $1::jsonb WHERE user_id = auth.uid()`. This is a PER-USER RLS write (NOT the global service-role settings writer — the user_settings.py:8-18 inverse case). Register `me_preferences.router` in main.py (import + include, one-line Phase-167 comment).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && venv/Scripts/python -c "from app.main import app; p={r.path for r in app.routes}; assert '/me/preferences' in p, sorted(x for x in p if x.startswith('/me'))"</automated>
  </verify>
  <done>load_user_model_default (async, fail-open None) + compose_effective_model_default (two-layer, lock-honoring) exist; GET/PUT /me/preferences read/write user_settings.preferences on the RLS user-JWT connection; registered in main.py.</done>
  <acceptance_criteria>
    - `grep -n "preferences" backend/app/api/me_preferences.py` shows the JSONB || merge write; `grep -n "get_user_pg_connection\|auth.uid()" backend/app/api/me_preferences.py` matches (per-user RLS write, NOT service-role).
    - PUT validates default_model ∈ the enabled allowed-set (400 on an out-of-set value); null clears the override.
    - compose_effective_model_default returns effective.llm_model when locked OR pref not in the allowed-set OR unset.
    - No new migration; `grep -rn "supabase/migrations/112" .` finds nothing authored by this plan.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The overlay helper + the one-line send-path guard (Deep byte-identical when unset) + tests</name>
  <files>backend/app/services/run_model_resolution.py, backend/app/api/threads.py, backend/tests/test_167_prefs.py</files>
  <read_first>
    - backend/app/services/run_model_resolution.py:167-241 (resolve_run_model — where user_settings.llm_model is read as the fallback)
    - backend/app/api/threads.py:832-849 (the load_user_settings + resolve_run_model send seam — the G-5 hot file; ONE-line guard only)
    - backend/app/models/user_settings.py (Task 1: load_user_model_default + compose_effective_model_default)
    - the CLAUDE.md G-5 hot-file ledger + the STATE.md 147/149 in-place-guard override precedent
  </read_first>
  <behavior>
    - apply_user_model_default returns the SAME settings object (identity) when no preference is set → the send path is byte-identical to today (Deep red line, D-14).
    - When a valid, unlocked, in-allowed-set preference is set, the returned settings' llm_model equals the preference; resolve_run_model then uses it as the fallback (only when body.model is absent — an explicit in-composer pick still wins).
    - When the operator lock is ON, the preference is ignored and llm_model stays the operator/org default.
    - A per-user Anthropic default routes to Anthropic (cross-provider, D-167-09) — the provider is re-derived from the model id by resolve_run_model, no per-provider fork.
  </behavior>
  <action>
    Add `async def apply_user_model_default(request, current_user, user_settings) -> UserEffectiveSettings` to run_model_resolution.py (adjacent to resolve_run_model — NOT in threads.py). It: reads `load_user_model_default(current_user["id"])`, the enabled allowed-set (`load_all_model_overrides()` keys), and the operator lock; calls `compose_effective_model_default(...)`; if the composed model DIFFERS from `user_settings.llm_model`, returns `user_settings.model_copy(update={"llm_model": composed})`; OTHERWISE returns `user_settings` UNCHANGED (identity — the strict no-op that keeps Deep byte-identical, D-14 / Pitfall 4). Fail-open: any error → return `user_settings` unchanged. Wrap any blocking read in run_in_threadpool / use the async asyncpg path (D-v2.5-01).

    In threads.py, add ONE line immediately after `_user_settings = load_user_settings(current_user["id"])` (:837): `_user_settings = await apply_user_model_default(request, current_user, _user_settings)`. This is the minimal G-5 in-place guard (no new endpoint in threads.py, no growth beyond the one call, no per-provider fork — mirrors the operator-approved 147/149 overrides). Record the G-5 override in the SUMMARY.

    Author `test_167_prefs.py`: (a) unset preference → apply_user_model_default returns the SAME object (assert identity / byte-identical llm_model) — the Deep red-line proof; (b) valid unlocked in-set preference → llm_model overlaid; (c) operator lock ON → preference ignored; (d) preference NOT in the enabled allowed-set → ignored (falls back to operator default).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && venv/Scripts/python -m pytest tests/test_167_prefs.py -x -q</automated>
  </verify>
  <done>apply_user_model_default is identity-preserving when unset (Deep byte-identical), applies a valid unlocked in-set preference, honors the operator lock; threads.py carries the single-line guard; tests green.</done>
  <acceptance_criteria>
    - `grep -c "apply_user_model_default" backend/app/api/threads.py` == 1 (the single-line guard; no other threads.py growth).
    - `grep -n "model_copy" backend/app/services/run_model_resolution.py` matches (copy only when the model differs; else identity).
    - test_167_prefs.py asserts the unset case returns the byte-identical settings (the D-14 proof) AND the lock-wins case.
    - `pytest tests/test_167_prefs.py -q` exits 0; the existing run_model_resolution / provider-router tests stay green (no shared-path regression).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → PUT /me/preferences | Untrusted default_model; validated ∈ the enabled allowed-set before the RLS write |
| user preference → shared send path | The overlay must not widen beyond the operator/org allowed-set nor bypass the lock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-167-13 | Elevation | user picks a model outside the operator/org-allowed set | mitigate | PUT validates default_model ∈ the enabled allowed-set (400); the overlay compose re-checks membership at read time (defense in depth) so a later-disabled model falls back to the operator default. |
| T-167-14 | Tampering | user override bypasses the operator lock | mitigate | compose_effective_model_default honors the lock SERVER-SIDE — when locked, the preference is ignored regardless of the client. |
| T-167-15 | Tampering | overlay mutates the shared/Deep path when unset | mitigate | apply_user_model_default is identity-preserving when unset (returns the same object); fail-open on error. Test-locked (the D-14 byte-identical proof). |
| T-167-16 | DoS | blocking supabase-py read in the async send handler | mitigate | The per-user read uses an async asyncpg seam / run_in_threadpool (D-v2.5-01); fail-open so a read blip never blocks or breaks a send. |
| T-167-SC | Tampering | npm/pip installs | accept | No new packages — resolver overlay + a per-user endpoint over the existing preferences column. No install task. |
</threat_model>

<verification>
- `pytest tests/test_167_prefs.py -x -q` green (unset byte-identical, valid overlay, lock wins, out-of-set ignored).
- The send path is byte-identical when no preference is set (D-14); ZERO migration.
- The G-5 threads.py guard is a single additive line (recorded in the SUMMARY per the 147/149 override precedent).
</verification>

<success_criteria>
- Per-user model default revives user_settings.preferences under the two-layer pattern (VIS-02 / D-167-04); the operator lock is honored server-side.
- The overlay is a strict no-op when unset (D-14); the chosen default routes cross-provider (D-167-09); the write is per-user RLS-scoped.
</success_criteria>

<output>
Create `.planning/phases/167-invitations-roles-greenlists-jit-per-user-prefs/167-04-SUMMARY.md` when done.
</output>
