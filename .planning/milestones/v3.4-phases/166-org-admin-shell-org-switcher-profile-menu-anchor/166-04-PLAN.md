---
phase: 166-org-admin-shell-org-switcher-profile-menu-anchor
plan: 04
type: execute
wave: 3
depends_on: ["166-02", "166-03"]
files_modified:
  - frontend/src/components/org/OrgAdminShell.tsx
  - frontend/src/components/org/OrgAdminShell.test.tsx
autonomous: true
requirements: [ADMIN-01, ADMIN-04, ADMIN-05]
must_haves:
  truths:
    - "D-166-01 / ADMIN-01: the shell renders 3 LIVE tabs (Members read-only · Audit lighter · Settings org-config home) + 4 LockedTab 'coming soon' placeholders (Invitations & Roles · SSO · Subscription · Retention) — the locked copy names NO roadmap phase number"
    - "ADMIN-01: the shell owns the fetch (alive.current guard + lazy per-tab load + honest-degrade .catch keeping last-known values); the tabs are pure leaves receiving rows + callbacks"
    - "D-166-04 / ADMIN-04: the shell threads the audit page's scope flag into OrgAuditTab so the RLS-honest degrade renders (canAuditView false → own-only + banner)"
    - "ADMIN-05: the Settings tab mounts OrgSettingsTab as the org-config home behind the org:manage-gated shell"
    - "the shell reuses the 061-B Control-Room band+tabs shape tinted org-indigo (amber reserved for operator)"
  artifacts:
    - path: "frontend/src/components/org/OrgAdminShell.tsx"
      provides: "the org-admin band+tabs shell (fetch owner) composing OrgBand + the 3 live tabs + 4 LockedTab"
      min_lines: 80
  key_links:
    - from: "frontend/src/components/org/OrgAdminShell.tsx"
      to: "frontend/src/components/admin/LockedTab.tsx"
      via: "4 locked-tab placeholders wired title+description (no phase number)"
      pattern: "LockedTab"
    - from: "frontend/src/components/org/OrgAdminShell.tsx"
      to: "getOrgMembers / getOrgAudit (Plan 02 api.ts)"
      via: "lazy per-tab fetch on tab-open"
      pattern: "getOrg(Members|Audit)"
---

<objective>
Compose the org-admin shell: the org-indigo band + a 7-tab strip (3 live: Members / Audit / Settings; 4 locked: Invitations & Roles / SSO / Subscription / Retention), owning the lazy per-tab fetch and passing rows + the audit `scope` into the Plan-03 leaves. Reuse the shipped Control-Room band+tabs shape (061-B), tinted indigo.

Purpose: this is the manager-only surface reached via the rail Shield-mirror (Plan 05). It is the ADMIN-01 shell + the home for the ADMIN-04 audit degrade and the ADMIN-05 org-config Settings tab.
Output: OrgAdminShell.tsx + its test.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/166-org-admin-shell-org-switcher-profile-menu-anchor/166-CONTEXT.md
@.planning/phases/166-org-admin-shell-org-switcher-profile-menu-anchor/166-PATTERNS.md
@.planning/sketches/080-org-admin-shell/README.md

<interfaces>
<!-- The shell composition source. Clone the shape, re-tint indigo, gate on canManage. -->

frontend/src/components/admin/ControlRoomPage.tsx — the direct composition source:
  interface TabDef { id; label; locked; lockedDescription? }  (:104-110)
  const TABS: readonly TabDef[] = [...]                        (:116-135)  — the 3-live-then-locked shape
  shell-owns-fetch: alive.current guard (:231), lazy per-tab fetch (:392-395), honest-degrade .catch keeping last-known (:239-293)
  tablist render (:594-628) — role="tablist" on a <div> host (A11Y note :590); Lock glyph on locked tabs (:617); active bg-primary/10 → re-tint indigo
  body switch (:630-793) — activeTab==="x" ? <XTab.../> : … : <LockedTab title description/>  (final LockedTab fallthrough :792)

frontend/src/components/admin/LockedTab.tsx:5-11,18-26 — REUSED AS-IS. Props {title, description}. HARD RULE: description must NEVER name a roadmap phase number (a plan grep gate asserts absence). Wire exactly as ControlRoomPage.tsx:792.

Plan 03 leaves (import): OrgBand {orgName, role, onBack, recordingPulse?} ; OrgMembersTab {members, query, onQueryChange, canManage} ; OrgAuditTab {page (has scope), filters, onFiltersChange, onPageChange} ; OrgSettingsTab {…}.
Plan 02 (context + api): useOrg() → {activeOrgId, orgs, role, canManage, canAuditView, switchOrg} ; getOrgMembers(page,pageSize) ; getOrgAudit(filters,page,pageSize) → {…, scope}.

The 4 LOCKED tabs (D-166-01), copy with NO phase number:
  { id:"invitations", label:"Invitations & Roles", locked:true, lockedDescription:"Inviting people and managing roles is coming soon." }
  { id:"sso",          label:"SSO",          locked:true, lockedDescription:"Single sign-on setup is coming soon." }
  { id:"subscription", label:"Subscription", locked:true, lockedDescription:"Plan and billing management is coming soon." }
  { id:"retention",    label:"Retention",    locked:true, lockedDescription:"Data-retention controls are coming soon." }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shell skeleton — TABS + band + tablist + lazy fetch orchestration</name>
  <files>frontend/src/components/org/OrgAdminShell.tsx</files>
  <read_first>
    - frontend/src/components/admin/ControlRoomPage.tsx:104-143 (TabDef + TABS + page-size consts), :200-310 (alive.current guard + fetch + honest-degrade), :392-395 (lazy per-tab fetch), :594-628 (tablist render)
    - frontend/src/components/org/OrgBand.tsx (Plan 03 — the band leaf)
    - frontend/src/providers/OrgProvider.tsx (Plan 02 — useOrg)
  </read_first>
  <action>
    Create OrgAdminShell.tsx as the fetch-owning shell. Props `{ onBack: () => void }`; read the org value via `useOrg()` (activeOrgId, orgs, role, canManage, canAuditView). Define `const TABS` with 3 live (`members`, `audit`, `settings`) + the 4 locked entries above. Hold `activeTab` state; render `<OrgBand orgName={…} role={role} onBack={onBack} recordingPulse={…}/>` then the tablist (copy ControlRoomPage:594-628 — `role="tablist"` on a `<div>`, `Lock` glyph on locked tabs, active-tab treatment re-tinted to org-indigo, NOT amber). Own the fetch exactly like ControlRoomPage: an `alive.current` guard, a lazy per-tab fetch (`getOrgMembers` when the Members tab opens, `getOrgAudit` when the Audit tab opens — not on mount), and an honest-degrade `.catch` that keeps last-known values (never blanks the surface). Keep the tabs as pure leaves — the shell holds the rows/filters/pagination state and passes them down. Body switch comes in Task 2 (this task can render a temporary body placeholder for the live tabs to keep it compiling).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && npx tsc --noEmit</automated>
  </verify>
  <done>Shell renders OrgBand + a 7-entry tablist (3 live + 4 locked) with indigo accents; owns the alive.current-guarded lazy per-tab fetch; compiles.</done>
  <acceptance_criteria>
    - `grep -c "locked: true" frontend/src/components/org/OrgAdminShell.tsx` == 4 and `grep -c "locked: false" ...` == 3.
    - `grep -ci "amber" frontend/src/components/org/OrgAdminShell.tsx` == 0 (indigo zone).
    - `grep -n "getOrgMembers\|getOrgAudit" frontend/src/components/org/OrgAdminShell.tsx` shows lazy per-tab fetch calls.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Body switch — compose live leaves + wire 4 LockedTab (scope threaded)</name>
  <files>frontend/src/components/org/OrgAdminShell.tsx, frontend/src/components/org/OrgAdminShell.test.tsx</files>
  <read_first>
    - frontend/src/components/admin/ControlRoomPage.tsx:630-793 (body switch + the final LockedTab fallthrough :792)
    - frontend/src/components/admin/LockedTab.tsx:5-11,18-26 (prop contract + the no-phase-number rule)
    - frontend/src/components/org/{OrgMembersTab,OrgAuditTab,OrgSettingsTab}.tsx (Plan 03 leaves)
  </read_first>
  <behavior>
    - Test: opening the Members tab renders OrgMembersTab; opening Audit renders OrgAuditTab; opening Settings renders OrgSettingsTab.
    - Test: opening any of the 4 locked tabs renders LockedTab with its title + description.
    - Test: no locked-tab description contains a roadmap phase number (assert against a /Phase\s*\d|1[0-9]{2}/ pattern → none).
    - Test: when getOrgAudit returns scope==='own', OrgAuditTab receives that page and the degrade banner renders (canAuditView false path).
  </behavior>
  <action>
    Extend OrgAdminShell with the body switch (copy ControlRoomPage:630-793 shape): `activeTab === "members" ? <OrgMembersTab members={…} query onQueryChange canManage/> : activeTab === "audit" ? <OrgAuditTab page={auditPage} filters onFiltersChange onPageChange/> : activeTab === "settings" ? <OrgSettingsTab …/> : <LockedTab title={active.label} description={active.lockedDescription}/>`. The final LockedTab fallthrough handles all 4 locked tabs (T-146-10 — the description is the roadmap-number-free copy from TABS). Thread the audit page's `scope` straight through to OrgAuditTab so the RLS-honest degrade renders from server truth. Author OrgAdminShell.test.tsx covering the four behaviors (mock useOrg + getOrgMembers/getOrgAudit).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && npx vitest run src/components/org/OrgAdminShell.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>All 3 live tabs mount their leaves; all 4 locked tabs render LockedTab with phase-number-free copy; the audit scope threads through to the degrade banner; tests green.</done>
  <acceptance_criteria>
    - `grep -n "LockedTab" frontend/src/components/org/OrgAdminShell.tsx` matches the fallthrough wiring.
    - The test asserting "no locked description names a phase number" passes.
    - `grep -n "scope" frontend/src/components/org/OrgAdminShell.tsx` shows the audit page's scope threaded to OrgAuditTab.
    - `npx vitest run src/components/org/OrgAdminShell.test.tsx` passes; `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell → tabs | The shell fetches server-authz'd rows and passes them to pure leaves; the client renders only what the server (Plan 01) permitted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-166-11 | Information Disclosure | shell audit fetch | mitigate | The shell renders the server's `scope`-tagged page unchanged — it cannot request cross-member rows the server would deny (the server branches on org:audit_view). scope threading is test-locked. |
| T-166-12 | Spoofing | shell reachable without org:manage | accept (client not a boundary) | The shell mounts only behind the canManage-gated entry (Plan 05) AND every fetch it makes (/org/members, /org/audit) is server-gated on org:manage (Plan 01). A forced client mount shows chrome but the fetches 403. |
| T-166-13 | Tampering | locked-tab copy leaking a roadmap number | mitigate | The 4 lockedDescription strings are phase-number-free; a grep/regex test asserts absence (T-146-10). |
</threat_model>

<verification>
- `npx vitest run src/components/org/OrgAdminShell.test.tsx` green (tab mounts, locked copy, scope threading).
- `npx tsc --noEmit` exits 0.
- 3 live + 4 locked tabs; indigo zone (zero amber).
</verification>

<success_criteria>
- The shell composes the band + 3 live tabs + 4 honest locks (D-166-01, ADMIN-01), owns the lazy fetch, and threads the audit scope for the honest degrade (ADMIN-04).
</success_criteria>

<output>
Create `.planning/phases/166-org-admin-shell-org-switcher-profile-menu-anchor/166-04-SUMMARY.md` when done.
</output>
