---
phase: 166-org-admin-shell-org-switcher-profile-menu-anchor
plan: 03
type: execute
wave: 2
depends_on: ["166-02"]
files_modified:
  - frontend/src/components/org/OrgBand.tsx
  - frontend/src/components/org/OrgSettingsTab.tsx
  - frontend/src/components/org/OrgMembersTab.tsx
  - frontend/src/components/org/OrgAuditTab.tsx
  - frontend/src/components/org/OrgAuditTab.test.tsx
  - frontend/src/components/org/OrgMembersTab.test.tsx
autonomous: true
requirements: [ADMIN-01, ADMIN-04, ADMIN-05]
must_haves:
  truths:
    - "D-166-05: the OrgBand is org-indigo (never amber — amber stays reserved for the operator zone), carrying org name + an ORG ADMIN chip + the ◆ Org-admin / Member role badge + the 062-A 'every action recorded' marker + a plain-first ⌥ Technical-names toggle"
    - "D-166-04 / ADMIN-04: OrgAuditTab renders an explicit 'you see only your own activity (RLS)' banner + own-only rows when scope==='own' (canAuditView false) — never a silent empty list; cross-member rows only when scope==='all'"
    - "D-166-01 / ADMIN-01: OrgMembersTab is read-only — invite/role-edit affordances are ABSENT (not disabled buttons that lie), with a banner pointing at the locked Invitations tab"
    - "ADMIN-05: OrgSettingsTab is the light org-config home behind org:manage — the three-homes seam (bulk global-knob relocation stays deferred to v3.5, not built here)"
    - "the leaves are pure presentational (props in, DOM out) — no fetch/auth; the shell (Plan 04) owns the fetch and passes rows + callbacks"
  artifacts:
    - path: "frontend/src/components/org/OrgBand.tsx"
      provides: "org-indigo band leaf (org name + ORG ADMIN chip + role badge + recording marker + ⌥ Technical-names)"
    - path: "frontend/src/components/org/OrgMembersTab.tsx"
      provides: "read-only members roster (identity + role chip + search); no write affordances"
    - path: "frontend/src/components/org/OrgAuditTab.tsx"
      provides: "lighter audit list (chip filters, no CSV, single-source) + RLS-honest degrade banner"
    - path: "frontend/src/components/org/OrgSettingsTab.tsx"
      provides: "light org-config home leaf"
  key_links:
    - from: "frontend/src/components/org/OrgAuditTab.tsx"
      to: "the scope flag from GET /org/audit"
      via: "banner + row filtering when scope==='own'"
      pattern: "scope"
---

<objective>
Build the four presentational leaves of the org-admin shell: `OrgBand` (org-indigo identity band), `OrgMembersTab` (read-only roster), `OrgAuditTab` (lighter audit + RLS-honest degrade), `OrgSettingsTab` (light org-config home). Each is a pure props-in/DOM-out leaf cloned from its shipped operator analog with the amber→indigo re-tint and the writes stripped.

Purpose: these are the tabs the shell (Plan 04) composes. Isolating them keeps the load-bearing audit degrade (ADMIN-04) and the read-only-roster honesty (ADMIN-01) high-quality and independently testable.
Output: OrgBand, OrgMembersTab, OrgAuditTab, OrgSettingsTab + component tests.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/166-org-admin-shell-org-switcher-profile-menu-anchor/166-CONTEXT.md
@.planning/phases/166-org-admin-shell-org-switcher-profile-menu-anchor/166-PATTERNS.md
@.planning/sketches/080-org-admin-shell/README.md
@.planning/notes/settings-control-room-boundary.md

<interfaces>
<!-- Analogs to clone (amber→indigo, strip writes). Sketch 080-A is the visual contract. -->

frontend/src/components/admin/OperatorBand.tsx:22-87 — the band leaf to clone. Props {identity, onBack, recordingPulse?}. Change: Shield text-amber-400 → indigo; border/gradient amber → indigo; OPERATOR chip → ORG ADMIN chip (indigo) + the ◆ Org-admin / Member role badge; add org name to the label; KEEP the "every action recorded" marker (58-74) verbatim. Add the plain-first ⌥ Technical-names toggle (146 LANG-01) — reuse the shared TechnicalNamesProvider value.

frontend/src/components/admin/UsersAndAccess.tsx — the roster to clone (READ the writes to know what to DELETE):
  :128-148 (section header + client-side search-over-loaded-page), :150-165 (rows==null loading + empty state), :242-265 (row identity block: avatar + email + joined sub-line), :292-300 (role chip).
  DELETE: the graded action guards + confirm sheets (:314-484) and the onDisable/onEnable/onGrant/onRevoke prop contract (:49-64). Members is read-only.

frontend/src/components/admin/AuditTab.tsx — the audit list to clone LIGHTER:
  KEEP: PLATFORM_ACTION_META plain-vocabulary map (:66-92), the 029-A chip-filter strip (:470-616) + resolvePreset/windowReadout/DATE_CHIP_LABEL (:140-181), the paged table (:702-730) + pager (:735-755), the live match-count "amber at zero" cue (:633-645), showTechnical raw-code reveal (:532-534,722-726).
  STRIP: the source switch (:414-440) — single-source; the CSV export button + handleExport + operatorCsv/downloadCsv (:206-233,375-452).
  ADD: the RLS-honest degrade — when scope==='own' render an explicit "you see only your own activity (RLS)" banner + own rows (model the visible-honesty strip on :455-461, re-tinted). NEVER a silent empty list.

frontend/src/pages/SettingsPage.tsx — partial analog for OrgSettingsTab. Read settings-control-room-boundary.md (three-surface rule + F1 reframe) + dynamic-control-inventory.md BEFORE deciding which knobs land here. Keep it THIN — establish the org-config home, do not build the v3.5 relocation.

frontend/src/providers/TechnicalNamesProvider.tsx — the shared plain⇄technical value (useTechnicalNames) the band toggle + audit raw-code reveal both read.
Plan 02 exports (frontend/src/lib/api.ts): OrgMember, OrgAuditPage (with `scope`), OrgPermissions types + the role literals ('super-admin'|'org-admin'|'dept-admin'|'member').
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OrgBand (indigo identity band) + OrgSettingsTab (light org-config home)</name>
  <files>frontend/src/components/org/OrgBand.tsx, frontend/src/components/org/OrgSettingsTab.tsx</files>
  <read_first>
    - frontend/src/components/admin/OperatorBand.tsx:1-87 (the full band to clone)
    - frontend/src/providers/TechnicalNamesProvider.tsx (useTechnicalNames)
    - .planning/sketches/080-org-admin-shell/README.md (the org-indigo band spec + role badge copy)
    - .planning/notes/settings-control-room-boundary.md (the three-surface rule + F1 reframe) + .planning/notes/dynamic-control-inventory.md
    - frontend/src/pages/SettingsPage.tsx (personal-sliver shape reference — read, do not port wholesale)
  </read_first>
  <action>
    OrgBand.tsx: clone OperatorBand as a pure leaf. Props `{ orgName: string, role: 'super-admin'|'org-admin'|'dept-admin'|'member', onBack: () => void, recordingPulse?: boolean }`. Re-tint every amber token to org-indigo (the sketch 080-A / 079-C rail-shield indigo — amber NEVER appears). Replace the OPERATOR chip with an indigo `ORG ADMIN` chip; add the org name to the headline; add the role badge — `◆ Org-admin` (indigo `admin` chip) for org-admin/super-admin, `Member` (muted `member` chip) otherwise (D-166-05 copy). KEEP the "every action recorded" marker verbatim (062-A honesty — sketch 080-A rejects variant C precisely because it drops this). Add the plain-first `⌥ Technical-names` toggle wired to the shared `useTechnicalNames` value (146 LANG-01) so the band toggle + the audit raw-code reveal move ONE value. No fetch, no auth, no state beyond prop-driven render.

    OrgSettingsTab.tsx: a THIN org-config home leaf. Per D-166-03 / ADMIN-05, carve only the obviously-org-config sliver — establish the three-homes seam, DEFER the bulk global-knob relocation to v3.5 (SEED-117 §1). Per the boundary note's F1 reframe, do NOT relocate the operator-gated global knobs here. Render an honest, minimal org-config surface (e.g., org display name read-only + a clearly-labeled "more org configuration is coming" seam) — no "v1/placeholder" language that lies; a genuine light home. Props carry whatever the shell fetches; keep it presentational.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && npx tsc --noEmit</automated>
  </verify>
  <done>OrgBand renders indigo (no amber tokens), carries org name + ORG ADMIN chip + role badge + recording marker + ⌥ Technical-names; OrgSettingsTab is a thin org-config home leaf; both compile.</done>
  <acceptance_criteria>
    - `grep -ci "amber" frontend/src/components/org/OrgBand.tsx` == 0 (amber reserved for operator; the band is indigo).
    - `grep -n "every action recorded" frontend/src/components/org/OrgBand.tsx` matches (062-A marker preserved).
    - `grep -n "Org-admin\|Member" frontend/src/components/org/OrgBand.tsx` shows the role-badge copy.
    - OrgSettingsTab contains no "v1"/"placeholder"/"static for now" scope-reduction language.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OrgMembersTab (read-only roster)</name>
  <files>frontend/src/components/org/OrgMembersTab.tsx, frontend/src/components/org/OrgMembersTab.test.tsx</files>
  <read_first>
    - frontend/src/components/admin/UsersAndAccess.tsx:49-64 (the write prop contract to DROP), :128-165 (header/search/loading/empty), :242-300 (row identity + role chip), :314-484 (the write guards/sheets to DELETE)
    - .planning/sketches/080-org-admin-shell/README.md ("Members is read-only — affordances absent, not disabled")
  </read_first>
  <behavior>
    - Test: given a members array, each row renders email + role chip + joined sub-line.
    - Test: NO invite/disable/grant/role-edit control is rendered (query for buttons with those labels → none).
    - Test: an empty roster shows the empty state; a null roster shows the loading placeholder.
    - Test: the read-only banner points at the (locked) Invitations tab and names no roadmap phase number.
  </behavior>
  <action>
    OrgMembersTab.tsx: a pure read-only roster leaf. Props `{ members: OrgMember[] | null, query: string, onQueryChange, canManage: boolean }` (or the shell-driven equivalent). Reuse UsersAndAccess's header + client-side search-over-loaded-page (never an unbounded fetch), the rows==null loading + empty state, and the row identity block. Map the role chip to the mig-104 4-tier role with the `◆ Org-admin` / `Member` copy (indigo, never amber). DELETE every write path — the onDisable/onEnable/onGrant/onRevoke props and the graded action guards/confirm sheets do NOT exist here. The invite/edit affordances must be ABSENT (not disabled buttons that lie); render a small banner noting member management arrives via the (locked) Invitations tab — with NO roadmap phase number in the copy (T-146-10 rule).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && npx vitest run src/components/org/OrgMembersTab.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Roster renders identity + role chip + search; no write affordances present; loading/empty states honest; banner points at Invitations without a phase number; tests green.</done>
  <acceptance_criteria>
    - `grep -vi '^ *//' frontend/src/components/org/OrgMembersTab.tsx | grep -ci "onDisable\|onGrant\|onRevoke\|confirm"` == 0 (no write contract).
    - The test asserting "no invite/disable/edit control renders" passes.
    - The read-only banner text contains no digit-sequence matching a phase number (no "167"/"Phase 1").
    - `npx vitest run src/components/org/OrgMembersTab.test.tsx` passes; `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: OrgAuditTab (lighter audit list + RLS-honest degrade)</name>
  <files>frontend/src/components/org/OrgAuditTab.tsx, frontend/src/components/org/OrgAuditTab.test.tsx</files>
  <read_first>
    - frontend/src/components/admin/AuditTab.tsx:66-92 (PLATFORM_ACTION_META), :140-181 (resolvePreset/windowReadout/DATE_CHIP_LABEL), :414-440 (source switch to STRIP), :455-461 (visible-honesty strip model), :470-616 (chip-filter strip), :206-233,375-452 (CSV to STRIP), :633-645 (match-count cue), :702-755 (paged table + pager)
    - .planning/sketches/080-org-admin-shell/README.md (the RLS-honest degrade is load-bearing)
  </read_first>
  <behavior>
    - Test: when scope==='own' (canAuditView false), an explicit "you see only your own activity (RLS)" banner renders AND the rows shown are the own-only rows — never a silent empty list.
    - Test: when scope==='all', the banner is absent and cross-member rows render.
    - Test: the chip-filter strip (action-type + date chip) renders; there is NO CSV export button and NO source switch.
    - Test: the plain-first action labels come from the reused vocabulary map; the ⌥ Technical-names reveal shows the raw action code.
  </behavior>
  <action>
    OrgAuditTab.tsx: a pure leaf taking `{ page: OrgAuditPage, filters, onFiltersChange, onPageChange }` where `page.scope ∈ {"all","own"}`. Reuse AuditTab's plain-vocabulary map, the chip-filter strip + preset/window helpers, the paged table + pager, the match-count "at zero" cue, and the showTechnical raw-code reveal (wired to the shared useTechnicalNames). STRIP the source switch (single-source: audit_log only) and the CSV export (deferred within-phase, D-166-04). ADD the RLS-honest degrade: when `page.scope === "own"`, render the explicit banner (modeled on AuditTab:455-461, re-tinted for the member-sees-own case) above the own-only rows. NEVER render a silent empty list on the degrade path. Re-tint any amber zone accents to org-indigo (the "amber at zero" match-count trust cue is a semantic warning color — keep that specific cue, but the tab/zone chrome is indigo).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && npx vitest run src/components/org/OrgAuditTab.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Audit list renders with chip filters, no CSV, no source switch; scope==='own' shows the RLS-honest banner + own rows; scope==='all' shows cross-member rows; tests green.</done>
  <acceptance_criteria>
    - The scope==='own' test asserts BOTH the banner presence AND that rows render (not empty).
    - `grep -vi '^ *//' frontend/src/components/org/OrgAuditTab.tsx | grep -ci "downloadCsv\|handleExport\|operatorCsv\|Export CSV"` == 0 (CSV stripped).
    - `grep -ci "source" frontend/src/components/org/OrgAuditTab.tsx` shows no operator/platform source-switch toggle (single-source).
    - `npx vitest run src/components/org/OrgAuditTab.test.tsx` passes; `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| props → DOM | These are pure leaves; they render server-supplied rows. The client is NOT a trust boundary — the server (Plan 01) decided scope + row visibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-166-08 | Information Disclosure | OrgAuditTab degrade | mitigate | The tab renders exactly the rows + `scope` the server returned; scope==='own' shows the honest banner + own rows. It cannot widen visibility (no client-side "show all") — the server already filtered. Locked by the scope tests. |
| T-166-09 | Repudiation | OrgMembersTab false affordances | mitigate | Write affordances are ABSENT (not disabled) so the UI never implies a capability the caller lacks; the banner honestly points to the locked Invitations tab. |
| T-166-10 | Elevation | client-side role display | accept | Role/badge are display-only from server data; no write path exists in these leaves. The backend gate (Plan 01) is the wall. |
</threat_model>

<verification>
- `npx vitest run src/components/org/OrgAuditTab.test.tsx src/components/org/OrgMembersTab.test.tsx` green.
- `npx tsc --noEmit` exits 0.
- OrgBand has zero amber tokens (indigo zone identity, D-166-05).
</verification>

<success_criteria>
- OrgBand is org-indigo with the recording marker + role badge + ⌥ Technical-names (D-166-05).
- OrgAuditTab degrades honestly by scope (ADMIN-04); OrgMembersTab is read-only with absent affordances (ADMIN-01); OrgSettingsTab is the light org-config home (ADMIN-05).
</success_criteria>

<output>
Create `.planning/phases/166-org-admin-shell-org-switcher-profile-menu-anchor/166-03-SUMMARY.md` when done.
</output>
