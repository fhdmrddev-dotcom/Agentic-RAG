# Milestone v3.1: Workflow & Skill Studio — Trust, Clarity & Triggers

**Status:** ✅ SHIPPED 2026-06-28
**Phases:** 120–129 (CORE 120–124 + inserted 123.1; STRETCH 127, 128, 129 shipped; STRETCH 125, 126, 130, 131 gated/deferred to backlog)
**Total Plans:** 40 (CORE 28 + STRETCH 12)
**Timeline:** 2026-06-21 → 2026-06-28 (7 days, 324 commits)
**Files changed:** 569 files (+61,830 / −914 lines)

## Overview

Made the agent's skills and workflows trustworthy, legible, and reliably triggered across all providers — fixed the live workflow↔skill collision (a confirmed, root-caused bug), lifted cross-provider honesty to OpenAI-parity, added a Skill Trigger Tuner, re-skinned the Workflow Studio so each workflow's "soul" is obvious with a strict↔loose authoring/running split, unified the chat tool-card across providers with provider logos, and added MiniMax/OpenRouter arg repair.

**Goal:** Make the agent's skills + workflows trustworthy, legible, and reliably triggered across *all* providers — fix the live workflow↔skill collision (a confirmed, root-caused bug), lift cross-provider honesty to OpenAI-parity, add a Skill Trigger Tuner, and re-skin the Workflow Studio so each workflow's "soul" is obvious with a strict↔loose authoring/running split.

**Red line (every phase):** never fork the shared Deep/agent-loop/provider path — provider differences stay at the gateway/adapter/sanitizer boundary (D-14). Deep Mode stays byte-identical; no new runtime.

## Phases

### Phase 120: Collision Fix + Context Isolation

**Goal**: A skill that runs in a thread that previously ran a workflow emits only its own output, and a subsequent Deep turn never replays the workflow's history — the live, root-caused collision (Mechanism A) is closed at the harvest baseline and the history-reconstruction filter.
**Requirements**: COLL-01, CTX-01
**Completed:** 2026-06-22

Plans:
- [x] 120-01-PLAN.md — COLL-01: run-scope the sandbox-output harvest (snapshot+hash baseline seed) + headline live-repro regression test
- [x] 120-02-PLAN.md — CTX-01: author migration 076 (messages.origin) + tag every harness insert site + asymmetric origin filter at agent_loop.py
- [x] 120-03-PLAN.md — [BLOCKING] apply migration 076 to live DB (SQL-editor paste) + regenerate full-schema.sql + live-DB integration test

### Phase 121: One Front Door for Workflows (IA)

**Goal**: A user launches workflows from a single, obvious front door (the Workflows page); the chat composer is simplified to a 2-pill General/Explorer control with the Harness pill and in-chat workflow selector removed, while the existing Harness↔Deep lock / 409 / reconcile behavior is preserved exactly.
**Requirements**: IA-01
**Completed:** 2026-06-23

Plans:
- [x] 121-01-PLAN.md — remove the Deep/Harness toggle + in-chat workflow picker (2-pill composer); preserve lock/409/reconcile + composer-stop Cancel
- [x] 121-02-PLAN.md — rewrite ChatAreaMode + extend ChatAreaBanner + new ChatLayout launch test (SC#1/SC#2/SC#3 oracles)

### Phase 122: Cross-Provider Trust & Honesty Parity

**Goal**: Structured emission is recovered-or-honest on every provider, each provider uses the emission path it actually supports (doc-verified), cross-provider reliability is measured on a per-provider scoreboard that gates any tier change, and task/step labels are concrete on every provider (OpenAI-parity).
**Requirements**: MP-01, MP-02, MP-03, TDP-01
**Completed:** 2026-06-23

Plans:
- [x] 122-01-PLAN.md — MP-02: explicit emit_tier field + 55-row registry migration (14/2/34/5) + remove the provider=="openai" gate
- [x] 122-02-PLAN.md — MP-01: the ordered force_strict→non-strict→coerce→fail rung ladder inside forced_emit + emit_rung telemetry
- [x] 122-03-PLAN.md — MP-03: --forced-emit scoreboard matrix (EASY+HARD × native-7, 4 axes PASS/FAIL/DOCUMENTED) + dated artifact
- [x] 122-04-PLAN.md — TDP-01: ungated execute_code.description SYSTEM_PROMPT nudge + verified frontend label floor

### Phase 123: Skill Triggering Quality

**Goal**: A skill author can measurably tune a skill's trigger description, the system flags weak trigger descriptions before a skill is saved, and a loaded skill's instructions stay available for the rest of the session.
**Requirements**: TRIG-01, TRIG-03, CTX-03
**Completed:** 2026-06-26 (secure 29/29 · validate NYQUIST 12/12 · verify 12/12 + SC#10 4-axis UAT 4/4 PASS)

Plans:
- [x] 123-01-PLAN.md — TRIG-03 deterministic save-time lint (3 hook points, warn-never-block) + D-01 catalog-note relaxation
- [x] 123-02-PLAN.md — CTX-03 trim-pin: load_skill tool-result as a third protected class in trim_messages_to_fit
- [x] 123-03-PLAN.md — TRIG-01 core: D-08 builder-model knob + skill_tuner_service (candidates, policy-faithful classification, 60/40 held-out)
- [x] 123-04-PLAN.md — TRIG-01 routes: owner-scoped skill_tuner router (bounded background job over the run-buffer + tuner SSE)
- [x] 123-05-PLAN.md — TRIG-01 UI: focused full-surface Trigger Tuner (reachability triad) — case editor, N-column scoreboard, candidate cards
- [x] 123-06-PLAN.md — TRIG-03 UI loop: inline never-block lint warning + "Tune this" handoff + D-08 builder-model Settings picker

### Phase 123.1: Skill Trigger Tuner — Design Fidelity & UX Polish (INSERTED)

**Goal**: The Skill Trigger Tuner reads cleanly at the org's real provider count, makes the benchmark visible/editable before a run, makes a completed result durable across refresh/restart, and lets the builder model be chosen from configured models.
**Requirements**: TRIG-01 gap-closure (BUG-260624-01)
**Completed:** 2026-06-25 (verify 22/22 + UAT 8/8 · secure 34/34 · validate 9/9)

Plans:
- [x] 123.1-01-PLAN.md — Backend seam: tuner_runs table (migration 077) + durable latest-result upsert + GET-latest + seeded-cases GET
- [x] 123.1-02-PLAN.md — Scoreboard polish: ProviderScoreboard vertical rows + magnitude bar + combined score; CandidateCard line-clamp/expand
- [x] 123.1-03-PLAN.md — Settings builder-model picker from configured models + soft hint; remove IN-02 placeholders
- [x] 123.1-04-PLAN.md — Page integration: full-width results, seeded-case hydrate/edit, standalone scoreboard, result rehydration
- [x] 123.1-05-PLAN.md — Editor-wall fix: backend seed-cap + seeded GET `total` + legible/bounded CaseEditor
- [x] 123.1-06-PLAN.md — Backend honesty: empty axis -> n/a; all-error column -> unmeasured; single-source cell_score
- [x] 123.1-07-PLAN.md — Run UX: emit stage=provider_start; owner-scoped cancel route + job checkpoint
- [x] 123.1-08-PLAN.md — Run resilience: unmeasured-cell render; reconciling sub-state no-flash; durable-poll reconnect honesty
- [x] 123.1-09-PLAN.md — Layout: delete decorative bg-sidebar deco-rails on SkillsPage + SkillTunerPage
- [x] 123.1-10-PLAN.md — Observability: silence LangSmith 429 uploader flood to ERROR

### Phase 124: Workflow Studio UX — Soul + Strict↔Loose

**Goal**: A user immediately sees the "soul" of a workflow in three consistent sizes (library card / run header / publish summary), and meets a clear strict↔loose disclosure with two doors ("Describe & run" vs "Author & govern") without removing any control.
**Requirements**: WUX-01, WUX-02
**Completed:** 2026-06-26 (code-review CR-01 fixed · verify 4/4 + operator UAT 7/7 · CORE complete)

Plans:
- [x] 124-01-PLAN.md — Wave 0: extract shared soulData + net-new WorkflowSoul (3 sizes) + glyph-dot PhaseSpine
- [x] 124-02-PLAN.md — card-scale soul on library cards + the two-door fork at the Studio authoring entry
- [x] 124-03-PLAN.md — run-surface soul (additive sibling in WorkspacePanel) + publish-summary soul block prepend

### Phase 127: Gauntlet Pip-Strip + Quiet Idle Cards (STRETCH)

**Goal**: The publish gauntlet reads at a glance as a pip-strip + worded verdict with raw detail on demand, and idle PhaseCards stay visually quiet.
**Requirements**: WUX-03
**Status:** Code-verified 11/11; UAT partial (2/5 PASS, 1 SKIP, 2 BLOCKED by env — no regressions). 2026-06-27.

Plans:
- [x] 127-01-PLAN.md — Icon foundation: build-time 3D-icon mechanism (unplugin-icons) + shared PHASE_GLYPHS 3D swap
- [x] 127-02-PLAN.md — Publish gauntlet re-skin: energy-spine + worded verdict + raw-on-demand + golden-run hero
- [x] 127-03-PLAN.md — Living step-flow re-skin: quiet idle / bloomed active / folded done (G-5 PhaseCard/PhaseTimeline)

### Phase 128: Chat Tool-Card Unification + Chat-Area Reclaim (STRETCH)

**Goal**: One honest, unified, space-efficient chat surface across every provider — the tool card becomes the single canonical place for live run info, provider logos in the header, redundant sticky timer removed, long prompts clamped.
**Requirements**: TDP-02, CTC-01, CTC-02, CTC-03, CTC-04
**Completed:** 2026-06-27 (verify 5/6 code truths; D-06 scoreboard partial — logos live-confirmed; 3 UAT rows carried)

Plans:
- [x] 128-01-PLAN.md — Install @lobehub/icons (supply-chain checkpoint)
- [x] 128-02-PLAN.md — CTC-04 long-prompt clamp + gradient fade + Read-more
- [x] 128-03-PLAN.md — providerLogo.tsx shared helper (logo map + preparingDescription) + Wave-0 unit tests
- [x] 128-04-PLAN.md — CTC-01 RunCard logo + TDP-02 ToolCallPanel description = unified card (CTC-02)
- [x] 128-05-PLAN.md — D-06 LIVE native-7+OpenRouter cross-provider scoreboard (operator-run)
- [x] 128-06-PLAN.md — CTC-03 StickyTimerBar deletion (gated on D-06 proof)

### Phase 129: MiniMax/OpenRouter Arg Repair (STRETCH)

**Goal**: MiniMax malformed tool-args are repaired at the adapter boundary; OpenRouter requests carry `require_parameters` for broader provider robustness.
**Requirements**: MP-04
**Completed:** 2026-06-27 (verify 11/11 · 12 unit tests green · SC#10 live 7/9 rows PASS; BUG-260607-03 folded)

Plans:
- [x] 129-01-PLAN.md — OpenRouter `require_parameters` wired into the quality strategy + unit test
- [x] 129-02-PLAN.md — MiniMax-gated arg-validity guard + bounded re-ask + recovered signal / honest-fail; folds BUG-260607-03
- [x] 129-03-PLAN.md — SC#10 4-axis live cross-provider scoreboard (operator-run)

---

## STRETCH Phases Deferred to Backlog

Not started (gated behind CORE — budget not remaining after CORE + 127/128/129). Roll forward as v3.2 candidates.

- **Phase 125** — Self-Improve Proposer (description-only) [SI-02]
- **Phase 126** — Smart-Dispatch Relevance Pre-Filter [TRIG-02]
- **Phase 130** — template_input Resolver Run-Scope [COLL-02]
- **Phase 131** — Non-Python Skill-Script Honesty [SRH-01]

---

## Milestone Summary

**Key Accomplishments:**

1. **Collision fix + context isolation** — run-scoped the sandbox-output harvest baseline (closes the confirmed 2-files bug, Mechanism A); `messages.origin` column + asymmetric filter ensures Deep/Harness never replay each other's history (COLL-01, CTX-01; migration 076)
2. **One front door** — removed the composer Harness pill + in-chat workflow selector → clean 2-pill General/Explorer composer; workflows launch from Workflows page only; lock/409/reconcile preserved byte-identical (IA-01)
3. **Cross-provider trust & honesty** — force→coerce→fail retry ladder in `forced_emit` (all 4 consumers); `emit_tier` doc-verified per provider (55-model registry, 14/2/34/5 tiers); per-provider scoreboard gates any tier flip; task labels concrete on all providers (OpenAI-parity ungated nudge; MP-01/02/03/TDP-01)
4. **Skill Trigger Tuner** — held-out should/should-not benchmark (60/40 split, 3-repeat, ≤5 iter, background job over run-buffer/SSE); N-column ProviderScoreboard (derives from server-returned providers only); durable latest-result upsert (migration 077); seeded-case visibility + CandidateCard confirm flow; builder-model choosable from configured models (TRIG-01, + 123.1 UX polish)
5. **Workflow Studio UX soul + strict↔loose** — shared `soulData.ts` as the single source of truth for tier derivation + phase glyphs + soul atoms; `WorkflowSoul` in 3 sizes (card/run-header/publish-summary all read the same object); two-door `WorkflowDoorSwitch` ("Describe & run" / "Author & govern") — nothing removed, advanced one click away; CR-01 fixed (typed describe text seeds the govern Builder and auto-drafts) (WUX-01, WUX-02)
6. **Chat tool-card unification + provider logos** — `@lobehub/icons` single-source logo map; RunCard header shows live provider logo; ToolCallPanel carries live description before tool_start; StickyTimerBar removed (reclaims chat-area space); long prompts collapse to a clamped Read-more; cross-provider scoreboard operator-run (TDP-02, CTC-01..04)
7. **MiniMax/OpenRouter arg repair** — MiniMax-gated single-shot re-ask at the adapter boundary (recover or honest-fail); OpenRouter `require_parameters` in the quality strategy; BUG-260607-03 folded; native-7+OpenRouter live cross-provider verified (MP-04)

**Architectural Decisions:**

- **D-14 red line held:** every provider fix stayed at the gateway/adapter/sanitizer boundary; the shared Deep path is byte-identical across the entire milestone; no new runtime; no shared-path fork
- **Icon convention established (RDD-43):** provider/model icons = single-source `@lobehub/icons` everywhere; phase-type icons = shared 3D `PHASE_GLYPHS` map; verify-or-bundle decorative slugs
- **Per-phase rigor substitutes for formal audit** (v2.9/v3.0 precedent): every CORE phase cleared verify + secure + validate with live evidence; SC#10 4-axis scoreboard gated all streaming/provider phases
- **WUX soul single source of truth:** `soulData.ts` is the only place tier-derivation + phase-glyph map live; the three soul sizes can never disagree

**Issues Resolved:**

- BUG-260607-03 (MiniMax truncated args) — folded into Phase 129
- BUG-260624-01 (Trigger Tuner UX gaps: cramped scoreboard, no seeded-case visibility, no persistence) — folded into Phase 123.1
- CR-01 Phase 124 (loose describe door silently dropped typed text → dead Builder) — fixed in d60d04b9

**Deferred to v3.2+:**

- Phase 125 (SI-02 self-improve proposer), Phase 126 (TRIG-02 smart-dispatch pre-filter), Phase 130 (COLL-02 template_input run-scope), Phase 131 (SRH-01 non-Python skill honesty) — gated, never started
- Phase 127 UAT remainder (2 BLOCKED by env: engine chip needs provider-wired workflow, 4-axis needs parallel setup) — accepted, no regressions
- Phase 128 D-06 exhaustive logo sweep deferred (logos live-confirmed on both themes; exhaustive provider×theme×mode sweep carried)

**Technical Debt:**

- `backend/app/api/threads.py` G-5 extraction still due (threads.py untouched this milestone; extraction is a v3.2 candidate)
- LangSmith not emitting since 2026-06-20 (raw-SDK `wrap_openai` path) — 123.1-10 silenced the 429 flood, tracing still off
- v2.9 STRETCH 105–109 remain backlog carry-forwards

---

_For current project status, see `.planning/ROADMAP.md`_
