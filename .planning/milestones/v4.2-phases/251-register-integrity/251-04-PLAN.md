---
phase: 251-register-integrity
plan: 04
type: execute
wave: 4
depends_on: ["251-03"]
files_modified:
  - .claude/get-shit-done/workflows/discuss-phase.md
  - .claude/get-shit-done/workflows/new-milestone.md
  - .claude/hooks/agent-bus-check.sh
  - scripts/lib/bus-age.sh
  - scripts/agent-bus.sh
  - CLAUDE.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/251-register-integrity/251-BUS-TRIAGE.md
autonomous: true
requirements: [REG-02, REG-03]
user_setup: []

must_haves:
  truths:
    - "D-03: the wiring is the deliverable, not the script — `/gsd:discuss-phase` and `/gsd:new-milestone` each CALL the sweep, so the rule is applied rather than merely written. A rule that exists and is not applied is the same as no rule, and this project has paid for that finding twice."
    - "D-19: the call lands in `.claude/get-shit-done/workflows/`, where the steps actually are, following the G-7 precedent at `9d3d887de` — not in the 76- and 45-line routers under `.claude/commands/gsd/`, where nothing would execute it in order."
    - "D-19 (second half): `/gsd:new-milestone`'s existing `## 2.5. Scan Planted Seeds` is REPLACED, not added beside — and its `never delete or modify seed files during this workflow` prohibition, which directly contradicts CLAUDE.md's `a seed is answered by editing the seed`, is SCOPED IN WRITING in the same commit."
    - "D-14: the SessionStart hook prints a one-line count of open `to:operator` items with the oldest age, so the queue's size is a number the operator sees rather than one somebody has to go and count — and the age is computed by the ONE existing `age_days`, never a third copy."
    - "D-15: the 26 open `to:gemini` items stay out of scope and are COUNTED by the hook, so their size stops being invisible."
    - "D-12: the 5 CURRENT open `to:operator` items (BUS-040, BUS-208, BUS-246, BUS-247, BUS-248) are classified into a decision list, and BUS-171 is closed IN WRITING naming commit `88a9ff861` (2026-09-14) as the evidence its 23-item premise was discharged."
    - "D-13: ⛔ Claude does not close, answer or otherwise write to any bus item. The deliverable is a LIST the operator rules on; every item in the `carries an unfixed finding` class is verified to be held by a durable register, with one planted where it is not."
    - "REG-02: a person can run one command that reads the register and prints the seeds whose trigger is already true, and that command is now invoked at both GSD touchpoints."
  artifacts:
    - path: ".claude/get-shit-done/workflows/discuss-phase.md"
      provides: "<step name=\"cross_reference_seeds\"> invoking the sweep at the phase touchpoint"
      contains: "check-seeds-register.cjs"
    - path: ".claude/get-shit-done/workflows/new-milestone.md"
      provides: "§2.5 rebuilt on the script instead of reading all 283 seeds by hand"
      contains: "check-seeds-register.cjs"
    - path: ".planning/phases/251-register-integrity/251-BUS-TRIAGE.md"
      provides: "the 5-item operator decision list, three-arm classified, rulable in one sitting"
    - path: "scripts/lib/bus-age.sh"
      provides: "the single home for age_days, sourced by both the hook and agent-bus.sh"
  key_links:
    - from: ".claude/get-shit-done/workflows/discuss-phase.md"
      to: "scripts/check-seeds-register.cjs"
      via: "a bash call in a numbered step, G-7 style"
      pattern: "node scripts/check-seeds-register"
    - from: ".claude/hooks/agent-bus-check.sh"
      to: "scripts/lib/bus-age.sh"
      via: "source, replacing the divergent inline copy"
      pattern: "age_days"
---

<objective>
Make the sweep FIRE, make the operator's queue a number they see, and hand them a decision list they
can rule on in one sitting.

Purpose: the instrument built in Plan 01 over the register repaired in Plans 02-03 is worth nothing
until something calls it. D-03 says so in one sentence — *the wiring is the deliverable, not the
script* — and D-19 is where it actually has to land, because the two files D-03 names contain no
steps at all.

Output: two wired GSD touchpoints, an aged bus summary at every session start, a 5-item operator
decision list, and a REQUIREMENTS.md that stops claiming `WATCH 0/8` for a closed phase.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/251-register-integrity/251-CONTEXT.md
@.planning/phases/251-register-integrity/251-RESEARCH.md
@.planning/phases/251-register-integrity/251-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the sweep into both GSD touchpoints, where the steps actually are</name>
  <files>.claude/get-shit-done/workflows/discuss-phase.md, .claude/get-shit-done/workflows/new-milestone.md, CLAUDE.md</files>
  <read_first>
    - `251-PATTERNS.md` §6a — `git show 9d3d887de`, the precedent diff in full: the commit message
      shape, the `## X.Y. PROJECT GATE — <rule>` section-naming convention, and the pattern of
      inserting the gate ABOVE the existing body while re-titling that body into the `exit 0` arm.
    - `.claude/get-shit-done/workflows/plan-phase.md` lines 175-201 — the live G-7 block, the exact
      text to model.
    - `251-PATTERNS.md` §6b — `.claude/get-shit-done/workflows/discuss-phase.md:262-283`, the
      `<step name="cross_reference_todos">` sibling quoted verbatim with its four arms (bash call ·
      skip-silently · present/AskUserQuestion routing into CONTEXT.md `<decisions>`/`<deferred>` ·
      `--auto` threshold). Insert AFTER it (`:278`) and BEFORE `<step name="scout_codebase">`
      (`:280`).
    - `251-PATTERNS.md` §6c — `.claude/get-shit-done/workflows/new-milestone.md:49-96` quoted in
      full: the block being REPLACED, its three presentation arms to preserve, and the
      `never delete or modify seed files` line at `:96`.
    - `CLAUDE.md` § "Seeds register cross-check (MANDATORY)" (as corrected in Plan 02) and
      § "Workflow guardrails" — the G-7 entry beside which the wiring sites are recorded.
    - `scripts/check-seeds-register.cjs` — the `--phase` surface this wiring calls.
  </read_first>
  <action>
    In `.claude/get-shit-done/workflows/discuss-phase.md`, insert a new
    `<step name="cross_reference_seeds">` between `cross_reference_todos` and `scout_codebase`. Model
    its four arms line-for-line on the todos step it sits beside: a bash call to
    `node scripts/check-seeds-register.cjs --phase "${PHASE_NUMBER}"`; a *skip silently* arm when the
    matched set is empty; a present-and-AskUserQuestion arm routing folded seeds into CONTEXT.md
    `<decisions>` and reviewed-but-not-folded into `<deferred>`; and an `--auto` arm. ⚠ Add the arm
    the todos step does NOT have and CLAUDE.md requires: **write the routing BACK into the seed** —
    flip `status` and record where it went — because a seed that shipped and still reads `planted`
    will be re-proposed forever. Handle a non-zero exit by printing the gate's output verbatim (it
    names its own derivation) and continuing the discussion rather than blocking it: this is a
    register-hygiene finding, not a gap-closure cap, so it informs the phase rather than stopping it.
    Say that in the step, so a later reader does not "harden" it into a blocker.

    In `.claude/get-shit-done/workflows/new-milestone.md`, REPLACE the mechanism inside
    `## 2.5. Scan Planted Seeds` — do not add a second section beside it. The `ls` + *"Read each
    `SEED-*.md` file"* instruction becomes a call to the sweep; the three presentation arms
    (`--auto`, `TEXT_MODE`, AskUserQuestion) are preserved unchanged, because they are the house
    convention and the gate's output feeds them as-is. ⛔ `Read each SEED-*.md file` — all 283, by
    hand — is REQUIREMENTS.md's *"that sweep is a phase of work, not a step in a command"* written
    into the workflow verbatim; deleting that instruction is the point of the whole requirement.

    Resolve the contradiction IN WRITING, in the same edit (D-19). The line
    *"never delete or modify seed files during this workflow"* stays, SCOPED: the workflow itself
    does not edit seeds while it is selecting them for a milestone, and answering a seed by editing
    it — CLAUDE.md's rule — stays correct outside it, including at `/gsd:discuss-phase`. State both
    halves explicitly and cite the other register by name, so neither reads as having been quietly
    overridden. ⭐ Note in the step, in one sentence, that this contradiction is very likely a real
    cause of the ~160-planted backlog: the workflow that surfaces seeds was forbidden from flipping
    them.

    ⚠ Describe the two blindnesses SEPARATELY (they are different defects and a plan that merges
    them will claim to fix one and actually fix the other): CLAUDE.md's RULE was blind because it
    filtered on `surface: Agentic-RAG`, a key present on 128 of 283 — fixed in Plan 02. This
    WORKFLOW was blind differently — it read everything and judged prose by hand, which is unbounded
    rather than partial — and that is what this task fixes.

    In `CLAUDE.md`, beside the existing G-7 mechanical-enforcement entry, record the new wiring
    sites: the two workflow files and `plant-seed.md`'s allocator and template block (Plan 02), each
    named by path, with one line stating that `.claude/get-shit-done/` is a VENDORED framework at
    v1.42.3 and that a future `chore(gsd): apply the pending GSD framework update` can clobber these
    edits — so they can be re-applied rather than silently lost. The G-7 wiring survived one update
    (`dec2577ae` touched none of its three files); that is a precedent, not a guarantee.
    ⛔ Re-measure CLAUDE.md with `node scripts/check-claude-md-size.cjs`, never `wc`.
  </action>
  <acceptance_criteria>
    - `grep -rn "check-seeds-register" .claude/get-shit-done/workflows/` returns at least one line in
      `discuss-phase.md` and at least one in `new-milestone.md`.
    - `grep -n "Read each \`SEED-\*\.md\` file" .claude/get-shit-done/workflows/new-milestone.md`
      returns nothing — the by-hand mechanism is gone, not supplemented.
    - `grep -c "## 2.5" .claude/get-shit-done/workflows/new-milestone.md` is `1` — the section was
      replaced, not duplicated.
    - `grep -n "never delete or modify seed files" .claude/get-shit-done/workflows/new-milestone.md`
      still returns a line, and within 6 lines of it there is text naming CLAUDE.md and stating the
      scope of the prohibition.
    - `sed -n '/cross_reference_todos/,/scout_codebase/p'
      .claude/get-shit-done/workflows/discuss-phase.md` contains the new
      `<step name="cross_reference_seeds">` — i.e. it is positioned between the two, not appended.
    - `grep -n "1.42.3\|vendored" CLAUDE.md` returns a line in the guardrails area naming the three
      edited framework files by path.
    - `node scripts/check-claude-md-size.cjs` exits `0`; the char count is recorded in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>grep -rn "check-seeds-register" .claude/get-shit-done/workflows/ | wc -l; node scripts/check-claude-md-size.cjs; echo "exit=$?"</automated>
  </verify>
  <done>Both touchpoints call the sweep, the by-hand 283-file read is gone, the
  workflow-vs-CLAUDE.md contradiction is resolved in writing, and the vendored-edit sites are
  recorded where a future framework update will be noticed.</done>
</task>

<task type="auto">
  <name>Task 2: The hook ages the queue — with ONE age_days, not a third copy</name>
  <files>.claude/hooks/agent-bus-check.sh, scripts/lib/bus-age.sh, scripts/agent-bus.sh</files>
  <read_first>
    - `251-PATTERNS.md` §5 — `.claude/hooks/agent-bus-check.sh` quoted in FULL (41 lines), with the
      three things to note called out by line: `:18`'s early exit, `:9-10`'s silence contract, and
      `:5`'s already-rotted comment.
    - `251-PATTERNS.md` §5, the side-by-side table — the TWO existing copies of the bus header parse
      and the one place they have ALREADY diverged: `scripts/agent-bus.sh:37-44` `age_days()` has a
      BSD `date -u -j -f` fallback and a `"?"` sentinel; the hook's inline copy at `:28-33` is
      GNU-only with neither.
    - `scripts/agent-bus.sh` lines 1-45 and 160-176 — the header, `next_id()`, `age_days()`, and the
      `case "${1:-}"` dispatch at the tail that makes a bare `source` of this file unsafe.
    - `251-RESEARCH.md` §6.1-6.4 — how the hook selects and ages today, and the confirmation that the
      `·` middot is not a parsing hazard.
    - `251-CONTEXT.md` D-14 and D-15.
  </read_first>
  <action>
    Create `scripts/lib/bus-age.sh` containing ONLY the `age_days()` function lifted verbatim from
    `scripts/agent-bus.sh:37-44` — no `set` line, no dispatch, nothing executable at load. Then have
    `scripts/agent-bus.sh` source it in place of its own definition, and have the hook source it in
    place of its inline copy. ⛔ Do NOT source `scripts/agent-bus.sh` directly from the hook: it sets
    `set -euo pipefail` (the hook deliberately runs without `-e`), it `die`s at `:22` when the bus is
    missing, and it dispatches on `$1` at its tail, so a bare source runs a command. That is why the
    extraction is the single-home solution and a dispatch guard is not.

    Restructure the hook so the `to:operator` summary prints even when the `to:claude` block is
    empty — which is the NORMAL state (exactly one open today, `BUS-171`, and it will close). Move
    `:18`'s `[ -z "$items" ] && exit 0` out of the top: compute all three counts first with
    `grep -cE '^### \[OPEN\].*to:<party>'`, print the summary line, then print the `to:claude` detail
    block only if non-empty.

    The summary line names all three queues on one line — the open `to:operator` count with the
    OLDEST item's age in days, the open `to:gemini` count (D-15: out of scope to answer, counted so
    its size stops being invisible), and the `to:claude` count. Derive the oldest age by taking the
    minimum ISO date among the open `to:operator` headers and passing it to the shared `age_days`;
    handle its `"?"` sentinel rather than printing a broken subtraction.

    ⚠ Preserve the silence contract the hook states in its own header at `:9-10` — *a hook that
    prints on every start is noise, and noise is how a real item gets scrolled past*. Print the
    summary line ONLY when the `to:operator` count is non-zero. Name the trade in a comment either
    way, rather than sliding past it: this hook's whole argument is that it is silent when there is
    nothing to say.

    Correct the rotted comment at `:5` — it claims *".planning/seeds/ holds 188 trigger_when
    entries"*; measured 2026-09-15 it is 157 across 283 files, and 188 was probably the seed count at
    the time. Record the correction BESIDE the original per house convention, and point the sentence
    at the sweep this phase built, since its premise (*the only command that greps for SEED is the
    one that WRITES them*) is now false.

    ⛔ This task writes no bus item. It does not call `agent-bus.sh open`, `answer` or `close`.
  </action>
  <acceptance_criteria>
    - `grep -c "age_days" .claude/hooks/agent-bus-check.sh scripts/agent-bus.sh scripts/lib/bus-age.sh`
      shows the FUNCTION DEFINITION exactly once, in `scripts/lib/bus-age.sh`; the other two files
      only call it.
    - `grep -n "date -u -j -f" scripts/lib/bus-age.sh` returns a line — the BSD fallback survived the
      extraction.
    - `bash .claude/hooks/agent-bus-check.sh` prints a single summary line matching
      `5 open to:operator, oldest <N> days` (N ≥ 16 on 2026-09-16, BUS-040 dated 2026-08-31) plus the
      `to:gemini` count `26` and the `to:claude` count, and then the `to:claude` detail block.
    - With the `to:claude` grep forced empty in a scratch copy, the hook still prints the
      `to:operator` summary and exits `0` — the `:18` early exit no longer suppresses it.
    - With the `to:operator` count forced to `0` in a scratch copy, the hook prints NOTHING and exits
      `0` — the silence contract holds.
    - `bash scripts/agent-bus.sh list --to operator` still works unchanged after the extraction, and
      `bash -n scripts/agent-bus.sh` and `bash -n .claude/hooks/agent-bus-check.sh` both exit `0`.
    - `git diff .agent-bus/OPEN.md` is EMPTY.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/lib/bus-age.sh && bash -n scripts/agent-bus.sh && bash -n .claude/hooks/agent-bus-check.sh && bash .claude/hooks/agent-bus-check.sh; echo "exit=$?"; git diff --name-only .agent-bus/ | wc -l</automated>
  </verify>
  <done>The queue's size and age appear at every session start, the third copy of `age_days` was not
  written, and the two existing copies became one.</done>
</task>

<task type="auto">
  <name>Task 3: The operator decision list, and the registers that still claim 0/8</name>
  <files>.planning/phases/251-register-integrity/251-BUS-TRIAGE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `251-CONTEXT.md` D-12 (the re-scope to the current 5, and the evidence commit), D-13 (the
      three-arm classification and the absolute prohibition), D-15 (the `to:gemini` queue is not
      Claude's to rule on).
    - `.agent-bus/OPEN.md` — the five open `to:operator` items in full: `BUS-040` (2026-08-31),
      `BUS-208` (2026-09-13), `BUS-246`, `BUS-247` (2026-09-14), `BUS-248` (2026-09-15). Read each
      item's body, not its header.
    - `.agent-bus/README.md` and `AGENTS.md` — the bus protocol and the role separation it implements.
    - `git show --stat 88a9ff861` — the evidence D-12 requires be named in writing: the BUS-171
      triage that already shipped, whose third arm found one true orphan and planted `SEED-276`.
    - `.planning/seeds/SEED-276-*.md` — the proof the third arm works, and the model for planting a
      register entry when an item turns out to carry an unheld finding.
    - `.planning/ROADMAP.md` lines 228-242 — the Progress note that assigns the `WATCH 0/8` /
      `CRED 0/4` checkbox sweep to this phase, and ⛔ its standing constraint: *claude did not tick
      them — flipping a box for work claude neither built nor reviewed would be a claim it cannot
      back*.
    - `.planning/REQUIREMENTS.md` § the WATCH and CRED sections.
  </read_first>
  <action>
    Write `251-BUS-TRIAGE.md` as a decision list the operator can rule on in one sitting. One row per
    open `to:operator` item, in age order, each carrying: the id, its age in days, one line of what
    it is actually asking (not its header text), and exactly one of D-13's three classifications —
    **superseded** (naming the specific evidence: a commit, a file, a shipped phase), **live
    decision** (one line stating the question so the operator can rule without re-reading the item),
    or **carries an unfixed finding**.

    For every item landing in the third class, VERIFY that a durable register holds the finding —
    grep `.planning/seeds/`, `.planning/reported-bugs/`, `docs/`, `CLAUDE.md` and the phase records
    for it — and plant a seed where none does, using `/gsd:capture --seed` so the new
    `max(id)+1` allocator gets its first real exercise. ⭐ This arm is the one that paid for itself:
    `BUS-040` carried a measured finding held by no register for 14 days, and `SEED-276` now holds
    it. Record the verification per item, naming the register that holds it or the seed planted.

    Close `BUS-171` IN WRITING (D-12): a section of this document recording that its original
    23-item premise was discharged at commit `88a9ff861` (2026-09-14) — 22 of its 23 named items are
    closed, four of the five currently-open items are NEWER than BUS-171 itself, and the triage it
    asked for already ran. State plainly that this is an honest re-scope recorded at discuss-phase,
    not a quiet narrowing, and that the requirement's GOAL is met while its stated COUNT was stale.

    Add a short section naming D-15's out-of-scope queue with its measured size — 26 open
    `to:gemini`, against 32 open and 212 closed overall — and its re-open trigger: any one of them is
    found to carry an unfixed finding held by no durable register, or the count crosses a threshold
    the operator cares about.

    End the document with the EXACT commands the operator runs to act on it —
    `bash scripts/agent-bus.sh answer <BUS-NNN> "<ruling>"` and
    `bash scripts/agent-bus.sh close <BUS-NNN>` — one per item, pre-filled with the id.
    ⛔ **Claude runs none of them.** D-13 is absolute: an item closed without the operator seeing it
    is a decision taken by the wrong party, and this includes `BUS-171`, which claude does not close
    for itself. `git diff .agent-bus/` must be empty at the end of this plan.

    Then sweep `.planning/REQUIREMENTS.md`, which ROADMAP:241 assigns to this phase. RE-DERIVE from
    the phase directories, never from a summary line — that drift ran in both directions at the v4.1
    close. For each unticked box in a CLOSED phase's requirement group, open the phase's own
    `*-SUMMARY.md` / `*-VERIFICATION.md` / `*-VERDICT.md` and tick it ONLY when a named artifact
    shows the requirement delivered, citing that artifact's path beside the tick. ⛔ Where the
    evidence does not support a tick, LEAVE THE BOX UNTICKED and write one line saying what is
    missing — ROADMAP:239-241 is explicit that flipping a box for work claude cannot back is itself
    the defect. Record the resulting counts and the evidence path per requirement in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `251-BUS-TRIAGE.md` contains exactly 5 item rows whose ids are `BUS-040`, `BUS-208`,
      `BUS-246`, `BUS-247`, `BUS-248`, each carrying exactly one of the three classification labels.
    - Every row classified *superseded* names a commit sha, file path or phase number as its
      evidence; no row says "superseded" without one.
    - Every row classified *carries an unfixed finding* names the register that holds it, or the
      `SEED-NNN` planted in this task with its new id.
    - The document contains the literal string `88a9ff861` in the BUS-171 section.
    - The document ends with one `agent-bus.sh answer` and one `agent-bus.sh close` line per item,
      pre-filled with the id, addressed to the operator.
    - `git diff --name-only .agent-bus/` is EMPTY. ⛔ `grep -rn "agent-bus.sh close" ` over this
      plan's own execution transcript shows it only inside the document's operator instructions,
      never as a command claude ran.
    - Any newly planted seed has an id of `285` or higher (the allocator derives `max(id)+1` after
      Plan 03 took the block through 284) and passes `node scripts/check-seeds-register.cjs`.
    - Every requirement box flipped in `REQUIREMENTS.md` has a phase-artifact path cited beside it;
      every box left unticked in a closed phase has a one-line reason.
    - `node scripts/check-seeds-register.cjs` exits `0` at the end of the plan.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-seeds-register.cjs; echo "gate exit=$?"; git diff --name-only .agent-bus/ | wc -l</automated>
  </verify>
  <done>Five items are classified with named evidence, every unheld finding is now held by a durable
  register, BUS-171 is discharged in writing, the operator has the exact commands, and no bus item
  was written by Claude.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendored framework ↔ project edits | `.claude/get-shit-done/` at v1.42.3 with a SessionStart update checker; an edit here can be reverted by a future framework update |
| agent ↔ operator authority | closing a bus item is a decision; taking it as the wrong party is the threat D-13 exists to stop |
| hook ↔ every session | the SessionStart hook runs before every session and subagent; a broken hook or an over-chatty one degrades every future turn |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-251-20 | Elevation of privilege | Claude closing or answering an operator bus item | mitigate | D-13 is absolute: no task calls `agent-bus.sh open/answer/close`; the document ships the pre-filled commands for the OPERATOR; `git diff --name-only .agent-bus/` asserted EMPTY as an acceptance criterion (Task 3) |
| T-251-21 | Tampering | a future `chore(gsd): apply the pending GSD framework update` reverting the wiring | mitigate | the three edited framework files are recorded by path in CLAUDE.md beside the G-7 entry, so the edits can be re-applied rather than silently lost. ⚠ Accepted, not eliminated — one precedent survived one update, which is a precedent and not a guarantee (Task 1) |
| T-251-22 | Denial of service | the hook printing on every session and subagent start, forever | mitigate | the summary prints only when the `to:operator` count is non-zero, preserving the silence contract the hook states in its own header; asserted by a scratch-copy drive with the count forced to 0 (Task 2) |
| T-251-23 | Tampering | a THIRD divergent copy of the bus header parse / age computation | mitigate | `age_days` is extracted to `scripts/lib/bus-age.sh` and sourced by both consumers; the definition-count acceptance criterion makes a third copy fail (Task 2). ⚠ Two copies already existed and had already diverged on the BSD fallback |
| T-251-24 | Denial of service | a broken hook or a `set -e` inherited from sourcing `agent-bus.sh` killing every session start | mitigate | the extraction is a function-only file with no `set` line and nothing executable at load; `bash -n` on all three files is an acceptance criterion, and the hook is driven once for real |
| T-251-25 | Repudiation | a REQUIREMENTS.md box ticked for work claude cannot back | mitigate | a tick requires a named phase-artifact path beside it; where the evidence does not support one the box stays unticked with a written reason (ROADMAP:239-241's explicit constraint) (Task 3) |
| T-251-26 | Denial of service | the wired sweep blocking `/gsd:discuss-phase` on a red register | mitigate | the step prints the gate's output verbatim and CONTINUES — it is a hygiene finding, not a G-7-style cap — and the step says so, so a later reader does not harden it into a blocker (Task 1) |
| T-251-SC | Tampering | npm/pip/cargo installs | mitigate | zero install tasks in this plan; the wiring adds no dependency to anything |
</threat_model>

<verification>
1. `grep -rn "check-seeds-register" .claude/get-shit-done/workflows/` → hits in BOTH
   `discuss-phase.md` and `new-milestone.md`.
2. `bash .claude/hooks/agent-bus-check.sh` → prints the three-queue summary with the oldest
   `to:operator` age, then the `to:claude` block; `bash -n` clean on all three shell files.
3. `grep -c "age_days()" scripts/lib/bus-age.sh scripts/agent-bus.sh
   .claude/hooks/agent-bus-check.sh` → the definition exists exactly once.
4. `git diff --name-only .agent-bus/` → EMPTY (D-13).
5. `node scripts/check-seeds-register.cjs` → exit 0 with the whole register green.
6. `node scripts/check-claude-md-size.cjs` → exit 0, reading recorded.
7. `node scripts/check-hot-file-ledger.cjs 251` → final reading recorded for the phase. ⚠ A
   `[no-row]` here is a finding about the gate's own WATCHED list (`backend/app/`, `frontend/src/`
   only), not a licence to add ledger rows for tooling.
</verification>

<success_criteria>
- Both GSD touchpoints call the sweep, and `/gsd:new-milestone` no longer instructs a human to read
  283 seeds by hand (REG-02, D-03, D-19).
- The workflow-vs-CLAUDE.md contradiction on editing seeds is resolved in writing, in the same
  commit, with both halves stated (D-19).
- The open `to:operator` count and its oldest age appear at every session start, computed by the one
  shared `age_days` (D-14, D-15).
- The five open items are a classified decision list with named evidence, every third-arm finding is
  held by a durable register, and BUS-171 is discharged in writing naming `88a9ff861` (D-12).
- ⛔ No bus item was opened, answered or closed by Claude (D-13).
- REQUIREMENTS.md's closed-phase boxes are re-derived from phase artifacts, with a cited path for
  every tick and a written reason for every one left unticked.
</success_criteria>

<output>
Create `.planning/phases/251-register-integrity/251-04-SUMMARY.md` when done, carrying the hook's
real output verbatim, the final gate readings, and the REQUIREMENTS.md tick/no-tick table with its
evidence paths.
</output>
