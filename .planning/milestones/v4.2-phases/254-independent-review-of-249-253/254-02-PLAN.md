---
phase: 254-independent-review-of-249-253
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/251-register-integrity/251-REVIEW.md
autonomous: true
requirements: [DEBT-06]
user_setup: []

must_haves:
  truths:
    - "Phase 251 has been read by something other than its own verification, for the first time"
    - "The file that records that reading says, in its own frontmatter, that it is NOT an independent review"
    - "Every finding it reports as live was driven against the tree before it was reported"
    - "Every finding carries exactly one recommended disposition, and none is fixed here"
  artifacts:
    - path: ".planning/phases/251-register-integrity/251-REVIEW.md"
      provides: "the self-assessed quality floor over Phase 251"
      contains: "review_type: self-assessed"
  key_links:
    - from: ".planning/phases/251-register-integrity/251-REVIEW.md"
      to: ".planning/DEBT-06-AUDIT.md"
      via: "the counting rule — a review file without `review_type: independent` is a code-review pass"
      pattern: "review_type:[ ]*self-assessed"
    - from: ".planning/phases/251-register-integrity/251-REVIEW.md"
      to: "scripts/check-seeds-register.cjs"
      via: "the reviewed artifact — driven, not read"
      pattern: "--self-test"
---

<objective>
Phase 251 is the only one of the five with **no review file of any kind**. This plan gives it the
quality floor D-08 asks for: a standard-depth `/gsd:code-review`-shaped pass, written by claude, and
labelled in its own frontmatter as the self-assessment it is.

Purpose: D-07 ranks 251 first for exactly this reason, and D-08 states the point — **no phase of v4.2
ships wholly unread.** The instrument has a measured catch rate on this very set: 2 blockers on 249,
2 criticals on 253, 2 more on 253's gap-closure round, each past a green self-verified close.
Output: `251-REVIEW.md` — a file that does not exist today, so nothing is overwritten (D-09).

⛔ **THIS DISCHARGES NOTHING.** `DEBT-06-AUDIT.md` adopts Phase 240's counting rule verbatim: *a review
file whose frontmatter does not assert `review_type: independent` is a **code-review pass**, not a §6.3
independent review.* Claude built Phase 251 — planned it, executed it and verified it in one session,
with the verifier a subagent of the orchestrating session. This pass improves quality and changes no
register. The file must say so about itself.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/254-independent-review-of-249-253/254-CONTEXT.md
@.planning/phases/254-independent-review-of-249-253/254-PATTERNS.md
@.planning/DEBT-06-AUDIT.md
@.planning/phases/251-register-integrity/251-VERIFICATION.md

<interfaces>
<!-- Scope, instrument and output path. Do not go looking for these. -->

**The output path is `251-REVIEW.md`, and that is a decision with a reason.** `/gsd:code-review <phase>`
writes `{padded_phase}-REVIEW.md` into the phase directory. 251 has **no** review artifact of any kind
(M-3), so this is a CREATE and D-09's do-not-overwrite rule is satisfied by construction. ⛔ It is NOT
named `251-REVIEW-IND.md` — that name is reserved for a verdict by an agent that did not build, and
using it here would be the false claim this whole phase exists to stop.

**The instrument.** `/gsd:code-review` is a thin dispatch that spawns the `gsd-code-reviewer` agent at
`workflow.code_review_depth` (config reads `standard`) and has it write the REVIEW file. An executor
cannot type a slash command, so spawn `gsd-code-reviewer` directly with an explicit file scope.
⛔ `/code-review ultra` is ruled out on cost by standing operator decision — do not use `--depth=deep`
as a substitute for it; `standard` is the instrument named by D-08 and by the ROADMAP.

**The scope, taken from Phase 251's four plans' `files_modified` (re-derive; do not trust this list
blindly):**
```
scripts/check-seeds-register.cjs
scripts/migrate-seeds-frontmatter.cjs
.planning/seeds/TEMPLATE.md
.claude/get-shit-done/workflows/plant-seed.md
.claude/get-shit-done/workflows/discuss-phase.md
.claude/get-shit-done/workflows/new-milestone.md
.claude/hooks/agent-bus-check.sh
scripts/lib/bus-age.sh
scripts/agent-bus.sh
```
Commit range at plan time: `600e28dde` (the plan commit) `..de6986fa2` (the verification commit).
Build commits inside it: `b38bd8444`, `99f7ba5c7`, `b03c66441`, `5fc75a0f0`, `78c8cf010`, `f5125638b`,
`134cb4ffa`, `81fab4927`. Re-derive with
`git log --oneline 600e28dde..de6986fa2 -- <the files above>`.

**The four highest-value targets, named by `BUS-249` itself so the pass does not have to find them:**
1. `scripts/check-seeds-register.cjs` `--self-test` claims `8/8 PASS`. ⭐ **Drive the arms RED.** Three
   times in Phase 251 a planted defect left every arm green except the counterfactual one — the
   sharpest case being a defanged md5 body check that left **6 of 7 arms green including the arm whose
   whole job was that invariant**, because the bodies were genuinely fine and only the CHECK was dead.
   A happy-path arm cannot distinguish *the property holds* from *nothing is checking*. Arms `1c` and
   `2b` were added late — assume nothing about them.
2. **D-11, the body invariant** — 293 seed bodies claimed md5-identical across the migration.
   ⚠ Verify blob-vs-blob via `git archive`, **NEVER** against the working tree: `core.autocrlf=true` on
   this box makes a working-tree read report **153/276 seeds drifted** when nothing drifted. `git diff`
   cannot see line-ending damage in content either, so it is not the instrument.
3. **The wiring, which is the deliverable — not the script.** `discuss-phase.md` and `new-milestone.md`
   are claimed to CALL the sweep. ⛔ `grep -rn "check-seeds-register"` still HITS when the fenced
   command is replaced by a prose mention with zero runnable calls. **Extract the fence as authored and
   RUN it.**
4. **The 8 renumbers and their redirect stubs.** D-07's rule is *oldest keeps the id, by date*, and
   `seedDate()` is not total (`SEED-001` carries neither `created:` nor `planted:`); two pairs broke on
   a git add-commit tie-break. The stub carve-out was `stubs.length === 1` — a COUNT with no SHAPE
   check — fixed late in 251-04 as `members.length === 2 && stubs.length === 1`.

**⛔ BOUNDARIES THAT WERE DELIBERATE — do not report these as defects:**
- 378+ references inside sealed `.planning/milestones/` were NOT rewritten (D-06).
- 33 product-source references in `backend/` / `frontend/` deliberately left pointing at stubs (D-17),
  listed by file in `251-RENUMBER-LEDGER.md`.
- The `251-register-integrity/` directory is itself not rewritten — it is the transcript OF the collision.

**The frontmatter shape to imitate, and the ONE key to change.** `238-INDEPENDENT-REVIEW.md:1-15` is
the repo's only `review_type: independent` file:
`phase / reviewed / review_type / reviewer / builder / range / status / findings{critical,blocker,warning,info,resolved_in_phase}`.
Use that key set exactly, with `review_type: self-assessed`.
**`review_type: self-assessed` is THE token for this whole phase** — every artifact 254 writes uses it,
spelled identically, and nothing 254 writes may contain the string `review_type: independent`.

**The finding-body shape used by every `*-REVIEW.md` in 249-253** (identical across all six):
`## Critical Issues` → `### CR-01: <one-sentence claim> — BLOCKER` → prose → a fenced repro/drive block
→ `## Warnings` → `### WR-01: …` → `## Info` → `### IN-01: …` → and a closing
`## Verified clean (recorded so a later reader does not re-derive it)` section. ⭐ Keep that last
section — it is what stops the next round re-deriving the same ground.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the standard-depth pass over Phase 251 and write 251-REVIEW.md</name>
  <files>.planning/phases/251-register-integrity/251-REVIEW.md</files>
  <read_first>
    - `.planning/phases/251-register-integrity/251-VERIFICATION.md` — the claims being checked.
    - `.planning/phases/251-register-integrity/251-01..04-SUMMARY.md` — what each plan says it did.
    - `.planning/phases/251-register-integrity/251-RENUMBER-LEDGER.md` and `251-GATE-BASELINE.md`.
    - `.agent-bus/OPEN.md` — `BUS-249`'s body, the builder's own list of targets and boundaries.
    - `.planning/DEBT-06-AUDIT.md` `:20-27` — the counting rule this file must obey about itself.
  </read_first>
  <action>
    Assert first that `.planning/phases/251-register-integrity/251-REVIEW.md` does NOT exist. If it
    does, STOP and report — D-09 forbids overwriting an existing review file, and its existence would
    refute M-3.

    Spawn the `gsd-code-reviewer` agent at **standard** depth with the explicit file scope listed in
    `<interfaces>`, re-derived first from Phase 251's four `*-PLAN.md` `files_modified` blocks and from
    `git log --oneline 600e28dde..de6986fa2`. Pass it the four highest-value targets and the three
    deliberate boundaries verbatim, so it neither hunts for the first nor raises the second as defects.

    Have it produce `251-REVIEW.md` in Phase 251's own directory, using the finding-body shape named in
    `<interfaces>` and closing with the `## Verified clean` section.

    ⛔ Do NOT pass `--fix`, do not spawn `gsd-code-fixer`, and do not change a single line of Phase
    251's code. D-11: findings are TRIAGED, never fixed in this phase — the reviewer fixing what it
    found stops being an independent verifier of that fix (`AGENTS.md` §6.3, verbatim), and fixing
    inside a review phase is the G-7 runaway that took Phase 187 from 15 plans to 29.

    Record in the SUMMARY the actual depth used, the file count the agent scoped, and the finding
    counts by severity.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/phases/251-register-integrity/251-REVIEW.md; test -f "$f" || { echo "FAIL missing"; exit 1; }; echo "findings:$(grep -cE "^### (CR|WR|IN)-[0-9]" "$f")"; echo "cleanblock:$(grep -c "^## Verified clean" "$f")"; git diff --name-only -- scripts/ .claude/ .planning/seeds/ | grep -q . && { echo "FAIL phase-251 code was modified"; exit 1; }; echo NO-FIXES-APPLIED'</automated>
  </verify>
  <acceptance_criteria>
    - `251-REVIEW.md` did not exist before this task and exists after it.
    - It contains at least one `## Verified clean` section.
    - Its finding headings match `^### (CR|WR|IN)-[0-9]` — the house shape shared by all six existing
      `*-REVIEW.md` files in 249-253.
    - `git diff --name-only` names **no** file under `scripts/`, `.claude/` or `.planning/seeds/` —
      the pass read and drove, and fixed nothing.
    - The SUMMARY records the depth (`standard`), the scoped file count, and the per-severity counts.
  </acceptance_criteria>
  <done>Phase 251 has been read for the first time by something other than its own verification, and not one line of its code moved.</done>
</task>

<task type="auto">
  <name>Task 2: Drive every reported-live finding against the tree, and record the still_live / fixed_since / refuted triple</name>
  <files>.planning/phases/251-register-integrity/251-REVIEW.md</files>
  <read_first>
    - `.planning/phases/251-register-integrity/251-REVIEW.md` — the findings just written.
    - `.planning/phases/252-close-the-v42-audit-gaps/252-REVIEW-R2.md` `:1-20` and its
      `### Drive evidence, verbatim` block — the reusable shape for a driven verdict.
    - `.planning/phases/254-independent-review-of-249-253/254-PATTERNS.md` §S-2.
  </read_first>
  <action>
    ⛔ **A finding is reported OPEN only after it has been DRIVEN against the tree.** On 2026-09-10 two
    Phase 239 criticals were escalated as live when they had been fixed two days earlier, in an
    ancestor of the reviewer's own base commit. **Each register only knows the one below it; the code
    is the bottom.**

    For every Critical and every Warning in `251-REVIEW.md`, drive it and record the result inline
    under the finding, in a fenced `Drive evidence, verbatim` block carrying the exact command and its
    exact output including the exit code. ⚠ Capture exit codes WITHOUT a pipe — `… | tail` returns
    tail's status.

    Where the finding is about a gate arm being vacuous, the drive is a RED: plant the defect the arm
    claims to catch, run the arm, and record whether it went red. ⭐ A green arm over a planted defect
    is the finding, not a pass. Restore afterwards and prove the restore with an `md5sum` pair —
    before and after — that are identical.

    ⚠ Two mechanical traps measured in this repo, both of which will produce a wrong verdict if
    ignored:
    - `core.autocrlf=true` means a working-tree md5 of a seed body reports drift that does not exist.
      Compare blob-vs-blob via `git archive`, never against the working tree; and `git diff` cannot see
      line-ending damage in content, so it is not the instrument either.
    - `git checkout -- <dir>` is NOT a restore here: it moved 224 seed body digests while `git status`
      read clean. Restore by explicit file path, and prove it with md5.

    Add a `verdicts:` triple to the file's frontmatter — `still_live`, `fixed_since`, `refuted` —
    whose values sum to the number of Critical + Warning findings. A finding that could not be driven
    is recorded as `not_driven` with the reason, never silently as `still_live`.

    Add a `## What this pass did NOT do` section naming, at minimum: the code it did not fix; the
    boundaries it did not re-litigate; and that it is not a §6.3 review.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/phases/251-register-integrity/251-REVIEW.md; grep -q "^verdicts:" "$f" && grep -q "still_live:" "$f" && grep -q "fixed_since:" "$f" && grep -q "refuted:" "$f" || { echo "FAIL triple"; exit 1; }; test "$(grep -c "^## What this pass did NOT do" "$f")" = "1" || { echo "FAIL did-not-do"; exit 1; }; cw=$(grep -cE "^### (CR|WR)-[0-9]" "$f"); de=$(grep -c "Drive evidence, verbatim" "$f"); echo "crit+warn:$cw drive-blocks:$de"; test "$de" -ge "$cw" || { echo "FAIL undriven findings"; exit 1; }; echo DRIVEN'</automated>
  </verify>
  <acceptance_criteria>
    - The frontmatter carries `verdicts:` with `still_live`, `fixed_since` and `refuted`, and their sum
      (plus any `not_driven`) equals the count of `^### (CR|WR)-[0-9]` headings.
    - The count of `Drive evidence, verbatim` blocks is **≥** the count of Critical + Warning findings.
    - Every drive block contains a literal command line and a literal exit code — assert the rendered
      content, not merely that a fenced block exists.
    - Any RED drive that planted a defect records an `md5sum` pair, before and after, that are
      **identical** — the restore is proven, not asserted.
    - `## What this pass did NOT do` exists and names the three items in the action.
    - `git status --short` shows no modified file under `scripts/` or `.claude/`.
  </acceptance_criteria>
  <done>No finding in 251-REVIEW.md is a claim about code that was not run; every restore is md5-proven.</done>
</task>

<task type="auto">
  <name>Task 3: Stamp the honesty frontmatter, and give every finding exactly one recommended disposition</name>
  <files>.planning/phases/251-register-integrity/251-REVIEW.md</files>
  <read_first>
    - `.planning/phases/251-register-integrity/251-REVIEW.md`.
    - `.planning/milestones/v4.0-phases/238-microsoft-graph-onedrive/238-INDEPENDENT-REVIEW.md` `:1-15`
      — the key set to imitate, and the one value that must differ.
    - `.planning/DEBT-06-AUDIT.md` `:20-27` — the counting rule, verbatim.
    - `.planning/phases/254-independent-review-of-249-253/254-CONTEXT.md` — D-08, D-11.
  </read_first>
  <action>
    Write the frontmatter to the key set of `238-INDEPENDENT-REVIEW.md` — `phase`, `reviewed`,
    `review_type`, `reviewer`, `builder`, `range`, `status`, `findings{}` — plus the `verdicts{}` triple
    from Task 2, with these values:
    - `review_type: self-assessed` — spelled exactly, and it is the token used identically across
      every artifact Phase 254 writes.
    - `reviewer: claude (the BUILDER — NOT an AGENTS.md §6.3 independent reviewer)`
    - `builder: claude`
    - `discharges_debt_06: false`
    - `range:` the commit range actually reviewed, re-derived, not copied from this plan.
    - `bus_item: BUS-249` — the ask this pass does NOT answer.

    Then write the disclaimer into the BODY, near the top, where a reader meets it before the findings.
    It must say, in its own words rather than by reference: claude planned, executed and verified Phase
    251 in one session; the verifier was a subagent of the session that orchestrated the build, which
    is what §6.3 forbids; `DEBT-06-AUDIT.md`'s counting rule makes this a **code-review pass**, not an
    independent review; it flips no register; `251-VERIFICATION.md` keeps `verification_mode:
    self-verified` and its `independent_review` key stays `owed`; and `BUS-249` stays OPEN.
    ⭐ State the positive case too, because it is the reason the pass exists: the same instrument found
    2 blockers on 249 and 2 criticals on 253 past green self-verified closes, so *not gate-catchable*
    is the defect class it reaches and a self-assessment reaches it too.

    Finally, give every finding — Critical, Warning and Info — exactly one `**Disposition:**` line
    whose value is one of exactly three literals: `fast-fix`, `next phase`, `accept`. ⛔ No fourth
    value, no hedge, no "fast-fix or next phase". The operator rules on the list; the reviewer
    recommends. ⛔ And nothing is fixed here (D-11).
  </action>
  <verify>
    <automated>bash -c 'f=.planning/phases/251-register-integrity/251-REVIEW.md; test "$(grep -c "^review_type: self-assessed$" "$f")" = "1" || { echo "FAIL token"; exit 1; }; test "$(grep -c "review_type: independent" "$f")" = "0" || { echo "FAIL false-independence-claim"; exit 1; }; test "$(grep -c "^discharges_debt_06: false$" "$f")" = "1" || { echo "FAIL debt key"; exit 1; }; n=$(grep -cE "^### (CR|WR|IN)-[0-9]" "$f"); d=$(grep -c "^\*\*Disposition:\*\* \(fast-fix\|next phase\|accept\)$" "$f"); echo "findings:$n dispositions:$d"; test "$n" = "$d" || { echo "FAIL disposition coverage"; exit 1; }; echo STAMPED'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^review_type: self-assessed$'` is exactly `1`.
    - `grep -c 'review_type: independent'` over the whole file is exactly `0`.
    - `grep -c '^discharges_debt_06: false$'` is exactly `1`.
    - The number of `^\*\*Disposition:\*\* (fast-fix|next phase|accept)$` lines **equals** the number
      of `^### (CR|WR|IN)-[0-9]` finding headings — every finding routed, none twice, none with a
      fourth value.
    - The body disclaimer is asserted on CONTENT, not presence: it contains the literal strings
      `self-verified`, `BUS-249` and `not an independent review`, and states that no register is
      flipped by this file.
    - `.planning/phases/251-register-integrity/251-VERIFICATION.md` is byte-unchanged by this plan —
      `git diff --name-only` does not name it. (Plan 254-04 owns that file.)
  </acceptance_criteria>
  <done>The file cannot be mistaken for a §6.3 review by a reader or by `DEBT-06-AUDIT.md`'s counting rule, and every finding it raises has a recommended route and no fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a review artifact → the `DEBT-06` audit | the audit counts files by their `review_type` frontmatter; a wrong token silently discharges a debt that is still owed |
| the reviewer → the reviewed code | a reviewer that edits what it found stops being a verifier of that fix (`AGENTS.md` §6.3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-254-06 | Spoofing | `251-REVIEW.md` frontmatter | mitigate | `grep -c 'review_type: independent'` must be `0` and `review_type: self-assessed` exactly `1`; `discharges_debt_06: false` is an explicit key, not an omission |
| T-254-07 | Repudiation | a finding reported live that was fixed before the base commit | mitigate | every Critical/Warning carries a `Drive evidence, verbatim` block with command + exit code; count of drive blocks ≥ count of findings |
| T-254-08 | Tampering | Phase 251's shipped code, mutated by a RED drive that did not restore | mitigate | every planted defect restored by explicit path with an identical `md5sum` pair; `git status --short` clean under `scripts/` and `.claude/` |
| T-254-09 | Elevation of privilege | the pass fixing what it found and closing its own loop | mitigate | no `--fix`, no `gsd-code-fixer`; `git diff --name-only` names no reviewed source file; D-11 |
| T-254-10 | Information disclosure | a working-tree md5 reporting seed drift that does not exist (`core.autocrlf=true`) | mitigate | blob-vs-blob via `git archive`; the trap is named in the action so a false finding is not published |
| T-254-SC | Tampering | npm/pip/cargo installs | accept | zero package-manager installs in this plan |
</threat_model>

<verification>
- `251-REVIEW.md` exists where none did; `git log --diff-filter=A` confirms it is a CREATE.
- `review_type: self-assessed` ×1, `review_type: independent` ×0, `discharges_debt_06: false` ×1.
- drive blocks ≥ Critical+Warning findings; dispositions == all findings.
- no file under `scripts/`, `.claude/` or `.planning/seeds/` modified.
</verification>

<success_criteria>
Phase 251 is read, every live claim is driven, every finding has a route, nothing is fixed, and the
file tells the truth about what it is.
</success_criteria>

<output>
Create `.planning/phases/254-independent-review-of-249-253/254-02-SUMMARY.md` when done.
</output>
