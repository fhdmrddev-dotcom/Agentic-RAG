---
phase: 253-the-bootstrap-artifact-tells-the-whole-truth
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [CRED-03]
gap_closure: false

files_modified:
  - scripts/check-greenfield-privileges.py
  - scripts/full-schema-supplement.sql
  - supabase/full-schema.sql
  - backend/tests/unit/test_253_supplement_column_parity.py
  - docs/DEPLOYMENT-WORKFLOW.md

user_setup: []

must_haves:
  truths:
    - "SC#1 — on a scratch database bootstrapped from supabase/full-schema.sql ALONE, a SELECT of public.connector_tokens.access_token_ciphertext issued as the `authenticated` role (SET LOCAL ROLE, row_security ON) is REFUSED. Measured as `authenticated`, never through the service role (D-01, D-03)."
    - "SC#2 — on that same scratch database, `authenticated` can SELECT every column named by connector_service._TABLE_SELECTABLE_KEYS on public.connector_connections; no connector list read answers 42501 (D-17)."
    - "The greenfield harness is shown RED (exit 1) against the UNFIXED supplement before the mirror lands, and GREEN (exit 0) after — the RED drive is recorded with its own transcript (D-19)."
    - "The assertion set is DERIVED by scanning supabase/migrations/ with no exception list, refuses to report a verdict over a collapsed scan set, and prints its derived expectation table into its own evidence (D-05, D-02)."
    - "The supplement's §5 column list and _TABLE_SELECTABLE_KEYS are pinned to each other by a PASSING pytest that needs no database and no JS tool parsing Python (D-08, D-09)."
    - "scripts/full-schema-supplement.sql and supabase/full-schema.sql's tail ship in the SAME COMMIT and are byte-identical, proven by md5 (D-11)."
  artifacts:
    - path: "scripts/check-greenfield-privileges.py"
      provides: "the standalone re-runnable greenfield privilege gate (D-04)"
      exports: ["assert_greenfield_target", "derive_expectations", "main"]
    - path: "scripts/full-schema-supplement.sql"
      provides: "the mirrored table/column ACLs for all seven ACL-bearing tables (D-16)"
      contains: "connector_tokens"
    - path: "supabase/full-schema.sql"
      provides: "the bootstrap artifact whose tail is the supplement, byte for byte (D-11)"
      contains: "connector_tokens"
    - path: "backend/tests/unit/test_253_supplement_column_parity.py"
      provides: "the no-database drift fence between §5 and _TABLE_SELECTABLE_KEYS (D-08)"
  key_links:
    - from: "scripts/full-schema-supplement.sql"
      to: "supabase/full-schema.sql"
      via: "byte-identical tail region, asserted by md5 in the same commit"
      pattern: "connector_tokens"
    - from: "backend/tests/unit/test_253_supplement_column_parity.py"
      to: "backend/app/services/connector_service.py"
      via: "from app.services.connector_service import _TABLE_SELECTABLE_KEYS"
      pattern: "_TABLE_SELECTABLE_KEYS"
    - from: "scripts/check-greenfield-privileges.py"
      to: "supabase/migrations/"
      via: "readdirSync-equivalent scan with a non-vacuity floor"
      pattern: "GRANT|REVOKE"
---

<objective>
Make a database born from `supabase/full-schema.sql` carry the same TABLE and COLUMN privileges as
one built by replaying `supabase/migrations/` — and prove it by connecting to a real scratch
database **as `authenticated`**, not by comparing text.

Purpose: `CR-01` is a live privilege-escalation path on **every new deployment**. `connector_tokens`
holds OAuth ciphertext, two migrations revoke it from `anon`/`authenticated`, and it is mirrored
into **NEITHER** bootstrap artifact — so a greenfield project ships with Supabase's stock
`GRANT ALL ON TABLES TO anon, authenticated` standing over it. `user_settings` and `app_settings`
are in the same state, and they are the exact two tables `BUG-260911-01` found in production with
`anon` holding all privileges. This closes `CRED-03` against the **table** half of the claim Phase
252 closed against the function half.

Output: a re-runnable greenfield gate (`scripts/check-greenfield-privileges.py`), seven mirrored
table blocks in `scripts/full-schema-supplement.sql` + `supabase/full-schema.sql`'s tail, and a
no-database pytest that stops §5 drifting from the response model again.

⛔ CR-01 PRE-DATES 252 and must not be written up as its regression. What 252 added is a §6 header
claiming *"table OR function"* over a gate enforcing only functions. The exposure is older than the
sentence that overstates the coverage.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/253-the-bootstrap-artifact-tells-the-whole-truth/253-CONTEXT.md
@.planning/phases/253-the-bootstrap-artifact-tells-the-whole-truth/253-PATTERNS.md
@CLAUDE.md
</context>

<binding_constraints>

⛔ **NEVER run `supabase db reset` or `supabase db push`.** CLAUDE.md forbids both; they destroy the
operator's dev data (159 documents / 7,953 chunks no migration can restore). D-01's scratch database
exists precisely so neither is needed.

⛔ **Docker is DENIED to you and there is no `psql` on PATH.** `asyncpg` from `backend/venv` is the
only reachable path, not a preference. Invocation on Windows is
`backend/venv/Scripts/python scripts/<name>.py`.

⛔ **`bash scripts/regenerate-full-schema.sh` is NOT AVAILABLE and must not be reached for.** It
hard-exits without `docker` (`:65-68`) and the `supabase` CLI (`:60-63`), and it would re-dump the
**live** schema, dragging unrelated local drift into the artifact.

⛔ **The backend unit suite sits at a ceiling of 71 failures with ZERO headroom.** Task 4's pytest
must be a PASSING test that needs no database. The canonical gate is
`pytest tests/unit -q --continue-on-collection-errors` run in `backend/` with the virtualenv.

⛔ **Do not read `backend/.env`, `POSTGRES_DSN`, or any environment variable for a DSN.** Both
analogs (`apply_migration_178.py:18-23`, `build-recall-bench.py:80-100`) rejected that by name, and
the recorded reason is that an env var is how a local-only script reaches cloud by accident.

⛔ **Restoring a file "md5-identical" is part of every RED drive, and `git checkout -- <dir>` is NOT
a restore on this box** — `core.autocrlf` moved 224 body digests while `git status` read clean.
Restore **explicit paths** and verify by hash.

</binding_constraints>

<measured_corrections>

These four measurements were taken at plan time and **correct the CONTEXT**. This project's house
rule is that a corrected claim is written BESIDE the original, never over it — so each one is
recorded here and must be repeated in the SUMMARY rather than silently applied.

**MC-1 — `supabase/full-schema.sql`'s last 433 lines are BYTE-IDENTICAL to the supplement.**
Verified at plan time: `tail -n 433 supabase/full-schema.sql | md5sum` and
`md5sum scripts/full-schema-supplement.sql` both read `6a58a47651156ef6dccdf75b93365e66`. The tail
is produced by a plain `cat "${SUPPLEMENT}"` at `scripts/regenerate-full-schema.sh:137-151`.
**Consequence:** D-11's same-commit rule is satisfiable by replacing the tail region byte for byte
and re-asserting the md5 equality — **without** running the regenerate script. That is NOT
"hand-editing `full-schema.sql`" in the sense CLAUDE.md forbids: the forbidden thing is
hand-authoring *dump* content, and nothing about the dump half changes here. The region is a
verbatim copy whose equality is machine-checkable, and Task 3 checks it.
**Second consequence:** the harness must apply `supabase/full-schema.sql` **ALONE**. The supplement
is already inside it. `supabase/SETUP.md`'s one-paste story is `full-schema.sql` alone, and a plan
that says *"apply both"* reads as *"the supplement is a separate artifact an operator must also
paste"*, which is false.

**MC-2 — the CONTEXT's "7 tables / 25 statements" does not reproduce; the measured figure is 32
statements across 12 migration files.** Re-derived at plan time:
`grep -rn -E "^[[:space:]]*(GRANT|REVOKE)[[:space:]]+" supabase/migrations/ | grep -viE "EXECUTE ON FUNCTION" | wc -l`
→ **32**, across `118 (4) · 126 (1) · 127 (1) · 128 (1) · 129 (3) · 150 (1) · 151 (2) · 156 (6) ·
168 (3) · 169 (3) · 172 (3) · 177 (4)`. The seven distinct tables in D-16's table DO reproduce; the
statement count and the file list do not — and **`126`, `127` and `150` are named NOWHERE in the
CONTEXT** while each carries a `connector_connections` column grant. (253-PATTERNS.md says *"11
files"* and then lists twelve; twelve is correct.) D-05 is explicit that the assertion set is
DERIVED with **no exception list**, so Task 1 re-derives it mechanically and records the numbers it
actually read.

**MC-3 — D-17's "15 → 19" is wrong in a way that would make Task 4's fence permanently RED, and the
correct target is 20.** Measured at plan time: `ConnectorConnectionResponse` has 21 fields; minus
`_NON_TABLE_RESPONSE_KEYS = {account_email, account_name}` gives `_TABLE_SELECTABLE_KEYS` = **19**.
The supplement's §5 grants **15**, and one of those 15 — **`created_by`** — is **not a response key
at all**. It is granted by `supabase/migrations/118_connector_secret_column_privilege.sql:115`, so
D-05's no-exception-list rule keeps it. The union of every migration column grant on
`connector_connections` is therefore **20** columns: the 19 response keys plus `created_by`.
**Consequence:** the fence in Task 4 is NOT plain set equality. It asserts
(a) `set(_TABLE_SELECTABLE_KEYS) - §5 == set()` — every response key is granted, which is SC#2; and
(b) `§5 - set(_TABLE_SELECTABLE_KEYS) == {"created_by"}` — the one intentional extra, **pinned by
name**, so a SECOND extra reds; and (c) `secret_ciphertext` is absent from §5, which is §5's whole
point. Plain set equality would red on day one and get loosened away.

**MC-4 — the ledger gate is STRUCTURALLY BLIND to every file this plan touches under `scripts/`.**
`scripts/check-hot-file-ledger.cjs:67` sets `WATCHED = [/^backend\/app\//, /^frontend\/src\//]` and
`:51`'s `EXEMPT` additionally drops `/^scripts\//` and `.md`/`.sql`. So
`node scripts/check-hot-file-ledger.cjs 253` exits 0 whether the D-23 rows exist or not. That is the
**structural cause** of CR-08's finding that `full-schema-supplement.sql` had no ledger row for its
entire life at 5 phases — the guardrail was blind by construction, not the author careless. D-23 is
therefore handled as a MANUAL task on plan `253-02` with its own evidence, and **must not claim the
ledger gate as proof**. Widening `WATCHED` is OUT of this phase's fixed scope (it is a change to a
shared guardrail affecting every phase); `253-02` Task 4 plants it as a named finding instead.

</measured_corrections>

<derived_facts>

Measured at plan time from the migrations, so Task 1 and Task 3 do not have to re-discover them.
**Task 1 still re-derives mechanically** — these are here to make the tasks auditable, not to be
retyped in place of a scan.

**The seven ACL-bearing tables and their source statements**

| Table | Migration : line | Statements |
|---|---|---|
| `connector_connections` | `118:103,104,112,130` · `126:84` · `127:225` · `128:106` · `129:101` · `150:58` · `156:31-37` | `REVOKE ALL FROM anon`; `REVOKE ALL FROM authenticated`; five `GRANT SELECT (…)` column blocks; `GRANT INSERT, UPDATE, DELETE … TO authenticated`; a `service_role` triple on `default_ingest_visibility` |
| `connector_tokens` | `129:85,88` · `151:90,92` | `REVOKE ALL ON TABLE … FROM anon, authenticated`; `GRANT SELECT (9 columns) … TO authenticated`; `REVOKE ALL … FROM anon` |
| `connector_watches` | `168:118-120` | `REVOKE ALL … FROM anon`; `GRANT SELECT, INSERT, UPDATE, DELETE … TO authenticated`; same TO `service_role` |
| `connector_watch_items` | `169:115-117` | same three-line shape |
| `connector_sync_runs` | `172:132-134` | same three-line shape |
| `app_settings` | `177:70,71` | `REVOKE ALL … FROM anon`; `REVOKE ALL … FROM authenticated` |
| `user_settings` | `177:79,100` | `REVOKE ALL … FROM anon`; `REVOKE DELETE … FROM authenticated` |

**`connector_tokens`'s 9 granted columns** (`129:88` and `151:92`, identical lists):
`id, connection_id, account_email, account_name, token_type, scopes, expires_at, created_at,
updated_at`. The two columns deliberately ABSENT are `access_token_ciphertext` and
`refresh_token_ciphertext` (`129:51,52`) — those two names are SC#1's subject.

**The five columns §5 is behind** (D-17), each with its migration:
`auth_type`, `status`, `error_message` ← `129:101-105`; `auth_type`, `status` also at `150:58`;
`default_approval_posture` ← `128:106`; `default_ingest_visibility` ← `156:31` (plus `INSERT`/
`UPDATE` at `:32,33` and a `service_role` triple at `:35-37`).

**§5's existing 15 columns:** `id, org_id, created_by, capability, name, config, is_enabled,
last_checked_at, last_check_verdict, created_at, updated_at, mcp_server_url, tool_grants,
discovered_tools, service_id`. After D-17 the block is **20** (see MC-3).

**`_TABLE_SELECTABLE_KEYS`'s 19 keys**, in `ConnectorConnectionResponse` order:
`id, org_id, capability, service_id, name, auth_type, status, error_message, config,
mcp_server_url, default_approval_posture, default_ingest_visibility, tool_grants, discovered_tools,
is_enabled, last_checked_at, last_check_verdict, created_at, updated_at`.
⚠ That ORDER follows the Pydantic model; §5's list is hand-ordered. **Assert set equality (as
qualified in MC-3), never an ordered comparison** — an ordered compare would red on a harmless
field reorder.

**The tail arithmetic:** `wc -l supabase/full-schema.sql` = 7687, `wc -l
scripts/full-schema-supplement.sql` = 433, `grep -n "CROSS-SCHEMA SUPPLEMENT" supabase/full-schema.sql`
= 7250. So the head region is lines 1..7254 and the tail region is 7255..EOF. Task 3 re-derives
these numbers rather than typing them.

</derived_facts>

<tasks>

<task type="auto">
  <name>Task 1: Build the greenfield privilege harness — scratch DB, derived preamble, derived assertion set</name>

  <files>scripts/check-greenfield-privileges.py</files>

  <read_first>
    - `scripts/build-recall-bench.py` — THE primary analog, in full for `:1-64` (docstring shape),
      `:80-210` (`BENCH_DB_NAME`, `LOOPBACK_HOSTS`, `ALLOWED_PORTS`, `BenchTargetRefused`,
      `assert_bench_target`), `:210-240` (`maintenance_dsn`), `:241-336` (`PRELUDE_SQL`),
      `:337-407` (`DROP_SIGNUP_TRIGGER_SQL`), `:421-442` (`_require_asyncpg`, `connect`),
      `:450-473` (create/teardown), `:481-511` (`apply_full_schema` and the `SET row_security = on`
      line), `:557-585` (`_assert_readable_as_authenticated`). ⛔ **Do not re-invent
      `assert_bench_target` — it exists and is already fenced RED by
      `backend/tests/unit/test_241_bench_safety.py`.**
    - `scripts/verify-v40-cloud-migrations.sql:70-92` — the privilege-assertion SQL idiom, and the
      `[REPAIRED D-242-05]` comment explaining why `information_schema.column_privileges` is a trap.
    - `scripts/apply_migration_178.py:1-40` — the hard-coded-loopback-and-say-why convention.
    - `scripts/check-security-advisors.sh` — the skip/exit-code analog D-04 departs from.
    - `scripts/check-schema-acl-parity.cjs:53-66` and `:142-163` — `MIN_MIGRATION_FILES`,
      `VacuousScanError`, and the collapsed-scan-set refusal this harness must reproduce.
    - `scripts/full-schema-supplement.sql:194-262` (§5, including the header at `:237-239` that
      still recommends the wrong catalog) and `:265-305` (§6 header).
    - `supabase/migrations/129_connector_oauth_tokens.sql:45-110` and
      `151_connector_tokens_rls_policy.sql:86-104`.
    - This plan's `<derived_facts>` and `<measured_corrections>`.
  </read_first>

  <action>
Create `scripts/check-greenfield-privileges.py` — a standalone, re-runnable gate (D-04) that proves
SC#1 and SC#2 against a REAL database rather than against text, satisfying D-01 through D-06.

Module docstring, in the `build-recall-bench.py:1-64` shape: state that this is Phase 253 /
`CRED-03` / D-01..D-06; that it issues irreversible `CREATE DATABASE` and `DROP DATABASE` against a
local Postgres cluster that also holds the operator's live Supabase development data; and that
`assert_greenfield_target` runs before every one of them. Record MC-1 verbatim in the docstring: the
harness applies `supabase/full-schema.sql` **ALONE** because the supplement is already its
byte-identical tail, and name the md5 it asserts. Give a `Usage` block with the Windows invocation
`backend/venv/Scripts/python scripts/check-greenfield-privileges.py`.

**The target guard (D-06).** Copy `assert_bench_target`'s seam wholesale, renamed
`assert_greenfield_target`, with `GreenfieldTargetRefused(RuntimeError)`, `LOOPBACK_HOSTS`,
`_ACCEPTED_SCHEMES` and `ALLOWED_PORTS = frozenset({54322})` unchanged. ⚠ **The one departure the
analog cannot absorb:** `assert_bench_target` compares the database name with `==` against a single
constant and says so in its own message. D-06 requires a unique-per-run name, so replace that
equality with an **anchored regex on a generated-name grammar** — the module-level constant
`GREENFIELD_DB_PATTERN = re.compile(r"^greenfield_acl_[0-9]{1,10}_[0-9]{1,14}$")`, matched with
`fullmatch`. Keep the host-list arm FIRST (`urlparse(...).port` raises `ValueError` on a
comma-separated netloc, so no later arm may read `.port` before that one has refused), keep the
`parsed.query` refusal, and state in a comment that the anchors are load-bearing because an
unanchored pattern would accept `greenfield_acl_1_1_prod`. Generate the name from
`os.getpid()` and `int(time.time())` inside the script; ⛔ the `CREATE`/`DROP` may only ever
interpolate a value THIS SCRIPT GENERATED, never one parsed from a flag. There is no `--dsn` flag
and no environment variable is read (S-5).

**The preamble, DERIVED not typed (D-02).** Connect to the maintenance DSN (same server, `/postgres`
database) and run the `pg_default_acl` query written out at `253-PATTERNS.md:197-199` —
`SELECT pg_get_userbyid(d.defaclrole) AS owner, n.nspname, d.defaclobjtype, d.defaclacl FROM
pg_default_acl d JOIN pg_namespace n ON n.oid = d.defaclnamespace WHERE n.nspname = 'public';`.
Render each returned row back into an `ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT <privs> ON
<TABLES|SEQUENCES|FUNCTIONS|TYPES> TO <roles>` statement, mapping `defaclobjtype` `r/S/f/T` to
`TABLES/SEQUENCES/FUNCTIONS/TYPES`, and execute them in the scratch database **before** applying the
artifact. ⚠ `defaclrole` matters: a row owned by `supabase_admin` can only be replayed by a role
that is a member of it — the maintenance connection is the superuser `postgres`, so issue it there;
if a row cannot be replayed, PRINT the row and the reason and continue, never skip silently. D-02
requires the derived text be printed into the harness's own evidence, so echo every rendered
statement under a `derived default-privilege preamble (pg_default_acl):` heading, along with the
owner count. ⛔ **D-03: a harness that omits this preamble is a FALSE GREEN** — without default
privileges the table has no grants at all, `authenticated` is refused for the WRONG reason, and SC#1
passes while proving nothing. Say that in a comment directly above the preamble call, and make the
harness REFUSE to continue (exit 2) if the `pg_default_acl` query returns zero rows.

Also carry `build-recall-bench.py`'s `PRELUDE_SQL` remainder, which a fresh database on the same
cluster genuinely needs because `full-schema.sql`'s supplement writes into both schemas: the `auth`
schema, `auth.users`, the `auth.uid()` stub, `storage.buckets`, `storage.objects`,
`storage.foldername`, the `supabase_realtime` publication, and `GRANT USAGE ON SCHEMA auth, storage
TO anon, authenticated, service_role`. ⛔ Lift the `auth.uid()` stub's verbatim refusal comment
(`build-recall-bench.py:224-235`) WITH it — it must never be copied into `supabase/migrations/` nor
into `full-schema.sql`, because production's `auth.uid()` is Supabase Auth's own and a hand-rolled
one would be an authentication bypass on 156 call sites.

**Applying the artifact.** Read `supabase/full-schema.sql`, apply it in one `conn.execute`, and on
`asyncpg.PostgresError` translate `exc.position` into a LINE NUMBER before re-raising (copy
`_line_of_offset`). Then run `DROP_SIGNUP_TRIGGER_SQL`. ⛔ **Then `SET row_security = on`, and this
is not optional:** `supabase/full-schema.sql:33` is `SET row_security = off`, a SESSION setting that
survives the apply, so without re-enabling it every later read runs with RLS DISABLED and a refusal
reads as a privilege failure when it is a session failure. Put the measured reason in a comment; it
is SEED-266 arm (b) in action. Before applying, assert MC-1: read
`scripts/full-schema-supplement.sql`, compare its md5 against the md5 of `full-schema.sql`'s last
`len(supplement_lines)` lines, and exit 2 with a named message if they differ — the harness is then
also a same-commit-rule checker for free.

**The derived assertion set (D-05, no exception list).** Scan `supabase/migrations/` with
`os.listdir` filtered by `^\d+_.*\.sql$`, sorted by the leading integer; ⛔ refuse to report a
verdict below a floor of 120 files, raising a `VacuousScanError`-equivalent (S-2, and Phase 242
measured a sibling gate printing `subject: 0 files` and `ledger gate OK` in one breath). Parse each
file for `GRANT`/`REVOKE` on a TABLE or COLUMN against `anon` / `authenticated` / `service_role`,
excluding `EXECUTE ON FUNCTION`; the grant lists are MULTI-LINE, so match across newlines. Replay
the parsed statements in ascending migration-number then line order into an expected map keyed
`(role, table, column_or_None, privilege)`, **last statement wins**, and write that rule down in the
docstring so a reviewer can audit it rather than infer it. PRINT the full derived expectation table,
plus the statement count and the file list it actually read — MC-2 says the CONTEXT's figure does
not reproduce, so the harness must report the number it measured, never a constant.

**Asserting it, as `authenticated` (SC#1).** Inside `async with conn.transaction()`, run
`SET LOCAL row_security = on` then `SET LOCAL ROLE authenticated`, and issue REAL reads so the
privilege is measured rather than a catalog row asserted; `RESET ROLE` before the transaction
closes. ⛔ **Use `has_table_privilege()` / `has_column_privilege()` for the derived set, and
`pg_class.relacl` / `pg_attribute.attacl` as the corroborating half. NEVER
`information_schema.column_privileges`** — it returns only the grants the CONNECTING role can see,
so it comes back EMPTY under a role that is neither grantor nor grantee, which is a false verdict
that says nothing about the database (repaired at D-242-05 in
`scripts/verify-v40-cloud-migrations.sql:70-92`). ⚠ The supplement's own §5 header at `:237-239`
still recommends that wrong catalog; Task 3 corrects it.

On top of the derived set, assert these named cases explicitly so a parser regression cannot make
SC#1 silently unmeasured: (1) a literal `SELECT access_token_ciphertext FROM public.connector_tokens`
as `authenticated` must RAISE, and the harness reports FAIL if it succeeds; (2) the same for
`refresh_token_ciphertext`; (3) every column in the supplement's own §5 `GRANT SELECT (` list must
answer `has_column_privilege('authenticated', 'public.connector_connections', <col>, 'SELECT')` =
true — that is SC#2's half the harness owns, while the cross-language binding to
`_TABLE_SELECTABLE_KEYS` is Task 4's pytest, and the two compose; (4) `has_table_privilege('anon',
'public.app_settings', 'SELECT')` and the same for `public.user_settings` must both be FALSE —
these are the two tables `BUG-260911-01` found in production with `anon` holding all privileges.

**Teardown.** `DROP DATABASE IF EXISTS "<name>" WITH (FORCE)` behind the same guard, in a `finally`,
and then PROVE the drop by re-reading `SELECT count(*) FROM pg_database WHERE datname = $1` and
raising if it is still there — a `finally: drop` that is not verified is how a scratch database
accumulates on a shared cluster.

**Exit codes (D-04).** `0` = clear · `1` = a privilege violation · `2` = harness error. ⛔ A skip is
a THIRD verdict and is NOT exit 0: when nothing is listening on `127.0.0.1:54322`, print a distinct
verdict line naming the reason and the remedy — `greenfield privileges SKIPPED — no Postgres on
127.0.0.1:54322 (<errno>); start it with scripts/start-local-infra.ps1` — and exit **2**. Exit 0 on
an unreachable database re-creates exactly the vacuous pass this project has now measured three
times.
  </action>

  <verify>
    <automated>backend/venv/Scripts/python -c "import ast,sys; ast.parse(open('scripts/check-greenfield-privileges.py',encoding='utf-8').read()); print('parses')"</automated>
  </verify>

  <acceptance_criteria>
    - `backend/venv/Scripts/python scripts/check-greenfield-privileges.py` runs to completion
      against the live local cluster and prints a verdict line; it does NOT raise an unhandled
      traceback.
    - `grep -c "information_schema.column_privileges" scripts/check-greenfield-privileges.py`
      returns `0` outside of a comment explaining why it is refused.
    - `grep -c "SET row_security = on" scripts/check-greenfield-privileges.py` returns at least `1`.
    - `grep -c "SET LOCAL ROLE authenticated" scripts/check-greenfield-privileges.py` returns at
      least `1`.
    - `grep -cE "os\.environ|getenv|load_dotenv" scripts/check-greenfield-privileges.py` returns `0`
      (S-5 — no env var, no `.env` read, no cloud-reachable shape).
    - `grep -cE "db (reset|push)" scripts/check-greenfield-privileges.py` returns `0`.
    - The script's own stdout contains a `derived default-privilege preamble (pg_default_acl):`
      section listing at least one rendered `ALTER DEFAULT PRIVILEGES` statement, and names the
      owner count it read (D-02 evidence).
    - The script's own stdout names the number of migration files scanned and the number of
      GRANT/REVOKE statements parsed, and both are re-derived at run time — no hardcoded total
      appears outside a comment (MC-2, D-05).
    - After a successful run, `psql`-free verification that the scratch database is gone:
      the script's own teardown assertion (`count(*) FROM pg_database`) must print a confirmation
      line; a non-zero count raises.
  </acceptance_criteria>

  <done>
`scripts/check-greenfield-privileges.py` exists, creates and drops a uniquely-named scratch database
behind an anchored target guard, derives its default-privilege preamble from `pg_default_acl`,
applies `supabase/full-schema.sql` alone, re-enables row security, derives its assertion set from
every migration with no exception list, and measures every assertion as `authenticated` via
`has_table_privilege`/`has_column_privilege`. Exit codes are 0/1/2 with a skip printed as a distinct
verdict line at exit 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: RED drive — the harness must FAIL against the UNFIXED supplement (D-19)</name>

  <files>scripts/check-greenfield-privileges.py</files>

  <read_first>
    - The file written in Task 1.
    - `scripts/check-schema-acl-parity.cjs:312-322` — `RED arm 1` and
      `RED arm 2 (COUNTERFACTUAL)`, the house shape for "the gate does not print everything it
      knows".
    - `docs/HOT-FILE-LEDGER.md` § `frontend/src/lib/workspaceAllowedExt.ts` — the
      falsify-by-deletion-then-restore-md5-identical precedent.
    - CLAUDE.md § *Parallel execution* rule 4 — this task touches the shared local Postgres cluster.
  </read_first>

  <action>
⛔ **This is its own task because D-19 says so: a guard nobody has seen fire is not a guard, and the
greenfield harness is explicitly subject to that rule — it must be shown RED against the UNFIXED
supplement BEFORE the mirror lands.** Run it now, while `scripts/full-schema-supplement.sql` still
contains zero occurrences of `connector_tokens`, and capture the transcript.

Drive four arms and record each verbatim in the plan's SUMMARY:

**RED arm 1 — the real, unfixed artifact.** Run
`backend/venv/Scripts/python scripts/check-greenfield-privileges.py` against the tree as it stands.
It must exit **1**, and the failure output must NAME `public.connector_tokens` and at least one of
`access_token_ciphertext` / `refresh_token_ciphertext`, and must NAME `public.app_settings` and
`public.user_settings` as readable by `anon`. Capture the exit code and the named lines. ⛔ If it
exits 0 here, the harness is wrong, not the artifact — the CONTEXT's
`grep -c connector_tokens scripts/full-schema-supplement.sql` → 0 is re-verifiable in one command
and the exposure is real; do not proceed to Task 3 until this arm is genuinely red.

**RED arm 2 — the COUNTERFACTUAL that the harness does not just print everything it knows.** The
same run's output must NOT list `public.connector_connections.secret_ciphertext` as a violation:
that column is correctly ungranted by §5 today, so a harness that reports it has an inverted
predicate. Assert its absence from the failure output.

**RED arm 3 — the target guard refuses a wrong target.** Without a database, call
`assert_greenfield_target` directly from Python on four refusable DSNs and assert each raises
`GreenfieldTargetRefused`: a non-loopback host (`postgresql://postgres:postgres@10.0.0.5:54322/greenfield_acl_1_1`),
a comma-separated host LIST, a DSN carrying a `?host=` query parameter, and a name that the
generated-name grammar must reject (`postgresql://postgres:postgres@127.0.0.1:54322/postgres`).
⛔ The last one is the arm that matters: it proves the harness cannot drop the operator's live
`postgres` database. Also assert the anchoring explicitly by feeding
`greenfield_acl_1_1_prod` and asserting it is refused — an unanchored pattern would accept it.

**RED arm 4 — the unreachable-database SKIP is not a pass.** Temporarily assert the skip path by
calling the harness's connect seam against port `54399` (nothing listens there) and confirm the
printed verdict contains the word `SKIPPED`, names the remedy `scripts/start-local-infra.ps1`, and
the process exits **2**, never 0.

⛔ **Do not modify `scripts/full-schema-supplement.sql` in this task.** If any temporary file was
written, restore explicit paths and verify by md5 — `git checkout -- <dir>` is NOT a restore on this
box.
  </action>

  <verify>
    <automated>backend/venv/Scripts/python scripts/check-greenfield-privileges.py; echo "exit=$?"</automated>
  </verify>

  <acceptance_criteria>
    - `backend/venv/Scripts/python scripts/check-greenfield-privileges.py` exits **1** against the
      pre-mirror supplement, and the transcript is pasted into the SUMMARY.
    - The RED arm 1 output contains the strings `connector_tokens` and `access_token_ciphertext`.
    - The RED arm 1 output contains `app_settings` and `user_settings`.
    - The RED arm 1 output does NOT contain `secret_ciphertext` as a violation line (RED arm 2, the
      counterfactual).
    - All five target-guard DSNs of RED arm 3 raise `GreenfieldTargetRefused`, including the bare
      `/postgres` database name and `greenfield_acl_1_1_prod`; the transcript names each.
    - The RED arm 4 run prints `SKIPPED`, names `scripts/start-local-infra.ps1`, and exits `2`.
    - `git status --short scripts/full-schema-supplement.sql supabase/full-schema.sql` shows no
      modification after this task.
    - `md5sum scripts/full-schema-supplement.sql` still reads
      `6a58a47651156ef6dccdf75b93365e66` at the end of this task.
  </acceptance_criteria>

  <done>
The greenfield harness has been SEEN to fire against the real, unfixed exposure, against a
counterfactual, against four refusable targets, and against an unreachable database — and the
artifacts it judges are byte-unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mirror all seven ACL-bearing tables into the supplement AND full-schema.sql's tail, same commit</name>

  <files>scripts/full-schema-supplement.sql, supabase/full-schema.sql</files>

  <behavior>
    - After this task, `grep -c connector_tokens scripts/full-schema-supplement.sql` returns >= 2
      (it returns 0 today).
    - After this task, `tail -n $(wc -l < scripts/full-schema-supplement.sql) supabase/full-schema.sql`
      hashes identically to `scripts/full-schema-supplement.sql`.
    - After this task, `git diff supabase/full-schema.sql` touches only lines at or below the
      `CROSS-SCHEMA SUPPLEMENT` banner — the dump half is byte-unchanged.
    - After this task, §5's `GRANT SELECT (` list on `public.connector_connections` names 20
      columns, one per line, and `secret_ciphertext` is still not among them.
  </behavior>

  <read_first>
    - `scripts/full-schema-supplement.sql` in full, especially `:20-34` (the MAINTENANCE `ACLs ->`
      source list, which names only 118 and 181), `:194-262` (§5, including `:200-201`'s
      *"`grep -c '^GRANT\|^REVOKE' supabase/full-schema.sql` -> 0, measured 2026-08-09"* and
      `:237-239`'s `information_schema.column_privileges` recommendation), and `:265-305` (§6's
      header, the *"table OR function"* claim).
    - `supabase/migrations/129_connector_oauth_tokens.sql:83-106` — the verbatim `connector_tokens`
      block and the `connector_connections` three-column grant.
    - `supabase/migrations/151_connector_tokens_rls_policy.sql:86-104`.
    - `supabase/migrations/168_connector_watches.sql:116-121`,
      `169_connector_watch_items.sql:113-118`, `172_connector_sync_runs.sql:130-135`.
    - `supabase/migrations/177_rls_app_settings_user_settings.sql:65-102`.
    - `supabase/migrations/128_connector_connection_posture.sql:104-110`,
      `150_connector_oauth_client_secret.sql:55-60`,
      `156_grant_default_ingest_visibility_column.sql:28-40`,
      `126_mcp_connector_connections.sql:82-90`,
      `127_connector_connection_service_identity.sql:222-230`.
    - `scripts/regenerate-full-schema.sh:101-107` (`--no-privileges`) and `:137-151` (the `cat`
      that produces the tail).
    - This plan's MC-1, MC-2, MC-3 and `<derived_facts>`.
  </read_first>

  <action>
⛔ **D-10: every statement here is COPIED VERBATIM from the migration, never retyped, with the source
migration and line range named INLINE** — 252's own recorded lesson, and the reason the gate's
expected set and the mirrored set end up being literally the same characters. ⛔ **D-09: §5's column
list stays HAND-MAINTAINED, one column per line.** No generated region inside a hand-written
pasteable file; the whole point of §5 is that the ABSENT column (`secret_ciphertext`) is visible in
a diff, and the artifact must stay something an operator can read and paste. Task 4's pytest
DETECTS drift; it does not generate the list.

**A. Extend §5's `connector_connections` block to 20 columns (D-17, corrected by MC-3).** Add
`auth_type`, `status`, `error_message` (naming `129:101-105`, and noting `150:58` re-states
`auth_type, status`), `default_approval_posture` (naming `128:106`), and
`default_ingest_visibility` (naming `156:31`). One per line, in the existing hand-order convention.
Also mirror `156:32,33`'s `GRANT INSERT (default_ingest_visibility)` and
`GRANT UPDATE (default_ingest_visibility)` to `authenticated`, and `156:35-37`'s `service_role`
triple — D-05 admits no exception list, and those three statements are in the migrations. Write MC-3
into a comment directly above the block: the list is **20**, not 19, because `created_by` is granted
by `118:115` and is NOT a key of `ConnectorConnectionResponse`; Task 4's fence pins that single
difference BY NAME so a second extra reds.

**B. Add a new §5b — `connector_tokens` (D-16, D-10).** Copy `129:85` (`REVOKE ALL ON TABLE
public.connector_tokens FROM anon, authenticated;`), `129:88`'s nine-column `GRANT SELECT (...) ON
public.connector_tokens TO authenticated;` and `151:90`'s `REVOKE ALL ON TABLE public.connector_tokens
FROM anon;`, naming both migrations and both line ranges inline. State in the header, in one
sentence, what this block is: `access_token_ciphertext` and `refresh_token_ciphertext` (`129:51,52`)
are the two columns deliberately absent, migration 129's own comment says they *"must NEVER be
granted SELECT to authenticated or anon"*, and a greenfield bootstrap grants them today because
`pg_dump --no-privileges` cannot carry a REVOKE. Note that 151's RLS policy `connector_tokens_select`
IS in the dump, so the policy shipped while the grant did not — a policy without the column grant is
as dead as a grant without the policy, and here it was the dangerous half that survived.

**C. Add §5c — the three watch tables (D-16).** Copy `168:118-120`, `169:115-117` and `172:132-134`
verbatim (each is the same three-line `REVOKE ALL … FROM anon` + two `GRANT SELECT, INSERT, UPDATE,
DELETE` shape), naming each migration and line range inline.

**D. Add §5d — `app_settings` and `user_settings` (D-16).** Copy `177:70,71,79,100` verbatim,
including 177's own comment explaining that `authenticated` keeps the table-level grants the
policies need and that DELETE is deliberately not granted a policy. ⛔ Put the finding in the header
in plain words: **these are the exact two tables `BUG-260911-01` found in PRODUCTION with RLS
disabled and `anon` holding all privileges**, and until this commit they were mirrored in neither
bootstrap artifact.

**E. Correct the three sentences this edit makes false, in the same edit.** (1) `:200-201` claims
`grep -c '^GRANT\|^REVOKE' supabase/full-schema.sql` → 0, measured 2026-08-09; re-measure it now and
write the new figure with today's date, keeping the original beside it struck through rather than
overwritten. (2) `:20-34`'s MAINTENANCE `ACLs ->` source list names only 118 and 181; extend it to
the twelve ACL-bearing migrations MC-2 measured. (3) `:237-239` recommends
`information_schema.column_privileges` as the way to re-derive the column set — replace it with
`has_column_privilege` / `pg_attribute.attacl` and name the reason (D-242-05: that catalog returns
only what the CONNECTING role can see, so it comes back empty and produces a false verdict).

**F. Apply the identical text to `supabase/full-schema.sql`'s tail (D-11, MC-1).** ⛔ Do NOT run
`scripts/regenerate-full-schema.sh` — it needs `docker` and the `supabase` CLI, both unavailable,
and it would re-dump live drift. Instead: compute `OLD=$(wc -l < scripts/full-schema-supplement.sql)`
BEFORE editing (433) and `HEAD_LINES=$(( $(wc -l < supabase/full-schema.sql) - OLD ))`; after editing
the supplement, write `head -n $HEAD_LINES supabase/full-schema.sql` followed by the NEW supplement
into a temp file under the session scratchpad and move it into place. Then assert the equality in
both directions: the md5 of `tail -n $(wc -l < scripts/full-schema-supplement.sql)
supabase/full-schema.sql` equals the md5 of `scripts/full-schema-supplement.sql`, and
`git diff --stat supabase/full-schema.sql` shows changes confined to the region below the
`CROSS-SCHEMA SUPPLEMENT` banner. Record MC-1 in the SUMMARY: this is a verbatim-copy region whose
equality is machine-checkable, not hand-authored dump content, so CLAUDE.md's *never hand-edit
`full-schema.sql`* rule is honoured rather than bent — and say so explicitly so a reviewer does not
read it as a rule violation.

⛔ Both files ship in the **SAME COMMIT**. Nothing automates that pairing (S-6 names it as the one
same-commit rule with no gate behind it), so the `tail | md5sum` assertion IS the proof.
  </action>

  <verify>
    <automated>test "$(tail -n $(wc -l < scripts/full-schema-supplement.sql) supabase/full-schema.sql | md5sum | cut -d' ' -f1)" = "$(md5sum scripts/full-schema-supplement.sql | cut -d' ' -f1)" && echo "tail identity OK"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c connector_tokens scripts/full-schema-supplement.sql` returns `>= 2` (it returns `0`
      before this task).
    - `grep -c connector_tokens supabase/full-schema.sql` returns `>= 2`.
    - `grep -c access_token_ciphertext scripts/full-schema-supplement.sql` returns `0` inside any
      `GRANT SELECT (` list — the name may appear only in a comment explaining its absence.
    - `grep -c "public.app_settings" scripts/full-schema-supplement.sql` and
      `grep -c "public.user_settings"` each return `>= 1`.
    - `grep -c "connector_watches\|connector_watch_items\|connector_sync_runs"
      scripts/full-schema-supplement.sql` returns `>= 3`.
    - The §5 `GRANT SELECT (` block on `public.connector_connections` names exactly **20** columns,
      one per line, and `secret_ciphertext` is not among them.
    - `tail -n $(wc -l < scripts/full-schema-supplement.sql) supabase/full-schema.sql | md5sum`
      equals `md5sum scripts/full-schema-supplement.sql` (MC-1 / D-11).
    - `git diff -U0 supabase/full-schema.sql` shows no hunk whose line numbers fall above the
      `CROSS-SCHEMA SUPPLEMENT` banner — the dump half is byte-unchanged.
    - `grep -n "information_schema.column_privileges" scripts/full-schema-supplement.sql` returns
      only lines that explain why that catalog is refused.
    - Every added SQL statement names its source migration and line range in an adjacent comment
      (D-10); a reviewer can `sed -n` the cited range and see the same characters.
  </acceptance_criteria>

  <done>
All seven ACL-bearing tables are mirrored into `scripts/full-schema-supplement.sql`, the identical
text is `supabase/full-schema.sql`'s tail (md5-proven), §5 carries 20 hand-maintained columns without
`secret_ciphertext`, and the three sentences this edit falsified are corrected in the same edit.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: The no-database column fence (D-08), the GREEN re-drive, and the deploy parity checklist</name>

  <files>backend/tests/unit/test_253_supplement_column_parity.py, docs/DEPLOYMENT-WORKFLOW.md</files>

  <behavior>
    - Test 1 (CONTROL, runs before the real file is read anywhere in the module): the SQL extractor
      returns a deliberately-wrong member set from synthetic input rather than normalising it away.
    - Test 2 (CONTROL): the extractor returns `None`, not `set()`, when fed a line of PROSE that
      mentions column names — `full-schema-supplement.sql:222-240` is 19 lines of comment naming
      `secret_ciphertext`, `mcp_server_url`, `tool_grants`, `discovered_tools` and `service_id`
      directly above the real `GRANT SELECT (`, so an extractor that reads comments would silently
      include the one column whose absence is the point.
    - Test 3: every key of `_TABLE_SELECTABLE_KEYS` appears in §5's granted column set — the set
      difference is empty (SC#2).
    - Test 4: the reverse difference is exactly `{"created_by"}`, pinned BY NAME (MC-3), so a second
      unexplained extra reds.
    - Test 5: `secret_ciphertext` is absent from §5's granted column set.
    - Test 6: the file path resolves and the extractor found a block — a green result cannot mean
      "the file moved" or "the regex matched nothing".
  </behavior>

  <read_first>
    - `backend/tests/unit/test_211_closed_set_agreement.py` in full — THE analog. Especially
      `:35-62` (path resolution and the `parents[N]` arithmetic comment), `:181-205` (the extractor
      and the `None`-vs-`set()` distinction), `:208-243` (the falsify-the-extractor-first control),
      `:245-270` (the two non-vacuity guards).
    - `backend/app/services/connector_service.py:100-145` — `_RESPONSE_KEYS`,
      `_NON_TABLE_RESPONSE_KEYS`, `_TABLE_SELECTABLE_KEYS`, `_SELECTABLE_COLUMNS`, and the
      module-scope `assert "secret_ciphertext" not in _RESPONSE_KEYS` this fence sits BESIDE rather
      than instead of.
    - `backend/app/models/connector.py:553-578` — `ConnectorConnectionResponse`'s 21 fields.
    - `scripts/full-schema-supplement.sql` §5 as edited by Task 3.
    - `docs/DEPLOYMENT-WORKFLOW.md:102-140` — §5 parity checklist and §6 pre-promotion checklist,
      and line `115` / `134` where `scripts/check-security-advisors.sh` sits.
    - `backend/tests/conftest.py:1-30` — it sets env vars only; no fixture does repo-root
      resolution, so this test does its own `parents[3]`.
  </read_first>

  <action>
**A. Write `backend/tests/unit/test_253_supplement_column_parity.py` (D-08).** It imports
`_TABLE_SELECTABLE_KEYS` from `app.services.connector_service` and reads
`scripts/full-schema-supplement.sql` from disk. ⛔ **No database, no network, no environment
variable** — D-04 keeps the greenfield HARNESS out of `backend/tests/unit` because that suite sits
at a 71-failure ceiling with zero headroom and must not gain a live-database precondition; a
file-reading fence over it is a different thing, and `backend/tests/unit/test_241_bench_safety.py`
is the precedent that the project already draws the line exactly there. ⭐ This is a **PASSING**
test, so it raises the suite total without touching the ceiling.

Resolve the repo root as `Path(__file__).resolve().parents[3]` and copy
`test_183_skip_parse_parity.py:28`'s habit of writing the arithmetic down in a comment — *"if this
file is moved, fix the `parents[3]` arithmetic above rather than deleting this control"*.

Write the extractor as a named-group regex that captures the BODY of the
`GRANT SELECT ( … ) ON public.connector_connections TO authenticated;` block across newlines, then
splits on commas and strips whitespace. ⛔ It must return `None` when it does not match, never an
empty set: an empty set and "the regex did not match" are different facts, and conflating them is
exactly how a cross-language fence ships green while reading nothing. ⛔ It must EXCLUDE comment
lines — the block is preceded by 19 lines of prose naming five column names — so strip `--`
comments before matching, and prove that with the prose control.

⛔ **D-08 rejects having the Node gate parse the Python constant, and this is why:** the value is
`tuple(ConnectorConnectionResponse.model_fields)` minus a frozenset — derived from a Pydantic
model's keys, which is exactly what a regex cannot see. A JS fence would pin a SPELLING rather than
a VALUE.

Assert set equality in the three-part form MC-3 requires: `set(_TABLE_SELECTABLE_KEYS) - granted ==
set()`; `granted - set(_TABLE_SELECTABLE_KEYS) == {"created_by"}` with the reason (`118:115` grants
it; it is not a response key) in the assertion message; and `"secret_ciphertext" not in granted`.
⚠ Assert SET equality, never ORDER — `_TABLE_SELECTABLE_KEYS` follows the Pydantic model order and
§5's list is hand-ordered, so an ordered compare would red on a harmless field reorder. Say that in
a comment.

Every assertion message must quote what it READ, not just what it expected — a failure message that
says "sets differ" sends the next reader to the wrong file.

**B. GREEN re-drive of the harness (D-19's second half).** Re-run
`backend/venv/Scripts/python scripts/check-greenfield-privileges.py` against the tree as Task 3 left
it. It must now exit **0**, and its evidence must show `connector_tokens.access_token_ciphertext`
refused as `authenticated`, all 20 §5 columns readable, and `anon` holding neither `app_settings`
nor `user_settings`. Paste the transcript into the SUMMARY beside Task 2's RED transcript — the pair
is what D-19 asks for.

**C. Add the harness to the deploy parity checklist (D-04).** In
`docs/DEPLOYMENT-WORKFLOW.md` §5 add a row beside the existing **Security advisors check** row
(`:115`) and a checkbox beside `:134` in §6, naming
`backend/venv/Scripts/python scripts/check-greenfield-privileges.py`, what it proves (a greenfield
bootstrap carries the same table and column privileges as a replayed migration history, measured as
`authenticated`), and its three exit codes including that a SKIP is exit 2 and never a pass. Add a
dated line to the doc's own change log at the bottom, in the format the 2026-09-14 entry uses.

**D. Confirm the backend ceiling did not move.** Run the canonical gate and read the tail line: in
`backend/`, with the virtualenv, `pytest tests/unit -q --continue-on-collection-errors`. The failed
count must be **at most 71** and collection errors must be **0**. ⛔ Capture the failing SET, not
just the count — `| tail` keeps a count and throws away the set, and this project has already
published a wrong baseline that way.
  </action>

  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_253_supplement_column_parity.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_253_supplement_column_parity.py -q`
      exits `0` with at least 6 passing cases.
    - Deleting `auth_type` from a COPY of `scripts/full-schema-supplement.sql` and pointing the
      test's path constant at that copy makes the test FAIL naming `auth_type`; the original is
      restored and `md5sum scripts/full-schema-supplement.sql` matches its pre-drive value.
    - Adding a fabricated column `bogus_col` to the same COPY makes Test 4 FAIL naming `bogus_col`
      (the reverse-difference arm is not vacuous).
    - `grep -cE "psycopg|asyncpg|supabase|requests|os\.environ" backend/tests/unit/test_253_supplement_column_parity.py`
      returns `0` — no database, no network, no env var.
    - `backend/venv/Scripts/python scripts/check-greenfield-privileges.py` exits **0**, and the
      transcript is pasted into the SUMMARY beside Task 2's RED transcript.
    - `cd backend && venv/Scripts/python -m pytest tests/unit -q --continue-on-collection-errors`
      reports **at most 71 failed** and **0 collection errors**; the failing SET (not only the
      count) is recorded in the SUMMARY.
    - `grep -c "check-greenfield-privileges" docs/DEPLOYMENT-WORKFLOW.md` returns `>= 2` (§5 row and
      §6 checkbox).
  </acceptance_criteria>

  <done>
A passing, database-free pytest pins §5's column list to `_TABLE_SELECTABLE_KEYS` with the single
intentional difference named; the greenfield harness has been driven RED before the mirror and GREEN
after; and the harness is on the deploy parity checklist beside `get_advisors(security)`. The
backend unit ceiling of 71 is intact.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PostgREST `authenticated` role → `public.*` tables | Any org member's JWT crosses here. Column privileges are the LAST gate before RLS; a missing REVOKE means the row policy is the only thing standing between a user and an OAuth ciphertext. |
| `anon` role → `public.app_settings` / `public.user_settings` | Unauthenticated. `BUG-260911-01` measured this boundary OPEN in production. |
| `scripts/check-greenfield-privileges.py` → the local Postgres cluster on `127.0.0.1:54322` | The harness issues `CREATE DATABASE` and `DROP DATABASE` against a cluster that also holds the operator's live dev data — 159 documents and 7,953 chunks no migration or backup can restore. |
| `supabase/full-schema.sql` → every future deployment | The artifact IS the privilege posture of a greenfield database. A statement absent from it is absent from every new deploy, silently, in the permissive direction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-253-01 | Information Disclosure | `public.connector_tokens` on a greenfield DB | mitigate | Task 3B mirrors `129:85` / `129:88` / `151:90` verbatim; Task 1 asserts a literal `SELECT access_token_ciphertext` as `authenticated` RAISES; Task 2 proves the assertion fires before the fix. |
| T-253-02 | Elevation of Privilege | `public.app_settings`, `public.user_settings` vs `anon` | mitigate | Task 3D mirrors `177:70,71,79,100`; Task 1 asserts `has_table_privilege('anon', …, 'SELECT')` is FALSE for both. This is `BUG-260911-01`'s exact pair. |
| T-253-03 | Denial of Service (functional) | every connector list read on a greenfield DB | mitigate | Task 3A takes §5 to 20 columns (D-17 corrected by MC-3); Task 4's pytest keeps `_TABLE_SELECTABLE_KEYS ⊆ §5` so a new response field cannot silently re-open `42501`. |
| T-253-04 | Tampering / Destruction | the operator's live `postgres` database on `:54322` | mitigate | `assert_greenfield_target` runs before every destructive statement; anchored `^greenfield_acl_[0-9]{1,10}_[0-9]{1,14}$` grammar (`fullmatch`); host-list arm FIRST; `?query` refused; loopback + port 54322 only; the name is GENERATED, never parsed from a flag; Task 2 RED arm 3 drives five refusals including the bare `/postgres`. ⛔ `supabase db reset` / `db push` are forbidden by CLAUDE.md and appear nowhere. |
| T-253-05 | Spoofing (false assurance) | the harness's own verdict | mitigate | `SET row_security = on` after the apply (`full-schema.sql:33` leaves it OFF and it is a SESSION setting); `SET LOCAL ROLE authenticated` inside a transaction, never the service role; `has_table_privilege`/`has_column_privilege` instead of `information_schema.column_privileges`, which returns EMPTY under a non-grantee role and is a measured false verdict (D-242-05). |
| T-253-06 | Spoofing (false assurance) | the derived preamble | mitigate | D-02/D-03: the preamble is DERIVED from `pg_default_acl` and PRINTED into evidence; the harness exits 2 if that query returns zero rows, because without default privileges `authenticated` is refused for the WRONG reason and SC#1 would pass while proving nothing. |
| T-253-07 | Denial of Service (vacuity) | the derived assertion set | mitigate | S-2: the migration scan is `os.listdir`-derived, refuses a verdict below a 120-file floor, and prints the count it measured. A skip on an unreachable database is exit **2** with a named reason, never exit 0. |
| T-253-08 | Tampering | `auth.uid()` stub leaking into a shipped artifact | mitigate | The stub lives ONLY in the harness's prelude and carries `build-recall-bench.py:224-235`'s verbatim refusal comment. Task 3's acceptance confirms `git diff` on `supabase/full-schema.sql` is confined to the tail region, so no prelude text can reach the artifact. |
| T-253-SC | Tampering | npm/pip installs | accept | **No package is installed by this plan.** `asyncpg` is already a `backend/venv` dependency and `pytest` is already the suite runner; `RESEARCH.md` carries no Package Legitimacy Audit because there is nothing to audit. If an executor finds itself typing `pip install`, that is a scope change and it stops instead. |
</threat_model>

<verification>
- `backend/venv/Scripts/python scripts/check-greenfield-privileges.py` → exit **1** before Task 3,
  exit **0** after. Both transcripts in the SUMMARY (D-19).
- `grep -c connector_tokens scripts/full-schema-supplement.sql` → `0` before Task 3, `>= 2` after.
- `tail -n $(wc -l < scripts/full-schema-supplement.sql) supabase/full-schema.sql | md5sum` ==
  `md5sum scripts/full-schema-supplement.sql` (D-11 / MC-1).
- `cd backend && venv/Scripts/python -m pytest tests/unit/test_253_supplement_column_parity.py -q` →
  exit 0.
- `cd backend && venv/Scripts/python -m pytest tests/unit -q --continue-on-collection-errors` →
  at most **71 failed**, **0 collection errors**; the failing SET recorded, not just the count.
- `node scripts/check-schema-acl-parity.cjs` still exits **0** (this plan must not break the function
  half plan `253-02` then rewrites).
- `git status --short` shows exactly the five files in `files_modified` and nothing else.
</verification>

<success_criteria>
- ROADMAP SC#1 is TRUE and MEASURED as `authenticated`: on a database bootstrapped from
  `supabase/full-schema.sql` alone, `select access_token_ciphertext from connector_tokens` is
  refused.
- ROADMAP SC#2 is TRUE: every column `_TABLE_SELECTABLE_KEYS` names is granted on a fresh database,
  and a fence pins the two so a new response field cannot silently drift them.
- All seven ACL-bearing tables (D-16) are mirrored — no allowlist, no exception list, no seed
  deferring a table to later.
- The supplement and `full-schema.sql` ship in one commit with a machine-checked byte identity
  (D-11).
- MC-1, MC-2, MC-3 and MC-4 are each recorded in the SUMMARY beside the CONTEXT claim they correct,
  never over it.
- The backend unit ceiling of 71 failures is intact and the failing set is published.
</success_criteria>

<output>
Create `.planning/phases/253-the-bootstrap-artifact-tells-the-whole-truth/253-01-SUMMARY.md` when
done. It MUST contain: Task 2's RED transcript and Task 4's GREEN transcript side by side; the
re-derived migration/statement counts (MC-2); the 20-column §5 list with `created_by` named as the
single intentional extra (MC-3); the `tail | md5sum` pair (MC-1); MC-4 stated as a finding handed to
plan `253-02`; and the backend unit failing SET.
</output>
