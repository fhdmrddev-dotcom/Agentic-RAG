---
phase: 217-the-library-one-home-for-documents
plan: 08
type: execute
wave: 3
depends_on: ["217-01", "217-04", "217-06"]
files_modified:
  - frontend/src/types/index.ts
  - frontend/src/hooks/useDocuments.ts
  - frontend/src/components/ingestion/ingestionStages.ts
  - frontend/src/components/ingestion/IngestionStrip.tsx
  - frontend/src/components/ingestion/__tests__/IngestionStrip.test.tsx
  - frontend/src/__tests__/hooks/useDocuments.test.ts
  - .planning/sketches/218-the-library-and-its-tabs/COPY.js
  - .planning/sketches/218-the-library-and-its-tabs/index.html
  - .planning/sketches/218-the-library-and-its-tabs/drive.cjs
  - .planning/sketches/218-the-library-and-its-tabs/BUILD-CONTRACT.generated.md
autonomous: true
requirements: [LIB-03, LIB-04]
decisions: [D-217-01, D-217-02, D-217-09, D-217-10, D-217-11, D-217-19, D-217-23, D-217-24]

must_haves:
  truths:
    - "A file being ingested shows the six stages the pipeline actually writes, in the order the backend writes them — the strip never jumps backwards"
    - "A conditional stage the pipeline would never run is STRUCK THROUGH, never left as an empty box that looks pending"
    - "A failed document's later segments are DIMMED / not reached — a third state, distinct from pending and from skipped"
    - "A completed document's strip does not read ingestion_step at all — it is 'metadata' by residue, not by observation"
    - "There is no percentage and no ETA on the strip"
  artifacts:
    - path: "frontend/src/components/ingestion/ingestionStages.ts"
      provides: "the six stages in BACKEND WRITE ORDER + the skipped predicate"
      contains: "extracting_tables"
    - path: "frontend/src/components/ingestion/IngestionStrip.tsx"
      provides: "the six-segment strip with four renderings: done, active, pending, skipped, not-reached"
    - path: "frontend/src/types/index.ts"
      provides: "the four new Document fields and the five detail row types"
      contains: "tables_stage_applies"
    - path: "frontend/src/components/ingestion/__tests__/IngestionStrip.test.tsx"
      provides: "an ORDERED comparison against documents.py's live source, plus the four status renderings"
  key_links:
    - from: "frontend/src/components/ingestion/ingestionStages.ts"
      to: "backend/app/api/documents.py"
      via: "a ?raw fence comparing ORDERED arrays of the six ingestion_step writes"
      pattern: "\"ingestion_step\":\\s*\"([a-z_]+)\""
    - from: "frontend/src/hooks/useDocuments.ts"
      to: "frontend/src/components/ingestion/IngestionStrip.tsx"
      via: "the Realtime spread-merge preserves server-derived fields absent from payload.new"
      pattern: "tables_stage_applies"
---

<objective>
LIB-03 / SC#3: the six stages the pipeline actually writes, in the order it writes them, with an
honest rendering for every document status.

Purpose: two measured facts decide this. **First**, `documents.py` writes
`extracting`(:1834) -> `chunking`(:2014) -> `embedding`(:2042) -> `extracting_tables`(:2080) ->
`extracting_images`(:2091) -> `metadata`(:2201), while the sketch drew Tables and Images second and
third — rendered against the real sequence the strip would **jump backwards**, lighting segment 5
and then segment 2 (D-217-09). **Second**, `ingestion_step` is never cleared, so it is load-bearing
diagnostic residue and NOT an observation on a finished document (D-217-23).

Output: an order constant fenced against the backend's live source, a strip with four honest
renderings, the wire types every later plan consumes, and the sketch's own stage order corrected in
the same commit.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/217-the-library-one-home-for-documents/217-CONTEXT.md
@.planning/phases/217-the-library-one-home-for-documents/217-PATTERNS.md
@.planning/phases/217-the-library-one-home-for-documents/217-VALIDATION.md

<interfaces>
Landed by plan `217-01` on the wire — `DocumentResponse` now serializes:
`extractor: str | None`, `ingestion_step: str | None`, `tables_stage_applies: bool`,
`images_stage_applies: bool`.

Shipped — `frontend/src/components/ingestion/DocumentStatusBadge.tsx:27-32`, THE GUARD the strip
inherits (`ingestion_step` is consulted ONLY while `status === "processing"`):

```tsx
const termKey: TermKey =
  status === "processing"
    ? `ingest.${ingestionStep}` in TERM_MAP ? (`ingest.${ingestionStep}` as TermKey) : "status.processing"
    : (`status.${status}` as TermKey)
const label = usePlainLabel(termKey)
```

Shipped — all six labels already exist in `frontend/src/lib/termMap.ts` at `:38, :43, :48, :53,
:58, :63` (*Reading the file · Reading tables · Reading images · Splitting into sections · Making it
searchable · Reading document details*), each with a `helper` and a `technical` form for the ⌥
reveal. ⛔ **Never a seventh vocabulary** — sketch fence `A5c` asserts termMap carries all six.

Shipped — `frontend/src/hooks/useDocuments.ts:47-62`, the reconcile whose comment already states the
rule this plan extends:

```ts
if (payload.eventType === "UPDATE") {
  const newDoc = payload.new as Document
  // Realtime payload reflects ONLY the documents table — table_count + image_count +
  // chunk_count are server-side aggregates … NOT in payload.new. Spread-merge …
  setDocuments((prev) => prev.map((d) => (d.id === newDoc.id ? { ...d, ...newDoc } : d)))
  if (newDoc.status === "completed" || newDoc.status === "failed") loadDocuments().catch(console.error)
} else if (payload.eventType === "INSERT") {
  const newDoc = payload.new as Document          // ⚠ :63-68 — NO merge on this arm
  setDocuments((prev) => (prev.some((d) => d.id === newDoc.id) ? prev : [newDoc, ...prev]))
}
```

⚠ `ingestion_step`, `extractor` and `chunk_count` are REAL columns and ride `payload.new`.
`tables_stage_applies` / `images_stage_applies` are DERIVED and behave exactly like
`table_count` — the spread-merge protects them on UPDATE, and the INSERT arm does not, so they must
be OPTIONAL on the frontend type and the strip must tolerate `undefined`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The wire types every later plan consumes, and the hook comment that owns the seam</name>
  <files>frontend/src/types/index.ts, frontend/src/hooks/useDocuments.ts</files>
  <read_first>
    - `frontend/src/types/index.ts:480-510` — the `Document` block from `:488` and `ingestion_step` at `:499`
    - `frontend/src/hooks/useDocuments.ts` (all 120 lines — especially `:44-70`)
    - `docs/HOT-FILE-LEDGER.md` § `frontend/src/hooks/useDocuments.ts` (`8 / 3 / 120`, FIRES exactly at threshold) — D-217-01 requires reading it; its binding invariant *"Realtime is a hint, never a source of truth… a design that assumes the socket delivered every transition will show a stale stage strip after a reconnect"* NAMES this plan's defect before the phase did
    - `backend/app/models/document.py` as landed by plan 01, and `backend/app/api/document_queries.py`'s `DocumentQueryRow` as landed by plan 03 — the TypeScript mirrors must match those shapes field for field
  </read_first>
  <action>
    In `frontend/src/types/index.ts`, inside `Document`:
    add `extractor?: string | null`, `tables_stage_applies?: boolean` and `images_stage_applies?: boolean`
    — all OPTIONAL, because the Realtime INSERT arm (`useDocuments.ts:63-68`) casts `payload.new`
    with no merge and a document uploaded in ANOTHER TAB arrives without the derived pair.
    ⚠ **Correct the existing `ingestion_step` docblock at `:499` in this same edit**: it lists FOUR
    steps (`'extracting', 'chunking', 'embedding', 'metadata'`) and the pipeline writes SIX. Replace
    the enumeration with the six in backend write order and add the sentence that makes the field
    readable: it is NEVER cleared, so on a `completed` document it reads `"metadata"` by RESIDUE.

    Add the five detail row types mirroring the backend models, so plans 10 and 11 consume them
    rather than re-deriving: `DocumentChunkRow`, `DocumentTableRow`, `DocumentImageRow`,
    `DocumentQueryRow`, `DocumentContentResponse`. Field names and optionality must match the
    Pydantic models exactly. ⚠ `DocumentImageRow` carries `description` and NO image bytes —
    `document_images` stores none, so a `thumbnail`/`url` field would promise a thing that does not
    exist.

    In `frontend/src/hooks/useDocuments.ts`: **no logic change** — RESEARCH measured that the merge
    is already correct for all four new fields. Extend the existing comment block at `:48-56` to
    name them explicitly and state which side of the line each falls on: `ingestion_step` and
    `extractor` are real columns and ride `payload.new`; `tables_stage_applies` and
    `images_stage_applies` are server-derived and are protected by the spread-merge exactly as
    `table_count` is; and the INSERT arm has no merge, which is why both are optional on the type.
    ⭐ **This comment is the point of listing this file at all** — it is the file in the middle of
    this phase's main seam, and Phase 214's five blockers were all values whose middle file belonged
    to no plan. State in the summary that the hook's G-5 obligation is honoured by construction:
    zero new branches, one comment.
  </action>
  <acceptance_criteria>
    - `grep -n "tables_stage_applies\|images_stage_applies\|extractor" frontend/src/types/index.ts` shows three optional fields inside `Document`
    - The `ingestion_step` docblock enumerates SIX steps in the order `extracting, chunking, embedding, extracting_tables, extracting_images, metadata` and contains the word `residue`
    - `grep -n "DocumentChunkRow\|DocumentTableRow\|DocumentImageRow\|DocumentQueryRow\|DocumentContentResponse" frontend/src/types/index.ts` returns five exported types
    - `grep -n "thumbnail\|image_url\|b64" frontend/src/types/index.ts` returns nothing new
    - `git diff frontend/src/hooks/useDocuments.ts` contains ONLY comment lines — no statement, no branch, no hook call changed
    - `npx tsc --noEmit -p tsconfig.app.json` -> no new errors
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.app.json && npx vitest run src/__tests__/hooks/useDocuments.test.ts --maxWorkers=2</automated>
  </verify>
  <done>The wire types mirror the backend field for field, the four-step docblock lie is corrected, and the hook's reconcile rule names the four new fields explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: The stage order constant and the strip — four honest renderings, no percentage</name>
  <files>frontend/src/components/ingestion/ingestionStages.ts, frontend/src/components/ingestion/IngestionStrip.tsx</files>
  <read_first>
    - `frontend/src/components/ingestion/DocumentStatusBadge.tsx` (all ~40 lines — the guard at `:27-32` and the `styles` status palette at `:10-15`, which is the ONE status vocabulary)
    - `frontend/src/lib/termMap.ts:30-70` — the six `ingest.*` entries with their plain/helper/technical forms
    - `backend/app/api/documents.py` around `:1834`, `:2014`, `:2042`, `:2074-2095`, `:2201`, `:2266-2273`, `:2292-2295` — the six write sites, the ONE `if raw and mime_type:` that contains both conditionals, and the terminal writes that do NOT clear the column
    - `backend/app/services/multimodal_service.py:479` (the `extracted_doc` fast path) and `:504` / `:759` (the two bare `return`s)
    - `.claude/skills/sketch-findings-agentic-rag/SKILL.md` — the ingestion/status surfaces it covers
    - `.planning/sketches/218-the-library-and-its-tabs/index.html` + `COPY.js` §4 — the drawn shape and the vocabulary
  </read_first>
  <action>
    Create `frontend/src/components/ingestion/ingestionStages.ts` — a strict leaf, zero React
    imports. Export `INGESTION_STAGES` as an ORDERED array in **backend write order**
    (D-217-09): `extracting` -> `chunking` -> `embedding` -> `extracting_tables` ->
    `extracting_images` -> `metadata`, each entry carrying its key, whether it is conditional, and
    which applicability flag governs it. ⛔ Do NOT use the sketch's drawn order; the sketch is being
    corrected to match, in task 3 of this plan.

    Export the predicate D-217-24 rules, verbatim in behaviour:
    `isStageSkipped(stage, doc)` is true **iff** the stage is conditional AND its applicability flag
    is explicitly `false` AND the corresponding count (`table_count` / `image_count`) is `0`.
    ⚠ Mime alone is NOT sufficient — the `extracted_doc` fast path can supply tables or images for
    ANY mime. ⚠ When the applicability flag is `undefined` (the Realtime INSERT arm), the stage
    renders as PENDING, never as skipped — the honest arm when applicability is unknown.

    Create `frontend/src/components/ingestion/IngestionStrip.tsx` rendering six segments with
    exactly **four** renderings and no fifth:
    - **done** — reached;
    - **active** — the current stage, only while `status === "processing"`;
    - **pending** — not yet reached;
    - **skipped** — STRUCK THROUGH (D-217-19: an absent thing that looks pending is the failure this
      avoids);
    - **not reached** — DIMMED, the third non-done state D-217-23 adds, used ONLY for segments after
      the failure point on a `failed` document.
    (The strip therefore has three non-done renderings: done · struck-through · dimmed.)

    Per status:
    `pending` -> all six pending. `processing` -> up to and including `ingestion_step` done, that one
    active, later pending, inapplicable conditionals struck through.
    `completed` -> all applicable segments done, inapplicable conditionals struck through, and
    ⛔ **`ingestion_step` is NOT read at all** — it is `"metadata"` by residue (D-217-23 / Pitfall 3).
    `failed` -> segments up to `ingestion_step` done, that step is the FAILURE POINT, later segments
    dimmed/not-reached; the reason comes from the row's existing `error_message`, which
    `DocumentList` already renders — do not invent a second reason vocabulary.

    Labels come from `usePlainLabel` over `TERM_MAP`'s six `ingest.*` entries; the status palette
    comes from `DocumentStatusBadge`'s `styles` map. ⛔ Never a seventh vocabulary and never a second
    status palette. ⛔ **No percentage, no ETA, no three-segment bar** (D-217-19) — two of the six
    stages are decided while the file runs, so there is no honest denominator.

    ⚠ **Do NOT null or clear `ingestion_step` anywhere, and do not ask the backend to:**
    `backend/app/services/text_sanitize.py:9` diagnoses the BUG-260825-01 NUL defect by reading
    `status=failed / ingestion_step=embedding`. Nulling it deletes a diagnostic.
  </action>
  <acceptance_criteria>
    - `INGESTION_STAGES.map(s => s.key)` equals `["extracting","chunking","embedding","extracting_tables","extracting_images","metadata"]` — ORDERED, not sorted
    - `isStageSkipped` returns `false` when the applicability flag is `undefined`, `false` when the flag is `false` but the count is `> 0`, and `true` only when the flag is `false` AND the count is `0`
    - Rendering a `completed` document whose `ingestion_step` is `"embedding"` still shows all applicable segments done — the component reads `status` first and never branches on `ingestion_step` for `completed`; assert by rendering, not by inspection
    - Rendering a `failed` document shows at least one segment in the dimmed/not-reached rendering, and that rendering's class/attribute is DISTINCT from both the pending and the skipped rendering
    - `grep -n "%\|ETA\|remaining\|estimate" frontend/src/components/ingestion/IngestionStrip.tsx` returns nothing
    - `grep -n "Reading the file\|Splitting into sections\|Making it searchable" frontend/src/components/ingestion/IngestionStrip.tsx` returns nothing — labels come from `usePlainLabel`, not from literals
    - `npx tsc --noEmit -p tsconfig.app.json` -> no new errors
  </acceptance_criteria>
  <verify>
    <!-- ⚠ NOT `IngestionStrip.test.tsx` — TASK 3 of this plan creates it, so naming it here errors
         file-not-found at execute-plan's HARD GATE and sends the executor into a repair cycle over a
         plan that was correct. This arm runs a SHIPPED suite (the strip inherits
         `DocumentStatusBadge`'s guard and its status palette, so a break there is a real regression)
         plus a source gate over this task's own two output files. The strip suite command lives on
         task 3, which creates it. -->
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.app.json && npx vitest run src/__tests__/components/DocumentStatusBadge.test.tsx --maxWorkers=2 && node -e 'const fs=require("fs");const s=fs.readFileSync("src/components/ingestion/ingestionStages.ts","utf8");const i0=s.indexOf("INGESTION_STAGES");if(i0<0)throw new Error("INGESTION_STAGES is not exported");const body=s.slice(i0);const want=["extracting","chunking","embedding","extracting_tables","extracting_images","metadata"];const at=want.map(k=>body.search(new RegExp(k+"[^a-z_]")));if(at.some(i=>i<0))throw new Error("a stage key is missing from INGESTION_STAGES: "+JSON.stringify(at));if(at.some((v,i)=>i&&v<at[i-1]))throw new Error("INGESTION_STAGES is not in backend write order: "+JSON.stringify(at));if(!/isStageSkipped/.test(s))throw new Error("the isStageSkipped predicate is absent");const strip=fs.readFileSync("src/components/ingestion/IngestionStrip.tsx","utf8");if(/ETA|remaining|estimate|%/.test(strip))throw new Error("a percentage or an ETA reached the strip");if(/Reading the file|Splitting into sections|Making it searchable/.test(strip))throw new Error("a label literal was inlined instead of coming from usePlainLabel");console.log("task 2 source gate OK")'</automated>
  </verify>
  <done>Six segments render in backend write order with four distinct renderings, no percentage, no new vocabulary, and `completed` never consults the residue column.</done>
</task>

<task type="auto">
  <name>Task 3: The ORDERED source fence, the strip suite, and the sketch's stage order corrected + re-emitted</name>
  <files>frontend/src/components/ingestion/__tests__/IngestionStrip.test.tsx, frontend/src/__tests__/hooks/useDocuments.test.ts, .planning/sketches/218-the-library-and-its-tabs/COPY.js, .planning/sketches/218-the-library-and-its-tabs/index.html, .planning/sketches/218-the-library-and-its-tabs/drive.cjs, .planning/sketches/218-the-library-and-its-tabs/BUILD-CONTRACT.generated.md</files>
  <read_first>
    - `frontend/src/components/workflows/argumentModel.test.ts:24-80` (the cross-language `?raw` fence idiom) and `frontend/src/components/files/FileRow.sweep.test.ts:32-41`, `:100-105` (the three-way guard + the LINE-ANCHORED stripper)
    - `frontend/src/__tests__/components/DocumentStatusBadge.test.tsx` — the render half to model the strip's cases on
    - `frontend/src/__tests__/hooks/useDocuments.test.ts` (201 L — ⚠ the gate has NEVER executed it)
    - `.planning/sketches/218-the-library-and-its-tabs/COPY.js:59-85` (`STAGES`, drawn order), `drive.cjs:152-175` (`A5`..`A5f` — ⚠ **`A5b` SORTS before comparing, so it will NOT fire on a reorder**; only `A5e` and the human eye will), and `drive.cjs:817` (`--emit` renders `stageRow` from `COPY.STAGES` while the *"Measured against the live tree"* table renders the backend order — **the generated contract already prints both orders, disagreeing**)
  </read_first>
  <action>
    Create `frontend/src/components/ingestion/__tests__/IngestionStrip.test.tsx` with two halves.

    ⭐ **The fence half.** `?raw` over `backend/app/api/documents.py`; extract every
    `"ingestion_step": "<name>"` write with a CRLF-tolerant regex, dedupe PRESERVING ORDER, and
    assert the result equals `INGESTION_STAGES.map(s => s.key)` as an **ORDERED** array comparison.
    ⛔ Do not copy `drive.cjs`'s `A5b`, which sorts both sides before comparing and is therefore
    blind to exactly the reorder D-217-09 exists to fix. Guard three ways: a length assertion on the
    `?raw` import, an identity symbol only `documents.py` contains, and a non-vacuity control
    asserting six steps were extracted before any claim rests on them.

    **The render half.** One case per status (`pending`, `processing`, `completed`, `failed`), plus:
    a `.txt` document whose conditionals are struck through (`applies === false`, `count === 0`); a
    document whose `tables_stage_applies` is `false` but `table_count > 0` (the `extracted_doc` fast
    path) rendering the stage as REACHED, not skipped; a document with `undefined` flags rendering
    the conditionals as PENDING; a negative fence asserting the rendered output contains no `%` and
    no `ETA`/`remaining`; and a case asserting the `failed` dimmed rendering is distinct from both
    pending and skipped.

    Extend `frontend/src/__tests__/hooks/useDocuments.test.ts` with the seam cases that make this
    phase's main wire honest: a Realtime UPDATE payload carrying `ingestion_step` but NOT carrying
    `tables_stage_applies` must leave the previously-fetched derived flags intact (the spread-merge),
    and an INSERT payload with neither must produce a document the strip renders without crashing.
    ⚠ M-1 remains manual — no test that mocks the fetch can prove the cold-load case; this covers the
    merge, not the load.

    **The sketch, four edits then emit (D-217-11 — Pitfall 7 is treating `--emit` as the whole
    obligation):**
    1. `COPY.js:71-78` — reorder `STAGES` into backend write order;
    2. `index.html` — reorder the drawn stage row to match;
    3. `drive.cjs` `A5e` (`:164-167`) — its regex requires
       `Read…Tbl…Img…Split…Index…Label` in the sketch's OLD drawn order and must be rewritten for the
       new one; while there, make `A5b` compare ORDERED arrays so the sketch stops being blind to a
       reorder;
    4. `node .planning/sketches/218-the-library-and-its-tabs/drive.cjs --emit` from the repo root,
       committing the regenerated contract in this same commit.
    ⛔ Do not touch `A3b` (plan 06 owns it) or `A7b`/`A7c`/the `src()` paths (plan 04 owns them).
    ⚠ After the reorder, `--emit`'s `stageRow` and its *"Measured against the live tree"* table must
    print the SAME order — assert that by reading the regenerated file, because that disagreement is
    the D-217-09 contradiction made visible inside one file.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/ingestion/__tests__/IngestionStrip.test.tsx --maxWorkers=2` passes with at least 12 cases
    - The ordered fence FAILS when `INGESTION_STAGES` is temporarily reordered — state that this RED was observed
    - The non-vacuity control asserts exactly six extracted steps BEFORE the ordered comparison
    - `npx vitest run src/__tests__/hooks/useDocuments.test.ts --maxWorkers=2` passes, including the two new merge cases
    - `node .planning/sketches/218-the-library-and-its-tabs/drive.cjs` prints `0 failed`
    - `grep -n "extracting_tables" .planning/sketches/218-the-library-and-its-tabs/COPY.js` shows it AFTER `embedding` in the `STAGES` array
    - In the regenerated `BUILD-CONTRACT.generated.md`, the `Ingestion stages` line and the *"backend ingestion steps"* row of the measured table list the SAME six names in the SAME order
    - `git diff .planning/sketches/218-the-library-and-its-tabs/drive.cjs` shows no hunk at `A3b`, `A7b`, `A7c` or the `src()` constants
  </acceptance_criteria>
  <verify>
    <automated>node .planning/sketches/218-the-library-and-its-tabs/drive.cjs && cd frontend && npx vitest run src/components/ingestion/__tests__/IngestionStrip.test.tsx src/__tests__/hooks/useDocuments.test.ts --maxWorkers=2</automated>
  </verify>
  <done>The order is pinned against the backend's live source as an ordered array, the four status renderings are covered, and the sketch now draws the order the pipeline writes — contract regenerated in the same commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server row -> strip render | `ingestion_step` and `error_message` are server-written strings rendered into the DOM |
| Realtime socket -> client state | `payload.new` is a database row delivered over a channel the client does not control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-217-27 | Tampering | `ingestion_step` rendered as a label | mitigate | The value is never rendered raw: it is a KEY into `TERM_MAP`, and an unknown key falls back to `status.processing` exactly as `DocumentStatusBadge.tsx:27-32` already does. An unexpected value cannot inject a string into the UI |
| T-217-28 | Information disclosure | `error_message` on a failed document | accept | Already rendered by `DocumentList` today; this plan adds no new exposure and introduces no second reason vocabulary. React escapes it as a text child |
| T-217-29 | Spoofing (UX) | a stage that looks pending forever | mitigate | The dimmed/not-reached third state (D-217-23) exists so a stalled file does not look like it is still working, and `undefined` applicability renders pending rather than falsely skipped. Both asserted by render cases |
| T-217-30 | Tampering | deleting a diagnostic | mitigate | `ingestion_step` is NEVER cleared by this plan or asked to be cleared — `text_sanitize.py:9` reads `status=failed / ingestion_step=embedding` to diagnose BUG-260825-01. Stated as a red line in the task action |
| T-217-31 | Denial of service | Realtime UPDATE storm during ingest | accept | Unchanged behaviour: the hook's merge and its terminal-transition refetch are untouched (the diff is comment-only), so this plan adds no new fetch on any transition |
| T-217-SC | Tampering | npm/pip/cargo installs | mitigate | No package installed by this plan |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/ingestion/__tests__/IngestionStrip.test.tsx src/__tests__/hooks/useDocuments.test.ts --maxWorkers=2` -> passes
- `node .planning/sketches/218-the-library-and-its-tabs/drive.cjs` -> `0 failed`
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` -> no new errors
- ⚠ Manual rows that stay OWED and cannot be closed here: **M-1** (a document already mid-ingest when the Library opens shows its stage — invisible to any mocked-fetch test), **M-4** (the strip never jumps backwards — only a real ingest proves a sequence), and G-4 rows **G4-2**, **G4-3**, **G4-4**, **G4-10**. ⭐ **G4-4 (reload the page mid-ingest) is the only thing that catches the D-217-10 defect live.**
</verification>

<success_criteria>
- Six segments in backend write order, pinned by an ORDERED fence over `documents.py`'s live source
- `skipped` means `!applies && count === 0`, never mime alone; `undefined` renders pending
- A `failed` document's later segments are dimmed, distinct from pending and from skipped
- `completed` never reads `ingestion_step`; the column is never cleared
- No percentage, no ETA, no seventh vocabulary
- The sketch draws the same order and its contract is regenerated from a green drive
</success_criteria>

<output>
Create `.planning/phases/217-the-library-one-home-for-documents/217-08-SUMMARY.md` when done.
Record: the observed RED of the ordered fence, the drive's assertion count, and the statement that
`useDocuments.ts`'s G-5 obligation is honoured by construction (comment-only diff, zero new branch).
</output>
