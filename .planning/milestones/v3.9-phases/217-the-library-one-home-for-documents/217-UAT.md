---
phase: 217
slug: the-library-one-home-for-documents
artifact: UAT
created: 2026-08-29
created_by: 217-12
status: awaiting-operator
rows_total: 16
rows_driven: 0
rows_owed: 16
---

# Phase 217 — G-4 lived-experience scoreboard

> ⛔ **A scoreboard that lists only what passed is not a scoreboard.** Every row below exists whether
> or not it was driven. An undriven row reads `⛔ OWED` **with a named reason** — never omitted, and
> never reported as passing.
>
> Source of the rows: `217-VALIDATION.md` § *G-4 lived-experience rows* (`G4-1`…`G4-10`) and
> § *Manual-Only Verifications* (`M-1`…`M-6`), plus two rows added at close by `217-12` and marked
> **NEW**. Drive order is the order printed here.

## Status at close of `217-12`

**Every automated layer is green** — count gate `153/153 · 6664 · 0 failing`, sketch drive
`200 passed · 0 failed`, backend `test_217_*` `40 passed`, typecheck at its exact pre-phase baseline
of 33, CLAUDE.md size gate and deploy-drift both clear. Full evidence:
`217-VALIDATION.md` § *Closing Evidence*.

⛔ **Nothing in this file was driven.** Driving it requires a running app, a real browser and a human
eye; the closing executor has none of the three. **The phase closes with these rows OWED, and that is
recorded as a DECISION** — CLAUDE.md: *"Closing a phase with owed manual UAT rows is legitimate … but
state it as a DECISION, never as a claim that everything ran."*

**How to drive:** `cd frontend && npm run dev`. ⚠ vite binds **IPv6 only** — use `localhost`, never
`127.0.0.1`. If local Supabase will not connect, the diagnosis is one command —
`netsh int ipv4 show excludedportrange protocol=tcp`, looking for an **UNMARKED** range straddling
54322 — and the recovery is `powershell -ExecutionPolicy Bypass -File scripts/start-local-infra.ps1`.

---

## ⭐ Drive `G4-4` FIRST

`G4-4` is **the only thing in this project that can catch the D-217-10 defect.** A component test
supplies whatever shape its author chose, so it passes **before AND after** the fix.

⭐ **The `217-12` seam audit independently landed on the same row.** `DocumentList.tsx:422` is a
**pre-existing** consumer of `doc.ingestion_step` — byte-unchanged by this phase, in no plan's
`files_modified` — whose *input* changed when `217-01` made the backend serialize the field. Its
render path runs through `DocumentStatusBadge.tsx`, whose suite is in **neither** count-gate knob.
A consumer whose input changed while its own bytes did not is exactly the Phase 214 shape.

---

## The rows

| # | Scenario | How failure is recognised | Verdict | Observation / reason |
|---|---|---|---|---|
| **G4-4** ⭐ | Upload a large PDF; press **F5 while it is still processing** | the strip shows **no stage** until the next transition fires | ⛔ **OWED** | **DRIVE FIRST.** Requires a running app + a real mid-ingest reload. No automated layer in this repo can observe it. |
| G4-1 | Land on the Library cold; upload a real PDF without hunting | the dropzone is not the first thing seen, or the target folder is unclear | ⛔ OWED | "first thing seen" is a perceptual claim; jsdom has no viewport. |
| G4-2 | Watch that PDF ingest **end to end** on the Ingestion tab | a segment lights out of order, a percentage appears, or a stage stays pending forever | ⛔ OWED | Covers `M-4`. A unit test asserts one snapshot of state; only a real ingest proves the **sequence**. |
| G4-3 | Upload a `.txt` and watch its strip | Tables/Images are **not struck through** — an absent thing looks pending | ⛔ OWED | Needs a real `.txt` through the real pipeline. |
| G4-10 | Ingest a file that **FAILS** | later segments render as *pending* (looks like it is still working) rather than **dimmed / not reached** — D-217-23 | ⛔ OWED | Needs a genuinely failing ingest. ⚠ See the `.xls` finding below — it is one cheap way to produce one. |
| G4-5 | Open a document with tables; read them in the **430px** panel | the table is clipped rather than scrollable, or a wide table pushes the panel | ⛔ OWED | ⚠ **Structurally unprovable here:** jsdom's `getBoundingClientRect` is always zero, so **only a human can prove a table scrolls.** |
| G4-6 | Select a saved view from the sidebar, then click a folder | the tab and the list beneath it disagree, **even for one frame** | ⛔ OWED | The reducer is proven exhaustively (25 cases); the *frame* is not. |
| G4-7 | Toggle Deep Midnight ↔ light with the tab bar visible | the active tab reads as **a hole** in either theme | ⛔ OWED | Covers `M-3`. `tabsContrast.test.ts` proves L(active) > L(track) from tokens; **it cannot prove perception**. |
| G4-8 | Open **Settings** and **Library Health** afterwards | their tab bars regressed as collateral (the two other `ui/tabs` mounts) | ⛔ OWED | `ui/tabs.tsx` is a shared cross-surface primitive — it crossed the G-5 threshold in `217-06`'s own commit. |
| **G4-9 / M-2** ⭐ | **Side by side with the approved sketch** — open `.planning/sketches/218-the-library-and-its-tabs/index.html` next to the running app | spacing, rhythm, hover or focus **visibly** diverges | ⛔ OWED | ⚠ **Driven BY LOOKING.** Sketch 218 §7 explicitly leaves pixel spacing to a human comparison; `drive.cjs`'s 200 green assertions do not cover it. |
| M-1 | A document **already mid-ingest** when the Library opens shows its stage | it shows nothing until the next transition | ⛔ OWED | Wants an integration test that mocks **NEITHER** side: a real `GET /documents` (backend TestClient, real `response_model` serialization) fed into the real `useDocuments` reconcile. ⚠ The **backend** half is load-bearing — it was the `response_model`, not the query, that stripped the field. |
| M-5 | The three empty arms are **honest** | *"No tables"* / *"No tables were extracted"* / *"This file type has no tables"* are three different claims | ⛔ OWED | `217-11-SUMMARY.md` tabulates all ten empty sentences verbatim, so this row can be driven without re-deriving them. |
| M-6 | The RLS asymmetry **as experienced** | a shared document returns text + chunks but **empty** tables/images and the screen is confusing | ⛔ OWED | Needs two accounts + a shared folder. A test asserts the empty result is *correct*; only a person can say whether it *reads* as broken. |
| **CHAT-SURFACE** | Open a document detail panel **from chat**, not from the Library | the five new sections regress the chat workspace panel | ⛔ OWED — **NEW** | ⚠ **Not in the original G-4 list; added at close.** `DocumentDetailPanel` is a **CROSS-SURFACE shell** reusing `WorkspacePanel`'s sheet, so this phase's changes land on **chat** — and chat UAT has not been run. |
| **STRIP-LABELS** | Look at the six-segment strip and say what each segment is | a wordless strip reads as **decorative** rather than informative | ⛔ OWED — **NEW** | ⚠ `217-08` recorded a real divergence from the drawn sketch: the segment labels are `title` / `aria-label` / `sr-only`, **not visible text**, because the sketch's short words would have been a forbidden seventh vocabulary. jsdom cannot judge this. |
| SC#10 4-axis | cross-provider × multi-tool × parallel-thread × long-message | — | **N/A — RULED, not skipped** | The ruling is recorded in `217-VALIDATION.md` § *The 4-axis UAT bandwidth rule — RULED NOT APPLICABLE*: no provider call, no model routing, nothing in the agent loop or the SSE path, and no new code reads `MODEL_CAPABILITIES`. ⛔ **Do not manufacture a provider roster here; do not quietly drop the ruling either.** |

---

## A cheap way to produce the `G4-10` failure — and a real defect found on the way

`217-07` measured a **pre-existing backend defect**, recorded here rather than fixed (it is outside
this phase's `files_modified` and outside the Library's scope):

> A `.csv` announced as `application/vnd.ms-excel` is **accepted**, routed to `openpyxl`, and dies on
> `BadZipFile` in the background. The upload returns **201** and the row later flips to `failed`.
> `.xls` is absent from `_EXT_MIME_OVERRIDES` **and** `application/vnd.ms-excel` is absent from
> `_UNRELIABLE_MIME_TYPES`. The one-line repair is written out in `217-07-SUMMARY.md`.

Also measured there: `documents.py:559`'s comment claims the mime set *"holds fifteen"*; extraction
counts **fourteen**.

⚠ This is a **real, reproducible failing ingest**, which makes it the cheapest way to drive `G4-10` —
but it is a defect in its own right and must not be closed by having been used as a test fixture.

---

## Recording a verdict

Replace the row's `⛔ OWED` with `PASS` or `FAIL` and write what was **observed** in the last column
— not what was expected. For `G4-4` specifically, the observation must state **whether the strip
showed a stage after the reload**; that sentence is the whole point of the row.

For `G4-9` / `M-2`, the observation must record that the comparison was made against
`.planning/sketches/218-the-library-and-its-tabs/index.html` **by looking**, and name any divergence.

⛔ A row that was attempted and blocked is `⛔` **with the blocker named** — never `PASS`, and never
deleted.
