---
phase: 217-the-library-one-home-for-documents
plan: 06
type: execute
wave: 2
depends_on: ["217-04"]
files_modified:
  - frontend/src/index.css
  - frontend/tailwind.config.js
  - frontend/src/components/ui/tabs.tsx
  - frontend/src/components/ui/__tests__/tabsContrast.test.ts
  - .planning/sketches/218-the-library-and-its-tabs/drive.cjs
  - .planning/sketches/218-the-library-and-its-tabs/BUILD-CONTRACT.generated.md
autonomous: true
requirements: [LIB-01]
decisions: [D-217-02, D-217-11, D-217-20, D-217-21]

must_haves:
  truths:
    - "The active tab is LIGHTER than its track in BOTH themes — on Deep Midnight it stops being a hole"
    - "The active tab carries a second, non-colour cue (an inset ring) so colour never carries alone"
    - "Nothing reflows when a tab becomes active — the ring is a box-shadow, not a border"
    - "Settings and Library Health inherit the fix without a Settings-local or Health-local branch being touched"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "the --tab-active token pair, :root and .dark"
      contains: "--tab-active"
    - path: "frontend/tailwind.config.js"
      provides: "the tab-active colour mapping beside the panel-* family"
      contains: "tab-active"
    - path: "frontend/src/components/ui/tabs.tsx"
      provides: "the corrected TabsTrigger active state"
      contains: "data-[state=active]:bg-tab-active"
    - path: "frontend/src/components/ui/__tests__/tabsContrast.test.ts"
      provides: "a token fence asserting L(active) > L(track) in BOTH blocks, with a non-vacuity control per parse"
  key_links:
    - from: "frontend/src/components/ui/tabs.tsx"
      to: "frontend/src/index.css"
      via: "tailwind.config.js maps --tab-active into the bg-tab-active utility"
      pattern: "tab-active"
---

<objective>
D-217-20/21: fix the SHARED tabs primitive so the selected tab reads as a raised chip instead of a
hole, in both themes, with two independent cues.

Purpose: this is the finding a Stitch pass structurally could not produce. Measured verbatim:
`data-[state=active]:bg-background` over `bg-muted` is **97% over 94% (+3, lighter) in light** and
**4% over 11% (−7, DARKER) in Deep Midnight** — the bar inverts. Its only other cue is a 5%-opacity
shadow, invisible at 4% lightness. Every reference image drew the active tab lighter.

Output: a new theme-paired token (no existing token is lighter-than-muted in BOTH themes — that was
measured, so a swap cannot fix this), the primitive updated, a token fence, and the sketch's `A3b`
assertion re-pinned in the same commit.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/217-the-library-one-home-for-documents/217-CONTEXT.md
@.planning/phases/217-the-library-one-home-for-documents/217-PATTERNS.md
@.planning/sketches/218-the-library-and-its-tabs/BUILD-CONTRACT.generated.md

<interfaces>
Shipped today — `frontend/src/components/ui/tabs.tsx` (53 lines, a thin Radix wrapper, exports
`{ Tabs, TabsList, TabsTrigger, TabsContent }` at `:53`, **no variant prop**):

```
// TabsList — :15
"inline-flex h-10 items-center justify-center rounded-md bg-muted p-1 text-muted-foreground"

// TabsTrigger — :30
"… rounded-sm px-3 py-1.5 text-sm font-medium ring-offset-background transition-colors
 focus-visible:… data-[state=active]:bg-background data-[state=active]:text-foreground
 data-[state=active]:shadow-sm"
```

Measured token lightness (`frontend/src/index.css`, `:root` from `:17`, `.dark` from `:104`):

| Token | light | dark |
|---|---|---|
| `--background` | 97% | 4% |
| `--muted` | 94% | 11% |
| `--card` | 100% | 7% |
| `--accent` | 92% | 14% |
| `--border` | 89% | 16% |

⚠ **No existing token is lighter than `--muted` in BOTH themes** — `card` fails dark, `accent` fails
light, `background` fails dark (today's bug). The fix requires a NEW token, not a swap.

Recommended values, following the repo's own `--panel-*` / `--accent-violet` precedent (a `:root`
value + a `.dark` value mapped in `tailwind.config.js` at `:47-64`):

```css
/* :root  — light */        --tab-active: 0 0% 100%;      /* L 100% > muted 94% : +6 */
/* .dark  — deep midnight */ --tab-active: 220 25% 16%;   /* L 16% > muted 11% : +5 */
```

⚠ The sketch's remedy uses `box-shadow: inset`, NOT `border` (`drive.cjs` `A4c` asserts
`\.tabslist\.remedy[^}]*box-shadow:\s*inset`) — a real border changes the box size unless the
inactive state carries a transparent one, and nothing should reflow on tab change.

⚠ **The two other mounts are `frontend/src/pages/KnowledgeHealthPage.tsx` and
`frontend/src/pages/SettingsPage.tsx`** — measured, `grep -rln 'from "@/components/ui/tabs"'`
returns exactly those two non-test files; the Library is the third.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The --tab-active token pair, mapped into Tailwind</name>
  <files>frontend/src/index.css, frontend/tailwind.config.js</files>
  <read_first>
    - `frontend/src/index.css` `:root` block from `:17` (locate `--background`, `--muted`, `--card`, `--accent`, `--border`) and the `.dark` block from `:104` (the same five)
    - `frontend/tailwind.config.js:20-95` — the `panel-muted-foreground` / `panel-status-done` / `accent-violet` entries are the exact mapping shape to copy
    - `.planning/phases/217-the-library-one-home-for-documents/217-RESEARCH.md` § *D-217-20/21 — the tabs primitive, measured in tokens*
  </read_first>
  <action>
    Add `--tab-active` to BOTH theme blocks in `frontend/src/index.css`, beside the existing
    surface tokens, with a comment recording the measured lightness delta against `--muted` in that
    theme (`+6` light, `+5` dark at the recommended values). Start from the recommended values above.

    ⚠ **Assumption A6 is not settled and must be measured, not assumed:** the active trigger renders
    `data-[state=active]:text-foreground` on top of this token. Compute the WCAG contrast ratio of
    `--foreground` over `--tab-active` in BOTH themes before committing the values. If either falls
    below **4.5:1**, adjust the token's lightness until it clears — keeping `L(tab-active) >
    L(muted)` in that theme, which is the non-negotiable half. Record both measured ratios and the
    final values in the summary.

    Map it in `frontend/tailwind.config.js` beside the `panel-*` entries as
    `"tab-active": "hsl(var(--tab-active))"`, so `bg-tab-active` resolves.

    ⛔ Do not modify `--background`, `--muted`, `--card`, `--accent` or `--border`. Sketch fences
    `A4` and `A4b` measure `--muted` against `--background` and must stay TRUE — they document the
    shipped inversion, and re-pointing them at the new token would delete the finding.
  </action>
  <acceptance_criteria>
    - `grep -c -- "--tab-active" frontend/src/index.css` returns exactly `2` (one per theme block)
    - `grep -n "tab-active" frontend/tailwind.config.js` returns one mapping line
    - Parsing `--tab-active` and `--muted` from the `:root` block gives `L(tab-active) > L(muted)`; the same holds in the `.dark` block
    - The measured contrast ratio of `--foreground` over `--tab-active` is `>= 4.5` in BOTH themes, and both numbers appear in the summary
    - `git diff frontend/src/index.css` shows no change to the `--background`, `--muted`, `--card`, `--accent` or `--border` declarations
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/ui/__tests__/tabsContrast.test.ts --maxWorkers=2</automated>
  </verify>
  <done>One theme-paired token exists, is mapped, is lighter than its track in both themes, and its text contrast was measured rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 2: The primitive — lighter chip plus an inset ring, and nothing reflows</name>
  <files>frontend/src/components/ui/tabs.tsx</files>
  <read_first>
    - `frontend/src/components/ui/tabs.tsx` (all 53 lines)
    - `frontend/src/pages/KnowledgeHealthPage.tsx` — its four `TabsTrigger`s, pinned by sketch fence `A9`; this file is BOTH the regression target and the tab-shell analog plan 09 copies
    - `docs/HOT-FILE-LEDGER.md` § `frontend/src/pages/SettingsPage.tsx` (`38 / 21 / 1500`, G-5 FIRES) — read it before making a change that lands on that page
  </read_first>
  <action>
    In `TabsTrigger`'s class string (`:30`), replace `data-[state=active]:bg-background` with
    `data-[state=active]:bg-tab-active`, keep `data-[state=active]:text-foreground`, keep
    `data-[state=active]:shadow-sm` (it still helps in light), and add the SECOND, independent cue as
    an inset ring: a `data-[state=active]:` shadow utility that produces `box-shadow: inset 0 0 0 1px`
    in the border colour, layered with the existing `shadow-sm`. ⛔ Do **not** add a `border`
    utility — it changes the box size and the tab row reflows on every switch.

    ⛔ Do not add a `variant` prop and do not create a Library-local tab component (D-217-20 rejects
    both: a `variant` makes the WRONG rendering the default, and two tab bars that disagree on one
    theme is how a design system stops being one).

    ⚠ **State in the summary, as D-217-01's second obligation requires:** this is a SHARED-PRIMITIVE
    edit, not a Settings edit and not a Library Health edit. `SettingsPage.tsx` (`38 / 21 / 1500`)
    and `KnowledgeHealthPage.tsx` (`11 / 5 / 571`) both FIRE G-5 and both inherit this change, and
    **neither is in this plan's `files_modified` because no Settings-local or Health-local branch is
    touched** — they receive a different rendering of a primitive they already mount. Re-derive both
    triples from git and record them; do not copy the ledger cells. Also state the gate side:
    `frontend/src/components/ui/` has **no suite for tabs at all**, so `tabsContrast.test.ts` is the
    first, and plan 12 adopts it into both knobs.
  </action>
  <acceptance_criteria>
    - `grep -n "data-\[state=active\]:bg-tab-active" frontend/src/components/ui/tabs.tsx` returns one line
    - `grep -n "data-\[state=active\]:bg-background" frontend/src/components/ui/tabs.tsx` returns nothing
    - `grep -n "data-\[state=active\]:border" frontend/src/components/ui/tabs.tsx` returns nothing — the second cue is a shadow, not a border
    - `grep -n "variant" frontend/src/components/ui/tabs.tsx` returns nothing
    - The file still exports exactly `{ Tabs, TabsList, TabsTrigger, TabsContent }`
    - `npx tsc --noEmit -p tsconfig.app.json` -> no new errors
    - Both inheriting pages' triples are re-derived from git and recorded in the summary
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/ui/__tests__/tabsContrast.test.ts --maxWorkers=2 && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <done>The active trigger uses the new token plus an inset ring, no variant prop was added, and the two inheriting pages are named with re-derived triples.</done>
</task>

<task type="auto">
  <name>Task 3: The token fence, and the sketch's A3b assertion re-pinned in the same commit</name>
  <files>frontend/src/components/ui/__tests__/tabsContrast.test.ts, .planning/sketches/218-the-library-and-its-tabs/drive.cjs, .planning/sketches/218-the-library-and-its-tabs/BUILD-CONTRACT.generated.md</files>
  <read_first>
    - `.planning/sketches/218-the-library-and-its-tabs/drive.cjs:118-149` — `A3b`, and `lightness()` + `A4`/`A4b`, which is the parser to PORT into vitest (there is no in-directory sibling suite to copy)
    - `frontend/src/components/files/FileRow.sweep.test.ts:32-41` — the three-way fence guard (length, identity, stripper non-vacuity)
    - `frontend/src/components/workflows/argumentModel.test.ts:24-80` — the `?raw` + CRLF discipline
  </read_first>
  <action>
    Create `frontend/src/components/ui/__tests__/tabsContrast.test.ts`. Import
    `frontend/src/index.css` with `?raw` and port `drive.cjs`'s `lightness(css, token, blockStart)`
    parser. Assert, per theme block:
    (a) `L(--tab-active) > L(--muted)` — the criterion, in both `:root` and `.dark`;
    (b) a NON-VACUITY CONTROL per parse: each parsed lightness is a finite number, not `null`.
    A regex that matched nothing yields `null`, and a comparison against `null` must not be allowed
    to read as a pass.
    (c) a `?raw` import of `tabs.tsx` asserting the active trigger references `bg-tab-active` and no
    longer references `bg-background`, plus a length + identity guard on that import.
    (d) the computed `--foreground`-over-`--tab-active` contrast ratio is `>= 4.5` in both themes
    (A6, measured in the suite rather than in a comment).
    CRLF-tolerant patterns throughout (`\r?\n`, `[\s\S]`).

    In `drive.cjs`, re-pin `A3b` (`:121-123`) — it asserts the SHIPPED trigger contains
    `data-[state=active]:bg-background`, and this plan makes that false BY DESIGN. Rewrite it as a
    rename map on the `A9c` precedent: the old class is gone, the new token class is present, and
    the sketch still records what was replaced so a RENAME cannot read as a LOSS. ⛔ Keep `A4` and
    `A4b` exactly as they are — they measure `--muted` against `--background` and remain TRUE; they
    are the recorded finding, not a target.

    Then run `node .planning/sketches/218-the-library-and-its-tabs/drive.cjs --emit` from the repo
    root and commit the regenerated contract in the SAME commit.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/ui/__tests__/tabsContrast.test.ts --maxWorkers=2` passes with at least 8 assertions
    - The suite fails when `--tab-active`'s dark value is temporarily set below `--muted` — state that this RED was observed
    - Every `lightness()` call site has a paired non-vacuity assertion
    - `node .planning/sketches/218-the-library-and-its-tabs/drive.cjs` prints `0 failed`
    - `git diff .planning/sketches/218-the-library-and-its-tabs/drive.cjs` shows a hunk at `A3b` and NO hunk at `A4`, `A4b`, `A5*` or `A7*`
    - `BUILD-CONTRACT.generated.md` is regenerated in this commit
  </acceptance_criteria>
  <verify>
    <automated>node .planning/sketches/218-the-library-and-its-tabs/drive.cjs && cd frontend && npx vitest run src/components/ui/__tests__/tabsContrast.test.ts --maxWorkers=2</automated>
  </verify>
  <done>The contrast criterion is asserted from the token source in both themes with non-vacuity controls, and the sketch fence that this change fires by design was re-pinned and re-emitted in the same commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | A CSS token and a class string. No input crosses a boundary; the risk is legibility and collateral regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-217-20 | Denial of service (usability) | the two inheriting mounts, `SettingsPage.tsx` and `KnowledgeHealthPage.tsx` | mitigate | The change is confined to the shared primitive; no page-local branch is edited. The regression is covered by G-4 row G4-8 (open Settings and Library Health after the tabs change), which jsdom cannot stand in for — every `getBoundingClientRect` is zero |
| T-217-21 | Denial of service (accessibility) | active-tab text contrast | mitigate | The `--foreground`-over-`--tab-active` ratio is computed IN the suite and asserted `>= 4.5` in both themes; assumption A6 is closed by measurement rather than by the recommended values being trusted |
| T-217-22 | Tampering | colour as the only cue | mitigate | A second, non-colour cue (an inset ring) is required by the class string and asserted by the `?raw` fence — colour never carries alone |
| T-217-SC | Tampering | npm/pip/cargo installs | mitigate | No package installed by this plan |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/ui/__tests__/tabsContrast.test.ts --maxWorkers=2` -> passes
- `node .planning/sketches/218-the-library-and-its-tabs/drive.cjs` -> `0 failed`
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` -> no new errors
- ⚠ G-4 rows **G4-7** (toggle Deep Midnight ↔ light with the tab bar visible) and **G4-8** (open Settings and Library Health afterwards) and manual row **M-3** (the chip reads as raised, not merely different) stay OWED — a token fence proves lightness, never perception.
</verification>

<success_criteria>
- The active tab is lighter than its track in both themes and carries an inset ring
- No reflow on tab change; no variant prop; no Library-local tab component
- The sketch fence this change fires by design was rewritten as a rename map, not deleted
</success_criteria>

<output>
Create `.planning/phases/217-the-library-one-home-for-documents/217-06-SUMMARY.md` when done.
Record: the final token values, both measured contrast ratios, the re-derived `SettingsPage.tsx` and
`KnowledgeHealthPage.tsx` triples, and the drive's assertion count.
</output>
