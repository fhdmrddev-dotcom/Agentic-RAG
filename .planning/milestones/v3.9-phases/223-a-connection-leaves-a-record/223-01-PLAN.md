---
phase: 223-a-connection-leaves-a-record
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/152_audit_log_connector_action_types_and_message_active_connectors.sql
  - backend/app/services/audit_service.py
  - backend/tests/test_110_boot_guard.py
  - backend/app/models/message.py
autonomous: true
requirements:
  - BUG-260902-05
  - BUG-260902-03
user_setup: []

must_haves:
  truths:
    - "VALID_ACTION_TYPES in audit_service.py contains 'connector.call' and 'connector.grant'"
    - "test_110_boot_guard.py asserts _ALL_21 matching VALID_ACTION_TYPES and passes"
    - "Migration 152 alters audit_log_action_type_check to include both action types and adds active_connector_ids jsonb to public.messages"
    - "MessageResponse in models/message.py exposes active_connector_ids: list[UUID] | None = None"
  artifacts:
    - supabase/migrations/152_audit_log_connector_action_types_and_message_active_connectors.sql
    - backend/app/services/audit_service.py
    - backend/tests/test_110_boot_guard.py
    - backend/app/models/message.py
  key_links:
    - "assert_action_types_synced checks live DB check constraint against VALID_ACTION_TYPES frozenset"
---

<objective>
Deliver database schema and code synchronization in lockstep for Phase 223:
1. Author migration 152 to add 'connector.call' and 'connector.grant' to the audit_log CHECK constraint, and add active_connector_ids jsonb to public.messages.
2. Register 'connector.call' and 'connector.grant' in VALID_ACTION_TYPES in audit_service.py.
3. Update _ALL_19 to _ALL_21 in backend/tests/test_110_boot_guard.py in the same commit to prevent boot failures.
4. Update MessageResponse model to expose active_connector_ids.
5. Apply migration 152 to local database on port 54322.

Purpose: Prevents silent swallows in write_audit_entry (D-223-01/02) and prepares durable storage for armed connectors (D-223-06).
Output: Migration 152 SQL file, updated audit_service.py, updated test_110_boot_guard.py, updated message.py.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/223-a-connection-leaves-a-record/223-CONTEXT.md
@.planning/phases/223-a-connection-leaves-a-record/223-PREFLIGHT.md
@backend/app/services/audit_service.py
@backend/tests/test_110_boot_guard.py
@backend/app/models/message.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Migration 152 and apply to local database</name>
  <files>supabase/migrations/152_audit_log_connector_action_types_and_message_active_connectors.sql</files>
  <read_first>supabase/migrations/151_connector_tokens_rls_policy.sql, backend/app/services/audit_service.py</read_first>
  <action>
    Create migration 152: `supabase/migrations/152_audit_log_connector_action_types_and_message_active_connectors.sql`:
    1. Update CHECK constraint `audit_log_action_type_check` on `public.audit_log` to include:
       - `'connector.call'`
       - `'connector.grant'`
       Retain all existing 19 action types in the constraint:
       `'auth.login'`, `'auth.logout'`, `'auth.signup'`, `'auth.password_reset'`, `'auth.session_revoked'`,
       `'rbac.role_assigned'`, `'rbac.role_revoked'`, `'rbac.permission_granted'`, `'rbac.permission_revoked'`,
       `'data.export'`, `'data.delete'`, `'admin.access'`, `'system.config_change'`, `'api.key_created'`,
       `'api.key_revoked'`, `'integration.enabled'`, `'integration.disabled'`, `'security.alert'`,
       `'governance.rule_triggered'`, `'connector.call'`, `'connector.grant'`.
    2. Add column `active_connector_ids` to `public.messages`:
       `ALTER TABLE public.messages ADD COLUMN IF NOT EXISTS active_connector_ids jsonb DEFAULT NULL;`
       Add column comment explaining: "Phase 223 (BUG-260902-03 / D-223-06): Array of armed connector UUIDs active when message was sent. NULL means absent/legacy; '[]'::jsonb means explicitly cleared/none."
    3. Apply migration 152 to the local Postgres database on port 54322 via asyncpg/psycopg2 script.
  </action>
  <verify>
    Run a python command against localhost:54322 to verify:
    - `audit_log_action_type_check` contains 'connector.call' and 'connector.grant'.
    - `public.messages` has column `active_connector_ids` with type `jsonb`.
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/152_audit_log_connector_action_types_and_message_active_connectors.sql` exists and contains `audit_log_action_type_check` and `ADD COLUMN IF NOT EXISTS active_connector_ids jsonb`.
    - Local DB at :54322 reflects the new column and altered check constraint.
  </acceptance_criteria>
  <done>Migration 152 authored and applied to local DB.</done>
</task>

<task type="auto">
  <name>Task 2: Update VALID_ACTION_TYPES, boot guard test, and MessageResponse model</name>
  <files>backend/app/services/audit_service.py, backend/tests/test_110_boot_guard.py, backend/app/models/message.py</files>
  <read_first>backend/app/services/audit_service.py, backend/tests/test_110_boot_guard.py, backend/app/models/message.py</read_first>
  <action>
    1. In `backend/app/services/audit_service.py`:
       Add `"connector.call"` and `"connector.grant"` to `VALID_ACTION_TYPES` frozenset (lines 13-26).
    2. In `backend/tests/test_110_boot_guard.py`:
       Update `_ALL_19` to `_ALL_21` (or add the two new types to the expected set) including `"connector.call"` and `"connector.grant"`.
       Update the assertion to verify that `len(VALID_ACTION_TYPES) == 21` and all 21 types match.
    3. In `backend/app/models/message.py`:
       In `MessageResponse`:
       Add `active_connector_ids: list[UUID] | None = None` to the fields, so `GET /threads/{id}/messages` serializes the stored column.
  </action>
  <verify>
    Run `pytest backend/tests/test_110_boot_guard.py -v`.
  </verify>
  <acceptance_criteria>
    - `backend/app/services/audit_service.py` contains `"connector.call"` and `"connector.grant"` in `VALID_ACTION_TYPES`.
    - `backend/tests/test_110_boot_guard.py` passes 100% with 21 action types.
    - `backend/app/models/message.py` has `active_connector_ids: list[UUID] | None = None` on `MessageResponse`.
  </acceptance_criteria>
  <done>Lockstep code updates complete and boot guard test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit_service → Postgres CHECK constraint | Action types must be valid per CHECK constraint or DB raises 23514 check_violation |
| API client → MessageResponse serialization | Deserializing active_connector_ids must preserve list[UUID] or None without leaking internal state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-223-01 | Tampering | audit_log_action_type_check | mitigate | Update check constraint in lockstep with VALID_ACTION_TYPES so write_audit_entry never encounters silent 23514 errors. |
| T-223-02 | Denial of Service | assert_action_types_synced | mitigate | Ensure test_110_boot_guard.py passes on literal list and live DB check passes on startup. |
| T-223-03 | Information Disclosure | messages.active_connector_ids | mitigate | Column holds only UUID arrays of connector IDs, never credentials, tokens, or plaintext secrets. |
| T-223-SC | Tampering | package dependencies | mitigate | Zero external dependencies installed. Standard PostgreSQL and Pydantic primitives only. |
</threat_model>

<verification>
- `pytest backend/tests/test_110_boot_guard.py -v` passes with 21 action types.
- Migration 152 file is well-formed SQL matching established migration conventions.
</verification>
