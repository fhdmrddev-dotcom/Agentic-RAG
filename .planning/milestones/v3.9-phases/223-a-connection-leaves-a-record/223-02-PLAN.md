---
phase: 223-a-connection-leaves-a-record
plan: 02
type: execute
wave: 2
depends_on:
  - 223-01
files_modified:
  - backend/app/services/tool_dispatcher.py
  - backend/tests/test_223_dispatcher_audit.py
autonomous: true
requirements:
  - BUG-260902-05
  - BUG-260902-04
user_setup: []

must_haves:
  truths:
    - "_handle_connector_chat_tool audits all five evaluated outcomes (policy_denial, user_rejected, timeout, execution_failure, success) via write_audit_entry"
    - "audit_log metadata contains arg_keys only and NEVER stores raw args values (G-6 / SEED-223)"
    - "Timeout payload states factually that nobody answered in time and instructs LLM not to advise checking workspace panels or re-authenticating (BUG-260902-04 copy half)"
    - "Precondition exits before connection resolution (4337, 4351, 4372, 4388) do not crash; 4337 logs warning"
  artifacts:
    - backend/app/services/tool_dispatcher.py
    - backend/tests/test_223_dispatcher_audit.py
  key_links:
    - "_handle_connector_chat_tool spawns write_audit_entry with action_type='connector.call' and explicit org_id"
---

<objective>
Instrument `_handle_connector_chat_tool` in `backend/app/services/tool_dispatcher.py` to audit all outbound connector tool invocations and provide truthful recovery copy:
1. Audit all 5 evaluated action outcomes (`policy_denial`, `user_rejected`, `timeout`, `execution_failure`, `success`).
2. Enforce argument privacy by logging only `arg_keys: list[str]`, never argument payloads.
3. Thread actor `user_id` and `org_id` into `write_audit_entry`.
4. Update timeout payload to factually state nobody answered in time without hallucinating workspace panels or asserting unverified connection health.
5. Create comprehensive unit tests in `backend/tests/test_223_dispatcher_audit.py`.

Purpose: Satisfies GRANT-05, SC#1, SC#3, and addresses BUG-260902-05 and BUG-260902-04 (copy half).
Output: Updated tool_dispatcher.py and new test_223_dispatcher_audit.py.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/223-a-connection-leaves-a-record/223-CONTEXT.md
@.planning/phases/223-a-connection-leaves-a-record/223-PREFLIGHT.md
@backend/app/services/tool_dispatcher.py
@backend/app/services/audit_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instrument _handle_connector_chat_tool with audit logging and truthful copy</name>
  <files>backend/app/services/tool_dispatcher.py</files>
  <read_first>backend/app/services/tool_dispatcher.py:4320-4610, backend/app/services/audit_service.py:55-88</read_first>
  <action>
    In `backend/app/services/tool_dispatcher.py` inside `_handle_connector_chat_tool`:
    1. Extract argument keys safely:
       `arg_keys = list(args.keys()) if isinstance(args, dict) else []`
    2. Helper for spawning connector audit entry:
       Define an internal helper or direct `ctx.spawn(write_audit_entry(...))` call:
       `action_type = "connector.call"`
       `metadata = { "connection_id": str(matched_conn.id), "service_id": service_id, "service_name": matched_conn.name, "tool_name": action_tool_name, "outcome": outcome, "arg_keys": arg_keys, "failure_reason": failure_reason }`
       `user_id = ctx.current_user["id"]`
       `org_id = str(matched_conn.org_id)`
       Invoke `write_audit_entry(user_id=user_id, action_type="connector.call", metadata=metadata, supabase=ctx.supabase, org_id=org_id)`.
    3. Audit the 5 evaluated exit points:
       - **Exit 1 (line 4401, posture == "deny")**:
         Record `outcome="policy_denial"`, `failure_reason="Denied by tool posture policy"`.
       - **Exit 2 (line 4445, user decision != "allow")**:
         Record `outcome="user_rejected"`, `failure_reason="User rejected execution"`.
         Update user rejection return message (line 4446):
         `"User rejected execution of tool '{action_tool_name}' on {matched_conn.name}. Do not retry this action unless explicitly requested by the user."`
       - **Exit 3 (line 4452, asyncio.TimeoutError)**:
         Record `outcome="timeout"`, `failure_reason="Approval request timed out after 120s"`.
         Update timeout return message (line 4455):
         `"Tool execution '{action_tool_name}' on {matched_conn.name} timed out waiting for human approval in the chat. Nobody answered in time. Do not advise the user to check a workspace panel or re-authenticate; if the user still wants this action, ask them in this chat if they would like to try again."`
       - **Exit 4 (line 4585, failure is not None)**:
         Record `outcome="execution_failure"`, `failure_reason=failure`.
       - **Exit 5 (line 4605, execution success)**:
         Record `outcome="success"`, `failure_reason=None`.
    4. Guard precondition exits:
       - At line 4337 (`not ctx.current_user or "id" not in ctx.current_user`):
         Log warning: `"Cannot audit connector call: user_id is missing (audit_log.user_id is NOT NULL)"`.
       - Precondition exits 4351 (`scope.ok` False), 4372 (`list_connections` error), 4388 (`matched_conn` not found) remain un-audited as connector calls since no connection action was evaluated.
  </action>
  <verify>
    Verify with grep:
    - `grep -n "outcome=" backend/app/services/tool_dispatcher.py` shows all 5 outcomes instrumented.
    - `grep -n "Nobody answered in time" backend/app/services/tool_dispatcher.py` finds the truthful timeout copy.
    - Confirm NO instance of `"The connection remains connected and healthy"` exists.
  </verify>
  <acceptance_criteria>
    - `_handle_connector_chat_tool` writes audit records on lines corresponding to policy denial, user rejection, timeout, failure, and success.
    - Raw `args` is never present in `metadata`; only `arg_keys` is included.
    - Timeout copy matches D-223-08 verbatim.
  </acceptance_criteria>
  <done>tool_dispatcher.py instrumented with 5 audit outcomes and truthful recovery text.</done>
</task>

<task type="auto">
  <name>Task 2: Author unit tests in test_223_dispatcher_audit.py</name>
  <files>backend/tests/test_223_dispatcher_audit.py</files>
  <read_first>backend/app/services/tool_dispatcher.py:4320-4610</read_first>
  <action>
    Create `backend/tests/test_223_dispatcher_audit.py`:
    1. Test `policy_denial`: Posture "deny" writes `connector.call` with `outcome="policy_denial"` and `arg_keys`.
    2. Test `user_rejected`: Tool approval decision "deny" writes `outcome="user_rejected"` and returns expected rejection copy.
    3. Test `timeout`: `asyncio.TimeoutError` during tool approval writes `outcome="timeout"` and returns expected timeout copy ("Nobody answered in time").
    4. Test `execution_failure`: Service tool returning error writes `outcome="execution_failure"`.
    5. Test `success`: Normal execution writes `outcome="success"`.
    6. Test argument privacy: Ensure `metadata` has `arg_keys` and does not contain sensitive values from `args`.
  </action>
  <verify>
    Run `pytest backend/tests/test_223_dispatcher_audit.py -v`.
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_223_dispatcher_audit.py` exists and passes 100%.
    - All 5 outcomes and privacy constraints are pinned by automated tests.
  </acceptance_criteria>
  <done>Dispatcher audit and recovery unit tests authored and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool args → audit_log.metadata | Tool arguments may carry sensitive PII, passwords, emails, query bodies |
| tool_dispatcher → audit_service | Audit writes must not crash or block tool execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-223-02-01 | Information Disclosure | audit_log.metadata | mitigate | Only log `arg_keys: list[str]`. Never store raw argument values in audit logs (G-6 / SEED-223). |
| T-223-02-02 | Repudiation | connector tool execution | mitigate | Audit all 5 outcomes (including denials, rejections, timeouts, and failures) so unexecuted actions are recorded. |
| T-223-02-03 | Elevation of Privilege | timeout recovery advice | mitigate | Truthful timeout copy prevents LLM from inventing fake panels or instructing users to re-authorize connections. |
| T-223-SC | Tampering | package dependencies | mitigate | No new packages or dependencies. |
</threat_model>

<verification>
- `pytest backend/tests/test_223_dispatcher_audit.py -v` passes 100%.
- `pytest backend/tests/test_110_boot_guard.py -v` passes 100%.
</verification>
