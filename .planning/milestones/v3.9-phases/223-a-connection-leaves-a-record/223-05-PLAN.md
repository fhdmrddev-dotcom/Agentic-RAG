---
phase: 223-a-connection-leaves-a-record
plan: 05
type: execute
wave: 3
depends_on:
  - 223-01
  - 223-02
  - 223-03
  - 223-04
files_modified:
  - backend/tests/integration/test_223_audit_live.py
autonomous: true
requirements:
  - BUG-260902-05
  - BUG-260902-03
user_setup: []

must_haves:
  truths:
    - "Integration test test_223_audit_live.py runs against live PostgreSQL on port 54322 and DOES NOT skip"
    - "Test proves rows land in public.audit_log for connector.call (all 5 outcomes) and connector.grant (both chat_card and settings sources)"
    - "Test proves public.messages stores [] as '[]'::jsonb and None as SQL NULL"
    - "All gates remain clean: tsc <= 66 baseline, vitest count gate OK 188/188 files 0 failing, backend rot <= 70"
  artifacts:
    - backend/tests/integration/test_223_audit_live.py
  key_links:
    - "test_223_audit_live.py connects directly to live PostgreSQL on :54322 to assert persisted rows"
---

<objective>
Deliver automated live integration verification proving ledger writes and database persistence cannot fail silently:
1. Author `backend/tests/integration/test_223_audit_live.py` patterned on `test_116_audit_live.py`.
2. Connect directly to live PostgreSQL on port 54322 to verify:
   - `connector.call` rows physically land in `public.audit_log` across all 5 outcomes (`policy_denial`, `user_rejected`, `timeout`, `execution_failure`, `success`).
   - `connector.grant` rows physically land in `public.audit_log` from both `chat_card` and `settings` sources.
   - `metadata` stores `arg_keys: list[str]` and never contains raw argument values (G-6).
   - `messages.active_connector_ids` persists `[]` as `'[]'::jsonb` and absent as SQL `NULL`.
3. Execute the test and prove it runs and passes (does NOT skip).
4. Run full gate suite across frontend and backend.

Purpose: Satisfies SC#4 and SC#5, guaranteeing that swallowed exceptions cannot masquerade as working code.
Output: New test_223_audit_live.py, verified live DB assertions, and complete gate verification.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/223-a-connection-leaves-a-record/223-CONTEXT.md
@.planning/phases/223-a-connection-leaves-a-record/223-PREFLIGHT.md
@backend/tests/integration/test_116_audit_live.py
@backend/app/services/audit_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author live database integration test test_223_audit_live.py</name>
  <files>backend/tests/integration/test_223_audit_live.py</files>
  <read_first>backend/tests/integration/test_116_audit_live.py</read_first>
  <action>
    Create `backend/tests/integration/test_223_audit_live.py`:
    1. Connect directly to Postgres `:54322` (`postgres:postgres@localhost:54322/postgres`).
       If port 54322 is not listening, raise a descriptive failure explaining that the live DB must be running for SC#4.
    2. Test 1 (`test_connector_call_audit_live_persisted`):
       - Write audit entries for each of the 5 outcomes (`policy_denial`, `user_rejected`, `timeout`, `execution_failure`, `success`) using `write_audit_entry`.
       - Query `public.audit_log` via raw SQL with `WHERE action_type = 'connector.call'` and assert:
         - Exactly 5 rows are found for the test run.
         - `user_id` and `org_id` are populated.
         - `metadata->'outcome'` matches the 5 outcomes.
         - `metadata->'arg_keys'` is a JSON array.
         - No raw arguments or secret values appear anywhere in the row.
    3. Test 2 (`test_connector_grant_audit_live_persisted`):
       - Write audit entries with `action_type = "connector.grant"` for both `source = "chat_card"` and `source = "settings"`.
       - Query `public.audit_log` via raw SQL with `WHERE action_type = 'connector.grant'` and assert:
         - Rows land and contain correct `source` and `connection_id`.
    4. Test 3 (`test_message_active_connector_ids_persistence`):
       - Insert a test thread and message with `active_connector_ids = []`.
       - Insert another message with `active_connector_ids = NULL`.
       - Query `public.messages` and assert:
         - Row 1: `active_connector_ids = '[]'::jsonb` (NOT NULL, empty JSON array).
         - Row 2: `active_connector_ids IS NULL`.
  </action>
  <verify>
    Run `pytest backend/tests/integration/test_223_audit_live.py -v -s`.
    Inspect output to confirm tests run against live Postgres and PASS with zero skips.
  </verify>
  <acceptance_criteria>
    - `backend/tests/integration/test_223_audit_live.py` exists.
    - Test executes without skipping and asserts rows in `public.audit_log` and `public.messages`.
  </acceptance_criteria>
  <done>Live integration test authored and executed against live DB.</done>
</task>

<task type="auto">
  <name>Task 2: Run all gate commands to verify zero regressions</name>
  <files>backend/tests/integration/test_223_audit_live.py</files>
  <read_first>scripts/vitest-count-gate.cjs</read_first>
  <action>
    Run all project gate checks:
    1. `pytest backend/tests/test_110_boot_guard.py -v`
    2. `pytest backend/tests/test_223_dispatcher_audit.py -v`
    3. `pytest backend/tests/test_223_grant_audit.py -v`
    4. `pytest backend/tests/integration/test_223_audit_live.py -v`
    5. `npx tsc -p tsconfig.app.json` (must be <= 66 errors)
    6. `node scripts/vitest-count-gate.cjs` (must be 188/188 files, 0 failures)
  </action>
  <verify>
    Confirm all commands exit with code 0 (or within established rot baselines).
  </verify>
  <acceptance_criteria>
    - All unit and integration tests pass.
    - TypeScript compilation introduces 0 new errors.
    - Vitest count gate passes 100%.
  </acceptance_criteria>
  <done>All gates green and verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → live database | Integration test performs real database reads/writes; must clean up or scope test data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-223-05-01 | Repudiation | test_223_audit_live.py | mitigate | Un-mocked direct database queries prove rows physically land in `public.audit_log` and are not dropped by silent exceptions (SC#4, SC#5). |
| T-223-05-02 | Tampering | test data isolation | mitigate | Use unique test UUIDs and cleanup fixtures to ensure integration tests do not collide with production or other test data. |
| T-223-SC | Tampering | package dependencies | mitigate | Zero new dependencies installed. |
</threat_model>

<verification>
- `pytest backend/tests/integration/test_223_audit_live.py -v -s` passes without skipping.
- `node scripts/vitest-count-gate.cjs` passes 188/188 files.
- `npx tsc -p tsconfig.app.json` <= 66 baseline errors.
</verification>
