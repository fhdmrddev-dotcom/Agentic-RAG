---
phase: 210-ground-truth-operability-and-failure-honesty
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/api/_core.ts
  - frontend/src/components/admin/FeatureVisibility.tsx
  - frontend/src/components/admin/ControlRoomPage.tsx
  - frontend/src/components/admin/__tests__/FeatureVisibility.a11y.test.tsx
  - frontend/src/components/admin/revertByteIdentical.test.tsx
autonomous: true
requirements: [CONN-09]
tags: [CONN-09, frontend, admin, control-room, live-connectors, governed-features, type-safety]
must_haves:
  truths:
    - "An operator opens the Control Room, sees the `live_connectors` kill-switch with its current state, and flips it between On and Off (Everyone vs Off)"
    - "The client-side GovernedFeature type union contains 'live_connectors', resolving D-190-DEF-09"
    - "All exhaustive Record<GovernedFeature, ...> maps across frontend typecheck cleanly at baseline 34 errors"
    - "connectionsCopy.ts remains in Phase 211's fence and works seamlessly via its existing interim cast"
  artifacts:
    - path: frontend/src/lib/api/_core.ts
      provides: "Widened GovernedFeature union including 'live_connectors'"
    - path: frontend/src/components/admin/FeatureVisibility.tsx
      provides: "FeatureDef entry for live_connectors with On/Off binary toggle"
    - path: frontend/src/components/admin/ControlRoomPage.tsx
      provides: "DEFAULT_VISIBILITY and state maps updated with live_connectors"
---

<objective>
Implement the operator Control Room card and type union support for the `live_connectors` kill-switch (CONN-09), closing BUG-260826-04 and D-190-DEF-09.
</objective>

<threat_model>
- T-210-01 (Misleading Operator State): The execution engine requires `live_audience == "everyone"` for live sends (`phase_types.py:2391`). If the UI exposes `operators` or `role` audiences for `live_connectors`, the operator sees an enabled state while live sending refuses. Mitigation: Restrict the `live_connectors` card to a clean On/Off toggle (mapping to "everyone" when on and "off" when off).
- T-210-02 (Type Union Drift & Incomplete Exhaustive Maps): Widening `GovernedFeature` breaks 5 exhaustive `Record<GovernedFeature, ...>` maps if any file is missed. Mitigation: Explicitly update `DEFAULT_VISIBILITY`, `FeatureVisibility.FEATURES`, admin page state, and test fixtures simultaneously; verify with `tsc -p tsconfig.app.json`.
</threat_model>

<tasks>
<task id="1">
  <read_first>
    frontend/src/lib/api/_core.ts
  </read_first>
  <action>
    1. In `frontend/src/lib/api/_core.ts`:
       - Add `"live_connectors"` to the `GovernedFeature` type union:
         ```ts
         export type GovernedFeature =
           | "skill_studio"
           | "model_management"
           | "workflow_authoring"
           | "governance_health"
           | "visual_workflow_canvas"
           | "live_connectors"
         ```
       - Update the comment referencing `D-190-DEF-09` to note that it is now fulfilled.
       - Note: `connectionsCopy.ts` is in Phase 211's fence; its existing cast will continue to work cleanly during Phase 210 and will be retired by Phase 211.
  </action>
  <verify>
    npx tsc --noEmit -p tsconfig.app.json (run from frontend/)
  </verify>
  <done>
    `GovernedFeature` union includes `live_connectors` without conflicting with Phase 211's fence.
  </done>
</task>

<task id="2">
  <read_first>
    frontend/src/components/admin/FeatureVisibility.tsx
    frontend/src/components/admin/ControlRoomPage.tsx
    frontend/src/components/admin/__tests__/FeatureVisibility.a11y.test.tsx
    frontend/src/components/admin/revertByteIdentical.test.tsx
  </read_first>
  <action>
    1. In `frontend/src/components/admin/FeatureVisibility.tsx`:
       - Add `live_connectors` to the `FEATURES` array with title "Live Connectors", description "Controls live external network calls and integration dispatches across all workflows. When off, external calls run in dry-run/mock mode.", and binary On/Off toggle behavior (Everyone vs Off).
    2. In `frontend/src/components/admin/ControlRoomPage.tsx`:
       - Update `DEFAULT_VISIBILITY` to include `live_connectors: "off"`.
       - Ensure `visibility` and `greenlist` state objects and update handlers handle `live_connectors`.
    3. In `frontend/src/components/admin/__tests__/FeatureVisibility.a11y.test.tsx` and `revertByteIdentical.test.tsx`:
       - Update the test mock records (`ALL_EVERYONE`, `MIXED`, etc.) to include `live_connectors`.
  </action>
  <verify>
    From frontend directory: `npx tsc --noEmit -p tsconfig.app.json` (baseline 34 errors, 0 in touched files) and `npm test -- src/components/admin/__tests__/FeatureVisibility.a11y.test.tsx`.
  </verify>
  <done>
    Control Room displays the `live_connectors` card, all exhaustive maps typecheck cleanly, and tests pass.
  </done>
</task>
</tasks>
