---
phase: 221-six-applications-one-token
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/connectors/service_tools.py
  - backend/app/services/connectors/grants.py
  - backend/app/services/connector_service.py
  - backend/tests/unit/test_221_application_grouping.py
  - frontend/src/lib/api/org.ts
  - frontend/src/lib/connectionMark.tsx
  - frontend/src/components/settings/toolGroups.ts
  - frontend/src/components/settings/toolGroups.test.ts
  - frontend/src/components/settings/DirectionBand.tsx
  - frontend/src/components/settings/ActionRow.tsx
  - frontend/src/components/settings/ApplicationGroup.tsx
  - frontend/src/components/settings/ConnectionGrantsList.tsx
  - frontend/src/components/settings/ConnectionGrantsList.grouping.test.tsx
  - frontend/src/components/settings/grantsVocabulary.ts
  - frontend/src/components/settings/ConnectionFormPanel.tsx
  - frontend/src/components/settings/ConnectionsTab.tsx
  - frontend/src/components/settings/connectionsCopy.ts
  - frontend/src/components/settings/connectionRowVerdict.ts
  - frontend/src/components/settings/connectionRowVerdict.test.ts
  - scripts/vitest-count-gate.cjs
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
autonomous: true
requirements: [D-221-01, D-221-02, D-221-03, D-221-05, D-221-06, D-221-07, D-221-08, D-221-09, D-221-10, D-221-11, D-221-12]

must_haves:
  truths:
    - "D-221-07: the grouping key reaches the browser as a NEW presentation-safe `app` field. `capability`, `http_method`, `path`, `api_method` and `writes` stay STRIPPED — `extra_descriptors_for_service`'s existing docstring reason is correct and is not reversed. A test asserts no descriptor carries any of those five keys."
    - "D-221-03 IS THE SAFETY PROPERTY OF THIS PLAN. `DirectionBand.tsx` renders a label and a count and NOTHING interactive. Driven RED against a planted posture control in the band, then restored — a guard nobody has seen fire is not a guard."
    - "D-221-06: an application posture of `allow` NEVER resolves a write tool to `allow`. Tested against a PLANTED write spec, because no Google write tool exists yet — the rule must exist before the first write does, not after."
    - "D-221-05: the ladder is action -> `app:<key>` -> `default_approval_posture` -> deny. `resolve_effective_posture` keeps failing closed at every arm. NO migration: `app:` lives inside the existing `tool_grants` jsonb."
    - "`_sanitize_tool_grants` already passes any KEY through `str(key)` and only validates VALUES, so an `app:` key survives it today. This plan ADDS a key-shape assertion so an unknown namespace (`foo:bar`) is refused rather than silently stored — the same refuse-never-coerce stance the function already takes for values."
    - "D-221-11: bands render ONLY where direction is KNOWN. A connector whose tools carry no `readOnlyHint` (measured: DeepWiki sends none) gets ONE ungrouped list, never a guessed 'Only reads' heading. Absence is not a third band."
    - "D-221-09: `apps.length === 1` skips the application header and lifts the bands. GitHub's real shape — 44 tools, 27 read / 17 write, read from the live row — is a test fixture, not an invention."
    - "D-221-08 CORRECTED BY MEASUREMENT: THREE marks exist, not six. `logos:google-drive` / `google-gmail` / `google-calendar` are present in the installed `@iconify-json/logos@1.2.13`; Sheets, Docs and Contacts are absent from all three installed packs and fall back to the monogram. NO new icon dependency is added."
    - "THE G-5 SEAM IS TAKEN BY CONSTRUCTION. `ConnectionGrantsList.tsx` must SHRINK, not grow: it keeps the default-posture card, the search box and the composition, and hands every row to a child. A summary reporting it grew is reporting this decision was not honoured."
    - "Four files in this blast radius have NO hot-file-ledger row and two rows were measured STALE. Rows and their detail sections land in the SAME commit, per CLAUDE.md's same-commit sync rule."
    - "This plan ships NO write tool, NO network probe and NO migration. The availability line is plan 02."
  artifacts:
    - path: "frontend/src/components/settings/toolGroups.ts"
      provides: "the pure leaf — group by app then direction, and the three-rung posture resolver. Zero JSX."
      contains: "groupToolsByApplication"
    - path: "frontend/src/components/settings/DirectionBand.tsx"
      provides: "the band: a label and a count, and nothing interactive (D-221-03)"
      contains: "DirectionBand"
    - path: "frontend/src/components/settings/ActionRow.tsx"
      provides: "one action row, extracted verbatim from the existing .map() body"
      contains: "ActionRow"
    - path: "frontend/src/components/settings/ApplicationGroup.tsx"
      provides: "one application: header, posture control, children"
      contains: "ApplicationGroup"
    - path: "frontend/src/components/settings/connectionRowVerdict.ts"
      provides: "the DERIVED list-row verdict (D-221-12) — Ready / Partly ready / Not usable"
      contains: "connectionRowVerdict"
    - path: "backend/tests/unit/test_221_application_grouping.py"
      provides: "the app key travels; the execution fields do not; the ladder; the write rule"
      contains: "test_application_allow_does_not_reach_a_write"
  key_links:
    - from: "backend/app/services/connectors/service_tools.py"
      to: "frontend/src/lib/api/org.ts"
      via: "the `app` key emitted by extra_descriptors_for_service and declared on McpDiscoveredTool"
      pattern: "app"
    - from: "frontend/src/components/settings/toolGroups.ts"
      to: "backend/app/services/connectors/grants.py"
      via: "the SAME three-rung order on both sides — action, app:<key>, connection default"
      pattern: "app:"
    - from: "frontend/src/components/settings/ConnectionGrantsList.tsx"
      to: "frontend/src/components/settings/ApplicationGroup.tsx"
      via: "composition — the list owns no row markup after this plan"
      pattern: "ApplicationGroup"
---

<objective>
Split one flat list of fifteen Google actions into six applications, each with reads and writes
separated, under one unchanged connection and one unchanged token.

Purpose: D-221-01..12, approved from the sketch. The operator asked for six applications; the
grouping key already exists on the backend and is deliberately stripped before it reaches the
browser, so this plan carries a NEW presentation-safe field instead of reversing that strip.

Output: one pure grouping leaf, three small components, a third rung on the posture ladder, a
derived row verdict, three real product marks, and four missing hot-file-ledger rows.

⛔ NOT in this plan: any write tool, any network probe, the availability line, any migration.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⚠ **FIRST ACTION IN ANY WORKTREE:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`.
⚠ **Cap every vitest run: `GSD_VITEST_MAX_WORKERS=2`.** A red gate is triaged from the gate's own
persisted JSON **before ANY re-run** — never by touching the cap (SEED-171). Capture the failing
filenames first; a session on 2026-08-31 re-ran before capturing and could not afterwards prove
which suite it was.
⚠ **`npx tsc --noEmit` checks ZERO files.** Always `-p tsconfig.app.json`. Baseline **66** errors.
⚠ **`npx vitest run <path-that-does-not-exist>` alongside real paths EXITS 0.** Every acceptance
criterion naming a test path is preceded by `test -f <path>`.
⚠ **Files check out CRLF.** Any source-reading regex uses `\r?\n`, never a bare `\n\n`.
⚠ **THE 187-24 TRAP: a `grep -c` criterion COUNTS ITS OWN PROSE**, including a docblock forbidding
the very thing. Prefer a RUNTIME assertion (render the component, call the resolver, read the
emitted descriptor). Where a source read is unavoidable, strip comments first and assert the
stripper removed something.
⚠ **Backend unit baseline: 70 failed / 3261 passed.** Do not read a pre-existing failure as yours.
</execution_context>

<context>
@.planning/phases/221-six-applications-one-token/221-CONTEXT.md
@.planning/sketches/221-six-applications-one-token/index.html

**Design authority:** the sketch above, operator-approved rev 2. It is the acceptance bar for
layout, copy and the band's emptiness. Also load `Skill("sketch-findings-agentic-rag")` — this is a
Settings connections surface, which that skill covers.
⛔ **Invent no visual language.** Every class in the sketch was copied from
`ConnectionGrantsList.tsx` as shipped; keep it that way in reverse.
</context>

<interfaces>
The contracts this plan MINTS. Plan 02 compiles against them, so they are written first.

```ts
// toolGroups.ts — the pure leaf. No JSX, no React import.

/** The application a tool belongs to. `null` == the connector has one unnamed application. */
export type ApplicationKey = string | null

export interface ApplicationGrouping {
  key: ApplicationKey
  /** Present only when the catalog knows this application. */
  label: string
  tools: McpDiscoveredTool[]
  /** D-221-11: absent when ANY tool in this application has an unknown direction. */
  bands: { direction: "read" | "write"; tools: McpDiscoveredTool[] }[] | null
}

/** D-221-09: a connector with one application returns exactly one entry with `key: null`. */
export function groupToolsByApplication(
  tools: readonly McpDiscoveredTool[],
): ApplicationGrouping[]

/** D-221-05 / D-221-06 — the THREE-rung ladder, mirroring grants.py exactly.
 *  ⚠ A tool whose direction is `write` NEVER resolves to "allow" from an application rung.
 *  Only an explicit per-action grant can. */
export function resolveGroupedPosture(args: {
  toolName: string
  application: ApplicationKey
  isWrite: boolean
  grants: Record<string, ToolGrantPosture | boolean>
  connectionDefault: ToolGrantPosture
}): { posture: ToolGrantPosture; source: "action" | "application" | "connection" }
```

```ts
// connectionRowVerdict.ts — D-221-12, DERIVED and never stored.
export type RowVerdict = "ready" | "partly" | "unusable"
export function connectionRowVerdict(args: {
  toolCount: number
  blockedApplicationCount: number
}): RowVerdict
```

```python
# grants.py — the middle rung. The key namespace, declared once.
APPLICATION_GRANT_PREFIX = "app:"

def resolve_effective_posture(
    connection: Any,
    tool_name: str | None,
    *,
    application: str | None = None,
    is_write: bool = False,
) -> ToolGrantPosture: ...
```

```python
# service_tools.py — the emitted descriptor gains exactly ONE key.
{"name", "title", "description", "inputSchema", "annotations", "app"}
```
</interfaces>

<tasks>

## T1 — The ledger debt, first and in its own commit

Four files in this blast radius have **no hot-file-ledger row** and two rows are **stale**. A hot
file missing from the scan list is permanently invisible to its own guardrail, and a row that is
present-and-wrong answers the auditor with `satisfied` and stops the audit.

- Add rows to CLAUDE.md's ledger table for `settings/ConnectionGrantsList.tsx`,
  `lib/api/org.ts`, `connectors/service_tools.py`, `connectors/grants.py`.
- Correct the stale triples for `ConnectionFormPanel.tsx` (`9/5/2009` → `15/6/2316`) and
  `connector_service.py` (`7/3/1149` → `16/5/1461`). **Record the old value beside the new, never
  over it** — this ledger's own repeated finding is that figures rot.
- Add each file's section to `docs/HOT-FILE-LEDGER.md` **in this same commit** (the same-commit
  sync rule).
- Keep every disposition cell **under 200 characters** — `scripts/check-claude-md-size.cjs` fails
  `[disposition-too-long]` above it, and this column reaching 45% of the file is what forced the
  2026-08-25 split.

**Accept:** `node scripts/check-claude-md-size.cjs` exits 0. Every new row has a resolving anchor
in the detail file.

## T2 — Backend: the `app` key travels, and nothing else does

- Add `"app": "<drive|gmail|sheets|docs|calendar|contacts>"` to each of the 15 Google specs in
  `SERVICE_TOOL_SPECS`. Derive nothing at runtime — the key is declared per spec, exactly as
  `capability` is, so a mis-keyed tool is visible at the spec.
- Emit it in `extra_descriptors_for_service`, beside `title`. **Leave the strip of `capability`,
  `http_method`, `path`, `api_method` and `writes` exactly as it is**, and extend that docstring to
  say why `app` is different: it names a product, not a route.

**Accept:**
- A descriptor for every one of the 15 carries `app`.
- **No descriptor carries any of the five stripped keys** — assert over the real emitted list, not
  over the source.
- Grouping the emitted list by `app` yields exactly `drive 2 · gmail 5 · sheets 2 · docs 1 ·
  calendar 4 · contacts 1`.

## T3 — Backend: the middle rung, and the write rule

- `grants.py`: `APPLICATION_GRANT_PREFIX = "app:"`, and one lookup inserted between the existing
  action lookup and the connection default. Every arm still fails closed.
- **D-221-06:** when `is_write` is true, the application rung may resolve `ask` or `deny` but
  **never `allow`**. Only an explicit per-action grant can allow a write.
- `_sanitize_tool_grants`: assert the key shape. A bare tool name or an `app:<key>` is accepted; an
  unknown namespace (`foo:bar`) raises `ValueError` → 422, the same refuse-never-coerce stance the
  function already takes for values.

**Accept (`test_221_application_grouping.py`):**
- `test_application_rung_sits_between_action_and_default`
- `test_application_allow_does_not_reach_a_write` — **against a PLANTED write spec.** Set
  `{"app:drive": "allow"}`, resolve a write tool, assert `ask`. Then set
  `{"app:drive": "allow", "create_file": "allow"}` and assert `allow`.
- `test_unknown_grant_namespace_is_refused` — `{"foo:bar": "allow"}` raises.
- `test_existing_two_rung_behaviour_is_unchanged` — every pre-existing grants test still passes
  with `application=None`.

## T4 — Frontend: the pure leaf

`toolGroups.ts`, per `<interfaces>`. No React import; this file must be unit-testable with no DOM.

- `groupToolsByApplication` — stable order (the order the applications first appear in the tool
  list, so the backend's spec order is the UI's order and neither has to sort).
- **D-221-11:** `bands: null` when any tool in that application has an unknown direction. Read the
  hint from BOTH legal places — top-level `readOnlyHint` first, then `annotations.readOnlyHint` —
  reusing the resolver already in `ConnectionGrantsList.tsx` rather than writing a second one.
- `resolveGroupedPosture` — the three rungs, mirroring `grants.py`, including the write rule.

**Accept (`toolGroups.test.ts`):**
- Google's 15 fixture → 6 groupings with the exact counts from T2.
- GitHub's real shape (44 tools, 27 read / 17 write) → **one** grouping, `key: null`, two bands.
- A DeepWiki-shaped fixture (3 tools, no `annotations` at all) → one grouping, **`bands: null`**.
- `resolveGroupedPosture` agrees with `grants.py` on all four ladder cases and on the write rule.

## T5 — Frontend: three small components, and the list SHRINKS

- `ActionRow.tsx` — lift the existing `.map()` body **verbatim**. No visual change; this is a move.
- `DirectionBand.tsx` — a label and a count. **Nothing interactive.** Its docblock states D-221-03
  and quotes the ROADMAP sentence.
- `ApplicationGroup.tsx` — header (mark, name, count, availability slot **left empty for plan 02**),
  the application posture control, and children.
- `ConnectionGrantsList.tsx` — keeps the default-posture card, the search box and the composition.
  **It owns no row markup after this task.** D-221-09: `apps.length === 1` renders bands directly.
  D-221-10: the search box stays above the groups and hides groups that match nothing.

**Accept (`ConnectionGrantsList.grouping.test.tsx`):**
- Google fixture renders 6 application headers; GitHub fixture renders **0** and two bands.
- **`DirectionBand` renders zero `button`, zero `[role=group]` and zero `[aria-pressed]`.**
  ⛔ **DRIVE THIS RED:** temporarily add a posture control to the band, observe the assertion fail,
  restore, and record both observations in the summary.
- Search filters across groups; a group with no match is absent from the DOM.
- `wc -l frontend/src/components/settings/ConnectionGrantsList.tsx` is **lower than 344**.

## T6 — Marks, copy, the panel prop, and the row verdict

- `connectionMark.tsx`: add **three** entries — `logos:google-drive`, `logos:google-gmail`,
  `logos:google-calendar`. Sheets, Docs and Contacts resolve to the monogram fallback.
  ⛔ **No new icon dependency.** Follow this file's own resolved-but-INVISIBLE assertion pattern:
  a slug that resolves is not proof a person can see it.
- `grantsVocabulary.ts` — every new sentence, from the sketch verbatim.
- `ConnectionFormPanel.tsx` — one prop (`onChangeApplicationGrant`) at the one mount site.
- `connectionRowVerdict.ts` + `ConnectionsTab.tsx` + `connectionsCopy.ts` — D-221-12. The verdict is
  derived at render; **nothing is stored.** `blockedApplicationCount` is `0` until plan 02 supplies
  it, so Google reads **Ready** today and degrades on its own once plan 02 lands.

**Accept:**
- `connectionRowVerdict.test.ts`: `{tools: 15, blocked: 0}` → `ready`; `{15, 3}` → `partly`;
  **`{0, 0}` → `unusable`** — the Microsoft 365 case, which is the whole point of D-221-12.
- The three added marks render a non-empty, non-black glyph on `--card`.

## T7 — Gate, pin, close

- Add every new suite to **both** knobs in `scripts/vitest-count-gate.cjs` — TARGETS decides what
  RUNS, BASELINE decides what is GUARDED, and a suite can sit on the wrong side of exactly one of
  them. Three chat suites were found in neither on 2026-08-31, one red for hours unseen.
- Run from the **repo root**: `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs`. Read
  the **verdict line verbatim**; a bigger total than any figure in CLAUDE.md is the gate WORKING.
- `npx tsc --noEmit -p tsconfig.app.json` at or below baseline **66**.
- `backend/venv/Scripts/python -m pytest backend/tests/unit` at or below baseline **70 failed**.
</tasks>

<acceptance>
1. `node scripts/check-claude-md-size.cjs` exits 0; four new ledger rows resolve to sections.
2. All 15 Google descriptors carry `app`; **none** carries `capability`, `http_method`, `path`,
   `api_method` or `writes`.
3. `{"app:drive": "allow"}` resolves a READ to `allow` and a planted WRITE to `ask`.
4. `{"foo:bar": "allow"}` is refused with a 422.
5. Google renders six application groups; GitHub renders one ungrouped application with two bands;
   a hint-less connector renders **no bands at all**.
6. `DirectionBand` contains no interactive element — **driven RED and restored**, both recorded.
7. `ConnectionGrantsList.tsx` is **shorter** than its 344-line starting point.
8. A connection with zero tools reads **Not usable**, not Ready.
9. Count gate OK from the repo root at cap 2; tsc ≤ 66; backend unit ≤ 70 failed.
</acceptance>

<verification>
⚠ **jsdom cannot prove what this surface is FOR.** It can prove the band has no button and that six
headers render; it cannot prove the split reads as six applications. **A UAT row against the live
Google connection in org 22f9c615 is owed** and is named in the summary as owed rather than run —
and it cannot be run until the operator has connected Google in that org as `fhdmrd@gmail.com`.
</verification>
