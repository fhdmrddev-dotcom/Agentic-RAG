---
phase: 211-the-connection-is-a-service-not-a-verb
plan: 05
type: execute
wave: 4
depends_on: ["211-03", "211-04"]
files_modified:
  - frontend/src/components/settings/__tests__/connectionVerbFence.test.ts
  - frontend/src/components/workflows/__tests__/connectionCardReachability.test.tsx
  - scripts/vitest-count-gate.cjs
  - backend/tests/integration/test_211_service_shape_seam.py
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
  - .planning/phases/211-the-connection-is-a-service-not-a-verb/211-VALIDATION.md
autonomous: false
requirements: [CONN-04, CONN-05, CONN-08]

# ── WHY >1 PLAN IN THIS WAVE (one sentence, per the plan-count constraint) ──
# Genuine depends_on: the source fence walks the final file set, the count-gate knobs can only
# land in the commit that creates the files they pin, and the seam test drives the frontend's
# service-shaped create body against the real database — none of which exists until BOTH 211-03
# and 211-04 have landed.

must_haves:
  truths:
    - "Absence of the three verb CATEGORIES is proved across every settings and workflows surface at once, by a fence falsified on synthetic input before it was trusted (SC#3)"
    - "⭐ The backend↔frontend seam is driven with NOTHING mocked on either side, and the frontend half of the seam — the render gate — is covered by a test that RENDERS, because a backend-only test structurally cannot see it (AGENTS.md §3.1)"
    - "Every new test file is watched by BOTH count-gate knobs — an unpinned file is UNGUARDED, not lightly guarded"
    - "Five hot files that had no ledger row gain one, each with its detail section in the same commit"
    - "Every per-shape row is driven or explicitly ⛔ with its reason; none is silently omitted"
  artifacts:
    - path: "frontend/src/components/settings/__tests__/connectionVerbFence.test.ts"
      provides: "The SC#3 negative fence — the three category words never appear as an option, chip, tab or filter label"
    - path: "frontend/src/components/workflows/__tests__/connectionCardReachability.test.tsx"
      provides: "⭐ The FRONTEND half of the seam — a real render against the REAL empty-`discovered_tools` shape a shipped row carries, which no backend test can see"
    - path: "backend/tests/integration/test_211_service_shape_seam.py"
      provides: "The BACKEND half of the seam — mocks neither side, drives the real routes against the real database"
    - path: "scripts/vitest-count-gate.cjs"
      provides: "Both knobs for BOTH new frontend suites — the pin map AND the file list"
      contains: "connectionVerbFence.test.ts"
    - path: "docs/HOT-FILE-LEDGER.md"
      provides: "Sections for the five files gaining rows, owed in the same commit as the rows"
  key_links:
    - from: "backend/tests/integration/test_211_service_shape_seam.py"
      to: "the real POST/GET /connectors/connections handlers and the real local Postgres"
      via: "no mock on either half — the column GRANT surfaces as a 503 at test time"
      pattern: "connector_connections"
    - from: "frontend/src/components/workflows/__tests__/connectionCardReachability.test.tsx"
      to: "McpToolPicker's card gate"
      via: "a rendered assertion against discovered_tools: [] — the state the seam test cannot observe"
      pattern: "discovered_tools"
---

<objective>
Prove the two absences this phase claims — no verb category anywhere, and no unreachable surface —
and pay the bookkeeping the phase owes: both count-gate knobs, the ledger rows, and the per-shape
scoreboard.

Purpose: `AGENTS.md §3.1` requires a split phase to own an integration test that mocks NEITHER side,
because this project's last two escaped defects were both SEAMS between parallel plans with each side
green. ⚠ **This phase has TWO seams, not one** — backend↔frontend (the wire) and
data-shape↔render-gate (the empty action list). A backend-only test can only see the first, and
revision iteration 1 caught exactly that gap. Both halves are owed.

Output: two falsified-first fences, both gate knobs, the seam driven end to end, five ledger rows with
their detail sections, and the filled per-shape board.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/211-the-connection-is-a-service-not-a-verb/211-CONTEXT.md
@.planning/phases/211-the-connection-is-a-service-not-a-verb/211-RESEARCH.md
@.planning/phases/211-the-connection-is-a-service-not-a-verb/211-VALIDATION.md
@.planning/phases/211-the-connection-is-a-service-not-a-verb/211-03-SUMMARY.md
@.planning/phases/211-the-connection-is-a-service-not-a-verb/211-04-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- RE-DERIVE line numbers before relying on them. -->

**The two gate knobs, stated in `scripts/vitest-count-gate.cjs`'s own comments:** `TARGETS` decides
what RUNS, `BASELINE` decides what is PINNED, and a file can sit outside BOTH by default.
`src/components/settings` is a FILE-LEVEL target, so a new file there is invisible to the gate unless
BOTH knobs are turned. ⚠ An entry pointing at a path that does not yet exist makes the gate **exit 2**,
so it can only land in the commit that creates the file.

**The cross-language fence discipline to model both new fences on:** `ExternalActionSection.tsx`'s
docblock — the suite reads `backend/app/models/harness.py` as raw source, extracts the Literal's
members, and asserts they equal `EXTERNAL_ACTION_CAPABILITIES`. **It was observed RED against a
planted mismatch before it was trusted.** Model on that discipline; do not invent one.

**⭐ Why a frontend reachability suite exists in THIS plan and not in 211-04.** 211-04's own tests
mount `McpToolPicker` directly (tier T1) or reach it through `ExternalActionSection` with a fixture
211-04 itself wrote. Neither can prove the property that matters after the phase ships: *the shape a
row in the REAL database carries reaches a rendered surface.* This file is the frontend half of the
seam and belongs beside the backend half, in the plan that owns the seam obligation.

**The three descriptors' real shape** (plan 211-01): four keys — `name`, `title`, `description`,
`inputSchema` — with `inputSchema.required` equal to the adapter's own array
(`post_message` → `["text"]`, `create_ticket` → `["summary","description"]`,
`send_email` → `["to","subject","body"]`).

**Gate baselines — compare against these, never against 0** (`211-MEASUREMENTS.md` §1):
tsc **34 errors** with `-p tsconfig.app.json` · count gate **OK · 114/114 pinned · total 5793 ·
0 failed** · backend unit **68 failed / 2680 passed** · CLAUDE.md **83,431 chars**.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The SC#3 negative fence, the frontend half of the seam, and BOTH gate knobs</name>

  <files>frontend/src/components/settings/__tests__/connectionVerbFence.test.ts, frontend/src/components/workflows/__tests__/connectionCardReachability.test.tsx, scripts/vitest-count-gate.cjs</files>

  <read_first>
    - `frontend/src/components/workflows/ExternalActionSection.tsx` — the `?raw` cross-language fence and the discipline it records (falsify the extractor on synthetic input BEFORE trusting it on the tree)
    - `frontend/src/components/workflows/McpToolPicker.reachability.test.tsx` (as left by 211-04) — the tier-discipline preamble, so the new file states which tier IT is and does not duplicate leg (b)
    - `frontend/src/components/workflows/McpToolPicker.tsx` (as left by 211-04) — the card gate, the tool-list gate, and the Refresh affordance
    - `scripts/vitest-count-gate.cjs` — the `TARGETS` file list entries for `src/components/settings/__tests__/*` and the BASELINE pin map entries for the same suites, INCLUDING their comment blocks
    - `frontend/src/components/settings/connectionRefusalCopy.ts`, `connectionsCopy.ts` and `workflows/phaseVocabulary.ts` — the ATTRIBUTE prose the fence must NOT flag
    - `backend/tests/unit/test_190_connector_source_fence.py` `test_the_walk_is_not_vacuous` — the non-vacuity shape to copy
  </read_first>

  <action>
**(a) The SC#3 negative fence.** Create
`frontend/src/components/settings/__tests__/connectionVerbFence.test.ts`. It is a SOURCE fence, not a
render test, so it covers every surface at once rather than only the ones a test happens to mount:
  - Walk the source of `src/components/settings/**` and `src/components/workflows/**` (excluding
    `__tests__` and `*.test.*`) and assert the three category words **`Message`, `Ticket`, `Email`**
    never appear as an option/chip/tab/filter LABEL. Define "label" MECHANICALLY: a string literal
    assigned to a key named `label`, `title`, `chip`, `tab`, `option` or `name` inside a module-scope
    options/choices constant, **or** a JSX text child of an element carrying `role="radio"` /
    `role="tab"` / `role="option"`. Report offenders as `file:line: the line`, so a RED is a work item
    rather than a boolean.
  - ⚠ **Falsify the matcher on synthetic input FIRST**, in the shape the `?raw` fence uses: a control
    array of lines that MUST fire (a `{ label: "Send an email" }` entry, a
    `<button role="radio">Creates a ticket</button>` child) and a control array that MUST NOT
    (`destinationFactsOf` prose, the refusal nouns, `CONNECTION_CAPABILITY_WORDS`'s per-row attribute
    words, an `EXTERNAL_CAPABILITY_SENTENCES` entry read as node-face vocabulary). **A fence that flags
    English produces noise, and noise is how a fence gets deleted.**
  - ⚠ Assert the walk is NON-VACUOUS on two independent floors: a minimum file count AND a pinned set
    of file names that must have been visited, including `ConnectionFormPanel.tsx`,
    `ConnectionPicker.tsx`, `ExternalActionSection.tsx` and `McpToolPicker.tsx`.

**(b) ⭐ The FRONTEND half of the seam.** Create
`frontend/src/components/workflows/__tests__/connectionCardReachability.test.tsx`. Its stated purpose,
in a docblock: *a backend test cannot see a render gate, and revision iteration 1 of this phase caught
a proposed gate that would have made every pre-existing row unreachable through the UI.* It asserts
the render-gate properties against the FOUR real row shapes, using row objects built to the wire shape
`GET /connectors/connections` returns — never a hand-tuned component fixture:
  1. **legacy · empty** — `capability: "post_message"`, no `mcp_server_url`, `discovered_tools: []`
     (the shape every shipped row carried before migration 127 §2b): the card renders, the "no actions
     yet" sentence renders, and the Refresh control is present and enabled for an admin. ⭐ **This is
     the closed loop; it is the case the whole plan set exists to keep open.**
  2. **legacy · populated** — the same row with the one-element `post_message` descriptor: the tool
     `<select>` renders and lists `post_message`.
  3. **MCP · empty** — a valid `mcp_server_url` with `discovered_tools: []`: the Refresh control is
     still present (the regression 211-04 Task 1(d) guards, re-asserted at the seam).
  4. **service-only** — no capability, no endpoint, `discovered_tools: []`: the card renders with its
     empty sentence rather than vanishing.
  Plus a NEGATIVE control: no connection bound → nothing renders. ⚠ Drive at least case 1 RED against
  the pre-211-04 gate (`git stash` the component, or assert against a local copy of the old gate) and
  **record that RED verbatim** — a control that has never been observed failing is not a control.
  ⚠ State plainly in the docblock **which half of the seam this file covers and which half
  `test_211_service_shape_seam.py` covers**, so neither is mistaken for the whole.

**(c) BOTH gate knobs, in this same commit, for BOTH new files.**
  - Add `"src/components/settings/__tests__/connectionVerbFence.test.ts"` and
    `"src/components/workflows/__tests__/connectionCardReachability.test.tsx"` to the `TARGETS` file
    list beside the existing settings/workflows entries, and
  - add a pin for each to the BASELINE map, taking the number from **this script's own `actual` column
    across two agreeing runs** — never a hand count of `it(` literals.
  ⚠ Both knobs, or a suite is UNGUARDED rather than lightly guarded. ⚠ The entries can only land in the
  commit that creates the files: an entry for a path that does not exist makes the gate exit 2.
  ⚠ **Do not RAISE any existing pin** — raising deletes a line, and the gate's contract is *no per-file
  DECREASE*. Additions only. If 211-03 or 211-04 legitimately DELETED cases from a pinned file (both
  plans delete obsolete assertions by design), that is the one case where a pin must be LOWERED — do it
  explicitly, name the file and the deleted cases from that plan's summary in the diff comment, and
  never lower a pin whose deletions are not named in a summary.
  </action>

  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/settings/__tests__/connectionVerbFence.test.ts --reporter=basic` exits 0, and its synthetic MUST-FIRE control is asserted before the tree is walked
    - `cd frontend && npx vitest run src/components/workflows/__tests__/connectionCardReachability.test.tsx --reporter=basic` exits 0 with at least 5 cases
    - `grep -c "connectionVerbFence.test.ts" scripts/vitest-count-gate.cjs` returns `2` (one `TARGETS` entry, one BASELINE pin)
    - `grep -c "connectionCardReachability.test.tsx" scripts/vitest-count-gate.cjs` returns `2`
    - `git diff scripts/vitest-count-gate.cjs | grep -E "^-" | grep -v "^---"` prints nothing, OR every deleted pin line is a deliberate LOWERING whose deleted cases are named in a 211-03/211-04 summary and quoted in this plan's summary
    - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` run from the repo root prints a verdict line reading `count gate OK`, with pinned files **≥ 116** and **0 failing**, and the verdict line is quoted **verbatim** in the summary. ⚠ A growing total is the gate WORKING; the contract is no per-file DECREASE, never a fixed total
    - The RED observation of the legacy-empty render case against the pre-211-04 gate is quoted verbatim in the summary
  </acceptance_criteria>

  <verify>
    <automated>cd frontend && npx vitest run src/components/settings/__tests__/connectionVerbFence.test.ts src/components/workflows/__tests__/connectionCardReachability.test.tsx --reporter=basic</automated>
  </verify>

  <done>Absence of the three categories is proved by a fence falsified before it was trusted; the render gate is proved reachable against the four REAL row shapes including the empty one; both new suites are watched by both knobs and the gate reads OK with no unexplained per-file decrease.</done>
</task>

<task type="auto">
  <name>Task 2: The backend half of the seam — mocks neither side — and the ledger rows this phase owes</name>

  <files>backend/tests/integration/test_211_service_shape_seam.py, CLAUDE.md, docs/HOT-FILE-LEDGER.md</files>

  <read_first>
    - `backend/tests/integration/conftest.py` and two neighbouring integration tests, for the house pattern
    - `backend/tests/test_migration_127.py` (as left by 211-02) — the skip conditions, the rollback discipline, and the "a green skip is not a passing fence" rule
    - `backend/app/services/connectors/descriptors.py` (plan 211-01) and the three adapters' `INPUT_SCHEMA`
    - `AGENTS.md` §3.1 — "a split phase owes an integration test that mocks NEITHER side, and that test is a blocking gate"
    - `CLAUDE.md` — the hot-file ledger table, the 200-char disposition cap, and the same-commit sync rule
    - `docs/HOT-FILE-LEDGER.md` — two existing sections, for the shape a new one takes
    - `.planning/phases/211-the-connection-is-a-service-not-a-verb/211-MEASUREMENTS.md` §2 (the already-measured triples — do NOT re-derive them)
  </read_first>

  <action>
**(a) ⭐ The cross-plan seam test.** Create `backend/tests/integration/test_211_service_shape_seam.py`.
It must drive the REAL path end to end, against the REAL local database, with no mock on either half:
  1. Create a **service-only** connection through the real `POST /connectors/connections` handler
     (`service_id` set, no capability, no endpoint) → 201; read it back through the real
     `GET /connectors/connections` → present, carrying its `service_id`, with `discovered_tools == []`.
     **This crosses plan 211-02's model arm, its row dict, migration 127's two constraints and the
     column GRANT in one call** — the GRANT omission surfaces here as a 503, which is the failure mode
     that looks like an outage.
  2. Create a **capability** connection the same way → it comes back with a one-element
     `discovered_tools` whose tool `name` is that capability and whose `inputSchema.required` equals
     that adapter's own `INPUT_SCHEMA["required"]`, **compared by importing the adapter in the test**
     rather than by a literal.
  3. The both-shapes body is refused with a 422 **and** the equivalent direct SQL write is refused by
     the database — two separate assertions, because one passing does not imply the other.
  4. `capability` on the two pre-existing legacy rows is byte-unchanged (CONN-05).
  5. ⭐ **The migration §2b backfill, read from the LIVE rows:** every `capability IS NOT NULL` row has
     `jsonb_array_length(discovered_tools) = 1` and that element's `name` equals its own `capability`.
     This is the DATA half of the closed loop; the RENDER half is Task 1(b)'s file, and the seam test's
     docstring must say so and name that file, because **a backend test cannot see a render gate.**
  6. Exercise the capability arm of `POST /connections/{id}/discover` against a real capability row:
     it returns the descriptor list and writes it, without any network call. ⚠ This is the self-heal
     path; it has exactly one caller in the product (211-04's Refresh control) and this is the only
     automated exercise it gets.
  ⚠ Every write inside a transaction that ROLLS BACK, or cleaned up explicitly — the operator's local
  database is the working environment. Skip cleanly and NAME the reason if `:54322` is unreachable or
  migration 127 is unapplied; **a green skip is not a passing fence** and the summary must say which
  it was.
  ⚠ **Serialize this plan's test run** — it mutates the local database, and worktrees isolate files,
  not Postgres (CLAUDE.md parallel-execution rule 4).

**(b) The ledger rows this phase owes, in the same commit as the work.** `211-MEASUREMENTS.md` §2 has
already measured the triples — **do not re-derive those**; re-derive only the files it did not cover,
using CLAUDE.md's recipe with six-digit dated quick-task buckets subtracted:
  - Add rows to CLAUDE.md's hot-file table for **`backend/app/models/connector.py`** and
    **`backend/app/services/mcp_client.py`** — both have NO row today, which means G-5 could never fire
    on either at any count.
  - Add rows for the files this phase made hot and which have no row: **`backend/app/api/connectors.py`**,
    **`backend/app/services/connector_service.py`**, and
    **`frontend/src/components/workflows/McpToolPicker.tsx`** (named inside Phase 206's section with no
    row of its own).
  - Update the existing rows for `harness/phase_types.py` and `harness/grounding.py` — this phase IS
    their refactor (ROADMAP Flags), but 211 modified NEITHER file. ⚠ **Say that plainly rather than
    writing `satisfied`**: a row that is present and WRONG answers the auditor and stops the audit,
    which is worse than an absent row. Record the disposition as *honoured by construction — the phase
    moved the axis out of the UI without editing either file*, with the empty-diff evidence in the
    detail file.
  ⚠ The disposition cell is capped at **200 characters** by `scripts/check-claude-md-size.cjs`; put the
  VERDICT in the cell and every reason in that file's own section in `docs/HOT-FILE-LEDGER.md`, in the
  same commit. A row without a section, or a section without a row, is drift.
  - Run `node scripts/check-claude-md-size.cjs` and record the verdict line. The warn band is 120,000
    characters; the baseline for this phase is 83,431.
  </action>

  <acceptance_criteria>
    - `cd backend && ./venv/Scripts/python.exe -m pytest tests/integration/test_211_service_shape_seam.py -q` exits 0 with **zero skips**, and the summary states it RAN rather than skipped
    - The seam test's docstring names `frontend/src/components/workflows/__tests__/connectionCardReachability.test.tsx` as the half it cannot cover, and vice versa (`grep -c "connectionCardReachability" backend/tests/integration/test_211_service_shape_seam.py` is at least `1`)
    - `node scripts/check-claude-md-size.cjs` exits 0, with no `[disposition-too-long]`, `[duplicate-row]` or `[malformed-row]`
    - `grep -c "backend/app/models/connector.py" CLAUDE.md docs/HOT-FILE-LEDGER.md` is at least `1` in both files
    - `grep -c "backend/app/services/mcp_client.py" CLAUDE.md docs/HOT-FILE-LEDGER.md` is at least `1` in both files
    - `grep -c "backend/app/api/connectors.py" CLAUDE.md docs/HOT-FILE-LEDGER.md` is at least `1` in both files
    - `grep -c "backend/app/services/connector_service.py" CLAUDE.md docs/HOT-FILE-LEDGER.md` is at least `1` in both files
    - `grep -c "workflows/McpToolPicker.tsx" CLAUDE.md docs/HOT-FILE-LEDGER.md` is at least `1` in both files
    - Neither `phase_types.py` nor `grounding.py` disposition cell reads the bare word `satisfied` (`grep -c "phase_types.py.*satisfied" CLAUDE.md` returns `0`)
    - `git diff --numstat backend/app/models/harness.py backend/app/services/harness/grounding.py backend/app/services/harness/phase_types.py` prints nothing, across the whole phase
  </acceptance_criteria>

  <verify>
    <automated>cd backend && ./venv/Scripts/python.exe -m pytest tests/integration/test_211_service_shape_seam.py -q && cd .. && node scripts/check-claude-md-size.cjs</automated>
  </verify>

  <done>The backend↔database↔wire seam is driven with nothing mocked, including the self-heal path and the §2b backfill read from live rows; each seam test names the half it cannot cover; five hot files gain the rows and detail sections they were owed, with every disposition cell under the cap and no cell reading a bare `satisfied`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: The per-shape scoreboard — the lived-experience gate (G-4)</name>

  <files>.planning/phases/211-the-connection-is-a-service-not-a-verb/211-VALIDATION.md</files>

  <read_first>
    - `.planning/phases/211-the-connection-is-a-service-not-a-verb/211-VALIDATION.md` — the *Manual-Only Verifications* table and the *Per-shape scoreboard* table, both of which this task fills in
    - `.planning/phases/211-the-connection-is-a-service-not-a-verb/211-RESEARCH.md` §J.1 (there is no `send_email` row on this box) and §K.5 (why the 8-row cross-provider roster does NOT apply)
    - `CLAUDE.md` § "UAT scoreboard recipe" — *rows may be blocked, but never silently omitted*
    - `.planning/phases/211-the-connection-is-a-service-not-a-verb/211-02-SUMMARY.md` — the operator's per-row `discovered_tools` query output, so row 1 and row 2 below can be checked against what the migration actually wrote
  </read_first>

  <action>
STOP after the automated gates are green and hand the per-shape board to the operator. Drive nothing
in a browser on their behalf and do not substitute a wire-format check or a screenshot for a row —
G-4 requires the driven scenario. Present the five per-shape rows and the four lived-experience checks
below, then write each returned verdict into `211-VALIDATION.md`'s per-shape scoreboard and
Manual-Only Verifications tables. A blocked row is recorded with its reason in the same cell; it is
never dropped from the table.
  </action>

  <what-built>
The full phase: a connection now names a service, its actions come from its own tool list — including
the two rows that already existed, whose action lists migration 127 §2b backfilled — no verb category
is offered anywhere, and a service-only row saves. Every automated gate is green.

⚠ **The cross-provider 8-row roster does NOT apply here** — no model call is anywhere in this phase's
blast radius. What is owed instead is the **per-shape** board.
  </what-built>

  <how-to-verify>
Start the backend and the frontend, then drive each row and record the verdict in
`211-VALIDATION.md`'s per-shape table:

1. **Legacy · Slack (`post_message`)** — open the existing `Slack-rag-test` connection. It presents as
   a SERVICE with a named action **without anyone pressing anything first**. Then drive it through a
   workflow external-action step and confirm it still SENDS, unchanged (SC#2).
2. **Legacy · Jira (`create_ticket`)** — the same, against the existing `Jira – KAN` row.
3. **Legacy · SMTP (`send_email`)** — ⛔ **there is no such row on this box.** Either create one and
   drive it, or record the row as ⛔ **with its reason**. **Never silently omit it.**
4. **Remote server (DeepWiki)** — its tool list still renders and its grants are unchanged.
5. **Service-only (CONN-08)** — create a connection naming a service nobody here has heard of, with no
   credential and no endpoint. It SAVES (SC#4), it appears in the list, and it is not offered a verb
   category anywhere.

Then the four lived-experience checks (G-4):
- **SC#1** — Settings → Connections → New. Confirm there is **no** Message/Ticket/Email chooser at any
  step of the flow.
- **SC#3** — sweep Settings → Connections *and* the canvas ConnectionPicker and the node form: chips,
  tabs, filters, form categories, option groups. No verb category anywhere.
- ⭐ **THE CLOSED LOOP (the defect revision iteration 1 caught)** — bind the **service-only** row from
  row 5 in a workflow external-action step. Its action list is legitimately EMPTY. Confirm the card
  still renders, that it says so in words, and that a **Refresh** control is present and pressable. ⚠
  If the card renders NOTHING here, STOP — that is the unreachability this phase was revised to
  prevent, and no amount of green automation contradicts it.
- **The column GRANT** — Settings → Connections loads and lists rows. ⚠ A 503 / "Could not load
  connections" here is the `service_id` GRANT, **not an outage** (mig 126 §3).
  </how-to-verify>

  <acceptance_criteria>
    - All five rows of `211-VALIDATION.md`'s per-shape scoreboard carry a verdict; any ⛔ carries its reason in the same cell
    - `211-VALIDATION.md`'s *Manual-Only Verifications* table has every row marked with an observed result
    - Settings → Connections returns 200 and renders the list (no 503)
    - Rows 1 and 2 present a named action with NO press required first — if a press was needed, the migration §2b backfill did not take and the row is recorded FAILED, not passed-with-a-note
    - The closed-loop check renders a card, an in-words empty state and a pressable Refresh for the service-only row
    - No screenshot or wire-format check substitutes for a row — G-4 requires the driven scenario
  </acceptance_criteria>

  <resume-signal>Type "approved" with the five per-shape verdicts and the closed-loop result, or describe what failed and where.</resume-signal>

  <verify>
    <automated>GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs</automated>
    <human-check>The per-shape scoreboard and the four G-4 checks above, recorded in 211-VALIDATION.md</human-check>
  </verify>

  <done>Every shape is driven or explicitly blocked with a reason; the three original lived-experience checks and the new closed-loop check are observed rather than inferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan 211-02's backend ↔ plans 211-03/04's frontend | A split seam — this project's last two escaped defects lived here, each side green |
| stored row shape ↔ render gate | The second seam: a row shape that is legal in the database but unrenderable in the UI |
| `authenticated` role → column-level SELECT | A new column is unreadable by default (mig 118's column-by-column grant) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-211-19 | Denial of Service | the `service_id` column GRANT | mitigate | The seam test's read-back call surfaces a missing grant as the 503 it really is, at test time rather than in a browser; the G-4 checkpoint names the same symptom so it is not misread as an outage |
| T-211-20 | Repudiation | an unwatched new test suite | mitigate | BOTH count-gate knobs land in the same commit as BOTH new files. An unpinned file is not lightly guarded, it is UNGUARDED |
| T-211-22b | Denial of Service (self-inflicted) | a stored row shape with no renderable surface | mitigate | Covered on BOTH sides and stated as two halves: `test_211_service_shape_seam.py` proves the data, `connectionCardReachability.test.tsx` proves the render, each docstring naming the other. A backend-only seam test is what revision iteration 1 caught as insufficient |
| T-211-24 | Repudiation | a fence that flags ordinary English | mitigate | The matcher is falsified on synthetic MUST-FIRE and MUST-NOT-FIRE controls before the tree is walked, and non-vacuity is asserted on two independent floors. Noise is how a fence gets deleted |
| T-211-25 | Tampering | a lowered count-gate pin hiding a real deletion | mitigate | The default is additions-only; a LOWERING is permitted only when the deleted cases are named in a 211-03/211-04 summary and re-quoted here, so a silent decrease cannot pass as bookkeeping |
| T-211-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed by this phase; no legitimacy checkpoint owed |
</threat_model>

<verification>
Run from the repo root. ⚠ Serialize — this plan's seam test mutates the local database.

1. `cd frontend && npx tsc --noEmit -p tsconfig.app.json` → **at most 34** errors (the `-p` flag is load-bearing; without it tsc checks ZERO files)
2. `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` → read the **verdict line verbatim**. Expect `count gate OK`, pinned files ≥ 116, 0 failing, no unexplained per-file decrease. ⚠ **A red run is not automatically a flake:** capture the failing filenames from the gate's own persisted JSON **before re-running anything**, check each against `git diff --numstat`, and only then compare against SEED-171's five known-flaky suites. **Do not reach for the worker cap** — Phase 195 measured that adjusting it does not fix these, and Phase 196 measured `failed 249` that was entirely REAL.
3. `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit -q` → per-file against the 68-failure rot baseline. No NEW failing file.
4. `cd backend && ./venv/Scripts/python.exe -m pytest tests/integration/test_211_service_shape_seam.py tests/test_migration_127.py -q` → 0 skips
5. `node scripts/check-claude-md-size.cjs` → exit 0, no `[disposition-too-long]`, `[duplicate-row]` or `[malformed-row]`
6. `node scripts/check-gap-closure-rounds.cjs 211` before any `--gaps` routing is proposed (G-7)
</verification>

<success_criteria>
- Browsing, filtering and picking never offer Message / Ticket / Email as a category, proved by a falsified-first source fence over BOTH surface trees (SC#3, CONN-05)
- ⭐ Both halves of the seam are covered and each names the other: the data shape against the real database, and the render gate against the real empty-array shape
- The self-heal discover path is exercised automatically at least once, on a capability row
- Both new suites are watched by both count-gate knobs; any lowered pin is traceable to named deleted cases
- Five hot files gain ledger rows with detail sections in the same commit; no cell reads a bare `satisfied`; every cell is under 200 characters
- Every per-shape row is driven or explicitly ⛔ with its reason; the closed-loop check is observed in a browser
</success_criteria>

<output>
Create `.planning/phases/211-the-connection-is-a-service-not-a-verb/211-05-SUMMARY.md` when done.
Record verbatim: the count-gate verdict line; the tsc error count; the RED observation of the
legacy-empty render case against the pre-211-04 gate; whether the seam test RAN or SKIPPED; the
`check-claude-md-size.cjs` verdict; any lowered count-gate pin with the named deleted cases behind it;
and the filled per-shape scoreboard including the closed-loop result and any ⛔ with its reason.
</output>
