---
phase: 214.1-an-author-can-declare-an-input
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/workflows/PublishGauntlet.tsx
  - frontend/src/components/workflows/PublishRefusalList.tsx
  - frontend/src/components/workflows/publishRefusalEntry.ts
  - frontend/src/components/workflows/PublishGauntlet.test.tsx
  - frontend/src/components/workflows/PublishRefusalList.test.tsx
  - backend/app/services/harness/phase_types.py
  - backend/tests/unit/test_2141_no_false_capability_refusal.py
autonomous: true
requirements: [STEP-03]

must_haves:
  truths:
    - "D-214.1-04: `BUG-260828-04` is DIAGNOSED, never assumed. Task 1 DRIVES the distinction before it writes a fix, because shipping a CSS clamp against the stale-bundle hypothesis would leave a real classification defect live while looking fixed."
    - "THREE hypotheses, not two. (a) a classification or mount defect in the publish modal; (b) a stale frontend bundle at drive time, which is live because BOTH 214-10 commits landed on 2026-08-28, the same day as the drive; (c) ⭐ the operator was reading a DIFFERENT surface — `ProblemsTray` renders `verdict.message` VERBATIM BY DESIGN and is fed by `POST /workflows/validate`, so a raw diagnostic there is correct behaviour and needs a different answer entirely."
    - "MEASURED AT HEAD, so the diagnosis starts from facts rather than from the bug's framing: `PublishGauntlet.tsx` contains NO `truncate`, NO `line-clamp` and NO `slice` on a message; `LintRow` / `PhaseRow` / `BlockMessage` each render `String(message)` in a plain `leading-relaxed` div. **A CSS clamp on the publish path does not exist to be removed.**"
    - "If the answer is (b), say so plainly and close the bug as not-reproducible. Inventing work to justify a phase is worse than a short summary."
    - "D-214.1-05: `BUG-260828-03` is answered by a LOOKUP, never by prompting alone. The model must not be the source of truth for whether a service is connected."
    - "SCOPE LIMIT, imposed by D-214.1-05 and binding: this plan makes the model AWARE of the fact at the point it would otherwise speculate. It adds NO post-hoc censor over generated text."
    - "The awareness block is derived PURELY from the definition the run already holds — no connection read, no new I/O, no new trust boundary, and no connection id ever reaches a prompt."
    - "It returns the empty string for every workflow with no `external_action` phase, so every other run's prompt is BYTE-IDENTICAL by construction rather than by review."
  artifacts:
    - path: "frontend/src/components/workflows/PublishGauntlet.test.tsx"
      provides: "the driven diagnosis of BUG-260828-04 against the real live wire shape"
      contains: "ask_undeclared"
    - path: "backend/app/services/harness/phase_types.py"
      provides: "the wired-services awareness block, a pure sibling of _stateful_framing_block"
      contains: "_wired_services_block"
    - path: "backend/tests/unit/test_2141_no_false_capability_refusal.py"
      provides: "the block's cases, its empty-string arm and its byte-identity control"
      contains: "_wired_services_block"
  key_links:
    - from: "frontend/src/components/workflows/PublishGauntlet.tsx"
      to: "frontend/src/components/workflows/publishRefusalEntry.ts"
      via: "isArgumentRefusal partitioning named_failures into composed and generic"
      pattern: "isArgumentRefusal"
    - from: "backend/app/services/harness/phase_types.py"
      to: "backend/app/models/harness.py"
      via: "reading external_action tool_name off the definition the run already holds"
      pattern: "external_action"
---

<objective>
Make a publish refusal readable, having first found out why it was not — and stop the agent stating
that a service is unconnected when the workflow it is running is wired to that very service.

Purpose: SC#3 and SC#5. `BUG-260828-04` presents as truncated text, but `PublishRefusalList.tsx`'s
own docblock FORBIDS rendering the backend diagnostic and the phase verifier's hand-trace says
`isArgumentRefusal` returns `true` for the exact live entry shape — so the composed sentence SHOULD
have rendered and something else is true instead. `BUG-260828-03` is worse than a wrong sentence: the
false claim reached a RECIPIENT and sat inside the approval pause a human was asked to approve.

Output: one driven diagnosis with a fix that matches what it found, and one pure prompt block that
hands the model a fact it was previously inventing.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⚠ **FIRST ACTION IN ANY WORKTREE:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`.
⚠ **The Python backend runs in its `venv`.** Its tests must NOT mutate the local Postgres.
⚠ **This plan adds NO new test suite and therefore edits NO registry.** Its frontend cases go into
the shipped, already-pinned `PublishGauntlet.test.tsx` (46) and `PublishRefusalList.test.tsx` (22).
⛔ **Do not edit `scripts/vitest-count-gate.cjs`, `CLAUDE.md` or `docs/HOT-FILE-LEDGER.md`** — plan
`214.1-02` owns every shared-registry edit for this phase, and this plan runs in the SAME wave as
`214.1-01`. Record your measured triples in the summary for `214.1-02` Task 3 to transcribe.
⚠ **Cap every vitest run: `GSD_VITEST_MAX_WORKERS=2`**; triage a red gate from its own persisted JSON
before ANY re-run (SEED-171).
⚠ **`npx vitest run <nonexistent path>` alongside real paths EXITS 0** — `test -f` first.
⚠ **CRLF** — `\r?\n`, never a bare `\n\n`.
⚠ **`npx tsc --noEmit` checks ZERO files** — `-p tsconfig.app.json`. Baseline **34**.
⚠ **THE 187-24 TRAP:** a `grep -c` criterion counts its own prose, INCLUDING a docblock that forbids
the thing. `PublishRefusalList.tsx`'s header spells the forbidden words out loud, so a naive grep
over it self-invalidates. Strip comments first and assert the stripper removed something, or assert
at RUNTIME on the rendered DOM.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/214.1-an-author-can-declare-an-input/214.1-CONTEXT.md
@.planning/reported-bugs/BUG-260828-04-publish-refusal-truncated-on-screen.md
@.planning/reported-bugs/BUG-260828-03-agent-claims-no-email-service-while-connection-exists.md
@.planning/phases/214-a-step-names-its-service-and-its-action/214-VERIFICATION.md

⭐ **`214-VERIFICATION.md` lines 22-31 and 91 and 101-127 are Task 1's brief.** They carry the
hand-trace of `isArgumentRefusal` against the LIVE entry shape observed in the drive
(`code="ask_undeclared"`, `phase="act"`, `step_name="act"`, `argument="to"`, `upstream=null`), the
verdict that the predicate returns `true` for it, and the explicit re-verification instruction:
*"Re-drive G4-4 against a hard-reloaded (non-HMR) build and confirm which text renders."*
</context>

<interfaces>
<!-- The live wire entry the diagnosis must reproduce EXACTLY. Anything else proves nothing. -->

```
{"code": "ask_undeclared", "phase": "act",
 "message": "phase 'act': the required argument 'to' is asked for at launch, but the workflow declares no matching input",
 "step_name": "act", "argument": "to", "upstream": null}
```

⚠ **`message` begins `phase 'act':` and NOT `step 'act':`, and that is a CLUE, not a contradiction.**
`reachability._gap_message` renders `f"step {step!r}"` only when `step` is truthy and it is handed
`str(step_name or "")` — so the phase carried NO authored `name`. The payload's own `step_name` then
falls back to the SLUG at `reachability.py:283`, which is the one documented case where the author's
word and the slug coincide. Both halves are consistent; a diagnosis that treats the `phase` prefix as
evidence of a broken payload has misread it.

The awareness block, a pure sibling of the shipped `_stateful_framing_block(ctx)`:

```python
def _wired_services_block(ctx) -> str:
    """'' when the running definition has no external_action phase."""
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DIAGNOSE `BUG-260828-04` against the real payload, then fix what the diagnosis found</name>
  <files>frontend/src/components/workflows/PublishGauntlet.test.tsx, frontend/src/components/workflows/PublishRefusalList.test.tsx, frontend/src/components/workflows/PublishGauntlet.tsx, frontend/src/components/workflows/PublishRefusalList.tsx, frontend/src/components/workflows/publishRefusalEntry.ts</files>
  <read_first>
    - `frontend/src/components/workflows/publishRefusalEntry.ts:76-87` — `isArgumentRefusal`. ⭐ **Its
      three declining arms are the candidate list for hypothesis (a):** an unrecognised `code`, an
      empty/absent `step_name`, and (for every kind but `shape_unknown`) an empty/absent `argument`.
      Anything it declines falls through to the generic renderer, where the server's diagnostic is
      printed — which is the observed symptom, reached honestly.
    - `frontend/src/components/workflows/PublishGauntlet.tsx:770-815` — `argumentRefusals` /
      `genericFailures`, and the mount `{argumentRefusals.length > 0 && <PublishRefusalList ... />}`.
      ⭐ **Note what is NOT passed: `onGoToStep`.** The next-action button therefore renders DISABLED.
      That is a legibility finding in its own right and this task REPORTS it explicitly.
    - `frontend/src/components/workflows/PublishGauntlet.tsx:1015-1040` — where `genericFailures`
      render, and the comment stating that 214-10 subtracts exactly one thing and nothing else.
    - `frontend/src/components/workflows/PublishGauntlet.tsx:281-300` — `LintRow` / `PhaseRow` /
      `BlockMessage`. ⭐ **MEASURED AT HEAD: none of the three truncates.** No `truncate`, no
      `line-clamp`, no `slice`, no `text-ellipsis`, no `whitespace-nowrap` anywhere in this file's
      failure-rendering path. **So there is no CSS clamp on the publish path to remove**, and a fix
      that removes one has fixed something that was not there.
    - `frontend/src/components/workflows/ProblemsTray.tsx:24-26, 263, 303` — ⭐ **HYPOTHESIS (c),
      and it is a MEASUREMENT rather than a guess.** *"Nothing is filtered, nothing is re-ordered,
      and no message is rewritten: the row shows the server's own `message` verbatim."* This surface
      is fed by the live `POST /workflows/validate` seam, not by the publish verdict, and a raw
      diagnostic there is CORRECT BEHAVIOUR by its own design. If that is the screen the operator was
      reading, the bug is neither a clamp nor a classification defect.
    - `git log --format="%h %ad %s" --date=short -- frontend/src/components/workflows/PublishRefusalList.tsx`
      — ⭐ **HYPOTHESIS (b)'s evidence: both 214-10 commits are dated 2026-08-28, the SAME DAY as the
      drive.** Confirm at execute time and put the dates in the summary either way.
  </read_first>
  <behavior>
    - Rendering the gauntlet with a verdict whose `named_failures` is exactly the `<interfaces>` entry produces the COMPOSED sentence from `publishRefusalVocabulary`, and the server's `message` string appears NOWHERE in the rendered output.
    - The same render with `step_name` removed produces the generic renderer and the raw diagnostic — the honest fall-through, asserted so the two arms are told apart by evidence.
    - The same render with `argument` removed does likewise.
    - The composed sentence is present in FULL: its final clause is in the DOM, not merely its opening.
    - The `data-testid="refusal-next"` control's `disabled` state is asserted, whatever it is, so the finding is recorded rather than noticed.
  </behavior>
  <action>
**Diagnose first, and write nothing until you have.** Add a case to `PublishGauntlet.test.tsx` that
renders the gauntlet with a verdict carrying the `<interfaces>` entry BYTE-FOR-BYTE — the real live
shape, not a convenient one — and READ WHAT COMES OUT. Assert both halves: the composed sentence IS
present in full, and the server's `message` string is ABSENT from `container.textContent`.

Then branch on what you measured, and take exactly ONE arm:

  - **(a) The composed sentence does NOT render.** A real classification or mount defect. Fix it at
    the arm the test identified — a declining predicate arm, or the mount condition — and pin the
    live shape so it cannot regress. ⚠ Widen `isArgumentRefusal` only if the payload it is declining
    is one it can phrase HONESTLY; its docblock's rule stands, and an entry whose sentence would
    contain empty curly quotes must keep falling through.
  - **(b) It renders in full, and the git dates put both 214-10 commits on the drive date.** The
    bug is a stale bundle. ⭐ **Say so plainly, close `BUG-260828-04` as not-reproducible with the
    evidence, and DO NOT invent work.** The two added cases are the regression fence and are the
    whole deliverable for this arm.
  - **(c) It renders in full AND the diagnostic the operator quoted is `ProblemsTray`'s.** Record
    that the raw diagnostic is that surface's DESIGNED behaviour, and re-scope the bug rather than
    changing the tray: composing there is a second refusal vocabulary on a live-validation surface
    fed by a different route, which is a phase and not a fix.

⚠ **THE ONE THING THAT IS FORBIDDEN IN EVERY ARM:** shipping a CSS clamp change. There is no clamp on
this path at HEAD, so such a change would defend nothing while making the bug look answered — which
is exactly what D-214.1-04 exists to prevent.

Whatever the arm, add the two DECLINE cases from `<behavior>` to `PublishRefusalList.test.tsx`: they
prove the generic fall-through is reached for a payload the surface cannot phrase, which is the
mechanism that produced the observed symptom if (a) is true and the fence against it if (a) is false.

**Report the `onGoToStep` finding.** The gauntlet mounts `PublishRefusalList` without it, so the
next-action control is disabled. SC#3 says a refusal *names its next action* — the words are there
and the control is not live. Assert the current state, state in the summary whether it was fixed,
and if it was not, name the re-open trigger. ⛔ Do not wire a navigation seam that does not already
exist in this file; that is a second concern on a file whose ledger row already fires.
  </action>
  <acceptance_criteria>
    - `test -f frontend/src/components/workflows/PublishGauntlet.test.tsx && test -f frontend/src/components/workflows/PublishRefusalList.test.tsx` exits 0 before the vitest command is trusted.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/PublishGauntlet.test.tsx src/components/workflows/PublishRefusalList.test.tsx src/components/workflows/publishRefusalVocabulary.test.ts` exits 0, with `PublishGauntlet.test.tsx` >= 46 and `PublishRefusalList.test.tsx` >= 22 (their pins; neither may DECREASE).
    - ⭐ The diagnosis case asserts BOTH: the composed sentence's FINAL clause is in `container.textContent`, AND the server's `message` string is not. One without the other does not distinguish the hypotheses.
    - The two decline cases (no `step_name`; no `argument`) each assert the generic renderer WAS reached — a POSITIVE CONTROL for the fall-through mechanism, so the absence assertion above is not free.
    - The summary states which arm was taken, quotes the rendered text that decided it, and quotes the `git log --date=short` output for `PublishRefusalList.tsx` and `publishRefusalEntry.ts`.
    - `git diff -- frontend/src/components/workflows/PublishGauntlet.tsx | grep -cE "truncate|line-clamp|text-ellipsis|whitespace-nowrap"` is **0** — no clamp was added OR removed, in any arm.
    - A criterion reading source strips comments FIRST and asserts the stripper removed something; `PublishRefusalList.tsx`'s docblock names the forbidden strings out loud and a bare grep over it self-invalidates.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — no more than 34 errors.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; test -f src/components/workflows/PublishGauntlet.test.tsx &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/PublishGauntlet.test.tsx src/components/workflows/PublishRefusalList.test.tsx</automated>
  </verify>
  <done>The real live payload was rendered and read, one of three hypotheses is named with the evidence that chose it, the fix matches what was found, and no clamp was shipped against a clamp that does not exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The agent stops inventing a capability refusal — a fact, handed over, not asked for</name>
  <files>backend/app/services/harness/phase_types.py, backend/tests/unit/test_2141_no_false_capability_refusal.py</files>
  <read_first>
    - `backend/app/services/harness/phase_types.py:242-267` — ⭐ **`_stateful_framing_block(ctx)`, the
      EXACT shape to mirror**: pure, reads `getattr(ctx, "definition", None)`, returns `""` when
      inapplicable, and is CONCATENATED into the system prompt beside its siblings.
    - `backend/app/services/harness/phase_types.py:705-712` (`llm_single`), `:781-787` (`llm_agent`)
      and `:1385` (`llm_emit`) — the THREE composition sites. ⚠ **`llm_emit` at `:1385` composes only
      `_skill_block` + `_retry_suffix` today**, so it is the one a reader is most likely to miss. The
      bug's own `write-summary` phase is exactly this class of step.
    - `backend/app/services/harness/phase_types.py:1843-1851` — `_INTENT_TEXT_MAX_CHARS`, this file's
      OWN clamp precedent for unbounded text.
    - `backend/app/services/harness/publish_service.py:889, 973` — `_LABEL_MAX_CHARS = 72` and
      `_clean_label`, the shipped scrubber for author- and model-authored strings. ⭐ **Prefer
      importing it — one scrubber, two callers.** If that creates an import cycle, use this file's own
      clamp precedent instead and RECORD the cycle as the reason; do not leave the choice unexplained.
    - `backend/app/services/workflow_authoring.py:334-360` — ⭐ **`_VOCABULARY_ALLOWED_FRAME` /
      `_VOCABULARY_FORBIDDEN_FRAME`, the shipped precedent for exactly this move one surface over**:
      the connected set is rendered INTO the prompt as a fact rather than left to the model. Its
      length-discipline comment binds here too — *an over-long block is a NUDGE* — and its figures are
      RE-DERIVED by a test rather than hand-counted.
    - `.planning/reported-bugs/BUG-260828-03-agent-claims-no-email-service-while-connection-exists.md`
      — the false claim reached a RECIPIENT and sat inside the approval pause.
  </read_first>
  <behavior>
    - `_wired_services_block(ctx)` returns `""` for a definition with no `external_action` phase.
    - It returns `""` for `ctx` with no `definition`, and for a `definition` whose `phases` is not a list.
    - For a definition with one `external_action` phase bound to `send_email`, the returned string names `send_email` and states that this workflow IS wired to a service that performs it.
    - It names NO `connection_id`, no URL, no host and no credential — asserted over the returned string.
    - A model-authored `tool_name` longer than the clamp is truncated; control characters and format characters are scrubbed.
    - Two phases naming the same tool produce ONE mention.
    - The block is present in the composed system prompt for `llm_single`, `llm_agent` and `llm_emit` when it is non-empty, and the composed prompt is BYTE-IDENTICAL to today's when it is empty.
  </behavior>
  <action>
Add `_wired_services_block(ctx)` beside `_stateful_framing_block`, and append it at all THREE
composition sites. It reads ONLY `ctx.definition` — the object the run already holds — and derives
the set of `tool_name` values across the definition's `external_action` phases.

⛔ **NO CONNECTION READ.** No `connection_id` in the prompt, no lookup, no I/O, no new trust
boundary, and therefore no `run_in_threadpool` question. ⚠ **State the LIMIT this buys in the
docblock, because it is narrower than "the workspace's connections":** the block tells the model what
THIS WORKFLOW is wired to, which is precisely the point at which it speculated — it denied a
capability its own next step then exercised successfully. **RE-OPEN TRIGGER**: the first phase that
needs the model to know about a service NO step names must add the connection read, and that read is
I/O and belongs at a seam this function is deliberately not at.

The block's content is two things and nothing else: the FACT (this workflow is wired to a service
that performs `<tool names>`) and the RULE (do not state that a service is unavailable or
unconnected; if you cannot do something, say what YOU could not do — never what the system lacks).
⛔ **No post-hoc censor over generated text.** D-214.1-05's scope limit is binding: that is a bigger
and more dangerous mechanism and it needs the operator.

⚠ **`tool_name` is a REMOTE-SERVER-ADVERTISED string on the MCP path and it is going into a system
prompt.** That is prompt injection surface the roadmap names as a class this tree has never faced.
Scrub and clamp every name before it is interpolated, and assert both.

Create `backend/tests/unit/test_2141_no_false_capability_refusal.py` with a case per `<behavior>`
bullet, plus:
  - ⭐ **THE BYTE-IDENTITY CONTROL, which is what makes the empty-string arm mean anything.** For a
    definition with no `external_action` phase, assert the composed system prompt at each of the
    three sites is byte-identical to the prompt composed WITHOUT this change. ⚠ Pair it with a
    POSITIVE CONTROL asserting the prompt DOES change for a definition that has one — otherwise the
    identity assertion passes against a block that never composes at all.
  - A length case that RE-DERIVES the block's character count FROM THE LITERAL and asserts a bound
    against the sibling blocks, exactly as `test_214_describe_vocabulary.py` does. ⚠ This file's own
    history records a hand-counted constant that was wrong within minutes of being written.
  - An injection case: a `tool_name` carrying newlines, control characters and an instruction-shaped
    sentence is scrubbed and clamped, asserted on the returned string.
  </action>
  <acceptance_criteria>
    - `test -f backend/tests/unit/test_2141_no_false_capability_refusal.py` exits 0 before pytest is trusted.
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_2141_no_false_capability_refusal.py -q` exits 0 with >= 9 cases.
    - ⭐ The byte-identity case covers ALL THREE composition sites and is paired with a positive control that the prompt DOES change when an `external_action` phase is present.
    - A case asserts the returned string contains no UUID-shaped substring, no `http`, and no `@` — the id/host/credential fence, asserted over the VALUE and not over the source.
    - `grep -c "_wired_services_block" backend/app/services/harness/phase_types.py` is exactly **4** — one definition and three call sites — measured on comment-stripped source with a stripper-did-something control.
    - The length case RE-DERIVES the figure from the literal; no hand-counted constant appears in a comment without a test that re-measures it.
    - `git diff -- backend/app/services/harness/phase_types.py | grep -cE "await |fetch|pool|supabase|connector_service"` is **0** — no I/O entered a pure block.
    - `cd backend && venv/Scripts/python -m pytest tests/unit -q` — no more than **68** failed.
  </acceptance_criteria>
  <verify>
    <automated>cd backend &amp;&amp; test -f tests/unit/test_2141_no_false_capability_refusal.py &amp;&amp; venv/Scripts/python -m pytest tests/unit/test_2141_no_false_capability_refusal.py -q</automated>
  </verify>
  <done>Every LLM step in a workflow wired to an external service is told what that workflow can do and told not to speculate about system state, with no new I/O, no id in a prompt, and every other workflow's prompt byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a remote MCP server's advertised `tool_name` -> a system prompt | ⚠ **NEW, and it is the prompt-injection class the roadmap names as one this tree has never faced.** A server we do not control chooses a string that reaches the model as instruction-adjacent text. |
| a publish verdict's `named_failures` -> the author's screen | A backend diagnostic on an author's screen is the whole of what `BUG-260815-06` is about. |
| a model's prose about system state -> an approval pause -> a delivered email | The false claim in `BUG-260828-03` reached a recipient AND sat inside the human gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-214.1-03-01 | Tampering | a hostile `tool_name` steering the model through the awareness block | mitigate | Every name is SCRUBBED and CLAMPED before interpolation, preferring the shipped `_clean_label` over a second copy. Tested by the injection case asserting newlines, control characters and an instruction-shaped sentence do not survive. ⚠ A clamp is a bound, not immunity; the block states a FACT and a RULE and grants nothing. |
| T-214.1-03-02 | Information Disclosure | a connection id, host or credential reaching a prompt | mitigate | The block reads only `tool_name` off the definition. Tested by a VALUE fence asserting no UUID-shaped substring, no `http` and no `@` in the returned string. |
| T-214.1-03-03 | Repudiation | a refusal an author cannot read, so nothing is fixable | mitigate | Task 1's diagnosis case asserts the composed sentence renders IN FULL and the diagnostic does not, on the byte-exact live payload. The two decline cases prove the fall-through path that produces the symptom. |
| T-214.1-03-04 | Spoofing | a fix that looks like a fix | mitigate | ⛔ A CSS clamp change is forbidden in every arm, and the diff criterion enforces it. There is no clamp on this path at HEAD, so removing one would defend nothing while closing the bug on paper. |
| T-214.1-03-05 | Elevation of Privilege | the awareness block widening into a censor over generated text | mitigate | Structurally excluded: the block is a pure string appended to a system prompt and touches no output path. D-214.1-05's scope limit is quoted in its docblock, and a censor needs the operator. |
| T-214.1-03-06 | Denial of Service | the block changing every other workflow's prompt | mitigate | It returns `""` for a definition with no `external_action` phase, proved by the three-site byte-identity case WITH its positive control. |
| T-214.1-03-SC | Tampering | package installs | mitigate | This plan installs nothing. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/unit -q` — no more than **68** failed.
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — no more than **34** errors.
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/PublishGauntlet.test.tsx src/components/workflows/PublishRefusalList.test.tsx src/components/workflows/publishRefusalVocabulary.test.ts` — green, paths confirmed with `test -f` first, per-file counts recorded and none decreased.
- ⚠ This plan edits NO registry, so it does not own a count-gate verdict. Record the per-file deltas for plan `214.1-02` Task 3.
</verification>

<how_wed_know_this_failed>
1. **`BUG-260828-04` is closed by a fix that matches no measurement.** A CSS clamp shipped against
   hypothesis (b) leaves a real classification defect live while looking fixed — D-214.1-04 by name,
   and the diff criterion is the mechanical guard.
2. **The diagnosis case uses a convenient payload rather than the live one.** A payload with a
   different `code`, a different `step_name` or a missing key proves something about a shape that
   never occurred. The `<interfaces>` entry is byte-exact for that reason.
3. **The bug is closed as not-reproducible when it was reproducible on a third surface nobody
   checked.** `ProblemsTray` renders the diagnostic verbatim BY DESIGN, and if that is the screen
   the operator read, both (a) and (b) are wrong answers. Naming it is the guard.
4. **The awareness block changes prompts it was never meant to touch.** A workflow with no external
   step whose prompt moved by one character has altered every non-external run in the product. The
   byte-identity case across all three sites, with its positive control, is the fence.
5. **The block grants what it was meant to bound.** A hostile MCP `tool_name` reaching the prompt
   unscrubbed turns an honesty fix into an injection vector. ⚠ **This is the only NEW trust boundary
   this phase opens**, and it is opened on the run path where a step can send.
6. **The model still says it.** `BUG-260828-03` is answered by a lookup, and a prompt is not a
   guarantee — this file's neighbour records that in writing. ⚠ **This plan buys AWARENESS, not
   impossibility**, and the summary must say so rather than claiming the class is closed.
</how_wed_know_this_failed>

<g5_disposition>
Re-derived from git 2026-08-28 (six-digit quick-task buckets NOT subtracted — re-derive at execute
time and hand the corrected figures to plan `214.1-02` Task 3, which owns the ledger commit):

| File | `CLAUDE.md` cell | **Measured** | Verdict |
|---|---|---|---|
| `backend/app/services/harness/phase_types.py` | 47 / 21 / 2664 | **50 / 23 / 2809** | ⚠ **STALE — FIRES** |
| `frontend/src/components/workflows/PublishGauntlet.tsx` | 14 / 7 / 1025 | **16 / 8 / 1245** | ⚠ **STALE — FIRES** |
| `frontend/src/components/workflows/PublishRefusalList.tsx` | *(NO ROW)* | **2 / 1 / 178** | young — row owed |
| `frontend/src/components/workflows/publishRefusalEntry.ts` | *(NO ROW)* | **1 / 1 / 87** | young — row owed |

**`phase_types.py` — honoured by construction, argued:** the file already owns the
prompt-block-composition concern with four shipped siblings (`_skill_block`,
`_stateful_framing_block`, `_citation_instruction`, `_retry_suffix`). This plan adds a FIFTH of
exactly that shape — pure, `ctx`-reading, empty-string-returning — plus three one-term
concatenations. No new executor, no new phase type, no new I/O, and its ledger cell's own note
(*the G-5 obligation is now OWED, not honoured*) is **not discharged here** and must not read as if it
were. ⚠ Its named seam is untouched.

**`PublishGauntlet.tsx` — honoured by construction, and the argument is weaker in arm (a) so it is
stated plainly:** in arms (b) and (c) the diff is TEST-ONLY and this file is not modified at all. In
arm (a) it gains at most a corrected mount condition or a corrected predicate arm — no new stage, no
new `blocked_stage`, no new renderer. Its ledger row reads *extraction due* and this plan does not
take it; the seam its sibling names (the BLOCK REGION AS A WHOLE — verdict + cause + spine) stays
owed.

**Two files on this plan's critical path have NO ledger row at all** — `PublishRefusalList.tsx` and
`publishRefusalEntry.ts`, both created 2026-08-28 by 214-10. They are young, so G-5 does not fire;
they are named here so the scan list gains them BEFORE they are hot rather than at their third phase.
⛔ This plan does not write the rows — plan `214.1-02` Task 3 does, in one commit, with the sections
beside them.
</g5_disposition>

<success_criteria>
- The real live refusal payload was rendered in a test and READ, and one of three named hypotheses was chosen by that evidence rather than by the bug report's framing.
- The fix matches the diagnosis, and no CSS clamp was shipped against a clamp that does not exist at HEAD.
- Both decline arms of `isArgumentRefusal` are pinned with the generic fall-through asserted, so the mechanism that produces the symptom cannot regress silently.
- The disabled next-action control is reported with a verdict, not left as something a reader might notice.
- Every LLM step in a workflow wired to an external service is handed the fact and the rule, at all three composition sites including `llm_emit`.
- Every other workflow's system prompt is byte-identical, proved at all three sites with a positive control.
- No connection id, host or credential appears in any prompt, and a remote-server-advertised tool name is scrubbed and clamped before it is interpolated.
- No registry, ledger or `CLAUDE.md` file was touched by this plan.
</success_criteria>

<output>
Create `.planning/phases/214.1-an-author-can-declare-an-input/214.1-03-SUMMARY.md` when done.
Record: (1) WHICH of the three hypotheses the diagnosis chose, the rendered text that decided it, and
the `git log --date=short` output for both 214-10 files; (2) the verdict on the disabled
`onGoToStep` control and, if unfixed, its re-open trigger; (3) whether `_clean_label` was imported or
a local clamp used, with the import-cycle evidence either way; (4) the re-derived G-5 triples for all
four files above, with quick-task buckets named and subtracted, **for plan `214.1-02` Task 3 to
transcribe**; (5) the per-file vitest deltas, since this plan owns no gate verdict.
</output>
