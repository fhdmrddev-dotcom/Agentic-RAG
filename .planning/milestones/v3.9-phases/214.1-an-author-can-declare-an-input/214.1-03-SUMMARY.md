---
phase: 214.1-an-author-can-declare-an-input
plan: 03
subsystem: publish-refusal + harness prompt composition
tags: [BUG-260828-04, BUG-260828-03, STEP-03, D-214.1-04, D-214.1-05, prompt-injection]
requires:
  - "214-10's PublishRefusalList / publishRefusalEntry / publishRefusalVocabulary"
  - "publish_service._clean_label — the shipped scrubber"
provides:
  - "a DRIVEN diagnosis of BUG-260828-04 with the publish surface measured innocent"
  - "_wired_services_block — the connected-service fact handed to the model at 4 sites"
affects:
  - "every LLM step of a workflow that binds an external action"
tech-stack:
  added: []
  patterns:
    - "AST-based composition-site assertion instead of grep (the 187-24 trap)"
    - "byte-identity proved as a corollary of an exact '' arm, not sampled"
key-files:
  created:
    - backend/tests/unit/test_2141_no_false_capability_refusal.py
  modified:
    - backend/app/services/harness/phase_types.py
    - backend/tests/unit/test_190_connector_source_fence.py
    - frontend/src/components/workflows/PublishGauntlet.test.tsx
    - frontend/src/components/workflows/PublishRefusalList.test.tsx
    - .planning/reported-bugs/BUG-260828-04-publish-refusal-truncated-on-screen.md
decisions:
  - "BUG-260828-04 arm (c): the publish path is innocent; the raw diagnostic the operator read is ProblemsTray's DESIGNED verbatim render. Re-scoped, tray unchanged."
  - "The onGoToStep control stays unmounted and therefore disabled — pinned, reported, NOT fixed; a navigation seam is a second concern on a G-5-firing file."
  - "_clean_label IMPORTED rather than copied — no import cycle exists, proven by a real fresh import."
metrics:
  duration: ~40 min
  completed: 2026-08-28
---

# Phase 214.1 Plan 03: A refusal that was never clipped, and an agent that stops guessing — Summary

Measured that `BUG-260828-04`'s publish surface renders the composed sentence in full and the backend
diagnostic nowhere, re-scoped the bug to the surface that renders that diagnostic by design, and gave
every LLM step of an externally-bound workflow the connected-service fact it was previously inventing.

## Task 1 — `BUG-260828-04` DIAGNOSED (commit `831a667ac`)

### Which hypothesis, and the evidence that chose it

**(a) a classification or mount defect — REFUTED BY MEASUREMENT.** The byte-exact live entry
(`code="ask_undeclared"`, `phase="act"`, `step_name="act"`, `argument="to"`, `upstream=null`, with the
server's diagnostic present on the entry) was rendered through the real `PublishGauntlet`. **Five
cases, green on the first run.** The rendered text that decided it:

- **PRESENT:** `“act” asks for “to” when it runs, but nothing asks for it.` — asserted whole, and its
  final clause (`but nothing asks for it.`, **derived from the vocabulary, never re-typed**) asserted
  separately, because an opening that rendered proves nothing about a clipping report.
- **ABSENT, counted rather than inferred:** the full server string, the operator's quoted fragment
  `but the wor`, the phrase `declares no matching input`, and the machine code `ask_undeclared`.

**(b) a stale bundle — LIVE, and now unfalsifiable.** `git log --date=short`, both files:

```
PublishRefusalList.tsx
3d508f28f 2026-08-28 feat(214-10): the cause above the spine, and the spine's state in words
0af16b150 2026-08-28 feat(214-10): the publish refusal is five sentences, not one

publishRefusalEntry.ts
3d508f28f 2026-08-28 feat(214-10): the cause above the spine, and the spine's state in words
```

Both 214-10 commits landed on the drive date. Named rather than dismissed — but nobody can now observe
the browser cache state, so it cannot be settled.

**⭐ (c) — THE ANSWER, and it is reproducible BY CONSTRUCTION rather than guessed.** `_gap_message`'s
diagnostic rides on the `LintError` that `lint_workflow` emits, and `lint_workflow` has **two**
callers: `publish_service` AND `POST /workflows/validate`. The `act` step bound the **native**
`send_email` capability — the arm `/validate` *does* evaluate (only the MCP arm is skipped there, for
want of a `tool_schemas` map). `api/workflows.py` classifies the five argument-gap codes as `error`,
and `ProblemsTray.tsx:24-26` states in its own docblock that it renders the server's `message`
**VERBATIM BY DESIGN** — already pinned by its own suite (`ProblemsTray.test.tsx:180`). So that exact
string is *supposed* to appear on the builder canvas, on every keystroke, and it is correct there.

**Re-scoped, not fixed.** Composing author-facing sentences on the live-validation surface would be a
second refusal vocabulary on a different route — a phase, not a fix. The tray was not changed.
`BUG-260828-04` is now `status: folded` / `folded_into: 214.1` with both re-open triggers written in.

### ⛔ No CSS clamp shipped, because there is none

`git diff -- PublishGauntlet.tsx | grep -cE "truncate|line-clamp|text-ellipsis|whitespace-nowrap"` →
**0**. Stronger: **no component source was modified at all in Task 1** — the diff is test-only plus the
bug record. `PublishGauntlet.tsx`'s only `.slice(` is `goldenRunId.slice(0, 8)`; `ProblemsTray.tsx`
matches none of the five needles either.

### The verdict on the disabled `onGoToStep`

`PublishGauntlet.tsx` mounts `PublishRefusalList` with `entries` + `totalSteps` and **no
`onGoToStep`**, so every `refusal-next` control renders `disabled`. SC#3 says a refusal names its next
action; the words are on screen and the control is inert. `RefusalItem` keeps it from being a *dead*
affordance by stating where the fix lives in the control's `title` — the shipped `RunLink` precedent.

**NOT FIXED, deliberately.** The gauntlet holds no canvas-navigation seam; wiring one is a second
concern on a file whose ledger row already fires. The current state is **pinned by a test** so it is
recorded rather than noticed. **Re-open trigger:** the next phase that touches `PublishGauntlet.tsx`
for any reason, or the first phase that gives the publish modal a way to reach a step on the canvas.
Written into the bug record too, so it is visible to the register sweep and not only here.

## Task 2 — the false capability claim (commit `7cda1b12a`)

`_wired_services_block(ctx)` sits beside `_stateful_framing_block` and is its exact shape: pure,
reads only `ctx.definition`, returns `""` when inapplicable. It states a **FACT** (this workflow is
wired to connected services that perform *X*) and a **RULE** (do not claim a service is unavailable or
unconnected; say what YOU could not do, never what the system lacks). **No connection read, no I/O, no
`connection_id`, no new trust boundary, and no post-hoc censor** — D-214.1-05's scope limit is quoted
in the docblock.

**`_clean_label` was IMPORTED, not copied.** No cycle exists and it was proven rather than reasoned:
`publish_service` has no app-level module-top imports at all (`logging`, `unicodedata`, `UUID` —
everything heavier is function-local), and the `harness` package `__init__` never loads it. A real
fresh `import app.services.harness.phase_types` was run before the line was written.

**13 cases**, and two of them were **driven RED against planted defects**, with
`phase_types.py` restored **md5-identical** (`dda1b20f…`) after each:
- removing one call site → `AssertionError: the prompt composed at phase_types.py:1496 does not append
  the awareness block` — the guard names the exact line.
- bypassing `_clean_label` → the injection case and the AST no-IO case both fired.

## Deviations from Plan

### **1. [Rule 2 — missing critical functionality] FOUR composition sites, not three**

- **Found during:** Task 2, by the AST assertion itself.
- **Issue:** the plan named `llm_single`, `llm_agent` and `llm_emit`. **`llm_batch_agents` composes
  the identical framing into `base_prompt`**, and the module's own comment eight lines above it says
  why a single-agent-only fix is wrong: a batch step carries the same `available_tools`, so every
  parallel branch would have stayed free to invent the same false refusal.
- **Fix:** wired all four; the test asserts `len(sites) == 4` over the AST.
- **Consequence for the plan's criterion:** `grep -c "_wired_services_block"` on comment-stripped
  source is **5**, not the specified 4 (1 definition + 4 sites). Stripper-did-something control:
  removed **105,379** chars.

### **2. [Rule 1 — bug] BOTH binding arms are read, not `tool_name` alone**

- **Found during:** Task 2, reading `models/harness.py:355,410`.
- **Issue:** an `external_action` step names EITHER a native `capability` (closed `Literal` of three)
  OR an MCP `tool_name`. **`BUG-260828-03`'s own SMTP step is the NATIVE `send_email` arm** — so the
  plan's stated interface would have returned `""` for the exact workflow the bug was reported
  against. A fix that fixes nothing.
- **Fix:** `tool_name or capability`, both through `_clean_label`.

### **3. [Rule 3 — blocking] `test_190_connector_source_fence.py` line number re-derived 107 → 114**

- **Issue:** the fence pins the module-scope registry-importer list *with line numbers*; my seven
  added import lines moved it. **The IMPORTER SET is unchanged — still exactly one, still
  `phase_types`** — which is the property the fence defends.
- **Fix:** number re-derived and the reason recorded in the same shape the fence's own two prior
  re-derivations use. The invariant was not weakened. Green: 8 passed.

### **4. [scope] `BUG-260828-04` record edited, though not in `files_modified`**

Arm (c)'s whole deliverable is a routing decision, and `status:` frontmatter **is** the index — prose
elsewhere is invisible to the register sweep. Recorded here rather than left implicit.

## Verification

| Check | Required | Measured |
|---|---|---|
| `backend/tests/unit` | ≤ 68 failed | **67 failed / 3069 passed** |
| failing-set delta vs measured baseline | none new | **exactly identical** (`comm` both directions empty) |
| new backend suite | ≥ 9 cases, exit 0 | **13 passed** |
| `tsc --noEmit -p tsconfig.app.json` | ≤ 34 | **34** |
| scoped vitest (3 files) | green | **149 passed / 3 files** |
| clamp needles in the gauntlet diff | 0 | **0** |
| I/O needles in added **code** (comments stripped) | 0 | **0** — the 1 raw hit is my own docblock saying *"no `run_in_threadpool` question"`*, the 187-24 trap; the authoritative check is the runtime AST case |

⚠ **The plan's backend baseline of `68 failed / 3055 passed` is slightly stale.** Re-measured on this
tree at the unmodified base: **67 failed / 3056 passed**. Published so the next plan compares against a
figure someone measured.

### Per-file vitest deltas — for plan `214.1-02` Task 3, this plan owns no gate verdict

| File | pin in `vitest-count-gate.cjs` | before | after | Δ |
|---|---|---|---|---|
| `PublishGauntlet.test.tsx` | 46 | 78 | **83** | +5 |
| `PublishRefusalList.test.tsx` | 22 | 22 | **25** | +3 |
| `publishRefusalVocabulary.test.ts` | 41 | 41 | **41** | 0 |

⚠ **`PublishGauntlet.test.tsx`'s PIN IS STALE BY 32 AND WAS ALREADY STALE BEFORE THIS PLAN** — it reads
46 against a file that ran 78 at the base commit. A pin that low guards almost nothing. `214.1-02`
should re-baseline it to **83**, not merely bump it. `scripts/vitest-count-gate.cjs` was **not touched**
by this plan.

### Re-derived G-5 triples — for `214.1-02` Task 3 to transcribe

Buckets scanned for six-digit dated quick tasks: **none present on any of the four files**, so nothing
was subtracted. Non-phase buckets excluded and named.

| File | `CLAUDE.md` cell | **measured 2026-08-28** | Verdict |
|---|---|---|---|
| `backend/app/services/harness/phase_types.py` | 50 / 23 / 2809 | **51 / 24 / 2925** | ⚠ **FIRES** — the 24th bucket is `214.1`, added by this plan. Non-phase buckets excluded: `ask_user`, `BUG`, `harness` |
| `frontend/src/components/workflows/PublishGauntlet.tsx` | 16 / 8 / 1245 | **16 / 8 / 1245** | ⚠ **FIRES** — **unchanged, because this plan modified only its TEST**. Non-phase bucket excluded: `publish` |
| `frontend/src/components/workflows/PublishRefusalList.tsx` | 2 / 1 / 178 | **2 / 1 / 178** | no (1 phase) — source untouched |
| `frontend/src/components/workflows/publishRefusalEntry.ts` | 1 / 1 / 87 | **1 / 1 / 87** | no (1 phase) — source untouched |

**`phase_types.py` — honoured by construction, and its OWED obligation is NOT discharged.** The file
already owns the prompt-block-composition concern with four shipped siblings; this adds a fifth of
exactly that shape plus four one-term concatenations. No new executor, no new phase type, no new I/O.
⚠ Its ledger cell's note — *the G-5 obligation is now OWED, not honoured* — **still stands**, and its
named seam is untouched.

**`PublishGauntlet.tsx` — the extraction its row calls for is still OWED.** This plan took the
test-only arm, so the file is byte-unchanged; the seam its sibling names (the BLOCK REGION AS A WHOLE
— verdict + cause + spine) stays owed.

## Known observations, recorded rather than claimed innocent

- **`test_email_ingestion.py::test_ingest_email_populates_metadata_and_attachments` flaked once**
  during Task 2's investigation — it failed in the full run and once in isolation, then passed on
  three consecutive runs (isolated ×2, whole-file ×1, full-suite ×1) with byte-identical files.
  `git diff --numstat` proves it is **provably unmodified** by this plan, and the final failing set
  matches the baseline exactly. **One green sample is not proof of innocence** — this is an
  observation, and the file is a candidate for SEED-171's register if it recurs.
- **⚠ This plan buys AWARENESS, not impossibility.** `BUG-260828-03` is answered by giving the model a
  fact instead of an occasion to guess, at the point it guessed. A prompt block is not a guarantee,
  and this module's neighbour records that in writing. The false-claim class is **narrowed, not
  closed**; the block also knows nothing about a service **no step names** — that needs a connection
  read, which is I/O at a seam this function is deliberately not at.

## Files NOT touched, as required

`scripts/vitest-count-gate.cjs` · `CLAUDE.md` · `docs/HOT-FILE-LEDGER.md` · `STATE.md` · `ROADMAP.md`
· `ProblemsTray.tsx` · `PublishGauntlet.tsx` · `PublishRefusalList.tsx` · `publishRefusalEntry.ts`

## Self-Check: PASSED

- `backend/tests/unit/test_2141_no_false_capability_refusal.py` — FOUND
- `.planning/phases/214.1-an-author-can-declare-an-input/214.1-03-SUMMARY.md` — FOUND
- commit `831a667ac` (Task 1) — FOUND
- commit `7cda1b12a` (Task 2) — FOUND
- both commits sit directly on the dispatched base `1b31f4786` — CONFIRMED
- `git diff --diff-filter=D` on each commit — no deletions
