---
phase: 224-what-the-agent-is-doing-reads-like-a-sentence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/toolNames.ts
  - frontend/src/components/panel/SeamCard.tsx
  - frontend/src/components/panel/__tests__/Seam.test.tsx
  - frontend/src/components/chat/MessageItem.tsx
  - frontend/src/components/chat/ToolCallPanel.tsx
autonomous: true
requirements:
  - SEED-240
user_setup: []

must_haves:
  truths:
    - "`@/lib/toolNames.ts` exports `toolName` and `TOOL_PHRASES` with safe `own()` lookup without cross-domain leakage"
    - "`SeamCard` has deleted `write_todos` and `workspace_write` arms (Winner D); `ask_user` is preserved"
    - "No raw machine identifiers (`WRITE_TODOS`, `WORKSPACE_WRITE`) reach the user; human phrases are used"
    - "`Seam.test.tsx` passes with the 2 deleted arm tests pruned"
  artifacts:
    - frontend/src/lib/toolNames.ts
    - frontend/src/components/panel/SeamCard.tsx
    - frontend/src/components/panel/__tests__/Seam.test.tsx
  key_links:
    - "SeamCard uses toolName for header copy without CSS uppercase"
---

<objective>
Eliminate raw machine identifiers and prune duplicate transcript cards:
1. Create `frontend/src/lib/toolNames.ts` re-exporting `toolName` and `TOOL_PHRASES` from `components/workflows/toolNames.ts` so Chat and Workflows share the dictionary without cross-domain boundaries.
2. Implement Winner D in `SeamCard.tsx`: remove `write_todos` and `workspace_write` arms, keeping `ask_user` intact. Update header label to use `toolName(kind)` without uppercase styling.
3. Prune tests for deleted arms in `frontend/src/components/panel/__tests__/Seam.test.tsx`.
4. Import `toolName` in `ToolCallPanel.tsx` and `MessageItem.tsx` so tool calls read in human language.

Output: Shared `toolNames.ts`, clean `SeamCard.tsx`, updated `Seam.test.tsx`, updated `ToolCallPanel.tsx` and `MessageItem.tsx`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/224-what-the-agent-is-doing-reads-like-a-sentence/224-CONTEXT.md
@.planning/phases/224-what-the-agent-is-doing-reads-like-a-sentence/224-PREFLIGHT.md
@frontend/src/components/workflows/toolNames.ts
@frontend/src/components/panel/SeamCard.tsx
@frontend/src/components/panel/__tests__/Seam.test.tsx
@frontend/src/components/chat/MessageItem.tsx
@frontend/src/components/chat/ToolCallPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared toolNames module and wire into Chat components</name>
  <files>frontend/src/lib/toolNames.ts, frontend/src/components/chat/ToolCallPanel.tsx, frontend/src/components/chat/MessageItem.tsx</files>
  <read_first>frontend/src/components/workflows/toolNames.ts, frontend/src/components/chat/ToolCallPanel.tsx, frontend/src/components/chat/MessageItem.tsx</read_first>
  <action>
    1. Create `frontend/src/lib/toolNames.ts`:
       Re-export `toolName` and `TOOL_PHRASES` from `@/components/workflows/toolNames`:
       ```typescript
       export { toolName, TOOL_PHRASES } from "@/components/workflows/toolNames"
       ```
    2. In `frontend/src/components/chat/ToolCallPanel.tsx`:
       Import `toolName` from `@/lib/toolNames`.
       Wherever tool name strings are displayed (e.g. `call.tool`, `call.name`), format with `toolName(tool)` instead of raw string or generic uppercase.
    3. In `frontend/src/components/chat/MessageItem.tsx`:
       Import `toolName` from `@/lib/toolNames` where tool active labels or names are rendered.
  </action>
  <verify>
    Run `npx vitest run src/components/workflows/__tests__/toolNames.test.ts` from `frontend/` to ensure toolNames contract holds.
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/toolNames.ts` exists and exports `toolName` and `TOOL_PHRASES`.
    - `ToolCallPanel.tsx` uses `toolName` for displaying human phrases.
  </acceptance_criteria>
  <done>Shared toolNames module created and wired into Chat components.</done>
</task>

<task type="auto">
  <name>Task 2: Delete write_todos and workspace_write arms from SeamCard (Winner D)</name>
  <files>frontend/src/components/panel/SeamCard.tsx, frontend/src/components/panel/__tests__/Seam.test.tsx</files>
  <read_first>frontend/src/components/panel/SeamCard.tsx, frontend/src/components/panel/__tests__/Seam.test.tsx</read_first>
  <action>
    1. In `frontend/src/components/panel/SeamCard.tsx`:
       - Remove `workspace_write` and `write_todos` blocks from the card body.
       - Clean up `SeamCardPayload` to keep `question?: string`, `answer?: string` (cleanly omitting dead `path`, `version`, `todoTotal`, `todoDone`).
       - Remove CSS `uppercase tracking-wider` on the header label and use `toolName(kind)` for `headLabel`:
         `function headLabel(kind: SeamKind): string { return toolName(kind) }`
       - Ensure `ask_user` renders cleanly: question + "You answered <value>".
    2. In `frontend/src/components/panel/__tests__/Seam.test.tsx`:
       - Remove the two tests targeting the deleted arms:
         `it("reload SeamCard for workspace_write renders a file chip + version", ...)`
         `it("reload SeamCard for write_todos renders the final-state note '☑ N todos · all done'", ...)`
       - In `never renders raw JSON`: update the payload to only include `ask_user` fields.
       - Ensure all remaining 8 tests in `Seam.test.tsx` pass.
  </action>
  <verify>
    Run `npx vitest run src/components/panel/__tests__/Seam.test.tsx` from `frontend/`. All 8 tests must pass.
  </verify>
  <acceptance_criteria>
    - `SeamCard.tsx` no longer renders `workspace_write` or `write_todos`.
    - `SeamCard.tsx` renders `ask_user` without raw uppercase styling.
    - `Seam.test.tsx` passes with 8 tests.
  </acceptance_criteria>
  <done>SeamCard arms deleted per Winner D and tests updated.</done>
</task>

</tasks>

<threat_model>
- Threat: Deleting SeamCard arms could leave a blank space or runtime error on historic transcripts if a thread has stored `workspace_write` or `write_todos` tool calls.
- Mitigation: MessageItem only mounts SeamCard if `message.role === 'assistant'` and tool is matched. With arms removed, SeamCard returns null or gracefully omits non-ask_user kinds, while the right-hand Workspace panel (`useDerivedPanel` and `FilesSection`) renders the durable state.
</threat_model>
