---
phase: 224-what-the-agent-is-doing-reads-like-a-sentence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/tool_dispatcher.py
  - backend/tests/test_224_approval_deadline.py
autonomous: true
requirements:
  - BUG-260902-04
user_setup: []

must_haves:
  truths:
    - "Module-level constant _APPROVAL_TIMEOUT_SECONDS = 120.0 is defined in tool_dispatcher.py and consumed by both wait_for and expires_at computation"
    - "When posture == 'ask', tool_dispatcher computes expires_at using _APPROVAL_TIMEOUT_SECONDS and includes both expires_at and timeout_seconds in tool_approval_required emit"
    - "The client receives an explicit server deadline and duration on the wire rather than inventing a client-side timer"
    - "Unit test imports _APPROVAL_TIMEOUT_SECONDS directly and asserts the emitted deadline against that constant (not against an independent literal)"
  artifacts:
    - backend/app/services/tool_dispatcher.py
    - backend/tests/test_224_approval_deadline.py
  key_links:
    - "ctx.emit('tool_approval_required', ..., expires_at=expires_at, timeout_seconds=_APPROVAL_TIMEOUT_SECONDS)"
---

<objective>
Put the approval deadline on the wire from the backend with a single source of truth:
1. In `backend/app/services/tool_dispatcher.py`, define a module-level constant `_APPROVAL_TIMEOUT_SECONDS = 120.0`.
2. Update `asyncio.wait_for(..., timeout=_APPROVAL_TIMEOUT_SECONDS)` and `now_utc + timedelta(seconds=_APPROVAL_TIMEOUT_SECONDS)` to both consume `_APPROVAL_TIMEOUT_SECONDS`.
3. Pass both `expires_at=expires_at_iso` and `timeout_seconds=_APPROVAL_TIMEOUT_SECONDS` into `ctx.emit(..., "tool_approval_required", ...)`.
4. In `backend/tests/test_224_approval_deadline.py`, import `_APPROVAL_TIMEOUT_SECONDS` and assert that the emitted `timeout_seconds` equals `_APPROVAL_TIMEOUT_SECONDS` and `expires_at` was derived using `_APPROVAL_TIMEOUT_SECONDS`.

Output: Single-sourced `tool_dispatcher.py` timeout and passing `test_224_approval_deadline.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/224-what-the-agent-is-doing-reads-like-a-sentence/224-CONTEXT.md
@.planning/phases/224-what-the-agent-is-doing-reads-like-a-sentence/224-PREFLIGHT.md
@backend/app/services/tool_dispatcher.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define _APPROVAL_TIMEOUT_SECONDS and emit deadline and duration</name>
  <files>backend/app/services/tool_dispatcher.py</files>
  <read_first>backend/app/services/tool_dispatcher.py</read_first>
  <action>
    In `backend/app/services/tool_dispatcher.py`:
    1. Define module constant near top:
       ```python
       _APPROVAL_TIMEOUT_SECONDS: float = 120.0
       ```
    2. In `_handle_connector_chat_tool`, when `if posture == "ask" and getattr(ctx, "redis", None) is not None:` begins:
       Compute the deadline timestamp:
       ```python
       now_utc = datetime.now(timezone.utc)
       deadline_utc = now_utc + timedelta(seconds=_APPROVAL_TIMEOUT_SECONDS)
       expires_at_iso = deadline_utc.isoformat()
       ```
    3. Pass `expires_at=expires_at_iso` and `timeout_seconds=_APPROVAL_TIMEOUT_SECONDS` into `ctx.emit`:
       ```python
       await ctx.emit(
           ctx.redis,
           getattr(ctx, "run_id", None),
           "tool_approval_required",
           connection_id=str(matched_conn.id),
           service_id=service_id,
           service_name=matched_conn.name,
           tool_name=action_tool_name,
           args=args,
           expires_at=expires_at_iso,
           timeout_seconds=_APPROVAL_TIMEOUT_SECONDS,
       )
       ```
    4. At line ~4475 (the `asyncio.wait_for` call):
       Change `asyncio.wait_for(_wait_for_decision(), timeout=120.0)` to:
       `asyncio.wait_for(_wait_for_decision(), timeout=_APPROVAL_TIMEOUT_SECONDS)`
       Ensuring zero divergence between the wait timeout and emitted deadline.
  </action>
  <verify>
    Inspect diffs in `tool_dispatcher.py` to confirm both sites use `_APPROVAL_TIMEOUT_SECONDS`.
  </verify>
  <acceptance_criteria>
    - `_APPROVAL_TIMEOUT_SECONDS` is defined and used by both `wait_for` and `timedelta`.
    - `ctx.emit` includes `expires_at` and `timeout_seconds`.
  </acceptance_criteria>
  <done>Single module constant used for wait_for and emit.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests asserting against imported _APPROVAL_TIMEOUT_SECONDS</name>
  <files>backend/tests/test_224_approval_deadline.py</files>
  <read_first>backend/tests/test_223_dispatcher_audit.py</read_first>
  <action>
    Create `backend/tests/test_224_approval_deadline.py`:
    1. Import `_APPROVAL_TIMEOUT_SECONDS` from `backend.app.services.tool_dispatcher`.
    2. Set up test for `_handle_connector_chat_tool` when `posture == "ask"`:
       - Mock `ctx.redis` and `ctx.emit`.
       - Mock pubsub message or timeout.
       - Assert `ctx.emit` was called with event name `"tool_approval_required"`.
       - Assert keyword argument `timeout_seconds == _APPROVAL_TIMEOUT_SECONDS`.
       - Validate that `expires_at` parses as UTC ISO datetime, and `(parsed_expires_at - now_utc).total_seconds()` is within 2 seconds of `_APPROVAL_TIMEOUT_SECONDS`.
  </action>
  <verify>
    Run `pytest backend/tests/test_224_approval_deadline.py -v` from repo root.
  </verify>
  <acceptance_criteria>
    - `test_224_approval_deadline.py` imports `_APPROVAL_TIMEOUT_SECONDS` and passes.
  </acceptance_criteria>
  <done>Unit tests asserting against single constant passing.</done>
</task>

</tasks>

<threat_model>
- Threat: Divergence between server timeout and emitted deadline leads to false timeout or hanging clock.
- Mitigation: Enforced by single module constant `_APPROVAL_TIMEOUT_SECONDS` and unit test asserting against that imported constant.
</threat_model>
