---
phase: 217.1-the-library-exactly-as-sketched
plan: 06
status: complete
started: 2026-08-29T21:25:00.000Z
completed: 2026-08-29T23:30:00.000Z
tasks: 2
---

## Plan 217.1-06: Documents tab stat tiles, breadcrumb, and client-side pager

Remainder of ROADMAP SC#4 — the page-level furniture the row extraction (plan 05) does not touch.

### What shipped (one commit, both tasks — executed INLINE because all sub-agent spawns hit gateway 503s)

**`LibraryStatTiles.tsx`** (LIB-01) — three tiles modeled on `HealthStatBar`'s label / big-number / description shape:
- **Chunks** — sum of `chunk_count` over the currently loaded documents (zero fetch), description `across N documents`.
- **Vectors** — `getReembedProgress().total` (the live chunk/vector total), description = the model name.
- **Found by a search** — `getHealthOverview().retrieved_this_month`, description `last 30 days`.
- ⛔ No sparkline on any tile — a DROP, not a deferral (neither has stored history; the *Embedding Quality 92%* lie in another shape).
- ⛔ An unreachable source renders `Not known yet`, never `0`. `useAsyncValue` handles the failure arm.

**`LibraryBreadcrumb.tsx`** — thin composition: `Library › <FolderBreadcrumb output> › <current tab label>`. When no folder is selected, FolderBreadcrumb renders null → `Library › Documents`.

**`DocumentsPager.tsx`** — extends `PaginationControls`' prev/next/page-count shape with a `Rows per page` Select (10/25/50/100). `total >= 1000` (PostgREST's max-rows ceiling, WR-04) renders the "list may be larger than shown" honest arm instead of a confidently-wrong exact total.

**`LibraryPage.tsx`** — three additive mounts + composition hooks:
- `LibraryStatTiles` in `documentsLead`, `LibraryBreadcrumb` in the header block, `DocumentsPager` below the table (folder view only — a filter/view resolve carries its own server count).
- Client-side slice `pagedDocuments = listDocuments.slice(pageOffset, pageOffset + pageSize)`, offset reset on folder/filter change.
- Composition hooks: `documents-pagehead`, `documents-tabslist`, `documents-sidebar`, `documents-doclist`, `documents-tfoot`, plus `documents-stat-tiles` on the tiles themselves.

### Regression fixed (pre-existing, not plan 06's fault)
`LibraryPage.test.tsx`'s "the Ingestion tab MOUNTS the stage strip" case was failing on the committed baseline — it predates plan 03's sub-tab restructure and looked for the strip on the default (Add files) sub-tab. It now clicks the **In progress** sub-tab first. Verified failing-before / passing-after by stashing the plan 06 diff.

### Verification
- `npx tsc --noEmit` — clean · `npx vite build --logLevel error` — clean (exit 0)
- `LibraryStatTiles.test.tsx` — 13/13 pass (labels normalized to the sketch contract's title case; VECTORS asserts `total`, not `re_embedded`)
- `LibraryPage.test.tsx` — 16/16 pass (11 pre-existing + the fixed strip case + 4 new: tiles, breadcrumb, pager range, 1000-row cap arm)
- `node drive.cjs` — 221 passed · 0 failed

### Self-Check: PASSED
- [x] Three stat tiles, each with an unreachable-source arm that never renders 0
- [x] Breadcrumb reads Library › folder › tab
- [x] Client-side pager with rows-per-page + range + prev/next, 1000-row honest arm
- [x] No sparkline on any tile
