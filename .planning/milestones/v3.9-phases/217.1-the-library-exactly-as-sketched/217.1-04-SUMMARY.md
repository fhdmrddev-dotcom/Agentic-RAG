---
phase: 217.1-the-library-exactly-as-sketched
plan: 04
status: complete
started: 2026-08-29T19:00:00.000Z
completed: 2026-08-29T21:20:00.000Z
tasks: 2
---

## Plan 217.1-04: The four-card pipeline row, queue table, and History

**ROADMAP SC#3 satisfied**: the Ingestion tab is screen 008 — sub-tabs, full dropzone, folder picker, monotonic four-card pipeline row, queue table, and History.

### Task 1: The six-to-four fold (`89094f51c`)

`frontend/src/components/library/ingestion/pipelineGroups.ts` (68 L) — the pure leaf mapping the six backend stages to the sketch's four cards:

| Backend stage | Card |
|---|---|
| `extracting` | Reading |
| `chunking` | Splitting |
| `embedding` | Indexing |
| `extracting_tables` | Indexing |
| `extracting_images` | Indexing |
| `metadata` | Labelling |

`extracting_tables` and `extracting_images` fold into Indexing per D-217.1-26 — both run AFTER `embedding` in the measured write order, so folding them into Reading would make a file visibly jump backwards. `PIPELINE_CARDS` is the ordered display array. `CARD_LABEL` is the one human name per card.

7 tests: exhaustive mapping, no-orphan coverage, ordered-card assertion, monotonicity over the full write-order sequence, pairwise forward-only check, and label completeness.

### Task 2: The pipeline row, queue table, and History (`efb46f838`)

`frontend/src/components/library/IngestionTab.tsx` — three changes:

1. **Four-card pipeline row** replaces the six `StageCount` cards. Cards render in `PIPELINE_CARDS` order with `›` chevrons between them. Counts are derived from `cardForStage` on each `processing` document's `ingestion_step`.

2. **Queue table** promotes the `<ul>` to a `<table>` with `thead` columns `File · Stage · Folder · Size · Actions`. `pending` documents show `Waiting its turn` in the Stage cell. An `N files` label appears alongside the queue heading.

3. **History sub-tab** renders completed + failed documents sorted by `updated_at` descending, with `Complete`/`Failed` badges and file name, status, size, and date columns. Empty state: "Nothing has been ingested yet."

38 tests (all passing): four-card render, PIPELINE_CARDS order, CARD_LABEL labels, three chevrons, six-doc fixture counting (1+1+3+1=6), queue table columns, pending="Waiting its turn", N-files label, history table with Complete/Failed badges, empty-history state.

### Verification

- `cd frontend && npx tsc --noEmit` — **passed**
- `cd frontend && npx vite build --logLevel error` — **passed** (exit 0)
- `cd frontend && npx vitest run src/components/library/ingestion/__tests__/pipelineGroups.test.ts` — **7/7 passed**
- `cd frontend && npx vitest run src/components/library/__tests__/IngestionTab.test.tsx` — **38/38 passed**

### Self-Check: PASSED

All must_haves verified:
- [x] The pipeline row shows exactly four cards in PIPELINE_CARDS order
- [x] Every in-flight document appears in exactly one card (no orphan, no double-count)
- [x] The fold is monotonic over the measured write order
- [x] The queue table renders from useDocuments' already-held rows; History has no new fetch