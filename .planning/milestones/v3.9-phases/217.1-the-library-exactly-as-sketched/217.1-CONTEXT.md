# Phase 217.1: The Library, Exactly As Sketched — Context

**Gathered:** 2026-08-29
**Status:** Ready for planning
**Source:** Operator ruling of 2026-08-29, recorded in `217.1-BUILD-TO-SKETCH.md` (this phase's element
ledger) on the diagnosis in `217-SKETCH-DRIFT-AUDIT.md`. **No `/gsd:discuss-phase` was run and none is
owed** — the scope arrived fully settled, element by element, with one verdict each.

⚠ **Decision ids in this file are `D-217.1-NN`.** The SDK's `check.decision-coverage-plan` gate matches a
literal `D-NN` and is structurally blind to three-segment ids — it returned `skipped — "no trackable
decisions"` against Phase 217's 24. **Coverage must be re-derived by hand at the plan-check step**, exactly
as 217 had to.

⚠ **THIS FILE HAS BEEN CORRECTED BY MEASUREMENT (2026-08-29, after research).** Two locked decisions were
written from inherited claims and were measured **false**; both corrections are recorded **beside** their
originals, never over them — see **D-217.1-14** and **D-217.1-17**. A third was upgraded from an open
question to an answered one. ⚠ **`217.1-RESEARCH.md` §H carries EIGHT further flagged notes** where the
sketch and the tree genuinely disagree; four were ruled by the operator on 2026-08-29 and are recorded as
**D-217.1-26..29** at the end of this section. **A plan must not silently pick either side of any of them.**

---

<domain>
## Phase Boundary

**What this phase is.** Phase 217 shipped 12 plans, marked 5/5 success criteria verified, and passed every
gate — and the Library still does not look like the sketch. The cause is measured, not guessed:
`BUILD-CONTRACT.generated.md`, the artifact the plans were written from, carries **200 assertions about
vocabulary, ordering and honesty and ZERO about composition**. No card, no stat tile, no table anatomy, no
row expansion, no pagination, no button. Grepping all twelve plans plus CONTEXT / RESEARCH / PATTERNS for
the sketch's own composition strings returns **0 hits** for `Vector store`, `Embedding model`,
`Re-index everything`, `Found by a search`, `In progress`, `Rows per page` and `Documents with no vectors`.

⭐ **The tell that this diagnosis is right:** every honesty rule in the contract *did* ship, precisely — no
percentage, no ETA, `87 of 224` as numerator-and-denominator, `Not known yet` instead of a zero,
struck-through skipped stages. **The phase executed its contract faithfully. The contract was the wrong
size.**

So this phase rebuilds the Library **element for element against sketch 218**, and its first act is to
close the gate that let the first pass through.

**The rule that governs every ambiguity in this phase** (operator, 2026-08-29):

> **Build the sketch exactly. Anything the sketch draws that is not backed today is either BUILT or
> DROPPED — no placeholder, no half-card, no "deferred with a trigger."**

**What this phase is NOT.** It is not a gap-closure round on 217 (G-7 does not apply the way it would to a
`--gaps` cycle; see the guardrail section). It is not a re-litigation of 217's vocabulary, honesty rules,
column order or selection model — all of those shipped correctly and are **fenced as untouchable**. And it
does not introduce a single element the sketch does not draw.

</domain>

<decisions>
## Implementation Decisions

### Scope, settled by the operator

- **D-217.1-01 — Five tabs, variant A.** The tab set is `Documents · Views · Ingestion · Indexing · Health`
  (operator, 2026-08-28). Shipped has four.
- **D-217.1-02 — Health is ABSORBED into this phase** (operator, 2026-08-29). Phase 218's five success
  criteria come with it: the `Library Health` + `Governance` nav retirement, the `governance_health` gate
  move, the `ChatLayout.tsx:879` fallback replacement, LIB-06 and LIB-07. ROADMAP.md records 218 as
  **ABSORBED, not deleted** — its row and detail section are kept so the coverage map and 219/220's
  dependency lines still resolve.
- **D-217.1-03 — The `Retrieval Score` tile takes the qualified arm** (operator, 2026-08-29):
  `MATCH STRENGTH` · `0.61` · *average similarity of what searches returned*. Not a bare score, not a grade.
- **D-217.1-04 — The `sources` tab is DROPPED from this phase.** It is Phase 219 and hard-blocked on Phase
  215 (BYO OAuth), which is not built. ⚠ **This is a dependency, not a design change** — do not record it as
  a scope cut of the sketch.
- **D-217.1-05 — Screen 008 (`upload`) is the Ingestion authority, over screen 006.** 008 is the
  operator-starred second pass and is strictly richer: sub-tabs, folder picker, pipeline row, queue table,
  History.
- **D-217.1-06 — One phase, all ten waves** (operator, 2026-08-29), rather than splitting the nav
  retirement or `checked_queries` into a 217.2.

### A correction to the prior audit, carried forward deliberately

- **D-217.1-07 —** `217-SKETCH-DRIFT-AUDIT.md` called the shipped six stage-count cards *"an invented
  surface"*. **That was wrong.** Screen 008 draws a stage-card row, and the sketch README calls it *"the
  reference's screen-07 stage-card row… a real aggregate we can produce today"*. Shipped built **six**; the
  sketch draws **four**. **That is a fidelity gap, not an invention** — the fix is to re-shape the row, not
  to delete a surface. The audit's Wave plan is likewise **superseded** by this file's; only its diagnosis
  and its per-tab diff stand.

### The gate — this is task zero, and nothing else may land before it

- **D-217.1-08 — `drive.cjs --emit` gains COMPOSITION assertions per screen**: the ordered block list, each
  block's required child atoms, and every named button. ⚠ **Emitted, never hand-transcribed** — a
  hand-written list is the thing that goes stale.
- **D-217.1-09 — A `sketch-composition` fence suite** mounts each tab body and asserts the emitted block
  list by `data-*` hook. ⚠ **Driven RED against the current tree first.** A guard nobody has seen fire is
  not a guard. Without this, 217.1 drifts for exactly the reason 217 did.

### What is fenced as ALREADY CORRECT — do not touch, do not re-plan

- **D-217.1-10 —** The page title `Library` + subtitle · the tab-bar contrast (`--tab-active` + inset ring,
  both themes, shipped 217-06) · the Documents column order `Filename · Type · Size · Chunks · Status ·
  Actions` **and its `nth-child(3–5)` shed at 430px** · the `4 tables` / `1 imgs` pills · the 430px panel
  track · parsed text, tables-as-tables and image descriptions (shipped 217-10 / 217-11) · the
  `librarySelection` single reducer (217-05 / SC#5).
- ⚠ **The `nth-child(3–5)` shed is load-bearing.** `LibraryPage` sheds columns 3–5 positionally when the
  430px panel opens. **Re-verify it after any cell change** — several waves touch the Status cell.
- ⚠ **Selection stays on the one reducer.** Views becoming a card grid must not add a second piece of
  selection state. Two renderings, one truth — the operator's own build constraint when variant A was chosen.

### The verdict per element

⚠ **The complete element ledger is `217.1-BUILD-TO-SKETCH.md` in this directory, and it is BINDING.** Every
element the sketch draws carries exactly one verdict there — `✅ already correct` · `BUILD` (front-end only)
· `BUILD+BE` (needs a backend read or write) · `DROP` with its reason. The planner MUST read it in full and
MUST NOT plan an element it does not list. Summarised here only so nothing is invisible:

- **Shell** — breadcrumb `Library › <folder> › <tab>` **BUILD**; five triggers with Health fifth **BUILD**.
- **Documents** — three stat tiles **BUILD**; ⛔ **their sparklines DROPPED** (neither CHUNKS nor VECTORS has
  stored history; a line from one point is the *Embedding Quality 92%* lie in another shape); expand
  chevron, folder pill, chunk proportion bar, inline six-stage strip in the Status cell, failure sentence,
  and a client-side pager **BUILD** (`GET /documents` returns the full list unpaginated, so the count is real).
- **Views** — 2-column card grid, rule-in-plain-words, `New view`, the `Built in · shared with everyone` line,
  and ⭐ **the zero-match card** (amber outline, `0`, `Matches nothing right now`) **BUILD**. The sketch says
  the zero-match card is what makes this a tab rather than a list.
- **Ingestion** — sub-tabs, full dropzone + `Choose files` + folder picker, the **four**-card pipeline row,
  the queue table, visible stage labels, `Waiting its turn`, the `Needs attention` row with `Try again`, and
  `History` **BUILD**. ⛔ **`✕` cancel DROPPED** — no cancel path exists in the backend and no way to kill an
  in-flight ingestion; a control that cannot do its verb is worse than no control. ⛔ **Upload percentage
  stays dropped** — `DocumentUpload.tsx` passes no `onUploadProgress`, so byte progress is unknowable.
- **Indexing** — Vector store card, Embedding model card, `Change model` (⛔ **routes to Settings' shipped
  picker, never a second picker** — the sketch's own "leaves for Settings" rule), `Re-index everything` with
  its consequence sentence, the Folders table and `Re-index selected` **BUILD / BUILD+BE**.
- **Health** — coverage ring (numerator **and** denominator), the stacked chart with its third segment, four
  tiles, `MATCH STRENGTH`, seven chips in two groups, and per-document bars **BUILD / BUILD+BE**.
- **Detail panel** — `DETAILS`, `RETRIEVAL`, real `Average relevance`, `FOUND PER WEEK`, `CHUNKS · N`
  **BUILD / BUILD+BE**. ⛔ **The full original-file content viewer stays cut** — the chunks are what the
  agent searches.

### Naming — a merge that leaves both words standing ships a lie

- **D-217.1-11 —** The two `Low Confidence` signals are **renamed apart**: `Weak matches` (retrieval
  similarity below `0.38`, `knowledge_health.py`) vs `Unsure metadata` (extracted field below `0.5`,
  `document_governance.py`). They are **different measures on different data** and have collided under one
  word.
- **D-217.1-12 —** ⛔ **The word `golden` stays banned** anywhere in this surface —
  `workflow_runs.is_golden_run` owns it.

### Backend — six items, and that is the complete list

Everything else in this phase is front-end composition over data already fetched.

- **D-217.1-13 — BE-1:** migration adding `document_chunks.embedded_at timestamptz`, written by the embed
  path **and** the re-embed path. ⚠ `created_at` is the *chunking* time and **does not move on a re-embed** —
  using it would print a lie after the first re-index. Serves `Last indexed` on the Vector store card and
  every Folders row.
- **D-217.1-14 — BE-2:** `GET /library/index-summary` — corpus totals (`vectors`, `documents with no
  vectors`, `last indexed`, `dimensions`, `provider`) **and** per-folder rows. Serves all three Indexing
  cards.
  - ✅ **ANSWERED 2026-08-29 by research + pattern map, independently:** `GET /settings/reembed-progress`
    carries **neither** `dimensions` nor `provider` — it returns exactly
    `{status, total, re_embedded, remaining, model, updated_at}`. **BE-2 must supply both.**
  - ✅ **ANSWERED — BE-2 needs NO service-role carve-out.** `document_chunks`, `documents` and `folders` all
    carry `authenticated` SELECT policies, and `get_user_pg_connection` (`dependencies.py:159-177`) gives an
    RLS-enforced `GROUP BY`. ⚠ `knowledge_health.py`'s service-role exception does **not** transfer — use the
    user-JWT + `run_in_threadpool` pattern 217-02 shipped.
  - ⛔ **BUT `GET /settings/reembed-progress` and `POST /settings/reembed` are BOTH behind
    `require_visible("model_management")` — they are OPERATOR-ONLY.** So the Indexing tab's facts and **both**
    re-index buttons are **dead for every non-operator today** (Phase 217 deferral **WR-02**, whose re-open
    trigger this phase fires). **BE-2 must decide knowingly** whether to match that gate or not; a plan that
    does not decide ships a tab that is blank for most users.
- **D-217.1-15 — BE-3:** folder scope on `POST /settings/reembed`, for `Re-index selected`.
- **D-217.1-16 — BE-4:** write the `search.query` audit row on the **error** path.
  `tool_dispatcher.py:725-742` returns before any write, so **a provider outage records nothing today and is
  indistinguishable from "your library had no answer."** Write it with `document_ids: []` and
  `retrieval_status: provider_error`. Serves the `could not search` segment (LIB-06).
- **D-217.1-17 — BE-5:** persist per-hit similarity as a key on the **existing retrieval jsonb** and `GET`
  it per document. ~~`retrieval_service.py` computes a per-hit similarity today and **throws it away** — the
  one retrieval fact the Library cannot show.~~ ⭐ **A jsonb key on an existing column, not a new table.**
  Serves `Average relevance` (LIB-07).
  - ⚠ **CORRECTED 2026-08-29 by research, and the original is struck through above rather than deleted —
    the claim was inherited from `SEED-224` and is FALSE.** `retrieval_service.py:173` **returns** the
    per-hit similarity on every hit, and `tool_dispatcher.py:781` **copies it into every citation**. Nothing
    is thrown away. **What is missing is the `audit_log.metadata` key** — and that dict literal is *the same
    one BE-4 edits*, so BE-4 and BE-5 are one edit site, not two. ⚠ **Every historic row lacks the key**, so
    `Average relevance` must read `not recorded yet`, never `0`.

  ⭐ **This is why the phase researches before it plans**: an unmeasured claim in a locked decision would
  have sent a plan hunting for a discard that does not exist.
- **D-217.1-18 — BE-6:** ⚠ **the only new table in this phase** — `checked_queries` (`question`,
  `expected_document_id`, `last_rank`, `previous_rank`, `checked_at`) + CRUD + **a rank evaluator that
  re-runs the shipped retrieval path and records where the expected document landed**. ⭐ **Build both or
  neither.** If BE-6 is cut, the `CHECKED QUERIES` tile and table are cut with it — a `Checked queries`
  surface with no evaluator is exactly the fake number the sketch's cut list exists to remove. **This is the
  one place this phase invents rather than renders.**

### The four live defects — visible to a person today, in the shipped build

- **D-217.1-19 —** `IngestionStrip.tsx:151` makes every stage label `sr-only` plus a `title`, so **the strip
  renders as six blank boxes** and a sighted user cannot read the pipeline this phase exists to make
  readable. The sketch draws the words `READ · TBL · IMG · SPLIT · INDEX · LABEL`.
- **D-217.1-20 —** `IngestionTab.tsx:122` renders `doc.error_message` verbatim, so a duplicate-key failure
  **prints a raw Postgres error dict to the operator** (`{'message': 'duplicate key value violates unique
  constraint …', 'code': '23505', …}`). Route it through a plain-language mapper: **the sentence for the
  user, the raw string behind ⌥ Technical-names.**
- **D-217.1-21 —** The failed row's only failure cue is a 1px ring on an unlabeled box, which compounds
  D-217.1-19. It becomes the sketch's row: `● Failed` badge · plain sentence · size · **`Try again`** wired
  to the shipped reingest endpoint.
- **D-217.1-22 —** **A tab named *Ingestion* cannot start an ingestion** — there is no dropzone on it.

### Wave order

- **D-217.1-23 —** Wave 0 gate → Wave 1 the four live defects → Wave 2 Ingestion becomes screen 008 →
  Wave 3 Documents furniture → Wave 4 Views stops being the sidebar → Wave 5 Indexing's three cards
  (BE-1/2/3) → Wave 6 Health absorbed (BE-4, BE-5) → **Wave 7 the merge's seams** → Wave 8 the detail panel
  → Wave 9 `checked_queries` (BE-6). ⚠ **Wave 7 is the sharpest thing in this phase** and is described in
  its own decision below.

### Wave 7 — the merge's seams, and why it is the sharpest thing here

- **D-217.1-24 —** Retiring `KnowledgeHealthPage` **requires installing its replacement fallback in the same
  commit.** `KnowledgeHealthPage` is `ChatLayout.tsx:879`'s **positional fallback** — the trailing `else` —
  and `App.tsx:98` says a union member with no branch silently renders it. **Deleting it turns a mis-route
  into a blank screen.**
- **D-217.1-25 —** The **`governance_health` gate moves with the three signals it guards.** Governance is
  feature-gated and Documents is not, so merging them without moving the gate exposes three signals to
  people who cannot see them today. ⚠ **Verified by a negative test, never by inspection.**

### The eight sketch-vs-tree conflicts, RULED (research §H)

⚠ **`217.1-RESEARCH.md` §H recorded eight places where the sketch and the shipped tree genuinely
disagree.** Build-or-drop makes every one of them a decision that must be made *before* planning — a plan
may not silently pick a side. **Four were ruled by the operator on 2026-08-29** (D-217.1-26..29); the other
four take the research's recommendation as a routine judgment call (D-217.1-30..33). All eight are now
closed.

- **D-217.1-26 — The four-card pipeline folds `extracting_tables` / `extracting_images` into `Indexing`,
  NOT into `Reading`** (operator, 2026-08-29). ⚠ **This overrides the literal mapping in
  `217.1-BUILD-TO-SKETCH.md`, and does so to preserve the property that mapping existed for.** Measured:
  both stages run **after** `embedding` (`documents.py:2277, 2315, 2326`), so folding them into card 1 makes
  an in-flight file **visibly jump backwards** — precisely the defect `ingestionStages.ts:12-25` records
  217-08 as having fixed. Folding into card 3 keeps every in-flight file in exactly one card, orphans no
  state, **and keeps the row monotonic.**
- **D-217.1-27 — BE-2 is UNGATED; the three action buttons stay operator-only** (operator, 2026-08-29).
  The Indexing tab's **facts** (Vector store, Embedding model, Folders) come from the new
  `GET /library/index-summary` with **no `require_visible` gate** — they are RLS-scoped reads of the user's
  own corpus. `Change model`, `Re-index everything` and `Re-index selected` stay behind
  `require_visible("model_management")` and render **as absent for non-operators, never as dead buttons**.
  ⚠ Everyone sees the same five tabs — a tab set that changes shape per user is a second Library.
  This fires and closes Phase 217's deferral **WR-02**.
- **D-217.1-28 — The per-document bar legend states the MEASURED meaning, and the `re-indexing` arm is
  DROPPED** (operator, 2026-08-29). The legend reads *"added more than 90 days ago"*, because `_fetch_stale`
  (`knowledge_health.py:322-357`) selects `documents.created_at < now - 90d` — **age, not modification** —
  and **no "changed after indexing" signal exists anywhere in the backend**. The sketch's verbatim
  *"the file changed after it was indexed"* would ship a sentence about a measurement we do not take. The
  `re-indexing` arm has **no per-document source at all** and is dropped outright; bars render `ready` /
  `stale` only. ⛔ Build-or-drop forbids the placeholder third arm.
- **D-217.1-29 — `MATCH STRENGTH` is a FIFTH TILE in the Health tile row** (operator, 2026-08-29).
  `index.html`'s Health screen draws four tiles and no MATCH STRENGTH element, so its placement was
  **undrawn** rather than decided. The row becomes
  `DOCUMENTS · SEARCHES · NEVER FOUND · CHECKED QUERIES · MATCH STRENGTH`, value `0.61`, description
  *average similarity of what searches returned* (the qualified arm of D-217.1-03).
  `COPY.SHIPPED_HEALTH_TILES` names `Retrieval Score` among the tiles that must not be lost, so a tile is
  the shape the vocabulary already expects.

- **D-217.1-30 — The expand chevron stays VERSION-GATED.** `DocumentList.tsx:347` already renders the
  chevron column, and `:369-379` renders the glyph only when `isExpandable(doc)` (`version_number > 1`,
  Phase 112 D-01). The sketch draws `›` on **every** row and **draws no expanded content**, so building it
  literally would require inventing what a non-versioned row expands to. ⛔ **A chevron that expands nothing
  is worse than no chevron.** SC#1's composition asserts the **column**, not a per-row glyph.
- **D-217.1-31 — The no-folder bucket stays `Root`, not `Uncategorized`.** `LibraryPage.tsx:508-513` already
  ships `Root` on screen. ⚠ Per D-217.1-11, **both words must not stand**: the Folders table says `Root`
  everywhere, and `Uncategorized` appears nowhere. The ledger's `Uncategorized · 4 · – · – · never` row is
  otherwise unchanged — the `–` and `never` arms are the point and are binding.
- **D-217.1-32 — The `RETRIEVAL` block is built inside the existing `Found by` section**, matching screen
  007's composition, and keeps the shipped panel's closed-by-default lazy prop. ⚠ `DocumentDetailPanel.tsx`
  is a **cross-surface** panel that **chat also mounts** — an always-open block would change chat's panel
  too, which is out of this phase's fence.
- **D-217.1-33 — `renameFence.test.ts` is REWRITTEN as a rename map, never deleted.** It will red in Wave 7,
  **correctly**: it pins the `governance` nav entry with its feature key (`:197`) and `ChatLayout.tsx:879`'s
  trailing `<KnowledgeHealthPage />`. The rewrite records replaced → asserts GONE → asserts replacement
  PRESENT. ⛔ Deleting a fence because the thing it pins was intentionally removed is how a fence stops
  guarding without anyone noticing.

### Two further facts the plan must carry

- **D-217.1-34 — BE-4 moves EARLY, not sixth, and it is the highest-consequence item in the phase.**
  `knowledge_health.py:376` increments `retrieval_count` for **every** `search.query` row, and
  `RetrievalTrendChart.tsx:90` plots exactly that series as the shipped **Coverage Trend** card. ⚠ **A naive
  error-path row therefore makes a provider outage read as a RISE** — strictly worse than the dip the sketch
  warns about. The distinguishing key is `retrieval_status: "provider_error"`, which is **already Phase
  210's literal** at `tool_dispatcher.py:741`. A **characterization test pinning `retrieval_count`'s current
  meaning** must land before the write does. ⭐ BE-4 and BE-5 edit the **same dict literal**.
- **D-217.1-35 — BE-1 has FOUR `document_chunks` write sites, not the two D-217.1-13 names.**
  `documents.py:2302` · `multimodal_service.py:425` (tables) · `multimodal_service.py:903` (images) ·
  `reembed_service.py:196`. ⚠ **A two-site plan leaves table and image chunks `NULL` forever**, and the
  Folders row then reads `never` for a folder that *is* indexed — a new lie in the row whose honesty is the
  point of the tab.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### The acceptance bar (G-2 is SATISFIED by these, not overridden)
- `.planning/sketches/218-the-library-and-its-tabs/index.html` — ⭐ **the approved mockup.** Committed
  2026-08-28, one day before 217 was planned, so it **is** this phase's bar and always was.
- `.planning/sketches/218-the-library-and-its-tabs/README.md` — the sketch's own annotations, cut list and
  reasons. The Views annotation (*"otherwise it is the sidebar again with more clicks"*) predicted in
  writing exactly what shipped.
- `.planning/sketches/218-the-library-and-its-tabs/BUILD-CONTRACT.generated.md` — the 200 text assertions.
  ⚠ **This artifact is the subject of SC#1**, not merely an input: it must gain composition assertions.
- `.planning/sketches/218-the-library-and-its-tabs/drive.cjs` — the emitter SC#1 extends.
- `.planning/sketches/218-the-library-and-its-tabs/COPY.js` — the vocabulary source.
- Screenshots: `screenshots/1.png`–`4.png` (**shipped**) vs `screenshots/FireShot Capture 003–013 …`
  (**sketch**). These are the operator's own side-by-side and the origin of the ruling.

### This phase's own scope
- `.planning/phases/217.1-the-library-exactly-as-sketched/217.1-BUILD-TO-SKETCH.md` — ⭐ **the BINDING
  element ledger.** One verdict per element. Read in full before planning.
- `.planning/phases/217-the-library-one-home-for-documents/217-SKETCH-DRIFT-AUDIT.md` — the diagnosis and
  the per-tab diff. ⚠ **Its Wave plan is SUPERSEDED and its "invented surface" claim is corrected by
  D-217.1-07.**

### What shipped, and must not be re-litigated
- `.planning/phases/217-the-library-one-home-for-documents/217-CONTEXT.md` — 217's 24 locked decisions,
  including the vocabulary and honesty rules this phase inherits unchanged.
- `.planning/phases/217-the-library-one-home-for-documents/217-VERIFICATION.md` — 5/5 verified, 16 UAT rows
  owed. ⚠ **Those owed rows are still owed** and overlap this phase's G-4 drive.
- `.planning/phases/217-the-library-one-home-for-documents/217-01..12-SUMMARY.md` — what each plan actually
  built, including the shipped section set of the detail panel and the shipped chunk section.
- `.planning/phases/217-the-library-one-home-for-documents/deferred-items.md` — what 217 deferred and why.
  ⚠ **Cross-check before planning**: under build-or-drop, a deferral here is no longer a valid outcome.

### The hot files (G-5 fires on six — read each section before planning)
- `docs/HOT-FILE-LEDGER.md` — `#frontendsrcpageslibrarypagetsx` (10 ph) ·
  `#frontendsrccomponentsingestiondocumentlisttsx` (12 ph) · `#frontendsrcpagesknowledgehealthpagetsx` (5 ph) ·
  `#backendappapiknowledgehealthpy` (5 ph) · `#backendappapidocumentgovernancepy` (3 ph) ·
  `#frontendsrccomponentsmetadatadocumentdetailpaneltsx` (6 ph) ·
  `#backendappservicesretrievalservicepy` (9 ph) · `#frontendsrchooksusedocumentsts` (3 ph) ·
  `#frontendsrccomponentsuitabstsx` · `#frontendsrccomponentsingestiondocumentuploadtsx`.
  ⚠ **Same-commit sync rule**: a ledger row and its detail section change together, and the disposition cell
  is capped at 200 chars.

### Project rules that bind this phase
- `CLAUDE.md` — the migration rule (numbered SQL under `supabase/migrations/`, applied via the **Supabase SQL
  editor**, then `bash scripts/regenerate-full-schema.sh`; never `db push` / `db reset`) · RLS on every table ·
  no blocking I/O in async handlers (`run_in_threadpool`, D-v2.5-01) · Realtime is a hint, reconcile by fetch
  (D-v2.5-03) · the CLAUDE.md size gate · the G-1..G-7 guardrails and the hot-file ledger.
- `.planning/REQUIREMENTS.md` — LIB-01..LIB-07 as written.
- `supabase/SETUP.md` — migration application and schema regeneration.

</canonical_refs>

<specifics>
## Specific Ideas

- **The four-card pipeline row replaces the shipped six**, and the mapping is exact:
  `extracting_tables` and `extracting_images` **count into `Reading`**, so every in-flight file sits in
  exactly one card and no state is orphaned. The four are `Reading · Splitting · Indexing · Labelling`, with
  icon, count, phrase and chevrons between.
- **The accepted-formats list stays read from `acceptedFormats.ts`** — the single constant 217-07 shipped,
  and the fence that caught `MSG`. The dropzone prints it; it never re-types it.
- **The `Uncategorized · 4 · – · – · never` row is the point of the Indexing tab.** It must read `–` and
  `never`, **never `0` and never a green tick** — a folder with no vectors is not a folder that is fine.
- **`GET /documents/{id}/queries` caps at 100 rows** (shipped 217-03). The `RETRIEVAL` block therefore
  renders `100+`, never a wrong exact count. All three of `Times found` · `Last question` · `Last found`
  derive from it, and so does the `FOUND PER WEEK` sparkline (bucket `asked_at` from the same rows).
- **`GET /knowledge-health/overview` already returns** `retrieved_this_month`, `total_documents` and
  `never_retrieved_count` over a 30-day window (`knowledge_health.py:399-530`) — the `FOUND BY A SEARCH`
  tile and the coverage ring both come from it. ⭐ `knowledge_health.py` **already derives retrieval
  analytics from `audit_log` `search.query`**, which refutes BUS-026's claim that nothing logs retrieval
  today.
- **`GET /settings/reembed-progress` is already called by `IndexingTab`** — the `VECTORS` tile and
  `Chunks indexed` need no new call.
- **`listViews()` already returns the filter**, so the rule-in-plain-words renderer is pure presentation.
  The sketch's own shape is `type is contract · added in the last 30 days`.
- **The `G` pill data already exists** for the `Built in · shared with everyone` line on system-global views.
- **A tile whose source is unreachable says so.** It never renders `0` — that is the same honesty rule 217
  shipped correctly everywhere else.
- **`ReembedStatusCard` stays composed unchanged** (D-217-16) when the Indexing card splits in two.

## How we'd know this failed

- A fifth tab exists but Health is a shell, or a card renders with a placeholder number — **build-or-drop
  was not applied.**
- The composition fence suite exists but was never seen RED, or asserts blocks that were written to match
  the code rather than emitted from the sketch — **SC#1 was performed, not achieved**, and 217.1 has
  drifted the same way 217 did.
- The nav retires `Library Health` and a mis-routed view renders blank — **D-217.1-24 was skipped.**
- Someone without the `governance_health` gate can see the three Governance chips — **D-217.1-25 was
  verified by inspection.**
- `Checked queries` renders a table with no evaluator behind it — **the exact fake number the cut list
  exists to remove.**
- The Documents table's columns stop shedding at 430px — **a Status-cell change broke the load-bearing
  `nth-child(3–5)` rule.**
- Views renders a card grid **and** a second piece of selection state — **the one-reducer constraint broke.**
- A re-indexed folder still shows its original chunking time as `Last indexed` — **`created_at` was used
  instead of BE-1's `embedded_at`.**
- A provider outage still shows up as *"your library had no answer"* — **BE-4 did not land on the error path.**

</specifics>

<deferred>
## Deferred Ideas

⚠ **This phase's governing rule makes "deferred" mostly unavailable**: an element is BUILT or DROPPED.
The list below is therefore **drops with reasons**, not deferrals with triggers — with two exceptions that
are genuinely other phases.

**Dropped, with the reason (all fenced; none may reappear as a placeholder):**
- CHUNKS and VECTORS **sparklines** — no stored history; a line drawn from one point.
- The `✕` **cancel** on a queue row — no cancel path in the backend, no way to kill an in-flight ingestion.
- Per-file upload **percentage** — `DocumentUpload.tsx` passes no `onUploadProgress`.
- The full **original-file content viewer** — the chunks are what the agent searches.
- The sketch's own pre-existing cut list, all still cut: `Semantic Match %` column · free-text tags with an
  `✕` · token pie charts · `Vector Database Health 98%` · `Query Latency p99` · the `Source` column
  (SharePoint / Drive) · the section-level heatmap + executive summary · per-file `Confidentiality` toggle ·
  S3 / Google Drive connector cards · the word `golden`.

**Genuinely another phase (a dependency, not a design change):**
- The **`sources` tab** → **Phase 219**, hard-blocked on **Phase 215** (BYO OAuth). *Re-open trigger:* 215
  and 219 both ship.
- **Phase 220** (the DXF takeoff SPIKE) surfaces its result in the Library — *re-open trigger:* 220 starts.

</deferred>

<scope_fence>
## Scope Fence

**IN scope:** every element listed with a `BUILD` or `BUILD+BE` verdict in `217.1-BUILD-TO-SKETCH.md`, the
six backend items BE-1..BE-6, and SC#1's composition gate.

**OUT of scope, and a plan that touches these is over-scoped:**
- Any element the sketch does not draw. ⚠ **One shipped exception is allowed to stand:** the
  `Where + condition` filter bar on Documents, which the sketch does not draw but which is Phase 114
  inheritance and defensible. **Leave it; do not extend it.**
- 217's shipped vocabulary, honesty rules, column order, stage names, or the `librarySelection` reducer.
- The Settings model picker — `Change model` **routes to it**; this phase never builds a second one.
- Any new provider call, any change to the retrieval algorithm, any ingestion pipeline change.
- Any branch added to `LibraryPage.tsx`. **Every wave adds a CHILD.**

</scope_fence>

<guardrails>
## Guardrails

- **G-2 — SATISFIED, not overridden.** The sketch **is** the approved mockup. No new `/gsd:sketch`. The
  executable acceptance bar is SC#1's emitted composition contract.
- **G-5 — fires on six files.** Honour **by construction**: every wave adds a CHILD; no wave adds a branch
  to `LibraryPage.tsx`. Read each file's `docs/HOT-FILE-LEDGER.md` section before planning, and update row
  + section in the same commit.
- **G-4 — BLOCKING at close.** The operator drives all five tabs in a browser **with the sketch open beside
  them**. ⚠ **Wire-format green is exactly what shipped the first pass** — it is not sufficient here.
- **G-7 —** this is a rebuild, not a gap-closure round. If verification returns `gaps_found` **twice**,
  triage; do not open a third round.
- **G-1 —** does not fire: 217.1 is the **first** `217.x` insert.
- ⚠ **Threat surface.** D-217.1-25 moves a permission gate and BE-6 adds a table. **Both need RLS and a
  negative test.** Everything else in this phase reads data the user can already see.
- ⚠ **Cloud parity.** BE-1 and BE-6 are migrations in block **140-149**. Apply via the Supabase SQL editor,
  regenerate `full-schema.sql` with `bash scripts/regenerate-full-schema.sh` (no `--reset`), and carry them
  to cloud **in the same operation as their code**.
- ⚠ **Count gate.** Run `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` **from the repo root**
  and read the verdict line verbatim. A growing total is the gate working; the contract is *no per-file
  decrease* and *zero failing*. New suites must be adopted into **both** knobs — TARGETS decides what runs,
  BASELINE decides what is guarded.
- ⚠ **SC#10's `--reviews`-style decision gate is blind here.** `check.decision-coverage-plan` matches a
  literal `D-NN` and this file's ids are `D-217.1-NN`. **Re-derive coverage by hand at plan-check.**

</guardrails>

---

*Phase: 217.1-the-library-exactly-as-sketched*
*Context recorded: 2026-08-29 — from the operator's settled element ledger, not from a discuss-phase run*
