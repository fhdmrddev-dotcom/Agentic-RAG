# Plan 18 SUMMARY — count-gate pin, sketchComposition GREEN, ledger closeout

**Phase:** 217.1-the-library-exactly-as-sketched
**Plan:** 18
**Type:** execute
**Wave:** 16 (final)

## Overview

The closing plan — three mechanical jobs that make SC#1's proof structural. **(1)** The Wave-0
`sketchComposition` fence, built RED on purpose in 217.1-01, now runs **GREEN** against the fully-built
five-tab tree: `47 passed (47)`. **(2)** Every suite this phase created or adopted is now in **BOTH**
count-gate knobs — TARGETS (named files) and BASELINE (pinned at the gate's own printed count, never
while red) — with the arithmetic closing at zero residual. **(3)** The hot-file ledger is current for all
eight files this phase touched, re-derived fresh, same-commit.

## Task 1 — the gate, proven both ways

`sketchComposition.test.tsx` was captured RED in `217.1-BASELINE.md` on 2026-08-29 (`40 failed | 7
passed`). It now runs green against the finished tree:

```
 Test Files  1 passed (1)
      Tests  47 passed (47)
   Duration  6.40s
```

- The 7 positive controls that passed on the RED run (contract resolution, mount harness, tab bar,
  `data-testid` convention) are still passing — the plumbing was never the failure.
- The 40 that failed (32 missing blocks, the missing Health tab, 7 missing named buttons) all resolve.
- The GREEN run is appended to `217.1-BASELINE.md` as a **CLEARED** note, never overwriting the RED
  capture — the file now shows both halves of the guard's proof: it fired, and it cleared for a real
  reason. (This CLEARED note was owed by Task 1's action and was found open at resume; closed here.)

## Task 2 — both count-gate knobs, zero residual

17 suites adopted into TARGETS (named-file entries — neither `src/components/library` nor `src/pages`
has a directory entry) and BASELINE (bare-filename pins):

sketchComposition **47** · ingestionFailureCopy **22** · IngestionTab **38** · pipelineGroups **7** ·
LibraryStatTiles **13** · viewRulePhrase **4** · ViewCardGrid **8** · IndexFoldersTable **6** ·
IndexingTab.gate **6** · HealthTiles **6** · HealthSignalChips **13** · CheckedQueriesSection **11** ·
ChatLayout.fallback **4** · DocumentQueriesSection **8** · FilterBar **10** · DocumentStatusBadge **19** ·
DocumentStatusBadge.a11y **19**

- **Sum of pins = 241.** Pinned total went `5932 → 6173`; grand total `6926`; every adopted suite
  delta 0. **Zero residual.**
- ⚠ **The three 217 deferred-§3 orphans are adopted, closing their re-open trigger**: `FilterBar`,
  `DocumentStatusBadge`, `DocumentStatusBadge.a11y`.
- ⚠ **`DocumentStatusBadge.test.tsx` TARGETS path was REPOINTED** — the entry pointed at the dead
  `src/components/ingestion/__tests__/` path (why it never printed as `new`); the file lives at
  `src/__tests__/components/`. Repointing it surfaced its 19-count and added exactly `+19` to the total.
- The five remaining `new` files in the gate output (`PromptVariableChips`, `RunHero`, `automationFacts`,
  `nodeEffectBanner`, `toolReadOnlyMap`) are the pre-existing SEED-222 unpinned suites, unchanged from
  Phase 214 — deliberately not adopted (recorded in the gate's own comment).
- `fileRowUtils.test.ts` reads `+4` — growth, not a decrease; the gate's contract (no per-file DECREASE)
  is satisfied.

**Final gate run:** `total 6926 · failed 0 · pinned total 6173` → `count gate OK — 171/171 pinned files
present, no per-file decrease, 0 failing.`

**One SEED-171 observation:** the run that captured the pins redded once on
`WorkflowBuilderPage.canvas.test.tsx` (`STACK_TRACE_ERROR`) — the named SEED-171 flake, **byte-unchanged
since phase base** (`git diff --numstat` empty, working tree clean). The final run did not re-fire. It is
recorded as an observation, never "fine"; per the plan's own procedure, the cap was untouched.

## Task 3 — hot-file ledger current, same-commit

Six rows re-derived fresh and corrected (`git log --follow`, six-digit quick-task buckets subtracted per
the CLAUDE.md recipe). Row + `docs/HOT-FILE-LEDGER.md` section updated in the same commit.

| File | was | now | reason |
|---|---|---|---|
| `tool_dispatcher.py` | 66 / 29 / 4336 | **67 / 28 / 4336** | the 29 counted `260529`/`260705` |
| `documents.py` | 71 / 31 / 2535 | **72 / 30 / 2535** | |
| `knowledge_health.py` | 10 / 5 / 737 | **11 / 6 / 737** | 217.1-11 added `could_not_search` |
| `LibraryPage.tsx` | 28 / 10 / 724 | **35 / 11 / 814** | fifth Health tab landed 217.1-12 |
| `DocumentList.tsx` | 22 / 12 / 609 | **24 / 13 / 294** | **seam TAKEN (217.1-05)** — `DocumentRow.tsx` extracted, −315 L |
| `KnowledgeHealthPage.tsx` | 11 / 5 / 571 | **RETIRED** | **DELETED by 217.1-14**; fallback → `UnknownViewFallback` (`:871`) |

`ViewsGroup.tsx` (5 / 3 / 259) and `document_governance.py` (5 / 3 / 416) verified current, no change.

`node scripts/check-claude-md-size.cjs` → **OK** (103,949 chars, 69.3%).

## Verification

| Check | Result |
|---|---|
| `sketchComposition.test.tsx` (verbose) | ✅ 47/47 |
| `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` | ✅ count gate OK, 171/171, 0 failing |
| `node scripts/check-claude-md-size.cjs` | ✅ OK |
| `npx tsc -p tsconfig.app.json --noEmit` | ⚠ **red, PRE-EXISTING** — see below |
| backend pytest subset | ✅ **24 passed** (rank_evaluator 4 · checked_queries_rls 2 · trend_segments · embedded_at · governance_gate · reembed_folder_scope · search_error_audit) |

**⚠ tsc measured, not assumed:** at close the typecheck reports errors in **28 files** — the file set is
**byte-identical** to the phase base (measured in a worktree at `af39073b5`: same 28 files, per-file
counts equal, comm diff empty both directions). Line count drifted `58 → 56` across this phase's own
commits — the phase's net change is −2 lines, zero new error files. ⚠ **The `tsc: clean` lines in plans
15/17 summaries were mis-measured** (the plan's own 217.1-14 note — *"`tsc --noEmit` alone checks ZERO
files here"* — is the correct instrument, and it was already red at base). Not this phase's defect, and
recorded so no later reader attributes the rot to 217.1-18.

**⚠ backend pytest:** the phase's seven backend suites ran **24 passed, 0 failed** at this close
(`test_2171_rank_evaluator` · `test_2171_checked_queries_rls` (integration) · `test_2171_trend_segments` ·
`test_2171_embedded_at` · `test_2171_governance_gate` · `test_2171_reembed_folder_scope` ·
`test_2171_search_error_audit`). The full backend suite is outside this plan's scope (this is a
vitest-only closeout); the phase's suites additionally carry per-plan pytest criteria and the backend
failure-count baseline, per the gate's own recorded decision.

## Commit history

- `4f884b3a8` — sketchComposition fence GREEN + composition hooks (Task 1, at resume point)
- `f38e5f3a8` — the two count-gate reds, both real (ChatLayout.launch, DetailSections.tables)
- `7714d7708` — both knobs for all 17 suites + DocumentStatusBadge path repoint + the em-dash rule-5 fix
- `7390125be` — hot-file ledger current at close
- *(this commit)* — BASELINE CLEARED note + this SUMMARY

## SC#1 standing

The Wave-0 gate has now been seen **both fire and clear**. Every ROADMAP success criterion (SC#1-13)
traces to a shipped plan; this plan is the mechanical proof that the tooling protecting all of them —
the composition fence, both count-gate knobs, the CLAUDE.md size gate, the hot-file ledger — is switched
on and current. **Phase 217.1 is complete.**
