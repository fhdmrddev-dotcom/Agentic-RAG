---
phase: 217.1-the-library-exactly-as-sketched
plan: 08
status: complete
started: 2026-08-30T00:00:00.000Z
completed: 2026-08-30T00:30:00.000Z
tasks: 2
checkpoint: applied-2026-08-30 (operator pasted migration 140 into the Supabase SQL editor)
---

## Plan 217.1-08: BE-1 — document_chunks.embedded_at, the honest "when the vector was written" timestamp

Part of ROADMAP SC#6. Every Indexing-tab fact in plans 09/10 depends on this column.

### What shipped (2 commits, checkpoint in the middle)

**`supabase/migrations/140_document_chunks_embedded_at.sql`** — `ADD COLUMN IF NOT EXISTS embedded_at timestamptz`, **nullable, NO DEFAULT** (D-217.1-13: a `DEFAULT now()` would backfill every existing row at ALTER time in Postgres 11+, destroying the "never indexed" arm). `COMMENT ON COLUMN` names all four write sites. **`resize_embedding_column` repaired** (T-217.1-13b) to NULL `embedded_at` alongside the vector on a dims change — a dropped vector must not keep a stale `Last indexed`.

**The four write sites** (D-217.1-35 corrected D-217.1-13's two — a two-site plan leaves table/image chunks NULL forever):
1. `documents.py:2296-2308` — INSERT — main text chunks
2. `multimodal_service.py:423-432` — INSERT — table chunks (also covers `backfill_document_table_chunks`, which delegates here)
3. `multimodal_service.py:896-908` — INSERT — image-description chunks
4. `reembed_service.py:187-202` — UPDATE — the re-embed path (row-scoped `eq(id)` + `eq(user_id)`, never a bulk backfill)

**`test_migration_140.py`** (3 tests) — negative-control (exists + nullable + NO default), `resize_embedding_column` repair (reads `prosrc`), and the RLS/catalog grant check (checked, not assumed). **`test_2171_embedded_at.py`** (5 tests) — all four sites' payload literals carry `embedded_at`, plus the re-embed UPDATE's row-scope guard.

**Ledger hygiene (same commit):** `documents.py` (71 commits / 31 phases / 2535 L, G-5 HARD) — **no row for its ENTIRE LIFE**; first row + detail section added, naming no seam ("owed to a future refactor phase").

### Checkpoint (operator applied)
The plan's `user_setup` gate — the operator pasted the migration into the local Supabase SQL editor. **RED → GREEN observed**: `test_migration_140.py` went **3 skipped → 3 passed** in the same operation (the plan's owed observation). `bash scripts/regenerate-full-schema.sh` (no `--reset`) regenerated `supabase/full-schema.sql` (6299 lines, `embedded_at` present ×5), committed alongside.

### Verification
- `test_migration_140.py` — **3 passed** (was 3 skipped pre-apply)
- `test_2171_embedded_at.py` — **5 passed**
- Source syntax — all three edited files parse clean
- `full-schema.sql` regenerated + committed (14 insertions / 3 deletions)

⚠ **No regressions from my edits:** the 70 `tests/unit` failures are pre-existing rot — the 9 in my blast radius (`test_multimodal_query`, `test_111_1_reembed_kickoff`) fail identically at HEAD~1 with my three source files checked out at their pre-change versions. CLAUDE.md records ~68 as the established rot baseline.

### Self-Check: PASSED
- [x] `embedded_at` nullable, no default — no pre-existing chunk gets a fabricated timestamp
- [x] All FOUR write sites set it
- [x] `resize_embedding_column` NULLs it alongside the vector
- [x] Migration applied via SQL editor (never `db push`/`reset`), artifact regenerated + committed
