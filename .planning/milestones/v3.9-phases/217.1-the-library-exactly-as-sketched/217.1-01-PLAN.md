---
phase: 217.1-the-library-exactly-as-sketched
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/sketches/218-the-library-and-its-tabs/drive.cjs
  - .planning/sketches/218-the-library-and-its-tabs/index.html
  - .planning/sketches/218-the-library-and-its-tabs/BUILD-CONTRACT.generated.md
  - .planning/sketches/218-the-library-and-its-tabs/BUILD-CONTRACT.composition.json
  - frontend/src/components/library/__generated__/sketchComposition.json
  - frontend/src/components/library/__tests__/sketchComposition.test.tsx
  - .planning/phases/217.1-the-library-exactly-as-sketched/217.1-BASELINE.md
autonomous: true
requirements: [LIB-01, LIB-02, LIB-03, LIB-04]
must_haves:
  truths:
    - "Running `node drive.cjs --emit` writes a machine-readable composition contract, generated from the live sketch markup, never hand-transcribed"
    - "The sketchComposition fence suite exists, mounts the current tree's tab bodies, and its RED output (the failing block/button assertions) is captured verbatim to 217.1-BASELINE.md before any other wave lands"
    - "The suite is NOT yet adopted into the count gate's TARGETS or BASELINE knobs — it stays red and un-adopted until the phase's final wave"
  artifacts:
    - path: ".planning/sketches/218-the-library-and-its-tabs/BUILD-CONTRACT.composition.json"
      provides: "ordered block list, child atoms and button labels per screen (v-a, v-v, v-u, v-i, v-h), machine-readable"
    - path: "frontend/src/components/library/__generated__/sketchComposition.json"
      provides: "in-package copy of the same JSON, importable across the repo boundary without depending on .planning/sketches surviving milestone archival"
    - path: "frontend/src/components/library/__tests__/sketchComposition.test.tsx"
      provides: "the composition fence — mounts each tab body, asserts the emitted block list by data-testid"
    - path: ".planning/phases/217.1-the-library-exactly-as-sketched/217.1-BASELINE.md"
      provides: "the captured RED run — failing block names, from the vitest JSON report, proving the guard was seen to fire"
  key_links:
    - from: "frontend/src/components/library/__tests__/sketchComposition.test.tsx"
      to: "frontend/src/components/library/__generated__/sketchComposition.json"
      via: "static import (never a hand-copied block list)"
      pattern: "sketchComposition\\.json"
---

<objective>
Close the gate that let Phase 217 through. Extend `drive.cjs --emit` to emit **composition** assertions
per screen — the ordered block list, each block's required child atoms, and every named button — sourced
from a new `data-block` marker convention added to the sketch's own HTML, never hand-transcribed
(D-217.1-08). Add a `sketch-composition` fence suite that mounts each tab body and asserts the emitted
list by `data-testid` (D-217.1-09), drive it against the **current four-tab tree** — where it cannot pass,
because there is no Health tab and none of the four existing tabs carry the sketch's furniture yet — and
record that failing run to `217.1-BASELINE.md` as proof the guard was seen to fire before it starts
protecting anything.

Purpose: without this, 217.1 drifts for the exact reason 217 did — a green, blind contract.
Output: an emitter that writes a generated composition contract; a fence suite that reads it; a captured
RED baseline. Nothing else in this phase may land before this plan merges (D-217.1-09, "nothing else may
land before it").
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/217.1-the-library-exactly-as-sketched/217.1-CONTEXT.md
@.planning/phases/217.1-the-library-exactly-as-sketched/217.1-BUILD-TO-SKETCH.md
@.planning/sketches/218-the-library-and-its-tabs/drive.cjs
@.planning/sketches/218-the-library-and-its-tabs/index.html
@frontend/src/components/workflows/argumentVocabulary.test.ts
@frontend/src/__tests__/library/renameFence.test.ts
</context>

<interfaces>
<!-- Extracted from 217.1-RESEARCH.md §C and 217.1-PATTERNS.md "The composition gate (SC#1)". -->

**`drive.cjs`'s existing shape (read before editing):**
- `ok(name, cond, detail)` at `:25-29` — the one assertion primitive. Every new check MUST use it.
- `SURFACE_HTML` (`:44-56`) — sketch markup with commentary stripped. ⚠ **Walk `SURFACE_HTML`, never
  `HTML`.** The docblock at `:32-43` records a real prior bug: the stripper once ate one level too many
  and silently deleted an entire arm while the suite still read green. Any new walker needs its own
  non-vacuity control (assert a floor on block count per screen — never just "no crash").
- `src(rel)` (`:65-68`) — reads any repo file as text; already used to load `LibraryPage.tsx`, `tabs.tsx`,
  `documents.py`, `nav-items.ts`, `ChatLayout.tsx`, `App.tsx`, `KnowledgeHealthPage.tsx`,
  `knowledge_health.py`, `tool_dispatcher.py`, `DocumentList.tsx` (`:84-97`).
- The ordered-extraction idiom already exists twice: `A1` (`:99-102`, live TSX `<th>` order via
  `matchAll`) and `A2b` (`:117-120`, sketch DOM order via a `data-*` marker). A composition walker is a
  **third instance of the same idiom**, not a new mechanism.
- `--emit` (`:940-1026`) — a single template literal, `fs.writeFileSync`. Two new outputs join it.

**Screen ids → tab (confirmed 2026-08-29, `grep 'id="v-'`):** `v-a` documents · `v-v` views ·
`v-u` ingestion · `v-i` indexing · `v-h` health · `v-d` detail-panel (not fenced this wave; the panel is
verified in Wave 8).

**Block vocabulary already stable in the sketch markup (217.1-RESEARCH §C.2 — reuse, do not reinvent):**
`.pagehead` (crumbs/h1/pagesub) · `.tabslist > button.tabtrigger` · `.subnav > button` · `.tiles > .tile`
· `.card > header > h4` + `.cardbody` · `.kv > .k`/`.v` · `table.doclist > thead th` · `.tfoot` ·
`.dropbig` · `.stagecards > .scard` · `.chipgroup` · `.vgrid > .vcard` · `.ringwrap`/`.chartwrap`/`.hrow`
· `table.checks` · `button.btn` (every named button, all screens).

**The `data-testid` convention (measured, 1059 occurrences tree-wide — do not mint `data-block` in the
build; it is a SKETCH-side-only marker):** shipped tab-body hooks are `data-testid="views-tab"`
(`ViewsTab.tsx:46`), `data-testid="ingestion-tab"` (`IngestionTab.tsx:62`),
`data-testid="indexing-tab"` (`IndexingTab.tsx:76`), `data-testid="library-sidebar"`
(`LibraryPage.tsx:364`). Name new composition hooks `data-testid="<screen>-<block>"` (e.g.
`documents-stat-tiles`, `indexing-vector-store-card`, `views-card-grid`, `health-coverage-ring`,
`ingestion-pipeline-row`) — matching the emitted `kind`, in every wave that builds the block.

**The cross-repo-boundary import precedent (already shipped and green):**
```ts
// frontend/src/components/workflows/argumentVocabulary.test.ts:58
import buildContractSource from "../../../../.planning/sketches/214-argument-form-and-its-source/BUILD-CONTRACT.generated.md?raw"
```
`?raw` across the repo boundary works today; a `.json` import resolves natively in Vite. ⭐ Also write the
in-package copy at `frontend/src/components/library/__generated__/sketchComposition.json` so the fence
survives `/gsd:complete-milestone` archiving `.planning/sketches/` (the residual `argumentVocabulary.test.ts`
itself still owes — do not repeat it here).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Mark the sketch, extend the emitter, write the composition contract</name>
  <files>.planning/sketches/218-the-library-and-its-tabs/index.html, .planning/sketches/218-the-library-and-its-tabs/drive.cjs</files>
  <action>
  In `index.html`, add a `data-block="&lt;kind&gt;"` attribute to each top-level composition element inside
  `section#v-a`, `section#v-v`, `section#v-u`, `section#v-i`, `section#v-h` — one attribute per block per
  the vocabulary table above (e.g. `data-block="pagehead"`, `data-block="tiles"`, `data-block="stat-tiles"`,
  `data-block="dropbig"`, `data-block="stagecards"`, `data-block="doclist"`, `data-block="tfoot"`,
  `data-block="vgrid"`, `data-block="ringwrap"`, `data-block="chartwrap"`, `data-block="chipgroup"`,
  `data-block="checks"`). Do not alter any visible text, class, or the D-217.1-26..29-ruled composition —
  this task only tags what already exists in the markup for D-217.1-26's four-card health tile row and the
  MATCH STRENGTH fifth tile per the ruled decisions.
  In `drive.cjs`, above the report section, add a `composition(variantId)` function that walks
  `SURFACE_HTML`'s `section#${variantId}` in document order via `matchAll` over `data-block="..."` and
  `button.btn` labels, returning `{ blocks: [{ kind, heading, atoms: string[] }], buttons: string[] }` for
  each of `v-a, v-v, v-u, v-i, v-h`. Add `ok()` calls: per screen, assert `blocks.length > 0`; assert every
  `buttons[]` entry is non-empty; assert at least one screen carries each distinct block kind found overall
  (the non-vacuity control the stripper docblock at `:32-43` demands). In the `--emit` arm, write two new
  artifacts: (1) a `## Composition — the ordered blocks per screen` section appended to
  `BUILD-CONTRACT.generated.md` (human-readable, one table per screen); (2)
  `BUILD-CONTRACT.composition.json` containing the same `{ screen: { blocks, buttons } }` map, machine-
  readable; (3) an identical copy written to `frontend/src/components/library/__generated__/sketchComposition.json`
  (create the `__generated__` directory). Run `node .../drive.cjs --emit` and confirm all three artifacts
  are written and `node .../drive.cjs` (no `--emit`) still reports the pre-existing 200 assertions passing
  plus your new composition `ok()` calls passing (they assert the SKETCH's own markup, which is
  self-consistent by construction).
  </action>
  <verify>
    <automated>node .planning/sketches/218-the-library-and-its-tabs/drive.cjs</automated>
  </verify>
  <done>`drive.cjs` reports 0 failures including the new composition assertions; `BUILD-CONTRACT.composition.json` and the in-package copy exist and are non-empty for all five screens; re-running `--emit` twice produces byte-identical output (deterministic).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The sketch-composition fence — driven RED, evidence captured</name>
  <files>frontend/src/components/library/__tests__/sketchComposition.test.tsx, .planning/phases/217.1-the-library-exactly-as-sketched/217.1-BASELINE.md</files>
  <behavior>
    - Test: importing `sketchComposition.json` yields a non-empty object keyed by all five screens (the
      character-floor / non-vacuity control per 217.1-RESEARCH §C.5 point 4 — a `?raw`/JSON import of a
      moved or misnamed file can silently resolve empty rather than throwing).
    - Test, per screen (`v-a` documents / `v-v` views / `v-u` ingestion / `v-i` indexing / `v-h` health):
      mount the corresponding live tab body (`ViewsTab`, `IngestionTab`, `IndexingTab`, the documents lead
      from `LibraryPage`, and — for health — assert the block list is EMPTY because no `data-testid="health-tab"`
      element exists in the current tree, which is itself a correct RED assertion, not an import error).
      For each block the JSON names, assert an element with `data-testid="<screen>-<block-kind>"` exists in
      the mounted output and, where the JSON records atom/button labels, assert those text nodes are
      present. This WILL fail on the current tree for most blocks — that is the point.
    - Positive control: assert the tab bar (`data-testid="ingestion-tab"` etc.) and the page title DO
      render — proving the suite fails on missing composition, not on a broken import path.
  </behavior>
  <action>
  Use the mount harness pattern from `LibraryPage.test.tsx:26-59` (`vi.hoisted` + `vi.mock("@/hooks/useDocuments")`
  / `useFolders` / a PARTIAL `vi.mock("@/lib/api", async (importOriginal) => ...)` — a full replacement
  throws at mount the moment a child imports a symbol the mock forgot; 196-08's nine-suite failure is the
  named warning). Import the generated JSON via a relative path to
  `../__generated__/sketchComposition.json`. Write the per-screen assertions described in `<behavior>`
  using `data-testid` per the interface block's naming convention. Run the suite, capture its full failure
  output (`npx vitest run src/components/library/__tests__/sketchComposition.test.tsx --reporter=basic`
  and the vitest JSON reporter's failing-assertion list), and write it verbatim into a new
  `217.1-BASELINE.md` in the phase directory, following the `193.1-BASELINE.md` precedent: what failed,
  why (missing block/tab), and which single positive-control assertion passed. Do **not** add this suite
  to `scripts/vitest-count-gate.cjs` `TARGETS` or `BASELINE` in this task — Pitfall 4 — it stays red and
  un-adopted until the phase's final wave (217.1-18).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/library/__tests__/sketchComposition.test.tsx --reporter=basic; echo "exit=$?"</automated>
  </verify>
  <done>The suite exists, fails (non-zero exit) on the current tree with the missing-Health-tab and missing-furniture assertions as the visible failures, the positive control passes, and 217.1-BASELINE.md records the captured failure list. `grep -c sketchComposition scripts/vitest-count-gate.cjs` returns 0 (confirming non-adoption).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build tooling → generated artifact | `drive.cjs --emit` writes files consumed by a test; no runtime user input crosses this boundary |
| Test suite → live component tree | jsdom mount of existing components with mocked data hooks; no network, no real DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-217.1-SC | Tampering | none — no new package installs this task | accept | no npm/pip/cargo install in this plan; N/A |
| T-217.1-06a | Tampering | `sketchComposition.json` drifting from the sketch silently | mitigate | emitted only via `--emit`, never hand-edited; `git diff --exit-code` on the artifacts is the per-task verify signal the executor runs before commit |
| T-217.1-06b | Repudiation | the RED baseline being lost/overwritten by a later wave | mitigate | `217.1-BASELINE.md` is committed in this plan and never edited by later waves — later waves append their own green evidence to `STATE.md`/summaries, not overwrite this file |
</threat_model>

<verification>
1. `node .planning/sketches/218-the-library-and-its-tabs/drive.cjs` — 0 failures (pre-existing 200 + new composition assertions).
2. `npx vitest run src/components/library/__tests__/sketchComposition.test.tsx --reporter=basic` — fails with the expected missing-block assertions, positive control passes.
3. `217.1-BASELINE.md` exists and quotes the captured failure list verbatim.
4. `grep -c sketchComposition scripts/vitest-count-gate.cjs` → 0.
</verification>

<success_criteria>
SC#1 is mechanically satisfied for its Wave-0 half: the emitter is generated-not-transcribed, the fence
suite exists and was seen RED with evidence captured, and the suite is deliberately un-adopted from the
count gate pending the phase's final wave.
</success_criteria>

<output>
Create `.planning/phases/217.1-the-library-exactly-as-sketched/217.1-01-SUMMARY.md` when done
</output>
