---
context: phase
phase: 217.1-the-library-exactly-as-sketched
plan: 18
task: 2
total_tasks: 3
status: in_progress
last_updated: 2026-08-30T07:19:54.766Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] **CONSTRAINT: A RED COUNT GATE IS NOT AUTOMATICALLY THIS PLAN'S FAILURE** — the gate was measured VIOLATED at this phase's base on `WorkflowBuilderPage.canvas.test.tsx`, a named SEED-171 flake that is byte-unchanged. If it reds: capture failing filenames from the gate's own persisted JSON report BEFORE re-running, check each against `git diff --numstat <base> HEAD`, byte-unchanged+SEED-171 → record as observation and move on. NEVER touch `GSD_VITEST_MAX_WORKERS` (keep =2). Never adopt a red suite into BASELINE.
- [ ] **CONSTRAINT: BASELINE keys must be globally unique tree-wide** — `bareName()` strips paths, so check `grep -c '"<bare-filename>":' scripts/vitest-count-gate.cjs` before adding each pin. `git mv` to disambiguate if a collision exists (217's own `DocumentList.test.tsx` precedent).
- [ ] **CONSTRAINT: CLAUDE.md size gate** — hot-file ledger disposition cells capped at 200 chars; run `node scripts/check-claude-md-size.cjs`. A >200-char cell FAILS the gate.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Adopting a red suite into BASELINE | makes every subsequent unrelated plan's gate fail (T-217.1-22a) | blocking | confirm green in isolation (`npx vitest run <file> --reporter=basic`) before adding either knob entry |
| Re-running a red gate before capturing failing filenames | a plan can never afterwards prove its suites were innocent | blocking | read the gate's persisted JSON report first, never re-run first |
| fireEvent.click on Radix Tabs | does NOT switch tabs (measured in isolation; needs pointer events) | advisory | use `userEvent.click` |
| Trusting a disposition cell instead of re-deriving | rows rot within a day; "satisfied" stops the audit | advisory | re-derive triple fresh via `git log --follow` at close |
| Hand-editing `full-schema.sql` | regenerated artifact; never hand-edit | advisory | `bash scripts/regenerate-full-schema.sh` (no `--reset`) |

_Remove rows that do not apply._

<current_state>
Executing **Plan 18 of 18** (closeout) in Phase 217.1 (the-library-exactly-as-sketched). Task 1 and the two gate-red fixes are COMMITTED. **Task 2 is in progress** — 17 TARGETS entries added to `scripts/vitest-count-gate.cjs` (UNCOMMITTED, +24 lines); the corresponding BASELINE pins are NOT yet added. Task 3 (hot-file ledger) not started. The phase's earlier Plans 1-17 are all committed (through `af39073b5`). Branch: `develop`.
</current_state>

<completed_work>

**Plan 18 so far (committed):**
- Task 1 — sketchComposition fence GREEN against the finished five-tab tree: `4f884b3a8`. The Wave-0 fence built RED in 217.1-01 is now proven to catch composition drift (it fired, then cleared for a real reason). Per plan, `217.1-BASELINE.md` should carry a "CLEARED" note appended to the RED capture — **verify this note was written** (it was part of Task 1's action).
- The two count-gate reds fixed (both REAL, both this phase's): `f38e5f3a8` — `ChatLayout.launch.test.tsx` (`indexOf("UnknownViewFallback")` → `"<UnknownViewFallback"`) and `DetailSections.tables.test.tsx` (single `findByText` → `findAllByText` length≥1, RETRIEVAL block duplicates text).

**Uncommitted in working tree (belongs to Plan 18 closeout):**
- `scripts/vitest-count-gate.cjs` — 17 TARGETS entries for phase-created suites (the TARGETS knob; BASELINE pins still owed).
- `frontend/src/components/library/ingestionErrorVocabulary.ts` + `__tests__/ingestionFailureCopy.test.ts` — the em-dash rule-5 fix ("re-save" → "save again") and the `_EMPTY_TEXT_MESSAGES` extraction rewrite (MIME-key accumulate-on-key walk). **These are NOT yet committed — must go into the Plan 18 closeout commit.** Gate measured green with them: `total 6907 · failed 0 · pinned total 5932`.

**Earlier this phase (Plans 1-17, all committed):** five-tab Library (Documents/Views/Ingestion/Indexing/Health), sketchComposition fence + contract hooks, checked_queries backend+frontend (BE-6), RETRIEVAL block + sparkline (Plan 15), Library Health + Governance nav retirement (Plan 14), governance_health chip gate (Plan 13), ingestion error vocabulary, Health tiles/chips, Indexing reindex-everything, etc.
</completed_work>

<remaining_work>

**Task 2 (in progress): adopt every new suite into both count-gate knobs.**
- Add BASELINE pins for the 17 new suites at the gate's OWN printed counts (captured while green): sketchComposition **47**, ingestionFailureCopy **22**, IngestionTab **38**, pipelineGroups **7**, LibraryStatTiles **13**, viewRulePhrase **4**, ViewCardGrid **8**, IndexFoldersTable **6**, IndexingTab.gate **6**, HealthTiles **6**, HealthSignalChips **13**, CheckedQueriesSection **11**, ChatLayout.fallback **4**, DocumentQueriesSection **8**, FilterBar **10**, DocumentStatusBadge.a11y **19**. ⚠ **DocumentStatusBadge.test.tsx count NOT yet captured** — its TARGETS entry points at `src/components/ingestion/__tests__/DocumentStatusBadge.test.tsx` but the file may live at `frontend/src/__tests__/components/DocumentStatusBadge.test.tsx` (why it didn't print as `new`). Resolve the path, capture its count, pin it, also adopt the 217 deferred-§3 orphans (`FilterBar` done above; `DocumentStatusBadge` + `.a11y`). Check bare-name collisions. Verify grand-total delta == sum of new pins, zero residual: `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs`.
- Commit the ingestionErrorVocabulary em-dash fix + test rewrite (still uncommitted).

**Task 3: hot-file ledger closeout.**
- Re-derive triples (measured now, `git log --follow`): `tool_dispatcher.py` **67 / 4336** · `documents.py` **72 / 2535** · `knowledge_health.py` **11 / 737** · `LibraryPage.tsx` **35 / 814** · `ViewsGroup.tsx` (`frontend/src/components/ingestion/`) **5 / 259** · `DocumentList.tsx` **24 / 294** · `KnowledgeHealthPage.tsx` **DELETED by Plan 14** (12 commits via `--follow`; row must reflect the retirement).
- Verify each G-5 row + `docs/HOT-FILE-LEDGER.md` section is current, same-commit; disposition cells ≤200 chars; run `node scripts/check-claude-md-size.cjs`.
- Write `217.1-18-SUMMARY.md`, then a closeout commit (WIP-free, per plan's autonomous closeout).

**⚠ After Plan 18 — the operator's FOUR lived-experience UAT observations (2026-08-30), NOT yet addressed.** These are the most recent user request and must be routed (recorded in memory: `library-health-uat-observations`). Plan 18 does NOT cover them:
1. **Layout** — huge unused space in the upper-right area; the scroll area is too narrow. Sketch-first UX follow-up (G-2 bar: the sketch is the acceptance bar).
2. **Loading states** — show a loader/spinner while values load, NOT `Not known yet` or other values (Health tiles currently render placeholder values).
3. **Match Strength renders `6800%`** — likely `HealthTab.tsx` `toStatTiles` computes `Math.round(overview.health_score * 100)` against a value already in percent (68 → 6800). Normalize to the actual score scale.
4. **"Re-index selected" in the Indexing tab is a fake, non-working control** — wire it to the folder-scoped backend mutation and verify in the browser.
</remaining_work>

<decisions_made>

- **Count-gate adoption sequencing (from plan 217.1-18 Task 2):** run gate once → confirm each candidate green in isolation → only then add TARGETS + BASELINE, arithmetic must close with zero residual.
- **userEvent over fireEvent for Radix Tabs** (measured: fireEvent does not activate tabs).
- **Em-dash rule 5** computed by codepoint (Pitfall 8 — a literal in a docblock is itself matchable).
- **The phase's governing rule** (operator 2026-08-29): *build the sketch exactly — anything it draws is BUILT or DROPPED, never placeheld and never deferred with a trigger.* This is why the 4 UAT observations cannot be dismissed as "cosmetic."
- **217.1's SC#1 is the composition gate itself** — `drive.cjs --emit` + the sketchComposition fence, driven RED first. Phase 217's failure was composition-blindness: 200 text assertions, zero about card/table/button anatomy.
</decisions_made>

<blockers>
- **Count gate not reliably green on demand** — SEED-171 flaky suites (`WorkflowBuilderPage.canvas.test.tsx` etc.); follow the JSON-report-first procedure. None seen at Plan 18's last run (`failed 0`).
- **`DocumentStatusBadge.test.tsx` path discrepancy** — TARGETS entry path vs actual file location; capture its real printed count before pinning.
- **Operator's 4 UAT observations are UNADDRESSED** — the layout, loaders, 6800%, and fake Re-index-selected items are not owned by Plan 18. Route to a follow-up (fast-fix vs a phase) at resume.
</blockers>

## Required Reading (in order)
1. `.planning/phases/217.1-the-library-exactly-as-sketched/217.1-18-PLAN.md` — the three closeout tasks, the two-knob rule, the red-gate procedure
1. `scripts/vitest-count-gate.cjs` — TARGETS/BASELINE structure; check current uncommitted state before adding pins
1. `.planning/phases/217.1-the-library-exactly-as-sketched/217.1-BASELINE.md` — verify the Task-1 CLEARED note exists (should, post-`4f884b3a8`)
1. `CLAUDE.md` → hot-file ledger table + `docs/HOT-FILE-LEDGER.md` (the eight G-5 rows to verify in Task 3)
1. `.planning/METHODOLOGY.md` (if it exists) — project analytical lenses

## Critical Anti-Patterns (do NOT repeat these)
- **RED gate → cap tuning:** cap stays 2; the cap is NOT the remedy (measured false across ~15 runs in Phase 195).
- **FireEvent on Radix Tabs** → use userEvent.
- **Placeholder values during loading** → the UAT observation #2: tiles must show skeletons, not `Not known yet`.
- **Reindex control that looks live but is not wired** → UAT observation #4: a fake control is a defect against the sketch, not cosmetic.

## Infrastructure State
- Working tree on `develop`; no worktree in use this session (main tree).
- Local Supabase expected up (port block `54320-54335` excluded persistently on this box, 2026-08-15). No migrations pending — migration 141 (checked_queries) was applied via SQL editor + full-schema regenerated at Plan 16.
- Frontend gate runs require `GSD_VITEST_MAX_WORKERS=2`.

<context>
Plan 18 is the mechanical closeout: prove the Wave-0 composition gate clears, adopt every phase-created suite into both count-gate knobs at the printed count, and leave the hot-file ledger current. The uncommitted working tree holds exactly Task 2's TARGETS additions plus the em-dash vocabulary fix — nothing should be lost. After Plan 18 commits, the phase's own closeout is done, and the four operator UAT observations become the next piece of work (they are against the same sketch, so they land as a follow-up under the same build-to-sketch rule, likely `/gsd:fast` or `/gsd:quick` per G-3, or a small plan if they need the layout rethink).
</context>

<next_action>
Resume Plan 18 Task 2: locate the real `DocumentStatusBadge.test.tsx` path and capture its `-- N new` count, then add ALL BASELINE pins (the 16 captured + DocumentStatusBadge + confirm the two orphans FilterBar/DocumentStatusBadge.a11y) into `scripts/vitest-count-gate.cjs` after checking bare-name collisions, run `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` and verify zero residual. Then commit the count-gate pins TOGETHER with the uncommitted `ingestionErrorVocabulary.ts` + `ingestionFailureCopy.test.ts` fix. Then Task 3 (ledger, using the triples measured above, `KnowledgeHealthPage.tsx` = DELETED), then `217.1-18-SUMMARY.md` + closeout commit. Then route the operator's 4 UAT observations.
</next_action>
