---
phase: 217.1-the-library-exactly-as-sketched
plan: 18
type: execute
wave: 16
depends_on: ["217.1-17"]
files_modified:
  - scripts/vitest-count-gate.cjs
  - frontend/src/components/library/__tests__/sketchComposition.test.tsx
  - .planning/phases/217.1-the-library-exactly-as-sketched/217.1-BASELINE.md
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md
autonomous: true
requirements: [LIB-01, LIB-02, LIB-03, LIB-04]
must_haves:
  truths:
    - "The sketchComposition fence (Wave 0) now runs GREEN against the fully-built five-tab tree — the guard that was seen RED in Wave 0 is now proven to actually catch composition drift, because it once did and now doesn't"
    - "Every suite this phase created is adopted into BOTH count-gate knobs — TARGETS (as named files, since there is no src/components/library directory entry) and BASELINE (pinned at the gate's own printed -- N new, never while red)"
    - "The 217 deferred-§3 suites (DocumentStatusBadge.test.tsx, DocumentStatusBadge.a11y.test.tsx, FilterBar.test.tsx) are adopted, closing their re-open trigger"
    - "Every in-scope suite named in 217.1-VALIDATION.md runs explicitly and is green, and no gated file DECREASES against the pre-phase baseline. \u26a0 NOT 'the gate is globally green' \u2014 it was measured VIOLATED at this phase's base on a named SEED-171 flake, so that criterion can fail for reasons no plan controls"
    - "Every G-5 hot-file row this phase touched (LibraryPage.tsx, DocumentList.tsx, KnowledgeHealthPage.tsx retirement, knowledge_health.py, document_governance.py, tool_dispatcher.py, documents.py, ViewsGroup.tsx) is present and current in docs/HOT-FILE-LEDGER.md and CLAUDE.md's ledger table"
  key_links:
    - from: "scripts/vitest-count-gate.cjs"
      to: "frontend/src/components/library/__tests__/sketchComposition.test.tsx"
      via: "TARGETS entry (named file) + BASELINE pin at the gate's own printed count, added ONLY after the suite is confirmed green"
      pattern: "sketchComposition"
---

<objective>
The closing plan, matching 217-12's own precedent ("the fourteen entries 217-12 landed in one closing
plan"). Three jobs, in order: (1) confirm the Wave 0 `sketchComposition` fence — built RED on purpose in
217.1-01 — now passes GREEN against the fully-built five-tab tree, which is the actual proof that SC#1's
gate works, not merely that it exists; (2) adopt every suite this phase created into BOTH count-gate
knobs, at the gate's own printed `-- N new`, never while red, per Pitfall 4 and the TARGETS/BASELINE
two-knob rule; (3) close out the G-5 hot-file ledger obligations this phase incurred (`tool_dispatcher.py`
had NO row at all before this phase — Plan 11 added it; verify it, and every other touched row, is
current here at phase close).

Purpose: this is where "driven RED first" becomes "proven to catch drift" — a guard nobody has seen both
fire and clear is not yet a guard.
Output: a green sketchComposition suite, both count-gate knobs fully updated, the hot-file ledger current.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/217.1-the-library-exactly-as-sketched/217.1-CONTEXT.md
@.planning/phases/217.1-the-library-exactly-as-sketched/217.1-BASELINE.md
@scripts/vitest-count-gate.cjs
@docs/HOT-FILE-LEDGER.md
@CLAUDE.md
</context>

<interfaces>
<!-- Extracted from 217.1-RESEARCH.md §C.5, §C.6 and 217.1-PATTERNS.md "Shared Pattern G". -->

**The two-knob rule, restated because getting it backwards is Pitfall 4:** `TARGETS` decides what the
gate EXECUTES; `BASELINE` decides what it GUARDS (fails if the count decreases). `TARGETS` reaches
`src/pages`/`src/components/library` by NAMED FILE ONLY — there is no directory entry for either, so every
suite this phase created under `src/components/library/__tests__/` or named elsewhere must be listed
explicitly or the gate never runs it at all.

**Every suite this phase is expected to have created (cross-check against actual `git status`/`find`
output before finalizing — this list is a floor, not a ceiling):**
`sketchComposition.test.tsx` · `ingestionFailureCopy.test.ts` · `IngestionTab.test.tsx` ·
`pipelineGroups.test.ts` · `LibraryStatTiles.test.tsx` · `viewRulePhrase.test.ts` ·
`ViewCardGrid.test.tsx` · `IndexFoldersTable.test.tsx` · `IndexingTab.gate.test.tsx` ·
`HealthTiles.test.tsx` · `HealthSignalChips.test.tsx` · `ChatLayout.fallback.test.tsx` ·
`DocumentQueriesSection.test.tsx` (extended) · `CheckedQueriesSection.test.tsx` · plus the ADOPTED but
pre-existing `DocumentStatusBadge.test.tsx`, `DocumentStatusBadge.a11y.test.tsx`, `FilterBar.test.tsx`.

**Sequencing — never adopt a red suite:**
1. Run the full frontend suite once, unmodified, to get the gate's own printed `-- N new` for each
   candidate file.
2. Confirm every candidate is green in isolation (`npx vitest run <file> --reporter=basic`).
3. Only then add TARGETS entries (named files) and BASELINE pins (bare filename → the printed count) —
   the arithmetic must close with zero residual (the grand-total delta must equal the sum of the new
   pins' counts) per the ledger's own "growth vs. drift" rule.

**`bareName()` strips the path** — every BASELINE key must be globally unique tree-wide; check for a
collision (`grep -c '"<bare-filename>":' scripts/vitest-count-gate.cjs`) before adding a key, and `git mv`
to disambiguate if one exists (217's own precedent for `DocumentList.test.tsx`).

**Hot-file ledger — same-commit sync rule, verify not merely re-derive:** for every file this phase
touched that fires G-5 (per the six named in the phase's `hard_constraints`, plus `tool_dispatcher.py`,
`documents.py`, `ViewsGroup.tsx` per RESEARCH §G's "three files with no row at all"), confirm: (a) a row
exists in `CLAUDE.md`'s hot-file table, (b) a matching section exists in `docs/HOT-FILE-LEDGER.md`, (c)
the disposition cell is ≤200 chars (`node scripts/check-claude-md-size.cjs`), (d) the triple (commits/
phases/lines) is re-derived fresh via `git log --follow`, not copied from an earlier plan's summary.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Prove the gate — sketchComposition GREEN against the finished tree</name>
  <files>frontend/src/components/library/__tests__/sketchComposition.test.tsx, .planning/phases/217.1-the-library-exactly-as-sketched/217.1-BASELINE.md</files>
  <action>
  Run `sketchComposition.test.tsx` against the fully-built tree. If it is not green, that is a real
  finding — diagnose which block/atom/button is missing or mismatched against the emitted contract and
  fix the composition (not the test) before proceeding; do not weaken an assertion to make it pass. Once
  green, append a short "CLEARED" note to `217.1-BASELINE.md` (never overwrite the original RED capture)
  recording the date and the full green run's summary line, so the file shows both halves of the guard's
  proof — that it fired, and that it later cleared for a real reason.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/library/__tests__/sketchComposition.test.tsx --reporter=basic</automated>
  </verify>
  <done>The composition fence is green against the finished five-tab tree; 217.1-BASELINE.md records both the original RED capture and this GREEN clearance, neither overwriting the other.</done>
</task>

<task type="auto">
  <name>Task 2: Adopt every new suite into both count-gate knobs, at the printed count, never while red</name>
  <files>scripts/vitest-count-gate.cjs</files>
  <action>
  Run the full frontend suite once to get the gate's own printed `-- N new` per candidate file. Confirm
  every suite in the interface block's list (plus any this phase actually created that is not listed —
  cross-check against `git diff --stat` for the whole phase) is green in isolation. Add each as a
  `TARGETS` entry (named file path, since neither `src/pages` nor `src/components/library` has a directory
  entry) and a `BASELINE` key (bare filename → printed count), checking for bare-name collisions first.
  Adopt the three 217 deferred-§3 orphans (`DocumentStatusBadge.test.tsx`,
  `DocumentStatusBadge.a11y.test.tsx`, `FilterBar.test.tsx`) the same way, closing their re-open trigger.
  Confirm the grand-total delta equals the sum of all newly-pinned counts with zero residual before
  committing.
  </action>
  <verify>
    <automated>GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs</automated>
  </verify>
  ⛔ **A RED GATE IS NOT AUTOMATICALLY THIS PLAN'S FAILURE, AND THE CAP IS NOT THE REMEDY.** The gate was
  measured **VIOLATED at this phase's own base** — `total 6666 · failed 1 · pinned 5932` — on
  `WorkflowBuilderPage.canvas.test.tsx`, a **named SEED-171 suite** that was byte-unchanged. CLAUDE.md
  records that `count gate OK` is **not reliably reachable on demand**.

  **If it reds, follow this procedure and do NOT touch `GSD_VITEST_MAX_WORKERS`:**
  1. Capture the failing filenames from the gate's **own persisted JSON report** — ⚠ **before re-running
     anything**. A plan that re-runs first can never afterwards prove its suites were innocent.
  2. Check each name against `git diff --numstat <phase base> HEAD` and `git status --short`.
  3. **Byte-unchanged and one of SEED-171's five** → record as an observation and proceed. Say *"provably
     unmodified"*, never *"fine"* — ⚠ **one green sample of a flaky suite proves nothing.**
  4. **Anything else is REAL.** Phase 196 hit `failed 249` and every one was a genuine missing mock.
  ⛔ **Never adopt a red suite into BASELINE.**

  <done>Every suite this phase created or adopted is in both TARGETS and BASELINE; the grand-total arithmetic closes with zero residual; every in-scope suite named in 217.1-VALIDATION.md was run explicitly and is green; and any residual red is a named SEED-171 suite proven byte-unchanged by the procedure above, recorded as an observation with its filename. ⚠ The criterion is the per-file deltas and the explicitly-run in-scope suites — which are deterministic — NOT a globally green gate, which is not reliably reachable.</done>
</task>

<task type="auto">
  <name>Task 3: Close the hot-file ledger obligations, verify current, same-commit</name>
  <files>docs/HOT-FILE-LEDGER.md, CLAUDE.md</files>
  <action>
  Re-derive the triple (`git log --follow` commits/phases/lines) for every file this phase touched that
  fires G-5, including the three RESEARCH §G names with no prior row (`tool_dispatcher.py`,
  `documents.py`, `ViewsGroup.tsx`) — verify Plan 11's `tool_dispatcher.py` row (already added there) is
  still current, and add/verify rows for any of the others still missing. Update `CLAUDE.md`'s hot-file
  table row (verdict only, ≤200 chars) and the matching `docs/HOT-FILE-LEDGER.md` section (the full
  reasoning) for each, in this commit. Run the CLAUDE.md size gate.
  </action>
  <verify>
    <automated>node scripts/check-claude-md-size.cjs</automated>
  </verify>
  <done>Every G-5-firing file this phase touched has a current row and section, re-derived fresh, disposition cells under 200 chars, and the size gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | this plan closes tooling/gate/ledger obligations; it introduces no new runtime code path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-217.1-22a | Tampering | adopting a suite that is actually red, making every subsequent unrelated plan's gate fail | mitigate | Task 2's explicit sequencing — confirm green in isolation before adding either knob entry |
| T-217.1-SC | Tampering | npm/pip/cargo installs | accept | no new package installs in this plan |
</threat_model>

<verification>
1. `cd frontend && npx vitest run src/components/library/__tests__/sketchComposition.test.tsx --reporter=basic`
2. `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` — read the verdict line verbatim
3. `node scripts/check-claude-md-size.cjs`
4. `cd frontend && npx tsc -p tsconfig.app.json --noEmit`
5. `backend/venv/Scripts/python -m pytest backend/tests -x -q` (or the phase's own `test_2171_*` subset at minimum)
</verification>

<success_criteria>
SC#1's Wave-0 gate is proven to have caught real drift and later cleared it; every suite this phase built
is guarded by the count gate; the hot-file ledger reflects reality. This is the phase's own closing
condition — every ROADMAP success criterion (SC#1-13) traces to a plan above; this plan is the mechanical
proof that the tooling protecting all of them is switched on.
</success_criteria>

<output>
Create `.planning/phases/217.1-the-library-exactly-as-sketched/217.1-18-SUMMARY.md` when done
</output>
