---
phase: 217.1-the-library-exactly-as-sketched
plan: 10
status: complete
started: 2026-08-30T01:15:00.000Z
completed: 2026-08-30T02:50:00.000Z
tasks: 2
---

## Plan 217.1-10: The Indexing tab's three cards over `GET /library/index-summary`

ROADMAP SC#6 satisfied — the sketch's Indexing tab (screen 005) is real, and Phase 217's deferred WR-02 is closed.

### Task 1 — Three cards + gated actions + the honest Root row

**`VectorStoreCard.tsx`** — Vectors · Chunks indexed · Documents with no vectors · Last indexed. Unknown → `Not known yet`, never `0`. `Last indexed` is `max(embedded_at)` (BE-1); null → `never`.

**`EmbeddingModelCard.tsx`** — Model · Dimensions · Provider + the gated `Change model` button (absent when `canManage` false — VANISH, never disabled).

**`FoldersIndexTable.tsx`** — `Folder · Documents · Chunks · Vectors · Last indexed · Actions`:
- ⛔ The **honest Root row**: zero-chunk folder reads `–`/`–`/`–` and `never`, NEVER `0` and never a tick (D-217.1-31 — "the point of the tab").
- ⛔ **Re-index selected gating** (Open Question 1): zero-chunk folder's Actions cell = quiet `—` (a control that cannot do its verb is cut). Stale non-empty folder → button. Already-current (BE-3's `scope: "already_current"`) → "Already indexed with the current model." on click, not false success.
- `kickReembed` extended with `{ folder_ids }` (the BE-3 body).

**`lib/api/library.ts`** (new) — `getIndexSummary()` client fn; `IndexSummary`/`FolderIndexRow` wire types in `settings.ts`; exported from the barrel.

### Task 2 — IndexingTab composition, single fetch, Change model routing

**`IndexingTab.tsx`** — ONE `useEffect` fetches `getIndexSummary()`; the result feeds all three cards as props (no card fetches independently). `ReembedStatusCard` stays composed **unchanged** (D-217-16). `canManage` via `useEffectiveFeaturesOptional()` — the three ACTION buttons VANISH for non-operators (T-217.1-02). `Change model` = the exact `onNavigate("settings")` + `scrollIntoView(#reembed-status-card)` shape.

**`LibraryPage.tsx`** — passes `onNavigate` to the `IndexingTab` mount.

### Verification
- `IndexFoldersTable.test.tsx` — **6/6** (rows, honest Root `–`/`never`, per-row reindex gate, zero-chunk no-action, already-current message, VANISH)
- `IndexingTab.gate.test.tsx` — **6/6** (facts for everyone, one fetch, ReembedStatusCard mount, unreachable stated, ⭐ T-217.1-02 negative control: no gated control in DOM when `model_management` false)
- `LibraryPage.test.tsx` — **16/16** (the pre-restructure "coverage numerator/denominator" case updated to assert the Plan-10 three-card composition; `getIndexSummary` stubbed in the partial `@/lib/api` mock)
- `npx tsc --noEmit` clean · `npx vite build --logLevel error` clean · `drive.cjs` — **221 passed · 0 failed**

### Self-Check: PASSED
- [x] Three cards render from one ungated fetch for every user
- [x] Gated actions render only when `model_management` is true — absent, never disabled
- [x] Root row reads `–`/`never`, never `0`/tick
- [x] Change model routes to Settings' picker, never a second one
