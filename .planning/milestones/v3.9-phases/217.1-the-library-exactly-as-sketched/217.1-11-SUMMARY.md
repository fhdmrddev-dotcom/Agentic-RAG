# Plan 11 SUMMARY — BE-4 error-path audit write + BE-5 similarities key

**Phase:** 217.1-the-library-exactly-as-sketched
**Plan:** 11
**Type:** execute (checkpoint — `autonomous: false`)
**Wave:** 9

## Overview

BE-4: the `_handle_search_documents` except block now writes a `search.query` audit row before returning, so a provider outage is visible in the analytics (LIB-06).

BE-5: the same success-path dict literal now also carries `similarities: {document_id: max_similarity}` — the per-hit similarity `retrieval_service.py` returns was being thrown away before reaching `audit_log.metadata`, so `Average relevance` had no data to average (LIB-07).

## Changes

| File | Change |
|------|--------|
| `backend/app/services/tool_dispatcher.py` | BE-4: error-path `write_audit_entry` with `document_ids: []` + `retrieval_status: "provider_error"`; BE-5: `similarities` key on the success-path dict literal |
| `backend/app/api/knowledge_health.py` | `_fetch_retrieval_trend` → three-way branch: `could_not_search` / `found_something` / `found_nothing`; `retrieval_count` byte-unchanged (error rows excluded) |
| `backend/app/api/document_queries.py` | `DocumentQueryRow.similarity: float \| None`; read from `meta.get("similarities", {}).get(document_id)` |
| `backend/tests/test_2171_trend_segments.py` | Extended with `test_error_row_does_not_change_retrieval_count` (D-217.1-34 regression); characterization test was committed FIRST |
| `backend/tests/test_2171_search_error_audit.py` | 5 tests: error write payload, no similarities on error, success similarities shape+rounding, zero-hit empty similarities, JSONB shape negative control |
| `docs/HOT-FILE-LEDGER.md` | New `tool_dispatcher.py` section; `knowledge_health.py` section re-derived with 217.1-11 edit |
| `CLAUDE.md` | New `tool_dispatcher.py` hot-file row; `knowledge_health.py` row re-derived |

## Decisions

1. **The view/filter `search.query` writer at `tool_dispatcher.py:952-966` does NOT get the error-path treatment.** Its failure mode is a caught-and-absorbed resolve error that does not represent a provider outage. A future LIB-06 undercount should re-open this decision.

2. **`retrieval_count`'s meaning is a contract pinned by test.** Every non-error `search.query` row counts; error rows never do. The characterization test at `test_2171_trend_segments.py:44-68` was committed FIRST (`b960f7f52`) before the edit, per D-217.1-34's ordering requirement.

3. **D-217.1-34 ordering observed.** Task 1 (characterization test) was committed against the UNMODIFIED `_fetch_retrieval_trend` before Task 2's edits — verified passing before the edit.

4. **`similarities` is max-per-document, 3dp.** A document can contribute several chunks with different similarity scores; we persist the max, rounded to 3dp, matching `_fetch_low_confidence_queries`'s existing convention.

## Tests

- `test_2171_trend_segments.py`: 2 tests — characterization pin (committed pre-edit), error-row regression (committed post-edit)
- `test_2171_search_error_audit.py`: 5 tests — error write payload, no similarities on error, success similarities, zero-hit empty similarities, JSONB shape negative control
- **All 7 pass.** Build imports clean.

## Ledger hygiene

- `tool_dispatcher.py`: first CLAUDE.md hot-file row + HOT-FILE-LEDGER.md section
- `knowledge_health.py`: row re-derived (10/5/737), section updated for the BE-4 structural edit
- CLAUDE.md size gate: OK (<200-char dispositions)