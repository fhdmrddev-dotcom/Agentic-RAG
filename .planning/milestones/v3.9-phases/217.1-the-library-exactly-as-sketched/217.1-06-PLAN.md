---
phase: 217.1-the-library-exactly-as-sketched
plan: 06
type: execute
wave: 5
depends_on: ["217.1-04"]
files_modified:
  - frontend/src/components/library/LibraryStatTiles.tsx
  - frontend/src/components/library/LibraryBreadcrumb.tsx
  - frontend/src/components/library/DocumentsPager.tsx
  - frontend/src/pages/LibraryPage.tsx
  - frontend/src/pages/__tests__/LibraryPage.test.tsx
  - frontend/src/components/library/__tests__/LibraryStatTiles.test.tsx
autonomous: true
requirements: [LIB-01]
must_haves:
  truths:
    - "Documents shows three stat tiles — CHUNKS, VECTORS, FOUND BY A SEARCH — each with an unreachable-source arm that never renders 0"
    - "The breadcrumb reads Library > <folder> > <tab>, using the already-shipped FolderBreadcrumb"
    - "A client-side pager (Rows per page: 25, 1-N of M, prev/next) renders under the Documents table"
    - "No sparkline renders on CHUNKS or VECTORS — the number stands alone (D-217.1's own sparkline drop)"
  artifacts:
    - path: "frontend/src/components/library/LibraryStatTiles.tsx"
      provides: "three stat tiles, modeled exactly on HealthStatBar.tsx's label/value/description shape"
    - path: "frontend/src/components/library/DocumentsPager.tsx"
      provides: "client-side pager with a Rows-per-page select, extending PaginationControls.tsx's fixed-limit shape"
    - path: "frontend/src/components/library/LibraryBreadcrumb.tsx"
      provides: "Library > folder > tab breadcrumb, composed from the already-shipped FolderBreadcrumb"
  key_links:
    - from: "frontend/src/components/library/LibraryStatTiles.tsx"
      to: "@/lib/api"
      via: "getReembedProgress() for VECTORS, getHealthOverview() for FOUND BY A SEARCH; CHUNKS sums chunk_count over already-loaded documents"
      pattern: "getReembedProgress|getHealthOverview"
---

<objective>
Build the Documents tab's remaining page-level furniture, independent of `DocumentList`/`DocumentRow`
(Plan 05, no file overlap — this plan runs in the same wave): three stat tiles (CHUNKS, VECTORS, FOUND BY
A SEARCH), the `Library › <folder> › <tab>` breadcrumb, and a client-side pager. CHUNKS and VECTORS both
drop their sparklines outright (neither has stored history — a line drawn from one point is the *Embedding
Quality 92%* lie in another shape); the numbers stand alone. `GET /documents` is unpaginated
(`documents.py:726-760`), so the pager's denominator is real client-side, bounded by PostgREST's 1000-row
ceiling (WR-04's trap).

Purpose: the remaining Documents-tab elements the sketch draws that Wave 3a's row extraction does not
touch.
Output: `LibraryStatTiles.tsx`, `LibraryBreadcrumb.tsx`, `DocumentsPager.tsx`, wired into `LibraryPage.tsx`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/217.1-the-library-exactly-as-sketched/217.1-CONTEXT.md
@frontend/src/pages/LibraryPage.tsx
@frontend/src/components/health/HealthStatBar.tsx
@frontend/src/components/health/PaginationControls.tsx
@frontend/src/components/ingestion/FolderBreadcrumb.tsx
</context>

<interfaces>
<!-- Extracted from 217.1-RESEARCH.md §D.1 and 217.1-PATTERNS.md "Shell" + "Documents tab (Wave 3)". -->

**Stat tiles — exact analog, whole file:** `components/health/HealthStatBar.tsx` — `label · big number ·
description`, already a four-card grid shape. Reuse it verbatim for three cards:
- **CHUNKS** — sum `doc.chunk_count` over the currently loaded `documents` array (already held by
  `LibraryPage`, no new fetch), description `across N documents`.
- **VECTORS** — `getReembedProgress()` (already called at `IndexingTab.tsx:56`; no new call needed —
  `LibraryPage` can call it once and pass down, or the tile self-fetches like `IndexingTab` does today),
  description = the model name.
- **FOUND BY A SEARCH** — `getHealthOverview()` → `retrieved_this_month` (`lib/api/knowledge.ts:23`),
  description `last 30 days`.
⛔ **No sparkline on any of the three** — this is a DROP, not a deferral. An unreachable source (a failed
fetch) renders the honest-unknown arm (`"Not known yet"`), never `0`.

**Breadcrumb — already shipped, already imported:** `FolderBreadcrumb.tsx`, already mounted at
`LibraryPage.tsx:518`. `LibraryBreadcrumb.tsx` is a thin composition: `Library › <FolderBreadcrumb output>
› <current tab label>`, placed in the header block at `LibraryPage.tsx:573-578`.

**Pager — extend the fixed-limit analog, do not rebuild:** `components/health/PaginationControls.tsx`
(45 L) is the exact pager shape but takes `limit` as a fixed prop with no control to change it. Add ONE
`Select` (import shape from `MoveToFolderDialog.tsx:9-15`) offering `Rows per page: 25` and rebuild
`DocumentsPager.tsx` around it, keeping `PaginationControls`' prev/next/page-count logic. The denominator
is `filtered.length` inside `DocumentList`/`LibraryPage` — bounded: if `documents.length >= 1000` (the
PostgREST default max-rows ceiling, `reembed_service.py:255-258`'s named trap, also WR-04), render a
"list capped" arm rather than a confidently wrong total.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stat tiles and breadcrumb</name>
  <files>frontend/src/components/library/LibraryStatTiles.tsx, frontend/src/components/library/LibraryBreadcrumb.tsx, frontend/src/components/library/__tests__/LibraryStatTiles.test.tsx</files>
  <behavior>
    - Test: CHUNKS renders the sum of `chunk_count` across a documents fixture and `across N documents`.
    - Test: VECTORS renders the vector count and model name from a `getReembedProgress` fixture; when the
      fetch fails/is unreachable, it renders the honest-unknown arm, never `0`.
    - Test: FOUND BY A SEARCH renders `retrieved_this_month` and `last 30 days`.
    - Test: no sparkline element (no `svg`/`polyline`/chart element) is present in any of the three tiles.
    - Test: `LibraryBreadcrumb` renders `Library › <folder> › <tab>` composing the existing
      `FolderBreadcrumb` output with the current tab's label.
  </behavior>
  <action>
  Build `LibraryStatTiles.tsx` as three `HealthStatBar.tsx`-shaped cards per the interface block, each with
  its own honest-unknown fallback. Build `LibraryBreadcrumb.tsx` as a thin wrapper composing the already-
  mounted `FolderBreadcrumb` with the active tab's label. Do not add a sparkline anywhere.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/library/__tests__/LibraryStatTiles.test.tsx --reporter=basic</automated>
  </verify>
  <done>Three tiles render with correct values and honest-unknown fallbacks, no sparkline element exists; the breadcrumb composes correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Client-side pager and LibraryPage wiring</name>
  <files>frontend/src/components/library/DocumentsPager.tsx, frontend/src/pages/LibraryPage.tsx, frontend/src/pages/__tests__/LibraryPage.test.tsx</files>
  <behavior>
    - Test: `DocumentsPager` renders `Rows per page: 25` (a `Select`), the current range (`1–25 of N`), and
      prev/next controls; changing rows-per-page updates the visible range.
    - Test: when `documents.length >= 1000`, the pager renders a "list may be larger" honest arm instead of
      a confident exact total.
    - Test (regression): `LibraryPage.test.tsx`'s existing suite passes unmodified — the tiles, breadcrumb,
      and pager are additive mounts, not replacements of existing Documents-tab structure.
  </behavior>
  <action>
  Build `DocumentsPager.tsx` extending `PaginationControls.tsx`'s prev/next logic with a `Rows per page`
  `Select`. Mount `LibraryStatTiles`, `LibraryBreadcrumb`, and `DocumentsPager` into `LibraryPage.tsx`'s
  `documentsLead`/header block (`:493-578` region) — additive mounts only; do not alter the existing
  `DocumentList`/filter-bar composition. Re-run the existing `LibraryPage.test.tsx` suite to confirm no
  regression.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/__tests__/LibraryPage.test.tsx --reporter=basic</automated>
  </verify>
  <done>The pager renders and paginates client-side with a rows-per-page control and the 1000-row honest arm; LibraryPage.test.tsx passes unmodified plus the new additive mounts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | client-side composition + pagination over already-authenticated, already-fetched data; no new endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-217.1-11a | Denial of Service (client) | pager rendering a confidently wrong total above PostgREST's 1000-row ceiling | mitigate | the `length >= 1000` honest arm, tested explicitly |
| T-217.1-SC | Tampering | npm/pip/cargo installs | accept | no new package installs in this plan |
</threat_model>

<verification>
1. `cd frontend && npx vitest run src/components/library/__tests__/LibraryStatTiles.test.tsx --reporter=basic`
2. `cd frontend && npx vitest run src/pages/__tests__/LibraryPage.test.tsx --reporter=basic`
3. `node .planning/sketches/218-the-library-and-its-tabs/drive.cjs`
</verification>

<success_criteria>
Remainder of ROADMAP SC#4 satisfied: three stat tiles (no sparklines), breadcrumb, and pager.
</success_criteria>

<output>
Create `.planning/phases/217.1-the-library-exactly-as-sketched/217.1-06-SUMMARY.md` when done
</output>
