---
phase: 214-a-step-names-its-service-and-its-action
plan: 10
type: execute
wave: 3
depends_on: ["214-02", "214-03", "214-05"]
files_modified:
  - frontend/src/components/workflows/PublishGauntlet.tsx
  - frontend/src/components/workflows/PublishRefusalList.tsx
  - frontend/src/components/workflows/PublishGauntlet.test.tsx
  - frontend/src/components/workflows/PublishRefusalList.test.tsx
autonomous: true
requirements: [STEP-03]

must_haves:
  truths:
    - "D-214-09: five refusals, not one — each source arm is proved on its own terms, so each failure is a different fact about the world and gets its own sentence."
    - "Every refusal names the step in the AUTHOR'S OWN WORDS, and the phase slug appears nowhere."
    - "`shape-unknown` is the one refusal that must NOT name an argument, and it offers re-discovery rather than an edit."
    - "No headline names a gauntlet stage, carries an exclamation, or uses a severity word."
    - "D-214-12: the surface STATES that the gate is not retroactive — already-published workflows keep running."
    - "The client COMPOSES the sentences from `publishRefusalVocabulary.ts`; the backend supplies kind + step name + argument name and never the English."
  artifacts:
    - path: "frontend/src/components/workflows/PublishRefusalList.tsx"
      provides: "the cause block: one headline and at least one next action per refusal, keyed by the IMPORTED ArgumentGapKind union"
      contains: "REFUSAL_FOR_KIND"
    - path: "frontend/src/components/workflows/PublishGauntlet.test.tsx"
      provides: "the stage-spine invariants — exactly one blocked stage, every later stage not reached"
      contains: "STAGE_NOT_REACHED"
  key_links:
    - from: "frontend/src/components/workflows/PublishRefusalList.tsx"
      to: "frontend/src/components/workflows/publishRefusalVocabulary.ts"
      via: "REFUSAL_FOR_KIND, a compiler-enforced Record over the five gap kinds"
      pattern: "REFUSAL_FOR_KIND"
    - from: "backend/app/services/harness/publish_service.py"
      to: "frontend/src/components/workflows/PublishRefusalList.tsx"
      via: "named_failures entries carrying code + step_name + argument + upstream"
      pattern: "step_name"
---

<objective>
Render the STEP-03 refusal: the cause first, in the author's own words, with a next action — and the
stage spine beneath it.

Purpose: sketch 215 is the acceptance bar and its §4 states the design argument the surface must
honour: **a single *"this step is invalid"* headline would collapse all five kinds, which is
`BUG-260815-06`'s stage-naming failure in a new costume.** D-214-13 declined to repair the other five
stages' copy precisely because **this** refusal is written correctly from birth.

Output: one new refusal list component, the gauntlet's block region wired to it, and a suite carrying
sketch 215's thirteen invariants.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⚠ **FIRST ACTION IN ANY WORKTREE:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/214-a-step-names-its-service-and-its-action/214-CONTEXT.md
@.planning/phases/214-a-step-names-its-service-and-its-action/214-PATTERNS.md
@.planning/sketches/215-publish-refuses-by-name/BUILD-CONTRACT.generated.md
@.planning/reported-bugs/BUG-260815-06-structural-gate-refusal-names-nothing-actionable.md
@.planning/phases/214-a-step-names-its-service-and-its-action/214-03-SUMMARY.md
@.planning/phases/214-a-step-names-its-service-and-its-action/214-05-SUMMARY.md

⚠ **`BUG-260815-06` is read here to be honoured, not fixed.** Its trigger fired at this phase and was
**declined a second time** (D-214-13). The other five stages' refusal copy is **not touched** by this
plan; only the new refusal is written correctly. Two declines is a signal the next phase must weigh —
do not silently repair it here and do not silently ignore it.

⚠ **Read `.claude/skills/sketch-findings-agentic-rag/references/icon-convention.md` §4 before drawing
any mark.** Sketch 215 invariant #10 is §4's standing rule: **the top-right corner is CLAIMED by the
governance seal (D-185)**, so a blocked node's mark is an edge lane, never the corner.
</context>

<interfaces>
<!-- What this plan CONSUMES, from plan 214-05's summary. Do not re-derive it. -->

A `named_failures` entry from the new gate:
```
{ code: "no_source" | "ask_undeclared" | "upstream_unreachable" | "shape_unknown" | "unrenderable"
  phase: string          // the slug — ⛔ NEVER RENDERED
  message: string        // backend diagnostic, for logs and the receipt — ⛔ NEVER RENDERED
  step_name: string      // the AUTHORED name — this is what a person reads
  argument: string | null
  upstream: string | null }
```

⚠ **`named_failures` is POLYMORPHIC across stages** — `PublishGauntlet.tsx`'s docblock rule (item 4)
is **key-detection per ENTRY, never a switch on `blocked_stage`**, and any bare string or
missing-key entry must still render. **Preserve that discipline**: the new renderer detects its own
five codes by key and falls through to the shipped generic renderer for everything else.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The cause block — five kinds, one headline each, a next action each</name>
  <files>frontend/src/components/workflows/PublishRefusalList.tsx, frontend/src/components/workflows/PublishRefusalList.test.tsx</files>
  <read_first>
    - `.planning/sketches/215-publish-refuses-by-name/BUILD-CONTRACT.generated.md` §1, §2 and §4 —
      §2's thirteen invariants are already phrased as assertions; §4's five-row table gives each kind
      its distinct *what is actually wrong* and *why it needs its own sentence*.
    - `frontend/src/components/workflows/publishRefusalVocabulary.ts` (plan `214-03`) — every string,
      and `REFUSAL_FOR_KIND`, the `Record` that makes a sixth kind a compile error rather than a
      silent fall-through. ⭐ **It is also the DECLARING HOME of the `ArgumentGapKind` union**
      (`export type ArgumentGapKind`). ⛔ **Import the type from there; never re-type the five
      literals inline.** A local copy would make plan `214-14`'s S-4 cross-language check compare
      Python against a copy of itself, and would let a sixth kind land without a compile error here.
    - `frontend/src/components/workflows/PublishGauntlet.tsx:20-40` — the four shipped rules,
      especially **KEY-DETECTION** (item 4): `named_failures` is polymorphic; detect the shape per
      ENTRY and never switch on `blocked_stage`; a bare string or missing key must still render.
    - `frontend/src/components/workflows/doorVocabulary.ts:240-265` — the governed-refusal register:
      no severity word, no exclamation, no mechanism; it names what is missing and what to do.
    - `frontend/src/lib/api/workflows.ts` — where `named_failures`' client type lives. ⚠ **Plan
      `214-02` already widened it** with `step_name?` / `argument?` / `upstream?`; **read it, do not
      edit it.** That file has one owner in this phase, in wave 1, so no two waves race it.
  </read_first>
  <action>
Build `PublishRefusalList.tsx`: one item per `named_failures` entry whose `code` is one of the five,
each rendering exactly **one** headline node (`data-testid="refusal-headline"`) composed from
`REFUSAL_FOR_KIND` and **at least one** next-action control — `REFUSE_NEXT` (*"Go to the step"*) for
four kinds, `REFUSE_NEXT_REDISCOVER` (*"Refresh actions"*) for `shape_unknown`, whose fix is
re-discovery and not an edit.

The count line uses `REFUSE_COUNT_ONE` / `REFUSE_COUNT_MANY(n)` and `REFUSE_REST_OK(n)`. ⚠ Invariant
#6: **the count must agree with the list** — derive both from the same array, never from two sources.

⚠ **Never render `phase` (the slug) and never render `message` (the backend diagnostic).** Invariant
#1 asserts both halves: the authored name IS there and the slug is NOT. The `message` field exists
for the audit receipt, and putting a backend diagnostic on an author's screen is what
`BUG-260815-06` is about.

⚠ **`shape_unknown` renders `REFUSE_SHAPE_UNKNOWN(step)` with NO argument name** (invariant #2). It is
the one refusal that must invent nothing.

The `named_failures` client type is **already widened** (plan `214-02`, wave 1) with `step_name?`,
`argument?` and `upstream?`, all optional because an entry lacking them is a pre-214 or other-stage
entry that must fall through to the generic renderer. ⚠ **Consume it; do not edit that file** — it
has one owner in this phase.

Write `PublishRefusalList.test.tsx` for invariants #1, #2, #3, #5, #6, #9 and #13:
  - a phase whose `name` differs from its slug: the name renders, the slug appears **zero** times;
  - one headline node per item (`getAllByTestId("refusal-headline")` length equals item count);
  - at least one button per item, and exactly the rediscovery label on `shape_unknown`;
  - a regex over every headline's text asserting no `!` and no severity word;
  - the headline count number equals the rendered item count, driven with N = 1, 2 and 5;
  - `--destructive` absent from the surface's computed rules (invariant #9);
  - `innerHTML` contains none of `send_email`, `create_ticket`, `post_message`, `external_action` (#13);
  - **a polymorphism case**: an entry that is a bare string, and an entry with an unknown `code`, both
    still render through the generic path without throwing.
  </action>
  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/PublishRefusalList.test.tsx` exits 0 with ≥ 12 cases.
    - A case asserts the phase slug string appears 0 times and the authored name ≥ 1 time.
    - `grep -c "\.message" frontend/src/components/workflows/PublishRefusalList.tsx` is 0.
    - The `shape_unknown` case asserts the headline text contains no argument name from the fixture.
    - The count case runs with three different N values and asserts headline-number === item count each time.
    - `grep -c "blocked_stage" frontend/src/components/workflows/PublishRefusalList.tsx` is 0 — key-detection per entry, never a switch on the stage.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — no more than 34 errors.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/PublishRefusalList.test.tsx</automated>
  </verify>
  <done>Five kinds render with distinct headlines and next actions, the slug and the diagnostic never appear, and the polymorphic fall-through still works.</done>
</task>

<task type="auto">
  <name>Task 2: The spine below the cause, and the two sentences the surface owes</name>
  <files>frontend/src/components/workflows/PublishGauntlet.tsx, frontend/src/components/workflows/PublishGauntlet.test.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/PublishGauntlet.tsx:139-200` — the `STAGES` table, its
      *"mirrors the server's stage table"* claim, and ⚠ the note that the claim is **asserted by the
      co-located suite, which reads the `stage="…"` literals out of `publish_service.py` itself**.
      Plan `214-05` extends stage 2 rather than adding a stage, so `STAGES` gains **no row** — verify
      that against the suite rather than assuming it.
    - `frontend/src/components/workflows/PublishGauntlet.tsx:1-45` — the four shipped rules, including
      *"the client never recomputes published/blocked_stage"*.
    - `frontend/src/components/workflows/PublishGauntlet.tsx:183-200` — `RUNNING_STAGE_INDEX` and the
      *located by the stage it belongs to* correction. ⚠ A row's position is not its identity.
    - `.planning/sketches/215-publish-refuses-by-name/BUILD-CONTRACT.generated.md` §2 invariants
      #4, #7, #8, #10, #11, #12.
  </read_first>
  <action>
Mount `PublishRefusalList` in the gauntlet's block region **above** the stage spine (invariant #7:
the spine sits BELOW the cause — a person reads what is wrong before they read where it stopped).

Render `GOLDEN_NO_SEND` on a passing golden-run row (invariant #11) and `ALREADY_PUBLISHED_NOTE`
whenever a refusal is shown (invariant #12 — D-214-12's not-retroactive statement, on the surface
rather than only in a decision record).

⚠ **Do not add a `STAGES` row.** Plan `214-05` extends stage 2; the surface therefore gains no stage,
and invariant #8 (*exactly one blocked stage, every later stage reads not reached*) stays trivially
true. If the co-located `stage="…"` literal assertion goes red, that is the signal that `214-05` added
a stage after all — **report it, do not paper over it by adding a row**.

⚠ **Draw no new mark.** Invariant #10 and `icon-convention.md` §4: the blocked node's mark is an edge
lane, never the top-right corner, which D-185 claimed for the governance seal. Reuse the shipped
stage glyphs; if a net-new glyph seems needed, **flag it as a proposal in the summary** rather than
drawing it.

Extend `PublishGauntlet.test.tsx`: invariant #4 (**none of the ten stage labels appears in any
headline node** — assert all ten, not a sample), #7 (DOM order: cause before spine), #8 (exactly one
row in the blocked state and every later row reading `STAGE_NOT_REACHED` — ⚠ *a later stage reading
`STAGE_PASSED` claims a check that never ran*), #11, #12.
  </action>
  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/PublishGauntlet.test.tsx` exits 0.
    - A case iterates all ten stage labels and asserts each is absent from every `refusal-headline` node.
    - A case asserts, by DOM position, that the refusal list precedes the stage spine.
    - A case asserts exactly one row carries the blocked state and that every row after it reads `STAGE_NOT_REACHED` (none reads `STAGE_PASSED`).
    - `git diff -- frontend/src/components/workflows/PublishGauntlet.tsx | grep -c "STAGES"` shows no added row inside the `STAGES` literal.
    - The co-located `stage="…"`-literal assertion against `publish_service.py` passes unchanged.
    - `PublishGauntlet.test.tsx`'s per-file count does not decrease.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/PublishGauntlet.test.tsx src/components/workflows/PublishRefusalList.test.tsx</automated>
  </verify>
  <done>The cause renders above the spine, the two owed sentences appear, no stage row was added, and no new mark was drawn.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend `named_failures` → the author's screen | Server-composed data crosses into a rendered refusal. It may contain author-supplied text (the step name), schema-supplied text (the argument name) and — on other stages — free-form diagnostics. |
| the refusal surface → the author's decision to publish | ⚠ A refusal that under-states or mis-attributes a problem lets an unsafe workflow through, or blocks a safe one with no route out. This is a safety surface, not a status surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-214-10-01 | Information Disclosure | a backend diagnostic or a wire id on the author's screen | mitigate | `message` and `phase` are never rendered; asserted by `grep -c "\.message"` = 0 and by the slug-absence case. The `message` field's home is the audit receipt. |
| T-214-10-02 | Spoofing | a refusal claiming a check that never ran | mitigate | Invariant #8: exactly one blocked row; every later row reads `STAGE_NOT_REACHED` and **none** reads `STAGE_PASSED`. Asserted directly, because a spine that shows later stages as checked is a false claim about what the gate did. |
| T-214-10-03 | Tampering | step-name text rendered as markup | mitigate | React text rendering; a `?raw` fence asserts `dangerouslySetInnerHTML` appears zero times in both components. |
| T-214-10-04 | Denial of Service | a malformed `named_failures` entry crashing the surface | mitigate | The shipped KEY-DETECTION rule is preserved — detect per entry, never switch on `blocked_stage` — and the polymorphism case drives a bare string and an unknown code through the generic path. ⚠ A publish blocked with an unrenderable refusal is a workflow nobody can fix. |
| T-214-10-05 | Repudiation | the surface implying the gate is retroactive | mitigate | `ALREADY_PUBLISHED_NOTE` renders with every refusal (invariant #12). Without it an author may believe live workflows were invalidated, and act on that. |
| T-214-10-SC | Tampering | package installs | mitigate | This plan installs nothing; the stage glyphs are the bundled 3D fluent-emoji set already in use. ⛔ **Drawing a net-new mark is out of scope** — flag it as a proposal instead (`icon-convention.md` §4). |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — no more than 34 errors.
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/PublishGauntlet.test.tsx src/components/workflows/PublishRefusalList.test.tsx` — green.
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root, verdict line
  verbatim, paired with the two per-file deltas. ⚠ **This plan does NOT edit `scripts/vitest-count-gate.cjs`.** Every `BASELINE` pin for this
  phase is collected in plan `214-15`, in one commit, once every new file exists — a `BASELINE`
  key naming a path that does not yet exist makes the gate **ERROR**, and distributing the pins
  across parallel worktrees is what would create that. **Record the gate's printed `— N new`
  figures verbatim in this plan's SUMMARY**; `214-15` pins those exact numbers, never a
  hand-derived one. The one exception in the whole phase is plan `214-08`'s `TARGETS` PATH edit
  for the moved `connectionMark.test.tsx`, which cannot wait because a pinned file the gate
  cannot find fails `N/N pinned files present` immediately.
</verification>

<how_wed_know_this_failed>
From CONTEXT.md's `<failure_modes>` — this plan is the visible half of #3 and #10:

3. **The publish gate and the executor disagree.** Observable HERE as an author who is refused with a
   named argument, fixes exactly that, publishes — and the run still fails at the send. The refusal's
   precision is what makes that disagreement visible rather than mysterious; a vague headline would
   hide it.
10. **The gate passes by loosening.** Observable here as a refusal surface that never renders, because
    nothing is ever refused. ⚠ **A surface with no failing fixture is a surface nobody has seen** —
    every one of the five kinds is driven with a real fixture, and the count case runs at N=1, 2, 5.

Also specific to this plan: **a single collapsed headline.** Sketch 215 §4 names it —
*"a single 'this step is invalid' headline would collapse all five, which is `BUG-260815-06`'s
stage-naming failure in a new costume."* `REFUSAL_FOR_KIND` being a `Record` rather than a `switch`
with a `default` is the structural guard.
</how_wed_know_this_failed>

<g5_disposition>
Re-derived from git 2026-08-28:

| File | Ledger cell | **Measured** | Verdict |
|---|---|---|---|
| `frontend/src/components/workflows/PublishGauntlet.tsx` | 14 / 7 / 1025 | **(re-derive at execute time)** | ⚠ **FIRES — ledger reads *absent at 7 phases (added 199)*** |
| `frontend/src/lib/api/workflows.ts` | *(inside the `lib/api/` split taken at 207)* | **(re-derive at execute time)** | check at execute time |

**`PublishGauntlet.tsx` — honoured by construction, and the extraction is NAMED here even though it
is not taken.** The plan's diff on this 1,129-line file is: one import, one mounted child above an
existing region, and two conditional sentence nodes. **All five refusal kinds and every string live
in the new leaf and the vocabulary module** — which is the point: the file's growth pattern is that
each new refusal shape has historically been rendered inline, and this plan breaks that pattern rather
than continuing it. ⚠ **The seam a future extraction should take is the block region as a whole** (the
verdict + cause + spine composition), and `PublishRefusalList.tsx` is the first component out through
it. Plan `214-15` records that seam in the file's `docs/HOT-FILE-LEDGER.md` section — a named seam is
what makes the next phase's refactor cheap, and its absence is what the ledger keeps finding.

⚠ **`STAGES` gains no row**, which is the specific way this plan avoids the growth the ledger tracks.
</g5_disposition>

<success_criteria>
- Five refusal kinds render with five distinct headlines, each with a next action.
- `shape_unknown` names no argument and offers re-discovery.
- The authored step name renders; the slug and the backend `message` render nowhere.
- No headline names any of the ten gauntlet stages, carries an exclamation, or uses a severity word.
- The count agrees with the list at N = 1, 2 and 5.
- Exactly one stage row is blocked and no later row claims a check that never ran.
- The surface states that the gate is not retroactive, and that the golden run sent nothing.
- `STAGES` gained no row and no new mark was drawn.
</success_criteria>

<output>
Create `.planning/phases/214-a-step-names-its-service-and-its-action/214-10-SUMMARY.md` when done.
Record: the `BASELINE` number as printed, whether the co-located `stage="…"`-literal assertion stayed
green (and if not, that `214-05` added a stage after all), and any net-new mark **flagged as a
proposal** rather than drawn.
</output>
