---
phase: 214-a-step-names-its-service-and-its-action
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/WorkflowBuilderPage.tsx
  - frontend/src/pages/WorkflowBuilderPage.test.tsx
  - frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx
  - frontend/src/components/workflows/WorkflowCanvas.composition.test.tsx
  - frontend/src/components/workflows/WorkflowCanvas.tsx
autonomous: true
requirements: [STEP-01]

must_haves:
  truths:
    - "D-214-22: the builder's form panel occupies `clamp(480px, 38%, 640px)`, the SAME track Settings has used since Phase 213 — after this change the two authoring panels are ONE track, not two."
    - "The 44px collapsed strip is unchanged — it is explicitly not part of the decision."
    - "Every pin that asserted the literal `400px` changes in the SAME commit as the source, and every prose mention of `400px` in the files this plan owns becomes truthful in that commit."
    - "The 900px overflow arithmetic was RE-RUN against the wider panel, not assumed to still hold."
  artifacts:
    - path: "frontend/src/pages/WorkflowBuilderPage.tsx"
      provides: "the widened grid track"
      contains: "clamp(480px, 38%, 640px)"
    - path: "frontend/src/pages/WorkflowBuilderPage.test.tsx"
      provides: "the pin, changed in lockstep"
      contains: "clamp(480px, 38%, 640px)"
  key_links:
    - from: "frontend/src/pages/WorkflowBuilderPage.tsx"
      to: "frontend/src/pages/WorkflowBuilderPage.test.tsx"
      via: "the grid style literal both sides assert"
      pattern: "clamp\\(480px, 38%, 640px\\)"
---

<objective>
Widen the builder's push/split form panel from a fixed `400px` to `clamp(480px, 38%, 640px)`, and
change every pin and every prose mention in the files this plan owns in the same commit.

Purpose: **operator decision D-214-22, taken at `/gsd:plan-phase` on 2026-08-28.** Sketch
`214-argument-form-and-its-source` §4 drew both tracks over byte-identical content and deliberately
left the fork open so a plan could not pick it silently. The clamp arm ships, which means **variant B
ships gutter and all** — the per-argument source lane plan `214-07` builds does not fit at 400px,
and that non-fit was the sketch's finding, not a stacked deck.

⚠ **The pins are NAMED here rather than discovered mid-run.** Phase 213 hit exactly this shape:
`ConnectionFormPanel.test.tsx:571` pinned `400px` and was found only once the plan was already
running. That is why this plan exists separately and lands in wave 1, ahead of the editor.

Output: one source line, one real pin, one test rename, one re-run arithmetic case, and truthful prose.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⚠ **FIRST ACTION IN ANY WORKTREE:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/214-a-step-names-its-service-and-its-action/214-CONTEXT.md
@.planning/sketches/214-argument-form-and-its-source/BUILD-CONTRACT.generated.md

**CONTEXT.md's D-214-22 lists the pins verbatim, measured with `grep -rn "400px" frontend/src` on
2026-08-28.** Re-run that grep first: this plan's whole risk is a mention it did not know about.
</context>

<tasks>

<task type="auto">
  <name>Task 1: The track widens, and its one real pin changes with it</name>
  <files>frontend/src/pages/WorkflowBuilderPage.tsx, frontend/src/pages/WorkflowBuilderPage.test.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowBuilderPage.tsx:2806-2820` — the grid, and the comment at `:2806`
      explaining that the test renders the `400px` panel AND the `44px` collapsed strip
      simultaneously, which in the product is impossible.
    - `frontend/src/pages/WorkflowBuilderPage.test.tsx:163-170` — **THE PIN**: `:167` asserts the
      literal string `"400px"` on the grid element's `style` attribute. Nothing else asserts a width.
    - `frontend/src/components/settings/ConnectionsTab.tsx:387` — the shipped `clamp(480px, 38%, 640px)`
      track Settings has used since Phase 213. ⚠ **Read it to copy the exact spelling**, spaces
      included; a differently-spaced clamp is a second track wearing the same number.
    - `frontend/src/pages/WorkflowBuilderPage.tsx:25, 27, 60, 958, 2181` — the five prose mentions
      this file owns.
  </read_first>
  <action>
Change `WorkflowBuilderPage.tsx:2817`'s `gridTemplateColumns` open-panel value from `"400px"` to
`"clamp(480px, 38%, 640px)"`, spelled exactly as `ConnectionsTab.tsx:387` spells it.
⚠ **The `"44px"` collapsed value does NOT change** — D-214-22 says so explicitly.

Change `WorkflowBuilderPage.test.tsx:167`'s assertion to the same literal. ⚠ Keep it an assertion on
the **literal string in the style attribute**, not a computed width: jsdom does not resolve `clamp()`,
so an assertion on `getComputedStyle` would be green-by-accident. State that in a comment beside the
line so the next reader does not "improve" it into a computed check.

Update the five prose mentions in `WorkflowBuilderPage.tsx` (`:25, :27, :60, :958, :2181`) and the
one at `:2806` so each names the new track. ⚠ `:2806`'s comment is about the *test rendering both
states at once* — its point survives; only the number changes.

Add one new case to `WorkflowBuilderPage.test.tsx` asserting the **collapsed** track is still exactly
`44px`, so a future widening cannot take the strip with it. That case did not exist; the pin only
ever covered the open arm.
  </action>
  <acceptance_criteria>
    - `grep -c "clamp(480px, 38%, 640px)" frontend/src/pages/WorkflowBuilderPage.tsx` is ≥ 1 and `grep -c '"400px"' frontend/src/pages/WorkflowBuilderPage.tsx` is 0.
    - The clamp literal in `WorkflowBuilderPage.tsx` is byte-identical to the one in `frontend/src/components/settings/ConnectionsTab.tsx` — verify with `diff <(grep -o "clamp([^)]*)" frontend/src/pages/WorkflowBuilderPage.tsx | sort -u) <(grep -o "clamp([^)]*)" frontend/src/components/settings/ConnectionsTab.tsx | sort -u)` producing no output for the panel track.
    - `grep -c '"44px"' frontend/src/pages/WorkflowBuilderPage.tsx` is unchanged from HEAD.
    - `grep -c "400px" frontend/src/pages/WorkflowBuilderPage.tsx` is 0 — no stale prose survives.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowBuilderPage.test.tsx` exits 0 with a case count **one higher** than its pinned baseline.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowBuilderPage.test.tsx</automated>
  </verify>
  <done>The open track is the shared clamp, the collapsed strip is pinned at 44px by a new case, and no `400px` remains in the page's source or prose.</done>
</task>

<task type="auto">
  <name>Task 2: The rename that is a rename, and the arithmetic that must be re-run</name>
  <files>frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx, frontend/src/components/workflows/WorkflowCanvas.composition.test.tsx, frontend/src/components/workflows/WorkflowCanvas.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx:367` — the test NAME says *"the shipped
      400px form panel"*; **its assertion is on panel presence, not width.** ⚠ D-214-22:
      **rename, do not re-baseline.**
    - `frontend/src/components/workflows/WorkflowCanvas.composition.test.tsx:120-130` — the shared
      column, the R12 width comment naming *"the same track the 400px panel shares"*.
    - `frontend/src/components/workflows/WorkflowCanvas.composition.test.tsx:225-245` — the 900px
      overflow case. ⚠ **Its arithmetic was written against a 400px panel and a wider panel moves the
      boundary.** RE-RUN it; do not reason about it.
    - `frontend/src/components/workflows/WorkflowCanvas.tsx:1330-1345` — the prose at `:1337` about a
      900px column that already carries a 400px panel.
  </read_first>
  <action>
Rename `canvas.test.tsx:367`'s test title to name the shared clamp track instead of `400px`. **Change
nothing else in that case** — its assertion is on panel presence and is unaffected.

Update `WorkflowCanvas.composition.test.tsx:123`'s comment to name the new track.

**Re-run the `:234` overflow case and record the measured result.** Two outcomes, and the plan must
take whichever the run gives rather than the one it hoped for:
  - The case still passes ⇒ record the measured margin (canvas column width at 900px minus the new
    panel minimum, 480px) in a comment beside it, so the next widening knows how much headroom is
    left. `900 − 480 = 420` is the arithmetic; state it and state that it was OBSERVED, not derived.
  - The case fails ⇒ **the failure is the finding.** Do NOT loosen the assertion. Adjust the case's
    viewport constant to the smallest width at which the composition is still honest, record the old
    and new constants side by side with the reason, and name the consequence in the summary: the
    builder's minimum usable viewport moved.

Update `WorkflowCanvas.tsx:1337`'s prose to name the new track.

⚠ `ConnectionPicker.tsx:73,433`, `McpToolPicker.tsx:125,521` and `PhaseFormPanel.tsx:30` also carry
`400px` prose. **They are NOT in this plan's `files_modified`** and are owned by plan `214-07`, which
edits all three for other reasons in the same phase. This plan's summary must name that hand-off
explicitly so the obligation cannot fall between two plans.
  </action>
  <acceptance_criteria>
    - `grep -c "400px" frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx` is 0.
    - `grep -c "400px" frontend/src/components/workflows/WorkflowCanvas.composition.test.tsx` is 0.
    - `grep -c "400px" frontend/src/components/workflows/WorkflowCanvas.tsx` is 0.
    - `git diff -- frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx | grep -c "^[-+]\s*expect"` is 0 — the rename changed no assertion.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowBuilderPage.canvas.test.tsx src/components/workflows/WorkflowCanvas.composition.test.tsx` exits 0.
    - The `:234` case carries a comment stating the measured margin **or** the old/new viewport constants with the reason — `grep -c "OBSERVED\|re-run" frontend/src/components/workflows/WorkflowCanvas.composition.test.tsx` is ≥ 1.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowBuilderPage.canvas.test.tsx src/components/workflows/WorkflowCanvas.composition.test.tsx</automated>
  </verify>
  <done>The name changed and no assertion did; the overflow case was re-run and its measured outcome recorded either way; the `214-07` hand-off is named.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new. This plan changes one CSS track literal and its assertions; no data crosses any boundary
that did not cross it before, no route changes, no field is added, nothing is sent.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-214-04-01 | Tampering | the pin at `WorkflowBuilderPage.test.tsx:167` | mitigate | The pin changes in the SAME commit as the source and is asserted on the literal style string, not a computed width — jsdom does not resolve `clamp()`, so a computed assertion would pass whatever the source said. A comment beside the line records that, so the guard cannot be "improved" into uselessness. |
| T-214-04-02 | Denial of Service | a viewport at which the canvas column collapses | mitigate | The 900px overflow case is RE-RUN rather than reasoned about, and a failure is recorded as a finding with the new minimum viewport named rather than absorbed by loosening the assertion. |
| T-214-04-03 | Repudiation | a `400px` prose mention left stale in a file this plan does not own | accept, with a named owner | `ConnectionPicker.tsx`, `McpToolPicker.tsx` and `PhaseFormPanel.tsx` are plan `214-07`'s. Recorded as an explicit hand-off in this plan's summary and re-checked by plan `214-15`'s phase-wide `grep -rn "400px" frontend/src` sweep, which must return only `components/settings/*` (out of scope) and `test-utils/handleSpike.test.tsx` (unrelated). |
| T-214-04-SC | Tampering | package installs | mitigate | This plan installs nothing. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — no more than 34 errors.
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowBuilderPage.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx src/components/workflows/WorkflowCanvas.composition.test.tsx` — all green.
- `grep -rn "400px" frontend/src | grep -v "components/settings" | grep -v handleSpike` — after this
  plan, the only survivors are `ConnectionPicker.tsx`, `McpToolPicker.tsx` and `PhaseFormPanel.tsx`,
  all three owned by plan `214-07`. Record the exact list in the summary.
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root — verdict line
  verbatim, paired with the per-file delta for `WorkflowBuilderPage.test.tsx` (+1). ⚠ Do not write
  "the gate is green" as this plan's acceptance; it is not reliably reachable on demand.
</verification>

<how_wed_know_this_failed>
Not from CONTEXT.md's numbered list — this plan's failure mode is specific and was named by the
sketch itself:

**A pin is discovered mid-run rather than named up front.** That is Phase 213's
`ConnectionFormPanel.test.tsx:571` recurring, and the whole reason D-214-22 enumerates the five
sites. The observable: a `grep -rn "400px" frontend/src` after this plan returns a file this plan did
not name and plan `214-07` does not own.

**And the sketch's conditional consequence is inverted.** If the clamp ships but plan `214-07` then
drops the source gutter anyway, the phase has paid the pin cost and shipped variant C — the arm the
operator did *not* pick. Plan `214-07`'s gutter assertion (sketch 214 invariant #5: an unsourced and
a sourced row have IDENTICAL label-column offsets, because the lane is a column that widens and is
never inserted) is what makes that visible.
</how_wed_know_this_failed>

<g5_disposition>
Re-derived from git 2026-08-28 with CLAUDE.md's recipe:

| File | Ledger cell | **Measured** | Verdict |
|---|---|---|---|
| `frontend/src/pages/WorkflowBuilderPage.tsx` | 51 / 17 / 2867 | **53 / 19 / 2927** | ⚠ **STALE — G-5 FIRES** |
| `frontend/src/components/workflows/WorkflowCanvas.tsx` | 27 / 8 / 1565 | **(re-derive at execute time)** | fires |

**Disposition: honoured by construction — and here the argument is unusually strong, so it is stated
rather than gestured at.** This plan's diff on `WorkflowBuilderPage.tsx` is **one value in one string
literal plus six comment lines**, and on `WorkflowCanvas.tsx` it is **one comment line**. No control
flow, no new state, no new prop, no new import. The extraction each file is owed is neither
performed nor made harder — the file is not larger in any structural sense and its responsibilities
are unchanged. ⚠ **Both rows are stale in the ledger and are corrected in plan `214-15`'s single
ledger commit**, which carries the CLAUDE.md row and the `docs/HOT-FILE-LEDGER.md` section together
per the same-commit sync rule.
</g5_disposition>

<success_criteria>
- The builder panel's open track is `clamp(480px, 38%, 640px)`, byte-identical to Settings' spelling.
- The collapsed `44px` strip is unchanged and now has a pin of its own.
- `WorkflowBuilderPage.test.tsx:167` asserts the new literal, in the same commit.
- `canvas.test.tsx:367` was renamed with zero assertion changes.
- The 900px overflow case was re-run and its measured outcome recorded either way.
- No `400px` survives in any file this plan owns.
</success_criteria>

<output>
Create `.planning/phases/214-a-step-names-its-service-and-its-action/214-04-SUMMARY.md` when done.
Record: the re-run grep's full output, the overflow case's measured result, and the explicit
hand-off of the three remaining `400px` prose sites to plan `214-07`.
</output>
