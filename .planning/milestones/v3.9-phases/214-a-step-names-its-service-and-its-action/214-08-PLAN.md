---
phase: 214-a-step-names-its-service-and-its-action
plan: 08
type: execute
wave: 2
depends_on: ["214-03"]
files_modified:
  - frontend/src/lib/connectionMark.tsx
  - frontend/src/components/settings/connectionMark.tsx
  - frontend/src/components/settings/ConnectionsTab.tsx
  - frontend/src/components/settings/__tests__/connectionMark.test.tsx
  - frontend/src/components/workflows/StepIdentity.tsx
  - frontend/src/components/workflows/StepIdentity.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [STEP-04]

must_haves:
  truths:
    - "D-214-16: ONE element renders a step's identity, and coverage of the five surfaces is a consequence of that element existing rather than a list kept in sync."
    - "D-214-17: the mark is a REUSE. `connectionMark.tsx` is moved to `lib/`, not forked and not re-mapped; a vendor shows its own mark and a vendorless shape is drawn in the interface's own ink."
    - "Size is a modifier, never a fork — the size prop selects a class from a Record, and adding the fourth size widens that Record rather than adding a branch."
    - "The element RECEIVES its two strings as props; it resolves nothing. The caller resolves (the same rule D-214-14 gives the approval composer)."
    - "`SEED-206`'s measured ClickUp hole is answered by the neutral mark, not by adding an icon."
  artifacts:
    - path: "frontend/src/lib/connectionMark.tsx"
      provides: "the ONE service-to-mark map, at the shipped home for a cross-surface resolver"
      contains: "ConnectionMarkGlyph"
    - path: "frontend/src/components/workflows/StepIdentity.tsx"
      provides: "the shared step-identity element — mark + action + service, four sizes"
      contains: "STEP_IDENTITY"
    - path: "frontend/src/components/workflows/StepIdentity.test.tsx"
      provides: "the invisibility, neutral-mark and separator assertions ported from connectionMark.test.tsx"
      contains: "innerHTML"
  key_links:
    - from: "frontend/src/components/workflows/StepIdentity.tsx"
      to: "frontend/src/lib/connectionMark.tsx"
      via: "ConnectionMarkGlyph, the ONE render path"
      pattern: "ConnectionMarkGlyph"
    - from: "frontend/src/components/settings/ConnectionsTab.tsx"
      to: "frontend/src/lib/connectionMark.tsx"
      via: "the single import line the move costs"
      pattern: "@/lib/connectionMark"
---

<objective>
Move the one service-to-mark map to `frontend/src/lib/`, and build the shared step-identity element
that five surfaces will mount.

Purpose: `SEED-206` is SC#4's second half verbatim, and its own warning is that **a partial answer
leaves the seed live for the next phase to rediscover** — *"do NOT wait for a catalog; the spine gap
is live TODAY on shipped surfaces."* The way to make coverage structural rather than a checklist is
one element. This plan builds it; plan `214-11` mounts it.

Output: one moved module with three import updates, one new element, one suite that ports the
assertions the import fence structurally cannot make.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⚠ **FIRST ACTION IN ANY WORKTREE:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/214-a-step-names-its-service-and-its-action/214-CONTEXT.md
@.planning/phases/214-a-step-names-its-service-and-its-action/214-PATTERNS.md
@.planning/sketches/216-the-mark-and-the-action-everywhere/BUILD-CONTRACT.generated.md
@.planning/seeds/SEED-206-the-service-mark-belongs-on-every-step-surface-not-only-the-canvas.md
@.planning/phases/214-a-step-names-its-service-and-its-action/214-03-SUMMARY.md

**PATTERNS.md §4 is this plan's brief** — §4a the measured proof that `connectionMark.tsx` has **zero**
`components/settings/` coupling and that `lib/` is the shipped home; §4b the three-key `SIZE_CLASS`
Record that must be widened not branched; §4c the three-ink contract and the resolved-but-INVISIBLE
assertion block to port; §4d the `titleOf` prop seam.

⚠ **Read `.claude/skills/sketch-findings-agentic-rag/SKILL.md` and
`references/icon-convention.md` §1 and §4 before drawing anything.** §1: one source for
provider/model marks — *reuse the seam, don't re-map per component*; `connectionMark.tsx` is the
SERVICE analog and D-214-17 forbids re-mapping it. §4: **nothing new may be drawn**, the top-right
corner is CLAIMED by the governance seal, the word-badge carries no glyph, and any net-new mark must
be FLAGGED as a proposal.
</context>

<interfaces>
<!-- What plan 214-11 mounts on five surfaces. -->

```
type StepIdentitySize = "row" | "chip" | "canvas" | "spine"      // the FOURTH key
function StepIdentity(props: {
  shape: ConnectionMarkShape          // { service_id?, capability?, mcp_server_url?, tool_name? }
  action: string                      // the tool's human name — RESOLVED BY THE CALLER
  service: string | null              // the connection's display name — RESOLVED BY THE CALLER
  size: StepIdentitySize
}): JSX.Element
```

`service === null` renders `STEP_IDENTITY_SERVICE_UNKNOWN(action)` — the action alone.
⛔ **Never "Unknown service", never the capability id, never the slug.**
The separator is the house middle dot, rendered as **its own node** (invariant #5).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The move — one module, three importers, two gate knobs</name>
  <files>frontend/src/lib/connectionMark.tsx, frontend/src/components/settings/connectionMark.tsx, frontend/src/components/settings/ConnectionsTab.tsx, frontend/src/components/settings/__tests__/connectionMark.test.tsx, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/components/settings/connectionMark.tsx` in full (294 L) — its complete import set
      at `:83-115` (zero from `components/settings/`, zero from `@/lib/api`, zero store, zero context),
      the STRUCTURAL `ConnectionMarkShape` at `:133-140` and why the `@/lib/api` abstention is
      deliberate, the module-private maps at `:155-198` (⚠ **module-private is load-bearing** — a
      second consumer reading the map directly would bypass `connectionMark()`'s own-property guard),
      `SIZE_CLASS` at `:266-271`, `INK_CLASS` at `:273-277`, and `ConnectionMarkGlyph` at `:285-294`.
    - `frontend/src/components/settings/connectionMark.tsx:118-124` — the LOCAL ALIAS of
      `lib/phaseGlyph.tsx:66`'s `PhaseMark`, with its recorded reason: *"if those ever diverge the
      widening belongs HERE — `lib/phaseGlyph.tsx` is a `lib/` leaf with other consumers and must not
      be widened to accommodate a `components/settings/` need."*
      ⚠ **Moving the module to `lib/` makes that question live again.** Record the decision either
      way; do not let the two type declarations silently converge or diverge.
    - `frontend/src/lib/phaseGlyph.tsx` and `frontend/src/lib/providerLogo.tsx` — the two shipped
      `lib/` precedents for a cross-surface mark resolver.
    - `scripts/vitest-count-gate.cjs` — the `TARGETS` array (a **path** list; `src/lib` has NO
      directory entry) and the `BASELINE` object (**basename** keys, so `"connectionMark.test.tsx": 39`
      still resolves after a move). ⚠ **A pinned file the gate cannot find makes the gate fail on
      `N/N pinned files present`; a `BASELINE` key naming a nonexistent path makes it ERROR.**
  </read_first>
  <action>
Move `frontend/src/components/settings/connectionMark.tsx` to `frontend/src/lib/connectionMark.tsx`
with `git mv`, **byte-for-byte** except the header. ⛔ **Do not re-map a single service, do not add a
mark, do not touch `MARKS` / `MCP_MARK` / `NEUTRAL_MARK` / `SERVICE_MARKS`.** D-214-17 forbids it and
the slugs were verified against the installed `@iconify-json/logos@1.2.13` rather than against any
document.

Update the header to state its new home and its new audience: it now serves Settings, the workflow
canvas and the run surfaces, and it is the SERVICE analog of `lib/providerLogo.tsx`'s one-source rule
(`icon-convention.md` §1). Add one sentence taking the `PhaseMark` alias decision explicitly — the
recommended call is **keep the local alias and record that the two declarations are deliberately
separate**, because `lib/phaseGlyph.tsx`'s stated reason for not being widened (*it has other
consumers*) is still true after the move; the module's location changed, its consumer set did not.

Move the test file to `frontend/src/lib/__tests__/connectionMark.test.tsx` with `git mv` and update its
import. ⚠ Its **basename is unchanged**, so its `BASELINE` pin still resolves — but its `TARGETS`
entry is a PATH and must be updated in the same commit. Verify by reading the array, not by belief.

Update `ConnectionsTab.tsx`'s import at `:94` (measured: it is the module's ONE current consumer, plus
two test files). Then `grep -rn "components/settings/connectionMark" frontend/src` must return zero.

**Widen `SIZE_CLASS` by one key — `spine`.** ⚠ Sketch 216 invariant #1: *"size is a modifier, never a
fork … assert the size prop selects a class rather than a branch."* Add the key to the `Record`; add
no `if`. `ConnectionMarkGlyph`'s single render path and its `aria-hidden="true"` are unchanged — *a
mark is never the accessible name of anything here*; the identity's accessible name is its two words.
  </action>
  <acceptance_criteria>
    - `grep -rn "components/settings/connectionMark" frontend/src scripts | wc -l` is 0.
    - `git log --follow --oneline -- frontend/src/lib/connectionMark.tsx | wc -l` is ≥ 6 — the history followed the move (a copy-and-delete would read 1).
    - `git diff -M --stat HEAD -- frontend/src/lib/connectionMark.tsx frontend/src/components/settings/connectionMark.tsx` shows a rename with ≤ 25 changed lines (header + the one `SIZE_CLASS` key).
    - `grep -c "SERVICE_MARKS\|MARKS\s*=" frontend/src/lib/connectionMark.tsx` is unchanged from the pre-move file, and `git diff -M` shows **zero** changed lines inside the map literals.
    - `SIZE_CLASS` has exactly 4 keys and `ConnectionMarkGlyph`'s body contains no `if` and no ternary on `size`.
    - `scripts/vitest-count-gate.cjs`'s `TARGETS` contains `src/lib/__tests__/connectionMark.test.tsx` and does **not** contain `src/components/settings/__tests__/connectionMark.test.tsx`.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/lib/__tests__/connectionMark.test.tsx src/components/settings/__tests__/ConnectionsTab.test.tsx` exits 0 and `connectionMark.test.tsx`'s count is **≥ its pin of 39**.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/lib/__tests__/connectionMark.test.tsx src/components/settings/__tests__/ConnectionsTab.test.tsx</automated>
  </verify>
  <done>The module lives in `lib/` with its history followed, no map row changed, `SIZE_CLASS` has four keys and no branch, and the gate's path knob moved with the file.</done>
</task>

<task type="auto">
  <name>Task 2: The element — mark plus two names, resolved by nobody but the caller</name>
  <files>frontend/src/components/workflows/StepIdentity.tsx, frontend/src/components/workflows/StepIdentity.test.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/stepIdentityVocabulary.ts` (plan `214-03`) — `STEP_IDENTITY`,
      `STEP_IDENTITY_SERVICE_UNKNOWN` and the six other ids. Every string comes from here.
    - `frontend/src/lib/connectionMark.tsx:204-294` — `connectionMark(shape)` is **TOTAL; never null**,
      and `ConnectionMarkGlyph` is the ONE render path.
    - `frontend/src/components/settings/__tests__/connectionMark.test.tsx:54, 430-470` — ⭐ the
      **resolved-but-EMPTY / resolved-but-IDENTICAL / resolved-but-INVISIBLE** block, reading
      `svg.innerHTML`. **Port that block; do not restate it in prose** — sketch 216 #6 says the import
      fence structurally cannot catch invisibility.
    - `frontend/src/components/workflows/phaseVocabulary.ts:276-299` — `nodeTitle` and its two floors:
      *"The SLUG NEVER appears in this string"* and *"A name is NEVER fabricated … an id-shaped face
      is worse than a generic one."*
    - `frontend/src/components/workflows/RunSpine.tsx:30` — *"the step's HUMAN NAME — the page's
      `titleOf`, the same `nodeTitle` the canvas paints."* ⚠ PATTERNS §4d: `PhaseCard`/`PhaseTimeline`
      have **no `titleOf`** — they read `Phase` from `StreamsProvider` — so the element must accept
      its two strings as props rather than resolving them, **or the two surfaces will disagree.**
    - `.claude/skills/sketch-findings-agentic-rag/references/icon-convention.md` §4 — the shipped glyph
      table; nothing net-new may be drawn without being flagged as a proposal.
  </read_first>
  <action>
Build `StepIdentity.tsx` to the `<interfaces>` signature. It is a leaf: no store, no context, no fetch,
no resolution. It renders `ConnectionMarkGlyph` at the given size, then the composed identity string
from `stepIdentityVocabulary`, with the middle-dot separator as **its own node** so a test can assert
the element rather than a substring (invariant #5).

`service === null` ⇒ `STEP_IDENTITY_SERVICE_UNKNOWN(action)` and **no separator node at all**. ⛔ No
placeholder, no "Unknown service", no capability id, no slug. This is `grounding.py`'s *never draw a
name the system cannot know* rule on a rendering surface.

`data-` attributes carry a SHAPE, never a wire id — `ExternalActionSection.tsx:126-137`'s rule, which
is sketch 216 invariant #4.

Write `StepIdentity.test.tsx`:
  - **#1** the size prop selects a class: render all four sizes and assert four distinct classes, and
    assert the component source contains no `if (size` / ternary on `size` (read via `?raw`).
  - **#2** both props are rendered and both are required in the type (a type-level assertion).
  - **#5** the separator is its own node and appears exactly once in the two-name case and zero times
    in the service-null case.
  - **#6** ⭐ **port the invisibility block verbatim in structure**: for each of the three inks assert
    the rendered `svg.innerHTML` is non-empty, that two different services do not resolve to identical
    markup, and that a `stroke`-ink mark carries no fill utility. ⚠ lucide sets
    `fill="none" stroke="currentColor"` as presentation attributes and a CSS rule on the same element
    beats them, filling the outline into a blob — that is why this assertion cannot be a class check.
  - **#7** a vendorless shape (SMTP) and an **unmapped** service (use ClickUp, `SEED-206`'s measured
    hole) both resolve to the neutral mark, and no borrowed vendor logo appears.
  - **#4** the rendered `innerHTML` contains none of `send_email`, `create_ticket`, `post_message`,
    `ask_question`, `external_action`.
  - A case asserting the accessible name of the identity is its two words and that the mark carries
    `aria-hidden="true"`.
  </action>
  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/StepIdentity.test.tsx` exits 0 with ≥ 10 cases.
    - A case reads `StepIdentity.tsx?raw` and asserts it contains no `size ===` and no `if (size`.
    - The four sizes produce four distinct class strings, asserted as a set of size 4.
    - The ClickUp case asserts the neutral mark's `svg.innerHTML` is non-empty and differs from Slack's.
    - `grep -c "Unknown service" frontend/src/components/workflows/StepIdentity.tsx` is 0.
    - `cd frontend && npx eslint src/components/workflows/StepIdentity.tsx` reports 0 `react-refresh/only-export-components` errors.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — no more than 34 errors.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/StepIdentity.test.tsx</automated>
  </verify>
  <done>One element, four sizes selected by a Record, the invisibility block ported rather than paraphrased, and the unmapped-service hole answered by the neutral mark.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a connection's display name → five run surfaces | Author- or admin-supplied text is rendered as an identity on surfaces that previously showed only a generic label. |
| an unmapped service id → the mark resolver | A `service_id` we do not recognise must resolve to something safe rather than to nothing or to a borrowed logo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-214-08-01 | Spoofing | a borrowed vendor logo on an unmapped service | mitigate | `connectionMark()` is TOTAL and falls through to the named neutral. Tested by the ClickUp case (Task 2 #7) — `SEED-206`'s own measured hole, asserted rather than assumed. ⚠ Showing Slack's mark for a service that is not Slack is a claim the system cannot support. |
| T-214-08-02 | Tampering | a display name rendered as markup | mitigate | The element renders text through React with no `dangerouslySetInnerHTML`; a `?raw` fence asserts that string appears zero times in `StepIdentity.tsx`. |
| T-214-08-03 | Information Disclosure | a wire id reaching a run surface | mitigate | Invariant #4's `innerHTML` assertion over the five ids, and the `data-`-attributes-carry-a-shape rule. |
| T-214-08-04 | Denial of Service | an invisible mark reading as "no service" | mitigate | The ported resolved-but-INVISIBLE block. ⚠ Sketch 216 #6 states plainly that the import fence **structurally cannot** catch this, which is why the assertion reads `svg.innerHTML` rather than a class. |
| T-214-08-05 | Repudiation | a fabricated service name filling a null | mitigate | `service === null` renders the action alone, asserted, and `Unknown service` is grepped to zero. |
| T-214-08-SC | Tampering | package installs | mitigate | **This plan installs nothing.** The move reuses `@iconify-json/logos@1.2.13`, already installed and already the verification source for every slug in the map. ⛔ **Adding an icon package to cover ClickUp is explicitly out of scope** — the neutral mark is the answer D-214-17 gives. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — no more than 34 errors.
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/lib/__tests__/connectionMark.test.tsx src/components/workflows/StepIdentity.test.tsx src/components/settings/__tests__/ConnectionsTab.test.tsx` — green.
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root, verdict line
  verbatim. ⚠ **This plan is the only one in its wave touching `scripts/vitest-count-gate.cjs`** —
  the `TARGETS` path move and the new `StepIdentity.test.tsx` pin. `StepIdentity.test.tsx` lives under
  `src/components/workflows` (a directory entry), so it needs a `BASELINE` key only.
  ⚠ **`connectionMark.test.tsx` must still resolve at `N/N pinned files present`** — that is the
  single riskiest line in this plan and it is checked before anything else.
</verification>

<how_wed_know_this_failed>
From CONTEXT.md's `<failure_modes>` — this plan is upstream of #5 and enables SC#4:

**The specific failure this plan can produce is `SEED-206` surviving its own closure.** The seed's
warning is that *a partial answer leaves the seed live for the next phase to rediscover*. If the
element resolves anything for itself instead of taking props, `PhaseCard` (which has no `titleOf`)
and `RunSpine` (which does) will disagree, and the "one element, five surfaces" claim becomes one
element and five different answers. PATTERNS §4d names that risk exactly; the prop-only signature is
the guard, and the type-level required-props assertion is how it is checked.

**And the gate can break on the move rather than on the code.** A pinned file the gate cannot find
fails `N/N pinned files present`, which looks like a test failure and is a path edit. The acceptance
criterion checks the `TARGETS` array by reading it.
</how_wed_know_this_failed>

<g5_disposition>
Re-derived from git 2026-08-28:

| File | Ledger cell | **Measured** | Verdict |
|---|---|---|---|
| `frontend/src/components/settings/connectionMark.tsx` | 4 / 2 / 217 | **6 / 3 / 294** | ⚠ **STALE — now FIRES, exactly at threshold** |

**Disposition: the move IS the seam, and it is taken here.** The ledger listed this file as *young
(206.1-01, 212)*; re-derivation puts it at **3 phases**, so G-5 fires for the first time in the same
commit that relocates it. The relocation is the correct response rather than a coincidence:
PATTERNS §4a measured **zero** `components/settings/` coupling and named `lib/` as the shipped home
for a cross-surface mark resolver (`lib/phaseGlyph.tsx`, `lib/providerLogo.tsx`), and the file's own
docblock at `:118-124` already reasons about `lib/` boundaries. **Its consumer set is about to go
from one surface to six**, which is precisely when a `components/<one-surface>/` home stops being
true.

⚠ The row must be **moved, not edited in place** — a ledger row whose path no longer exists is
invisible to the audit scan, which is the failure mode the completeness rule exists to prevent. Plan
`214-15`'s ledger commit re-keys the row to `frontend/src/lib/connectionMark.tsx`, carries the
measured triple, records the move as the disposition, and moves its `docs/HOT-FILE-LEDGER.md` section
under the new anchor in the same commit. `StepIdentity.tsx` gets a young row there too.
</g5_disposition>

<success_criteria>
- `connectionMark.tsx` lives in `lib/`, moved with `git mv` so its history follows, with zero changed lines inside its map literals.
- No import anywhere still names `components/settings/connectionMark`.
- `SIZE_CLASS` has four keys and `ConnectionMarkGlyph` has no branch on `size`.
- The gate still finds every pinned file after the path move.
- `StepIdentity` takes its action and service as props and resolves nothing.
- A null service renders the action alone, with no separator and no substitute string.
- The resolved-but-INVISIBLE block is ported, and an unmapped service takes the neutral mark.
</success_criteria>

<output>
Create `.planning/phases/214-a-step-names-its-service-and-its-action/214-08-SUMMARY.md` when done.
Record: the `StepIdentity` prop signature verbatim (plan `214-11` mounts against it), the `BASELINE`
number as printed, the `PhaseMark`-alias decision taken, and the gate's `N/N pinned files present`
line after the move.
</output>
