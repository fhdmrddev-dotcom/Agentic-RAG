---
phase: 214-a-step-names-its-service-and-its-action
plan: 12
type: execute
wave: 3
depends_on: ["214-09", "214-16"]
files_modified:
  - frontend/src/components/workflows/LaunchInputFields.tsx
  - frontend/src/components/workflows/library/RunModal.tsx
  - frontend/src/components/layout/ChatLayout.tsx
  - frontend/src/components/layout/ChatLaunchForm.tsx
  - frontend/src/components/layout/ChatLayout.launch.test.tsx
  - frontend/src/components/workflows/LaunchInputFields.test.tsx
autonomous: true
requirements: [STEP-02]

must_haves:
  truths:
    - "D-214-04: chat gains a launch moment it does not have today — a workflow launched from the chat surface collects its declared inputs before the run starts."
    - "ONE field renderer serves all three launchers; `RunModal`, the schedule modal and chat do not each grow their own copy."
    - "REJECTED and enforced: the agent does NOT fill the argument from the conversation. An LLM choosing a recipient address is a new trust surface and is not reproducible between runs."
    - "The shipped launch path is unchanged — `createThread` + `sendMessage(workflow_definition_id)`, never a bespoke `/workflows/{id}/run` route (D-103-CONF-1)."
    - "A definition declaring no inputs launches exactly as it does today, with no new screen."
    - "⭐ ONE COLLECTION POINT PER LAUNCH. `doRun` collects ONLY when the caller supplied none (`opts?.inputs === undefined`). A library Run has already collected in `RunModal`, so it must NOT be asked a second time; chat and Test Run supply none, so `doRun` asks."
    - "⚠ `doRun` has THREE call sites once this plan lands, measured with `grep -n onLaunch frontend/src/pages/WorkflowsPage.tsx`: the library Run modal (`WorkflowsPage.tsx:1351`), `WorkflowsPage.tsx:907-923`'s **Test Run** adapter (`:909`), and the chat launch this plan adds. ⚠ **THREE, not four — the earlier count included 'any future one', which is not a caller.** The gate lives in `doRun` rather than in each door precisely so a fourth door inherits it; that is the argument, and it does not need an inflated count."
    - "The wire exists: plan `214-16` gives `postMessage` an `inputs` option and merges it server-side. Before that plan there was NO channel, and this plan must not claim one."
  artifacts:
    - path: "frontend/src/components/workflows/LaunchInputFields.tsx"
      provides: "the ONE declared-input field renderer, extracted from RunModal"
      contains: "entryInputFields"
    - path: "frontend/src/components/layout/ChatLaunchForm.tsx"
      provides: "the chat surface's launch moment"
      contains: "LaunchInputFields"
  key_links:
    - from: "frontend/src/components/layout/ChatLayout.tsx"
      to: "frontend/src/components/layout/ChatLaunchForm.tsx"
      via: "doRun gating on def.inputs before it creates the thread"
      pattern: "doRun"
    - from: "frontend/src/components/layout/ChatLayout.tsx"
      to: "create_workflow_run.inputs"
      via: "postMessage's `inputs` option, ADDED IN PLAN 214-16 — it did not exist before"
      pattern: "inputs"
---

<objective>
Give the chat surface a launch moment, and make the three launchers share one field renderer.

Purpose: SC#2 names *the library, a thread and a schedule* explicitly. Two are done in plan `214-09`;
**chat has no launch form at all today** — PATTERNS' *No Analog Found* table says so and names
`RunModal` as the nearest shape. Without this, a `send_email` step launched from a thread still
receives nothing, and SC#2 is two-thirds true.

⚠ **The rejected alternative is a security decision, not a UX one.** Letting the agent fill the
argument from the conversation was considered and refused under D-214-04: an LLM choosing a recipient
address is a new trust surface and is not reproducible between runs.

Output: one extracted shared renderer, one chat launch form, `doRun` gated on it, and the shipped
launch path untouched.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⚠ **FIRST ACTION IN ANY WORKTREE:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`.
⚠ **This plan does NOT edit `scripts/vitest-count-gate.cjs`.** Its chat-side cases go into the
existing **pinned** `ChatLayout.launch.test.tsx`, and `LaunchInputFields.test.tsx` lives under
`src/components/workflows`, already a `TARGETS` directory entry. Record the printed `— N new` figure
for plan `214-15` to pin.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/214-a-step-names-its-service-and-its-action/214-CONTEXT.md
@.planning/phases/214-a-step-names-its-service-and-its-action/214-PATTERNS.md
@.planning/reported-bugs/BUG-260826-01-send-email-arguments-unreachable-from-every-launch-path.md
@.planning/phases/214-a-step-names-its-service-and-its-action/214-09-SUMMARY.md

**PATTERNS.md § No Analog Found** is this plan's honest starting point: *"Chat has no launch moment
today. `RunModal.tsx` is the nearest shape but it is a library-page modal mounted from
`WorkflowsPage`; nothing in `ChatArea` / `MessageInput` / `ChatLayout` opens a per-run form. The plan
must decide the mount point; `ChatLayout.tsx` is the panel/route host and **fires G-5 at 21 phases**.
Nearest partial precedent: `ChatLayout.launch.test.tsx`."*
</context>

<interfaces>
<!-- What this plan EXTRACTS and both other launchers then import. -->

```
function LaunchInputFields(props: {
  fields: ReturnType<typeof entryInputFields>   // the definition's declared inputs
  values: Record<string, string>
  onChange: (key: string, value: string) => void
}): JSX.Element | null
```

⚠ Returns `null` for an empty `fields` array — so a caller renders nothing rather than an empty
region, and the *"no new screen for a definition that declares none"* property is the component's,
not each caller's.

⚠ **Two arms, never three** travels with the component: an AUTHORED label renders as prose; a key with
no label renders the key in the mono face. **Absence renders the key, never a fabricated friendly name.**
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract the one renderer — plan `214-09`'s named re-open trigger, honoured</name>
  <files>frontend/src/components/workflows/LaunchInputFields.tsx, frontend/src/components/workflows/library/RunModal.tsx, frontend/src/components/workflows/LaunchInputFields.test.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/library/RunModal.tsx` as edited by plan `214-09` — the field
      block to extract, and the two-arm docblock that travels with it.
    - `.planning/phases/214-a-step-names-its-service-and-its-action/214-09-SUMMARY.md` — ⭐ plan
      `214-09` **named this extraction and deliberately deferred it**, on the grounds that an
      extraction with one consumer is not an extraction. **This plan is its stated re-open trigger**,
      and it must either take the extraction or state why not.
    - `frontend/src/components/workflows/ExternalActionSection.tsx:64-77` — ⚠ **no runtime
      `export const` beside a component** (`react-refresh/only-export-components`, a measured ERROR on
      this directory).
    - `frontend/src/components/workflows/library/RunModal.tsx:490-556` — the docblock whose two-arm
      rule and whose preserved original sentence must **not** be orphaned by the extraction: keep the
      rule with the component and leave a pointer at the old site.
  </read_first>
  <action>
Extract the declared-input field block from `RunModal.tsx` into
`frontend/src/components/workflows/LaunchInputFields.tsx`, to the `<interfaces>` signature. Move the
**two-arm rule docblock with it** and leave a one-line pointer at the old site — a rule separated
from the code it governs is a rule nobody applies.

Re-point `RunModal.tsx` at the component. ⚠ **The render must be byte-equivalent**: plan `214-09`
re-baselined six whole-`innerHTML` captures, and this extraction must not move them again. Run those
captures before and after and assert they are **identical**; if they differ, the extraction changed
the DOM and the difference must be resolved, not re-baselined a second time in one phase.

Create `LaunchInputFields.test.tsx`: the two-arm rule (authored label → body face; bare key → mono
face); `null` for an empty `fields` array; `onChange` fires with the right key; no field is
pre-filled from anything but `values`; and a `?raw` fence asserting the component imports no store, no
context and no `@/lib/api` — it is a leaf that renders what it is handed.
  </action>
  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/LaunchInputFields.test.tsx src/pages/__tests__/RunModal.test.tsx` exits 0.
    - The six `RunModal` whole-`innerHTML` captures are **byte-identical** before and after this plan's diff (assert in the suite, and record both in the summary).
    - `grep -c "entryInputFields" frontend/src/components/workflows/library/RunModal.tsx` is ≥ 1 and the field-rendering JSX no longer appears inline there.
    - `grep -cE "useStore|useContext|@/lib/api" frontend/src/components/workflows/LaunchInputFields.tsx` is 0.
    - `cd frontend && npx eslint src/components/workflows/LaunchInputFields.tsx` reports 0 `react-refresh/only-export-components` errors.
    - `RunModal.test.tsx`'s per-file count does not decrease from its post-`214-09` value.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/LaunchInputFields.test.tsx src/pages/__tests__/RunModal.test.tsx</automated>
  </verify>
  <done>One renderer serves the library modal, its rule travelled with it, and the six captures are byte-identical rather than re-baselined twice.</done>
</task>

<task type="auto">
  <name>Task 2: The chat launch moment — gated, and the shipped path untouched</name>
  <files>frontend/src/components/layout/ChatLaunchForm.tsx, frontend/src/components/layout/ChatLayout.tsx, frontend/src/components/layout/ChatLayout.launch.test.tsx</files>
  <read_first>
    - `frontend/src/components/layout/ChatLayout.tsx:48-53, 252-330` — `doRun`, the Run-from-page
      launch. ⚠ **Its shipped contract is load-bearing and must not move**: *"reuses the EXISTING
      kickoff path — `createThread` + `sendMessage(workflow_definition_id)` — NEVER a bespoke
      `/workflows/{id}/run` route (D-103-CONF-1; threads.py byte-identical)"*, the template upload
      **BETWEEN** thread creation and `postMessage`, the orphan cleanup on a failed launch
      (`deleteLaunchThread`), and the per-run KB-folder override riding into
      `create_workflow_run.inputs`.
    - `frontend/src/components/layout/ChatLayout.tsx:229-250` — the `workflow_runs.id`-not-`runs.run_id`
      warning and `canvasEnabled`.
    - `frontend/src/components/layout/ChatLayout.launch.test.tsx:250-420` — the shipped launch suite,
      including the **flag-off** cases and the POSITIVE CONTROL. ⚠ Its case at `:502` asserts
      `WorkspacePanel` mounts **only** in the chat branch — a fact Phase 213 measured and that this
      plan must not disturb.
    - `frontend/src/components/workflows/LaunchInputFields.tsx` (Task 1).
    - ⭐ `frontend/src/components/layout/ChatLayout.tsx:711` — **`onLaunch={doRun}`, the ONE binding
      that makes `doRun` the page's launcher.** ⚠ **This line is in `ChatLayout.tsx`, not
      `WorkflowsPage.tsx`** (an earlier draft of this plan cited `WorkflowsPage.tsx:711`, which is
      `openBuilderFresh` — a different function entirely). `WorkflowsPage` never imports `doRun`; it
      receives it as the `onLaunch` prop.
    - ⭐ `frontend/src/pages/WorkflowsPage.tsx:909` and `:1351` — the **two shipped invocation sites** of
      that prop, measured with `grep -n "onLaunch" frontend/src/pages/WorkflowsPage.tsx` (which returns
      the type at `:221`, the destructure at `:267`, the guard at `:908`, and these two calls).
      `:1351` is the library Run modal, which passes `{ templateFile, folderId }`. `:909` is the
      builder's **Test Run** adapter: `WorkflowBuilderPage.tsx:1253-1271` calls an `onTestRun` prop,
      `WorkflowsPage.tsx:907` supplies it, and its body synthesises a `PublishedWorkflow` and calls
      **`onLaunch(def, "")` with no third argument** — so Test Run supplies no `inputs` and therefore
      gets the launch form for free under the gate below. **Measured, not assumed** (plan `214-09`'s
      W-9 finding). With the chat launch this plan adds, that is **three call sites**.
    - `frontend/src/pages/WorkflowsPage.tsx:221-225` — `WorkflowsPageProps.onLaunch`'s declared type, as
      widened by plan `214-09` with an optional `inputs` key. ⚠ **THE TYPE SYSTEM WILL NOT CATCH THE
      SILENT-DROP VARIANT.** A widened `onLaunch` stays assignable from a `doRun` whose `opts` parameter
      is narrower, under **parameter contravariance** — the extra key typechecks and is discarded. So the
      assertion has to be on a RUNTIME value that reached `postMessage`, never on the build passing.
    - `.planning/phases/214-a-step-names-its-service-and-its-action/214-16-SUMMARY.md` — the `postMessage`
      `inputs` option and the server merge. ⛔ **Do not invent a payload key**; use the one that plan
      defined, and read its recorded precedence rule before choosing key names.
  </read_first>
  <action>
Build `ChatLaunchForm.tsx` — a small sheet/dialog over `LaunchInputFields`, with a confirm control
that resolves the collected dict and a cancel that resolves nothing.

Widen `doRun`'s third parameter to `{ templateFile?, folderId?, inputs?: Record<string, string> }`,
and gate the form on **BOTH** terms:

```
if (def.inputs?.length && opts?.inputs === undefined) { ...open the form, await the dict... }
```

⭐ **THE SECOND TERM IS THE WHOLE POINT AND IT IS NOT OPTIONAL.** `doRun` is the launcher for
**three measured doors**, not one — the library Run modal (`WorkflowsPage.tsx:1351`, which after plan
`214-09` has ALREADY collected the values in `RunModal`), the chat launch this plan builds, and the
builder's **Test Run** (`WorkflowsPage.tsx:909`, which calls `onLaunch(def, "")` with no third
argument) — **plus every door added after this phase, which is the reason the gate lives in `doRun`
and not in each door.** ⚠ A future door is a reason, not a fourth data point; the count above is the
measured one. Gating on `def.inputs` alone would open `ChatLaunchForm` **on top of** a library
Run that had just collected the same values — asking the person twice for one launch, and
discarding what they already typed.

**The single collection point is `doRun`, and it collects only when nobody else did.** A caller that
supplies `inputs` has collected; a caller that supplies none has not. ⚠ **`RunModal`'s fields do NOT
come out** — they are the library door's own moment and plan `214-09` shipped them; what changes is that
`doRun` stops asking again.

⚠ **Before, not after** — the form resolves **before** `createThread`, so a cancelled launch creates
no thread and the orphan-cleanup path is never entered for a launch the person did not start. When
`def.inputs` is empty **or** `opts.inputs` is already present, `doRun` behaves **exactly as today**
with no new screen and no new await.

Forward the dict — whether collected here or handed in — through **`postMessage`'s `inputs` option,
which plan `214-16` added**. ⛔ **Do not add a route.** D-103-CONF-1's purpose is that there is ONE
kickoff path; plan `214-16` amended its *"threads.py byte-identical"* clause **in writing, with a
stated boundary** (one request field, one dict literal, no route, no response key), and this plan
stays inside that boundary. ⚠ **Read `214-16`'s amendment before touching the payload** — a plan
that re-violates a constraint someone else already re-decided has made the record incoherent.

⚠ **Do NOT let the agent fill anything.** No conversation reading, no inference, no default from the
last message. The values come from the person, in the form. This is D-214-04's rejected arm and it is
enforced by a fence: assert `ChatLaunchForm.tsx` contains no read of messages, thread state or model
output (`?raw`, with a non-vacuity control).

Extend `ChatLayout.launch.test.tsx` — **the existing pinned suite**, not a new file:
  - a definition with declared inputs opens the form and creates **no thread** until it is confirmed;
  - cancelling creates no thread and navigates nowhere;
  - confirming forwards the dict inside `postMessage`'s `inputs` option (assert the **argument object
    actually passed to `postMessage`**, not the DOM and not the type);
  - ⭐ **THE DOUBLE-PROMPT CASE.** Drive a **library** launch — `doRun(def, text, {inputs: {...}})`,
    the shape `WorkflowsPage.tsx:1351` produces after plan `214-09` — and assert `ChatLaunchForm`
    **did NOT mount** (`queryBy*` null) while `postMessage` still received the supplied dict intact.
    ⚠ Pair it with a positive control that the same definition DOES open the form when `inputs` is
    absent, or the absence assertion is free;
  - ⭐ **THE SILENT-DROP CASE**, which the compiler cannot give you: call `doRun` with an `inputs`
    key and assert the **same key/value pair** appears in `postMessage`'s options. Under parameter
    contravariance a narrower `opts` would typecheck and discard the key, so this must be a runtime
    value assertion;
  - **the Test Run arm**: `doRun(def, "")` with no third argument opens the form for a definition
    with declared inputs (the third measured door, at `WorkflowsPage.tsx:907-923`, invoked at `:909`);
  - a definition with **no** declared inputs launches with **zero** extra renders and the shipped
    assertions at `:250-340` pass **unchanged** — ⚠ that is the real regression fence;
  - the flag-off cases at `:374-410` still pass unchanged;
  - the `WorkspacePanel`-mounts-only-in-chat assertion at `:502` still passes.
  </action>
  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/layout/ChatLayout.launch.test.tsx` exits 0 with **no per-file decrease** and the six shipped launch cases passing with **zero edits** to their bodies (`git diff` on those line ranges shows no assertion changes).
    - A case asserts `createThread` is called 0 times while the form is open and 1 time after confirm.
    - A case asserts cancel leaves `createThread` at 0 and `deleteLaunchThread` at 0.
    - ⭐ A case drives a **library-shaped** call (`opts.inputs` present) and asserts `ChatLaunchForm` did NOT mount, paired with a positive control that the same definition opens it when `inputs` is absent.
    - ⭐ A case asserts the exact key/value pair handed to `doRun` in `opts.inputs` appears in the object passed to `postMessage` — a RUNTIME assertion, because contravariance makes the type check meaningless here.
    - A case asserts `doRun(def, "")` with **no** third argument opens the form (the Test Run door).
    - `grep -c "opts?.inputs === undefined\|opts?.inputs == null" frontend/src/components/layout/ChatLayout.tsx` is >= 1 — the second gate term exists in source, not only in a test.
    - `grep -c "workflows/.*\/run\b" frontend/src/components/layout/ChatLayout.tsx` is 0 — no bespoke route.
    - `grep -cE "messages|assistant|lastMessage|generate" frontend/src/components/layout/ChatLaunchForm.tsx` is 0 — the agent fills nothing.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — no more than 34 errors.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/layout/ChatLayout.launch.test.tsx</automated>
  </verify>
  <done>Chat collects declared inputs before creating anything, the zero-input path is byte-unchanged, no route was added, and the agent fills nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a person's typed value in a chat sheet → the run's `inputs` → an adapter argument → a vendor | ⚠ A **new** launch path into an outbound send. |
| ⛔ the conversation → an adapter argument | **This boundary must not be opened.** D-214-04 rejected letting the agent fill arguments from the thread; an LLM choosing a recipient is a new trust surface and is not reproducible. |
| a cancelled launch → a created thread | An orphaned thread is a resource created by an action the person abandoned. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-214-12-01 | Elevation of Privilege | the agent filling an outbound argument from conversation text | mitigate | Structurally prevented: `ChatLaunchForm` reads no messages, no thread state and no model output, asserted by a `?raw` fence with a non-vacuity control and by the `grep -c` criterion. ⚠ This is the rejected arm of D-214-04 and the fence is why the rejection is enforceable rather than a note. |
| T-214-12-02 | Spoofing | a second launch path with a different governance story | mitigate | No route is added; the existing `createThread` + `sendMessage(workflow_definition_id)` kickoff is reused, per D-103-CONF-1's *threads.py byte-identical* rule. Asserted by the zero-bespoke-route grep and by the six shipped launch cases passing unedited. |
| T-214-12-03 | Denial of Service | orphaned threads from abandoned launches | mitigate | The form resolves **before** `createThread`, so a cancel creates nothing. Tested by the cancel case asserting both `createThread` and `deleteLaunchThread` at 0. |
| T-214-12-04 | Tampering | the collected dict overwriting the KB-folder override | mitigate | The merge into `create_workflow_run.inputs` is explicit and tested: a case asserts the folder override survives alongside declared keys. |
| T-214-12-05 | Repudiation | a chat launch that silently sends nothing | mitigate | This plan's whole content. Guarded by the confirm case asserting the dict is inside the `postMessage` payload — the DOM is not the assertion. |
| T-214-12-SC | Tampering | package installs | mitigate | This plan installs nothing; the sheet reuses shipped shadcn primitives. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — no more than 34 errors.
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/layout/ChatLayout.launch.test.tsx src/components/workflows/LaunchInputFields.test.tsx src/pages/__tests__/RunModal.test.tsx` — green.
- Per-file deltas recorded; **none may decrease**.
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root, verdict line
  verbatim, paired with those deltas. ⚠ Do not write "the gate is green" as sole acceptance.
</verification>

<how_wed_know_this_failed>
From CONTEXT.md's `<failure_modes>` — this plan owns the third of the doors in #1:

1. **A `send_email` step still fails with `the 'to' recipient must be a string, got NoneType`** when
   launched **from a thread**. SC#2 unmet. ⚠ The DOM is not the proof: a form that renders and whose
   values never reach `create_workflow_run.inputs` fails identically to no form at all, which is
   `BUG-260826-01`'s own shape. The assertion is on the `postMessage` payload.

Also specific to this plan, and it is the failure that would ship as a UX bug rather than a crash:
**a library launch is prompted TWICE, or its values are silently dropped.** Gating `doRun` on
`def.inputs` alone opens `ChatLaunchForm` on top of `RunModal`'s already-collected values — and the
silent-drop variant is worse, because `WorkflowsPageProps.onLaunch` can be widened to carry `inputs`
while `doRun`'s narrower `opts` stays assignable under **contravariance**: the extra key typechecks,
is discarded, and every build stays green. ⚠ **`depends_on: ["214-09"]` did not surface this** —
a dependency edge orders two plans, it does not make either read the other's call sites. The two
guards are the second gate term in source and the runtime value assertion in the test.

Also specific to this plan: **the shipped launch path drifts.** `doRun` carries a template upload
sequenced BETWEEN thread creation and `postMessage`, an orphan cleanup, a flag gate and an anchor-id
rule — four measured behaviours, each with its own shipped case. **The regression fence is that those
six cases pass with zero edits to their bodies**, and a plan that edits them to make them pass has
removed the fence rather than satisfied it.
</how_wed_know_this_failed>

<g5_disposition>
Re-derived from git 2026-08-28:

| File | Ledger cell | **Measured** | Verdict |
|---|---|---|---|
| `frontend/src/components/layout/ChatLayout.tsx` | 40 / 21 / 815 | **(re-derive at execute time)** | ⚠ **FIRES — ledger says *absent from BOTH at 21 phases*, added only at 200** |
| `frontend/src/components/workflows/library/RunModal.tsx` | 4 / 3 / 526 | **6 / 4 / 655** | ⚠ **STALE — FIRES** |

**`ChatLayout.tsx` — honoured by construction, and the argument is that the new surface is a SIBLING,
not an addition.** The chat launch form is its own component in its own file; `ChatLayout.tsx` gains
one import, one piece of local open/resolve state and one `await` inside the existing `doRun`. ⛔ **No
new route, no new provider, no new panel branch** — the file's four measured behaviours (the kickoff
path, the template-upload ordering, the orphan cleanup, the flag gate) are untouched, and the six
shipped launch cases passing **unedited** is the falsifiable form of that claim. ⚠ This file was
invisible to G-5 for its entire life until Phase 200 added its row; plan `214-15` re-derives the
triple and corrects the cell.

**`RunModal.tsx` — the extraction its plan-`214-09` row deferred is TAKEN HERE**, on the ground that
the deferral's stated reason (*an extraction with one consumer is not an extraction*) stopped holding
the moment chat became the third consumer. That is a discharge, not a claim of construction, and plan
`214-15`'s ledger commit records it as such.
</g5_disposition>

<success_criteria>
- One field renderer serves the library modal, the schedule modal and chat.
- The six `RunModal` captures are byte-identical across this plan — re-baselined once in the phase, not twice.
- A chat launch of a workflow with declared inputs collects them before any thread exists.
- A library launch, which already collected in `RunModal`, is **not asked a second time** — asserted by an absence with a positive control.
- A Test Run of a workflow with declared inputs opens the form (the third measured door, `WorkflowsPage.tsx:909`).
- The dict handed to `doRun` reaches `postMessage`'s `inputs` option, asserted at RUNTIME rather than by the build.
- A cancel creates nothing and cleans nothing up, because nothing was created.
- A workflow declaring no inputs launches exactly as today, with the six shipped cases unedited.
- No bespoke run route exists and the chat form reads nothing from the conversation.
</success_criteria>

<output>
Create `.planning/phases/214-a-step-names-its-service-and-its-action/214-12-SUMMARY.md` when done.
Record: the before/after capture comparison, the printed `— N new` figure for the new suite, and the
exact payload key path the collected dict travels (plan `214-14`'s seam audit checks it against the
backend's `_external_action_inputs` read).
</output>
