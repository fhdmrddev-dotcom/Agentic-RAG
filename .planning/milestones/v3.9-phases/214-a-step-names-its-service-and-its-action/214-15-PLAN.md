---
phase: 214-a-step-names-its-service-and-its-action
plan: 15
type: execute
wave: 5
depends_on: ["214-14"]
files_modified:
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
  - scripts/vitest-count-gate.cjs
  - .planning/reported-bugs/BUG-260826-01-send-email-arguments-unreachable-from-every-launch-path.md
  - .planning/reported-bugs/BUG-260826-02-publish-gauntlet-does-not-validate-adapter-argument-satisfiability.md
  - .planning/reported-bugs/BUG-260826-05-run-failed-reason-empty-for-external-action-failure.md
  - .planning/reported-bugs/approval-pause-never-names-the-service.md
  - .planning/reported-bugs/BUG-260815-06-structural-gate-refusal-names-nothing-actionable.md
  - .planning/seeds/SEED-206-the-service-mark-belongs-on-every-step-surface-not-only-the-canvas.md
  - .planning/seeds/SEED-208-the-describe-door-hands-the-generator-its-connections-as-vocabulary.md
  - .planning/phases/214-a-step-names-its-service-and-its-action/214-UAT.md
autonomous: false
requirements: [STEP-01, STEP-02, STEP-03, STEP-04, STEP-05, STEP-06]

must_haves:
  truths:
    - "Every hot-file ledger row this phase touched is RE-DERIVED from git in one batch pass, and every row is synced with its `docs/HOT-FILE-LEDGER.md` section in the SAME commit."
    - "Every source file this phase CREATED has a ledger row — a young file gets one too, because an absence is invisible to G-5 exactly as a stale cell is."
    - "Every folded bug and seed carries the frontmatter that IS the index — `status:` and `folded_into:`, never prose in the body."
    - "D-214-13: `BUG-260815-06` stays `open` / `folded_into: null`, with its second decline recorded — it was read, understood and passed over twice."
    - "Every `T-214-NN-MM` mitigation across all sixteen threat models maps to a named test at a file:line, or it is an OPEN FINDING rather than a mitigation."
    - "The phase's lived-experience checks were DRIVEN by the operator, and a row that was not run says 'not run', never 'pass'."
  artifacts:
    - path: "docs/HOT-FILE-LEDGER.md"
      provides: "a section per row touched, in the same commit as the row"
      contains: "connectionMark"
    - path: "scripts/vitest-count-gate.cjs"
      provides: "a BASELINE pin per new suite, from the gate's own printed figure"
      contains: "StepIdentity"
    - path: ".planning/phases/214-a-step-names-its-service-and-its-action/214-UAT.md"
      provides: "the filled scoreboard — a verdict or a named ⛔ per row"
      contains: "openrouter"
  key_links:
    - from: "CLAUDE.md hot-file ledger table"
      to: "docs/HOT-FILE-LEDGER.md"
      via: "the same-commit sync rule, one row ↔ one section"
      pattern: "HOT-FILE-LEDGER"
    - from: "the sixteen plans' threat models"
      to: "the mitigation→test map in this plan's summary"
      via: "one row per T-214-NN-MM, each with a test file:line"
      pattern: "T-214-"
---

<objective>
Close the phase: sync the hot-file ledger, pin the gate, write the registers, map every threat-model
mitigation to the test that exercises it, and put the lived-experience checks in front of the operator.

Purpose: three recorded failures, each of which this plan exists to prevent recurring.

1. ⚠ **Phase 203 wrote three threat-model mitigations and implemented none while every task passed.**
   **Sixteen** threat models ship in this phase (plan `214-16` was added at revision time). **A mitigation with no test row is an open finding.**
2. ⚠ **A re-derive of thirty-one hot-file rows once found FIVE wrong, THREE of them reading
   `satisfied`** — the exact state `StreamsProvider.tsx`'s row was in when it turned out to be wrong by
   28 phases. And `libraryFilter.ts` was invisible to G-5 for four phases, `ChatLayout.tsx` for
   twenty-one, `config.py` for forty-two: **absences, not stale cells.**
3. ⚠ **Phase 213 closed once at `93fc2f521` with every gate green and its headline feature INERT.**
   The post-flight refused it; a driven check found it. That is why Task 2 is `gate="blocking"`.

Output: one ledger commit carrying rows and sections together, one pinned gate, seven written
registers, one mitigation→test map, and one operator drive.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⚠ **FIRST ACTION IN ANY WORKTREE:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`.
⚠ **SERIALIZE THIS PLAN.** The operator drive in Task 2 mutates the local database, and it is the last
thing the phase does. It is alone in wave 5.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/214-a-step-names-its-service-and-its-action/214-CONTEXT.md
@.planning/phases/214-a-step-names-its-service-and-its-action/214-UAT.md
@.planning/phases/214-a-step-names-its-service-and-its-action/214-14-SUMMARY.md

**All fifteen prior SUMMARYs are required reading** (plans `214-01`..`214-14` and `214-16`). Each carries a `<g5_disposition>` block with a
re-derived triple, and each records the gate's printed `— N new` figure for any suite it created.
**This plan uses those figures; it does not derive its own.**
</context>

<tasks>

<task type="auto">
  <name>Task 1: The ledger, the gate pins, and the registers — each written, none merely noted</name>
  <files>CLAUDE.md, docs/HOT-FILE-LEDGER.md, scripts/vitest-count-gate.cjs, .planning/reported-bugs/BUG-260826-01-send-email-arguments-unreachable-from-every-launch-path.md, .planning/reported-bugs/BUG-260826-02-publish-gauntlet-does-not-validate-adapter-argument-satisfiability.md, .planning/reported-bugs/BUG-260826-05-run-failed-reason-empty-for-external-action-failure.md, .planning/reported-bugs/approval-pause-never-names-the-service.md, .planning/reported-bugs/BUG-260815-06-structural-gate-refusal-names-nothing-actionable.md, .planning/seeds/SEED-206-the-service-mark-belongs-on-every-step-surface-not-only-the-canvas.md, .planning/seeds/SEED-208-the-describe-door-hands-the-generator-its-connections-as-vocabulary.md</files>
  <read_first>
    - `CLAUDE.md` → *Workflow guardrails* → the hot-file ledger table and its rules: it is the **AUDIT
      SCAN LIST** and is deliberately complete; ⚠ **the disposition cell is capped at 200 chars** by
      `scripts/check-claude-md-size.cjs`; ⚠ **SAME-COMMIT SYNC RULE** — a row without a section, or a
      section without a row, is drift.
    - `CLAUDE.md` → the re-derivation recipe. ⚠ **Six-digit buckets (`260814`, `260828`, …) are DATED
      QUICK TASKS, not phases — subtract them.**
    - `docs/HOT-FILE-LEDGER.md` — the per-file section format, one anchor per row.
    - All fifteen `214-NN-SUMMARY.md` `<g5_disposition>` blocks — the triples each plan re-derived and
      the disposition each argued.
    - `scripts/vitest-count-gate.cjs` — `TARGETS` (paths) and `BASELINE` (basenames). ⚠ **A `BASELINE`
      key naming a path that does not exist makes the gate ERROR**, which is why every pin in this
      phase is collected here, after all files exist.
    - `.planning/reported-bugs/TEMPLATE.md` and each report's current frontmatter.
    - ⚠ **`status:` frontmatter IS the index.** Prose inside a record is invisible to the scan.
  </read_first>
  <action>
**(a) The ledger — re-derive, do not copy forward.** Run CLAUDE.md's recipe over **every** file this
phase's sixteen plans touched, in **one batch pass**, and compare against the table. ⚠ The phase's
own planning already found rows stale on `ConnectionPicker.tsx`, `ExternalActionSection.tsx`,
`McpToolPicker.tsx`, `phase_types.py`, `grounding.py`, `doorVocabulary.ts`, `WorkflowRunPage.tsx`,
`db/workflows.py`, `api/workflow_runs.py`, `models/harness.py`, `connectionMark.tsx`,
`RunTranscript.tsx`, `WorkflowDoorSwitch.tsx`, `WorkflowBuilderPage.tsx` and `RunModal.tsx` —
**re-derive at execute time anyway**, because that list was measured at plan time and this ledger's own
finding is that a figure written at a phase's close goes stale on the next commit.

Update every row **and** its `docs/HOT-FILE-LEDGER.md` section **in one commit**. ⚠ Keep each
disposition cell **under 200 characters** — put the verdict in the cell and the reasons in the section.

⚠ **`connectionMark.tsx`'s row is MOVED, not edited** — its path changed in plan `214-08`, and a row
whose path no longer exists is invisible to the audit scan. Re-key the row and move its section under
the new anchor.

**Add rows that do not exist yet**, so the scan list stays complete:
`backend/app/services/harness/reachability.py`, `frontend/src/components/workflows/WorkflowScheduleModal.tsx`,
`backend/app/services/connectors/args.py`, `frontend/src/components/workflows/ArgumentEditor.tsx`,
⭐ **and the four the mechanical seam derivation exposed, three of which have been invisible to
G-5 for their entire lives**: `backend/app/services/workflow_kickoff.py` (it builds the LIVE run
context and its absence is why the `ctx.inputs` mirror went unowned in this phase's first plan set),
`backend/app/models/message.py` (on every chat send), `frontend/src/lib/api/threads.ts` (⚠ **not
covered by `frontend/src/lib/api.ts`'s row — that row is the BARREL**), and
`frontend/src/providers/StreamsProvider.tsx` (row exists; re-derive and correct — its cell reads
*honoured by construction (194.1)*). Also
`ArgumentRow.tsx`, `argumentModel.ts`, `argumentVocabulary.ts`, `publishRefusalVocabulary.ts`,
`stepIdentityVocabulary.ts`, `StepIdentity.tsx`, `PublishRefusalList.tsx`, `LaunchInputFields.tsx`,
`ChatLaunchForm.tsx`, `DescribeServicePicker.tsx`, `describeServiceMatch.ts`.
⚠ **A young file gets a row too** — `libraryFilter.ts` was invisible to G-5 for four phases and
`ChatLayout.tsx` for twenty-one, and both were absences, not stale cells.

⚠ **Record explicitly, in the sections, the three obligations this phase does NOT discharge:**
`api/workflows.py` (*extraction due*), `grounding.py` (*extraction still owed*), and
`ExternalActionSection.tsx`/`ConnectionPicker.tsx`'s named seams. **A `honoured` cell must not read as
`satisfied`.**

Run `node scripts/check-claude-md-size.cjs` and record the printed character count and percentage.

**(b) The gate pins.** Add every new suite's `BASELINE` key, using the **printed `— N new` figures
recorded in the SUMMARYs**, never a hand-derived number. Add any `TARGETS` path a new suite needs
(plan `214-08` already moved `connectionMark.test.tsx`'s). Then run the gate and read the verdict line
verbatim, **paired with the per-file deltas**.

⚠ **Two suites created by this revision's new plans need no `TARGETS` edit, and that was CHECKED
rather than assumed** — record the check: plan `214-16` creates no frontend suite at all (it extends
the already-pinned `src/lib/apiRunFields.fences.test.ts`, a `TARGETS` **file** entry), and plan
`214-07`'s reachability case lives inside the already-owned `ArgumentEditor.test.tsx` under
`src/components/workflows`, a `TARGETS` **directory** entry. **TARGETS decides what RUNS; BASELINE
decides what is GUARDED.**

⛔ **Backend suites are not gate-pinned** (the gate is vitest-only). Plan `214-16`'s
`backend/tests/integration/test_214_launch_inputs_wire.py` and plan `214-14`'s
`test_214_argument_seams.py` are guarded by their plans' own pytest criteria and by the backend
failure-count baseline — record that as a decision, not a silence, so nobody later reads the gate's
green as covering them.

**(c) The registers — written, not noted.**
  - `BUG-260826-01` / `-02` / `-05` and `BUG-260828-01`: already `status: folded`, `folded_into: 214`
    at plan time — **verify, and set `verified_closed_by: 214` only for those the driven check in the
    checkpoint actually confirms.** ⛔ A bug closed on a green gate rather than a drive stays `folded`.
    ⭐ **`BUG-260826-01` in particular needs BOTH halves and they are in different plans**: the
    launcher forms (`214-09`, `214-12`) and **the wire** (`214-16`, whose integration suite reads
    `workflow_runs.inputs` back out of Postgres). ⚠ It may be closed only if G-4 check 2 was
    driven on **all three** doors. Two of three is `folded`, with the un-driven door named.
  - `BUG-260815-06`: stays `open`, `folded_into: null`. Its `re_open_trigger` already records the
    second decline — **verify it is intact and add nothing** (D-214-13).
  - `SEED-206`: flip to `verified_closed_by`-equivalent only if plan `214-11`'s five-surface coverage
    is confirmed by the checkpoint; otherwise leave `folded` and say why.
  - `SEED-208`: same treatment against plan `214-13`.
  - `SEED-214`: stays `partially-folded`. `SEED-199` stays `planted`. `BUG-260823-04` stays `open`,
    reviewed-not-folded.
  - ⚠ **Every edit is to FRONTMATTER.** A record whose body says "shipped" while `status:` says
    `open` will be re-proposed forever.
  </action>
  <acceptance_criteria>
    - `node scripts/check-claude-md-size.cjs` exits 0 and its printed count is recorded in the summary.
    - Every row this task edits has a matching anchor in `docs/HOT-FILE-LEDGER.md`: a script or manual diff proves `rows == sections` for the touched set, and the count is recorded.
    - `grep -c "components/settings/connectionMark" CLAUDE.md docs/HOT-FILE-LEDGER.md` is 0 — the row moved with the file.
    - Every disposition cell edited is ≤ 200 characters (the size gate enforces it; record that it fired zero `[disposition-too-long]`).
    - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root, run from the repo root, verdict line recorded **verbatim**. The PASS/FAIL criteria are the two deterministic halves: **`N/N pinned files present`** and **no per-file DECREASE**. Both must hold.
    - Every suite this phase created or edited was ALSO run explicitly by path and its per-file count recorded; **none may decrease**. That list, not the whole-gate line, is what a reviewer checks.
    - ⚠ **`0 failing` IS RECORDED AS AN OBSERVATION AND TRIAGED — IT IS NOT A BALD GATE HERE, AND THIS PLAN IS THE ONE THAT WOULD OTHERWISE GET IT WRONG.** `SEED-171` records **five** suites that flake independent of the worker cap, three of them failing with `AssertionError` rather than a timeout, and **three sit inside this phase's blast radius**: `src/pages/WorkflowsPage.test.tsx`, `src/pages/WorkflowBuilderPage.canvas.test.tsx`, `src/pages/WorkflowRunPage.test.tsx`. If the gate reds: capture failing filenames from the gate's **own persisted JSON BEFORE re-running anything**, check each against `git diff --numstat <base> HEAD` and `git status --short`, and if a named file is **byte-unchanged and on SEED-171's list**, record it as *"provably unmodified"* — never *"fine"*, and never *"pass"*. ⛔ **Do not touch the worker cap** — that remedy is MEASURED not to fix these (CLAUDE.md, correction (b) of 2026-08-17).
    - ⛔ **A red gate whose failures are NOT on SEED-171's list, or ARE in a file this phase changed, is a REAL failure and blocks.** `196-08` hit `failed 249` and every one was real. **What separates the two cases is the PROCEDURE, not the colour.**
    - `grep -c "folded_into: 214" .planning/reported-bugs/*.md .planning/seeds/*.md` is ≥ 6.
    - `.planning/reported-bugs/BUG-260815-06-*.md` still reads `status: open` and `folded_into: null`.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-claude-md-size.cjs &amp;&amp; GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>Every touched row re-derived and synced with its section in one commit, every new file has a row, every pin is a printed figure, and every register's frontmatter is written.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: G-4 — the lived-experience checks, driven</name>
  <what-built>
Sixteen plans: the shared argument predicate, the argument editor with its three-arm source picker,
the deleted JSON surface, the publish refusal, the approval pause naming its service, the step
identity on five run surfaces, the failure reason reaching the panel, real launch fields on three
doors, the describe door's service vocabulary and its refusal, and the canvas flag flipped to
`everyone` at its cold default.

Every automated gate is recorded in this plan's Task 1 and in `214-14-SUMMARY.md`. **They are not the acceptance bar.**
  </what-built>
  <how-to-verify>
⚠ **Wire format and screenshots are insufficient (G-4).** Drive these in a real browser.
⚠ **Say which flag state each row ran under.** `visual_workflow_canvas` now cold-reads `everyone`;
`live_connectors` is still `off`, so **any live outbound send is a deliberate local arming** and must
be recorded as such.

The scoreboard, the eight-provider roster and the four-axis rows are in
**`.planning/phases/214-a-step-names-its-service-and-its-action/214-UAT.md`**. Run it, and record each
row's verdict there — ⛔ **a blocked row is recorded as ⛔ with its reason and its blocking id, never
silently omitted.**

The four operator-defined *"I'd recognise failure here"* checks, in order:

1. **Author a `send_email` step end to end.** Pick a service, pick `send_email`, and confirm the form
   shows exactly `to`, `subject`, `body` — **no `Cc`, no `Reply to`, and no JSON box anywhere, in any
   state**. Set `to` to *Asked when this runs*, `subject` to *Set here*, `body` to *From an earlier
   step*. Confirm the body row arrived pre-set and that you could change it.
2. **Launch it three ways** — from the library, from a thread, and on a schedule — and confirm each
   asks you for `to` and that the run actually receives it. ⚠ This is `BUG-260826-01`, the milestone's
   blocking defect; if any door still sends nothing, SC#2 is unmet.
3. **Break it and publish.** Remove `to`'s source and publish: confirm the refusal **names your step
   by the name you gave it and names `to`**, and that it does not mention a gauntlet stage. Then set
   `to` to *Asked when this runs* without declaring the key and confirm you get the **different**
   refusal.
4. **Watch it fail, and watch it pause.** Run a step that will fail and confirm the **panel** shows the
   adapter's own sentence — not *"Failure reason not captured by the backend"* — and that chat and the
   panel say the same thing at the same moment. Then arm a step and confirm the pause **names the
   service** (not `post_message` twice, not a nameless MCP row) and lists **all three** arguments,
   including the one you asked for at launch and the one from the earlier step.

Also confirm, in one pass: the builder panel is visibly wider than before and the source lane lines up
across sourced and unsourced rows; a service you have not connected, named in the describe box,
produces a refusal that names it and offers two next actions and **drafts nothing**.
  </how-to-verify>
  <files>.planning/phases/214-a-step-names-its-service-and-its-action/214-UAT.md</files>
  <action>
Hand the operator the eight checks below and the `214-UAT.md` scoreboard, wait, and **write their
observations into `214-UAT.md` verbatim** — a verdict, a named ⛔ with its blocking id, or the words
**not run**. Do not paraphrase a partial into a pass, and do not close a row the operator did not
reach. Any defect they report is a finding recorded in the summary with the check that surfaced it;
⚠ **a fix authored in response needs its own new UAT row**, because a fix verified only by the test
written for it has been verified by its author.
  </action>
  <resume-signal>Type "approved" or describe what you saw. ⚠ A row you did not run is "not run", not a pass.</resume-signal>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/214-a-step-names-its-service-and-its-action/214-UAT.md','utf8');const rows=s.split('
').filter(l=>/^\| *[0-9G]/.test(l));const blank=rows.filter(l=>/\|\s*\|\s*$/.test(l));if(blank.length){console.error('unfilled UAT rows: '+blank.length);process.exit(1)}console.log('UAT rows all carry a verdict: '+rows.length)"</automated>
    <human-check>The operator ran the eight checks in a real browser and reported what they saw.</human-check>
  </verify>
  <done>Every roster row, axis row and G-4 row in `214-UAT.md` carries a verdict, a named ⛔, or **not run**; the operator's response and any defect they found are recorded in the summary.</done>
</task>


</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the phase's own gates → the claim that it shipped | ⚠ **Green gates are not evidence of a feature existing.** Phase 213 closed once with every gate green and the approval moment inert. |
| a threat model → the code that was written | ⚠ A mitigation is a sentence until a test exercises it. Phase 203 shipped three sentences. |
| a register's frontmatter → every future sweep | `status:` IS the index. A record whose body says "shipped" while `status:` says `open` will be re-proposed forever. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-214-15-01 | Tampering | a threat-model mitigation written and not implemented | mitigate | The **mitigation→test map**: one row per `T-214-NN-MM` across all sixteen threat models, each naming a test file and line. ⚠ **An unmapped row is recorded as an OPEN FINDING in the summary, not quietly dropped** — that distinction is the whole content of the Phase 203 lesson. |
| T-214-15-02 | Repudiation | a hot-file row present and WRONG | mitigate | A single batch re-derivation with CLAUDE.md's recipe over every file the phase touched, dated buckets subtracted, compared against the table. ⚠ **A wrong row answers the auditor with a pass and stops the audit** — worse than an absent one. |
| T-214-15-03 | Repudiation | a new source file with no ledger row | mitigate | Every file the phase created gets a row, young or not. Asserted by comparing the phase's created-file list against the table. ⚠ G-5 cannot fire on a file that is not in the scan list. |
| T-214-15-04 | Tampering | ledger prose growing back into CLAUDE.md | mitigate | `node scripts/check-claude-md-size.cjs` runs and must report zero `[disposition-too-long]`. The 200-char cap exists because that column reached **60,558 chars — 45% of the file** and forced the 2026-08-25 split. |
| T-214-15-05 | Spoofing | a bug closed on a green gate rather than a drive | mitigate | `verified_closed_by: 214` is set **only** for reports the Task 2 drive confirms. Everything else stays `folded`. ⚠ Phase 212 closed on green gates and the operator then found two more defects, one of which nobody had thought to look for. |
| T-214-15-06 | Repudiation | a UAT row silently omitted | mitigate | `214-UAT.md`'s own rule: a blocked row is ⛔ with a named reason and a blocking id; a row not driven says **not run**. **A scoreboard that lists only what passed is not a scoreboard.** |
| T-214-15-SC | Tampering | package installs | mitigate | This plan installs nothing. |
</threat_model>

<verification>
- `node scripts/check-claude-md-size.cjs` — exit 0; the printed character count and percentage recorded.
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root — **verdict line
  verbatim**, with `N/N pinned files present`, the grand total and the pinned total recorded, **paired
  with the per-file deltas of every suite this phase created or edited** rather than standing alone.
  ⚠ A growing total is the gate **WORKING**; its contract is *no per-file DECREASE* and
  *`N/N pinned present`*, never a fixed number.
  ⭐ **AND `count gate OK` IS NOT RELIABLY REACHABLE ON DEMAND**, so it is not this plan's sole
  acceptance. Three of `SEED-171`'s five cap-independent flaky suites — `WorkflowsPage.test.tsx`,
  `WorkflowBuilderPage.canvas.test.tsx`, `WorkflowRunPage.test.tsx` — sit in this phase's blast
  radius. On a red run: filenames from the persisted JSON **first**, then `git diff --numstat`, then a
  verdict of *"provably unmodified"* or *"real"*. ⛔ **Never the worker cap.**
- `cd backend && venv/Scripts/python -m pytest tests/unit -q` — no more than 68 failed, recorded.
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — no more than 34 errors, recorded.
- `bash scripts/check-deploy-drift.sh` — exit 0, recorded as unaffected.
- The **mitigation→test map** for all sixteen threat models, in the summary, with a file:line per row
  and every unmapped row named as an open finding.
- `214-UAT.md` filled in: every roster row, every axis row and every G-4 row carries a verdict, a
  named ⛔, or the words **not run**.
</verification>

<how_wed_know_this_failed>
From CONTEXT.md's `<failure_modes>` — this plan owns #9 and is the last chance on all ten:

9. **A ledger row is updated without its `docs/HOT-FILE-LEDGER.md` section in the same commit**, or a
   triple is copied forward rather than re-derived. Both are checkable: the row↔section count and the
   batch re-derivation, both recorded in the summary.

Also specific to this plan, and it is the one that has actually happened here before:

**The phase closes on green gates.** Phase 213 did exactly that — every gate green, the approval
moment inert, the whole ask/refusal vocabulary shipped consumed by nothing, and `ask` being inert made
the MCP path **more permissive than before the phase**. ⚠ The gates in this plan's `<verification>`
block are **evidence of non-regression, not evidence of arrival.** Task 2's drive is the arrival
check, and closing without it is legitimate only as an explicitly recorded DECISION naming the owed
rows and which one to run first — never as a claim that they ran.

And: **the registers get "noted" rather than written.** A seed that shipped but still reads `planted`
is re-proposed forever; a bug whose body says "fixed" while `status:` says `open` is invisible to every
sweep. ⚠ `grep -rln "SEED" .claude/commands/gsd/` returns **`capture.md` only** — the writer. Nothing
executable reads these registers, so the frontmatter edit in this plan is the entire mechanism.
</how_wed_know_this_failed>

<g5_disposition>
This plan touches **no application source at all**. Its files are CLAUDE.md, `docs/HOT-FILE-LEDGER.md`,
`scripts/vitest-count-gate.cjs`, seven register files and the UAT scoreboard.

⚠ **`scripts/vitest-count-gate.cjs` FIRES G-5** — its ledger row reads `118 / 21 / 3909` with the
disposition *⚠ absent at 16 phases (196) — the file that ENFORCES the guardrails.* Re-derive it in this
plan's own batch pass; the irony of a stale row on the guardrail enforcer is exactly the kind of thing
this ledger keeps finding. **Disposition: honoured by construction** — this plan's diff is `BASELINE`
key additions taken from the gate's own printed output, plus at most one `TARGETS` path. No logic
changes.

**This plan IS the phase's ledger sync**, so its own obligation is discharged by construction: it is
the single commit that carries every row and every section together.
</g5_disposition>

<success_criteria>
- Every hot-file row this phase touched is re-derived in one batch pass and synced with its section in one commit.
- Every source file the phase created has a ledger row, including the young ones.
- `connectionMark.tsx`'s row was MOVED, not edited — no stale path survives in CLAUDE.md or the detail file.
- Every disposition cell is ≤ 200 characters and `check-claude-md-size.cjs` reports zero `[disposition-too-long]`.
- Every new suite has a `BASELINE` pin taken from the gate's own printed figure.
- The gate's verdict line is recorded **verbatim** and PAIRED with the deterministic facts: `N/N pinned files present` and **no per-file decrease**, plus the explicitly-run in-scope suites. ⚠ **`0 failing` is an OBSERVATION TO TRIAGE, never a bald pass/fail** — SEED-171 records five suites that flake independent of the worker cap, three of them (`WorkflowsPage.test.tsx`, `WorkflowBuilderPage.canvas.test.tsx`, `WorkflowRunPage.test.tsx`) inside this phase's blast radius. A red run is triaged by filenames from the gate's own persisted JSON checked against `git diff --numstat`, ⛔ never by touching the cap — and a red file NOT on SEED-171's list still blocks.
- Every folded bug and seed carries the frontmatter that is the index; `BUG-260815-06` still reads `open` / `null`.
- A mitigation→test map exists for all sixteen threat models, with every unmapped row named as an open finding.
- The operator drove the eight-row roster, the three axis rows and the eight G-4 checks, and every row in `214-UAT.md` is a verdict, a named ⛔, or **not run**.
</success_criteria>

<output>
Create `.planning/phases/214-a-step-names-its-service-and-its-action/214-15-SUMMARY.md` when done.
It carries: the re-derived ledger triples with a before/after per changed cell, the row↔section count,
the CLAUDE.md character count, every gate's verdict line verbatim, the mitigation→test map, the
register edits, and the operator's checkpoint response — including anything they saw that no gate did.
</output>
