---
phase: 212-the-catalog-and-its-doors
plan: 05
type: execute
wave: 4
depends_on: [212-03, 212-04]
files_modified:
  - frontend/src/lib/__tests__/apiBarrel.test.ts
  - frontend/src/components/settings/__tests__/connectionVerbFence.test.ts
  - scripts/vitest-count-gate.cjs
  - backend/tests/unit/test_212_discover_seam.py
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-05, CONN-06, CONN-07]

must_haves:
  truths:
    - "D-207-06 is guarded: all symbols in frontend/src/lib/api/connectors.ts are re-exported by lib/api.ts without drift"
    - "scripts/vitest-count-gate.cjs pins servicesCatalog.test.ts and apiBarrel.test.ts, growing the count gate (116 -> 118 pinned files)"
    - "A mock-neither integration test verifies the discover-tools contract end-to-end (S-3 seam check: server_url, tools, count, name, title, description, inputSchema, outputSchema)"
    - "Zero verb filter chips or organizing verb axes remain in any settings component (connectionVerbFence)"
    - "G-5 hot-file ledger rows and detail sections are updated with git-derived counts for ConnectionsTab.tsx, ConnectionFormPanel.tsx, connectionsCopy.ts, connectionFormCopy.ts, SettingsPage.tsx, and ModelPillRow.tsx"
    - "All gates pass: tsc error count <= 34 (baseline), vitest count gate OK (118/118 pinned, 0 failing), backend rot set <= 68"
  artifacts:
    - path: "frontend/src/lib/__tests__/apiBarrel.test.ts"
      provides: "Automated test asserting D-207-06 barrel completeness for lib/api.ts"
    - path: "backend/tests/unit/test_212_discover_seam.py"
      provides: "Cross-plan seam integration test for discover-tools contract"
    - path: "scripts/vitest-count-gate.cjs"
      provides: "Updated count gate targets pinning new test suites"
    - path: "CLAUDE.md"
      provides: "Updated G-5 hot-file ledger rows with git-verified commit/phase/line counts"
    - path: "docs/HOT-FILE-LEDGER.md"
      provides: "Updated G-5 ledger sections and history entries"
  key_links:
    - from: "frontend/src/lib/__tests__/apiBarrel.test.ts"
      to: "frontend/src/lib/api.ts"
      via: "barrel inspection"
      pattern: "connectors"
---

<objective>
Execute cross-plan integration checks, barrel completeness tests (`D-207-06`), count-gate adoption (`GATE-1`), G-5 hot-file ledger audits, and the full multi-gate verification gauntlet.

Purpose: Guarantee that all Phase 212 deliverables join seamlessly (`S-3`), no symbols drift from the `lib/api` barrel, new test suites are adopted into the count gate, hot-file ledger records are accurately updated, and all quality gates are 100% green.

Output: `apiBarrel.test.ts`, `test_212_discover_seam.py`, updated `scripts/vitest-count-gate.cjs`, updated `CLAUDE.md` and `docs/HOT-FILE-LEDGER.md`, and clean gate verification outputs.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/212-the-catalog-and-its-doors/212-CONTEXT.md
@.planning/phases/212-the-catalog-and-its-doors/212-RESEARCH.md
@.planning/phases/212-the-catalog-and-its-doors/212-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" id="212-05-01">
<name>Add D-207-06 barrel completeness test and pin new test files in vitest-count-gate.cjs</name>
<files>
  - frontend/src/lib/__tests__/apiBarrel.test.ts
  - frontend/src/components/settings/__tests__/connectionVerbFence.test.ts
  - scripts/vitest-count-gate.cjs
</files>
<action>
1. Create `frontend/src/lib/__tests__/apiBarrel.test.ts`:
   - Inspect all exported symbols from `frontend/src/lib/api/connectors.ts`.
   - Assert every exported function and type is re-exported from `frontend/src/lib/api.ts`.
2. In `scripts/vitest-count-gate.cjs`:
   - Add `"src/components/settings/__tests__/servicesCatalog.test.ts"` and `"src/lib/__tests__/apiBarrel.test.ts"` to `TARGETS`.
   - Update pinned target count from 116 to 118.
3. Run `connectionVerbFence.test.ts` and verify all verb fence assertions pass.
</action>
<verify>
GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs
</verify>
<done>
`apiBarrel.test.ts` passes, count gate adopts the two new test suites, and verb fence passes.
</done>
</task>

<task type="auto" id="212-05-02">
<name>Author S-3 cross-plan discover seam integration test and update G-5 ledger</name>
<files>
  - backend/tests/unit/test_212_discover_seam.py
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
</files>
<action>
1. Create `backend/tests/unit/test_212_discover_seam.py`:
   - Test the pre-save discovery contract end-to-end: verify `POST /api/connectors/discover-tools` matches the wire payload shape expected by `probeMcpServer` (`server_url`, `tools`, `count`, and tool fields `name`, `title`, `description`, `inputSchema`, `outputSchema`).
2. Re-derive G-5 metrics for files touched/affected by Phase 212:
   - `frontend/src/components/settings/ConnectionsTab.tsx`
   - `frontend/src/components/settings/ConnectionFormPanel.tsx`
   - `frontend/src/components/settings/connectionsCopy.ts`
   - `frontend/src/components/settings/connectionFormCopy.ts`
   - `frontend/src/pages/SettingsPage.tsx`
   - `frontend/src/components/settings/ModelPillRow.tsx`
3. Update the ledger rows in `CLAUDE.md` and detail sections in `docs/HOT-FILE-LEDGER.md` with git-verified counts and Phase 212 disposition summaries.
</action>
<verify>
node scripts/check-claude-md-size.cjs
</verify>
<done>
Cross-plan seam integration test passes and G-5 hot-file ledger rows and detail sections are accurately updated.
</done>
</task>

<task type="auto" id="212-05-03">
<name>Run full multi-gate verification battery</name>
<files>
  - package.json
</files>
<action>
1. Run frontend typecheck:
   `npx tsc --noEmit -p tsconfig.app.json` (from `frontend/`) → verify <= 34 baseline errors.
2. Run frontend Vitest Count Gate:
   `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` → verify OK, 118/118 pinned files, 0 failed.
3. Run backend unit tests:
   `./venv/Scripts/python.exe -m pytest tests/unit -q` → verify rot set <= 68 baseline and 0 connector failures.
</action>
<verify>
GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs
</verify>
<done>
All gates pass cleanly across frontend and backend suites.
</done>
</task>

</tasks>
