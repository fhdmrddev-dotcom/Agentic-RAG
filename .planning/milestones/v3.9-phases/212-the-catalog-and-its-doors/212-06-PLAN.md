---
phase: 212-the-catalog-and-its-doors
plan: 06
type: execute
wave: 5
depends_on: [212-04, 212-05]
files_modified:
  - frontend/src/components/settings/servicesCatalog.ts
  - frontend/src/components/settings/ConnectionsTab.tsx
  - frontend/src/components/settings/__tests__/ConnectionsTab.test.tsx
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-05]

must_haves:
  truths:
    - "ConnectionsTab merges the services catalog registry into the unified 5-column table so unconfigured services appear as browsable catalog rows (SC#1, CAT-02)"
    - "Unconfigured catalog service rows render Mark, Name, one-line Purpose tagline in Column 1, default host with 🔒 in Column 2, '—' in Column 3, 'Not set' in Column 4, and 'Not connected' in Column 5"
    - "Unconfigured catalog service rows provide a Connect button (and row click) that opens ConnectionFormPanel with presetServiceId pre-filled (SC#3)"
    - "In the default view (unfiltered, no query), Popular services and All services are grouped cleanly with header dividers inside the single unified table per G-2 sketch 203 Section 4"
    - "The 'Not connected' filter chip shows unconfigured catalog services plus any configured connections that are unready or disabled"
    - "Search query filters across both configured connections and unconfigured catalog services"
    - "Starter prompts are NOT added to the presentation layer (CAT-04 is deferred to Phase 216)"
  artifacts:
    - path: "frontend/src/components/settings/servicesCatalog.ts"
      provides: "Expanded curated services catalog with default hosts"
      contains: "defaultHost"
    - path: "frontend/src/components/settings/ConnectionsTab.tsx"
      provides: "Unified catalog and connection management view merging configured DB rows and catalog services with one-click Connect and group headers"
      contains: "CATALOG_SERVICES"
    - path: "frontend/src/components/settings/__tests__/ConnectionsTab.test.tsx"
      provides: "Unit tests asserting browsable catalog rows, one-click Connect trigger, Not connected filter behavior, and Popular grouping"
  key_links:
    - from: "frontend/src/components/settings/ConnectionsTab.tsx"
      to: "frontend/src/components/settings/servicesCatalog.ts"
      via: "CATALOG_SERVICES, POPULAR_SERVICES"
      pattern: "CATALOG_SERVICES"
---

<objective>
Close the Phase 212 catalog reachability gap by merging the presentation catalog registry (`servicesCatalog.ts`) into `ConnectionsTab.tsx`.

Purpose: Enable users to browse the catalog of services (`SC#1`), connect curated Popular services in one click with `presetServiceId` (`SC#3`), filter by `Not connected` to find unconfigured services, and view Popular and All services grouped inside the single unified table per Sketch 203 Section 4.

Output: Updated `servicesCatalog.ts`, `ConnectionsTab.tsx`, and `ConnectionsTab.test.tsx`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/212-the-catalog-and-its-doors/212-CONTEXT.md
@.planning/sketches/203-the-catalog-at-density/index.html
@CLAUDE.md
</context>

<tasks>

<task type="auto" id="212-06-01">
<name>Expand servicesCatalog with default hosts and curated services</name>
<files>
  - frontend/src/components/settings/servicesCatalog.ts
</files>
<action>
1. In `frontend/src/components/settings/servicesCatalog.ts`:
   - Add `defaultHost?: string` property to `CatalogServiceEntry`.
   - Update entries in `POPULAR_SERVICES` and `CATALOG_SERVICES` with their default endpoints/hosts (e.g. `hooks.slack.com`, `acme.atlassian.net`, `smtp.fastmail.com`, `api.github.com`, `googleapis.com`, `api.notion.com`, `mcp.internal.corp`).
   - Add standard curated catalog entries from Sketch 203 Section 4 (e.g. `linear` -> `api.linear.app`, `figma` -> `api.figma.com`, `sentry` -> `sentry.io`).
</action>
<verify>
npx vitest run src/components/settings/__tests__/servicesCatalog.test.ts
</verify>
<done>
Catalog entries carry default hosts and curated service definitions.
</done>
</task>

<task type="auto" id="212-06-02">
<name>Merge catalog services into ConnectionsTab and wire one-click Connect</name>
<files>
  - frontend/src/components/settings/ConnectionsTab.tsx
</files>
<action>
1. In `frontend/src/components/settings/ConnectionsTab.tsx`:
   - Import `CATALOG_SERVICES`, `POPULAR_SERVICES`, and `CatalogServiceEntry` from `servicesCatalog.ts`.
   - Update `ConnectionsTabViewProps.onAdd` to accept optional `serviceId?: string`.
   - In `ConnectionsTabView`:
     - Construct unified display items from `connections` (configured DB rows) and `CATALOG_SERVICES`:
       - Track configured services by `row.service_id.toLowerCase()`.
       - For catalog services without a configured DB row, generate unconfigured catalog display items with:
         - `id`: `catalog:${entry.serviceId}`
         - `serviceId`: `entry.serviceId`
         - `name`: `entry.name`
         - `tagline`: `entry.tagline`
         - `destination`: `entry.defaultHost || "—"`
         - `usedBy`: `—`
         - `credential`: `Not set`
         - `state`: `not_connected`
         - `isConfigured`: `false`
         - `isPopular`: `entry.isPopular`
     - Filtering logic:
       - `filterState === "ready"`: keep configured rows where `connectionStateOf(row) === "ready"`.
       - `filterState === "not_connected"`: keep unconfigured catalog items PLUS configured rows where `connectionStateOf(row) !== "ready"`.
       - `filterState === null` (All): keep configured rows + unconfigured catalog items.
       - Search query matching: match across configured rows and unconfigured catalog entries (name, service_id, tagline, description, host).
     - Section grouping (Sketch 203 Section 4):
       - When `filterState === null` and `!query.trim()`, render group dividers:
         - `<div className="grouphd bg-muted/15 border-t border-border/40 px-3.5 py-1.5 font-mono text-[10px] uppercase tracking-wider text-muted-foreground">Popular</div>`
         - `<div className="grouphd bg-muted/15 border-t border-border/40 px-3.5 py-1.5 font-mono text-[10px] uppercase tracking-wider text-muted-foreground">All services</div>`
     - Unconfigured catalog row rendering:
       - Column 1: Mark + Name + Purpose tagline.
       - Column 2: `🔒` + default host.
       - Column 3: `—`.
       - Column 4: `Not set`.
       - Column 5: `Not connected` (dim/hollow dot).
       - Column 6: `Connect` button (e.g. `data-testid="connections-catalog-connect"`) invoking `onAdd(serviceId)`. Clicking the row or name also calls `onAdd(serviceId)`.
     - In container component `ConnectionsTab`:
       - `onAdd={(presetServiceId) => setPanelState({ mode: "create", connection: null, presetServiceId: typeof presetServiceId === "string" ? presetServiceId : null })}`
       - Ensure `panel={<ConnectionFormPanel ... presetServiceId={panelState.presetServiceId ?? null} />}` passes `presetServiceId` to pre-select the service when creating.
</action>
<verify>
npx vitest run src/components/settings/__tests__/ConnectionsTab.test.tsx
</verify>
<done>
ConnectionsTab renders browsable catalog services, group headers, and one-click Connect affordance.
</done>
</task>

<task type="auto" id="212-06-03">
<name>Add unit tests for catalog browsing, Not connected filter, and one-click Connect</name>
<files>
  - frontend/src/components/settings/__tests__/ConnectionsTab.test.tsx
</files>
<action>
1. In `frontend/src/components/settings/__tests__/ConnectionsTab.test.tsx`:
   - Add test: when `connections = []` (0 DB connections), table renders the browsable catalog of services with Popular and All services group headers.
   - Add test: unconfigured catalog rows render Mark, Name, Tagline, default host, `—` used by, `Not set` credential, `Not connected` state, and a `Connect` button.
   - Add test: clicking `Connect` on an unconfigured catalog service invokes `onAdd` with that service's ID.
   - Add test: filtering by `Not connected` includes unconfigured catalog services.
   - Add test: searching for a catalog service (e.g. "GitHub" or "Notion") returns the matching catalog row.
   - Add test: when a service IS connected in the DB (e.g. Slack in `THREE_ROWS`), it renders as a connected row without creating a duplicate unconfigured row.
   - Add test: zero `title` attribute rule holds across all new nodes.
</action>
<verify>
npx vitest run src/components/settings/__tests__/ConnectionsTab.test.tsx
</verify>
<done>
All tests in ConnectionsTab.test.tsx pass cleanly with 100% assertions green.
</done>
</task>

</tasks>

<verifier>
1. `npm test -- src/components/settings/__tests__/ConnectionsTab.test.tsx` -> 100% passing.
2. `node scripts/vitest-count-gate.cjs` -> 118/118 pinned files OK, 0 failing.
3. `npx tsc --noEmit -p tsconfig.app.json` -> 34 baseline errors (0 errors in Phase 212 files).
4. `pytest tests/unit/test_*connector*.py tests/unit/test_212*.py` -> 100% passing.
</verifier>
