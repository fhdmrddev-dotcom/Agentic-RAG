---
phase: 212-the-catalog-and-its-doors
plan: 03
type: execute
wave: 3
depends_on: [212-02]
files_modified:
  - frontend/src/components/settings/ConnectionFormPanel.tsx
  - frontend/src/components/settings/connectionFormCopy.ts
  - frontend/src/components/settings/__tests__/ConnectionFormPanel.test.tsx
autonomous: true
requirements: [CONN-06, CONN-07]

must_haves:
  truths:
    - "Typing or pasting an MCP server URL allows clicking 'Discover Tools' to invoke probeMcpServer and preview discovered tool names, titles, descriptions, and input schemas before saving (CONN-06)"
    - "Selecting a curated Popular Service pre-populates form fields, labels, placeholders, and help links from servicesCatalog.ts (CAT-02)"
    - "Updating an existing connection preserves all existing tool_grants decisions untouched; newly discovered tools default to ungranted (CONN-07)"
    - "Deleting a connection checks usageCounts and displays an impact-aware victim-naming confirmation sheet if published workflows depend on it (CONN-07)"
  artifacts:
    - path: "frontend/src/components/settings/ConnectionFormPanel.tsx"
      provides: "The enhanced 400px push/split Add & Edit connection panel with MCP discovery preview, template initialization, and grant preservation"
      contains: "probeMcpServer"
    - path: "frontend/src/components/settings/__tests__/ConnectionFormPanel.test.tsx"
      provides: "Unit tests asserting interactive discovery, grant preservation merge, and deletion sheet contracts"
  key_links:
    - from: "frontend/src/components/settings/ConnectionFormPanel.tsx"
      to: "frontend/src/components/settings/servicesCatalog.ts"
      via: "getServiceCatalogEntry"
      pattern: "getServiceCatalogEntry"
    - from: "frontend/src/components/settings/ConnectionFormPanel.tsx"
      to: "frontend/src/lib/api.ts"
      via: "probeMcpServer"
      pattern: "probeMcpServer"
---

<objective>
Enhance the Add and Edit Connection panel with interactive pre-save MCP discovery via `probeMcpServer`, popular service templates, key-preserving grant merge, and impact-aware deletion.

Purpose: Allow users to preview and inspect arbitrary MCP tools before persisting (`CONN-06`), configure popular services smoothly (`CAT-02`), and safely update/delete connections without destroying per-tool permissions or breaking active workflows (`CONN-07`).

Output: Updated `ConnectionFormPanel.tsx`, `connectionFormCopy.ts`, and updated `ConnectionFormPanel.test.tsx`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/212-the-catalog-and-its-doors/212-CONTEXT.md
@.planning/phases/212-the-catalog-and-its-doors/212-RESEARCH.md
@.planning/phases/212-the-catalog-and-its-doors/212-UI-SPEC.md
@.planning/phases/212-the-catalog-and-its-doors/212-VALIDATION.md
@.planning/sketches/203-the-catalog-at-density/index.html
@CLAUDE.md

<interfaces>
`frontend/src/components/settings/servicesCatalog.ts`:
- `getServiceCatalogEntry(serviceId: string | null | undefined): CatalogServiceEntry`

`frontend/src/lib/api.ts`:
- `probeMcpServer(payload: McpProbeRequest): Promise<McpProbeResponse>`
- `type McpDiscoveredTool`
</interfaces>
</context>

<tasks>

<task type="auto" id="212-03-01">
<name>Add interactive pre-save MCP discovery and schema preview using probeMcpServer</name>
<files>
  - frontend/src/components/settings/ConnectionFormPanel.tsx
  - frontend/src/components/settings/connectionFormCopy.ts
</files>
<action>
1. In `frontend/src/components/settings/connectionFormCopy.ts`:
   - Add copy constants for discovery button (`DISCOVER_BUTTON_LABEL`, `DISCOVERING_LABEL`), tool preview header, schema viewer toggles, and discovery error alerts.
2. In `frontend/src/components/settings/ConnectionFormPanel.tsx`:
   - Add discovery state: `isDiscovering: boolean`, `discoveredPreview: McpDiscoveredTool[] | null`, `discoveryError: string | null`.
   - Wire "Discover Tools" button alongside MCP URL input.
   - When clicked, invoke `probeMcpServer({ mcp_server_url: draft.mcp_server_url, headers: draft.headers })`.
   - On success, populate `discoveredPreview` and render the interactive tools list with tool name, title, description, and expandable JSON Schema property viewer.
   - On error, display formatted error diagnostic alert (SSRF refusal vs network unreachable vs invalid payload).
</action>
<verify>
npx vitest run src/components/settings/__tests__/ConnectionFormPanel.test.tsx
</verify>
<done>
ConnectionFormPanel allows interactive tool discovery and preview via `probeMcpServer` before saving custom MCP connections.
</done>
</task>

<task type="auto" id="212-03-02">
<name>Implement Popular service template pre-fill and key-preserving grant merge</name>
<files>
  - frontend/src/components/settings/ConnectionFormPanel.tsx
</files>
<action>
1. In `ConnectionFormPanel.tsx`:
   - When creating a connection from a catalog preset (passed via `initialServiceId` prop or selected in form), initialize the draft with `service_id`, default name, placeholders, and tailored credential fields from `servicesCatalog.ts`.
   - On save/update of an existing connection:
     - Merge existing `connection.tool_grants` with newly discovered/static tools using `mergeToolGrants` logic (preserving all existing granted/denied values for tools that still exist; defaulting new tools to false).
   - In deletion handler:
     - Inspect `usageCounts[connection.id] || 0`.
     - If `usageCounts > 0`, format the confirmation sheet with victim-naming warning detailing that published workflows reference this connection.
</action>
<verify>
npx vitest run src/components/settings/__tests__/ConnectionFormPanel.test.tsx
</verify>
<done>
Popular service templates pre-fill accurately, tool grants are preserved across updates, and deletion is impact-aware.
</done>
</task>

<task type="auto" id="212-03-03">
<name>Update ConnectionFormPanel unit test suite</name>
<files>
  - frontend/src/components/settings/__tests__/ConnectionFormPanel.test.tsx
</files>
<action>
1. Update `ConnectionFormPanel.test.tsx`:
   - Add tests asserting interactive MCP discovery triggers `probeMcpServer` and renders preview.
   - Add tests asserting that editing an existing connection with tool updates preserves prior `tool_grants` keys.
   - Add tests verifying the impact-aware deletion warning when `usageCounts > 0`.
   - Run vitest suite and ensure all tests pass cleanly.
</action>
<verify>
npx vitest run src/components/settings/__tests__/ConnectionFormPanel.test.tsx
</verify>
<done>
All ConnectionFormPanel tests pass with 0 failures.
</done>
</task>

</tasks>
