---
phase: 212-the-catalog-and-its-doors
plan: 04
type: execute
wave: 3
depends_on: [212-02]
files_modified:
  - frontend/src/components/settings/ConnectionsTab.tsx
  - frontend/src/components/settings/connectionsCopy.ts
  - frontend/src/components/settings/__tests__/ConnectionsTab.test.tsx
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-05]

must_haves:
  truths:
    - "ConnectionsTab renders the single unified catalog list locked in G-2 sketch 203-the-catalog-at-density (CAT-01, CAT-02)"
    - "Column 1 contains the service mark, service name, and the one-line purpose beneath it (truncating on one line at density)"
    - "Uncurated / custom MCP connections display the quiet mono provenance badge ADDED BY URL beside the name, preserving the distinction between provenance and connection state"
    - "Filter chips are strictly state-based (All / Connected / Not connected); zero verb/capability filter chips appear anywhere (CAT-03)"
    - "Not-connected services do not render false tool counts"
    - "When live_connectors is off (cloud cold default), the tab displays an honest platform policy banner instructing that an operator flips the switch in Control Room (CAT-05, BUG-260810-01)"
  artifacts:
    - path: "frontend/src/components/settings/ConnectionsTab.tsx"
      provides: "The G-2 sketch-aligned Connections catalog table with one-line purpose in column 1, ADDED BY URL provenance tag, state chips, and cloud policy guidance"
      contains: "PROVENANCE_ADDED_BY_URL"
    - path: "frontend/src/components/settings/__tests__/ConnectionsTab.test.tsx"
      provides: "Unit tests asserting sketch-aligned catalog layout, search, state filtering, provenance tagging, and cloud parity"
  key_links:
    - from: "frontend/src/components/settings/ConnectionsTab.tsx"
      to: "frontend/src/components/settings/servicesCatalog.ts"
      via: "getServiceCatalogEntry"
      pattern: "getServiceCatalogEntry"
    - from: "frontend/src/components/settings/ConnectionsTab.tsx"
      to: "frontend/src/components/settings/connectionMark.tsx"
      via: "ConnectionMarkGlyph"
      pattern: "ConnectionMarkGlyph"
---

<objective>
Implement the unified Services Catalog table aligned with G-2 sketch `203-the-catalog-at-density`, purpose in column 1, provenance badge, state filtering, and cloud parity guidance.

Purpose: Deliver the operator-approved catalog UX (`CAT-01`, `CAT-02`), enforce state-only filtering (`CAT-03`), and resolve the cloud "missing add button" issue (`CAT-05`, `BUG-260810-01`) with honest, non-overclaiming messaging.

Output: Updated `ConnectionsTab.tsx`, `connectionsCopy.ts`, and updated `ConnectionsTab.test.tsx`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/212-the-catalog-and-its-doors/212-CONTEXT.md
@.planning/phases/212-the-catalog-and-its-doors/212-RESEARCH.md
@.planning/phases/212-the-catalog-and-its-doors/212-UI-SPEC.md
@.planning/phases/212-the-catalog-and-its-doors/212-VALIDATION.md
@.planning/sketches/203-the-catalog-at-density/index.html
@CLAUDE.md

<interfaces>
`frontend/src/components/settings/servicesCatalog.ts`:
- `getServiceCatalogEntry(serviceId: string | null | undefined): CatalogServiceEntry`
</interfaces>
</context>

<tasks>

<task type="auto" id="212-04-01">
<name>Render sketch-aligned unified catalog table with purpose and provenance badge in ConnectionsTab</name>
<files>
  - frontend/src/components/settings/ConnectionsTab.tsx
  - frontend/src/components/settings/connectionsCopy.ts
</files>
<action>
1. In `frontend/src/components/settings/ConnectionsTab.tsx`:
   - Import `getServiceCatalogEntry` from `servicesCatalog.ts` and `PROVENANCE_ADDED_BY_URL` from `catalogCopy.ts`.
   - Update Column 1 (`Connection`) to render:
     - Service mark (`ConnectionMarkGlyph`, passing `shape: { service_id: row.service_id, mcp_server_url: row.mcp_server_url }`).
     - Service Name (`nm`), with `<span className="prov">ADDED BY URL</span>` rendered beside the name if `row.mcp_server_url` is present and `row.service_id` is not in the curated preset list (or if `row.service_id` is a hostname like `mcp.deepwiki.com`).
     - Line 2 inside Column 1: One-line Purpose (`purpose`), populated from `getServiceCatalogEntry(row.service_id).tagline` (or custom fallback), truncating on one line (`truncate text-xs text-muted-foreground`).
   - Preserve the locked 5-column contract: `Connection · Sends to · Used by · Credential · State`.
   - Ensure not-connected services in the catalog do NOT display an invented tools count badge.
</action>
<verify>
npx vitest run src/components/settings/__tests__/ConnectionsTab.test.tsx
</verify>
<done>
ConnectionsTab renders the unified 5-column catalog table with purpose and provenance badges per G-2 sketch.
</done>
</task>

<task type="auto" id="212-04-02">
<name>Implement state-based filtering and honest cloud policy banner</name>
<files>
  - frontend/src/components/settings/ConnectionsTab.tsx
  - frontend/src/components/settings/connectionsCopy.ts
</files>
<action>
1. In `ConnectionsTab.tsx`:
   - Wire state filter chips: `All` | `Connected` | `Not connected` (`filterState`).
   - Filter rows based on `connectionStateOf(row)`:
     - `All`: show all rows.
     - `Connected`: show rows where `connectionStateOf(row) === "ready"` (or active).
     - `Not connected`: show rows where `connectionStateOf(row) !== "ready"`.
   - Assert zero verb/capability filter chips are present in DOM.
   - Update platform policy banner for when `liveConnectorsOn` is false:
     - State honestly: *"Live sending is off for this platform. An operator turns `live_connectors` on in the Control Room."*
     - Avoid breaking links to `/admin` for non-operator users (retaining honest copy without assuming every org admin is a platform operator).
</action>
<verify>
npx vitest run src/components/settings/__tests__/ConnectionsTab.test.tsx
</verify>
<done>
State-based filtering and honest cloud policy banner are active and tested.
</done>
</task>

<task type="auto" id="212-04-03">
<name>Update ConnectionsTab unit test suite</name>
<files>
  - frontend/src/components/settings/__tests__/ConnectionsTab.test.tsx
</files>
<action>
1. Update `ConnectionsTab.test.tsx`:
   - Test rendering of the 5-column table with purpose in column 1.
   - Test `ADDED BY URL` badge on uncurated / custom MCP rows.
   - Test search filtering by service name and tagline.
   - Test state filter chips (`All`, `Connected`, `Not connected`) and assert absence of verb chips.
   - Test cloud parity: when `liveConnectorsOn` is false, assert policy banner renders with honest operator instruction.
   - Verify all tests pass cleanly.
</action>
<verify>
npx vitest run src/components/settings/__tests__/ConnectionsTab.test.tsx
</verify>
<done>
All ConnectionsTab unit tests pass with 0 failures.
</done>
</task>

</tasks>
