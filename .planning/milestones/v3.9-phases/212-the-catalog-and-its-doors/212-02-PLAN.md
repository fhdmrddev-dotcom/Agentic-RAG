---
phase: 212-the-catalog-and-its-doors
plan: 02
type: execute
wave: 2
depends_on: [212-01]
files_modified:
  - frontend/src/components/settings/servicesCatalog.ts
  - frontend/src/components/settings/catalogCopy.ts
  - frontend/src/components/settings/connectionMark.tsx
  - frontend/src/lib/api/connectors.ts
  - frontend/src/lib/api.ts
  - frontend/src/components/settings/__tests__/servicesCatalog.test.ts
  - frontend/src/components/settings/__tests__/connectionMark.test.tsx
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03]

must_haves:
  truths:
    - "servicesCatalog.ts provides a presentation registry keyed on service_id with curated entries for Slack, Jira, SMTP/Email, GitHub, Google Workspace, Notion, and Custom MCP (D-211-02)"
    - "A lookup for an unknown service_id or hostname degrades gracefully to neutral mark and generic metadata without throwing or hiding the row"
    - "connectionMark.tsx resolves official brand marks for known service_id strings while preserving ink contract (self / fill / stroke)"
    - "frontend/src/lib/api/connectors.ts exports probeMcpServer without overwriting existing discoverConnectorTools(id), and lib/api.ts re-exports it without barrel drift (D-207-06, S-1 blocker fix)"
    - "McpDiscoveredTool is reused from @/lib/api/connectors.ts rather than inventing a non-existent DiscoveredTool type (S-2)"
  artifacts:
    - path: "frontend/src/components/settings/servicesCatalog.ts"
      provides: "The authored catalog presentation registry, field templates, and starter prompts"
      exports: ["CATALOG_SERVICES", "POPULAR_SERVICES", "getServiceCatalogEntry", "type CatalogServiceEntry"]
    - path: "frontend/src/components/settings/catalogCopy.ts"
      provides: "Catalog UI copy, section headers, search placeholders, and empty state strings"
      exports: ["CATALOG_SEARCH_PLACEHOLDER", "PROVENANCE_ADDED_BY_URL"]
    - path: "frontend/src/components/settings/connectionMark.tsx"
      provides: "Service-identity aware mark resolver supporting service_id keys"
      exports: ["connectionMark", "ConnectionMarkGlyph"]
    - path: "frontend/src/lib/api/connectors.ts"
      provides: "probeMcpServer client function hitting POST /api/connectors/discover-tools while preserving discoverConnectorTools(id)"
      exports: ["probeMcpServer", "discoverConnectorTools"]
    - path: "frontend/src/components/settings/__tests__/servicesCatalog.test.ts"
      provides: "Unit tests for catalog lookup totality, preset data, and fallback behavior"
  key_links:
    - from: "frontend/src/components/settings/servicesCatalog.ts"
      to: "frontend/src/components/settings/connectionMark.tsx"
      via: "markKey reference"
      pattern: "markKey"
    - from: "frontend/src/lib/api.ts"
      to: "frontend/src/lib/api/connectors.ts"
      via: "barrel re-export"
      pattern: "probeMcpServer"
---

<objective>
Build the presentation-driven services catalog registry, update the service mark resolver, and export the pre-save tool discovery API client with its own distinct symbol.

Purpose: Provide the single source of truth for service branding, taglines, default form templates, and starter prompts (`CAT-01`, `CAT-02`), and enable `probeMcpServer` across the frontend without breaking existing `discoverConnectorTools(id)` (`S-1` blocker).

Output: `servicesCatalog.ts`, `catalogCopy.ts`, updated `connectionMark.tsx`, and updated `api/connectors.ts`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/212-the-catalog-and-its-doors/212-CONTEXT.md
@.planning/phases/212-the-catalog-and-its-doors/212-RESEARCH.md
@.planning/phases/212-the-catalog-and-its-doors/212-UI-SPEC.md
@.planning/phases/212-the-catalog-and-its-doors/212-VALIDATION.md
@.planning/sketches/203-the-catalog-at-density/index.html
@CLAUDE.md

<interfaces>
`frontend/src/lib/api/connectors.ts`:
- Existing: `export async function discoverConnectorTools(id: string): Promise<McpDiscoveredTool[]>` (DO NOT OVERWRITE)
- New: `export async function probeMcpServer(payload: McpProbeRequest): Promise<McpProbeResponse>`
- Types: `McpDiscoveredTool`
</interfaces>
</context>

<tasks>

<task type="auto" id="212-02-01">
<name>Author servicesCatalog.ts and catalogCopy.ts presentation modules</name>
<files>
  - frontend/src/components/settings/servicesCatalog.ts
  - frontend/src/components/settings/catalogCopy.ts
</files>
<action>
1. Create `frontend/src/components/settings/servicesCatalog.ts`:
   - Define types `CatalogServiceEntry`, `ServiceFieldTemplate`, `ServiceCategory`.
   - Register curated Popular services: `slack`, `jira`, `smtp`, `github`, `google`, `notion`, `custom_mcp`.
   - Each entry declares: `serviceId`, `name`, `tagline` (the one-line purpose for column 1), `description`, `markKey`, `isPopular`, `category`, `fieldTemplates`, and `starterPrompts`.
   - Implement `getServiceCatalogEntry(serviceId: string | null | undefined): CatalogServiceEntry`:
     - If `serviceId` matches a registered key, return the curated entry.
     - If `serviceId` is empty or unknown (e.g. custom hostname like `mcp.deepwiki.com`), construct a clean dynamic fallback entry with `name: serviceId || "Custom MCP"`, `tagline: "Connected MCP service"`, `markKey: "mcp"`, `isPopular: false`.
     - Guarantee totality: never throws and never returns null.
2. Create `frontend/src/components/settings/catalogCopy.ts`:
   - Export copywriting constants for catalog headings, state filter labels (`All`, `Connected`, `Not connected`), search placeholders, `PROVENANCE_ADDED_BY_URL = "ADDED BY URL"`, and empty states.
</action>
<verify>
npx vitest run src/components/settings/__tests__/servicesCatalog.test.ts
</verify>
<done>
`servicesCatalog.ts` and `catalogCopy.ts` provide curated metadata and total fallback lookup for all catalog services.
</done>
</task>

<task type="auto" id="212-02-02">
<name>Update connectionMark.tsx to resolve marks by service_id with ink contract</name>
<files>
  - frontend/src/components/settings/connectionMark.tsx
  - frontend/src/components/settings/__tests__/connectionMark.test.tsx
</files>
<action>
1. Update `frontend/src/components/settings/connectionMark.tsx`:
   - Widen `ConnectionMarkShape` to include `service_id?: string | null`.
   - Add icon components / mapping for `github`, `google`, `notion` if needed, or map to existing logo/lucide components.
   - Update `connectionMark(shape)` resolution logic:
     1. If `shape?.service_id` is present, check `MARKS` by `service_id` (e.g. `slack`, `jira`, `smtp`, `github`, `google`, `notion`).
     2. If `shape?.mcp_server_url` or `shape?.tool_name` is present, resolve to `MCP_MARK`.
     3. If `shape?.capability` is present, resolve legacy capabilities (`send_email`, `create_ticket`, `post_message`).
     4. Fall back to `NEUTRAL_MARK` (`Plug`).
   - Preserve ink contract (`self`, `fill`, `stroke`) and own-property checks.
2. Update `connectionMark.test.tsx` with test cases verifying `service_id` resolution.
</action>
<verify>
npx vitest run src/components/settings/__tests__/connectionMark.test.tsx
</verify>
<done>
`connectionMark.tsx` cleanly resolves service marks by `service_id` and passes all ink contract assertions.
</done>
</task>

<task type="auto" id="212-02-03">
<name>Add probeMcpServer API client function and re-export in barrel</name>
<files>
  - frontend/src/lib/api/connectors.ts
  - frontend/src/lib/api.ts
</files>
<action>
1. In `frontend/src/lib/api/connectors.ts`:
   - PRESERVE existing `discoverConnectorTools(id: string)` intact!
   - Define TypeScript types `McpProbeRequest { mcp_server_url: string; headers?: Record<string, string>; timeout?: number }` and `McpProbeResponse { server_url: string; tools: McpDiscoveredTool[]; count: number }`.
   - Implement `probeMcpServer(payload: McpProbeRequest): Promise<McpProbeResponse>` calling `POST /api/connectors/discover-tools`.
2. In `frontend/src/lib/api.ts`:
   - Re-export `probeMcpServer`, `McpProbeRequest`, `McpProbeResponse` in the barrel to adhere to `D-207-06`.
</action>
<verify>
npx tsc --noEmit -p tsconfig.app.json
</verify>
<done>
`probeMcpServer` is exported from `lib/api/connectors.ts` alongside preserved `discoverConnectorTools` and re-exported in `lib/api.ts` with zero type errors.
</done>
</task>

</tasks>
