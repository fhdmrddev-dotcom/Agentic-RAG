---
phase: 226-the-public-landing-page
plan: 05
type: execute
wave: 4
depends_on:
  - 226-04
files_modified:
  - docs/LANDING.md
  - .planning/seeds/SEED-241-living-landing-page.md
  - .github/workflows/landing-drift.yml
  - scripts/check-landing-drift.cjs
  - scripts/check-deploy-drift.sh
autonomous: true
requirements:
  - D-226-05
  - SC#1
  - SC#3
  - SC#4
  - SC#6
  - SC#7
  - SEED-241
user_setup: []

must_haves:
  truths:
    - "docs/LANDING.md documents landing page architecture, fact mappings, B2B routing (/app vs /), continuous loop behavior, and the landing-worthy closeout touchpoint"
    - "SEED-241 frontmatter status is flipped to folded"
    - ".github/workflows/landing-drift.yml runs check-landing-drift.cjs on push and pull_request across relevant paths"
    - "scripts/check-landing-drift.cjs is driven RED by a simulated source divergence, confirms exit 1, and returns to exit 0 on clean tree"
    - "Production built landing chunk greps completely clean for supabase, /lib/api, StreamsProvider, and OrgProvider (SC#1, F-3)"
    - "scripts/check-deploy-drift.sh passes clean on phase-226"
    - "The production landing JS bundle is under 400 KB gzipped, and CSS size is reported"
    - "scripts/vitest-count-gate.cjs is untouched; new suites are recorded as OWED pins for Claude's merge commit (F-6)"
    - "Chrome UAT script and steps are documented for Claude reviewer post-phase signoff"
  artifacts:
    - docs/LANDING.md
    - .github/workflows/landing-drift.yml
  key_links:
    - "docs/LANDING.md provides the narrative and maintenance standard for future phase builders"
---

<objective>
Document the landing surface, demonstrate the drift guard firing RED, establish CI workflow, verify deployment consistency and production bundle metrics, and prepare the UAT pack:
1. Write `docs/LANDING.md` describing the landing page purpose, architecture, section catalog, source-of-truth mappings, B2B routing (`/app` vs `/`), continuous loops, and the `landing-worthy?` closeout checklist. Update any doc mentions of `localhost:5173` to clarify `/app`.
2. Flip `SEED-241` frontmatter status to `status: folded` (Advisory A-9).
3. Create `.github/workflows/landing-drift.yml` running `node scripts/check-landing-drift.cjs` on push/PR for landing files and sources (F-5).
4. Execute mandatory RED-first validation for `scripts/check-landing-drift.cjs` (temporarily alter a source value, observe exit code 1, restore, observe exit code 0).
5. Build production bundle (`npm --prefix frontend run build`), measure gzipped JS and CSS sizes (confirming JS gz < 400 KB), and grep the built landing chunk to prove absence of `supabase`, `/lib/api`, `StreamsProvider`, and `OrgProvider` (F-3, SC#1).
6. Verify `bash scripts/check-deploy-drift.sh`.
7. Prepare G-4 UAT verification pack for Claude reviewer, and document the 4 new test suites as OWED pins for `scripts/vitest-count-gate.cjs` without modifying that hot file (F-6).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/226-the-public-landing-page/226-PROPOSAL.md
@.planning/phases/226-the-public-landing-page/226-PREFLIGHT.md
@.planning/phases/226-the-public-landing-page/226-CONTEXT.md
@.planning/seeds/SEED-241-living-landing-page.md
@frontend/src/landing/facts.ts
@scripts/check-landing-drift.cjs
@scripts/check-deploy-drift.sh
</context>

<threat_model>
ASVS L1 Threat Assessment:
- Threat: Unobserved security or configuration drift during deployment or ongoing maintenance.
  Mitigation: `check-deploy-drift.sh` and `.github/workflows/landing-drift.yml` run in CI. Built chunk grep guarantees no auth/API code leaks into public production assets.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Documentation, SEED-241 Folding, and CI Workflow</name>
  <files>docs/LANDING.md, .planning/seeds/SEED-241-living-landing-page.md, .github/workflows/landing-drift.yml</files>
  <read_first>.planning/phases/226-the-public-landing-page/226-PROPOSAL.md, .planning/seeds/SEED-241-living-landing-page.md</read_first>
  <action>
    1. Create `docs/LANDING.md`:
       - Purpose: Public B2B marketing surface at `/` isolated from the app SPA.
       - Architecture: Dual HTML entry (`index.html` = landing, `app.html` = app), Vercel filesystem precedence, dev server middleware.
       - Routing note: `localhost:5173/` is the landing; the app lives at `localhost:5173/app`.
       - Section map: Full breakdown of the 16 sections.
       - Living facts & Sources of Truth: Exact mappings between `facts.ts` and code files (`MODEL_CAPABILITIES`, `acceptedFormats.ts`, `PublishGauntlet.tsx`, `_TOOL_REGISTRY`, `servicesCatalog.ts`, and tab files).
       - Anti-Drift Guard: Explanation of `check-landing-drift.cjs`, hook registration, and CI workflow.
       - B2B Routing & Placeholders: `VITE_DEMO_URL` and `VITE_APP_URL` (`${VITE_APP_URL}/app`).
       - Continuous Loops: Explanation of infinite keyframe loops and reduced-motion explicit terminal overrides.
       - Phase Closeout Touchpoint: Mandatory `landing-worthy?` question for `/gsd:complete-milestone` and phase summaries.
    2. In `.planning/seeds/SEED-241-living-landing-page.md`:
       - Update frontmatter: change `status: active` to `status: folded`.
    3. Create `.github/workflows/landing-drift.yml` (F-5):
       - Trigger: `push` and `pull_request` on `branches: [develop, main]`.
       - Paths:
         - `frontend/src/landing/**`
         - `scripts/check-landing-drift.cjs`
         - `backend/app/config.py`
         - `backend/app/services/tool_dispatcher.py`
         - `frontend/src/components/ingestion/acceptedFormats.ts`
         - `frontend/src/components/workflows/PublishGauntlet.tsx`
         - `frontend/src/components/settings/servicesCatalog.ts`
       - Steps: Checkout repository, setup Node, run `node scripts/check-landing-drift.cjs`.
  </action>
  <verify>
    Verify markdown and yaml syntax.
  </verify>
  <acceptance_criteria>
    - `docs/LANDING.md` exists and covers all operational topics.
    - `SEED-241` status is `folded`.
    - `.github/workflows/landing-drift.yml` exists with proper triggers.
  </acceptance_criteria>
  <done>Landing documentation, seed update, and CI workflow authored.</done>
</task>

<task type="auto">
  <name>Task 2: Red-Drift Guard Demonstration and Deploy Drift Verification</name>
  <files>scripts/check-landing-drift.cjs, scripts/check-deploy-drift.sh</files>
  <read_first>scripts/check-landing-drift.cjs, scripts/check-deploy-drift.sh</read_first>
  <action>
    1. Run `node scripts/check-landing-drift.cjs` and verify clean exit 0.
    2. Demonstrate RED failure:
       - Temporarily modify a tracked claim in `frontend/src/landing/facts.ts` (e.g. change a toolCount or provider name).
       - Run `node scripts/check-landing-drift.cjs` and verify exit code 1 with explicit diagnostic diff.
       - Restore `facts.ts` to exact clean state and verify exit code 0.
    3. Run `bash scripts/check-deploy-drift.sh` to confirm zero deployment drift.
  </action>
  <verify>
    Run drift check commands and verify exit codes (1 on simulated drift, 0 on clean tree).
  </verify>
  <acceptance_criteria>
    - Drift guard is verified to catch mismatches (proven RED before GREEN).
    - `check-deploy-drift.sh` exits 0.
  </acceptance_criteria>
  <done>Drift guard proven functional and deploy drift checks passing.</done>
</task>

<task type="auto">
  <name>Task 3: Production Bundle Verification, Chunk Grep, and Reviewer UAT Pack</name>
  <files>docs/LANDING.md</files>
  <read_first>frontend/vite.config.ts</read_first>
  <action>
    1. Run production build: `npm --prefix frontend run build`.
    2. Measure gzipped size of landing bundle (`dist/index.html` + `dist/assets/landing-*.js` + `dist/assets/landing-*.css` or shared CSS).
       - Confirm JS gzipped size is strictly < 400 KB.
       - Report both JS and CSS sizes.
    3. Run built chunk grep (SC#1, F-3):
       - Search landing output chunk files in `dist/assets/` for `supabase`, `/lib/api`, `StreamsProvider`, and `OrgProvider`.
       - Assert 0 matches found.
    4. Run full test suite for landing:
       - `npx vitest run src/landing` with `GSD_VITEST_MAX_WORKERS=2`.
    5. Document UAT verification checklist in summary for Claude reviewer:
       - Chrome visit to `/`
       - Visual check of 3D tilt hero and 3D models orbit
       - Inspection of all 8 tour scene tabs and continuous loop playback
       - Viewport resize to 390px width checking no horizontal page overflow
       - CTA links validation
    6. Record OWED pins for `scripts/vitest-count-gate.cjs` without modifying the script (F-6).
  </action>
  <verify>
    Confirm build output exists, chunk size is < 400 KB gz, grep has 0 matches, and vitest runs green.
  </verify>
  <acceptance_criteria>
    - Landing production JS bundle is < 400 KB gzipped.
    - Built landing chunk greps clean of auth/api modules.
    - All vitest tests in `src/landing` pass.
    - `scripts/vitest-count-gate.cjs` is untouched.
  </acceptance_criteria>
  <done>Production bundle verified and UAT preparation complete.</done>
</task>

</tasks>
