---
phase: 222-one-click-connects-any-mcp-server
plan: 02
type: execute
wave: 2
depends_on: ["222-01"]
files_modified:
  - frontend/src/components/settings/servicesCatalog.ts
  - frontend/src/components/settings/connectionFormCopy.ts
  - frontend/src/components/settings/__tests__/servicesCatalog.test.ts
  - frontend/src/components/settings/__tests__/connectionFormCopy.mcp.test.ts
autonomous: true
requirements: [D-222-03, D-222-04, D-222-05, D-222-06, D-222-07]

must_haves:
  truths:
    - "D-222-07: Notion entry in servicesCatalog.ts declares shape: 'mcp' and points to 'mcp.notion.com/mcp' as default host / endpoint."
    - "D-222-03: Copy constant for kind: 'open' clearly states no credentials are required."
    - "D-222-04: Copy constants for kind: 'oauth' name the authorization host and provide distinct guidance for DCR vs BYO credentials."
    - "D-222-05: Copy helper for kind: 'token' returns backend detail string verbatim when present."
    - "D-222-06: Distinct copy functions keep security policy refusals (422 reason_code) separate from unreachable server errors (kind: 'unreachable')."
  artifacts:
    - path: "frontend/src/components/settings/servicesCatalog.ts"
      provides: "Catalog entry for Notion with MCP endpoint configuration"
      contains: "mcp.notion.com"
    - path: "frontend/src/components/settings/connectionFormCopy.ts"
      provides: "Copy tokens and presentation helpers for the 5 MCP door states"
      contains: "MCP_AUTH_DOOR"
    - path: "frontend/src/components/settings/__tests__/connectionFormCopy.mcp.test.ts"
      provides: "Unit tests asserting copy derivation honesty and state distinctions"
      contains: "describe(\"connectionFormCopy"
  key_links:
    - from: "frontend/src/components/settings/connectionFormCopy.ts"
      to: "frontend/src/lib/api/connectors.ts"
      via: "McpAuthKind and McpProbeAuthResponse types"
      pattern: "McpAuthKind"
---

<objective>
Update the presentation catalog for Notion and add complete copy tokens and derivation helpers in `connectionFormCopy.ts` for all MCP auth states.

Output: Updated `servicesCatalog.ts`, new MCP auth copy constants and functions in `connectionFormCopy.ts`, and unit tests.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⚠ **Cap every vitest run: `GSD_VITEST_MAX_WORKERS=2`.**
</execution_context>

<context>
Copy rules (`222-CONTEXT.md` & `BUS-047` / `BUS-052`):
- `kind === 'open'`: State plainly that no credentials are required.
- `kind === 'oauth'`: Name `authorization_host` before the user commits. DCR = single click; BYO = client credentials prompt.
- `kind === 'token'`: Show backend's `detail` sentence verbatim.
- Refusals vs Unreachable: Refusals (422) map closed reason codes (`address_not_public`, `scheme_not_tls`, `host_not_allowed`, `host_not_ascii`, `unresolvable`, `redirected`) to policy explanation; `unreachable` states server did not answer. Never merge them.
</context>

<tasks>
<task>
<action>
1. In `frontend/src/components/settings/servicesCatalog.ts`:
   - Update the `notion` catalog entry:
     - Set `shape: "mcp"`
     - Set `defaultHost: "mcp.notion.com/mcp"`
     - Ensure `tagline` reflects MCP connection capability.
2. In `frontend/src/components/settings/connectionFormCopy.ts`:
   - Add copy constants:
     - `MCP_PROBING_STATUS = "Checking server authentication..."`
     - `MCP_AUTH_OPEN_DESC = "No credentials required. This server allows direct connection."`
     - `MCP_AUTH_OAUTH_DCR_DESC = (host: string) => \`Authentication required. Sign in with \${host} to connect.\``
     - `MCP_AUTH_OAUTH_BYO_DESC = (host: string) => \`Authentication required. Supply your client credentials to sign in with \${host}.\``
     - `MCP_AUTH_TOKEN_DESC = (detail?: string | null) => detail?.trim() || "This server requires an API token or personal access token."`
     - `MCP_REFUSAL_POLICY_HEADING = "Connection refused by security policy"`
     - `MCP_UNREACHABLE_HEADING = "Server did not respond"`
   - Implement helper functions for formatting status badges and button labels:
     - `mcpAuthActionLabel(kind: McpAuthKind, registrationRequired?: boolean, host?: string | null): string`
     - `mcpPolicyRefusalMessage(reasonCode: string | null, fallbackMessage?: string | null): string`
3. In `frontend/src/components/settings/__tests__/connectionFormCopy.mcp.test.ts`:
   - Assert all copy tokens for the 4 kinds and refusal codes.
   - Verify that policy refusals and unreachable error headings do not flatten.
</action>
<verify>
Run: `npx vitest run frontend/src/components/settings/__tests__/connectionFormCopy.mcp.test.ts`
Run: `npx vitest run frontend/src/components/settings/__tests__/servicesCatalog.test.ts`
</verify>
<done>
All copy derivations and catalog tests pass with 100% honesty.
</done>
</task>
</tasks>

<verification>
- Notion catalog entry resolves to `mcp` shape and `mcp.notion.com/mcp`.
- Copy constants strictly preserve distinction between policy refusal and unreachable server.
</verification>
