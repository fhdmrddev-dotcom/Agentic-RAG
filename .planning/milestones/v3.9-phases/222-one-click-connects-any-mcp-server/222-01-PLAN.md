---
phase: 222-one-click-connects-any-mcp-server
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/api/connectors.ts
  - frontend/src/lib/api.ts
  - frontend/src/lib/api/__tests__/connectors.mcp_auth.test.ts
autonomous: true
requirements: [D-222-01, D-222-04, D-222-06]

must_haves:
  truths:
    - "D-222-01: McpAuthKind is strictly 'open' | 'oauth' | 'token' | 'unreachable'. The API client branches on this union alone."
    - "probeMcpAuth calls POST /api/connectors/mcp/probe-auth with { server_url: string } and returns McpProbeAuthResponse."
    - "A 422 egress refusal returns a structured ConnectorApiError carrying reasonCode from detail.reason_code."
    - "A 502 response too large returns ConnectorApiError with status 502 and detail message."
    - "createMcpOAuthAuthorizeUrl calls POST /api/connectors/mcp/oauth/authorize with { connection_id } and returns McpOAuthAuthorizeResponse."
    - "D-207-06 barrel completeness is respected: probeMcpAuth and createMcpOAuthAuthorizeUrl are re-exported in frontend/src/lib/api.ts."
  artifacts:
    - path: "frontend/src/lib/api/connectors.ts"
      provides: "probeMcpAuth and createMcpOAuthAuthorizeUrl API client functions and wire types"
      contains: "probeMcpAuth"
    - path: "frontend/src/lib/api.ts"
      provides: "barrel re-export for MCP auth functions"
      contains: "probeMcpAuth"
    - path: "frontend/src/lib/api/__tests__/connectors.mcp_auth.test.ts"
      provides: "unit tests for probeMcpAuth and createMcpOAuthAuthorizeUrl"
      contains: "describe(\"probeMcpAuth"
  key_links:
    - from: "frontend/src/lib/api/connectors.ts"
      to: "backend/app/api/connectors.py"
      via: "POST /connectors/mcp/probe-auth and POST /connectors/mcp/oauth/authorize endpoints"
      pattern: "/connectors/mcp/"
    - from: "frontend/src/lib/api.ts"
      to: "frontend/src/lib/api/connectors.ts"
      via: "re-export in api barrel"
      pattern: "probeMcpAuth"
---

<objective>
Implement the frontend API client functions, error parsers, and TypeScript types for Phase 222's MCP auth discovery and OAuth authorize wire contracts.

Output: `probeMcpAuth`, `createMcpOAuthAuthorizeUrl`, wire types (`McpAuthKind`, `McpProbeAuthResponse`, `McpOAuthAuthorizeResponse`), `lib/api.ts` barrel re-exports, and dedicated unit tests.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⚠ **Cap every vitest run: `GSD_VITEST_MAX_WORKERS=2`.**
</execution_context>

<context>
The backend wire contract is live and committed (`7b009d685` / `BUS-047` / `BUS-052`):
- `POST /connectors/mcp/probe-auth` takes `{ server_url: string }` and returns `{ kind, authorization_host, registration_required, code_challenge_methods, detail, resource_status }`.
  - 422 Unprocessable Entity returns `{ detail: { reason_code: string, message: string } }` on security policy refusals.
  - 502 Bad Gateway returns `{ detail: string }` when metadata exceeds size limits.
- `POST /connectors/mcp/oauth/authorize` takes `{ connection_id: string }` only (`extra='forbid'`) and returns `{ authorize_url: string, authorization_host: string }`.
</context>

<tasks>
<task>
<action>
1. In `frontend/src/lib/api/connectors.ts`:
   - Export type `McpAuthKind = "open" | "oauth" | "token" | "unreachable"`
   - Export interface `McpProbeAuthResponse`:
     ```ts
     export interface McpProbeAuthResponse {
       kind: McpAuthKind
       authorization_host?: string | null
       registration_required?: boolean
       code_challenge_methods?: string[]
       detail?: string | null
       resource_status?: number | null
     }
     ```
   - Export interface `McpOAuthAuthorizeResponse`:
     ```ts
     export interface McpOAuthAuthorizeResponse {
       authorize_url: string
       authorization_host: string
     }
     ```
   - Implement `export async function probeMcpAuth(serverUrl: string): Promise<McpProbeAuthResponse>`
     - Calls `POST ${API_BASE}/connectors/mcp/probe-auth` with `{ server_url: serverUrl }` and auth headers.
     - On non-200, parses failure via `readConnectorFailure(res)` or `readConnectorReasonCode(res)` and throws `ConnectorApiError`.
   - Implement `export async function createMcpOAuthAuthorizeUrl(connectionId: string): Promise<McpOAuthAuthorizeResponse>`
     - Calls `POST ${API_BASE}/connectors/mcp/oauth/authorize` with `{ connection_id: connectionId }` and auth headers.
     - On non-200, throws `ConnectorApiError`.
2. In `frontend/src/lib/api.ts`:
   - Re-export `probeMcpAuth`, `createMcpOAuthAuthorizeUrl`, `McpAuthKind`, `McpProbeAuthResponse`, and `McpOAuthAuthorizeResponse` under the connectors section.
3. In `frontend/src/lib/api/__tests__/connectors.mcp_auth.test.ts`:
   - Write comprehensive unit tests for `probeMcpAuth` (testing 200 for open/oauth/token/unreachable, 422 with structured reason_code, 502 for oversize response).
   - Write unit tests for `createMcpOAuthAuthorizeUrl`.
</action>
<verify>
Run: `npx vitest run frontend/src/lib/api/__tests__/connectors.mcp_auth.test.ts`
Run: `npx tsc --noEmit -p frontend/tsconfig.app.json`
</verify>
<done>
All unit tests in `connectors.mcp_auth.test.ts` pass, `lib/api.ts` cleanly re-exports the new symbols, and `tsc` error count is at baseline.
</done>
</task>
</tasks>

<verification>
- `probeMcpAuth` handles all four `kind` branches.
- 422 egress refusals extract `reasonCode`.
- Barrel re-exports verified with no type errors.
</verification>
