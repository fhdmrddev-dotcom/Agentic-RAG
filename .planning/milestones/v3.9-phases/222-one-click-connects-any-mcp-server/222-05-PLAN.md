---
phase: 222-one-click-connects-any-mcp-server
plan: 05
type: execute
wave: 4
depends_on: ["222-04"]
files_modified:
  - backend/tests/integration/test_222_mcp_oauth_seam.py
  - scripts/vitest-count-gate.cjs
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md
autonomous: true
requirements: [D-222-09]

must_haves:
  truths:
    - "AGENTS.md §3.1: An integration test that mocks NEITHER side verifies the wire between door client and backend routes."
    - "The integration test tests probe-auth -> authorize -> callback redirect shape with zero mocking of either side."
    - "scripts/vitest-count-gate.cjs adopts the new frontend test suites and verifies count gate OK with 0 failures."
    - "npx tsc -p tsconfig.app.json reports baseline 66 errors (no new errors introduced)."
    - "Hot-file ledger in docs/HOT-FILE-LEDGER.md and CLAUDE.md are updated with re-derived commit counts and phase lines."
  artifacts:
    - path: "backend/tests/integration/test_222_mcp_oauth_seam.py"
      provides: "Unmocked end-to-end seam integration test"
      contains: "test_mcp_oauth_seam_full_flow"
    - path: "scripts/vitest-count-gate.cjs"
      provides: "Updated count gate pins for new suites"
      contains: "McpAuthDoor.test.tsx"
  key_links:
    - from: "frontend/src/lib/api/connectors.ts"
      to: "backend/app/api/connectors.py"
      via: "test_222_mcp_oauth_seam.py executing actual client against backend router"
      pattern: "probe-auth"
---

<objective>
Deliver the mandatory unmocked cross-seam integration test (AGENTS.md §3.1), pin new frontend suites in the count gate, clear all mechanical gates, and update the hot-file ledger.

Output: `test_222_mcp_oauth_seam.py`, updated `scripts/vitest-count-gate.cjs`, updated `docs/HOT-FILE-LEDGER.md` and `CLAUDE.md`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⚠ **Cap every vitest run: `GSD_VITEST_MAX_WORKERS=2`.**
</execution_context>

<context>
AGENTS.md §3.1 rule for split phases:
"A split phase owes an integration test that mocks NEITHER side, and that test is a blocking gate, not a nice-to-have."
Phase 204 shipped a silent disarmed spend cap because both waves mocked the other side. This plan ensures the join between Door frontend contract and Crypto backend routes is tested end-to-end.
</context>

<tasks>
<task>
<action>
1. Create `backend/tests/integration/test_222_mcp_oauth_seam.py`:
   - Test `POST /connectors/mcp/probe-auth` against actual handlers without mocking the route boundary.
   - Test `POST /connectors/mcp/oauth/authorize` ensuring returned authorization URL and parameters match the door's expectation.
   - Test `GET /connectors/mcp/oauth/callback` redirect response handling.
2. In `scripts/vitest-count-gate.cjs`:
   - Adopt newly created test suites (`connectors.mcp_auth.test.ts`, `connectionFormCopy.mcp.test.ts`, `McpAuthDoor.test.tsx`).
   - Pin their passing counts.
3. Run the complete mechanical gate check:
   - Run `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` (must be OK, 0 failures).
   - Run `npx tsc --noEmit -p frontend/tsconfig.app.json` (must be at or below baseline 66).
4. Update `docs/HOT-FILE-LEDGER.md` and `CLAUDE.md`:
   - Synchronize lines, commit counts, and phase counts for touched files (`ConnectionFormPanel.tsx`, `ConnectionsTab.tsx`, `connectors.ts`, `servicesCatalog.ts`, `connectionFormCopy.ts`).
</action>
<verify>
Run: `pytest backend/tests/integration/test_222_mcp_oauth_seam.py`
Run: `node scripts/vitest-count-gate.cjs`
Run: `npx tsc --noEmit -p frontend/tsconfig.app.json`
</verify>
<done>
Unmocked integration test passes, count gate is green with 0 failing, tsc is at baseline 66, and ledger rows are current.
</done>
</task>
</tasks>

<verification>
- Cross-seam integration test passes with neither side mocked.
- Count gate is green with all new suites pinned.
- Baseline tsc count preserved.
</verification>
