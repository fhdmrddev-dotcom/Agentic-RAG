---
phase: 227-the-run-frame-has-one-owner
plan: 02
type: execute
wave: 2
depends_on:
  - 227-01
files_modified:
  - frontend/src/components/chat/ToolCallDetails.tsx
  - frontend/src/components/chat/StepRow.tsx
  - frontend/src/components/chat/toolStepDerivation.ts
  - frontend/src/components/chat/ToolCallPanel.tsx
autonomous: true
requirements:
  - SC#3
  - SC#4
user_setup: []

must_haves:
  truths:
    - "ToolCallDetails.tsx encapsulates tool payload inspection: arguments block, result block, diff view, JSON tree, and subagent state (~180 lines)"
    - "StepRow.tsx encapsulates step timeline rail: connecting line, status node, step number, and ToolEssenceLine (~155 lines)"
    - "toolStepDerivation.ts encapsulates pure, side-effect-free step computation: displayItems, toolStepNumber, stepKeyOf, nodeStateOf, lastPreparingIndex, and dedupToolCalls (~150 lines)"
    - "ToolCallPanel.tsx is refactored from 1019 lines to a lean orchestrator of ~350-400 lines (B-3: measured outcome, shedding ~650 lines across 3 modules)"
    - "Right-aligning the result column (SC#3) is localized entirely within StepRow.tsx as a clean 1-file edit without being made in this phase"
    - "All existing data-testids (step-rail-line, step-node, step-snum, tool-result-summary, tool-args-block, tool-result-block) and DOM structures are byte-identical"
    - "All Vitest suites covering ToolCallPanel, StepRow, and RunCard characterization remain 100% green under vitest-count-gate.cjs"
  artifacts:
    - frontend/src/components/chat/ToolCallDetails.tsx
    - frontend/src/components/chat/StepRow.tsx
    - frontend/src/components/chat/toolStepDerivation.ts
    - frontend/src/components/chat/ToolCallPanel.tsx
  key_links:
    - "ToolCallPanel.tsx imports and renders StepRow and ToolCallDetails"
    - "ToolCallPanel.tsx imports pure derivation helpers from toolStepDerivation.ts"
    - "ToolCallPanel.test.tsx and RunCard.characterization.test.tsx pass cleanly against the refactored component"
---

<objective>
Decompose `ToolCallPanel.tsx` (1019 lines, "extraction due") into focused, single-responsibility submodules (SC#4, B-3):
1. Extract `ToolCallDetails.tsx`: Payload inspection, `ToolArgsBlock`, `ToolResultBlock` (renders diffs, JSON previews, image previews, copy button, markdown), and `SubAgentBlock`.
2. Extract `StepRow.tsx`: Timeline rail spine (`step-rail-line`), status node (`step-node`), step number (`step-snum`), and `ToolEssenceLine`. This satisfies SC#3 prep by making right-aligned result columns a 1-file edit in `StepRow.tsx`.
3. Extract `toolStepDerivation.ts`: Pure, side-effect-free step state derivations (`displayItems`, `toolStepNumber`, `stepKeyOf`, `nodeStateOf`, `lastPreparingIndex`, and `dedupToolCalls`).
4. Refactor `ToolCallPanel.tsx`: Maintain skill rows, collapse state, and container layout, reducing file size from 1019 lines to ~350-400 lines (a ~60% reduction).

Output: `ToolCallDetails.tsx` (new), `StepRow.tsx` (new), `toolStepDerivation.ts` (new), and slim `ToolCallPanel.tsx`.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/227-the-run-frame-has-one-owner/227-CONTEXT.md
@.planning/phases/227-the-run-frame-has-one-owner/227-PREFLIGHT.md
@frontend/src/components/chat/ToolCallPanel.tsx
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Extract ToolCallDetails.tsx from ToolCallPanel.tsx</name>
  <files>frontend/src/components/chat/ToolCallDetails.tsx</files>
  <read_first>frontend/src/components/chat/ToolCallPanel.tsx</read_first>
  <action>
    1. Create `frontend/src/components/chat/ToolCallDetails.tsx`.
    2. Move the detail inspection blocks from `ToolCallPanel.tsx`:
       - `ToolArgsBlock({ tc }: { tc: ToolCall })`
       - `ToolResultBlock({ tc, defaultOpen }: { tc: ToolCall; defaultOpen?: boolean })`
       - `SubAgentBlock({ agent }: { agent: SubAgentState })`
       - Helper functions: `formatDuration`, payload formatters, diff viewer, copy affordance, and JSON display.
    3. Export `ToolArgsBlock`, `ToolResultBlock`, and `SubAgentBlock`.
    4. Ensure all testids (`data-testid="tool-args-block"`, `data-testid="tool-result-block"`, etc.) and CSS classes match byte-identically.
  </action>
  <verify>
    Verify TypeScript compilation in `frontend/`: `npx tsc -p tsconfig.app.json --noEmit`.
  </verify>
  <acceptance_criteria>
    - `ToolCallDetails.tsx` exists and exports detail inspection components.
    - Zero typescript compilation errors.
    - DOM structure and testids preserved.
  </acceptance_criteria>
  <done>Extracted ToolCallDetails.tsx containing arguments, result, diff, and subagent views.</done>
</task>

<task type="auto">
  <name>Task 2: Extract StepRow.tsx from ToolCallPanel.tsx</name>
  <files>frontend/src/components/chat/StepRow.tsx</files>
  <read_first>
    frontend/src/components/chat/ToolCallPanel.tsx
    frontend/src/components/chat/StepIdentity.tsx
  </read_first>
  <action>
    1. Create `frontend/src/components/chat/StepRow.tsx`.
    2. Move `StepRow` and `ToolEssenceLine` from `ToolCallPanel.tsx`:
       - `StepRow({ snum, node, isLast, children })`: Rail column (`data-testid="step-rail-line"`), status node (`data-testid="step-node"`), step number (`data-testid="step-snum"`), and step main column.
       - `ToolEssenceLine({ tc, snum, node, isLast, isRunning, isInterrupted, isExpanded, onToggle, stepIdentity })`: Renders the single-row essence line button (`data-testid="tool-result-summary"`), tool icon, label, and `StatusPill`.
       - Helper `pillStatus`.
    3. SC#3 preparation: By localizing `ToolEssenceLine` inside `StepRow.tsx`, future right-alignment of the result column is a 1-file edit inside `StepRow.tsx` (the flex/grid layout of `ToolEssenceLine` and its pill). Leave the current layout unchanged in this refactor.
    4. Export `StepRow` and `ToolEssenceLine`.
  </action>
  <verify>
    Verify TypeScript compilation in `frontend/`: `npx tsc -p tsconfig.app.json --noEmit`.
  </verify>
  <acceptance_criteria>
    - `StepRow.tsx` exists and exports `StepRow` and `ToolEssenceLine`.
    - All data-testids (`step-rail-line`, `step-node`, `step-snum`, `tool-result-summary`) are preserved.
    - SC#3 right-aligned column is prepared as a 1-file edit in `StepRow.tsx`.
  </acceptance_criteria>
  <done>Extracted StepRow.tsx containing timeline rail and essence line.</done>
</task>

<task type="auto">
  <name>Task 3: Extract toolStepDerivation.ts from ToolCallPanel.tsx (B-3)</name>
  <files>frontend/src/components/chat/toolStepDerivation.ts</files>
  <read_first>frontend/src/components/chat/ToolCallPanel.tsx</read_first>
  <action>
    1. Create `frontend/src/components/chat/toolStepDerivation.ts`.
    2. Move pure step state computation helpers out of `ToolCallPanel.tsx`:
       - `dedupToolCalls(toolCalls: ToolCall[]): ToolCall[]`
       - `displayItems(toolCalls: ToolCall[], subagents: SubAgentState[])`
       - `toolStepNumber(index: number, total: number): number`
       - `stepKeyOf(item: DisplayItem): string`
       - `nodeStateOf(item: DisplayItem, isRunning: boolean): StepNodeState`
       - `lastPreparingIndex(items: DisplayItem[]): number`
    3. Export these pure functions for direct unit testability and use in `ToolCallPanel.tsx`.
  </action>
  <verify>
    Verify TypeScript compilation in `frontend/`: `npx tsc -p tsconfig.app.json --noEmit`.
  </verify>
  <acceptance_criteria>
    - `toolStepDerivation.ts` exists and exports pure step state derivations.
    - Zero TypeScript compilation errors.
  </acceptance_criteria>
  <done>Extracted toolStepDerivation.ts for side-effect-free step computation.</done>
</task>

<task type="auto">
  <name>Task 4: Refactor ToolCallPanel.tsx to orchestrate submodules (B-3)</name>
  <files>frontend/src/components/chat/ToolCallPanel.tsx</files>
  <read_first>
    frontend/src/components/chat/ToolCallPanel.tsx
    frontend/src/components/chat/StepRow.tsx
    frontend/src/components/chat/ToolCallDetails.tsx
    frontend/src/components/chat/toolStepDerivation.ts
  </read_first>
  <action>
    1. Update `frontend/src/components/chat/ToolCallPanel.tsx`:
       - Import `StepRow` and `ToolEssenceLine` from `./StepRow`.
       - Import `ToolArgsBlock`, `ToolResultBlock`, and `SubAgentBlock` from `./ToolCallDetails`.
       - Import derivation helpers from `./toolStepDerivation`.
       - Keep `SkillRow`, panel expand/collapse state, and container layout in `ToolCallPanel.tsx`.
       - Remove the extracted helper functions and component definitions.
       - Verify file size drops from 1019 lines to ~350-400 lines (a ~60% reduction).
    2. Run tests to confirm zero regressions:
       - `npx vitest run src/__tests__/components/ToolCallPanel.test.tsx`
       - `npx vitest run src/components/chat/__tests__/RunCard.characterization.test.tsx`
       - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs`
  </action>
  <verify>
    Run `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from repo root and `npx tsc -p tsconfig.app.json --noEmit` in `frontend/`.
  </verify>
  <acceptance_criteria>
    - `ToolCallPanel.tsx` is reduced to ~350-400 lines.
    - `ToolCallPanel.test.tsx` and `RunCard.characterization.test.tsx` pass with 0 failures.
    - All pinned vitest count gate suites pass.
  </acceptance_criteria>
  <done>ToolCallPanel.tsx decomposed into modular subcomponents; file size reduced by ~60%.</done>
</task>

</tasks>
