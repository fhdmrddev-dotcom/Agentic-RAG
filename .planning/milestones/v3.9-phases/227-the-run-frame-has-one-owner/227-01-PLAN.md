---
phase: 227-the-run-frame-has-one-owner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/__tests__/components/ToolCallPanel.test.tsx
  - frontend/src/__tests__/components/MessageItem.test.tsx
  - frontend/src/components/chat/__tests__/MessageItem.cancelledRun.test.tsx
  - frontend/src/components/chat/__tests__/RunCard.characterization.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements:
  - SC#2
  - SC#5
user_setup: []

must_haves:
  truths:
    - "MessageItem.test.tsx bare-name collision is resolved via git mv to MessageItem.cancelledRun.test.tsx (B-1)"
    - "All 15 covering test suites for the chat run frame plus the new RunCard.characterization.test.tsx (16 suites total) are in TARGETS and BASELINE of scripts/vitest-count-gate.cjs"
    - "Pre-existing test assertions in ToolCallPanel.test.tsx and MessageItem.test.tsx are aligned with shipped project contracts (A-2: positive control on failed/interrupted pill; literal 'Setting up agent…' match)"
    - "RunCard.characterization.test.tsx captures the exact visual/DOM contract for all 8 run states prior to any refactoring (A-1 / SC#2 mechanical proof)"
    - "scripts/vitest-count-gate.cjs runs green with 0 failing tests and no unpinned test decrease before any refactoring code is touched"
    - "The count gate verdict line is re-derived from actual run output and committed as baseline infrastructure"
  artifacts:
    - scripts/vitest-count-gate.cjs
    - frontend/src/__tests__/components/ToolCallPanel.test.tsx
    - frontend/src/__tests__/components/MessageItem.test.tsx
    - frontend/src/components/chat/__tests__/MessageItem.cancelledRun.test.tsx
    - frontend/src/components/chat/__tests__/RunCard.characterization.test.tsx
  key_links:
    - "vitest-count-gate.cjs TARGETS array includes all covering suite paths with distinct basenames"
    - "vitest-count-gate.cjs BASELINE map includes each bare filename with its measured test count"
    - "RunCard.characterization.test.tsx serves as the pre-refactor visual snapshot baseline for Wave 3"
---

<objective>
Pin all covering suites in `scripts/vitest-count-gate.cjs` BEFORE any refactoring begins (SC#5) and establish the Wave 1 characterization baseline (SC#2):
1. Resolve the `MessageItem.test.tsx` bare-name collision (B-1) by `git mv`ing `frontend/src/components/chat/__tests__/MessageItem.test.tsx` to `MessageItem.cancelledRun.test.tsx`.
2. Align two pre-existing failing assertions in previously-unpinned suites with shipped project contracts (A-2):
   - `frontend/src/__tests__/components/ToolCallPanel.test.tsx`: Align with the 2026-08-31 operator noise audit (`ToolCallPanel.tsx:420-430`) by asserting that `status-pill` is absent on `done` steps and present on `failed`/`interrupted` steps (positive control).
   - `frontend/src/__tests__/components/MessageItem.test.tsx`: Align the empty-content streaming indicator test with Phase 174/194's literal `"Setting up agent…"` contract (`toolMeta.ts:197`).
3. Author `RunCard.characterization.test.tsx` (A-1) to mechanically lock in DOM rendering for all 8 run states.
4. Add all 16 suites to `TARGETS` and `BASELINE` in `scripts/vitest-count-gate.cjs`, run the gate, and verify 0 failures.

Output: Resolved test filenames, updated assertions, new characterization suite, and updated `scripts/vitest-count-gate.cjs`.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/227-the-run-frame-has-one-owner/227-CONTEXT.md
@.planning/phases/227-the-run-frame-has-one-owner/227-MEASUREMENT-PACK.md
@.planning/phases/227-the-run-frame-has-one-owner/227-PREFLIGHT.md
@scripts/vitest-count-gate.cjs
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve bare-name collision and align pre-existing assertions (B-1, A-2)</name>
  <files>
    frontend/src/__tests__/components/ToolCallPanel.test.tsx
    frontend/src/__tests__/components/MessageItem.test.tsx
    frontend/src/components/chat/__tests__/MessageItem.cancelledRun.test.tsx
  </files>
  <read_first>
    frontend/src/__tests__/components/ToolCallPanel.test.tsx
    frontend/src/components/chat/ToolCallPanel.tsx
    frontend/src/__tests__/components/MessageItem.test.tsx
    frontend/src/components/chat/MessageItem.tsx
    frontend/src/components/chat/__tests__/MessageItem.test.tsx
  </read_first>
  <action>
    1. Resolve bare-name collision (B-1):
       - Execute `git mv frontend/src/components/chat/__tests__/MessageItem.test.tsx frontend/src/components/chat/__tests__/MessageItem.cancelledRun.test.tsx`.
       - This cleanly separates the Phase 147-06 cancelled-run persistence suite from the general `src/__tests__/components/MessageItem.test.tsx` suite.
    2. In `frontend/src/__tests__/components/ToolCallPanel.test.tsx` (A-2):
       - The 2026-08-31 operator noise audit (`ToolCallPanel.tsx:420-430`) removed `status-pill` for plain `done` steps because the run header already carries `✓ done`.
       - Update the assertion so `done` essence cards assert that `status-pill` is NOT present (`expect(row.querySelector("[data-testid='status-pill']")).toBeFalsy()`).
       - Add a POSITIVE CONTROL: Add a step with `status: "failed"` or `status: "interrupted"` and assert that its `[data-testid='status-pill']` IS present in the DOM.
    3. In `frontend/src/__tests__/components/MessageItem.test.tsx` (A-2):
       - Line 106 currently asserts `/thinking/i`.
       - Update the test assertion to expect the exact literal `"Setting up agent…"` from `toolMeta.ts:197` (Phase 174/194 contract).
    4. Run `npx vitest run src/__tests__/components/ToolCallPanel.test.tsx src/__tests__/components/MessageItem.test.tsx src/components/chat/__tests__/MessageItem.cancelledRun.test.tsx` to verify all pass with 0 failures.
  </action>
  <verify>
    Run `npx vitest run src/__tests__/components/ToolCallPanel.test.tsx src/__tests__/components/MessageItem.test.tsx src/components/chat/__tests__/MessageItem.cancelledRun.test.tsx` in `frontend/`. All suites must pass 100%.
  </verify>
  <acceptance_criteria>
    - `MessageItem.cancelledRun.test.tsx` is renamed and distinct in the global namespace.
    - `ToolCallPanel.test.tsx` passes with 0 failures and includes a positive control on non-done status pills.
    - `MessageItem.test.tsx` passes with 0 failures asserting the literal setup copy.
  </acceptance_criteria>
  <done>Bare-name collision resolved and pre-existing assertions aligned.</done>
</task>

<task type="auto">
  <name>Task 2: Author RunCard characterization suite for 8 run states (A-1 / SC#2)</name>
  <files>frontend/src/components/chat/__tests__/RunCard.characterization.test.tsx</files>
  <read_first>
    frontend/src/components/chat/RunCard.tsx
    frontend/src/components/chat/ToolCallPanel.tsx
    frontend/src/components/chat/MessageItem.tsx
  </read_first>
  <action>
    1. Create `frontend/src/components/chat/__tests__/RunCard.characterization.test.tsx`.
    2. Author tests capturing the exact rendered DOM structure across all 8 canonical run states:
       - State 1: `streaming` (active tool execution, running spinner, active rail node).
       - State 2: `settled` / `done` (completed tools, duration, collapsed/expandable rail).
       - State 3: `failed` (error message, failed node indicator, error detail preview).
       - State 4: `timed_out` (terminal status "Agent reached time limit").
       - State 5: `cancelled` (terminal status "Response stopped" / cancel affordance).
       - State 6: `paused_on_approval` / ask state (approval card, pending decision).
       - State 7: `no_tools` (plain assistant message without run frame).
       - State 8: `sub_agent` (nested sub-agent block and badge).
    3. Assert data-testids (`run-card-header`, `tool-call-panel`, `step-rail-line`, `step-node`, `tool-result-summary`, `tool-args-block`, `tool-result-block`).
    4. Verify this suite passes 100% on the untouched components: `npx vitest run src/components/chat/__tests__/RunCard.characterization.test.tsx`.
  </action>
  <verify>
    Run `npx vitest run src/components/chat/__tests__/RunCard.characterization.test.tsx` in `frontend/`.
  </verify>
  <acceptance_criteria>
    - `RunCard.characterization.test.tsx` exists and covers all 8 run states.
    - Test passes 100% on the pre-refactor tree.
  </acceptance_criteria>
  <done>Characterization suite authored to provide mechanical proof for SC#2.</done>
</task>

<task type="auto">
  <name>Task 3: Adopt all 16 covering suites into TARGETS and BASELINE in scripts/vitest-count-gate.cjs</name>
  <files>scripts/vitest-count-gate.cjs</files>
  <read_first>
    scripts/vitest-count-gate.cjs
    .planning/phases/227-the-run-frame-has-one-owner/227-MEASUREMENT-PACK.md
  </read_first>
  <action>
    1. The 16 covering suites to adopt (15 from pack with collision resolved + 1 characterization suite):
       - `src/components/chat/__tests__/MessageItem.cancelledRun.test.tsx`
       - `src/components/chat/__tests__/MessageItem.blockedNotice.test.tsx`
       - `src/components/chat/__tests__/MessageItem.harnessBanner.test.tsx`
       - `src/components/chat/__tests__/ChatAreaBanner.test.tsx`
       - `src/components/chat/__tests__/ChatAreaMode.test.tsx`
       - `src/components/chat/__tests__/RunCard.characterization.test.tsx`
       - `src/__tests__/components/MessageItem.test.tsx`
       - `src/__tests__/components/MessageItem.clamp.test.tsx`
       - `src/__tests__/components/MessageItem.fallbackNotice.test.tsx`
       - `src/__tests__/components/MessageItem.memo.test.tsx`
       - `src/__tests__/components/MessageItem.sticky.test.tsx`
       - `src/__tests__/components/RunCard.logo.test.tsx`
       - `src/__tests__/components/ToolCallPanel.test.tsx`
       - `src/__tests__/components/chat/MessageList.test.tsx`
       - `src/__tests__/components/chat/MessageList.dedup.test.tsx`
       - `src/__tests__/components/chat/MessageList.runline.baseline.test.tsx`
    2. Add all 16 paths to the `TARGETS` array in `scripts/vitest-count-gate.cjs` under a dedicated Phase 227 block.
    3. Run `npx vitest run <16 suites>` to capture the exact test count for each bare filename from the `actual` output.
    4. Add each of the 16 bare filenames and its measured count to the `BASELINE` object in `scripts/vitest-count-gate.cjs`.
    5. Run `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from repo root to verify that the gate passes with all pinned suites present and 0 failures.
  </action>
  <verify>
    Run `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from repo root. The verdict line must read `count gate OK — N/N pinned files present, no per-file decrease, 0 failing.`
  </verify>
  <acceptance_criteria>
    - All 16 covering suites are listed in `TARGETS`.
    - All 16 bare filenames are pinned in `BASELINE` with counts read from actual execution.
    - Zero bare-name collisions in `BASELINE`.
    - `vitest-count-gate.cjs` successfully executes all adopted suites with 0 failures.
  </acceptance_criteria>
  <done>Wave 1 gate pinning complete: all 16 covering suites are guarded by vitest-count-gate.cjs before refactoring starts.</done>
</task>

</tasks>
