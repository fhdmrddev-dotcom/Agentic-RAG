---
phase: 227-the-run-frame-has-one-owner
plan: 03
type: execute
wave: 3
depends_on:
  - 227-02
files_modified:
  - frontend/src/components/chat/RunCard.tsx
  - frontend/src/components/chat/messageText.ts
  - frontend/src/components/chat/UserMessageBubble.tsx
  - frontend/src/components/chat/MessageItem.tsx
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
  - .planning/phases/227-the-run-frame-has-one-owner/227-VALIDATION.md
autonomous: true
requirements:
  - SC#1
  - SC#2
  - SC#3
  - SC#4
user_setup: []

must_haves:
  truths:
    - "RunCard.tsx owns the run frame: header, rail, steps, terminal status, footer"
    - "RunTerminalStatus is co-located in RunCard.tsx and encapsulates terminal status copy and conditions"
    - "messageText.ts encapsulates pure text transforms (dedupParagraphs, ~107 lines) extracted from MessageItem.tsx (B-2)"
    - "UserMessageBubble.tsx encapsulates user role bubble rendering (~58 lines) extracted from MessageItem.tsx (B-2)"
    - "MessageItem.tsx delegates terminal status to RunTerminalStatus, user bubble to UserMessageBubble, and text dedup to messageText.ts, dropping from 822 lines to ~620 lines and genuinely shedding 3 distinct responsibilities (B-2 / SC#4)"
    - "Moving the terminal status line inside the frame (SC#3) is localized entirely within RunCard.tsx as a clean 1-file edit"
    - "MessageItem.tsx and ToolCallPanel.tsx leave 'extraction due' legitimately in CLAUDE.md and docs/HOT-FILE-LEDGER.md in the same commit (SC#4)"
    - "RunCard.characterization.test.tsx passes 100% green proving byte-identical visual / DOM fidelity across all 8 run states (A-1 / SC#2)"
    - "227-VALIDATION.md is authored detailing the 8 run states with verification evidence"
    - "All gates pass: Vitest count gate OK with 0 failures, tsc 66 (holding baseline), check-claude-md-size.cjs passes, and backend unit tests 71 (holding baseline)"
  artifacts:
    - frontend/src/components/chat/RunCard.tsx
    - frontend/src/components/chat/messageText.ts
    - frontend/src/components/chat/UserMessageBubble.tsx
    - frontend/src/components/chat/MessageItem.tsx
    - CLAUDE.md
    - docs/HOT-FILE-LEDGER.md
    - .planning/phases/227-the-run-frame-has-one-owner/227-VALIDATION.md
  key_links:
    - "MessageItem.tsx mounts RunTerminalStatus exported from RunCard.tsx"
    - "MessageItem.tsx mounts UserMessageBubble and imports dedupParagraphs from messageText.ts"
    - "CLAUDE.md and docs/HOT-FILE-LEDGER.md rows for MessageItem.tsx and ToolCallPanel.tsx are updated to honoured by extraction in the same commit"
---

<objective>
Consolidate Run Frame ownership into `RunCard.tsx`, shed distinct responsibilities from `MessageItem.tsx` (B-2), update hot-file ledger rows (B-4), verify mechanical characterization (A-1), and author `227-VALIDATION.md`:
1. In `RunCard.tsx`: Extract and export `RunTerminalStatus`, encapsulating terminal state checking and copy. Provide the internal structure so that moving the status line inside the frame in a future phase is a 1-file edit in `RunCard.tsx` (SC#1, SC#3).
2. In `MessageItem.tsx`:
   - Extract `messageText.ts`: Pure text transforms (`dedupParagraphs`, ~107 lines).
   - Extract `UserMessageBubble.tsx`: User message role rendering (`UserBubble`, ~58 lines).
   - Delegate terminal status to `<RunTerminalStatus message={message} isStreaming={isStreaming} />`.
   - `MessageItem.tsx` drops from 822 to ~620 lines, shedding 3 distinct jobs and earning its SC#4 discharge (B-2).
3. Verify byte-identical visual output against `RunCard.characterization.test.tsx` (A-1, SC#2).
4. Update `CLAUDE.md` and `docs/HOT-FILE-LEDGER.md` in the same commit: Re-derive triples for `MessageItem.tsx`, `ToolCallPanel.tsx`, and `RunCard.tsx`. Record legitimate extraction discharge validated by `node scripts/check-claude-md-size.cjs` (B-4, SC#4).
5. Author `.planning/phases/227-the-run-frame-has-one-owner/227-VALIDATION.md` with explicit validation details across all 8 run states.
6. Run full gate checks (`vitest-count-gate.cjs`, `tsc`, `pytest`).

Output: Updated `RunCard.tsx`, `messageText.ts`, `UserMessageBubble.tsx`, `MessageItem.tsx`, `CLAUDE.md`, `docs/HOT-FILE-LEDGER.md`, and `227-VALIDATION.md`.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/227-the-run-frame-has-one-owner/227-CONTEXT.md
@.planning/phases/227-the-run-frame-has-one-owner/227-PREFLIGHT.md
@frontend/src/components/chat/RunCard.tsx
@frontend/src/components/chat/MessageItem.tsx
@CLAUDE.md
@docs/HOT-FILE-LEDGER.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Extract RunTerminalStatus in RunCard.tsx with SC#3 1-file edit readiness</name>
  <files>frontend/src/components/chat/RunCard.tsx</files>
  <read_first>
    frontend/src/components/chat/RunCard.tsx
    frontend/src/components/chat/MessageItem.tsx
  </read_first>
  <action>
    1. In `frontend/src/components/chat/RunCard.tsx`:
       - Define and export `RunTerminalStatus({ message, isStreaming, className }: { message: Message; isStreaming: boolean; className?: string })`:
         ```tsx
         export function RunTerminalStatus({
           message,
           isStreaming,
           className,
         }: {
           message: Message
           isStreaming: boolean
           className?: string
         }) {
           if (isStreaming) return null
           const showTerminal =
             message.stopped ||
             message.runStatus === "timed_out" ||
             (message.runStatus === "cancelled" && !!message.content)
           if (!showTerminal) return null

           const copy =
             message.runStatus === "timed_out"
               ? "Agent reached time limit"
               : "Response stopped"

           return (
             <div className={cn("flex items-center gap-1.5 mt-1 text-xs text-muted-foreground", className)}>
               <span className="italic">{copy}</span>
             </div>
           )
         }
         ```
       - SC#3 preparation: Document clearly with a comment in `RunCard.tsx` that moving `RunTerminalStatus` inside the card frame (e.g. at the bottom of the expanded body or below the Next-up footer) is a 1-file edit inside `RunCard.tsx`.
  </action>
  <verify>
    Verify TypeScript compilation in `frontend/`: `npx tsc -p tsconfig.app.json --noEmit`.
  </verify>
  <acceptance_criteria>
    - `RunTerminalStatus` is exported from `RunCard.tsx`.
    - Terminal condition derivation and copy are encapsulated.
    - Zero TypeScript compilation errors.
  </acceptance_criteria>
  <done>Created RunTerminalStatus in RunCard.tsx ready for delegation and 1-file move.</done>
</task>

<task type="auto">
  <name>Task 2: Extract messageText.ts and UserMessageBubble.tsx from MessageItem.tsx (B-2)</name>
  <files>
    frontend/src/components/chat/messageText.ts
    frontend/src/components/chat/UserMessageBubble.tsx
    frontend/src/components/chat/MessageItem.tsx
  </files>
  <read_first>frontend/src/components/chat/MessageItem.tsx</read_first>
  <action>
    1. Create `frontend/src/components/chat/messageText.ts`:
       - Move `dedupParagraphs(text: string): string` (~107 lines of pure paragraph deduplication logic) and related text cleanup helpers from `MessageItem.tsx:198-304`.
       - Export `dedupParagraphs` for direct unit testability.
    2. Create `frontend/src/components/chat/UserMessageBubble.tsx`:
       - Move `UserBubble({ message }: { message: Message })` (~58 lines of user role rendering) from `MessageItem.tsx:93-151`.
       - Export `UserBubble`.
    3. In `frontend/src/components/chat/MessageItem.tsx`:
       - Import `dedupParagraphs` from `./messageText`.
       - Import `UserBubble` from `./UserMessageBubble`.
       - Import `RunTerminalStatus` from `./RunCard`.
       - Replace lines 752-761 with `<RunTerminalStatus message={message} isStreaming={isStreaming} />`.
       - Replace inline `UserBubble` implementation with imported `<UserBubble message={message} />`.
       - `MessageItem.tsx` drops from 822 lines to ~620 lines, shedding text deduplication, user role rendering, and terminal status derivation.
    4. Run all MessageItem and RunCard tests:
       - `npx vitest run src/__tests__/components/MessageItem.test.tsx`
       - `npx vitest run src/components/chat/__tests__/MessageItem.cancelledRun.test.tsx`
       - `npx vitest run src/components/chat/__tests__/RunCard.characterization.test.tsx`
  </action>
  <verify>
    Run `npx vitest run src/__tests__/components/MessageItem.test.tsx src/components/chat/__tests__/MessageItem.cancelledRun.test.tsx src/components/chat/__tests__/RunCard.characterization.test.tsx` in `frontend/`.
  </verify>
  <acceptance_criteria>
    - `messageText.ts` and `UserMessageBubble.tsx` are extracted and cleanly imported.
    - `MessageItem.tsx` delegates terminal status to `RunTerminalStatus`.
    - `RunCard.characterization.test.tsx` passes 100% proving byte-identical visual output (SC#2).
    - All tests pass with 0 failures.
  </acceptance_criteria>
  <done>Extracted messageText.ts and UserMessageBubble.tsx; MessageItem.tsx delegates 3 responsibilities.</done>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md and docs/HOT-FILE-LEDGER.md in the same commit (SC#4, B-4, A-6)</name>
  <files>
    CLAUDE.md
    docs/HOT-FILE-LEDGER.md
  </files>
  <read_first>
    CLAUDE.md
    docs/HOT-FILE-LEDGER.md
  </read_first>
  <action>
    1. Re-derive triples for all touched hot files via git (`commits / phases / lines`, subtracting 6-digit quick-task buckets per A-6):
       - `frontend/src/components/chat/MessageItem.tsx`
       - `frontend/src/components/chat/ToolCallPanel.tsx`
       - `frontend/src/components/chat/RunCard.tsx`
    2. In `CLAUDE.md` lines 535-537:
       - Update `ToolCallPanel.tsx`: triple updated, status updated from `extraction due` to `honoured by extraction (227: ToolCallDetails + StepRow + toolStepDerivation extracted)`. (Capped under 200 chars).
       - Update `MessageItem.tsx`: triple updated, status updated from `extraction due` to `honoured by extraction (227: RunTerminalStatus + messageText + UserMessageBubble extracted)`. (Capped under 200 chars).
       - Update `RunCard.tsx`: triple updated, status noted for Phase 227 run frame ownership.
    3. In `docs/HOT-FILE-LEDGER.md`:
       - Append extraction discharge narrative under `## frontend/src/components/chat/ToolCallPanel.tsx` and `## frontend/src/components/chat/MessageItem.tsx`.
       - Record that both files left "extraction due" legitimately by shedding responsibilities (~650 lines from ToolCallPanel across 3 modules; ~200 lines from MessageItem across 3 modules).
    4. Run size check (B-4): `node scripts/check-claude-md-size.cjs`. Must pass with exit code 0.
  </action>
  <verify>
    Run `node scripts/check-claude-md-size.cjs` from repo root.
  </verify>
  <acceptance_criteria>
    - Both `MessageItem.tsx` and `ToolCallPanel.tsx` rows leave "extraction due" legitimately.
    - Triples match current HEAD state.
    - `docs/HOT-FILE-LEDGER.md` details the extractions in the same commit.
    - `node scripts/check-claude-md-size.cjs` passes with 0 errors.
  </acceptance_criteria>
  <done>Updated CLAUDE.md and docs/HOT-FILE-LEDGER.md discharging extraction due in the same commit.</done>
</task>

<task type="auto">
  <name>Task 4: Author 227-VALIDATION.md and run full gate suite</name>
  <files>.planning/phases/227-the-run-frame-has-one-owner/227-VALIDATION.md</files>
  <read_first>
    .planning/phases/227-the-run-frame-has-one-owner/227-CONTEXT.md
    .planning/phases/227-the-run-frame-has-one-owner/227-PREFLIGHT.md
  </read_first>
  <action>
    1. Author `.planning/phases/227-the-run-frame-has-one-owner/227-VALIDATION.md`:
       - Document verification results across all 8 run states: streaming, settled, failed, timed-out, cancelled, paused-on-approval, no-tools, sub-agent.
       - Document SC#3 1-file edit verification: moving terminal status is localized to `RunCard.tsx` and right-aligning result column is localized to `StepRow.tsx`.
       - Document gate results: `vitest-count-gate.cjs`, `tsc -p tsconfig.app.json`, and backend pytest.
    2. Run full mechanical gate sweep:
       - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs`
       - `(npx tsc --noEmit -p tsconfig.app.json 2>&1 | Select-String "error TS").Count`
       - `pytest backend/tests/unit/`
  </action>
  <verify>
    Run `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` and `pytest backend/tests/unit/test_224_approval_deadline.py`.
  </verify>
  <acceptance_criteria>
    - `227-VALIDATION.md` exists and details validation across all 8 states and SC#1..SC#5 requirements.
    - All repository gates pass.
  </acceptance_criteria>
  <done>Authored 227-VALIDATION.md and verified all mechanical gates.</done>
</task>

</tasks>
