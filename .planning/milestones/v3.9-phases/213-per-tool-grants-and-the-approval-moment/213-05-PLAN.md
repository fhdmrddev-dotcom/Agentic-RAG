---
phase: 213-per-tool-grants-and-the-approval-moment
plan: 05
type: execute
wave: 5
depends_on: [01, 02, 03, 04]
files_modified:
  - backend/tests/integration/test_213_grants_approval_integration.py
  - scripts/vitest-count-gate.cjs
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [GRANT-01, GRANT-02, GRANT-03, GRANT-04, GRANT-05]

must_haves:
  truths:
    - "An integration test exercises setting posture via the real write path and evaluating it via the real gate for BOTH connection shapes, mocking NEITHER side (S-3, AGENTS.md rule)"
    - "scripts/vitest-count-gate.cjs explicitly pins all new test files created in Phase 213 under TARGETS (GATE-1)"
    - "CLAUDE.md and docs/HOT-FILE-LEDGER.md are updated in the same commit with re-derived line counts and dispositions under 200 chars (LEDGER-1, LEDGER-2, LEDGER-3)"
    - "vitest count gate reports 0 failing tests and 0 missing files (GATE-2)"
    - "tsc -p tsconfig.app.json reports no new errors beyond baseline (34 errors)"
    - "pytest backend/tests/unit reports no new failures beyond the 68 rot set"
  artifacts:
    - path: "backend/tests/integration/test_213_grants_approval_integration.py"
      provides: "End-to-end integration test connecting write path to gate execution without mock leakage"
    - path: "scripts/vitest-count-gate.cjs"
      provides: "Updated count-gate target list pinning new test suites"
    - path: "CLAUDE.md"
      provides: "Updated hot-file ledger rows with re-derived metrics"
  key_links:
    - from: "scripts/vitest-count-gate.cjs"
      to: "frontend/src/components/settings/__tests__/ToolGrantsSection.test.tsx"
      via: "TARGETS pin"
      pattern: "ToolGrantsSection\\.test\\.tsx"
---

<objective>
Verify cross-plan integration without mocks, pin all newly created test suites in `scripts/vitest-count-gate.cjs`, synchronize the G-5 hot-file ledger in `CLAUDE.md` and `docs/HOT-FILE-LEDGER.md`, and execute the full verification gauntlet.

Purpose: S-3 and AGENTS.md §3 require a real integration test mocking neither side to ensure the join between write paths, data storage, and gate enforcement is sound. GATE-1 requires manual pinning in `vitest-count-gate.cjs`. LEDGER-1/2/3 require keeping hot file metrics honest.

Output: `test_213_grants_approval_integration.py`, updated `vitest-count-gate.cjs`, updated `CLAUDE.md` / `docs/HOT-FILE-LEDGER.md`, and updated roadmap/state docs.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/213-per-tool-grants-and-the-approval-moment/213-CONTEXT.md
@.planning/phases/213-per-tool-grants-and-the-approval-moment/213-PREFLIGHT.md
@CLAUDE.md

<interfaces>
From `213-PREFLIGHT.md` §1 (S-3), §4 (GATE-1), §5 (LEDGER-1, LEDGER-2, LEDGER-3):
- New test suites to pin in `scripts/vitest-count-gate.cjs`:
  * `src/components/settings/__tests__/grantsVocabulary.test.ts`
  * `src/components/settings/__tests__/ToolGrantsSection.test.tsx`
- Hot-file re-derivations for `CLAUDE.md`:
  * `ConnectionFormPanel.tsx`
  * `connectionFormCopy.ts`
  * `models/connector.py`
  * `api/connectors.py`
  * `phase_types.py`
  * `connector_service.py`
  * `ConnectionsTab.tsx`
</interfaces>
</context>

<tasks>

<task type="auto" id="213-05-01">
  <name>Create end-to-end integration test mocking neither side</name>
  <description>
    Create `backend/tests/integration/test_213_grants_approval_integration.py` asserting:
    1. Create MCP connection draft with discovered tools.
    2. Set per-tool postures via service write path (`update_connection_grants`).
    3. Execute phase step via `_exec_external_action` and verify gate enforcement respects the exact stored posture.
    4. Repeat for capability connection (e.g. SMTP send_email) with `"deny"` and `"allow"` postures.
    5. Verify `external_action_sent` and `tool_refused` receipts are logged into database.
  </description>
  <files>
    backend/tests/integration/test_213_grants_approval_integration.py
  </files>
  <verification>
    `pytest backend/tests/integration/test_213_grants_approval_integration.py` passes.
  </verification>
</task>

<task type="auto" id="213-05-02">
  <name>Pin new test files in scripts/vitest-count-gate.cjs</name>
  <description>
    1. In `scripts/vitest-count-gate.cjs`:
       - Add `src/components/settings/__tests__/grantsVocabulary.test.ts` and `src/components/settings/__tests__/ToolGrantsSection.test.tsx` to `TARGETS` array (GATE-1).
       - Re-run `node scripts/vitest-count-gate.cjs` to confirm 0 failures and all targets present.
  </description>
  <files>
    scripts/vitest-count-gate.cjs
  </files>
  <verification>
    `node scripts/vitest-count-gate.cjs` executes cleanly.
  </verification>
</task>

<task type="auto" id="213-05-03">
  <name>Update G-5 Hot-File Ledger in CLAUDE.md and docs/HOT-FILE-LEDGER.md</name>
  <description>
    1. Re-derive line counts, commit counts, and phase counts for all touched files using CLAUDE.md's recipe:
       - `git log --follow --oneline <file> | wc -l`
       - `git log --follow --format=%s <file> | grep -oE '\b(Phase )?[0-9]{3}\b' | sort -u | wc -l`
       - `wc -l <file>`
    2. Update `CLAUDE.md` Hot-File Ledger table ensuring disposition cells remain <= 200 characters (LEDGER-3).
    3. Update `docs/HOT-FILE-LEDGER.md` with detail sections.
    4. Run `node scripts/check-claude-md-size.cjs`.
  </description>
  <files>
    CLAUDE.md
    docs/HOT-FILE-LEDGER.md
  </files>
  <verification>
    `node scripts/check-claude-md-size.cjs` passes.
  </verification>
</task>

<task type="auto" id="213-05-04">
  <name>Run full multi-gate verification gauntlet</name>
  <description>
    Execute full project verification:
    1. `npx tsc --noEmit -p tsconfig.app.json` (must not exceed baseline 34 errors).
    2. `node scripts/vitest-count-gate.cjs` (0 failing tests).
    3. `pytest backend/tests/unit` (must not exceed baseline 68 failures).
    4. Update `.planning/ROADMAP.md` and `.planning/STATE.md` with Phase 213 progress.
  </description>
  <files>
    .planning/ROADMAP.md
    .planning/STATE.md
  </files>
  <verification>
    All gates green according to baseline tolerances.
  </verification>
</task>

</tasks>
