---
phase: 213-per-tool-grants-and-the-approval-moment
plan: 04
type: execute
wave: 4
depends_on: [02, 03]
files_modified:
  - backend/app/services/harness_engine.py
  - backend/app/services/ask_user_service.py
  - frontend/src/components/panel/PendingAskCard.tsx
  - frontend/src/pages/WorkflowRunPage.tsx
  - backend/tests/unit/test_213_approval_moment.py
  - frontend/src/components/panel/__tests__/PendingAskCard.test.tsx
  - frontend/src/pages/__tests__/WorkflowRunPage.test.tsx
autonomous: true
requirements: [GRANT-03, GRANT-05]

must_haves:
  truths:
    - "When a tool evaluates to 'ask' posture, harness_engine pauses at the armed action-risk checkpoint and creates an ask prompt (D-213-09)"
    - "The ask prompt names the service, tool, and exact formatted arguments (GRANT-03)"
    - "The grant approval waits indefinitely without timer countdown or expiry (D-213-12, Invariant #9)"
    - "PendingAskCard on WorkflowRunPage displays the grant approval ask at the paused step (D-213-10 / CORR-1)"
    - "PendingAskCard provides 'Approve once', 'Deny', and 'Always allow <tool> on this connection' (D-213-11)"
    - "'Always allow' is only visible if the user holds require_org_manage authorization (SEC-4)"
    - "Clicking 'Always allow' writes an audit receipt, updates the connection tool_grants to 'allow', and resumes the run (D-213-11)"
    - "Denying the prompt records a tool_refused receipt with reason='denied_by_user' (D-213-16)"
  artifacts:
    - path: "backend/app/services/harness_engine.py"
      provides: "Approval pause integration for 'ask' posture tools"
    - path: "frontend/src/components/panel/PendingAskCard.tsx"
      provides: "Grant approval UI with formatted arguments, 'Always allow', and honest no-countdown line"
    - path: "backend/tests/unit/test_213_approval_moment.py"
      provides: "Unit tests verifying harness pause, resume on approval, and refusal on denial"
  key_links:
    - from: "frontend/src/components/panel/PendingAskCard.tsx"
      to: "frontend/src/lib/api/connectors.ts"
      via: "updateConnectionGrants on 'Always allow'"
      pattern: "updateConnectionGrants"
    - from: "backend/app/services/harness_engine.py"
      to: "backend/app/services/connectors/grants.py"
      via: "resolve_tool_posture"
      pattern: "from app\\.services\\.connectors\\.grants import"
---

<objective>
Implement the run-time approval moment: pausing executions when an external tool requires "Ask first" approval, presenting the prompt in `PendingAskCard` on `WorkflowRunPage`, supporting "Approve once", "Deny", and "Always allow" with audit logging.

Purpose: Satisfies GRANT-03 (run pauses and asks a real person with service, tool, and arguments) and GRANT-05 (audit receipts). Reuses the existing armed action-risk checkpoint (D-213-09) and the honest no-countdown line (D-213-12).

Output: Updated `harness_engine.py`, updated `PendingAskCard.tsx`, `WorkflowRunPage.tsx` integration, and tests in `test_213_approval_moment.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/213-per-tool-grants-and-the-approval-moment/213-CONTEXT.md
@.planning/phases/213-per-tool-grants-and-the-approval-moment/213-PREFLIGHT.md
@.planning/sketches/213-grants-and-the-approval-moment/BUILD-CONTRACT.generated.md
@CLAUDE.md

<interfaces>
From `BUILD-CONTRACT.generated.md` and `213-CONTEXT.md` (D-213-09, D-213-10, D-213-11, D-213-12, D-213-16):
- Ask strings:
  `ASK_HEADLINE`: "Paused — waiting for you."
  `ASK_NOTHING_SENT`: "Nothing has been sent yet."
  `ASK_ARGS_LABEL`: "What it will send"
  `ASK_APPROVE`: "Approve once"
  `ASK_DENY`: "Deny"
  `ASK_ALWAYS(tool_name)`: `Always allow ${tool_name} on this connection`
  `ASK_ALWAYS_NOTE`: "Changes the setting above, not just this run."

- Flow on `WorkflowRunPage`:
  * `PendingAskCard` is rendered via `RunSpine`'s `renderAsk` when `slug === askAnchorSlug` (CORR-1).
  * No deadline is set (`hasDeadline === false`), so `PendingAskCard` displays `NO_DEADLINE_WAITING_LINE` ("Waiting indefinitely until approved or denied").
  * On "Always allow":
    1. Check user has `can_manage` (SEC-4).
    2. Call `updateConnectionGrants(connectionId, { ...toolGrants, [toolName]: "allow" })`.
    3. Write audit log entry for permission change.
    4. Submit ask approval response.
</interfaces>
</context>

<tasks>

<task type="auto" id="213-04-01">
  <name>Integrate 'ask' posture into harness_engine armed checkpoint</name>
  <description>
    1. In `backend/app/services/harness_engine.py`:
       - When executing `external_action` phase, evaluate tool posture via `resolve_tool_posture`.
       - If posture is `"ask"`, trigger the action-risk pause:
         * Create ask payload containing `service_id`, `service_name`, `tool_name`, `connection_id`, and formatted `arguments` JSON.
         * Set `timeout_seconds = None` (no deadline / waits indefinitely — D-213-12).
         * Block until user responds via `subscribe_for_response`.
       - On user denial:
         * Record `tool_refused` audit receipt with `reason: "denied_by_user"` (D-213-16).
         * Raise `PhaseExecutionError` with composed refusal message.
       - On user approval:
         * Proceed to execute external action.
  </description>
  <files>
    backend/app/services/harness_engine.py
    backend/app/services/ask_user_service.py
  </files>
  <verification>
    Check harness pause triggers only on "ask" and does not pause twice on armed steps (D-213-09).
  </verification>
</task>

<task type="auto" id="213-04-02">
  <name>Update PendingAskCard and WorkflowRunPage for grant approval</name>
  <description>
    1. In `frontend/src/components/panel/PendingAskCard.tsx`:
       - Detect when an ask is a connector tool approval ask (carrying `tool_name` and `connection_id`).
       - Render `ASK_HEADLINE`, `ASK_NOTHING_SENT`, and formatted arguments in a collapsible block (`ASK_ARGS_LABEL`).
       - Render `ASK_APPROVE` ("Approve once") and `ASK_DENY` ("Deny").
       - Render `ASK_ALWAYS(tool_name)` button and `ASK_ALWAYS_NOTE` when user has org manage permissions (SEC-4).
       - Ensure `hasDeadline` is false so no countdown or progress bar is displayed (Invariant #9).
    2. In `frontend/src/pages/WorkflowRunPage.tsx`:
       - Verify `PendingAskCard` mounts correctly within `RunSpine` when run is paused on tool approval.
  </description>
  <files>
    frontend/src/components/panel/PendingAskCard.tsx
    frontend/src/pages/WorkflowRunPage.tsx
  </files>
  <verification>
    `npx vitest run src/components/panel/__tests__/PendingAskCard.test.tsx` passes.
  </verification>
</task>

<task type="auto" id="213-04-03">
  <name>Add unit tests for approval moment and run-time pause/resume</name>
  <description>
    1. Create `backend/tests/unit/test_213_approval_moment.py`:
       - Test harness pauses on "ask" posture and creates prompt with tool name and arguments.
       - Test resume with approval proceeds with tool execution.
       - Test resume with denial aborts with `tool_refused` (`reason="denied_by_user"`).
    2. In `frontend/src/components/panel/__tests__/PendingAskCard.test.tsx`:
       - Test rendering of grant approval ask, argument formatting, "Approve once", "Deny", and "Always allow".
       - Test "Always allow" calls `updateConnectionGrants` and approves ask.
  </description>
  <files>
    backend/tests/unit/test_213_approval_moment.py
    frontend/src/components/panel/__tests__/PendingAskCard.test.tsx
  </files>
  <verification>
    `pytest backend/tests/unit/test_213_approval_moment.py` and `npx vitest run src/components/panel/__tests__/PendingAskCard.test.tsx` pass.
  </verification>
</task>

</tasks>
