---
phase: 213-per-tool-grants-and-the-approval-moment
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - backend/app/services/connectors/descriptors.py
  - frontend/src/components/settings/grantsVocabulary.ts
  - frontend/src/components/settings/ToolGrantsSection.tsx
  - frontend/src/components/settings/ConnectionFormPanel.tsx
  - frontend/src/components/settings/connectionFormCopy.ts
  - frontend/src/components/settings/__tests__/grantsVocabulary.test.ts
  - frontend/src/components/settings/__tests__/ToolGrantsSection.test.tsx
  - frontend/src/components/settings/__tests__/ConnectionFormPanel.test.tsx
  - backend/tests/unit/test_211_static_descriptors.py
autonomous: true
requirements: [GRANT-01, GRANT-02]

must_haves:
  truths:
    - "static_descriptors_for_capability can return N descriptors (D-213-01 / SEED-214 mechanism unlock) while keeping Slack/Jira/SMTP at 1 each"
    - "grantsVocabulary.ts is ported verbatim from .planning/sketches/213-grants-and-the-approval-moment/COPY.js and imported by UI components (BUILD CONTRACT §1)"
    - "ToolGrantsSection renders 3-state posture control (Allow / Ask first / Deny) per tool with connection-level default posture selector (GRANT-01, GRANT-02)"
    - "Overridden tool row shows 'You changed this', is highlighted with --primary left edge, and offers 'Use the default' reset button (Invariants #1, #2, #10)"
    - "Pressed Deny arm uses --destructive styling, never --primary (Invariant #4)"
    - "Tool direction tag renders Reads, Changes things, or Unknown with help text when readOnlyHint is absent (Invariant #7)"
    - "ConnectionFormPanel panel width track is clamp(480px, 38%, 640px) (Invariant #5)"
    - "ConnectionFormPanel.test.tsx:571 assertion is updated from 400px to clamp(480px, 38%, 640px) in the same commit (Invariant #5)"
    - "Zero [title] attributes are rendered across the grant surface (Invariant #8)"
  artifacts:
    - path: "frontend/src/components/settings/grantsVocabulary.ts"
      provides: "Exact user-facing copy dictionary for posture selectors, tool directions, overrides, and search placeholders"
    - path: "frontend/src/components/settings/ToolGrantsSection.tsx"
      provides: "Searchable per-tool grants list component with 3-state posture toggle and connection default picker"
    - path: "frontend/src/components/settings/__tests__/ToolGrantsSection.test.tsx"
      provides: "React Testing Library suite verifying all 10 sketch invariants"
  key_links:
    - from: "frontend/src/components/settings/ToolGrantsSection.tsx"
      to: "frontend/src/components/settings/grantsVocabulary.ts"
      via: "imports COPY constants"
      pattern: "from \"./grantsVocabulary\""
    - from: "frontend/src/components/settings/ConnectionFormPanel.tsx"
      to: "frontend/src/components/settings/ToolGrantsSection.tsx"
      via: "embeds ToolGrantsSection"
      pattern: "<ToolGrantsSection"
---

<objective>
Implement the frontend per-tool grants list UI and connection-level default posture controls based on the approved G-2 sketch contract, widen the panel track to `clamp(480px, 38%, 640px)`, and unlock `static_descriptors_for_capability` to return N descriptors.

Purpose: GRANT-01 and GRANT-02 require an interactive, searchable list of tools where users can grant, ask, or deny permissions individually or inherit the connection default. Satisfies all 10 sketch invariants from `BUILD-CONTRACT.generated.md`.

Output: `grantsVocabulary.ts`, `ToolGrantsSection.tsx`, updated `ConnectionFormPanel.tsx`, updated `descriptors.py`, and test suites.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/213-per-tool-grants-and-the-approval-moment/213-CONTEXT.md
@.planning/phases/213-per-tool-grants-and-the-approval-moment/213-PREFLIGHT.md
@.planning/sketches/213-grants-and-the-approval-moment/BUILD-CONTRACT.generated.md
@CLAUDE.md

<interfaces>
From `BUILD-CONTRACT.generated.md`:
- `PANEL_TITLE`, `PANEL_SUBTITLE`, `DEFAULT_POSTURE_LABEL`, `DEFAULT_POSTURE_HELP`, `POSTURE_ALLOW`, `POSTURE_ASK`, `POSTURE_DENY`, `LIST_EMPTY`, `OVERRIDDEN_LABEL`, `OVERRIDDEN_RESET`, `DIRECTION_READS`, `DIRECTION_CHANGES`, `DIRECTION_UNKNOWN`, `DIRECTION_UNKNOWN_HELP`.
- Composed functions: `SEARCH_PLACEHOLDER(count)`, `REFUSED_HEADLINE(tool)`, `REFUSED_BECAUSE_DENIED(tool)`, `REFUSED_NEXT`.
- 10 invariants:
  1. Override edge is `--primary`, spending no state colour.
  2. `.arow` reserves 2px transparent edge lane (`border-left: 2px solid transparent`).
  3. Every posture control has exactly 3 arms, exactly 1 pressed (`aria-pressed`).
  4. Pressed Deny arm uses `--destructive`.
  5. Panel track is `clamp(480px, 38%, 640px)`.
  6. Connections list renders beside open panel.
  7. Unknown direction says "Unknown" and explains itself in DOM text.
  8. Zero `[title]` attributes.
  9. No countdown / timer on the ask moment.
  10. Overridden row also carries text "You changed this".
</interfaces>
</context>

<tasks>

<task type="auto" id="213-03-01">
  <name>Unlock static_descriptors_for_capability in descriptors.py</name>
  <description>
    1. In `backend/app/services/connectors/descriptors.py`:
       - Update `static_descriptors_for_capability(capability: str) -> list[dict[str, Any]]`:
         Allow returning a list of multiple descriptors (SEED-214 mechanism unlock), while maintaining current 1-element mappings for Slack, Jira, SMTP.
    2. In `backend/tests/unit/test_211_static_descriptors.py`:
       - Verify `static_descriptors_for_capability` returns a list of dicts.
  </description>
  <files>
    backend/app/services/connectors/descriptors.py
    backend/tests/unit/test_211_static_descriptors.py
  </files>
  <verification>
    `pytest backend/tests/unit/test_211_static_descriptors.py` passes.
  </verification>
</task>

<task type="auto" id="213-03-02">
  <name>Port COPY.js to grantsVocabulary.ts and test</name>
  <description>
    1. Create `frontend/src/components/settings/grantsVocabulary.ts` by porting `.planning/sketches/213-grants-and-the-approval-moment/COPY.js` verbatim.
    2. Export all constants and composed functions matching `BUILD-CONTRACT.generated.md` §1.
    3. Create `frontend/src/components/settings/__tests__/grantsVocabulary.test.ts` asserting exact string contents.
  </description>
  <files>
    frontend/src/components/settings/grantsVocabulary.ts
    frontend/src/components/settings/__tests__/grantsVocabulary.test.ts
  </files>
  <verification>
    `npx vitest run src/components/settings/__tests__/grantsVocabulary.test.ts` passes.
  </verification>
</task>

<task type="auto" id="213-03-03">
  <name>Create ToolGrantsSection component with 10 sketch invariants</name>
  <description>
    1. Create `frontend/src/components/settings/ToolGrantsSection.tsx`:
       - Props: `tools: McpDiscoveredTool[]`, `toolGrants: Record<string, ToolGrantPosture>`, `defaultPosture: ToolGrantPosture`, `onChangeGrants: (grants: Record<string, ToolGrantPosture>) => void`, `onChangeDefaultPosture: (posture: ToolGrantPosture) => void`.
       - Render connection default posture segment (`Allow` / `Ask first` / `Deny`).
       - Render search input with `SEARCH_PLACEHOLDER(tools.length)`.
       - Filter tools by search query; render `LIST_EMPTY` when 0 matches.
       - For each tool row:
         * Determine posture: `toolGrants[tool.name] ?? defaultPosture`.
         * Is overridden: `tool.name in toolGrants && toolGrants[tool.name] !== defaultPosture`.
         * 3-state segmented button group (`Allow`, `Ask first`, `Deny`) with `aria-pressed`.
         * Direction tag: `Reads` (if `readOnlyHint === true`), `Changes things` (if `readOnlyHint === false`), `Unknown` (if absent or undefined).
         * Overridden row styling: `border-l-2 border-primary` when overridden, `border-l-2 border-transparent` when inherited (Invariant #2).
         * "You changed this" badge and "Use the default" button to remove key from `toolGrants`.
         * Deny button styled with destructive tokens when pressed (Invariant #4).
         * No `title` attributes (Invariant #8).
  </description>
  <files>
    frontend/src/components/settings/ToolGrantsSection.tsx
  </files>
  <verification>
    Check component compiles cleanly without lint or title attributes.
  </verification>
</task>

<task type="auto" id="213-03-04">
  <name>Integrate ToolGrantsSection into ConnectionFormPanel and update width</name>
  <description>
    1. In `frontend/src/components/settings/ConnectionFormPanel.tsx`:
       - Update split grid layout style from `400px` to `clamp(480px, 38%, 640px)` (Invariant #5).
       - Mount `ToolGrantsSection` when connection has discovered/advertised tools (or in edit mode).
       - Update form state management to track `draft.default_approval_posture` and `draft.tool_grants`.
       - When saving, include `default_approval_posture` and `tool_grants` in payload.
    2. In `frontend/src/components/settings/__tests__/ConnectionFormPanel.test.tsx`:
       - Update line 571 (and any other 400px pinning tests) to assert `clamp(480px, 38%, 640px)`.
       - Add test verifying `ToolGrantsSection` renders inside panel and updates posture state.
    3. Create `frontend/src/components/settings/__tests__/ToolGrantsSection.test.tsx` testing all 10 invariants.
  </description>
  <files>
    frontend/src/components/settings/ConnectionFormPanel.tsx
    frontend/src/components/settings/connectionFormCopy.ts
    frontend/src/components/settings/__tests__/ToolGrantsSection.test.tsx
    frontend/src/components/settings/__tests__/ConnectionFormPanel.test.tsx
  </files>
  <verification>
    `npx vitest run src/components/settings/__tests__/ToolGrantsSection.test.tsx src/components/settings/__tests__/ConnectionFormPanel.test.tsx` passes.
  </verification>
</task>

</tasks>
