---
status: complete
phase: 095.1-cross-provider-run-honesty-workspace-parity
source: [095.1-01-SUMMARY.md, 095.1-02-SUMMARY.md, 095.1-03-SUMMARY.md, 095.1-04-SUMMARY.md, 095.1-05-SUMMARY.md, 095.1-06-SUMMARY.md, 095.1-07-SUMMARY.md, 095.1-VALIDATION.md]
driver: operator-driven clicks (live app — frontend :5173 + backend :8000 both up) cross-checked against live Supabase runs/messages via psycopg2; re-UAT after gap plans 06 + 07
started: 2026-06-06T13:45:00Z
updated: 2026-06-06T20:45:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

[testing complete]

## Tests

### 1. Model attribution at rest (D-04, cross-provider axis)
expected: A finished run's run-sub line shows the real `{provider} · {model} · turn N` at rest (no expand needed). Cross-provider spot-check: the label is correct per provider (e.g. openai · gpt-…, anthropic · claude-…, google · gemini-…).
result: pass
evidence: |
  All 8 native providers verified by reopening existing completed runs (zero new API cost):
  openai·gpt-5.4-mini·turn 1 | anthropic·claude-haiku-4-5·turn 1 | google·gemini-3.5-flash·turn 1 |
  openrouter·z-ai/glm-5.1·turn 1 | deepseek·deepseek-v4-flash·turn 1 | moonshot·kimi-k2.6·turn 1 |
  zhipu·glm-4.6·turn 1 | minimax·MiniMax-M2.5-highspeed·turn 1. No "model OMITTED" stub anywhere.

### 2. Workspace panel fill vs clean (WORKSPACE-PARITY D-01/02, cross-provider axis)
expected: A genuine multi-step prompt (≥2 meaningful tools) fills the workspace todos panel with REAL task rows (with a subtle "derived from activity" marker when no write_todos was called — incl. providers that never call write_todos). A one-shot Q&A leaves the panel CLEAN (no forced rows).
result: pass
reverified: 2026-06-06 (post Plan 06 gap-closure — commit ef6d1a92)
evidence: |
  RE-UAT after Plan 06 (WorkspacePanel.hasActivity now ORs derived.length>0 via useDerivedPanel).
  Operator confirmed live on 3 reopened threads (zero new API cost), each cross-checked against DB truth:
  - FILLS: "Survey Data Analysis Python Visualization" (anthropic, NO write_todos, 2 meaningful tool_calls)
    → panel shows "derived from activity" marker + 2 ✓ rows: "Search documents" + "Creating CSV and bar
    chart for survey sample size data" (exact rows pre-computed by replaying deriveWorkspacePanel over the
    thread's persisted tool_calls). Was "No workspace activity yet" before the fix — GAP-1 CLOSED live.
  - STAYS CALM: "Simple Addition Question" (no-tool thread) → "No workspace activity yet" (discriminates;
    not always-on).
  - REAL TODOS WIN: "Survey Sample Size CSV and Chart" (openai, write_todos) → 4 real todos (Fibonacci /
    running sum / primes / combined table), NO derived marker (precedence honored).
  Cross-provider generalization: DB-verified that ALL 8 providers have qualifying NO_write_todos runs
  (anthropic 10 / deepseek 6 / google 3 / minimax 5 / moonshot 6 / openai 3 / openrouter 3 / zhipu 4);
  the derivation is pure frontend over persisted tool_calls (identical code per provider), so the visual
  confirmation generalizes to all 8 by construction.

### 3. Multi-tool derived items (WORKSPACE-PARITY D-01/02, multi-tool axis)
expected: A prompt exercising `search_documents` + `execute_code` in one turn → the derived panel shows BOTH as semantic items (not generic "Run code"), each mirroring its tool's status.
result: pass
reverified: 2026-06-06 (folds into Test 2 — same Plan 06 fix)
evidence: |
  Same Plan-06 gate fix makes the multi-tool derived items reachable. Confirmed on the "Survey Data
  Analysis Python Visualization" thread: search_documents → "Search documents" + execute_code → its real
  description "Creating CSV and bar chart for survey sample size data" (NOT generic "Run code") — both
  semantic, both ✓ done. The humanize() precedence (execute_code.description > inferLabel(code) > "Run
  code") produces the real description here. GAP-1 multi-tool axis CLOSED.
folds_into: 2

### 4. Flat "Generated files" list (WORKSPACE-PARITY D-06)
expected: A multi-file run renders ONE flat "Generated files" list — no "★ Your file" hero crown, no separate "Working files" group. Every file is an equal row, all downloadable. A url-less file shows a clearly-disabled "Download unavailable" affordance (not a silent plain filename).
result: pass
evidence: "Live OpenAI run generated survey_sample_size.csv (77 B) + survey_sample_size.png (35.3 KB). FinalOutputsPanel renders ONE flat 'GENERATED FILES' list — no '★ Your file' crown, no 'Working files' group — each row an equal OutputFileCard with the per-extension icon (.CSV green table glyph / .PNG) + size + download button. D-06 confirmed."

### 5. Live timer honesty (RUN-HONESTY D-05)
expected: While a run streams, the elapsed timer ticks live and NEVER vanishes; at the streaming→terminal edge it FREEZES once at the true elapsed and stays visible.
result: pass
evidence: "Live OpenAI run: timer ticked 9.7s → 41.9s during streaming, then froze once at the terminal edge (collapsed row 'Run · 5 steps · ✓ done · 41.9s', streaming=false). Never vanished."

### 6. True reload duration — no inflation (RUN-HONESTY D-05, long-message axis)
expected: Reopen a completed run (ideally an old one / a long thread ≥50 msgs) → the timer shows the TRUE `completedAt − startedAt`, identical to what it froze at live. It does NOT inflate to "time since creation" (the BUG-260606-02 "1440m for a day-old run" lie). A finished run with no completedAt shows NO duration (never a fabricated one).
result: pass
evidence: |
  8-provider reopened runs all show true durations proportional to work (1 step→5.5s … 14 steps→1m4s),
  which is mathematically incompatible with the old now−created_at inflation. Decisive literal-old case:
  `sse-diff openai` was created in Phase 089 (~7 days ago) and its reopened timer reads 23.4s — NOT the
  ~10,000 minutes the old bug would show. NEW-095-A / BUG-260606-02 closed.

### 6b. Live model attribution + turn consistency (RUN-HONESTY D-04 — NEW finding)
expected: A just-finished run (without reload) shows the same `{provider} · {model} · turn N` it shows after reload.
result: pass
reverified: 2026-06-06 (post Plan 07 gap-closure — commits 4c4c7401 + 9e605012)
evidence: |
  RE-UAT after Plan 07 (dispatch JSONResponse carries additive model/provider; StreamsProvider stamps them
  onto the live placeholder; RunCard turn decoupled to a stable 1). Operator confirmed live on 2 providers:
  - OpenAI gpt-5.4-mini: just-finished run (NO reload) showed "openai · gpt-5.4-mini · turn 1"; after reload
    the IDENTICAL string. Was a bare "turn N" with no provider·model before the fix.
  - Anthropic claude-haiku-4-5: same — "anthropic · claude-haiku-4-5 · turn 1" live AND after reload.
  DB cross-check (runs table, last 30 min): openai/gpt-5.4-mini completed 11.7s + anthropic/
  claude-haiku-4-5-20251001 completed 10.5s — model/provider match the UI attribution exactly. SC#10 ≥2
  providers live; shared dispatch path byte-identical for the other 6 (1-hunk threads.py diff). GAP-2 CLOSED.
  BONUS (gate-calm on a fresh run): both prompts fired only 1 meaningful tool (search_documents — the model
  printed the number inline rather than calling execute_code; DB-confirmed meaningful=1 each), so the panel
  correctly STAYED CALM live (1 < the ≥2 gate). Operator endorsed this as the intended design — derived
  tasks surface only when work warrants it, never forced. A third live confirmation that the gate discriminates.

### 7. Parallel-thread isolation (RUN-HONESTY D-04/05, parallel-thread axis)
expected: Thread A streaming (live-ticking timer + its own attribution) while Thread B accepts a new prompt → neither run's timer or model attribution corrupts the other.
result: pass
evidence: "Started a 9-step run in Thread A; while it streamed, switched to Thread B (eval anthropic) → B showed its own anthropic·claude-haiku-4-5·turn 1 / 16.8s, uncorrupted, not streaming. A's run completed in the background (Run · 9 steps · ✓ done · 21.1s); back on A, all 3 runs show independent correct timers (41.4s / 8.9s / 21.1s). No cross-thread bleed. (Both-threads-simultaneously-streaming variant not stress-tested; isolation mechanism — thread-scoped StreamsProvider — exercised.)"

### 8. Deliverable-aware Resume (RUN-HONESTY D-07)
expected: A run that produced output files and then failed/timed_out shows NO "Resume" affordance; a run that failed with NO deliverables still shows "Resume". Completed/cancelled/streaming runs unchanged.
result: skipped
reason: "Operator skipped the live reopen (the two qualifying runs are hard to locate among busy threads) and accepted the static + DB-verified backstop. Evidence upgraded beyond the prior 'blocked': BOTH halves were located as REAL runs in the live DB and traced through the actual render path — (A) failed-WITH-deliverables: run 3a1b3dc3 / thread 949ac810 (moonshot, status=failed) links to msg c0f58591 whose execute_code results carry real output_files (sample_data.csv + line_chart.png); api.ts:129-152 reconstructs finalOutputFiles from those persisted tool_calls on reload → producedDeliverables=true → MessageItem.tsx:405 gate hides Resume. (B) failed-NO-deliverables: run 2f697daa / thread ef6586fb (google 429, status=failed) links to msg 3ee7b135 (error copy, no output_files) → producedDeliverables=false → Resume shown. runStatus reconstructs on reload via _enrich_messages_with_runs SELECT status → run_status (threads.py:260,279) → api.ts:114. So the gate WOULD render correctly on both; only the manual click was skipped. Unit-tested 11/11 (BUG-260518-01); predicate confirmed in code review."

### 9. 429 reads as rate-limit, not billing (PROVIDER-ERR D-03)
expected: Forcing a real rate-limit (e.g. Google over quota → RESOURCE_EXHAUSTED whose text contains "quota") surfaces a rate-limit-with-retry message, NEVER "billing / insufficient credits".
result: skipped
reason: "Cannot be exercised by reopening — the user-facing error copy is FROZEN into messages.content at error time, so a reopen shows the stored string, not a re-classification. Proven directly: the live DB still holds the OLD bug copy ('API billing or rate-limit error: ... check your provider's billing dashboard') on real Google 429 RESOURCE_EXHAUSTED runs (ef6586fb, 17b1e158) — historical evidence of the exact bug. The shipped code provably inverts it: classify_provider_error('google', exc) returns 'rate_limit' whenever exc.code==429, BEFORE any billing branch (errors.py:128-136); message_for_kind('rate_limit') = 'Rate limited by the provider — please retry in a moment.' (no 'billing'). Unit-tested 30/30. Only a fresh live 429 would re-run the classifier (unforceable on demand without disrupting the operator's accounts), so operator accepted the verified-by-code backstop. NOTE: those real 429s were literally 'prepayment credits are depleted' (genuinely billing, returned by Google as 429) — the fix now reads those as rate-limit too; deliberate trade-off (no structured signal distinguishes Google rate-limit vs credit-depletion, and the prior direction was the worse lie). Captured as SEED-057."

## Current Test

[testing complete]

## Summary

total: 10
passed: 8
issues: 0
blocked: 0
pending: 0
skipped: 2

passed_tests: [1 attribution-at-rest (8 providers), 2 panel-fill-derived (RE-VERIFIED post Plan 06), 3 multi-tool-derived (RE-VERIFIED post Plan 06), 4 flat-files, 5 live-timer, 6 reload-timer (8 providers + week-old), 6b live-attribution (RE-VERIFIED post Plan 07), 7 parallel-thread-isolation]
issue_tests: []  # all 3 prior issues (2, 3, 6b) re-verified PASS after gap plans 06 + 07
skipped_tests: [8 deliverable-aware-Resume (operator-skipped — hard to locate runs; static + DB-traced backstop accepted), 9 429-not-billing (unforceable live — frozen error copy; verified by code + 30/30 unit; SEED-057 captured)]

## Re-UAT note (2026-06-06, post gap-closure)

This UAT was first run pre-gap-closure (5 pass / 3 issue / 2 blocked). Gap plans 06 (WORKSPACE-PARITY hasActivity gate) + 07 (RUN-HONESTY live attribution + turn) then shipped. This re-UAT confirmed ALL 3 prior issues now PASS live (operator-driven clicks, each cross-checked against the live Supabase `runs` + `messages` truth via psycopg2). GAP-1 generalized to all 8 providers by DB-verified qualifying-run presence + identical provider-agnostic render path. The 2 prior blocked tests were operator-skipped with a strengthened static + DB-traced backstop. Net: phase goal achieved — every provider's runs are honest and legible.

## Gaps

- truth: "A multi-step run (≥2 meaningful tools) fills the workspace todos panel with derived rows; a no-write_todos provider still gets a populated panel (the OpenAI-parity bar)."
  status: failed
  reason: "User-observable: reopening a 14-step Google run (and a 2-tool OpenAI run) shows 'No workspace activity yet' — the derived panel never appears."
  severity: major
  test: 2
  root_cause: "WorkspacePanel.tsx:113 `hasActivity` omits the useDerivedPanel output → PanelEmpty short-circuits before TodosSection (which holds the Plan-02 derivation at :145) can mount. Unreachable on the no-write_todos parity case the phase targets."
  artifacts: [frontend/src/components/panel/WorkspacePanel.tsx:113-114, frontend/src/components/panel/TodosSection.tsx:145, frontend/src/lib/workspacePanel.ts]
  missing: ["WorkspacePanel.hasActivity must also be true when shouldPopulate(viewing thread tool_calls) is true (or when useDerivedPanel returns items), so TodosSection mounts and renders the derived rows"]

- truth: "A just-finished run shows the same {provider}·{model}·turn N live as it shows after reload (WHO answered is honest in the live moment, and the turn number is stable)."
  status: failed
  reason: "Live/just-finished run-sub shows only 'turn N' (N=iterationCount+1, e.g. turn 6) with NO provider·model; after reload/refetch the same run shows 'openai · gpt-5.4-mini · turn 1'. WHO answered is invisible until refresh, and the turn number diverges live-vs-reload."
  severity: minor
  test: 6b
  root_cause: "Plan 03 enrich SELECT (runs.model/provider) runs only on GET /messages. The live SSE/dispatch message carries iterationCount but not the enriched model/provider, so RunCard renders the graceful 'turn N' fallback (N from iterationCount). On reload, model/provider arrive but iterationCount is dropped → turn 1. The two states disagree."
  artifacts: [backend/app/api/threads.py (enrich SELECT — reload only), frontend/src/components/chat/RunCard.tsx (run-sub derivation + turn = iterationCount+1), frontend/src/lib/api.ts (live dispatch mapper)]
  missing: ["Carry runs.model/runs.provider onto the live message (dispatch response / SSE run-start) so attribution shows live; and/or reconcile the turn-N source so live and reload agree (persist/derive a stable turn, or drop iterationCount-as-turn in favor of the persisted value)"]
