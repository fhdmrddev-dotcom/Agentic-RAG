---
phase: 095.1-cross-provider-run-honesty-workspace-parity
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - frontend/src/providers/StreamsProvider.tsx
  - frontend/src/components/panel/TodosSection.tsx
  - frontend/src/components/panel/__tests__/TodosSection.test.tsx
autonomous: true
requirements: [WORKSPACE-PARITY]
nyquist_compliant: true
user_setup: []

must_haves:
  truths:
    - "On a genuine multi-step run (≥2 meaningful tools) where the model never called write_todos, the workspace todos panel fills with read-only items derived from the actual tool activity"
    - "When the model DID call write_todos, the panel shows those real todos — derivation never overwrites a real plan"
    - "A one-shot Q&A / single-tool lookup leaves the panel CLEAN (no derived items)"
    - "Derived items are clearly marked 'derived from activity' and are not interactive (no tick)"
    - "Reopening the thread next day recomputes the same derived panel from persisted tool_calls (reload-safe for free)"
  artifacts:
    - path: "frontend/src/components/panel/TodosSection.tsx"
      provides: "Real-todos-first precedence; else derived read-only rows with the activity marker"
      contains: "deriveWorkspacePanel"
    - path: "frontend/src/providers/StreamsProvider.tsx"
      provides: "A panel-only derived-todos selector hook over the viewing thread's chat tool_calls (PANEL-06-safe)"
      contains: "deriveWorkspacePanel"
  key_links:
    - from: "frontend/src/components/panel/TodosSection.tsx"
      to: "frontend/src/lib/workspacePanel.ts"
      via: "deriveWorkspacePanel fallback when real todos empty + gate passes"
      pattern: "deriveWorkspacePanel"
    - from: "frontend/src/providers/StreamsProvider.tsx"
      to: "bucketsBySurface chat tool_calls"
      via: "panel-only derived selector reading the viewing thread's messages"
      pattern: "deriveWorkspacePanel|bucketsBySurface"
---

<objective>
Wire the Plan-01 `deriveWorkspacePanel` selector into the workspace todos panel so it
fills CONSISTENTLY for every provider on genuine multi-step work — including providers
that never call `write_todos` (the OpenAI-gpt-5.4-mini parity bar) — and stays CLEAN for
trivial chats (D-095.1-01/02).

Precedence (locked): real `write_todos` todos win; else, when the smart gate passes,
synthesize READ-ONLY items from the persisted `tool_calls`; else clean. Rendered in the
SAME TodosSection UI with a subtle "derived from activity" marker; status mirrors each
tool's status; the user cannot tick a derived item.

Purpose: closes the panel-fill symptom of BUG-260604-01 (the model won't loop todos →
the panel was empty) and `non-anthropic-generic-code-task-descriptions` (the label
inference chain gives non-Anthropic runs meaningful labels). NO backend, NO new SSE event,
NO migration — a pure frontend projection over DB-truth tool_calls.

Output: 1 StreamsProvider derived-selector hook + TodosSection precedence render + test.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/095.1-cross-provider-run-honesty-workspace-parity/095.1-CONTEXT.md
@.planning/phases/095.1-cross-provider-run-honesty-workspace-parity/095.1-RESEARCH.md
@.planning/phases/095.1-cross-provider-run-honesty-workspace-parity/095.1-PATTERNS.md
@frontend/src/lib/workspacePanel.ts
@frontend/src/components/panel/TodosSection.tsx

<interfaces>
<!-- The contracts the executor needs. -->

From frontend/src/lib/workspacePanel.ts (Plan 01 — the selector to consume):
```typescript
export interface DerivedPanelItem { label: string; status: "pending" | "in_progress" | "completed" }
export function shouldPopulate(toolCalls: ToolCall[] | undefined | null): boolean
export function deriveWorkspacePanel(toolCalls: ToolCall[] | undefined | null): DerivedPanelItem[]
```

The panel reads ONLY the panel Maps via dedicated hooks (PANEL-06 isolation). The current
TodosSection (src/components/panel/TodosSection.tsx) reads `useTodos(threadId)` →
`todosByThread`, renders read-only `<TodoRow>`s, and `return null` when empty.

The chat message bucket (where tool_calls live) is read in StreamsProvider via:
```typescript
useStreamsStore.getState().bucketsBySurface.get("chat")?.get(threadId) ?? []
```
This is a Message[]; each Message has `tool_calls?: ToolCall[]`. PANEL-06 contract
(StreamsProvider.tsx:118-128, test FC#1): a panel-Map mutation must NEVER change the chat
bucket reference, and chat MessageList selectors read `bucketsBySurface` exclusively.

The current panel-write seam (StreamsProvider.tsx:783) — how derived/SSE state reaches the panel:
```typescript
onTodoUpdated: (todos) =>
  useStreamsStore.getState().actions.replaceTodosForThread(threadId, todos),
```

`useViewingThread()` (StreamsProvider) returns the currently-viewed threadId.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Panel-only derived-todos selector hook in StreamsProvider (PANEL-06-safe)</name>
  <files>frontend/src/providers/StreamsProvider.tsx</files>
  <read_first>
    - frontend/src/providers/StreamsProvider.tsx:105-128 (the EMPTY_TODOS stable-ref + PANEL-06 persistence-trigger contract)
    - frontend/src/providers/StreamsProvider.tsx:2228-2245 (the useTodos hook shape to mirror)
    - frontend/src/providers/StreamsProvider.tsx:1699,2214 (how the chat bucket is read: bucketsBySurface.get("chat").get(threadId))
    - frontend/src/lib/workspacePanel.ts (Plan 01 — shouldPopulate + deriveWorkspacePanel)
    - frontend/src/providers/__tests__/panelHooks.test.tsx + streamsProvider.test.tsx (the FC#1 isolation test — confirm no chat re-render)
    - 095.1-RESEARCH.md §"D-095.1-01 + D-095.1-02" step 3 (the option-a/option-b architectural decision + Open Question 1) + Assumptions A2
  </read_first>
  <behavior>
    - useDerivedPanel(threadId) returns DerivedPanelItem[] derived from the viewing thread's persisted assistant tool_calls (flattened across the thread's messages)
    - Returns [] (the SAME stable EMPTY ref) when threadId is null OR the gate (shouldPopulate) does not pass — so reading it never forces a chat re-render (PANEL-06)
    - Reading the derived selector does NOT mutate todosByThread and does NOT change the chat bucket reference (FC#1 isolation preserved)
    - The derivation flattens tool_calls from all assistant messages in the thread's chat bucket, in order
  </behavior>
  <action>
    Implement the panel wiring as OPTION (b) — a panel-side READ-ONLY derived selector that
    does NOT write into `todosByThread` (chosen over RESEARCH option (a) because writing derived
    items into the panel store from the chat path risks the PANEL-06 isolation contract and muddies
    real-vs-derived precedence; a pure read selector keeps ONE clean precedence and zero cross-store
    write). If, while implementing, the read selector cannot satisfy PANEL-06 (the FC#1 isolation
    test goes red), fall back to option (a) — feed derived items through `replaceTodosForThread` ONLY
    when `todosByThread.get(threadId)` is empty — and document the deviation. Verify against FC#1.

    Add a new exported hook near `useTodos` (StreamsProvider.tsx ~2228):
    ```typescript
    // Phase 095.1 (D-095.1-01/02): the activity-derived workspace panel. A PURE
    // read selector over the viewing thread's persisted chat tool_calls — NEVER
    // writes todosByThread (keeps real write_todos precedence trivial) and NEVER
    // mutates the chat bucket ref (PANEL-06 / FC#1). Reload-safe because tool_calls
    // are DB truth reconstructed by _mapMessageResponse.
    const EMPTY_DERIVED: DerivedPanelItem[] = []   // stable ref (module-level, beside EMPTY_TODOS)
    export function useDerivedPanel(threadId: string | null): DerivedPanelItem[] {
      return useStreamsStore((s) => {
        if (!threadId) return EMPTY_DERIVED
        const msgs = s.bucketsBySurface.get("chat")?.get(threadId) ?? []
        const allToolCalls = msgs.flatMap((m) => m.tool_calls ?? [])
        if (!shouldPopulate(allToolCalls)) return EMPTY_DERIVED
        return deriveWorkspacePanel(allToolCalls)
      })
    }
    ```
    NOTE: a selector returning a fresh array each render can churn useSyncExternalStore — if the
    isolation/perf tests flag re-render churn, memoize the derivation by computing it once per
    bucket-reference change (e.g. a small module-level WeakMap keyed on the Message[] ref, or
    `useMemo` in the consumer over a stable tool_calls signature). Prefer the simplest form that
    passes FC#1 + the existing panelHooks render-count assertions.

    Place `EMPTY_DERIVED` at module level beside `EMPTY_TODOS` (line ~109). Import `shouldPopulate`,
    `deriveWorkspacePanel`, and the `DerivedPanelItem` type from `@/lib/workspacePanel`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/providers/__tests__/panelHooks.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/providers/__tests__/panelHooks.test.tsx` passes (FC#1 isolation + render-count assertions green — ZERO net-new failures)
    - `grep -c "export function useDerivedPanel" frontend/src/providers/StreamsProvider.tsx` == 1
    - `grep -c "deriveWorkspacePanel" frontend/src/providers/StreamsProvider.tsx` >= 1
    - `grep -c "EMPTY_DERIVED" frontend/src/providers/StreamsProvider.tsx` >= 2 (declared + returned)
    - The selector does NOT call `replaceTodosForThread` (unless the documented option-(a) fallback fired): `grep -A8 "useDerivedPanel" frontend/src/providers/StreamsProvider.tsx | grep -c "replaceTodosForThread"` == 0 under option (b)
    - `cd frontend && npx tsc -b` zero net-new vs baseline 37
  </acceptance_criteria>
  <done>useDerivedPanel exported; reads chat tool_calls; gate-respecting; stable EMPTY ref; FC#1 isolation green; tsc baseline-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TodosSection precedence render — real todos first, else derived read-only rows + marker</name>
  <files>frontend/src/components/panel/TodosSection.tsx, frontend/src/components/panel/__tests__/TodosSection.test.tsx</files>
  <read_first>
    - frontend/src/components/panel/TodosSection.tsx (the full file — the TodoRow read-only shape + the empty-return + the status icon/label system to reuse)
    - frontend/src/components/panel/__tests__/TodosSection.test.tsx (the test analog to extend)
    - frontend/src/providers/StreamsProvider.tsx useDerivedPanel (Task 1 output)
    - frontend/src/components/panel/WorkspacePanel.tsx:89,136 (how TodosSection is mounted under PanelSection with the todos count)
    - 095.1-RESEARCH.md §"D-095.1-01" Pitfall 2 (precedence inversion) + the "derived from activity" marker discretion note
    - 095.1-PATTERNS.md §"Pattern E" (render model-derived strings as text children only)
  </read_first>
  <behavior>
    - Real write_todos present (useTodos non-empty) → render the real todos exactly as today; NO derived items, NO marker
    - Real todos empty + useDerivedPanel non-empty → render the derived items as READ-ONLY rows (reuse the StatusIndicator + text label) with a subtle "derived from activity" marker visible once at the section top
    - Real todos empty + derived empty → return null (clean — unchanged)
    - Derived rows have NO interactive control (no checkbox/tick onClick); status mirrors the derived item status
    - All derived labels render as React text children (never dangerouslySetInnerHTML)
  </behavior>
  <action>
    Edit `frontend/src/components/panel/TodosSection.tsx`. Keep the existing real-todos path verbatim
    (TodoRow, StatusIndicator, STATUS_LABEL, the sr-only progress region) as PRECEDENCE 1. Add the
    derived fallback:

    1. Import `useDerivedPanel` from `@/providers/StreamsProvider` and call it with the same
       `useViewingThread()` threadId already obtained at the top of `TodosSection`.
    2. Change the early return: instead of `if (todos.length === 0) return null`, branch:
       - `if (todos.length > 0)` → render the existing real-todos `<ul>` UNCHANGED (precedence 1).
       - `else if (derived.length > 0)` → render the derived items: a subtle marker line at the top
         (`<span className="block px-3 pb-1 text-[0.62rem] font-mono uppercase tracking-wider text-panel-muted-foreground-dim">derived from activity</span>`
         — non-interactive, reuse an existing dim token), then a `<ul>` of read-only rows. Each row
         reuses the same layout as `TodoRow` (the `<StatusIndicator>` + the content text + the status
         text label) but renders a derived item: `normalizeStatus(item.status)` for the icon/label and
         `{item.label}` as the content (text children — NEVER innerHTML). Give each derived row a NON-
         interactive container (a `<li>` with no onClick); do NOT render a checkbox or any tick affordance.
         Key the derived rows by index.
       - `else` → `return null` (clean).
    3. The PanelSection count in WorkspacePanel currently reads `todos.length`; leave WorkspacePanel
       UNTOUCHED in this plan (the derived count surfacing in the section header is optional polish, out
       of scope — files_modified excludes WorkspacePanel.tsx to keep this plan disjoint). The derived
       rows render inside the existing "Todos" PanelSection body via TodosSection.

    Extend `frontend/src/components/panel/__tests__/TodosSection.test.tsx`: (a) real todos present →
    renders real rows, NO "derived from activity" marker; (b) real todos empty + a mocked non-empty
    useDerivedPanel → renders derived rows + the marker, and the rows have no interactive tick; (c)
    both empty → renders nothing. Mock `useDerivedPanel`/`useTodos` from `@/providers/StreamsProvider`
    (the test already mocks the provider hooks — mirror that). Assert the marker text "derived from
    activity" appears ONLY in the derived case, and a derived label renders as text.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/panel/__tests__/TodosSection.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/panel/__tests__/TodosSection.test.tsx` passes (real-first / derived-fallback / clean cases green)
    - `grep -c "useDerivedPanel" frontend/src/components/panel/TodosSection.tsx` >= 1
    - `grep -c "derived from activity" frontend/src/components/panel/TodosSection.tsx` >= 1
    - `grep -c "dangerouslySetInnerHTML" frontend/src/components/panel/TodosSection.tsx` == 0 (T-095.1-02-01)
    - The derived rows carry NO onClick tick: `grep -c "onClick" frontend/src/components/panel/TodosSection.tsx` == 0 (the section had none before; derived rows add none)
    - `cd frontend && npx tsc -b` zero net-new vs baseline 37
    - `cd frontend && npx vitest run src/providers/__tests__/panelHooks.test.tsx` still green (no isolation regression)
  </acceptance_criteria>
  <done>TodosSection renders real todos first, else derived read-only rows + "derived from activity" marker, else null; labels as text; no tick; tests green; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model/sandbox → derived todo labels | `deriveWorkspacePanel` labels (from `tc.args.description` / inferred code / `write_todos` content) are model-written strings rendered in the panel |
| chat store → panel render | the derived selector reads the chat bucket — must not break PANEL-06 isolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-095.1-02-01 | Tampering/XSS (V5 Output Encoding) | TodosSection derived rows | mitigate | Derived labels render as React text children (`{item.label}`) only — never dangerouslySetInnerHTML. Acceptance greps `dangerouslySetInnerHTML`==0 in TodosSection.tsx. Reuses the existing read-only TodoRow text idiom. |
| T-095.1-02-02 | Elevation (isolation breach) | useDerivedPanel selector | mitigate | The derived selector is READ-ONLY over the chat bucket; returns a stable EMPTY ref on miss/clean so it never forces a chat re-render; never writes todosByThread (option b). Verified against FC#1 + panelHooks render-count assertions. |
| T-095.1-02-03 | Spoofing (precedence inversion) | TodosSection precedence | mitigate | Real `write_todos` (useTodos non-empty) ALWAYS wins; derivation is strictly the empty-real fallback (Pitfall 2). Test (a) proves a real plan is never overwritten by derived rows. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/panel/__tests__/TodosSection.test.tsx` green
- `cd frontend && npx vitest run src/providers/__tests__/panelHooks.test.tsx` green (FC#1 isolation intact)
- `cd frontend && npx tsc -b` zero net-new vs baseline 37; `npx vite build` exit 0
- Manual UAT (deferred to /gsd:verify-work, authored in VALIDATION.md): cross-provider panel fill/clean across all 8 providers
</verification>

<success_criteria>
- A multi-step no-write_todos run fills the panel with semantic derived items; a one-shot Q&A stays clean; a real write_todos plan is shown unchanged
- Derived items are read-only with the "derived from activity" marker; labels render as text
- PANEL-06 isolation preserved (FC#1 green); tsc baseline-clean; vite build clean
</success_criteria>

<output>
After completion, create `.planning/phases/095.1-cross-provider-run-honesty-workspace-parity/095.1-02-SUMMARY.md`
</output>
