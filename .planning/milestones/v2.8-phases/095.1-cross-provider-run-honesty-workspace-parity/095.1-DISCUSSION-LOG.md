# Phase 095.1: Cross-Provider Run Honesty & Workspace Parity - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-06
**Phase:** 095.1-cross-provider-run-honesty-workspace-parity
**Areas discussed:** Panel derivation, Smart gate, Provider errors, Model attribution, Reload timer, Flat files, Requirement IDs, Bug routing
**Mode:** Interactive, one-decision-at-a-time (operator preference: freeform plain-text, reasoning + recommendation per item). Advisor mode OFF (no USER-PROFILE.md).

---

## Decision 1 — Deterministic panel: derivation locus + render shape

| Option | Description | Selected |
|--------|-------------|----------|
| Frontend-derived projection | Synthesize panel from already-present persisted `tool_calls`; honor `write_todos` when present; render as read-only todo items in the same panel | ✓ |
| Backend-derived SSE | Server computes panel, pushes synthetic todo events | |

**User's choice:** Frontend-derived ("proceed").
**Notes:** Grounded in spike-006/007 code. Frontend avoids touching the byte-identical Deep emit path (no cross-provider regression risk), is reload-safe for free (tool_calls are persisted DB truth), zero added cost. Research flag: confirm `execute_code.description` survives reload-reconstruction in the store.

---

## Smart gate definition (re-framed)

Initial framing (which tools count as "meaningful") was too in-the-weeds for the operator. Re-anchored on the **outcome**: every provider's panel should show the *real tasks it performed*, matching the OpenAI gpt-5.4-mini run. Operator confirmed this is exactly the goal.

**User's choice:** Outcome-locked — panel mirrors the real steps each provider took (OpenAI-parity), clean only for trivial one-shot chats. Claude handles the tool-set detail. Gate = `write_todos` OR ≥2 meaningful tools (GATE_MIN_STEPS=2). Inclusive on retrieval.
**Notes:** Operator: "I want all providers to behave like OpenAI when we tried 5.4-mini — it showed what tasks it actually did."

---

## Decision 3 — Provider errors (Google 429 mislabeled as billing)

| Option | Description | Selected |
|--------|-------------|----------|
| Proper fix at gateway boundary | Per-provider adapters classify on structured status codes; small honest error-kind taxonomy; never guess billing | ✓ |
| Reorder keyword list | Quick patch: move rate-limit ahead of billing in the shared string classifier | |

**User's choice:** Proper fix at the gateway boundary.
**Notes:** Matches the operator's standing direction (092.5 gateway separation). Honesty rule: claim a specific cause only when a structured signal proves it.

---

## Decision 4 — Model attribution

| Option | Description | Selected |
|--------|-------------|----------|
| `{provider} · {model}` real ID, visible at rest | Populate the existing run-sub from `runs.model`/`runs.provider`, no migration | ✓ |
| Generic / model-only / prose form | "Generated by …" prose, or model without provider | |

**User's choice:** Proper fix, real model ID visible at rest.
**Notes:** Finishes the 095-07 stub. Real resolved model ID so "which model actually answered" is honest. Additive SELECT, no migration.

---

## Decision 5 — Reload timer

| Option | Description | Selected |
|--------|-------------|----------|
| True `completed_at − started_at`; show nothing if missing | Send persisted run end-time on the existing join; never compute vs now | ✓ |
| Keep now-based / approximate fallback | Fall back to time-since-creation when no edge witnessed | |

**User's choice:** Proper fix; show nothing rather than a fake number.
**Notes:** Confirmed `runs.completed_at` already exists + populated on finalize → SELECT-only, no migration, no new write. Honesty rule: never fabricate a duration.

---

## Decision 6 — Flat "Generated files" list

| Option | Description | Selected |
|--------|-------------|----------|
| One flat equal list; drop crown; keep dead-link affordance; backend untouched | Frontend-only flatten of the hero/working split | ✓ |
| Keep hero/working split | Retain the "★ Your file" crown + collapsible working group | |

**User's choice:** Flat list ("yes").
**Notes:** Operator UAT call — a wrong crown misleads; can't reliably know which file is primary. Backend `is_hero` left unused (forward-compatible). Future: plain length "show all" only if bloat felt — never a "primary" guess.

---

## Requirement IDs (confirmed)

- WORKSPACE-PARITY → Decisions 1, 2, 6
- RUN-HONESTY → Decisions 4, 5 + deliverable-aware Resume
- PROVIDER-ERR → Decision 3

**User's choice:** Confirmed.

---

## Bug routing (mandatory cross-check, confirmed)

- Fold IN: `resume-button-after-success` (deliverable-aware Resume); `agent-ignores-step-by-step` (panel-fill symptom only; behavioral root deferred to v2.9/SEED-052).
- Addressed by design: `non-anthropic-generic-code-task-descriptions` (label fallback).
- Already folded: `google-429-mislabeled-as-billing`, `reload-timer-inflated`.
- Leave OPEN: `orphaned-askuser-404` (→ 096/HITL); `general-chat-silent-send-drop` (different surface).

**User's choice:** Confirmed (a) and (b).

## Claude's Discretion

- The exact `MEANINGFUL_TOOLS` membership and the run-sub/file-list visual specifics (within the locked outcomes).

## Deferred Ideas

- Dedicated planner sub-agent (spike option B); `agent-ignores-step-by-step` behavioral root; file-bloat length management.
