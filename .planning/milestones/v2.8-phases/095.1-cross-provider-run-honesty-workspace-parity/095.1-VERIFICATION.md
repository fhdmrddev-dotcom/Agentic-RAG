---
phase: 095.1-cross-provider-run-honesty-workspace-parity
verified: 2026-06-06T20:45:00Z
status: verified
score: 7/7 (6/7 static + SC#7 live gate now PASSED via 095.1-UAT.md)
live_uat:
  ran: 2026-06-06 (operator-driven clicks; every observation cross-checked vs live Supabase runs/messages via psycopg2)
  result: "GAP-1 (panel fill + multi-tool derived items) and GAP-2 (live attribution + turn live==reload) re-verified PASS live on real runs; panel-fill generalized to ALL 8 providers (DB-verified qualifying runs + identical render path). Tests 8 (Resume) + 9 (429->rate-limit) operator-skipped with a strengthened static+DB-traced backstop (Google credit-depletion-as-rate-limit -> SEED-057). UAT: 8 pass / 0 issue / 2 skipped."
  artifact: .planning/phases/095.1-cross-provider-run-honesty-workspace-parity/095.1-UAT.md
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 6/7
  gaps_closed:
    - "GAP-1 (WORKSPACE-PARITY): WorkspacePanel.hasActivity now ORs derived.length > 0 via the existing useDerivedPanel(threadId) hook — the activity-derived panel is structurally reachable for no-write_todos cross-provider runs."
    - "GAP-2 (RUN-HONESTY): threads.py dispatch JSONResponse carries additive model/provider (already-resolved values); StreamsProvider stamps them onto the live assistant placeholder; RunCard run-sub turn decoupled from iterationCount to a stable 1 so live == reload."
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "All-8-provider cross-provider + 4-axis UAT (SC#7 / SC#10)"
    expected: "Panel fills for ALL 8 providers on genuine multi-step work; 429 reads as rate-limit (not billing) on each provider; timer shows TRUE duration on day-old reload; model attribution visible live AND at rest; flat file list with no crown; deliverable-aware Resume correct."
    why_human: "Real LLM streams required; timer honesty depends on actual DB completedAt; 429 forcing, parallel-thread isolation, and long-message axis require a live browser. Automated tests prove the mechanism; live UAT proves it fires correctly across all 8 providers."
---

# Phase 095.1: Cross-Provider Run Honesty & Workspace Parity — Verification Report (Post-Gap-Closure)

**Phase Goal:** Every provider's runs are honest and legible — the workspace todos/tasks panel fills consistently (deterministically, never forced), provider errors read truthfully, finished runs never falsely offer Resume, the timer never lies on reload, and the user can always see which model answered — verified live across all 8 providers.
**Verified:** 2026-06-06T20:00:00Z
**Status:** human_needed
**Re-verification:** Yes — post-gap-closure (plans 06 + 07 shipped after initial verification ecca8a68)

---

## Re-verification Summary

The initial verification (ecca8a68, status human_needed, 6/7 static) was produced before gap-closure plans 06 and 07 shipped. This is the authoritative post-gap-closure report. Both gaps are confirmed CLOSED in code:

- **GAP-1** (Plan 06, commits be1d657f + ef6d1a92): `WorkspacePanel.tsx` now imports and calls `useDerivedPanel(threadId)` and includes `derived.length > 0` in `hasActivity`. The derivation path was previously structurally unreachable; it is now reachable. Guard test `WorkspacePanel.derived.test.tsx` exists (4 tests green) and exercises the real gate→TodosSection→derived-render path that the prior unit tests bypassed.
- **GAP-2** (Plan 07, commits 46eb92a0 → 4c4c7401 → c2dc809b → 6bb1beda → 9e605012): `threads.py` dispatch response carries additive `model`/`provider`; `PostMessageResponse` in `api.ts` extended; `StreamsProvider` stamps them onto the live placeholder; `RunCard` run-sub turn fixed to stable `1`. Cross-provider shared-path is byte-identical (1-hunk diff on threads.py content only).

Both gaps are closed at the unit/static level. The sole remaining open item is the live 8-provider Chrome-MCP UAT (SC#7 / SC#10 4-axis), which is human-only by VALIDATION.md design.

---

## Goal Achievement

### Observable Truths (Roadmap Success Criteria SC#1–SC#7)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC#1 | Workspace panel fills deterministically for ALL providers via smart-gate + activity-derivation; stays clean for simple chats | VERIFIED | `workspacePanel.ts` (223 lines): `shouldPopulate` + `deriveWorkspacePanel`. `useDerivedPanel` in `StreamsProvider.tsx`. `TodosSection.tsx`: 3-way precedence render. **GAP-1 CLOSED:** `WorkspacePanel.tsx` L109 `const derived = useDerivedPanel(threadId)`; L126-131 `hasActivity` includes `derived.length > 0` (commit ef6d1a92). Guard test `WorkspacePanel.derived.test.tsx` confirms the real gate→render path (4/4 green). Full suite: 31/31 + 58/58 green. |
| SC#2 | A 429 rate-limit reads as rate-limit with retry guidance, NEVER "billing" — at the per-provider gateway boundary | VERIFIED | `errors.py` (213 lines): `classify_provider_error` branches on `provider=="google"` using `.code` (int), 429 → `rate_limit` before ANY billing check; `message_for_kind("rate_limit")` never contains "billing". Wired into `agent_loop.py` line 2271. 30/30 unit tests green. |
| SC#3 | A run that produced deliverables but ended failed/timed_out does NOT falsely offer Resume | VERIFIED | `MessageItem.tsx` line 405: `!producedDeliverables` guard in Resume gate. `producedDeliverables = (message.finalOutputFiles?.length ?? 0) > 0`. 11/11 render tests green. |
| SC#4 | Reloaded completed run shows TRUE duration (completedAt − startedAt), never time-since-creation | VERIFIED | `RunCard.tsx` lines 121–161: `wasStreamingRef` gate distinguishes live→terminal from reloaded already-terminal; honesty rule: no `completedAt` → no duration shown. 34/34 RunCard tests green (6 timer cases). |
| SC#5 | Every assistant response surfaces which model/provider generated it (`{provider} · {model} · turn N`) | VERIFIED | **GAP-2 CLOSED (live path):** `threads.py` L1592-1606 dispatch JSONResponse carries additive `"model": _resolved_model` / `"provider": _resolved_provider` (commit 4c4c7401). `api.ts` `PostMessageResponse` += `model?/provider?`. `StreamsProvider.tsx` destructures `resolvedModel`/`resolvedProvider` and stamps them onto the assistant placeholder (`null → undefined`). `RunCard.tsx` L245-248: `const turnNumber = 1`; run-sub stable live==reload (commit 9e605012). Reload path: `threads.py` additive enrich SELECT (Plan 03, unchanged). 35/35 RunCard tests green incl. the decisive live==reload guard. |
| SC#6 | Output files render as ONE flat "Generated files" list — no hero crown, no hero/working split, all downloadable | VERIFIED | `MessageItem.tsx` `FinalOutputsPanel`: flat `space-y-1.5` map. `OutputFileCard.tsx`: hero glow/gradient removed; uniform 30px icon; `variant` prop inert; "Download unavailable" kept. 11/11 flat-list tests green. |
| SC#7 | All-8-provider live verification (SC#10 4-axis: multi-tool, parallel-thread, cross-provider, long-message) | HUMAN NEEDED | Requires live browser Chrome-MCP UAT per VALIDATION.md. Automated backstop green; live confirmation is the remaining step. |

**Score:** 6/7 truths statically verified. SC#7 is manual-only by design (VALIDATION.md explicitly routes this to `/gsd:verify-work`).

---

## Gap-Closure Verification (GAP-1 + GAP-2)

### GAP-1: WorkspacePanel hasActivity now reaches useDerivedPanel

**Plan 06 must-haves against the actual codebase:**

| Must-have | Status | Evidence |
|-----------|--------|----------|
| `WorkspacePanel.tsx` `hasActivity` includes `derived.length > 0` via EXISTING `useDerivedPanel` hook | VERIFIED | L126-131: `const hasActivity = todos.length > 0 \|\| files.length > 0 \|\| pendingAsks.length > 0 \|\| phases.length > 0 \|\| derived.length > 0` |
| `useDerivedPanel` imported and called at the top of WorkspacePanel | VERIFIED | L42: import from `@/providers/StreamsProvider`; L109: `const derived = useDerivedPanel(threadId)` |
| Todos count badge honest: omitted (not "0/0") when derived-only | VERIFIED | L161: `count={todos.length > 0 ? { done: todosDone, total: todos.length } : undefined}` |
| Guard test `WorkspacePanel.derived.test.tsx` exists and exercises the real gate path | VERIFIED | File exists at `frontend/src/components/panel/__tests__/WorkspacePanel.derived.test.tsx`; 4 tests: Test A (derived rows render + marker), Test B (trivial → PanelEmpty), Test C (real todos precedence wins), count-badge honesty |
| Key link: `WorkspacePanel.tsx` → `useDerivedPanel(threadId)` in `hasActivity` gate | WIRED | Import + call confirmed; `hasActivity` gate includes `derived.length > 0` |
| Key link: `WorkspacePanel.hasActivity` → TodosSection mount | WIRED | TodosSection mounts inside the `hasActivity` true branch at L163 |

**TDD evidence (RED confirmed before GREEN):**
- Commit `be1d657f` (test RED): `WorkspacePanel.derived.test.tsx` added; Test A + count-badge guard fail on unmodified WorkspacePanel (hasActivity omits derived → PanelEmpty short-circuit → Todos section never mounts).
- Commit `ef6d1a92` (feat GREEN): hasActivity extended; all 4 derived tests pass; existing suites (WorkspacePanel 24 + TodosSection 12 + panelHooks 18) stay green.

### GAP-2: Live attribution stamp + turn reconciliation

**Plan 07 must-haves against the actual codebase:**

| Must-have | Status | Evidence |
|-----------|--------|----------|
| `threads.py` dispatch JSONResponse carries `model: _resolved_model` / `provider: _resolved_provider` (additive) | VERIFIED | L1597-1605: additive comment + `"model": _resolved_model` / `"provider": _resolved_provider` in the content dict. git diff = 1 hunk, 8 lines (6 comment + 2 data). |
| threads.py diff confined to dispatch content — NO `_emit`/SSE event/chunk/agent_runner/provider-branch change | VERIFIED | `git diff HEAD~8 HEAD -- backend/app/api/threads.py` shows only 8 added lines inside the JSONResponse content block. No `_emit`, no `'delta'`/`'error'`, no chunk handler. |
| `PostMessageResponse` in `api.ts` += optional `model?`/`provider?` | VERIFIED | L214-224: `model?: string \| null` / `provider?: string \| null` with comment. |
| `StreamsProvider.tsx` stamps `resolvedModel`/`resolvedProvider` onto assistant placeholder | VERIFIED | L1468-1472: destructure with aliasing (`model: resolvedModel, provider: resolvedProvider`); L1511-1518: assistant placeholder branch stamps `model: resolvedModel ?? undefined` / `provider: resolvedProvider ?? undefined`. |
| `RunCard.tsx` run-sub turn stable `1` (decoupled from iterationCount) | VERIFIED | L245: `const turnNumber = 1`; L246-248: run-sub reads `message.provider && message.model ? \`${provider} · ${model} · turn 1\` : \`turn 1\``. |
| Decisive live==reload guard test in `RunCard.test.tsx` | VERIFIED | Committed in 9e605012; live-shaped message (model+provider+iterationCount 5) and reload-shaped message (model+provider, no iterationCount) both render identical run-sub strings — asserted with byte-compare. |

**Key link verification:**

| From | To | Via | Status |
|------|----|-----|--------|
| `threads.py send_message` | `PostMessageResponse {model, provider}` | additive JSONResponse keys `"model": _resolved_model` | WIRED |
| `StreamsProvider.tsx dispatch` | assistant placeholder `message.model/provider` | `resolvedModel ?? undefined` stamp in `setMessagesForBucket` map | WIRED |
| `RunCard.tsx runSub` | stable `turn 1` (live == reload) | `const turnNumber = 1` — decoupled from `iterationCount` | WIRED |

**TDD evidence (RED confirmed before GREEN):**
- Commit `46eb92a0` (test RED Task 1): dispatch test asserts `model`/`provider` keys → KeyError (absent before fix).
- Commit `4c4c7401` (feat GREEN Task 1): dispatch response extended; 2/2 TestSendMessageDispatchAttribution green.
- Commit `6bb1beda` (test RED Task 2): decisive live==reload guard asserts identical run-sub strings → fails (live `turn 6` ≠ reload `turn 1`).
- Commit `9e605012` (feat GREEN Task 2): live attribution stamp + `turnNumber = 1`; 35/35 RunCard suites green.

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `frontend/src/lib/workspacePanel.ts` | shouldPopulate, deriveWorkspacePanel, MEANINGFUL_TOOLS, inferLabel | VERIFIED | 223 lines; all exports present; 31/31 tests green |
| `frontend/src/components/panel/WorkspacePanel.tsx` | hasActivity includes derived.length > 0 via useDerivedPanel | VERIFIED | Import + call + gate confirmed in code (Plan 06 gap-closure) |
| `frontend/src/components/panel/__tests__/WorkspacePanel.derived.test.tsx` | Guard test: real WorkspacePanel + real TodosSection through the gate | VERIFIED | File exists; 4 tests covering headline guard, negative, real-todos precedence, count-badge honesty |
| `frontend/src/providers/StreamsProvider.tsx` | useDerivedPanel export + live dispatch stamps model/provider | VERIFIED | useDerivedPanel exported; resolvedModel/resolvedProvider stamped in dispatch |
| `frontend/src/components/panel/TodosSection.tsx` | 3-way precedence: real todos → derived rows + marker → null | VERIFIED | DerivedRow + "derived from activity" marker; real write_todos wins |
| `backend/app/services/provider_gateway/errors.py` | ErrorKind, classify_provider_error, message_for_kind | VERIFIED | 213 lines; 30/30 tests green |
| `backend/app/services/agent_loop.py` | classify_provider_error wired at error catch | VERIFIED | Line 2271: `kind = classify_provider_error(...)` |
| `backend/app/api/threads.py` | Additive enrich SELECT (Plan 03) + additive dispatch model/provider (Plan 07) | VERIFIED | Enrich SELECT (Plans 01-05, unchanged); dispatch JSONResponse += model/provider (Plan 07, 1-hunk) |
| `frontend/src/lib/api.ts` | PostMessageResponse += model?/provider?; snake→camel mapper for enrich fields | VERIFIED | PostMessageResponse extended; `startedAt`/`completedAt`/`model`/`provider` in `_mapMessageResponse` |
| `frontend/src/types/index.ts` | Message += model?/provider?/startedAt?/completedAt? | VERIFIED | All 4 optional fields with doc-comments |
| `frontend/src/components/chat/RunCard.tsx` | run-sub {provider}·{model}·turn 1 (stable) + wasStreamingRef timer honesty | VERIFIED | L245: `const turnNumber = 1`; L246-248: run-sub; L121-161: wasStreamingRef + completedAt−startedAt |
| `frontend/src/components/chat/RunCard.timer.test.tsx` | 6 timer tests + retargeted case (f) (turn 1) | VERIFIED | 35/35 RunCard suites green including retargeted case (f) from `turn 3` → `turn 1` |
| `frontend/src/components/chat/MessageItem.tsx` | FinalOutputsPanel flat list + producedDeliverables Resume gate | VERIFIED | Flat map; `producedDeliverables` predicate + `&& !producedDeliverables` in Resume gate |
| `frontend/src/components/chat/OutputFileCard.tsx` | Hero presentation removed; variant inert; dead-state kept | VERIFIED | 30px uniform icon; no glow/gradient/caption; "Download unavailable" preserved |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `workspacePanel.ts` | `stepCount.ts` | `import { dedupToolCalls }` | WIRED | Line 2 |
| `StreamsProvider.tsx` | `workspacePanel.ts` | `import { shouldPopulate, deriveWorkspacePanel }` | WIRED | Lines 99–103 |
| `WorkspacePanel.tsx` | `useDerivedPanel(threadId)` | `hasActivity` gate | WIRED | L42 import + L109 call + L131 `derived.length > 0` in hasActivity |
| `WorkspacePanel.hasActivity` | `TodosSection` mount | panel body renders when true | WIRED | L163 TodosSection inside hasActivity true branch |
| `TodosSection.tsx` | `StreamsProvider.tsx` | `import { useDerivedPanel }` | WIRED | Line 20 |
| `provider_gateway/__init__.py` | `errors.py` | re-exports ErrorKind/classify_provider_error/message_for_kind | WIRED | `__all__` entries |
| `agent_loop.py` | `provider_gateway` | `from app.services.provider_gateway import classify_provider_error, message_for_kind` | WIRED | Line 67; consumed at L2268-2272 |
| `threads.py send_message` | dispatch `{model, provider}` | additive JSONResponse content | WIRED | L1603-1604 |
| `api.ts PostMessageResponse` | `StreamsProvider dispatch` | destructure `resolvedModel/resolvedProvider` | WIRED | L1468-1472 StreamsProvider |
| `StreamsProvider dispatch` | assistant placeholder | stamp `model/provider` on `m.id === assistantId` branch | WIRED | L1511-1518 |
| `threads.py _enrich_messages_with_runs` | `runs` table | additive SELECT model/provider/started_at/completed_at | WIRED | Line 260; 4 null stamps |
| `api.ts mapper` | `types/index.ts Message` | `startedAt`/`completedAt`/`model`/`provider` coercion | WIRED | Lines 117–120 |
| `RunCard.tsx` | stable `turn 1` | `const turnNumber = 1` (decoupled from iterationCount) | WIRED | L245 |
| `MessageItem.tsx` | `message.finalOutputFiles` | `producedDeliverables` predicate gate | WIRED | L228 predicate + L405 gate |

---

## Data-Flow Trace (Level 4)

### WORKSPACE-PARITY panel derivation (SC#1)

| Stage | What flows | Source produces real data |
|-------|-----------|--------------------------|
| `tool_calls` in messages | Persisted DB truth; reconstructed by `_mapMessageResponse` on reload | Real — 095 invariant proven |
| `dedupToolCalls` | Deduped tool call array | Real — reads the persisted array |
| `shouldPopulate` | Gate result (boolean) | Real — computed from actual tool names |
| `deriveWorkspacePanel` | DerivedPanelItem[] with real labels | Real — reads `tc.args.description`/`tc.args.code` |
| `useDerivedPanel` in StreamsProvider | Memoized derivation over stable `Message[]` ref | Real — wraps deriveWorkspacePanel over the chat bucket |
| `WorkspacePanel derived.length > 0` | hasActivity gate signal | Real — reads the hook's already-gated output |
| `TodosSection` DerivedRow | Renders `item.label` as text child | Real — plain string, no innerHTML |

**Status: FLOWING**

### RUN-HONESTY live attribution (SC#5)

| Stage | What flows | Source produces real data |
|-------|-----------|--------------------------|
| `threads.py` `_resolved_model`/`_resolved_provider` | Already-resolved before insert_run | Real — NOT NULL guaranteed at L921-954 |
| Dispatch JSONResponse `model`/`provider` | Additive keys on 201 response | Real — same values runs row holds |
| `PostMessageResponse` destructure in StreamsProvider | `resolvedModel`/`resolvedProvider` | Real — direct destructure, null→undefined |
| Assistant placeholder stamp | `message.model`/`message.provider` live | Real — immediately after dispatch |
| `RunCard runSub` | `{provider} · {model} · turn 1` | Real — reads message.provider/model (live or reload) |
| Reload path: enrich SELECT | `model`, `provider` from runs row | Real — same row dispatch wrote |

**Status: FLOWING (both live and reload paths)**

---

## Behavioral Spot-Checks

Step 7b: live streaming/browser paths skipped (requires uvicorn + Chrome MCP). Backend and frontend unit/integration tests serve as the automated backstop.

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| workspacePanel gate + derive | `vitest run workspacePanel.test.ts` | 31/31 | PASS |
| WorkspacePanel.derived gate→render path | `vitest run WorkspacePanel.derived.test.tsx` | 4/4 | PASS |
| WorkspacePanel + TodosSection + panelHooks suites | `vitest run WorkspacePanel.test.tsx TodosSection.test.tsx panelHooks.test.tsx` | 58/58 | PASS |
| Google 429 → rate_limit (not billing) | `pytest test_errors.py` | 30/30 | PASS |
| Agent-loop wiring guards | `pytest test_errors.py -k wiring` | 3/3 | PASS |
| Backend dispatch attribution | `pytest test_threads.py -k dispatch` | 2/2 | PASS |
| Backend enrich SELECT shape | `pytest test_threads.py -k "enrich or runs"` | 5/5 | PASS |
| RunCard timer honesty + live==reload guard | `vitest run RunCard.test.tsx RunCard.timer.test.tsx` | 35/35 | PASS |
| Flat files + deliverable-aware Resume | `vitest run MessageItem.finalOutputs.test.tsx` | 11/11 | PASS |
| TypeScript baseline | `tsc -b` | 37 errors = documented baseline, zero net-new | PASS |
| Backend import smoke | `python -c "from app.services.provider_gateway import classify_provider_error"` | exit 0 | PASS |
| vite build | `vite build` | exit 0 | PASS |

---

## Cross-Provider Invariant Audit

### threads.py (G-5 hot file)

**Plan 03 delta** (base plans): additive columns in `_enrich_messages_with_runs` SELECT; WHERE byte-identical; no `_emit`/SSE/chunk/provider-branch change.

**Plan 07 delta** (gap-closure): `git diff HEAD~8 HEAD -- backend/app/api/threads.py` = 8 lines (6 comment + 2 data), all inside the `JSONResponse` content block. Zero non-comment code lines match `_emit`/SSE event names/chunk handler/`agent_runner`/provider branches. No IDOR surface widened.

**Verdict: ADDITIVE-ONLY. Provider-agnostic. Invariant HELD.**

### agent_loop.py

The error-catch billing-first keyword ladder fully replaced by `classify_provider_error` + `message_for_kind` (Plan 04). SSE event types (`delta`, `error`), `_emit` signatures, adapters, and the transient-retry path are byte-identical. No adapter touched.

**Verdict: PROVIDER-SCOPED FIX at consumer catch only. Shared SSE path byte-identical. Invariant HELD.**

### StreamsProvider.tsx / WorkspacePanel.tsx

Plan 02 changes: additive `useDerivedPanel` hook — READ-ONLY selector, never writes `todosByThread`. Plan 07 dispatch stamp: additive metadata on the placeholder in the `m.id === assistantId` branch only; no SSE callbacks/chunk path/panel-store change. Plan 06 WorkspacePanel: one boolean OR added to `hasActivity`, one call to the existing hook — no streaming path touched.

**Verdict: PANEL-SIDE PURE READ + ADDITIVE PLACEHOLDER ONLY. Streaming path unchanged. Invariant HELD.**

---

## Requirements Coverage

| Requirement | Plans | Description | Status | Evidence |
|-------------|-------|-------------|--------|---------|
| WORKSPACE-PARITY | 01, 02, 05, 06 | Deterministic panel fill (D-01/02) + flat files list (D-06) + GAP-1 hasActivity gate fix | SATISFIED | workspacePanel.ts + useDerivedPanel + TodosSection + WorkspacePanel.hasActivity (gap-closed) + FinalOutputsPanel flatten |
| RUN-HONESTY | 03, 05, 07 | Model attribution (D-04) + true timer (D-05) + deliverable-aware Resume (D-07) + GAP-2 live attribution + turn stable | SATISFIED | threads.py additive dispatch + additive enrich SELECT + StreamsProvider placeholder stamp + RunCard run-sub + wasStreamingRef timer + producedDeliverables gate |
| PROVIDER-ERR | 01, 04 | Gateway-boundary error classification (D-03), Google 429 → rate_limit | SATISFIED | errors.py classify_provider_error + message_for_kind + agent_loop wiring at L2271 |

---

## Anti-Patterns Found

No stubs, placeholders, or regression-introducing patterns found across the 12+ touched source files.

| Finding | Severity | Assessment |
|---------|----------|------------|
| `const turnNumber = 1` hardcoded constant (IN-01 from code review) | INFO only | Intentional per operator decision (live==reload was the goal; the reload value is the truth; `_mapMessageResponse` omits `iterationCount` on reload so `turn 1` is the reload-truth). Comment block at L235-244 documents the rationale. Not a stub. |
| Gate and body both call `useDerivedPanel(threadId)` (IN-02 from code review) | INFO only | Both source `threadId` from `useViewingThread()` — same source, no divergence possible. `useDerivedPanel` is a pure `useMemo` returning the stable `EMPTY_DERIVED` ref when ungated. No churn risk. Not a stub. |

No blocker or warning patterns found.

---

## Human Verification Required

### 1. All-8-provider cross-provider + 4-axis UAT (SC#7 / SC#10)

**Test:** For each of the 8 providers (openai, anthropic, google, deepseek, moonshot, glm/zhipu, minimax, openrouter), run a genuine multi-step prompt (≥2 meaningful tools) and verify the workspace todos panel fills with activity-derived rows + the "derived from activity" marker. Then run a one-shot Q&A and verify the panel stays calm (PanelEmpty).
**Expected:** Every provider's panel reflects the real tasks it performed. The parity bar is the OpenAI gpt-5.4-mini run showing "print the numbers / create the CSV / make the chart". The Todos section header shows NO badge (not "0/0") when derived-only.
**Why human:** Real LLM streams required; panel fill cannot be asserted in Vitest against a fixture because the live stream is the data source.

### 2. Live attribution — live moment + reload identity (GAP-2 confirmation)

**Test:** Send a fresh prompt on 2+ providers (e.g. OpenAI + Anthropic). Watch the run finish WITHOUT reloading. Note the run-sub text. Then reload and observe the same run.
**Expected:** Run-sub immediately reads `{provider} · {model} · turn 1` in the live moment (not a bare `turn 4`/`turn 6`). After reload, the IDENTICAL string appears — same provider·model, same `turn 1`. Thread-switch-away-and-back also identical.
**Why human:** Live dispatch stamping requires real provider round-trip; the unit test proves the mechanism fires but a real run confirms the wiring end-to-end.

### 3. Multi-tool derived items

**Test:** A prompt exercising `search_documents` + `execute_code` in one run on any provider.
**Expected:** Derived panel shows both items with semantic labels (e.g. "Search documents" + the description or code-inferred label); neither labeled "Run code".
**Why human:** Requires live tool dispatch and panel observation.

### 4. Flat file list visual (no crown)

**Test:** A run that produces multiple output files.
**Expected:** ONE flat "Generated files" section; all files shown as equal rows; no "Your file" header or caption; all present with download links (or "Download unavailable" if no URL).
**Why human:** Visual confirmation required.

### 5. Parallel-thread timer/attribution isolation

**Test:** Thread A streaming (live-ticking timer) while Thread B accepts a new prompt.
**Expected:** Thread A's timer and run-sub are unaffected by Thread B's activity; neither shows stale or corrupted attribution or timer.
**Why human:** Requires two concurrent live runs.

### 6. True reload duration (long-message / day-old reopen)

**Test:** Reopen a day-old completed run on any provider. Observe the timer. Also run with a ≥50-message or ≥5 KB prompt and reload.
**Expected:** Timer shows `completedAt − startedAt` (typically seconds/minutes), never "1440m" (BUG-260606-02 guard).
**Why human:** Day-old DB data required; live clock delta test not automatable.

### 7. Deliverable-aware Resume

**Test:** Engineer a run that produces files (execute_code output) then fails/times out. Separately, engineer a run that fails with NO output files.
**Expected:** First case → NO Resume button. Second case → Resume button present.
**Why human:** Requires triggering a real failed run with/without deliverables.

### 8. 429 truthful error (Google over-quota)

**Test:** Force a real 429 from Google (trigger quota exhaustion or use a rate-limited key).
**Expected:** Error message reads as rate-limit-with-retry ("Rate limited by the provider — please retry in a moment."), NOT "billing / insufficient credits".
**Why human:** Real provider 429 required; unit test proves the mechanism, live confirmation proves the wiring fires end-to-end.

---

## Statically Proven (This Report)

- `WorkspacePanel.hasActivity` includes `derived.length > 0` via the EXISTING `useDerivedPanel` hook — the derivation is structurally reachable (GAP-1 CLOSED).
- The guard test `WorkspacePanel.derived.test.tsx` exercises the real gate→TodosSection→derived-render path that the prior unit tests bypassed — 4/4 green.
- The dispatch JSONResponse carries additive `model`/`provider` equal to the already-resolved values written to the runs row — no re-resolution (GAP-2 CLOSED).
- StreamsProvider stamps `resolvedModel`/`resolvedProvider` onto the assistant placeholder immediately, so attribution is visible in the live moment.
- `RunCard` run-sub turn is a stable `1` — decoupled from `iterationCount` — so live and reload produce the identical string by construction.
- The decisive live==reload guard test confirms byte-equal run-sub strings for a live-shaped and reload-shaped message of the same run — 35/35 RunCard suites green.
- `classify_provider_error("google", exc_with_code_429)` returns `"rate_limit"` — not `"billing"`.
- `message_for_kind("rate_limit")` never contains the word "billing".
- The billing-first keyword ladder in `agent_loop.py` is fully replaced.
- `threads.py` diff across all gap-closure commits = 8 lines in the JSONResponse content block only — no `_emit`, no SSE event vocabulary, no chunk handler, no adapter touched.
- `RunCard.tsx` and `WorkspacePanel.tsx`: `dangerouslySetInnerHTML` count == 0.
- All SSE event types, `_emit` signatures, and the transient-retry path are byte-identical across all 8 providers.

## Requires Live `/gsd:verify-work 095.1` UAT (SC#7 / SC#10 4-axis)

Per VALIDATION.md and SC#10, the following require a real browser with Chrome-MCP:

1. Cross-provider panel fill/clean (all 8 providers) — real LLM streams required.
2. Live attribution in the live moment + reload identity — real dispatch round-trip required.
3. Multi-tool derived items — live tool dispatch required.
4. Flat file list visual — visual confirmation required.
5. Parallel-thread timer/attribution isolation — two concurrent live runs required.
6. True reload duration (≥50-msg / ≥5 KB / day-old) — day-old DB data required.
7. Deliverable-aware Resume — real failed run with/without deliverables required.
8. 429 truthful error — real provider quota exhaustion required.

---

## Gaps Summary

No blocking gaps. All 6 code-deliverable success criteria (SC#1–SC#6) are verified. Both previously identified gaps (GAP-1 derived-panel unreachable; GAP-2 live attribution lag + turn divergence) are CLOSED at the unit/static level with TDD evidence. SC#7 is manual-only by design (live Chrome-MCP UAT per VALIDATION.md). No anti-patterns or stubs found. The phase is structurally complete and ready for `/gsd:verify-work 095.1`.

---

_Verified: 2026-06-06T20:00:00Z_
_Verifier: Claude (gsd-verifier)_
_Mode: Re-verification (post-gap-closure plans 06 + 07)_
