---
phase: 095.1
slug: cross-provider-run-honesty-workspace-parity
status: planned
nyquist_compliant: true
wave_0_complete: false  # Plan 01 lands the Wave-0 scaffolds
created: 2026-06-06
---

# Phase 095.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: 095.1-RESEARCH.md §"Validation Architecture". Per-task map is a scaffold — the planner binds rows to real task IDs.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework (frontend)** | Vitest + React Testing Library (existing — e.g. `MessageItem.finalOutputs` 7/7, `TodosSection.test.tsx`, `MessageList` 9/9) |
| **Framework (backend)** | pytest (existing — `test_093_*`, live-DB suites) |
| **Frontend quick run** | `cd frontend && npx vitest run <file>` |
| **Frontend full suite** | `cd frontend && npx vitest run` (documented baseline: 17 failed / 497 passed — the 7-file pre-existing cluster; **ZERO net-new** is the bar) |
| **Backend quick run** | `cd backend && venv/Scripts/python -m pytest <path> -x` |
| **Typecheck / build** | `cd frontend && npx tsc -b` (baseline 37, zero net-new) + `npx vite build` |
| **Estimated runtime** | frontend full ~40s; backend touched-suite ~15s |

---

## Sampling Rate

- **After every task commit:** Run the touched test file (`npx vitest run <file>` or `pytest <path> -x`) + `tsc -b` zero-net-new.
- **After every plan wave:** frontend full vitest (zero net-new vs the 7-file baseline) + backend touched-suite + `vite build`.
- **Before `/gsd:verify-work`:** Full suites green-modulo-baseline; THEN the SC#7 all-8-provider LIVE Chrome-MCP UAT.
- **Max feedback latency:** ~40 seconds.

---

## Per-Task Verification Map

> Bound to real task IDs at plan-phase (2026-06-06). Every task maps to an `<automated>` verify; Wave-0 (Plan 01) lands the net-new selector+classifier + their tests so Wave-1 verifies against real tests.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 095.1-01-1 | 01 | 0 | WORKSPACE-PARITY | T-095.1-01-01 | derived labels = plain strings (no innerHTML) | unit | `cd frontend && npx vitest run src/lib/__tests__/workspacePanel.test.ts` | ❌→W0 | ⬜ pending |
| 095.1-01-2 | 01 | 0 | PROVIDER-ERR | T-095.1-01-02/03 | bounded raw detail; no import cycle | unit | `cd backend && venv/Scripts/python -m pytest app/services/provider_gateway/test_errors.py -x` | ❌→W0 | ⬜ pending |
| 095.1-02-1 | 02 | 1 | WORKSPACE-PARITY | T-095.1-02-02 | read-only selector; PANEL-06 isolation | render | `cd frontend && npx vitest run src/providers/__tests__/panelHooks.test.tsx` | ✅ extend | ⬜ pending |
| 095.1-02-2 | 02 | 1 | WORKSPACE-PARITY | T-095.1-02-01/03 | text-children-only; real-todos precedence | render | `cd frontend && npx vitest run src/components/panel/__tests__/TodosSection.test.tsx` | ✅ extend | ⬜ pending |
| 095.1-03-1 | 03 | 1 | RUN-HONESTY | T-095.1-03-02 | additive SELECT, same user_id WHERE (no IDOR) | unit (be) | `cd backend && venv/Scripts/python -m pytest tests/integration/test_threads.py -x -k "GetMessages or enrich or runs"` | ✅ extend | ⬜ pending |
| 095.1-03-2 | 03 | 1 | RUN-HONESTY | T-095.1-03-01/03 | text-children run-sub; no fabricated duration | render (timer) | `cd frontend && npx vitest run src/components/chat/RunCard.timer.test.tsx src/components/chat/RunCard.test.tsx` | ❌→co-located new | ⬜ pending |
| 095.1-04-1 | 04 | 1 | PROVIDER-ERR | T-095.1-04-01/02/03 | structured classify; byte-identical emit/raise | unit | `cd backend && venv/Scripts/python -m pytest app/services/provider_gateway/test_errors.py -x` | ✅ (Plan 01) | ⬜ pending |
| 095.1-05-1 | 05 | 1 | WORKSPACE-PARITY | T-095.1-05-01/03 | text-children files; keep dead-link affordance | render | `cd frontend && npx vitest run src/__tests__/components/MessageItem.finalOutputs.test.tsx` | ✅ update | ⬜ pending |
| 095.1-05-2 | 05 | 1 | RUN-HONESTY | T-095.1-05-02 | deliverable-aware Resume gate | render | `cd frontend && npx vitest run src/__tests__/components/MessageItem.finalOutputs.test.tsx src/__tests__/components/MessageItem.test.tsx` | ✅ extend | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

### Requirements → Test surfaces (from RESEARCH §Validation Architecture)

| Req | Behavior under test | Test type | File |
|-----|---------------------|-----------|------|
| WORKSPACE-PARITY (D-01/02 gate) | simple-qa / single-search / 0-tool → clean; ≥2 meaningful OR `write_todos` → populates; anthropic-no-`write_todos` → semantic items not "Run code" | unit (selector over fixtures) | `frontend/src/lib/__tests__/workspacePanel.test.ts` (❌ Wave 0) |
| WORKSPACE-PARITY (D-02 labels) | label precedence + infer_label cases (print/csv/chart/leading-comment/generic) | unit | same file (❌ Wave 0) |
| WORKSPACE-PARITY (D-01 precedence) | real `write_todos` honored over derivation; derived items read-only | render | `TodosSection.test.tsx` (✅ extend) |
| WORKSPACE-PARITY (D-06) | one flat list, no hero caption, no Working group, all downloadable, url-less → "Download unavailable" | render | `MessageItem.finalOutputs…` (✅ update assertions) |
| RUN-HONESTY (D-04) | enrich SELECT adds 4 cols + stamps; mapper snake→camel/null→undefined; RunCard shows `{provider} · {model} · turn N` / graceful | unit (be) + unit (fe) + render | `tests/integration/test_threads.py` (✅ extend — Plan 03 Task 1) + api mapper + RunCard run-sub (Plan 03 Task 2) |
| RUN-HONESTY (D-05) | terminal → `completedAt − startedAt`; no completedAt → no duration; streaming → live tick; day-old `created_at` does NOT inflate | render (timer) | `RunCard.timer.test.tsx` (❌ Wave 0) |
| RUN-HONESTY (D-07) | failed+deliverables → no Resume; failed+no-deliverables → Resume; completed/cancelled/streaming unchanged | render | `MessageItem*.test.tsx` (✅ extend) |
| PROVIDER-ERR (D-03) | Google 429/RESOURCE_EXHAUSTED → rate_limit (NOT billing); anthropic/openai 429→rate_limit, 401→auth, insufficient_quota→billing, 400→bad_request, 5xx→server, unknown→neutral; message never says "billing" for a 429 | unit (classifier matrix) | `backend/app/services/provider_gateway/test_errors.py` (❌ Wave 0) |

---

## Wave 0 Requirements

- [ ] `frontend/src/lib/workspacePanel.ts` — the derived-panel selector module under test (new source; ports spike-006/007)
- [ ] `frontend/src/lib/__tests__/workspacePanel.test.ts` — D-01/02 gate + derive + label precedence (port spike fixtures)
- [ ] `backend/app/services/provider_gateway/errors.py` + `test_errors.py` — the `classify_provider_error` helper + its status-code matrix (D-03)
- [ ] Extend `backend/tests/integration/test_threads.py` (TestGetMessages) — the enrich SELECT shape + per-message stamp (D-04/05) [Plan 03 Task 1 — not Wave 0; fixtures already wired]
- [ ] `frontend/src/components/chat/__tests__/RunCard.timer.test.tsx` — D-05 timer honesty (terminal vs streaming vs no-completedAt vs day-old-no-inflate)
- [ ] Extend `MessageItem.finalOutputs` (D-06 flat list) + `MessageItem` Resume test (D-07) + `TodosSection` (D-01 precedence/read-only) + the api mapper test (D-04 fields)

---

## Manual-Only Verifications

> SC#7 — the all-8-provider LIVE Chrome-MCP UAT (per the MANDATORY SC#10 4-axis recipe). Each fix needs LIVE confirmation across openai, anthropic, google, deepseek, moonshot, glm/zhipu, minimax, openrouter (OpenRouter/Ollama = best-effort, structured mode). Authored in full here; executed at `/gsd:verify-work`.

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Cross-provider panel fill/clean | WORKSPACE-PARITY (D-01/02) | Real LLM streams; can't fixture true provider behavior | On each provider: a genuine multi-step prompt (≥2 meaningful tools) → panel fills with REAL tasks; a one-shot Q&A → panel stays clean |
| Multi-tool derived items | WORKSPACE-PARITY (D-01/02) | Live 2+ tool exercise | A prompt exercising `search_documents` + `execute_code` → derived panel shows both as semantic items |
| Flat file list visual | WORKSPACE-PARITY (D-06) | Visual confirm of no crown | A multi-file run → ONE flat "Generated files" list, no "★ Your file", all downloadable |
| Parallel-thread timer/attribution | RUN-HONESTY (D-04/05) | Two live threads | Thread A streaming (live-ticking timer) while Thread B accepts a prompt → neither run-sub/timer corrupts the other |
| True reload duration (≥50-msg / ≥5 KB) | RUN-HONESTY (D-05) | Day-old reopen + long-thread | Reopen a day-old completed run → timer shows TRUE `completedAt − startedAt`, never inflated (BUG-260606-02 guard) |
| Model attribution at rest | RUN-HONESTY (D-04) | Visual confirm per provider | Each provider response → run-sub shows the real `{provider} · {model}` without expanding |
| Deliverable-aware Resume | RUN-HONESTY (D-07) | Live successful-then-failed run | A run that produced files then failed/timed_out → NO Resume; a run that failed with no deliverables → Resume present |
| 429 truthful error | PROVIDER-ERR (D-03) | Force a real rate-limit | Force a 429 (e.g. Google over quota) → reads as rate-limit-with-retry, NOT "billing / insufficient credits" |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: every task has an automated verify (no 3-consecutive gap)
- [x] Wave 0 (Plan 01) covers the net-new selector + classifier + their tests
- [x] No watch-mode flags (all vitest run / pytest -x)
- [x] Feedback latency < 40s (touched-file runs)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** bound at plan-phase 2026-06-06 (task IDs 095.1-01-1 .. 095.1-05-2); SC#7 all-8-provider manual UAT authored below, executed at /gsd:verify-work
