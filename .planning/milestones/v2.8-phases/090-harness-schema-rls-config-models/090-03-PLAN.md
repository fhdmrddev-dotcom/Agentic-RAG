---
phase: 090-harness-schema-rls-config-models
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - supabase/full-schema.sql
autonomous: false
requirements: [HARNESS-02, HARNESS-06]
nyquist_compliant: true

must_haves:
  truths:
    - "All four new migrations (056→059) are applied to the live local Supabase via the SQL editor, in order"
    - "supabase/full-schema.sql is regenerated (no-reset live dump) and reflects the 4 new tables + threads.active_workflow_run_id"
    - "verify_090.sql blocks A–F all confirm expected results against the live DB (immutability raise 23514, DELETE RESTRICT 23503, UNIQUE 23505, cross-user 0 rows, INSERT-only policies, presence/absence introspection)"
  artifacts:
    - path: "supabase/full-schema.sql"
      provides: "Regenerated single-file deploy artifact reflecting migrations 056–059"
      contains: "workflow_definitions"
  key_links:
    - from: "migrations 056-059"
      to: "live local Supabase DB"
      via: "manual paste into SQL editor in order"
      pattern: "workflow_definitions|workflow_runs|workflow_phases|harness_audit"
    - from: "verify_090.sql"
      to: "live DB objects"
      via: "SQL-editor run confirming all 6 blocks"
      pattern: "information_schema|pg_policies"
---

<objective>
Apply migrations 056–059 to the live local Supabase BY HAND (paste into the SQL editor, in order — never `db push`/`db reset`, per CLAUDE.md), regenerate `supabase/full-schema.sql` via the no-reset live dump, then run `supabase/verify_090.sql` against the live DB to confirm the success criteria that pytest cannot verify (RLS denial, immutability refusal, DELETE RESTRICT, UNIQUE collision, INSERT-only policy presence, table/column introspection). This is the `autonomous: false` human-run verification gate for the phase.

Purpose: SC#1/#2/#3/#4 + HARNESS-06 cannot pass until the migrations are LIVE in the DB — the pytest suite mocks Supabase entirely (no live-DB fixture). This plan closes that gap with the operator-run SQL-editor flow.
Output: regenerated `supabase/full-schema.sql`; a documented pass/fail result for each verify_090.sql block (recorded in the SUMMARY).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/090-harness-schema-rls-config-models/090-VALIDATION.md
@CLAUDE.md
@.planning/phases/090-harness-schema-rls-config-models/090-01-PLAN.md
@.planning/phases/090-harness-schema-rls-config-models/090-02-PLAN.md

<interfaces>
<!-- Apply + regen process from CLAUDE.md § schema-changes (verbatim, MANDATORY). -->
- Apply: paste each migration file into the Supabase SQL editor in order 056 → 057 → 058 → 059. NEVER `supabase db push` / `db reset` (wipes dev data).
  (057 must precede 059 because threads.active_workflow_run_id FKs workflow_runs — sequential apply handles this.)
- Regen: `bash scripts/regenerate-full-schema.sh` (defaults to no-reset live dump; rebuilds supabase/full-schema.sql). NEVER hand-edit full-schema.sql. Pass --reset ONLY for clean-slate CI verification (destructive — do NOT use here).
- Verify: paste supabase/verify_090.sql blocks A–F into the SQL editor against the live local DB; confirm each EXPECT.
- The user starts/operates their own infra in a visible terminal (project memory) — give plain-language step-by-step instructions and WAIT for confirmation.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Apply migrations 056–059 + regenerate full-schema.sql</name>
  <what-built>Plan 02 authored four migration files (056_workflow_definitions, 057_workflow_runs, 058_workflow_phases, 059_harness_audit_and_threads_col). They are NOT yet in the live DB — CLAUDE.md forbids `supabase db push`/`db reset` (would wipe your dev data), so they must be applied by hand via the Supabase SQL editor.</what-built>
  <read_first>
    - CLAUDE.md § schema-changes rule (apply-via-SQL-editor + regenerate, the MANDATORY process)
    - supabase/migrations/056_workflow_definitions.sql, 057_workflow_runs.sql, 058_workflow_phases.sql, 059_harness_audit_and_threads_col.sql (the files to apply)
    - .planning/phases/090-harness-schema-rls-config-models/090-VALIDATION.md § Manual-Only Verifications
  </read_first>
  <how-to-verify>
    Plain-language steps (do these in your own terminal / browser — I will wait):
    1. Make sure local Supabase is running (`supabase status` should show the API + DB up). If not, `supabase start`.
    2. Open the Supabase Studio SQL editor for your LOCAL project (http://localhost:54323 → SQL Editor).
    3. Paste the FULL contents of `supabase/migrations/056_workflow_definitions.sql` and run it. Confirm "Success". (This also inserts the one global seed row.)
    4. Repeat IN ORDER for `057_workflow_runs.sql`, then `058_workflow_phases.sql`, then `059_harness_audit_and_threads_col.sql`. Order matters — 059 references workflow_runs created in 057.
    5. Back in your terminal at the repo root, run: `bash scripts/regenerate-full-schema.sh` (this does a no-reset live dump — it will NOT wipe data). Confirm it finishes and `supabase/full-schema.sql` now contains `workflow_definitions`.
    6. Tell me "applied" (or paste any error you hit on a specific migration).
  </how-to-verify>
  <action>
    This is a human-action checkpoint — Claude CANNOT run the SQL editor or `supabase db push`. After the user confirms "applied", Claude verifies the regen landed by grepping supabase/full-schema.sql for the four new table names + active_workflow_run_id, and confirms `deep_mode_metadata` is ABSENT. If a migration errored, Claude reads the error, fixes the offending migration file in Plan 02's output, and asks the user to re-run that single file.
  </action>
  <acceptance_criteria>
    - User confirms all four migrations ran "Success" in the SQL editor, in order
    - `supabase/full-schema.sql` contains workflow_definitions, workflow_runs, workflow_phases, harness_audit
    - `supabase/full-schema.sql` contains `active_workflow_run_id` and does NOT contain `deep_mode_metadata`
    - regenerate-full-schema.sh was run with NO --reset (no data wipe)
  </acceptance_criteria>
  <resume-signal>Type "applied" once all four migrations ran successfully and full-schema.sql is regenerated, or paste the error from the migration that failed.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Run verify_090.sql live-DB gate (SC#1/#2/#3/#4 + HARNESS-06)</name>
  <what-built>Plan 01 authored `supabase/verify_090.sql` — a 6-block live-DB verification script. It proves the success criteria pytest cannot (the suite mocks Supabase). Now that the tables are live, run it.</what-built>
  <read_first>
    - supabase/verify_090.sql (the 6-block script — A immutability, B DELETE RESTRICT, C UNIQUE, D cross-user RLS, E INSERT-only, F presence/absence)
    - .planning/phases/090-harness-schema-rls-config-models/090-VALIDATION.md § Per-Task Verification Map (expected results per block)
  </read_first>
  <how-to-verify>
    In the Supabase SQL editor (local), run `supabase/verify_090.sql` block by block and confirm each EXPECT:
    1. Block A — publish a definition then UPDATE it: EXPECT the trigger to raise (error code 23514 / "published and immutable"). The draft→published UPDATE itself should SUCCEED first.
    2. Block B — insert a run referencing a definition, then DELETE the definition: EXPECT a foreign-key violation (23503).
    3. Block C — insert two definitions with the same (slug, version): EXPECT a unique violation (23505).
    4. Block D — using two user contexts, SELECT user A's workflow_runs/phases/audit as user B: EXPECT 0 rows.
    5. Block E — query pg_policies for harness_audit: EXPECT only a SELECT and an INSERT policy (no UPDATE/DELETE); attempting an UPDATE returns 0 rows affected / denied.
    6. Block F — introspection: EXPECT all 4 tables present, threads.active_workflow_run_id present, threads.deep_mode_metadata ABSENT, and org_id nullable on all 4 tables.
    Tell me the result of each block (pass / fail + any unexpected output). If a block fails, paste the actual error/output.
  </how-to-verify>
  <action>
    Human-verify checkpoint — the operator runs the SQL against the live DB (Claude cannot open a live-DB session; conftest mocks Supabase). Claude records each block's pass/fail in the SUMMARY. If a block FAILS, Claude diagnoses against the corresponding migration (056 trigger for A, 057 RESTRICT FK for B, 056 UNIQUE for C, 057/058 RLS for D, 059 policies for E, 056–059 columns for F), proposes a fix to the Plan-02 migration file, and re-runs the apply→verify loop for the affected file. The phase gate is: full pytest GREEN (Plan 01) AND all 6 blocks confirmed.
  </action>
  <acceptance_criteria>
    - Block A: post-publish UPDATE raises 23514 (immutability) — HARNESS-02
    - Block B: DELETE of a referenced definition raises 23503 (RESTRICT) — HARNESS-02 / SC#2
    - Block C: duplicate (slug,version) raises 23505 — HARNESS-02
    - Block D: cross-user SELECT returns 0 rows on runs/phases/audit — SC#3
    - Block E: harness_audit shows only SELECT+INSERT policies; UPDATE denied — HARNESS-06
    - Block F: 4 tables present, active_workflow_run_id present, deep_mode_metadata absent, org_id nullable on all 4 — SC#1 / SC#4
  </acceptance_criteria>
  <resume-signal>Report the pass/fail of each of the 6 blocks (A–F). Type "all green" if every block matched its EXPECT, or paste the failing block's actual output.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored migration → live DB | A migration only enforces its security controls (RLS, trigger, RESTRICT, INSERT-only) once actually applied; an authored-but-unapplied migration is a false sense of security (RESEARCH Pitfall 2). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-090-07 | Tampering / Information Disclosure (V4) | controls authored but never applied to the live DB | mitigate | The [BLOCKING] apply checkpoint + verify_090.sql gate ensure the RLS/immutability/RESTRICT/INSERT-only controls are confirmed LIVE — Block D (cross-user RLS), A/B (immutability+RESTRICT), E (INSERT-only) all run against the real DB. The phase does not pass on file-presence alone. |
</threat_model>

<verification>
- Full `pytest` suite GREEN (Plan 01's model test + no import breakage).
- `supabase/full-schema.sql` regenerated, contains the 4 tables + active_workflow_run_id, lacks deep_mode_metadata.
- verify_090.sql blocks A–F all confirmed against the live local Supabase.
</verification>

<success_criteria>
- SC#1: migrations applied; full-schema.sql regenerated reflecting the 4 tables + threads column (Block F).
- SC#2 / HARNESS-02: immutability raise (Block A), DELETE RESTRICT (Block B), UNIQUE (Block C) confirmed live.
- SC#3: cross-user RLS denial confirmed live (Block D); INSERT-only harness_audit confirmed (Block E).
- SC#4: deep_mode_metadata absent, org_id nullable on all 4 tables (Block F).
- HARNESS-06: harness_audit INSERT-only policy set confirmed live (Block E).
</success_criteria>

<output>
After completion, create `.planning/phases/090-harness-schema-rls-config-models/090-03-SUMMARY.md`
</output>
