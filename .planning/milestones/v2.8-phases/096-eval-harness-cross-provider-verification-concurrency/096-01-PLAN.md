---
phase: 096-eval-harness-cross-provider-verification-concurrency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/harness/programmatic.py
  - supabase/migrations/066_eval_coverage_seed.sql
  - supabase/full-schema.sql
autonomous: false
requirements: [EVAL-01, EVAL-02]
tags: [harness, eval, migration, seed-workflow]

must_haves:
  truths:
    - "A published global workflow 'eval_coverage' exists in the live local DB touching all 5 phase types (programmatic -> llm_batch_agents -> llm_agent -> llm_human_input -> llm_single)"
    - "The workflow's programmatic phase takes ~20s, giving the operator a humanly-possible mid-programmatic kill window (D-08 / SC#4)"
    - "Migration applied via SQL editor (never db push) and full-schema.sql regenerated and committed"
  artifacts:
    - path: "supabase/migrations/066_eval_coverage_seed.sql"
      provides: "eval_coverage 5-type seed workflow (D-02 max-coverage workflow)"
      contains: "eval_coverage"
    - path: "backend/app/services/harness/programmatic.py"
      provides: "eval_slow_step registered programmatic fn (pure, idempotent, ~20s sleep)"
      contains: "eval_slow_step"
  key_links:
    - from: "supabase/migrations/066_eval_coverage_seed.sql"
      to: "backend/app/services/harness/programmatic.py"
      via: "definition phase 0 config.fn = 'eval_slow_step' resolved from PROGRAMMATIC_PHASE_REGISTRY"
      pattern: "eval_slow_step"
    - from: "supabase/migrations/066_eval_coverage_seed.sql"
      to: "live local DB workflow_definitions"
      via: "operator SQL-editor paste (BLOCKING checkpoint)"
      pattern: "00000000-0000-0000-0000-0000000000c1"
---

<objective>
Create the D-02 max-coverage eval seed: a new `eval_slow_step` programmatic fn (the only humanly-killable mid-`programmatic` window — RESEARCH Pitfall 4) plus migration 066 seeding the published `eval_coverage` workflow that touches all 5 phase types. Apply it to the live DB via the operator (BLOCKING). This one definition serves EVAL-01 (the eval drives it per provider), D-02a (its `llm_human_input` phase is the robot-watched ask_user round-trip), and D-08 (one canonical restart-smoke target with all 3 kill points).

Purpose: no existing seed covers all 5 phase types (VERIFIED: migration 061 — literature_review covers programmatic+batch+single; doc_qa_human covers agent+human_input+single), and there is no workflow-authoring API — only a migration can ship this.
Output: `eval_slow_step` in the closed programmatic registry; `supabase/migrations/066_eval_coverage_seed.sql`; regenerated `supabase/full-schema.sql`; seed row live in local DB.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/096-eval-harness-cross-provider-verification-concurrency/096-CONTEXT.md (D-02, D-02a, D-08)
@.planning/phases/096-eval-harness-cross-provider-verification-concurrency/096-PATTERNS.md (Assignments 7 + 8)
@supabase/migrations/061_harness_seed_templates.sql (structural analog: seed-user block, definition row shape, JSONB phase configs)
@backend/app/services/harness/programmatic.py (registry + split_topic analog)

<interfaces>
From backend/app/services/harness/programmatic.py (verified 2026-06-07):
```python
PROGRAMMATIC_PHASE_REGISTRY: dict[str, Callable[[dict, object], Awaitable[dict]]] = {}
__all__ = ["PROGRAMMATIC_PHASE_REGISTRY", "register_programmatic", "split_topic"]

@register_programmatic("split_topic")
async def split_topic(input: dict, ctx: object) -> dict:
    # reads input.get("topic") or input.get("kickoff_prompt")
    # returns {"sub_questions": [str, ...]}
```
From migration 061 (row shape to copy):
```sql
INSERT INTO public.workflow_definitions (id, slug, version, name, status, definition, created_by, is_global)
VALUES ('<fixed uuid>', '<slug>', 1, '<name>', 'published', '{...}'::jsonb,
        '00000000-0000-0000-0000-000000000001', true)
ON CONFLICT (id) DO NOTHING;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add eval_slow_step programmatic fn (slow, pure, idempotent)</name>
  <files>backend/app/services/harness/programmatic.py</files>
  <read_first>
    - backend/app/services/harness/programmatic.py (full file — registry contract, split_topic docstring conventions, __all__)
    - .planning/phases/096-eval-harness-cross-provider-verification-concurrency/096-PATTERNS.md (Assignment 8)
  </read_first>
  <action>
    Add a new registered programmatic fn `eval_slow_step` to `backend/app/services/harness/programmatic.py`, directly below `split_topic`:

    ```python
    # Mid-`programmatic` kill window for the 096 restart smoke (D-08 / SC#4):
    # split_topic completes in microseconds — no human can land a uvicorn kill
    # inside its 2-phase-write window. eval_slow_step holds the phase `active`
    # for EVAL_SLOW_STEP_SECONDS so the operator-driven kill is real, never faked.
    EVAL_SLOW_STEP_SECONDS = 20

    @register_programmatic("eval_slow_step")
    async def eval_slow_step(input: dict, ctx: object) -> dict:
        """split_topic semantics with a deliberate ~20s sleep.

        PURE + IDEMPOTENT (Pattern 3): the sleep is not a side effect; output is
        deterministic for the same input, so re-running on resume is safe.
        Referenced ONLY by the eval_coverage seed workflow (migration 066).
        """
        await asyncio.sleep(EVAL_SLOW_STEP_SECONDS)
        return await split_topic(input, ctx)
    ```

    Concrete requirements:
    - Add `import asyncio` to the module imports (currently absent — verify; only `re` and typing are imported).
    - Delegate to `split_topic(input, ctx)` for the output shape — do NOT duplicate the clause-split logic. Output stays `{"sub_questions": [...]}` (the shape `llm_batch_agents` reads).
    - Extend `__all__` to `["PROGRAMMATIC_PHASE_REGISTRY", "register_programmatic", "split_topic", "eval_slow_step"]`.
    - Do NOT modify split_topic or the registry/decorator.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && venv/Scripts/python.exe -c "import asyncio; from app.services.harness.programmatic import PROGRAMMATIC_PHASE_REGISTRY, eval_slow_step; assert 'eval_slow_step' in PROGRAMMATIC_PHASE_REGISTRY; assert 'split_topic' in PROGRAMMATIC_PHASE_REGISTRY; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "register_programmatic(\"eval_slow_step\")" backend/app/services/harness/programmatic.py` == 1
    - `grep -c "EVAL_SLOW_STEP_SECONDS = 20" backend/app/services/harness/programmatic.py` == 1
    - `grep -c "eval_slow_step" backend/app/services/harness/programmatic.py` >= 3 (constant comment, decorator, __all__)
    - `grep -c "import asyncio" backend/app/services/harness/programmatic.py` == 1
    - split_topic's body is byte-identical (git diff shows only additions below it + the import + __all__ line)
    - The python -c registry import check above prints OK
  </acceptance_criteria>
  <done>eval_slow_step is registered in the closed registry, delegates to split_topic, sleeps 20s, and is exported via __all__.</done>
</task>

<task type="auto">
  <name>Task 2: Author migration 066 — eval_coverage 5-type seed workflow</name>
  <files>supabase/migrations/066_eval_coverage_seed.sql</files>
  <read_first>
    - supabase/migrations/061_harness_seed_templates.sql (FULL file — seed-user block :33-43, llm_agent phase block in seed 1 :58-67, llm_human_input block in seed 4 :216-223, literature_review programmatic+batch composition :143-189, header discipline :24-27)
    - supabase/migrations/065_harness_seed_fixes.sql (input_keys lesson — programmatic fn only receives keys listed in config.input_keys)
    - .planning/phases/096-eval-harness-cross-provider-verification-concurrency/096-PATTERNS.md (Assignment 7)
  </read_first>
  <action>
    Create `supabase/migrations/066_eval_coverage_seed.sql` (066 verified as the next free number — 065 is the latest on disk). Copy migration 061's structure exactly:

    1. **Header comment** (copy 061's discipline): apply by pasting into the Supabase SQL editor — never `db push`/`db reset` — then `bash scripts/regenerate-full-schema.sh` (no reset) and commit both.
    2. **Idempotent seed-user block** — copy 061:33-43 verbatim (`INSERT INTO auth.users ... VALUES ('00000000-0000-0000-0000-000000000001', 'seed@system.local', ...) ON CONFLICT (id) DO NOTHING;`).
    3. **One workflow_definitions row** with FIXED uuid `00000000-0000-0000-0000-0000000000c1` (grep all migrations first to confirm no collision), slug `eval_coverage`, version 1, name `Eval coverage (5-type)`, status `published`, created_by the seed user, is_global `true`, ON CONFLICT (id) DO NOTHING. The `definition` JSONB carries `slug`/`version`/`name`/`status` mirrors plus exactly 5 phases:

    - phase_index 0, slug `split`, phase_type `programmatic`: `{"phase_type": "programmatic", "fn": "eval_slow_step", "input_keys": ["topic", "kickoff_prompt"]}` — the migration-065 lesson: include BOTH keys so live runs (which carry `kickoff_prompt`) reach the fn.
    - phase_index 1, slug `fanout`, phase_type `llm_batch_agents`: prompt instructing each sub-agent to answer its sub-question from the knowledge base, `"available_tools": ["search_documents"]`, `"max_parallel_agents": 5`, `"merge_strategy": "concat_numbered"` (copy field names from 061's literature_review `review` phase).
    - phase_index 2, slug `deep_dive`, phase_type `llm_agent`: copy the field shape from 061 seed 1's llm_agent block (prompt + `available_tools` — use `["search_documents", "read_document"]` if that is what seed 1 uses; mirror seed 1's exact tool list and step-cap fields if present). Prompt: synthesize/extend the fanout output.
    - phase_index 3, slug `confirm`, phase_type `llm_human_input`: copy the field shape from 061 seed 4's block (`prompt` + `options` keys; include a `timeout_seconds` if seed 4 carries one). Prompt: "Approve the draft synthesis or request changes." with options like ["Approve", "Request changes"].
    - phase_index 4, slug `summarize`, phase_type `llm_single`: prompt producing the final summary from prior outputs.

    All phases: `"validators": []` — the live cross-provider eval must not depend on a regex gate (gate-retry coverage is deterministic in the CI structural test, Plan 02; gate breadth was proven live in 093 D-21). Escape single quotes inside the JSONB literal as `''`. Field names inside each phase config MUST byte-match what 061 uses for that phase type (read 061 first; do not invent keys).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && python -c "import json,re; sql=open('supabase/migrations/066_eval_coverage_seed.sql',encoding='utf-8').read(); m=re.search(r\"'(\{.*\})'::jsonb\", sql, re.S); d=json.loads(m.group(1).replace(\"''\",\"'\")); assert d['slug']=='eval_coverage'; types=[p['config']['phase_type'] for p in d['phases']]; assert types==['programmatic','llm_batch_agents','llm_agent','llm_human_input','llm_single'], types; assert d['phases'][0]['config']['fn']=='eval_slow_step'; print('OK 5 types')"</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/066_eval_coverage_seed.sql` exists and matches `^066_` numbered-SQL convention
    - `grep -c "ON CONFLICT (id) DO NOTHING" supabase/migrations/066_eval_coverage_seed.sql` >= 2 (seed user + definition)
    - `grep -c "0000000000c1" supabase/migrations/066_eval_coverage_seed.sql` == 1 AND `grep -rl "0000000000c1" supabase/migrations/ | wc -l` == 1 (no uuid collision)
    - The python JSONB validity check above prints "OK 5 types" (phase order exactly programmatic, llm_batch_agents, llm_agent, llm_human_input, llm_single)
    - `grep -c '"input_keys": \["topic", "kickoff_prompt"\]' supabase/migrations/066_eval_coverage_seed.sql` == 1
    - `grep -c '"validators": \[\]' supabase/migrations/066_eval_coverage_seed.sql` == 5
  </acceptance_criteria>
  <done>Migration 066 authored, JSONB parses, 5 phase types in locked order, fn = eval_slow_step, idempotent inserts, no uuid collision.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Operator applies migration 066 via Supabase SQL editor, then regen + verify + commit</name>
  <files>supabase/full-schema.sql</files>
  <read_first>
    - supabase/migrations/066_eval_coverage_seed.sql (the file just authored — paste THIS)
  </read_first>
  <what-built>Migration 066 (`eval_coverage` 5-type seed workflow) is authored and validated. Project rule: migrations are applied by PASTING into the Supabase SQL editor — never `supabase db push` / `db reset` (preserves dev data).</what-built>
  <how-to-verify>
    1. Open the local Supabase Studio SQL editor (http://localhost:54323 → SQL Editor).
    2. Paste the full contents of `supabase/migrations/066_eval_coverage_seed.sql` and run it. Expect "Success" (idempotent — safe to re-run).
    3. Reply "applied" here.

    After your "applied", Claude will automatically:
    - Assert the seed row exists: psycopg2 SELECT against localhost:54322 — `SELECT slug, status, is_global FROM workflow_definitions WHERE id = '00000000-0000-0000-0000-0000000000c1'` must return ('eval_coverage', 'published', true), and `jsonb_array_length(definition->'phases')` == 5.
    - Run `bash scripts/regenerate-full-schema.sh` (NO --reset) to rebuild `supabase/full-schema.sql` from the live DB.
    - Commit `supabase/migrations/066_eval_coverage_seed.sql` + `supabase/full-schema.sql` together.
  </how-to-verify>
  <action>Operator pastes migration 066 into the SQL editor (the ONLY human step). Then executor: (1) verify seed row via psycopg2 (connection per scripts/eval_cross_provider.py connect_db pattern — localhost:54322, parameterized constant SQL); (2) `bash scripts/regenerate-full-schema.sh` with no flags; (3) verify `grep -c "eval_coverage" supabase/full-schema.sql` >= 1; (4) commit both files.</action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && grep -c "eval_coverage" supabase/full-schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - psycopg2 SELECT returns slug='eval_coverage', status='published', is_global=true, 5 phases
    - `grep -c "eval_coverage" supabase/full-schema.sql` >= 1 (regenerated, not hand-edited)
    - Both `supabase/migrations/066_eval_coverage_seed.sql` and `supabase/full-schema.sql` are in the same commit
  </acceptance_criteria>
  <resume-signal>Type "applied" after running the migration in the SQL editor, or describe the SQL error verbatim.</resume-signal>
  <done>eval_coverage seed live in local DB, full-schema regenerated, both committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL editor apply | Operator-pasted SQL runs with full DB privileges against the dev DB |
| Global seed workflow | `is_global: true` definition is readable by every user (the established 061 posture for seed templates) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-096-01-01 | Tampering | migration 066 inserts | mitigate | Fixed UUIDs + `ON CONFLICT (id) DO NOTHING` (idempotent, can never overwrite an existing definition); no dynamic SQL |
| T-096-01-02 | Elevation | `eval_slow_step` in closed registry | mitigate | Registered via the closed `PROGRAMMATIC_PHASE_REGISTRY` (T-091-12 posture — never resolved dynamically/eval'd); pure fn, no side effects |
| T-096-01-03 | Denial of service | 20s sleep holds a worker task | accept | Bounded single sleep in an async task (non-blocking); only reachable via the eval seed workflow on a dev box |
</threat_model>

<verification>
- `venv/Scripts/python.exe -c` registry import check passes (eval_slow_step + split_topic both registered)
- Migration JSONB parses with the exact 5-type phase order
- Live DB carries the published eval_coverage definition (post-checkpoint psycopg2 assert)
- full-schema.sql regenerated (contains eval_coverage), committed together with the migration
</verification>

<success_criteria>
- The D-02 max-coverage workflow exists, published + global, in the live local DB
- Plan 06 (eval extension) and Plan 07 (restart smoke) can resolve it by slug `eval_coverage`
- The mid-`programmatic` kill window is real (~20s), satisfying SC#4 without faking the cell
</success_criteria>

<output>
After completion, create `.planning/phases/096-eval-harness-cross-provider-verification-concurrency/096-01-SUMMARY.md`
</output>
