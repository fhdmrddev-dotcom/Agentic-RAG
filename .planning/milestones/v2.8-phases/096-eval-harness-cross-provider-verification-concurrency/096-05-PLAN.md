---
phase: 096-eval-harness-cross-provider-verification-concurrency
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/providers/StreamsProvider.tsx
  - frontend/src/__tests__/providers/streamPool.test.tsx
autonomous: true
requirements: [CONC-01]
tags: [streaming, stream-cap, StreamsProvider, concurrency, bug-fix]

must_haves:
  truths:
    - "At most 3 held-open streaming fetches exist at any time: the viewed thread + the 2 most-recently-viewed background threads (D-09)"
    - "Evicting a stream leaves NO ghost bookkeeping: subscriptionsRef + subscriptionsByThread entries removed, lastSeenOffsetRef cursor RETAINED (the D-11 replay substrate)"
    - "Returning to an evicted thread re-attaches: snapshot renders instantly, then the live stream resumes with replay from the Redis run buffer via the retained cursor (D-11) — the :1259 short-circuit never skips re-attach"
    - "Chat message selectors never re-render from pool churn (PANEL-06 isolation — RESEARCH assumption A4 confirmed by test)"
    - "Background 'running' indicators derive from runs-list state, never from stream presence — no fake activity (D-10)"
  artifacts:
    - path: "frontend/src/providers/StreamsProvider.tsx"
      provides: "thread-keyed LRU-3 stream pool"
      contains: "STREAM_POOL_SIZE = 3"
    - path: "frontend/src/__tests__/providers/streamPool.test.tsx"
      provides: "pool enforcement / eviction bookkeeping / reattach / PANEL-06 isolation tests"
      min_lines: 100
  key_links:
    - from: "setViewingThread (StreamsProvider.tsx:1134-1161)"
      to: "enforceStreamPool helper"
      via: "MRU ref update + eviction AFTER reconcile fires"
      pattern: "enforceStreamPool"
    - from: "eviction helper"
      to: "api.ts AbortError silent-return semantics (:516-517)"
      via: "controller.abort() + manual bookkeeping (onTerminal never fires on abort)"
      pattern: "subscriptionsRef.current.delete"
---

<objective>
Fix BUG-260530-01 (15-30s thread-switch hang): cap held-open streaming fetches at a thread-keyed LRU-3 pool in `StreamsProvider.tsx` (D-09), with honest background indicators (D-10) and snapshot-then-replay re-attach on return (D-11 — the EXISTING reconcile path; the only new work is eviction). Frontend-only: the backend, the SSE vocabulary, and the shared chunk path are untouched.

Purpose: one held-open fetch per active run saturates the browser's 6-per-host HTTP/1.1 connection cap; pool=3 leaves 3 connections free for normal traffic, making thread-switch reconcile <1s (SC#5).
Output: STREAM_POOL_SIZE pool in StreamsProvider; streamPool.test.tsx green; PANEL-06 isolation proven.

**MANDATORY FIRST STEP: load `Skill("sketch-findings-agentic-rag")`** — StreamsProvider is a G-5 hot file and the skill holds the chat-surface design contracts (PANEL-06 isolation + per-thread demux MUST survive; D-10's visual element reuses the EXISTING running indicator — no new visual surface, UI-SPEC intentionally skipped per operator decision 2026-06-07).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/reported-bugs/thread-switch-hang-stream-connection-saturation.md (mechanism)
@.planning/phases/096-eval-harness-cross-provider-verification-concurrency/096-PATTERNS.md (Assignment 9 — REQUIRED read: all 5 call sites, bookkeeping symmetry, cursor rules)
@frontend/src/providers/StreamsProvider.tsx (subscriptionsRef :975; onTerminal bookkeeping :1051-1059; producer re-subscribe :1062; setViewingThread :1134-1161; cursor seeding :1218-1222; reconcile short-circuit :1259; reconcile attach :1362; transient re-attach :1315; sendMessage attach :1654 + transient :1595)
@frontend/src/lib/api.ts (:516-517 — AbortError is a SILENT return: no onTerminal fires)
@frontend/src/__tests__/providers/streamsProvider.test.tsx (test harness analog — mock scaffold :34-67, makeSseRecorder :74+)

<interfaces>
The onTerminal bookkeeping the evictor must replicate (StreamsProvider.tsx:1051-1059):
```typescript
subscriptionsRef.current.delete(producerRunId)
useStreamsStore.setState((s) => ({
  subscriptionsByThread: _removeRunFromThread(s.subscriptionsByThread, threadId, producerRunId),
}))
```
Cursor seeding to preserve UNTOUCHED (:1218-1222 — client cursors win):
```typescript
for (const [rid, cursor] of Object.entries(snapshot.since_cursors)) {
  if (!lastSeenOffsetRef.current.has(rid)) lastSeenOffsetRef.current.set(rid, cursor)
}
```
The ghost-bookkeeping trap (:1259): `if (subscriptionsRef.current.has(run.run_id)) continue` — stale entries make reconcile SKIP re-attach forever (Pitfall 3).
Test scaffold (streamsProvider.test.tsx:34-67): vi.hoisted mocks for postMessage/subscribeToRun/getMessages/getActiveRuns/cancelRun + supabase mock. NOTE: the existing mock set LACKS `getSnapshot` — the pool test must add it to the vi.mock("@/lib/api") factory (reconcile calls it at :1179).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread-keyed LRU-3 pool — constant, MRU ref, enforceStreamPool, gated open sites</name>
  <files>frontend/src/providers/StreamsProvider.tsx, frontend/src/__tests__/providers/streamPool.test.tsx</files>
  <read_first>
    - Skill("sketch-findings-agentic-rag") — load FIRST (G-5 hot file design contracts)
    - .planning/phases/096-eval-harness-cross-provider-verification-concurrency/096-PATTERNS.md (Assignment 9 — verbatim code blocks for eviction + hook point)
    - frontend/src/providers/StreamsProvider.tsx (all line anchors listed in context — verify against current HEAD before editing; the file is 2,517 lines)
    - frontend/src/lib/api.ts (:500-560 — subscribeToRun AbortError semantics)
    - frontend/src/__tests__/providers/streamsProvider.test.tsx (mock scaffold to copy)
  </read_first>
  <behavior>
    - Test 1 (pool enforcement): 4 threads each with 1 active run, visited in order A,B,C,D → only D + C + B hold subscriptions; A's controller aborted and its entries removed from subscriptionsRef AND subscriptionsByThread
    - Test 2 (cursor retention): after A's eviction, lastSeenOffsetRef still has A's run cursor
    - Test 3 (reattach on return): navigate back to A → reconcile calls getSnapshot AND subscribeToRun fires again for A's run with the retained since-cursor (the :1259 short-circuit does NOT skip it)
    - Test 4 (PANEL-06 isolation / A4 confirm): a chat-message selector subscriber records zero re-renders across an evict + reattach cycle (pool churn never re-renders chat)
    - Test 5 (gating): an open attempt for a thread OUTSIDE the keep-set is skipped (no subscribeToRun call)
  </behavior>
  <action>
    In StreamsProvider.tsx, additive + surgical (keep the file's existing structure; `activeThreadIdRef` assignment count MUST stay 1 — a regression test greps for it):

    1. Module-level: `const STREAM_POOL_SIZE = 3` with a comment citing D-09 + BUG-260530-01 (one configurable constant — the browser allows 6 per-host connections; 3 streams leave 3 free).

    2. `const mruThreadsRef = useRef<string[]>([])` — most-recently-viewed thread ids, most-recent first, deduped, capped at ~10 entries.

    3. Helpers (names exact, for greppability):
    ```typescript
    const isThreadInStreamPool = (threadId: string | null): boolean => {
      if (threadId === null) return true   // pre-navigation sends never blocked
      const viewed = activeThreadIdRef.current
      const keep = new Set<string>([viewed, ...mruThreadsRef.current.filter((t) => t !== viewed)].filter(Boolean).slice(0, STREAM_POOL_SIZE) as string[])
      return keep.has(threadId)
    }
    const enforceStreamPool = (viewedThreadId: string) => {
      // compute keep-set = {viewed} ∪ first (STREAM_POOL_SIZE-1) of MRU minus viewed
      // for each runId in subscriptionsRef whose owning thread (reverse lookup via
      // the store's subscriptionsByThread) is OUTSIDE the keep-set:
      //   controller.abort()                                   // api.ts:516-517 — SILENT; no onTerminal
      //   subscriptionsRef.current.delete(runId)                // replicate :1051-1059 bookkeeping
      //   _removeRunFromThread(subscriptionsByThread, ...)      //   "
      // NEVER touch lastSeenOffsetRef — the cursor is the D-11 replay substrate.
    }
    ```

    4. Hook point — `setViewingThread` (:1134-1161): after `activeThreadIdRef.current = threadId` and AFTER the `reconcile(threadId)` call fires, update `mruThreadsRef` (move threadId to front, dedupe) and call `enforceStreamPool(threadId)`. Do not reorder the existing statements; the assignment stays the sole write.

    5. Gate ALL 5 open sites on `isThreadInStreamPool(<the thread the stream belongs to>)` — skip opening (and skip the slot-reservation write) when outside the keep-set: producer re-subscribe :1062, reconcile transient re-attach :1315, reconcile attach :1362, sendMessage transient re-attach :1595, sendMessage attach :1654. (In practice the send-time thread IS the viewed thread, so only navigation evicts — but the gate must exist at every site so a future code path can't leak a 4th connection.)

    Write the 5 behavior tests FIRST (RED) in `frontend/src/__tests__/providers/streamPool.test.tsx` (sibling of the existing streamsProvider.test.tsx — copy its vi.hoisted mock scaffold and ADD `getSnapshot` to the api mock factory; reuse makeSseRecorder). Then implement (GREEN). NOTE: RESEARCH/VALIDATION earlier referenced `src/providers/__tests__/streamPool.test.tsx` — the canonical location is `src/__tests__/providers/` per the existing convention; this plan + the updated VALIDATION.md use that path.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && npx vitest run src/__tests__/providers/streamPool.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "STREAM_POOL_SIZE = 3" frontend/src/providers/StreamsProvider.tsx` == 1
    - `grep -c "enforceStreamPool" frontend/src/providers/StreamsProvider.tsx` >= 2 (def + setViewingThread call)
    - `grep -c "isThreadInStreamPool" frontend/src/providers/StreamsProvider.tsx` >= 6 (def + 5 gated sites)
    - `grep -c "activeThreadIdRef.current = " frontend/src/providers/StreamsProvider.tsx` == 1 (sole-writer invariant)
    - `grep -c "lastSeenOffsetRef.current.delete" frontend/src/providers/StreamsProvider.tsx` == 0 in the eviction helper's hunk (cursors never evicted — inspect diff)
    - streamPool.test.tsx green (all 5 behaviors incl. PANEL-06 zero-re-render)
  </acceptance_criteria>
  <done>Pool enforced at 3, eviction bookkeeping symmetric, cursors retained, all 5 open sites gated, chat isolation proven.</done>
</task>

<task type="auto">
  <name>Task 2: D-10 honest-indicator audit + regression sweep</name>
  <files>frontend/src/providers/StreamsProvider.tsx</files>
  <read_first>
    - frontend/src/providers/StreamsProvider.tsx (the streamingThreads / subscriptionsByThread consumers — grep both store keys across frontend/src)
    - Skill("sketch-findings-agentic-rag") (running-indicator contracts — the pulse derives from run status, not stream presence)
  </read_first>
  <action>
    1. **Audit (grep-driven, evidence in SUMMARY):** `grep -rn "subscriptionsByThread" frontend/src --include="*.tsx" --include="*.ts"` — for every consumer OUTSIDE StreamsProvider's own bookkeeping, classify: does it key a "running" pulse/spinner/badge on subscription membership? That membership now UNDER-COUNTS by design (capped threads have active runs but no subscription). Any such consumer must be re-pointed at run-status state (`streamingThreads`, `snapshot.active_runs`-derived store state, or the runs list) — presentation-only changes, no new visual element (D-10 keeps the existing pulse; the indicator's LOOK is unchanged, only its data source is corrected). If the audit finds ZERO consumers keying visuals on subscription membership (the expected outcome — RESEARCH believes indicators already derive from streamingThreads), record the grep evidence in the SUMMARY and change nothing.
    2. **Regression sweep:** run the existing StreamsProvider suites + full frontend gates:
       - `npx vitest run src/__tests__/providers/` (existing streamsProvider tests + new streamPool tests)
       - `npx tsc -b` — baseline 37, zero net-new referencing touched files
       - `npm run build` — exit 0
       - Full `npx vitest run` — net-new failures == 0 vs pre-plan commit via the 095.1 git-stash pattern (documented ~16-failure baseline cluster; never raw counts)
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && npx vitest run src/__tests__/providers/ && npx tsc -b 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY contains the D-10 audit table: every subscriptionsByThread consumer listed with its classification (or the zero-consumer grep evidence)
    - No new visual element added (diff shows no new JSX surface — D-10 reuses the existing running indicator)
    - providers suites green; tsc baseline 37; build exit 0; full-vitest net-new == 0 (stash-proven)
    - Backend diff EMPTY: `git diff backend/` shows nothing (frontend-only fix — shared SSE path byte-identical across all 8 providers by construction)
  </acceptance_criteria>
  <done>Indicators provably honest (status-derived), all baselines clean, zero backend churn.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ↔ backend SSE | per-user authenticated streaming fetches (unchanged) |
| pool bookkeeping | client-side refs/store only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-096-05-01 | Denial of service | browser connection pool | mitigate | The pool IS the mitigation — bounded at 3 held-open streams, 3 connections always free (BUG-260530-01 fix) |
| T-096-05-02 | Tampering | shared SSE/chunk path | mitigate | Frontend-only; `git diff backend/` empty acceptance criterion; no SSE vocabulary or provider-branch change (all 8 providers byte-identical) |
| T-096-05-03 | Information disclosure | retained cursors for evicted runs | accept | Cursors are opaque per-user offsets into the user's OWN run buffers; replay re-auth happens at re-subscribe (existing auth path) |
</threat_model>

<verification>
- streamPool.test.tsx green (5 behaviors); existing providers suites green
- tsc -b baseline 37 / build exit 0 / full-vitest net-new 0 (stash-proven)
- backend diff empty
- Live SC#5 (<1s switch with ≥6 runs) + D-11a assertion are MANUAL — authored in 096-VALIDATION.md, exercised at /gsd:verify-work
</verification>

<success_criteria>
- BUG-260530-01 structurally fixed: max 3 held-open streams, eviction safe, return-reattach lossless via cursor replay
- PANEL-06 isolation + per-thread demux survive (test-proven)
- D-10/D-11a honesty semantics in place for the live UAT rows
</success_criteria>

<output>
After completion, create `.planning/phases/096-eval-harness-cross-provider-verification-concurrency/096-05-SUMMARY.md`
</output>
