---
phase: 096-eval-harness-cross-provider-verification-concurrency
plan: 07
type: execute
wave: 2
depends_on: ["096-01"]
files_modified:
  - scripts/restart_smoke.py
  - scripts/conc_probe.py
autonomous: true
requirements: [EVAL-02, CONC-01]
tags: [scripts, restart-smoke, concurrency, measurement, harness]

must_haves:
  truths:
    - "One command per kill point seeds the right workflow, prints an unmistakable KILL-NOW banner exactly when the target phase is active, and after the operator restarts uvicorn asserts DB truth: no skipped phases, no double-applied side effects, ask_user prompt re-emitted (D-08 / SC#2+#4)"
    - "The mid-ask_user smoke leg additionally POSTs the answer post-restart and asserts the run completes — this IS the BUG-260605-01 live fix verification"
    - "One command drives an N=10 fan-out and proves ≤5 concurrent sub-agents (DB interval-overlap analysis) plus records cross-tab GET latency p95 vs 50ms and the AnyIO borrowed/total budget (SC#3)"
  artifacts:
    - path: "scripts/restart_smoke.py"
      provides: "D-08 operator-driven restart smoke helper (3 kill points)"
      contains: "--kill-at"
    - path: "scripts/conc_probe.py"
      provides: "fan-out overlap + latency + backpressure probe"
      contains: "backpressure"
  key_links:
    - from: "scripts/restart_smoke.py"
      to: "workflow_definitions slug eval_coverage / doc_qa_human"
      via: "seed kickoff with workflow_definition_id"
      pattern: "eval_coverage"
    - from: "scripts/conc_probe.py"
      to: "GET /admin/backpressure"
      via: "anyio_threadpool_depth borrowed/total sampling"
      pattern: "/admin/backpressure"
---

<objective>
Build the two operator measurement scripts: `scripts/restart_smoke.py` (D-08 — the human IS the restart mechanism; the robot owns seed + kill-signal + DB-truth assertions, per kill point: mid-`programmatic` via eval_slow_step's 20s window, mid-`llm_agent`, mid-`ask_user`) and `scripts/conc_probe.py` (CONC-01/SC#3 — N=10 fan-out proves the 091 semaphore live, cross-tab GET p95 <50ms, AnyIO threadpool budget reading). Both reuse the eval script's five-piece plumbing kit verbatim.

Purpose: HARNESS-03 resumability and batch fairness get INDEPENDENT verification with greppable evidence — these scripts are proofs over existing machinery, not new runtime code.
Output: two scripts in `scripts/` (NEVER in backend/ — uvicorn watched tree); live runs are operator-driven per VALIDATION.md.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@scripts/eval_cross_provider.py (the five-piece kit to copy: load_env :188-203, report_env_presence :206-230, assert_localhost_only :239-256, connect_db :264-275, get_bearer_token :411-449, BackendUnavailable :389-392, create_thread :456, run_prompt :478-497, wait_for_run :500-521)
@.planning/phases/096-eval-harness-cross-provider-verification-concurrency/096-PATTERNS.md (Assignments 5 + 6)
@backend/app/api/admin.py (:52-99 — backpressure response shape)
@backend/app/services/harness/programmatic.py (split_topic — no N cap; 10 semicolons → 10 branches)
@backend/app/services/harness/phase_types.py (:347 — the Semaphore(max_parallel_agents) being verified)

<interfaces>
Backpressure response (verified — admin.py:52-99; dev/local fail-open for any authenticated user):
```python
bp = requests.get(f"{base_url()}/admin/backpressure", headers=_auth_headers(token)).json()
# {"anyio_threadpool_depth": {"borrowed": int, "total": int},
#  "redis_active_runs": int, "postgres_pool_in_use": int, "per_worker_run_count": int}
```
Health poll for backend-back-up detection: GET {base_url()}/health (main.py:343).
Double-execution detector (INSERT-only harness_audit):
```sql
SELECT metadata->>'phase' AS slug, count(*) AS n FROM harness_audit
WHERE run_id = %s AND event_type = 'phase_completed' GROUP BY 1 HAVING count(*) > 1
```
Kill-point honesty: mid-`programmatic` uses eval_coverage's eval_slow_step (~20s window — Plan 01); mid-`llm_agent` and mid-`ask_user` can use eval_coverage too (it has both phases). split_topic/eval_slow_step both fan out on `;`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/restart_smoke.py — seed, kill-signal, post-restart DB-truth assertions</name>
  <files>scripts/restart_smoke.py</files>
  <read_first>
    - scripts/eval_cross_provider.py (the helper functions listed in context — copy bodies, do not import across scripts unless the file already supports it; self-contained copy is the established pattern per observe-run.py)
    - .planning/phases/096-eval-harness-cross-provider-verification-concurrency/096-PATTERNS.md (Assignment 5 — the copy-from table)
    - backend/app/services/harness_engine.py (:1085-1180 resume_stranded_workflows + resume_pending_prompt — what is being VERIFIED, never rebuilt)
  </read_first>
  <action>
    Create `scripts/restart_smoke.py`:

    1. **Plumbing:** the five-piece kit copied from eval_cross_provider.py: `load_env` → `assert_localhost_only` FIRST → `report_env_presence` (presence-only) → `get_bearer_token` → `connect_db`. `BackendUnavailable` for clean no-traceback exits. Constant-string allowlisted SQL, %s params only.

    2. **CLI:** `--kill-at {programmatic|llm_agent|ask_user}` (required); optional `--provider` (default openai); `--workflow-slug` (default `eval_coverage`).

    3. **Flow:**
       a. Resolve definition by slug (published, max version). Create thread; kick off via POST /threads/{id}/messages with `workflow_definition_id` + 3-subtopic kickoff. Resolve the workflow_run id.
       b. Poll `workflow_phases` every 0.5s (conn.rollback() per poll). Map kill points → target phase slug: programmatic→`split`, llm_agent→`deep_dive`, ask_user→`confirm` (slug names from migration 066; verify by SELECTing the definition's phases at runtime rather than hardcoding — read slugs from the definition JSONB). For ask_user, additionally wait until the prompt message exists (the JSONB containment) so the kill lands with a pending prompt.
       c. When the target phase status == 'active' (and for ask_user the prompt is pending), print the banner:
          `SMOKE_KILL >>> PHASE '<slug>' IS NOW ACTIVE — KILL THE BACKEND NOW (Ctrl+C in the uvicorn terminal) <<<`
       d. Detect the kill: poll GET /health until it errors (backend down — print `SMOKE_DOWN detected`), then until it answers again (operator restarted — print `SMOKE_UP detected`; remind the operator the startup sweep runs on boot). Bound each wait at 10 minutes with a clean diagnostic exit.
       e. Wait for workflow terminal (or for ask_user: wait for the re-emitted prompt FIRST — assert a fresh ask_user_prompt emission exists post-restart with no matching response, then POST the answer via /runs/{workflow_run_id}/ask_user_response and wait for completion; this leg IS the BUG-260605-01 live verification).
       f. **Assertions (each printed as `SMOKE_ASSERT <name> PASS|FAIL <detail>`):**
          - `no_skipped_phases`: every workflow_phases row reached 'completed' (none 'skipped', none stuck)
          - `single_completion_audit`: the HAVING-count>1 detector returns ZERO rows (no double-applied side effects)
          - `no_duplicate_subagents`: sub-agent runs count for the batch phase == number of sub_questions (no double fan-out)
          - `prompt_reemitted` (ask_user leg only): a post-restart prompt emission exists
          - `answer_reached_engine` (ask_user leg only): run completed after the POST
       g. Final `SMOKE_RESULT {kill_at} PASS|FAIL` summary line (greppable).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && backend/venv/Scripts/python.exe -m py_compile scripts/restart_smoke.py && backend/venv/Scripts/python.exe scripts/restart_smoke.py --help</automated>
  </verify>
  <acceptance_criteria>
    - `--help` shows `--kill-at` with choices programmatic|llm_agent|ask_user
    - `grep -c "SMOKE_KILL\|SMOKE_ASSERT\|SMOKE_RESULT" scripts/restart_smoke.py` >= 6 (greppable markers)
    - `grep -n "assert_localhost_only" scripts/restart_smoke.py` — called in main() before any DB/HTTP
    - `grep -c "HAVING count" scripts/restart_smoke.py` >= 1 (double-execution detector)
    - `grep -c "run_in_background\|subprocess.*uvicorn" scripts/restart_smoke.py` == 0 (the HUMAN is the restart mechanism — script never touches the backend process)
    - File lives in scripts/, not backend/
  </acceptance_criteria>
  <done>Script compiles, --help works, all markers/assertions present; the 3 live kill-point runs are operator-driven (VALIDATION.md restart-smoke matrix).</done>
</task>

<task type="auto">
  <name>Task 2: scripts/conc_probe.py — N=10 fan-out overlap + latency + AnyIO budget</name>
  <files>scripts/conc_probe.py</files>
  <read_first>
    - scripts/eval_cross_provider.py (same kit)
    - backend/app/api/admin.py (:52-99 — exact backpressure JSON keys)
    - .planning/phases/096-eval-harness-cross-provider-verification-concurrency/096-PATTERNS.md (Assignment 6 — incl. the /snapshot idle short-circuit caveat at threads.py:418-423)
  </read_first>
  <action>
    Create `scripts/conc_probe.py` (same five-piece kit, localhost-gate first):

    1. **Drive:** create a thread; kick off the `literature_review` seed workflow (programmatic split → llm_batch_agents → single — no human input, the canonical fan-out target) with a kickoff of EXACTLY 10 semicolon-separated sub-topics (split_topic has no cap — 10 clauses → N=10 branches into Semaphore(5)). Also create one IDLE second thread before kickoff (the cross-tab probe target).

    2. **During the run** (until workflow terminal), two sampling loops:
       a. Every ~1s: GET /admin/backpressure — record `anyio_threadpool_depth.borrowed/.total`, `redis_active_runs`, `postgres_pool_in_use`, `per_worker_run_count` (WORKER_COUNT=2 — record per_worker so readings are interpretable when the probe and workflow land on different workers).
       b. Every ~500ms (separate thread via threading.Thread): time TWO endpoints — `GET /threads/{idle_thread_id}/snapshot` AND `GET /threads` (the list endpoint exercises the supabase-py threadpool path; /snapshot of an idle thread short-circuits before Redis, so BOTH are needed for a representative reading). Record wall-clock ms per request. Anti-pattern guard: this is a DIRECT HTTP client, never a browser (browser measurement conflates the 6-connection cap with backend starvation).

    3. **Post-run analysis + greppable output (`PROBE_ASSERT` / `PROBE_RESULT` markers):**
       - `fanout_bounded`: fetch the batch phase's sub-agent runs `(started_at, completed_at)` windows (constant parameterized SQL); compute max pairwise overlap (sweep-line over sorted start/end events); PASS iff max ≤ 5 AND total sub-runs == 10
       - `cross_tab_latency`: p50/p95/max for each probed endpoint; PASS iff p95 < 50ms
       - `anyio_budget`: print `total` (the live budget — expected AnyIO default 40) and peak `borrowed`; informational `PROBE_BUDGET total=<n> peak_borrowed=<n>` (the SC#3 "verified before sizing defaults" deliverable is this documented reading)
       - `PROBE_RESULT PASS|FAIL` summary.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && backend/venv/Scripts/python.exe -m py_compile scripts/conc_probe.py && backend/venv/Scripts/python.exe scripts/conc_probe.py --help</automated>
  </verify>
  <acceptance_criteria>
    - py_compile + --help exit 0
    - `grep -c "PROBE_ASSERT\|PROBE_RESULT\|PROBE_BUDGET" scripts/conc_probe.py` >= 5
    - `grep -c "anyio_threadpool_depth" scripts/conc_probe.py` >= 1 AND `grep -c "per_worker_run_count" scripts/conc_probe.py` >= 1
    - The kickoff string contains exactly 10 semicolon-separated clauses (count ";" == 9 in the constant)
    - `grep -c "GET /threads\b\|/threads\"" scripts/conc_probe.py` — both probe endpoints present (snapshot + list)
    - `grep -n "assert_localhost_only" scripts/conc_probe.py` — first in main()
  </acceptance_criteria>
  <done>Probe compiles with overlap analysis, dual-endpoint latency, and budget reporting; the live N=10 run is operator-driven (VALIDATION.md).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → local DB / backend | psycopg2 + HTTP against localhost only |
| /admin/backpressure | fail-open ONLY in dev/local; allow-list gated in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-096-07-01 | Tampering/DoS | scripts pointed at cloud | mitigate | assert_localhost_only FIRST in both scripts (acceptance grep) |
| T-096-07-02 | Information disclosure | script output | mitigate | Presence-only env reporting; markers carry metrics/slugs only, never secrets |
| T-096-07-03 | Elevation | /admin/backpressure probing | accept | Dev-local only by construction (localhost gate); the endpoint's production allow-list is unchanged |
| T-096-07-04 | Tampering (SQLi) | overlap/audit queries | mitigate | Constant query strings, %s params (the allowlist pattern) |
</threat_model>

<verification>
- Both scripts py_compile + --help green; all acceptance greps pass
- Live runs (3 restart kill points + N=10 probe) are operator-driven — matrix + assertions authored in 096-VALIDATION.md Manual-Only table
</verification>

<success_criteria>
- D-08 deliverable complete: operator can run each kill point with robot-owned setup + assertions; mid-programmatic is REAL (eval_slow_step window), never faked
- SC#3 measurable in one command: fan-out bound, latency p95, AnyIO budget — all greppable
- The mid-ask_user leg doubles as BUG-260605-01's live verification (with Plans 03+04 shipped)
</success_criteria>

<output>
After completion, create `.planning/phases/096-eval-harness-cross-provider-verification-concurrency/096-07-SUMMARY.md`
</output>
