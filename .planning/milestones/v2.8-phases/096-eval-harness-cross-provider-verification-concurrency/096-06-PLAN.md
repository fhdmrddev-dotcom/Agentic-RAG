---
phase: 096-eval-harness-cross-provider-verification-concurrency
plan: 06
type: execute
wave: 2
depends_on: ["096-01"]
files_modified:
  - scripts/eval_cross_provider.py
  - .planning/eval/README.md
autonomous: true
requirements: [EVAL-01]
tags: [eval, cross-provider, scripts, capability-table]

must_haves:
  truths:
    - "One command drives the eval_coverage 5-type workflow end-to-end per native-7 provider and asserts: locked phase sequence completed, correct tool round-trips, exactly one phase_completed audit event per phase, ask_user auto-answered (D-02, D-02a)"
    - "The scoreboard reports the EFFECTIVE model from sub-agent runs.model rows — never the eval's requested model column (Pitfall 1: body.model does not steer harness phases)"
    - "Each full run persists a versioned per-provider capability table (JSON + Markdown) under .planning/eval/ (D-04 — measure feature-fit, don't act on it)"
    - "Native-7 is the hard pass/fail bar; OpenRouter is recorded best-effort, never gating (D-03)"
    - "The script header documents the D-01 part-2 operator live-gate ritual (mandatory for phase closure on provider-touching phases)"
  artifacts:
    - path: "scripts/eval_cross_provider.py"
      provides: "workflow row type + ask_user auto-answer + capability-table emitter"
      contains: "--workflow"
    - path: ".planning/eval/README.md"
      provides: "capability-table artifact format + operator gate ritual docs"
  key_links:
    - from: "scripts/eval_cross_provider.py"
      to: "workflow_definitions.slug = 'eval_coverage'"
      via: "runtime slug lookup (never hardcode the uuid)"
      pattern: "eval_coverage"
    - from: "scripts/eval_cross_provider.py"
      to: "POST /runs/{workflow_run_id}/ask_user_response"
      via: "D-02a robot answer (the panel's own endpoint)"
      pattern: "ask_user_response"
---

<objective>
Extend `scripts/eval_cross_provider.py` (702 lines, native-8, localhost-gated, scoreboard-emitting) with a workflow row type: per provider, kick off the `eval_coverage` workflow via the existing `POST /threads/{id}/messages` route (+`workflow_definition_id`), poll DB truth, auto-answer the `llm_human_input` prompt, and assert the locked phase sequence with correct tool round-trips. Emit the D-04 capability table as a versioned JSON+MD artifact.

Purpose: EVAL-01's live half — "only the operator's live eval proves providers." The robot watches ask_user because that round-trip broke twice (F10, BUG-260605-01).
Output: extended eval script (reusing its plumbing verbatim); `.planning/eval/` artifact home + docs.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@scripts/eval_cross_provider.py (FULL — every helper is reused: load_env :188, report_env_presence :206, assert_localhost_only :239, connect_db :264, _COUNT_QUERIES :97, count_rows :339, assert_ask_user :361, BackendUnavailable :389, get_bearer_token :411, create_thread :456, run_prompt :478, wait_for_run :500, run_cell :535, print_scoreboard :564, _parse_args :598, main :644)
@.planning/phases/096-eval-harness-cross-provider-verification-concurrency/096-PATTERNS.md (Assignment 1 — kickoff/poll/answer code blocks)
@.planning/phases/096-eval-harness-cross-provider-verification-concurrency/096-RESEARCH.md (Pattern 1 — verified mechanics; Pitfalls 1+2; Capability-Table recommendation)
@backend/app/api/panel.py (:144-155 — pending-prompt payload shape: message_id, tool_call_id, prompt, options, timeout_seconds, run_id, draft, created_at)

<interfaces>
Kickoff (verified — threads.py:835 accepts workflow_definition_id; provider override flows to harness ctx, model does NOT):
```python
resp = requests.post(f"{base_url()}/threads/{thread_id}/messages", headers=_auth_headers(token),
    json={"content": kickoff, "provider": provider, "workflow_definition_id": def_id}, timeout=30)
```
ask_user answer (the panel's own endpoint; F10 fallback resolves at runs.py:521-567):
```python
requests.post(f"{base_url()}/runs/{workflow_run_id}/ask_user_response", headers=_auth_headers(token),
    json={"tool_call_id": tcid, "response_text": "Approve", "choice_index": 0}, timeout=15)
```
Workflow terminal states: completed/failed/cancelled (threads.py:815). Poll with conn.rollback() per iteration (wait_for_run pattern :500-521 — defeats psycopg2 stale snapshots).
Double-execution detector (harness_audit is INSERT-only):
```sql
SELECT metadata->>'phase' AS slug, count(*) AS n FROM harness_audit
WHERE run_id = %s AND event_type = 'phase_completed' GROUP BY 1 HAVING count(*) > 1
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workflow row type — kickoff, DB-truth polling, D-02a auto-answer, assertions</name>
  <files>scripts/eval_cross_provider.py</files>
  <read_first>
    - scripts/eval_cross_provider.py (FULL FILE — the extension must reuse every helper; understand run_cell's "one bad cell never aborts the matrix" error capture :535-561 and the EVAL_ROW/EVAL_SUMMARY markers :587-589)
    - .planning/phases/096-eval-harness-cross-provider-verification-concurrency/096-PATTERNS.md (Assignment 1)
    - backend/app/api/panel.py (:103-156 — pending payload field names the auto-answer parses)
  </read_first>
  <action>
    Extend the script (additive — Deep-mode rows untouched):

    1. **CLI:** add `--workflow` flag to `_parse_args` (runs workflow rows; combinable with the existing `--providers` filter). Entry-point discipline UNCHANGED: `load_env()` → `assert_localhost_only()` FIRST → `report_env_presence()` (presence-only, never values).

    2. **Definition resolution:** `def resolve_workflow_def(conn) -> str` — `SELECT id FROM workflow_definitions WHERE slug = %s AND status = 'published' ORDER BY version DESC LIMIT 1` with `('eval_coverage',)`. Never hardcode the uuid. Clean error if absent ("apply migration 066 first").

    3. **`run_workflow_cell(token, conn, provider, model)`** (mirrors run_cell's error-capture so one bad provider never aborts the matrix):
       a. Create a thread; POST the kickoff with `workflow_definition_id` + `provider` (per-request override — NEVER mutate global settings, the anti-pattern). Kickoff content: 3 semicolon-separated sub-topics (e.g. "Compare ingestion pipeline; retrieval quality; sandbox security") so `eval_slow_step` fans out N=3 (cheap but real).
       b. Resolve the workflow_run id (threads.active_workflow_run_id, or workflow_runs by thread — constant parameterized SQL; extend the allowlist dict with constant strings for `workflow_runs`, `workflow_phases`, `harness_audit`).
       c. **Concurrent poll loop** (Pitfall 2): every ~3s, conn.rollback() then check BOTH `workflow_runs.status` AND pending ask_user prompts (the existing `tool_calls @> '[{"kind":"ask_user_prompt"}]'` containment without a matching response). When a prompt appears AND the run is non-terminal: extract `tool_call_id` + `run_id` from the prompt payload (panel.py:144-155 field names) and POST the answer (interface block above). If the run goes terminal BEFORE a prompt is answered, record outcome `terminal_before_answer` — a DISTINCT diagnostic, not a generic FAIL.
       d. On terminal, assert: (i) workflow_runs.status == 'completed'; (ii) every workflow_phases row reached 'completed' in phase_index order (no 'skipped' — the eval_coverage definition routes none); (iii) harness_audit has EXACTLY one phase_completed per phase (HAVING-count detector above — zero rows); (iv) tool round-trip: ≥1 `search_documents` tool call recorded in the thread's messages (reuse get_tool_calls/assert_tool_invoked); (v) ask_user_roundtrip_ok == answered + run proceeded past the confirm phase.
       e. **Effective model (Pitfall 1):** `SELECT DISTINCT model FROM runs WHERE thread_id = %s AND model IS NOT NULL` — the scoreboard row reports THIS as `model_effective`, never the PROVIDERS-constant model (body.model does not reach harness phases; the per-provider defaults table at config.py:596-603 decides — accepted + curated in Plan 08 per Open-Q1 recommendation).
       f. Record retry_count/gate_failures (harness_audit event counts) + wall_clock_s per row.

    4. **Gating (D-03):** native-7 rows (openai, anthropic, google, deepseek, moonshot, zhipu, minimax) are pass/fail; openrouter row runs but is marked `best-effort` and excluded from the EVAL_SUMMARY pass/fail tally. Missing API key → row marked MISSING, never blocks others (existing convention).

    5. **Scoreboard:** workflow rows print with the existing `EVAL_ROW` prefix (greppable diff ritual) including columns: provider, model_effective, workflow_completed, phases_ok, audit_single_completion, tools_ok, ask_user_roundtrip_ok, retries, wall_clock_s.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && backend/venv/Scripts/python.exe -m py_compile scripts/eval_cross_provider.py && backend/venv/Scripts/python.exe scripts/eval_cross_provider.py --help</automated>
  </verify>
  <acceptance_criteria>
    - `--help` output contains `--workflow`
    - `grep -c "eval_coverage" scripts/eval_cross_provider.py` >= 1 AND `grep -c "0000000000c1" scripts/eval_cross_provider.py` == 0 (slug lookup, no hardcoded uuid)
    - `grep -c "terminal_before_answer" scripts/eval_cross_provider.py` >= 1 (Pitfall 2 diagnostic outcome)
    - `grep -c "HAVING count(\*) > 1\|HAVING count(\*)>1" scripts/eval_cross_provider.py` >= 1 (double-execution detector)
    - `grep -c "model_effective" scripts/eval_cross_provider.py` >= 1 (Pitfall 1 — runs.model asserted)
    - `grep -n "assert_localhost_only()" scripts/eval_cross_provider.py` — still called before any DB/HTTP work in main()
    - All new SQL uses constant strings + %s params (no f-string SQL — review diff)
  </acceptance_criteria>
  <done>Workflow rows drive eval_coverage per provider with DB-truth assertions, robot-answered ask_user, effective-model honesty, and one-bad-cell isolation. (Live native-7 run = manual, VALIDATION.md.)</done>
</task>

<task type="auto">
  <name>Task 2: D-04 capability-table emitter + D-01 part-2 operator-gate docs</name>
  <files>scripts/eval_cross_provider.py, .planning/eval/README.md</files>
  <read_first>
    - scripts/eval_cross_provider.py (print_scoreboard :564-589 — the row dicts the emitter extends)
    - .planning/phases/096-eval-harness-cross-provider-verification-concurrency/096-RESEARCH.md (§Capability-Table Artifact — the recommended JSON schema)
  </read_first>
  <action>
    1. **Emitter:** after a full `--workflow` run, write BOTH (planner-resolved discretion: JSON + Markdown twin, git-versioned):
       - `.planning/eval/capability-table-<YYYY-MM-DD>.json` — one object per provider:
         `{"provider": str, "model_effective": str, "workflow_completed": bool, "phase_type_results": {"programmatic": str, "llm_batch_agents": str, "llm_agent": str, "llm_human_input": str, "llm_single": str}, "tool_invocation_fidelity": bool, "arg_shape_ok": bool, "retry_count": int, "gate_failures": int, "wall_clock_s": float, "ask_user_roundtrip_ok": bool}`
       - `.planning/eval/capability-table-<YYYY-MM-DD>.md` — the same data as a human-readable table.
       Create the `.planning/eval/` directory if absent. The artifact is MEASUREMENT ONLY — routing changes are deferred to v2.9 (D-04 locked; do not add any routing logic).
    2. **Script header docstring** (D-01 part 2): add the operator live-gate ritual — verbatim intent: "MANDATORY GATE for phase closure on any provider-touching phase (same standing as the 4-axis UAT recipe): run `--workflow` against the native-7, grep-diff `EVAL_ROW` lines vs the prior capability table, attach the scoreboard to the phase's VALIDATION evidence. Localhost-only by design; provider keys never leave backend/.env; no CI secrets (D-01)."
    3. **`.planning/eval/README.md`:** document the artifact format (the JSON schema above), the emit trigger (full --workflow runs), the grep ritual (`grep EVAL_ROW`), and the v2.9 consumer (feature-fit routing decision pass reads these diffs — measure-only until then).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && backend/venv/Scripts/python.exe -m py_compile scripts/eval_cross_provider.py && test -f .planning/eval/README.md && grep -c "capability-table" .planning/eval/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "capability-table" scripts/eval_cross_provider.py` >= 2 (json + md emit paths)
    - `grep -c "ask_user_roundtrip_ok" scripts/eval_cross_provider.py` >= 2 (row dict + JSON emit)
    - Script header contains "MANDATORY GATE" (grep)
    - `.planning/eval/README.md` exists, documents the JSON schema fields verbatim
    - No secret values printed anywhere: `grep -c "API_KEY" scripts/eval_cross_provider.py` diff hunks reference names/presence only (review)
  </acceptance_criteria>
  <done>Every full eval run persists a diffable capability table; the operator gate is documented at the script's front door and in .planning/eval/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → live providers | operator's API keys drive real provider calls (localhost-only) |
| script → local DB | psycopg2 direct reads for assertions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-096-06-01 | Tampering/DoS | eval pointed at cloud DB | mitigate | `assert_localhost_only()` FIRST in main(), before any DB/HTTP (existing T-088-02-01 posture — acceptance grep) |
| T-096-06-02 | Information disclosure | script output / artifacts | mitigate | Presence-only env reporting; capability table contains model NAMES + metrics only, never keys; no GitHub secrets (D-01) |
| T-096-06-03 | Tampering (SQLi) | new DB assertions | mitigate | Constant-string allowlisted queries + %s params (the _COUNT_QUERIES pattern extended) |
| T-096-06-04 | Spoofing | ask_user auto-answer | mitigate | Uses the panel's own owner-scoped endpoint; the F10 anchor-confirm (runs.py:521-567) stays the server-side guard — script adds no bypass |
</threat_model>

<verification>
- py_compile + --help green; acceptance greps pass
- Dry-run sanity: `--workflow --providers openai` against the running local stack completes one row end-to-end (executor MAY run this if backend+Supabase are up and OPENAI key present; otherwise it is the operator's first live-gate run, VALIDATION.md)
- Full native-7 live run = manual (VALIDATION.md Manual-Only table)
</verification>

<success_criteria>
- EVAL-01 live half delivered: one command per provider proves the 5-type workflow with DB-truth assertions and a versioned capability artifact
- D-02a regression insurance in place: the ask_user round-trip is robot-watched on every eval run
</success_criteria>

<output>
After completion, create `.planning/phases/096-eval-harness-cross-provider-verification-concurrency/096-06-SUMMARY.md`
</output>
