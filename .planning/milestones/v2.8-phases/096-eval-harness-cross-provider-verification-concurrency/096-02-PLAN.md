---
phase: 096-eval-harness-cross-provider-verification-concurrency
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_096_ci_workflow_regression.py
autonomous: true
requirements: [EVAL-01]
tags: [ci, harness, testing, provider-gateway]

must_haves:
  truths:
    - "Every push to backend/** runs a deterministic full-workflow harness regression with a fake provider — no secrets, no network, never flaky (D-01 part 1)"
    - "A structural break in phase sequencing, gate retry, whitelist enforcement, or resume 2-phase writes turns CI red"
    - "The fake provider is injected at the gateway seam (open_stream), so the REAL engine, task_service drain loop, and dispatch_tool whitelist guard execute (093 mock-blind-spot lesson)"
  artifacts:
    - path: "backend/tests/test_096_ci_workflow_regression.py"
      provides: "D-01 CI structural gate — full multi-phase workflow journey with fake gateway adapter"
      contains: "test_096_ci_workflow_regression"
      min_lines: 150
  key_links:
    - from: "backend/tests/test_096_ci_workflow_regression.py"
      to: "app.services.task_service.open_stream"
      via: "patch.object at the CONSUMED seam (not where defined)"
      pattern: "patch\\.object\\(task_service, \"open_stream\""
    - from: "backend/tests/test_096_ci_workflow_regression.py"
      to: "harness_engine.resume_stranded_workflows"
      via: "resume leg — interrupted between mark_phase_active and complete_phase, then swept"
      pattern: "resume_stranded_workflows"
---

<objective>
Build the D-01 part-1 CI structural gate: `backend/tests/test_096_ci_workflow_regression.py` drives a full multi-phase workflow through the REAL harness engine with a scripted fake provider at the gateway `open_stream` seam, asserting phase sequencing, gate retry, whitelist refusal, and resume 2-phase writes. It runs in the existing GitHub Actions pytest job (`.github/workflows/backend-tests.yml` already triggers on `backend/**` and boots real compose Redis — zero YAML edits required, verified in PATTERNS.md).

Purpose: "CI proves structure; operator proves providers" (CONTEXT specifics). Free, deterministic, no secrets — the standing regression gate so future work can't silently break the harness.
Output: one new pytest file, picked up automatically by CI.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/096-eval-harness-cross-provider-verification-concurrency/096-PATTERNS.md (Assignment 2 — the three composed analogs)
@backend/tests/unit/test_085_task_service.py (lines 546-619 — _SyncEventStream + _make_open_stream_stub + patch.object(task_service, "open_stream", stub) at :603)
@backend/tests/test_harness_resume.py (lines 274-330 test_sweep_reruns_active_phase; :417-476 CAS-claim tests)
@backend/tests/conftest.py (mock_asyncpg_pool :495-502; _FakeRedis/_FakePubSub :505-547; four_seed_defs :826)
@backend/app/services/provider_gateway/events.py (GatewayEvent canonical schema the fake must speak)
@backend/app/services/provider_gateway/dispatcher.py (open_stream contract: (provider, request) -> (stream, CallingMode))

<interfaces>
From backend/tests/unit/test_085_task_service.py:546-574 (generalize, don't duplicate):
```python
class _SyncEventStream:
    """Bare SYNC generator-like stream (IN-05 shape): iterable + sync .close().
    _stream_one_iteration MUST drive it with `for event in stream:` (never async for)."""
    def __init__(self, events): self._events = list(events); self.closed = False
    def __iter__(self): yield from self._events
    def close(self): self.closed = True

def _make_open_stream_stub(events, calling_mode, *, captured: dict):
    stream = _SyncEventStream(events)
    async def _stub(provider, request):
        captured["provider"] = provider; captured["request"] = request
        return stream, calling_mode
    return _stub, stream
```
Patch seam (test_085_task_service.py:603): `with patch.object(task_service, "open_stream", stub):` — patch where CONSUMED.

From backend/tests/conftest.py:
- `mock_asyncpg_pool` — `.calls` lists every `(sql, args)` in execution order; per-test returns via `set_fetchrow_result` / `set_fetch_result`. This IS the 2-phase-write-order assertion mechanism.
- `_FakeRedis` + `_FakePubSub` — records XADD/sadd/publish; drives ask_user subscribe→emit→block.
- `four_seed_defs` — builder for in-memory WorkflowDefinitions byte-mirroring migration 061; copy this shape for the in-test definition (the CI test does NOT need migration 066 — it builds its definition in-process).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fake gateway adapter + happy-path full-workflow journey</name>
  <files>backend/tests/test_096_ci_workflow_regression.py</files>
  <read_first>
    - backend/tests/unit/test_085_task_service.py (lines 540-620 — stub builder + patch seam, copy exactly)
    - backend/tests/conftest.py (mock_asyncpg_pool, _FakeRedis, four_seed_defs — fixture shapes)
    - backend/tests/test_harness_engine.py (lines 540-700 — how existing tests drive run_workflow; note they mock at the EXECUTOR seam, which this test must NOT do)
    - backend/app/services/provider_gateway/events.py (exact GatewayEvent dict/type shapes — text delta, tool_call, finish)
    - backend/app/services/harness_engine.py (run_workflow signature + the mark_phase_active/complete_phase 2-phase write helpers)
  </read_first>
  <action>
    Create `backend/tests/test_096_ci_workflow_regression.py` with:

    1. **A scripted fake gateway adapter** generalizing `_make_open_stream_stub`: a `ScriptedProvider` whose `open_stream(provider, request)` pops the NEXT per-call event script from a queue (one `_SyncEventStream` per LLM call), so a multi-phase workflow gets phase-appropriate streams in order. Events MUST byte-match the canonical GatewayEvent schema from `provider_gateway/events.py` (read it first — do not invent field names). Patch with `patch.object(task_service, "open_stream", ...)` — the CONSUMED seam (093 anti-pattern: NEVER mock `phase_types._stream_one_iteration` / `run_task_sub_agent`; the real engine + drain loop + dispatch_tool must run).

    2. **An in-process 5-type WorkflowDefinition** built with the `four_seed_defs` builder shape (slug e.g. `ci_coverage`): programmatic `split_topic` (NOT eval_slow_step — keeps this plan independent of Plan 01 and the test fast) → `llm_batch_agents` (available_tools ["search_documents"], max_parallel_agents 5) → `llm_agent` (with one regex_match validator: `{"kind": "regex_match", "config": {"pattern": "VERIFIED"}, "on_failure": "retry", "max_retries": 2}` — copy 061 seed 2's exact validator shape) → `llm_human_input` → `llm_single`.

    3. **`test_096_ci_workflow_regression_happy_path`**: drive `harness_engine.run_workflow(...)` with `mock_asyncpg_pool` + `_FakeRedis`. The script: batch sub-agents emit a text delta + one whitelisted `search_documents` tool_call + finish; llm_agent emits output containing "VERIFIED" (gate passes first try in THIS test); human_input answered via Pitfall-5 determinism — monkeypatch `subscribe_for_response` ONLY (pre-published answer; the one seam where determinism beats realism — its real path is covered by the operator restart smoke); llm_single emits final text + finish. Wrap every await in `asyncio.wait_for(..., timeout=30)`.
       Assertions:
       - Phase sequencing: from `pool.calls` SQL order — `mark_phase_active` precedes `complete_phase` for EACH phase, and phases activate in phase_index order 0→4.
       - Tool round-trip: the fake's captured requests show the `search_documents` result fed back on the following call.
       - All 5 phases reach completed state writes; the run terminalizes completed.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && venv/Scripts/python.exe -m pytest tests/test_096_ci_workflow_regression.py -x -q -k happy_path</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "patch.object(task_service, \"open_stream\"" backend/tests/test_096_ci_workflow_regression.py` >= 1
    - `grep -c "_stream_one_iteration\|run_task_sub_agent" backend/tests/test_096_ci_workflow_regression.py` == 0 in mock/patch targets (executor seam never mocked — grep proof; references in comments allowed only if prefixed "NEVER")
    - `grep -c "asyncio.wait_for" backend/tests/test_096_ci_workflow_regression.py` >= 3 (bounded awaits — Pitfall 5)
    - pytest -k happy_path exits 0 in < 60s
  </acceptance_criteria>
  <done>Happy-path journey green: real engine + real drain loop, fake provider at the gateway seam, 5 phases sequenced and completed.</done>
</task>

<task type="auto">
  <name>Task 2: Structural-break legs — gate retry, whitelist refusal, resume 2-phase writes</name>
  <files>backend/tests/test_096_ci_workflow_regression.py</files>
  <read_first>
    - backend/tests/test_harness_resume.py (lines 274-330 — test_sweep_reruns_active_phase, the resume-leg model; :417-476 CAS claim)
    - backend/app/services/harness_engine.py (resume_stranded_workflows :1085-1180; gate retry handling)
    - backend/app/services/harness/phase_types.py (dispatch_tool whitelist guard path — how a non-whitelisted tool call is refused)
  </read_first>
  <action>
    Extend the same file with three more tests (same fixtures/fake):

    1. **`test_096_gate_retry_bounded`**: the llm_agent phase script emits an output WITHOUT "VERIFIED" first, then WITH "VERIFIED" on the retry call. Assert: exactly 2 LLM calls for that phase (bounded retry honored, max_retries=2 not exceeded), phase ultimately completes, and a gate-failure audit/event write exists in `pool.calls` between the two.

    2. **`test_096_whitelist_refusal`**: a batch sub-agent script emits a tool_call for a NON-whitelisted tool (e.g. `execute_code` when available_tools=["search_documents"]). Assert: the REAL `dispatch_tool` guard refuses it (a refusal-shaped tool_result is fed back to the model — read phase_types.py for the exact refusal shape and assert its marker string), the phase does NOT crash, and no `execute_code` side-effect write appears in `pool.calls`.

    3. **`test_096_resume_two_phase_writes`**: model on `test_sweep_reruns_active_phase` (:274-330). Drive the workflow and cancel the run task at a scripted event BETWEEN a phase's `mark_phase_active` and `complete_phase` (e.g. the fake's stream raises asyncio.CancelledError mid-phase after the active write is observed in pool.calls). Then seed the pool state the sweep expects (a phase left `active`) and call `await harness_engine.resume_stranded_workflows(pool, redis)`. Assert: the active phase re-runs from the top (a SECOND mark_phase_active for the same phase appears in pool.calls), no phase is skipped, and the CAS claim write precedes the re-run (copy the claim assertion pattern from :417-476).

    Keep the greppable name prefix `test_096_` on every test. All awaits bounded via asyncio.wait_for. NO edits to `.github/workflows/backend-tests.yml` — it already picks the file up (verified: triggers on backend/**, runs `pytest tests -q`, boots compose Redis). Record in the SUMMARY that zero YAML edits were needed.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && venv/Scripts/python.exe -m pytest tests/test_096_ci_workflow_regression.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_096_" backend/tests/test_096_ci_workflow_regression.py` == 4 (happy_path, gate_retry_bounded, whitelist_refusal, resume_two_phase_writes)
    - Full file pytest run exits 0 in < 120s with no network access (run once with no provider keys in env to prove: `env -i` style or assert no requests import used for provider calls)
    - `grep -c "resume_stranded_workflows" backend/tests/test_096_ci_workflow_regression.py` >= 1
    - `git diff --stat .github/workflows/backend-tests.yml` is empty (zero YAML edits)
    - Net-new backend failures == 0 vs pre-plan commit on the touched surface (no existing test file modified)
  </acceptance_criteria>
  <done>All 4 structural legs green and deterministic; CI picks the file up with zero workflow YAML changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI logs (public-ish) | pytest output lands in GitHub Actions logs |
| Gateway seam | the fake adapter substitutes the provider network boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-096-02-01 | Information disclosure | CI logs | mitigate | Test uses NO provider keys, NO GitHub secrets (D-01 explicit); fake provider only — grep test file for `_API_KEY` == 0 |
| T-096-02-02 | Tampering | whitelist guard | mitigate | test_096_whitelist_refusal asserts the REAL dispatch_tool guard refuses non-whitelisted calls (this test IS the mitigation's regression lock) |
| T-096-02-03 | Repudiation | double-executed phases | mitigate | resume leg asserts 2-phase-write ordering via pool.calls (mark_phase_active before complete_phase; sweep re-run visible) |
</threat_model>

<verification>
- `venv/Scripts/python.exe -m pytest tests/test_096_ci_workflow_regression.py -q` exits 0, < 120s, no network
- Executor-seam mocks absent (grep); gateway-seam patch present
- backend-tests.yml untouched
</verification>

<success_criteria>
- D-01 part 1 delivered: a push that breaks sequencing, gate retries, whitelist enforcement, or resume 2-phase writes fails CI deterministically
- The 093 mock-blind-spot is structurally closed at the gateway seam
</success_criteria>

<output>
After completion, create `.planning/phases/096-eval-harness-cross-provider-verification-concurrency/096-02-SUMMARY.md`
</output>
