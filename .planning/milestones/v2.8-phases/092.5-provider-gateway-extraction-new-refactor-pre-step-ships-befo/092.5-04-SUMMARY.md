---
phase: 092.5-provider-gateway-extraction
plan: 04
wave: 3
type: execute
autonomous: false
status: complete
verdict: PASS (byte-identical — clean-providers cut)
completed: 2026-06-01
requirements: [GATEWAY-01]
---

# Plan 092.5-04 Summary — PROOF GATE #1 (clean-providers byte-identical)

**Checkpoint plan (autonomous: false).** Operator-run live SSE proof that Deep is byte-identical
after the Wave-2 extraction of the two CLEAN providers (Anthropic + Google). **Verdict: PASS.**

## What happened

The live `capture_sse_baseline.py --mode after` returned `SSE_DIFF_RESULT: BLOCK` (6/7 native-7
providers showed 1–3 structural skeleton edits; anthropic was CLEAN). Per the plan's own protocol
("re-run 2–3× to rule out LLM tool-path non-determinism; a PERSISTENT diff is the blocker"), the
BLOCK was investigated and **ruled out as noise + one rate-limit capture failure** — NOT a regression.

## Evidence trail (why PASS despite the live BLOCK)

The byte-identical question was settled four independent ways:

1. **Re-run non-persistence (operator).** Targeted `--mode after --provider` re-runs:
   anthropic = CLEAN → 1 edit (a deleted assistant `delta`); google = 3 → 4 → 5 edits, **a different
   tool sequence every run**. Three same-code runs producing three different diffs IS the same-code
   noise floor — the signature of flaky agentic non-determinism, not a stable code edit.

2. **Offline skeleton classification.** For BOTH extracted providers the event-type GRAMMAR is
   byte-identical (anthropic 16 types / google 15 types, equal both sides), terminal classification
   is identical (`stream_end`+`done` both `error=None`), and the code-exec lifecycle is unchanged.
   Every edit is case-(a) tool-path selection or case-(b) delta/reasoning_delta presence. The 5
   non-extracted providers (openai/deepseek/moonshot/zhipu/minimax) run the UNCHANGED OpenAI-compat
   branch → cannot regress from this extraction by construction.

3. **Adversarial code-diff (decisive).** A refuter agent proved at the SOURCE level that the
   `agent_loop.py` change is verbatim/behavior-preserving (high confidence, zero behavioral deltas):
   diff isolated to lines 1393–1576; the OpenAI branch + all else byte-identical; the new shared
   `_on_chunk` == former `_on_chunk_google` statement-for-statement; the one added line group (Google
   `thought_signature` hydration) is a PROVEN no-op on Anthropic (anthropic_service emits no
   `thought_signature`). Every `_emit`, accumulator mutation, drain, and `close_fn` preserved; the 3
   subtle textual deltas (api_key getattr form, added `await open_stream`, inert `_calling_mode`) each
   checked and cleared.

4. **Moonshot row = 429, not code.** moonshot's `--mode after` failed with an OpenAI-org TPD
   rate-limit (current 1506263 / limit 1500000), truncating its stream to
   `['iteration_start','delta','error']`. A capture failure (untouched code anyway) — re-capture at
   the Plan 06 final gate after the TPD window resets.

## Acceptance

- SSE structural-skeleton diff: PASS-equivalent (BLOCK ruled out as non-persistent noise per protocol
  + proven verbatim at the code level). anthropic + google grammar/terminal/lifecycle byte-identical.
- Eval backstop: **deferred to the final gate (Plan 06)** — the eval is the slow/noisy secondary
  check; this is the low-risk clean cut and the SSE-diff + code-proof are decisive. (Operator-agreed.)
- E2E backstop: not run for this intermediate gate (orchestrator already proved seam tests GREEN +
  full suite zero net-new in Plan 03).

## Findings carried to Wave 4

- **Seam-contract under-specification (from the adversarial seam-test refuter).** The
  `test_provider_gateway_seam.py` Anthropic/Google groups RUN + PASS and assert I2
  (`thought_signature`), but only assert a 2–4-event SUBSET — not the full canonical vocabulary
  (`tool_preparing`, `tool_args_progress` incl. the 075.6-additive `code_so_far`, `usage`,
  `usage_delta`) nor inner key fields; the Google test asserts no type-order. Fine for byte-identical
  (adapters are proven verbatim pass-throughs), but the **seam contract Phase 093 consumes is
  under-specified.** → **Wave 4 / Plan 05 directive:** while authoring the OpenAI-compat seam tests,
  ALSO strengthen the anthropic/google seam tests to assert the full canonical vocabulary + key fields.
- **moonshot final-gate dependency:** Plan 06's full native-7 capture needs the moonshot TPD window
  to have room — schedule/retry accordingly.

## Self-Check: PASSED

- [x] Clean-providers cut proven byte-identical (4 independent legs; code-level proof decisive).
- [x] Live BLOCK correctly ruled out as tool-path noise + a 429 capture failure (per plan protocol).
- [x] No structural regression on either extracted provider.
- [x] Findings (seam-contract coverage, moonshot 429) captured for Wave 4 / Plan 06.

GATEWAY-01 stays Pending — closes at phase verification after Wave 4 + the Plan 06 final gate.
