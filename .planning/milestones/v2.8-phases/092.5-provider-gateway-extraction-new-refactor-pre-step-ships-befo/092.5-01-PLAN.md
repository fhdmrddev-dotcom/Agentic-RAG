---
phase: 092.5-provider-gateway-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/provider_gateway/__init__.py
  - backend/app/services/provider_gateway/events.py
  - backend/app/services/provider_gateway/dispatcher.py
  - backend/tests/unit/test_provider_gateway_seam.py
autonomous: true
requirements: [GATEWAY-01]

must_haves:
  truths:
    - "The provider_gateway/ package exists with a canonical event schema and a dispatcher entrypoint (no behavior wired into agent_loop yet)."
    - "The D-08 seam-contract test file exists and COLLECTS green (importorskip RED scaffold) before any adapter lands."
    - "The canonical event schema losslessly carries every field today's _on_chunk_* handlers consume (incl. code_so_far, thought_signature, reasoning_content)."
  artifacts:
    - path: "backend/app/services/provider_gateway/events.py"
      provides: "Canonical GatewayEvent TypedDict union (delta/reasoning_delta/tool_preparing/tool_args_progress/tool_start/finish/usage/usage_delta)"
      contains: "GatewayEvent"
      min_lines: 50
    - path: "backend/app/services/provider_gateway/dispatcher.py"
      provides: "open_stream(provider, request) -> (AsyncIterator[GatewayEvent], CallingMode) thin entrypoint"
      contains: "def open_stream"
    - path: "backend/app/services/provider_gateway/__init__.py"
      provides: "Package exports: open_stream + GatewayEvent types + CallingMode re-export"
      contains: "open_stream"
    - path: "backend/tests/unit/test_provider_gateway_seam.py"
      provides: "D-08 deterministic seam-contract test scaffold (synthetic-stream-in → canonical-events-out)"
      contains: "importorskip"
  key_links:
    - from: "backend/app/services/provider_gateway/dispatcher.py"
      to: "backend/app/services/provider_gateway/events.py"
      via: "import GatewayEvent / CallingMode carrier"
      pattern: "from .events import|from app.services.provider_gateway.events import"
    - from: "backend/app/services/provider_gateway/dispatcher.py"
      to: "app.services.openai_service.CallingMode"
      via: "re-export of the existing CallingMode enum (no new enum)"
      pattern: "CallingMode"
---

<objective>
Create the `provider_gateway/` package skeleton — the one home for provider logic (D-01) — WITHOUT touching `agent_loop.py` behavior yet. This plan lands the canonical event schema (D-02 seam contract), the thin `open_stream` dispatcher entrypoint (the `(stream, calling_mode)` shape 093 consumes), and the D-08 seam-contract test scaffold (RED, collects green before adapters exist). No Deep behavior changes in this plan — it is pure new-file authoring, so the byte-identical proof is trivially satisfied (no edit to the loop).

Purpose: Give Waves 2/4 a typed target to extract INTO, and give 093 the seam contract to consume. The schema and dispatcher signature are the load-bearing decisions; getting them right here means the verbatim adapter moves later are mechanical.
Output: `provider_gateway/{__init__,events,dispatcher}.py` + `tests/unit/test_provider_gateway_seam.py` (scaffold).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-CONTEXT.md
@.planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-RESEARCH.md
@.planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-PATTERNS.md

<interfaces>
<!-- The canonical event schema ALREADY EXISTS as a docstring. Copy it; do not invent. -->
<!-- Source: backend/app/services/google_service.py:1-19 (THE template), anthropic_service.py:6-14 (same shape) -->

The canonical event vocabulary all 3 adapters emit (verbatim from google_service.py:7-14):
```python
{"type": "delta",              "content": str}                                              # assistant text
{"type": "tool_preparing",     "id": str, "name": str, "index": int}                        # name known, args still streaming
{"type": "tool_args_progress", "tool_index": int, "name": str, "args_so_far": str,
                               "total_args_bytes_so_far": int}                               # 5KB-boundary progress
{"type": "tool_start",         "id": str, "name": str, "args": dict}                         # args complete (Anthropic/Google)
{"type": "finish",             "finish_reason": str, "tool_calls": list[dict]}              # tool_calls carry thought_signature (Google)
{"type": "usage",              "input_tokens": int, "output_tokens": int}
{"type": "usage_delta",        "output_tokens": int}
```

TWO fields the docstring schema OMITS but the consumer DOES consume (lossless-superset requirement, CONTEXT D-02 discretion note — PATTERNS.md lines 44-46):
- `code_so_far: str` on `tool_args_progress` — added Phase 075.6; consumed at agent_loop.py:1508, :1621, :1923 (all 3 branches do `.get("code_so_far", "")`). ToolArgsProgressEvent MUST carry it.
- `thought_signature: str` (Google base64 sig) — carried on `finish.tool_calls[]` and hydrated at agent_loop.py:1644-1647. ToolStartEvent + FinishEvent.tool_calls MUST carry it (else Gemini-3 400s on round 2 — invariant I2/D-07).

ONE more event type the OpenAI path emits today that the docstring omits:
- `{"type": "reasoning_delta", "content": str}` — emitted only by the OpenAI path today (DeepSeek reasoning_content + `<think>`-stripped Kimi/MiniMax/GLM). Source: agent_loop.py:1836-1839 + the `<think>` machine :1802-1830. ReasoningDeltaEvent is first-class in the schema (the harness/094 renders it).

The existing CallingMode enum + the (stream, calling_mode) contract to MIRROR (do NOT define a new enum):
```python
# Source: openai_service.py:1197-1205 + :1155 (resolve_calling_mode)
from app.services.openai_service import CallingMode  # NATIVE | STRUCTURED (existing enum)
def create_adaptive_streaming_chat(...) -> tuple:   # returns (stream, calling_mode)
```
RESEARCH Open Q1 recommendation: `open_stream(provider, request) -> (AsyncIterator[GatewayEvent], CallingMode)`, with Anthropic/Google returning `CallingMode.NATIVE`. Mirrors the existing OpenAI contract — minimal consumer churn (Pitfall 3: dropping calling_mode is the exact bug that makes the harness OpenAI-only).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the canonical event schema (events.py)</name>
  <read_first>
    - backend/app/services/google_service.py:1-19 (THE canonical-event template docstring — copy field names exactly)
    - backend/app/services/anthropic_service.py:6-14 (same schema, older/narrower — cross-check)
    - .planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-RESEARCH.md §"Proposed Canonical Event Schema" (lines 268-328, the recommended TypedDict union + lossless-superset checklist)
    - .planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-PATTERNS.md §"provider_gateway/events.py" (lines 26-49)
  </read_first>
  <action>
    Create `backend/app/services/provider_gateway/__init__.py` (empty package marker for now — Task 2 fills exports) and `backend/app/services/provider_gateway/events.py`.

    In `events.py`, define a TypedDict-per-type `Union` named `GatewayEvent` (D-02 discretion: TypedDict, per RESEARCH recommendation). Define EXACTLY these 8 event types with EXACTLY these fields (lossless superset — copy field names verbatim from the google_service docstring + the two omitted fields + reasoning_delta):

    - `DeltaEvent`: `type: Literal["delta"]`, `content: str`
    - `ReasoningDeltaEvent`: `type: Literal["reasoning_delta"]`, `content: str`  (OpenAI-path only today: DeepSeek reasoning_content + `<think>`-stripped Kimi/MiniMax/GLM)
    - `ToolPreparingEvent`: `type: Literal["tool_preparing"]`, `id: str`, `name: str`, `index: int`
    - `ToolArgsProgressEvent`: `type: Literal["tool_args_progress"]`, `tool_index: int`, `name: str`, `args_so_far: str`, `total_args_bytes_so_far: int`, `code_so_far: str`  (code_so_far is 075.6-additive — MANDATORY for losslessness)
    - `ToolStartEvent`: `type: Literal["tool_start"]`, `id: str`, `name: str`, `args: dict`, and `thought_signature: NotRequired[str]` (Google base64 sig — MANDATORY for I2/D-07)
    - `FinishEvent`: `type: Literal["finish"]`, `finish_reason: str`, `tool_calls: list[dict]`  (each dict may carry thought_signature)
    - `UsageEvent`: `type: Literal["usage"]`, `input_tokens: int`, `output_tokens: int`
    - `UsageDeltaEvent`: `type: Literal["usage_delta"]`, `output_tokens: int`

    Then `GatewayEvent = Union[DeltaEvent, ReasoningDeltaEvent, ToolPreparingEvent, ToolArgsProgressEvent, ToolStartEvent, FinishEvent, UsageEvent, UsageDeltaEvent]`. Use `from typing import TypedDict, Literal, Union, NotRequired` (NotRequired is py3.11+; if the interpreter is <3.11, use `typing_extensions.NotRequired` — check `backend/venv` python version first).

    Add a module docstring stating: "Canonical provider event schema (D-02 seam contract). Lossless superset of the 3 producers' events — the contract Phase 093 consumes. DO NOT re-shape Anthropic/Google events 'for consistency' — they are already this template (RESEARCH anti-pattern)." Do NOT add a `calling_mode` event type — calling_mode rides ALONGSIDE the stream (Task 2), not as an event (Pitfall 3 / L-3).

    NO import of `agent_loop`/`threads.py` (SC#3 — no import cycle). `events.py` imports only from `typing`/`typing_extensions`.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -c "from app.services.provider_gateway.events import GatewayEvent, ToolArgsProgressEvent, ToolStartEvent, ReasoningDeltaEvent; import typing; print('OK', 'code_so_far' in ToolArgsProgressEvent.__annotations__, 'thought_signature' in ToolStartEvent.__annotations__)"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/services/provider_gateway/events.py` exists and `grep -c "GatewayEvent" backend/app/services/provider_gateway/events.py` >= 2 (the Union def + at least one reference).
    - `grep -c "code_so_far" backend/app/services/provider_gateway/events.py` >= 1 AND `grep -c "thought_signature" backend/app/services/provider_gateway/events.py` >= 1 AND `grep -c "reasoning_delta" backend/app/services/provider_gateway/events.py` >= 1 (lossless superset).
    - The Task 1 verify command prints `OK True True`.
    - `grep -c "import agent_loop\|from app.services.agent_loop\|import threads\|from app.api.threads" backend/app/services/provider_gateway/events.py` == 0 (no import cycle).
  </acceptance_criteria>
  <done>events.py exports GatewayEvent (8-member union) carrying code_so_far + thought_signature + reasoning_delta; imports nothing from agent_loop/threads; Python imports it cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Author the thin dispatcher entrypoint (dispatcher.py + package exports)</name>
  <read_first>
    - backend/app/services/openai_service.py:1197-1205 (create_adaptive_streaming_chat — the (stream, calling_mode) RETURN CONTRACT to mirror) + :1155 (resolve_calling_mode)
    - backend/app/services/agent_loop.py:1386-1665 (the while-True retry block: Anthropic branch :1396, Google branch :1530, OpenAI-compat branch :1657 — the dispatch site that will call open_stream in Wave 2/4)
    - .planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-RESEARCH.md §"Open Questions" #1 (lines 509-512, the recommended open_stream signature) + §"Recommended Project Structure" (lines 157-167, the no-import-cycle constraint)
    - .planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-PATTERNS.md §"dispatcher.py" (lines 52-70)
  </read_first>
  <action>
    Create `backend/app/services/provider_gateway/dispatcher.py` with the thin entrypoint. This plan ships the SIGNATURE + the dispatch skeleton ONLY — the adapter bodies land in Wave 2 (Anthropic/Google) and Wave 4 (OpenAI). Define:

    ```python
    async def open_stream(provider: str, request: "GatewayRequest") -> tuple[AsyncIterator[GatewayEvent], CallingMode]:
        ...
    ```
    where the dispatcher picks the adapter by `provider` (active_provider_name: "anthropic" → anthropic adapter, "google" → google adapter, everything else → openai_compat adapter — match the live branch logic at agent_loop.py:1396/:1530/:1657 where the OpenAI-compat path is the `else`). Re-export `CallingMode` from `app.services.openai_service` (do NOT define a new enum — Pitfall 3). Anthropic/Google adapters return `CallingMode.NATIVE`.

    Define a `GatewayRequest` dataclass/TypedDict in dispatcher.py (or events.py — your call) carrying the fields the 3 stream constructions need today: `messages: list[dict]`, `model: str`, `tools: list[dict]`, `system_prompt: str`, `force_no_tools: bool`, `max_tokens: int | None`, `api_key: str`, `user_settings`, `tool_choice: str`, plus `active_provider_name: str`. (Source fields: stream_anthropic call agent_loop.py:1415-1422, stream_google :1553-1561, create_adaptive_streaming_chat :1659-1665.) This is the request envelope the consumer fills and Waves 2/4 destructure inside each adapter.

    For THIS plan, the adapter dispatch bodies raise `NotImplementedError("anthropic adapter lands in Wave 2 / openai_compat in Wave 4")` — they are stubs. The dispatcher's job here is to fix the SHAPE (signature, provider→adapter routing, CallingMode surfacing) so the seam test (Task 3) can assert against it and Waves 2/4 fill the bodies. Do NOT wire agent_loop to call this yet (that is Wave 2).

    Fill `__init__.py` to export: `open_stream`, `GatewayRequest`, `GatewayEvent` (+ the per-type event TypedDicts), and `CallingMode` (re-exported). Add module docstrings noting D-01 (one home per provider) and the SC#3 no-import-cycle rule: the gateway MAY import anthropic_service/google_service/openai_service/tool_parser (below it in the graph) but NEVER agent_loop/threads.py.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -c "from app.services.provider_gateway import open_stream, GatewayRequest, GatewayEvent, CallingMode; import inspect; print('OK', inspect.iscoroutinefunction(open_stream) or callable(open_stream), CallingMode.NATIVE)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def open_stream" backend/app/services/provider_gateway/dispatcher.py` >= 1.
    - `grep -c "from app.services.openai_service import" backend/app/services/provider_gateway/dispatcher.py` >= 1 AND `grep -c "class CallingMode\|CallingMode =" backend/app/services/provider_gateway/dispatcher.py` == 0 (re-export, no new enum).
    - `grep -rc "import agent_loop\|from app.services.agent_loop\|from app.api.threads\|import threads" backend/app/services/provider_gateway/` == 0 (no import cycle — SC#3).
    - The Task 2 verify command prints a line starting with `OK True` and shows `CallingMode.NATIVE`.
    - `backend/venv/Scripts/python.exe -c "from app.services.provider_gateway import GatewayRequest; print(GatewayRequest)"` succeeds (the request envelope exists).
  </acceptance_criteria>
  <done>open_stream(provider, request) -> (AsyncIterator, CallingMode) is importable from the package; routes provider→adapter; CallingMode is re-exported (not redefined); GatewayRequest envelope exists; adapter bodies are NotImplementedError stubs; zero import cycle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author the D-08 seam-contract test scaffold (RED, collects green)</name>
  <read_first>
    - backend/tests/unit/test_chunk_handler_provider_aware.py:63-104 (the synthetic-SimpleNamespace-chunk driver _mk_chunk + _drive — the canonical "feed synthetic provider chunks, assert output" idiom to copy)
    - backend/tests/unit/test_075_5_google_native.py:38-58 (synthetic-fixture translation asserts — per-aspect test naming + inline-import idiom)
    - backend/tests/unit/test_anthropic_service.py:11-16 (the pytest.importorskip TDD-RED scaffold pattern)
    - .planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-PATTERNS.md §"test_provider_gateway_seam.py" (lines 198-222)
    - .planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-VALIDATION.md (the GATEWAY-01 SC#3 / D-08 row + the Per-Task Verification Map)
  </read_first>
  <behavior>
    - The test file COLLECTS green NOW (before adapters exist) via `pytest.importorskip("app.services.provider_gateway.anthropic", reason="anthropic adapter lands Wave 2")` etc. — so `pytest --collect-only` does not error.
    - Each adapter gets a test that feeds a synthetic SDK chunk stream IN and asserts the YIELDED canonical GatewayEvent sequence (event types + key fields) matches the expected list. Author the EXPECTED-sequence assertions now (they describe the contract); they SKIP until the adapter module exists, then go GREEN in Waves 2/4.
    - At minimum scaffold: `test_anthropic_adapter_yields_canonical_events`, `test_google_adapter_yields_canonical_events` (+ thought_signature on finish.tool_calls), `test_openai_adapter_yields_delta_and_tool_preparing`, `test_openai_adapter_think_tag_routes_to_reasoning_delta`, `test_openai_adapter_surfaces_calling_mode`, `test_openai_adapter_usage_via_accumulate_chunk_usage`.
  </behavior>
  <action>
    Create `backend/tests/unit/test_provider_gateway_seam.py`. Copy the `_mk_chunk()` SimpleNamespace factory + the `_drive(chunks, provider)` idiom from `test_chunk_handler_provider_aware.py:63-104` and adapt it to: build synthetic provider SDK chunks (OpenAI: `SimpleNamespace(choices=[...], usage=...)`; Anthropic/Google: synthetic event dicts matching their stream_* output), feed them through each adapter's stream construction (mocking the raw SDK boundary `stream_anthropic`/`stream_google`/`create_adaptive_streaming_chat` with the synthetic stream), and collect the adapter's yielded GatewayEvents into a list.

    Use `pytest.importorskip("app.services.provider_gateway.<adapter>", reason="...lands in Wave N")` at the top of each adapter's test group so the file COLLECTS GREEN before the adapters exist (Wave 0 authoring), then runs RED→GREEN as each adapter lands. The events.py + dispatcher.py from Tasks 1-2 DO exist, so import GatewayEvent + open_stream + CallingMode directly (no importorskip for those).

    Author the EXPECTED canonical-event sequences as concrete assertions (e.g. for the OpenAI `<think>` test: feed chunks with `<think>reasoning</think>visible`, assert the yielded events are `[{"type":"reasoning_delta","content":"reasoning"}, {"type":"delta","content":"visible"}]` — no `<think>` leak into a `delta`, invariant I4). For the calling_mode test: assert `open_stream(...)` returns a 2-tuple whose `[1]` is a `CallingMode` member. NO live keys — fully deterministic, runs in CI.

    Set markers/structure so `pytest backend/tests/unit/test_provider_gateway_seam.py -x` exits 0 today (all importorskip-gated adapter tests SKIP; the events/dispatcher-shape tests PASS).
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -m pytest backend/tests/unit/test_provider_gateway_seam.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/unit/test_provider_gateway_seam.py` exists; `grep -c "importorskip" backend/tests/unit/test_provider_gateway_seam.py` >= 3 (Anthropic/Google/OpenAI adapter gates).
    - `grep -c "reasoning_delta" backend/tests/unit/test_provider_gateway_seam.py` >= 1 (the I4 `<think>` contract is asserted) AND `grep -c "thought_signature" backend/tests/unit/test_provider_gateway_seam.py` >= 1 (I2 contract).
    - `pytest backend/tests/unit/test_provider_gateway_seam.py -x -q` exits 0 (collects green; adapter tests SKIP, shape tests PASS).
    - `grep -c "calling_mode\|CallingMode" backend/tests/unit/test_provider_gateway_seam.py` >= 1 (the Pitfall-3 surfacing contract is asserted).
  </acceptance_criteria>
  <done>The D-08 seam test exists, collects/exits 0 today, asserts the per-adapter canonical-event sequences (incl. I4 `<think>`-routing, I2 thought_signature, calling_mode surfacing), gated by importorskip so it goes RED→GREEN as Waves 2/4 land adapters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer (`agent_loop`) → gateway | In-process Python call; the request envelope crosses here. No network/user boundary — both run in the same backend worker with the same RLS-scoped context the consumer already holds. |
| gateway → raw SDK services | In-process; the adapters wrap existing `stream_*` boundaries that already handle provider keys. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-092.5-01 | Information Disclosure | events.py / dispatcher.py | accept | Pure code-relocation refactor — no new attack surface (RESEARCH §Security Domain). The schema/dispatcher carry NO secrets; provider keys stay in the existing `user_settings.llm_api_key or settings.llm_api_key` resolution path (unchanged). No new auth/session/DB/crypto path. `thought_signature` is base64 transport (not crypto), schema-only here. |
| T-092.5-02 | Tampering | test_provider_gateway_seam.py | accept | Deterministic unit test with synthetic fixtures, no live keys, no DB writes — zero runtime surface. |

**Statement (per security_gate):** This plan introduces NO HIGH-severity threats. It adds typed schema + a dispatcher skeleton + a deterministic test. All security controls (RLS on messages/runs/threads, tool-arg Pydantic validation via the existing parse_structured_tool_calls, secret resolution) are UNCHANGED and live consumer-side, untouched by this plan. The v2.8 closure SECURITY PASS anchors on 092, not here.
</threat_model>

<verification>
- `provider_gateway/` package importable; `open_stream` + `GatewayEvent` + `GatewayRequest` + `CallingMode` exported.
- events.py schema is a lossless superset (code_so_far, thought_signature, reasoning_delta present).
- Seam-contract test collects + exits 0 (RED scaffold via importorskip).
- Zero import cycle: `grep -rc "from app.services.agent_loop\|from app.api.threads\|import agent_loop\|import threads" backend/app/services/provider_gateway/` == 0.
- Full suite is NOT degraded by this plan (no edit to agent_loop): `backend/venv/Scripts/python.exe -m pytest backend/tests/ -q` shows zero net-new failures vs the pre-plan baseline.
</verification>

<success_criteria>
- The `provider_gateway/` package exists with events.py (canonical schema), dispatcher.py (open_stream skeleton + GatewayRequest envelope + CallingMode re-export), __init__.py (exports).
- The D-08 seam-contract test exists and collects/exits 0 (RED scaffold).
- NO agent_loop.py edit in this plan → Deep is trivially byte-identical (no behavior change to prove yet; the proof gates are Waves 3/5).
- No import cycle (SC#3 callable-injection seam preserved).
</success_criteria>

<output>
After completion, create `.planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-01-SUMMARY.md`
</output>
