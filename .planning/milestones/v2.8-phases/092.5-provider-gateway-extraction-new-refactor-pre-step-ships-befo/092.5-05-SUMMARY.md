---
phase: 092.5-provider-gateway-extraction
plan: 05
subsystem: api
tags: [provider-gateway, openai-compat, streaming, sse, agent-loop, byte-identical, refactor]

# Dependency graph
requires:
  - phase: 092.5-03
    provides: the shared provider-agnostic `_on_chunk` (Anthropic+Google collapsed) that the OpenAI events now fold into; the GatewayRequest envelope + open_stream dispatch the OpenAI else-branch extends
  - phase: 092.5-04
    provides: PROOF GATE #1 PASS (clean-providers byte-identical) — the verified-stable base this high-risk OpenAI extraction builds on
provides:
  - backend/app/services/provider_gateway/openai_compat.py — the entangled OpenAI/OpenRouter/Ollama adapter (wraps create_adaptive_streaming_chat, owns the <think> machine + reasoning_content routing + _accumulate_chunk_usage + per-provider 5KB boundary dicts, surfaces calling_mode, emits canonical events)
  - agent_loop's OpenAI else-branch dispatching through open_stream; the third _on_chunk folded into the ONE shared consumer handler
  - all 3 provider families (clean: anthropic/google + compat: openai/openrouter/ollama/deepseek/moonshot/zhipu/minimax) now dispatch through ONE gateway home
  - the seam (open_stream + canonical events + surfaced calling_mode) fully shaped for Phase 093 to CONSUME
affects: [092.5-06, 093, harness-cross-provider-parity, openai_compat]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Entangled-adapter extraction = stream-construction + normalization MOVE-IN (the adapter owns <think>/reasoning/usage/boundary, EMITS canonical events) while consumer accumulators (full_content, tool_calls_buffer) + parse_structured_tool_calls post-parse + STRUCTURED injection + retry/cap-pause STAY consumer-side (L-1/L-3/L-5)"
    - "calling_mode is SURFACED through the seam ((stream, calling_mode) tuple) — the structural fix for the bug that made the harness OpenAI-only (Pitfall 3)"
    - "Per-stream adapter-local state (_in_think_block, usage totals) replaces the original per-iteration nonlocal reset — same scope (one stream = one tracker), verified equivalent"

key-files:
  created: []
  modified:
    - backend/app/services/provider_gateway/openai_compat.py  # Task 1 — filled the Plan-01 stub (402 lines): adapter + _accumulate_chunk_usage + <think> machine + boundary dicts
    - backend/app/services/provider_gateway/dispatcher.py      # Task 1 — last NotImplementedError stub removed; else-branch -> open_openai_compat_stream
    - backend/app/services/agent_loop.py                       # Task 2 — OpenAI else-branch dispatches via open_stream; _on_chunk_openai folded into shared _on_chunk
    - backend/app/api/threads.py                               # Task 2 — incidental wiring (12 lines)
    - backend/tests/unit/test_chunk_handler_provider_aware.py  # Task 3 — _accumulate_chunk_usage import repointed
    - backend/tests/unit/test_provider_gateway_seam.py         # Task 3 — OpenAI adapter groups flipped live (I4/Pitfall-3/I8/Open-Q2)
    - backend/tests/unit/test_explorer_agent.py                # Task 3 — 6 create_adaptive patch targets repointed
    - "(+19 integration/router tests — create_adaptive_streaming_chat patch-target sweep)"

key-decisions:
  - "VERBATIM move only — NO 'while-I'm-in-here' cleanup (agent_loop.py:15-19 warns this re-opens the 075.x cascade). The adapter is a relocation, not a rewrite."
  - "_accumulate_chunk_usage moved with BOTH branches intact (Google-cumulative-overwrite + OpenAI-+=) even though this is the OpenAI adapter — a re-derive would 2-3x over-count Google billing; the I8 unit test exercises both."
  - "Adapter emits NO synthetic tool_start (Open Q2) — it emits tool_preparing + tool_args_progress; the consumer rebuilds tool_calls_buffer. A synthetic tool_start would add a skeleton token the OpenAI path lacks."
  - "parse_structured_tool_calls(full_content) post-parse + STRUCTURED messages injection + provider-error retry/cap-pause/end_turn/empty-retry/round-trip persistence ALL stay consumer-side verbatim (they touch consumer accumulators)."

patterns-established:
  - "Rigorous zero-net-new-failures proof via git-worktree A/B: run the full suite at the immediately-pre-plan commit in an isolated worktree, normalize node-ids, comm-diff against current — isolates true regressions from flaky live-infra variance."
  - "Adversarial byte-identical SOURCE review (6 independent refutation lenses + synthesis/completeness-critic) front-runs the live SSE proof gate; suspicious-but-not-breaking deltas become an explicit live-gate checklist."

requirements-completed: [GATEWAY-01]

# Metrics
duration: ~6h elapsed (source 14:47-14:55; verification + test sweep + close-out 21:04)
completed: 2026-06-01
---

# Phase 092.5 Plan 05 Summary — Entangled OpenAI-Compat Adapter Extracted (byte-identical)

**The entangled OpenAI/OpenRouter/Ollama branch (`<think>` machine + reasoning routing + provider-aware usage + per-provider 5KB boundaries) is extracted VERBATIM into `provider_gateway/openai_compat.py`, the third `_on_chunk` is folded into the ONE shared consumer handler, `calling_mode` is surfaced through the seam, and Deep is verified non-regressing via worktree A/B + a 6-lens adversarial byte-identical review (zero breaking deltas).**

## Performance
- **Duration:** source extraction 14:47→14:55 (8 min, Tasks 1+2); verification + monkeypatch sweep + close-out completed 21:04 (Task 3 + gates)
- **Completed:** 2026-06-01
- **Tasks:** 3 (all complete)
- **Files modified:** 27 (4 source + 23 test)

## Accomplishments
- **The hard adapter shipped byte-identically.** The genuinely entangled unit (D-04) — the OpenAI-compat branch + `_on_chunk_openai` + `_accumulate_chunk_usage` — now lives in one adapter that wraps `create_adaptive_streaming_chat` and emits canonical events.
- **One home for all providers.** With clean (Anthropic/Google) already extracted, all provider families now dispatch through `open_stream`; the dispatcher's last `NotImplementedError` stub is gone.
- **The harness-OpenAI-only bug is structurally fixed.** `calling_mode` is surfaced through the `(stream, calling_mode)` seam and consumed for the STRUCTURED skip/inject/post-parse — Phase 093 reaches parity by *consuming* this, not re-implementing it.
- **Verified non-regressing three ways** (see Verification Evidence) — the strongest close-out in the gateway extraction so far.

## Task Commits
Each task committed atomically:
1. **Task 1: Author `openai_compat.py` — move the entangled normalization in VERBATIM, surface calling_mode** — `8635d989` (feat)
2. **Task 2: Fold the OpenAI else-branch into the shared `_on_chunk`; post-parse + injection + retry stay consumer-side** — `14b75c96` (feat)
3. **Task 3: Repoint `_accumulate_chunk_usage` + `create_adaptive_streaming_chat` monkeypatch targets; flip seam OpenAI groups live** — `dfc2c915` (test)

## Files Created/Modified
- `backend/app/services/provider_gateway/openai_compat.py` — the extracted adapter (402 lines): stream construction, `<think>` state machine (moonshot/deepseek/minimax/zhipu), `reasoning_content` routing, `_accumulate_chunk_usage` (both branches), per-provider 5KB boundary dicts (OpenAI-native vs OpenRouter independent), `normalize_finish_reason`; surfaces `calling_mode`; emits `delta`/`reasoning_delta`/`usage`/`usage_delta`/`tool_preparing`/`tool_args_progress`/`finish`.
- `backend/app/services/provider_gateway/dispatcher.py` — else-branch → `open_openai_compat_stream`; final stub removed.
- `backend/app/services/agent_loop.py` — OpenAI else-branch dispatches via `await open_stream(...)`; `_on_chunk_openai` folded into the shared `_on_chunk`; consumer residue verbatim.
- `backend/app/api/threads.py` — incidental wiring (12 lines).
- `backend/tests/**` (23 files) — `_accumulate_chunk_usage` import repoint + ~21 `create_adaptive_streaming_chat` patch-target repoints + seam test OpenAI groups flipped live.

## Decisions Made
None beyond the plan — followed as specified (see `key-decisions` frontmatter). The extraction was a verbatim relocation with no scope creep.

## Deviations from Plan
None — plan executed exactly as written. All acceptance-criteria greps pass (the 2 grep hits over their `==0` thresholds — `parse_structured_tool_calls`×2 in `openai_compat.py`, `create_adaptive_streaming_chat`×3 in `agent_loop.py` — were verified to be docstring/comment references, not behavioral code; the dispatch site uses `await open_stream(...)`).

## Verification Evidence
The standout of this plan. Three independent proofs:

**1. Task-3 unit gate — GREEN.** `test_provider_gateway_seam.py` + `test_chunk_handler_provider_aware.py` = **17 passed, 0 skipped** (`importorskip` resolves now that the adapter exists). All five OpenAI contract tests live: `think_tag_routes_to_reasoning_delta` (I4), `surfaces_calling_mode` (Pitfall 3), `usage_via_accumulate_chunk_usage` (I8), `no_synthetic_tool_start` (Open Q2), `yields_delta_and_tool_preparing`.

**2. Full-suite zero-net-new — PROVEN via worktree A/B.** Ran the full suite at the pre-Plan-05 commit `4a4bd327` in an isolated git worktree (baseline = 99 failed), normalized node-ids, and `comm`-diffed against current (105 failed). The 6 comm-deltas (`test_075_code_stdout_progressive`×4, `test_077_multi_worker`×2) are **flaky live-infra, NOT Plan 05** — re-run proves it: root causes are a Postgres FK-race (`runs.thread_id` absent) + a 0.3s perf-threshold miss (`2.30s < 2.0s`), neither in the provider-stream path; the same tests come back **3 passed / 4 skipped (zero failures)** on the pre-Plan-05 code. (`test_077_*` was explicitly named flaky in Plan 03's baseline note.) **Zero net-new failures attributable to Plan 05.**

**3. Adversarial byte-identical SOURCE review — ZERO breaking deltas.** 6 independent refutation lenses + synthesis/completeness-critic (7 agents). Verdict: **RESIDUAL-RISK (proceed, flag for Plan 06 live gate)**.
   - `_accumulate_chunk_usage` is a **byte-identical `diff` move** (both branches); consumer-residue (retry/cap-pause/end_turn/empty-retry/round-trip) is a **zero-difference contiguous diff**; `<think>`/`reasoning_content`/5KB-boundary math are **character-for-character identical** with per-stream state correctly replacing the per-iteration nonlocal reset; dispatcher routes all compat providers incl. **Ollama**; no import cycle; no dangling refs; api_key path verbatim.
   - **2 suspicious (non-breaking) deltas → Plan 06 live-gate checklist** (by design — Plan 05 defers OpenAI-path byte-identical proof to the Wave 5 operator gate):
     1. **tool_call `id` capture** moved from last-write-per-delta → frozen-at-`tool_preparing`. Byte-identical for the standard `id`+`name`-in-first-delta wire; offline review can't rule out OpenRouter-proxied `name`-before-`id` or later `id` re-supply. → **Plan 06 must confirm the assistant-message `tool_call_id` is non-empty and round-trips on a live multi-tool turn per compat provider.**
     2. **failed-mid-stream usage retention** moved from run-scoped-nonlocal (retained across `except APIError → continue`) → per-stream (dropped if `_normalize` raises before the trailing `usage` yield). Zero divergence on real traffic (usage emitted only on the final `choices=[]` chunk per D-073-08). → **Plan 06 confirms token totals are non-zero + equal across a normal multi-iteration run per provider** (the contrived stray-usage-then-error case isn't worth a synthetic capture).

## Issues Encountered
**Pre-existing test-infra defect surfaced (NOT a Plan 05 blocker, NOT fixed here).** The 2 `test_066_langsmith_clean` failures (`test_no_generator_exit_on_timeout`, `test_track_a_clean_trace_exception`) fail in **both** trees (in the baseline failure set; the only Plan-05 change to that file is the mandatory Task-3 monkeypatch repoint). Root cause (standalone-reproduced by the review): `_run_helpers.py:69-88` build bare `MagicMock()` chunks that never set `.usage` → `getattr(chunk, "usage", None)` auto-vivifies a non-`None` MagicMock → `_accumulate_chunk_usage` returns MagicMock totals → `json.dumps` raises `TypeError: MagicMock not JSON serializable` → run finalizes `failed` instead of `timed_out`. Compounded by a stale timeout monkeypatch (`get_per_call_timeout` sync vs the `get_per_call_timeout_async` actually called). **Fix (test-only, ~2 lines, separate follow-up — matches `feedback_mock_completeness`):** set `chunk.usage = None` in both `_run_helpers` chunk factories, and repoint the timeout monkeypatch to `app.config.get_per_call_timeout_async`. Tracked for a small `/gsd:fast`-class fix; intentionally NOT folded into Plan 05 (pre-existing + out of scope).

## User Setup Required
None — no external service configuration. (The byte-identical proof for the OpenAI-path providers is the Plan 06 operator SSE gate, which requires live keys + a visible-terminal uvicorn restart.)

## Next Phase Readiness
- **Plan 06 (PROOF GATE #2) is unblocked.** The final operator-run native-7 SSE diff (`scripts/capture_sse_baseline.py --mode after`) is the live gate. **Carry forward the 2 suspicious-delta checks above** as explicit Plan 06 acceptance items (tool_call_id round-trip per compat provider; token totals non-zero + equal per provider). Also: the moonshot row needs the OpenAI-org TPD rate-limit window to have reset (it 429'd at the Plan 04 gate).
- **Phase 093 is structurally ready.** `open_stream` + canonical events + surfaced `calling_mode` are the seam 093 consumes for harness cross-provider parity — no re-implementation needed.
- **One known pre-existing test-infra defect** (`test_066_langsmith_clean`, above) to close in a small follow-up so the suite goes fully green.

---
*Phase: 092.5-provider-gateway-extraction*
*Plan: 05*
*Completed: 2026-06-01*
