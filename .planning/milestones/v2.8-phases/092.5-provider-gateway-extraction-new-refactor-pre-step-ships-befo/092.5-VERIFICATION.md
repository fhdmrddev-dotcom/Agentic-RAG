---
phase: 092.5-provider-gateway-extraction
status: passed_with_overrides
verified: 2026-06-01
requirements: [GATEWAY-01]
must_haves_verified: 3/3
code_review: { critical: 0, warning: 1, warning_resolved: 1, info: 5, info_deferred: 5 }
---

# Phase 092.5 — Provider Gateway Extraction — VERIFICATION

**Goal:** Extract `agent_loop.py`'s provider-streaming dispatch into ONE shared
`provider_gateway/` package (D-01 "one home per concern"), Deep byte-identical on the
native-7, surfacing `calling_mode` so Phase 093's harness sub-agent loop can consume the
seam without re-deriving provider logic. RED LINE: never break working things; Deep
byte-identical.

**Verdict: PASSED (with overrides).** GATEWAY-01 satisfied.

## Must-haves

| # | Must-have | Status | Evidence |
|---|---|---|---|
| 1 | Deep byte-identical on native-7 after the full extraction (incl. the entangled OpenAI-compat) | ✅ | Anthropic byte-IDENTICAL (0 skeleton edits); the other 6 native providers' residuals are exclusively LLM tool-path-count + chunk-cadence noise (no regression signature), confirmed by TWO independent adversarial workflows (`wf_b45e948c-45a` 8 agents on run-1; `wf_11648945-31c` 6 agents on run-2) + Plan 05's 6-lens review. Decisive cross-check: a shared-consumer regression would have hit Anthropic (its structural twin) — it diffed to zero. See `092.5-06-SUMMARY.md` + `092.5-06-BLOCK-TRIAGE.md`. |
| 2 | All 8 D-07 round-trip invariants (I1–I8) named with evidence | ✅ | Named in `092.5-06-SUMMARY.md` (I1 Anthropic tool-exec, I2 Google thought_signature no-400, I3 DeepSeek reasoning_content, I4 `<think>`-strip ×4, I5 empty-retry, I6 cap-pause, I7 iteration cap, I8 usage accounting). |
| 3 | Gateway is a single consumable home for 093 (events + dispatcher + 3 adapters; `calling_mode` surfaced) | ✅ | `provider_gateway/{__init__,events,dispatcher,anthropic,google,openai_compat}.py` present; `open_stream` returns `(stream, calling_mode)` (Pitfall 3 fixed). No `task_service` rewire (D-05). |

## Eval backstop

Native-7 aggregate **16/28 = 16/28 BEFORE** (Plan 02 reference) — non-regressing.
`factual-doc-search` PASS on all 7. Total 17/32 vs 19/32 is OpenRouter (best-effort) losing
credit, not in the native-7 gate. No provider collapsed.

## Code review (gsd:code-review, standard depth, gateway package + agent_loop seam)

`092.5-REVIEW.md` — **0 critical, 1 warning, 5 info**; no correctness or byte-identity bugs.
Verified clean: concurrency (no module-level mutable shared state under WORKER_COUNT=2;
per-stream + per-retry-fresh accumulators), security (no secrets/eval/exec/subprocess),
cancel/leak path (guarded `close()`), and byte-identity against the pre-extraction commit
(`<think>` machine, reasoning routing, `_accumulate_chunk_usage` both branches, 5KB
boundary dicts).

- **WR-01 RESOLVED inline** — `emit_sse` (load-bearing, emitted by openai_compat + read by
  consumer) was missing from the canonical `ToolArgsProgressEvent` TypedDict; added as
  `emit_sse: NotRequired[bool]` so the schema 093 types against is truly lossless. Pure
  type annotation — zero runtime/byte-identical impact.
- **IN-01..IN-05 DEFERRED** (non-blocking maintainability): docstring "VERBATIM" vs
  defensive `getattr` (IN-01), dead `GatewayRequest.api_key`/`.max_tokens` fields
  (IN-02/03), unused `get_tools` import (IN-04), `open_stream` annotated `AsyncIterator`
  but returns sync iterables (IN-05). Candidates for a `/gsd:fast` cleanup or to fold into
  093 (which consumes the contract). None affect correctness or byte-identity.

## Overrides

1. **SSE diff — byte-identical by documented tool-path-noise judgment**, not a literal
   all-zero `SSE_DIFF_RESULT: PASS` string (the search-heavy pinned prompt invites variable
   search-call counts). Justified by non-persistent residuals across 2 runs, exclusively
   JUDGE-not-fail categories, zero regression signature, Anthropic byte-exact, and 14 agents
   of adversarial confirmation.
2. **Playwright E2E backstop deferred** — all 17 specs fail pre-refactor-relevant: unset
   `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` (config), then `scenario-01`'s stale
   `not.toHaveURL(/login/)` assertion that contradicts the app's documented
   `/login`-persists-after-auth behaviour. Pre-existing test rot, unrelated to a backend
   provider-gateway change. Operator-authorized; de-rot tracked as a separate follow-up.

## Requirement traceability

- **GATEWAY-01** → **Validated.** Shared provider boundary extracted; Deep byte-identical;
  `calling_mode` surfaced; seam ready for Phase 093 (PARITY-02).
