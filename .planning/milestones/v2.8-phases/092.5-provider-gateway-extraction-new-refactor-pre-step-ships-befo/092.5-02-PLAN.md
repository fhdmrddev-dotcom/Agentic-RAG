---
phase: 092.5-provider-gateway-extraction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/.sse_baseline/anthropic.json
  - scripts/.sse_baseline/google.json
  - scripts/.sse_baseline/openai.json
  - scripts/.sse_baseline/deepseek.json
  - scripts/.sse_baseline/moonshot.json
  - scripts/.sse_baseline/zhipu.json
  - scripts/.sse_baseline/minimax.json
autonomous: false
requirements: [GATEWAY-01]

must_haves:
  truths:
    - "Fresh native-7 BEFORE baselines exist, captured against the PRE-extraction (current, post-092) agent_loop — the reference both proof gates diff against."
    - "The eval scoreboard is captured BEFORE the extraction as the cross-provider backstop reference."
  artifacts:
    - path: "scripts/.sse_baseline/anthropic.json"
      provides: "Pre-extraction SSE skeleton baseline for Anthropic (and 6 sibling native-7 JSONs)"
  key_links:
    - from: "scripts/capture_sse_baseline.py --mode before"
      to: "scripts/.sse_baseline/<provider>.json"
      via: "XRANGE run:{run_id} capture + skeleton normalize"
      pattern: "BASELINE CAPTURED"
---

<objective>
Capture the native-7 BEFORE baselines against the CURRENT (pre-092.5-extraction) `agent_loop.py` — the byte-identical reference for BOTH proof gates (Wave 3 after clean providers, Wave 5 after OpenAI). This MUST happen before any extraction begins (Pitfall 5: if you start extracting before capturing `--mode before`, the reference is lost and byte-identical cannot be proven). `scripts/.sse_baseline/` already holds 089-era JSONs, but 092 just shipped cap-pause edits into `agent_loop.py` — so these baselines are STALE for this phase and MUST be re-captured against the live pre-extraction loop.

Purpose: Establish the byte-identical reference. No code changes — this is an operator capture run.
Output: 7 fresh `scripts/.sse_baseline/<provider>.json` files + a captured eval `EVAL_SUMMARY` reference.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-RESEARCH.md
@.planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-VALIDATION.md
@scripts/SSE_DIFF_RUNBOOK.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator captures native-7 BEFORE baselines (pre-extraction)</name>
  <read_first>
    - scripts/SSE_DIFF_RUNBOOK.md (the operator runbook: prereqs, pinned prompt, before/after procedure)
    - scripts/capture_sse_baseline.py:88-100 (the --mode before|after + --provider flags; choices=["before","after"])
    - .planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-RESEARCH.md §"The exact 089 proof procedure" (lines 401-411) + §"Pitfall 5" (lines 244-247)
  </read_first>
  <what-built>
    No code was built in this plan. This is the mandatory pre-extraction capture: the operator runs the EXISTING 089 proof harness in `--mode before` to save fresh native-7 skeleton baselines against the current `agent_loop.py` (which 092 just edited). The orchestrator cannot run this — it needs live native-7 provider keys and the operator-started backend (per project rule `feedback_user_starts_backend`: the operator starts uvicorn in a visible terminal, never `run_in_background`).
  </what-built>
  <how-to-verify>
    1. Operator: ensure local Supabase + Redis are up (`supabase start`; `docker compose -f docker-compose.dev.yml up -d`) and native-7 keys are in `backend/.env` (OPENAI, ANTHROPIC, GOOGLE, DEEPSEEK, MOONSHOT, ZHIPU/GLM, MINIMAX).
    2. Operator: start the backend in a VISIBLE terminal: `cd backend && venv\Scripts\uvicorn app.main:app --reload` (never background it). Wait for "Application startup complete".
    3. Operator: run the BEFORE capture against the pre-extraction loop:
       `backend/venv/Scripts/python.exe scripts/capture_sse_baseline.py --mode before`
       Expected final line: `SSE_DIFF_RESULT: BASELINE CAPTURED — all native-7 baselines saved to scripts/.sse_baseline/`.
    4. Operator: capture the eval backstop reference (the before-side of the SC#1 backstop):
       `backend/venv/Scripts/python.exe scripts/eval_cross_provider.py`
       Record the `EVAL_SUMMARY N/M cells PASS` line (this is the reference the AFTER gates must match or strictly non-regress).
    5. Confirm 7 fresh JSONs exist with current mtimes: `scripts/.sse_baseline/{anthropic,google,openai,deepseek,moonshot,zhipu,minimax}.json`.
  </how-to-verify>
  <acceptance_criteria>
    - `scripts/capture_sse_baseline.py --mode before` prints `SSE_DIFF_RESULT: BASELINE CAPTURED` (not BASELINE INCOMPLETE).
    - All 7 native-7 `scripts/.sse_baseline/<provider>.json` files have mtimes from this capture run (post-dating the 089 originals).
    - `eval_cross_provider.py` prints an `EVAL_SUMMARY` line; operator records the N/M for the AFTER comparison.
    - OpenRouter is best-effort (experimental) — its baseline is optional and does NOT block.
  </acceptance_criteria>
  <resume-signal>Type "baselines captured" (with the EVAL_SUMMARY N/M recorded), or describe any provider that failed to capture.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capture script → live provider APIs | The eval/capture scripts make live LLM calls with the operator's keys. No new boundary — this is the existing 089 harness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-092.5-03 | Information Disclosure | capture_sse_baseline.py / eval_cross_provider.py | accept | The scripts print key PRESENCE only (never values) — existing 089 rule. `capture_run_events.normalize` masks message_id/run_id. No secret crosses into the saved JSON baselines. localhost-only hard gate (`assert_localhost_only`). |

**Statement (per security_gate):** No HIGH threats. This plan runs the existing, already-vetted 089 proof harness with no code change. Secret-handling (presence-only) and localhost gating are unchanged.
</threat_model>

<verification>
- 7 fresh native-7 baseline JSONs in `scripts/.sse_baseline/` with current mtimes.
- `SSE_DIFF_RESULT: BASELINE CAPTURED` observed.
- `EVAL_SUMMARY` reference recorded for the AFTER gates.
</verification>

<success_criteria>
- The byte-identical reference (native-7 skeleton baselines) is captured against the PRE-extraction loop, unblocking the Wave 3 / Wave 5 `--mode after` proof gates.
</success_criteria>

<output>
After completion, create `.planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-02-SUMMARY.md` (record the EVAL_SUMMARY N/M and the 7 captured providers).
</output>
