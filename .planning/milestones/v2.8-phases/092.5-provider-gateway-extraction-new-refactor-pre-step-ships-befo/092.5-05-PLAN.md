---
phase: 092.5-provider-gateway-extraction
plan: 05
type: execute
wave: 4
depends_on: ["092.5-03", "092.5-04"]
files_modified:
  - backend/app/services/provider_gateway/openai_compat.py
  - backend/app/services/provider_gateway/dispatcher.py
  - backend/app/services/agent_loop.py
  - backend/tests/unit/test_provider_gateway_seam.py
  - backend/tests/unit/test_chunk_handler_provider_aware.py
autonomous: true
requirements: [GATEWAY-01]

must_haves:
  truths:
    - "The entangled OpenAI-compat branch is extracted VERBATIM into provider_gateway/openai_compat.py: the <think> state machine, reasoning_content routing, _accumulate_chunk_usage, and the per-provider 5KB boundary dicts MOVE IN and become EMITTED canonical events (delta/reasoning_delta/usage/usage_delta/tool_preparing/tool_args_progress)."
    - "The OpenAI adapter SURFACES calling_mode (the (stream, calling_mode) tuple) so the consumer runs parse_structured_tool_calls post-drain — parse_structured_tool_calls + the STRUCTURED messages injection STAY consumer-side (L-1/L-3)."
    - "agent_loop's OpenAI else-branch dispatches through open_stream and its _on_chunk_openai folds into the ONE shared consumer handler; the post-parse + provider-error retry + cap-pause + empty-retry STAY consumer-side."
    - "The OpenAI adapter does NOT emit a synthetic tool_start (would change the skeleton) — it emits tool_preparing + tool_args_progress; the consumer builds tool_calls_buffer (Open Q2)."
  artifacts:
    - path: "backend/app/services/provider_gateway/openai_compat.py"
      provides: "OpenAI-compat adapter: wraps create_adaptive_streaming_chat, owns <think>/reasoning/usage/boundary normalization, surfaces calling_mode"
      contains: "create_adaptive_streaming_chat"
      min_lines: 120
  key_links:
    - from: "backend/app/services/agent_loop.py"
      to: "backend/app/services/provider_gateway/dispatcher.py"
      via: "open_stream(provider, request) -> (stream, calling_mode) for the OpenAI-compat else-branch"
      pattern: "open_stream"
    - from: "backend/app/services/provider_gateway/openai_compat.py"
      to: "app.services.openai_service.create_adaptive_streaming_chat"
      via: "wraps the (stream, calling_mode) boundary; surfaces calling_mode"
      pattern: "create_adaptive_streaming_chat"
    - from: "backend/app/services/provider_gateway/openai_compat.py"
      to: "_accumulate_chunk_usage"
      via: "MOVES IN (provider-aware usage: Google-cumulative vs OpenAI-+=)"
      pattern: "_accumulate_chunk_usage"
---

<objective>
Extract the ONE genuinely entangled unit (D-04) — the OpenAI/OpenRouter/Ollama branch (`:1657-1974` + `_on_chunk_openai:1762-1924`) + the pure helper `_accumulate_chunk_usage:688-730` — into `provider_gateway/openai_compat.py`, VERBATIM. This is the high-skeleton-diff-risk step; it is isolated as its own provable gate (Wave 5). The `<think>` state machine, `reasoning_content` routing, `_accumulate_chunk_usage`, and the per-provider 5KB boundary dicts MOVE IN and become EMITTED canonical events. The `parse_structured_tool_calls(full_content)` post-parse (L-3) + the STRUCTURED `messages` injection (L-1) STAY consumer-side because they touch consumer accumulators. The adapter surfaces `calling_mode` (Pitfall 3 — dropping it is the exact bug that makes the harness OpenAI-only). The OpenAI `else`-branch then folds into the ONE shared consumer `_on_chunk` settled in Plan 03.

Purpose: Complete the gateway with the hard adapter, byte-identically. After this, all 3 providers dispatch through one home and the seam is fully shaped for 093 to consume.
Output: openai_compat.py (the entangled adapter); agent_loop's OpenAI else-branch consuming the gateway; the third _on_chunk folded into the shared handler; the doubly-stale _accumulate_chunk_usage test import repointed; seam test OpenAI groups GREEN.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-CONTEXT.md
@.planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-RESEARCH.md
@.planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-PATTERNS.md

<interfaces>
<!-- The OpenAI Adapter Entanglement disposition table (RESEARCH lines 334-348 / PATTERNS lines 145-156) — copy into the action -->
| State / behavior | LIVE location | Disposition under D-02 cut | Landmine |
|---|---|---|---|
| STRUCTURED pre-injection (OpenRouter XML, mutates messages) | :1375-1384 | CONSUMER stays | L-1 |
| STRUCTURED fallback injection (mutates messages) | :1669-1678 | CONSUMER stays | L-1 |
| `_in_think_block` `<think>` state machine | decl :1682, machine :1802-1830 | GATEWAY owns; EMITS delta + reasoning_delta | L-2 |
| `reasoning_content` (DeepSeek separate field) | :1836-1839 | GATEWAY emits reasoning_delta | L-2 |
| `_accumulate_chunk_usage` (provider-aware) | def :688, call :1774 | GATEWAY calls it; EMITS usage/usage_delta | L-2 |
| native delta.tool_calls → buffer build + tool_preparing | :1841-1862 | GATEWAY emits tool_preparing + tool_args_progress; CONSUMER builds buffer (NO synthetic tool_start — Open Q2) | L-2 |
| 5KB tool_args_progress boundary dicts (per-provider independence) | decl :1702-1703, compute :1886-1924 | GATEWAY owns; keep OpenAI-native vs OpenRouter independent | L-4 |
| `parse_structured_tool_calls(full_content)` post-parse | :1941-1962 | CONSUMER stays (reads full_content post-drain) | L-3 |
| provider-error retry (except APIError) + TPM 429 msg append | :1976-2010 | CONSUMER stays (loop retry wraps open_stream) | L-5 |
| `_active_cap` / `active_provider_name` derivation | :1747-1748 | GATEWAY needs it (usage branch + boundary key) — pass in request | — |

<!-- The <think> machine to MOVE verbatim (agent_loop.py:1802-1830) — providers: moonshot/deepseek/minimax/zhipu -->
```python
if active_provider_name in ("moonshot", "deepseek", "minimax", "zhipu"):
    _visible = ""; _reasoning = ""; _remaining = _content
    while _remaining:
        if _in_think_block:
            end_idx = _remaining.find("</think>")   # routes to _reasoning, clears flag on close
            ...
        else:
            start_idx = _remaining.find("<think>")  # routes to _visible, sets flag on open
            ...
    if _reasoning:  full_reasoning_content += _reasoning;  await _emit(redis, run_id, 'reasoning_delta', content=_reasoning)
    if _visible:    full_content += _visible;              await _emit(redis, run_id, 'delta', content=_visible)
```
Under the D-02 cut: `_in_think_block` becomes adapter-local stream state; the `full_*` += / `_emit` lines become EMITTED reasoning_delta/delta events; the consumer's ONE handler does the += and _emit.

<!-- The post-parse that STAYS consumer-side (agent_loop.py:1941-1962) — L-3 -->
```python
if calling_mode == CallingMode.STRUCTURED:
    structured_calls = parse_structured_tool_calls(full_content)
    if structured_calls:
        for idx, call in enumerate(structured_calls):
            tool_calls_buffer[idx] = {"id": call.id, "name": call.function.name, "arguments": call.function.arguments}
        full_content = ""; finish_reason = "tool_calls"
```
Reads the consumer's `full_content` AFTER the drain → STAYS consumer-side. The adapter surfaces calling_mode so the consumer knows to run it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author openai_compat.py — move the entangled normalization in VERBATIM, surface calling_mode</name>
  <read_first>
    - backend/app/services/agent_loop.py:1657-1974 (the ENTIRE OpenAI-compat else-branch) — construction :1659-1665, fallback inject :1669-1678, _in_think_block decl :1682, boundary dicts :1702-1703, _on_chunk_openai :1762-1924 (state machine :1802-1830, reasoning :1836-1839, tool-call build :1841-1862, inline boundary emit :1886-1924), drain :1926, post-parse :1941-1962, provider-error retry :1976-2010
    - backend/app/services/agent_loop.py:688-730 (_accumulate_chunk_usage — the pure provider-aware usage fn that MOVES IN)
    - backend/app/services/openai_service.py:1197-1205 (create_adaptive_streaming_chat — the (stream, calling_mode) contract) + :1155 (resolve_calling_mode) + normalize_finish_reason (imported agent_loop.py:53)
    - .planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-RESEARCH.md §"OpenAI Adapter Entanglement" (lines 330-355, L-1..L-5) + §"Pitfall 4" (5KB boundary independence) + §"Open Questions" #2 (no synthetic tool_start)
    - .planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-PATTERNS.md §"openai_compat.py" (lines 131-194) — the full MOVES/STAYS table + copyable excerpts
  </read_first>
  <action>
    Fill in `backend/app/services/provider_gateway/openai_compat.py` (the dispatcher stub from Plan 01 raises NotImplementedError for this branch). The adapter wraps `create_adaptive_streaming_chat` and SURFACES `calling_mode`, and EMITS canonical GatewayEvents. MOVE these pieces IN, VERBATIM (anti-pattern: NO "while-I'm-in-here" cleanup — agent_loop.py:15-19 warns this re-opens the 075.x cascade):

    1. Stream construction (`:1659-1665`): `stream, calling_mode = create_adaptive_streaming_chat(messages=..., model=..., user_settings=..., tool_choice=..., tools_override=...)`. Return `calling_mode` to the consumer (the adapter's open_stream returns `(event_iterator, calling_mode)`).
    2. `_accumulate_chunk_usage` (`:688-730`) MOVES IN as a pure fn in openai_compat.py (Google-cumulative-overwrite vs OpenAI-`+=` — a re-derive would 2-3× over-count Google billing). The adapter calls it and EMITS `usage`/`usage_delta` events (matching what Anthropic/Google already emit). NOTE: keep the Google branch inside it intact even though this adapter is the OpenAI path — the unit test exercises both branches; preserve byte-for-byte.
    3. The `<think>` state machine (`:1802-1830`, providers `moonshot/deepseek/minimax/zhipu`) MOVES IN. `_in_think_block` becomes adapter-local stream state (one stream = one tracker). Instead of `full_*` += and `_emit`, the adapter EMITS `reasoning_delta` (for `_reasoning`) and `delta` (for `_visible`) events. The consumer's shared handler does the accumulation + emit.
    4. `reasoning_content` (DeepSeek separate field, `:1836-1839`) → EMIT `reasoning_delta`.
    5. Native `delta.tool_calls` → the adapter EMITS `tool_preparing` (name known) + `tool_args_progress` (boundary). DO NOT emit a synthetic `tool_start` (Open Q2 — it would add a skeleton token the OpenAI path lacks). The consumer builds `tool_calls_buffer` from these events.
    6. The per-provider 5KB boundary dicts (`_emit_boundary_openai_native` / `_emit_boundary_openrouter`, `:1702-1703` + compute `:1886-1924`) MOVE IN, keyed on provider (`active_provider_name` from the request). Keep OpenAI-native vs OpenRouter INDEPENDENT (075.6 SPEC, L-4). The adapter accumulates args internally (it sees every chunk) — it does NOT need the consumer's tool_calls_buffer to compute the boundary.
    7. `normalize_finish_reason` (`:1786`) — use it inside the adapter to canonicalize finish_reason before emitting `finish`.

    DO NOT move into the adapter (they STAY consumer-side — they mutate consumer accumulators):
    - STRUCTURED `messages` injection (pre `:1375-1384` + fallback `:1669-1678`) — L-1. The adapter returns `calling_mode`; the consumer owns the inject (Task 2).
    - `parse_structured_tool_calls(full_content)` post-parse (`:1941-1962`) — L-3. Reads consumer's `full_content` post-drain.
    - provider-error retry (`:1976-2010`) + TPM 429 msg append — L-5. Loop-orchestration wraps open_stream.

    Wire openai_compat into `dispatcher.open_stream` as the `else` (any provider not anthropic/google → openai_compat adapter), matching the live branch logic.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -c "from app.services.provider_gateway import openai_compat as oc; print('OK', hasattr(oc,'_accumulate_chunk_usage') or 'usage' in dir(oc)); from app.services.provider_gateway import open_stream, CallingMode; print('dispatch ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/services/provider_gateway/openai_compat.py` exists; `grep -c "create_adaptive_streaming_chat" backend/app/services/provider_gateway/openai_compat.py` >= 1 AND `grep -c "_accumulate_chunk_usage" backend/app/services/provider_gateway/openai_compat.py` >= 1 (usage fn moved in).
    - `grep -c "_in_think_block\|<think>\|</think>" backend/app/services/provider_gateway/openai_compat.py` >= 1 (the think machine moved in) AND `grep -c "moonshot\|deepseek\|minimax\|zhipu" backend/app/services/provider_gateway/openai_compat.py` >= 1 (provider gating preserved).
    - `grep -c "_emit_boundary_openai_native\|_emit_boundary_openrouter\|openrouter" backend/app/services/provider_gateway/openai_compat.py` >= 1 (boundary independence preserved, L-4).
    - `grep -c "tool_start" backend/app/services/provider_gateway/openai_compat.py` — the adapter does NOT emit a synthetic tool_start for OpenAI (Open Q2); if "tool_start" appears it must NOT be a yield/emit for the OpenAI native path (verify by reading; the emitted events are tool_preparing + tool_args_progress).
    - `grep -c "parse_structured_tool_calls" backend/app/services/provider_gateway/openai_compat.py` == 0 (post-parse STAYS consumer-side, L-3).
    - `grep -rc "from app.services.agent_loop\|from app.api.threads\|import agent_loop\|import threads" backend/app/services/provider_gateway/` == 0 (no import cycle, SC#3).
    - The dispatcher's last NotImplementedError stub is gone: `grep -c "raise NotImplementedError" backend/app/services/provider_gateway/dispatcher.py` == 0.
  </acceptance_criteria>
  <done>openai_compat.py wraps create_adaptive_streaming_chat, surfaces calling_mode, owns the <think> machine + reasoning + _accumulate_chunk_usage + per-provider 5KB boundary dicts (independent), emits canonical events (no synthetic tool_start), and is wired as the dispatcher else-branch; parse_structured_tool_calls + messages injection NOT in the adapter; no import cycle.</done>
</task>

<task type="auto">
  <name>Task 2: Fold agent_loop's OpenAI else-branch into the shared _on_chunk; keep post-parse + injection + retry consumer-side</name>
  <read_first>
    - backend/app/services/agent_loop.py:1657-1974 (the OpenAI else-branch being rewired) — esp. fallback inject :1669-1678, _on_chunk_openai :1762-1924, drain :1926, post-parse :1941-1962, retry :1976-2010
    - backend/app/services/agent_loop.py:1375-1384 (STRUCTURED pre-injection — STAYS) + :1052-1085 (STAY accumulators)
    - The Plan 03 shared `_on_chunk` (now in agent_loop after Plan 03 collapsed Anthropic+Google) — the OpenAI events fold into the SAME handler
    - .planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-RESEARCH.md §"Pitfall 2/3" (calling_mode surfacing + post-parse) + §"OpenAI Adapter Entanglement" landmine summary (lines 350-355)
  </read_first>
  <action>
    Rewire the OpenAI-compat `else` branch (`:1657-1974`) in `run_agent_loop` to dispatch through the gateway:

    1. Replace `stream, calling_mode = create_adaptive_streaming_chat(...)` with `_stream, calling_mode = await open_stream(active_provider_name, request)` (the request envelope already built in Plan 03's wiring — extend it to carry the OpenAI-path fields: tools_override/active_tools, tool_choice). The adapter surfaces `calling_mode` — KEEP it (Pitfall 3); the consumer needs it for the STRUCTURED skip + injection + post-parse.
    2. The STRUCTURED fallback injection (`:1669-1678`) STAYS consumer-side: after `open_stream` returns `calling_mode==STRUCTURED`, run the existing inject (mutates `messages` + `_structured_tools_injected`) — verbatim (L-1). The pre-injection (`:1375-1384`) already STAYS (it's above the branch).
    3. Fold the OpenAI event handling into the ONE shared `_on_chunk` from Plan 03: the adapter now EMITS `delta`/`reasoning_delta`/`usage`/`usage_delta`/`tool_preparing`/`tool_args_progress`, so the shared handler's existing branches (built for Anthropic/Google) accumulate `full_content`/`full_reasoning_content`/token totals + build `tool_calls_buffer` from `tool_preparing`/`tool_args_progress` + `_emit` — verbatim with how `_on_chunk_openai` did it (`:1762-1924`). Remove the now-dead `async def _on_chunk_openai`. The `tool_calls_buffer` build from `tool_preparing`/`tool_args_progress` (Open Q2) lives in the shared handler.
    4. KEEP consumer-side, verbatim: the drain (`:1926`), the `parse_structured_tool_calls(full_content)` post-parse (`:1941-1962`, L-3 — reads full_content, sets full_content="", finish_reason="tool_calls", tool_calls_buffer), the STRUCTURED parse-failure log (`:1963-1972`), the provider-error retry (`:1976-2010`, L-5), the cap-pause (`:2028-2066`), end_turn (`:2107-2111`, I1), empty-retry (`:2120-2126`, I5), and the message-append round-trip (`:2129-2167`, persists reasoning_content/thought_signature — I3/I2).

    NO behavior change. This is the verbatim fold of the third handler into the shared one + the consumer-side residue staying put. Any skeleton-diff on the OpenAI-path providers (openai/deepseek/moonshot/zhipu/minimax/openrouter) in Wave 5 is a BLOCKER.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -c "import app.services.agent_loop as al; import app.api.threads as t; print('OK', hasattr(al,'run_agent_loop'))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "async def _on_chunk_openai" backend/app/services/agent_loop.py` == 0 (third handler folded into the shared one).
    - `grep -c "create_adaptive_streaming_chat" backend/app/services/agent_loop.py` == 0 (the OpenAI branch now dispatches via open_stream — direct call gone from run_agent_loop). [If a non-loop reference remains elsewhere, confirm it's not in run_agent_loop's dispatch.]
    - `grep -c "parse_structured_tool_calls" backend/app/services/agent_loop.py` >= 1 (post-parse STAYS consumer-side, L-3) AND `grep -c "calling_mode" backend/app/services/agent_loop.py` >= 1 (surfaced + consumed, Pitfall 3).
    - `grep -c "except (APIError" backend/app/services/agent_loop.py` >= 1 (provider-error retry STAYS, L-5) AND the cap-pause/end_turn/empty-retry blocks are intact (`grep -c "cap_paused\|end_turn\|_empty_retries" backend/app/services/agent_loop.py` >= 3).
    - agent_loop + threads import cleanly (Task 2 verify prints `OK ... True`).
    - NO edits to task_service.py / sub_agent_service.py (`git diff --stat` shows neither file changed in this plan).
  </acceptance_criteria>
  <done>OpenAI else-branch dispatches through open_stream (calling_mode surfaced + consumed); the third _on_chunk folds into the shared handler; parse_structured_tool_calls post-parse + STRUCTURED messages injection + provider-error retry + cap-pause + end_turn + empty-retry + round-trip persistence all STAY consumer-side verbatim; no task_service/sub_agent edit; imports clean.</done>
</task>

<task type="auto">
  <name>Task 3: Repoint the doubly-stale _accumulate_chunk_usage + create_adaptive_streaming_chat monkeypatch targets; flip seam test OpenAI groups GREEN; full suite</name>
  <read_first>
    - backend/tests/unit/test_chunk_handler_provider_aware.py:13/:40/:44-45 (imports _accumulate_chunk_usage from the DOUBLY-stale `app.api.threads` path — now lives in provider_gateway/openai_compat.py)
    - The ~30 integration tests patching `app.services.agent_loop.create_adaptive_streaming_chat` (grep to enumerate — test_059/062/058/066/061/063/067_4/075/089/threads_skills/explorer_agent etc.)
    - backend/tests/unit/test_provider_gateway_seam.py (flip the OpenAI importorskip groups to live)
    - .planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-PATTERNS.md §"Monkeypatch-target sweep" (lines 244-246) + §"_accumulate_chunk_usage MOVES" note (line 192)
    - .planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-VALIDATION.md §"Wave 0 Requirements" (monkeypatch sweep) + the GATEWAY-01 SC#2/I8 usage-accounting row
  </read_first>
  <action>
    1. Repoint `test_chunk_handler_provider_aware.py:44` from `from app.api.threads import _accumulate_chunk_usage` to `from app.services.provider_gateway.openai_compat import _accumulate_chunk_usage` (it was doubly-stale: threads → agent_loop → now openai_compat). The test's Google-vs-OpenAI assertions are the I8 usage-accounting guard — they MUST still pass unchanged (the fn moved verbatim).
    2. Repoint every test patching `app.services.agent_loop.create_adaptive_streaming_chat` to where the OpenAI adapter imports it: `app.services.provider_gateway.openai_compat.create_adaptive_streaming_chat` (grep-confirm the adapter's import site and match it). Enumerate via `grep -rln "app.services.agent_loop.create_adaptive_streaming_chat" backend/tests/` and repoint each. Title-gen patches in `threads.py` STAY untouched.
    3. In `test_provider_gateway_seam.py`, flip the OpenAI importorskip-gated groups to live: feed synthetic OpenAI/OpenRouter SDK chunks, assert the adapter yields the expected canonical events — including `test_openai_adapter_think_tag_routes_to_reasoning_delta` (I4: `<think>` content → reasoning_delta, no leak into delta), `test_openai_adapter_surfaces_calling_mode` (Pitfall 3), `test_openai_adapter_usage_via_accumulate_chunk_usage` (I8), and `test_openai_adapter_no_synthetic_tool_start` (Open Q2 — emits tool_preparing + tool_args_progress, NOT tool_start).
    4. Run the full suite; confirm ZERO net-new failures vs the Plan-02 baseline.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -m pytest backend/tests/unit/test_provider_gateway_seam.py backend/tests/unit/test_chunk_handler_provider_aware.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "app.api.threads import _accumulate_chunk_usage\|from app.api.threads import _accumulate" backend/tests/` == 0 AND `grep -c "provider_gateway.openai_compat import _accumulate_chunk_usage" backend/tests/unit/test_chunk_handler_provider_aware.py` >= 1 (repointed).
    - `grep -rc "app.services.agent_loop.create_adaptive_streaming_chat" backend/tests/` == 0 (all repointed to the openai_compat namespace).
    - The seam test OpenAI groups RUN (not skip): `pytest backend/tests/unit/test_provider_gateway_seam.py -q` shows all adapter groups (anthropic/google/openai) passed, zero skipped.
    - `grep -c "reasoning_delta\|calling_mode\|no_synthetic_tool_start\|tool_start" backend/tests/unit/test_provider_gateway_seam.py` covers the I4 + Pitfall-3 + Open-Q2 contracts (the named OpenAI tests exist and pass).
    - `test_chunk_handler_provider_aware.py` passes (I8 usage accounting preserved — Google-cumulative vs OpenAI-+=).
    - Full suite (`backend/venv/Scripts/python.exe -m pytest backend/tests/ -q`) shows ZERO net-new failures vs the Plan-02 baseline failure set.
  </acceptance_criteria>
  <done>The doubly-stale _accumulate_chunk_usage import + all create_adaptive_streaming_chat patch targets repointed to the openai_compat namespace; seam test all 3 adapter groups GREEN (incl. I4/I8/Pitfall-3/Open-Q2); I8 usage-accounting unit test green; full suite zero net-new failures; title-gen patches untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer (`agent_loop`) → openai_compat adapter | In-process; GatewayRequest (incl. resolved api_key + messages) crosses. Same worker/RLS context. |
| openai_compat adapter → openai_service / tool_parser | In-process; wraps create_adaptive_streaming_chat. parse_structured_tool_calls (Pydantic-validated tool-arg parsing) STAYS consumer-side — moved nothing about validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-092.5-07 | Tampering | openai_compat.py STRUCTURED-mode handling | accept | The STRUCTURED `messages` injection (TOOL_USAGE_INSTRUCTIONS) + `parse_structured_tool_calls` Pydantic-backed recovery STAY consumer-side and are moved NOWHERE in their validation logic — no relaxation. Tool-arg parsing path is byte-identical (V5 unchanged, RESEARCH §Security Domain). |
| T-092.5-08 | Information Disclosure | openai_compat.py (key resolution + `<think>` routing) | accept | api_key resolution moves verbatim (same in-process path); the `<think>`-strip routes reasoning to reasoning_delta (no new exposure — same routing as today). No key logged; base64 thought_signature is transport not crypto. |
| T-092.5-09 | Denial of Service | provider-error retry (STAYS consumer-side) | accept | The bounded provider-error retry (`_provider_retries`) + iteration cap (I7) + empty-retry (I5) all STAY consumer-side verbatim — the retry bound is unchanged, no new unbounded loop. |

**Statement (per security_gate):** NO HIGH-severity threats. This is the verbatim relocation of the entangled OpenAI normalization; every validation/secret/retry control is preserved (parse_structured_tool_calls Pydantic validation, key resolution, bounded retries). The byte-identical SSE-diff gate (Wave 5) + the I8 usage unit test are the behavioral proofs that no control was altered. V5 (input validation) unchanged.
</threat_model>

<verification>
- openai_compat.py owns the <think> machine + reasoning + _accumulate_chunk_usage + per-provider 5KB boundary dicts (independent); surfaces calling_mode; emits canonical events (no synthetic tool_start); wired as dispatcher else.
- agent_loop's OpenAI else-branch dispatches through open_stream; third _on_chunk folded into the shared handler; post-parse + injection + retry + cap-pause + end_turn + empty-retry + round-trip persistence STAY consumer-side verbatim.
- _accumulate_chunk_usage + create_adaptive_streaming_chat monkeypatch targets repointed; seam test all 3 adapter groups GREEN; I8 usage unit test green; full suite zero net-new failures.
- NO task_service / sub_agent_service.py edits (D-05); no import cycle (SC#3).
- (Byte-identical proof for the OpenAI-path providers is the Wave 5 / Plan 06 operator gate.)
</verification>

<success_criteria>
- The entangled OpenAI-compat adapter is extracted verbatim; all 3 providers now dispatch through the one gateway home.
- calling_mode is surfaced (the bug that made the harness OpenAI-only is structurally fixed — the seam carries it).
- D-07 invariants I3 (reasoning_content), I4 (<think>-strip), I8 (usage accounting) preserved + asserted; I1/I5/I6/I7 stay consumer-side verbatim.
- The seam (open_stream + canonical events + calling_mode) is fully shaped for 093 to consume; no task_service rewire (D-05).
</success_criteria>

<output>
After completion, create `.planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-05-SUMMARY.md`
</output>
