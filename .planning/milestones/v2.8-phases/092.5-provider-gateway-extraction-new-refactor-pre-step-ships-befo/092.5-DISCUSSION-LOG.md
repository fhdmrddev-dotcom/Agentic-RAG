# Phase 092.5: Provider Gateway Extraction - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-01
**Phase:** 092.5-provider-gateway-extraction
**Areas discussed:** Gateway shape & home, Where the seam cuts, This-phase scope, Byte-identical proof gate

---

## Area selection

All four candidate gray areas selected by the operator (multiSelect).

---

## Gateway shape & home

| Option | Description | Selected |
|--------|-------------|----------|
| Adapter-per-provider package | `provider_gateway/` dir, one file per provider + thin dispatcher; physical "separate per concern" | ✓ |
| Single module, internal branch | One `provider_gateway.py` branching on provider; smaller, 3-way branch in one file | |
| Class registry (tool_dispatcher style) | `ProviderGateway` register()/dispatch(); pluggable, adds indirection | |

**User's choice:** Adapter-per-provider package (Recommended)
**Notes:** Matches the operator's "one home, separated per concern / separate per feature / easiest to find a provider bug" principle. The 3 `_on_chunk_*` handlers are already structurally separate in `agent_loop.py` — this makes that physical. → D-01

---

## Where the seam cuts

| Option | Description | Selected |
|--------|-------------|----------|
| Events out — consumer accumulates & emits | Gateway = construct stream + normalize → canonical events; `agent_loop` keeps emit + accumulation (3 handlers → 1). Lowest Deep-wire risk. | ✓ |
| Full turn in the gateway | Gateway also accumulates + emits via injected callback, returns result; max dedup, higher byte-identical risk | |
| You decide (within the red line) | Planner picks safest shape | |

**User's choice:** Events out — consumer accumulates & emits (Recommended)
**Notes:** Keeps the load-bearing SSE-emit + `nonlocal` accumulation (the 075.x-cascade surface) in `agent_loop` — never moves. Gateway owns only chunk→canonical-event normalization (the part 093 actually needs). "One UX, four adapters" made literal. → D-02/D-03/D-04

---

## This-phase scope

| Option | Description | Selected |
|--------|-------------|----------|
| Seam only — task_service deferred to 093 | Build gateway, point Deep's agent_loop at it, prove byte-identical, expose seam; 093 rewires task_service + kills 3rd copy | ✓ |
| Also migrate task_service now | Additionally repoint `_consume_sync_stream`; kills 3rd copy + proves 2nd consumer, but widens byte-identical surface + front-runs 093's UAT gate | |

**User's choice:** Seam only — task_service deferred to 093 (Recommended)
**Notes:** Tightest provable surface this phase; the 3rd copy dies in 093 where the cross-provider UAT gate can prove the harness/`task()` path on native-7. → D-05

---

## Byte-identical proof gate

| Option | Description | Selected |
|--------|-------------|----------|
| 089 method, native-7, operator runs live UAT | same-code run1-vs-run2 noise floor + before/after SSE structural diff + eval + E2E across all native-7; + new seam contract test | ✓ |
| 089 method, Deep-only lighter smoke | Same method, 1–2 representative providers live; faster, less coverage | |
| You decide | Pick breadth that satisfies the red line | |

**User's choice:** 089 method, native-7, you run live UAT (Recommended)
**Notes:** Operator restarts backend + runs live UAT (established pattern); orchestrator runs automated capture/diff + a new seam contract test (gateway fed a recorded stream yields expected canonical events — 093's foundation). All round-trip invariants named in VERIFICATION. → D-06/D-07/D-08

---

## Final check

"Anything else before CONTEXT.md?" → **Ready for context** (declined plan-shape/risk-sequencing
deep-dive and further gray areas). Plan-shape left to the planner per Claude's Discretion, with
the guidance that the entangled OpenAI adapter is the high-risk unit to isolate.

## Claude's Discretion

- Dispatcher entrypoint signature + optional thin internal registry (D-01).
- Exact canonical event dataclass/TypedDict shape — must losslessly carry every field today's `_on_chunk_*` consume (D-02).
- Plan-shape / risk sequencing (one wave vs clean-providers-first then OpenAI).

## Deferred Ideas

- `task_service` rewire + 3rd-copy kill → Phase 093.
- Native Google SDK service (SEED-028) → future, behind the gateway.
- Pluggable provider plugin framework → v2.9 (Plugin Contract).
