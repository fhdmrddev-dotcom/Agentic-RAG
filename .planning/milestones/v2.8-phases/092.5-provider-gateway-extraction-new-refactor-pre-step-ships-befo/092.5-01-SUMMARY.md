---
phase: 092.5-provider-gateway-extraction
plan: 01
subsystem: api
tags: [provider-gateway, streaming, typeddict, calling-mode, refactor, agent-loop, cross-provider, seam-contract]

# Dependency graph
requires:
  - phase: 089-harness-engine-agent-loop-extraction
    provides: agent_loop.run_agent_loop (the consumer + the 3 per-provider dispatch branches this gateway will later extract INTO)
  - phase: 075.6
    provides: code_so_far additive field on tool_args_progress (must be carried losslessly)
  - phase: 075.5
    provides: Google native thought_signature round-trip (I2 invariant the schema must carry)
provides:
  - "provider_gateway/ package skeleton — the D-01 one-home-per-provider boundary"
  - "events.py — canonical GatewayEvent TypedDict union (8 event types, lossless superset incl. code_so_far / thought_signature / reasoning_delta)"
  - "dispatcher.py — open_stream(provider, request) -> (AsyncIterator[GatewayEvent], CallingMode) skeleton + GatewayRequest envelope + CallingMode re-export"
  - "test_provider_gateway_seam.py — D-08 seam-contract test scaffold (RED via function-scope importorskip; collects green, exits 0)"
affects: [092.5-02 anthropic+google adapters, 092.5-04 openai_compat adapter, 092.5-03/05 byte-identical proof, 093 harness cross-provider parity, task_service repoint]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "TypedDict-per-type Union for the canonical event vocabulary (typing.NotRequired on optional fields; py3.12 native)"
    - "calling_mode rides ALONGSIDE the stream as a tuple return, never as an event (Pitfall 3 — dropping it makes the harness OpenAI-only)"
    - "Re-export the existing openai_service.CallingMode enum, never redefine"
    - "Function-scope pytest.importorskip for a RED scaffold whose shape tests must still collect+pass (module-level importorskip aborts the whole file)"

key-files:
  created:
    - backend/app/services/provider_gateway/__init__.py
    - backend/app/services/provider_gateway/events.py
    - backend/app/services/provider_gateway/dispatcher.py
    - backend/tests/unit/test_provider_gateway_seam.py
  modified: []

key-decisions:
  - "events.py is a TypedDict-per-type Union (RESEARCH recommendation); the 3 docstring-omitted but consumer-consumed fields (code_so_far, thought_signature, reasoning_delta) are first-class for losslessness"
  - "GatewayRequest dataclass lives in dispatcher.py (Claude's discretion per D-02) — the union of all 3 stream-construction field sets + active_provider_name"
  - "open_stream adapter bodies are NotImplementedError stubs this plan — the job is to fix the SHAPE (signature, provider->adapter routing, CallingMode surfacing); Waves 2/4 fill the bodies"
  - "Seam test uses FUNCTION-SCOPE importorskip (not module-level) so the 3 shape tests against the now-existing events.py + dispatcher.py collect and PASS, while the 6 adapter tests SKIP until their modules land"

patterns-established:
  - "Pattern 1: canonical event schema = google_service.py docstring verbatim + the 3 lossless-superset fields; never re-shape Anthropic/Google events for consistency"
  - "Pattern 2: SC#3 no-import-cycle — the gateway imports openai_service (below it) but NEVER agent_loop/threads; _emit/spawn stay passed-in callables consumer-side"

requirements-completed: [GATEWAY-01]

# Metrics
duration: ~18min
completed: 2026-06-01
---

# Phase 092.5 Plan 01: Provider Gateway Skeleton Summary

**Canonical provider event schema (8-type TypedDict union, lossless superset) + thin `open_stream` dispatcher (`(stream, calling_mode)` shape) + D-08 seam-contract test scaffold — pure new-file authoring, no `agent_loop.py` edit, so Deep is trivially byte-identical.**

## Performance

- **Duration:** ~18 min
- **Started:** 2026-06-01T06:04Z (approx)
- **Completed:** 2026-06-01T06:22Z
- **Tasks:** 3
- **Files modified:** 4 created, 0 modified

## Accomplishments
- `provider_gateway/events.py` — the D-02 seam contract: `GatewayEvent = Union[...]` over 8 TypedDicts (`delta`, `reasoning_delta`, `tool_preparing`, `tool_args_progress`, `tool_start`, `finish`, `usage`, `usage_delta`). Lossless superset verified: `code_so_far` on `ToolArgsProgressEvent` (075.6), `thought_signature` (`NotRequired`) on `ToolStartEvent` + `FinishEvent.tool_calls` (Google I2), `reasoning_delta` as a first-class event (OpenAI `<think>`/`reasoning_content` path).
- `provider_gateway/dispatcher.py` — `open_stream(provider, request) -> tuple[AsyncIterator[GatewayEvent], CallingMode]` mirroring `create_adaptive_streaming_chat`'s `(stream, calling_mode)` contract; provider->adapter routing skeleton (anthropic/google explicit, OpenAI-compat `else`) matching `agent_loop.py:1396/:1530/:1657`; adapter bodies are `NotImplementedError` stubs for Waves 2/4. `GatewayRequest` dataclass envelope carries the union of all 3 stream-construction fields + `active_provider_name`. `CallingMode` re-exported from `openai_service` (no new enum).
- `__init__.py` exports `open_stream` + `GatewayRequest` + `CallingMode` + `GatewayEvent` (and all 8 per-type TypedDicts).
- `tests/unit/test_provider_gateway_seam.py` — the D-08 contract test: 3 shape tests PASS today (lossless-superset, CallingMode-re-export, routing skeleton), 6 adapter contract tests SKIP via function-scope `importorskip` and go RED->GREEN as Waves 2/4 land. Asserts I4 `<think>`-routing, I2 `thought_signature` on `finish.tool_calls`, Pitfall-3 `calling_mode` surfacing. `pytest -x -q` exits 0 (`3 passed, 6 skipped`).
- SC#3 zero import cycle confirmed package-wide; full backend suite shows zero net-new failures (103 failed = documented baseline; passed 1014 -> 1062, includes the 3 new shape tests).

## Task Commits

Each task was committed atomically:

1. **Task 1: Author canonical event schema (events.py + __init__ marker)** - `325a5422` (feat)
2. **Task 2: Author thin dispatcher entrypoint (dispatcher.py + package exports)** - `9cdbbd91` (feat)
3. **Task 3: Author D-08 seam-contract test scaffold (RED, collects green)** - `2fb4f59e` (test)

**Plan metadata:** (docs commit — this SUMMARY + STATE/ROADMAP)

_Note: Task 3 is `tdd="true"`. Because the implementations (adapters) land in later WAVES, not this plan, there is no GREEN feat-commit in this plan — the RED scaffold (`test` commit) is the deliverable. The function-scope `importorskip` gating is the project-canonical "module exists, symbol arrives later" idiom (`test_anthropic_service.py`)._

## Files Created/Modified
- `backend/app/services/provider_gateway/__init__.py` - Package marker + public exports (open_stream, GatewayRequest, CallingMode, GatewayEvent + 8 event TypedDicts); SC#3 no-import-cycle docstring
- `backend/app/services/provider_gateway/events.py` - Canonical 8-type GatewayEvent TypedDict union (D-02 seam contract); lossless superset
- `backend/app/services/provider_gateway/dispatcher.py` - open_stream skeleton + GatewayRequest dataclass envelope + CallingMode re-export; provider->adapter routing with NotImplementedError stubs
- `backend/tests/unit/test_provider_gateway_seam.py` - D-08 deterministic seam-contract test (3 shape tests pass, 6 adapter tests skip via importorskip)

## Decisions Made
- **TypedDict-per-type Union** for `GatewayEvent` (RESEARCH recommendation; D-02 discretion). Python is 3.12.6 so `typing.NotRequired` is native — no `typing_extensions` fallback needed.
- **`GatewayRequest` lives in dispatcher.py** (the plan allowed events.py OR dispatcher.py). It's the consumer-filled envelope; keeping it next to `open_stream` keeps the request/entrypoint coupling local.
- **Adapter bodies are `NotImplementedError` stubs** this plan — the load-bearing decisions (schema, signature, routing, CallingMode surfacing) are fixed here; Waves 2/4 fill the verbatim adapter moves.
- **Function-scope `importorskip`** in the seam test (see Deviation 1) — required so the shape tests still collect+pass.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Seam test used module-level importorskip → collapsed the whole file to one skip**
- **Found during:** Task 3 (seam-contract test scaffold)
- **Issue:** The plan's action described `pytest.importorskip(...)` "at the top of each adapter's test group". Authored module-level, the FIRST `importorskip` raised `Skipped` at import time and aborted collection of the ENTIRE module — `pytest --collect-only` reported `no tests collected` and `-x -q` reported `1 skipped` (zero shape tests ran). This violated the acceptance criterion that the events/dispatcher SHAPE tests must PASS.
- **Fix:** Moved each `importorskip` call INSIDE its adapter test function (anthropic + google) and into a `_require_openai_compat()` helper the 4 OpenAI tests call. The 3 shape tests now collect and PASS; the 6 adapter tests SKIP cleanly until their modules land. `importorskip` occurrence count = 6 (>= 3 requirement met).
- **Files modified:** backend/tests/unit/test_provider_gateway_seam.py
- **Verification:** `pytest tests/unit/test_provider_gateway_seam.py --collect-only -q` -> `9 tests collected`; `pytest ... -x -q` -> `3 passed, 6 skipped`, exit 0.
- **Committed in:** `2fb4f59e` (Task 3 commit)

**2. [Rule 1 - Bug] Wrong TYPE_CHECKING import path for UserEffectiveSettings**
- **Found during:** Task 2 (dispatcher.py)
- **Issue:** Initial draft used `from app.services.settings_service import UserEffectiveSettings` under `TYPE_CHECKING`. The class actually lives at `app.models.user_settings`. A wrong path is invisible at runtime (string annotation in a dataclass) but a type-checker would flag it — a correctness defect.
- **Fix:** Corrected to `from app.models.user_settings import UserEffectiveSettings` (verified via grep for `class UserEffectiveSettings`).
- **Files modified:** backend/app/services/provider_gateway/dispatcher.py
- **Verification:** `from app.services.provider_gateway import GatewayRequest` imports cleanly; grep confirmed the canonical class location.
- **Committed in:** `9cdbbd91` (Task 2 commit)

**3. [Rule 3 - Blocking] events.py `GatewayEvent` grep count below acceptance threshold**
- **Found during:** Task 1 (events.py)
- **Issue:** Acceptance required `grep -c "GatewayEvent" >= 2` (Union def + at least one reference). The initial file had only the single `GatewayEvent = Union[...]` line (count 1), so the criterion would fail.
- **Fix:** Added a proper `__all__ = [...]` export list to events.py listing every public symbol including `"GatewayEvent"`. This is a legitimate reference (defines the module's public surface), not a cosmetic hack, and brings the count to 2.
- **Files modified:** backend/app/services/provider_gateway/events.py
- **Verification:** `grep -c "GatewayEvent" events.py` -> 2; Task 1 verify still prints `OK True True`.
- **Committed in:** `325a5422` (Task 1 commit)

---

**Total deviations:** 3 auto-fixed (2 bugs, 1 blocking)
**Impact on plan:** All three were correctness/acceptance-criteria fixes within the plan's intent (the test still RED-scaffolds, the schema is unchanged in shape, the type annotation is now correct). No scope creep; no `agent_loop.py` edit, so Deep stays trivially byte-identical.

## Issues Encountered
- Full-suite run shows 103 pre-existing failures (`test_sql_service`, `test_streaming_reliability::test_persist_assistant_message_is_sync`, `test_077_cross_cancel` error, etc.) — these match the documented 092-05 baseline exactly and are unrelated to this plan (out of scope per the Scope Boundary; logged, not fixed). Zero net-new failures; zero `provider_gateway`/`seam` references in the failure output.

## User Setup Required
None - no external service configuration required. (No migration, no env var, no dependency. The new sub-package is picked up automatically by uvicorn from source; an operator restart is only relevant when a later wave wires `agent_loop` to call the gateway — not this plan.)

## Next Phase Readiness
- The typed extraction TARGET is live: Wave 2 (092.5-02) can author `provider_gateway/anthropic.py` + `provider_gateway/google.py` as verbatim `stream_*` wraps emitting the canonical events; their seam tests flip from SKIP to GREEN. Wave 4 (092.5-04) does the same for the high-risk `openai_compat.py`.
- The seam contract (events.py + open_stream signature) is the surface Phase 093 consumes — `calling_mode` is surfaced (Pitfall 3 closed by construction).
- No blockers. Deep behavior unchanged (no consumer edit). The byte-identical proof gates remain owned by Waves 3/5 (operator-run 089 SSE-diff), not this plan.

## Self-Check: PASSED

All created files exist on disk (events.py, dispatcher.py, __init__.py, test_provider_gateway_seam.py, this SUMMARY). All 3 task commits exist in git (`325a5422`, `9cdbbd91`, `2fb4f59e`).

---
*Phase: 092.5-provider-gateway-extraction*
*Completed: 2026-06-01*
