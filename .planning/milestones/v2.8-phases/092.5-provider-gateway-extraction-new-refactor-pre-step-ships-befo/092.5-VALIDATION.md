---
phase: 092.5
slug: provider-gateway-extraction-new-refactor-pre-step-ships-befo
status: planned
nyquist_compliant: true
wave_0_complete: false
created: 2026-06-01
plan_map:
  wave_0: "092.5-01 (seam-test scaffold + dispatcher/events) + 092.5-02 (native-7 BEFORE baselines)"
  extraction_step_1: "092.5-03 (Anthropic+Google adapters + stream_* monkeypatch repoint) -> 092.5-04 (operator byte-identical gate #1)"
  extraction_step_2: "092.5-05 (OpenAI-compat adapter entangled + create_adaptive_streaming_chat/_accumulate_chunk_usage repoint) -> 092.5-06 (operator byte-identical gate #2 + I1-I8 named)"
  nyquist_note: "Every autonomous task has an <automated> verify. The two byte-identical gates (04/06) are operator-run (live native-7 keys = manual-only). No 3 consecutive tasks without an automated verify: the operator gate plans are single-task and sit between automated extraction plans. The monkeypatch sweep is split across Plan 03 (stream_anthropic/stream_google) and Plan 05 (create_adaptive_streaming_chat + _accumulate_chunk_usage) so each repoint lands with the branch it follows (repointing create_adaptive_streaming_chat early would break the still-direct OpenAI call)."
---

# Phase 092.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> This is a **byte-identical extraction**: the validation gate is the Phase 089 SSE
> skeleton-diff proof (Deep must be structurally identical before AND after), plus one
> NEW deterministic seam-contract test (D-08). The harness + round-trip unit tests already
> exist — no re-author.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (`asyncio_mode = auto`) |
| **Config file** | `backend/pytest.ini` (`testpaths = tests`) |
| **Quick run command** | `backend/venv/Scripts/python.exe -m pytest backend/tests/unit/test_chunk_handler_provider_aware.py backend/tests/integration/test_075_4_terminal_race.py -x` |
| **Full suite command** | `backend/venv/Scripts/python.exe -m pytest backend/tests/ -q` |
| **Seam-contract test (NEW, D-08)** | `backend/venv/Scripts/python.exe -m pytest backend/tests/unit/test_provider_gateway_seam.py -x` |
| **SSE-diff proof (operator, native-7)** | `backend/venv/Scripts/python.exe scripts/capture_sse_baseline.py --mode before\|after` |
| **Eval backstop (operator, live keys)** | `backend/venv/Scripts/python.exe scripts/eval_cross_provider.py` |
| **E2E backstop** | `frontend/tests/e2e/scenario-*.spec.ts` (Playwright, Phase 075.4) |
| **Estimated runtime** | quick ~30s · full suite ~minutes · SSE-diff/eval operator-run (live) |

---

## Sampling Rate

- **After every task commit:** Run quick command (chunk-handler unit + terminal-race) — < 30s.
- **After every plan wave:** Full pytest suite GREEN + the NEW seam-contract test GREEN.
- **Before `/gsd-verify-work`:** Full suite GREEN + seam-contract GREEN + `SSE_DIFF_RESULT: PASS` (native-7, operator) + `EVAL_SUMMARY` native-7 all-pass before AND after (operator) + E2E GREEN.
- **Max feedback latency:** < 30s per commit (automated); live UAT operator-gated at phase boundary.

---

## Per-Task Verification Map

> The skeleton gate structurally covers I1/I2/I3/I4/I6; I5/I7/I8 via backend log + Supabase + unit test. Plan/task IDs filled in below.

| Req / SC | Behavior | Plan/Task | Test Type | Automated / surface command | File Exists | Status |
|----------|----------|-----------|-----------|------------------------------|-------------|--------|
| GATEWAY-01 SC#3 / D-08 | Seam contract: synthetic provider stream → expected canonical events | 092.5-01 T3 (scaffold) → 092.5-03 T3 (Ant/Goog green) → 092.5-05 T3 (OpenAI green) | unit (deterministic, no live keys) | `pytest backend/tests/unit/test_provider_gateway_seam.py -x` | ❌→✅ W0 NEW | ⬜ pending |
| GATEWAY-01 SC#1 (plumbing) | Full suite GREEN after the move (monkeypatch targets updated) | 092.5-03 T3 (stream_*) + 092.5-05 T3 (create_adaptive + _accumulate) | unit/integration | full suite | ⚠️ W0 sweep split 03/05 | ⬜ pending |
| GATEWAY-01 SC#2 / I8 | Usage accounting: `_accumulate_chunk_usage` Google-cumulative vs OpenAI-`+=` preserved | 092.5-05 T1 (moves in) + T3 (test repoint) | unit | `pytest backend/tests/unit/test_chunk_handler_provider_aware.py -x` | ✅ exists | ⬜ pending |
| GATEWAY-01 SC#1 | Deep BYTE-IDENTICAL clean providers: skeleton SSE diff empty native-7 | 092.5-04 T1 (operator gate #1, after Ant/Goog) | manual (live keys) | `scripts/capture_sse_baseline.py --mode after` → `SSE_DIFF_RESULT: PASS` | ✅ harness; baselines from 092.5-02 | ⬜ pending |
| GATEWAY-01 SC#1 | Deep BYTE-IDENTICAL full native-7: skeleton SSE diff empty (incl. OpenAI-path) | 092.5-06 T1 (operator gate #2, after OpenAI) | manual (live keys) | `scripts/capture_sse_baseline.py --mode after` → `SSE_DIFF_RESULT: PASS` | ✅ harness | ⬜ pending |
| GATEWAY-01 SC#1 (backstop) | eval + E2E GREEN before AND after | 092.5-02 (before) / 092.5-04 / 092.5-06 (after) | manual + Playwright | `eval_cross_provider.py` → `EVAL_SUMMARY`; `scenario-*.spec.ts` | ✅ exist | ⬜ pending |

### D-07 Per-Provider Round-Trip Invariants (MUST be named in VERIFICATION — owned by Plan 092.5-06 T1)

| # | Invariant | Post-extraction owner | How to observe / assert | Evidence surface |
|---|-----------|------------------------|--------------------------|------------------|
| I1 | Anthropic `end_turn` tool execution | CONSUMER `agent_loop.py:2107-2111` (stays) | Multi-tool Anthropic prompt dispatches tools; `messages.tool_calls` populated; skeleton shows dispatch sequence | SSE capture (anthropic.json) + Supabase `messages.tool_calls` |
| I2 | Google `thought_signature` echo (base64 sig round-tripped on round 2+) | GATEWAY google.py emits sig; CONSUMER persists `:2150-2154` + rebuilds `_reconstruct_history:776-786` | 2+-tool-round Google run does NOT 400 on round 2; `messages.tool_calls[].thought_signature` present | SSE (google.json), Supabase, backend log (no INVALID_ARGUMENT) |
| I3 | DeepSeek `reasoning_content` round-trip | GATEWAY openai_compat emits `reasoning_delta`; CONSUMER accumulates + appends `:2166` | DeepSeek tool-call turn does NOT 400 "reasoning_content must be passed back" | SSE (deepseek.json — `reasoning_delta` token), backend log |
| I4 | Moonshot/Kimi `<think>`-strip (routed to reasoning, not visible) | GATEWAY openai_compat `<think>` state machine (moves in) | Moonshot/MiniMax/GLM run shows reasoning_delta, no `<think>` leak into `delta` | SSE (moonshot/minimax/zhipu.json), clean `delta` |
| I5 | Empty-retry (empty response retried once) | CONSUMER `agent_loop.py:2120-2126` (stays) | A run returning empty content retries once before terminal | backend log "retrying once" + skeleton |
| I6 | `force_no_tools`-on-last-iteration + cap-pause persists buffered calls | CONSUMER `:1367-1368` + cap-pause `:2028-2066` (stays) | Final iteration `tool_choice="none"`; buffered calls → `cap_paused` carrier row + event | SSE (`cap_paused` event), Supabase carrier row |
| I7 | Iteration cap (explorer=8, general=15) | CONSUMER `:1317` + `:946/:950` (stays) | Loop terminates at cap; no infinite loop | backend log iteration count |
| I8 | Usage accounting (Google cumulative-overwrite vs OpenAI `+=`) | GATEWAY openai_compat `_accumulate_chunk_usage` (moves in); Anthropic/Google emit usage events | Token totals match pre-extraction (no 2-3× Google over-count) | `runs` token columns, unit test |

---

## Wave 0 Requirements (split across Plans 092.5-01 / 092.5-02 / and the per-step sweep in 03/05)

- [ ] `scripts/.sse_baseline/<provider>.json` — native-7 BEFORE baselines → **092.5-02** (operator runs `--mode before` against the PRE-extraction loop FIRST; STALE 089-era JSONs re-captured because 092 edited agent_loop).
- [ ] `backend/tests/unit/test_provider_gateway_seam.py` — the D-08 seam-contract test scaffold (importorskip RED) → **092.5-01 T3**; Anthropic/Google groups GREEN in **092.5-03 T3**; OpenAI groups GREEN in **092.5-05 T3**.
- [ ] **Monkeypatch-target sweep** (split by branch): `stream_anthropic`/`stream_google` patch targets → **092.5-03 T3**; `app.services.agent_loop.create_adaptive_streaming_chat` (~30 tests) + the doubly-stale `app.api.threads._accumulate_chunk_usage` → **092.5-05 T3**. Title-gen patches in `threads.py` STAY.
- [ ] Framework install: **none** — pytest + Playwright already present.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Native-7 byte-identical SSE diff | GATEWAY-01 SC#1 | Requires live provider keys; raw-diffing live LLMs is impossible (skeleton gate only) | Operator starts uvicorn (visible terminal, never `run_in_background`), local Supabase + Redis up, native-7 keys in `backend/.env`. Run `--mode before` (pre-extraction, 092.5-02) → baselines. After each provable step + uvicorn restart: `--mode after` → `SSE_DIFF_RESULT: PASS` (092.5-04 after clean providers; 092.5-06 after OpenAI). Re-run 2–3× to rule out LLM tool-path non-determinism. |
| Cross-provider eval scoreboard | GATEWAY-01 SC#1 | Live keys; SEED-034 scoreboard | `scripts/eval_cross_provider.py` before AND after → `EVAL_SUMMARY` native-7 all-pass identical or strictly non-regressing |
| E2E flow | GATEWAY-01 SC#1 | Browser-driven | Playwright `scenario-*.spec.ts` GREEN before AND after |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 / operator-gate dependencies (autonomous tasks: automated; gate plans 02/04/06: operator manual-only, justified above)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (operator gates sit between automated extraction plans)
- [x] Wave 0 covers all MISSING references (seam test 092.5-01, BEFORE baselines 092.5-02, monkeypatch sweep split 092.5-03/05)
- [x] No watch-mode flags
- [x] Feedback latency < 30s (automated)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** planner-approved 2026-06-01 (status: planned). `wave_0_complete` flips true once 092.5-01 + 092.5-02 ship.
