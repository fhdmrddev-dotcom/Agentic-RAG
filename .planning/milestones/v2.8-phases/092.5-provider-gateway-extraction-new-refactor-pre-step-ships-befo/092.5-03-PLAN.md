---
phase: 092.5-provider-gateway-extraction
plan: 03
type: execute
wave: 2
depends_on: ["092.5-01", "092.5-02"]
files_modified:
  - backend/app/services/provider_gateway/anthropic.py
  - backend/app/services/provider_gateway/google.py
  - backend/app/services/provider_gateway/dispatcher.py
  - backend/app/services/agent_loop.py
  - backend/tests/unit/test_provider_gateway_seam.py
  - backend/tests/test_provider_router.py
  - backend/tests/integration/test_075_tool_args_progress.py
autonomous: true
requirements: [GATEWAY-01]

must_haves:
  truths:
    - "The Anthropic + Google provider branches are extracted VERBATIM into provider_gateway/anthropic.py + google.py (stream-construction move; they already emit canonical events)."
    - "agent_loop's Anthropic + Google branches call gateway.open_stream(...) and the two _on_chunk_anthropic/_on_chunk_google callbacks collapse into ONE shared provider-agnostic _on_chunk for these two providers — full_content/token/tool_calls_buffer accumulation STAYS consumer-side (D-02/D-03)."
    - "The Google thought_signature hydration onto tool_calls_buffer STAYS consumer-side (it mutates a consumer accumulator); the adapter only EMITS the finish event carrying tool_calls[].thought_signature (I2 preserved)."
    - "Monkeypatch targets for stream_anthropic/stream_google are repointed so the suite stays green; the seam test's Anthropic/Google groups go GREEN."
  artifacts:
    - path: "backend/app/services/provider_gateway/anthropic.py"
      provides: "Anthropic adapter wrapping stream_anthropic, yielding canonical events"
      contains: "def open_stream\|async def stream\|stream_anthropic"
    - path: "backend/app/services/provider_gateway/google.py"
      provides: "Google adapter wrapping stream_google, yielding canonical events incl. thought_signature on finish.tool_calls"
      contains: "stream_google"
  key_links:
    - from: "backend/app/services/agent_loop.py"
      to: "backend/app/services/provider_gateway/dispatcher.py"
      via: "open_stream(provider, request) for the anthropic + google branches"
      pattern: "open_stream|provider_gateway"
    - from: "backend/app/services/provider_gateway/anthropic.py"
      to: "app.services.anthropic_service.stream_anthropic"
      via: "wraps the raw SDK boundary (does not reimplement)"
      pattern: "stream_anthropic"
---

<objective>
Extract the two CLEAN provider branches (Anthropic `:1396-1528`, Google `:1530-1655`) into `provider_gateway/anthropic.py` + `google.py` and point `agent_loop`'s those-two branches at `gateway.open_stream(...)`. These are low-risk because both `stream_anthropic`/`stream_google` ALREADY yield the canonical event schema (their docstrings ARE the template) — the adapters are essentially a stream-construction move. The two `_on_chunk_anthropic`/`_on_chunk_google` callbacks collapse into ONE shared provider-agnostic consumer handler that mutates the STAY accumulators (`full_content`, token totals, `tool_calls_buffer`, `finish_reason`) and calls `_emit`. This is the first provable byte-identical step (proven by the operator in Wave 3 / Plan 04).

Purpose: Prove the seam cut on the easy providers first (D-04 / RESEARCH Open Q3), isolating the entangled OpenAI adapter to its own later step. Get the dispatcher's two clean adapters live + the shared-handler shape settled before touching the hard unit.
Output: anthropic.py + google.py adapters; agent_loop's two clean branches consuming the gateway; collapsed shared _on_chunk for those two; repointed monkeypatch targets; seam test Anthropic/Google groups GREEN.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-CONTEXT.md
@.planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-RESEARCH.md
@.planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-PATTERNS.md

<interfaces>
<!-- From Plan 01 (already shipped): the gateway package -->
From backend/app/services/provider_gateway/:
```python
from app.services.provider_gateway import open_stream, GatewayRequest, GatewayEvent, CallingMode
# open_stream(provider: str, request: GatewayRequest) -> tuple[AsyncIterator[GatewayEvent], CallingMode]
# Anthropic + Google adapters return CallingMode.NATIVE.
```

<!-- The stream constructions to MOVE (verbatim, from agent_loop.py — LIVE line numbers) -->
Anthropic construction (agent_loop.py:1415-1422) — MOVES into anthropic.py:
```python
_ant_gen = stream_anthropic(
    messages=messages, tools=_ant_tools, system_prompt=active_system_prompt,
    model=_model_id, api_key=_ant_api_key, max_tokens=_ant_max_tokens,
    force_no_tools=force_no_tools,
)
```
Google construction (agent_loop.py:1553-1561) — MOVES into google.py:
```python
_g_gen = stream_google(
    messages=messages, tools=_g_tools, system_prompt=active_system_prompt,
    model=_model_id, api_key=_g_api_key, max_tokens=_g_max_tokens,
    force_no_tools=force_no_tools,
)
```
`stream_anthropic` (anthropic_service.py:152) and `stream_google` (google_service.py:381) ALREADY yield the canonical event dicts. The kwargs-assembly currently inline (key resolution via `user_settings.llm_api_key or settings.llm_api_key`, `_resolve_max_tokens`, tool select) moves with the construction.

<!-- The Google I2 invariant that STAYS consumer-side (PATTERNS.md lines 115-125) -->
thought_signature hydration in _on_chunk_google's finish branch (agent_loop.py:1638-1647) MUTATES tool_calls_buffer (a consumer accumulator) → STAYS consumer-side. The adapter only EMITS the finish event carrying tool_calls[].thought_signature (stream_google already does this, google_service.py:510-517/:611-615). The events.py FinishEvent already carries the field (Plan 01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Anthropic + Google adapters (verbatim stream-construction move)</name>
  <read_first>
    - backend/app/services/agent_loop.py:1396-1528 (the ENTIRE Anthropic branch — construction :1415-1422, _on_chunk_anthropic :1449-1520, drain :1522-1527, per-branch accumulators :1424-1426)
    - backend/app/services/agent_loop.py:1530-1655 (the ENTIRE Google branch — construction :1553-1561, _on_chunk_google :1566-1647 incl. thought_signature hydrate :1638-1647, drain :1649-1655, accumulators :1562-1564)
    - backend/app/services/anthropic_service.py:6-14 + :152 (stream_anthropic + its canonical-event docstring)
    - backend/app/services/google_service.py:1-19 + :381 + :510-517 + :611-615 (stream_google + canonical schema + thought_signature carriage)
    - .planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-PATTERNS.md §"anthropic.py" (lines 74-97) + §"google.py" (lines 101-127) — the MOVES/STAYS disposition tables
  </read_first>
  <action>
    Fill in `backend/app/services/provider_gateway/anthropic.py` and `google.py` (the dispatcher stubs from Plan 01 currently raise NotImplementedError for these). Each adapter:

    1. Takes the `GatewayRequest` envelope and assembles the `stream_anthropic`/`stream_google` kwargs from it (the key resolution `user_settings.llm_api_key or settings.llm_api_key`, `_resolve_max_tokens`, tool select — MOVE the inline assembly from agent_loop.py:1398-1422 / :1537-1561 verbatim into the adapter). Import `_resolve_max_tokens` from `app.services.openai_service` (it's already imported inline at agent_loop.py:1398/:1537 — same source).
    2. Wraps the construction: `async for event in stream_anthropic(...): yield event` (and the Google equivalent). stream_anthropic/stream_google ALREADY yield canonical GatewayEvent dicts — DO NOT re-shape them (RESEARCH anti-pattern: "Re-shaping Anthropic/Google events 'for consistency' risks a skeleton-diff for no gain"). Pass through verbatim.
    3. Returns `CallingMode.NATIVE` alongside the stream (these providers have no STRUCTURED mode).

    Wire these two adapters into `dispatcher.open_stream`: provider "anthropic" → anthropic adapter, "google" → google adapter (replace the Plan 01 NotImplementedError stubs for these two; OpenAI-compat stays a stub until Wave 4).

    The `close_fn=_ant_gen.close` arg (agent_loop.py:1522-1527) is Anthropic-specific (sync generator close on timeout, load-bearing) — the drain stays consumer-side (Task 2), so the adapter must EXPOSE its underlying generator's `.close` to the consumer (e.g. return the async generator object whose `.close`/`.aclose` the consumer's drain can call). Preserve this exactly — carry `close_fn` on whichever side owns the drain (the consumer, per RESEARCH A2).

    DO NOT touch the OpenAI branch, the OpenAI adapter, `task_service`, or `sub_agent_service.py`. DO NOT do any "while-I'm-in-here" cleanup. Verbatim move only.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -c "from app.services.provider_gateway import open_stream, CallingMode; from app.services.provider_gateway import anthropic as a, google as g; print('OK', hasattr(a,'__name__'), hasattr(g,'__name__'))"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/services/provider_gateway/anthropic.py` and `google.py` exist and each `grep -c "stream_anthropic\|stream_google" <file>` >= 1 (wraps the raw SDK boundary).
    - `grep -c "raise NotImplementedError" backend/app/services/provider_gateway/dispatcher.py` decreases vs Plan 01 (anthropic+google stubs replaced; openai stub may remain — expect <= 1 remaining for the openai branch).
    - `grep -rc "from app.services.agent_loop\|from app.api.threads\|import agent_loop\|import threads" backend/app/services/provider_gateway/` == 0 (no import cycle — SC#3).
    - `grep -c "close" backend/app/services/provider_gateway/anthropic.py` >= 1 OR the consumer-side drain still has access to `.close`/`.aclose` (the Anthropic sync-gen close is preserved).
  </acceptance_criteria>
  <done>anthropic.py + google.py wrap stream_anthropic/stream_google, yield canonical events verbatim, return CallingMode.NATIVE, and are wired into open_stream; the Anthropic close_fn is preserved; no import cycle; no OpenAI/task_service edit.</done>
</task>

<task type="auto">
  <name>Task 2: Wire agent_loop's Anthropic + Google branches to the gateway + collapse their two _on_chunk into ONE shared handler</name>
  <read_first>
    - backend/app/services/agent_loop.py:1052-1085 (the STAY accumulator declarations — full_content, full_reasoning_content, input/output_tokens_total, _empty_retries — these do NOT move, D-02/D-03)
    - backend/app/services/agent_loop.py:1386-1655 (the while-True retry block + Anthropic + Google branches being rewired)
    - backend/app/services/agent_loop.py:1449-1520 (_on_chunk_anthropic) + :1566-1647 (_on_chunk_google) — the two handlers collapsing into one
    - backend/app/services/agent_loop.py:311-338 (_drain_stream_with_close_on_cancel — STAYS consumer-side, RESEARCH A2)
    - .planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-RESEARCH.md §"Pattern 2: ONE provider-agnostic consumer _on_chunk" (lines 185-188) + §"Architectural Responsibility Map" (lines 57-66)
  </read_first>
  <action>
    In `agent_loop.run_agent_loop`, rewire ONLY the Anthropic branch (:1396-1528) and the Google branch (:1530-1655) to call the gateway. Leave the OpenAI-compat `else` branch (:1657-1974) EXACTLY as-is (Wave 4 rewires it).

    1. Build a `GatewayRequest` from the in-scope loop values (messages, model, tools, system_prompt, force_no_tools, max_tokens, api_key, user_settings, tool_choice, active_provider_name) and call `_stream, _mode = await open_stream(active_provider_name, request)`. For the Anthropic + Google providers this replaces the inline `_ant_gen = stream_anthropic(...)` / `_g_gen = stream_google(...)` construction (now moved into the adapters).
    2. Collapse `_on_chunk_anthropic` + `_on_chunk_google` into ONE shared `async def _on_chunk(event)` that switches on `event["type"]` and mutates the SAME STAY accumulators they mutate today (full_content via `delta`, full_reasoning_content via `reasoning_delta`, input/output_tokens_total via `usage`/`usage_delta`, tool_calls_buffer + finish_reason via tool_preparing/tool_args_progress/tool_start/finish, and `_emit(...)` mid-stream EXACTLY where the two handlers emit today). Since Anthropic/Google handlers are ALREADY near-identical (the Google docstring at :1566-1573 literally says "structurally a copy of the Anthropic branch"), this collapse is mechanical — preserve every `_emit` call site, every `.get("code_so_far","")` defensive-get (:1508/:1621), and the per-event mutation order verbatim.
    3. Keep the thought_signature hydration loop (:1638-1647) consumer-side INSIDE the shared `_on_chunk`'s `finish` branch — it mutates `tool_calls_buffer` (a consumer accumulator), so it STAYS (PATTERNS.md "Seam decision": do NOT move it into the adapter — same class as L-3). The adapter emits the finish event carrying tool_calls[].thought_signature; the consumer hydrates the buffer.
    4. Keep `_drain_stream_with_close_on_cancel(_stream, ..., on_chunk_async=_on_chunk, close_fn=<gen close>)` consumer-side (it's cancel/loop machinery, RESEARCH A2). Carry the Anthropic-specific `close_fn` from the adapter's returned generator.

    The accumulator declarations at :1052-1085 do NOT move. `_emit`/`_spawn` stay passed-in callables (no import cycle). NO behavior change intended — this is a verbatim collapse. Any skeleton-diff on anthropic.json/google.json in Wave 3 is a BLOCKER, not a thing to plan around.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -c "import app.services.agent_loop as al; import app.api.threads as t; print('OK imports clean', hasattr(al,'run_agent_loop'))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "open_stream\|provider_gateway" backend/app/services/agent_loop.py` >= 1 (the two clean branches now dispatch through the gateway).
    - The OpenAI branch is UNTOUCHED: `grep -c "create_adaptive_streaming_chat" backend/app/services/agent_loop.py` is still >= 1 (the OpenAI `else` still calls it directly — Wave 4 rewires it).
    - The Anthropic + Google inline constructions are gone from agent_loop: after this task `grep -c "_ant_gen = stream_anthropic\|_g_gen = stream_google" backend/app/services/agent_loop.py` == 0 (moved into adapters) — OR the inline call is replaced by open_stream (no direct stream_anthropic/stream_google construction remains in run_agent_loop for these two branches).
    - The two handler defs collapse: `grep -c "async def _on_chunk_anthropic\|async def _on_chunk_google" backend/app/services/agent_loop.py` == 0 (replaced by one shared `_on_chunk`).
    - agent_loop + threads import cleanly (Task 2 verify command prints `OK imports clean True`).
  </acceptance_criteria>
  <done>agent_loop's Anthropic + Google branches dispatch through gateway.open_stream; their two _on_chunk callbacks are collapsed into ONE shared consumer handler that mutates the STAY accumulators + emits exactly as before; thought_signature hydration + drain stay consumer-side; OpenAI branch untouched; imports clean.</done>
</task>

<task type="auto">
  <name>Task 3: Repoint monkeypatch targets for stream_anthropic/stream_google + flip seam test Anthropic/Google groups GREEN + full suite</name>
  <read_first>
    - backend/tests/test_provider_router.py:243-246 (patches app.services.agent_loop.stream_anthropic — must repoint)
    - backend/tests/integration/test_075_tool_args_progress.py:506 (patches app.services.agent_loop.stream_anthropic) + :839 (patches app.services.agent_loop.stream_google) — must repoint
    - backend/tests/unit/test_provider_gateway_seam.py (the Plan 01 scaffold — flip the Anthropic/Google importorskip groups to live, assert the canonical-event sequences)
    - .planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-PATTERNS.md §"Monkeypatch-target sweep" (lines 244-246)
    - .planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-VALIDATION.md §"Wave 0 Requirements" (the monkeypatch sweep bullet)
  </read_first>
  <action>
    Repoint every test that patches `app.services.agent_loop.stream_anthropic` / `app.services.agent_loop.stream_google` to the new location the adapters import from. Because the adapters now own the `stream_anthropic`/`stream_google` calls, patch the symbol IN THE ADAPTER namespace (`app.services.provider_gateway.anthropic.stream_anthropic`, `app.services.provider_gateway.google.stream_google`) — OR, if the adapter imports them via `from app.services.anthropic_service import stream_anthropic`, patch there. Grep-confirm the exact import site in each adapter and repoint accordingly. Known sites to fix:
    - `backend/tests/test_provider_router.py:246` (`app.services.agent_loop.stream_anthropic`)
    - `backend/tests/integration/test_075_tool_args_progress.py:506` (stream_anthropic) + `:839` (stream_google)

    Do NOT touch the `create_adaptive_streaming_chat` patch sites yet (~30 tests) — those still target the OpenAI branch which Wave 4 rewires; repointing them now would break the still-direct OpenAI call. (The Wave 4 plan owns the create_adaptive_streaming_chat + `_accumulate_chunk_usage` sweep.) Title-gen patches in `threads.py` STAY untouched (title-gen is not in the gateway).

    In `test_provider_gateway_seam.py`, flip the Anthropic + Google importorskip-gated groups to live tests now that the adapters exist: feed each a synthetic stream_anthropic/stream_google event stream (mock the raw SDK boundary), assert the adapter yields the expected canonical GatewayEvent sequence — and for Google, assert the `finish` event's `tool_calls[].thought_signature` is carried (I2). Keep the OpenAI groups importorskip-gated (Wave 4).

    Run the full suite and confirm ZERO net-new failures vs the Plan-02 baseline.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -m pytest backend/tests/unit/test_provider_gateway_seam.py backend/tests/test_provider_router.py backend/tests/integration/test_075_tool_args_progress.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "app.services.agent_loop.stream_anthropic\|app.services.agent_loop.stream_google" backend/tests/` == 0 (all stream_* patch targets repointed off the agent_loop namespace).
    - The seam test's Anthropic + Google groups now RUN (not skip): `pytest backend/tests/unit/test_provider_gateway_seam.py -q` shows the anthropic/google tests as passed, openai tests as skipped.
    - `grep -c "thought_signature" backend/tests/unit/test_provider_gateway_seam.py` >= 1 and the Google adapter test asserting it PASSES (I2 contract live).
    - `create_adaptive_streaming_chat` patch sites are NOT changed in this plan: `grep -rc "app.services.agent_loop.create_adaptive_streaming_chat" backend/tests/` is unchanged from before this plan (Wave 4 owns it).
    - Full suite (`backend/venv/Scripts/python.exe -m pytest backend/tests/ -q`) shows ZERO net-new failures vs the Plan-02 baseline failure set.
  </acceptance_criteria>
  <done>stream_anthropic/stream_google monkeypatch targets repointed; seam test Anthropic/Google groups GREEN (incl. I2 thought_signature); OpenAI patch sites + title-gen patches untouched; full suite zero net-new failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer (`agent_loop`) → gateway adapters | In-process call; the GatewayRequest envelope (incl. resolved api_key) crosses here. Same worker, same RLS-scoped context. No new external boundary. |
| adapters → raw SDK services | In-process; adapters wrap the existing stream_anthropic/stream_google boundaries that already handle provider keys. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-092.5-04 | Information Disclosure | anthropic.py / google.py (key resolution moves into adapters) | accept | The api_key resolution (`user_settings.llm_api_key or settings.llm_api_key`) MOVES verbatim with the stream construction — same resolution path, same in-process scope, no new logging of the value, no persistence of the key in the adapter. RESEARCH §Security Domain confirms the adapters read the SAME resolved key the inline code does today. |
| T-092.5-05 | Tampering | shared _on_chunk consumer handler | accept | The handler mutates the same accumulators verbatim; tool-arg parsing/validation stays consumer-side (parse_structured_tool_calls is OpenAI-path-only, untouched here). No relaxation of any validation. |

**Statement (per security_gate):** NO HIGH-severity threats. Pure verbatim relocation of stream construction + handler collapse. Secret resolution unchanged (same path, in-process). RLS/ownership enforcement stays consumer-side + endpoints (unchanged). The byte-identical SSE-diff gate (Wave 3) is the behavioral proof that no control was altered.
</threat_model>

<verification>
- anthropic.py + google.py exist, wrap stream_anthropic/stream_google, yield canonical events verbatim, return CallingMode.NATIVE, wired into open_stream.
- agent_loop's Anthropic + Google branches dispatch through the gateway; the two _on_chunk callbacks collapsed into one; OpenAI branch untouched.
- thought_signature hydration (I2) + drain (A2) stay consumer-side.
- stream_anthropic/stream_google monkeypatch targets repointed; seam test Anthropic/Google groups GREEN.
- Full suite zero net-new failures.
- NO edits to task_service / sub_agent_service.py (D-05); no import cycle (SC#3).
- (Byte-identical proof for anthropic.json/google.json is the Wave 3 / Plan 04 operator gate — this plan's automated proof is the seam test + suite-green; the SSE-diff is operator-gated.)
</verification>

<success_criteria>
- The two clean providers (Anthropic + Google) are extracted into the gateway and Deep consumes them through `open_stream`.
- The shared provider-agnostic `_on_chunk` shape is settled (Wave 4 just adds the OpenAI branch onto it).
- D-07 invariant I2 (Google thought_signature echo) is preserved + asserted in the seam test.
- Suite green; no OpenAI/task_service/sub_agent edits.
</success_criteria>

<output>
After completion, create `.planning/phases/092.5-provider-gateway-extraction-new-refactor-pre-step-ships-befo/092.5-03-SUMMARY.md`
</output>
