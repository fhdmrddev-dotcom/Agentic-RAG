---
phase: 094-workflow-legibility-mode-clarity
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/stores/streamsStore.ts
  - frontend/src/providers/StreamsProvider.tsx
  - frontend/src/types/index.ts
  - frontend/src/providers/__tests__/phaseHooks.test.tsx
autonomous: true
requirements: [PANEL-08, PANEL-09]
must_haves:
  truths:
    - "A harness phase_* / gate_failed / run_failed event becomes panel state via exactly one additive else-if branch in api.ts, with NO change to any Deep branch"
    - "A phasesByThread mutation triggers ZERO chat message-list re-renders (PANEL-09 reference-identity)"
    - "phasesByThread is keyed by the OWNING thread id so a background harness run never corrupts the viewed thread's timeline"
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "additive onPhaseStarted/onPhaseCompleted/onPhaseTransition/onGateFailed/onRunFailed/onRunCompleted callbacks + dispatch branches"
      contains: "phase_started"
    - path: "frontend/src/stores/streamsStore.ts"
      provides: "phasesByThread Map slice + no-op action stubs + Phase type"
      contains: "phasesByThread"
    - path: "frontend/src/providers/StreamsProvider.tsx"
      provides: "onPhase* demux defaults + phase action bodies + usePhases selector"
      contains: "usePhases"
  key_links:
    - from: "frontend/src/lib/api.ts"
      to: "frontend/src/providers/StreamsProvider.tsx"
      via: "onPhase* callbacks calling actions.*PhasesForThread(threadId, ...)"
      pattern: "appendPhaseForThread|setPhaseStatusForThread"
    - from: "frontend/src/providers/StreamsProvider.tsx"
      to: "frontend/src/stores/streamsStore.ts"
      via: "usePhases reads phasesByThread.get(threadId)"
      pattern: "phasesByThread\\.get"
---

<objective>
Wire the harness producer event vocabulary into panel state: add the additive `phase_*` / `gate_failed` / `run_failed` / `run_completed` SSE branches in `api.ts` (after the Deep dispatch, byte-identical), a panel-only `phasesByThread` store slice mirroring `tasksByThread`, the demux defaults + action bodies + `usePhases` selector in StreamsProvider, and the `Phase` type. This is the PANEL-08/PANEL-09 substrate that Plan 03's timeline renders.

Purpose: the events are wire-emitted today but silently dropped (api.ts has ZERO phase handlers, verified). This plan is the single place a wire event becomes panel state. The hard part is discipline: Deep branches stay byte-identical (Pitfall 1), chat never reads `phasesByThread` (PANEL-09), and every write keys by the owning thread (Pitfall 6).
Output: 4 source files + the phaseHooks.test.tsx flipped GREEN.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/094-workflow-legibility-mode-clarity/094-CONTEXT.md
@.planning/phases/094-workflow-legibility-mode-clarity/094-RESEARCH.md
@.planning/phases/094-workflow-legibility-mode-clarity/094-PATTERNS.md
@.planning/sketches/094-grounding/DATA-CONTRACT.md

<interfaces>
From frontend/src/lib/api.ts (the dispatch switch + StreamCallbacks interface to mirror):
- StreamCallbacks interface declared at line 223; onCapPaused at :305-316 (the NON-terminal panel-callback shape to mirror); onTaskStart/onTaskDone at :341/344.
- The dispatch switch opens at line 485 (`delta`), the last branch `cap_paused` closes at ~666, the switch closes at 667. Cursor-advance block fires at 676-679 (only when a branch did NOT `return`).
- New phase branches insert as the LAST else-if branches of the switch, after cap_paused, BEFORE the closing brace. They carry NO `return`.
- Backend emits FLAT fields (verified harness_engine.py): phase_started{phase,phase_index,phase_type}, phase_completed{phase,phase_index}, phase_transition{from_phase,to_phase,via}, gate_failed{phase,attempt,error}, run_failed{reason}, run_completed{status}.

From frontend/src/stores/streamsStore.ts (the slice to mirror):
- tasksByThread: Map<string, TaskRunIndexItem[]> at interface :120, default :254.
- workflowLockByThread: Map<string, WorkflowLock> at :135/258.
- No-op action stubs at :272-286 (synchronous () => {} stubs, NOT notMounted).

From frontend/src/providers/StreamsProvider.tsx:
- makeStreamCallbacks factory at :278; panel-event defaults at :681-720 (closes over threadId).
- tasksByThread action bodies at :1806-1837 (new Map(prev) copy-then-mutate immutable replace).
- useThreadMessages (the CHAT selector, PANEL-09 boundary) at :1974 reads bucketsBySurface EXCLUSIVELY.
- useTasks selector at :2052 ({data, isLoading, error, reconcile}, fetcher getThreadTasks).
- useWorkflowLockForThread at :2132.

The normalized Phase shape (DATA-CONTRACT section 3b): {slug, phaseIndex, phaseType, status, attempt?, error?, subAgents, pendingAsk}; status in {pending,running,done,failed,retrying,skipped}.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Phase type + phasesByThread store slice + no-op action stubs</name>
  <read_first>
    - frontend/src/stores/streamsStore.ts (the modified file; tasksByThread interface :120 + default :254 + workflowLockByThread :135/258 + the no-op stub block :272-286 + the action interface decls :186-196)
    - frontend/src/types/index.ts (where TaskRunIndexItem / PendingAsk / WorkflowLock types live; add the Phase type here)
    - .planning/sketches/094-grounding/DATA-CONTRACT.md section 3(b) (the exact normalized Phase shape + status literals)
    - .planning/phases/094-workflow-legibility-mode-clarity/094-PATTERNS.md (the streamsStore analog section; new Map copy-then-mutate + ephemeral-empty-default + no-op-stub conventions)
  </read_first>
  <action>
    In `frontend/src/types/index.ts`, add the `Phase` type per DATA-CONTRACT section 3(b): `{ slug: string; phaseIndex: number; phaseType: string; status: "pending"|"running"|"done"|"failed"|"retrying"|"skipped"; attempt?: number; error?: string; subAgents: TaskRunIndexItem[]; pendingAsk: string | null }` (subAgents reuse the existing TaskRunIndexItem shape; pendingAsk is a tool_call_id POINTER into pendingAsksByThread, do NOT duplicate the ask).

    In `frontend/src/stores/streamsStore.ts`:
    - Add `phasesByThread: Map<string, Phase[]>` to the state interface BESIDE `workflowLockByThread` (interface ~:135).
    - Default it EMPTY beside `workflowLockByThread: new Map(...)` (~:258): `phasesByThread: new Map<string, Phase[]>(),`. Do NOT read from streamsCache/localStorage (ephemeral, reconciled on mount, mirror pendingAsksByThread/workspaceFilesByThread, NOT tasksByThread which persists).
    - Add the action interface declarations beside the *TasksForThread decls (~:186-196): `appendPhaseForThread(threadId: string, phase: Phase): void`, `setPhaseStatusForThread(threadId: string, slug: string, status: Phase["status"], patch?: Partial<Phase>): void`, `replacePhasesForThread(threadId: string, phases: Phase[]): void`.
    - Add the three as synchronous no-op `() => {}` stubs in the action stub block (~:272-286), NOT `notMounted` (panel SSE can fire BEFORE the provider's mount-time useEffect registers real bodies; Pitfall 5).
  </action>
  <verify>
    <automated>cd frontend && grep -c "phasesByThread" src/stores/streamsStore.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "phasesByThread" frontend/src/stores/streamsStore.ts returns >=3 (interface + default + at least one action site)
    - grep "appendPhaseForThread\|setPhaseStatusForThread\|replacePhasesForThread" frontend/src/stores/streamsStore.ts returns all three names
    - grep "export interface Phase\|export type Phase" frontend/src/types/index.ts matches
    - grep "new Map<string, Phase" frontend/src/stores/streamsStore.ts matches (empty default, no cache read)
    - cd frontend and npm run build exits 0
  </acceptance_criteria>
  <done>The Phase type exists; phasesByThread is declared + defaulted empty + has three no-op action stubs; build clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the additive SSE branches in api.ts + onPhase* callbacks (Deep byte-identical)</name>
  <read_first>
    - frontend/src/lib/api.ts (the modified file; StreamCallbacks interface :223 + onCapPaused shape :305-316; the dispatch switch :485-667 incl. the cap_paused branch and the cursor-advance block :676-679)
    - .planning/phases/094-workflow-legibility-mode-clarity/094-PATTERNS.md (the api.ts analog section; the exact new-branch code reading parsed.phase/parsed.phase_index/parsed.phase_type/parsed.attempt/parsed.error/parsed.reason; NO return)
    - frontend/src/test-fixtures/harness094.ts (Plan 01; the fixtures the test replays)
    - frontend/src/providers/__tests__/phaseHooks.test.tsx (Plan 01 scaffold; flip INV-1/INV-5 GREEN)
  </read_first>
  <behavior>
    - phaseHooks.test.tsx INV-1: replaying fxPhaseLlmAgent through the REAL subscribeToRun leaves bucketsBySurface reference-identical (expect(after).toBe(before)) AND populates phasesByThread.get(THREAD_A).
    - phaseHooks.test.tsx INV-5: a phase_started on THREAD_A does not mutate THREAD_B's phasesByThread.
    - A gate_failed{attempt:1} marks the phase retrying; a run_failed marks the active phase failed; a phase_completed marks it done.
  </behavior>
  <action>
    In `frontend/src/lib/api.ts`:
    - Append the new optional callbacks to the StreamCallbacks interface after onTaskDone (~:344), mirroring the onCapPaused shape: onPhaseStarted?(p: {phase:string;phaseIndex:number;phaseType:string}):void; onPhaseCompleted?(phase:string,phaseIndex:number):void; onPhaseTransition?(from:string,to:string,via:string):void; onGateFailed?(g:{phase:string;attempt:number;error:string}):void; onRunFailed?(reason?:string):void; onRunCompleted?(status?:string):void.
    - Add the dispatch branches as the LAST else-if branches of the switch, AFTER the cap_paused branch (~:666) and BEFORE the closing brace (:667). Read FLAT payload fields verbatim: parsed.phase, parsed.phase_index, parsed.phase_type, parsed.attempt, parsed.error, parsed.reason, parsed.from_phase, parsed.to_phase, parsed.via, parsed.status. Each branch carries NO return (so the cursor-advance block at 676-679 still fires, exactly like todo_updated/cap_paused). Mirror the PATTERNS analog excerpt: e.g. `else if (t === "phase_started" && callbacks.onPhaseStarted) callbacks.onPhaseStarted({ phase: parsed.phase as string, phaseIndex: parsed.phase_index as number, phaseType: parsed.phase_type as string })`; gate_failed reads {phase,attempt,error}; run_failed reads parsed.reason as string | undefined; phase_completed/phase_transition/run_completed same shape.

    DISCIPLINE (Pitfall 1, byte-identical Deep guard): do NOT touch any existing branch. The Deep branches delta(:485), reasoning_delta(:486), tool_preparing/start/args_progress/end(:490-505), sub_agent_start/done(:511-532), code_execution_*(:537-555), sources/citations/confidence(:566-575), todo_updated/workspace_file_*(:582-599), ask_user_prompt/response(:600-613), the terminal done/stream_end/error/timed_out/cancelled(:614-647), planning/iteration_start/fallback_model/cap_paused(:648-666) MUST be byte-identical pre/post. git diff shows ONLY additions.

    Then flip the phaseHooks.test.tsx INV-1 + INV-5 it.todo scaffolds to real tests (the slice + actions land in Task 3, but the api.ts branches + the store stubs from Task 1 make the wiring testable once Task 3 registers real bodies; if the action bodies are required for the assertion, this test goes green after Task 3 and the executor runs the suite at the end of Task 3).
  </action>
  <verify>
    <automated>cd frontend && grep -c "phase_started" src/lib/api.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "phase_started" frontend/src/lib/api.ts returns >=1 (additive branch present)
    - grep "onPhaseStarted\|onGateFailed\|onRunFailed\|onPhaseCompleted\|onPhaseTransition\|onRunCompleted" frontend/src/lib/api.ts returns all six callback names
    - git diff frontend/src/lib/api.ts shows ONLY added lines on the Deep branches (no Deep branch line deleted/modified; verify the dispatch lines for delta/sources/tool_end/ask_user_prompt/done are unchanged)
    - grep -c "return" within the new phase branches block is 0 (no return on any new branch)
    - cd frontend and npm run build exits 0
  </acceptance_criteria>
  <done>The six additive phase branches + their callbacks exist in api.ts reading FLAT fields with NO return; every Deep branch is byte-identical; build clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire the demux defaults + phase action bodies + usePhases selector (PANEL-09)</name>
  <read_first>
    - frontend/src/providers/StreamsProvider.tsx (the modified file; makeStreamCallbacks panel defaults :681-720 closing over threadId; tasksByThread action bodies :1806-1837 new Map copy-then-mutate; useTasks selector :2052; useThreadMessages chat selector :1974 = the PANEL-09 boundary that MUST stay untouched; useWorkflowLockForThread :2132)
    - .planning/phases/094-workflow-legibility-mode-clarity/094-PATTERNS.md (the StreamsProvider analog section; demux-default + action-body + selector excerpts)
    - frontend/src/providers/__tests__/phaseHooks.test.tsx (Plan 01 scaffold; the INV-1/INV-5 assertions to flip GREEN)
    - frontend/src/test-fixtures/harness094.ts (Plan 01; the fixtures replayed)
  </read_first>
  <behavior>
    - After replaying fxPhaseLlmAgent, phasesByThread.get(THREAD_A) has one phase with status transitions running -> done.
    - useThreadMessages reference is identical across the phase-event replay (INV-1).
    - A phase_started on THREAD_A leaves THREAD_B's phasesByThread untouched (INV-5).
  </behavior>
  <action>
    In `frontend/src/providers/StreamsProvider.tsx`:
    - Add the onPhase* defaults in the makeStreamCallbacks panel-defaults block (after onCapPaused, ~:714-720), each calling `useStreamsStore.getState().actions.<action>(threadId, ...)` and closing over the factory's threadId (the OWNING thread, Pitfall 6). Mirror the PATTERNS excerpt: onPhaseStarted appends a {slug,phaseIndex,phaseType,status:"running",subAgents:[],pendingAsk:null} phase; onPhaseCompleted -> setPhaseStatusForThread(threadId, phase, "done"); onGateFailed -> setPhaseStatusForThread(threadId, g.phase, g.attempt-based "retrying" with {attempt,error}); onRunFailed -> mark the latest running phase "failed" with {error: reason}; onPhaseTransition -> if via==="skip_to_phase" mark the from-phase "skipped"; onRunCompleted -> no-op on phase status (the done phase already flipped).
    - Register the real action bodies in the mount-time useEffect setState block beside the tasksByThread mutators (~:1806-1837), using the new Map(prev) copy-then-mutate immutable-replace shape keyed by slug: appendPhaseForThread (push if slug absent, else no-op); setPhaseStatusForThread (map over phases, patch the matching slug with {status, ...patch}); replacePhasesForThread (set the whole array, used by reconcile).
    - Add the `usePhases(threadId)` selector alphabetically among the panel hooks (near useTasks :2052), returning {data, isLoading, error, reconcile} via usePanelReconcile with fetcher getThreadWorkflow and replace=replacePhasesForThread; data selects `s.phasesByThread.get(threadId) ?? EMPTY_PHASES` (add an EMPTY_PHASES const).

    PANEL-09 ANTI-PATTERN GUARD: do NOT add any phasesByThread read to useThreadMessages (:1974) or any chat component. useThreadMessages stays byte-identical, reading bucketsBySurface only.

    Then flip the phaseHooks.test.tsx scaffolds GREEN: INV-1 (bucketsBySurface reference-identity across the replay) + INV-5 (per-thread isolation). Run the suite.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/providers/__tests__/phaseHooks.test.tsx --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run src/providers/__tests__/phaseHooks.test.tsx exits 0 (INV-1 reference-identity GREEN + INV-5 isolation GREEN)
    - grep "usePhases" frontend/src/providers/StreamsProvider.tsx matches (selector exported)
    - grep "appendPhaseForThread\|setPhaseStatusForThread\|replacePhasesForThread" frontend/src/providers/StreamsProvider.tsx returns all three action bodies
    - git diff frontend/src/providers/StreamsProvider.tsx shows the useThreadMessages selector (~:1974) UNCHANGED (no phasesByThread read added to it)
    - grep "fetcher: getThreadWorkflow\|getThreadWorkflow" within the usePhases selector matches (reconcile floor)
    - cd frontend and npm run build exits 0
  </acceptance_criteria>
  <done>The onPhase* demux defaults (threadId-keyed) + the three action bodies + the usePhases selector exist; phaseHooks.test.tsx is GREEN (INV-1 + INV-5); useThreadMessages is untouched; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE producer stream to client | run:{run_id} events parsed by subscribeToRun; agent-influenced strings (phase slug, error, reason) cross here |
| cross-thread state | phasesByThread keyed by owning thread id; a background run must not mutate the viewed thread |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-094-02-01 | Tampering | additive api.ts branches | mitigate | Branches are additive-only with NO return; Deep dispatch byte-identical (git-diff acceptance proves no Deep branch changed). A malformed/hostile event field is read as a typed cast and only flows to panel state, never eval'd. |
| T-094-02-02 | Information disclosure / cross-thread bleed | phasesByThread keying | mitigate | Every write keys by the factory's closed-over OWNING threadId (Pitfall 6); INV-5 test proves THREAD_B is untouched by THREAD_A events. No global flag. |
| T-094-02-03 | Elevation of privilege | StreamCallbacks dispatch | accept | The new callbacks only mutate client-side panel state; no auth/access-control decision; the run stream is already owner-scoped server-side (subscribeToRun targets the user's run). |
| T-094-02-04 | Denial of service | chat re-render flood | mitigate | PANEL-09: chat selector reads bucketsBySurface only; a phase-event storm cannot re-render the message list (INV-1 reference-identity proves it). |
</threat_model>

<verification>
- `npx vitest run src/providers/__tests__/phaseHooks.test.tsx` GREEN (INV-1 + INV-5)
- git diff confirms Deep branches in api.ts unchanged + useThreadMessages unchanged
- `cd frontend && npm run test && npm run build` clean (no regression to the existing panel suite)
- grep proves all six phase callbacks + the three actions + usePhases exist
</verification>

<success_criteria>
- Six additive phase SSE branches in api.ts, Deep byte-identical, no return
- phasesByThread panel-only slice + three actions + usePhases selector
- phaseHooks.test.tsx GREEN proving PANEL-09 zero-chat-re-render + per-thread isolation
</success_criteria>

<output>
After completion, create `.planning/phases/094-workflow-legibility-mode-clarity/094-02-SUMMARY.md`
</output>
