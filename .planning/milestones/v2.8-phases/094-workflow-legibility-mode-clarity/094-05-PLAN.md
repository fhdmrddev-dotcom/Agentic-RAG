---
phase: 094-workflow-legibility-mode-clarity
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - frontend/src/components/chat/ChatArea.tsx
  - frontend/src/components/chat/MessageInput.tsx
  - frontend/src/components/panel/PendingAskCard.tsx
  - frontend/src/components/panel/BatchResultList.tsx
  - frontend/src/components/panel/__tests__/PendingAskCard.test.tsx
  - frontend/src/components/chat/__tests__/ChatAreaMode.test.tsx
autonomous: true
requirements: [PANEL-09]
must_haves:
  truths:
    - "The displayed mode label derives from server truth (the workflow lock from active_workflow_run_id), not the stale ChatArea launch-toggle useState"
    - "The draft before ask_user renders ABOVE the question, labelled DRAFT not-yet-saved, with a preview + open-wide overlay for long drafts"
    - "Batch sub-agent summaries render per-subtopic from the panel-only task store, with no chat re-render"
  artifacts:
    - path: "frontend/src/components/panel/PendingAskCard.tsx"
      provides: "DraftBlock rendering ask.draft above the prompt"
      contains: "draft"
    - path: "frontend/src/components/panel/BatchResultList.tsx"
      provides: "per-subtopic summary rows from useTasks"
      min_lines: 30
  key_links:
    - from: "frontend/src/components/chat/MessageInput.tsx"
      to: "frontend/src/components/chat/ChatArea.tsx"
      via: "displayed mode label reads workflowLocked (server truth), launch toggle stays workflowMode"
      pattern: "workflowLocked"
    - from: "frontend/src/components/panel/BatchResultList.tsx"
      to: "frontend/src/providers/StreamsProvider.tsx"
      via: "useTasks(threadId) panel-only read"
      pattern: "useTasks"
---

<objective>
Three pure-frontend renders over data already on the wire: (D-02) derive the DISPLAYED mode label from server truth (`workflowLocked`, reconciled from `active_workflow_run_id`) instead of the stale `ChatArea` launch-toggle `useState`; (D-06) render the `ask.draft` ABOVE the question in `PendingAskCard` (preview + open-wide overlay), and a `BatchResultList` of per-subtopic `sub_agent_done.summary` rows. Zero backend changes.

Purpose: D-02 kills finding #5 (mode reads "Deep" during a running Harness run). D-06 closes finding #2 (you review a draft you can't see) and surfaces batch sub-results (progressive disclosure). All data is already on the wire post-093-05; this is read-derivation + render only.

Sequencing: this is Wave 4 (after Plan 03) because Plan 03 also edits `ChatArea.tsx` (the `requestOpenPanel()` auto-open call) — serializing the two ChatArea edits avoids a within-wave file conflict. Plan 05's ChatArea edit is the additive `displayedMode` prop derivation, orthogonal to Plan 03's kickoff auto-open line.
Output: ChatArea/MessageInput mode-label fix + PendingAskCard draft + new BatchResultList + 2 tests GREEN.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/094-workflow-legibility-mode-clarity/094-CONTEXT.md
@.planning/phases/094-workflow-legibility-mode-clarity/094-RESEARCH.md
@.planning/phases/094-workflow-legibility-mode-clarity/094-PATTERNS.md
@.planning/phases/094-workflow-legibility-mode-clarity/094-UI-SPEC.md
@.planning/sketches/094-grounding/DATA-CONTRACT.md

<interfaces>
From frontend/src/components/chat/ChatArea.tsx (verified):
- workflowMode useState at :68 (the LAUNCH toggle, which workflow to kick off; KEEP for kickoff at :307).
- workflowLock = useWorkflowLockForThread(thread?.id ?? null) at :83; workflowLocked = workflowLock !== null at :84 (the SERVER-TRUTH lock, reconciled from getThreadWorkflow at :153-179).
- Passed to MessageInput: workflowMode={workflowMode} at :363 AND workflowLocked={workflowLocked} at :368.
- NOTE: Plan 03 added a requestOpenPanel() call at the harness kickoff path (~:303-307); leave it intact when you add displayedMode here.

From frontend/src/components/chat/MessageInput.tsx (verified):
- The Deep/Harness toggle pill renders the displayed label at :348: workflowMode === "harness" ? "Harness" : "Deep". This is the badge that goes stale (D-02 bug). The dropdown items + the launch-staging stay AS-IS.

From frontend/src/components/panel/PendingAskCard.tsx (verified):
- Destructure at :53 OMITS draft: const { tool_call_id, prompt, timeout_seconds, run_id } = ask. ask.draft EXISTS on the type (types/index.ts:311 draft?: string), populated at api.ts:610, but NOT read here.
- The prompt render at :179-181: a p element with id labelId containing {prompt}. The DraftBlock inserts ABOVE this.

From frontend/src/providers/StreamsProvider.tsx:
- useTasks(threadId): { data: TaskRunIndexItem[]; ... }. TaskRunIndexItem.summary (types/index.ts:319) is the per-subtopic sub_agent_done.summary; already panel-only.

shadcn Dialog (already installed) for the wide-draft overlay (min(760px,88%)); no registry add.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive the displayed mode label from server truth (D-02)</name>
  <read_first>
    - frontend/src/components/chat/ChatArea.tsx (the modified file; workflowMode :68 launch toggle, workflowLock/workflowLocked :83-84 server truth, the props passed to MessageInput :363/368, the kickoff path :303-307 incl. Plan 03's requestOpenPanel call)
    - frontend/src/components/chat/MessageInput.tsx (the modified file; the displayed label at :348 workflowMode === "harness" ? "Harness" : "Deep"; the dropdown items + launch staging that stay AS-IS)
    - .planning/phases/094-workflow-legibility-mode-clarity/094-PATTERNS.md (the ChatArea analog section; the read-derivation change, launch toggle stays)
    - .planning/sketches/094-grounding/DATA-CONTRACT.md section 4.1 "Mode badge" + 4.5 "Panel mode tag" (server-truth mode = active_workflow_run_id presence; never a client workflowMode useState)
    - frontend/src/components/chat/__tests__/ChatAreaMode.test.tsx (this plan's scaffold; assert the displayed label follows the lock, not the local toggle)
  </read_first>
  <behavior>
    - When workflowLocked is true (a workflow run is active per server truth), the displayed mode badge reads "Harness" REGARDLESS of the local workflowMode toggle.
    - When workflowLocked is false, the displayed badge reads "Deep" (no active run).
    - The launch toggle (workflowMode/setWorkflowMode) still controls which workflow a kickoff send stages (:307); unchanged.
  </behavior>
  <action>
    Make the DISPLAYED mode label derive from server truth without changing the launch-toggle state shape:
    - In frontend/src/components/chat/ChatArea.tsx: pass a NEW derived prop to MessageInput for the displayed label, e.g. displayedMode={workflowLocked ? "harness" : "deep"} (server truth from the already-present workflowLocked :84). Keep workflowMode={workflowMode} for the launch toggle dropdown selection state and keep the :307 kickoff staging on workflowMode AS-IS. Leave Plan 03's requestOpenPanel() call intact.
    - In frontend/src/components/chat/MessageInput.tsx: change ONLY the displayed pill label at :348 to read the new displayedMode prop (displayedMode === "harness" ? "Harness" : "Deep") instead of workflowMode. The dropdown menu items (Deep/Harness selection) + the "active" markers + the launch-staging continue to read workflowMode (the user's toggle intent for the NEXT kickoff). Add the displayedMode?: "deep" | "harness" prop to the MessageInput props (default to workflowMode for back-compat if absent).

    This is a read-derivation change (the signal workflowLocked already exists at ChatArea :84), not a state-shape change. Do NOT remove workflowMode/setWorkflowMode (the kickoff at :307 needs it). Do NOT touch the composer 2-pill redesign / workflow picker (deferred to v2.9 per D-02).

    Create frontend/src/components/chat/__tests__/ChatAreaMode.test.tsx (or extend if present): render the MessageInput pill with workflowLocked=true + displayedMode derived true + workflowMode="deep" and assert the displayed badge reads "Harness" (server truth wins); render with workflowLocked=false and assert "Deep".
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/chat/__tests__/ChatAreaMode.test.tsx --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run src/components/chat/__tests__/ChatAreaMode.test.tsx exits 0 (locked -> "Harness" regardless of local toggle; unlocked -> "Deep")
    - grep "displayedMode" frontend/src/components/chat/ChatArea.tsx matches (server-truth prop derived from workflowLocked)
    - grep "displayedMode" frontend/src/components/chat/MessageInput.tsx matches (displayed label reads it)
    - grep "setWorkflowMode" frontend/src/components/chat/ChatArea.tsx still matches (the launch toggle + kickoff staging unchanged)
    - grep "requestOpenPanel" frontend/src/components/chat/ChatArea.tsx still matches (Plan 03's auto-open preserved)
    - cd frontend and npm run build exits 0
  </acceptance_criteria>
  <done>The displayed mode badge reads server truth (workflowLocked), the launch toggle + kickoff staging stay on workflowMode AS-IS, Plan 03's requestOpenPanel preserved; ChatAreaMode.test.tsx GREEN; build clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render ask.draft in PendingAskCard (D-06 draft preview + open-wide)</name>
  <read_first>
    - frontend/src/components/panel/PendingAskCard.tsx (the modified file; the destructure :53 that omits draft; the prompt render :179-181 to insert the DraftBlock ABOVE; the amber-chrome block :162-181)
    - frontend/src/types/index.ts (PendingAsk.draft?: string at :311)
    - .planning/phases/094-workflow-legibility-mode-clarity/094-UI-SPEC.md (the "Not yet saved" draft label copy :174 = DRAFT · awaiting your review — not yet saved; the Long content = preview + open-wide interaction contract)
    - .planning/sketches/094-grounding/DATA-CONTRACT.md section 4.4 DRAFT block + section 6 DRAFT-MISSING (ask.draft undefined -> hide block, show question only, never an empty DRAFT box)
    - frontend/src/components/panel/__tests__/PendingAskCard.test.tsx (the existing test; add the draft cases)
  </read_first>
  <behavior>
    - When ask.draft is set, a DRAFT block renders ABOVE the question with the amber mono tag "DRAFT · awaiting your review — not yet saved" and the draft body.
    - A long draft shows a faded-mask preview + a "Review & edit full draft" control opening a wide overlay (min(760px,88%)); the word/char count is computed from ask.draft.length at render (never a fixture number).
    - When ask.draft is undefined (older streams), NO draft block renders, only the question + chips (DRAFT-MISSING guard).
  </behavior>
  <action>
    In frontend/src/components/panel/PendingAskCard.tsx:
    - Add draft to the destructure: const { tool_call_id, prompt, timeout_seconds, run_id, draft } = ask (:53).
    - Insert a DraftBlock IMMEDIATELY ABOVE the prompt p element at :179-181: an amber mono tag with the VERBATIM copy DRAFT · awaiting your review — not yet saved (the "not yet saved" half is non-negotiable, prevents the draft being read as the final answer) + the draft body. When the draft is long, render a faded-mask preview + a Review & edit full draft control opening a shadcn Dialog wide overlay (min(760px,88%)). Compute the word/char count from draft.length at render.
    - GUARD: when draft === undefined, hide the DraftBlock entirely and show the question + chips only (DATA-CONTRACT section 6 DRAFT-MISSING), never an empty DRAFT box.
    - The draft body renders as plain React text children, NEVER dangerouslySetInnerHTML (T-087-11 XSS guard).
    Zero backend changes (draft is already on the wire + the type).

    Add the draft cases to PendingAskCard.test.tsx: ask.draft set -> the DRAFT tag + body render above the prompt; ask.draft undefined -> no DRAFT block; the existing vitest-axe assertion stays GREEN. Run the suite.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/panel/__tests__/PendingAskCard.test.tsx --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run src/components/panel/__tests__/PendingAskCard.test.tsx exits 0 (draft renders above prompt; undefined-draft hides the block; axe stays clean)
    - grep "awaiting your review" frontend/src/components/panel/PendingAskCard.tsx matches (verbatim DRAFT copy)
    - grep "draft" frontend/src/components/panel/PendingAskCard.tsx shows the destructure includes draft AND the DRAFT-MISSING guard (draft === undefined hide)
    - grep "dangerouslySetInnerHTML" frontend/src/components/panel/PendingAskCard.tsx returns 0
    - grep "Dialog\|Review & edit full draft" frontend/src/components/panel/PendingAskCard.tsx matches (open-wide overlay)
    - cd frontend and npm run build exits 0
  </acceptance_criteria>
  <done>PendingAskCard renders the ask.draft above the question with the verbatim DRAFT label + preview + open-wide overlay, hides cleanly when draft is undefined, and renders as plain text; the test (incl. axe) is GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Build BatchResultList (per-subtopic summaries, panel-only)</name>
  <read_first>
    - frontend/src/components/panel/PendingAskCard.tsx (the card chrome + plain-text-children precedent to mirror)
    - frontend/src/types/index.ts (TaskRunIndexItem.summary at :319, the per-subtopic sub_agent_done.summary)
    - .planning/sketches/094-grounding/DATA-CONTRACT.md section 4.4 "Batch sub-results list" (summary for each child, already panel-only via tasksByThread; per-item source counts INVENTED -> omit)
    - .planning/phases/094-workflow-legibility-mode-clarity/094-PATTERNS.md (the PendingAskCard analog; the useTasks panel-only read; PANEL-09 reads tasksByThread, not chat)
  </read_first>
  <action>
    Create frontend/src/components/panel/BatchResultList.tsx that reads const { data: tasks } = useTasks(threadId) (panel-only, already isolated) and renders per-subtopic rows: each child = its description (the sub-question, trimmed of the "Overall topic… / Sub-question…" prefix presentationally) -> its summary (on done). Progressive disclosure: collapsed by default, expand to read the summary. SUPPRESS per-item source/tool counts (INVENTED, DATA-CONTRACT section 8). Render summary/description as plain React text children, NEVER dangerouslySetInnerHTML. Empty state: no children -> render nothing (the consuming section omits it). Export it for reuse: PhaseCard's batch phases (Plan 03) consume it as the shared summary-row component, OR it mounts as a sibling panel block where a batch phase has children.

    PANEL-09: reads useTasks (tasksByThread, already panel-only), NEVER a chat selector. No chat re-render.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/src/components/panel/BatchResultList.tsx exists and is >=30 lines
    - grep "useTasks" frontend/src/components/panel/BatchResultList.tsx matches (panel-only read, not a chat selector)
    - grep "summary" frontend/src/components/panel/BatchResultList.tsx matches (renders TaskRunIndexItem.summary)
    - grep "dangerouslySetInnerHTML" frontend/src/components/panel/BatchResultList.tsx returns 0 (XSS guard)
    - No per-item source/tool count chip rendered (grep shows no hardcoded "searches" / "tool calls" count)
    - cd frontend and npm run build exits 0
  </acceptance_criteria>
  <done>BatchResultList renders per-subtopic description -> summary rows from the panel-only useTasks store with progressive disclosure, suppressed counts, and plain-text children; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-truth mode vs client toggle | the displayed mode must reflect active_workflow_run_id, not a stale local useState |
| agent-rendered draft/summary text | ask.draft + sub_agent_done.summary are agent-influenced strings rendered in the panel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-094-05-01 | Spoofing / trust | mode label | mitigate | The displayed mode derives from server truth (workflowLocked, reconciled from active_workflow_run_id); a stale client toggle can no longer mislabel a running Harness run as Deep (finding #5). |
| T-094-05-02 | Tampering / XSS | draft + summary render | mitigate | ask.draft and sub_agent_done.summary render as plain React text children; NEVER dangerouslySetInnerHTML (grep acceptance proves 0); mirrors the PendingAskCard T-087-11 guard. |
| T-094-05-03 | Repudiation / trust | draft label | mitigate | The draft is labelled "DRAFT · awaiting your review — not yet saved" so it is never read as the final saved answer; the "not yet saved" half is non-negotiable. |
| T-094-05-04 | Denial of service | chat re-render via batch list | accept | BatchResultList reads useTasks (tasksByThread, already panel-only); it never reads bucketsBySurface; PANEL-09 isolation preserved. |
</threat_model>

<verification>
- `npx vitest run src/components/chat/__tests__/ChatAreaMode.test.tsx src/components/panel/__tests__/PendingAskCard.test.tsx` GREEN
- `cd frontend && npm run test && npm run build` clean
- grep confirms displayedMode (server truth), verbatim DRAFT copy, DRAFT-MISSING guard, no dangerouslySetInnerHTML, useTasks panel-only read
</verification>

<success_criteria>
- Displayed mode label reads server truth (workflowLocked); launch toggle + kickoff staging unchanged
- ask.draft renders above the question (labelled, preview + open-wide), hides when undefined
- BatchResultList renders per-subtopic summaries from the panel-only task store; no chat re-render
</success_criteria>

<output>
After completion, create `.planning/phases/094-workflow-legibility-mode-clarity/094-05-SUMMARY.md`
</output>
