---
phase: 094-workflow-legibility-mode-clarity
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - backend/app/services/harness_engine.py
  - backend/tests/test_094_rc4_failure.py
autonomous: true
requirements: [PANEL-08]
must_haves:
  truths:
    - "A failed harness run persists a real assistant failure message before returning, at BOTH failure-return sites, so a later reconcile renders failed-with-reason instead of a silent empty done"
    - "The empty-reason case persists the reason_unknown sentinel, never empty content"
    - "Deep stays byte-identical: the failure persist lives inside run_workflow, never in the shared _shielded_finalize"
  artifacts:
    - path: "backend/app/services/harness_engine.py"
      provides: "_surface_failure_message helper called at both failure-return sites"
      contains: "_surface_failure_message"
  key_links:
    - from: "backend/app/services/harness_engine.py"
      to: "backend/app/db/runs.py"
      via: "insert_assistant_message on the failure path"
      pattern: "insert_assistant_message"
---

<objective>
Land the one backend touch (D-04 / RC-4): persist a real assistant failure message before returning at BOTH `run_workflow` failure-return sites (`fail_run` at ~699-709 AND the `skip_to_phase` runtime-guard at ~729-735), via a new `_surface_failure_message` helper that mirrors the persist block of `_surface_final_answer`. This makes a failed run renderable as failed-with-reason on reconcile instead of a silent empty `done` — UI-only is insufficient because the reconcile needs a durable messages row to key off.

Purpose: the failure path emits run_failed and returns WITHOUT persisting any message; the success path persists via _surface_final_answer. A later reconcile reads a terminal run with empty content -> renders as a silent done (the RC-4 trust bug, finding #3). This plan is the ONLY backend code 094 writes. It is the BACKEND track of Wave 2 and runs in PARALLEL with Plan 02 (the frontend SSE substrate) — zero file overlap (harness_engine.py + its test vs api.ts/store/provider).
Output: 1 backend file modified + the RC-4 test flipped GREEN.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/094-workflow-legibility-mode-clarity/094-CONTEXT.md
@.planning/phases/094-workflow-legibility-mode-clarity/094-RESEARCH.md
@.planning/phases/094-workflow-legibility-mode-clarity/094-PATTERNS.md

<interfaces>
From backend/app/services/harness_engine.py (verified live anchors):
- _surface_final_answer at :235 (the success template); its persist block at :332-347 lazy-imports insert_assistant_message from app.db.runs and _strip_nul from app.services.agent_loop, reads ctx.thread_id + ctx.current_user["id"], calls insert_assistant_message(pool, thread_id, user_id, content, source_refs, confidence_*). Called on success at :822.
- Failure site #1, fail_run (verified :698-709): fail_phase -> finish_run(pool, run_id, "failed") -> write_audit(event_type="run_failed", metadata={"reason": outcome.reason}) -> _emit(redis, stream_run_id, "run_failed", reason=outcome.reason) -> return at :709 (NO persist — the RC-4 bug).
- Failure site #2, skip_to_phase runtime-guard (verified :722-735): when index_by_slug.get(target) is None -> reason string built -> finish_run("failed") -> write_audit("run_failed", {reason}) -> _emit("run_failed", reason=reason) -> return at :735 (NO persist — the SECOND site the CONTEXT anchor misses, Pitfall 7).
- _shielded_finalize: the SHARED Deep+harness terminal path (referenced :246/808/818). The fix MUST NOT touch it (Deep byte-identical guard, Pitfall 2).
- insert_assistant_message accepts source_refs=None + confidence_*=None cleanly (the success path proves the None-shape; a failure message is a strict subset).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _surface_failure_message + call it at both failure-return sites (RC-4)</name>
  <read_first>
    - backend/app/services/harness_engine.py (the modified file; _surface_final_answer :235-350 incl. the persist block :332-347; the fail_run branch :698-709; the skip_to_phase runtime-guard :711-737)
    - .planning/phases/094-workflow-legibility-mode-clarity/094-PATTERNS.md (the harness_engine analog section; the persist-block template + BOTH failure-site insertion points + the reason_unknown sentinel)
    - backend/app/db/runs.py (insert_assistant_message signature + the source_refs/confidence_* optionality)
    - backend/tests/test_094_rc4_failure.py (Plan 01 scaffold; flip INV-3a GREEN)
    - backend/tests/test_harness_engine.py + test_harness_gates.py (how a test drives run_workflow to fail_run / a missing-skip-target outcome)
  </read_first>
  <behavior>
    - Driving run_workflow to a fail_run outcome persists exactly one assistant messages row whose content is the failure reason.
    - Driving run_workflow to a missing-skip-target runtime guard ALSO persists an assistant messages row with the guard reason.
    - A fail_run with an empty reason persists the "Failure reason not captured by the backend." sentinel, never empty content.
    - _shielded_finalize is NOT in the call path of the persist (Deep byte-identical).
  </behavior>
  <action>
    In `backend/app/services/harness_engine.py`, add a new helper `_surface_failure_message(ctx, run_id, reason, pool)` INSIDE the module (near _surface_final_answer), mirroring ONLY the persist block of _surface_final_answer as a strict subset:
    - Lazy-import `from app.db.runs import insert_assistant_message` and `from app.services.agent_loop import _strip_nul` (keeps the harness import cycle broken).
    - Read `_thread_id = getattr(ctx, "thread_id", None)` and `_user_id = (getattr(ctx, "current_user", None) or {}).get("id")`; guard both present (if either missing, skip the persist gracefully — never crash a failing run).
    - `content = _strip_nul(reason or "Failure reason not captured by the backend.")` — the reason_unknown sentinel; NEVER persist empty content.
    - `await insert_assistant_message(pool, thread_id=UUID(_thread_id) if isinstance(_thread_id, str) else _thread_id, user_id=UUID(_user_id) if isinstance(_user_id, str) else _user_id, content=content)` — grounding params OMITTED (None), a strict subset of the success persist. Owner-scoped (ctx.current_user["id"] + ctx.thread_id), identical to the success path -> no IDOR.

    Call it at BOTH failure-return sites, AFTER the existing `_emit(... "run_failed" ...)` and BEFORE the `return`:
    - Site #1 (fail_run, :708-709): insert `await _surface_failure_message(ctx, run_id, outcome.reason, pool)` after the _emit at :708, before `return` at :709.
    - Site #2 (skip_to_phase runtime-guard, :734-735): insert `await _surface_failure_message(ctx, run_id, reason, pool)` after the _emit at :734, before `return` at :735.

    DO NOT touch _shielded_finalize, _surface_final_answer (the success path), the Deep `else` branch, or run_agent_loop (Pitfall 2 — Deep byte-identical). The helper is called ONLY from run_workflow's two harness-only failure branches.

    Flip backend/tests/test_094_rc4_failure.py INV-3a GREEN (both sites persist + reason_unknown sentinel + _shielded_finalize not in path). Run the test.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_094_rc4_failure.py -x</automated>
  </verify>
  <acceptance_criteria>
    - cd backend and pytest tests/test_094_rc4_failure.py -x exits 0 (both failure sites persist + reason_unknown sentinel + _shielded_finalize not in path GREEN)
    - grep -c "_surface_failure_message" backend/app/services/harness_engine.py returns >=3 (definition + 2 call sites)
    - grep -c "insert_assistant_message" backend/app/services/harness_engine.py increased by exactly the helper's one usage vs baseline (the success path's usage stays; the helper adds one)
    - grep "Failure reason not captured by the backend." backend/app/services/harness_engine.py matches (the reason_unknown sentinel)
    - git diff shows NO change to _shielded_finalize, _surface_final_answer body, or any Deep path; grep -c "_shielded_finalize" backend/app/services/harness_engine.py unchanged from baseline
    - cd backend and pytest tests/test_harness_engine.py tests/test_harness_gates.py -q passes (no harness regression)
  </acceptance_criteria>
  <done>_surface_failure_message persists an owner-scoped assistant failure row at BOTH failure-return sites with the reason_unknown sentinel for empty reasons; _shielded_finalize + the Deep path are untouched; test_094_rc4_failure.py GREEN and the harness suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness failure path to DB | a failure reason string (engine-derived) is persisted as an assistant messages row |
| owner scope | the persist must be scoped to the run owner, identical to the success path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-094-04-01 | Repudiation / trust | RC-4 silent empty done | mitigate | The failure path now persists a real messages row before returning, at BOTH sites; a reconcile renders failed-with-reason, never a silent success. This IS the fix. |
| T-094-04-02 | Elevation of privilege / IDOR | failure message persist | mitigate | The persist is owner-scoped via ctx.current_user["id"] + ctx.thread_id, identical to the proven _surface_final_answer success path; no new auth surface, no cross-user write. |
| T-094-04-03 | Tampering | Deep byte-identical regression | mitigate | The helper lives in harness_engine.py, called ONLY from run_workflow's two harness-only failure branches; _shielded_finalize (the shared Deep+harness terminal path) is untouched (git-diff + grep-count acceptance prove it). No double-persist for Deep (the 075.x defect class). |
| T-094-04-04 | Information disclosure | _strip_nul + sentinel | mitigate | content is _strip_nul'd; an empty/missing reason becomes the explicit sentinel, never empty or raw NUL-bearing content; no secrets/PII in a failure reason (engine-derived taxonomy strings). |
</threat_model>

<verification>
- `cd backend && pytest tests/test_094_rc4_failure.py -x` GREEN (both sites + sentinel + _shielded_finalize not in path)
- `cd backend && pytest tests/test_harness_engine.py tests/test_harness_gates.py tests/test_harness_resume.py -q` passes (no harness regression)
- git diff confirms _shielded_finalize + _surface_final_answer body + Deep path unchanged
- grep -c "_surface_failure_message" >=3 (def + 2 sites)
</verification>

<success_criteria>
- _surface_failure_message persists an owner-scoped failure row at BOTH failure-return sites; empty reason -> reason_unknown sentinel
- Deep byte-identical (_shielded_finalize untouched, no double-persist)
- test_094_rc4_failure.py GREEN + harness suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/094-workflow-legibility-mode-clarity/094-04-SUMMARY.md`
</output>
