---
phase: 094-workflow-legibility-mode-clarity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/index.css
  - frontend/tailwind.config.js
  - frontend/src/test-fixtures/harness094.ts
  - frontend/src/providers/__tests__/phaseHooks.test.tsx
  - frontend/src/components/panel/__tests__/PhaseTimeline.test.tsx
  - frontend/src/components/panel/__tests__/FailReason.test.tsx
  - frontend/src/components/panel/__tests__/PhaseReconcile.test.tsx
  - backend/tests/test_094_rc4_failure.py
autonomous: true
requirements: [A11Y-03]
must_haves:
  truths:
    - "The --accent-violet token resolves to a real color in BOTH light and dark themes before any purple surface renders"
    - "Wave 0 test files exist as RED scaffolds so every downstream implementation task has an automated verify or a Wave 0 dependency"
    - "The DATA-CONTRACT section 7 fixtures are a shared replayable module matching the exact producer wire shape"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "--accent-violet in :root (light) and .dark blocks, HSL channels"
      contains: "--accent-violet"
    - path: "frontend/tailwind.config.js"
      provides: "accent-violet utility registration"
      contains: "accent-violet"
    - path: "frontend/src/test-fixtures/harness094.ts"
      provides: "fx-phase-* and fx-run-* raw-SSE wire fixtures"
      min_lines: 60
    - path: "frontend/src/providers/__tests__/phaseHooks.test.tsx"
      provides: "INV-1 PANEL-09 reference-identity scaffold + INV-5 isolation"
    - path: "backend/tests/test_094_rc4_failure.py"
      provides: "INV-3a both-failure-site persist scaffold"
  key_links:
    - from: "frontend/tailwind.config.js"
      to: "frontend/src/index.css"
      via: "hsl(var(--accent-violet)) mapping"
      pattern: "accent-violet.*hsl"
---

<objective>
Land the `--accent-violet` design token FIRST (UI-SPEC Build-Prerequisite A / D-05) in both theme blocks and the Tailwind theme, and create all Wave 0 test scaffolds + the shared DATA-CONTRACT section 7 fixtures module. This unblocks every later purple surface (the `retrying` row glyph, the `llm_batch_agents` left-border) and gives every downstream task an automated verify or a Wave 0 dependency (Nyquist Dimension 8).

Purpose: `var(--accent-violet)` resolves to nothing before the token migration, producing transparent/broken CSS (Pitfall 3). The token MUST land before any component uses `text-accent-violet`/`border-accent-violet`. The Wave 0 scaffolds + fixtures gate the implementation plans (02/03/04/05).
Output: 2 config files modified (token + Tailwind), 1 shared fixtures module, 5 RED test scaffolds (4 frontend + 1 backend).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/094-workflow-legibility-mode-clarity/094-CONTEXT.md
@.planning/phases/094-workflow-legibility-mode-clarity/094-PATTERNS.md
@.planning/phases/094-workflow-legibility-mode-clarity/094-VALIDATION.md
@.planning/sketches/094-grounding/DATA-CONTRACT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the --accent-violet token to both theme blocks + Tailwind (D-05, FIRST)</name>
  <read_first>
    - frontend/src/index.css (the modified file; the --panel-status-done/-active token convention at :58-61 :root and :122-125 .dark)
    - frontend/tailwind.config.js (the modified file; the panel-status-* registration at :52-54)
    - .planning/phases/094-workflow-legibility-mode-clarity/094-PATTERNS.md (the index.css/tailwind.config.js analog section; exact HSL channels + verification comment)
  </read_first>
  <action>
    In `frontend/src/index.css`, add `--accent-violet` to the `:root` (light) block IMMEDIATELY AFTER line 61 (`--panel-status-active: 32 90% 30%;`), using HSL channels with NO hsl() wrapper, with a verification comment matching the existing token style. Light declaration: `--accent-violet: 258 80% 40%;` (comment it: 258 80% 40% = #4514b8 -> 8.52:1 on light --panel-surface, text+graphic).

    Then add `--accent-violet` to the `.dark` block IMMEDIATELY AFTER line 125 (`--panel-status-active: 38 92% 62%;`). Dark declaration: `--accent-violet: 258 90% 66%;` (comment it: 258 90% 66% = #895af6 -> 4.35:1 graphic on dark --panel-surface, meets >=3:1; pill LABEL text uses lightened 258 95% 84% -> 9.83:1).

    In `frontend/tailwind.config.js`, register the utility IMMEDIATELY AFTER line 54 (`"panel-status-active": "hsl(var(--panel-status-active))",`), same mapping shape: `"accent-violet": "hsl(var(--accent-violet))",`.

    Touch NOTHING else in either file. Do NOT render any text-accent-violet/border-accent-violet here; that arrives in Plan 03 (the retrying/batch surfaces). This task only establishes the token so the variable resolves.
  </action>
  <verify>
    <automated>cd frontend && grep -c "accent-violet" src/index.css</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "accent-violet" frontend/src/index.css returns >=2 (both :root and .dark declarations)
    - grep -c "accent-violet" frontend/tailwind.config.js returns >=1
    - grep "258 80% 40%" frontend/src/index.css matches the light --accent-violet value (no hsl() wrapper)
    - grep "258 90% 66%" frontend/src/index.css matches the dark --accent-violet value
    - cd frontend and npm run build exits 0 (Tailwind rebuilds the utility, CSS compiles)
  </acceptance_criteria>
  <done>Both theme blocks carry --accent-violet (light 258 80% 40%, dark 258 90% 66%) as raw HSL channels; the Tailwind accent-violet utility is registered; npm run build is clean. No component renders the token yet.</done>
</task>

<task type="auto">
  <name>Task 2: Create the shared DATA-CONTRACT section 7 fixtures module</name>
  <read_first>
    - .planning/sketches/094-grounding/DATA-CONTRACT.md section 7 (TEST FIXTURES; the exact per-phase-type and per-run-state wire sequences) AND section 2 (the 5 phase_type literals) AND section 3(b) (the normalized Phase shape)
    - frontend/src/providers/__tests__/panelHooks.test.tsx (the raw-SSE-string-array shape replayed through subscribeToRun; a fixture is a flat data line string array)
    - frontend/src/components/panel/__tests__/fixtures.ts (the existing panel-test fixtures module convention)
  </read_first>
  <action>
    Create `frontend/src/test-fixtures/harness094.ts` exporting the DATA-CONTRACT section 7 fixtures as a shared module so Plans 02/03/05 tests import them (single source of truth, never re-hand-roll a wire payload). Each fixture is a flat string[] of `data: <JSON>` blank-line-terminated lines matching the EXACT _emit producer wire shape. Backend emits FLAT fields: phase, phase_index, phase_type, attempt, error, reason, from_phase, to_phase, via, status.

    Per-phase-type fixtures (verbatim from section 7):
    - fxPhaseProgrammatic: phase_started{phase:"split",phase_index:0,phase_type:"programmatic"} then phase_completed{phase:"split",phase_index:0} then phase_transition{from_phase:"split",to_phase:"review",via:"normal"}
    - fxPhaseLlmSingle: phase_started{phase:"draft",phase_index:1,phase_type:"llm_single"} then phase_completed{phase:"draft",phase_index:1}
    - fxPhaseLlmAgent: phase_started{...phase_type:"llm_agent"} then sub_agent_start{sub_run_id:"sa-1",description,tools:["search_documents"],max_steps:12} then sub_agent_done{sub_run_id:"sa-1",status:"completed",summary} then phase_completed
    - fxPhaseLlmBatchAgents: phase_started{...phase_type:"llm_batch_agents"} then 4x sub_agent_start{sub_run_id:"sa-N",...} then 4x sub_agent_done{sub_run_id:"sa-N",...} then phase_completed
    - fxPhaseLlmHumanInput: phase_started{...phase_type:"llm_human_input"} then ask_user_prompt{tool_call_id:"tc-1",prompt,options:["Looks good","Needs changes"],timeout_seconds:300,draft:"<prior phase text>"} then ask_user_response{tool_call_id:"tc-1"} then phase_completed

    Per-run-state fixtures (verbatim from section 7); each exports BOTH the live string[] AND a reconcileSeed object matching ThreadWorkflowState:
    - fxRunRunning: reconcileSeed {mode:"harness",definition_name:"X",current_phase_index:1,total_phases:3,run_status:"running",latest_producer_run_id:"r-1"} + live phases advancing, last still running
    - fxRunGatefailRetry: phase_started then gate_failed{phase,attempt:1,error:"<validator message>"} then gate_failed{phase,attempt:2,error} then phase_completed. Also export fxRunGatefailWallclock variant with gate_failed{attempt:1,error:"wall_clock_timeout after 600s"}
    - fxRunFailed: phase_started then gate_failed{phase,attempt:2,error} then run_failed{reason:"..."} then done. Also export fxRunFailedReasonUnknown with run_failed{reason:""}
    - fxRunAskuserPaused: phase_started{phase_type:"llm_human_input"} then ask_user_prompt{tool_call_id,prompt,options,timeout_seconds:300,draft} then (no terminal, idles)
    - fxRunDone: full happy path: phases xN then delta then sources{sources:[...]} then citations{citations:[...]} then confidence{level,avg_similarity,disclaimer} then run_completed{status:"completed"} then done

    Use plain object literals + a small wire(obj) helper that returns the `data:` SSE frame string with a trailing blank line. Export every fixture as a named const. These are SAMPLE FIXTURE DATA; no renderer keys on the content (DATA-CONTRACT section 1 rule 1).
  </action>
  <verify>
    <automated>cd frontend && grep -c "export const fx" src/test-fixtures/harness094.ts</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/src/test-fixtures/harness094.ts exists with >=10 exported fixture consts (grep -c "export const fx" returns >=10)
    - grep -c "phase_type" frontend/src/test-fixtures/harness094.ts returns >=5 (all 5 literals present)
    - grep "wall_clock_timeout after 600s" frontend/src/test-fixtures/harness094.ts matches (the verbatim typed signal)
    - grep "reason_unknown\|reason.*\"\"" frontend/src/test-fixtures/harness094.ts matches the empty-reason fixture
    - cd frontend and npm run build exits 0 (module type-checks)
  </acceptance_criteria>
  <done>The shared fixtures module exports the full section 7 set (5 per-phase-type + 6 per-run-state incl. the reason-unknown + wall-clock variants) as flat wire-string arrays + reconcile seeds; type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold the 5 Wave 0 RED test files (frontend + backend)</name>
  <read_first>
    - .planning/phases/094-workflow-legibility-mode-clarity/094-VALIDATION.md (the Wave 0 Requirements list + the Per-Task Verification Map; each file's invariant + command)
    - frontend/src/providers/__tests__/panelHooks.test.tsx (the FC#1 reference-identity pattern at :292-313; const before = bucketsBySurface then replay then expect(...).toBe(before); partial-mock keeping REAL subscribeToRun + makeStreamCallbacks)
    - frontend/src/components/panel/__tests__/PendingAskCard.test.tsx (vitest-axe import at :12 + the vi.mock("@/providers/StreamsProvider") controlled-hook pattern at :27-35)
    - backend/tests/test_harness_engine.py AND backend/tests/test_harness_gates.py (how run_workflow is driven to a fail_run/skip outcome in a test)
    - frontend/src/test-fixtures/harness094.ts (Task 2; import the fixtures)
  </read_first>
  <action>
    Create 5 RED test scaffolds. Each must COLLECT (be discovered by the runner) and contain at least one it.todo (frontend) or a skip/xfail-marked test (backend) so the suite registers the test; the downstream plan flips it GREEN. DO NOT implement production code here. Each scaffold names its downstream owner in a top-of-file comment.

    1. frontend/src/providers/__tests__/phaseHooks.test.tsx (owner: Plan 02). Mirror panelHooks.test.tsx: partial-mock @/lib/api keeping REAL subscribeToRun; mock getThreadWorkflow. Scaffold as it.todo until Plan 02 lands phasesByThread/onPhase*: INV-1 (const before = bucketsBySurface; replay fxPhaseLlmAgent through subscribeToRun; assert expect(bucketsBySurface).toBe(before) AND phasesByThread.get(THREAD_A) has the phase) and INV-5 (replay phase_started on THREAD_A's callbacks while THREAD_B has its own phases; assert THREAD_B's phasesByThread untouched, Pitfall 6 per-thread keying).
    2. frontend/src/components/panel/__tests__/PhaseTimeline.test.tsx (owner: Plan 03). Import vitest-axe; scaffold INV-2 as it.todo: render against fxRunRunning/fxRunFailed/fxRunDone/fxRunAskuserPaused/fxRunGatefailRetry per state + 1-phase + N-phase with expect(await axe(container)).toHaveNoViolations(); plus announcer-fires-once + aria-expanded-toggle + aria-busy-flips asserts (UI-SPEC A11Y test gate).
    3. frontend/src/components/panel/__tests__/FailReason.test.tsx (owner: Plan 03). Scaffold INV-3b as it.todo: a run_failed fixture renders FAILED not done; run_failed{reason:""} renders the reason_unknown sentinel copy ("Failure reason not captured by the backend"), never an empty red card.
    4. frontend/src/components/panel/__tests__/PhaseReconcile.test.tsx (owner: Plan 03). Scaffold INV-4 as it.todo: seed reconcile {current_phase_index:1,total_phases:3}, replay regressing live events, assert Phase i/N only advances forward; the full N-phase skeleton renders from total_phases alone.
    5. backend/tests/test_094_rc4_failure.py (owner: Plan 04). Scaffold INV-3a: drive run_workflow to BOTH failure-return sites (a fail_run outcome AND a missing-skip-target runtime guard); assert a messages assistant row IS persisted with the failure reason; assert the empty-reason case persists the reason_unknown sentinel; assert _shielded_finalize is NOT in the call path (Deep byte-identical). Use the existing harness test conftest fixtures; mark bodies with pytest.mark.skip(reason="Wave 0 scaffold, Plan 04 implements") so the suite collects without erroring.

    Use it.todo (frontend) / skip-or-xfail (backend) placeholders where the implementation does not exist yet; the scaffold must COLLECT, not necessarily pass.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/providers/__tests__/phaseHooks.test.tsx src/components/panel/__tests__/PhaseTimeline.test.tsx src/components/panel/__tests__/FailReason.test.tsx src/components/panel/__tests__/PhaseReconcile.test.tsx --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - All 4 frontend scaffold files exist and COLLECT under vitest (no collection/syntax error in the run output)
    - grep -c "it.todo" across the 4 frontend scaffolds returns >=5 (the seeded invariants)
    - backend/tests/test_094_rc4_failure.py exists and `cd backend; pytest tests/test_094_rc4_failure.py --collect-only` lists the test functions with no collection error
    - Each scaffold's top comment names its downstream owner plan (grep "owner: Plan" finds all 5)
  </acceptance_criteria>
  <done>5 Wave 0 scaffolds exist, collect cleanly, and seed every falsifiable invariant (INV-1..5 + INV-3a) as RED placeholders that the downstream plans flip GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time CSS/config | --accent-violet is a static design token; no runtime/user input crosses it |
| test fixtures | static SAMPLE wire data; never reaches production code paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-094-01-01 | Tampering | --accent-violet token | accept | Pure CSS variable, no user input; contrast values are author-fixed and verified in UI-SPEC. No attack surface. |
| T-094-01-02 | Information disclosure | test fixtures module | accept | Fixtures are static placeholder strings (DATA-CONTRACT section 1 rule 1); contain no secrets, PII, or real workflow content; never bundled into the production build (test-fixtures dir is test-only). |
</threat_model>

<verification>
- `cd frontend && npm run build` clean (token resolves, fixtures type-check)
- `grep -c "accent-violet" frontend/src/index.css` >=2 AND `grep -c "accent-violet" frontend/tailwind.config.js` >=1
- The 4 frontend scaffolds collect under vitest; the backend scaffold collects under pytest
- No production component renders the token yet (Plan 03 owns that)
</verification>

<success_criteria>
- --accent-violet present in both theme blocks (light 258 80% 40%, dark 258 90% 66%) + the Tailwind utility, build clean
- Shared fixtures module exports >=10 fixtures covering all 5 phase types + 6 run states incl. reason-unknown + wall-clock variants
- 5 Wave 0 scaffolds exist and collect, seeding INV-1..5 + INV-3a as RED
</success_criteria>

<output>
After completion, create `.planning/phases/094-workflow-legibility-mode-clarity/094-01-SUMMARY.md`
</output>
