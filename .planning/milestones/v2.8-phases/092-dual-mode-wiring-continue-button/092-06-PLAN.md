---
phase: 092-dual-mode-wiring-continue-button
plan: 06
type: execute
wave: 2
depends_on: [05]
gap_closure: true
# autonomous=false because the final task is an operator Chrome-MCP / cross-provider UAT checkpoint
autonomous: false
requirements: [MODE-01, MODE-02, CONT-01]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/providers/StreamsProvider.tsx
  - frontend/src/hooks/useMessages.ts
  - frontend/src/components/chat/ChatArea.tsx
  - frontend/src/components/chat/MessageInput.tsx

must_haves:
  truths:
    - "On a thread with an active locked workflow, the Deep/Harness toggle, the General/Explorer selector, the textarea, and Send are ALL disabled with a tooltip (D-03/D-05)"
    - "The per-thread lock is SEEDED at workflow kickoff and on the mount-time getThreadWorkflow reconcile — not only on a cap_paused SSE event"
    - "A 409 lock-refusal on send surfaces a user-visible error AND rolls back BOTH the optimistic user bubble and the orphaned assistant placeholder (no ghost messages)"
    - "The lock is per-thread keyed (never a global boolean) — a background workflow on Thread A does not disable Thread B's composer (SC#3)"
    - "A reload mid-workflow reconciles the lock from GET /threads/{id}/workflow (Realtime-as-hint, reconcile-on-fetch — D-v2.5-03)"
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "postMessage throws a typed error carrying HTTP status so a 409 is distinguishable"
      contains: "status"
    - path: "frontend/src/providers/StreamsProvider.tsx"
      provides: "lock seeded at kickoff + on reconcile; 409 rollback of both optimistic bubbles + error surface"
      contains: "setWorkflowLockForThread"
    - path: "frontend/src/components/chat/ChatArea.tsx"
      provides: "textarea/Send disable derived from the per-thread workflowLocked, passed to MessageInput"
      contains: "workflowLocked"
  key_links:
    - from: "frontend/src/components/chat/ChatArea.tsx workflowLocked"
      to: "MessageInput composer disable (textarea + Send + both selectors)"
      via: "disabled prop derived from useWorkflowLockForThread(thread?.id)"
      pattern: "workflowLocked"
    - from: "frontend/src/providers/StreamsProvider.tsx getThreadWorkflow reconcile + kickoff"
      to: "workflowLockByThread Map (per-thread keyed)"
      via: "setWorkflowLockForThread(threadId, {...}) when locked && !lock_is_stale"
      pattern: "setWorkflowLockForThread"
    - from: "frontend/src/providers/StreamsProvider.tsx sendMessage catch (409)"
      to: "optimistic user + assistant bubble rollback + error surface"
      via: "remove temp user + assistant ids; set per-thread error"
      pattern: "409"
---

<objective>
Close F3 (MEDIUM UX) from the Wave 4 UAT, then run the F1-unblocked lived-experience UAT as the phase verification gate.

F3 has two halves, both verified against the live frontend:
- **Disable-while-locked is not wired:** `MessageInput` ALREADY consumes a `workflowLocked` prop to disable the Deep/Harness toggle + General/Explorer selector with tooltips, and `ChatArea` ALREADY derives `workflowLocked` from `useWorkflowLockForThread(thread?.id)`. BUT the `workflowLockByThread` Map is only populated on a `cap_paused` SSE event (StreamsProvider:708) — it is NOT seeded at workflow kickoff or on the mount-time `getThreadWorkflow` reconcile. So on the freshly-started locked thread the lock is absent → `workflowLocked=false` → every control stays enabled (the UAT's `modeSelDisabled:false`). The textarea + Send are also not gated on the lock.
- **409 has no feedback + leaves ghosts:** on a 409 lock-refusal, `sendMessage`'s catch (StreamsProvider:1320) only flips the assistant placeholder to `runStatus:"failed"` — it leaves the optimistic USER bubble AND an orphaned empty assistant placeholder, with NO user-visible error. `postMessage` (api.ts:379) throws a generic `Error` that does not preserve the 409 status.

Purpose: make the lock honest on the active thread and give a 409 clean feedback + rollback, so the dual-mode composer behaves as D-03/D-05 specified. Then exercise the Manual-Only UAT rows that F1 BLOCKED — they are this phase's binding verification gate (and ALSO satisfy Phase 091's parked cross-provider workflow UAT).

OUT OF SCOPE (deferred to NEW phases per 092-WORKFLOW-UX-STRATEGY-BRIEF §5/§7): composer consolidation (Provider-into-Model fold / move workflow-start to the panel), the Phase 094 panel timeline. Fix F3 to the CURRENT design (the brief notes the later consolidation will only simplify it). Do NOT plan any consolidation here.

Constraints (G-5 hot files + 075.x cascade): StreamsProvider / useMessages / MessageInput / ChatArea are G-5-firing hot files. Keep EVERY change additive + per-thread keyed (the BUG-260523-01 pattern). NEVER a global boolean. NEVER touch a provider-specific streaming branch.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/092-dual-mode-wiring-continue-button/092-04-UAT-FINDINGS.md
@.planning/phases/092-dual-mode-wiring-continue-button/092-04-SUMMARY.md
@.planning/phases/092-dual-mode-wiring-continue-button/092-VALIDATION.md
@CLAUDE.md

<project_skill>
Read `Skill("sketch-findings-agentic-rag")` (panel-shell + chat↔panel-seam + composer findings) BEFORE editing the composer — it is the design baseline for the disable/tooltip treatment.
</project_skill>

<interfaces>
<!-- Verified live against the frontend. Use directly — the lock state + selector already exist. -->

frontend/src/providers/StreamsProvider.tsx:
  - useWorkflowLockForThread(threadId) (line 1969): reads workflowLockByThread.get(threadId) ?? null — per-thread keyed selector, ALREADY EXPORTED.
  - setWorkflowLockForThread(threadId, lock) action (line 1699) via _setWorkflowLock copy-then-mutate (line 770); _clearWorkflowLock GC-deletes the key (line 786).
  - onCapPaused (line 708): the ONLY current setWorkflowLockForThread call — seeds the lock on a cap_paused SSE event. Missing: a seed at KICKOFF and on the mount RECONCILE.
  - reconcile action (line 915) — fires getThreadWorkflow-equivalent on mount/thread-switch; this is where the lock should be seeded from the reconcile result.
  - sendMessage action (line 1139): optimistic user bubble (1146-1158, temp id `userMsg.id`), optimistic assistant placeholder (1161-1176, `assistantId`); the catch (1320) currently only sets the assistant to runStatus:"failed".
  - WorkflowLock type carries { runId, mode, capPaused, continuesRemaining } (see onCapPaused payload shape).

frontend/src/components/chat/ChatArea.tsx:
  - workflowLock = useWorkflowLockForThread(thread?.id ?? null) (line 82); workflowLocked = workflowLock !== null (line 83).
  - <MessageInput onSend={handleSend} disabled={isStreaming} ... workflowLocked={workflowLocked} /> (lines 348-367).
  - reconcileErrors per-thread Map + useReconcileErrorForThread selector (line 77) rendered as an inline banner (line 465) — the ESTABLISHED per-thread user-error surface (reuse this for the 409 feedback; do NOT add a new toast lib — none exists in the app).

frontend/src/components/chat/MessageInput.tsx:
  - workflowLocked prop (line 58) ALREADY disables the General/Explorer selector (lines 273-291) and the Deep/Harness toggle (lines 323-341) with tooltips. The textarea (line 154 `disabled={disabled}`) + Send (line 440 `disabled={!canSend}`, canSend at 124 = `!disabled && value.trim().length>0`) are gated on `disabled` only — NOT on workflowLocked.

frontend/src/lib/api.ts:
  - postMessage (line 350): `if (!res.ok) throw new Error("Failed to send message")` (line 379) — generic, loses the 409 status.
  - getThreadWorkflow already exists (092-04, api.ts) returning the ThreadWorkflowState incl. locked / lock_is_stale / run_status / cap_paused / continues_remaining / active_workflow_run_id.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Typed 409 from postMessage + seed the per-thread lock at kickoff and on reconcile (F3 wiring)</name>
  <files>frontend/src/lib/api.ts, frontend/src/providers/StreamsProvider.tsx</files>
  <read_first>
    - frontend/src/lib/api.ts:350-380 (postMessage — the generic throw to replace) + the getThreadWorkflow client (returns locked/lock_is_stale/run_status/active_workflow_run_id/cap_paused/continues_remaining)
    - frontend/src/providers/StreamsProvider.tsx:703-715 (onCapPaused — the only setWorkflowLockForThread seed today) + :770-797 (_setWorkflowLock / _clearWorkflowLock helpers) + :915-1000 (reconcile action — where the mount/thread-switch reconcile result is available) + :1189-1205 (sendMessage post-POST: run_id known → kickoff seed point)
    - 092-04-UAT-FINDINGS.md F3 + 092-CONTEXT.md D-03/D-05
  </read_first>
  <action>
**1. Typed 409 (api.ts):** Replace the generic throw in `postMessage` so the HTTP status survives. Define/reuse a lightweight typed error (mirror any existing status-carrying error idiom in the file; if none, a minimal class):
```typescript
export class ApiError extends Error {
  constructor(message: string, public readonly status: number) {
    super(message)
    this.name = "ApiError"
  }
}
// in postMessage, replacing line ~379:
if (!res.ok) throw new ApiError("Failed to send message", res.status)
```
Apply the same typed throw only at `postMessage` (the send path) — do NOT churn other api.ts throws (keep the diff additive/minimal).

**2. Seed the lock at KICKOFF (StreamsProvider sendMessage):** after `postMessage` resolves with `{message_id, run_id}` on a Harness send (i.e. `opts?.workflowDefinitionId` was set), seed the per-thread lock so the composer disables immediately (not only on cap_paused):
```typescript
if (opts?.workflowDefinitionId && run_id) {
  useStreamsStore.getState().actions.setWorkflowLockForThread(threadId, {
    runId: run_id, mode: "harness", capPaused: false, continuesRemaining: undefined,
  })
}
```
(Match the existing WorkflowLock shape used in onCapPaused.)

**3. Seed/clear the lock on the mount RECONCILE (StreamsProvider reconcile action):** the reconcile already runs `getThreadWorkflow`-equivalent on mount/thread-switch (D-v2.5-03 reconcile-on-fetch). After it has the ThreadWorkflowState, set the lock when the run is genuinely locked, and CLEAR it when stale/terminal:
```typescript
const wf = await getThreadWorkflow(threadId)   // (use the already-wired reconcile fetch)
if (wf.locked && !wf.lock_is_stale && wf.active_workflow_run_id) {
  actions.setWorkflowLockForThread(threadId, {
    runId: wf.active_workflow_run_id, mode: "harness",
    capPaused: wf.cap_paused, continuesRemaining: wf.continues_remaining,
  })
} else {
  actions.clearWorkflowLockForThread(threadId)   // stale/terminal/Deep → unlock (F2 self-heal honored)
}
```
Wire this into the EXISTING reconcile flow (do not add a second fetch if the reconcile already fetches workflow state — fold the seed/clear into the existing result handling). Crucial: key everything by the OWNING `threadId` from the reconcile closure — never a global flag (SC#3 / BUG-260523-01).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "class ApiError" frontend/src/lib/api.ts` exits 0
    - `grep -q "new ApiError(\"Failed to send message\", res.status)" frontend/src/lib/api.ts` exits 0
    - `grep -c "setWorkflowLockForThread" frontend/src/providers/StreamsProvider.tsx` returns >= 3 (onCapPaused + kickoff + reconcile)
    - `grep -q "clearWorkflowLockForThread" frontend/src/providers/StreamsProvider.tsx` exits 0 (reconcile clears stale/terminal)
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>postMessage throws a status-carrying ApiError; the per-thread lock is seeded at kickoff and seeded/cleared on the mount reconcile (honoring lock_is_stale from the F2 backstop); tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Disable textarea+Send while locked + 409 rollback of both optimistic bubbles + error surface (F3 UX)</name>
  <files>frontend/src/components/chat/ChatArea.tsx, frontend/src/components/chat/MessageInput.tsx, frontend/src/providers/StreamsProvider.tsx, frontend/src/hooks/useMessages.ts</files>
  <read_first>
    - frontend/src/components/chat/MessageInput.tsx:107-124 (handleSend + canSend) + :150-156 (textarea disabled) + :420-445 (Send button disabled) — gate these on workflowLocked too
    - frontend/src/components/chat/ChatArea.tsx:75-83 (workflowLock derivation) + :77,465 (reconcileErrors banner — reuse for the 409 surface) + :348-367 (MessageInput render — disabled prop)
    - frontend/src/providers/StreamsProvider.tsx:1146-1176 (optimistic userMsg.id + assistantId) + :1320-1330 (catch — the place to branch on a 409 and roll back both) + the reconcileErrors / setReconcileError per-thread idiom (usePanelReconcile.ts:96 shows the Map-set shape)
    - 092-04-UAT-FINDINGS.md F3 ("added optimistically … no error toast … orphaned empty assistant placeholder")
  </read_first>
  <action>
**1. Gate the composer input on the lock (MessageInput.tsx):** the toggle + agent selector already disable on `workflowLocked`. Extend the textarea and Send so a locked thread cannot type/send:
   - `handleSend` (line 107): early-return when `workflowLocked` (already returns on `disabled`; add `|| workflowLocked`).
   - `canSend` (line 124): `!disabled && !workflowLocked && value.trim().length > 0`.
   - textarea `disabled` (line 154): `disabled={disabled || workflowLocked}` and add a `title`/placeholder hint when locked ("Workflow running — Cancel to switch back" per D-05).
   Keep the disable a courtesy (the server 409 stays the authority — D-05).

**2. Roll back both optimistic bubbles + surface the 409 (StreamsProvider sendMessage catch, ~1320):** branch on the typed ApiError 409. On a 409, REMOVE both the optimistic user bubble (`userMsg.id`) and the assistant placeholder (`assistantId`) from the bucket (do NOT just flip the assistant to failed), and write a per-thread error so the user sees feedback:
```typescript
} catch (err) {
  if (err instanceof Error && err.name === "AbortError") {
    // caller-initiated abort
  } else if (err instanceof ApiError && err.status === 409) {
    // Lock refusal — roll back BOTH optimistic bubbles (no ghost user msg, no orphan placeholder)
    useStreamsStore.getState().actions.setMessagesForBucket(surfaceId, threadId, (prev) =>
      prev.filter((m) => m.id !== assistantId && m.id !== userMsg.id),
    )
    // Reuse the established per-thread error surface (reconcileErrors banner in ChatArea).
    useStreamsStore.setState((s) => ({
      reconcileErrors: new Map(s.reconcileErrors).set(
        threadId, "This thread is running a workflow — cancel it to send a Deep message.",
      ),
    }))
  } else {
    console.error("sendMessage failed:", err)
    useStreamsStore.getState().actions.setMessagesForBucket(surfaceId, threadId, (prev) =>
      prev.map((m) => (m.id === assistantId ? { ...m, runStatus: "failed" } : m)),
    )
  }
}
```
Confirm `userMsg` and `assistantId` are in scope at the catch (they are — declared at 1146/1161, function scope). Use the EXISTING per-thread `reconcileErrors` Map (ChatArea already renders `useReconcileErrorForThread` as a banner at line 465 and clears it on thread switch) — do NOT introduce a toast library (none exists). Key by the OWNING `threadId`.

**3. Pass the locked state to the textarea/Send (ChatArea + MessageInput props):** ChatArea already passes `workflowLocked={workflowLocked}` (line 367) and `disabled={isStreaming}`. MessageInput now consumes `workflowLocked` for the textarea/Send too (step 1) — no ChatArea change needed beyond confirming the prop flows (it does). If a separate visible "locked" affordance on the textarea is cleaner, add it inside MessageInput keyed on the prop (no new ChatArea state).

Keep all of this per-thread keyed and additive — no provider streaming branch, no global flag.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "disabled || workflowLocked" frontend/src/components/chat/MessageInput.tsx` exits 0
    - `grep -q "!workflowLocked" frontend/src/components/chat/MessageInput.tsx` exits 0 (canSend gates on the lock)
    - `grep -q "err.status === 409" frontend/src/providers/StreamsProvider.tsx` exits 0
    - `grep -q "m.id !== assistantId && m.id !== userMsg.id" frontend/src/providers/StreamsProvider.tsx` exits 0 (both bubbles rolled back)
    - `grep -q "reconcileErrors" frontend/src/providers/StreamsProvider.tsx` exits 0 (409 surfaced via the existing per-thread error banner)
    - `cd frontend && npx tsc --noEmit && npm run build` exits 0 (no NET-NEW tsc errors beyond the documented 54-error baseline; build clean)
  </acceptance_criteria>
  <done>While locked, the textarea + Send are disabled (toggle + selector already are); a 409 on send rolls back BOTH optimistic bubbles and shows the per-thread error banner — no ghost messages, clear feedback. Per-thread keyed, additive, build clean.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: F1-unblocked lived-experience UAT — the phase verification gate (Chrome MCP + native-7 cross-provider)</name>
  <files>(no source — operator-driven verification against the running app)</files>
  <read_first>
    - 092-VALIDATION.md § Manual-Only Verifications (the 6 rows below — they were BLOCKED by F1 and are now the binding gate)
    - CLAUDE.md § UAT scoreboard recipe (the 4-axis native-7 bandwidth: cross-provider × multi-tool × parallel-thread × long-message) + § feedback_user_starts_backend (the operator starts uvicorn themselves)
    - 092-04-UAT-FINDINGS.md (what failed in Wave 4 — every row here was previously unreachable)
  </read_first>
  <action>
Operator + orchestrator (Chrome DevTools MCP) drive the running app. The operator starts the backend uvicorn themselves in a visible terminal (CLAUDE.md — never `run_in_background`) and `npm run dev`; login fhdmrd@gmail.com / 123456 at http://localhost:5173/. The orchestrator must NOT start the backend.

Verify ALL of the following (these are the F1-unblocked Manual-Only rows — the whole point of Wave 4). No "if data permits" deferrals (feedback_uat_cover_all_design_contracts) — seed the runs:

1. **F1 closed (end-to-end run):** Kick off Research→Summarize (Harness) on a thread, type a topic, send. Phases execute; the assistant produces real output; the `run:{run_id}` SSE stream shows NO NotNullViolationError. `SELECT user_id, event_type FROM harness_audit WHERE run_id='<run>'` → rows with non-null user_id.
2. **SC#2 anchor → NULL (live Supabase query):** after a Cancel (Stop button) AND after a natural workflow completion: `SELECT active_workflow_run_id FROM threads WHERE id='<thread>'` → NULL both times.
3. **SC#5 reload-reconcile + Stop-unlock:** reload mid-workflow on the locked thread → composer/lock reconciles from GET /threads/{id}/workflow (not a stale hint); Cancel via Stop → thread unlocks; the composer (toggle + selector + textarea + Send) re-enables.
4. **F2 closed (no wedged lock):** force/observe a run that fails (or use the F1 reproduction is now fixed — instead confirm a deliberately-failing gate path) → the thread does NOT stay wedged: GET /threads/{id}/workflow reports lock_is_stale or terminal, and the composer re-enables (the F2 terminalize + self-heal).
5. **F3 closed (composer + 409):** on the active locked thread confirm the Deep/Harness toggle, General/Explorer selector, textarea, and Send are ALL disabled with tooltips; attempt a Deep "sneak" send → the per-thread error banner appears AND no ghost user bubble / orphaned assistant placeholder remains.
6. **SC#3 parallel-thread (binding gate):** Thread A runs a workflow (locked + streaming); confirm Thread B's toggle + selector + composer stay FREE; switch back to A — still locked. (Per-thread lock is NOT a global boolean.)
7. **CONT-01 cap-drive → Continue → 3-cap (LangSmith trace):** drive a Deep run / Harness phase to its step cap → the inline Continue card shows the dropped tool names → click → the dropped tools actually run (transcript + LangSmith), SAME run_id; repeat to the 3-cap; the 4th is refused.
8. **SC#10 native-7 4-axis cross-provider scoreboard (operator-owned — ALSO closes Phase 091's parked UAT):** seed a workflow kickoff + a Deep multi-tool prompt across OpenAI / Anthropic / Google / DeepSeek / Moonshot / GLM / MiniMax (OpenRouter best-effort) × multi-tool (≥2 tools in one prompt, e.g. search_documents + execute_code) × parallel-thread × long-message (≥50 prior msgs OR ≥5KB prompt); both agent_mode values; zero cross-provider regressions; confirm a persisted `workflow_runs` row (`SELECT id,status,user_id FROM workflow_runs ORDER BY created_at DESC LIMIT 1`).
9. **Deep Mode byte-identical:** a Deep multi-tool run's SSE event sequence is structurally identical pre/post (089 run1-vs-run1 structural diff empty); the `active_workflow_run_id IS NULL` path is unchanged.

Record results in a `092-06-UAT-FINDINGS.md` (PASS rows + any new gaps). If any binding row fails, route a follow-up gap plan; do NOT mark MODE-01/MODE-02/CONT-01 complete until all binding rows pass.
  </action>
  <what-built>F3 wired (Tasks 1-2) + the F1/F2 backend fixes (092-05); this checkpoint exercises every Manual-Only UAT row that F1 had blocked.</what-built>
  <how-to-verify>Run the 9 steps above against the live app with Chrome DevTools MCP + live Supabase/LangSmith queries. The operator starts the backend uvicorn; the orchestrator drives the browser + queries.</how-to-verify>
  <acceptance_criteria>
    - All 9 steps pass (or new gaps are recorded in 092-06-UAT-FINDINGS.md and routed).
    - SC#2: `SELECT active_workflow_run_id FROM threads WHERE id='<thread>'` returns NULL after both cancel and natural completion (operator pastes result).
    - SC#10: a persisted `workflow_runs` row exists with a non-null user_id (operator pastes result) — confirms F1 closed AND Phase 091's parked UAT satisfied.
    - SC#3: Thread B composer demonstrably stays enabled while Thread A is locked.
  </acceptance_criteria>
  <resume-signal>Type "UAT pass" with the SC#2 NULL result + the workflow_runs row, or "UAT fail: <rows>" with evidence to route a follow-up gap plan.</resume-signal>
  <done>The F1-unblocked Manual-Only UAT rows (F1 end-to-end, SC#2, SC#3, SC#5, F2, F3, CONT-01, SC#10 native-7, Deep byte-identical) are all exercised and pass; results recorded; MODE-01/MODE-02/CONT-01 closure confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client composer → POST /threads/{id}/messages | the client disable is a courtesy; the server-side 409 lock-refusal is the authority (D-05) — never trust the client to enforce the lock |
| Realtime/SSE hint → client lock state | Realtime is a hint, not truth (D-v2.5-03); the lock is reconciled from GET /threads/{id}/workflow on mount, never trusted from a stale hint alone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-092-06-01 | Spoofing | client bypasses the disabled composer | accept | the server 409 at POST /threads/{id}/messages is the authoritative refusal (092-02/03); the client disable + 409 rollback are UX only — a bypass is harmless |
| T-092-06-02 | Tampering | global-flag lock regression (SC#3) | mitigate | the lock is per-thread keyed in workflowLockByThread (BUG-260523-01 pattern); the parallel-thread UAT row is the binding gate against a global-boolean regression |
| T-092-06-03 | Information disclosure | 409 error copy leaks internals | mitigate | the surfaced message is a fixed user-facing string ("cancel it to send a Deep message"), never the raw server error/body |
</threat_model>

<verification>
- Per task: Task 1 `tsc --noEmit` clean; Task 2 `tsc --noEmit && npm run build` clean (zero net-new tsc errors over the documented 54-error baseline).
- Frontend wave gate: `cd frontend && npx tsc --noEmit && npm run build` green.
- Phase gate: Task 3's 9-step lived-experience UAT all pass (the F1-unblocked Manual-Only rows), recorded in 092-06-UAT-FINDINGS.md.
</verification>

<success_criteria>
- F3 closed: on a locked thread, toggle + selector + textarea + Send are all disabled with tooltips; a 409 send rolls back both optimistic bubbles and shows a per-thread error banner (no ghosts).
- The per-thread lock is seeded at kickoff and reconciled on mount (honoring the F2 lock_is_stale self-heal); never a global boolean (SC#3 holds in the parallel-thread UAT).
- All F1-unblocked Manual-Only UAT rows pass (SC#2/3/5 + F2 + CONT-01 + SC#10 native-7 + Deep byte-identical), which ALSO satisfies Phase 091's parked cross-provider workflow UAT.
- MODE-01, MODE-02, CONT-01 verifiably complete.
- No provider streaming branch touched; all frontend changes additive + per-thread keyed (G-5 / 075.x cascade rules honored).
</success_criteria>

<output>
After completion, create `.planning/phases/092-dual-mode-wiring-continue-button/092-06-SUMMARY.md` and record the UAT results in `.planning/phases/092-dual-mode-wiring-continue-button/092-06-UAT-FINDINGS.md`.
</output>
