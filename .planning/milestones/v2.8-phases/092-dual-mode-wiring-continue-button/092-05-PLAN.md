---
phase: 092-dual-mode-wiring-continue-button
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
# autonomous=false because Wave 1 Task 1 is an operator migration-apply checkpoint
autonomous: false
requirements: [MODE-01, MODE-02, CONT-01]
files_modified:
  - supabase/migrations/064_workflow_runs_user_id.sql
  - supabase/full-schema.sql
  - backend/app/db/workflows.py
  - backend/app/services/harness_engine.py
  - backend/app/api/threads.py
  - backend/tests/test_dual_mode_wiring.py
  - backend/tests/integration/test_092_harness_audit_live.py

must_haves:
  truths:
    - "A Harness workflow run executes end-to-end — its phases run and produce assistant output (F1 closed; no NotNullViolationError on harness_audit)"
    - "Every harness_audit row written for a live run carries a non-null user_id (the run-owner)"
    - "workflow_runs persists the run-owner's user_id at creation, so the resume path (harness_engine:574) resolves a real user, not None"
    - "When a workflow run dies via an unhandled exception, workflow_runs.status moves to a terminal state AND threads.active_workflow_run_id is cleared — the thread is never wedged locked (F2 primary fix)"
    - "GET /threads/{id}/workflow reports lock_is_stale=true when the anchor is set but the underlying producer run is terminal/missing (F2 self-heal backstop)"
  artifacts:
    - path: "supabase/migrations/064_workflow_runs_user_id.sql"
      provides: "Idempotent ADD COLUMN workflow_runs.user_id + backfill from threads.user_id + FK to auth.users"
      contains: "ADD COLUMN IF NOT EXISTS user_id"
    - path: "backend/app/db/workflows.py"
      provides: "create_workflow_run persists user_id; write_audit includes user_id in the INSERT"
      contains: "INSERT INTO harness_audit (run_id, user_id, event_type, metadata)"
    - path: "backend/app/services/harness_engine.py"
      provides: "every write_audit call passes the run-owner user_id from ctx.current_user"
      contains: "ctx.current_user"
    - path: "backend/tests/integration/test_092_harness_audit_live.py"
      provides: "live-DB integration test asserting a real harness_audit row is written for a created run (closes the mock blind spot)"
      contains: "FROM harness_audit"
  key_links:
    - from: "backend/app/api/threads.py create_workflow_run call (~986)"
      to: "workflow_runs.user_id column"
      via: "user_id=current_user['id'] arg"
      pattern: "create_workflow_run\\("
    - from: "backend/app/services/harness_engine.py write_audit calls (272/288/306/415/449/456/473/489/503/529)"
      to: "harness_audit.user_id NOT NULL column"
      via: "user_id=ctx.current_user['id'] arg threaded through every call"
      pattern: "write_audit\\("
    - from: "backend/app/api/threads.py harness branch except path (~1238)"
      to: "workflow_runs terminal status + threads anchor clear"
      via: "finish_run(pool, _active_workflow_run_id, 'failed') on exception escape"
      pattern: "finish_run"
---

<objective>
Close the two CRITICAL/HIGH backend gaps from the Wave 4 UAT (092-04-UAT-FINDINGS.md) that make Harness mode end-to-end non-functional and leave a wedged lock:

- **F1 (CRITICAL):** `write_audit` omits `user_id`; `harness_audit.user_id` is `NOT NULL`; `workflow_runs` has no `user_id` column. The FIRST audit write (`phase_started`, harness_engine.py:415) raises `NotNullViolationError` → the run dies before any phase executes → empty assistant, no tool calls. Harness mode is end-to-end broken.
- **F2 (HIGH):** when the run dies via that unhandled exception, `run_workflow` never reaches `finish_run`, so `workflow_runs.status` stays `'active'` and `threads.active_workflow_run_id` stays set → the thread is wedged locked with `lock_is_stale=false`. No UI recovery path.

Purpose: make a workflow actually run end-to-end (the whole point of Wave 4) and make the lock honest. This ALSO unblocks Phase 091's parked cross-provider workflow UAT (no live `INSERT INTO workflow_runs` succeeded before this fix).
Output: migration 064 + `workflow_runs.user_id`, `create_workflow_run`/`write_audit` threading `user_id`, every `harness_engine` audit call site passing the owner, a failure-path terminalize-and-clear in the producer's harness branch, a `lock_is_stale` self-heal backstop, and a **live-DB** integration test that closes the mock blind spot that let F1 ship.

OUT OF SCOPE (deferred to NEW phases per 092-WORKFLOW-UX-STRATEGY-BRIEF §5/§7): composer consolidation, workflow authoring API/NL-generate/form-editor, Phase 094 panel timeline. Do NOT plan any of these.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/092-dual-mode-wiring-continue-button/092-04-UAT-FINDINGS.md
@.planning/phases/092-dual-mode-wiring-continue-button/092-CONTEXT.md
@.planning/phases/092-dual-mode-wiring-continue-button/092-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Verified live against the code + schema. Use these directly — no exploration needed. -->

supabase/full-schema.sql — workflow_runs (line 747): columns id, thread_id, definition_id,
status, current_phase_id, org_id, created_at, updated_at, claimed_at, inputs, model,
continues_used. NO user_id column. status CHECK allows
('active','paused','cap_paused','completed','failed','cancelled').

supabase/full-schema.sql — harness_audit (line 431):
  id uuid PK, user_id uuid NOT NULL, run_id uuid (nullable), event_type text NOT NULL,
  metadata jsonb NOT NULL DEFAULT '{}', org_id uuid, created_at.
  user_id has NO default → every INSERT MUST supply it.

backend/app/db/workflows.py:
  - create_workflow_run(pool, *, thread_id, definition_id, definition, inputs, model) -> UUID
      INSERT INTO workflow_runs (thread_id, definition_id, status, inputs, model)
      VALUES ($1,$2,'active',$3::jsonb,$4) RETURNING id   (lines 96-106)
  - finish_run(pool, run_id, status)  (line 378) — wraps the workflow_runs terminal UPDATE
      + `UPDATE threads SET active_workflow_run_id = NULL WHERE active_workflow_run_id = $1`
      in ONE transaction (the SINGLE authoritative lock-clear site, SC#2). REUSE THIS for F2.
  - write_audit(pool, run_id, event_type, metadata)  (line 457)
      INSERT INTO harness_audit (run_id, event_type, metadata) VALUES ($1,$2,$3::jsonb)  (line 472-473)
      — OMITS user_id. asserts event_type in _AUDIT_EVENT_TYPES first.

backend/app/services/harness_engine.py:
  - run_workflow(run_id, definition, ctx, *, pool, redis) (line 359) — NO top-level try/except;
    an exception in any write_audit propagates to the producer.
  - write_audit call sites (all currently 4-arg, all have `ctx` in scope inside run_workflow /
    _run_phase_with_gates which receives ctx): lines 272, 288, 306, 415, 449, 456, 473, 489, 503, 529.
  - _build_resume_context (line 559): current_user={"id": run.get("user_id")} — resolves to None
    until workflow_runs.user_id is persisted; resume path depends on this fix too.

backend/app/api/threads.py:
  - create_workflow_run call (line 986) — has `current_user` in scope (the route param).
  - harness producer branch (lines 1141-1171): builds wf_ctx with current_user=current_user (1158),
    calls run_workflow (1165). This branch sits inside the middle try (1129) whose
    `except Exception as e:` (1238) sets _terminal_status='failed' but only finalizes the
    producer-shell `runs` row via _shielded_finalize — it does NOT terminalize workflow_runs
    or clear the anchor. THAT is F2's primary cause.
  - GET /threads/{id}/workflow get_thread_workflow (line 1436); lock_is_stale derived at line
    1509 from run_status (workflow_runs.status) ONLY — does not consider the producer `runs` row.

backend/tests/integration/test_073_concurrency.py — the live-DB test template:
  - PG_AVAILABLE skipif guard + pytestmark (lines 38-63)
  - pg_pool fixture: asyncpg.create_pool on local :54322 (line 71)
  - test_thread_user fixture: seeds an auth.users row + a threads row, yields (user_id, thread_id),
    cleans up (line 99). REUSE this exact pattern for the new live audit test.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Author migration 064 + operator-apply via SQL editor + regenerate full-schema</name>
  <files>supabase/migrations/064_workflow_runs_user_id.sql, supabase/full-schema.sql</files>
  <read_first>
    - supabase/full-schema.sql:747-761 (workflow_runs — confirm NO user_id) + :431-440 (harness_audit.user_id NOT NULL, FK pattern to mirror)
    - supabase/full-schema.sql:1662-1666 (threads_active_workflow_run_id_fkey — the ON DELETE pattern to mirror for the new FK)
    - supabase/migrations/063_dual_mode_continue.sql (the immediately-prior migration — style/idempotency reference)
    - CLAUDE.md § Rules (migration discipline: numbered SQL, apply via SQL editor NOT db push/reset, then `bash scripts/regenerate-full-schema.sh` no-reset, commit both)
  </read_first>
  <action>
Create `supabase/migrations/064_workflow_runs_user_id.sql`. The DDL must be idempotent and paste-replayable (the operator pastes it into the Supabase SQL editor — never `db push`/`db reset`):

```sql
-- 064_workflow_runs_user_id.sql
-- F1 (Phase 092-05): harness_audit.user_id is NOT NULL, but workflow_runs never
-- persisted the run-owner. Add it + backfill from the owning thread + mirror the
-- ON DELETE CASCADE FK that harness_audit.user_id uses. Idempotent / replayable.

ALTER TABLE public.workflow_runs
  ADD COLUMN IF NOT EXISTS user_id uuid;

-- Backfill existing rows from the owning thread (threads.user_id is NOT NULL).
UPDATE public.workflow_runs wr
SET user_id = t.user_id
FROM public.threads t
WHERE wr.thread_id = t.id
  AND wr.user_id IS NULL;

-- FK → auth.users, mirroring harness_audit.user_id (ON DELETE CASCADE). Guarded
-- so a re-paste does not raise "constraint already exists".
DO $$
BEGIN
  IF NOT EXISTS (
    SELECT 1 FROM pg_constraint WHERE conname = 'workflow_runs_user_id_fkey'
  ) THEN
    ALTER TABLE public.workflow_runs
      ADD CONSTRAINT workflow_runs_user_id_fkey
      FOREIGN KEY (user_id) REFERENCES auth.users(id) ON DELETE CASCADE;
  END IF;
END $$;

-- Owner-scoped lookups (resume sweep + RLS-mirroring reads).
CREATE INDEX IF NOT EXISTS idx_workflow_runs_user_id
  ON public.workflow_runs (user_id);
```

Do NOT add `NOT NULL` to the column in this migration — existing rows backfill, but enforcing NOT NULL here would fail if any orphan run predates a thread. The application layer (Task 2/3) always supplies it on new inserts; a future migration can tighten it once backfill is proven.

THEN (operator steps — this is a `checkpoint:human-action` because applying the migration is SQL-editor-only per CLAUDE.md):
1. Operator pastes `064_workflow_runs_user_id.sql` into the Supabase SQL editor and runs it (local DB on :54322).
2. Operator (or executor via Bash) runs `bash scripts/regenerate-full-schema.sh` (NO --reset — live-DB dump only) to rebuild `supabase/full-schema.sql`.
3. Commit BOTH `064_workflow_runs_user_id.sql` and the regenerated `full-schema.sql`.
  </action>
  <what-built>Migration 064 file authored; operator applies it via the Supabase SQL editor, then full-schema.sql is regenerated from the live DB.</what-built>
  <how-to-verify>
After the operator applies + regenerates:
1. `grep -q "ADD COLUMN IF NOT EXISTS user_id" supabase/migrations/064_workflow_runs_user_id.sql` → exit 0
2. `grep -q "user_id uuid" supabase/full-schema.sql` near the workflow_runs CREATE TABLE block (the regenerated dump now shows the column).
3. In the SQL editor: `SELECT column_name FROM information_schema.columns WHERE table_name='workflow_runs' AND column_name='user_id';` returns one row.
  </how-to-verify>
  <acceptance_criteria>
    - `grep -q "ADD COLUMN IF NOT EXISTS user_id" supabase/migrations/064_workflow_runs_user_id.sql` exits 0
    - `grep -q "workflow_runs_user_id_fkey" supabase/migrations/064_workflow_runs_user_id.sql` exits 0
    - `grep -q "user_id uuid" supabase/full-schema.sql` exits 0 (regenerated dump carries the column)
    - migration filename matches `^064_.*\.sql$`
  </acceptance_criteria>
  <resume-signal>Type "applied" once 064 is run in the SQL editor and full-schema.sql is regenerated + committed, or describe any error.</resume-signal>
  <done>Migration 064 authored, applied to the live local DB via SQL editor, full-schema.sql regenerated (no reset) and both committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread user_id through create_workflow_run + write_audit + every harness_engine audit site (F1)</name>
  <files>backend/app/db/workflows.py, backend/app/services/harness_engine.py, backend/app/api/threads.py, backend/tests/test_dual_mode_wiring.py</files>
  <read_first>
    - backend/app/db/workflows.py:58-122 (create_workflow_run — the INSERT to extend) + :457-477 (write_audit — the INSERT that omits user_id)
    - backend/app/services/harness_engine.py:242-310 (_run_phase_with_gates — receives `ctx`; write_audit at 272/288/306) + :359-530 (run_workflow — write_audit at 415/449/456/473/489/503/529; `ctx` is the param) + :559-579 (_build_resume_context — current_user={"id": run.get("user_id")})
    - backend/app/api/threads.py:984-997 (create_workflow_run call, current_user in scope) + :1155-1171 (wf_ctx build, current_user=current_user)
    - backend/tests/test_dual_mode_wiring.py (existing create_workflow_run atomicity test — extend it to assert user_id is in the INSERT column list)
    - 092-04-UAT-FINDINGS.md F1 (root cause + fix shape)
  </read_first>
  <behavior>
    - Test: create_workflow_run INSERT column list includes `user_id` and binds the passed value (assert against mock_asyncpg_pool.calls — the workflow_runs INSERT SQL contains `user_id` and the bound args include the user UUID).
    - Test: write_audit issues `INSERT INTO harness_audit (run_id, user_id, event_type, metadata) VALUES ($1, $2, $3, $4::jsonb)` and binds the user_id arg (assert the SQL string + arg order against the mock pool).
    - Test: write_audit still raises ValueError on an unknown event_type BEFORE issuing the INSERT (preserve the existing _AUDIT_EVENT_TYPES guard).
  </behavior>
  <action>
Three coordinated source edits — all additive, all at the run-creation/audit seam (never in a provider streaming branch):

1. **`backend/app/db/workflows.py` — `create_workflow_run`**: add a keyword-only param `user_id: UUID` to the signature. Extend the workflow_runs INSERT to:
   ```python
   INSERT INTO workflow_runs (thread_id, definition_id, status, inputs, model, user_id)
   VALUES ($1, $2, 'active', $3::jsonb, $4, $5)
   RETURNING id
   ```
   and pass `user_id` as `$5` (after `model`). Update the docstring's RLS note to mention the persisted owner.

2. **`backend/app/db/workflows.py` — `write_audit`**: add a keyword-only param `user_id: UUID` (place it right after `run_id`). Change the INSERT to:
   ```python
   INSERT INTO harness_audit (run_id, user_id, event_type, metadata) VALUES ($1, $2, $3, $4::jsonb)
   ```
   bind order `run_id, user_id, event_type, json.dumps(metadata)`. Keep the `_AUDIT_EVENT_TYPES` ValueError guard BEFORE the INSERT (fail-fast preserved). Update the comment that claimed the table "omits user_id was correct" — it was the bug.

3. **`backend/app/services/harness_engine.py`** — thread the owner into EVERY write_audit call. The owner id resolves from `ctx.current_user["id"]` (the live producer sets `wf_ctx.current_user = current_user`, threads.py:1158; the resume path's `_build_resume_context` now reads the persisted `workflow_runs.user_id`). Add a single local at the top of `run_workflow` (and pass it into `_run_phase_with_gates` so its three sites can use it):
   ```python
   _audit_user_id = (ctx.current_user or {}).get("id") if getattr(ctx, "current_user", None) else None
   ```
   Then update all 10 write_audit calls (lines 272, 288, 306, 415, 449, 456, 473, 489, 503, 529) to pass `user_id=_audit_user_id` (the 3 inside `_run_phase_with_gates` take it via a new keyword param threaded from run_workflow). Keep `run_id` positional, add `user_id=` as a keyword.

4. **`backend/app/api/threads.py`** — at the `create_workflow_run` call (~986) add:
   ```python
   user_id=UUID(current_user["id"]) if isinstance(current_user["id"], str) else current_user["id"],
   ```
   (mirror the existing UUID-coercion idiom used elsewhere in this handler, e.g. line 946).

5. **`backend/tests/test_dual_mode_wiring.py`** — add/extend the create_workflow_run atomicity assertions to require `user_id` in the INSERT column list + bound args; add a write_audit unit test asserting the new 4-column INSERT + arg order + the preserved ValueError-on-bad-event-type guard (against `mock_asyncpg_pool`).

Do NOT change the engine's behavior beyond passing the owner id — the audit content, ordering, and SSE emits stay byte-identical.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_dual_mode_wiring.py -k "audit or create_workflow_run or user_id" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "INSERT INTO harness_audit (run_id, user_id, event_type, metadata)" backend/app/db/workflows.py` exits 0
    - `grep -q "thread_id, definition_id, status, inputs, model, user_id" backend/app/db/workflows.py` exits 0
    - `grep -c "user_id=_audit_user_id" backend/app/services/harness_engine.py` returns 10
    - `grep -q "user_id=UUID(current_user\[.id.\])" backend/app/api/threads.py` exits 0
    - `cd backend && venv/Scripts/python -m pytest tests/test_dual_mode_wiring.py -k "audit or create_workflow_run" -q` exits 0
  </acceptance_criteria>
  <done>create_workflow_run persists user_id; write_audit includes user_id; all 10 harness_engine audit sites pass the owner; the producer call supplies current_user['id']; unit tests assert the new INSERT shapes and pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Failure-path terminalize-and-clear in the harness producer branch + lock_is_stale self-heal (F2)</name>
  <files>backend/app/api/threads.py, backend/tests/test_dual_mode_wiring.py</files>
  <read_first>
    - backend/app/api/threads.py:1129-1171 (middle try + harness branch) + :1238-1246 (`except Exception as e:` — sets _terminal_status='failed', does NOT touch workflow_runs/anchor) + :1247-1310 (_shielded_finalize — finalizes only the producer-shell `runs` row)
    - backend/app/api/threads.py:1436-1553 (get_thread_workflow) — specifically :1501-1511 (locked / lock_is_stale derivation from run_status only) + :1518-1532 (the existing `runs` row query idiom, reuse it)
    - backend/app/db/workflows.py:378-407 (finish_run — the reusable single-clear-site: terminal UPDATE + anchor NULL in one txn)
    - 092-04-UAT-FINDINGS.md F2 (root cause + fix shape) + 092-03-SUMMARY.md key-decisions (single-clear-site = finish_run, Landmine 5)
  </read_first>
  <behavior>
    - Test: when the harness producer branch raises (simulate run_workflow raising), the except path calls `finish_run(pool, _active_workflow_run_id, "failed")` exactly once with the workflow run id — so workflow_runs terminalizes AND the anchor clears (assert the finish_run call / the resulting UPDATE-threads-anchor SQL against mock_asyncpg_pool.calls). A Deep run (no anchor) must NOT call finish_run from this path (byte-identical).
    - Test: lock_is_stale derivation returns True when the anchor is set but the producer `runs` row is terminal even if workflow_runs.status were still 'active' (the self-heal backstop) — extend the existing lock_is_stale rule test in test_thread_workflow_endpoint.py is owned there; here add the threads.py-side query coverage in test_dual_mode_wiring.py if cleaner, else assert the derivation helper.
  </behavior>
  <action>
**Primary F2 fix (the real bug) — terminalize on exception escape from the harness branch:**

In `backend/app/api/threads.py`, the middle-try `except Exception as e:` block (~1238) currently only sets `_terminal_status='failed'`. Add, INSIDE that except (and also in the `except asyncio.TimeoutError` and the `except asyncio.CancelledError` branches — any non-success escape of a harness run must terminalize), a guard that terminalizes the workflow_runs row + clears the anchor when this was a Harness send:

```python
# Phase 092-05 F2: a harness run that escapes via exception/timeout never reached
# run_workflow's own finish_run, so workflow_runs stays 'active' and the thread is
# wedged locked. Terminalize + clear the anchor here (idempotent: finish_run no-ops
# if run_workflow already cleared it). Deep runs (no anchor) skip this entirely.
if _active_workflow_run_id is not None:
    try:
        from app.db.workflows import finish_run as _finish_wf
        await _finish_wf(await get_pg_pool(), _active_workflow_run_id, "failed")
    except Exception:
        logger.exception("F2 harness-failure terminalize failed for run %s", _active_workflow_run_id)
```

Place this so it runs on the failure/timeout escape paths but NOT on natural success (natural success goes through run_workflow's own `finish_run(..., "completed")` at harness_engine:528). For `CancelledError` the cancel path (runs.py DELETE) already clears the anchor (092-03 zombie-heal sibling) — but a belt-and-suspenders terminalize here is safe because `finish_run` is idempotent (clears 0 rows if already cleared). Keep the existing `raise` in the CancelledError branch (Pitfall 3) — put the terminalize BEFORE the re-raise.

**Backstop F2 self-heal — `lock_is_stale` also considers the producer `runs` row:**

In `get_thread_workflow` (~1501-1511), the `lock_is_stale` derivation keys only off `run_status` (workflow_runs.status). Extend it so a thread whose `workflow_runs` row is somehow still non-terminal but whose underlying producer `runs` row is terminal/absent ALSO reports `lock_is_stale=true` (self-heals a future stranded lock on reconcile). Query the thread's latest `runs` row status (reuse the `runs` query idiom already at ~1520) and OR a terminal/absent producer-run condition into `lock_is_stale`:

```python
# F2 backstop: even if workflow_runs.status lagged at 'active', if the underlying
# producer run for this thread is terminal/missing the lock is stale (self-heal).
producer_terminal = False
if active_workflow_run_id is not None and not lock_is_stale:
    prod_row = await pool.fetchrow(
        "SELECT status FROM runs WHERE thread_id = $1 ORDER BY started_at DESC LIMIT 1",
        UUID(thread_id) if isinstance(thread_id, str) else thread_id,
    )
    prod_status = prod_row["status"] if prod_row is not None else None
    producer_terminal = prod_status in ("completed", "failed", "cancelled", "timed_out")
lock_is_stale = lock_is_stale or producer_terminal
```
Keep `locked` derived as-is (server-authoritative), but ensure a stale lock surfaces to the client so the F3 frontend treats it as unlocked. Keep the endpoint a PURE READ (no writes) — the test_thread_workflow_endpoint.py pure-read invariant must still hold.

**Tests** (`backend/tests/test_dual_mode_wiring.py`): add a test driving the harness branch to raise and asserting `finish_run` is invoked once with the workflow run id (terminalize + anchor clear), and a negative test that a Deep run failure does NOT call finish_run from this path. Add a lock_is_stale-with-terminal-producer-run assertion (or extend the existing test_thread_workflow_endpoint.py rule test if that file is cleaner — keep it where the existing lock_is_stale coverage lives).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_dual_mode_wiring.py -k "terminalize or stale or wedged or anchor" -x -q && cd backend && venv/Scripts/python -m pytest tests/test_thread_workflow_endpoint.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "F2 harness-failure terminalize" backend/app/api/threads.py` exits 0
    - `grep -q "lock_is_stale = lock_is_stale or producer_terminal" backend/app/api/threads.py` exits 0
    - `cd backend && venv/Scripts/python -m pytest tests/test_dual_mode_wiring.py -k "terminalize or stale or anchor" -q` exits 0
    - `cd backend && venv/Scripts/python -m pytest tests/test_thread_workflow_endpoint.py -q` exits 0 (pure-read invariant + lock_is_stale heal still pass)
  </acceptance_criteria>
  <done>A harness run that escapes via exception/timeout terminalizes workflow_runs + clears the anchor (no wedged lock); lock_is_stale self-heals when the producer run is terminal; Deep runs are unaffected; the endpoint stays a pure read.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Live-DB harness_audit integration test (closes the mock blind spot that let F1 ship)</name>
  <files>backend/tests/integration/test_092_harness_audit_live.py</files>
  <read_first>
    - backend/tests/integration/test_073_concurrency.py:38-63 (PG_AVAILABLE skipif + pytestmark) + :71-96 (pg_pool fixture) + :99-140 (test_thread_user fixture — seeds auth.users + threads, yields ids, cleans up) — COPY this exact template
    - backend/app/db/workflows.py (post-Task-2: create_workflow_run with user_id, write_audit with user_id)
    - backend/app/services/harness_engine.py:415, 529 (phase_started + run_completed audit writes)
    - 092-04-UAT-FINDINGS.md F1 "Why 091 missed it" (write_audit was only ever exercised against the mock asyncpg pool — this test is the closure)
  </read_first>
  <behavior>
    - Test (live DB, skipif PG unavailable): given a seeded auth.users + threads row, call `create_workflow_run(pool, ..., user_id=<seeded user>)` then `write_audit(pool, run_id, user_id=<seeded user>, event_type="phase_started", metadata={...})` and assert a REAL row exists: `SELECT user_id, event_type FROM harness_audit WHERE run_id = $1` returns the row with the correct user_id and NO NotNullViolationError. This is the test that would have caught F1.
    - Test: a `write_audit` with `user_id=None` (the pre-fix bug condition) raises asyncpg NotNullViolationError against the live table — proving the constraint is real and the fix is load-bearing (assert via pytest.raises).
    - Cleanup: delete the harness_audit + workflow_runs + thread + user rows in FK-safe order (mirror test_thread_user teardown).
  </behavior>
  <action>
Create `backend/tests/integration/test_092_harness_audit_live.py` modeled byte-for-byte on `test_073_concurrency.py`'s live-DB harness:
1. Copy the `PG_AVAILABLE` probe + `pytestmark = pytest.mark.skipif(...)` guard so the test SKIPS cleanly when local Postgres :54322 is unreachable (CI-safe, never a false red).
2. Copy the `pg_pool` (function-scoped `asyncpg.create_pool`) and `test_thread_user` (seeds `auth.users` + `threads`, yields `(user_id, thread_id)`, cleans up) fixtures.
3. `test_harness_audit_row_written_for_created_run`: create a minimal published WorkflowDefinition (or reuse a seed definition_id present from migration 061 — query `SELECT id FROM workflow_definitions WHERE status='published' LIMIT 1`; skip if none), call `create_workflow_run(pg_pool, thread_id=..., definition_id=..., definition=..., inputs={}, model=None, user_id=user_id)`, then `write_audit(pg_pool, run_id, user_id=user_id, event_type="phase_started", metadata={"phase":"p1"})`, then assert `SELECT user_id::text, event_type FROM harness_audit WHERE run_id=$1` returns exactly that row with `user_id == str(user_id)`.
4. `test_write_audit_null_user_id_violates_constraint`: `with pytest.raises(asyncpg.exceptions.NotNullViolationError): await write_audit(pg_pool, run_id, user_id=None, event_type="run_started", metadata={})` — proves the NOT NULL constraint is live and the fix is necessary (the F1 reproduction, now guarded).
5. Teardown deletes harness_audit → workflow_phases → workflow_runs → thread → user in FK-safe order.

Mark clearly in the module docstring: this is the live-pool test that closes the 091 mock-blind-spot; it uses the `pg_pool` live fixture, NOT the `mock_asyncpg_pool` fixture.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/integration/test_092_harness_audit_live.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "FROM harness_audit" backend/tests/integration/test_092_harness_audit_live.py` exits 0
    - `grep -q "NotNullViolationError" backend/tests/integration/test_092_harness_audit_live.py` exits 0
    - `grep -q "pg_pool" backend/tests/integration/test_092_harness_audit_live.py` exits 0 (uses the LIVE pool fixture, not the mock)
    - `cd backend && venv/Scripts/python -m pytest tests/integration/test_092_harness_audit_live.py -q` exits 0 when PG is up (or SKIPS cleanly when PG :54322 is down — never errors)
  </acceptance_criteria>
  <done>A live-DB integration test asserts a real harness_audit row is written for a created run (and that a null user_id violates the live constraint), closing the mock-only blind spot that let F1 ship. Skips cleanly without local Postgres.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /threads/{id}/messages | untrusted kickoff prompt + workflow_definition_id; the route already ownership-checks the thread + RLS-resolves the definition |
| producer → harness_audit / workflow_runs INSERT | service-role asyncpg writes; owner-scoping is enforced UPSTREAM (the route resolved current_user) — the persisted user_id is the now-trusted owner stamp |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-092-05-01 | Tampering | workflow_runs.user_id | mitigate | user_id is sourced from the server-side `current_user["id"]` (auth dependency), never from the request body; FK → auth.users ON DELETE CASCADE keeps it referentially honest |
| T-092-05-02 | Information disclosure | harness_audit cross-user reads | accept | harness_audit is INSERT-only with RLS (HARNESS-06 / migration 059); this plan only adds the owner stamp to the write path — read-side RLS is unchanged and out of scope |
| T-092-05-03 | Denial of service | wedged lock (F2) | mitigate | failure-path terminalize + lock_is_stale self-heal guarantee a crashed run never permanently locks a thread; finish_run is idempotent so double-clear is safe |
| T-092-05-04 | Tampering | SQL injection via metadata/inputs | mitigate | all writes use `$N` placeholders + `json.dumps(...)::jsonb`; no f-string SQL (existing file convention preserved) |
</threat_model>

<verification>
- Quick (per task): the per-task pytest `-k` command exits 0.
- Backend wave gate: `cd backend && venv/Scripts/python -m pytest tests/test_dual_mode_wiring.py tests/test_continue.py tests/test_thread_workflow_endpoint.py tests/integration/test_092_harness_audit_live.py -q` — green (or live tests SKIP cleanly without PG).
- Full-suite no-regression: `cd backend && venv/Scripts/python -m pytest tests/ -q` shows ZERO new failures vs the documented 092-03 baseline (the deterministic unit suite stays at its 54-failed/569-passed baseline; live-infra FK tests stay nondeterministic-but-unchanged — see 092-03-SUMMARY Issues).
- Live F1 proof (operator, after migration applied + backend restarted): kick off a Research→Summarize workflow; the `run:{run_id}` SSE stream no longer errors with NotNullViolationError; phases execute; assistant produces output. `SELECT user_id, event_type FROM harness_audit WHERE run_id='<run>'` returns rows with non-null user_id.
</verification>

<success_criteria>
- F1 closed: a Harness workflow runs end-to-end; no harness_audit NotNullViolationError; harness_audit rows carry the owner user_id; workflow_runs persists user_id.
- F2 closed: a failed/timed-out harness run terminalizes workflow_runs + clears the anchor (no wedged lock); lock_is_stale self-heals on a terminal producer run.
- Live-DB integration test present and green (closes the mock blind spot).
- Deep Mode byte-identical: the changes are additive at the run-creation/audit/failure seam; no provider streaming branch touched; a Deep run (anchor NULL) skips every new code path.
- Full backend suite shows zero new failures vs the 092-03 baseline.
</success_criteria>

<output>
After completion, create `.planning/phases/092-dual-mode-wiring-continue-button/092-05-SUMMARY.md`.
</output>
