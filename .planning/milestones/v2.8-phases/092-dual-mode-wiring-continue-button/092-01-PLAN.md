---
phase: 092-dual-mode-wiring-continue-button
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/063_dual_mode_continue.sql
  - supabase/full-schema.sql
  - backend/tests/test_dual_mode_wiring.py
  - backend/tests/test_continue.py
  - backend/tests/test_thread_workflow_endpoint.py
  - supabase/verify_092.sql
autonomous: false
requirements: [MODE-01, MODE-02, CONT-01]
user_setup: []

must_haves:
  truths:
    - "workflow_runs has inputs, model, continues_used columns (SEED-047 + D-06)"
    - "runs has a continues_used column (Deep-run cap, D-06)"
    - "Both runs.status and workflow_runs.status CHECK constraints accept cap_paused"
    - "Migration 063 is applied to the live DB and full-schema.sql reflects it"
    - "Wave-0 test files exist with it.todo()-style contract anchors for SC#1/SC#2/SC#4/SC#5"
  artifacts:
    - path: "supabase/migrations/063_dual_mode_continue.sql"
      provides: "inputs/model/continues_used columns + cap_paused status on both tables"
      contains: "cap_paused"
    - path: "backend/tests/test_dual_mode_wiring.py"
      provides: "SC#1 branch + SC#2 lock-clear + Q5 create_workflow_run atomicity contracts"
      contains: "create_workflow_run"
    - path: "backend/tests/test_continue.py"
      provides: "SC#4 persist-then-consume + 3-cap refusal contracts"
      contains: "continues_used"
    - path: "backend/tests/test_thread_workflow_endpoint.py"
      provides: "SC#5 ThreadWorkflowState shape + lock_is_stale contracts"
      contains: "lock_is_stale"
    - path: "supabase/verify_092.sql"
      provides: "live-DB gate asserting migration 063 columns + CHECK values"
      contains: "continues_used"
  key_links:
    - from: "supabase/migrations/063_dual_mode_continue.sql"
      to: "live Supabase DB"
      via: "operator paste into SQL editor (NOT db push)"
      pattern: "cap_paused"
    - from: "supabase/full-schema.sql"
      to: "regenerate-full-schema.sh"
      via: "live-DB dump after migration applied"
      pattern: "continues_used"
---

<objective>
Lay the schema + test foundation the entire phase depends on. Migration 063 adds the
columns and status values 092's backend needs (`workflow_runs.inputs`/`model` to close
SEED-047, `continues_used` on both `runs` and `workflow_runs` for the D-06 cap, and the
non-terminal `cap_paused` status on both status CHECK constraints). Also lay down the
Wave-0 pytest scaffolds (RED/contract anchors) that downstream plans flip to live tests,
and the `verify_092.sql` live-DB gate.

Purpose: Every downstream backend task reads these new columns. The migration must be
applied to the live dev DB by the OPERATOR (SQL-editor paste, never `db push`) before any
backend code that reads `inputs`/`model`/`continues_used`/`cap_paused` can be verified.
Output: migration 063, regenerated full-schema.sql, 3 test scaffold files, verify_092.sql.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/092-dual-mode-wiring-continue-button/092-RESEARCH.md
@.planning/phases/092-dual-mode-wiring-continue-button/092-PATTERNS.md

<interfaces>
<!-- Current schema facts the migration must respect (RESEARCH-verified line numbers) -->

runs.status CHECK (full-schema.sql:564):
  ('streaming','completed','failed','cancelled','timed_out')

workflow_runs.status CHECK (full-schema.sql:749):
  ('active','paused','completed','failed','cancelled')

workflow_phases.status CHECK (full-schema.sql:724):
  ('pending','active','completed','failed','skipped')   -- NOT modified by this migration

workflow_runs (full-schema.sql:739-750): has NO inputs, NO model, NO continues_used column today.
runs: has NO continues_used column today.

FK ordering (full-schema.sql:1634 / :1698):
  threads.active_workflow_run_id -> workflow_runs.id  ON DELETE SET NULL
  workflow_runs.thread_id -> threads.id  ON DELETE CASCADE

Migration head is 062 (supabase/migrations/062_workflow_run_claim_lease.sql). Next = 063.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 063_dual_mode_continue.sql</name>
  <read_first>
    - supabase/migrations/062_workflow_run_claim_lease.sql (analog — header doc-block, idempotent ADD COLUMN IF NOT EXISTS, COMMENT ON COLUMN, "apply via SQL editor" note)
    - supabase/full-schema.sql lines ~560-570 (runs_status_check), ~739-750 (workflow_runs), ~720-725 (workflow_phases_status_check — for the constraint-name convention)
    - .planning/phases/092-dual-mode-wiring-continue-button/092-RESEARCH.md "Migration (063 ...)" section
    - .planning/phases/092-dual-mode-wiring-continue-button/092-PATTERNS.md "Migration 063_dual_mode_continue.sql" section
  </read_first>
  <files>supabase/migrations/063_dual_mode_continue.sql</files>
  <action>
Create `supabase/migrations/063_dual_mode_continue.sql` (filename digits-underscore — letter suffixes are silently skipped by the Supabase CLI). Mirror 062's header doc-block convention: a `-- 063_dual_mode_continue.sql` title line, a PROBLEM/FIX narrative (Phase 092 dual-mode wiring + Continue — SEED-047 inputs/model rehydration + D-06 Continue cap + the cap_paused non-terminal pause state), an "Idempotent — safe to paste-replay" note, and "Apply via the Supabase SQL editor (per CLAUDE.md — never `supabase db push`/`db reset`)."

The DDL, in order:

1. Columns on workflow_runs (SEED-047 + D-06):
```sql
ALTER TABLE public.workflow_runs ADD COLUMN IF NOT EXISTS inputs jsonb NOT NULL DEFAULT '{}'::jsonb;
ALTER TABLE public.workflow_runs ADD COLUMN IF NOT EXISTS model text;
ALTER TABLE public.workflow_runs ADD COLUMN IF NOT EXISTS continues_used integer NOT NULL DEFAULT 0;
```

2. Column on runs (D-06, Deep-run cap):
```sql
ALTER TABLE public.runs ADD COLUMN IF NOT EXISTS continues_used integer NOT NULL DEFAULT 0;
```

3. Add `cap_paused` to runs.status CHECK — drop the existing constraint by name then recreate with the new value (preserving the existing 5 values):
```sql
ALTER TABLE public.runs DROP CONSTRAINT IF EXISTS runs_status_check;
ALTER TABLE public.runs ADD CONSTRAINT runs_status_check
  CHECK (status IN ('streaming','cap_paused','completed','failed','cancelled','timed_out'));
```

4. Add `cap_paused` to workflow_runs.status CHECK (preserving the existing 5 values, keeping `paused` distinct — do NOT remove `paused`):
```sql
ALTER TABLE public.workflow_runs DROP CONSTRAINT IF EXISTS workflow_runs_status_check;
ALTER TABLE public.workflow_runs ADD CONSTRAINT workflow_runs_status_check
  CHECK (status IN ('active','paused','cap_paused','completed','failed','cancelled'));
```

IMPORTANT: before writing the DROP CONSTRAINT statements, grep full-schema.sql for the EXACT constraint names (`runs_status_check`, `workflow_runs_status_check`) — if the live names differ (e.g. a table-inline anonymous CHECK), adjust the DROP to match the real constraint name. Add `COMMENT ON COLUMN` lines per 062 for `workflow_runs.inputs` ('SEED-047: kickoff inputs persisted at creation for resume rehydration'), `workflow_runs.model`, and both `continues_used` columns ('D-06: Continue cap counter, max 3/run').

Do NOT add any `supabase db push` or `db reset` to this plan — application is the operator checkpoint (Task 3).
  </action>
  <acceptance_criteria>
    - File `supabase/migrations/063_dual_mode_continue.sql` exists
    - File contains the string `cap_paused` (both CHECK recreations)
    - File contains `ADD COLUMN IF NOT EXISTS inputs jsonb`
    - File contains `ADD COLUMN IF NOT EXISTS model text`
    - File contains `continues_used integer NOT NULL DEFAULT 0` for both `workflow_runs` and `runs`
    - File contains `workflow_runs_status_check` with all of: active, paused, cap_paused, completed, failed, cancelled
    - File contains the comment string "never `supabase db push`"
    - File does NOT contain the string `db push` outside the do-not-use comment, and does NOT contain `db reset`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "cap_paused" "supabase/migrations/063_dual_mode_continue.sql" && grep -q "ADD COLUMN IF NOT EXISTS inputs jsonb" "supabase/migrations/063_dual_mode_continue.sql" && grep -q "continues_used integer NOT NULL DEFAULT 0" "supabase/migrations/063_dual_mode_continue.sql" && echo OK</automated>
  </verify>
  <done>Migration 063 file exists with all 4 columns + cap_paused on both status CHECKs + 062-style header/comments.</done>
</task>

<task type="auto">
  <name>Task 2: Write Wave-0 pytest scaffolds + verify_092.sql gate</name>
  <read_first>
    - backend/tests/test_harness_resume.py and backend/tests/test_harness_engine.py (analog — async pytest with mock_asyncpg_pool / fake_redis fixtures; the `conftest` fixture set: mock_asyncpg_pool UPDATE-recorder, fake_redis, make_tool_context, make_run_context, build_workflow_definition)
    - .planning/phases/092-dual-mode-wiring-continue-button/092-RESEARCH.md "## Validation Architecture" (the SC -> verification map + the Wave 0 Gaps list)
    - .planning/phases/092-dual-mode-wiring-continue-button/092-VALIDATION.md (the per-task map this scaffold feeds)
    - supabase/migrations/060_*.sql or any existing verify_NNN.sql (the 090 verify_090.sql precedent for the live-DB gate shape)
  </read_first>
  <files>backend/tests/test_dual_mode_wiring.py, backend/tests/test_continue.py, backend/tests/test_thread_workflow_endpoint.py, supabase/verify_092.sql</files>
  <action>
Create three pytest scaffold files using the existing `backend/tests/conftest.py` fixtures (mock_asyncpg_pool, fake_redis, make_run_context, build_workflow_definition). Each scaffold defines the contract test names that downstream plans flip from `pytest.mark.skip`/`it.todo`-style stubs to live assertions. Use `@pytest.mark.skip(reason="contract — owned by plan NN")` so the suite stays green now and each downstream plan removes its own skip.

`backend/tests/test_dual_mode_wiring.py` (owned by Plan 02/03):
- `test_create_workflow_run_inserts_run_phases_and_sets_anchor_atomically` — Q5: asserts one workflow_runs INSERT + one workflow_phases INSERT per PhaseSpec + the threads UPDATE, all inside a single transaction, FK-ordered (run INSERT before threads UPDATE). Use a built `WorkflowDefinition` with 3 phases; assert 3 phase-row inserts recorded.
- `test_create_workflow_run_persists_inputs_and_model` — SEED-047: inputs jsonb + model column written.
- `test_producer_branches_harness_when_anchor_set` — SC#1: active_workflow_run_id not None -> run_workflow path; None -> run_agent_loop path (assert via a spy on which branch is taken).
- `test_cancel_clears_anchor_in_same_transaction` — SC#2: after the terminal-status write, active_workflow_run_id is NULL; the clear rides the SAME UPDATE/transaction (assert the recorded statements).

`backend/tests/test_continue.py` (owned by Plan 03):
- `test_cap_persists_dropped_tool_calls_before_clearing_buffer` — SC#4: at the cap, the buffered tool_calls are persisted durably BEFORE `tool_calls_buffer = {}`.
- `test_continue_consumes_persisted_tool_calls_not_redrop` — SC#4: continue re-feeds the exact persisted tool calls (consume, not re-drop).
- `test_continue_refused_when_continues_used_at_3` — D-06: 4th continue refused server-side.
- `test_continue_harness_rereads_available_tools_from_definition` — D-08: the active phase's available_tools is re-read from the parsed definition.
- `test_deep_run_without_cap_drop_is_byte_identical` — the resume_dropped_tool_calls flag OFF leaves Deep Mode unchanged.

`backend/tests/test_thread_workflow_endpoint.py` (owned by Plan 02):
- `test_thread_workflow_state_shape` — SC#5: ThreadWorkflowState returns mode/locked/active_workflow_run_id/run_status/current_phase_*/cap_paused/continues_*.
- `test_lock_is_stale_when_run_terminal_or_absent` — SC#5: lock_is_stale=true when anchor set but run terminal/missing.
- `test_get_workflow_is_pure_read_never_writes` — SC#5: the GET never mutates the anchor.

`supabase/verify_092.sql` — mirror the 090 verify precedent: SELECT statements asserting the migration applied — (a) `information_schema.columns` shows `workflow_runs.inputs`, `workflow_runs.model`, `workflow_runs.continues_used`, `runs.continues_used`; (b) a query proving `cap_paused` is an allowed status on both tables (e.g. insert-rollback probe or pg_get_constraintdef of the two status checks containing 'cap_paused'); (c) a comment block noting it is run by the operator after pasting migration 063.
  </action>
  <acceptance_criteria>
    - All three test files exist and import from `conftest` fixtures
    - `backend/tests/test_dual_mode_wiring.py` contains `create_workflow_run` and `test_cancel_clears_anchor_in_same_transaction`
    - `backend/tests/test_continue.py` contains `continues_used` and `test_continue_refused_when_continues_used_at_3`
    - `backend/tests/test_thread_workflow_endpoint.py` contains `lock_is_stale`
    - `supabase/verify_092.sql` exists and contains `continues_used` and `cap_paused`
    - `cd backend && venv/Scripts/python -m pytest tests/test_dual_mode_wiring.py tests/test_continue.py tests/test_thread_workflow_endpoint.py -q` exits 0 (all skipped, none erroring on collection)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_dual_mode_wiring.py tests/test_continue.py tests/test_thread_workflow_endpoint.py -q</automated>
  </verify>
  <done>Three scaffold files collect cleanly (skipped contracts), verify_092.sql exists with the column + cap_paused assertions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator applies migration 063 + regenerates full-schema.sql</name>
  <action>Operator-gated step: the operator pastes supabase/migrations/063_dual_mode_continue.sql into the Supabase SQL editor (NEVER db push/db reset), runs verify_092.sql to confirm the columns + cap_paused, then runs `bash scripts/regenerate-full-schema.sh` (no reset) and commits the migration + regenerated full-schema.sql. See how-to-verify for the exact steps.</action>
  <what-built>
    Migration `supabase/migrations/063_dual_mode_continue.sql` (Task 1) adds `workflow_runs.inputs`,
    `workflow_runs.model`, `workflow_runs.continues_used`, `runs.continues_used`, and the `cap_paused`
    status value on both `runs` and `workflow_runs` status CHECK constraints. This MUST be applied to the
    live local Supabase DB by the operator (SQL-editor paste) — NEVER `supabase db push`/`db reset` (would
    wipe dev data). Then full-schema.sql is regenerated from the live DB (no reset).
  </what-built>
  <how-to-verify>
    1. Open the Supabase SQL editor for the local project.
    2. Paste the FULL contents of `supabase/migrations/063_dual_mode_continue.sql` and run it. It is
       idempotent (ADD COLUMN IF NOT EXISTS + DROP CONSTRAINT IF EXISTS) — safe to re-run.
    3. Run `supabase/verify_092.sql` in the SQL editor — confirm it reports the 4 new columns present and
       `cap_paused` allowed on both status CHECKs.
    4. From a shell at the repo root: `bash scripts/regenerate-full-schema.sh` (live-DB dump, NO reset).
       Confirm `supabase/full-schema.sql` now contains `continues_used` and `cap_paused`.
    5. Commit both `supabase/migrations/063_dual_mode_continue.sql` and the regenerated
       `supabase/full-schema.sql`.
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms migration 063 applied to live DB (verify_092.sql passes)
    - `supabase/full-schema.sql` contains `continues_used` and `cap_paused` after regeneration
    - grep "continues_used" supabase/full-schema.sql exits 0
  </acceptance_criteria>
  <resume-signal>Type "applied" once migration 063 is live and full-schema.sql is regenerated, or describe any DDL error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live DB | Migration DDL is applied by a trusted operator via the SQL editor; no untrusted input crosses here |
| migration → schema | New columns must default safely for existing rows (NOT NULL columns get DEFAULTs) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-092-01 | Tampering | runs/workflow_runs status CHECK | mitigate | DROP CONSTRAINT IF EXISTS + recreate with the FULL value set (never drop an existing valid status) — a missing value would silently break terminal-status writes on existing rows |
| T-092-02 | Denial of Service | NOT NULL columns on existing rows | mitigate | `inputs` and both `continues_used` get a DEFAULT so the ALTER cannot fail on existing rows; `model` is nullable (older rows have none) |
| T-092-03 | Information Disclosure | migration content | accept | DDL only; no secrets, no PII, applied by operator |
</threat_model>

<verification>
- `grep -q cap_paused supabase/migrations/063_dual_mode_continue.sql`
- `cd backend && venv/Scripts/python -m pytest tests/test_dual_mode_wiring.py tests/test_continue.py tests/test_thread_workflow_endpoint.py -q` exits 0
- After operator checkpoint: `grep -q continues_used supabase/full-schema.sql`
</verification>

<success_criteria>
Migration 063 written, applied to live DB by the operator, full-schema.sql regenerated and committed; the 3 Wave-0 test scaffolds collect cleanly (skipped contracts) and verify_092.sql exists. All downstream backend plans can now read the new columns.
</success_criteria>

<output>
After completion, create `.planning/phases/092-dual-mode-wiring-continue-button/092-01-SUMMARY.md`
</output>
