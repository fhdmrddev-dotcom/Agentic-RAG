---
phase: 091-harness-engine-5-phase-types-gates-whitelist
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/harness.py
  - backend/tests/conftest.py
  - backend/tests/test_harness_engine.py
  - backend/tests/test_harness_resume.py
  - backend/tests/test_harness_gates.py
  - backend/tests/test_harness_whitelist.py
  - backend/tests/test_harness_templates.py
  - backend/tests/test_tool_budget.py
  - backend/tests/test_harness_reachability.py
autonomous: true
requirements: [HARNESS-01, HARNESS-03, HARNESS-04, HARNESS-05, HARNESS-07, TOOL-05]
must_haves:
  truths:
    - "The 5 PhaseConfig models carry every field the engine reads (input_keys, model/temperature overrides, system_prompt_override semantics) — no engine task hits a missing field"
    - "All Wave-0 harness test files exist and run (skips/xfails allowed) so every downstream plan flips its own contracts live"
    - "Shared pytest fixtures (mocked asyncpg UPDATE-order recorder, fake Redis pub/sub+XADD, ToolContext/RunContext factory, WorkflowDefinition builder) are importable by every test_harness_*.py"
  artifacts:
    - path: "backend/app/models/harness.py"
      provides: "Finalized PhaseConfig discriminated union + ValidatorSpec + PhaseSpec + WorkflowDefinition"
      contains: "input_keys"
    - path: "backend/tests/conftest.py"
      provides: "mock_asyncpg_pool, fake_redis, make_tool_context, make_run_context, build_workflow_definition fixtures"
      contains: "def build_workflow_definition"
    - path: "backend/tests/test_harness_engine.py"
      provides: "Wave-0 skeleton for HARNESS-01 phase-dispatch + ordered-transition contracts"
      contains: "test_"
  key_links:
    - from: "backend/tests/conftest.py"
      to: "backend/app/models/harness.py"
      via: "build_workflow_definition imports WorkflowDefinition"
      pattern: "from app.models.harness import"
---

<objective>
Finalize the harness Pydantic config models against the real engine needs (Phase 090 marked the per-field shapes PROVISIONAL) and stand up the complete Wave-0 test scaffold + shared fixtures that every downstream plan in this phase flips to live tests.

Purpose: The engine (Plan 02), executors (Plan 03), resume (Plan 04), gates (Plan 05), whitelist/budget (Plan 06), and seeds (Plan 07) all depend on (a) firm model field names they can read without guessing and (b) a shared fixture layer (mocked asyncpg UPDATE-order recorder, fake Redis, context factories, a WorkflowDefinition builder). This plan is the contracts-first foundation so no later plan goes on a scavenger hunt.

Output: Final `harness.py` models + 8 test files (1 conftest + 7 test_harness_*.py) with skeleton contracts (skipped/xfail or trivially-passing structural asserts) that downstream plans turn green.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-CONTEXT.md
@.planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-RESEARCH.md
@.planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-VALIDATION.md

<interfaces>
<!-- Current harness.py (Phase 090, PROVISIONAL per-field). Discriminator + extra='forbid' + union LOCKED — DO NOT change those. -->
From backend/app/models/harness.py:
```python
class _StrictBase(BaseModel):
    model_config = ConfigDict(extra="forbid")   # LOCKED — keep on every model

class ProgrammaticPhaseConfig(_StrictBase):
    phase_type: Literal["programmatic"]
    fn: str

class LlmSinglePhaseConfig(_StrictBase):
    phase_type: Literal["llm_single"]
    prompt: str

class LlmAgentPhaseConfig(_StrictBase):
    phase_type: Literal["llm_agent"]
    prompt: str
    available_tools: list[str]
    max_steps: int = 10

class LlmBatchAgentsPhaseConfig(_StrictBase):
    phase_type: Literal["llm_batch_agents"]
    prompt: str
    available_tools: list[str]
    max_steps: int = 10
    max_parallel_agents: int = 5
    merge_strategy: str = "concat"

class LlmHumanInputPhaseConfig(_StrictBase):
    phase_type: Literal["llm_human_input"]
    prompt: str

PhaseConfig = Annotated[Union[...5 above...], Field(discriminator="phase_type")]

class ValidatorSpec(_StrictBase):
    kind: Literal["json_schema", "regex_match", "workspace_file_exists", "programmatic"]  # LOCKED
    config: dict = Field(default_factory=dict)
    on_failure: str = "fail_run"   # fail_run | retry | skip_to_phase:<slug>
    max_retries: int = 2

class PhaseSpec(_StrictBase):
    slug: str
    phase_index: int
    config: PhaseConfig
    validators: list[ValidatorSpec] = Field(default_factory=list)

class WorkflowDefinition(_StrictBase):
    slug: str
    version: int
    name: str
    status: Literal["draft", "published"] = "draft"
    phases: list[PhaseSpec]
```
<!-- DB column shapes the engine reads (from migrations 056-059, RESEARCH-verified):
     workflow_phases.status CHECK = pending|active|completed|failed|skipped ; output jsonb ; phase_index ; slug
     workflow_runs.status ; current_phase_id
     harness_audit.event_type text+CHECK (9 kinds): phase_started, phase_completed, phase_transition,
       gate_passed, gate_failed, tool_refused, run_started, run_completed, run_failed
     workflow_definitions: id, slug, version, name, status, definition jsonb, created_by, is_global
     threads.active_workflow_run_id -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finalize the 5 PhaseConfig models + ValidatorSpec against the engine's real reads</name>
  <read_first>
    - backend/app/models/harness.py (the file being modified — the full 96 lines)
    - .planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-RESEARCH.md § Pattern 2 (phase-type → substrate map) + Code Examples (lines 471-520) + Open Questions 1
    - .planning/phases/090-harness-schema-rls-config-models/090-CONTEXT.md (the D-090-07 extra='forbid' + provisional-fields decision)
  </read_first>
  <behavior>
    - Test: `WorkflowDefinition.model_validate({...4 seeds...})` parses each of the 4 seed shapes (Research→Summarize, Plan→Execute→Verify, Literature review batch, Doc Q&A) with zero ValidationError
    - Test: an unknown key inside any phase config still raises ValidationError (extra='forbid' preserved)
    - Test: `ProgrammaticPhaseConfig` accepts `input_keys` mapping; `LlmSinglePhaseConfig`/`LlmAgentPhaseConfig` accept optional `model`/`temperature` overrides; defaults applied when absent
  </behavior>
  <action>
    Refine ONLY the per-field shapes (the discriminator, `extra='forbid'`, the 5 phase_type literals, the 4 validator kind literals, and the Union structure are LOCKED — leave them exactly as-is). Apply these concrete additions, all keeping `_StrictBase`:

    1. `ProgrammaticPhaseConfig`: add `input_keys: list[str] = Field(default_factory=list)` — the keys this fn reads from the accumulated phase-output context (the Literature-review `split_topic` fn reads the run's topic). Keep `fn: str`.
    2. `LlmSinglePhaseConfig`: add `model: str | None = None` and `temperature: float | None = None` (per-phase model/temperature overrides; None = inherit thread settings). Keep `prompt: str`.
    3. `LlmAgentPhaseConfig`: keep `prompt`, `available_tools`, `max_steps: int = 10`. ADD `wall_clock_seconds: int | None = None` (None → engine default, sized in Plan 05). ADD `model: str | None = None`.
    4. `LlmBatchAgentsPhaseConfig`: keep all existing fields. ADD `wall_clock_seconds: int | None = None` and `model: str | None = None`. Constrain `merge_strategy: Literal["concat", "concat_numbered"] = "concat"` (only the strategies Plan 03 implements — `extra='forbid'` already blocks typos; the Literal blocks invalid strategy values).
    5. `LlmHumanInputPhaseConfig`: keep `prompt`. ADD `options: list[str] = Field(default_factory=list)` (the ask_user choice menu — empty = free-text) and `timeout_seconds: int = 300` (per-call default; the 1800s hard cap lives in Settings.ask_user_max_timeout_seconds, enforced in Plan 03).
    6. Add a module docstring line flipping the "PROVISIONAL per-field" note to "FINALIZED in Phase 091 — engine is the consumer". Keep the LOCKED-structure note.

    Do NOT add a `final_output` field anywhere (D-11 — no v1 final_output knob). Do NOT add an `on_failure` enum beyond the existing `str` (Plan 05 parses `skip_to_phase:<slug>` at runtime; keep the field a plain `str` so arbitrary slug suffixes parse).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_harness_engine.py -k models -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "input_keys" backend/app/models/harness.py` matches (ProgrammaticPhaseConfig)
    - `grep -n "wall_clock_seconds" backend/app/models/harness.py` matches at least twice (llm_agent + llm_batch_agents)
    - `grep -n "options" backend/app/models/harness.py` matches (LlmHumanInputPhaseConfig)
    - `grep -c "extra=\"forbid\"" backend/app/models/harness.py` is unchanged from before (≥1, on _StrictBase) — discriminator/union untouched
    - `grep -n "final_output" backend/app/models/harness.py` returns NOTHING (D-11)
    - `cd backend && venv/Scripts/python -c "from app.models.harness import WorkflowDefinition; print('ok')"` prints ok
  </acceptance_criteria>
  <done>The 5 phase configs + ValidatorSpec carry every field the engine, executors, gates, resume, and seeds need; extra='forbid' + discriminator + the two Literal sets are untouched; importing the module succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared test fixtures (conftest) — mocked asyncpg UPDATE-recorder, fake Redis, context factories, WorkflowDefinition builder</name>
  <read_first>
    - backend/tests/conftest.py (the file being extended — read the existing mock_redis / mock_supabase / anyio_backend fixtures so additions compose, do not clobber)
    - .planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-VALIDATION.md § Wave 0 Requirements + § Highest-Risk Surfaces (the deterministic proofs each fixture must enable)
    - .planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-RESEARCH.md § Pattern 3 (the 2-phase write UPDATE sequence the recorder must capture) + § Pattern 9 (the _emit signature the fake Redis must record)
  </read_first>
  <action>
    Extend `backend/tests/conftest.py` (do NOT remove existing fixtures). Add these pytest fixtures, each importable by the test_harness_*.py files:

    1. `mock_asyncpg_pool` — an object whose `.acquire()` async-context yields a connection that records every `execute`/`fetchrow`/`fetch` call as `(sql, args)` tuples on a public `.calls` list, in order. The connection's `execute` returns "UPDATE 1" by default; `fetchrow`/`fetch` return values are settable per-test via `pool.set_fetchrow_result(...)` / `pool.set_fetch_result(...)`. This is the UPDATE-ORDER RECORDER the 2-phase-write proof needs (assert `status='active'` UPDATE index < `status='completed'` UPDATE index).
    2. `fake_redis` — records `xadd(stream, fields)` calls on a public `.xadds` list as `(stream, decoded_fields_dict)`; supports `pubsub()` returning an object with awaitable `subscribe`/`unsubscribe`/`get_message`/`close` plus a test-settable inbound-message queue (`fr.push_message({...})`); records `sadd`/`expire`/`publish` on public lists. Enough to drive both the workflow `_emit` (XADD to `run:{run_id}`) and the ask_user subscribe→sadd→emit→block flow (Plan 04).
    3. `make_tool_context` — a factory returning a minimal `ToolContext` (import from `app.services.tool_dispatcher`) with sensible defaults and a `phase_whitelist` kwarg (default None) so whitelist tests (Plan 06) can pass `make_tool_context(phase_whitelist=frozenset({"search_documents"}))`.
    4. `make_run_context` — a factory for the minimal RunContext/run-identity bag the engine needs (run_id, thread_id, user_id, redis, pool).
    5. `build_workflow_definition` — a builder taking a list of phase dicts and returning a parsed `WorkflowDefinition` (import from `app.models.harness`), so every test composes definitions without hand-writing the full JSONB. Provide a `single_phase(...)` helper and a `four_seed_defs()` helper returning the 4 canonical seed shapes used by Plan 07.

    Keep all fixtures pure-Python (no live Redis/DB). Wrap nothing in real I/O.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/ -k "conftest_smoke or fixtures_importable" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def build_workflow_definition" backend/tests/conftest.py` matches
    - `grep -n "def mock_asyncpg_pool" backend/tests/conftest.py` matches
    - `grep -n "def fake_redis" backend/tests/conftest.py` matches
    - `grep -n "def make_tool_context" backend/tests/conftest.py` matches
    - `grep -n "phase_whitelist" backend/tests/conftest.py` matches (the kwarg passthrough)
    - existing `mock_redis`/`mock_supabase`/`anyio_backend` fixtures still present (grep each name still matches)
    - `cd backend && venv/Scripts/python -m pytest tests/ --collect-only -q` exits 0 (no import errors)
  </acceptance_criteria>
  <done>conftest exposes the 5 new fixtures, preserves the 3 existing ones, and the full suite collects without import errors.</done>
</task>

<task type="auto">
  <name>Task 3: Create the 7 Wave-0 harness test files as skeleton contracts mapped to every requirement</name>
  <read_first>
    - .planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-VALIDATION.md § Per-Task Verification Map + § Phase Requirements → Test Map + § Wave 0 Requirements (the exact file list + behaviors)
    - .planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-RESEARCH.md § Validation Architecture lines 588-641 (the per-req behaviors + deterministic proofs)
    - backend/tests/conftest.py (the fixtures created in Task 2 — reference them in the skeletons)
  </read_first>
  <action>
    Create 7 test files under `backend/tests/`, each a real pytest module that COLLECTS and RUNS green at Wave 0 by using `pytest.mark.skip(reason="Wave 0 contract — flipped live by Plan NN")` on the not-yet-implemented behaviors, and at least one trivially-passing structural assert per file so the file is non-empty and runs. Each `skip` must name the owning plan. Map exactly:

    - `tests/test_harness_engine.py` — HARNESS-01: a `test_models_*` group (LIVE now — Task 1 models), plus skipped contracts `test_phase_dispatch_routes_each_of_5_types`, `test_engine_drives_ordered_transitions`, `test_completion_final_phase_output_is_chat_message` (owned by Plan 02/03).
    - `tests/test_harness_resume.py` — HARNESS-03: skipped `test_two_phase_write_active_before_completed`, `test_crash_leaves_phase_active_not_completed`, `test_sweep_reruns_active_phase`, `test_ask_user_answered_not_reasked`, `test_ask_user_pending_resubscribes_and_reemits` (owned by Plan 02/04).
    - `tests/test_harness_gates.py` — HARNESS-04: skipped `test_each_of_4_validator_kinds_pass_and_fail`, `test_bounded_retry_reaches_failed_after_3_attempts`, `test_consecutive_identical_short_circuits`, `test_retry_feeds_error_into_prompt`, `test_skip_to_phase_routing`, `test_caps_step_and_wall_clock`, `test_fail_run_keeps_partial_outputs` (owned by Plan 05).
    - `tests/test_harness_whitelist.py` — HARNESS-05: skipped `test_out_of_whitelist_refused_clean_tool_result`, `test_refusal_carries_matching_tool_call_id`, `test_deep_mode_none_is_noop`, `test_refusal_audited` (owned by Plan 06).
    - `tests/test_tool_budget.py` — TOOL-05: skipped `test_budget_caps_at_max_tools`, `test_whitelist_tools_always_retained`, `test_no_cap_when_max_tools_absent` (owned by Plan 06).
    - `tests/test_harness_reachability.py` — HARNESS-07 safety: skipped `test_lint_flags_orphan_phase`, `test_lint_flags_unsatisfiable_skip_to_phase`, `test_lint_flags_missing_terminal`, `test_4_seeds_lint_clean` (owned by Plan 02/07).
    - `tests/test_harness_templates.py` — HARNESS-07: skipped `test_each_seed_parses_via_model_validate`, `test_seeds_cover_all_5_phase_types`, `test_each_seed_runs_end_to_end_mocked_llm` (owned by Plan 07).

    Every test file imports from `app.models.harness` and uses the Task-2 fixtures by parameter name so the contracts are real call shapes, not placeholders.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_harness_engine.py tests/test_harness_resume.py tests/test_harness_gates.py tests/test_harness_whitelist.py tests/test_tool_budget.py tests/test_harness_reachability.py tests/test_harness_templates.py -q</automated>
  </verify>
  <acceptance_criteria>
    - all 7 files exist: `ls backend/tests/test_harness_engine.py backend/tests/test_harness_resume.py backend/tests/test_harness_gates.py backend/tests/test_harness_whitelist.py backend/tests/test_tool_budget.py backend/tests/test_harness_reachability.py backend/tests/test_harness_templates.py` lists 7 files
    - `grep -rc "Wave 0 contract" backend/tests/test_harness_*.py` shows skips in each file
    - `grep -rn "from app.models.harness import" backend/tests/test_harness_*.py` matches in all 7 files
    - the pytest command above exits 0 (skips count as pass; the Task-1 model tests run green)
  </acceptance_criteria>
  <done>7 mapped test files collect and run green (Task-1 model tests live, the rest skipped with named owning plans); every requirement ID has at least one declared contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow author → engine | A `workflow_definitions.definition` JSONB authored by a user (or seed) crosses into the engine via `model_validate`. Untrusted shape. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-091-01 | Tampering | `harness.py` config parse | mitigate | `extra='forbid'` on `_StrictBase` rejects typo'd/injected keys before the engine consumes them (T-090-01 carried forward). Constrain `merge_strategy` to a Literal so only implemented strategies parse. |
| T-091-02 | Tampering | `merge_strategy` / `on_failure` free strings | mitigate | `merge_strategy` is a Literal (closed set); `on_failure` stays `str` but Plan 05 parses only `fail_run|retry|skip_to_phase:<slug>` and routes unknown values to `fail_run` (fail-safe). |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/ --collect-only -q` exits 0 (no import errors across the whole suite after the new files).
- `cd backend && venv/Scripts/python -c "from app.models.harness import WorkflowDefinition, ProgrammaticPhaseConfig, LlmHumanInputPhaseConfig; print('ok')"` prints ok.
</verification>

<success_criteria>
- harness.py models carry every engine-read field (input_keys, model/temperature overrides, wall_clock_seconds, ask_user options/timeout) with the LOCKED structure intact.
- conftest exposes the 5 shared fixtures + preserves the 3 existing.
- 7 Wave-0 test files exist, collect, and run green; every phase requirement ID is represented by a declared contract a later plan flips live.
</success_criteria>

<output>
After completion, create `.planning/phases/091-harness-engine-5-phase-types-gates-whitelist/091-01-SUMMARY.md`.
</output>
