---
phase: 089-agent-loop-extraction-g-5-kickoff-uat
plan: 04
type: execute
wave: 3
depends_on: ["089-02", "089-03"]
files_modified:
  - .planning/phases/089-agent-loop-extraction-g-5-kickoff-uat/089-VERIFICATION.md
  - .planning/reported-bugs/title-generation-broken-deepseek-moonshot-google.md
  - .planning/reported-bugs/sub-agent-cross-provider-model-default-404.md
  - .planning/reported-bugs/final-outputs-pinned-panel-no-download-link.md
autonomous: false
requirements: [CF-01]

must_haves:
  truths:
    - "After the lift, the SSE event sequence is byte-identical per native-7 provider (operator-run capture, empty diff) and the eval scoreboard is GREEN before AND after"
    - "VERIFICATION.md enumerates all 14 per-provider invariants (I1-I14) with file:line carried-forward verbatim (SC#2 named checklist)"
    - "CF-01 item C1 (title-gen native-7), C2 (Google secondary-model 404), C3 (download-link) each end verified-closed OR re-opened with a concrete re_open_trigger routed to a named later slot"
    - "ZERO feature fixes inside 089 — any still-broken item is re-opened + deferred, never fixed here"
    - "The 4-axis UAT (U1-U5 in 089-VALIDATION.md) is exercised in BOTH General AND Explorer mode via Chrome MCP + operator eval"
  artifacts:
    - path: ".planning/phases/089-agent-loop-extraction-g-5-kickoff-uat/089-VERIFICATION.md"
      provides: "The I1-I14 named invariant checklist + the before/after SSE-diff + eval results + CF-01 dispositions + 4-axis UAT results"
      contains: "I1"
  key_links:
    - from: "089-VERIFICATION.md"
      to: ".planning/reported-bugs/*.md"
      via: "CF-01 dispositions update bug frontmatter (status + re_open_trigger or verified_closed_by)"
      pattern: "re_open_trigger|verified_closed_by"
---

<objective>
Close the phase: prove byte-identical native-7 SSE AFTER the lift (operator runs the Plan 02
runbook's AFTER half + the eval before/after), enumerate the I1-I14 invariants in VERIFICATION.md
(SC#2 named checklist), run the 4-axis kickoff UAT (U1-U5) in BOTH modes via Chrome MCP, and
disposition the three CF-01 carry-forwards (C1/C2/C3) — each verified-closed or re-opened with a
concrete re_open_trigger routed to a named later slot.

Purpose: CF-01 + FOUND-03 SC#2/SC#3/SC#4/SC#5. The proof bar is byte-identical SSE per provider,
NOT "tests pass". The CF-01 sweep is hybrid-driven (D-089-11: operator runs the live-key eval,
Claude drives Chrome MCP) and ZERO-feature-fix (D-089-12: still-broken → re-open + defer, never fix).

Output: a complete VERIFICATION.md + dispositioned bug reports. No code change to the loop.

GATE: autonomous=false — the operator runs uvicorn in a visible terminal (never run_in_background),
runs the live-key eval + SSE-capture AFTER half, and pastes results. Claude drives the Chrome MCP
browser checks + records dispositions.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/089-agent-loop-extraction-g-5-kickoff-uat/089-CONTEXT.md
@.planning/phases/089-agent-loop-extraction-g-5-kickoff-uat/089-RESEARCH.md
@.planning/phases/089-agent-loop-extraction-g-5-kickoff-uat/089-VALIDATION.md
@scripts/SSE_DIFF_RUNBOOK.md
@scripts/capture_run_events.py

<interfaces>
<!-- The I1-I14 named checklist (089-RESEARCH.md L476-491) + the CF-01 per-item checks
     (089-RESEARCH.md §"CF-01 Carry-Forward Sweep" L349-372) + UAT rows (089-VALIDATION.md L74-92). -->
<!-- Claude reads these directly; no codebase exploration needed for the disposition surfaces. -->

CF-01 item checks (089-RESEARCH.md L349-372):
- C1 title-gen native-7: per provider, send one message → assert threads.title != "New Chat" AND multi-word.
  DB cross-check: SELECT title FROM threads WHERE id=... . Known risk: auto-memory says still-broken on
  DeepSeek/Moonshot (A1). Disposition if broken → re-open → 093 vicinity / standalone quick (D-089-12).
- C2 Google secondary-model 404: eval --provider google cell → assert run completes, no 404 in logs,
  secondary-model resolves to a Google-family model (not gpt-4.1). Disposition if reproduces → 093 vicinity.
- C3 download-link: research says ALREADY FIXED (L2753-2762 projects {filename,url,size}). Chrome MCP:
  run primes-matplotlib recipe → assert OutputFileCard shows a clickable download link + size (not
  opacity-40 fallback). Disposition: verify-closed if working; else SEED-037 /gsd:quick (D-089-14).

UAT rows U1-U5 (089-VALIDATION.md L74-81) — exercise, do NOT re-author:
  U1 cross-provider × multi-tool (General, native-7) · U2 same in Explorer · U3 parallel-thread
  · U4 long-message (≥50 msgs / ≥5 KB; ask_user thread loads w/o 500) · U5 Explorer preservation.

Chrome MCP: http://localhost:5173/ , login fhdmrd@gmail.com / 123456 (reference_local_dev_app).
Operator runs uvicorn + the live-key eval (D-089-11); Claude drives the browser-observable rows.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author VERIFICATION.md with the I1-I14 named invariant checklist (SC#2)</name>
  <read_first>
    - 089-RESEARCH.md §"Per-Provider Invariant Inventory" (L476-494) — the I1-I14 table with file:line + one-line excerpt + provider branch, plus the GLM/MiniMax explicit-absence note.
    - 089-RESEARCH.md §"Explorer-branch preservation" (L496-505) — the Explorer-specific values (prompt, 6-KB tools, max_iterations=8) for SC#4.
    - backend/app/services/agent_loop.py (post-move) — to confirm each I1-I14 anchor now lives in agent_loop.py and cite the NEW line numbers.
  </read_first>
  <files>.planning/phases/089-agent-loop-extraction-g-5-kickoff-uat/089-VERIFICATION.md</files>
  <action>
    Create `089-VERIFICATION.md`. Section 1 = the SC#2 NAMED per-provider invariant checklist: copy the I1-I14 table (089-RESEARCH.md L476-491) and for EACH invariant, confirm it survives verbatim in `agent_loop.py` and record its NEW file:line (grep agent_loop.py for each anchor: `end_turn`, `thought_signature`, `reasoning_content`, the moonshot/deepseek `<think>` branch, `_empty_retries`, `force_no_tools`, `iteration_cap_dropped_tool_calls`, the transient-retry/429/length-recovery branches, the OpenRouter XML pre-inject). Mark each ✅ carried-verbatim or ❌ (a ❌ is a phase blocker — the move changed behavior).
    Include the GLM/MiniMax explicit-absence note (089-RESEARCH L493-494): ZERO provider-specific branches; they route through `_on_chunk_openai`; their invariants = the shared OpenAI-compat I4-I14 as applicable; no extra carry-forward.
    Section 2 = Explorer-branch preservation (SC#4): confirm the Explorer prompt / 6-KB toolset / max_iterations=8 / skills-memory-skip survive verbatim (089-RESEARCH L496-505), citing agent_loop.py (or threads.py if B2) line numbers.
    Leave placeholder sub-sections for the operator-run results filled in Tasks 2-4 (SSE-diff, eval, UAT, CF-01 dispositions).
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -c "import pathlib,sys,re; t=pathlib.Path('.planning/phases/089-agent-loop-extraction-g-5-kickoff-uat/089-VERIFICATION.md').read_text(encoding='utf-8'); ids=['I'+str(n) for n in range(1,15)]; missing=[i for i in ids if i not in t]; assert not missing, f'missing invariants: {missing}'; assert 'agent_loop.py' in t and ('GLM' in t or 'zhipu' in t) and 'Explorer' in t; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `089-VERIFICATION.md` enumerates all 14 invariants `I1`..`I14` (grep finds each literal token).
    - Each invariant row cites its NEW location in `agent_loop.py` (or notes it STAYS in threads.py for I10) and a ✅/❌ carried-verbatim mark.
    - The GLM/MiniMax explicit-absence note is present (zero provider-specific branches; shared OpenAI-compat path).
    - The Explorer-branch preservation section is present (prompt / 6-KB tools / max_iterations=8 / skills-memory-skip).
    - The `<automated>` check exits 0.
  </acceptance_criteria>
  <done>VERIFICATION.md carries the SC#2 I1-I14 named checklist (each ✅ carried-verbatim with new line cites), the GLM/MiniMax absence note, and the Explorer-preservation section.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator runs the AFTER SSE-diff capture + the eval (before AND after) — paste results</name>
  <what-built>
    The verbatim move (Plan 03) is complete and the full pytest suite is GREEN. The native-7 eval
    gate + the SSE capture helper + the before/after runbook (Plan 02) are ready, and the BEFORE
    baseline was captured against the pre-move loop in Wave 1.
  </what-built>
  <how-to-verify>
    Operator (uvicorn started in a visible terminal — NEVER run_in_background; local Supabase + Redis up):
    1. Run `scripts/SSE_DIFF_RUNBOOK.md`'s AFTER procedure: re-run the pinned representative multi-tool
       prompt per native-7 provider against the EXTRACTED loop, capture via `capture_run_events`, and
       assert `diff_event_streams(before, after) == []` per provider. Empty diff per native-7 = SC#3 PASS.
       (OpenRouter best-effort — log but not a blocker; Ollama opportunistic.)
    2. Run `python scripts/eval_cross_provider.py` and record the `EVAL_SUMMARY` scoreboard — confirm it
       is GREEN (native-7 all-pass) and matches the BEFORE eval run (GREEN before AND after, D-089-08 b).
    3. Confirm the 075.4 Playwright E2E backstop is GREEN (`frontend-tests.yml` scenarios).
    Paste back: the per-provider empty-diff confirmation, the EVAL_SUMMARY before+after, and the E2E result.
  </how-to-verify>
  <resume-signal>Paste the SSE-diff results (empty per native-7?), the EVAL_SUMMARY before+after, and the E2E status. Type "byte-identical confirmed" if all native-7 diffs are empty + eval GREEN both runs, OR report which provider diverged (that BLOCKS — the move changed behavior and must be corrected before the phase closes).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Drive the 4-axis UAT (U1-U5) in BOTH modes via Chrome MCP + record results in VERIFICATION.md</name>
  <read_first>
    - 089-VALIDATION.md §"4-Axis Kickoff UAT" (L70-81) — U1-U5 scenarios + pass conditions (authored there; exercise, do NOT re-author).
    - 089-RESEARCH.md §"Explorer-branch preservation" (L496-505) — what "byte-identical Explorer" means for U2/U5.
    - STATE.md §"Phase 086 surface note" — the ask_user-history thread must load without 500 (U4 checks the .neq filter intact).
    - reference_local_dev_app — http://localhost:5173/, login fhdmrd@gmail.com / 123456.
  </read_first>
  <files>.planning/phases/089-agent-loop-extraction-g-5-kickoff-uat/089-VERIFICATION.md</files>
  <action>
    Using Chrome DevTools MCP against `http://localhost:5173/` (login fhdmrd@gmail.com / 123456), exercise the U1-U5 rows from 089-VALIDATION.md (operator backend running). For browser-observable axes Claude drives directly; for the live-key multi-provider rows, coordinate with the operator's eval results from Task 2:
    - U1 (cross-provider × multi-tool, General): the pinned multi-tool prompt on each native-7 provider completes the multi-tool round; SSE matches the pre-lift capture (ties to Task 2's empty diff).
    - U2 (Explorer multi-tool): same prompt on a representative provider in Explorer mode — confirm 6-KB toolset + max_iterations=8 unchanged.
    - U3 (parallel-thread): Thread A streaming a long run while Thread B accepts + starts a new prompt — confirm independent streaming, no cross-thread bleed, no global isStreaming lockout.
    - U4 (long-message): a ≥50-message thread OR a ≥5 KB single prompt — confirm history reconstruction + trim unchanged AND an ask_user-history thread loads without 500 (086 filter intact).
    - U5 (Explorer preservation): load a thread that exercised Explorer pre-lift; replay — byte-identical Explorer prompt/toolset/cap behavior.
    Record each row PASS/FAIL with a one-line observation + a screenshot reference in 089-VERIFICATION.md (a new "4-Axis UAT Results" section). Any behavior change vs pre-lift = a blocker (the move was not byte-identical).
    Take screenshots into `screenshots/` for the felt-experience evidence (exhaustive UI-state sweep — both modes, parallel threads).
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -c "import pathlib,sys; t=pathlib.Path('.planning/phases/089-agent-loop-extraction-g-5-kickoff-uat/089-VERIFICATION.md').read_text(encoding='utf-8'); need=['U1','U2','U3','U4','U5','Explorer','parallel']; sys.exit(0 if all(k in t for k in need) else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - 089-VERIFICATION.md has a "4-Axis UAT Results" section recording U1-U5 each with PASS/FAIL + a one-line observation.
    - U2 + U5 confirm Explorer mode (6-KB tools, max_iterations=8, dedicated prompt) is byte-identical to pre-lift.
    - U4 confirms an ask_user-history thread loads without 500 (the 086 `.neq("role","system")` filter is intact).
    - U3 confirms no global isStreaming lockout / no cross-thread bleed.
    - Screenshot references recorded; the `<automated>` check exits 0.
  </acceptance_criteria>
  <done>The 4-axis UAT is exercised in BOTH General and Explorer modes; all rows recorded PASS (or a blocker raised if any behavior changed); Explorer preservation + 086 filter + parallel-thread independence confirmed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: CF-01 hybrid sweep — disposition C1/C2/C3 (verified-closed or re-open+defer); ZERO feature fixes</name>
  <what-built>
    The lift is proven byte-identical (Task 2) and the 4-axis UAT passed (Task 3). The CF-01
    verification surfaces are ready: C1 title-gen (Chrome MCP + operator eval), C2 Google-404
    (operator eval), C3 download-link (Chrome MCP).
  </what-built>
  <how-to-verify>
    Hybrid (D-089-11): Claude drives the Chrome MCP browser-observable checks; the operator runs the
    live-key eval cells and pastes results. For EACH item, record verified-closed OR re-opened.
    1. **C1 title-gen on native-7** (D-089-13 — all 7, not just DeepSeek/Moonshot/Google): create a new
       chat per native-7 provider, send one message, assert the sidebar title changes from "New Chat" to a
       multi-word title within ~2s; DB cross-check `SELECT title FROM threads WHERE id=...`. Known risk:
       auto-memory flags this STILL-BROKEN on DeepSeek/Moonshot (sends wrong model name).
    2. **C2 Google secondary-model 404** (operator eval): run the eval `--provider google` cell; assert
       the run reaches `completed`, no 404 in backend logs, secondary-model resolves to a Google-family
       model (not gpt-4.1).
    3. **C3 download-link** (Chrome MCP): run the primes-matplotlib recipe on Anthropic; assert the pinned
       Final-outputs `OutputFileCard` shows a clickable download link + size badge (not the opacity-40
       url-absent fallback). Research says already-fixed in code (L2753-2762).
    DISPOSITION (D-089-12 — ZERO feature fixes in 089): for any STILL-BROKEN item, re-open with a concrete
    `re_open_trigger` routed to a named slot — title-gen / Google-404 → 093 vicinity or a standalone quick;
    download-link → SEED-037 `/gsd:quick` (D-089-14: run the quick right then ONLY if it proves the
    SEED-037 in-panel wire-up gap; otherwise leave SEED-037 as a later quick). Do NOT fix anything in 089.
  </how-to-verify>
  <resume-signal>For each of C1/C2/C3, state "verified-closed" OR "re-open → [named slot] with re_open_trigger: [concrete trigger]". Confirm ZERO feature fixes were made inside 089. Type "dispositions recorded" once C1/C2/C3 each have a verdict and the bug-report frontmatter is updated.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Record CF-01 dispositions in VERIFICATION.md + update bug-report frontmatter</name>
  <read_first>
    - .planning/reported-bugs/TEMPLATE.md — the frontmatter shape (status, re_open_trigger, folded_into, verified_closed_by).
    - .planning/reported-bugs/title-generation-broken-deepseek-moonshot-google.md, sub-agent-cross-provider-model-default-404.md, final-outputs-pinned-panel-no-download-link.md — the three CF-01 bug reports to disposition.
    - 089-VALIDATION.md §"CF-01 Carry-Forward Dispositions" (L84-92) — the C1/C2/C3 disposition rows to fill.
  </read_first>
  <files>.planning/phases/089-agent-loop-extraction-g-5-kickoff-uat/089-VERIFICATION.md, .planning/reported-bugs/title-generation-broken-deepseek-moonshot-google.md, .planning/reported-bugs/sub-agent-cross-provider-model-default-404.md, .planning/reported-bugs/final-outputs-pinned-panel-no-download-link.md</files>
  <action>
    Persist the Task 4 verdicts:
    1. In 089-VERIFICATION.md, add a "CF-01 Dispositions" section filling the C1/C2/C3 rows from
       089-VALIDATION.md L88-92 with the actual verdict (verified-closed OR re-open → named slot + the
       concrete re_open_trigger).
    2. Update each bug report's frontmatter accordingly (using the TEMPLATE.md fields):
       - If verified-closed: set `status: closed` + `verified_closed_by: 089`.
       - If still-broken: set `status: deferred` (or keep `open`) + a concrete `re_open_trigger` + the
         routed slot (`folded_into:` the named later phase/quick — title-gen/Google-404 → 093 vicinity or
         a quick; download-link → SEED-037 quick). Do NOT invent a fix — disposition only (D-089-12).
    3. If C3 proves the SEED-037 in-panel wire-up gap AND the operator chose to run the quick now (D-089-14),
       note that decision; otherwise leave SEED-037 as a separate later quick.
    NO loop/feature code is touched in this task — it is verification bookkeeping only.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -c "import pathlib,sys; v=pathlib.Path('.planning/phases/089-agent-loop-extraction-g-5-kickoff-uat/089-VERIFICATION.md').read_text(encoding='utf-8'); assert 'CF-01' in v and 'C1' in v and 'C2' in v and 'C3' in v; import glob; reports=['title-generation-broken-deepseek-moonshot-google','sub-agent-cross-provider-model-default-404','final-outputs-pinned-panel-no-download-link']; ok=all(any(k in pathlib.Path(f'.planning/reported-bugs/{r}.md').read_text(encoding='utf-8') for k in ('verified_closed_by','re_open_trigger','status:')) for r in reports); sys.exit(0 if ok else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - 089-VERIFICATION.md "CF-01 Dispositions" section records C1/C2/C3 each with a verdict (verified-closed or re-open → named slot + re_open_trigger).
    - Each of the three bug reports' frontmatter is updated: `verified_closed_by: 089` (if closed) OR `status` + `re_open_trigger` + routed slot (if re-opened).
    - No loop/feature code file appears in this task's diff (verification bookkeeping only — D-089-12 zero-fix).
    - The `<automated>` check exits 0.
  </acceptance_criteria>
  <done>CF-01 C1/C2/C3 dispositions are recorded in VERIFICATION.md and reflected in the bug-report frontmatter; zero feature fixes made in 089; any still-broken item routed to a named later slot with a concrete re_open_trigger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval driver → live provider APIs | Operator-run with live keys; presence-only secret reporting |
| Chrome MCP browser session → app | Authenticated as the local test user; reads only that user's threads (RLS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-089-13 | Information disclosure | eval output / pasted results | mitigate | The eval prints provider-key presence (set/MISSING) only, never values; the operator pastes EVAL_SUMMARY + diff confirmations, not secrets (089-RESEARCH §Security; secret-hygiene rule). |
| T-089-14 | Elevation of privilege | Chrome MCP cross-user read | accept | The browser session authenticates as the local test user; RLS limits reads to that user's own threads (089-RESEARCH §Security V4). No cross-user surface. |
| T-089-15 | Tampering | CF-01 "while-I'm-here" fix re-opens the cascade | mitigate | D-089-12 zero-feature-fix discipline enforced: still-broken items are re-opened + deferred, NEVER fixed in 089 (the two deferred Anthropic agent-loop bugs stay Phase 093). Acceptance: no loop/feature code in Task 5 diff. |

Controls preserved verbatim: presence-only secret reporting in the eval; RLS on every browser/eval read. This plan touches NO loop/feature code — it is verification + bookkeeping only.
</threat_model>

<verification>
- Operator confirms byte-identical native-7 SSE (empty diff per provider) + eval GREEN before AND after (Task 2).
- VERIFICATION.md enumerates I1-I14 (SC#2) + Explorer preservation (SC#4).
- 4-axis UAT U1-U5 exercised in BOTH modes, all PASS (SC#4/SC#5) — or a blocker raised.
- CF-01 C1/C2/C3 each dispositioned (verified-closed or re-open+defer); zero feature fixes (D-089-12).
- Bug-report frontmatter updated to reflect dispositions.
</verification>

<success_criteria>
- Byte-identical SSE per native-7 provider proven (operator-run) + eval/E2E GREEN before AND after (SC#3).
- I1-I14 named in VERIFICATION.md, all carried verbatim (SC#2).
- 4-axis UAT passes in BOTH General and Explorer modes (SC#4/SC#5).
- CF-01 swept + dispositioned with zero feature fixes (CF-01 / SC#5).
</success_criteria>

<output>
After completion, create `.planning/phases/089-agent-loop-extraction-g-5-kickoff-uat/089-04-SUMMARY.md`
</output>
