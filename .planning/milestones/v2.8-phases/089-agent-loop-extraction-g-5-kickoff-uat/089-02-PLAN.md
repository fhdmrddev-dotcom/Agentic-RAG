---
phase: 089-agent-loop-extraction-g-5-kickoff-uat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/eval_cross_provider.py
  - scripts/capture_run_events.py
  - scripts/SSE_DIFF_RUNBOOK.md
autonomous: true
requirements: [FOUND-03]

must_haves:
  truths:
    - "scripts/eval_cross_provider.py PROVIDERS list includes (\"zhipu\", ...) and (\"minimax\", ...) — native-7 gate"
    - "scripts/eval_cross_provider.py report_env_presence() checks ZHIPU_API_KEY and MINIMAX_API_KEY (presence only, never values)"
    - "The --provider argparse choices auto-derive zhipu + minimax (NOT hand-edited)"
    - "scripts/capture_run_events.py provides capture_run_events(redis, run_id) via XRANGE run:{run_id} - + and normalize() masking run_id/message_id/timestamps"
    - "scripts/SSE_DIFF_RUNBOOK.md gives the operator the before/after per-native-7-provider SSE capture + empty-diff procedure"
  artifacts:
    - path: "scripts/eval_cross_provider.py"
      provides: "Native-7 eval gate (6 → 7 native providers + OpenRouter best-effort)"
      contains: "(\"zhipu\","
    - path: "scripts/capture_run_events.py"
      provides: "SSE event-sequence capture + normalize for the byte-identical SC#3 proof"
      contains: "def capture_run_events"
    - path: "scripts/SSE_DIFF_RUNBOOK.md"
      provides: "Operator runbook: capture BEFORE the lift, capture AFTER, assert empty diff per provider"
      contains: "XRANGE"
  key_links:
    - from: "scripts/SSE_DIFF_RUNBOOK.md"
      to: "scripts/capture_run_events.py"
      via: "runbook invokes capture_run_events after wait_for_run"
      pattern: "capture_run_events"
---

<objective>
Build the ADDITIVE proof harness for SC#3 (byte-identical native-7 SSE) WITHOUT
touching the agent loop: extend `eval_cross_provider.py` to the native-7 (add zhipu/GLM +
minimax), author the `capture_run_events`/`normalize` SSE-capture helper, and write the
operator runbook that captures the per-provider SSE sequence BEFORE the lift (this plan
runs in Wave 1, so the "before" baseline is captured against the pre-move loop).

Purpose: D-089-08 + D-089-09. The byte-identical proof needs (a) the SSE-diff capture and
(b) the eval gate extended to all 7 native providers. This is additive proof-harness work,
NOT agent-loop edits — it does not violate extraction-purity (D-089-09) and it gives the
verbatim move (Plan 03) and CF-01 sweep (Plan 04) a complete native-7 regression gate.

Output: native-7 eval gate + SSE capture helper + before/after runbook. Zero loop edits.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/089-agent-loop-extraction-g-5-kickoff-uat/089-CONTEXT.md
@.planning/phases/089-agent-loop-extraction-g-5-kickoff-uat/089-RESEARCH.md
@.planning/phases/089-agent-loop-extraction-g-5-kickoff-uat/089-PATTERNS.md
@.planning/phases/089-agent-loop-extraction-g-5-kickoff-uat/089-VALIDATION.md

<interfaces>
<!-- Exact eval edits + the SSE capture/normalize shape. From 089-PATTERNS.md + 089-RESEARCH.md. -->
<!-- Model IDs are RESEARCH-verified against config.py MODEL_CAPABILITIES. -->

eval PROVIDERS list — append two rows (089-PATTERNS.md L122-134, IDs verified 089-RESEARCH.md L376-389):
```python
PROVIDERS: list[tuple[str, str]] = [
    ("openai", "gpt-5.4-mini"),
    ("anthropic", "claude-haiku-4-5"),
    ("google", "gemini-3.5-flash"),
    ("openrouter", "z-ai/glm-5.1"),
    ("deepseek", "deepseek-v4-flash"),
    ("moonshot", "kimi-k2.6"),
    # --- ADD (Phase 089 D-089-09 — native-7 baseline; _PROVIDER_BASE_URLS source of truth;
    #     provider-class representatives, full pinning pass is Phase 096 EVAL-01) ---
    ("zhipu", "glm-4-flash"),            # GLM — _SUB_AGENT_MODEL_DEFAULTS["zhipu"] (config.py:555)
    ("minimax", "minimax-m2.7"),         # _SUB_AGENT_MODEL_DEFAULTS["minimax"] (config.py:554)
]
```

report_env_presence() checks list — append two (089-PATTERNS.md L139-148):
```python
    checks = [
        ...
        ("OPENROUTER_API_KEY", True),
        ("ZHIPU_API_KEY", True),       # ADD — Phase 089 D-089-09 (config.py:580, .env.example:100)
        ("MINIMAX_API_KEY", True),     # ADD — Phase 089 D-089-09 (config.py:581, .env.example:101)
    ]
```

SSE capture + normalize (089-RESEARCH.md L283-305):
```python
async def capture_run_events(redis, run_id: str) -> list[dict]:
    entries = await redis.xrange(f"run:{run_id}", "-", "+")
    events = []
    for _entry_id, fields in entries:
        events.append(json.loads(fields["data"]))   # _emit stores {"data": json.dumps({...})}
    return events

def normalize(events: list[dict]) -> list[dict]:
    out = []
    for e in events:
        e = dict(e)
        e.pop("captured_at", None)
        for k in ("message_id", "run_id"):
            if k in e: e[k] = "<masked>"
        out.append(e)
    return out
# Assert: normalize(before) == normalize(after) per provider → empty diff = SC#3 PASS.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend eval_cross_provider.py to native-7 (add zhipu/GLM + minimax PROVIDERS rows + env-presence checks)</name>
  <read_first>
    - scripts/eval_cross_provider.py L68-75 (PROVIDERS list), L202-224 (report_env_presence), L605 (the --provider argparse choices line that auto-derives).
    - 089-PATTERNS.md §"scripts/eval_cross_provider.py (MODIFY in place)" (L118-152) — the exact additive row shapes + the "do NOT hand-edit choices" rule.
    - 089-RESEARCH.md §"D-089-09 Eval Curation" (L374-389) — the verified current model IDs + env-var names (config.py:580-581).
  </read_first>
  <files>scripts/eval_cross_provider.py</files>
  <action>
    Two ADDITIVE edits, mirroring existing rows exactly (D-089-09 — additive proof-harness work, NOT loop edits):
    1. In the `PROVIDERS` list (~L68-75), append exactly two tuples after the moonshot row:
       `("zhipu", "glm-4-flash"),` with trailing comment `# GLM — _SUB_AGENT_MODEL_DEFAULTS["zhipu"] (config.py:555); provider-class rep, full pinning = Phase 096 EVAL-01`
       `("minimax", "minimax-m2.7"),` with trailing comment `# _SUB_AGENT_MODEL_DEFAULTS["minimax"] (config.py:554); provider-class rep`
       Before committing, quickly confirm these IDs still resolve in `backend/app/config.py` MODEL_CAPABILITIES (RESEARCH verified them; if the registry default changed, pin to the current `glm-*` / `minimax-*` default — consult config.py `_SUB_AGENT_MODEL_DEFAULTS` L554-555, the source of truth).
    2. In `report_env_presence()`'s `checks` list (~L208-218), append exactly two tuples mirroring the existing provider-key rows:
       `("ZHIPU_API_KEY", True),` and `("MINIMAX_API_KEY", True),` (is_secret=True so the print loop emits only `set`/`MISSING`, never the value).
    DO NOT hand-edit the `--provider` argparse `choices` line (~L605) — it auto-derives from PROVIDERS (`[p for p, _ in PROVIDERS] + [...]`). DO NOT touch the localhost hard-gate (~L233) or any query. The print loop itself is UNCHANGED.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -c "import ast,sys; src=open('scripts/eval_cross_provider.py',encoding='utf-8').read(); assert '(\"zhipu\",' in src and '(\"minimax\",' in src, 'PROVIDERS rows missing'; assert 'ZHIPU_API_KEY' in src and 'MINIMAX_API_KEY' in src, 'env checks missing'; ast.parse(src); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/eval_cross_provider.py` PROVIDERS contains `("zhipu",` and `("minimax",` (grep-exact).
    - `report_env_presence()` checks contain `("ZHIPU_API_KEY", True)` and `("MINIMAX_API_KEY", True)`.
    - The file still parses (`ast.parse` succeeds) — no syntax break.
    - The `--provider` argparse `choices` line is NOT hand-edited (grep shows it still reads `[p for p, _ in PROVIDERS]` — choices auto-derive).
    - `report_env_presence` print loop is unchanged (no value-printing added — secret hygiene preserved).
  </acceptance_criteria>
  <done>The eval gate now covers native-7 (openai/anthropic/google/deepseek/moonshot/zhipu/minimax) + OpenRouter best-effort; env presence reports the two new keys set/MISSING only.</done>
</task>

<task type="auto">
  <name>Task 2: Author the SSE capture helper (capture_run_events + normalize)</name>
  <read_first>
    - 089-RESEARCH.md §"Code Examples" (L283-307) — the capture_run_events + normalize implementation + the capture-point rationale (the run:{run_id} XRANGE IS the wire payload).
    - scripts/eval_cross_provider.py — how the eval already gets run_id (returned by run_prompt) and waits for terminal (wait_for_run); the capture runs right AFTER wait_for_run.
  </read_first>
  <files>scripts/capture_run_events.py</files>
  <action>
    Create `scripts/capture_run_events.py` (NEW) containing the SC#3 capture utilities (089-RESEARCH.md L283-305):
    1. `async def capture_run_events(redis, run_id: str) -> list[dict]:` — calls `await redis.xrange(f"run:{run_id}", "-", "+")`, decodes each entry's `fields["data"]` via `json.loads`, returns the ordered list of event dicts. (The `run:{run_id}` stream is the exact byte sequence runs.py forwards to the browser — server-side, deterministic, zero new in-loop instrumentation.)
    2. `def normalize(events: list[dict]) -> list[dict]:` — copies each event, pops `captured_at` (non-deterministic wall-clock), masks `message_id` and `run_id` to `"<masked>"`, returns the list. This makes the before/after diff deterministic (only volatile ids/timestamps vary run-to-run).
    3. A small `def diff_event_streams(before, after) -> list:` returning the per-index differences of `normalize(before)` vs `normalize(after)` (empty list == SC#3 PASS), so the runbook can assert `diff_event_streams(...) == []` per provider.
    Add a module docstring naming this as the Phase 089 SC#3 byte-identical-SSE proof helper. Pure utility module — no top-level execution, importable by the runbook + the operator's eval driver.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -c "import importlib.util,sys; s=importlib.util.spec_from_file_location('cre','scripts/capture_run_events.py'); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); assert hasattr(m,'capture_run_events') and hasattr(m,'normalize') and hasattr(m,'diff_event_streams'); a=[{'type':'delta','message_id':'x','run_id':'r','captured_at':1}]; b=[{'type':'delta','message_id':'y','run_id':'q','captured_at':2}]; assert m.diff_event_streams(a,b)==[], 'normalize should mask ids+ts → empty diff'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/capture_run_events.py` contains `async def capture_run_events`, `def normalize`, and `def diff_event_streams`.
    - `capture_run_events` reads via `redis.xrange(f"run:{run_id}", "-", "+")` (grep confirms the XRANGE call).
    - `normalize` pops `captured_at` and masks both `message_id` and `run_id`.
    - The `<automated>` self-check passes: two event streams differing ONLY in ids+timestamp normalize to an empty diff.
  </acceptance_criteria>
  <done>The SSE capture + normalize + diff helper exists, is importable, and proves that id/timestamp-only differences normalize away (the byte-identical bar ignores volatile ids).</done>
</task>

<task type="auto">
  <name>Task 3: Write the before/after SSE-diff operator runbook + capture the BEFORE baseline procedure</name>
  <read_first>
    - 089-RESEARCH.md §"Don't Hand-Roll" (L226-232) — the harness ADDS a capture step around the existing eval driver, not a replacement.
    - 089-VALIDATION.md §"Manual-Only Verifications" (L98-104) + §"4-Axis Kickoff UAT" — the operator-run SSE-capture-before+after rows + the native-7 + multi-tool scoping.
    - 089-CONTEXT.md D-089-10, D-089-11 — "representative multi-tool run" = Claude's discretion (search_documents + execute_code and/or write_todos); operator runs the live-key driver, never run_in_background.
  </read_first>
  <files>scripts/SSE_DIFF_RUNBOOK.md</files>
  <action>
    Write `scripts/SSE_DIFF_RUNBOOK.md` — the operator-run procedure for the byte-identical SC#3 proof (D-089-08 + D-089-11 hybrid driver). Include:
    1. **Prereqs:** operator starts uvicorn in a visible terminal (NEVER run_in_background), local Supabase + Redis up, native-7 keys present in `backend/.env` (verified via `python scripts/eval_cross_provider.py --env-check` or report_env_presence output).
    2. **The representative multi-tool prompt** (Claude's discretion per D-089-10): a single scripted prompt exercising 2+ tools — e.g. "Search my documents for X, then write and run code that summarizes the result" (`search_documents` + `execute_code`), and a `write_todos` variant. Pin the exact prompt string so before/after use the identical input.
    3. **BEFORE procedure (run NOW, against the pre-move loop — this plan is Wave 1):** for each native-7 provider, POST the scripted prompt (via the eval's per-request provider/model override — NOT a user_settings UPDATE), `wait_for_run` to terminal, then `capture_run_events(redis, run_id)` → save `normalize(events)` to `scripts/.sse_baseline/<provider>.json`. Document the exact save path + the per-provider model from PROVIDERS.
    4. **AFTER procedure (Plan 04 / post-lift):** re-run the identical prompt per provider, capture, and assert `diff_event_streams(before, after) == []` per native-7 provider. Empty diff = SC#3 PASS; any non-empty diff = the lift changed behavior → BLOCK.
    5. **Native-7 gate vs OpenRouter best-effort:** the 7 native are hard pass/fail; OpenRouter regression is logged not blocking (D-089-07); Ollama opportunistic.
    6. **The eval backstop:** also run `python scripts/eval_cross_provider.py` before AND after and record the `EVAL_SUMMARY` scoreboard (native-7 all-pass) both times (D-089-08 b).
    Note clearly that Claude authors this runbook; the OPERATOR executes it (live keys are operator-only — D-089-11) and pastes the before-baseline confirmation back.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -c "import pathlib,sys; t=pathlib.Path('scripts/SSE_DIFF_RUNBOOK.md').read_text(encoding='utf-8'); need=['capture_run_events','diff_event_streams','XRANGE','native-7','EVAL_SUMMARY','run_in_background']; sys.exit(0 if all(k in t for k in need) else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/SSE_DIFF_RUNBOOK.md` exists and references `capture_run_events`, `diff_event_streams`, the `XRANGE run:{run_id}` capture point, and the `EVAL_SUMMARY` backstop.
    - The runbook pins ONE representative multi-tool prompt string used identically before + after.
    - The runbook documents the per-native-7 BEFORE-baseline save path (`scripts/.sse_baseline/<provider>.json`) and the AFTER empty-diff assertion.
    - The runbook states "operator starts uvicorn in a visible terminal, NEVER run_in_background" and "operator runs the live-key driver" (D-089-11 hybrid).
    - The `<automated>` check exits 0 (all required keys present).
  </acceptance_criteria>
  <done>The operator has a complete before/after SSE-diff runbook + the eval backstop procedure; the BEFORE baseline can be captured against the pre-move loop in Wave 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval driver → live provider APIs | The eval mints a bearer for the LOCAL test user only; provider keys read from operator backend/.env |
| eval output → console / pasted results | Secrets must never appear — presence-only reporting |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-089-04 | Information disclosure | `report_env_presence()` + the 2 new keys | mitigate | New rows use `(NAME, is_secret=True)` so the UNCHANGED print loop emits `set`/`MISSING` only, never the value (089-RESEARCH §Security; secret-hygiene rule). Acceptance criterion verifies no value-printing added. |
| T-089-05 | Information disclosure | localhost hard-gate (eval L233) | mitigate | NOT touched — the gate refuses a cloud SUPABASE_URL; adding 2 PROVIDERS rows does not change it (preserve verbatim, 089-RESEARCH L568). |
| T-089-06 | Tampering | SQL injection in eval | accept | Fixed allowlist `_COUNT_QUERIES`, parameterized thread_id; adding providers adds no queries (089-RESEARCH L569). No new surface. |
| T-089-07 | Information disclosure | traceback leaking env values | mitigate | `BackendUnavailable` clean-exit pattern preserved (no traceback) — not modified by additive rows. |

Controls preserved verbatim through the additive edits: V2 bearer-for-local-test-user-only, V6 presence-only secret reporting, the localhost hard-gate. No agent-loop / DB-write surface touched in this plan.
</threat_model>

<verification>
- `python scripts/eval_cross_provider.py` parses + lists native-7 in PROVIDERS (operator runs the live eval separately).
- `capture_run_events.py` self-check: id/timestamp-only differences normalize to empty diff.
- SSE_DIFF_RUNBOOK.md is complete enough for the operator to capture the BEFORE baseline against the pre-move loop.
- No agent-loop file touched (files_modified is scripts/ only) — extraction purity preserved (D-089-09).
</verification>

<success_criteria>
- eval gate extended to native-7 (additive, secret-safe, choices auto-derived).
- SSE capture/normalize/diff helper authored + self-verifying.
- before/after SSE-diff runbook + eval backstop procedure authored; BEFORE baseline capturable in Wave 1.
</success_criteria>

<output>
After completion, create `.planning/phases/089-agent-loop-extraction-g-5-kickoff-uat/089-02-SUMMARY.md`
</output>
