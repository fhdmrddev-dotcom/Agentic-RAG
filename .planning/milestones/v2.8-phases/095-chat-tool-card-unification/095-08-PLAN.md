---
phase: 095-chat-tool-card-unification
plan: 08
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [CHAT-04]
files_modified:
  - frontend/src/components/chat/OutputFileCard.tsx
  - frontend/src/components/chat/MessageItem.tsx
  - frontend/src/components/chat/MessageList.tsx
  - frontend/src/__tests__/components/MessageItem.finalOutputs.test.tsx

must_haves:
  truths:
    - "The hero file icon renders at 48px and working icons at 30px (sketch 016 sizes), up from 30/16 (GAP-095-03 MED icon size)."
    - "The hero card carries a soft 24px primary glow halo, not a flat 1px ring (GAP-095-03 MED hero glow)."
    - "The generated-files area is a borderless top-rule + uppercase dim eyebrow, not a bordered card (GAP-095-03 MED top-rule/eyebrow)."
    - "The floating chip leads with status and trails with the jump affordance; the working-files toggle says '— intermediates, all downloadable' (GAP-095-03 LOW)."
  artifacts:
    - path: "frontend/src/components/chat/OutputFileCard.tsx"
      provides: "hero icon 48 / working 30; soft 24px primary halo glow"
      contains: "48"
    - path: "frontend/src/components/chat/MessageItem.tsx"
      provides: "borderless top-rule + uppercase dim eyebrow; working toggle copy"
      contains: "border-t"
    - path: "frontend/src/components/chat/MessageList.tsx"
      provides: "floating chip status-first, jump trailing"
      contains: "Jump to live"
  key_links:
    - from: "OutputFileCard hero variant"
      to: "fileIcon size 48"
      via: "isHeroVariant ? 48 : 30"
      pattern: "48 : 30"
    - from: "MessageItem FinalOutputsPanel"
      to: "borderless top-rule eyebrow"
      via: "border-t hairline + uppercase tracked dim label"
      pattern: "Generated files"
---

<objective>
Land the file-axis visual divergences from sketch 016 (3 MED) plus two cheap LOW items, with NO change to the hero/working PARTITION logic (that is correct here; the multi-hero leak is Plan 095-09's backend fix).

Today: hero icon 30px / working 16px (sketch wants 48/30); the hero card glow is a flat `0 0 0 1px rgba(99,102,241,0.08)` ring (sketch wants a soft 24px primary halo); the generated-files area is a full bordered `rounded-md ghost-border bg-card/40` box with a bold label (sketch wants a borderless `border-top` hairline + `pt-3.5` + an uppercase letter-spaced DIM eyebrow). Plus: the floating chip leads with "↓ Jump to live" then status (sketch leads with status, jump trailing); the working toggle label lacks "— intermediates, all downloadable".

Purpose: match the operator-approved output-files sketch 016 + the chip ordering in sketch 015.
Output: hero/working icons at 48/30, a real hero halo, a borderless top-rule + dim eyebrow container, status-first chip, and the intermediates copy.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/095-chat-tool-card-unification/095-VERIFICATION.md
@.planning/phases/095-chat-tool-card-unification/095-HUMAN-UAT.md

# THE design contract — invoke Skill("sketch-findings-agentic-rag"), match sketch 016 (output files) + 015 (chip).
@.claude/skills/sketch-findings-agentic-rag/references/chat-tool-card-unification.md

<interfaces>
<!-- OutputFileCard.tsx — current icon size (:82) and hero shadow (:165) -->
```tsx
const icon = fileIcon(file.filename, isHeroVariant ? 30 : 16)   // :82  sketch wants 48 : 30
// :165 hero className includes:
"shadow-[0_0_0_1px_rgba(99,102,241,0.08)]"                       // flat ring; sketch wants soft 24px halo
```

<!-- MessageItem.tsx FinalOutputsPanel — current container (:83-84) + working toggle (:106) -->
```tsx
<div className="mt-3 rounded-md ghost-border bg-card/40 p-3" data-testid="final-outputs-panel">
  <div className="text-xs font-semibold mb-2 text-foreground/80">Generated files</div>
  ...
  Working files ({working.length})                              // :106 — append the intermediates copy
```

<!-- MessageList.tsx floating chip (:191-212) — currently leads with "↓ Jump to live" then the strip.
     Sketch 015 .live-chip: status segments FIRST, the .jump affordance TRAILING. -->
```tsx
<span className="font-mono text-xs font-semibold text-primary">↓ Jump to live</span>
<span ...>·</span>
<RunStatusStrip elapsedLabel={chipElapsed} stepCount={chipStepCount} activityVerb="Streaming…" placement="header" />
```

<!-- The sketch hero glow token: --shadow-glow-primary = 0 0 24px hsl(239 100% 82% / 0.18) -->
<!-- The sketch eyebrow: text-[10px] uppercase tracking-[0.1em] text-muted-foreground font-mono -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero icon 48 / working 30 + soft 24px hero glow halo (GAP-095-03 MED ×2)</name>
  <files>frontend/src/components/chat/OutputFileCard.tsx</files>
  <read_first>
    - frontend/src/components/chat/OutputFileCard.tsx (lines 73-220 — the variant/icon at 79-82, the hero anchor className at 159-173, the url-less dead branch at 90-127)
    - .claude/skills/sketch-findings-agentic-rag/references/chat-tool-card-unification.md (the CSS "Output files — hero + working" block: `.hero` uses `box-shadow: var(--shadow-glow-primary)`)
  </read_first>
  <action>
    Two changes in OutputFileCard.tsx, both per sketch 016.

    1. Icon sizes. Line ~82 currently: `const icon = fileIcon(file.filename, isHeroVariant ? 30 : 16)`. Change to:
       `const icon = fileIcon(file.filename, isHeroVariant ? 48 : 30)`
       Verify the Lucide glyph + the `.EXT` ribbon label read well at 48px (the fileIcon module from Plan 01 scales by `sizePx`). The dead-state branch (lines ~90-127) uses the same `icon` const, so it inherits the new sizes — confirm the dead hero icon at 48px still lays out (it sits in a `px-4 py-3` row).

    2. Hero glow halo. Line ~165 currently ends the hero className with `shadow-[0_0_0_1px_rgba(99,102,241,0.08)]` (a flat 1px ring). Replace with a soft 24px primary halo matching `--shadow-glow-primary` (`0 0 24px hsl(239 100% 82% / 0.18)`):
       `shadow-[0_0_24px_hsl(239_100%_82%/0.18)]`
       Keep the `border border-primary/40` + the gradient bg — only the box-shadow changes from the flat ring to the soft blurred glow. (The dead-state hero branch has no glow today; leave it — a dead file should not glow.)
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/components/MessageItem.finalOutputs.test.tsx && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isHeroVariant ? 48 : 30" frontend/src/components/chat/OutputFileCard.tsx` is 1.
    - `grep -c "isHeroVariant ? 30 : 16" frontend/src/components/chat/OutputFileCard.tsx` returns 0 (old sizes gone).
    - `grep -c "shadow-\[0_0_24px_hsl(239_100%_82%/0.18)\]" frontend/src/components/chat/OutputFileCard.tsx` is >= 1.
    - `grep -c "0_0_0_1px_rgba(99,102,241,0.08)" frontend/src/components/chat/OutputFileCard.tsx` returns 0 (flat ring gone).
    - `cd frontend && npx vite build` exits 0.
  </acceptance_criteria>
  <done>Hero file icon is 48px, working icons 30px; the hero card carries a soft 24px primary glow halo instead of the flat 1px ring.</done>
</task>

<task type="auto">
  <name>Task 2: Borderless top-rule + dim eyebrow container + intermediates copy (GAP-095-03 MED + LOW)</name>
  <files>frontend/src/components/chat/MessageItem.tsx, frontend/src/__tests__/components/MessageItem.finalOutputs.test.tsx</files>
  <read_first>
    - frontend/src/components/chat/MessageItem.tsx (lines 74-119 — the FinalOutputsPanel sub-component: the container at 83, the label at 84, the working toggle at 106)
    - .claude/skills/sketch-findings-agentic-rag/references/chat-tool-card-unification.md (the HTML "Output files area" block: `outputs-label` "Generated files" + `working-head` "Working files (3) — intermediates, all downloadable")
  </read_first>
  <action>
    Convert the FinalOutputsPanel container to the borderless top-rule + dim eyebrow (sketch 016) and add the intermediates copy.

    1. Container (line ~83). Change from the bordered box `"mt-3 rounded-md ghost-border bg-card/40 p-3"` to a borderless top-rule:
       `"mt-3 border-t border-border/60 pt-3.5"`
       Remove the `rounded-md ghost-border bg-card/40 p-3` framing. Keep `data-testid="final-outputs-panel"`.

    2. Eyebrow label (line ~84). Change from the bold label `"text-xs font-semibold mb-2 text-foreground/80"` (text "Generated files") to an uppercase, letter-spaced, DIM, mono eyebrow:
       `<div className="text-[10px] font-mono uppercase tracking-[0.1em] text-muted-foreground mb-2.5">Generated files</div>`
       Keep the text "Generated files" verbatim (sketch `outputs-label`).

    3. Working-files toggle copy (line ~106). Currently `Working files ({working.length})`. Append the sketch copy: `Working files ({working.length}) — intermediates, all downloadable`.

    4. Do NOT change the hero/working PARTITION logic (the `heroes`/`working` filter at lines ~78-80, the graceful no-hero fallback, the collapse useState). That logic is correct here — the multi-hero leak is fixed in Plan 095-09 (backend). Leave `data-testid="final-outputs-hero"` and `data-testid="final-outputs-working"` intact.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/components/MessageItem.finalOutputs.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "border-t border-border" frontend/src/components/chat/MessageItem.tsx` is >= 1 (the top-rule).
    - The FinalOutputsPanel container no longer uses `rounded-md ghost-border bg-card/40 p-3`: `grep -c "rounded-md ghost-border bg-card/40 p-3" frontend/src/components/chat/MessageItem.tsx` returns 0.
    - `grep -c "uppercase tracking-\[0.1em\]" frontend/src/components/chat/MessageItem.tsx` is >= 1 (the dim eyebrow).
    - `grep -c "intermediates, all downloadable" frontend/src/components/chat/MessageItem.tsx` is 1.
    - `grep -c "data-testid=\"final-outputs-panel\"" frontend/src/components/chat/MessageItem.tsx` is 1 (preserved).
    - `cd frontend && npx vitest run src/__tests__/components/MessageItem.finalOutputs.test.tsx` passes (update any test asserting the old container classes/label weight).
  </acceptance_criteria>
  <done>The generated-files area is a borderless top-rule + uppercase dim eyebrow; the working toggle reads "Working files (N) — intermediates, all downloadable"; partition logic unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Floating chip status-first, jump trailing (GAP-095-03 LOW)</name>
  <files>frontend/src/components/chat/MessageList.tsx</files>
  <read_first>
    - frontend/src/components/chat/MessageList.tsx (lines 179-213 — the floating chip button; the chip derivations chipElapsed/chipStepCount at ~143-148)
    - .claude/skills/sketch-findings-agentic-rag/references/chat-tool-card-unification.md (the CSS ".live-chip" + ".live-chip .jump" — the jump morphs in AFTER the status segments)
  </read_first>
  <action>
    Reorder the floating chip so the status strip leads and the "↓ Jump to live" affordance trails (sketch 015 `.live-chip` shows the status segments first, with `.jump` appended).

    Currently the button renders `<span>↓ Jump to live</span>` FIRST, then a `·`, then the `<RunStatusStrip>`. Swap the order:
    1. Render the `<RunStatusStrip elapsedLabel={chipElapsed} stepCount={chipStepCount} activityVerb="Streaming…" placement="header" />` FIRST (the status segments lead).
    2. Then the separator, then the `<span className="font-mono text-xs font-semibold text-primary">↓ Jump to live</span>` TRAILING.
    3. Keep the button itself as the single floating pill (`onClick={jumpToLive}`, the `data-testid="jump-to-live-chip"`, the sticky bottom-center positioning, the glow). Only the inner order of the two spans changes.

    NOTE: Plan 095-07 also touches RunStatusStrip's header chrome. Both plans are Wave 1 and run sequentially per the orchestrator; this task only REORDERS the chip's children — it does not edit RunStatusStrip — so there is no file overlap (MessageList.tsx vs RunStatusStrip.tsx are disjoint).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/components/chat/MessageList.test.tsx && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - In MessageList.tsx, the `<RunStatusStrip ...>` JSX appears BEFORE the `↓ Jump to live` span inside the `data-testid="jump-to-live-chip"` button (verify by reading the diff — the strip element's line number is now lower than the jump-span's line number within the button).
    - The rendered "↓ Jump to live" span is still present exactly once (the affordance is preserved, just reordered): the JSX `<span ...>↓ Jump to live</span>` element appears once inside the button. (Note: the literal string also appears in comments/aria — count only the rendered span by inspecting the diff, not a raw `grep -c`.)
    - `grep -c "aria-label=\"Jump to live\"" frontend/src/components/chat/MessageList.tsx` is 1 (the button aria-label is preserved).
    - `grep -c "data-testid=\"jump-to-live-chip\"" frontend/src/components/chat/MessageList.tsx` is 1.
    - `cd frontend && npx vitest run src/__tests__/components/chat/MessageList.test.tsx` passes.
    - `cd frontend && npx vite build` exits 0.
  </acceptance_criteria>
  <done>The floating chip leads with the status strip and trails with "↓ Jump to live", matching sketch 015.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file metadata -> output-file render | model-derived filenames cross into the DOM; fileIcon renders only the parsed ext label (XSS-safe, Plan 01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-095-08-01 | Tampering (XSS) | fileIcon at 48px | mitigate | fileIcon renders the ext label as React text children only (Plan 01 contract) — no raw HTML; size change does not alter the XSS posture. |
| T-095-08-02 | Tampering (XSS) | eyebrow + toggle copy | mitigate | all static/derived text rendered as React children — no `dangerouslySetInnerHTML`. |
</threat_model>

<verification>
- `grep -c "dangerouslySetInnerHTML" frontend/src/components/chat/OutputFileCard.tsx` returns 0.
- `grep -c "dangerouslySetInnerHTML" frontend/src/components/chat/MessageItem.tsx` returns 0.
- The hero/working partition logic (FinalOutputsPanel `heroes`/`working` filter) is UNCHANGED — only container chrome + copy changed.
- DEFERRED to SEED-054 and explicitly OUT of scope (do NOT build): the per-file descriptive subtitle (needs a backend description/role field) and the folded-page SVG icon (the Lucide form is contract-permitted). Interim allowed but optional: render the hero size as a subline under the filename instead of only a right-aligned badge.
- `tsc -b` reports no NET-NEW errors referencing the three touched files.
</verification>

<success_criteria>
- GAP-095-03 (MED icon size + MED hero glow + MED top-rule/eyebrow + LOW chip-order + LOW intermediates copy) closed.
- Frontend-only, provider-agnostic; partition logic untouched.
- finalOutputs + MessageList suites green; `vite build` clean.
</success_criteria>

<output>
After completion, create `.planning/phases/095-chat-tool-card-unification/095-08-SUMMARY.md`
</output>
