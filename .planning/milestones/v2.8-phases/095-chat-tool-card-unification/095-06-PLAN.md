---
phase: 095-chat-tool-card-unification
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [CHAT-04]
files_modified:
  - frontend/src/components/chat/ToolCallPanel.tsx
  - frontend/src/__tests__/components/ToolCallPanel.test.tsx
  - frontend/src/index.css

must_haves:
  truths:
    - "Expanding ONE finished tool-card expands only THAT card; the other finished cards stay folded (closes GAP-095-01)."
    - "Every finished tool step folds to a single essence line from step 1 — no >=3-step gate (GAP-095-03 un-gate + essence)."
    - "The active step blooms (primary-dim wash + inset 2px primary left bar) instead of the old outer glow (GAP-095-03 bloom)."
  artifacts:
    - path: "frontend/src/components/chat/ToolCallPanel.tsx"
      provides: "Per-step expand Set keyed on stepKeyOf; single essence-line resting state; un-gated fold; active bloom"
      contains: "expandedSteps"
    - path: "frontend/src/__tests__/components/ToolCallPanel.test.tsx"
      provides: "Partial-expand test (expand one row -> only that row body shows); updated former expand-all assertion"
      contains: "expandedSteps"
    - path: "frontend/src/index.css"
      provides: ".tc-active-wrap replaced by sketch-014 bloom (primary-dim wash + inset 2px left bar)"
      contains: "inset 2px 0 0"
  key_links:
    - from: "collapsed essence row onClick"
      to: "expandedSteps Set"
      via: "setExpandedSteps(prev => new Set(prev).add(stepKey))"
      pattern: "setExpandedSteps"
    - from: "collapsed-branch render gate"
      to: "expandedSteps membership"
      via: "&& !expandedSteps.has(stepKey)"
      pattern: "expandedSteps\\.has"
---

<objective>
Fix the #1 felt bug (GAP-095-01 fold-all) and land the two HIGH + one MED sketch-fidelity divergences that live in the same file: the single essence line, the un-gated Focus-Mode fold, and the active-step bloom.

Today a SINGLE shared `stepsCollapsed` boolean governs the in-flight Focus-Mode done-step window. Every collapsed summary row's onClick calls `setStepsCollapsed(false)`, so one click expands ALL finished cards (violates D-01 click-to-expand + SC#1 details-on-demand). And the fold only kicks in at >=3 done steps, while finished cards show args + a SEPARATE result row instead of the sketch's single essence line.

Purpose: make finished-card collapse PER-CARD (each finished step folds/expands independently) and match the sketch 014 contract — essence resting line, fold-from-step-1, active bloom.

Output: ToolCallPanel renders every finished step as a one-line essence (click-to-expand its own body), the active step blooms, and expand/collapse is per-step.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/095-chat-tool-card-unification/095-VERIFICATION.md
@.planning/phases/095-chat-tool-card-unification/095-HUMAN-UAT.md

# THE design contract — the executor MUST invoke this skill and match sketch 014.
# Skill("sketch-findings-agentic-rag")
@.claude/skills/sketch-findings-agentic-rag/references/chat-tool-card-unification.md

<interfaces>
<!-- The exact identity scheme ALREADY present in ToolCallPanel.tsx. The per-step
     expand Set MUST key on this same identity so expanded state survives the
     preparing->running->done id mutation (075.9) and survives reload. -->

From frontend/src/components/chat/ToolCallPanel.tsx (current, lines ~420-433):
```ts
const toolStepNumber = new Map<string, number>()
deduplicatedToolCalls.forEach((tc, idx) => {
  const key = tc.clientKey ?? tc.id ?? `${tc.name}-${tc.startedAt ?? ''}-${idx}`
  toolStepNumber.set(key, idx + 1)
})
const stepKeyOf = (tc: ToolCall, idx: number) =>
  tc.clientKey ?? tc.id ?? `${tc.name}-${tc.startedAt ?? ''}-${idx}`

const nodeStateOf = (tc: ToolCall): NodeState => {
  if (tc.status === "running" || tc.status === "preparing") return "active"
  if (tc.status === "done" || tc.status === "interrupted") return "done"
  return "queued"
}
```

From the same file (current collapse model, lines ~486-488):
```ts
const hiddenStepsCount = activeIndex === -1 ? 0 : activeIndex
const shouldCollapse = hiddenStepsCount >= 3
const [stepsCollapsed, setStepsCollapsed] = useState(true)
```

DO NOT touch these — they are correct and orthogonal to this fix:
- `panelExpanded` Record + `togglePanel` + `lastPreparingIndex` (lines ~441-456, ~821-822) — the 075.6 default-expand-active-preparing rule.
- `ToolResultBlock`'s own `open` useState (lines ~147-148, ~204) — the per-card body toggle.
- The iteration-divider / `data-iteration-min` logic (lines ~591-600, ~647, ~554).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-step expand Set — kill the shared stepsCollapsed fold-all (GAP-095-01) + un-gate the fold (GAP-095-03)</name>
  <files>frontend/src/components/chat/ToolCallPanel.tsx, frontend/src/__tests__/components/ToolCallPanel.test.tsx</files>
  <read_first>
    - frontend/src/components/chat/ToolCallPanel.tsx (lines 415-580 — the collapse model, the displayItems map, the collapsed-summary branch and its two onClicks)
    - frontend/src/__tests__/components/ToolCallPanel.test.tsx (lines 1-130 — the suite + the expand-all assertion at ~99-108)
  </read_first>
  <behavior>
    - Test (RED first): render ToolCallPanel with >=3 finished tool calls preceding an active tool. Click the FIRST collapsed essence row. Assert ONLY that row's body becomes visible (the others stay one-line essence rows). This replaces the former expand-all test at ToolCallPanel.test.tsx:99-108.
    - Test: clicking a second collapsed row expands the second too (the Set grows); the first stays expanded.
    - Test: with 1 finished step before the active tool, that finished step STILL folds to an essence row (no >=3 gate) — proves the un-gate.
    - Test: a per-row re-collapse affordance returns an expanded row to its essence (the Set shrinks for that key only).
  </behavior>
  <action>
    Replace the single shared `stepsCollapsed` boolean with a per-step expanded Set, and remove the `>= 3` gate.

    1. DELETE the line `const shouldCollapse = hiddenStepsCount >= 3` and `const [stepsCollapsed, setStepsCollapsed] = useState(true)` (lines ~487-488). Keep `hiddenStepsCount` (the `aria-label` count still reads it).

    2. ADD a per-step expanded Set keyed on the SAME identity `stepKeyOf` produces:
       `const [expandedSteps, setExpandedSteps] = useState<Set<string>>(() => new Set())`
       Add helpers next to it:
       ```ts
       const expandStep = (key: string) => setExpandedSteps(prev => new Set(prev).add(key))
       const collapseStep = (key: string) => setExpandedSteps(prev => { const n = new Set(prev); n.delete(key); return n })
       ```

    3. CHANGE the collapsed-branch gate. The branch currently fires on `shouldCollapse && stepsCollapsed && i < activeIndex` (line ~544). Replace with un-gated, per-step:
       `const collapsedStepKey = item.kind === 'tool' ? stepKeyOf(item.tc, i) : undefined`
       and gate the branch on: `i < activeIndex && (item.kind !== 'tool' || !expandedSteps.has(collapsedStepKey!))`
       (i.e. EVERY finished step before the active one folds to an essence row UNLESS its key is in `expandedSteps`). The `>= 3` gate is gone — a single finished step folds too.

    4. CHANGE the two collapsed-row onClicks (the tool row at ~552 and the skill row at ~570) from `() => setStepsCollapsed(false)` to expand ONLY that row's key:
       - tool row: `onClick={() => expandStep(stepKeyOf(collapsedTc, i))}`
       - skill rows have no stepKey identity in `toolStepNumber`; for a skill collapsed row use a stable composite key `` `skill-${item.activation.occurredAt}` `` and the same `expandStep`/`!expandedSteps.has(...)` gate so skill rows are individually expandable too. (If simpler, keep skill rows always-visible-as-one-line and exclude them from the expand Set — but a tool-row click must NEVER expand a skill row or any other tool row.)

    5. ADD a per-row RE-COLLAPSE affordance. The old "Hide earlier steps" button (lines ~517-531) only rendered while `!stepsCollapsed` and re-folded ALL — that model is gone. Instead: when an expanded former-collapsed step renders its full body (i.e. `i < activeIndex && expandedSteps.has(key)`), render a small per-row "Hide" control (a `ChevronUp`/`ChevronDown` button) whose onClick is `collapseStep(key)`. Place it so each expanded earlier step can re-fold to its essence independently. DELETE the old aggregate "Hide earlier steps" block at ~517-531.

    6. The `data-iteration-min` summary-row attribute (line ~554, `i === 0 ? collapsedIterationMin : undefined`) assumed a contiguous collapsed block. Under interleaved partial-expand the contiguous block can break. Keep `data-iteration-min` on the FIRST still-collapsed earlier step (recompute "first collapsed index" instead of hard-coding `i === 0`), OR drop the attribute if no test reads it — verify with `grep -rn "data-iteration-min" frontend/src/__tests__` first; if zero hits, remove the attribute to avoid a stale invariant.

    Identity rule (NON-NEGOTIABLE): the expand Set MUST key on `stepKeyOf` (`clientKey ?? id ?? composite`), the SAME identity the rail snum + dedup use, so an expanded state survives the preparing->running->done id mutation (075.9) and a reload (state is reconstructed from `toolCalls` each render).

    State stays component-local and provider-agnostic — no StreamsProvider/api.ts/backend change. Cross-provider safe by construction.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/components/ToolCallPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "stepsCollapsed" frontend/src/components/chat/ToolCallPanel.tsx` returns 0 (the shared boolean is fully removed).
    - `grep -c "setStepsCollapsed" frontend/src/components/chat/ToolCallPanel.tsx` returns 0.
    - `grep -c "shouldCollapse" frontend/src/components/chat/ToolCallPanel.tsx` returns 0 (the >=3 gate is removed).
    - `grep -c "expandedSteps" frontend/src/components/chat/ToolCallPanel.tsx` is >= 3 (state + the membership gate + at least one mutator).
    - `grep -c "expandedSteps.has" frontend/src/components/chat/ToolCallPanel.tsx` is >= 1.
    - The ToolCallPanel test file contains a partial-expand assertion: `grep -c "expandedSteps\|only that\|partial" frontend/src/__tests__/components/ToolCallPanel.test.tsx` is >= 1, AND the former expand-all assertion (one click reveals all) is gone.
    - `cd frontend && npx vitest run src/__tests__/components/ToolCallPanel.test.tsx` passes.
  </acceptance_criteria>
  <done>Clicking one finished essence row expands only that row; the others stay folded; every finished step folds from step 1; each expanded earlier step can re-fold independently.</done>
</task>

<task type="auto">
  <name>Task 2: Single essence line for finished cards + active-step bloom (GAP-095-03 essence + bloom)</name>
  <files>frontend/src/components/chat/ToolCallPanel.tsx, frontend/src/index.css</files>
  <read_first>
    - frontend/src/components/chat/ToolCallPanel.tsx (lines 600-870 — the per-tool render body: the head row at 690-747, the ToolResultBlock usage at 206-228 + 846-848, the StepRow wrapper, the isToolActive wrapper at 630-639)
    - frontend/src/index.css (lines 390-405 — the .tc-active-wrap class)
    - .claude/skills/sketch-findings-agentic-rag/references/chat-tool-card-unification.md (section A "The unified card frame" + the CSS "Rail + nodes + active bloom" block + the HTML "A step on the rail" block)
  </read_first>
  <action>
    Two changes, both matching sketch 014.

    (A) Finished card = the single essence line. Per sketch 014 / chat-tool-card-unification.md section A D-01, a FINISHED (done/interrupted) tool's resting state is ONE line: `{snum} {icon} {tool} -> {result} {pill} {chev}` where the resting text is the RESULT (the `summarizeToolCall(tc)` value), NOT the args summary. Click-to-expand reveals the full per-tool body.

    Currently a done card renders: the head row (icon + `toolLabel` + the args `summary` in quotes, lines ~715-722) AND a SEPARATE `ToolResultBlock` (the `→ {summary}` row at ~206-228 / mounted at ~846-848). Collapse this to ONE essence line for the done/interrupted resting state:
       - For a DONE/INTERRUPTED tool that is NOT individually expanded, render a single clickable essence row: `{icon} {toolLabel} → {summarizeToolCall(tc) || "View results"} {StatusPill} {chevron}`. The whole row is the click target that toggles this card's body (reuse the existing per-card body-open mechanism — `ToolResultBlock`'s internal `open` state, or lift a per-card open the same way). The result text uses `text-muted-foreground` (the sketch `.ess-text` — "finished essence recedes").
       - When EXPANDED, show the full per-tool body (the `ToolResultBlock` content + `ToolArgsBlock`) below the essence line, chevron rotated.
       - The RUNNING/PREPARING head row stays as-is (active = open/bloom, shows the live verb + ElapsedTimer + the streaming bodies). Do NOT change the preparing/running render.
       - Keep `execute_code`'s dedicated `TOOL_BODIES.execute_code` path (line ~686) intact — for execute_code the essence is the existing body's collapsed form; do not double-render. If a clean essence line for execute_code is awkward, the minimum bar is: a done execute_code card shows ONE result-bearing line at rest, not args + a separate result row.
       - Preserve `data-testid="tool-result-summary"` on the essence/result element (a test reads it).

    (B) Active-step bloom. Replace the old 075.8 outer-glow with the sketch 014 bloom.
       - In index.css, change `.tc-active-wrap` (lines ~399-404): remove `box-shadow: 0 0 24px hsl(var(--primary) / 0.18)` and the soft gradient; replace with the sketch `.step.active` bloom:
         ```css
         .tc-active-wrap {
           position: relative;
           background: hsl(var(--primary) / 0.06);            /* --color-primary-dim wash */
           box-shadow: inset 2px 0 0 hsl(var(--primary));     /* inset 2px primary left bar */
           padding-bottom: 4px;
         }
         ```
       - The active essence/verb text should read primary (the sketch `.step.active .ess-text { color: var(--color-primary) }`). The running head row already uses `text-primary` for the icon/verb — ensure the active row's text leans primary, not muted.
       - Keep the bottom shimmer (`.tool-progress-bar` at ~861-866) — it is the active motion cue and is separate from the bloom.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/components/ToolCallPanel.test.tsx && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "inset 2px 0 0" frontend/src/index.css` is >= 1 (the bloom left bar is present).
    - `grep -c "0 0 24px hsl(var(--primary) / 0.18)" frontend/src/index.css` returns 0 inside the `.tc-active-wrap` block (the old outer glow is removed; note the same shadow may legitimately remain on the RunCard frame — only the `.tc-active-wrap` rule must change).
    - `grep -c "data-testid=\"tool-result-summary\"" frontend/src/components/chat/ToolCallPanel.tsx` is >= 1 (the essence/result element id is preserved).
    - A finished, non-expanded card renders ONE result-bearing essence row (verified by the ToolCallPanel test: the args-in-quotes summary is NOT shown as a separate row above a separate result row for a done card).
    - `cd frontend && npx vite build` exits 0.
    - `cd frontend && npx vitest run src/__tests__/components/ToolCallPanel.test.tsx` passes.
  </acceptance_criteria>
  <done>Finished tool cards rest as a single essence line (result, not args); clicking expands the full body; the active step blooms with a primary-dim wash + inset 2px primary left bar instead of the outer glow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE stream -> ToolCallPanel render | `tool_calls` (incl. model-derived `result`/`args`) cross into the DOM; already React-escaped as text children. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-095-06-01 | Tampering (XSS) | essence-line render of `summarizeToolCall(tc)` | mitigate | render via React text children only — no `dangerouslySetInnerHTML` (grep must stay 0 in this file). |
| T-095-06-02 | Denial of Service (state blowup) | `expandedSteps` Set growth | accept | bounded by tool-card count per run (tens, not unbounded); reconstructed from `toolCalls` each render. |
| T-095-06-03 | Information disclosure (cross-thread) | component-local state | accept | state is component-instance-local, never shared across threads; no provider/store write. |
</threat_model>

<verification>
- `grep -c "dangerouslySetInnerHTML" frontend/src/components/chat/ToolCallPanel.tsx` returns 0.
- The 075.6 default-expand-active-preparing rule still works (`panelExpanded` / `togglePanel` / `lastPreparingIndex` greps unchanged from baseline).
- `tsc -b` reports no NET-NEW errors referencing ToolCallPanel.tsx (the documented baseline is 37).
</verification>

<success_criteria>
- GAP-095-01 closed: per-card expand/collapse (one click expands only its own card).
- GAP-095-03 (HIGH essence + HIGH un-gate + MED bloom) closed for this file.
- ToolCallPanel test suite green incl. the new partial-expand test; `vite build` clean.
- Provider-agnostic, additive component-local state; no shared-path / StreamsProvider touch.
</success_criteria>

<output>
After completion, create `.planning/phases/095-chat-tool-card-unification/095-06-SUMMARY.md`
</output>
