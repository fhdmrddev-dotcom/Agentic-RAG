---
phase: 095-chat-tool-card-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/stepCount.ts
  - frontend/src/lib/__tests__/stepCount.test.ts
  - frontend/src/lib/fileIcon.tsx
  - frontend/src/lib/__tests__/fileIcon.test.tsx
autonomous: true
requirements: [CHAT-04]

must_haves:
  truths:
    - "There is exactly ONE step-count derivation in the codebase that every consumer (RunCard step label, RunCard collapsed-row, RunCard header, status strip, rail) reads — they can never disagree (D-04)."
    - "The dedup logic that ToolCallPanel uses is extracted into ONE shared helper so the panel count and the headline count use byte-identical dedup (no drift)."
    - "There is ONE per-extension file-icon module the output-file cards reuse — no second icon system (D-07/G1)."
  artifacts:
    - path: "frontend/src/lib/stepCount.ts"
      provides: "unifiedStepCount(message) + the extracted shared dedupToolCalls() — the embodied D-04 single source of truth"
      contains: "export function unifiedStepCount"
    - path: "frontend/src/lib/fileIcon.tsx"
      provides: "fileIcon(filename) per-extension Lucide-based icon + colored ext label (D-07)"
      contains: "export function fileIcon"
  key_links:
    - from: "frontend/src/lib/stepCount.ts dedupToolCalls()"
      to: "frontend/src/components/chat/ToolCallPanel.tsx deduplicatedToolCalls useMemo"
      via: "ToolCallPanel imports the shared helper in a later plan instead of its inline copy"
      pattern: "dedupToolCalls"
---

<objective>
Build the THREE genuinely-new, FOUNDATIONAL primitives the rest of Phase 095 consumes, and stand up the Wave-0 test scaffolds. This plan touches NO existing consumer — it only creates the shared single-sources-of-truth so later plans cannot fork them.

1. `unifiedStepCount(message)` (D-04) — the one integer behind the RunCard step label, the collapsed-row "N steps", the RunCard header, the status strip, and the rail node count. It MUST reuse the EXACT dedup ToolCallPanel uses today (`tc.clientKey ?? tc.id ?? composite`), so the dedup is ALSO extracted into a shared `dedupToolCalls()` here. This is the embodied D-04/D-05 honesty fix per SKETCH-CONSISTENCY §4.
2. `fileIcon(filename)` (D-07) — one per-extension Lucide-based icon + colored ext label module, reused by every output-file card. No second icon system (resolves SKETCH-CONSISTENCY G1).
3. Wave-0 test files for D-04 and D-07-icon so downstream plans have RED→GREEN targets.

Purpose: SKETCH-CONSISTENCY §1 + §4 — "build each shared primitive ONCE." Making these foundational (built first, consumed by the rest) is what prevents the honesty bugs (260528-02 step mismatch) from reopening.
Output: 2 new lib modules + 2 new test files. Zero behavior change to any rendered component (nothing imports these yet — wiring happens in Plans 02/03/05).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/095-chat-tool-card-unification/095-RESEARCH.md
@.planning/phases/095-chat-tool-card-unification/095-VALIDATION.md
@.claude/skills/sketch-findings-agentic-rag/references/chat-tool-card-unification.md

<interfaces>
<!-- The exact current dedup ToolCallPanel uses (ToolCallPanel.tsx:321-334) — extract this VERBATIM. -->
```ts
// ToolCallPanel.tsx:321-334 — the canonical dedup the shared helper must reproduce
const deduplicatedToolCalls = useMemo(() => {
  const seen = new Set<string>()
  const result: ToolCall[] = []
  ;(toolCalls ?? []).forEach((tc, idx) => {
    const key = tc.clientKey ?? tc.id ?? `${tc.name}-${tc.startedAt ?? ''}-${idx}`
    if (seen.has(key)) return
    seen.add(key)
    result.push(tc)
  })
  return result
}, [toolCalls])
```

<!-- makeToolKey contract (toolKey.ts:36-45) — for reference; do NOT modify it here. -->
```ts
export function makeToolKey(opts: MakeToolKeyOptions): string  // provider|messageId|name|observedAt|index
```

<!-- The ToolCall and Message types live in frontend/src/types/index.ts.
     unifiedStepCount must accept a Message-shaped object that has `tool_calls?: ToolCall[]`.
     The per-extension fileIcon map (canonical) is in chat-tool-card-unification.md §"Per-extension file icon":
     pptx→orange / pdf→red / docx→blue / md→slate / csv→green / png→violet / json→teal; default→gray.
     In code use Lucide equivalents (FileText / Image / Table / Code / Presentation) + a colored ext label. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the shared dedup + create unifiedStepCount (D-04)</name>
  <files>frontend/src/lib/stepCount.ts, frontend/src/lib/__tests__/stepCount.test.ts</files>
  <read_first>
    - frontend/src/components/chat/ToolCallPanel.tsx:321-334 (the canonical `deduplicatedToolCalls` useMemo — the dedup to extract)
    - frontend/src/types/index.ts (the `ToolCall` + `Message` types — confirm `tool_calls?: ToolCall[]`, `clientKey`, `id`, `name`, `startedAt` fields)
    - 095-RESEARCH.md §"D-04 — unifiedStepCount() source-of-truth" (the 5 consumer sites + the cross-provider rationale: deduped count removes the iteration_start divergence)
    - 095-VALIDATION.md Per-Task Verification Map → D-04 row
  </read_first>
  <behavior>
    - dedupToolCalls(toolCalls): returns first-occurrence-ordered array deduped on `tc.clientKey ?? tc.id ?? `${tc.name}-${tc.startedAt ?? ''}-${idx}`` (byte-identical to ToolCallPanel.tsx:321-334).
    - Empty/undefined input → returns [].
    - unifiedStepCount(message): returns `dedupToolCalls(message.tool_calls).length`.
    - A message with 5 raw tool_calls where 2 share a clientKey → unifiedStepCount = 4.
    - A message with N deduped tools spread across M iterations → unifiedStepCount = N (NOT M) — proves it ignores iterationCount entirely (the cross-provider-safe count).
    - A DB-loaded message whose tool_calls lack clientKey but have distinct names/startedAt → composite fallback dedups correctly.
  </behavior>
  <action>
    Create `frontend/src/lib/stepCount.ts`. Export two functions:

    (1) `export function dedupToolCalls(toolCalls: ToolCall[] | undefined | null): ToolCall[]` — copy the dedup logic from ToolCallPanel.tsx:321-334 VERBATIM (the `Set<string>` + `seen` + first-occurrence push, same key expression `tc.clientKey ?? tc.id ?? `${tc.name}-${tc.startedAt ?? ''}-${idx}``). This becomes the ONE dedup; ToolCallPanel will import it in Plan 03 instead of keeping its inline copy.

    (2) `export function unifiedStepCount(message: { tool_calls?: ToolCall[] | null }): number` — return `dedupToolCalls(message.tool_calls ?? undefined).length`.

    Import `ToolCall` from `frontend/src/types/index.ts` (or wherever the type is exported — confirm via read_first). Do NOT import React. This is pure logic, no hooks.

    Per D-04 (095-RESEARCH.md): this is the single source of truth that RunCard's `Step N` (currently `iterationCount+1` at RunCard.tsx:132-135), the collapsed-row "N tool calls" (raw length at RunCard.tsx:232-234), the header "Run · N tools" (raw length at RunCard.tsx:129), the status strip, and the rail node count ALL switch to in later plans. Do NOT wire any consumer in this task — only create the helper.

    Per RESEARCH correction #5: switching to the deduped `tool_calls.length` (which IS persisted, unlike `iterationCount` which `_mapMessageResponse` omits) also fixes the "Step N vanishes on next-day reopen" bug for free — note this in a code comment.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/__tests__/stepCount.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/stepCount.ts` exists and `grep -c "export function unifiedStepCount" frontend/src/lib/stepCount.ts` ≥ 1
    - `grep -c "export function dedupToolCalls" frontend/src/lib/stepCount.ts` ≥ 1
    - `grep -c "performance.now\|useState\|useEffect\|useMemo" frontend/src/lib/stepCount.ts` == 0 (pure logic, no React)
    - `grep "tc.clientKey ?? tc.id ??" frontend/src/lib/stepCount.ts` matches (same key expression as ToolCallPanel.tsx:321-334)
    - `frontend/src/lib/__tests__/stepCount.test.ts` exists with: a 5-raw-2-dup → 4 case; an N-tools-across-M-iterations → N case; a composite-fallback case
    - `cd frontend && npx vitest run src/lib/__tests__/stepCount.test.ts` exits 0
    - Maps to 095-VALIDATION.md D-04 row: "all three read `unifiedStepCount(msg)` == deduped length"
  </acceptance_criteria>
  <done>stepCount.ts exports unifiedStepCount + dedupToolCalls; stepCount.test.ts proves dedup == ToolCallPanel semantics and count ignores iterationCount; per-file vitest exits 0. No consumer wired yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-extension fileIcon module (D-07)</name>
  <files>frontend/src/lib/fileIcon.tsx, frontend/src/lib/__tests__/fileIcon.test.tsx</files>
  <read_first>
    - .claude/skills/sketch-findings-agentic-rag/references/chat-tool-card-unification.md §"Per-extension file icon — one shared fileIcon()" (the canonical ext→(color, glyph-category) map: pptx orange / pdf red / docx blue / md slate / csv green / png violet / json teal; default gray)
    - 095-SKETCH-GROUNDING.md §2 (the per-tool Lucide icon vocabulary — use the Lucide-based equivalent: FileText/Image/Table/Code/Presentation)
    - frontend/src/components/chat/OutputFileCard.tsx:14-22 (current single Download icon — fileIcon will be consumed here in Plan 05; do NOT modify OutputFileCard in this plan)
    - 095-VALIDATION.md → D-07 hero file row
  </read_first>
  <behavior>
    - fileIcon("deck.pptx") → a Presentation-category glyph + orange-ish ext label ".PPTX".
    - fileIcon("report.pdf") → text/file glyph + red ext label ".PDF".
    - fileIcon("chart.png") → Image glyph + violet ext label ".PNG".
    - fileIcon("data.csv") → Table glyph + green ext label ".CSV".
    - fileIcon("notes") (no extension) → default gray + label ".FILE".
    - The returned element renders plain-text only (no dangerouslySetInnerHTML — filenames are model/user-derived).
  </behavior>
  <action>
    Create `frontend/src/lib/fileIcon.tsx`. Export `export function fileIcon(filename: string, sizePx?: number): JSX.Element`.

    Implement the ext→(color, glyph) map from chat-tool-card-unification.md §"Per-extension file icon", using Lucide icons (already a project dependency) NOT raw SVG strings:
    - text-category (pdf #e03131, docx #1c7ed6, md #5c677d, txt, rtf, doc) → `FileText`
    - table-category (csv #2f9e44, xls, xlsx) → `Table`
    - image-category (png #7048e8, jpg, jpeg, gif, svg, webp) → `Image`
    - code-category (json #0c8599, js, ts, html, py, xml) → `Code`
    - slides-category (pptx #f76707, ppt) → `Presentation`
    - default (#687076) → `FileText`

    Render: the Lucide glyph colored with the category color + a small mono ext label `.{EXT}` (e.g. `.PPTX`). Parse the extension via `filename.split('.').pop()?.toLowerCase()`. Default size 24px when `sizePx` omitted. ALL children are plain text (the ext label) — NO `dangerouslySetInnerHTML`, NO interpolating the filename into markup.

    Reuse-only: import icons from `lucide-react`; use Tailwind/inline color via the category hex. No new CSS keyframes.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/__tests__/fileIcon.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/fileIcon.tsx` exists and `grep -c "export function fileIcon" frontend/src/lib/fileIcon.tsx` ≥ 1
    - `grep -c "dangerouslySetInnerHTML" frontend/src/lib/fileIcon.tsx` == 0 (XSS guard — T-095-XSS)
    - `grep -E "pptx|pdf|docx|csv|png|json" frontend/src/lib/fileIcon.tsx` matches (the ext map present)
    - `grep -E "Presentation|FileText|Image|Table|Code" frontend/src/lib/fileIcon.tsx` matches (Lucide glyphs, not raw SVG)
    - `frontend/src/lib/__tests__/fileIcon.test.tsx` renders fileIcon for .pptx/.pdf/.png/.csv/no-ext and asserts the ext label text + that no raw HTML is injected
    - `cd frontend && npx vitest run src/lib/__tests__/fileIcon.test.tsx` exits 0
  </acceptance_criteria>
  <done>fileIcon.tsx maps extensions to Lucide glyph + colored ext label, plain-text only; fileIcon.test.tsx green; per-file vitest exits 0. Not yet consumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model/user → render | Filenames passed to `fileIcon` originate from the model/sandbox (user-influenced); they cross into the render layer here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-095-01-01 | Tampering/XSS | `fileIcon(filename)` | mitigate | Filename is rendered ONLY as a parsed extension label via React text children; NO `dangerouslySetInnerHTML`, NO markup interpolation. Asserted by acceptance grep `dangerouslySetInnerHTML == 0`. |
| T-095-01-02 | Information disclosure | `stepCount.ts` | accept | Pure derivation over already-in-memory `message.tool_calls`; reads no new store, no PII surface, no network. Low risk. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lib/__tests__/stepCount.test.ts src/lib/__tests__/fileIcon.test.tsx` — both green.
- `cd frontend && npx tsc -b` — zero NET-NEW type errors attributable to the 2 new files (nothing imports them yet, so the count must be unchanged from baseline).
- No existing component file modified in this plan (grep that RunCard/ToolCallPanel/MessageItem/MessageList/OutputFileCard are untouched in this plan's diff).
</verification>

<success_criteria>
- `unifiedStepCount` + `dedupToolCalls` exist as the single shared D-04 source; tests prove deduped count ignores iterationCount.
- `fileIcon` exists as the single per-extension icon module; tests prove the ext map + XSS safety.
- Zero behavior change to any rendered surface (no consumer wired); `tsc -b` net-new = 0; `vite build` clean.
</success_criteria>

<output>
After completion, create `.planning/phases/095-chat-tool-card-unification/095-01-SUMMARY.md`
</output>
