---
phase: 095-chat-tool-card-unification
plan: 05
type: execute
wave: 3
depends_on: [01]
files_modified:
  - backend/app/services/agent_loop.py
  - backend/tests/test_095_final_output_tag.py
  - frontend/src/lib/api.ts
  - frontend/src/components/chat/OutputFileCard.tsx
  - frontend/src/components/chat/MessageItem.tsx
  - frontend/__tests__/components/MessageItem.finalOutputs.test.tsx
autonomous: true
requirements: [CHAT-04]

must_haves:
  truths:
    - "At run end the final-outputs payload carries a hero flag: the agent's declared deliverable if present, ELSE a backend heuristic (match the user-requested extension; fall back to the largest/last-written deliverable) so the hero is NEVER empty across all 6 providers (D-07/D-08, operator-resolved this session)."
    - "Every emitted final-output file carries a `url` field (guard at the emit site) so there is no silent dead-link / no-download-affordance entry (RESEARCH dead-link root #1)."
    - "The hero flag persists into the execute_code result so a chat reopened the next day re-heroes the same file (RESEARCH dead-link relevance — reload reconstruction; closes the next-day half of SC#3)."
    - "The chat output area heroes the deliverable above a quieter, collapsible 'Working files (N)' group; ALL files are present and downloadable — re-rank, never hide (D-07, supersedes BUG-260514-01 with re-rank)."
    - "The hero tag is additive: older streams without it → the frontend treats all files as working (graceful); the field never bypasses the owner-fenced re-sign endpoint and never renames/reshapes the final_output_files event (094 byte-identical Deep)."
  artifacts:
    - path: "backend/app/services/agent_loop.py"
      provides: "additive hero tag on the final_output_files emit + url guard + persist into execute_code result (D-08)"
      contains: "final_output_files"
    - path: "frontend/src/components/chat/OutputFileCard.tsx"
      provides: "hero vs working layout flag + fileIcon (Plan 01) + the existing download states (idle/ok/dead)"
      contains: "fileIcon"
    - path: "frontend/src/components/chat/MessageItem.tsx"
      provides: "hero/working grouping of finalOutputFiles (hero block + collapsible Working files group)"
      contains: "Working files"
  key_links:
    - from: "backend/app/services/agent_loop.py final_output_files emit"
      to: "frontend/src/lib/api.ts onFinalOutputFiles dispatch"
      via: "the additive hero field flows through the {filename, url, size, <hero>} payload"
      pattern: "final_output_files"
    - from: "frontend/src/components/chat/MessageItem.tsx"
      to: "frontend/src/components/chat/OutputFileCard.tsx + frontend/src/lib/fileIcon.tsx"
      via: "hero/working grouping renders OutputFileCard with the hero flag + fileIcon"
      pattern: "OutputFileCard"
---

<objective>
Deliver the file axis — the ONE small, contained backend touch (D-08) plus the frontend hero/working render (D-07):

1. **D-08 backend hero tag (operator-resolved this session = "agent marks + backend fallback"):** add ONE additive field to the `final_output_files` emit (agent_loop.py:2020-2042) flagging the hero file(s). The agent's declaration is honored if present; ELSE a backend heuristic (match the user-requested extension from `body.content`, e.g. `.docx`/`.pptx`; else the largest/last-written deliverable) so the hero is NEVER empty across all 6 providers. Persist the flag into the execute_code result (agent_loop.py:1995-2002) so reload-reconstruction (api.ts:111-133) re-heroes next-day. Guard that every emitted file carries a `url` (RESEARCH dead-link root #1). Do NOT touch the re-sign endpoint (sandbox_outputs.py — already re-signs on every click, the dead-link root is NOT signed-URL expiry per RESEARCH correction #2).
2. **D-07 frontend hero/working split:** extend `OutputFileCard` with a hero-vs-working layout flag + the `fileIcon` (Plan 01); group `finalOutputFiles` in `MessageItem` into a "★ Your file" hero block above a collapsible "Working files (N)" group. Show ALL files, all downloadable — re-rank, never hide.

Purpose: SKETCH-CONSISTENCY §1 (`OutputFileCard` +Hero/Working EXTEND; `fileIcon` reuse) + the D-07/D-08 root-cause findings + the operator-resolved hero mechanism. The download path stays owner-fenced; the tag is additive and never bypasses it.
Output: agent_loop.py hero tag + url guard + persist; new pytest; api.ts passes the field; OutputFileCard hero flag; MessageItem grouping; finalOutputs test extended.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/095-chat-tool-card-unification/095-RESEARCH.md
@.planning/phases/095-chat-tool-card-unification/095-VALIDATION.md
@.claude/skills/sketch-findings-agentic-rag/references/chat-tool-card-unification.md

<interfaces>
<!-- From Plan 01 -->
```tsx
export function fileIcon(filename: string, sizePx?: number): JSX.Element
```

<!-- The backend emit to extend (agent_loop.py:2033-2042). body.content = the user message (MessageCreate.content, agent_loop.py:100 + used as user_message at :2204). -->
```python
if _previous_files_in_run:
    await _emit(
        redis, run_id, 'final_output_files',
        files=[
            {"filename": meta["filename"], "url": meta["url"], "size": meta["size"]}
            for meta in _previous_files_in_run.values()        # meta dict also has "iteration"
        ],
    )
```

<!-- The execute_code persist to extend (agent_loop.py:1995-2002) — output_files comes from _r.get("output_files", []) -->
```python
persisted_result = json.dumps({
    "status": ..., "exit_code": ..., "duration_ms": ...,
    "output_files": _r.get("output_files", []),               # add the hero flag onto matching entries here
    "stdout": ...[:800], "stderr": ...[:200],
})
```

<!-- Frontend dispatch (api.ts:589-592): onFinalOutputFiles((parsed.files ?? [])); reads {filename, url?}. Reload reconstruct: api.ts:111-133 rebuilds finalOutputFiles from persisted tool_calls[].result.output_files using f.url. -->

<!-- OutputFileCard current (OutputFileCard.tsx): resolveOutputUrl (14-22), url-optional back-compat (72-86 — plain filename, NO download affordance when url missing), Bearer-fetch download (88-107), download states idle/ok/dead. -->

<!-- MessageItem final-outputs render (MessageItem.tsx:506-527): flat space-y-1.5 map of <OutputFileCard key={i} file={f} />, data-testid="final-outputs-panel". -->
```

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-08 backend — agent-marks + backend-fallback hero tag, url guard, persist into result</name>
  <files>backend/app/services/agent_loop.py, backend/tests/test_095_final_output_tag.py</files>
  <read_first>
    - backend/app/services/agent_loop.py:2020-2042 (the final_output_files emit + the _previous_files_in_run.values() projection; meta dict keys filename/url/size/iteration), :1995-2002 (the execute_code persisted_result with output_files), :1180-1192 (the _previous_files_in_run dict[content_hash, meta] structure + comment), :100 + :2204 (body.content = the user message)
    - backend/app/api/sandbox_outputs.py:35-97 (the owner-fenced 60s-TTL re-sign endpoint — DO NOT touch; confirm the tag does not bypass it)
    - 095-RESEARCH.md §"D-08 / D-07 — Dead-download-link ROOT" (NOT signed-URL expiry; roots are missing-url + reconcile-swap) + §"Minimal D-08 backend touch" (additive field; thread it into the persisted execute_code result; guard every emitted file carries a url) + Open Q1 (the heuristic recommendation: match requested extension, fall back to largest/last-written) + Assumptions Log A1
    - 095-RESEARCH.md §Landmines rows "075.1 download" (re-rank not hide), "PANEL-06 isolation", "094 byte-identical Deep dispatch" (do NOT rename/reshape final_output_files or add a return), "final_output_files reload reconstruct"
    - operator_decisions_resolved_this_session #1 (agent marks + backend fallback; the field must persist into execute_code result; do NOT touch the re-sign endpoint)
    - 095-VALIDATION.md → D-08 tag (backend) row
  </read_first>
  <behavior>
    - final_output_files emit: each file dict gains `is_hero: bool` (or a `hero_filenames` companion — Claude's discretion, document the field name); the agent's declaration is honored if present in the run state; ELSE the backend heuristic picks the hero.
    - Heuristic: if `body.content` mentions a requested extension (e.g. "docx"/"pptx"/"pdf"/"xlsx") and a generated file matches → that file is hero; ELSE the largest deliverable (by size) OR the last-written (highest iteration) → hero. NEVER zero heroes when ≥1 file exists.
    - Every emitted file dict carries a `url` (files lacking a url are still emitted but flagged/guarded so the frontend never renders a silent dead anchor — at minimum url is always present as a string).
    - The execute_code persisted result's output_files entries carry the same hero flag so reload reconstruction re-heroes.
    - Additive: an older run / a run with no files → no behavior change; the emit event NAME and existing fields (filename/url/size) are unchanged; no new `return`.
    - Deep byte-identical: this is the shared agent loop; the change is purely additive at the post-loop emit + the execute_code persist — no terminal-path or cursor-advance change.
  </behavior>
  <action>
    Add the hero tag at the emit site (agent_loop.py:2033-2042) WITHOUT renaming the event or removing fields:

    (1) Write a small pure helper near the emit (e.g. `_select_hero_filenames(files: list[dict], user_message: str) -> set[str]`): lower-case the user message; detect a requested extension among a small set (docx, pptx, pdf, xlsx, csv, png, md); if any file's filename ends with the requested ext → hero is that/those file(s); else hero = the single largest-size file (tie-break: highest `iteration`). Returns a set of hero filenames; empty set ONLY when `files` is empty. If the run carries an explicit agent declaration (check whether any meta dict already has an agent-set hero flag — if the agent loop has a place to record intent; if not, the heuristic is the sole source — document which), honor that first.

    (2) Extend the emit projection: each file dict gains `"is_hero": meta["filename"] in _hero_set`. Compute `_hero_set = _select_hero_filenames([...the projected files...], body.content or "")` before the comprehension. Guard the url: `"url": meta.get("url") or ""` (never missing). Do NOT add a `return`; do NOT rename `final_output_files`; keep filename/url/size.

    (3) Persist into the execute_code result (agent_loop.py:1995-2002): when building `persisted_result`, map each `output_files` entry to add `"is_hero": <entry filename in the run's hero set>`. If the hero set is computed only at the post-loop emit, also stamp the flag onto the persisted tool_calls' output_files at persist time using the same helper over the cumulative `_previous_files_in_run` — Claude's discretion on ordering, but the persisted result MUST carry the flag so api.ts:111-133 reload reconstruction re-heroes (RESEARCH landmine "final_output_files reload reconstruct"). Document the approach.

    Do NOT touch `backend/app/api/sandbox_outputs.py` (the re-sign endpoint — the dead-link root is NOT expiry per RESEARCH correction #2; the tag must not bypass the owner fence). The tag is read by the CHAT render only — never a panel store (PANEL-06).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_095_final_output_tag.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "is_hero\|hero_filenames" backend/app/services/agent_loop.py` ≥ 2 (emit + persist)
    - `grep -c "_select_hero_filenames\|def _select_hero" backend/app/services/agent_loop.py` ≥ 1 (the helper)
    - `grep -c "'final_output_files'" backend/app/services/agent_loop.py` == baseline (event NOT renamed; still emitted once at the post-loop site)
    - `git diff backend/app/api/sandbox_outputs.py` is EMPTY (re-sign endpoint untouched)
    - NEW `backend/tests/test_095_final_output_tag.py` cases: (a) user asks for a .docx and a .docx is generated → that file is_hero=True, others False; (b) no requested ext → the largest file is_hero=True; (c) no files → empty hero set, no crash; (d) every emitted file carries a non-missing url; (e) the persisted execute_code result carries is_hero for reload; (f) an agent-declared hero (if the path exists) is honored over the heuristic
    - `cd backend && venv/Scripts/python -m pytest tests/test_095_final_output_tag.py -x` exits 0
    - Maps to 095-VALIDATION.md D-08 row: "tag field present in emit + persisted execute_code result"
  </acceptance_criteria>
  <done>The final_output_files emit + the persisted execute_code result carry an additive hero flag (agent-declared else heuristic, never empty when files exist); every file carries a url; the re-sign endpoint is untouched; the event is byte-identical-additive; pytest green.</done>
</task>

<task type="auto">
  <name>Task 2: D-07 frontend — OutputFileCard hero flag + fileIcon; MessageItem hero/working grouping; api.ts passes the field</name>
  <files>frontend/src/lib/api.ts, frontend/src/components/chat/OutputFileCard.tsx, frontend/src/components/chat/MessageItem.tsx, frontend/__tests__/components/MessageItem.finalOutputs.test.tsx</files>
  <read_first>
    - frontend/src/components/chat/OutputFileCard.tsx (resolveOutputUrl 14-22; url-optional 72-86; Bearer-fetch download 88-107; download states idle/ok/dead 88-145)
    - frontend/src/components/chat/MessageItem.tsx:506-527 (the flat finalOutputFiles map; data-testid="final-outputs-panel")
    - frontend/src/lib/api.ts:589-592 (onFinalOutputFiles((parsed.files ?? []))) + :111-133 (_mapMessageResponse reload reconstruct from tc.result.output_files using f.url)
    - frontend/src/lib/fileIcon.tsx (Plan 01)
    - frontend/src/types/index.ts (the finalOutputFiles / OutputFile type — add the optional hero flag matching the backend field name)
    - .claude/skills/sketch-findings-agentic-rag/references/chat-tool-card-unification.md §C + §"Output files — hero + working" CSS + §"HTML Structures — Output files area" (the "★ Your file" hero block + collapsible "Working files (N)" group; dl-btn ok/dead states; per-extension fileIcon)
    - 095-RESEARCH.md §Landmines "075.1 download" (re-rank not hide — show ALL files), §"D-07 url-optional" dead-link #1
    - 095-VALIDATION.md → D-07 hero file + D-07 dead-link (no url) rows
  </read_first>
  <action>
    (1) **Type + api.ts:** add the optional hero flag (matching Task 1's field name, e.g. `is_hero?: boolean`) to the OutputFile/finalOutputFiles type in types/index.ts. api.ts already spreads `parsed.files` at :589-592 — confirm the field flows through untouched (it's an extra key on each file dict; no dispatch change needed). Confirm `_mapMessageResponse` (api.ts:111-133) carries `is_hero` through when reconstructing from `tc.result.output_files` (add the field to the reconstruction mapping so reload re-heroes).

    (2) **OutputFileCard:** add a `variant?: "hero" | "working"` prop (default "working"). Hero variant = the emphasized "★ Your file" block (the §C CSS maps to: primary glow border, gradient bg via the allowed `bg-primary/5`-style utilities, larger fileIcon, prominent Download). Working variant = the quieter row. BOTH use `fileIcon(file.filename)` (Plan 01) for the icon. Preserve the existing url-optional back-compat (72-86) AND the download states (idle/ok/dead, 88-145) — but for a url-less file, render the affordance as clearly disabled (the `dl-btn.dead` state) rather than a silent plain filename (095-VALIDATION.md D-07 dead-link pass: "download works OR affordance clearly disabled"). Plain-text children only (filename is model-derived — NO dangerouslySetInnerHTML).

    (3) **MessageItem grouping (MessageItem.tsx:506-527):** replace the flat map. Split `message.finalOutputFiles` into `heroes = files.filter(f => f.is_hero)` and `working = files.filter(f => !f.is_hero)`. If NO file has `is_hero` (older stream — graceful), treat ALL as working (per the additive contract). Render: the hero block (each hero via `<OutputFileCard variant="hero" />`) ABOVE a collapsible "Working files (N)" group (working files via `<OutputFileCard variant="working" />`), the group VISIBLE-by-default with a collapse affordance (Radix Collapsible or the existing pattern). Keep `data-testid="final-outputs-panel"` and the `finalOutputFiles.length > 0` empty-state guard. Show ALL files — re-rank, never hide (D-07; do NOT reintroduce the BUG-260514-01 hiding stance).

    Reuse-only CSS; no new keyframes. The output area renders below the answer (the seam already established at MessageItem).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run __tests__/components/MessageItem.finalOutputs.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fileIcon" frontend/src/components/chat/OutputFileCard.tsx` ≥ 1 (Plan 01 icon reused)
    - `grep -E "variant|hero|working" frontend/src/components/chat/OutputFileCard.tsx` matches (the layout flag)
    - `grep -c "Working files" frontend/src/components/chat/MessageItem.tsx` ≥ 1 AND `grep -c "is_hero\|heroes" frontend/src/components/chat/MessageItem.tsx` ≥ 1 (the grouping)
    - `grep -c "data-testid=\"final-outputs-panel\"" frontend/src/components/chat/MessageItem.tsx` ≥ 1 (preserved)
    - `grep -c "dangerouslySetInnerHTML" frontend/src/components/chat/OutputFileCard.tsx frontend/src/components/chat/MessageItem.tsx` == 0
    - EXTENDED MessageItem.finalOutputs.test.tsx: a fixture with 1 is_hero + N working → hero rendered emphasized/separate, working in a collapsible group, ALL with a download affordance when url present (095-VALIDATION.md D-07 hero row)
    - EXTENDED MessageItem.finalOutputs.test.tsx: a url-less file → affordance clearly disabled (dead state), NOT a silent dead anchor (095-VALIDATION.md D-07 dead-link row)
    - EXTENDED MessageItem.finalOutputs.test.tsx: an older fixture with NO is_hero on any file → all render as working (graceful, no crash)
    - `cd frontend && npx vitest run __tests__/components/MessageItem.finalOutputs.test.tsx` exits 0
  </acceptance_criteria>
  <done>OutputFileCard has a hero/working variant + fileIcon + clearly-disabled dead state; MessageItem groups heroes above a collapsible Working files group showing ALL files; the hero field flows through api.ts incl. reload; older streams degrade gracefully; finalOutputs test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sandbox/model → final_output_files | filenames + the hero tag originate from the model/sandbox (user-influenced) and cross into the emit, the DB persist, and the render. |
| client → re-sign endpoint | the download click hits the owner-fenced sandbox_outputs endpoint (404 on IDOR). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-095-05-01 | Elevation / IDOR | the hero tag vs the re-sign download | mitigate | The hero flag is presentation-only; it NEVER feeds the download path — the click still hits `sandbox_outputs.py` which owner-fences (404 on cross-user/miss). The re-sign endpoint is UNTOUCHED (git diff empty). The tag cannot bypass the owner fence. |
| T-095-05-02 | Tampering / cross-user file | the hero heuristic over _previous_files_in_run | mitigate | _previous_files_in_run is closure-local per run (agent_loop.py:1192) — scoped to THIS run's owner; the heuristic only re-ranks files already owned by this run, it cannot reference another user's file. |
| T-095-05-03 | Tampering/XSS | filename in OutputFileCard / MessageItem | mitigate | filenames rendered as React text children + via fileIcon (Plan 01, text-only); NO `dangerouslySetInnerHTML`. Asserted by grep == 0. |
| T-095-05-04 | DoS / 094 byte-identical Deep | final_output_files emit | mitigate | Additive field only — event NOT renamed, no field removed, no new `return` (cursor-advance preserved). Asserted: `'final_output_files'` grep == baseline; sandbox_outputs.py diff empty. |
| T-095-05-05 | PANEL-06 isolation breach | the hero tag consumer | accept→assert | The tag is read by MessageItem (chat bucket) only — never todosByThread/tasksByThread/phasesByThread/workspaceFilesByThread/pendingAsksByThread. Asserted by grep. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/test_095_final_output_tag.py -x` — green.
- `cd frontend && npx vitest run __tests__/components/MessageItem.finalOutputs.test.tsx` — green; `cd frontend && npm test` — full suite green.
- `cd frontend && npx tsc -b` — zero net-new type errors; `npx vite build` clean.
- 094 byte-identical Deep: `grep -c "'final_output_files'" backend/app/services/agent_loop.py` == baseline; `git diff backend/app/api/sandbox_outputs.py` empty.
- PANEL-06: grep MessageItem.tsx + OutputFileCard.tsx add no panel-store read.
- 075.1 NON-REGRESSION: the working-files group shows ALL files (re-rank, not hide) — assert the MessageItem test renders working files, not a hidden/filtered subset.
</verification>

<success_criteria>
- The hero is never empty when ≥1 file exists (agent-declared else heuristic), every file carries a url, the flag persists for next-day re-hero (D-08); the re-sign endpoint + owner fence untouched.
- The chat output area heroes the deliverable above a collapsible Working files group, all files downloadable, re-rank not hide (D-07); older streams degrade gracefully.
- Deep byte-identical (additive emit), PANEL-06 held, full suite + pytest green.
</success_criteria>

<output>
After completion, create `.planning/phases/095-chat-tool-card-unification/095-05-SUMMARY.md`
</output>
