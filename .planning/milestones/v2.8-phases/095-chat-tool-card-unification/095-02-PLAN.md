---
phase: 095-chat-tool-card-unification
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - frontend/src/components/chat/RunCard.tsx
  - frontend/src/components/chat/RunStatusStrip.tsx
  - frontend/src/components/chat/RunCard.test.tsx
autonomous: true
requirements: [CHAT-04]

must_haves:
  truths:
    - "The run timer is visible continuously from kickoff and freezes ONLY at a true terminal — it never blinks out on a long Kimi/Moonshot run, a transient stream_end, a backgrounded tab, or a temp-id→DB-id remount (D-06, closes BUG-260528-01)."
    - "Elapsed is derived from message.created_at (wall-clock), so it survives the 083 temp-id→DB-id remount — REINFORCES the 083 fix BUG-260526-04, never regresses it."
    - "RunCard's step label, collapsed-row count, and header count ALL read unifiedStepCount(message) — they can never disagree, and the label survives next-day reopen (D-04 + RESEARCH correction #5)."
    - "The run-status strip (⏱ elapsed · Step N · activity) is ONE component fed by the D-06 timer + unifiedStepCount + the activity verb — no duplicated segment logic."
  artifacts:
    - path: "frontend/src/components/chat/RunStatusStrip.tsx"
      provides: "ONE ⏱ elapsed · Step N · activity strip with a placement prop (header | floating) — the single RunStatusStrip (D-06)"
      contains: "export function RunStatusStrip"
    - path: "frontend/src/components/chat/RunCard.tsx"
      provides: "D-06 continuous timer from created_at; D-04 unifiedStepCount on all 3 sites; header hosts RunStatusStrip"
      contains: "unifiedStepCount"
  key_links:
    - from: "frontend/src/components/chat/RunCard.tsx"
      to: "frontend/src/lib/stepCount.ts"
      via: "import { unifiedStepCount }"
      pattern: "unifiedStepCount"
    - from: "frontend/src/components/chat/RunCard.tsx"
      to: "message.created_at"
      via: "Date.parse(message.created_at) as the elapsed baseline"
      pattern: "created_at"
---

<objective>
EXTEND `RunCard.tsx` (the existing `RunFrame`) with the two RunCard-internal honesty fixes, and create the ONE `RunStatusStrip` it hosts:

1. **D-06 persistent timer (ROOT fix):** replace the `performance.now()`-baseline + `{(isStreamingNow || elapsedMs > 0)}` render gate (the exact vanish trap) with a continuous wall-clock derivation from `message.created_at`, frozen only at a TRUE terminal. This survives the 083 temp-id→DB-id remount and background-tab throttling.
2. **D-04 unified count:** switch all THREE RunCard count sites — the `Step N` subtitle (currently `iterationCount+1`), the collapsed-row "N tool calls" (raw length), and the header "Run · N tools" (raw length) — to `unifiedStepCount(message)` from Plan 01. This makes the strip and the cards structurally agree AND fixes the next-day-reopen vanish (RESEARCH correction #5).
3. **RunStatusStrip:** one `⏱ elapsed · Step N · activity` component (Plan 03/Plan 04 reuse the floating placement; this plan wires the header placement) so the timer + count + activity verb are composed once.

Purpose: SKETCH-CONSISTENCY §1 (`RunFrame` EXTEND + one `RunStatusStrip`, two placements) + the D-04/D-06 root-cause findings. Honors G-5 (RunCard extended additively, not rebuilt).
Output: RunCard.tsx extended; RunStatusStrip.tsx new; RunCard.test.tsx extended with D-06 + D-04 cases.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/095-chat-tool-card-unification/095-RESEARCH.md
@.planning/phases/095-chat-tool-card-unification/095-VALIDATION.md
@.claude/skills/sketch-findings-agentic-rag/references/chat-tool-card-unification.md

<interfaces>
<!-- From Plan 01 (frontend/src/lib/stepCount.ts) -->
```ts
export function unifiedStepCount(message: { tool_calls?: ToolCall[] | null }): number
export function dedupToolCalls(toolCalls: ToolCall[] | undefined | null): ToolCall[]
```

<!-- Current RunCard timer (RunCard.tsx:83-98) — the ROOT to replace -->
```tsx
const startedAtRef = useRef<number | null>(null)
const [elapsedMs, setElapsedMs] = useState(0)
useEffect(() => {
  if (!isStreamingNow) return
  if (startedAtRef.current == null) startedAtRef.current = performance.now()   // ROOT: perf.now baseline in a streaming-gated effect
  const id = window.setInterval(() => {
    if (startedAtRef.current != null) setElapsedMs(performance.now() - startedAtRef.current)
  }, 250)
  return () => window.clearInterval(id)
}, [isStreamingNow])
const elapsedSeconds = (elapsedMs / 1000).toFixed(1)
```

<!-- Current render gate (RunCard.tsx:192-199) — the EXACT vanish trap -->
```tsx
{(isStreamingNow || elapsedMs > 0) && (
  <span className="...tabular-nums" aria-live="polite">{elapsedSeconds}s</span>
)}
```

<!-- Current count sites to switch (all read raw / iterationCount):
     RunCard.tsx:129  header  `Run · ${message.tool_calls?.length ?? 0} tool…`
     RunCard.tsx:132-135  subtitle `Step ${message.iterationCount + 1}`
     RunCard.tsx:232-234  collapsed-row `Run · {message.tool_calls?.length} tool call(s)` -->

<!-- The activity verb source (RunCard.tsx:122-127): outerBannerLabel(activeTool, hasTools, isPlanning) -->
<!-- message.created_at: ISO string present on every assistant message incl. DB-loaded (_mapMessageResponse); reconcile placeholder sets it to run.started_at (StreamsProvider:1113). Confirm the field name on the Message type in types/index.ts. -->
```

</context>

<tasks>

<task type="auto">
  <name>Task 1: D-06 persistent timer + RunStatusStrip (continuous wall-clock, freeze at true terminal)</name>
  <files>frontend/src/components/chat/RunCard.tsx, frontend/src/components/chat/RunStatusStrip.tsx, frontend/src/components/chat/RunCard.test.tsx</files>
  <read_first>
    - frontend/src/components/chat/RunCard.tsx:46-135 (state, the perf.now timer 83-98, headerTitle 122-130, stepLabel 132-135) AND 148-248 (the header render incl. the 192-199 vanish gate + the collapsed-row 222-248)
    - frontend/src/types/index.ts (confirm `Message.created_at` is an ISO string field; confirm runStatus values streaming|completed|failed|cancelled|timed_out|undefined)
    - 095-RESEARCH.md §"D-06 — Timer-vanish ROOT" (the full mechanism + the robust derivation: read Date.parse(message.created_at), render continuously, freeze frozenEnd at terminal, never gate on elapsedMs>0; "Using created_at actively REINFORCES the 083 fix")
    - 095-RESEARCH.md §Landmines row "083 temp-id remount" and Assumptions Log A2 (created_at == run start on the reconcile placeholder)
    - .claude/skills/sketch-findings-agentic-rag/references/chat-tool-card-unification.md §B + §"Status strip — one component, two homes" (the ⏱ elapsed · Step N · activity strip; freeze only on TRUE terminal; the ❌ "Gating the timer on isStreamingNow || elapsedMs>0" anti-pattern)
    - 095-VALIDATION.md Per-Task Verification Map → D-06 persistent timer row
  </read_first>
  <action>
    Replace the RunCard timer derivation and create the strip.

    (1) **Remove** the `startedAtRef = performance.now()` baseline + the streaming-gated effect (RunCard.tsx:85-96) AND remove the `{(isStreamingNow || elapsedMs > 0)}` render gate (RunCard.tsx:192-199).

    (2) **New derivation:** compute `const startMs = Date.parse(message.created_at)` (the stable wall-clock baseline, preserved through the temp-id→DB-id remount — RESEARCH A2). Keep a 250ms `setInterval` that calls `setNow(Date.now())` ONLY while `isStreamingNow` (so a finished card stops ticking), but compute `elapsedMs = (frozenEndMs ?? now) - startMs`. At the streaming→terminal edge, capture `frozenEndMs` to `Date.now()` ONCE (a ref set when `!isStreamingNow && frozenEndMs == null` becomes true) so the frozen duration is correct. Because elapsed is recomputed `now - start` each tick (not an accumulator), background-tab interval throttling only coarsens the tick — never wrong/frozen (RESEARCH: this property is the immune-to-throttle guarantee). Guard `Number.isFinite(startMs)` — if `created_at` is missing/unparseable, fall back to NOT rendering elapsed (never NaN).

    (3) **Render CONTINUOUSLY** — show the timer whenever `Number.isFinite(startMs)` is true, regardless of `isStreamingNow` or `elapsedMs`. The vanish gate is GONE. Format `(elapsedMs/1000).toFixed(1)`s OR a `Xm Ys` form for long runs (Claude's discretion; the sketch shows `3m12s`).

    (4) **Create `frontend/src/components/chat/RunStatusStrip.tsx`** — `export function RunStatusStrip({ elapsedLabel, stepCount, activityVerb, placement }: { elapsedLabel: string; stepCount: number; activityVerb: string | null; placement: "header" | "floating" })`. Renders `⏱ {elapsedLabel} · Step {stepCount} · {activityVerb}` as ONE inline-flex strip. `placement="header"` = the inline header chip; `placement="floating"` = the bottom live-chip styling (used by Plan 04). Use ONLY the reuse-only CSS / allowed classes (font-mono, the `.done` modifier pattern, `animate-dotBounce` for the activity dot, `animate-pulse`) — NO new keyframes. Plain-text children only (the activity verb comes from `outerBannerLabel`, a controlled string — still render as text, no HTML).

    (5) **Wire the header placement:** render `<RunStatusStrip placement="header" elapsedLabel={...} stepCount={unifiedStepCount(message)} activityVerb={isStreamingNow ? outerBannerLabel(activeTool, hasTools, message.isPlanning ?? false) : null} />` into the RunCard header (replacing the separate timer span + stepLabel subtitle as the single strip per SKETCH §B header placement). Keep the avatar, file-count badge, and Loader2 spinner as-is.

    Do NOT touch `_isTransientStreamEnd` or any StreamsProvider code (the 075.2 transient fix neighborhood is owned by Plan 03 and must stay frozen here). Do NOT change the MessageList `key={run-${runId}}` (the 083 remount guard).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/chat/RunCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "performance.now" frontend/src/components/chat/RunCard.tsx` == 0 (perf.now baseline removed)
    - `grep -c "elapsedMs > 0" frontend/src/components/chat/RunCard.tsx` == 0 (the vanish gate removed)
    - `grep -c "Date.parse(message.created_at)\|message.created_at" frontend/src/components/chat/RunCard.tsx` ≥ 1 (wall-clock baseline)
    - `frontend/src/components/chat/RunStatusStrip.tsx` exists; `grep -c "export function RunStatusStrip" RunStatusStrip.tsx` ≥ 1; `grep -c "placement" RunStatusStrip.tsx` ≥ 1
    - `grep -c "dangerouslySetInnerHTML" frontend/src/components/chat/RunStatusStrip.tsx` == 0
    - NEW RunCard.test.tsx case: set `created_at`, flip runStatus completed mid-cycle with elapsed never having ticked (elapsedMs would be 0 under old code) → assert the timer is STILL rendered (the D-06 pass condition in 095-VALIDATION.md: "timer present when start-time exists regardless of elapsedMs/isStreamingNow")
    - NEW RunCard.test.tsx case: timer freezes at terminal (frozenEnd captured) — elapsed does not keep growing after runStatus terminal
    - `cd frontend && npx vitest run src/components/chat/RunCard.test.tsx` exits 0
  </acceptance_criteria>
  <done>Timer derives from created_at, renders continuously, freezes at true terminal; RunStatusStrip is one component with a placement prop wired into the header; D-06 test cases green.</done>
</task>

<task type="auto">
  <name>Task 2: D-04 unify all three RunCard count sites onto unifiedStepCount</name>
  <files>frontend/src/components/chat/RunCard.tsx, frontend/src/components/chat/RunCard.test.tsx</files>
  <read_first>
    - frontend/src/components/chat/RunCard.tsx:129 (header `Run · N tools`), :132-135 (`Step ${iterationCount+1}` subtitle), :232-234 (collapsed-row raw `tool_calls.length`)
    - frontend/src/lib/stepCount.ts (Plan 01 — `unifiedStepCount`)
    - 095-RESEARCH.md §"D-04" (the 5 consumers; correction #5 — iterationCount is NOT reconstructed on DB reload so the deduped count fixes next-day vanish for free)
    - .claude/skills/sketch-findings-agentic-rag/references/chat-tool-card-unification.md §A ("Row numbering is load-bearing"; the collapsed form reads "N steps")
    - 095-VALIDATION.md → D-04 unified count row
  </read_first>
  <action>
    Import `unifiedStepCount` from `frontend/src/lib/stepCount.ts` and switch every RunCard count to it:

    (1) **Header title (RunCard.tsx:129):** the terminal branch `Run · ${message.tool_calls?.length ?? 0} tool…` → `Run · ${unifiedStepCount(message)} step${unifiedStepCount(message) === 1 ? "" : "s"} · …`. (Relabel "tools/tool calls" → "step(s)" per SKETCH-CONSISTENCY "relabel N tools→N steps".)

    (2) **Step subtitle / strip (RunCard.tsx:132-135):** the `Step N` value now comes from `unifiedStepCount(message)` (NOT `iterationCount + 1`). Since Task 1 routes the strip's `stepCount={unifiedStepCount(message)}`, ensure no remaining `message.iterationCount` reference drives a visible step number. (The standalone `stepLabel` const driven by `iterationCount` is removed/replaced.)

    (3) **Collapsed-row (RunCard.tsx:232-234):** `Run · {message.tool_calls?.length} tool call(s)` → `Run · {unifiedStepCount(message)} step{… === 1 ? "" : "s"}`.

    Per RESEARCH correction #5, add a code comment that `unifiedStepCount` is persisted (reads `tool_calls`, which DB-reload reconstructs) whereas `iterationCount` is omitted on reload — so the step label now survives next-day reopen. Leave the in-panel `tc.iteration` divider alone HERE (that is Plan 03's relabel to "Round N").
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/chat/RunCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "message.iterationCount" frontend/src/components/chat/RunCard.tsx` == 0 (no visible step number reads iterationCount anymore)
    - `grep -c "unifiedStepCount" frontend/src/components/chat/RunCard.tsx` ≥ 3 (header + subtitle/strip + collapsed-row)
    - `grep -E "step\b|steps\b" frontend/src/components/chat/RunCard.tsx` matches the relabeled header + collapsed-row copy
    - NEW RunCard.test.tsx case: fixture with N deduped tools across M iterations (N ≠ M) → header, strip step number, and collapsed-row all show N (== unifiedStepCount), never M
    - NEW RunCard.test.tsx case: a DB-loaded message WITHOUT iterationCount still shows a Step number (proves the next-day-reopen fix, RESEARCH #5)
    - `cd frontend && npx vitest run src/components/chat/RunCard.test.tsx` exits 0
    - Maps to 095-VALIDATION.md D-04 row pass condition: "all three read unifiedStepCount(msg) == deduped length"
  </acceptance_criteria>
  <done>All three RunCard count sites read unifiedStepCount; no visible number reads iterationCount; step label survives a DB-loaded message; D-04 test cases green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reducer state → render | RunCard renders `message.*` (chat bucket) values only; no panel store, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-095-02-01 | Tampering/XSS | RunStatusStrip activity verb | mitigate | `activityVerb` is `outerBannerLabel(...)` (a controlled enum-derived string) rendered as React text children; NO `dangerouslySetInnerHTML`. Asserted by grep. |
| T-095-02-02 | DoS (wrong/frozen value) | D-06 timer derivation | mitigate | Elapsed is `now - Date.parse(created_at)` recomputed each tick (not an accumulator) → background-tab throttling cannot freeze it; `Number.isFinite(startMs)` guard prevents NaN render. |
| T-095-02-03 | PANEL-06 isolation breach | RunCard render | accept→assert | RunCard reads ONLY `message.*`; this plan adds no read of todosByThread/tasksByThread/phasesByThread/workspaceFilesByThread/pendingAsksByThread. Asserted: no new panel-store import in RunCard/RunStatusStrip. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/chat/RunCard.test.tsx` — green incl. the new D-06 + D-04 cases.
- `cd frontend && npm test` — full suite green (no regression to ToolCallPanel/MessageItem/StreamsProvider tests).
- `cd frontend && npx tsc -b` — zero net-new type errors.
- 083 NON-REGRESSION: grep that MessageList `key={run-${...runId}` is untouched and RunCard reads `created_at` (not perf.now) — REINFORCES the 083 fix.
- PANEL-06: grep RunCard.tsx + RunStatusStrip.tsx contain NO `todosByThread|tasksByThread|phasesByThread|workspaceFilesByThread|pendingAsksByThread`.
</verification>

<success_criteria>
- The timer never vanishes: visible whenever `created_at` parses, frozen only at a true terminal (D-06, closes BUG-260528-01); REINFORCES 083.
- RunCard step label, collapsed-row, header all read `unifiedStepCount` (D-04); the label survives next-day reopen.
- One `RunStatusStrip` composes timer + count + activity; header placement wired; floating placement available for Plans 03/04.
- Full frontend suite green; PANEL-06 isolation held.
</success_criteria>

<output>
After completion, create `.planning/phases/095-chat-tool-card-unification/095-02-SUMMARY.md`
</output>
