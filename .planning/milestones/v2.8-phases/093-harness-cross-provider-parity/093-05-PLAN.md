---
phase: 093-harness-cross-provider-parity
plan: 05
type: execute
wave: 2
depends_on: [01, 03]
files_modified:
  - backend/app/services/harness_engine.py
  - backend/app/api/threads.py
  - backend/app/services/harness/phase_types.py
  - backend/app/api/panel.py
  - frontend/src/lib/api.ts
  - backend/tests/test_093_surfacing.py
autonomous: true
requirements: [PARITY-02]
user_setup: []

must_haves:
  truths:
    - "A harness run surfaces its final answer (delta + sources + citations + confidence) on live kickoff, resume, AND Continue — not just live"
    - "There is exactly ONE surfacing site and ONE persist owner per entry path (no double assistant message)"
    - "Surfacing emits delta + grounding BEFORE the terminal run_completed"
    - "The ask_user prompt carries the prior phase's draft to the durable row, the SSE event, and the /pending replay"
    - "The frontend PendingAsk shape carries draft (additive) and the api.ts phase_* dispatch stays purely additive"
    - "The live-kickoff and resume wf_ctx thread the resolved ctx model (D-04 sites 1 and 2)"
  artifacts:
    - path: "backend/app/services/harness_engine.py"
      provides: "shared surfacing helper invoked on run_workflow success terminal (D-11) + resume ctx model threading (D-04 site 2)"
      contains: "_surface_final_answer"
    - path: "backend/app/api/threads.py"
      provides: "live-kickoff surfacing block removed (now in the shared helper) + wf_ctx model threading (D-04 site 1)"
      contains: "resolve_workflow_ctx_model"
    - path: "backend/app/services/harness/phase_types.py"
      provides: "draft carried into ask_user_prompt durable row + SSE event (D-12)"
      contains: "draft"
    - path: "backend/app/api/panel.py"
      provides: "/pending replay payload carries draft (D-12)"
      contains: "draft"
    - path: "frontend/src/lib/api.ts"
      provides: "PendingAsk.draft additive field (D-12)"
      contains: "draft"
    - path: "backend/tests/test_093_surfacing.py"
      provides: "shared helper single-emit + ordering test (GREEN)"
  key_links:
    - from: "backend/app/services/harness_engine.py run_workflow terminal"
      to: "the shared surfacing helper"
      via: "emit delta+grounding then persist, before run_completed"
      pattern: "_surface_final_answer"
    - from: "backend/app/services/harness/phase_types.py _exec_llm_human_input"
      to: "the ask_user_prompt durable row + SSE event"
      via: "draft = prior phase text"
      pattern: "draft"
---

<objective>
Wire the answer surfacing into ALL THREE harness entry paths via one shared helper (D-11) and carry the ask_user draft (D-12). Today the F6/F7 surfacing (delta + sources + citations + confidence emit + persist) lives INLINE in the live-kickoff branch only (`threads.py:1268-1418`); resume (`_build_resume_context` path) and Continue (`_harness_continuation`) call `run_workflow` directly and NEVER reach it, so resumed/Continue'd workflows lose their answer. Refactor the surfacing into one shared helper invoked on `run_workflow`'s success terminal so live + resume + Continue surface identically — with exactly ONE persist owner per path (Pitfall 5 / Landmine 5). Also thread the resolved ctx model onto the live-kickoff wf_ctx (D-04 site 1) and the resume ctx (D-04 site 2), and carry the prior-phase draft into the ask_user prompt (D-12).

Purpose: SC#4 (resume + Continue surface the answer + the ask_user draft; one shared helper) + SC#1's live/resume model-threading. This plan owns `harness_engine.py` + `threads.py` + `phase_types.py` + `panel.py` + `frontend/src/lib/api.ts` (parallel with Plan 04's `runs.py` — no file overlap).
Output: The shared surfacing helper + live/resume model threading + the D-12 draft carry + the GREEN surfacing test + the per-phase event-shape contract documented for Phase 094.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/093-harness-cross-provider-parity/093-CONTEXT.md
@.planning/phases/093-harness-cross-provider-parity/093-RESEARCH.md
@.planning/phases/093-harness-cross-provider-parity/093-PATTERNS.md
@.planning/phases/093-harness-cross-provider-parity/093-VALIDATION.md

<interfaces>
<!-- The surfacing block to extract + the engine terminal + the draft-carry sites. Extracted this session. -->

The engine terminal TODAY (harness_engine.py:661-696) — sets ctx.final_output / final_source_refs / final_citations / final_confidence, then: finish_run -> write_audit run_completed -> _emit(redis, stream_run_id, "run_completed", status="completed"). It does NOT surface (no delta/sources/citations/confidence emit, no persist). `stream_run_id` is the threaded producer stream; `run_id` is the workflow_run id.

The inline live-kickoff surfacing block to EXTRACT (threads.py:1268-1418):
- reads `wf_ctx.final_output["text"]`, `wf_ctx.final_source_refs`, `wf_ctx.final_citations`, `wf_ctx.final_confidence`.
- emits on the PRODUCER stream (run:{run_id}): `delta` (content=text), `sources` (sources=refs), `citations` (citations=refs, passage truncated to 400 chars), `confidence` (level/avg_similarity/disclaimer). Each best-effort (try/except logs).
- persists: installs `_result_sink["persist"] = _persist_harness_message` (a zero-arg async callable inserting the assistant messages row via insert_assistant_message with content/source_refs/confidence). _shielded_finalize reads _result_sink["persist"], awaits it, threads the id into finalize_run.

The two surfacing-less entry paths:
- resume: `_build_resume_context` (harness_engine.py:765-836) builds the resume wf_ctx with user_settings=None, the sweep calls run_workflow directly — no threads.py block runs.
- Continue: `_harness_continuation` (runs.py:814-873, owned by Plan 04) calls run_workflow directly — no threads.py block runs. (Plan 04 does NOT add surfacing; this helper covers it via run_workflow.)

The draft sites (D-12):
- durable ask_user prompt row (phase_types.py:427-435): the `tool_calls[0]` dict {kind, tool_call_id, prompt, options, timeout_seconds, run_id} — ADD "draft".
- the ask_user_prompt SSE emit (phase_types.py:457-463): emit(redis, _stream_id, "ask_user_prompt", tool_call_id, prompt, options, timeout_seconds) — ADD draft=...
- the /pending replay payload (panel.py:144-152): the result dict {message_id, tool_call_id, prompt, options, timeout_seconds, run_id, created_at} — ADD "draft": payload.get("draft").
- the draft VALUE: the prior phase's text. _exec_llm_human_input(phase, accumulated_outputs, ctx) — scan accumulated_outputs in reverse for a dict with a non-empty "text" (mirror _collect_sub_questions at phase_types.py:479-484). The doc_qa_human `draft` phase produces {"text": <answer>}; that's the thing being confirmed.

resolve_workflow_ctx_model (from Plan 03, app.services.sub_agent_models): None -> "".
- live-kickoff wf_ctx (threads.py:1216-1257): currently NO model field; user_settings is in scope -> add model=resolve_workflow_ctx_model(user_settings).
- resume wf_ctx (harness_engine.py:791-836): user_settings=None today -> resolve returns "". Open Q2: optionally load owner settings from run["user_id"]; if too heavy on the sweep, accept "" + phase.config.model (document it).

Per-phase event-shape contract (D-12 — agree NOW so Phase 094 renders without redoing):
```
phase_started     { phase, phase_index }            # already emitted (engine)
phase_completed   { phase, phase_index }            # already emitted (engine)
phase_transition  { from_phase, to_phase }          # already emitted (engine)
ask_user_prompt   { tool_call_id, prompt, options, timeout_seconds, draft }   # D-12 ADD draft
delta             { content }                        # surfacing helper (D-11)
sources           { sources: [...] }                 # surfacing helper (F7)
citations         { citations: [{document_id, chunk_index, passage(<=400), ...}] }
confidence        { level, avg_similarity, disclaimer }
run_completed     { status }                         # already emitted (engine)
```
api.ts dispatch (frontend/src/lib/api.ts:485-575): delta/sources/citations/confidence handlers already exist (Deep drives them); PendingAsk shape gets an additive `draft` field; the phase_* dispatch stays purely additive (protect surface — do NOT alter the existing delta/sources/confidence dispatch Deep depends on).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the surfacing into one shared helper on the run_workflow terminal (D-11) + remove the inline threads.py block</name>
  <files>backend/app/services/harness_engine.py, backend/app/api/threads.py, backend/tests/test_093_surfacing.py</files>
  <read_first>
    - backend/app/services/harness_engine.py:620-696 (the engine terminal — where the helper is invoked; mirror the _shielded_finalize ordering: durable UPDATE -> persist -> terminal emit)
    - backend/app/api/threads.py:1258-1418 (the inline live-kickoff surfacing block to EXTRACT + remove in the SAME commit — the emit half + the _persist_harness_message closure + _result_sink install)
    - backend/app/api/threads.py:1419-1443 (the Deep else branch + run_agent_loop — must stay byte-identical; the helper change must NOT touch it)
    - backend/app/services/harness_engine.py:765-836 (the resume _build_resume_context — the helper now covers its surfacing via run_workflow)
    - backend/tests/test_093_surfacing.py (the RED scaffold from Plan 01 — flip it GREEN)
    - .planning/phases/093-harness-cross-provider-parity/093-PATTERNS.md (Finding 6 + Pitfall 5 — the single-persist-owner reconciliation)
  </read_first>
  <action>
Create one shared surfacing helper in `harness_engine.py` and invoke it on `run_workflow`'s success terminal, BEFORE the terminal `run_completed` emit (Pitfall 5 ordering: durable run-status UPDATE -> surfacing emit + persist -> terminal `run_completed`).

1. Add an async helper `_surface_final_answer(ctx, run_id, stream_run_id, redis, pool)` in `harness_engine.py` that:
   - reads `getattr(ctx, "final_output", {}).get("text", "")`, `final_source_refs`, `final_citations`, `final_confidence` (the engine already set these at :665-687).
   - emits on `stream_run_id` (the producer stream): `delta` (content=text) if text; `sources` if refs; `citations` (passage truncated to 400) if citations; `confidence` (level/avg_similarity/disclaimer) if confidence. Copy the EXACT emit shapes + the 400-char passage truncation from threads.py:1310-1359 verbatim — same event vocabulary, same best-effort try/except.
   - persists the assistant message DIRECTLY (resume/Continue have no `_result_sink`/`_shielded_finalize`): call `insert_assistant_message(pool, thread_id=..., user_id=..., content=_strip_nul(text), source_refs=..., confidence_level/avg_similarity/disclaimer=...)` mirroring the `_persist_harness_message` closure shape (threads.py:1393-1416). Lazy-import `insert_assistant_message` + `_strip_nul` if a harness-package import cycle would otherwise form (the engine already lazy-imports the Deep grounding helpers — match that). Return the inserted message_id.
   - thread the returned message_id into the run row: `finish_run` is already called at :691 with `(pool, run_id, "completed")`. Read its signature — if it accepts `message_id`, pass it; if not, do the assistant-message persist before `finish_run` and let finish_run's existing message_id handling stand (the engine path's producer-shell runs.message_id legitimately stays NULL per threads.py:1374-1377; the assistant `messages` row is the durable answer either way). Do NOT add a message_id column write that did not exist.
   - **SINGLE PERSIST OWNER (Landmine 5):** the helper is THE persist owner for ALL THREE paths now. Call `_surface_final_answer(ctx, run_id, stream_run_id, redis, pool)` inside `run_workflow` AFTER `finish_run` durable status (mirroring _shielded_finalize: durable UPDATE first) and BEFORE the terminal `_emit(redis, stream_run_id, "run_completed", ...)` at :696. Because the helper now persists, the live-kickoff path must NOT also persist — see step 2.

2. In `threads.py`, REMOVE the entire inline surfacing block (threads.py:1268-1418 — the `_wf_final_text`/`_wf_source_refs` reads, the `_harness_emit` delta/sources/citations/confidence emits, the `_persist_harness_message` closure, and the `_result_sink["persist"] = ...` install) in the SAME commit as the helper lands. After `await run_workflow(...)` returns on the harness branch, there should be NO surfacing code — the helper inside run_workflow did it. The harness branch's `_result_sink` must NOT carry a harness persist callable anymore (so `_shielded_finalize` does not double-persist). Confirm `_shielded_finalize` is a no-op for the harness path when `_result_sink["persist"]` is absent (read its body around threads.py:1359 — it does `if _result_sink.get("persist"): await it`, so absent = no-op; good). The Deep `else` branch + run_agent_loop + the Deep `_result_sink` flow stay byte-identical (D-14).

3. In `backend/tests/test_093_surfacing.py`, REMOVE the skip and make it GREEN: mock a redis recorder (capture `_emit` calls in order) + a mock pool (record the insert). Drive `_surface_final_answer` (or `run_workflow` with a stub ctx carrying final_output/final_* and a one-phase definition) and assert: `delta`, `sources`, `citations`, `confidence` each emitted EXACTLY ONCE and ALL before `run_completed`; the assistant message persisted EXACTLY ONCE; `run_completed` is the LAST emit and no event emitted twice (Pitfall 5 double-emit guard).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && backend/venv/Scripts/python.exe -m pytest backend/tests/test_093_surfacing.py -x && backend/venv/Scripts/python.exe -m py_compile backend/app/services/harness_engine.py backend/app/api/threads.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_surface_final_answer" backend/app/services/harness_engine.py` ≥ 2 (definition + invocation inside run_workflow)
    - The inline surfacing block is GONE from threads.py: `grep -c "_persist_harness_message" backend/app/api/threads.py` == 0 AND `grep -c "_wf_final_text" backend/app/api/threads.py` == 0
    - `grep -c "run_agent_loop" backend/app/api/threads.py` unchanged vs pre-plan (Deep else branch intact — D-14)
    - `pytest backend/tests/test_093_surfacing.py -x` exits 0: delta+sources+citations+confidence each emitted once, all before run_completed, persisted once
    - `python -m py_compile` on both files exits 0
  </acceptance_criteria>
  <done>One shared surfacing helper on the run_workflow terminal surfaces the answer (delta+grounding) before run_completed and persists exactly once for live/resume/Continue; the inline threads.py block is removed in the same change; Deep is byte-identical; the surfacing test is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Thread the resolved ctx model onto the live-kickoff wf_ctx (site 1) + the resume ctx (site 2)</name>
  <files>backend/app/api/threads.py, backend/app/services/harness_engine.py</files>
  <read_first>
    - backend/app/api/threads.py:1216-1257 (the live-kickoff wf_ctx SimpleNamespace build — user_settings in scope; add the model field)
    - backend/app/services/harness_engine.py:765-836 (the resume _build_resume_context — user_settings=None today; Open Q2)
    - backend/app/services/sub_agent_models.py (resolve_workflow_ctx_model from Plan 03)
    - .planning/phases/093-harness-cross-provider-parity/093-RESEARCH.md (D-04 the 3 build sites + Open Q2 resume guidance)
  </read_first>
  <action>
Thread the resolved ctx model onto build sites 1 (live) and 2 (resume) — site 3 (Continue) is owned by Plan 04.

1. Live-kickoff (threads.py:1216-1257): the `wf_ctx = SimpleNamespace(...)` build has `user_settings=user_settings` in scope but NO `model` field. Add `model=resolve_workflow_ctx_model(user_settings)` to the SimpleNamespace. Add `from app.services.sub_agent_models import resolve_workflow_ctx_model` near the other harness imports in threads.py (lazy import inside the harness branch is also fine, matching the existing pattern). Keep every other field (run_id, producer_run_id, inputs, supabase, spawn, per_run_task_semaphore, etc.) exactly as-is.

2. Resume (`_build_resume_context`, harness_engine.py:791-836): currently `user_settings=None`. Per Open Q2's recommendation, prefer loading the owner's effective settings from `run["user_id"]` so the resolver fires on resume too (closes part of SEED-047). If a settings loader is cleanly callable on the sweep path, load it and set `model=resolve_workflow_ctx_model(_owner_settings)` + `user_settings=_owner_settings`. If loading is too heavy on the startup sweep (it runs for every stranded run), ACCEPT `user_settings=None` + `model=resolve_workflow_ctx_model(None)` (= "") so `phase.config.model` applies, and DOCUMENT the limitation in the SUMMARY (the deterministic resume mechanics still stand; the live kill-and-resume model proof is Phase 096 EVAL-02). Do NOT block on Open Q2 — either path is acceptable; record which was chosen.

3. Phase-level precedence stays `phase.config.model or ctx.model` (phase_types._effective_model — unchanged). This only sets `ctx.model`.

Resolve-never-mutate (D-05): the resolver does not write saved settings; confirm no settings UPDATE is added on either path.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && backend/venv/Scripts/python.exe -m py_compile backend/app/api/threads.py backend/app/services/harness_engine.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "resolve_workflow_ctx_model" backend/app/api/threads.py` ≥ 1 (live-kickoff site)
    - `grep -c "resolve_workflow_ctx_model" backend/app/services/harness_engine.py` ≥ 1 (resume site — even if it resolves to "" via None)
    - The live-kickoff wf_ctx build now carries `model=` (confirm by reading the build span 1216-1257)
    - `python -m py_compile` on both files exits 0
    - No `user_settings` UPDATE / mutation is added (D-05) — grep confirms no new `.update(` on a settings table in either changed span
  </acceptance_criteria>
  <done>The live-kickoff and resume wf_ctx thread the resolved ctx model (D-04 sites 1 and 2); resolve-never-mutate preserved; the chosen resume approach (load-owner-settings vs accept-phase-model) is documented.</done>
</task>

<task type="auto">
  <name>Task 3: Carry the prior-phase draft into the ask_user prompt — durable row + SSE + /pending + PendingAsk (D-12)</name>
  <files>backend/app/services/harness/phase_types.py, backend/app/api/panel.py, frontend/src/lib/api.ts</files>
  <read_first>
    - backend/app/services/harness/phase_types.py:392-484 (_exec_llm_human_input — the durable row at :427-435, the SSE emit at :457-463; accumulated_outputs is the draft source; _collect_sub_questions at :479-484 is the reverse-scan analog)
    - backend/app/api/panel.py:103-153 (the /pending replay payload dict at :144-152 — add draft)
    - frontend/src/lib/api.ts:485-575 (the PendingAsk shape + the phase_* / ask_user dispatch — additive only; do NOT alter the delta/sources/confidence dispatch Deep depends on)
    - .planning/phases/093-harness-cross-provider-parity/093-PATTERNS.md (D-12 the draft-carry trio + the per-phase event-shape contract)
  </read_first>
  <action>
Carry the prior phase's draft (the thing being confirmed) through the 3 backend sites + the frontend shape (D-12). The draft VALUE = the latest upstream phase output text.

1. In `phase_types.py`, add a small helper near `_collect_sub_questions` (:479-484):
```python
def _latest_phase_text(accumulated_outputs: dict) -> str:
    """The most-recent upstream phase's answer text — the draft an llm_human_input
    phase asks the user to confirm (D-12). Mirrors _collect_sub_questions' reverse scan."""
    for out in reversed(list(accumulated_outputs.values())):
        if isinstance(out, dict) and isinstance(out.get("text"), str) and out["text"].strip():
            return out["text"]
    return ""
```
In `_exec_llm_human_input` (it receives `accumulated_outputs`), compute `draft = _latest_phase_text(accumulated_outputs)` near the top.

2. ADD `"draft": draft` to the durable prompt row's `tool_calls[0]` dict (phase_types.py:427-435) — keep every existing key.

3. ADD `draft=draft` to the `ask_user_prompt` SSE emit (phase_types.py:457-463) — keep every other emit kwarg.

4. In `panel.py` `/pending` (the result dict at :144-152), ADD `"draft": payload.get("draft")` (defensive `.get` — older prompt rows have no draft -> None, harmless).

5. In `frontend/src/lib/api.ts`, ADD an optional `draft?: string` field to the `PendingAsk` type/interface and parse it from the `/pending` payload + the `ask_user_prompt` SSE event into the PendingAsk object. PURELY ADDITIVE (optional field; the existing dispatch + the Deep-driven delta/sources/confidence handlers untouched — protect surface). Do NOT render the draft (the visible frame is Phase 094); just plumb it onto the shape so 094 can read it.

6. Document the per-phase event-shape contract (the block in the interfaces section) in the SUMMARY so Phase 094 renders it without re-deriving (D-12).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && backend/venv/Scripts/python.exe -m py_compile backend/app/services/harness/phase_types.py backend/app/api/panel.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "draft" backend/app/services/harness/phase_types.py` ≥ 3 (helper + durable-row key + SSE kwarg)
    - `grep -c "_latest_phase_text" backend/app/services/harness/phase_types.py` ≥ 2 (definition + call)
    - `grep -c "draft" backend/app/api/panel.py` ≥ 1 (the /pending payload field)
    - `grep -c "draft" frontend/src/lib/api.ts` ≥ 1 (the additive PendingAsk field)
    - `python -m py_compile` on both backend files exits 0
    - `cd frontend && npx tsc -b` shows ZERO net-new errors vs the 54 documented baseline (the additive optional field must not introduce a type error)
  </acceptance_criteria>
  <done>The ask_user prompt carries the prior-phase draft through the durable row, the SSE event, the /pending replay, and the PendingAsk shape (all additive); the phase_* / delta dispatch stays purely additive; tsc shows zero net-new errors; the per-phase event contract is documented for Phase 094.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness engine terminal → producer SSE stream + messages table | The surfaced answer + grounding cross into the client stream and durable storage |
| resume sweep (service-role client) → user documents | A resumed run's retrieval must stay owner-scoped despite the RLS-bypassing service-role client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-093-DOUBLE | Tampering (data integrity) | the surfacing persist (live vs helper double-persist) | mitigate | Single persist owner per path (Pitfall 5 / Landmine 5): the helper is the sole persist site; the inline threads.py block + `_result_sink` install are removed in the same change; the test asserts exactly-one persist + exactly-one of each emit |
| T-093-INFO | Information Disclosure | resume `_build_resume_context` service-role client retrieval | mitigate | Retrieval stays owner-scoped — `search_documents` filters by `current_user["id"]` set from the durable `run["user_id"]` (harness_engine.py:772 comment); threading owner settings for the resolver (Task 2 option) reads the SAME verified owner id, never widening scope |
| T-093-DEEP | Tampering (protected-path regression) | shared `threads.py` (the Deep else branch / `_result_sink` flow) | mitigate | Only the harness branch's inline block is removed; the Deep `else` + run_agent_loop + Deep `_result_sink` are untouched (D-14); `grep run_agent_loop` count unchanged |
| T-093-DRAFT | Information Disclosure | the draft carried into the ask_user prompt | accept | The draft is the SAME thread's prior-phase output, already visible to the owner; it is plumbed through the existing owner-scoped /pending + SSE surfaces (no cross-thread/cross-user exposure) |
</threat_model>

<verification>
- `pytest backend/tests/test_093_surfacing.py -x` GREEN — single-emit + ordering before run_completed (Task 1).
- The inline threads.py surfacing block is gone (`_persist_harness_message`/`_wf_final_text` count == 0); Deep else branch intact (Task 1).
- `resolve_workflow_ctx_model` threaded at the live + resume sites; no settings mutation (Task 2).
- `draft` plumbed through phase_types + panel + api.ts; tsc zero net-new (Task 3).
</verification>

<success_criteria>
- One shared surfacing helper surfaces the answer (delta+sources+citations+confidence) on live/resume/Continue, persists once, emits before run_completed (SC#4 — D-11, Pitfall 5).
- The live + resume wf_ctx thread the resolved ctx model (D-04 sites 1 + 2; SC#1's live/resume half).
- The ask_user prompt carries the prior-phase draft (durable row + SSE + /pending + PendingAsk) using the existing event vocabulary (SC#4 — D-12); the per-phase event contract is fixed for Phase 094.
- Deep byte-identical (D-14 RED LINE) — only the harness branch changes.
</success_criteria>

<output>
After completion, create `.planning/phases/093-harness-cross-provider-parity/093-05-SUMMARY.md`
</output>
