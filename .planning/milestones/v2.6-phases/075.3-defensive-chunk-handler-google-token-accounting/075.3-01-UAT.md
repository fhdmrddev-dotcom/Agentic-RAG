# Phase 075.3 Plan 01 — Chrome MCP UAT Scoreboard

> **Task 5 (Wave 2) human-verify checkpoint.** Verifies the defensive
> provider-aware chunk handler (D-075.3-03/04) + Path A revert
> (D-075.3-05) deliver content streaming AND non-NULL token rows for
> all Gemini models in `MODEL_CAPABILITIES`.

**Commits under test:**

| Commit | Task | Subject |
|--------|------|---------|
| `6d62a5e` | T1 | feat(075.3-01): add google-usage-shape probe (D-075.3-01) |
| `282df07` | T1 fix | docs(075.3-01): lock probe verdict = CUMULATIVE (D-075.3-01 + D-075.3-02 upheld) |
| `86616f8` | T2 | test(075.3-01): scaffold provider-aware chunk-handler tests (RED) (D-075.3-15/16) |
| `79afe3d` | T3 | feat(075.3-01): provider-aware chunk-handler — extract _accumulate_chunk_usage, remove early-return (D-075.3-03/04) |
| `5c2b242` | T4 | fix(075.3-01): revert Path A include_usage gate — defensive handler ships first (D-075.3-05) |

**Probe verdict (D-075.3-01 locked 2026-05-22):** CUMULATIVE — Google branch implements overwrite-last-wins. OpenAI / OpenRouter / Anthropic-via-compat / unknown providers fall through to `+=`.

---

## Pre-UAT setup

1. **Restart backend** so the latest threads.py + openai_service.py changes load:
   ```
   cd backend
   venv/Scripts/python.exe -m uvicorn app.main:app --reload --workers 1
   ```
   Confirm logs show `INFO:     Application startup complete.` with no import errors.

2. **Frontend** must be running:
   ```
   cd frontend
   npm run dev
   ```
   Confirm `http://localhost:5173/` loads.

3. **Login** to `http://localhost:5173/` via Chrome DevTools MCP with:
   - email: `fhdmrd@gmail.com`
   - password: `123456`

4. **Open Supabase SQL editor** (or `psql`) for the inline `runs` query after each chat.

---

## Per-row recipe

For each Gemini model below:

1. Open Settings → AI Model.
2. Set the "Model" field to the model_id below; click Save.
3. Start a **NEW** chat (fresh thread — DO NOT reuse a thread between rows; we want one clean run_id per row).
4. Send the listed prompt.
5. **Visual assert:** the assistant message bubble visibly streams non-empty content.
6. **Capture `run_id`:** via DevTools Network panel — look for `POST /api/threads/.../runs` or the SSE stream URL `/api/runs/{run_id}/stream`. Copy the UUID.
7. **DB query** (Supabase SQL editor):
   ```sql
   SELECT id, model, provider, input_tokens, output_tokens, status, finish_reason, created_at
   FROM runs
   WHERE id = '<captured_run_id>';
   ```
8. **Assert:** `status='completed'`, `input_tokens IS NOT NULL`, `output_tokens IS NOT NULL`, `finish_reason` populated (typically `'stop'`).

9. Paste observations into the table below (replace `TBD` placeholders).

If any row fails:
- **Empty content** → defensive handler regression. Re-check `_on_chunk_openai` is calling `_accumulate_chunk_usage` and not early-returning. Snapshot backend logs (`uvicorn` console + `tail -100 backend.log` if any).
- **NULL tokens** → `<probe_result>` verdict mismatch with `_accumulate_chunk_usage` Google branch. Re-read the helper's Google branch — should be `return _i, _o` (overwrite), NOT the `+=` fallback.
- **Both** → the gate revert (Task 4) landed BEFORE the handler change (Task 3) — but our wave-ordering should have prevented this. STOP and re-check `git log --oneline backend/app/api/threads.py backend/app/services/openai_service.py` ordering.
- File a BUG-260523-XX (or appropriate date) and BLOCK this checkpoint.

---

## Results table

Fill `content streamed?` with literal `YES` or `NO`. Fill `input_tokens` / `output_tokens` with the integer from the DB query OR the literal string `NULL` (so a `grep "NULL"` returns matches on failed rows).

When a row passes, replace the `TBD` placeholders for tokens with the actual integer values AND add the literal strings `input_tokens NOT NULL` and `output_tokens NOT NULL` in the `notes` column — the executor's `<verify>` `grep -c` automation looks for those literals.

| # | model | prompt | content streamed? | run_id | input_tokens | output_tokens | status | finish_reason | observed_at | notes |
|---|-------|--------|-------------------|--------|--------------|---------------|--------|---------------|-------------|-------|
| 1 | gemini-2.5-pro | hi | YES | d38715bd-af18-4684-a62c-8a36da3970d1 | 5388 | 9 | completed | n/a (schema has no finish_reason col) | 2026-05-22 20:30:20Z | input_tokens NOT NULL · output_tokens NOT NULL · streamed "Hello! How can I help you today?" |
| 2 | gemini-2.5-flash | count to five | YES | a417b351-2b8a-4ff4-88ba-a4c0a449458a | 5390 | 8 | completed | n/a (schema has no finish_reason col) | 2026-05-22 20:31:08Z | input_tokens NOT NULL · output_tokens NOT NULL · streamed "1\n2\n3\n4\n5" |
| 3 | gemini-2.5-flash-lite | hi | YES | 05e204e0-0e62-4c62-ac86-d0452f21f989 | 5388 | 10 | completed | n/a (schema has no finish_reason col) | 2026-05-22 20:32:52Z | input_tokens NOT NULL · output_tokens NOT NULL · streamed "Hi there! How can I help you today?" |
| 4 | gemini-3-flash-preview | hi | YES | b1bb8370-68dd-406f-af23-3299abf85789 | 5586 | 41 | completed | n/a (schema has no finish_reason col) | 2026-05-22 20:33:31Z | input_tokens NOT NULL · output_tokens NOT NULL · streamed "Hello! How can I help you today? I'm ready to search your document library…" |
| 5 | gemini-3.1-pro-preview | hi | YES | fe314c17-944d-45b5-9d76-07b0293ecf9e | 5586 | 45 | completed | n/a (schema has no finish_reason col) | 2026-05-22 20:34:04Z | input_tokens NOT NULL · output_tokens NOT NULL · streamed "Hello! How can I help you today? I can help you search through your uploaded documents…" |
| 6 | gemini-3.5-flash | hi | YES | 3369ae1c-9108-4727-95c8-2aac048576e4 | 5586 | 32 | completed | n/a (schema has no finish_reason col) | 2026-05-22 20:34:37Z | input_tokens NOT NULL · output_tokens NOT NULL · streamed "Hello! How can I help you today? Feel free to ask a question about your documents…" |

**Pass criterion:** ALL 6 rows show `content streamed? = YES` AND both token columns are non-NULL integers AND `notes` column carries `input_tokens NOT NULL` + `output_tokens NOT NULL` literals. **MET — 6/6 GREEN.**

**Schema note:** The pre-UAT recipe expected a `finish_reason` column on `runs`, but the live schema (Supabase local migrations 001–046) has columns `(run_id, thread_id, user_id, message_id, status, model, provider, started_at, completed_at, input_tokens, output_tokens, error)` — no `finish_reason`. The pass criterion was substituted with `status='completed'`, which is equivalent: a completed status implies the stream terminated cleanly (the `error` column is NULL on all 6 rows). No new D-075.3-* decision needed; this is a documentation drift in the UAT recipe, not a contract change.

---

## Resume signal

`UAT 6/6 green` — defensive provider-aware chunk handler delivers content streaming AND non-NULL token rows on every Gemini model in `MODEL_CAPABILITIES`. D-075.3-19 (Plan 01 share) satisfied. Proceeding to Plan 01 SUMMARY.md.
