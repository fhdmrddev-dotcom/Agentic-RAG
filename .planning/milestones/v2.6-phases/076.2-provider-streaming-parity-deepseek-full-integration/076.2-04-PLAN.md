---
phase: 076.2-provider-streaming-parity-deepseek-full-integration
plan: 04
type: execute
wave: 3
depends_on: ["076.2-01", "076.2-02", "076.2-03"]
files_modified:
  - .planning/phases/076.2-provider-streaming-parity-deepseek-full-integration/076.2-VALIDATION.md
autonomous: false
requirements: []
user_setup:
  - service: deepseek
    why: "DeepSeek API key for thinking mode + multi-tool UAT"
    env_vars:
      - name: DEEPSEEK_API_KEY
        source: "DeepSeek Dashboard -> API Keys"
  - service: moonshot
    why: "Kimi direct API for smoke test"
    env_vars:
      - name: MOONSHOT_API_KEY
        source: "Kimi Platform -> API Keys (platform.kimi.ai)"
  - service: minimax
    why: "MiniMax direct API for smoke test"
    env_vars:
      - name: MINIMAX_API_KEY
        source: "MiniMax Platform -> API Keys (platform.minimax.io)"
  - service: zhipu
    why: "GLM/Zhipu direct API for smoke test"
    env_vars:
      - name: ZHIPU_API_KEY
        source: "Zhipu Open Platform -> API Keys (open.bigmodel.cn)"

must_haves:
  truths:
    - "DeepSeek multi-tool run completes without 400 error (thinking mode enabled)"
    - "DeepSeek reasoning_content displays in collapsible Thinking block during streaming"
    - "DeepSeek title generation works (no refusal)"
    - "DeepSeek suggestions generate correctly"
    - "Kimi multi-tool smoke test passes via direct API"
    - "MiniMax multi-tool smoke test passes (or failure documented as provider limitation)"
    - "GLM multi-tool smoke test passes (or failure documented as provider limitation)"
    - "Skills load for all providers tested (D-11)"
    - "Sub-agent/title/suggestion model resolution uses chat's provider (D-09)"
  artifacts:
    - path: ".planning/phases/076.2-provider-streaming-parity-deepseek-full-integration/076.2-VALIDATION.md"
      provides: "UAT scoreboard with per-provider results"
  key_links:
    - from: "076.2-VALIDATION.md"
      to: "076.2-01-SUMMARY.md"
      via: "DeepSeek UAT validates Plan 01 thinking mode fix"
    - from: "076.2-VALIDATION.md"
      to: "076.2-03-SUMMARY.md"
      via: "Google Skills UAT validates Plan 03 BUG-260524-01 fix"
---

<objective>
Cross-provider live UAT with the CLAUDE.md mandatory 4-axis bandwidth.

Purpose: Validate that all code changes from Plans 01-03 work in production with real API keys. Per D-10, DeepSeek gets deep testing (thinking mode + multi-tool + streaming trace); Kimi/MiniMax/GLM get smoke tests. Per D-12, the UAT must cover cross-provider, multi-tool, parallel-thread, and long-message axes. Per D-09, sub-agent/title/suggestion model resolution is verified during UAT. Per D-11, skills load is a UAT row for all providers.

Output: 076.2-VALIDATION.md with per-provider pass/fail results and documented provider-side limitations.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/076.2-provider-streaming-parity-deepseek-full-integration/076.2-CONTEXT.md
@.planning/phases/076.2-provider-streaming-parity-deepseek-full-integration/076.2-RESEARCH.md
@.planning/phases/076.2-provider-streaming-parity-deepseek-full-integration/076.2-01-SUMMARY.md
@.planning/phases/076.2-provider-streaming-parity-deepseek-full-integration/076.2-02-SUMMARY.md
@.planning/phases/076.2-provider-streaming-parity-deepseek-full-integration/076.2-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VALIDATION.md scoreboard + run automated test suite</name>
  <files>.planning/phases/076.2-provider-streaming-parity-deepseek-full-integration/076.2-VALIDATION.md</files>
  <read_first>
    - .planning/phases/076.2-provider-streaming-parity-deepseek-full-integration/076.2-CONTEXT.md (D-10, D-11, D-12 -- UAT strategy)
    - .planning/phases/076.2-provider-streaming-parity-deepseek-full-integration/076.2-RESEARCH.md (Validation Architecture section)
    - .planning/phases/076.2-provider-streaming-parity-deepseek-full-integration/076.2-01-SUMMARY.md
    - .planning/phases/076.2-provider-streaming-parity-deepseek-full-integration/076.2-02-SUMMARY.md
    - .planning/phases/076.2-provider-streaming-parity-deepseek-full-integration/076.2-03-SUMMARY.md
  </read_first>
  <action>
**A. Run the full backend test suite to confirm no regressions:**

```bash
cd backend && python -m pytest tests/ -x -q --timeout=60
```

All existing tests must pass. Report the count.

**B. Run the frontend typecheck:**

```bash
cd frontend && npx tsc --noEmit
```

Must exit 0.

**C. Create the VALIDATION.md scoreboard:**

Create `.planning/phases/076.2-provider-streaming-parity-deepseek-full-integration/076.2-VALIDATION.md` with the following UAT rows. Mark all manual rows as `PENDING`. Fill automated gate results from steps A and B.

Scoreboard must include:
- Automated Gates table (4 gates: backend unit, backend full, frontend typecheck, DeepSeek thinking tests)
- Axis 1 Cross-Provider: DeepSeek 6 rows (DS-1..DS-6), Kimi 4 rows (KI-1..KI-4), MiniMax 3 rows (MM-1..MM-3), GLM 3 rows (GL-1..GL-3), Google 1 row (GG-1)
- Axis 2 Multi-Tool: 1 row (MT-1 -- DeepSeek search_documents + execute_code)
- Axis 3 Parallel-Thread: 1 row (PT-1 -- Thread A streaming while Thread B accepts)
- Axis 4 Long-Message: 1 row (LM-1 -- DeepSeek 50+ messages or 5KB+ prompt)
- Provider Limitations table (empty, filled during UAT)
- SEED-032 Closure Status table (Gap 1 + Gap 2)
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_deepseek_thinking.py -x -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - 076.2-VALIDATION.md exists with all 4 axes represented
    - Automated gates section has real test results filled in (not PENDING)
    - Cross-provider section has rows for DeepSeek (6), Kimi (4), MiniMax (3), GLM (3), Google (1)
    - Multi-tool section has at least 1 row
    - Parallel-thread section has at least 1 row
    - Long-message section has at least 1 row
    - SEED-032 closure status section present
  </acceptance_criteria>
  <done>VALIDATION.md scoreboard created with all D-12 axes covered. Automated gates recorded. All manual UAT rows ready for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Cross-provider live UAT with 4-axis bandwidth</name>
  <files>.planning/phases/076.2-provider-streaming-parity-deepseek-full-integration/076.2-VALIDATION.md</files>
  <read_first>
    - .planning/phases/076.2-provider-streaming-parity-deepseek-full-integration/076.2-VALIDATION.md
    - .planning/phases/076.2-provider-streaming-parity-deepseek-full-integration/076.2-CONTEXT.md (D-05, D-09, D-10, D-11, D-12)
  </read_first>
  <action>
Plans 01-03 delivered:
1. DeepSeek thinking mode enabled -- reasoning_effort="high", in-memory reasoning_content round-trip fixed, reasoning_delta SSE events streaming, MessageResponse exposes reasoning_content
2. Frontend Thinking block -- collapsible reasoning content in RunCard, SSE wiring through StreamsProvider, _mapMessageResponse mapping
3. Google Skills investigation -- BUG-260524-01 investigated with debug logging added

Operator verifies with real API keys across all 4 curated providers. Steps:

**Prerequisites:**
1. Ensure API keys set in `backend/.env`: DEEPSEEK_API_KEY, MOONSHOT_API_KEY, MINIMAX_API_KEY, ZHIPU_API_KEY
2. Restart backend (uvicorn)
3. Open http://localhost:5173/ in Chrome

**DeepSeek Deep Testing (DS-1 through DS-6):**
1. Switch to DeepSeek, select deepseek-v4-flash
2. DS-1: Simple question -- verify collapsible Thinking block appears, collapsed by default
3. DS-2: Multi-tool prompt ("Search docs then analyze with code") -- verify both tools execute, Thinking block shows reasoning per iteration (SEED-032 Gap 1)
4. DS-3: New thread -- verify title updates (no refusal)
5. DS-4: After response -- verify suggestion pills appear
6. DS-5: During response -- observe whether content streams progressively or all-at-once (D-07)
7. DS-6: "Load the [skill]" -- verify load_skill invoked (D-11)

**Kimi Smoke Test (KI-1 through KI-4):**
1. Switch to kimi-k2.6 via direct API
2. KI-1: Multi-tool prompt, KI-2: Title gen, KI-3: Suggestions, KI-4: Skills

**MiniMax Smoke Test (MM-1 through MM-3):**
1. Switch to minimax-m2.7
2. MM-1: Multi-tool, MM-2: Title gen, MM-3: Skills (document failures as limitations)

**GLM Smoke Test (GL-1 through GL-3):**
1. Switch to glm-5-flash
2. GL-1: Multi-tool, GL-2: Title gen, GL-3: Skills

**Google Skills (GG-1):**
1. Switch to gemini-2.5-flash, send "Load the [skill]" -- BUG-260524-01 verification gate

**Parallel-Thread (PT-1):**
1. Start DeepSeek response in Thread A, switch to Thread B and send prompt while A streams

**Long-Message (LM-1):**
1. In DeepSeek thread with 50+ messages or 5KB+ prompt, verify response completes with thinking

Record each result in 076.2-VALIDATION.md. Document provider-side limitations per D-05.
  </action>
  <verify>All VALIDATION.md rows filled with PASS/FAIL/DOCUMENTED. All 4 axes exercised.</verify>
  <done>Cross-provider UAT complete. DeepSeek thinking mode validated live. SEED-032 closure status determined. BUG-260524-01 status resolved. Provider limitations documented per D-05. Type "approved" with results summary or describe issues.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> 4 provider APIs | API keys sent to external services; each provider authenticates independently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-076.2-08 | Information Disclosure | API keys in .env | mitigate | Keys stored in `backend/.env` (gitignored); never logged or included in SSE events |
</threat_model>

<verification>
- 076.2-VALIDATION.md has all PENDING rows filled with PASS/FAIL/DOCUMENTED
- SEED-032 Gap 1 status updated based on DS-2 result
- BUG-260524-01 status updated based on GG-1 result
- Provider Limitations table populated with any documented limitations
</verification>

<success_criteria>
- DeepSeek: DS-1 through DS-6 all PASS (or DS-5 documented as provider behavior)
- Kimi: KI-1 through KI-4 all PASS (or failures documented)
- MiniMax: MM-1 through MM-3 PASS or documented limitations
- GLM: GL-1 through GL-3 PASS or documented limitations
- Google: GG-1 PASS or documented finding
- 4-axis bandwidth: all 4 axes exercised (D-12)
- SEED-032 closure status determined
</success_criteria>

<output>
After completion, create `.planning/phases/076.2-provider-streaming-parity-deepseek-full-integration/076.2-04-SUMMARY.md`
</output>
