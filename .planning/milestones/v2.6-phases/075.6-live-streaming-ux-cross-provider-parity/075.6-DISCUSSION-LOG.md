# Phase 075.6: Live Streaming UX + Cross-Provider Parity — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `075.6-CONTEXT.md` — this log preserves the alternatives considered.

**Date:** 2026-05-23
**Phase:** 075.6-live-streaming-ux-cross-provider-parity
**Mode:** `/gsd:discuss-phase 075.6` (SPEC-locked — focus on HOW to implement)
**Areas discussed:** Plan split & wave shape, Component file placement, UAT execution order + 4-axis bandwidth, Bug routing + re_open_trigger sweep, Sub-agent visual indicator (user-added)

---

## Plan split & wave shape

| Option | Description | Selected |
|--------|-------------|----------|
| 3 plans, 075.1-style waves | Plan 01 = backend cross-provider emit (Reqs 1-3); Plan 02 = frontend live panel + reducer (Reqs 4-5, depends on Plan 01 schema); Plan 03 = frontend UX bundle (Reqs 6-8, independent). Wave 1 = Plan 01. Wave 2 = Plan 02 + Plan 03 parallel. Mirrors 075.1 (backend root cause first). | ✓ |
| 3 plans, parallel after schema lock | Plan 01 = backend Anthropic + Google validate existing + add code_so_far. Plan 02 = OpenAI + OpenRouter adapters parallel. Plan 03 = full frontend bundle. Trades parallelism for smaller Plan 01 schema commit. | |
| 4 plans, split frontend UX bundle | Plan 01 backend; Plan 02 panel+reducer; Plan 03 sub-agent gate drop + step-collapse; Plan 04 Working badge. More atomic but adds coordination overhead. | |

**User's choice:** 3 plans, 075.1-style waves (Recommended).
**Notes:** Matches the established phase-pattern; Plan 01 commits the cross-provider field, then Plans 02+03 race ahead in parallel.

---

## Component file placement

| Option | Description | Selected |
|--------|-------------|----------|
| Flat under components/chat/ | ToolArgsLivePanel + WorkingBadge alongside ExecuteCodeBlock, OutputFileCard, ToolCallPanel, MessageItem. Matches established pattern. Reusability via component API, not folder structure. | ✓ |
| New components/streaming/ subfolder | Signals 'streaming primitive, reusable for extended thinking / eval pane'. Trade-off: folder churn for one phase. | |
| Inline sub-components inside parents | ToolArgsLivePanel local inside ToolCallPanel; WorkingBadge local inside MessageItem. Most narrowly-scoped. Closes door on extended-thinking reuse. | |

**User's choice:** Flat under components/chat/ (Recommended).
**Notes:** Promotion to a streaming/ subfolder deferred to a phase that introduces ≥3 streaming components (likely v2.7 thinking-models). Reusability via props-driven API.

---

## UAT execution order + 4-axis bandwidth

| Option | Description | Selected |
|--------|-------------|----------|
| Anthropic-first cross-provider, then composed axes | (1) cross-provider Anthropic→Google→OpenAI→OpenRouter (12-page matplotlib gallery prompt); (2) multi-tool on Anthropic; (3) parallel-thread 2 tabs same user; (4) long-message pre-seed via SQL editor. Anthropic baseline confirms panel UX before risky new-emit-path providers. | ✓ |
| OpenAI-first cross-provider | Flip provider order: OpenAI → OpenRouter → Anthropic → Google. Surface backend bugs first. Trade-off: debugging two unknowns at once. | |
| Parallel-thread + long-message FIRST | Validate per-thread state invariant before cross-provider variance. Trade-off: late discovery of provider × parallel-thread regressions. | |

**User's choice:** Anthropic-first cross-provider, then composed axes (Recommended).
**Notes:** Cross-provider sequence respects "one UX, four adapters" invariant — once panel UX confirms on the known-good emit path, OpenAI/OpenRouter runs test backend only.

---

## Bug routing — streaming-indicator-top-bottom-desync.md

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, close it — fold into 075.6 | Frontmatter: add verified_closed_by: "075.6", neutralize re_open_trigger. Working badge is strictly-better signal (top-of-bubble + branded + pinned). | ✓ |
| Keep folded into 075, badge is additional signal | Don't formally close. Badge is bonus coverage. | |

**User's choice:** Yes, close it — fold into 075.6 (Recommended).
**Notes:** Frontmatter update pending at plan-phase time.

---

## Bug routing — anthropic-end-of-cycle-shows-actions-not-summary.md

| Option | Description | Selected |
|--------|-------------|----------|
| Defer again, stay surgical | Root cause is system-prompt / terminal-frame OR block-ordering, orthogonal to 075.6 surface. Per D-075-15 + D-074-10 consistent precedent. Refresh re_open_trigger to v2.7 Agent Workspace OR focused Anthropic-path phase. | ✓ |
| Fold into 075.6 step-list collapse | Argue collapse implicitly addresses 'actions vs summary'. Trade-off: collapse is purely visual; doesn't synthesize. | |
| Surface to user during 075.6 plan-phase | Revisit at plan-phase. Trade-off: discuss-phase exists for this call. | |

**User's choice:** Defer again, stay surgical (Recommended).
**Notes:** Frontmatter re_open_trigger refresh pending at plan-phase time.

---

## Sub-agent visual indicator (user-added area)

| Option | Description | Selected |
|--------|-------------|----------|
| No badge — inline text is the signal | After Req #6 gate-drop, sub-agent text streams visibly inside the tool row. Turn-level Working badge covers parent silent windows. Sub-agent-scoped badge would be redundant noise. | ✓ |
| Tiny inline "✦ Sub-agent" label in sub-agent header | Small label rendered above streaming sub-agent text inside the tool row, gated on sub-agent active. Trade-off: visual element competing with parent row signals. | |
| Defer to follow-up phase — plant a seed | Don't add to 075.6. Plant SEED-NN-sub-agent-streaming-indicator. Trade-off: kicks the can. | |

**User's choice:** No badge — inline text is the signal (Recommended).
**Notes:** Seed planted at D-075.6-E2 with re_open_trigger pointing at felt silent windows during sub-agent first-token-time discovered in 075.6 UAT.

---

## Claude's Discretion (not interactively discussed)

- **Aggregation buffer shape (OpenAI/OpenRouter):** separate string accumulator per `tool_index` (NOT reuse of byte-count buffer).
- **Reducer behavior on tool_start:** clear `argsCodeText` to `undefined` (force re-render to `tc.args.code`).
- **Test fixture extension:** add new assertions to existing `streamsProvider.test.tsx` + `streamsProvider_067_5_regression.test.tsx`.
- **Working badge styling:** reuse Phase 068.5 brand pulse if present; otherwise new 1.5s CSS keyframe in `index.css`. Position inline at top of MessageItem.
- **Vitest file naming:** existing `frontend/src/__tests__/providers/*.test.tsx` pattern.
- **Code-review depth:** Standard (matches 075.1 + 075.4 precedent).

---

## Deferred Ideas (mentioned but not folded)

- Sub-agent visual badge → SEED with re_open_trigger
- File output card polish (T-260523-10) → next polish bundle
- Live PPTX preview pane → defer
- Extended-thinking summary collapsible block → v2.7+ thinking-models phase (will reuse ToolArgsLivePanel via component API)
- T-260523-09 byte counter removal → separate post-075.6 ship commit
- OpenAI/OpenRouter token usage parity → tracked elsewhere
- Backend buffering strategy redesign → tune later if UAT surfaces a problem
- Storing args text in messages.tool_calls JSONB → not needed (panel is in-flight only)
- Promotion to components/streaming/ subfolder → defer to phase with ≥3 streaming components
