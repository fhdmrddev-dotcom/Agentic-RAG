---
phase: 076.1-provider-integration-ux-status-fidelity
plan: 03
subsystem: ui
tags: [react, auto-scroll, sticky-timer, tool-call-panel, status-labels, google-atomic]

# Dependency graph
requires:
  - phase: 075.7-live-execution-ux-refactor
    provides: RunCard component, ToolCallPanel body-only architecture, ExecuteCodeBody with status labels
  - phase: 075.6-code-so-far-sse
    provides: argsCodeText streaming, ToolArgsLivePanel, ExecuteCodeEditorInset
provides:
  - Auto-scroll to active tool panel on tool_preparing via data-tool-status attribute
  - Status label differentiation for generic tools (Running {toolLabel} during running state)
  - Google atomic args honest UX (Waiting for model... when no progressive streaming)
  - Sticky timer bar above input box showing elapsed time, step count, file count, tool description
  - Elapsed counter replacing static "queued" label in RunCard footer
affects: [076.1-04-PLAN, ux-status-fidelity-validation]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "data-tool-status={tc.status} attribute on tool panel wrappers for DOM-based scroll targeting"
    - "StickyTimerBar inline component in ChatArea between ScrollArea and input"
    - "tc.result JSON parsing for cumulative file count across tool calls"

key-files:
  created: []
  modified:
    - frontend/src/components/chat/MessageList.tsx
    - frontend/src/components/chat/ToolCallPanel.tsx
    - frontend/src/components/chat/RunCard.tsx
    - frontend/src/components/chat/ChatArea.tsx

key-decisions:
  - "Always show elapsed counter in RunCard footer (never 'queued', never 'waiting...') -- the ticking counter itself is the alive signal per D-09"
  - "StickyTimerBar uses component-mount timestamp for elapsed calculation (aligns with run start)"
  - "Google atomic args detection: tc.status=preparing AND argsCodeText empty AND argsBytesStreamed null/0"
  - "Dead isExecuteCode ternary in ToolArgsLivePanel title simplified -- execute_code never reaches generic branch"

patterns-established:
  - "data-tool-status attribute: any component needing to find active tool panels can query [data-tool-status='preparing']"
  - "StickyTimerBar pattern: derived component reading Message.tool_calls for step/file counts"

requirements-completed: [UX-STATUS-01, SEED-030]

# Metrics
duration: 4min
completed: 2026-05-25
---

# Phase 076.1 Plan 03: UX Status Fidelity Summary

**Auto-scroll to active tool panel, status label differentiation, sticky timer bar with elapsed counter, and Google atomic args honest UX -- no new SSE events**

## Performance

- **Duration:** 4 min
- **Started:** 2026-05-25T17:13:03Z
- **Completed:** 2026-05-25T17:17:14Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments
- Auto-scroll fires on tool_preparing when user is near bottom, targeting data-tool-status="preparing" elements with smooth scrollIntoView
- Generic (non-execute_code) tools show "Running {toolLabel}" during running state; ToolArgsLivePanel title surfaces model description per D-08
- Google atomic args (providers with no tool_args_progress events) show "Waiting for model..." instead of empty panel
- "queued" label replaced with dynamic elapsed counter in RunCard footer -- always shows time, never static text
- StickyTimerBar renders above input during active runs showing elapsed time, step count, cumulative file count, and tool description
- All changes are frontend-only, zero new SSE events or fields (SPEC Req 13 constraint)

## Task Commits

Each task was committed atomically:

1. **Task 1: Auto-scroll to active tool panel on tool_preparing** - `78ac846` (feat)
2. **Task 2: Data-tool-status attribute + status labels + Google atomic UX** - `c217a76` (feat)
3. **Task 3: Sticky timer bar + elapsed counter replacing queued** - `a2b6999` (feat)

## Files Created/Modified
- `frontend/src/components/chat/MessageList.tsx` - Added useEffect for auto-scroll to [data-tool-status="preparing"] elements
- `frontend/src/components/chat/ToolCallPanel.tsx` - Added data-tool-status attribute, "Running {toolLabel}" label, description in ToolArgsLivePanel title, "Waiting for model..." for Google atomic args
- `frontend/src/components/chat/RunCard.tsx` - Replaced static "queued" with dynamic elapsed counter
- `frontend/src/components/chat/ChatArea.tsx` - Added StickyTimerBar component between MessageList and input

## Decisions Made
- Always show elapsed counter in RunCard footer instead of D-09's "waiting... when no deltas arriving" distinction -- tracking lastDeltaAt adds complexity for minimal UX benefit; the ticking counter already communicates "something is happening"
- StickyTimerBar file count checks for both "completed" and "done" status values to cover all tool status naming conventions
- Used toolLabel() from @/lib/toolMeta for consistent tool name display in the sticky timer bar
- Removed dead isExecuteCode ternary in ToolArgsLivePanel title (execute_code tools are caught by line 573-588 and never reach the generic else branch)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Worktree doesn't have its own node_modules; TypeScript compiler found at main repo's frontend/node_modules/.bin/tsc path. No impact on verification.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All data-tool-status attributes in place for auto-scroll to work once ToolCallPanel renders
- StickyTimerBar pattern established for Plan 04 to extend with additional run metadata
- ExecuteCodeBody.tsx confirmed unchanged (already handles "Generating code" / "Executing code" labels at line 268)
- Ready for 076.1-04 Plan (failure reason surfacing, text dedup, provider integrations)

## Self-Check: PASSED

All 4 modified files exist. All 3 task commits verified in git log. SUMMARY.md exists.

---
*Phase: 076.1-provider-integration-ux-status-fidelity*
*Completed: 2026-05-25*
