---
phase: 075.9-live-execution-fidelity-handoff
verified: 2026-05-24T18:05:00Z
status: human_needed
score: 4/4 must-haves verified
must_haves_total: 4
must_haves_verified: 4
must_haves_failed: 0
human_verification:
  - test: "Sub-agent dedup — Chrome MCP"
    expected: "Send a prompt that triggers an analyze sub-agent invocation. Watch mid-stream: ONE card per logical sub-agent invocation through preparing → running → done. Card-count holds steady — never flickers to 2."
    why_human: "Requires a live browser driving an actual sub-agent invocation mid-stream; programmatic tests exercise the reducer data-path but cannot verify rendered card count in a real React tree with live SSE"
  - test: "Live code stream — Chrome MCP"
    expected: "execute_code 'for i in range(10): print(i); import time; time.sleep(0.2)' — from first ~5 bytes Shiki-highlighted Python tokens visible in the editor inset, NO plain-text moment, NO blink at tool_start. Compare against Claude.ai gold standard."
    why_human: "useDeferredValue batching + Shiki WASM tokenization timing are only observable in a live browser with real SSE streaming; jsdom tests confirm the data path, not the perceived fidelity"
  - test: "Long code stream (~2 KB) — Chrome MCP"
    expected: "Paste a ~2 KB Python snippet as the code arg. Throttle smoothness: editor scrolls smoothly, no token drops or stuttering, byte counter ticks up in real time."
    why_human: "Throughput and perceived smoothness of useDeferredValue + WASM under high-frequency prop changes cannot be verified without a real browser scheduler"
  - test: "Shiki failure recovery — Chrome MCP / DevTools"
    expected: "DevTools → block import('shiki') → plain-pre fallback renders. Restore import → trigger a new execute_code call → Shiki re-attempts (singleton was reset via WR-01 fix) → highlighted HTML renders."
    why_human: "Network-level import blocking and subsequent recovery require DevTools in a real browser; the unit test exercises the singleton-reset code path in isolation, not the full recovery UX"
---

# Phase 075.9: Live-Execution Fidelity Handoff — Verification Report

**Phase Goal:** Close two felt-experience defects (sub-agent card dup mid-stream; code-stream blink at tool_start) plus 3 advisory code-review warnings from 075.8. Streaming → terminal handoff should be seamless.
**Verified:** 2026-05-24T18:05:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Stable `clientKey` stamped at first observation in StreamsProvider reducer; never mutated; `ToolCallPanel` dedup + `panelExpanded` + step-summary React key all key on `clientKey`; regression test exercises preparing→running with different `tc.id` → 1 entry | VERIFIED | `StreamsProvider.tsx:283-291` (onToolPreparing stamp) + `StreamsProvider.tsx:394-404` (onToolStart fallback stamp); `ToolCallPanel.tsx:328` (dedup chain) + `:473` (step-summary) + `:673` (panelExpanded); `streamsProvider_075_9_clientkey.test.tsx` 7/7 PASS |
| 2 | `ExecuteCodeEditorInset` reads `tc.argsCodeText ?? tc.args.code`; `ShikiCode` has `streaming` prop wrapping `code` in `useDeferredValue`; `ToolArgsLivePanel` has `hideBody` prop TRUE for execute_code preparing; test confirms cross-state stability | VERIFIED | `ExecuteCodeBody.tsx:36-37` (fallback chain + isStreaming flag); `ShikiCode.tsx:103-110` (streaming prop + useDeferredValue); `ToolArgsLivePanel.tsx:50+78` (hideBody prop + header-only mode); `ToolCallPanel.tsx:688` (hideBody={isExecuteCode}); `ExecuteCodeEditorInset.test.tsx` 5/5 PASS |
| 3 | `getHighlighter()` wraps inner IIFE in try/catch, sets `highlighterPromise = null` before rethrowing on rejection; `ShikiCode.test.tsx` mocks shiki to throw on first call and succeed on second; asserts 2 invocations + 2nd mount renders highlighted HTML | VERIFIED | `ShikiCode.tsx:52-77` (getHighlighter with try/catch + null reset on line 71); `ShikiCode.test.tsx` WR-01 test at line 62 asserts `shikiCallCount > callCountAfterFirst` AND `screen.getByTestId("shiki-code")` on second mount; 4/4 PASS |
| 4 | SECURITY CONTRACT JSDoc added on `ShikiCodeProps.code` AND above the `dangerouslySetInnerHTML` call site | VERIFIED | `ShikiCode.tsx:83-88` (ShikiCodeProps.code JSDoc) + `ShikiCode.tsx:149-163` (dangerouslySetInnerHTML call-site comment with SECURITY note) |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `frontend/src/lib/toolKey.ts` | NEW — stable client-side key util | VERIFIED | 46 lines; exports `makeToolKey` + `MakeToolKeyOptions` |
| `frontend/src/lib/__tests__/toolKey.test.ts` | NEW — 5 vitest cases | VERIFIED | 5/5 PASS |
| `frontend/src/__tests__/providers/streamsProvider_075_9_clientkey.test.tsx` | NEW — 7 vitest cases for clientKey invariant | VERIFIED | 7/7 PASS |
| `frontend/src/__tests__/components/ExecuteCodeEditorInset.test.tsx` | NEW — 5 vitest cases for byte-stable handoff | VERIFIED | 5/5 PASS |
| `frontend/src/components/chat/tool-bodies/ShikiCode.test.tsx` | NEW — 4 vitest cases (WR-01 retry + happy path) | VERIFIED | 4/4 PASS |
| `frontend/src/types/index.ts` | MODIFIED — `ToolCall.clientKey?: string` | VERIFIED | Lines 76-85; field is optional with JSDoc contract |
| `frontend/src/providers/StreamsProvider.tsx` | MODIFIED — stamp clientKey at 2 creation paths | VERIFIED | `onToolPreparing:283-291`, `onToolStart no-preparing fallback:394-404` |
| `frontend/src/components/chat/ToolCallPanel.tsx` | MODIFIED — 3 consumer sites migrated to clientKey | VERIFIED | Lines 328 (dedup), 472 (step-summary), 673 (panelExpanded) |
| `frontend/src/components/chat/ToolArgsLivePanel.tsx` | MODIFIED — `hideBody?: boolean` prop | VERIFIED | Lines 50 (prop def), 78-92 (header-only mode) |
| `frontend/src/components/chat/tool-bodies/ExecuteCodeBody.tsx` | MODIFIED — `ExecuteCodeEditorInset` exported; reads `argsCodeText ?? args.code` | VERIFIED | Lines 30-64 (exported component with fallback chain) |
| `frontend/src/components/chat/tool-bodies/ShikiCode.tsx` | MODIFIED — streaming prop + WR-01 reset + WR-02 JSDoc + test hook | VERIFIED | Lines 48-77 (reset hook + getHighlighter with catch), 83-101 (props + JSDoc), 103-110 (streaming prop + useDeferredValue) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `StreamsProvider.makeStreamCallbacks` | `toolKey.ts:makeToolKey` | `import { makeToolKey } from "@/lib/toolKey"` | WIRED | StreamsProvider.tsx:81 import, called at :283 and :394 |
| `ToolCallPanel` | `ExecuteCodeEditorInset` | `import { ExecuteCodeEditorInset }` | WIRED | ToolCallPanel.tsx mounts `<ExecuteCodeEditorInset tc={tc} />` at line 704 for execute_code preparing |
| `ExecuteCodeBody` | `ExecuteCodeEditorInset` | same-file export, called at :310 | WIRED | ExecuteCodeBody.tsx exports `ExecuteCodeEditorInset` and calls `<ExecuteCodeEditorInset tc={tc} />` in the body render at line 310 |
| `ExecuteCodeEditorInset` | `ShikiCode` (lazy) | `const ShikiCode = lazy(() => import("./ShikiCode")...)` | WIRED | ExecuteCodeBody.tsx:12-14; passes `streaming={isStreaming}` |
| `ToolCallPanel` | `ToolArgsLivePanel.hideBody` | prop `hideBody={isExecuteCode}` | WIRED | ToolCallPanel.tsx:688 |
| `ShikiCode.test.tsx` | `__resetShikiHighlighterForTests` | named import from `./ShikiCode` | WIRED | ShikiCode.test.tsx:16 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `ExecuteCodeEditorInset` | `displayCode` | `tc.argsCodeText ?? tc.args.code` | Yes — SSE stream populates `argsCodeText` during preparing; reducer copies to `args.code` at tool_start | FLOWING |
| `ShikiCode` | `html` | `getHighlighter().then(hl => hl.codeToHtml(effectiveCode, ...))` | Yes — real Shiki WASM tokenization | FLOWING |
| `ToolArgsLivePanel (hideBody=true)` | title, byteCount | Caller-provided from `tc.argsCodeText` and `tc.argsBytesStreamed` | Yes — live SSE values | FLOWING |
| `ToolCallPanel.deduplicatedToolCalls` | `key` | `tc.clientKey ?? tc.id ?? composite` | Yes — clientKey stamped by reducer at creation | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `makeToolKey` determinism | `npx vitest run src/lib/__tests__/toolKey.test.ts` | 5/5 PASS | PASS |
| clientKey preserved across state transitions | `npx vitest run streamsProvider_075_9_clientkey.test.tsx` | 7/7 PASS | PASS |
| ExecuteCodeEditorInset byte-stable handoff | `npx vitest run ExecuteCodeEditorInset.test.tsx` | 5/5 PASS | PASS |
| ShikiCode WR-01 retry after rejection | `npx vitest run ShikiCode.test.tsx` | 4/4 PASS | PASS |
| TypeScript type safety | `npx tsc --noEmit` | 0 errors | PASS |
| Pre-existing regression check | `npx vitest run` (full suite) | 11 failures (same pre-existing count) / 312 passing | PASS (no new regressions) |

### Failure Criteria Check (from PLAN.md §"How we'd know this failed")

| Failure Criterion | Test Coverage | Status |
|-------------------|--------------|--------|
| Sub-agent dup persisting mid-stream — test must exercise preparing→running with different tc.id | `streamsProvider_075_9_clientkey.test.tsx` line 103: `onToolPreparing("analyze_document", 0)` → `onToolStart("analyze_document", {...})` where tc.id changes; asserts `tool_calls.length === 1` and `keyAtRunning === keyAtPreparing` | EXERCISED |
| Code-stream blink at tool_start — test must assert byte-stable content | `ExecuteCodeEditorInset.test.tsx` test 3 ("preparing → running transition"): renders both states, asserts gutter line count is identical (same source text → same DOM shape) | EXERCISED |
| Shiki promise stuck on reject — test must verify subsequent mounts re-attempt | `ShikiCode.test.tsx` WR-01 test: first mount fails, `shikiCallCount === 1`; second mount (`shikiShouldFail = false`) asserts `shikiCallCount > callCountAfterFirst` AND `screen.getByTestId("shiki-code")` renders | EXERCISED |

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `ToolCallPanel.tsx:325` | `// REMOVE after migration window` comment on composite dedup fallback | Info | Intentional tracked debt — plan explicitly called for it; composite fallback is defense-in-depth for DB-loaded historical messages without clientKey stamps |
| `ShikiCode.tsx:48` | `__resetShikiHighlighterForTests` exported from production module | Info (WR-01 from 075.9 REVIEW) | Double-underscore convention discourages but does not prevent production callers; worst case = redundant WASM init. Follow-up can move to sibling testUtils module |
| `StreamsProvider.tsx:283,394` | `provider` field falls through to `"unknown"` in makeToolKey | Info (WR-02 from 075.9 REVIEW) | Already collision-stable without provider (messageId + name + observedAt + index); aspirational provider field in docstring is noted. Not a bug. |

None of the above are blockers.

### Human Verification Required (G-4 MANDATORY — Phase touches live UI)

Per CLAUDE.md G-4: any phase touching user-visible UI requires operator-defined Chrome MCP scenarios. The executor delegated all 4 scenarios to the verifier. These must be driven in a live browser before marking the phase fully closed.

#### 1. Sub-agent dedup — Chrome MCP

**Test:** Send "use the analyze sub-agent to summarize this folder" (or equivalent sub-agent-triggering prompt) against any provider that supports sub-agents.
**Expected:** ONE card per logical sub-agent invocation, visible through preparing → running → done. Card count never flickers to 2 mid-stream.
**Why human:** Requires live browser driving actual SSE mid-stream; programmatic tests confirm reducer data-path correctness, not rendered card count in a live React tree.

#### 2. Live code stream — Chrome MCP

**Test:** Send `execute_code "for i in range(10): print(i); import time; time.sleep(0.2)"` against any provider.
**Expected:** From first ~5 bytes, Shiki-highlighted Python tokens visible in the editor inset. NO plain-text moment. NO blink at tool_start. Visual parity with Claude.ai's live-code execution rendering.
**Why human:** useDeferredValue batching + Shiki WASM tokenization timing are only observable in a live browser; jsdom tests confirm data path only.

#### 3. Long code stream (~2 KB) — Chrome MCP

**Test:** Paste a ~2 KB Python snippet as the code argument to execute_code.
**Expected:** Throttle smoothness — editor scrolls smoothly as bytes append, no token drops or stuttering, byte counter ticks up in real time.
**Why human:** Perceived smoothness of high-frequency prop changes through useDeferredValue + WASM tokenizer cannot be verified without a real browser scheduler.

#### 4. Shiki failure recovery — Chrome MCP / DevTools

**Test:** DevTools → block `import("shiki")` (network block or ad-blocker rule). Trigger an execute_code call. Confirm plain-pre fallback renders. Restore the import. Trigger another execute_code call (new chat or existing thread).
**Expected:** Second execute_code shows Shiki-highlighted HTML — singleton reset (WR-01) allows re-attempt. No permanent degradation to plain-pre.
**Why human:** Network-level import blocking and subsequent recovery require DevTools in a real browser; unit test exercises code path, not the full recovery UX.

### Gaps Summary

No code-level gaps. All 4 must-haves are implemented and verified in the codebase. All 3 failure-criteria tests correctly exercise the described failure modes. The 4 human verification items above are the only remaining gate for full phase closure — these are standard G-4 requirements for a UI-touching phase, not implementation gaps.

---

_Verified: 2026-05-24T18:05:00Z_
_Verifier: Claude (gsd-verifier)_
