---
phase: 079-d-v2-5-02-supersession-multi-worker-enable
plan: 02
type: execute
wave: 2
depends_on: [079-01]
files_modified:
  - supabase/full-schema.sql
autonomous: false
requirements: [WORKER-LIFT-03, WORKER-LIFT-01]

must_haves:
  truths:
    - "Migration 052 applied to live local DB and full-schema.sql regenerated"
    - "uvicorn starts with --workers 2 without crash"
    - "Two concurrent chats complete without data corruption"
    - "Phase 077 test suite passes under WORKER_COUNT=2 config"
    - "Backpressure endpoint reports correct per-worker counts"
  artifacts:
    - path: "supabase/full-schema.sql"
      provides: "Regenerated schema including spawned_by_worker column"
      contains: "spawned_by_worker"
  key_links:
    - from: "supabase/migrations/052_runs_worker_id.sql"
      to: "supabase/full-schema.sql"
      via: "regenerate-full-schema.sh"
      pattern: "spawned_by_worker"
---

<objective>
Apply migration 052 via Supabase SQL editor, regenerate full-schema.sql, then perform live verification: start backend with WORKER_COUNT=2, run two-tab smoke test, re-run Phase 077 test suite, and spot-check backpressure endpoint.

Purpose: Prove the formal multi-worker enablement works in practice on the real local environment. The checkpoint gates ensure the user pastes the migration and confirms the live behavior.

Output: Applied migration, regenerated schema, verified multi-worker operation.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/079-d-v2-5-02-supersession-multi-worker-enable/079-CONTEXT.md
@.planning/phases/079-d-v2-5-02-supersession-multi-worker-enable/079-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Apply migration 052 via Supabase SQL editor</name>
  <files>supabase/migrations/052_runs_worker_id.sql</files>
  <read_first>
    - supabase/migrations/052_runs_worker_id.sql (the migration to apply)
  </read_first>
  <action>
Present the migration SQL to the user for paste into the Supabase SQL editor. The migration adds a nullable TEXT column `spawned_by_worker` to the `runs` table. The user must:
1. Open the Supabase SQL editor (local dashboard at http://127.0.0.1:54323)
2. Paste the contents of `supabase/migrations/052_runs_worker_id.sql`:
   ```sql
   ALTER TABLE public.runs ADD COLUMN IF NOT EXISTS spawned_by_worker TEXT;
   COMMENT ON COLUMN public.runs.spawned_by_worker IS
     'OS PID of the uvicorn worker that INSERTed this run. Populated at INSERT time (Phase 079). NULL for pre-079 runs.';
   ```
3. Click Run
4. Verify via: `SELECT column_name, data_type FROM information_schema.columns WHERE table_name = 'runs' AND column_name = 'spawned_by_worker';`
  </action>
  <verify>
    <automated>MISSING -- human action: user confirms migration applied in SQL editor</automated>
  </verify>
  <done>User confirms migration 052 applied successfully in Supabase SQL editor. Column `spawned_by_worker TEXT` exists on `runs` table.</done>
  <acceptance_criteria>
    - Query `SELECT column_name FROM information_schema.columns WHERE table_name='runs' AND column_name='spawned_by_worker'` returns 1 row
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Regenerate full-schema.sql</name>
  <read_first>
    - supabase/full-schema.sql (current state)
  </read_first>
  <files>supabase/full-schema.sql</files>
  <action>
Run `bash scripts/regenerate-full-schema.sh` to rebuild `supabase/full-schema.sql` from the live local DB (which now includes migration 052).

Do NOT pass `--reset` — we want a live DB dump, not a destructive rebuild.

After regeneration, verify the file contains the new column:
```bash
grep "spawned_by_worker" supabase/full-schema.sql
```

Expected: at least one line showing the column definition in the `runs` table.
  </action>
  <verify>
    <automated>grep -c "spawned_by_worker" supabase/full-schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/full-schema.sql` contains `spawned_by_worker` (grep returns >= 1)
    - File was regenerated AFTER migration 052 was applied (check file modification time is recent)
  </acceptance_criteria>
  <done>full-schema.sql regenerated with spawned_by_worker column present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live verification — multi-worker startup + smoke test + backpressure check</name>
  <files>backend/.env</files>
  <read_first>
    - backend/.env.example (WORKER_COUNT setting)
    - .planning/phases/079-d-v2-5-02-supersession-multi-worker-enable/079-CONTEXT.md (D-15 through D-17 verification scope)
  </read_first>
  <action>
Instruct the user to perform the following live verification steps. Claude prepares the verification script and SQL queries in advance.

**Pre-step:** Ensure `WORKER_COUNT=2` is set in `backend/.env`.

**Step 1 — Multi-worker startup:** User starts backend with `--workers 2`. Verify two worker PIDs appear in uvicorn logs. No crash within 30s.

**Step 2 — Two-tab smoke test (D-15):** User opens two browser tabs at http://localhost:5173, starts a chat in Tab A, starts a different chat in Tab B while Tab A streams. Both complete.

**Step 3 — spawned_by_worker column:** User runs SQL: `SELECT run_id, spawned_by_worker, status FROM runs ORDER BY started_at DESC LIMIT 5;` — new runs show non-NULL PID strings.

**Step 4 — Backpressure endpoint (D-17):** User hits `GET http://localhost:8000/admin/backpressure` with auth. Verify `per_worker_run_count` present.

**Step 5 — Phase 077 re-run (D-16, optional):** User runs `pytest tests/integration/test_077*.py -v`. All tests pass.
  </action>
  <verify>
    <automated>MISSING -- human verification: user reports results of Steps 1-5</automated>
  </verify>
  <done>User reports: Step 1 pass (2 workers started), Step 2 pass (both chats complete), Step 3 pass (spawned_by_worker populated), Step 4 pass/skipped, Step 5 pass/skipped.</done>
  <acceptance_criteria>
    - Step 1: uvicorn starts with 2 worker PIDs visible in logs, no crash within 30s
    - Step 2: Both tabs complete chats without error or data corruption
    - Step 3: New runs have non-NULL spawned_by_worker; historical runs show NULL
    - Step 4: Backpressure endpoint returns JSON with per_worker_run_count field
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL editor to production DB | Migration applied manually to live local DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-079-04 | Tampering | Migration 052 SQL paste | mitigate | `IF NOT EXISTS` guard makes migration idempotent; re-run is safe |
| T-079-05 | Denial of Service | --workers 2 resource usage | accept | 2 workers is conservative; memory impact is ~2x single worker; acceptable for dev/prod |
</threat_model>

<verification>
- `grep "spawned_by_worker" supabase/full-schema.sql` returns matches
- User reports Steps 1-3 pass
- New runs in DB have non-NULL spawned_by_worker values
</verification>

<success_criteria>
Migration 052 applied, full-schema.sql regenerated, multi-worker startup verified, two-tab smoke test passed, spawned_by_worker column populating correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/079-d-v2-5-02-supersession-multi-worker-enable/079-02-SUMMARY.md`
</output>
