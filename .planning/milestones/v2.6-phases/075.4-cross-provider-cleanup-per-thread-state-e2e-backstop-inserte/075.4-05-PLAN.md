---
phase: 075.4
plan: 05
type: execute
wave: 0
files_modified:
  - CLAUDE.md
  - frontend/playwright.config.ts
  - frontend/tests/e2e/fixtures/auth.fixture.ts
  - frontend/tests/e2e/fixtures/db-teardown.fixture.ts
  - frontend/tests/e2e/fixtures/langsmith.fixture.ts
  - scripts/restart-backend.sh
  - scripts/restart-backend.ps1
  - .github/workflows/frontend-tests.yml
  - backend/tests/conftest.py
  - backend/app/api/health.py
  - backend/app/main.py
  - frontend/package.json
  - .planning/reported-bugs/composer-locked-globally-during-any-stream.md
  - .planning/reported-bugs/gemini-3-thought-signature-missing-on-tool-rounds.md
  - .planning/reported-bugs/openrouter-duplicate-output-and-delayed-finish.md
  - .planning/reported-bugs/empty-response-fallback-misleading-iter-count.md
  - .planning/reported-bugs/final-outputs-backend-omits-url-in-event-payload.md
  - .planning/reported-bugs/final-outputs-pinned-panel-no-download-link.md
  - .planning/reported-bugs/anthropic-excessive-tool-iterations-on-multi-step-tasks.md
autonomous: true
success_criteria_addressed: [SC#9, SC#10]

must_haves:
  truths:
    - "CLAUDE.md has new ## UAT scoreboard recipe (MANDATORY) section enforcing 4-axis bandwidth"
    - "Playwright installed with config + 3 fixtures (auth env-driven, db-teardown localhost-gated, langsmith poller)"
    - "restart-backend.{ps1,sh} kills+relaunches uvicorn; Windows orphan-worker aware"
    - "/health endpoint returns 200 with {status: 'ok'}"
    - "frontend-tests.yml runs vitest + Playwright on PR; uses secrets.LANGSMITH_API_KEY"
    - "backend/tests/conftest.py has fk_aware_runs_factory (consumed by Plan 06 Task 3)"
    - "5 bug reports flipped to folded_into: 075.4; BUG-260523-04 stays deferred; BUG-260521-02 frontmatter notes auto-close"
  artifacts:
    - path: "CLAUDE.md"
      provides: "## UAT scoreboard recipe (MANDATORY)"
      contains: "cross-provider × multi-tool × parallel-thread × long-message"
    - path: "frontend/playwright.config.ts"
      provides: "Playwright config"
      min_lines: 20
    - path: ".github/workflows/frontend-tests.yml"
      provides: "vitest + Playwright CI"
      min_lines: 50
    - path: "scripts/restart-backend.ps1"
      provides: "Windows uvicorn restart"
      min_lines: 15
    - path: "backend/app/api/health.py"
      provides: "/health endpoint"
      contains: '"/health"'
    - path: "backend/tests/conftest.py"
      provides: "fk_aware_runs_factory fixture for Plan 06 Task 3"
      contains: "fk_aware_runs_factory"
  key_links:
    - from: "frontend/tests/e2e/fixtures/db-teardown.fixture.ts"
      to: "process.env.SUPABASE_URL contains 'localhost' or '127.0.0.1'"
      via: "hard-gate at fixture entry"
      pattern: "(localhost|127\\.0\\.0\\.1)"
    - from: ".github/workflows/frontend-tests.yml"
      to: "secrets.LANGSMITH_API_KEY"
      via: "GitHub Actions secret + auto-redaction"
      pattern: "secrets\\.LANGSMITH_API_KEY"
---

<objective>
**Wave 0 — Test infra bootstrap** so Plans 01-04 author UAT scoreboards under the new CLAUDE.md normative rule, and Plan 06 (Wave 3) inherits the substrate:

1. CLAUDE.md normative UAT recipe rule (SC#10).
2. Playwright config + 3 fixtures (auth, db-teardown, langsmith) — fixtures consumed by Plan 06 scenarios.
3. restart-backend.{ps1,sh} scripts.
4. .github/workflows/frontend-tests.yml CI.
5. /health endpoint.
6. backend/tests/conftest.py FK-aware `fk_aware_runs_factory` (consumed by Plan 06 Task 3 to close FK-violation cluster).
7. Bug-report frontmatter updates (5 folded + 1 deferred + 1 auto-close).

**Note:** The Wave 3 portion (6 Playwright scenarios + 95-failure triage + FK-fix cluster + operator UAT sweep) was split out at plan-phase time into `075.4-06-PLAN.md`. Plan 06 depends on Plans 01-04 AND Plan 05's bootstrap.

Purpose: After 075.4 ships, Phases 076 → 082.5 proceed without insert-phases — the test backstop + normative UAT rule catch what phase-internal UATs have been missing.
Output (this plan): 4 frontend fixture/config files + 2 scripts + 1 CI workflow + 1 backend route + 1 conftest fixture + 7 bug-report frontmatter updates.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-CONTEXT.md
@.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-RESEARCH.md
@.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-PATTERNS.md
@.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-VALIDATION.md

@CLAUDE.md
@.github/workflows/backend-tests.yml
@scripts/regenerate-full-schema.sh

<interfaces>
SC#10 verbatim from ROADMAP:
"Any phase touching streaming, agent loop, provider routing, or UI state MUST include UAT rows for cross-provider × multi-tool × parallel-thread × long-message scenarios."

6 E2E scenarios per RESEARCH §Plan 05 + AUDIT:
1. scenario-01-parallel-composers — Thread A streaming + Thread B composer accepts input (BUG-260523-01)
2. scenario-02-gemini-thought-signature — gemini-3-flash-preview 2+ tool rounds, no 400 (BUG-260523-02)
3. scenario-03-openrouter-final-pptx — OpenRouter pptx-with-charts, exactly 1 final pptx (BUG-260523-03a)
4. scenario-04-done-latency — SSE done → UI settled < 500ms (BUG-260523-03b)
5. scenario-05-unknown-model-inference — gpt-99-mega under OpenAI, inference fallback (Phase 075.3 Plan 02 guard)
6. scenario-06-iteration-parity — same prompt × 4 providers, counts within ~50% (BUG-260523-04 measurement only)

Backend pytest baseline (RESEARCH §Plan 05, 2026-05-23): 95 failed / 625 passed / 5 skipped of 728 collected.
Top failing files: test_retrieval_service.py (15), test_sql_service.py (12), test_threads_skills.py (11), test_explorer_agent.py (6), test_multimodal_query.py (5), test_provider_router.py (4 FK), test_075_code_stdout_progressive.py (4), test_066_per_call_timer.py (4).

Bug routing table (CONTEXT.md §Reported Bug Routing):
- BUG-260523-01..03 + BUG-260522-01..02 → folded_into: "075.4", status: folded
- BUG-260523-04 → status: deferred (E2E #6 measures only; root-cause fix post-075.4)
- BUG-260521-02 → status: partial-closed (current); re_open_trigger field added
</interfaces>
</context>

<tasks>

<task type="auto" wave="0">
  <name>Task 1 (Wave 0): CLAUDE.md normative UAT recipe rule</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (locate `## Reported bugs cross-check (MANDATORY)` heading for insertion below)
    - .planning/phases/075.4-.../075.4-PATTERNS.md §`CLAUDE.md (modify — Wave 0 normative UAT recipe rule, ~30 LOC)` (verbatim shape)
  </read_first>
  <action>
    Insert a NEW `## UAT scoreboard recipe (MANDATORY)` section directly BELOW the existing `## Reported bugs cross-check (MANDATORY)` section. Verbatim shape from PATTERNS.md (sized 25-35 lines). Must contain (a) blockquote of SC#10 verbatim, (b) 4-axis bandwidth table (Cross-provider / Multi-tool / Parallel-thread / Long-message), (c) closing rule that UAT rows MUST be in VALIDATION.md not PLAN.md.
  </action>
  <verify>
    <automated>grep -c "## UAT scoreboard recipe (MANDATORY)" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## UAT scoreboard recipe (MANDATORY)" CLAUDE.md` returns 1
    - `grep -c "cross-provider × multi-tool × parallel-thread × long-message" CLAUDE.md` returns >= 1
    - `grep -c "ROADMAP SC#10" CLAUDE.md` returns >= 1
    - `grep -A 50 "## UAT scoreboard recipe (MANDATORY)" CLAUDE.md | grep -c "OpenAI, Anthropic, Google, OpenRouter"` returns >= 1
    - Placed AFTER `## Reported bugs cross-check (MANDATORY)`: `awk '/## Reported bugs cross-check/{found=1} found && /## UAT scoreboard recipe/{print "OK"; exit}' CLAUDE.md` prints `OK`
  </acceptance_criteria>
  <done>CLAUDE.md has a new MANDATORY section enforcing the 4-axis UAT recipe; placed below the bugs cross-check rule.</done>
</task>

<task type="auto" wave="0">
  <name>Task 2 (Wave 0): Playwright bootstrap — config + 3 fixtures + package.json devDep</name>
  <files>frontend/playwright.config.ts, frontend/tests/e2e/fixtures/auth.fixture.ts, frontend/tests/e2e/fixtures/db-teardown.fixture.ts, frontend/tests/e2e/fixtures/langsmith.fixture.ts, frontend/package.json</files>
  <read_first>
    - frontend/package.json (verify `@playwright/test` is NOT yet devDep)
    - frontend/vitest.config.ts (ensure `tests/e2e/**/*.spec.ts` does NOT overlap vitest's glob)
    - .planning/phases/075.4-.../075.4-PATTERNS.md §`frontend/playwright.config.ts + frontend/tests/e2e/*.spec.ts (create)` + §Security Threat Surfaces Plan 05
    - reference_local_dev_app.md memory (test login fhdmrd@gmail.com / 123456)
  </read_first>
  <action>
    **Step 1:** `cd frontend && npm install --save-dev @playwright/test && npx playwright install --with-deps chromium`. Add `"e2e": "playwright test"` to scripts in package.json.

    **Step 2 — playwright.config.ts (≥20 lines):**
    ```typescript
    import { defineConfig } from '@playwright/test'
    export default defineConfig({
      testDir: './tests/e2e',
      testMatch: /scenario-\d+-.+\.spec\.ts$/,
      fullyParallel: false,  // DB-teardown shared state
      forbidOnly: !!process.env.CI,
      retries: process.env.CI ? 2 : 0,
      workers: 1,
      reporter: process.env.CI ? 'github' : 'list',
      use: {
        baseURL: process.env.E2E_BASE_URL || 'http://localhost:5173',
        trace: 'on-first-retry',
        screenshot: 'only-on-failure',
      },
      projects: [{ name: 'chromium', use: { browserName: 'chromium' } }],
    })
    ```

    **Step 3 — auth.fixture.ts:** env-driven creds (NOT committed) — `process.env.E2E_USER_EMAIL` / `E2E_USER_PASSWORD` with `fhdmrd@gmail.com`/`123456` defaults; Playwright `test.extend` pattern yielding `authedPage` that logs in via `/login` form fill+submit, waits for redirect away from `/login`.

    **Step 4 — db-teardown.fixture.ts:** export `teardownTestUserData(email)` + `assertNoOrphanedStreamingRuns(email)`. CRITICAL hard-gate at top of both functions: if `process.env.SUPABASE_URL` does NOT contain `'localhost'` or `'127.0.0.1'`, throw with message `"db-teardown refusing to run: SUPABASE_URL must contain 'localhost' or '127.0.0.1'"`. Use `@supabase/supabase-js` admin client with `SUPABASE_SERVICE_ROLE_KEY`; find user via `supabase.auth.admin.listUsers()`; delete runs → messages → threads in FK-safe order. `assertNoOrphanedStreamingRuns` queries runs.status='streaming' for the user and throws if any rows.

    **Step 5 — langsmith.fixture.ts:** export `pollLangSmithTrace(runId, opts)` + `assertLangSmithTraceExists(runId, providerExpected?)`. Polls `https://api.smith.langchain.com/runs/{run_id}` with `X-API-Key: $LANGSMITH_API_KEY`. Exponential backoff: `[1000, 2000, 4000, 4000, 4000]ms` (5 attempts, ~15s ceiling). If `process.env.LANGSMITH_API_KEY` unset, returns null + console.warn (soft-skip in dev; CI sets secret). On 200 response, validates `trace.run_type === "llm"` AND if `providerExpected` passed, validates `trace.metadata.provider || trace.metadata.ls_provider` matches case-insensitive.
  </action>
  <verify>
    <automated>cd frontend && cat package.json | grep -c "@playwright/test" && test -f playwright.config.ts && test -f tests/e2e/fixtures/auth.fixture.ts && test -f tests/e2e/fixtures/db-teardown.fixture.ts && test -f tests/e2e/fixtures/langsmith.fixture.ts && echo "all-present"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"@playwright/test"' frontend/package.json` returns >= 1
    - `test -f frontend/playwright.config.ts`
    - `test -f frontend/tests/e2e/fixtures/auth.fixture.ts`
    - `test -f frontend/tests/e2e/fixtures/db-teardown.fixture.ts`
    - `test -f frontend/tests/e2e/fixtures/langsmith.fixture.ts`
    - `wc -l frontend/playwright.config.ts` returns >= 20
    - `grep -c "process.env.E2E_USER_EMAIL\|process.env.E2E_USER_PASSWORD" frontend/tests/e2e/fixtures/auth.fixture.ts` returns >= 2
    - `grep -c "localhost\|127.0.0.1" frontend/tests/e2e/fixtures/db-teardown.fixture.ts` returns >= 2
    - `grep -c "process.env.LANGSMITH_API_KEY" frontend/tests/e2e/fixtures/langsmith.fixture.ts` returns >= 1
    - `grep -cE "1000.*2000.*4000|backoff|delays" frontend/tests/e2e/fixtures/langsmith.fixture.ts` returns >= 1
    - `cd frontend && npx playwright --version 2>&1` succeeds
    - `cd frontend && npx tsc --noEmit 2>&1 | grep -cE "playwright\.config|tests/e2e/fixtures"` returns 0
  </acceptance_criteria>
  <done>Playwright installed; config + 3 fixtures landed; creds env-driven; localhost hard-gate in db-teardown; LangSmith poller exponential backoff; vitest glob unchanged.</done>
</task>

<task type="auto" wave="0">
  <name>Task 3 (Wave 0): restart-backend.{ps1,sh} + /health endpoint</name>
  <files>scripts/restart-backend.sh, scripts/restart-backend.ps1, backend/app/api/health.py, backend/app/main.py</files>
  <read_first>
    - scripts/regenerate-full-schema.sh (header convention)
    - backend/app/main.py (find router-mount pattern; grep for existing /health endpoint)
    - .planning/phases/075.4-.../075.4-PATTERNS.md §`scripts/restart-backend.sh (create)` + §`scripts/restart-backend.ps1 (create)` (verbatim shapes)
  </read_first>
  <action>
    **Step 1 — scripts/restart-backend.sh** per PATTERNS.md verbatim: shebang + `set -euo pipefail` + `pkill -f 'uvicorn.*app.main:app'` + `sleep 2` + relaunch via `source venv/bin/activate; nohup uvicorn ...`. Header comment cites FORWARD-REF #2 + Phase 079. `chmod +x` after creation.

    **Step 2 — scripts/restart-backend.ps1** per PATTERNS.md verbatim: `Get-CimInstance Win32_Process -Filter "Name='python.exe'" | Where-Object { $_.CommandLine -match 'uvicorn.*app\.main:app' } | ForEach-Object { Stop-Process -Id $_.ProcessId -Force }`; Start-Sleep 2; Start-Process relaunch with venv activate. Header cites FORWARD-REF #2.

    **Step 3 — /health endpoint.** First grep `backend/app/api/` for existing `/health` route. If absent, create `backend/app/api/health.py`:
    ```python
    from fastapi import APIRouter
    router = APIRouter(tags=["health"])

    @router.get("/health")
    async def health() -> dict:
        """Plan 075.4-05 Wave 0 — minimal health probe."""
        return {"status": "ok"}
    ```
    In `backend/app/main.py` find existing `app.include_router(...)` calls; add `from .api import health as health_router; app.include_router(health_router.router)`. If /health already exists, ensure response includes `status` key.
  </action>
  <verify>
    <automated>test -f scripts/restart-backend.sh && test -f scripts/restart-backend.ps1 && cd backend && venv/Scripts/python -c "from fastapi.testclient import TestClient; from app.main import app; c = TestClient(app); r = c.get('/health'); assert r.status_code == 200 and 'status' in r.json(); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/restart-backend.sh` AND `test -f scripts/restart-backend.ps1`
    - `wc -l scripts/restart-backend.sh` returns >= 10; `wc -l scripts/restart-backend.ps1` returns >= 15
    - `grep -c "FORWARD-REF #2\|Phase 079" scripts/restart-backend.sh` returns >= 1
    - `grep -c "FORWARD-REF #2\|Phase 079" scripts/restart-backend.ps1` returns >= 1
    - `grep -c "pkill -f 'uvicorn" scripts/restart-backend.sh` returns 1
    - `grep -c "Get-CimInstance Win32_Process" scripts/restart-backend.ps1` returns 1
    - `cd backend && venv/Scripts/python -c "from fastapi.testclient import TestClient; from app.main import app; c = TestClient(app); r = c.get('/health'); assert r.status_code == 200 and 'status' in r.json(); print('OK')"` prints `OK`
  </acceptance_criteria>
  <done>POSIX + PowerShell restart scripts ship with FORWARD-REF #2 header; /health endpoint returns 200 with {"status": "ok"}.</done>
</task>

<task type="auto" wave="0">
  <name>Task 4 (Wave 0): FK-aware runs fixture in backend/tests/conftest.py</name>
  <files>backend/tests/conftest.py</files>
  <read_first>
    - backend/tests/conftest.py (current state — may not exist at top-level; only tests/integration/conftest.py)
    - backend/tests/test_provider_router.py (failure pattern: ForeignKeyViolationError runs_thread_id_fkey)
    - backend/tests/integration/test_066_langsmith_clean.py (same FK pattern, 2 fails)
    - PROJECT.md Phase 073-04 Rule-1 deviation (FK-safe seed: auth.users → threads → runs)
  </read_first>
  <action>
    Read existing `backend/tests/conftest.py` (may be empty or absent). Add an FK-aware factory fixture for `runs` rows that auto-creates parent `threads` row + seeds `auth.users` row. Pattern from PROJECT.md Phase 073-04 Rule-1 deviation:

    ```python
    import pytest_asyncio
    import uuid

    @pytest_asyncio.fixture
    async def fk_aware_runs_factory(_supabase):  # adapt to your existing supabase fixture name (check tests/integration/conftest.py)
        """Plan 075.4-05 Wave 0 — closes ~5 of 95 pre-existing failures (FK violations: runs without threads).
        Yields make_run(user_id, **overrides). FK-safe cleanup in reverse order on teardown.
        """
        created = {"users": [], "threads": [], "runs": []}
        async def make_run(user_id=None, **overrides):
            uid = user_id or str(uuid.uuid4())
            try:
                _supabase.table("auth.users").insert({"id": uid}).execute()
                created["users"].append(uid)
            except Exception: pass
            tid = str(uuid.uuid4())
            _supabase.table("threads").insert({"id": tid, "user_id": uid, "title": "test"}).execute()
            created["threads"].append(tid)
            rid = overrides.pop("id", str(uuid.uuid4()))
            _supabase.table("runs").insert({"id": rid, "user_id": uid, "thread_id": tid, "status": "queued", **overrides}).execute()
            created["runs"].append(rid)
            return {"run_id": rid, "thread_id": tid, "user_id": uid}
        yield make_run
        for rid in created["runs"]:
            try: _supabase.table("runs").delete().eq("id", rid).execute()
            except Exception: pass
        for tid in created["threads"]:
            try: _supabase.table("threads").delete().eq("id", tid).execute()
            except Exception: pass
        for uid in created["users"]:
            try: _supabase.table("auth.users").delete().eq("id", uid).execute()
            except Exception: pass
    ```

    The fixture name `_supabase` may need adjustment to match the existing supabase fixture in tests/integration/conftest.py. Read that conftest first to confirm the exact fixture name. Comment block: `# Plan 075.4-05 Wave 0 — FK-safe factory; pattern from PROJECT.md Phase 073-04 Rule-1 deviation. Used by Task 9 triage to fix the 4 test_provider_router.py + 2 test_066_langsmith_clean.py FK-violation failures.`
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -c "import importlib.util; spec = importlib.util.spec_from_file_location('conftest', 'tests/conftest.py'); m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m); assert hasattr(m, 'fk_aware_runs_factory'); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f backend/tests/conftest.py`
    - `grep -c "fk_aware_runs_factory" backend/tests/conftest.py` returns >= 1
    - `grep -c "Plan 075.4-05" backend/tests/conftest.py` returns >= 1
    - `grep -cE "FK-safe|FK violation|ForeignKey" backend/tests/conftest.py` returns >= 1
    - `cd backend && venv/Scripts/python -m pytest --collect-only tests/test_provider_router.py 2>&1 | tail -3` runs without ImportError
  </acceptance_criteria>
  <done>FK-aware factory in top-level conftest.py; available via pytest discovery; commented hand-off to Task 9 triage.</done>
</task>

<task type="auto" wave="0">
  <name>Task 5 (Wave 0): frontend-tests.yml CI workflow + 7 bug-report frontmatter updates</name>
  <files>.github/workflows/frontend-tests.yml, .planning/reported-bugs/composer-locked-globally-during-any-stream.md, .planning/reported-bugs/gemini-3-thought-signature-missing-on-tool-rounds.md, .planning/reported-bugs/openrouter-duplicate-output-and-delayed-finish.md, .planning/reported-bugs/empty-response-fallback-misleading-iter-count.md, .planning/reported-bugs/final-outputs-backend-omits-url-in-event-payload.md, .planning/reported-bugs/final-outputs-pinned-panel-no-download-link.md, .planning/reported-bugs/anthropic-excessive-tool-iterations-on-multi-step-tasks.md</files>
  <read_first>
    - .github/workflows/backend-tests.yml (full file — mirror verbatim)
    - .planning/phases/075.4-.../075.4-PATTERNS.md §`.github/workflows/frontend-tests.yml (create)` (verbatim shape)
    - All 7 reported-bug .md files — read frontmatter to preserve all fields not being changed
    - .planning/reported-bugs/TEMPLATE.md (frontmatter shape)
    - CONTEXT.md §Reported Bug Routing (5 folded + 1 deferred + 1 auto-close)
  </read_first>
  <action>
    **Step 1 — Create `.github/workflows/frontend-tests.yml`** per PATTERNS.md verbatim shape (50+ lines). Two jobs: `vitest` (npm ci + npm test) + `playwright` (needs vitest; boots Redis from docker-compose.dev.yml + backend uvicorn + waits for /health then runs `npx playwright test` with `secrets.LANGSMITH_API_KEY` in env). Path filters: `frontend/**` + `.github/workflows/frontend-tests.yml`. Branches: master, main, v2.5-dev. After playwright job, add documentation step:
    ```yaml
    - name: LangSmith key redaction note
      if: always()
      run: echo "GitHub Actions auto-redacts secrets.* references in step outputs."
    ```

    **Step 2 — Update bug-report frontmatter (use Edit tool, NOT sed). For 5 reports flip `status: open` → `status: folded` + add `folded_into: "075.4"`:**
    - `composer-locked-globally-during-any-stream.md`
    - `gemini-3-thought-signature-missing-on-tool-rounds.md`
    - `openrouter-duplicate-output-and-delayed-finish.md`
    - `empty-response-fallback-misleading-iter-count.md`
    - `final-outputs-backend-omits-url-in-event-payload.md`

    **Step 3 — `final-outputs-pinned-panel-no-download-link.md`:** Preserve existing status (`partial-closed`). ADD field `re_open_trigger: "If pinned-panel download link still missing after 075.4 ships (BUG-260522-02 final_output_files now includes url+size); else closes naturally."`

    **Step 4 — `anthropic-excessive-tool-iterations-on-multi-step-tasks.md` (BUG-260523-04):** Flip `status` → `deferred`. ADD `re_open_trigger: "After 075.4 ships — Plan 05 E2E #6 measures iteration parity. Focused root-cause investigation phase to follow."`. Do NOT add `folded_into` (per CONTEXT.md: stays open/deferred, NOT folded).

    Preserve all body text and other frontmatter fields. Only modify the listed fields.
  </action>
  <verify>
    <automated>test -f .github/workflows/frontend-tests.yml && grep -c "secrets.LANGSMITH_API_KEY" .github/workflows/frontend-tests.yml && grep -l 'folded_into:.*075.4\|folded_into: "075.4"' .planning/reported-bugs/composer-locked-globally-during-any-stream.md .planning/reported-bugs/gemini-3-thought-signature-missing-on-tool-rounds.md .planning/reported-bugs/openrouter-duplicate-output-and-delayed-finish.md .planning/reported-bugs/empty-response-fallback-misleading-iter-count.md .planning/reported-bugs/final-outputs-backend-omits-url-in-event-payload.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .github/workflows/frontend-tests.yml`
    - `wc -l .github/workflows/frontend-tests.yml` returns >= 50
    - `grep -c "secrets.LANGSMITH_API_KEY" .github/workflows/frontend-tests.yml` returns >= 1
    - `grep -c "npx playwright test" .github/workflows/frontend-tests.yml` returns >= 1
    - `grep -c "npm test" .github/workflows/frontend-tests.yml` returns >= 1
    - `grep -c "docker compose -f docker-compose.dev.yml" .github/workflows/frontend-tests.yml` returns >= 1
    - For each of the 5 folded bugs: `grep -c 'folded_into:.*075.4' <file>` returns >= 1 AND `grep -c '^status: folded\|status: "folded"' <file>` returns >= 1
    - For `final-outputs-pinned-panel-no-download-link.md`: `grep -c "re_open_trigger" <file>` returns >= 1 AND existing status preserved
    - For `anthropic-excessive-tool-iterations-on-multi-step-tasks.md`: `grep -c 'status:.*deferred' <file>` returns >= 1 AND `grep -c "re_open_trigger" <file>` returns >= 1 AND NO `folded_into` field
  </acceptance_criteria>
  <done>frontend-tests.yml CI workflow lands; 7 bug-report frontmatter updates applied per CONTEXT.md routing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E test user credentials | `fhdmrd@gmail.com / 123456` is the documented dev login; reused for local Playwright. |
| LangSmith API key in CI | GitHub Actions secret; flows through workflow step env. |
| DB teardown (TRUNCATE / DELETE) | If pointed at prod, catastrophic data loss. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-075.4-07 | I (Information disclosure) — test-user data leak in CI logs | Playwright fixtures + scenario specs | mitigate | Creds loaded from `process.env.E2E_USER_EMAIL` / `E2E_USER_PASSWORD` (NOT committed). CI uses GitHub Actions secrets (auto-redacted). Local dev: defaults to known fhdmrd@gmail.com / 123456 (already in repo memory; acceptable for local-only fixture per D-075.4-G1). Dedicated test user deferred to v3.0 multi-tenancy work. |
| T-075.4-08 | I (Information disclosure) — LangSmith API key exposure in CI logs | `.github/workflows/frontend-tests.yml` | mitigate | Workflow uses `${{ secrets.LANGSMITH_API_KEY }}` reference; GitHub Actions auto-redacts secret values in step output. Documented in workflow comment + explicit redaction-verification step. Local dev: key lives only in `backend/.env` (gitignored). |
| T-075.4-EXTRA | T (Tampering) — accidental prod data wipe via teardown | `db-teardown.fixture.ts` | mitigate | Hard-gate at fixture entry: throw if `SUPABASE_URL` does NOT contain `'localhost'` or `'127.0.0.1'`. Prevents accidental TRUNCATE against prod. Confirmed by Security Threat Surface Plan 05 row. |
</threat_model>

<verification>
## Plan-Level Checks
1. `grep -c "## UAT scoreboard recipe (MANDATORY)" CLAUDE.md` returns 1 (Wave 0 SC#10 landed).
2. `test -f frontend/playwright.config.ts` AND `wc -l frontend/playwright.config.ts` returns >= 20.
3. `test -f .github/workflows/frontend-tests.yml` AND `grep -c "secrets.LANGSMITH_API_KEY" .github/workflows/frontend-tests.yml` returns >= 1.
4. `test -f scripts/restart-backend.sh` AND `test -f scripts/restart-backend.ps1`.
5. `curl -fsS http://localhost:8000/health` returns 200 with `{"status": "ok"}` (when backend running).
6. 5 folded bug reports have `folded_into: "075.4"` AND `status: folded`; BUG-260523-04 has `status: deferred`; BUG-260521-02 has `re_open_trigger:` field added.
7. `grep -c "fk_aware_runs_factory" backend/tests/conftest.py` returns >= 1 (substrate for Plan 06 Task 3).

## UAT Scoreboard — Plan 05 is the substrate that the OTHER plans' UAT scoreboards run on. Plan 06 ships the operator-led 4-axis sweep across all touched plans + the CI dry-run.
</verification>

<success_criteria>
- CLAUDE.md ## UAT scoreboard recipe (MANDATORY) section landed (SC#10)
- Playwright config + 3 fixtures (auth env-driven, db-teardown localhost-gated, langsmith poller) — fixtures consumed by Plan 06 scenarios
- LangSmith trace assertion fixture (exponential backoff poller)
- DB-state teardown with localhost hard-gate (substrate for Plan 06)
- restart-backend.{ps1,sh} scripts ship with FORWARD-REF #2 multi-worker update path header
- /health endpoint returns 200 with `{"status": "ok"}`
- backend/tests/conftest.py fk_aware_runs_factory landed; consumed by Plan 06 Task 3 to close ~6 FK-violation failures
- .github/workflows/frontend-tests.yml CI workflow runs vitest + Playwright on PR; uses secrets.LANGSMITH_API_KEY
- 7 bug-report frontmatter updates applied: 5 folded → 075.4, 1 deferred (BUG-260523-04), 1 auto-close noted (BUG-260521-02)
</success_criteria>

<forward_references>
- → Phase 077/079 (multi-worker enable): `restart-backend.ps1` + `restart-backend.sh` headers cite FORWARD-REF #2/#3 — update for `--workers N` when Phase 079 lands. Document update path inline.
- → Plan 06 (this phase, Wave 3): Plan 05 ships the Playwright config + 3 fixtures + FK-aware conftest fixture; Plan 06 ships the 6 scenarios + 95-failure triage + FK-fix cluster + operator UAT sweep.
- → Phase 082 (cross-cutting verification): Plan 05's Playwright infrastructure + Plan 06's scenarios are the test substrate 082 rides on.
</forward_references>

<output>
After completion, create `.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-05-SUMMARY.md`
</output>
