---
phase: 075.4
plan: 04
type: execute
wave: 2
depends_on: ["075.4-03"]  # Plan 03 emits the supersedes: field consumed by OutputFileCard
files_modified:
  - backend/app/models/user_settings.py  # save_override sentinel guard (D-075.4-F1)
  - frontend/src/components/chat/MessageItem.tsx  # React.memo wrap
  - frontend/src/components/chat/MarkdownRenderer.tsx  # useMemo
  - frontend/src/components/chat/ExecuteCodeBlock.tsx  # stderr badge
  - frontend/src/components/chat/OutputFileCard.tsx  # supersedes prop
  - frontend/src/components/chat/ChatArea.tsx  # useCallback stabilization for memo
  - frontend/src/pages/KnowledgeHealthPage.tsx  # lazy recharts
  - frontend/src/pages/SettingsPage.tsx  # 2-click bug narrow fix
  - CLAUDE.md  # SANDBOX_IMAGE docs prominence
  - backend/README.md  # ## Sandbox Setup section
  - backend/tests/unit/test_075_4_save_override_sentinel.py
  - frontend/src/__tests__/components/MessageItem.memo.test.tsx
  - frontend/src/__tests__/components/MarkdownRenderer.memo.test.tsx
  - .planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-04-DIAGNOSIS-settings-2click.md  # diagnosis artifact
autonomous: false
success_criteria_addressed: [SC#5 (partial — OutputFileCard supersedes UI), SC#6, SC#7]
bugs_folded: [BUG-260523-03]  # supersedes UI affordance — Plan 03 is backend, Plan 04 is the user-visible UI

must_haves:
  truths:
    - "MessageItem is wrapped in React.memo with stable onSendMessage / onResume callbacks (ChatArea uses useCallback)"
    - "MarkdownRenderer html parse output is memoized via useMemo on the content prop"
    - "RetrievalTrendChart is lazy-loaded via React.lazy + Suspense; recharts is a separate chunk in dist/"
    - "SANDBOX_IMAGE setup is documented prominently in backend/README.md ## Sandbox Setup section + linked from CLAUDE.md"
    - "ExecuteCodeBlock renders a visible stderr badge alongside red stderr lines"
    - "OutputFileCard renders 'Replaces: <prev_filename>' subline when supersedes prop is set"
    - "save_override rejects api_key writes that fail allowlist-by-provider-prefix; logs warning; skips silently (closes WR-02)"
    - "Settings handleSaveAIModel persists fields on first click (2-click bug fixed; narrow code-change after Chrome DevTools diagnosis)"
  artifacts:
    - path: "backend/app/models/user_settings.py"
      provides: "Allowlist sentinel guard wrapping existing KEY_PLACEHOLDER skip"
      contains: "_is_valid_api_key"
    - path: "frontend/src/components/chat/MessageItem.tsx"
      provides: "React.memo wrap"
      contains: "memo(function MessageItem"
    - path: "frontend/src/components/chat/MarkdownRenderer.tsx"
      provides: "useMemo on html parse"
      contains: "useMemo"
    - path: "frontend/src/components/chat/OutputFileCard.tsx"
      provides: "supersedes?: string prop + 'Replaces:' subline render"
      contains: "supersedes"
    - path: ".planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-04-DIAGNOSIS-settings-2click.md"
      provides: "Diagnostic artifact for Settings 2-click bug (Chrome DevTools MCP capture)"
      min_lines: 30
  key_links:
    - from: "frontend/src/components/chat/OutputFileCard.tsx"
      to: "Plan 03 sandbox_service.py supersedes field"
      via: "prop pass-through"
      pattern: "supersedes\\?: string"
    - from: "backend/app/models/user_settings.py:_is_valid_api_key"
      to: "_PROVIDER_KEY_PREFIXES"
      via: "allowlist by provider prefix (sk- / sk-ant- / sk-or- / length / any)"
      pattern: "_PROVIDER_KEY_PREFIXES"
---

<objective>
Perf + UX + safety polish:
- **Perf:** React.memo MessageItem + useMemo MarkdownRenderer + lazy recharts (≥30% render-cost reduction target on 50-message thread during streaming).
- **UX:** SANDBOX_IMAGE setup docs prominent; stderr badge in ExecuteCodeBlock; OutputFileCard "Replaces:" affordance reading Plan 03's `supersedes` field.
- **Safety:** Defensive sentinel-allowlist guard in `save_override` closes WR-02 (the 075.3 orchestrator data-loss footgun); narrow Settings 2-click fix after Chrome DevTools MCP diagnosis (per D-075.4-F3).

Purpose: User experience of streaming threads stays responsive under load. API keys never get clobbered by masked submits. Settings "type-then-save" works on the first click.
Output: 8 source files modified + 1 diagnosis artifact + 3 new test files.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-CONTEXT.md
@.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-RESEARCH.md
@.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-PATTERNS.md

@backend/app/models/user_settings.py
@frontend/src/components/chat/MessageItem.tsx
@frontend/src/components/chat/MarkdownRenderer.tsx
@frontend/src/components/chat/ExecuteCodeBlock.tsx
@frontend/src/components/chat/OutputFileCard.tsx
@frontend/src/components/chat/ChatArea.tsx
@frontend/src/pages/KnowledgeHealthPage.tsx
@frontend/src/pages/SettingsPage.tsx

<interfaces>
<!-- Plan 03 contract Plan 04 consumes — supersedes field shape per Plan 03 sandbox_service.py -->

```typescript
// Plan 03 emits via final_output_files / output_file delta SSE event:
type FileDelta = {
  filename: string;
  url: string;
  size: number;
  supersedes?: string;  // NEW (Plan 03 D-075.4-D2)
}

// Existing OutputFileCardProps shape (lines 37-43 of OutputFileCard.tsx):
interface OutputFileCardProps {
  file: { filename: string; url?: string; size?: number }
}

// Plan 04 extends to (relaxed prop shape preserved):
interface OutputFileCardProps {
  file: {
    filename: string;
    url?: string;
    size?: number;
    supersedes?: string;   // Plan 04 — Wave 2 from Plan 03
  }
}
```

```python
# backend/app/models/user_settings.py:134-146 — save_override (BEFORE):
def save_override(updates: dict[str, Any]) -> None:
    current = _load_override()
    for k, v in updates.items():
        if v == KEY_PLACEHOLDER:
            continue
        if v is None:
            current.pop(k, None)
        else:
            current[k] = v
    _OVERRIDE_FILE.write_text(json.dumps(current, indent=2), encoding="utf-8")
```

```python
# After (D-075.4-F1 + F2):
_PROVIDER_KEY_PREFIXES = {"openai_api_key": "sk-", "anthropic_api_key": "sk-ant-", "openrouter_api_key": "sk-or-"}
_SENTINEL_VALUES = frozenset({"***", "__KEEP__", "••••••"})

def _is_valid_api_key(key: str, value: str) -> bool:
    if not value or not value.strip(): return False
    if value in _SENTINEL_VALUES: return False
    if "•" in value or value.replace("*", "").strip() == "": return False
    prefix = _PROVIDER_KEY_PREFIXES.get(key)
    if prefix is not None: return value.startswith(prefix)
    if key == "google_api_key": return len(value) >= 30
    if key == "ollama_api_key": return True
    return True
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Settings 2-click bug — Chrome DevTools MCP diagnosis FIRST, then narrow fix</name>
  <files>frontend/src/pages/SettingsPage.tsx, .planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-04-DIAGNOSIS-settings-2click.md</files>
  <read_first>
    - frontend/src/pages/SettingsPage.tsx (lines 530-625 — incl. hydrate at L:537-544, handleSaveAIModel at L:586-620)
    - .planning/phases/075.4-.../075.4-RESEARCH.md §Settings 2-click bug (4 hypothesized causes + diagnostic approach via Chrome DevTools MCP)
    - .planning/phases/075.4-.../075.4-PATTERNS.md §`frontend/src/pages/SettingsPage.tsx lines 586-620 — handleSaveAIModel 2-click bug`
    - reference_local_dev_app.md memory (Chrome MCP available at http://localhost:5173/; test login fhdmrd@gmail.com / 123456)
  </read_first>
  <what-built>
    Per D-075.4-F3 — diagnose FIRST via Chrome DevTools MCP + React DevTools, fix narrowly. Two-step task:
    1. **Diagnose** — capture the failing render cycle. Use Chrome DevTools MCP to navigate to http://localhost:5173/settings; type a new api_key into a provider input (do NOT blur); click Save once; inspect the network request body's `api_key` field. Cross-check the suspected root causes (RESEARCH §Settings 2-click bug — empty-coercion at L:596 vs hydrate race vs batched-update vs controlled-input flush). Capture findings in `075.4-04-DIAGNOSIS-settings-2click.md` (≥30 lines: hypothesis, observed behavior, network capture summary, root cause verdict).
    2. **Narrow fix** — based on diagnosis, ship the SMALLEST possible patch in `handleSaveAIModel` (`SettingsPage.tsx:586-620`). Candidates ranked by smallness:
       - (a) Drop `|| KEY_PLACEHOLDER` coercion at L:596 (let backend handle empty as no-op) — 1-line change.
       - (b) `flushSync(() => setProviderStates(...))` from `react-dom` before `await updateSettings(body)` — 2-3 line wrap.
       - (c) Switch api_key inputs from controlled `value`+`onChange` to uncontrolled `defaultValue` — 1-2 lines per input.
       Pick the candidate that the diagnosis directly supports. If diagnosis reveals a structural problem (e.g., needs `useReducer` rewrite), document deferral to Phase 081.1 (FORWARD-REF #5) in the diagnosis artifact AND ship the minimum-change defensive fix (e.g., add a TODO comment block at L:586 + the smallest coercion-drop or flush-sync fix).

    **Acceptance bound:** the diff at `SettingsPage.tsx:586-620` is bounded — `git diff --shortstat frontend/src/pages/SettingsPage.tsx` shows ≤ 6 insertions + ≤ 4 deletions. If the fix needs more, the diagnosis must explicitly note the deferral to Phase 081.1.
  </what-built>
  <how-to-verify>
    1. Run the dev stack — backend on http://localhost:8000, frontend on http://localhost:5173.
    2. Use Chrome DevTools MCP: navigate to /settings, log in with fhdmrd@gmail.com / 123456.
    3. Expand a provider (e.g., OpenAI); type a NEW valid api_key (e.g., `sk-testkey123abc`) into the api_key input. Do NOT click outside the field.
    4. Open Chrome DevTools → Network tab → filter for `/settings` PATCH/PUT.
    5. Click Save **once**. Capture the request body's `providers[i].api_key` value.
    6. EXPECTED (post-fix): the typed value `sk-testkey123abc` appears in the request body on the FIRST save click, and `db query user_settings_overrides` shows the new key persisted.
    7. NEGATIVE CHECK: type a sentinel string `"***"` → click Save → request body should still show `"***"` (frontend doesn't pre-filter); backend save_override should LOG WARN and SKIP (Plan 04 Task 2). Confirm via backend logs.
    8. Document the result in `075.4-04-DIAGNOSIS-settings-2click.md` (root cause verdict, fix shipped, deferred items if any).
  </how-to-verify>
  <action>
    See <what-built> above. Operator-driven 2-step process: (1) diagnose via Chrome DevTools MCP capture, (2) ship narrow fix in handleSaveAIModel based on diagnosis findings.
  </action>
  <verify>
    <automated>test -f .planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-04-DIAGNOSIS-settings-2click.md && wc -l .planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-04-DIAGNOSIS-settings-2click.md</automated>
  </verify>
  <done>Diagnosis artifact ≥30 lines with root cause verdict; narrow fix landed in SettingsPage.tsx with ≤10-line diff bound; first-click save persists field per Chrome DevTools network capture.</done>
  <acceptance_criteria>
    - `test -f .planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-04-DIAGNOSIS-settings-2click.md` (artifact exists)
    - `wc -l .planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-04-DIAGNOSIS-settings-2click.md` returns >= 30
    - `git diff --shortstat HEAD frontend/src/pages/SettingsPage.tsx` shows total inserted+deleted lines <= 10 (narrow fix bound)
    - Diagnosis artifact contains the section "## Root Cause Verdict" with one of: (a)/(b)/(c) candidate selected OR explicit "structural — deferred to Phase 081.1" note
    - On the first Save click in Chrome DevTools session, the request body's `api_key` field contains the operator-typed value (NOT empty, NOT KEY_PLACEHOLDER)
    - `cd frontend && npx tsc --noEmit 2>&1 | grep -c "SettingsPage.tsx"` returns 0
  </acceptance_criteria>
  <resume-signal>Type "approved" with the diagnosis artifact path, OR describe what didn't work in the Chrome DevTools session (e.g., "first-click submitted empty body — fix doesn't work" → revise fix and re-run).</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: save_override sentinel allowlist guard (closes WR-02) + unit tests</name>
  <files>backend/app/models/user_settings.py, backend/tests/unit/test_075_4_save_override_sentinel.py</files>
  <read_first>
    - backend/app/models/user_settings.py (full file — incl. KEY_PLACEHOLDER at L:37, save_override at L:134-146, _OVERRIDE_FILE path)
    - .planning/phases/075.4-.../075.4-PATTERNS.md §`backend/app/models/user_settings.py lines 134-146 — save_override sentinel guard` (verbatim shape)
    - .planning/phases/075.4-.../075.4-RESEARCH.md §`save_override` sentinel guard (D-075.4-F1 + F2)
    - Phase 075.3 REVIEW.md (WR-02 — the 075.3 orchestrator data-loss case)
  </read_first>
  <behavior>
    - Test 1: `_is_valid_api_key("openai_api_key", "sk-realkey123abc")` returns True.
    - Test 2: `_is_valid_api_key("openai_api_key", "***")` returns False (sentinel).
    - Test 3: `_is_valid_api_key("openai_api_key", "__KEEP__")` returns False (sentinel).
    - Test 4: `_is_valid_api_key("openai_api_key", "••••••")` returns False (masked-dots).
    - Test 5: `_is_valid_api_key("openai_api_key", "")` returns False (empty).
    - Test 6: `_is_valid_api_key("openai_api_key", "   ")` returns False (whitespace).
    - Test 7: `_is_valid_api_key("openai_api_key", "claude-key-no-prefix")` returns False (wrong provider prefix — does not start with `sk-`).
    - Test 8: `_is_valid_api_key("anthropic_api_key", "sk-ant-validkey")` returns True.
    - Test 9: `_is_valid_api_key("openrouter_api_key", "sk-or-validkey")` returns True.
    - Test 10: `_is_valid_api_key("google_api_key", "x" * 35)` returns True (length-only >= 30).
    - Test 11: `_is_valid_api_key("google_api_key", "tooshort")` returns False (length < 30).
    - Test 12: `_is_valid_api_key("ollama_api_key", "anything-non-sentinel")` returns True.
    - Test 13: `_is_valid_api_key("ollama_api_key", "***")` returns False (sentinel still rejected).
    - Test 14: `_is_valid_api_key("not_an_api_key_field", "anything")` returns True (non-api-key fields pass through; matters only because save_override only invokes the guard when k.endswith("_api_key")).
    - Test 15 (integration): `save_override({"openai_api_key": "***"})` does NOT write a new key (cache stays unchanged); a WARNING log fires.
    - Test 16 (integration): `save_override({"openai_api_key": "sk-newkey"})` DOES write the new key.
    - Test 17: `save_override({"openai_api_key": "claude-wrong-prefix"})` does NOT write (allowlist by provider prefix rejects); WARNING log fires.
  </behavior>
  <action>
    Modify `backend/app/models/user_settings.py` per PATTERNS.md §`save_override sentinel guard` verbatim shape:

    1. Add imports at top: `import logging; logger = logging.getLogger(__name__)` (if not already present).
    2. Add module-level constants near `KEY_PLACEHOLDER` (~L:37):
       ```python
       _PROVIDER_KEY_PREFIXES: dict[str, str] = {
           "openai_api_key": "sk-",
           "anthropic_api_key": "sk-ant-",
           "openrouter_api_key": "sk-or-",
           # google_api_key: length-only minimum (~35-40 chars)
           # ollama_api_key: any non-sentinel non-empty
       }
       _SENTINEL_VALUES: frozenset[str] = frozenset({"***", "__KEEP__", "••••••"})
       ```
    3. Add `_is_valid_api_key` helper function (verbatim from PATTERNS.md) before `save_override`.
    4. Refactor `save_override` to insert the allowlist layer AFTER the existing `KEY_PLACEHOLDER` skip:
       ```python
       def save_override(updates: dict[str, Any]) -> None:
           global _override_cache_time
           current = _load_override()
           for k, v in updates.items():
               if v == KEY_PLACEHOLDER:
                   continue
               # Plan 075.4-04 D-075.4-F1 — new allowlist layer (wraps existing KEY_PLACEHOLDER skip)
               if k.endswith("_api_key") and v is not None and not _is_valid_api_key(k, str(v)):
                   logger.warning(
                       "rejected api_key write — sentinel/invalid format detected for key=%s",
                       k,
                   )
                   continue
               if v is None:
                   current.pop(k, None)
               else:
                   current[k] = v
           _OVERRIDE_FILE.write_text(json.dumps(current, indent=2), encoding="utf-8")
           _override_cache_time = 0.0
       ```

    5. Add a comment block citing FORWARD-REF #4 + #5: `# Plan 075.4-04 D-075.4-F1/F2 — defense-in-depth sentinel allowlist; closes WR-02. Phase 081.1 (Settings Architecture Unification) replaces save_override entirely — port this logic to 081.1's new codepath (FORWARD-REF #4 + #5).`

    Create `backend/tests/unit/test_075_4_save_override_sentinel.py` covering Tests 1-17 above. Use `tmp_path` fixture to redirect `_OVERRIDE_FILE` to a per-test temp path. Use `caplog` fixture to assert WARNING log fires for rejected writes (test 15 + 17).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/test_075_4_save_override_sentinel.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def _is_valid_api_key(" backend/app/models/user_settings.py` returns 1
    - `grep -c "_PROVIDER_KEY_PREFIXES" backend/app/models/user_settings.py` returns >= 2
    - `grep -c "_SENTINEL_VALUES" backend/app/models/user_settings.py` returns >= 2
    - `grep -c 'logger.warning(\s*\n*\s*"rejected api_key write' backend/app/models/user_settings.py` returns >= 1 (multiline grep allowed)
    - `grep -c "FORWARD-REF #4\|Phase 081.1" backend/app/models/user_settings.py` returns >= 1
    - `cd backend && venv/Scripts/python -c "from app.models.user_settings import _is_valid_api_key; print(_is_valid_api_key('openai_api_key', '***'))"` prints `False`
    - `cd backend && venv/Scripts/python -c "from app.models.user_settings import _is_valid_api_key; print(_is_valid_api_key('openai_api_key', 'sk-real'))"` prints `True`
    - `cd backend && venv/Scripts/python -c "from app.models.user_settings import _is_valid_api_key; print(_is_valid_api_key('anthropic_api_key', 'sk-or-wrong'))"` prints `False`
    - `test -f backend/tests/unit/test_075_4_save_override_sentinel.py`
    - `wc -l backend/tests/unit/test_075_4_save_override_sentinel.py` returns >= 80
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_075_4_save_override_sentinel.py -x -q` exits 0
  </acceptance_criteria>
  <done>Allowlist-by-provider-prefix sentinel guard live in save_override; existing KEY_PLACEHOLDER skip preserved; WARNING log on rejection; 17 unit-test cases green; WR-02 data-loss footgun closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: React.memo MessageItem + useMemo MarkdownRenderer + useCallback stabilization in ChatArea + memo tests</name>
  <files>frontend/src/components/chat/MessageItem.tsx, frontend/src/components/chat/MarkdownRenderer.tsx, frontend/src/components/chat/ChatArea.tsx, frontend/src/__tests__/components/MessageItem.memo.test.tsx, frontend/src/__tests__/components/MarkdownRenderer.memo.test.tsx</files>
  <read_first>
    - frontend/src/components/chat/MessageItem.tsx (lines 1-40 + grep for current export shape at L:22)
    - frontend/src/components/chat/MarkdownRenderer.tsx (lines 13-22 — current sync DOMPurify.sanitize(marked.parse(content)))
    - frontend/src/components/chat/ChatArea.tsx (find onSendMessage + onResume creation sites + where MessageList consumes MessageItem; identify pass-down callbacks)
    - .planning/phases/075.4-.../075.4-PATTERNS.md §`frontend/src/components/chat/MessageItem.tsx line 22 — React.memo` + §`MarkdownRenderer.tsx lines 13-22 — useMemo`
    - .planning/phases/075.4-.../075.4-RESEARCH.md §Plan 04 React.memo + useMemo
  </read_first>
  <behavior>
    - Test 1 (memo): MessageItem re-render count when parent re-renders with byte-identical message prop is 0 (component renders ONCE for initial mount + 0 re-renders).
    - Test 2 (memo): MessageItem re-renders when `message` prop changes by reference (new Message object).
    - Test 3 (useMemo): MarkdownRenderer with byte-identical `content` prop passed across re-renders calls `marked.parse` + `DOMPurify.sanitize` only ONCE (verify via spy / mock).
    - Test 4 (useMemo): MarkdownRenderer with changed `content` prop calls marked.parse + DOMPurify.sanitize again (memo invalidates on prop change).
    - Test 5 (useCallback): ChatArea passes stable references for `onSendMessage` / `onResume` callbacks to MessageList → MessageItem (the same function ref across re-renders when dependencies unchanged).
  </behavior>
  <action>
    Modify `frontend/src/components/chat/MessageItem.tsx` per PATTERNS.md verbatim:
    ```typescript
    import { memo } from "react"

    export const MessageItem = memo(function MessageItem({ message, isStreaming, onSendMessage, onResume }: Props) {
      // ... existing body unchanged ...
    })
    ```
    The named-inner-function preserves DevTools name display. Default `React.memo` does shallow equality on props. The `message` prop must NOT mutate by replacement-with-same-data (verify via `useThreadMessages` semantics — RESEARCH §Plan 04 React.memo notes shallow eq sufficient if store mutates by REPLACE not in-place push).

    Modify `frontend/src/components/chat/MarkdownRenderer.tsx` per PATTERNS.md verbatim:
    ```typescript
    import { useMemo } from "react"

    export function MarkdownRenderer({ content, className }: Props) {
      const html = useMemo(
        () => DOMPurify.sanitize(marked.parse(content) as string),
        [content],
      )

      return (
        <div
          className={cn("markdown text-sm text-foreground leading-relaxed", className)}
          dangerouslySetInnerHTML={{ __html: html }}
        />
      )
    }
    ```

    Modify `frontend/src/components/chat/ChatArea.tsx` — stabilize the `onSendMessage` and `onResume` callbacks passed to MessageList/MessageItem via `useCallback`. Locate the current call-site definitions (likely inline arrow functions in the JSX or top-level handlers); wrap each in `useCallback(() => { ... }, [<deps>])` with minimal dependency arrays. If the existing handlers are already `useCallback`-wrapped, no change needed. Add a comment: `// Plan 075.4-04 D-075.4-SC#6 — stabilize callbacks for React.memo(MessageItem) shallow-eq path.`

    Create `frontend/src/__tests__/components/MessageItem.memo.test.tsx`:
    - Mount MessageItem inside a parent that re-renders without changing props; assert `MessageItem` body function runs once (use a render-counter ref OR React Testing Library + spies).
    - Change the `message` prop; assert re-render fires.

    Create `frontend/src/__tests__/components/MarkdownRenderer.memo.test.tsx`:
    - Mock `marked.parse` + `DOMPurify.sanitize` via `vi.mock` / `vi.spyOn`; render MarkdownRenderer with content="hello"; re-render parent with same content="hello"; assert `marked.parse` was called exactly once.
    - Re-render with content="goodbye"; assert `marked.parse` was called twice total (memo invalidated).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/components/MessageItem.memo.test.tsx src/__tests__/components/MarkdownRenderer.memo.test.tsx 2>&1 | grep -E "(passed|failed)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "memo(function MessageItem" frontend/src/components/chat/MessageItem.tsx` returns >= 1
    - `grep -c "import { memo }" frontend/src/components/chat/MessageItem.tsx` returns >= 1
    - `grep -c "useMemo" frontend/src/components/chat/MarkdownRenderer.tsx` returns >= 1
    - `grep -cE "DOMPurify\.sanitize.*marked\.parse|marked\.parse.*DOMPurify" frontend/src/components/chat/MarkdownRenderer.tsx` returns >= 1 (the parsed-html call inside useMemo)
    - `grep -cE "const onSendMessage\s*=\s*useCallback|const onResume\s*=\s*useCallback|onSendMessage\s*=\s*useCallback|onResume\s*=\s*useCallback" frontend/src/components/chat/ChatArea.tsx` returns >= 2 (BOTH onSendMessage AND onResume specifically wrapped — not just any useCallback usage)
    - `test -f frontend/src/__tests__/components/MessageItem.memo.test.tsx`
    - `test -f frontend/src/__tests__/components/MarkdownRenderer.memo.test.tsx`
    - `grep -cE "renderCount|toHaveBeenCalledTimes\(1\)|expect\(.*\)\.not\.toHaveBeenCalled" frontend/src/__tests__/components/MessageItem.memo.test.tsx` returns >= 1 (the test MUST actually count re-renders — a vacuous pass that doesn't assert render-count is not acceptable)
    - `cd frontend && npx vitest run src/__tests__/components/MessageItem.memo.test.tsx src/__tests__/components/MarkdownRenderer.memo.test.tsx` exits 0
    - `cd frontend && npx tsc --noEmit 2>&1 | grep -cE "MessageItem\.tsx|MarkdownRenderer\.tsx|ChatArea\.tsx"` returns 0
  </acceptance_criteria>
  <done>React.memo wrapping MessageItem; useMemo wrapping MarkdownRenderer html parse; ChatArea callbacks useCallback-stabilized; 2 memo tests green; TypeScript clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Lazy recharts (React.lazy + Suspense) + ExecuteCodeBlock stderr badge + OutputFileCard supersedes prop + Sandbox docs prominence</name>
  <files>frontend/src/pages/KnowledgeHealthPage.tsx, frontend/src/components/chat/ExecuteCodeBlock.tsx, frontend/src/components/chat/OutputFileCard.tsx, CLAUDE.md, backend/README.md</files>
  <read_first>
    - frontend/src/pages/KnowledgeHealthPage.tsx (line 28 — current sync import of RetrievalTrendChart; find the JSX render site for `<RetrievalTrendChart>` + existing `<ChartSkeleton />` fallback usage at ~L:475)
    - frontend/src/components/chat/ExecuteCodeBlock.tsx (lines 92, 155-175 — stderr-line generation + red-error-box)
    - frontend/src/components/chat/OutputFileCard.tsx (lines 30-43 — current OutputFileCardProps interface + JSX render)
    - CLAUDE.md (Local dev infrastructure section — current SANDBOX_IMAGE one-liner)
    - backend/README.md (full file — locate top-of-file or first H2 heading for new ## Sandbox Setup section)
    - .planning/phases/075.4-.../075.4-PATTERNS.md §`frontend/src/pages/KnowledgeHealthPage.tsx line 28 — lazy recharts` + §`OutputFileCard.tsx — supersedes prop`
  </read_first>
  <behavior>
    - Test 1 (lazy): `cd frontend && npm run build && ls dist/assets/` shows a `recharts-*.js` (or similarly chunked) file as a distinct asset. The main `index-*.js` does NOT contain the recharts code.
    - Test 2 (Suspense): KnowledgeHealthPage renders `<ChartSkeleton />` (the existing fallback at L:475) during the lazy chunk load.
    - Test 3 (stderr badge): ExecuteCodeBlock renders a visible "stderr" badge alongside lines whose `kind === "stderr"` (visible text or visible styling difference).
    - Test 4 (OutputFileCard supersedes): When `file.supersedes` is provided, the card renders a `<span>Replaces: <prev_filename></span>` (or equivalent text element).
    - Test 5 (XSS guard): `supersedes` value containing `<script>alert(1)</script>` renders as escaped text (React's default text-content escaping).
    - Test 6 (docs): `grep -c "## Sandbox Setup" backend/README.md` returns >= 1; the section explains the build/run command for the project's pre-built sandbox image.
    - Test 7 (docs): CLAUDE.md links to backend/README.md `## Sandbox Setup` from the existing SANDBOX_IMAGE one-liner.
  </behavior>
  <action>
    **Step 1 — Lazy recharts (`frontend/src/pages/KnowledgeHealthPage.tsx`):**

    Per PATTERNS.md verbatim:
    ```typescript
    import { lazy, Suspense } from "react"

    const RetrievalTrendChart = lazy(() =>
      import("@/components/health/RetrievalTrendChart").then((m) => ({
        default: m.RetrievalTrendChart,
      })),
    )
    ```
    Replace the existing sync import at L:28. Wrap the `<RetrievalTrendChart data={trend} />` JSX site (~L:475 area) in `<Suspense fallback={<ChartSkeleton />}>...</Suspense>` reusing the existing `ChartSkeleton`. If `RetrievalTrendChart` is not a named export, adjust the `.then((m) => ({ default: m.RetrievalTrendChart }))` block accordingly.

    After applying, run `cd frontend && npm run build && ls dist/assets/` to verify `recharts-*.js` is a distinct chunk. If Vite doesn't split by default, add a `manualChunks` config to `vite.config.ts`:
    ```typescript
    build: {
      rollupOptions: {
        output: {
          manualChunks: {
            recharts: ['recharts'],
          },
        },
      },
    }
    ```
    (only modify vite.config.ts if necessary — verify build output first).

    **Step 2 — ExecuteCodeBlock stderr badge:**

    Read `frontend/src/components/chat/ExecuteCodeBlock.tsx` lines 92-175 (incl. `TerminalOutput` consumer at L:175). The line shape at L:92 is `{ kind: "stdout" | "stderr", content }`. Find the `TerminalOutput` component (separate file — likely `frontend/src/components/chat/TerminalOutput.tsx` or similar) and add an inline badge for stderr lines:
    ```tsx
    {line.kind === "stderr" && (
      <span className="ml-2 inline-flex items-center rounded-sm bg-red-500/15 px-1.5 py-0.5 text-[10px] uppercase tracking-wider text-red-400 font-semibold">
        stderr
      </span>
    )}
    ```
    Place this badge inline alongside each red stderr line (NOT replacing the existing error-box at L:155; the badge is per-line, the error-box is for `errorMessage`).

    **Step 3 — OutputFileCard supersedes prop:**

    Modify `frontend/src/components/chat/OutputFileCard.tsx` per PATTERNS.md verbatim. Extend the interface:
    ```typescript
    interface OutputFileCardProps {
      file: {
        filename: string
        url?: string
        size?: number
        /** Plan 04 D-075.4-D2 — emitted by Plan 03 content-hash dedup when
         *  iteration N produces a different hash for the same filename. */
        supersedes?: string
      }
    }
    ```
    In the JSX (alongside the filename render):
    ```tsx
    {file.supersedes && (
      <span className="text-[10px] text-muted-foreground/70 truncate">
        Replaces: {file.supersedes}
      </span>
    )}
    ```
    XSS guard: rendered as text content (React auto-escapes).

    **Step 4 — Sandbox docs prominence:**

    Add a new section to `backend/README.md`. Read the file first to find the appropriate insertion point (likely near the top, after the project description). Insert:
    ```markdown
    ## Sandbox Setup

    The agent's `execute_code` tool runs inside a Docker container managed by `llm_sandbox`. To avoid ~10-15s `pip install` warm-up on every new chat, build the project's pre-bundled sandbox image once:

    ```bash
    docker build -f backend/Dockerfile.sandbox -t agentic-rag-sandbox:075.1 backend/
    ```

    Then add to `backend/.env`:

    ```
    SANDBOX_IMAGE=agentic-rag-sandbox:075.1
    ```

    The pre-built image includes matplotlib + numpy + pandas + python-pptx + openpyxl + python-docx + pypdf + seaborn + scipy + scikit-learn + plotly (Claude.ai-analysis-tool parity).

    When `SANDBOX_IMAGE` is unset, `SandboxSessionManager.get_or_create` (`backend/app/services/sandbox_service.py:38`) falls back to the bare `llm_sandbox` Python image — each new chat re-installs packages on first `execute_code` call.

    Sessions are cached per `thread_id` until idle eviction (default 30min); env-var changes only affect NEW chats.
    ```

    Modify `CLAUDE.md` — locate the existing `**Sandbox image** (v2.6+ / Phase 075.1)` paragraph in the Local dev infrastructure section. Add a short pointer at the end: `See backend/README.md#sandbox-setup for the verbose setup walkthrough.`

    Stretch: surface this in agent UX. In the agent's `execute_code` first-error message (locate via `grep -rn "matplotlib" backend/app/services/ | grep -v test`), add a one-line hint: `"If you're seeing pip install matplotlib happen — set SANDBOX_IMAGE; see backend/README.md#sandbox-setup."` Only if the location is straightforward (no `execute_code` deep-internal modification).
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20 && ls frontend/dist/assets/ 2>/dev/null | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "lazy(() => import" frontend/src/pages/KnowledgeHealthPage.tsx` returns >= 1
    - `grep -c "Suspense" frontend/src/pages/KnowledgeHealthPage.tsx` returns >= 1
    - `grep -c "ChartSkeleton" frontend/src/pages/KnowledgeHealthPage.tsx` returns >= 1 (fallback reuse)
    - `cd frontend && npm run build && ls dist/assets/ | grep -c "recharts"` returns >= 1 (recharts MUST exist as a distinct chunk) AND `grep -l "Recharts\|recharts" frontend/dist/assets/index-*.js | wc -l` returns 0 (the main index bundle MUST NOT contain recharts strings — both conditions required, no OR fallback)
    - `grep -c "stderr" frontend/src/components/chat/ExecuteCodeBlock.tsx` returns >= 2 (existing kind="stderr" + new badge ref)
    - `grep -c "supersedes" frontend/src/components/chat/OutputFileCard.tsx` returns >= 2 (interface field + JSX render conditional)
    - `grep -c "Replaces:" frontend/src/components/chat/OutputFileCard.tsx` returns >= 1
    - `grep -c "## Sandbox Setup" backend/README.md` returns 1
    - `grep -c "SANDBOX_IMAGE=agentic-rag-sandbox" backend/README.md` returns >= 1
    - `grep -c "backend/README.md#sandbox-setup" CLAUDE.md` returns >= 1
    - `cd frontend && npx tsc --noEmit 2>&1 | grep -cE "KnowledgeHealthPage\.tsx|ExecuteCodeBlock\.tsx|OutputFileCard\.tsx"` returns 0
  </acceptance_criteria>
  <done>Recharts lazy-loaded into a distinct chunk; stderr badge in ExecuteCodeBlock; OutputFileCard renders "Replaces:" subline when supersedes set; backend/README.md ## Sandbox Setup section landed; CLAUDE.md links to it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings save (POST/PATCH) | Operator submits provider configs incl. api_keys; untrusted values until validated. |
| Markdown content render | Assistant message text → DOMPurify.sanitize → innerHTML. XSS risk if sanitization bypassed. |
| OutputFileCard supersedes prop | `prev_filename` originates from backend dedup; could be a user-controlled filename. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-075.4-06 | T (Tampering) — api_key clobber via masked-value submit (WR-02) | `save_override` in `backend/app/models/user_settings.py:134-146` | mitigate | Allowlist by provider prefix (`sk-`, `sk-ant-`, `sk-or-`, length-only for google, any for ollama) + sentinel-value rejection (`***`, `__KEEP__`, `••••••`, empty, whitespace, sub-min-length). Rejection mode = skip + WARNING log (no HTTP 422 — that's 081.1's API-rewrite territory per D-075.4-F2). Test `test_075_4_save_override_sentinel.py` covers 17 cases including 5 sentinel patterns + per-provider prefix mismatch. |
| T-075.4-EXTRA-A | I (Information disclosure) via XSS — OutputFileCard supersedes | `OutputFileCard.tsx` | accept | `supersedes` rendered as text content (`<span>Replaces: {file.supersedes}</span>`), NOT `dangerouslySetInnerHTML`. React auto-escapes. Verified in Test 5 of Task 4. |
| T-075.4-EXTRA-B | I (Information disclosure) — useMemo memoizes parsed HTML | `MarkdownRenderer.tsx` | accept | DOMPurify.sanitize still runs on every content change (memo invalidates on prop change). No new sanitization bypass. Existing XSS surface unchanged. |
</threat_model>

<verification>
## Plan-Level Checks
1. `cd backend && venv/Scripts/python -m pytest tests/unit/test_075_4_save_override_sentinel.py -x -q` exits 0.
2. `cd frontend && npx vitest run src/__tests__/components/MessageItem.memo.test.tsx src/__tests__/components/MarkdownRenderer.memo.test.tsx` exits 0.
3. `cd frontend && npm run build` succeeds; recharts is in a distinct chunk OR not in main bundle.
4. `cd frontend && npx tsc --noEmit` exits 0 (no TS errors).
5. `grep -c "supersedes" frontend/src/components/chat/OutputFileCard.tsx` returns >= 2; renders "Replaces:" subline (manually verified via Chrome MCP in a real OpenRouter pptx round).
6. Diagnosis artifact `075.4-04-DIAGNOSIS-settings-2click.md` exists with root cause verdict; SettingsPage.tsx narrow-fix diff bounded.
7. `grep -c "## Sandbox Setup" backend/README.md` returns 1.

## UAT Scoreboard (under new CLAUDE.md §UAT scoreboard recipe rule)

| Provider | Tools | Threads | Msg len | Expected | Actual | Pass |
|---|---|---|---|---|---|---|
| OpenAI gpt-5.x | execute_code (multi-iter chart) | 1 | short | OutputFileCard shows "Replaces:" line when iteration N supersedes N-1; pinned panel HAS download link | | |
| OpenRouter | execute_code (pptx 5-iter loop) | 1 | short | Single final pptx; "Replaces:" affordance visible; stderr badge renders on red lines | | |
| Anthropic claude-4.x | 0 | 2 parallel + long (≥50 messages) | long (5KB) | MessageItem re-render count stable during streaming (React DevTools profiler shows ≥30% reduction vs HEAD); MarkdownRenderer doesn't re-parse same content | | |
| Google gemini-3 | 0 | 1 | short | Knowledge Health page initial load doesn't ship recharts in main bundle | | |
| Settings save (any provider) | n/a | n/a | n/a | Typing new sk-key + clicking Save ONCE persists the key (no 2-click bug); typing "***" rejected silently with warning log | | |
| Settings save (mismatched prefix) | n/a | n/a | n/a | Submitting "sk-or-key" to anthropic_api_key field → rejected silently + WARNING log; existing key preserved | | |
</verification>

<success_criteria>
- React.memo wrapping MessageItem + useCallback-stabilized onSendMessage/onResume in ChatArea
- useMemo on MarkdownRenderer html parse keyed by content
- Recharts lazy-loaded via React.lazy + Suspense; verified as distinct chunk in dist/
- ExecuteCodeBlock renders stderr badge alongside red lines
- OutputFileCard renders "Replaces: <prev_filename>" subline when supersedes prop set (closes BUG-260523-03 UI side; backend was Plan 03)
- SANDBOX_IMAGE setup section landed in backend/README.md; CLAUDE.md links to it
- save_override sentinel allowlist guard live; closes WR-02; 17 test cases green
- Settings 2-click bug — diagnosis artifact captures root cause; narrow fix shipped (≤10 line diff bound) OR deferred to Phase 081.1 with TODO in code
- All 3 new test files green; no TS errors; existing component tests stay green
</success_criteria>

<forward_references>
- → Phase 081.1 (Settings Architecture Unification): sentinel guard in save_override is INTERIM defense-in-depth (FORWARD-REF #4). When 081.1 replaces save_override entirely, port the `_is_valid_api_key` allowlist logic forward.
- → Phase 081.1: the 2-click save fix lands now (small, prevents user pain); 081.1's rewrite must preserve the fix (FORWARD-REF #5).
- → Phase 082.5: stderr badge is lightweight UI surfacing — Phase 082.5 unified error sink retrofits the underlying SSE event shape; the badge style stays.
</forward_references>

<output>
After completion, create `.planning/phases/075.4-cross-provider-cleanup-per-thread-state-e2e-backstop-inserte/075.4-04-SUMMARY.md`
</output>
