---
phase: 069-pdfextractor-abstraction-scaffold
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/prd-reset/DECISIONS.md
autonomous: true
requirements: []
tags:
  - decisions
  - agpl
  - pymupdf
  - q-v2.6-06
  - phase-069

must_haves:
  truths:
    # SC#4 only — Plan 2 owns SC#4 exclusively per CONTEXT.md plan-split
    - "Q-v2.6-06 (PyMuPDF AGPL fallback license posture) is closed by an appendix entry under D-PRD-07 in `.planning/prd-reset/DECISIONS.md` before Phase 069 ships"
    - "The appendix documents: AGPL-3.0 license, D-PRD-03 closed-core/open-peripherals constraint, subprocess fence mechanism, dev/personal-use acceptance, commercial-redistribution path (subprocess fence intact OR PyMuPDF Pro)"
    - "Documentation only — no code, no requirements.txt edits, no subprocess fence implementation (Phase 071's job per D-069-05 + CONTEXT.md §deferred)"
  artifacts:
    - path: .planning/prd-reset/DECISIONS.md
      provides: "D-PRD-07 Appendix on PyMuPDF AGPL fallback license posture (closes Q-v2.6-06)"
      contains: "D-PRD-07 Appendix"
      min_lines: 30
  key_links:
    - from: .planning/prd-reset/DECISIONS.md
      to: ".planning/PRDs/v2.6.md §13 Q-v2.6-06"
      via: "documents that the appendix closes the question"
      pattern: "Q-v2.6-06"
    - from: .planning/prd-reset/DECISIONS.md (appendix)
      to: ".planning/prd-reset/DECISIONS.md D-PRD-03 (closed core + open peripherals)"
      via: "references the AGPL-vs-open-peripherals constraint"
      pattern: "D-PRD-03"
---

<objective>
Append a "D-PRD-07 Appendix: PyMuPDF AGPL fallback license posture" entry to `.planning/prd-reset/DECISIONS.md`, closing Q-v2.6-06 per D-069-05 before Phase 069 ships.

Purpose: Phase 069 ships the `PdfExtractor` abstraction; Phase 071 will plug PyMuPDF as the AGPL fallback. The license posture (subprocess fence in dev / personal use; PyMuPDF Pro deferred until paying customer) must be locked in writing before the fallback is wired so Phase 071 has a clean ADR to reference.

Output: ~30-50 line appendix subsection under the existing D-PRD-07 entry (lines 502-580 of DECISIONS.md). Match the existing D-PRD-NN style (bulleted facts, no code, no deps).

**This is documentation only.** No code changes. No dependency adds (no `pymupdf` in `requirements.txt`). No subprocess-fence implementation (Phase 071). No test changes.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/069-pdfextractor-abstraction-scaffold/069-CONTEXT.md
@.planning/phases/069-pdfextractor-abstraction-scaffold/069-PATTERNS.md
@.planning/prd-reset/DECISIONS.md
@.planning/PRDs/v2.6.md
@CLAUDE.md

<interfaces>
<!-- Style reference: existing D-PRD-NN entries in DECISIONS.md, especially D-PRD-07 itself (lines 502-580). -->

### D-PRD-07 (existing — DO NOT modify the body):
- Lines 502-580 in `.planning/prd-reset/DECISIONS.md`
- Title: "D-PRD-07 — RAG quality: Docling-first (MIT) + PyMuPDF AGPL fallback behind PdfExtractor abstraction; PyMuPDF Pro deferred until first paying customer"
- Subsections: Context / Decision / Consequences / Alternatives considered + why rejected
- Status: ACCEPTED 2026-05-10

### Where the appendix attaches:
- Append a new subsection AT THE END of the existing D-PRD-07 entry — DO NOT create a new D-PRD-NN; D-069-05 explicitly says "appendix entry" not "new ADR".
- Title: `### Appendix — Q-v2.6-06 closure: PyMuPDF AGPL fallback license posture (Phase 069)`
- Match the heading depth of D-PRD-07's existing subsections (`### Context`, `### Decision`, etc. — likely `###`).
- Insert BEFORE the next top-level `## D-PRD-08` heading (if it exists) and AFTER the last line of D-PRD-07's "Alternatives considered + why rejected" subsection.

### Existing D-PRD-03 reference (for the appendix to cite):
- D-PRD-03 — "Closed core + open peripherals" (the constraint that AGPL must not link into open peripherals; MCP server / SDKs are open)
- The appendix references D-PRD-03 to anchor the subprocess-fence requirement.

### Q-v2.6-06 (the question being closed):
- From `.planning/PRDs/v2.6.md` §13, line 433: "PyMuPDF AGPL fallback license posture — accept in dev / personal use; defer commercial Pro license to first paying customer; mechanism for fence between closed-core distribution and AGPL-linked code TBD before Phase 071 ships."
- This appendix is the TBD answer.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append "D-PRD-07 Appendix — Q-v2.6-06 closure" to `.planning/prd-reset/DECISIONS.md`</name>
  <files>.planning/prd-reset/DECISIONS.md</files>
  <read_first>
    - .planning/prd-reset/DECISIONS.md (entire file — especially lines 500-590 for D-PRD-07 body; identify the exact line where D-PRD-07 ends and where to insert the appendix; also skim D-PRD-03 for the closed-core/open-peripherals constraint phrasing to cite verbatim)
    - .planning/PRDs/v2.6.md §13 Q-v2.6-06 (line ~433 — the question being closed)
    - .planning/phases/069-pdfextractor-abstraction-scaffold/069-CONTEXT.md (D-069-05 — the binding instruction: "writes a D-PRD-07 appendix entry in `.planning/prd-reset/DECISIONS.md` covering: PyMuPDF is AGPL-3.0; D-PRD-03 closed-core constraint; subprocess fence mechanism; dev/personal-use acceptance; commercial-redistribution requirements; documentation only — no code, no dependency adds, no fence implementation (fence lands in Phase 071)")
    - .planning/phases/069-pdfextractor-abstraction-scaffold/069-PATTERNS.md (lines 469-477 — appendix pattern: ~30-50 lines, bulleted facts, no code, no dep edits)
    - CLAUDE.md (project context — this is a planning artifact, not a runtime file; no schema/migrations involved)
  </read_first>
  <action>
    Identify the insertion point in `.planning/prd-reset/DECISIONS.md`:
    1. Locate the existing D-PRD-07 entry at line 502 (`## D-PRD-07 — RAG quality: Docling-first (MIT) + PyMuPDF AGPL fallback ...`).
    2. Find the last line of D-PRD-07's "Alternatives considered + why rejected" subsection (after the "OpenAI / Anthropic vision-API extraction at ingest time: rejected" bullet ends, around lines 587-589).
    3. Insert the appendix block BETWEEN the end of D-PRD-07's body and the next top-level `## D-PRD-08` heading (or end-of-file if D-PRD-08 doesn't exist — verify with `grep -n '^## D-PRD-08' .planning/prd-reset/DECISIONS.md`).

    Append exactly the following block (verbatim text — do NOT paraphrase the bullets; match the existing tone of D-PRD-07):

    ```markdown

    ### Appendix — Q-v2.6-06 closure: PyMuPDF AGPL fallback license posture (Phase 069)

    **Closes:** Q-v2.6-06 (PRD v2.6 §13, line 433)
    **Authored:** 2026-05-13 (Phase 069 — `PdfExtractor` Abstraction Scaffold)
    **Status:** APPENDED to D-PRD-07; documentation only.

    The `PdfExtractor` abstraction (Phase 069) creates the seam; Phase 071 will plug
    PyMuPDF as the AGPL fallback alongside Docling-primary. Before that wire-in
    happens, the license posture must be locked in writing.

    **Facts:**

    - **PyMuPDF (`pymupdf>=1.24`) is AGPL-3.0.** This is the upstream Artifex license.
      AGPL's copyleft and network-use triggers fire when AGPL code is linked into a
      distributed work.

    - **D-PRD-03 ("closed core + open peripherals") forbids linking AGPL code into the
      open peripherals.** The MCP server, SDKs, and any future open-source client
      components are MIT/Apache-licensed; an AGPL link would force the closed core's
      source disclosure obligation onto code paths we do not want to publish.

    - **Subprocess fence is the mechanism.** PyMuPDF runs as a separate OS process
      (its own Python interpreter, its own memory space) and communicates with the
      closed core via stdio / IPC over bytes. AGPL's linking trigger does NOT fire
      across a subprocess boundary — only across an in-process link (`import pymupdf`
      or a shared-library load). The closed core consumes PyMuPDF's output as data,
      not as a linked library.

    - **Dev / personal-use posture today: AGPL acceptance is fine.** No external
      network users are served by the dev environment; no closed-core binary is
      distributed; the closed-core `PdfExtractor` consumer can simply `import pymupdf`
      in-process during dev without triggering the redistribution obligation. This
      mirrors how the project ran pre-Phase 069 with no commercial pressure.

    - **Before any commercial redistribution that links PyMuPDF:** one of
      (a) **keep the subprocess fence intact** — the closed core ships without
          `import pymupdf` anywhere; the PyMuPDF subprocess is shipped as a separate
          binary or invoked from a separate `pip install pymupdf` env on the host
          (AGPL ships disclosed at the subprocess level only); OR
      (b) **acquire PyMuPDF Pro (commercial license from Artifex)** — removes the
          AGPL obligation entirely; deferred to first paying customer per D-PRD-07
          ("PyMuPDF Pro is NOT purchased now. Re-trigger condition: first paying
          customer signs").

    - **Phase 069 scope:** documentation only. No `pymupdf` in `requirements.txt`.
      No subprocess fence implementation. No `PyMuPDFExtractor` skeleton in
      `extraction_service.py`.

    - **Phase 071 scope:** wires `PyMuPDFExtractor` behind the `PdfExtractor` ABC,
      implements the subprocess fence per (a) above, and treats Docling (MIT) as the
      primary path. The `EXTRACTOR_PRIMARY` env var + per-document fallback (RAG-DOCLING-01)
      surface the AGPL fallback choice operationally.

    **Re-trigger conditions for re-litigation:**

    - First paying customer signs → re-evaluate path (b) PyMuPDF Pro purchase.
    - Subprocess fence breaks for performance / latency reasons in Phase 071 → must
      formally supersede this appendix (path (a) is the contract; can't be quietly
      moved in-process).
    - Artifex changes PyMuPDF license terms → re-read the upstream license + revisit.
    - Closed-core distribution moves to a model that links PyMuPDF in-process before
      a Pro license is acquired → blocked by this appendix; must be re-decided.

    ```

    Do NOT add a trailing `## D-PRD-08` if one doesn't already follow D-PRD-07 — preserve the existing file's tail (could be EOF or another section). After insertion, the next text in the file should be whatever was previously after D-PRD-07's body.

    Do NOT modify any other section of `DECISIONS.md`. The diff should be additive only.

    Do NOT add this entry to a Table of Contents (DECISIONS.md does not currently maintain one; do not introduce one).

    Do NOT edit `.planning/PRDs/v2.6.md` to mark Q-v2.6-06 closed. The PRD is signed off and locked at 2026-05-12. The closure happens in DECISIONS.md per D-069-05; Q-v2.6-06 will be marked closed in a future PRD amendment if/when the user chooses (out of scope for Phase 069).

    Do NOT add `pymupdf` to `backend/requirements.txt` or `backend/requirements-dev.txt`. Phase 069 ships zero new Python deps.

    Do NOT create `backend/app/services/pymupdf_extractor.py` or any PyMuPDFExtractor stub. CONTEXT.md §specifics: "One concrete extractor in Phase 069. No `DoclingExtractor`, no `PyMuPDFExtractor` skeletons, no placeholder classes."
  </action>
  <verify>
    <automated>grep -q '^### Appendix — Q-v2.6-06 closure' .planning/prd-reset/DECISIONS.md &amp;&amp; grep -q 'D-PRD-07 Appendix\|D-PRD-07 ... Appendix\|Q-v2.6-06 closure' .planning/prd-reset/DECISIONS.md &amp;&amp; grep -q 'AGPL-3.0' .planning/prd-reset/DECISIONS.md &amp;&amp; grep -q 'subprocess fence' .planning/prd-reset/DECISIONS.md &amp;&amp; grep -q 'PyMuPDF Pro' .planning/prd-reset/DECISIONS.md &amp;&amp; grep -q 'D-PRD-03' .planning/prd-reset/DECISIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '^### Appendix — Q-v2.6-06 closure: PyMuPDF AGPL fallback license posture (Phase 069)$' .planning/prd-reset/DECISIONS.md` (exact heading present).
    - `grep -q 'Closes:\*\* Q-v2.6-06' .planning/prd-reset/DECISIONS.md` (closes-line present).
    - `grep -q 'AGPL-3.0' .planning/prd-reset/DECISIONS.md` (license name documented).
    - `grep -q 'subprocess fence' .planning/prd-reset/DECISIONS.md` (mechanism documented).
    - `grep -q 'PyMuPDF Pro' .planning/prd-reset/DECISIONS.md` (commercial path documented).
    - `grep -q 'D-PRD-03' .planning/prd-reset/DECISIONS.md` AND the appendix references it (closed-core / open-peripherals constraint cited).
    - `grep -q 'Phase 071' .planning/prd-reset/DECISIONS.md` (defers fence implementation explicitly).
    - `grep -q 'Phase 069' .planning/prd-reset/DECISIONS.md` (notes the authoring phase).
    - Appendix line count: filter non-blank lines between the `### Appendix` heading and the next `## ` or EOF. `awk '/^### Appendix — Q-v2.6-06 closure/{f=1; next} f && /^## /{exit} f' .planning/prd-reset/DECISIONS.md | grep -v '^$' | wc -l` returns ≥ 30 and ≤ 80 (per D-069-05 + PATTERNS.md "~30-50 lines").
    - No new code/dep artifacts created: `ls backend/app/services/pymupdf_extractor.py 2>/dev/null` returns nothing; `grep -q '^pymupdf' backend/requirements.txt` returns no match; `grep -q '^pymupdf' backend/requirements-dev.txt 2>/dev/null` returns no match.
    - `.planning/PRDs/v2.6.md` is UNMODIFIED: `cd "C:\Vibe Apps\Agentic RAG" && git diff --stat .planning/PRDs/v2.6.md` shows no changes (the PRD body stays signed-off; closure recorded only in DECISIONS.md).
    - The appendix is appended UNDER D-PRD-07, not inserted elsewhere: `awk '/^## D-PRD-07/{p=NR} /^### Appendix — Q-v2.6-06 closure/{a=NR} END{exit !(p>0 && a>p)}' .planning/prd-reset/DECISIONS.md` exits 0.
  </acceptance_criteria>
  <done>D-PRD-07 Appendix written under the existing D-PRD-07 entry; covers AGPL-3.0 + D-PRD-03 + subprocess fence + dev posture + commercial path; ~30-50 lines; references Phase 071 for fence implementation; no code/dep side-effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan modifies a single Markdown ADR file in the planning tree. No runtime code, no I/O sinks, no auth boundaries, no endpoints. |

## STRIDE Threat Register

Phase 069 Plan 02 is documentation-only — no executable code, no runtime artifacts. STRIDE analysis is degenerate; no threats apply.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-069.02-01 | Information Disclosure | License posture documented in a tracked planning file | accept | DECISIONS.md is the project's authoritative ADR log; the AGPL posture is information that future maintainers + auditors MUST see. Publishing this in the planning tree is the point of the artifact. Repository visibility is governed at the repo level (project is private during dev). |
| T-069.02-02 | Tampering | Future edit to the appendix could weaken license posture | mitigate | Re-trigger conditions section explicitly lists what must trigger re-litigation. Any future supersession requires its own ADR entry (project convention — D-PRD-NN entries are immutable; supersessions add new entries that reference the old one). Git history preserves the original text. |

**Verdict: no new code, no new runtime threats.** The appendix is a textual artifact; threat model for Phase 069 Plan 02 is degenerate.
</threat_model>

<verification>
**Per-task verification commands are in `<verify>` blocks above. Plan-level cross-cuts:**

1. `grep -q 'Q-v2.6-06' .planning/prd-reset/DECISIONS.md` — closure tag present.
2. `grep -A2 '^### Appendix — Q-v2.6-06 closure' .planning/prd-reset/DECISIONS.md | head -5` — appendix header + first lines visible (manual eyeball).
3. `cd "C:\Vibe Apps\Agentic RAG" && git diff --stat .planning/prd-reset/DECISIONS.md` — diff is additive only (insertions only, no deletions in unrelated regions; ~30-50 lines added).
4. `cd "C:\Vibe Apps\Agentic RAG" && git diff --stat .planning/PRDs/v2.6.md backend/` — should be empty (Plan 02 touches nothing else).
5. Markdown lint sanity: `cd "C:\Vibe Apps\Agentic RAG" && grep -nE '^### Appendix' .planning/prd-reset/DECISIONS.md` — appendix heading uses `###` level (matches D-PRD-07's subsection depth).
</verification>

<success_criteria>
SC#4 (Q-v2.6-06 closed via D-PRD-07 appendix): satisfied when `.planning/prd-reset/DECISIONS.md` contains a `### Appendix — Q-v2.6-06 closure: PyMuPDF AGPL fallback license posture (Phase 069)` subsection under D-PRD-07, the appendix covers the 5 required facts (AGPL-3.0 / D-PRD-03 / subprocess fence / dev posture / commercial paths), and no code/dep side-effects exist.

SC#1, SC#2, SC#3: NOT in scope of this plan — owned by Plan 01.
</success_criteria>

<output>
After completion, create `.planning/phases/069-pdfextractor-abstraction-scaffold/069-02-SUMMARY.md` per the GSD summary template. Capture:
- Final appendix text (paste verbatim into summary or reference the DECISIONS.md line range it occupies).
- Line count of the appendix (target 30-50).
- Confirmation that no code / no deps changed (`git diff --stat backend/` empty; `git diff --stat .planning/PRDs/v2.6.md` empty).
- Q-v2.6-06 closure recorded; phase 069 SC#4 satisfied.
- Note for Phase 071: this appendix is the contract — PyMuPDF wire-in MUST implement subprocess fence per path (a) unless a follow-up ADR explicitly supersedes.
</output>
