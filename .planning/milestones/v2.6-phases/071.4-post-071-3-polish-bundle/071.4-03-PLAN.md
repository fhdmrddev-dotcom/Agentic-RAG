---
phase: 071.4
plan: 03
type: execute
wave: 3
depends_on: []
files_modified:
  - frontend/src/<TBD — discovered during Task 1>
  - .planning/reported-bugs/document-status-not-realtime-on-upload.md
  - .planning/reported-bugs/reingest-button-missing-from-documents-page.md
autonomous: false
requirements: []
related_bugs: [BUG-260516-01, BUG-260516-03]
notes: |
  Two concerns, both touching the documents-list page:

  **(A) Realtime status (BUG-260516-01) — primary task:**
  Two implementation paths possible (decision during Task 1 investigation):

  (a1) Realtime subscription exists but isn't reconciling → fix the handler
       to refetch on event arrival per CLAUDE.md D-v2.5-03 "Realtime is a
       best-effort hint, always reconcile via fetch on (re)connect".
  (a2) No subscription exists → ship a polling shim: setInterval(refetch,
       3000) while any row in the list has status in {pending, processing}.
       Clear interval when all rows are terminal (completed | failed).

  Both are acceptable per the bug report. Polling shim is simpler + more
  robust against Realtime channel drops; subscription is more elegant +
  doesn't burn requests on idle pages. Pick whichever the existing
  documents-list architecture suggests. Document the choice in SUMMARY.

  **(B) Add Reingest action button (BUG-260516-03) — co-deliverable:**
  The Library Health page already has a per-row Reingest button that hits
  POST /documents/{id}/reextract. The Documents page lacks it — users have
  to navigate to a secondary page to re-extract. Copy the existing handler
  from the Library Health implementation and surface a per-row "Reingest"
  action (button or menu item depending on the row's existing affordance
  pattern). ~10-15 LOC.

  Verification REQUIRES live upload via Chrome MCP — observe the status
  badge transition pending → processing → completed without manual refresh,
  AND click the new Reingest button to confirm it triggers /reextract.

must_haves:
  truths:
    - "Documents-list page transitions row status without user-initiated refresh"
    - "Chrome MCP verification: upload a small fixture, watch status update in <60s"
    - "Documents page has a per-row Reingest button/action that triggers POST /documents/{id}/reextract"
    - "Chrome MCP verification: clicking Reingest returns 202 and the row transitions back to pending → processing → completed"
    - "BUG-260516-01 frontmatter flipped from status: open to status: closed"
    - "BUG-260516-03 frontmatter flipped from status: open to status: closed"
    - "Architectural decision (Realtime subscription vs polling shim) recorded in SUMMARY.md"
---

<objective>
Make uploaded documents' status badges update in real time on the documents
list. Either by wiring Supabase Realtime properly (D-v2.5-03) or by adding a
narrow polling shim. Verify via live upload using Chrome MCP.
</objective>

<tasks>

<task id="1" name="Investigate current state">
- Grep frontend for documents-list page + any Realtime subscription code (`supabase.channel`, `.on('postgres_changes'`, `useRealtime*`, `useDocuments*`).
- Determine:
  - Does a subscription exist on `documents` table?
  - If yes: what's the event handler? Does it refetch? Does it patch local state?
  - If no: what hook fetches the list initially? Where would a polling shim hook in?
- Update `files_modified` in this plan's frontmatter with the actual file paths.
- Decide path: (a) fix subscription, (b) add polling shim. Record decision.
</task>

<task id="2" name="Apply the realtime status fix">
- **If path (a) — Realtime subscription fix:** wire the event handler to refetch the list (or patch the affected row) on UPDATE events. Add reconnect handler if missing.
- **If path (b) — polling shim:** in the documents-list hook, add a `useEffect` that polls every 3s while `documents.some(d => d.status in ['pending', 'processing'])`. Stop polling when all rows are terminal.
- Whichever path: keep the change SMALL (< 30 LOC). This is a polish phase, not a refactor.
- Commit: `fix(071.4-03): <subscription | polling> for documents-list status — BUG-260516-01`
</task>

<task id="3" name="Add Reingest action button to Documents page">
- Locate the Library Health page's per-row Reingest button (grep frontend for `reextract` or `reingest`).
- Read its handler — likely calls `POST /documents/{id}/reextract` via the project's API client.
- Add the same action to the Documents page rows (button, menu item, or row-hover action depending on the existing UI pattern for the documents list).
- Visual consistency: match the existing row-action affordance pattern; don't introduce a new style.
- Keep change small (< 15 LOC).
- Commit: `feat(071.4-03): add Reingest action to Documents page — BUG-260516-03`
</task>

<task id="4" name="Verify live via Chrome MCP">
- Backend + frontend running (from 071.4-02 setup).
- Login as fhdmrd@gmail.com / 123456.
- Navigate to documents list (likely a Library or My Documents page).
- **Realtime verification:** Take a tiny PDF (smallest fixture in `backend/tests/fixtures/` or any small PDF). Upload via the UI. Watch the status badge. Confirm: pending → processing → completed without manual refresh, within ~60s.
- **Reingest verification:** click the new Reingest button on an existing completed row. Confirm: row transitions back to pending → processing → completed without manual refresh. Network panel shows POST /documents/{id}/reextract → 202.
- Take screenshots: initial pending state, mid-processing, final completed, Reingest button click.

**This is a checkpoint:human-verify** — operator confirms both transitions.
</task>

<task id="5" name="Close both bugs + SUMMARY">
- Edit `.planning/reported-bugs/document-status-not-realtime-on-upload.md` frontmatter: `status: open` → `status: closed`. Add closing note with commit SHA + verification timestamp + chosen path (subscription or polling).
- Edit `.planning/reported-bugs/reingest-button-missing-from-documents-page.md` frontmatter: `status: open` → `status: closed`. Add closing note with commit SHA.
- Write `.planning/phases/071.4-post-071-3-polish-bundle/071.4-03-SUMMARY.md`. Include the decision rationale (why subscription vs why polling) AND note the Reingest button co-deliverable.
- Commit: `docs(071.4-03): close BUG-260516-01 + BUG-260516-03 + SUMMARY`
</task>

</tasks>

<success_criteria>
- [ ] Status updates without manual refresh (verified via live upload)
- [ ] Reingest button visible on Documents page rows; click triggers /reextract → 202
- [ ] Chrome MCP screenshots of both transitions committed or referenced in SUMMARY
- [ ] BUG-260516-01 + BUG-260516-03 both status flipped to closed
- [ ] Architectural choice (subscription vs polling) documented in SUMMARY
</success_criteria>
