---
phase: 071.4
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - frontend/src/<TBD — discovered during Task 1>
  - .planning/reported-bugs/knowledge-health-low-confidence-infinite-fetch-loop.md
autonomous: false
requirements: []
related_bugs: [BUG-260516-02]
notes: |
  React useEffect dep-loop fix. Classic root causes (per the bug report):
  unstable object/array dep, unmemoized callback dep, state-set-by-fetch in
  dep array, unstable queryKey. The fix is almost always 1-5 LOC in one file.

  Verification REQUIRES Chrome MCP (per feedback_chrome_mcp_testing memory):
  navigate to Library Health → Stale/Low-confidence, watch DevTools Network
  tab, confirm exactly one GET /knowledge-health/low-confidence/documents
  request fires on mount, no further requests fire while idle. UI does not
  shake.

must_haves:
  truths:
    - "Frontend offender located (file path + line range)"
    - "Fix applied (memoization, primitive-dep flattening, or queryKey stabilization)"
    - "Chrome MCP verification: 1 fetch on mount, no shake, no follow-up fetches while idle"
    - "BUG-260516-02 frontmatter flipped from status: open to status: closed with closing note"
---

<objective>
Stop the Library Health → Stale/Low-confidence page from infinite-fetching
`/knowledge-health/low-confidence/documents`. Verify the fix lands via Chrome
MCP — DevTools Network panel must show exactly one request on mount.
</objective>

<tasks>

<task id="1" name="Locate the offending hook/component">
- Grep frontend for `/knowledge-health/low-confidence/documents` to find the consumer.
- Likely entry points: `frontend/src/pages/library-health/`, `frontend/src/hooks/useLowConfidence*`, `frontend/src/lib/api/knowledge-health*`.
- Read the consumer + any custom hook it uses.
- Identify the unstable dep: look for `useEffect`, `useQuery`, `useSWR`, `useInfiniteQuery` with deps containing object/array literals, callbacks not wrapped in useCallback, or state that the fetch itself writes.
- Update `files_modified` in this plan's frontmatter with the actual file path.
</task>

<task id="2" name="Apply the fix">
- Smallest viable change. Preferred ordering:
  (a) If deps contain `{ offset, limit }` literal → flatten to `[offset, limit]` (primitives).
  (b) If a callback is in deps → wrap in `useCallback`.
  (c) If `setState` from the fetch is in deps → remove it from deps (use functional setState if needed for stale closure).
  (d) If `queryKey` is a fresh array literal in every render → memoize via `useMemo`.
- Commit: `fix(071.4-02): stabilize <hook-name> deps — BUG-260516-02`
</task>

<task id="3" name="Verify via Chrome MCP">
- Start dev server (`cd backend && source venv/Scripts/activate && uvicorn app.main:app --reload --port 8000 &` if not running; `cd frontend && npm run dev &` for the frontend).
- Login at http://localhost:5173/ as fhdmrd@gmail.com / 123456 (from reference_local_dev_app memory).
- Navigate to Library Health → Stale/Low-confidence page.
- Chrome DevTools MCP: list_network_requests filtered for `low-confidence`. Confirm exactly 1 GET fires on mount. Wait 10s, confirm no follow-ups.
- Take a screenshot of the stabilized page (no shake) as evidence.

**This is a checkpoint:human-verify** — operator confirms the page no longer shakes and the network panel shows a single fetch.
</task>

<task id="4" name="Close the bug + SUMMARY">
- Edit `.planning/reported-bugs/knowledge-health-low-confidence-infinite-fetch-loop.md` frontmatter: `status: open` → `status: closed`. Add a closing note with the commit SHA and verification timestamp.
- Write `.planning/phases/071.4-post-071-3-polish-bundle/071.4-02-SUMMARY.md`.
- Commit: `docs(071.4-02): close BUG-260516-02 + SUMMARY`
</task>

</tasks>

<success_criteria>
- [ ] Offender located and fix applied
- [ ] Chrome MCP verification: 1 request on mount, no shake, idle = no requests
- [ ] BUG-260516-02 status flipped to closed
- [ ] SUMMARY.md committed
</success_criteria>
