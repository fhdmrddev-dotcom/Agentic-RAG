---
phase: 081.1-settings-architecture-unification
plan: 03
subsystem: api
tags: [asyncpg, settings, db-cache, pydantic, fastapi]

# Dependency graph
requires:
  - phase: 081.1-01
    provides: "async DB cache functions (_load_settings_from_db, save_app_settings, _load_model_overrides) + 4-tier async resolution functions (get_per_call_timeout_async, get_model_capability_async)"
  - phase: 081.1-02
    provides: "migration 053 applied to live DB; settings_override.json migrated to app_settings table"
provides:
  - "load_app_settings() reads from DB cache, not from settings_override.json"
  - "load_app_settings_async() for async callers with DB refresh"
  - "_build_providers() merges DB provider_model_lists JSONB + model_capabilities_overrides + static registry (D-13/D-17/D-24)"
  - "Settings API writes to DB via save_app_settings() (D-20)"
  - "get_per_call_timeout_async() active in all threads.py streaming paths"
  - "get_model_capability_async() active in threads.py model resolution"
  - "Zero code paths read settings_override.json (except migration runner in main.py)"
affects: [081.1-04, threads, settings-api, provider-routing]

# Tech tracking
tech-stack:
  added: []
  patterns: ["_build_settings_from_row shared constructor for sync+async load paths", "_val/_val_bool DB-row-to-field helpers with env fallback chain", "provider_model_lists JSONB merge with DB-registered + static registry models"]

key-files:
  created: []
  modified:
    - "backend/app/models/user_settings.py"
    - "backend/app/api/settings.py"
    - "backend/app/api/threads.py"
    - "backend/app/services/anthropic_service.py"

key-decisions:
  - "Shared _build_settings_from_row() function eliminates code duplication between sync/async load paths"
  - "_val/_val_bool helpers provide clean DB-row > env_settings > hardcoded-default fallback chain, replacing _str/_int/_float/_bool type-coercion helpers"
  - "Static registry models now sorted alphabetically when appended (was unsorted in original _build_providers)"

patterns-established:
  - "_build_settings_from_row: single function constructs UserEffectiveSettings from a dict -- shared by load_app_settings (sync/cache) and load_app_settings_async (async/DB)"
  - "provider_model_lists JSONB merge: user ordering first, then DB-registered models alphabetically, then static registry models alphabetically"

requirements-completed: [SETTINGS-UNIFY-01, SETTINGS-UNIFY-02]

# Metrics
duration: 8min
completed: 2026-05-27
---

# Phase 081.1 Plan 03: Consumer Rewire Summary

**All settings consumers rewired from file-based I/O to DB-backed async path; file-I/O code deleted; 4-tier model resolution active in streaming hot paths**

## Performance

- **Duration:** 8 min
- **Started:** 2026-05-27T10:35:32Z
- **Completed:** 2026-05-27T10:44:06Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments
- Eliminated all file-based settings I/O from user_settings.py (deleted _OVERRIDE_FILE, _load_override, save_override, _override_cache, type-coercion helpers)
- Rewired Settings API to write via async save_app_settings() and read via load_app_settings_async() -- API contract (FullSettingsResponse/SettingsUpdate) unchanged per D-21
- Activated 4-tier async resolution (DB > env CSV > static > default) in all 3 streaming paths (Anthropic, Google, OpenAI) and model resolution in threads.py
- Provider model lists now stored as JSONB dict in provider_model_lists column (D-17) instead of individual CSV keys
- Zero stale file-I/O references remain in backend/app/ (migration runner in main.py is the only legitimate reference)

## Task Commits

Each task was committed atomically:

1. **Task 1: Rewrite user_settings.py -- DB-backed load_app_settings + _build_providers merge** - `6d16a75` (feat)
2. **Task 2: Rewire settings.py API to async DB write path** - `d1eaac6` (feat)
3. **Task 3: Activate 4-tier resolution in config.py + verify no _OVERRIDE_FILE references remain** - `b7af10f` (feat)

## Files Created/Modified
- `backend/app/models/user_settings.py` - Complete rewrite: DB-backed settings with sync+async API, _build_providers merges 3 model sources, file-I/O code deleted
- `backend/app/api/settings.py` - Rewired to async DB path: _build_response async, save_app_settings replaces save_override, provider_model_lists JSONB
- `backend/app/api/threads.py` - 3x get_per_call_timeout -> await get_per_call_timeout_async; 2x get_model_capability -> await get_model_capability_async
- `backend/app/services/anthropic_service.py` - Updated stale _load_override comment to reference DB-backed cache

## Decisions Made
- Created _build_settings_from_row() shared helper to avoid duplicating the UserEffectiveSettings construction between sync and async load paths
- Introduced _val/_val_bool helpers for clean DB-row > env_settings > default fallback chains, replacing the old _str/_int/_float/_bool type-coercion helpers that were designed for JSON string values
- Static registry models are now sorted alphabetically when appended to provider model lists (was unsorted before; D-24 specifies alphabetical merge)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed stale comment in anthropic_service.py**
- **Found during:** Task 3 (codebase sweep)
- **Issue:** Comment at anthropic_service.py:200 referenced "_load_override" and "file-backed override has a 5s TTL cache" -- stale after Plan 03
- **Fix:** Updated comment to reference "DB-backed cache has a 30s TTL (Phase 081.1)"
- **Files modified:** backend/app/services/anthropic_service.py
- **Committed in:** b7af10f (Task 3 commit)

---

**Total deviations:** 1 auto-fixed (1 bug -- stale comment)
**Impact on plan:** Trivial comment fix. No scope creep.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required. The migration runner (Plan 02) already migrated settings_override.json to the DB.

## Next Phase Readiness
- Plan 04 (integration tests) can now verify the full flow: Settings API PUT -> DB write -> cache invalidation -> load returns updated values
- All consumers (threads.py, documents.py, extraction_service.py, settings.py) now read from DB cache
- The sync load_app_settings() path (used by ingest_document at documents.py:1403 and tool_args_progress_emit_boundary_bytes) reads from _settings_cache populated by async loads; if cache is cold, returns env_settings defaults

## Self-Check: PASSED

- All 4 modified files exist on disk
- All 3 task commits verified in git log (6d16a75, d1eaac6, b7af10f)
- Unit tests: 8/8 passing (test_settings_cache.py)

---
*Phase: 081.1-settings-architecture-unification*
*Completed: 2026-05-27*
