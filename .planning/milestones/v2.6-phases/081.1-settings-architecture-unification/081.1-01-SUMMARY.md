---
phase: 081.1-settings-architecture-unification
plan: 01
subsystem: database, api
tags: [asyncpg, postgres, migration, settings, cache, ttl]

# Dependency graph
requires:
  - phase: 073-asyncpg-pool-integration
    provides: asyncpg pool singleton via get_pg_pool()
provides:
  - Migration 053 with app_settings new columns + model_capabilities_overrides table
  - Async DB cache functions (_load_settings_from_db, _load_model_overrides) with 30s TTL
  - 4-tier async model capability resolution (get_per_call_timeout_async, get_model_capability_async)
  - save_app_settings() with _is_valid_api_key sentinel guard ported from save_override
affects: [081.1-02 migration runner, 081.1-03 consumer rewire, 081.1-04 integration tests]

# Tech tracking
tech-stack:
  added: []
  patterns: [async-db-ttl-cache, 4-tier-resolution-chain, invalidate-on-write]

key-files:
  created:
    - supabase/migrations/053_settings_unification.sql
    - backend/tests/unit/test_settings_cache.py
  modified:
    - backend/app/models/user_settings.py
    - backend/app/config.py

key-decisions:
  - "30s TTL cache with invalidate-on-write pattern for async DB settings reads (D-06/D-07/D-08)"
  - "4-tier resolution: DB > env CSV > static MODEL_CAPABILITIES dict > 300s default (D-11/D-12)"
  - "_is_valid_api_key sentinel guard ported to save_app_settings for DB write path (D-14)"

patterns-established:
  - "async-db-ttl-cache: Module-level dict + float timestamp + async loader with lazy get_pg_pool import"
  - "4-tier-resolution: DB overrides checked first, then sync path tiers unchanged"
  - "additive-alongside: New async functions coexist with old file-based ones until Plan 03 flips"

requirements-completed: [SETTINGS-UNIFY-01, SETTINGS-UNIFY-02]

# Metrics
duration: 4min
completed: 2026-05-27
---

# Phase 081.1 Plan 01: Schema Foundation + Async DB Cache Summary

**Migration 053 DDL (15 new app_settings columns + model_capabilities_overrides table with RLS) plus async DB cache infrastructure with 30s TTL and 4-tier model capability resolution -- all additive alongside existing file-based code**

## Performance

- **Duration:** 4 min
- **Started:** 2026-05-27T09:18:19Z
- **Completed:** 2026-05-27T09:22:39Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Migration 053 with 15 `ADD COLUMN IF NOT EXISTS` clauses on app_settings (llm_provider, llm_model, web_search, sandbox, context, sub_agent, openrouter, ollama, provider_model_lists JSONB, title_drafting_config JSONB, sub_agent_config JSONB, token_capture_enabled) + model_capabilities_overrides table with RLS
- Async DB cache infrastructure: `_load_settings_from_db()`, `_load_model_overrides()`, `save_app_settings()`, `invalidate_settings_cache()` -- all using 30s TTL in-process cache via asyncpg pool
- 4-tier async resolution: `get_per_call_timeout_async()` and `get_model_capability_async()` in config.py (DB > env CSV > static dict > default)
- All existing `_load_override()`, `save_override()`, `load_app_settings()` functions remain UNCHANGED -- zero regression risk

## Task Commits

Each task was committed atomically:

1. **Task 1: Write migration 053_settings_unification.sql** - `0860b7f` (feat)
2. **Task 2 RED: Failing tests for async cache + resolution** - `b8d61dd` (test)
3. **Task 2 GREEN: Implement async DB cache + 4-tier resolution** - `a3cede0` (feat)

## TDD Gate Compliance

- RED gate: `b8d61dd` -- 8 tests fail with AttributeError (functions not yet implemented)
- GREEN gate: `a3cede0` -- 8/8 tests pass
- REFACTOR gate: skipped (code is clean, no cleanup needed)

## Files Created/Modified
- `supabase/migrations/053_settings_unification.sql` - Schema DDL for 15 new app_settings columns + model_capabilities_overrides table + RLS + idempotent seed
- `backend/app/models/user_settings.py` - Added async DB cache functions (_load_settings_from_db, invalidate_settings_cache, save_app_settings, _load_model_overrides, invalidate_model_overrides_cache) alongside existing file-based code
- `backend/app/config.py` - Added get_per_call_timeout_async and get_model_capability_async with DB-first resolution tier
- `backend/tests/unit/test_settings_cache.py` - 8 unit tests covering TTL warm/cold, invalidation, model overrides cache, 4-tier resolution precedence

## Decisions Made
- 30s TTL cache matches D-06/D-08 spec; file-based 5s cache untouched
- Lazy import of `get_pg_pool` inside each async function to avoid circular imports at module load
- `save_app_settings` builds parameterized `$N` queries from code-controlled column names (T-081.1-01 mitigation)
- `get_model_capability_async` merges DB row onto static defaults then sets `capability_source="db_override"` for caller disambiguation

## Deviations from Plan

None -- plan executed exactly as written.

## Issues Encountered
- Worktree does not have its own `venv/` -- used the main repo's Python interpreter for test execution. This is a worktree infrastructure quirk, not a code issue.

## User Setup Required
None -- migration 053 must be applied via Supabase SQL editor before Plan 02 (migration runner) executes. This is standard per CLAUDE.md migration workflow.

## Next Phase Readiness
- Migration 053 SQL ready for apply
- Async cache functions ready for Plan 02 (migration runner) and Plan 03 (consumer rewire) to use
- All 8 unit tests green, existing tests unaffected

## Self-Check: PASSED

- All 5 key files FOUND on disk
- All 3 commit hashes FOUND in git log
- 8/8 unit tests pass
- 3/3 existing settings tests pass (no regression)

---
*Phase: 081.1-settings-architecture-unification*
*Completed: 2026-05-27*
