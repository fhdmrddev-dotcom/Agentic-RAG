---
phase: 068-streamsprovider-context-lift
plan: 02
plan_id: 068-02
type: execute
wave: 2
depends_on: [068-01]
files_modified:
  - frontend/src/providers/StreamsProvider.tsx
  - frontend/src/hooks/useMessages.ts
  - frontend/src/__tests__/providers/streamsProvider.test.tsx
autonomous: true
requirements: [STREAMS-PROVIDER-01]
tags: [frontend, zustand, useMessages-rewrite, branch-d-3-regression, binding-gate]

must_haves:
  truths:
    - "The `clearThreadBucket` / `clearMessages` action refuses to wipe a bucket whose thread is `streamingThreadIdRef.current` — Branch D-3 guard verbatim (L-068-01 / SC#2). Fires identically for `surfaceId='chat'` AND `surfaceId='mock-eval'`."
    - "Concurrent `reconcile()` calls bail at the top guard (L-068-02 single-bit lock); lock releases in finally even on thrown exception (SC#4)."
    - "`setViewingThread` is the SOLE writer of `activeThreadIdRef.current` (L-068-03); post-await guards inside actions discard stale writes. Static-analysis evidence: Plan 01 Task 3 acceptance_criteria gates on `grep -cE 'activeThreadIdRef\\.current\\s*=' StreamsProvider.tsx == 1` (external grep, not in-test readFileSync)."
    - "`setViewingThread` is the SOLE programmatic owner of post-switch reconcile firing — the pre-lift `ChatArea.tsx:165` `useEffect([thread?.id])` semantics are preserved verbatim via a new fire path inside the lifted action body (RESEARCH §Finding #8 point 2 — mount-time-reconcile-fire responsibility shifts into `setViewingThread` post-lift). `setViewingThread(null)` is a no-op for reconcile (D-068-08 listener gate semantics extended to programmatic path). This is the producer-side commitment that Plan 3 Task 2's pre-flight gate consumes (resolves the iter-2 MED #4 producer gap)."
    - "Incoming SSE deltas route to the streaming thread's bucket, NOT the viewing thread's bucket (L-068-04)."
    - "Reconcile's runId-match dedup reuses `m.runId === run.run_id` message's id as the assistantId (L-068-05)."
    - "`loadMessages` MERGE 3-clause filter preserves live in-flight temp placeholders (L-068-06)."
    - "Subscription cleanup `subscriptionsRef.current.delete(runId)` fires inside `onTerminal`, NOT inside the promise `.finally()` chain (L-068-07; safety-net finally still allowed)."
    - "`frontend/src/hooks/useMessages.ts` is a thin reader < 100 LOC delegating to named hooks; public `UseMessages` interface UNCHANGED so ChatArea.tsx:27-38 imports work without edit."
    - "Existing `frontend/src/__tests__/hooks/useMessages.test.ts` test 'clearMessages does not wipe a bucket whose thread is currently streaming (Branch D-3)' passes UNMODIFIED against the new thin reader."
    - "New `frontend/src/__tests__/providers/streamsProvider.test.tsx` covers L-068-01..07 + SC#1/2/4; the L-068-01 Branch D-3 test uses `it.each(['chat', 'mock-eval'])` per-surface enumeration."
    - "`isStreaming` audit completed: either (a) `isStreaming` is hoisted into Zustand state with a `useIsStreaming()` hook — chat UX preserved, OR (b) audit confirmed no live `isStreaming` consumer exists in chat surface; spinner UX unaffected by hard-coded `false`. The branch taken is documented in 068-02-SUMMARY.md."
  artifacts:
    - path: "frontend/src/providers/StreamsProvider.tsx"
      provides: "Real action bodies for sendMessage / reconcile / loadMessages / stopStream / resumeFromFailed lifted from useMessages.ts (Plan 1 LIFT-IN-PLAN-2 markers replaced); subscriptionsByRunId mirror wired at every subscribe/unsubscribe site; `setViewingThread` extended with an internal reconcile fire on non-null threadId (preserves ChatArea.tsx:165 mount-time-reconcile-fire semantics — feeds Plan 3 pre-flight gate)"
      contains: "subscriptionsRef.current.delete"
    - path: "frontend/src/hooks/useMessages.ts"
      provides: "Thin reader delegating to useThreadMessages + useStreamActions + useViewingThread"
      min_lines: 30
    - path: "frontend/src/__tests__/providers/streamsProvider.test.tsx"
      provides: "L-068-01..07 + SC#2/4 unit test coverage + setViewingThread-reconcile-fire assertion (producer-side of Plan 3 gate)"
      contains: "it.each([\"chat\", \"mock-eval\"]"
  key_links:
    - from: "frontend/src/hooks/useMessages.ts"
      to: "frontend/src/providers/StreamsProvider.tsx"
      via: "import { useThreadMessages, useStreamActions, useViewingThread }"
      pattern: "from \"@/providers/StreamsProvider\""
    - from: "frontend/src/__tests__/providers/streamsProvider.test.tsx"
      to: "frontend/src/providers/StreamsProvider.tsx"
      via: "renderHook wrapper + makeSseRecorder mocking pattern"
      pattern: "renderHook.*wrapper.*StreamsProvider"
    - from: "frontend/src/providers/StreamsProvider.tsx (setViewingThread action body)"
      to: "frontend/src/providers/StreamsProvider.tsx (reconcile action body)"
      via: "in-action call `useStreamsStore.getState().actions.reconcile(threadId, surfaceId)` when threadId !== null"
      pattern: "setViewingThread:[\\s\\S]{0,200}actions\\.reconcile"
---

<objective>
This is the BINDING GATE plan for Phase 068. The Branch D-3 regression test (L-068-01 / SC#2) is THE binding test — if it goes red, the lift broke STREAM-04-correctness-round3 in a new shape and Phase 067.5's 5/5 lived-experience cycles regress.

Two structural moves:

1. **Port useMessages.ts logic into the provider in three reviewable chunks.** Take the real bodies of `sendMessage` / `reconcile` / `loadMessages` / `stopStream` / `resumeFromFailed` from `frontend/src/hooks/useMessages.ts` (currently 1229 LOC) and rehouse them as Zustand store actions defined inside `StreamsProvider.tsx`'s mount-time `useEffect` (per RESEARCH §Pattern 4 — actions close over provider-scoped refs). Replace every `setMessagesByThread(threadId, ...)` call site with `setMessagesForBucket(surfaceId, threadId, ...)` (D-068-04 bucket model). The 7 locked invariants (L-068-01..07) survive verbatim — the file header comments on the relevant blocks must cite their L-068-NN tag for traceability. The lift is split into three tasks (2a / 2b / 2c) so a partial completion leaves the test surface in a partially-GREEN state that maps onto the actually-shipped action.

2. **Rewrite `useMessages` as a thin reader.** Public `UseMessages` interface stays IDENTICAL so `ChatArea.tsx:27-38` does not need editing. Hook body is ~30-50 LOC — `useViewingThread()` + `useThreadMessages(viewedThreadId, 'chat')` + `useStreamActions()` composed into a `useMemo`-stable return object matching the pre-lift shape.

The binding test (L-068-01 Branch D-3) MUST land FIRST (TDD-flavored — RED before GREEN), assert against the new provider boundary using `renderHook` with `wrapper: <StreamsProvider>` per RESEARCH §Finding #6, and use `it.each(['chat', 'mock-eval'])` to prove per-surface invariance (per D-068-05). The unmodified existing `useMessages.test.ts` Branch D-3 test is the third evidence layer for "preserved verbatim" per RESEARCH §Validation Architecture point 3.

Purpose: Close the substrate lift — useMessages.ts is no longer the source of truth for the chat surface; the provider is. Existing chat behavior is byte-identical (every existing test stays green); the multi-surface substrate is wired but only chat consumes it (Plan 4 adds the mock-eval consumer).

Output: One new test file, one heavily-modified provider file, one substantially-shrunk hook file. Zero schema, zero backend, zero new env var.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/068-streamsprovider-context-lift/068-CONTEXT.md
@.planning/phases/068-streamsprovider-context-lift/068-RESEARCH.md
@.planning/phases/068-streamsprovider-context-lift/068-VALIDATION.md
@.planning/phases/068-streamsprovider-context-lift/068-PATTERNS.md
@.planning/phases/068-streamsprovider-context-lift/068-01-SUMMARY.md
@frontend/src/hooks/useMessages.ts
@frontend/src/providers/StreamsProvider.tsx
@frontend/src/__tests__/hooks/useMessages.test.ts
@frontend/src/components/chat/ChatArea.tsx

<interfaces>
<!-- Public surface of useMessages.ts AFTER the rewrite. ChatArea.tsx:27-38 imports must continue to work UNCHANGED. -->

```typescript
// frontend/src/hooks/useMessages.ts — post-Plan-2 public contract (UNCHANGED from pre-lift; lines 6-29 of current file)
interface UseMessages {
  messages: Message[]
  isStreaming: boolean
  fallbackNotice: string | null
  loadMessages: (threadId: string) => Promise<void>
  sendMessage: (
    threadId: string,
    content: string,
    model?: string,
    onTitleUpdate?: (title: string) => void,
    agentMode?: string,
    provider?: string,
  ) => Promise<void>
  stopStreaming: () => Promise<void>
  abortStream: () => void
  clearMessages: () => void
  setViewingThread: (threadId: string | null) => void
  reconcile: (threadId: string) => Promise<void>
  resumeFromFailed: (failedMessage: Message) => Promise<void>
}
export function useMessages(): UseMessages
```

<!-- Existing mocking primitives in `frontend/src/__tests__/hooks/useMessages.test.ts:23-91` — REUSE VERBATIM in new test file. -->
```typescript
// vi.mock("@/lib/api", ...) + vi.mock("@/lib/supabase", ...) + makeSseRecorder() helper
// All lifted byte-identical into streamsProvider.test.tsx per PATTERNS.md §test analog.
```
</interfaces>

<branch_d_3_predicate>
The Plan 1 scaffold placed the Branch D-3 guard inside `StreamsProvider.tsx`'s `clearThreadBucket` action body. The literal predicate (per L-068-01 / useMessages.ts:589-598):

```typescript
const tid = activeThreadIdRef.current
if (tid && tid !== streamingThreadIdRef.current) {
  useStreamsStore.setState((state) => {
    const surfMap = state.bucketsBySurface.get(surface)
    if (!surfMap?.has(tid)) return state
    const nextSurfMap = new Map(surfMap)
    nextSurfMap.delete(tid)
    return { bucketsBySurface: new Map(state.bucketsBySurface).set(surface, nextSurfMap) }
  })
  lastViewedAtRef.current.delete(tid)
}
```

The predicate `tid && tid !== streamingThreadIdRef.current` is BYTE-IDENTICAL to the source at `useMessages.ts:590`. Plan 2's executor MUST NOT modify this predicate. The block was placed by Plan 1; Plan 2 wires the surrounding action set so the predicate is reached.
</branch_d_3_predicate>

<environment_notes>
- The existing `frontend/src/__tests__/hooks/useMessages.test.ts` Branch D-3 test at lines 536-604 is THE third evidence layer for "preserved verbatim". This file must stay AT ITS CURRENT LOCATION and pass WITHOUT MODIFICATION post-rewrite. If you find yourself editing this file, stop — the abstraction broke.
- `subscriptionsByRunId: Set<string>` was added to `StreamsState` in Plan 1 Task 2 as the Zustand-visible mirror of `subscriptionsRef.current.keys()`. Plan 2 Task 2c wires the add/remove sites inside the lifted `sendMessage` / `reconcile` / `stopStream` bodies — every `subscriptionsRef.current.set(runId, controller)` gets a matching `setState((s) => ({ subscriptionsByRunId: new Set(s.subscriptionsByRunId).add(runId) }))`; every `subscriptionsRef.current.delete(runId)` gets a matching `setState` remove call. These two are mirrored, not redundant.
- The TS path alias `@/` resolves to `frontend/src/` per `frontend/vitest.config.ts:13-15`.
- React 19 `useEffect` cleanup semantics under StrictMode are documented in RESEARCH §Finding #3 — the unmount cleanup must be idempotent (mirror useMessages.ts:1209-1214 verbatim).
- **Verification command hygiene (Windows PowerShell):** verify commands MUST NOT pipe through `| tail` — `npm test` and `npm run build` exit codes are dropped by tail, which causes verification to report success on a failed test run. Run commands separately, no tail filter. If the verbose output is genuinely too noisy, route to a log file (`> test.log 2>&1`) instead — that preserves the exit code.
</environment_notes>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the L-068-01..07 Vitest regression test FIRST (RED before GREEN — TDD discipline per RESEARCH §Recommendations #5)</name>
  <files>frontend/src/__tests__/providers/streamsProvider.test.tsx</files>
  <read_first>
    - `.planning/phases/068-streamsprovider-context-lift/068-VALIDATION.md` §Per-Task Verification Map (all rows tagged 068-02-* — L-068-01..07 binding tests)
    - `.planning/phases/068-streamsprovider-context-lift/068-RESEARCH.md` §Finding #6 (Branch D-3 regression test shape) + §Validation Architecture point 3 (three-layer evidence stack)
    - `.planning/phases/068-streamsprovider-context-lift/068-PATTERNS.md` §`streamsProvider.test.tsx` (mocking analog from useMessages.test.ts:20-58 + makeSseRecorder helper at :76-91)
    - `frontend/src/__tests__/hooks/useMessages.test.ts:1-100` (full mocking setup + makeSseRecorder — port verbatim)
    - `frontend/src/__tests__/hooks/useMessages.test.ts:536-604` (existing Branch D-3 test body — adapt with `it.each` enumeration)
    - `.planning/phases/068-streamsprovider-context-lift/068-CONTEXT.md` D-068-05 (Branch D-3 binding gate per-surface)
  </read_first>
  <behavior>
    The new test file `frontend/src/__tests__/providers/streamsProvider.test.tsx` covers exactly 7 binding tests + 2 SCs:

    - **L-068-01 / SC#2 Branch D-3 (per-surface):** `it.each(["chat", "mock-eval"])` — for each surface, mount provider, fire setViewingThread('A'), trigger sendMessage('A'), record SSE callbacks via makeSseRecorder, fire onDelta('hello'), switch to thread-X, fire clearMessages, switch back to thread-A. Assert: assistant message with content containing 'hello' survives on the streamed surface; the OTHER surface's bucket is unaffected (cross-surface isolation).

    - **L-068-02 / SC#4 concurrent reconcile lock:** mount provider, call `useStreamActions().reconcile('thread-A')` twice in the same act() tick. Assert: `mockGetActiveRuns.mock.calls.length === 1` (second call bailed at top guard). Then await first call to settle, fire third call — assert call count is now 2 (lock released).

    - **L-068-03 sole writer (RUNTIME ONLY — no readFileSync grep):** setViewingThread('A'); fire mid-await navigation (loadMessages('A') with mocked slow getMessages); during the await call setViewingThread('B'); when getMessages resolves, assert no bucket-A write happened (post-await guard caught the stale write). The static-analysis side of this invariant is enforced by Plan 01 Task 3's external acceptance criterion `grep -cE 'activeThreadIdRef\\.current\\s*=' frontend/src/providers/StreamsProvider.tsx == 1` — DO NOT duplicate that check inside the Vitest test via `readFileSync`/`fs` (fragile relative-path coupling on Windows; duplicates Plan 01's external check).

    - **L-068-04 R-1 bucket routing:** mirror Phase 067.4 R-1 test shape from `useMessages.test.ts:181-231`. User on thread-A, sendMessage on thread-A, switch view to thread-B mid-stream, fire onDelta('streamed'). Assert: `useStreamsStore.getState().bucketsBySurface.get('chat').get('thread-A')` contains 'streamed' (streaming bucket); `.get('thread-B')` does NOT contain it.

    - **L-068-05 runId-match dedup:** mock getMessages to return a temp placeholder with `runId: 'run-A'` already present in the bucket. Fire reconcile('thread-A') with getActiveRuns returning `[{run_id: 'run-A', status: 'running', ...}]`. Assert: the existing placeholder's id is reused as assistantId (not a fresh `temp-${runId}` generated).

    - **L-068-06 MERGE 3-clause filter:** mock getMessages to return a DB-persisted message with `runId: 'run-DB'`. Pre-seed the bucket with two temp placeholders: `{id: 'temp-keep', runId: 'run-LIVE'}` and `{id: 'temp-drop', runId: 'run-DB'}` (already DB-persisted). Fire loadMessages('thread-A'). Assert: post-merge bucket contains the DB row + 'temp-keep'; 'temp-drop' was filtered out (`!dbRunIds.has(runId)` filter fired).

    - **L-068-07 cleanup on onTerminal:** sendMessage('thread-A'), record callbacks, fire `cbA.onTerminal('stream_end')`. Assert: `useStreamsStore.getState().subscriptionsByRunId.has('run-A')` is false. Then fire the SSE promise's .finally() (simulate by resolving subscribeToRun's mock promise). Assert: same — already deleted by onTerminal, finally is safety-net no-op.

    Each test should turn RED against the Plan 1 scaffold (action stubs still throwing `notMounted` for the async actions). After Tasks 2a/2b/2c land real action bodies, every test turns GREEN — wave-by-wave (see those tasks' acceptance_criteria for which tests turn GREEN under which sub-task).
  </behavior>
  <action>
    Create new directory `frontend/src/__tests__/providers/` and new file `frontend/src/__tests__/providers/streamsProvider.test.tsx`.

    Structure:
    1. Header: file purpose comment citing Phase 068, L-068-01..07, SC#2/4, RESEARCH §Validation Architecture three-layer stack.
    2. Imports: mirror `useMessages.test.ts:20-22` (describe/it/expect/vi/beforeEach + renderHook/waitFor/act/render). Do NOT import `readFileSync` or any `node:fs` — static-analysis is enforced externally by Plan 01 Task 3 acceptance criterion.
    3. Mocks: copy the `vi.hoisted({...})` block + `vi.mock("@/lib/api", ...)` + `vi.mock("@/lib/supabase", ...)` from `useMessages.test.ts:23-58` BYTE-IDENTICAL. Also mock any additional `@/lib/api` exports the provider's lifted actions reference.
    4. `makeSseRecorder()` helper: copy from `useMessages.test.ts:76-91` byte-identical.
    5. `renderProvider()` helper: returns `{ result, store }` where `store = useStreamsStore.getState()`. Uses `renderHook` with `wrapper: ({ children }) => <StreamsProvider>{children}</StreamsProvider>`.
    6. Test blocks per the `<behavior>` section above. Total ~600-800 LOC.

    RED commit message: `test(068-02): add streamsProvider regression tests (L-068-01..07 + SC#2/4) — RED before Plan 2 Tasks 2a/2b/2c`.

    Run `cd frontend && npm test -- src/__tests__/providers/streamsProvider.test.tsx` and CAPTURE the failure log. Several tests will fail (the L-068-01 Branch D-3 test will fail because `sendMessage` is still a throwing-`notMounted` stub from Plan 1). This is EXPECTED — Tasks 2a/2b/2c will turn them green wave-by-wave.

    DO NOT modify the existing `frontend/src/__tests__/hooks/useMessages.test.ts` file. It stays exactly as-is. It will pass post-Task-3 because Plan 2 Task 3 (the useMessages rewrite) preserves the public surface and the underlying invariant.
  </action>
  <verify>
    <automated>cd frontend && npm test -- src/__tests__/providers/streamsProvider.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/__tests__/providers/streamsProvider.test.tsx` exists
    - `grep -c "it.each(\[\"chat\", \"mock-eval\"\]" frontend/src/__tests__/providers/streamsProvider.test.tsx` returns ≥ 1 (per-surface enumeration for L-068-01)
    - `grep -cE "describe.*\"(L-068-0[1-7]|Branch D-3|concurrent reconcile|sole writer|R-1 protection|runId-match dedup|MERGE temp placeholders|cleanup on onTerminal)\"" frontend/src/__tests__/providers/streamsProvider.test.tsx` returns ≥ 7 (one describe per invariant; test grep gate per VALIDATION.md per-task verification map row strings)
    - `grep -c "makeSseRecorder" frontend/src/__tests__/providers/streamsProvider.test.tsx` returns ≥ 2 (helper defined + called from at least one test)
    - `grep -c "wrapper:" frontend/src/__tests__/providers/streamsProvider.test.tsx` returns ≥ 1 (renderHook wrapper pattern per RESEARCH §Finding #6)
    - `grep -c "<StreamsProvider>" frontend/src/__tests__/providers/streamsProvider.test.tsx` returns ≥ 1
    - `grep -cE "(readFileSync|from\s+['\"]node:fs|from\s+['\"]fs['\"])" frontend/src/__tests__/providers/streamsProvider.test.tsx` returns **0** (no filesystem grep inside the test — L-068-03 static-analysis is enforced externally by Plan 01 Task 3 acceptance criterion)
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports 0 errors for the new test file (test compiles even if it fails at runtime)
    - Vitest reports test file LOADED with multiple test failures (RED state — expected at end of Task 1). At least one test should fail with the `notMounted` error string from Plan 1's throwing stubs.
    - Existing `useMessages.test.ts` runs UNMODIFIED and exits 0 (`cd frontend && npm test -- src/__tests__/hooks/useMessages.test.ts --run` passes)
  </acceptance_criteria>
  <done>Regression test file landed RED with all 7 binding invariant assertions in place + per-surface enumeration for L-068-01. L-068-03 test is runtime-only (no readFileSync). Existing useMessages.test.ts untouched and still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2a: Lift `makeStreamCallbacks` factory + `sendMessage` action — turn L-068-04 (bucket-routing) and L-068-07 (cleanup on onTerminal) GREEN</name>
  <files>frontend/src/providers/StreamsProvider.tsx</files>
  <read_first>
    - `frontend/src/hooks/useMessages.ts` — the `makeStreamCallbacks` factory (top of file, ~lines 35-…) AND `sendMessage` body (line 673 onwards through its closing brace)
    - `frontend/src/providers/StreamsProvider.tsx` (the Plan 1 scaffold — confirm the `// LIFT-IN-PLAN-2` marker for sendMessage is present + `subscriptionsByRunId` mirror field exists in `streamsStore.ts` per Plan 01 Task 2)
    - `.planning/phases/068-streamsprovider-context-lift/068-PATTERNS.md` §Shared Patterns (bucket-routing invariant, BL-03 cleanup, makeStreamCallbacks factory analog)
    - `.planning/phases/068-streamsprovider-context-lift/068-RESEARCH.md` §Pattern 5 (getState() replacement for messagesByThreadRef mirror) + §Finding #7 (surfaceId binds in closures, NOT in lib/api.ts)
    - `.planning/phases/068-streamsprovider-context-lift/068-CONTEXT.md` L-068-04 + L-068-07
    - `frontend/src/__tests__/providers/streamsProvider.test.tsx` (Task 1's test file — note which tests this sub-task makes GREEN)
  </read_first>
  <behavior>
    After this sub-task lands, two tests in `streamsProvider.test.tsx` turn GREEN:

    - **L-068-04 R-1 bucket routing** — incoming SSE deltas route to `streamingThreadIdRef.current`'s bucket, NOT the viewing thread's.
    - **L-068-07 cleanup on onTerminal** — `subscriptionsRef.current.delete(runId)` fires inside onTerminal; `subscriptionsByRunId.has(runId)` is false post-terminal.

    The lift mechanics:

    - **`makeStreamCallbacks` factory:** copy from `useMessages.ts:35-…` (the factory function declaration through its closing brace; ~250 LOC). Place it as a module-level function inside `StreamsProvider.tsx` ABOVE the `StreamsProvider` component declaration. Adapt its `setMessages` parameter type to accept the surfaceId-bound writer. SSE event handler bodies survive byte-identical. The `surfaceId` parameter is captured by closure (RESEARCH §Finding #7) — NEVER passed through `subscribeToRun`'s signature.

    - **`sendMessage` body:** ported from `useMessages.ts:673`. Every `setMessagesForThread(threadId, ...)` call site becomes `setMessagesForBucket(opts?.surfaceId ?? 'chat', threadId, ...)`. Every `subscriptionsRef.current.set(runId, controller)` is followed by a Zustand state-mirror update: `useStreamsStore.setState((s) => ({ subscriptionsByRunId: new Set(s.subscriptionsByRunId).add(runId) }))`. The `streamingThreadIdRef.current = threadId` assignment goes before the subscribeToRun call. The `makeStreamCallbacks` invocation binds the `surfaceId` from opts via closure. Inside the constructed `onTerminal` callback (per L-068-07): `subscriptionsRef.current.delete(runId)` PLUS the matching `useStreamsStore.setState((s) => { const next = new Set(s.subscriptionsByRunId); next.delete(runId); return { subscriptionsByRunId: next } })`.

    NO change to action signatures. NO new exports. NO modifications to `lib/api.ts` (RESEARCH §Finding #7).
  </behavior>
  <action>
    Open `frontend/src/providers/StreamsProvider.tsx`.

    1. **Lift `makeStreamCallbacks` factory.** Place it as a module-level function ABOVE the `StreamsProvider` component declaration. Header comment: `// Phase 068 (L-068-04): bucket-routing factory for SSE callbacks; surfaceId captured by closure (RESEARCH §Finding #7). Source: useMessages.ts:35-…`.

    2. **Lift `sendMessage` body** into the `useStreamsStore.setState({ actions: { ... } })` block, replacing the Plan 1 `// LIFT-IN-PLAN-2` stub for `sendMessage`. Header comment: `// Phase 068 (L-068-04 + L-068-07): bucket-routing on streaming thread; cleanup on onTerminal. Source: useMessages.ts:673-…`.

    3. **Wire `subscriptionsByRunId` mirror at every subscribe site inside `sendMessage` and the onTerminal callback:**
       - `subscriptionsRef.current.set(runId, controller)` → followed by `useStreamsStore.setState((s) => ({ subscriptionsByRunId: new Set(s.subscriptionsByRunId).add(runId) }))`.
       - `subscriptionsRef.current.delete(runId)` (in onTerminal per L-068-07) → followed by `useStreamsStore.setState((s) => { const next = new Set(s.subscriptionsByRunId); next.delete(runId); return { subscriptionsByRunId: next } })`.

    After lifting, run the targeted tests and iterate until L-068-04 and L-068-07 turn GREEN:

    ```
    cd frontend && npm test -- src/__tests__/providers/streamsProvider.test.tsx --run -t "R-1 protection"
    cd frontend && npm test -- src/__tests__/providers/streamsProvider.test.tsx --run -t "cleanup on onTerminal"
    ```

    The L-068-01 / L-068-02 / L-068-03 / L-068-05 / L-068-06 tests can stay RED at the end of this sub-task — Tasks 2b and 2c make them GREEN.

    Commit: `feat(068-02-2a): lift makeStreamCallbacks + sendMessage into StreamsProvider — L-068-04 + L-068-07 GREEN`.
  </action>
  <verify>
    <automated>cd frontend && npm test -- src/__tests__/providers/streamsProvider.test.tsx --run -t "R-1 protection|cleanup on onTerminal"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "makeStreamCallbacks" frontend/src/providers/StreamsProvider.tsx` returns ≥ 1 (factory landed)
    - `grep -cE "// Phase 068 \(L-068-(04|07)" frontend/src/providers/StreamsProvider.tsx` returns ≥ 1
    - `grep -c "subscriptionsRef.current.set" frontend/src/providers/StreamsProvider.tsx` returns ≥ 1 (subscription open site)
    - `grep -c "subscriptionsRef.current.delete" frontend/src/providers/StreamsProvider.tsx` returns ≥ 1 (L-068-07 onTerminal cleanup)
    - `grep -cE "subscriptionsByRunId:\s*new Set\(s\.subscriptionsByRunId\)\.add" frontend/src/providers/StreamsProvider.tsx` returns ≥ 1 (mirror add wired)
    - `grep -cE "next\.delete\(runId\)" frontend/src/providers/StreamsProvider.tsx` returns ≥ 1 (mirror remove wired)
    - `grep -c "// LIFT-IN-PLAN-2" frontend/src/providers/StreamsProvider.tsx` returns ≤ 3 (sendMessage marker removed; reconcile/loadMessages/stopStream/resumeFromFailed markers may still be present — they're addressed in 2b/2c)
    - `cd frontend && npm test -- src/__tests__/providers/streamsProvider.test.tsx --run -t "R-1 protection"` exits 0 (L-068-04 GREEN)
    - `cd frontend && npm test -- src/__tests__/providers/streamsProvider.test.tsx --run -t "cleanup on onTerminal"` exits 0 (L-068-07 GREEN)
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports 0 errors
  </acceptance_criteria>
  <done>makeStreamCallbacks factory + sendMessage action lifted; L-068-04 (bucket routing) and L-068-07 (cleanup on onTerminal) tests GREEN; subscriptionsByRunId mirror wired at sendMessage open/close sites.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2b: Lift `reconcile` + `loadMessages` actions — turn L-068-02 (in-flight lock), L-068-05 (runId-match dedup), L-068-06 (MERGE 3-clause filter) GREEN</name>
  <files>frontend/src/providers/StreamsProvider.tsx</files>
  <read_first>
    - `frontend/src/hooks/useMessages.ts` lines 956-1140 (`reconcile` body) + the `loadMessages` declaration through its closing brace (~150 LOC)
    - `frontend/src/providers/StreamsProvider.tsx` (post-Task-2a — confirm reconcile/loadMessages still have `// LIFT-IN-PLAN-2` markers AND the Plan 1 top-of-`reconcile` guard `if (reconcileInFlightRef.current) return` is present)
    - `.planning/phases/068-streamsprovider-context-lift/068-PATTERNS.md` §Shared Patterns (in-flight lock, MERGE filter, runId-match dedup)
    - `.planning/phases/068-streamsprovider-context-lift/068-RESEARCH.md` §Pattern 5 (getState() replacement for messagesByThreadRef mirror) + §Finding #11 (mirror dies clean)
    - `.planning/phases/068-streamsprovider-context-lift/068-CONTEXT.md` L-068-02 + L-068-05 + L-068-06
  </read_first>
  <behavior>
    After this sub-task lands, three more tests in `streamsProvider.test.tsx` turn GREEN:

    - **L-068-02 / SC#4 concurrent reconcile lock** — top-of-function bail; lock releases in finally.
    - **L-068-05 runId-match dedup** — for-loop dedup `m.runId === run.run_id` reuses the existing placeholder's id as the assistantId.
    - **L-068-06 MERGE 3-clause filter** — `m.id.startsWith('temp-') && m.runId && !dbRunIds.has(m.runId)` preserves live in-flight temp placeholders.

    Lift mechanics:

    - **`reconcile`:** ported from `useMessages.ts:956-1140`. The Plan 1 top-guard wrapper (`if (reconcileInFlightRef.current) return; try { ... } finally { reconcileInFlightRef.current = false }`) is already in place from Plan 1; this sub-task wires the real body inside the `try`. `messagesByThreadRef.current.get(threadId)` reads become `useStreamsStore.getState().bucketsBySurface.get(surfaceId ?? 'chat')?.get(threadId) ?? []` per RESEARCH §Pattern 5. The for-loop runId-match dedup at useMessages.ts:990-992 ports byte-identical. Inner setState writes go through `setMessagesForBucket(surfaceId ?? 'chat', threadId, ...)`. Header comment: `// Phase 068 (L-068-02 + L-068-05): in-flight lock + runId-match dedup. Source: useMessages.ts:956-1140`.

    - **`loadMessages`:** ported. The MERGE 3-clause filter `m.id.startsWith('temp-') && m.runId && !dbRunIds.has(m.runId)` (L-068-06 / D-063.1-12) survives byte-identical. The post-await guard `if (activeThreadIdRef.current !== threadId) return` (L-068-03 / D-060-02) survives byte-identical (L-068-03 is verified by Task 2c's runtime test). Writes route to `setMessagesForBucket(surfaceId ?? 'chat', threadId, ...)`. Header comment: `// Phase 068 (L-068-03 + L-068-06): post-await sole-writer guard + MERGE 3-clause filter. Source: useMessages.ts:[range]`.

    NO modifications to action signatures. NO new exports. NO modifications to `lib/api.ts`.
  </behavior>
  <action>
    Open `frontend/src/providers/StreamsProvider.tsx`.

    1. **Lift `reconcile`** — replace the `// LIFT-IN-PLAN-2` body inside the existing `try` block with the real body from `useMessages.ts:956-1140`. Keep the Plan 1 top-guard wrapper untouched. Translate every `messagesByThreadRef.current.get(threadId)` read into `useStreamsStore.getState().bucketsBySurface.get(surfaceId ?? 'chat')?.get(threadId) ?? []` (RESEARCH §Pattern 5; §Finding #11). The for-loop dedup `m.runId === run.run_id` ports byte-identical.

    2. **Lift `loadMessages`** — replace the `// LIFT-IN-PLAN-2` stub with the real body. Preserve the MERGE 3-clause filter byte-identical: `m.id.startsWith('temp-') && m.runId && !dbRunIds.has(m.runId)`. Preserve the post-await guard byte-identical: `if (activeThreadIdRef.current !== threadId) return`. Writes through `setMessagesForBucket(surfaceId ?? 'chat', threadId, ...)`.

    After lifting, run the targeted tests until L-068-02, L-068-05, L-068-06 turn GREEN:

    ```
    cd frontend && npm test -- src/__tests__/providers/streamsProvider.test.tsx --run -t "concurrent reconcile"
    cd frontend && npm test -- src/__tests__/providers/streamsProvider.test.tsx --run -t "runId-match dedup"
    cd frontend && npm test -- src/__tests__/providers/streamsProvider.test.tsx --run -t "MERGE temp placeholders"
    ```

    Commit: `feat(068-02-2b): lift reconcile + loadMessages into StreamsProvider — L-068-02 + L-068-05 + L-068-06 GREEN`.
  </action>
  <verify>
    <automated>cd frontend && npm test -- src/__tests__/providers/streamsProvider.test.tsx --run -t "concurrent reconcile|runId-match dedup|MERGE temp placeholders"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "// Phase 068 \(L-068-(02|05|06)" frontend/src/providers/StreamsProvider.tsx` returns ≥ 1 (lifted blocks tagged)
    - `grep -cE "m\.id\.startsWith\(['\"]temp-['\"]\) && m\.runId && !dbRunIds\.has\(m\.runId\)" frontend/src/providers/StreamsProvider.tsx` returns 1 (L-068-06 MERGE 3-clause filter byte-identical)
    - `grep -cE "m\.runId === run\.run_id" frontend/src/providers/StreamsProvider.tsx` returns 1 (L-068-05 runId-match dedup byte-identical)
    - `grep -c "reconcileInFlightRef" frontend/src/providers/StreamsProvider.tsx` returns ≥ 3 (declare + set true + set false in finally — unchanged from Plan 1)
    - `grep -c "// LIFT-IN-PLAN-2" frontend/src/providers/StreamsProvider.tsx` returns ≤ 2 (only stopStream + resumeFromFailed markers may remain — Task 2c clears them)
    - `cd frontend && npm test -- src/__tests__/providers/streamsProvider.test.tsx --run -t "concurrent reconcile"` exits 0 (L-068-02 / SC#4 GREEN)
    - `cd frontend && npm test -- src/__tests__/providers/streamsProvider.test.tsx --run -t "runId-match dedup"` exits 0 (L-068-05 GREEN)
    - `cd frontend && npm test -- src/__tests__/providers/streamsProvider.test.tsx --run -t "MERGE temp placeholders"` exits 0 (L-068-06 GREEN)
    - Previously-GREEN tests from Task 2a (R-1 protection, cleanup on onTerminal) STILL exit 0
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports 0 errors
  </acceptance_criteria>
  <done>reconcile + loadMessages action bodies lifted; L-068-02 (in-flight lock), L-068-05 (runId-match dedup), L-068-06 (MERGE 3-clause filter) tests GREEN. L-068-04/07 from Task 2a stay GREEN. L-068-01 and L-068-03 still pending Task 2c.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2c: Lift `stopStream` + `resumeFromFailed` actions, EXTEND `setViewingThread` with internal reconcile-fire on non-null threadId (producer side of Plan 3 pre-flight gate — resolves iter-2 MED #4), wire `subscriptionsByRunId` mirror at every remaining subscribe/unsubscribe site — turn L-068-01 (Branch D-3 per-surface) and L-068-03 (sole writer runtime) GREEN</name>
  <files>frontend/src/providers/StreamsProvider.tsx</files>
  <read_first>
    - `frontend/src/hooks/useMessages.ts` line 477 onwards (`stopStreaming` body) + the `resumeFromFailed` declaration through its closing brace
    - `frontend/src/providers/StreamsProvider.tsx` (post-Task-2b — confirm Branch D-3 predicate from Plan 1 still present in `clearThreadBucket`; verify `// LIFT-IN-PLAN-2` markers for stopStream/resumeFromFailed are still there; locate the current `setViewingThread` action body — Plan 1 Task 3 placed it at approximately the same setState block where the other actions live; this task EXTENDS that body, it does NOT relocate the `activeThreadIdRef.current =` assignment which remains the sole writer per L-068-03)
    - `.planning/phases/068-streamsprovider-context-lift/068-RESEARCH.md` §Pattern 5 + §Finding #11 (getState() replacement for refs) + §Finding #8 point 2 (mount-time-reconcile-fire responsibility moves from `ChatArea.tsx:165` `useEffect([thread?.id])` into `setViewingThread` post-lift — this is the producer commitment Plan 3 Task 2 pre-flight-gates against)
    - `.planning/phases/068-streamsprovider-context-lift/068-CONTEXT.md` L-068-01 + L-068-03 + D-068-08 (listener gate — extended to programmatic path: `setViewingThread(null)` is a no-op for reconcile)
    - `.planning/phases/068-streamsprovider-context-lift/068-03-PLAN.md` lines 187-225 (Plan 3 Task 2's pre-flight gate — confirm the producer text it expects: `grep -n -A 8 "setViewingThread:" frontend/src/providers/StreamsProvider.tsx` must show `actions.reconcile(threadId)` invocation when threadId is non-null)
  </read_first>
  <behavior>
    After this sub-task lands, the remaining tests in `streamsProvider.test.tsx` turn GREEN PLUS one new test asserts the producer-side `setViewingThread` reconcile-fire contract:

    - **L-068-01 / SC#2 Branch D-3 (per-surface)** — `clearThreadBucket` guard preserved verbatim; the guard is reached now that `sendMessage` (Task 2a) wires `streamingThreadIdRef.current` correctly AND `stopStream` (this task) clears it correctly on stream termination.
    - **L-068-03 sole writer (runtime)** — `setViewingThread` is the only writer of `activeThreadIdRef.current`; post-await guards in `loadMessages` (Task 2b) catch stale writes. The runtime mid-await navigation test in Task 1 turns GREEN once `loadMessages` is fully wired (Task 2b) AND `stopStream` properly nulls `streamingThreadIdRef` on terminal (this task).
    - **NEW: `setViewingThread`-reconcile-fire contract (producer side of Plan 3 pre-flight gate)** — calling `setViewingThread('thread-A')` invokes `reconcile('thread-A', surfaceId)` exactly once; calling `setViewingThread(null)` is a no-op for reconcile (D-068-08 listener-gate semantics extended to the programmatic path). The post-await guard pattern inside `reconcile` itself (L-068-03 / D-060-01) still applies — if the user switches threads again before the reconcile-fetched run-state lands, the stale write is discarded; this is correct behavior, not a regression.

    Lift mechanics:

    - **`stopStream`:** ported from `useMessages.ts:477-…`. The Pitfall-3 logic (derive run_id from streaming bucket, fall back to viewing bucket) reads via `useStreamsStore.getState().bucketsBySurface.get('chat').get(streamingThreadIdRef.current ?? activeThreadIdRef.current)` per RESEARCH §Pattern 5. The stopStream path ALSO triggers the `subscriptionsByRunId` remove side of the mirror — when the controller aborts and the SSE promise's safety-net finally fires, decrement the mirror Set. Header comment: `// Phase 068 (L-068-07 safety-net side): stopStream tears down subscription; mirror remove. Source: useMessages.ts:477-…`.

    - **`resumeFromFailed`:** straightforward port. May internally call `sendMessage` (already lifted). Header comment: `// Phase 068 (L-068-07): resume retries via sendMessage; mirror semantics inherited. Source: useMessages.ts:[range]`.

    - **`setViewingThread` EXTENSION (iter-3 MED #4 producer fix):** Plan 1 Task 3 landed `setViewingThread` as the sole writer of `activeThreadIdRef.current` (per L-068-03). This task EXTENDS the action body to also fire reconcile internally when threadId is non-null — taking over the mount-time-reconcile-fire responsibility from the to-be-deleted `ChatArea.tsx:165` `useEffect([thread?.id])` block (RESEARCH §Finding #8 point 2). The fire is fire-and-forget (`.catch(console.error)`) — the action remains synchronous from the caller's perspective so existing call sites (`ChatArea`'s destructured `setViewingThread(threadId)` and the thin-reader rewrite in Task 3) keep working unchanged. The `surfaceId` second parameter defaults to `'chat'` so the public single-arg call signature (`setViewingThread(threadId)`) is preserved. The post-await guard pattern in `reconcile` itself (L-068-03 / D-060-01) still applies — if a user switches threads again before reconcile completes, the fetched run-state is discarded; this is correct.

      The literal post-extension body:

      ```typescript
      setViewingThread: (threadId: string | null, surfaceId: SurfaceId = 'chat') => {
        activeThreadIdRef.current = threadId;  // L-068-03 sole writer — unchanged
        useStreamsStore.setState({ viewedThreadId: threadId });
        if (threadId !== null) {
          useStreamsStore.getState().actions.reconcile(threadId, surfaceId).catch((err) => {
            console.error('[StreamsProvider] reconcile from setViewingThread failed', err);
          });
        }
        // setViewingThread(null) is a no-op for reconcile — D-068-08 listener gate semantics extended to programmatic path.
      }
      ```

      Header comment on the extension: `// Phase 068 (L-068-03 + Finding #8 point 2): mount-time-reconcile-fire responsibility lives here post-lift; ChatArea.tsx:165 useEffect([thread?.id]) deleted by Plan 3 Task 2. Sole writer of activeThreadIdRef.current preserved.`

    - **`subscriptionsByRunId` mirror — final audit pass:** grep `subscriptionsRef.current.set` and `subscriptionsRef.current.delete` across StreamsProvider.tsx. Each `.set(runId, ...)` MUST have a matching `setState((s) => ({ subscriptionsByRunId: new Set(s.subscriptionsByRunId).add(runId) }))` adjacent. Each `.delete(runId)` MUST have a matching `setState((s) => { const next = new Set(s.subscriptionsByRunId); next.delete(runId); return { subscriptionsByRunId: next } })` adjacent. This is the wiring step — Tasks 2a/2b started it; this task closes any remaining sites in stopStream/resumeFromFailed.

    NO modifications to action signatures consumed by ChatArea (the `surfaceId` second parameter is additive with a default — single-arg callers continue to work). NO new exports. NO modifications to `lib/api.ts`. The `activeThreadIdRef.current =` assignment line remains exactly one (sole writer L-068-03 preserved — the assignment count check stays at 1).
  </behavior>
  <action>
    Open `frontend/src/providers/StreamsProvider.tsx`.

    1. **Lift `stopStream`** — replace the `// LIFT-IN-PLAN-2` body. Read-replacements per RESEARCH §Pattern 5. Wire the matching `subscriptionsByRunId` Set remove call alongside `subscriptionsRef.current.delete(runId)`.

    2. **Lift `resumeFromFailed`** — replace the `// LIFT-IN-PLAN-2` body. Confirm any internal subscribe calls inherit the mirror semantics from sendMessage.

    3. **EXTEND `setViewingThread` (producer side of Plan 3 pre-flight gate — iter-3 MED #4 fix):** Locate the existing `setViewingThread` action body (placed by Plan 1 Task 3; structurally `setViewingThread: (threadId) => { activeThreadIdRef.current = threadId; useStreamsStore.setState({ viewedThreadId: threadId }) }`). Extend the signature to accept an optional `surfaceId: SurfaceId = 'chat'` second parameter (default preserves existing single-arg call sites). Append an `if (threadId !== null) { ... actions.reconcile(threadId, surfaceId).catch(...) }` block after the existing `setState` call. Do NOT add a second `activeThreadIdRef.current =` assignment — the existing one stays the sole writer (L-068-03). Add the header comment per the behavior section. The reconcile fire is fire-and-forget (returns void to the caller); rationale: the pre-lift `ChatArea.tsx:165` block fired reconcile on every `thread?.id` change synchronously without awaiting, so callers never observed the promise — preserve that exact contract.

    4. **Audit pass for the mirror:** run `grep -nE "subscriptionsRef\.current\.(set|delete)" frontend/src/providers/StreamsProvider.tsx`. For each hit, verify an adjacent `setState((s) => ({ subscriptionsByRunId: ... }))` call exists. Add any that are missing.

    5. **Extend Task 1's test file with two new assertions** for the `setViewingThread`-reconcile-fire contract. In the existing `describe("Phase 068 — L-068-03 sole writer ...")` block (or a new sibling describe `describe("Phase 068 — setViewingThread reconcile-fire contract (producer side of Plan 3 gate)")`), add:

       ```typescript
       it('setViewingThread on a non-null thread fires reconcile', async () => {
         mockGetActiveRuns.mockResolvedValueOnce({ runs: [] });
         const { result } = renderProvider();
         act(() => {
           useStreamsStore.getState().actions.setViewingThread('thread-A');
         });
         await waitFor(() => expect(mockGetActiveRuns).toHaveBeenCalledWith('thread-A', expect.any(Object)));
       });

       it('setViewingThread(null) does NOT fire reconcile (D-068-08 gate extended to programmatic path)', async () => {
         const { result } = renderProvider();
         act(() => {
           useStreamsStore.getState().actions.setViewingThread(null);
         });
         // Flush any microtasks scheduled by the action (defensive — there should be none for null).
         await Promise.resolve();
         expect(mockGetActiveRuns).not.toHaveBeenCalled();
       });
       ```

       These two assertions are the producer-side contract that Plan 3 Task 2's `<read_first>` pre-flight gate consumes (it greps `setViewingThread:[\s\S]{0,200}actions\.reconcile` against the post-Task-2c StreamsProvider.tsx). If either assertion is RED after this task, Plan 3 is structurally blocked.

    After lifting, run the FULL targeted suite plus the previously-GREEN tests to confirm no regressions:

    ```
    cd frontend && npm test -- src/__tests__/providers/streamsProvider.test.tsx --run
    cd frontend && npm test -- src/__tests__/hooks/useMessages.test.ts --run
    ```

    BOTH must exit 0. The useMessages.test.ts Branch D-3 test (third evidence layer) MUST still pass UNMODIFIED — Plan 2 Task 3 (the thin-reader rewrite) hasn't run yet, but the underlying state path that Plan 1 left in `useMessages.ts` is still backing it; if it broke, the lift corrupted shared state.

    Commit: `feat(068-02-2c): lift stopStream + resumeFromFailed; extend setViewingThread with internal reconcile-fire (Plan 3 pre-flight gate producer side); wire subscriptionsByRunId mirror — L-068-01 + L-068-03 GREEN`.
  </action>
  <verify>
    <automated>cd frontend && npm test -- src/__tests__/providers/streamsProvider.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "// LIFT-IN-PLAN-2" frontend/src/providers/StreamsProvider.tsx` returns **0** (all markers replaced)
    - `grep -cE "// Phase 068 \(L-068-0[1-7]" frontend/src/providers/StreamsProvider.tsx` returns ≥ 7 (every locked invariant has at least one annotated lift block)
    - `grep -cE "activeThreadIdRef\.current\s*=" frontend/src/providers/StreamsProvider.tsx` returns 1 (L-068-03 sole-writer preserved — STILL exactly ONE assignment after all lifts; the setViewingThread extension does NOT add a second assignment)
    - **NEW (iter-3 producer gate for Plan 3 MED #4):** `awk '/setViewingThread:/{flag=1; lines=0} flag{print; lines++; if(lines>=12) flag=0}' frontend/src/providers/StreamsProvider.tsx | grep -cE "actions\.reconcile\(" returns ≥ 1` — the lifted `setViewingThread` body contains an `actions.reconcile(...)` invocation within ~12 lines of its declaration. Portable equivalent (rg multiline): `rg -U -c "setViewingThread:[\\s\\S]{0,400}actions\\.reconcile\\(" frontend/src/providers/StreamsProvider.tsx` returns ≥ 1. Either form suffices — pick the one available on the executor's shell. **If both fail to find a match, this task is INCOMPLETE — Plan 3 cannot proceed.**
    - **NEW (no-op gate for null threadId):** `grep -cE "threadId !== null" frontend/src/providers/StreamsProvider.tsx` returns ≥ 1, AND the matching block is structurally adjacent to the `actions.reconcile` call inside `setViewingThread` (D-068-08 listener-gate semantics extended to programmatic path). Verifiable by inspecting `awk '/setViewingThread:/{flag=1} flag{print} /^\s*},?\s*$/{if(flag){flag=0}}' frontend/src/providers/StreamsProvider.tsx`.
    - `grep -cE "subscriptionsRef\.current\.(set|delete)" frontend/src/providers/StreamsProvider.tsx` returns ≥ 2 (open + close sites)
    - For every `subscriptionsRef.current.set(runId, ...)` and `.delete(runId)` line, there is an adjacent `setState((s) => (... subscriptionsByRunId ...))` line. Verify manually via grep with context: `grep -n -B 1 -A 3 "subscriptionsRef.current.set\|subscriptionsRef.current.delete" frontend/src/providers/StreamsProvider.tsx`
    - `cd frontend && npm test -- src/__tests__/providers/streamsProvider.test.tsx --run` exits 0 (ALL 7 binding invariant tests + SC#2/4 + the two new `setViewingThread`-reconcile-fire assertions GREEN — full file passes)
    - `cd frontend && npm test -- src/__tests__/providers/streamsProvider.test.tsx --run -t "Branch D-3"` exits 0 (L-068-01 / SC#2 GREEN for both `chat` AND `mock-eval`)
    - `cd frontend && npm test -- src/__tests__/providers/streamsProvider.test.tsx --run -t "sole writer"` exits 0 (L-068-03 runtime test GREEN)
    - **NEW:** `cd frontend && npm test -- src/__tests__/providers/streamsProvider.test.tsx --run -t "setViewingThread on a non-null thread fires reconcile"` exits 0 (producer-side fire wired)
    - **NEW:** `cd frontend && npm test -- src/__tests__/providers/streamsProvider.test.tsx --run -t "setViewingThread\(null\) does NOT fire reconcile"` exits 0 (null no-op gate honored)
    - `cd frontend && npm test -- src/__tests__/hooks/useMessages.test.ts --run` STILL exits 0 (existing tests unmodified)
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports 0 errors
  </acceptance_criteria>
  <must_haves>
    <truths>
      - "`setViewingThread` is the sole programmatic owner of post-switch reconcile firing — the pre-lift `ChatArea.tsx:165` `useEffect([thread?.id])` semantics are preserved verbatim via this new fire path. Producer-side contract for Plan 3 Task 2's pre-flight gate (iter-3 MED #4 resolution)."
      - "`setViewingThread(null)` is a no-op for reconcile — D-068-08 listener-gate semantics extended to the programmatic path."
      - "The `activeThreadIdRef.current =` assignment inside `setViewingThread` remains the ONLY such assignment in StreamsProvider.tsx (L-068-03 sole-writer preserved — extension does not add a second writer)."
      - "The reconcile fire is fire-and-forget (`.catch(console.error)`) — `setViewingThread` returns void to the caller; matches the pre-lift `ChatArea.tsx:165` synchronous-call-no-await contract; existing single-arg callers (`ChatArea`, the thin-reader in Task 3) keep working unchanged."
    </truths>
  </must_haves>
  <done>All 7 locked invariants survive verbatim in StreamsProvider.tsx; full new test file GREEN (L-068-01 + L-068-03 closed by this task) PLUS the two new `setViewingThread`-reconcile-fire assertions GREEN (producer-side of Plan 3 pre-flight gate landed — iter-3 MED #4 resolved); existing useMessages.test.ts still GREEN. subscriptionsByRunId mirror wired at every subscribe/unsubscribe site across the provider.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewrite `frontend/src/hooks/useMessages.ts` as a thin reader (< 100 LOC) delegating to named hooks; public UseMessages interface UNCHANGED; audit isStreaming consumers BEFORE deciding hard-code-false vs hoist-to-store</name>
  <files>frontend/src/hooks/useMessages.ts, frontend/src/providers/StreamsProvider.tsx, frontend/src/stores/streamsStore.ts</files>
  <read_first>
    - `.planning/phases/068-streamsprovider-context-lift/068-RESEARCH.md` §Code Examples §"The thin `useMessages` reader (Plan 2 target)" — the literal target shape
    - `.planning/phases/068-streamsprovider-context-lift/068-PATTERNS.md` §`frontend/src/hooks/useMessages.ts` (rewrite to thin reader — NEW pattern; ChatArea.tsx:27-38 must keep working)
    - `.planning/phases/068-streamsprovider-context-lift/068-RESEARCH.md` §Recommendation #4 (LOC target < 100 — plan-checker assertion)
    - `frontend/src/hooks/useMessages.ts` lines 1-29 (the public `UseMessages` interface — MUST be preserved byte-identical)
    - `frontend/src/components/chat/ChatArea.tsx` lines 27-38 (the destructure — must work without ChatArea edits)
    - **ISSTREAMING AUDIT (MANDATORY before deciding hard-code-false vs hoist):** `grep -rn "isStreaming" frontend/src/components/chat/ChatArea.tsx` AND `grep -rln "isStreaming" frontend/src/components` — record EVERY consumer site and classify each as: (a) destructured-and-discarded (safe to hard-code false), or (b) actively read for UI logic / spinner / disabled-state / button-state (REQUIRES hoist).
  </read_first>
  <behavior>
    The post-Task-3 `useMessages.ts` body is approximately the literal RESEARCH §Code Examples §"thin useMessages reader" shape:

    ```typescript
    import { useMemo } from "react"
    import type { Message } from "../types"
    import {
      useThreadMessages,
      useStreamActions,
      useViewingThread,
    } from "@/providers/StreamsProvider"

    interface UseMessages {
      // ... (preserved byte-identical from current lines 6-29)
    }

    export function useMessages(): UseMessages {
      const viewedThreadId = useViewingThread()
      const messages = useThreadMessages(viewedThreadId, "chat")
      const actions = useStreamActions()
      const isStreaming = useIsStreaming()  // OR: hard-coded `false` — depends on audit outcome
      return useMemo<UseMessages>(
        () => ({
          messages,
          isStreaming,
          fallbackNotice: null,
          loadMessages: (threadId) => actions.loadMessages(threadId, "chat"),
          sendMessage: (threadId, content, model, onTitleUpdate, agentMode, provider) =>
            actions.sendMessage(threadId, content, { model, provider, agentMode, onTitleUpdate, surfaceId: "chat" }),
          stopStreaming: actions.stopStream,
          abortStream: () => {},     // legacy no-op (D-063.1-07 — call site already removed from ChatArea)
          clearMessages: () => actions.clearThreadBucket("chat"),
          setViewingThread: actions.setViewingThread,
          reconcile: (threadId) => actions.reconcile(threadId, "chat"),
          resumeFromFailed: actions.resumeFromFailed,
        }),
        [messages, actions, isStreaming],
      )
    }
    ```

    Critical preservation rules:

    1. **Public `UseMessages` interface (current lines 6-29)** stays byte-identical. ChatArea.tsx:27-38 imports must NOT need editing.
    2. **`sendMessage` signature adaptation:** the current public signature is `(threadId, content, model?, onTitleUpdate?, agentMode?, provider?)`. The store-action signature is `(threadId, content, opts?)`. The thin reader's `sendMessage` repacks the positional args into the opts object + binds `surfaceId: "chat"`.
    3. **`isStreaming` — MANDATORY AUDIT-DRIVEN BRANCH (MED #6 fix).** Phase 068's goal mandates "existing chat behavior is byte-identical". Before deciding how to populate `isStreaming` in the thin reader, run the audit step (in `<read_first>`). Then:
        - **Branch A — Hoist into Zustand state (REQUIRED if audit shows any live consumer):** Add `useIsStreaming(): boolean` to `StreamsProvider.tsx` (named hook reading `state.isStreaming` — already declared in `streamsStore.ts` Plan 01 Task 2 as a state slot, currently always-false). Wire `sendMessage` (Task 2a-lifted) to `useStreamsStore.setState({ isStreaming: true })` on subscription open and to `setState({ isStreaming: false })` on terminal/abort/error. The thin reader imports `useIsStreaming` and the return object's `isStreaming` is the hook's output (not a literal `false`).
        - **Branch B — Hard-code `false` (PERMITTED only if audit explicitly confirms no live consumer):** the audit must show that every `isStreaming` reference in chat surface code is destructured-and-discarded (e.g., `const { isStreaming, ...rest } = useMessages()` with `isStreaming` never read again). In this case the thin reader returns `isStreaming: false`, and the SUMMARY.md must document the audit grep output as evidence.
        - The choice is recorded in 068-02-SUMMARY.md with the grep output and the branch taken. Do NOT silently default to `false` without performing the audit — that violates the phase invariant.
    4. **`abortStream` is a no-op** — D-063.1-07 confirmed the only call site in ChatArea is deleted; abortStream stays exported as a legacy stub for diff-minimization.
    5. **LOC target: < 100 lines** including imports + interface + body + comments. Per RESEARCH §Recommendation #4 — this is a plan-checker assertion.
    6. **Delete EVERYTHING ELSE** from current useMessages.ts (lines ~31-1229) — `makeStreamCallbacks` moved to StreamsProvider.tsx in Task 2a; all refs moved; all action bodies moved (Tasks 2a/2b/2c). The thin reader does NOT re-declare `messagesByThread`, `subscriptionsRef`, etc.

    Verify ChatArea.tsx still compiles and runs by:
    - Running `npm run build` (TypeScript compilation gate)
    - Running `npm test -- src/__tests__/hooks/useMessages.test.ts --run` (existing Branch D-3 test must pass UNMODIFIED against the new thin reader — third evidence layer per RESEARCH §Validation Architecture point 3)
  </behavior>
  <action>
    1. **Audit isStreaming first.** Run from repo root:
       ```
       grep -n "isStreaming" frontend/src/components/chat/ChatArea.tsx
       grep -rln "isStreaming" frontend/src/components/
       ```
       Capture the output. For each hit, classify: destructured-only (safe to hard-code) vs actively read (REQUIRES hoist via Branch A).

    2. **If Branch A required:** open `frontend/src/providers/StreamsProvider.tsx`. Add a new named hook:
       ```typescript
       export const useIsStreaming = (): boolean =>
         useStreamsStore((state) => state.isStreaming)
       ```
       Inside the lifted `sendMessage` body (from Task 2a), wire `useStreamsStore.setState({ isStreaming: true })` on the subscription-open path (right after `subscriptionsRef.current.set(runId, controller)`) and `useStreamsStore.setState({ isStreaming: false })` on every terminal path (onTerminal, abort, error). Also wire `false` in `stopStream` (Task 2c) and the safety-net finally. `streamsStore.ts` already declares `isStreaming: boolean` (Plan 01 Task 2) — no store edits needed if the field is present; otherwise add it.

    3. **Open `frontend/src/hooks/useMessages.ts`.** Read lines 1-29 (the interface) — copy them aside. Read line 1 (imports) — note which imports come from `../types` vs `../lib/api`.

    4. Replace the ENTIRE file body with the thin-reader shape from the behavior section above. Preserve:
       - The file's `interface UseMessages` block lines 6-29 (byte-identical from current)
       - The `Message` import path from `../types` (use the existing relative form, not `@/types`, for minimal-diff)
       - Use Branch A or Branch B for `isStreaming` per audit outcome.

    5. After the rewrite, run `wc -l frontend/src/hooks/useMessages.ts` and confirm < 100.

    6. Run the test suite. Run each command separately (no `| tail` — preserves exit codes on Windows PowerShell):
       ```
       cd frontend && npm test
       cd frontend && npm run build
       ```

    Both `streamsProvider.test.tsx` and `useMessages.test.ts` (existing — Branch D-3 test at lines 536-604) MUST exit 0. The third evidence layer for "preserved verbatim" is: the unmodified existing test passing.

    Commit message: `refactor(068-02): useMessages.ts is a thin reader (1229→<100 LOC); ChatArea import surface unchanged; existing Branch D-3 test passes unmodified; isStreaming audit + branch documented in SUMMARY`.

    Optional rename `useMessages → useChatMessages` is per Claude's Discretion (CONTEXT.md line 68 + Open Question #2). If the rename diff falls out cleanly (< 50 lines total — one ChatArea.tsx import + the export declaration), do it; otherwise keep the name. Default: keep `useMessages`.
  </action>
  <verify>
    <automated>cd frontend && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l frontend/src/hooks/useMessages.ts` returns ≤ 100
    - `grep -c "export function useMessages(): UseMessages" frontend/src/hooks/useMessages.ts` returns 1 (public signature unchanged)
    - `grep -c "interface UseMessages" frontend/src/hooks/useMessages.ts` returns 1
    - All 11 properties of the legacy UseMessages interface are present in the thin reader's return object:
      `grep -cE "(messages|isStreaming|fallbackNotice|loadMessages|sendMessage|stopStreaming|abortStream|clearMessages|setViewingThread|reconcile|resumeFromFailed):" frontend/src/hooks/useMessages.ts` returns ≥ 11
    - `grep -c "from \"@/providers/StreamsProvider\"" frontend/src/hooks/useMessages.ts` returns 1
    - `grep -c "useThreadMessages\|useStreamActions\|useViewingThread" frontend/src/hooks/useMessages.ts` returns ≥ 3 (all 3 named hooks consumed; +1 if useIsStreaming branch taken)
    - **isStreaming audit evidence captured:** 068-02-SUMMARY.md (created by execute-plan workflow) records the grep output AND the branch taken (A: hoisted via `useIsStreaming` named hook; B: hard-coded `false` because audit found no live consumer)
    - If Branch A: `grep -c "export const useIsStreaming" frontend/src/providers/StreamsProvider.tsx` returns 1 AND `grep -c "useIsStreaming" frontend/src/hooks/useMessages.ts` returns ≥ 1
    - If Branch B: `grep -nE "isStreaming" frontend/src/components/chat/ChatArea.tsx` shows only destructure sites (no usage in JSX/JSX-prop logic)
    - **CRITICAL — third evidence layer:** `cd frontend && npm test -- src/__tests__/hooks/useMessages.test.ts --run` exits 0; the existing Branch D-3 test at `useMessages.test.ts:536-604` passes UNMODIFIED. If this fails, the lift broke the abstraction — DO NOT modify the test file; fix the thin reader or the provider lift.
    - `cd frontend && npm test` (full suite) exits 0 — streamsProvider.test.tsx + useMessages.test.ts + useFolders.test.ts + useDocuments.test.ts all GREEN.
    - `cd frontend && npm run build` exits 0
    - `grep -c "messagesByThread\|messagesByThreadRef\|subscriptionsRef\|reconcileInFlightRef\|streamingThreadIdRef\|activeThreadIdRef" frontend/src/hooks/useMessages.ts` returns 0 (all refs deleted — they live in StreamsProvider.tsx now)
    - `grep -c "makeStreamCallbacks" frontend/src/hooks/useMessages.ts` returns 0 (factory lifted to StreamsProvider.tsx in Task 2a)
    - `grep -c "useMessages" frontend/src/components/chat/ChatArea.tsx` returns ≥ 1 (ChatArea import still works, untouched)
  </acceptance_criteria>
  <done>useMessages.ts shrunk from 1229 to < 100 LOC; public surface unchanged; ChatArea works unmodified; existing Branch D-3 test passes unmodified (third evidence layer GREEN); isStreaming audit performed and branch documented (A=hoist or B=hard-code with grep evidence).</done>
</task>

</tasks>

<threat_model>
**Frontend refactor — no new attack surfaces (continuation of Plan 1 posture).**

The store-action bodies lifted in Tasks 2a/2b/2c are pure reshapings of existing `useMessages.ts` logic. No new API call, no new env var, no new input surface. Authentication / authorization remains the existing Phase 062 RLS gate on `/runs/{rid}/stream` (unchanged). The `subscriptionsByRunId: Set<string>` state mirror is in-tab UI state, never serialized off the browser. The `setViewingThread` extension to fire reconcile internally invokes only the already-existing `reconcile` action (which calls the already-existing Phase 062 RLS-gated `/threads/{id}/active-runs` endpoint) — no new endpoint, no new auth path.

| Trust boundary | Status |
|---|---|
| Client → API (Bearer + RLS) | Unchanged |
| Cross-tab state | Unchanged — Phase 062 replay-and-tail substrate untouched |
| Cross-surface state isolation | NEW invariant tested at L-068-04 + SC#3 (Plan 4) — `bucketsBySurface` per-surface Map isolation is structural, not auth-related |

**ASVS L1 categories: N/A** — no new attack surface; this plan only reshapes existing client-side state mechanics. Threat register: 0.
</threat_model>

<verification>
After all 4 tasks (1, 2a, 2b, 2c, 3) land:

1. `cd frontend && npm test -- src/__tests__/providers/streamsProvider.test.tsx --run` exits 0 — all 7 binding tests GREEN (L-068-01..07 + SC#2/4) PLUS the two new `setViewingThread`-reconcile-fire assertions (producer-side of Plan 3 gate).
2. `cd frontend && npm test -- src/__tests__/hooks/useMessages.test.ts --run` exits 0 — existing Branch D-3 test passes UNMODIFIED (third evidence layer for "preserved verbatim").
3. `cd frontend && npm test` (full suite) exits 0.
4. `cd frontend && npm run build` exits 0.
5. `wc -l frontend/src/hooks/useMessages.ts` ≤ 100.
6. `grep -cE "activeThreadIdRef\.current\s*=" frontend/src/providers/StreamsProvider.tsx` returns 1 (sole-writer L-068-03 preserved — extension does not add a second writer).
7. `awk '/setViewingThread:/{flag=1; lines=0} flag{print; lines++; if(lines>=12) flag=0}' frontend/src/providers/StreamsProvider.tsx | grep -cE "actions\.reconcile\("` returns ≥ 1 (producer-side fire for Plan 3 pre-flight gate landed — iter-3 MED #4 resolved).
8. Manual sanity check: `npm run dev` → http://localhost:5173/ → log in → existing chat sends/receives/scrolls normally; no visible behavioral change.

These 8 checks together prove the "preserved verbatim" property per RESEARCH §Validation Architecture three-layer evidence stack PLUS the Plan 3 entry condition.
</verification>

<success_criteria>
- **L-068-01 / SC#2 binding gate GREEN:** Branch D-3 guard preserved verbatim; per-surface test passes for both `chat` and `mock-eval`.
- **L-068-02..07 invariants GREEN** in `streamsProvider.test.tsx`.
- **Producer-side of Plan 3 pre-flight gate landed (iter-3 MED #4 resolution):** `setViewingThread` extended to fire `reconcile` internally on non-null `threadId`; `setViewingThread(null)` is a no-op for reconcile (D-068-08 listener-gate semantics extended to programmatic path). Two new Vitest assertions GREEN. `awk`-or-`rg` grep against the lifted `setViewingThread` body finds the `actions.reconcile(...)` invocation within ~12 lines of the declaration. The `activeThreadIdRef.current =` assignment count remains exactly 1 (sole-writer L-068-03 unbroken).
- **Third evidence layer GREEN:** existing `useMessages.test.ts` Branch D-3 test at lines 536-604 passes UNMODIFIED against the new thin reader.
- **`useMessages.ts < 100 LOC`** with public interface byte-identical to pre-lift.
- **`ChatArea.tsx` is UNCHANGED** in this plan — the import + destructure at lines 27-38 keeps working; the `setViewingThread(threadId)` single-arg call site still works because the `surfaceId` second parameter defaults to `'chat'`.
- **isStreaming audit performed** — branch (A=hoist or B=hard-code) documented in SUMMARY.md with grep evidence.
- Full `npm test` and `npm run build` both exit 0 (run separately, no `| tail`).
</success_criteria>

<output>
After completion, create `.planning/phases/068-streamsprovider-context-lift/068-02-SUMMARY.md` documenting:
- Final `wc -l` of `useMessages.ts` (pre: 1229 / post: actual)
- Final `wc -l` of `StreamsProvider.tsx` (pre: Plan 1 size / post: actual after lift)
- Final `wc -l` of new test file
- Per-sub-task GREEN evidence: which test names turned GREEN at 2a (R-1, cleanup), 2b (concurrent reconcile, runId-match dedup, MERGE filter), 2c (Branch D-3 per-surface, sole writer runtime, setViewingThread fires reconcile, setViewingThread(null) no-op)
- Each invariant L-068-01..07 with the line range in StreamsProvider.tsx where its body lives + the test name that proves it
- **`setViewingThread` reconcile-fire extension (iter-3 MED #4 producer fix):** line range in StreamsProvider.tsx where the extended action body lives; the two new Vitest assertion names + GREEN evidence; the `awk`/`rg` grep output that confirms `actions.reconcile(...)` is structurally within the lifted action body; confirmation that the `activeThreadIdRef.current =` assignment count stayed at 1 (sole-writer L-068-03 unbroken). Plan 3 Task 2's pre-flight gate is now structurally satisfied.
- The "preserved verbatim" three-layer evidence outcome (predicate-grep + per-surface enumeration + unmodified-existing-test all GREEN)
- Optional rename decision (kept `useMessages` vs renamed to `useChatMessages`)
- **isStreaming audit result + branch:** grep output captured; branch taken (A=hoisted to `useIsStreaming` named hook OR B=hard-coded `false` because no live consumer); justification with grep evidence
- Plan 3 entry conditions confirmed (the listener block at ChatArea.tsx:162-187 is still present and now double-attaches with the provider's listeners — the L-068-02 in-flight lock makes this safe pending Plan 3 deletion; the `setViewingThread`-reconcile-fire path is wired so Plan 3 Task 2's pre-flight gate will pass)
</output>
</content>
