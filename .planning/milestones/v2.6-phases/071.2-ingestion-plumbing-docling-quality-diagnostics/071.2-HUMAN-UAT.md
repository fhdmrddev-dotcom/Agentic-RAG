---
status: partial
phase: 071.2-ingestion-plumbing-docling-quality-diagnostics
source: [071.2-02-PLAN.md, 071.2-03-PLAN.md, 071.2-04-PLAN.md, 071.2-05-PLAN.md, 071.2-05-SUMMARY.md]
started: 2026-05-15
updated: 2026-05-15
---

## Current Test

[awaiting human testing — gated on either a workable test fixture or production-env retry]

## Context

Phase 071.2 closed code-complete on 2026-05-15. All 5 plans landed; all 35 unit + integration tests pass; the per-aspect extraction dispatcher (D-071.2-01..04) is wired through `/upload` and `/reextract` with `?engines=` per-call hint plumbing; migration 045 applied; `supabase/full-schema.sql` regenerated.

Live UAT against the thesis PDF (`Fahed Mrad Chapters 1 to 4.pdf`, id `1790bbfb-b598-444a-8904-7b8ad9c432a9`) was attempted on 2026-05-15 but **both extraction paths crashed in the local WSL env**:

- Docling primary path: timed out at 136s (Layer-2 wall-clock fail-safe fired)
- PyMuPDF subprocess fallback: child process exited 1 with `MemoryError`

This is an environmental constraint, not a code defect. The same dispatcher / aspect engines / per-call hint plumbing are exercised by the test suite — see SUMMARY.md for the binding tests. The root-cause investigation is deferred to **Phase 071.3 (PyMuPDF subprocess OOM diagnostic + non-Docling engine evaluation)** — see [[seed-019-pymupdf-subprocess-oom-non-docling-eval]] for the re-open trigger.

## Tests

### 1. Plan 02 Task 2 — Realtime status badge UAT
expected: After uploading a small PDF via the dev app (http://localhost:5173/), the documents-list badge transitions `pending → processing (with the correct ingestion_step label: extracting → extracting_tables → extracting_images → chunking → embedding → metadata) → completed` with NO manual page refresh. SC#1 latency: /upload Status 201 in <1500ms.
result: [pending]
notes: Requires the dev backend + frontend running AND a fixture that doesn't OOM the local env. Drive via Chrome DevTools MCP or manually.

### 2. Plan 03 Task 3 — Docling probe harness (D-071.2-13 GREEN gate)
expected: Run `cd backend && venv/Scripts/python.exe scripts/probe_docling_timeout.py <thesis-pdf-id>` against a fixture Docling can process. Probe verifies: (a) markdown sanity (≥1 `\n## ` header), (b) tables/images/equations counts surface, (c) wall-clock under the configured timeout. Record results in `071.2-PROBE-RESULTS.md` (placeholder already created).
result: [pending]
notes: Currently blocked by the same Docling timeout on the thesis PDF that broke /reextract. Will become testable when (a) Phase 071.3 makes Docling reliable OR (b) a smaller arxiv-style PDF is uploaded.

### 3. Plan 04 Task 3 — telemetry vs storage parity
expected: On a successful Docling-path extract, `pdf_extraction_runs.tables_count` equals `(SELECT count(*) FROM document_tables WHERE document_id=X)`. Same for `images_count` vs `document_images`. Pre-Plan-04, the storage layer re-extracted with pdfplumber and dropped ~95% of tables/figures. Post-Plan-04 (via `extracted_doc` kwarg precedence), counts should match.
result: [pending]
notes: Same blocker — requires a Docling-completable fixture.

### 4. Plan 05 Step D — D-071.2-12 quality floor
expected: `/reextract` with default engines (or `?engines=text:legacy,tables:docling_tf,images:pymupdf_full,equations:docling_formula`) on the thesis PDF yields `tables ≥ 20 AND figures ≥ 10 AND chunks ≥ 200` in the `document_tables` / `document_images` / `document_chunks` rows.
result: [pending — env-blocked]
notes: This is the headline phase outcome. Live verification blocked on the OOM described above. The deferred direction (per user feedback 2026-05-15) is to make non-Docling engines competitive on this floor; see [[seed-019-pymupdf-subprocess-oom-non-docling-eval]]. Baseline pre-Plan-05 on the thesis PDF: tables=4, figures=2, chunks=443.

### 5. Plan 05 Step D — RAG-MM-LIFT-02 DOCX closure
expected: On a DOCX whose only picture is a floating `<wp:anchor>` (not an inline shape), `/reextract` with default engines yields `document_images ≥ 1`. python-docx's `inline_shapes` collection misses anchor-only images; the new `zip_xpath_docx` engine ports Docling's `MsWordDocumentBackend` XPath verbatim.
result: [pending]
notes: No DOCX sibling exists in the local test user's library. Unit test `test_inline_shapes_zero_zip_xpath_nonzero_on_anchor_only` passes against a programmatic anchor-only fixture (closes the binding gate at the unit level). End-to-end verification requires either uploading a real anchor-only DOCX OR running against a production env that has one.

### 6. Plan 05 — per-call hint smoke tests
expected: `/reextract?engines=images:pdfplumber` produces fewer images than the default (`pymupdf_full`); `/reextract?engines=tables:pdfplumber` produces fewer tables than the default (`docling_tf`). Confirms per-call override actually overrides.
result: [pending]
notes: Trivially testable once any baseline run succeeds. Same env blocker.

### 7. Plan 05 — telemetry composer signature
expected: `SELECT extractor_name FROM pdf_extraction_runs ORDER BY created_at DESC LIMIT 1` returns `composable[<text>/<tables>/<images>/<equations>]` after a dispatcher-path run.
result: [pending]
notes: Easy verify; requires a completed run.

## Summary

total: 7
passed: 0
issues: 0
pending: 7
skipped: 0
blocked: 7 (all gated on a Docling-or-PyMuPDF-completable fixture; root-cause investigation deferred to Phase 071.3 per SEED-019)

## Gaps

### G-1: Local-env extractor stack unreliable on real PDFs

- **status:** open
- **severity:** high (blocks live UAT for entire phase)
- **observed:** 2026-05-15 — `/reextract` on thesis PDF (1790bbfb..., ~150 pages, single-digit MB) failed with `Docling timed out; PyMuPDF fallback also failed: pymupdf child exited 1: MemoryError`
- **root cause:** unknown — to be diagnosed in Phase 071.3
- **re_open_trigger:** Phase 071.3 closes SEED-019; or user uploads a non-thesis fixture that the dispatcher processes successfully
- **deferred_to:** SEED-019 (`.planning/seeds/SEED-019-pymupdf-subprocess-oom-non-docling-eval.md`)
