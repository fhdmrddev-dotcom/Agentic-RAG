---
phase: 071.2
slug: ingestion-plumbing-docling-quality-diagnostics
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-15
---

# Phase 071.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: `071.2-RESEARCH.md § Validation Architecture` (verified by direct source read).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x + pytest-asyncio (auto mode) |
| **Config file** | `backend/pytest.ini` (`asyncio_mode = auto`, `testpaths = tests`) |
| **Quick run command** | `cd backend && venv/Scripts/python.exe -m pytest tests/integration/test_documents.py tests/unit/test_071_1_threadpool_sweep.py -x -q` |
| **Full suite command** | `cd backend && venv/Scripts/python.exe -m pytest -q` |
| **Estimated runtime** | ~25s quick / ~3min full (excluding slow Docling-model-download integration tests) |

---

## Sampling Rate

- **After every task commit:** Run the **Quick run command** above
- **After every plan wave:** Run the **Full suite command**
- **Before `/gsd-verify-work`:** Full suite must be green AND manual UAT for SC#1 / SC#3 / SC#4 (live) + SQL UAT for SC#5 completed
- **Max feedback latency:** ~25 seconds (quick suite)

---

## Per-Task Verification Map

> Plans 01–04 will assign specific task IDs. This map links Success Criteria to the tests that must exist when each plan ships.

| SC ID | Plan | Behavior | Test Type | Automated Command | File Exists | Status |
|-------|------|----------|-----------|-------------------|-------------|--------|
| SC#1 | 01 | `/upload` returns 201 within ~1.5s on any size PDF | live UAT (timed curl) | `time curl -X POST -F "file=@thesis.pdf" -H "Authorization: Bearer $JWT" http://localhost:8000/documents/upload` — assert elapsed < 1.5s | ❌ W0 (doc the one-liner in `backend/README.md` or `backend/scripts/upload_latency_curl.sh`) | ⬜ pending |
| SC#1 | 01 | `/upload` schedules `ingest_document` via BackgroundTask, NOT inline | unit / integration | New `TestUploadDocument::test_upload_uses_background_task_for_extraction` in `backend/tests/integration/test_documents.py` (mock `get_extractor` with `time.sleep(2)` side_effect; assert TestClient measured response < 200ms before BackgroundTask drains) | ❌ W0 new test | ⬜ pending |
| SC#1 | 01 | `documents` row INSERTed with `status='pending'` BEFORE extract starts | integration | Same test as above — assert the INSERT happens before `mock_ingest.assert_called_once()` fires | ❌ W0 (covered by the test above) | ⬜ pending |
| SC#2 | 01 | `/reingest` no longer blocks async handler on `extractor.extract()` | unit (static grep) | Extend `backend/tests/unit/test_071_1_threadpool_sweep.py` — grep for `run_in_threadpool` wrapping `extractor.extract` in `/upload` + `/reingest` handlers | ❌ W0 extend existing test | ⬜ pending |
| SC#2 | 01 | `/reingest` storage download wrapped in `run_in_threadpool` | unit (static grep) | Same extended static grep test — also assert `supabase.storage.from_("documents").download()` is wrapped on the `/reingest` code path | ❌ W0 extend existing test | ⬜ pending |
| SC#2 | 01 | Backend `/health` stays <2s during in-flight extract on either route | manual UAT | `while true; do time curl -s http://localhost:8000/health > /dev/null; sleep 0.5; done` concurrent with thesis-PDF upload | None — manual UAT per 071.1 protocol | ⬜ pending |
| SC#3 | 02 | Frontend documents list shows new row + status transitions via Realtime | manual UAT (Chrome MCP authenticated flow at `localhost:5173` with `fhdmrd@gmail.com` / `123456`) | Upload a PDF; verify new row appears within 1s with `pending` badge; verify badge transitions through `processing (Extracting)` → `processing (Chunking)` → `processing (Embedding)` → `completed` without manual page refresh | None — manual UAT | ⬜ pending |
| SC#3 | 02 | New `ingestion_step` labels (if needed) cover `extracting_tables` / `extracting_images` | unit (snapshot or grep) | If Plan 02 adds new label cases, snapshot-test or grep-assert in `frontend/src/components/ingestion/DocumentStatusBadge.test.tsx` (NEW) | ⚠️ TBD pending Plan 02 scope decision | ⬜ pending |
| SC#4 | 03 | Thesis PDF re-ingested via `/reextract engine='docling'` produces chunk count within 20% of legacy baseline (~320+ vs ~400) | live UAT + SQL | Run `/reextract` on `23cc112a-92c2-440f-83c9-13aa8bf3d53d` with `engine='docling'`; then `SELECT COUNT(*) FROM document_chunks WHERE document_id = '23cc112a-92c2-440f-83c9-13aa8bf3d53d'` → assert count ≥ 320 | None — SQL-driven UAT | ⬜ pending |
| SC#4 | 03 | Diagnostic probe confirms hypothesis (`len(full_markdown) ≥ len(text)`) before shipping the swap | manual diagnostic | Extend `backend/scripts/probe_docling_timeout.py` to print `len(text)`, `len(full_markdown)`, and `chunk_text(...)` count on both — then `venv/Scripts/python.exe scripts/probe_docling_timeout.py 23cc112a-...` | ❌ W0 extend probe | ⬜ pending |
| SC#5 | 04 | `pdf_extraction_runs.table_count` equals `document_tables` row count for the same `(document_id, extractor)` pair | SQL UAT | `SELECT pr.table_count AS telemetry, (SELECT count(*) FROM document_tables WHERE document_id=pr.document_id) AS persisted FROM pdf_extraction_runs pr WHERE pr.document_id = '23cc112a-...' AND pr.engine = 'docling'` — assert `telemetry = persisted` | None — SQL-driven UAT | ⬜ pending |
| SC#5 | 04 | `extract_and_store_tables(extracted_doc=...)` reads from `extracted_doc.tables` when present | unit | New `backend/tests/unit/test_multimodal_extraction.py::test_extract_and_store_tables_uses_extracted_doc` — pass a mocked `ExtractedDocument` with 3 known `TableData` objects; assert `supabase.insert` called with rows matching the `TableData` inputs (not pdfplumber's output) | ❌ W0 new test | ⬜ pending |
| SC#5 | 04 | `extract_and_store_images(extracted_doc=...)` mirrors the same pattern | unit | New `backend/tests/unit/test_multimodal_extraction.py::test_extract_and_store_images_uses_extracted_doc` — parallel structure | ❌ W0 new test | ⬜ pending |
| SC#5 | 04 | Legacy path still works when `extracted_doc=None` | unit (regression) | Existing tests at `backend/tests/unit/test_multimodal_extraction.py` should stay green; adding `extracted_doc=None` default keeps them passing | ✓ existing tests cover the None path implicitly | ⬜ pending |
| SC#6 | 04 | `/reextract` returns 404 (not 500) when `is_latest=False` | integration | New `TestReextractDocument::test_reextract_is_latest_false_returns_404` — mock `.maybe_single().execute()` to raise; assert 404 returned | ❌ W0 new test | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `backend/tests/integration/test_documents.py` — add 3 new tests:
  - `TestUploadDocument::test_upload_uses_background_task_for_extraction` (covers SC#1 unit-level)
  - `TestReingestDocument::test_reingest_wraps_extract_in_threadpool` (covers SC#2 integration; NEW test class if absent)
  - `TestReextractDocument::test_reextract_is_latest_false_returns_404` (covers SC#6)
- [ ] `backend/tests/unit/test_071_1_threadpool_sweep.py` — extend grep-based static test to cover `/upload` and `/reingest` (currently only `/reextract`)
- [ ] `backend/tests/unit/test_multimodal_extraction.py` — add `test_extract_and_store_tables_uses_extracted_doc` + `test_extract_and_store_images_uses_extracted_doc` (parallel) covering SC#5 unit-level
- [ ] `backend/scripts/probe_docling_timeout.py` — extend `probe()` to print `len(text)`, `len(full_markdown)`, and pre-run `chunk_text` from `embedding_service` on both inputs (side-by-side diagnostic for SC#4 hypothesis)
- [ ] Optional: `backend/scripts/upload_latency_curl.sh` (or one-liner committed to `backend/README.md`) — SC#1 live timing probe

**Framework install:** none needed — pytest + asyncio + httpx all already in `backend/requirements.txt`.

---

## Manual-Only Verifications

| Behavior | SC | Why Manual | Test Instructions |
|----------|-----|------------|-------------------|
| `/upload` 201 latency on real thesis PDF | SC#1 | TestClient runs BackgroundTasks synchronously (per `test_documents.py:459-463` comment) — can't measure async-defer behavior in-process | `time curl -X POST -F "file=@thesis.pdf" -H "Authorization: Bearer $JWT" http://localhost:8000/documents/upload` — assert < 1.5s |
| Frontend Realtime status transitions | SC#3 | Requires browser + Supabase Realtime channel + DOM render | Chrome MCP at `localhost:5173` with test login `fhdmrd@gmail.com` / `123456`; observe badge transitions through `pending` → `processing (...)` → `completed` |
| `/health` responsiveness during in-flight extract | SC#2 | Requires concurrent HTTP traffic against running uvicorn process | `while true; do time curl -s http://localhost:8000/health > /dev/null; sleep 0.5; done` during thesis upload — assert <2s on every sample |
| Thesis-PDF chunk count under Docling | SC#4 | Requires live `/reextract` round-trip + Postgres query | Run `/reextract` on `23cc112a-...` with `engine='docling'`; then `SELECT COUNT(*) FROM document_chunks WHERE document_id='23cc112a-...'` → assert ≥ 320 |
| Telemetry-vs-persistence table-count parity | SC#5 | Requires live re-extract + cross-table SQL | After re-extract: `SELECT pr.table_count, (SELECT count(*) FROM document_tables WHERE document_id=pr.document_id) FROM pdf_extraction_runs pr WHERE pr.document_id='23cc112a-...' AND pr.engine='docling'` → assert equal |

---

## Validation Sign-Off

- [ ] All tasks (when plans land) have `<automated>` verify or Wave 0 dependency declared
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references above
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s on quick suite
- [ ] `nyquist_compliant: true` set in frontmatter when plans pass plan-checker

**Approval:** pending
