---
phase: 071.2
plan: 02
type: execute
wave: 3
depends_on: [01, 04]
files_modified:
  - frontend/src/components/ingestion/DocumentStatusBadge.tsx
autonomous: false
requirements:
  - RAG-DOCLING-01
tags:
  - frontend
  - status-badge
  - realtime
  - uat

must_haves:
  truths:
    - "After /upload returns 201, the new documents-list row renders within ~1s carrying a `pending` badge"
    - "Badge transitions pending → processing (Extracting → Chunking → Embedding → Extracting metadata) → completed without manual refresh"
    - "Two new ingestion_step labels — extracting_tables, extracting_images — render with friendly text instead of the default 'processing'"
  artifacts:
    - path: "frontend/src/components/ingestion/DocumentStatusBadge.tsx"
      provides: "Two new ingestion_step label cases (D-071.2-11)"
      contains: "extracting_tables"
  key_links:
    - from: "frontend/src/hooks/useDocuments.ts"
      to: "Supabase Realtime channel 'documents-changes'"
      via: ".on('postgres_changes', { event: '*', schema: 'public', table: 'documents' }, ...)"
      pattern: "postgres_changes"
    - from: "frontend/src/components/ingestion/DocumentStatusBadge.tsx::ingestionStepLabel"
      to: "backend ingestion_step writes (documents.py:953/966/993/1014/1024/1055)"
      via: "switch on step string"
      pattern: "step === \"extracting_tables\""
---

<objective>
Verify (mostly) and minimally extend the frontend documents-list status badge so the Plan 01 instant-201 BackgroundTask lifecycle renders correctly via Supabase Realtime — `pending → processing (with the right ingestion_step label) → completed` — with NO manual page refresh required. Adds two missing `ingestion_step` label cases that the backend already writes (`extracting_tables`, `extracting_images`) but the frontend currently falls through to a generic "processing" label for.

Purpose: closes D-071.2-11 — closes the SC#3 UX gap that surfaced during Phase 072 setup ("upload spinner spins for 1–120s with no row visible until done"). Plan 01 fixes the backend half (instant-201 + row INSERT before BackgroundTask); this plan verifies the frontend half is correct AND adds the two missing labels.

**Dependency note (Wave 3):** This plan depends on Plan 01 (instant-201 BackgroundTask lifecycle) AND Plan 04 (which both modifies the multimodal call sites in `ingest_document` AND preserves the `ingestion_step` UPDATE lines at documents.py:1014/1024 that emit `extracting_tables` / `extracting_images`). Without Plan 04's explicit preservation guarantee on those two writes, the new label cases this plan adds would render but never fire.

Output:
- 2 new switch cases in `DocumentStatusBadge.tsx::ingestionStepLabel` (4 lines total addition)
- Manual UAT pass via Chrome DevTools MCP at `localhost:5173` with login `fhdmrd@gmail.com` / `123456` — uploaded thesis PDF transitions through the full lifecycle without manual refresh
- HUMAN-UAT.md scoreboard entry confirming SC#3 GREEN
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Vibe Apps/Agentic RAG/.planning/STATE.md
@C:/Vibe Apps/Agentic RAG/.planning/ROADMAP.md
@C:/Vibe Apps/Agentic RAG/CLAUDE.md
@C:/Vibe Apps/Agentic RAG/.planning/phases/071.2-ingestion-plumbing-docling-quality-diagnostics/071.2-CONTEXT.md
@C:/Vibe Apps/Agentic RAG/.planning/phases/071.2-ingestion-plumbing-docling-quality-diagnostics/071.2-PATTERNS.md
@C:/Vibe Apps/Agentic RAG/.planning/phases/071.2-ingestion-plumbing-docling-quality-diagnostics/071.2-RESEARCH.md
@C:/Vibe Apps/Agentic RAG/.planning/phases/071.2-ingestion-plumbing-docling-quality-diagnostics/071.2-01-SUMMARY.md
@C:/Vibe Apps/Agentic RAG/.planning/phases/071.2-ingestion-plumbing-docling-quality-diagnostics/071.2-04-SUMMARY.md

<interfaces>
<!-- Current DocumentStatusBadge.tsx structure — extracted from PATTERNS.md §Plan 02 -->

From frontend/src/components/ingestion/DocumentStatusBadge.tsx (39 LOC; existing switch at lines 16-22):
```typescript
function ingestionStepLabel(step: string | null | undefined): string {
  if (step === "extracting") return "Extracting"
  if (step === "chunking") return "Chunking"
  if (step === "embedding") return "Embedding"
  if (step === "metadata") return "Extracting metadata"
  return "processing"
}
```

Backend ingest_document writes these `ingestion_step` values (existing — verified in documents.py per PATTERNS.md):
- "extracting" → labeled
- "extracting_tables" → currently UNLABELED, falls to "processing"   <-- ADD
- "extracting_images" → currently UNLABELED, falls to "processing"   <-- ADD
- "chunking" → labeled
- "embedding" → labeled
- "metadata" → labeled

From frontend/src/hooks/useDocuments.ts (lines 37-65 — Realtime subscription, EXISTING — no change):
```typescript
supabase.channel("documents-changes")
  .on("postgres_changes", { event: "*", schema: "public", table: "documents" }, (payload) => {
    if (payload.eventType === "INSERT") { /* setDocuments(prev => [newDoc, ...prev]) */ }
    if (payload.eventType === "UPDATE") { /* setDocuments(prev => prev.map(...)) */ }
    if (payload.eventType === "DELETE") { /* setDocuments(prev => prev.filter(...)) */ }
  })
  .subscribe()
```

Test login (memory `reference_local_dev_app.md`): http://localhost:5173/, email `fhdmrd@gmail.com`, password `123456`.
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Add extracting_tables + extracting_images label cases to DocumentStatusBadge.tsx</name>
  <files>
    frontend/src/components/ingestion/DocumentStatusBadge.tsx
  </files>
  <read_first>
    frontend/src/components/ingestion/DocumentStatusBadge.tsx
    frontend/src/hooks/useDocuments.ts
    .planning/phases/071.2-ingestion-plumbing-docling-quality-diagnostics/071.2-PATTERNS.md  (§ "PLAN 02 — Frontend status badge UAT + 2 optional label cases" — full section)
    .planning/phases/071.2-ingestion-plumbing-docling-quality-diagnostics/071.2-CONTEXT.md  (§ "D-071.2-11: Frontend status badge")
  </read_first>
  <action>
    Per D-071.2-11 + PATTERNS.md §Plan 02 verbatim:

    In `frontend/src/components/ingestion/DocumentStatusBadge.tsx`, locate the `ingestionStepLabel` function at lines 16-22. Insert two new `if` cases AFTER the existing `if (step === "extracting") return "Extracting"` line, BEFORE the `if (step === "chunking") return "Chunking"` line. The two new lines:

    ```typescript
    if (step === "extracting_tables") return "Extracting tables"
    if (step === "extracting_images") return "Extracting images"
    ```

    Match the existing 2-space indentation and quote style. Total diff: 2 lines added (4 lines with the no-op spacing if the formatter touches anything).

    Do NOT touch the style classes (lines 9-14), the component props (lines 24-30), or the JSX block (lines 32-38). Do NOT touch `useDocuments.ts` — Realtime wiring and optimistic-add already handle the BackgroundTask lifecycle per PATTERNS.md §Plan 02 verify-only note. If TypeScript compilation passes and grep finds both strings, this task is mechanically done.

    Verification: `cd frontend && npx tsc --noEmit` returns 0; `grep -c "extracting_tables" frontend/src/components/ingestion/DocumentStatusBadge.tsx` returns 1.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/ingestion/DocumentStatusBadge.tsx` contains the literal string `step === "extracting_tables"` (grep)
    - `frontend/src/components/ingestion/DocumentStatusBadge.tsx` contains the literal string `step === "extracting_images"` (grep)
    - `frontend/src/components/ingestion/DocumentStatusBadge.tsx` contains the literal string `"Extracting tables"` (grep)
    - `frontend/src/components/ingestion/DocumentStatusBadge.tsx` contains the literal string `"Extracting images"` (grep)
    - `frontend/src/components/ingestion/DocumentStatusBadge.tsx` still contains the existing 4 cases (`extracting`, `chunking`, `embedding`, `metadata`) — sanity check no accidental deletion
    - `cd frontend && npx tsc --noEmit` exits 0
    - `git diff frontend/src/components/ingestion/DocumentStatusBadge.tsx` shows ≤ 4 lines changed (2 inserted, 0 removed; possibly minor formatter touch)
  </acceptance_criteria>
  <done>
    ingestionStepLabel renders friendly text for the two new backend-written step values. TypeScript compiles. No other file touched.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Chrome MCP UAT — verify Realtime status lifecycle end-to-end</name>
  <what-built>
    Plan 01 made /upload return 201 in ~1s and moved extract into a BackgroundTask, so the documents row exists with status='pending' before the BackgroundTask fires. Plan 04 preserved the existing `ingestion_step` UPDATE writes at documents.py:1014 and 1024 (extracting_tables / extracting_images) while threading `extracted_doc` through to multimodal_service. Task 1 added the two missing ingestion_step labels.

    UAT checks that:
    1. The new row appears in the documents list within 1s of /upload returning 201 (Realtime INSERT event from `useDocuments.ts:37-65`).
    2. The badge transitions through `pending → processing (Extracting → Extracting tables → Extracting images → Chunking → Embedding → Extracting metadata) → completed` driven entirely by Realtime UPDATE events (no manual page refresh).
    3. Final state is `completed` and the row stays visible.
  </what-built>
  <how-to-verify>
    Drive a browser via Chrome DevTools MCP (preferred — `feedback_chrome_mcp_testing` memory note + `reference_local_dev_app.md` test login). If Chrome MCP cannot find the tools this session, fall back to a manual browser run + Supabase Studio for row inspection.

    Step 1. Confirm the dev stack is running:
       - Backend: `curl -s http://localhost:8000/health` returns 200 in <500ms
       - Frontend: `curl -sI http://localhost:5173/ | head -1` returns `HTTP/1.1 200 OK`
       - Supabase local: `supabase status` shows running

    Step 2. Navigate to http://localhost:5173/ and log in with `fhdmrd@gmail.com` / `123456`. Open the Documents / Knowledge page (whichever surfaces `useDocuments`).

    Step 3. Open Chrome DevTools Network panel + Console panel. In a second terminal, run:
       ```
       supabase studio &     # if not already open — used for SQL spot-check
       ```

    Step 4. Upload the thesis PDF (`23cc112a-...`'s source file — use the same fixture the user uploaded in Phase 071.1; if not at hand, any ~1MB PDF will exercise the same path).

    Step 5. Within 1.5s of the upload click, observe:
       - The /upload response in DevTools Network panel: Status 201, Time < 1500ms.
       - A new row in the documents list with a `pending` badge (visible in the DOM).
       - In Supabase Studio: `SELECT status, ingestion_step FROM documents ORDER BY created_at DESC LIMIT 1;` shows `status='pending'`, `ingestion_step IS NULL` (or whatever the initial value is).

    Step 6. Watch the badge transition WITHOUT refreshing the page:
       - `pending` → `processing` with label cycling through `Extracting`, `Extracting tables`, `Extracting images`, `Chunking`, `Embedding`, `Extracting metadata`.
       - Finally → `completed`.
       - At every transition, confirm the DOM updates within ~1s of the Supabase Studio row showing the new state (Realtime is best-effort but should be reliable on localhost).

    Step 7. Concurrent /health probe (proves SC#2 doesn't regress):
       ```
       while true; do printf '%s ' $(date +%H:%M:%S); time curl -s http://localhost:8000/health > /dev/null; sleep 1; done
       ```
       Every sample during the in-flight extract should complete < 2s real time.

    Pass conditions:
      - 201 latency < 1.5s on the upload click (SC#1)
      - Row visible with `pending` badge within 1s of 201 (SC#3 part A)
      - Badge transitions through `processing (...)` with the two new labels `Extracting tables` AND `Extracting images` rendered (SC#3 part B — confirms Task 1 wiring + Plan 04 preservation)
      - No manual refresh needed (SC#3 part C)
      - /health stays < 2s for every sample during the extract (SC#2 — sanity)

    Failure modes worth noting:
      - If the badge label says "processing" during the tables/images phase, EITHER Task 1's grep verification didn't catch a missed write OR Plan 04 accidentally removed the `ingestion_step` UPDATE lines at documents.py:1014/1024. Re-check `grep -n "ingestion_step.*extracting_tables" backend/app/api/documents.py` — must return ≥ 1 hit.
      - If the row doesn't appear until refresh, Realtime is not firing — check Supabase Studio → Realtime → channels for `documents-changes` activity. (D-v2.5-03 says Realtime is best-effort — `useDocuments.ts` reconciles via `loadDocuments()` on mount, so worst case the page-refresh fallback works; but for SC#3 we want the no-refresh path.)
      - If /health exceeds 2s at any point, Plan 01's threadpool sweep missed a call site — re-grep documents.py.

    Record the result in `.planning/phases/071.2-ingestion-plumbing-docling-quality-diagnostics/071.2-HUMAN-UAT.md` (create the file if absent; mirror the 071.1 HUMAN-UAT.md shape).
  </how-to-verify>
  <resume-signal>Type "approved" if all pass conditions met. If any fail: describe which one and paste a screenshot or DOM snippet; I will revise/escalate before proceeding.</resume-signal>
</task>

</tasks>

<threat_model>
No high-severity threats identified in scope; threat surface unchanged from current state. Plan 02 modifies a pure-render component (no auth, no API, no PII handling) and adds a manual UAT step. The badge label strings are static (compile-time constants) and contain no user-controlled data, so no XSS surface. Supabase Realtime is already authenticated via the existing session JWT (`useDocuments.ts` setup); RLS on `documents` table ensures users only see their own rows. No new attack surface.
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0
- Grep: `grep -c "extracting_tables" frontend/src/components/ingestion/DocumentStatusBadge.tsx` returns 1
- Chrome MCP UAT (Task 2) passes — SC#1 latency + SC#2 health responsiveness + SC#3 Realtime lifecycle all GREEN, recorded in 071.2-HUMAN-UAT.md
</verification>

<success_criteria>
1. Task 1 — TypeScript compiles + grep confirms both new label cases present
2. Task 2 — Chrome MCP UAT proves /upload returns 201 in <1.5s AND row appears within 1s with pending badge AND badge transitions through processing (with the new labels) to completed WITHOUT manual refresh AND /health stays <2s under concurrent load
3. 071.2-HUMAN-UAT.md scoreboard entry exists with `status: green` or equivalent UAT-approval marker
</success_criteria>

<output>
After completion, create `.planning/phases/071.2-ingestion-plumbing-docling-quality-diagnostics/071.2-02-SUMMARY.md` covering: the 2-line DocumentStatusBadge.tsx change, the UAT result (with Chrome MCP screenshots referenced or pasted DOM/console snippets), and any Realtime-reliability observations (D-v2.5-03 is best-effort — if reconciliation kicked in, note it).

Also append the SC#1/SC#2/SC#3 outcomes to `.planning/phases/071.2-.../071.2-HUMAN-UAT.md`.
</output>
</content>
</invoke>
