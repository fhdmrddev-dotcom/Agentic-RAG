---
phase: 071.3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/bench_table_engines.py
  - .planning/research/071.3-bench-results.md
  - .planning/phases/071.3-docling-demotion-table-engine-full-rip/WINNER.md
autonomous: false
requirements:
  - RAG-DOCLING-01
notes: |
  Roadmap lists Plan 01 + Plan 02 both as Wave 1, but Plan 02 must know the winner
  name to write the final adapter. We assign Plan 02 = Wave 2 (depends_on: [01])
  so that Plan 01 fully completes (including user-confirmed winner sentinel
  WINNER.md) before Plan 02 starts. This makes the dependency chain
  01 → 02 → 03 → 04 → 05 linear and removes the placeholder-substitution risk.
  Plan 01 is autonomous: false because the final task pauses for the user to
  observe bench numbers and name the winning engine.
user_setup:
  - service: user-thesis-pdf
    why: "Bench script requires the user's reference thesis PDF (35 tables / 59 figures ground truth) as fixture 1 (D-071.3-01). Not committed to repo."
    dashboard_config:
      - task: "User supplies absolute path to thesis PDF when invoking bench script"
        location: "CLI arg --thesis <abs-path>"
must_haves:
  truths:
    - "backend/scripts/bench_table_engines.py exists and runs on a Python venv with stdlib + project deps"
    - "Three table engines (PyMuPDF find_tables text+lines_strict, gmft TATR, Camelot 1.0 Stream) execute on two fixtures (user thesis + friendly_real.pdf)"
    - "Bench records median wall time + peak RSS + table count over 3 runs per engine per fixture"
    - "Bench output written to .planning/research/071.3-bench-results.md as markdown table"
    - "Winner sentinel file WINNER.md exists in phase directory with chosen engine name (one of: gmft, pymupdf, camelot, pdfplumber)"
  artifacts:
    - path: backend/scripts/bench_table_engines.py
      provides: "3-engine bench script with argparse CLI, triplicate-run median calculation, bbox-area>=25 recall metric per D-071.3-17"
      contains: "argparse, time.perf_counter, psutil"
    - path: .planning/research/071.3-bench-results.md
      provides: "Markdown table of per-engine results on 2 fixtures with winner pick + rationale"
      contains: "| engine | thesis_tables | friendly_tables | wall_time_s | peak_rss_mb |"
    - path: .planning/phases/071.3-docling-demotion-table-engine-full-rip/WINNER.md
      provides: "Single-line file naming chosen engine for Plan 02 to consume deterministically"
      contains: "winner: (gmft|pymupdf|camelot|pdfplumber)"
  key_links:
    - from: backend/scripts/bench_table_engines.py
      to: backend/tests/fixtures/extraction/friendly_real.pdf
      via: "FIXTURES = Path(__file__).parent.parent / 'tests' / 'fixtures' / 'extraction'"
      pattern: "friendly_real\\.pdf"
    - from: .planning/research/071.3-bench-results.md
      to: .planning/phases/071.3-docling-demotion-table-engine-full-rip/WINNER.md
      via: "user reads numbers, names winner, writes sentinel"
      pattern: "winner:"
---

<objective>
Implement RAG-DOCLING-01 Plan 01: Bench script + spike on two fixtures to pick a non-Docling table engine. Per D-071.3-01..03, D-071.3-08, D-071.3-17.

Purpose: Replace pdfplumber (4 of 35 visible tables on user's thesis) with an engine that recovers >=20 tables (~57% recall, 5x pdfplumber). Research brief (.planning/research/071.3-table-engine-comparison.md) expected winner = gmft (TATR-backed MIT). Plan 01 produces empirical numbers, not a hunch.

Output: bench_table_engines.py committed, bench-results.md markdown table committed, WINNER.md sentinel committed naming the chosen engine.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-CONTEXT.md
@.planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-PATTERNS.md
@.planning/research/071.3-table-engine-comparison.md
@CLAUDE.md

<interfaces>
<!-- Plan 01 is self-contained — produces a CLI script. No code interfaces consumed. -->
<!-- Bench script may import existing fixture-loading helpers from analog probe script. -->

Existing fixture path pattern (matches backend/tests/unit/test_aspect_engines_text.py:13):
```python
FIXTURES = Path(__file__).parent.parent / "tests" / "fixtures" / "extraction"
```

Existing committed fixture available:
- backend/tests/fixtures/extraction/friendly_real.pdf  (arXiv 2605.15184v1 CC-BY 4.0)

User-supplied (NOT committed):
- thesis PDF — passed via --thesis <abs-path> CLI flag

D-071.3-17 recall metric:
- bbox-area >= 25 px^2 counts as a detection
- recall = engine_count / 35 on thesis
- structural fidelity (rows x cols, sample cell content) recorded but NOT used to gate winner
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bench-only deps to a local-only requirements file</name>
  <read_first>
    - backend/requirements.txt (see what's currently pinned; gmft/camelot NOT yet present)
    - .planning/research/071.3-table-engine-comparison.md (sections 2.2 gmft, 2.3 Camelot)
    - .planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-CONTEXT.md (D-071.3-08 LOC ceiling ~250)
  </read_first>
  <action>
    Create `backend/scripts/bench_requirements.txt` (a SEPARATE file from `backend/requirements.txt` — these deps are bench-only and must NOT pollute the production install). Contents (literal):
    ```
    # Phase 071.3 Plan 01 — bench-only deps (NOT installed by production).
    # Used by backend/scripts/bench_table_engines.py to compare 3 table engines.
    # gmft and camelot-py do NOT enter backend/requirements.txt until Plan 02
    # ships only the winning engine.
    gmft>=0.4.3,<0.5.0
    camelot-py[base]>=1.0.0,<2.0.0
    pypdfium2>=4.0.0
    psutil>=5.9.0
    ```
    NOTE: PyMuPDF (`pymupdf>=1.24`) is already in `backend/requirements.txt` — DO NOT duplicate.

    Then install: `cd backend && venv\Scripts\python.exe -m pip install -r scripts/bench_requirements.txt`. Surface any install errors. If `camelot-py` fails on Windows (historical Ghostscript issue is now resolved in 1.0 per pdfium switch — but verify), document the error in `.planning/research/071.3-bench-results.md` "Install Notes" section and proceed with PyMuPDF + gmft (2-engine bench is acceptable per D-071.3-03 ship-best-available).
  </action>
  <verify>
    <automated>test -f backend/scripts/bench_requirements.txt &amp;&amp; grep -q "gmft" backend/scripts/bench_requirements.txt &amp;&amp; grep -q "camelot-py" backend/scripts/bench_requirements.txt &amp;&amp; grep -q "psutil" backend/scripts/bench_requirements.txt</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/scripts/bench_requirements.txt` exists
    - File contains exact strings: `gmft>=0.4.3,<0.5.0`, `camelot-py[base]>=1.0.0,<2.0.0`, `pypdfium2`, `psutil`
    - File does NOT contain `docling` (Docling is being deleted; bench MUST NOT reintroduce)
    - `backend/requirements.txt` is BYTE-IDENTICAL to its pre-Task-1 state (no production deps added by Plan 01)
    - venv install succeeds OR install failure is documented for the failing engine and the bench proceeds with the remaining engines
  </acceptance_criteria>
  <done>Bench-only requirements file committed; production requirements.txt untouched; deps installed in venv (or failures documented).</done>
</task>

<task type="auto">
  <name>Task 2: Author bench script with 3 engine adapters, triplicate runs, markdown output</name>
  <read_first>
    - backend/scripts/probe_docling_timeout.py (shape analog — argparse CLI, load_from_file helper, timed-execution pattern at lines 1-227)
    - backend/tests/unit/test_aspect_engines_text.py (lines 1-30 for FIXTURES path pattern)
    - .planning/research/071.3-table-engine-comparison.md (Section 3 ranks top 3; Section 2.1-2.3 detail each engine's API)
    - .planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-CONTEXT.md (D-071.3-17 bbox-area>=25 metric)
    - .planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-PATTERNS.md (Plan 01 section, lines 43-135 for shape patterns)
  </read_first>
  <action>
    Create `backend/scripts/bench_table_engines.py` (~150-250 LOC ceiling per D-071.3-08). REQUIRED structure:

    1. **Module docstring** following probe_docling_timeout.py:1-33 shape. Include Usage block:
    ```
    Usage:
        cd backend
        venv\Scripts\python.exe scripts/bench_table_engines.py --thesis "C:\path\to\thesis.pdf"
        venv\Scripts\python.exe scripts/bench_table_engines.py --thesis "C:\path\to\thesis.pdf" --output ../.planning/research/071.3-bench-results.md
    ```

    2. **Imports** (top-level): `argparse`, `time`, `gc`, `json`, `sys`, `traceback`, `pathlib.Path`, `dataclasses.dataclass`, `statistics.median`. NO heavy imports at module level — lazy inside engine adapters (Pattern SP-4).

    3. **Constants**:
    ```python
    FIXTURES_DIR = Path(__file__).parent.parent / "tests" / "fixtures" / "extraction"
    FRIENDLY_PDF = FIXTURES_DIR / "friendly_real.pdf"
    THESIS_GROUND_TRUTH_TABLES = 35
    THESIS_GROUND_TRUTH_FIGURES = 59
    BBOX_AREA_THRESHOLD_PX2 = 25.0   # D-071.3-17
    RUNS_PER_ENGINE = 3
    ```

    4. **`@dataclass class EngineResult`** with fields: `engine: str`, `fixture: str`, `table_count: int`, `runs_wall_s: list[float]`, `peak_rss_mb: float`, `status: str`, `error: str | None`, `notes: str`. Property `median_wall_s` returns `statistics.median(runs_wall_s)`. Property `wall_range_s` returns `(min, max)`.

    5. **`_count_detections_above_threshold(detections, threshold_px2)`** helper that filters by bbox area; if engine doesn't expose bbox, returns `len(detections)` unchanged. This implements D-071.3-17.

    6. **Three engine adapters** (each is a `def run_<engine>(raw: bytes) -> tuple[int, str]` returning `(table_count, notes)`. Each lazy-imports its heavy deps INSIDE the function body):

       a. **`run_pymupdf(raw: bytes) -> tuple[int, str]`**:
       ```python
       import fitz  # noqa: PLC0415 — bench-script only; AGPL OK here (D-PRD-07)
       doc = fitz.Document(stream=raw, filetype="pdf")
       try:
           # Try strategy="lines_strict" first then "text" per research brief sec 2.1
           total_lines_strict = 0
           total_text = 0
           # ... iterate pages, call page.find_tables(strategy=...)
           # Wrap find_tables in _redirect_stdout_to_stderr to silence PyMuPDF marketing nudge
           # Count detections via _count_detections_above_threshold using each table's .bbox
           # Return max(total_lines_strict, total_text) and a note like "lines_strict=N text=M"
       finally:
           doc.close()
       ```
       Include the `_redirect_stdout_to_stderr` contextmanager (copy verbatim from `backend/extractors/pymupdf_isolated.py:22-44`) — PyMuPDF 1.27 prints "Consider using the pymupdf_layout package..." to stdout which would corrupt non-JSON output.

       b. **`run_gmft(raw: bytes) -> tuple[int, str]`**:
       ```python
       from gmft.auto import AutoTableDetector, AutoFormatter  # noqa: PLC0415
       from gmft.pdf_bindings.pdfium import PyPDFium2Document  # noqa: PLC0415
       # Write raw to NamedTemporaryFile(suffix=".pdf") because gmft API takes path
       doc = PyPDFium2Document(tmp_path)
       try:
           detector = AutoTableDetector()
           formatter = AutoFormatter()
           total = 0
           for page in doc:
               cropped = detector.extract(page)
               for ct in cropped:
                   # Apply bbox-area threshold per D-071.3-17 if ct exposes bbox
                   total += 1
           return total, "gmft AutoTableDetector"
       finally:
           doc.close()
           os.unlink(tmp_path)
       ```

       c. **`run_camelot(raw: bytes) -> tuple[int, str]`**:
       ```python
       import camelot  # noqa: PLC0415
       # camelot.read_pdf takes a path; write raw to NamedTemporaryFile(suffix=".pdf")
       # Use flavor="stream" per research brief sec 2.3 (best for borderless academic)
       tables = camelot.read_pdf(tmp_path, flavor="stream", pages="all")
       return len(tables), f"camelot stream pages=all"
       ```

       Each adapter wraps its inner body in try/except. On exception, returns `(0, f"FAILED: {type(exc).__name__}: {exc}")` — DO NOT swallow silently; the caller records the status.

    7. **`bench_one_engine(engine_fn, raw, engine_name, fixture_name) -> EngineResult`**:
       - Triplicate runs (RUNS_PER_ENGINE=3): call engine_fn(raw), record wall time per run via `time.perf_counter()` deltas, call `gc.collect()` between runs.
       - Use `psutil.Process(os.getpid()).memory_info().rss` BEFORE and AFTER all runs; record peak RSS in MB.
       - All 3 runs MUST return the same table_count (deterministic); if they don't, take the median count and add note "count_variance=<min>..<max>".
       - On the FIRST run only, capture exceptions and set status="failed"+ skip remaining 2 runs.
       - Otherwise status="success".

    8. **`bench_all(thesis_path, friendly_path) -> list[EngineResult]`**:
       - Load both PDFs as bytes via `Path.read_bytes()`.
       - For each of (thesis, friendly), for each of (run_pymupdf, run_gmft, run_camelot), call bench_one_engine and accumulate results.
       - Skip thesis if `--thesis` arg not provided (allow friendly-only run for CI sanity).

    9. **`write_results(results, output_path)`** — emits markdown to `.planning/research/071.3-bench-results.md`:
    ```markdown
    # Phase 071.3 — Table Engine Bench Results

    **Run date:** {datetime.now().isoformat()}
    **Bench script:** backend/scripts/bench_table_engines.py
    **Fixtures:**
    1. User thesis PDF (ground truth: 35 tables, 59 figures)
    2. backend/tests/fixtures/extraction/friendly_real.pdf (arXiv 2605.15184v1)

    ## Results Matrix

    | Engine | Fixture | Tables Found | Recall vs GT | Median Wall (s) | Wall Range (s) | Peak RSS (MB) | Status | Notes |
    |---|---|---|---|---|---|---|---|---|
    | pymupdf | thesis | 12 | 34% | 1.8 | 1.7-1.9 | 95 | success | lines_strict=8 text=12 |
    | gmft | thesis | 22 | 63% | 28.4 | 26-31 | 740 | success | AutoTableDetector |
    | camelot | thesis | 9 | 26% | 4.2 | 4.0-4.5 | 180 | success | flavor=stream |
    | pymupdf | friendly | ... | ... | ... | ... | ... | ... | ... |

    ## Per-Engine Notes

    - **Acceptance floor (D-071.3-02):** >=20 tables on thesis
    - **Install footprint:**
      - pymupdf: ~25 MB (already shipped)
      - gmft: ~700 MB (torch CPU + transformers + pypdfium2; ~120 MB TATR weights lazy-downloaded first call)
      - camelot: ~80 MB (opencv, pdfium, pdfminer.six)

    ## Winner

    *To be filled by user after review. Update WINNER.md with chosen engine.*
    ```

    10. **`main()`** — argparse with `--thesis` (optional path), `--output` (default: relative `../.planning/research/071.3-bench-results.md`), `--engines` (optional comma-list filter, default all three). Calls bench_all, writes results, prints summary to stdout.

    Keep total LOC under 280 (D-071.3-08 ceiling is ~250, we allow modest slack for the dataclass + result writer; refactor if it sprawls).
  </action>
  <verify>
    <automated>test -f backend/scripts/bench_table_engines.py &amp;&amp; cd backend &amp;&amp; venv/Scripts/python.exe -c "import ast; ast.parse(open('scripts/bench_table_engines.py').read())" &amp;&amp; venv/Scripts/python.exe scripts/bench_table_engines.py --help</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/scripts/bench_table_engines.py` exists and is valid Python (ast.parse succeeds)
    - `--help` invocation exits 0 and prints argparse description
    - File contains exact strings: `RUNS_PER_ENGINE`, `BBOX_AREA_THRESHOLD_PX2`, `_redirect_stdout_to_stderr`, `def run_pymupdf`, `def run_gmft`, `def run_camelot`, `psutil`, `statistics.median`
    - All heavy imports (fitz, gmft, camelot) are INSIDE function bodies (lazy — Pattern SP-4). Grep `^from gmft\|^import gmft\|^from camelot\|^import camelot\|^import fitz\|^from fitz` at top of file returns 0 matches.
    - File line count `wc -l backend/scripts/bench_table_engines.py` <= 280
    - File contains NO `docling` import or reference
  </acceptance_criteria>
  <done>Bench script committed, lints clean, --help works. Ready to execute on fixtures.</done>
</task>

<task type="auto">
  <name>Task 3: Run bench on friendly_real.pdf + (if user supplies) thesis, write results, commit</name>
  <read_first>
    - backend/scripts/bench_table_engines.py (verify it's the version from Task 2)
    - .planning/research/071.3-table-engine-comparison.md (Section 3 — expected gmft wins)
  </read_first>
  <action>
    Execute the bench. Two scenarios:

    **Scenario A — User provides thesis path:**
    ```
    cd backend
    venv\Scripts\python.exe scripts/bench_table_engines.py --thesis "<user-supplied-path>" --output ../.planning/research/071.3-bench-results.md
    ```

    **Scenario B — User has not yet supplied thesis path:**
    Run on friendly_real.pdf only:
    ```
    cd backend
    venv\Scripts\python.exe scripts/bench_table_engines.py --output ../.planning/research/071.3-bench-results.md
    ```
    Then surface to user: "Bench has run on friendly_real.pdf only. To complete D-071.3-13 floor verification, supply absolute path to your reference thesis PDF and I will re-run with `--thesis <path>`."

    Expected timing on first gmft run: 5-10 minutes (TATR model lazy-downloads ~120 MB to ~/.cache/huggingface). Subsequent runs fast. Use timeout 1800s on the Bash invocation.

    On completion:
    - Verify `.planning/research/071.3-bench-results.md` is non-empty and contains the results matrix table.
    - Commit both `backend/scripts/bench_table_engines.py` + `backend/scripts/bench_requirements.txt` + `.planning/research/071.3-bench-results.md` in a single commit: `feat(071.3-01): bench table engines (pymupdf|gmft|camelot)`.
  </action>
  <verify>
    <automated>test -s .planning/research/071.3-bench-results.md &amp;&amp; grep -q "Results Matrix" .planning/research/071.3-bench-results.md &amp;&amp; grep -qE "(pymupdf|gmft|camelot)" .planning/research/071.3-bench-results.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/research/071.3-bench-results.md` is non-empty (`test -s`)
    - File contains literal heading `## Results Matrix`
    - File contains at least one row mentioning `pymupdf`, `gmft`, or `camelot` (whichever engines successfully ran)
    - File contains the `friendly_real.pdf` fixture row (always — committed fixture, no user action needed)
    - If user supplied --thesis: file contains thesis row(s) with recall % vs ground truth 35
    - If a particular engine failed install: file contains an `## Install Notes` section documenting the failure
    - Commit exists with message starting `feat(071.3-01):`
  </acceptance_criteria>
  <done>Bench results captured to .planning/research/071.3-bench-results.md; committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: User reviews bench, names winning engine, executor writes WINNER.md</name>
  <what-built>
    Bench script + results matrix at `.planning/research/071.3-bench-results.md`. Per D-071.3-02, the winner should clear >=20 tables on the thesis. Per D-071.3-03, if all 3 underperform, ship best-available.
  </what-built>
  <how-to-verify>
    1. Open `.planning/research/071.3-bench-results.md` and read the Results Matrix.
    2. Compare each engine's thesis_tables count against the D-071.3-02 floor (>=20).
    3. Consider tradeoffs:
       - **gmft** (expected primary winner per research brief): high recall, +700 MB install, 28s/page on CPU first call
       - **pymupdf** (already shipped): zero new deps, ~2s/page, lower recall on borderless
       - **camelot** (likely fallback): MIT, ~80 MB, mid-pack on academic per arXiv 2410.09871
       - **pdfplumber** (best-of-bad-lot fallback per D-071.3-03): only if all 3 candidates miss the floor AND user explicitly accepts shipping the current interim
    4. Decide winner. Reply in chat with one of: `winner: gmft`, `winner: pymupdf`, `winner: camelot`, or `winner: pdfplumber`.

    Executor on resume then:
    - Writes `.planning/phases/071.3-docling-demotion-table-engine-full-rip/WINNER.md` with literal content (no extra prose):
    ```
    ---
    plan: 071.3-01
    chose_at: <ISO date>
    winner: <user-chosen engine name>
    rationale: <one paragraph summarizing user's reasoning from chat>
    thesis_tables: <count from bench, or "N/A — user did not supply thesis">
    thesis_recall_pct: <pct, or "N/A">
    friendly_tables: <count from bench>
    floor_cleared: <true|false>  # true if thesis_tables >= 20 per D-071.3-02
    ---
    ```
    - Updates the `## Winner` section of `.planning/research/071.3-bench-results.md` to reflect the choice (move out of placeholder state).
    - Commits BOTH files: `feat(071.3-01): name <winner> as table engine`.
    - Plan 02 will read `winner:` field from WINNER.md frontmatter to determine adapter name, registry key, requirements.txt addition.
  </how-to-verify>
  <resume-signal>Reply with one of: `winner: gmft`, `winner: pymupdf`, `winner: camelot`, `winner: pdfplumber`. Include 1-2 sentences of rationale.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user filesystem -> bench script | User supplies thesis PDF path; bench reads bytes via Path.read_bytes() |
| pdf bytes -> engine library | gmft TATR / camelot / fitz consume raw PDF bytes; malicious PDF could OOM or crash |
| bench script -> stdout/stderr | PyMuPDF prints marketing nudge to stdout; could corrupt downstream tools if not redirected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-071.3-01-01 | D (DoS) | gmft TATR model on malicious crafted PDF | mitigate | Each engine call wrapped in try/except; on OOM/crash sets EngineResult.status="failed" and continues to next engine. RUNS_PER_ENGINE=3 with gc.collect() between runs bounds memory accumulation. Bash invocation uses timeout=1800s ceiling. |
| T-071.3-01-02 | T (Tampering) | bench output corruption | mitigate | _redirect_stdout_to_stderr() contextmanager around PyMuPDF find_tables (copied from pymupdf_isolated.py:22-44) prevents marketing nudge from corrupting stdout. Results file written via Path.write_text(); no shell escapes. |
| T-071.3-01-03 | I (Info disclosure) | thesis PDF path in committed artifacts | accept | Bench results file MAY include absolute path string in the "fixtures" section. User instructed to redact via the chat prompt if path is sensitive. Internal dev tool — LOW. |
| T-071.3-01-04 | S (Spoofing) | bench-requirements.txt supply chain | mitigate | All deps pinned with version ranges (gmft>=0.4.3,<0.5.0; camelot-py>=1.0.0,<2.0.0). Production requirements.txt UNTOUCHED in Plan 01 — bench-only deps cannot affect FastAPI runtime. |

**Severity:** LOW overall. Bench is an internal dev tool run by user against user's own PDFs in a venv; no user-facing surface.
</threat_model>

<verification>
- `backend/scripts/bench_table_engines.py` exists and is syntactically valid Python.
- `backend/scripts/bench_requirements.txt` exists with gmft, camelot-py, pypdfium2, psutil pins.
- `backend/requirements.txt` is BYTE-IDENTICAL to pre-plan state.
- `.planning/research/071.3-bench-results.md` exists with results matrix (markdown table).
- `.planning/phases/071.3-docling-demotion-table-engine-full-rip/WINNER.md` exists with `winner:` field naming chosen engine.
- All Plan 01 changes committed via at least one commit with message starting `feat(071.3-01):` or `docs(071.3-01):`.
</verification>

<success_criteria>
- Bench numbers exist for all 3 engines on at least friendly_real.pdf; on thesis if user supplied path.
- User has reviewed numbers, named a winner, and the winner is recorded in WINNER.md frontmatter.
- Plan 02 has a deterministic input (`yq '.winner' WINNER.md` or grep `winner:` returns single value).
- ROADMAP SC#1 satisfied: "Bench script in backend/scripts/bench_table_engines.py ... runs ... and prints per-engine: tables found, wall time, peak RSS, install footprint. Output committed to .planning/research/071.3-bench-results.md."
</success_criteria>

<output>
After completion, create `.planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-01-SUMMARY.md` describing: bench engines tried, fixtures used, winner chosen, recall numbers, any install issues, any unexpected behavior to flag to Plan 02.
</output>
</output>
