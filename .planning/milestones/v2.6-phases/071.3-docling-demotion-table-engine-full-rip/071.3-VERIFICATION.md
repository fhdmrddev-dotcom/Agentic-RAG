---
phase: 071.3-docling-demotion-table-engine-full-rip
verified: 2026-05-16T12:00:00Z
status: passed
score: 7/7 must-haves verified (SC#6 closed via override 2026-05-16)
overrides_applied: 1
overrides:
  - sc: 6
    decided_at: 2026-05-16
    decided_by: operator
    rationale: |
      The 30-image baseline cited in the original SC#6 wording was Docling's image
      extractor output, which was retired in Plan 04 (Phase G). pymupdf_full has
      always been at 20 images on the thesis PDF — there is no regression, only
      a baseline change inherited from the rip. The image recall lift is now
      tracked formally by SEED-021 (planted on the image axis at 20 < 45 per
      D-071.3-16), with a concrete re-open trigger.

      ROADMAP SC#6 text was rewritten in-place to reflect what was actually
      shipped: tables-only ship floor (>= 15 cleared 14.3x), pymupdf_full image
      preservation (>= 20), image lift deferred to SEED-021. Phase ships green.
    artifacts:
      - path: ".planning/ROADMAP.md"
        change: "SC#6 wording updated at phase close to match shipped scope"
      - path: ".planning/seeds/SEED-021-table-image-recall-lift.md"
        change: "Captures the image recall lift as a planned follow-up with re-open trigger"
---

# Phase 071.3: Docling Demotion — Table Engine + Full Rip — Verification Report

**Phase Goal:** Replace the interim pdfplumber table default with a non-Docling engine that recovers borderless/layout-aligned academic tables (thesis floor: >=15 tables vs current 4), then complete the Docling demotion by deleting the runtime dependency and reclaiming the install-pollution side-effects (httpx pin, PyMuPDF subprocess fence). Promotes SEED-019.
**Verified:** 2026-05-16T12:00:00Z
**Status:** passed (SC#6 closed via override — see frontmatter `overrides`)
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Success Criteria)

| #  | Truth                                                                                                | Status          | Evidence                                                                                                       |
|----|------------------------------------------------------------------------------------------------------|-----------------|----------------------------------------------------------------------------------------------------------------|
| 1  | Bench script exists; results committed; winning engine documented with rationale                     | ✓ VERIFIED      | `backend/scripts/bench_table_engines.py` exists; `.planning/research/071.3-bench-results.md` exists with Results Matrix and `## Winner: chosen: camelot`; WINNER.md frontmatter has `winner: camelot` with full rationale |
| 2  | Winning engine (camelot) wired in tables.py; in TABLE_ENGINES; lazy-imported; unit tests cover 3 cases | ✓ VERIFIED   | `camelot_tables` at `aspects/tables.py:36`; `TABLE_ENGINES = {"camelot": camelot_tables, "pdfplumber": pdfplumber_tables}` at `aspects/__init__.py:58-61`; `import camelot` is inside function body (Pattern SP-4); `test_camelot_tables_happy_path`, `test_camelot_tables_invalid_mime_returns_empty`, `test_camelot_tables_empty_pdf_returns_empty` all present in `test_aspect_engines_tables.py` |
| 3  | Migration 047 exists; constraint = (camelot, pdfplumber); default = camelot in user_settings.py + full-schema.sql | ✓ VERIFIED | `supabase/migrations/047_app_settings_table_engine_default.sql` exists with `CHECK (extraction_table_engine_pdf IN ('camelot', 'pdfplumber'))` and `SET DEFAULT 'camelot'`; `user_settings.py:97` has `extraction_table_engine_pdf: str = "camelot"` and line 305 has `_str(override, "extraction_table_engine_pdf", "camelot")`; `full-schema.sql` line 254 shows `DEFAULT 'camelot'::text` and line 259 shows the CHECK constraint |
| 4  | Docling rip: `docling*` removed from requirements.txt; docling.py deleted; 4 adapters deleted; EXTRACTOR_DOCLING_* env knobs deleted | ✓ VERIFIED | `requirements.txt` has no `docling` package line (only a comment mentioning it). `backend/app/services/extractors/docling.py` — not found (deleted). All 4 aspect functions gone (`docling_tf` absent from tables.py, text.py, images_pdf.py, equations.py). `aspects/__init__.py` imports only camelot/pdfplumber/legacy/pymupdf/pymupdf_full/pdfplumber/inline_shapes/zip_xpath/none — no docling imports. `EXTRACTOR_DOCLING_*` grep returns only comments/docstrings, zero live code. Note: knobs were never in `config.py`; deletion occurred at call sites (.env.example, documents.py, main.py) per Plan 04's PATTERNS.md audit — intent of SC#4 fully achieved. |
| 5  | httpx unpinned; pymupdf_isolated.py + pymupdf.py (parent) + test_pymupdf_fence.py deleted; pymupdf_full uses in-process fitz; AGPL note retained | ✓ VERIFIED | `requirements.txt:37` reads `httpx>=0.28.0` (no upper bound); `backend/extractors/pymupdf_isolated.py` — not found; `backend/app/services/extractors/pymupdf.py` — not found; `backend/tests/integration/test_pymupdf_fence.py` — not found; AGPL comment block at `requirements.txt:19-24` retained; `aspects/images_pdf.py` and `aspects/text.py` import fitz inline (Plan 04 G-PASS confirmed) |
| 6  | Live UAT: thesis tables >= 15 (camelot target), images preserve pymupdf_full baseline, chunks >= 200; SC#6 rewritten at phase close to reflect shipped scope (image lift carried by SEED-021) | ✓ VERIFIED (override) | Tables: 214 (PASS, 14.3x floor). Images: 20 (pymupdf_full baseline preserved — 30-image figure in original SC#6 was Docling's extractor output; retired with the rip; no regression). Chunks: 461 (PASS). SEED-021 planted on image axis (20 < 45 per D-071.3-16) captures the recall lift as a tracked follow-up. ROADMAP SC#6 text rewritten 2026-05-16 to match shipped scope. Override rationale in frontmatter. |
| 7  | SEED-019 status = closed; SEED-020 planted; SEED-021 conditional plant decision recorded in UAT     | ✓ VERIFIED      | SEED-019 frontmatter: `status: closed`, `closed: 2026-05-16`, `closed_by: 071.3-docling-demotion-table-engine-full-rip`. SEED-020 frontmatter: `status: planted`, `planted: 2026-05-16`. SEED-021 frontmatter: `status: planted`, planted on image-axis trigger (20 < 45 threshold per D-071.3-16). HUMAN-UAT.md has full "D-071.3-16 SEED-021 Decision" section. |

**Score:** 7/7 truths verified (SC#6 closed via override 2026-05-16 — see frontmatter `overrides`)

### Deferred Items

None — all items expected to be delivered in Phase 071.3 itself.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `backend/scripts/bench_table_engines.py` | Bench script (SC#1) | ✓ EXISTS | Found via Glob; ~338 LOC with argparse CLI, triplicate runs, psutil RSS |
| `.planning/research/071.3-bench-results.md` | Bench results with winner (SC#1) | ✓ EXISTS | Results Matrix + Install Notes + `## Winner: chosen: camelot` section |
| `.planning/phases/071.3-docling-demotion-table-engine-full-rip/WINNER.md` | Winner sentinel (SC#1) | ✓ EXISTS | Frontmatter `winner: camelot`, `floor_cleared: true`, full rationale |
| `backend/app/services/extractors/aspects/tables.py` | camelot_tables adapter (SC#2) | ✓ SUBSTANTIVE | 119 lines; full camelot implementation with tempfile write, flavor=stream, bbox parsing, D-071.3-06 bubble policy |
| `backend/app/services/extractors/aspects/__init__.py` | TABLE_ENGINES registry (SC#2) | ✓ VERIFIED | `{"camelot": camelot_tables, "pdfplumber": pdfplumber_tables}` — no docling entries |
| `backend/tests/unit/test_aspect_engines_tables.py` | Unit tests (SC#2) | ✓ VERIFIED | 3 camelot tests: happy_path, invalid_mime, empty_pdf |
| `supabase/migrations/047_app_settings_table_engine_default.sql` | Migration 047 (SC#3) | ✓ EXISTS | Idempotent guards, correct constraint and default |
| `backend/app/models/user_settings.py` | camelot default (SC#3) | ✓ VERIFIED | Lines 97 + 305 both show "camelot" |
| `supabase/full-schema.sql` | Regen with camelot default (SC#3) | ✓ VERIFIED | Line 254: `DEFAULT 'camelot'::text`; line 259: CHECK constraint |
| `backend/app/services/extractors/docling.py` | DELETED (SC#4) | ✓ DELETED | File not found — confirmed absent |
| `backend/extractors/pymupdf_isolated.py` | DELETED (SC#5) | ✓ DELETED | File not found — confirmed absent |
| `backend/app/services/extractors/pymupdf.py` | DELETED (SC#5) | ✓ DELETED | File not found — confirmed absent |
| `backend/tests/integration/test_pymupdf_fence.py` | DELETED (SC#5) | ✓ DELETED | File not found — confirmed absent |
| `backend/tests/integration/test_pdf_extractor_docling_compat.py` | DELETED (SC#4) | ✓ DELETED | File not found — confirmed absent |
| `.planning/phases/071.3-docling-demotion-table-engine-full-rip/071.3-HUMAN-UAT.md` | Live UAT record (SC#6) | ✓ EXISTS + ✗ PARTIAL | File exists; tables=214/images=20/chunks=461 recorded; images floor (>=30) not met |
| `.planning/seeds/SEED-019-pymupdf-subprocess-oom-non-docling-eval.md` | SEED-019 closed (SC#7) | ✓ VERIFIED | `status: closed`, `closed_by: 071.3-...` |
| `.planning/seeds/SEED-020-retrieval-quality-audit.md` | SEED-020 planted (SC#7) | ✓ EXISTS | `status: planted`, scope matches D-071.3-15 |
| `.planning/seeds/SEED-021-table-image-recall-lift.md` | SEED-021 planted (SC#7) | ✓ EXISTS | `status: planted`, image-axis trigger documented |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `aspects/__init__.py` | `camelot_tables` in `tables.py` | `from app.services.extractors.aspects.tables import camelot_tables` | ✓ WIRED | Line 35 imports camelot_tables; line 59 assigns to `TABLE_ENGINES["camelot"]` |
| `aspects/__init__.py` → no docling imports | docling removed | absence check | ✓ CLEAN | Grep for `from app.services.extractors.docling` returns 0 matches across all backend files |
| `user_settings.py` default | DB migration 047 default | both set to `"camelot"` | ✓ ALIGNED | user_settings.py line 97 = "camelot"; migration 047 `SET DEFAULT 'camelot'`; full-schema.sql `DEFAULT 'camelot'::text` |
| `requirements.txt` | camelot dep | `camelot-py[base]>=1.0.0,<2.0.0` | ✓ WIRED | Line 45 |
| `requirements.txt` | docling dep removed | absence | ✓ CLEAN | Grep for `docling` in requirements.txt returns only a comment (line 34: "existed because docling 2.x...") |
| `requirements.txt` | httpx unpinned | `httpx>=0.28.0` | ✓ WIRED | Line 37; no upper bound |
| `.env.example` | no EXTRACTOR_DOCLING_* | absence check | ✓ CLEAN | Grep for `docling` or `EXTRACTOR` in `.env.example` returns 0 matches |
| SEED-019 | closed | `status: closed` + `closed_by:` | ✓ LINKED | Closing note section maps Q1-Q5 to specific plan outcomes |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `camelot_tables` adapter | `out: list[TableData]` | `camelot.read_pdf(tmp_path, flavor="stream", pages="all")` | Yes — iterates camelot Table objects, extracts df rows + bbox | ✓ FLOWING |
| `TABLE_ENGINES["camelot"]` → `extract_composable` | tables result | registry lookup + adapter call | Yes — real dispatch per aspects/__init__.py | ✓ FLOWING |
| `app_settings.extraction_table_engine_pdf` | default value | `load_app_settings()` reads DB row or falls back to "camelot" | Yes — DB-backed with Python fallback matching | ✓ FLOWING |

### Behavioral Spot-Checks

Step 7b: SKIPPED for this phase — the relevant behaviors are extraction-pipeline calls that require a running backend + Supabase connection. The live UAT in Plan 05 is the behavioral verification record. Static code checks performed above are sufficient for automated verification.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| RAG-DOCLING-01 | All 5 plans claim it | PDF + DOCX produce comparable table + image counts under new extractor; `PdfExtractor` abstraction preserved | PARTIALLY SATISFIED | Tables: 214 (dramatically up from 4). DOCX: 1 table, 1 image (continuity confirmed). However, images dropped from prior 30-baseline to 20 — the "comparable counts" intent for images is not met vs SC#6's stated >=30 floor. The requirement text ("within 20% delta") was originally about PDF-vs-DOCX parity, but ROADMAP SC#6 operationalized it with an absolute image floor of 30. |

**Orphaned requirements check:** REQUIREMENTS.md Traceability maps RAG-DOCLING-01 to Phase 071 (Complete). Phase 071.3 re-asserts it under non-Docling defaults. No other requirement IDs are mapped to Phase 071.3 in REQUIREMENTS.md, consistent with the PLAN frontmatter (`requirements-completed: [RAG-DOCLING-01]` across all plans).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `backend/.env.example` | 152-155 | Dead env var `PYMUPDF_TIMEOUT_S` — subprocess fence was deleted in Plan 04 Phase G; this knob is no longer consumed anywhere | Warning (WR-02) | Misleads operators that a tuning knob exists when no code reads it; no runtime impact |
| `backend/app/main.py` | 11-12 | Comment references `PYMUPDF_TIMEOUT_S` as justification for `load_dotenv()` — same dead reference | Info | Comment-only; no runtime impact |
| `backend/app/api/documents.py` | 344-350, 725-733 | `Query(description=...)` strings still mention `docling` and `docling_tf` as valid engine names in the OpenAPI schema | Warning (WR-01) | User-facing: Swagger UI + /openapi.json will show invalid engine names; submitting them causes KeyError in extract_composable |
| `backend/app/services/extraction_service.py` | 78-79, 104-106 | Docstrings mention `'docling'` as a valid extractor_name and `future = Docling` | Info (IN-01, IN-02) | Documentation drift; no runtime impact |
| `backend/tests/unit/test_aspect_engines_tables.py` | — | No test for failure-bubble policy (D-071.3-06): camelot raises → exception propagates out of adapter unswallowed | Info (IN-07) | Test coverage gap; D-071.3-06 contract is unverified by test suite |
| `backend/app/api/documents.py` | 165, 764 | `get_extractor` imported but never called (dead import post-rip) | Info (IN-04) | Unused import; no runtime impact |

Stub classification: None of the above are data-path stubs. They are documentation/cleanup debris and one test coverage gap. The `PYMUPDF_TIMEOUT_S` case is a dead env-var documentation artifact (WR-02) — no code path reads it — confirmed by grep returning zero matches in Python files.

### Human Verification Required

None. All SC gaps are verifiable from static code analysis and committed artifacts. The images=20 vs >=30 failure is a quantitative gap fully documented in the UAT artifact — no further human testing is needed to characterize it.

## Gaps Summary

**One gap blocks goal achievement:** SC#6 requires `document_images >= 30` on the user's reference thesis (explicitly stated in ROADMAP as "preserved from prior baseline post-migration 046"). The live UAT in Plan 05 recorded 20 images — a regression from the prior 30-image baseline. The phase's own HUMAN-UAT.md narrowed the phase-ship floor to tables-only (>=15) and declared status: green, but this contradicts the ROADMAP contract.

**Root cause:** When the PyMuPDF subprocess fence was retired in Plan 04 Phase G and `pymupdf_full_images_pdf` was rewritten to use in-process `import fitz` directly, the image count may have changed behavior. The Plan 05 SUMMARY notes "Image recall stuck at 20 across both PDFs" and acknowledges this as a carry-forward issue absorbed by SEED-021 — but this was decided without noting it failed a ROADMAP SC.

**Disposition options:**
1. Accept with override: the ROADMAP SC#6 images floor was set against the old subprocess-fenced baseline; the in-process pymupdf_full produces 20 images, and SEED-021 owns the lift. Add an override entry acknowledging this deviation.
2. Fix: investigate whether the in-process `pymupdf_full_images_pdf` rewrite changed image detection behavior vs the old subprocess path, and bring images back to >=30 before closing.

To accept option 1, add to VERIFICATION.md frontmatter:

```yaml
overrides:
  - must_have: "Live UAT on thesis: document_images count >= 30"
    reason: "In-process pymupdf_full yields 20 images post-fence-retire vs the subprocess-fence baseline of 30. The delta is tracked by SEED-021 (image-recall lift). The ROADMAP SC#6 images floor was set against the old implementation; the new in-process path's lower count is a known measurement, not an undocumented regression. SEED-021 owns the lift."
    accepted_by: "<your-name>"
    accepted_at: "<ISO timestamp>"
```

**Non-blocking findings from REVIEW.md (WR-01, WR-02):** Two warnings from the code review (stale API docstrings mentioning docling engine names in the OpenAPI schema; dead PYMUPDF_TIMEOUT_S env var in .env.example) are documentation cleanup items that do not affect extraction correctness or phase-goal achievement. They are surfaced here for awareness but do not constitute goal-blocking gaps.

---

_Verified: 2026-05-16T12:00:00Z_
_Verifier: Claude (gsd-verifier)_
