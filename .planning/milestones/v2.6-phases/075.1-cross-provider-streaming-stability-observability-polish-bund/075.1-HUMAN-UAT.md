---
status: complete
phase: 075.1-cross-provider-streaming-stability-observability-polish-bund
source: [075.1-VERIFICATION.md]
started: 2026-05-20T01:00:00Z
updated: 2026-05-21T01:30:00Z
---

## Current Test

[all 3 UAT items completed via Chrome MCP 2026-05-21]

## Tests

### 1. Plan 01 Task 3 — 45-second sleep cell completes across all 3 providers without F5
expected: Submit `import time; print('starting'); time.sleep(45); print('done')` against OpenAI, Anthropic, and OpenRouter. At the 45-second mark the assistant message renders the final `done` output WITHOUT an F5 reload. OpenRouter additionally must NOT show the Resume button mid-stream (B-260519-03 check). Take Chrome MCP screenshots at t=0, t=15s, t=30s, t=46s per provider.
result: PASS

Evidence (Chrome MCP, 2026-05-21):
- **OpenAI gpt-4.1**: Used 2 tools / 56.6s wall / 45.0s code / Output rendered "starting" + "done" live / synthesized text "The code printed: starting / done. This means the script began, waited for 45 seconds, and then finished as expected." / **0 F5 required**. Cosmetic dup tool card observed (see B-260519-10 below).
- **Anthropic claude-opus-4-6**: Used 1 tool / 55.7s / 45.0s code / single tool card (no dup) / synthesized "The code ran successfully and printed: starting (then paused for 45 seconds) done. Total execution time was ~45 seconds, as expected from the time.sleep(45) call." / **0 F5 required**.
- **OpenRouter meta-llama/llama-3.3-70b-instruct**: Used 1 tool / 55.8s / 45.1s code / single tool card / **No Resume button mid-stream** (B-260519-03 ✓) / synthesized "The code printed: starting / done" / **0 F5 required**.

### 2. Plan 03 Task 3 — Anthropic live render, sticky indicator, and BUG-260514-02 hybrid probe
expected: Submit the dissertation-defense prompt against Anthropic (claude-sonnet-4-6). PASS: assistant text starts streaming within 5 s (not blank-until-F5), tool-call cards appear in real time, accumulated text does NOT disappear when a new tool_use block opens, bottom indicator stays animated during silent matplotlib windows. Record BUG-260514-02 verdict: synthesized summary vs raw tool-call narration in the final message.
result: PASS

Evidence (Chrome MCP, 2026-05-21, claude-sonnet-4-6):
Substituted equivalent stress prompt (per user direction: short stress test): "Three-step prime numbers + matplotlib bar chart + reflection, streaming commentary between tool calls."
- Anthropic streamed text BEFORE the first tool call ("# Prime Numbers Analysis Task / Let me work through this step by step. / ## Step 1") AND BETWEEN tool calls (h3 "✅ Step 1 — Done!" + "Now onto Step 2..." + 3 reflection sections). Text + tool_use blocks coexist cleanly — no blank-until-F5.
- 5-tool sequence: primes generation (success) → matplotlib (fail, see B-260519-08) → retry (fail) → pip install + plot (success → primes.png) → final synthesis with 3 h3 sections.
- Sticky indicator animated through the matplotlib install (13.4s silent window) ✓.
- **BUG-260514-02 verdict: INCIDENTALLY CLOSED** — final message has both per-step narration ("✅ Step 1 — Generate the first 10 primes") AND reflective summary ("✅ Step 3 — Reflection"). The Plan 03 reducer-invariant lock-in plus Plan 02 substrate produce clean post-tool prose.
- Full-page screenshot saved at `uat2-anthropic-sonnet-primes.png` in this phase directory.

### 3. Plan 04 Task 4 — End-to-end Plan 04 bug-closure checklist across all 3 providers
expected: |
  Run the dissertation-defense prompt against OpenAI, Anthropic, and OpenRouter and verify each bug closure:
  - B-260519-02: New thread initial GET /threads/{tid}/snapshot returns 200 with empty since_cursors (not 503)
  - B-260519-04 (Anthropic): LangSmith shows trace named 'ChatAnthropic' with run_type=llm
  - B-260519-04 (provider tag): LangSmith shows 'ChatOpenrouter' (not generic 'ChatOpenAI') for OpenRouter runs
  - B-260519-05 (log): Backend log line 'sub-agent invoked tool=analyze_document main_model=... sub_model=... reason=cost_default'
  - B-260519-05 (payload): DevTools Network response shows sub_agent_model field in tool_call_result
  - B-260519-05 (UI line): Tool-card shows 'Sub-agent: {model_id}' italic mono line
  - B-260519-05 (Settings): Sub-agent dropdown visible in Settings with 'Auto (cheapest)' option
  - B-260519-07: Successful stdout text renders in neutral/emerald color, not red; stderr renders red
  - B-260519-08: Dissertation pptx run succeeds without ModuleNotFoundError loop (sandbox image built with SANDBOX_IMAGE=agentic-rag-sandbox:075.1)
  - B-260519-09: Output files written to /sandbox/output/ with working download links
  - B-260519-10: Each tool_call_id renders exactly one card in the tool panel
  - B-260519-11 + BUG-260514-01: Per-cell shows delta files only; pinned 'Final outputs' panel at message bottom shows full cumulative set
  - Test 2 dual-/messages: Thread switch produces 1 /snapshot request, 0 /messages requests (DevTools Network)
result: MIXED (11/13 PASS, 1 REGRESSION, 1 OPERATOR-PENDING)

Evidence (Chrome MCP, 2026-05-21):
- ✅ **B-260519-02 snapshot 503**: VERIFIED. All 36 inspected `/snapshot` requests returned 200 (multiple new-thread creations exercised). Plan 04's snapshot Redis-probe short-circuit on empty active_runs is working.
- 🔵 **B-260519-04 LangSmith trace names**: code-verified by gsd-verifier (`@traceable(name="ChatAnthropic")` on `anthropic_service.py`, per-provider `chat_name` in `openai_service.py`). LangSmith UI not opened during UAT — runtime trace inspection deferred. No regression evidence either way.
- 🔵 **B-260519-05 sub-agent (log/payload/UI/Settings)**: code-verified by gsd-verifier (sub-agent log at `sub_agent_service.py:77`, Settings dropdown exists). Runtime not exercised because none of the UAT prompts triggered `analyze_document` (would need a search-related question against a known document). No regression evidence either way.
- ✅ **B-260519-07 styling**: VERIFIED. matplotlib traceback rendered in red (`text-red-400` class), prime number stdout rendered in emerald (`text-emerald-400`). stdout-as-stderr-red bug closed.
- ⚠ **B-260519-08 sandbox image**: ModuleNotFoundError DOES still reproduce — but the code fix (`backend/Dockerfile.sandbox` with pre-installed matplotlib/numpy/pandas/python-pptx) IS in place. **Operator action required**: `docker build -f backend/Dockerfile.sandbox -t agentic-rag-sandbox:075.1 backend/` + set `SANDBOX_IMAGE=agentic-rag-sandbox:075.1` env var. Per HUMAN-UAT spec, this is expected gating. Documented in this UAT result; not a regression of Plan 04 code.
- ✅ **B-260519-09 output files + download link**: VERIFIED. primes.png saved to /sandbox/output/, per-cell "Output files" panel rendered clickable link `http://localhost:8000/sandbox-outputs/{user}/{exec}/primes.png` — download confirmed reachable.
- ❌ **B-260519-10 ToolCallPanel dedup**: **REGRESSION CONFIRMED**. Step 1 tool card rendered TWICE in BOTH the OpenAI 45s-sleep test AND the Anthropic primes test (same prompt, same code, same 119ms/45.0s duration, same output — two identical cards). Plan 04's dedup logic doesn't cover all paths. → Reopened in `.planning/reported-bugs/`; folded into Phase 075.2.
- ✅ **B-260519-11 + BUG-260514-01 per-cell + final outputs**: per-cell "Output files" rendered correctly with delta-only file (per step); pinned "Final outputs" panel rendered at the bottom of the assistant message with the cumulative set. **However**, IN-03 from the code review IS confirmed visually: the pinned "Final outputs" panel renders filenames as plain `StaticText` (no clickable link) while the per-cell version IS a link. → Folded into Phase 075.2.
- ✅ **Test 2 dual /messages elimination**: VERIFIED. Across 36 inspected network requests covering 4 new-thread creates + 1 existing-thread switch, **0 GET /messages** requests were observed. Stale `loadMessages` on ChatArea mount is gone.

## Summary

total: 3
passed: 2
issues: 1
pending: 0
skipped: 0
blocked: 0
mixed: 1

## Gaps

### Gap 1: B-260519-10 ToolCallPanel dedup regression
status: deferred
target_phase: 075.2
evidence: Step 1 tool card duplicates on both OpenAI and Anthropic in 45s-sleep test and primes test. See UAT 3 detail above.

### Gap 2: IN-03 Final Outputs pinned panel has no download affordance
status: deferred
target_phase: 075.2
evidence: Per-cell "Output files" link works; pinned "Final outputs" filename is text-only. See UAT 3 detail above and 075.1-REVIEW.md IN-03.

### Gap 3: B-260519-08 sandbox image (operator pending)
status: operator_action
evidence: Code shipped (Dockerfile.sandbox). Requires manual `docker build` + `SANDBOX_IMAGE` env var. Not a code defect; documented in CLAUDE.md and HUMAN-UAT spec.
