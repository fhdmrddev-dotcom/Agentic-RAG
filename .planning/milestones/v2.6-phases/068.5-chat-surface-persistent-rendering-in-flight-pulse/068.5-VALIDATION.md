---
phase: 068.5
slug: chat-surface-persistent-rendering-in-flight-pulse
status: ready
nyquist_compliant: true
wave_0_complete: false
created: 2026-05-13
last_updated: 2026-05-13
---

# Phase 068.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Finalized 2026-05-13 against the authored PLAN.md files.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 4.1.0 (frontend) — no backend changes per D-068.5-14 RESOLVED |
| **Config file** | `frontend/vitest.config.ts` |
| **Quick run command** | `cd frontend && npm test -- --run -t "Phase 068.5"` |
| **Full suite command** | `cd frontend && npm test -- --run` |
| **Estimated runtime** | ~30 seconds (frontend suite is 136/140 GREEN baseline from Phase 068; 4 pre-existing MessageItem failures are NOT this phase's surface and stay flagged) |

---

## Sampling Rate

- **After every task commit:** Run quick run command (phase-scoped tests).
- **After every plan wave:** Run full suite command.
- **Before `/gsd:verify-work`:** Full suite must be at no-worse-than the 136/140 GREEN Phase 068 baseline + the new GREEN tests from Plans 01/02.
- **Max feedback latency:** ~30 seconds.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 068.5-01-01 | 01 | 0 | CHAT-RESILIENCE-01 | — | Wave 0 RED stubs land for throttle + streamsCache + provider hydrate/MERGE/cross-state/skeleton extensions | unit (RED → GREEN cycle) | `npm test -- --run -t "Phase 068.5"` returns FAIL on first run, PASS by end of Plan 01 | ❌ Plan 01 Task 1 creates | ⬜ pending |
| 068.5-01-02 | 01 | 1 | CHAT-RESILIENCE-01 | — | DIY trailing-edge throttle with `.flush()` (~15 LOC) | unit | `npm test -- --run -t "Phase 068.5 — makeThrottle"` | ❌ Plan 01 Task 2 creates | ⬜ pending |
| 068.5-01-03 | 01 | 1 | CHAT-RESILIENCE-01 | T-068.5-02, T-068.5-03 | streamsCache.ts: LRU eviction + QuotaExceededError fallback + version-drift recovery + parse-failure graceful empty | unit | `npm test -- --run -t "Phase 068.5 — streamsCache"` | ❌ Plan 01 Task 3 creates | ⬜ pending |
| 068.5-01-04 | 01 | 1 | CHAT-RESILIENCE-01 | T-068.5-05 | Synchronous hydrate inside Zustand store factory (Pitfall 1/8); `reconcileError` state slot added; `bucketsBySurface` shape preserved (L-068.5-03 grep gate) | unit | `npm test -- --run -t "Phase 068.5 — hydrate"` | ❌ Plan 01 Task 4 modifies | ⬜ pending |
| 068.5-01-05 | 01 | 1 | CHAT-RESILIENCE-01 | — | MessageSkeleton component + MessageList empty-state ternary (D-068.5-11/12); `data-testid="message-skeleton"` for the gate | unit | `npm test -- --run -t "Phase 068.5 — MessageList skeleton"` | ❌ Plan 01 Task 4 creates | ⬜ pending |
| 068.5-01-06 | 01 | 1 | CHAT-RESILIENCE-01 | — | Throttled-write subscription (useEffect #4) + flush-on-thread-switch in setViewingThread; ChatArea.tsx:134 `clearMessages()` DELETED (Finding #5 cold-render fix) | unit (regression + grep) | `npm test -- --run streamsProvider` + grep gates listed in PLAN 01 Task 5 acceptance criteria | ❌ Plan 01 Task 5 modifies | ⬜ pending |
| 068.5-02-01 | 02 | 0 | CHAT-RESILIENCE-01 | — | Wave 0 extension RED stubs for pulse-class gating (7 tests) + retry-banner flow (2 describe blocks) | unit (RED) | `npm test -- --run -t "Phase 068.5 — pulse class gating"` + `npm test -- --run -t "Phase 068.5 — retry banner"` | ❌ Plan 02 Task 1 creates | ⬜ pending |
| 068.5-02-02 | 02 | 2 | CHAT-RESILIENCE-01 (SC#3) | T-068.5-09 | `@keyframes brandPulse` registered in BOTH `index.css` + `tailwind.config.js`; `animate-brandPulse` applied to Bot icon when `runStatus === 'streaming'` (literal value; NOT `running` / `queued`); enum-mismatch grep guard returns 0 | unit | `npm test -- --run -t "Phase 068.5 — pulse class gating"` (7 GREEN) | ❌ Plan 02 Task 2 modifies | ⬜ pending |
| 068.5-02-03 | 02 | 2 | CHAT-RESILIENCE-01 (SC#4) | T-068.5-06, T-068.5-07, T-068.5-08 | Silent-1s-then-banner retry wrap around `loadMessages` body; MERGE 3-clause filter byte-identical inside wrap (L-068.5-02 grep gate); AbortError not retried; banner not surfaced on first failure | unit | `npm test -- --run -t "Phase 068.5 — retry banner"` (both describes GREEN) | ❌ Plan 02 Task 3 modifies | ⬜ pending |
| 068.5-02-04 | 02 | 2 | CHAT-RESILIENCE-01 (SC#4) | T-068.5-08 | Retry banner in ChatArea (Pattern S2 — sibling of fallbackNotice; amber-400 chrome; Retry + dismiss buttons; testid present; role=status + aria-live=polite) | unit (DOM) | banner-render test inside Phase 068.5 — retry banner describes | ❌ Plan 02 Task 3 modifies | ⬜ pending |
| 068.5-02-05 | 02 | 3 | CHAT-RESILIENCE-01 (SC#5 + SC#6) | — | Chrome MCP UAT (SC#5): cold-cache F5 → 38KB thread → ≤100ms cached paint + ≤1s reconcile + no blank intermediate state. 5/5 lived-experience cycles (SC#6): mirrors Phase 067.5 precedent; flips BUG-260513-01 `folded → closed` at `/gsd:complete-milestone`. SC#4 banner UX network-blocking sanity also captured. | manual (Chrome MCP) | Chrome MCP runbook in Plan 02 Task 4 (checkpoint:human-verify); results recorded in `068.5-VERIFICATION.md` | ⚠️ manual | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

These test files / extensions land FIRST (RED) at the start of each plan so the executor implements against deterministic gates. After Plan 01 Task 1 and Plan 02 Task 1 both ship, `wave_0_complete: true` is set in the frontmatter.

### Plan 01 Wave 0 (new test files + provider extensions)
- [ ] `frontend/src/__tests__/lib/throttle.test.ts` — NEW; trailing-edge + .flush() + new-window-after-flush cases (Vitest fake timers)
- [ ] `frontend/src/__tests__/lib/streamsCache.test.ts` — NEW; serialize round-trip + LRU eviction + L-068.5-03 shape invariant + version drift + QuotaExceededError fallback + parse-failure graceful empty
- [ ] `frontend/src/__tests__/providers/streamsProvider.test.tsx` extension — APPEND new describes at end: hydrate, hydrate-respects-Branch-D-3 (it.each per surface), MERGE-filter-survives, cross-state precedence, throttled-write timing, setViewingThread flush, clearMessages-deletion-grep-marker
- [ ] `frontend/src/__tests__/components/chat/MessageList.test.tsx` — NEW or extension; skeleton-on-empty + skeleton-clears-on-first-message

### Plan 02 Wave 0 (test extensions only)
- [ ] `frontend/src/__tests__/components/chat/MessageItem.test.tsx` extension — APPEND `describe("Phase 068.5 — pulse class gating on runStatus")` block: 7 tests covering all 5 enum values + undefined + Resume button mutual exclusivity (the 4 pre-existing MessageItem failures stay flagged as Phase 068 baseline, NOT this plan's surface)
- [ ] `frontend/src/__tests__/providers/streamsProvider.test.tsx` extension — APPEND retry-banner describes: silent-retry-then-banner-on-second-failure + AbortError-not-retried

*Existing infrastructure: vitest 4.1.0 + React Testing Library + `_build_mock_supabase` fixture + Phase 068 substrate. No framework install required.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Cold-cache F5 paint timing (<100ms cache, <1s reconcile, 38KB thread) | CHAT-RESILIENCE-01 SC#5 | Requires real Chrome + Performance API marks against the live dev server; happens-before semantics aren't reproducible in JSDOM | Chrome MCP runbook in Plan 02 Task 4; `localStorage.clear()` → F5 → navigate to dissertation thread → capture Performance.mark deltas; record in `068.5-VERIFICATION.md` |
| 5/5 lived-experience UAT cycles (switch A→X→A while X mid-streaming, repeat 5x; cached content survives every cycle) | CHAT-RESILIENCE-01 SC#6 | Mirrors Phase 067.5 precedent — bucket-survival under real SSE streaming can't be JSDOM-mocked at the cycle level | Chrome MCP cycle runbook in Plan 02 Task 4; flips BUG-260513-01 `folded → closed` at `/gsd:complete-milestone` |
| Pulse keyframe aesthetic feel (subtle "breathing", not "blinking") | CHAT-RESILIENCE-01 SC#3 (visual quality) | Subjective animation tuning — opacity/scale/duration values need eyeball verification against Aether Intelligence Deep Midnight design feel | Captured during Plan 02 Task 4 Chrome MCP exercise; tune values in `index.css @keyframes brandPulse` if needed and update VERIFICATION.md |
| Retry banner network-blocking exercise | CHAT-RESILIENCE-01 SC#4 (banner UX) | Requires real Chrome DevTools Network blocking — JSDOM tests cover the state-transition logic but not the user-visible dismissable affordance | Plan 02 Task 4 banner-UX sub-runbook (right-click block URL → observe 1s silent retry → observe banner → dismiss → retry) |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify (auto tasks) or are documented manual checkpoints (Plan 02 Task 4 — `checkpoint:human-verify` per planner template; precedent from Phase 068 Plan 04 Task 3 accepted same shape).
- [x] Sampling continuity: no 3 consecutive tasks without automated verify. Plan 02 Task 4 is one manual checkpoint sandwiched between auto Tasks 3 and 5.
- [x] Wave 0 covers all MISSING references (6 NEW test files/extensions across Plans 01 + 02).
- [x] No watch-mode flags in commands (`--run` flag enforces single-pass).
- [x] Feedback latency < 30s.
- [x] `nyquist_compliant: true` set in frontmatter (test stubs are first-task in BOTH plans; the gates are explicit).
- [ ] `wave_0_complete: true` will be flipped after Plan 01 Task 1 + Plan 02 Task 1 both ship (Plan 01 Task 6 owns the Plan 01 flip; Plan 02 Task 5 owns the Plan 02 flip).

**Approval:** ready (planner-finalized 2026-05-13 against the authored Plans 01 + 02).
