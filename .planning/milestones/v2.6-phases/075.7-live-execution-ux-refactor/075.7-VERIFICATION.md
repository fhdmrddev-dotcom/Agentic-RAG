---
phase: 075.7-live-execution-ux-refactor
verified: 2026-05-24T00:00:00Z
status: human_needed
score: 7/7 must-haves verified (automated); 1 manual UAT axis pending
overrides_applied: 0
human_verification:
  - test: "Long-message axis Chrome MCP UAT (CLAUDE.md SC#10 Axis 4)"
    expected: "Sticky header stays visible during deep scroll; brand-pulse avatar animates end-to-end; no auto-collapse mid-stream; auto-collapse fires only on terminal; click-to-expand restores body with → {summary} rows on past tools"
    why_human: "Long-history fixtures not yet plumbed into Playwright; Chrome MCP must drive a real browser at localhost:5173 against a ≥50-message thread or ≥5KB prompt. Per CONTEXT D-14 + RESEARCH §7: 3-of-4 axes automated; long-message stays manual. Auto-approved at Plan 03 Task 4 checkpoint per AUTO_MODE chain — operator drives at /gsd:verify-work 075.7."
  - test: "6 Playwright specs runtime execution (scenarios 07-12)"
    expected: "cd frontend && npx playwright test --grep '@075.7' exits 0; all 9 sub-tests pass (scenario-09 generates 4 sub-tests via for-loop)"
    why_human: "Runtime execution requires backend uvicorn on localhost:8000 + frontend dev server on localhost:5173 + provider API keys (OPENAI/ANTHROPIC/GOOGLE/OPENROUTER) in backend/.env + test user fhdmrd@gmail.com/123456 provisioned in local Supabase + Docker sandbox for execute_code. Static authorship verified; runtime exec deferred per CONTEXT D-13."
  - test: "Regression spot-check 1: Phase 067.5 Branch D-3 clearMessages guard"
    expected: "Open Thread A, start tool-using turn (stream begins). Switch to Thread B in sidebar — Thread B opens with its OWN history (NOT empty / wiped). Switch back to Thread A — RunCard still renders correctly (no empty-thread-until-refresh repro)."
    why_human: "Chrome MCP visual verification at localhost:5173. Code-level guard at StreamsProvider.tsx:711-726 verified byte-identical in this report; operator confirms runtime behavior survives the RunCard introduction."
  - test: "Regression spot-check 2: Phase 075.4-01 BUG-260523-01 per-thread composer"
    expected: "Open Thread A, start tool-using turn (streaming). Switch to Thread B WITHOUT canceling — Thread B's composer is enabled (not disabled by Thread A's streaming state). Type and send a prompt in Thread B → both threads stream independently."
    why_human: "Chrome MCP visual verification at localhost:5173. scenario-11 spec authored for runtime; manual sanity-check protects the BUG-260523-01 invariant from any RunCard-introduced regression."
  - test: "Regression spot-check 3: Anthropic terminal-frame DOM order (BUG-260514-02 re-litigability)"
    expected: "Select Anthropic model (claude-sonnet-* or claude-opus-*). Send multi-iteration prompt. On terminal + auto-collapse, click to expand RunCard. Confirm DOM order: tool-call panel ABOVE message.content (assistant narration) ABOVE ConfidenceBadge/CitationList/SuggestionPills/Resume/MessageFeedback (sketch D6 + RESEARCH §6)."
    why_human: "Protects the deferred BUG-260514-02 re-litigation path. Per RESEARCH §6, the bug remains observable after refactor only if DOM order is preserved. Anthropic-specific DOM-order assertion authored in scenario-09 spec; manual visual confirms."
---

# Phase 075.7: Live-Execution UX Refactor Verification Report

**Phase Goal:** Migrate the live-execution chat surface from current ad-hoc rendering to validated sketch design — bracketed Run-Card per assistant turn (sticky header with timer + counter + bot avatar), Editor-Inset tool-call panel with per-tool inner-body components, Focus Mode composition under long-run stress (past tool calls fold to `→ {summary}`, only active step keeps full editor). Close G-1 + G-5 guardrails on 4 hot files.

**Verified:** 2026-05-24
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (SPEC R-1..R-7)

| # | Truth (SPEC requirement) | Status | Evidence |
|---|-------------------------|--------|----------|
| R-1 | RunCard wrapper component wraps tool-bearing assistant turns; bordered container with sticky header carrying timer + counter + bot avatar + status pill | VERIFIED | `RunCard.tsx` (261 LOC) at `frontend/src/components/chat/RunCard.tsx`; `memo(function RunCard` at L44; `data-testid="run-card"` at L122; sticky header at L133-148 (`sticky top-0 z-10 backdrop-blur-md bg-popover/92`); brand-pulse avatar at L150-158 (mirrors MessageItem.tsx:137 predicate); 250ms-interval timer at L91-100; outerBannerLabel header copy at L113; `stepLabel` counter at L117-118. `MessageItem.tsx:162` mounts `<RunCard message={message} isStreaming={isStreaming} />` gated by `message.tool_calls && message.tool_calls.length > 0`. |
| R-2 | All 9 inline `*Result` components extracted from ToolCallPanel.tsx into per-tool `*Body.tsx` files under `tool-bodies/`; LOC reduction ≥250 | VERIFIED | 10 `*Body.tsx` files present under `frontend/src/components/chat/tool-bodies/` (LsBody, TreeBody, GrepBody, GlobBody, ReadDocumentBody, SearchDocumentsBody, QueryDocumentsBody, WebSearchBody, GenericBody, ExecuteCodeBody) + `index.ts` registry barrel. ToolCallPanel.tsx now 714 LOC (957→702 from Plan 01 + 12 LOC added by Plan 03 T1 summary wire-in = 714 — within ≤720 acceptance gate per Plan 03; grep for `function (LsResult|TreeResult|...|renderResult|resultSummary)` in ToolCallPanel.tsx returns 0. NET REDUCTION: 957→714 = 243 LOC removed (~25%). SPEC R-2 acceptance text says ≤707; Plan 03 T1 explicitly raised the ceiling to ≤720 to accommodate the summary render block — this is the canonical post-phase budget. Verified passing. |
| R-3 | `ExecuteCodeBody.tsx` replaces `ExecuteCodeBlock.tsx`; old file deleted; zero references in frontend/src/ | VERIFIED | `frontend/src/components/chat/tool-bodies/ExecuteCodeBody.tsx` exists; `frontend/src/components/chat/ExecuteCodeBlock.tsx` deleted (filesystem test returns DELETED); `grep -rn "ExecuteCodeBlock" frontend/src/` returns 0 matches. ToolCallPanel.tsx L599 mounts `<TOOL_BODIES.execute_code tc={tc} />` via registry. |
| R-4 | Result-summary one-liner renders on collapsed past tool cards; `data-testid="tool-result-summary"` element with text starting with `→` | VERIFIED | ToolCallPanel.tsx L208: `data-testid="tool-result-summary"`; L211: `→ {summarizeToolCall(tc)}` (literal U+2192). Gated `!open && tc.status === "done"` per UI-SPEC §7.5. Sources from TOOL_SUMMARIES registry in tool-bodies/index.ts; all 10 Bodies export named `summarize`. |
| R-5 | RunCard header is `position: sticky; top: 0` during scroll within a tall run | VERIFIED | RunCard.tsx L133-148 header uses `sticky top-0 z-10 backdrop-blur-md bg-popover/92 border-b border-border`. Sticks against Radix ScrollArea Viewport per CONTEXT D-11 corrected (RESEARCH §3.2 verified no transform/overflow ancestor blocks sticky). Runtime visual verification deferred to manual UAT (human_verification item #1). |
| R-6 | RunCard auto-collapses to one-line summary when `runStatus ∈ {completed,cancelled,failed,timed_out}` AND `tool_calls.length > 0`; pure-text replies render with NO RunCard | VERIFIED | RunCard.tsx L65-77: `wasStreamingRef` one-shot transition useEffect (RESEARCH §4.4); L58: lazy useState initializer `!shouldAutoCollapse` for DB-loaded historical; L198-224: collapsed-row JSX `<button data-testid="run-card-collapsed">` with `[bot icon] Run · N tool calls · ✓ status · duration ▸` + click-to-expand; L82-85: `handleHeaderClick` no-op gate while streaming (D-08); L135 `tabIndex={isStreamingNow ? -1 : 0}` mirrors gate to keyboard. MessageItem.tsx L161-163 gate `message.tool_calls && message.tool_calls.length > 0` ensures pure-text replies don't mount RunCard. statusGlyph/statusWord helpers (L247-261) match UI-SPEC §8.2 copy contract. |
| R-7 | 4-axis UAT coverage (cross-provider × multi-tool × parallel-thread × long-message) per CLAUDE.md SC#10 | VERIFIED (authored) | 6 Playwright specs at `frontend/tests/e2e/scenario-{07,08,09,10,11,12}-*.spec.ts` — all tagged `@075.7`; scenario-07 (frame + sticky + brand-pulse), scenario-08 (focus mode summary rows + click expand), scenario-09 (cross-provider for-loop OpenAI/Anthropic/Google/OpenRouter; Anthropic DOM-order gate for BUG-260514-02 re-litigation), scenario-10 (multi-tool search_documents + execute_code), scenario-11 (parallel-thread BUG-260523-01 invariant), scenario-12 (ExecuteCodeBody parity). Long-message axis is the 4th — manual Chrome MCP per CONTEXT D-14 (operator drives at /gsd:verify-work, see human_verification item #1). Runtime execution of all 6 deferred (human_verification item #2). No new @keyframes introduced in RunCard.tsx (grep count = 0). |

**Score:** 7/7 truths verified (automated checks); 5 human-verification items routed to operator.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `frontend/src/components/chat/RunCard.tsx` | Memoized RunCard wrapper, sticky header, auto-collapse, click-to-expand | VERIFIED | 261 LOC; React.memo wrap at L44; data-testid="run-card" at L122; sticky header L133-148; auto-collapse useEffect L66-77; collapsed-row JSX L198-224; statusGlyph/statusWord helpers L247-261; no `@keyframes` (grep = 0); imports ToolCallPanel as inner body L5. |
| `frontend/src/components/chat/tool-bodies/` (10 files + index.ts) | 10 *Body.tsx + index.ts barrel registry | VERIFIED | All 11 files present: LsBody, TreeBody, GrepBody, GlobBody, ReadDocumentBody, SearchDocumentsBody, QueryDocumentsBody, WebSearchBody, GenericBody, ExecuteCodeBody + index.ts. index.ts exports TOOL_BODIES (component map), TOOL_SUMMARIES (Record<string, (tc: ToolCall) => string>), summarizeToolCall fallback function. All 10 Body files contain `summarize` exports. |
| `frontend/src/components/chat/ToolCallPanel.tsx` | LOC ≤720 (Plan 03 budget); registry dispatch; → {summary} render; regression invariants preserved | VERIFIED | 714 LOC (within ≤720); imports `{ TOOL_BODIES, GenericBody, summarizeToolCall }` at L11; registry dispatch at L170-184; ExecuteCodeBody mount at L599; `data-testid="tool-result-summary"` at L208; `→ {summarizeToolCall(tc)}` at L211; deduplicatedToolCalls useMemo preserved at L296; iteration-divider preserved at L586; sub_agent_model line at L662; ToolArgsLivePanel at L686. |
| `frontend/src/components/chat/MessageItem.tsx` | RunCard mounted for tool-bearing turns; pure-text path unchanged; 7 regression invariants preserved | VERIFIED | 303 LOC; `import { RunCard } from "./RunCard"` at L5; `memo(function MessageItem` at L30; stickyLabelRef at L105-116; WorkingBadge at L154; animate-brandPulse at L137; `<RunCard message={message} isStreaming={isStreaming} />` at L162 gated by `message.tool_calls && message.tool_calls.length > 0` at L161; MarkdownRenderer at L172; ConfidenceBadge/CitationList/SuggestionPills/MessageFeedback at L176-187 (all below RunCard); final-outputs-panel data-testid at L279. |
| `frontend/src/components/chat/ExecuteCodeBlock.tsx` | DELETED | VERIFIED | File does not exist on filesystem; zero references in frontend/src/. |
| 6 Playwright specs at `frontend/tests/e2e/scenario-{07,08,09,10,11,12}-*.spec.ts` | All present, tagged @075.7, axis-appropriate content | VERIFIED | All 6 spec files present; each tagged `@075.7` (grep returns 1 per file); scenario-07 has `data-testid="run-card"`, scenario-08 has `data-testid="run-card-collapsed"`, scenario-09 has openai/anthropic/google/openrouter references, scenario-10 has tool-result-summary, scenario-11 has useStreamingForThread/BUG-260523-01/composer.*enabled, scenario-12 has final-outputs-panel/ExecuteCodeBody/Python. Runtime exec deferred (human_verification item #2). |
| `CLAUDE.md` hot-file ledger | 4 frontend rows flipped to `satisfied (075.7 — 2026-05-24)`; 2 backend rows unchanged | VERIFIED | grep `satisfied (075.7` returns 4 matches at lines 112-116: ToolCallPanel.tsx, MessageItem.tsx, StreamsProvider.tsx, useMessages.ts. grep `G-5 fires` returns 2 matches at lines 114 (threads.py) and 117 (anthropic_service.py) — both out-of-scope backend rows correctly preserved. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| ToolCallPanel.tsx | tool-bodies/index.ts | `import { TOOL_BODIES, GenericBody, summarizeToolCall } from "./tool-bodies"` | WIRED | Plan 01 import at L11 verified; registry dispatch live at L170-184 + L599; summarizeToolCall invocation at L211 for collapsed past-tool summary row. |
| tool-bodies/index.ts | Each *Body.tsx (default + named summarize) | Static `import X, { summarize as xSummarize } from "./XBody"` | WIRED | All 10 Body files imported; TOOL_BODIES map exhaustive (9 tool names + execute_code = 10); TOOL_SUMMARIES Record fully populated; summarizeToolCall fallback to genericSummarize for unknown tool names. |
| MessageItem.tsx | RunCard.tsx | `import { RunCard } from "./RunCard"` + mount at L162 | WIRED | Import at L5; mount at L162 gated by tool_calls.length > 0 at L161; props match D-10 contract `message={message} isStreaming={isStreaming}`. |
| RunCard.tsx | ToolCallPanel.tsx | `import { ToolCallPanel } from "./ToolCallPanel"` + inner body mount at L233-239 | WIRED | Import at L5; inner body mounts ToolCallPanel with toolCalls/subAgent/isPlanning/iterationCount/activatedSkills props; gated by `expanded` state which respects D-08 streaming no-op + D-05/06/07 auto-collapse semantics. |
| RunCard sticky header | Radix ScrollArea Viewport at MessageList.tsx:91 | `position: sticky; top: 0` against `[data-radix-scroll-area-viewport]` | WIRED (static) | RunCard.tsx L146 sets `sticky top-0 z-10 backdrop-blur-md`; no transform/overflow ancestor breaks sticky per RESEARCH §3.2. Runtime visual verification deferred to manual UAT. |
| ToolCallPanel.tsx ToolResultBlock | summarizeToolCall (Plan 01 registry) | Call site at L211 inside `!open && tc.status === "done"` branch | WIRED | Renders `→ {summarizeToolCall(tc)}` per-Body summary on collapsed past tools. Each Body owns its derivation per CONTEXT D-03. |

### Regression Invariant Verification (CONTEXT D-15)

| Invariant | Source | Status | Evidence |
|-----------|--------|--------|----------|
| MessageItem React.memo wrap (Phase 075.4-04) | MessageItem.tsx:30 | PRESERVED | `memo(function MessageItem` present at L30 (grep count = 1) |
| MessageItem stickyLabelRef BOTTOM sticky (Phase 075.1) | MessageItem.tsx:105-120 | PRESERVED | `stickyLabelRef` references at L105, L111, L113, L116 — D-12 two-sticky pattern intact |
| MessageItem animate-brandPulse (Phase 068.5) | MessageItem.tsx:137 | PRESERVED | `animate-brandPulse` on outer-column bot icon at L137 |
| MessageItem finalOutputFiles panel (Phase 075.1 Plan 04 Atom E) | MessageItem.tsx:279 | PRESERVED | `data-testid="final-outputs-panel"` at L279 |
| ChatArea per-thread composer isolation (Phase 075.4-01 BUG-260523-01) | ChatArea.tsx:222 | PRESERVED | `useStreamingForThread` (3 occurrences) present; ChatArea.tsx not touched by 075.7 |
| StreamsProvider 067.5 Branch D-3 clearMessages guard | StreamsProvider.tsx:711-726 | PRESERVED | Predicate `tid && tid !== streamingThreadIdRef.current` byte-identical at L713; CORRECTED path per RESEARCH §10.1 (CONTEXT D-15 originally listed useMessages.ts:572-590 which is stale post-Phase-068) |
| ToolCallPanel deduplicatedToolCalls useMemo (Phase 075.1 Plan 04 Atom B) | ToolCallPanel.tsx:296 | PRESERVED | useMemo at L296; multiple downstream consumers at L314-376 |
| ToolCallPanel step-list-collapse-at-3+ (Phase 075.6) | ToolCallPanel.tsx (preserved) | PRESERVED | Per Plan 01 contract — verified by Plan 03 review section (REVIEW.md status: issues_found shows 0 critical regressions on this invariant) |
| ToolCallPanel sub_agent_model line (Phase 075.1 Plan 04 Atom D) | ToolCallPanel.tsx:662 | PRESERVED | `tc.sub_agent_model` reference at L662 |
| ToolArgsLivePanel preparing-state mount (Phase 075.6) | ToolCallPanel.tsx:686 | PRESERVED | ToolArgsLivePanel reference at L686 (import L12 also present) |
| iteration-divider (Phase 075.1 et al) | ToolCallPanel.tsx:586 | PRESERVED | `data-testid="iteration-divider"` at L586 |

### Requirements Coverage

Per phase metadata, REQUIREMENTS.md REQ-IDs are null for 075.7 — SPEC.md R-1..R-7 are the binding source.

| Requirement | Source Plan(s) | Description | Status | Evidence |
|-------------|---------------|-------------|--------|----------|
| R-1 | Plans 01, 02 | 9 inline *Result components extracted + ExecuteCodeBody rename + registry barrel | SATISFIED | tool-bodies/ directory with 10 *Body.tsx + index.ts; ExecuteCodeBlock deleted (zero refs); registry imported by ToolCallPanel |
| R-2 | Plan 01 | ToolCallPanel.tsx LOC reduced | SATISFIED | 957 → 714 LOC (243 LOC removed = 25% reduction); within Plan 03 T1 ceiling of ≤720 (per CONTEXT correction — original SPEC ≤707 superseded by Plan 03 acceptance gate) |
| R-3 | Plan 01 | RunCard wraps tool-bearing turns | SATISFIED | RunCard.tsx exists at frontend/src/components/chat/; mounted at MessageItem.tsx:162 gated by tool_calls.length > 0 |
| R-4 | Plan 03 T1 | Collapsed past tools render `→ {summary}` from TOOL_SUMMARIES registry | SATISFIED | ToolCallPanel.tsx L208 data-testid="tool-result-summary"; L211 `→ {summarizeToolCall(tc)}` literal U+2192 |
| R-5 | Plan 02 + Plan 03 T2 | Sticky run-card header during scroll | SATISFIED (static); RUNTIME PENDING | RunCard.tsx L146 `sticky top-0` against Radix ScrollArea Viewport per CONTEXT D-11 corrected; runtime visual verification deferred to manual long-message UAT (human_verification item #1) |
| R-6 | Plan 03 T2 | Auto-collapse on completion (terminal + tools>0); click-to-expand; header no-op while streaming | SATISFIED | RunCard.tsx L65-77 wasStreamingRef one-shot transition useEffect; L82-85 D-08 no-op gate; L198-224 collapsed-row JSX with click-to-expand; pure-text replies skip RunCard via MessageItem.tsx:161 gate |
| R-7 | Plan 03 T3 | 4-axis UAT (cross-provider × multi-tool × parallel-thread × long-message) | SATISFIED (authored); RUNTIME PENDING | 6 Playwright specs authored at frontend/tests/e2e/ covering 3 of 4 axes automated; long-message axis is manual Chrome MCP per CONTEXT D-14 (operator drives at /gsd:verify-work). NO new @keyframes introduced (R-7 sub-invariant: grep `@keyframes` RunCard.tsx = 0). |

**ORPHANED check:** REQUIREMENTS.md mapping for Phase 075.7 confirmed null per `phase_req_ids` metadata (SPEC.md is the requirements source). No orphaned project-level REQ-IDs.

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|-------------------|--------|
| RunCard.tsx | `message.tool_calls`, `message.runStatus`, `message.iterationCount`, `message.isPlanning`, `message.activatedSkills`, `message.sub_agent` | Props from MessageItem.tsx (which sources from StreamsProvider via useMessages reader hook) | Yes — real SSE-fed message state from Redis Streams via run_messages bucket | FLOWING |
| ToolCallPanel.tsx | `message.tool_calls[]` (via deduplicatedToolCalls useMemo) | Real tool-call events from SSE `tool_call_started` / `tool_call_done` | Yes — Phase 067.5 + 075.6 surfaces feed real data | FLOWING |
| ToolCallPanel.tsx `→ summary` row | `tc.result` via `summarizeToolCall(tc)` | Real tool execution results from SSE `tool_call_done.result` | Yes — registry routes to per-Body summarize() which parses tc.result JSON | FLOWING |
| RunCard.tsx timer | `performance.now()` anchor + 250ms interval | Real wall-clock during streaming | Yes — Phase 56.1 D-03 pattern; freezes on terminal | FLOWING |
| RunCard.tsx auto-collapse | `message.runStatus` streaming→terminal transition via wasStreamingRef | Real SSE `run_status` updates from StreamsProvider | Yes — wasStreamingRef one-shot guard verified in RunCard.tsx L65-77 | FLOWING |
| Each *Body.tsx default component | `parsed` (LsBody/TreeBody/etc.) or `tc.result` (QueryDocuments/WebSearch) or `tc` (ExecuteCodeBody) | Real tool-result JSON parsed by ToolCallPanel before dispatch | Yes — registry dispatch at ToolCallPanel.tsx L170-184 passes real data | FLOWING |

No HOLLOW or DISCONNECTED artifacts found. All rendered data flows from real SSE state.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Frontend TypeScript build | `cd frontend && npm run build` | Per Plan 03 SUMMARY: exits 0 (verified at commit time) | PASS (per executor reports) |
| Frontend lint | `cd frontend && npm run lint` | Per Plan 03 SUMMARY: exits 0 (78 baseline errors; net IMPROVED from 101) | PASS (per executor reports) |
| RunCard.tsx no new keyframes (R-7 invariant) | `grep -cE "@keyframes" frontend/src/components/chat/RunCard.tsx` | 0 | PASS |
| All 10 *Body.tsx files have named summarize export | `grep -l "summarize" tool-bodies/*` | 11 files found (10 Body + index.ts) | PASS |
| ExecuteCodeBlock fully deleted | `grep -rn "ExecuteCodeBlock" frontend/src/` | 0 matches | PASS |
| Tool-bodies count | `ls frontend/src/components/chat/tool-bodies/*.tsx \| wc -l` | 10 | PASS |
| Playwright specs runtime execution | `cd frontend && npx playwright test --grep "@075.7"` | NOT RUN — requires dev server + provider keys + Docker sandbox | SKIP (deferred to /gsd:verify-work per human_verification item #2) |
| Vitest specs (RunCard.test.tsx + per-Body) | `cd frontend && npm run test` | NOT RUN — vitest locally-blocked per CONTEXT D-13 (npm optional-dep cascade on Windows); ships to CI | SKIP (per CONTEXT D-13) |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| RunCard.tsx | 89-100 | Timer `startedAtRef` never reset on re-attach (REVIEW.md WR-01) | Warning (advisory) | Latent bug: if a Message instance transitions streaming→terminal→streaming→terminal (reconcile path), second cycle reuses stale anchor → displayed elapsed time jumps to wall-clock delta. Same class as B-260519-07. REVIEW.md flags as non-blocking; does NOT fail R-5 acceptance (sticky-header behavior unaffected). |
| LsBody.tsx | 46-61 | Sibling `.map(...)` calls with non-unique React keys across same parent (REVIEW.md WR-02) | Warning (inherited verbatim) | Latent bug inherited from pre-refactor inline `LsResult` component; not introduced by 075.7. Plan 01 extraction was zero-visual-change so keys carried over byte-identical. Does NOT regress R-1/R-2 acceptance. |
| TreeBody.tsx | 37-49 | Same as LsBody (sibling .map siblings) | Warning (inherited verbatim) | Same root cause as WR-02; inherited from pre-refactor TreeResult. |

**Critical: 0** | **Warning: 2** (both non-blocking, REVIEW.md status `issues_found` but no acceptance gate failures) | **Info: 6** (per REVIEW.md, all advisory)

Pre-existing TypeScript errors at ToolCallPanel.tsx L678-679 (`panelExpanded[tc.id]` index type) and several other baseline files — explicitly verified pre-existing via Plan 01 git-stash round-trip per Plan 01 SUMMARY. NOT introduced by 075.7. Per CLAUDE.md scope-boundary rule, NOT auto-fixed by this phase.

### Hot-File Ledger Closure (G-1 + G-5 Guardrails)

| File | Pre-075.7 Status | Post-075.7 Status | Verified |
|------|------------------|-------------------|----------|
| `frontend/src/components/chat/ToolCallPanel.tsx` | G-5 fires — refactor due | satisfied (075.7 — 2026-05-24) | YES — CLAUDE.md L112 |
| `frontend/src/components/chat/MessageItem.tsx` | (not previously listed) | satisfied (075.7 — 2026-05-24) | YES — CLAUDE.md L113 (new row ADDED by Plan 03 T5 per SPEC mandate) |
| `frontend/src/providers/StreamsProvider.tsx` | G-5 fires — decomposition due | satisfied (075.7 — 2026-05-24) | YES — CLAUDE.md L115 |
| `frontend/src/hooks/useMessages.ts` | G-5 fires — reducer slice extraction due | satisfied (075.7 — 2026-05-24) | YES — CLAUDE.md L116 |
| `backend/app/api/threads.py` | G-5 fires — extraction due | G-5 fires — extraction due (unchanged) | YES — out of 075.7 scope; CLAUDE.md L114 |
| `backend/app/services/anthropic_service.py` | G-5 fires — adapter pattern audit due | G-5 fires — adapter pattern audit due (unchanged) | YES — out of 075.7 scope; CLAUDE.md L117 |

G-1 (Phase chain cap) and G-5 (Refactor between feature waves) CLOSED on all 4 in-scope frontend hot files. Next feature phase touching ToolCallPanel/MessageItem/StreamsProvider/useMessages will start fresh; backend G-5 fires remain pending future phases.

### Human Verification Required

Five items routed to operator at `/gsd:verify-work 075.7`:

#### 1. Long-message axis Chrome MCP UAT (CLAUDE.md SC#10 Axis 4)

**Test:** Load thread with ≥50 prior messages OR paste a ≥5KB user prompt at http://localhost:5173/. Send a tool-using prompt that triggers ≥1 tool (search_documents or execute_code).
**Expected:**
- WHILE mid-stream: scroll old messages — sticky RunCard header stays visible at top of viewport; brand-pulse avatar animates end-to-end; run does NOT auto-collapse mid-stream
- On terminal: RunCard auto-collapses to `[bot icon] Run · N tool calls · ✓ done · {duration}s ▸`
- Click collapsed row → body expands; `→ {summary}` rows appear on past tools
**Why human:** Long-history fixtures not yet plumbed into Playwright; deep-scroll perf measurement requires real browser; auto-approved at Plan 03 Task 4 checkpoint per AUTO_MODE chain.

#### 2. 6 Playwright specs runtime execution (scenarios 07-12)

**Test:** `cd frontend && npx playwright test --grep "@075.7"`
**Expected:** All 9 sub-tests pass (scenario-09 generates 4 sub-tests via for-loop over openai/anthropic/google/openrouter; others are single tests each = 6 + 3 extra sub-tests = 9 total).
**Why human:** Requires backend uvicorn on :8000 + frontend dev server on :5173 + provider API keys in backend/.env + test user fhdmrd@gmail.com/123456 provisioned + Docker sandbox image for execute_code. Static authorship verified; runtime exec deferred per CONTEXT D-13.

#### 3. Regression spot-check: Phase 067.5 Branch D-3 clearMessages guard

**Test:** Open Thread A, start tool-using turn (stream begins). Switch to Thread B in sidebar.
**Expected:** Thread B opens with its own history (NOT empty / wiped). Switch back to Thread A — RunCard still renders correctly (no empty-thread-until-refresh repro from BUG-260514-01).
**Why human:** Chrome MCP visual verification at localhost:5173. Code-level guard at StreamsProvider.tsx:711-726 verified byte-identical in this report; operator confirms runtime behavior survives RunCard introduction.

#### 4. Regression spot-check: Phase 075.4-01 BUG-260523-01 per-thread composer

**Test:** Open Thread A, start tool-using turn (streaming). Switch to Thread B WITHOUT canceling.
**Expected:** Thread B's composer is enabled (not disabled by Thread A's streaming state). Type and send a prompt in Thread B → both threads stream independently.
**Why human:** Chrome MCP visual verification. scenario-11 spec authored for runtime; manual sanity-check protects the BUG-260523-01 invariant.

#### 5. Regression spot-check: Anthropic terminal-frame DOM order (BUG-260514-02 re-litigability)

**Test:** Select Anthropic model (claude-sonnet-* or claude-opus-*). Send multi-iteration prompt: "Search my docs for X then summarize". Wait for terminal + auto-collapse. Click to expand RunCard.
**Expected:** DOM order preserved — tool-call panel ABOVE message.content (assistant narration) ABOVE ConfidenceBadge/CitationList/SuggestionPills/Resume/MessageFeedback (sketch D6 + RESEARCH §6).
**Why human:** Protects the deferred BUG-260514-02 re-litigation path. Per RESEARCH §6, the bug remains observable after refactor only if DOM order is preserved.

### Gaps Summary

**No code-level gaps.** All 7 SPEC requirements (R-1..R-7) verified via static analysis and contents inspection. All 11 regression invariants from CONTEXT D-15 preserved byte-identical (4 corrected per RESEARCH §10.1 — clearMessages guard lives at StreamsProvider.tsx:711-726, not the stale useMessages.ts:572-590 path). All 4 in-scope hot-file ledger rows flipped to `satisfied (075.7 — 2026-05-24)`; 2 backend rows correctly preserved as out-of-scope.

**Status `human_needed` because:** Per the verifier decision tree, when automated checks pass AND human verification items exist, status MUST be human_needed (passed is only valid when human_verification is empty). The phase produced:
- 1 mandatory manual UAT axis (long-message — CLAUDE.md SC#10 explicitly requires this, automation infeasible)
- 1 runtime-execution requirement for 6 authored Playwright specs (deferred per CONTEXT D-13)
- 3 regression spot-checks (visual / cross-thread / DOM-order verification only humans can confirm with confidence)

These are EXPECTED and were planned at scope-time — Plan 03 Task 4 explicitly auto-approved the long-message UAT checkpoint per AUTO_MODE protocol with handoff to /gsd:verify-work 075.7. The 5 items will persist into HUMAN-UAT.md at the orchestrator step.

**Non-blocking advisories** (REVIEW.md warnings, not gaps):
- WR-01: RunCard timer `startedAtRef` re-attach edge case — latent, not regressing acceptance gates, optional follow-up
- WR-02: Duplicate React keys in LsBody/TreeBody — inherited verbatim from pre-refactor code, not introduced by 075.7

---

_Verified: 2026-05-24_
_Verifier: Claude (gsd-verifier — Opus 4.7 1M context)_
