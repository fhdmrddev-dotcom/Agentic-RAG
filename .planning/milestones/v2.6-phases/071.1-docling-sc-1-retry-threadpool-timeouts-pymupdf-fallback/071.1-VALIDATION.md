---
phase: 071.1
slug: docling-sc-1-retry-threadpool-timeouts-pymupdf-fallback
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-15
---

# Phase 071.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x |
| **Config file** | `backend/pytest.ini` |
| **Quick run command** | `cd backend && . venv/Scripts/activate && pytest tests/integration/test_documents.py tests/integration/test_docling_extractor.py -x -q` |
| **Full suite command** | `cd backend && . venv/Scripts/activate && pytest tests/ -x -q` |
| **Estimated runtime** | ~120 seconds (quick) / ~300 seconds (full) |

---

## Sampling Rate

- **After every task commit:** Run quick run command on the modified file's nearest test surface
- **After every plan wave:** Run full quick run command
- **Before `/gsd-verify-work`:** Full suite must be green AND live UAT (friendly fixture + thesis SC#1) must pass
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

> **Note:** Filled in by gsd-planner during plan generation. Each task gets a row.
> Plan 02 tasks are predominantly manual-only (live UAT) — they live in the Manual-Only Verifications table below.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD     | TBD  | TBD  | RAG-DOCLING-01 (umbrella) | TBD | TBD | TBD | TBD | TBD | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `backend/tests/integration/test_documents.py::TestReextractDocument` — extend with `test_docling_timeout_falls_back_to_pymupdf` (mocked `asyncio.TimeoutError` → asserts PyMuPDF path runs, 202 returned, `pdf_extraction_runs` row has `engine='pymupdf-fallback'`)
- [ ] `backend/tests/integration/test_documents.py::TestReextractDocument` — extend with `test_both_engines_fail_returns_422` (both Docling timeout AND PyMuPDF subprocess fail → asserts 422 + `error_message` populated)
- [ ] `backend/tests/integration/test_docling_extractor.py` — extend with `test_env_knobs_disable_table_structure` + `test_env_knobs_images_scale_override` (monkeypatch env vars, reset `_CONVERTER=None` in fixture, assert singleton re-instantiated with toggled options)
- [ ] `backend/tests/conftest.py` — add `reset_docling_singleton` fixture (sets `docling._CONVERTER = None` before/after the env-knob tests)

*Existing infrastructure (`pytest`, `pytest-asyncio`, `httpx`, supabase mocks) covers all unit + integration surface — no new framework installs needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Friendly-fixture extraction succeeds on Docling primary path | D-071.1-05 | Real PDF + Docling 600MB model + machine-state — not codifiable as pytest | Per CONTEXT.md D-071.1-05: source CC-BY ~10-page paper with ≥2 tables/figures, commit to `backend/tests/fixtures/extraction/friendly_real.{pdf,docx}` with `friendly_real.LICENSE.txt`. Run Chrome MCP at `http://localhost:5173/` under `fhdmrd@gmail.com`: upload PDF + DOCX, hit `/reextract` with `engine='docling'` on each, confirm both complete < 120s, telemetry rows have `error=NULL`, deltas < 20%. |
| SC#1 binding-gate retry on thesis pair | D-071.1-06 (Phase 071 SC#1 carry-forward) | Live thesis PDF + DOCX + Supabase row state — same reason as above | Per CONTEXT.md D-071.1-06: Chrome MCP `/reextract` with `engine='docling'` on `23cc112a` PDF then `911225a6` DOCX. Capture before/after `document_tables` + `document_images` counts via Supabase Studio SQL. Compute `abs(pdf − docx) / max(pdf, docx)`. **Pass:** `tables_delta ≤ 0.20 AND images_delta ≤ 0.20 AND both pdf_extraction_runs rows have error IS NULL`. |
| 071-VERIFICATION.md AFTER COUNTS re-fill + status flip | D-071.1-06 | Human-edited frontmatter | Open `.planning/phases/071-docling-primary-path/071-VERIFICATION.md`, populate AFTER COUNTS table with thesis numbers, flip frontmatter `status: red → green` only if all three pass conditions met. |
| Thread-leak observation (operator awareness, not pass gate) | D-071.1-02 trade-off | Process-level observation; uvicorn restart heals | After any timeout-fallback fires, note in `071.1-SUMMARY.md` whether worker memory remained stable or required restart. Informational only — not a pass gate. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies (or are listed in Manual-Only table)
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers the 3 listed test extensions before Plan 01 ships
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s on quick run
- [ ] `nyquist_compliant: true` set in frontmatter (after planner fills Per-Task Verification Map)

**Approval:** pending
