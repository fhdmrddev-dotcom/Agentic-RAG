---
milestone: "4.2"
milestone_name: "The Connected Knowledge You Can Actually Run"
audited: 2026-09-18
audited_by: claude (orchestrator + gsd-integration-checker subagent; every blocker re-driven by the orchestrator independently)
status: gaps_found
re_audit: true
supersedes:
  previous_audited: 2026-09-16
  previous_status: gaps_found
  previous_head: 46cf36bf6
  previous_phases: [247, 248, 249, 250, 251]
  previous_scores: "requirements 15/26 · integration 16/22 · flows 1/3 · 4 blockers · 9 warnings"
  preserved_verbatim_at: ".planning/v4.2-MILESTONE-AUDIT-260916.md (md5 9bb03d3de56ae0a5165ab62ccfc8ab88)"
  why_superseded: "124 commits and three phases (252, 253, 254) landed after that head, all three built to close its findings. Its gaps_found verdict described a tree that no longer exists."
phases: [247, 248, 249, 250, 251, 252, 253, 254]
base_commit: 1db32c863
head_commit: bb474e5cc
range_stats: "237 commits · 559 files · +79,321 / -4,197 · 2026-09-13 to 2026-09-18"
scores:
  requirements: "25 / 26 satisfied · 0 partial · 1 unsatisfied"
  phases: "8 / 8 closed with a VERIFICATION.md"
  integration: "19 / 19"
  flows: "3 / 3"
gaps:
  requirements:
    - id: "DEBT-06"
      status: "unsatisfied"
      phase: "milestone-wide standing gate (no phase)"
      verification_status: "re-derived from disk 2026-09-18 — 8 of 14 rows unmet"
      claimed_by_plans: ["254-01", "254-02", "254-03", "254-04"]
      completed_by_plans: []
      evidence: >
        Re-derived with yaml.safe_load over every *-VERIFICATION.md in scope, never from
        REQUIREMENTS.md's hand-typed list. UNMET (8): 239 `owed`; 241 key ABSENT; 242 bare
        `False` (not a vocabulary value); 244 frontmatter UNPARSEABLE YAML; 245 NO verification
        file anywhere; 251, 252, 253 each `owed`. ACCOUNTED (6): 238 `complete`, 240 `complete`,
        243 `refused`, 246 `done`, 249 `refused`, 250 `refused`.
        This agrees exactly with REQUIREMENTS.md:331, which is itself honest.
        This is a GOVERNANCE gap, not a wiring break — the gate is working as designed by
        refusing to self-certify. No plan can close it: `done` needs Gemini (cannot be driven
        from here), `refused` needs an operator ruling (REG-03 forbids Claude closing a bus item).
  integration: []
  flows: []
findings:
  - id: "F-1"
    severity: warning
    category: register-integrity
    requirements: []
    evidence: >
      .planning/phases/254-independent-review-of-249-253/254-VERIFICATION.md frontmatter is
      UNPARSEABLE YAML — yaml.safe_load fails at line 5 col 130, a bare colon-space inside the
      unquoted `score:` value ("...completion condition (254-CONTEXT.md D-04/D-05: an operator
      ruling secured...)"). This is the EXACT defect Phase 254 itself reported against Phase 244,
      and that STATE.md was repaired for on 2026-09-17 — authored by the phase that named the
      class, in the same week, and caught by no gate. Fix is one pair of quotes.
  - id: "F-2"
    severity: warning
    category: register-integrity
    requirements: ["REG-01"]
    evidence: >
      .planning/reported-bugs/ carries TWO live duplicate-id clusters — BUG-260528-01 is the
      `id:` of THREE files, BUG-260906-01 of TWO (measured: grep -h '^id:' | sort | uniq -d).
      scripts/check-seeds-register.cjs sweeps .planning/seeds/ only; NO gate sweeps reported-bugs
      for duplicate ids at all. This is REG-01's defect class one register over. Named as
      pre-existing debt in 252-VERIFICATION.md section 5; confirmed still true.
  - id: "F-3"
    severity: warning
    category: register-integrity
    requirements: ["HONEST-03"]
    evidence: >
      BUG-260915-01 (todo-rows-flash-not-ticked-on-a-live-run-after-thread-open.md) reads
      `status: folded` / `folded_into: 252` / `verified_closed_by: null` — the fix IS in the code
      (Flow C Hole 1, verified at TodosSection.tsx:288) but the report was never flipped to
      `closed`. Fix and register out of sync by one field.
  - id: "F-4"
    severity: warning
    category: register-integrity
    requirements: []
    evidence: >
      253-VERIFICATION.md still reads `status: gaps_found` on disk. Its one gap — R2's 13 unfixed
      findings living only inside the review file — IS closed: SEED-290 exists with a re-open
      trigger per finding. The status was never re-flipped, so every mechanical read of the
      milestone sees a gaps_found phase that has no gap.
  - id: "F-5"
    severity: info
    category: audit-method
    requirements: []
    evidence: >
      The 2026-09-16 audit recorded that "247 and 251 use requirements_met". RE-MEASURED, and it
      is wrong in the worse direction: 251 carries NO requirements field in ANY of its four
      summaries. Only 247 (requirements_met) and 253-03 (requirements) carry one; 248 has ZERO
      summary files. Source 3 of the 3-source cross-reference is structurally unavailable for
      7 of 8 phases. Requirements were verified against each phase's VERIFICATION requirements
      table instead, which is what the matrix prescribes for that row.
  - id: "F-6"
    severity: info
    category: register-integrity
    requirements: []
    evidence: >
      `independent_review` takes SIX spellings across the 14 DEBT-06 rows, two of which are in no
      register vocabulary: `complete` (238, 240) and bare `False` (242). Already triaged by
      254-REVIEW-INDEX.md as "two spellings of one state, a bare false"; unfixed. A sweep counting
      done|refused silently omits `complete`.
tech_debt:
  - phase: 247
    items:
      - "W-3 — token_revoked still reads 'Reconnect {name}' (disposed by 252, not re-driven here)"
      - "standing red: sourceComposition.test.tsx 16 failed / 33 passed, in neither count-gate knob (Phase 235 decision)"
  - phase: 248
    items:
      - "G-4 UAT scenario S2 (McpAuthDoor BYO-OAuth, live) still owed — needs a browser and a third-party account"
      - "check-security-advisors.sh is operator-run, not an unskippable CI hook"
      - "migration 181 is NOT applied to cloud — CRED-04's full discharge waits on the next promotion"
  - phase: 249
    items:
      - "MODEL-04 end-to-end in chat — needs a live self-hosted endpoint (undriven)"
      - "MODEL-06 multi-worker observation — not reproducible on this box"
      - "MODEL-09 cloud sweep — one operator click"
      - "BUG-260916-01 — llm-call-timeout above 600s accepted by admin but has no effect; status open"
  - phase: 250
    items:
      - "HONEST-02 per-provider behavioural rows owed; parallel-thread axis owed"
      - "independent_review refused by operator ruling — a refusal is not a pass"
  - phase: 251
    items:
      - "251-REVIEW.md — 2 CRITICAL unfixed: CR-01 (the seeds self-test has no RED arm for its own missing-key check — re-driven, 8/8 arms pass and none covers it), CR-02 (the status enum's change-all-three rule has zero executable enforcement)"
      - "unswept seeds: 134 carry no trigger_when at all, 114 carry prose the sweep cannot match — two figures, never summed"
  - phase: 252
    items:
      - "embedding_service.py:167 and tool_dispatcher.py:970/983 carry B-2's pydantic-into-log class with document text, not credentials — named, untouched, out of scope"
      - "backend/app/api/connectors.py extraction OWED at its seventh landing"
      - "src/components/sources is still not a TARGETS directory entry (SEED-287) — adopting it would adopt the deliberately-red sourceComposition.test.tsx"
  - phase: 253
    items:
      - "253-REVIEW-R2.md — 7 warnings + 6 info, recorded as SEED-290, unfixed"
      - "schema-acl-parity.yml has never executed in CI (wiring confirmed, pipeline undriven)"
  - phase: 254
    items:
      - "254-REVIEW-INDEX.md — 10 findings triaged, 0 fixed, 2 of them criticals (they are 251's CR-01/CR-02 above)"
      - "5 draft *-REVIEW-REFUSAL.md; three (251, 252, 253) still await a ruling or a Gemini review"
  - phase: milestone
    items:
      - "DEBT-06 older arm (239, 241, 242, 244, 245) untouched — 239 carries no bus ask at all"
      - "BUS-246 and BUS-248 open to:operator, BUS-246 three days old"
      - "nothing outside .planning/ reads independent_review — the field DEBT-06 closes on has no gate (M-7)"
nyquist: skipped
nyquist_reason: "workflow.nyquist_validation is false in .planning/config.json"
---

# v4.2 "The Connected Knowledge You Can Actually Run" — Milestone Re-Audit

**Verdict: `gaps_found` — on ONE requirement, and it is a governance gate, not a defect.**

Eight phases, 31 plans, 237 commits, 559 files, `+79,321 / −4,197`, 2026-09-13 → 2026-09-18,
migration head **181**.

⭐ **Every one of the 2026-09-16 audit's four blockers is CLOSED, and both Flow-C holes are closed.**
That audit was right when it was written; phases 252 and 253 fixed the product code and hardened the
gate, and phase 254 added no code at all. **Integration moved 16/22 → 19/19, flows 1/3 → 3/3.**

⛔ **The one thing still open is `DEBT-06`, and no plan can close it.** `done` needs Gemini answering
a bus item — which cannot be driven from this session. `refused` needs an operator ruling — `REG-03`
forbids Claude answering or closing a bus item, and `AGENTS.md` §6.3 forbids a builder ruling on its
own work. **The gate is working by refusing to self-certify.**

⚠ **The prior audit is preserved byte-identical at `.planning/v4.2-MILESTONE-AUDIT-260916.md`
(md5 `9bb03d3de56ae0a5165ab62ccfc8ab88`), not overwritten.**

---

## 1 · The four blockers — re-driven on the current tree

Every row below was measured against `bb474e5cc` by the orchestrator, independently of the
integration checker, with no line number quoted from the 2026-09-16 audit.

| # | Requirements | Verdict | Evidence measured 2026-09-18 |
|---|---|---|---|
| **B-1** | CRED-03, CRED-04 | ✅ **CLOSED** | `scripts/full-schema-supplement.sql` carries **61** `REVOKE` statements; `supabase/full-schema.sql` carries the same **61**. `node scripts/check-schema-acl-parity.cjs` → **exit 0**, `mirrored: 133/133` (61 function + 72 table/column), tail 653 lines, md5 `da9c561634d417ebd289bedf07b75f69`. Self-test **37/37**. The checker additionally drove `check-greenfield-privileges.py` against a **real scratch DB** bootstrapped from `full-schema.sql` alone: `SELECT access_token_ciphertext FROM connector_tokens` as `authenticated` → `InsufficientPrivilegeError`, teardown verified. |
| **B-2** | CRED-01 | ✅ **CLOSED** | `backend/app/services/connector_service.py:618` — `e.errors(include_input=False)`, not the raw `ValidationError`. The rationale is written into the file at `:601-614`. |
| **B-3** | CRED-01 | ✅ **CLOSED** | `connector_service.py:213` defines `ConnectorClientIdRefused`; `:713` raises it from the one write seam the RFC 7591 DCR path also uses. `backend/app/api/connectors.py:1361` catches it **by name, ahead of the generic `ConnectorError` arm** → 422 naming the repair, never echoing the refused value. |
| **B-4** | WATCH-04, WATCH-06 | ✅ **CLOSED** | `frontend/src/components/sources/WatchRowCard.tsx:195` — `onSyncNow: (watch) => Promise<WatchSyncOutcome>` (was `Promise<void>`); `:296` awaits the real result into one derived slot. **`grep -c "0 changes"` → 0** — the literal is gone. A new `__tests__/WatchRowCard.test.tsx` (9 cases) asserts rendered **content**, not presence, and is pinned in **both** count-gate knobs. |

**4 / 4 closed. None reopened. None partially closed.**

---

## 2 · Flows

| Flow | 2026-09-16 | 2026-09-18 | Where it stands |
|---|---|---|---|
| **A — watched source → Library** | broken | ✅ **complete** | The data path was always complete; the break was the reporting step (B-4), now closed. |
| **B — add a model → run it** | complete | ✅ **complete** | `git diff --stat 46cf36bf6..HEAD` over `config.py`, `admin.py`, `ModelRegistryTab.tsx` is **empty** — 252/253/254 touched none of Flow B's surface, so the prior verdict carries forward unchanged. ⛔ The live self-hosted-endpoint leg is **undriven**, not passing. |
| **C — run ends → panel tells the truth** | broken (2 holes) | ✅ **complete** | **Hole 1:** `TodosSection.tsx:288` — `isRunLive` now includes a new `isReconciling` term, closing BUG-260915-01's window. **Hole 2:** `PhaseTimeline.tsx:250` derives `runTerminal` from the **authoritative fetched** `frame.run_status` — never SSE phase status — and passes `runLive` at `:394` into `PhaseCard:436/439`. Zero second-derivation; `WorkspacePanel.tsx` diff empty. |

---

## 3 · Requirements — 25 / 26

| Status | N | Requirements |
|---|---|---|
| **satisfied** | 25 | WATCH-01..08 · CRED-01..04 · MODEL-04..09 · HONEST-01..04 · REG-01..03 |
| **unsatisfied** | 1 | **DEBT-06** |

⚠ **Method note (F-5).** The 3-source cross-reference degrades to 2 sources here, and by more than
the last audit thought. Source 3 (`requirements_completed` in SUMMARY frontmatter) is present in
**1 of 8** phases under any spelling: 247 uses `requirements_met`, 253-03 uses `requirements`, 248
has **zero** SUMMARY files, and **251 — which the prior audit named as a `requirements_met` user —
carries no requirements field in any of its four summaries.** Requirements were verified against each
phase's VERIFICATION requirements table instead.

### DEBT-06, re-derived from disk

Fourteen rows, read with `yaml.safe_load` over each `*-VERIFICATION.md`, never from a hand-typed list:

| Unmet (8) | Reading | Accounted (6) | Reading |
|---|---|---|---|
| 239 | `owed` | 238 | `complete` |
| 241 | key **ABSENT** | 240 | `complete` |
| 242 | bare **`False`** | 243 | `refused` |
| 244 | frontmatter **UNPARSEABLE** | 246 | `done` |
| 245 | **no verification file anywhere** | 249 | `refused` (operator ruling) |
| 251 · 252 · 253 | `owed` ×3 | 250 | `refused` (operator ruling) |

⛔ **Never sum these two figures.** They agree exactly with `REQUIREMENTS.md:331`, which is honest.

---

## 4 · Phases — 8 / 8 verified

| Phase | status | mode | independent_review |
|---|---|---|---|
| 247 Sources & Watches | complete | **peer-reviewed** | — |
| 248 The Credential Boundary | complete | **peer-reviewed** | — |
| 249 The Model You Actually Run | complete | self-verified | refused |
| 250 Run Honesty — the residue | *(no `status:` key — uses `verdict: PASS`)* | self-verified | refused |
| 251 Register Integrity | passed | self-verified | owed |
| 252 Close the v4.2 audit gaps | complete | self-verified | owed |
| 253 The bootstrap artifact… | **gaps_found** (F-4 — the gap IS closed, status never re-flipped) | self-verified | owed |
| 254 Independent review of 249-253 | passed | self-verified | n/a |

---

## 5 · Integration — 19 / 19

All re-driven this session; none inherited. Failing: **0**.

- **Bootstrap / ACL (1-7):** supplement mirrors migration 181 · artifact tail byte-identical ·
  parity gate exit 0 at 133/133 · self-test 37/37 · greenfield harness green on a real scratch DB ·
  `regenerate-full-schema.sh` appends the supplement · advisor step present in
  `docs/DEPLOYMENT-WORKFLOW.md` §5/§6.
- **Credential boundary (8-10):** log sanitised · DCR `client_id` validated at the one seam ·
  refusal → 422.
- **Watch reporting (11-13):** typed outcome · parent returns a real outcome on all three paths ·
  content-based test pinned in both knobs.
- **Panel liveness (14-15):** `isReconciling` term · `runLive` from the authoritative fetch.
- **Registers / gates (16-19):** seeds 297/297, 0 duplicate ids · REG-02 sweep executably wired at
  both touchpoints · ledger + honesty gates exit 0 · CI workflow files reference the right scripts —
  ⛔ **undriven at the pipeline level** (`schema-acl-parity.yml` has never executed).

---

## 6 · What changed since 2026-09-16

| | 2026-09-16 | 2026-09-18 |
|---|---|---|
| Requirements satisfied | 15 / 26 | **25 / 26** |
| Blockers | **4** | **0** |
| Integration | 16 / 22 | **19 / 19** |
| Flows | 1 / 3 | **3 / 3** |
| Phases | 5 | **8** |
| Status | gaps_found | **gaps_found — on DEBT-06 alone** |

---

## 7 · The gap, and the only two doors out of it

`DEBT-06` is the single unsatisfied requirement. **It cannot be closed by inserting a phase** — that
is what Phase 254 already was, and it correctly ticked nothing.

1. **Gemini reviews the three open rows** (251, 252, 253 — `BUS-249`, `BUS-256`, `BUS-257`, all still
   `[OPEN]`, deadline **2026-09-24**). Each answered row moves to `independent_review: done` with the
   verdict in `<phase>-REVIEW-IND.md`.
2. **The operator rules on the three drafted refusals** (`251/252/253-REVIEW-REFUSAL.md`, already
   written and pending). Each ruling moves a row to `refused`. ⛔ A refusal is **not** a pass:
   `verification_mode` stays `self-verified`, and the accepted risk is that a builder read its own work.

The older arm (239, 241, 242, 244, 245) is untouched and **239 carries no bus ask at all**. Closing
the 249-253 arm alone still leaves `DEBT-06` unticked.

**Accepting DEBT-06 as documented debt and closing v4.2 is a legitimate operator decision** — it is
the only requirement open, no wiring is broken, and every flow completes. It must be recorded as a
decision, never as a claim that the reviews ran.
