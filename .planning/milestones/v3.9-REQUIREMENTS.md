> **ARCHIVE — v3.9 shipped 2026-09-04.** Copied from `.planning/REQUIREMENTS.md` at the milestone
> close; the live file is removed so the next milestone starts with a fresh register.
>
> **Final state: 34 ✅ delivered · 3 ⚠ partial · 2 ⛔ shipped-but-never-driven, of 39.**
> Every Status cell was re-derived at the close against phase artifacts — see the Traceability
> section's own note for why that mattered.
>
> ⭐ **ONE CELL WAS CORRECTED AFTER ARCHIVING — 2026-09-04, at the v3.9 production push.**
> `CAT-05` reads ⚠ **Partial** below because *the cloud half of Add-a-connection had never been
> verified*. It was verified that evening, on cloud, minutes after `826ca7022` deployed: the catalog
> renders 13 services with the Popular row and the state filter, `+ Add a connection` is PRESENT on
> `superrag.cloud`, and it opens a form that refuses honestly. **The cell is left as written rather
> than rewritten** — the archive records what was true at the close, and the correction is dated
> beside it. ⚠ `BUG-260810-01` still does NOT close: its own bar is two things, and connecting a
> Popular service on cloud remains undriven.
>
> ⚠ **`LIB-08 / LIB-09 / LIB-10` never appeared in this register at all.** They belong to Phase 219,
> deferred to the Connected Knowledge milestone. Write them into that milestone's requirements file.

---

# Requirements — Milestone v3.9 "Connections: Any Service, Any Tool"

**Defined:** 2026-08-26 via `/gsd:new-milestone`
**Phase numbering:** continues at **210** (v3.8 ended at 209)
**Prior milestone requirements:** archived to `.planning/milestones/v3.8-REQUIREMENTS.md`

---

## The one sentence this milestone is measured against

> A person connects a **service** — not a protocol — sees every tool it offers, grants each one
> individually, and then uses it **by name in chat** and as a **specific step** on the canvas.

⭐ **Nothing is per-vendor.** A connection is `{service identity, auth, discovered tools, per-tool
grants}`. Adding a service adds **rows, not code**. That is the property every requirement below has
to preserve, and the one a reviewer should try hardest to break.

---

## v3.9 Requirements

### Connection model — the foundation everything else rests on

`SEED-207` is a **PREREQUISITE**, not a nice-to-have: while two connection models coexist, every
downstream surface (node face, service mark, filter, chat mention, catalog entry, picker) must
branch, and **each new surface pays the branch again**.

- [x] **CONN-04**: A connection is created against a **service**, and its available actions come from
      that service's own advertised tools — never from a fixed three-verb list
- [x] **CONN-05**: The legacy `send_email` / `create_ticket` / `post_message` capabilities keep
      working unchanged as **one shape among many**, and no longer organise any surface
      ⚠ They must NOT be deleted — measured at `phase_types.py:2311-2322`, they are the only external
      path that works with **no MCP server**
- [x] **CONN-06**: A user can add a service by **pasting its MCP URL**, and its tools are discovered
      and grantable with **zero engineering work on our side**
- [x] **CONN-07**: A user can see, edit and delete a connection's identity, auth and endpoint without
      losing its existing per-tool grants
- [x] **CONN-08**: A connection stores an OAuth-authenticated service — which has **neither a
      capability nor an MCP URL** — without the database refusing the row
      ⚠ Migration 126's `CHECK (capability IS NOT NULL OR mcp_server_url IS NOT NULL)` refuses it today

### Catalog — how a person finds a service

- [x] **CAT-01**: A user browses connections as a **catalog of services** — each with its own mark,
      display name and one-line purpose — and can search it
- [x] **CAT-02**: A **Popular** row offers one-click connect for a curated set, while every other
      service remains reachable through the same generic list
- [x] **CAT-03**: The catalog filters on **connection state** (`All / Connected / Not connected`),
      never on what a connector can do
      ⚠ The competitor study's verdict, verbatim: *"we promoted an attribute to be the browse axis"*
- [x] **CAT-04**: A connected service shows **starter prompt suggestions**, so a fresh connection is
      not a capability with no visible way in
- [ ] **CAT-05**: A user on a **cloud** install can add a connection from Settings → Connections
      (closes `BUG-260810-01`)

### Authentication — BYO first

- [x] **OAUTH-01**: A user can connect a service through an **OAuth authorization-code flow** using a
      **client id and secret they registered themselves**, and the connection works on every
      deployment including self-hosted
- [x] **OAUTH-02**: An expired access token is **refreshed without the user reconnecting**, and a
      revoked or unrefreshable connection says so plainly instead of failing as a tool error
- [x] **OAUTH-03**: OAuth client secrets and refresh tokens are encrypted at rest, org-scoped, and
      readable by no database role that does not need them
      ⚠ This is the exact defect migration 118 closed for `secret_ciphertext` — do not re-introduce
      it by copying an RLS shape from a table with no secret column

### Per-tool permission & approval — the hard prerequisite for chat

- [x] **GRANT-01**: A user grants or denies **each tool of a connection individually**, with reads
      and writes in **one list**, so search can be granted freely while a create is held back
- [x] **GRANT-02**: A connection carries a **default approval posture** the user can set once, which
      individual tools inherit until overridden
- [ ] **GRANT-03**: When the agent calls a tool whose posture requires approval, the run **pauses and
      asks a real person**, naming the service, the tool and the arguments before anything leaves
- [x] **GRANT-04**: A denied or ungranted tool is **refused**, and the refusal names which grant would
      allow it
- [x] **GRANT-05**: Every outbound call made through a connection writes an **audit receipt** naming
      the service, tool, actor and outcome

### Chat surface — connections as platform assets

- [x] **CHAT-05**: A user adds **any connected service** to a chat thread by name, and the agent
      chooses which of that service's **granted** tools to use
- [ ] **CHAT-06**: A user can see which connected services are active in the current thread and remove
      one without starting a new thread
- [x] **CHAT-07**: A tool call made from chat renders with the **service's own mark and the tool's
      real name**, never as a generic external action

### Workflow steps — a step says what it actually does, everywhere

- [x] **STEP-01**: An author picks a **service and then a named action** when adding an external step —
      no server URL, no hand-written JSON argument object
- [x] **STEP-02**: A step's required arguments are **author-fillable from every launch path**, and a
      run supplies them
      ⚠ Closes BLOCKING `BUG-260826-01` — a `send_email` step can never receive its arguments today
- [x] **STEP-03**: Publish **refuses** a workflow containing a step whose required arguments nothing
      can supply, and names the step and the missing argument (closes `BUG-260826-02`)
- [x] **STEP-04**: The service mark and action name appear on the **run spine and the run surfaces**,
      not only on the builder canvas (`SEED-206`)
- [x] **STEP-05**: A failed external step reports **its own failure reason** on the panel
      (closes `BUG-260826-05`)
- [x] **STEP-06**: The describe-a-workflow door offers the author's **connected services and granted
      tools as vocabulary** before drafting, and a named-but-absent service produces a stated
      **refusal** rather than an invented step (`SEED-208`)

### Knowledge — the one ingestion scenario that dodges the hard problems

- [x] **ATTACH-01**: A user can **pick a specific file from a connected source** and attach it to a
      thread or ingest it, as a deliberate human action
      ⚠ Deliberately human-initiated. **Automatic or background sync is OUT** — see *Out of Scope*

### Operability

- [x] **CONN-09**: An operator can see and control the `live_connectors` kill-switch from the Control
      Room (closes `BUG-260826-04`)
- [ ] **CONN-10**: A scheduled run cannot be silently accepted on an install whose scheduler is off,
      and its default token budget does not guarantee cancellation
      (closes `BUG-260826-06` and `BUG-260826-07`)
- [ ] **CONN-11**: A manual schedule trigger succeeds when `org_id` is null instead of returning a 500
      the browser reports as CORS (closes `BUG-260826-03`)
- [x] **RAG-09**: An embedding-provider failure is reported **as a provider failure**, never as
      "your documents returned nothing"
      ⚠ BLOCKING `BUG-260815-05`. Not connector work — folded because it poisons the trustworthiness
      of every deliverable this milestone produces

### The Library — the document space becomes one home

⚠ **These four IDs existed only in `ROADMAP.md` until 2026-08-29.** The traceability register could
not see Phase 217's requirements at all; they are added here at plan-time per **D-217-03**, using the
ROADMAP's own wording. ⚠ **Phase 217 has FIVE success criteria and FOUR requirement IDs** — SC#5
(one source of selection truth) carries no ID of its own and is tracked by the phase's success
criteria, not by this register.

- [x] **LIB-01**: The document space is called **Library** in the nav and on the page, and
      `IngestionPage.tsx` is renamed `LibraryPage.tsx` in the same commit
      ⚠ The `ActiveView` key stays `"documents"` — it is never printed to a user
- [x] **LIB-02**: A person landing on the Library can **start an upload without hunting for it** — a
      full-width dropzone, the real accepted formats, and the folder it will land in
- [x] **LIB-03**: A file being ingested shows **the six stages the pipeline actually writes**, two of
      them conditional and struck through when skipped
      ⛔ **Never a three-segment bar, never a percentage, never an ETA** — `onUploadProgress` is absent
      from the upload path, so any percentage would be unknowable
- [x] **LIB-04**: A document's detail panel shows **what we already store and never showed**: the
      parsed text, extracted tables **as tables**, image descriptions, its chunks, and the questions
      that found it
      ⚠ The data is in Postgres; **none of it was on the wire** — five new read routes are required

- [x] **LIB-05**: `Library Health` and `Governance` **retire into the Library's Health tab** with
      nothing lost, proven against a declared rename map
      ⚠ Added to this register at the v3.9 close — it had a traceability row but no requirement entry
- [x] **LIB-06**: A search that **could not run** is visible in the analytics as its own segment,
      never indistinguishable from one that ran and found nothing
      ⚠ Added to this register at the v3.9 close — it had a traceability row but no requirement entry
- [x] **LIB-07**: **Per-hit relevance is recorded**, so the detail panel's *average relevance* stops
      saying *not recorded yet*
      ⚠ Added to this register at the v3.9 close — it had a traceability row but no requirement entry

---

## Future Requirements — deferred, each with a re-open trigger

| Deferred | Why | Re-open trigger |
|---|---|---|
| **We own the OAuth application** (Claude.ai's 2-step Connect) | Our redirect URI points at our cloud, so self-hosted installs could not use it. Google's review for restricted Gmail/Drive scopes runs weeks | A hosted-only tier is confirmed, or the first customer blocked by BYO friction |
| **`SEED-209/210/211/212` — connector→KB ingestion** | `SEED-210` measures that synced documents **flatten source ACLs** and source deletions never propagate; `SEED-211` is the M-Files metadata-permissions fork. Shipping auto-ingest without them is a **security defect, not a gap** → *Connected Knowledge* milestone | **The first automatic or background sync from a connected source** |
| **`SEED-013` / `SEED-195` — inbound Open Platform** | Public REST API, webhooks, service accounts, us-as-an-MCP-server. A different product: its own auth model, quotas and versioned public contract; amplifies concurrent load (`SEED-001`) | This milestone ships and an integration partner asks to call us |
| **`SEED-198` — Experts** | A bundle of skills + connections + knowledge scope. **Depends on** this milestone; without the connection model it is a prompt pack | The connection model and per-tool grants are shipped |
| **`SEED-199` — full xyOps canvas grammar** | Two node classes, triggers/constraints as nodes, typed edges. `STEP-04` takes only the connector-mark half | A canvas phase is scoped in its own right |
| **`SEED-193` / `SEED-194` — artifacts & image generation** | *"Connections are about reaching other systems; artifacts are about what the agent produces."* ⚠ `SEED-194`'s real blocker is that `ModelCapability` has **no modality dimension** — model-registry work, unrelated to connectors | Sequenced after this milestone, and after `SEED-185` (the app has no URL router) |

---

## Out of Scope — explicit exclusions

- **Automatic / background sync from a connected source.** See the deferral above. `ATTACH-01` is
  human-initiated precisely because that dodges ACL mirroring, deletion propagation and sync loops.
- **Arbitrary-code / community-node connectors.** A supply-chain surface, excluded **by
  construction** rather than by omission — carried forward from the recorded architecture verdict.
- **A per-vendor adapter treadmill.** If a requirement can only be met by writing code per service,
  it is the wrong requirement. The custom-MCP-URL door is the answer to breadth.

---

## Cross-cutting constraints every phase inherits

1. **UAT scoreboard (CLAUDE.md, MANDATORY).** Any phase touching streaming, the agent loop, provider
   routing or UI state owes the **full 8-row native roster + OpenRouter**, plus multi-tool,
   parallel-thread and long-message rows. Blocked rows are recorded ⛔ with a reason, **never
   silently omitted**.
2. **G-2 (sketch before plan) fires on every user-visible surface here** — the catalog, the grant
   list, the approval moment, the chat service chip, the step picker. `screenshots/` is the
   acceptance bar. ⚠ **They are Claude.ai** (verified by reading the images 2026-08-26), plus one
   xyOps canvas reference.
3. **G-5 hot files already firing in this blast radius**, so a refactor recommendation is owed at
   discuss-phase before the feature: `backend/app/services/harness/phase_types.py`,
   `frontend/src/components/settings/ConnectionsTab.tsx`,
   `frontend/src/components/settings/ConnectionFormPanel.tsx`,
   `frontend/src/components/workflows/ConnectionPicker.tsx`,
   `frontend/src/components/workflows/ExternalActionSection.tsx`.
   **Re-derive each row from git — do not trust the cell.**
4. **D-v2.5-01** — no blocking I/O in an async handler. ⚠ **`mcp_client.py:220` violates this today**
   with a blocking DNS call, while the sibling capability path IS threadpooled. In scope by adjacency.
5. **Every schema change ships as a numbered migration** under `supabase/migrations/`, applied by
   pasting into the Supabase SQL editor — never `db push` / `db reset`.
6. **Deployment-artifact parity (same-commit rule)** — any new env var, seed row or bundled service
   updates the Phase-157 artifacts in the same commit.

---

## Traceability

Filled by `/gsd:new-milestone`'s roadmapper (2026-08-26). **39 requirements, every one mapped to
exactly one phase — no orphans, no duplicates.**

⭐ **EVERY STATUS CELL BELOW WAS RE-DERIVED AT THE v3.9 CLOSE (2026-09-04)** against phase
`VERIFICATION.md` / `SUMMARY.md` artifacts and the reported-bugs register — not copied forward, and
not inferred from a phase being closed. Before that pass **32 of 39 rows read `Planned` while their
phases were shipped and closed**, and the close ARCHIVES this table, so it would have been wrong at
exactly the moment it became permanent. This is the same finding the milestone audit's GAP 2 recorded
for `ROADMAP.md`'s progress table, one file over — and it would have been the **third consecutive
milestone** to close on a stale register.

⚠ **Was 32 rows until 2026-08-29**, when `LIB-01`..`LIB-04` were added at Phase 217's plan-time
(**D-217-03**) because they existed only in `ROADMAP.md`; `LIB-05`..`LIB-07` got rows the same day
when Phase 218 was absorbed into **217.1**. ⚠ **LIB-05..07 still had no requirement ENTRY in the
body of this file until the v3.9 close** — a row in the table and an entry in the register are two
different things, and only the entry carries a checkbox.

**Counts by phase:** 210 → 4 · 211 → 3 · 212 → 6 · 213 → 5 · 214 → 6 · 215 → 3 · 216 → 5 ·
217 → 4 · 217.1 → 3.

**Verdict: 34 ✅ delivered · 3 ⚠ partial · 2 ⛔ shipped-but-never-driven, of 39.**

| REQ-ID | Phase | What the phase must make true | Status |
|---|---|---|---|
| **CONN-04** | Phase 211 | A connection is created against a service; actions come from advertised tools | ✅ **Shipped** — mig 127's `service_id`; 211-05's SC#3 negative fence swept both surface trees and was driven RED against three plants in real source |
| **CONN-05** | Phase 211 | The three legacy verbs keep working as one shape among many and organise nothing | ✅ **Shipped** — `connectors/descriptors.py` makes a capability advertise a descriptor byte-compatible with a discovered tool; the verbs organise no surface |
| **CONN-06** | Phase 212 | Paste an MCP URL — tools discovered and grantable with zero engineering | ✅ **Shipped & driven** — 212 drove a real remote MCP server; **222 re-drove four real servers live**, observing all four `kind` values |
| **CONN-07** | Phase 212 | See / edit / delete a connection without losing its per-tool grants | ✅ **Shipped & driven** (212 verification, in-browser) |
| **CONN-08** | Phase 211 | An OAuth-authenticated service row is accepted by the database (mig 126 CHECK) | ✅ **Shipped** — mig 127 replaces mig 126's shape CHECK with identity-mandatory + shapes-mutually-exclusive; proven downstream by 215's real Google rows |
| **CAT-01** | Phase 212 | Browse connections as a searchable catalog of services | ✅ **Shipped & driven** |
| **CAT-02** | Phase 212 | A Popular row of one-click connects inside the same generic list | ✅ **Shipped & driven** — ⚠ regressed twice inside 213's window (a logo flip broke `shapeForService`; a connected GitHub still offered *Connect*), both fixed at `7f36b505` / `56423bb2` |
| **CAT-03** | Phase 212 | Filter on connection STATE, never on what a connector can do | ✅ **Shipped** — the `All / Connected / Not connected` axis; `capability` retired as the browse axis (`SEED-207`) |
| **CAT-04** | Phase 216 | Starter prompt suggestions on a connected service | ✅ **Shipped** (216-04, `ChatArea.tsx` empty-state pills) — ⚠ not independently driven |
| **CAT-05** | Phase 212 | Add a connection from Settings → Connections on a cloud install | ⚠ **PARTIAL — the CLOUD half was never verified.** Local shipped and driven; `BUG-260810-01` remains `status: folded`, `verified_closed_by: null`. **Re-open trigger: the v3.9 production push** |
| **OAUTH-01** | Phase 215 | Authorization-code flow with a customer-registered client id/secret | ✅ **Shipped & driven live** (real Google consent) — reworked by **225** (opaque 43-char state handle, Redis-held secret) and re-driven in a real browser 2026-09-03 |
| **OAUTH-02** | Phase 215 | Silent refresh; a revoked/unrefreshable connection says so plainly | ✅ **Shipped** — lease-locked refresh (`refresh_claimed_until`, 2-worker concurrency test), `invalid_grant` → `status="revoked"`. ⚠ Never driven against a real provider revocation |
| **OAUTH-03** | Phase 215 | Client secrets and refresh tokens encrypted at rest and org-scoped | ✅ **Shipped** — AES-256-GCM `enc:v1:`; mig 129 REVOKEs both ciphertext columns from `anon` / `authenticated` |
| **GRANT-01** | Phase 213 | Grant or deny each tool individually, reads and writes in one list | ✅ **DRIVEN** (2026-08-28, live app + live Postgres) — GitHub renders 44 tri-state rows; search filters to one; the empty state is honest |
| **GRANT-02** | Phase 213 | A connection-level default approval posture tools inherit | ✅ **DRIVEN** — an override persists to `tool_grants`, *"Use the default"* removes the key (44 → 43) and the row falls back. ⚠ **D-2: the "You changed this" marker tracks key PRESENCE, not authorship** — mig 128 backfills a key onto every capability row |
| **GRANT-03** | Phase 213 | The run pauses and asks a person, naming service / tool / arguments | ⚠ **PARTIAL — the pause is real, the SERVICE is never named.** Hold, tool, arguments and refuse-arm all driven on both shapes. `_external_action_clause` fills the service slot from `config.capability`: an MCP row omits it, and a capability row reads *"run post_message through post_message"* — a tautology. The service name sits on the connection row and is not resolved (D-1) |
| **GRANT-04** | Phase 213 | A denied or ungranted tool is refused, naming the grant that would allow it | ✅ **DRIVEN — both reasons, both shapes** (`not_granted` / `posture_denied`; a real Slack step stopped by Gate 5.5, closing `BUG-260827-02`'s run half) |
| **GRANT-05** | Phase 213 → surfaced in **223** | Every outbound call writes an audit receipt | ✅ **DRIVEN** — `external_action_sent` carrying `capability`, `connection_id`, `destination_host`, `tool_name`, `raw_status`; `user_id` non-null across all 9 audit kinds. Phase 223 built the record surface |
| **CHAT-05** | Phase 216 | Add any connected service to a thread by name; the agent picks a granted tool | ✅ **Shipped & driven** — the connectors chat drive is green and **non-vacuous** (both tools, a real file) |
| **CHAT-06** | Phase 216 | See and remove the services active in the current thread | ⚠ **PARTIAL — the armed set is stored NOWHERE.** Chips show and dismiss, and a response does not disarm; **F5 does, silently.** No thread-connection table, no column on `threads`, and `active_connector_ids` is a request field never persisted — so no audit record of a turn's connectors exists either (`BUG-260902-03`). Also fixed in-window: turning every connector OFF asked for all of them (`37fb46ada`) |
| **CHAT-07** | Phase 216 | A chat tool call renders with the service's mark and the tool's real name | ✅ **Shipped** — `ConnectionMarkGlyph` + the real tool label in `ToolCallPanel`; re-worded by 224 into `@/lib/toolNames` |
| **STEP-01** | Phase 214 | Pick a service then a named action — no URL, no hand-written JSON | ✅ **Shipped** — the raw-JSON surface was **deleted**, not hidden (214-07, net −319 L on `ExternalActionSection`) |
| **STEP-02** | Phase 214 → **214.1** | Required arguments author-fillable from every launch path | ✅ **Shipped — OPERATOR-DRIVEN 2026-08-28 18:00.** ⚠ **It failed twice first:** 214 shipped no way to DECLARE an input (`BUG-260828-02`), then 214.1's declare half hit a golden run that supplies none (`BUG-260828-10`). All three blockers — `-260826-01`, `-260828-02`, `-260828-10` — now `closed`, `verified_closed_by: 214.1`, with run inputs carrying the declared key from a library launch |
| **STEP-03** | Phase 214 | Publish refuses an unsatisfiable step, naming the step and the missing argument | ✅ **Shipped & driven** (G4-4, `ask_undeclared` on phase `act`). ⚠ The on-screen sentence was illegible at the drive (`BUG-260828-04`, `folded` into 214.1 behind a fence); **its re-open trigger stands** — the operator seeing the raw backend diagnostic inside the publish modal on a hard-reloaded build |
| **STEP-04** | Phase 214 | Service mark + action name on the run spine and run surfaces | ✅ **Shipped** — one `StepIdentity` element, four sizes, resolved once and rendered by every child |
| **STEP-05** | Phase 214 | A failed external step reports its own failure reason on the panel | ✅ **Shipped** — the `PhaseCard` failure sentinel narrowed to both-sources-empty |
| **STEP-06** | Phase 214 | The describe door is bound to connected services + granted tools, and refuses | ✅ **Shipped** — `DescribeServicePicker` at the GRANT grain, with `describeServiceMatch` as the one refusal rule |
| **ATTACH-01** | Phase 216 | Pick one specific file from a connected source and attach or ingest it | ✅ **Shipped & driven with a real file** — `GET /connections/{id}/files` + `ConnectedFilePickerModal`. ⚠ A human picking ONE file only: **no ACL mirroring, no deletion propagation, no sync loop** — which is why `SEED-213` stayed in v3.9 while `SEED-209..212` travel with Phase 219 |
| **CONN-09** | Phase 210 | The `live_connectors` kill-switch is visible and controllable in the Control Room | ✅ **DRIVEN in-browser** — flip → DB `"audience": "off"` + receipt; Connections rendered the read-only refusal naming the Control Room; flipped back and the install restored |
| **CONN-10** | Phase 210 | No silent schedule on a scheduler-off install; the default budget does not self-cancel | ⛔ **CODE SHIPPED, NEVER DRIVEN — structurally unreachable on this install.** The door gates on `provenance === "published"` and every visible row is a Shared starter; `workflow_schedules` = 0 rows, and the budget lift fires only on an exact `50_000` while the modal defaults to `500_000` (210 SC#2 / SC#3) |
| **CONN-11** | Phase 210 | A manual schedule trigger succeeds when `org_id` is null | ⛔ **CODE SHIPPED, NEVER DRIVEN** — it depends on CONN-10's unreachable schedule row (210 SC#4) |
| **RAG-09** | Phase 210 | An embedding-provider failure is reported as a provider failure | ✅ **Shipped and roster-driven** — the SC#10 roster was run and **FAILED 5 of 18 rows** (five providers claimed `openai` whenever `embedding_provider` was empty, the default of every env-configured install), was fixed by making `resolve_embedding_endpoint` the single resolution both client and name read from, and re-driven **18/18 honest**, guarded by 19 cases driven RED first. ⚠ Never observed against a real outage; the fix is reviewer-authored with **no independent verifier** |
| **LIB-01** | Phase 217 → 217.1 | The surface is called Library, and the page file is renamed in the same commit | ✅ **Shipped** (217-04) |
| **LIB-02** | Phase 217 → 217.1 | Upload is a real front door — a full-width dropzone with the real formats | ✅ **Shipped** — was ⚠ Partial at 217 (the dropzone shipped on Documents only); closed by 217.1's Ingestion-tab work, proven by `sketchComposition` 47/47 |
| **LIB-03** | Phase 217 → 217.1 | Six ingestion stages in the order the pipeline writes; no percentage, no ETA | ✅ **Shipped** — was ⚠ Partial at 217 (every label `sr-only`, so the strip rendered as six blank boxes); closed by 217.1 |
| **LIB-04** | Phase 217 → 217.1 | The detail panel renders text, tables, images, chunks, and the queries that found it | ✅ **Shipped** — was ⚠ Partial at 217 (the `RETRIEVAL` block and *Average relevance* absent); closed by 217.1-11 + SC#12 |
| **LIB-05** | **Phase 217.1** (⛔ absorbed from 218) | `Library Health` and `Governance` retire into the Library's Health tab with nothing lost | ✅ **Shipped** — the fifth Health tab landed at 217.1-12/13; `KnowledgeHealthPage.tsx` and `GovernancePage.tsx` **DELETED**, and `ChatLayout`'s positional fallback replaced by `UnknownViewFallback` (217.1-14) |
| **LIB-06** | **Phase 217.1** (⛔ absorbed from 218) | A search that **could not run** is its own segment in the analytics | ✅ **Shipped** — `_handle_search_documents`'s except block now writes a `search.query` audit row, so a provider outage is visible rather than indistinguishable from a miss (217.1-11, BE-4) |
| **LIB-07** | **Phase 217.1** (⛔ absorbed from 218) | Per-hit relevance is recorded, so *average relevance* stops saying *not recorded yet* | ✅ **Shipped** — the success path now carries `similarities: {document_id: max_similarity}`; the per-hit similarity `retrieval_service.py` already computed was being thrown away before it reached the analytics (217.1-11, BE-5) |

⚠ **LIB-08 / LIB-09 / LIB-10 were NEVER mapped in this file, and they still are not.** They belong
to **Phase 219**, which the operator **deferred to the Connected Knowledge milestone on 2026-09-04**
together with `SEED-209/210/211/212` — because 219's SC#1 (*"watched on a schedule"*) is word for word
v3.9's own binding re-open trigger for those four security seeds, and shipping auto-ingest without
them *"is not a gap, it is a security defect"*. **The trigger did not fire; it was kept from firing by
moving the feature.** ⚠ **They must be written into the NEXT milestone's `REQUIREMENTS.md`** — a
requirement that exists only in a roadmap heading is invisible to every coverage check, which is
precisely how LIB-01..07 spent this milestone.

**Load-bearing order** (see `.planning/ROADMAP.md` → *The sequencing constraints*):
**211** (`SEED-207`, the connection model) precedes **212 / 214 / 216** · **213** (per-tool approval)
is a HARD prerequisite for **216** (chat) · **CONN-08** (211) precedes **OAUTH-01** (215) ·
MCP-first (212) precedes OAuth (215) · the governed canvas (214) precedes chat (216).

⚠ **Update a row's Status when its phase closes, not at the milestone audit.** This register was
stale from day one for the **third consecutive milestone** — v3.8 closed with seven of twelve phases
carrying no `VERIFICATION.md`, and v3.9 closed with 32 of 39 rows reading `Planned`. The next
milestone's register is worth a per-phase-close gate rather than another warning paragraph.
