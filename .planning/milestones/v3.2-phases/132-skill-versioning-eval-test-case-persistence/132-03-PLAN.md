---
phase: 132-skill-versioning-eval-test-case-persistence
plan: 03
type: execute
wave: 3
depends_on: ["132-02"]
files_modified:
  - frontend/src/types/index.ts
  - frontend/src/lib/api.ts
  - frontend/src/components/skills/SkillTestCasesSection.tsx
  - frontend/src/components/skills/SkillFormDialog.tsx
autonomous: false
requirements: [EVAL-01, VER-01]
user_setup: []

must_haves:
  truths:
    - "From the skill edit surface, a user can add/edit/delete eval test cases and they persist across reload (EVAL-01)"
    - "From the skill edit surface, a user can read the skill's version history list (VER-01)"
    - "The surface is thin/functional only — it does not introduce designed Evals-panel structure that would pre-empt the Phase 137 sketch (operator scope fence)"
  artifacts:
    - path: "frontend/src/components/skills/SkillTestCasesSection.tsx"
      provides: "Thin non-designed test-case editor + version-history read, mounted in the skill detail surface"
      min_lines: 40
    - path: "frontend/src/lib/api.ts"
      provides: "Test-case CRUD + version-list client functions"
      contains: "listTestCases"
  key_links:
    - from: "SkillTestCasesSection.tsx"
      to: "/skills/{id}/test-cases + /test-cases/{id} + /skills/{id}/versions"
      via: "api.ts client functions"
      pattern: "listTestCases|createTestCase|listSkillVersions"
    - from: "SkillFormDialog.tsx (SkillDetailPanel)"
      to: "SkillTestCasesSection"
      via: "mounted for an existing (saved) skill"
      pattern: "SkillTestCasesSection"
---

<objective>
Ship a deliberately thin, non-designed functional surface so the persistence foundation is usable end-to-end: a test-case editor (add/edit/delete) and a read-only version-history list, mounted in the existing skill edit surface.

Purpose: Operator scope decision — 132 ships a usable foundation, NOT the designed Evals panel. The real sketch-gated Skill Evals panel is Phase 137 (G-2). This surface must be functional/plain on purpose and must NOT introduce layout/structure that pre-empts or constrains the 137 design.
Output: api.ts client functions + types, a new `SkillTestCasesSection` component, mounted in SkillDetailPanel for existing skills, verified end-to-end by the operator.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/132-skill-versioning-eval-test-case-persistence/132-CONTEXT.md
@.planning/phases/132-skill-versioning-eval-test-case-persistence/132-RESEARCH.md

<interfaces>
<!-- Contracts the executor builds against. -->

Backend routes (from Plan 02, already live):
- GET    /skills/{skill_id}/test-cases  → TestCaseResponse[]
- POST   /skills/{skill_id}/test-cases  → TestCaseResponse  (body: {prompt, expected_behavior, order_index?, name?})
- PATCH  /test-cases/{case_id}          → TestCaseResponse  (partial body)
- DELETE /test-cases/{case_id}          → 204/200
- GET    /skills/{skill_id}/versions    → SkillVersionResponse[]  (version_number DESC, read-only)

From frontend/src/lib/api.ts:
- `const API_BASE = import.meta.env.VITE_API_BASE_URL` ; existing skill funcs at ~line 1469 (`listSkills`, `createSkill`, `updateSkill`, `deleteSkill`) use `fetch(`${API_BASE}/skills...`, { headers })` with an auth-headers helper — mirror that exact pattern (headers builder + res.json() typed cast).

From frontend/src/types/index.ts — add `TestCase`, `TestCaseCreate`, `TestCaseUpdate`, `SkillVersion` types mirroring the backend Pydantic shapes.

From frontend/src/components/skills/SkillFormDialog.tsx — `SkillDetailPanel` is exported here and used by SkillsPage.tsx:201; it is the inline edit surface for an existing skill (has the skill id). Mount the new section here, only when editing an existing (already-saved) skill.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API client + types for test cases and versions</name>
  <read_first>
    - frontend/src/lib/api.ts (the listSkills/createSkill/updateSkill/deleteSkill block ~line 1469 — headers helper + fetch + typed json pattern)
    - frontend/src/types/index.ts (existing Skill/SkillCreate/SkillUpdate type shapes)
    - backend/app/models/skill_test_case.py + skill_version.py (the response shapes to mirror)
  </read_first>
  <action>
    In `frontend/src/types/index.ts` add: `TestCase` (id, skill_id, user_id, prompt, expected_behavior, order_index, name: string | null, created_at, updated_at), `TestCaseCreate` (prompt, expected_behavior?, order_index?, name?), `TestCaseUpdate` (all optional), and `SkillVersion` (id, skill_id, user_id, version_number, name, description, instructions, source, created_at).
    In `frontend/src/lib/api.ts` add five functions mirroring the existing skill funcs' header/fetch/typed-cast pattern: `listTestCases(skillId): Promise<TestCase[]>` (GET /skills/{id}/test-cases), `createTestCase(skillId, body: TestCaseCreate): Promise<TestCase>` (POST), `updateTestCase(caseId, body: TestCaseUpdate): Promise<TestCase>` (PATCH /test-cases/{id}), `deleteTestCase(caseId): Promise<void>` (DELETE /test-cases/{id}), `listSkillVersions(skillId): Promise<SkillVersion[]>` (GET /skills/{id}/versions). Import the new types into api.ts's type import line.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/types/index.ts` exports TestCase, TestCaseCreate, TestCaseUpdate, SkillVersion.
    - `frontend/src/lib/api.ts` exports listTestCases, createTestCase, updateTestCase, deleteTestCase, listSkillVersions, each hitting the correct path/method.
    - `npx tsc --noEmit` passes (no new type errors).
  </acceptance_criteria>
  <done>Typed client functions for test-case CRUD and version-history reads exist and typecheck.</done>
</task>

<task type="auto">
  <name>Task 2: Thin SkillTestCasesSection mounted in SkillDetailPanel</name>
  <read_first>
    - frontend/src/components/skills/SkillFormDialog.tsx (SkillForm + SkillDetailPanel structure; how an existing skill id is available; the ui/button, ui/input, ui/textarea imports already in use)
    - frontend/src/pages/SkillsPage.tsx:201 (how SkillDetailPanel is mounted)
    - frontend/src/lib/api.ts (the functions from Task 1)
  </read_first>
  <action>
    Create `frontend/src/components/skills/SkillTestCasesSection.tsx`: a plain, functional component taking `skillId: string`. On mount, load test cases (`listTestCases`) and versions (`listSkillVersions`). Render: (a) a simple list of test cases each with an editable prompt + expected_behavior (existing `Input`/`Textarea`/`Button` primitives), an "Add test case" button (`createTestCase`), inline save (`updateTestCase`) and a delete button (`deleteTestCase`), reloading the list after each mutation; (b) a read-only version-history list showing `v{version_number} · {source} · {created_at}` newest-first. Keep it visually minimal — NO new design system structure, NO panel chrome, NO tabs; functional/plain on purpose (operator scope fence — must not pre-empt the Phase 137 sketch-gated Evals panel). Add a short code comment marking it as the thin 132 foundation to be superseded by Phase 137 (PANEL-01, G-2).
    Mount it in `SkillDetailPanel` (in SkillFormDialog.tsx) ONLY when editing an existing/saved skill (a skill id exists) — render below the existing form fields.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/skills/SkillTestCasesSection.tsx` exists (>=40 lines) and uses listTestCases/createTestCase/updateTestCase/deleteTestCase/listSkillVersions.
    - It is mounted in SkillDetailPanel only for an existing (saved) skill with an id.
    - The component introduces no new design-system panel/tab structure (thin/plain — grep shows no new shadcn panel/tabs imports beyond Input/Textarea/Button).
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>A thin functional test-case editor + version-history read is mounted in the skill edit surface and builds clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator end-to-end verification of the thin test-case + version surface</name>
  <action>Operator exercises the thin surface end-to-end: add/edit/delete test cases and confirm they persist across reload, and confirm version history increments on an instructions change but NOT on an enabled/global toggle. Steps in how-to-verify.</action>
  <what-built>The end-to-end thin surface: backend CRUD + version-history routes (Plan 02), client functions, and the SkillTestCasesSection mounted in the skill detail panel. This is the operator-approved usability check that the foundation works (not a design review — the surface is intentionally plain; the designed Evals panel is Phase 137).</what-built>
  <how-to-verify>
    1. Start the backend (your usual uvicorn terminal) and the frontend dev server; log in at http://localhost:5173/ (fhdmrd@gmail.com / 123456) and open the Skills page.
    2. Open an existing skill's edit/detail panel. Confirm the thin "test cases" section appears.
    3. Add a test case (prompt + expected-behavior), save. Reload the page, reopen the skill — the case PERSISTS (EVAL-01 SC#2).
    4. Edit the case's prompt, save, reload — the edit persists. Delete a case, reload — it's gone (EVAL-01 SC#3).
    5. Confirm the version-history list shows at least the backfilled `v1 · backfill` (or a `manual` row). Now change the skill's instructions and save; reopen — a NEW version row appears with an incremented version_number (VER-01 SC#1). Toggle the skill's enabled/global state — NO new version row appears (D-02).
    6. (Owner-scope spot check, optional) Confirm the section only ever shows your own cases/versions.
  </how-to-verify>
  <resume-signal>Type "verified" if test cases persist + edit/delete works + version history increments on instruction change but not on toggle — or describe what broke.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API | The frontend sends the user's bearer token; all owner-scoping is enforced server-side (Plan 02). The UI is a thin client and is not itself a security boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-10 | Information disclosure | UI requesting another user's cases/versions | mitigate | Server enforces `.eq("user_id", …)` on every route (Plan 02, T-132-06); the UI only renders what the owner-scoped API returns. |
| T-132-11 | Tampering | Client forging skill_id/user_id | accept | Server ignores body user_id and scopes by token (Plan 02 T-132-07); no client-side trust. |
| T-132-SC | Tampering | npm installs | accept | No new packages — reuses existing shadcn Input/Textarea/Button + fetch. |
</threat_model>

<verification>
- `npx tsc --noEmit` and `npm run build` pass.
- Operator end-to-end check passes: test cases persist across reload, edit/delete works, version history increments on instruction change but not on toggle.
</verification>

<success_criteria>
- The persistence foundation is usable end-to-end via a thin functional surface (operator scope decision).
- EVAL-01 (define/save/edit/delete cases that persist) and VER-01 (read version history) are observable in the app (ROADMAP SC#1-3).
- The surface introduces no designed structure that would constrain the Phase 137 sketch (G-2).
</success_criteria>

<output>
Create `.planning/phases/132-skill-versioning-eval-test-case-persistence/132-03-SUMMARY.md` when done.
</output>
