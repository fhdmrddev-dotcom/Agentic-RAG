---
phase: 138-run-end-honesty-stretch
plan: 03
type: execute
wave: 2
depends_on: [138-01, 138-02]
files_modified: []
autonomous: false
requirements: [RUN-01]

must_haves:
  truths:
    - "RUN-01a live: a second code-generating run in a reused sandbox thread shows only its own new file(s) — no dead 'Download unavailable' card, no re-surfaced prior-run file"
    - "RUN-01b live: a clean run ending with open todos shows each open item's content suffixed '(run ended — not completed)' in the Workspace TODOS panel; completed items are untouched"
    - "D-04 live: a second clean run on the same thread does NOT double-append the marker"
    - "D-05/LOCK-2 live: a run that hits the iteration cap (cap_paused) with open todos leaves them UNMARKED while paused"
    - "LOCK-1 live: Continuing that cap-paused run to clean completion DOES then mark the still-open todos"
    - "D-14 red line live: a normal Deep run with no open todos + no leftover files behaves exactly as before — no spurious marker text, output files render normally"
    - "SC#10 spirit: the RUN-01b marker behaviour holds on a second provider (shared finalizer/emit path — no provider fork)"
  artifacts: []
  key_links: []
---

<objective>
Live confirmation that both RUN-01 fixes behave honestly in a running app, and that the red line holds (Deep Mode byte-identical, no provider fork). This is the acceptance bar for closing SEED-094 — both bugs (BUG-260626-02 baseline leak + BUG-260626-03 stuck todos) originally ESCAPED automated tests and were caught only in Phase 123's live SC#10 Axis-2 UAT, so a live check is the honest close.

Purpose: Automated tests (138-01, 138-02) prove the unit behaviour and wiring. This checkpoint proves the lived experience and covers the two things unit tests can't cheaply reach: the cap_paused NEGATIVE (D-05 trap) and the Continue-completed positive (LOCK-1), both requiring a real running agent loop.

Output: Operator verdict recorded; on issues, gaps route to a `/gsd:plan-phase 138 --gaps` follow-up.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/138-run-end-honesty-stretch/138-CONTEXT.md
@.planning/phases/138-run-end-honesty-stretch/138-01-SUMMARY.md
@.planning/phases/138-run-end-honesty-stretch/138-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator live-verifies both RUN-01 fixes + the red line + a cross-provider spot check</name>
  <files>none — verification-only plan (no code changes)</files>
  <read_first>
    - .planning/phases/138-run-end-honesty-stretch/138-01-SUMMARY.md (what RUN-01a changed)
    - .planning/phases/138-run-end-honesty-stretch/138-02-SUMMARY.md (what RUN-01b changed + the exact marker text)
  </read_first>
  <what-built>
    - RUN-01a: the `final_output_files` emit is now filtered to content-hashes genuinely new to the run — baseline/leftover sandbox files no longer surface as dead cards.
    - RUN-01b: a run-end reconciler appends `(run ended — not completed)` to each open (pending/in_progress) todo's content on a genuinely-clean run end, at BOTH the first-turn finalizer and the Continue finalizer, gated so cancelled/failed/timed_out/cap_paused runs never trigger it.
  </what-built>
  <action>
    Operator executes Scenarios A–F (detailed in how-to-verify) against the live app and reports a pass/fail verdict per scenario. This is a manual verification gate — the executor writes NO code here; it pauses and resumes only on the operator's signal. Give the operator the steps verbatim, trust their observation, and offer the psycopg2 cross-check queries where noted. On any failure, capture what was observed and route to `/gsd:plan-phase 138 --gaps`.
  </action>
  <how-to-verify>
    Pre-req: local backend running (operator starts uvicorn), dev app at http://localhost:5173/, login fhdmrd@gmail.com / 123456. DB cross-checks use psycopg2 against Postgres :54322 (`SELECT content, status FROM todos WHERE thread_id = '<id>' ORDER BY order_index, created_at`). Sandbox must be enabled (SANDBOX_ENABLED) for the file scenarios.

    Scenario A — RUN-01a leftover exclusion (one thread, two runs):
    1. New chat. Prompt: "Use execute_code to create a bar chart PNG and save it to /sandbox/output/chart1.png." Wait for completion; note the output-file card(s).
    2. SAME thread, next prompt: "Now use execute_code to create a DIFFERENT line chart and save it as /sandbox/output/chart2.png." Wait for completion.
    3. PASS if run 2's final output-file cards show only chart2 (the genuinely-new file) — chart1 is NOT re-listed and there is NO dead "Download unavailable" card. FAIL if chart1 re-appears or any dead card shows.

    Scenario B — RUN-01b open-todo marker (clean completion):
    1. New chat. Prompt something that makes the model write a multi-step todo list AND leave some steps open, e.g.: "Create a todo list with 4 steps for building a report, mark step 1 completed and start step 2, then stop and summarize — do not finish the remaining steps." Let the run END cleanly.
    2. Open the Workspace TODOS panel. PASS if each still-open (pending/in_progress) item's text ends with ` (run ended — not completed)`, the completed step does NOT, and no item's status flipped to completed. Cross-check via psycopg2: open rows carry the suffix in `content`, `status` unchanged.

    Scenario C — D-04 no-stack:
    3. SAME thread as B, send another clean prompt that does NOT touch those todos ("Thanks, just reply OK."). Let it complete. PASS if the previously-marked items still carry exactly ONE `(run ended — not completed)` suffix (not doubled).

    Scenario D — D-05/LOCK-2 cap_paused NEGATIVE, then LOCK-1 Continue positive:
    4. Trigger a run that hits the iteration cap with open todos (a long/complex Deep task that exhausts the iteration budget and pauses as cap_paused with a Continue affordance, leaving todos open). WHILE PAUSED: PASS if the open todos are NOT marked (psycopg2: no suffix). This is the D-05 trap — a paused run hasn't ended.
    5. Click Continue and let it finish cleanly. PASS if the still-open todos NOW carry the `(run ended — not completed)` suffix (LOCK-1 — the continuation finalizer fired).

    Scenario E — D-14 red line (Deep byte-identical):
    6. New chat, normal Deep prompt with NO todos and NO file generation ("Explain what RAG means in two sentences."). PASS if the reply is normal, no todo panel marker text appears anywhere, and behaviour is indistinguishable from before this phase.

    Scenario F — SC#10 cross-provider spot check:
    7. Repeat Scenario B on a DIFFERENT provider than you used in B (switch model to another of OpenAI / Anthropic / Google). PASS if the open-todo marker behaviour is identical (shared finalizer/emit path — no provider fork).
  </how-to-verify>
  <verify>
    <human-check>All six scenarios (A–F) pass as described in acceptance_criteria; psycopg2 cross-checks agree with the browser for B/C/D.</human-check>
  </verify>
  <acceptance_criteria>
    - Scenario A: run 2 emits only genuinely-new files; no re-surfaced prior-run file; no dead "Download unavailable" card.
    - Scenario B: every open todo's `content` ends with the exact suffix ` (run ended — not completed)`; completed items and all `status` values unchanged (browser + psycopg2 agree).
    - Scenario C: no double marker after a second clean run (exactly one suffix).
    - Scenario D: cap_paused run leaves open todos UNMARKED while paused; Continue-to-completion then marks them.
    - Scenario E: a no-todo/no-file Deep run shows zero marker text and normal output — red line intact.
    - Scenario F: marker behaviour identical on a second provider.
  </acceptance_criteria>
  <resume-signal>Type "approved" if all six scenarios pass, or describe which scenario failed and what you observed (route failures to `/gsd:plan-phase 138 --gaps`).</resume-signal>
  <done>Operator recorded a pass/fail verdict for all six scenarios (A–F); on all-pass, SEED-094 is closable; any failure is routed to `/gsd:plan-phase 138 --gaps`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (verification-only plan) | This plan changes no code and adds no attack surface; it validates the mitigations shipped by 138-01/138-02 in a live environment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-08 | Tampering (honesty regression) | live cap_paused / Continue behaviour | mitigate | Scenario D live-exercises the D-05 trap (T-138-06 from 138-02) that unit tests can't cheaply reach — confirms a cap_paused run is NOT marked and a Continue-completed run IS. |
| T-138-09 | Information Disclosure | cross-thread file/todo isolation | mitigate | Scenarios A/B run in owned threads under the test login; RUN-01a's run-scoped accumulator (T-138-01) and RUN-01b's thread-scoped replace (T-138-04) are observed to affect only the acting thread. |
| T-138-SC | Tampering | npm/pip/cargo installs | accept | Verification-only — no packages installed. |
</threat_model>

<verification>
- Operator runs Scenarios A–F against the live app; all six must PASS.
- psycopg2 cross-checks the `todos.content`/`status` for Scenarios B/C/D.
</verification>

<success_criteria>
- All six scenarios pass live: RUN-01a leftover exclusion, RUN-01b marker on clean completion, no-stack, cap_paused negative + Continue positive, Deep red-line, cross-provider parity.
- SEED-094 can be marked closed (both re-open triggers verified fixed live).
</success_criteria>

<output>
Create `.planning/phases/138-run-end-honesty-stretch/138-03-SUMMARY.md` when done (record the operator verdict + any observations).
</output>
