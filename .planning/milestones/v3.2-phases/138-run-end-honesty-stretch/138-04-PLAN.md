---
phase: 138-run-end-honesty-stretch
plan: 04
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - frontend/src/providers/StreamsProvider.tsx
  - frontend/src/providers/StreamsProvider.test.tsx
autonomous: true
requirements: [RUN-01]

must_haves:
  truths:
    - "RUN-01 live-surfacing: on a genuinely-clean run terminal (kind 'done' or 'reader_done'), the Workspace TODOS panel reconciles LIVE — the backend-committed '(run ended — not completed)' marker appears at run-end with NO thread-switch and NO refresh"
    - "Reuse-only: the fix routes through the existing getThreadTodos (GET /threads/{id}/todos) fetch helper + the existing replaceTodosForThread store action — NO new fetch machinery, NO new route, NO new component, NO new package (D-01: the marker rides on content; TodosSection.tsx is untouched)"
    - "Both onTerminal handlers reconcile: the reconcile-path handler (~1446-1530) AND the sendMessage-path handler (~1742-1834) each invoke the single shared _reconcileTodosOnTerminal helper — no copy-paste drift on the G-5 hot file"
    - "Clean-completion gated: _reconcileTodosOnTerminal fetches ONLY on kind 'done'/'reader_done' and short-circuits on 'cancelled'/'error'/'timed_out' — the backend only writes the marker on clean completion, so nothing changed on those kinds (matches the 138-02 two-clause finalizer gate)"
    - "D-v2.5-03 fetch-on-terminal, not SSE-trust: the reconcile always re-fetches on a clean terminal (never gated on whether the live todo_updated SSE was delivered/applied) — Realtime is a best-effort hint, the GET is the source of truth"
    - "Best-effort + additive-only: a failed/slow todos fetch is swallowed (never throws into the terminal handler), the write is thread-keyed so a stale post-switch fetch updates its OWN thread's slot only, and StreamsProvider.tsx changes are pure-additive (no reorder of any existing runStatus-flip / subscription-cleanup line on this G-5 file)"
    - "D-14 red line intact: a normal Deep run with no open todos produces no visible marker text and no panel change (the clean-terminal GET returns the empty/unchanged list; replaceTodosForThread is a no-op empty→empty)"
  artifacts:
    - path: "frontend/src/providers/StreamsProvider.tsx"
      provides: "exported _reconcileTodosOnTerminal(threadId, kind) helper (fetch-on-clean-terminal → replaceTodosForThread, best-effort) + two call sites in the reconcile-path and sendMessage-path onTerminal handlers"
      contains: "_reconcileTodosOnTerminal"
    - path: "frontend/src/providers/StreamsProvider.test.tsx"
      provides: "focused Vitest cases locking the helper contract: 'done'/'reader_done' → fetch + replace; 'cancelled'/'error' → no fetch, no store write; a rejected fetch → no throw"
      contains: "_reconcileTodosOnTerminal"
  key_links:
    - from: "frontend/src/providers/StreamsProvider.tsx onTerminal (reconcile path ~1505-1522)"
      to: "_reconcileTodosOnTerminal"
      via: "fire-and-forget call after the runStatus flip + subscription cleanup (true-terminal path, post transient-reattach return)"
      pattern: "_reconcileTodosOnTerminal\\(threadId, kind\\)"
    - from: "frontend/src/providers/StreamsProvider.tsx onTerminal (sendMessage path ~1797-1818)"
      to: "_reconcileTodosOnTerminal"
      via: "fire-and-forget call after the runStatus flip + subscription cleanup (true-terminal path, post transient-reattach return)"
      pattern: "_reconcileTodosOnTerminal\\(threadId, kind\\)"
    - from: "_reconcileTodosOnTerminal"
      to: "getThreadTodos + replaceTodosForThread"
      via: "GET /threads/{id}/todos then full-state store replace, gated to clean-completion kinds"
      pattern: "getThreadTodos|replaceTodosForThread"
---

<objective>
Close the ONE live-surfacing gap from Phase 138's live UAT (Scenario B / VERIFICATION.md must-have #5): the 138-02 backend reconciler correctly commits the `(run ended — not completed)` marker to the todos table BEFORE the terminal sentinel, but the live Workspace TODOS panel does not reflect it at run-end — the marker only appears after a manual thread-switch/refresh (which triggers the existing `usePanelReconcile` GET). Reconcile the todos panel ON the run-terminal so the marker surfaces live.

Purpose: Deliver the lived-experience honesty the phase set out to ship. The DB already holds the correct marked state by the time the clean-terminal sentinel fires (138-02, DB-verified twice via psycopg2), so a fetch-on-terminal guarantees the marker surfaces live with no refresh — robust regardless of whether the finalize-time `todo_updated` SSE emit is delivered/applied. This is the project's own architecture rule: CLAUDE.md D-v2.5-03 — "Realtime is a best-effort hint, NOT a source of truth — always reconcile via fetch on (re)connect."

Output: One tiny exported helper `_reconcileTodosOnTerminal(threadId, kind)` on `StreamsProvider.tsx` plus one call from EACH of the two onTerminal handlers, reusing the already-present `getThreadTodos` + `replaceTodosForThread`. Additive-only on a G-5 hot file (satisfied at 075.7 — additive is fine). Backend (138-02) is correct and is NOT touched; `TodosSection.tsx` (D-01, renders `content` verbatim) is NOT touched; no new component, route, or package.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-run-end-honesty-stretch/138-CONTEXT.md
@.planning/phases/138-run-end-honesty-stretch/138-VERIFICATION.md
@.planning/phases/138-run-end-honesty-stretch/138-03-SUMMARY.md

<interfaces>
<!-- Exact primitives to REUSE (verified in the tree). Do NOT invent new fetch/store machinery. -->

getThreadTodos (frontend/src/lib/api.ts:957-965) — the GET helper to reuse VERBATIM. Already imported into StreamsProvider.tsx at line 77:
  export async function getThreadTodos(threadId: string, signal?: AbortSignal): Promise<Todo[]>
  GET /threads/{tid}/todos; backend reshapes todo_id -> id (no client mapper needed); returns the FULL marked list.

replaceTodosForThread (StreamsProvider.tsx:2183-2188) — the full-state-replace store action to reuse VERBATIM:
  replaceTodosForThread: (threadId: string, todos: Todo[]) => void
  Body: `next = new Map(s.todosByThread); next.set(threadId, todos)` — keyed by threadId, so a resolved fetch always
  writes ITS OWN thread's slot (not the viewed thread's). This is the SAME action the live SSE handler already calls:
  StreamsProvider.tsx:818-819 `onTodoUpdated: (todos) => useStreamsStore.getState().actions.replaceTodosForThread(threadId, todos)`.

IN-FILE BEHAVIOURAL ANALOG (mirror this exact shape) — onRunCompleted (StreamsProvider.tsx:933-951):
  On a clean HARNESS terminal it fire-and-forgets `getThreadWorkspaceFiles(threadId).then((files) =>
  ...replaceWorkspaceFilesForThread(threadId, files)).catch(() => { /* best-effort, never throw into SSE consumer */ })`.
  KEY: run_completed is HARNESS-ONLY (Deep never emits it — comment at 937-939), which is exactly WHY Deep-run todos
  never reconcile today. The todos fix is the same fire-and-forget shape, but on the Deep onTerminal path, for todos.
  No AbortController is needed (matches this analog): the write is thread-keyed, so a stale post-switch resolve
  updates the correct background-thread slot and never corrupts the viewed thread.

TERMINAL-KIND shape (StreamsProvider.tsx:195) — the union already used by _isTransientStreamEnd:
  kind: "done" | "error" | "cancelled" | "timed_out" | "reader_done"
  Both onTerminal handlers map "done" AND "reader_done" -> runStatus "completed" (the clean-completion kinds).

EXPORT-FOR-TESTS pattern (StreamsProvider.tsx:194 `export async function _isTransientStreamEnd`, :257
  `export async function _reattachAfterTransient`; comment at :300 "export-for-tests pattern") — module-level `_`-prefixed
  helpers are exported ONLY so unit tests import + drive them directly (see StreamsProvider.transient.test.ts:61
  `import { _isTransientStreamEnd, _reattachAfterTransient } from "@/providers/StreamsProvider"`). The new helper follows
  this exact pattern so it is unit-testable without spinning up the full sendMessage/reconcile SSE-replay path.

The two onTerminal handlers to wire (BOTH need the call):
  - Reconcile path: `callbacks.onTerminal = async (kind, errorPayload) => {...}` at ~1446; the true-terminal runStatus flip
    in setMessagesForBucket at ~1505-1516, subscription cleanup ~1519-1522; closure has `threadId`.
  - sendMessage path: `callbacks.onTerminal = async (kind, errorPayload) => {...}` at ~1742; the true-terminal runStatus flip
    at ~1797-1808, subscription cleanup ~1812-1818; closure has `threadId`.
  Both handlers first run the transient-reattach probe and `return` early on a transient re-attach — so code AFTER the
  runStatus-flip block is the TRUE-terminal path (the correct place to call the reconcile).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the exported _reconcileTodosOnTerminal helper + focused Vitest cases</name>
  <files>frontend/src/providers/StreamsProvider.tsx, frontend/src/providers/StreamsProvider.test.tsx</files>
  <read_first>
    - frontend/src/providers/StreamsProvider.tsx (the module-level helpers region ~194-300 — `_isTransientStreamEnd`/`_reattachAfterTransient` export-for-tests shape; the imports ~77-78 that already bring in `getThreadTodos`; the `onRunCompleted` fire-and-forget workspace refetch analog ~933-951; the `replaceTodosForThread` action ~2183-2188; the `onTodoUpdated` live handler ~818-819)
    - frontend/src/lib/api.ts (`getThreadTodos` ~957-965 — the exact signature to reuse)
    - frontend/src/hooks/usePanelReconcile.ts (the AbortController-per-fetch → post-await guard → replace pattern that ALREADY surfaces the marker on thread-switch — the behavioural analog whose only gap is that it never fires at run-terminal)
    - frontend/src/providers/StreamsProvider.test.tsx (whole file — it already `vi.hoisted`-mocks `getThreadTodos` -> `mockGetThreadTodos` at ~45-72, seeds the store in `beforeEach` ~108-128, and imports from `@/providers/StreamsProvider` ~74-76; extend it)
    - frontend/src/__tests__/providers/StreamsProvider.transient.test.ts (~61 — the `import { _isTransientStreamEnd } from "@/providers/StreamsProvider"` direct-drive test pattern to mirror)
    - .planning/phases/138-run-end-honesty-stretch/138-VERIFICATION.md (the gap: must-have #5 failed — marker not surfaced LIVE)
    - .planning/phases/138-run-end-honesty-stretch/138-CONTEXT.md (D-01 marker-rides-on-content / TodosSection untouched; D-14 red line)
  </read_first>
  <behavior>
    - kind "done" → calls getThreadTodos(threadId) exactly once, then replaceTodosForThread(threadId, <fetched list>); todosByThread.get(threadId) equals the fetched list.
    - kind "reader_done" → same as "done" (also a clean-completion kind → reconciles).
    - kind "cancelled" → does NOT call getThreadTodos and does NOT write the store (nothing changed on the backend for a non-clean terminal).
    - kind "error" → same as "cancelled" (no fetch, no write).
    - getThreadTodos rejects → the returned promise resolves without throwing (best-effort); the store is left unchanged.
  </behavior>
  <action>
    Add a module-level `export async function _reconcileTodosOnTerminal(threadId: string, kind: "done" | "error" | "cancelled" | "timed_out" | "reader_done"): Promise<void>` beside the existing exported terminal helpers (`_isTransientStreamEnd` ~194 / `_reattachAfterTransient` ~257), following the same export-for-tests pattern and reusing the union type already used there.

    Behaviour (mirror the `onRunCompleted` fire-and-forget workspace-refetch analog at ~933-951, but for todos on the clean Deep terminal):
    1. Clean-completion gate FIRST: if `kind !== "done" && kind !== "reader_done"` return immediately — no fetch, no write. This mirrors the 138-02 backend two-clause finalizer gate (the marker is only written on a genuinely-clean completion; on cancelled/error/timed_out nothing changed, so there is nothing to surface).
    2. Otherwise await `getThreadTodos(threadId)` and pass the returned list to `useStreamsStore.getState().actions.replaceTodosForThread(threadId, todos)`. ALWAYS fetch on a clean terminal — do NOT gate on whether the local todos store is non-empty or whether the live `todo_updated` SSE was applied (D-v2.5-03: the GET is the source of truth, not the best-effort SSE). The write is thread-keyed by `threadId`, so a resolve that lands after a thread-switch updates that thread's own slot and never corrupts the viewed thread — no AbortController needed (matches the `onRunCompleted` analog).
    3. Best-effort: wrap the fetch+replace in `try/catch` and swallow (a `console.error` is acceptable, matching the fire-and-forget `.catch` shapes at ~1490-1496 / ~942-950) — the helper must NEVER throw so a callers' fire-and-forget invocation cannot reject into the terminal handler.

    Then extend `frontend/src/providers/StreamsProvider.test.tsx`: add `_reconcileTodosOnTerminal` to the existing `import { ... } from "@/providers/StreamsProvider"` line (~75), and add a new `describe("Phase 138-04 — _reconcileTodosOnTerminal live panel reconcile", ...)` block. Reuse the already-hoisted `mockGetThreadTodos` and the store seed in `beforeEach`. Cases (per <behavior>): (a) `mockGetThreadTodos.mockResolvedValue(<marked list>)`, `await _reconcileTodosOnTerminal(THREAD_A, "done")`, assert `mockGetThreadTodos` called once with `THREAD_A` and `useStreamsStore.getState().todosByThread.get(THREAD_A)` deep-equals the marked list; (b) same for `"reader_done"`; (c) `await _reconcileTodosOnTerminal(THREAD_A, "cancelled")`, assert `mockGetThreadTodos` NOT called and `todosByThread.get(THREAD_A)` unchanged; (d) same for `"error"`; (e) `mockGetThreadTodos.mockRejectedValue(new Error("boom"))`, assert `await _reconcileTodosOnTerminal(THREAD_A, "done")` does NOT reject (best-effort) and the store is unchanged. Wrap store reads/writes in `act(...)` where the existing tests do.
  </action>
  <verify>
    <automated>cd frontend && grep -q 'export async function _reconcileTodosOnTerminal' src/providers/StreamsProvider.tsx && npx vitest run src/providers/StreamsProvider.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/providers/StreamsProvider.tsx` contains a module-level `export async function _reconcileTodosOnTerminal` (asserted by the verify grep).
    - `npx vitest run src/providers/StreamsProvider.test.tsx` exits 0 with the new describe block's cases included (the whole file stays green at HEAD).
    - Test proves kind "done" AND kind "reader_done" each call `getThreadTodos(THREAD_A)` once and then set `todosByThread.get(THREAD_A)` to the fetched list.
    - Test proves kind "cancelled" AND kind "error" call getThreadTodos ZERO times and leave the store unchanged (clean-completion gate).
    - Test proves a rejected `getThreadTodos` does NOT cause `_reconcileTodosOnTerminal` to reject and leaves the store unchanged (best-effort / never-throws).
    - The helper reuses `getThreadTodos` + `replaceTodosForThread` only — no new fetch call, no new store action, no new import beyond the already-present `getThreadTodos` (grep shows no added `fetch(` in the helper).
  </acceptance_criteria>
  <done>`_reconcileTodosOnTerminal` exists as an exported module-level helper that fetch-reconciles the todos panel on clean-completion kinds only, is best-effort/never-throws, and is locked by the new Vitest cases in `StreamsProvider.test.tsx`.</done>
</task>

<task type="auto">
  <name>Task 2: Wire _reconcileTodosOnTerminal into BOTH onTerminal handlers (additive-only, fire-and-forget)</name>
  <files>frontend/src/providers/StreamsProvider.tsx</files>
  <read_first>
    - frontend/src/providers/StreamsProvider.tsx (the reconcile-path onTerminal ~1446-1530: transient-reattach probe + `if (reattached) return` ~1466-1504, the true-terminal `setMessagesForBucket` runStatus flip ~1505-1516, the subscription cleanup ~1519-1522; AND the sendMessage-path onTerminal ~1742-1834: transient probe + return ~1764-1795, the true-terminal runStatus flip ~1797-1808, the subscription cleanup ~1812-1818, the workflow-lock clear ~1825; AND the fire-and-forget `.catch` shapes to mirror at ~1490-1496 / ~942-950)
    - frontend/src/providers/StreamsProvider.tsx (the `_reconcileTodosOnTerminal` helper added in Task 1)
    - .planning/phases/138-run-end-honesty-stretch/138-CONTEXT.md (G-5 hot file: additive-only, do NOT reorder finalize/cleanup lines)
    - CLAUDE.md (G-5 hot-file ledger — StreamsProvider.tsx satisfied at 075.7, so additive is fine; D-v2.5-03 reconcile-via-fetch)
  </read_first>
  <action>
    StreamsProvider.tsx is a G-5 hot file (satisfied at 075.7) — add ONLY the two call sites; do NOT reorder or delete any existing runStatus-flip, subscription-cleanup, workflow-lock-clear, or `originalOnTerminal` line. Both call sites go on the TRUE-terminal path, i.e. AFTER the transient-reattach `if (reattached) return` short-circuit (so a transient re-attach never fires a spurious reconcile).

    SITE A — reconcile-path onTerminal (~1446-1530): after the true-terminal `setMessagesForBucket(...)` runStatus-flip block (~1516) and the subscription-cleanup block (~1519-1522), add a single fire-and-forget line `void _reconcileTodosOnTerminal(threadId, kind)` (append a `.catch(() => {})` to match the fire-and-forget shape used for `subscribeToRun(...)` at ~1490-1496 — the helper already swallows, so this is belt-and-suspenders). `threadId` is already in the handler closure. Do NOT await it (must not block terminal teardown).

    SITE B — sendMessage-path onTerminal (~1742-1834): after the true-terminal `setMessagesForBucket(...)` runStatus-flip block (~1808) and the subscription-cleanup block (~1812-1818), add the SAME `void _reconcileTodosOnTerminal(threadId, kind).catch(() => {})` line. `threadId` is already in the `sendMessage` closure. Do NOT await it.

    Both sites call the SAME shared helper (no copy-pasted fetch/replace block — the anti-drift requirement on this G-5 file). The clean-completion gate lives INSIDE the helper (Task 1), so both call sites pass `kind` unconditionally and the helper short-circuits non-clean kinds. Add NOTHING else to the file.
  </action>
  <verify>
    <automated>cd frontend && test $(grep -v '^[[:space:]]*//' src/providers/StreamsProvider.tsx | grep -c '_reconcileTodosOnTerminal(threadId, kind)') -eq 2 && npx vitest run src/providers/StreamsProvider.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Exactly TWO non-comment call sites `_reconcileTodosOnTerminal(threadId, kind)` exist in StreamsProvider.tsx (asserted by the verify `test ... -eq 2`) — one per onTerminal handler.
    - Each call is fire-and-forget (`void ...` / `.catch(...)`) and NOT awaited — verify by reading the diff around ~1516-1522 and ~1808-1818.
    - Each call is placed AFTER the transient-reattach `if (reattached) return` short-circuit (true-terminal path only) — verify by diff position relative to the runStatus-flip block.
    - `npx vitest run src/providers/StreamsProvider.test.tsx` still exits 0 (the module compiles with the new calls; the helper contract stays green).
    - `git diff` on StreamsProvider.tsx shows ONLY added lines around the two handlers (no moved/deleted existing finalize/cleanup line) — additive-only on the G-5 file.
  </acceptance_criteria>
  <done>Both onTerminal handlers fire-and-forget the shared `_reconcileTodosOnTerminal(threadId, kind)` on the true-terminal path; the clean-completion gate lives in the helper; the change is additive-only on the G-5 file and the test suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (StreamsProvider onTerminal) → backend GET /threads/{id}/todos | a read-only fetch of the user's OWN RLS-scoped thread todos, using the run's owning-thread id from the handler closure (not request/user-supplied cross-thread) |
| fetched list → client-side Zustand store (replaceTodosForThread) | in-process store write keyed by threadId; no boundary re-crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-10 | Information Disclosure | GET /threads/{id}/todos on terminal | mitigate | Reuses the EXISTING `getThreadTodos` helper — the same RLS-scoped GET the thread-switch `usePanelReconcile` already calls — with the run's own owning-thread id from the onTerminal closure (never a request- or user-supplied cross-thread id). Backend RLS scopes every todo row to the authenticated user. No new route, no new query, no new external entry point. |
| T-138-11 | Tampering (stale-thread write) | a late fetch resolving after a thread-switch | mitigate | `replaceTodosForThread(threadId, todos)` writes the Map slot keyed by the CAPTURED owning `threadId`, so a fetch that resolves after the user switched threads updates its OWN thread's slot — never the currently-viewed thread's. This is the same per-thread closure discipline (Pitfall 6) used across all panel handlers; no AbortController needed. |
| T-138-12 | Denial of Service (terminal-handler integrity) | reconcile call inside the byte-locked onTerminal on a G-5 hot file | mitigate | Fire-and-forget (`void ... .catch`), never awaited, placed AFTER the runStatus flip + subscription cleanup on the true-terminal path — a failed/slow fetch cannot block or reorder terminal teardown. The helper itself is best-effort (try/catch swallow, never throws). Additive-only (no existing finalize/cleanup line moved). Degrades to "marker not surfaced live" (recoverable by the existing thread-switch reconcile), never a stuck run. |
| T-138-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages — reuses `getThreadTodos`, `replaceTodosForThread`, `useStreamsStore`. No install task, so the package-legitimacy gate does not fire. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/providers/StreamsProvider.test.tsx` passes (helper contract: clean-completion → fetch+replace, non-clean → no-op, reject → no throw).
- Exactly 2 non-comment `_reconcileTodosOnTerminal(threadId, kind)` call sites (one per onTerminal handler); `git diff` is additive-only on the G-5 file.
- Live confirmation that the marker surfaces in the Workspace TODOS panel at run-end with NO refresh (Scenario B), plus the D-14 red-line re-confirm, is covered by plan 138-05 (checkpoint).
</verification>

<success_criteria>
- RUN-01 live-surfacing gap closed: on a genuinely-clean Deep run terminal, the Workspace TODOS panel reconciles live and the `(run ended — not completed)` marker appears at run-end without a thread-switch/refresh.
- The fix is reuse-only (getThreadTodos + replaceTodosForThread), clean-completion-gated, best-effort/never-throws, and additive-only on the G-5 StreamsProvider.tsx — no backend change (138-02 stays correct), no TodosSection.tsx change (D-01), no new component/route/package.
- D-14 red line intact — a normal Deep run with no open todos shows no marker text and no panel change.
</success_criteria>

<output>
Create `.planning/phases/138-run-end-honesty-stretch/138-04-SUMMARY.md` when done.
</output>
