---
phase: 137-skill-evals-panel-ui-panel-01
plan: 07
type: execute
wave: 4
depends_on: [137-01, 137-06]
files_modified:
  - frontend/src/pages/SkillsPage.tsx
  - frontend/src/components/skills/SkillCard.tsx
  - frontend/src/components/skills/PublishGateDialog.tsx
  - frontend/src/components/skills/PublishGateDialog.test.tsx
  - frontend/src/components/skills/SkillFormDialog.tsx
  - frontend/src/components/skills/SkillFormDialog.test.tsx
autonomous: true
requirements: [PANEL-01]

must_haves:
  truths:
    - "The detail panel slims down: SkillEvalSection + SkillTestCasesSection LEAVE the edit dialog; the panel keeps the edit form + a status section + 'Open studio' + counts (D-07)"
    - "The panel's 'Open studio' button is the SOLE 'Open studio' control in the app (the 057 MAP: it lives inside the Detail panel — Plan 06 threads the navigator but adds no second button) (D-06, D-07)"
    - "The panel status section renders the FULL 054-B LifecycleStepper (the SAME shared component the Studio header condenses) — dissolving the 'Publish ready 1/1 vs 0/2 cases' contradiction where the operator first hit it (D-07, D-10)"
    - "The panel derives the stepper's live version number via the SAME shared `deriveLiveVersion` helper (frontend/src/lib/skillVersion.ts, created in Plan 06) the Studio shell uses — one rule, one implementation, two homes (D-07, D-10)"
    - "The panel's counts line reuses the ALREADY-fetched data: cases = listTestCases length, latest-run summary = gate.passed/gate.measured from the same getPublishGate the stepper consumes — no new endpoint call (D-07)"
    - "The 136 PublishGateDialog gains ONE net-new 'Review evals →' link on the unmet branch, wired to onReviewEvals -> Studio·Evals (D-06)"
    - "The Studio is otherwise additive — no Skills-tab redesign beyond the deliberate detail-panel slim-down (PANEL-01 exclusion)"
  artifacts:
    - path: "frontend/src/components/skills/SkillFormDialog.tsx"
      provides: "Slimmed detail panel: sections removed, shared LifecycleStepper (version via shared deriveLiveVersion helper) + Open studio (sole button) + counts (gate.passed/measured summary) added"
    - path: "frontend/src/components/skills/PublishGateDialog.tsx"
      provides: "'Review evals →' link on the unmet branch"
  key_links:
    - from: "SkillFormDialog.tsx (SkillDetailPanel)"
      to: "LifecycleStepper (shared, full variant) + deriveLiveVersion (shared helper)"
      via: "panel status section; version via the shared @/lib/skillVersion helper"
      pattern: "LifecycleStepper|deriveLiveVersion"
    - from: "PublishGateDialog.tsx"
      to: "onReviewEvals -> Studio·Evals"
      via: "unmet-branch 'Review evals →' link"
      pattern: "onReviewEvals"
---

<objective>
Slim the Skills detail panel and wire the remaining 057-MAP navigation links. The two heavy sections (`SkillEvalSection` + `SkillTestCasesSection`) leave the edit dialog; in their place the panel renders the SHARED full `LifecycleStepper` (the same one the Studio header condenses — one truth-teller, two homes) + the SOLE "Open studio" button + counts. The 136 `PublishGateDialog` gains its one net-new "Review evals →" link on the unmet branch. This plan also destructures + drills the `onOpenStudio`/`onReviewEvals` navigators (whose Props were added to SkillsPage in Plan 06) to their consumers, and consumes the shared `deriveLiveVersion` helper Plan 06 created.

Purpose: Close the "messy UI" complaint (the U11 panel-density finding) with a calm slim panel (D-07), dissolve the 1/1-vs-0/2 contradiction where the operator first hit it via the shared stepper (D-10), and make the gate discoverable from the publish flow (D-06) — additive, with no Skills-tab redesign. The panel "Open studio" button is the app's single such control (Plan 06 deliberately added none).
Output: slimmed `SkillFormDialog.tsx` (SkillDetailPanel), `PublishGateDialog.tsx` link, `SkillCard.tsx` + `SkillsPage.tsx` navigator drill (+ spec extensions).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-skill-evals-panel-ui-panel-01/137-RESEARCH.md
@.planning/phases/137-skill-evals-panel-ui-panel-01/137-PATTERNS.md
@.planning/sketches/057-skill-studio-linkage/README.md
@.planning/phases/136-skill-publish-gate-gate-01/136-CONTEXT.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
@frontend/src/components/skills/SkillFormDialog.tsx
@frontend/src/components/skills/PublishGateDialog.tsx
@frontend/src/components/skills/SkillCard.tsx
@frontend/src/pages/SkillsPage.tsx
@frontend/src/components/skills/studio/LifecycleStepper.tsx

<interfaces>
<!-- Slim-down target: SkillDetailPanel (exported from SkillFormDialog.tsx). -->
SkillFormDialog.tsx: imports :14-15 (SkillTestCasesSection, SkillEvalSection — REMOVE); SkillDetailPanel savedSkillId :426; the saved skill record (with `instructions`) the panel already holds; onTuneThis prop precedent :44/:64/:524-528; the two section mounts :535-548 (REMOVE, replace with stepper + Open studio + counts).
PublishGateDialog.tsx: unmet branch :117-126 (UNMET_COPY[gate.state] + gate.reason + "Run an eval…"); SkillCard mounts it :269-271 with showPublishDialog.
SkillCard.tsx: :39 showPublishDialog; :269-271 PublishGateDialog mount.
SkillsPage.tsx: onOpenStudio/onReviewEvals props (added to the Props INTERFACE in Plan 06, not yet destructured); SkillCard mount :141; SkillDetailPanel mount :202-206. THIS plan destructures both in the SkillsPage signature and drills them (so they become used — noUnusedLocals clean).
LifecycleStepper (Plan 01): props { publishGate, caseCount, skillVersion, variant?: "full"|"strip", onNavigateStage? }; use variant="full" here.
PublishGate (types/index.ts): { met, state, measured: number|null, passed: number|null, passing_run_id, reason, last_override }.
Navigators (from App via Plan 06): onOpenStudio(skillId, tab?), onReviewEvals(skillId).

<!-- W1 fix — the panel derives the version number via the SHARED helper (one rule, one implementation, two homes). -->
Import `deriveLiveVersion(skill, versions)` from `@/lib/skillVersion` (created in Plan 06). The panel fetches `listSkillVersions(savedSkillId)` and calls `deriveLiveVersion(savedSkill, versions)` — NO inline re-implementation of the content-equality rule. (Rule, for reference: instructions match → that version_number; else MAX; no versions → 1.)

<!-- W2 fix — the panel's counts line reuses the ALREADY-fetched gate (no new endpoint). -->
The counts line reads: "cases" = `caseCount` (the `listTestCases(savedSkillId)` length already fetched for the stepper); "latest run summary" = `${gate.passed}/${gate.measured}` from the `getPublishGate(savedSkillId)` response the stepper already consumes (both are `null` when never evaled → render an honest "not evaled yet", not a fabricated 0/0). NO new list/get eval-run endpoint call is added for the counts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Slim the detail panel — remove sections, add shared stepper + Open studio (sole button)</name>
  <files>frontend/src/components/skills/SkillFormDialog.tsx, frontend/src/components/skills/SkillFormDialog.test.tsx</files>
  <read_first>
    - frontend/src/components/skills/SkillFormDialog.tsx (imports :14-15; SkillDetailPanel savedSkillId :426; onTuneThis precedent :524-528; the two section mounts :535-548 to remove; the saved skill record the panel holds)
    - frontend/src/components/skills/studio/LifecycleStepper.tsx (the shared full-variant stepper to mount here)
    - .planning/sketches/057-skill-studio-linkage/README.md + .planning/sketches/053-eval-studio-shell/README.md (the slim-panel "calm at rest" bar + the panel status = full stepper, D-10; the panel "Open studio" as the SINGLE such control)
    - frontend/src/lib/api.ts (getPublishGate, listTestCases, listSkillVersions — for the panel's gate + case count + version derivation) + frontend/src/lib/skillVersion.ts (the shared deriveLiveVersion helper from Plan 06 — IMPORT, do not re-implement)
  </read_first>
  <behavior>
    - The SkillEvalSection + SkillTestCasesSection mounts are removed from the detail panel.
    - In their place: a status section rendering the SHARED <LifecycleStepper variant="full"> fed the skill's getPublishGate + case count + derived version; an "Open studio" button (calls onOpenStudio(savedSkillId, "evals")) — the app's single "Open studio" control; and a counts line — cases = the listTestCases length, latest-run summary = gate.passed/gate.measured from the already-fetched getPublishGate (no new endpoint call).
    - The panel at rest is calm (closes the U11 density complaint); the "Publish ready 1/1 vs 0/2" contradiction is dissolved by the stepper (D-10).
    - The edit form itself is unchanged.
  </behavior>
  <action>
    In SkillFormDialog.tsx (the exported `SkillDetailPanel`): remove the `SkillTestCasesSection` + `SkillEvalSection` imports (:14-15) and their two mounts (:535-548). Add an `onOpenStudio?: (skillId: string, tab?: "evals"|"triggering"|"versions") => void` prop (thread it like the existing `onTuneThis`/`onTuneSkill` precedent). In place of the removed sections render: (1) a status section with `<LifecycleStepper variant="full" publishGate={gate} caseCount={caseCount} skillVersion={version} />` where `gate` is a `getPublishGate(savedSkillId)` fetch, `caseCount` is a `listTestCases(savedSkillId)` length, and `version` is `deriveLiveVersion(savedSkill, versions)` from the SHARED `@/lib/skillVersion` helper (created in Plan 06 — W1: one rule, one implementation, no inline copy) fed a `listSkillVersions(savedSkillId)` fetch; (2) an "Open studio" button calling `onOpenStudio?.(savedSkillId, "evals")` — this is the SOLE "Open studio" control in the app (Plan 06 added no second one); (3) a small counts line — cases = `caseCount`, latest-run summary = `${gate.passed}/${gate.measured}` reusing the ALREADY-fetched `gate` (W2: no new endpoint call; both `null` → an honest "not evaled yet"). Keep the skill-switch guard on the panel's gate/case/version fetch. Do NOT reintroduce eval-run or case-CRUD machinery in the panel — that now lives in the Studio. Extend `SkillFormDialog.test.tsx`: assert the two sections are gone (SkillTestCasesSection/SkillEvalSection not rendered), the LifecycleStepper renders, and the "Open studio" button calls onOpenStudio with (skillId, "evals").
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/skills/SkillFormDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The section imports are gone: `grep -c 'SkillTestCasesSection\|SkillEvalSection' frontend/src/components/skills/SkillFormDialog.tsx` == 0.
    - The shared stepper is mounted full-variant: grep matches `LifecycleStepper` and `variant="full"`.
    - The panel derives the version via the SHARED helper (W1 — not an inline copy): grep matches BOTH `deriveLiveVersion` and `listSkillVersions` in frontend/src/components/skills/SkillFormDialog.tsx.
    - The counts line reuses the fetched gate (W2 — no new endpoint): `grep -c 'gate\.passed\|gate\.measured' frontend/src/components/skills/SkillFormDialog.tsx` >= 1.
    - The Open studio button calls onOpenStudio: grep matches `onOpenStudio(`.
    - Spec asserts the sections are absent + the stepper present + Open studio wired.
  </acceptance_criteria>
  <done>The detail panel is slimmed to form + shared stepper (version via the shared deriveLiveVersion helper) + the sole Open studio button + counts (cases + gate.passed/measured summary, no new endpoint); the two heavy sections are gone; spec green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PublishGateDialog "Review evals →" + the navigator drill</name>
  <files>frontend/src/components/skills/PublishGateDialog.tsx, frontend/src/components/skills/PublishGateDialog.test.tsx, frontend/src/components/skills/SkillCard.tsx, frontend/src/pages/SkillsPage.tsx</files>
  <read_first>
    - frontend/src/components/skills/PublishGateDialog.tsx (the unmet branch :117-126 — add the link after the "Run an eval…" line)
    - frontend/src/components/skills/SkillCard.tsx (:39 showPublishDialog; :269-271 the PublishGateDialog mount — pass onReviewEvals through)
    - frontend/src/pages/SkillsPage.tsx (the Props already carry onOpenStudio/onReviewEvals from Plan 06; the SkillCard mount :141 + the SkillDetailPanel mount :202-206 — destructure both navigators and drill onReviewEvals to SkillCard, onOpenStudio to SkillDetailPanel)
    - .planning/sketches/057-skill-studio-linkage/README.md (the MAP: PublishGateDialog unmet -> "Review evals →" -> Studio·Evals)
  </read_first>
  <behavior>
    - The PublishGateDialog unmet branch (gate && !gate.met) shows a "Review evals →" link that calls onReviewEvals(skillId) -> the Studio Evals tab.
    - The link is only on the UNMET branch (the "gate only discoverable in the dialog" gap closes).
    - onReviewEvals threads App -> ChatLayout -> SkillsPage -> SkillCard -> PublishGateDialog; onOpenStudio threads SkillsPage -> SkillDetailPanel.
  </behavior>
  <action>
    In PublishGateDialog.tsx: add an `onReviewEvals?: (skillId: string) => void` prop; on the unmet branch (:117-126), after the existing "Run an eval…" copy, render a "Review evals →" link/button that calls `onReviewEvals?.(skillId)`. In SkillCard.tsx: add an `onReviewEvals?: (skillId: string) => void` prop and pass it to the `<PublishGateDialog … onReviewEvals={onReviewEvals} />` mount (:269-271). In SkillsPage.tsx: DESTRUCTURE `onOpenStudio` + `onReviewEvals` in the component signature (their Props were declared in Plan 06) and drill them — `onReviewEvals` into the `<SkillCard>` mount (:141) and `onOpenStudio` into the `<SkillDetailPanel>` mount (:202-206). Destructuring + drilling makes both navigators USED (clearing the Plan-06 deferral without a `noUnusedLocals` error). Extend `PublishGateDialog.test.tsx`: on an unmet gate the "Review evals →" link renders and clicking it calls onReviewEvals with the skill id; on a met gate the link is absent.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/skills/PublishGateDialog.test.tsx && npx tsc -p tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - PublishGateDialog renders the link only on unmet: `grep -c 'onReviewEvals' frontend/src/components/skills/PublishGateDialog.tsx` >= 1 and the spec asserts the link is absent when gate.met is true.
    - SkillCard passes onReviewEvals to the dialog: `grep -c 'onReviewEvals' frontend/src/components/skills/SkillCard.tsx` >= 1.
    - SkillsPage destructures + drills both navigators: `grep -c 'onReviewEvals\|onOpenStudio' frontend/src/pages/SkillsPage.tsx` >= 3 (interface + destructure + drill sites).
    - `npx tsc -p tsconfig.json --noEmit` clean (the full navigator drill typechecks end to end, no unused props).
  </acceptance_criteria>
  <done>The unmet PublishGateDialog offers "Review evals →" to Studio·Evals; both navigators are destructured + drilled to their consumers; the app typechecks; specs green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server PublishGate -> panel status section | The slim panel's stepper renders the same server gate the Studio header condenses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-01 | Elevation of Privilege | panel status stepper / Publish button | mitigate | The slim panel renders the SHARED `LifecycleStepper` fed the server `getPublishGate` — it never recomputes `met` (D-07/054 one-truth lock); the counts line reuses `gate.passed`/`gate.measured` verbatim; the same component the Studio header condenses (one truth-teller, two homes, D-10). |
| T-137-02 | Information Disclosure | panel gate/case/version fetch on skill switch | mitigate | The panel's gate/case/version fetch carries the skill-switch guard so a late fetch never renders under a different skill; the version derivation (shared deriveLiveVersion) reads only the owner-scoped listSkillVersions rows. |
| T-137-03 | Information Disclosure | Review-evals navigation | mitigate | The "Review evals →" link passes only the owner's `skillId` to the navigator; it opens the owner-scoped Studio and constructs no cross-user id. |
| T-137-05 | Injection (XSS) | panel counts / gate copy render | mitigate | The panel status copy, counts, and gate reason render as React text nodes; no `dangerouslySetInnerHTML`. |
| T-137-SC | Tampering (supply chain) | npm install surface | accept | Zero new packages; slopcheck N/A. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/skills/SkillFormDialog.test.tsx src/components/skills/PublishGateDialog.test.tsx` green; `npx tsc -p tsconfig.json --noEmit` clean.
- Grep gates confirm the removed sections, the shared full stepper + shared-helper-derived version in the panel, the gate.passed/measured counts reuse, the single "Open studio" button, the unmet-only "Review evals →" link, and the drilled navigators.
</verification>

<success_criteria>
The detail panel is slimmed to a calm form + shared full stepper (version via the shared deriveLiveVersion helper) + the sole Open studio button + counts (cases + gate.passed/measured summary) (D-07/D-10), the PublishGateDialog unmet branch offers "Review evals →" (D-06), and the whole navigator drill typechecks — additive, no Skills-tab redesign. Specs green.
</success_criteria>

<output>
Create `.planning/phases/137-skill-evals-panel-ui-panel-01/137-07-SUMMARY.md` when done.
</output>
</content>
</invoke>
