---
phase: 145-run-lifecycle-honesty-threads-py-extraction-stretch
plan: 06
type: execute
wave: 2
depends_on: [145-01]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/seeds/SEED-109-migrate-eval-tuner-runs-active-writers-to-run-lifecycle.md
autonomous: true
requirements: [FND-01]

must_haves:
  truths:
    - "D-145-01: the FND-01 line in .planning/REQUIREMENTS.md is corrected — the self-contradictory 'Redis runs:active is the single source of truth ... the frontend derives running/Stop deterministically from it' clause is replaced with 'Postgres runs.status is authoritative; runs:active is a derived mirror co-written on every transition'; the rest of the sentence (both-directions honesty, Stop actually cancels, reconcile-on-reconnect per D-v2.5-03, extraction out of threads.py) stands unchanged"
    - "D-145-12: a follow-up SEED (SEED-109) is planted to migrate the 5 deferred eval/tuner/eval_runner runs:active writers (evals.py :315/:1915/:2624, runs.py :1247, skill_tuner.py :643/:738, eval_runner_service.py :864) onto the shared run_lifecycle owner — the true single-owner end-state — with re-open trigger = 'any future runs:active drift observed on eval or tuner runs, OR the next G-5 touch of evals.py/skill_tuner.py'"
    - "The SEED also records the two other deferred items with concrete re-open triggers: the Open Q1 cross-worker cancel gap (WORKER_COUNT=2, task.cancel in-process) and the last_heartbeat schema column (re-open iff stream-age proves insufficient — a provider legitimately emits zero stream events > STALE_TIMEOUT)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "FND-01 corrected to the runs.status-authoritative model"
      contains: "runs.status"
    - path: ".planning/seeds/SEED-109-migrate-eval-tuner-runs-active-writers-to-run-lifecycle.md"
      provides: "the deferred 5-writer migration seed + cross-worker-cancel + last_heartbeat re-open triggers"
      contains: "re_open_trigger"
  key_links:
    - from: "SEED-109"
      to: "run_lifecycle.py"
      via: "future adopters of the shared owner (the 5 eval/tuner writers)"
      pattern: "run_lifecycle"
---

<objective>
Close the phase's documentation + governance debt: (1) correct the FND-01
requirement text so its acceptance is no longer self-contradictory with the locked
design — Postgres `runs.status` is authoritative, `runs:active` is a derived mirror
(D-145-01, superseding the pre-trace "runs:active is the single source of truth"
wording that `c12ff264` corrected); and (2) plant the follow-up SEED that captures
everything deliberately deferred out of 145 — the 5-writer eval/tuner migration onto
the shared owner (D-145-12), the pre-existing cross-worker cancel gap (Open Q1), and
the rejected `last_heartbeat` column — each with a concrete re-open trigger so
nothing is silently lost.

This plan is docs-only (no code, no runtime surface) and independent of the code
plans — it depends only on the LIVE repro (Plan 01) so the phase's "repro-first"
ordering holds. The run_reconciler.py docstring correction is companion to this and
is done in Plan 04 (same file as the predicate swap).

Purpose: honesty at the requirement layer + preserve every deferred idea with a
re-open trigger (project convention).
Output: corrected FND-01 + SEED-109.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-CONTEXT.md
@.planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/seeds/SEED-108-rag-to-sandbox-file-bridge.md

<seed_frontmatter_shape>
<!-- Match the existing seed frontmatter (SEED-108). Highest seed today = SEED-108 →
     the new one is SEED-109. -->
```yaml
---
id: SEED-109
status: planted
planted: 2026-07-09
planted_during: v3.2 STRETCH (Phase 145 — Run-Lifecycle Honesty + threads.py extraction)
trigger_when: <one-line description>
scope: Medium
related: [[Phase 145]], run_lifecycle.py, [[project_foundation_pass_cross_provider_uat]]
re_open_trigger: <concrete trigger>
---
```
</seed_frontmatter_shape>

<deferred_items_to_record>
<!-- From CONTEXT <deferred> + RESEARCH Open Questions / U4. Verbatim triggers. -->
1. 5-writer migration (D-145-12): migrate eval/tuner/eval_runner runs:active writers
   onto run_lifecycle — evals.py :315/:1915/:2624, runs.py :1247 (zombie-heal already
   done in Plan 03 — exclude), skill_tuner.py :643/:738, eval_runner_service.py :864.
   re_open_trigger: "any future runs:active drift observed on eval or tuner runs, OR
   the next G-5 touch of evals.py/skill_tuner.py."
2. Cross-worker cancel gap (Open Q1): task.cancel() is in-process; under WORKER_COUNT=2
   a Stop on the non-owning worker falls to zombie-heal (marks DB cancelled) while the
   live producer keeps running on the other worker (finalize_run has no CAS). The only
   cross-worker signal today is publish_cancel_sentinel (ask_user wake only).
   re_open_trigger: "a live run is not cancellable when Stop lands on the non-owning worker."
3. last_heartbeat schema column: rejected this phase (no schema; stream-age is the
   oracle). re_open_trigger: "stream-age proves insufficient — a provider legitimately
   emits zero stream events for > STALE_TIMEOUT."
</deferred_items_to_record>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the FND-01 requirement wording</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md line 50 (the FND-01 bullet — the exact current sentence with the "Redis runs:active is the single source of truth ... the frontend derives running/Stop deterministically from it" clause)
    - .planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-CONTEXT.md D-145-01/D-145-02 (the authoritative correction) + 145-RESEARCH.md §"Phase Requirements" (the exact replacement wording + "the rest of the sentence stands unchanged" note)
  </read_first>
  <action>
    Edit the FND-01 bullet in `.planning/REQUIREMENTS.md`. Replace ONLY the
    self-contradictory clause — "Redis `runs:active` is the single source of truth
    (registered on run start, removed only on terminal, reconciled via fetch on
    reconnect per D-v2.5-03); the frontend derives running/Stop deterministically from
    it." — with: "Postgres `runs.status` is authoritative; Redis `runs:active` is a
    derived mirror co-written with the status on every transition (register on start,
    ZREM on terminal), reconciled via fetch on reconnect per D-v2.5-03; the frontend
    derives running/Stop from `runs.status` (via get_snapshot), and `runs:active` +
    the reconciler + admin backpressure stay consistent with it (corrected root cause,
    c12ff264 / D-145-01/02)." Keep the REST of the FND-01 sentence intact —
    both-directions honesty, "Stop actually cancels", the extraction OUT of threads.py
    into a dedicated tested module, and the BUG-260709-01 / BUG-260702-02 references.
    Do NOT change any other requirement line. Leave the checkbox state as-is.
  </action>
  <verify>
    <automated>grep -c "runs.status" .planning/REQUIREMENTS.md && ! grep -q "runs:active\` is the single source of truth" .planning/REQUIREMENTS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The FND-01 bullet contains "Postgres `runs.status` is authoritative" and "derived mirror".
    - The phrase "`runs:active` is the single source of truth" no longer appears in the FND-01 line.
    - The unchanged remainder (both-directions honesty, Stop actually cancels, extraction out of threads.py, BUG references) is still present.
    - No other REQUIREMENTS.md line is modified (`git diff` shows a single-line-region change).
  </acceptance_criteria>
  <done>FND-01 reads runs.status-authoritative / runs:active-derived; the rest of the requirement is intact.</done>
</task>

<task type="auto">
  <name>Task 2: Plant SEED-109 (5-writer migration + cross-worker cancel + last_heartbeat)</name>
  <files>.planning/seeds/SEED-109-migrate-eval-tuner-runs-active-writers-to-run-lifecycle.md</files>
  <read_first>
    - .planning/seeds/SEED-108-rag-to-sandbox-file-bridge.md (the seed frontmatter + body shape to match)
    - .planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-CONTEXT.md <deferred> (the three deferred items verbatim) + 145-RESEARCH.md §"Open Questions" (Open Q1 detail) and §"U4" (cross-worker cancel re-open trigger)
    - The `<seed_frontmatter_shape>` + `<deferred_items_to_record>` blocks above
  </read_first>
  <action>
    Create `.planning/seeds/SEED-109-migrate-eval-tuner-runs-active-writers-to-run-lifecycle.md`
    using the frontmatter from `<seed_frontmatter_shape>`. Body: a "## The gap" section
    stating that after 145 the shared `run_lifecycle` owner exists and chat/Deep runs
    co-write status⇄runs:active atomically, but the 5 eval/tuner/eval_runner writers
    still ZADD/ZREM `runs:active` in their own code paths (evals.py :315/:1915/:2624,
    skill_tuner.py :643/:738, eval_runner_service.py :864) — so the invariant
    `runs:active == {status='streaming'}` is CHAT-SCOPED, not global; those writers can
    still drift. The true single-owner end-state migrates them onto
    `register_run_start`/`finalize_run_terminal`. Note the runs.py zombie-heal was
    already migrated in Phase 145 Plan 03 (exclude it). Add a "## Also deferred here"
    section recording items 2 (cross-worker cancel gap, Open Q1) and 3 (last_heartbeat
    column), each with its concrete re_open_trigger from `<deferred_items_to_record>`.
    Set the frontmatter `re_open_trigger` to the 5-writer trigger ("any future
    runs:active drift observed on eval or tuner runs, OR the next G-5 touch of
    evals.py/skill_tuner.py"). Keep it identifier-only; reference the CONTEXT decisions
    (D-145-09/12) + RESEARCH U4.
  </action>
  <verify>
    <automated>test -f ".planning/seeds/SEED-109-migrate-eval-tuner-runs-active-writers-to-run-lifecycle.md" && grep -c "re_open_trigger" .planning/seeds/SEED-109-migrate-eval-tuner-runs-active-writers-to-run-lifecycle.md</automated>
  </verify>
  <acceptance_criteria>
    - SEED-109 exists with valid frontmatter (id SEED-109, status planted, a re_open_trigger).
    - The body names all 5 deferred writers (evals.py/skill_tuner.py/eval_runner_service.py sites) and states the chat-scoped-vs-global invariant.
    - The two additional deferred items (cross-worker cancel Open Q1, last_heartbeat) are recorded with their own re-open triggers.
  </acceptance_criteria>
  <done>SEED-109 captures the 5-writer migration + cross-worker cancel + last_heartbeat, each with a concrete re-open trigger — nothing deferred is lost.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only plan. No runtime surface, no request path, no data flow — the edits are a requirement-text correction and a planning seed file. No trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-06-01 | (n/a — docs) | .planning/*.md edits | accept | No executable code, no attack surface introduced. The FND-01 correction + SEED are planning artifacts only. |
| T-145-06-SC | Tampering (supply chain) | package installs | accept | No package-manager installs — T-145-SC not applicable. |
</threat_model>

<verification>
- `grep -q "runs.status\` is authoritative" .planning/REQUIREMENTS.md` (FND-01 corrected).
- SEED-109 present with a re_open_trigger and all 5 writer sites named.
- `git diff --name-only` == REQUIREMENTS.md + the new SEED file only.
</verification>

<success_criteria>
- FND-01 no longer contradicts the locked design — runs.status authoritative, runs:active derived (D-145-01).
- Every deferred idea (5-writer migration D-145-12, cross-worker cancel Open Q1, last_heartbeat) is captured in SEED-109 with a concrete re-open trigger.
</success_criteria>

<output>
Create `.planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-06-SUMMARY.md` when done.
</output>
