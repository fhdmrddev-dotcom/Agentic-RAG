---
phase: 145-run-lifecycle-honesty-threads-py-extraction-stretch
plan: 02
type: execute
wave: 2
depends_on: [145-01]
files_modified:
  - backend/app/services/run_lifecycle.py
  - backend/tests/test_run_lifecycle.py
autonomous: true
requirements: [FND-01]

must_haves:
  truths:
    - "D-145-09: chat-run lifecycle (start register + terminal) is extracted into a NEW dedicated, unit-tested module `backend/app/services/run_lifecycle.py` — NOT inside threads.py — exposing `register_run_start` and `finalize_run_terminal` with explicit `pool`/`redis` DI"
    - "D-145-02: `register_run_start` co-writes `runs.status='streaming'` (via db.runs.insert_run) AND `ZADD runs:active` + `ZADD runs_by_thread` as ONE unit; `finalize_run_terminal` co-writes the terminal status (via db.runs.finalize_run) AND `ZREM runs:active` + `ZREM runs_by_thread` — so `runs:active == {chat runs WHERE status='streaming'}` by construction"
    - "D-145-01: the module docstring names Postgres `runs.status` as authoritative and `runs:active` as the derived mirror it co-writes (superseding the pre-trace 'runs:active is the source of truth' framing)"
    - "D-145-12: the module is NEW and additive — it reuses db.runs.finalize_run/insert_run (the shared writers) and does NOT fork or migrate the 5 eval/tuner writers; the docstring states the invariant is CHAT-SCOPED this phase"
    - "The atomicity is proven: test_run_lifecycle.py asserts the status write AND the Redis ZADD/ZREM both fire for the SAME run_id in one call, via the in-memory _FakePool + _FakeRedis"
  artifacts:
    - path: "backend/app/services/run_lifecycle.py"
      provides: "register_run_start + finalize_run_terminal atomic co-writers (the D-145-02 anti-drift owner)"
      contains: "async def finalize_run_terminal"
      min_lines: 40
    - path: "backend/tests/test_run_lifecycle.py"
      provides: "atomic co-write unit tests (status + ZADD/ZREM fire together) using in-memory fakes"
      contains: "test_finalize_cowrites_terminal_and_zrem"
  key_links:
    - from: "run_lifecycle.finalize_run_terminal"
      to: "db.runs.finalize_run"
      via: "terminal status write routed through the shared writer (parity)"
      pattern: "finalize_run\\("
    - from: "run_lifecycle.register_run_start"
      to: "db.runs.insert_run"
      via: "start status write routed through the shared writer"
      pattern: "insert_run\\("
---

<objective>
Create the atomic run-lifecycle owner — the single module that co-writes
`runs.status` and its `runs:active` mirror together, killing the drift that lives
in threads.py today (ZADD/ZREM in different code paths than the status writes).
This is the G-5 paydown foundation (D-145-09): a dedicated, DI'd, unit-tested
`backend/app/services/run_lifecycle.py` that later plans wire threads.py (Plan 03),
the cancel zombie-heal (Plan 03), and the reconciler sweep (Plan 04) onto.

This plan ONLY creates the module + its tests. It does NOT yet re-point any caller
(that is Plan 03/04) — so the running app is byte-identical after this plan; the
new module is dead code until wired. Building + testing the owner in isolation
first isolates the correctness-critical atomic co-write before any control-flow
change consumes it.

Purpose: one owner guarantees the mirror can't drift from the truth (D-145-02).
Output: `run_lifecycle.py` (two async co-writers) + `test_run_lifecycle.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-CONTEXT.md
@.planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-RESEARCH.md
@.planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-PATTERNS.md
@backend/app/db/runs.py
@backend/app/services/run_reconciler.py
@backend/tests/test_run_reconciler.py

<interfaces>
<!-- The two shared DB writers the owner MUST reuse (READ, do not re-implement).
     Verified against db/runs.py this session — match these signatures EXACTLY so
     the owner is a thin wrapper, not a re-write. -->

From backend/app/db/runs.py:26 (insert_run — pool positional, then keyword-only):
```python
async def insert_run(pool, *, run_id, thread_id, user_id, status, model, provider,
                     spawned_by_worker=None, parent_run_id=None) -> None: ...
```

From backend/app/db/runs.py:69 (finalize_run — status-only DB write, the shared terminal writer):
```python
async def finalize_run(pool, *, run_id, status, error, completed_at, message_id,
                       input_tokens, output_tokens) -> None: ...
# status $2, error $3, completed_at $4, message_id $5, input_tokens $6, output_tokens $7
```

From backend/app/services/run_reconciler.py:74 (the DI shape + key constant to mirror):
```python
async def reconcile_orphaned_runs(*, pool, redis, supabase) -> int: ...
_ACTIVE_SET_KEY = "runs:active"   # reuse the exact same constant name
# runs_status_check enum: streaming / cap_paused / completed / failed / cancelled / timed_out
```
</interfaces>

<extraction_source>
<!-- The exact threads.py blocks the co-writers reproduce (PATTERNS, VERIFIED refs).
     Plan 03 removes these from threads.py; this plan lifts their SHAPE into the owner. -->
- START register (threads.py:1108-1127): insert_run(status='streaming') then
  `_score = time.time()`; `ZADD runs_by_thread:{tid} {run_id: _score}`; `ZADD runs:active {run_id: _score}`.
- Deep TERMINAL (threads.py:1684-1724): finalize_run(...) then (later) `ZREM runs:active`; `ZREM runs_by_thread:{tid}`.
  The terminal sentinel XADD + `EXPIRE run:{id}` (:1697-1717) STAY in the producer — the owner owns status+ZREM ONLY.
</extraction_source>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the atomic co-writer test scaffold (RED)</name>
  <files>backend/tests/test_run_lifecycle.py</files>
  <read_first>
    - backend/tests/test_run_reconciler.py:34-136 (the in-memory `_FakePool` recording `executed: list[(sql, params)]`, and `_FakeRedis` honoring set(nx)/zscore/zrem — the fakes to REUSE; note `_FakeRedis` today has NO `zadd`, so you add one)
    - .planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-PATTERNS.md §"backend/tests/test_run_lifecycle.py" (the exact zadd-recorder + atomicity-assertion recipe)
    - .planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-VALIDATION.md (rows: test_register_cowrites_status_and_active, test_finalize_cowrites_terminal_and_zrem)
    - backend/app/db/runs.py:26-109 (the insert_run/finalize_run SQL so the assertions target the right params — status is $2 on the UPDATE)
  </read_first>
  <action>
    Create `backend/tests/test_run_lifecycle.py`. Copy the `_FakePool` + `_FakeRedis`
    in-memory fakes from `test_run_reconciler.py:34-136`; EXTEND `_FakeRedis` with an
    async `zadd(self, key, mapping, *a, **k)` recorder that updates `self.active` when
    key == "runs:active" else a `self.by_thread.setdefault(key, {})` dict (mirror the
    existing `zrem` shape). Import `register_run_start` and `finalize_run_terminal`
    from `app.services.run_lifecycle`. Write two `@pytest.mark.asyncio` tests:
    `test_register_cowrites_status_and_active` — call `register_run_start(pool=fp,
    redis=fr, run_id=<uuid>, thread_id=<uuid>, user_id=<uuid>, model="gpt-x",
    provider="openai")`; assert `fp.executed` grew by 1 (the INSERT) AND
    `str(run_id) in fr.active` AND `f"runs_by_thread:{thread_id}"` gained the run_id
    — all three in one call. `test_finalize_cowrites_terminal_and_zrem` — pre-seed
    the run into `fr.active` + `fr.by_thread`, call `finalize_run_terminal(pool=fp,
    redis=fr, run_id=..., thread_id=..., status="completed", error=None,
    completed_at=<now>, message_id=None, input_tokens=None, output_tokens=None)`;
    assert `fp.executed` grew by 1 (the UPDATE) with status param == "completed" AND
    `str(run_id) not in fr.active` AND removed from `runs_by_thread`. These MUST stay
    red (ImportError) until Task 2 creates the module. Pure fakes — no live Redis/DB.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_run_lifecycle.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Before Task 2 the file fails on ImportError (module absent) — proving RED.
    - `_FakeRedis` in the test has an async `zadd` recorder distinguishing `runs:active` from `runs_by_thread:*`.
    - Each test asserts BOTH the pool write (status) AND the Redis membership change fired for the SAME run_id in one call.
  </acceptance_criteria>
  <done>test_run_lifecycle.py present with the two atomic-co-write tests; red pending Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create run_lifecycle.py — register_run_start + finalize_run_terminal</name>
  <files>backend/app/services/run_lifecycle.py</files>
  <behavior>
    - register_run_start co-writes: db.runs.insert_run(status='streaming') THEN ZADD runs:active + ZADD runs_by_thread:{tid}, all keyed on the same run_id, score = time.time().
    - finalize_run_terminal co-writes: db.runs.finalize_run(status=<terminal>) THEN ZREM runs:active + ZREM runs_by_thread:{tid}, same run_id.
    - On success the invariant holds for chat runs: run_id ∈ runs:active ⇔ runs.status == 'streaming'.
    - Callers own best-effort framing (the owner stays thin — matches how threads.py wraps these ops today); the owner does not swallow the DB write's exceptions.
  </behavior>
  <read_first>
    - backend/app/services/run_reconciler.py:1-72 (the module-header / imports / logger / `_ACTIVE_SET_KEY` constant + docstring STYLE to mirror — DI, best-effort, finalize_run reuse)
    - backend/app/db/runs.py:26-109 (insert_run + finalize_run — the EXACT keyword-only signatures to wrap)
    - .planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-PATTERNS.md §"backend/app/services/run_lifecycle.py" (the verbatim co-write bodies for start + terminal, and the "docstring must state the chat-scoped invariant" note)
    - The `<interfaces>` + `<extraction_source>` blocks above (reproduce the co-write order from threads.py:1108-1127 / :1684-1724)
  </read_first>
  <action>
    Create `backend/app/services/run_lifecycle.py`. Header `from __future__ import
    annotations`; imports `logging`, `time`, `from app.db.runs import finalize_run,
    insert_run`; `logger = logging.getLogger(__name__)`; reuse `_ACTIVE_SET_KEY =
    "runs:active"` (same constant name as run_reconciler.py). Module docstring MUST
    state (D-145-01) that Postgres `runs.status` is authoritative and `runs:active`
    is the DERIVED mirror co-written here, and (D-145-12) that the invariant
    `runs:active == {status='streaming'}` holds for CHAT/Deep runs ONLY this phase —
    the 5 eval/tuner writers are not migrated (name them so a future reader doesn't
    assume a global invariant). Implement `async def register_run_start(*, pool,
    redis, run_id, thread_id, user_id, model, provider, spawned_by_worker=None,
    parent_run_id=None, status="streaming")` → `await insert_run(pool, run_id=...,
    thread_id=..., user_id=..., status=status, model=model, provider=provider,
    spawned_by_worker=spawned_by_worker, parent_run_id=parent_run_id)` then
    `_score = time.time()`; `await redis.zadd(f"runs_by_thread:{thread_id}",
    {str(run_id): _score})`; `await redis.zadd(_ACTIVE_SET_KEY, {str(run_id):
    _score})`. Implement `async def finalize_run_terminal(*, pool, redis, run_id,
    thread_id, status, error, completed_at, message_id=None, input_tokens=None,
    output_tokens=None)` → `await finalize_run(pool, run_id=run_id, status=status,
    error=error, completed_at=completed_at, message_id=message_id,
    input_tokens=input_tokens, output_tokens=output_tokens)` then `await
    redis.zrem(_ACTIVE_SET_KEY, str(run_id))`; `await
    redis.zrem(f"runs_by_thread:{thread_id}", str(run_id))`. Keep the owner THIN — do
    NOT XADD the terminal sentinel or EXPIRE the stream (those stay in the threads.py
    producer, Plan 03). Export both via `__all__`. Do NOT import or touch threads.py,
    evals.py, skill_tuner.py, or agent_loop.py (D-145-12 red line).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_run_lifecycle.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_run_lifecycle.py` passes GREEN (both atomic co-writes proven).
    - `grep -nE "async def (register_run_start|finalize_run_terminal)" backend/app/services/run_lifecycle.py` shows both.
    - `run_lifecycle.py` imports `finalize_run` and `insert_run` from `app.db.runs` (reuses the shared writers — no fresh SQL/UPDATE in the module).
    - The module docstring contains the words "authoritative" (runs.status) and "chat" (chat-scoped invariant) and names the deferred eval/tuner writers.
    - `git grep -l "import" backend/app/services/run_lifecycle.py` shows NO import of threads.py / evals.py / skill_tuner.py / agent_loop.py.
  </acceptance_criteria>
  <done>run_lifecycle.py exists with the two DI'd atomic co-writers reusing db.runs; fully covered; no caller re-pointed yet (byte-identical runtime).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| already-authorized callers → run_lifecycle | The owner is invoked only by already-authorized paths (the request producer in threads.py, the owner-scoped cancel in runs.py, the service-role sweep). It performs NO auth of its own — it must not weaken the callers' ownership SELECTs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-02-01 | Information Disclosure | error string passed to finalize_run → RLS-readable runs.error | mitigate | The owner writes the caller-supplied `error` verbatim; callers pass short identifier/reason strings (never tracebacks) — preserve the T-073-04 discipline. The owner itself logs nothing containing content. |
| T-145-02-02 | Tampering (partial co-write) | status write succeeds, Redis ZADD/ZREM fails | accept | Owner stays thin; callers keep their existing per-op best-effort try/except (threads.py:1716/1723). A missed ZREM is self-healed by the reconciler stream-age sweep (Plan 04). Invariant is eventually-consistent for chat runs; documented in the module docstring. |
| T-145-02-EoP | Elevation of Privilege | owner bypassing ownership checks | accept | The owner takes `run_id`/`thread_id`/`user_id` from already-authorized callers and does no cross-user lookup; it never becomes a new unauthenticated entry point (no route, no request handling). |
| T-145-02-SC | Tampering (supply chain) | package installs | accept | No package-manager installs — pure first-party module reusing installed redis/asyncpg. T-145-SC not applicable. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/test_run_lifecycle.py -q` → green.
- `git diff --name-only` includes ONLY `backend/app/services/run_lifecycle.py` + `backend/tests/test_run_lifecycle.py` (no caller re-pointed — Plan 03/04 do that).
- The module is importable and dead-code until wired: existing suites unaffected.
</verification>

<success_criteria>
- The atomic co-writer owner exists, reuses db.runs.finalize_run/insert_run (D-145-09 parity), and proves status⇄mirror atomicity in unit tests (D-145-02).
- Its docstring establishes runs.status as authoritative + the chat-scoped invariant (D-145-01 / D-145-12) — no eval/tuner writer migrated.
- Runtime is byte-identical (nothing calls the owner yet); Plan 03 + Plan 04 adopt it.
</success_criteria>

<output>
Create `.planning/phases/145-run-lifecycle-honesty-threads-py-extraction-stretch/145-02-SUMMARY.md` when done.
</output>
