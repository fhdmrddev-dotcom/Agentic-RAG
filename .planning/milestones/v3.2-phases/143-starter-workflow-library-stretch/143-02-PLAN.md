---
phase: 143-starter-workflow-library-stretch
plan: 02
type: execute
wave: 1
depends_on: [143-01]
files_modified:
  - backend/app/db/workflows.py
  - backend/app/api/workflows.py
autonomous: true
requirements: [WF-01]

must_haves:
  truths:
    - "`list_starter_workflows` returns ONLY curated globals (`status='published' AND is_global=true AND definition->>'category'='starter'`) and EXCLUDES the 5 mig-061 scaffolds (D-143-2)"
    - "`list_published_workflows` gains an additive `owned_only` param (default False) — default path stays byte-identical (`is_global OR mine`) so the composer picker, WorkspacePanel run-soul and threads.py kickoff still see globals; `owned_only=True` narrows the Workflows-page Published shelf to `created_by=me` (D-143-2a, D-143-2b, Pitfall 3)"
    - "`GET /workflows/starters` returns the curated rows unscoped; `GET /workflows/published?scope=mine` threads `owned_only=true` — a static route segment so `/{definition_id}` never shadows it (D-143-2a)"
    - "The `category='starter'` predicate is a constant SQL literal, not user input — no injection surface; the fork create route stays UNCHANGED (still forces is_global=false/draft/created_by)"
  artifacts:
    - path: "backend/app/db/workflows.py"
      provides: "list_starter_workflows + owned_only param on list_published_workflows"
      contains: "list_starter_workflows"
    - path: "backend/app/api/workflows.py"
      provides: "GET /workflows/starters + scope=mine on GET /workflows/published"
      contains: "/starters"
  key_links:
    - from: "backend/app/api/workflows.py get_starter_workflows"
      to: "backend/app/db/workflows.py list_starter_workflows"
      via: "get_pg_pool() delegate"
      pattern: "list_starter_workflows"
    - from: "backend/app/db/workflows.py list_starter_workflows"
      to: "definition->>'category'"
      via: "JSONB-path predicate (project_folder_id precedent)"
      pattern: "definition->>'category'"
---

<objective>
Build the read side of the Starters shelf: a curated-globals query and a scoped Published-shelf
de-dupe, exposed as two thin routes. Turn the Plan 01 backend backstops GREEN.

The de-dupe is the delicate part: D-143-2a says "Published = mine only," but `list_published_workflows`
has THREE consumers that NEED global rows (composer Harness picker, `WorkspacePanel.tsx:125` run-soul,
`threads.py:51` kickoff — Pitfall 3). So the narrowing is a default-OFF `owned_only` param used ONLY by
the Workflows-page Published shelf; every existing caller stays byte-identical (D-143-2b).

Purpose: give the frontend a contract to consume and de-dupe scaffolds/starters from the Published shelf.
Output: `list_starter_workflows`, `owned_only` param, `GET /workflows/starters`, `?scope=mine`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/143-starter-workflow-library-stretch/143-PATTERNS.md
@.planning/phases/143-starter-workflow-library-stretch/143-RESEARCH.md

<interfaces>
<!-- Extracted contracts the executor consumes — no exploration needed. -->
From backend/app/db/workflows.py:
- `list_published_workflows` (159-200): current predicate `status='published' AND (is_global=true OR created_by=$1)`;
  the `definition->>'project_folder_id'` JSONB-path AND-append precedent (195-197); `$N`-only binding rule (179-182);
  `ORDER BY name`. `create_workflow_definition` (269-297) already hard-sets status='draft'/is_global=false/created_by — DO NOT TOUCH.
From backend/app/api/workflows.py:
- `get_published_workflows` (104-139): `project_folder_id: UUID | None = Query(None)`; pool via `get_pg_pool()`;
  rows → `PublishedWorkflow(...)` via `_coerce_definition` (43-63). `/drafts` static-segment route precedent (254-263).
  `create_draft` (225-251) maps `UniqueViolationError → 409` — the fork's hash-collision path; DO NOT TOUCH.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: list_starter_workflows + additive owned_only param</name>
  <files>backend/app/db/workflows.py</files>
  <read_first>
    - backend/app/db/workflows.py: `list_published_workflows` (159-200) incl. the `definition->>'project_folder_id'`
      precedent (195-197) and the `$N`-only binding header note (T-073-02 / T-091-03); `create_workflow_definition` (269-297).
    - backend/tests/unit/test_starter_workflows.py (from Plan 01) — the exact assertions this task must satisfy.
    - .planning/phases/143-starter-workflow-library-stretch/143-PATTERNS.md § db/workflows.py.
  </read_first>
  <behavior>
    - `list_starter_workflows(pool)` returns rows WHERE status='published' AND is_global=true AND definition->>'category'='starter', ORDER BY name; excludes the 5 unmarked scaffolds.
    - `list_published_workflows(pool, user_id=uid, owned_only=True)` → created_by=uid only.
    - `list_published_workflows(pool, user_id=uid, owned_only=False)` (default) → is_global OR created_by=uid (UNCHANGED).
  </behavior>
  <action>
    Add `async def list_starter_workflows(pool)` mirroring the `project_folder_id` JSONB precedent: select
    `id, slug, name, definition` WHERE `status='published' AND is_global=true AND definition->>'category'='starter'`
    ORDER BY name, return `[dict(r) for r in rows]`. The `'starter'` literal is a CONSTANT (not user input) so it is
    a `$`-free literal per the file's `$N`-only rule — do NOT f-string any user value.
    Add a keyword-only `owned_only: bool = False` param to `list_published_workflows` (mirror the `is_golden_run=False`
    keyword-only precedent). When True, the predicate becomes `status='published' AND created_by=$1` (drops the bare
    `is_global`); when False, keep the EXACT current `status='published' AND (is_global=true OR created_by=$1)`. The
    `project_folder_id` AND-append and `ORDER BY name` tail apply to both branches. Default False keeps every existing
    caller byte-identical (D-143-2b / Pitfall 3). Do NOT touch `create_workflow_definition`.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -m pytest backend/tests/unit/test_starter_workflows.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `test_starters_query_excludes_scaffolds` GREEN: starters query returns the seeded curated row, none of the 5 scaffolds.
    - `test_published_owned_only_and_default` GREEN: owned_only=True → mine-only; owned_only=False (default) → still returns globals.
    - `git diff` shows `create_workflow_definition` untouched; no user value interpolated into SQL (grep the new code — only `$N` bindings for user_id).
  </acceptance_criteria>
  <done>Both db-layer tests green; default published path provably unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GET /workflows/starters + scope=mine on GET /workflows/published</name>
  <files>backend/app/api/workflows.py</files>
  <read_first>
    - backend/app/api/workflows.py: `get_published_workflows` (104-139), `_coerce_definition` (43-63), the `/drafts`
      static-segment route (254-263), `create_draft`'s 409 mapping (225-251).
    - backend/tests/integration/test_workflows_routes.py (from Plan 01) — the route assertions to satisfy.
    - .planning/phases/143-starter-workflow-library-stretch/143-PATTERNS.md § api/workflows.py.
  </read_first>
  <behavior>
    - `GET /workflows/starters` → `list_starter_workflows(pool)` mapped through `PublishedWorkflow` + `_coerce_definition`; unscoped.
    - `GET /workflows/published?scope=mine` → `list_published_workflows(..., owned_only=True)`; no scope / other value → default (globals kept).
  </behavior>
  <action>
    Add `GET /workflows/starters` (`get_starter_workflows`) as an EXPLICIT static segment declared BEFORE any
    `/{definition_id}` route (mirror the `/drafts` precedent) so it is never shadowed. Body: `pool = await get_pg_pool()`;
    rows = `await list_starter_workflows(pool)`; return `[PublishedWorkflow(id=r["id"], slug=r["slug"], name=r["name"],
    definition=_coerce_definition(r.get("definition"))) for r in rows]`; `response_model=list[PublishedWorkflow]`;
    `Depends(get_current_user)`. Add a `scope: str | None = Query(None)` param to `get_published_workflows` and thread
    `owned_only=(scope == "mine")` into `list_published_workflows`. Leave `create_draft` (`POST /workflows`) and its 409
    mapping UNCHANGED — the fresh-copy fork uses that route as-is. Import `list_starter_workflows` from `app.db.workflows`.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -m pytest backend/tests/integration/test_workflows_routes.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `GET /workflows/starters` test GREEN (returns the seeded starters); `?scope=mine` test GREEN (narrows); fork-409 test GREEN.
    - `/starters` is registered before `/{definition_id}` (route-order check: it resolves, not a 422 UUID-parse of "starters").
    - `create_draft` diff-clean; no new fork endpoint added.
  </acceptance_criteria>
  <done>Two routes shipped; integration backstops green; fork route untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (GET shelf) | `scope` query param + the starters read cross here; only world-readable is_global published rows are returned. |
| API → DB (SQL predicate) | The `category='starter'` literal + `$N` user bindings cross here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-01 | Information disclosure | `list_starter_workflows` | mitigate | Query returns ONLY `is_global=true` published rows (world-readable by the mig-056 SELECT policy) filtered to `category='starter'` — no private row can appear. |
| T-143-04 | Information disclosure | `owned_only` param | mitigate | Additive default-OFF param NARROWS the Published shelf only; the default (globals OR mine) is byte-identical for the picker/run-soul/threads.py (Task 1 test asserts default symmetry). |
| — (V5) | Tampering/Injection | SQL predicate | mitigate | `'starter'` is a constant literal; the only user value (`user_id`) binds as `$1`; `scope` is compared in Python (`== "mine"`), never interpolated. |
| T-143-SC | Tampering (supply chain) | package installs | accept | No package installs (RESEARCH § Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `pytest backend/tests/unit/test_starter_workflows.py backend/tests/integration/test_workflows_routes.py -q` green.
- Wave-merge regression: `pytest backend/tests -k workflows -q` shows no new failures (Pitfall 3 default-path guard).
</verification>

<success_criteria>
- Curated shelf query + scoped de-dupe live; the shared helper's default is provably unchanged.
</success_criteria>

<output>
Create `.planning/phases/143-starter-workflow-library-stretch/143-02-SUMMARY.md` when done.
</output>
</output>
