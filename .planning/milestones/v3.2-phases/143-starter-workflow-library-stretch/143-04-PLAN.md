---
phase: 143-starter-workflow-library-stretch
plan: 04
type: execute
wave: 2
depends_on: [143-01, 143-02]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/pages/WorkflowsPage.tsx
autonomous: true
requirements: [WF-01]

must_haves:
  truths:
    - "The Workflows page shows a curated Starters shelf at the TOP fed by `listStarterWorkflows()` → `GET /workflows/starters`, rendering each starter with the existing `PublishedCard` + `WorkflowSoul(scale=\"card\")` chrome plus a small 'Starter'/'Official' chip — no new card component, G-2 waived (D-143-8)"
    - "'Use this starter' forks a FRESH personal copy (D-143-1/1a): `onUseStarter` mints a NEW suffixed slug `<slug>-<shortHash>` + `version:1` (NOT the same-slug v(N+1) Tweak) via `createWorkflowDraft` → `POST /workflows` (server forces is_global=false/draft/created_by), then opens the Builder; the published starter row is never mutated; a 409 hash collision retries with a fresh hash (Pitfall 5)"
    - "Shelf order becomes Starters → Published → Drafts & seeds — a JSX section REORDER that folds BUG-260628-01 (runnable no longer buried under drafts), Build-card kept discoverable (D-143-5, Pitfall 6)"
    - "The Workflows-page Published shelf calls `listPublishedWorkflows(scope='mine')` so scaffolds + starters stop double-rendering; the composer picker + WorkspacePanel run-soul keep calling it WITHOUT scope (globals preserved — D-143-2a, D-143-2b)"
    - "The forked object carries `category` safely because Plan 01 added the field — no 422 on the fork round-trip (D-143-2, Shared Pattern 1)"
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "listStarterWorkflows() + scope option on listPublishedWorkflows"
      contains: "listStarterWorkflows"
    - path: "frontend/src/pages/WorkflowsPage.tsx"
      provides: "Starters shelf (top), onUseStarter fork, section reorder, Published mine-only, Starter chip"
      contains: "onUseStarter"
  key_links:
    - from: "frontend/src/pages/WorkflowsPage.tsx onUseStarter"
      to: "createWorkflowDraft"
      via: "fresh-copy fork (new slug + v1)"
      pattern: "createWorkflowDraft"
    - from: "frontend/src/pages/WorkflowsPage.tsx Starters shelf"
      to: "listStarterWorkflows"
      via: "fetch on mount"
      pattern: "listStarterWorkflows"
    - from: "frontend/src/pages/WorkflowsPage.tsx Published shelf"
      to: "listPublishedWorkflows(scope='mine')"
      via: "owned_only narrowing"
      pattern: "scope"
---

<objective>
Build the user-facing shelf: a curated Starters gallery on top of the Workflows page, a
"Use this starter" fresh-copy fork, the BUG-260628-01 section reorder, and the Published-shelf
de-dupe. All on existing card/soul chrome (no new component; G-2 waived) plus one "Starter" chip.

The fork is a sibling of `onTweak` with exactly two deltas: a new suffixed slug and `version:1`
(instead of same-slug v(N+1)) — required because `UNIQUE(slug,version)` is global, so two users
forking a shared starter can't both mint the same slug/version. Turns the Plan 01 frontend
backstops GREEN.

Purpose: deliver the WF-01 user experience (fork a starter → own it) + fold the sort bug.
Output: `listStarterWorkflows`, `scope` option, Starters shelf, `onUseStarter`, section reorder, chip.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/143-starter-workflow-library-stretch/143-PATTERNS.md
@.planning/phases/143-starter-workflow-library-stretch/143-RESEARCH.md
@.planning/reported-bugs/BUG-260628-01.md

<interfaces>
<!-- Extracted contracts — reuse directly, no exploration. -->
From frontend/src/lib/api.ts:
- `listPublishedWorkflows` (1203-1218): fetches `/workflows/published`, appends `projectFolderId` to the URL (1212-1214),
  returns `PublishedWorkflow[]`. `PublishedWorkflow` (1178) = { id, slug, name, definition } — NO created_by/is_global.
- `createWorkflowDraft` (3076-3089): `POST /workflows`, body = the definition JSON, returns { id, ... }. UNCHANGED by this plan.
From frontend/src/pages/WorkflowsPage.tsx:
- `onTweak` (151-177): the fork-into-Builder flow — `createWorkflowDraft` → `refetchDrafts` → `setBuilderInitial({definition, draftId, label})` → `setPageView("builder")`. Same-slug + `version: N+1`.
- shelf `<section>`s (350-413): Drafts (350-381) currently ABOVE Published (383-413); each uses `data-testid="…-shelf"` + heading + `grid grid-cols-1 gap-3 md:grid-cols-2` + `.map(PublishedCard)`.
- `PublishedCard` (514-573): the `published` status pill (543-545), `<WorkflowSoul def={def} scale="card">` (549), the `⑂ Tweak` button (552-561).
- `refetchPublished` (108-122): the `publishedSeqRef` latest-wins fetch pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: api.ts — listStarterWorkflows() + scope option on listPublishedWorkflows</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts: `listPublishedWorkflows` (1203-1218) incl. the `projectFolderId` URL-append (1212-1214),
      the `PublishedWorkflow` type (1178), `getAuthHeaders`, `API_BASE`; `createWorkflowDraft` (3076-3089).
    - .planning/phases/143-starter-workflow-library-stretch/143-PATTERNS.md § api.ts.
  </read_first>
  <action>
    Add `export async function listStarterWorkflows(signal?): Promise<PublishedWorkflow[]>` — a clone of
    `listPublishedWorkflows` with NO query params, fetching `${API_BASE}/workflows/starters`. Thread an optional
    `scope` into `listPublishedWorkflows` (extend its signature with a `{ scope?: "mine" }`-style option, keeping the
    existing `projectFolderId` argument shape backward-compatible) that appends `?scope=mine` (alongside the existing
    projectFolderId append) ONLY when passed. Callers that omit `scope` (the composer picker, WorkspacePanel run-soul)
    stay byte-identical — do NOT change their call sites here (D-143-2b). `createWorkflowDraft` is UNCHANGED — the fork
    reuses it as-is.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "node_modules" | (! grep -Ei "api\.ts.*error") && echo "TS OK"</automated>
  </verify>
  <acceptance_criteria>
    - `listStarterWorkflows` exported, returns `PublishedWorkflow[]`, hits `/workflows/starters`, no query params.
    - `listPublishedWorkflows` accepts an optional `scope` that appends `?scope=mine` only when set; the no-scope signature still type-checks for existing callers.
    - `tsc --noEmit` reports no new errors in api.ts.
  </acceptance_criteria>
  <done>Client has a starters fetch + an opt-in scope; existing callers unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WorkflowsPage.tsx — Starters shelf, fork, section reorder, mine-only, chip</name>
  <files>frontend/src/pages/WorkflowsPage.tsx</files>
  <read_first>
    - frontend/src/pages/WorkflowsPage.tsx: `onTweak` (151-177), the shelf `<section>`s (350-413),
      `PublishedCard` (514-573) incl. the `published` pill (543-545) + `WorkflowSoul` (549) + `⑂ Tweak` button (552-561),
      `refetchPublished` (108-122).
    - frontend/src/pages/WorkflowsPage.test.tsx (Plan 01) — the exact assertions (starters-shelf, use-starter,
      `/^risk-register-[a-z0-9]{6}$/`, version===1, section order) this task must satisfy.
    - .planning/phases/143-starter-workflow-library-stretch/143-PATTERNS.md § WorkflowsPage.tsx + § Starter card chip.
  </read_first>
  <behavior>
    - Starters shelf (data-testid="starters-shelf") renders the mocked starter card with a "Starter"/"Official" chip and a `use-starter` control.
    - Clicking `use-starter` calls createWorkflowDraft once with slug matching `/^<slug>-[a-z0-9]{6}$/`, version===1, status==="draft"; never calls the update/tweak path.
    - DOM order: starters-shelf precedes published-shelf precedes the drafts shelf.
    - Published shelf fetch uses scope="mine".
  </behavior>
  <action>
    Add a `starters` state + a fetch via `listStarterWorkflows()` on mount (mirror `refetchPublished` minus the
    project-filter branch — starters are global/unscoped; a race guard is likely unnecessary for a single unscoped fetch).
    Add a Starters `<section data-testid="starters-shelf">` mirroring the Published section structure (heading + the same
    grid + `.map`), rendered as the FIRST shelf. For the starter card, reuse `PublishedCard`/`WorkflowSoul(scale="card")`
    but (a) add a small "Starter"/"Official" chip cloned from the `published` pill chrome (543-545), and (b) swap the
    `⑂ Tweak` affordance for a `data-testid="use-starter"` button labeled "Use this starter" wired to `onUseStarter`.
    Add `onUseStarter` as a sibling of `onTweak`: `shortHash = Math.random().toString(36).slice(2,8)`;
    `forked = { ...starter.definition, slug: `${starter.slug}-${shortHash}`, version: 1, status: "draft" }`;
    `createWorkflowDraft(forked)` → `refetchDrafts()` → `setBuilderInitial({ definition: forked, draftId: created.id,
    label: `From starter · ${starter.name}` })` → `setPageView("builder")`; on a 409 (astronomically unlikely hash
    collision) retry once with a fresh hash. The `forked` object may carry `category:"starter"` — that is SAFE now
    (Plan 01 added the field; do NOT strip it).
    REORDER the shelf `<section>`s to Starters → Published → Drafts & seeds (fold BUG-260628-01 — a JSX section move, NOT a
    within-list sort). Keep the Build-card discoverable in the Drafts shelf (optionally add a header "Build a workflow"
    affordance — minor, Claude's discretion).
    Point the Published shelf fetch at the mine-only scope: in `refetchPublished`, call `listPublishedWorkflows` with
    `scope: "mine"` so scaffolds + starters stop double-rendering (D-143-2a end state). Do NOT change any OTHER caller of
    `listPublishedWorkflows`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/WorkflowsPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All Plan 01 frontend backstops GREEN: Starters shelf renders + chip + use-starter; fork mints `<slug>-<6hash>` + version===1 + status draft, no UPDATE call; section order Starters→Published→Drafts.
    - `refetchPublished` passes `scope: "mine"`; no other `listPublishedWorkflows` call site changed (grep confirms a single scope="mine" usage).
    - Pre-existing WorkflowsPage tests (Tweak, shelves) still pass; no new tsc errors.
  </acceptance_criteria>
  <done>Starters shelf + fork + reorder + mine-only + chip shipped; frontend backstops green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (fork) | `onUseStarter` POSTs a forked definition; the server (unchanged) forces is_global=false/draft/created_by. |
| client → API (shelf reads) | starters + Published (scope=mine) reads cross here; only world-readable/owned rows return. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-02 | Info disclosure / Tampering | fork / starter row | mitigate | Fork is a fresh owner-bound INSERT (`createWorkflowDraft` → server forces created_by=caller/is_global=false/draft); the published starter is never UPDATEd (frontend test asserts no update call; DB immutability trigger backs it). |
| T-143-04 | Information disclosure | Published shelf de-dupe | mitigate | ONLY `refetchPublished` opts into `scope="mine"`; the composer picker + WorkspacePanel keep the default (globals) — grep-verified single scope usage (Pitfall 3). |
| — (V5) | Tampering | fork body `category` | mitigate | `category` is now a declared optional field (Plan 01); the body still parses under `extra="forbid"` — no junk key smuggled. |
| T-143-SC | Tampering (supply chain) | package installs | accept | No npm installs (RESEARCH § Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `npx vitest run src/pages/WorkflowsPage.test.tsx` all green (Starters render, fork new-slug+v1, section order).
- Wave-merge regression: `npx vitest run src/components/panel/WorkspacePanel.test.tsx` (if present) + confirm the composer picker call site is unchanged (Pitfall 3).
- Manual: the live page shows Starters on top with the chip + "Use this starter" (full run UAT is Plan 05).
</verification>

<success_criteria>
- A user can see curated starters on top and fork one into an owned draft; runnable rows no longer buried; Published shows only mine.
</success_criteria>

<output>
Create `.planning/phases/143-starter-workflow-library-stretch/143-04-SUMMARY.md` when done.
</output>
