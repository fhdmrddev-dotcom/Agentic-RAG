---
phase: 133-eval-runner-with-skill-vs-without-skill
plan: 04
type: execute
wave: 3
depends_on: ["133-03"]
files_modified:
  - backend/app/api/evals.py
  - backend/app/main.py
  - backend/tests/test_eval_runner.py
autonomous: true
requirements: [EVAL-02]

must_haves:
  truths:
    - "Per D-06, POST /skills/{skill_id}/evals/runs owner-verifies the skill, resolves the latest skill_version_id (owner-scoped), reads owner-scoped test cases, mints one run_id, inserts the eval_runs row + a companion public.runs row (same run_id), spawns the bounded background job, and returns {run_id} immediately (202, non-blocking)"
    - "Per RESEARCH Pattern 3, the single run_id = eval_runs.id = public.runs.run_id = run:{run_id} buffer key, so getActiveRuns / subscribeToRun / DELETE /runs/{id} work unchanged with zero new frontend stream code (reattach + cancel for free)"
    - "Per SC#3, GET /skills/{skill_id}/evals/runs/{run_id} returns the eval_run + its eval_results from the DB and remains readable after the Redis buffer TTL-expires; GET /skills/{skill_id}/evals/runs lists a skill's runs owner-scoped newest-first"
    - "Per SC#4 owner-scoping, user B cannot launch/stream/read user A's eval run — every route gates on app-code .eq(user_id) and 404s (never 403) on a cross-user miss"
    - "Per D-01, the POST validates provider/model against get_model_capability and rejects unknown models"
  artifacts:
    - path: "backend/app/api/evals.py"
      provides: "owner-scoped eval router: POST kickoff, GET results, GET list; companion runs row insert; in-flight claim"
      exports: ["router"]
    - path: "backend/app/main.py"
      provides: "evals.router mounted alongside skill_tuner / skill_test_cases"
      contains: "evals.router"
  key_links:
    - from: "evals.py POST"
      to: "public.runs companion row (insert_run, same run_id)"
      via: "Pattern 3 reattach/cancel reuse"
      pattern: "insert_run"
    - from: "evals.py routes"
      to: "owner gate"
      via: ".eq(user_id) / 404-not-403"
      pattern: "eq.*user_id"
---

<objective>
Ship the owner-scoped eval router (modeled on `skill_tuner.py`): POST kicks off the Plan 03 background job and returns a `run_id` immediately, GET endpoints expose the durable readout, and a companion `public.runs` row makes reattach + cancel work through the EXISTING chat-run machinery with zero new frontend stream code (RESEARCH Pattern 3). Then complete the integration tests (persist-after-buffer-expiry, reattach-via-runs-row, cross-user 404).

Purpose: This is the EVAL-02 control surface. One `run_id` doubles as `eval_runs.id`, the `run:{run_id}` buffer key, and the companion `public.runs.run_id` so `getActiveRuns`/`subscribeToRun`/`DELETE /runs/{id}` are reused verbatim. Every route owner-verifies (V4) and validates the model (V5).
Output: `evals.py` router + `main.py` mount + the route-level integration tests in `test_eval_runner.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-eval-runner-with-skill-vs-without-skill/133-RESEARCH.md
@.planning/phases/133-eval-runner-with-skill-vs-without-skill/133-PATTERNS.md
@.planning/phases/133-eval-runner-with-skill-vs-without-skill/133-VALIDATION.md

<interfaces>
<!-- Route contracts + reuse map. From RESEARCH §Backend Conventions / PATTERNS.md. -->
Routes (mirror skill_tuner prefix style under /skills/{skill_id}/...; stream/cancel reuse /runs/*):
  POST   /skills/{skill_id}/evals/runs        body=StartEvalRunBody -> {run_id} (202)
  GET    /skills/{skill_id}/evals/runs/{run_id}  -> eval_run + eval_results (DB readout)
  GET    /skills/{skill_id}/evals/runs           -> list runs owner-scoped newest-first
  GET    /runs/{run_id}/stream?since=          REUSED verbatim (runs.py) — eval_* events
  DELETE /runs/{run_id}                        REUSED verbatim (runs.py) — cancel
Reuse: _fetch_owned_or_global_skill (skill_tuner.py:177-206 — service-role .or_(own,global), 404 on miss, run_in_threadpool); the stricter owned-only _verify_owned_skill (skill_test_cases.py:49-63) for the create path. insert_run(pool, run_id, thread_id, user_id, status="streaming", model, provider) (db/runs.py:26-66). get_model_capability (config.py:489).
Mount: append `evals` to `from app.api import (...)` (main.py:423) + `app.include_router(evals.router)` after skill_test_cases.router (main.py:446).
The bounded job lives in eval_runner_service (Plan 03); the POST spawns it via asyncio.create_task and registers RUN_TASKS[run_id] for cancel parity.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: evals.py router + companion runs row + main.py mount</name>
  <files>backend/app/api/evals.py, backend/app/main.py</files>
  <read_first>
    - backend/app/api/skill_tuner.py:40-62 (import set + router prefix), :177-206 (_fetch_owned_or_global_skill owner gate), :210-240 (emit helpers), :650-772 (POST route + ZADD + spawn + return {run_id} immediately), :775-814 (stream-route shape — but prefer reusing GET /runs/{run_id}/stream verbatim per Pattern 3)
    - backend/app/api/skill_test_cases.py:49-63 (owned-only _verify_owned_skill for the create path) + the owner-scoped reads to mirror
    - backend/app/db/runs.py:26-66 (insert_run — the companion runs row; same run_id)
    - backend/app/main.py:423-446 (router import + mount pattern)
    - backend/app/services/eval_runner_service.py (the job entrypoint from Plan 03 to spawn)
    - .planning/phases/133-eval-runner-with-skill-vs-without-skill/133-PATTERNS.md lines 24-66 + 204-214 (router spec + mount); 133-RESEARCH.md lines 263-279 (endpoint shapes + provider resolution)
  </read_first>
  <action>
    Create `backend/app/api/evals.py` with `router = APIRouter(prefix="/skills", tags=["skill-evals"])`. Implement POST `/skills/{skill_id}/evals/runs`: owner-verify the skill (owned-only for the create path), validate body.model/provider via `get_model_capability` (reject unknown -> 400/422), resolve the LATEST owner-scoped skill_version (max version_number) and capture its name/description snapshot, read owner-scoped skill_test_cases for the skill (reject if zero cases), mint `run_id = uuid4()`, claim the atomic Redis in-flight `eval_inflight:{skill_id}` (SET NX -> 409 on contention), insert the `eval_runs` row (status=running, case_count, skill_version_id, provider, model, user_id from current_user), insert a companion `public.runs` row via `insert_run` keyed by the SAME run_id (status streaming, the eval thread_id, model, provider), ZADD the runs:active / runs_by_thread sorted sets, spawn the Plan-03 `run_eval_job` via asyncio.create_task + register RUN_TASKS[run_id], and return `{run_id}` immediately (202, non-blocking — D-06). Implement GET `/skills/{skill_id}/evals/runs/{run_id}`: owner-verify, return the eval_run + its eval_results from the DB (the durable readout — survives buffer TTL). Implement GET `/skills/{skill_id}/evals/runs`: owner-scoped list newest-first. Do NOT add a bespoke stream or cancel route — reuse GET /runs/{run_id}/stream and DELETE /runs/{run_id} verbatim (the companion runs row makes them work). Every route: user_id from current_user never body; 404 (never 403) on cross-user miss; wrap supabase-py in run_in_threadpool; truncate any error string to <=200 chars. Then mount in `main.py`: append `evals` to the `from app.api import (...)` tuple (line ~423) and add `app.include_router(evals.router)` after `skill_test_cases.router` (line ~446).
  </action>
  <verify>
    <automated>cd backend && . venv/Scripts/activate && python -c "from app.main import app; paths=[r.path for r in app.routes]; assert any('/evals/runs' in p for p in paths), paths; print('mounted')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from app.main import app; ..."` confirms `/skills/{skill_id}/evals/runs` is mounted (exits 0, prints "mounted")
    - `grep -c "insert_run" backend/app/api/evals.py` >= 1 (companion runs row) and `grep -c "get_model_capability" backend/app/api/evals.py` >= 1 (model validation)
    - `grep -c "evals.router" backend/app/main.py` >= 1
    - `grep -c "status_code=404\|HTTPException(404\|status.HTTP_404" backend/app/api/evals.py` >= 1 (404-not-403 on cross-user miss)
    - No bespoke `@router.delete` cancel or `/stream` route in evals.py (`grep -ci "EventSourceResponse\|@router.delete" backend/app/api/evals.py` == 0 — reuse runs.py)
  </acceptance_criteria>
  <done>Router exposes POST kickoff + GET results/list, writes the companion runs row for free reattach/cancel, validates model + owner on every route, and is mounted in main.py.</done>
</task>

<task type="auto">
  <name>Task 2: integration tests — persist-after-expiry, reattach, cross-user 404</name>
  <files>backend/tests/test_eval_runner.py</files>
  <read_first>
    - backend/tests/test_skill_tuner_routes.py:19-75 (route-level harness: ASGITransport, dependency_overrides, _FakeRedis) — reuse the scaffold Plan 03 Task 1 already added to this file
    - backend/tests/test_132_test_cases.py:35-36 (OWNER/OTHER_USER) for cross-user-404
    - .planning/phases/133-eval-runner-with-skill-vs-without-skill/133-VALIDATION.md lines 47-50 (the exact test names + commands)
    - backend/app/api/runs.py:386-407 (the runs-row owner check the reattach path relies on) + frontend reattach contract in 133-PATTERNS.md:217-223
  </read_first>
  <action>
    Add three route/integration tests to `backend/tests/test_eval_runner.py`: `test_results_persist_after_buffer_expiry` (after a run completes, simulate run:{run_id} buffer gone/TTL-expired in _FakeRedis, then GET /skills/{skill_id}/evals/runs/{run_id} still returns the full eval_results set from the DB — SC#3); `test_reattach_via_runs_row` (the companion public.runs row makes the eval run discoverable by the runs-by-thread query that getActiveRuns uses, and the stream GET owner-checks the runs row and replays buffered eval_* events from since=0 — SC#3); `test_cross_user_404` (OTHER_USER calling POST/GET/GET-list against OWNER's skill or run_id gets 404, never 403 — SC#4). Mock the inner run_agent_loop (reuse the Plan 03 fixture) so no real LLM is hit.
  </action>
  <verify>
    <automated>cd backend && . venv/Scripts/activate && pytest tests/test_eval_runner.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_eval_runner.py -x` exits 0 (all tests green: two_results_per_case, sse_vocabulary, results_persist_after_buffer_expiry, reattach_via_runs_row, cross_user_404)
    - `test_cross_user_404` asserts status_code == 404 (not 403) for OTHER_USER on all three eval routes
    - `test_results_persist_after_buffer_expiry` asserts the GET readout is non-empty after the Redis buffer is cleared
  </acceptance_criteria>
  <done>Full test_eval_runner.py suite green — SC#1/SC#2/SC#3 + cross-user 404 all proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → eval router | Untrusted skill_id/run_id path params + provider/model body cross here |
| eval router → public.runs / Redis sorted sets | The companion row + sorted sets feed the reused reattach/cancel machinery |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-01 | Information Disclosure | GET run/results/list + reused stream | mitigate | Every route .eq(user_id) owner-verify; 404-not-403 on cross-user; the companion runs row already 404s cross-user in runs.py:397-407 (proven by test_cross_user_404) |
| T-133-03 | Tampering / EoP | forged skill_id / user_id in body | mitigate | skill_id from path + owner-verify; user_id from current_user never body (T-132-07); run↔skill membership checked before stream |
| T-133-02 | Denial of Service | repeat POST kickoffs | mitigate | Atomic Redis SET NX in-flight claim (one eval per skill -> 409); single provider/run; reject runs with zero cases |
| T-133-08 | Input Validation | unknown provider/model in body | mitigate | Validate against get_model_capability; reject unknown (V5) — prevents narrate-as-text + bad routing |
| T-133-SC | Tampering | package installs | accept | No installs in this phase |
</threat_model>

<verification>
- Full test_eval_runner.py green (SC#1/SC#2/SC#3 + cross-user 404).
- /evals/runs routes mounted; companion runs row written; model validated on POST.
- No bespoke stream/cancel route (reattach + cancel reuse runs.py).
</verification>

<success_criteria>
A user can launch a run (202 + run_id), watch it via the reused /runs/{id}/stream, read the durable result set after the buffer expires, and cancel via DELETE /runs/{id}; cross-user access 404s on every route.
</success_criteria>

<output>
Create `.planning/phases/133-eval-runner-with-skill-vs-without-skill/133-04-SUMMARY.md` when done.
</output>
