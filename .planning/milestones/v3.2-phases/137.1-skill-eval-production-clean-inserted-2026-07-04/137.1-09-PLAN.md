---
phase: 137.1-skill-eval-production-clean-inserted-2026-07-04
plan: 09
type: execute
wave: 5
depends_on: ["137.1-02", "137.1-07", "137.1-08"]
files_modified:
  - frontend/src/components/skills/studio/RunHistory.tsx
  - frontend/src/components/skills/studio/RunCaseDetail.tsx
  - frontend/src/components/skills/studio/RunHistory.test.tsx
  - frontend/src/components/skills/studio/RunCaseDetail.test.tsx
autonomous: true
requirements: [EVAL-05]
must_haves:
  truths:
    - "D-07 UI: the matrix card FOOTER renders per-config mean±stddev from run HISTORY (stddev only at >=2 runs; a first run shows 'first run — no spread yet') + a Δ skill-lift column — rendered VERBATIM from the Plan 07 aggregate endpoint (no client-side stats)"
    - "D-08 UI: deterministic analyst notes render as tagged fixed-phrasing rows (non-discriminating / flaky variance / time-token) served by the backend — never LLM prose, never client-derived"
    - "EVAL-05d UI: the judge's case_feedback renders INLINE under the flagged case as a violet dashed-left '◇ Judge on this case' advisory block — visually distinct from PASS/FAIL, captioned 'feedback only — never blocks the run', and NEVER in the rollup math"
    - "EVAL-05e UI: per-arm wall-clock duration renders as '⏱ {ms}' beside the existing token line — metadata, never a verdict"
  artifacts:
    - path: "frontend/src/components/skills/studio/RunHistory.tsx"
      provides: "aggregation footer (mean±σ grid + Δ column) + deterministic analyst-note rows, rendered from the aggregate endpoint"
      contains: "no spread yet"
    - path: "frontend/src/components/skills/studio/RunCaseDetail.tsx"
      provides: "inline violet '◇ Judge on this case' case_feedback block + ⏱ duration beside tokens"
      contains: "Judge on this case"
  key_links:
    - from: "RunHistory aggregation footer"
      to: "getEvalAggregate (api.ts / Plan 07 endpoint)"
      via: "server-computed per-config stats + notes rendered verbatim"
      pattern: "getEvalAggregate|aggregate"
    - from: "RunCaseDetail case_feedback block"
      to: "EvalResult.case_feedback (mig 085 / Plan 02)"
      via: "violet advisory render, excluded from verdictBadge + rollup"
      pattern: "case_feedback"
---

<objective>
Complete the Studio finalized-display surface (058-A footer + 059-A advisory): the matrix card's aggregation FOOTER (per-config mean±stddev/Δ + deterministic analyst notes, rendered VERBATIM from the Plan 07 backend endpoint), and the per-case advisory block in RunCaseDetail — the judge's `case_feedback` as a violet "◇ Judge on this case" note plus per-arm `⏱ duration` beside tokens. All honesty locks inherited: notes are server-authored fixed phrasing, case_feedback is advisory (violet, never PASS/FAIL, never in rollup), duration is metadata.

Purpose: Close the aggregation + case-critique + duration display, the last of the sketch-locked surfaces.
Output: RunHistory aggregation footer + analyst notes; RunCaseDetail case_feedback + duration; tests.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-CONTEXT.md
@.claude/skills/sketch-findings-agentic-rag/references/eval-production-clean.md

<interfaces>
<!-- Shipped seams + the sketch locks (058-A footer, 059-A advisory). -->
RunHistory.tsx: the matrix card (Plan 08) exists; add the FOOTER below its sub-rows. rollupSummary/tone (:55-75) — neutral never red, inherit it.
getEvalAggregate(skillId) (api.ts / Plan 07): returns per-config {run_count, with_mean, without_mean, with_stddev|null, delta, analyst_notes[]}. Render verbatim — no client stats.
RunCaseDetail.tsx: pure presentational leaf. verdictBadge (:35-46) — graded→PASS/FAIL, not_measured→"not measured" NEUTRAL, judge_error→"judge error"; DO NOT touch it (honesty lock). Token line (:170-174): `{in} → {out} tok` — append `· ⏱ {duration_ms}ms` here.
EvalResult.case_feedback / duration_ms (Plan 01 types / Plan 02 backend).
Sketch locks: 058-A footer = mean±σ grid + Δ column + tagged analyst notes; "first run — no spread yet". 059-A: case_feedback = violet dashed-left "◇ Judge on this case", captioned "feedback only — never blocks the run", visually distinct from PASS/FAIL, NEVER in rollup; violet = info (green/red reserved for verdicts). Pitfall 3: case_feedback must not read as a fail.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server aggregate/verdict → UI render | Stats + notes + verdicts render verbatim from the server; no client re-derivation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137.1-U2 | Tampering (misleading stats) | aggregation footer | mitigate | Rendered verbatim from the Plan 07 endpoint; stddev only when the server provides it (n>=2); "first run — no spread yet" otherwise; notes are server fixed-phrasing — no client stats/prose. |
| T-137.1-U3 | Tampering (fabricated fail) | case_feedback render (Pitfall 3) | mitigate | 059-A violet advisory vocabulary, dashed-border, captioned never-blocks; stored/read separately from verdict_*; excluded from rollup math; verdictBadge untouched. |
| T-137.1-SC | Tampering | npm installs | mitigate | No new packages. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Matrix card aggregation footer + deterministic analyst notes (058-A / D-07/D-08)</name>
  <files>frontend/src/components/skills/studio/RunHistory.tsx, frontend/src/components/skills/studio/RunHistory.test.tsx</files>
  <read_first>
    - frontend/src/components/skills/studio/RunHistory.tsx (the Plan 08 matrix card + rollupSummary/tone at :55-75)
    - frontend/src/lib/api.ts (getEvalAggregate + the EvalAggregate type — Plan 01/07)
    - .claude/skills/sketch-findings-agentic-rag/references/eval-production-clean.md (058-A footer anatomy + "×1 run per click — spread accumulates from history" + Pitfall 4)
  </read_first>
  <action>
    Add the aggregation FOOTER to the matrix card: fetch `getEvalAggregate(skillId)` and render, per config, a mean±σ grid (mean judge score; σ ONLY when the server returns a non-null with_stddev at run_count>=2; otherwise render "first run — no spread yet") + a Δ column (with−without skill-lift, from the server delta). Below it, render the server `analyst_notes` as tagged fixed-phrasing rows (non-discriminating / flaky-variance / time-token) — render them VERBATIM; do NOT compute any stats or phrasing client-side. Inherit the neutral-never-red rollup tone. Extend RunHistory.test.tsx: a 1-run config shows "first run — no spread yet" (no numeric σ); a 2+-run config shows a numeric σ; the analyst-note rows render from the server payload (mock the aggregate) and no stats are computed in the component.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/skills/studio/RunHistory.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The footer renders mean±σ + Δ per config from `getEvalAggregate`; σ shows only when the server provides it (else "first run — no spread yet").
    - Analyst notes render verbatim from the server payload (`grep -n "Math\.\|stddev\|pstdev" RunHistory.tsx` shows no client stats computation for the footer).
    - Test proves the 1-run vs 2+-run stddev behavior + verbatim note rendering.
  </acceptance_criteria>
  <done>Aggregation footer + analyst notes render verbatim from the backend; spread honesty preserved; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Inline violet case_feedback + ⏱ duration in RunCaseDetail (059-A / EVAL-05d/e)</name>
  <files>frontend/src/components/skills/studio/RunCaseDetail.tsx, frontend/src/components/skills/studio/RunCaseDetail.test.tsx</files>
  <read_first>
    - frontend/src/components/skills/studio/RunCaseDetail.tsx (:35-46 verdictBadge — DO NOT touch; :170-174 the token line to extend)
    - .claude/skills/sketch-findings-agentic-rag/references/eval-production-clean.md (059-A: violet dashed-left "◇ Judge on this case", captioned "feedback only — never blocks the run", visually distinct from PASS/FAIL; Pitfall 3)
    - frontend/src/components/skills/studio/RunCaseDetail.test.tsx (existing verdict-badge assertions)
  </read_first>
  <action>
    When an EvalResult carries a non-empty `case_feedback`, render an inline advisory block under the flagged case header: a violet (`--color-accent-violet`) dashed-left-border "◇ Judge on this case" block containing the feedback text + a small caption "feedback only — never blocks the run". It MUST be visually distinct from the PASS/FAIL verdict chips (never amber/red, never a verdict tone) and MUST NOT enter any rollup/pass count. Append `· ⏱ {duration_ms}ms` to the existing token line (:170-174) when `duration_ms != null` — duration is metadata, next to tokens, never a verdict. Do NOT modify `verdictBadge` (the honesty lock). Extend RunCaseDetail.test.tsx: a result with case_feedback renders the violet advisory block (not a verdict tone) and does not change the verdict badge; a result with duration_ms renders "⏱" beside tokens; a result with null case_feedback renders no advisory block.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/skills/studio/RunCaseDetail.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Non-empty `case_feedback` renders a violet dashed "◇ Judge on this case" advisory block, captioned never-blocks, visually distinct from PASS/FAIL.
    - `verdictBadge` is unchanged (honesty lock); the advisory block never alters the verdict/rollup.
    - `duration_ms` renders as "⏱" beside the token line; null duration renders nothing extra.
  </acceptance_criteria>
  <done>Per-case advisory feedback + duration render honestly (advisory violet, metadata ⏱) without touching the verdict; tests green.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run src/components/skills/studio` green.
- Aggregation footer + notes verbatim from the server; stddev honesty preserved.
- case_feedback violet/advisory/never-in-rollup; duration = metadata; verdictBadge untouched.
</verification>

<success_criteria>
The matrix card shows honest per-config aggregation (mean±σ/Δ + deterministic notes from the server) and each case surfaces the judge's advisory critique (violet, never a verdict) plus per-arm duration — completing the sketch-locked eval production-clean display.
</success_criteria>

<output>
Create `.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-09-SUMMARY.md` when done.
</output>
