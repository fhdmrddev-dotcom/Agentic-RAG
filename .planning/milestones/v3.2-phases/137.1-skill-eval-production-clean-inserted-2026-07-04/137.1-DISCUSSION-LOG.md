# Phase 137.1: Skill Eval Production-Clean - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-04
**Phase:** 137.1-skill-eval-production-clean
**Areas discussed:** Smoke sweep shape, Matrix run semantics, Ops half (reconciliation + judge knob), Scope extras

---

## Smoke sweep shape

| Option | Description | Selected |
|--------|-------------|----------|
| In-app, via matrix machinery | One click fans a built-in smoke case across all 8 providers using the EVAL-05b matrix machinery; results = live run rows | ✓ |
| Backend script only | Terminal script/pytest, console + JSON report | |
| Both | In-app button + thin CLI wrapper | |

| Option | Description | Selected |
|--------|-------------|----------|
| Built-in synthetic smoke case | Shipped deterministic skill + one case proving ENGINE health only | ✓ |
| Operator picks a real skill/case | Realer but conflates engine health with model quality | |
| Synthetic default, real-skill optional | Both, more surface | |

| Option | Description | Selected |
|--------|-------------|----------|
| Engine-health card in Settings | Per-provider ✓/✗ board + timestamp + Run button; smoke skill hidden from Skills list | ✓ |
| Visible built-in smoke skill | Sweep runs from its Studio; clutters the Skills list | |
| Health strip on Skills page | Ops content on the Skills tab | |

| Option | Description | Selected |
|--------|-------------|----------|
| On-demand + staleness shown | Click to run; board shows last-sweep age | ✓ |
| On-demand + config-change nudge | Extra heuristic on provider/key changes | |
| Scheduled sweeps | Needs scheduler infra that doesn't exist (v3.4) | |

**User's choice:** All four recommended options.

---

## Matrix run semantics

| Option | Description | Selected |
|--------|-------------|----------|
| Explicit gate-feeder pick at launch | ONE config designated (default active provider), "feeds gate" chip; rest analysis-only | ✓ |
| Matrix never feeds the gate | Analysis-only matrices; single runs for the gate | |
| Best passing run feeds it | Permissive but muddies publish evidence | |

| Option | Description | Selected |
|--------|-------------|----------|
| One claim for the whole matrix | Skill lock acquired once per group; arms parallel underneath | ✓ |
| Per provider-config locks | More concurrency, racy gate reads | |

| Option | Description | Selected |
|--------|-------------|----------|
| ×1 per provider, aggregate history | mean±stddev/delta across accumulated runs per config | ✓ |
| Repeat knob (×R) at launch | Immediate stddev, multiplied cost | |
| Both | ×1 default + capped knob | |

| Option | Description | Selected |
|--------|-------------|----------|
| Deterministic rules | Computable flags, fixed plain-language phrasing; judge case_feedback is the LLM critique | ✓ |
| LLM-written summary | Extra judge call per matrix group | |
| Hybrid | Flags + optional LLM paragraph | |

**User's choice:** All four recommended options.

---

## Ops half: reconciliation + judge knob

| Option | Description | Selected |
|--------|-------------|----------|
| Boot-time sweep | Startup flip of stale running rows + stale-stream cleanup; worker-safe | ✓ |
| Boot sweep + staleness watchdog | Also catches hung runs without restart | |
| Watchdog only | Leaves a post-restart window | |

| Option | Description | Selected |
|--------|-------------|----------|
| Eval + chat runs | eval→interrupted, chat→failed; standalone module, no threads.py growth | ✓ |
| Eval runs only | Chat orphans keep accumulating | |

| Option | Description | Selected |
|--------|-------------|----------|
| One shared knob | Surface existing harness_judge_model (drives eval + harness judges) | ✓ |
| Split eval judge | New setting + migration + divergence risk | |

| Option | Description | Selected |
|--------|-------------|----------|
| Registry-validated picker | Only MODEL_CAPABILITIES-known models; shows claude-opus-4-8 default | ✓ |
| Free-text model field | Re-introduces the picker↔registry drift trap | |

**User's choice:** All four recommended options.

---

## Scope: the 2 SEED-100 extras

| Option | Description | Selected |
|--------|-------------|----------|
| Fold both minimal halves | Description-builder 1024 cap + auto-shorten; additive lint warnings; iteration mode OUT; stamp roadmap SC | ✓ |
| Defer both | Keep the 6 original success criteria | |
| Fold lint only | Trivial half only | |

**User's choice:** Fold both minimal halves (recommended).

---

## Claude's Discretion

- Migration shape for matrix groups / gate-feeder flag / case_feedback / duration columns
- Synthetic smoke case content
- Determinate-progress unit mechanics (judge step granularity)
- Matrix launcher provider selection UI (sketch-bounded)
- Engine-health card data query; reconciler staleness predicate + stream disposal
- case_feedback placement in RunCaseDetail

## Deferred Ideas

- Tuner failure-feedback iteration mode (explicitly OUT per SEED-100's hedge)
- Scheduled sweeps (v3.4); config-change re-run nudge; periodic staleness watchdog
- LLM analyst paragraph; dynamic model registry (SEED-088/095); eval role-gating (SEED-099)
- Todo `spike-nl-workflow-authoring` reviewed, not folded (6th consecutive phase)
