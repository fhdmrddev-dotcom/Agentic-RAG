---
phase: 137.1-skill-eval-production-clean-inserted-2026-07-04
plan: 05
subsystem: api
tags: [settings, harness_judge_model, resolve_judge_model, skill_lint, skill_tuner, kebab-case, forced_emit, registry-validation]

# Dependency graph
requires:
  - phase: 133-eval-runner
    provides: "resolve_judge_model (validator_kinds.py:58) — the ONE shared judge resolver reused by the settings API"
  - phase: 123-skill-triggering-quality
    provides: "skill_lint.lint_description (pure, never-blocking) + skill_tuner_service.build_candidates (forced_emit) + skill_builder_model settings wiring (the verbatim mirror precedent)"
provides:
  - "Settings API exposure of the shared harness_judge_model (raw + resolved_harness_judge_model), registry-validated on write — the backend half of the judge-model knob (EVAL-05f); UI picker lands in Plan 10"
  - "skill_lint: additive, never-blocking name_not_kebab portability warning (D-13 ii)"
  - "skill_tuner_service.build_candidates: 1024-char cap + bounded auto-shorten retry on description candidates (D-13 i)"
affects: [137.1-plan-10-settings-ui, skill-studio-settings, judge-model-picker]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Verbatim-mirror an existing settings knob (skill_builder_model) to expose an already-end-to-end field with no new setting/migration"
    - "Registry validation at the settings write boundary: get_model_capability(id).capability_source == 'registry' (mirrors evals.py:165-170)"
    - "Function-local import of the shared resolve_judge_model (ONE source of truth — no second resolver)"
    - "Additive never-blocking lint warning + never-crash cap-with-truncation floor (D-09 posture)"

key-files:
  created:
    - .planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/deferred-items.md
  modified:
    - backend/app/api/settings.py
    - backend/app/services/skill_lint.py
    - backend/app/services/skill_tuner_service.py
    - backend/tests/unit/test_settings.py
    - backend/tests/unit/test_skill_lint.py
    - backend/tests/unit/test_skill_tuner_service.py

key-decisions:
  - "No migration + no second resolver: harness_judge_model already exists in app_settings (config.py:1006) and resolve_judge_model is the single shared resolver (D-11)"
  - "Empty string persists as unset (registry validation only runs on a non-empty value); the resolver then surfaces the effective default (claude-opus-4-8) — D-12"
  - "Registry-validation returns 400 (matching evals.py), not settings.py's 422 confidence-bucket path"
  - "Kebab warning is name-only (fires even with an empty description) and never blocks save; healthy-example test switched to a kebab name to reflect the new rule"
  - "Test path corrected: the existing settings-API test file is backend/tests/unit/test_settings.py (plan referenced backend/tests/test_settings.py, which does not exist)"

patterns-established:
  - "Judge-knob exposure = skill_builder_model wiring, field-for-field, with a registry-validation guard on write"
  - "Cap-with-bounded-retry: one auto-shorten re-emit, then hard-truncate, preserving the honest-fail []-on-None floor"

requirements-completed: [EVAL-05]

# Metrics
duration: ~18min
completed: 2026-07-04
---

# Phase 137.1 Plan 05: Judge-Model Settings Knob + SEED-100 Portability Extras Summary

**Backend judge-model knob — the shared `harness_judge_model` is now settable via the settings API (registry-validated, effective-default surfaced) reusing the ONE `resolve_judge_model` — plus a kebab-case skill-name lint warning and a 1024-char Tuner description cap, both additive and never-blocking.**

## Performance

- **Duration:** ~18 min
- **Started:** 2026-07-04T18:37Z (approx)
- **Completed:** 2026-07-04T18:45Z
- **Tasks:** 2 (both `tdd="true"` — RED then GREEN)
- **Files modified:** 6 (+1 created: deferred-items.md)

## Accomplishments

- **EVAL-05f judge knob (backend):** `FullSettingsResponse` now returns `harness_judge_model` (raw, `""` when unset) + `resolved_harness_judge_model` (via the shared `resolve_judge_model` — the effective default `claude-opus-4-8` surfaces when unset). `SettingsUpdate` accepts `harness_judge_model`; the persist path registry-validates a non-empty value (`get_model_capability(id).capability_source == "registry"` → 400 on unknown, mirroring `evals.py:165-170`) then writes it. **No new setting, no migration, no second resolver** (D-11/D-12).
- **D-13 ii — kebab lint:** `skill_lint.lint_description` appends an additive `name_not_kebab` warning for a non-kebab skill name (name-only check, pure, never raises, never blocks save).
- **D-13 i — Tuner cap:** `skill_tuner_service.build_candidates` caps its output at `MAX_DESCRIPTION_CHARS` (shared constant imported from `skill_lint`); an over-cap candidate triggers ONE bounded auto-shorten re-emit, then any still-over candidate is hard-truncated — the `emitted is None → []` never-crash floor is preserved.

## Task Commits

Each task was executed TDD (RED test commit → GREEN feat commit):

1. **Task 1: Judge-model settings knob (RED)** - `63aef374` (test)
2. **Task 1: Judge-model settings knob (GREEN)** - `6e701b12` (feat)
3. **Task 2: Kebab lint + Tuner cap (RED)** - `af92432e` (test)
4. **Task 2: Kebab lint + Tuner cap (GREEN)** - `8e21c73b` (feat)

**Plan metadata:** (this SUMMARY + deferred-items.md) — see final docs commit.

## Files Created/Modified

- `backend/app/api/settings.py` — `harness_judge_model` + `resolved_harness_judge_model` in `FullSettingsResponse`; `harness_judge_model` in `SettingsUpdate`; function-local `resolve_judge_model` in `_build_response`; registry-validation + persist in `update_settings`.
- `backend/app/services/skill_lint.py` — module-level `_KEBAB_RE`; additive `name_not_kebab` warning in `lint_description`.
- `backend/app/services/skill_tuner_service.py` — import `MAX_DESCRIPTION_CHARS`; cap logic in `build_candidates` + new `_shorten_over_cap` bounded-retry helper.
- `backend/tests/unit/test_settings.py` — 5 judge-knob tests (`_fake_settings` helper; response/update model shape, `_build_response` shared-resolver wiring, 400-on-unknown, registry-model persist).
- `backend/tests/unit/test_skill_lint.py` — kebab warning tests; healthy-example test switched to a kebab name.
- `backend/tests/unit/test_skill_tuner_service.py` — 3 cap/auto-shorten tests.
- `.planning/phases/.../deferred-items.md` — logged one out-of-scope pre-existing failure (see below).

## Test Results (run on the main tree via `backend/venv`)

- `tests/unit/test_settings.py` — **8 passed** (3 pre-existing + 5 new judge-knob).
- `tests/unit/test_skill_lint.py` — **all pass** (kebab warning + regression).
- `tests/unit/test_skill_tuner_service.py` — **all pass** (cap/auto-shorten + regression).
- Combined three plan files: **55 passed** (`python -m pytest tests/unit/test_settings.py tests/unit/test_skill_lint.py tests/unit/test_skill_tuner_service.py -q`).
- Adjacent sweep (`tests/test_eval_runner.py tests/integration/test_skill_tuner_routes.py`): **55 passed, 1 failed** — the single failure is pre-existing and out of scope (see Deviations).
- RED gates confirmed genuinely failing before each GREEN implementation.

## Decisions Made

- **No migration / no second resolver (D-11):** `harness_judge_model` already exists end-to-end; the settings API reuses `resolve_judge_model` (the eval judge + publish judge share it). Verified `supabase/migrations/` has no reference to `harness_judge_model`.
- **`""` = unset (D-12):** registry validation only runs on a non-empty value; the resolver surfaces the effective default (`claude-opus-4-8`) when unset — cloud stays safe unset.
- **400 for the judge model** (matching `evals.py`), distinct from settings.py's 422 confidence-bucket path.
- **Kebab warning is name-only** and never blocks; the one existing `== []` healthy test switched to a kebab name so it stays warning-free under the new rule.

## Deviations from Plan

### Path corrections (Rule 3 — blocking / missing referenced file)

**1. [Rule 3 - Blocking] Settings-API test file lives at `tests/unit/test_settings.py`, not `tests/test_settings.py`**
- **Found during:** Task 1
- **Issue:** The plan's `files_modified` + verify command reference `backend/tests/test_settings.py`, which does not exist. The real existing settings-API test file is `backend/tests/unit/test_settings.py`.
- **Fix:** Extended the existing `backend/tests/unit/test_settings.py` (its intent matches — "settings API models" tests). Ran the corrected path for verification.
- **Files modified:** backend/tests/unit/test_settings.py
- **Verification:** `python -m pytest tests/unit/test_settings.py -q` → 8 passed.
- **Committed in:** `63aef374` / `6e701b12`

**2. [Rule 3 - Test regression from new additive rule] Healthy-lint example switched to a kebab name**
- **Found during:** Task 2
- **Issue:** `test_healthy_description_returns_empty_list` asserted `warnings == []` for the non-kebab name "Quarterly Risk Register" — which the new `name_not_kebab` warning would (correctly) now flag, breaking the test.
- **Fix:** Changed that example's name to the kebab form `quarterly-risk-register` (preserves the test's intent: a genuinely-healthy name+description yields no warnings).
- **Files modified:** backend/tests/unit/test_skill_lint.py
- **Verification:** the full `test_skill_lint.py` file passes.
- **Committed in:** `af92432e` (RED test commit)

### Out-of-scope discovery (logged, NOT fixed — SCOPE BOUNDARY)

**3. Pre-existing failure `test_eval_runner.py::test_judge_provider_independent`**
- **Found during:** Task 2 adjacent sweep
- **Issue:** The test constructs `JudgeVerdict(...)` without the required `case_feedback` field (added to the schema by EVAL-05d at `validator_kinds.py:120`). Confirmed pre-existing: `case_feedback` is present in `JudgeVerdict` at this plan's base commit (`git show HEAD~4:.../validator_kinds.py`), and Plan 05 never touches `validator_kinds.py` or `test_eval_runner.py`.
- **Disposition:** Logged to `deferred-items.md`; owned by the EVAL-05d / `case_feedback` work (one-line test fix). NOT fixed here — outside Plan 05's scope (judge-model knob + kebab lint + Tuner cap).

---

**Total deviations:** 2 path/test corrections (both blocking, within Plan-05 files) + 1 out-of-scope discovery logged.
**Impact on plan:** No scope creep — both corrections were necessary to land the plan's own tests; the eval_runner failure is a sibling plan's debt, deferred per the rules.

## Issues Encountered

- The plan's Task 2 verify command chains two `-k` flags (`-k kebab ... -k "shorten or cap or length"`); pytest applies only the LAST `-k`, silently deselecting the kebab tests. Ran the kebab and Tuner selections separately to confirm both RED and GREEN states honestly.

## User Setup Required

None — no new setting, no migration, no external service configuration. (The judge-model UI picker is a later plan; this plan ships only the settings-API surface it reads.)

## Next Phase Readiness

- The judge model is now selectable via `PUT /settings` (registry-validated) and readable via `GET /settings` (raw + resolved). Plan 10's Settings UI picker can bind directly to `harness_judge_model` / `resolved_harness_judge_model`.
- The two D-13 portability extras ship additively; no hot files touched.
- One deferred item (`test_eval_runner.py` `case_feedback`) awaits the EVAL-05d plan.

## Self-Check: PASSED

- Created files exist: `137.1-05-SUMMARY.md`, `deferred-items.md` — both FOUND.
- All commits exist: `63aef374`, `6e701b12`, `af92432e`, `8e21c73b`, `cb04fac7` — all FOUND.
- My commits (`63aef374~1..HEAD`) touched ONLY the 6 plan files + 2 phase-doc files. No `STATE.md`, no `ROADMAP.md`, no unrelated `.claude/` files. Every commit path-scoped.
- Artifact `contains` checks: `harness_judge_model` present in `settings.py`; `name_not_kebab` present in `skill_lint.py`; `MAX_DESCRIPTION_CHARS` present in `skill_tuner_service.py`.
- No migration added (`supabase/migrations/` has no `harness_judge_model` reference).

---
*Phase: 137.1-skill-eval-production-clean-inserted-2026-07-04*
*Completed: 2026-07-04*
