---
phase: 137.1-skill-eval-production-clean-inserted-2026-07-04
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/settings.py
  - backend/app/services/skill_lint.py
  - backend/app/services/skill_tuner_service.py
  - backend/tests/test_settings.py
  - backend/tests/unit/test_skill_lint.py
  - backend/tests/unit/test_skill_tuner_service.py
autonomous: true
requirements: [EVAL-05]
must_haves:
  truths:
    - "D-11 the ONE shared harness_judge_model setting is exposed through the settings API (raw + resolved), mirroring the skill_builder_model wiring — no new setting, no migration, no split (it already drives both the eval judge and the publish judge via resolve_judge_model)"
    - "D-12 the exposed judge model is registry-validated: the settings update accepts only MODEL_CAPABILITIES-known models and the response surfaces the effective default (claude-opus-4-8) when unset"
    - "D-13 (i): the Tuner description-builder caps candidates at MAX_DESCRIPTION_CHARS=1024 with a bounded auto-shorten retry, preserving the honest-fail floor (never crashes)"
    - "D-13 (ii): skill_lint adds a kebab-case skill-name portability warning that is additive and NEVER blocks save (D-09 posture)"
  artifacts:
    - path: "backend/app/api/settings.py"
      provides: "harness_judge_model + resolved_harness_judge_model in SettingsResponse; harness_judge_model in SettingsUpdate + persist; registry validation"
      contains: "harness_judge_model"
    - path: "backend/app/services/skill_lint.py"
      provides: "name_not_kebab additive warning code (pure, never raises)"
      contains: "name_not_kebab"
    - path: "backend/app/services/skill_tuner_service.py"
      provides: "1024-char cap + bounded auto-shorten on description candidates"
      contains: "MAX_DESCRIPTION_CHARS"
  key_links:
    - from: "backend/app/api/settings.py"
      to: "resolve_judge_model (validator_kinds.py:58)"
      via: "resolved_harness_judge_model = resolve_judge_model(s) — ONE source of truth (D-11)"
      pattern: "resolve_judge_model"
    - from: "skill_lint / skill_tuner_service"
      to: "MAX_DESCRIPTION_CHARS (skill_lint.py:25)"
      via: "shared portability constant"
      pattern: "MAX_DESCRIPTION_CHARS"
---

<objective>
Ship the backend half of the judge-model knob (EVAL-05f) and the two folded SEED-100 harvest extras (D-13). The judge knob is a pure settings-API exposure of the already-end-to-end `harness_judge_model` (mirror `skill_builder_model` verbatim — no migration). The extras are additive, never-blocking: a 1024-char cap + auto-shorten on Tuner description candidates, and a kebab-case name portability lint warning.

Purpose: Unblock single-provider/local-model orgs from picking a judge (the UI picker lands in Plan 10), and land the two small portability improvements without touching hot files.
Output: settings-API judge exposure + registry validation; skill_lint kebab warning; skill_tuner 1024-cap.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-CONTEXT.md
@.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-PATTERNS.md

<interfaces>
<!-- Verbatim precedents to mirror. -->
settings.py skill_builder_model wiring (VERIFIED at these lines):
  :16 import resolve_skill_builder_model
  :78-79 SettingsResponse: skill_builder_model: str / resolved_skill_builder_model: str | None
  :143 SettingsUpdate: skill_builder_model: str | None = None
  :195-196 builder: skill_builder_model=s.skill_builder_model, resolved_skill_builder_model=resolve_skill_builder_model(s)
  :344-345 persist: if body.skill_builder_model is not None: updates["skill_builder_model"] = body.skill_builder_model
resolve_judge_model(settings) (validator_kinds.py:58-80): returns harness_judge_model if set, else first registry default in (claude-opus-4-8, gpt-5.5) with forced_emission, else None. ONE source of truth (D-11) — used by eval judge AND publish judge.
harness_judge_model exists in app_settings (config.py:1006) + models/user_settings.py:157 (DB-only, no UI).
Registry rule (evals.py:165-170): get_model_capability(id).capability_source == "registry".
skill_lint (skill_lint.py): MAX_DESCRIPTION_CHARS=1024 (:25); lint_description (:121-200) is pure/never-raises; warnings are dicts {code, message}; too_long already exists.
skill_tuner_service.build_candidates (:201-241): forced-emit returning rewritten descriptions; honest-fail floor at :238-239 (emitted is None → [], never crash).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → settings judge-model write | An untrusted model id crosses into the shared judge setting. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137.1-J1 | Tampering | judge-model setting write | mitigate | Registry-validated on write (D-12) — only `get_model_capability(id).capability_source == "registry"` models accepted; an unknown id is rejected 400. Effective default (resolve_judge_model → claude-opus-4-8) surfaced when unset so cloud stays safe unset. |
| T-137.1-J2 | Tampering | resolve_judge_model divergence | mitigate | D-11 — the settings API reuses resolve_judge_model; no second resolver is introduced (the eval + publish judge stay identical). |
| T-137.1-L1 | DoS (usability) | lint / cap regressions | mitigate | D-13 posture: both extras are additive and NEVER block save (lint returns a warning dict; the cap/auto-shorten preserves the never-crash floor). |
| T-137.1-SC | Tampering | pip installs | mitigate | No new packages. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose harness_judge_model through the settings API, registry-validated (EVAL-05f, D-11/D-12)</name>
  <files>backend/app/api/settings.py, backend/tests/test_settings.py</files>
  <behavior>
    - GET settings returns harness_judge_model (raw, "" when unset) + resolved_harness_judge_model (resolve_judge_model → claude-opus-4-8 default when unset).
    - PUT settings with a registry-known model persists it; with an unknown model returns 400 (registry validation).
    - No new setting/table/resolver — the existing harness_judge_model + resolve_judge_model are reused.
  </behavior>
  <read_first>
    - backend/app/api/settings.py (:16, :73-79, :143, :195-196, :344-345 — the skill_builder_model wiring to mirror verbatim)
    - backend/app/services/harness/validator_kinds.py (:58-80 resolve_judge_model)
    - backend/app/api/evals.py (:165-170 the registry-validation rule to reuse)
    - backend/tests/test_settings.py (the existing settings-API test file — the judge-knob tests extend it)
  </read_first>
  <action>
    Mirror the `skill_builder_model` wiring for `harness_judge_model`: add `harness_judge_model: str` and `resolved_harness_judge_model: str | None` to `SettingsResponse`; in the response builder set `harness_judge_model=s.harness_judge_model` and `resolved_harness_judge_model=resolve_judge_model(s)` (function-local import from validator_kinds — the ONE source of truth, D-11). Add `harness_judge_model: str | None = None` to `SettingsUpdate`. In the persist handler, when `body.harness_judge_model is not None`, VALIDATE it with `get_model_capability(body.harness_judge_model).capability_source == "registry"` (reject 400 with a clear message if not registry-known — D-12), then `updates["harness_judge_model"] = body.harness_judge_model`. Do NOT add a migration (the field already exists in app_settings). Extend `backend/tests/test_settings.py` (the existing settings-API test file) asserting: unset → resolved default surfaces; a registry model persists; an unknown model → 400.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_settings.py -k "judge" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `SettingsResponse` returns both `harness_judge_model` and `resolved_harness_judge_model`.
    - `SettingsUpdate` accepts `harness_judge_model`; the persist path registry-validates (400 on unknown) and updates.
    - `resolved_harness_judge_model` uses `resolve_judge_model` (no second resolver introduced).
    - No migration added; `grep -rn "harness_judge_model" supabase/migrations/085*.sql` returns nothing.
  </acceptance_criteria>
  <done>The shared judge model is settable via the settings API, registry-validated, with the effective default surfaced when unset; no migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Kebab-case name portability lint (D-13 ii) + Tuner 1024-cap auto-shorten (D-13 i)</name>
  <files>backend/app/services/skill_lint.py, backend/app/services/skill_tuner_service.py, backend/tests/unit/test_skill_lint.py, backend/tests/unit/test_skill_tuner_service.py</files>
  <behavior>
    - lint_description on a non-kebab skill name (e.g. "My Skill") appends a `name_not_kebab` warning; a kebab name ("my-skill") does not; the lint never raises and never blocks save.
    - build_candidates never returns a candidate longer than MAX_DESCRIPTION_CHARS; an over-long candidate is auto-shortened (bounded retry) then truncated; the honest-fail floor (returns [] not a crash on emit None) is preserved.
  </behavior>
  <read_first>
    - backend/app/services/skill_lint.py (:5-9 non-blocking docstring; :25 MAX_DESCRIPTION_CHARS; :121-200 lint_description + the warning dict shape + the too_long check)
    - backend/app/services/skill_tuner_service.py (:201-241 build_candidates + :238-239 the honest-fail floor)
    - backend/tests/unit/test_skill_lint.py + backend/tests/unit/test_skill_tuner_service.py (existing warning + candidate assertions)
  </read_first>
  <action>
    In `skill_lint.py`, after the existing length checks in `lint_description` (or the appropriate name-linting site), add ONE additive warning: if the skill name is non-empty and does not match a kebab regex `^[a-z0-9]+(-[a-z0-9]+)*$`, append `{"code": "name_not_kebab", "message": "Skill name isn't kebab-case (lowercase-with-hyphens). Portable skill names use kebab-case so they travel cleanly on import/export."}`. Keep the function pure and never-raising (D-09 posture — warning only, never blocks). In `skill_tuner_service.py` `build_candidates`, import `MAX_DESCRIPTION_CHARS` from `skill_lint`; after collecting the string candidates, for any candidate whose length exceeds `MAX_DESCRIPTION_CHARS`, run a bounded auto-shorten retry (re-emit once asking for a shorter rewrite) and if it still exceeds, hard-truncate to the cap — preserving the existing `emitted is None → []` never-crash floor. Extend both unit tests: a non-kebab name yields `name_not_kebab` and a kebab name does not; an over-long candidate is capped to <= MAX_DESCRIPTION_CHARS with no crash.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_skill_lint.py -k kebab tests/unit/test_skill_tuner_service.py -k "shorten or cap or length" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `lint_description("My Skill", ...)` includes a `name_not_kebab` warning; `lint_description("my-skill", ...)` does not; the call never raises.
    - `build_candidates` output contains no string longer than `MAX_DESCRIPTION_CHARS`; over-long inputs are auto-shortened/truncated with the `[]`-on-None floor intact.
    - Both are additive: `grep -n "raise" skill_lint.py` shows no new raise in lint_description; save is never blocked.
  </acceptance_criteria>
  <done>Kebab-name warning + 1024-cap auto-shorten shipped, additive and never-blocking; tests green.</done>
</task>

</tasks>

<verification>
- `cd backend && python -m pytest tests/unit/test_skill_lint.py tests/unit/test_skill_tuner_service.py -x` green.
- Judge knob exposed + registry-validated; no migration; resolve_judge_model reused.
- Both D-13 extras additive, never-blocking.
</verification>

<success_criteria>
The shared judge model is selectable via the settings API (registry-validated, effective-default surfaced) and the two folded SEED-100 extras (kebab-name warning + 1024-char description cap) ship additively without blocking save or touching hot files.
</success_criteria>

<output>
Create `.planning/phases/137.1-skill-eval-production-clean-inserted-2026-07-04/137.1-05-SUMMARY.md` when done.
</output>
