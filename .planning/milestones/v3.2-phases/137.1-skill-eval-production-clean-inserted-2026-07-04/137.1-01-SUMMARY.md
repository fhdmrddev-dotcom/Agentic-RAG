---
phase: 137.1-skill-eval-production-clean-inserted-2026-07-04
plan: 01
subsystem: database
tags: [postgres, migration, pydantic, typescript, eval, matrix, judge]

# Dependency graph
requires:
  - phase: 132-133-134
    provides: eval_runs / eval_results tables, JudgeVerdict schema, api client
provides:
  - "migration 085: eval_runs.matrix_group_id + feeds_gate; eval_results.duration_ms + case_feedback; 3 NOT NULL relaxations; group index"
  - "JudgeVerdict.case_feedback advisory field (OpenAI-strict-safe, in required list)"
  - "frontend TS types + api.ts client signatures for matrix + duration + case_feedback"
affects: [137.1-02, 137.1-04, 137.1-07, 137.1-08, 137.1-10]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Additive-and-relaxing-only migration (no data migration, no DROP TABLE/COLUMN, no UPDATE)"
    - "Interface-first: one migration + shared verdict field + TS types consumed by all downstream plans"

key-files:
  created:
    - supabase/migrations/085_eval_matrix_duration_case_feedback.sql
  modified:
    - backend/app/services/harness/validator_kinds.py
    - frontend/src/types/index.ts
    - frontend/src/lib/api.ts
    - supabase/full-schema.sql

key-decisions:
  - "D-05: feeds_gate NOT NULL DEFAULT false — single/pre-085 runs read false, publish-gate behavior unchanged"
  - "D-06: matrix_group_id nullable uuid — NULL for single runs, shared id for the N arms of one matrix run"
  - "D-02: DROP NOT NULL on skill_id/skill_version_id/test_case_id so the skill-less in-memory smoke sweep persists honestly (data seeds are not in schema-only full-schema.sql)"

patterns-established:
  - "case_feedback is advisory: never a verdict, never a gate input, never in rollup math"

requirements-completed: []  # EVAL-05 is phase-spanning; foundation slice only — mark at phase verification

# Metrics
duration: ~15min
completed: 2026-07-04
---

# Phase 137.1 Plan 01: Foundation (Data + Interface Contracts) Summary

**Migration 085 (matrix grouping + gate-feeder flag + per-arm duration + advisory case_feedback + skill-less smoke-sweep relaxation), the `JudgeVerdict.case_feedback` field, and the TS types + api.ts signatures every downstream 137.1 plan consumes.**

## Performance

- **Duration:** ~15 min (executor tasks 1–2 ≈ 10.5 min + orchestrator continuation for migration apply/regen)
- **Completed:** 2026-07-04
- **Tasks:** 3
- **Files modified:** 5 (1 created, 4 modified)

## Accomplishments
- Migration 085 authored + applied live: `eval_runs.matrix_group_id` (uuid, nullable), `eval_runs.feeds_gate` (bool NOT NULL DEFAULT false), `eval_results.duration_ms` (int, nullable), `eval_results.case_feedback` (text, nullable), `idx_eval_runs_matrix_group_id`, and 3 NOT NULL relaxations (`skill_id`, `skill_version_id`, `test_case_id`).
- `JudgeVerdict.case_feedback` added to the strict schema (in `required` list — OpenAI-strict-safe).
- Frontend TS types + `api.ts` client function signatures added for matrix runs, duration, and case_feedback.
- `full-schema.sql` regenerated from the live DB (no reset) and committed alongside.

## Task Commits

1. **Task 1: Author migration 085** — `6e00585d` (feat)
2. **Task 2: case_feedback verdict field + TS types + api client fns** — `6fb90761` (feat)
3. **Task 3: Apply migration 085 live + regenerate full-schema** — `769239d2` (feat)

## Files Created/Modified
- `supabase/migrations/085_eval_matrix_duration_case_feedback.sql` — the additive/relaxing migration
- `backend/app/services/harness/validator_kinds.py` — `JudgeVerdict.case_feedback` advisory field
- `frontend/src/types/index.ts` — matrix + duration + case_feedback types
- `frontend/src/lib/api.ts` — client function signatures
- `supabase/full-schema.sql` — regenerated bootstrap artifact

## Decisions Made
- Migration is additive-and-relaxing only; all pre-085 rows stay valid unchanged (new columns backfill NULL / DEFAULT false).
- Skill-less smoke sweep persists with NULL skill_id/version/test_case_id — skill-scoped Studio queries (`.eq("skill_id", …)`) naturally exclude these rows.

## Deviations from Plan
None — plan executed as written. Task 3 was a human-action checkpoint (migration applied to the live DB directly per operator authorization, since the SQL-editor path is interactive); the continuation (regen + commit) ran in the orchestrator.

## Issues Encountered
- Plan 05 (parallel Wave-1 sibling) hit a session/quota limit; unrelated to this plan. Task 3's regen + commit were completed inline to keep the critical path moving under quota pressure.

## User Setup Required
Migration 085 has been applied to the local dev DB. **For any cloud/staging/production deploy, this migration must also be applied to that environment's Supabase DB** (paste `085_*.sql` into its SQL editor) per the deployment parity checklist.

## Next Phase Readiness
- Interface contracts are live: Wave 2 (plan 02 runner signals), Wave 3 (plan 04 matrix fan-out), Wave 4 (07 aggregation, 08 Studio, 10 Settings) can now consume the schema + types.
- `tsc --noEmit` clean; migration verified live (4 columns present, 3 relaxed to nullable, index created).

---
*Phase: 137.1-skill-eval-production-clean-inserted-2026-07-04*
*Completed: 2026-07-04*
