---
phase: 137.1-skill-eval-production-clean-inserted-2026-07-04
plan: 06
subsystem: cross-provider / provider-gateway
tags: [anthropic, deepseek, provider-gateway, eval, cross-provider, D-14, bug-fix]

# Dependency graph
requires:
  - phase: 133-134
    provides: single-run eval route (with/without-skill arms), eval_results.error + verdict_state
provides:
  - "supports_assistant_prefill MODEL_CAPABILITIES flag (config.py) — False on the Claude 4.6+/5 family"
  - "open_anthropic_stream strips a trailing assistant prefill for prefill-unsupported models (adapter boundary, D-14)"
  - "BUG-260701-01 CLOSED (fixed); BUG-260630-01 CLOSED (clean re-verify, no code change)"
affects: [137.1-07, 137.1-09]  # trustworthy per-provider verdicts feed matrix aggregation

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Provider request-shape quirks absorbed at the gateway/adapter boundary, never the shared agent loop (D-14)"
    - "Capability-gated, additive request sanitize: absent flag => byte-identical (Deep unchanged)"

key-files:
  created: []
  modified:
    - backend/app/config.py
    - backend/app/services/provider_gateway/anthropic.py
    - backend/tests/unit/test_provider_gateway_seam.py
    - .planning/reported-bugs/BUG-260701-01-agent-loop-assistant-prefill-400-newer-models.md
    - .planning/reported-bugs/BUG-260630-01-deepseek-without-skill-reasoning-content-400.md

key-decisions:
  - "D-14: fix at open_anthropic_stream ONLY; agent_loop.py/threads.py untouched (git diff --name-only verified)"
  - "Re-test BEFORE fix (DB evidence): DeepSeek CLEAN post-f47d6736 => closed with zero code change; Claude prefill still reproduced 5x through 2026-07-02 => fixed"
  - "supports_assistant_prefill default absent=True => byte-identical for older Claude + every other provider; explicit False only on the 4.6+/5 family"
  - "Operator override of the autonomous:false human-verify checkpoint: 'all models should just work' — proceed to fix on the DB reproduction evidence rather than a fresh manual run"

patterns-established:
  - "A trailing role:assistant turn == a prefill; strip it for prefill-unsupported models, returning a NEW list (no mutation of request.messages)"

requirements-completed: []  # EVAL-05 marked at phase verification

# Metrics
duration: ~20min
completed: 2026-07-04
---

# Phase 137.1 Plan 06: Cross-provider bug re-verify + adapter fix Summary

**Re-tested the two provider request-shape 400s live (DB evidence) BEFORE any fix, then applied an adapter-boundary fix only for the one that still reproduced — Deep byte-identical, shared path untouched (D-14).**

## Performance
- **Duration:** ~20 min
- **Completed:** 2026-07-04
- **Tasks:** 2 (Task 1 human-verify checkpoint satisfied via eval_results evidence + operator live test; Task 2 conditional fix)

## Accomplishments
- **BUG-260630-01 (DeepSeek `reasoning_content` 400) → CLOSED, zero code change.** `eval_results` shows the `without_skill` arm failing through 2026-07-03 21:27, then clean from 22:12 onward (graded verdicts, empty error). Root-caused to `f47d6736` (reset-before-each-arm). Operator's live DeepSeek test corroborated. Honors D-14 "no fix without reproduction."
- **BUG-260701-01 (Claude assistant-prefill 400) → CLOSED, fixed at the adapter boundary.** Reproduced deterministically 5× on `claude-sonnet-5`/`claude-opus-4-8` `without_skill` through 2026-07-02.
  - `config.py`: additive `supports_assistant_prefill: False` on sonnet-5, opus-4-6/4-7/4-8, sonnet-4-6.
  - `provider_gateway/anthropic.py`: `_strip_unsupported_assistant_prefill` drops a trailing assistant prefill when the model's capability is False; returns a NEW list (no mutation). Wired into `open_anthropic_stream`.
  - Because the fix lives in the shared adapter, it also protects Deep/Explorer chat on the 4.6+/5 family (the bug's re-open trigger).
- **Regression + Deep-mode-unchanged assertions** in `test_provider_gateway_seam.py`: unsupported model ⇒ prefill stripped; supported model (`claude-haiku-4-5`) ⇒ messages byte-identical; helper edge cases (trailing user, single assistant, other-provider) covered. 12/12 seam tests pass.

## Decisions Made
- Operator override of the `autonomous:false` human-verify checkpoint ("all models should just work — no excuse a model won't run"): proceeded to the fix on the DB reproduction evidence (5× deterministic) rather than a fresh manual run. Recorded as a guardrail override.
- Default `supports_assistant_prefill` absent ⇒ True: byte-identical for everything currently working; only the erroring 4.6+/5 family changes (error → works).

## Deviations from Plan
- Task 1 was authored as an operator-run live re-test; satisfied instead from `eval_results` history (stronger — hard rows, not a single observation) plus the operator's live GLM/OpenAI/DeepSeek test. No fresh manual Claude run was required because the reproduction was already deterministic in the data.
- `openai_compat.py` (listed in plan `files_modified` for the IF-DeepSeek-reproduces branch) was NOT touched — DeepSeek was clean, so per Task 2 no adapter diff landed for it.

## Issues Encountered
- None. `get_model_capability` returns a dict for every id (registry or inferred), so the capability read is null-safe.

## Verification
- `git diff --name-only` shows NO `agent_loop.py` / `threads.py` change (D-14 red line held).
- `pytest tests/unit/test_provider_gateway_seam.py -q` → 12 passed (2 new + 10 existing adapter contracts).
- Capability flags resolve: sonnet-5 / opus-4-8 / sonnet-4-6 = False (strip); haiku-4-5 = absent/True (byte-identical).

## Next Phase Readiness
- Per-provider eval verdicts are now trustworthy across the Claude tier — matrix aggregation (07/09) reflects real skill lift, not baseline-arm errors.
- Both folded bug reports are `status: closed`.

---
*Phase: 137.1-skill-eval-production-clean-inserted-2026-07-04*
*Completed: 2026-07-04*
