---
phase: 140-smart-dispatch-relevance-pre-filter-stretch
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/app/config.py
  - backend/app/models/user_settings.py
  - backend/app/services/skill_catalog_filter.py
  - backend/tests/test_140_catalog_trim.py
autonomous: true
requirements: [TRIG-02]

must_haves:
  truths:
    - "The token budget resolves from a global app_settings value (skill_catalog_max_tokens); 0 or negative disables the filter = today's inject-all (D-04 kill switch, bounds-checked)"
    - "When the FULL catalog fits budget, build_skill_catalog_block returns BYTE-IDENTICAL output to today's format with NO trim marker (D-03 fast path, SC#1)"
    - "When over budget, the least-relevant skills are cut first (highest similarity kept) and the honest _CATALOG_TRIM_MARKER with the correct N-cut count is appended (SC#1 + SC#3, D-02 never-silent / D-14)"
    - "Pinned/recently-loaded skills are always kept even at low similarity, capped so pins can't starve the whole menu (D-02 always-keep, PIN_BUDGET_FRACTION mirror)"
    - "When sim_by_id is None (fail-open / fast path), the block trims by name order only and never crashes (D-05)"
    - "A MIXED sim_by_id (real floats for some ids, None for others — the normal LEFT-JOIN shape) is None-safe: a None score resolves to the lowest sentinel and sorts last, never `-(None)` TypeError (Blocker-3 — build_skill_catalog_block runs OUTSIDE Plan 04's try/except, so an uncaught sort crash would violate D-05 fail-open)"
  artifacts:
    - path: "backend/app/services/skill_catalog_filter.py"
      provides: "resolve_skill_catalog_budget + build_skill_catalog_block pure fn + _CATALOG_TRIM_MARKER + _recently_loaded_skill_names pin-scan"
      exports: ["resolve_skill_catalog_budget", "build_skill_catalog_block", "_recently_loaded_skill_names"]
      min_lines: 50
    - path: "backend/tests/test_140_catalog_trim.py"
      provides: "unit tests for SC#1 byte-identical + cut-least-relevant, SC#2 budget-zero, SC#3 marker + pinned-kept, D-05 fail-open, Blocker-3 mixed-sim None-safe"
      exports: ["test_fits_budget_byte_identical", "test_over_budget_cuts_least_relevant", "test_budget_zero_injects_all", "test_marker_appended_with_count", "test_pinned_always_kept", "test_fail_open_on_none_sim", "test_mixed_sim_none_safe_sorts_last"]
  key_links:
    - from: "backend/app/services/skill_catalog_filter.py (build_skill_catalog_block fits path)"
      to: "agent_loop.py:1216-1224 today's catalog line format"
      via: "byte-identical line + header emit: f'- **{name}**: {description}' under '## Available Skills\\n... {LOAD_SKILL_POLICY}\\n'"
      pattern: "- \\*\\*.*\\*\\*: "
    - from: "backend/app/models/user_settings.py"
      to: "app_settings.skill_catalog_max_tokens DB row"
      via: "_val(row, 'skill_catalog_max_tokens', None, 1500) readback (env_attr=None, app_settings-only)"
      pattern: "skill_catalog_max_tokens"
---

<objective>
Author the DB-free heart of the pre-filter: the settings-resolved token budget and the pure
`build_skill_catalog_block` trim function. This plan builds the CONTRACTS (Plan 04 wires them into the hot
path). Everything here is unit-testable with no DB and no LLM — so SC#1/SC#2/SC#3 + fail-open are proven
deterministically up front.

Purpose: Prove the byte-identical fast path (D-03), the cut-least-relevant trim (SC#1), the honest marker
(SC#3 / D-14), the always-keep pinned set (D-02), the fail-open None-sim behavior (D-05), and the None-safe
MIXED-sim sort (Blocker-3) as pure logic, before any hot-path wiring risk.

Output: `config.py` + `user_settings.py` budget knob, `backend/app/services/skill_catalog_filter.py`, and
`backend/tests/test_140_catalog_trim.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140-smart-dispatch-relevance-pre-filter-stretch/140-CONTEXT.md
@.planning/phases/140-smart-dispatch-relevance-pre-filter-stretch/140-RESEARCH.md
@.planning/phases/140-smart-dispatch-relevance-pre-filter-stretch/140-PATTERNS.md
@backend/app/services/context_window.py

<interfaces>
<!-- estimate_tokens(text, model) -> int (context_window.py:102-125) — tiktoken for OpenAI, chars/4 else. Call, don't hand-roll. -->
<!-- LOAD_SKILL_POLICY (skill_lint.py:57-60) — the catalog-note policy string. Import + reuse; do NOT re-template. -->
<!-- _TRIM_MARKER analog (context_window.py:65) + PIN_BUDGET_FRACTION analog (context_window.py:76). -->
<!-- Today's byte-identical target (agent_loop.py:1216-1224): -->
<!--   header = "\n\n## Available Skills\nThe following skills are available. " + LOAD_SKILL_POLICY + "\n" -->
<!--   line(s) = f"- **{s['name']}**: {s['description']}"; body = header + "\n".join(line for name-sorted enabled) -->
<!-- _CATALOG_TRIM_MARKER_TMPL (RESEARCH Pattern 3): -->
<!--   "\n- _[{n} additional skill(s) exist that weren't listed here to fit the catalog budget. Ask me to list all skills, or name one directly and I'll load it.]_" -->
<!-- None-SAFE score resolution (Blocker-3): raw = (sim_by_id or {}).get(id); score = raw if raw is not None else -1.0. -->
<!--   NEVER `(sim_by_id or {}).get(id, -1.0)` — dict.get returns the stored None when the key IS present, and -(None) raises TypeError. -->
<!-- harness_judge_model / skill_builder_model analogs: config.py:1013; user_settings.py field @174/@168, readback @539/@535. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Budget knob — config field + user_settings readback + resolve_skill_catalog_budget (bounds-checked)</name>
  <files>backend/app/config.py, backend/app/models/user_settings.py, backend/app/services/skill_catalog_filter.py, backend/tests/test_140_catalog_trim.py</files>
  <read_first>
    - backend/app/config.py (line ~811-815 embedding_model/embedding_dimensions; line ~1013 harness_judge_model) — the field-declaration style to mirror.
    - backend/app/models/user_settings.py (lines 157-174 skill_builder_model + harness_judge_model fields; lines 334-346 the `_val` helper contract; lines 535/539 readback) — the env_attr=None app_settings-only readback pattern.
    - backend/app/services/context_window.py (line 79-99 resolve_context_budget) — the resolver shape to mirror.
  </read_first>
  <behavior>
    - Test 1: `resolve_skill_catalog_budget(settings_with(skill_catalog_max_tokens=1500))` returns 1500.
    - Test 2: budget of `0` returns 0 (caller treats 0 as inject-all — kill switch).
    - Test 3 (bounds-check): a negative or non-int value coerces safely — negative clamps to 0 (disable), never produces negative/NaN token math.
    - Test 4 (default): a settings object missing the attr resolves to the default 1500.
  </behavior>
  <action>
    Add `skill_catalog_max_tokens: int = 1500` to the config.py Settings class (mirror `embedding_dimensions:
    int = 1536` at config.py:812). Add `skill_catalog_max_tokens: int = 1500` to the `UserEffectiveSettings`
    model in user_settings.py (mirror `harness_judge_model` @174) with the comment "app_settings-only; 0 =
    inject-all kill switch (D-04)", and add the loader readback `skill_catalog_max_tokens=int(_val(row,
    "skill_catalog_max_tokens", None, 1500))` in `_build_effective_settings` (mirror @539). Create
    `backend/app/services/skill_catalog_filter.py` and add `resolve_skill_catalog_budget(app_settings) ->
    int`: read `int(getattr(app_settings, "skill_catalog_max_tokens", 1500))`, bounds-check the result — if
    it is negative or not a finite int, return `0` (clean disable); otherwise return it. This keeps ALL
    magic numbers out of the hot path (D-04 / CTX-03 discipline) and hardens the untrusted admin value
    (T-140-05). Add the four behavior tests to `test_140_catalog_trim.py`.
  </action>
  <acceptance_criteria>
    - config.py Settings has `skill_catalog_max_tokens: int = 1500`; user_settings.py has the field + a `_val(row, "skill_catalog_max_tokens", None, 1500)` readback.
    - `resolve_skill_catalog_budget` returns the configured int, coerces a missing attr to 1500, and returns 0 for a negative/invalid value (never a negative token budget).
    - `pytest tests/test_140_catalog_trim.py -k budget -x` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && python -m pytest tests/test_140_catalog_trim.py -k "budget" -x && grep -q "skill_catalog_max_tokens" app/models/user_settings.py && echo OK</automated>
  </verify>
  <done>Budget knob wired through config + user_settings + a bounds-checked resolver; 0/negative disables cleanly; default 1500.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build_skill_catalog_block pure fn + _CATALOG_TRIM_MARKER + None-safe sort + pin-scan + unit tests</name>
  <files>backend/app/services/skill_catalog_filter.py, backend/tests/test_140_catalog_trim.py</files>
  <read_first>
    - backend/app/services/context_window.py (lines 65-68 `_TRIM_MARKER`, 70-76 `PIN_BUDGET_FRACTION`, 102-125 `estimate_tokens`) — clone the honest-marker + pin-cap + token-count idioms.
    - backend/app/services/skill_lint.py (lines 57-60 `LOAD_SKILL_POLICY`) — import + reuse the exact policy string.
    - backend/app/services/agent_loop.py (lines 1215-1225) — today's byte-identical catalog line + header format the fits-path MUST reproduce; and lines 915-917 (`_pinned_skill` provenance: tool_call name == "load_skill", `args.skill_name`).
    - 140-RESEARCH.md §"Pattern 3" (the full `build_skill_catalog_block` algorithm — note the CORRECTED None-safe `_score` helper, NOT the old `dict.get(id, -1.0)` form) + §"Pattern 4" (`_recently_loaded_skill_names`).
  </read_first>
  <behavior>
    - Test 1 (fits → byte-identical): given a small enabled list under budget, output == today's exact block `"\n\n## Available Skills\nThe following skills are available. {LOAD_SKILL_POLICY}\n" + name-sorted `- **name**: description` lines`, with NO marker and NO embed dependency.
    - Test 2 (over budget → cut least-relevant): given sim_by_id where skill X has the lowest similarity and the block exceeds budget, X is ABSENT and the higher-sim skills are present; total estimate_tokens ≤ budget.
    - Test 3 (marker): when any skill is cut, the output ends with `_CATALOG_TRIM_MARKER` containing the correct N-cut count.
    - Test 4 (pinned always kept): a pinned/recent skill with the LOWEST similarity is still present in an over-budget trim (D-02), while non-pinned low-sim skills are cut.
    - Test 5 (budget 0 → inject-all): budget <= 0 returns the full byte-identical block (kill switch).
    - Test 6 (fail-open whole-dict None sim): sim_by_id=None over budget → trims by name order only, never raises, marker still appended.
    - Test 7 (pin cap): pins beyond the pin-budget cap do not starve the whole menu (PIN_BUDGET_FRACTION mirror).
    - Test 8 (pin-scan): `_recently_loaded_skill_names` collects `args.skill_name` from history rows whose `tool_calls` include a `load_skill` call.
    - Test 9 (mixed-sim None-safe — Blocker-3): an over-budget `sim_by_id` containing BOTH real floats AND `None` for different skill ids (the real LEFT-JOIN shape) → build_skill_catalog_block does NOT raise, and the None-scored skill sorts LAST (cut before the real-scored ones), never first and never erroring.
  </behavior>
  <action>
    In `skill_catalog_filter.py`, add `_CATALOG_TRIM_MARKER_TMPL` = the RESEARCH Pattern 3 string (honest,
    never-silent — the escape hatch is REAL: `load_skill` works by name). Add `_recently_loaded_skill_names
    (history_rows: list[dict]) -> set[str]`: scan each row's `tool_calls` for `name == "load_skill"`,
    collect `(args or {}).get("skill_name")` (mirror agent_loop.py:915-917). Add `build_skill_catalog_block
    (enabled, budget, model, pinned_recent_ids, sim_by_id) -> str` per RESEARCH Pattern 3: reuse
    `LOAD_SKILL_POLICY` (import, do not re-template) and the exact header + `- **{name}**: {description}`
    line format; the DISPLAY order is name-sorted in BOTH paths (selection is by relevance, presentation is
    stable). Fast path: `budget <= 0 or estimate_tokens(full, model) <= budget` → return the full
    byte-identical block with NO marker. Over budget: force-keep `pinned_recent_ids` (capped via a
    PIN_BUDGET_FRACTION-style bound so pins can't starve the menu), then greedily fill by similarity desc
    using a None-SAFE score resolver — for each skill resolve `raw = (sim_by_id or {}).get(s["id"])` then
    `score = raw if raw is not None else -1.0` (a `None` value is treated EXACTLY like a missing key: the
    `-1.0` sentinel, so it sorts last but stays eligible — fail-open keep). DO NOT rely on
    `dict.get(id, -1.0)`'s default to catch `None`: when the key IS present but maps to `None`, `.get`
    returns `None` and `-(None)` raises `TypeError`, crashing the turn (Blocker-3 — a D-05 violation, since
    this fn runs OUTSIDE Plan 04's try/except). Sort by `-score`. Cut the least-relevant first (SC#1); append
    `_CATALOG_TRIM_MARKER` with the actual N-cut. Keep the function PURE (no DB, no LLM) so the tests drive it
    directly. Author all nine behavior tests in `test_140_catalog_trim.py`.
  </action>
  <acceptance_criteria>
    - Fits-budget output is byte-identical to today's format (header + name-sorted `- **name**: description` lines, no marker) — asserted by string equality in Test 1.
    - Over-budget output omits the lowest-similarity non-pinned skill(s), keeps higher-sim skills, stays ≤ budget, and ends with the honest marker carrying the correct N.
    - A pinned/recent skill at lowest similarity is retained; the pin set is capped so it can't consume the whole budget.
    - `budget <= 0` returns the full block; `sim_by_id=None` over budget trims by name and never raises.
    - A mixed `sim_by_id` (real floats + `None` values) never raises; the None-scored skill sorts last (Test 9). The sort key resolves `None` to the same sentinel as a missing key — NOT via `dict.get`'s default (Blocker-3).
    - `_recently_loaded_skill_names` extracts `skill_name` from `load_skill` tool_calls in history rows.
    - `pytest tests/test_140_catalog_trim.py -x` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && python -m pytest tests/test_140_catalog_trim.py -x</automated>
  </verify>
  <done>Pure trim fn proves SC#1 (byte-identical + cut-least-relevant), SC#2 (budget-zero), SC#3 (marker + pinned-kept), D-05 (fail-open None-sim), and Blocker-3 (mixed-sim None-safe sort, no crash); pin-scan extracts recent load_skill names. All unit tests green, no DB/LLM.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin `app_settings` value → hot-path token math | The budget is admin-tunable; an out-of-range value must not corrupt token accounting or starve the catalog. |
| trimmed catalog → model | Any cut skill must be honestly disclosed (never silently dropped). |
| RPC similarity shape → sort math | `match_skills` may return `None` similarity for some ids alongside real floats; the sort must be None-safe (a crash here runs outside Plan 04's fail-open guard). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-05 | Tampering / Denial | untrusted `app_settings.skill_catalog_max_tokens` value | mitigate | `resolve_skill_catalog_budget` bounds-checks: `int()` coercion, negative/invalid → 0 (clean disable = inject-all), never negative/NaN token math. Test 3 asserts. |
| T-140-06 | Repudiation / Denial (of honesty) | silent truncation hides skills | mitigate | Any cut appends the honest `_CATALOG_TRIM_MARKER` with the real N-cut (CTX-03 D-14 never-silent); the escape hatch (`load_skill` by name) is real. Test 3 asserts. |
| T-140-14 | Denial (of correct behavior) | mixed `None`+float similarity crashes the sort (uncaught `TypeError`) | mitigate | None-SAFE score resolver (`raw if raw is not None else -1.0`), never `dict.get`'s default — a present-but-None value sorts last, no `-(None)` crash (Blocker-3). Test 9 asserts no raise + None sorts last. |
| T-140-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH audit = none). |
</threat_model>

<verification>
- `pytest tests/test_140_catalog_trim.py -x` fully green (all nine behavior cases, incl. mixed-sim None-safe).
- `grep` confirms `skill_catalog_max_tokens` in user_settings.py readback.
- `files_modified` excludes `backend/app/api/threads.py` (G-5) and `agent_loop.py` (Plan 04 owns the wiring).
</verification>

<success_criteria>
- Budget resolves from app_settings, bounds-checked, 0/negative = inject-all.
- Pure trim fn is byte-identical on the fast path and cuts least-relevant first with an honest marker on the trim path.
- Pinned/recent always kept; None-sim fails open; MIXED float+None sim is None-safe (no crash, None sorts last — Blocker-3). All proven with DB-free/LLM-free unit tests.
</success_criteria>

<output>
Create `.planning/phases/140-smart-dispatch-relevance-pre-filter-stretch/140-03-SUMMARY.md` when done.
</output>
</content>
