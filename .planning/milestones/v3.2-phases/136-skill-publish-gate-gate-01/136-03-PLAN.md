---
phase: 136-skill-publish-gate-gate-01
plan: 03
type: execute
wave: 2
depends_on: [136-01]
files_modified:
  - frontend/src/types/index.ts
  - frontend/src/lib/api.ts
  - frontend/src/hooks/useSkills.ts
  - frontend/src/components/skills/PublishGateDialog.tsx
  - frontend/src/components/skills/PublishGateDialog.test.tsx
  - frontend/src/components/skills/SkillCard.tsx
autonomous: true
requirements: [GATE-01]
user_setup: []

must_haves:
  truths:
    - "'Share globally' on a private skill opens a confirm dialog that shows the gate-met satisfied status ('Eval passed X/N on current version' + Publish) OR an honest unmet status (never evaled / latest run failed / passed on an older version) + a pointer to run an eval + an explicit Force-publish affordance (D-05)"
    - "The dialog renders server-computed gate status from GET /skills/{id}/publish-gate only — no client-side gate math; Force-publish sends override=true and the server remains the gate (D-07)"
    - "Global→private unshare stays a direct ungated call — the dialog only intercepts the private→global direction (D-07)"
    - "The publish UI is thin/undesigned, reusing the existing AlertDialog primitive — no new design-system chrome (137 fence)"
  artifacts:
    - path: "frontend/src/components/skills/PublishGateDialog.tsx"
      provides: "Thin confirm dialog rendering server gate status + force-publish"
      contains: "getPublishGate"
    - path: "frontend/src/lib/api.ts"
      provides: "getPublishGate(skillId) + toggleSkillGlobal(id, override?) with 409 gate parsing"
      contains: "publish-gate"
    - path: "frontend/src/types/index.ts"
      provides: "PublishGate TS interface mirroring the backend model"
      contains: "interface PublishGate"
  key_links:
    - from: "frontend/src/components/skills/SkillCard.tsx"
      to: "PublishGateDialog"
      via: "Globe click branches on skill.is_global (share→dialog, unshare→direct)"
      pattern: "is_global"
    - from: "frontend/src/components/skills/PublishGateDialog.tsx"
      to: "/skills/{id}/publish-gate"
      via: "getPublishGate on open"
      pattern: "getPublishGate"
---

<objective>
Add the thin, deliberately-undesigned publish-gate UX: a confirm dialog on "Share globally" that
renders the server-computed gate status (satisfied X/N vs honest unmet + how-to-satisfy + explicit
force-publish), plus the client plumbing (`getPublishGate`, `toggleSkillGlobal(override)`, the
`PublishGate` type, the `useSkills.toggleGlobal` passthrough). The dialog is UX only — the server
is the gate (D-07); Force-publish just sends `override=true`.

Purpose: D-05 — one home for status + evidence + override on the share action. Keep it plain
(AlertDialog primitive, no new chrome) — Phase 137 (PANEL-01, G-2) owns the designed experience.

Output: the client contracts + the dialog + the SkillCard intercept. The SkillEvalSection
gate-status line + override record is Plan 04.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/136-skill-publish-gate-gate-01/136-CONTEXT.md
@.planning/phases/136-skill-publish-gate-gate-01/136-PATTERNS.md
@.planning/phases/136-skill-publish-gate-gate-01/136-01-SUMMARY.md

<interfaces>
<!-- Wire contract (from Plan 01; endpoints implemented in Plan 02 — mock in tests, no live backend needed here): -->
<!--   GET  /skills/{id}/publish-gate  -> PublishGate JSON:
         { met, state: "never_evaled"|"latest_failed"|"passed_on_older_version"|"passed",
           measured: int|null, passed: int|null, passing_run_id: string|null, reason: string,
           last_override: {gate_state, created_at}|null } -->
<!--   PATCH /skills/{id}/toggle-global  body { override?: boolean }
         -> 200 Skill on success; 409 {detail: {error:"publish_gate_unmet", gate: PublishGate}} on unmet+no-override;
            403 on non-owner (existing). -->
<!-- Existing SkillCard.tsx: onToggleGlobal prop (id)=>Promise<void>; handleToggleGlobal at :58 calls it;
     Globe button at :214-228 (tooltip "Share globally"/"Unshare" branches on skill.is_global). -->
<!-- Existing useSkills.ts:63 toggleGlobal(id) -> apiToggleSkillGlobal(id) -> setSkills local update. -->
<!-- Existing api.ts:1569 toggleSkillGlobal(id) -> PATCH, 403 branch. proposalError helper :1739 parses FastAPI detail. -->
<!-- AlertDialog primitive: @/components/ui/alert-dialog (AlertDialog/AlertDialogContent/AlertDialogHeader/
     AlertDialogTitle/AlertDialogDescription/AlertDialogFooter/AlertDialogAction/AlertDialogCancel). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Client contracts — PublishGate type + api client + useSkills passthrough</name>
  <files>frontend/src/types/index.ts, frontend/src/lib/api.ts, frontend/src/hooks/useSkills.ts</files>
  <read_first>
    - frontend/src/types/index.ts:583-664 (EvalRun / PromotionGate — the flat-interface, nullable-field, status-union style to mirror)
    - frontend/src/lib/api.ts:1528-1580 (createSkill / toggleSkillGlobal — the fetch+getAuthHeaders+403 pattern to extend) and :1673-1690 (getEvalRun/listEvalRuns — the typed-json-cast GET shape for getPublishGate) and :1739-1751 (proposalError — the FastAPI detail parser)
    - frontend/src/hooks/useSkills.ts:8,20,63-68 (toggleGlobal(id) → apiToggleSkillGlobal → setSkills — the exact function + type to extend)
  </read_first>
  <action>
    In frontend/src/types/index.ts add `interface PublishGate { met: boolean; state: "never_evaled" | "latest_failed" | "passed_on_older_version" | "passed"; measured: number | null; passed: number | null; passing_run_id: string | null; reason: string; last_override: { gate_state: string; created_at: string } | null }` — exact mirror of the backend Pydantic model from Plan 01.
    In frontend/src/lib/api.ts add `getPublishGate(skillId: string): Promise<PublishGate>` = GET `${API_BASE}/skills/${skillId}/publish-gate` (getAuthHeaders → fetch → typed json cast, mirroring getEvalRun). Modify `toggleSkillGlobal` to accept an optional second arg `override?: boolean`: when provided, send `method: "PATCH"` with a JSON body `{ override }` and the JSON content-type header; keep the existing `res.status === 403` branch; ADD a `res.status === 409` branch that parses `detail.gate` (an OBJECT, not a string — do not reuse proposalError's string path blindly) and throws an `Error` whose `.cause` (or a typed field) carries the `PublishGate` so the dialog can render evidence. When called with no override (unshare direction), behavior is unchanged.
    In frontend/src/hooks/useSkills.ts extend `toggleGlobal` to `(id: string, override?: boolean) => Promise<void>` and pass `override` through to `apiToggleSkillGlobal(id, override)`; keep the `setSkills` local is_global update on success. Update the `UseSkills` interface signature to match.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "publish-gate|PublishGate|toggleGlobal|getPublishGate" || cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `interface PublishGate` exists in types/index.ts with all 7 fields incl `last_override`
    - `getPublishGate` exported from api.ts hitting `/skills/{id}/publish-gate`
    - `toggleSkillGlobal(id, override?)` sends `{ override }` as a JSON body when override is passed, and has a `res.status === 409` branch that surfaces the `gate` object
    - `useSkills().toggleGlobal` accepts `(id, override?)` and the `UseSkills` interface reflects it
    - `npx tsc --noEmit` reports no type errors for the touched files
  </acceptance_criteria>
  <done>PublishGate type + getPublishGate + override-capable toggle + hook passthrough compile cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: PublishGateDialog component + component test</name>
  <files>frontend/src/components/skills/PublishGateDialog.tsx, frontend/src/components/skills/PublishGateDialog.test.tsx</files>
  <read_first>
    - frontend/src/components/ui/alert-dialog.tsx:130-142 (the exported AlertDialog primitives to compose)
    - frontend/src/components/skills/SkillEvalSection.tsx:566-572 (the numeric "X/N with-skill cases passed" honesty readout style to echo) and :263-272 (refetch-not-optimistic idiom)
    - frontend/src/components/skills/SkillFormDialog.test.tsx (the same-directory dialog test that mocks @/lib/api — the analog to copy; note MEMORY project_frontend_vitest_rot: author a fresh clean test, do not lean on a rotted sibling)
    - .planning/phases/136-skill-publish-gate-gate-01/136-PATTERNS.md (PublishGateDialog.tsx section — behavior contract D-05)
  </read_first>
  <action>
    Create frontend/src/components/skills/PublishGateDialog.tsx: props `{ skillId: string; open: boolean; onOpenChange: (o: boolean) => void; onConfirm: (override: boolean) => Promise<void> }`. On open, call `getPublishGate(skillId)` into local state (loading spinner while pending; refetch-not-optimistic). Render two branches (D-05):
    - gate.met → satisfied status text "Eval passed {gate.passed}/{gate.measured} on the current version" + a primary "Publish" AlertDialogAction that calls `onConfirm(false)`.
    - !gate.met → an HONEST status derived from gate.state ("never_evaled" → "This skill has never been evaled — run an eval on its current version to publish."; "latest_failed" → "The latest eval run failed on the current version."; "passed_on_older_version" → "The last passing eval was on an older version — re-eval the current version to publish.") using gate.reason, PLUS a pointer to run an eval from the skill's eval section, PLUS an explicit destructive-styled "Force publish anyway" action that calls `onConfirm(true)` (evidence shown at that moment — the counts/reason are already rendered). Include an AlertDialogCancel.
    Keep it PLAIN — AlertDialog primitives only, no new design-system chrome (137 fence). Surface any onConfirm error inline; close on success (`onOpenChange(false)`).
    Create frontend/src/components/skills/PublishGateDialog.test.tsx (vitest + @testing-library/react, `vi.mock("@/lib/api")`): (a) met branch → mock getPublishGate resolves met=true measured=3 passed=3 → assert "3/3" satisfied text + a Publish control that calls onConfirm(false); (b) unmet never_evaled branch → assert the honest "never been evaled" copy + a Force-publish control that calls onConfirm(true).
  </action>
  <verify>
    <automated>cd frontend && npm run test -- PublishGateDialog --run</automated>
  </verify>
  <acceptance_criteria>
    - Test (a): met render shows "3/3" + Publish action triggers `onConfirm(false)`
    - Test (b): unmet never_evaled render shows the honest "never been evaled" pointer copy + a Force-publish action triggers `onConfirm(true)`
    - PublishGateDialog imports only from `@/components/ui/alert-dialog` for dialog chrome (grep: no bespoke modal/Portal added)
    - `npm run test -- PublishGateDialog --run` is green
  </acceptance_criteria>
  <done>Dialog renders server gate status (met satisfied X/N vs honest unmet + force) with 2 green tests.</done>
</task>

<task type="auto">
  <name>Task 3: SkillCard intercept — dialog on share, direct on unshare</name>
  <files>frontend/src/components/skills/SkillCard.tsx</files>
  <read_first>
    - frontend/src/components/skills/SkillCard.tsx:15,27,58-65 (onToggleGlobal prop + handleToggleGlobal) and :105-130 (the inline confirmingDelete local-state idiom to mirror for dialog open state) and :214-228 (the Globe button + is_global tooltip branch)
    - frontend/src/components/skills/PublishGateDialog.tsx (the component from Task 2 — its props contract)
  </read_first>
  <action>
    Change `SkillCard`'s `onToggleGlobal` prop type to `(id: string, override?: boolean) => Promise<void>` (matches the useSkills passthrough from Task 1). Add local state `const [showPublishDialog, setShowPublishDialog] = useState(false)`. Change the Globe button `onClick` (:220) to branch on `skill.is_global`: if `skill.is_global` is TRUE → call `handleToggleGlobal()` directly (unshare — never gated, D-07, existing behavior — passes no override); if FALSE (share) → `setShowPublishDialog(true)`. Mount `<PublishGateDialog skillId={skill.id} open={showPublishDialog} onOpenChange={setShowPublishDialog} onConfirm={async (override) => { await onToggleGlobal(skill.id, override); }} />`. Keep the existing `handleToggleGlobal` for the unshare path and its error handling. Do NOT change the unshare direction to route through the dialog.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json && npm run test -- SkillCard --run 2>&1 | tail -20 || true</automated>
  </verify>
  <acceptance_criteria>
    - grep SkillCard.tsx: the Globe onClick branches on `skill.is_global` (share → setShowPublishDialog(true); unshare → handleToggleGlobal direct)
    - `<PublishGateDialog ... onConfirm={... onToggleGlobal(skill.id, override) ...}/>` is mounted
    - `onToggleGlobal` prop type is `(id: string, override?: boolean) => Promise<void>`
    - `npx tsc --noEmit` clean; no frontend test regressions introduced by the SkillCard change
  </acceptance_criteria>
  <done>Share opens the gate dialog; unshare stays a direct ungated toggle; types compile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser dialog → skills API | The dialog is UX; the server re-checks the gate. A user can bypass the dialog by calling the API directly — the server must still enforce (D-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-06 | Elevation of Privilege (client-side bypass) | PublishGateDialog / SkillCard | mitigate | The dialog renders server-computed status only (no client gate math); the actual gate is server-side (Plan 02) — a direct API call without a passing eval still 409s. Force-publish merely sends `override=true`, which the server records (Task 1/2/3) |
| T-136-03 | Tampering (forged gate) | getPublishGate / toggle client | mitigate | Client never computes met; it renders the server PublishGate and echoes `override` — it cannot fabricate a pass (Task 1) |
| T-136-SC | Tampering (supply chain) | npm installs | accept | Zero new packages — RESEARCH Package Legitimacy Audit N/A |
</threat_model>

<verification>
- `cd frontend && npm run test -- PublishGateDialog --run` green
- `cd frontend && npx tsc --noEmit -p tsconfig.json` clean
- No backend / agent-loop / threads.py touch (D-10) — frontend-only diff
- G-4 lived UAT of the dialog is a phase-gate manual check (136-VALIDATION.md Manual-Only), not automated here
</verification>

<success_criteria>
- Share-globally opens a thin confirm dialog showing server gate status (satisfied X/N vs honest unmet + how-to-satisfy + Force-publish) (D-05)
- Unshare stays a direct ungated call (D-07)
- Client is UX only — no client-side gate math (D-07); PublishGate type + getPublishGate + override-capable toggle wired
- UI is plain/undesigned, AlertDialog primitive only (137 fence)
</success_criteria>

<output>
Create `.planning/phases/136-skill-publish-gate-gate-01/136-03-SUMMARY.md` when done.
</output>
