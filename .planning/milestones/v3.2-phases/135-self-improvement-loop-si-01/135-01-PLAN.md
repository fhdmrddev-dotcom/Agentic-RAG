---
phase: 135-self-improvement-loop-si-01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/083_skill_proposals.sql
  - supabase/full-schema.sql
autonomous: true
requirements: [SI-01]

must_haves:
  truths:
    - "A public.skill_proposals owner-scoped table exists with the D-07 lifecycle status enum (proposed/rejected/approved/re_evaling/promoted/not_promoted/interrupted) and FKs into skills/skill_versions/eval_runs"
    - "Migration 083 is applied to the live local DB via psycopg2 :54322 per D-17, and supabase/full-schema.sql is regenerated (no --reset)"
    - "skill_proposals has owner-only RLS SELECT and NO client write policies — service-role router writes only (D-07)"
  artifacts:
    - path: "supabase/migrations/083_skill_proposals.sql"
      provides: "Owner-scoped proposals table + lifecycle CHECK enum + FKs + owner-only RLS + set_updated_at trigger"
      contains: "CREATE TABLE public.skill_proposals"
    - path: "supabase/full-schema.sql"
      provides: "Regenerated single-file deploy artifact including skill_proposals"
      contains: "skill_proposals"
  key_links:
    - from: "supabase/migrations/083_skill_proposals.sql"
      to: "live local Postgres (:54322)"
      via: "psycopg2 apply (D-17 — never db push/db reset)"
      pattern: "skill_proposals"
---

<objective>
Create the owner-scoped `public.skill_proposals` persistence table (migration 083) that anchors the
whole self-improvement loop: proposed instructions + proposer rationale/evidence summary + the D-07
lifecycle status + FKs into `skills`, `skill_versions` (base + new draft), and `eval_runs` (source +
re-eval). This is the schema foundation every backend plan in this phase reads and writes.

Purpose: Give the loop a durable, auditable home. The version row is created ONLY on approval (Plan 05),
so version history stays clean of unapproved drafts; rejections keep an audit trail here.
Output: `supabase/migrations/083_skill_proposals.sql` applied live + regenerated `supabase/full-schema.sql`.

This plan honors D-07 (owner-scoped table + lifecycle + FKs), D-17 (migration discipline).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/135-self-improvement-loop-si-01/135-CONTEXT.md
@.planning/phases/135-self-improvement-loop-si-01/135-RESEARCH.md
@.planning/phases/135-self-improvement-loop-si-01/135-PATTERNS.md
@CLAUDE.md

<canonical_schema>
<!-- LOCKED CONTRACT — every downstream plan (04, 05, 06, 07) uses these exact column + enum names. -->
Table `public.skill_proposals`:
- id                    uuid PK DEFAULT gen_random_uuid()
- skill_id              uuid NOT NULL  FK public.skills(id)          ON DELETE CASCADE
- base_skill_version_id uuid NOT NULL  FK public.skill_versions(id) ON DELETE CASCADE   -- what the diff is against
- new_skill_version_id  uuid           FK public.skill_versions(id) ON DELETE SET NULL  -- created only on approval (source='self_improve')
- re_eval_run_id        uuid           FK public.eval_runs(id)      ON DELETE SET NULL  -- the auto re-eval (D-12); Phase 136 can consume
- source_eval_run_id    uuid           FK public.eval_runs(id)      ON DELETE SET NULL  -- the run whose evidence drove the proposal (D-13 baseline)
- user_id               uuid NOT NULL  FK auth.users(id)            ON DELETE CASCADE
- proposed_instructions text NOT NULL
- rationale             text NOT NULL DEFAULT ''
- evidence_summary      text NOT NULL DEFAULT ''
- status                text NOT NULL DEFAULT 'proposed'
      CHECK (status IN ('proposed','rejected','approved','re_evaling','promoted','not_promoted','interrupted'))
- override_forced       boolean NOT NULL DEFAULT false   -- D-06 force-promote-with-evidence recorded
- created_at            timestamptz NOT NULL DEFAULT now()
- updated_at            timestamptz NOT NULL DEFAULT now()
Indexes: idx_skill_proposals_skill_id (skill_id), idx_skill_proposals_user_id (user_id)
RLS: ENABLE; SELECT policy `auth.uid() = user_id`; NO INSERT/UPDATE/DELETE policies.
Trigger: skill_proposals_set_updated_at BEFORE UPDATE EXECUTE public.set_updated_at() (reuse — never redefine).
</canonical_schema>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 083 — skill_proposals table + FKs + RLS + trigger</name>
  <files>supabase/migrations/083_skill_proposals.sql</files>
  <read_first>
    - supabase/migrations/080_eval_runs_and_results.sql (the FK + status-CHECK + timestamps + owner-only RLS SELECT precedent, :42-55 table shape, :104-123 RLS)
    - supabase/migrations/081_eval_verdict_and_ratings.sql (the set_updated_at trigger reuse, :103-108; header comment block :34-40)
    - .planning/phases/135-self-improvement-loop-si-01/135-RESEARCH.md (§ Code Examples "Proposals table shape" — the full DDL to lift; § Common Pitfalls #2 for why version-row-on-approval-only)
    - .planning/phases/135-self-improvement-loop-si-01/135-PATTERNS.md (Shared-G migration discipline; the "083_skill_proposals.sql" assignment)
  </read_first>
  <action>
    Create `supabase/migrations/083_skill_proposals.sql` implementing the LOCKED `public.skill_proposals`
    shape in `<canonical_schema>` above — copy the FK/CHECK/timestamps pattern verbatim from
    `080_eval_runs_and_results.sql:42-55` and the owner-only RLS SELECT from `080:104-123`. Reuse
    `public.set_updated_at()` via a `DROP TRIGGER IF EXISTS skill_proposals_set_updated_at` +
    `CREATE TRIGGER … BEFORE UPDATE … EXECUTE FUNCTION public.set_updated_at()` (copy `081:103-108`; do
    NOT redefine the function). Column and enum names are per the locked contract — use them EXACTLY
    (`base_skill_version_id`, `new_skill_version_id`, `re_eval_run_id`, `source_eval_run_id`,
    `override_forced`, and the 7-value `status` CHECK). Add the two indexes. Enable RLS with ONLY the
    SELECT policy `USING (auth.uid() = user_id)` — NO INSERT/UPDATE/DELETE policies (service-role router
    writes bypass RLS; app-code `.eq("user_id")` is the real gate). Prepend the standard header comment
    block copied from `080/081` documenting: apply via Supabase SQL editor / psycopg2 :54322 (never
    `db push`/`db reset`), then `bash scripts/regenerate-full-schema.sh` (no `--reset`), commit the
    migration + regenerated `full-schema.sql` together (D-17). Filename must match `<digits>_name.sql`
    (no letter suffix — those are silently skipped by the Supabase CLI).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && grep -q "CREATE TABLE public.skill_proposals" supabase/migrations/083_skill_proposals.sql && grep -q "status IN ('proposed','rejected','approved','re_evaling','promoted','not_promoted','interrupted')" supabase/migrations/083_skill_proposals.sql && grep -q "FOR SELECT USING (auth.uid() = user_id)" supabase/migrations/083_skill_proposals.sql && echo MIGRATION_OK</automated>
  </verify>
  <done>Migration file exists with the skill_proposals table, all FKs, the 7-value status CHECK, both indexes, owner-only RLS SELECT (no write policies), and the reused set_updated_at trigger.</done>
  <acceptance_criteria>
    - `supabase/migrations/083_skill_proposals.sql` contains `CREATE TABLE public.skill_proposals`.
    - The `status` CHECK lists exactly the 7 values in the locked contract.
    - Both `base_skill_version_id` (NOT NULL) and `new_skill_version_id` (nullable) FK `public.skill_versions`; `re_eval_run_id` + `source_eval_run_id` FK `public.eval_runs`.
    - RLS is enabled with a SELECT-only policy `auth.uid() = user_id`; grep finds NO `FOR INSERT`/`FOR UPDATE`/`FOR DELETE` policy in the file.
    - The trigger uses `EXECUTE FUNCTION public.set_updated_at()` (function not redefined in the file).
    - Header comment block documents the D-17 apply-via-psycopg2 + regen-full-schema + commit-together discipline.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply migration 083 to the live DB + regenerate full-schema</name>
  <files>supabase/migrations/083_skill_proposals.sql, supabase/full-schema.sql</files>
  <read_first>
    - supabase/SETUP.md (local Supabase + migration apply story)
    - scripts/regenerate-full-schema.sh (confirm it defaults to a live-DB dump with NO reset)
    - CLAUDE.md (migrations discipline paragraph — apply via SQL editor / psycopg2 :54322, never db push/reset; regen defaults to live dump)
  </read_first>
  <action>
    Apply `supabase/migrations/083_skill_proposals.sql` to the live LOCAL database autonomously via
    psycopg2 against `localhost:54322` (host=localhost port=54322 user=postgres password=postgres
    dbname=postgres) — the established 080/081/082 autonomous apply path (do NOT use `supabase db push`
    or `db reset`). Read the SQL file and execute it in one transaction. Then run
    `bash scripts/regenerate-full-schema.sh` (no `--reset` — the default live dump, preserves dev data)
    to rebuild `supabase/full-schema.sql`. Do NOT hand-edit `full-schema.sql`. Stage the migration +
    regenerated `full-schema.sql` together so the phase commit pairs them (D-17). Do NOT write scratch
    `.py` files under `backend/` (uvicorn --reload watched tree wedges on Windows) — run the apply via a
    `python -c` one-liner or a script under the scratchpad dir.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && venv/Scripts/python -c "import psycopg2; c=psycopg2.connect(host='localhost',port=54322,user='postgres',password='postgres',dbname='postgres'); cur=c.cursor(); cur.execute(\"select count(*) from information_schema.tables where table_schema='public' and table_name='skill_proposals'\"); assert cur.fetchone()[0]==1, 'skill_proposals table not applied'; cur.execute(\"select count(*) from information_schema.columns where table_name='skill_proposals' and column_name in ('base_skill_version_id','new_skill_version_id','re_eval_run_id','source_eval_run_id','override_forced','status')\"); assert cur.fetchone()[0]==6, 'missing columns'; print('DB_APPLIED_OK')"</automated>
  </verify>
  <done>The skill_proposals table exists in the live local DB with all locked columns; supabase/full-schema.sql regenerated and contains skill_proposals; both files staged for the paired commit.</done>
  <acceptance_criteria>
    - The psycopg2 verify prints `DB_APPLIED_OK` (table present with the 6 sentinel columns).
    - `grep -c skill_proposals supabase/full-schema.sql` returns ≥ 1 (regen picked up the new table).
    - No `supabase db push` or `db reset` was run; no scratch `.py` left under `backend/`.
    - The migration file and regenerated `full-schema.sql` are both present/staged for one commit.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Postgres (via backend router) | All proposal writes must cross the service-role router; clients never write directly. RLS SELECT is defense-in-depth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-07 | Information Disclosure | skill_proposals rows | mitigate | Owner-only RLS SELECT (`auth.uid() = user_id`); a user can never read another user's proposals/version drafts even if the router leaks (defense-in-depth). Verified by the SELECT-only policy in Task 1. |
| T-135-01 | Elevation of Privilege | skill_proposals writes | mitigate | NO client INSERT/UPDATE/DELETE policies — writes are service-role via the backend router only (owner gate enforced app-side in Plans 04/05). Absence of write policies verified by grep in Task 1. |
</threat_model>

<verification>
- `grep -q "CREATE TABLE public.skill_proposals" supabase/migrations/083_skill_proposals.sql` passes.
- psycopg2 verify prints `DB_APPLIED_OK`; the table has all locked columns in the live DB.
- `supabase/full-schema.sql` regenerated (contains `skill_proposals`), never hand-edited.
</verification>

<success_criteria>
The `skill_proposals` owner-scoped table exists in both the migration file and the live local DB with the exact locked shape (7-value status enum, all FKs, owner-only RLS SELECT, no write policies), and `full-schema.sql` is regenerated and paired for commit — the persistence foundation every backend plan builds on (D-07, D-17).
</success_criteria>

<output>
Create `.planning/phases/135-self-improvement-loop-si-01/135-01-SUMMARY.md` when done.
</output>
