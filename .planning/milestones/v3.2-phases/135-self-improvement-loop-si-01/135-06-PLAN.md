---
phase: 135-self-improvement-loop-si-01
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/lineDiff.ts
  - frontend/src/lib/lineDiff.test.ts
  - frontend/src/types/index.ts
  - frontend/src/lib/api.ts
autonomous: true
requirements: [SI-01]

must_haves:
  truths:
    - "frontend/src/lib/lineDiff.ts computes a unified line diff (removed red / added green / unchanged) via an in-repo LCS util with NO new npm dependency (D-09)"
    - "types + api helpers expose the full proposal lifecycle (propose/list/get/approve/reject/rerun/force-promote) matching the LOCKED backend endpoint + response contract, including the nullable gate honest-counts field (D-13)"
  artifacts:
    - path: "frontend/src/lib/lineDiff.ts"
      provides: "Pure LCS unified-line-diff: (a,b) -> {type:'add'|'remove'|'unchanged', text}[]"
      exports: ["lineDiff"]
    - path: "frontend/src/lib/lineDiff.test.ts"
      provides: "add/remove/unchanged classification over insert/delete/replace/no-op"
      exports: ["describe"]
    - path: "frontend/src/types/index.ts"
      provides: "SkillProposal interface + status union + gate field"
      contains: "SkillProposal"
    - path: "frontend/src/lib/api.ts"
      provides: "proposal fetch helpers (propose/list/get/approve/reject/rerun/forcePromote)"
      contains: "proposeImprovement"
  key_links:
    - from: "frontend/src/lib/api.ts helpers"
      to: "/skills/{id}/proposals* endpoints"
      via: "fetch + getAuthHeaders + detail-extraction (startEvalRun shape)"
      pattern: "proposals"
    - from: "frontend/src/lib/lineDiff.ts"
      to: "SkillEvalSection proposal card (Plan 07)"
      via: "consumed to render the reviewable diff"
      pattern: "lineDiff"
---

<objective>
Build the frontend CONTRACTS the proposal card (Plan 07) consumes: (1) `lineDiff.ts` — a pure ~40-line
LCS unified line diff (removed / added / unchanged) with NO new npm dependency (D-09 recommended path —
the in-repo util sidesteps the supply-chain surface entirely); (2) `SkillProposal` types matching the
LOCKED backend response shape (including the nullable `gate` honest-counts field, D-13); (3) `api.ts`
fetch helpers for the whole proposal lifecycle.

Purpose: interface-first — define the diff util + types + api surface so the card task is pure UI wiring.
Output: `lineDiff.ts` + test, `SkillProposal` types, proposal api helpers.

Honors D-09 (unified line diff, in-repo LCS, zero dep). No supply-chain checkpoint needed (no package added).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/135-self-improvement-loop-si-01/135-CONTEXT.md
@.planning/phases/135-self-improvement-loop-si-01/135-RESEARCH.md
@.planning/phases/135-self-improvement-loop-si-01/135-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Verified anchors (read this session) + the LOCKED backend contract (Plans 04/05). -->
api.ts:1648-1669 startEvalRun — the POST helper shape to copy (getAuthHeaders + fetch + detail extraction + typed return)
api.ts:1673-1690 listEvalRuns / getEvalRun — GET helper shapes
api.ts:420-434  SubscribeCallbacks interface (onEvalVerdict optional-callback shape — if a proposal SSE callback is added)
api.ts:828-857  eval_* SSE demux (additive else-if, NO return — Shared-F; the re-eval rides eval_* unchanged so likely NO new branch)
types/index.ts:583-639 EvalRun/EvalResult/EvalRunReadout — the FLAT-interface + status-union + nullable-field style to mirror

LOCKED endpoint contract (Plans 04/05):
  POST /skills/{skillId}/proposals                        body {source_eval_run_id}  -> SkillProposal
  GET  /skills/{skillId}/proposals                        -> SkillProposal[]
  GET  /skills/{skillId}/proposals/{proposalId}           -> SkillProposal
  POST /skills/{skillId}/proposals/{proposalId}/reject    -> SkillProposal
  POST /skills/{skillId}/proposals/{proposalId}/approve       -> {proposal: SkillProposal, re_eval_run_id: string}
  POST /skills/{skillId}/proposals/{proposalId}/rerun         -> {proposal: SkillProposal, re_eval_run_id: string}
  POST /skills/{skillId}/proposals/{proposalId}/force-promote -> SkillProposal

SkillProposal (LOCKED fields): id, skill_id, base_skill_version_id, new_skill_version_id (string|null),
  re_eval_run_id (string|null), source_eval_run_id (string|null), proposed_instructions, base_instructions,
  rationale, evidence_summary,
  status: "proposed"|"rejected"|"approved"|"re_evaling"|"promoted"|"not_promoted"|"interrupted",
  override_forced: boolean, gate?: {passed,no_regression,improved,prev_pass,prev_fail,still_pass,newly_pass,excluded_not_measured}|null,
  created_at, updated_at
  (gate mirrors the backend PromotionGate model owned by Plan 04's eval_run.py — Plan 05 populates it on promoted/not_promoted; D-13.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lineDiff.ts LCS unified line diff + lineDiff.test.ts</name>
  <files>frontend/src/lib/lineDiff.ts, frontend/src/lib/lineDiff.test.ts</files>
  <behavior>
    - lineDiff("a\nb\nc", "a\nb\nc") -> all rows type 'unchanged'.
    - Pure insert (add a line) -> the new line is one 'add' row; surrounding lines 'unchanged'.
    - Pure delete (remove a line) -> the removed line is one 'remove' row.
    - Replace (change a line) -> one 'remove' (old) + one 'add' (new) row for that line.
    - Empty base ("") vs non-empty -> all 'add'; non-empty vs empty -> all 'remove'.
  </behavior>
  <read_first>
    - frontend/src/lib/api.ts (confirm the file layout + how utils are exported in frontend/src/lib)
    - frontend/package.json (confirm vitest is available; run command `npx vitest run`)
    - .planning/phases/135-self-improvement-loop-si-01/135-RESEARCH.md (§ Standard Stack / Alternatives — in-repo LCS recommended over jsdiff; D-09)
    - .planning/phases/135-self-improvement-loop-si-01/135-PATTERNS.md (§ No Analog Found — lineDiff.ts is a pure ~40-line LCS)
  </read_first>
  <action>
    Create `frontend/src/lib/lineDiff.ts` exporting `export function lineDiff(base: string, next: string):
    { type: 'add' | 'remove' | 'unchanged'; text: string }[]`. Split both inputs on `\n`, compute a
    standard LCS table over the two line arrays, then backtrack to emit rows in order: matched lines ->
    `unchanged`; lines only in `base` -> `remove`; lines only in `next` -> `add` (a changed line surfaces
    as a `remove` of the old + an `add` of the new). Pure function, no dependency, no React. Create
    `frontend/src/lib/lineDiff.test.ts` (vitest `describe`/`it`) covering every case in `<behavior>`
    (unchanged, insert, delete, replace, empty-base, empty-next). Do NOT add any npm package (no jsdiff).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && npx vitest run src/lib/lineDiff.test.ts</automated>
  </verify>
  <done>lineDiff.ts exports the pure LCS util; lineDiff.test.ts is green across unchanged/insert/delete/replace/empty cases; no new dependency added.</done>
  <acceptance_criteria>
    - `npx vitest run src/lib/lineDiff.test.ts` passes.
    - `lineDiff` returns rows typed `'add' | 'remove' | 'unchanged'` and a replace produces both a `remove` and an `add` for the changed line.
    - `frontend/package.json` and `package-lock.json` are unchanged (no dependency added — grep shows no `diff`/`jsdiff` addition).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: SkillProposal types (incl. gate) + api.ts proposal helpers</name>
  <files>frontend/src/types/index.ts, frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/types/index.ts (:583-639 EvalRun/EvalResult/EvalRunReadout — the interface + status-union + nullable style to mirror)
    - frontend/src/lib/api.ts (:1648-1690 startEvalRun/listEvalRuns/getEvalRun helper shapes to copy; :420-434 SubscribeCallbacks; :828-857 eval_* SSE demux)
    - .planning/phases/135-self-improvement-loop-si-01/135-PATTERNS.md (the api.ts + types/index.ts assignments)
  </read_first>
  <action>
    (1) In `frontend/src/types/index.ts` add the `SkillProposal` interface with EXACTLY the LOCKED fields
    in `<interfaces>` (FLAT, nullable where marked, the 7-value `status` union, and the optional/nullable
    `gate` object whose keys match the backend `PromotionGate` — passed/no_regression/improved/prev_pass/
    prev_fail/still_pass/newly_pass/excluded_not_measured). Add a `ProposalApproveResult` type
    `{ proposal: SkillProposal; re_eval_run_id: string }`.
    (2) In `frontend/src/lib/api.ts` add proposal helpers copying the `startEvalRun` fetch +
    `getAuthHeaders()` + detail-extraction shape (:1648-1669): `proposeImprovement(skillId,
    sourceEvalRunId)` -> POST proposals (body `{source_eval_run_id}`) -> `SkillProposal`;
    `listProposals(skillId)` / `getProposal(skillId, proposalId)` -> GET; `approveProposal(skillId,
    proposalId)` -> POST approve -> `ProposalApproveResult`; `rerunProposalReeval(skillId, proposalId)` ->
    POST rerun -> `ProposalApproveResult`; `rejectProposal(skillId, proposalId)` -> POST reject ->
    `SkillProposal`; `forcePromoteProposal(skillId, proposalId)` -> POST force-promote -> `SkillProposal`.
    Use the exact endpoint paths in `<interfaces>`. The re-eval rides the existing eval_* SSE via the
    companion run — do NOT add a new demux branch unless a proposal-specific event is introduced (none is;
    reuse `subscribeToRun` with `re_eval_run_id`).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && ! (npx tsc --noEmit -p tsconfig.json 2>&1 | grep -Ei "lib/api|types/index|lineDiff")</automated>
  </verify>
  <done>SkillProposal (with gate) + ProposalApproveResult types exist; api.ts exports proposeImprovement/list/get/approve/reject/rerun/forcePromote hitting the locked paths; no type errors in the touched files.</done>
  <acceptance_criteria>
    - `grep -q "interface SkillProposal" frontend/src/types/index.ts` and the status union has all 7 values.
    - `SkillProposal` includes the optional/nullable `gate` object with the 8 PromotionGate keys (D-13 surface).
    - `grep -q "proposeImprovement" frontend/src/lib/api.ts` and the six other helper names are present.
    - `approveProposal`/`rerunProposalReeval` return `ProposalApproveResult` (carrying `re_eval_run_id`).
    - The inverted tsc gate exits 0 — no `npx tsc --noEmit` error line mentions `lib/api`, `types/index`, or `lineDiff`; the command FAILS (exit 1) if any does. (Uses `! ( tsc | grep )` so a matching error line is a real failure, not a swallowed `|| true`.)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client api helpers → backend routes | Helpers send the auth header; all authorization is enforced server-side (owner gate). The client never trusts its own state for authorization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-01 | Elevation of Privilege | proposal api helpers | mitigate | Helpers only carry `getAuthHeaders()`; the real IDOR gate is server-side (Plans 04/05, 404-not-403). The client cannot bypass ownership. |
| T-135-SC | Tampering (supply chain) | frontend deps | accept | No npm package added — the in-repo LCS util (D-09) sidesteps the `diff`/jsdiff supply-chain surface. `package.json`/`package-lock.json` unchanged (acceptance criterion). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx vitest run src/lib/lineDiff.test.ts` green.
- The inverted tsc gate (`! ( tsc --noEmit | grep -Ei "lib/api|types/index|lineDiff" )`) exits 0 — fails on any touched-file type error.
- No dependency added (package.json/lock unchanged).
</verification>

<success_criteria>
A pure in-repo unified line diff (D-09, zero dep) plus proposal types (incl. the nullable `gate` honest-counts field, D-13) and api helpers matching the locked backend contract — the interface layer the proposal card wires against.
</success_criteria>

<output>
Create `.planning/phases/135-self-improvement-loop-si-01/135-06-SUMMARY.md` when done.
</output>
</output>
