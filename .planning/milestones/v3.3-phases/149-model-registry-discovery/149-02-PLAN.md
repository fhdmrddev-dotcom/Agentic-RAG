---
phase: 149-model-registry-discovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/model_discovery_service.py
  - backend/tests/test_149_discovery.py
autonomous: true
requirements: [MODEL-02]

must_haves:
  truths:
    - "D-149-11: one call fans out concurrently to all keyed cloud providers (per-provider ~12s timeout) and returns per-provider outcomes in one result"
    - "D-149-14: a provider with no key is reported `no_key` (skipped, honest — not failed); a 429/non-200 provider is reported with its verbatim status code (excluded, not failed — the 058/060 lesson)"
    - "D-149-13 + SC#3: the diff marks every NEW model `enabled=false` and fills capabilities ONLY where the provider returned them — token limits for Google+OpenRouter, native_tools for OpenRouter ONLY; every other field is `unknown` (propose-only, never auto-enabled)"
    - "D-149-12: proposals are ephemeral — compute_diff returns a JSON-serializable diff that lives only in the HTTP response; the service builds no proposals table and no staleness lifecycle"
    - "Pitfall 5: Google (`nextPageToken`) and Anthropic (`has_more`/`last_id`) are paginated so known models are never falsely reported vanished; Pitfall 6: provider model-id casing is preserved verbatim"
  artifacts:
    - path: "backend/app/services/model_discovery_service.py"
      provides: "async httpx fan-out over the hardcoded PROVIDER_ENDPOINTS allowlist + diff computation with propose-only capability fill"
      exports: ["discover_all", "compute_diff", "PROVIDER_ENDPOINTS"]
      min_lines: 120
    - path: "backend/tests/test_149_discovery.py"
      provides: "mocked-payload tests: diff, propose-only (SC#3), per-provider outcomes"
  key_links:
    - from: "backend/app/services/model_discovery_service.py:discover_all"
      to: "PROVIDER_ENDPOINTS (hardcoded)"
      via: "iterate the code-constant table only — no client URL"
      pattern: "PROVIDER_ENDPOINTS"
---

<objective>
Build the model-discovery service (MODEL-02) as a self-contained backend module: a lift-and-wrap of `scripts/curate_models.py` (its endpoint table, per-shape extractors, pagination, newest-first sort) converted from blocking `requests` to `httpx.AsyncClient` + `asyncio.gather`, plus the genuinely net-new diff computation and the propose-only "unknown — you set it" capability-fill asymmetry that is the SC#3 hero.

Purpose: This is the concurrent external fan-out that discovery runs on. It has no analog as a service (only the CLI script exists) and no in-repo dependency, so it ships independently in Wave 1; Plan 05 wires it behind the operator-gated `POST /admin/models/discover` endpoint.
Output: `backend/app/services/model_discovery_service.py` with `discover_all(...)` (fan-out) + `compute_diff(...)` (new/changed/vanished + propose-only fill), fully unit-tested with mocked provider payloads (all 8 mocked).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/149-model-registry-discovery/149-CONTEXT.md
@.planning/phases/149-model-registry-discovery/149-RESEARCH.md
@.planning/phases/149-model-registry-discovery/149-PATTERNS.md

<interfaces>
<!-- LIFT these verbatim from scripts/curate_models.py into the new service. Do NOT rewrite. -->

scripts/curate_models.py:
  PROVIDER_ENDPOINTS (:67)      # 8-provider {url, auth, key_env} table — copy VERBATIM (hardcoded allowlist)
  _extract_openai_compat (:140) # data[].id + created extractor (openai/deepseek/moonshot/zhipu/openrouter base)
  _extract_minimax (:153)       # probes data[]/model_list[]/bare-list → id
  per-auth header/param block (:199-209)  # bearer | anthropic | google_query | public
  google pagination (:223-235)  # pageSize=1000 + pageToken=nextPageToken loop
  anthropic pagination (:237-243) # limit=1000 + after_id=last_id while has_more loop
  sort_newest_first (:263)      # epoch/ISO stamp or lexical fallback; preserves id casing via str(el.get("id"))

backend/app/services/rerank_service.py — the httpx.AsyncClient idiom to mirror (async with, per-request timeout).

Provider /models capability matrix (RESEARCH §"Provider /models Capability Matrix" — the SC#3 evidence base):
  OpenRouter → context_length + top_provider.max_completion_tokens + supported_parameters (contains "tools")
               → auto-fill context + max_output + native_tools
  Google     → inputTokenLimit + outputTokenLimit + supportedGenerationMethods (NO clean tools boolean)
               → auto-fill context + max_output ONLY; native_tools = unknown
  OpenAI / Anthropic-native / DeepSeek / Moonshot / Zhipu / MiniMax → IDs only → ALL caps = unknown
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Async fan-out over the hardcoded provider allowlist with honest per-provider outcomes</name>
  <files>backend/app/services/model_discovery_service.py, backend/tests/test_149_discovery.py</files>
  <read_first>
    - scripts/curate_models.py (PROVIDER_ENDPOINTS :67, _extract_openai_compat :140, _extract_minimax :153, the per-auth header/param block :199-209, google pagination :223-235, anthropic pagination :237-243, sort_newest_first :263)
    - backend/app/services/rerank_service.py (the httpx.AsyncClient async pattern)
    - backend/app/config.py (settings — how provider API keys are read from env, e.g. settings.openai_api_key)
  </read_first>
  <behavior>
    - `discover_all(keyed)` fans out concurrently and returns one list of per-provider dicts.
    - A provider whose key is absent (and auth != "public") → `{"provider": p, "status": "no_key"}` (skipped, not failed).
    - A provider returning 429 or any non-200 → `{"provider": p, "status": "http-429", "ids": []}` (excluded, not failed); the message is names-only — never echoes the response body or the key.
    - A 200 provider → `{"provider": p, "status": "ok", "ids": [...], "caps": {...}, "capabilities_returned": bool}` with ids in newest-first order and verbatim id casing.
    - Google + Anthropic results include models past the first page (pagination followed).
    - One provider raising an exception does NOT abort the others (`asyncio.gather(..., return_exceptions=True)` or per-task try/except).
  </behavior>
  <action>
    Create `backend/app/services/model_discovery_service.py`. Copy `PROVIDER_ENDPOINTS` VERBATIM from `curate_models.py:67` into the module (a hardcoded code-constant table — NO client-supplied URL ever reaches the HTTP client; this is the SSRF defense, Pitfall 7). Lift `_extract_openai_compat`, `_extract_minimax`, the per-auth header/param construction (bearer / anthropic `x-api-key`+`anthropic-version` / google `?key=` query / public), the google `nextPageToken` loop and the anthropic `has_more`/`last_id` loop, and `sort_newest_first` — preserving id casing verbatim (Pitfall 6: never normalize case or `MiniMax-M2.7` downgrades to inferred defaults). Convert the fetch from blocking `requests` to `httpx.AsyncClient` (async with, `timeout=12.0` per request) per the `rerank_service.py` idiom. Implement `async def discover_all(keyed: dict[str, str | None]) -> list[dict]` using `asyncio.gather` over `_fetch_provider(client, provider, key)`; each `_fetch_provider` returns the honest per-provider dict described in <behavior>. Resolve which providers are keyed from `settings` (env) — a `no_key` provider (auth != public) is skipped, never failed. Set `capabilities_returned=True` only for `google` and `openrouter` (the two providers that return any capability metadata); everything else False. Write `backend/tests/test_149_discovery.py::test_provider_outcomes` mocking `httpx.AsyncClient.get` (mock ALL 8 providers) to cover: no_key skip, 429 excluded-not-failed, 200 ok with ids, exception isolation, and google/anthropic pagination (two-page mock → both pages' ids present).
  </action>
  <verify>
    <automated>cd backend && ./venv/Scripts/python -m pytest tests/test_149_discovery.py::test_provider_outcomes -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/services/model_discovery_service.py` exists and exports `discover_all` + `PROVIDER_ENDPOINTS`
    - `PROVIDER_ENDPOINTS` has all 8 keys: openai, anthropic, google, deepseek, moonshot, zhipu, minimax, openrouter (grep the 8 provider names)
    - The module contains NO parameter/argument that accepts a provider URL/base from a caller (only PROVIDER_ENDPOINTS is used for URLs)
    - `test_provider_outcomes` asserts: no_key→status "no_key"; a 429 mock→status "http-429" with the provider still present in the result (not dropped); an exception on one provider leaves the other 7 results intact; a two-page google mock yields ids from both pages
    - `pytest tests/test_149_discovery.py::test_provider_outcomes` exits 0
  </acceptance_criteria>
  <done>The async fan-out returns honest per-provider outcomes over the hardcoded allowlist; pagination + casing preserved; test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Diff computation + propose-only capability fill (SC#3)</name>
  <files>backend/app/services/model_discovery_service.py, backend/tests/test_149_discovery.py</files>
  <read_first>
    - backend/app/services/model_discovery_service.py (Task 1 output — the fan-out results shape)
    - .planning/phases/149-model-registry-discovery/149-RESEARCH.md (§"Provider /models Capability Matrix" — the auto-fill asymmetry)
    - .planning/sketches/071-model-discovery-propose-confirm/README.md (the new/changed/vanished grouping + "unknown — you set it" contract)
  </read_first>
  <behavior>
    - `compute_diff(current, discovered)` returns groups: `new` (returned-but-not-in-current), `changed` (in-current with a differing returned capability), `vanished` (in-current-but-not-returned by a provider that responded ok).
    - `current` is INJECTED by the caller (the registry union: config + all DB rows) — the service does NOT read the DB itself, keeping it pure/testable and Wave-1-independent.
    - A provider whose status is `no_key`/`http-*`/exception contributes NO `vanished` entries (its models are unknown, not gone — the 058/060 lesson).
    - Every `new` model carries `enabled=false`.
    - Capability fill: for OpenRouter `new` models → context + max_output + native_tools filled from the payload; for Google → context + max_output filled, native_tools = `unknown`; for every other provider → ALL capability fields = `unknown`. `unknown` is a distinct sentinel the UI renders as the amber "you set it" input — NEVER a guessed value, NEVER an auto-enable.
  </behavior>
  <action>
    Add `compute_diff(current: dict[str, dict], discovered: list[dict]) -> dict` to the service. `current` maps model_id → its known capability dict (the caller supplies config ∪ DB rows). Build the returned-id set per provider from `discovered` (only `status=="ok"` providers contribute a definitive returned set). Compute `new` = returned ids absent from `current`; `changed` = ids present in both where a provider-returned capability differs from the stored value (only compare fields the provider actually returned); `vanished` = current ids belonging to an `ok` provider that were not in that provider's returned set (a provider that failed/no_key contributes none). Attach to each `new` entry `enabled=False` plus a per-field capability map where each field is either a concrete provider-returned value or the `unknown` sentinel per the asymmetry in <behavior> — OpenRouter fills context/max_output/native_tools, Google fills context/max_output only (native_tools=unknown), all others fill nothing (all unknown). Newest-first order the `new` list. Keep the result a plain dict/JSON-serializable structure (it is ephemeral — lives only in the HTTP response, D-149-12; no proposals table). Add `test_discovery` (a synthetic current + a mocked discovered list → asserts correct new/changed/vanished partition and vanished-excludes-failed-providers) and `test_propose_only` (a Google new model → native_tools is the `unknown` sentinel, never a boolean; an OpenAI new model → every capability unknown; an OpenRouter new model → native_tools filled from supported_parameters; NO new model has `enabled=True`).
  </action>
  <verify>
    <automated>cd backend && ./venv/Scripts/python -m pytest tests/test_149_discovery.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `compute_diff` exported; returns `new`/`changed`/`vanished` keys
    - `test_propose_only` asserts: a Google `new` model's `native_tools` is the `unknown` sentinel (not True/False); an OpenAI `new` model has ALL capability fields `unknown`; an OpenRouter `new` model has `native_tools` filled from `supported_parameters`; every `new` model has `enabled` == False
    - `test_discovery` asserts a `vanished` entry is produced ONLY for a provider whose status is `ok`, never for a `no_key`/`http-*` provider
    - `pytest tests/test_149_discovery.py` exits 0 (all discovery tests)
  </acceptance_criteria>
  <done>The diff partitions new/changed/vanished correctly, new models land disabled with propose-only capability fill (SC#3 upheld), and failed providers never manufacture false-vanished; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → external provider `/models` | Server-side outbound calls carrying provider API keys; keys are secrets, responses are untrusted |
| caller → discover_all | Provider selection (if any) must be a code-constant, never a URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-03 | Tampering / Info-disclosure (SSRF) | discover_all URL source | mitigate | URLs come ONLY from the hardcoded `PROVIDER_ENDPOINTS` table; no function parameter accepts a URL/base; provider selection (Plan 05) validates against `set(PROVIDER_ENDPOINTS)` (Pitfall 7) |
| T-149-04 | Information Disclosure | provider error messages | mitigate | Per-provider failure is names-only (`http-{status}` / `error-{ExceptionName}`); NEVER echo the response body or the API key (curate_models.py:218 discipline) |
| T-149-05 | Tampering (routing integrity, SC#3) | compute_diff capability fill | mitigate | New models land `enabled=false`; token limits fill for Google+OpenRouter only, native_tools for OpenRouter only; everything else is the `unknown` sentinel — never a guessed/auto-enabled capability (reproducing the silent no-tools bug is barred) |
| T-149-SC | Tampering | package installs | accept | Zero new packages (`httpx` already in requirements.txt per RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `discover_all` fans out over the hardcoded allowlist with honest per-provider outcomes (no_key / http-* / ok), pagination followed, casing preserved.
- `compute_diff` partitions new/changed/vanished; new models disabled; propose-only fill asymmetry holds.
- `pytest tests/test_149_discovery.py` green (all 8 providers mocked; no live network calls in tests).
</verification>

<success_criteria>
- The service is self-contained (imports the lifted extractors/endpoints; no admin.py or DB coupling — `current` is injected).
- SC#3 is provable in a unit test: no discovered capability the provider did not return is ever filled or auto-enabled.
- Zero new external packages.
</success_criteria>

<output>
Create `.planning/phases/149-model-registry-discovery/149-02-SUMMARY.md` when done.
</output>
