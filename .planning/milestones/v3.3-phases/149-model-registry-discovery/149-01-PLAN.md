---
phase: 149-model-registry-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/099_model_registry_deprecated.sql
  - supabase/full-schema.sql
  - backend/app/config.py
autonomous: false
requirements: [MODEL-01, MODEL-02]

must_haves:
  truths:
    - "D-149-04: model_capabilities_overrides has a `deprecated boolean NOT NULL DEFAULT false` column plus an optional `deprecated_reason text`"
    - "D-149-07: app_settings has an `llm_model_locked boolean NOT NULL DEFAULT false` column (the forward-compatible lock policy flag)"
    - "D-149-03: get_model_capability_async overlays `deprecated` onto the capability dict so a discovery-confirmed DB-only row carries its deprecated state with ZERO code edits"
    - "Migration 099 is applied to the live local DB and full-schema.sql is regenerated (no reset) and committed alongside the migration"
  artifacts:
    - path: "supabase/migrations/099_model_registry_deprecated.sql"
      provides: "deprecated + deprecated_reason columns on model_capabilities_overrides; llm_model_locked on app_settings"
      contains: "ADD COLUMN IF NOT EXISTS deprecated"
    - path: "backend/app/config.py"
      provides: "ModelCapability.deprecated field + deprecated in the DB-overlay field tuple"
      contains: "deprecated"
  key_links:
    - from: "backend/app/config.py:get_model_capability_async"
      to: "model_capabilities_overrides.deprecated"
      via: "DB-overlay field tuple"
      pattern: "deprecated"
---

<objective>
Lay the schema + read-path foundation for the model registry: add the `deprecated` state (D-149-04) and the `llm_model_locked` policy flag (D-149-07) to the DB, apply the migration to the live database, and teach the already-live capability read path to carry `deprecated` through its DB overlay (D-149-03) so a discovery-confirmed DB-only row works end-to-end with no code edit.

Purpose: Every downstream plan (registry read/write, guards, picker badge) needs these two columns and the overlaid field to exist first. This is the interface-first contract wave for the phase's data model.
Output: Migration 099 (applied + full-schema regenerated), `ModelCapability.deprecated` in the TypedDict, `deprecated` in the `get_model_capability_async` overlay tuple.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-model-registry-discovery/149-CONTEXT.md
@.planning/phases/149-model-registry-discovery/149-RESEARCH.md
@.planning/phases/149-model-registry-discovery/149-PATTERNS.md

<interfaces>
<!-- The read path is already live (mig 053). This plan only ADDS `deprecated` to the overlay
     tuple and the TypedDict. Do NOT touch the merge logic itself. -->

backend/app/config.py — ModelCapability TypedDict (~line 141, total=False):
  native_tools: bool
  provider: str
  llm_call_timeout_seconds: int
  max_output_tokens: int
  context_window_tokens: int
  capability_source: str          # "registry" | "inferred" | "db_override"
  → ADD: deprecated: bool         # NotRequired via total=False

backend/app/config.py:680 — get_model_capability_async(model_id) DB-overlay loop:
  overlays these DB fields when non-None:
    ("llm_call_timeout_seconds", "context_window_tokens", "max_output_tokens", "native_tools")
  → ADD "deprecated" to that tuple so the badge state flows to the registry + picker.

supabase migration precedent (053_settings_unification.sql §2 created the table;
098_feature_visibility.sql is the metadata-only ALTER shape to mirror):
  model_capabilities_overrides(model_id PK, provider, llm_call_timeout_seconds,
    context_window_tokens, max_output_tokens, native_tools, enabled)  — mig 053
  053 already has `ENABLE ROW LEVEL SECURITY` + a `FOR SELECT TO authenticated USING (true)`
  read-all policy → NO new policy needed; existing rows default deprecated=false (no backfill).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 099 (deprecated + deprecated_reason + llm_model_locked)</name>
  <files>supabase/migrations/099_model_registry_deprecated.sql</files>
  <read_first>
    - supabase/migrations/098_feature_visibility.sql (the metadata-only ALTER shape + header comment style to mirror)
    - supabase/migrations/053_settings_unification.sql (the model_capabilities_overrides table + its existing RLS read-all policy — confirm no new policy is needed)
  </read_first>
  <action>
    Create `supabase/migrations/099_model_registry_deprecated.sql` (099 is the next free number — 098 is latest on disk). Three idempotent DDL statements:
    (1) `ALTER TABLE public.model_capabilities_overrides ADD COLUMN IF NOT EXISTS deprecated boolean NOT NULL DEFAULT false;` — D-149-04 "deprecated ≠ disabled": deprecated = provider is sunsetting / vanished, warn-and-steer, can stay enabled.
    (2) `ALTER TABLE public.model_capabilities_overrides ADD COLUMN IF NOT EXISTS deprecated_reason text;` — optional operator note (nullable).
    (3) `ALTER TABLE public.app_settings ADD COLUMN IF NOT EXISTS llm_model_locked boolean NOT NULL DEFAULT false;` — D-149-07 forward-compatible lock policy flag beside `llm_model`; when the v3.4 per-user layer arrives, locked = users cannot override.
    Include a header comment block (mirror 098's style): phase 149, the D-IDs, and a one-line "deprecated ≠ disabled; llm_model_locked pins the org default (one lock max, enforced in app code)". Do NOT add any RLS policy — 053 already ships `FOR SELECT TO authenticated USING (true)` on model_capabilities_overrides; app_settings write is service-role only. Existing override rows inherit `deprecated=false` (no data backfill statement needed).
  </action>
  <verify>
    <automated>test -f supabase/migrations/099_model_registry_deprecated.sql && grep -q "ADD COLUMN IF NOT EXISTS deprecated boolean" supabase/migrations/099_model_registry_deprecated.sql && grep -q "llm_model_locked boolean" supabase/migrations/099_model_registry_deprecated.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/099_model_registry_deprecated.sql` exists
    - File contains `ADD COLUMN IF NOT EXISTS deprecated boolean NOT NULL DEFAULT false`
    - File contains `ADD COLUMN IF NOT EXISTS deprecated_reason text`
    - File contains `ADD COLUMN IF NOT EXISTS llm_model_locked boolean NOT NULL DEFAULT false`
    - File contains NO `CREATE POLICY` / `db push` / `db reset` statements
    - Filename matches `099_[a-z_]+\.sql` (digits-underscore-name; no letter suffix)
  </acceptance_criteria>
  <done>Migration 099 authored with the three idempotent ADD COLUMN statements and a phase-149 header comment.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Apply migration 099 to the live DB + regenerate full-schema</name>
  <files>supabase/full-schema.sql</files>
  <action>
    Per the CLAUDE.md project rule (NEVER `supabase db push` / `db reset` — it wipes dev data), the operator applies the migration by hand and Claude regenerates the schema artifact:
    1. Operator opens the local Supabase SQL editor (Studio, local project), pastes the entire contents of `supabase/migrations/099_model_registry_deprecated.sql`, and runs it (idempotent — safe to re-run).
    2. Operator confirms the columns exist:
       `SELECT column_name FROM information_schema.columns WHERE table_name = 'model_capabilities_overrides' AND column_name IN ('deprecated','deprecated_reason');` (expect 2 rows) and
       `SELECT column_name FROM information_schema.columns WHERE table_name = 'app_settings' AND column_name = 'llm_model_locked';` (expect 1 row).
    3. Operator types "applied" — Claude then runs `bash scripts/regenerate-full-schema.sh` (NO `--reset` — a live-DB dump) and commits `supabase/migrations/099_model_registry_deprecated.sql` + the regenerated `supabase/full-schema.sql` together.
    Cloud parity is DEFERRED to the standing production-push checklist (migrations 079+ are already pending on cloud; do not touch cloud now). This BLOCKING step is mandatory: build/type checks pass without the live column, so skipping it creates a false-positive verification state.
  </action>
  <verify>
    <automated>grep -q "llm_model_locked" supabase/full-schema.sql && grep -q "deprecated" supabase/full-schema.sql && echo SCHEMA_REGENERATED</automated>
  </verify>
  <resume-signal>Type "applied" once the three columns exist in the local DB (then Claude regenerates full-schema.sql + commits).</resume-signal>
  <done>The three columns exist in the live local DB; `supabase/full-schema.sql` is regenerated (no reset) and committed with the migration.</done>
</task>

<task type="auto">
  <name>Task 3: Add `deprecated` to ModelCapability TypedDict + the read-path overlay</name>
  <files>backend/app/config.py</files>
  <read_first>
    - backend/app/config.py (lines ~141-159 for the ModelCapability TypedDict; lines ~680-712 for get_model_capability_async's overlay loop)
  </read_first>
  <action>
    Two surgical edits, NO new static models (GPT-5.6 on 2026-07-11 was the last hand-add — the DB is the living registry now, D-149-03):
    (1) In the `ModelCapability` TypedDict (~line 141, `total=False`), add `deprecated: bool` with a short docstring line noting it is informational (end-user effect is a badge only, D-149-05; `enabled` alone controls availability).
    (2) In `get_model_capability_async` (~line 697), add the string `"deprecated"` to the overlay field tuple currently reading `("llm_call_timeout_seconds", "context_window_tokens", "max_output_tokens", "native_tools")`, so a non-None DB `deprecated` value overlays onto `base`. Do NOT change the merge structure, the `capability_source="db_override"` tag, or the `_build_inferred_defaults` base — only extend the tuple.
    Add a unit test `backend/tests/test_149_config_overlay.py` that mocks `_load_model_overrides` to return a row with `deprecated=true` and asserts `(await get_model_capability_async("some-model")).get("deprecated") is True` and `capability_source == "db_override"`.
  </action>
  <verify>
    <automated>cd backend && ./venv/Scripts/python -m pytest tests/test_149_config_overlay.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/config.py` ModelCapability TypedDict contains `deprecated: bool`
    - The overlay tuple in `get_model_capability_async` includes `"deprecated"` (grep: `grep -n '"deprecated"' backend/app/config.py` returns a line inside the overlay loop)
    - No new keys added to the `MODEL_CAPABILITIES` dict (the diff touches only the TypedDict + the overlay tuple)
    - `pytest tests/test_149_config_overlay.py` exits 0; the test asserts a mocked `deprecated=true` DB row surfaces `deprecated is True` with `capability_source == "db_override"`
  </acceptance_criteria>
  <done>ModelCapability carries an optional `deprecated`; the async read path overlays it from the DB row; the overlay test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → DB schema | Migration is applied by the human operator via the Supabase SQL editor (not a code path); no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-01 | Tampering | migration 099 DDL | accept | Idempotent `ADD COLUMN IF NOT EXISTS` only; no data migration; no destructive statement; applied by operator via SQL editor (never `db push`/`db reset`) |
| T-149-02 | Information Disclosure | new columns | accept | `deprecated`/`deprecated_reason` are global config (not user data); `model_capabilities_overrides` already has a `SELECT TO authenticated USING (true)` read-all policy (053); `llm_model_locked` on `app_settings` is service-role-read only |
| T-149-SC | Tampering | package installs | accept | No package installs in this plan (zero new deps per RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- Migration file exists, is idempotent, and adds exactly the three columns.
- The three columns exist in the live local DB (Task 2 checkpoint) and `full-schema.sql` is regenerated + committed with the migration.
- The read-path overlay carries `deprecated`; `pytest tests/test_149_config_overlay.py` green.
</verification>

<success_criteria>
- `supabase/migrations/099_model_registry_deprecated.sql` applied to the live local DB; `supabase/full-schema.sql` regenerated (no reset) and committed with the migration.
- `ModelCapability` has `deprecated`; `get_model_capability_async` overlays it; overlay test green.
- Zero new `MODEL_CAPABILITIES` entries; zero new RLS policies.
</success_criteria>

<output>
Create `.planning/phases/149-model-registry-discovery/149-01-SUMMARY.md` when done.
</output>
