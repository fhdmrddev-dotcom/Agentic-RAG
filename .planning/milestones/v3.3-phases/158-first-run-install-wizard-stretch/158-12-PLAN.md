---
phase: 158-first-run-install-wizard-stretch
plan: 12
type: execute
wave: 7
depends_on: ["158-02", "158-06", "158-11"]
files_modified:
  - supabase/full-schema.sql
autonomous: false
requirements: [DEPLOY-02]

must_haves:
  truths:
    - "Migration 102 (`app_settings.setup_complete`) is applied to the LIVE local Supabase via the SQL editor (or psycopg2 to 127.0.0.1:54322) — NEVER `supabase db push` / `db reset` (preserves dev data, per CLAUDE.md)"
    - "`supabase/full-schema.sql` is regenerated by `bash scripts/regenerate-full-schema.sh` (no `--reset`) so the greenfield deploy artifact includes `setup_complete`"
    - "Migration 102 is appended to the pending cloud-parity list (migs 099/100/101 + SECRETS_ENCRYPTION_KEY) — cloud apply stays operator-gated"
    - "This is the launch point for the DEFERRED live end-to-end operator UAT (analog of Phase 157's D-09 smoke) — code-complete + verify-work + secure-phase are autonomous; the live run is operator-gated"
  artifacts:
    - path: "supabase/full-schema.sql"
      provides: "regenerated bootstrap artifact including app_settings.setup_complete"
      contains: "setup_complete"
  key_links:
    - from: "supabase/migrations/102_setup_complete.sql"
      to: "live local DB + supabase/full-schema.sql"
      via: "SQL-editor apply then regenerate (no reset)"
      pattern: "setup_complete"
---

<objective>
The single operator-gated infra step: apply migration 102 to the live local DB (via the Supabase SQL editor, never a reset), regenerate `supabase/full-schema.sql`, and record the cloud-parity note. Placed last so it never blocks the autonomous code chain; it is also the launch point for the deferred live wizard UAT.

Purpose: make `app_settings.setup_complete` real on the running DB so the finalize DB-write half works at runtime, and keep the greenfield deploy artifact honest.
Output: applied migration 102 + regenerated `full-schema.sql`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/158-first-run-install-wizard-stretch/158-VALIDATION.md
@supabase/migrations/102_setup_complete.sql
@supabase/SETUP.md

<interfaces>
CLAUDE.md migration discipline: apply by pasting into the LOCAL Supabase SQL editor (or psycopg2 to 127.0.0.1:54322) — NEVER `supabase db push` / `db reset`; then `bash scripts/regenerate-full-schema.sh` (no `--reset`, live-DB dump); commit both files. Never hand-edit full-schema.sql.
Precedent operator-apply plans: 146-01, 148-03, 150-02, 151-02 ("[BLOCKING] live apply + full-schema regen").
Cloud-parity pending (MEMORY): migs 099/100/101 + SECRETS_ENCRYPTION_KEY → append 102.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: apply migration 102 to the live local DB (operator step)</name>
  <files>supabase/migrations/102_setup_complete.sql (applied to the live DB, not edited)</files>
  <read_first>
    - supabase/migrations/102_setup_complete.sql (authored in 158-02 — the exact DDL to apply)
    - CLAUDE.md → the migration rules (apply via the Supabase SQL editor / psycopg2 to :54322; NEVER `db push` / `db reset`) ; supabase/SETUP.md
  </read_first>
  <what-built>
    `supabase/migrations/102_setup_complete.sql` (authored in 158-02) adds `app_settings.setup_complete boolean NOT NULL DEFAULT false` + the `('global') ON CONFLICT DO NOTHING` row. It must be applied to the live local Supabase before the wizard's finalize DB-write works at runtime.
  </what-built>
  <action>
    OPERATOR STEP (blocking — Claude cannot paste into the Supabase Studio GUI). Apply migration 102 to the live LOCAL Supabase WITHOUT a reset (preserves dev data), by EITHER path: (1) open the local Supabase Studio SQL editor, paste the full contents of `supabase/migrations/102_setup_complete.sql`, and Run; OR (2) in autonomous/unattended mode, apply via psycopg2 to `127.0.0.1:54322` (the documented non-reset equivalent per the project's evidence-tools). NEVER `supabase db push` / `db reset`. Do not proceed to Task 2 (the full-schema regen) until the column is present on the live DB — the regen is a live-DB dump.
  </action>
  <how-to-verify>
    After applying, verify the column exists on the live local DB:
    `python -c "import psycopg2; c=psycopg2.connect(host='127.0.0.1',port=54322,user='postgres',password='postgres',dbname='postgres'); cur=c.cursor(); cur.execute(\"select column_name from information_schema.columns where table_name='app_settings' and column_name='setup_complete'\"); print(cur.fetchone())"`
    → prints `('setup_complete',)`.
  </how-to-verify>
  <acceptance_criteria>
    - the `information_schema.columns` query above returns `('setup_complete',)` (column present on the live local DB)
    - no `db push` / `db reset` was run (dev data preserved)
  </acceptance_criteria>
  <resume-signal>Type "applied" once migration 102 is on the live local DB (column verified present), or describe any error.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: regenerate full-schema.sql (no reset) + cloud-parity note</name>
  <files>supabase/full-schema.sql</files>
  <read_first>
    - CLAUDE.md → the migration rules (regenerate-full-schema.sh = live-DB dump, no reset; never hand-edit) ; supabase/SETUP.md
    - scripts/regenerate-full-schema.sh (confirm the no-`--reset` default before running)
  </read_first>
  <action>
    Run `bash scripts/regenerate-full-schema.sh` (NO `--reset` — the default live-DB dump) to rebuild `supabase/full-schema.sql` including the new `setup_complete` column. Do NOT hand-edit `full-schema.sql`. Append migration 102 to the pending cloud-parity list wherever it is tracked (the OPERATOR.md / STATE cloud-parity note beside migs 099/100/101 + `SECRETS_ENCRYPTION_KEY`) so the operator applies it to cloud Supabase at the next production push (cloud apply stays operator-gated per CLAUDE.md). Record in the SUMMARY that the DEFERRED live end-to-end operator UAT (fresh `docker compose -f docker-compose.prod.yml up --build` → read the setup token from `docker compose logs backend` → walk `/setup` → finalize → `docker compose restart backend` → log in as operator) is the SC#1/2/3 lived-experience proof, operator-gated per D-18/158-VALIDATION.md.
  </action>
  <verify>
    <automated>grep -c "setup_complete" supabase/full-schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "setup_complete" supabase/full-schema.sql` >= 1 (the regenerated artifact includes the column)
    - `bash scripts/check-deploy-drift.sh` still exits 0 (no artifact drift introduced)
    - the SUMMARY records the cloud-parity append + the deferred live-UAT runbook
  </acceptance_criteria>
  <done>Migration 102 applied live, full-schema.sql regenerated with setup_complete, cloud-parity list updated, deferred live-UAT runbook recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| operator → live DB | Applying DDL to the running DB is an operator-gated infra action |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-158-mig | Tampering | migration 102 apply | mitigate | Applied via the SQL editor / psycopg2 (no `db push`/`db reset`, so dev data is preserved); `ADD COLUMN IF NOT EXISTS` is idempotent and re-runnable; the `('global') ON CONFLICT` row prevents the A6 silent-no-op. Cloud apply stays operator-gated (parity list). |
| T-158-SC | Tampering | pip installs | accept | Zero new packages. |
</threat_model>

<verification>
- `python -c "..."` confirms `app_settings.setup_complete` exists on the live local DB
- `grep -c "setup_complete" supabase/full-schema.sql` >= 1
- `bash scripts/check-deploy-drift.sh` exits 0
</verification>

<success_criteria>
Migration 102 is applied to the live local DB (no reset), `full-schema.sql` is regenerated including `setup_complete`, the cloud-parity list is updated, and the deferred live operator UAT runbook is recorded. This is the only operator-gated plan in the phase.
</success_criteria>

<output>
Create `.planning/phases/158-first-run-install-wizard-stretch/158-12-SUMMARY.md` when done.
</output>
