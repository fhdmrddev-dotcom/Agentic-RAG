---
phase: 158-first-run-install-wizard-stretch
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/conftest.py
  - backend/tests/test_setup_gate.py
  - backend/tests/test_setup_token.py
  - backend/tests/test_setup_overlay.py
  - backend/tests/test_setup_boot_tolerant.py
  - backend/tests/test_setup_finalize.py
  - backend/tests/test_setup_idempotent.py
  - backend/tests/test_setup_status.py
  - backend/tests/test_setup_detect.py
  - backend/tests/test_setup_probe.py
  - backend/tests/test_setup_operator.py
  - backend/tests/test_setup_provider.py
  - backend/tests/test_setup_smoke.py
  - frontend/src/lib/__tests__/supabase.test.ts
  - frontend/src/pages/__tests__/SetupWizard.test.tsx
autonomous: true
requirements: [DEPLOY-02]

must_haves:
  truths:
    - "The Nyquist test scaffold exists: every SC row in 158-VALIDATION.md has a named test file, so later waves land green against a real gate (not a moving target)"
    - "conftest exposes a temp SETUP_STORE_PATH fixture + mock asyncpg/redis/supabase for submitted-value probes, reusing the existing mock_asyncpg_pool / _reset_pg_pool_singleton idioms"
    - "The byte-identical invariant has a named home: test_setup_gate.py::test_configured_box_noop"
    - "Collection is clean — new-module tests importorskip until their module exists; modified-module tests fail RED until behavior lands; ZERO collection errors"
  artifacts:
    - path: "backend/tests/conftest.py"
      provides: "setup-store temp path + mock asyncpg/redis/supabase fixtures"
      contains: "SETUP_STORE_PATH"
    - path: "backend/tests/test_setup_gate.py"
      provides: "gate 503-vs-allowlist-vs-latched-noop + byte-identical invariant"
      contains: "test_configured_box_noop"
    - path: "frontend/src/lib/__tests__/supabase.test.ts"
      provides: "runtime-config shim contract stub"
    - path: "frontend/src/pages/__tests__/SetupWizard.test.tsx"
      provides: "pre-auth branch + lock-out contract stub"
  key_links:
    - from: "backend/tests/test_setup_*.py"
      to: "158-VALIDATION.md Per-Task Verification Map"
      via: "one test file per SC row"
      pattern: "pytest.importorskip|pytest.mark"
---

<objective>
Author the Wave-0 Nyquist test scaffold for the entire phase: the 12 `backend/tests/test_setup_*.py` contracts, the `conftest.py` fixtures they share, and the 2 frontend vitest stubs — as clean-collecting failing/skip stubs that every later wave flips green.

Purpose: `nyquist_validation: true`. Every impl task in Waves 1-6 verifies against a test that already exists here, so no wave chases a moving target and the byte-identical invariant has a permanent home.
Output: 12 backend test files + conftest fixtures + 2 frontend vitest stubs. No production code.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/158-first-run-install-wizard-stretch/158-VALIDATION.md
@.planning/phases/158-first-run-install-wizard-stretch/158-RESEARCH.md
@.planning/phases/158-first-run-install-wizard-stretch/158-PATTERNS.md

<interfaces>
<!-- The exact modules the scaffold will (eventually) exercise. Executor: DO NOT create these here — importorskip on the NEW ones so collection is clean until the impl wave. -->
NEW modules (use `pytest.importorskip("...")` at file top — skips cleanly until created):
  app.services.setup_store        (setup_finalized, read_store, write_store, get_or_create_token, verify_token, INFRA_KEYS)
  app.middleware.setup            (SetupMiddleware)
  app.services.setup_service      (probe_submitted_postgres/redis/supabase, bootstrap_operator, run_smoke_checks)
  app.api.setup                   (router, require_setup_token)
MODIFIED modules (already import-safe; assert the NEW behavior → RED until it lands):
  app.config                      (apply_setup_overlay / load_settings_with_overlay)
  app.models.user_settings        (setup_complete)
  app.main                        (setup-mode-tolerant lifespan)
  frontend/src/lib/supabase.ts    (hydrateSupabaseFromRuntime, reassignable `export let supabase`)

Reuse verbatim (conftest self-precedent, from PATTERNS.md §Backend tests):
  backend/tests/conftest.py:233-256   _reset_pg_pool_singleton (autouse, event-loop-bound reset)
  backend/tests/conftest.py:564-571   mock_asyncpg_pool (_MockAsyncpgPool .calls recorder)
  backend/tests/test_147_maintenance_mw.py   — the ASGI TestClient + monkeypatch-the-read-seam template
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: conftest fixtures + the 6 SC#2/SC#3-invariant backend stubs</name>
  <files>backend/tests/conftest.py, backend/tests/test_setup_gate.py, backend/tests/test_setup_token.py, backend/tests/test_setup_overlay.py, backend/tests/test_setup_boot_tolerant.py, backend/tests/test_setup_finalize.py, backend/tests/test_setup_idempotent.py</files>
  <read_first>
    - backend/tests/conftest.py:225-256 (the `_reset_pg_pool_singleton` autouse fixture) + :558-571 (`mock_asyncpg_pool` / `_MockAsyncpgPool`) — extend, do not fork
    - backend/tests/test_147_maintenance_mw.py — the ASGI-gate test idiom: a tiny `_make_app()` wired with ONE middleware + dummy routes, drive the flag by monkeypatching the middleware read seam; copy the 503-body assertion, the `/administrate`-not-allowlisted prefix-boundary test, and the bare-TestClient-no-lifespan trick
    - 158-VALIDATION.md → "Per-Task Verification Map" (the exact behavior each file asserts) + "Wave 0 Requirements"
    - 158-RESEARCH.md → Pattern 4 (gate/latch), Pattern 1 (overlay), Pattern 3 (boot-tolerant), Code Examples (token constant-time)
  </read_first>
  <action>
    In conftest.py add: a function-scoped `setup_store_path` fixture that sets `SETUP_STORE_PATH` via `monkeypatch.setenv` to a `tmp_path / "setup.json"` (so `app.services.setup_store` reads a throwaway file); a `mock_submitted_supabase` fixture returning a MagicMock with `.auth.admin.create_user` and a schema-sentinel `.table(...).select(...)` path; reuse the existing `mock_asyncpg_pool` for submitted-DSN probes and `_reset_pg_pool_singleton` as-is. Do NOT create any `app.services.setup_*` module.
    Author each of the 6 stub files. For files whose target is a NEW module (test_setup_gate → `app.middleware.setup`, test_setup_token → `app.services.setup_store`, test_setup_finalize → `app.services.setup_store`, test_setup_idempotent → `app.api.setup`), put `pytest.importorskip("app.<module>")` at the TOP so the file SKIPS cleanly until the module exists. For files whose target is a MODIFIED existing module (test_setup_overlay → `app.config.apply_setup_overlay`, test_setup_boot_tolerant → the lifespan guard), write real assertions that FAIL RED until the behavior lands (guard the not-yet-present symbol with `getattr(mod, "apply_setup_overlay", None)` + an explicit `assert ... is not None` so it's an honest RED, not a collection error).
    Name (as function stubs with docstrings quoting the VALIDATION behavior) every assertion: test_setup_gate — pre-finalize non-allowlisted→503, allowlist (`/health`, `/public-config`, `/setup`, `/setup/status`) passes, `/administrate`-not-allowlisted, and `test_configured_box_noop` (latched sticky-True → single-bool passthrough = the byte-identical invariant); test_setup_token — missing/wrong `X-Setup-Token`→401, correct passes, `verify_token` uses `hmac.compare_digest`; test_setup_overlay — store value overrides a placeholder env infra key, app-level keys never overridden; test_setup_boot_tolerant — setup-mode lifespan does NOT call `assert_action_types_synced`, configured-mode DOES; test_setup_finalize — finalize writes BOTH markers and `setup_finalized()` latches sticky-True; test_setup_idempotent — a re-POST after finalize→409, each step re-run = no duplicate write.
    NEVER `db push` / `db reset` / touch a live DB — all DB interaction is mocked.
  </action>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/test_setup_gate.py tests/test_setup_token.py tests/test_setup_overlay.py tests/test_setup_boot_tolerant.py tests/test_setup_finalize.py tests/test_setup_idempotent.py -q` → collects with 0 ERRORS (skips/xfails/reds allowed; NO ImportError at collection)
    - `grep -c "def test_configured_box_noop" backend/tests/test_setup_gate.py` == 1 (the byte-identical invariant home)
    - `grep -q "SETUP_STORE_PATH" backend/tests/conftest.py` succeeds
    - `cd backend && python -m pytest -q` still collects the full suite with 0 net-new collection errors vs the pre-existing baseline
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: the 6 step-contract backend stubs</name>
  <files>backend/tests/test_setup_status.py, backend/tests/test_setup_detect.py, backend/tests/test_setup_probe.py, backend/tests/test_setup_operator.py, backend/tests/test_setup_provider.py, backend/tests/test_setup_smoke.py</files>
  <read_first>
    - backend/tests/test_150_save_seam.py — the `_StubPool` / encrypt-on-write idiom; test_setup_provider copies `test_encrypts_on_write` (asserts an `enc:v1:` envelope lands through `save_app_settings`) verbatim in intent
    - 158-VALIDATION.md → Per-Task Verification Map rows for D-08/D-10/D-11/D-12/D-13
    - 158-RESEARCH.md → Pattern 5 (submitted-value throwaway probes + `to_regclass` schema sentinel), Pattern 6 (operator bootstrap `admin.create_user(email_confirm=True)`), §Security Domain (SSRF sanitization: return only `type(exc).__name__`)
  </read_first>
  <action>
    Author 6 stub files, each `pytest.importorskip("app.services.setup_service")` (all target the new service) at top. Name every assertion: test_setup_status — `GET /setup/status` returns `needs_setup` from the static marker+placeholder check (no live DB probe); test_setup_detect — env-detect returns `in_docker` / `store_present` / `db_reachable` / `redis_reachable` flags (light); test_setup_probe — submitted-value probes: reachable→`state:up`, schema-absent→`schema_present:false`, bad DSN→`state:down` with a SANITIZED reason (asserts the raw exception string / host is NOT reflected — only `type(exc).__name__`); test_setup_operator — `bootstrap_operator` calls `admin.create_user` with `email_confirm=True` and upserts `operator_users` with `ON CONFLICT (user_id) DO NOTHING`, and a re-run with an existing email returns `already_exists` (no 500); test_setup_provider — a provider key routes through `save_app_settings` and lands as an `enc:v1:` envelope (encrypt-on-write via `SECRET_COLUMNS`), toggling the key via `monkeypatch.setattr(settings, "secrets_encryption_key", ...)`; test_setup_smoke — the 5-way checklist (Supabase auth / Postgres+schema / Redis PING / provider key / operator row); any red ⇒ `all_green:false`.
    Drive the data-access layer the seam actually uses (project lesson: match mocks to the data-access layer) — use `mock_asyncpg_pool` for the operator upsert / provider write and the `mock_submitted_supabase` fixture for `admin.create_user`.
  </action>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/test_setup_status.py tests/test_setup_detect.py tests/test_setup_probe.py tests/test_setup_operator.py tests/test_setup_provider.py tests/test_setup_smoke.py -q` → collects with 0 ERRORS (all skip via importorskip until Wave 2)
    - each file names ≥1 test function whose docstring quotes its VALIDATION behavior row
    - `grep -rl "type(exc).__name__\|__name__" backend/tests/test_setup_probe.py` is non-empty (SSRF-sanitization assertion present)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: the 2 frontend vitest stubs</name>
  <files>frontend/src/lib/__tests__/supabase.test.ts, frontend/src/pages/__tests__/SetupWizard.test.tsx</files>
  <read_first>
    - frontend/src/lib/supabase.ts (the current eager `createClient` at module load — the fragile init the shim will harden)
    - an existing `frontend/src/**/__tests__/*.test.tsx` (e.g. the layout `__tests__`) for the vitest + @testing-library/react house idiom
    - 158-VALIDATION.md rows SC#3/D-07 and SC#3/D-06 ; 158-RESEARCH.md Pattern 8 (runtime public-config shim) + Pitfall 4 (placeholder URL throws at import)
    - 158-UI-SPEC.md §0 (shell + entry) and §9 (finalized lock-out) for the SetupWizard contract
  </read_first>
  <action>
    Author both as `it.todo(...)` contract stubs that name every Wave-8/Wave-11 assertion but import ZERO not-yet-built symbols (mirror the 156-01 `it.todo` scaffold precedent — no import of `hydrateSupabaseFromRuntime` or `SetupWizard` yet, so collection is clean and there are 0 failures).
    supabase.test.ts todos: "createClient never throws at import when VITE_SUPABASE_URL is a `<placeholder>`"; "hydrateSupabaseFromRuntime overlays runtime creds from GET /public-config, keeping baked VITE_* as fallback"; "a real baked URL is NOT overridden by an identical runtime URL".
    SetupWizard.test.tsx todos: "App renders <SetupWizard/> when GET /api/setup/status returns needs_setup:true"; "a `window.location.pathname === '/setup'` visit forces the wizard"; "post-finalize /setup renders the 'Setup is already complete' lock-out (SC#2), never a config field".
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/lib/__tests__/supabase.test.ts src/pages/__tests__/SetupWizard.test.tsx` → 0 failures (todos only), 0 import errors
    - neither file imports `hydrateSupabaseFromRuntime` or `SetupWizard` (grep: `grep -c "hydrateSupabaseFromRuntime\|SetupWizard" frontend/src/lib/__tests__/supabase.test.ts` == 0)
    - `npx tsc -b` (frontend) shows 0 net-new errors vs the SEED-056/049 baseline
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness → app modules | Tests import app modules; no network, no live DB, no new runtime surface |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-158-SC | Tampering | pip installs | accept | Phase adds ZERO packages (158-RESEARCH Package Legitimacy Audit — "none proposed"); scaffold uses only pytest/vitest already vendored. If a dev-only helper is ever added, run the Package Legitimacy Gate first. |
| T-158-scaffold | Information Disclosure | test fixtures | mitigate | The SETUP_STORE_PATH fixture writes to `tmp_path` only; no real secrets, DSNs, or tokens in fixtures — mocks use obvious dummy values. |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_setup_*.py -q` → clean collection, 0 ERRORS (skips/reds expected pre-impl)
- `cd frontend && npx vitest run src/lib/__tests__/supabase.test.ts src/pages/__tests__/SetupWizard.test.tsx` → 0 failures
- No file outside `backend/tests/` and `frontend/src/**/__tests__/` is modified
</verification>

<success_criteria>
All 12 backend `test_setup_*.py` + conftest fixtures + 2 frontend vitest stubs exist; the full backend suite collects with 0 net-new collection errors; `test_configured_box_noop` (the byte-identical invariant) is named; frontend todos run with 0 failures.
</success_criteria>

<output>
Create `.planning/phases/158-first-run-install-wizard-stretch/158-01-SUMMARY.md` when done.
</output>
