---
phase: 158-first-run-install-wizard-stretch
plan: 11
type: execute
wave: 6
depends_on: ["158-08", "158-09", "158-10"]
files_modified:
  - frontend/src/pages/SetupWizard.tsx
  - frontend/src/App.tsx
  - frontend/src/pages/__tests__/SetupWizard.test.tsx
autonomous: true
requirements: [DEPLOY-02]

must_haves:
  truths:
    - "D-06: a pre-auth branch in `App.tsx` (BEFORE `if (!user)`) probes `GET /api/setup/status` at startup and renders `<SetupWizard/>` when `needs_setup`; a `window.location.pathname === '/setup'` check also forces it (no react-router)"
    - "D-06: post-finalize, `/setup` renders the `<FinalizedLockout/>` 'Setup is already complete' state (SC#2), never a config field"
    - "D-14: the wizard is idempotent — completed steps are click-to-revisit (LifecycleStepper), re-entry shows current state, no duplicate writes; after finalize the only surface is the lock-out"
    - "`App.tsx` calls `hydrateSupabaseFromRuntime` at bootstrap so login works with wizard-entered Supabase creds; the configured-box render path (AuthPage → ChatLayout) stays byte-identical"
    - "SetupWizard is a full-page step machine (LifecycleStepper 6 nodes: Detect · Preset · Connect · Operator · Provider · Smoke) — no react-router, pathname check only"
  artifacts:
    - path: "frontend/src/pages/SetupWizard.tsx"
      provides: "full-page 6-step wizard host (LifecycleStepper + the 9 step components) + finalize/lockout flow"
      min_lines: 120
      contains: "LifecycleStepper"
    - path: "frontend/src/App.tsx"
      provides: "pre-auth needs_setup branch + /setup pathname + hydrateSupabaseFromRuntime bootstrap"
      contains: "SetupWizard"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "getSetupStatus() + window.location.pathname"
      via: "pre-auth branch before !user"
      pattern: "getSetupStatus|/setup"
    - from: "frontend/src/pages/SetupWizard.tsx"
      to: "the 9 components/setup/* + setupApi.ts"
      via: "step machine composition"
      pattern: "SetupTokenGate|SmokeChecklist"
---

<objective>
Assemble the wizard: the `SetupWizard.tsx` full-page step-machine host that wires the token gate + 6 steps + smoke/finalize/lock-out, and the `App.tsx` pre-auth branch that probes `GET /api/setup/status`, renders the wizard when `needs_setup` (or on a literal `/setup` path), and hydrates the runtime Supabase creds — all without react-router and with the configured-box path byte-identical.

Purpose: D-06 (pre-auth branch + /setup path + lock-out), D-14 (idempotent host). Consumes 158-08 (setupApi/hydrate) + 158-09/10 (the 9 components).
Output: `SetupWizard.tsx` (NEW) + `App.tsx` (branch). Flips `SetupWizard.test.tsx` green.

**Load `Skill("sketch-findings-agentic-rag")` before building — the ControlRoomPage shell + LifecycleStepper rail ARE the acceptance bar (G-2 override, D-17).**
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/158-first-run-install-wizard-stretch/158-UI-SPEC.md
@.planning/phases/158-first-run-install-wizard-stretch/158-PATTERNS.md
@.planning/phases/158-first-run-install-wizard-stretch/158-08-PLAN.md

<interfaces>
Host analogs (compose) — PATTERNS.md §reused UI primitives + §App.tsx:
  components/admin/ControlRoomPage.tsx:36-93 (full-page tab/step machine, NO react-router — the shell to mirror)
  components/skills/studio/LifecycleStepper.tsx (full/strip variants; NODE_TONE; click-to-revisit completed nodes = D-14 idempotency)
  App.tsx:153-163 (the `if (loading)` spinner + `if (!user)` AuthPage branch — insert the needs_setup branch BEFORE `!user`)
  App.tsx:23-62 MaintenanceBanner (the public-probe/poll idiom — the startup needs_setup probe is its one-shot form)
The 9 step components (158-09/10): SetupTokenGate, EnvironmentDetectCard, PresetPickerStep, ConnectionBindStep, SchemaGuidancePanel, OperatorBootstrapStep, ProviderKeyStep, SmokeChecklist, FinalizedLockout
158-08 seams: getSetupStatus, hydrateSupabaseFromRuntime, setupApi.*
UI-SPEC.md §0 (shell + entry), §7-§9 (smoke/finalize/lockout), Responsive (strip variant < sm)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SetupWizard.tsx — full-page 6-step host</name>
  <files>frontend/src/pages/SetupWizard.tsx</files>
  <read_first>
    - components/admin/ControlRoomPage.tsx:36-93 (full-page step-machine shell, no router) + components/skills/studio/LifecycleStepper.tsx (the 6-node rail + click-to-revisit)
    - the 9 setup components (158-09/10) + lib/setupApi.ts ; 158-UI-SPEC.md §0 + §7-§9 + Responsive
  </read_first>
  <action>
    Create `frontend/src/pages/SetupWizard.tsx`: a full-page `flex min-h-screen flex-col` on `bg-background` mirroring the ControlRoomPage shell, holding a `currentStep` state machine (NO react-router). First screen = `<SetupTokenGate/>`; on token accept, mint the `🔒 setup session` header chip and hold the token in state (threaded to every `setupApi` write). Render the `LifecycleStepper` full variant generalized to 6 nodes (Detect · Preset · Connect · Operator · Provider · Smoke) — only the current step narrates (quiet-idle); completed nodes are click-to-revisit (LifecycleStepper `<button aria-label="Go to {step}">` — the D-14 idempotency + a11y already solved). Body renders the step component for `currentStep`; each step's Continue advances `currentStep`; the Smoke step's Finalize runs `postFinalize`, then transitions to `<FinalizedLockout/>`. Below `sm`, swap the rail for the LifecycleStepper `strip` variant ("Step N of 6 · {name}"). A slim top setup-state banner (MaintenanceBanner shape, neutral/primary tint, `role="status"`) "First-run setup — configure this workspace to get started." Carry the full idle→loading→success→error set per UI-SPEC Interaction States. No secret is rendered in full; no `dangerouslySetInnerHTML`.
  </action>
  <acceptance_criteria>
    - `grep -c "LifecycleStepper" frontend/src/pages/SetupWizard.tsx` >= 1 ; `grep -c "SetupTokenGate\|SmokeChecklist\|FinalizedLockout" frontend/src/pages/SetupWizard.tsx` >= 3 (composes the leaf components)
    - `grep -c "react-router\|BrowserRouter\|useNavigate" frontend/src/pages/SetupWizard.tsx` == 0 (no router)
    - `grep -c "dangerouslySetInnerHTML" frontend/src/pages/SetupWizard.tsx` == 0
    - `cd frontend && npx tsc -b` → 0 net-new errors
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: App.tsx pre-auth branch + runtime hydrate + green SetupWizard.test.tsx</name>
  <files>frontend/src/App.tsx, frontend/src/pages/__tests__/SetupWizard.test.tsx</files>
  <behavior>
    - App renders `<SetupWizard/>` when `getSetupStatus()` returns `needs_setup:true`
    - a `window.location.pathname === "/setup"` visit forces the wizard even when `needs_setup:false`... and when finalized, `/setup` renders `<FinalizedLockout/>` (SC#2), never a config field
    - the configured, non-/setup path is unchanged: `if (!user) → AuthPage`, else `ChatLayout` (byte-identical)
    - `hydrateSupabaseFromRuntime` is called once at bootstrap before `useAuth` matters
  </behavior>
  <read_first>
    - frontend/src/App.tsx:1-75 (imports + the MaintenanceBanner probe idiom) + :150-175 (the loading spinner + `!user` AuthPage branch to insert BEFORE) — read this pass
    - lib/api.ts getSetupStatus + lib/supabase.ts hydrateSupabaseFromRuntime (158-08) ; frontend/src/pages/__tests__/SetupWizard.test.tsx (the 158-01 todos to realize)
    - 158-UI-SPEC.md §0 (mount) + §9 (lock-out)
  </read_first>
  <action>
    In `App.tsx`, add a `setupStatus` state + a one-shot startup `useEffect` that awaits `getSetupStatus()` (resolves safe on failure) AND calls `hydrateSupabaseFromRuntime(API_BASE)` before `useAuth` matters (so login uses the wizard-entered Supabase). Compute `atSetupPath = window.location.pathname === "/setup"`. Insert a branch BEFORE the `if (!user)` branch (App.tsx:161): while the setup probe is loading, keep the existing spinner; then `if (setupStatus.needs_setup || (atSetupPath && !setupStatus.finalized)) return <SetupWizard/>`; `if (atSetupPath && setupStatus.finalized) return <FinalizedLockout onGoToApp={...}/>` (the SC#2 lock-out on a literal /setup visit of a configured box). Leave the `if (!user) → AuthPage` and the `ChatLayout` render path BYTE-IDENTICAL for the normal configured, non-/setup case (a configured box that never visits /setup renders exactly as today). Do NOT add react-router.
    Realize the SetupWizard.test.tsx todos as real tests (mock getSetupStatus + render App): needs_setup:true → `<SetupWizard/>` shown; pathname `/setup` + finalized:true → the "Setup is already complete" lock-out (no config input); needs_setup:false + non-/setup → AuthPage/ChatLayout path unchanged.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/pages/__tests__/SetupWizard.test.tsx` → all pass (no todos remain)
    - `grep -c "getSetupStatus\|hydrateSupabaseFromRuntime" frontend/src/App.tsx` >= 2 ; `grep -c "window.location.pathname" frontend/src/App.tsx` >= 1
    - `grep -c "react-router\|BrowserRouter" frontend/src/App.tsx` == 0
    - `cd frontend && npx tsc -b && npx vite build` → 0 net-new errors, build exits 0
    - a test proves the configured non-/setup render path is unchanged (AuthPage when !user)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| startup probe → wizard render | The frontend decides wizard-vs-app from the public status probe; the backend gate is the real wall |
| /setup path → lock-out | A configured box's /setup visit must render the lock-out, never a config surface |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-158-11 | Tampering / Elevation | client-side branch | mitigate | The pre-auth branch is UX only — the SetupMiddleware gate + require_setup_token (158-03/06) are the security wall; a forged client state that renders the wizard cannot write config without the token, and post-finalize the server 409s every write. `/setup` on a configured box renders the no-re-entry lock-out (SC#2). |
| T-158-05 | DoS | needs_setup honesty | mitigate | `getSetupStatus` resolves safe (`needs_setup:false`) on any failure, so a transient backend blip never bounces a configured box's users into the wizard (the server's static marker+placeholder check is the source of truth). |
| T-158-SC | Tampering | npm installs | accept | Zero new packages — no react-router (pathname check), no cmdk. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/pages/__tests__/SetupWizard.test.tsx` → green
- `cd frontend && npx tsc -b && npx vite build` → 0 net-new errors, build exits 0
- `grep -c "react-router\|BrowserRouter" frontend/src/App.tsx frontend/src/pages/SetupWizard.tsx` == 0 (no router added)
</verification>

<success_criteria>
The SetupWizard 6-step host composes the 9 leaf components with a click-to-revisit LifecycleStepper rail; App.tsx renders it on `needs_setup` or a `/setup` path, renders the lock-out for a finalized `/setup` visit, hydrates runtime Supabase creds, and leaves the configured non-/setup path byte-identical; SetupWizard.test.tsx is green; build clean.
</success_criteria>

<output>
Create `.planning/phases/158-first-run-install-wizard-stretch/158-11-SUMMARY.md` when done.
</output>
