---
phase: 158-first-run-install-wizard-stretch
plan: 09
type: execute
wave: 5
depends_on: ["158-08"]
files_modified:
  - frontend/src/components/setup/SetupTokenGate.tsx
  - frontend/src/components/setup/EnvironmentDetectCard.tsx
  - frontend/src/components/setup/PresetPickerStep.tsx
  - frontend/src/components/setup/ConnectionBindStep.tsx
  - frontend/src/components/setup/SchemaGuidancePanel.tsx
autonomous: true
requirements: [DEPLOY-02]

must_haves:
  truths:
    - "D-08: `EnvironmentDetectCard` is a read-only, orienting grid of neutral HealthSignals-style tiles (in-Docker / config-found / DB / Redis) — light, no inputs, unknown = neutral em-dash (never red)"
    - "D-09: `PresetPickerStep` is a real `role=radiogroup` with one-box as the prominent Recommended card selected by default; Managed/On-prem are secondary pre-fill cards that link OPERATOR.md variants (NOT a multi-preset engine)"
    - "D-10: `ConnectionBindStep` collects Supabase (URL + 4 keys + POSTGRES_DSN) + Redis, masks every secret (ProviderPicker Eye/EyeOff), runs the per-group Test-connection probe, and Continue is gated until both groups test green (schema sub-check included)"
    - "D-10: `SchemaGuidancePanel` renders the schema-absent AMBER guidance (not an error) — the exact OPERATOR.md Step-3 copy-to-clipboard SQL sequence (full-schema + 9 seeds + the ('global') row)"
    - "The setup-token is entered once, masked to last-4, and never echoed in full (SetupTokenGate)"
    - "Never colour-alone (dot + word); env-var names are secondary mono hints; the POSTGRES_DSN session-pooler :5432 hint (OPERATOR.md A4) is shown"
  artifacts:
    - path: "frontend/src/components/setup/ConnectionBindStep.tsx"
      provides: "Supabase+Redis masked forms + per-group Test-connection + schema sub-check gate"
      min_lines: 80
      contains: "EyeOff"
    - path: "frontend/src/components/setup/PresetPickerStep.tsx"
      provides: "one-box-default radiogroup preset picker"
      contains: "radiogroup"
    - path: "frontend/src/components/setup/SchemaGuidancePanel.tsx"
      provides: "amber copy-to-clipboard OPERATOR.md Step-3 SQL guide"
      contains: "clipboard"
  key_links:
    - from: "frontend/src/components/setup/ConnectionBindStep.tsx"
      to: "setupApi.postValidate"
      via: "server-side throwaway probe of submitted values"
      pattern: "postValidate|validate"
    - from: "frontend/src/components/setup/SetupTokenGate.tsx"
      to: "setupApi (X-Setup-Token session)"
      via: "token accepted once, masked last-4"
      pattern: "token"
---

<objective>
Build the first five wizard step components — all ~90% composition of shipped, operator-approved primitives (G-2 satisfied by reuse, D-17): the token gate, the light env-detect tiles, the one-box-default preset picker, the Supabase/Redis bind form with live per-group validation, and the schema-missing copy-guide.

Purpose: D-08, D-09, D-10 surfaces. These are leaf components consumed by the SetupWizard host (158-11).
Output: 5 new components under `frontend/src/components/setup/`.

**Load `Skill("sketch-findings-agentic-rag")` before building — the shipped Deep Midnight primitives ARE the acceptance bar (G-2 override, D-17).**
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/158-first-run-install-wizard-stretch/158-UI-SPEC.md
@.planning/phases/158-first-run-install-wizard-stretch/158-PATTERNS.md
@.planning/phases/158-first-run-install-wizard-stretch/158-08-PLAN.md
@docs/OPERATOR.md

<interfaces>
Reuse primitives (compose, do NOT re-build) — PATTERNS.md §reused UI primitives + UI-SPEC.md Component Inventory:
  components/admin/HealthSignals.tsx (dot+label tiles; DEP_DOT/DEP_STATUS_LABEL vocabulary; off/unknown NEUTRAL never red)
  components/settings/ProviderPicker.tsx:273-291 (masked Eye/EyeOff input) + :187-195 (preset <select>)
  pages/AuthPage.tsx:15-43 (centered-card shell) + components/auth/SignInForm.tsx (field/error markup)
  lib/setupApi.ts (158-08): postValidate, postSchemaBootstrap, postDetect
Copywriting + per-surface contract — UI-SPEC.md §1 (token gate), §2 (env-detect), §3 (preset), §4 (bind + schema guidance)
OPERATOR.md Step-3 = the exact SQL sequence SchemaGuidancePanel copies (full-schema.sql + the 9 ordered seed migrations by filename + INSERT INTO app_settings (id) VALUES ('global'))
Icon convention: lucide for glyphs; @lobehub via @/lib/providerLogo for any provider logo (Icon Convention §1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SetupTokenGate + EnvironmentDetectCard</name>
  <files>frontend/src/components/setup/SetupTokenGate.tsx, frontend/src/components/setup/EnvironmentDetectCard.tsx</files>
  <read_first>
    - pages/AuthPage.tsx:15-43 + components/auth/SignInForm.tsx (centered card + field/error) — SetupTokenGate mirrors this
    - components/admin/HealthSignals.tsx:117-133 (DEP_DOT/label vocab) + :253-274 (tile markup) — EnvironmentDetectCard clones the read-only tiles
    - 158-UI-SPEC.md §1 + §2 (copy, states, a11y) ; Skill("sketch-findings-agentic-rag")
  </read_first>
  <action>
    `SetupTokenGate.tsx`: a centered card (AuthPage shell) with a single `Label` "Setup token" + shadcn `Input` (autofocus) + a "Continue" button; the source command `docker compose logs backend` in a mono chip with a copy button; states idle / validating ("Checking…" spinner) / error (bad → destructive inline; rate-limited → amber inline + brief disable) / success (advance + mint the `🔒 setup session` chip). Accept the token, hold it in the wizard session (prop callback up to the host), and mask any echo to last-4 mono — NEVER render the full token. Copy verbatim from UI-SPEC §1 Copywriting.
    `EnvironmentDetectCard.tsx`: read-only orienting grid (HealthSignals tile markup) of "Running in Docker" / "Existing config found" / "Database reachable" / "Redis reachable" driven by `postDetect()` results; dots use the status trio, unknown/not-probed = neutral em-dash (never red); a single "Continue" CTA + the honest caption "This just orients the next steps — nothing is changed yet." No inputs.
  </action>
  <acceptance_criteria>
    - both files compile: `cd frontend && npx tsc -b` → 0 net-new errors
    - `grep -c "autoFocus\|autofocus" frontend/src/components/setup/SetupTokenGate.tsx` >= 1 ; the token is never rendered in full (no template that prints the whole token string — masked to last-4)
    - `grep -c "role=\"status\"\|role='status'\|em-dash\|—" frontend/src/components/setup/EnvironmentDetectCard.tsx` >= 1 (neutral unknown state)
    - `grep -c "dangerouslySetInnerHTML" frontend/src/components/setup/*.tsx` == 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: PresetPickerStep (one-box default radiogroup)</name>
  <files>frontend/src/components/setup/PresetPickerStep.tsx</files>
  <read_first>
    - components/admin/CapabilityGrid (card) + a shipped radiogroup for the `role="radiogroup"` arrow-selectable pattern (Phase 155 AA bar)
    - 158-UI-SPEC.md §3 (preset pick — one-box prominent/Recommended/default; Managed/On-prem secondary pre-fill + OPERATOR.md link)
  </read_first>
  <action>
    `PresetPickerStep.tsx`: a real `role="radiogroup"` (arrow-key selectable) of preset cards. One-box is the prominent, primary-tinted "Recommended" card, selected by DEFAULT (D-09). Managed and On-prem are visibly-secondary sibling cards, each captioned "Pre-fills defaults + opens the runbook" — selecting one shows an inline honesty banner linking the relevant OPERATOR.md variant section (they do NOT launch a multi-preset engine — SHOULD, D-18; one-box is the built happy path). Emit the chosen preset up to the host (drives the ConnectionBindStep pre-fill defaults). Never colour-alone; the selected card carries an explicit checked indicator + `aria-checked`.
  </action>
  <acceptance_criteria>
    - `grep -c "radiogroup" frontend/src/components/setup/PresetPickerStep.tsx` >= 1 ; `grep -c "aria-checked\|role=\"radio\"" frontend/src/components/setup/PresetPickerStep.tsx` >= 1
    - one-box is the default-selected option (a test-visible default state; grep for a `Recommended`/default marker)
    - `cd frontend && npx tsc -b` → 0 net-new errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: ConnectionBindStep + SchemaGuidancePanel (live validate + schema guide)</name>
  <files>frontend/src/components/setup/ConnectionBindStep.tsx, frontend/src/components/setup/SchemaGuidancePanel.tsx</files>
  <read_first>
    - components/settings/ProviderPicker.tsx:273-291 (masked Eye/EyeOff input — the EXACT pattern for the 4 keys + POSTGRES_DSN + REDIS_URL)
    - components/admin/HealthSignals.tsx dot vocabulary (per-group test result) ; lib/setupApi.ts postValidate/postSchemaBootstrap
    - 158-UI-SPEC.md §4 (two grouped forms, DSN A4 hint, Test-connection states, schema sub-state) ; docs/OPERATOR.md Step-3 (the SQL sequence to copy)
  </read_first>
  <action>
    `ConnectionBindStep.tsx`: two grouped forms in one scroll — Supabase (`SUPABASE_URL` plain + the 4 keys `SUPABASE_ANON_KEY`/`SUPABASE_SERVICE_ROLE_KEY`/`SUPABASE_PUBLISHABLE_KEY`/`SUPABASE_SECRET_KEY` masked + `POSTGRES_DSN` masked) and Redis (`REDIS_URL` masked). Each field: a plain `Label` + shadcn `Input`; the env-var name as a secondary mono hint under the label. Masked fields use the ProviderPicker `Eye`/`EyeOff` toggle (aria-label "Show/Hide value"). Add the always-visible `ⓘ` DSN hint from OPERATOR.md A4 ("Use the Session pooler on `:5432` — not the direct `db.<ref>` host, not `:6543`", mono examples). A per-group "Test connection" secondary button calls `postValidate(...)` server-side and renders the HealthSignals dot result (idle / testing spinner / green "Connected" / red "Couldn't connect — {verbatim sanitized reason}"). "Continue" is disabled until BOTH groups test green, schema sub-check included. If Supabase connects but the sentinel table is absent, render `<SchemaGuidancePanel/>`.
    `SchemaGuidancePanel.tsx`: an AMBER notice (guidance, not error — the LifecycleStepper amber tone reference) containing the exact OPERATOR.md Step-3 sequence (full-schema.sql + the 9 ordered seed migrations by filename + `INSERT INTO app_settings (id) VALUES ('global')`) in a mono code block with a copy-to-clipboard button (`navigator.clipboard`, aria-label). Body: "Your database is reachable but empty. Paste this setup SQL into the Supabase SQL editor, run it, then Test again." If the SHOULD server-side "run it for me" is wired (158-06 schema-bootstrap), also render a single "Set up the database for me" primary button that calls `postSchemaBootstrap` and streams progress, falling back to the copy-guide on the `{fallback:"guide"}` privilege response — the copy-guide is the MUST.
  </action>
  <acceptance_criteria>
    - `grep -c "EyeOff\|EyeOff\|type=\"password\"" frontend/src/components/setup/ConnectionBindStep.tsx` >= 1 (secrets masked)
    - `grep -c ":5432\|Session pooler" frontend/src/components/setup/ConnectionBindStep.tsx` >= 1 (the A4 DSN hint)
    - `grep -c "clipboard" frontend/src/components/setup/SchemaGuidancePanel.tsx` >= 1 ; the panel tone is amber (not destructive) — schema-missing is guidance
    - `cd frontend && npx tsc -b && npx vite build` → 0 net-new errors, build exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| operator → masked secret inputs | The 4 keys + DSN + Redis URL are secrets typed into the browser; never echoed in full |
| browser → /setup/validate | Submitted values leave the browser only to the token-gated backend probe |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-158-10 | Information Disclosure | masked secret UI | mitigate | The setup token (last-4 only) + the 4 Supabase keys + POSTGRES_DSN + REDIS_URL use the ProviderPicker masked-input (`type=password` + Eye/EyeOff); no field renders a secret in full by default; `dangerouslySetInnerHTML` == 0 (no XSS sink for the copied SQL). |
| T-158-03 | Information Disclosure | test-connection result | mitigate | The failure message renders the backend's SANITIZED reason (`type(exc).__name__`), never a raw host/DSN — the backend already stripped it (158-05). |
| T-158-SC | Tampering | npm installs | accept | Zero new packages — lucide + @lobehub/icons (via providerLogo) + shadcn already vendored; no `react-router`. |
</threat_model>

<verification>
- `cd frontend && npx tsc -b && npx vite build` → 0 net-new errors, build exits 0
- `grep -c "dangerouslySetInnerHTML" frontend/src/components/setup/*.tsx` == 0
- `grep -c "radiogroup" frontend/src/components/setup/PresetPickerStep.tsx` >= 1 ; `grep -c "clipboard" frontend/src/components/setup/SchemaGuidancePanel.tsx` >= 1
</verification>

<success_criteria>
Five leaf components exist composing the shipped primitives: token gate (masked last-4), light env-detect tiles, one-box-default radiogroup preset picker, the masked Supabase/Redis bind form with per-group live validation gated to green, and the amber schema-missing copy-guide; build clean; never colour-alone; no XSS sink.
</success_criteria>

<output>
Create `.planning/phases/158-first-run-install-wizard-stretch/158-09-SUMMARY.md` when done.
</output>
