---
phase: 158-first-run-install-wizard-stretch
plan: 10
type: execute
wave: 5
depends_on: ["158-08"]
files_modified:
  - frontend/src/components/setup/OperatorBootstrapStep.tsx
  - frontend/src/components/setup/ProviderKeyStep.tsx
  - frontend/src/components/setup/SmokeChecklist.tsx
  - frontend/src/components/setup/FinalizedLockout.tsx
autonomous: true
requirements: [DEPLOY-02]

must_haves:
  truths:
    - "D-11: `OperatorBootstrapStep` collects email + password + confirm ('This is your admin login'), posts to create the operator, and Continue is gated on valid email + matching passwords + minimum length; confirm-mismatch = inline destructive, weak = amber advisory (never a hard block beyond minimum)"
    - "D-12: `ProviderKeyStep` REUSES `ProviderPicker` for the default-LLM select + always-on 🔒 endpoint footer + @lobehub logos, requires exactly one masked provider key (OpenAI default), others optional (neutral 'not configured', never red)"
    - "D-13: `SmokeChecklist` REUSES HealthSignals rows + the PublishGauntlet gated verdict — 5 server-truth rows; all-green unlocks the single `Finalize setup` CTA, any red blocks it with a plain-language fix + a 'Back to fix' jump; a not-yet-run row is neutral (never optimistically green)"
    - "`FinalizedLockout` (SC#2) is a centered success card shown after finalize AND on any later /setup visit — no config fields, no re-entry, a single 'Go to the app' CTA + the D-07 login-fallback note"
  artifacts:
    - path: "frontend/src/components/setup/SmokeChecklist.tsx"
      provides: "5-row server-truth checklist + all-green Finalize gate"
      min_lines: 60
      contains: "Finalize"
    - path: "frontend/src/components/setup/FinalizedLockout.tsx"
      provides: "already-configured lock-out (SC#2)"
      contains: "already"
  key_links:
    - from: "frontend/src/components/setup/SmokeChecklist.tsx"
      to: "setupApi.postSmoke + postFinalize"
      via: "all-green gates Finalize; server truth only"
      pattern: "postSmoke|postFinalize"
    - from: "frontend/src/components/setup/OperatorBootstrapStep.tsx"
      to: "setupApi.postOperator"
      via: "creates the first admin account"
      pattern: "postOperator|operator"
---

<objective>
Build the last four wizard step components: the operator bootstrap form, the reuse-`ProviderPicker` provider-key step, the smoke-checklist that IS the finalize gate, and the post-finalize lock-out.

Purpose: D-11, D-12, D-13, D-14 (lock-out surface). Leaf components consumed by the SetupWizard host (158-11).
Output: 4 new components under `frontend/src/components/setup/`.

**Load `Skill("sketch-findings-agentic-rag")` before building — the shipped primitives ARE the acceptance bar (G-2 override, D-17).**
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/158-first-run-install-wizard-stretch/158-UI-SPEC.md
@.planning/phases/158-first-run-install-wizard-stretch/158-PATTERNS.md
@.planning/phases/158-first-run-install-wizard-stretch/158-08-PLAN.md

<interfaces>
Reuse primitives (compose, do NOT re-build) — PATTERNS.md §reused UI primitives + UI-SPEC.md Component Inventory:
  components/settings/ProviderPicker.tsx (WHOLE — ProviderKeyStep reuses it) + @/lib/providerLogo (@lobehub)
  components/admin/HealthSignals.tsx rows (SmokeChecklist) + workflows/PublishGauntlet.tsx:453/:548 (canPublish disabled-until gate) + :380-404 (PublishingNotice elapsed clock, reduced-motion-gated) + the success card
  pages/AuthPage.tsx sign-up form (OperatorBootstrapStep email/password/confirm) + the eval RunBar thin determinate bar (password-strength hint)
  lib/setupApi.ts (158-08): postOperator, postProviderKey, postSmoke, postFinalize
Copywriting + per-surface contract — UI-SPEC.md §5 (operator), §6 (provider key), §7 (smoke = finalize gate), §8 (finalize in-flight), §9 (lock-out)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OperatorBootstrapStep + ProviderKeyStep</name>
  <files>frontend/src/components/setup/OperatorBootstrapStep.tsx, frontend/src/components/setup/ProviderKeyStep.tsx</files>
  <read_first>
    - pages/AuthPage.tsx sign-up form + components/auth/SignUpForm.tsx (email/password/confirm + inline error) ; the eval RunBar thin determinate bar (strength hint)
    - components/settings/ProviderPicker.tsx (the WHOLE component to reuse) + @/lib/providerLogo ; lib/setupApi.ts postOperator/postProviderKey
    - 158-UI-SPEC.md §5 + §6 (copy, gating, states)
  </read_first>
  <action>
    `OperatorBootstrapStep.tsx`: an email + password + confirm-password form (AuthPage sign-up markup), plain caption "This is your admin login — you'll use it to sign in and open the Admin area." A slim determinate password-strength bar + word (RunBar analog; length + character-class heuristic, client-side advisory only). Confirm-mismatch → inline destructive; weak → amber advisory (never a hard block beyond a minimum). On submit → `postOperator({email,password})`; a GoTrue 400 renders the server message verbatim; a duplicate → "Admin account created — you can log in as operator right after setup." Continue disabled until email valid + passwords match + minimum met.
    `ProviderKeyStep.tsx`: REUSE `<ProviderPicker>` whole for the default-LLM select (preset `<select>` + the always-on 🔒 endpoint footer + @lobehub logos via providerLogo). Exactly one required masked key (OpenAI default, Eye/EyeOff `***`); other providers are optional/skippable rows rendering neutral "not configured" when empty (never red). Inline note "We'll verify this key in the next step." On Continue → `postProviderKey({provider, api_key, embedding_key?})`. Continue disabled until the chosen provider has a non-empty key.
  </action>
  <acceptance_criteria>
    - `grep -c "ProviderPicker" frontend/src/components/setup/ProviderKeyStep.tsx` >= 1 (reused, not re-built) ; `grep -c "providerLogo" frontend/src/components/setup/ProviderKeyStep.tsx` >= 1
    - `grep -c "confirm\|password" frontend/src/components/setup/OperatorBootstrapStep.tsx` >= 2 ; strength hint present (RunBar-style bar)
    - `cd frontend && npx tsc -b` → 0 net-new errors
    - `grep -c "dangerouslySetInnerHTML" frontend/src/components/setup/OperatorBootstrapStep.tsx frontend/src/components/setup/ProviderKeyStep.tsx` == 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: SmokeChecklist (the finalize gate) + FinalizedLockout</name>
  <files>frontend/src/components/setup/SmokeChecklist.tsx, frontend/src/components/setup/FinalizedLockout.tsx</files>
  <read_first>
    - components/admin/HealthSignals.tsx rows + workflows/PublishGauntlet.tsx:453/:548 (canPublish disabled-until gate) + :380-404 (PublishingNotice elapsed clock) + the publish-success card
    - lib/setupApi.ts postSmoke/postFinalize ; 158-UI-SPEC.md §7 + §8 + §9 (server-truth rows, all-green gate, Back-to-fix, irreversible-lock confirm, lock-out)
  </read_first>
  <action>
    `SmokeChecklist.tsx`: five HealthSignals-style rows (Supabase auth reachable · Postgres + schema present · Redis PING · Provider key works · Operator account exists), each dot + plain label + sub + verbatim reason on fail; a not-yet-run row is NEUTRAL (never optimistically green). A primary "Run checks" triggers `postSmoke(cfg)`; while running, a live elapsed clock + per-row "checking…" (PublishingNotice, reduced-motion-gated). Each row resolves from SERVER TRUTH. All-green unlocks the single `Finalize setup` primary CTA (mirror PublishGauntlet's `canPublish` disabled-until — the ONLY gate); any red blocks it with the row's plain-language fix + a "Back to fix" jump to the owning step (idempotent re-entry, D-14). Finalize → the irreversible-lock confirmation (UI-SPEC §7 Copywriting: "This locks first-run setup…"), then `postFinalize(cfg)`; render the §8 in-flight "Saving your configuration…" panel; on success emit "finalized" up to the host.
    `FinalizedLockout.tsx`: a centered success card (PublishGauntlet publish-success tone) "Setup is already complete." with NO config fields and NO re-entry (re-config is an Admin action, D-14); a single primary CTA "Go to the app" (routes to the normal auth/app surface); include the D-07 login-fallback note ("If login doesn't work right away, your server may need a quick restart… `docker compose restart`") as a quiet secondary line, not an alarm. Shown after finalize AND on any later `/setup` visit (SC#2).
  </action>
  <acceptance_criteria>
    - `grep -c "Finalize" frontend/src/components/setup/SmokeChecklist.tsx` >= 1 ; the Finalize button is disabled unless all-green (grep for a `disabled`/`canFinalize` gate)
    - `grep -c "Back to fix\|owning step\|onBackToFix" frontend/src/components/setup/SmokeChecklist.tsx` >= 1
    - `grep -ci "already\|Go to the app" frontend/src/components/setup/FinalizedLockout.tsx` >= 1 ; no config `<input>` in FinalizedLockout (grep `input` == 0)
    - `cd frontend && npx tsc -b && npx vite build` → 0 net-new errors, build exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| operator → password/provider-key inputs | Secrets typed into the browser; masked; the finalize is the irreversible lock |
| client all-green claim → finalize | The client never authorizes finalize — the backend re-runs smoke server-side (158-06) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-158-10 | Information Disclosure | provider key / password inputs | mitigate | Provider key + password use masked inputs; no field renders a secret in full; `dangerouslySetInnerHTML` == 0. |
| T-158-02 | Tampering | client-forced finalize | mitigate | `Finalize setup` is UI-gated on all-green, but the SERVER re-runs smoke and refuses a non-green finalize (158-06) — the client gate is UX, not the wall; post-finalize the lock-out is the only surface (D-14). |
| T-158-06 | Spoofing | weak operator password | mitigate | The step surfaces the GoTrue password-policy error verbatim (advisory + server-enforced), never a client-only bypass. |
| T-158-SC | Tampering | npm installs | accept | Zero new packages — ProviderPicker / HealthSignals / PublishGauntlet / RunBar all shipped. |
</threat_model>

<verification>
- `cd frontend && npx tsc -b && npx vite build` → 0 net-new errors, build exits 0
- `grep -c "dangerouslySetInnerHTML" frontend/src/components/setup/*.tsx` == 0
- SmokeChecklist Finalize disabled-until-all-green; FinalizedLockout has no config input
</verification>

<success_criteria>
The operator form (gated, plain "admin login"), the ProviderPicker-reusing provider-key step (one required masked key), the server-truth 5-row smoke checklist that gates the single Finalize CTA with Back-to-fix, and the no-re-entry FinalizedLockout all exist; build clean; never colour-alone; no XSS sink.
</success_criteria>

<output>
Create `.planning/phases/158-first-run-install-wizard-stretch/158-10-SUMMARY.md` when done.
</output>
