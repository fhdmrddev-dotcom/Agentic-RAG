---
phase: 147-operator-control-plane
plan: 07
type: execute
wave: 2
depends_on: [147-06]
files_modified:
  - frontend/src/components/admin/HealthSignals.tsx
  - frontend/src/components/admin/ActiveRunsSection.tsx
  - frontend/src/components/admin/__tests__/ActiveRunsSection.test.tsx
autonomous: true
requirements: [ADMIN-02]
must_haves:
  truths:
    - "HealthSignals shows dependency-health dots (Redis/Database/Code sandbox) as up/slow/down, and off-by-config renders neutral not red (Pitfall 6) — alongside the existing 4 backpressure signals"
    - "ActiveRunsSection renders every run as a 064-B card with its real @lobehub provider mark, user, model, live-ticking elapsed (client math from started_at, D-07), and a kind badge (chat/workflow/eval/tuner, D-01)"
    - "Kill opens a confirm sheet that NAMES the victim (who / model / elapsed) before firing (064-B); the killed card does NOT vanish — it goes Cancelling… → Cancelled (never optimistic removal)"
    - "Tuner/eval rows render with NO Kill affordance and honest 'ends on its own' copy (D-01); a zombie-heal reads 'recovered a stuck run' not 'killed' (064-B)"
    - "long-running (>8min elapsed, client math) and not-responding (the server-derived ActiveRun.not_responding boolean from plan 02) tags render on the right rows; the empty state 'No runs in flight' is calm, not broken"
  artifacts:
    - path: "frontend/src/components/admin/ActiveRunsSection.tsx"
      provides: "064-B run cards + confirm-sheet Kill + honest cancel states"
      min_lines: 60
    - path: "frontend/src/components/admin/HealthSignals.tsx"
      provides: "dependency-health dots (up/slow/down/off)"
      contains: "dependencies"
  key_links:
    - from: "frontend/src/components/admin/ActiveRunsSection.tsx"
      to: "lib/api killRun"
      via: "confirm-sheet Kill → killRun(runId) → Cancelling…→Cancelled"
      pattern: "killRun"
---

<objective>
Build the ADMIN-02 monitoring surfaces per the locked 064-B sketch: extend
`HealthSignals` with dependency-health dots (Redis/Database/Code sandbox, up/slow/down,
off-by-config neutral) and create `ActiveRunsSection` — calm cross-provider run cards with
real `@lobehub` marks, client-side live-ticking elapsed (D-07), kind badges (D-01), a
victim-naming confirm sheet for Kill, and the honest Cancelling… → Cancelled two-state
(never optimistic removal). Presentational leaves: the shell owns fetch/poll and threads
data down.

Purpose: The operator can tell a healthy run from a runaway in under 3 seconds, and Kill
feels deliberate + honest.
Output: extended `HealthSignals` + new `ActiveRunsSection` + tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/147-operator-control-plane/147-CONTEXT.md
@.planning/phases/147-operator-control-plane/147-PATTERNS.md
@.planning/sketches/064-active-runs-and-kill/README.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md

<interfaces>
<!-- The client contract (from plan 06) + the presentational-leaf discipline. -->

From lib/api.ts (plan 06): `ActiveRun` { run_id, kind:"chat"|"workflow"|"eval"|"tuner", thread_id, user_id, user_email, model, provider, started_at (unix epoch), killable, not_responding (server-derived stalled-stream boolean) }; `killRun(runId): Promise<void>`; `BackpressureSignals.dependencies?: { redis, supabase, sandbox: {state:"off"|"up"|"down", latency_ms} }`.

Presentational-leaf discipline (VERIFIED HealthSignals.tsx:14-16, 64-98; OperatorBand.tsx:11-15): props in, DOM out. null data → calm dimmed placeholder, never a crash. The shell (ControlRoomPage, plan 09) owns fetch/poll/state and passes `signals` / `runs` / callbacks down. The status-dot idiom already exists (HealthSignals.tsx:82: rounded-full bg-success span). Keep the `showTechnical` raw-field reveal.

Provider marks: `@lobehub/icons` (RDD 43 / sketch 048 — the single source; NEVER placeholder art). Resolve provider → mark from the existing 048 provider-mark map used elsewhere in the app.

064-B contract (LOCKED): mixed cross-provider cards; elapsed live-ticks (client math now−started_at); long-running tag at >8min; not-responding tag (stalled) highlighted; Kill → confirm sheet naming victim ("End maria's run on GPT-5, 2m 14s in — cancels immediately, recorded with your name") → Cancelling… → Cancelled·recorded; a stuck run reads "recovering a stuck run" → "Cancelled · recovered a stuck run". Tuner/eval = no Kill, honest "ends on its own" copy. Empty = calm "No runs in flight".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend HealthSignals with dependency-health dots (up/slow/down/off)</name>
  <files>frontend/src/components/admin/HealthSignals.tsx</files>
  <read_first>
    - frontend/src/components/admin/HealthSignals.tsx (the whole file — the presentational leaf, the status-dot at ~82, the SIGNAL_ORDER map, the showTechnical reveal at 89-93)
    - frontend/src/lib/api.ts (the BackpressureSignals.dependencies shape from plan 06)
    - .planning/phases/147-operator-control-plane/147-PATTERNS.md (section "HealthSignals.tsx — EXTEND")
  </read_first>
  <action>
Add a dependency-health row/group to `HealthSignals` rendering three probes from `signals.dependencies`: Redis, Database (supabase), Code sandbox. Map state → dot color using the existing status-dot idiom: up → success (green), slow (if a latency threshold is exceeded — treat a high latency_ms as slow/amber) → warning (amber), down → destructive (red), and for the sandbox specifically `state === "off"` → a NEUTRAL/muted dot with an "off by config" label (Pitfall 6 — a deliberately-disabled sandbox is NEVER red). Keep the plain-language default labels (Redis / Database / Code sandbox) and preserve the `showTechnical` reveal for raw field names + latency_ms. When `signals` or `signals.dependencies` is null/undefined, render the existing calm dimmed placeholder (never crash). Do not change the existing 4 backpressure signal rows.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "HealthSignals" || echo "HealthSignals typechecks clean"</automated>
  </verify>
  <acceptance_criteria>
    - `HealthSignals.tsx` reads `signals.dependencies` and renders three named rows (Redis / Database / Code sandbox).
    - Sandbox `state==="off"` renders a neutral (non-destructive) dot + "off by config" label — grep the off branch does NOT use the destructive/red class.
    - A null `signals.dependencies` renders the existing placeholder (no thrown error) — the existing null-guard path is preserved.
    - `npx tsc --noEmit` reports no new HealthSignals error.
  </acceptance_criteria>
  <done>Dependency-health dots render honestly (up/slow/down/off) alongside the 4 backpressure signals.</done>
</task>

<task type="auto">
  <name>Task 2: ActiveRunsSection — 064-B cards + confirm-sheet Kill + honest Cancelling→Cancelled</name>
  <files>frontend/src/components/admin/ActiveRunsSection.tsx, frontend/src/components/admin/__tests__/ActiveRunsSection.test.tsx</files>
  <read_first>
    - frontend/src/components/admin/HealthSignals.tsx (the presentational-leaf shape to mirror: props in, DOM out, null→calm placeholder)
    - frontend/src/lib/api.ts (ActiveRun type + killRun from plan 06)
    - .planning/sketches/064-active-runs-and-kill/README.md (the full 064-B contract)
    - the existing 048 provider-mark map / @lobehub usage in the app (grep for the provider→icon resolver used by chat/run cards so the marks match the rest of the product)
    - .claude/skills/sketch-findings-agentic-rag/SKILL.md (cross-provider run-card grammar; RDD 43 icon convention)
  </read_first>
  <action>
Create `ActiveRunsSection.tsx` as a presentational leaf: props `{ runs: ActiveRun[] | null, onKill: (runId: string) => Promise<void> }`. Render each run as a calm 064-B card: the real `@lobehub` provider mark (resolve provider → mark via the existing app-wide 048 provider-mark map — NEVER placeholder art), the user (user_email or a masked fallback), the model, a live-ticking elapsed computed client-side from `now − started_at` (a local 1s tick via setInterval, cleared on unmount — D-07, no server poll for ticking), and a kind badge (chat/workflow/eval/tuner). Show a `long-running` tag when elapsed > 8 minutes (pure client math) and a `not-responding` tag (highlighted) when `run.not_responding` is true — the server-derived stalled-stream boolean from plan 02; do NOT guess stalls client-side. For `killable === false` runs (eval/tuner per D-01), render NO Kill button and honest "ends on its own" copy. For killable runs, Kill opens a confirm SHEET that NAMES the victim ("End {user}'s run on {model}, {elapsed} in — cancels immediately, recorded with your name") with Confirm / Cancel; on Confirm call `onKill(run_id)` and transition the card to Cancelling… then Cancelled·recorded — the card does NOT vanish (NO optimistic removal — Pitfall/anti-pattern). If the kill resolved via the zombie-heal path, show "recovered a stuck run" wording rather than "killed" (064-B honesty — derive from the kill result/state, not a guess). Empty (`runs` empty) → a calm "No runs in flight" state; null → dimmed placeholder. Create `__tests__/ActiveRunsSection.test.tsx`: (1) a killable card shows a Kill button, a tuner card does not; (2) clicking Kill shows the victim-naming confirm sheet, not an immediate cancel; (3) after confirm the card shows Cancelling…/Cancelled and is NOT removed; (4) elapsed renders from started_at; (5) empty state renders "No runs in flight"; (6) a run with not_responding:true renders the highlighted not-responding tag, false does not.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- ActiveRunsSection 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - A run with `killable:true` renders a Kill control; a run with `kind:"tuner"` (killable:false) renders NO Kill control and "ends on its own"-style copy (test assertions).
    - Clicking Kill renders a confirm sheet whose text includes the user, the model, and the elapsed (victim-naming assertion) — the run is NOT cancelled until Confirm.
    - After Confirm, the card remains in the list showing Cancelling…/Cancelled (assert the card is still present — no optimistic removal).
    - Elapsed is derived from `started_at` (mock `now` → assert the rendered elapsed) and the empty state renders "No runs in flight".
    - Provider marks come from the shared `@lobehub`/048 resolver (grep: imports the existing provider-mark map, not inline SVG placeholders).
    - A run with `not_responding:true` renders the highlighted not-responding tag; `false` renders no such tag (test assertions on the field, not on a client-side stall heuristic).
  </acceptance_criteria>
  <done>064-B active-run cards with victim-naming Kill and honest cancel states; presentational leaf; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser render → operator data | Presentation only; the security boundary is the server 404 gate, not this UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-147-12 | Elevation of Privilege | client-side Kill affordance | mitigate | The Kill button only calls `killRun` (server-gated 204/404); the client makes no trust decision. Non-operators never receive run data (server 404). |
| T-147-05 | Information Disclosure (privacy) | run cards | mitigate | Cards show metadata only (user/model/elapsed); no thread-content drill-in (sketch linkage rule #11 — read-only monitoring, not surveillance). |
| T-147-SC | Tampering (supply chain) | npm installs | accept | Zero new dependencies (@lobehub/icons already installed; RESEARCH audit N/A). |
</threat_model>

<verification>
- `cd frontend && npm run test -- ActiveRunsSection` green; `npx tsc --noEmit` no new HealthSignals/ActiveRunsSection errors.
</verification>

<success_criteria>
- Dependency-health dots honest (up/slow/down/off).
- 064-B cards: cross-provider marks, live elapsed, kind badges, victim-naming Kill, honest Cancelling→Cancelled (no optimistic removal), tuner/eval no-Kill, calm empty state.
</success_criteria>

<output>
Create `.planning/phases/147-operator-control-plane/147-07-SUMMARY.md` when done.
</output>
