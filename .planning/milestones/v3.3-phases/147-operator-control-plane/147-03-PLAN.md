---
phase: 147-operator-control-plane
plan: 03
type: execute
wave: 2
depends_on: [147-01, 147-02]
files_modified:
  - backend/app/api/runs.py
  - backend/app/services/run_lifecycle.py
  - backend/app/api/admin.py
  - backend/tests/test_147_operator_kill.py
  - backend/tests/test_062_cancel_run.py
autonomous: true
requirements: [ADMIN-02, FLAG-01]
user_setup: []

must_haves:
  truths:
    - "The cancel/zombie-heal internals (idempotent-terminal 204, PUBLISH-first ask_user sentinel, finalize_run_terminal co-write, SETNX cancel-lock, EXPIRE) are factored into ONE shared helper reused by both cancel_run and the operator Kill (D-02, refactor-to-share not copy-paste)"
    - "POST /admin/runs/{id}/kill cancels ANY user's chat/workflow run with NO ownership filter — the ONLY difference from cancel_run is the missing .eq(user_id) — and is 404-non-discoverable on a missing run (D-02)"
    - "The kill writes operator_audit_log naming the victim (Ended {victim}'s run on {model}); a zombie-heal sub-path reads 'recovered a stuck run' not 'killed' (064-B honesty); action run.kill"
    - "The victim sees exactly a self-cancel — no operator attribution reaches their chat (D-03); who/why lives only in operator_audit_log"
    - "cancel_run still 404s cross-user after the refactor (owner-path regression holds)"
    - "PUT /admin/flags writes a validated flag through save_app_settings (code-constant key allowlist) and records flag.<key> (FLAG-01)"
  artifacts:
    - path: "backend/app/services/run_lifecycle.py"
      provides: "shared _cancel_run_internals helper (Steps 2/3a/3b)"
      contains: "finalize_run_terminal"
    - path: "backend/app/api/admin.py"
      provides: "POST /admin/runs/{run_id}/kill + PUT /admin/flags"
      contains: "run.kill"
  key_links:
    - from: "backend/app/api/admin.py"
      to: "run_lifecycle._cancel_run_internals"
      via: "operator kill calls the shared helper without the ownership SELECT"
      pattern: "_cancel_run_internals|cancel_run_internals"
---

<objective>
Deliver the operator Kill (ADMIN-02) and the flag-write endpoint (FLAG-01) by
refactoring `cancel_run`'s zombie-heal internals into a shared helper and calling it
from a new operator-scoped `POST /admin/runs/{id}/kill` that skips ONLY the ownership
filter — the D-02 refactor-to-share, never copy-paste. The kill names the victim in the
audit ledger (064-B) while the victim sees exactly a self-cancel (D-03). Add
`PUT /admin/flags` on the code-constant key allowlist so the 065-A capability grid and
maintenance panel have a write seam.

Purpose: A single correct cancel discipline (no drift from the D-062 zombie-heal
subtleties) exposed to the operator behind the sole-authority router gate.
Output: refactored `runs.py`/`run_lifecycle.py` + operator kill + flag write + tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/147-operator-control-plane/147-CONTEXT.md
@.planning/phases/147-operator-control-plane/147-RESEARCH.md
@.planning/phases/147-operator-control-plane/147-PATTERNS.md
@.planning/phases/147-operator-control-plane/147-02-PLAN.md

<interfaces>
<!-- The cancel_run sub-paths to factor (VERIFIED runs.py:1097-1265). Reuse Steps 2/3a/3b verbatim. -->

- Step 1 (ownership SELECT — operator SKIPS the .eq user_id): `runs.py:1103-1119`.
    cancel_run: .select("run_id,status,thread_id").eq("run_id",id).eq("user_id",current_user["id"]).maybe_single() → 404 on others' runs.
    operator variant: .select("run_id,status,thread_id,user_id,model,provider").eq("run_id",id).maybe_single() (NO .eq user_id) → still 404 on missing (non-discoverable).
- Step 2 (idempotent-terminal 204): `runs.py:1121-1125` — status in {completed,failed,cancelled,timed_out} → 204, no UPDATE, no Redis touch.
- Step 3a (happy path): `runs.py:1133-1155` — PUBLISH ask_user cancel sentinel (publish_cancel_sentinel) BEFORE task.cancel() (D-085-04 ordering) → 204.
- Step 3b (zombie heal): `runs.py:1157-1265` — finalize_run_terminal(status="cancelled", error="cancelled_by_user") + workflow anchor clear + SETNX-gated synthetic zombie_healed sentinel + EXPIRE 60.
- finalize_run_terminal lives in `run_lifecycle.py` (already the terminal co-writer) — the recommended helper home.

Audit label (D-02 / 064-B): request.state.audit_label = f"Ended {victim}'s run on {model}"; audit_action = "run.kill".
  A zombie-heal (Step 3b) reached → label reads "Recovered a stuck run on {model}" (064-B: never "killed" for a heal).

PUT /admin/flags contract: body {key: FlagKey, value: bool}; key validated against the SAME code-constant set as _DIRECT_COLUMNS (reject others with 422 — T-147-01). save_app_settings({key: value}). audit_action = f"flag.{key}.{'on' if value else 'off'}" (maintenance_mode → "maintenance.set"). Floor-attached.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Factor cancel_run internals into a shared run_lifecycle helper (D-02) + owner-path regression</name>
  <files>backend/app/api/runs.py, backend/app/services/run_lifecycle.py, backend/tests/test_062_cancel_run.py</files>
  <read_first>
    - backend/app/api/runs.py (lines 1092-1265 — the whole cancel_run, all four sub-paths)
    - backend/app/services/run_lifecycle.py (the full file — finalize_run_terminal + register_run_start; where the new helper belongs)
    - backend/tests/test_062_cancel_run.py (the existing owner-path tests to extend)
    - .planning/phases/147-operator-control-plane/147-PATTERNS.md (section "runs.py + run_lifecycle.py — EXTRACT HELPER")
  </read_first>
  <behavior>
    - `_cancel_run_internals(run_id, thread_id, redis, pool, ...)` performs Steps 2/3a/3b exactly as cancel_run does today and reports which sub-path fired (terminal-noop / task-cancelled / zombie-healed) so callers can pick the audit verb.
    - cancel_run does Step 1 (ownership SELECT) then delegates to the helper — its owner-path behavior is byte-equivalent (still 204 idempotent, still PUBLISH-first, still zombie-heals) and STILL 404s cross-user.
    - The helper never performs an ownership check (that stays in the caller).
  </behavior>
  <action>
Extract Steps 2, 3a, 3b of `cancel_run` (`runs.py:1121-1265`) into `async def _cancel_run_internals(...)` in `run_lifecycle.py` (already the terminal co-writer — keeps the finalize/zombie-heal discipline in one home). Preserve verbatim: the idempotent-terminal 204 short-circuit, the PUBLISH-first `publish_cancel_sentinel` BEFORE `task.cancel()` ordering (D-085-04), the `finalize_run_terminal(status="cancelled", error="cancelled_by_user")` atomic co-write, the SETNX cancel-lock (runs.py:1225-1247), and the EXPIRE 60 (runs.py:1256-1261). Return an outcome discriminator (e.g. an enum/str: "terminal_noop" | "task_cancelled" | "zombie_healed") so callers can choose the audit verb. Rewrite `cancel_run` to keep Step 1 (the ownership SELECT with `.eq("user_id", ...)`) then call the shared helper — its external behavior is unchanged. Extend `tests/test_062_cancel_run.py` to prove the owner path STILL 404s cross-user, still returns 204 on an already-terminal run, and still zombie-heals — after the refactor (regression backstop for the shared discipline).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_062_cancel_run.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `run_lifecycle.py` defines `async def _cancel_run_internals` (or equivalently-named shared helper) that calls `finalize_run_terminal` for the zombie-heal path.
    - `cancel_run` in `runs.py` retains its `.eq("user_id", ...)` ownership SELECT and delegates the rest to the helper (grep: the `.eq("user_id"` still present in cancel_run; the finalize/EXPIRE logic is NO LONGER inline in cancel_run — moved to the helper).
    - `pytest tests/test_062_cancel_run.py -x` passes including a cross-user-404 assertion and an already-terminal-204 assertion.
  </acceptance_criteria>
  <done>The cancel/zombie-heal discipline lives in one shared helper; cancel_run is byte-equivalent for owners and still 404s cross-user.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /admin/runs/{id}/kill (operator, no ownership) + PUT /admin/flags</name>
  <files>backend/app/api/admin.py, backend/tests/test_147_operator_kill.py</files>
  <read_first>
    - backend/app/api/admin.py (current state after plan 02 — the router, floor pattern 44-60, the /admin/runs list + /record endpoint)
    - backend/app/services/run_lifecycle.py (the new _cancel_run_internals from Task 1)
    - backend/app/main.py (lines 97-121 — _DIRECT_COLUMNS, the code-constant flag key allowlist to validate against)
    - backend/app/models/user_settings.py (save_app_settings 250-288 + invalidate_settings_cache — the flag write path)
    - .planning/phases/147-operator-control-plane/147-RESEARCH.md (Pattern 3 operator kill sketch; Pitfall 3 free-text operator action)
  </read_first>
  <behavior>
    - POST /admin/runs/{run_id}/kill: operator-scoped SELECT of ANY user's run row (for victim naming) → 404 on missing → call the shared _cancel_run_internals → 204. For a run whose kind is eval/tuner (killable=false per D-01) refuse with 409/400 and a plain message (no Kill affordance for internal jobs).
    - The audit label names the victim + model; a zombie-heal outcome uses "Recovered a stuck run" not "Ended ...'s run" (064-B). action = "run.kill". Floor-attached.
    - NO operator attribution is written anywhere the victim can read (D-03 — only operator_audit_log).
    - PUT /admin/flags: validate `key` against the code-constant flag set; unknown key → 422. save_app_settings + invalidate cache. Floor-attached with action flag.<key>.<on|off> (maintenance_mode → maintenance.set). value must be bool.
  </behavior>
  <action>
Add `POST /admin/runs/{run_id}/kill` to the `admin.py` router (`run_id: UUID` path param — V5 typed input). Do the operator-scoped SELECT (`.select("run_id,status,thread_id,user_id,model,provider")` WITHOUT `.eq user_id`) — 404 on missing (non-discoverable). If the run is an internal job (eval/tuner — derive the same way plan 02's /admin/runs does, or reject when there is no owning user_id / when kind is not chat/workflow), return a plain 409 refusal (D-01: no Kill for bounded internal jobs). Otherwise call the shared `_cancel_run_internals`; from its outcome discriminator pick the audit label: "Ended {victim}'s run on {model}" for a task-cancel, "Recovered a stuck run on {model}" for a zombie-heal (064-B). Resolve {victim} from the run's user_email (enrichment lookup — same as plan 02). Set `request.state.audit_label` / `audit_action="run.kill"`, attach `_floor`. Return 204. Write NOTHING to the victim's thread/run beyond the normal cancel terminal state (D-03).

Add `PUT /admin/flags`: Pydantic body `{key: str, value: bool}`. Validate `key` is in the code-constant flag set (the same names added to `_DIRECT_COLUMNS`: web_search_enabled, sandbox_enabled, self_improve_enabled, workflows_enabled, maintenance_mode) — reject others with 422 (T-147-01: never let a client name an arbitrary column). Call `save_app_settings({key: value})` (which invalidates the cache). Floor-attached; audit_action = `f"flag.{key}.{'on' if value else 'off'}"`, EXCEPT `maintenance_mode` → `"maintenance.set"`; audit_label plain-language ("Turned ON maintenance mode" / "Turned OFF code sandbox for everyone").

Create `backend/tests/test_147_operator_kill.py`: (1) operator kills another user's chat run → 204 + a run.kill audit row naming the victim; (2) killing an already-terminal run → 204 idempotent, no double side-effect; (3) a zombie-heal path yields the "Recovered a stuck run" label; (4) kill on a tuner/eval run → 409 refusal; (5) non-operator → 404; (6) PUT /admin/flags with a bogus key → 422; valid key → save_app_settings called + a flag.* audit row; (7) the victim's run row shows the ordinary cancelled terminal state with NO operator field (D-03).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_147_operator_kill.py -x</automated>
  </verify>
  <acceptance_criteria>
    - The operator kill SELECT has NO `.eq("user_id"` (grep the kill handler — the missing ownership filter is the D-02 difference) and calls the shared `_cancel_run_internals`.
    - A kill of a chat run belonging to another user returns 204 and writes an `operator_audit_log` row with action `run.kill` whose label contains the victim + model (test assertion).
    - A zombie-heal outcome produces a label containing "Recovered a stuck run" (never "Ended"/"killed") — test assertion.
    - `PUT /admin/flags` with key not in the allowlist → 422; valid key → `save_app_settings` invoked (mock assertion) + a `flag.*`/`maintenance.set` audit row.
    - Non-operator → 404 on both endpoints.
    - `pytest tests/test_147_operator_kill.py -x` passes.
  </acceptance_criteria>
  <done>Operator Kill reuses the shared cancel discipline without ownership, names the victim in the ledger only, and the flag-write endpoint is on the code-constant allowlist; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → any user's run | Kill deliberately crosses ownership — legitimate ONLY behind the operator router gate. |
| operator action → victim's chat | The victim must never learn who/why from their own surface (privacy). |
| client → PUT /admin/flags key | A client-supplied column name must never reach SQL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-147-06 | Elevation of Privilege | operator kill skips ownership | mitigate | The ownership skip lives ONLY inside the `admin.py` `require_operator` router; a non-operator gets 404. The helper itself performs no privileged decision. |
| T-147-07 | Information Disclosure (privacy) | operator attribution in victim chat | mitigate | D-03: who/why is written ONLY to operator_audit_log; the victim's run shows the ordinary cancelled terminal state (test-asserted no operator field). |
| T-147-08 | Tampering / double-effect | double-cancel side effects | mitigate | Idempotent-terminal 204 + SETNX cancel-lock reused verbatim from the shared helper (no re-cancel side effects). |
| T-147-04 | Information Disclosure | run existence to non-operators | mitigate | 404-non-discoverable on missing run (operator SELECT with no user filter still 404s missing ids). |
| T-147-01 | Tampering | PUT /admin/flags key → SQL | mitigate | Key validated against the code-constant flag allowlist (422 otherwise); value is a bool; write goes through parameterized save_app_settings. |
| T-147-SC | Tampering (supply chain) | npm/pip installs | accept | Zero new dependencies (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/test_062_cancel_run.py tests/test_147_operator_kill.py -x` green.
- No inline finalize/EXPIRE logic remains in `cancel_run` (moved to the shared helper).
</verification>

<success_criteria>
- One shared cancel/zombie-heal helper; cancel_run byte-equivalent for owners + still 404s cross-user.
- Operator Kill cancels any user's chat/workflow run, names the victim in the ledger only (D-02/D-03/064-B honesty verb).
- PUT /admin/flags writes validated flags on the code-constant allowlist.
</success_criteria>

<output>
Create `.planning/phases/147-operator-control-plane/147-03-SUMMARY.md` when done.
</output>
