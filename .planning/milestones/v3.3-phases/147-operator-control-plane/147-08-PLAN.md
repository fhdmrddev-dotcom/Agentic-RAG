---
phase: 147-operator-control-plane
plan: 08
type: execute
wave: 2
depends_on: [147-06]
files_modified:
  - frontend/src/components/admin/CapabilityGrid.tsx
  - frontend/src/components/admin/MaintenancePanel.tsx
  - frontend/src/components/admin/__tests__/CapabilityGrid.test.tsx
autonomous: true
requirements: [FLAG-01]
must_haves:
  truths:
    - "CapabilityGrid renders the four kill-switches (web search / code sandbox / self-improve / workflows) as a 2×2 grid; OFF looks ARMED — red tint + 'off for everyone' tag + an impact line — not a neutral preference (065-A, D-04)"
    - "Impact copy is count-based only where derivable from ActiveRun.kind (workflows switch → count of active workflow runs); switches with no derivable count show the plain 'off for everyone' fallback — the sketch's exact per-tool counts ('2 runs using code…') are best-effort, never a claimed truth (no per-run tool-usage signal exists)"
    - "Capability switches flip DIRECTLY (no confirm) via setFlag — fast for an emergency (065-A graded friction)"
    - "MaintenancePanel is a spatially-separate amber Platform-state panel using arm-to-confirm (Put the whole platform in read-only mode? · Confirm / Cancel) before flipping maintenance (065-A, D-06)"
    - "Maintenance ON shows a persistent present-tense consequence banner distinct from the past-tense ledger receipt (consequence ≠ receipt, 062-A rule)"
  artifacts:
    - path: "frontend/src/components/admin/CapabilityGrid.tsx"
      provides: "2×2 armed-OFF capability switches + impact copy"
      min_lines: 40
    - path: "frontend/src/components/admin/MaintenancePanel.tsx"
      provides: "amber Platform-state panel + arm-to-confirm maintenance"
      min_lines: 30
  key_links:
    - from: "frontend/src/components/admin/CapabilityGrid.tsx"
      to: "lib/api setFlag"
      via: "direct-flip switch → setFlag(key, value)"
      pattern: "setFlag"
---

<objective>
Build the FLAG-01 controls per the locked 065-A sketch: a 2×2 `CapabilityGrid` where OFF
looks armed (red tint + "off for everyone" + concrete impact copy) and switches flip
directly for emergency speed, plus a spatially-separate amber `MaintenancePanel` that uses
arm-to-confirm before putting the whole platform read-only and shows a persistent
present-tense consequence banner distinct from the ledger receipt. Presentational leaves;
the shell owns the write callbacks.

Purpose: A kill-switch reads as consequential (affects real people right now), and the
operator can never confuse a reversible feature switch with the platform-wide state switch.
Output: `CapabilityGrid` + `MaintenancePanel` + tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/147-operator-control-plane/147-CONTEXT.md
@.planning/phases/147-operator-control-plane/147-PATTERNS.md
@.planning/sketches/065-system-controls-and-maintenance/README.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md

<interfaces>
<!-- The client contract (plan 06) + the 065-A weight/distinction rules. -->

From lib/api.ts (plan 06): `setFlag(key: FlagKey, value: boolean): Promise<void>`; FlagKey = web_search_enabled | sandbox_enabled | self_improve_enabled | workflows_enabled | maintenance_mode.

Analogs (VERIFIED): the grid leaf `HealthSignals.tsx:71-96` (`grid grid-cols-1 sm:grid-cols-2 ...`) for the 2×2 capability grid; `OperatorBand.tsx:38` (the amber full-width band) for the amber Platform-state panel.

065-A contract (LOCKED, winner A): four capability CARDS in a 2×2 grid; OFF tints the card red + adds an "off for everyone" tag + reveals a concrete impact line (impact derived from active runs + run metadata, e.g. "2 runs using code will error on their next call"). Capability switches flip DIRECTLY (no confirm — emergency speed). Maintenance sits APART in its own amber-framed "Platform state" panel with arm-to-confirm ("Put the whole platform in read-only mode? · Confirm / Cancel"). Consequence ≠ receipt: maintenance ON raises a persistent present-tense banner ("the whole platform is read-only right now") distinct from the past-tense ledger row ("Turned ON maintenance mode"). Plain-language default + ⌥ technical reveal.

Impact-copy data source (narrowed per checker WARNING 2): `ActiveRun` carries NO per-run tool-usage signal, so exact per-tool counts ("2 runs using code…") CANNOT be computed accurately — do NOT add a per-run tool signal (scope creep). Derivable count: the workflows switch only (count of ActiveRun.kind === "workflow" — the shell passes it as a prop; do NOT re-fetch). All other switches: the plain "off for everyone" fallback; render a count-based line only if a count prop happens to be supplied (best-effort).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CapabilityGrid — 2×2 armed-OFF switches + concrete impact copy (direct flip)</name>
  <files>frontend/src/components/admin/CapabilityGrid.tsx, frontend/src/components/admin/__tests__/CapabilityGrid.test.tsx</files>
  <read_first>
    - frontend/src/components/admin/HealthSignals.tsx (the 2×2 grid leaf at 71-96 to mirror)
    - frontend/src/lib/api.ts (setFlag + FlagKey from plan 06)
    - .planning/sketches/065-system-controls-and-maintenance/README.md (the 065-A winner-A contract)
    - .claude/skills/sketch-findings-agentic-rag/SKILL.md (armed-OFF weight; plain-language default)
  </read_first>
  <action>
Create `CapabilityGrid.tsx` as a presentational leaf: props `{ flags: { web_search_enabled, sandbox_enabled, self_improve_enabled, workflows_enabled }, impactCounts: { sandbox?: number, ... }, onToggle: (key, value) => Promise<void>, showTechnical: boolean }`. Render a 2×2 grid (mirror the HealthSignals grid classes) of four capability cards: Web search / Code sandbox / Self-improvement / Workflows. Each card has a plain-language name, a direct-flip switch (no confirm — on toggle call `onToggle(key, nextValue)`), and when OFF the card gets a red/armed tint + an "off for everyone" tag + an impact line. Impact copy is count-based ONLY where a count prop is supplied (derivable today: the workflows switch — "{n} running workflows will finish; new launches are blocked"); when no count is available (web/sandbox/self-improve — ActiveRun has no per-run tool signal) render the plain fallback impact copy ("off for everyone — in-flight calls will get a plain refusal"). Never fabricate a count. Plain-language default; reveal the technical flag key (e.g. `sandbox_enabled`) only when `showTechnical`. ON = calm/neutral. Create `__tests__/CapabilityGrid.test.tsx`: (1) four switches render; (2) toggling a switch calls onToggle with the right key + inverted value; (3) an OFF card shows the armed red styling + "off for everyone" + impact copy — count-based when a count prop is supplied (workflows with count 2 renders a "2 running workflows…" line), plain fallback when no count; (4) an ON card is neutral (no armed styling); (5) showTechnical reveals the raw flag key.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- CapabilityGrid 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Exactly four capability switches render (Web search / Code sandbox / Self-improvement / Workflows).
    - Toggling the sandbox switch calls `onToggle("sandbox_enabled", false)` when it was on (test assertion) — direct flip, no confirm dialog appears.
    - An OFF workflows card with a supplied count of 2 renders a count-based impact line ("2 running workflows…") + the "off for everyone" tag with armed/destructive styling; an OFF sandbox card with NO count renders the plain fallback impact copy (no fabricated number); an ON card has neither (test assertions).
    - `showTechnical` reveals the raw flag key string; default hides it.
  </acceptance_criteria>
  <done>2×2 armed-OFF capability grid with concrete impact copy and emergency-fast direct flips; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: MaintenancePanel — amber Platform-state panel, arm-to-confirm, consequence banner</name>
  <files>frontend/src/components/admin/MaintenancePanel.tsx</files>
  <read_first>
    - frontend/src/components/admin/OperatorBand.tsx (the amber full-width band shape at 38-85 to mirror the amber framing)
    - frontend/src/lib/api.ts (setFlag from plan 06)
    - .planning/sketches/065-system-controls-and-maintenance/README.md (the arm-to-confirm + consequence≠receipt rules)
  </read_first>
  <action>
Create `MaintenancePanel.tsx` as a presentational leaf: props `{ maintenanceOn: boolean, onSetMaintenance: (value: boolean) => Promise<void> }`. Render a spatially-separate amber-framed "Platform state" panel (mirror the OperatorBand amber framing) that is visually distinct from the CapabilityGrid (distinction-by-location, 065-A). Flipping maintenance uses ARM-TO-CONFIRM: the first click arms an inline "Put the whole platform in read-only mode? · Confirm / Cancel" prompt; Confirm calls `onSetMaintenance(true)` (turning off maintenance is the inverse). When `maintenanceOn` is true, show a persistent PRESENT-TENSE consequence banner ("the whole platform is read-only right now") — this is distinct from the past-tense ledger receipt (consequence ≠ receipt, 062-A). Plain-language copy throughout ("Users can read everything, but nothing new runs — no chats, workflows, or uploads."). Keep it a pure leaf (no fetch).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "MaintenancePanel" || echo "MaintenancePanel typechecks clean"</automated>
  </verify>
  <acceptance_criteria>
    - Clicking the maintenance toggle does NOT immediately call `onSetMaintenance` — it arms an inline "Put the whole platform in read-only mode? · Confirm / Cancel" prompt first (source shows a two-step arm→confirm, not a direct call).
    - Confirm calls `onSetMaintenance(true)`; Cancel dismisses without calling it.
    - When `maintenanceOn` is true, a persistent present-tense read-only banner renders (grep the present-tense "read-only right now" copy).
    - The panel uses amber framing distinct from the capability grid (grep amber/warning class on the panel container).
    - `npx tsc --noEmit` reports no new MaintenancePanel error.
  </acceptance_criteria>
  <done>Amber arm-to-confirm Platform-state panel with a present-tense consequence banner distinct from the receipt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser toggle → operator write | Presentation only; the server-side `require_operator` gate + PUT /admin/flags key allowlist are the boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-147-12 | Elevation of Privilege | client-side flag toggles | mitigate | Toggles only call `setFlag` (server-gated + key allowlist, plan 03); the client makes no authorization decision. |
| T-147-16 | Tampering (fat-finger DoS) | accidental maintenance-on | mitigate | Arm-to-confirm on the platform-wide switch (065-A graded friction) prevents a single stray click from wedging the platform. |
| T-147-SC | Tampering (supply chain) | npm installs | accept | Zero new dependencies (RESEARCH audit N/A). |
</threat_model>

<verification>
- `cd frontend && npm run test -- CapabilityGrid` green; `npx tsc --noEmit` no new CapabilityGrid/MaintenancePanel errors.
</verification>

<success_criteria>
- Armed-OFF 2×2 capability grid with concrete impact copy + direct flip.
- Amber arm-to-confirm maintenance panel with a present-tense consequence banner (consequence ≠ receipt).
</success_criteria>

<output>
Create `.planning/phases/147-operator-control-plane/147-08-SUMMARY.md` when done.
</output>
