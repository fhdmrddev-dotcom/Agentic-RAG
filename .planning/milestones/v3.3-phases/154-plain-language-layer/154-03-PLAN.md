---
phase: 154-plain-language-layer
plan: 03
type: execute
wave: 2
depends_on: [154-01]
files_modified:
  - frontend/src/pages/SettingsPage.tsx
  - frontend/src/components/chat/MessageInput.tsx
autonomous: true
requirements: [LANG-01]

must_haves:
  truths:
    - "Every user can flip 'Show technical names' from Settings (D-01) — the toggle writes the shared context and the global toggle is the PRIMARY app-wide reveal mechanism (D-03)."
    - "Bounded Settings labels a real user reads (Search, search index) render plainly by default (D-04) with the technical term behind the reveal; deep expert-config knobs stay as-is (Pitfall 4)."
    - "The chat composer keeps its already-plain General/Explorer labels and adds HELPERS only (D-03); MessageItem.tsx and StreamsProvider.tsx stay UNTOUCHED (G-5), verified by their suites still green."
    - "No backend, enum, API-field, or audit-action string is renamed (D-05)."
  artifacts:
    - path: "frontend/src/pages/SettingsPage.tsx"
      provides: "The app-wide 'Show technical names' toggle host (reuses TechnicalNamesToggle → useTechnicalNames) + bounded user-facing relabels"
      contains: "TechnicalNamesToggle"
    - path: "frontend/src/components/chat/MessageInput.tsx"
      provides: "General/Explorer mode helpers via the term-map (labels unchanged)"
      contains: "usePlainLabel"
  key_links:
    - from: "frontend/src/pages/SettingsPage.tsx"
      to: "frontend/src/providers/TechnicalNamesProvider.tsx"
      via: "TechnicalNamesToggle enabled={showTechnical} onToggle={toggle} from useTechnicalNames()"
      pattern: "useTechnicalNames\\("
    - from: "frontend/src/components/chat/MessageInput.tsx"
      to: "frontend/src/lib/termMap.ts"
      via: "usePlainLabel helper strings for agentmode.*"
      pattern: "usePlainLabel"
---

<objective>
Host the app-wide "Show technical names" toggle in Settings (so every user can flip the reveal — SC#3) and apply bounded plain-language relabels to the user-facing Settings + chat-composer surfaces (RESEARCH ranks 3 & 4).

Purpose: The reveal toggle must live where every user can reach it (Settings), not only in the admin Control Room. The composer is the highest-traffic surface but its labels are already plain — it needs HELPERS, not relabels — and is a G-5-adjacent area, so changes stay additive and never touch MessageItem/StreamsProvider.
Output: The Settings toggle host + bounded Settings relabels, and composer mode helpers.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/154-plain-language-layer/154-CONTEXT.md
@.planning/phases/154-plain-language-layer/154-RESEARCH.md
@.planning/phases/154-plain-language-layer/154-PATTERNS.md

# Wave-1 spine + the reusable control
@frontend/src/lib/termMap.ts
@frontend/src/providers/TechnicalNamesProvider.tsx
@frontend/src/components/admin/TechnicalNamesToggle.tsx

<interfaces>
<!-- From Wave 1 (Plan 01) + existing controls. Use directly. -->
// frontend/src/providers/TechnicalNamesProvider.tsx
export function useTechnicalNames(): { showTechnical: boolean; toggle: () => void; setShowTechnical: (v: boolean) => void }  // THROWS outside provider — Settings is always inside it

// frontend/src/components/admin/TechnicalNamesToggle.tsx  (reuse VERBATIM)
export function TechnicalNamesToggle(props: { enabled: boolean; onToggle: () => void }): JSX.Element   // renders "⌥ Technical names"

// frontend/src/lib/termMap.ts
export function usePlainLabel(key: TermKey): string
// keys that RESOLVE to real SettingsPage/composer fields (wire only these):
//   settings.tab.retrieval ("Search" / "Search & Retrieval")  → the Search & Retrieval TabsTrigger
//   settings.embedding ("search index" / "Embedding")          → the EMBEDDING_PRESETS-backed picker label
//   agentmode.default (helper: "Quick, direct answers.") | agentmode.explorer (helper: "Digs deeper — searches + multiple steps.")
// NOTE: settings.temperature / settings.context_window / settings.reembed have NO matching field in SettingsPage.tsx
//   (temperature lives in PhaseFormPanel, Surface E — below the cut line, out of scope). Do NOT wire them here;
//   they may remain defined-but-unused term-map keys for a later surface.

// frontend/src/lib/PlainLabel.tsx
export function PlainLabel(props: { term: TermKey; showHelper?: boolean }): JSX.Element
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings — host the "Show technical names" toggle + bounded user-facing relabels</name>
  <files>frontend/src/pages/SettingsPage.tsx</files>
  <read_first>
    - frontend/src/pages/SettingsPage.tsx:852-859 — the `Tabs`/`TabsList`/`TabsTrigger` (AI Model=0, Search &amp; Retrieval=1, Integrations=2, Memory=3, Audit Log=4). The `value` attrs are the routing keys — DO NOT change them; only the visible label text.
    - 154-RESEARCH.md §Surface D + Open Question #1 (which tab hosts the toggle — planner picks a small row near the top of "AI Model") + Open Question #2 (do NOT invent plain labels for expert config).
    - frontend/src/components/admin/TechnicalNamesToggle.tsx:14-38 — the prop-controlled control to reuse verbatim.
    - frontend/src/providers/TechnicalNamesProvider.tsx + frontend/src/lib/termMap.ts (Wave 1).
  </read_first>
  <action>
    (a) TOGGLE HOST (required for SC#3 / D-01): near the top of the "AI Model" tab content (value="0"), add a compact labelled row — a "Show technical names" label + `<TechnicalNamesToggle enabled={showTechnical} onToggle={toggle} />` where `const { showTechnical, toggle } = useTechnicalNames()`. The toggle is app-wide (tab-agnostic); this makes the GLOBAL toggle the primary reveal mechanism (D-03) reachable by every user. (b) BOUNDED RELABELS (D-04): relabel the "Search &amp; Retrieval" TabsTrigger label (:855) to `{usePlainLabel("settings.tab.retrieval")}` keeping `value="1"` unchanged; where the embedding / search-index user-facing field is rendered (the `EMBEDDING_PRESETS`-backed picker), use `usePlainLabel("settings.embedding")` for its label (plain default, technical on reveal) and optionally an ⓘ helper via `<PlainLabel term="settings.embedding" showHelper />`. NOTE — there is NO `temperature` field in `SettingsPage.tsx` (`grep -i temperature` = 0 hits; the only temperature field lives in `PhaseFormPanel.tsx`, Surface E, below the cut line — OUT of scope): do NOT wire `settings.temperature` here; leave that term-map key defined-but-unused. LEAVE deep expert-config knobs (sub-agent max output tokens, OpenRouter tool strategy) as-is — add a helper only if trivial; do NOT invent plain labels for them (Pitfall 4). No `value`/enum/API-field/audit rename — display strings only (D-02a/D-05).
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b 2>&1 | grep -cE 'error TS' && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TechnicalNamesToggle" frontend/src/pages/SettingsPage.tsx` ≥ 1 AND `grep -c "useTechnicalNames(" frontend/src/pages/SettingsPage.tsx` ≥ 1 (toggle wired to the shared context).
    - `grep -c "Show technical names" frontend/src/pages/SettingsPage.tsx` ≥ 1 (the host row label).
    - `grep -c 'usePlainLabel("settings.tab.retrieval")' frontend/src/pages/SettingsPage.tsx` ≥ 1; the `value="1"` TabsTrigger attr is unchanged (grep still shows `value="1"`).
    - `grep -c 'usePlainLabel("settings.temperature")' frontend/src/pages/SettingsPage.tsx` == 0 (phantom field NOT wired).
    - `npx tsc -b` prints exactly 30 `error TS` lines (0 net-new); `npx vite build` exits 0.
  </acceptance_criteria>
  <done>Every user can flip "Show technical names" from Settings (writes the shared context); bounded Settings labels that RESOLVE (Search, search index) read plainly by default; tab `value` keys + expert config unchanged; no phantom temperature wiring; tsc 0 net-new; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Chat composer — General/Explorer helpers (additive, G-5-safe)</name>
  <files>frontend/src/components/chat/MessageInput.tsx</files>
  <read_first>
    - frontend/src/components/chat/MessageInput.tsx:320-350 — the mode dropdown: the pill label (:327 `agentMode === "explorer" ? "Explorer" : "General"`) and the two `DropdownMenuItem`s (General :337, Explorer :345). These labels are ALREADY plain over the `"default"`/`"explorer"` enum.
    - 154-PATTERNS.md §MessageInput (Surface C) + the G-5 discipline note.
    - 154-RESEARCH.md §Surface C — this surface needs HELPERS, not relabels; keep General/Explorer.
    - frontend/src/lib/termMap.ts — `agentmode.default` / `agentmode.explorer` helper strings.
  </read_first>
  <action>
    Add a one-line muted HELPER under each mode dropdown item — General → the term-map helper "Quick, direct answers."; Explorer → "Digs deeper — searches + multiple steps." Source the helper text from the term-map (`TERM_MAP["agentmode.default"].helper` / `["agentmode.explorer"].helper`, or a small `<PlainLabel term="agentmode.default" showHelper />`) and render each as a small `text-[10px] text-muted-foreground` line inside the existing `DropdownMenuItem` (additive markup only). KEEP the "General"/"Explorer" labels and the `onAgentModeChange("default")`/`("explorer")` enum calls exactly as-is (the enum is the technical side; do NOT surface the raw `"default"`/`"explorer"` enum as a user label — Pitfall 4). This is the composer SHELL (`MessageInput.tsx`) — acceptable per G-5, but the change is display-string/markup ONLY: no render-flow, no stream logic, and MessageItem.tsx / StreamsProvider.tsx are NOT touched. Re-run the composer suites as non-regression.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/chat/__tests__ && npx tsc -b 2>&1 | grep -cE 'error TS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"General"\|Explorer' frontend/src/components/chat/MessageInput.tsx` still shows the labels present (labels NOT removed) AND `grep -c 'onAgentModeChange("default")' frontend/src/components/chat/MessageInput.tsx` ≥ 1 (enum call intact).
    - `git diff --name-only <base>..HEAD` includes `MessageInput.tsx` but NOT `MessageItem.tsx` NOR `StreamsProvider.tsx` (G-5 untouched target).
    - Composer/chat test suites green (non-regression); `npx tsc -b` prints exactly 30 `error TS` lines (0 net-new).
  </acceptance_criteria>
  <done>General/Explorer keep their plain labels and gain helper text off the term-map; enum calls intact; MessageItem/StreamsProvider untouched; suites green, tsc 0 net-new.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| display string → wire value | Settings tab `value` keys and the `"default"`/`"explorer"` agent-mode enum stay untouched; only visible labels/helpers change. No client→server / auth / data-access boundary crossed or added (display-only, D-05). |
| localStorage ↔ Settings toggle | The Settings toggle writes the same non-sensitive `"technical-names"` boolean via the shared context — not a session token or secret. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-154-01 | Tampering | SettingsPage relabels / MessageInput helpers | mitigate | Relabels change DISPLAY text only; TabsTrigger `value` keys and `onAgentModeChange` enum calls are unchanged (D-02a). Contract-Safety Recipe (D-05a) at phase end shows zero backend files changed. |
| T-154-02 | Tampering (XSS) | rendered labels/helpers | mitigate | Static term-map literals rendered as auto-escaped React text; no `dangerouslySetInnerHTML`, no user/model-supplied string in the sink. |
| T-154-04 | Tampering (regression) | MessageInput (G-5-adjacent) | mitigate | Change is additive markup only in the composer SHELL; MessageItem.tsx + StreamsProvider.tsx untouched (verified by diff) and their suites re-run as non-regression per G-5. |
| T-154-03 | Information Disclosure | technical reveal | accept | Field/mode wording is not secret (D-01); no packages installed (Package Legitimacy Gate not triggered). |
</threat_model>

<verification>
- `npx vitest run src/components/chat/__tests__` — composer/chat suites green (G-5 non-regression).
- `cd frontend && npm run build` — `tsc -b` exactly 30 `error TS` lines (0 net-new), `vite build` exit 0.
- `git diff --name-only <base>..HEAD` — includes `SettingsPage.tsx` + `MessageInput.tsx`; excludes `MessageItem.tsx`, `StreamsProvider.tsx`, and all backend/migration files.
</verification>

<success_criteria>
- The "Show technical names" toggle is reachable by every user from Settings and flips the whole app off the shared context.
- Bounded Settings labels + composer helpers read plainly by default; enum/keys unchanged; G-5 hot files untouched; tsc 0 net-new; suites green.
</success_criteria>

<output>
Create `.planning/phases/154-plain-language-layer/154-03-SUMMARY.md` when done.
</output>
