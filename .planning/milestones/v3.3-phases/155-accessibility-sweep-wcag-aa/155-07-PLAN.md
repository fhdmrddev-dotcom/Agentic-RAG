---
phase: 155-accessibility-sweep-wcag-aa
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/chat/ToolCallPanel.tsx
  - frontend/src/components/chat/MessageInput.tsx
  - frontend/src/components/chat/OutputFileCard.tsx
  - frontend/src/components/chat/RunCard.tsx
  - frontend/src/components/chat/CitationCard.tsx
  - frontend/src/components/chat/tool-bodies/ExecuteCodeBody.tsx
  - frontend/src/components/chat/tool-bodies/SearchDocumentsBody.tsx
  - frontend/src/components/skills/studio/RunHistory.tsx
  - frontend/src/components/skills/studio/VersionsTab.tsx
  - frontend/src/components/skills/studio/RunCaseDetail.tsx
  - frontend/src/components/skills/studio/RunBar.tsx
  - frontend/src/components/classification/RuleBuilderPanel.tsx
  - frontend/src/components/settings/ReembedConfirmModal.tsx
  - frontend/src/components/settings/ReembedStatusCard.tsx
  - frontend/src/components/settings/ProviderPicker.tsx
  - frontend/src/components/settings/EngineHealthCard.tsx
  - frontend/src/components/settings/ModelPillRow.tsx
  - frontend/src/components/settings/MemorySection.tsx
  - frontend/src/components/ingestion/ConditionPopover.tsx
  - frontend/src/components/ingestion/AutomationGroup.tsx
  - frontend/src/components/ingestion/ViewsGroup.tsx
  - frontend/src/components/ingestion/FolderTree.tsx
  - frontend/src/components/ingestion/FolderBreadcrumb.tsx
  - frontend/src/components/ingestion/DocumentUpload.tsx
  - frontend/src/components/layout/NavPanel.tsx
  - frontend/src/components/layout/ChatLayout.tsx
  - frontend/src/components/panel/PhaseCard.tsx
  - frontend/src/components/workflows/WorkflowSoul.tsx
  - frontend/src/components/workflows/WorkflowDoorSwitch.tsx
  - frontend/src/pages/SettingsPage.tsx
  - frontend/src/pages/WorkflowsPage.tsx
autonomous: true
requirements: [A11Y-01]
user_setup: []

must_haves:
  truths:
    - "D-04: the remaining opacity-modified muted-text offenders outside the admin cluster (chat / skills-studio / classification / settings / ingestion / layout / panel / workflows / pages — 31 files, 85 occurrences) on MEANINGFUL text are swept onto full-opacity AA-passing tokens; only truly decorative/disabled elements stay exempt (WCAG-allowed)"
    - "D-04: after this plan plus 155-02, zero meaningful-text `text-muted-foreground/{40,50,60,70}` occurrences remain app-wide — every residual is a documented decorative/disabled exemption"
    - "D-13: MessageItem.tsx / StreamsProvider.tsx are untouched (zero offenders confirmed in both; the G-5 red line is never crossed); ToolCallPanel.tsx receives className-only edits (G-5-adjacent, allowed)"
    - "D-03: the color-contrast category reaches zero failing nodes app-wide on the SEED-092 baseline pages, provable only by the live Chrome DevTools scan (jsdom cannot compute contrast)"
  artifacts:
    - path: "frontend/src/components/chat/ToolCallPanel.tsx"
      provides: "chat-cluster sweep: meaningful muted text on full-opacity AA tokens"
      contains: "text-muted-foreground"
  key_links:
    - from: "frontend/src/components/**/*.tsx + frontend/src/pages/*.tsx (clusters B)"
      to: "frontend/src/index.css tokens"
      via: "className text-muted-foreground / text-muted-foreground-dim (no opacity modifier on meaningful text)"
      pattern: "text-muted-foreground"
---

<objective>
Complete the D-04 opacity-offender sweep: convert the remaining `text-muted-foreground/{40,50,60,70}` classNames outside the admin cluster (31 files / 85 occurrences across chat, skills-studio, classification, settings, ingestion, layout, panel, workflows, and pages) onto full-opacity AA-passing tokens. Together with plan 155-02 (token lift + admin cluster), this closes the muted-text contrast offender class app-wide.

Purpose: 155-02 makes the dim token safe and proves the look (D-07 eyeball); this plan finishes the mechanical className transform on the same decision rule so no page anywhere composites muted text below AA. Files are fully disjoint from 155-02 (same wave, no ordering constraint — the className targets `text-muted-foreground`/`-dim` are correct regardless of which plan lands first).
Output: Zero meaningful-text opacity offenders app-wide; residuals documented as exemptions.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-accessibility-sweep-wcag-aa/155-CONTEXT.md
@.planning/phases/155-accessibility-sweep-wcag-aa/155-RESEARCH.md
@.planning/phases/155-accessibility-sweep-wcag-aa/155-PATTERNS.md

<interfaces>
<!-- The decision rule (identical to 155-02 Task 2 — one rule, two plans): -->
(a) MEANINGFUL text → drop the opacity modifier → `text-muted-foreground` (full opacity, ~7.7:1);
    quietest meta that must stay a step dimmer → `text-muted-foreground-dim` (lifted to 220 16% 70% by 155-02 Task 1).
(b) TRULY DECORATIVE / DISABLED → leave as-is (WCAG-allowed); document WHY in the SUMMARY.

<!-- Cluster B inventory (grep-verified, occurrences per file): -->
Task 1 — chat (7 files, 26 occ): ToolCallPanel(9) ExecuteCodeBody(5) MessageInput(5) OutputFileCard(3) RunCard(2) SearchDocumentsBody(1) CitationCard(1)
Task 2 — studio/classification/settings (11 files, 30 occ): RunHistory(8) VersionsTab(6) RuleBuilderPanel(6) ReembedConfirmModal(4) ReembedStatusCard(3) ProviderPicker(3) EngineHealthCard(2) RunCaseDetail(2) ModelPillRow(1) MemorySection(1) RunBar(1)
Task 3 — ingestion/layout/panel/workflows/pages long tail (13 files, 21 occ — 1-2 per file, purely mechanical):
  ConditionPopover(4) AutomationGroup(3) ViewsGroup(2) NavPanel(2) ChatLayout(2) SettingsPage(2)
  FolderTree(1) FolderBreadcrumb(1) DocumentUpload(1) PhaseCard(1) WorkflowSoul(1) WorkflowDoorSwitch(1) WorkflowsPage(1)

<!-- G-5: MessageItem.tsx / StreamsProvider.tsx have ZERO offenders — MUST NOT be touched. ToolCallPanel is G-5-ADJACENT (hot-file ledger) but NOT a red-line file: className-only edits allowed. -->
<!-- CitationCard(1): className-only; its a11y contract is verified separately in 155-06 — do not change roles/labels here. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep the CHAT-cluster opacity offenders (7 files, 26 occurrences)</name>
  <files>frontend/src/components/chat/ToolCallPanel.tsx, frontend/src/components/chat/tool-bodies/ExecuteCodeBody.tsx, frontend/src/components/chat/MessageInput.tsx, frontend/src/components/chat/OutputFileCard.tsx, frontend/src/components/chat/RunCard.tsx, frontend/src/components/chat/tool-bodies/SearchDocumentsBody.tsx, frontend/src/components/chat/CitationCard.tsx</files>
  <read_first>
    - frontend/src/index.css (L671 citation-CSS note — "meaningful muted text uses --muted-foreground, never the dim trap")
    - .planning/phases/155-accessibility-sweep-wcag-aa/155-PATTERNS.md ("Opacity sweep" section — the transform rule)
    - The 7 chat files at the offending lines (jump via `grep -rnE 'text-muted-foreground/(40|50|60|70)' src/components/chat`)
  </read_first>
  <action>
    Apply the interfaces decision rule per occurrence across the 7 chat files (26 hits): meaningful text → `text-muted-foreground`; quietest meta → `text-muted-foreground-dim`; truly decorative/disabled → exempt + document WHY in the SUMMARY. className-only edits — never surrounding logic or JSX structure. `ToolCallPanel.tsx` is G-5-ADJACENT (hot-file ledger) but NOT a red-line file — className-only change is allowed; `MessageItem.tsx` / `StreamsProvider.tsx` have zero offenders and MUST NOT appear in the diff. `CitationCard.tsx` gets its 1 className swap only — do NOT touch its roles/aria (155-06 verifies that contract). After the sweep, `grep -rnE 'text-muted-foreground/(40|50|60|70)' src/components/chat` must return only documented exemptions.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/chat/__tests__ >/dev/null 2>&1 && npx vite build >/dev/null 2>&1 && echo "chat residuals:" && (grep -rcE 'text-muted-foreground/(40|50|60|70)' src/components/chat | grep -vE ':0$' | wc -l) && echo CHAT_SWEEP_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only` for this task shows only the 7 listed chat files (no MessageItem.tsx, no StreamsProvider.tsx).
    - All edits are className-only (verify via `git diff` — no logic/JSX-structure lines changed).
    - Residual chat-cluster occurrences are all documented exemptions in 155-07-SUMMARY.md.
    - `cd frontend && npx vitest run src/components/chat/__tests__` → green (targeted non-regression; full `npm test` at the wave boundary per 155-VALIDATION.md Sampling Rate).
    - `cd frontend && npx vite build` exits 0.
  </acceptance_criteria>
  <done>Chat-cluster meaningful-text offenders swept; chat suites + build green; G-5 files untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the STUDIO / CLASSIFICATION / SETTINGS-cluster offenders (11 files, 30 occurrences)</name>
  <files>frontend/src/components/skills/studio/RunHistory.tsx, frontend/src/components/skills/studio/VersionsTab.tsx, frontend/src/components/skills/studio/RunCaseDetail.tsx, frontend/src/components/skills/studio/RunBar.tsx, frontend/src/components/classification/RuleBuilderPanel.tsx, frontend/src/components/settings/ReembedConfirmModal.tsx, frontend/src/components/settings/ReembedStatusCard.tsx, frontend/src/components/settings/ProviderPicker.tsx, frontend/src/components/settings/EngineHealthCard.tsx, frontend/src/components/settings/ModelPillRow.tsx, frontend/src/components/settings/MemorySection.tsx</files>
  <read_first>
    - .planning/phases/155-accessibility-sweep-wcag-aa/155-PATTERNS.md ("Opacity sweep" section)
    - The 11 files at the offending lines (jump via `grep -rnE 'text-muted-foreground/(40|50|60|70)' src/components/skills src/components/classification src/components/settings`)
  </read_first>
  <action>
    Apply the same interfaces decision rule per occurrence across the 11 files (30 hits): meaningful text → `text-muted-foreground`; quietest meta → `text-muted-foreground-dim`; truly decorative/disabled → exempt + document WHY in the SUMMARY. className-only edits, never logic. These are Skill-Studio (RunHistory/VersionsTab/RunCaseDetail/RunBar), the classification RuleBuilderPanel, and the Settings re-embed/provider/engine-health/memory surfaces — all shipped operator-approved designs; the transform changes text brightness only, no layout/structure. After the sweep, the scoped grep must return only documented exemptions.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/skills src/components/settings >/dev/null 2>&1 && npx vite build >/dev/null 2>&1 && echo "studio/settings residuals:" && (grep -rcE 'text-muted-foreground/(40|50|60|70)' src/components/skills src/components/classification src/components/settings | grep -vE ':0$' | wc -l) && echo STUDIO_SETTINGS_SWEEP_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only` for this task shows only the 11 listed files.
    - All edits are className-only (verify via `git diff`).
    - Residual occurrences in skills/classification/settings are all documented exemptions in 155-07-SUMMARY.md.
    - `cd frontend && npx vitest run src/components/skills src/components/settings` → green (targeted; full `npm test` at the wave boundary).
    - `cd frontend && npx vite build` exits 0.
  </acceptance_criteria>
  <done>Studio/classification/settings meaningful-text offenders swept; targeted suites + build green.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep the long-tail offenders — ingestion / layout / panel / workflows / pages (13 files, 21 occurrences, 1-2 per file)</name>
  <files>frontend/src/components/ingestion/ConditionPopover.tsx, frontend/src/components/ingestion/AutomationGroup.tsx, frontend/src/components/ingestion/ViewsGroup.tsx, frontend/src/components/ingestion/FolderTree.tsx, frontend/src/components/ingestion/FolderBreadcrumb.tsx, frontend/src/components/ingestion/DocumentUpload.tsx, frontend/src/components/layout/NavPanel.tsx, frontend/src/components/layout/ChatLayout.tsx, frontend/src/components/panel/PhaseCard.tsx, frontend/src/components/workflows/WorkflowSoul.tsx, frontend/src/components/workflows/WorkflowDoorSwitch.tsx, frontend/src/pages/SettingsPage.tsx, frontend/src/pages/WorkflowsPage.tsx</files>
  <read_first>
    - .planning/phases/155-accessibility-sweep-wcag-aa/155-PATTERNS.md ("Opacity sweep" section)
    - The 13 files at the offending lines (jump via `grep -rnE 'text-muted-foreground/(40|50|60|70)' src/components/ingestion src/components/layout src/components/panel src/components/workflows src/pages`)
  </read_first>
  <action>
    Apply the same interfaces decision rule across the long tail (13 files, 21 hits — 1-2 per file, purely mechanical className swaps): meaningful text → `text-muted-foreground`; quietest meta → `text-muted-foreground-dim`; truly decorative/disabled → exempt + document WHY. NavPanel's decorative `opacity-50` `MessageSquare` icon (a graphic, not a `text-muted-foreground/*` class) is explicitly out of scope — do not convert it. NavPanel/ChatLayout edits here are className-only; the NavPanel aria-label/button work belongs to plan 155-03 (wave 2), do not do it here. After this task, run the APP-WIDE closing check: `grep -rnE 'text-muted-foreground/(40|50|60|70)' src` — together with 155-02's admin sweep, every remaining hit anywhere in src must be a documented decorative/disabled exemption; record the final app-wide residual list in the SUMMARY.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/__tests__ src/components/ingestion >/dev/null 2>&1 && npx vite build >/dev/null 2>&1 && echo "app-wide residual files:" && (grep -rcE 'text-muted-foreground/(40|50|60|70)' src | grep -vE ':0$' | wc -l) && echo LONGTAIL_SWEEP_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only` for this task shows only the 13 listed files.
    - All edits are className-only (verify via `git diff`); NavPanel's decorative MessageSquare `opacity-50` untouched; no aria/button changes in NavPanel (that is 155-03's job).
    - The app-wide grep residual list (post-155-02 + this plan) contains ONLY documented decorative/disabled exemptions, recorded in 155-07-SUMMARY.md.
    - `cd frontend && npx vitest run src/pages/__tests__ src/components/ingestion` → green (targeted; full `npm test` at the wave boundary).
    - `cd frontend && npx vite build` exits 0; `npx tsc -b` → 30 baseline errors, 0 net-new.
  </acceptance_criteria>
  <done>Long-tail offenders swept; app-wide meaningful-text opacity offenders now zero (residuals documented); targeted suites + build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design-token consumers → rendered text | className swaps change only composited text color/opacity on already-shipped surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-07-G5 | Tampering | `MessageItem.tsx` / `StreamsProvider.tsx` | mitigate | Zero offenders confirmed in both — they MUST NOT appear in any task's diff (D-13 RED LINE); ToolCallPanel is G-5-adjacent, className-only allowed; `git diff --name-only` is the tripwire |
| T-155-07-VIS | Tampering (visual regression) | 31-file className transform | mitigate | Same decision rule as the D-07-approved 155-02 sweep; the token values are the operator-approved ones (D-07 checkpoint lives in 155-02); className-only edits, no layout/structure changes |
| T-155-07-FALSEGREEN | Repudiation | contrast SC claimed from vitest | mitigate | jsdom cannot compute contrast; the app-wide color-contrast zero is proven ONLY by the live Chrome scan (D-03) in VALIDATION.md (G-6 #1) |
</threat_model>

<verification>
- App-wide closing check: `cd frontend && grep -rnE 'text-muted-foreground/(40|50|60|70)' src` → only documented exemptions remain (cross-checked against 155-02 + 155-07 SUMMARY exemption lists).
- `cd frontend && npx vitest run src/components/chat/__tests__ src/components/skills src/components/settings src/pages/__tests__ src/components/ingestion` → green (full `npm test` at the wave boundary per 155-VALIDATION.md).
- `cd frontend && npx vite build` → exit 0; `npx tsc -b` → 30 baseline errors, 0 net-new (SEED-056/049).
- `git diff --name-only` → MessageItem.tsx / StreamsProvider.tsx NOT listed; only the 31 declared files.
- Live (VALIDATION.md, D-03): Chrome DevTools color-contrast scan on the SEED-092 pages → 0 failing nodes.
</verification>

<success_criteria>
Every meaningful-text opacity offender outside the admin cluster is swept onto full-opacity AA-passing tokens, completing the app-wide D-04 sweep begun in 155-02 — with G-5 files untouched and all residuals documented as WCAG-allowed exemptions.
</success_criteria>

<output>
Create `.planning/phases/155-accessibility-sweep-wcag-aa/155-07-SUMMARY.md` when done. Record: per-cluster residual counts, the final app-wide exemption list (feeds the D-03 live scan + the D-06 remainder doc), and confirmation that MessageItem/StreamsProvider never appeared in a diff.
</output>
