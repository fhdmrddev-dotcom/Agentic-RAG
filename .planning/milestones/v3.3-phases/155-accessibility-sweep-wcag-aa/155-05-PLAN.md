---
phase: 155-accessibility-sweep-wcag-aa
plan: 05
type: execute
wave: 3
depends_on: ["155-03"]
files_modified:
  - frontend/src/components/admin/__tests__/AuditTab.a11y.test.tsx
  - frontend/src/components/admin/__tests__/UsersAndAccess.a11y.test.tsx
  - frontend/src/components/admin/__tests__/FeatureVisibility.a11y.test.tsx
  - frontend/src/components/admin/__tests__/ModelRegistryTab.a11y.test.tsx
  - frontend/src/components/admin/__tests__/ModelDiscoveryPanel.a11y.test.tsx
autonomous: true
requirements: [A11Y-01]
user_setup: []

must_haves:
  truths:
    - "D-01: each net-new Governance/Registry admin sub-component (148 audit browser + users roster + feature-visibility map; 149 model registry + discovery) has a `*.a11y.test.tsx` vitest-axe suite asserting zero STRUCTURAL axe violations across its honest states"
    - "D-12: the Control Room `/admin` Governance + Model-Registry tabs are covered by the zero-violations bar via per-sub-component isolation (the custom tablist hides inactive panels — Pitfall 3)"
    - "D-05: the audit browser chip-filter buttons, users-roster action controls, and registry row controls are queryable by role + accessible name (icon-only controls carry aria-label) — the regression proof for the button-name sweep on this cluster"
    - "D-14: any confirmed false positive is a documented per-rule/per-selector axe exclusion with a WHY comment, mirrored in VALIDATION.md — never a global disable"
  artifacts:
    - path: "frontend/src/components/admin/__tests__/AuditTab.a11y.test.tsx"
      provides: "audit browser (chip filters + ledger rows) a11y contract"
      contains: "toHaveNoViolations"
    - path: "frontend/src/components/admin/__tests__/UsersAndAccess.a11y.test.tsx"
      provides: "users roster + disable/grant action a11y contract"
      contains: "toHaveNoViolations"
  key_links:
    - from: "admin/__tests__/*.a11y.test.tsx"
      to: "vitest-axe axe()"
      via: "render(component) then settle then expect(await axe(container)).toHaveNoViolations()"
      pattern: "toHaveNoViolations"
---

<objective>
Lock the D-01 regression gate on the Control Room Governance + Model-Registry surfaces: author `*.a11y.test.tsx` suites for the audit browser (148), users-and-access roster (148), feature-visibility map (148), model registry tab (149), and model discovery panel (149). Each scans with vitest-axe across honest states and asserts the named role/name contract for its filter chips, roster action controls, and registry row controls.

Purpose: These four tabs are the highest-density opacity-offender surfaces (AuditTab=13, UsersAndAccess=8, ModelRegistryTab=6, FeatureVisibility=3) that Plan 02 swept and Plan 03 labeled — the suites lock those fixes so a regression fails CI. Per-sub-component isolation avoids Pitfall 3 (the custom tablist hides inactive panels).
Output: Five Governance/Registry a11y suites, all green.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-accessibility-sweep-wcag-aa/155-CONTEXT.md
@.planning/phases/155-accessibility-sweep-wcag-aa/155-RESEARCH.md
@.planning/phases/155-accessibility-sweep-wcag-aa/155-PATTERNS.md

<interfaces>
<!-- THE reference suites to copy (structure, mocks, settle-then-scan): -->
frontend/src/components/relationships/RelationshipsSection.a11y.test.tsx
frontend/src/components/metadata/DocumentDetailPanel.a11y.test.tsx

<!-- Shared setup (global — do NOT re-add): setupTests.ts extends the axe matcher; import { axe } from "vitest-axe". -->
<!-- Mock @/lib/supabase (auth) + partial-mock @/lib/api (override this surface's fetches). Wrap useTechnicalNames() consumers in <TechnicalNamesProvider>. -->
<!-- CRITICAL: color-contrast is a NO-OP in jsdom - STRUCTURAL rules only; contrast lives in the live scan. -->
<!-- Honest-state roles: loading=role="status", error=role="alert", receipt=role="status". -->

<!-- Components under test (read each for real props/data before writing): -->
admin/AuditTab.tsx, admin/UsersAndAccess.tsx, admin/FeatureVisibility.tsx,
admin/ModelRegistryTab.tsx (has __tests__), admin/ModelDiscoveryPanel.tsx (has __tests__)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: a11y suites for the governance tabs (AuditTab, UsersAndAccess, FeatureVisibility)</name>
  <files>frontend/src/components/admin/__tests__/AuditTab.a11y.test.tsx, frontend/src/components/admin/__tests__/UsersAndAccess.a11y.test.tsx, frontend/src/components/admin/__tests__/FeatureVisibility.a11y.test.tsx (+ additive aria fix to the owning component or shared ui/* primitive only if axe surfaces a real violation, per D-13)</files>
  <read_first>
    - frontend/src/components/relationships/RelationshipsSection.a11y.test.tsx (copy this shape)
    - frontend/src/components/admin/AuditTab.tsx, UsersAndAccess.tsx, FeatureVisibility.tsx (props + honest states + the chip filters / roster action controls / audience map toggles)
    - frontend/src/components/admin/__tests__/ControlRoomPage.test.tsx (existing admin mock/provider setup to reuse)
  </read_first>
  <action>
    Create `AuditTab.a11y.test.tsx`, `UsersAndAccess.a11y.test.tsx`, `FeatureVisibility.a11y.test.tsx` in `admin/__tests__/`. Render each with representative props, settle a node, then `expect(await axe(container)).toHaveNoViolations()` (STRUCTURAL only). Contract asserts: AuditTab chip-filter controls queryable by role+accessible-name and the ledger rows have an accessible structure; UsersAndAccess roster action controls (disable / grant) reachable by role+name with the victim-naming confirm exposed via ARIA (button with a name, never color-alone); FeatureVisibility audience-map toggles reachable by role+name with visible WORD state (never color-alone). Wrap useTechnicalNames() consumers in `<TechnicalNamesProvider>`. Assert loading=`role="status"` / error=`role="alert"` where the tab fetches. Apply D-13 (additive fix at owning component/primitive if a real violation surfaces) and D-14 (documented per-rule/per-selector exclusion + VALIDATION.md) as needed. No "verify contrast" assertion.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/admin/__tests__/AuditTab.a11y.test.tsx src/components/admin/__tests__/UsersAndAccess.a11y.test.tsx src/components/admin/__tests__/FeatureVisibility.a11y.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The three named `*.a11y.test.tsx` files exist and the vitest command above exits 0.
    - AuditTab suite asserts chip-filter controls queryable by role+accessible-name.
    - UsersAndAccess suite asserts disable/grant controls reachable by role+name with the confirm exposed via ARIA (not color-alone).
    - FeatureVisibility suite asserts audience toggles reachable by role+name with a visible WORD state.
    - Each suite calls `toHaveNoViolations()`; no contrast assertion; any D-14 exclusion is per-rule/per-selector with a WHY comment.
  </acceptance_criteria>
  <done>Three governance a11y suites green; controls reachable by role+name (state never color-alone); structural zero-violations.</done>
</task>

<task type="auto">
  <name>Task 2: a11y suites for the model-registry surfaces (ModelRegistryTab, ModelDiscoveryPanel)</name>
  <files>frontend/src/components/admin/__tests__/ModelRegistryTab.a11y.test.tsx, frontend/src/components/admin/__tests__/ModelDiscoveryPanel.a11y.test.tsx (+ additive aria fix to the owning component or shared ui/* primitive only if axe surfaces a real violation, per D-13)</files>
  <read_first>
    - frontend/src/components/relationships/RelationshipsSection.a11y.test.tsx (copy this shape)
    - frontend/src/components/admin/ModelRegistryTab.tsx, ModelDiscoveryPanel.tsx (props + editable-column controls + discovery states)
    - frontend/src/components/admin/__tests__/ModelRegistryTab.test.tsx, ModelDiscoveryPanel.test.tsx (existing mocks to reuse)
  </read_first>
  <action>
    Create `ModelRegistryTab.a11y.test.tsx` and `ModelDiscoveryPanel.a11y.test.tsx` in `admin/__tests__/`. Same settle-then-scan pattern (STRUCTURAL only). Contract asserts: the registry table's editable-column controls (toggles / inputs) have associated labels or accessible names; icon-only row controls carry aria-label; the discovery panel's loading/empty/error states use the role vocabulary (`role="status"` / `role="alert"`); any provider/model icon is decorative-or-labeled (the Phase-127 @lobehub icon convention — assert it is `aria-hidden` when decorative, or has an accessible name). Wrap useTechnicalNames() consumers in `<TechnicalNamesProvider>`. Apply D-13 / D-14 as needed. No contrast assertion.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/admin/__tests__/ModelRegistryTab.a11y.test.tsx src/components/admin/__tests__/ModelDiscoveryPanel.a11y.test.tsx && npx vitest run src/components/admin/__tests__ >/dev/null 2>&1 && echo ADMIN_SUITES_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - The two named `*.a11y.test.tsx` files exist and the vitest command above exits 0.
    - ModelRegistryTab suite asserts editable-column controls have associated labels/accessible names and icon-only row controls carry aria-label.
    - ModelDiscoveryPanel suite asserts the loading/empty/error honest-state roles.
    - Provider/model icons are asserted decorative-or-labeled (aria-hidden when decorative, else named).
    - Targeted admin suites (`npx vitest run src/components/admin/__tests__`) green — full `npm test` runs at the wave boundary per 155-VALIDATION.md Sampling Rate; each suite calls `toHaveNoViolations()`; no contrast assertion.
  </acceptance_criteria>
  <done>Two registry a11y suites green; editable + icon-only controls labeled; discovery honest-state roles asserted; targeted admin suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test render → axe scan | Additive test files; the only source risk is a D-13 additive fix if a real violation surfaces on a governance/registry surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-05-FALSEGREEN | Repudiation (false-green) | jsdom color-contrast no-op | mitigate | STRUCTURAL rules only; contrast proven live (D-03); no "verify contrast" assertion (G-6 #1) |
| T-155-05-EXCLUDE | Repudiation | axe rule exclusion to hit zero | mitigate | Per-rule + per-selector with a WHY comment + VALIDATION.md mirror (D-14); never blanket (G-6 #2) |
| T-155-05-INFO | Information Disclosure | audit/roster row accessible names | mitigate | Assert accessible names are role verbs / already-visible field text; the suite must not add labels exposing hidden IDs or secrets (T-155-03-INFO carries forward) |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/admin/__tests__/{AuditTab,UsersAndAccess,FeatureVisibility,ModelRegistryTab,ModelDiscoveryPanel}.a11y.test.tsx` → all green.
- `npx tsc -b` → 30 baseline, 0 net-new; `npx vite build` → 0; `npm test` → full suite green.
- Any D-14 exclusion appears in the test (per-rule/selector + WHY) and VALIDATION.md.
- Live (VALIDATION.md, D-03): button-name + color-contrast audit on the `/admin` governance/registry tabs → 0 failing nodes.
</verification>

<success_criteria>
Every net-new Governance/Registry admin sub-component has a passing vitest-axe a11y suite asserting zero structural violations and the named role/name contract, locking the D-01 zero-violations bar for this cluster.
</success_criteria>

<output>
Create `.planning/phases/155-accessibility-sweep-wcag-aa/155-05-SUMMARY.md` when done. Record: suites created, any real violations fixed (D-13), any D-14 exclusions encoded.
</output>
