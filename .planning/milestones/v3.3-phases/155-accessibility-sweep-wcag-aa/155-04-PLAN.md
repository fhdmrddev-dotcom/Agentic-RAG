---
phase: 155-accessibility-sweep-wcag-aa
plan: 04
type: execute
wave: 3
depends_on: ["155-03"]
files_modified:
  - frontend/src/components/admin/__tests__/OperatorBand.a11y.test.tsx
  - frontend/src/components/admin/__tests__/HealthSignals.a11y.test.tsx
  - frontend/src/components/admin/__tests__/RecentActionsCard.a11y.test.tsx
  - frontend/src/components/admin/__tests__/LockedTab.a11y.test.tsx
  - frontend/src/components/admin/__tests__/TechnicalNamesToggle.a11y.test.tsx
  - frontend/src/components/admin/__tests__/ActiveRunsSection.a11y.test.tsx
  - frontend/src/components/admin/__tests__/CapabilityGrid.a11y.test.tsx
  - frontend/src/components/admin/__tests__/MaintenancePanel.a11y.test.tsx
autonomous: true
requirements: [A11Y-01]
user_setup: []

must_haves:
  truths:
    - "D-01: each net-new Control-Plane sub-component has a `*.a11y.test.tsx` vitest-axe suite asserting zero STRUCTURAL axe violations across its honest states (populated / empty / loading=role=status / error=role=alert)"
    - "D-12: the Control Room `/admin` Control-Plane surface (146 shell band + 147 active-runs/capability/maintenance + 150 encryption tile) is covered by the zero-violations bar via per-sub-component isolation (Pitfall 3: the custom tablist does not render inactive panels)"
    - "D-09: the kill-switch arm-to-confirm guard (CapabilityGrid / MaintenancePanel) and Active-Runs Kill control expose keyboard-operable roles + accessible names — the vitest half of scenario 3 (the operator confirms the live keyboard drive in VALIDATION.md)"
    - "D-14: any genuinely unfixable violation (Radix internal / confirmed false positive) is encoded as a per-rule, per-selector exclusion IN the axe test with a WHY comment, mirrored in VALIDATION.md — never a silent global disable"
  artifacts:
    - path: "frontend/src/components/admin/__tests__/CapabilityGrid.a11y.test.tsx"
      provides: "kill-switch guard a11y contract (roles + accessible names + no structural violations)"
      contains: "toHaveNoViolations"
    - path: "frontend/src/components/admin/__tests__/ActiveRunsSection.a11y.test.tsx"
      provides: "active-runs + Kill control a11y contract"
      contains: "toHaveNoViolations"
  key_links:
    - from: "admin/__tests__/*.a11y.test.tsx"
      to: "vitest-axe axe()"
      via: "render(component) then settle then expect(await axe(container)).toHaveNoViolations()"
      pattern: "toHaveNoViolations"
---

<objective>
Lock the D-01 regression gate on the Control Room Control-Plane surface: author `*.a11y.test.tsx` suites for the operational admin sub-components (146 shell band, health, recent actions, locked/toggle; 147 active-runs + capability grid + maintenance panel with the kill-switch/read-only guards). Each suite scans with vitest-axe across the component's honest states and asserts the named ARIA/role contract — including the keyboard-operable roles on the destructive-action guards (D-09 scenario 3's automatable half).

Purpose: The custom Control Room tablist conditionally hides inactive panels (Pitfall 3), so a single page-level scan misses 4 of 5 tabs — the fix is per-sub-component isolation, matching the shipped DocumentDetailPanel/RelationshipsSection precedent. These suites make the zero-violations bar permanent (a future regression fails CI).
Output: Per-sub-component a11y suites, all green.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-accessibility-sweep-wcag-aa/155-CONTEXT.md
@.planning/phases/155-accessibility-sweep-wcag-aa/155-RESEARCH.md
@.planning/phases/155-accessibility-sweep-wcag-aa/155-PATTERNS.md

<interfaces>
<!-- THE reference suites to copy (structure, mocks, settle-then-scan): -->
frontend/src/components/relationships/RelationshipsSection.a11y.test.tsx
frontend/src/components/metadata/DocumentDetailPanel.a11y.test.tsx  (richer role/aria contract asserts)

<!-- Shared setup (already global — do NOT re-add): -->
frontend/src/setupTests.ts  -> expect.extend(vitest-axe matchers); just import { axe } from "vitest-axe" and call it.
Mock pattern: vi.mock("@/lib/supabase", ...auth getSession...) + partial vi.mock("@/lib/api", keep actual, override this surface's fetches).
jsdom has no matchMedia -> stub it when a component reads prefers-reduced-motion (DocumentDetailPanel L205-234 template).
Components that call useTechnicalNames() must render inside <TechnicalNamesProvider> (the ControlRoomPage.test precedent).

<!-- Honest-state role vocabulary to assert: loading=role="status" aria-live=polite; error=role="alert"; receipt=role="status". -->
<!-- CRITICAL: color-contrast is a NO-OP in jsdom - assert STRUCTURAL rules (roles/aria/names) only; contrast lives in the live scan. Do NOT add a "verify contrast" assertion. -->

<!-- Components under test (read each for its real props/data contract before writing the suite): -->
admin/OperatorBand.tsx, admin/HealthSignals.tsx, admin/RecentActionsCard.tsx, admin/LockedTab.tsx,
admin/TechnicalNamesToggle.tsx, admin/ActiveRunsSection.tsx (has __tests__), admin/CapabilityGrid.tsx (has __tests__),
admin/MaintenancePanel.tsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: a11y suites for the shell band + status components (OperatorBand, HealthSignals, RecentActionsCard, LockedTab, TechnicalNamesToggle)</name>
  <files>frontend/src/components/admin/__tests__/OperatorBand.a11y.test.tsx, frontend/src/components/admin/__tests__/HealthSignals.a11y.test.tsx, frontend/src/components/admin/__tests__/RecentActionsCard.a11y.test.tsx, frontend/src/components/admin/__tests__/LockedTab.a11y.test.tsx, frontend/src/components/admin/__tests__/TechnicalNamesToggle.a11y.test.tsx (+ additive aria fix to the owning admin component or shared ui/* primitive only if axe surfaces a real violation, per D-13)</files>
  <read_first>
    - frontend/src/components/relationships/RelationshipsSection.a11y.test.tsx (copy this shape)
    - frontend/src/components/metadata/DocumentDetailPanel.a11y.test.tsx (role/aria contract + matchMedia stub patterns)
    - frontend/src/components/admin/OperatorBand.tsx, HealthSignals.tsx, RecentActionsCard.tsx, LockedTab.tsx, TechnicalNamesToggle.tsx (their real props + honest states)
    - frontend/src/components/admin/__tests__/ControlRoomPage.test.tsx (how existing admin tests mount providers/mocks — reuse)
  </read_first>
  <action>
    Create one `*.a11y.test.tsx` per component in `frontend/src/components/admin/__tests__/`. For each: render with representative props, `await` a settled node (findByText/findByRole) BEFORE calling `axe()`, then `expect(await axe(container)).toHaveNoViolations()` (STRUCTURAL rules only). Add the named contract asserts — HealthSignals loading→`role="status"` / error→`role="alert"` (distinct); TechnicalNamesToggle exposes an accessible name + switch/`aria-pressed` semantics reachable by role; LockedTab announces its locked state as a visible WORD (never color-alone); RecentActionsCard rows queryable by role+name. Wrap any component consuming `useTechnicalNames()` in `<TechnicalNamesProvider>` (else the hook throws). If a component reads prefers-reduced-motion, stub matchMedia and assert it STILL renders. If axe surfaces a REAL violation the Plan 03 sweep missed, apply the additive fix at the owning component or shared primitive (D-13) and note it in the SUMMARY; if it is a confirmed Radix false positive, encode a per-rule/per-selector exclusion (`axe(container, { rules: { "rule-id": { enabled: false } } })`) with a WHY comment (D-14) and log it to VALIDATION.md — never a global disable.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/admin/__tests__/OperatorBand.a11y.test.tsx src/components/admin/__tests__/HealthSignals.a11y.test.tsx src/components/admin/__tests__/RecentActionsCard.a11y.test.tsx src/components/admin/__tests__/LockedTab.a11y.test.tsx src/components/admin/__tests__/TechnicalNamesToggle.a11y.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The five named `*.a11y.test.tsx` files exist under `frontend/src/components/admin/__tests__/` and the vitest command above exits 0.
    - Each suite calls `expect(await axe(container)).toHaveNoViolations()` after a settled render.
    - HealthSignals suite asserts distinct `role="status"` (loading) and `role="alert"` (error).
    - TechnicalNamesToggle suite asserts an accessible name + `aria-pressed`/switch role reachable by role query.
    - No suite contains a "contrast" assertion; any D-14 exclusion is per-rule + per-selector with a WHY comment.
  </acceptance_criteria>
  <done>Five status/shell a11y suites green; structural zero-violations + named role/aria contracts; no contrast assertion.</done>
</task>

<task type="auto">
  <name>Task 2: a11y suites for the destructive-action surfaces (ActiveRunsSection, CapabilityGrid, MaintenancePanel)</name>
  <files>frontend/src/components/admin/__tests__/ActiveRunsSection.a11y.test.tsx, frontend/src/components/admin/__tests__/CapabilityGrid.a11y.test.tsx, frontend/src/components/admin/__tests__/MaintenancePanel.a11y.test.tsx (+ additive aria fix to the owning component or shared ui/* primitive only if axe surfaces a real violation, per D-13)</files>
  <read_first>
    - frontend/src/components/relationships/RelationshipsSection.a11y.test.tsx (copy this shape)
    - frontend/src/components/admin/ActiveRunsSection.tsx, CapabilityGrid.tsx, MaintenancePanel.tsx (their props + the arm-to-confirm / Kill / read-only guard states)
    - frontend/src/components/admin/__tests__/ActiveRunsSection.test.tsx, CapabilityGrid.test.tsx, ModelRegistryTab.test.tsx (existing non-a11y mocks to reuse)
  </read_first>
  <action>
    Create `ActiveRunsSection.a11y.test.tsx`, `CapabilityGrid.a11y.test.tsx`, `MaintenancePanel.a11y.test.tsx` in `admin/__tests__/`. Same settle-then-scan pattern (STRUCTURAL rules only). Focus the contract asserts on the destructive-action guards (D-09 scenario 3, the automatable half): the Active-Runs Kill control and the CapabilityGrid kill-switch / MaintenancePanel read-only toggle must be reachable by role+accessible-name and expose their arm-to-confirm state via ARIA (e.g. `aria-pressed` / an accessible confirm dialog with `role` + accessible name, NOT color-alone) — assert the confirm step is a queryable button with a name, not a hover-only or color-only affordance. Include loading=`role="status"` / error=`role="alert"` states where the component fetches. Apply the D-13/D-14 rules as in Task 1 if a real violation or false positive appears. Do NOT drive the full keyboard walkthrough here (that is the live Chrome-MCP + operator step in VALIDATION.md) — this task proves the ROLES/NAMES exist so the keyboard drive can succeed.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/admin/__tests__/ActiveRunsSection.a11y.test.tsx src/components/admin/__tests__/CapabilityGrid.a11y.test.tsx src/components/admin/__tests__/MaintenancePanel.a11y.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The three named `*.a11y.test.tsx` files exist and the vitest command above exits 0.
    - The CapabilityGrid suite asserts the kill-switch confirm step is a button queryable by role+accessible-name (not color-alone).
    - The ActiveRunsSection suite asserts the Kill control is reachable by role+accessible-name.
    - The MaintenancePanel suite asserts the read-only/maintenance toggle exposes state via ARIA (not color-alone).
    - Each suite calls `toHaveNoViolations()`; no contrast assertion; any D-14 exclusion is per-rule/per-selector with a WHY comment.
  </acceptance_criteria>
  <done>Three destructive-guard a11y suites green; guards reachable by role+name with ARIA-exposed confirm state; structural zero-violations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test render → axe scan | Additive test files; no production runtime surface. The only source risk is a D-13 additive fix if a real violation surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-04-FALSEGREEN | Repudiation (false-green) | jsdom color-contrast no-op | mitigate | Suites assert STRUCTURAL rules only; contrast is proven live (D-03) — no "verify contrast" assertion is written (G-6 #1) |
| T-155-04-EXCLUDE | Repudiation | axe rule exclusion to hit zero | mitigate | Any exclusion is per-rule + per-selector with a WHY comment in the test AND mirrored in VALIDATION.md (D-14); never a global/blanket disable (G-6 #2) |
| T-155-04-GUARD | Elevation of Privilege (adjacent) | kill-switch / Kill confirm reachable by a11y tree | accept | a11y hardening makes the destructive guard keyboard-operable; it does not change the require_operator authorization (untouched, backend) — display/role assertions only |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/admin/__tests__/*.a11y.test.tsx` → all green.
- Each suite calls `toHaveNoViolations()` and asserts named roles; no contrast assertion.
- `npx tsc -b` → 30 baseline, 0 net-new; `npx vite build` → 0.
- Any D-14 exclusion appears both in the test (per-rule/selector + WHY) and in VALIDATION.md.
- Live (VALIDATION.md, D-09 scenario 3): operator keyboard-drives `/admin` tab switch → kill-switch arm-to-confirm → audit receipt.
</verification>

<success_criteria>
Every net-new Control-Plane admin sub-component has a passing vitest-axe a11y suite asserting zero structural violations and the named role/name contract (including the destructive-action guards), locking the D-01 zero-violations bar for this surface cluster.
</success_criteria>

<output>
Create `.planning/phases/155-accessibility-sweep-wcag-aa/155-04-SUMMARY.md` when done. Record: suites created, any real violations fixed (D-13), any D-14 exclusions encoded, and whether the destructive-guard roles were already present or needed an additive fix.
</output>
