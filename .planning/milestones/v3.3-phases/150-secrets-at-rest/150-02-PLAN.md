---
phase: 150-secrets-at-rest
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/100_secret_key_columns.sql
  - supabase/full-schema.sql
autonomous: true
requirements: [SEC-01]

must_haves:
  truths:
    - "The 10 missing secret columns (9 {provider}_api_key + tavily_api_key) exist as text columns on app_settings after the migration (D-150-08)"
    - "The migration is idempotent (ADD COLUMN IF NOT EXISTS) and re-runnable without error (D-150-08)"
    - "A provider-key save through Settings no longer fails with UndefinedColumn (root cause of the D-150-07 silent-save is closed)"
    - "supabase/full-schema.sql is regenerated from the live DB (no --reset) and reflects the 12 secret columns"
  artifacts:
    - path: supabase/migrations/100_secret_key_columns.sql
      provides: "Idempotent DDL adding the 10 missing app_settings secret text columns"
      contains: "ADD COLUMN IF NOT EXISTS openai_api_key text"
    - path: supabase/full-schema.sql
      provides: "Regenerated single-file deploy artifact reflecting all 12 secret columns"
  key_links:
    - from: supabase/migrations/100_secret_key_columns.sql
      to: "live local Postgres app_settings"
      via: "psycopg2 / Supabase SQL editor apply against 127.0.0.1:54322"
      pattern: "ALTER TABLE public.app_settings"
---

<objective>
Close the load-bearing DB divergence RESEARCH found: of the nominal 12 secret columns in `_API_KEY_COLUMNS`, only `embedding_api_key` and `rerank_api_key` exist in the live database — the 9 `{provider}_api_key` columns and `tavily_api_key` were designed in code but never migrated. Per operator decision D-150-08, add the 10 missing `text` columns via one numbered migration (mig 100), apply it to the live local DB, and regenerate the schema artifact.

Purpose: Make SC#1 literally true for provider keys (they become DB-persistable + encryptable) and fix the true root cause of the D-150-07 silent-save (`UndefinedColumn` → caught → bool ignored → HTTP 200). Without this, encrypt-on-write into a nonexistent provider column would fail the whole UPDATE atomically (RESEARCH Pitfall 2), and Plan 04's D-150-07 raise-on-failure would turn today's silent 200 into a 500 that also loses the co-submitted embedding key.
Output: `supabase/migrations/100_secret_key_columns.sql`, applied to the live local DB, plus a regenerated `supabase/full-schema.sql`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/150-secrets-at-rest/150-CONTEXT.md
@.planning/phases/150-secrets-at-rest/150-RESEARCH.md
@.planning/phases/150-secrets-at-rest/150-PATTERNS.md

<interfaces>
<!-- The exact 10 columns to add (the 12-member _API_KEY_COLUMNS minus the 2 that already exist). -->
Add (all `text`, no default): openai_api_key, anthropic_api_key, google_api_key,
  openrouter_api_key, ollama_api_key, deepseek_api_key, moonshot_api_key,
  minimax_api_key, zhipu_api_key, tavily_api_key
Already exist (research-verified — IF NOT EXISTS makes re-including harmless but they are NOT the target):
  embedding_api_key, rerank_api_key
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 100 (10 missing secret text columns)</name>
  <files>supabase/migrations/100_secret_key_columns.sql</files>
  <read_first>
    - supabase/migrations/099_model_registry_deprecated.sql (COPY its full shape: the header comment block with APPLY/regenerate/CLOUD-PARITY notes, and the idempotent `ALTER TABLE ... ADD COLUMN IF NOT EXISTS ... text` statements)
    - supabase/migrations/097_operator_flags.sql (the `INSERT INTO public.app_settings (id) VALUES ('global') ON CONFLICT DO NOTHING;` ensure-global-row line, if the global row is not guaranteed)
    - backend/app/main.py:123-130 (the `_API_KEY_COLUMNS` allowlist — the column names are the migration's source of truth)
    - .planning/phases/150-secrets-at-rest/150-PATTERNS.md (migration section — the exact text-column-add convention and cloud-parity note)
  </read_first>
  <action>
    Create `supabase/migrations/100_secret_key_columns.sql`. Filename must match `<digits>_name.sql` (no letter suffix — the Supabase CLI silently skips `100b`-style names).
    Header comment block (copy 099's structure): name it Phase 150 (SEC-01, D-150-08); explain that only 2 of the nominal 12 secret columns existed and this adds the 10 missing so provider/tavily keys are DB-persistable and encryptable at rest; note metadata-only, no backfill (a NULL secret column is the correct "unset → env fallback" state); note no RLS change (app_settings is service-role write only). Include the standard APPLY block: paste the FULL file into the LOCAL Supabase SQL editor (or psycopg2 to 127.0.0.1:54322) and run; idempotent (ADD COLUMN IF NOT EXISTS) so safe to re-run; NEVER `supabase db push`/`db reset`. Include the regenerate note (`bash scripts/regenerate-full-schema.sh`, no --reset) and the CLOUD PARITY note (mig 100 joins mig 099 in the pending-on-cloud set — apply at promotion, do NOT touch cloud now).
    Body: one `INSERT ... ('global') ON CONFLICT DO NOTHING` global-row guard (optional but harmless), then 10 statements `ALTER TABLE public.app_settings ADD COLUMN IF NOT EXISTS {col} text;` — one per name in the plan's `<interfaces>` add-list. Plain `text`, NO default. Do NOT add the 2 already-existing columns to the target set (including them is harmless via IF NOT EXISTS, but the 10 listed are the deliverable).
  </action>
  <verify>
    <automated>test -f "supabase/migrations/100_secret_key_columns.sql" && grep -cE 'ADD COLUMN IF NOT EXISTS (openai|anthropic|google|openrouter|ollama|deepseek|moonshot|minimax|zhipu|tavily)_api_key text' supabase/migrations/100_secret_key_columns.sql</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/100_secret_key_columns.sql` exists; filename matches `^[0-9]+_.*\.sql$`.
    - It contains exactly 10 `ADD COLUMN IF NOT EXISTS {provider|tavily}_api_key text` statements covering all 10 names in the add-list (grep count == 10).
    - Every statement uses `IF NOT EXISTS` (idempotent) and targets `public.app_settings`.
    - The header carries the APPLY-via-SQL-editor + regenerate + CLOUD-PARITY notes (grep `db push` shows the NEVER warning; `CLOUD PARITY` present).
    - No `DEFAULT` clause on any secret column (the added api_key lines contain no `DEFAULT`).
  </acceptance_criteria>
  <done>The migration file exists, is idempotent, adds exactly the 10 missing `text` secret columns, and documents the apply + cloud-parity steps.</done>
</task>

<task type="auto">
  <name>Task 2 [BLOCKING]: Apply migration 100 to the live local DB and regenerate full-schema</name>
  <files>supabase/full-schema.sql</files>
  <read_first>
    - supabase/migrations/100_secret_key_columns.sql (the file authored in Task 1 — the SQL to execute)
    - scripts/regenerate-full-schema.sh (confirm it is the no-reset live-DB dump; run it, do NOT pass --reset)
    - scripts/pending-cloud-migrations.sh (git-diff-based; auto-detects the new migration vs origin/production — nothing to edit, just confirm it lists 100)
    - .planning/reported-bugs (none folded here) and CLAUDE.md migrations rule (NEVER db push/db reset)
  </read_first>
  <action>
    [BLOCKING] Apply the migration to the RUNNING local Supabase Postgres — this is a schema change that build/type checks cannot detect (secret columns are referenced dynamically via `$N` params, so `tsc`/import checks pass green without it — a false-positive verification state). This MUST complete before Wave-2 provider-key verification (Plan 03 ciphertext-at-rest, Plan 04 D-150-07 500), else a provider-key save 500s on `UndefinedColumn` (RESEARCH Pitfall 2). Wave ordering already sequences this ahead (mig = Wave 1, seams = Wave 2).
    Execute the statements in `100_secret_key_columns.sql` against the live local DB using psycopg2 with DSN `postgresql://postgres:postgres@127.0.0.1:54322/postgres` (the project's standard evidence tool) — run each `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` and the global-row INSERT, then commit. The migration is idempotent, so re-running is safe. Do NOT use `supabase db push` / `supabase db reset` (project rule — those wipe dev data). Equivalent operator path: paste the file into the LOCAL Supabase SQL editor.
    After applying, run `bash scripts/regenerate-full-schema.sh` (NO --reset) from the repo root to rebuild `supabase/full-schema.sql` from the live DB. Confirm cloud parity is carried: `bash scripts/pending-cloud-migrations.sh` must list `100_secret_key_columns.sql` (do NOT apply anything to cloud — deploys are operator-gated). Never write scratch `.py` files into `backend/` (the uvicorn --reload watcher wedges on Windows) — run the psycopg2 apply as an inline `python -c` or from the repo-root scratch area.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -c "import psycopg2; c=psycopg2.connect('postgresql://postgres:postgres@127.0.0.1:54322/postgres'); cur=c.cursor(); cur.execute(\"select column_name from information_schema.columns where table_name='app_settings' and column_name like '%_api_key'\"); cols={r[0] for r in cur.fetchall()}; need={'openai_api_key','anthropic_api_key','google_api_key','openrouter_api_key','ollama_api_key','deepseek_api_key','moonshot_api_key','minimax_api_key','zhipu_api_key','tavily_api_key','embedding_api_key','rerank_api_key'}; missing=need-cols; assert not missing, ('MISSING', missing); print('all 12 secret columns present as text')"</automated>
  </verify>
  <acceptance_criteria>
    - `information_schema.columns` for `app_settings` shows all 12 secret columns present (the automated check asserts the 12-set ⊆ live columns and prints success).
    - A raw `SELECT openai_api_key FROM app_settings` executes without `UndefinedColumn`.
    - `supabase/full-schema.sql` regenerated via `regenerate-full-schema.sh` (no --reset) and contains the 10 new columns.
    - `scripts/pending-cloud-migrations.sh` lists `100_secret_key_columns.sql` as pending on cloud (parity carried, not applied).
    - No `supabase db push` / `db reset` was run (dev data preserved); no scratch `.py` written into `backend/`.
  </acceptance_criteria>
  <done>All 12 secret columns exist in the live local DB as `text`, `full-schema.sql` is regenerated, and cloud parity is carried — Wave-2 encryption verification is unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → DB schema | DDL changes the persisted schema; a column added without encryption active could hold plaintext. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-07 | Information Disclosure | new app_settings secret columns | mitigate | Columns default NULL (the migration writes no plaintext); encryption activates on the next keyed boot (Plan 04 sweep). The migration only creates capacity, it does not populate. |
| T-150-07b | Tampering | migration idempotence / re-run | mitigate | `ADD COLUMN IF NOT EXISTS` — re-running never errors or drops data (099 precedent). |
| T-150-04a | Denial of Service | provider-key save after D-150-07 raise | mitigate | This [BLOCKING] apply precedes Wave-2 provider-key verification so encrypt-on-write targets real columns; wave ordering (mig Wave 1 → seams Wave 2) enforces it. |
</threat_model>

<verification>
- The automated psycopg2 check asserts all 12 secret columns exist as `text`.
- `grep -cE 'ADD COLUMN IF NOT EXISTS ..._api_key text'` == 10 for the new provider/tavily columns.
- `scripts/pending-cloud-migrations.sh` shows `100_secret_key_columns.sql`.
</verification>

<success_criteria>
All 12 secret columns exist in the live local DB as `text`; the migration is idempotent; `full-schema.sql` is regenerated; cloud parity is carried (not applied). Provider-key saves can now target real columns, unblocking Plan 03/04 encryption verification.
</success_criteria>

<output>
Create `.planning/phases/150-secrets-at-rest/150-02-SUMMARY.md` when done.
</output>
