---
phase: 153-inline-citations
plan: 04
type: execute
wave: 2
depends_on: ["153-02"]
files_modified:
  - frontend/src/components/chat/CitationPeek.tsx
  - frontend/src/components/chat/__tests__/CitationPeek.test.tsx
autonomous: true
requirements: [CITE-01]

must_haves:
  truths:
    - "SC#2: the peek surfaces the source passage for a chunk citation (head [n] filename + snippet + Open document) so the marker click-through reveals what was retrieved"
    - "D-10: a full-doc peek shows a 'Full document — no single passage' affordance + Open document, NOT a chunk snippet or similarity score (reuse the is_full_doc branch)"
    - "D-04: a null/short passage degrades to the calm path (head + Open document, snippet omitted) — never an error banner or red text"
    - "The peek is keyboard-reachable and pinnable: focus surfaces it, Enter/click pins it, Esc closes and restores focus; the pin toggle carries a state-toggled accessible name (Pin/Unpin citation {n})"
  artifacts:
    - path: "frontend/src/components/chat/CitationPeek.tsx"
      provides: "positioned hover-peek/pin popover (chunk + full-doc variants), a11y focus mgmt, Open document"
      min_lines: 60
      exports: ["CitationPeek"]
    - path: "frontend/src/components/chat/__tests__/CitationPeek.test.tsx"
      provides: "chunk + full-doc variant / pin toggle name / Esc close / null-passage degrade coverage"
      contains: "Full document"
  key_links:
    - from: "frontend/src/components/chat/CitationPeek.tsx"
      to: "useCitationNav().openDocument"
      via: "peek footer Open document → openDocument(citation.document_id)"
      pattern: "openDocument"
    - from: "CitationPeek pin control"
      to: "aria-label Pin/Unpin citation {n}"
      via: "state-toggled accessible name"
      pattern: "Pin citation|Unpin citation"
---

<objective>
Build the net-new hover-peek → click-to-pin popover the click-through sketch locked
(075-A winner A). Given a citation + anchor rect + pinned state, `CitationPeek` renders a
positioned card: the chunk variant shows head `[n] {filename}`, the passage snippet
(italic), and a footer `{loc} ↗ Open document`; the full-doc variant (D-10) shows
`Full document — no single passage` + `↗ Open document` only (no snippet/score). It is
keyboard-reachable and pinnable with full focus management (Esc restores focus, the pin
toggle carries a state-toggled accessible name), and a null/short passage degrades to the
calm path (D-04) — never an error banner.

Purpose: SC#2 (clicking/focusing a marker reveals the retrieved passage) + D-10 full-doc
shape. Presentational + interaction only; the marker that drives it lands in 153-05.
Output: `CitationPeek.tsx` + `CitationPeek.test.tsx`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/153-inline-citations/153-CONTEXT.md
@.planning/phases/153-inline-citations/153-UI-SPEC.md
@.planning/phases/153-inline-citations/153-PATTERNS.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md

<interfaces>
<!-- From 153-02 (Wave 1) — consume; do NOT redefine. -->
citationNav.tsx: `useCitationNav()` → { openDocument(documentId) }. Citation CSS + --primary reserved live in index.css.

<!-- Analogs (verified HEAD 2026-07-15) -->
Surface tokens to mirror — components/ui/tooltip.tsx: `bg-popover`, `border`, `text-popover-foreground`, `shadow-md`,
`animate-in fade-in-0 zoom-in-95`. createPortal is used NOWHERE in frontend/src today — the portal + pin state machine is net-new.
is_full_doc branch shape (CitationCard.tsx:55–61): `is_full_doc ? "Full document" : chunk_index != null ? \`Chunk ${chunk_index+1}\` : ""`.
Version suffix (CitationCard.tsx:51–54): `(v${version_number})` when > 1.
Citation type: {document_id, filename, chunk_index, passage, similarity, is_full_doc, version_number}.

UI-SPEC §Click-through Contract (LOCKED): peek `position: absolute`, `z-index: 60`, `width: 320px`, `bg --popover`,
`border --border`, `radius --radius-md (10px)`, `shadow --shadow-lg`, `padding 12px 14px`; enter/exit opacity + translateY(4px) 120ms.
Positioning: `top = markerRect.bottom + scrollY + 8`, `left = min(markerRect.left, viewportWidth − 340)`; clamp within the chat column on mobile.
Pin: `.pinned` → border --primary/35% + ring; pin toggle shows 📌; persists until unpin/Esc/outside-click.
§A11y: pinned peek `role="dialog" aria-modal="false"` labelled by its head; Esc closes + restores focus to the marker;
pin toggle aria-label `Pin citation {n}` / `Unpin citation {n}`; meaningful muted text --muted-foreground (NOT --muted-foreground-dim).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CitationPeek popover — chunk + full-doc variants, positioning, Open document (TDD)</name>
  <files>frontend/src/components/chat/CitationPeek.tsx, frontend/src/components/chat/__tests__/CitationPeek.test.tsx</files>
  <read_first>
    - frontend/src/components/ui/tooltip.tsx (the Radix surface-token + motion-class pattern to mirror)
    - frontend/src/components/chat/CitationCard.tsx L51–73 (version suffix + is_full_doc branch + passage — the content shapes to reuse)
    - frontend/src/lib/citationNav.tsx (useCitationNav.openDocument — from 153-02)
    - .planning/phases/153-inline-citations/153-UI-SPEC.md §"Click-through Contract" + §"Copywriting Contract" (peek chunk/full-doc copy, error state) + §"Accessibility"
    - .planning/phases/153-inline-citations/153-PATTERNS.md §"CitationPeek.tsx" + §"No Analog Found"
  </read_first>
  <behavior>
    - Chunk variant: renders head `[n] {filename}` (+ `(v{version_number})` when >1), the `passage` as an italic muted snippet, and a footer with `{loc}` (Chunk {chunk_index+1} · {similarity 2dp}) left and `↗ Open document` right.
    - Full-doc variant (is_full_doc): renders head + a `Full document — no single passage` line (NO snippet), footer `↗ Open document` only (no loc/score). (D-10)
    - Null/short passage on a chunk citation: renders head + Open document with the snippet omitted — no error banner, no red text. (D-04)
    - Open document: activating the footer link calls `openDocument(citation.document_id)`.
    - Pin/a11y: `pinned` renders `role="dialog" aria-modal="false"` labelled by the head; the pin toggle's accessible name is `Pin citation {n}` when unpinned and `Unpin citation {n}` when pinned; Esc invokes the onClose handler.
    - Positioning uses an injected anchor rect to compute top/left (assert the computed style clamps `left` to `viewportWidth − 340`).
  </behavior>
  <action>
    Create `CitationPeek` accepting `{ citation, n, anchorRect, pinned, onPin, onClose }`. Render an absolutely-positioned card
    (via `createPortal` to `document.body`, or an absolutely-positioned node — pick portal for z-index safety) using the
    `bg-popover`/`border`/`shadow` tokens mirrored from `ui/tooltip.tsx`, sized per the UI-SPEC (`width 320`, `z-index 60`,
    `padding 12px 14px`, `radius --radius-md`). Branch the body on `citation.is_full_doc` REUSING the CitationCard branch shape
    (full-doc → `Full document — no single passage`, no snippet/score; chunk → passage snippet + `{loc}` footer). Degrade a
    null/short `passage` to head + Open document (D-04) — never an error banner. Wire `↗ Open document` to
    `useCitationNav().openDocument(citation.document_id)`. Add focus management: when `pinned`, mark `role="dialog"
    aria-modal="false"` labelled by the head, Tab reaches the pin toggle + Open document, Esc calls `onClose` and (via the
    parent marker in 153-05) restores focus; the pin toggle carries the state-toggled `aria-label`. Compute `top/left` from
    `anchorRect` per the UI-SPEC (clamp `left` to `viewportWidth − 340`). Use `--primary` only for the Open-document link + the
    pinned ring; muted text uses `--muted-foreground`. Honor `prefers-reduced-motion` (CSS-owned, from 153-02). No new package.
    Author `CitationPeek.test.tsx` FIRST (RED) then GREEN.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/chat/__tests__/CitationPeek.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/chat/__tests__/CitationPeek.test.tsx` → all pass.
    - A test asserts the full-doc variant shows `Full document` copy and NO snippet/similarity (D-10); the chunk variant shows the passage + loc.
    - A test asserts a null-passage chunk citation degrades to head + Open document with no error/red text (D-04).
    - A test asserts the pin toggle accessible name flips `Pin citation {n}` ↔ `Unpin citation {n}` and Esc calls `onClose`.
    - `cd frontend && npx vite build` → exit 0, 0 net-new tsc errors vs baseline.
    - `grep -c "muted-foreground-dim" frontend/src/components/chat/CitationPeek.tsx` shows no new usage.
  </acceptance_criteria>
  <done>CitationPeek renders both variants with correct copy, calm degradation, Open document, and accessible pin/Esc behavior; the test file is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| citation passage/filename → peek render | passage text is from a retrieved doc (attacker-influenceable) shown inside the peek |
| peek "Open document" → documents view | opening the cited doc must respect visibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153-04-01 | Tampering / XSS | peek passage/filename render | mitigate | rendered as React text nodes (auto-escaped); NO `dangerouslySetInnerHTML` in the peek — model text is never re-parsed as HTML |
| T-153-04-02 | Information Disclosure | peek "Open document" | mitigate | routes through `useCitationNav().openDocument` → the existing owner-scoped panel fetch (T-153-02-01) |
| T-153-04-03 | Denial (a11y trap) | pinned peek focus | mitigate | `aria-modal="false"` (non-trapping), Esc closes + restores focus to the marker; pin toggle carries a state-toggled accessible name (feeds Phase 155) |
| T-153-04-SC | Tampering | npm installs | accept | NO package installs (portal via react-dom, tokens vendored) — Legitimacy Gate not triggered |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/chat/__tests__/CitationPeek.test.tsx` → green.
- `cd frontend && npx vite build` → exit 0, 0 net-new tsc errors vs baseline.
- Full-doc variant is snippet/score-free (D-10); null passage degrades calmly (D-04); no `dangerouslySetInnerHTML`.
- Pin toggle accessible name toggles; Esc closes.
</verification>

<success_criteria>
- The click-through peek reveals the retrieved passage for chunk citations and a "Full document" affordance for full-doc citations (SC#2/D-10), degrades calmly on missing passage (D-04), and is fully keyboard/AT-operable.
</success_criteria>

<output>
Create `.planning/phases/153-inline-citations/153-04-SUMMARY.md` when done.
</output>
