---
phase: 157-deployment-presets-runbook-stretch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/nginx.conf
  - frontend/Dockerfile
  - frontend/.dockerignore
autonomous: true
requirements: [DEPLOY-01]

must_haves:
  truths:
    - "A net-new frontend/Dockerfile + nginx.conf + .dockerignore static-serve the Vite dist and reverse-proxy /api/ to backend:8000, built with npx vite build NOT npm run build (D-03)"
    - "nginx.conf ports the recovered guide's SPA try_files fallback + SSE directives (proxy_buffering off, proxy_read_timeout 300s) + client_max_body_size 100m + the trailing-slash /api/ strip on proxy_pass http://backend:8000/"
    - "frontend/.dockerignore excludes .env* so no frontend secret enters the image layers"
  artifacts:
    - path: frontend/nginx.conf
      provides: "SPA static serve + /api/ SSE reverse proxy"
      contains: "proxy_read_timeout 300s"
    - path: frontend/Dockerfile
      provides: "2-stage node build -> nginx serve, VITE_* as build args"
      contains: "npx vite build"
    - path: frontend/.dockerignore
      provides: "build-context secret + bloat exclusion"
      contains: ".env"
  key_links:
    - from: frontend/nginx.conf
      to: backend
      via: "proxy_pass http://backend:8000/ over the compose network (trailing slash strips /api/)"
      pattern: "backend:8000"
---

<objective>
Author the net-new frontend build trio — `frontend/Dockerfile`, `frontend/nginx.conf`, `frontend/.dockerignore` — that static-serve the Vite `dist` and reverse-proxy `/api/` to `backend:8000` (D-03). The nginx block is ported from the recovered VPS guide, retargeted from `127.0.0.1:8000` to the compose service name `backend:8000`. The Dockerfile is a 2-stage build (node build -> nginx serve) that takes the three `VITE_*` values as build args and runs `npx vite build` (NEVER `npm run build`, which runs `tsc -b` and fails on pre-existing test-file type rot — LESSONS A7).

Purpose: The all-in-one compose needs a self-contained frontend image; this is the only genuinely net-new build artifact in the phase.
Output: `frontend/Dockerfile`, `frontend/nginx.conf`, `frontend/.dockerignore`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/157-deployment-presets-runbook-stretch/157-CONTEXT.md
@.planning/phases/157-deployment-presets-runbook-stretch/157-RESEARCH.md

<interfaces>
<!-- Contracts this trio must honor. Names VERIFIED from source. -->
Build command: `npx vite build` (output dir `dist`). NEVER `npm run build` (= `tsc -b && vite build`; tsc -b fails on test rot — LESSONS A7). frontend/vercel.json already uses buildCommand "vite build".
Lockfile: frontend/package-lock.json EXISTS -> use `npm ci` (reproducible).
Build-time VITE args (ARG -> ENV BEFORE the build): VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, VITE_API_BASE_URL (default /api).
Reverse-proxy target: the compose backend service is `backend` on port `8000` (backend/Dockerfile EXPOSE 8000). Backend routes are unprefixed (e.g. /threads, /health) -> the nginx `/api/` location MUST use a TRAILING SLASH on proxy_pass to strip `/api/`.
nginx web root: /usr/share/nginx/html (nginx:alpine default) — copy dist there.
Base images (pin at author time): node:22-alpine (build), nginx:1.27-alpine (runtime).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the nginx SPA + SSE reverse-proxy config, retargeted to backend:8000</name>
  <files>frontend/nginx.conf</files>
  <read_first>
    - frontend/nginx.conf (the net-new file to create)
    - .planning/research/recovered/RECOVERED_VPS_Deployment_Guide.md § Step 10 (SOURCE OF TRUTH — the server block to port: SPA try_files fallback, client_max_body_size 100m, SSE proxy directives, trailing-slash strip)
    - .planning/phases/157-deployment-presets-runbook-stretch/157-RESEARCH.md § "Code Examples > nginx.conf" (the retargeted block, verbatim) and § "Pitfall 6" (resolver + variable upstream so nginx starts before backend DNS resolves)
  </read_first>
  <action>
    Create `frontend/nginx.conf` as a single `server {}` block (destined for `/etc/nginx/conf.d/default.conf`) ported from the recovered guide's Step 10, retargeting `proxy_pass http://127.0.0.1:8000/` to `http://backend:8000/` (the compose service name). Keep: `listen 80`, `root /usr/share/nginx/html`, `index index.html`, the SPA fallback `location /` with `try_files $uri $uri/ /index.html`, `client_max_body_size 100m` (uploads). For the `/api/` location apply the Pitfall-6 deferred-DNS pattern: `resolver 127.0.0.11 valid=10s;` + `set $backend_upstream http://backend:8000;` + `proxy_pass $backend_upstream/;` (variable form defers DNS to request time so nginx boots even if backend is not up). Preserve the SSE directives `proxy_buffering off`, `proxy_cache off`, `proxy_read_timeout 300s`, `chunked_transfer_encoding on`, and the standard forwarding headers (Host, X-Real-IP, X-Forwarded-For, X-Forwarded-Proto). The TRAILING SLASH on `$backend_upstream/` is critical (strips `/api/`). Reference the RESEARCH Code-Examples block for the exact directives.
  </action>
  <acceptance_criteria>
    - `frontend/nginx.conf` exists.
    - nginx syntax passes: `docker run --rm -v "$PWD/frontend/nginx.conf:/etc/nginx/conf.d/default.conf" nginx:1.27-alpine nginx -t` exits 0.
    - Contains `try_files $uri $uri/ /index.html` (SPA fallback).
    - Contains `client_max_body_size 100m`.
    - Contains `proxy_buffering off` and `proxy_read_timeout 300s` (SSE).
    - The `/api/` upstream resolves to `backend:8000` WITH a trailing slash (`proxy_pass $backend_upstream/;` where `$backend_upstream` = `http://backend:8000`).
    - Contains `resolver 127.0.0.11` (deferred DNS, Pitfall 6).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author the 2-stage frontend Dockerfile + the secret-excluding .dockerignore</name>
  <files>frontend/Dockerfile, frontend/.dockerignore</files>
  <read_first>
    - frontend/Dockerfile and frontend/.dockerignore (the net-new files to create)
    - backend/.dockerignore (SOURCE OF TRUTH — the exclude pattern to mirror; confirm it excludes `.env*`)
    - frontend/package.json (proof the build script is `tsc -b && vite build` — the reason to call `npx vite build` directly) and frontend/vercel.json (buildCommand "vite build")
    - .planning/phases/157-deployment-presets-runbook-stretch/157-RESEARCH.md § "Pattern 1" (build-time VITE injection via build args — the exact Dockerfile shape) and § "Pitfall 4"
  </read_first>
  <action>
    Create `frontend/Dockerfile` as two stages. Build stage `FROM node:22-alpine AS build`, WORKDIR /app: declare `ARG VITE_SUPABASE_URL`, `ARG VITE_SUPABASE_ANON_KEY`, `ARG VITE_API_BASE_URL=/api`, then `ENV` each from its ARG BEFORE the build (Vite inlines `import.meta.env.VITE_*` at build time). COPY package.json + package-lock.json, `RUN npm ci`, COPY the rest, then `RUN npx vite build` (produces `dist`). Runtime stage `FROM nginx:1.27-alpine`: COPY `nginx.conf` to `/etc/nginx/conf.d/default.conf`, COPY `--from=build /app/dist` to `/usr/share/nginx/html`, `EXPOSE 80`. Create `frontend/.dockerignore` mirroring `backend/.dockerignore`, excluding at minimum `node_modules`, `dist`, `.env*` (secret exclusion — critical), `.git`, test/coverage dirs. Reference the RESEARCH Pattern-1 block for the exact Dockerfile content; do not invent alternate build steps.
  </action>
  <acceptance_criteria>
    - `frontend/Dockerfile` and `frontend/.dockerignore` exist.
    - Dockerfile contains `RUN npx vite build` and does NOT contain `npm run build` (`grep -c 'npm run build' frontend/Dockerfile` == 0).
    - Dockerfile declares all three build args: `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `VITE_API_BASE_URL`, each promoted to `ENV` before the build.
    - Dockerfile runtime stage is `nginx:1.27-alpine` and COPYs `/app/dist` to `/usr/share/nginx/html` and the nginx.conf to `/etc/nginx/conf.d/default.conf`.
    - Dockerfile uses `npm ci` (not `npm install`).
    - `.dockerignore` excludes `.env*`: `grep -c '\.env' frontend/.dockerignore` >= 1 (secret leakage into image layers blocked — T-157-SEC).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host build context → frontend image layers | Anything not excluded by `.dockerignore` is baked into the image (and its history) |
| browser → nginx → backend | nginx is the only ingress; it proxies `/api/` to the internal backend and passes SSE through unbuffered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-157-SEC | Information disclosure | frontend image layers | mitigate | `frontend/.dockerignore` excludes `.env*` (and `node_modules`, `.git`) so `frontend/.env.local` never enters a layer — mirrors the `backend/.dockerignore` precedent |
| T-157-VITE | Information disclosure | baked VITE_* values | accept | Only public browser-safe values are baked: the Supabase URL and the ANON key (public by design) + the relative `/api` path — no service-role/secret keys ever passed as build args |
| T-157-SC | Tampering | Docker base images | mitigate | Pin official Docker Hub tags `node:22-alpine` + `nginx:1.27-alpine`; adds NO npm/pip language packages (`npm ci` installs the existing verified lockfile only) — slopcheck N/A |
</threat_model>

<verification>
- `nginx -t` passes on `frontend/nginx.conf` (via the docker run one-liner above).
- Dockerfile grep-gates green: `npx vite build` present, `npm run build` absent, three VITE ARGs present.
- `.dockerignore` excludes `.env*`.
- (Build itself is exercised end-to-end only in the Plan 05 D-09 smoke; per-artifact proof here is static.)
</verification>

<success_criteria>
The frontend build trio exists: nginx.conf ports the SPA+SSE+trailing-slash directives retargeted to `backend:8000` and passes `nginx -t`; the 2-stage Dockerfile builds with `npx vite build` and injects the three `VITE_*` as build args; `.dockerignore` excludes `.env*`. The compose (Plan 03) can now build the `frontend` service.
</success_criteria>

<output>
Create `.planning/phases/157-deployment-presets-runbook-stretch/157-02-SUMMARY.md` when done.
</output>
