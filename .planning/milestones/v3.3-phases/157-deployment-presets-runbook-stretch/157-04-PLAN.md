---
phase: 157-deployment-presets-runbook-stretch
plan: 04
type: execute
wave: 3
depends_on: [157-01, 157-02, 157-03]
files_modified:
  - docs/OPERATOR.md
  - .planning/research/recovered/RECOVERED_VPS_Deployment_Guide.md
autonomous: true
requirements: [DEPLOY-01]

must_haves:
  truths:
    - "docs/OPERATOR.md is the day-0 stand-up-from-zero runbook that SUPERSEDES the recovered VPS guide (kept as history with a superseded-by header note; not deleted) and CROSS-LINKS (never duplicates) the 3 docs/DEPLOYMENT-*.md (D-07)"
    - "The one-box docker compose path is the canonical happy path; the managed Coolify+Vercel path is documented as a variant cross-linking DEPLOYMENT-PIPELINE.md (D-01)"
    - "OPERATOR.md BAKES IN the DEPLOYMENT-LESSONS fixes: A6 fresh-DB bootstrap (full-schema + the 9 seed migrations 010/018/053/056/061/066/087->088->089 + app_settings 'global'), A4 session pooler :5432, A5 rediss:// TLS, B1 multi-origin FRONTEND_URL, B2 docker.sock+keeper single-tenant, B3 model pinning, A3 Coolify, SEC-01 SECRETS_ENCRYPTION_KEY (D-08)"
    - "Presets use the 4-homes model — Enterprise = homes C/D (on-prem/BYO/local-GPU), scale is a dial inside a home, and the app's existing Ollama/LM Studio support means the on-prem story needs no new app code (D-04)"
    - "Homes B (one-box) and A (managed map) are fully documented; homes C/D ship as SHORT variant-delta sections with the full sealed air-gapped runbook explicitly deferred (D-05)"
    - "The docker.sock=host-root elevation-of-privilege risk is mitigated by the loudly-stated single-tenant-only constraint in OPERATOR.md"
  artifacts:
    - path: docs/OPERATOR.md
      provides: "the day-0 stand-up-from-zero operator runbook"
      contains: "SECRETS_ENCRYPTION_KEY"
    - path: .planning/research/recovered/RECOVERED_VPS_Deployment_Guide.md
      provides: "superseded-history header note (file preserved, not deleted)"
      contains: "superseded by docs/OPERATOR.md"
  key_links:
    - from: docs/OPERATOR.md
      to: docs/DEPLOYMENT-PIPELINE.md
      via: "managed-variant (home A) cross-link — accounts/architecture map"
      pattern: "DEPLOYMENT-PIPELINE"
    - from: docs/OPERATOR.md
      to: supabase/full-schema.sql
      via: "fresh-DB bootstrap section (A6) references full-schema + the 9 seed migrations"
      pattern: "full-schema"
---

<objective>
Author `docs/OPERATOR.md` — the day-0 "stand up from zero" runbook — and add a "superseded" header note to the recovered VPS guide (edit, do NOT delete). OPERATOR.md walks the canonical home-B one-box `docker compose` happy path (D-01), BAKES IN the real DEPLOYMENT-LESSONS fixes so an operator never re-hits them (D-08), CROSS-LINKS (never duplicates) the three existing `docs/DEPLOYMENT-*.md` (D-07), documents home A (managed) as a variant map, and ships homes C/D as SHORT variant-delta sections (D-04, D-05) with the full sealed air-gapped runbook explicitly deferred.

Purpose: The single front-door doc a new operator follows to a working production deployment. SC#2.
Output: `docs/OPERATOR.md` + the superseded-header edit on the recovered guide.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/157-deployment-presets-runbook-stretch/157-CONTEXT.md
@.planning/phases/157-deployment-presets-runbook-stretch/157-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- What OPERATOR.md documents + cross-links. Do NOT duplicate these docs — link them. -->
Cross-link targets (exist, keep single-sourced):
  - docs/DEPLOYMENT-PIPELINE.md  — accounts + architecture map (Vercel + Coolify + Supabase + Upstash; superrag.cloud). The home-A managed-variant link.
  - docs/DEPLOYMENT-WORKFLOW.md  — day-2 branch promotion (develop->master->production) + the cloud-parity checklist.
  - docs/DEPLOYMENT-LESSONS.md   — the failure modes A1-A7 / B1-B5 baked into the happy path here.
Superseded (edit, keep as history): .planning/research/recovered/RECOVERED_VPS_Deployment_Guide.md
Fresh-DB bootstrap (A6 — VERIFIED filenames): paste supabase/full-schema.sql (Run without RLS) THEN apply the 9 seed migrations
  010_app_settings.sql, 018_skill_creator_seed.sql, 053_settings_unification.sql, 056_workflow_definitions.sql,
  061_harness_seed_templates.sql, 066_eval_coverage_seed.sql, 087_skill_creator_reborn.sql, 088_..._sequencing.sql, 089_..._file_attach_honesty.sql
  (order matters ONLY within the trio 087->088->089) THEN `INSERT INTO app_settings (id) VALUES ('global') ON CONFLICT DO NOTHING;`
  THEN verify: SELECT count(*) FROM public.skills WHERE id='00000000-0000-0000-0000-000000000010' AND is_system=true;  -- expect 1
Baked-in lesson fixes: A4 POSTGRES_DSN session pooler :5432 IPv4 (not db.<ref> IPv6, not :6543 txn pooler); A5 rediss:// TLS for managed Redis;
  B1 FRONTEND_URL comma-split multi-origin; B2 sandbox docker.sock (Directories mount) + host-built agentic-rag-sandbox:101.1 + sandbox-image-keeper + GHCR durable fix; B3 pin known-good models per env; A3 Coolify base-dir /backend + Ports 8000 + Domain https:// (managed only); SEC-01 generate+set SECRETS_ENCRYPTION_KEY.
Home D no-new-code fact: backend/.env.example already supports LLM_PROVIDER=ollama + OLLAMA_BASE_URL + LMSTUDIO_BASE_URL + EMBEDDING_BASE_URL.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the home-B happy path + fresh-DB bootstrap + cross-links</name>
  <files>docs/OPERATOR.md</files>
  <read_first>
    - docs/OPERATOR.md (the net-new file to create)
    - docs/DEPLOYMENT-LESSONS.md § A6 (SOURCE OF TRUTH — the fresh-DB bootstrap gap + the app_settings 'global' fix)
    - supabase/full-schema.sql (header — confirm it is schema-only / MISSES seed rows) and supabase/SETUP.md § Path 2
    - docs/DEPLOYMENT-PIPELINE.md and docs/DEPLOYMENT-WORKFLOW.md (the two docs to CROSS-LINK, not duplicate)
    - .planning/phases/157-deployment-presets-runbook-stretch/157-RESEARCH.md § "Fresh-DB Bootstrap Sequence" (the verified 9-migration list + the verify SELECT)
  </read_first>
  <action>
    Create `docs/OPERATOR.md`. Open with the day-0 framing: this is the stand-up-from-zero runbook; it SUPERSEDES the recovered bare-VPS guide (link it, note it is kept as history); the 4-homes model (A managed / B one-box / C BYO-cloud / D on-prem) where "where each piece runs is configuration, not a code change" (D-01/D-04). Prerequisites section: Docker Engine + Compose v2, a Supabase cloud project, one provider API key. The canonical HOME-B HAPPY PATH: `git clone` -> `cp deploy/onebox.env.example ./.env` -> fill the DB URL/keys + one AI key + `SECRETS_ENCRYPTION_KEY` (include the Fernet generate one-liner) -> `docker compose -f docker-compose.prod.yml up -d --build` -> open the browser at the published nginx port. A FRESH-DB BOOTSTRAP section (bake in A6): paste `supabase/full-schema.sql` (Run without RLS) then apply the 9 seed migrations by exact filename (010, 018, 053, 056, 061, 066, then 087->088->089 IN ORDER — note order matters only within the trio) then run `INSERT INTO app_settings (id) VALUES ('global') ON CONFLICT DO NOTHING;` then the skill-creator verify SELECT (expect 1). A CROSS-LINKS section pointing to DEPLOYMENT-PIPELINE.md (accounts/architecture), DEPLOYMENT-WORKFLOW.md (day-2 promotion + parity), DEPLOYMENT-LESSONS.md (failure modes) — explicitly "see X, do not duplicate" (D-07). Reference RESEARCH for the exact migration filenames and SQL; do not paste full-schema contents.
  </action>
  <acceptance_criteria>
    - `docs/OPERATOR.md` exists with a home-B happy-path section containing the `cp deploy/onebox.env.example ./.env` step and the `docker compose -f docker-compose.prod.yml up` command.
    - Fresh-DB bootstrap section names all 9 seed migrations by filename and states the 087->088->089 order, plus the `INSERT INTO app_settings (id) VALUES ('global') ON CONFLICT DO NOTHING;` line and the verify SELECT.
    - Contains working relative links to all three: `DEPLOYMENT-PIPELINE.md`, `DEPLOYMENT-WORKFLOW.md`, `DEPLOYMENT-LESSONS.md` (link-resolve: each target file exists).
    - Contains the Fernet `SECRETS_ENCRYPTION_KEY` generate one-liner.
    - Does NOT duplicate the accounts map or the branch-promotion checklist (links out instead).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Bake in the remaining LESSONS fixes + managed (A) + variant-delta (C/D) sections + verification checklist</name>
  <files>docs/OPERATOR.md</files>
  <read_first>
    - docs/OPERATOR.md (the Task 1 output — extend it)
    - docs/DEPLOYMENT-LESSONS.md § A3/A4/A5/B1/B2/B3 (SOURCE OF TRUTH — the exact fixes to bake in)
    - docs/DEPLOYMENT-PIPELINE.md (the home-A managed map to cross-link)
    - backend/.env.example (confirm LLM_PROVIDER=ollama + OLLAMA_BASE_URL + EMBEDDING_BASE_URL exist — the home-D no-new-code deltas)
    - .planning/phases/157-deployment-presets-runbook-stretch/157-RESEARCH.md § "Security Domain" (the controls to encode) and § "Common Pitfalls 2/3/7/8"
  </read_first>
  <action>
    Extend `docs/OPERATOR.md`. A "gotchas baked in" / configuration section encoding the LESSONS fixes: A4 `POSTGRES_DSN` must use the Supabase SESSION pooler on :5432 (IPv4) — not direct db.<ref> (IPv6-only) nor :6543 (breaks asyncpg); A5 managed Redis must be `rediss://` TLS (Upstash); B1 `FRONTEND_URL` is comma-split multi-origin — list every live frontend origin; B3 pin known-good models per env (a model that works locally can 404 on cloud keys). A SECURITY section: SEC-01 generate + set `SECRETS_ENCRYPTION_KEY` (blank = plaintext + loud warning, malformed = refuse boot; per-env in Coolify for managed); B2 the sandbox needs the `/var/run/docker.sock` mount (Directories mount) + a HOST-built `agentic-rag-sandbox:101.1` image + the `sandbox-image-keeper` pin container, with GHCR/private-registry as the durable fix — and a LOUD callout that a mounted docker.sock = host root, so the one-box compose is SINGLE-TENANT ONLY (this is the T-157-EOP mitigation). A MANAGED VARIANT (home A) section: document-only, cross-linking DEPLOYMENT-PIPELINE.md, plus A3 (Coolify base-dir /backend + Ports Exposes 8000 + Domain https://) — managed only (D-01/D-05). SHORT VARIANT-DELTA sections for home C (BYO-cloud: env-delta note only) and home D (on-prem/local-GPU: the env deltas LLM_PROVIDER=ollama + OLLAMA_BASE_URL + EMBEDDING_BASE_URL + self-hosted Supabase URL + a local-GPU pointer) — NOT full runbooks; state that the full sealed air-gapped runbook is DEFERRED until a real buyer (D-04/D-05). Finish with a VERIFICATION CHECKLIST the operator runs on a real box (/health 200, frontend loads, login, one chat turn). Reference DEPLOYMENT-LESSONS.md and RESEARCH for exact values; keep managed/day-2 detail as links, not copies.
  </action>
  <acceptance_criteria>
    - Contains the A4 session-pooler `:5432` guidance (and warns against `:6543` / direct db.<ref>).
    - Contains `rediss://` (A5 managed Redis TLS) and multi-origin `FRONTEND_URL` (B1).
    - Contains a LOUD single-tenant-only callout tied to the `docker.sock` = host-root risk (B2 / T-157-EOP) AND the `sandbox-image-keeper` + GHCR note.
    - Contains `SECRETS_ENCRYPTION_KEY` set/generate guidance (SEC-01) and the A3 Coolify base-dir `/backend` + Ports `8000` note under the managed variant.
    - Home A section is document-only and cross-links `DEPLOYMENT-PIPELINE.md`; homes C and D are SHORT variant-delta sections (home D lists `LLM_PROVIDER=ollama` + `OLLAMA_BASE_URL` + `EMBEDDING_BASE_URL`) and the file states the full air-gapped runbook is deferred.
    - Contains an operator verification checklist (/health, frontend, login, one chat turn).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add the superseded header note to the recovered guide + validate cross-links</name>
  <files>.planning/research/recovered/RECOVERED_VPS_Deployment_Guide.md</files>
  <read_first>
    - .planning/research/recovered/RECOVERED_VPS_Deployment_Guide.md (SOURCE — add a header note at the very top; do NOT delete or rewrite the body)
    - docs/OPERATOR.md (the superseding doc the note points to)
  </read_first>
  <action>
    Edit `.planning/research/recovered/RECOVERED_VPS_Deployment_Guide.md` to add a short note block at the very top: this guide is SUPERSEDED by `docs/OPERATOR.md` (the containerized `docker compose` runbook) and is kept only as historical reference — do not follow the host-nginx/systemd/venv/WinSCP steps for a new deploy. Do NOT delete the file or alter the historical body below the note. Then run the doc cross-link static check across OPERATOR.md and this note.
  </action>
  <acceptance_criteria>
    - The recovered guide file STILL EXISTS (not deleted) and its historical body is intact.
    - The recovered guide's top contains a note with the literal string `superseded by docs/OPERATOR.md`.
    - Cross-link check green: every `docs/DEPLOYMENT-*.md` link in OPERATOR.md resolves to an existing file; OPERATOR.md's only reference to the recovered guide is the explicit supersede note (no instruction to follow it).
    - `docs/OPERATOR.md` contains the baked-in tokens: `full-schema`, `5432`, `rediss://`, `FRONTEND_URL`, `single-tenant`, `SECRETS_ENCRYPTION_KEY` (grep each — all present).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator following the runbook → production posture | OPERATOR.md's instructions ARE the security posture for a self-hosted box (secrets, CORS, transport, sandbox isolation) |
| single-tenant box → multi-tenant misuse | The docker.sock mount is safe only if the box is single-tenant; the doc must make that boundary explicit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-157-EOP | Elevation of privilege | documented docker.sock mount | mitigate | HIGH item — mitigation is the LOUD single-tenant-only constraint stated in OPERATOR.md (B2); GHCR/private-registry noted as the durable image fix. Modeled mitigated (the doc constraint IS the control). |
| T-157-KEY | Information disclosure | provider keys at rest | mitigate | SEC-01 — runbook instructs generating + setting `SECRETS_ENCRYPTION_KEY` (Fernet, Phase 150); documents blank=plaintext+warn, malformed=refuse boot |
| T-157-COR | Spoofing / DoS | CORS origins | mitigate | B1 — runbook prescribes exact comma-split multi-origin `FRONTEND_URL` listing every live frontend origin |
| T-157-TLS | Information disclosure | transport to managed Redis / DB | mitigate | A5 `rediss://` TLS for managed Redis; A4 session pooler; cloud Supabase `https://`; managed Coolify domain `https://` (A3) |
| T-157-RED | Tampering | Redis exposure | mitigate | Runbook notes the bundled redis publishes NO host port; never expose 6379 publicly without `--requirepass` |
| T-157-SC | Tampering | package/image installs | accept | Docs-only plan — authors no packages/images; slopcheck N/A |
</threat_model>

<verification>
- `docs/OPERATOR.md` exists; cross-links to the 3 DEPLOYMENT-*.md resolve; baked-in tokens all present (full-schema, 5432, rediss://, FRONTEND_URL, single-tenant, SECRETS_ENCRYPTION_KEY).
- Recovered guide preserved with the `superseded by docs/OPERATOR.md` note; body intact.
- Homes B + A fully documented; C/D are short variant deltas; air-gapped runbook marked deferred.
</verification>

<success_criteria>
`docs/OPERATOR.md` is the canonical day-0 runbook: home-B happy path + fresh-DB bootstrap + baked-in A3/A4/A5/A6/B1/B2/B3/SEC-01 fixes + the loud single-tenant docker.sock constraint + managed (A) variant map + short C/D variant deltas + a verification checklist + cross-links (never duplicates) to the 3 DEPLOYMENT-*.md. The recovered VPS guide is preserved with a superseded-by header note. SC#2 met.
</success_criteria>

<output>
Create `.planning/phases/157-deployment-presets-runbook-stretch/157-04-SUMMARY.md` when done.
</output>
