---
phase: 157-deployment-presets-runbook-stretch
plan: 05
type: execute
wave: 4
depends_on: [157-03, 157-04]
files_modified: []
autonomous: false
requirements: [DEPLOY-01]

must_haves:
  truths:
    - "Following OPERATOR.md with the onebox preset produces a working deployment: a LOCAL docker compose -f docker-compose.prod.yml up of the one-box preset (local Supabase via host.docker.internal + bundled redis) serves /health 200, the nginx frontend loads, login works, and one chat turn completes (D-09)"
    - "The docker.sock=host-root risk is inherited-mitigated for the smoke because it runs on a local single-tenant box, and the chat turn is a NON-code prompt so the host sandbox image is not required (Pitfall 7)"
  artifacts:
    - path: docker-compose.prod.yml
      provides: "the all-in-one stack the smoke exercises end-to-end (the only end-to-end test of the new compose file)"
      contains: "backend"
  key_links:
    - from: browser
      to: backend /health
      via: "docker compose up + curl through nginx /api/ and direct :8000"
      pattern: "health"
---

<objective>
Run the D-09 smoke — the ONLY end-to-end exercise of the new compose file (SC#3). The operator runs a LOCAL `docker compose -f docker-compose.prod.yml up --build` of the one-box preset pointed at their LOCAL Supabase (via `host.docker.internal`) + the bundled redis; Claude verifies `/health` returns 200; the operator confirms the nginx frontend loads, login works, and one non-code chat turn completes. This is operator-gated because it requires running Docker locally and confirming a real browser chat turn.

Purpose: Prove the runbook + preset + compose actually stand up a working app (SC#3). The live box uses `backend/Dockerfile` via Coolify, not this compose — so this smoke is the only thing that exercises the compose file end-to-end.
Output: A green smoke recorded in the SUMMARY (no source files modified).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/157-deployment-presets-runbook-stretch/157-CONTEXT.md
@.planning/phases/157-deployment-presets-runbook-stretch/157-VALIDATION.md
@.planning/phases/157-deployment-presets-runbook-stretch/157-RESEARCH.md

<interfaces>
<!-- The smoke contract. VERIFIED from source. -->
/health: backend/app/main.py:577-590 — GET /health, no auth, returns 200 {"status":"ok","redis":"ok"|"unreachable","maintenance":<bool>}.
Reachable directly at http://localhost:8000/health (backend port publish) AND through nginx at http://localhost:8080/api/health.
Local test login (dev app): fhdmrd@gmail.com / 123456.
Landmines to watch (from RESEARCH):
  - Pitfall 1: VITE_* are build-time -> the frontend must be (re)built with `--build`; a plain `up` won't pick up changed VITE_* values.
  - Pitfall 2: host.docker.internal needs `extra_hosts: host.docker.internal:host-gateway` (already in the backend service) on Linux.
  - Pitfall 3: browser sees local Supabase at 127.0.0.1:54321 (VITE build arg) while the backend container uses host.docker.internal:54321/54322.
  - Pitfall 7: make the one chat turn a NON-code prompt so the host-built sandbox image is not required.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: D-09 one-box compose smoke (operator-run)</name>
  <read_first>
    - docker-compose.prod.yml (the stack under test)
    - deploy/onebox.env.example (the preset the operator copies to ./.env)
    - docs/OPERATOR.md § the home-B happy path + verification checklist (the runbook being validated)
    - .planning/phases/157-deployment-presets-runbook-stretch/157-VALIDATION.md § "Manual-Only Verifications" (the authoritative D-09 step list)
  </read_first>
  <action>
    Operator-gated verification — NO source files are modified in this task. Claude drives the D-09 smoke: it walks the operator through the numbered steps in `<how-to-verify>` (copy `deploy/onebox.env.example` to the gitignored root `./.env`, fill the local-Supabase URL split + one provider key + a generated `SECRETS_ENCRYPTION_KEY`, then `docker compose -f docker-compose.prod.yml up --build`), runs the `/health` curls itself (direct `:8000` and through nginx `:8080/api/health`), and records the operator's confirmation of the frontend load + login + one non-code chat turn. Watch the Pitfall-1/2/3/7 landmines named in `<interfaces>`. Do not treat this as complete until the operator types "approved".
  </action>
  <what-built>
    The all-in-one `docker-compose.prod.yml` stack (frontend nginx + backend uvicorn via backend/Dockerfile + bundled ephemeral redis), the `deploy/onebox.env.example` preset, and the `docs/OPERATOR.md` runbook. This checkpoint validates them end-to-end (SC#3) — nothing here is auto-verifiable; it needs the operator to run Docker and confirm a live browser chat turn.
  </what-built>
  <how-to-verify>
    1. Ensure LOCAL Supabase is running (`supabase start`; API :54321, Postgres :54322) with the schema present (existing dev DB is fine; a truly fresh DB would follow OPERATOR.md's bootstrap section).
    2. `cp deploy/onebox.env.example ./.env` at the repo root, then fill:
       - Backend vantage: `SUPABASE_URL=http://host.docker.internal:54321`, `POSTGRES_DSN=postgresql://postgres:postgres@host.docker.internal:54322/postgres`.
       - Browser vantage (build arg): `VITE_SUPABASE_URL=http://127.0.0.1:54321`, `VITE_SUPABASE_ANON_KEY=<local anon key>`, `VITE_API_BASE_URL=/api` (Pitfall 3 split).
       - One provider key (e.g. `OPENAI_API_KEY=...`) and a generated `SECRETS_ENCRYPTION_KEY` (the Fernet one-liner from the preset header).
    3. Operator runs: `docker compose -f docker-compose.prod.yml up --build` (the `--build` matters — Pitfall 1).
    4. Claude verifies the backend: `curl http://localhost:8000/health` AND `curl http://localhost:8080/api/health` -> both expect HTTP 200 with `"status":"ok"` (and `"redis":"ok"` once the bundled redis is healthy).
    5. Operator opens the nginx frontend at http://localhost:8080, logs in with the test creds (fhdmrd@gmail.com / 123456), sends ONE NON-code chat turn (e.g. a plain question — no `execute_code`, so the host sandbox image is not required — Pitfall 7), and confirms a streamed reply renders.
    6. Green when all of: /health 200, frontend loads, login works, one chat turn streams a reply.
  </how-to-verify>
  <acceptance_criteria>
    - `curl http://localhost:8000/health` returns HTTP 200 with `"status":"ok"`.
    - `curl http://localhost:8080/api/health` (through nginx) returns HTTP 200 (proves the `/api/` trailing-slash proxy + SSE-friendly config works).
    - The nginx frontend serves the SPA at http://localhost:8080 (index.html loads, not a blank/placeholder — proves the VITE_* build args reached the bundle, Pitfall 1).
    - Login succeeds with the test creds (proves the browser-vantage Supabase URL is correct).
    - One non-code chat turn returns a streamed reply through nginx (proves the end-to-end path: nginx -> backend -> redis run buffer -> SSE back, unbuffered).
    - Operator confirms with "approved".
  </acceptance_criteria>
  <resume-signal>Operator types "approved" (SC#3 satisfied), or reports which step failed (e.g. blank frontend => VITE_* not rebuilt; backend can't reach Supabase => host-gateway/URL split) for gap-closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local smoke box → data | The smoke runs on a local single-tenant machine against local Supabase + throwaway `./.env`; no production secrets or tenants involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-157-EOP | Elevation of privilege | docker.sock mount during smoke | mitigate | Inherited: the smoke runs on a LOCAL single-tenant box (the exact constraint OPERATOR.md documents); non-code chat turn means `execute_code`/sandbox isn't even invoked |
| T-157-CFG | Information disclosure | the smoke `./.env` | mitigate | `./.env` is gitignored and holds only LOCAL Supabase creds + a throwaway provider key; never committed, never production |
| T-157-SC | Tampering | package/image installs | accept | No packages/images authored; the smoke only builds the repo's own Dockerfiles + official base images |
</threat_model>

<verification>
- `/health` returns 200 `"status":"ok"` both directly (:8000) and through nginx (:8080/api/health).
- Frontend loads, login works, one non-code chat turn streams a reply.
- Operator "approved" recorded — SC#3 satisfied and the D-09 smoke is the phase gate before `/gsd:verify-work`.
</verification>

<success_criteria>
The one-box compose stack stands up locally from the preset + runbook and serves a working app: /health 200, nginx frontend loads, login succeeds, one chat turn completes. SC#3 proven by the operator-run smoke (the only end-to-end exercise of `docker-compose.prod.yml`).
</success_criteria>

<output>
Create `.planning/phases/157-deployment-presets-runbook-stretch/157-05-SUMMARY.md` when done.
</output>
