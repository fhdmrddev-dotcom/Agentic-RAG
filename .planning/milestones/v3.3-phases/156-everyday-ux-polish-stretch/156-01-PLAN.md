---
phase: 156-everyday-ux-polish-stretch
plan: 01
type: execute
wave: 0
depends_on: []
use_worktrees: false
autonomous: true
requirements: [POLISH-01]
files_modified:
  - frontend/src/lib/threadGroups.ts
  - frontend/src/lib/__tests__/threadGroups.test.ts
  - frontend/src/components/layout/__tests__/NavPanel.test.tsx
  - frontend/src/components/layout/__tests__/ChatHistoryColumn.test.tsx
  - frontend/src/components/layout/__tests__/ChatHistoryColumn.a11y.test.tsx
  - frontend/src/components/layout/__tests__/ThreadCommandPalette.test.tsx

must_haves:
  truths:
    - "threadGroups.ts exports the SHARED matchesTitle predicate, bucketFor/groupByDate date bucketing (Today/Yesterday/Last 7 days/Last 30 days/Older, empty-fold, within-bucket updated_at DESC), a folderLabel helper, and a HighlightTitle helper that renders JSX text nodes — the one engine reused by the column, the ⌘K palette, and the mobile search (SC#2/SC#3, D-03/D-04)."
    - "The XSS control (V5) ORIGINATES here: HighlightTitle escapes via React text nodes, NEVER dangerouslySetInnerHTML; threadGroups.test.ts proves an HTML-payload title (`<img onerror=...>`) renders as inert text, not an element (hard-directive 4, T-156-01)."
    - "The four component test files (NavPanel, ChatHistoryColumn, ChatHistoryColumn.a11y, ThreadCommandPalette) exist as GREEN it.todo contracts naming every assertion Waves 1-2 will implement — the Nyquist Wave-0 targets so no downstream task ships without an automated verify."
  artifacts:
    - path: "frontend/src/lib/threadGroups.ts"
      provides: "shared date-bucketing + title-filter + safe-highlight engine"
      exports: ["bucketFor", "groupByDate", "matchesTitle", "HighlightTitle", "folderLabel", "DateBucket"]
      min_lines: 45
    - path: "frontend/src/lib/__tests__/threadGroups.test.ts"
      provides: "bucket-boundary + fold + DESC + filter + XSS-inert unit coverage"
      contains: "dangerouslySetInnerHTML"
    - path: "frontend/src/components/layout/__tests__/ChatHistoryColumn.test.tsx"
      provides: "SC#2 inline-filter + D-09 preserved-row it.todo contract"
      contains: "it.todo"
  key_links:
    - from: "frontend/src/lib/threadGroups.ts"
      to: "safe match highlight"
      via: "HighlightTitle JSX <mark> text nodes"
      pattern: "<mark"
---

<objective>
Wave 0 — build the ONE shared pure module every later wave transforms `threads` through, and scaffold the four component test files as green `it.todo` contracts so every Wave 1-2 implementation task has a real automated verify target (Nyquist).

Purpose: `threadGroups.ts` is the shared engine (date buckets + title filter + safe highlight). Front-loading it — with its full unit test — means the column, the ⌘K palette, and the mobile search all consume ONE tested predicate, and the XSS control (JSX text-node highlight) is built and proven before any user-controlled title is ever rendered.
Output: `threadGroups.ts` (+ test, green) and four `it.todo` test scaffolds (green) under `components/layout/__tests__/`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/156-everyday-ux-polish-stretch/156-CONTEXT.md
@.planning/phases/156-everyday-ux-polish-stretch/156-RESEARCH.md
@.planning/phases/156-everyday-ux-polish-stretch/156-VALIDATION.md
@.planning/sketches/078-chat-history-home/index.html
@frontend/src/types/index.ts
@frontend/src/setupTests.ts
@frontend/src/components/relationships/RelationshipsSection.a11y.test.tsx

<interfaces>
<!-- The Thread fields this module reads (from frontend/src/types/index.ts). No codebase exploration needed. -->
Thread { id: string; title: string; folder_id: string | null; updated_at: string; ... }
Folder { id: string; name: string; ... }

<!-- Sketch source-of-truth for labels/boundaries (index.html): -->
- relDate / groupByDate at index.html:245-247 → bucket LABELS + boundaries: Today / Yesterday / Last 7 days / Last 30 days / Older
- filt at index.html:380-392 → the vanilla `innerHTML` highlight — DO NOT port literally (XSS); reimplement as JSX (Pattern 4)
- .th-empty at index.html:68-69,265 → the honest empty-state string "No chats match your search."

<!-- vitest-axe matcher is globally wired (setupTests.ts:6-8: expect.extend(axeMatchers)); axe idiom in RelationshipsSection.a11y.test.tsx:16,101-106. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build threadGroups.ts (shared bucket + filter + safe highlight) with its unit test</name>
  <files>frontend/src/lib/threadGroups.ts, frontend/src/lib/__tests__/threadGroups.test.ts</files>
  <read_first>
    - 156-RESEARCH.md § "Pattern 3: Hand-rolled date bucketing" (bucketFor/groupByDate/startOfDay, within-bucket DESC — Pitfall 3) and § "Pattern 4: Shared live filter + safe match-highlight" (matchesTitle + HighlightTitle)
    - .planning/sketches/078-chat-history-home/index.html lines 245-266 (relDate/groupByDate/rowCompact — the label + boundary source of truth) and 380-392 (the vanilla `filt`/`innerHTML` highlight this module REPLACES with JSX)
    - frontend/src/types/index.ts (the `Thread` shape — `title`, `updated_at`, `folder_id`; and `Folder.name`)
    - 156-RESEARCH.md § "Security Domain" (V5 — the one real control) and § Pitfall 2 (XSS via match-highlight)
  </read_first>
  <behavior>
    - `bucketFor(updatedAtISO, now=new Date())`: calendar-day-aware via a local `startOfDay` (NOT raw 24h). days<=0 → "Today"; days===1 → "Yesterday"; days<=7 → "Last 7 days"; days<=30 → "Last 30 days"; else "Older".
    - `groupByDate(threads, now?)`: returns ordered [{label, items}] in ORDER Today→Older, DROPS empty buckets, and sorts items WITHIN each bucket by `updated_at` DESC (defensive — do not trust API order, Pitfall 3).
    - `matchesTitle(t, q)`: true when q is blank OR `t.title.toLowerCase()` includes `q.trim().toLowerCase()` (case-insensitive substring).
    - `HighlightTitle({title, query})`: returns a React fragment; blank/no-match query → plain `{title}`; a match → `title.slice(0,i)` + a `<mark>` around `title.slice(i,i+len)` + the remainder — all as JSX text nodes (React auto-escapes each segment). NEVER dangerouslySetInnerHTML.
    - `folderLabel(folders, folderId)`: the folder name; else "Unfiled" when folderId is null, else "Folder" (unknown id).
    - XSS proof: a thread titled `<img src=x onerror=alert(1)>` filtered/highlighted renders that string as visible TEXT — no `<img>` element is created in the DOM.
  </behavior>
  <action>
    Create `frontend/src/lib/threadGroups.ts`. Export a `DateBucket` union type and a private `ORDER: DateBucket[]`. Implement `startOfDay(d: Date): number`, `bucketFor(updatedAtISO: string, now?: Date): DateBucket`, `groupByDate(threads: Thread[], now?: Date): {label: DateBucket; items: Thread[]}[]`, `matchesTitle(t: Thread, q: string): boolean`, `folderLabel(folders: Folder[], folderId: string | null): string`, and a `HighlightTitle` React component `({ title, query }: { title: string; query: string }) => JSX.Element`. `HighlightTitle` uses `<mark className="bg-primary/25 text-foreground rounded-[3px] px-px">` around the matched slice (Deep Midnight token, matching the sketch's `mark` at index.html:58) and renders every segment as a plain string child — do NOT use dangerouslySetInnerHTML anywhere in this file. Import `Thread`/`Folder` from `@/types`. Then create `frontend/src/lib/__tests__/threadGroups.test.ts` covering: each bucket boundary (a thread at 0/1/7/8/30/31 days ago via a fixed `now`), empty-bucket fold, within-bucket DESC ordering, `matchesTitle` case-insensitivity + blank-passthrough, `folderLabel` all three branches, and the XSS-inert assertion (render `<HighlightTitle title="<img src=x onerror=alert(1)>" query="img" />` with `@testing-library/react` and assert the container has no `<img>` element and the raw text is present, plus a `<mark>` wraps "img"). Also assert the file contains no `dangerouslySetInnerHTML` (a source-level grep in the test or a documented acceptance grep).
  </action>
  <acceptance_criteria>
    - [test-command] `cd frontend && npx vitest run src/lib/__tests__/threadGroups.test.ts` → all green.
    - [behavior] `bucketFor` returns Today/Yesterday/Last 7 days/Last 30 days/Older exactly at the 0/1/≤7/≤30/>30 calendar-day boundaries (proven by fixed-`now` cases).
    - [behavior] `groupByDate` omits empty buckets and orders items within a bucket newest-`updated_at` first.
    - [source] `grep -c dangerouslySetInnerHTML frontend/src/lib/threadGroups.ts` == 0; `<mark` is present in threadGroups.ts.
    - [behavior] the XSS test renders NO `<img>` element for a title of `<img src=x onerror=alert(1)>` — the payload is inert text.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/__tests__/threadGroups.test.ts</automated>
  </verify>
  <done>threadGroups.ts exports bucketFor/groupByDate/matchesTitle/HighlightTitle/folderLabel; threadGroups.test.ts is green including the XSS-inert `<img onerror>` case; no dangerouslySetInnerHTML in the module.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold the four component test files as green it.todo contracts</name>
  <files>frontend/src/components/layout/__tests__/NavPanel.test.tsx, frontend/src/components/layout/__tests__/ChatHistoryColumn.test.tsx, frontend/src/components/layout/__tests__/ChatHistoryColumn.a11y.test.tsx, frontend/src/components/layout/__tests__/ThreadCommandPalette.test.tsx</files>
  <read_first>
    - 156-VALIDATION.md § "Per-Task Verification Map" + § "Wave 0 Requirements" (the exact behaviors each file must eventually assert)
    - 156-RESEARCH.md § "Validation Architecture → Phase Requirements → Test Map" and § "Wave 0 Gaps"
    - frontend/src/components/layout/__tests__/ChatLayoutLaunch.test.tsx (the `vi.hoisted` mock + `vi.mock("../NavPanel")` idiom Waves 1-2 will reuse)
    - frontend/src/components/relationships/RelationshipsSection.a11y.test.tsx:16,101-106 (the `axe(container) → toHaveNoViolations()` idiom the a11y file will use)
  </read_first>
  <action>
    Create the four test files as `describe`/`it.todo(...)` scaffolds. IMPORTANT: do NOT import the not-yet-existent components (`ChatHistoryColumn`, `ThreadCommandPalette`, or the refactored `NavPanel` render) — `it.todo` takes only a string, so the files collect GREEN as pending todos until Waves 1-2 replace each todo with a live test. Each todo names the exact assertion.
    `NavPanel.test.tsx` (SC#1 + D-07): todos for — renders a New Chat button reachable by role name `/new chat/i`; renders each `navItems` entry as a tooltip-wrapped icon button; renders NO operator shield when `isOperator=false`; renders the amber operator shield (name `/control room/i`) when `isOperator=true`; renders NO "Collapse navigation"/"Expand navigation" toggle (collapse machinery removed); renders WITHOUT a StreamsProvider (rail is stream-free post-refactor).
    `ChatHistoryColumn.test.tsx` (SC#2 inline + SC#3 + D-09): todos for — renders threads grouped by date with group headers + counts; typing in the "Filter this list…" input narrows rows to title-substring matches; a matched substring is wrapped in `<mark>`; groups with no match fold away; the honest empty-state "No chats match your search." shows when nothing matches; a folder chip (or "Unfiled") renders per row; the options menu opens Rename + Delete; Delete opens the "Delete thread?" confirm; the SEED-064 running dot + Stop appear for a streaming thread; inline rename commits on Enter and cancels on Escape.
    `ChatHistoryColumn.a11y.test.tsx` (A11Y-01 do-no-harm): todos for — `axe(container)` reports no violations across populated / empty / filtered states; every row is a real `<button>`; the row actions container is CSS-gated on `group-focus-within` (revealed by keyboard focus, never render-gated).
    `ThreadCommandPalette.test.tsx` (SC#2 ⌘K): todos for — the palette exposes `role="dialog"` with an accessible name; typing filters the list; ArrowDown/ArrowUp move the active option (aria-selected / aria-activedescendant); Enter calls `onSelectThread` + navigates to chat + closes; Escape closes and restores focus; the results expose `role="listbox"` / `role="option"`; NO `cmdk` import.
    End each file with a header comment: "// Wave 1/2 replaces each it.todo with a live render test importing the component."
  </action>
  <acceptance_criteria>
    - [test-command] `cd frontend && npx vitest run src/components/layout/__tests__/NavPanel.test.tsx src/components/layout/__tests__/ChatHistoryColumn.test.tsx src/components/layout/__tests__/ChatHistoryColumn.a11y.test.tsx src/components/layout/__tests__/ThreadCommandPalette.test.tsx` → 0 failures (all reported as todo/pending).
    - [source] none of the four files import a not-yet-existent component (`grep -E "from \"\.\./(ChatHistoryColumn|ThreadCommandPalette)\"" ...` == 0); each file contains `it.todo`.
    - [behavior] the four files collectively enumerate every SC#1/SC#2/SC#3 + D-07/D-09/A11Y-01 assertion listed in 156-VALIDATION.md.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/layout/__tests__/NavPanel.test.tsx src/components/layout/__tests__/ChatHistoryColumn.test.tsx src/components/layout/__tests__/ChatHistoryColumn.a11y.test.tsx src/components/layout/__tests__/ThreadCommandPalette.test.tsx</automated>
  </verify>
  <done>The four component test files exist under components/layout/__tests__/ as green it.todo contracts; the target vitest run reports 0 failures and no imports of not-yet-existent components.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-controlled thread title → rendered DOM | Thread titles are auto-generated from prompts or user-renamed; the highlight helper defined here renders them |

This is pure-client code over already-authenticated, already-RLS-scoped data. No endpoint, query, auth path, or session is introduced (Architectural Responsibility Map: Browser/Client only).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-01 | Tampering (stored XSS) | `HighlightTitle` in `threadGroups.ts` | mitigate | Render each highlighted segment as a JSX text node (React auto-escapes); NEVER dangerouslySetInnerHTML. `threadGroups.test.ts` asserts an `<img onerror>` title is inert text. This is the ONE real control and it is built here. |
| T-156-04 | Info Disclosure | `matchesTitle` predicate | accept | Operates only over the caller's own already-loaded, RLS-scoped `threads`; adds no fetch and no cross-user data. Client is not a trust boundary (ASVS V4 N/A). |
| T-156-SC | Tampering (supply chain) | npm installs | mitigate | NO package is installed this phase (⌘K is hand-rolled on the existing `@radix-ui/react-dialog`); supply-chain surface is unchanged. No install task exists in any plan. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lib/__tests__/threadGroups.test.ts` green.
- `cd frontend && npx vitest run src/components/layout/__tests__/` shows the four new files collecting with 0 failures.
- `cd frontend && npm run build` (`tsc -b && vite build`) → the new `.ts`/`.tsx` files type-check with **no net-new tsc errors beyond the pre-existing baseline** (30 errors / 17 files as of 2026-07-16 HEAD `98c77718`, all unrelated to this phase; do NOT edit unrelated baseline files to force a literal exit 0).
</verification>

<success_criteria>
- `threadGroups.ts` exports bucketFor/groupByDate/matchesTitle/HighlightTitle/folderLabel and is fully unit-tested including the XSS-inert case.
- The four component test files exist as green `it.todo` contracts matching 156-VALIDATION.md's Wave-0 list.
- No new npm dependency; no `dangerouslySetInnerHTML`; no `checkpoint:*` task.
</success_criteria>

<output>
Create `.planning/phases/156-everyday-ux-polish-stretch/156-01-SUMMARY.md` when done.
</output>
