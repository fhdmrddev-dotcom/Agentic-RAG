---
phase: 156-everyday-ux-polish-stretch
plan: 02
type: execute
wave: 1
depends_on: ["156-01"]
use_worktrees: false
autonomous: true
requirements: [POLISH-01]
files_modified:
  - frontend/src/components/layout/ChatHistoryColumn.tsx
  - frontend/src/components/layout/__tests__/ChatHistoryColumn.test.tsx
  - frontend/src/components/layout/__tests__/ChatHistoryColumn.a11y.test.tsx
  - frontend/src/components/layout/NavPanel.tsx
  - frontend/src/components/layout/__tests__/NavPanel.test.tsx
  - frontend/src/components/layout/ChatLayout.tsx

must_haves:
  truths:
    - "The left nav is a permanent ~58px icon rail (icons + tooltips) that can NEVER hide New Chat on any view — SC#1 satisfied by construction (D-01, D-02, D-07); the nav_panel_collapsed/isCollapsed collapse machinery and w-64/w-16 masking are removed (D-07)."
    - "A dedicated full-height chat-history column owns the thread list on the chat view, so rail growth can no longer starve the list — this structurally closes BUG-260711-01 crowding (D-01, D-10)."
    - "Typing in the column's 'Filter this list…' box narrows rows to title-substring matches with a highlighted match, folds empty groups, and shows the honest empty-state 'No chats match your search.' — SC#2 inline tier (D-03)."
    - "Threads are grouped Today / Yesterday / Last 7 days / Last 30 days / Older with counts and empty buckets hidden; each row carries a folder chip (or Unfiled) — SC#3 (D-04, date default)."
    - "Every moved row behavior is preserved intact — select, inline rename, delete-confirm AlertDialog, SEED-064 running dot + Stop + ActiveRunsTray, options menu, folder-scoped New Chat, and the Phase-155 A11Y-01 keyboard reveal (real <button> rows; actions on group-hover AND group-focus-within, never render-gated) — D-09, do-no-harm."
    - "The operator shield stays probe-gated and OUTSIDE NAV_ITEMS (nav-items.test.ts stays green) and NavPanel.tsx keeps its filename (the ChatLayoutLaunch vi.mock('../NavPanel') stays intact) — D-07, do-no-harm."
    - "The loadThreads() bootstrap effect moves UP to ChatLayout (always mounted) so the whole app owns one thread list — the SC#2 ⌘K palette in Wave 2 is never empty on non-chat views (RESEARCH Pitfall 1)."
  artifacts:
    - path: "frontend/src/components/layout/ChatHistoryColumn.tsx"
      provides: "the dedicated chat-history column (lifted rows + inline filter + date groups + folder chip)"
      contains: "group-focus-within:opacity-100"
      min_lines: 170
    - path: "frontend/src/components/layout/NavPanel.tsx"
      provides: "the permanent 58px icon rail (logo + New + navItems + footer icons)"
      contains: "w-[58px]"
      min_lines: 120
    - path: "frontend/src/components/layout/ChatLayout.tsx"
      provides: "composition root — rail + history column + lifted loadThreads bootstrap"
      contains: "ChatHistoryColumn"
  key_links:
    - from: "frontend/src/components/layout/ChatLayout.tsx"
      to: "ChatHistoryColumn"
      via: "conditional mount only on the chat view"
      pattern: "activeView === \"chat\""
    - from: "frontend/src/components/layout/ChatHistoryColumn.tsx"
      to: "frontend/src/lib/threadGroups.ts"
      via: "groupByDate + matchesTitle + HighlightTitle import"
      pattern: "groupByDate|matchesTitle|HighlightTitle"
    - from: "frontend/src/components/layout/ChatLayout.tsx"
      to: "loadThreads"
      via: "lifted bootstrap useEffect with the 2s retry guard"
      pattern: "loadThreads\\(\\)\\.catch"
    - from: "frontend/src/components/layout/NavPanel.tsx"
      to: "operator shield"
      via: "isOperator gate OUTSIDE navItems.map"
      pattern: "isOperator &&"
---

<objective>
Wave 1 — deliver all three Success Criteria as one atomic IA refactor (Sketch 078-D). Split the single collapsible `NavPanel` into (a) a permanent thin icon rail and (b) a new dedicated `ChatHistoryColumn` that owns the thread list with an inline filter and date grouping; compose both in `ChatLayout` and lift the `loadThreads` bootstrap up so the whole app shares one thread list.

Purpose: This is a MOVE + wrap, not a rewrite (D-09). The row logic is lifted VERBATIM from `NavPanel.renderThreadList()`; the risk is preserving behavior (A11Y-01, SEED-064), not building it. After this plan, SC#1 (New Chat always reachable), SC#2 inline (list search), and SC#3 (date grouping) are ALL true, and BUG-260711-01 crowding is structurally fixed (D-10).
Output: `ChatHistoryColumn.tsx` (+ its two live tests), the repurposed `NavPanel.tsx` rail (+ its live test), and the rewired `ChatLayout.tsx`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/156-everyday-ux-polish-stretch/156-CONTEXT.md
@.planning/phases/156-everyday-ux-polish-stretch/156-RESEARCH.md
@.planning/sketches/078-chat-history-home/index.html
@.planning/reported-bugs/chat-list-too-narrow-nav-panel-crowding.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
@frontend/src/components/layout/NavPanel.tsx
@frontend/src/components/layout/ChatLayout.tsx
@frontend/src/lib/threadGroups.ts
@frontend/src/components/chat/ActiveRunsTray.tsx
@frontend/src/lib/nav-items.ts
@frontend/src/lib/nav-items.test.ts
@frontend/src/components/layout/__tests__/ChatLayoutLaunch.test.tsx

<interfaces>
<!-- threadGroups (Plan 01 output, frontend/src/lib/threadGroups.ts): -->
bucketFor(updatedAtISO, now?) → DateBucket
groupByDate(threads, now?) → { label: DateBucket; items: Thread[] }[]   // empty buckets dropped, within-bucket updated_at DESC
matchesTitle(t: Thread, q: string) → boolean
folderLabel(folders: Folder[], folderId: string | null) → string        // name | "Unfiled" | "Folder"
HighlightTitle({ title, query }) → JSX                                    // JSX text nodes, NEVER dangerouslySetInnerHTML

<!-- useThreads() (frontend/src/hooks/useThreads.ts) — all CRUD already exists: -->
{ threads, selectedThread, loadThreads, selectThread, newThread(folderId?), deleteThread(id), renameThread(id,title), updateThreadTitle }

<!-- NavPanel current Props to SHRINK (RESEARCH Pattern 1) — drop: threads, selectedThread, onSelectThread,
     loadThreads, onDeleteThread, onRenameThread, folders. KEEP: activeView, onNavigate, navItems, isOperator,
     onNewThread, onSignOut, theme, onToggleTheme. -->

<!-- ChatHistoryColumn NEW Props: threads, selectedThread, onSelectThread, onNewThread(folderId?), onDeleteThread,
     onRenameThread, folders. (onOpenPalette?: () => void is added in Wave 2 — leave a placeholder now.) -->

<!-- Deep Midnight tokens already used by NavPanel (reuse verbatim): bg-sidebar, border-border/20, gradient-primary,
     bg-primary/15 text-primary, text-muted-foreground, hover:text-sidebar-foreground, hover:bg-accent/40,
     focus-visible:ring-2 focus-visible:ring-primary/30, ghost-border, bg-popover. Sketch geometry: rail 58px, history-col ~300px. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ChatHistoryColumn.tsx by lifting renderThreadList VERBATIM and wrapping it with the filter + date groups</name>
  <files>frontend/src/components/layout/ChatHistoryColumn.tsx, frontend/src/components/layout/__tests__/ChatHistoryColumn.test.tsx, frontend/src/components/layout/__tests__/ChatHistoryColumn.a11y.test.tsx</files>
  <read_first>
    - frontend/src/components/layout/NavPanel.tsx — the exact regions to LIFT: `renderThreadList()` (:125-283, the rows), the "Chats" header + `ActiveRunsTray` + New (+) + folder-picker toggle + select (:370-429), the delete `AlertDialog` (:434-459), the row state (:93-99: menuOpenId/editingId/editValue/deleteConfirmId/showFolderPicker/selectedFolderId + editInputRef), the editing focus effect (:108-110), and the startRename/commitRename fns (:112-122). The SEED-064 imports (:29-31) and A11Y-01 CSS-gated actions block (:220-225) MUST come along verbatim.
    - frontend/src/lib/threadGroups.ts — matchesTitle / groupByDate / HighlightTitle / folderLabel (Plan 01)
    - .planning/sketches/078-chat-history-home/index.html — the column shell: `.history-col` (:88-93), `.hc-top`/`.hc-title` h2 "Chats" + New (:90-92,352), `.search` filter input (:31-37,353), `.hc-list` (:93), the `.grp`/`.grp-head`/`.grp-count`/`.row`/`.r-meta` group+row shapes (:40-58), `.th-empty` (:68-69,265)
    - frontend/src/components/chat/ActiveRunsTray.tsx — moves with the list (it also pulls StreamsProvider)
    - The Wave-0 ChatHistoryColumn.test.tsx + ChatHistoryColumn.a11y.test.tsx it.todo contracts (Plan 01) — flesh these out here
  </read_first>
  <action>
    Create `ChatHistoryColumn.tsx` exporting `ChatHistoryColumn(props)` with Props { threads, selectedThread, onSelectThread, onNewThread, onDeleteThread, onRenameThread, folders }. LIFT the entire row implementation from `NavPanel.renderThreadList()` (:138-278) VERBATIM — do NOT restyle the rows (D-09): keep the `group relative` wrapper, the `isSelected`/`isEditing`/`isMenuOpen`/`isRunning` branches, the active-indicator gradient bar, the primary `<button>` title row, the SEED-064 resting running dot (:201-212) with its `group-hover:opacity-0 group-focus-within:opacity-0` gating, the ALWAYS-rendered CSS-gated actions block (:220-251) with `opacity-0 group-hover:opacity-100 group-focus-within:opacity-100` + the Stop `<button>` + the options `<button>`, and the dropdown menu (:256-277). Bring across the row state (menuOpenId/editingId/editValue/deleteConfirmId + editInputRef), the editing focus `useEffect`, `startRename`/`commitRename`, the delete `AlertDialog` (:434-459), and the SEED-064 imports `useStreamingThreadIds`/`useStreamActions` + `ActiveRunsTray`. WRAP it: container className `hidden md:flex flex-col w-[300px] shrink-0 bg-sidebar border-r border-border/20` (mobile still uses the drawer). Header `hc-top`: an `<h2>Chats</h2>` plus the lifted New (+) button (`onNewThread(selectedFolderId)`) and the lifted folder-scope picker toggle + `<select>` (keep the `selectedFolderId`/`showFolderPicker` state). Below the header, a controlled filter `<input>` (`query` state, a lucide `Search` icon, placeholder "Filter this list…", the sketch `.search:focus-within` ring via `focus-within:ring-2 focus-within:ring-primary/30`); leave a comment `{/* ⌘K chip added in Wave 2 (Plan 03) */}` where the chip goes — do NOT wire an onClick to a palette that does not exist yet. Then REPLACE the flat `threads.map` with: `const filtered = threads.filter(t => matchesTitle(t, query))`, iterate `groupByDate(filtered)`, and for each group render a `.grp-head` (label + a mono count pill, tokens per the sketch mapped to `text-muted-foreground`/`bg-muted`) followed by that group's rows via the lifted row renderer — but swap the raw `{thread.title}` for `<HighlightTitle title={thread.title} query={query} />` and add a folder chip via `folderLabel(folders, thread.folder_id)` on each row (the sketch `.r-meta`, replacing the bare FolderIcon). When `filtered.length === 0`, render the honest empty-state "No chats match your search." Do NOT use dangerouslySetInnerHTML anywhere. Then flesh out the two test files (replace the Plan-01 it.todo entries with live tests): mock `@/providers/StreamsProvider` (useStreamingThreadIds → a controllable Set, useStreamActions → { stopThread: vi.fn() }, getActiveRunStartMs → a stub) so `ActiveRunsTray` + the dot render without a real provider (RESEARCH Pitfall 5); render `<ChatHistoryColumn>` with mock threads/folders and assert the SC#2/SC#3/D-09 behaviors and (in the a11y file) `expect(await axe(container)).toHaveNoViolations()` across populated / empty / filtered plus that rows are real buttons and the actions block carries `group-focus-within`.
  </action>
  <acceptance_criteria>
    - [test-command] `cd frontend && npx vitest run src/components/layout/__tests__/ChatHistoryColumn.test.tsx src/components/layout/__tests__/ChatHistoryColumn.a11y.test.tsx` → all green (todos replaced by live tests).
    - [behavior] Typing "revenue" filters to titles containing "revenue"; the match is wrapped in `<mark>`; groups with no match disappear; clearing the box restores all groups.
    - [behavior] Grouping shows Today/Yesterday/Last 7 days/Last 30 days/Older headers with counts; empty buckets absent.
    - [behavior] Options → Rename/Delete; Delete opens "Delete thread?"; a streaming thread shows the SEED-064 dot + a Stop button; a folder chip (or "Unfiled") shows per row.
    - [source] `grep -c "group-focus-within:opacity-100" ChatHistoryColumn.tsx` >= 1 (A11Y-01 reveal preserved); `grep -c "useStreamingThreadIds" ChatHistoryColumn.tsx` >= 1 (SEED-064 preserved); `grep -c dangerouslySetInnerHTML ChatHistoryColumn.tsx` == 0; `w-[300px]` present.
    - [behavior] a11y: axe reports no violations in populated/empty/filtered; every row is a `<button>`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/layout/__tests__/ChatHistoryColumn.test.tsx src/components/layout/__tests__/ChatHistoryColumn.a11y.test.tsx</automated>
  </verify>
  <done>ChatHistoryColumn.tsx renders the lifted rows + inline filter + date groups + folder chip; both column tests green; the A11Y-01 group-focus-within reveal and SEED-064 dot+Stop are preserved; no dangerouslySetInnerHTML.</done>
</task>

<task type="auto">
  <name>Task 2: Repurpose NavPanel.tsx into the permanent icon rail and flesh out NavPanel.test.tsx</name>
  <files>frontend/src/components/layout/NavPanel.tsx, frontend/src/components/layout/__tests__/NavPanel.test.tsx</files>
  <read_first>
    - frontend/src/components/layout/NavPanel.tsx — the full file (know precisely what to DELETE vs KEEP)
    - 156-RESEARCH.md § "Pattern 1: Repurpose NavPanel into a permanent icon rail" — the moves-out / stays / deleted lists, the prop change, the rail nav-item snippet, and the rail container class; § Pitfall 4 (shield) + Pitfall 6 (keep the filename)
    - .planning/sketches/078-chat-history-home/index.html — the rail: `.rail` (:76-77), `.rail-nav`/`.rail-btn`/`.rail-btn.active`/`.tip` tooltip (:78-87), the logo (:274,349), and the New (+) in the header context (:277,352)
    - frontend/src/components/ui/tooltip.tsx — the `side="right"` / `delayDuration={0}` idiom (already used in NavPanel:353-357)
    - frontend/src/lib/nav-items.test.ts — the D-07 lock this refactor must keep GREEN
    - The Wave-0 NavPanel.test.tsx it.todo contract (Plan 01)
  </read_first>
  <action>
    DELETE the collapse machinery: the `isCollapsed` state + `handleToggle` (:76-86), the toggle `<button>` (:301-310), the `nav_panel_collapsed` localStorage read/write, the `w-64`/`w-16` + opacity/`pointer-events-none` masking (:287-297,365-369), and the now-unused `PanelLeftClose`/`PanelLeftOpen` imports. DELETE the thread region entirely (it moved to ChatHistoryColumn in Task 1): `renderThreadList` (:125-283), the "Chats" header + `ActiveRunsTray` + New + folder picker (:370-429), the delete `AlertDialog` (:434-459), the `loadThreads` bootstrap effect (:101-106, moves to ChatLayout in Task 3), the editing state + `startRename`/`commitRename` + `editInputRef` + editing effect, and the `useStreamingThreadIds`/`useStreamActions` + `ActiveRunsTray` imports. SHRINK the Props interface to { activeView, onNavigate, navItems, isOperator, onNewThread, onSignOut, theme, onToggleTheme } — drop threads/selectedThread/onSelectThread/loadThreads/onDeleteThread/onRenameThread/folders. REBUILD the render as a permanent rail: root `<div className="hidden md:flex flex-col items-center h-full w-[58px] shrink-0 bg-sidebar border-r border-border/20 py-3">`. Top: the logo (gradient-primary Sparkles, ALWAYS visible — no opacity gate). Then a New Chat (+) icon `<button>` wrapped in a `side="right"` Tooltip (label "New chat", lucide `Plus`, onClick `() => { onNewThread(); onNavigate("chat") }` — D-02, reachable from every view). Then `navItems.map` rendering EACH entry as a Tooltip(`side="right"`, `delayDuration={0}`)-wrapped icon `<button>` (aria-current when `activeView === view`; className `flex items-center justify-center w-10 h-10 rounded-lg ... focus-visible:ring-2 focus-visible:ring-primary/30`, active `bg-primary/15 text-primary` else `text-muted-foreground hover:text-sidebar-foreground hover:bg-accent/40`; Icon `w-5 h-5`) — ALWAYS tooltip-wrapped now (no isCollapsed branch). Footer at `mt-auto`: the theme toggle, the probe-gated operator shield (KEEP it `isOperator &&` and OUTSIDE `navItems` — amber `Shield`, tooltip "Control Room", active-highlight on `control-room`), and sign out — each a `side="right"` Tooltip icon button, preserving the existing aria-labels. Keep the export name `NavPanel` and the filename `NavPanel.tsx` (Pitfall 6). Then flesh out NavPanel.test.tsx: render `<NavPanel>` inside a `<TooltipProvider>` (tooltips need it) with minimal props and NO StreamsProvider (proving the rail is stream-free); assert the New Chat button by role+name `/new chat/i`; assert nav icon buttons render; assert the shield is ABSENT with `isOperator={false}` and PRESENT (name `/control room/i`) with `isOperator={true}`; assert no "Collapse navigation"/"Expand navigation" control exists.
  </action>
  <acceptance_criteria>
    - [test-command] `cd frontend && npx vitest run src/components/layout/__tests__/NavPanel.test.tsx src/lib/nav-items.test.ts` → all green (D-07 lock intact).
    - [source] `grep -c nav_panel_collapsed NavPanel.tsx` == 0; `grep -c isCollapsed NavPanel.tsx` == 0; `grep -c renderThreadList NavPanel.tsx` == 0; `grep -c useStreamingThreadIds NavPanel.tsx` == 0; `w-[58px]` present.
    - [behavior] NavPanel renders WITHOUT a StreamsProvider (the test wraps only in TooltipProvider and does not throw).
    - [behavior] the New Chat (+) is present on the rail regardless of activeView; the operator shield renders only when `isOperator` and is NOT part of `navItems`.
    - [source] the file is still named `NavPanel.tsx` and exports `NavPanel`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/layout/__tests__/NavPanel.test.tsx src/lib/nav-items.test.ts</automated>
  </verify>
  <done>NavPanel.tsx is a permanent 58px icon rail (collapse machinery + thread region removed, New Chat + present, shield isOperator-gated OUTSIDE navItems); NavPanel.test green (renders sans StreamsProvider) and nav-items.test green.</done>
</task>

<task type="auto">
  <name>Task 3: Compose the rail + history column in ChatLayout.tsx and lift the loadThreads bootstrap up</name>
  <files>frontend/src/components/layout/ChatLayout.tsx</files>
  <read_first>
    - frontend/src/components/layout/ChatLayout.tsx — the `<NavPanel .../>` call site (:206-222), the flex root (:205), the mobile drawer (:234-340, untouched here), the chat grid (:342-357), the activeView branches (:376-437)
    - NavPanel's shrunk Props (Task 2) and ChatHistoryColumn's Props (Task 1)
    - 156-RESEARCH.md § Pitfall 1 (lift loadThreads WITH the retry-once-after-2s guard, do NOT put it in the chat-only column), § Pitfall 5 (ChatLayoutLaunch stays green: activeView="workflows" → column not mounted, NavPanel stubbed), § Pitfall 7 (rail + column are `shrink-0` fixed widths; chat grid stays `flex-1 min-w-0` → no horizontal overflow)
    - frontend/src/components/layout/__tests__/ChatLayoutLaunch.test.tsx — must stay green; do not change its mock or the props it passes
  </read_first>
  <action>
    Add the bootstrap effect to ChatLayout (lifted from the old NavPanel:101-106): `useEffect(() => { loadThreads().catch(() => setTimeout(() => loadThreads().catch(console.error), 2000)) }, [loadThreads])` — this is now the single app-wide thread load (Pitfall 1). Update the `<NavPanel>` call site to the SHRUNK props only: activeView, onNavigate, navItems, isOperator, onNewThread={newThread}, onSignOut, theme, onToggleTheme — REMOVE threads/selectedThread/onSelectThread/loadThreads/onDeleteThread/onRenameThread/folders from that call site. Insert `<ChatHistoryColumn>` immediately after `<NavPanel>` and before the mobile-drawer/chat-grid, rendered ONLY when `activeView === "chat"` (mirror the chat-only mount) — pass threads, selectedThread, onSelectThread={selectThread}, onNewThread={newThread}, onDeleteThread={deleteThread}, onRenameThread={renameThread}, folders. Keep the root `flex h-screen`; ChatHistoryColumn is `hidden md:flex ... shrink-0` (mobile drawer unchanged) and the chat grid keeps `flex-1 min-w-0` so the three desktop columns never overflow (Pitfall 7 — the chat `main` simply narrows). Do not touch the mobile drawer (that is Plan 04) or the activeView page branches. Re-run ChatLayoutLaunch.test.tsx to confirm it stays green (it renders at activeView="workflows" with NavPanel stubbed and threads:[] → ChatHistoryColumn does not mount, so no StreamsProvider is needed).
  </action>
  <acceptance_criteria>
    - [test-command] `cd frontend && npx vitest run src/components/layout/__tests__/ChatLayoutLaunch.test.tsx` → green (no StreamsProvider regression, no prop-shape break).
    - [test-command] `cd frontend && npm run build` (`tsc -b && vite build`) → the NavPanel↔ChatHistoryColumn↔ChatLayout prop-contract change compiles with **0 net-new tsc errors beyond the pre-existing baseline** (30 errors / 17 files as of 2026-07-16 HEAD `98c77718`; the only baseline errors in touched files are `NavPanel.tsx:3` unused-`Button` TS6133 — expected to clear when the rail refactor drops that import — and `ChatLayoutLaunch.test.tsx:126` TS2740 mock rot; do NOT edit unrelated baseline files to force a literal exit 0).
    - [source] ChatHistoryColumn is mounted only under `activeView === "chat"`; the lifted `loadThreads().catch` bootstrap effect lives in ChatLayout; the `<NavPanel>` call site no longer passes `threads`/`onDeleteThread`/`onRenameThread`/`loadThreads`/`folders`.
    - [behavior] BUG-260711-01 (D-10): a full-height ChatHistoryColumn now sits between the thin rail and the chat grid — nav growth cannot shrink the thread list.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/layout/__tests__/ChatLayoutLaunch.test.tsx</automated>
    <!-- Machine-gate is the reliably-green vitest run. The `npm run build` type-check is the caveated acceptance-criteria [test-command] above — the executor runs it and interprets vs. the documented 30-error baseline (do NOT chain `&& npm run build` here: it hard-exits non-zero on the pre-existing baseline and would false-fail the gate on an unattended run — Phase-155 precedent: run tsc, compare to baseline, proceed on 0 net-new). -->
  </verify>
  <done>ChatLayout renders the rail + the chat-only ChatHistoryColumn and owns the lifted loadThreads bootstrap (2s retry); ChatLayoutLaunch stays green; npm run build shows no net-new tsc errors beyond the documented 30-error baseline (do not fix unrelated files) across the prop-contract change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-controlled thread title → rendered rows | The column renders every thread title (and its filtered highlight) |
| client render ↔ RLS-scoped data | `threads`/`folders` are already owner/RLS-scoped by the backend; the client is not a trust boundary |

Pure-client IA refactor (Architectural Responsibility Map: Browser/Client only). No endpoint, query, migration, auth path, or session is added or changed — any task reaching for a backend/DB change is out of scope (D-03/D-09).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-01 | Tampering (stored XSS) | `ChatHistoryColumn` row title + filter highlight | mitigate | Titles render via `HighlightTitle` (JSX text nodes, Plan 01) — never dangerouslySetInnerHTML. Acceptance greps `dangerouslySetInnerHTML == 0` in ChatHistoryColumn.tsx. |
| T-156-02 | Info Disclosure | inline filter over "loaded threads" | accept | The column filters ONLY the caller's own already-loaded RLS-scoped `threads`; no new fetch, no cross-user data. ASVS V4 access-control N/A — the client is not the trust boundary. |
| T-156-05 | Elevation of Privilege | operator shield in the rail | mitigate | Shield stays `isOperator`-gated and OUTSIDE `NAV_ITEMS`; `nav-items.test.ts` fails the build if a control-room entry is ever added (D-07, Pitfall 4). |
| T-156-SC | Tampering (supply chain) | npm installs | mitigate | No package installed (no `cmdk`); supply-chain surface unchanged. No install task. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/layout/__tests__/ChatHistoryColumn.test.tsx src/components/layout/__tests__/ChatHistoryColumn.a11y.test.tsx src/components/layout/__tests__/NavPanel.test.tsx src/components/layout/__tests__/ChatLayoutLaunch.test.tsx src/lib/nav-items.test.ts` — all green.
- `cd frontend && npm run build` (`tsc -b && vite build`) → no net-new tsc errors beyond the documented 30-error baseline (do not fix unrelated files); `npm run lint:a11y` (Phase-155 gate) exit 0.
- Live Chrome-MCP lived-experience UAT (jsdom cannot prove layout): with 20+ threads the column shows many rows at rest; visiting Documents/Settings/Workflows and back never hides New Chat; date groups render with counts; select/rename/delete-confirm/SEED-064 dot+Stop/options all still work; keyboard-Tab reaches Stop/Rename/Delete on a focused row; no horizontal overflow at ~800px with the workspace panel open.
</verification>

<success_criteria>
- SC#1: New Chat is reachable on EVERY view (rail (+) + column header "New") — no state hides it.
- SC#2 (inline): the column filter narrows rows by title substring with a highlighted match and honest empty-state.
- SC#3: rows are grouped by date (Today→Older) with counts, folder chip per row.
- D-09/A11Y-01/SEED-064 preserved; D-07 shield lock + nav-items.test green; BUG-260711-01 structurally relieved (D-10).
</success_criteria>

<output>
Create `.planning/phases/156-everyday-ux-polish-stretch/156-02-SUMMARY.md` when done.
</output>
