---
phase: 156-everyday-ux-polish-stretch
plan: 03
type: execute
wave: 2
depends_on: ["156-01", "156-02"]
use_worktrees: false
autonomous: true
requirements: [POLISH-01]
files_modified:
  - frontend/src/components/layout/ThreadCommandPalette.tsx
  - frontend/src/components/layout/__tests__/ThreadCommandPalette.test.tsx
  - frontend/src/components/layout/ChatLayout.tsx
  - frontend/src/components/layout/ChatHistoryColumn.tsx

must_haves:
  truths:
    - "Pressing ⌘K / Ctrl+K from ANY view opens a global command palette over the whole loaded backlog (not just the chat column) — SC#2 ⌘K tier (D-03); the shortcut mirrors the existing ⌘. idiom (e.key.toLowerCase() === 'k', preventDefault)."
    - "The palette filters by title as you type, ↑↓ move the active option, ↵ opens the thread and lands on chat, Esc closes and restores focus — hand-rolled listbox roving on top of the existing @radix-ui/react-dialog (D-05 fallback, NOT cmdk)."
    - "No new npm dependency is installed — the palette is hand-rolled on the already-present ui/dialog.tsx (Radix Dialog gives focus-trap + Esc + aria-modal + scroll-lock + focus-restore for free); NO checkpoint:human-verify, NO install task (hard-directive 1)."
    - "The palette is NOT a dependency of any Success Criterion — all three SCs are already TRUE after Wave 1; ⌘K is the additive cut-line (D-06)."
    - "The palette is StreamsProvider-free and reads the ChatLayout-owned threads (the Plan-02 lifted loadThreads), so it is never empty on non-chat views (RESEARCH Pitfall 1); highlighted titles render as JSX text nodes, never dangerouslySetInnerHTML (T-156-01)."
  artifacts:
    - path: "frontend/src/components/layout/ThreadCommandPalette.tsx"
      provides: "the hand-rolled ⌘K palette (Dialog + role=listbox/option + roving keyboard)"
      contains: "role=\"listbox\""
      min_lines: 110
    - path: "frontend/src/components/layout/ChatLayout.tsx"
      provides: "the global ⌘K keydown + the palette mounted once at the layout root"
      contains: "ThreadCommandPalette"
  key_links:
    - from: "frontend/src/components/layout/ChatLayout.tsx"
      to: "ThreadCommandPalette"
      via: "paletteOpen state + the ⌘K window keydown"
      pattern: "toLowerCase\\(\\) === \"k\""
    - from: "frontend/src/components/layout/ChatHistoryColumn.tsx"
      to: "ChatLayout paletteOpen"
      via: "the ⌘K chip in the filter box calls onOpenPalette"
      pattern: "onOpenPalette"
    - from: "frontend/src/components/layout/ThreadCommandPalette.tsx"
      to: "frontend/src/lib/threadGroups.ts"
      via: "matchesTitle + groupByDate + HighlightTitle import"
      pattern: "matchesTitle|groupByDate|HighlightTitle"
---

<objective>
Wave 2 — add the global ⌘K / Ctrl+K command palette that searches ALL loaded threads and jumps anywhere, HAND-ROLLED on the existing Radix Dialog (no `cmdk`). Mount it once at the ChatLayout root so it is reachable from every view, and wire the ⌘K chip inside the Wave-1 filter box.

Purpose: The column filters what you are looking at; ⌘K jumps anywhere (Sketch 078-D). This is the designated cut-line (D-06): the three SCs already shipped in Wave 1, so ⌘K is purely additive. Per hard-directive 1 this is an unattended run — we hand-roll the palette on `ui/dialog.tsx` (which already gives focus-trap, Esc, aria-modal, scroll-lock, and focus-restore) so there is NO `cmdk` install and therefore NO install-gating `checkpoint:human-verify` to stall the pipeline. (156-CONTEXT D-05 preferred `cmdk`; D-05 itself names this exact Radix-Dialog hand-roll as the sanctioned fallback, and 156-RESEARCH § Alternatives confirms the app already ships a listbox/aria-activedescendant pattern to mirror.)
Output: `ThreadCommandPalette.tsx` (+ its live test), the ⌘K keydown + palette mount in `ChatLayout.tsx`, and the ⌘K chip in `ChatHistoryColumn.tsx`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/156-everyday-ux-polish-stretch/156-CONTEXT.md
@.planning/phases/156-everyday-ux-polish-stretch/156-RESEARCH.md
@.planning/sketches/078-chat-history-home/index.html
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
@frontend/src/components/ui/dialog.tsx
@frontend/src/lib/threadGroups.ts
@frontend/src/components/layout/ChatLayout.tsx
@frontend/src/components/layout/ChatHistoryColumn.tsx
@frontend/src/components/layout/__tests__/ChatLayoutLaunch.test.tsx

<interfaces>
<!-- ui/dialog.tsx (frontend/src/components/ui/dialog.tsx) — the Radix base that gives focus-trap/Esc/aria-modal/
     scroll-lock/focus-restore for free. Compose the palette on these; do NOT hand-roll the overlay/focus-trap: -->
Dialog (Root, controlled open/onOpenChange), DialogPortal, DialogOverlay, DialogContent, DialogTitle, DialogClose

<!-- threadGroups (Plan 01): matchesTitle(t,q), groupByDate(threads) → [{label, items}], HighlightTitle({title,query}),
     folderLabel(folders, folderId). Feed the palette the SAME predicate + grouping as the inline filter (fidelity). -->

<!-- ChatLayout owns (from Plan 02): threads, selectThread, onNavigate, and the app-wide loadThreads bootstrap.
     The existing ⌘. window keydown to MIRROR is at ChatLayout.tsx:188-197. -->

<!-- ChatHistoryColumn (Plan 02) already renders a filter box with a `{/* ⌘K chip added in Wave 2 */}` placeholder. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ThreadCommandPalette.tsx hand-rolled on ui/dialog.tsx (role=listbox/option + roving keyboard) and flesh out its test</name>
  <files>frontend/src/components/layout/ThreadCommandPalette.tsx, frontend/src/components/layout/__tests__/ThreadCommandPalette.test.tsx</files>
  <read_first>
    - frontend/src/components/ui/dialog.tsx — the primitives (Dialog/DialogContent/DialogOverlay/DialogTitle); Radix provides focus-trap, Esc-to-close, aria-modal, scroll-lock, and focus-restore — do NOT re-implement any of those
    - frontend/src/lib/threadGroups.ts — matchesTitle / groupByDate / HighlightTitle / folderLabel (Plan 01)
    - .planning/sketches/078-chat-history-home/index.html — the palette shell: `.cmdk` (:130-132), `.cmdk-search` input (:133-136,365), `.cmdk-results` (:137,366), `.cmdk-foot` ↑↓/↵/esc hints (:139-140,367), and the `role="dialog" aria-label` at :334,364; the shared `groupsHTML` reused for both (:336,366)
    - 156-RESEARCH.md § "Pattern 5" (adapt to the hand-rolled Dialog path — NOT cmdk) and § "Anti-Patterns"; hard-directive 1 (the exact hand-roll contract: role=listbox/option or roving-tabindex, ↑↓ moves the active option, ↵ selects → selectThread + navigate, Esc closes)
    - The Wave-0 ThreadCommandPalette.test.tsx it.todo contract (Plan 01)
  </read_first>
  <action>
    Create `ThreadCommandPalette.tsx` exporting `ThreadCommandPalette(props)` with Props { open, onOpenChange, threads, onSelectThread, onNavigate }. Compose on the controlled `<Dialog open={open} onOpenChange={onOpenChange}>` from `ui/dialog.tsx`. Render a custom top-anchored content (either a styled `<DialogContent>` or a hand-composed `DialogPortal`+`DialogOverlay`+`DialogPrimitive.Content`) sized like the sketch `.cmdk` (`w-[min(600px,88%)]`, `max-h-[520px]`, `bg-popover`/surface, Deep Midnight tokens, top offset ~74px). Inside: a visually-hidden `<DialogTitle className="sr-only">Search all chats</DialogTitle>` (Radix requires a titled dialog for its accessible name). A search `<input>` (controlled `q` state, autofocus, placeholder `Search all ${threads.length} chats…`, `role="combobox"`, `aria-expanded`, `aria-controls={listId}`, `aria-activedescendant={activeOptionId}`, a lucide `Search` icon). A results `<ul id={listId} role="listbox">`: compute `const filtered = threads.filter(t => matchesTitle(t, q))`; iterate `groupByDate(filtered)`; render a group heading (label · count) then each thread as `<li id={optionId(t.id)} role="option" aria-selected={idx === activeIndex}>` containing `<HighlightTitle title={t.title} query={q} />` + a folder chip via `folderLabel`. When `filtered.length === 0`, render the honest empty-state "No chats match your search." A `.cmdk-foot` with "↑↓ navigate · ↵ open · esc close" hints. HAND-OWN the roving keyboard only: keep a flat ordered array of the visible option thread-ids + an `activeIndex` state (reset to 0 whenever `q` changes); the input's `onKeyDown` handles ArrowDown/ArrowUp (move + clamp activeIndex; do not let the caret scroll the page — preventDefault) and Enter (select the active thread → `onSelectThread(t)` + `onNavigate("chat")` + `onOpenChange(false)`); a click on an option selects it the same way. Do NOT hand-roll Esc, the overlay, the focus-trap, or focus-restore — Radix Dialog already does them. Do NOT import `useStreamingThreadIds`/`useStreamActions` (keep the palette StreamsProvider-free so it mounts at the ChatLayout root and renders in tests without a provider). Do NOT import `cmdk`. Do NOT use dangerouslySetInnerHTML. Then flesh out ThreadCommandPalette.test.tsx (replace the Plan-01 todos): render with `open={true}` + mock threads/handlers (no provider needed); assert `getByRole("dialog")` has an accessible name (`/search all chats/i`); typing narrows the options; ArrowDown updates `aria-activedescendant`/`aria-selected`; Enter fires `onSelectThread` + `onNavigate("chat")` + `onOpenChange(false)`; the empty query with a no-match string shows "No chats match your search."; `role="listbox"` + `role="option"` are present; re-rendering with `open={false}` removes the dialog content.
  </action>
  <acceptance_criteria>
    - [test-command] `cd frontend && npx vitest run src/components/layout/__tests__/ThreadCommandPalette.test.tsx` → all green.
    - [behavior] Typing filters options; ArrowDown/ArrowUp move the active option; Enter selects it → `onSelectThread` + `onNavigate("chat")` + `onOpenChange(false)`; the empty-state string renders on no match.
    - [source] `grep -c "from \"cmdk\"" ThreadCommandPalette.tsx` == 0 (no cmdk); `grep -c useStreamingThreadIds ThreadCommandPalette.tsx` == 0 (StreamsProvider-free); `grep -c dangerouslySetInnerHTML ThreadCommandPalette.tsx` == 0; `role="listbox"` and `role="option"` present; a `sr-only` `DialogTitle` "Search all chats" present.
    - [behavior] a11y: the palette exposes `role="dialog"` (aria-modal via Radix) with a named title, and `role="listbox"`/`role="option"` for results.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/layout/__tests__/ThreadCommandPalette.test.tsx</automated>
  </verify>
  <done>ThreadCommandPalette.tsx is a hand-rolled Dialog palette with role=listbox/option + roving ArrowUp/Down + Enter-selects + a sr-only DialogTitle; its test is green; no cmdk import, no StreamsProvider, no dangerouslySetInnerHTML.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the global ⌘K keydown + palette mount in ChatLayout, and the ⌘K chip in ChatHistoryColumn</name>
  <files>frontend/src/components/layout/ChatLayout.tsx, frontend/src/components/layout/ChatHistoryColumn.tsx</files>
  <read_first>
    - frontend/src/components/layout/ChatLayout.tsx — the ⌘. window keydown to mirror (:188-197), the root `<div className="flex h-screen ...">` (:205), the lifted loadThreads bootstrap (Plan 02), the threads/selectThread/onNavigate it already owns
    - 156-RESEARCH.md § "Pattern 5 → ⌘K listener" (`(e.metaKey || e.ctrlKey) && e.key.toLowerCase() === "k"` → preventDefault + toggle) and § Pitfall 1 (the palette reads the ChatLayout-owned threads so it is never empty off-chat)
    - frontend/src/components/layout/ChatHistoryColumn.tsx — the Plan-02 filter box with the `{/* ⌘K chip added in Wave 2 */}` placeholder
    - .planning/sketches/078-chat-history-home/index.html — the `.cmdk-chip` inside `.search` (:147-149,353) and the global keydown (:411-418)
    - frontend/src/components/layout/__tests__/ChatLayoutLaunch.test.tsx — must stay green (the real palette will mount closed with threads:[] → inert)
  </read_first>
  <action>
    In ChatLayout: add `const [paletteOpen, setPaletteOpen] = useState(false)`. Add a window keydown `useEffect` mirroring the ⌘. idiom but matching `(e.metaKey || e.ctrlKey) && e.key.toLowerCase() === "k"` → `e.preventDefault(); setPaletteOpen(v => !v)` (register/cleanup the listener; empty/stable deps). Render `<ThreadCommandPalette open={paletteOpen} onOpenChange={setPaletteOpen} threads={threads} onSelectThread={selectThread} onNavigate={onNavigate} />` at the ChatLayout root, OUTSIDE the `activeView` switch (so it is reachable on every view — the Plan-02 lifted loadThreads keeps `threads` populated globally, Pitfall 1). Thread an `onOpenPalette={() => setPaletteOpen(true)}` prop down to `<ChatHistoryColumn>` (add it to the call site). In ChatHistoryColumn: add `onOpenPalette?: () => void` to Props (OPTIONAL so the Plan-02 tests keep passing) and replace the Wave-2 chip placeholder inside the filter box with a `<button>` (the sketch `.cmdk-chip` — a small kbd-hint "⌘K", `title="Search all chats (⌘K)"`, `aria-label="Search all chats"`) that calls `onOpenPalette?.()`. Do not change the inline filter behavior. Leave the mobile drawer for Plan 04.
  </action>
  <acceptance_criteria>
    - [test-command] `cd frontend && npx vitest run src/components/layout/__tests__/ChatLayoutLaunch.test.tsx src/components/layout/__tests__/ChatHistoryColumn.test.tsx` → green (no regression; the real palette mounts closed + inert with threads:[]).
    - [test-command] `cd frontend && npm run build` (`tsc -b && vite build`) → **0 net-new tsc errors beyond the pre-existing baseline** (30 errors / 17 files as of 2026-07-16 HEAD `98c77718`, all unrelated to this phase — e.g. `ChatLayoutLaunch.test.tsx:126` TS2740 is pre-existing mock rot; do NOT edit unrelated baseline files to force a literal exit 0).
    - [source] ChatLayout contains the ⌘K keydown (`e.key.toLowerCase() === "k"`) and mounts `<ThreadCommandPalette` at the root OUTSIDE the activeView switch; the ⌘K chip in ChatHistoryColumn calls `onOpenPalette`.
    - [behavior] the palette reads ChatLayout-owned `threads` (Pitfall 1 — not empty on Documents/Settings/Workflows).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/layout/__tests__/ChatLayoutLaunch.test.tsx src/components/layout/__tests__/ChatHistoryColumn.test.tsx</automated>
    <!-- Machine-gate is the reliably-green vitest run. The `npm run build` type-check is the caveated acceptance-criteria [test-command] above — executor runs it and interprets vs. the documented 30-error baseline (do NOT chain `&& npm run build` here: it hard-exits non-zero on the pre-existing baseline and would false-fail the gate on an unattended run). -->
  </verify>
  <done>ChatLayout owns paletteOpen + the (meta||ctrl)+k keydown and mounts the palette at the root outside the activeView switch; the ChatHistoryColumn Ctrl+K chip opens it; ChatLayoutLaunch + column tests green; npm run build shows no net-new tsc errors beyond the documented 30-error baseline (do not fix unrelated files).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-controlled thread title → palette results | The palette renders every loaded thread title + its filtered highlight |
| keyboard/global shortcut → modal focus | ⌘K opens a modal over any view; focus management crosses here |

Pure-client overlay over the caller's own already-loaded, RLS-scoped `threads` (Architectural Responsibility Map: Browser/Client only). No new endpoint, query, or backend search — the "search all" set is the caller's own threads, already client-side.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-01 | Tampering (stored XSS) | palette result title + highlight | mitigate | Titles render via `HighlightTitle` JSX text nodes (Plan 01) — never dangerouslySetInnerHTML (acceptance greps == 0 in ThreadCommandPalette.tsx). |
| T-156-03 | Denial of Service / focus-trap correctness | the modal focus-trap + Esc + restore | mitigate | Delegated to the vendored, battle-tested `@radix-ui/react-dialog` (already in the tree) — the hand-rolled part is only the inert listbox roving; Radix owns focus-trap/Esc/scroll-lock/restore. |
| T-156-02 | Info Disclosure | "search all threads" set | accept | The palette adds NO fetch; it only ever holds the caller's own RLS-scoped threads (ASVS V4 N/A — client not a trust boundary). |
| T-156-SC | Tampering (supply chain) | npm installs | mitigate | NO new dependency — the palette is hand-rolled on the existing `@radix-ui/react-dialog`; supply-chain surface unchanged. NO install task and NO `checkpoint:human-verify` (hard-directive 1). The `cmdk` package is deliberately NOT installed; the RESEARCH Package Legitimacy Audit is therefore moot (its `[ASSUMED]` gate never fires). |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/layout/__tests__/ThreadCommandPalette.test.tsx src/components/layout/__tests__/ChatLayoutLaunch.test.tsx src/components/layout/__tests__/ChatHistoryColumn.test.tsx` — all green.
- `cd frontend && npm run build` → no net-new tsc errors beyond the documented 30-error baseline (do not fix unrelated files); `npm run lint:a11y` exit 0.
- Live Chrome-MCP: press ⌘K from a NON-chat view (Documents) → palette opens over the whole backlog; ↑↓ moves, ↵ opens the thread and lands on chat, Esc closes and restores focus; the ⌘K chip in the column also opens it.
</verification>

<success_criteria>
- SC#2 (⌘K tier): ⌘K/Ctrl+K opens a global palette over all loaded threads, keyboard-forward (↑↓/↵/Esc), selecting a result opens the thread on chat.
- Hand-rolled on Radix Dialog — no `cmdk`, no install, no checkpoint; StreamsProvider-free; not a dependency of any SC (cut-line intact).
</success_criteria>

<output>
Create `.planning/phases/156-everyday-ux-polish-stretch/156-03-SUMMARY.md` when done.
</output>
