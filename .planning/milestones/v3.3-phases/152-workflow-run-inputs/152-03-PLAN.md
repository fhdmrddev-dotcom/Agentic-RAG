---
phase: 152-workflow-run-inputs
plan: 03
type: execute
wave: 2
depends_on: [152-01]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/components/layout/ChatLayout.tsx
  - frontend/src/pages/WorkflowsPage.tsx
  - frontend/src/pages/__tests__/RunModal.test.tsx
autonomous: true
requirements: [WFIN-01, WFIN-02]
user_setup: []

must_haves:
  truths:
    - "The read-only bound-folder chip becomes an inline native <select> in the 560px Run modal (D-LOCK-01)"
    - "The author folder option is tagged 'workflow default'; picking another = a per-run override (D-LOCK-01)"
    - "A quiet Upload-template button stages a file; doRun uploads it to the launched thread before sendMessage (D-LOCK-02)"
    - "An honest provenance note reads: stored untrusted, never run as code, never fed to the fill engine (D-LOCK-02)"
    - "The template is discovered by kind on the thread — the modal sequences createThread → upload_template → sendMessage (Landmine 8)"
    - "The per-run folder override rides on postMessage as folder_id into create_workflow_run.inputs (D-01)"
    - "Absent inputs (no upload, folder on 'workflow default') = today's behavior, byte-identical (D-06)"
  artifacts:
    - path: "frontend/src/pages/WorkflowsPage.tsx"
      provides: "RunModal <select> scope control + template upload button + provenance note"
      contains: "workflow default"
    - path: "frontend/src/components/layout/ChatLayout.tsx"
      provides: "doRun sequencing createThread → upload → postMessage(folderId)"
      contains: "uploadWorkspaceTemplate"
    - path: "frontend/src/lib/api.ts"
      provides: "postMessage folderId option passthrough"
      contains: "folderId"
  key_links:
    - from: "frontend/src/pages/WorkflowsPage.tsx"
      to: "frontend/src/components/layout/ChatLayout.tsx"
      via: "onLaunch(def, kickoff, {templateFile, folderId})"
      pattern: "onLaunch"
    - from: "frontend/src/components/layout/ChatLayout.tsx"
      to: "frontend/src/lib/api.ts"
      via: "uploadWorkspaceTemplate(thread.id, file) between createThread and postMessage"
      pattern: "uploadWorkspaceTemplate"
    - from: "frontend/src/lib/api.ts"
      to: "backend create_workflow_run.inputs"
      via: "postMessage body folder_id"
      pattern: "folder_id"
---

<objective>
Turn the Run modal into a real run-input channel — the frontend half of WFIN-01 (template upload) and WFIN-02 (folder scope). The read-only bound-folder chip becomes an inline native `<select>` (D-LOCK-01); a quiet Upload-template button stages a file with an honest provenance note (D-LOCK-02); and `doRun` sequences createThread → `upload_template(newThread)` → `sendMessage(folder_id)` so the template is discovered by `kind` on the launched thread (Landmine 8) and the folder override rides into `create_workflow_run.inputs`.

Purpose: The backend override channel (Plan 01) and the reused upload pipeline become usable from the modal. No new upload endpoint, no new scope path — this wires shipped seams to the UI per the APPROVED 152-UI-SPEC.
Output: `RunModal` gains the `<select>` + upload button + provenance note; `ChatLayout.doRun` + `api.postMessage` widen; `RunModal.test.tsx`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/152-workflow-run-inputs/152-UI-SPEC.md
@.planning/phases/152-workflow-run-inputs/152-CONTEXT.md
@.planning/phases/152-workflow-run-inputs/152-RESEARCH.md
@.planning/phases/152-workflow-run-inputs/152-PATTERNS.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md

@frontend/src/components/panel/TemplateUpload.tsx
@frontend/src/components/chat/ChatArea.tsx

<interfaces>
<!-- Extracted this session — executor uses these directly. -->

WorkflowsPage.tsx:
  - onLaunch prop type  :83  → `(def: PublishedWorkflow, kickoff: string) => Promise<void>`  ← WIDEN to carry {templateFile?: File | null, folderId?: string | null}
  - WorkflowsPageProps receives `folders` (id, name, parent_id) :88; folderName() derives from project_folder_id :172
  - RunModal function :719 (folderName prop :729); read-only run-folder-chip :794-800 (REPLACE with <select>); input_keys hint :813; onLaunch call :534
  - PublishedCard :612 (UNCHANGED by this plan — its ⋯-menu is Plan 04)

ChatLayout.tsx:
  - doRun :114-123  → createThread(def.name) → postMessage(thread.id, kickoff, {workflowDefinitionId: def.id}) → loadThreads → selectThread → onNavigate("chat")
  - passes onLaunch={doRun} to WorkflowsPage :351

api.ts:
  - postMessage(threadId, content, options{workflowDefinitionId?}) :511-537 — additive body build :535 (ADD folder_id when options.folderId present)
  - uploadWorkspaceTemplate(threadId, file) :1377 — FormData + Bearer, NO Content-Type (browser sets multipart boundary — load-bearing); reuse verbatim
  - createThread(title, folderId?) :99

ChatArea scope-select vocab to byte-match (ChatArea.tsx:405-421): native <select>, option "" = "All documents", `rounded-md border border-border bg-card ... focus:ring-primary/30`

TemplateUpload button shape to reuse (TemplateUpload.tsx:61-79): quiet `border-border px-2.5 py-1.5 text-[12px]` button, Upload glyph, "Upload template"/"Uploading…", inline 422 `role="alert"` server message VERBATIM
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen the launch contract — api.postMessage folder_id + ChatLayout.doRun sequencing</name>
  <files>frontend/src/lib/api.ts, frontend/src/components/layout/ChatLayout.tsx</files>
  <read_first>
    - frontend/src/lib/api.ts (:511-537 postMessage additive body, :1377 uploadWorkspaceTemplate, :99 createThread)
    - frontend/src/components/layout/ChatLayout.tsx (:104-123 doRun, :350-351 onLaunch={doRun})
    - frontend/src/pages/WorkflowsPage.tsx (:80-83 onLaunch type)
    - 152-PATTERNS.md § "ChatLayout.tsx — doRun sequencing" and § "lib/api.ts"
  </read_first>
  <action>
    In `api.ts`, extend the `postMessage` options bag with `folderId?: string | null` and add `...(options.folderId ? { folder_id: options.folderId } : {})` to the body build (:535) — the SAME additive shape as `workflow_definition_id` (absence = D-06). Do NOT modify `uploadWorkspaceTemplate` (reused verbatim).
    In `ChatLayout.tsx`, widen `doRun` to accept a third options arg `{ templateFile?: File | null, folderId?: string | null }` and slot the upload BETWEEN createThread and postMessage: `const thread = await createThread(def.name); if (templateFile) await uploadWorkspaceTemplate(thread.id, templateFile); await postMessage(thread.id, kickoff, { workflowDefinitionId: def.id, folderId })`. The template MUST upload to THIS launched (owned) thread so the fill path discovers it by `kind` (Landmine 8). Widen the `onLaunch` prop type in `WorkflowsPage.tsx` in lockstep and update the stale `:112-113` "we never pass a folder here" comment. Keep the createThread→ (upload) →send order strict; a failed upload surfaces before send (do not swallow).
  </action>
  <acceptance_criteria>
    - `grep -n "folderId\|folder_id" frontend/src/lib/api.ts` shows the postMessage option + additive body key.
    - `grep -n "uploadWorkspaceTemplate" frontend/src/components/layout/ChatLayout.tsx` shows the upload call sequenced inside doRun between createThread and postMessage.
    - The `onLaunch` type in WorkflowsPage.tsx carries the `{templateFile?, folderId?}` third arg (source review).
    - `cd frontend && npx vite build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vite build</automated>
  </verify>
  <done>doRun sequences createThread → upload_template(newThread) → sendMessage with the folder override; api.postMessage passes folder_id additively; build is green.</done>
</task>

<task type="auto">
  <name>Task 2: RunModal — inline scope <select> + quiet template button + provenance note (WFIN-01/02 UI)</name>
  <files>frontend/src/pages/WorkflowsPage.tsx</files>
  <read_first>
    - frontend/src/pages/WorkflowsPage.tsx (:719 RunModal, :794-800 read-only run-folder-chip, :813 input_keys hint, :534 onLaunch call, :761 focus-trap querySelectorAll)
    - frontend/src/components/chat/ChatArea.tsx (:405-421 native <select> scope vocab to byte-match)
    - frontend/src/components/panel/TemplateUpload.tsx (:61-79 quiet button + inline 422 role="alert")
    - 152-UI-SPEC.md §§ "Component Inventory 1/2", "Copywriting Contract → Run modal", "Visual Hierarchy", "Color" (accent reserved for the ▶ Run button only)
    - 152-PATTERNS.md § "WorkflowsPage.tsx — Surface 1 & Surface 2"
  </read_first>
  <action>
    Replace the read-only `run-folder-chip` (:794-800) with a NATIVE `<select>` styled to match ChatArea.tsx:410 (`rounded-md border border-border bg-card ... focus:ring-primary/30`) — NOT the shadcn Select popover (byte-match the app scope UX; keep the modal's 3-second read). Options in order: `All documents` (value `""`) → `📁 {authorDefaultFolder} — workflow default` (the definition's `project_folder_id`) → then every other owner-reachable folder `📁 {name}`. Default selection = the author default (or "All documents" if no `project_folder_id`); keep the "workflow default" tag on that one option. Composition guard (A4 / UI-SPEC §1): if the workflow declares per-phase `folder_scope`, offer ONLY folders ⊆ the author `project_folder_id` (out-of-project options would silently empty retrieval). Hide the whole control when `folders.length === 0` (match ChatArea's guard). The modal must receive `folders` + the author default id as props (widen RunModal's signature accordingly).
    Add a quiet self-start bordered Upload-template button below the kickoff textarea, reusing the TemplateUpload button shape (`border-border px-2.5 py-1.5 text-[12px]`, Upload glyph, "Upload template"/"Uploading…"). It STAGES the picked `File` in modal state (no thread exists yet — doRun uploads it, Task 1); show the validated-file card (`{filename} ✓` + remove `✕` aria-label "Remove template") or the inline 422 error (`text-destructive`, `role="alert"`, server message VERBATIM). Directly beneath, a 12px muted provenance note: `Stored untrusted — never run as code, never fed to the fill engine.`
    Pass the staged `templateFile` + selected `folderId` through the existing `onLaunch(target, text, { templateFile, folderId })` call (:534). The single accent-filled element stays the `▶ Run` button (UI-SPEC Visual Hierarchy) — the `<select>`, upload button, and note are neutral-bordered subordinates. Keep the shipped focus trap (:761 querySelectorAll already includes `select`), Escape-to-close, and initial focus intact.
  </action>
  <acceptance_criteria>
    - `grep -n "workflow default\|All documents\|Stored untrusted" frontend/src/pages/WorkflowsPage.tsx` shows the tagged author-default option, the "All documents" option, and the exact provenance note copy.
    - The read-only `run-folder-chip` at the old :794-800 is replaced by a `<select>` (source review — no static chip remains for scope).
    - The `<select>` hides when `folders.length === 0`; for a per-phase-`folder_scope` workflow it offers only folders ⊆ the author default (source review of the option filter).
    - The upload button stages a File in modal state and renders either a validated-file card or an inline `role="alert"` 422 error with the verbatim server message; the provenance note renders at 12px muted.
    - `onLaunch` is called with `{ templateFile, folderId }`; the `▶ Run` button remains the sole `bg-primary` element.
    - `cd frontend && npx vite build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vite build</automated>
  </verify>
  <done>The Run modal shows an inline scope <select> (author default tagged, override selectable, composition-guarded), a quiet staged template upload with an honest provenance note, and passes both inputs to onLaunch — matching the APPROVED UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 3: RunModal component test (select ordering + staged upload + onLaunch payload)</name>
  <files>frontend/src/pages/__tests__/RunModal.test.tsx</files>
  <read_first>
    - frontend/src/pages/WorkflowsPage.tsx (the RunModal surfaces from Task 2)
    - 152-VALIDATION.md § "Wave 0 Requirements" (RunModal.test.tsx expected) and § "Manual-Only Verifications" (the SC#10 live rows stay in VALIDATION, NOT here)
    - an existing vitest under frontend/src/**/__tests__/ for the render/act harness convention (measure vs the ~14-17 SEED-056 vitest baseline, not zero-fail)
  </read_first>
  <action>
    Add `RunModal.test.tsx` proving the modal contract deterministically: (1) the scope `<select>` renders "All documents" + the author-default option tagged "workflow default" in order, and hides when no folders; (2) picking another folder + launching calls `onLaunch` with that `folderId`; (3) staging a template File then launching calls `onLaunch` with the `templateFile`; (4) an absent-input launch (no upload, folder on "workflow default") passes no override (D-06). Mock `onLaunch`; do NOT hit the network. This is the frontend Nyquist backstop — the cross-provider SC#10 proof is the LIVE UAT in 152-VALIDATION.md and is NOT duplicated here.
  </action>
  <acceptance_criteria>
    - `frontend/src/pages/__tests__/RunModal.test.tsx` exists with cases for select ordering/hide, folderId payload, templateFile payload, and D-06 absent-input.
    - `cd frontend && npx vitest run src/pages/__tests__/RunModal.test.tsx` passes the new cases (baseline-aware).
    - `cd frontend && npx vite build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/__tests__/RunModal.test.tsx</automated>
  </verify>
  <done>The RunModal test locks the select ordering/hide, the folderId + templateFile onLaunch payloads, and the D-06 absent-input path; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → workspace upload | the picked template file is untrusted bytes uploaded to the launched (owned) thread |
| browser → threads.py send | `folder_id` is client-selected; the server (Plan 01) is the authorization wall |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-152-03-01 | Tampering / EoP (SSTI) | template upload → fill path | mitigate | The reused `upload_template` stamps `kind='template_input'`; `resolve_template_source` Branch 2 routes it to `run_replace`, NEVER `docxtpl`/Jinja (D-07). No fill-path change this plan. |
| T-152-03-02 | EoP | upload target thread | mitigate | doRun uploads to the FRESH createThread()'d thread (owned by the user); `upload_template` `_verify_thread_ownership` 404s a non-owner. |
| T-152-03-03 | DoS / Tampering | file bytes | mitigate | Reused `validate_upload` — 10MB cap pre-buffer + post-read, magic-byte gate, office-bomb guard; 422 at the door renders the server message verbatim. |
| T-152-03-04 | Info Disclosure | scope <select> | mitigate | The client only proposes `folder_id`; the server owner-gate + subtree resolver (Plan 01, D-05/D-04) is the wall. The UI additionally offers only owner-reachable / ⊆-project folders (A4) for honest UX. |
| T-152-03-SC | Tampering | dependency installs | accept | No package install — `dropdown-menu`/`sheet`/`select` are already vendored; native `<select>` reused (UI-SPEC Registry Safety). slopcheck N/A. |
</threat_model>

<verification>
- `cd frontend && npx vite build` exits 0 after each task.
- `cd frontend && npx vitest run src/pages/__tests__/RunModal.test.tsx` green (baseline-aware).
- Source review: template uploads to the launched owned thread (createThread → upload → send order); the ▶ Run button is the sole accent element; provenance note copy is verbatim; the scope `<select>` is composition-guarded and matches the ChatArea vocab.
- WFIN-01/02 live cross-provider + multi-tool proof is the SC#10 UAT in 152-VALIDATION.md — NOT a task here.
</verification>

<success_criteria>
- From the Run modal a user can upload a template (stored untrusted, never Jinja) and choose a KB folder (author default + per-run override) — WFIN-01 + WFIN-02 UI (D-LOCK-01/02).
- The template is discovered by `kind` on the launched thread (createThread → upload → send, Landmine 8); the folder override rides into create_workflow_run.inputs (D-01).
- Absent inputs = today's behavior (D-06); the modal keeps its 3-second read and a11y contract.
</success_criteria>

<output>
Create `.planning/phases/152-workflow-run-inputs/152-03-SUMMARY.md` when done.
</output>
