---
phase: 152-workflow-run-inputs
plan: 04
type: execute
wave: 3
depends_on: [152-02, 152-03]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/pages/WorkflowsPage.tsx
  - frontend/src/pages/__tests__/PublishedCardDelete.test.tsx
autonomous: true
requirements: [WFIN-03]
user_setup: []

must_haves:
  truths:
    - "The delete entry point is a net-new ⋯-menu on PublishedCard whose single item opens a confirm sheet (D-LOCK-03)"
    - "The victim-naming sheet names exact Removed (definition · N versions · N runs) vs Kept (N threads → normal chats) counts from the server preview (D-LOCK-03)"
    - "An in-flight run shows an amber cancel-first banner before the destructive action (D-LOCK-05)"
    - "The card transitions in place (Deleting… → Deleted · recorded) with NO optimistic vanish and NO undo (D-LOCK-04)"
    - "Counts come from GET /workflows/{id}/delete-preview — never guessed (D-LOCK-03)"
    - "The ⋯ trigger + menu item are neutral/destructive-tinted; the destructive weight lands only on the Delete-forever button"
  artifacts:
    - path: "frontend/src/pages/WorkflowsPage.tsx"
      provides: "PublishedCard ⋯-menu + victim-naming delete Sheet"
      contains: "Delete this workflow?"
    - path: "frontend/src/lib/api.ts"
      provides: "deleteWorkflowCascade + getWorkflowDeletePreview clients"
      contains: "deleteWorkflowCascade"
  key_links:
    - from: "frontend/src/pages/WorkflowsPage.tsx"
      to: "frontend/src/lib/api.ts"
      via: "getWorkflowDeletePreview before commit + deleteWorkflowCascade on confirm"
      pattern: "getWorkflowDeletePreview"
    - from: "frontend/src/lib/api.ts"
      to: "backend api/workflows.py cascade route"
      via: "DELETE /workflows/{id}/cascade"
      pattern: "/cascade"
---

<objective>
Add the WFIN-03 delete surface — the frontend half. `PublishedCard` gains a NET-NEW `⋯` menu (RESEARCH Pitfall 8 — no menu exists today) whose single "Delete workflow…" item opens a victim-naming confirm Sheet (D-LOCK-03, the shipped 064/068 pattern). The sheet names exact Removed vs Kept counts from the server delete-preview, shows an amber cancel-first banner when a run is in flight (D-LOCK-05), and transitions the card in place (Deleting… → Deleted · recorded) with no optimistic vanish and no undo (D-LOCK-04).

Purpose: A safe, honest, operator-grade delete confirmation that reads exactly like the shipped kill-run vocabulary. The backend endpoint + preview (Plan 02) are the source of truth for counts and the owner gate.
Output: `deleteWorkflowCascade` + `getWorkflowDeletePreview` in `api.ts`; the `⋯` menu + victim-naming Sheet on `PublishedCard`; `PublishedCardDelete.test.tsx`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/152-workflow-run-inputs/152-UI-SPEC.md
@.planning/phases/152-workflow-run-inputs/152-CONTEXT.md
@.planning/phases/152-workflow-run-inputs/152-PATTERNS.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md

@frontend/src/components/admin/ActiveRunsSection.tsx
@frontend/src/components/ingestion/FolderNode.tsx

<interfaces>
<!-- Extracted this session — executor uses these directly. -->

WorkflowsPage.tsx:
  - PublishedCard :612-668 — header :628-644 (add ⋯ top-right, left of the `published` pill); footer ⑂ Tweak / ▶ Run :650-668 (UNCHANGED)
  - PublishedCard has ONLY Tweak+Run today — the ⋯ menu is NET-NEW (Pitfall 8)

api.ts analogs:
  - deleteWorkflowDraft(id, signal?) :3231-3241 — getAuthHeaders + status-mapped errors (WorkflowConflictError 409 / WorkflowNotFoundError 404); mirror the shape for the NEW route DELETE /workflows/{id}/cascade
  - ADD getWorkflowDeletePreview(id) → GET /workflows/{id}/delete-preview → {name, versions, runs, threads}

⋯-menu analog (FolderNode.tsx:138-182): DropdownMenu → DropdownMenuTrigger asChild (Button ghost, onClick stopPropagation) → DropdownMenuContent align="end" → destructive DropdownMenuItem (Trash2). `@/components/ui/dropdown-menu` already installed.

victim-naming Sheet analog (ActiveRunsSection.tsx):
  - Sheet open/onOpenChange + SheetContent side="bottom" className="mx-auto max-w-lg" :285-313
  - KillPhase state machine "idle"|"cancelling"|"cancelled"|"error" :125-173 ; right-rail terminal states (Loader2 spin role="status" / Check --success / destructive error + Try again) :252-279
  - NO optimistic removal — transitions in place then list re-fetch
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: api.ts delete clients — deleteWorkflowCascade + getWorkflowDeletePreview</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts (:3231-3241 deleteWorkflowDraft — getAuthHeaders + status-mapped-error shape; WorkflowConflictError / WorkflowNotFoundError classes)
    - 152-PATTERNS.md § "lib/api.ts — Analog A" and 152-RESEARCH.md § "Route + gate" (distinct route DELETE /workflows/{id}/cascade)
    - backend/app/api/workflows.py (the Plan 02 cascade + delete-preview routes — confirm the exact paths + response shape)
  </read_first>
  <action>
    Add `deleteWorkflowCascade(id, signal?)` hitting the NEW distinct route `DELETE /workflows/${id}/cascade` (NOT the draft `DELETE /workflows/${id}` — do not collide), reusing `getAuthHeaders` and the same status-mapped-error shape as `deleteWorkflowDraft` (404 → not-found, non-ok → thrown Error). Add `getWorkflowDeletePreview(id)` → `GET /workflows/${id}/delete-preview` returning `{name, versions, runs, threads}` for the sheet counts. Keep types explicit (a `WorkflowDeletePreview` interface). Do not swallow errors — the sheet's error state renders on a thrown failure.
  </action>
  <acceptance_criteria>
    - `grep -n "deleteWorkflowCascade\|getWorkflowDeletePreview\|/cascade\|/delete-preview" frontend/src/lib/api.ts` shows both functions hitting the distinct routes.
    - `deleteWorkflowCascade` uses `DELETE /workflows/{id}/cascade` — NOT the draft path (source review — the existing `deleteWorkflowDraft` is unchanged).
    - `cd frontend && npx vite build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vite build</automated>
  </verify>
  <done>The delete clients call the distinct cascade + delete-preview routes with the shipped auth + error shape; build green.</done>
</task>

<task type="auto">
  <name>Task 2: PublishedCard ⋯-menu + victim-naming delete Sheet (net-new UI)</name>
  <files>frontend/src/pages/WorkflowsPage.tsx</files>
  <read_first>
    - frontend/src/pages/WorkflowsPage.tsx (:612-668 PublishedCard — header :628-644, footer ⑂ Tweak/▶ Run :650-668)
    - frontend/src/components/ingestion/FolderNode.tsx (:138-182 DropdownMenu + destructive item pattern)
    - frontend/src/components/admin/ActiveRunsSection.tsx (:125-173 KillPhase state machine, :252-279 terminal states, :285-313 victim-naming Sheet)
    - 152-UI-SPEC.md §§ "Component Inventory 3/4", "Copywriting Contract → Delete cascade", "Visual Hierarchy" (Delete-forever = sole destructive), "Color" (destructive reserved list; ⋯ trigger neutral), "States matrix"
    - 152-PATTERNS.md § "WorkflowsPage.tsx — Surface 3"
  </read_first>
  <action>
    Add a lucide `MoreHorizontal` icon button to the PublishedCard header (top-right, immediately left of the `published` pill; `text-muted-foreground hover:text-foreground`, `aria-label="Workflow actions"`, min 32px hit target, `onClick` stopPropagation). Open `@/components/ui/dropdown-menu` (already installed) with a single destructive-tinted item `Delete workflow…` (`text-destructive focus:text-destructive`, optional `Trash2`). The footer `⑂ Tweak` / `▶ Run` pair stays UNCHANGED.
    The item opens a `@/components/ui/sheet` `side="bottom" className="mx-auto max-w-lg"` (the SAME primitive as ActiveRunsSection's Kill sheet). On open, call `getWorkflowDeletePreview(id)` and render EXACT counts (never guessed, never "several"): title `Delete this workflow?`; Removed group (danger-tinted heading `Permanently removed`, body `{name} · {N} versions · {N} run records`); Kept group (neutral heading `Kept — not touched`, body `{N} chat threads become normal chats — transcripts & files stay. Your knowledge base is untouched.` — always render, even at 0: `No chat threads to keep.`); an amber in-flight banner ONLY when a run is live (`1 run is still in progress. It's cancelled safely first, then the workflow is deleted.`, D-LOCK-05); an action row (`Keep it` neutral · `Delete forever` destructive — the SOLE `--destructive` element, UI-SPEC Visual Hierarchy); footer `✎ Recorded with your name in the audit log.`.
    Wire the in-place lifecycle by adapting the KillPhase state machine (`idle`→`cancelling`→`cancelled`→`error`): `Delete forever` → `Deleting…` (Loader2 spin, `role="status"`) → on success `Deleted · recorded` (Check, `--success`, `role="status"`) then re-fetch the workflow list (mirror ActiveRunsSection — NO optimistic card removal, NO undo, hard-delete is irreversible); on failure `Couldn't delete the workflow` + a `Try again` control. The `Sheet` provides focus-trap/restore; all icon-only controls carry `aria-label`.
  </action>
  <acceptance_criteria>
    - `grep -n "MoreHorizontal\|Delete this workflow?\|Permanently removed\|Recorded with your name" frontend/src/pages/WorkflowsPage.tsx` shows the ⋯ trigger and the exact sheet copy.
    - The sheet fetches counts via `getWorkflowDeletePreview` before commit (source review — no hardcoded/placeholder counts); the Kept sentence always renders (incl. the 0-threads variant).
    - The amber in-flight banner renders only when a run is live; `Delete forever` is the sole `bg-destructive`/`--destructive` element (the ⋯ trigger + menu item are neutral/destructive-tinted only).
    - The card transitions in place (Deleting… → Deleted · recorded) then re-fetches — no optimistic vanish, no undo (source review of the KillPhase adaptation).
    - The footer `⑂ Tweak` / `▶ Run` pair is unchanged.
    - `cd frontend && npx vite build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vite build</automated>
  </verify>
  <done>PublishedCard has a net-new ⋯-menu opening a victim-naming Sheet with exact server-sourced Removed/Kept counts, an amber cancel-first banner, an in-place no-undo lifecycle, and the destructive weight only on Delete-forever.</done>
</task>

<task type="auto">
  <name>Task 3: PublishedCard delete component test (preview counts + lifecycle + honesty)</name>
  <files>frontend/src/pages/__tests__/PublishedCardDelete.test.tsx</files>
  <read_first>
    - frontend/src/pages/WorkflowsPage.tsx (the ⋯-menu + Sheet from Task 2)
    - 152-VALIDATION.md § "Manual-Only Verifications" (the live delete-cascade row stays in VALIDATION; this is the deterministic component backstop)
    - an existing vitest under frontend/src/**/__tests__/ for the render/act + mocked-api convention (baseline-aware vs SEED-056)
  </read_first>
  <action>
    Add `PublishedCardDelete.test.tsx` proving: (1) opening the ⋯-menu → Delete item opens the Sheet and calls `getWorkflowDeletePreview` (mocked) with the definition id; (2) the sheet renders the EXACT mocked counts (`{name} · N versions · N run records` + the Kept sentence, incl. the 0-threads `No chat threads to keep.` variant); (3) the amber in-flight banner shows only when the preview reports a live run; (4) `Delete forever` calls `deleteWorkflowCascade` and drives the in-place `Deleting…` → `Deleted · recorded` states with NO optimistic card removal; (5) a rejected `deleteWorkflowCascade` shows `Couldn't delete the workflow` + Try again. Mock `api.ts`; no network. The live destructive UAT (real DB counts, cancel-first) is the row in 152-VALIDATION.md — NOT duplicated here.
  </action>
  <acceptance_criteria>
    - `frontend/src/pages/__tests__/PublishedCardDelete.test.tsx` exists with cases for preview-fetch, exact counts + Kept-sentence (incl. 0-threads), in-flight banner gating, in-place success lifecycle, and error → Try again.
    - `cd frontend && npx vitest run src/pages/__tests__/PublishedCardDelete.test.tsx` passes the new cases (baseline-aware).
    - `cd frontend && npx vite build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/__tests__/PublishedCardDelete.test.tsx</automated>
  </verify>
  <done>The delete test locks the preview fetch, exact counts, in-flight banner, in-place no-undo lifecycle, and error handling; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → cascade route | the client issues the delete; the backend owner gate (Plan 02) is the authorization wall |
| server preview → sheet | the Removed/Kept counts must be the server's truth, shown before an irreversible action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-152-04-01 | EoP (IDOR) | delete cascade client | mitigate | The client cannot bypass authorization — the backend owner app-gate (Plan 02, `created_by`) 404s a non-owner; the UI merely calls the route. |
| T-152-04-02 | Info Disclosure / Integrity | victim-naming counts | mitigate | Counts come from `GET /delete-preview` (owner-scoped server truth) rendered before commit — never fabricated, never "several" (honesty contract). |
| T-152-04-03 | Repudiation / Tampering | irreversibility signalling | mitigate | In-place lifecycle, no optimistic vanish, NO undo (hard-delete is irreversible — do not imply reversibility); the `✎ Recorded with your name` footer surfaces the audit receipt. |
| T-152-04-04 | Availability | in-flight run | mitigate | The amber cancel-first banner sets the expectation that a live run is cancelled safely first (D-LOCK-05); the actual cancel happens server-side (Plan 02). |
| T-152-04-SC | Tampering | dependency installs | accept | No package install — `dropdown-menu` + `sheet` already vendored (UI-SPEC Registry Safety). slopcheck N/A. |
</threat_model>

<verification>
- `cd frontend && npx vite build` exits 0 after each task.
- `cd frontend && npx vitest run src/pages/__tests__/PublishedCardDelete.test.tsx` green (baseline-aware).
- Source review: counts are server-sourced (getWorkflowDeletePreview), no optimistic vanish / no undo, Delete-forever is the sole destructive element, ⋯-menu is neutral, Tweak/Run unchanged, all icon-only controls carry aria-label.
- The live delete-cascade destructive UAT (real DB counts, cancel-first, no orphans) is the row in 152-VALIDATION.md — NOT a task here.
</verification>

<success_criteria>
- A user can delete a workflow via a net-new ⋯-menu → victim-naming sheet behind a confirmation (WFIN-03, D-LOCK-03).
- The sheet names exact Removed/Kept counts from the server preview; an amber banner signals cancel-first for a live run (D-LOCK-05).
- The card transitions in place with no undo; the destructive weight lands only on Delete-forever (D-LOCK-04, UI-SPEC).
</success_criteria>

<output>
Create `.planning/phases/152-workflow-run-inputs/152-04-SUMMARY.md` when done.
</output>
