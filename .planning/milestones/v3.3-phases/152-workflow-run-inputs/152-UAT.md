---
status: complete
operator_accepted_exception: "OpenRouter cross-provider row (test 4) accepted as a known exception 2026-07-15 — blocked by pre-existing external BUG-260714-02 (tool-404), NOT a phase-152 defect. SC#10 cross-provider proof = OpenAI + Anthropic + Google + minimax (4 working providers, scope held on all). OpenRouter is documented experimental."
phase: 152-workflow-run-inputs
mode: whole-phase-sc10
supersedes: "gap-closure re-verify (commit ebc115d0) — Gap #1 (D-04 widen) already CLOSED on 3 surfaces; that record lives in git history + STATE.md"
scope_note: "Whole-phase SC#10 4-axis manual UAT (152-VALIDATION.md Manual-Only Verifications) — the real gate before 152 COMPLETE. Cross-provider row expanded per-provider (OpenAI/Anthropic/Google/OpenRouter) for honest tracking. Claude drives the live app via claude-in-chrome; DB truth via psycopg2 (:54322). Backend/frontend confirmed up (health ok). WFIN-01/02/03 stay OPEN until all axes pass live."
source:
  - 152-VALIDATION.md (Manual-Only Verifications — MANDATORY SC#10)
  - 152-01-SUMMARY.md (folder override + resolve_run_scope_root)
  - 152-03-SUMMARY.md (template upload + Run modal sequencing)
  - 152-04-SUMMARY.md + 152-05-SUMMARY.md (delete cascade)
started: 2026-07-14T22:07:36Z
updated: 2026-07-14T22:07:36Z
---

## Current Test

[testing complete — 8/9 pass; Test 4 (OpenRouter) BLOCKED by external BUG-260714-02, operator-accepted as a known exception 2026-07-15. Phase marked complete.]

## Tests

### 1. Cross-provider folder-scope — OpenAI
expected: Under an OpenAI model, a workflow run launched with a folder override retrieves ONLY from that folder's subtree; "what documents do you have?" is answered within the scoped folder, not the whole KB.
result: pass
evidence: "Live via chat scoped to 📁 Weekly reports (folder_id 2a33b3e3), run header `openai · gpt-4o · turn 1` (6.1s). Model listed EXACTLY the 5 ground-truth folder docs (SKILL_INSTRUCTIONS.md, kb_doc1/2/3_*.md, weekly_report_structure.md) and ZERO decoys (unfiled weekly-status-report.docx, risk-register.docx, uat111_* all absent; no other folder's docs). Note: the app default gpt-5.6-sol first threw 'Model parameter error' (known GPT-5.6 issue BUG-260714-01/SEED-088) — switched to gpt-4o to isolate scope. Surface = chat MessageCreate.folder_id (the phase-152 field) + shared search_documents scope clip (T-152-01-02)."

### 2. Cross-provider folder-scope — Anthropic
expected: Same run under an Anthropic model — retrieval stays constrained to the chosen folder's subtree. Provider swap does not widen scope.
result: pass
evidence: "Same scoped thread, run header `anthropic · claude-sonnet-5 · turn 1` (9.0s). Answer: 'exact filenames of the 5 documents accessible in this scope (/Weekly reports)' — exactly the 5 folder docs, zero decoys."

### 3. Cross-provider folder-scope — Google
expected: Same run under a Google (Gemini) model — retrieval stays constrained to the chosen folder's subtree.
result: pass
evidence: "Same scoped thread, run header `google · gemini-3.5-flash · turn 1` (14.1s). Answer: 'exact filenames of all documents currently accessible' = exactly the 5 folder docs, zero decoys. (Bonus: native minimax MiniMax-M2.5-highspeed also returned exactly the 5.)"

### 4. Cross-provider folder-scope — OpenRouter
expected: Same run under an OpenRouter model — retrieval stays constrained to the chosen folder's subtree. (Known risk: BUG-260714-02 OpenRouter tool-404 may break tool calls; if so, record as blocked/issue, not a scope leak.)
result: blocked
blocked_by: third-party
reason: "OpenRouter tool-calling is broken by the pre-existing BUG-260714-02. 3 models tried: nvidia/nemotron-3-ultra-550b (hung ~50s, no run card), deepseek/deepseek-v4-pro and meta-llama/llama-3.3-70b-instruct BOTH returned HTTP 404 'No endpoints found that can handle the requested parameters' (OpenRouter provider-routing rejects the tools param). The tool call never reaches the model, so folder-scope retrieval cannot be exercised. This is an external/experimental-provider bug (OpenRouter is documented experimental), NOT a phase-152 scope defect — the scope bind is server-side and provider-independent, proven on OpenAI + Anthropic + Google + minimax. Gate note: the SC#10 cross-provider axis is 3/4 native providers PASS; OpenRouter blocked until BUG-260714-02 is fixed."

### 5. Multi-tool: template upload + folder scope in one launch
expected: In one workflow launch, stage a docx template AND set a folder override; the run fills the template using folder-scoped retrieval. Both inputs take effect — template is used (kind=template_input) and retrieval is folder-constrained.
result: pass
evidence: "LIVE via the actual phase-152 WORKFLOW Run modal (KB Cited Answer, unbound). Set Knowledge base=📁 Weekly reports AND staged ssti_min.docx (green ✓ = validate_upload passed) in ONE launch → Run workflow. New thread 2e1ff124 created; Workspace panel showed FILES: /51ad2c82-ssti_min.docx tagged TEMPLATE (1.1KB); both workflow phases ✓ Complete. The agent answered 'using the contents of these 5 documents' = the 5 Weekly-reports docs → folder override took effect on the real workflow-run path (resolve_run_scope_root → folder_subtree_ids), AND the template input took effect, in a single launch. Both inputs honored."

### 6. Parallel-thread isolation
expected: Start a folder-scoped run in Thread A; while it streams, open the Run modal in Thread B and launch a run with a DIFFERENT (or no) override. Inputs do not cross-contaminate — Thread A keeps its scope, Thread B gets its own.
result: pass
evidence: "GENUINE concurrent overlap proven live. Thread C = Risk Register (long report gen) launched folder=Weekly reports (2a33b3e3), created 22:40:26, still status=active. While C was streaming, Thread D = KB Cited Answer launched folder=DBA (37380338), created 22:41:33 and completed 22:41:59 — ENTIRELY inside C's active window. DB workflow_runs: C.inputs.folder_id=2a33b3e3, D.inputs.folder_id=37380338 — each run kept its OWN durable per-run override, ZERO cross-contamination under real concurrency (WORKER_COUNT=2). Confirms T-152-01-03 (override read from durable per-run workflow_runs.inputs, not shared state). (First back-to-back pair A=Meridian/B=DBA also showed distinct folder_ids.)"

### 7. Long-message scope enforcement
expected: On a long thread (≥50 prior messages OR ≥5 KB prompt), launch a folder-scoped run; scope still constrains retrieval under context-window pressure.
result: pass
evidence: "Folder-scoped chat (thread 90f97db0, thread.folder_id=2a33b3e3 Weekly reports). A TRUE ≥5 KB single prompt (user message octet_length=6205 bytes) under deepseek-v4-flash returned EXACTLY the 5 Weekly-reports docs (SKILL_INSTRUCTIONS.md, kb_doc1/2/3_*.md, weekly_report_structure.md): 'That's all 5 documents. No subfolders. No documents from outside this scope.' Scope held firm under context-window pressure. (Also confirmed at 3.3 KB + 3.9 KB earlier messages in the same thread → ~8 KB cumulative context, all scoped.) HARNESS NOTE: a JS-injected FIRST message on a brand-new chat created a thread with folder_id=None (whole-KB, 36 docs) — a test-method artifact where scope didn't attach; the real user flow (typed msg + Enter, or a follow-up in an already-scoped thread) attaches scope correctly, as proven in tests 1-3 and here. Not a user-facing defect."

### 8. Delete-cascade destructive (victim-naming + no orphans + in-flight cancel-first)
expected: Delete a published workflow that has runs + linked threads (incl. one in-flight run). The victim-naming sheet names EXACT Removed vs Kept counts matching the DB; an in-flight run is cancelled-first; after delete there are no orphaned workflow_runs/workflow_phases, linked threads survive as normal chats, KB untouched. Hard-delete, no undo.
result: pass
evidence: "LIVE via UI on a THROWAWAY seeded workflow (zz-152-delete-test: 1 published def, 2 runs [1 active/in-flight], 2 phases, 2 threads each w/ active_workflow_run_id + 1 message). Victim-naming Sheet showed EXACT server-truth counts matching the seed: 'Permanently removed: ZZ 152 DELETE TEST · 1 versions · 2 run records'; 'Kept — not touched: 2 chat threads become normal chats — transcripts & files stay. Your knowledge base is untouched'; amber banner '1 run is still in progress. It's cancelled safely first, then the workflow is deleted' (D-LOCK-05 cancel-first); '✎ Recorded with your name in the audit log'; buttons Keep it / Delete forever (no undo). Clicked Delete forever. DB after: definition rows=0, runs=0, workflow_phases orphaned=0, GLOBAL orphaned phases=0 (FK order runs→phases CASCADE honored); BOTH threads SURVIVE with active_workflow_run_id=NULL (SET NULL → normal chats), 2 messages preserved; user documents still 36 (KB untouched). UI card vanished only on server-confirmed re-fetch (no optimistic vanish, T-152-04-03). Confirms WFIN-03 + T-152-02-01..05 / T-152-05-01..05."

### 9. Template stored untrusted — SSTI provenance
expected: Upload a template whose content contains Jinja/SSTI-looking markup ({{ 7*7 }} etc.); confirm it is stored with kind=template_input and routed through the run_replace path (NOT the docxtpl/Jinja fill engine) — no code execution, the markup is treated as literal bytes.
result: pass
evidence: "Uploaded ssti_min.docx containing {{ 7*7 }}, {{ config }}, {% for i in range(3) %}X{% endfor %}, {{ ''.__class__.__mro__[1].__subclasses__() }}. Live DB (workspace_files id 948bffa6): kind='template_input' ✓; mime=docx; stored inline as raw bytes (size 1094 = original). word/document.xml markers: contains '{{ 7*7 }}'=True but '49'=False (Jinja would render 49 → NOT evaluated); '{% for'=True but 'XXX'=False (loop NOT rendered); '__subclasses__' present literal. => stored untrusted, never Jinja/docxtpl-evaluated. run_replace fill-path (never docxtpl for untrusted) is additionally code-verified in 152-SECURITY.md T-152-03-01 (template_asset_service.py:182) + existing test_workspace_template.py."

## Summary

total: 9
passed: 8
issues: 0
pending: 0
skipped: 0
blocked: 1

## WFIN requirement roll-up

- WFIN-01 (template upload input + SSTI provenance): PASS — tests 5, 9 (live workflow-run template stage + kind=template_input + SSTI markup stored literal).
- WFIN-02 (per-run folder override, scope narrows-not-widens): PASS on OpenAI + Anthropic + Google (+ minimax bonus) + the real workflow-run path (test 5) + parallel-thread isolation (test 6) + long-message ≥5KB (test 7). BLOCKED on OpenRouter (test 4, BUG-260714-02 tool-404 — external, not a 152 defect).
- WFIN-03 (delete-cascade, no orphans, threads survive, in-flight cancel-first): PASS — test 8 (exact victim-naming counts + full DB cascade verification).

Outstanding for full SC#10 sign-off: OpenRouter cross-provider row (blocked by BUG-260714-02). **RESOLVED 2026-07-15 — operator ACCEPTED 3/4-native + minimax as the cross-provider proof (OpenRouter is documented experimental; its 404 is external BUG-260714-02, not a 152 defect). WFIN-02 signed off; 152 marked complete. Re-run test 4 if/when BUG-260714-02 is fixed.**

## Gaps

[none yet]
