---
phase: 152-workflow-run-inputs
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
closes: [CR-01, WR-01, IN-02]
files_modified:
  - backend/app/api/workflows.py
  - backend/app/db/workflows.py
  - backend/tests/test_152_delete_cascade.py
autonomous: true
requirements: [WFIN-03]

must_haves:
  truths:
    - "An in-flight kickoff-started workflow run is cancelled through its PRODUCER runs.run_id (the RUN_TASKS key) BEFORE its rows are deleted, so the live engine actually stops (D-LOCK-05)"
    - "The cascade route publishes the ask_user cancel sentinel keyed by the WORKFLOW run id and durably terminalizes the workflow_runs row (finish_run) before the delete — covering the paused + cross-worker (WORKER_COUNT=2) cases"
    - "A route-level test seeds a REAL status='streaming' producer runs row and proves _cancel_run_internals is invoked with the producer run_id exactly once — never the workflow_runs id"
    - "Both /cascade and /delete-preview collapse a foreign / unknown definition_id to 404 (no existence leak) and a seeded status='active' run makes the preview report in_flight == 1"
    - "A cascade on an is_global definition that has OTHER users' runs is refused with 409 (WR-01), and the delete_workflow_cascade_preview docstring no longer claims run-level owner-scoping"
  artifacts:
    - path: "backend/app/api/workflows.py"
      provides: "producer-identity cancel-first in delete_workflow_cascade + is_global cross-user 409 refuse"
      contains: "producer_id"
    - path: "backend/app/db/workflows.py"
      provides: "cross-user global-run count helper + corrected preview docstring"
    - path: "backend/tests/test_152_delete_cascade.py"
      provides: "route-level cancel-first (producer identity) + 404-both-routes + in_flight preview + WR-01 refuse tests"
  key_links:
    - from: "backend/app/api/workflows.py (delete_workflow_cascade)"
      to: "app.services.run_lifecycle._cancel_run_internals"
      via: "cancel through r['producer_id'] (the RUN_TASKS key)"
      pattern: "_cancel_run_internals\\(\\s*run_id=r\\[.producer_id.\\]"
    - from: "backend/app/api/workflows.py (delete_workflow_cascade)"
      to: "app.services.ask_user_service.publish_cancel_sentinel + app.db.workflows.finish_run"
      via: "sentinel keyed by wf_id + durable terminalize before the delete txn"
      pattern: "publish_cancel_sentinel|finish_run"
---

<objective>
Close the ONE re-verification BLOCKER (CR-01) and Warning WR-01 on the shipped delete cascade, WITHOUT re-planning 152-02/152-04 (their code stays; this builds on top).

CR-01: `delete_workflow_cascade` (`backend/app/api/workflows.py`) cancels in-flight runs by the WRONG identity — it passes `workflow_runs.id` to `_cancel_run_internals`, but a live kickoff-started run's producer task is registered in `RUN_TASKS` under the PRODUCER `runs.run_id` (`threads.py:2011`). The cancel misses, so the FK-safe txn hard-deletes `workflow_runs`/`workflow_phases` out from under a still-executing engine — defeating locked decision D-LOCK-05 and making the shipped amber banner copy false. Fix: resolve the producer identity via a JOIN to the live `runs` rows and cancel through it (the same identity the admin Kill path uses), publish the ask_user cancel sentinel keyed by the workflow run id, and durably terminalize the workflow_runs row via `finish_run` — all cancel-first, outside the delete transaction.

WR-01: cascade + preview on an `is_global` definition sweep/aggregate ALL users' runs; the `delete_workflow_cascade_preview` docstring falsely claims run-level owner-scoping. Fix: refuse the cascade with 409 when an is_global definition has runs owned by other users, and correct the misleading docstring.

IN-02: the missing route-level backstop that would have caught CR-01. Fix: add httpx dependency-override route tests proving the producer-identity cancel, 404-collapse on both routes, the in_flight preview count, and the WR-01 refuse.

Purpose: the shipped "Delete forever" is honestly safe (a live run truly stops before its rows vanish) and cannot silently blast another user's runs.
Output: producer-identity cancel-first + WR-01 refuse + a route-level cancel-first test.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/152-workflow-run-inputs/152-REVIEW.md
@.planning/phases/152-workflow-run-inputs/152-VERIFICATION.md
@.planning/phases/152-workflow-run-inputs/152-02-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs — extracted this session; do NOT re-explore. -->

app.services.run_lifecycle._cancel_run_internals (run_lifecycle.py:158) — the shared cancel/zombie-heal:
  async def _cancel_run_internals(*, run_id, status: str, thread_id, redis, supabase) -> str
  Internally does RUN_TASKS.get(run_id) (run_lifecycle.py:204) — so run_id MUST be the PRODUCER runs.run_id
  (registered at threads.py:2011 as RUN_TASKS[run_id]=task), NOT the workflow_runs.id. Returns
  "terminal_noop" | "task_cancelled" | "zombie_healed". Late-imported at the call site (admin.py:479 discipline).

app.services.ask_user_service.publish_cancel_sentinel(redis, run_id) (ask_user_service.py:241) — best-effort
  broadcast to ask_user:channels:{run_id}; harness prompts subscribe on the WORKFLOW run id channel. Never raises.

app.db.workflows.finish_run(pool, run_id: UUID, status: str) (db/workflows.py:785) — terminal workflow_runs
  status UPDATE + anchor-clear in one txn, keyed by workflow_runs.id. Accepts "cancelled".

Producer runs row (db/runs.py:54) — columns: run_id, thread_id, user_id, status, model, provider,
  spawned_by_worker, parent_run_id. A LIVE run has runs.status='streaming'.

admin.py:479-487 (Kill path — the correct discipline to mirror): late-imports _cancel_run_internals and calls it
  with run_id=<the run's own id>, status=row["status"], thread_id=str(row["thread_id"]).

Current broken loop — backend/app/api/workflows.py:509-528: SELECT wr.id, wr.status, wr.thread_id ... then
  _cancel_run_internals(run_id=r["id"], ...) where r["id"] is workflow_runs.id (the miss).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-01 — cancel in-flight runs through the PRODUCER identity, sentinel + finish_run before the delete</name>
  <files>backend/app/api/workflows.py</files>
  <read_first>
    - backend/app/api/workflows.py:475-553 (the delete_workflow_cascade route — the broken cancel loop at :509-528)
    - backend/app/api/admin.py:475-494 (the admin Kill path — mirror this cancel discipline)
    - backend/app/api/threads.py:2005-2016 (RUN_TASKS[run_id]=task registered under the PRODUCER runs.run_id)
    - backend/app/db/workflows.py:785-814 (finish_run — durable workflow_runs terminalize)
    - .planning/phases/152-workflow-run-inputs/152-REVIEW.md (CR-01 Fix block — the verbatim approach)
  </read_first>
  <behavior>
    - A workflow with a status='active' workflow_run whose thread has a status='streaming' producer runs row → the cascade cancels via the producer runs.run_id (the RUN_TASKS key), so the live engine task is actually cancelled.
    - The ask_user cancel sentinel is published keyed by the WORKFLOW run id (paused ask_user harness prompts wake).
    - The workflow_runs row is durably terminalized (finish_run → "cancelled") before the delete, covering the paused + cross-worker WORKER_COUNT=2 cases.
    - All of this remains cancel-FIRST — strictly before delete_published_workflow_cascade's transaction; the existing structure (cancel loop, then delete, then audit) is preserved.
  </behavior>
  <action>
    In `delete_workflow_cascade`, replace the in-flight query (currently `SELECT wr.id, wr.status, wr.thread_id FROM workflow_runs wr JOIN workflow_definitions wd ... WHERE ... wr.status IN ('active','paused','cap_paused')`) with a query that ALSO resolves the live producer identity: keep the `workflow_definitions` join, add `LEFT JOIN runs r ON r.thread_id = wr.thread_id AND r.status = 'streaming'`, and select `wr.id AS wf_id, wr.thread_id, r.run_id AS producer_id, r.status AS producer_status`. Keep the same owner-gated WHERE (`wd.slug = $1 AND wd.created_by = $2 AND wr.status IN ('active','paused','cap_paused')`) and the same `$N` binding — never an f-string on user values.

    For each in-flight row, in cancel-first order OUTSIDE the delete txn: (1) if `r["producer_id"] is not None`, call the late-imported `_cancel_run_internals(run_id=r["producer_id"], status=r["producer_status"], thread_id=str(r["thread_id"]) if r["thread_id"] else None, redis=redis, supabase=supabase)` — mirror the admin.py:479 Kill discipline; this is the ONLY change to the cancel identity (producer, not `r["id"]`). (2) `await publish_cancel_sentinel(redis, r["wf_id"])` (late-import from `app.services.ask_user_service`) so a paused ask_user harness prompt wakes on the workflow-run channel. (3) `await finish_run(pool, r["wf_id"], "cancelled")` (import from `app.db.workflows`) to durably terminalize the workflow_runs row before the delete — this is the cross-worker + paused backstop D-LOCK-05 needs.

    Do NOT touch `backend/app/api/threads.py` (D-08 / G-5 red line). Do NOT edit `run_lifecycle.py` (read-only reference — the primitives already exist). Keep the delete + best-effort audit receipt steps exactly as they are.
  </action>
  <acceptance_criteria>
    - SOURCE: `grep -n 'producer_id' backend/app/api/workflows.py` shows the SELECT aliases `r.run_id AS producer_id` and the cancel call uses `run_id=r["producer_id"]`.
    - SOURCE: `grep -n "LEFT JOIN runs r ON r.thread_id = wr.thread_id AND r.status = 'streaming'" backend/app/api/workflows.py` returns a match.
    - SOURCE: `grep -n 'run_id=r\["id"\]' backend/app/api/workflows.py` returns NO match (the wrong-identity cancel is gone).
    - SOURCE: `grep -n 'publish_cancel_sentinel\|finish_run' backend/app/api/workflows.py` shows both called inside the cascade route, before `delete_published_workflow_cascade`.
    - SOURCE: `git diff --name-only` for this task lists ONLY `backend/app/api/workflows.py` — `threads.py` and `run_lifecycle.py` are absent.
    - BEHAVIOR: `cd backend && venv/Scripts/python -m pytest tests/test_152_delete_cascade.py -q` still passes the 4 pre-existing helper tests (import + module load clean).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_152_delete_cascade.py -q</automated>
  </verify>
  <done>The cascade cancel loop resolves the producer runs.run_id via LEFT JOIN and cancels through it (never workflow_runs.id), publishes publish_cancel_sentinel(wf_id), and calls finish_run(wf_id,"cancelled") before the delete; threads.py and run_lifecycle.py untouched.</done>
</task>

<task type="auto">
  <name>Task 2: WR-01 — refuse an is_global cascade with cross-user runs (409) + correct the preview docstring</name>
  <files>backend/app/api/workflows.py, backend/app/db/workflows.py</files>
  <read_first>
    - backend/app/db/workflows.py:431-530 (delete_published_workflow_cascade + delete_workflow_cascade_preview — the misleading T-152-02-05 docstring is at :493-504)
    - backend/app/api/workflows.py:475-553 (the cascade route — add the guard after the owner-gate, before cancel-first)
    - .planning/phases/152-workflow-run-inputs/152-REVIEW.md (WR-01 Fix block)
  </read_first>
  <action>
    Add an owner-scoped helper in `backend/app/db/workflows.py` (e.g. `count_foreign_runs_on_global(pool, *, slug, user_id) -> int`) that resolves the caller's own version_ids for the slug WHERE `created_by = $2 AND is_global = true`, then returns `COUNT(*)` of `workflow_runs WHERE definition_id = ANY($1::uuid[]) AND user_id <> $2` — the count of OTHER users' runs on the caller's global definitions. `$N` / `ANY($1::uuid[])` binding only. A non-global or foreign slug yields 0.

    In `delete_workflow_cascade` (`api/workflows.py`), after `_owned_slug_or_404` resolves the slug and BEFORE the cancel-first loop, call the helper; if it returns > 0, raise `HTTPException(status_code=status.HTTP_409_CONFLICT, ...)` with an honest, human-readable detail (e.g. "This is a shared workflow with runs owned by other users — deleting it here would remove their run history, so it's blocked."). This fail-closes the WR-01 blast radius: a global starter's owner can no longer silently cancel/delete every user's runs. Non-global workflows and global workflows with only the owner's own runs are unaffected.

    Correct the `delete_workflow_cascade_preview` docstring: the DEFINITIONS are owner-scoped, but the `runs`/`threads`/`in_flight` counts DO aggregate across all users for an is_global definition (runs.user_id is the runner, not the definition owner). Remove/replace the "no cross-user count leak … computed over the caller's OWN definitions" claim with the honest statement that counts are over the caller's own definitions' RUNS regardless of runner, and that the WR-01 409 guard is what protects the destructive path for global definitions.

    Do NOT touch threads.py. No migration.
  </action>
  <acceptance_criteria>
    - SOURCE: `grep -n 'is_global' backend/app/db/workflows.py` shows the new cross-user-run helper filtering `is_global = true` and `user_id <> $2`.
    - SOURCE: `grep -n 'HTTP_409_CONFLICT' backend/app/api/workflows.py` shows the cascade route raising 409 on the is_global cross-user path, placed after `_owned_slug_or_404` and before the cancel loop.
    - SOURCE: the `delete_workflow_cascade_preview` docstring no longer contains the phrase "no cross-user count leak" (`grep -c 'no cross-user count leak' backend/app/db/workflows.py` returns 0); it honestly describes the run-count aggregation.
    - SOURCE: all params in the new helper bind `$N` / `ANY($1::uuid[])` — `grep -n 'f"' backend/app/db/workflows.py` shows no f-string interpolating a user value into the new SQL.
    - BEHAVIOR: `cd backend && venv/Scripts/python -m pytest tests/test_152_delete_cascade.py -q` passes (the additive helper does not break the existing helper tests).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_152_delete_cascade.py -q</automated>
  </verify>
  <done>An is_global cascade with any other user's run is refused with 409 before cancel-first; the preview docstring honestly describes cross-user run aggregation; $N binding only; threads.py untouched; no migration.</done>
</task>

<task type="auto">
  <name>Task 3: IN-02 — route-level tests: producer-identity cancel-first + 404-both-routes + in_flight preview + WR-01 refuse</name>
  <files>backend/tests/test_152_delete_cascade.py</files>
  <read_first>
    - backend/tests/test_152_delete_cascade.py (whole file — the live pg_pool + two_users fixtures + seed helpers to reuse)
    - backend/tests/test_dual_mode_wiring.py:352-425 (the httpx ASGITransport + app.dependency_overrides route-harness pattern to copy)
    - backend/tests/conftest.py:79-130 (the global get_current_user = mock_user_data override + autouse restore)
    - backend/tests/test_148_require_visible.py (how a require_visible('...') feature-flag gate is satisfied/overridden in a route test)
    - backend/app/db/runs.py:52-58 (the producer runs INSERT columns — to seed a status='streaming' row)
  </read_first>
  <action>
    Extend `test_152_delete_cascade.py` with route-level tests using the httpx `AsyncClient(transport=ASGITransport(app=app))` + `app.dependency_overrides` harness (copy `test_dual_mode_wiring.py`). Reuse the existing `pg_pool` + `two_users` fixtures and seed helpers; patch `app.api.workflows.get_pg_pool` to return the live `pg_pool`, override `get_supabase`/`get_redis`, and seed the OWNER so it matches the `get_current_user` identity (align the seeded `created_by`/`user_id` with the current-user override the same way the existing route tests do; satisfy the `require_visible("workflow_authoring")` gate per test_148_require_visible). Add:

    (a) Producer-identity cancel test (the CR-01 backstop): seed an owner definition, a thread, a status='active' workflow_run on that thread, AND a real `runs` row on the SAME thread_id with status='streaming' (a distinct producer run_id). Spy `_cancel_run_internals` (patch `app.services.run_lifecycle._cancel_run_internals` with an AsyncMock returning "task_cancelled"), no-op `publish_cancel_sentinel` (patch on `app.services.ask_user_service`), and let/spy `finish_run`. Call `DELETE /workflows/{definition_id}/cascade` → 204. Assert the spy was awaited exactly once AND its `run_id` kwarg equals the PRODUCER runs.run_id and does NOT equal the workflow_runs.id.

    (b) 404 on both routes: with a valid authenticated owner, `DELETE /workflows/{random_uuid}/cascade` and `GET /workflows/{random_uuid}/delete-preview` (an id the owner does not own / does not exist) each return 404 — the `_owned_slug_or_404` boundary (the Phase-150 SQLi seam class), no existence leak.

    (c) in_flight preview: seed the owner's definition with one status='active' workflow_run → `GET /workflows/{definition_id}/delete-preview` returns 200 with `in_flight == 1` (and the existing runs/threads/versions counts).

    (d) WR-01 refuse: seed an owner definition with `is_global = true` plus a workflow_run whose `user_id` is the OTHER user → `DELETE /workflows/{definition_id}/cascade` returns 409, and after the call assert both the definition and the other user's run are UNTOUCHED (nothing cross-user deleted).

    Skip cleanly (never error) when local Postgres :54322 is unreachable — reuse the module's PG_AVAILABLE skip guard.
  </action>
  <acceptance_criteria>
    - SOURCE: `grep -n "status = 'streaming'\|status='streaming'\|'streaming'" backend/tests/test_152_delete_cascade.py` shows a seeded producer runs row with status='streaming'.
    - SOURCE: `grep -n 'producer\|_cancel_run_internals' backend/tests/test_152_delete_cascade.py` shows the spy asserting the producer run_id (and the `!= workflow_run_id` assertion).
    - SOURCE: `grep -n 'delete-preview\|/cascade' backend/tests/test_152_delete_cascade.py` shows both routes exercised (404 case + in_flight case + 409 case).
    - SOURCE: `grep -n 'HTTP_409_CONFLICT\|== 409\|409' backend/tests/test_152_delete_cascade.py` shows the WR-01 refuse assertion.
    - BEHAVIOR (the re-verification gate): `cd backend && venv/Scripts/python -m pytest tests/test_152_delete_cascade.py -q` exits 0 with the new route tests green (when PG :54322 is up; cleanly skipped otherwise).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_152_delete_cascade.py -q</automated>
  </verify>
  <done>Route-level tests prove the producer-identity cancel-first (spy asserts producer run_id != workflow_runs.id, called once), 404-collapse on both routes, preview in_flight==1 on an active run, and the WR-01 409 refuse leaving cross-user rows untouched — all green when PG is up, cleanly skipped otherwise.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → DELETE /workflows/{id}/cascade | untrusted `definition_id`; `_owned_slug_or_404` is the ONLY authz gate on a service-role (no-RLS-backstop) path |
| cascade route → live engine tasks | cancel loop must reach the producer task registry (RUN_TASKS) across WORKER_COUNT=2 |
| cascade route → other users' runs | an is_global definition's runs are owned by RUNNERS, not the definition owner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-152-05-01 | Tampering/EoP | delete_workflow_cascade on is_global | mitigate | Task 2 — refuse 409 when other users' runs exist; owner gate `_owned_slug_or_404` remains the definition-level boundary |
| T-152-05-02 | Information Disclosure | preview run/thread/in_flight counts aggregate cross-user on is_global | accept (documented) | Task 2 — docstring corrected to state the aggregation honestly; the destructive path is fail-closed by T-152-05-01; the read counts are owner-definition-scoped and low-sensitivity |
| T-152-05-03 | Tampering (data integrity) | delete-out-from-under a live engine (CR-01) | mitigate | Task 1 — cancel via producer runs.run_id + publish_cancel_sentinel(wf_id) + finish_run(wf_id) before the delete txn (D-LOCK-05) |
| T-152-05-04 | Tampering | TOCTOU: a run starts between the cancel loop and the delete txn | accept (residual) | narrow window; durable finish_run + SET NULL FK + cancel-first minimize it; matches the review's documented residual, no new surface |
| T-152-05-05 | Tampering | `_owned_slug_or_404` service-role SQLi seam (Phase-150 class) | mitigate | `$N` / `ANY($1::uuid[])` binding only (no f-string on user values); Task 3 asserts foreign-id → 404 on both routes |
| T-152-05-SC | Tampering | npm/pip/cargo installs | mitigate | no new packages this plan; slopcheck N/A (RESEARCH Package Legitimacy Audit = no-op) |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/test_152_delete_cascade.py -q` → green (route + helper tests; the producer-identity cancel test is the CR-01 re-verification backstop).
- `grep -n 'producer_id' backend/app/api/workflows.py` confirms the cancel uses the producer identity; `grep -n 'run_id=r\["id"\]' backend/app/api/workflows.py` returns nothing.
- `git diff --name-only` for this plan lists only `api/workflows.py`, `db/workflows.py`, `tests/test_152_delete_cascade.py` — `threads.py` absent (D-08 / G-5 red line).
- No `supabase/migrations/*.sql` added (no schema change).
- The live SC#10 destructive-delete UAT row (152-VALIDATION.md row 5) remains the MANUAL live gate — this plan makes it pass at the code+test level.
</verification>

<success_criteria>
- CR-01 fully closed: an in-flight kickoff-started run is cancelled by its producer identity, the sentinel + finish_run cover paused + cross-worker, all cancel-first.
- WR-01 closed: is_global cross-user cascade refused (409); preview docstring honest.
- IN-02 closed: route-level test proves producer-identity cancel-first, 404-collapse on both routes, in_flight preview, and the WR-01 refuse.
- No threads.py edit; no migration; no new packages.
</success_criteria>

<output>
Create `.planning/phases/152-workflow-run-inputs/152-05-SUMMARY.md` when done.
</output>
