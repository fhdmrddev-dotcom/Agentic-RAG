---
phase: 152-workflow-run-inputs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/db/workflows.py
  - backend/app/api/workflows.py
  - backend/tests/test_152_delete_cascade.py
autonomous: true
requirements: [WFIN-03]
user_setup: []

must_haves:
  truths:
    - "Deleting a workflow hard-deletes its definition + all versions + all runs (D-LOCK-04)"
    - "Threads are detached-but-kept: active_workflow_run_id set NULL, they become normal chats (D-LOCK-04)"
    - "An in-flight run is cancelled first via the 064 zombie-heal path, never deleted out from under the engine (D-LOCK-05)"
    - "The cascade endpoint lives in api/workflows.py, NEVER threads.py (D-08 / G-5 red line)"
    - "A non-owner delete collapses to 404 with no existence leak (owner app-gate, service role has no RLS backstop)"
    - "The victim-naming sheet's Removed/Kept counts come from a server preview read, never guessed (D-LOCK-03)"
    - "FK-safe order leaves no orphaned runs / phases / thread anchors (WFIN-03)"
  artifacts:
    - path: "backend/app/db/workflows.py"
      provides: "delete_published_workflow_cascade() + delete-preview count helper"
      contains: "delete_published_workflow_cascade"
    - path: "backend/app/api/workflows.py"
      provides: "distinct cascade DELETE route + delete-preview GET + cancel-first"
      contains: "cascade"
    - path: "backend/tests/test_152_delete_cascade.py"
      provides: "FK-safe cascade, owner 404, cancel-first, preview counts"
      contains: "test_"
  key_links:
    - from: "backend/app/api/workflows.py"
      to: "backend/app/services/run_lifecycle.py"
      via: "_cancel_run_internals cancel-first before delete"
      pattern: "_cancel_run_internals"
    - from: "backend/app/api/workflows.py"
      to: "backend/app/db/workflows.py"
      via: "delete_published_workflow_cascade call"
      pattern: "delete_published_workflow_cascade"
    - from: "backend/app/db/workflows.py"
      to: "workflow_runs / workflow_definitions"
      via: "FK-safe DELETE (runs before definitions)"
      pattern: "DELETE FROM workflow_runs"
---

<objective>
Add the WFIN-03 safe delete cascade — the backend half. A new owner-gated endpoint in `api/workflows.py` (D-08 red line — NEVER `threads.py`) cancels any in-flight run first (D-LOCK-05), then hard-deletes the definition + all versions + all runs in FK-safe order (D-LOCK-04), leaving threads detached-but-kept as normal chats and no orphaned runs/phases. A companion delete-preview read supplies the exact Removed/Kept counts the victim-naming sheet requires (D-LOCK-03).

Purpose: "Delete a workflow" must be safe, honest, and leave no orphans. No migration is needed — the immutability trigger is UPDATE-only and the only FK blocker (`workflow_runs.definition_id ON DELETE RESTRICT`) is satisfied by deleting runs first.
Output: `delete_published_workflow_cascade()` + a count/preview helper in `db/workflows.py`; a distinct cascade DELETE route + delete-preview GET in `api/workflows.py`; `test_152_delete_cascade.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/152-workflow-run-inputs/152-CONTEXT.md
@.planning/phases/152-workflow-run-inputs/152-RESEARCH.md
@.planning/phases/152-workflow-run-inputs/152-PATTERNS.md
@.planning/phases/152-workflow-run-inputs/152-VALIDATION.md

@backend/app/db/workflows.py

<interfaces>
<!-- Extracted this session — executor uses these directly. -->

db/workflows.py analogs:
  - create_workflow_run(...) :77 / txn shape :126-156  — the canonical acquire()→transaction() FK-ordered multi-write to mirror
  - delete_workflow_definition(pool, definition_id, *, user_id) -> bool  :404-427  — owner WHERE + status='draft' + RETURNING → bool (draft-only precedent)
  - get_definition 404-collapse precedent  :271/:279-285  — non-owner → treated as not-found

api/workflows.py analogs:
  - delete_draft route  @router.delete("/{definition_id}", 204, dependencies=[Depends(require_visible("workflow_authoring"))])  :372-401  (draft-only — DO NOT overload it, Pitfall 7)
  - _coerce_user_id(current_user), get_pg_pool(), get_current_user  (in-file helpers)

cancel-first (D-LOCK-05) — run_lifecycle.py:
  - _cancel_run_internals(*, run_id, status, thread_id, redis, supabase) -> "terminal_noop"|"task_cancelled"|"zombie_healed"  :158
    atomically co-writes terminal state, clears threads.active_workflow_run_id, EXPIREs the Redis run buffer
  - used verbatim by admin.py kill_run :479-487 (the 064 pattern) — get_redis() supplies redis

FK map (supabase/full-schema.sql — VERIFIED, drives the delete order):
  - workflow_runs.definition_id → workflow_definitions.id   ON DELETE RESTRICT  :3203  ← THE blocker; delete runs FIRST
  - workflow_phases.workflow_run_id → workflow_runs.id       ON DELETE CASCADE   :3195  ← auto when runs deleted
  - threads.active_workflow_run_id → workflow_runs.id        ON DELETE SET NULL  :3131  ← auto-detach (threads KEPT)
  - harness_audit.run_id: NO FK (nullable :859) → rows linger as append-only audit — KEEP (A3), no action
  - immutability trigger workflow_definitions_block_published is BEFORE UPDATE ONLY :2598 → does NOT block DELETE (no migration)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: db-layer cascade helper + delete-preview counts (FK-safe, one txn)</name>
  <files>backend/tests/test_152_delete_cascade.py, backend/app/db/workflows.py</files>
  <read_first>
    - backend/app/db/workflows.py (:77 create_workflow_run, :126-156 txn shape, :404-427 delete_workflow_definition owner-scoped delete, module header $N-only + owner-gate posture T-073-02/T-091-03)
    - 152-PATTERNS.md § "backend/app/db/workflows.py" (the cascade helper excerpt + FK-safe order) and § "Shared Patterns" (owner authorization, $N-only SQL)
    - 152-RESEARCH.md § "WFIN-03 — Delete cascade (deep dive)" (FK map, immutability-trigger-is-UPDATE-only, all-versions-by-slug A1, harness_audit KEEP A3, preview counts)
  </read_first>
  <behavior>
    - Test (all-versions, A1): delete-by-slug for the owner removes ALL rows sharing that slug (definition + every version), not just one version.
    - Test (FK-safe / no orphans): after cascade, `workflow_runs` for those definition_ids = 0, `workflow_phases` for those runs = 0 (auto-cascade), and previously-linked threads survive with `active_workflow_run_id IS NULL`.
    - Test (owner gate): a slug owned by another user resolves to 0 version_ids → helper returns `{"deleted": False}` (route maps to 404 — no existence leak).
    - Test (preview counts): the preview helper returns `{name, versions, runs, threads}` where runs = COUNT(*) of workflow_runs for the versions and threads = COUNT(DISTINCT thread_id) of the kept threads, all owner-scoped, matching reality.
  </behavior>
  <action>
    Add `async def delete_published_workflow_cascade(pool, *, slug: str, user_id: UUID) -> dict` to db/workflows.py, mirroring `create_workflow_run`'s `acquire() → transaction()` shape. Inside ONE transaction: resolve `version_ids = SELECT id FROM workflow_definitions WHERE slug=$1 AND created_by=$2` (owner-scoped, all versions — A1); empty → return `{"deleted": False}`. Then `DELETE FROM workflow_runs WHERE definition_id = ANY($1::uuid[])` (FIRST — the ON DELETE RESTRICT blocker; this auto-cascades workflow_phases and auto-SET-NULLs threads.active_workflow_run_id), then `DELETE FROM workflow_definitions WHERE id = ANY($1::uuid[])`. Return `{"deleted": True, "name": ..., "versions": len(version_ids), "runs": <deleted run count>}`. Bind everything `$N` / `ANY($1::uuid[])` — NEVER f-string on user values. Do NOT touch `harness_audit` (KEEP as append-only; its `run_id` has no FK — A3). Do NOT amend the immutability trigger (UPDATE-only; no migration).
    Add `async def delete_workflow_cascade_preview(pool, *, slug: str, user_id: UUID) -> dict` returning `{name, versions, runs, threads}` for the sheet — owner-scoped `COUNT(*)` on workflow_runs and `COUNT(DISTINCT thread_id)` for kept threads. Non-owner / unknown slug → a not-found marker the route maps to 404.
    Create `backend/tests/test_152_delete_cascade.py` — an integration test against real FKs (live local PG) plus the owner-gate unit case. Measure against the captured baseline, not zero-fail.
  </action>
  <acceptance_criteria>
    - `grep -n "async def delete_published_workflow_cascade\|async def delete_workflow_cascade_preview" backend/app/db/workflows.py` returns both helpers.
    - Source review: `DELETE FROM workflow_runs` executes BEFORE `DELETE FROM workflow_definitions`; all params bind `$N` / `ANY($1::uuid[])`; no f-string on slug/user_id/version_ids; `harness_audit` is not referenced.
    - `cd backend && venv/Scripts/python -m pytest tests/test_152_delete_cascade.py -x -q` passes (all-versions, no-orphans, owner-404, preview-counts).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_152_delete_cascade.py -x -q</automated>
  </verify>
  <done>The FK-safe cascade + preview helpers exist, delete all versions by slug owner-scoped, leave no orphaned runs/phases and detached-but-kept threads, and are $N-parameterized.</done>
</task>

<task type="auto">
  <name>Task 2: distinct cascade DELETE route + delete-preview GET + cancel-first (api/workflows.py)</name>
  <files>backend/app/api/workflows.py</files>
  <read_first>
    - backend/app/api/workflows.py (:372-401 delete_draft — the endpoint/gate analog; _coerce_user_id, get_pg_pool, get_current_user, require_visible imports)
    - backend/app/api/admin.py (:479-487 kill_run — the _cancel_run_internals cancel-first delegation; get_redis())
    - backend/app/services/run_lifecycle.py (:158 _cancel_run_internals signature + outcome discriminator)
    - backend/app/db/workflows.py (the Task-1 helpers)
    - 152-PATTERNS.md § "backend/app/api/workflows.py" (distinct-route requirement, cancel-first, preview endpoint) and 152-RESEARCH.md § "Route + gate" (A2)
  </read_first>
  <action>
    Add a DISTINCT route in api/workflows.py — `@router.delete("/{definition_id}/cascade", status_code=204, dependencies=[Depends(require_visible("workflow_authoring"))])` (A2) — do NOT overload the draft `DELETE /{definition_id}` (Pitfall 7 — the client `deleteWorkflowDraft` already owns that path). Resolve the target definition by id, owner-check `created_by == current_user` and map non-owner/unknown → 404 (no existence leak, the get_definition collapse precedent). Determine the `slug` from the owned definition, then BEFORE the db txn, loop every in-flight run (status `active`/`paused`/`cap_paused`) for that slug's definition_ids and call `_cancel_run_internals(run_id=..., status=..., thread_id=..., redis=get_redis(), supabase=supabase)` (D-LOCK-05 — cancel-first lives in the route/service layer, NOT inside the db txn). Then call `delete_published_workflow_cascade(pool, slug=slug, user_id=user_id)`; a `{"deleted": False}` → 404. Write the audit receipt (victim-naming: "Deleted {name} — recorded with your name") via the existing operator/audit write seam.
    Add `@router.get("/{definition_id}/delete-preview")` returning `delete_workflow_cascade_preview(...)` `{name, versions, runs, threads}` — same owner gate + 404 collapse — so the sheet shows exact counts before commit (A6). Wrap all blocking supabase/psycopg calls in `run_in_threadpool` (D-v2.5-01). Do NOT add any surface to `threads.py` (D-08 / G-5).
  </action>
  <acceptance_criteria>
    - `grep -n "cascade\|delete-preview\|_cancel_run_internals" backend/app/api/workflows.py` shows the new DELETE `/{definition_id}/cascade` route, the GET `/{definition_id}/delete-preview` route, and the cancel-first call.
    - Source review: cancel-first runs BEFORE `delete_published_workflow_cascade`; owner check maps non-owner → 404; the route carries `Depends(require_visible("workflow_authoring"))`; the draft `DELETE /{definition_id}` route is unchanged.
    - `git diff --name-only` does NOT include `backend/app/api/threads.py` (D-08 red line).
    - `cd backend && venv/Scripts/python -m pytest tests/test_152_delete_cascade.py -x -q` green.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_152_delete_cascade.py -x -q && grep -c "delete-preview" backend/app/api/workflows.py</automated>
  </verify>
  <done>A distinct owner-gated cascade DELETE + delete-preview GET exist in api/workflows.py; in-flight runs are cancelled first; threads.py is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → api/workflows.py delete | `definition_id` is untrusted; the delete runs as service role (RLS bypassed) |
| service role → DB cascade | the app-layer owner WHERE is the ONLY authorization boundary (no RLS backstop) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-152-02-01 | EoP (IDOR) | cascade DELETE route + db helper | mitigate | Owner app-gate `created_by = $user` on the definition + `WHERE created_by=$2` in the helper; non-owner collapses to 404 (no existence leak). Service role → the WHERE is load-bearing (no RLS backstop). |
| T-152-02-02 | Availability | delete of a live run | mitigate | Cancel-first via the shared `_cancel_run_internals` zombie-heal BEFORE the DB delete (D-LOCK-05); never delete a run out from under the engine. |
| T-152-02-03 | Integrity (orphans) | FK cascade order | mitigate | Delete runs FIRST (RESTRICT satisfied) → phases auto-cascade → thread anchors auto-SET-NULL (threads kept) → then versions (D-LOCK-04). |
| T-152-02-04 | Tampering (SQLi) | slug / version_ids binding | mitigate | `$N` / `ANY($1::uuid[])` only; slug is a param, version_ids a uuid[] param; no f-string on user values. |
| T-152-02-05 | Info Disclosure | delete-preview counts | mitigate | Preview counts are owner-scoped (same `created_by` gate) — no cross-user count leak; unknown/non-owner slug → 404. |
| T-152-02-SC | Tampering | dependency installs | accept | This plan installs NO packages (RESEARCH audit = no-op). slopcheck N/A. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/test_152_delete_cascade.py -x -q` green (all-versions / no-orphans / owner-404 / cancel-first / preview-counts).
- Source review: FK-safe order (runs before definitions); cancel-first precedes the txn; owner gate collapses to 404; `require_visible("workflow_authoring")` present.
- `git diff --name-only` excludes `threads.py` (D-08 / G-5).
- No migration added (immutability trigger is UPDATE-only; FK blocker satisfied by ordering).
- Full backend suite delta vs the SEED-056 baseline = 0 new failures.
</verification>

<success_criteria>
- A user can delete a workflow with a safe cascade behind confirmation, leaving no orphaned runs/threads (WFIN-03).
- Definition + all versions + all runs hard-deleted; threads detached-but-kept as normal chats; KB untouched (D-LOCK-04).
- In-flight runs cancelled first (D-LOCK-05); endpoint in api/workflows.py, never threads.py (D-08).
- Non-owner → 404; preview counts are exact and server-sourced (D-LOCK-03).
</success_criteria>

<output>
Create `.planning/phases/152-workflow-run-inputs/152-02-SUMMARY.md` when done.
</output>
