---
phase: 151-agent-file-tools
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/workspace.py
  - frontend/src/components/panel/TemplateUpload.tsx
  - backend/tests/unit/test_151_upload_allowlist.py
autonomous: true
requirements: [FILE-01]
must_haves:
  truths:
    - "The mid-chat template upload accepts real skill assets (.md/.json/.csv/.txt/.py/.js/.sh + images) beyond OOXML (D-09)"
    - "The widened allowlist keeps the magic-byte gate and size guard for every new type — a renamed binary is still rejected (T-02)"
    - "Uploaded files keep the kind='template_input' untrusted provenance stamp and are never routed to the Jinja engine (T-02)"
    - "The frontend accept= list is widened in lockstep with the backend allowlist (D-09)"
  artifacts:
    - path: "backend/app/api/workspace.py"
      provides: "generalized validate_upload replacing OOXML-only validate_ooxml"
      contains: "validate_upload"
    - path: "frontend/src/components/panel/TemplateUpload.tsx"
      provides: "widened accept= file picker"
    - path: "backend/tests/unit/test_151_upload_allowlist.py"
      provides: "allowlist accept/reject + provenance unit coverage"
  key_links:
    - from: "upload_template route"
      to: "validate_upload"
      via: "magic-byte/size validation at the door"
      pattern: "validate_upload"
    - from: "TemplateUpload.tsx accept="
      to: "workspace.py _ALLOWED_EXT"
      via: "lockstep allowlist"
      pattern: "accept="
---

<objective>
SC#3 — let a user hand the agent a template mid-conversation. Reuse Phase-100's `TemplateUpload` → `POST /workspace/files` (`upload_template`) path and WIDEN the magic-byte allowlist (D-09) beyond `.docx/.pptx/.xlsx` to real skill assets (scripts, `.md`, `.json`, `.csv`, images) so they can be handed in and then attached to a skill via FILE-01 source #1. No net-new upload surface.

Purpose: The uploaded file lands in the thread workspace; the agent then calls `attach_skill_file(source="workspace")`. This plan only widens the gate — it does not touch the tool.
Output: A generalized `validate_upload` (magic-byte + size gate preserved for all new types, `kind='template_input'` provenance intact) + a lockstep `TemplateUpload.tsx` `accept=` widen. `threads.py` untouched.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/151-agent-file-tools/151-CONTEXT.md
@.planning/phases/151-agent-file-tools/151-RESEARCH.md
@.planning/phases/151-agent-file-tools/151-PATTERNS.md

<interfaces>
<!-- The one gate to widen (workspace.py:114-146) + the frontend lockstep. -->
_ALLOWED_EXT (workspace.py:114): {".docx", ".pptx", ".xlsx"}   ← widen this set (D-09)
_OOXML_MARKER (workspace.py:116): per-ext part-name markers (word/ ppt/ xl/) — keep for OOXML branch
validate_ooxml(filename, raw) -> str (workspace.py:119): ext gate → size guard (MAX_FILE_SIZE, 10MB) → zipfile.is_zipfile → [Content_Types].xml + marker. Raises HTTPException(422).
upload_template route (workspace.py:149-204): _verify_thread_ownership → size pre-check → validate_ooxml → ws_write_file(kind="template_input", expires_at). WR-05 filename sanitize at :184-185. kind='template_input' at :195.
Frontend: TemplateUpload.tsx:53 → accept=".docx,.pptx,.xlsx"
conftest fixtures (conftest.py:966-1033): valid_docx_bytes (:993), renamed_binary_bytes (:1012, MZ-header EXE), oversized_ooxml_bytes (:1022).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — widened-allowlist validator tests (RED)</name>
  <files>backend/tests/unit/test_151_upload_allowlist.py</files>
  <read_first>
    - backend/app/api/workspace.py (validate_ooxml :119-146; _ALLOWED_EXT :114)
    - backend/tests/conftest.py (:966-1033 — valid_docx_bytes, renamed_binary_bytes, oversized_ooxml_bytes)
    - .planning/phases/151-agent-file-tools/151-PATTERNS.md (validate_upload section — per-category branches)
    - .planning/phases/151-agent-file-tools/151-RESEARCH.md (Code Examples D-09 + A6 validation rules)
  </read_first>
  <behavior>
    Import validate_upload from app.api.workspace (renamed/generalized validate_ooxml). Assert:
    - Accepts a valid .docx (valid_docx_bytes) — OOXML branch unchanged
    - Accepts text-ish types: a utf-8 `.md`, `.json`, `.csv`, `.txt`, `.py` payload returns its canonical ext (no raise)
    - Accepts an image: a `\x89PNG` byte header `.png` returns ".png"
    - REJECTS renamed_binary_bytes uploaded as `.md`/`.png` (MZ EXE header → NUL-heavy / wrong magic → 422)
    - REJECTS oversized_ooxml_bytes (> MAX_FILE_SIZE → 422) for ALL new types (office-bomb/DoS guard survives)
    - REJECTS an unknown extension (e.g. `.exe`) → 422
  </behavior>
  <action>
    Author the test file as failing tests (validate_upload does not exist yet → RED). Use the existing conftest fixtures; for text/image cases construct small byte payloads inline. Assert 422 via pytest.raises(HTTPException) for the reject cases. Keep every assertion mechanically checkable (ext return value or raised status).
  </action>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/unit/test_151_upload_allowlist.py -x` FAILS with ImportError (validate_upload absent) — RED confirmed
    - Test covers accept (docx/md/json/csv/png), reject (renamed binary, oversize, unknown ext)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_151_upload_allowlist.py -x 2>&1 | grep -E "Error|FAILED|collected"</automated>
  </verify>
  <done>Test file exists, exercises the widened accept + preserved reject behavior, and fails RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generalize validate_ooxml → validate_upload + lockstep TemplateUpload.tsx accept= (GREEN)</name>
  <files>backend/app/api/workspace.py, frontend/src/components/panel/TemplateUpload.tsx</files>
  <read_first>
    - backend/app/api/workspace.py (validate_ooxml :119-146; upload_template :149-204; kind='template_input' :195; WR-05 sanitize :184)
    - frontend/src/components/panel/TemplateUpload.tsx (:53 accept=)
    - .planning/phases/151-agent-file-tools/151-PATTERNS.md (validate_upload widen — exact per-category branches)
  </read_first>
  <action>
    (a) workspace.py: rename/generalize `validate_ooxml` → `validate_upload(filename, raw) -> str`, widening `_ALLOWED_EXT` to include text-ish (`.md`,`.json`,`.csv`,`.txt`,`.py`,`.js`,`.sh`) and image (`.png`,`.jpg`,`.jpeg`,`.gif`,`.webp`) types plus the existing `.docx`,`.pptx`,`.xlsx`. Branch by category: OOXML → keep the exact ZIP/`[Content_Types].xml`/marker check; text-ish → utf-8-decodable sanity + reject NUL-heavy/binary payloads; images → verify leading magic bytes (`\x89PNG`, `\xFF\xD8\xFF`, `GIF8`, `RIFF`…`WEBP`). Keep the `len(raw) > MAX_FILE_SIZE` size guard trip for EVERY type BEFORE any parsing (office-bomb DoS — T-02). Preserve the same-signature return of the canonical ext and HTTPException(422) on failure. Update the `upload_template` call site to `validate_upload` (all other route logic — ownership 404, size pre-check, WR-05 sanitize, `kind='template_input'`, expires_at — unchanged; provenance MUST stay `template_input`).
    (b) TemplateUpload.tsx:53: widen `accept=` to the same extension set (lockstep with `_ALLOWED_EXT`). This is a data-array widen only — no UI redesign. Do NOT touch threads.py.
    Do NOT route `template_input` files to the docxtpl Jinja engine (T-02 provenance lock — the Jinja render path takes owner-authored library templates only).
  </action>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/unit/test_151_upload_allowlist.py -x` → GREEN
    - `grep -n "template_input" backend/app/api/workspace.py` still present in upload_template (provenance preserved)
    - `grep -n "MAX_FILE_SIZE" backend/app/api/workspace.py` shows the size guard still trips inside validate_upload for all types
    - TemplateUpload.tsx accept= includes at minimum `.md,.json,.csv,.png` alongside the OOXML types; `cd frontend && npx vite build` exit 0
    - `git diff --name-only` does NOT include backend/app/api/threads.py
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_151_upload_allowlist.py -x</automated>
  </verify>
  <done>validate_upload accepts widened skill-asset types while preserving magic-byte + size gate + template_input provenance; frontend accept= matches; no threads.py touch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser upload → workspace_files | User-supplied file bytes + filename cross into stored workspace state (untrusted) |
| widened allowlist → later skill exec | A `template_input` file can be attached to a skill (FILE-01 #1) and a Python skill file is executable via execute_code injection — the widen must not weaken the gate |

## STRIDE Threat Register — SC#3 upload widen (T-02 input-validation portion)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-151-03-01 | Tampering | renamed binary bypassing widened allowlist | mitigate | Per-category magic-byte check survives the widen (OOXML ZIP marker; text utf-8/NUL reject; image leading magic). Proven: renamed_binary_bytes as `.md`/`.png` → 422 (unit) |
| T-151-03-02 | Denial of Service | office/zip bomb or huge file on new types | mitigate | `len(raw) > MAX_FILE_SIZE` size guard trips BEFORE any parse for every type. Proven: oversized_ooxml_bytes → 422 (unit) |
| T-151-03-03 | Elevation of Privilege | untrusted upload → skill → execute_code | mitigate | `kind='template_input'` provenance stamp preserved on the workspace row; `template_input` files NEVER routed to the docxtpl Jinja engine (RESEARCH lock). Full untrusted-file→skill→exec chain reasoned about in Plan 04's FILE-01 threat model (T-02) |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/unit/test_151_upload_allowlist.py -x` green.
- workspace.py `upload_template` still stamps `kind='template_input'`; size guard intact.
- Frontend builds; accept= widened in lockstep.
- No threads.py touch.
</verification>

<success_criteria>
- A user can upload `.md/.json/.csv/.py/.png` (and OOXML) mid-chat; renamed binaries and oversized files are still refused.
- Provenance stamp survives the widen; Jinja engine still receives only owner-authored templates.
</success_criteria>

<output>
Create `.planning/phases/151-agent-file-tools/151-03-SUMMARY.md` when done.
</output>
