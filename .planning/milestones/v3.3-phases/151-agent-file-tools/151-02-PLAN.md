---
phase: 151-agent-file-tools
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/101_skill_files_unique_index.sql
  - supabase/full-schema.sql
autonomous: false
requirements: [FILE-01]
must_haves:
  truths:
    - "A UNIQUE index on skill_files(skill_id, filename) exists in the live LOCAL database (D-10)"
    - "The migration enables the race-immune D-07 overwrite via PostgREST .upsert(on_conflict=skill_id,filename) under WORKER_COUNT=2"
    - "supabase/full-schema.sql is regenerated from the live DB (never hand-edited) and reflects the new index"
    - "Migration 101 is recorded as a PENDING CLOUD APPLY that must land before Phase 151 ships live"
  artifacts:
    - path: "supabase/migrations/101_skill_files_unique_index.sql"
      provides: "ADDITIVE unique index enabling D-07 upsert"
      contains: "CREATE UNIQUE INDEX"
    - path: "supabase/full-schema.sql"
      provides: "regenerated single-file bootstrap artifact including the new index"
  key_links:
    - from: "skill_files(skill_id, filename)"
      to: "PostgREST upsert on_conflict"
      via: "unique index"
      pattern: "skill_files_skill_filename_uniq"
---

<objective>
Ship the D-10 migration: a UNIQUE index on `skill_files(skill_id, filename)` so FILE-01's D-07 overwrite-in-place is a clean, race-immune PostgREST upsert (`on_conflict=skill_id,filename`) correct under `WORKER_COUNT=2`. `skill_files` has NO such constraint today, so a read-check-then-write would race under two workers.

Purpose: HARD gate before FILE-01's upsert verification — the upsert path CANNOT work without this index, and a build/type check passes without it (false-positive verification). FILE-01 (Plan 04) `depends_on` this plan.
Output: `supabase/migrations/101_skill_files_unique_index.sql` authored + applied to the live LOCAL DB via the Supabase SQL editor + `full-schema.sql` regenerated + cloud-parity obligation recorded.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/151-agent-file-tools/151-CONTEXT.md
@.planning/phases/151-agent-file-tools/151-PATTERNS.md

<interfaces>
<!-- Migration convention + analog. CLAUDE.md rule: NEVER db push / db reset. -->
Analog migration: supabase/migrations/075_document_relationships_idempotency_index.sql
  (additive partial unique index, IF NOT EXISTS, full APPLY/regenerate/CLOUD-PARITY header).
Filename rule: <digits>_name.sql — next free number is 101 (100_secret_key_columns.sql is the latest).
  Letter suffixes (101b) are SILENTLY SKIPPED by the Supabase CLI.
Target DDL:
  CREATE UNIQUE INDEX IF NOT EXISTS skill_files_skill_filename_uniq
      ON public.skill_files (skill_id, filename);
skill_files table today (full-schema.sql:1116): PK id + non-unique indexes on skill_id/user_id; NO (skill_id, filename) unique constraint.
Apply protocol (CLAUDE.md): paste into local Supabase SQL editor OR psycopg2 to 127.0.0.1:54322;
  then `bash scripts/regenerate-full-schema.sh` (NO --reset — live-DB dump); commit migration + full-schema.sql together.
Cloud-parity tracker: scripts/pending-cloud-migrations.sh (standing production-push checklist).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 101_skill_files_unique_index.sql</name>
  <files>supabase/migrations/101_skill_files_unique_index.sql</files>
  <read_first>
    - supabase/migrations/075_document_relationships_idempotency_index.sql (copy header structure verbatim, adapt table/columns)
    - supabase/migrations/100_secret_key_columns.sql (confirm 100 is the latest number → 101 is free)
    - .planning/phases/151-agent-file-tools/151-PATTERNS.md (migration section — exact header + DDL)
    - CLAUDE.md (migration rules: <digits>_name.sql, never db push/reset, regenerate full-schema)
  </read_first>
  <action>
    Create `supabase/migrations/101_skill_files_unique_index.sql`. Header (comment block, mirror 075): state it is Phase 151 (FILE-01 / D-10), ADDITIVE, enables race-immune D-07 overwrite via `.upsert(on_conflict="skill_id,filename")` under WORKER_COUNT=2, notes `skill_files` has NO (skill_id, filename) unique constraint today (full-schema.sql:1116), `IF NOT EXISTS` makes re-apply a no-op, APPLY-by-SQL-editor-never-db-push instruction, regenerate full-schema (no --reset), and CLOUD PARITY: pending cloud apply BEFORE Phase 151 ships live. Body: `CREATE UNIQUE INDEX IF NOT EXISTS skill_files_skill_filename_uniq ON public.skill_files (skill_id, filename);`. Do NOT touch full-schema.sql in this task (Task 2 regenerates it). Do NOT apply it here.
  </action>
  <acceptance_criteria>
    - File exists at `supabase/migrations/101_skill_files_unique_index.sql`, filename matches `^[0-9]+_.*\.sql$` (no letter suffix)
    - `grep -c "CREATE UNIQUE INDEX IF NOT EXISTS skill_files_skill_filename_uniq" supabase/migrations/101_skill_files_unique_index.sql` == 1
    - Header contains "CLOUD PARITY" (case-insensitive) and a "db push" negation instruction
  </acceptance_criteria>
  <verify>
    <automated>ls supabase/migrations/101_skill_files_unique_index.sql && grep -c "skill_files_skill_filename_uniq" supabase/migrations/101_skill_files_unique_index.sql</automated>
  </verify>
  <done>Migration file authored, correct number/name, additive unique index, full apply/parity header — but NOT yet applied and full-schema.sql untouched.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Operator applies migration to live local DB + regenerate full-schema.sql</name>
  <files>supabase/migrations/101_skill_files_unique_index.sql, supabase/full-schema.sql</files>
  <read_first>
    - supabase/migrations/101_skill_files_unique_index.sql (the file authored in Task 1)
    - CLAUDE.md (migration apply protocol — SQL editor, never db push/reset; regenerate-full-schema.sh)
    - scripts/regenerate-full-schema.sh (the no-reset live-DB dump)
  </read_first>
  <action>[OPERATOR MANUAL STEP — no CLI equivalent per the project rule that forbids `supabase db push`/`db reset`.] The operator pastes the entire contents of `supabase/migrations/101_skill_files_unique_index.sql` into the local Supabase SQL editor (or psycopg2 to 127.0.0.1:54322) and runs it (idempotent). Then confirm the index exists via `pg_indexes`, run `bash scripts/regenerate-full-schema.sh` (NO --reset) to rebuild `supabase/full-schema.sql` from the live dump (never hand-edit it), and record migration 101 as a pending cloud apply in the pending-cloud-migrations tracking. Do NOT touch cloud Supabase now.</action>
  <what-built>Migration `101_skill_files_unique_index.sql` (a `CREATE UNIQUE INDEX ... skill_files(skill_id, filename)`) is authored and ready to apply. It CANNOT be applied via `supabase db push`/`db reset` (project rule — would wipe dev data); it must be pasted into the Supabase SQL editor against the live LOCAL database.</what-built>
  <how-to-verify>
    1. Open the local Supabase SQL editor (or psycopg2 to 127.0.0.1:54322) and paste the entire contents of `supabase/migrations/101_skill_files_unique_index.sql`; run it. Expect success (idempotent — safe to re-run).
    2. Confirm the index exists: `SELECT indexname FROM pg_indexes WHERE tablename='skill_files' AND indexname='skill_files_skill_filename_uniq';` returns one row.
    3. Run `bash scripts/regenerate-full-schema.sh` (NO `--reset`) to rebuild `supabase/full-schema.sql` from the live DB dump. Do NOT hand-edit full-schema.sql.
    4. Add migration 101 to the pending-cloud-migrations tracking (it is a PENDING CLOUD APPLY that must land on cloud Supabase before Phase 151 ships live — standing parity rule; do NOT touch cloud now).
  </how-to-verify>
  <acceptance_criteria>
    - `pg_indexes` shows `skill_files_skill_filename_uniq` on `skill_files` in the live local DB
    - `supabase/full-schema.sql` contains `skill_files_skill_filename_uniq` after regeneration
    - full-schema.sql regenerated with NO --reset (dev data preserved)
    - Cloud-parity obligation recorded for migration 101
  </acceptance_criteria>
  <verify>
    <automated>grep -c "skill_files_skill_filename_uniq" supabase/full-schema.sql</automated>
  </verify>
  <resume-signal>Type "applied" once the index exists in the live local DB and full-schema.sql is regenerated, or describe the failure.</resume-signal>
  <done>The unique index is live in the local DB, full-schema.sql reflects it, and cloud parity is tracked. FILE-01's upsert can now function.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live schema | DDL applied by an operator; additive-only, no data rewrite |

## STRIDE Threat Register — Migration 101 (D-10)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-151-02M-01 | Tampering | concurrent same-filename attach under WORKER_COUNT=2 | mitigate | The unique index collapses the read-check-then-write race to one winner; `.upsert(on_conflict=skill_id,filename)` becomes atomic (this migration is the enabling mitigation for FILE-01's D-07) |
| T-151-02M-02 | Denial of Service | index build fails on pre-existing duplicate tuples | accept | `IF NOT EXISTS`; skill_files has no duplicate `(skill_id, filename)` tuples in dev (the per-file insert path never created same-name-same-skill rows before D-07); operator verifies the apply succeeds |
| T-SC | Tampering | package installs | N/A | No npm/pip/cargo install in this phase — the Package Legitimacy Audit is N/A (RESEARCH confirms) |
</threat_model>

<verification>
- Local DB `pg_indexes` shows `skill_files_skill_filename_uniq`.
- `supabase/full-schema.sql` regenerated (no --reset) and contains the index name.
- Migration 101 recorded in the pending-cloud-migrations checklist.
</verification>

<success_criteria>
- The `(skill_id, filename)` unique index is live locally and captured in the bootstrap artifact.
- FILE-01's Plan 04 upsert has its hard dependency satisfied.
- Cloud-parity obligation is explicit for the eventual production push.
</success_criteria>

<output>
Create `.planning/phases/151-agent-file-tools/151-02-SUMMARY.md` when done. Note the cloud-parity obligation for migration 101.
</output>
