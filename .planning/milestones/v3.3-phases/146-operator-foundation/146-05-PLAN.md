---
phase: 146-operator-foundation
plan: 05
type: execute
wave: 3
depends_on: [146-04]
files_modified:
  - frontend/src/components/admin/OperatorBand.tsx
  - frontend/src/components/admin/HealthSignals.tsx
  - frontend/src/components/admin/LockedTab.tsx
  - frontend/src/components/admin/TechnicalNamesToggle.tsx
  - frontend/src/components/admin/RecentActionsCard.tsx
autonomous: true
requirements: [ADMIN-01]
user_setup: []

must_haves:
  truths:
    - "The operator band renders the 061-B zone identity per D-07: amber Shield + 'Control Room' + OPERATOR chip + operator identity + 'every action recorded' marker + '‹ Back to app'"
    - "Health signals render the four real /admin/backpressure values under plain labels per D-07 (Server capacity · Agents working · Database connections · Work spread); an '⌥ Technical names' toggle reveals the raw field names (anyio_threadpool_depth etc.) — the LANG-01 two-audience pattern, born plain at 146"
    - "Locked tabs (System Controls / Users & Access / AI Models / API Keys) render a calm 'Not built yet — coming soon' refusal with NO phase numbers in shipped copy per D-07"
    - "RecentActionsCard is the receipt per D-08: actions land as top rows (the ledger IS the receipt, no toasts, no counters); write actions carry a leading ✎ mark, views do not"
    - "The shield glyph is a plain lucide Shield with an amber tint, distinct from Governance's ShieldCheck"
  artifacts:
    - path: "frontend/src/components/admin/OperatorBand.tsx"
      provides: "amber operator zone band (061-B)"
      contains: "OPERATOR"
    - path: "frontend/src/components/admin/HealthSignals.tsx"
      provides: "4 plain-labeled backpressure signals + technical-names reveal"
      contains: "Server capacity"
    - path: "frontend/src/components/admin/LockedTab.tsx"
      provides: "calm coming-soon refusal, no phase numbers"
      contains: "coming soon"
    - path: "frontend/src/components/admin/RecentActionsCard.tsx"
      provides: "ledger-is-receipt card with ✎ write mark"
      contains: "RecentActionsCard"
  key_links:
    - from: "frontend/src/components/admin/HealthSignals.tsx"
      to: "BackpressureSignals"
      via: "props typed from api.ts"
      pattern: "BackpressureSignals"
    - from: "frontend/src/components/admin/RecentActionsCard.tsx"
      to: "OperatorAuditRow"
      via: "props typed from api.ts"
      pattern: "OperatorAuditRow"
---

<objective>
Build the five sketch-driven presentational leaf components for the Control Room — pure, prop-driven,
no data fetching (the shell in Plan 06 fetches and passes props). These encode the 061-B zone identity
(D-07) and the 062-A receipt vocabulary (D-08): OperatorBand, HealthSignals (+ TechnicalNamesToggle),
LockedTab, RecentActionsCard.

Purpose: The zone must be FELT (amber band, OPERATOR chip, recording marker), the health readable in
plain language with a technical reveal, locked sections honest without leaking phase numbers, and the
ledger legible as the receipt. UI is sketch-locked — do not re-litigate shape.
Output: five components under `frontend/src/components/admin/`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/146-operator-foundation/146-CONTEXT.md
@.planning/phases/146-operator-foundation/146-PATTERNS.md
@.planning/sketches/061-control-room-shell/README.md
@.planning/sketches/062-gate-honesty-and-receipts/README.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
@frontend/src/pages/SkillStudioPage.tsx

<interfaces>
<!-- Types from Plan 04 (frontend/src/lib/api.ts):
       OperatorIdentity = { id, email, granted_at }
       BackpressureSignals = { anyio_threadpool_depth:{borrowed,total}, redis_active_runs, postgres_pool_in_use, per_worker_run_count }
       OperatorAuditRow = { id, action, label, is_write, target_type, target_id, created_at } -->

<!-- Plain-label mapping (D-07 / sketch 061-B copy):
       anyio_threadpool_depth  → "Server capacity"       (raw shown under ⌥ Technical names)
       redis_active_runs       → "Agents working"
       postgres_pool_in_use    → "Database connections"
       per_worker_run_count    → "Work spread" -->

<!-- Sketch-driven specifics (no code analog — read the sketch READMEs / index.html for exact copy/layout/animation):
       061-B: amber-warmed band, OPERATOR chip, "every action recorded" marker, "⌥ Technical names" toggle
       062-A: row-slides-in receipt, marker flash on write, ✎ leading write mark, no toasts, no counters -->

<!-- Shell/band structural analog: SkillStudioPage.tsx:130-187 (persistent header band + horizontal tab bar). Shield glyph = lucide Shield + amber tint (NOT ShieldCheck). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OperatorBand + LockedTab + TechnicalNamesToggle</name>
  <files>frontend/src/components/admin/OperatorBand.tsx, frontend/src/components/admin/LockedTab.tsx, frontend/src/components/admin/TechnicalNamesToggle.tsx</files>
  <read_first>
    - .planning/sketches/061-control-room-shell/README.md (the zone identity, honest locks, amber-marks-the-zone direction)
    - .planning/sketches/061-control-room-shell/index.html (exact band layout, chip, marker copy, locked-tab refusal copy)
    - frontend/src/pages/SkillStudioPage.tsx (the persistent header-band structure at :130-187 to mirror)
    - .claude/skills/sketch-findings-agentic-rag/SKILL.md (Aether Deep Midnight tokens; amber = needs-you/zone color)
  </read_first>
  <action>
    Create three prop-driven components in `frontend/src/components/admin/`:
    (1) `OperatorBand.tsx` — the full-width amber-warmed 061-B operator band. Props: `identity: OperatorIdentity | null`, `onBack: () => void`. Renders a plain lucide `Shield` with an amber tint (DISTINCT from Governance's `ShieldCheck`), the label "Control Room", an amber OPERATOR chip, the operator identity (email), the "every action recorded" recording marker, and a "‹ Back to app" affordance calling `onBack`. Amber marks the zone sparingly (the app's needs-you color). Expose an imperative or prop hook so the shell can trigger the marker FLASH on a write (Plan 06 wires it) — e.g. a `recordingPulse` boolean prop or a forwarded ref; keep the flash mechanism prop-controlled here.
    (2) `LockedTab.tsx` — the calm honest-lock refusal. Props: `title: string` (e.g. "System Controls"). Renders "Not built yet — coming soon" style copy per the sketch. HARD RULE: NEVER render a phase number in shipped copy (no "Phase 147", no "arrives with 147"). Dimmed/locked styling per 061-B.
    (3) `TechnicalNamesToggle.tsx` — the "⌥ Technical names" two-audience toggle. Props: `enabled: boolean`, `onToggle: () => void`. Controls whether raw field/action/endpoint names are revealed alongside plain labels (the LANG-01 pattern, born plain at 146). Presentational only — parent owns the state.
  </action>
  <verify>
    <automated>cd frontend && for f in OperatorBand LockedTab TechnicalNamesToggle; do test -f "src/components/admin/$f.tsx" || { echo "MISSING $f"; exit 1; }; done && grep -qi "OPERATOR" src/components/admin/OperatorBand.tsx && grep -qi "coming soon" src/components/admin/LockedTab.tsx && (grep -RniE "phase *1[0-9][0-9]" src/components/admin/LockedTab.tsx && { echo "PHASE NUMBER LEAK"; exit 1; } || true) && npx tsc --noEmit -p tsconfig.json 2>&1 | (! grep -qiE "components/admin/(OperatorBand|LockedTab|TechnicalNamesToggle)") && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - OperatorBand renders Shield (not ShieldCheck) + "Control Room" + an OPERATOR chip + identity + recording marker + a back affordance
    - LockedTab renders a "coming soon" refusal and contains NO phase-number token (grep asserts absence)
    - TechnicalNamesToggle is prop-controlled (enabled + onToggle)
    - All three type-check clean
  </acceptance_criteria>
  <done>The zone band, honest-lock tab, and technical-names toggle exist per 061-B, phase-number-free.</done>
</task>

<task type="auto">
  <name>Task 2: HealthSignals — four plain-labeled signals + technical reveal</name>
  <files>frontend/src/components/admin/HealthSignals.tsx</files>
  <read_first>
    - .planning/sketches/061-control-room-shell/README.md (the four posture signals grounding + the plain-label copy)
    - frontend/src/lib/api.ts (BackpressureSignals type from Plan 04)
    - frontend/src/components/admin/TechnicalNamesToggle.tsx (Task 1 — the reveal control)
  </read_first>
  <action>
    Create `frontend/src/components/admin/HealthSignals.tsx`. Props: `signals: BackpressureSignals | null`, `showTechnical: boolean`. Render the four REAL backpressure values under plain labels with one-line subtexts, per the sketch 061-B copy: `anyio_threadpool_depth` → "Server capacity", `redis_active_runs` → "Agents working", `postgres_pool_in_use` → "Database connections", `per_worker_run_count` → "Work spread". When `showTechnical` is true, reveal the raw field name (e.g. `anyio_threadpool_depth`) beside each plain label (the ⌥ two-audience reveal). Handle the null/loading state gracefully (no crash — a calm placeholder). This is the Overview health block; the shell (Plan 06) owns the toggle state + fetch.
  </action>
  <verify>
    <automated>cd frontend && test -f src/components/admin/HealthSignals.tsx && grep -q "Server capacity" src/components/admin/HealthSignals.tsx && grep -q "Agents working" src/components/admin/HealthSignals.tsx && grep -q "Database connections" src/components/admin/HealthSignals.tsx && grep -q "Work spread" src/components/admin/HealthSignals.tsx && grep -q "anyio_threadpool_depth" src/components/admin/HealthSignals.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | (! grep -qi "components/admin/HealthSignals") && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All four plain labels present: "Server capacity", "Agents working", "Database connections", "Work spread"
    - Each maps to its real backpressure field; raw field names shown only when `showTechnical` is true
    - Null/loading handled without crashing
    - Type-checks clean
  </acceptance_criteria>
  <done>The four real signals render under plain labels with a raw-name reveal (D-07 two-audience).</done>
</task>

<task type="auto">
  <name>Task 3: RecentActionsCard — the ledger-is-receipt card</name>
  <files>frontend/src/components/admin/RecentActionsCard.tsx</files>
  <read_first>
    - .planning/sketches/062-gate-honesty-and-receipts/README.md (the always-on ledger = receipt; ✎ write mark; no toasts; consequence ≠ receipt)
    - .planning/sketches/062-gate-honesty-and-receipts/index.html (exact row layout, slide-in animation, marker flash, ✎ mark placement)
    - frontend/src/lib/api.ts (OperatorAuditRow type from Plan 04)
  </read_first>
  <action>
    Create `frontend/src/components/admin/RecentActionsCard.tsx`. Props: `rows: OperatorAuditRow[]`. Render the always-visible "Recent operator actions" card (062-A winner). Each row shows the plain-sentence `label` (never the raw `action` code), the actor/time, and a leading ✎ mark when `is_write` is true (views have no ✎). New actions land at the TOP with a row-slides-in animation (the ledger IS the receipt — NO toasts, NO counters). Newest first (rows arrive `created_at DESC` from the feed). Keep it presentational — the shell (Plan 06) owns fetch + re-fetch after the refresh honesty beat. Do NOT render a toast or a running counter (062-A rejected B and C). Use the Aether Deep Midnight tokens; restrained motion for the slide-in.
  </action>
  <verify>
    <automated>cd frontend && test -f src/components/admin/RecentActionsCard.tsx && grep -q "RecentActionsCard" src/components/admin/RecentActionsCard.tsx && grep -qE "is_write|✎" src/components/admin/RecentActionsCard.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | (! grep -qi "components/admin/RecentActionsCard") && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Renders rows from `OperatorAuditRow[]`, showing the plain `label` (not the raw `action`)
    - Write rows (`is_write` true) carry a leading ✎ mark; view rows do not
    - New rows land at the top with a slide-in; no toast, no counter component
    - Type-checks clean
  </acceptance_criteria>
  <done>The ledger-is-receipt card exists with the ✎ write distinction, no toasts/counters (D-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped copy → end user | Locked-tab copy must not leak internal phase numbers (information hygiene) |
| rendered audit rows → operator | Rows are display-only; these components hold no secrets and enforce no access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-10 | Information Disclosure | phase-number leak in locked-tab copy | mitigate | LockedTab renders "coming soon" with NO phase number; Task 1 grep asserts absence of any `Phase 1xx` token (D-07 / D-09 #2). |
| T-146-06 | Spoofing / Elevation of Privilege | presentational components as a security boundary | accept | These are pure prop-driven leaves with no fetch and no auth logic; the backend 404 gate (Plan 02) is the sole authority. No client-side access decisions live here. |
| T-146-SC | Tampering | npm installs | accept | No package installs (RESEARCH Package Legitimacy Audit: N/A — reuses lucide + existing tokens). |
</threat_model>

<verification>
- `tsc --noEmit` clean for all five files.
- LockedTab contains no phase-number token (grep gate).
- The four plain health labels + the raw-name reveal present; RecentActionsCard shows the ✎ write mark and no toast/counter.
</verification>

<success_criteria>
- The zone band, health signals with two-audience reveal, honest locks, and ledger receipt all match 061-B / 062-A (D-07, D-08).
- No phase numbers leak into shipped copy.
</success_criteria>

<output>
Create `.planning/phases/146-operator-foundation/146-05-SUMMARY.md` when done.
</output>
