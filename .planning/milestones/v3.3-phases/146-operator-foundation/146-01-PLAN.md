---
phase: 146-operator-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/095_operator_foundation.sql
  - supabase/migrations/096_org_id_stub_sweep.sql
  - supabase/full-schema.sql
autonomous: false
requirements: [ADMIN-01]
user_setup: []

must_haves:
  truths:
    - "operator_audit_log carries an org_id stub column per D-05 (nullable, no FK, no index, no backfill)"
    - "The four owned-root tables (documents, folders, threads, skills) each gain an org_id stub per D-05"
    - "operator_users is an org-agnostic principal per D-06 — it has NO org_id column (an operator spans all orgs)"
    - "Both operator tables ship RLS-enabled with NO policies (deny-all for anon/authenticated; only the service-role backend reads them)"
    - "operator_audit_log.operator_user_id is a PLAIN uuid with NO foreign key so an operator's history survives their deletion (tamper-resistance)"
  artifacts:
    - path: "supabase/migrations/095_operator_foundation.sql"
      provides: "operator_users + operator_audit_log tables, RLS deny-all"
      contains: "CREATE TABLE IF NOT EXISTS public.operator_users"
    - path: "supabase/migrations/096_org_id_stub_sweep.sql"
      provides: "org_id metadata stub on documents/folders/threads/skills"
      contains: "ADD COLUMN IF NOT EXISTS org_id uuid"
    - path: "supabase/full-schema.sql"
      provides: "regenerated single-file bootstrap artifact including the new tables"
      contains: "operator_audit_log"
  key_links:
    - from: "supabase/migrations/095_operator_foundation.sql"
      to: "auth.users"
      via: "operator_users.user_id PK REFERENCES auth.users(id) ON DELETE CASCADE"
      pattern: "REFERENCES auth.users"
    - from: "supabase/full-schema.sql"
      to: "operator_users"
      via: "live-DB dump after apply"
      pattern: "operator_users"
---

<objective>
Ship the operator-foundation schema: migration 095 creates `operator_users` (the org-agnostic
operator principal, D-06) and the append-only `operator_audit_log` (D-03 audit target), both
RLS-enabled with deny-all; migration 096 adds the `org_id` forward-compat stub (D-05) to the four
owned-root tables the v3.4 org-RBAC rewrite will scope. Then apply both migrations to the live
local DB and regenerate the single-file bootstrap artifact.

Purpose: The whole `/admin` gate, audit floor, and seed (Plans 02–06) read/write these tables.
No live table = the backend seed at startup and the D-09 live UAT cannot run.
Output: `supabase/migrations/095_operator_foundation.sql`, `supabase/migrations/096_org_id_stub_sweep.sql`, regenerated `supabase/full-schema.sql`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/146-operator-foundation/146-CONTEXT.md
@.planning/phases/146-operator-foundation/146-RESEARCH.md
@.planning/phases/146-operator-foundation/146-PATTERNS.md
@supabase/migrations/059_harness_audit_and_threads_col.sql

<interfaces>
<!-- The append-only audit-table idioms to copy verbatim from migration 059 (VERIFIED file:line in 146-PATTERNS.md): -->
<!-- 059:16  actor uuid is a PLAIN stored uuid, NO FK — audit survives deletion (NEVER CASCADE) -->
<!-- 059:19  org_id uuid — forward-compat, nullable, NO FK -->
<!-- 059:36-37  COMMENT ON COLUMN ... org_id IS '...forward-compat...' -->
<!-- 059:43  ALTER TABLE ... ENABLE ROW LEVEL SECURITY; -->
<!-- 059:45-51  owner SELECT/INSERT policies — 146 is STRICTER: NO policies (deny-all) -->

<!-- Target DDL shapes (from 146-RESEARCH.md §Schema, Claude's-discretion — refine only if the live schema demands): -->
<!-- operator_users(user_id uuid PK REFERENCES auth.users(id) ON DELETE CASCADE, granted_at timestamptz NOT NULL DEFAULT now(), granted_by uuid, note text) -->
<!-- operator_audit_log(id uuid PK DEFAULT gen_random_uuid(), operator_user_id uuid NOT NULL [PLAIN, NO FK], action text NOT NULL [free-text, NO CHECK], label text NOT NULL, is_write boolean NOT NULL DEFAULT false, target_type text, target_id text, metadata jsonb NOT NULL DEFAULT '{}', org_id uuid, created_at timestamptz NOT NULL DEFAULT now()) + INDEX idx_operator_audit_created ON (created_at DESC) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 095 — operator_users + operator_audit_log (RLS deny-all)</name>
  <files>supabase/migrations/095_operator_foundation.sql</files>
  <read_first>
    - supabase/migrations/059_harness_audit_and_threads_col.sql (the append-only audit-table precedent: PLAIN-uuid-no-FK actor, org_id stub + COMMENT, ENABLE RLS)
    - .planning/phases/146-operator-foundation/146-RESEARCH.md (§Schema — Migration 095 target DDL, the free-text-action A4 rationale, RLS deny-all)
    - .planning/phases/146-operator-foundation/146-PATTERNS.md (§095 migration — the exact 059 idioms to copy)
  </read_first>
  <action>
    Create `supabase/migrations/095_operator_foundation.sql`. Two tables:
    (1) `public.operator_users` — `user_id uuid PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE`, `granted_at timestamptz NOT NULL DEFAULT now()`, `granted_by uuid` (nullable; NULL = env-bootstrap provenance, populated when Phase 148 adds grant-by-operator), `note text`. Deliberately NO `org_id` column (D-06 — org-agnostic principal; adding org_id here poisons the v3.4 one-way door).
    (2) `public.operator_audit_log` — `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`, `operator_user_id uuid NOT NULL` as a PLAIN stored uuid with NO foreign key (matches `harness_audit.run_id` at 059:16 — audit survives operator deletion; NEVER FK CASCADE), `action text NOT NULL` free-text with NO CHECK constraint (per RESEARCH A4 — the auto-floor derives an action per route, so a CHECK would force a migration per new admin action), `label text NOT NULL` (the plain-sentence receipt, 062-A), `is_write boolean NOT NULL DEFAULT false` (the ✎ write mark), `target_type text` nullable, `target_id text` nullable, `metadata jsonb NOT NULL DEFAULT '{}'`, `org_id uuid` nullable with NO FK and NO index and NO backfill (the D-05 stub, 059:19 idiom), `created_at timestamptz NOT NULL DEFAULT now()`. Add `CREATE INDEX idx_operator_audit_created ON public.operator_audit_log(created_at DESC)` for feed ordering, and a `COMMENT ON COLUMN public.operator_audit_log.org_id` mirroring 059:36-37 (`'Forward-compat (D-PRD-02/D-11): org-level multi-tenancy. NULL in v3.3; no FK until org schema exists.'`).
    Use `CREATE TABLE IF NOT EXISTS` for both. Then `ALTER TABLE public.operator_users ENABLE ROW LEVEL SECURITY;` and `ALTER TABLE public.operator_audit_log ENABLE ROW LEVEL SECURITY;` with NO `CREATE POLICY` statements at all — deny-all by omission (STRICTER than 059, which had owner SELECT/INSERT). Add a header comment naming ADMIN-01, D-06 (no org_id on principal), and the deny-all-RLS rationale. Filename digits only, no letter suffix.
  </action>
  <verify>
    <automated>test -f "supabase/migrations/095_operator_foundation.sql" && grep -q "CREATE TABLE IF NOT EXISTS public.operator_users" supabase/migrations/095_operator_foundation.sql && grep -q "CREATE TABLE IF NOT EXISTS public.operator_audit_log" supabase/migrations/095_operator_foundation.sql && grep -q "ENABLE ROW LEVEL SECURITY" supabase/migrations/095_operator_foundation.sql && ! grep -qi "CREATE POLICY" supabase/migrations/095_operator_foundation.sql && ! grep -q "org_id" <(grep -A6 "CREATE TABLE IF NOT EXISTS public.operator_users" supabase/migrations/095_operator_foundation.sql) && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `operator_users` block contains NO `org_id` column (D-06 org-agnostic principal)
    - `operator_users.user_id` is `PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE`
    - `operator_audit_log.operator_user_id` has NO `REFERENCES` clause (PLAIN uuid, tamper-resistant)
    - `operator_audit_log.action` has NO `CHECK` constraint (free-text per A4)
    - `operator_audit_log` has `org_id uuid` nullable with a forward-compat COMMENT and NO index on it
    - Both tables have `ENABLE ROW LEVEL SECURITY` and the file has ZERO `CREATE POLICY` statements
    - `idx_operator_audit_created` indexes `created_at DESC`
  </acceptance_criteria>
  <done>Migration 095 authored with both tables, deny-all RLS, tamper-resistant audit actor, and the org_id stub only on the audit log (not the principal).</done>
</task>

<task type="auto">
  <name>Task 2: Author migration 096 — org_id stub sweep on the four owned roots</name>
  <files>supabase/migrations/096_org_id_stub_sweep.sql</files>
  <read_first>
    - supabase/migrations/059_harness_audit_and_threads_col.sql (the bare-column org_id stub + COMMENT idiom, 059:19,36-37)
    - .planning/phases/146-operator-foundation/146-RESEARCH.md (§Migration 096 + the D-05 target-list table + the INDEX TENSION flag)
    - .planning/phases/146-operator-foundation/146-PATTERNS.md (§096 migration — bare column + COMMENT only, NO index)
  </read_first>
  <action>
    Create `supabase/migrations/096_org_id_stub_sweep.sql`. Add exactly one nullable `org_id uuid` column to each of the FOUR unambiguous owned-root tables: `public.documents`, `public.folders`, `public.threads`, `public.skills` — using `ALTER TABLE public.<t> ADD COLUMN IF NOT EXISTS org_id uuid;`. Add a `COMMENT ON COLUMN public.<t>.org_id` for each, identical text to the 095 audit-log comment (`'Forward-compat (D-PRD-02/D-11): org-level multi-tenancy. NULL in v3.3; no FK until org schema exists.'`). Follow the `harness_audit` precedent strictly per D-05: NO foreign key, NO index, NO backfill — do NOT add `idx_*_org_id` indexes even though the DM-era org_id columns did (RESEARCH INDEX TENSION flag). Do NOT stub child tables (messages, chunks, skill_files, etc. — they inherit org through their parent FK) and do NOT stub `workflow_definitions`/`workflow_runs` (already stubbed). Split from 095 so the operator tables can ship/rollback independently. Header comment names D-05 and the harness_audit precedent.
  </action>
  <verify>
    <automated>test -f "supabase/migrations/096_org_id_stub_sweep.sql" && for t in documents folders threads skills; do grep -q "ALTER TABLE public.$t ADD COLUMN IF NOT EXISTS org_id uuid" supabase/migrations/096_org_id_stub_sweep.sql || { echo "MISSING $t"; exit 1; }; done && ! grep -qi "CREATE INDEX" supabase/migrations/096_org_id_stub_sweep.sql && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `ADD COLUMN IF NOT EXISTS org_id uuid` present for each of documents, folders, threads, skills
    - A forward-compat COMMENT exists for each of the four columns
    - File contains ZERO `CREATE INDEX` statements (bare column only, per D-05)
    - No ALTER touches workflow_definitions/workflow_runs or any child table
  </acceptance_criteria>
  <done>Migration 096 adds the org_id metadata stub to exactly the four owned roots, no index, no FK, no backfill.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Apply migrations 095 + 096 to the live local DB and regenerate full-schema</name>
  <files>supabase/full-schema.sql</files>
  <action>
    PAUSE for the operator (checkpoint — Claude cannot paste into the Supabase SQL editor). Present the two migration files for the operator to run in the LOCAL Supabase SQL editor (095 first, then 096 — never `supabase db push`/`db reset`). After the operator replies "applied", run `bash scripts/regenerate-full-schema.sh` (live dump, NO --reset), verify the regenerated `supabase/full-schema.sql` contains `operator_users`, `operator_audit_log`, and the four new `org_id` columns, and commit the regenerated artifact together with both migration files. Never hand-edit full-schema.sql.
  </action>
  <what-built>Migrations 095 (operator tables + deny-all RLS) and 096 (org_id stub sweep) are authored on disk. They must now be applied to the live local Supabase DB via the SQL editor (project rule — never `supabase db push`/`db reset`, which would wipe dev data), then the single-file bootstrap artifact regenerated.</what-built>
  <how-to-verify>
    1. Open the local Supabase SQL editor.
    2. Paste the full contents of `supabase/migrations/095_operator_foundation.sql` and run it. Confirm success (no error; tables `operator_users` and `operator_audit_log` now exist).
    3. Paste the full contents of `supabase/migrations/096_org_id_stub_sweep.sql` and run it. Confirm success.
    4. Reply "applied" here. Claude then runs `bash scripts/regenerate-full-schema.sh` (live dump, no reset — rebuilds `supabase/full-schema.sql`) and verifies the regenerated file contains `operator_users`, `operator_audit_log`, and the four new `org_id` columns.
    Deployment parity note (do NOT do now — operator-gated at promotion): the same two migrations must later be pasted into the CLOUD Supabase SQL editor, and Coolify must SET `OPERATOR_EMAILS` + REMOVE `BACKPRESSURE_ADMIN_USER_IDS` at the same promotion (docs/DEPLOYMENT-WORKFLOW.md, D-02).
  </how-to-verify>
  <verify>
    <automated>grep -q "operator_audit_log" supabase/full-schema.sql && grep -q "operator_users" supabase/full-schema.sql && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Operator confirms both migrations ran cleanly in the local Supabase SQL editor
    - `supabase/full-schema.sql` regenerated via `scripts/regenerate-full-schema.sh` (no `--reset`) and contains `operator_users` + `operator_audit_log`
    - `supabase/full-schema.sql` shows `org_id` on documents, folders, threads, skills
  </acceptance_criteria>
  <resume-signal>Reply "applied" once both migrations have run in the local SQL editor; Claude regenerates full-schema.sql and commits.</resume-signal>
  <done>Both migrations live in the local DB; full-schema.sql regenerated and committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| normal JWT → operator tables | An authenticated non-operator must NEVER read `operator_users`/`operator_audit_log` |
| v3.3 schema → v3.4 org-RBAC rewrite | The principal shape is a one-way door; wrong shape poisons the future rewrite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-04 | Elevation of Privilege (future) | operator_users principal shape | mitigate | Org-agnostic principal — NO `org_id` on `operator_users`; not a JWT claim / is_admin boolean / special org (D-06, Pitfall 3). Task 1 acceptance asserts no org_id in the principal block. |
| T-146-09 | Information Disclosure | operator_users + operator_audit_log readability | mitigate | Both tables `ENABLE ROW LEVEL SECURITY` with ZERO policies = deny-all for anon/authenticated; only the service-role backend behind `require_operator` reads them. Task 1 asserts no CREATE POLICY. |
| T-146-07 | Repudiation | operator_audit_log.operator_user_id | mitigate | PLAIN uuid, NO FK — deleting an operator cannot erase their action history (059:16 tamper-resistance idiom). |
| T-146-SC | Tampering | npm/pip installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: N/A — schema-only). |
</threat_model>

<verification>
- Both migration files exist, match the DDL idioms above, and pass their grep gates.
- After the blocking apply: `supabase/full-schema.sql` contains the two operator tables + the four org_id stubs.
- No `CREATE POLICY` anywhere in 095 (deny-all); no `CREATE INDEX` in 096 (bare stub).
</verification>

<success_criteria>
- Migration 095 creates both operator tables with deny-all RLS and a tamper-resistant audit actor (D-06 principal has no org_id).
- Migration 096 stubs org_id on documents/folders/threads/skills only (D-05).
- Migrations applied live and full-schema.sql regenerated (blocking checkpoint satisfied).
</success_criteria>

<output>
Create `.planning/phases/146-operator-foundation/146-01-SUMMARY.md` when done.
</output>
