---
phase: 146-operator-foundation
plan: 04
type: execute
wave: 2
depends_on: [146-02]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/hooks/useOperatorProbe.ts
  - frontend/src/hooks/useOperatorProbe.test.ts
autonomous: true
requirements: [ADMIN-01]
user_setup: []

must_haves:
  truths:
    - "getOperatorProbe() returns the operator identity on 200 and null on 404 — a non-operator's probe yields null so their nav stays byte-identical to today (the D-07 non-discoverable contract)"
    - "The probe result decides RENDERING ONLY; the backend 404 gate is the security authority — a forged isOperator=true still gets 404 on every /admin call (Pitfall 13)"
    - "useOperatorProbe runs a one-shot mount probe and exposes { isOperator, identity, loading }"
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "getOperatorProbe (404→null), getBackpressure, getOperatorAudit + their types"
      contains: "getOperatorProbe"
    - path: "frontend/src/hooks/useOperatorProbe.ts"
      provides: "one-shot mount probe hook"
      contains: "useOperatorProbe"
    - path: "frontend/src/hooks/useOperatorProbe.test.ts"
      provides: "200→isOperator true / 404→false contract test"
      contains: "useOperatorProbe"
  key_links:
    - from: "frontend/src/hooks/useOperatorProbe.ts"
      to: "getOperatorProbe"
      via: "one-shot fetch on mount"
      pattern: "getOperatorProbe"
    - from: "frontend/src/lib/api.ts"
      to: "/admin/me"
      via: "authed fetch, 404 → null"
      pattern: "/admin/me"
---

<objective>
Add the frontend data layer for the Control Room: `getOperatorProbe()` (calls `GET /admin/me`,
returns the operator identity on 200, `null` on 404 — the getTunerLatest 404→null idiom),
`getBackpressure()` and `getOperatorAudit()` (plain authed GETs), plus their TypeScript types, and
a one-shot `useOperatorProbe` hook. This is the interface-first plan — Plans 05 and 06 consume these
types and functions without re-exploring the codebase.

Purpose: The probe drives the non-discoverable nav (D-07) — 404 means "render nothing." The security
authority is the backend 404 gate, never this client flag (Pitfall 13).
Output: three api.ts functions + types, `useOperatorProbe.ts`, `useOperatorProbe.test.ts`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/146-operator-foundation/146-CONTEXT.md
@.planning/phases/146-operator-foundation/146-PATTERNS.md
@frontend/src/lib/api.ts
@frontend/src/hooks/useSkills.ts

<interfaces>
<!-- 404→null probe idiom to copy (api.ts:3388-3394, getTunerLatest):
       const res = await fetch(`${API_BASE}/skills/${skillId}/tuner/runs/latest`, { headers })
       if (res.status === 404) return null
       if (!res.ok) throw new ApiError("…", res.status)
       return (await res.json()) -->
<!-- Plain authed GET idiom (api.ts:46-51, listThreads) + getAuthHeaders() (api.ts:29-37, Bearer token from supabase session). -->
<!-- Backend contracts this plan targets (Plan 02): GET /admin/me → {id,email,granted_at}; GET /admin/backpressure → {anyio_threadpool_depth:{borrowed,total}, redis_active_runs, postgres_pool_in_use, per_worker_run_count}; GET /admin/audit → recent operator_audit_log rows {id, action, label, is_write, target_type, target_id, created_at}. -->
<!-- fetch-on-mount hook shape (useSkills.ts:23-39): useState + useCallback loader + useEffect(() => { load().catch(...) }, [load]). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: api.ts — getOperatorProbe (404→null), getBackpressure, getOperatorAudit + types</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts (getAuthHeaders at :29-37; the plain-GET listThreads idiom at :46-51; the getTunerLatest 404→null idiom at :3388-3394; the ApiError type)
    - .planning/phases/146-operator-foundation/146-PATTERNS.md (§api.ts — the two idioms + the note that no getBackpressure exists yet)
  </read_first>
  <action>
    In `frontend/src/lib/api.ts` add three exported functions and their types. Define `OperatorIdentity` = `{ id: string; email: string; granted_at: string }`, `BackpressureSignals` = `{ anyio_threadpool_depth: { borrowed: number; total: number }; redis_active_runs: number; postgres_pool_in_use: number; per_worker_run_count: number }`, and `OperatorAuditRow` = `{ id: string; action: string; label: string; is_write: boolean; target_type: string | null; target_id: string | null; created_at: string }`.
    `getOperatorProbe(): Promise<OperatorIdentity | null>` — authed `fetch(`${API_BASE}/admin/me`, { headers })`; `if (res.status === 404) return null` (non-operator → render nothing); `if (!res.ok) throw new ApiError(...)`; else return the identity. Copy the getTunerLatest 404→null idiom exactly.
    `getBackpressure(): Promise<BackpressureSignals>` — plain authed GET `/admin/backpressure` (listThreads idiom).
    `getOperatorAudit(limit?: number): Promise<OperatorAuditRow[]>` — plain authed GET `/admin/audit` (optionally `?limit=`). All three use `getAuthHeaders()` for the Bearer token.
  </action>
  <verify>
    <automated>cd frontend && grep -q "getOperatorProbe" src/lib/api.ts && grep -q "getBackpressure" src/lib/api.ts && grep -q "getOperatorAudit" src/lib/api.ts && grep -q "res.status === 404" src/lib/api.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "node_modules" | (! grep -qi "api.ts") && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `getOperatorProbe` returns `null` on a 404 status (grep: `res.status === 404` in its body) and `OperatorIdentity` on 200
    - `getBackpressure` and `getOperatorAudit` are plain authed GETs using `getAuthHeaders()`
    - `OperatorIdentity`, `BackpressureSignals`, `OperatorAuditRow` types are exported
    - `tsc --noEmit` reports no error in api.ts
  </acceptance_criteria>
  <done>The three admin API-client functions + types exist; the probe treats 404 as null.</done>
</task>

<task type="auto">
  <name>Task 2: useOperatorProbe hook + contract test</name>
  <files>frontend/src/hooks/useOperatorProbe.ts, frontend/src/hooks/useOperatorProbe.test.ts</files>
  <read_first>
    - frontend/src/hooks/useSkills.ts (the fetch-on-mount hook shape at :23-39 — useState + useCallback + useEffect)
    - frontend/src/lib/api.ts (getOperatorProbe + OperatorIdentity from Task 1)
    - .planning/phases/146-operator-foundation/146-PATTERNS.md (§useOperatorProbe — one-shot probe, 200→isOperator true / 404→false; the No-Analog note for the vitest)
  </read_first>
  <action>
    Create `frontend/src/hooks/useOperatorProbe.ts` — a one-shot mount probe modeled on useSkills.ts:23-39. It calls `getOperatorProbe()` once on mount and returns `{ isOperator: boolean, identity: OperatorIdentity | null, loading: boolean }`: `identity !== null` → `isOperator = true`; a 404/null → `isOperator = false`. Guard against setState-after-unmount (an `alive` ref, matching the SkillStudioPage pattern). The probe result decides RENDERING ONLY — add a code comment stating the backend 404 gate is the security authority (Pitfall 13). Create `frontend/src/hooks/useOperatorProbe.test.ts` (vitest) mocking `getOperatorProbe`: (1) resolves to an identity → `isOperator` true + identity populated; (2) resolves to `null` (404) → `isOperator` false + identity null. Keep it minimal per the RESEARCH Wave-0 gap.
  </action>
  <verify>
    <automated>cd frontend && grep -q "useOperatorProbe" src/hooks/useOperatorProbe.ts && npx vitest run src/hooks/useOperatorProbe.test.ts 2>&1 | grep -qiE "pass|✓" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `useOperatorProbe` calls `getOperatorProbe()` once on mount and returns `{ isOperator, identity, loading }`
    - identity present → isOperator true; null (404) → isOperator false
    - A comment documents that the probe is render-only, not the security authority
    - `useOperatorProbe.test.ts` passes both the 200 and 404 cases
  </acceptance_criteria>
  <done>The one-shot probe hook + its contract test exist and are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser probe result → nav rendering | The client probe decides only whether to render the shield; it is NOT a security boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-06 | Spoofing / Elevation of Privilege | client-forged operator flag | mitigate | The probe result gates RENDERING only; every /admin data call is independently 404-gated server-side (Plan 02). A forged `isOperator=true` reveals nothing and reaches no data. Hook comment documents this (Pitfall 13). |
| T-146-02 | Information Disclosure | probe distinguishing operator vs non-operator | mitigate | The probe uses the same 404-not-403 gate; a non-operator's `GET /admin/me` is byte-identical to a nonexistent route → `null`, no signal that the surface exists. |
| T-146-SC | Tampering | npm installs | accept | No package installs (RESEARCH Package Legitimacy Audit: N/A — reuses existing fetch/vitest). |
</threat_model>

<verification>
- `tsc --noEmit` clean for api.ts and the hook.
- `npx vitest run src/hooks/useOperatorProbe.test.ts` green (200 and 404 cases).
</verification>

<success_criteria>
- The probe returns identity on 200, null on 404; the hook exposes isOperator/identity/loading.
- The client is documented as render-only; the backend gate is the authority.
</success_criteria>

<output>
Create `.planning/phases/146-operator-foundation/146-04-SUMMARY.md` when done.
</output>
