---
phase: 146-operator-foundation
plan: 06
type: execute
wave: 4
depends_on: [146-04, 146-05]
files_modified:
  - frontend/src/components/admin/ControlRoomPage.tsx
  - frontend/src/components/admin/AuditTab.tsx
  - frontend/src/App.tsx
  - frontend/src/components/layout/ChatLayout.tsx
  - frontend/src/components/layout/NavPanel.tsx
  - frontend/src/lib/nav-items.test.ts
autonomous: true
requirements: [ADMIN-01]
user_setup: []

must_haves:
  truths:
    - "Health data loads on Control Room entry and via the visible ↻ Refresh button ONLY — no auto-polling per D-04; every ledger row is a deliberate human action"
    - "↻ Refresh visibly prepends its own 'Viewed system health' row to the Recent operator actions card (the ledger IS the receipt per D-08) — the row lives in operator_audit_log, not client state, so it persists across a page reload (D-09 scenario 3)"
    - "The Control Room shell is 061-B per D-07: operator band over horizontal section tabs (NOT a second left nav rail); Overview + Audit tabs live; System Controls / Users & Access / AI Models / API Keys render locked"
    - "The shield entry is probe-gated at the bottom of the app rail and rendered OUTSIDE NAV_ITEMS per D-07 — a non-operator's nav is byte-identical to today (nav-items.ts is NOT modified; a regression test asserts NAV_ITEMS carries no control-room entry)"
    - "The reachability triad lands complete in this phase: the 'control-room' ActiveView union entry, the ChatLayout mount branch, AND the probe-gated shield entry action (no built-but-unreachable surface)"
    - "The Audit tab is the honest minimal full-history view with a count-pill per D-08 (browser filters/CSV are Phase 148 territory, not built here)"
  artifacts:
    - path: "frontend/src/components/admin/ControlRoomPage.tsx"
      provides: "the 061-B shell: band + tabs + Overview (health + ledger) + locked tabs + refresh beat"
      contains: "ControlRoomPage"
      min_lines: 60
    - path: "frontend/src/components/admin/AuditTab.tsx"
      provides: "honest minimal audit history view"
      contains: "AuditTab"
    - path: "frontend/src/App.tsx"
      provides: "control-room ActiveView union entry + probe hosting"
      contains: "control-room"
    - path: "frontend/src/components/layout/ChatLayout.tsx"
      provides: "control-room mount branch + mobile probe-gated shield"
      contains: "control-room"
    - path: "frontend/src/components/layout/NavPanel.tsx"
      provides: "probe-gated shield footer entry (outside NAV_ITEMS)"
      contains: "Shield"
    - path: "frontend/src/lib/nav-items.test.ts"
      provides: "NAV_ITEMS byte-identity regression (no control-room entry)"
      contains: "NAV_ITEMS"
  key_links:
    - from: "frontend/src/components/admin/ControlRoomPage.tsx"
      to: "getBackpressure / getOperatorAudit"
      via: "fetch-once-on-entry + refresh re-fetch"
      pattern: "getOperatorAudit"
    - from: "frontend/src/App.tsx"
      to: "ChatLayout"
      via: "activeView='control-room' + isOperator/identity props"
      pattern: "control-room"
    - from: "frontend/src/components/layout/NavPanel.tsx"
      to: "ActiveView 'control-room'"
      via: "probe-gated shield onClick → onNavigate"
      pattern: "control-room"
---

<objective>
Assemble the Control Room and make it reachable: `ControlRoomPage` composes the Plan-05 leaves into
the 061-B shell (band + horizontal tabs; Overview = HealthSignals + RecentActionsCard; Audit = honest
minimal history; four locked tabs), with fetch-on-entry + the manual ↻ Refresh honesty beat (D-04/D-08).
Then wire the reachability triad: the `"control-room"` ActiveView union entry in App.tsx (hosting the
probe), the ChatLayout mount branch, and the probe-gated shield at the rail bottom — rendered OUTSIDE
NAV_ITEMS so a non-operator's nav stays byte-identical (D-07), locked in by a regression test.

Purpose: This is the phase's user-visible deliverable — the operator zone that Phases 147-150 move
into. Without the triad the surface is built-but-unreachable (the Phase-118 lesson).
Output: ControlRoomPage + AuditTab, the App/ChatLayout/NavPanel wiring, the NAV_ITEMS regression test.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/146-operator-foundation/146-CONTEXT.md
@.planning/phases/146-operator-foundation/146-PATTERNS.md
@.planning/sketches/061-control-room-shell/README.md
@.planning/sketches/062-gate-honesty-and-receipts/README.md
@frontend/src/App.tsx
@frontend/src/components/layout/ChatLayout.tsx
@frontend/src/components/layout/NavPanel.tsx
@frontend/src/pages/SkillStudioPage.tsx

<interfaces>
<!-- From Plan 04 (frontend/src/lib/api.ts + hooks):
       getOperatorProbe(): Promise<OperatorIdentity | null>   // 404 → null
       getBackpressure(): Promise<BackpressureSignals>
       getOperatorAudit(limit?: number): Promise<OperatorAuditRow[]>
       useOperatorProbe(): { isOperator: boolean; identity: OperatorIdentity | null; loading: boolean } -->

<!-- From Plan 05 (frontend/src/components/admin/):
       <OperatorBand identity onBack recordingPulse />   // band flash prop-controlled
       <HealthSignals signals showTechnical />
       <TechnicalNamesToggle enabled onToggle />
       <LockedTab title />
       <RecentActionsCard rows />                        // newest-first, ✎ write mark -->

<!-- ActiveView union (App.tsx:10) — ADD "control-room":
       export type ActiveView = "chat" | "documents" | "skills" | "settings" | "library-health" | "workflows" | "classification-rules" | "governance" | "skill-studio" -->

<!-- ChatLayout render-branch precedent (ChatLayout.tsx:321-343): add
       ) : activeView === "control-room" ? ( <ControlRoomPage ... />
     BEFORE the trailing KnowledgeHealthPage else — a full-surface branch like governance/skill-studio,
     inside <main className="flex-1 overflow-hidden"> (:296). -->

<!-- NavPanel footer-button + collapsed-tooltip idiom (NavPanel.tsx:414-471, the sign-out button shape).
     The shield renders ONLY when isOperator; lucide Shield + amber tint (NOT ShieldCheck at nav-items.ts:39).
     NAV_ITEMS (nav-items.ts:24-42) is consumed by BOTH NavPanel (:287) and the mobile drawer (ChatLayout.tsx:234)
     — do NOT touch nav-items.ts. -->

<!-- Day-one tabs (D-07): Overview + Audit live; locked = "System Controls", "Users & Access", "AI Models", "API Keys". -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ControlRoomPage + AuditTab — the 061-B shell with the refresh honesty beat</name>
  <files>frontend/src/components/admin/ControlRoomPage.tsx, frontend/src/components/admin/AuditTab.tsx</files>
  <read_first>
    - .planning/sketches/061-control-room-shell/README.md + index.html (exact shell layout, tab order, landing composition, refresh beat placement)
    - .planning/sketches/062-gate-honesty-and-receipts/README.md (the receipt beat: row lands top, marker flash; consequence ≠ receipt)
    - frontend/src/pages/SkillStudioPage.tsx (the header-band + role="tablist" tab bar structure at :130-187; the fetch-once-per-entry guarded reads at :66-94)
    - frontend/src/components/admin/OperatorBand.tsx, HealthSignals.tsx, RecentActionsCard.tsx, LockedTab.tsx, TechnicalNamesToggle.tsx (the Plan-05 leaf props)
    - frontend/src/lib/api.ts (getBackpressure, getOperatorAudit, the three types)
  </read_first>
  <action>
    Create `frontend/src/components/admin/ControlRoomPage.tsx`. Props: `identity: OperatorIdentity | null`, `onBack: () => void` (navigates back to "chat"). Structure mirrors SkillStudioPage: `<OperatorBand>` on top, then a horizontal `role="tablist"` tab bar with SIX tabs in sketch order — Overview (live), Audit (live, count-pill), System Controls (locked), Users & Access (locked), AI Models (locked), API Keys (locked). Locked tabs render `<LockedTab title>` as their body. Tab state is local `useState` (no router).
    Overview tab: on ENTRY fetch `getBackpressure()` + `getOperatorAudit()` once (independent guarded reads, each `.catch(() => {})` so one failure never nukes the other — the SkillStudioPage idiom); render `<HealthSignals signals showTechnical>` + `<TechnicalNamesToggle>` (page owns the `showTechnical` state) + `<RecentActionsCard rows>`. Add the visible ↻ Refresh button: on click re-run `getBackpressure()` AND then re-fetch `getOperatorAudit()` so the backend audit-floor's new "Viewed system health" row visibly prepends to the card (the D-04 honesty beat — the operator watches their own view get recorded), and pulse the band's recording marker (`recordingPulse` prop). NO auto-polling, NO setInterval — manual refresh only (D-04; auto-poll is Phase 147 territory).
    Create `frontend/src/components/admin/AuditTab.tsx`: the honest minimal full-history view — fetch `getOperatorAudit()` with a larger limit, render the rows (plain `label`, time, ✎ write mark — may reuse RecentActionsCard's row rendering or a simple list). NO search/filters/CSV (Phase 148 — do not build). The Audit tab button in ControlRoomPage shows a count-pill of loaded rows. No phase numbers anywhere in copy.
  </action>
  <verify>
    <automated>cd frontend && test -f src/components/admin/ControlRoomPage.tsx && test -f src/components/admin/AuditTab.tsx && grep -q "OperatorBand" src/components/admin/ControlRoomPage.tsx && grep -q "getOperatorAudit" src/components/admin/ControlRoomPage.tsx && ! grep -qE "setInterval|setTimeout\(.*fetch" src/components/admin/ControlRoomPage.tsx && (grep -RniE "phase *1[0-9][0-9]" src/components/admin/ControlRoomPage.tsx src/components/admin/AuditTab.tsx && { echo "PHASE NUMBER LEAK"; exit 1; } || true) && npx tsc --noEmit -p tsconfig.json 2>&1 | (! grep -qiE "components/admin/(ControlRoomPage|AuditTab)") && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - ControlRoomPage composes OperatorBand + six tabs (Overview/Audit live; the four D-07 locked titles via LockedTab)
    - Overview fetches health + audit once on entry; the ↻ Refresh handler re-fetches BOTH (backpressure then audit) and pulses the recording marker
    - No `setInterval`/polling anywhere in the file (grep asserts absence — D-04)
    - AuditTab renders history rows with no filter/CSV controls; the Audit tab button carries a count-pill
    - No `Phase 1xx` token in either file; both type-check clean
  </acceptance_criteria>
  <done>The 061-B shell is assembled with the manual-refresh ledger honesty beat and honest locked tabs.</done>
</task>

<task type="auto">
  <name>Task 2: Reachability triad — App.tsx union + probe, ChatLayout branch, NavPanel shield</name>
  <files>frontend/src/App.tsx, frontend/src/components/layout/ChatLayout.tsx, frontend/src/components/layout/NavPanel.tsx</files>
  <read_first>
    - frontend/src/App.tsx (the ActiveView union at :10; the skill-studio per-view state + navigator precedent at :26-37; the ChatLayout prop threading at :54-66)
    - frontend/src/components/layout/ChatLayout.tsx (the render branches at :295-346; the mobile drawer bottom icon row at :233-256; the NAV_ITEMS map at :234)
    - frontend/src/components/layout/NavPanel.tsx (the footer buttons + collapsed-tooltip idiom at :414-471; the NAV_ITEMS map at :287-318)
    - frontend/src/hooks/useOperatorProbe.ts (the Plan-04 hook — host it ONCE at App level)
  </read_first>
  <action>
    In `frontend/src/App.tsx`: extend the `ActiveView` union with `"control-room"` (:10). Call `useOperatorProbe()` once at App level (single probe per app mount — Pitfall 4 relies on the probe being floor-exempt server-side, but still probe once, not per-component) and thread `isOperator` + `identity` down to ChatLayout as props. Mirror the skill-studio threading precedent.
    In `frontend/src/components/layout/ChatLayout.tsx`: accept the new `isOperator`/`identity` props; add the `activeView === "control-room"` render branch BEFORE the trailing `KnowledgeHealthPage` else, rendering `<ControlRoomPage identity={identity} onBack={() => onNavigate("chat")} />` as a full-surface branch (like governance/skill-studio). In the mobile drawer's bottom icon row (:233-256), add the probe-gated shield as a SEPARATE element after the `NAV_ITEMS.map` output — rendered only when `isOperator`, navigating to "control-room".
    In `frontend/src/components/layout/NavPanel.tsx`: accept `isOperator`; render the amber lucide `Shield` entry at the rail bottom (footer area, above/beside theme + sign-out) using the footer-button + collapsed-tooltip idiom (:435-467) — ONLY when `isOperator` is true. onClick → `onNavigate("control-room")`. Tooltip label "Control Room". HARD RULE: do NOT modify `frontend/src/lib/nav-items.ts` — the shield lives outside the shared array (D-07 byte-identity; the sketch-061 README's "extends NAV_ITEMS conditionally" is superseded by CONTEXT D-07). When `isOperator` is false or loading, render NOTHING (no placeholder, no reserved space — byte-identical rail).
  </action>
  <verify>
    <automated>cd frontend && grep -q '"control-room"' src/App.tsx && grep -q "useOperatorProbe" src/App.tsx && grep -q '"control-room"' src/components/layout/ChatLayout.tsx && grep -q "ControlRoomPage" src/components/layout/ChatLayout.tsx && grep -q "isOperator" src/components/layout/NavPanel.tsx && git diff --name-only -- src/lib/nav-items.ts | (! grep -q "nav-items") && npx tsc --noEmit -p tsconfig.json 2>&1 | (! grep -qiE "(App.tsx|ChatLayout|NavPanel)") && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `ActiveView` union includes `"control-room"`; App hosts `useOperatorProbe()` exactly once and threads isOperator/identity to ChatLayout
    - ChatLayout has the control-room full-surface branch mounting ControlRoomPage with `onBack` → chat
    - NavPanel renders the amber Shield footer entry ONLY when isOperator (no element at all otherwise); mobile drawer gets the same probe-gated entry
    - `git diff` shows `frontend/src/lib/nav-items.ts` UNTOUCHED
    - Type-checks clean across the three files
  </acceptance_criteria>
  <done>The Control Room is reachable via the probe-gated shield; non-operator nav is byte-identical (triad complete).</done>
</task>

<task type="auto">
  <name>Task 3: NAV_ITEMS byte-identity regression test + frontend build gate</name>
  <files>frontend/src/lib/nav-items.test.ts</files>
  <read_first>
    - frontend/src/lib/nav-items.ts (the NAV_ITEMS array shape — id/label/icon entries)
    - frontend/src/hooks/useOperatorProbe.test.ts (the Plan-04 vitest style to match)
  </read_first>
  <action>
    Create `frontend/src/lib/nav-items.test.ts` (vitest): import `NAV_ITEMS` and assert (1) no entry has an id/view of `"control-room"`, and (2) no entry label contains "Control Room" — locking the D-07 contract that the shared nav array (consumed by BOTH the desktop rail and the mobile drawer) never carries the operator entry, so a non-operator's nav stays byte-identical to today. Add a comment naming D-07 and the RESEARCH anti-pattern ("Adding the shield to NAV_ITEMS shows it to everyone"). Then run the full frontend gates: `npx vitest run` (all frontend tests, including the Plan-04 probe test) and `npm run build` (vite build — catches integration breaks tsc alone misses). Note: some pre-existing vitest failures are known rot (SEED-056) — the gate is that nav-items.test.ts + useOperatorProbe.test.ts pass and the BUILD succeeds; do not chase unrelated pre-existing failures.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/nav-items.test.ts src/hooks/useOperatorProbe.test.ts 2>&1 | grep -qiE "pass|✓" && npm run build && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `nav-items.test.ts` asserts NAV_ITEMS contains no control-room id and no "Control Room" label; passes green
    - `npm run build` succeeds
    - The Plan-04 probe test still passes (no regression from the wiring)
  </acceptance_criteria>
  <done>The byte-identity contract is regression-locked and the frontend builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe result → nav render | Client-side probe decides rendering only; forging it must yield nothing |
| shared NAV_ITEMS → all users | Any operator entry in the shared array leaks the surface's existence to every user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-06 | Spoofing / Elevation of Privilege | client-forged isOperator flag | mitigate | The shield/page render off the probe, but every data call (`/admin/backpressure`, `/admin/audit`) is independently 404-gated server-side (Plan 02); a forged flag renders an empty shell that can fetch nothing (Pitfall 13 — UI-only gating is never the boundary). |
| T-146-02 | Information Disclosure | operator-surface discoverability via shared nav | mitigate | Shield rendered OUTSIDE NAV_ITEMS, probe-gated, no placeholder when absent; `nav-items.test.ts` regression-locks the array (D-07 / D-09 #1). |
| T-146-10 | Information Disclosure | phase-number leak in Control Room / Audit copy | mitigate | Task 1 grep asserts no `Phase 1xx` token in ControlRoomPage/AuditTab (D-09 #2). |
| T-146-11 | Repudiation | refresh actions bypassing the ledger beat | mitigate | ↻ Refresh re-fetches the audit feed AFTER backpressure so the floor's own "Viewed system health" row visibly lands (D-04/D-08); the row persists server-side (D-09 #3 verifies live). |
| T-146-SC | Tampering | npm installs | accept | No package installs (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `npx vitest run src/lib/nav-items.test.ts src/hooks/useOperatorProbe.test.ts` green; `npm run build` succeeds.
- `git diff` confirms `nav-items.ts` untouched; grep confirms no polling and no phase-number tokens.
- The reachability triad (union entry + mount branch + shield action) all present in one phase.
</verification>

<success_criteria>
- An operator reaches the Control Room via the probe-gated shield; Overview shows the four plain-labeled signals + the ledger; ↻ Refresh visibly prepends "Viewed system health" (D-04, D-08).
- A non-operator's nav is byte-identical (regression-locked); locked tabs are honest and phase-number-free (D-07).
</success_criteria>

<output>
Create `.planning/phases/146-operator-foundation/146-06-SUMMARY.md` when done.
</output>
