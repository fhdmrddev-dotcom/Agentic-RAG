---
phase: 148-governance-audit-users-feature-visibility
plan: 03
type: execute
wave: 3
depends_on: ["148-02"]
files_modified:
  - supabase/full-schema.sql
autonomous: false
requirements: [VIS-01]

must_haves:
  truths:
    - "Migration 098 is applied to the live local Supabase DB via the SQL editor (NEVER supabase db push / db reset — preserves dev data)"
    - "app_settings.feature_visibility exists live and the 'global' row carries the D-05 day-one seed"
    - "supabase/full-schema.sql is regenerated from the live DB (no reset) and reflects the new column"
    - "A cloud-parity note records that migration 098 + the seed must be pasted into cloud Supabase at promotion"
  artifacts:
    - path: "supabase/full-schema.sql"
      provides: "regenerated single-file deploy artifact including feature_visibility"
      contains: "feature_visibility"
  key_links:
    - from: "supabase/migrations/098_feature_visibility.sql"
      to: "live local app_settings.feature_visibility column"
      via: "operator paste into Supabase SQL editor"
      pattern: "feature_visibility"
---

<objective>
Apply migration 098 to the live local database and regenerate the bootstrap schema artifact. This is the project-specific schema-apply flow (CLAUDE.md), NOT the generic `supabase db push`. It is REQUIRED because backend/type checks pass without the live column (types come from config, not the DB) — a false-positive verification state until the column actually exists.

Purpose: Make the `feature_visibility` column real so the final G-4 live UAT exercises true audience resolution + the visibility-set write path.
Output: applied migration + regenerated `supabase/full-schema.sql` + a cloud-parity note.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/148-governance-audit-users-feature-visibility/148-RESEARCH.md
@supabase/migrations/098_feature_visibility.sql
@docs/DEPLOYMENT-WORKFLOW.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator applies migration 098 to the live local DB</name>
  <files>supabase/migrations/098_feature_visibility.sql (applied to the live DB, not edited)</files>
  <read_first>
    - supabase/migrations/098_feature_visibility.sql (the exact SQL to paste)
    - CLAUDE.md (migrations rule: apply via SQL editor — never db push / db reset)
  </read_first>
  <what-built>
    Migration `supabase/migrations/098_feature_visibility.sql` was written in 148-02: it adds the `feature_visibility jsonb` column to `app_settings` and seeds the `global` row with the D-05 day-one map (skill_studio + model_management = operators; workflow_authoring + governance_health = everyone). It is metadata-only — no data transform, no CHECK change.
  </what-built>
  <action>
    HUMAN-ONLY step (no CLI equivalent — the project forbids `supabase db push` / `db reset`). The operator opens the local Supabase SQL editor, pastes the FULL contents of `supabase/migrations/098_feature_visibility.sql`, runs it, and confirms with `SELECT id, feature_visibility FROM app_settings WHERE id = 'global';`. Claude must NOT attempt `db push` / `db reset` and must WAIT for the operator's "applied" signal before proceeding to Task 2 (the regen).
  </action>
  <how-to-verify>
    1. Open the local Supabase SQL editor (local Studio, project on Docker).
    2. Paste the FULL contents of `supabase/migrations/098_feature_visibility.sql` and run it. Do NOT run `supabase db push` or `supabase db reset` (both wipe / desync dev data).
    3. Confirm success by running `SELECT id, feature_visibility FROM app_settings WHERE id = 'global';` — the row should show the four-key JSON with `{"audience":"operators"}` for skill_studio/model_management and `{"audience":"everyone"}` for workflow_authoring/governance_health.
  </how-to-verify>
  <acceptance_criteria>
    - `SELECT feature_visibility FROM app_settings WHERE id='global'` returns the four-key enum-record map (operator confirms the JSON).
    - Migration applied via SQL editor, not `db push` / `db reset`.
  </acceptance_criteria>
  <verify>
    <human-check>Operator confirms the SELECT returns the four-key enum-record map on the live local DB.</human-check>
  </verify>
  <resume-signal>Type "applied" once the SELECT shows the four-key map, or paste the error if it failed.</resume-signal>
  <done>Migration 098 applied to the live local DB via the SQL editor; the global row shows the four-key audience map.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate full-schema.sql (no reset) + cloud-parity note</name>
  <files>supabase/full-schema.sql</files>
  <read_first>
    - CLAUDE.md (regenerate-full-schema.sh = live-DB dump, no reset; never hand-edit full-schema.sql)
    - scripts/regenerate-full-schema.sh
    - docs/DEPLOYMENT-WORKFLOW.md (cloud parity for new app_settings columns/rows at promotion)
  </read_first>
  <action>
    After the operator confirms 098 is applied, run `bash scripts/regenerate-full-schema.sh` (default = live-DB dump, NO reset — do NOT pass `--reset`). This rebuilds `supabase/full-schema.sql` to include the `feature_visibility` column. Do NOT hand-edit `full-schema.sql`. Then record a cloud-parity note in the plan SUMMARY: migration 098 (the column + the day-one seed UPDATE) MUST be pasted into the CLOUD Supabase SQL editor at the next production promotion (per docs/DEPLOYMENT-WORKFLOW.md + the standing v3.3 parity rule) — local and cloud each carry their own `app_settings.global` row, so the seed must run in both.
  </action>
  <acceptance_criteria>
    - `grep -c "feature_visibility" supabase/full-schema.sql` ≥ 1 (regen captured the column).
    - `git diff --stat supabase/full-schema.sql` shows the file changed (regenerated, not hand-edited — the diff is a dump delta, not a targeted line edit).
    - The SUMMARY carries an explicit cloud-parity line naming migration 098 + the seed as a promotion-time cloud step.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "feature_visibility" supabase/full-schema.sql</automated>
  </verify>
  <done>full-schema.sql regenerated from the live DB with the new column; cloud-parity note recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| local dev DB <-> cloud DB | Schema drift: a column applied locally but not to cloud silently breaks the live visibility write path at promotion. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-DRIFT | Tampering (config drift) | migration 098 apply | mitigate | Apply via SQL editor (never db push/reset — preserves dev data); regenerate full-schema.sql from the live dump; cloud-parity note ensures the promotion checklist re-applies 098 to cloud. |
| T-148-SC | Tampering | pip installs | accept | Zero new packages this phase. |
</threat_model>

<verification>
`SELECT feature_visibility FROM app_settings WHERE id='global'` shows the four-key enum map (operator-confirmed). `grep -c feature_visibility supabase/full-schema.sql` ≥ 1. No `supabase db push`/`db reset` was run.
</verification>

<success_criteria>
Migration 098 applied to the live local DB via the SQL editor; `full-schema.sql` regenerated (no reset) with the new column; cloud-parity step recorded for promotion.
</success_criteria>

<output>
Create `.planning/phases/148-governance-audit-users-feature-visibility/148-03-SUMMARY.md` when done (include the cloud-parity note).
</output>
