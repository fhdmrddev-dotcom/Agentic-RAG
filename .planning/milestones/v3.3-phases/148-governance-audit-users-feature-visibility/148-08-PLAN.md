---
phase: 148-governance-audit-users-feature-visibility
plan: 08
type: execute
wave: 5
depends_on: ["148-06", "148-07"]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/components/admin/ControlRoomPage.tsx
  - frontend/src/components/admin/AuditTab.tsx
autonomous: true
requirements: [ADMIN-03]

must_haves:
  truths:
    - "067-A: the Audit tab gains a source switch — Operator actions (operator_audit_log) | Platform activity (audit_log) — one filter/pagination/CSV grammar on both; never a second platform-activity page"
    - "The filter strip is the 029-A chip grammar: action-type + date-preset (Today/7d/30d/All/custom) chips with a live match count (amber at zero)"
    - "CSV export names its exact row count, exports exactly the filtered set, and the export itself lands in the ledger (audit.export receipt)"
    - "Switching to Platform activity shows the quiet in-surface note 'looking at user activity is itself recorded' (the SC#4 threat made legible)"
    - "Plain-first action labels (Documents / Chat & agent / Organizing / Other) with raw action_type codes behind ⌥ Technical names; clicking a user in a platform row filters to them"
  artifacts:
    - path: "frontend/src/components/admin/AuditTab.tsx"
      provides: "source switch + chip filters + pager + CSV button + view-platform note"
      contains: "source"
    - path: "frontend/src/lib/api.ts"
      provides: "platform-audit browse + export client calls"
      contains: "platformAudit"
  key_links:
    - from: "frontend/src/components/admin/AuditTab.tsx"
      to: "GET /admin/platform-audit + /export"
      via: "ControlRoomPage fetchers on source=platform"
      pattern: "platform"
    - from: "frontend/src/components/admin/AuditTab.tsx"
      to: "029-A chip-strip grammar (FilterBar heritage)"
      via: "action-type + date chips + live match count"
      pattern: "chip"
---

<objective>
Deliver the 067-A audit browser: upgrade the current placeholder AuditTab (which today renders a "search, date filters & export coming soon" note) into the locked source-switch + chip-filter + paged + recorded-CSV browser over BOTH ledgers. The sketch is the acceptance bar.

Purpose: An operator investigates what happened across both the operator ledger and the platform ledger, with the cross-user read made legible and the export honest.
Output: platform-audit api calls + ControlRoomPage fetchers + the rebuilt AuditTab.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/148-governance-audit-users-feature-visibility/148-PATTERNS.md
@.claude/skills/sketch-findings-agentic-rag/references/governance-audit-users-visibility.md
@frontend/src/components/admin/AuditTab.tsx
@frontend/src/components/admin/ControlRoomPage.tsx
@frontend/src/components/ingestion/FilterBar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: platform-audit api calls + ControlRoomPage source/fetch plumbing</name>
  <files>frontend/src/lib/api.ts, frontend/src/components/admin/ControlRoomPage.tsx</files>
  <read_first>
    - frontend/src/lib/api.ts (getBackpressure/killRun shape for admin GETs; OperatorAuditRow interface ~:3548-3556; ApiError ~:20-22)
    - frontend/src/components/admin/ControlRoomPage.tsx (fetchAudit ~:197-204 — the fetch-owner pattern with the alive.current guard; tab-render switch ~:378-472)
    - .planning/phases/148-governance-audit-users-feature-visibility/148-PATTERNS.md (AuditTab section — ControlRoomPage owns the fetch, AuditTab holds local filter state)
  </read_first>
  <action>
    Add to api.ts: `getPlatformAudit({userId?, actionTypes?, since?, until?, page, pageSize})` -> `GET /admin/platform-audit`, and `exportPlatformAudit(sameFilters)` -> `GET /admin/platform-audit/export` (CSV download; surface an over-cap 413/422 as a caught ApiError so the UI can show "narrow the filter"). In ControlRoomPage, add a `source` state ("operator" | "platform") and fetchers for both sources mirroring `fetchAudit` (`:197-204`) with the same `alive.current` guard + per-fetch `.catch` honest-degrade; pass rows + filter state + the source + the export handler down to AuditTab as props. Do NOT create a second tab/page — one AuditTab, two sources.
  </action>
  <acceptance_criteria>
    - api.ts has `getPlatformAudit` + `exportPlatformAudit` calling the two /admin/platform-audit endpoints; the over-cap case is a caught ApiError.
    - ControlRoomPage owns `source` state + both fetchers with the alive.current guard; AuditTab receives rows/filters/source/export via props.
    - `cd frontend && npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>Client calls + source/fetch plumbing land; build green; one tab two sources.</done>
</task>

<task type="auto">
  <name>Task 2: AuditTab source switch + 029-A chip filters + pager + live count</name>
  <files>frontend/src/components/admin/AuditTab.tsx</files>
  <read_first>
    - frontend/src/components/admin/AuditTab.tsx (current leaf ~:36-84; the "coming soon" note ~:77-79 this task replaces; keep the ✎ write-mark + formatWhen ~:24-33/:58-69)
    - frontend/src/components/ingestion/FilterBar.tsx (chip render + remove-X ~:225-255; the dashed "+ condition" chip; the live debounced count ~:118-155/:257-274; amber-at-zero signal ~:262-263)
    - .claude/skills/sketch-findings-agentic-rag/references/governance-audit-users-visibility.md (067-A: source switch, chip grammar, date presets + resolved-window readout, plain-first labels + ⌥ raw codes, click-a-user-to-filter, HTML row shapes + pager)
  </read_first>
  <action>
    Rebuild AuditTab per 067-A. Add the segmented source switch (Operator actions | Platform activity) using the sketch `.source-switch` CSS. Add the 029-A chip strip copied from FilterBar: an action-type chip (plain-first grouped Documents / Chat & agent / Organizing / Other, raw `action_type` codes behind ⌥ Technical names), a date chip with presets Today / 7d / 30d / All / custom + a resolved-window readout, and (platform source) an optional per-user chip; render the dashed "+ condition" affordance + remove-X on each chip. Show a live match count that goes amber at zero (the trust cue). Render the paged table: operator-source row `[when mono] [✎? label] [operator email right]`; platform-source row `[when] [user — click to filter] [label + detail + ⌥code]`; pager `‹ Prev · page i of N · Next ›`. Clicking a user in a platform row sets the per-user filter. Keep the existing ✎ write-mark + formatWhen rendering.
  </action>
  <acceptance_criteria>
    - AuditTab renders the source switch, the action-type + date chips (029-A shape) with the live amber-at-zero count, and the paged table with the two row shapes + pager.
    - Plain-first labels group into Documents / Chat & agent / Organizing / Other; raw codes sit behind ⌥ Technical names.
    - Clicking a user in a platform row filters to them.
    - `cd frontend && npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>The placeholder "coming soon" note is replaced by the 067-A source-switch + chip-filter + paged browser; build green.</done>
</task>

<task type="auto">
  <name>Task 3: Recorded CSV export (names its count) + the view-platform note</name>
  <files>frontend/src/components/admin/AuditTab.tsx</files>
  <read_first>
    - frontend/src/components/admin/AuditTab.tsx (the filter state + count from Task 2)
    - .claude/skills/sketch-findings-agentic-rag/references/governance-audit-users-visibility.md (067-A: CSV names its row count / exports the filtered set / export lands in the ledger; the quiet "looking at user activity is itself recorded" note on switch-to-Platform)
    - frontend/src/lib/api.ts (exportPlatformAudit from Task 1)
  </read_first>
  <action>
    Add the CSV export button that NAMES its exact match count (e.g. "Export 342 entries") and exports exactly the filtered set via `exportPlatformAudit` — the server records the `audit.export` receipt (148-06); on an over-cap 413/422 show the caught "Too many rows — narrow the filter" message instead of downloading. When the source switches to Platform activity, show the quiet in-surface note "looking at user activity is itself recorded" (the SC#4 threat made legible). The freshly-recorded export/view rows appear in the ledger on the next fetch (the ✎ receipt vocabulary).
  </action>
  <acceptance_criteria>
    - The export button label names the live match count; export calls `exportPlatformAudit` with the current filters; over-cap shows the narrow-the-filter message (no partial download).
    - Switching to Platform activity renders the "looking at user activity is itself recorded" note.
    - `cd frontend && npm run build` exits 0.
    - (Lived-experience: "export row count matches the shown count + a ✎ Exported N receipt appears" is the phase G-4 UAT in 148-VALIDATION.md.)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>Count-naming recorded CSV export + the view-platform legibility note land; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| operator viewing platform activity | Cross-user reads are a governance action — the UI must make them legible (the recorded note), never present them as free/silent. |
| CSV export request | The client passes the current filters; the server enforces the cap + records the receipt (148-04/148-06) — the client only surfaces the refusal. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-01 | Information Disclosure | platform browse + CSV UI | mitigate | The UI names the export count + surfaces the over-cap refusal; the "viewing is recorded" note makes the SC#4 cross-user read legible. Enforcement (scope/cap/record) is server-side (148-04/06). |
| T-148-SC | Tampering | npm installs | accept | Zero new packages (reuses FilterBar chip grammar + existing api client). |
</threat_model>

<verification>
`cd frontend && npm run build` exits 0. AuditTab shows the source switch + 029-A chips + pager + count-naming CSV + the recorded-view note. Full lived-experience (export count == shown count + ✎ receipt) is the phase G-4 UAT (148-VALIDATION.md).
</verification>

<success_criteria>
The 067-A one-browser-two-sources audit surface with chip filters, pager, count-naming recorded CSV, and the cross-user-read legibility note lands; frontend build green.
</success_criteria>

<output>
Create `.planning/phases/148-governance-audit-users-feature-visibility/148-08-SUMMARY.md` when done.
</output>
