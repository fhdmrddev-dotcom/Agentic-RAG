---
phase: 148-governance-audit-users-feature-visibility
plan: 07
type: execute
wave: 4
depends_on: ["148-05"]
files_modified:
  - frontend/src/hooks/useEffectiveFeatures.ts
  - frontend/src/lib/api.ts
  - frontend/src/App.tsx
  - frontend/src/lib/nav-items.ts
autonomous: true
requirements: [VIS-01]

must_haves:
  truths:
    - "D-04: the frontend learns the map via an authenticated GET /features fetch (mirrors useOperatorProbe — one-shot per session keyed to userId, re-probes on user switch, clears on sign-out)"
    - "Hidden features simply do not render in nav/pages (the sketch's vanish — never badge-locked); nav items filter by the effective map"
    - "D-04: a mid-session flip is a graceful bounce — a governed page's next data fetch returns 403 -> catch ApiError.status===403 -> plain refusal + setActiveView('chat')"
    - "The hook fails CLOSED to {} (hide governed features) on error — render-only; the API (148-05) is the security authority"
  artifacts:
    - path: "frontend/src/hooks/useEffectiveFeatures.ts"
      provides: "one-shot per-session effective-features fetch + refetch()"
      contains: "useEffectiveFeatures"
    - path: "frontend/src/App.tsx"
      provides: "nav filtering by effective map + ActiveView 403 graceful bounce"
      contains: "useEffectiveFeatures"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "GET /features (via getEffectiveFeatures)"
      via: "useEffectiveFeatures(user?.id ?? null) alongside useOperatorProbe"
      pattern: "useEffectiveFeatures"
    - from: "frontend/src/App.tsx"
      to: "ApiError.status === 403"
      via: "governed page fetch catch -> plain refusal + setActiveView('chat')"
      pattern: "403"
---

<objective>
Wire the VIS-01 frontend render layer: a `useEffectiveFeatures` hook mirroring `useOperatorProbe`, nav filtering so hidden features vanish, and the graceful 403 bounce for a mid-session flip. This is render-only — 148-05's API is the wall; the frontend just hides + bounces.

Purpose: End users never see (or dead-click) a feature they cannot use; a mid-session tighten routes them home with a plain refusal, not a broken page.
Output: 1 new hook + api client fn + App.tsx nav/bounce wiring + nav-items gating source.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/148-governance-audit-users-feature-visibility/148-RESEARCH.md
@.planning/phases/148-governance-audit-users-feature-visibility/148-PATTERNS.md
@.claude/skills/sketch-findings-agentic-rag/references/governance-audit-users-visibility.md
@frontend/src/hooks/useOperatorProbe.ts
@frontend/src/App.tsx
@frontend/src/lib/nav-items.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: useEffectiveFeatures hook + getEffectiveFeatures client fn</name>
  <files>frontend/src/hooks/useEffectiveFeatures.ts, frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/hooks/useOperatorProbe.ts (whole file — mirror it: one-shot fetch keyed to userId, the `cancelled` local-flag guard ~:45-83, the fail-closed catch ~:71-75)
    - frontend/src/lib/api.ts (getOperatorProbe ~:3563-3569 for the fetch shape; ApiError ~:20-22 for the status-carrying error)
    - .planning/phases/148-governance-audit-users-feature-visibility/148-RESEARCH.md §Pattern 5 (the TS hook contract) + WR-01 (keyed to userId, not App mount)
  </read_first>
  <action>
    Create `useEffectiveFeatures.ts` mirroring `useOperatorProbe.ts`: a one-shot per-session fetch keyed to `userId` (re-probes on user switch, clears on sign-out — NOT on App mount, WR-01), the same `cancelled` guard. DIFFERENCE: resolve to a features map object (e.g. `{ skill_studio:boolean, model_management:boolean, workflow_authoring:boolean, governance_health:boolean }`), and fail CLOSED to `{}` (hide all governed features) on error — not `null` identity. Expose a `refetch()` for the graceful-bounce re-check (D-04). Add `getEffectiveFeatures()` to `api.ts` (a plain 200 fetch of `GET /features`, mirroring `getOperatorProbe` but not 404->null — every user has a map). Ensure `ApiError` carries `.status` (it already does at ~:20-22) so governed pages can catch 403.
  </action>
  <acceptance_criteria>
    - `useEffectiveFeatures.ts` exists, exports a hook keyed to `userId` with a `refetch`, fails closed to `{}` on error (grep the empty-map fallback).
    - `getEffectiveFeatures()` added to api.ts calling `GET /features`.
    - `cd frontend && npm run build` exits 0 (tsc -b + vite build gate — not just tsc --noEmit).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>Hook + client fn land; build green; fail-closed on error.</done>
</task>

<task type="auto">
  <name>Task 2: Nav filtering by the effective map</name>
  <files>frontend/src/App.tsx, frontend/src/lib/nav-items.ts</files>
  <read_first>
    - frontend/src/App.tsx (~:102 useOperatorProbe(user?.id ?? null) wiring threaded as isOperator into ChatLayout ~:126; activeView state ~:71)
    - frontend/src/lib/nav-items.ts (NAV_ITEMS — the gating source; identify the nav entries for Skill Studio, Settings/model management, workflow authoring, governance health)
    - .claude/skills/sketch-findings-agentic-rag/references/governance-audit-users-visibility.md (069-A: hidden features simply do not render — the vanish, never badge-locked)
  </read_first>
  <action>
    Add `useEffectiveFeatures(user?.id ?? null)` in App.tsx alongside the operator probe (`:102`). Tag each governed NAV_ITEMS entry with its feature key (skill_studio / model_management / workflow_authoring / governance_health) and filter the nav list by the effective map before passing it to the nav — a feature resolving false simply is not rendered (the sketch's vanish; NEVER a locked/badged item). Operators see everything (the map returns all-true for them). Leave ungoverned nav items (Chat, Documents, Run, etc.) always visible.
  </action>
  <acceptance_criteria>
    - NAV_ITEMS governed entries carry a feature-key tag; App.tsx filters nav by the effective map (grep the filter using the map).
    - A false-resolving feature renders no nav item (no locked/badge placeholder) — assert in source that the filter DROPS the item, not disables it.
    - `cd frontend && npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>Governed nav items vanish for non-operators per the effective map; build green.</done>
</task>

<task type="auto">
  <name>Task 3: ActiveView 403 graceful bounce (mid-session flip)</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/App.tsx (activeView state ~:71; the ActiveView render switch for governed views)
    - frontend/src/lib/api.ts (ApiError.status ~:20-22)
    - .planning/phases/148-governance-audit-users-feature-visibility/148-RESEARCH.md §Pattern 5 (graceful bounce) + .../148-CONTEXT.md D-04
  </read_first>
  <action>
    Implement the graceful bounce (D-04): when a governed page's next data fetch throws `ApiError` with `status === 403`, catch it, show the plain refusal ("This feature is available to administrators only." — matching the server body), and `setActiveView("chat")` to route home; optionally `refetch()` the effective map so the nav re-syncs within the ~30s TTL window. Do NOT gate this on the client map alone — the 403 from the server is the trigger (the flip may land mid-session before the map refetch). Keep it a plain refusal + route, never a crash/blank.
  </action>
  <acceptance_criteria>
    - A governed view's fetch catch handles `err.status === 403` -> plain refusal + `setActiveView("chat")` (grep the 403 catch + setActiveView).
    - The refusal copy matches the server body ("...available to administrators only.").
    - `cd frontend && npm run build` exits 0.
    - (Lived-experience: the mid-session-flip bounce + Run-still-works is exercised by the phase G-4 UAT in 148-VALIDATION.md — not re-run here.)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>Mid-session 403 routes home with a plain refusal; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client render <-> API authority | The frontend hides + bounces only; VIS-01 enforcement lives server-side (148-05). A hidden nav item is not a security control. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-07 | Information Disclosure | nav vanish + 403 refusal | accept | The 403 body is a plain product message (D-03); the client hides features but the server is the wall — no reliance on client hiding for security (Pitfall 13). |
| T-148-FAILCLOSED | Elevation | useEffectiveFeatures error path | mitigate | Hook fails CLOSED to {} (hide governed features) on fetch error — a blip never flashes an operators-only feature to an end user. |
| T-148-SC | Tampering | npm installs | accept | Zero new packages this phase. |
</threat_model>

<verification>
`cd frontend && npm run build` exits 0. Nav vanishes governed features per the effective map; a 403 routes home with the plain refusal. Full lived-experience (mid-session flip, Run-still-works) is the phase G-4 UAT (148-VALIDATION.md).
</verification>

<success_criteria>
useEffectiveFeatures (per-session, fail-closed) + nav vanish + graceful 403 bounce land; frontend build green; the API remains the sole enforcement authority.
</success_criteria>

<output>
Create `.planning/phases/148-governance-audit-users-feature-visibility/148-07-SUMMARY.md` when done.
</output>
