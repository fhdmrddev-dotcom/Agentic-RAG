---
phase: 148-governance-audit-users-feature-visibility
plan: 09
type: execute
wave: 6
depends_on: ["148-06", "148-08"]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/components/admin/ControlRoomPage.tsx
  - frontend/src/components/admin/UsersAndAccess.tsx
  - frontend/src/components/admin/FeatureVisibility.tsx
autonomous: true
requirements: [ADMIN-03, VIS-01]

must_haves:
  truths:
    - "068-A: the locked Users & Access tab unlocks into an instrument-table roster — identity + honest last-active (recent=green, stale=dim, 'never signed in' italic, never fabricated) + status chip + role chip + per-row actions"
    - "Disable = the 064-B victim-naming sheet (names the user, states the immediate effect + what is KEPT + reversibility + that it's recorded); Enable = restorative, flips direct (deliberate asymmetry)"
    - "D-01: Grant/revoke operator = an AMBER sheet naming the blast radius; self-row Disable/Remove-operator are disabled with a tooltip (courtesy — the server self-guard in 148-04/06 is the real wall)"
    - "069-A: feature-visibility audience rows live BELOW the roster (never next to the kill-switches — the rejected 069-C foil); two-position control Everyone | ⛨ Operators only; the stored/sent value is an enum, NEVER a boolean"
    - "Operators-only styling is amber-warmed, NEVER kill-switch red; flipping reveals the concrete consequence line + an expandable 'what exactly this controls'; every write flips the row to a ✎ … · recorded receipt + band-marker flash (062-A)"
    - "D-02: impersonation ('Sign in as user') is NOT built — the roster + audit browser + active-runs are the support surface (deferred with a named trigger)"
  artifacts:
    - path: "frontend/src/components/admin/UsersAndAccess.tsx"
      provides: "roster instrument table + victim-naming disable sheet + amber grant/revoke sheets + self-guards"
      contains: "never signed in"
    - path: "frontend/src/components/admin/FeatureVisibility.tsx"
      provides: "audience rows (Everyone | ⛨ Operators only) + consequence line + expandable details"
      contains: "Operators only"
  key_links:
    - from: "frontend/src/components/admin/ControlRoomPage.tsx"
      to: "UsersAndAccess + FeatureVisibility"
      via: "unlock users-access tab -> render roster then visibility below it"
      pattern: "UsersAndAccess"
    - from: "frontend/src/components/admin/FeatureVisibility.tsx"
      to: "PUT /admin/visibility"
      via: "enum audience value (never boolean)"
      pattern: "audience"
---

<objective>
Unlock the locked Users & Access tab into the 068-A instrument roster (with graded action guards) and stack the 069-A feature-visibility audience rows below it. The two locked sketches are the acceptance bar — translate, do not re-litigate. Impersonation is deliberately NOT built (D-02).

Purpose: An operator governs WHO gets in (roster + disable/enable + grant/revoke) and WHO sees advanced features (audience rows), all with honest last-active, graded guards, and the never-boolean audience contract.
Output: roster/visibility api calls + ControlRoomPage unlock + two new components.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/148-governance-audit-users-feature-visibility/148-PATTERNS.md
@.claude/skills/sketch-findings-agentic-rag/references/governance-audit-users-visibility.md
@frontend/src/components/admin/ControlRoomPage.tsx
@frontend/src/components/admin/LockedTab.tsx
@frontend/src/components/admin/ActiveRunsSection.tsx
@frontend/src/components/admin/CapabilityGrid.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: roster/visibility api calls + ControlRoomPage unlock + fetch plumbing</name>
  <files>frontend/src/lib/api.ts, frontend/src/components/admin/ControlRoomPage.tsx</files>
  <read_first>
    - frontend/src/components/admin/ControlRoomPage.tsx (tab-def array ~:90-111 — flip users-access locked:false ~:92-97; the LockedTab fallback branch ~:468-472 -> swap for the render branch; fetchAudit ~:197-204 fetcher pattern + alive.current guard)
    - frontend/src/lib/api.ts (killRun/getBackpressure admin-call shape; ApiError ~:20-22)
    - .planning/phases/148-governance-audit-users-feature-visibility/148-PATTERNS.md (ControlRoomPage unlock seam + the roster/visibility fetchers)
  </read_first>
  <action>
    Add to api.ts: `getUsersRoster({page,pageSize})` -> GET /admin/users; `disableUser(id)` / `enableUser(id)` -> POST /admin/users/{id}/disable|enable; `grantOperator(id)` / `revokeOperator(id)` -> POST|DELETE /admin/users/{id}/operator; `setFeatureVisibility(feature, audience)` -> PUT /admin/visibility with the ENUM audience string (never a boolean). In ControlRoomPage: flip the `users-access` tab def `locked: false` (`:92-97`), replace its `<LockedTab>` branch (`:468-472`) with a branch that renders `<UsersAndAccess/>` and, below it, `<FeatureVisibility/>` (069-A location: visibility below the roster, NOT in the Controls/kill-switch tab). Add roster + visibility fetchers mirroring `fetchAudit` (`:197-204`) with the same `alive.current` guard + per-fetch `.catch` honest-degrade. Keep `LockedTab` for model-registry (149) / secrets (150).
  </action>
  <acceptance_criteria>
    - api.ts has roster/disable/enable/grant/revoke calls + `setFeatureVisibility` sending the enum audience (grep confirms no boolean payload).
    - ControlRoomPage users-access tab is `locked: false` and renders `<UsersAndAccess/>` then `<FeatureVisibility/>` below it; other locked tabs still use LockedTab.
    - `cd frontend && npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>Client calls + tab unlock + stacked render plumbing land; build green.</done>
</task>

<task type="auto">
  <name>Task 2: UsersAndAccess roster — instrument table + graded action guards</name>
  <files>frontend/src/components/admin/UsersAndAccess.tsx</files>
  <read_first>
    - frontend/src/components/admin/ActiveRunsSection.tsx (the 064-B victim-naming Kill row + confirm-sheet anchored to the row)
    - frontend/src/components/admin/CapabilityGrid.tsx (the ✎ … · recorded receipt + band-marker flash 062-A vocabulary to reuse)
    - .claude/skills/sketch-findings-agentic-rag/references/governance-audit-users-visibility.md (068-A: roster columns + last-active honesty CSS `.last-active.recent/.never`, status/role chip CSS, the disable victim-naming sheet copy, restorative direct Enable, self-protection tooltips, the amber grant/revoke sheet blast-radius copy, HTML row shape) + the CSS Patterns block
  </read_first>
  <action>
    Create `UsersAndAccess.tsx` per 068-A. One dense instrument table, row shape `[avatar] [email / joined · docs · chats] [last-active] [status] [role] [actions]`, search box, sorted newest-active first. Last-active honesty: `last_sign_in_at` recent -> `.last-active.recent` (green), stale -> dim, NULL -> `never signed in` italic (`.last-active.never`) — never fabricated. Status chip `.u-status.active/.disabled`; role chip `.u-role` (⛨ operator). Disable = the 064-B victim-naming confirm SHEET anchored to the row (names the user; states "loses access immediately — sign-in refused, API refused, in-flight run cancelled"; states what is KEPT — "their documents, chats and settings are kept, untouched"; states reversibility; says it's recorded) -> calls `disableUser`. Enable = restorative, flips DIRECT (no sheet). Grant/revoke operator = an AMBER sheet naming the blast radius ("they can see every user's activity, kill anyone's runs…"; revoke keeps their normal account; past operator actions stay in the trail) -> `grantOperator`/`revokeOperator`. Self-row Disable + Remove-operator are DISABLED with a tooltip ("You cannot disable yourself") — courtesy only; the server self-guard (148-04/06) is the real wall. The disabled row shows what THAT user sees ("This account is disabled — contact your administrator"). Every write flips the row to a `✎ … · recorded` receipt + band-marker flash (062-A). Do NOT build any impersonation / "Sign in as user" affordance (D-02 deferred).
  </action>
  <acceptance_criteria>
    - Roster renders the instrument-table row shape with honest last-active (`never signed in` italic for NULL), status + role chips, search, newest-active-first sort.
    - Disable opens a victim-naming sheet with the effect + KEPT + reversibility + recorded copy; Enable flips direct (no sheet); grant/revoke open amber blast-radius sheets.
    - Self-row Disable + Remove-operator are disabled with the tooltip; no impersonation control exists anywhere in the file (grep: no "sign in as" / "impersonate").
    - Operators-only/role styling is amber (`.u-role` warning tone), never kill-switch red.
    - `cd frontend && npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>The 068-A roster with victim-naming disable, direct enable, amber grant/revoke, self-guards, and honest last-active lands (no impersonation); build green.</done>
</task>

<task type="auto">
  <name>Task 3: FeatureVisibility audience rows — enum control + consequence lines</name>
  <files>frontend/src/components/admin/FeatureVisibility.tsx</files>
  <read_first>
    - frontend/src/components/admin/CapabilityGrid.tsx + MaintenancePanel.tsx (the toggle-row + consequence-line grammar; the ✎ receipt)
    - .claude/skills/sketch-findings-agentic-rag/references/governance-audit-users-visibility.md (069-A: audience rows, the `.aud-seg`/`.feat-card.op-only` amber-warmed CSS never-red, the consequence line "End users no longer see X — and their API calls to it are refused server-side, not just hidden", the `<details>` enforcement grid, the day-one map, the extensible-audience never-boolean contract) + the CSS Patterns block
  </read_first>
  <action>
    Create `FeatureVisibility.tsx` per 069-A. One card per advanced feature — day-one map: Skill Studio (ONE flag covering eval studio + trigger tuner) · model management · workflow authoring & publishing · governance health. Each card `[glyph] [name / desc · lives on X] [receipt?] [audience seg]`. The audience control is a TWO-position segmented control Everyone | ⛨ Operators only that reads/writes an ENUM value (never a boolean — the extensible-audience contract; design it to grow into an audience picker) and calls `setFeatureVisibility(feature, "everyone"|"operators")`. Styling: `.aud-seg button.on.everyone` (success tone), `.aud-seg button.on.ops` (amber) + `.feat-card.op-only` amber-warmed border — NEVER kill-switch red, and this surface NEVER sits next to the kill-switches (it lives on Users & Access, below the roster — the rejected 069-C in-Controls placement). Flipping to Operators-only reveals the concrete consequence line ("End users no longer see X — and their API calls to it are refused server-side, not just hidden") + an expandable `<details>` "what exactly this controls" (UI surface · refused API · who decides; route prefixes behind ⌥). Every flip = a ✎ `visibility.set` receipt, direct with receipt (reversible, no victim — the 066 graded-guard rule).
  </action>
  <acceptance_criteria>
    - Four feature cards (skill_studio, model_management, workflow_authoring, governance_health) each with the two-position Everyone | ⛨ Operators only segmented control writing an ENUM audience (grep: the setFeatureVisibility call sends "everyone"/"operators", not a boolean).
    - Operators-only uses amber `.aud-seg .ops` / `.feat-card.op-only`, never a kill-switch-red class; the component lives below the roster (rendered by ControlRoomPage in Task 1), not in the Controls tab.
    - Flipping to Operators-only shows the "refused server-side, not just hidden" consequence line + the expandable enforcement `<details>`; each flip shows the ✎ visibility.set receipt.
    - `cd frontend && npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>The 069-A audience rows (enum, amber-never-red, consequence line + enforcement details, ✎ receipt) land below the roster; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| operator UI action -> server write | Disable/grant/revoke/visibility are all server-enforced (148-06); the sheets/tooltips are UX, not controls. |
| operator self-target | The self-row disable/remove tooltip is courtesy; the server self-guard (148-04/06) is the real lockout wall. |
| audience value client -> server | The control emits an enum ("everyone"/"operators"), never a boolean — preserves the SEED-115 forward-compat contract; the server allowlists it (148-06). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-04 | Denial of Service | self-row guard | mitigate | Self Disable/Remove-operator disabled in the UI (tooltip) AND refused server-side (148-04/06) — defense in depth against self-lockout. |
| T-148-CONTRACT | Tampering (forward-compat) | audience control | mitigate | The segmented control reads/writes an enum audience, never a boolean; designed to grow into an audience picker (SEED-115 never-boolean contract). |
| T-148-STYLE | (safety/UX) | visibility vs kill-switch | mitigate | Operators-only = amber-warmed, never kill-switch red; the visibility map lives below the roster, never adjacent to the kill switches (rejected 069-C foil). |
| T-148-D02 | (scope) | impersonation | accept | "Sign in as user" is NOT built (D-02 deferred); the roster + audit browser + active-runs are the support surface with a named re-open trigger. |
| T-148-SC | Tampering | npm installs | accept | Zero new packages. |
</threat_model>

<verification>
`cd frontend && npm run build` exits 0. Users & Access unlocks into the 068-A roster (honest last-active, victim-naming disable, direct enable, amber grant/revoke, self-guards) with the 069-A audience rows (enum control, amber-never-red, consequence line, ✎ receipt) below it; no impersonation control. Full lived-experience (disabled user is really out; the map is honest + API is the wall) is the phase G-4 UAT (148-VALIDATION.md).
</verification>

<success_criteria>
The 068-A roster + graded guards and the 069-A never-boolean audience rows land on the unlocked Users & Access tab; impersonation deliberately absent (D-02); frontend build green.
</success_criteria>

<output>
Create `.planning/phases/148-governance-audit-users-feature-visibility/148-09-SUMMARY.md` when done.
</output>
