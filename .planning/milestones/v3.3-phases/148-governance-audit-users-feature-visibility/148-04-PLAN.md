---
phase: 148-governance-audit-users-feature-visibility
plan: 04
type: execute
wave: 3
depends_on: ["148-02"]
files_modified:
  - backend/app/services/governance_service.py
  - backend/app/services/operator_service.py
autonomous: true
requirements: [ADMIN-03]

must_haves:
  truths:
    - "The platform audit query is parameterized ($N), explicitly user-scoped-or-all, and ALWAYS paginated (page_size le=100) — no unbounded SELECT * (SC#4 no-full-tenant-leak)"
    - "The CSV generator streams EXACTLY the filtered set; over ~50000 rows it REFUSES (narrow the filter) rather than truncating"
    - "The roster query is ONE join returning honest last-active (last_sign_in_at NULL preserved, never fabricated) + doc/chat counts + is_operator"
    - "D-01: grant populates operator_users.granted_by = the acting operator; revoke refuses self-revoke server-side BEFORE the delete (lockout-proof); past operator actions stay in operator_audit_log forever"
  artifacts:
    - path: "backend/app/services/governance_service.py"
      provides: "platform audit query builder + capped CSV generator + roster query"
      exports: ["query_platform_audit", "export_platform_audit_csv", "list_users_roster"]
    - path: "backend/app/services/operator_service.py"
      provides: "grant_operator / revoke_operator helpers populating granted_by"
      contains: "granted_by"
  key_links:
    - from: "backend/app/services/governance_service.py"
      to: "audit_log table"
      via: "asyncpg parameterized WHERE (user filter?) AND action_type = ANY AND created_at window, LIMIT/OFFSET"
      pattern: "action_type = ANY"
    - from: "backend/app/services/operator_service.py grant_operator"
      to: "operator_users.granted_by"
      via: "INSERT ... ON CONFLICT DO UPDATE SET granted_by = EXCLUDED.granted_by"
      pattern: "granted_by"
---

<objective>
Build the service layer for the audit browser + users roster + operator grant/revoke — the query builders, the capped CSV generator, and the membership writers — all delegating to asyncpg via the live pool. No request handlers here (those land in 148-06); this is the reusable, unit-tested core that makes the no-full-tenant-leak and lockout-proof guarantees.

Purpose: Centralize the SC#4-sensitive cross-user reads so the admin.py controllers stay thin.
Output: 1 new service module + grant/revoke/roster helpers on operator_service.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/148-governance-audit-users-feature-visibility/148-RESEARCH.md
@.planning/phases/148-governance-audit-users-feature-visibility/148-PATTERNS.md
@backend/app/services/operator_service.py
@backend/app/api/audit.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Platform audit query builder + capped CSV generator (governance_service.py)</name>
  <files>backend/app/services/governance_service.py</files>
  <read_first>
    - .planning/phases/148-governance-audit-users-feature-visibility/148-RESEARCH.md §Pattern 3 (verbatim parameterized SQL) + the CSV cap rule (COUNT-first, refuse-if-exceeded)
    - backend/app/api/audit.py (lines ~80-112: csv.writer + io.StringIO + StreamingResponse shape — copy the writer, DROP the .eq(user_id) owner filter)
    - backend/app/services/operator_service.py (swallow-and-log read posture ~:89-109; lazy `from app.dependencies import get_pg_pool` inside functions ~:38/:119/:143)
    - backend/tests/test_148_platform_audit_filters.py, test_148_platform_audit_scope.py, test_148_csv_export.py (targets)
  </read_first>
  <behavior>
    - query_platform_audit(user_id=None, action_types=None, since=None, until=None, page_size, offset): builds `WHERE ($1::uuid IS NULL OR user_id=$1) AND ($2::text[] IS NULL OR action_type = ANY($2)) AND ($3::timestamptz IS NULL OR created_at >= $3) AND ($4::timestamptz IS NULL OR created_at < $4) ORDER BY created_at DESC LIMIT $5 OFFSET $6`; page_size clamped to <=100.
    - user_id NULL -> all users (deliberate cross-user read); a value -> single-user scope. Never emits an unbounded SELECT *.
    - export_platform_audit_csv(same filters): COUNT-first with the SAME WHERE; if count > _CSV_MAX_ROWS (50000) raise a refuse error ("Too many rows ({count}) — narrow the filter"); else stream the filtered rows via csv.writer/io.StringIO/StreamingResponse. Returns (StreamingResponse, count) so the caller can record the exact count.
  </behavior>
  <action>
    Create `governance_service.py`. Implement `query_platform_audit(...)` per RESEARCH §Pattern 3 — column names are code constants, every filter is a `$N` bind, `page_size` clamped `min(page_size, 100)`, always LIMIT/OFFSET. Implement `export_platform_audit_csv(...)` copying the `audit.py:95-112` writer shape but with asyncpg (not the supabase builder), DROP the owner `.eq(user_id)`, add `_CSV_MAX_ROWS = 50000`, COUNT-first, and REFUSE (raise a domain error the endpoint maps to 413/422) when over cap — never a silent truncate. Return the exact row count alongside the stream so 148-06 can stamp `audit.export` with it. Use the swallow-and-log posture from `operator_service.get_recent_operator_audit` for the browse read; the export refusal must propagate (it is a deliberate 4xx, not a swallow). Lazy-import `get_pg_pool` inside each function (avoid the dependencies<->service import cycle). asyncpg reads use the live pool.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_148_platform_audit_filters.py tests/test_148_platform_audit_scope.py tests/test_148_csv_export.py -x` exits 0.
    - The query SQL contains `action_type = ANY` and `LIMIT` + `OFFSET`; page_size is clamped `<= 100` (grep the `min(` clamp or an le=100 guard).
    - `_CSV_MAX_ROWS = 50000` present; the export path COUNT-checks and raises-not-truncates over cap; returns the exact count.
    - No `SELECT *` without LIMIT anywhere in the module.
    - SCOPE (revision): `test_148_csv_export.py` holds ONLY service-level cap/refuse/exact-count assertions — this task's `<verify>` passes in wave 3 with NO controller code. The `audit.export`-recorded / refused-writes-nothing assertions live in `test_148_view_platform_recorded.py`, owned by 148-06 (wave 4).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_148_platform_audit_filters.py tests/test_148_platform_audit_scope.py tests/test_148_csv_export.py -x</automated>
  </verify>
  <done>Parameterized/scoped/paginated audit query + capped refuse-if-exceeded CSV land; three audit tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Roster query — one join, honest last-active (governance_service.py)</name>
  <files>backend/app/services/governance_service.py</files>
  <read_first>
    - .planning/phases/148-governance-audit-users-feature-visibility/148-RESEARCH.md §Code Examples "Roster read" (verbatim SQL) + Assumption A1 (documents/threads carry user_id)
    - backend/app/api/admin.py (~:261-265 email_by_user enrich — the same auth.users cross-user read pattern; confirms user_id join columns)
    - backend/tests/test_148_roster.py (target: test_last_active_honesty)
  </read_first>
  <behavior>
    - list_users_roster(page_size, offset): returns rows of {id, email, created_at, last_sign_in_at, banned_until, is_operator, doc_count, chat_count}.
    - last_sign_in_at NULL is PRESERVED as None (surfaces as "never signed in" in the UI) — never backfilled/fabricated.
    - banned_until in the future is returned so the UI can render the Disabled chip.
    - ORDER BY last_sign_in_at DESC NULLS LAST; paginated.
  </behavior>
  <action>
    Add `list_users_roster(...)` to `governance_service.py` per RESEARCH §Code Examples "Roster read": `auth.users u LEFT JOIN operator_users o ... LEFT JOIN (counts from documents) d ... LEFT JOIN (counts from threads) t ...`, selecting `last_sign_in_at`, `banned_until`, `(o.user_id IS NOT NULL) AS is_operator`, coalesced doc/chat counts, `ORDER BY u.last_sign_in_at DESC NULLS LAST LIMIT $1 OFFSET $2`. Confirm at read-time that `documents` and `threads` carry `user_id` (Assumption A1 — the admin.py email enrich already reads auth.users this way); if a table/column name differs, adjust the join and note it. Do NOT fabricate last-active — preserve NULL.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_148_roster.py::test_last_active_honesty -x` exits 0 (NULL last_sign_in_at preserved).
    - The roster SQL has `ORDER BY ... last_sign_in_at DESC NULLS LAST` and `LIMIT $1 OFFSET $2`.
    - `is_operator` derives from the operator_users join (not a JWT claim).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_148_roster.py::test_last_active_honesty -x</automated>
  </verify>
  <done>One-join roster query with honest last-active lands; roster honesty test GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Grant / revoke operator helpers (operator_service.py)</name>
  <files>backend/app/services/operator_service.py</files>
  <read_first>
    - backend/app/services/operator_service.py (seed_operators_from_env INSERT ~:161-165 for the idempotent grant shape; is_operator parameterized read ~:29-44; lazy get_pg_pool import ~:143)
    - .planning/phases/148-governance-audit-users-feature-visibility/148-RESEARCH.md §Code Examples "Grant / revoke operator" + Pitfall 7 (self-lockout)
    - backend/tests/test_148_operator_grant.py (target)
  </read_first>
  <behavior>
    - grant_operator(target_id, acting_operator_id): INSERT into operator_users (user_id, granted_by, note) VALUES ($1, $2, 'granted via roster') ON CONFLICT (user_id) DO UPDATE SET granted_by = EXCLUDED.granted_by — granted_by = the acting operator (D-01); idempotent.
    - revoke_operator(target_id, acting_operator_id): if target_id == acting_operator_id -> raise 409 "You cannot remove your own operator access." BEFORE any delete; else DELETE FROM operator_users WHERE user_id = $1. Past operator_audit_log rows are untouched (operator_user_id is a plain uuid, no FK — mig 095).
  </behavior>
  <action>
    Add `grant_operator` and `revoke_operator` to `operator_service.py`. grant copies the `seed_operators_from_env` INSERT (`:161-165`) but swaps `NULL, 'env-bootstrap'` for `$2 (acting operator), 'granted via roster'` and adds `ON CONFLICT (user_id) DO UPDATE SET granted_by = EXCLUDED.granted_by`. revoke checks `target_id == acting_operator_id` FIRST and raises HTTPException(409, "You cannot remove your own operator access.") before the DELETE (Pitfall 7 — the server is the real lockout wall, not the UI tooltip). Lazy-import `get_pg_pool` inside each function. Do NOT delete or rewrite operator_audit_log history on revoke.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_148_operator_grant.py -x` exits 0.
    - grant SQL populates `granted_by` = the acting operator and is idempotent (ON CONFLICT DO UPDATE).
    - revoke raises 409 on self-revoke BEFORE the DELETE (the self-check precedes any mutation in source order).
    - SCOPE (revision): `test_148_operator_grant.py` holds ONLY grant `granted_by` + self-revoke-409 (both service-level) — passes in wave 3. The self-disable-409 assertion lives in `test_148_disable.py`, owned by 148-06 (wave 4).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_148_operator_grant.py -x</automated>
  </verify>
  <done>Idempotent grant (granted_by populated) + lockout-proof revoke land; grant test GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| operator -> cross-user audit read | Service-role client, NO RLS backstop — every read must be explicitly scoped + paginated (SC#4). |
| operator filter input -> SQL | action_type / date / page_size / user_id must be bound params, never string-interpolated. |
| operator -> operator_users membership | Grant/revoke changes who holds the keys; self-revoke is a lockout vector. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-01 | Information Disclosure | query_platform_audit / CSV | mitigate | Parameterized + explicitly user-scoped-or-all + always paginated (page_size<=100); CSV COUNT-first + refuse-if-over-50000 — no unbounded dump (Pitfall 5). |
| T-148-03 | Tampering | filter args -> SQL | mitigate | asyncpg `$N` binds; column names code-constant; action_type as a text[] ANY param. |
| T-148-04 | Denial of Service | revoke_operator | mitigate | Self-revoke refused server-side (409) before the DELETE (Pitfall 7). |
| T-148-SC | Tampering | pip installs | accept | Zero new packages (stdlib csv/io + asyncpg already present). |
</threat_model>

<verification>
`cd backend && venv/Scripts/python -m pytest tests/test_148_platform_audit_filters.py tests/test_148_platform_audit_scope.py tests/test_148_csv_export.py tests/test_148_roster.py tests/test_148_operator_grant.py -x` GREEN. 146/147 regression suite stays green; Phase-148 Wave-0 stubs not yet owned by this wave remain expected-RED (owned downstream: `test_148_disable.py`, `test_148_enable.py`, `test_148_view_platform_recorded.py` → 148-06).
</verification>

<success_criteria>
Parameterized/scoped/paginated audit query + capped CSV, one-join honest roster, and lockout-proof grant/revoke exist; the five service-layer tests are GREEN.
</success_criteria>

<output>
Create `.planning/phases/148-governance-audit-users-feature-visibility/148-04-SUMMARY.md` when done.
</output>
