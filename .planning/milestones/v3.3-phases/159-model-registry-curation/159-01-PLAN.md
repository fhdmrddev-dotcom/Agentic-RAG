---
phase: 159-model-registry-curation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/model_discovery_service.py
  - scripts/curate_models.py
autonomous: true
requirements: [MODEL-03]

must_haves:
  truths:
    - "D-159-01: the non-chat-utility exclude tokens live in ONE shared importable constant (UTILITY_MODEL_EXCLUDE) — curate_models.py and the discovery service can't drift"
    - "D-159-01: each discovered 'new' model carries a display-only `utility` flag; the confirmable diff (changed/vanished/capabilities) is byte-identical to before"
    - "SC#3: the filter is display metadata only — it never enables, deletes, or mutates the diff the operator confirms (149 'Discovery proposes, humans confirm' red line holds)"
    - "chatgpt-4o-latest survives the filter (utility=false); embeddings/audio/image/moderation/transcribe are flagged utility=true"
  artifacts:
    - path: "backend/app/services/model_discovery_service.py"
      provides: "UTILITY_MODEL_EXCLUDE regex + is_utility_model() + the display-only `utility` tag on _build_new_entry output"
      contains: "UTILITY_MODEL_EXCLUDE"
    - path: "scripts/curate_models.py"
      provides: "_MISSING_EXCLUDE re-pointed to import the shared non-chat-utility core (chat-legacy tokens stay local)"
    - path: "backend/tests/test_159_utility_filter.py"
      provides: "classification assertions for the tuned token set + the display-only tag"
  key_links:
    - from: "scripts/curate_models.py"
      to: "backend/app/services/model_discovery_service.py"
      via: "import UTILITY_MODEL_EXCLUDE"
      pattern: "from app.services.model_discovery_service import"
    - from: "model_discovery_service._build_new_entry"
      to: "is_utility_model"
      via: "display-only utility tag"
      pattern: "utility.*is_utility_model"
---

<objective>
Lift the proven utility-model exclude regex out of `scripts/curate_models.py` into ONE
shared importable constant in the live `model_discovery_service`, tuned for the
chat-filter purpose, and tag each discovered "new" model with a display-only `utility`
flag (D-159-01). This is the single biggest de-risker of the phase: the curation logic
is already proven — only the shared home, the tuned token set, and the display tag are new.

Purpose: give the discovery panel (Plan 06) a per-model `utility` signal to hide by
default, WITHOUT ever mutating the confirmable diff (149 red line / SC#3).
Output: `UTILITY_MODEL_EXCLUDE` + `is_utility_model()` in the service, the `utility` tag on
new entries, and `curate_models.py` DRY'd to the same source.

Security invariants (no server write surface added — pure display/read tag):
- The `utility` flag is display metadata only; `compute_diff` / accept / enable / delete paths
  are untouched. A utility-flagged model is still fully confirmable if the operator opts in.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/159-model-registry-curation/159-CONTEXT.md
@.planning/phases/159-model-registry-curation/159-PATTERNS.md
@backend/app/services/model_discovery_service.py
@scripts/curate_models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared UTILITY_MODEL_EXCLUDE constant + is_utility_model() + display-only `utility` tag</name>
  <files>backend/app/services/model_discovery_service.py, backend/tests/test_159_utility_filter.py</files>
  <read_first>
    - backend/app/services/model_discovery_service.py — the file being modified. Read L1-38 (module docstring + imports; note there is no `import re` yet), L80-84 (`UNKNOWN`, `_CAP_FIELDS`), L396-412 (`_build_new_entry` — where each new model becomes a dict), L415-499 (`compute_diff` — must stay value-identical for changed/vanished).
    - scripts/curate_models.py L104-111 — the `_MISSING_EXCLUDE` regex to lift (the source of the token set).
    - .planning/phases/159-model-registry-curation/159-PATTERNS.md §1 (the tuning nuance + the recommended tag-not-drop approach) and §2 (the DRY-home recommendation).
    - .planning/phases/159-model-registry-curation/159-CONTEXT.md D-159-01 (the chat-legacy tuning nuance — keep the true non-chat utility excludes; DROP chatgpt|instruct|codex|davinci|babbage for the CHAT filter because chatgpt-4o-latest is a valid chat model).
    - backend/tests/test_149_discovery.py — the existing discovery unit-test style/fixtures to mirror for the new test file.
  </read_first>
  <action>
    Add `import re` if absent. Define a module-level constant
    `UTILITY_MODEL_EXCLUDE = re.compile(r"(embed|whisper|tts|audio|realtime|image|dall-e|moderation|transcribe|rerank|search-preview|computer-use)", re.IGNORECASE)`
    — the true non-chat-utility core. DROP the chat-legacy tokens (`chatgpt|instruct|codex|davinci|babbage`)
    from THIS constant: they wrongly hide valid chat models (e.g. `chatgpt-4o-latest`). Add a
    helper `def is_utility_model(model_id: str) -> bool: return bool(UTILITY_MODEL_EXCLUDE.search(model_id or ""))`.
    Anchor the tokens with bounded classes (they already are — plain alternation, linear scan;
    keep it ReDoS-safe). In `_build_new_entry`, add ONE display-only key to the returned dict:
    `"utility": is_utility_model(model_id)` alongside the existing `enabled`/`capabilities` keys.
    Do NOT gate anything on it — `compute_diff`, the `changed`/`vanished` builders, and the
    capability-fill asymmetry stay byte-identical (the tag is display metadata, D-159-04 "pure
    display/curation concern"). If provider API keys + network are available at execute time, run
    one live discovery pull and confirm the tuned token set behaves; otherwise the deterministic
    id set in the acceptance criteria is the contract.
    Create `backend/tests/test_159_utility_filter.py` asserting the classification set below and
    that a `_build_new_entry(...)` dict contains a boolean `utility` key.
  </action>
  <acceptance_criteria>
    - `is_utility_model` returns True for: `text-embedding-3-large`, `whisper-1`, `dall-e-3`, `tts-1`, `omni-moderation-latest`, `gpt-4o-transcribe`.
    - `is_utility_model` returns False for: `chatgpt-4o-latest`, `gpt-5.4`, `claude-opus-4-8`, `kimi-k3`, `gemini-3.1-pro-preview`, `deepseek-v4`.
    - `_build_new_entry("openai", "gpt-x", {})` returns a dict whose keys include `"utility"` (bool) plus the pre-existing `provider`/`model_id`/`enabled`/`capabilities`.
    - `compute_diff` output for `changed` and `vanished` groups is structurally identical to before (no `utility` key leaked into those; only `new` entries carry it).
    - `UTILITY_MODEL_EXCLUDE` is defined exactly once in the repo (`grep -rn "UTILITY_MODEL_EXCLUDE = re.compile" backend scripts` → 1 line).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/test_159_utility_filter.py -x</automated>
  </verify>
  <done>Shared constant + helper defined in the service; discovery `new` entries carry a display-only `utility` flag; the tuned token set classifies the acceptance ids correctly; the confirmable diff is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: DRY curate_models.py to the shared non-chat-utility core</name>
  <files>scripts/curate_models.py</files>
  <read_first>
    - scripts/curate_models.py — the file being modified. Read L104-111 (`_MISSING_EXCLUDE`), L88-102 (`_FAMILY_INCLUDE`, unchanged), L120-129 (the `_requests()` local-import discipline that keeps `--help` fast), and the two exclude call sites (`_MISSING_EXCLUDE.search(mid)` — search the file to confirm current line numbers ~390 and ~417).
    - backend/app/services/model_discovery_service.py — the new `UTILITY_MODEL_EXCLUDE` (Task 1) this file will import.
    - .planning/phases/159-model-registry-curation/159-PATTERNS.md §3 (the compose-locally guidance: DRY the shared half, keep curate's chat-legacy tokens local).
  </read_first>
  <action>
    Re-point curate so the non-chat-utility tokens come from the shared source and cannot drift.
    Keep a curate-LOCAL `_CHAT_LEGACY_EXCLUDE = re.compile(r"(codex|chatgpt|instruct|davinci|babbage)", re.IGNORECASE)`
    (these legacy families ARE correctly excluded from CURATE_MISSING registry-gap flagging — that
    purpose differs from the chat filter). Replace the two `_MISSING_EXCLUDE.search(mid)` call sites so
    a model is excluded from MISSING when `UTILITY_MODEL_EXCLUDE.search(mid) or _CHAT_LEGACY_EXCLUDE.search(mid)`
    is truthy. Import `UTILITY_MODEL_EXCLUDE` from `app.services.model_discovery_service`. To preserve
    `--help` fastness (the service imports `app.config.settings` at module load), do the shared import
    LOCALLY inside the function(s) that reference the excludes — matching curate's existing local-import
    pattern — rather than at module top. Remove the old `_MISSING_EXCLUDE` literal (or keep only the
    local chat-legacy half). The non-chat-utility token set now has exactly one source of truth.
  </action>
  <acceptance_criteria>
    - `scripts/curate_models.py` no longer defines the non-chat-utility literal (`grep -n "embed|whisper|tts" scripts/curate_models.py` → 0 lines matching a compiled literal; the tokens arrive via the shared import).
    - curate's CURATE_MISSING behavior is preserved: an `embed`/`whisper` id is still excluded (via the shared core) AND a `chatgpt`/`instruct` id is still excluded (via the local chat-legacy set).
    - `backend/venv/Scripts/python.exe scripts/curate_models.py --help` exits 0 without requiring provider keys or network.
    - `grep -n "from app.services.model_discovery_service import" scripts/curate_models.py` → shows the shared import.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && backend/venv/Scripts/python.exe scripts/curate_models.py --help && grep -c "from app.services.model_discovery_service import" scripts/curate_models.py</automated>
  </verify>
  <done>curate imports the shared constant; the non-chat-utility token set has a single source; curate's MISSING flagging is preserved; `--help` stays fast.</done>
</task>

</tasks>

<verification>
- `cd backend && venv/Scripts/python.exe -m pytest tests/test_159_utility_filter.py tests/test_149_discovery.py -x` — new classification tests pass AND the shipped discovery tests do not regress.
- `backend/venv/Scripts/python.exe scripts/curate_models.py --help` exits 0.
- `grep -rn "UTILITY_MODEL_EXCLUDE = re.compile" backend scripts` shows exactly one definition.
</verification>

<success_criteria>
- The utility-exclude token set lives in one shared constant; curate and the service can't drift (D-159-01).
- Discovery `new` entries carry a display-only `utility` flag; the confirmable diff is byte-identical (SC#3).
- `chatgpt-4o-latest` survives; embeddings/audio/image/moderation/transcribe are flagged utility.
</success_criteria>

<output>
Create `.planning/phases/159-model-registry-curation/159-01-SUMMARY.md` when done.
</output>
