---
phase: 159-model-registry-curation
plan: 04
type: execute
wave: 2
depends_on: [159-01, 159-02, 159-03]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/lib/model-defaults.ts
autonomous: true
requirements: [MODEL-03]

must_haves:
  truths:
    - "D-159-02: the client exposes addModelById(body) → POST /admin/models with an AddModelBody type; ApiError carries the server 409/422 detail so the form can show the plain-language refusal"
    - "D-159-03: a per-provider-family default table (familyDefaults) mirrors the shipped substring→family idiom + model-info.ts values; it returns REVIEWED pre-fills (null where no sensible default), never authoritative values"
    - "D-159-04: FullAppSettings carries model_discovery_filter_enabled and FlagKey includes it; DiscoveredNewModel carries the display-only utility flag"
    - "SC#3: the client adds NO authority — the server 404-gates; enabled is never client-sent"
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "addModelById + AddModelBody + DiscoveredNewModel.utility + FullAppSettings.model_discovery_filter_enabled + FlagKey extension"
      contains: "addModelById"
    - path: "frontend/src/lib/model-defaults.ts"
      provides: "familyDefaults(idOrProvider) → {context, maxOutput, tools} substring→family table"
      contains: "export function familyDefaults"
    - path: "frontend/src/lib/model-defaults.test.ts"
      provides: "family-default resolution + null-for-unknown assertions"
  key_links:
    - from: "frontend/src/lib/api.ts:addModelById"
      to: "POST /admin/models"
      via: "fetch + getAuthHeaders + ApiError(errorDetail)"
      pattern: "admin/models"
    - from: "frontend/src/lib/model-defaults.ts:familyDefaults"
      to: "providerLogo.tsx:MODEL_MARKS idiom"
      via: "first-match-wins substring→family"
      pattern: "familyDefaults"
---

<objective>
Define the frontend client contract the two UI plans consume (interface-first): the
`addModelById` write seam + `AddModelBody` type (D-159-02), the `utility` field on
`DiscoveredNewModel` (D-159-01 tag) and `model_discovery_filter_enabled` on `FullAppSettings`
+ `FlagKey` (D-159-04), and a new per-provider-family default table `familyDefaults` used by
both the add-by-ID form and the discovery hand-fill for source-labeled capability pre-fills
(D-159-03). No component logic here — just the shared seam so Plans 05/06 have real types to
call.

Purpose: prevent a cross-plan "scavenger hunt" — the components receive concrete contracts.
Output: extended `api.ts` + new `frontend/src/lib/model-defaults.ts`.

Security invariants (client-only; no server surface added):
- The client adds NO authority — `POST /admin/models` is 404-gated server-side (Plan 02).
- `AddModelBody` has NO `enabled` field — the server forces `enabled=false`.
- `familyDefaults` returns REVIEWED pre-fills (null where no sensible default) — never authoritative.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/159-model-registry-curation/159-CONTEXT.md
@.planning/phases/159-model-registry-curation/159-PATTERNS.md
@frontend/src/lib/api.ts
@frontend/src/lib/providerLogo.tsx
@frontend/src/lib/model-info.ts

<interfaces>
<!-- Backend contracts THIS seam must match (from Plans 01/02/03, verified). -->
POST /admin/models  (Plan 02) body:
  { model_id: string, provider: string, context_window_tokens?: number|null, max_output_tokens?: number|null,
    native_tools?: boolean|null, deprecated?: boolean, deprecated_reason?: string|null }   // NO enabled field
  → 200 { ok, model_id, provider, enabled:false } | 409 duplicate | 422 bad provider/type | 404 non-operator

Provider roster (8-cloud, native-7 + openrouter): openai, anthropic, google, deepseek, moonshot, zhipu, minimax, openrouter
Discovery new entry now carries a display-only `utility: boolean` (Plan 01).
GET /settings (Plan 03) now returns `model_discovery_filter_enabled: boolean` (default true).
Write path: setFlag("model_discovery_filter_enabled", value) rides PUT /admin/flags (Plan 02 key).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: api.ts client seam — addModelById + AddModelBody + utility + settings field + FlagKey</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts — the file being modified. Read L4136-4292 (`ModelRegistryRow`, `ModelCapabilityPatch`, `DiscoveredNewModel`, `getModelRegistry`, `setModelCapability`, `runModelDiscovery` — the CR-01 "unwrap envelopes in the client" note + the ApiError/errorDetail shape to mirror), L2164-2238 (`FullAppSettings` — where the flag booleans live), L4056-4099 (`FlagKey` union + `setFlag`).
    - .planning/phases/159-model-registry-curation/159-PATTERNS.md §12.
  </read_first>
  <action>
    (a) Add `export interface AddModelBody { model_id: string; provider: string; context_window_tokens?: number | null; max_output_tokens?: number | null; native_tools?: boolean | null; deprecated?: boolean; deprecated_reason?: string | null }` (NO `enabled` — server forces false).
    (b) Add `export async function addModelById(body: AddModelBody): Promise<void>` that POSTs to
    `${API_BASE}/admin/models` with `await getAuthHeaders()` + `JSON.stringify(body)` and, on `!res.ok`,
    throws `new ApiError(await errorDetail(res, "Failed to add the model."), res.status)` (mirrors
    setModelCapability — surfaces the 409 "already in the registry" detail).
    (c) Add `utility?: boolean` to the `DiscoveredNewModel` interface (the Plan-01 display tag; optional for backward-compat).
    (d) Add `model_discovery_filter_enabled: boolean` to `FullAppSettings` (beside the FLAG-01 booleans).
    (e) Add `"model_discovery_filter_enabled"` to the `FlagKey` union so `setFlag` types the write.
    Do not touch any component. Keep the CR-01 discipline (envelope-unwrap stays in the client, never a component).
  </action>
  <acceptance_criteria>
    - `AddModelBody` + `addModelById` are exported; `addModelById` POSTs to `/admin/models` and throws `ApiError` carrying the server `detail` on `!ok`.
    - `AddModelBody` has NO `enabled` field.
    - `DiscoveredNewModel` has an optional `utility?: boolean`.
    - `FullAppSettings` has `model_discovery_filter_enabled: boolean`.
    - the `FlagKey` union includes `"model_discovery_filter_enabled"`.
    - `npm run build` (tsc) reports 0 net-new type errors.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc -b 2>&1 | tail -5 && grep -c "export async function addModelById" src/lib/api.ts</automated>
  </verify>
  <done>The client seam exposes addModelById + AddModelBody + the utility field + the settings field + the FlagKey extension — the contract Plans 05/06 call.</done>
</task>

<task type="auto">
  <name>Task 2: model-defaults.ts — per-provider-family capability default table</name>
  <files>frontend/src/lib/model-defaults.ts, frontend/src/lib/model-defaults.test.ts</files>
  <read_first>
    - frontend/src/lib/providerLogo.tsx L120-162 — `MODEL_MARKS` (the ordered substring→family array) + `modelLogo` (first-match-wins lookup) — the idiom to mirror.
    - frontend/src/lib/model-info.ts L1-55 — `MODEL_INFO` per-model `{contextWindow, maxOutputTokens, costTier}` — the VALUE source for family defaults (and its "keys mirror config.py" contract).
    - backend/app/config.py L52-62 (`PROVIDER_CONTEXT_DEFAULTS`), L443-456 (`_NATIVE_TOOL_PROVIDERS` / `_INFERRED_DEFAULT_MAX_TOKENS`) — the backend constants to keep the frontend mirror honest.
    - .planning/phases/159-model-registry-curation/159-PATTERNS.md §13.
  </read_first>
  <action>
    Create `frontend/src/lib/model-defaults.ts` exporting
    `export function familyDefaults(idOrProvider: string): { context: number | null; maxOutput: number | null; tools: boolean | null }`.
    Build an ordered substring→family array mirroring `MODEL_MARKS` (first-match-wins, most-specific-first)
    plus a bare-provider fallback, mapping each family to sensible-modern defaults seeded from
    `MODEL_INFO`'s flagship values + the backend constants, e.g.:
      claude → {200000, 32768, true}; gemini/gemma/`google` → {600000, 32768, true};
      gpt/`openai` → {400000, 32768, true}; kimi/moonshot → {256000, 65536, true};
      glm/chatglm/`zhipu` → {128000, 32768, true}; minimax/abab → {200000, 32768, true};
      deepseek → {128000, 16384, true}; openrouter/unknown → {null, null, null} (blank — operator sets).
    `tools` defaults follow `_NATIVE_TOOL_PROVIDERS` (native-7 families → true; openrouter/unknown → null).
    Lowercase the input, match by substring first, else treat the input as a bare provider name, else return
    all-null. Return `null` for any capability with no sensible family default (the UI then shows a blank
    "operator sets it" input). Header comment: "REVIEWED pre-fills, never authoritative — mirrors config.py
    family defaults + model-info.ts; keep in sync when families are added." Add `model-defaults.test.ts`.
  </action>
  <acceptance_criteria>
    - `familyDefaults("kimi-k3")` → non-null `context` + `maxOutput` + `tools === true`.
    - `familyDefaults("claude-opus-4-8").tools === true` and `.context` is a positive number.
    - `familyDefaults("gpt-5.4").context` is a positive number.
    - `familyDefaults("google")` (bare provider) resolves the google family (non-null context).
    - `familyDefaults("somevendor/opaque-xyz")` → `{ context: null, maxOutput: null, tools: null }` (blank so the UI shows an empty input).
    - substring matching is first-match-wins mirroring the MODEL_MARKS order.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/model-defaults.test.ts</automated>
  </verify>
  <done>A substring→family default table exists mirroring the shipped idiom, returning reviewed pre-fills (null where none), consumed by both UI plans.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc -b` — 0 net-new type errors (the new exports type-check).
- `npx vitest run src/lib/model-defaults.test.ts` passes.
- `grep -c "model_discovery_filter_enabled" src/lib/api.ts` ≥ 2 (FullAppSettings + FlagKey).
</verification>

<success_criteria>
- The client contract for add-by-ID, the utility tag, the persisted filter setting, and the family-default table all exist and type-check (D-159-02/03/04 client half).
- `familyDefaults` returns reviewed pre-fills, never authoritative values (SC#3).
</success_criteria>

<output>
Create `.planning/phases/159-model-registry-curation/159-04-SUMMARY.md` when done.
</output>
