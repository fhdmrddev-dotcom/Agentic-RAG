---
phase: 261-an-expert-you-can-author
plan: 05
type: execute
wave: 4
depends_on: [261-01, 261-02, 261-03, 261-04]
files_modified:
  - backend/tests/unit/test_261_single_expert_authoring_gate.py
  - backend/tests/unit/test_261_expert_authoring_scenarios.py
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md
  - .planning/STATE.md
autonomous: true
requirements: [PACK-07, PACK-08, PACK-09, PACK-10]
mutates_local_db: false

must_haves:
  truths:
    - "AST fence test_261_single_expert_authoring_gate.py verifies that expert authoring endpoints gate strictly via role_permissions(role, 'experts:manage') with zero ad-hoc role branches (PACK-08)."
    - "AST fence is driven RED against a planted violation and restored md5-clean."
    - "Comprehensive scenario test test_261_expert_authoring_scenarios.py verifies S1 (Authoring CRUD), S2 (Permission denial for member), S4 (Union scope retrieval), S5 (Strict isolation retrieval), S6 (Deliverable tool execution), and S8 (Clone-on-customise for system templates)."
    - "Hot-file ledger in docs/HOT-FILE-LEDGER.md and CLAUDE.md reflect exact re-derived triples."
    - "All gates pass: backend unit baseline <= 71 failed, vitest count gate clean, tsc clean, size gate OK."
  artifacts:
    - path: "backend/tests/unit/test_261_single_expert_authoring_gate.py"
      provides: "AST fence verifying single-home data-driven authoring gate"
    - path: "backend/tests/unit/test_261_expert_authoring_scenarios.py"
      provides: "End-to-end scenario validation suite for SEED-303 and PACK requirements"
---

<objective>
Enforce the PACK-08 single-home data-driven authoring gate via AST fence driven RED against a planted check, verify end-to-end authoring and scoping scenarios, sync hot-file ledgers, and establish clean gate baselines.

Requirements: PACK-07, PACK-08, PACK-09, PACK-10, and SEED-303 scenarios.
Output: Passing AST fence and scenario test suite, synced governance ledgers, and verified gates.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<tasks>
<task id="261-05-01" name="Author AST Single-Home Fence in test_261_single_expert_authoring_gate.py">
1. Inspect AST of `backend/app/api/experts.py`:
   - Assert mutating endpoints (`POST`, `PATCH`, `DELETE`) require `experts:manage`.
   - Assert zero hardcoded `if role == 'org-admin'` checks exist across backend expert services.
2. Drive fence RED:
   - Plant a mock second role check in `backend/app/api/experts.py`.
   - Verify `pytest backend/tests/unit/test_261_single_expert_authoring_gate.py` fails.
   - Restore file and verify md5 matches.
</task>

<task id="261-05-02" name="Author Scenario Driver in test_261_expert_authoring_scenarios.py">
1. Scenario S1 (Authoring CRUD): Org-admin creates bundle, updates it, reads it, and deletes it.
2. Scenario S2 (Data-driven permission denial): Member without `experts:manage` receives structured HTTP 403.
3. Scenario S4 (Union scope): Chat with thread folder + expert folder retrieves documents across both folders.
4. Scenario S5 (Strict isolation): Chat with expert in `scope_mode='restricted'` searches only expert folder.
5. Scenario S6 (Additive tool floor): Deliverable tools (`execute_code`, `workspace_write`, `render_template`, `ask_user`) are retained and executable.
6. Scenario S8 (Clone-on-customise): System template can be cloned into an editable tenant bundle without altering the immutable system row.
</task>

<task id="261-05-03" name="Synchronize Hot-File Ledger & Run All Governance Gates">
1. Re-derive G-5 triples for modified files (`check-hot-file-ledger.cjs 261`).
2. Update `docs/HOT-FILE-LEDGER.md` and `CLAUDE.md`.
3. Run backend unit tests (`node scripts/check-backend-unit-baseline.cjs` <= 71 failed).
4. Run vitest count gate (`node scripts/vitest-count-gate.cjs`).
5. Run tsc check (`npx --prefix frontend tsc -p tsconfig.app.json --noEmit`).
6. Run seeds register gate (`node scripts/check-seeds-register.cjs`).
7. Run size gate (`node scripts/check-claude-md-size.cjs`).
8. Update `.planning/STATE.md`.
</task>
</tasks>
