---
phase: 262-an-expert-you-can-discover
plan: 04
type: tdd
wave: 4
depends_on: ["262-03"]
files_modified:
  - frontend/src/components/experts/catalog/ExpertDetailModal.tsx
  - frontend/src/components/experts/catalog/startScopedChat.ts
  - frontend/src/components/experts/catalog/ExpertCatalogPage.tsx
  - frontend/src/components/experts/catalog/__tests__/ExpertDetailModal.test.tsx
  - frontend/src/components/experts/catalog/__tests__/startScopedChat.test.ts
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [PACK-12, PACK-13]

must_haves:
  truths:
    - "A card opens a detail view that renders what the Expert does, when to use it, the NAMED knowledge folders, the named connections and skills, its example prompts and its example output."
    - "A knowledge folder the caller cannot see is named as such — never blank, never silently dropped."
    - "Every one of those is proven by asserting the rendered CONTENT, never the presence of a block."
    - "One action from the detail view puts the caller in a chat thread already scoped to that Expert, through the seam Phase 260 shipped."
  artifacts:
    - path: "frontend/src/components/experts/catalog/ExpertDetailModal.tsx"
      provides: "PACK-12's only renderer of when_to_use, example_output and folder names"
      min_lines: 140
    - path: "frontend/src/components/experts/catalog/startScopedChat.ts"
      provides: "PACK-13's ordered handoff over the shipped thread.active_expert_id seam"
      exports: ["startScopedChat"]
  key_links:
    - from: "frontend/src/components/experts/catalog/ExpertDetailModal.tsx"
      to: "frontend/src/components/experts/catalog/expertCatalog.ts"
      via: "resolveFolderNames"
      pattern: "resolveFolderNames"
    - from: "frontend/src/components/experts/catalog/startScopedChat.ts"
      to: "lib/api/threads.ts setThreadActiveExpert"
      via: "injected dependency — the same seam InviteExpertDialog's handler uses"
      pattern: "setExpert"
---

<objective>
Render the four columns that have never reached a normal user, and give the detail view the single
action that starts a scoped conversation.

Purpose: `262-RECORD.md` Finding 2 — `icon`, `category`, `when_to_use` and `example_output` are
*"authored by one role and rendered only to another"*, and RESEARCH R-3 sharpened it further:
**`example_output` renders in ZERO components today, admin included.** So PACK-12's hardest half has
no precedent anywhere to copy, and D-262-05 forbids the cheap proof: *"a `data-testid` presence
assertion does not satisfy this criterion"* — because here the content does not drift, **it was
never rendered at all.**

PACK-13 rides the seam that already ships. ⛔ It is NOT `MessageInput`'s local `handleSelectExpert`:
that function needs a `threadId` and lives inside a component `ChatLayout.launch.test.tsx:558-576`
structurally fences out of the non-chat branch, so a catalog cannot call it. ⭐ One register lower,
`ChatArea.tsx:234-252` hydrates the spotlight from `thread.active_expert_id` — so the same
mechanism, reached differently, is `create thread → setThreadActiveExpert → refresh → select →
navigate`.

Output: the detail modal, the ordered handoff, and both wired into the catalog page.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/262-an-expert-you-can-discover/262-RESEARCH.md
@.planning/phases/262-an-expert-you-can-discover/262-CONTEXT.md
@.planning/sketches/261-262-expert-authoring-and-catalog/README.md
@CLAUDE.md

<interfaces>
<!-- Extracted at the phase base + plan 03's declared contract. Build against these. -->

frontend/src/components/experts/catalog/expertCatalog.ts (plan 03):
  resolveFolderNames(ids: string[], folders: Folder[]): ResolvedFolder[]
  ResolvedFolder = { id: string; known: true; name: string } | { id: string; known: false }

frontend/src/lib/api/threads.ts:190-199 — the shipped seam, and it RETURNS the updated row:
  setThreadActiveExpert(threadId: string, expertId: string | null): Promise<Thread>
  (PATCH /threads/{id} with body { active_expert_id }, returns the patched Thread)

frontend/src/hooks/useThreads.ts:20-63 — what ChatLayout already holds:
  loadThreads(): Promise<void>            // refetches the list from the server
  selectThread(thread: Thread | null): void
  newThread(folderId?: string | null): Promise<Thread>   // ⚠ the returned Thread's active_expert_id is null
  ⛔ There is NO updater for `active_expert_id` on this hook, and this plan does not add one.

frontend/src/components/chat/ChatArea.tsx:234-252 — the consumer, unchanged by this phase:
  a useEffect keyed on [thread?.id, thread?.active_expert_id] that calls getExpert(expertId)
  and sets the spotlight, or clears it when the id is null.

THE MODAL CONTENTS, from `.planning/sketches/261-262-expert-authoring-and-catalog/index.html:1552-1640`
and README §2 "Pop-Up Detail Modal", in order:
  header — icon gem, name, scope-mode badge, a meta line of category + System Template/Org Custom
  "Knowledge Composition" — what the scope mode means for this Expert, in plain words
  "When To Summon This Expert" — `when_to_use`
  "Grounded Knowledge & Tool Envelope" — bound folders (NAMES), bound skills and connections (NAMES)
  "One-Click Action Prompts" — the `prompt_suggestions` tiles
  "Sample Deliverable Output" — `example_output`, collapsible
  footer — Close + `Start Scoped Chat with Expert`
⛔ `Clone & Customise (S8)` appears in that footer in the sketch and is OUT OF SCOPE.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The detail modal — six sections of content that has never been on screen</name>
  <files>frontend/src/components/experts/catalog/ExpertDetailModal.tsx</files>
  <read_first>
    - .planning/sketches/261-262-expert-authoring-and-catalog/index.html (lines 1552-1640 — the modal markup, verbatim)
    - frontend/src/components/chat/InviteExpertDialog.tsx (lines 75-95 — the shipped `Dialog`/`DialogContent`/`DialogHeader` usage and its class vocabulary)
    - frontend/src/components/org/OrgExpertsTab.tsx (lines 215-280 — the ONLY place `category` and `when_to_use` render today, and the counts-not-names section that must NOT be copied)
    - .planning/phases/262-an-expert-you-can-discover/262-RESEARCH.md §2 R-3 and §8 P-5, P-6
  </read_first>
  <behavior>
    - `when_to_use` renders verbatim; when the field is absent the section renders an honest line
      ("The author has not said when to use this Expert yet"), never an empty block and never
      `undefined`.
    - `example_output` renders verbatim behind a disclosure control; clicking the control reveals
      the exact authored string. When absent, the section is replaced by an honest line.
    - Each bound folder renders by NAME. A folder id that `resolveFolderNames` reports unknown
      renders the words "a knowledge folder you cannot see" — so the count still adds up and the
      caller is told why one has no name.
    - Each `member_skills` entry and each `required_connections` entry renders verbatim; these are
      already human strings and are not resolved against anything.
    - Each `prompt_suggestions` entry renders its `title` and its `prompt`.
    - `category` renders, or "Uncategorised" when absent; `is_system` renders as "System Template"
      and its absence as "Org Custom"; `scope_mode` renders as its shipped word.
    - The footer's primary control calls `onStartChat` exactly once per click and the modal closes.
  </behavior>
  <action>
Create `ExpertDetailModal.tsx` on the shipped shadcn `Dialog` primitives `InviteExpertDialog.tsx`
already uses, so the modal inherits the app's existing overlay, focus trap and escape handling
rather than inventing one. Props: `expert: ExpertBundle | null`, `folders: Folder[]`,
`open: boolean`, `onOpenChange(open: boolean)`, `onStartChat(expert: ExpertBundle)`.

Render the six sections in the sketch's order. Resolve folder names through plan 03's
`resolveFolderNames(expert.knowledge_folder_ids, folders)`. ⛔ Do NOT render counts here — counts are
the CARD's vocabulary and `OrgExpertsTab.tsx:274-276`'s `📁 N folders / ⚡ N skills / 🔌 N conns` is
the exact anti-pattern this requirement exists to replace. PACK-12's words are *"what knowledge it
reads, which connections it needs"*.

`example_output` is free text with no renderer precedent anywhere in the repository (R-3). Render it
as PRE-FORMATTED TEXT inside a bordered block behind a disclosure control — collapsed by default,
per the sketch's "collapsible preview". ⛔ Do NOT route it through a markdown renderer: it is
org-authored free text on a read surface, a markdown pass buys presentation and costs an injection
surface, and no other component in the repository treats this column at all. Implement the
disclosure with React state, ⛔ not a native `<details>` element — a closed `<details>` keeps its
content in the DOM, which would let a content assertion pass against text nobody can see.

⛔ EVERY OPTIONAL FIELD OWES AN HONEST LINE. All four presentation fields are optional on
`ExpertBundle` (`types/index.ts:17-35`). A blank section states a fact about the Expert that nobody
measured; name the absence instead.

⛔ AND THE FOOTER CARRIES TWO CONTROLS, NOT THREE. `Clone & Customise (S8)` is in the sketch and is
deferred by CONTEXT (`SEED-303 S8` — a write on a read surface). Do not render it disabled, greyed
or "coming soon"; a control that cannot act is the same dead affordance D-262-02 refuses one
requirement over.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc -p tsconfig.app.json --noEmit 2>&amp;1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ciE "clone|dangerouslySetInnerHTML|<details" frontend/src/components/experts/catalog/ExpertDetailModal.tsx` returns 0.
    - `grep -cE "\.length\} *(folder|skill|conn)" frontend/src/components/experts/catalog/ExpertDetailModal.tsx` returns 0 — the modal names, it does not count.
    - `grep -c "resolveFolderNames" frontend/src/components/experts/catalog/ExpertDetailModal.tsx` is at least 1.
    - `grep -c "example_output" frontend/src/components/experts/catalog/ExpertDetailModal.tsx` is at least 1 — this is the first component in the repository to contain it.
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` is at most 70.
  </acceptance_criteria>
  <done>Four columns that have only ever been written are now rendered to the person the ROADMAP's goal names.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The content fence — rendered text, never a testid; and the folder that legitimately has no name</name>
  <files>frontend/src/components/experts/catalog/__tests__/ExpertDetailModal.test.tsx, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - frontend/src/components/chat/__tests__/ExpertSpotlightCard.test.tsx (as rewritten by plan 02 — the mount + assert vocabulary)
    - .planning/phases/262-an-expert-you-can-discover/262-CONTEXT.md D-262-05
    - .planning/phases/262-an-expert-you-can-discover/262-RESEARCH.md §8 P-5 and P-6
  </read_first>
  <behavior>
    - Given a fixture whose `when_to_use` is a distinctive sentence, that exact sentence is in the
      rendered output.
    - Given a fixture whose `example_output` is a distinctive multi-line string, clicking the
      disclosure puts that exact string in the rendered output; before the click it is NOT there.
    - Given `knowledge_folder_ids: ["known-id", "invisible-id"]` and a `folders` prop containing only
      `known-id` named "SEC 10-K Filings 2025", the output contains "SEC 10-K Filings 2025" AND the
      words "a knowledge folder you cannot see", and the two entries are both present.
    - Given `member_skills: ["ratio_calculator"]` and `required_connections: ["Edgar MCP"]`, both
      strings are in the rendered output.
    - Given two `prompt_suggestions`, both titles and both prompts are in the rendered output.
    - Given a fixture with `when_to_use` and `example_output` both absent, neither section is blank:
      each renders its honest line, and the output contains no literal "undefined".
    - Clicking the primary footer control calls `onStartChat` once with the expert.
  </behavior>
  <action>
Create `ExpertDetailModal.test.tsx` driving all seven behaviours.

⛔ ASSERT THE RENDERED CONTENT. Every assertion is `getByText` / `queryByText` / a check over
`container.textContent` against the fixture's own distinctive strings. ⛔ No case may be satisfied by
a `data-testid` being present — D-262-05 is explicit, the ROADMAP criterion says *"the rendered
content is asserted, never the presence of a block"*, and CLAUDE.md's own recorded finding is that
*"presence assertions cannot see content drift"*. Here the situation is sharper still: the content
was never rendered at all, so a presence assertion would have passed over the defect for two phases.

Include a `not.toContain("undefined")` arm over the whole rendered text in the all-absent case — the
literal string reaching the DOM is the specific way an optional field fails in this codebase.

Drive the unknown-folder case FIRST against a modal stub that renders only the known names, and
quote the RED. It is the one assertion that distinguishes an honest modal from a convenient one.

Adopt the suite into `TARGETS` **and** `BASELINE` in the same commit, pin taken from the gate's own
printed `— N new` column.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/experts/catalog/__tests__/ExpertDetailModal.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/experts/catalog/__tests__/ExpertDetailModal.test.tsx` exits 0 with at least 7 cases.
    - `grep -c "getByTestId\|queryByTestId" frontend/src/components/experts/catalog/__tests__/ExpertDetailModal.test.tsx` returns 0 for every case that carries a PACK-12 content claim; if a testid is used anywhere it is only to scope a query, and the SUMMARY names where and why.
    - `grep -c "a knowledge folder you cannot see" frontend/src/components/experts/catalog/__tests__/ExpertDetailModal.test.tsx` is at least 1.
    - A case asserts `example_output`'s exact string is ABSENT before the disclosure click and PRESENT after it.
    - `grep -c "ExpertDetailModal.test" scripts/vitest-count-gate.cjs` is at least 2.
    - The SUMMARY quotes the RED from the known-names-only stub.
  </acceptance_criteria>
  <done>PACK-12's criterion is proven the way the ROADMAP demands it be proven, including the case where honesty costs something.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: One action — the ordered handoff onto the seam Phase 260 shipped</name>
  <files>frontend/src/components/experts/catalog/startScopedChat.ts, frontend/src/components/experts/catalog/__tests__/startScopedChat.test.ts, frontend/src/components/experts/catalog/ExpertCatalogPage.tsx</files>
  <read_first>
    - frontend/src/lib/api/threads.ts (lines 185-200 — setThreadActiveExpert and its return)
    - frontend/src/hooks/useThreads.ts (all 64 lines — what exists and what does not)
    - frontend/src/components/chat/ChatArea.tsx (lines 228-255 — the consuming effect and its dependency array)
    - frontend/src/components/chat/MessageInput.tsx (lines 155-175 — `handleSelectExpert`, the seam D-262-08 named and research refuted)
    - .planning/phases/262-an-expert-you-can-discover/262-RESEARCH.md §6 "PACK-13's seam is NOT the one D-262-08 names" and §8 P-8
  </read_first>
  <behavior>
    - `startScopedChat` calls its dependencies in exactly this order: `createThread`, then
      `setExpert(threadId, expertId)`, then `refreshThreads`, then `selectThread`, then `navigate`.
    - `selectThread` receives the Thread that `setExpert` RETURNED — the one carrying
      `active_expert_id` — never the one `createThread` returned, whose field is null.
    - `refreshThreads` runs BEFORE `selectThread`, so the thread list holds the patched row and
      re-selecting it later does not silently un-scope the conversation.
    - If `setExpert` rejects, `navigate` is NOT called and the rejection reaches the caller — an
      unscoped chat that looks scoped is the failure this ordering exists to prevent.
    - If `createThread` rejects, neither `setExpert` nor `navigate` is called.
  </behavior>
  <action>
Create `frontend/src/components/experts/catalog/startScopedChat.ts` exporting
`startScopedChat(deps, expert)` where `deps` is an object of injected functions —
`createThread(): Promise<Thread>`, `setExpert(threadId: string, expertId: string): Promise<Thread>`,
`refreshThreads(): Promise<void>`, `selectThread(t: Thread): void`, `navigate(): void`. No imports
from `@/lib/api`, no React. Injecting the seams is what makes the ORDER testable without mounting
`ChatLayout`, and the order is the whole requirement.

⭐ THIS IS THE SAME MECHANISM, NOT A SECOND ONE — which is exactly what PACK-13 requires. `ChatArea.tsx:234-252`
hydrates the spotlight from `thread.active_expert_id`, and `setThreadActiveExpert` is the same PATCH
`MessageInput.handleSelectExpert` issues. ⛔ D-262-08 named `onSelectExpert` and research measured
that it is unreachable from a fourth home: it is local to `MessageInput`, needs a `threadId`, and
`ChatLayout.launch.test.tsx:558-576` fences `<ChatArea` and `<WorkspacePanel` out of the non-chat
branch with a positive control proving the probe works.

⚠ `refreshThreads` BEFORE `selectThread` is load-bearing and is not tidiness. `useThreads.newThread`
pushes the pre-patch Thread into its own `threads` array and exposes no `active_expert_id` updater
(`useThreads.ts:38-43`). Without the refetch the list row keeps `active_expert_id: null`; the moment
the person clicks that thread in the history column, `ChatArea`'s effect sees null and clears the
spotlight while the server still holds the Expert. That is the surface disagreeing with the truth,
and it is cheap to prevent here and expensive to find later.

⚠ AND THE ALTERNATIVE WAS CONSIDERED AND REJECTED, ON THE RECORD. Research recommends mirroring
`prefillMessage` (`App.tsx:144/358-359` → `ChatLayout.tsx:96-97/817-818` → `ChatArea.tsx:573-586`)
as a one-shot handoff. This shape is preferred because it touches ZERO files in `App.tsx`'s prop
chain and therefore cannot acquire `libraryTabAfterNavigate`'s re-fire defect (`App.tsx:178-190`,
which cost Phase 235 a gap-closure round): there is no pending value to clear, because the state
lives in the thread row on the server. If a future need forces the one-shot shape, the clearing rule
is mandatory and its owner is the navigator.

Then wire the catalog page: hold the selected expert in state, open `ExpertDetailModal` from the
card's `Details` control, and route both the card's and the modal's `Start Chat` control into the
page's `onStartChat` prop — which plan 05 supplies from `ChatLayout` with its four shipped seams.
⛔ The page still fetches nothing new and still calls `listExperts()` exactly once.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/experts/catalog</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/experts/catalog/__tests__/startScopedChat.test.ts` exits 0 with at least 5 cases, including both rejection arms.
    - A case asserts the call ORDER explicitly (e.g. via a shared call-log array), not merely that each mock was called.
    - A case asserts `selectThread` received the object `setExpert` resolved, identity-compared.
    - `grep -cE "@/lib/api|from \"react\"" frontend/src/components/experts/catalog/startScopedChat.ts` returns 0.
    - `grep -c "listExperts(" frontend/src/components/experts/catalog/ExpertCatalogPage.tsx` still returns exactly 1.
    - `grep -c "startScopedChat.test" scripts/vitest-count-gate.cjs` is at least 2.
  </acceptance_criteria>
  <done>One click from the detail view produces a thread the server already knows is scoped, and the order that makes it true is fenced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| org author → viewer | `when_to_use` and `example_output` are the longest author-controlled strings this app renders to other members |
| client → `PATCH /threads/{id}` | the scoped-conversation write, issued on behalf of the caller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-262-13 | Tampering | `example_output` rendered into the modal | mitigate | Rendered as a TEXT child in a preformatted block. ⛔ No markdown pass and no `dangerouslySetInnerHTML` — grep-asserted 0. React escapes text children, so an author cannot inject markup through the longest field they control |
| T-262-14 | Information disclosure | folder-name resolution | mitigate | Names come ONLY from the caller's own `useFolders()` result. An id the caller cannot resolve renders the words "a knowledge folder you cannot see" — ⛔ the modal never fetches a folder by id, so it cannot name a folder the caller has no access to |
| T-262-15 | Spoofing | a chat that looks scoped but is not | mitigate | `startScopedChat` does not `navigate` when `setExpert` rejects, driven by a case; the spotlight is hydrated from the SERVER's `thread.active_expert_id`, never from client state |
| T-262-16 | Repudiation | the thread list holding a stale `active_expert_id` | mitigate | `refreshThreads` runs before `selectThread`, driven by an order assertion — the list and the server agree, so a later re-selection cannot silently un-scope |
| T-262-SC | Tampering | npm/pip/cargo installs | mitigate | ⛔ NONE. The modal is built on the shipped shadcn `Dialog`; no new dependency |
</threat_model>

<verification>
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/experts/catalog` — exits 0.
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the REPO ROOT — pinned-file count up by exactly 2; no per-file DECREASE; no failing filename beyond `sketchComposition.test.tsx` (RED AT BASE, inherited).
- `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` — at most 70.
- `node scripts/check-hot-file-ledger.cjs --files frontend/src/components/experts/catalog/ExpertDetailModal.tsx frontend/src/components/experts/catalog/startScopedChat.ts frontend/src/components/experts/catalog/ExpertCatalogPage.tsx` — exit 0.
- ⛔ Still not reachable. Plan 05 owns the union member, the branch and the entry actions.
</verification>

<success_criteria>
- `when_to_use`, `example_output`, every folder NAME, every skill, every connection and every prompt suggestion are asserted by their rendered text.
- An unresolvable folder id produces a named state, proven against a stub that dropped it.
- `startScopedChat` fires its five seams in a fenced order and refuses to navigate on a failed patch.
- The modal footer carries two controls; `Clone` appears nowhere.
</success_criteria>

<output>
Create `.planning/phases/262-an-expert-you-can-discover/262-04-SUMMARY.md` when done.
</output>
