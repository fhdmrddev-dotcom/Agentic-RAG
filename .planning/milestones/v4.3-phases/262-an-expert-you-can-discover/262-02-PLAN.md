---
phase: 262-an-expert-you-can-discover
plan: 02
type: tdd
wave: 2
depends_on: ["262-01"]
files_modified:
  - frontend/src/components/experts/expertIcon.tsx
  - frontend/src/components/experts/__tests__/expertIcon.test.tsx
  - frontend/src/components/chat/InviteExpertDialog.tsx
  - frontend/src/components/chat/ExpertSpotlightCard.tsx
  - frontend/src/components/chat/__tests__/ExpertSpotlightCard.test.tsx
  - frontend/src/components/org/OrgExpertsTab.tsx
  - backend/tests/unit/test_262_expert_list_grants_api.py
  - scripts/vitest-count-gate.cjs
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md
autonomous: true
requirements: [PACK-11, PACK-12]

must_haves:
  truths:
    - "An Expert's icon on every user-facing surface comes from the row's `icon` column, not from a slug string-match."
    - "`ExpertSpotlightCard` no longer invents Financial-Analyzer content for an Expert that did not author it."
    - "`GET /experts` for a normal caller hands the caller's identity and roles to the grant-aware list path — driven at the API layer, which `test_261_expert_grants_db.py` does not reach."
  artifacts:
    - path: "frontend/src/components/experts/expertIcon.tsx"
      provides: "The ONE home of expert-icon resolution; ICON_MAP + ExpertIcon"
      exports: ["EXPERT_ICON_MAP", "ExpertIcon"]
    - path: "backend/tests/unit/test_262_expert_list_grants_api.py"
      provides: "API-layer PACK-11 proof for the no-grant caller"
    - path: "frontend/src/components/chat/__tests__/ExpertSpotlightCard.test.tsx"
      provides: "The retirement rewritten with its original reason preserved; at least 5 cases"
      min_lines: 120
  key_links:
    - from: "frontend/src/components/chat/ExpertSpotlightCard.tsx"
      to: "frontend/src/components/experts/expertIcon.tsx"
      via: "import ExpertIcon"
      pattern: "from \"@/components/experts/expertIcon\""
    - from: "frontend/src/components/chat/InviteExpertDialog.tsx"
      to: "frontend/src/components/experts/expertIcon.tsx"
      via: "import ExpertIcon"
      pattern: "from \"@/components/experts/expertIcon\""
    - from: "backend/tests/unit/test_262_expert_list_grants_api.py"
      to: "backend/app/api/experts.py list_experts"
      via: "TestClient GET /experts with list_experts_service patched"
      pattern: "caller_user_id"
---

<objective>
Retire the five hardcoded `financial-analyzer` sites into one honest home that reads the row, and
drive PACK-11 at the API layer — the one layer research proved nothing reaches.

Purpose: `262-RECORD.md`'s finding is that `icon`, `category`, `when_to_use` and `example_output`
are *"authored by one role and rendered only to another."* Two of this phase's three requirements
are about a value reaching a person. The catalog (plans 03-04) is the big half; this plan closes the
half that is already on screen and lying — an Expert whose name contains "financial" gets a 📊 and
three SEC prompts it never authored, while its own `icon` column sits unread.

⚠ RESEARCH R-7 widened D-262-06 from two artefacts to **five sites in two files**, and R-4 narrowed
PACK-11's owed work: the DB predicate IS driven (`test_261_expert_grants_db.py:354-364`, live PG,
`pytest.skip`s without `:54322`); the API layer is NOT.

Output: one icon home, five retirements landed as rewrites, one API-layer test.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/262-an-expert-you-can-discover/262-RESEARCH.md
@.planning/phases/262-an-expert-you-can-discover/262-RECORD.md
@CLAUDE.md

<interfaces>
<!-- Extracted at the phase base. The executor needs no codebase exploration for these. -->

frontend/src/types/index.ts:17-35 — `ExpertBundle` carries, among others:
  id, name, slug, description, scope_mode: "restricted" | "biased",
  member_skills: string[], required_connections: string[], knowledge_folder_ids: string[],
  prompt_suggestions: Array<{ title: string; prompt: string }>,
  visibility: string, is_system: boolean, is_enabled: boolean,
  icon?: string, category?: string, when_to_use?: string, example_output?: string,
  tool_floor_enabled?: boolean
⚠ The four presentation fields are OPTIONAL — every renderer owes an honest empty state, never `undefined`.

frontend/src/components/org/OrgExpertsTab.tsx:30-46 — the ready-made replacement, verbatim:
  a module-level `ICON_MAP: Record<string, React.ComponentType<{ className?: string }>>` with eleven
  lucide glyphs keyed `chart · scale · shield · briefcase · truck · terminal · cpu · database ·
  book · file-text · sparkles`, and `renderExpertIcon(iconName?: string, className = "h-5 w-5")`
  falling back to `Sparkles`.

backend/app/api/experts.py:385-426 — `list_experts(request, include_system=True, enabled_only=True,
for_management=False, active_org=Depends(get_active_org_id), current_user=Depends(get_current_user),
pool=Depends(get_pg_pool)) -> list[dict]`. No `response_model`. The `for_management=False` arm calls
`list_experts_service(pool=…, caller_org_id=…, caller_user_id=…, caller_roles=[…], include_system=…,
enabled_only=…)`; the `for_management=True` arm first requires `experts:manage` and then calls
`list_experts_service` WITHOUT `caller_user_id`/`caller_roles`.

backend/tests/unit/test_259_expert_entitlement_gate.py — the mounting pattern to copy: a `FastAPI()`
with `experts_router` included, `dependency_overrides` for `get_current_user` / `get_active_org_id` /
`get_pg_pool`, and `patch("app.services.entitlement_service.check_entitlement", new_callable=AsyncMock)`
returning an `EntitlementResult`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One home for the expert icon, reading the column that already exists</name>
  <files>frontend/src/components/experts/expertIcon.tsx, frontend/src/components/experts/__tests__/expertIcon.test.tsx, frontend/src/components/org/OrgExpertsTab.tsx</files>
  <read_first>
    - frontend/src/components/org/OrgExpertsTab.tsx (lines 1-60 — the ICON_MAP and renderExpertIcon being extracted, and its imports)
    - frontend/src/components/chat/InviteExpertDialog.tsx (lines 27-38 — the slug-matching function being retired)
    - frontend/src/components/chat/ExpertSpotlightCard.tsx (lines 70-81 — the verbatim duplicate)
    - .planning/phases/262-an-expert-you-can-discover/262-RESEARCH.md §2 R-3 and R-7
  </read_first>
  <behavior>
    - `ExpertIcon` given `icon="scale"` renders the `Scale` lucide glyph.
    - `ExpertIcon` given `icon="chart"` renders `BarChart3`.
    - `ExpertIcon` given an icon key absent from the map renders the `Sparkles` fallback.
    - `ExpertIcon` given `icon` undefined renders the `Sparkles` fallback — NOT a slug- or
      name-derived guess. An Expert named "Financial Whatever" with no `icon` gets the neutral
      fallback, and a case asserts exactly that (it is the behaviour change, stated as a test).
    - `EXPERT_ICON_MAP` carries the same eleven keys `OrgExpertsTab.tsx:30-46` shipped — the move is
      a move, and a case pins the key set so a silent drop is visible.
  </behavior>
  <action>
Create `frontend/src/components/experts/expertIcon.tsx` as the ONE home of expert-icon resolution
(this project's "one home per concern" rule; the duplication R-7 measured is the defect). Export
`EXPERT_ICON_MAP` — the eleven-entry `Record<string, React.ComponentType<{ className?: string }>>`
moved verbatim out of `OrgExpertsTab.tsx:30-46` — and a component `ExpertIcon` taking
`{ icon?: string; className?: string }`, resolving through the map and falling back to `Sparkles`.
Default `className` to `"h-5 w-5"`, matching the shipped `renderExpertIcon`.

⛔ The resolution reads the `icon` FIELD and nothing else. It never inspects `slug`, `name`, or any
string content — that inspection is the artefact being retired, and re-implementing it one file over
would move the defect rather than end it.

Then make `OrgExpertsTab.tsx` import `ExpertIcon` and delete its local `ICON_MAP` and
`renderExpertIcon`, replacing the two call sites with the component. ⛔ This is a PURE MOVE: the
rendered output must be identical, and `OrgExpertsTab.test.tsx` (BASELINE pin 5) must stay green
without being edited. If it reds, the move was not pure — fix the move, never the pin.

Create `frontend/src/components/experts/__tests__/expertIcon.test.tsx` covering the five behaviours
above. Drive the undefined-icon case FIRST against a stub that still string-matches on the name, so
the SUMMARY quotes a real RED for the behaviour change rather than asserting it.

⭐ Add a ledger row AT CREATION for `frontend/src/components/experts/expertIcon.tsx` — a scan-list
row in CLAUDE.md plus its own section in `docs/HOT-FILE-LEDGER.md`, same commit, triple re-derived
(`0 / 0 / <wc -l>`). G-5 does not fire on it; the row goes in anyway, because an absent row is
invisible to G-5 at any count. Precedent: `frontend/src/lib/workspaceAllowedExt.ts`.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/experts/__tests__/expertIcon.test.tsx src/components/experts/__tests__/OrgExpertsTab.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/experts/__tests__/expertIcon.test.tsx` exits 0 with at least 5 cases.
    - `src/components/experts/__tests__/OrgExpertsTab.test.tsx` is byte-unchanged (`git diff --stat` shows it absent) and still reports 5 passing cases.
    - `grep -cE "slug *===|name\.toLowerCase\(\)" frontend/src/components/experts/expertIcon.tsx` returns 0 — the icon home cannot string-match.
    - `grep -cE "^\s*(const ICON_MAP|function renderExpertIcon)" frontend/src/components/org/OrgExpertsTab.tsx` returns 0.
    - `node scripts/check-hot-file-ledger.cjs --files frontend/src/components/experts/expertIcon.tsx` exits 0.
  </acceptance_criteria>
  <done>There is exactly one place in the repository that decides what glyph an Expert wears, and it reads the column the author filled in.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The four ExpertSpotlightCard hardcodes and the duplicated icon function, retired as rewrites</name>
  <files>frontend/src/components/chat/ExpertSpotlightCard.tsx, frontend/src/components/chat/InviteExpertDialog.tsx, frontend/src/components/chat/__tests__/ExpertSpotlightCard.test.tsx</files>
  <read_first>
    - frontend/src/components/chat/ExpertSpotlightCard.tsx (all 202 lines)
    - frontend/src/components/chat/__tests__/ExpertSpotlightCard.test.tsx (all 136 lines — the five cases being rewritten, and the fixture with `prompt_suggestions: []`)
    - frontend/src/components/chat/InviteExpertDialog.tsx (lines 20-45 and 115-180)
    - .planning/phases/262-an-expert-you-can-discover/262-RESEARCH.md §8 P-7 (the pinned-suite trap) and §2 R-7 (the five sites)
  </read_first>
  <behavior>
    - An Expert with `prompt_suggestions: []` renders NO action tiles carrying "Q3 Revenue Growth
      YoY" / "Gross Margin Comparison" / "Operating Cash Flow". It renders an honest state instead:
      the single "Explore Scope" tile derived from the Expert's own name, exactly as the non-financial
      branch already does.
    - An Expert with `prompt_suggestions` populated renders those, unchanged — the shipped good path.
    - An Expert whose `knowledge_folder_ids` has two entries renders a folder pill reading the COUNT
      ("2 Folders"), never the literal "SEC Filings & Reports". The spotlight card is a hero summary
      and stays a count here; the NAMES are PACK-12's job in the detail modal (plan 04).
    - An Expert with `member_skills: ["ratio_calculator"]` renders "ratio_calculator" because that is
      its OWN first skill; an Expert with `member_skills: []` renders "domain_tools", and neither
      value is reachable by matching the slug.
    - The identity gem comes from `ExpertIcon` with `expert.icon`; an Expert named "Financial
      Analyzer" with `icon: "scale"` shows the Scale glyph and NOT 📊.
  </behavior>
  <action>
Retire all four `ExpertSpotlightCard` sites plus the duplicated icon function, as REWRITES with the
original reason preserved — the `SEED-177` rule, precedent `D-206-07`. Each shipped for a reason at
Phase 260 (there was no `icon` column read path and one seeded Expert to demo), and the record
should say what changed, in a comment that keeps the original intent readable rather than deleting
it in silence.

In `ExpertSpotlightCard.tsx`: delete `DEFAULT_FINANCIAL_TILES` (`:26-42`) and its use at `:58-59`;
delete the local `getExpertIcon` (`:70-81`) and render the gem with `ExpertIcon icon={expert.icon}`;
rewrite `folderLabel` (`:93-96`) to the count-only branch it already has for non-financial Experts;
rewrite `skillLabel` (`:98-103`) to the `member_skills[0] ?? "domain_tools"` branch it already has.
⛔ Do not introduce a new fallback that depends on slug or name content anywhere.

In `InviteExpertDialog.tsx`: delete `getExpertIcon` (`:27-37`) and render the list gem with
`ExpertIcon icon={expert.icon}` in place of the emoji text node at `:134-136`. ⚠ `ComposerExpert.test.tsx`
(BASELINE pin 5) reaches this dialog through `expert-card-financial-analyzer` testids — that testid
is derived from `expert.slug` and is NOT touched by this change; confirm the suite is green rather
than assuming it.

⛔ P-7 IS THE READYMADE RED AND IT IS A FLOOR, NOT A CEILING. `ExpertSpotlightCard.test.tsx:47-69`
asserts all three fallback tiles verbatim against a fixture with `prompt_suggestions: []`, and
`:22-45` additionally asserts `"SEC Filings & Reports"` and `"ratio_calculator"`. Run the suite
BEFORE touching the component and capture the green; then make the component change and capture the
RED with its failing case names verbatim; then rewrite the file. The gate's contract is no per-file
DECREASE and the pin is **5**, so the rewritten file lands **at least 5** cases. Each rewritten case
carries, in its own body, why the assertion it replaces existed — the retirement is deliberate and
the reason travels with it.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/chat/__tests__/ExpertSpotlightCard.test.tsx src/components/chat/__tests__/ComposerExpert.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rcE "financial-analyzer|SEC Filings|ratio_calculator|Q3 Revenue Growth" frontend/src/components/chat/ExpertSpotlightCard.tsx frontend/src/components/chat/InviteExpertDialog.tsx` returns 0 for both files.
    - `grep -c "getExpertIcon" frontend/src/components/chat/ExpertSpotlightCard.tsx frontend/src/components/chat/InviteExpertDialog.tsx` returns 0 for both files.
    - `npx vitest run src/components/chat/__tests__/ExpertSpotlightCard.test.tsx` exits 0 reporting at least 5 passing cases — a DECREASE below 5 fails the count gate.
    - `npx vitest run src/components/chat/__tests__/ComposerExpert.test.tsx` exits 0 with 5 cases, and the file is byte-unchanged.
    - The SUMMARY quotes the pre-rewrite GREEN and the post-change RED of `ExpertSpotlightCard.test.tsx`, with the failing case names verbatim.
    - If the pin for `ExpertSpotlightCard.test.tsx` changes in `scripts/vitest-count-gate.cjs`, it is RAISED to the gate's own printed figure with a comment naming this plan — never lowered.
  </acceptance_criteria>
  <done>No user-facing Expert surface invents Financial-Analyzer content for an Expert that did not author it, and the deleted assertions' reasons are still readable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PACK-11 at the API layer — the one layer nothing reaches</name>
  <files>backend/tests/unit/test_262_expert_list_grants_api.py</files>
  <read_first>
    - backend/app/api/experts.py (lines 380-430 — `list_experts` and both arms)
    - backend/app/services/expert_service.py (lines 163-186 — `list_experts_service` and its `caller_user_id is not None` branch)
    - backend/tests/unit/test_259_expert_entitlement_gate.py (the whole file — the mounting/override/patch pattern to copy)
    - backend/tests/unit/test_261_expert_grants_db.py (lines 290-300 and 354-364 — the live-DB test this one must NOT duplicate, and its skip guard)
    - .planning/phases/262-an-expert-you-can-discover/262-RESEARCH.md §6 (the traced predicate and the "driven / not driven" table)
  </read_first>
  <behavior>
    - `GET /experts` with the default `for_management=False`, entitlement allowed, calls
      `list_experts_service` with `caller_user_id` equal to the authenticated caller's id and
      `caller_roles` equal to that caller's role list — i.e. it reaches the grant-aware path,
      never the unfiltered one.
    - The endpoint returns EXACTLY the rows that grant-aware path returned: patched to return two
      bundles, the response body is those two and nothing is re-added afterwards.
    - `GET /experts?for_management=true` from a caller WITHOUT `experts:manage` is refused 403, and
      `list_experts_service` is not called at all — the management arm cannot be used to see past
      one's own grants.
    - A caller whose `role` is absent yields `caller_roles == []`, never `[None]` — a null role must
      not widen a role-grant match.
  </behavior>
  <action>
Create `backend/tests/unit/test_262_expert_list_grants_api.py`.

⛔ SCOPE THE CLAIM HONESTLY. `test_261_expert_grants_db.py:354-364` already drives the SQL predicate
against real Postgres with a no-grant plain member; R-4 measured that the ROADMAP's *"no test drives
the no-grant user"* is FALSE at that layer. ⛔ Do NOT re-prove the DB predicate here, and do NOT
write a live-DB test — that file `pytest.skip`s without `:54322` and this one must run everywhere.
The gap this closes is narrower and stated in the module docstring: **the endpoint's own wiring** —
that `for_management=False` hands the caller's identity and roles to the grant-aware service, and
that nothing downstream of it re-widens the result.

Mount an app the way `test_259_expert_entitlement_gate.py` does: a bare `FastAPI()` with
`experts_router`, `dependency_overrides` for `get_current_user` / `get_active_org_id` /
`get_pg_pool`, and `patch("app.services.entitlement_service.check_entitlement", new_callable=AsyncMock)`
returning an allowed `EntitlementResult` so the tier gate is not the subject. Patch
`app.api.experts.list_experts_service` with an `AsyncMock` and assert on its call kwargs.

For the 403 case, override or patch the `experts:manage` permission check so it resolves false, and
assert both the status code and that the service mock was never awaited.

⚠ The backend unit baseline is at its ceiling with ZERO headroom (`71 failed, allowed 71`). This file
adds passing cases only. Verify with the harness, never with a hand-rolled pytest invocation:
`node scripts/check-backend-unit-baseline.cjs`. ⛔ `source venv/Scripts/activate` under Git Bash does
NOT activate this venv and reports a bogus `96 failed / 3 errors` — that figure must never be quoted.
Drive at least one case RED first (assert the WRONG kwarg name) and quote the failure in the SUMMARY.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python.exe -m pytest tests/unit/test_262_expert_list_grants_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/venv/Scripts/python.exe -m pytest tests/unit/test_262_expert_list_grants_api.py -q` exits 0 with at least 4 passing cases and 0 skips.
    - `node scripts/check-backend-unit-baseline.cjs` prints `[GATE PASSED]` with `Failed tests: 71` and `Errors: 0`.
    - `grep -c "54322\|pytest.skip" backend/tests/unit/test_262_expert_list_grants_api.py` returns 0 — this test never depends on a live database.
    - The module docstring names what it does NOT claim: it does not re-prove the SQL predicate, which `test_261_expert_grants_db.py:354-364` already drives.
    - The SUMMARY quotes the deliberate RED.
  </acceptance_criteria>
  <done>PACK-11's API layer is driven, and the claim written down is the one the test actually supports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| org author → every viewer of an Expert | `icon` is now author-controlled data that selects a rendered component |
| authenticated caller → `GET /experts` | the boundary where "which Experts may I see" is decided |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-262-05 | Elevation of privilege | `ExpertIcon` resolving an author-supplied string | mitigate | Resolution is a LOOKUP IN A CLOSED MAP of eleven lucide components with a `Sparkles` fallback. ⛔ Never `React.createElement(userString)`, never a dynamic import, never an `<img src>` — an unknown key falls back, it does not render author-controlled markup |
| T-262-06 | Information disclosure | `GET /experts?for_management=true` | mitigate | Task 3 drives the 403 arm and asserts `list_experts_service` is never awaited on refusal — the management arm must not be a way past one's own grants |
| T-262-07 | Spoofing | `caller_roles` built from a null role | mitigate | Task 3's fourth case pins `caller_roles == []` for an absent role, so a null cannot widen a `grantee_type = 'role'` match |
| T-262-08 | Repudiation | Deleting the three fallback-tile assertions | mitigate | The retirement is a REWRITE preserving the original reason in each replacing case body (SEED-177 / D-206-07); the pre-change GREEN and the post-change RED are both quoted in the SUMMARY |
| T-262-SC | Tampering | npm/pip/cargo installs | mitigate | ⛔ NONE. No new dependency in either language; `lucide-react` and `typescript` are already present. No package-legitimacy checkpoint is owed |
</threat_model>

<verification>
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/experts src/components/chat/__tests__/ExpertSpotlightCard.test.tsx src/components/chat/__tests__/ComposerExpert.test.tsx` — exits 0.
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the REPO ROOT — no per-file DECREASE; `ExpertSpotlightCard.test.tsx` at 5 or more; no failing filename beyond `sketchComposition.test.tsx` (RED AT BASE, inherited, `failed 2`).
- `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` — at most 70 (SET diff against the measured base; ⛔ "zero errors" is unreachable and bare `tsc --noEmit` checks ZERO files).
- `node scripts/check-backend-unit-baseline.cjs` — `[GATE PASSED]`, `Failed tests: 71`, `Errors: 0`.
- `node scripts/check-hot-file-ledger.cjs --files frontend/src/components/experts/expertIcon.tsx frontend/src/components/chat/ExpertSpotlightCard.tsx frontend/src/components/chat/InviteExpertDialog.tsx frontend/src/components/org/OrgExpertsTab.tsx` — exit 0.
- `node scripts/check-claude-md-size.cjs` — exit 0, under 120,000 chars.
</verification>

<success_criteria>
- All five sites R-7 named are gone from both files, provable by grep returning 0.
- `ExpertSpotlightCard.test.tsx` lands at 5 or more cases, each replacing case carrying the reason its predecessor existed.
- `OrgExpertsTab.test.tsx` and `ComposerExpert.test.tsx` are byte-unchanged and green.
- `test_262_expert_list_grants_api.py` drives 4 or more cases with no skips and the backend ceiling holds at 71.
</success_criteria>

<output>
Create `.planning/phases/262-an-expert-you-can-discover/262-02-SUMMARY.md` when done.
</output>
