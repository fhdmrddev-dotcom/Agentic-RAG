---
phase: 258-a-tier-becomes-enforceable
plan: 03
type: execute
wave: 3
depends_on: ['258-01', '258-02']
files_modified:
  - backend/app/api/workflows.py
  - backend/tests/unit/test_258_workflow_entitlement_gate.py
autonomous: true
requirements: [TIER-01, TIER-02, TIER-03, TIER-05]
mutates_local_db: false

must_haves:
  truths:
    - "The Workflows API (POST /workflows create_draft and publish_workflow) gates access using Depends(require_capability('workflows')) (D-258-09)."
    - "A Standard organization calling workflow creation endpoints receives an HTTP 403 Forbidden naming 'enterprise' tier and the upgrade hint (TIER-03, D-258-05)."
    - "An organization on Standard tier with add_ons: {'workflows': true} is admitted via the additive override rule (D-258-08)."
    - "An Enterprise organization executes workflow creation and publish actions without restriction (TIER-01)."
    - "Re-packaging 'workflows' to 'standard' via a tier_capabilities row change immediately allows Standard orgs without server restart or code edit (TIER-02, D-258-02)."
    - "An unreadable tier or DB blip triggers strict fail-closed refusal, and this failure path is driven and verified (TIER-05, D-258-06)."
    - "G-5 FIRES on backend/app/api/workflows.py (42/22/2255 L, extraction still OWED). Honoured by construction: exactly two Depends(require_capability('workflows')) additions to existing endpoint decorators; zero new branch, zero new handler, zero new state. The owed extraction is acknowledged and left unobstructed."
  artifacts:
    - path: "backend/app/api/workflows.py"
      provides: "require_capability('workflows') dependency wired on workflow authoring endpoints"
      contains: "require_capability(\"workflows\")"
    - path: "backend/tests/unit/test_258_workflow_entitlement_gate.py"
      provides: "End-to-end integration and scenario tests verifying tier enforcement on workflows"
  key_links:
    - from: "backend/app/api/workflows.py"
      to: "backend/app/services/entitlement_service.py"
      via: "imports require_capability"
      pattern: "from app\\.services\\.entitlement_service import require_capability"
---

<objective>
Wire the first live gated consumer slice (`require_capability('workflows')`) into the Workflows API (`backend/app/api/workflows.py`), and verify end-to-end entitlement enforcement, structured refusals, additive add-on overrides, dynamic row-based re-packaging, and fail-closed error handling.

Purpose: Prove TIER-01, TIER-02, TIER-03, and TIER-05 against real API route handlers.
Output: Wired endpoints in `backend/app/api/workflows.py` and integration tests in `backend/tests/unit/test_258_workflow_entitlement_gate.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

Base SHA to assert: `837a4cc03` on `develop`.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/258-a-tier-becomes-enforceable/258-CONTEXT.md
@.planning/phases/258-a-tier-becomes-enforceable/258-RESEARCH.md
@.planning/phases/258-a-tier-becomes-enforceable/258-PATTERNS.md
@backend/app/services/entitlement_service.py
@backend/app/api/workflows.py

<interfaces>
In backend/app/api/workflows.py:

```python
from app.services.entitlement_service import require_capability

# On create_draft (POST /workflows):
@router.post(
    "",
    response_model=DraftCreateResponse,
    status_code=status.HTTP_201_CREATED,
    dependencies=[
        Depends(require_visible("workflow_authoring")),
        Depends(require_capability("workflows")),
    ],
)
async def create_draft(...): ...

# On publish_workflow (POST /workflows/{definition_id}/publish):
@router.post(
    "/{definition_id}/publish",
    response_model=PublishVerdict,
    dependencies=[
        Depends(require_visible("workflow_authoring")),
        Depends(require_capability("workflows")),
    ],
)
async def publish_workflow(...): ...
```
</interfaces>

<guardrails>
### G-5 Guardrail Disposition on backend/app/api/workflows.py
- **Triple Measured**: `42 commits / 22 phases / 2255 lines` (quick task `260814` excluded: 21 phases).
- **G-5 FIRES** (threshold is 3 phases).
- **Ledger Status**: Prior verdict `⚠ extraction still OWED`.
- **Phase 258 Disposition**: **Honoured by construction**.
- **Execution Constraints**:
  - Add only `Depends(require_capability("workflows"))` to existing endpoint decorators (`create_draft` and `publish_workflow`).
  - Zero new endpoints or route handlers.
  - Zero new branches or conditional logic in `workflows.py`.
  - Zero new state or database queries in `workflows.py` (delegated to `entitlement_service`).
  - Total file delta is minimal (~4 lines: 1 import + 2 decorator dependency parameters).
  - Owed extraction is acknowledged and left unobstructed.
</guardrails>
</context>

<tasks>

<task type="auto" name="1. Wire require_capability('workflows') on Workflows API endpoints">
<action>
In `backend/app/api/workflows.py`:
1. Import `require_capability` from `app.services.entitlement_service`.
2. Add `Depends(require_capability("workflows"))` to `dependencies` list of:
   - `create_draft` (`@router.post("", ...)`)
   - `publish_workflow` (`@router.post("/{definition_id}/publish", ...)`)
   - `generate_workflow` (`@router.post("/generate", ...)`)
3. Enforce G-5: Do NOT add any branching logic, state, or new route handlers. Keep the edit purely compositional.
</action>
<verification>
Check syntax and import resolution: `python -m py_compile backend/app/api/workflows.py`.
Verify G-5 honoured by construction: confirm diff on `backend/app/api/workflows.py` contains only import and dependency parameters with zero branching.
</verification>
</task>

<task type="auto" name="2. Author integration & scenario tests in backend/tests/unit/test_258_workflow_entitlement_gate.py">
<action>
Create `backend/tests/unit/test_258_workflow_entitlement_gate.py`:
1. Test Standard org hitting `POST /workflows`:
   - Returns 403 Forbidden.
   - Response body JSON contains:
     `{"detail": "Capability 'workflows' requires 'enterprise' tier (current tier: 'standard')", "capability": "workflows", "required_tier": "enterprise", "current_tier": "standard", "upgrade_hint": "Upgrade to Enterprise to use workflows."}`.
2. Test Standard org with `add_ons: {"workflows": true}`:
   - Proceeds past entitlement gate (mock handler returns 201).
3. Test Enterprise org hitting `POST /workflows`:
   - Proceeds past entitlement gate (mock handler returns 201).
4. Test Dynamic Re-packaging (TIER-02):
   - Mock `tier_capabilities` returning `workflows` as enabled for `standard`.
   - Verify Standard org now succeeds without code edits or server redeployment.
5. Test Fail-Closed Semantics (TIER-05):
   - Mock database connection error / timeout during entitlement resolution.
   - Verify request is refused with 403/503 and never admitted.
</action>
<verification>
Run `pytest backend/tests/unit/test_258_workflow_entitlement_gate.py -v`.
All tests must pass.
</verification>
</task>

<task type="auto" name="3. Run Full Test Suite Baseline & Fence Verification">
<action>
1. Run backend unit baseline check:
   `node scripts/check-backend-unit-baseline.cjs`
   Verify failed test count <= 71 ceiling with 0 collection errors.
2. Run AST single-home fence:
   `pytest backend/tests/unit/test_258_single_entitlement_home.py -v`
3. Run hot file ledger check:
   `node scripts/check-hot-file-ledger.cjs 258`
4. Run seeds register sweep:
   `node scripts/check-seeds-register.cjs --phase 258`
</action>
<verification>
All automated gates must report OK.
</verification>
</task>

</tasks>
