---
phase: 256-every-token-is-counted-and-kept
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/182_workflow_runs_token_totals.sql
  - supabase/full-schema.sql
  - backend/app/services/circuit_breaker.py
  - backend/app/db/workflows.py
  - backend/app/services/harness_engine.py
  - backend/tests/unit/test_256_persist_run_usage.py
  - backend/tests/unit/test_256_finish_run_unchanged.py
  - backend/tests/unit/test_256_enforce_budget_persist.py
  - backend/tests/unit/test_256_producer_shell_site3.py
  - backend/tests/integration/test_256_migration_182.py
  - backend/tests/integration/test_256_persist_restart.py
autonomous: false
requirements: [METER-03, METER-04, METER-05]
mutates_local_db: true

must_haves:
  truths:
    - "A harness phase boundary persists this phase's token DELTA onto workflow_runs, and it does so on an INTERACTIVE (disarmed-breaker) run — not only on a scheduled one. This is D-256-04 corrected by RESEARCH.md C-1: the chosen absorb point is right, the chosen LINE (:1875) is below `if not breaker.armed: return` and unreachable for nearly every run."
    - "The per-phase write is a DELTA derived from CircuitBreaker's own watermark and is ADDED at the database, never SET (D-256-09). Calling _enforce_budget twice on an unchanged box writes nothing."
    - "workflow_runs.input_tokens / .output_tokens are NULLABLE. A run that never measured usage reads NULL, never 0 (D-256-06)."
    - "workflow_runs.token_coverage records which counting legs the total includes (D-256-07), written from ONE module-level constant, never inferred from created_at."
    - "finish_run is BYTE-UNCHANGED (D-256-05) and a fence proves it."
    - "The boot-sweep resume producer shell (harness_engine.py:3051) writes real segment totals instead of input_tokens=None (D-256-08 site 3), with the documented missing-usage warning."
    - "workflow_runs is AUTHORITATIVE at the WORKFLOW grain; the runs producer shell is the SEGMENT grain; the columns' own COMMENTs say never to sum across the two tables (D-256-03) and state D-256-01's `parent_run_id IS NULL` read rule."
  artifacts:
    - path: "supabase/migrations/182_workflow_runs_token_totals.sql"
      provides: "input_tokens integer NULL, output_tokens integer NULL, token_coverage text[] NULL, three COMMENT ON COLUMN blocks, one partial index"
      contains: "ADD COLUMN IF NOT EXISTS token_coverage text[]"
    - path: "backend/app/db/workflows.py"
      provides: "persist_run_usage() one-home writer + TOKEN_COVERAGE_LEGS constant"
      exports: ["persist_run_usage", "TOKEN_COVERAGE_LEGS"]
    - path: "backend/app/services/circuit_breaker.py"
      provides: "absorb_usage_box returns the clamped (d_in, d_out) delta it just recorded"
    - path: "backend/tests/unit/test_256_enforce_budget_persist.py"
      provides: "the DISARMED-breaker persist drive (SC#1 on an interactive run) + the armed-breaker trip is unchanged"
    - path: "backend/tests/unit/test_256_finish_run_unchanged.py"
      provides: "Fence 3 — finish_run source digest + call-site set"
  key_links:
    - from: "backend/app/services/harness_engine.py"
      to: "backend/app/db/workflows.py::persist_run_usage"
      via: "await persist_run_usage(...) inside _enforce_budget, ABOVE the `if not breaker.armed` guard"
      pattern: "persist_run_usage\\("
    - from: "backend/app/services/harness_engine.py"
      to: "backend/app/services/circuit_breaker.py::absorb_usage_box"
      via: "the returned (d_in, d_out) tuple becomes the persisted delta"
      pattern: "= breaker\\.absorb_usage_box\\("
---

<objective>
Make a harness run's token total DURABLE. Migration 182 gives `workflow_runs` three nullable
columns; a new one-home writer `persist_run_usage` ADDs a per-phase delta; `absorb_usage_box`
starts returning the delta it already computes; and `_enforce_budget` is reordered so the persist
happens ABOVE the `armed` early-return — without which METER-03 would persist nothing for any
interactive run.

Purpose: SC#1 — *"re-reading the run after the process restarts returns the same totals the
in-memory ceiling saw"*. Phase 257 turns this number into money; a number that is not persisted is
a number about nothing.
Output: migration 182 applied locally, `persist_run_usage`, the reorder, the coverage marker,
three unit fences, two integration round-trips, and METER-05 site 3 closed.

⛔ **THIS PLAN MUTATES THE LOCAL DATABASE.** Worktrees isolate files, not Postgres (CLAUDE.md
worktree rule 4). No other plan in wave 1 touches the DB; do not run a DB-mutating plan
concurrently with this one.

⛔ **G-8 / D-256-16 note on shape:** this plan carries FOUR tasks rather than the usual 2-3. That
is deliberate and it is the cheaper arithmetic: O-2 forbids splitting the `_enforce_budget` reorder
from the writer, and folding METER-05 site 3 in here (it lives in `harness_engine.py`, which this
plan already opens) avoids a THIRD sequential wave on that one file. Four plans total, two waves.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⛔ **FIRST ACTION IN A WORKTREE, before the HEAD assertion and before reading anything:**
`bash scripts/bootstrap-worktree.sh "$(pwd)"`. A worktree that skipped it reports green
typechecks and red tests for reasons that look like this plan's fault.

**Base SHA to assert: `edf2d1024` on `develop`.** ⚠ Agent worktrees start on the DEFAULT branch
(`master`, stuck at the v3.9 ship commit) — assert the SHA, do not assume it. ⚠ CONTEXT.md says
`772f53354` and RESEARCH.md says `e78cf5e63`; **both are stale** (RESEARCH.md C-8 predicted exactly
this). Every line number below was re-derived at `edf2d1024` by `256-PATTERNS.md`.

⛔ **BUS-264:** claude BUILDS, gemini REVIEWS. Read `.agent-bus/OPEN.md` before the first source
edit (AGENTS.md 6.1 baseline-before-build).
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/256-every-token-is-counted-and-kept/256-CONTEXT.md
@.planning/phases/256-every-token-is-counted-and-kept/256-RESEARCH.md
@.planning/phases/256-every-token-is-counted-and-kept/256-PATTERNS.md
@.planning/phases/256-every-token-is-counted-and-kept/256-BASELINE-backend-failing-set.txt
@CLAUDE.md
@docs/HOT-FILE-LEDGER.md
@supabase/SETUP.md

<interfaces>
<!-- The contracts this plan CREATES. Downstream plans 03 and 04 consume them; they do not need to
     explore for them. -->

NEW in backend/app/db/workflows.py (module level):

  TOKEN_COVERAGE_LEGS: tuple[str, ...] = ("agent", "single", "batch")
      # D-256-07. The counting legs the INSTRUMENTATION covers, not the legs a given run used.
      # ⛔ Plan 256-04 (METER-06) appends "emit" HERE, in the same commit as the drain arms (O-4).
      # ⛔ This is a DATA VALUE written into a column. It never resolves a writer, emitter,
      #    executor or validator — the Phase 255 Extension Contract is untouched.

  async def persist_run_usage(
      pool: asyncpg.Pool,
      run_id: UUID,
      *,
      input_delta: int | None,
      output_delta: int | None,
  ) -> None

CHANGED in backend/app/services/circuit_breaker.py:

  def absorb_usage_box(self, box: dict | None) -> tuple[int, int]
      # was -> None. Returns the CLAMPED (d_in, d_out) it just handed to record_tokens.
      # ⭐ MEASURED at edf2d1024: no test and no production caller reads this return
      #    (`grep -rn "absorb_usage_box" backend/tests` → 9 hits, every one a bare call
      #    statement). RESEARCH.md A3/U-4 is CONFIRMED — widening breaks nothing.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Author migration 182, have the operator apply it, regenerate full-schema</name>
  <files>supabase/migrations/182_workflow_runs_token_totals.sql, supabase/full-schema.sql, backend/tests/integration/test_256_migration_182.py</files>

  <read_first>
    - `.planning/phases/256-every-token-is-encoded-and-kept` does not exist — the phase dir is
      `.planning/phases/256-every-token-is-counted-and-kept`. Read
      `256-RESEARCH.md` §"Migration 182" (the exact DDL, lines 536-632) and §"The ACL / supplement
      / advisor tail".
    - `256-PATTERNS.md` §1 (`supabase/migrations/182_*.sql`, lines 36-102) and §11
      (`text[]` + partial index — is there a precedent?, lines 881-901).
    - `supabase/migrations/175_documents_thread_key.sql` — the ONLY style analog: a partial index
      (`:32-34`) and a COMMENT explaining a state (`:20-25`).
    - `supabase/migrations/129_connector_oauth_tokens.sql:54` — ⛔ the ANTI-analog. `scopes TEXT[]
      NOT NULL DEFAULT '{}'` is the two-state collapse D-256-07 must avoid.
    - `supabase/SETUP.md` — the application route.
    - `CLAUDE.md` §"Local dev infrastructure" and the migration rule.
  </read_first>

  <action>
Create `supabase/migrations/182_workflow_runs_token_totals.sql` adding THREE nullable columns to
`public.workflow_runs`: `input_tokens integer`, `output_tokens integer`, `token_coverage text[]`,
each via `ADD COLUMN IF NOT EXISTS`.

⛔ **NO `NOT NULL`, NO `DEFAULT 0`, NO `DEFAULT '{}'`** (D-256-06). `NULL` = never measured, `0` =
measured as zero. `integer` mirrors `public.runs` (`035_runs_table.sql:30-31`) exactly so one grain
reads the same in both tables.

⛔ **`token_coverage` is THREE-state and there is NO IN-REPO ANALOG for that** (`256-PATTERNS.md`
gap G-c): `NULL` = no instrumented leg ever reported usage for this run · `{}` = reported, covers
nothing · populated = the legs listed. Do NOT copy `129`'s `NOT NULL DEFAULT '{}'`.

Write three `COMMENT ON COLUMN` blocks, using RESEARCH.md §"Exact DDL" as the source text. Each
comment must carry, in its own words: the ADD-not-SET accumulation rule and its reason (the box
resets per SEGMENT at `harness_engine.py:1844`) per D-256-09; the `NULL` ≠ `0` rule per D-256-06;
D-256-03's *never sum across `runs` and `workflow_runs`*; and D-256-01's read rule *any org- or
thread-level total over `runs` sums only rows `WHERE parent_run_id IS NULL`*. The `token_coverage`
comment additionally states D-256-07: coverage is recorded, never inferred from `created_at` vs a
ship date, and Phase 257's METER-07 reads this column rather than prose.

Then the partial index `idx_workflow_runs_org_coverage_incomplete` on `(org_id, created_at DESC)`
with predicate `WHERE token_coverage IS NULL OR NOT (token_coverage @> ARRAY['agent','single','batch','emit']::text[])`.
⚠ `workflow_runs` has only two indexes today (`thread_id`, `user_id`) and **no `org_id` index at
all**, so this is also Phase 257's only per-org access path.

⛔ **`@>` IN AN INDEX PREDICATE HAS NO IN-REPO PRECEDENT** (gap G-b; RESEARCH.md A1/U-2 flags it
`[ASSUMED]`). It is PASTE-AND-VERIFY, not assumed: if the SQL editor refuses the predicate, fall
back — in the same migration file, before it is committed — to
`WHERE token_coverage IS NULL OR array_length(token_coverage, 1) < 4`, and write a comment saying
the fallback hard-codes ARITY instead of MEMBERS and is therefore weaker.

**CHECKPOINT — the operator applies it, not you.** ⛔ `supabase db push` and `supabase db reset`
are FORBIDDEN by CLAUDE.md: they wipe dev data. Present the file contents and ask the operator to
paste it into the local Supabase SQL editor.

After the operator confirms it applied, run `bash scripts/regenerate-full-schema.sh` **WITHOUT
`--reset`** to rebuild `supabase/full-schema.sql`. ⛔ Never hand-edit `full-schema.sql`.

Then write `backend/tests/integration/test_256_migration_182.py` in the shape of
`backend/tests/integration/test_120_migration.py` (real asyncpg on `localhost:54322`,
`information_schema.columns` reads, writes only inside a rolled-back transaction, clean skip when
the DB is unreachable). It asserts: the three columns exist; `is_nullable = 'YES'` on all three;
`data_type` is `integer`/`integer`/`ARRAY`; the index exists in `pg_indexes`; and — the three-state
pin that has no house pattern to inherit — that a row can hold `token_coverage IS NULL`, `= '{}'`,
and a populated array, and that all three are distinguishable by query.

⚠ `tests/integration/` is OUTSIDE `pytest tests/unit`, so this file is invisible to the 71-name
baseline and can never be this phase's only proof. Say so in the test's module docstring.

Record, for the SUMMARY: that `node scripts/check-schema-acl-parity.cjs` and
`backend/venv/Scripts/python scripts/check-greenfield-privileges.py` were RUN and are unchanged
("unchanged" is a measurement, not an assumption); that 182 owes `scripts/full-schema-supplement.sql`
NOTHING (`workflow_runs` has zero explicit GRANT/REVOKE anywhere — RESEARCH.md proved this four
ways); and that 182 adds no env var and no seed row, so nothing in `deploy/onebox.env.example` or
`docs/OPERATOR.md` moves. ⛔ **Cloud parity is OWED at the next operator-gated production push,
with `get_advisors(security)` in the checklist** — note it so the deploy-parity list inherits it;
it is out of scope here.
  </action>

  <acceptance_criteria>
    - `ls supabase/migrations/182_workflow_runs_token_totals.sql` exists and the filename matches
      `^[0-9]+_.*\.sql$`. ⛔ NO letter suffix — `182b` is silently skipped by the Supabase CLI.
    - `grep -c "NOT NULL" supabase/migrations/182_workflow_runs_token_totals.sql` → `0`.
    - `grep -c "DEFAULT" supabase/migrations/182_workflow_runs_token_totals.sql` → `0`.
    - `grep -c "COMMENT ON COLUMN" supabase/migrations/182_workflow_runs_token_totals.sql` → `3`.
    - `grep -c "parent_run_id IS NULL" supabase/migrations/182_workflow_runs_token_totals.sql` → at
      least `1` (D-256-01's read rule is written beside the columns).
    - The operator has confirmed the paste succeeded, and the SUMMARY records whether the `@>`
      predicate was ACCEPTED or the arity fallback was used.
    - `git diff --stat supabase/full-schema.sql` shows it changed, and
      `grep -c "token_coverage" supabase/full-schema.sql` → at least `2` (column + index predicate).
    - `git diff --numstat supabase/full-schema.sql` is the output of the script only — the SUMMARY
      states `regenerate-full-schema.sh` was run WITHOUT `--reset`.
    - `cd backend && venv/Scripts/python -m pytest tests/integration/test_256_migration_182.py -q`
      → passes, or skips with the explicit "local DB unreachable" reason printed.
    - The SUMMARY records the ACL-parity and greenfield-privileges commands and their verdicts
      verbatim.
  </acceptance_criteria>

  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python -m pytest tests/integration/test_256_migration_182.py -q</automated>
  </verify>

  <done>Migration 182 exists with the exact filename pattern, carries zero `NOT NULL` and zero
  `DEFAULT`, has been applied to the local DB by the operator via the SQL editor, `full-schema.sql`
  was regenerated without `--reset`, and the integration shape test passes or skips with a
  reason.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: The write seam — absorb_usage_box returns the delta, persist_run_usage ADDs it, finish_run is fenced byte-unchanged</name>
  <files>backend/app/services/circuit_breaker.py, backend/app/db/workflows.py, backend/tests/unit/test_256_persist_run_usage.py, backend/tests/unit/test_256_finish_run_unchanged.py</files>

  <read_first>
    - `backend/app/services/circuit_breaker.py:113-194` — `record_tokens` (`:140-148`),
      `absorb_usage_box` (`:150-164`), `armed` (`:128-130`), `check_limits` (`:188-193`).
    - `backend/app/db/workflows.py:2023-2035` — the house style for a single-statement
      `workflow_runs` writer (`256-PATTERNS.md` §2a).
    - `backend/app/db/workflows.py:2169-2199` — `finish_run` and its contract docstring.
      ⛔ **DO NOT EDIT THIS FUNCTION** (D-256-05).
    - `backend/app/db/runs.py:82-122` — `finalize_run`: the `None`-may-be-legitimate writer and the
      missing-usage WARNING CONTRACT at `:93-99` (`256-PATTERNS.md` §2c).
    - `256-RESEARCH.md` §"The Write Seam" (lines 432-533) — the signature, the docstring text, the
      delta derivation quoted verbatim, and the idempotency table.
    - `256-PATTERNS.md` §4 (`circuit_breaker.py` — widened to RETURN the delta) and §S-1, §S-2.
    - `backend/tests/unit/test_scheduler_circuit_breaker.py:340-370` and `:780-795` — the existing
      `absorb_usage_box` cases, and the proof no caller reads the return.
    - `backend/tests/unit/services/sources/test_240_contract_unchanged.py` — the md5-restoration
      precedent for Fence 3's RED drive.
  </read_first>

  <behavior>
    RED cases to author BEFORE the implementation, in
    `backend/tests/unit/test_256_persist_run_usage.py`:
    - `absorb_usage_box({"input_tokens": 100, "output_tokens": 40})` on a fresh breaker returns
      `(100, 40)`; a second call with `{"input_tokens": 250, "output_tokens": 90}` returns
      `(150, 50)` — the watermark, not the cumulative box.
    - `absorb_usage_box(None)` returns `(0, 0)`; `absorb_usage_box({})` returns `(0, 0)`.
    - A BACKWARDS box (`{"input_tokens": 1}` after `{"input_tokens": 500}`) returns `(0, 0)` — the
      `max(0, ...)` clamp is on the RETURNED delta, not only inside `record_tokens`. ⛔ Without
      this clamp a reset box would hand the DB a negative and SUBTRACT real spend.
    - `absorb_usage_box` still advances `breaker.input_tokens` / `.output_tokens` exactly as today
      (re-assert the two existing cases' final counter values).
    - `persist_run_usage` with `(0, 0)` issues NO SQL at all (fake pool records zero calls).
    - `persist_run_usage` with `(None, None)` issues NO SQL.
    - `persist_run_usage` with `(150, 50)` issues exactly ONE statement, parameterised, whose SQL
      text contains `COALESCE(input_tokens, 0)` and `+ $2` and `token_coverage = $4`, and whose
      params are `(run_id, 150, 50, list(TOKEN_COVERAGE_LEGS))`.
    - The SQL text contains `UPDATE workflow_runs` and `WHERE id = $1` and does NOT contain
      `thread_id` or any f-string interpolation marker.
    - `TOKEN_COVERAGE_LEGS == ("agent", "single", "batch")` at this plan's close — `"emit"` is
      appended by plan 256-04, in the same commit as the drain arms (O-4). A marker claiming a leg
      that has not shipped is a lie in a column built to prevent lies.

    Fence 3, in `backend/tests/unit/test_256_finish_run_unchanged.py`:
    - `finish_run`'s function source, extracted with `ast` and hashed, equals a digest committed in
      the test file.
    - The set of `await finish_run(` call sites under `backend/app/` is unchanged.
  </behavior>

  <action>
**(a) `backend/app/services/circuit_breaker.py` — widen `absorb_usage_box`'s return.** Keep the
`if not box: return (0, 0)` early exit, keep the two `max(0, int(... or 0))` reads, compute
`d_in = max(0, total_in - self.input_tokens)` and `d_out = max(0, total_out - self.output_tokens)`,
pass those to the existing `record_tokens`, and `return (d_in, d_out)`. Change the annotation from
`-> None` to `-> tuple[int, int]` and extend the docstring to say the return is the delta just
recorded and that it is what makes the DB write idempotent against repetition.
⛔ **No new field, no new branch, no new import.** Arithmetic to record: method count 9 → 9,
branches in `absorb_usage_box` 1 → 1, new state 0, new imports 0.
⚠ **The clamp must move WITH the extraction.** `record_tokens` clamps internally today; if
`d_in`/`d_out` are computed without the `max(0, ...)` the clamp is lost on the returned value.

**(b) `backend/app/db/workflows.py` — add `TOKEN_COVERAGE_LEGS` and `persist_run_usage`.**
Module-level constant `TOKEN_COVERAGE_LEGS: tuple[str, ...] = ("agent", "single", "batch")` with a
comment naming D-256-07, O-4 and the fact that plan 256-04 appends `"emit"` here. Then a NEW
module-level `async def persist_run_usage(pool, run_id, *, input_delta, output_delta) -> None`.

Body: return immediately when `not input_delta and not output_delta` (this single guard is what
makes the write idempotent against repetition — D-256-09, and `_enforce_budget` is invoked TWICE per
phase iteration at `harness_engine.py:1978` and `:2547`). Otherwise one parameterised
`pool.execute` of
`UPDATE workflow_runs SET input_tokens = COALESCE(input_tokens, 0) + $2, output_tokens = COALESCE(output_tokens, 0) + $3, token_coverage = $4 WHERE id = $1`
with `int(input_delta or 0)`, `int(output_delta or 0)`, `list(TOKEN_COVERAGE_LEGS)`.

⛔ **ADD, never SET, for the token columns** (D-256-09): `ctx.run_usage_box` resets to `{}` on every
`_resume_run`, so a SET would make the last segment's spend the whole run's total.
⛔ **No retry loop inside this writer** — a retry after a committed `UPDATE` would re-add. Let the
exception propagate, exactly as every other writer here does.
⛔ **Parameterised SQL only**, no f-string (T-091-03, and `claim_run`'s docstring names the rule).
⛔ **Never add an org-less `UPDATE ... WHERE thread_id` variant** — this runs on the service-role
pool, which BYPASSes RLS, so `WHERE id = $1` is the entire access boundary.

The docstring must carry, in its own words: ADD-not-SET and why; that `(0,0)`/`(None,None)` writes
NOTHING and that `NULL` means never measured while `0` means measured as zero; that this is NOT
value-identity-safe the way `finish_run` is (`db/workflows.py:2191-2199`) and that the `(0,0)` guard
is therefore load-bearing rather than an optimisation; and that under a genuine double-drive BOTH
producers paid the provider, so two ADDs of two real deltas is the TRUTHFUL total and a SET would
report half the money actually spent (RESEARCH.md §Q2-A).

⛔ **`finish_run` stays BYTE-UNCHANGED** (D-256-05). Do not widen it, do not touch its docstring.

**(c) Fence 3.** Write `backend/tests/unit/test_256_finish_run_unchanged.py`. Extract `finish_run`'s
source with `ast` (find the `AsyncFunctionDef` named `finish_run`, use `ast.get_source_segment`),
hash it, and compare to a digest committed in the test as a module constant. Also collect every
`await finish_run(` occurrence under `backend/app/` (excluding `__pycache__`) and assert the
file:line SET matches a committed list.

⚠ **RE-DERIVE that call-site set; do not copy a number.** ⛔ **A CORRECTION OWED — record it beside
the original, never overwriting it:** D-256-05 and RESEARCH.md both state *"7 call sites across 4
files"*. **Measured at `edf2d1024`: `grep -rn "await finish_run(" backend/app/` returns FIVE call
sites across THREE files** — `api/workflows.py:1802`, `harness_engine.py:2257`, `:2298`, `:2580`,
`run_lifecycle.py:521`. The other `finish_run(` hits are the `def` itself (`db/workflows.py:2169`)
and four prose mentions in docstrings/comments (`circuit_breaker.py:13`, `harness_engine.py:1860`,
`:1943`, `run_producer.py:302`). The decision (leave it alone) is unaffected; the FIGURE is wrong
and the plan says so rather than inheriting it.

**RED DRIVE for Fence 3, and it must be done:** make a whitespace-only edit inside `finish_run`,
run ONLY this test file, record the non-zero exit, restore, run again, record zero. ⛔ Prove the
restoration by **md5**, not by "I reverted it": record `md5sum backend/app/db/workflows.py` before
the plant, after the plant (must differ), and after removal (must be byte-identical to the first).
The Phase 255 `emitters.py` incident — a planted `eval()` sat live for a period — is why the proof
is mechanical.

⚠ How RED is proven without touching the 71-name ceiling: every RED is recorded from a TARGETED run
(`pytest tests/unit/test_256_<name>.py -x`), never from a full-gate figure. The full gate runs ONCE
at the wave close and the SET is diffed against
`256-BASELINE-backend-failing-set.txt` (D-256-15, zero headroom). A test left RED at the close is a
+1 above 71 and breaks the gate.
  </action>

  <acceptance_criteria>
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_256_persist_run_usage.py tests/unit/test_256_finish_run_unchanged.py -q` → all pass, 0 failed.
    - `grep -c "^async def \|^def " backend/app/db/workflows.py` recorded BEFORE and AFTER; the
      delta is exactly `+1`.
    - `md5sum` of `finish_run`'s ast-extracted source is IDENTICAL before and after this task
      (record both digests in the SUMMARY).
    - `grep -rn "await finish_run(" backend/app/ --include=*.py | wc -l` → `5`, the same set as
      before (record the five `file:line` entries).
    - `grep -c "def absorb_usage_box" backend/app/services/circuit_breaker.py` → `1`; the file's
      method count is recorded 9 → 9.
    - `grep -c "tuple\[int, int\]" backend/app/services/circuit_breaker.py` → at least `1`.
    - `grep -cE "f\"\"\"|f'|%s" backend/app/db/workflows.py` — no NEW f-string or `%`-interpolated
      SQL introduced by `persist_run_usage`; the statement uses `$1`/`$2`/`$3`/`$4` only
      (`grep -c '\$4' backend/app/db/workflows.py` → at least `1`).
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_scheduler_circuit_breaker.py tests/unit/test_scheduler_breaker_seam.py tests/unit/test_085_task_service.py -q` → pass unchanged. ⚠ If any of them
      goes red, that is a LEGITIMATE change to name in the SUMMARY, not a baseline regression — but
      measured at `edf2d1024` no caller reads the return, so none should.
    - The three md5 digests of `backend/app/db/workflows.py` (pre-plant / planted / restored) are in
      the SUMMARY, with digest 1 == digest 3.
  </acceptance_criteria>

  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python -m pytest tests/unit/test_256_persist_run_usage.py tests/unit/test_256_finish_run_unchanged.py tests/unit/test_scheduler_circuit_breaker.py -q</automated>
  </verify>

  <done>`absorb_usage_box` returns a clamped delta with zero new branches and zero new state;
  `persist_run_usage` exists as the single home of the token write, ADD-shaped, parameterised, with
  a `(0,0)` no-op guard; `TOKEN_COVERAGE_LEGS` is a three-member tuple; `finish_run` is provably
  byte-unchanged and Fence 3 has been observed RED and then green with md5-proved restoration.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: The reorder — persist ABOVE the armed guard, close METER-05 site 3, correct the stale count comment</name>
  <files>backend/app/services/harness_engine.py, backend/tests/unit/test_256_enforce_budget_persist.py, backend/tests/unit/test_256_producer_shell_site3.py</files>

  <read_first>
    - `backend/app/services/harness_engine.py:1818-1890` — the budget block: the ceiling read at
      `:1818-1819`, the STALE comment at `:1826`, the STALE `llm_emit` comment at `:1837-1842`,
      `_enforce_budget`'s def at `:1848`, its docstring at `:1850-1873` (⚠ the
      `CircuitBreakerTrippedError`-not-`CancelledError` contract and the Phase-194 `cancel_phase`
      call-site fence), the `armed` guard at `:1873-1874`, the absorb at `:1875`.
    - `backend/app/services/harness_engine.py:1844` — `ctx.run_usage_box = {}`, once per run SEGMENT.
    - `backend/app/services/harness_engine.py:1978` and `:2547` — the TWO `_enforce_budget` call sites.
    - `backend/app/services/harness_engine.py:3024-3057` — METER-05 site 3: the boot-sweep resume
      producer shell, `ctx` built at `:3024`, `_pid` at `:3040`, `input_tokens=None` at `:3051`.
    - `backend/app/services/run_producer.py:229-247` — ⭐ **THE SHAPE TO COPY.** It measures, WARNS,
      then writes. ⛔ **NOT `run_lifecycle.py:386`** — that is `finalize_run_terminal`'s body, a
      pass-through with `None` DEFAULTS at `:369-370` (RESEARCH.md C-5, confirmed by
      `256-PATTERNS.md` §6; CONTEXT.md is the wrong register here).
    - `backend/app/db/runs.py:93-99` — the missing-usage warning CONTRACT.
    - `256-RESEARCH.md` §Q2-B (lines 201-251) — the unreachable-write-point measurement and the
      behaviour-preservation proof.
    - `256-PATTERNS.md` §3 (lines 209-272) — the current code verbatim, the docstring contract, and
      the PROSE-ROT obligation.
    - `backend/tests/unit/test_scheduler_circuit_breaker.py` — the direct-`CircuitBreaker`
      construction analog for Fence 4.
  </read_first>

  <behavior>
    RED cases to author BEFORE the implementation, in
    `backend/tests/unit/test_256_enforce_budget_persist.py`:
    - ⭐ **THE HEADLINE CASE — a DISARMED breaker.** With `max_tokens=None` and
      `max_duration_seconds=None` (i.e. `breaker.armed is False`, which is what an INTERACTIVE
      harness run gets), a non-empty `ctx.run_usage_box` still reaches `persist_run_usage` with a
      real delta. ⛔ This must fail against the code as D-256-04 literally specifies it, because
      `:1873` returns first. **Do not write this case against a scheduled-run fixture** — that is
      the exact test that would pass while METER-03 persisted nothing for nearly every run.
    - With a DISARMED breaker, `check_limits` is never reached and `trip_breaker` is never called.
    - With an ARMED breaker at its ceiling, the trip still fires with `>=` semantics and
      `CircuitBreakerTrippedError` is still what is raised — ⛔ never `asyncio.CancelledError`.
    - Two consecutive `_enforce_budget` invocations with an UNCHANGED box produce exactly ONE
      `persist_run_usage` SQL statement (the second delta is `(0,0)`).
    - A `ctx` with NO `run_usage_box` attribute at all (the Deep-run / unit-stub / golden-run case
      named by `phase_types.py:758-762`) persists nothing and raises nothing.

    In `backend/tests/unit/test_256_producer_shell_site3.py` (site 3):
    - The boot-sweep resume finalize receives the box's `input_tokens` / `output_tokens` values.
    - When the box is empty, it receives `None`/`None` — ⛔ never `0`/`0` — and a
      `logger.warning` is emitted BEFORE the finalize call.
    - The warning's format string is the shipped literal
      `"runs.usage missing for run=%s provider=%s model=%s"` and contains none of `tokens=`,
      `value=`, `usage_dict` (T-073-04).
  </behavior>

  <action>
**(a) The reorder (D-256-04 as corrected by RESEARCH.md C-1 / O-2).** Inside `_enforce_budget`,
move `breaker.absorb_usage_box(getattr(ctx, "run_usage_box", None))` from `:1875` to ABOVE the
`if not breaker.armed: return` guard, capture its returned `(d_in, d_out)`, and immediately
`await persist_run_usage(pool, run_id, input_delta=d_in, output_delta=d_out)` — also above the
guard. Then the `armed` guard, then `check_limits`, unchanged.

⛔ **The trip decision must not move.** Proof to state in the SUMMARY: on a disarmed breaker
`max_tokens` and `max_duration_seconds` are both `None`, so `check_limits` returns `(False, None)`
unconditionally (`circuit_breaker.py:188-193`) — the `armed` guard is a short-circuit, never a
semantic; and `absorb_usage_box` mutates only the breaker's own counters, which a disarmed breaker
never reads.

⛔ **Do NOT wrap the persist in a `try`.** The function's own docstring (`:1851-1856`) records that
it raises `CircuitBreakerTrippedError` and **not** `asyncio.CancelledError`, because a
`CancelledError` would take the escape arm that calls `cancel_phase` — and a shipped Phase-194
fence AST-counts `cancel_phase` call sites in this module at EXACTLY ONE. A `try/except` here adds
a branch and breaks the `3 → 3` arithmetic.
⛔ **No inline SQL in this file** (D-256-05): its inline-SQL count stays `0 → 0`.

Arithmetic to record: `_enforce_budget` branch count `3 → 3`; its `await` count `1 → 2`;
`_enforce_budget` call sites `2 → 2` (`:1978`, `:2547`); `ctx.run_usage_box =` assignments
`1 → 1`; inline SQL statements `0 → 0`; new functions `0`; new state `0`.

**(b) METER-05 site 3 (D-256-08 site 3).** In the boot-sweep resume producer shell at
`harness_engine.py:3044-3053`, replace the literal `input_tokens=None, output_tokens=None` with the
segment box's values, read as `_box = getattr(ctx, "run_usage_box", None) or {}` then
`_box.get("input_tokens")` and `_box.get("output_tokens")`.
⛔ **NO default and NO `or 0`** — an absent key stays `None` all the way to the column (D-256-06).
Add the missing-usage `logger.warning` BEFORE the finalize call, inside the same `try`, using the
shipped literal format string exactly: `"runs.usage missing for run=%s provider=%s model=%s"`, with
identifier arguments only (S-3 / T-073-04). ⛔ **Never token VALUES in the format string.**
⚠ **Grain (D-256-03):** the box holds the SEGMENT's spend and this shell is a per-segment `runs`
row, so writing segment onto segment is correct here. ⛔ Do not write the `workflow_runs`
cumulative from this site.

**(c) The prose-rot obligations — both of them, in this commit.** ⛔ `harness_engine.py:1826` reads
*"`harness/` contained two `usage` references in total, both `input_tokens=None`"*. **That count is
measurably wrong:** `grep -rn "input_tokens=None" backend/app/` returns SEVEN argument sites plus
one default parameter (D-256-08, re-confirmed at `edf2d1024`). Correct the comment, recording the
original beside the correction per the project's standing rule (S-5) — never overwriting it.
⛔ **DO NOT touch the `llm_emit` comment at `:1837-1842`** — that one is plan 256-04's, by F-4, and
it must move in the same commit as the drain arms. Leave it exactly as it is.
  </action>

  <acceptance_criteria>
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_256_enforce_budget_persist.py tests/unit/test_256_producer_shell_site3.py -q` → all pass, 0 failed.
    - In `backend/app/services/harness_engine.py`, the line number of `persist_run_usage(` is
      STRICTLY LESS than the line number of `if not breaker.armed:` — assert it, do not eyeball it:
      `awk '/persist_run_usage\(/{p=NR} /if not breaker.armed:/{a=NR} END{exit !(p>0 && p<a)}' backend/app/services/harness_engine.py`
      exits `0`.
    - `grep -c "input_tokens=None" backend/app/services/harness_engine.py` → `0` (site 3 closed AND
      the `:1826` comment's stale literal rewritten).
    - `grep -c "runs.usage missing for run=%s provider=%s model=%s" backend/app/services/harness_engine.py` → `1`.
    - `grep -cE "SELECT |UPDATE |INSERT " backend/app/services/harness_engine.py` recorded before
      and after — UNCHANGED (no inline SQL added).
    - `grep -c "cancel_phase(" backend/app/services/harness_engine.py` recorded before and after —
      UNCHANGED (the Phase-194 fence still reads exactly one).
    - `grep -c "llm_emit.. PHASES ARE NOT COUNTED" backend/app/services/harness_engine.py` → `1`
      (⛔ still present — plan 256-04 owns it).
    - The SUMMARY records: `_enforce_budget` branches 3 → 3, its `await` count 1 → 2, its call sites
      2 → 2, `ctx.run_usage_box =` assignments 1 → 1, inline SQL 0 → 0, new functions 0, new state 0.
    - `cd backend && venv/Scripts/python -m pytest tests/test_harness_engine.py -q` → no NEW failures. ⚠ This file is in
      `tests/`, NOT `tests/unit/`, so it is OUTSIDE the canonical gate and a RED there is invisible
      to the 71-name baseline — run it explicitly and record its figure.
  </acceptance_criteria>

  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python -m pytest tests/unit/test_256_enforce_budget_persist.py tests/unit/test_256_producer_shell_site3.py -q</automated>
  </verify>

  <done>`persist_run_usage` is called from `_enforce_budget` at a line number strictly above the
  `if not breaker.armed:` guard (asserted mechanically); a DISARMED breaker persists a real delta;
  an ARMED breaker still trips with `CircuitBreakerTrippedError`; site 3 writes real segment totals
  with the identifier-only warning; the `:1826` stale count is corrected beside its original; the
  `:1837-1842` `llm_emit` comment is UNTOUCHED (plan 256-04 owns it); and every count in the
  arithmetic list is recorded before and after.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 4: SC#1's durability, proven across a real process boundary — and the residual gap named</name>
  <files>backend/tests/integration/test_256_persist_restart.py</files>

  <read_first>
    - `256-PATTERNS.md` §10 (lines 845-878) — ⛔ **`NO IN-REPO ANALOG`**, gap G-a. Read all three
      partial matches and exactly where each diverges.
    - `backend/tests/unit/test_239_settings_cross_worker_invalidation.py:124-168` — the ONLY in-repo
      mechanism that genuinely crosses a process boundary (`subprocess.Popen([sys.executable, "-c",
      textwrap.dedent(src)], cwd=BACKEND_DIR, ...)` reporting over a JSON line on stdout). ⚠ Read
      its own docstring at `:166-168`: *"WHAT THIS DOES NOT PROVE: that the child read the real
      Postgres row (its pool is a stub)"*.
    - `backend/tests/integration/test_093_ask_user_workflow_run_live.py` — a fresh asyncpg pool
      against live `workflow_runs` on `localhost:54322` with the service-role client. The other half
      of the shape.
    - `backend/tests/integration/test_120_migration.py` — the skip-if-unreachable + rolled-back
      transaction discipline.
    - `backend/app/db/workflows.py::persist_run_usage` as written in Task 2.
  </read_first>

  <behavior>
    In `backend/tests/integration/test_256_persist_restart.py`:
    - Seed a `workflow_runs` row (with its required `org_id`, `thread_id`), call
      `persist_run_usage` twice with different deltas over a REAL asyncpg pool, close that pool,
      then have a **child interpreter** (`subprocess.Popen([sys.executable, "-c", ...])`) open its
      OWN real asyncpg pool and print the row's `input_tokens`, `output_tokens` and
      `token_coverage` as a JSON line on stdout. The parent asserts the child read the SUM of the
      two deltas and the coverage array.
    - ⛔ The child's pool must be REAL, not a stub — that is the exact divergence
      `test_239_settings_cross_worker_invalidation.py` discloses about itself, and copying the stub
      would reproduce a test that proves the process boundary was crossed while proving nothing
      about the DB.
    - A second `persist_run_usage` with a `(0, 0)` delta leaves the row's numbers unchanged
      (idempotence against repetition, observed through the DB rather than through a fake pool).
    - The module docstring states, verbatim and without softening: which property is proven (a
      value written by one process is read by a DIFFERENT process through a fresh pool), which is
      NOT (a crash / `kill -9` / OOM mid-phase, and a worker whose in-flight transaction was never
      committed), and that this file lives in `tests/integration/` and is therefore INVISIBLE to
      the 71-name unit baseline.
    - Clean skip with a printed reason when `localhost:54322` is unreachable.
  </behavior>

  <action>
Write `backend/tests/integration/test_256_persist_restart.py`, combining
`test_239_settings_cross_worker_invalidation.py`'s `subprocess.Popen([sys.executable, "-c", ...])`
mechanism with `test_093_ask_user_workflow_run_live.py`'s live `workflow_runs` pool. ⭐ **This
combination does not exist in the repository** (`256-PATTERNS.md` gap G-a) — it is being invented
here, deliberately, because SC#1's literal words are *"re-reading the run after the process
restarts returns the same totals"* and no weaker shape can claim them.

⛔ **STATE THE PROPERTY, do not overstate it.** The plan asserts: *a value written by process A and
committed is read back by process B through a pool B created itself.* It does NOT assert: *a value
survives a `kill -9` at an arbitrary instruction.* Both halves go in the module docstring and in
the SUMMARY. A silent overstatement here would be SC#1 claimed and not proven — the exact failure
mode this phase exists to end.

All writes go through `persist_run_usage`, never raw SQL from the test — otherwise the test proves
Postgres works rather than proving the writer works. Clean up the seeded row in a `finally`.

⚠ Because `tests/integration/` is outside `pytest tests/unit`, this cannot be the phase's only
proof of durability. The unit-level proof is Task 3's disarmed-breaker drive plus Task 2's
statement-level assertions; this file is the end-to-end one. Say that in the docstring.
  </action>

  <acceptance_criteria>
    - `cd backend && venv/Scripts/python -m pytest tests/integration/test_256_persist_restart.py -q` → passes
      against a running local Supabase, or skips with the printed reason "local DB unreachable".
    - `grep -c "subprocess.Popen" backend/tests/integration/test_256_persist_restart.py` → at least `1`.
    - `grep -c "create_pool\|asyncpg.connect" backend/tests/integration/test_256_persist_restart.py`
      → at least `2` (the parent's pool and the child's own).
    - `grep -c "persist_run_usage" backend/tests/integration/test_256_persist_restart.py` → at least `2`.
    - The module docstring contains BOTH an explicit "proves" sentence and an explicit "does NOT
      prove" sentence, and names `tests/integration/` as outside the unit gate.
    - The SUMMARY quotes the docstring's "does NOT prove" sentence verbatim.
  </acceptance_criteria>

  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python -m pytest tests/integration/test_256_persist_restart.py -q</automated>
  </verify>

  <done>A child interpreter with its own real asyncpg pool reads back the accumulated totals and
  the coverage array written by the parent through `persist_run_usage`; the gap between that
  property and SC#1's literal words is written down rather than glossed; and the file skips cleanly
  when the local DB is down.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness engine loop → Postgres (service-role pool) | `persist_run_usage` runs on `get_pg_pool()`, which **BYPASSes RLS**. `WHERE id = $1` is the ENTIRE access boundary, and `run_id` originates in the engine's own loop — never in a request body. |
| provider stream → `ctx.run_usage_box` → the persisted integer | A provider-reported usage number becomes a durable figure Phase 257 turns into money. The value is attacker-*influenceable* only by a party who already controls the model response. |
| operator → Supabase SQL editor (migration 182) | A human-applied DDL step. Cloud parity is a separate operator-gated action, OWED, and out of scope here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-256-01 | Tampering | `persist_run_usage` SQL | mitigate | Parameterised `$1..$4` ONLY. ⛔ No f-string, no `%`-interpolation, no identifier built from a variable (T-091-03; `claim_run`'s docstring names the rule). Acceptance criterion greps for `$4`. |
| T-256-02 | Elevation of privilege | service-role pool bypassing RLS | mitigate | `WHERE id = $1` with an engine-internal `run_id`. ⛔ **Never add an org-less `UPDATE … WHERE thread_id` variant.** This is the posture `finish_run` already has; `workflow_runs`' four existing RLS policies (`org_id IN (SELECT current_user_org_ids())` AND thread-owner) are inherited unchanged by a new column. |
| T-256-03 | Information disclosure | the three new columns through any serializer | accept, with a check | 182 adds NO route and NO response model. ⚠ Verify rather than assert: `grep -rn "input_tokens\|output_tokens\|token_coverage" backend/app/models/ backend/app/api/workflows.py backend/app/api/runs.py` — if a `workflow_runs` serializer uses `SELECT *` or a permissive model the columns become visible to the row's owner ONLY, which is already the RLS boundary. Record the grep result. |
| T-256-04 | Tampering / integrity | a provider inflating a counted number | accept | A party who controls the model response can already inflate `usage`, and the breaker's own ceiling reads the same figure — so this is the shipped trust model, not a new exposure. ⛔ Do NOT add validation that clamps a "too large" usage figure: that would silently UNDER-report real spend, which is the repudiation failure this phase exists to close. |
| T-256-05 | Tampering / arithmetic | `COALESCE(x,0) + $n` driven to overflow, or to poison a per-org aggregate | mitigate | Deltas are watermark-derived and `max(0, …)`-clamped, so one write is bounded by one phase's real usage and is never negative. ⚠ The column is `integer` (~2.1 B ceiling, mirroring `runs`); overflow needs ~2 billion tokens on ONE run, which the breaker ceiling and `llm_call_timeout_seconds` make unreachable. ⛔ Per-org poisoning is prevented by D-256-01's read rule, not by this writer — that is plan 256-02's fence. |
| T-256-06 | Repudiation | a run under-reporting what it cost | mitigate | ⭐ The whole plan. Its security-relevant failure mode is a silent `0` where the truth is *unknown*: `NULL ≠ 0` is a SECURITY property here, not a style preference (D-256-06). Enforced by the nullable DDL, the `(0,0)` no-op guard, and the `.get()`-with-no-default rule. |
| T-256-07 | Information disclosure | the missing-usage `logger.warning` | mitigate | Identifier-only format string, the shipped literal `"runs.usage missing for run=%s provider=%s model=%s"`. ⛔ **T-073-04: no token VALUES** — pinned by `test_token_accumulator_missing_usage.py:41` and re-asserted in this plan's site-3 test. |
| T-256-08 | Information disclosure | an unauthenticated (`anon`) read of the new columns | mitigate at deploy | ⚠ **BUG-260911-01's lesson: every gate in this project reads through the SERVICE ROLE, so nothing in the suite ever requests as `anon`.** ⛔ `get_advisors(security)` belongs in migration 182's cloud deploy-parity checklist. Local-only here; recorded so the checklist inherits it. |
| T-256-SC | Tampering | npm/pip/cargo installs | mitigate | ⭐ **No package is installed by this plan** — zero additions to `requirements.txt` / `package.json`, so the Package Legitimacy Gate has no subject and no `[ASSUMED]`/`[SUS]` checkpoint is owed. Acceptance: `git diff --numstat backend/requirements.txt frontend/package.json` is EMPTY. |
</threat_model>

<verification>
Run at the CLOSE of this plan, in this order:

1. The targeted suites (per task, already in each `<verify>`).
2. ⛔ **THE BACKEND BASELINE, ONCE, and diff the SET — never the count** (D-256-15, zero headroom):
   `cd backend && venv/Scripts/python -m pytest tests/unit -q --continue-on-collection-errors 2>&1 | grep "^FAILED" | sort`
   then `comm -13` and `comm -23` against
   `.planning/phases/256-every-token-is-counted-and-kept/256-BASELINE-backend-failing-set.txt`.
   ⛔ Count with `grep -c`; **never `| tail`** — this project once published 71 when the truth was
   72 because a `tail` kept the count and threw the set away. A name that LEFT the set is also a
   change and must be explained.
3. `cd backend && venv/Scripts/python -m pytest tests/test_harness_engine.py -q` — ⚠ `tests/`, not
   `tests/unit/`, therefore OUTSIDE the canonical gate. Record its figure explicitly.
4. `node scripts/check-hot-file-ledger.cjs 256` — ⛔ read the **`watched: N`** line, not the exit
   code (255's vacuous pass was `watched: 0` with exit 0). ⚠ This plan names
   `backend/app/services/circuit_breaker.py`, which has **no ledger row**; the row is added by plan
   **256-02 in this same wave**. The gate is phase-level, so it clears once 256-02 lands.
5. `node scripts/check-extension-contract.cjs` — this plan modifies none of the six trigger files,
   so it must stay green. `TOKEN_COVERAGE_LEGS` is a DATA value written to a column and resolves
   nothing.
6. **Frontend: SKIPPED, deliberately.** This plan modifies zero files under `frontend/`, so
   `scripts/vitest-count-gate.cjs` has no subject. ⛔ Do not run it and do not quote a figure from
   it. Per **D-256-14** the frontend gate is **NON-DETERMINISTIC at base** (`failed 3` then
   `failed 0` on a byte-identical tree, cap 2 on both) — quoting `failed 0` as "green" would be
   false either way.
7. `bash scripts/check-deploy-drift.sh` — 182 adds no env var and no seed row; record it unchanged.
</verification>

<success_criteria>
- Migration `182_workflow_runs_token_totals.sql` exists, was applied locally by the operator via
  the SQL editor, `full-schema.sql` regenerated WITHOUT `--reset`; zero `NOT NULL`, zero `DEFAULT`
  (D-256-06); three `COMMENT ON COLUMN` blocks carrying D-256-01, D-256-03, D-256-07 and D-256-09;
  the partial index created, with the SUMMARY stating whether `@>` was accepted or the arity
  fallback was used (gap G-b closed by measurement, not assumption).
- `persist_run_usage` is the ONE home of the token write (D-256-05), ADD-shaped (D-256-09),
  parameterised, with a `(0,0)` no-op guard; `finish_run` provably byte-unchanged (Fence 3,
  RED-driven, md5-restored).
- `absorb_usage_box` returns a clamped delta; branches 1 → 1, methods 9 → 9, new state 0.
- `persist_run_usage(` appears at a line number strictly ABOVE `if not breaker.armed:` in
  `harness_engine.py` — the C-1 correction to D-256-04, asserted mechanically.
- A DISARMED (interactive) breaker persists a real delta; an ARMED one still trips identically.
- `grep -c "input_tokens=None" backend/app/services/harness_engine.py` → `0` (D-256-08 site 3).
- The SC#1 round-trip is proven across a REAL child process with its own real pool, and the
  residual gap (`kill -9` mid-phase) is NAMED in the test docstring and the SUMMARY (gap G-a).
- The backend failing SET is byte-identical to the committed 71 names, in both `comm` directions
  (D-256-15).
- The SUMMARY carries the arithmetic, not the adjective (D-256-13): every count in Task 2's and
  Task 3's acceptance lists, before and after.
</success_criteria>

<output>
Create `.planning/phases/256-every-token-is-counted-and-kept/256-01-SUMMARY.md` when done.

It MUST contain: the asserted base SHA; the operator's migration-apply confirmation and whether the
`@>` predicate was accepted; the three md5 digests (pre-plant / planted / restored) for Fence 3;
the full arithmetic table (D-256-13); the `finish_run` call-site correction recorded BESIDE
D-256-05's original *"7 call sites across 4 files"* figure, never overwriting it; the backend SET
diff in both directions; and the verbatim "does NOT prove" sentence from the restart test's
docstring.
</output>
