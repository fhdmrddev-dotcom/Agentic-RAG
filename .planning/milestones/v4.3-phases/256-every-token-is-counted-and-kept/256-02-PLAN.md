---
phase: 256-every-token-is-counted-and-kept
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/test_256_token_sum_narrowing.py
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
  - .planning/seeds/SEED-074-workflow-harness-token-usage-rollup.md
  - .planning/seeds/SEED-297-boot-reconciler-nulls-cap-paused-token-totals.md
  - .planning/seeds/SEED-298-max-tokens-per-run-is-really-per-segment.md
  - .planning/seeds/SEED-299-stranded-deep-chat-run-token-count-unknowable.md
  - .planning/seeds/SEED-300-three-token-holes-surviving-phase-256.md
autonomous: true
requirements: [METER-04, METER-06]
mutates_local_db: false

must_haves:
  truths:
    - "D-256-02: a summing site over the `runs` table's token columns that omits the `parent_run_id IS NULL` narrowing makes a fence FAIL, and that fence has been OBSERVED RED against a planted un-narrowed SUM, with the plant proven removed by md5 — never by timing and never by 'I deleted it'."
    - "D-256-01's read rule is discharged as a HIERARCHY: a child row plus an INCLUSIVE parent row is a tree, not a double count, and what SC#2 forbids is more than one NON-OVERLAPPING sum."
    - "The fence's VACUITY CONTROL is mandatory, not advisable: the subject set is measurably EMPTY today (nothing in the backend aggregates those columns — RESEARCH.md Q3, seven search strategies), so a fence over nothing would pass forever. It must prove it visited a plausible number of files AND that its matcher fires on an in-test haystack."
    - "Every G-5-relevant file this phase touches or reads has a ledger row in BOTH registers (CLAUDE.md scan list + docs/HOT-FILE-LEDGER.md section), re-derived rather than copied forward."
    - "D-256-10 and D-256-08 site #7 are NAMED IN THE REGISTER, not fixed — a named hole beats a silent one, and that is this phase's thesis applied to itself."
    - "R-1, a NEW defect measured during research (the boot reconciler NULLing a cap_paused run's real totals), is dispositioned EXPLICITLY as REGISTER, with the reason written down."
  artifacts:
    - path: "backend/tests/unit/test_256_token_sum_narrowing.py"
      provides: "Fence 1 — the parent_run_id IS NULL narrowing fence + its two vacuity controls"
      contains: "ast.parse"
    - path: "docs/HOT-FILE-LEDGER.md"
      provides: "scan-list rows + per-file sections for the no-row files, and corrected triples for the stale ones"
    - path: ".planning/seeds/SEED-297-boot-reconciler-nulls-cap-paused-token-totals.md"
      provides: "R-1 — the measured NULL-overwrite defect, registered with a concrete trigger"
  key_links:
    - from: "backend/tests/unit/test_256_token_sum_narrowing.py"
      to: "backend/app/**/*.py"
      via: "an ast walk that joins string literal pieces per statement and tests a CONJUNCTION"
      pattern: "parent_run_id IS NULL"
    - from: "CLAUDE.md"
      to: "docs/HOT-FILE-LEDGER.md"
      via: "the same-commit sync rule — a row here, its section there, one commit"
      pattern: "backend/app/services/circuit_breaker.py"
---

<objective>
Establish the READ rule before its first reader exists, and close the register debts.

Three things: (1) **Fence 1** — a source fence that fails on any site aggregating the `runs` table's
token columns without the `parent_run_id IS NULL` narrowing (D-256-02), authored EARLY because it is
green from birth and its whole value is the RED drive; (2) the **hot-file ledger rows** for the
files this phase touches or reads that have none — G-5 is not weak on a rowless file, it is ABSENT
there, at any commit count, forever, silently; (3) the **five register entries** (R-1..R-5) that
carry every hole this phase does not close.

Purpose: SC#2 (METER-04) — *"a sub-agent's tokens appear in exactly one place rather than twice or
nowhere"* — plus METER-06's *"or named in a register entry with a concrete re-open trigger"* half.
⭐ **Phase 257 is the FIRST consumer of these columns**, which is the good news: the read rule can be
fenced BEFORE any reader exists, so the fence guards the future rather than retrofitting the past.
Output: one fence with its RED evidence, N ledger rows in two registers, four new seeds, one seed
status flip.

**This plan mutates NO database** and touches no `backend/app` source at rest. It runs in wave 1
alongside 256-01 and their `files_modified` sets are disjoint.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⛔ **FIRST ACTION IN A WORKTREE:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`.
**Base SHA to assert: `edf2d1024` on `develop`** (⚠ worktrees start on the default branch).
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/256-every-token-is-counted-and-kept/256-CONTEXT.md
@.planning/phases/256-every-token-is-counted-and-kept/256-RESEARCH.md
@.planning/phases/256-every-token-is-counted-and-kept/256-PATTERNS.md
@.planning/seeds/TEMPLATE.md
@CLAUDE.md
@docs/HOT-FILE-LEDGER.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Fence 1 — the parent_run_id IS NULL narrowing fence, driven RED against a planted un-narrowed SUM</name>
  <files>backend/tests/unit/test_256_token_sum_narrowing.py</files>

  <read_first>
    - `256-RESEARCH.md` §Q3 (lines 254-327) — the seven search strategies, the evidence table, and
      the four "known narrowed sites" that turn out to select NO token column. ⭐ Read the
      consequence list: the subject set is EMPTY, which is what makes the vacuity control mandatory.
    - `256-RESEARCH.md` §"Fences To Build" → Fence 1 (lines 638-654).
    - `256-PATTERNS.md` §8 (lines 663-796) — and read its TWO caveats carefully.
    - `backend/tests/unit/test_255_extension_contract_guard.py:122-144` — ⭐ the LIVE reporting /
      vacuity analog. Copy: a collected `violations` list reported as ONE assertion formatted
      `f"{rel_path}:{idx} -> ..."`; `code_part = line.split("#")[0]` so a comment cannot trip it;
      and ⭐ **a missing file is a VIOLATION, not a skip** — a renamed subject cannot silently empty
      the set. ⛔ **It imports `ast` at `:15` and NEVER USES IT** (`grep -c "ast\." → 0`) — it is a
      REGEX fence. RESEARCH.md citing it as "the AST mechanics" is WRONG; take only its reporting
      shape from it.
    - `backend/tests/unit/test_190_egress.py` and `backend/tests/unit/test_222_no_undefined_names.py`
      — real `ast.parse` fences in the canonical gate. ⭐ Take the AST mechanics from one of these.
    - ⛔ `backend/tests/unit/test_189_no_egress.py` — RESEARCH.md names it as THE analog and that is
      only half right. Its walk helper `_app_python_files` (`:80`) has **ZERO callers** and its gate
      was retired to a 2-line import assert under D-206-07, so **its promised file-count vacuity
      guard executes nowhere.** Read `:215-244` (the live matcher positive control) for shape, and
      read `:59-71` where a matcher was FALSIFIED BY ITS OWN CONTROL and the matcher was
      strengthened rather than the control weakened — budget for the same happening here.
    - `backend/tests/unit/services/sources/test_240_contract_unchanged.py` — the md5-restoration
      precedent.
    - `backend/app/db/runs.py` — the plant target (and `finalize_run` at `:82-122`, the writer the
      fence must NOT flag).
    - `backend/app/api/runs.py:1558-1566` and `backend/app/api/workflows.py:1755-1774` — how SQL is
      actually assembled here: concatenated string literals across lines. This is why a
      line-oriented grep cannot see a `SUM(` on one line and a missing `WHERE` four lines down.
  </read_first>

  <behavior>
    Cases in `backend/tests/unit/test_256_token_sum_narrowing.py`, authored and OBSERVED RED before
    any matcher exists:

    - **The fence itself:** for every string literal under `backend/app/**/*.py` — including
      implicitly-concatenated pieces and f-string parts, joined per enclosing statement via
      `ast.parse` — a statement that contains BOTH a `runs`-table token column name
      (`input_tokens` / `output_tokens`) AND an aggregation token (`SUM(`, `sum(`, `count(` over
      `runs`) MUST also contain `parent_run_id IS NULL`. Violations are collected and reported in
      ONE assertion, each entry naming `file:line`.
    - **The supabase-py builder form too:** a chained select carrying a token column without a
      sibling `.is_("parent_run_id", "null")` in the same expression.
    - ⭐ **VACUITY CONTROL A — the matcher fires.** Drive the matcher against an IN-TEST haystack
      string that DOES contain `SELECT SUM(input_tokens) FROM runs WHERE org_id = $1` and assert it
      is reported. ⛔ **MANDATORY, not advisable:** RESEARCH.md Q3 measured ZERO current subjects, so
      without this the fence passes over nothing, forever. That is the textbook vacuous pass this
      project has recorded twice.
    - ⭐ **VACUITY CONTROL B — the walk visited something.** Assert the number of `.py` files parsed
      under `backend/app` is `>= 150`. ⛔ Do NOT copy `test_189_no_egress.py`'s file-count guard by
      reference — it is dead code with zero callers; write a live one.
    - ⭐ **VACUITY CONTROL C — a missing subject is a violation.** If the walk root does not exist,
      the fence FAILS rather than skipping (`test_255_extension_contract_guard.py`'s
      `"File not found"` arm).
    - **NEGATIVE control — the writer is not a violation.** `persist_run_usage`'s
      `UPDATE workflow_runs SET input_tokens = COALESCE(input_tokens, 0) + $2 ...` must NOT be
      flagged: it has no aggregation token, and `workflow_runs` has no `parent_run_id` column at all
      (D-256-03 — a DIFFERENT grain). ⚠ This case is what stops the fence breaking plan 256-01's
      merge. Pin it explicitly, with a haystack string, not by hoping.
    - **NEGATIVE control — the four existing narrowed joins are not violations.**
      `api/workflows.py:1772`, `api/runs.py:1563`, `api/threads.py:384`/`:468` are cancel/reconcile
      identity guards that select NO token column (RESEARCH.md C-3 corrects CONTEXT.md here). The
      fence must be green over them today.
  </behavior>

  <action>
Write `backend/tests/unit/test_256_token_sum_narrowing.py`. Take the **AST mechanics** from
`test_190_egress.py` or `test_222_no_undefined_names.py` (real `ast.parse`) and the
**reporting/vacuity shape** from `test_255_extension_contract_guard.py:122-144`. Neither file alone
is the analog — say so in the module docstring so the next reader does not inherit RESEARCH.md's
wrong citation.

The matcher must be a **CONJUNCTION** test over a joined statement, not a line test. Collect
`ast.Constant` string pieces per enclosing statement (`ast.Expr` / `ast.Call` / `ast.Assign` and
`ast.JoinedStr` parts), join them, and test the joined text. A line-oriented matcher cannot see
`SUM(` and a missing `WHERE` four lines apart, and that is exactly how this codebase builds SQL.

The module docstring must state: the threat/decision id **D-256-02**; that **D-256-01**'s read rule
is a HIERARCHY (an inclusive parent plus its children is a tree, not a double count — what SC#2
forbids is more than one NON-OVERLAPPING sum); that the subject set was measured EMPTY at
`edf2d1024` and therefore the positive control is load-bearing; and **D-256-03** — the rule binds
the `runs` table only, because `workflow_runs` is a different grain with no `parent_run_id`.

**THE RED DRIVE — this is the point of the task, and it is not optional (`tdd_mode: true`, and G-8
names TDD RED drives as one of the four things that may NEVER be cut):**

1. `md5sum backend/app/db/runs.py` — record digest #1.
2. Plant ONE method in `backend/app/db/runs.py` that does
   `await pool.fetchrow("SELECT SUM(input_tokens) AS t FROM runs WHERE org_id = $1", org_id)` with
   NO `parent_run_id` clause.
3. `md5sum backend/app/db/runs.py` — record digest #2; it MUST differ from #1.
4. Run ONLY the fence: `cd backend && venv/Scripts/python -m pytest tests/unit/test_256_token_sum_narrowing.py -x`.
   Record the **non-zero exit** and the fence's own output line naming `backend/app/db/runs.py` and
   the line number. ⛔ A fence that fails without naming the file is not finished.
5. Remove the plant. `md5sum` again — digest #3 MUST be byte-identical to digest #1, and
   `git diff --numstat -- backend/app/db/runs.py` must produce NO output.
6. Re-run the fence; record the zero exit.

⛔ **Prove the removal by md5 or an armed watcher, NEVER by timing and never by "I reverted it."**
The Phase 255 `emitters.py` incident — a planted `eval()` sat live for a period — is why. All three
digests go in the SUMMARY.

⚠ **`backend/app/db/runs.py` is NOT in this plan's `files_modified`, deliberately:** the plant is
transient and the file's net diff is empty. Listing a file that is not touched is drift; the
transient plant is disclosed here and in the SUMMARY instead.
  </action>

  <acceptance_criteria>
    - `cd backend && venv/Scripts/python -m pytest tests/unit/test_256_token_sum_narrowing.py -q` → passes, 0 failed.
    - `grep -c "ast.parse" backend/tests/unit/test_256_token_sum_narrowing.py` → at least `1`.
    - `grep -c "D-256-02" backend/tests/unit/test_256_token_sum_narrowing.py` → at least `1`.
    - `grep -c "parent_run_id IS NULL" backend/tests/unit/test_256_token_sum_narrowing.py` → at least `1`.
    - The test file contains at least THREE vacuity/negative controls that can be named
      individually: matcher-fires-on-haystack, files-parsed `>= 150`, missing-root-is-a-violation.
    - `md5sum backend/app/db/runs.py` digest #1 == digest #3, and both are in the SUMMARY alongside
      digest #2 (which must differ).
    - `git diff --numstat -- backend/app/db/runs.py` → NO output at the task's end.
    - The SUMMARY quotes the fence's RED output line verbatim, including the file and line number it
      named.
    - `git status --short backend/app/` → shows no modification from this plan.
  </acceptance_criteria>

  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python -m pytest tests/unit/test_256_token_sum_narrowing.py -q</automated>
  </verify>

  <done>Fence 1 exists as a real `ast.parse` conjunction fence with three live controls; it was
  OBSERVED RED against a planted un-narrowed SUM and named the file and line; the plant is proven
  removed by md5 with an empty net diff; and the writer plan 256-01 adds is pinned as a NEGATIVE
  control so the fence cannot break the merge.</done>
</task>

<task type="auto">
  <name>Task 2: The hot-file ledger rows — G-5 is ABSENT on a rowless file, not weak</name>
  <files>CLAUDE.md, docs/HOT-FILE-LEDGER.md</files>

  <read_first>
    - `CLAUDE.md` §"Hot-file ledger (update as phases ship)" — the scan-list table, the
      same-commit sync rule, and the **200-character cap** on a disposition cell.
    - `CLAUDE.md` §"CLAUDE.md context budget (MANDATORY)" — the 150,000-CHARACTER gate (characters,
      not bytes; `wc -c` disagrees with the gate) and why the DISPOSITION column is capped.
    - `docs/HOT-FILE-LEDGER.md` — the `## Scan list` section (exact header
      `| File | commits / phases / lines | G-5 | Disposition |`) and the existing per-file sections
      for `backend/app/services/scheduler_service.py` (`:8661`) and
      `backend/app/services/run_producer.py`, as style models.
    - `scripts/check-hot-file-ledger.cjs:39-70` — the EXEMPT list and `WATCHED` roots, so the
      obligation set is derived rather than guessed. ⚠ Only `backend/app/**` and `frontend/src/**`
      are watched; `.sql`, `.md`, `backend/tests/**`, `scripts/**` and `supabase/migrations/**` are
      exempt.
    - `256-RESEARCH.md` §"G-5 Arithmetic" (lines 840-885) — the 13-file table, the FOUR stale rows,
      and C-4 (⛔ `scheduler_service.py` ALREADY HAS a row; D-256-13 is wrong about it, and
      `task_service.py` was missed entirely).
  </read_first>

  <action>
⛔ **RE-DERIVE every triple with CLAUDE.md's own recipe. Do NOT copy a figure from RESEARCH.md,
CONTEXT.md or this plan** — this ledger's own recurring finding is that a figure written at a
phase's close goes stale on the next commit, and a row that is present and WRONG answers the
auditor and stops the audit, which is worse than an absent row. The recipe, per file:
`git log --oneline -- <f> | wc -l` · `git log --format=%s -- <f> | sed -E 's/^[a-z]+\(([^)]+)\).*/\1/' | sed -E 's/-.*//' | grep -E '^[0-9]+(\.[0-9]+)?$' | sort -u | wc -l` · `wc -l <f>`.
⚠ Six-digit buckets (`260814`, `260918`) are DATED QUICK TASKS, not phases — drop them.

**ADD a scan-list row AND its own section, in the SAME COMMIT, for each of these three:**

1. `backend/app/services/circuit_breaker.py` — **touched by plan 256-01 (wave 1, this same wave).**
   Measured `1 / 1 / 331` at `edf2d1024`, so G-5 does NOT fire; the row is added **at the touch**
   by the `settingsSearchPayload.ts` / `LibraryCloudImport.tsx` precedent — an absent row is
   invisible to G-5 at ANY count. Verdict cell: honoured by construction (256-01) — `absorb_usage_box`
   returns the delta it already recorded; branches 1→1, methods 9→9, new state 0. Its section names
   the binding invariant: ⛔ the `max(0, …)` clamp must stay ON THE RETURNED delta, or a reset box
   hands the DB a negative and SUBTRACTS real spend.
2. `backend/app/services/task_service.py` — ⛔ **FIRES at 10 phases and is absent from BOTH
   registers for its ENTIRE LIFE** (`19 / 10 / 958` measured; **not named by D-256-13** — found by
   running the gate rather than reading the table, RESEARCH.md C-4). This is `config.py`'s failure
   repeating for at least the fourth time. ⚠ **No plan in this phase MODIFIES it** — it is the file
   METER-06 mirrors FROM (`:414-430`, the canonical two-arm usage reader) — so the gate will not
   demand it. **Add it anyway, and say in the section that the row was added while the file was
   read-only**, because an absent row is invisible at any count.
3. `backend/app/services/run_reconciler.py` — no row, `3 / 2 / 325`, not firing. **Not modified by
   this phase and deliberately so** (D-256-08 site #7 is REGISTERED, not fixed). The section states
   the R-1 defect chain measured here (`main.py:434-436` → `include_cap_paused` default `True` →
   `finalize_run`'s unconditional `SET input_tokens = $6` NULLs real totals) and points at
   `SEED-297`.

**CORRECT the stale rows** for every file this phase touches or measured, recording the corrected
triple BESIDE the original rather than overwriting the original's verdict (S-5, the project's
standing rule): `backend/app/services/harness/publish_service.py` (row said `25`, measured `26`
commits), `backend/app/services/run_producer.py` (row says `3 / 2 / 693` and verdict **"below"** —
measured `5 / 3 / 749`, i.e. **AT the threshold**, so the verdict itself is now wrong;
⚠ **not modified by this phase** — it is the analog to COPY), and
`backend/app/services/run_lifecycle.py` (row says `6 / 3 / 459`, measured `8 / 4 / 748`;
⛔ **not modified and must NOT be copied** — RESEARCH.md C-5).

⛔ **`backend/app/services/forced_emit.py` IS NOT IN THIS PLAN.** Its row is owed in the SAME COMMIT
as that file's first edit (O-6), which is plan **256-04**. Adding it here would satisfy the gate and
break the rule. ⚠ Note that in this plan's SUMMARY so the omission reads as deliberate.

⛔ **DISPOSITION CELL CAP: 200 characters.** Verdict in the cell, every reason in the file's own
section in `docs/HOT-FILE-LEDGER.md`. `scripts/check-claude-md-size.cjs` FAILS
`[disposition-too-long]`, `[duplicate-row]` and `[malformed-row]`, and it runs on a PostToolUse hook
— it will fire in the turn you author the prose, not eight days later.

Run `node scripts/check-claude-md-size.cjs` because CLAUDE.md is edited. ⚠ The hard limit is
**150,000 characters** and the warn band is **120,000**; it has already tripped twice.
  </action>

  <acceptance_criteria>
    - `node scripts/check-hot-file-ledger.cjs 256` → exit `0`, AND its printed **`watched: N`** line
      shows N > 0. ⛔ Never read a bare exit 0 — 255's vacuous pass was `watched: 0` with exit 0.
      ⚠ It will still report `[no-row] backend/app/services/forced_emit.py` until plan 256-04 lands;
      record that reading and the reason (O-6), do not fix it here.
    - `node scripts/check-claude-md-size.cjs` → exit `0`. Record the character count it prints.
    - For each of the three added files: `grep -c "<path>" CLAUDE.md` → at least `1` AND
      `grep -c "<path>" docs/HOT-FILE-LEDGER.md` → at least `2` (scan-list row + its own section
      heading). ⛔ A row without a section, or a section without a row, is drift.
    - Every disposition cell added or edited is `<= 200` characters (the gate asserts it; record that
      it was RUN).
    - The SUMMARY carries a table of every re-derived triple with the command output, and marks each
      as CONFIRMED or CORRECTED against what CONTEXT.md / RESEARCH.md claimed.
    - `git diff --stat CLAUDE.md docs/HOT-FILE-LEDGER.md` shows both files changed in the SAME
      commit.
  </acceptance_criteria>

  <verify>
    <automated>node scripts/check-hot-file-ledger.cjs 256; node scripts/check-claude-md-size.cjs</automated>
  </verify>

  <done>`circuit_breaker.py`, `task_service.py` and `run_reconciler.py` each have a scan-list row and
  their own section, in one commit; every stale triple this phase measured is corrected beside its
  original; `forced_emit.py` is deliberately absent with the reason recorded; and both the ledger
  gate and the CLAUDE.md size gate were RUN with their output recorded.</done>
</task>

<task type="auto">
  <name>Task 3: The register — five entries, and R-1 dispositioned explicitly</name>
  <files>.planning/seeds/SEED-297-boot-reconciler-nulls-cap-paused-token-totals.md, .planning/seeds/SEED-298-max-tokens-per-run-is-really-per-segment.md, .planning/seeds/SEED-299-stranded-deep-chat-run-token-count-unknowable.md, .planning/seeds/SEED-300-three-token-holes-surviving-phase-256.md, .planning/seeds/SEED-074-workflow-harness-token-usage-rollup.md</files>

  <read_first>
    - `.planning/seeds/TEMPLATE.md` — the FULL frontmatter contract. ⛔ `status` is an enum synced
      across THREE homes; `trigger_surfaces` is a **closed 15-word vocabulary**
      (`admin auth chat connectors deployment harness ingestion library panel provider retrieval
      sandbox settings skills workflow`) and a word outside it matches nothing, forever;
      `trigger_paths` is the LOAD-BEARING field — prose without paths is still UNSWEPT.
    - `256-RESEARCH.md` §"Register Entries Owed" (lines 889-1041) — R-1..R-5 drafted in full,
      including the exact `trigger_when` prose and `trigger_paths` for each.
    - `.planning/seeds/SEED-074-workflow-harness-token-usage-rollup.md` — read it in full; R-5
      flips its status and records its THREE measurably stale body claims.
    - `CLAUDE.md` §"Seeds register cross-check (MANDATORY)".
  </read_first>

  <action>
**Allocate ids by `max(id)+1`, re-derived at execution time — do not trust the numbers below.**
Measured at `edf2d1024` the register's highest is **SEED-296**, so the expected allocation is
**297, 298, 299, 300**; if another agent has planted meanwhile, shift and record the actual ids in
the SUMMARY (and rename the plan's `files_modified` entries accordingly). The register parsed
**303/303** at base, so a hand-picked number is a duplicate waiting to happen.

**R-1 → SEED-297. ⛔ DISPOSITION: REGISTER, NOT FIX. Decide it out loud, because an unnamed hole is
exactly what this phase exists to stop.** The measured chain: `run_producer.py:249-257` finalizes a
`cap_paused` run with REAL totals via `finalize_run`; `db/runs.py:104-122` `finalize_run` does an
**unconditional** `SET input_tokens = $6`; `run_reconciler.py:101` has `include_cap_paused: bool =
True` as its DEFAULT; `main.py:434-436`'s BOOT sweep passes nothing, i.e. `True` (the periodic sweep
at `:490` correctly passes `False`); `run_reconciler.py:236-247` then calls
`finalize_run_terminal(..., input_tokens=None, ...)`. ⇒ **a `cap_paused` chat run carrying real
totals loses them at the next backend restart** — literally *"a run loses its token count"*, the
sentence this phase is named for.
The reason for REGISTER over FIX, recorded in the seed body: the 2-line fix
(`input_tokens = COALESCE($6, input_tokens)`) changes a SHIPPED writer with multiple callers and
would silently make a genuine `None`-after-a-value un-writable — a semantic change, not a patch,
and inconsistent with D-256-05's posture on `finish_run`.
⭐ **SIZE IT FIRST with a free read.** `⛔ Supabase MCP READS need NO approval and are free; every
WRITE needs explicit per-action operator approval.* Run RESEARCH.md's U-3 query as a READ:
`SELECT count(*) FROM runs WHERE status='failed' AND error LIKE 'failed: orphaned%' AND input_tokens IS NULL;`
and put the number in the seed. ⛔ Run NO write, no DDL, no `apply_migration`.

**R-2 → SEED-298. D-256-10: `max_tokens_per_run` is really `max_tokens_per_SEGMENT`. ⛔ NAME IT, DO
NOT FIX IT** — changing a shipped safety cap operators have tuned is a live behaviour change needing
its own phase and its own UAT. Record BOTH halves measured: (a) the box resets per `_resume_run`
(`harness_engine.py:1844`) so a run resumed five times can spend 5× its ceiling, and (b) ⭐ the
setting is **per-SCHEDULE and nothing else** — every writer is a `workflow_schedules` path and
**nothing anywhere assigns `ctx.max_tokens_per_run`**, so no interactive run can configure one at
all. Note that the fix (seeding the breaker from the persisted `workflow_runs.input_tokens` on
resume) is made possible **for the first time** by this phase.

**R-3 → SEED-299. D-256-08 site #7** (`run_reconciler.py:245`): a stranded Deep chat run's count is
genuinely unknowable — the producer process is gone and the in-memory box died with it, so its
`input_tokens=None` is HONEST. Record the nuance that makes it safe: `run_reconciler.py:217-247`
only touches **non-terminal** rows, so it can never overwrite a COMPLETED run's real total.

**R-4 → SEED-300.** Three holes in one entry: **D-256-12**'s deferred failed-rung breakdown
(`forced_emit` retry spend is counted but not broken out); the eval **WITHOUT arm**, which was PAID
FOR while `eval_runner_service.py:852-855` deliberately ignores its return (correct for a VERDICT
rollup, wrong for a SPEND rollup); and ⭐ the eval **JUDGE shot** (`:648 _judge_eval_answer`), a third
paid provider call measured NOWHERE — a hole this research found that no decision covers.

**R-5 → edit `.planning/seeds/SEED-074-workflow-harness-token-usage-rollup.md` IN PLACE.**
⛔ **A seed is answered by EDITING the seed; `status:` frontmatter IS the index** — prose in a body
saying "still open" is invisible to the scan. Set `status: partially-answered`, `partial: true`,
`folded_into: "256"`. ⛔ **NOT `answered`, NOT `shipped`**: the seed's step-4 **cross-provider parity
check** is NOT discharged here — nothing in this phase drives a live provider. Record its three
measurably stale body claims BESIDE the originals, never overwriting: the moved line numbers
(`harness_engine.py:1413-1421` → the producer-shell finalize is now `:3044-3053`); ⛔ the claim
*"no rollup of per-sub-agent usage into the producer/workflow run"* is **FALSE since Phase 093/204**
(`phase_types.py:768 _record_run_usage`); and the storage question is resolved as option (a),
columns on `workflow_runs` — the per-phase usage table is **NOT built**, so a future reader must not
read the seed's preference as a decision.
⛔ **`SEED-073` is untouched** — it is METER-01/02, Phase 257, and nothing here builds a rate table.

Finally, **re-run the seeds sweep now that PLAN.md files exist**:
`node scripts/check-seeds-register.cjs --phase 256`. ⚠ At discuss it reported **0 matched over
303/303 parsed** and **that zero was an ARTEFACT** — the phase declared no surfaces because no
PLAN.md existed, and the gate says so itself. Record the new figure and each matched seed's routing
(fold / defer / leave), writing the routing back into the matched seed's frontmatter.
⭐ **THE SWEEP WAS ALREADY RUN AT PLANNING TIME AND IT FIRES — the discuss-time `0 matched` is
confirmed an ARTEFACT.** Measured with all four PLAN.md files present: **`10 seed(s) matched` over
303/303 parsed, 0 duplicate ids, 303/303 carrying all 5 required keys, exit 0**; unswept, ⛔ never
summed: **134** carry no `trigger_when` at all · **114** carry prose a sweep cannot match. The
proposed routing — **re-run the gate and confirm the set before writing any of it back**, because a
plan's `files_modified` may have shifted:

| Seed | Matched on | Proposed routing |
|---|---|---|
| `SEED-266` (`full-schema.sql` carries no table ACLs; leaks `SET row_security = off`) | `**/full-schema.sql` | ⚠ **the only DDL-relevant hit.** LEAVE OPEN — plan 256-01 regenerates that artifact but adds NO privilege: `workflow_runs` has zero explicit GRANT/REVOKE anywhere, so 182 owes the supplement nothing (proven four ways in RESEARCH.md). Record the overlap in the seed's body as a touch that did NOT change its finding. |
| `SEED-291` (the Extension Contract) | `backend/app/services/harness/phase_types.py` | LEAVE `partially-answered` — it fires as a **CONSTRAINT**, and plan 256-04 honours it: one call to an existing module-level function, `check-extension-contract.cjs` run and green. Record that it fired and was honoured, not folded. |
| `SEED-177` (MCP connections both ways) · `SEED-188` (anti-prompt-injection verified by nobody) · `SEED-198` (Experts) | broad `backend/app/**` globs | LEAVE OPEN — glob matches, not surface overlap. None has a metering surface; `SEED-198` is Phase 259's. |
| `SEED-292` (assurance export) · `SEED-296` (evaluation has no front door) | `backend/app/services/eval_runner_service.py` | LEAVE OPEN — this phase adds a token rollup to that file; it builds no export and no UI. |
| `SEED-295` (named outcomes / forward-only edges) | `harness_engine.py`, `publish_service.py` | LEAVE OPEN — and it says of itself that it is ⛔ UNJUSTIFIED until a real workflow is recorded as blocked. |
| `SEED-284` (three elapsed formatters) · `SEED-290` (Phase 253's 13 unfixed findings) | `docs/HOT-FILE-LEDGER.md`, `supabase/full-schema.sql` | LEAVE OPEN — artifact-path matches with no bearing on token accounting. |

⛔ **Write the routing into each matched seed's frontmatter**, per CLAUDE.md's touchpoint rule — a
`trigger_when` that fired and was never answered is a deferral with no re-open, which is a deletion
that looks like a decision. ⛔ **Do not flip any of these to `folded`**: none is claimed by this phase.

⚠ Report the gate's TWO unswept figures separately and ⛔ **never sum them** (`N carry no
`trigger_when` at all` · `M carry prose a sweep cannot match`).
  </action>

  <acceptance_criteria>
    - Four new seed files exist under `.planning/seeds/` and
      `node scripts/check-seeds-register.cjs` → exit `0` with `0 duplicate ids` and no
      `[unknown-status]`, `[missing-key]` or `[unknown-surface]` code against any of them.
    - For each new seed: `trigger_when` is NOT `unset`; `trigger_paths` is NON-EMPTY; every member
      of `trigger_surfaces` is in the 15-word vocabulary; `seed_id` matches the filename.
    - `grep -c "^status: partially-answered" .planning/seeds/SEED-074-workflow-harness-token-usage-rollup.md` → `1`
      and `grep -c "^partial: true" ...` → `1` and `grep -c 'folded_into: "256"' ...` → `1`.
    - `grep -c "status" .planning/seeds/SEED-073-model-cost-rate-registry-token-to-usd.md` — file
      UNCHANGED: `git diff --numstat .planning/seeds/SEED-073*` produces NO output.
    - `node scripts/check-seeds-register.cjs --phase 256` was RUN and its `N matched` figure plus
      both unswept figures are recorded in the SUMMARY, ⛔ unsummed.
    - The Supabase MCP U-3 read was executed and its count appears in SEED-297. ⛔ The SUMMARY states
      that NO write, DDL or `apply_migration` was issued.
    - SEED-297's body contains the words "REGISTER, not fix" (or equivalent explicit disposition)
      together with the reason — ⛔ not merely the defect.
  </acceptance_criteria>

  <verify>
    <automated>node scripts/check-seeds-register.cjs &amp;&amp; node scripts/check-seeds-register.cjs --phase 256</automated>
  </verify>

  <done>Four new seeds pass the register gate with concrete `trigger_when`, non-empty
  `trigger_paths` and enum-legal `trigger_surfaces`; SEED-074 reads `partially-answered` / `partial:
  true` / `folded_into: "256"` with its three stale claims recorded beside the originals; SEED-073
  is byte-unchanged; R-1's disposition is REGISTER with the reason and a live size figure; and the
  phase-scoped sweep has been re-run now that PLAN.md files exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future reader → the read rule | The only thing standing between D-256-01's inclusive-parent design and a silent double count in Phase 257 is Fence 1. A vacuous fence here IS the vulnerability. |
| planner/executor → `backend/app/db/runs.py` | A deliberately planted SQL violation lives in a real source file for the duration of the RED drive. |
| Supabase MCP → PRODUCTION | `.mcp.json` points at the production project and is NO LONGER pinned `read_only`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-256-09 | Tampering | the planted un-narrowed SUM in `backend/app/db/runs.py` | mitigate | ⛔ **md5 before / after plant / after removal**, digest #1 == digest #3, plus `git diff --numstat -- backend/app/db/runs.py` empty. ⚠ The Phase 255 `emitters.py` incident (a planted `eval()` sat live) is why the proof is mechanical, never timing and never "I reverted it". |
| T-256-10 | Repudiation | a VACUOUS Fence 1 | mitigate | Three live controls: matcher-fires-on-haystack, files-parsed `>= 150`, missing-root-is-a-violation. ⛔ The subject set is measurably EMPTY today, so a fence with no control would read green forever — this project has recorded a green fence coexisting with the defect it named, twice. |
| T-256-11 | Information disclosure | Supabase MCP against PRODUCTION for the U-3 sizing read | mitigate | ⛔ **READS are free and need no approval; EVERY WRITE needs explicit per-action operator approval.** This task issues ONE `SELECT count(*)`. No DDL, no DML, no `apply_migration`, no settings change. The SUMMARY records that fact affirmatively. |
| T-256-12 | Repudiation | a seed that cannot be swept | mitigate | `trigger_paths` non-empty and `trigger_surfaces` inside the closed 15-word enum, asserted per seed. ⛔ A word outside the enum matches nothing forever; prose without paths is UNSWEPT, which is a deferral with no re-open, which is a deletion that looks like a decision. |
| T-256-13 | Denial of service | CLAUDE.md crossing its 150,000-CHARACTER gate | mitigate | `node scripts/check-claude-md-size.cjs` is RUN in Task 2 (not `wc -c` — they disagree), disposition cells capped at 200 chars, reasons pushed into `docs/HOT-FILE-LEDGER.md`. ⚠ Above the limit **every instruction in CLAUDE.md silently stops applying, to every agent, everywhere**. |
| T-256-SC | Tampering | npm/pip/cargo installs | mitigate | ⭐ No package is installed by this plan. `git diff --numstat backend/requirements.txt frontend/package.json` is EMPTY. |
</threat_model>

<verification>
1. `cd backend && venv/Scripts/python -m pytest tests/unit/test_256_token_sum_narrowing.py -q` → green,
   with the RED evidence and all three md5 digests recorded.
2. ⛔ **THE BACKEND BASELINE, ONCE, SET-DIFFED** (D-256-15, zero headroom): full
   `pytest tests/unit -q --continue-on-collection-errors`, `grep "^FAILED"`, `comm` both directions
   against `256-BASELINE-backend-failing-set.txt`. ⛔ Count with `grep -c`, never `| tail`.
3. `node scripts/check-hot-file-ledger.cjs 256` — read `watched: N`, not the bare exit.
4. `node scripts/check-claude-md-size.cjs` — record the character count.
5. `node scripts/check-seeds-register.cjs` and `... --phase 256` — record `N matched` and BOTH
   unswept figures, ⛔ unsummed.
6. `node scripts/check-extension-contract.cjs` — this plan modifies no trigger file; must stay green.
7. **Frontend: SKIPPED, deliberately.** Zero files under `frontend/` are modified, so
   `vitest-count-gate.cjs` has no subject. ⛔ Do not run it and do not quote a figure from it
   (D-256-14: the gate is NON-DETERMINISTIC at base — `failed 3` then `failed 0` on a byte-identical
   tree at cap 2, so neither reading would mean anything here).
</verification>

<success_criteria>
- Fence 1 is a real `ast.parse` CONJUNCTION fence with three live controls, OBSERVED RED against a
  planted un-narrowed SUM that named its file and line, and GREEN after an md5-proved removal
  (D-256-02).
- The fence is green over `persist_run_usage` (the negative control that protects plan 256-01's
  merge) and over the four existing narrowed identity joins (RESEARCH.md C-3).
- `circuit_breaker.py`, `task_service.py` and `run_reconciler.py` each have a scan-list row AND
  their own section, added in the same commit, every triple RE-DERIVED; `forced_emit.py` is
  deliberately left to plan 256-04 (O-6) with the omission recorded.
- Four seeds (expected SEED-297..300) pass the register gate; SEED-074 reads
  `partially-answered` / `partial: true` / `folded_into: "256"` with its three stale claims recorded
  beside the originals; SEED-073 byte-unchanged.
- R-1 is explicitly REGISTERED with its reason and a live size figure from a **read-only** Supabase
  MCP query; D-256-10 is NAMED and NOT FIXED.
- The backend failing SET is byte-identical to the committed 71 names in both `comm` directions.
</success_criteria>

<output>
Create `.planning/phases/256-every-token-is-counted-and-kept/256-02-SUMMARY.md` when done.

It MUST contain: the three md5 digests with #1 == #3; the fence's RED output line verbatim; the
re-derived triple table marked CONFIRMED or CORRECTED per file; the CLAUDE.md character count; the
actual seed ids allocated; the `--phase 256` sweep figure with both unswept figures unsummed; the
U-3 read's count and an affirmative statement that no MCP write was issued; and the explicit note
that `forced_emit.py`'s row was withheld for plan 256-04 per O-6.
</output>
