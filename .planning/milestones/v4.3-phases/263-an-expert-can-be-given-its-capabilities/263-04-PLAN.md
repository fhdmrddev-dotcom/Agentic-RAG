---
phase: 263-an-expert-can-be-given-its-capabilities
plan: 04
type: execute
wave: 4
depends_on: ["263-01", "263-02", "263-03"]
autonomous: false
requirements: [PACK-14, PACK-15, PACK-16]
files_modified:
  - frontend/src/components/skills/SkillFormDialog.tsx
  - frontend/src/components/skills/SkillFormDialog.test.tsx
  - frontend/src/components/experts/ProposedSkillCard.tsx
  - frontend/src/components/experts/ExpertAuthoringStudio.tsx
  - frontend/src/components/experts/__tests__/ExpertAuthoringStudio.test.tsx
  - frontend/src/components/experts/__tests__/OrgExpertsTab.test.tsx
  - scripts/vitest-count-gate.cjs
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md

must_haves:
  truths:
    - "After drafting, the studio shows the domain skills that do NOT exist, named and described, in a group visibly distinct from the library pills — dashed and violet, never red (PACK-14, D-263-01)."
    - "Pressing 'Create this skill →' drafts the body in place on the card, opens the EXISTING SkillFormDialog pre-filled, and creates the row through the EXISTING POST /skills on human approval (PACK-15, D-263-03/04/05)."
    - "Save Expert is disabled while any named capability does not exist, including a name typed into the quick-add box (PACK-16, D-263-09)."
    - "A save that gets past the client anyway renders the server's own unknown_skills, not a re-derived list (D-263-10)."
    - "Every skills suite this phase touches is in BOTH count-gate knobs — before this plan, `grep -ic skill scripts/vitest-count-gate.cjs` was 0 and 8 passing tests ran nowhere."
  artifacts:
    - path: "frontend/src/components/experts/ProposedSkillCard.tsx"
      provides: "the dashed ⬡ proposal card with its generating state"
      min_lines: 60
    - path: "frontend/src/components/skills/SkillFormDialog.tsx"
      provides: "one additive initialValues prop, consumed inside the existing reset effect"
      contains: "initialValues"
    - path: "scripts/vitest-count-gate.cjs"
      provides: "SkillFormDialog.test.tsx adopted into BOTH knobs"
      contains: "SkillFormDialog.test.tsx"
  key_links:
    - from: "frontend/src/components/experts/ExpertAuthoringStudio.tsx"
      to: "frontend/src/components/skills/SkillFormDialog.tsx"
      via: "the modal's FIRST production mount, pre-filled through initialValues"
      pattern: "SkillFormDialog"
    - from: "frontend/src/components/experts/ExpertAuthoringStudio.tsx"
      to: "createSkill in @/lib/api/skills"
      via: "the dialog's onSave — the existing D-263-03 write path, unchanged"
      pattern: "createSkill"
    - from: "frontend/src/components/experts/ExpertAuthoringStudio.tsx"
      to: "draftSkillBody in @/lib/api/experts"
      via: "the in-place generating state on the proposal card"
      pattern: "draftSkillBody"
---

<objective>
Ship sketch 263 variant A: the shipped `Bound Knowledge & Capabilities` card splits into two headed
groups, a proposal becomes real one human approval at a time through the dialog that already exists,
and Save is held while the blueprint claims something the library does not have.

Purpose: PACK-14's visible half, PACK-15's approval moment, PACK-16's client fence.
Output: one new render-only component, one additive dialog prop, the studio split, and the count-gate
adoption without which every test this plan writes would falsify nothing.

⛔ **G-2 ACCEPTANCE BAR:** `.planning/sketches/263-the-skills-an-expert-needs/README.md`, variant A,
operator-ratified 2026-09-21. There is deliberately NO UI-SPEC.md — the README plus
`index.html` ARE the design contract. ⛔ No new studio screen and no wizard step: B (a provisioning
step) and C (a refusal sheet) were the REJECTED alternatives and are preserved in the sketch, not
deleted.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/sketches/263-the-skills-an-expert-needs/README.md
@.planning/phases/263-an-expert-can-be-given-its-capabilities/263-CONTEXT.md
@.planning/phases/263-an-expert-can-be-given-its-capabilities/263-RESEARCH.md
@.planning/phases/263-an-expert-can-be-given-its-capabilities/263-PATTERNS.md
@.planning/phases/263-an-expert-can-be-given-its-capabilities/263-03-SUMMARY.md

<interfaces>
CONSUMED from 263-03 (`frontend/src/lib/api/experts.ts`) — no exploration needed:

  export interface SuggestedNewSkill { name: string; description: string; why_needed: string }
  // ExpertDraftOutput now carries: suggested_new_skills: SuggestedNewSkill[]
  export interface AuthoredSkillBody { instructions: string; summary: string }
  export class ExpertMemberSkillsUnknownError extends Error { readonly unknownSkills: string[] }
  export class SkillBodyDisabledError extends Error {}
  export async function draftSkillBody(payload: {
    skill_name: string; skill_description: string; why_needed: string;
    expert_name: string; expert_description: string;
  }): Promise<AuthoredSkillBody>

EXISTING, unchanged (`frontend/src/lib/api/skills.ts:22-31` — D-263-03, this file stays byte-identical):
  export async function createSkill(body: SkillCreate): Promise<Skill>

EXISTING (`frontend/src/types/index.ts:748-754`) — already sufficient, ⛔ no change:
  SkillCreate { name; description?; instructions?; is_org_shared? }

CREATED here:
  frontend/src/components/skills/SkillFormDialog.tsx
    interface Props { ...existing...; initialValues?: { name?: string; description?: string; instructions?: string } }
  frontend/src/components/experts/ProposedSkillCard.tsx
    interface ProposedSkillCardProps {
      proposal: SuggestedNewSkill
      isGenerating?: boolean
      onCreate: (p: SuggestedNewSkill) => void
      onRemove: (name: string) => void
    }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pre-fill the existing dialog, and ADOPT its suite into both count-gate knobs</name>
  <files>frontend/src/components/skills/SkillFormDialog.tsx, frontend/src/components/skills/SkillFormDialog.test.tsx, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - frontend/src/components/skills/SkillFormDialog.tsx lines 33-57 (`SkillFormProps` — the shared `SkillForm`'s FLAT CONTROLLED props), 271-285 (the `Props` contract), 287-288 (`const isEdit = !!skill`), 304-322 (the reset effect, and the `listSkillFiles(skill.id)` call inside it), 437 (`SkillDetailPanel`, the OTHER export)
    - frontend/src/pages/SkillsPage.tsx line 8 and lines 76-86 — ⚠ production imports ONLY `SkillDetailPanel`; `handleSave` is the `createSkill` / `updateSkill` shape the studio's handler mirrors
    - frontend/src/components/skills/SkillFormDialog.test.tsx lines 19-30 (the `@/lib/supabase` mock every dialog-mounting suite needs) and 38-45 (the `importActual` spread factory — Shape B, immune to the closed-factory trap)
    - scripts/vitest-count-gate.cjs lines 165-170 (BASELINE, keyed by BASENAME) and 4326-4331 (TARGETS, keyed by FULL PATH) — and the comment block at ~4333-4339 explaining why both
    - .planning/phases/263-an-expert-can-be-given-its-capabilities/263-RESEARCH.md §G.2, §G.4; 263-PATTERNS.md §9, T-6, F-5, F-7
  </read_first>
  <behavior>
    - Rendering `SkillFormDialog` with `open` and `initialValues={{ name, description, instructions }}` and NO `skill` pre-fills all three fields.
    - In that state the dialog is in CREATE mode: its title is the create wording and `listSkillFiles` is never called.
    - Rendering with a real `skill` and an `initialValues` also supplied uses the `skill`'s values — `skill` wins.
    - Rendering with neither is byte-identical in behaviour to today (all three fields empty).
    - Opening a SECOND proposal without unmounting shows the second one's values, not the first's.
  </behavior>
  <action>
**(a) One additive optional prop** on `SkillFormDialog`'s `Props`:
`initialValues?: { name?: string; description?: string; instructions?: string }`, with a comment
naming Phase 263 / D-263-01 / D-263-04 and stating it is only read when `skill` is ABSENT.

**(b) Three `??` insertions inside the EXISTING reset effect** (`:304-322`):
`setName(skill?.name ?? initialValues?.name ?? "")`,
`setDescription(skill?.description ?? initialValues?.description ?? "")`,
`setInstructions(skill?.instructions ?? initialValues?.instructions ?? "")`.
⛔ **Add `initialValues` to the effect's dependency array** — without it, a second proposal opened
without unmounting shows the first one's body. That is behaviour bullet 5 and it has its own test.

⛔ **DO NOT pass a synthetic object as `skill` to pre-fill the dialog.** `const isEdit = !!skill`
(`:288`) would flip to `true`, which (a) changes the title to edit wording, (b) fires
`listSkillFiles(skill.id)` against an id that does not exist, and (c) makes the save read as an
update. Not viable — the three `??` are the whole change.

⭐ `SkillForm`'s props are a flat CONTROLLED set, so the pre-fill lands entirely inside
`SkillFormDialog`'s reset effect: **`SkillForm` needs zero changes and `SkillDetailPanel` is
untouched.** Assert that with `git diff` rather than assuming it.

**(c) ⛔ ADOPT THE SUITE INTO BOTH COUNT-GATE KNOBS — this is a first-class deliverable, not
housekeeping.** `grep -ic "skill" scripts/vitest-count-gate.cjs` returns **0**: not one skills suite
is in either knob, and `SkillFormDialog.test.tsx`'s **8 passing tests run NOWHERE and guard
NOTHING**. Without adoption, every test this plan adds to that file falsifies nothing — the exact
Phase 260 F-2 shape, where a total that did not move after a phase added tests WAS the tell.
⚠ The knobs are ASYMMETRIC and this file is NOT under a `__tests__/` directory, so the pair is:
BASELINE gets the BASENAME key `"SkillFormDialog.test.tsx": <n>` near `:170` under a
`// ── Phase 263 (PACK-14/PACK-15) — the dialog an Expert proposal reuses ──` comment; TARGETS gets
the FULL PATH `"src/components/skills/SkillFormDialog.test.tsx"` near `:4331` under the same comment.
⚠ Set `<n>` to the MEASURED new total after adding this task's cases (8 at the phase base), in the
SAME commit. A raised pin is the gate working.

**(d) Tests in `frontend/src/components/skills/SkillFormDialog.test.tsx`** — one per behavior
bullet. Keep the file's existing Shape-B `importActual` factory; it is immune to the closed-factory
trap and must not be converted to a literal. For the "never calls `listSkillFiles`" case, assert on
the existing `listSkillFiles` spy's `toHaveBeenCalledTimes(0)`.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/skills/SkillFormDialog.test.tsx --maxWorkers=2</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/skills/SkillFormDialog.test.tsx` passes with `>= 13` tests (8 at the phase base + at least 5 new).
    - `grep -c "SkillFormDialog.test.tsx" scripts/vitest-count-gate.cjs` returns `2` — one BASELINE basename key, one TARGETS full path.
    - The BASELINE number equals the measured test count from the run above (quote both in the SUMMARY).
    - `git diff -- frontend/src/components/skills/SkillFormDialog.tsx` touches ONLY the `Props` interface, the three `setX(...)` lines and the effect dependency array — `grep -c "SkillDetailPanel" ` output and the `SkillForm` function body are unchanged (`git diff` shows no hunk inside either).
    - `grep -c "initialValues" frontend/src/components/skills/SkillFormDialog.tsx` returns `>= 5` (prop declaration + 3 reads + dependency array).
    - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo ROOT reports `SkillFormDialog.test.tsx` as present and the total INCREASED. ⛔ Criterion is: no per-file DECREASE, zero failing — never a fixed grand total.
  </acceptance_criteria>
  <done>The dialog pre-fills without becoming an edit dialog, and its suite finally runs inside a gate that can see it change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The variant-A split — the ⬡ proposal group, the held Save, and the approval round trip</name>
  <files>frontend/src/components/experts/ProposedSkillCard.tsx, frontend/src/components/experts/ExpertAuthoringStudio.tsx, frontend/src/components/experts/__tests__/ExpertAuthoringStudio.test.tsx, frontend/src/components/experts/__tests__/OrgExpertsTab.test.tsx, scripts/vitest-count-gate.cjs, docs/HOT-FILE-LEDGER.md, CLAUDE.md</files>
  <read_first>
    - .planning/sketches/263-the-skills-an-expert-needs/README.md — the whole file; variant A and its "What to Look For" list are the acceptance bar
    - .planning/sketches/263-the-skills-an-expert-needs/index.html lines 445-476 (variant A's two headed groups and the save bar), 890-935 (the proposal card render and the banner text)
    - .claude/skills/sketch-findings-agentic-rag/SKILL.md — the design-system section covering the authoring studio and the skills surfaces; read before drawing any new mark
    - frontend/src/components/experts/ExpertAuthoringStudio.tsx lines 211-241 (the guarded draft-apply idiom AND `isDrafting`, the existing in-place busy pattern), 245-252 (`handleAddCustomSkill` — ⛔ pushes ANY free-text string into `memberSkills` with no library check; this is how phantom names enter today), 701-706 (the card heading `Bound Knowledge & Capabilities`), 744-834 (the skills sub-block: 756-774 active pills, 777-799 quick-add, 802-834 available picker), 1033-1038 (the destructive save-error banner), 359-361 (`setSaveError(err.message)`)
    - frontend/src/components/org/OrgIdentity.tsx lines 66-69 — `AVATAR_PENDING` = `border border-dashed border-primary/40 bg-primary/[0.06] text-primary`. ⭐ D-263-01's "dashed + violet, never red" ALREADY EXISTS in this design system's tokens. Copy the string; do not invent one.
    - frontend/src/components/org/OrgIdentity.test.tsx lines 100-108 — the class-token assertion WITH a negative arm, the shape that makes "proposed ≠ real" falsifiable
    - frontend/src/components/experts/__tests__/ExpertAuthoringStudio.test.tsx lines 1-33 and OrgExpertsTab.test.tsx — ⛔ BOTH use CLOSED `vi.mock` factories for `@/lib/api/experts` AND `@/lib/api/skills`
    - .planning/phases/263-an-expert-can-be-given-its-capabilities/263-PATTERNS.md §7, §8, T-5, F-4, F-8
  </read_first>
  <behavior>
    - A draft returning `suggested_new_skills` renders one `ProposedSkillCard` per entry, each showing the name and its one-line description, under a heading `Proposed for this Expert`.
    - The proposal group's container carries `border-dashed`, `border-primary/40`, `bg-primary/[0.06]`, `text-primary` and carries NEITHER `bg-gradient-to-br` NOR `border-destructive` NOR `text-destructive`.
    - The library group keeps the existing solid `⚡` pill rail, unchanged, under a heading `In your library`.
    - With one or more proposals open, `Save Expert` is DISABLED and a violet (not destructive) banner names the count.
    - With a phantom name typed into the quick-add box and no proposals open, `Save Expert` is STILL disabled — the fence covers that entry point too.
    - With zero proposals and every `memberSkills` entry present in `availableSkills`, `Save Expert` is enabled.
    - Clicking `Create this skill →` puts THAT card into its generating state in place (no dialog yet), calls `draftSkillBody`, then opens `SkillFormDialog` with the returned `instructions` pre-filled.
    - `SkillBodyDisabledError` from `draftSkillBody` opens the dialog anyway with EMPTY instructions and a note that drafting is off — manual creation is never blocked.
    - Confirming the dialog calls `createSkill`, and on success the name moves out of the proposal group into the `⚡` library rail and the counters change.
    - `Remove` drops a proposal without creating anything.
    - A save rejected with `ExpertMemberSkillsUnknownError` renders the SERVER's `unknownSkills` in the destructive banner.
  </behavior>
  <action>
**(a) `frontend/src/components/experts/ProposedSkillCard.tsx`** — render-only, no data fetching.
`data-testid="proposed-skill-card"` on the root, a `⬡` glyph, the name in the mono/`text-xs` scale,
the one-line description beneath, and two actions: a ghost `Remove` and a violet
`Create this skill →`. Class tokens on the root copied verbatim from `OrgIdentity.tsx:69`'s
`AVATAR_PENDING`: `border border-dashed border-primary/40 bg-primary/[0.06] text-primary`.
⛔ **No red, no error styling, no `border-destructive`** — D-263-01: a proposal is an opportunity,
not a failure, and the dashed border plus the violet accent are the ONLY things carrying "does not
exist". Match the group wrapper geometry of the solid rail (`rounded-lg border … p-2`, `gap-1.5`,
`text-xs`) so the two headed groups read as peers; the ⬡ item is a `div` block rather than a pill
because it carries a description line.
When `isGenerating`, disable `Create this skill →` and render the busy state IN PLACE on the card
(e.g. "Drafting instructions…"). ⛔ **Do not open an empty dialog and fill it, and do not add a
separate "Generate instructions" button** — both are explicitly forbidden by CONTEXT's Claude's-
discretion note.

**(b) The split in `ExpertAuthoringStudio.tsx`.** The card at `:701-874` holds THREE peer
sub-blocks — Folders, Skills, Connections — and ⛔ **only the middle one (`:744-834`) changes.**
Insert the `Proposed for this Expert` group as a FOURTH inner part of the skills sub-block, between
the close of the active-pills block (`:774`) and the quick-add input (`:776`). It replaces nothing
and touches neither peer. Headings and counts copied from the sketch: `In your library` with
`· {n} skills this Expert can actually use`; `Proposed for this Expert` with
`· {n} named by the draft, none in your library yet` and the right-aligned legend `⬡ = does not exist`.

**(c) Draft-apply.** Add `suggestedNewSkills` state and the single guarded line
`if (draft.suggested_new_skills) setSuggestedNewSkills(draft.suggested_new_skills)` beside the
existing `member_skills` line at `:230`, in the same one-`if`-per-optional-field idiom. ⛔ No
destructuring, no defaults, no client-side derivation — the server already said which list each name
belongs to.

**(d) The client fence (PACK-16's advisory half).** Compute
`const unresolvedCapabilities = [...suggestedNewSkills.map(p => p.name), ...memberSkills.filter(n => !availableSkills.some(s => s.name === n))]`
and disable `Save Expert` when it is non-empty. ⛔ **The second half is not optional:**
`handleAddCustomSkill` (`:245-252`) pushes any free-text string into `memberSkills` with no library
check, and that is a live phantom-name entry point today. A fence that only watched the draft path
would leave it open.
Render the count banner in the VIOLET/primary family (`border-primary/30 bg-primary/5 text-primary`),
⛔ **not** the destructive tokens at `:1033-1038` — a proposal is not an error. Banner wording from
the sketch: "{n} capabilities are not real yet. Saved as-is, they would be stripped at run time by
the member check and this Expert would run without them — silently. Create or remove each one first."
When the list is empty, the clear state reads "Every capability in this blueprint exists."
⚠ The destructive banner STAYS, for the server's 422 arriving after a stale-client save.

**(e) The approval round trip.** `onCreate(proposal)` → set that proposal's card generating → `await
draftSkillBody({ skill_name: proposal.name, skill_description: proposal.description, why_needed:
proposal.why_needed, expert_name: name, expert_description: description })` → open `SkillFormDialog`
with `initialValues={{ name, description, instructions }}`. Catch `SkillBodyDisabledError` and open
the dialog with empty instructions plus an inline note — ⛔ **only GENERATION is gated (D-263-14);
manual creation must never be blocked.** Any other failure clears the generating state and surfaces a
real message — ⛔ never a fabricated body.
The dialog's `onSave` calls `createSkill(body as SkillCreate)` and RETURNS the created `Skill` so the
dialog can surface its `lint_warnings` (the `SkillsPage.tsx:76-86` shape, `createSkill` arm only —
a proposal never has a `selectedSkill`). ⛔ **`createSkill` is the existing, unchanged D-263-03 write
path** — `frontend/src/lib/api/skills.ts` must end this plan byte-identical.
On success (D-263-05 — the row is written IMMEDIATELY on approve): push the name into `memberSkills`,
drop it from `suggestedNewSkills`, refresh `availableSkills`, close the dialog.

**(f) The server's refusal.** In the save handler's catch, branch on
`err instanceof ExpertMemberSkillsUnknownError` and render `err.unknownSkills` in the destructive
banner. ⛔ Render the SERVER's names, never a re-derived list — that is the whole point of D-263-10.
Render them as React text children; ⛔ no `dangerouslySetInnerHTML`.

**(g) ⛔ MOCK-FACTORY BUILD OBLIGATION — name it now, do not discover it at merge.** Both
`ExpertAuthoringStudio.test.tsx` AND `OrgExpertsTab.test.tsx` use CLOSED `vi.mock` factories (a
literal return, no `importActual`) for `@/lib/api/experts` **and** `@/lib/api/skills`. Any export the
studio newly imports is `undefined` at mount and the WHOLE suite reds — the Phase-196 `failed 249`
shape. Add keys for `draftSkillBody` and `ExpertMemberSkillsUnknownError` / `SkillBodyDisabledError`
to the experts factory, and `createSkill` (plus anything `SkillFormDialog` pulls, e.g.
`listSkillFiles`) to the skills factory, in **BOTH** files, in the SAME commit. ⚠ `OrgExpertsTab.test.tsx`
does not render the studio directly and reds anyway if it mounts anything importing the new symbol.
⭐ Prefer Shape B (`importActual` spread) for any NEW mock you add; converting the two existing closed
factories is optional and out of scope.

**(h) Raise the pin.** Adding cases to `ExpertAuthoringStudio.test.tsx` raises its BASELINE number
(4 at the phase base; the experts directory measured 9 = 5 + 4). Update
`scripts/vitest-count-gate.cjs`'s BASELINE entry to the measured new count in the SAME commit.
`OrgExpertsTab.test.tsx`'s pin changes only if its case count changes.

**(i) Ledger row AT CREATION for the new component**, plus re-derived triples for the modified ones.
`frontend/src/components/experts/ProposedSkillCard.tsx` is under `frontend/src/`, so
`node scripts/check-hot-file-ledger.cjs 263` FAILS `[no-row]` until it has one — add the row to
CLAUDE.md's scan table and its section to `docs/HOT-FILE-LEDGER.md` in the SAME commit that creates
the file, triple `1 / 1 / <wc -l>`, disposition (≤200 chars): "young (created 263). Row added AT
CREATION. ⛔ The ONE home of the 'does not exist' mark — dashed+violet, never red (D-263-01); a
second styling makes the proposal read as an error." Re-derive the triples for
`ExpertAuthoringStudio.tsx` (**2 / 1 / 1194** at planning, row STALE at `1 / 1 / 1082`) and
`SkillFormDialog.tsx` (**12 / 8 / 635**, FIRES G-5, row CURRENT) — honoured by construction here:
the dialog gains **one optional prop and three `??`**, nothing else.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/experts/__tests__ src/components/skills/SkillFormDialog.test.tsx --maxWorkers=2</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/experts/__tests__` passes with `>= 15` tests (9 at the phase base + at least 6 new), zero failing.
    - A test asserts the proposal card's className contains ALL of `border-dashed`, `border-primary/40`, `bg-primary/[0.06]`, `text-primary` AND a negative arm asserting it contains NONE of `bg-gradient-to-br`, `border-destructive`, `text-destructive`.
    - A test asserts `Save Expert` is disabled with a proposal open, disabled with a quick-add phantom name and zero proposals, and enabled when neither condition holds — three separate assertions.
    - A test drives `Create this skill →` and asserts, in order: the card's generating state appears, `draftSkillBody` was called once with the proposal's three fields, the dialog opens with the returned `instructions`, `createSkill` is called on confirm, and the name then appears in the `⚡` rail and not in the proposal group.
    - A test asserts `SkillBodyDisabledError` still opens the dialog (with empty instructions) — manual creation is not blocked.
    - A test asserts an `ExpertMemberSkillsUnknownError` save rejection renders each name from `err.unknownSkills`.
    - `git diff --stat -- frontend/src/lib/api/skills.ts frontend/src/types/index.ts backend/app/api/skills.py` is EMPTY.
    - `grep -c "draftSkillBody" frontend/src/components/experts/__tests__/ExpertAuthoringStudio.test.tsx frontend/src/components/experts/__tests__/OrgExpertsTab.test.tsx` returns a non-zero count for BOTH files.
    - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo ROOT: zero failing, no per-file DECREASE, and the total is HIGHER than at the phase base. ⚠ A total that does NOT move after this plan adds tests is the tell, not the reassurance.
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit` — SET DIFF: no error in a file this plan modified that was not present at the phase base. ⛔ Never the bare `--noEmit`; ⛔ never "zero errors" (base measured 72).
    - `node scripts/check-hot-file-ledger.cjs 263` exits 0 with `watched:` > 0; `node scripts/check-claude-md-size.cjs` exits 0.
  </acceptance_criteria>
  <done>Variant A is shipped: two headed groups in one card, a dashed violet proposal that cannot read as an error, a Save held while the blueprint over-promises, and an approval that routes through the dialog and the write path that already existed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: G-4 lived-experience UAT — an Expert whose capabilities match its description</name>
  <files>.planning/phases/263-an-expert-can-be-given-its-capabilities/263-04-SUMMARY.md</files>
  <read_first>
    - .planning/reported-bugs/an-authored-expert-has-no-domain-skills-because-none-exist-to-select.md — `BUG-260921-01`, `folded_into: "263"`. ⚠ Finding (c): the existing `phd-lr` row is STALE mid-development data from NEITHER current path — **re-draft before judging.**
    - .planning/sketches/263-the-skills-an-expert-needs/README.md § "What to Look For"
    - CLAUDE.md § "UAT scoreboard recipe" — the cross-provider roster rule and the "derive the roster from MODEL_CAPABILITIES, never re-type it" instruction
  </read_first>
  <action>
Bring the full stack up and hand the studio to the operator. Start the backend against the local
Supabase (Postgres on `127.0.0.1:54322`) and the frontend dev server; confirm migration 191 is
applied by reading back `information_schema.columns` for `public.skills.born_for_expert_bundle_id`,
and record `SELECT count(*) FROM public.skills` BEFORE the session so R-4's "nothing was
auto-created" claim is a delta rather than an impression. Derive the R-9 provider roster by grouping
`MODEL_CAPABILITIES` on `provider` and taking the newest registry-backed id per group — do NOT
re-type a list, because an id absent from the registry resolves `capability_source=inferred` and
silently loses `emit_tier`. Then PAUSE and let the operator drive rows R-1 through R-9 themselves;
transcribe each verdict with its own evidence into the SUMMARY. A row that cannot be driven is
recorded with its blocking reason and a named next step, never dropped.
  </action>
  <what-built>
The whole phase: a draft that names the skills it needs and does not have; a dashed violet proposal
group beside the library rail; `Create this skill →` drafting a body from `skill-creator`'s own craft
doctrine and opening the existing dialog pre-filled; a real `public.skills` row on approval; a Save
held while a capability is phantom; a server 422 that names each one; and a provenance marker that
makes an Expert-born skill resolvable to the rest of the org through that Expert only.
  </what-built>
  <how-to-verify>
⛔ **Operator's bar, stated at scope time: "an Expert whose capabilities match its description." A
UAT row that does not re-drive the PhD-literature-review prompt end-to-end has NOT tested this phase.**

Prerequisites: backend running against the local Supabase (Postgres on `127.0.0.1:54322`), migration
191 applied (263-01), frontend dev server up. ⚠ Probe `localhost`, not `127.0.0.1` — vite binds IPv6
only.

| # | Row | What to do | Expected |
|---|---|---|---|
| **R-1** | The defect is gone | In the Authoring Studio, brainstorm a NEW doctoral-literature-review Expert with the same prompt as `BUG-260921-01`. ⚠ Do NOT judge the existing `phd-lr` row — it is stale mid-development data from neither current path. | The draft returns `member_skills` containing only names that exist, and a `Proposed for this Expert` group naming the domain skills it needs. The five worked-example names are the target shape: `systematic-search-strategy`, `prisma-screening`, `methodology-appraisal`, `thematic-synthesis`, `citation-integrity-check`. ⛔ `docx`/`xlsx`/`pptx` alone is the FAILURE this phase exists for. |
| **R-2** | Proposed reads as opportunity, not failure | Look at the two groups side by side. | Dashed `⬡` + violet on the proposals, solid `⚡` on the library. No red anywhere in the capabilities card. |
| **R-3** | One approval at a time, through the existing path | Press `Create this skill →` on ONE proposal. | The CARD enters a generating state in place (no empty dialog appears first). Then `SkillFormDialog` opens pre-filled with a real instruction body. Confirm → the skill appears in the `⚡` rail, the counters move, and `SELECT name FROM public.skills ORDER BY created_at DESC LIMIT 1` shows the new row. |
| **R-4** | Never auto-create | Press `Remove` on another proposal, then re-check the library. | Nothing was created. `SELECT count(*) FROM public.skills` rose by exactly the number you approved. |
| **R-5** | Save says so before it saves | With at least one proposal still open, try to save. Then type a made-up name into the quick-add box with zero proposals open and try again. | `Save Expert` is disabled in BOTH cases, with the violet banner naming the count. |
| **R-6** | The server is the real fence | With a REST client and a real bearer token, `POST /experts` directly with a `member_skills` entry that does not exist. | HTTP **422**, body `detail.error == "expert_member_skills_unknown"`, `detail.unknown_skills` naming it. ⛔ A 400 here means the check landed inside the `try`. |
| **R-7** | The second hollowness is closed | Approve every proposal, save the Expert org-wide, then sign in as a DIFFERENT member of the same org and invite it in chat. | The Expert resolves with ALL of its skills. Before this phase, every skill it created would have been stripped for this user. |
| **R-8** | FLAG-01 gates generation only | Turn self-improvement OFF in Settings. Re-draft. | The proposals still LIST (PACK-14 is not hidden). `Create this skill →` refuses with a message saying drafting is off, and the dialog still opens so the skill can be written by hand. |
| **R-9** | Cross-provider board for `emit_skill_body` | ⛔ This phase adds a structured-output call, so CLAUDE.md's roster rule BINDS: the full native roster + OpenRouter = **8 rows**. Derive the roster from `MODEL_CAPABILITIES` by grouping on `provider` and taking the newest REGISTRY-BACKED id per group — never re-type a list. Drive R-3 once per provider using a per-request `model` + `provider` so no global setting is mutated. | Each row either produced a usable body, or is recorded **⛔ with its reason** (no key configured, known defect + issue id). ⛔ A row is never silently omitted. ⚠ Moonshot/Kimi is the only `emit_tier: coerce` native row — the weakest emission guarantee — and every OpenRouter row is `native_tools: False`. ⚠ Recovery is TWO rungs here, not three (`strict=False` skips `strict_force`), so a thin emission gets ONE retry. |

Record every row's verdict AND its own evidence in `263-04-SUMMARY.md`. ⛔ A scoreboard that lists
only what passed is not a scoreboard.
  </how-to-verify>
  <resume-signal>Type "approved" with the R-1..R-9 verdicts, or describe what you saw.</resume-signal>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_263_expert_save_refuses_unknown_skills.py tests/unit/test_263_expert_born_skill_resolution.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All nine rows are recorded in `263-04-SUMMARY.md` with a verdict and its own evidence — a quoted response body, a SQL read-back, or a screenshot path. ⛔ Never one blanket "all passed".
    - R-1 is judged against a FRESHLY drafted Expert, and the SUMMARY says so explicitly.
    - R-6 quotes the 422 body verbatim including `unknown_skills`.
    - R-9 lists all 8 roster rows; any blocked row carries its reason and, where applicable, an issue id.
    - Every row whose verdict is not PASS is either fixed in this plan or recorded as OWED with a named next step.
  </acceptance_criteria>
  <done>The operator has driven the PhD-literature-review prompt end to end and every row carries its own evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server response → rendered DOM | Proposal names, descriptions and `unknown_skills` are model- or server-supplied text rendered in the studio. |
| browser fence → server | The client's disabled Save is advisory only; a stale client or a direct call bypasses it. |
| author input → `POST /skills` | The dialog's pre-filled body is edited by a human before it becomes a row. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-263-20 | Information disclosure (XSS) | `ProposedSkillCard`, the banners, the pre-filled dialog | mitigate | All model- and server-supplied strings are rendered as React text children (auto-escaped). ⛔ No `dangerouslySetInnerHTML` is introduced anywhere in this plan; asserted by `grep -c "dangerouslySetInnerHTML"` returning 0 across the three touched components. |
| T-263-21 | Spoofing | the client-side Save fence | mitigate | Advisory by design (D-263-09). The server re-checks independently (263-03) and UAT row R-6 drives the bypass directly rather than assuming it. |
| T-263-22 | Tampering | a generated instruction body written to the library unreviewed | mitigate | D-263-05 writes only on human approval, through the dialog, through the existing `POST /skills` which already lints the description pre-persist. ⛔ Never auto-create — driven by UAT R-4. |
| T-263-23 | Elevation of privilege | a second write path in the UI | mitigate | `createSkill` from `@/lib/api/skills` is the only write; `frontend/src/lib/api/skills.ts` must end this plan byte-identical, asserted by an empty `git diff --stat`. |
| T-263-24 | Repudiation | a fabricated body presented as authored | mitigate | `draftSkillBody` surfaces a real failure (409 disabled / 503 unavailable); the UI never fills the field with invented text. Driven by the `SkillBodyDisabledError` test and UAT R-8. |
| T-263-SC | Tampering | npm/pip/cargo installs | accept | ⭐ **This phase installs NOTHING** (RESEARCH §2, measured). No `npm install` appears in any task. |
</threat_model>

<verification>
1. `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/experts/__tests__ src/components/skills/SkillFormDialog.test.tsx --maxWorkers=2` — the deterministic in-scope suites.
2. `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` **from the repo ROOT**. Contract: no per-file DECREASE, zero failing — ⛔ never a fixed grand total (CLAUDE.md records six documented rots of that figure). ⚠ `count gate OK` is not reliably reachable on demand, so PAIR it with the per-file deltas and step 1's explicit run. ⛔ If it reds: capture failing FILENAMES from the gate's own persisted JSON BEFORE any re-run, check each against `git diff --numstat <phase base>..HEAD` and `git status --short`, and do NOT touch the worker cap. ⚠ Two of SEED-171's five cap-independent flaky suites are workflow pages, not these — a red in `src/components/experts` or `src/components/skills` should be treated as REAL until proven inherited at the base commit.
3. `cd frontend && npx tsc -p tsconfig.app.json --noEmit` — SET DIFF against the phase base. ⛔ Never bare `--noEmit` (solution-style config, checks ZERO files, exits 0 over anything). ⛔ Never "zero errors": the base measured **72** at research and STATE.md's frozen **65** has drifted +7.
4. `cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_263_*.py tests/unit/test_259_expert_*.py tests/unit/test_260_expert_*.py tests/unit/test_261_expert_*.py tests/unit/test_261_single_expert_authoring_gate.py tests/unit/test_259_closed_core_inventory.py tests/unit/test_seed125_skill_visibility_filter.py -q` — the full expert/skill sweep at phase close.
5. **Backend baseline as a SET DIFF** against the phase base commit (method in 263-01 step 3). Ceiling 71, zero headroom.
   ⛔ **The phase base commit is DEFINED in `263-01-PLAN.md`’s objective and its SHA is recorded verbatim in `263-01-SUMMARY.md` as `Phase base commit: <sha>`. READ IT FROM THERE, never re-derive it** — after waves 1-3 land, a re-derived base compares HEAD against itself and every SET diff passes vacuously. The same SHA is what step 2’s red-triage `git diff --numstat <phase base>..HEAD` means.
6. `node scripts/check-hot-file-ledger.cjs 263` · `node scripts/check-claude-md-size.cjs` · `node scripts/check-seeds-register.cjs --phase 263` (route `SEED-303`, whose `trigger_paths` name `expert_service.py`, `api/experts.py` and `models/expert.py` — write the routing back into the seed's own frontmatter).
7. ⚠ Only if verification returns `gaps_found`: `node scripts/check-gap-closure-rounds.cjs 263` BEFORE emitting any `--gaps` routing.

## VALIDATION — the phase's UAT scoreboard

Task 3 rows **R-1 … R-9** are the scoreboard, and they close the rows owed by the earlier plans:
U-02 (263-01) is R-7 · U-03 (263-02) is R-9 · U-04 (263-02) is R-8 · U-05/U-06 (263-03) are R-6.
⛔ A row that cannot be driven is recorded ⛔ WITH ITS REASON and a named next step — never omitted.
</verification>

<success_criteria>
- A freshly drafted doctoral-literature-review Expert names the domain skills it lacks, and they are visibly distinct from the library without reading as an error.
- One approval at a time creates one real `public.skills` row through the dialog and the write path that already existed; nothing is auto-created.
- Save is held while any named capability is phantom, from either entry point, and the server refuses independently with the names.
- A second member of the same org gets the Expert's full capability set.
- `SkillFormDialog.test.tsx` is in both count-gate knobs and its pin rose; the grand total moved.
- `frontend/src/lib/api/skills.ts`, `frontend/src/types/index.ts` and `backend/app/api/skills.py` are byte-unchanged.
- The cross-provider board for `emit_skill_body` is recorded with every row's verdict or its blocking reason.
</success_criteria>

<output>
Create `.planning/phases/263-an-expert-can-be-given-its-capabilities/263-04-SUMMARY.md` when done.
Record: the vitest per-file deltas and the gate's verdict line VERBATIM (never a summary); the tsc
error SET diff; the nine UAT rows each with its own evidence; the re-derived ledger triples including
the new component's row; and anything left OWED with a named next step.
</output>
