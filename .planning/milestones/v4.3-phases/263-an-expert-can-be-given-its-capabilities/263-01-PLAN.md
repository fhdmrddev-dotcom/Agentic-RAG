---
phase: 263-an-expert-can-be-given-its-capabilities
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
requirements: [PACK-16, PACK-17]
files_modified:
  - supabase/migrations/191_skill_expert_provenance.sql
  - supabase/full-schema.sql
  - backend/app/services/expert_service.py
  - backend/app/db/experts.py
  - backend/tests/unit/test_263_expert_born_skill_resolution.py
  - backend/tests/unit/test_seed125_skill_visibility_filter.py
  - backend/tests/integration/test_263_skill_org_stamp_live.py
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md

must_haves:
  truths:
    - "A skill born for Expert A resolves through Expert A for any member of the same org, even when it is private to its author (D-263-06)."
    - "A skill born for Expert A is STRIPPED when resolved through Expert B (D-263-07 narrowness)."
    - "A skill in another org is STRIPPED even when born_for_expert_bundle_id equals this bundle and is_org_shared is true (PACK-17, D-263-12)."
    - "Saving an Expert cannot admit another user's private skill through a NULL-vs-None comparison (the bundle_id=None trap)."
    - "A skill created by an org-A user is stamped org-A by the database, regardless of any payload (PACK-17 structural half, D-263-08)."
  artifacts:
    - path: "supabase/migrations/191_skill_expert_provenance.sql"
      provides: "public.skills.born_for_expert_bundle_id uuid NULL + COMMENT + partial index"
      contains: "born_for_expert_bundle_id"
    - path: "backend/app/services/expert_service.py"
      provides: "filter_visible_skill_names — the ONE Expert-side visibility helper, with the third arm"
      contains: "filter_visible_skill_names"
    - path: "backend/app/db/experts.py"
      provides: "stamp_skills_born_for_bundle — the D-263-08 backfill UPDATE"
      contains: "stamp_skills_born_for_bundle"
    - path: "backend/tests/unit/test_263_expert_born_skill_resolution.py"
      provides: "the five-case D-263-06 drive incl. the PACK-17 cross-org case"
      min_lines: 180
    - path: "backend/tests/integration/test_263_skill_org_stamp_live.py"
      provides: "the live-DB drive of skills_autofill_org_id (PACK-17 case 3)"
      min_lines: 80
  key_links:
    - from: "backend/app/services/expert_service.py"
      to: "public.skills.born_for_expert_bundle_id"
      via: "the widened SELECT column list"
      pattern: "born_for_expert_bundle_id"
    - from: "backend/app/services/expert_service.py::create_expert_service"
      to: "backend/app/db/experts.py::stamp_skills_born_for_bundle"
      via: "the save-path backfill call"
      pattern: "stamp_skills_born_for_bundle"
---

<objective>
Give a skill a provenance marker, and teach `resolve_expert_bundle` to honour exactly that marker —
so an Expert authored for an org stops being hollow for every org member except the person who
created its skills. Then DRIVE the cross-org fence against the new arm, because widening an
isolation predicate is how `SEED-125` would recur.

Purpose: PACK-17 is driven, not asserted (D-263-12); PACK-16's server fence gets its one helper.

⚠ **TAGGING PRECISION — this plan is tagged `PACK-16` as a PRECONDITION, not as its delivery.**
PACK-16’s user-facing save-time refusal is delivered by **263-03**, which also carries the tag.
What lands here is the one helper that refusal calls, plus the `bundle_id is not None` guard that
keeps it from admitting every other user’s private skill. ⛔ **Do not double-count this plan as
closing PACK-16 at phase close** — 263-03 is the owner.
Output: migration 191, the third disjunct (D-263-06), the save-path stamp (D-263-08), and four
driven test files — one of them against the live local Postgres.

⛔ **DEFINITION — "THIS PHASE’S BASE COMMIT", pinned here once because three later plans diff
against it and none of them defines it.** It is the SHA of `HEAD` **before this plan makes its
first commit**. Capture it as the FIRST action of this plan, after `bootstrap-worktree.sh` and
before any edit:

    git rev-parse HEAD    # record verbatim in 263-01-SUMMARY.md as "Phase base commit: <sha>"

⚠ It is NOT the same as "this plan’s own base commit", which 263-02/03/04 each capture for
their own backend/tsc SET diffs and which differs per worktree. **263-03’s D-263-11 closed-core
comparison and 263-04’s red-triage both mean the PHASE base**, and both must read the SHA from
263-01’s SUMMARY rather than re-deriving it — a re-derived base drifts once wave 1 has landed,
which would make the inventory comparison compare HEAD against itself and pass vacuously.

⛔ **THIS PLAN IS DB-MUTATING AND UN-PARALLELISABLE.** It applies DDL and its integration test
inserts and deletes a real `public.skills` row. Worktrees isolate files, not Postgres. Run it alone.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/263-an-expert-can-be-given-its-capabilities/263-CONTEXT.md
@.planning/phases/263-an-expert-can-be-given-its-capabilities/263-RESEARCH.md
@.planning/phases/263-an-expert-can-be-given-its-capabilities/263-PATTERNS.md

<interfaces>
Contracts this plan CREATES, that plans 263-02/03/04 consume. They are declared here so no
downstream executor has to go looking.

backend/app/services/expert_service.py
  async def filter_visible_skill_names(
      pool: asyncpg.Pool,
      skill_names: list[str],
      caller_org_id: UUID,
      caller_user_id: UUID,
      bundle_id: UUID | None = None,
  ) -> set[str]
  Returns the subset of `skill_names` that the caller may resolve. `bundle_id=None` means
  "no bundle exists yet" (the POST /experts save-time fence) and DISABLES the third arm.

backend/app/db/experts.py
  async def stamp_skills_born_for_bundle(
      pool: asyncpg.Pool,
      *,
      bundle_id: UUID,
      skill_names: list[str],
      org_id: UUID,
      user_id: UUID,
  ) -> list[str]
  Returns the names actually stamped. Stamps ONLY rows the saving user owns, in the caller's org,
  whose marker is still NULL.

Column added by migration 191:
  public.skills.born_for_expert_bundle_id uuid NULL
    REFERENCES public.expert_bundles(id) ON DELETE SET NULL
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Author migration 191 and have the operator apply it</name>
  <files>supabase/migrations/191_skill_expert_provenance.sql, supabase/full-schema.sql</files>
  <read_first>
    - supabase/migrations/188_expert_chat_scoping.sql — the EXACT analog (PATTERNS §2): nullable uuid FK to `expert_bundles`, `ON DELETE SET NULL`, `COMMENT ON COLUMN`, partial `WHERE … IS NOT NULL` index
    - supabase/migrations/189_expert_presentation_and_grants.sql — ⛔ NOT the analog for the ACL question: it carries a `GRANT`, which is what pulls a migration into `scripts/check-schema-acl-parity.cjs`
    - supabase/full-schema.sql lines 2696-2715 (table `public.skills`) and 5234/5241/5248/5255 (its four triggers)
    - .planning/phases/263-an-expert-can-be-given-its-capabilities/263-RESEARCH.md §B.1-B.7
    - CLAUDE.md § "Rules" — the migration procedure (SQL editor, never `db push`/`db reset`)
  </read_first>
  <action>
Create `supabase/migrations/191_skill_expert_provenance.sql`. Header comment in 188's idiom:
`-- Migration 191 — Phase 263 (PACK-17 / D-263-07, D-263-08)`, then one `--` line per section.

Three statements, copying 188's shapes and changing only the names:
1. `ALTER TABLE public.skills ADD COLUMN IF NOT EXISTS born_for_expert_bundle_id uuid REFERENCES public.expert_bundles(id) ON DELETE SET NULL;` — `ON DELETE SET NULL` is what 188 already does for a nullable `expert_bundles` FK; `CASCADE` would delete real, independently-useful library rows and is wrong.
2. `COMMENT ON COLUMN public.skills.born_for_expert_bundle_id` stating: it is the provenance marker for the Expert bundle a skill was authored for (D-263-07); NULL for every skill not born from Expert authoring, INCLUDING an abandoned draft (D-263-08); it is read as the THIRD disjunct of `resolve_expert_bundle`'s phase-2 check and the `org_id` fence above it is UNCHANGED.
3. `CREATE INDEX IF NOT EXISTS idx_skills_born_for_expert ON public.skills (born_for_expert_bundle_id) WHERE born_for_expert_bundle_id IS NOT NULL;` — name matches the two nearest neighbours (`idx_threads_active_expert`, `idx_expert_grants_lookup`).

⛔ **The file contains NO `GRANT` and NO `REVOKE`.** That is load-bearing, not stylistic: it is the
only reason 191 stays outside `scripts/check-schema-acl-parity.cjs`'s scan and outside
`scripts/full-schema-supplement.sql`. `scripts/` is EXEMPT from the ledger gate, so **no gate would
ask** if this were violated. Verify with `grep -icE "grant|revoke" supabase/migrations/191_skill_expert_provenance.sql` returning `0`.

⛔ **No RLS policy change.** All four `public.skills` policies are row-scoped and reference only
`org_id`, `user_id`, `is_system`, `is_org_shared`; Postgres applies them per row regardless of column
count, so the new column is covered the instant it exists (RESEARCH §B.4, with the policy text).

⛔ **No historical backfill.** Every one of the 10 existing rows keeps `NULL`. Retro-stamping would
grant Experts skills their authors never scoped to them (RESEARCH, Runtime State Inventory).

Then PAUSE for the operator. After they confirm the paste, run
`bash scripts/regenerate-full-schema.sh` (no `--reset`) and confirm `supabase/full-schema.sql` now
contains `born_for_expert_bundle_id` in the `public.skills` CREATE TABLE block. ⛔ Never hand-edit
`full-schema.sql`. ⛔ Never `supabase db push` or `supabase db reset`.
  </action>
  <what-built>
`supabase/migrations/191_skill_expert_provenance.sql` — a nullable uuid FK to `public.expert_bundles`
on `public.skills`, a `COMMENT`, and a partial index. No GRANT, no RLS change, no data backfill.
  </what-built>
  <how-to-verify>
1. Open the Supabase SQL editor for the LOCAL project (Postgres on `127.0.0.1:54322`).
2. Paste the full contents of `supabase/migrations/191_skill_expert_provenance.sql` and run it.
   ⛔ Do NOT run `supabase db push` or `supabase db reset` — both destroy or bypass dev data.
3. Confirm it reports success, then run this read-back in the same editor:
   `SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_schema='public' AND table_name='skills' AND column_name='born_for_expert_bundle_id';`
   Expected: exactly one row, `uuid`, `YES`.
4. Confirm `SELECT count(*) FROM public.skills WHERE born_for_expert_bundle_id IS NOT NULL;` returns `0`.
  </how-to-verify>
  <resume-signal>Type "applied" once the column read-back returns one `uuid`/`YES` row, or paste the error.</resume-signal>
  <verify>
    <automated>grep -c "born_for_expert_bundle_id" supabase/full-schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/191_skill_expert_provenance.sql` exists and its filename matches `^[0-9]+_[a-z0-9_]+\.sql$` (a letter suffix like `191b` is silently skipped by the Supabase CLI).
    - `grep -icE "grant|revoke" supabase/migrations/191_skill_expert_provenance.sql` returns `0`.
    - `grep -c "born_for_expert_bundle_id" supabase/full-schema.sql` returns `>= 1` after regeneration.
    - `git diff --name-only -- scripts/full-schema-supplement.sql` is EMPTY — 191 creates no ACL obligation.
    - `node scripts/check-schema-acl-parity.cjs` exits 0.
    - `SELECT count(*) FROM public.skills WHERE born_for_expert_bundle_id IS NOT NULL` returns 0 (no historical backfill).
  </acceptance_criteria>
  <done>Migration 191 exists, is applied to the local DB, `full-schema.sql` is regenerated from the live DB, and the ACL-parity gate is green with zero supplement changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The third arm, the one Expert-side visibility helper, and the save-path stamp</name>
  <files>backend/app/services/expert_service.py, backend/app/db/experts.py, backend/tests/unit/test_263_expert_born_skill_resolution.py</files>
  <read_first>
    - backend/app/services/expert_service.py lines 249-291 — the phase-2 skill block. ⛔ CONTEXT.md cites `:205-224` in FOUR places and that citation is STALE by ~45 lines; `:205-224` is inside `update_expert_service`/`delete_expert_service`. Opening `:205` edits the wrong function.
    - backend/app/utils/skill_visibility.py — ⛔ READ THIS BEFORE WRITING THE HELPER. It is the DECLARED single home of the skill-visibility rule (`build_skill_visibility_or`, `skill_row_visible`), whose docstring says "There is exactly ONE copy of this rule". `resolve_expert_bundle`'s predicate is in fact a THIRD encoding written in Phase 259 without it.
    - backend/app/db/experts.py lines 343-409 (`update_expert_bundle`) — the module's only existing `UPDATE`, and its `$1`-numbered asyncpg idiom
    - backend/tests/unit/test_259_expert_member_isolation.py — the mock-pool scaffolding and the four-assertion block to copy (PATTERNS T-1)
    - backend/tests/unit/test_259_closed_core_inventory.py lines 65-83 — the AST constraint on `expert_service.py`: no `while` loop, no `openai`/`anthropic`/`litellm`/`agent_loop` import
    - backend/tests/unit/test_261_single_expert_authoring_gate.py lines 22-29 — `EXPERT_MODULE_PATHS` includes both files edited here: ZERO hardcoded `org-admin`/`super-admin`/`admin` literals
    - .planning/phases/263-an-expert-can-be-given-its-capabilities/263-RESEARCH.md §C, §D.3
  </read_first>
  <behavior>
    - A skill row with `org_id == caller_org_id`, `is_enabled`, `user_id != caller`, `is_org_shared == False`, `born_for_expert_bundle_id == bundle_id` → ADMITTED.
    - Same row but `born_for_expert_bundle_id` set to a DIFFERENT bundle uuid → STRIPPED, counted, logged.
    - Same row but `born_for_expert_bundle_id is None` → STRIPPED (no regression; abandoned drafts behave exactly as today).
    - A row with `org_id == <another org>`, `is_org_shared == True`, `born_for_expert_bundle_id == bundle_id` → STRIPPED, counted, `EXPERT_MEMBER_CROSS_ORG_STRIPPED` in the log. (PACK-17 against the new arm.)
    - `filter_visible_skill_names(..., bundle_id=None)` with a row whose `born_for_expert_bundle_id is None`, owned by ANOTHER user, not shared → NOT visible. (The `None == None` trap.)
    - `stamp_skills_born_for_bundle` stamps only rows matching name ∈ skill_names AND `org_id = $org` AND `user_id = $user` AND `born_for_expert_bundle_id IS NULL`; it returns the stamped names.
  </behavior>
  <action>
**(a) Extract the helper, in `backend/app/services/expert_service.py`.** Add
`async def filter_visible_skill_names(pool, skill_names, caller_org_id, caller_user_id, bundle_id=None) -> set[str]`
holding the phase-2 skill predicate exactly once, and have `resolve_expert_bundle` call it instead of
inlining the loop. `resolve_expert_bundle` keeps ownership of the stripped counter, the
`stripped_details` `f"skill:{s}"` entries and the `EXPERT_MEMBER_CROSS_ORG_STRIPPED` warning — ⛔ do
not move the log into the helper and do not introduce a second log verb; six existing tests match
that literal through `caplog.text`.

**(b) Widen the SELECT — this is the half most likely to be missed.** The predicate is Python over
rows already fetched, and the query at `:255` filters only on `name`. Add
`born_for_expert_bundle_id` to the SELECT column list. ⛔ Without it the feature does nothing and
**every existing test still passes** — with `.get()` it fails silently open-to-strip. The signature
of that bug is: behaviour case 1 fails while cases 2-4 pass.

**(c) The third disjunct goes in the INNER parenthesis.** Keep the shape
`elif (s_org_id == caller_org_id and s_enabled and (s_user_id == caller_user_id or s_shared or <new arm>))`.
⛔ A fourth top-level `elif` is exactly the bug PACK-17 exists to prevent — placing the arm inside
the inner parenthesis is what makes D-263-06's "the `org_id` fence is untouched" true BY
CONSTRUCTION rather than by assertion.

**(d) ⛔ Guard the arm on `bundle_id is not None` — a measured trap, not a style preference.** The
new arm must read `bundle_id is not None and row.get("born_for_expert_bundle_id") == bundle_id`. At
save time (plan 263-03) the helper is called with `bundle_id=None`, and every unstamped row carries
`born_for_expert_bundle_id = None`; a bare `==` then evaluates `None == None` to True and admits
**every other user's private skill in the org**. That is a privilege widening the save-time fence
would ship silently.

**(e) Use `row.get(...)`, never `row[...]`** for the new key, and compare a `UUID` to a `UUID`.
Every mock fixture in `test_259_expert_member_isolation.py` is a plain dict without the key; `.get()`
keeps all six green without editing them, and a `str` fixture would compare unequal and make a test
pass for the wrong reason.

**(f) ⛔ Do NOT extend `app/utils/skill_visibility.py`, and say why in a comment on the new helper.**
That module is the declared one home of the *agent-side* rule and is imported by
`tool_dispatcher.py` (five call sites) and `harness/grounding.py`. Adding the born-for arm there
would widen skill resolution for the agent loop and for workflow grounding — two consumers this
phase has no business touching, and the exact shape of `SEED-125`. The Expert arm therefore stays in
the Expert module, deliberately. Record that as the reason in the docstring, so the next reader sees
a decision rather than an oversight. (Note for the record: `app/utils/skill_visibility.py` has no
hot-file ledger row — out of scope here, but worth a seed.)

**(g) The D-263-08 backfill, in `backend/app/db/experts.py`.** Add
`async def stamp_skills_born_for_bundle(pool, *, bundle_id, skill_names, org_id, user_id) -> list[str]`
running one `UPDATE public.skills SET born_for_expert_bundle_id = $1 WHERE name = ANY($2::text[]) AND org_id = $3 AND user_id = $4 AND born_for_expert_bundle_id IS NULL RETURNING name`.
Numbered `$n` parameters, never f-string interpolation. Three predicates are each load-bearing:
`org_id = $3` stops a foreign-org row being stamped (the stamp IS a privilege grant under D-263-06);
`user_id = $4` stops another author's pre-existing row being silently conscripted into this Expert;
`born_for_expert_bundle_id IS NULL` encodes D-263-07's rejection of re-stamping on reuse (at two
bundles the first Expert would otherwise silently lose the skill). Return early with `[]` when
`skill_names` is empty.

**(h) Wire it into the save path.** Call it from `create_expert_service` after
`create_expert_bundle` returns (the bundle id exists only then), and from `update_expert_service`
when `bundle_in.member_skills is not None`. Log the returned names at INFO. A stamp failure must not
fail the save — wrap the call so an exception is logged and swallowed, because the Expert row is the
deliverable and the marker is an optimisation of resolution.

**(i) Tests — `backend/tests/unit/test_263_expert_born_skill_resolution.py`.** Copy
`test_259_expert_member_isolation.py`'s header, imports and mock-pool scaffolding verbatim
(`MagicMock()` pool, `fetchrow = AsyncMock(return_value=bundle_row)`,
`fetch = AsyncMock(side_effect=[skill_rows, folder_rows, conn_rows])` — the ordered side_effect list
matters; omitting the folder/connection lists fails inside `resolve_expert_bundle`, not at the
assertion). One test per behavior bullet. Each cross-org/strip case asserts FOUR things, as the 259
file does: membership, absence, `stripped_members_count`, and `EXPERT_MEMBER_CROSS_ORG_STRIPPED` in
`caplog.text`. Add a direct unit test of `stamp_skills_born_for_bundle` against an `AsyncMock` pool
asserting the SQL text contains all four WHERE predicates and that the four bound arguments are
passed in order.

**(j) THE RED DRIVE — it is a deliverable, not a formality.** Plant the third disjunct as a
top-level `elif` OUTSIDE the org fence, run the file, and record that behaviour case 4 (the org-B
row) turns RED. Then restore and verify `expert_service.py` is md5-identical to before the plant.
⭐ That plant proves the PLACEMENT, not merely the presence — which is the whole point of D-263-06's
"the `org_id` fence is untouched". Record the md5 before and after in the SUMMARY.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_263_expert_born_skill_resolution.py tests/unit/test_259_expert_member_isolation.py tests/unit/test_259_closed_core_inventory.py tests/unit/test_261_single_expert_authoring_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/test_263_expert_born_skill_resolution.py` passes with at least 6 cases, covering all six behavior bullets.
    - `tests/unit/test_259_expert_member_isolation.py` still reports **6 passed** with ZERO fixture edits (`git diff --stat -- backend/tests/unit/test_259_expert_member_isolation.py` is empty).
    - `grep -c "born_for_expert_bundle_id" backend/app/services/expert_service.py` returns `>= 2` — one in the SELECT column list, one in the predicate.
    - `grep -c "bundle_id is not None" backend/app/services/expert_service.py` returns `>= 1` (the `None == None` guard).
    - `grep -cE "org_id = \\$3" backend/app/db/experts.py` and `grep -cE "user_id = \\$4" backend/app/db/experts.py` each return `>= 1`.
    - `grep -c "born_for_expert_bundle_id IS NULL" backend/app/db/experts.py` returns `>= 1`.
    - `grep -c "EXPERT_MEMBER_CROSS_ORG_STRIPPED" backend/app/services/expert_service.py` returns exactly `1` — no second log verb.
    - `tests/unit/test_261_single_expert_authoring_gate.py` passes (no hardcoded role literal entered either edited module).
    - `tests/unit/test_259_closed_core_inventory.py` reports **6 passed** — `expert_service.py` is still AST-pure data transformation.
    - The RED drive is recorded in the SUMMARY with the failing case name, and the pre/post md5 of `backend/app/services/expert_service.py` are identical.
  </acceptance_criteria>
  <done>The third arm resolves a born-for skill through its own bundle and nothing else; the org fence is structurally above it and proven so by a plant; the save path stamps only the saver's own unstamped rows in its own org.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Drive PACK-17 — cross-org isolation, on all three axes</name>
  <files>backend/tests/unit/test_seed125_skill_visibility_filter.py, backend/tests/integration/test_263_skill_org_stamp_live.py, docs/HOT-FILE-LEDGER.md, CLAUDE.md</files>
  <read_first>
    - backend/tests/unit/test_seed125_skill_visibility_filter.py — the existing visibility-axis suite. ⛔ EXTEND it; do not open a third home for the same rule.
    - backend/app/utils/skill_visibility.py — the predicate under test on the visibility axis
    - backend/tests/integration/test_137_2_skill_creator_seed_content.py lines 53-100 — the live-DB gate idiom to copy: `_POSTGRES_TEST_DSN` env-with-default, `_pg_reachable`, module-level `pytestmark = pytest.mark.skipif(...)`, function-scoped `pg_pool` fixture, and "an absent expected row is a HARD FAILURE naming the migration, not a clean skip"
    - backend/tests/unit/test_261_expert_grants_db.py — the live-local-Postgres precedent that owns its own cleanup
    - supabase/full-schema.sql line 5234 and the body of `public.autofill_org_id_by_owner` — note the NO-OP arm: if `NEW.org_id IS NOT NULL` the trigger keeps the caller's value verbatim
    - backend/app/api/skills.py lines 246-256 — `POST /skills` supplies exactly five keys and `org_id` is NOT among them (`is_org_shared` is hard-set at `:253`, not `:250` as CONTEXT.md says)
    - .planning/seeds/SEED-125-skill-tools-service-role-cross-org-leak.md
    - CLAUDE.md § hot-file ledger — the re-derivation recipe and the same-commit sync rule
  </read_first>
  <behavior>
    - Visibility axis: a foreign-org skill with `is_org_shared=True` is not visible to a cross-org caller, and adding `born_for_expert_bundle_id` to the row changes nothing on this axis.
    - Creation axis (LIVE DB): inserting a `public.skills` row with `user_id` set and `org_id` omitted stamps the org from `public.org_members` — the value is never caller-supplied.
    - Creation axis (LIVE DB): the `born_for_expert_bundle_id` column exists and defaults to NULL on a fresh insert.
  </behavior>
  <action>
**(a) Extend `backend/tests/unit/test_seed125_skill_visibility_filter.py`** with one case per
encoding (`build_skill_visibility_or` and `skill_row_visible`) proving that a row carrying
`born_for_expert_bundle_id` is still invisible cross-org. The point is the NEGATIVE: this phase adds
a column to a table whose visibility rule must be unaffected by it. Name the cases so the reason is
legible, e.g. `test_born_for_marker_does_not_widen_cross_org_visibility`.

**(b) New live-DB drive `backend/tests/integration/test_263_skill_org_stamp_live.py`.** Copy the
gate idiom from `test_137_2_skill_creator_seed_content.py` verbatim. Then:
- Read an existing `public.org_members` row to obtain a real `(user_id, org_id)` pair; if none
  exists, HARD-FAIL with a message naming the setup step — ⛔ never skip, because a skip here looks
  identical to a pass.
- `INSERT INTO public.skills (user_id, name, description, instructions) VALUES (...) RETURNING id, org_id, born_for_expert_bundle_id` — ⛔ deliberately OMIT `org_id`, which is what makes the trigger's
  derivation the thing under test. Use a name prefixed `test_263_org_stamp_` plus a uuid so it cannot
  collide with a real row.
- Assert the returned `org_id` equals that user's `org_members.org_id` and that
  `born_for_expert_bundle_id IS NULL`.
- Second insert in the same test, this time SUPPLYING a foreign `org_id`, to document the trigger's
  NO-OP arm: assert the supplied value is kept verbatim, and assert in a comment that this is
  precisely why `POST /skills` must never place `org_id` in its insert payload.
- `DELETE FROM public.skills WHERE id = ANY(...)` in a `finally`, then assert the row count returned
  to its pre-test value. ⛔ This test MUTATES the local database — that is why this plan is
  un-parallelisable.

**(c) Ledger hygiene, same commit as the code.** Re-derive the triple for every file this plan
modified using CLAUDE.md's three-command recipe (`git log --oneline | wc -l`;
`git log --format=%s | sed -E 's/^[a-z]+\(([^)]+)\).*/\1/' | sed -E 's/-.*//' | grep -E '^[0-9]+(\.[0-9]+)?$' | sort -u | wc -l`;
`wc -l`) — ⛔ subtract six-digit dated quick-task buckets such as `260814`, they are not phases.
Update the cell in CLAUDE.md's scan table AND that file's section in `docs/HOT-FILE-LEDGER.md` in the
SAME commit. Measured at planning (2026-09-21, HEAD `10f1a582e`) for the two files touched here:
`backend/app/services/expert_service.py` = **5 / 3 / 378** (FIRES at 3 phases) and
`backend/app/db/experts.py` = **3 / 2 / 525** (below threshold). Both will have moved by the time
this lands — re-derive, do not copy these numbers. Disposition cells are capped at 200 chars by
`scripts/check-claude-md-size.cjs`: verdict in the cell, reasons in the section. For
`expert_service.py` the verdict is "honoured by construction (263-01): ONE disjunct inside the
EXISTING inner parenthesis + one SELECT column; the org fence is structurally above it".
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_seed125_skill_visibility_filter.py tests/integration/test_263_skill_org_stamp_live.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/test_seed125_skill_visibility_filter.py` passes with at least 2 more cases than at the phase base (`git diff --numstat` shows added lines, and the run reports a higher count than the base run).
    - `tests/integration/test_263_skill_org_stamp_live.py` passes against the live local Postgres and does NOT skip — the run output contains no `SKIPPED`.
    - `SELECT count(*) FROM public.skills` returns the same number before and after the integration run (recorded in the SUMMARY).
    - `node scripts/check-hot-file-ledger.cjs 263` exits 0 and reports `watched:` greater than 0 (a gate that parsed nothing is not a pass).
    - `node scripts/check-claude-md-size.cjs` exits 0.
    - Every ledger cell touched carries a re-derived triple, and its section in `docs/HOT-FILE-LEDGER.md` was updated in the same commit.
  </acceptance_criteria>
  <done>PACK-17 is driven on all three axes — resolution (task 2 case 4), visibility (extended existing suite), and creation (live trigger drive) — and the ledger registers agree with git.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller org → `public.skills` rows of another org | `resolve_expert_bundle` runs on a **service-role** pool; RLS cannot save it. This plan widens the predicate that guards this boundary. |
| Expert bundle A → Expert bundle B | A new marker makes a private skill resolvable to org members. Its narrowness is the control. |
| application payload → `public.skills.org_id` | The org stamp must be derived, never supplied. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-263-01 | Information disclosure | `expert_service.filter_visible_skill_names` third arm | mitigate | The arm lands in the INNER parenthesis, structurally under `s_org_id == caller_org_id and s_enabled`. Driven by the org-B/`born_for == bundle_id`/`is_org_shared=True` case, RED-driven against a top-level-`elif` plant. |
| T-263-02 | Elevation of privilege | `filter_visible_skill_names(bundle_id=None)` | mitigate | Arm guarded on `bundle_id is not None`. Without it `None == None` admits every other user's private org skill at save time. Driven by its own case. |
| T-263-03 | Elevation of privilege | `stamp_skills_born_for_bundle` | mitigate | `WHERE … AND org_id = $3 AND user_id = $4 AND born_for_expert_bundle_id IS NULL` — a foreign-org row, another author's row, and an already-claimed row are all unstampable. SQL-text and argument-order asserted. |
| T-263-04 | Elevation of privilege | the marker itself, by design | accept | An author binding their own private skill to an org-visible Expert DOES widen it to org members. That is D-263-06's stated purpose and the hollowness being closed; it is author-initiated, org-bounded, and per-bundle. Recorded as accepted, not unnoticed. |
| T-263-05 | Tampering | migration 191 DDL | mitigate | Applied by the operator through the SQL editor with a read-back; no `GRANT`/`REVOKE`; no RLS change; no data backfill; ACL-parity gate run. |
| T-263-06 | Spoofing | `public.skills.org_id` on insert | transfer | Owned by the `skills_autofill_org_id` BEFORE-INSERT trigger plus the INSERT RLS policy. Two independent mechanisms, neither caller-controlled — this plan DRIVES them (task 3b) rather than rebuilding them. |
| T-263-07 | Denial of service | the save-path stamp | accept | The stamp is wrapped so an exception is logged and swallowed; the Expert row is the deliverable and an unstamped skill degrades to today's behaviour. |
| T-263-SC | Tampering | npm/pip/cargo installs | accept | ⭐ **This phase installs NOTHING** — measured in RESEARCH §2. No package-manager command appears in any task, so the legitimacy gate has no subject. |
</threat_model>

<verification>
Run from the repo root unless stated.

1. `cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_263_expert_born_skill_resolution.py tests/unit/test_259_expert_member_isolation.py tests/unit/test_259_expert_bundles_db.py tests/unit/test_259_expert_entitlement_gate.py tests/unit/test_261_expert_grants_db.py tests/unit/test_261_single_expert_authoring_gate.py tests/unit/test_259_closed_core_inventory.py tests/unit/test_seed125_skill_visibility_filter.py -q`
   — the 42-test targeted loop plus this plan's additions. Base reading measured at research: **42 passed** across the first six files.
2. `cd backend && venv/Scripts/python.exe -m pytest tests/integration/test_263_skill_org_stamp_live.py -q` — must PASS, must not SKIP.
3. **Backend baseline, as a SET DIFF against this plan's own base commit — ⛔ never "zero failures".**
   `pytest tests/unit -q --continue-on-collection-errors` in `backend/` with the venv, capturing
   `grep -E "^FAILED " > <file>` at the base commit and at HEAD, then `comm -13` and `comm -23` in
   BOTH directions. ⚠ Strip trailing warning text before diffing — a `RuntimeWarning` glued to a
   `FAILED` line made one test read as both new and disappeared during the Phase 260 review.
   Criterion: the failing SET at HEAD is a subset of the SET at base; the ceiling is **71 failed**
   with zero headroom.
4. `node scripts/check-hot-file-ledger.cjs 263` exits 0 with `watched:` > 0.
5. `node scripts/check-schema-acl-parity.cjs` exits 0 and `scripts/full-schema-supplement.sql` is unchanged.
6. `node scripts/check-seeds-register.cjs --phase 263` — read the matched seeds and route them.
   `SEED-303`'s `trigger_paths` name `expert_service.py` and will match; record fold/defer/leave in
   the seed's own frontmatter.
7. Frontend is untouched by this plan, so the vitest count gate is correctly N/A. ⛔ Do not run it
   and do not report a number for it.

## VALIDATION (UAT rows owed by this plan)

| # | Row | How |
|---|---|---|
| U-01 | The column exists and every pre-existing skill is NULL | Operator's own read-back in task 1 step 4. **Closed by task 1.** |
| U-02 | An Expert-born skill resolves for a SECOND org member | ⛔ Cannot be driven until plan 263-04 ships the create-on-approve flow. **Owed to 263-04's UAT.** Recorded here rather than omitted. |
</verification>

<success_criteria>
- Migration 191 applied locally, `full-schema.sql` regenerated from the live DB, no supplement change, ACL parity green.
- `resolve_expert_bundle` admits a born-for skill through its own bundle and through no other.
- A cross-org skill carrying the maximally-privileged marker value is still stripped, counted and logged.
- The save-time helper cannot admit a foreign row through a `None == None` comparison.
- The stamp cannot reach a foreign-org row, another author's row, or an already-claimed row.
- The RED drive fired on the placement plant and the file was restored md5-identical.
- The backend failing SET at HEAD is a subset of the SET at this plan's base commit.
</success_criteria>

<output>
Create `.planning/phases/263-an-expert-can-be-given-its-capabilities/263-01-SUMMARY.md` when done.
Record: the migration read-back output, the RED-drive failing case name with pre/post md5, the
`public.skills` row count before and after the integration run, the re-derived ledger triples, and
the backend failing-SET diff in both directions.
</output>
