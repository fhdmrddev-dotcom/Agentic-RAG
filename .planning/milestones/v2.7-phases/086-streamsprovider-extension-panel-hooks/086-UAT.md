---
status: partial
phase: 086-streamsprovider-extension-panel-hooks
source:
  - 086-01-SUMMARY.md
  - 086-02-SUMMARY.md
  - 086-VERIFICATION.md (3 human-verification items)
  - quick task 260529-0sc (WR-04 write-path fix)
started: 2026-05-28T20:44:55Z
updated: 2026-05-28T20:55:00Z
driver: Chrome DevTools MCP (orchestrator-driven, logged in as fhdmrd@gmail.com)
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

[testing complete — driven via Chrome DevTools MCP]

## Tests

### 1. Cold Start Smoke Test
expected: Fully stop the frontend dev server and backend, then start both fresh and hard-reload the browser. The app boots, login works, the thread list and a prior conversation load with no red Console errors. (Cache v1->2 bump means one expected one-time cold reload of cached chat state.)
result: pass
evidence: |
  Chrome MCP — navigated fresh to http://localhost:5173 (backend already running, serving 200s).
  App booted, session/login active, full thread list rendered, default chat screen shown.
  list_console_messages (error+warn) = none. Caveat: backend was already running (operator-owned),
  not restarted — frontend cold-load + boot verified clean.

### 2. Panel Data Survives Hard-Refresh (WR-04 fix)
expected: |
  This verifies the gap just closed by quick task 260529-0sc. In a thread, get the agent to
  use a todo/task tool (e.g. ask it to "make a to-do list" so a write_todos / todo_updated
  event fires, or run something that spawns a sub-task). Then hard-refresh the page (Ctrl+F5)
  and immediately — BEFORE switching threads — check that the todo/task data is still there
  on first paint (hydrated from localStorage, not fetched).

  Since the panel UI ships in Phase 087, observe via DevTools Console with this one-liner
  (paste and run after refresh, before any thread-switch):

    (() => { const k = Object.keys(localStorage).find(x => x.startsWith('streams-cache:')); const s = k && JSON.parse(localStorage.getItem(k)); return { version: s?.version, todoThreads: Object.keys(s?.todosByThread ?? {}), taskThreads: Object.keys(s?.tasksByThread ?? {}) }; })()

  EXPECTED: version is 2, and todoThreads / taskThreads include the thread id you just used
  (non-empty). Before this phase's fix, those arrays were always empty — the write path never
  persisted them.

  NOTE: the live localStorage key prefix is `agentic-rag.streams.v1.<userId>` (not
  `streams-cache:`); the snapshot's internal `version` field is 2.
result: pass
evidence: |
  Chrome MCP. OpenAI run created thread 42f3fa81 and called write_todos (run card: "1 tool · done").
  Read live snapshot key agentic-rag.streams.v1.d8a54002...: version=2, todosByThread[42f3fa81]
  = 3 todos with step 1 status="completed" (matches the prompt). taskThreadIds empty (no task tool
  this run — expected). Hard-reloaded with ignoreCache=true: snapshot SURVIVED — version still 2,
  3 todos, step1 completed. Combined with the code-verified synchronous factory hydration
  (readTodosSyncOrEmpty at streamsStore.ts:200), first-paint F5-resilience is restored.
  Console clean before and after reload. This is the exact behavior WR-04 reported as dead.

### 3. Cross-Provider SSE Routing + Chat Isolation (multi-tool x cross-provider)
expected: |
  Send a prompt that triggers at least one panel tool (e.g. write_todos AND a workspace
  write, or a sub-task), once on EACH provider class: OpenAI, Anthropic, Google, OpenRouter,
  DeepSeek, and Moonshot (6 native providers — extended per operator request).
  For each: the agent streams and finishes normally, the assistant message renders in chat,
  and the DevTools Console shows NO errors. The legacy document-analysis flow (analyze_document
  sub-agent) should still behave exactly as before. Nothing in the chat message list should
  flicker/re-render when a panel event arrives (PANEL-06 isolation).
result: pass (all 6 providers route correctly — DB-confirmed; 2 non-persists were model weakness + backend BUG-260529-01, not 086 — see verdict)
provider_matrix:
  - OpenAI (gpt-5.4-mini): pass — thread 42f3fa81, write_todos 1-tool done, run card rendered, console clean, todoByThread persisted (3 todos, step1 completed), chat not re-rendered by panel event.
  - Anthropic (claude-opus-4-6): pass — thread 0540ab00, write_todos DONE, run card "1 tool done", reply rendered, console clean, todos persisted independently (3 todos, step1 completed).
  - Google: routing PASS. gemini-2.5-flash did NOT invoke write_todos (narrated; backend /todos=0, snapshot toolCalls=[]) — model weakness. Operator retried same prompt on gemini-3.5-flash → SUCCESS, 3 todos persisted (DB-confirmed). DECISION: focus Google on 3.x+ models; 2.5 series deprioritized/neglected. Routing is provider-agnostic and proven once a capable model emits the call.
  - OpenRouter: routing PASS. The FREE meta-llama/llama-3.3-70b looped 14x — ROOT CAUSE (DB-confirmed, NOT a model "no-op"): it emitted `todos` as a JSON STRING, and the backend write_todos handler crashed on it ('str' object has no attribute 'get' x14 in backend log) instead of parsing/erroring gracefully → see BUG-260529-01. Operator retried same prompt on glm, gemma, deepseek-r1 → ALL SUCCEEDED, todos persisted (DB-confirmed). So OpenRouter routing is fine; the free llama is just too weak + tripped a backend tool bug. SSE routing rendered all iterations, ZERO console errors, frontend matched backend (persisted nothing while tool errored).
  - DeepSeek (deepseek-v4-flash): pass — thread 820b9a89, "Run · 1 tool · done", reply confirms list+step1, console clean, todos persisted (3, step1 completed).
  - Moonshot (kimi-k2.6): pass — thread 1563146f, "Run · 1 tool · done" (16.8s), reply confirms list+step1, console clean, todos persisted (3, step1 completed).
verdict: |
  PASS for Phase 086. ALL 6 providers' panel SSE routing works (provider-agnostic shared code:
  api.ts dispatch + makeStreamCallbacks). DB-confirmed successful write_todos persistence on
  OpenAI, Anthropic, DeepSeek, Moonshot (orchestrator runs) + OpenRouter glm/gemma/deepseek-r1
  and Google gemini-3.5-flash (operator retries). The only 2 non-persisting runs were NOT routing
  failures: gemini-2.5-flash (didn't emit the tool call — model weakness, deprioritized) and free
  llama-3.3-70b (tripped backend BUG-260529-01 — write_todos crashes on a stringified `todos` arg).
  In every case the frontend matched backend state with zero chat re-render and zero console errors.
  PANEL-05 + PANEL-06 hold across the matrix.
follow_ups_out_of_scope:
  - "BUG-260529-01 (backend, major): write_todos crashes ('str' object has no attribute 'get') when a model sends `todos` as a JSON string instead of an array. Free llama-3.3-70b triggers it; capable models send a real array. Fix is ~5 lines (json.loads + isinstance guard) in tool_dispatcher._handle_write_todos + todos_service.replace_todos. NOT a Phase 086 defect."
  - "Google gemini-2.5-flash unreliable at invoking write_todos (narrates instead of calling). gemini-3.5-flash works. DECISION: focus Google on 3.x+; deprioritize 2.5 series."
db_evidence:
  - "Threads with persisted todos (successful write_todos, DB-confirmed): 42f3fa81 (OpenAI), 0540ab00 (Anthropic), 820b9a89 (DeepSeek), 1563146f (Moonshot), + operator retries a8e8d19f/17e5fa03/908b7c59/1dc51f5e (glm/gemma/deepseek-r1/gemini-3.5-flash) — all 3 todos, step1 completed."
  - "Threads with NO persisted todos (the only 2 failures): 81d39db9 (gemini-2.5-flash, no tool call) and 6e284fb5 (free llama, stringified-arg crash)."

### 4. Rapid Thread-Switch Abort Window (parallel-thread)
expected: |
  Start a tool-heavy run on Thread A, then IMMEDIATELY switch to Thread B while A is still
  streaming / its reconcile fetch is in flight. EXPECTED: Thread B shows ITS OWN data (no
  bleed-in of Thread A's todos/tasks), no crash, and in DevTools Network the in-flight GET
  for Thread A's panel data shows as cancelled/aborted. Switching back to A later still shows
  A's correct data.
result: blocked
blocked_by: prior-phase
reason: |
  Not exercisable in the live app yet. usePanelReconcile (the AbortController-guarded
  thread-switch reconcile) only fires when a component MOUNTS one of the named hooks
  (useTodos/useWorkspaceFiles/useAskUserPrompt/useTasks). Those consumers ship in Phase 087
  (panel UI) — not built yet. Verified via Chrome MCP: switching threads (blog 42f3fa81 ->
  party 0540ab00) fired ONLY GET /snapshot for each; ZERO panel reconcile GETs
  (/todos|/pending|/workspace|/tasks) were issued by the app. The abort + no-cross-thread-write
  invariant is covered by unit test FC#5 in panelHooks.test.tsx (GREEN per Plan 02). Re-run
  this live test during Phase 087 verification once the hooks are mounted.

### 5. Long-Message Axis (SC#10 axis 4)
expected: |
  In a thread with a large history (>= 50 prior messages) OR with a >= 5 KB user prompt,
  send a prompt that triggers write_todos and/or a workspace write. EXPECTED: the todo_updated
  / workspace_file_written events still arrive and update state with no stream drop, timeout,
  or Console error — on the same thread, without needing a thread-switch.
result: pass
evidence: |
  Chrome MCP, Anthropic (claude-opus-4-6), party thread 0540ab00 (already had 2 prior large
  turns + tool calls). Three escalating prompts on the SAME thread: ~3.5KB, ~4KB, then a
  decisive 14,840-byte (>5KB) prompt (60-requirement operational brief). Every turn:
  write_todos fired (final run "Run · 1 tool · done", write_todos DONE 20ms), todo Map
  full-replaced correctly each time (final: "Finalize the venue contract" completed +
  2 pending), NO stream drop / timeout, ZERO console errors. todosByThread persisted
  across all turns. SC#10 axis 4 satisfied with a genuine >5KB prompt AND a long cumulative
  thread context.

## Summary

total: 5
passed: 4
issues: 0
blocked: 1
pending: 0
skipped: 0

## Gaps

[no code defects found]

WR-04 (the sole blocking gap from 086-VERIFICATION.md) is now LIVE-VERIFIED CLOSED via Test 2
(quick task 260529-0sc): the localStorage write-path persists todosByThread + tasksByThread and
survives a hard reload, across OpenAI/Anthropic/DeepSeek/Moonshot threads.

## Notes

- Test 4 (rapid thread-switch reconcile-abort) is BLOCKED on Phase 087: usePanelReconcile only
  fires when a component mounts the named hooks, and no consumer exists until the panel UI ships.
  Verified via Chrome MCP that thread-switch currently issues only GET /snapshot, no panel
  reconcile GETs. Abort logic is unit-tested (FC#5, GREEN). Re-run live during Phase 087 UAT.
- Cross-provider tool-calling caveats (NOT 086 defects, tracked separately):
  gemini-2.5-flash narrates write_todos without calling it / empty response after 2 iters;
  OpenRouter llama-3.3-70b loops calling write_todos with empty args until max-steps.
  Both streamed cleanly with graceful error handling and zero console errors.
