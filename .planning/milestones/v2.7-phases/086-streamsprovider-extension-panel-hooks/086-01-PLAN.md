---
phase: 086-streamsprovider-extension-panel-hooks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/index.ts
  - frontend/src/lib/api.ts
  - frontend/src/stores/streamsStore.ts
  - frontend/src/lib/streamsCache.ts
autonomous: true
requirements: [PANEL-05, PANEL-06]

must_haves:
  truths:
    - "Backend wire-mirror types Todo, WorkspaceFile, PendingAsk, TaskRunIndexItem exist and match the panel.py/workspace.py JSON field names byte-for-byte"
    - "The 6 new SSE event types (todo_updated, workspace_file_written, workspace_file_deleted, ask_user_prompt, ask_user_response, sub_agent_start/done with sub_run_id) route to dedicated store actions"
    - "The legacy analyze_document sub_agent_start/delta/done dispatch is byte-identical post-Phase-086 (cross-provider safety)"
    - "Panel events mutate only the 4 new per-thread Maps via immutable Map-replace, so chat message selectors never re-render (PANEL-06)"
    - "todosByThread and tasksByThread hydrate from localStorage on first paint; pendingAsks and workspaceFiles do not"
  artifacts:
    - path: "frontend/src/types/index.ts"
      provides: "Todo, WorkspaceFile, PendingAsk, TaskRunIndexItem wire-mirror interfaces"
      contains: "export interface Todo"
    - path: "frontend/src/lib/api.ts"
      provides: "7 new StreamCallbacks callbacks + 6 SSE dispatch branches (payload-shape branch for sub_agent_*) + 4 GET helpers"
      contains: "getThreadTodos"
    - path: "frontend/src/stores/streamsStore.ts"
      provides: "4 new per-thread Maps + 11 action signatures + synchronous no-op stubs + factory defaults"
      contains: "todosByThread"
    - path: "frontend/src/lib/streamsCache.ts"
      provides: "todosByThread + tasksByThread persisted slots, version bump 1->2, user-scoped key reuse"
      contains: "STREAMS_CACHE_VERSION = 2"
  key_links:
    - from: "frontend/src/lib/api.ts dispatch chain"
      to: "callbacks.onTaskStart / callbacks.onSubAgentStart"
      via: "parsed.sub_run_id != null discriminator (D-086-06)"
      pattern: "sub_run_id != null"
    - from: "frontend/src/stores/streamsStore.ts action bodies"
      to: "todosByThread / workspaceFilesByThread / pendingAsksByThread / tasksByThread"
      via: "immutable new Map(prev) replace"
      pattern: "new Map\\("
---

<objective>
Extend the data-plumbing layer (types, store Maps, SSE dispatch, GET helpers, cache) so the 6 new SSE event types from Phases 084/085 demux into 4 dedicated per-thread Zustand Maps, and so the 4 backend GETs are callable for thread-switch reconcile. Zero UI rendering — that is Phase 087.

Purpose: PANEL-05 (single SSE stream, demultiplexed by event type) and PANEL-06 (panel events route to separate state stores, never re-render chat) are achieved structurally at the dispatch + store layer here. Plan 086-02 builds the consumer hooks on top.

Output: Modified frontend/src/types/index.ts, api.ts, streamsStore.ts, streamsCache.ts — strictly additive (D-086-18; existing handlers untouched).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/086-streamsprovider-extension-panel-hooks/086-CONTEXT.md
@.planning/phases/086-streamsprovider-extension-panel-hooks/086-PATTERNS.md

<interfaces>
<!-- VERIFIED backend wire shapes (read directly from emit sites + GET reshapes 2026-05-28). -->
<!-- These OVERRIDE the placeholder field names in 086-PATTERNS.md §4/§9. Use these exact names. -->

GET /threads/{tid}/todos  (panel.py:67, reshapes todo_id -> id):
  { id, content, status, parent_id, order_index, created_at, updated_at }

SSE todo_updated  (tool_dispatcher.py:1239):
  { type: "todo_updated", todos: [ { id, content, status, parent_id, order_index } ] }
  NOTE: full-state-replace — `todos` is the complete canonical list, not a delta.

GET /threads/{tid}/workspace/files  (workspace.py:99):
  [ { id, path, size_bytes, mime_type, created_at, updated_at } ]

SSE workspace_file_written  (tool_dispatcher.py:892) — FLAT payload, NOT a nested `file`:
  { type: "workspace_file_written", path, version, size_bytes, mime_type }
  NOTE: no `id` in the SSE payload. The store keys workspace files by `path`.

SSE workspace_file_deleted  (tool_dispatcher.py:987) — keyed by `path`, NOT `file_id`:
  { type: "workspace_file_deleted", path }

GET /threads/{tid}/ask_user/pending  (panel.py:103):
  [ { message_id, tool_call_id, prompt, options, timeout_seconds, run_id, created_at } ]

SSE ask_user_prompt  (tool_dispatcher.py:1357) — FLAT payload, NOT a nested `ask`:
  { type: "ask_user_prompt", tool_call_id, prompt, options, timeout_seconds }
  NOTE: no `message_id`/`run_id`/`created_at` on the SSE (those only exist on the GET).
  The PendingAsk identity key is `tool_call_id`.

SSE ask_user_response  (runs.py:560) — removal key is `tool_call_id`, NOT `ask_id`:
  { type: "ask_user_response", tool_call_id, response_text, choice_index }

GET /threads/{tid}/tasks  (panel.py:156):
  [ { sub_run_id, parent_run_id, status, model, provider, started_at, completed_at } ]

SSE sub_agent_start (TASK variant, task_service.py:264) — has sub_run_id:
  { type: "sub_agent_start", sub_run_id, description, tools, max_steps }
SSE sub_agent_done  (TASK variant, task_service.py:428) — has sub_run_id:
  { type: "sub_agent_done", sub_run_id, status, summary }

SSE sub_agent_start (LEGACY analyze_document, tool_dispatcher.py:235) — NO sub_run_id:
  { type: "sub_agent_start", filename, task }
SSE sub_agent_done  (LEGACY analyze_document, tool_dispatcher.py:276) — NO payload.
SSE sub_agent_delta (LEGACY, tool_dispatcher.py:270) — { content }. LEAVE UNTOUCHED.

Existing api.ts fetch-helper analog (getActiveRuns @575-586): getAuthHeaders() + fetch with optional AbortSignal + non-OK throw. Reuse verbatim; NO new fetch library.
Existing streamsStore.ts import (line 38): `import type { Message } from "@/types"`.
Existing cache version (streamsCache.ts:41): `STREAMS_CACHE_VERSION = 1 as const`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add 4 wire-mirror types + extend StreamsState with 4 Maps and 11 action stubs</name>
  <files>frontend/src/types/index.ts, frontend/src/stores/streamsStore.ts</files>
  <read_first>
    - frontend/src/types/index.ts (find the existing wire-mirror interfaces; api.ts:185-202 ActiveRun/ThreadSnapshot are the role-match analog per PATTERNS §9)
    - frontend/src/stores/streamsStore.ts in full (146 LOC) — interface decl block 62-79, factory defaults 127-135, action stubs 136-145, actions-interface block 80-103, import line 38
    - The <interfaces> block above for the EXACT field names (overrides PATTERNS placeholders)
  </read_first>
  <action>
    In frontend/src/types/index.ts add 4 exported interfaces using the EXACT field names from the <interfaces> block: `Todo` (id, content, status, parent_id, order_index, created_at?, updated_at?), `WorkspaceFile` (id, path, size_bytes, mime_type, created_at?, updated_at?, version?), `PendingAsk` (tool_call_id, prompt, options, timeout_seconds, message_id?, run_id?, created_at?), `TaskRunIndexItem` (sub_run_id, parent_run_id, status, model, provider, started_at?, completed_at?, and optional description/tools/max_steps/summary populated from the sub_agent SSE bookends). Field names are snake_case to mirror backend JSON (no client-side reshape — backend already reshapes todo_id->id).

    In frontend/src/stores/streamsStore.ts:
    1. Extend the import on line 38 to also import type { Todo, WorkspaceFile, PendingAsk, TaskRunIndexItem } from "@/types".
    2. Add 4 fields to the StreamsState interface (in the 62-79 per-thread block, mirroring streamingThreads/fallbackNotices/reconcileErrors verbatim): `todosByThread: Map<string, Todo[]>`, `workspaceFilesByThread: Map<string, WorkspaceFile[]>`, `pendingAsksByThread: Map<string, PendingAsk[]>`, `tasksByThread: Map<string, TaskRunIndexItem[]>`.
    3. Add 4 factory defaults (127-135 block, with the type-annotation comment ABOVE each per D-075.4-A1 convention): `new Map<string, Todo[]>()` etc.
    4. Add 11 action signatures to the actions-interface block (80-103) mirroring setMessagesForBucket's shape: `setTodosForThread`, `replaceTodosForThread`, `setWorkspaceFileForThread`, `removeWorkspaceFileForThread`, `replaceWorkspaceFilesForThread`, `addPendingAskForThread`, `removePendingAskForThread`, `replacePendingAsksForThread`, `setTaskForThread`, `updateTaskStatusForThread`, `replaceTasksForThread`. Use precise arg signatures: removePendingAskForThread takes (threadId, toolCallId); removeWorkspaceFileForThread takes (threadId, path); updateTaskStatusForThread takes (threadId, subRunId, status, summary).
    5. Add 11 synchronous no-op stubs in the factory actions object (136-145 block) as `() => {}` — NOT notMounted — per PATTERNS §1 Part C / RESEARCH Pitfall 5 (they can fire pre-mount).
    Do NOT touch any existing field, default, or action. Strictly additive (D-086-18).
  </action>
  <verify>
    <automated>cd frontend; npx tsc --noEmit -p tsconfig.json 2>&1 | Select-String -Pattern "streamsStore|types/index" -SimpleMatch -NotMatch | Out-Null; npx tsc --noEmit 2>&1 | Select-Object -First 5</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: frontend/src/types/index.ts exports exactly 4 new interfaces; PendingAsk has a `tool_call_id` field and NO `ask_id` field; WorkspaceFile has a `path` field used as the identity key.
    - SOURCE: streamsStore.ts StreamsState has the 4 new `Map<string, T[]>` fields and the factory creates 4 new empty Maps with type-annotation comments.
    - SOURCE: 11 new action stubs are synchronous `() => {}` (grep confirms none use `notMounted`).
    - SOURCE: zero diff lines remove or alter any pre-existing StreamsState field, default, or action (additive-only — `git diff` shows only insertions in those blocks).
    - TEST: `npx tsc --noEmit` reports no new type errors introduced by these files.
  </acceptance_criteria>
  <done>4 types exist with verified backend field names; StreamsState carries 4 new Maps; 11 action signatures + synchronous no-op stubs registered; no existing symbol modified.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add 7 StreamCallbacks + 6 SSE dispatch branches (payload-shape branch) + 4 GET helpers in api.ts</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts:209 (StreamCallbacks interface) + 251-279 (existing optional callback decls — analog)
    - frontend/src/lib/api.ts:419-468 (SSE dispatch chain) — ESPECIALLY 440-445 (the legacy analyze_document sub_agent_start/delta/done lines that MUST stay byte-identical) and the terminal `else if (t === "done")` ~489
    - frontend/src/lib/api.ts:534-544 (onCursor block — new branches must sit ABOVE it and must NOT `return`)
    - frontend/src/lib/api.ts:575-586 (getActiveRuns) + 601-621 (getSnapshot) — GET-helper analog; line 11 getAuthHeaders
    - The <interfaces> block above for EXACT event field names + the sub_run_id discriminator
    - PATTERNS.md §3, §4, §7 (verbatim copy templates)
  </read_first>
  <action>
    1. StreamCallbacks interface (~api.ts:209): add 7 optional callbacks with docstrings matching existing style: `onTodoUpdated?: (todos: Todo[]) => void`, `onWorkspaceFileWritten?: (file: WorkspaceFile) => void`, `onWorkspaceFileDeleted?: (path: string) => void`, `onAskUserPrompt?: (ask: PendingAsk) => void`, `onAskUserResponse?: (toolCallId: string) => void`, `onTaskStart?: (subRunId: string, description: string, tools: string[], maxSteps: number) => void`, `onTaskDone?: (subRunId: string, status: string, summary: string) => void`. Import the 4 types from @/types at top of api.ts.

    2. SSE dispatch (api.ts:419-468) — payload-shape branch per D-086-06. REWRITE the two bookend branches at 440-445 ONLY: `else if (t === "sub_agent_start")` becomes a block — `if (parsed.sub_run_id != null) callbacks.onTaskStart?.(parsed.sub_run_id, parsed.description, parsed.tools, parsed.max_steps)` else preserve the EXACT legacy 2-arg call `callbacks.onSubAgentStart(parsed.filename as string, parsed.task as string)` guarded by `else if (callbacks.onSubAgentStart)`. Same shape for sub_agent_done: `if (parsed.sub_run_id != null) callbacks.onTaskDone?.(parsed.sub_run_id, parsed.status, parsed.summary)` else `callbacks.onSubAgentDone()` guarded by `else if (callbacks.onSubAgentDone)`. The `sub_agent_delta` branch is UNTOUCHED.

    3. Add 4 plain new branches (no payload-shape branching) inserted ABOVE the terminal `else if (t === "done")`, using EXACT field names from <interfaces>: `todo_updated` -> onTodoUpdated(parsed.todos); `workspace_file_written` -> onWorkspaceFileWritten({ path, version, size_bytes, mime_type } — build the WorkspaceFile from the FLAT SSE fields, since the SSE has no nested `file` and no `id`); `workspace_file_deleted` -> onWorkspaceFileDeleted(parsed.path); `ask_user_prompt` -> onAskUserPrompt({ tool_call_id, prompt, options, timeout_seconds } built from FLAT fields); `ask_user_response` -> onAskUserResponse(parsed.tool_call_id). NONE of these may carry a `return` (cursor-advance safety, PATTERNS §4).

    4. Add 4 GET helpers mirroring getActiveRuns (getAuthHeaders + optional AbortSignal + non-OK throw, NO new fetch library): `getThreadTodos(threadId, signal?): Promise<Todo[]>` -> GET ${API_BASE}/threads/${threadId}/todos; `getThreadWorkspaceFiles(threadId, signal?): Promise<WorkspaceFile[]>` -> GET .../workspace/files; `getThreadPendingAsks(threadId, signal?): Promise<PendingAsk[]>` -> GET .../ask_user/pending; `getThreadTasks(threadId, signal?): Promise<TaskRunIndexItem[]>` -> GET .../tasks. Backend already reshapes (todo_id->id) so no client mapper is needed; cast the JSON to the typed array.
  </action>
  <verify>
    <automated>cd frontend; npx tsc --noEmit 2>&1 | Select-String -Pattern "api.ts" | Select-Object -First 5; if ($LASTEXITCODE -ne 0) { "tsc had errors — inspect above" }</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE (LEGACY BYTE-IDENTICAL — D-086-06, failure mode #3/#4): the analyze_document call site is preserved verbatim as `callbacks.onSubAgentStart(parsed.filename as string, parsed.task as string)` and `callbacks.onSubAgentDone()`; `git diff api.ts` shows these exact call expressions still present (only their guard wrapper changed). The `sub_agent_delta` branch is unchanged.
    - SOURCE (discriminator): the ONLY thing routing to onTaskStart/onTaskDone vs the legacy callbacks is `parsed.sub_run_id != null` (grep confirms exactly this expression in both bookend branches).
    - SOURCE (no terminal return): none of the 4 new plain branches contain `return`; they sit above the onCursor block.
    - SOURCE (wire fidelity): the new branches read `parsed.todos`, `parsed.path`, `parsed.tool_call_id` (NOT `parsed.file`, `parsed.file_id`, or `parsed.ask_id`).
    - SOURCE: 4 GET helpers exist, each calls getAuthHeaders, accepts an optional AbortSignal, throws on non-OK, returns the typed array.
    - TEST: `npx tsc --noEmit` produces no new errors in api.ts.
  </acceptance_criteria>
  <done>7 callbacks declared; sub_agent_* payload-shape branch splits new task payload from byte-identical legacy path; 4 plain branches route by verified field names without terminal return; 4 GET helpers callable.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend streamsCache.ts persisted shape with todosByThread + tasksByThread and bump version 1->2</name>
  <files>frontend/src/lib/streamsCache.ts</files>
  <read_first>
    - frontend/src/lib/streamsCache.ts in full (316 LOC) — version const line 41, SerializedSnapshot shape 113-121, readSnapshotSyncOrEmpty 130-166 (version-guard + structural rebuild), writeSnapshotToLocalStorage 182-275 (incl. QuotaExceeded fallback 260-270), streamsCacheKey user-scoped key 80-82
    - frontend/src/stores/streamsStore.ts factory body ~line 117 (where readSnapshotSyncOrEmpty is called for first-paint hydration)
    - PATTERNS.md §8 + D-086-03 (ONLY todos+tasks cached; NOT pendingAsks/workspaceFiles) + D-086-04 (single minor bump)
  </read_first>
  <action>
    1. Bump `STREAMS_CACHE_VERSION` from `1 as const` to `2 as const` (single minor bump — invalidates ALL chat-bucket caches too; one-time cold reload accepted per <specifics>; document in commit message).
    2. Extend the SerializedSnapshot interface with two OPTIONAL slots: `todosByThread?: Record<string, Todo[]>` and `tasksByThread?: Record<string, TaskRunIndexItem[]>`. Do NOT add pendingAsks (ephemeral, D-086-03) or workspaceFiles (large blobs, D-086-03). Import the 2 types from @/types.
    3. Read side: add the two slots to the structural rebuild inside readSnapshotSyncOrEmpty (or add parallel readTodosSyncOrEmpty()/readTasksSyncOrEmpty() helpers) following the same try/catch + version-guard + Object->Map rebuild discipline. The version guard already drops the key on mismatch — that closes failure mode #8 for the bumped version.
    4. Write side: add the two slots to the serialized `snapshot` object in writeSnapshotToLocalStorage (serialize the two Maps as Object records). Reuse streamsCacheKey (user-scoped) and the QuotaExceeded fallback unchanged.
    5. Wire the new reads into the streamsStore.ts factory body (~line 117) so `todosByThread` + `tasksByThread` hydrate on first paint alongside bucketsBySurface. pendingAsksByThread + workspaceFilesByThread initialize EMPTY (no hydration).
    Strictly additive to the persisted shape; the user-scoped key + QuotaExceeded fallback are reused unchanged.
  </action>
  <verify>
    <automated>cd frontend; npx tsc --noEmit 2>&1 | Select-String -Pattern "streamsCache|streamsStore" | Select-Object -First 5</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `STREAMS_CACHE_VERSION === 2`.
    - SOURCE: SerializedSnapshot has exactly two new optional slots (todosByThread, tasksByThread); grep confirms NO `pendingAsks` or `workspaceFiles` slot was added to the serialized shape (D-086-03).
    - SOURCE (failure mode #8 — cross-user leak): the cache read still goes through streamsCacheKey(userId) (user-scoped) AND the version-guard drops the key when `parsed.version !== STREAMS_CACHE_VERSION`; both lines unchanged in the read path.
    - SOURCE: streamsStore.ts factory hydrates todosByThread + tasksByThread on first paint; pendingAsksByThread + workspaceFilesByThread start as empty Maps.
    - TEST: `npx tsc --noEmit` produces no new errors in streamsCache.ts or streamsStore.ts.
  </acceptance_criteria>
  <done>Cache version is 2; only todos+tasks persisted and hydrated on first paint; user-scoped key + version guard intact (closes cross-user-leak threat); write/read symmetric.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend SSE/REST -> frontend store | Server-emitted event/JSON payloads cross into client reactive state. Already authenticated + RLS-protected (panel.py/workspace.py verify thread ownership; 084/085 shipped + tested). Phase 086 adds NO new network/auth surface — it consumes 4 already-shipped GETs + parses existing SSE events. |
| localStorage <-> hydrated panel state | Cached `todosByThread`/`tasksByThread` persisted under a user-scoped key are read back on first paint. The boundary is the same-origin localStorage namespace, shared across users on a shared device. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-086-01 | Information Disclosure | streamsCache.ts localStorage hydration of todosByThread/tasksByThread (failure mode #8) | mitigate | Cache key is user-scoped via streamsCacheKey(userId) (reused unchanged); on user-switch the new userId yields a different key so user A's cache is never read into user B's session. The version bump 1->2 additionally drops all v1 keys on first read. Acceptance criteria in Task 3 assert both lines are preserved. |
| T-086-02 | Tampering | api.ts SSE dispatch parsing untrusted `parsed.*` fields into typed callbacks | accept | Same-origin authenticated EventSource; payloads originate from the user's own RLS-scoped runs. No new privilege boundary crossed. Type casts are display-only data (read-only panel per D-086-11); no eval/DOM-injection introduced in this plan (rendering is Phase 087). |
| T-086-03 | Spoofing/Routing | sub_agent_start payload-shape branch misrouting legacy analyze_document vs new task payload (failure mode #3) | mitigate | `parsed.sub_run_id != null` is the sole discriminator; legacy 2-arg call kept byte-identical. Verified by Task 2 acceptance criteria + Plan 02 E2E backstop + VALIDATION row mapping FC#3/#4. |

No new high-severity (ASVS L1) findings: zero new endpoints, zero new auth surface, zero PII persistence beyond the already-shipped, RLS-protected, user-scoped cache.
</threat_model>

<verification>
- `npx tsc --noEmit` passes (no new type errors) after all 3 tasks.
- `git diff frontend/src/lib/api.ts` shows the legacy analyze_document call expressions preserved verbatim; sub_agent_delta untouched.
- grep `parsed.sub_run_id != null` returns exactly the two bookend branches.
- grep confirms streamsCache persists only todosByThread + tasksByThread (no pendingAsks/workspaceFiles).
- `STREAMS_CACHE_VERSION === 2`.
</verification>

<success_criteria>
- 4 wire-mirror types exist with backend-verified field names (PendingAsk keyed by tool_call_id; WorkspaceFile keyed by path).
- StreamsState carries 4 new per-thread Maps + 11 additive actions; no existing symbol altered (D-086-18).
- api.ts demuxes the 6 new SSE events; legacy analyze_document path byte-identical (D-086-06); 4 GET helpers callable.
- Cache hydrates todos + tasks on first paint under a user-scoped, version-guarded key.
- PANEL-05 (single demuxed stream) + PANEL-06 (separate stores) achieved structurally at the dispatch + store layer.
</success_criteria>

<output>
After completion, create `.planning/phases/086-streamsprovider-extension-panel-hooks/086-01-SUMMARY.md`
</output>
