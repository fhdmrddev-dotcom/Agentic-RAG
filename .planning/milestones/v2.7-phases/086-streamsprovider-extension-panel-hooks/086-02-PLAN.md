---
phase: 086-streamsprovider-extension-panel-hooks
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - frontend/src/hooks/usePanelReconcile.ts
  - frontend/src/providers/StreamsProvider.tsx
  - frontend/src/providers/__tests__/panelHooks.test.tsx
autonomous: true
requirements: [PANEL-05, PANEL-06]

must_haves:
  truths:
    - "useTodos / useWorkspaceFiles / useAskUserPrompt / useTasks return { data, isLoading, error, reconcile } and never return undefined data"
    - "Each hook reconciles via its own GET on thread-switch (useEffect on [threadId]) and aborts the in-flight fetch on cleanup (no stale cross-thread data)"
    - "A failure in one hook's reconcile (e.g. workspace/files 500) does not break the other 3 hooks (per-hook composite error key threadId:hookId)"
    - "useAskUserPrompt returns an array (parallel asks); thread-switch reconcile replaces the inner Map atomically"
    - "The 7 makeStreamCallbacks default handlers write to the new store actions so live SSE events update the 4 Maps without re-rendering chat"
  artifacts:
    - path: "frontend/src/hooks/usePanelReconcile.ts"
      provides: "Shared SWR + AbortController + composite-error-key reconcile helper"
      contains: "AbortController"
    - path: "frontend/src/providers/StreamsProvider.tsx"
      provides: "4 named hooks + 4 EMPTY constants + 11 action bodies + 7 makeStreamCallbacks defaults"
      contains: "export function useTodos"
    - path: "frontend/src/providers/__tests__/panelHooks.test.tsx"
      provides: "Integration backstop for dispatch routing + abort + per-hook error isolation (SC#10 axes 1-3 automated)"
      contains: "sub_run_id"
  key_links:
    - from: "frontend/src/providers/StreamsProvider.tsx useTodos useEffect"
      to: "getThreadTodos -> replaceTodosForThread"
      via: "usePanelReconcile on [threadId] with AbortController cleanup"
      pattern: "controller.abort"
    - from: "frontend/src/providers/StreamsProvider.tsx makeStreamCallbacks"
      to: "useStreamsStore.getState().actions.replaceTodosForThread"
      via: "closure over threadId"
      pattern: "getState\\(\\).actions"
---

<objective>
Build the consumer layer on top of Plan 086-01's plumbing: the 4 named hooks Phase 087 will import, their reconcile-on-thread-switch behavior, the 11 store action bodies, the 7 makeStreamCallbacks default handlers, and an integration backstop covering the failure modes that automated tests can reach.

Purpose: Success Criterion #3 (per-thread hooks reconcile on thread-switch via fetch, D-v2.5-03) and the consumer half of PANEL-05/PANEL-06. Implements D-086-02/09/10/13/14/15.

Output: New frontend/src/hooks/usePanelReconcile.ts (shared SWR helper), modified StreamsProvider.tsx (hooks + action bodies + factory defaults), new integration test file.

Decision (Claude's Discretion per D-086 discussion): FACTOR the shared usePanelReconcile<T> helper rather than inlining 4 copies. Rationale: PATTERNS §5 flags the in-hook reconcile useEffect as the single structurally-NEW shape (composed, not copied from one analog) and the highest-care item; concentrating the AbortController + isLoading + composite-error-key plumbing in one place makes it testable in isolation and removes 4x copy drift risk. The 4 hooks become thin wrappers (selector + EMPTY guard + helper call).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/086-streamsprovider-extension-panel-hooks/086-CONTEXT.md
@.planning/phases/086-streamsprovider-extension-panel-hooks/086-PATTERNS.md
@.planning/phases/086-streamsprovider-extension-panel-hooks/086-01-SUMMARY.md

<interfaces>
<!-- From Plan 086-01 (consumed here). -->
Store actions (streamsStore.ts): setTodosForThread, replaceTodosForThread, setWorkspaceFileForThread,
  removeWorkspaceFileForThread(threadId, path), replaceWorkspaceFilesForThread,
  addPendingAskForThread, removePendingAskForThread(threadId, toolCallId), replacePendingAsksForThread,
  setTaskForThread, updateTaskStatusForThread(threadId, subRunId, status, summary), replaceTasksForThread.
Store Maps: todosByThread, workspaceFilesByThread, pendingAsksByThread, tasksByThread.
Existing store field reused for errors: reconcileErrors: Map<string, Error> — key as `${threadId}:${hookId}`.
GET helpers (api.ts): getThreadTodos, getThreadWorkspaceFiles, getThreadPendingAsks, getThreadTasks (each (threadId, signal?) -> Promise<T[]>).
Callbacks (api.ts StreamCallbacks): onTodoUpdated, onWorkspaceFileWritten, onWorkspaceFileDeleted, onAskUserPrompt, onAskUserResponse, onTaskStart, onTaskDone.
Hook return contract (D-086-10): { data: T[]; isLoading: boolean; error: Error | null; reconcile: () => Promise<void> }.
WorkspaceFile identity key = path; PendingAsk identity key = tool_call_id (D-086-08 removal key).
</interfaces>

<analog_refs>
<!-- Exact in-file analogs the executor must compose from (PATTERNS §5/§6/§2). DO NOT explore. -->
- useThreadMessages selector + null-safe guard: StreamsProvider.tsx:1540-1546 (+ 1589-1599 thread-scoped selectors).
- EMPTY constant: StreamsProvider.tsx:86 (`const EMPTY_ARRAY: Message[] = []`).
- AbortController + post-await thread guard + reconcileErrors set/clear: StreamsProvider.tsx:1348-1430 (set at 1423-1428, clear at 1386-1392), cleanup abort at 1483-1490.
- DO NOT copy useEffect #2 (visibility/focus listeners) at 1456-1481 — D-086-15 excludes these for panel hooks.
- Immutable Map-replace action body: StreamsProvider.tsx:717-731 (setMessagesForBucket); delete-key: 1386-1392.
- makeStreamCallbacks factory + getState().actions indirection: StreamsProvider.tsx:241-278; setMessagesForBucketBound helper at 709-712; call sites at 901 + 1104 already pass threadId.
- Named-hook block header: StreamsProvider.tsx:1535-1538.
</analog_refs>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add usePanelReconcile helper + 11 action bodies + 7 makeStreamCallbacks defaults</name>
  <files>frontend/src/hooks/usePanelReconcile.ts, frontend/src/providers/StreamsProvider.tsx</files>
  <read_first>
    - 086-01-SUMMARY.md (confirm exact action names + GET helper names as shipped)
    - StreamsProvider.tsx:241-278 (makeStreamCallbacks factory + onTerminal no-op + onDone closure analog), 709-712 (setMessagesForBucketBound), 717-731 (immutable Map-replace), 1348-1430 (AbortController + thread guard + error set/clear), 1386-1392 (delete-key)
    - The <interfaces> + <analog_refs> blocks above
    - PATTERNS.md §2 (action bodies), §6 (factory defaults), §5 (reconcile shape)
  </read_first>
  <action>
    1. Create frontend/src/hooks/usePanelReconcile.ts exporting `usePanelReconcile<T>(opts: { threadId: string | null; hookId: string; fetcher: (threadId: string, signal?: AbortSignal) => Promise<T[]>; replace: (threadId: string, data: T[]) => void })` returning `{ isLoading, error, reconcile }`. Internals (composed from analog_refs, NOT copied from one source): a local `isLoading` useState (true ONLY during initial reconcile fetch, D-086-10); `error` read from the store via selector keyed `${threadId}:${hookId}` (D-086-13 composite key); a `reconcile` useCallback that creates an AbortController, calls fetcher with signal, on success calls replace(threadId, data) + clears the composite error key, on AbortError returns silently, on other error sets the composite error key; a useEffect on [threadId] that fires reconcile and returns `() => controller.abort()` cleanup (L-068-02). NO visibility/focus listeners (D-086-15). When threadId === null: skip the effect, return isLoading=false, error=null.

    2. In StreamsProvider.tsx, register the 11 action BODIES inside the existing mount-time useEffect setState({ actions: {...} }) block (the 714-1449 registration span). Use the immutable `new Map(prev)` clone-then-set discipline from 717-731 for every body. Specifically: replaceTodosForThread / replaceWorkspaceFilesForThread / replacePendingAsksForThread / replaceTasksForThread atomically set the inner array; setTodosForThread mirrors replace; setWorkspaceFileForThread upserts by `path` (replace matching path or append); removeWorkspaceFileForThread filters the inner array by `path`; addPendingAskForThread appends; removePendingAskForThread filters by `tool_call_id`; setTaskForThread upserts by `sub_run_id`; updateTaskStatusForThread maps the matching sub_run_id to set status+summary. Every body returns a NEW outer Map ref so subscribeWithSelector selectors stay stable (PANEL-06).

    3. In makeStreamCallbacks (241-278), add 7 default handlers that close over threadId and call the store actions via useStreamsStore.getState().actions.X (the getState().actions indirection from 709-712): onTodoUpdated -> replaceTodosForThread(threadId, todos); onWorkspaceFileWritten -> setWorkspaceFileForThread(threadId, file); onWorkspaceFileDeleted -> removeWorkspaceFileForThread(threadId, path); onAskUserPrompt -> addPendingAskForThread(threadId, ask); onAskUserResponse -> removePendingAskForThread(threadId, toolCallId); onTaskStart -> setTaskForThread(threadId, { sub_run_id, description, tools, max_steps, status: "running" }); onTaskDone -> updateTaskStatusForThread(threadId, subRunId, status, summary). The call sites at 901 + 1104 already pass threadId in opts — NO call-site signature change.

    4. Add 4 module-level EMPTY constants (next to EMPTY_ARRAY at line 86): EMPTY_TODOS, EMPTY_FILES, EMPTY_ASKS, EMPTY_TASKS (D-086-05).
  </action>
  <verify>
    <automated>cd frontend; npx tsc --noEmit 2>&1 | Select-String -Pattern "usePanelReconcile|StreamsProvider" | Select-Object -First 5</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: usePanelReconcile creates an AbortController per reconcile and returns `() => controller.abort()` from its useEffect; grep confirms NO `visibilitychange`/`focus`/`pageshow` listener in the file (D-086-15).
    - SOURCE: usePanelReconcile reads error via the composite key `${threadId}:${hookId}` (D-086-13).
    - SOURCE (PANEL-06): every one of the 11 action bodies constructs a `new Map(prev)` before mutating and returns the new outer Map ref (grep `new Map(` count >= 11 in the new action bodies).
    - SOURCE: removePendingAskForThread filters by `tool_call_id`; removeWorkspaceFileForThread filters/upserts by `path` (matches the SSE identity keys from Plan 01).
    - SOURCE: the 7 makeStreamCallbacks defaults use getState().actions and close over threadId; the two existing call sites (901, 1104) are unchanged.
    - SOURCE: 4 EMPTY constants declared at module level.
    - TEST: `npx tsc --noEmit` produces no new errors.
  </acceptance_criteria>
  <done>Shared reconcile helper isolates AbortController + isLoading + composite error key; 11 immutable action bodies + 7 closure-bound default handlers + 4 EMPTY constants registered; live SSE events update the 4 Maps without touching chat buckets.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add the 4 named hooks (useTodos / useWorkspaceFiles / useAskUserPrompt / useTasks)</name>
  <files>frontend/src/providers/StreamsProvider.tsx</files>
  <read_first>
    - StreamsProvider.tsx:1535-1538 (named-hook block header), 1540-1546 (useThreadMessages selector), 1589-1599 (null-safe thread-scoped selectors)
    - frontend/src/hooks/usePanelReconcile.ts (from Task 1)
    - The <interfaces> block above (return contract D-086-10, EMPTY constants from Task 1)
    - PATTERNS.md §5 (hook return contract + reconcile composition) — flagged highest-care item
  </read_first>
  <action>
    In the named-hook block of StreamsProvider.tsx (alongside useThreadMessages, after 1546), add 4 exported hooks, each `(threadId: string | null)` and each returning `{ data, isLoading, error, reconcile }` (D-086-10):
    - useTodos: data = useStreamsStore selector `threadId ? s.todosByThread.get(threadId) ?? EMPTY_TODOS : EMPTY_TODOS`; { isLoading, error, reconcile } = usePanelReconcile({ threadId, hookId: "todos", fetcher: getThreadTodos, replace: replaceTodosForThread }).
    - useWorkspaceFiles: same shape, todosByThread->workspaceFilesByThread, EMPTY_FILES, hookId "files", getThreadWorkspaceFiles, replaceWorkspaceFilesForThread.
    - useAskUserPrompt: returns PendingAsk[] (array — parallel asks, D-085-06); pendingAsksByThread, EMPTY_ASKS, hookId "asks", getThreadPendingAsks, replacePendingAsksForThread (reconcile REPLACES inner Map atomically per D-086-08).
    - useTasks: tasksByThread, EMPTY_TASKS, hookId "tasks", getThreadTasks, replaceTasksForThread.
    Each hook: when threadId === null, the selector returns the EMPTY constant and usePanelReconcile skips reconcile (D-086-09 null-safe). data never undefined (always an EMPTY constant fallback, stable ref). Replace actions are resolved from useStreamsStore actions. NEVER export useStreamsStore (D-086-02 — named-hook layer hides the raw store).
  </action>
  <verify>
    <automated>cd frontend; npx tsc --noEmit 2>&1 | Select-String -Pattern "StreamsProvider" | Select-Object -First 5</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: 4 hooks exported; each returns exactly { data, isLoading, error, reconcile } (D-086-10).
    - SOURCE: data selector returns the matching EMPTY constant when the per-thread Map has no entry OR threadId === null (D-086-09; stable ref for PANEL-06).
    - SOURCE: useAskUserPrompt's data type is PendingAsk[] (array).
    - SOURCE: no `export const useStreamsStore` / `export { useStreamsStore }` added (D-086-02).
    - TEST: `npx tsc --noEmit` produces no new errors.
  </acceptance_criteria>
  <done>4 panel hooks live in StreamsProvider.tsx with null-safe selectors + reconcile via usePanelReconcile; raw store stays hidden.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Integration backstop — dispatch routing, abort, per-hook error isolation (SC#10 axes 1-3 automated)</name>
  <files>frontend/src/providers/__tests__/panelHooks.test.tsx</files>
  <read_first>
    - frontend/src/providers/__tests__ (existing test setup — find the Phase 075.4 dispatch/E2E backstop pattern referenced by failure mode #4; mirror its mock/render harness)
    - frontend/src/lib/api.ts dispatch chain (the SSE parse path under test)
    - 086-VALIDATION.md (the failure-mode -> row mapping these automated rows back)
    - CLAUDE.md "UAT scoreboard recipe" (Plan-05 E2E backstop covers axes 1-3 automated; long-message stays manual)
  </read_first>
  <action>
    Create frontend/src/providers/__tests__/panelHooks.test.tsx covering the failure modes reachable by automated tests (axes 1-3 of SC#10 per CLAUDE.md Plan-05 pattern; axis 4 long-message + cross-provider matrix stay manual in VALIDATION.md):
    1. sub_agent_start/done payload-shape routing (FC#3/#4): feed a parsed `sub_agent_start` WITHOUT sub_run_id -> assert onSubAgentStart(filename, task) fires and onTaskStart does NOT; feed one WITH sub_run_id -> assert onTaskStart(sub_run_id, description, tools, max_steps) fires and onSubAgentStart does NOT. Exercise BOTH on the same thread in sequence (the cross-wiring catch from <specifics>). Assert sub_agent_delta still routes to onSubAgentDelta unchanged.
    2. Event-name fidelity (FC#9): each of todo_updated / workspace_file_written / workspace_file_deleted / ask_user_prompt / ask_user_response routes to its callback with the verified field names (todos / path / tool_call_id).
    3. ask_user_response orphan (FC#6): addPendingAskForThread then dispatch ask_user_response with the same tool_call_id -> assert pendingAsksByThread for the thread no longer contains that ask.
    4. Rapid thread-switch abort (FC#5): mount useTodos(A), switch to B, assert the A fetch's AbortController.abort was called (mock getThreadTodos to capture the signal) and the store never shows B's data under key A.
    5. Per-hook error isolation (D-086-13): make getThreadWorkspaceFiles reject (500) while getThreadTodos resolves -> assert reconcileErrors has `${thread}:files` but NOT `${thread}:todos`, and useTodos.data is populated.
    6. PANEL-06 selector stability (FC#1): dispatch a workspace_file_written and assert the chat message selector (bucketsBySurface) reference is unchanged (no chat re-render).
    Use the existing test harness/mocks; mock api.ts GET helpers + getAuthHeaders. Map each test back to its FC number in a comment.
  </action>
  <verify>
    <automated>cd frontend; npx vitest run src/providers/__tests__/panelHooks.test.tsx 2>&1 | Select-Object -Last 20</automated>
  </verify>
  <acceptance_criteria>
    - TEST: all rows pass (`vitest run` exit 0 for this file).
    - TEST (FC#3/#4): both sub_agent variants asserted on the same thread; legacy 2-arg call asserted; delta unchanged.
    - TEST (FC#5): abort asserted on thread-switch; no cross-thread data bleed.
    - TEST (FC#6): ask removed by tool_call_id.
    - TEST (D-086-13): files error present, todos error absent, todos data populated.
    - TEST (FC#1): chat bucket selector ref stable after a panel event.
    - BEHAVIOR: each test comment names the failure-criteria number it backstops (cross-reference to 086-VALIDATION.md).
  </acceptance_criteria>
  <done>Automated backstop covers FC#1/#3/#4/#5/#6/#9 (SC#10 axes 1-3); cross-provider + long-message remain manual UAT in 086-VALIDATION.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend GET/SSE -> hook reconcile/state | The 4 reconcile fetches + 7 SSE callbacks move server data into per-thread reactive state. Endpoints are RLS-protected + thread-ownership-gated (shipped 084/085). Phase 086 adds NO new endpoint/auth surface. |
| per-thread state isolation | Multiple threads' panel state coexist in one client; the risk is one thread's data surfacing under another thread's key (cross-thread bleed, not cross-user). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-086-04 | Information Disclosure | Rapid thread-switch stale data (FC#5) — A's late fetch resolving into A's key after switch to B, or B's data shown under A | mitigate | AbortController per reconcile aborts the in-flight fetch on [threadId] cleanup (L-068-02), and the helper keys writes by the threadId it was invoked with. Task 3 FC#5 test asserts abort + no cross-thread bleed. |
| T-086-05 | Information Disclosure | localStorage cross-user leak via hydrated todos/tasks (FC#8) | mitigate (inherited from Plan 01) | Plan 086-01 owns the user-scoped cache key + version guard; this plan's hooks read whatever the store hydrated, never bypassing the key. No new persistence added here. |
| T-086-06 | Denial of Service | reconcile fan-out hammering (4 fetches per hook) on noisy tab-switching | accept | D-086-15 deliberately excludes visibility/focus triggers; reconcile fires only on thread-switch + manual escape hatch. 4 parallel GETs per switch is bounded and acceptable for v1 (single batched snapshot endpoint deferred to v2.8). |

No new high-severity (ASVS L1) findings: consumer layer only; same authenticated same-origin surface as Plan 01.
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- `npx vitest run src/providers/__tests__/panelHooks.test.tsx` exits 0.
- grep confirms no visibilitychange/focus listener in usePanelReconcile.ts (D-086-15).
- grep confirms useStreamsStore is not exported (D-086-02).
- Each automated test comment cross-references a failure-criteria number tracked in 086-VALIDATION.md.
</verification>

<success_criteria>
- 4 named hooks return { data, isLoading, error, reconcile }; data never undefined (D-086-10).
- Reconcile fires on thread-switch with AbortController cleanup (SC#3 / D-086-14); no visibility/focus triggers (D-086-15).
- Per-hook error isolation via composite key (D-086-13).
- useAskUserPrompt surfaces a PendingAsk[] (parallel asks).
- Integration backstop GREEN for FC#1/#3/#4/#5/#6/#9 (SC#10 axes 1-3 automated).
</success_criteria>

<output>
After completion, create `.planning/phases/086-streamsprovider-extension-panel-hooks/086-02-SUMMARY.md`
</output>
