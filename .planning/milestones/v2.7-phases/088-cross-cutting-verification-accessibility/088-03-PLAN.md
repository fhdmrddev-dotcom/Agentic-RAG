---
phase: 088-cross-cutting-verification-accessibility
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tests/e2e/scenario-13-workspace-deep-flow.spec.ts
autonomous: true
requirements: [A11Y-01]
user_setup:
  - service: providers
    why: "scenario-13 drives Anthropic + Google (gemini-3.x) live; the run fails LOUDLY (scenario-02 precedent) if a provider/key is missing — the operator must configure both"
    env_vars:
      - name: E2E_USER_EMAIL
        source: "frontend/.env / CI secret (defaults to fhdmrd@gmail.com)"
      - name: E2E_USER_PASSWORD
        source: "frontend/.env / CI secret (defaults to the local test password)"
    dashboard_config:
      - task: "Anthropic API key + Google API key (with a gemini-3.x model available) configured so the live deep flow can run on both providers"
        location: "Settings UI / backend/.env"

must_haves:
  truths:
    - "scenario-13 runs the deep workspace flow on Anthropic AND Google: write file → see in panel → update → view diff → ask_user → respond → resume — with NO page refresh"
    - "On the Google axis it asserts zero 400 INVALID_ARGUMENT (re-verifies the gemini-3 thought-signature hotfix, D-17, at the network level)"
    - "It asserts the panel surfaces appear: file row in the listbox, the diff region, the PendingAskCard, then resume after answering"
    - "It reuses the existing auth / db-teardown / langsmith fixtures (localhost-hard-gated teardown)"
  artifacts:
    - path: "frontend/tests/e2e/scenario-13-workspace-deep-flow.spec.ts"
      provides: "Automated Playwright backstop for the D-15 deep flow on Anthropic + Google + the D-17 thought-signature live re-verify"
      contains: "ask_user"
      min_lines: 120
  key_links:
    - from: "frontend/tests/e2e/scenario-13-workspace-deep-flow.spec.ts"
      to: "fixtures/{auth,db-teardown,langsmith}.fixture.ts"
      via: "import test/expect + teardown + trace assert"
      pattern: "fixtures/auth.fixture"
    - from: "scenario-13 panel assertions"
      to: "FilesSection listbox + VersionDiff region + PendingAskCard"
      via: "role-based selectors"
      pattern: "role=\"(listbox|option|region|group)\""
---

<objective>
Author `scenario-13-workspace-deep-flow.spec.ts` — the automated Playwright backstop for the D-15 deep workspace flow on Anthropic + Google, AND the network-level live re-verification of the gemini-3 thought-signature hotfix (D-17, BUG-260523-02).

The full flow (no page refresh): agent writes a file → user sees it in the panel FILES listbox → agent updates the file → user views the diff → agent calls `ask_user` → user responds → agent resumes (SC#4). On the Google axis, the multi-tool-round chain also asserts zero 400 INVALID_ARGUMENT (the thought-signature regression signal — RESEARCH Pitfall 4 + D-17).

This is the REUSE-AND-EXTEND path (research correction #5): extend the existing `scenario-02-gemini-thought-signature.spec.ts` pattern + the 3 mature fixtures (auth / db-teardown / langsmith). Do NOT build a new harness.

Purpose: the durable CI wire-level regression guard for the deep flow + thought-signature. The operator Chrome MCP lived-experience pass (D-14/G-4) is the companion manual gate authored in Plan 05 / VALIDATION.md.
Output: `frontend/tests/e2e/scenario-13-workspace-deep-flow.spec.ts` (matches the `scenario-\d+-.+\.spec\.ts` testMatch; runs under `npm run e2e`, workers:1).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/088-cross-cutting-verification-accessibility/088-CONTEXT.md
@.planning/phases/088-cross-cutting-verification-accessibility/088-RESEARCH.md
@.planning/phases/088-cross-cutting-verification-accessibility/088-VALIDATION.md

# The template to extend (provider switch + multi-tool prompt + no-400 + run-completion + langsmith assert):
@frontend/tests/e2e/scenario-02-gemini-thought-signature.spec.ts

# The locked design contract — the flow's expected panel surfaces:
@.claude/skills/sketch-findings-agentic-rag/SKILL.md

<interfaces>
<!-- Source-of-truth verified this session. Use directly. -->

Fixtures (frontend/tests/e2e/fixtures/ — all VERIFIED, used by 12 scenarios):
- auth.fixture.ts → `import { test, expect } from "./fixtures/auth.fixture"`; provides `authedPage` (env-driven login, defaults fhdmrd@gmail.com / local pw; asserts the composer post-login).
- db-teardown.fixture.ts → `teardownTestUserData(email)` + `assertNoOrphanedStreamingRuns(email)`; HARD-GATED to localhost (refuses non-local SUPABASE_URL). Call in beforeEach/afterEach (scenario-02 pattern).
- langsmith.fixture.ts → `assertLangSmithTraceExists(runId, "google")` (soft-skips if key unset).

Provider-switch + run boilerplate (COPY from scenario-02-gemini-thought-signature.spec.ts):
- Settings nav: `page.goto("/settings")` with link fallback (scenario-02:49-56).
- Provider select: broad `getByRole("combobox", {name:/provider/i}).or(getByLabel(/provider/i)).selectOption({label:/google/i})` with radio/card fallback (scenario-02:61-72). For Anthropic use `/anthropic/i`.
- Model select: `selectOption({label:/gemini.*3.*flash/i})` for Google (3.x — NOT 2.5, per D-03); for Anthropic `/claude.*haiku|claude.*opus/i`.
- Save: `getByRole("button",{name:/save|apply|update/i}).click()` + `waitForTimeout(500)` (scenario-02:86-93).
- Compose+send: `getByPlaceholder(/message|ask|type a message/i).or(getByRole("textbox").first())`; send button `/send|submit/i` (scenario-02:104-129).
- run_id capture: `page.waitForResponse(resp => /\/threads\/[^/]+\/(messages|runs)/.test(url) && POST && 200|201)` then `body.run_id` (scenario-02:118-138).
- Run completion: `expect(getByRole("button",{name:/stop|cancel/i}).first()).toBeHidden({timeout:120_000})` (scenario-02:143-145).
- 400 capture: `page.on("response", r => r.status()===400 && bad400.push(...))`; assert `bad400.toEqual([])` (scenario-02:110-151).

Panel selectors the flow asserts (RESEARCH §Pattern 4, verified from source):
- File row: `[role="option"]` inside `[role="listbox"][aria-label="Workspace files"]` (FilesSection:170,181)
- Diff region: `[role="region"]` with aria-label starting "Diff v" (VersionDiff:207, e.g. aria-label^="Diff v")
- ask_user card: `[role="group"]` containing the "Needs you" text; free-text `textarea` (id pattern `ask-{id}-free`); `Send Answer` button (PendingAskCard:163-244)
- paused cue: PausedRunCue `[role="status"]` with text "ask_user · awaiting your answer"

Config (playwright.config.ts, VERIFIED): testMatch `/scenario-\d+-.+\.spec\.ts$/`, workers:1 (shared DB-teardown), baseURL http://localhost:5173. Backend restart: scripts/restart-backend.{sh,ps1}.

D-17 / thought-signature (RESEARCH Pitfall 4): Google's SDK round-trips thought_signature only when the full model response is appended to history; our app reconstructs from messages.tool_calls JSONB + manually re-attaches the signature to the Part (google_service.py:184-203). A missing signature → 400 INVALID_ARGUMENT on round 2. The Google axis of this scenario IS the live re-verify.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author scenario-13 — the deep flow as a provider-parameterized test (Anthropic + Google)</name>
  <read_first>
    - frontend/tests/e2e/scenario-02-gemini-thought-signature.spec.ts (the FULL template — copy the provider switch, run-start capture, no-400, run-completion, langsmith blocks)
    - frontend/tests/e2e/fixtures/auth.fixture.ts (authedPage shape + post-login composer assertion)
    - frontend/tests/e2e/fixtures/db-teardown.fixture.ts (teardownTestUserData + assertNoOrphanedStreamingRuns + the localhost gate)
    - frontend/src/components/panel/FilesSection.tsx (lines 170,181 — listbox/option selectors), VersionDiff.tsx (line 207 — region aria-label), PendingAskCard.tsx (lines 163-244 — group/textarea/Send Answer)
    - 088-RESEARCH.md §Pattern 4 (selectors + substrate) + Pitfall 4 (thought-signature regression mechanics)
  </read_first>
  <action>
Create `frontend/tests/e2e/scenario-13-workspace-deep-flow.spec.ts` extending the scenario-02 pattern. Parameterize over TWO providers (D-15): `[{name:"anthropic", model:/claude.*haiku|claude.*opus/i}, {name:"google", model:/gemini.*3.*flash/i}]`. Use a `for (const provider of PROVIDERS) test.describe(...)` loop so each provider is its own describe block.

Header + imports + fixtures (copy scenario-02:22-41):
```ts
import { test, expect } from "./fixtures/auth.fixture"
import { teardownTestUserData, assertNoOrphanedStreamingRuns } from "./fixtures/db-teardown.fixture"
import { assertLangSmithTraceExists } from "./fixtures/langsmith.fixture"
const TEST_USER_EMAIL = process.env.E2E_USER_EMAIL || "fhdmrd@gmail.com"
// beforeEach: teardownTestUserData(TEST_USER_EMAIL); afterEach: assertNoOrphanedStreamingRuns(TEST_USER_EMAIL)
```

The deep-flow test body (per provider), NO page.reload() anywhere:

1. **Switch provider/model** — copy scenario-02:49-93 (Settings nav, provider select with fallback, model select, Save, waitForTimeout(500)). Use the provider's name/model regex.

2. **Open the workspace panel** — navigate to "/", New chat, then open the panel via the toggle (`⌘.`/`Ctrl+.` keyboard `page.keyboard.press("Meta+Period").catch(()=>page.keyboard.press("Control+Period"))`, OR click the panel toggle button if a role/name selector is more robust). Confirm the panel `[role="complementary"][aria-label="Agent workspace"]` is visible.

3. **Agent writes a file** — send a prompt that triggers `workspace_write` (e.g. "Create a file called analysis.md with a short 2-line summary of embeddings."). Capture run_id (scenario-02:118-138 pattern). Wait for run completion (Stop button hidden). Assert a file row appears: `expect(page.locator('[role="listbox"][aria-label="Workspace files"] [role="option"]').first()).toBeVisible({timeout:...})`.

4. **Agent updates the file → user views diff** — send a follow-up that triggers a second `workspace_write` to the SAME file (e.g. "Now add a third line about cosine similarity to analysis.md."). Wait for completion. Open the file's VERSIONS/diff (click the file row → the VERSIONS affordance). Assert the diff region: `expect(page.locator('[role="region"]').filter({hasText:/Diff v|v\d/}).first()).toBeVisible()` (or by aria-label^="Diff v").

5. **Agent calls ask_user → user responds → resume** — send a prompt that triggers `ask_user` (e.g. "Rename analysis.md to final.md, but ask me to confirm the new name first."). Wait for the PendingAskCard: `const askCard = page.locator('[role="group"]').filter({hasText:/Needs you/i}).first(); await expect(askCard).toBeVisible({timeout:...})`. Optionally assert the paused cue `[role="status"]` "awaiting your answer". Fill the free-text `askCard.locator('textarea').fill("Yes, final.md is correct")` and click `Send Answer` (`getByRole("button",{name:/send answer/i})`). Assert the run RESUMES: the PendingAskCard unmounts (`await expect(askCard).toBeHidden()`) and the run reaches a terminal state (Stop button hidden again) — ALL without a page reload.

6. **No-400 assertion (D-17, Google axis especially)** — track 400s for the whole test (scenario-02:110-116 pattern) and assert `expect(bad400Responses).toEqual([])` at the end. On Google this is the thought-signature live re-verify; keep the assertion on BOTH providers (cheap, and catches Anthropic regressions too).

7. **LangSmith trace** — if a run_id was captured, `await assertLangSmithTraceExists(runId, provider.name)` (scenario-02:154-161 pattern, soft if key unset).

Use BROAD role+regex selectors (scenario-02 style) so a future minor UI change doesn't break the scenario. If the environment lacks a provider/key, the early Settings step fails LOUDLY (the intended scenario-02 behavior — no silent skip; the operator must know UAT requires both providers). Keep timeouts generous (multi-tool Gemini-3 chains: 120s wall-clock per run, scenario-02:145).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "scenario-13" || echo "no scenario-13 type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/tests/e2e/scenario-13-workspace-deep-flow.spec.ts` exists and is ≥120 lines
    - File imports from `./fixtures/auth.fixture`, `./fixtures/db-teardown.fixture`, `./fixtures/langsmith.fixture`
    - File references both `anthropic` and a `gemini` (3.x) model regex (NOT gemini-2.5)
    - File contains the panel selectors: `role="listbox"`, `role="option"`, `role="region"` (or aria-label "Diff"), `role="group"`, and the text `ask_user` or `Needs you`
    - File contains `Send Answer` and asserts the ask card hides after answering (resume)
    - File contains the 400 capture + `toEqual([])` assertion (D-17)
    - File contains NO `page.reload(` and NO `page.goto("/")` AFTER the file is written (the no-refresh constraint — only the initial nav to "/")
    - `cd frontend && npx playwright test --list 2>&1` includes scenario-13 (matches testMatch)
  </acceptance_criteria>
  <done>scenario-13 authored: parameterized over Anthropic + Google, runs the full write→see→update→diff→ask_user→respond→resume flow with panel-surface assertions, no page refresh, zero-400 assertion (D-17 live re-verify), reusing the 3 fixtures. Listed by playwright.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke-list the scenario + document the run command (no live execution here)</name>
  <read_first>
    - frontend/playwright.config.ts (testMatch, workers:1, baseURL, projects)
    - scripts/restart-backend.ps1 (the orphan-worker-aware backend restart the operator uses before E2E)
    - 088-VALIDATION.md (the E2E command + the deep-E2E manual companion row)
  </read_first>
  <action>
This task confirms scenario-13 is well-formed and wired into the runner WITHOUT requiring a live backend / provider keys (that live execution is the operator's job in Plan 05 / VALIDATION.md, and the scenario fails loudly if keys are absent — by design).

1. Verify the scenario is discovered by Playwright:
```bash
cd frontend && npx playwright test --list
```
Confirm `scenario-13-workspace-deep-flow.spec.ts` appears in the list with both provider describe blocks (Anthropic + Google).

2. Verify it type-checks (no compile errors specific to the new file):
```bash
cd frontend && npx tsc --noEmit
```
(Pre-existing unrelated type errors elsewhere are out of scope; scenario-13 itself must be clean.)

3. Record the exact run command in the SUMMARY for the operator: `cd frontend && npm run e2e -- scenario-13` (after starting the local backend via `scripts/restart-backend.ps1` and confirming Anthropic + Google keys + a gemini-3.x model are configured). Note that workers:1 means it runs serially with shared DB-teardown.

Do NOT run the full live E2E here (no run_in_background of the backend — the operator starts uvicorn in a visible terminal per [[feedback_user_starts_backend]]). This plan's deliverable is the authored, discovered, type-clean scenario; the live green pass is a Plan 05 / VALIDATION gate.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test --list 2>&1 | grep -i "scenario-13" && echo "DISCOVERED"</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx playwright test --list` output contains `scenario-13`
    - The list shows the scenario under both an anthropic and a google describe block (parameterized)
    - `cd frontend && npx tsc --noEmit 2>&1` reports no NEW errors attributable to scenario-13 (grep for the filename returns nothing)
    - SUMMARY records the operator run command `npm run e2e -- scenario-13` + the backend-restart prerequisite
  </acceptance_criteria>
  <done>scenario-13 is discovered by Playwright (both provider blocks), type-clean, and the operator run command is documented. Live execution is deferred to the Plan 05 operator gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E scenario → live database (test-user data write + teardown) | scenario-13 triggers real runs that write workspace_files/messages/todos and tears them down |
| E2E scenario → backend/.env + CI secrets | auth + langsmith + provider keys flow from env / GitHub secrets |
| E2E scenario → live LLM providers (Anthropic, Google) | real API calls including the gemini-3 thought-signature multi-round path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-088-03-01 | Tampering / DoS | db-teardown wiping the wrong database | mitigate | Reuse `db-teardown.fixture.ts` which is HARD-GATED to localhost (refuses non-local SUPABASE_URL, fixture lines 18-29). scenario-13 does not add its own teardown — it calls the gated fixture. |
| T-088-03-02 | Authentication / Secrets | test credentials + API keys in the scenario | mitigate | V2 — auth is env-driven via auth.fixture (E2E_USER_*); defaults are the documented LOCAL test user; CI uses GitHub secrets (auto-redacted). No credentials hardcoded in scenario-13 beyond the documented local-default email (matches scenario-02). |
| T-088-03-03 | Information disclosure | cross-thread data bleed observed during the flow | accept | The flow uses a single test-user thread; cross-thread bleed is a separate verification target (the parallel-thread axis in Plan 05), not a risk introduced here. |
</threat_model>

<verification>
- `cd frontend && npx playwright test --list` includes scenario-13 under both Anthropic + Google blocks.
- `cd frontend && npx tsc --noEmit` — no new errors from scenario-13.
- The scenario contains no `page.reload(` and no post-write `page.goto` (the no-refresh constraint).
- The 400-capture + `toEqual([])` assertion is present (D-17 re-verify).
- Live green pass on both providers is the operator gate in Plan 05 / VALIDATION.md (backend up + keys configured).
</verification>

<success_criteria>
- SC#4 automated backstop: the deep workspace flow (write→see→update→diff→ask_user→respond→resume, no refresh) is encoded as a CI-runnable Playwright scenario on Anthropic + Google.
- D-17: the Google axis asserts zero 400 INVALID_ARGUMENT (live thought-signature re-verification at the network level).
- REUSE-AND-EXTEND: built on the existing scenario-02 pattern + 3 fixtures, no new harness.
</success_criteria>

<output>
After completion, create `.planning/phases/088-cross-cutting-verification-accessibility/088-03-SUMMARY.md`
</output>
