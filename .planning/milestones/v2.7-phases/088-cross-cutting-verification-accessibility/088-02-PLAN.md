---
phase: 088-cross-cutting-verification-accessibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/eval_cross_provider.py
autonomous: true
requirements: [A11Y-01]
user_setup:
  - service: backend-uvicorn
    why: "The eval script drives the REAL HTTP route POST /threads/{id}/messages — the operator starts uvicorn in a visible terminal (never run_in_background)"
    env_vars:
      - name: SUPABASE_URL
        source: "backend/.env (must be a LOCALHOST URL — the script hard-gates against cloud)"
      - name: DATABASE_URL
        source: "backend/.env (local Postgres, default postgresql://postgres:postgres@127.0.0.1:54322/postgres)"
    dashboard_config:
      - task: "Provider API keys (OpenAI, Anthropic, Google gemini-3.x, OpenRouter) configured in user_settings / backend/.env so the live agent loop can call each provider"
        location: "Settings UI / backend/.env"

must_haves:
  truths:
    - "Running `python scripts/eval_cross_provider.py` drives the REAL POST /threads/{id}/messages route (measures the actual active_system_prompt, not the service layer)"
    - "The script refuses to run against a non-localhost SUPABASE_URL (hard-gate, never points at cloud)"
    - "For each (provider × canonical-prompt) it asserts tool-invocation happened, arg-shape conformance, and DB persistence"
    - "It reads backend/.env via dotenv and never logs API key VALUES (presence/absence only)"
    - "It runs the 4 canonical prompts across OpenAI, Anthropic, Google (3.x), OpenRouter and prints a per-row PASS/FAIL summary"
  artifacts:
    - path: "scripts/eval_cross_provider.py"
      provides: "Reusable cross-provider tool-use measurement engine (MVP seed of the v2.8 harness)"
      contains: "POST"
      min_lines: 120
  key_links:
    - from: "scripts/eval_cross_provider.py"
      to: "POST /threads/{id}/messages"
      via: "live HTTP request to the running backend"
      pattern: "/threads/"
    - from: "scripts/eval_cross_provider.py"
      to: "messages.tool_calls JSONB"
      via: "psycopg2 SELECT tool_calls FROM messages"
      pattern: "FROM messages"
    - from: "scripts/eval_cross_provider.py"
      to: "todos / workspace_files tables"
      via: "psycopg2 count(*) persistence assertion"
      pattern: "FROM (todos|workspace_files)"
---

<objective>
Build the reusable cross-provider eval script (D-02) — the measurement engine for SC#1 and the evidence source for the SEED-034 fold-gate (D-05, consumed by Plan 04). It is the MVP SEED of the v2.8 eval harness: small, repeatable, operator-runnable on demand — NOT a productized harness (D-08).

Per (provider × canonical-prompt) it drives the LIVE agent loop via the REAL HTTP route `POST /threads/{id}/messages` (Pitfall 2 — driving the service layer directly would bypass `threads.py`'s `active_system_prompt` assembly, so a SEED-034 prompt change wouldn't be measured; the fold-gate is about the shared prompt — measure it). It then asserts three things against durable truth: (a) tool-invocation happened, (b) arg-shape conformance, (c) DB persistence.

Purpose: a regression gate that an operator runs per new-model onboarding (D-08 seed) AND the before/after evidence the SEED-034 fold-gate needs.
Output: `scripts/eval_cross_provider.py`, modeled on `scripts/observe-run.py` (RESEARCH §Pattern 1 / Don't Hand-Roll — env load, psycopg2 query, redis/langsmith dumps already solved there).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/088-cross-cutting-verification-accessibility/088-CONTEXT.md
@.planning/phases/088-cross-cutting-verification-accessibility/088-RESEARCH.md
@.planning/phases/088-cross-cutting-verification-accessibility/088-VALIDATION.md

# The proven template — copy its env-load / psycopg2 / redis / langsmith patterns:
@scripts/observe-run.py

<interfaces>
<!-- Source-of-truth verified this session. Use directly. -->

Eval-script template (scripts/observe-run.py — REUSE these patterns, RESEARCH §Pattern 1):
```python
# Env loading (observe-run.py:36-48):
from dotenv import load_dotenv
from pathlib import Path
env_path = Path(__file__).resolve().parent.parent / "backend" / ".env"
load_dotenv(env_path)   # SUPABASE_URL / DATABASE_URL / LANGSMITH_API_KEY / REDIS_URL

# Postgres assertions (observe-run.py:151-194 pattern):
import os, psycopg2
from psycopg2.extras import RealDictCursor
db_url = os.getenv("DATABASE_URL") or "postgresql://postgres:postgres@127.0.0.1:54322/postgres"
conn = psycopg2.connect(db_url)
```

DB assertion targets (VERIFIED this session):
- messages.tool_calls is a JSONB COLUMN, NOT a table (migration 013; .kind values doc'd in migration 055). Query:
  `SELECT tool_calls FROM messages WHERE thread_id=%s AND role='assistant' AND tool_calls IS NOT NULL ORDER BY created_at DESC LIMIT 5`
  Each element: {tool_call_id, name, args, result, status, kind?, sub_agent?, thought_signature?}
- todos table (migration 055): `SELECT count(*) AS n FROM todos WHERE thread_id=%s` → n>0 proves write_todos persisted
- workspace_files table (migration 054): `SELECT count(*) AS n FROM workspace_files WHERE thread_id=%s` → n>0 proves workspace_write
- ask_user containment query (panel.py:128): `tool_calls @> '[{"kind":"ask_user_prompt"}]'`

Driving the live agent loop (RESEARCH §Pattern 1 option 1 — the REQUIRED path):
- Provider/model is set in `user_settings` (effective settings resolved in openai_service.get_llm_client, line 785). Flip active_provider/llm_model per provider BEFORE each prompt (via the same settings update the app uses, or a direct user_settings UPDATE for the test user).
- Then POST the prompt to `/threads/{id}/messages` against the running local backend; capture run_id from the response; the agent loop runs SYSTEM_PROMPT (threads.py:336) + get_tools() descriptions — the REAL shared path.
- Backend base URL: local uvicorn (e.g. http://127.0.0.1:8000). Auth: the test user (fhdmrd@gmail.com) — reuse the app's auth (bearer token / supabase session); follow observe-run.py's client setup.

Localhost hard-gate pattern (db-teardown.fixture.ts:18-29 precedent — replicate in Python):
- Before any DB write/teardown, assert SUPABASE_URL contains 'localhost' or '127.0.0.1'; raise/exit if not. NEVER point the eval script at cloud (RESEARCH Security — Tampering/DoS).

Canonical prompt set N=4 (RESEARCH §Code Examples, D-02):
1. factual-doc-search: "What does my dissertation say about X?" → assert search_documents invoked
2. multi-tool: "Plan a 3-step analysis of my Q3 data and write the summary to a file." → assert write_todos (todos rows) AND workspace_write (workspace_files row)
3. task sub-agent: "Find every mention of Y across my documents and summarize." → assert task invoked (tool_calls[].sub_agent present)
4. ask_user: "Overwrite my existing report — but confirm with me first." → assert ask_user invoked (tool_calls @> '[{"kind":"ask_user_prompt"}]')

Representative models (D-01 / D-03): OpenAI gpt-5.4-mini · Anthropic claude-haiku-4-5 · Google gemini-3.5-flash (3.x+ — NOT 2.5) · OpenRouter z-ai/glm-5.1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the eval script — env load, localhost hard-gate, DB assertion helpers</name>
  <read_first>
    - scripts/observe-run.py (the template — env load :36-48, psycopg2 query :151-194, redis/langsmith setup)
    - frontend/tests/e2e/fixtures/db-teardown.fixture.ts (lines 18-29 — the assertLocalhostOnly hard-gate to replicate in Python)
    - 088-RESEARCH.md §Pattern 1 + Pitfall 3 (tool_calls is JSONB, not a table) + §Security Domain (V4 RLS, V7 secrets)
    - backend/.env.example (the env var NAMES — confirm SUPABASE_URL / DATABASE_URL / LANGSMITH_API_KEY / REDIS_URL)
  </read_first>
  <action>
Create `scripts/eval_cross_provider.py` with the foundation (modeled verbatim on observe-run.py — do NOT hand-roll new plumbing, RESEARCH Don't Hand-Roll):

1. **Env load** (observe-run.py:36-48): `load_dotenv(Path(__file__).resolve().parent.parent / "backend" / ".env")`. Read SUPABASE_URL, DATABASE_URL, LANGSMITH_API_KEY, REDIS_URL. Print presence/absence ONLY — NEVER print key values (RESEARCH Security V7; [[feedback_env_secrets_handling]]).

2. **Localhost hard-gate** (replicate db-teardown.fixture.ts:18-29 in Python): a function `assert_localhost_only()` that checks `SUPABASE_URL` contains `localhost` or `127.0.0.1`; if not, print an error and `sys.exit(1)`. Call it at the top of `main()` BEFORE any DB connection or any agent-loop call. This script must NEVER run against cloud (RESEARCH Security — Tampering/DoS).

3. **DB assertion helpers** (psycopg2 + RealDictCursor, observe-run.py:151-194 pattern):
   - `get_tool_calls(conn, thread_id) -> list[dict]`: `SELECT tool_calls FROM messages WHERE thread_id=%s AND role='assistant' AND tool_calls IS NOT NULL ORDER BY created_at DESC LIMIT 5`, flatten the JSONB arrays into one list of tool-call dicts.
   - `assert_tool_invoked(calls, name) -> bool`: `any(tc.get("name") == name for tc in calls)`.
   - `assert_arg_shape(calls, name, key, expected_type) -> bool`: find the call, assert `isinstance(tc["args"][key], expected_type)` — e.g. write_todos args["todos"] must be a `list`, NOT a JSON string (the BUG-260529-01 arg-shape case).
   - `count_rows(conn, table, thread_id) -> int`: `SELECT count(*) AS n FROM {table} WHERE thread_id=%s` for `todos` / `workspace_files` (use a fixed table allowlist {todos, workspace_files} — never interpolate arbitrary table names).
   - `assert_ask_user(conn, thread_id) -> bool`: containment query `SELECT count(*) AS n FROM messages WHERE thread_id=%s AND tool_calls @> '[{"kind":"ask_user_prompt"}]'` → n>0.

V4 RLS (RESEARCH Security): operate ONLY on the test user's own threads — never bypass RLS, never use a service-role key to read other users' rows for assertions.

This task delivers the scaffold + helpers + the hard-gate; Task 2 wires the live agent-loop driver + the prompt loop.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && python -c "import ast; ast.parse(open('scripts/eval_cross_provider.py').read()); print('parse OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/eval_cross_provider.py` exists and parses (python ast.parse OK)
    - File contains `load_dotenv` AND `backend` (env path) AND a localhost gate referencing `127.0.0.1` or `localhost`
    - File contains `FROM messages` AND `tool_calls` (the JSONB query, NOT a tool_calls table)
    - File contains `FROM todos` AND `FROM workspace_files` (persistence assertions) AND `ask_user_prompt` (containment query)
    - File does NOT contain any `print(` that outputs an env var holding `KEY` or `SECRET` value (presence/absence only)
  </acceptance_criteria>
  <done>Eval-script scaffold created: env load, localhost hard-gate, and the three DB assertion families (invocation, arg-shape, persistence) implemented from the observe-run.py pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the live agent-loop driver + the 4-prompt × 4-provider loop + PASS/FAIL summary</name>
  <read_first>
    - scripts/eval_cross_provider.py (the scaffold from Task 1)
    - scripts/observe-run.py (its HTTP/run_id capture + langsmith Client setup, for the live-call shape)
    - backend/app/services/openai_service.py (around line 785 — get_llm_client resolves user_settings effective provider/model; confirm how active_provider/llm_model are read)
    - 088-RESEARCH.md §Pattern 1 (option 1 = drive POST /threads/{id}/messages; provider set in user_settings) + §Code Examples (the N=4 canonical prompts + representative models)
  </read_first>
  <action>
Wire the measurement loop onto the Task-1 scaffold (RESEARCH §Pattern 1 option 1 — the REAL HTTP path, Pitfall 2):

1. **Provider switch:** `set_provider(provider, model)` — update the test user's `user_settings.active_provider` + `llm_model` (the same effective-settings the app resolves in openai_service.get_llm_client:785). Use the app's settings-update path or a direct UPDATE on user_settings for the test user.

2. **Drive the live route:** `run_prompt(thread_id, prompt) -> run_id` — POST the prompt to `/threads/{id}/messages` against the running local backend (base URL from env / default http://127.0.0.1:8000), authenticated as the test user (reuse observe-run.py's auth/client). Capture run_id; wait for run completion (poll the run status or the SSE stream end, observe-run.py pattern). This exercises SYSTEM_PROMPT (threads.py:336) + get_tools() descriptions — the REAL shared path.

3. **Canonical prompt set N=4** (RESEARCH §Code Examples) — define as a list of (prompt_id, prompt_text, assertions) where assertions reference the Task-1 helpers:
   - factual-doc-search → assert_tool_invoked(calls, "search_documents")  (or query_tables for tabular)
   - multi-tool → assert_tool_invoked(calls, "write_todos") AND count_rows(conn,"todos",tid)>0 AND assert_tool_invoked(calls,"workspace_write") AND count_rows(conn,"workspace_files",tid)>0 AND assert_arg_shape(calls,"write_todos","todos",list)
   - task sub-agent → any(tc.get("sub_agent") for tc in calls)  (tool_calls[].sub_agent present)
   - ask_user → assert_ask_user(conn, tid)

4. **The matrix loop:** for provider,model in [(openai, gpt-5.4-mini), (anthropic, claude-haiku-4-5), (google, gemini-3.5-flash), (openrouter, z-ai/glm-5.1)]: set_provider; for each canonical prompt: create a fresh thread (or reuse a per-(provider,prompt) thread), run_prompt, run the assertions, record PASS/FAIL + which assertion failed.

5. **Output:** print a per-row table `provider | prompt_id | invoked | arg_shape | persisted | RESULT` and an overall summary. Make the rows machine-greppable (so Plan 04's fold-gate can diff before/after runs). Models should be configurable via constants at the top (so an operator can swap a representative model per new-model onboarding — D-08 seed). Optionally accept `--provider X` / `--prompt Y` CLI args to run a single cell (lightweight re-run for the fold-gate).

Keep it SMALL and repeatable — do NOT productize (no config framework, no plugin system, no DB-backed result store — that's the v2.8 harness, D-08). gemini-2.5 is NOT a representative (D-03) — if an operator passes it, record it as a data point, do not gate.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && python -c "import ast; ast.parse(open('scripts/eval_cross_provider.py').read()); print('parse OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/eval_cross_provider.py` parses (python ast.parse OK) and is ≥120 lines
    - File contains `/threads/` (the live route) AND a POST request
    - File defines the 4 canonical prompts (contains `search_documents`, `write_todos`, `workspace_write`, and `ask_user`)
    - File iterates the 4 providers (contains `anthropic`, `google`, `openrouter`, and `gpt` or `openai`) with `gemini-3` (NOT gemini-2.5 as the representative)
    - File prints a per-row PASS/FAIL summary (greppable result rows)
    - `python scripts/eval_cross_provider.py --help` (or running with no backend) exits cleanly with a usage/connection message, NOT a traceback that leaks env values
  </acceptance_criteria>
  <done>The eval script drives the real /threads/{id}/messages route per (provider × canonical-prompt), runs the 3 assertion families, and prints a greppable PASS/FAIL scoreboard. Runs against localhost only. Ready for Plan 04 to use as fold-gate evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval script → backend/.env (secrets) | Script reads SUPABASE_URL / DATABASE_URL / LANGSMITH_API_KEY / provider keys |
| eval script → live database (writes test data + reads tool_calls) | Script triggers real agent runs that WRITE todos/workspace_files/messages and READs them back |
| eval script → live LLM providers | Real API calls to OpenAI/Anthropic/Google/OpenRouter via the agent loop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-088-02-01 | Tampering / DoS | eval script DB writes/teardown against the wrong DB | mitigate | `assert_localhost_only()` hard-gate (replicated from db-teardown.fixture.ts:18-29) — refuses any non-localhost SUPABASE_URL; called before any DB connection or agent-loop call. Acceptance criteria assert the gate references 127.0.0.1/localhost. |
| T-088-02-02 | Information disclosure | API key / secret leakage in script stdout or tracebacks | mitigate | Script prints env-var PRESENCE/ABSENCE only (observe-run.py pattern), never values; acceptance criteria forbid printing key values; [[feedback_env_secrets_handling]]. |
| T-088-02-03 | Elevation / Access control (RLS bypass) | reading other users' tool_calls/todos for assertions | mitigate | V4 — script operates on the TEST USER's own threads only (FK→threads.user_id RLS, migration 055:20-37); never uses a service-role key to bypass RLS for cross-user reads; table access limited to a fixed {todos, workspace_files, messages} allowlist (no arbitrary table interpolation). |
| T-088-02-04 | Tampering (SQL injection) | dynamic table/thread_id in psycopg2 queries | mitigate | thread_id passed as a parameterized `%s` value (never f-string interpolated); table names from a fixed code allowlist, not user input. |
</threat_model>

<verification>
- `python -c "import ast; ast.parse(open('scripts/eval_cross_provider.py').read())"` — parses clean.
- Script drives `POST /threads/{id}/messages` (the real path — RESEARCH Pitfall 2), not the service layer.
- Localhost hard-gate present and called before any DB/agent-loop access.
- DB assertions hit `messages.tool_calls` (JSONB), `todos`, `workspace_files` (Pitfall 3 — column not table).
- No env-var key values printed.
- Live execution (with backend up + provider keys) is exercised in Plan 04 (fold-gate) + Plan 05 (scoreboard); this plan delivers the runnable, parse-clean, localhost-gated script.
</verification>

<success_criteria>
- A reusable, localhost-gated eval script exists that measures tool-use across all 4 providers via the REAL agent loop and asserts invocation + arg-shape + DB persistence.
- It is the MVP seed of the v2.8 harness (small, repeatable, operator-runnable) — NOT productized.
- It produces greppable before/after PASS/FAIL evidence consumable by the Plan 04 SEED-034 fold-gate (D-05).
- It never points at cloud and never logs secret values.
</success_criteria>

<output>
After completion, create `.planning/phases/088-cross-cutting-verification-accessibility/088-02-SUMMARY.md`
</output>
