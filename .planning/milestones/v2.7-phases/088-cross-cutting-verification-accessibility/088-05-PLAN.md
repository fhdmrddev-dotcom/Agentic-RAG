---
phase: 088-cross-cutting-verification-accessibility
plan: 05
type: execute
wave: 3
depends_on: ["088-01", "088-02", "088-03", "088-04"]
files_modified:
  - .planning/phases/088-cross-cutting-verification-accessibility/088-VALIDATION.md
  - .planning/reported-bugs/gemini-3-thought-signature-missing-on-tool-rounds.md
autonomous: false
requirements: [A11Y-01, A11Y-02]
user_setup:
  - service: chrome-devtools-mcp
    why: "The lived-experience a11y pass, Lighthouse contrast/focus audit, keyboard walk, cross-provider lived parity, long-message axis, and deep-flow lived pass are all operator/Chrome-MCP-driven against the live authenticated app"
    env_vars: []
    dashboard_config:
      - task: "Local app running at http://localhost:5173 (test login fhdmrd@gmail.com / 123456); backend uvicorn started by the operator; OpenAI/Anthropic/Google(3.x)/OpenRouter keys configured for the 4-axis live runs"
        location: "Local dev environment"

must_haves:
  truths:
    - "The 4-axis cross-provider UAT scoreboard is filled in 088-VALIDATION.md: OpenAI/Anthropic/Google(3.x)/OpenRouter x multi-tool x parallel-thread x long-message (long-message manual per provider)"
    - "A Chrome MCP Lighthouse a11y audit (real contrast >=4.5:1 + real focus rings) is recorded for the live populated panel, both Deep Midnight dark + light themes"
    - "A Chrome-MCP keyboard walk (Tab/Shift-Tab/Enter/Space/Escape through todos -> files -> preview -> diff -> ask_user) is recorded with focus-state evidence"
    - "An operator final lived-experience a11y pass (G-4/D-14) is recorded as passed"
    - "scenario-13 has been run LIVE green on Anthropic + Google (the deep-flow backstop) and the lived deep-flow pass recorded"
    - "The D-17 gemini-3 thought-signature report is routed: closed-as-verified if clean, or fixed-in-088 if it reproduces (D-16)"
  artifacts:
    - path: ".planning/phases/088-cross-cutting-verification-accessibility/088-VALIDATION.md"
      provides: "Filled 4-axis scoreboard + a11y manual-walk checklist + Lighthouse evidence + deep-flow lived pass + D-17 verdict"
      contains: "Scoreboard"
    - path: ".planning/reported-bugs/gemini-3-thought-signature-missing-on-tool-rounds.md"
      provides: "Updated status (closed/verified or fix-in-088) after live re-verification"
      contains: "status:"
  key_links:
    - from: "088-VALIDATION.md 4-axis scoreboard"
      to: "live cross-provider runs (Chrome MCP) + scenario-13 + eval script"
      via: "evidence rows per provider/axis"
      pattern: "PASS"
    - from: "088-VALIDATION.md a11y manual-walk checklist"
      to: "Chrome MCP Lighthouse + keyboard walk + lived pass"
      via: "recorded operator evidence"
      pattern: "Lighthouse"
---

<objective>
The verification capstone (D-01, D-14, D-15, D-17, G-4). This plan AUTHORS the operator/manual portions of 088-VALIDATION.md and RECORDS the operator-driven passes that the automated backstops (Plan 01 vitest-axe, Plan 02 eval script, Plan 03 scenario-13, Plan 04 fold-gate) cannot cover. It also routes the one folded reported-bug (D-17).

Per the hard constraints (D-01), the 4-axis UAT scoreboard, the long-message axis, the manual a11y passes, and the SEED-034 fold-gate decision live in 088-VALIDATION.md — NOT as plan-task acceptance criteria. This plan POPULATES those sections with live evidence.

Because every deliverable here depends on operator/Chrome-MCP manual passes (the recognize-failure-here gate per [[feedback_uat_lived_experience_gap]] + [[feedback_exhaustive_ui_state_sweep]]), this plan is autonomous: false with a checkpoint:human-verify gate.

Purpose: SC#1 (4-axis matrix complete), SC#2 (real-contrast/focus AA via Lighthouse — jsdom can't, Pitfall 5), SC#3 (lived keyboard walk), SC#4 (deep-flow lived pass + scenario-13 live green), A11Y-01/02 sign-off, and the D-17 live re-verification verdict.
Output: a fully-filled 088-VALIDATION.md + the gemini-3 thought-signature report routed (closed-verified or fix-in-088).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/088-cross-cutting-verification-accessibility/088-CONTEXT.md
@.planning/phases/088-cross-cutting-verification-accessibility/088-RESEARCH.md
@.planning/phases/088-cross-cutting-verification-accessibility/088-VALIDATION.md
@.planning/reported-bugs/gemini-3-thought-signature-missing-on-tool-rounds.md

# The scoreboard format to reuse verbatim + the locked design contract to verify against:
@.planning/phases/087-panel-ui/087-VALIDATION.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md

<interfaces>
<!-- Source-of-truth verified this session. Use directly. -->

4-axis scoreboard row format (REUSE verbatim from 087-VALIDATION.md:107-114):
  | Provider (representative)     | Multi-tool          | Parallel-thread | Long-message    | Panel/seam parity | Status   |
  |-------------------------------|---------------------|-----------------|-----------------|-------------------|----------|
  | OpenAI (gpt-5.4-mini)         | tools+ask           | A while B       | (manual)        | parity            | PASS     |
  | Anthropic (claude-haiku-4-5)  | todos+file          | -               | >=5KB / >=50    | parity            | PASS     |
  | Google (gemini-3.5-flash)     | todos+file          | -               | -               | parity            | PASS     |
  | OpenRouter (z-ai/glm-5.1)     | todos+file          | reconciled      | -               | parity            | PASS     |
Plus a clearly-marked "gemini-2.5-flash (known-degraded — not gating)" row for provenance (D-03).

LOCKED rules (CLAUDE.md UAT scoreboard recipe + 087-VALIDATION.md):
- 4 axes: cross-provider (OpenAI/Anthropic/Google(3.x)/OpenRouter) x multi-tool (>=1 row, 2+ tools) x parallel-thread (A streaming while B accepts a prompt) x long-message (>=50 prior messages OR >=5 KB prompt).
- Long-message stays MANUAL per provider (felt no-drop behavior).
- "the bandwidth, not the cell count, is the bar" (087-VALIDATION.md:116) — axes 1-3 may combine into fewer live runs.

SSE/tool map the matrix must exercise (RESEARCH Pattern 3): write_todos (todos table + SeamPointer), workspace_write (workspace_files + SeamPointer), ask_user (PendingAskCard + paused cue, tool_calls @> ask_user_prompt), task (sub_agent), tool_args_progress/tool_start (Redis run stream).

Chrome MCP a11y procedure (RESEARCH Code Examples / D-13b/D-14):
1. navigate http://localhost:5173, login fhdmrd@gmail.com / 123456
2. trigger a workspace run (write_todos + workspace_write + ask_user) so the panel is populated
3. Lighthouse a11y category -> REAL contrast ratios + REAL focus rings (jsdom CANNOT — Pitfall 5)
4. keyboard walk: Tab/Shift-Tab/Enter/Space/Escape through todos -> files (Arrow nav) -> file preview (Escape) -> versions/diff (pills) -> ask_user (radio + free-text); screenshot focus states
5. repeat in BOTH themes (Deep Midnight dark + light — contrast differs per theme)
Open Question 2 (RESEARCH): test FilePreview Escape + DiffExpandOverlay (Radix) both-open — confirm Escape closes the right layer.

D-17 routing (CLAUDE.md reported-bugs lifecycle): the report at .planning/reported-bugs/gemini-3-thought-signature-missing-on-tool-rounds.md has folded_into: "088", status: folded, re_open_trigger documented. After live re-verify (scenario-13 Google axis + Chrome MCP deep flow): clean -> set status: closed with a "verified 088" note; reproduces -> it is a flow-blocker (D-16) -> fix-in-088 (narrow, then re-verify). Filter rule: this is a surface: Agentic-RAG report (in scope).

D-16 blocker policy during UAT: fix in 088 ONLY bugs that break a verified v2.7 capability (panel surface, SSE event, cross-provider tool-use correctness, the E2E flow). Everything else -> log to .planning/reported-bugs/ with a re_open_trigger, roll to v2.8. SEED-039 fast-switch race: pull in ONLY if it blocks the deep flow; else stays deferred.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the VALIDATION.md scoreboard + a11y manual-walk skeleton + per-task verification map</name>
  <read_first>
    - .planning/phases/087-panel-ui/087-VALIDATION.md (lines 83-116 — the 4-axis scoreboard format + the "bandwidth not cell count" rule to mirror)
    - .planning/phases/088-cross-cutting-verification-accessibility/088-VALIDATION.md (the scaffold — fill the scoreboard section :88-95, the manual-only table :76-84, the per-task map :57-59)
    - 088-RESEARCH.md Pattern 2 (row format + models) + Pattern 3 (the SSE/tool map) + Validation Architecture (the SC->method map)
  </read_first>
  <action>
Author the manual/operator scaffolding into 088-VALIDATION.md so the live passes (Task 2) have a place to record evidence. This is documentation authoring — no live runs yet.

1. **4-Axis Cross-Provider UAT Scoreboard section** (088-VALIDATION.md:88-95): insert the empty scoreboard TABLE using the 087-VALIDATION.md:107-114 format verbatim, with the four representative-model rows (OpenAI gpt-5.4-mini, Anthropic claude-haiku-4-5, Google gemini-3.5-flash, OpenRouter z-ai/glm-5.1) + the marked "gemini-2.5-flash (known-degraded — not gating)" provenance row. Columns: Multi-tool | Parallel-thread | Long-message | Panel/seam parity | Status. Add a one-line note: "bandwidth not cell count; long-message MANUAL per provider".

2. **Per-Task Verification Map** (088-VALIDATION.md:57-59): replace the "_TBD by planner_" row with the real task->command rows for Plans 01-04 (Plan 01 a11y -> `npx vitest run src/components/panel/`; Plan 02 eval -> `python scripts/eval_cross_provider.py`; Plan 03 E2E -> `npm run e2e -- scenario-13`; Plan 04 fold-gate -> eval before/after + git diff), each mapped to A11Y-01/A11Y-02 or the SC it serves.

3. **a11y manual-walk checklist**: add a checklist subsection (mirroring 087's manual structure) enumerating the Chrome MCP steps: Lighthouse a11y (dark + light), keyboard walk stops (todos / files-Arrow / preview-Escape / diff-pills / ask_user radio+free-text), the FilePreview+overlay both-open Escape check (RESEARCH Open Q2), and the operator lived-experience sign-off line.

4. Confirm the Manual-Only Verifications table (already at :76-84) and the SEED-034 Fold-Gate Decision section (filled by Plan 04) are present and consistent.

Do NOT run live UAT in this task — only author the structure. Set up the empty rows so Task 2 fills them with evidence.
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && grep -Eiq "Scoreboard" .planning/phases/088-cross-cutting-verification-accessibility/088-VALIDATION.md && grep -Eiq "gemini-3" .planning/phases/088-cross-cutting-verification-accessibility/088-VALIDATION.md && echo "scoreboard authored"</automated>
  </verify>
  <acceptance_criteria>
    - 088-VALIDATION.md contains a 4-axis scoreboard TABLE with the 4 representative-model rows + a "gemini-2.5" known-degraded provenance row
    - The columns include Multi-tool, Parallel-thread, Long-message, and Panel/seam parity
    - The Per-Task Verification Map no longer contains "_TBD by planner_" and lists commands for Plans 01-04
    - An a11y manual-walk checklist enumerates the Chrome MCP Lighthouse (both themes) + keyboard-walk stops + the FilePreview/overlay Escape check
  </acceptance_criteria>
  <done>088-VALIDATION.md has the empty-but-structured 4-axis scoreboard, the filled per-task map, and the a11y manual-walk checklist — ready for live evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: OPERATOR — run the live 4-axis UAT + Chrome MCP a11y passes + deep-flow + record evidence</name>
  <action>Operator/Chrome-MCP-driven LIVE verification gate (autonomous: false). Run the 4-axis cross-provider UAT, the Chrome MCP Lighthouse + keyboard-walk a11y passes (both themes), the scenario-13 deep-flow live on Anthropic + Google, and the operator lived-experience sign-off — recording all evidence into the 088-VALIDATION.md sections authored in Task 1. See &lt;how-to-verify&gt; for the step-by-step procedure. Pause here until the operator returns the verdict.</action>
  <what-built>
Plans 01-04 delivered: the a11y remediation + vitest-axe gate (Plan 01), the localhost-gated eval script (Plan 02), the scenario-13 deep-flow Playwright backstop (Plan 03), and the SEED-034 fold-gate decision (Plan 04). Task 1 authored the VALIDATION.md scoreboard + a11y checklist skeleton. This checkpoint is the operator/Chrome-MCP-driven LIVE verification that fills it with evidence (the recognize-failure-here gate — orchestrator-driven Chrome MCP can miss felt defects, so exercise the FULL state matrix per [[feedback_exhaustive_ui_state_sweep]]).

PREREQUISITES (operator): local app at http://localhost:5173 (login fhdmrd@gmail.com / 123456); backend uvicorn started in a visible terminal (NOT run_in_background — [[feedback_user_starts_backend]]); OpenAI/Anthropic/Google(3.x)/OpenRouter keys configured.
  </what-built>
  <how-to-verify>
Record each result in 088-VALIDATION.md (the sections Task 1 authored). Drive Chrome MCP for all live UI checks.

1. **4-axis cross-provider UAT** (fill the scoreboard, SC#1 / D-01):
   - For each of the 4 representative models (OpenAI gpt-5.4-mini, Anthropic claude-haiku-4-5, Google gemini-3.5-flash, OpenRouter z-ai/glm-5.1): send a MULTI-TOOL prompt exercising 2+ panel-owned tools (e.g. write_todos + workspace_write + ask_user) and confirm the panel surfaces render + persist (todos table / workspace_files / tool_calls). Cross-check vs Supabase.
   - PARALLEL-THREAD: with Thread A streaming, open Thread B and submit a prompt; confirm no cross-thread bleed and B reconciles (at least 1 row, e.g. OpenAI + OpenRouter).
   - LONG-MESSAGE (MANUAL per provider): build a thread of >=50 messages OR send a >=5 KB prompt; confirm the panel + stream don't drop/truncate. Record per provider.
   - CROSS-PROVIDER LIVED PARITY: confirm the panel/seam renders identically across providers (the felt check).
   - Mark gemini-2.5-flash as a known-degraded data point if you test it (NOT gating, D-03).

2. **Chrome MCP a11y — Lighthouse + keyboard walk** (SC#2/SC#3, A11Y-01/02, D-13b/D-14):
   - Populate the panel (a run with todos + files + a diff + an ask_user prompt).
   - Run Lighthouse a11y category -> record REAL contrast ratios (>=4.5:1 on Deep Midnight) + REAL focus rings (the global :focus-visible ring from Plan 01). Repeat in BOTH dark + light themes (Pitfall 5 — jsdom can't measure this).
   - Keyboard walk: Tab/Shift-Tab/Enter/Space/Escape through todos -> files (Arrow nav, Enter to drill in) -> file preview (Escape closes) -> versions/diff (pills) -> ask_user (radio + free-text + Send Answer). Screenshot focus states as evidence. Confirm NO mouse-only path.
   - Test FilePreview Escape + DiffExpandOverlay (Radix) BOTH open — confirm Escape closes the intended layer (RESEARCH Open Q2).

3. **Deep-flow LIVE pass** (SC#4 / D-15 / D-17):
   - Run the scenario-13 Playwright backstop live on Anthropic + Google: `cd frontend && npm run e2e -- scenario-13` (after backend restart). Record green/red.
   - Drive the SAME flow via Chrome MCP on Anthropic + Google (write file -> see in panel -> update -> view diff -> ask_user -> respond -> resume, NO refresh) for the lived-experience pass.
   - D-17: on the Google axis, confirm NO 400 "missing thought_signature" across the 2+ tool rounds. (This is the highest-value cross-provider check — the 075.4 hotfix live re-verify.)

4. **Operator final lived-experience a11y sign-off** (G-4): after the systematic walk, give the holistic "I'd recognize failure here" pass across both themes + screen-reader feel.

Record PASS/FAIL + evidence (screenshots, Supabase counts, Lighthouse scores, scenario-13 result) into 088-VALIDATION.md. If any axis/check FAILS, note it for Task 3 (bug routing under D-16).
  </how-to-verify>
  <resume-signal>Type "approved" when the 4-axis scoreboard is filled, Lighthouse + keyboard walk + lived pass are recorded, scenario-13 ran on both providers, and the D-17 result is captured — or describe failures (which axis/check, what reproduced) for D-16 routing.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Route the D-17 reported-bug + log any non-blocking findings (D-16) + finalize VALIDATION sign-off</name>
  <read_first>
    - .planning/reported-bugs/gemini-3-thought-signature-missing-on-tool-rounds.md (the folded_into: 088 report + its re_open_trigger + status lifecycle)
    - .planning/reported-bugs/TEMPLATE.md (the frontmatter shape for any NEW deferred finding)
    - .planning/phases/088-cross-cutting-verification-accessibility/088-VALIDATION.md (the Task-2 evidence + the Validation Sign-Off checklist :110-122)
    - CLAUDE.md "Reported bugs cross-check (MANDATORY)" (the lifecycle + the surface: Agentic-RAG filter)
  </read_first>
  <action>
Close the loop on bug routing and finalize VALIDATION.md (depends on the Task-2 operator evidence).

1. **Route D-17** (gemini-3-thought-signature-missing-on-tool-rounds.md) per the Task-2 result:
   - If the Google-axis deep flow ran CLEAN (no 400 across 2+ tool rounds): set `status: closed` in the report frontmatter + add a note "verified live in Phase 088 (scenario-13 Google axis + Chrome MCP deep flow), 2026-05-29 — no 400 INVALID_ARGUMENT". This flips the lifecycle folded -> closed (CLAUDE.md).
   - If it REPRODUCED: it is a flow-blocker (D-16) -> the fix belongs in 088. Record the repro in the report, keep status: folded, and either (a) apply the narrow fix here + re-verify, or (b) if the fix is non-trivial, surface it to the operator as a scope decision. Document whichever path.

2. **Log non-blocking findings** (D-16 fix-blockers-defer-polish): for any defect Task-2 surfaced that does NOT break a verified v2.7 capability (panel surface, SSE event, cross-provider tool-use correctness, the deep flow), create/append a `.planning/reported-bugs/*.md` report (surface: Agentic-RAG, status: open or deferred) with a concrete re_open_trigger, and roll to v2.8. Do NOT fix polish here (the 075.x cascade lesson). The known-deferred polish bugs (chat-tool-cards-scroll-collapse-duplicate, non-anthropic-generic-code-task-descriptions, step-count-mismatch, timer-disappears) stay deferred — just confirm they did NOT block the deep flow. SEED-039 fast-switch race: pull in ONLY if it blocked the deep flow; else leave deferred.

3. **Finalize VALIDATION sign-off** (088-VALIDATION.md:110-122): check the sign-off boxes that are now satisfied (4-axis scoreboard filled, fold-gate decision recorded, D-17 re-verified, no watch-mode flags), set `nyquist_compliant: true` and `status:` to reflect completion in the frontmatter, and set Approval to the operator's verdict from Task 2.

Cross-check (CLAUDE.md plan-phase touchpoint): confirm every report with folded_into: 088 (i.e. the D-17 report) is addressed by this plan — it is (routed in step 1).
  </action>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && grep -Eiq "verified|closed|reproduc|fix-in-088" .planning/reported-bugs/gemini-3-thought-signature-missing-on-tool-rounds.md && grep -q "nyquist_compliant: true" .planning/phases/088-cross-cutting-verification-accessibility/088-VALIDATION.md && echo "routed + signed off"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/reported-bugs/gemini-3-thought-signature-missing-on-tool-rounds.md has an updated status (closed+verified-note, OR a documented repro + fix-in-088 path) reflecting the live re-verify
    - Any non-blocking finding from Task 2 is logged as a reported-bug with a re_open_trigger (or there were none — stated explicitly)
    - 088-VALIDATION.md frontmatter has `nyquist_compliant: true` and the Validation Sign-Off reflects the operator verdict
    - The known-deferred polish bugs are confirmed NOT to have blocked the deep flow (D-16)
  </acceptance_criteria>
  <done>D-17 routed (closed-verified or fix-in-088); non-blocking findings deferred with re_open_triggers; VALIDATION.md signed off with the operator verdict + nyquist_compliant true. The v2.7 close-out verification gate is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live UAT runs -> production-like DB + secrets | The 4-axis + deep flow write real test data and use provider keys |
| operator Chrome MCP session -> authenticated live app | Real login + real agent runs against localhost |
| parallel-thread axis -> cross-thread data isolation | Two threads active simultaneously |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-088-05-01 | Information disclosure | cross-thread data bleed during the parallel-thread axis | mitigate | The parallel-thread axis EXPLICITLY tests reconcile-abort / no cross-thread bleed (a verification target — RESEARCH Security); any observed bleed is a blocker logged under D-16. RLS (FK->threads.user_id) isolates per-user data. |
| T-088-05-02 | Tampering / DoS | live UAT against the wrong DB | mitigate | All UAT runs against localhost (http://localhost:5173 + local backend); the eval script (Plan 02) is localhost-hard-gated; operator clears test threads after manual UAT (precedent STATE.md:115). |
| T-088-05-03 | Information disclosure | secrets/keys in screenshots or VALIDATION evidence | mitigate | Record Lighthouse scores / Supabase counts / PASS-FAIL rows — never API keys; [[feedback_env_secrets_handling]]; screenshots capture the panel UI, not Settings key fields. |
| T-088-05-04 | Tampering (XSS regression check) | agent content rendered in panel during UAT | accept | The no-raw-HTML invariant is enforced in code (Plan 01 + Phase 087); UAT VERIFIES it renders as text, introduces no new surface. |
</threat_model>

<verification>
- 088-VALIDATION.md 4-axis scoreboard filled across all 4 providers (axes 1-3 + manual long-message); "bandwidth not cell count" satisfied.
- Chrome MCP Lighthouse a11y recorded for the live populated panel, both dark + light themes (real contrast >=4.5:1 + real focus rings).
- Keyboard walk recorded with focus-state evidence; no mouse-only path; FilePreview/overlay Escape conflict checked.
- Operator final lived-experience a11y pass recorded (G-4).
- scenario-13 ran live on Anthropic + Google; deep-flow lived pass recorded; D-17 verdict captured.
- gemini-3 thought-signature report routed (closed-verified or fix-in-088).
- VALIDATION sign-off complete; nyquist_compliant: true.
</verification>

<success_criteria>
- SC#1: 4-axis UAT matrix complete across OpenAI/Anthropic/Google(3.x)/OpenRouter (multi-tool, parallel-thread, long-message), authored in VALIDATION.md per D-01.
- SC#2/SC#3: panel surfaces pass WCAG 2.1 AA live (Lighthouse real contrast/focus + Chrome MCP keyboard walk), both themes; A11Y-01/02 signed off.
- SC#4: deep workspace flow verified live on Anthropic + Google (scenario-13 backstop + Chrome MCP lived pass), no refresh.
- D-17: gemini-3 thought-signature re-verified live and the report routed per the lifecycle.
- D-16 honored: only verified-capability blockers fixed in 088; polish deferred with re_open_triggers.
</success_criteria>

<output>
After completion, create `.planning/phases/088-cross-cutting-verification-accessibility/088-05-SUMMARY.md`
</output>
