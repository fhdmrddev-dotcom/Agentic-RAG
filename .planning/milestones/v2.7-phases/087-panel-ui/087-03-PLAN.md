---
phase: 087-panel-ui
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - frontend/src/components/panel/FilesSection.tsx
  - frontend/src/components/panel/FilePreview.tsx
  - frontend/src/components/panel/CsvTablePreview.tsx
  - frontend/src/components/panel/__tests__/FilePreview.test.tsx
  - frontend/src/components/panel/__tests__/CsvTablePreview.test.tsx
autonomous: true
requirements: [PANEL-03]
user_setup: []

must_haves:
  truths:
    - "File list renders thread files with mono name + size·vN meta, fileFlash on fresh write"
    - "Clicking a file full-replaces the list with a per-type preview and a '‹ Files' back button"
    - "md→MarkdownRenderer, code→ShikiCode, csv→CsvTablePreview, image(bucket+signed_url)→<img>, else/null-url/too-large→calm fallback"
    - "Malformed or too-large CSV falls back gracefully without breaking layout"
  artifacts:
    - path: "frontend/src/components/panel/FilesSection.tsx"
      provides: "File list rows + click-to-drill-in"
      min_lines: 40
    - path: "frontend/src/components/panel/FilePreview.tsx"
      provides: "Per-type preview router with graceful fallback"
      contains: "MarkdownRenderer"
    - path: "frontend/src/components/panel/CsvTablePreview.tsx"
      provides: "Minimal in-panel <table> from CSV string (D-01, no dep)"
      contains: "<table"
  key_links:
    - from: "FilePreview"
      to: "getWorkspaceFileContent"
      via: "fetch on file select"
      pattern: "getWorkspaceFileContent"
    - from: "FilePreview code branch"
      to: "ShikiCode"
      via: "import"
      pattern: "ShikiCode"
---

<objective>
Build the file browser + preview drill-in (PANEL-03): `FilesSection` (list rows) → `FilePreview` (full-replace per-type router) → `CsvTablePreview` (the one new renderer, D-01). Reuse `MarkdownRenderer` and `ShikiCode` (the LIVE highlighter — A1) instead of `react-syntax-highlighter`.

Purpose: Make agent-written files browsable and previewable in-panel without a refresh, reusing existing renderers (SC#3 — no new UI deps).
Output: 3 new components + their two live test files flipped GREEN.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/087-panel-ui/087-RESEARCH.md
@.planning/phases/087-panel-ui/087-PATTERNS.md
@.planning/phases/087-panel-ui/087-UI-SPEC.md
@.planning/phases/087-panel-ui/087-01-SUMMARY.md

<interfaces>
<!-- VERIFIED live signatures — use directly. -->

From frontend/src/components/chat/MarkdownRenderer.tsx:
```typescript
export function MarkdownRenderer({ content, className }: { content: string; className?: string }): JSX.Element
```

From frontend/src/components/chat/tool-bodies/ShikiCode.tsx:
```typescript
export function ShikiCode({ code, language = "python", theme = "github-dark", streaming = false }: {
  code: string        // SECURITY: treated as untrusted plain text, Shiki HTML-escapes it. Pass RAW source — never pre-render through markdown/HTML.
  language?: string
  theme?: string
  streaming?: boolean
}): JSX.Element
```

From frontend/src/lib/api.ts (Plan 01):
```typescript
export async function getWorkspaceFileContent(threadId, fileId, signal?): Promise<WorkspaceFileContent>
// WorkspaceFileContent = inline {storage_type:"inline", content} | bucket {storage_type:"bucket", signed_url: string|null}
```

From frontend/src/providers/StreamsProvider.tsx (Phase 086):
```typescript
useWorkspaceFiles(threadId: string | null): { data: WorkspaceFile[]; isLoading; error; reconcile }  // data never undefined
useViewingThread(): string | null
```

formatBytes already exists at frontend/src/components/chat/OutputFileCard.tsx:24-28 — import/copy, do NOT re-derive.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CsvTablePreview — minimal in-panel table from CSV string (D-01, no dep)</name>
  <read_first>
    - 087-PATTERNS.md §"No Analog Found" (this is the one component with no analog — minimal table, quote-aware split, graceful fallback)
    - 087-UI-SPEC.md §"Files / Preview" + Copywriting ("No preview available · Download", "File too large to preview")
    - frontend/src/components/panel/__tests__/CsvTablePreview.test.tsx (the it.todo contract from Plan 01) + fixtures.ts (mockCsvValid, mockCsvMalformed)
  </read_first>
  <behavior>
    - Test: valid CSV string (incl. a quoted field containing a comma) → renders an HTML `<table>` with a header row + N body rows; cell text matches source.
    - Test: malformed CSV (ragged rows / unparseable) → renders the calm "No preview available · Download" fallback, does NOT throw, does NOT break layout.
    - Test: too-large CSV (over a row/byte cap, e.g. >2000 rows or >256KB) → "File too large to preview" fallback.
    - Test: all cell content is rendered as TEXT (no HTML interpretation) — a CSV cell containing `<img src=x onerror=…>` renders as literal text.
  </behavior>
  <action>
    Create `CsvTablePreview.tsx` with prop `{ content: string; onDownload?: () => void }`. Parse the CSV string client-side with a small quote-aware splitter (handle `"a,b"` quoted commas + `""` escaped quotes; split rows on `\n`, trim trailing `\r`). Guard: if parse yields ragged/zero rows OR exceeds the size cap (≥2000 rows or content.length > 256_000), render the calm fallback notice (reuse the UI-SPEC copy) with an optional Download affordance — NEVER throw. Render the first row as `<thead>` and the rest as `<tbody>` (`scroll-area` / overflow-x for wide tables). All cells use `{cell}` text children (React auto-escapes — no `dangerouslySetInnerHTML`). Use the spacing/typography tokens per UI-SPEC (mono identifiers, 13px list text).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/panel/__tests__/CsvTablePreview.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/panel/CsvTablePreview.tsx` exists
    - `grep "<table" frontend/src/components/panel/CsvTablePreview.tsx` matches
    - `grep -c "dangerouslySetInnerHTML" frontend/src/components/panel/CsvTablePreview.tsx` returns 0
    - `grep "No preview available" frontend/src/components/panel/CsvTablePreview.tsx` matches the fallback copy
    - `npx vitest run …/CsvTablePreview.test.tsx` all GREEN (no .todo/.skip left)
  </acceptance_criteria>
  <done>CSV renders as a table; malformed/huge/XSS-attempt cases all fall back safely; tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FilePreview — per-type drill-in router with graceful fallback</name>
  <read_first>
    - 087-PATTERNS.md §"FilesSection + FilePreview" + §"File preview per-type routing (D-02)" code example
    - 087-RESEARCH.md Pitfall 5 (signed_url can be null → treat as fallback) + A1 (ShikiCode not RSH)
    - frontend/src/components/panel/__tests__/FilePreview.test.tsx (it.todo contract) + fixtures.ts (mockContentInline/Bucket)
    - 087-UI-SPEC.md §"Files / Preview" (‹ Files back mandatory; Escape returns to list; focus restore)
  </read_first>
  <behavior>
    - Test: inline + .md path → renders `<MarkdownRenderer>` with the content.
    - Test: inline + code ext (.py/.ts/.json) → renders `<ShikiCode>` with `code` = raw content + language from ext.
    - Test: inline + .csv → renders `<CsvTablePreview>`.
    - Test: bucket + image mime + non-null signed_url → renders `<img src={signed_url}>`.
    - Test: bucket + signed_url === null → calm "No preview available · Download" fallback (no broken img).
    - Test: unknown/binary mime → fallback. Back button labelled `‹ Files` is present in every preview state.
  </behavior>
  <action>
    Create `FilePreview.tsx` with prop `{ threadId: string; file: WorkspaceFile; onBack: () => void }`. On mount/file-change, call `getWorkspaceFileContent(threadId, file.id, signal)` (AbortController on unmount). Route:
    - `storage_type==="inline"` → branch by ext/mime: md → `<MarkdownRenderer content={content}/>`; code (langFromExt helper: py→python, ts/tsx→typescript, js→javascript, json→json, etc.) → `<ShikiCode code={content} language={lang}/>` (A1 — ShikiCode is the live highlighter; NOTE in a code comment that UI-SPEC's literal "react-syntax-highlighter" is superseded by ShikiCode per A1/Pitfall 2, no new dep); csv → `<CsvTablePreview content={content}/>`; plain text → `<pre>` or MarkdownRenderer.
    - `storage_type==="bucket"` + image mime + `signed_url !== null` → `<img src={signed_url} alt={file.path}/>`; else (null url / binary / too-large) → the calm fallback notice (UI-SPEC copy "No preview available · Download" / "File too large to preview"), must not break layout.
    Always render the `‹ Files` back button (calls `onBack`); Escape key also calls `onBack`; restore focus to the originating row is handled by FilesSection. Never `dangerouslySetInnerHTML` on raw content (MarkdownRenderer sanitizes; ShikiCode escapes; CSV/text are plain).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/panel/__tests__/FilePreview.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ShikiCode" frontend/src/components/panel/FilePreview.tsx` matches (A1 — not react-syntax-highlighter)
    - `grep -c "react-syntax-highlighter" frontend/src/components/panel/FilePreview.tsx` returns 0
    - `grep "MarkdownRenderer\|CsvTablePreview" frontend/src/components/panel/FilePreview.tsx` matches both
    - `grep "getWorkspaceFileContent" frontend/src/components/panel/FilePreview.tsx` matches
    - `grep "‹ Files" frontend/src/components/panel/FilePreview.tsx` matches the back button
    - `grep -c "dangerouslySetInnerHTML" frontend/src/components/panel/FilePreview.tsx` returns 0
    - `npx vitest run …/FilePreview.test.tsx` all GREEN
  </acceptance_criteria>
  <done>All 6 preview routes resolve; null signed_url + binary fall back calmly; ShikiCode reused for code; tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: FilesSection — file list rows with flash + click-to-drill-in</name>
  <read_first>
    - 087-PATTERNS.md §"FilesSection + FilePreview" (row chrome analog OutputFileCard.tsx:109-145; formatBytes at :24-28; states :hover bg-accent, .sel primary-dim+glow, .flash green fileFlash 1.4s; a11y role=listbox/option, Enter/Space opens, Escape returns)
    - frontend/src/components/chat/OutputFileCard.tsx (icon + mono filename + meta + formatBytes)
    - 087-UI-SPEC.md §"Component Inventory" FilesSection + Accessibility Contract (File list)
  </read_first>
  <action>
    Create `FilesSection.tsx` consuming `useWorkspaceFiles(threadId)` + `useViewingThread()`. Render each file as a row: file-type icon (lucide), mono filename, meta `formatBytes(size) · v{version}` (import/copy `formatBytes` from OutputFileCard — do NOT re-derive). Local state `selectedFile: WorkspaceFile | null`; when set, full-replace the list with `<FilePreview threadId file onBack={()=>setSelected(null)}/>`. Row states per UI-SPEC: `:hover` → `bg-accent`; selected → primary-dim bg + primary-glow border; `.flash` → green `fileFlash` 1.4s animation on a freshly-written file (key off most-recent path change). A11y: `role="listbox"` with `role="option"` rows, roving tabindex or arrow-key nav, Enter/Space opens preview, Escape returns to list AND restores focus to the originating row (store the row ref). No mouse-only path (A11Y-02). Respect `prefers-reduced-motion` for the flash. This section is rendered by `WorkspacePanel` (Plan 02) — export as default-or-named for import there.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/panel/</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/panel/FilesSection.tsx` exists
    - `grep "useWorkspaceFiles" frontend/src/components/panel/FilesSection.tsx` matches
    - `grep "FilePreview" frontend/src/components/panel/FilesSection.tsx` matches (drill-in wiring)
    - `grep "formatBytes" frontend/src/components/panel/FilesSection.tsx` matches
    - `grep "role=\"listbox\"\|role=\"option\"" frontend/src/components/panel/FilesSection.tsx` matches
    - `cd frontend && node node_modules/typescript/bin/tsc --noEmit` exits 0
    - `npx vitest run src/components/panel/` GREEN
  </acceptance_criteria>
  <done>File rows render with meta + flash + keyboard nav; click drills into FilePreview; back returns + restores focus.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent file content→render | File content (inline string or signed-URL bucket object) is agent-produced, untrusted |
| client→API | getWorkspaceFileContent crosses to the RLS-protected, thread-ownership-verified endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-087-04 | Tampering (XSS via file content) | FilePreview / CsvTablePreview | mitigate | md→MarkdownRenderer (sanitizes), code→ShikiCode (HTML-escapes per its security contract — pass RAW text, never pre-rendered), csv/text→plain React text children. Zero `dangerouslySetInnerHTML` on raw content (grep-verified =0 in both files). |
| T-087-05 | Information Disclosure (signed-URL leak / broken render) | FilePreview image branch | mitigate | signed_url is server-side 60s TTL; client never persists it; `signed_url===null` routes to fallback (no broken `<img>`, no leak). |
| T-087-06 | Information Disclosure (cross-thread file bleed) | FilesSection / FilePreview fetch | mitigate | Fetch keyed by `useViewingThread()` threadId; FilePreview AbortController cancels stale content fetches on file/thread change (mirrors Phase 086 usePanelReconcile abort). |
| T-087-07 | Denial of Service (huge CSV) | CsvTablePreview | mitigate | Size cap (≥2000 rows / >256KB) short-circuits to fallback before building DOM. |
</threat_model>

<verification>
- `tsc --noEmit` clean.
- `npx vitest run src/components/panel/` GREEN (FilePreview + CsvTablePreview tests now live, no .todo left for PANEL-03).
- grep confirms ShikiCode (not RSH) and zero dangerouslySetInnerHTML.
</verification>

<success_criteria>
File list browses thread files with size·version meta + fresh-write flash; click full-replaces into a per-type preview that reuses MarkdownRenderer/ShikiCode and renders CSV as a table; null-url/binary/too-large/malformed all fall back calmly; keyboard-operable end to end.
</success_criteria>

<output>
After completion, create `.planning/phases/087-panel-ui/087-03-SUMMARY.md`
</output>
