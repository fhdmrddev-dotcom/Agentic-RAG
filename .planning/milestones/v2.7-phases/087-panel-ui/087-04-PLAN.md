---
phase: 087-panel-ui
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - frontend/src/components/panel/VersionDiff.tsx
  - frontend/src/components/panel/DiffExpandOverlay.tsx
  - frontend/src/lib/diffParse.ts
  - frontend/src/components/panel/__tests__/VersionDiff.test.tsx
autonomous: true
requirements: [PANEL-07]

must_haves:
  truths:
    - "A pure parser splits a unified-diff string into hunk/add/del/context lines"
    - "VersionDiff renders red-base/green-target version pills with text+aria (not color-only) and a one-click Compare v{n-1}↔v{n} default"
    - "Additions/deletions render in-column with a fixed 16px sign gutter; +N/−M summary shown"
    - "delta.truncated===true surfaces an explicit 'diff truncated at 500 lines' notice"
    - "The ⤢ overlay shows the SAME payload (no second fetch) in a focus-trapped dialog"
  artifacts:
    - path: "frontend/src/lib/diffParse.ts"
      provides: "parseUnifiedDiff(diff: string): DiffLine[] — pure, testable"
      contains: "parseUnifiedDiff"
    - path: "frontend/src/components/panel/VersionDiff.tsx"
      provides: "Version pills + in-column diff + truncation notice + ⤢ trigger"
      contains: "getWorkspaceFileDiff"
    - path: "frontend/src/components/panel/DiffExpandOverlay.tsx"
      provides: "Opt-in wide diff in a shadcn dialog (never auto-widens panel)"
      contains: "Dialog"
  key_links:
    - from: "VersionDiff"
      to: "getWorkspaceFileDiff"
      via: "fetch on version pick"
      pattern: "getWorkspaceFileDiff"
    - from: "VersionDiff"
      to: "parseUnifiedDiff"
      via: "import"
      pattern: "parseUnifiedDiff"
---

<objective>
Build the version diff viewer (PANEL-07): a pure client-side unified-diff string parser (`diffParse.ts`), the `VersionDiff` component (version pills + in-column `+/−` diff + truncation notice), and `DiffExpandOverlay` (opt-in wide view in a Radix dialog). No diff library (SC#3) — the backend already emits a ready unified-diff string (D-04).

Purpose: Let the user compare any two file versions with syntax-aware coloring, surfacing backend truncation honestly.
Output: 2 components + 1 pure parser lib + the VersionDiff test flipped GREEN.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/087-panel-ui/087-RESEARCH.md
@.planning/phases/087-panel-ui/087-PATTERNS.md
@.planning/phases/087-panel-ui/087-UI-SPEC.md
@.planning/phases/087-panel-ui/087-01-SUMMARY.md

<interfaces>
<!-- VERIFIED wire shape (D-04, workspace_service.py:99-132 difflib.unified_diff output). -->

From frontend/src/lib/api.ts (Plan 01):
```typescript
export async function getWorkspaceFileVersions(threadId, fileId, signal?): Promise<WorkspaceVersion[]>  // [{id,version,size_bytes,created_at}] DESC
export async function getWorkspaceFileDiff(threadId, fileId, from, to, signal?): Promise<WorkspaceDiff>
// WorkspaceDiff.delta = { format:"unified", diff:"<string>", stats:{additions,deletions}, truncated:boolean }
```

Unified-diff line classification (parse delta.diff split on "\n"):
  "@@ ..."           → hunk header   (primary indigo bg)
  "+" not "+++"      → addition      (.add, sign "+", green)
  "-" not "---"      → deletion      (.del, sign "−", red)
  "--- v2" / "+++ v3"→ file headers  (meta — skip or render dim)
  else               → context (.ctx)

From frontend/src/components/ui/dialog.tsx — Radix Dialog wrapper (focus-trap + Escape + restore free).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parseUnifiedDiff — pure unified-diff string parser (Pattern 2, no lib)</name>
  <read_first>
    - 087-PATTERNS.md §"Wire Contracts §diff" + §"Pattern 2: Client-side unified-diff parse"
    - 087-RESEARCH.md Pattern 2 line-classification rules + Pitfall 4 (truncation)
    - frontend/src/components/panel/__tests__/VersionDiff.test.tsx (it.todo string-parse contract) + fixtures.ts (mockDiffString, mockDiffTruncated)
  </read_first>
  <behavior>
    - Test: `parseUnifiedDiff("--- v2\n+++ v3\n@@ -1,5 +1,7 @@\n ctx\n-old\n+new\n")` → ordered `DiffLine[]` with kinds: header, header, hunk, context, del, add.
    - Test: a line `"+++"` is classified header (not add); `"---"` is header (not del).
    - Test: `"+added"` → {kind:"add", text:"added", sign:"+"}; `"-removed"` → {kind:"del", text:"removed", sign:"−"}; `" same"` → {kind:"context", text:"same"}.
    - Test: empty string → [].
  </behavior>
  <action>
    Create `frontend/src/lib/diffParse.ts` exporting `type DiffLine = { kind: "hunk"|"add"|"del"|"context"|"header"; text: string; sign?: "+"|"−" }` and a pure `parseUnifiedDiff(diff: string): DiffLine[]`. Split on `\n`; classify each line: starts with `@@` → hunk; starts with `+++ ` or `--- ` → header; starts with `+` (and not `+++`) → add (strip leading `+`, sign "+"); starts with `-` (and not `---`) → del (strip leading `-`, sign "−" — the Unicode minus per UI-SPEC, not ASCII hyphen); else → context (strip a single leading space if present). No React, no fetch — pure function for fast unit testing.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/panel/__tests__/VersionDiff.test.tsx -t "parse"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/diffParse.ts` exists
    - `grep "export function parseUnifiedDiff\|export const parseUnifiedDiff" frontend/src/lib/diffParse.ts` matches
    - `grep -c "react\|fetch" frontend/src/lib/diffParse.ts` returns 0 (pure)
    - The parse-related tests in VersionDiff.test.tsx are GREEN
  </acceptance_criteria>
  <done>Pure parser classifies hunk/add/del/context/header correctly; tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: DiffExpandOverlay — opt-in wide diff in a Radix dialog</name>
  <read_first>
    - 087-PATTERNS.md §"VersionDiff + DiffExpandOverlay" (analog ui/dialog.tsx; never auto-widen panel; ⤢ opens overlay over chat)
    - frontend/src/components/ui/dialog.tsx (the Radix wrapper to use)
    - 087-UI-SPEC.md §"Accessibility Contract" (⤢ overlay: focus trap + Escape + focus restore via Radix) + §"Versions / Diff"
  </read_first>
  <action>
    Create `DiffExpandOverlay.tsx` with prop `{ open: boolean; onOpenChange: (o:boolean)=>void; lines: DiffLine[]; truncated: boolean; label: string }`. Render a shadcn `Dialog` (Radix → free focus-trap/Escape/restore) sized wide over chat. Inside, render the SAME parsed `DiffLine[]` it receives (NO second fetch — D-04). Reuse the same diff-line renderer markup as VersionDiff (extract a shared `<DiffLines lines truncated/>` presentational sub-component, or co-locate and import it). Surface the truncation notice when `truncated`. `role="region" aria-label={label}` (e.g. "Diff v2 to v3"). The panel itself must NEVER auto-widen — this overlay is the only wide affordance.
  </action>
  <verify>
    <automated>cd frontend && node node_modules/typescript/bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/panel/DiffExpandOverlay.tsx` exists
    - `grep "Dialog" frontend/src/components/panel/DiffExpandOverlay.tsx` matches (Radix dialog reuse)
    - `grep -c "getWorkspaceFileDiff\|fetch" frontend/src/components/panel/DiffExpandOverlay.tsx` returns 0 (no second fetch — receives lines as prop)
    - `tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Overlay renders the passed-in diff lines in a focus-trapped dialog; no extra fetch; panel never auto-widens.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: VersionDiff — version pills + in-column diff + truncation notice + ⤢</name>
  <read_first>
    - 087-PATTERNS.md §"VersionDiff + DiffExpandOverlay" + §"Diff fetch + version pills" code example
    - 087-UI-SPEC.md §"Versions / Diff" (red .a base / green .b target pills; Compare v{n-1}↔v{n} latest-two default; +N/−M summary; 16px sign gutter; .diff scrolls-x never wraps; ⤢ opt-in) + Accessibility (version picker text/aria not color-only; diff role=region)
    - frontend/src/components/panel/__tests__/VersionDiff.test.tsx + fixtures.ts (mockVersions, mockDiffTruncated/NonTruncated)
  </read_first>
  <behavior>
    - Test: given mockVersions (DESC), the default selection is latest two (Compare v{n-1}↔v{n}); base pill = lower version (red, aria base), target pill = higher (green, aria target).
    - Test: rendering mockDiffNonTruncated → add lines green with "+" gutter, del lines red with "−" gutter, hunk header indigo; +N/−M summary matches delta.stats.
    - Test: mockDiffTruncated (truncated:true) → an explicit "diff truncated at 500 lines" notice is present.
    - Test: pills carry text/aria labels (e.g. aria-label="base version 2" / "target version 3") — not color-only.
  </behavior>
  <action>
    Create `VersionDiff.tsx` with prop `{ threadId: string; file: WorkspaceFile }`. On mount, `getWorkspaceFileVersions(threadId, file.id)`; default-select the latest two (`from = v{n-1}`, `to = v{n}`); fetch `getWorkspaceFileDiff(threadId, file.id, from, to)` (AbortController on version-change). Parse `delta.diff` via `parseUnifiedDiff`. Render: version-pill strip (`.a` red base/before with `aria-label="base version {from}"`, `.b` green target/after with `aria-label="target version {to}"`, keyboard-operable; "Compare" label + one-click latest-two default); `+{additions}/−{deletions}` summary from `delta.stats`; the in-column diff (shared `<DiffLines>` from Task 2 — fixed 16px `.sign` gutter, `overflow-x` scroll, never wrap, `line-height:1.6`); when `delta.truncated` render the "diff truncated at 500 lines" notice; a `⤢` button that opens `<DiffExpandOverlay>` passing the SAME parsed lines + truncated flag (no re-fetch). `role="region" aria-label={`Diff v${from} to v${to}`}`. Respect `prefers-reduced-motion`. Rendered by FilesSection/WorkspacePanel as the Versions section body.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/panel/__tests__/VersionDiff.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "getWorkspaceFileDiff" frontend/src/components/panel/VersionDiff.tsx` matches
    - `grep "parseUnifiedDiff" frontend/src/components/panel/VersionDiff.tsx` matches
    - `grep "truncated" frontend/src/components/panel/VersionDiff.tsx` matches (notice surfaced)
    - `grep "aria-label" frontend/src/components/panel/VersionDiff.tsx` shows base/target version labels (not color-only)
    - `grep "DiffExpandOverlay" frontend/src/components/panel/VersionDiff.tsx` matches (⤢ wiring)
    - `npx vitest run …/VersionDiff.test.tsx` all GREEN (no .todo left for PANEL-07)
  </acceptance_criteria>
  <done>Version pick + in-column diff + truncation notice + ⤢ overlay all functional; pills accessible; tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent diff string→render | The unified-diff string is derived from agent-written file versions (untrusted text) |
| client→API | getWorkspaceFileVersions/Diff cross to RLS-protected, ownership-verified endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-087-08 | Tampering (XSS via diff content) | VersionDiff / DiffExpandOverlay | mitigate | Each diff line is rendered as a React text child (`{line.text}`) inside styled spans — never `dangerouslySetInnerHTML`. A diff line containing markup renders as literal text. (grep-verified =0 dangerouslySetInnerHTML in both component files.) |
| T-087-09 | Information Disclosure (cross-thread diff bleed) | VersionDiff fetch | mitigate | Fetch keyed by threadId + AbortController cancels stale diff fetches on version/file/thread change. |
| T-087-10 | Denial of Service (huge diff) | VersionDiff render | accept | Backend already truncates at 500 diff lines (delta.truncated) — client renders a bounded payload; the notice surfaces the cap. No client-side compute. |
</threat_model>

<verification>
- `tsc --noEmit` clean.
- `npx vitest run …/VersionDiff.test.tsx` GREEN (parser + render + truncation + pill-aria).
- grep: zero dangerouslySetInnerHTML across VersionDiff + DiffExpandOverlay; DiffExpandOverlay does no second fetch.
</verification>

<success_criteria>
User compares any two versions (default latest-two); additions/deletions render in-column with the fixed sign gutter and a +N/−M summary; truncation is surfaced honestly; ⤢ opens a focus-trapped wide overlay on the same payload; version pills are accessible (text+aria, not color-only).
</success_criteria>

<output>
After completion, create `.planning/phases/087-panel-ui/087-04-SUMMARY.md`
</output>
