---
phase: 087-panel-ui
plan: 07
type: execute
wave: 2
depends_on: [06]
files_modified:
  - frontend/src/index.css
  - frontend/src/components/panel/WorkspacePanel.tsx
  - frontend/src/components/panel/PanelRail.tsx
autonomous: false
gap_closure: true
requirements: [PANEL-01]
must_haves:
  truths:
    - "In dark theme the panel reads as a distinct surface from the chat — visible left border + surface tint separation (panel != chat background)"
    - "In light theme the 52px rail reads as a distinct strip from the page (rail surface/border separation)"
    - "All re-architected states pass live Chrome MCP re-verification: no overflow, flush-right panel, stable left edge, persistent toggle reopens a hidden panel, zero pane-mock-eval in prod, <768px bottom-sheet intact"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "Strengthened panel/rail surface + border tokens for both themes (or a dedicated panel-surface token)"
    - path: "frontend/src/components/panel/WorkspacePanel.tsx"
      provides: "Panel container left-border/surface classes that read as a distinct surface in dark"
    - path: "frontend/src/components/panel/PanelRail.tsx"
      provides: "Rail strip surface/border classes that read as distinct in light"
  key_links:
    - from: "frontend/src/components/panel/WorkspacePanel.tsx"
      to: "frontend/src/index.css panel/border tokens"
      via: "bg-sidebar + border-l class (or new token) with strengthened contrast"
      pattern: "border-l|bg-sidebar"
    - from: "frontend/src/components/panel/PanelRail.tsx"
      to: "frontend/src/index.css rail surface"
      via: "rail strip border/surface class"
      pattern: "border-l|bg-"
---

<objective>
Strengthen the panel↔chat visual separation (dark theme) and the rail↔page separation (light theme) so the panel and rail read as distinct surfaces per the locked panel-shell design contract, then run the full live Chrome MCP re-verification of all 7 UAT gaps.

Closes UAT gaps 5 and 6 (cosmetic surface contrast) and provides the lived-experience verification gate (G-4) for the entire 087 gap-closure effort.

Purpose: After Plan 06 makes the layout correct, the panel still does not read as its own surface (dark: bg-sidebar ≈ bg-background, low-contrast border; light: rail bg ≈ page). This plan tunes the tokens/borders and then proves the whole re-architecture live.

Output: A panel + rail that visually separate from chat/page in both themes, verified end-to-end in a real browser.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/087-panel-ui/087-UAT.md
@CLAUDE.md

# Locked design contract — the panel-shell surface language.
@.claude/skills/sketch-findings-agentic-rag/references/panel-shell.md

# Files this plan tunes (read first).
@frontend/src/components/panel/WorkspacePanel.tsx
@frontend/src/components/panel/PanelRail.tsx

<token_context>
index.css current values (HSL channels, no hsl() wrapper):
  LIGHT  :root  --background 220 20% 97% · --sidebar 220 15% 95% · --border 220 13% 89%
  DARK   .dark  --background 216 45% 4%  · --sidebar 220 40% 5%  · --border 220 20% 16%

Gap root cause: in dark, --sidebar (5% L) ≈ --background (4% L) → panel doesn't separate;
border-border (16% L) is low-contrast. In light, the rail uses --sidebar (95% L) ≈ --background (97% L)
→ rail floats. The panel container uses `border-l border-border bg-sidebar`; the rail uses
`border-l border-border` over the same bg-sidebar surface.

Approach (executor's discretion within the locked surface language — do NOT redesign):
  - Strengthen the panel left border (e.g. a dedicated `--panel-border` token, or bump the border
    opacity/lightness for the panel edge specifically) so it reads in both themes.
  - Nudge the panel surface tint away from --background: in DARK lift --sidebar a step (e.g. ~7-8% L)
    or introduce a `--panel-surface` token; in LIGHT drop the rail/panel surface a step (e.g. ~92% L)
    so it separates from the 97% page. Keep it a TINT, not a redesign — matches the Deep Midnight /
    Aether Light palettes already in use (bg-sidebar vs bg-background per UAT gap-5 `expected`).
  - Verify the chosen pairs keep ≥4.5:1 where text sits on them (CONTEXT Claude's-Discretion contrast rule).
Prefer a token-level change in index.css so both themes stay coherent; touch WorkspacePanel/PanelRail
classNames only to point at the strengthened token (e.g. add a stronger left-border class).
</token_context>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen panel + rail surface/border separation in both themes (gaps 5, 6)</name>
  <files>frontend/src/index.css, frontend/src/components/panel/WorkspacePanel.tsx, frontend/src/components/panel/PanelRail.tsx</files>
  <action>
1. In index.css, strengthen the panel/rail surface separation for BOTH themes. Recommended (executor may choose an equivalent token approach):
   - DARK (.dark): nudge `--sidebar` up so the panel surface separates from `--background 216 45% 4%` — e.g. `--sidebar: 220 40% 7%` (or 8%) — and add a dedicated panel edge token `--panel-border: 220 25% 22%` (clearer than the 16% body border).
   - LIGHT (:root): nudge the panel/rail surface down from the 97% page — e.g. `--sidebar: 220 15% 93%` — and `--panel-border: 220 13% 82%`.
   Keep the existing `--border`/`--sidebar` usages elsewhere intact if you add `--panel-border` as a NEW token (preferred — avoids regressing NavPanel which also uses bg-sidebar). If you instead bump `--sidebar` directly, sanity-check NavPanel still looks right (it shares the token).
2. In WorkspacePanel.tsx, change the desktop panel container's left border from `border-l border-border` to the strengthened edge, e.g. `border-l border-[hsl(var(--panel-border))]` (keep `bg-sidebar`). This is the container introduced in Plan 06 Task 2.
3. In PanelRail.tsx, give the 52px strip the same strengthened left border (`border-l border-[hsl(var(--panel-border))]`) so the collapsed rail reads as a distinct strip in light theme; keep its existing layout/icons.
4. Verify the amber/text contrast pairs that sit on these surfaces still meet ≥4.5:1 (CONTEXT contrast rule) — if a tint change drops a pair below, darken the dim background rather than lighten text.

Token-level change only; no layout change, no new dependency, no Tailwind config edit (use the `[hsl(var(--token))]` arbitrary-value pattern already used across the panel components).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx vitest run src/components/panel</automated>
  </verify>
  <done>index.css carries strengthened panel/rail surface + border tokens for both themes; WorkspacePanel + PanelRail point at the strengthened edge; tsc + panel tests green; NavPanel unaffected (or intentionally re-checked if --sidebar bumped).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live Chrome MCP re-verification of all 7 UAT gaps (G-4 lived-experience gate)</name>
  <what-built>
The full Phase 087 panel re-architecture: ChatLayout-level chat|panel grid (flush-right stable ~30% panel), DevTwoPaneMock removed from prod, persistent chat-header workspace toggle with pulsing-amber-dot pending indicator, and strengthened panel/rail surface contrast in both themes. Plans 06 + 07 combined.
  </what-built>
  <how-to-verify>
Backend + frontend running (user starts backend uvicorn themselves; `cd frontend && npm run dev`). Log in at http://localhost:5173/ (fhdmrd@gmail.com / 123456). Drive Chrome DevTools MCP. Run the re-verify protocol at viewport 1442 AND 1280, on an empty/plain-prose thread AND a content-heavy thread (one with workspace files/todos), in BOTH dark and light themes:

(a) NO OVERFLOW — in open, rail, and hidden state assert `document.documentElement.scrollWidth <= window.innerWidth` (no horizontal scrollbar). This was the gap-1 failure (documentScrollW up to 1688 vs viewport 1442-1536).
(b) FLUSH RIGHT — in open state the panel's right edge == viewport right edge (no empty gap to its right). Measure the panel container's getBoundingClientRect().right ≈ innerWidth.
(c) STABLE POSITION — the panel's left edge x is IDENTICAL across the empty thread and the content-heavy thread (gap-1 was 1230/1262/1388 per thread). gridTemplateColumns should resolve to `1fr clamp(...)`, not `0px 300px`.
(d) PERSISTENT TOGGLE — a workspace toggle button is present in the chat header top-right; hide the panel (click toggle or ⌘./Ctrl+.), then click the persistent toggle and confirm the panel reopens by MOUSE ALONE on a thread with no panel-owned seam event. Confirm the pulsing amber dot appears on the toggle when an ask_user is pending AND the panel is closed (seed a pending ask if feasible; otherwise note as carry-forward).
(e) NO DEV LEAK — assert `document.querySelector('[data-testid="pane-mock-eval"]')` is null in the production tree.
(f) MOBILE INTACT — at <768px (375px) the panel still renders as a bottom Sheet (Radix dialog), does not occlude the composer, dismissable.
(g) SURFACE CONTRAST (gaps 5/6) — in dark theme the panel reads as a distinct surface from chat (visible border + tint); in light theme the 52px rail reads as a distinct strip from the page. Eyeball against the panel-shell contract.
(h) NO DEAD BAND (gap 7) — in open state confirm the chat content centers within its own column with no large empty band before the panel border.

Also carry-forward the 3 UAT 'Not Reached' visual checks if data permits (note as deferred if not): multi-version VersionDiff (red/green pills, Compare, ⤢ overlay), live ask_user PendingAskCard (amber→green-on-answer), reloaded SeamCard vs live SeamPointer after a real page reload.
  </how-to-verify>
  <resume-signal>Type "approved" if all of (a)-(h) pass, or describe which checks failed (with viewport/theme/thread) so the gap can be re-opened.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Token/className cosmetic change + a human verification gate. No new input, network, or auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-087-07-01 | Tampering | --sidebar token shared with NavPanel | mitigate | Prefer a NEW `--panel-border` token + a contained surface nudge; if `--sidebar` is bumped directly, re-verify NavPanel in the Chrome MCP gate (item g) so the shared-token change doesn't regress the nav. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean; `cd frontend && npx vitest run src/components/panel` green.
- Full suite stays at the documented 17-failure baseline (no new reds).
- Chrome MCP checkpoint (a)-(h) all pass at 1442 + 1280, empty + content-heavy thread, both themes.
</verification>

<success_criteria>
- Panel reads as a distinct surface from chat in dark theme; rail reads as a distinct strip in light theme (gaps 5/6 closed).
- All 7 UAT gaps verified closed live via Chrome MCP across the required viewport × thread × theme matrix.
- No new test failures; mobile bottom-sheet + NavPanel unregressed.
</success_criteria>

<output>
After completion, create `.planning/phases/087-panel-ui/087-07-SUMMARY.md`
</output>
