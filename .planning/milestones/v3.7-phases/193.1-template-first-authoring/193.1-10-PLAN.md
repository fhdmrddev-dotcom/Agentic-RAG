---
phase: 193.1-template-first-authoring
plan: 10
type: execute
wave: 7
depends_on: ["193.1-09"]
files_modified:
  - CLAUDE.md
  - .planning/phases/193.1-template-first-authoring/193.1-UAT.md
  - scripts/vitest-count-gate.cjs
autonomous: false
requirements: [AUTH-03]

must_haves:
  truths:
    - "D-01 / D-03 / D-22: all three G-5-firing files this phase touched carry a ledger row with figures RE-DERIVED at close, not re-read from a cell"
    - "D-01: `.planning/STATE.md` records NO guardrail override for Phase 193.1 — a waiver was offered and declined, and that absence is a measurement"
    - "G-4: the three lived-experience rows are driven by the operator; a row may be ⛔ with its reason but never silently omitted"
    - "Every gate is re-run at the phase's own HEAD and its numbers recorded, not inherited from a wave merge"
  artifacts:
    - path: "CLAUDE.md"
      provides: "corrected hot-file ledger rows for WorkflowBuilderPage.tsx, backend/app/api/workflows.py and PhaseFormPanel.tsx"
    - path: ".planning/phases/193.1-template-first-authoring/193.1-UAT.md"
      provides: "four driven rows with evidence and verdicts"
  key_links:
    - from: "CLAUDE.md hot-file ledger"
      to: "the next phase's discuss-phase G-5 audit"
      via: "the audit scans PLAN.md files_modified AGAINST this table"
      pattern: "PhaseFormPanel.tsx"
---

<objective>
Close the phase honestly: re-run every gate at HEAD, correct the three hot-file ledger rows with
figures re-derived rather than re-read, and drive the three lived-experience rows the code cannot
answer.

Purpose: this project's standing lesson is that **a guardrail cannot see what is absent from its
list** — G-5 never fired on `WorkflowsPage.tsx` across ten phases and never on `WorkflowDoorSwitch.tsx`
across six, purely because the audit scans `files_modified` AGAINST the ledger table. This phase
touched three G-5-firing files and measured all three ledger cells stale. A phase that leaves them
stale hands the next phase the same blindness.

Output: three corrected ledger rows, four driven UAT rows, and a recorded set of gate numbers.
</objective>

<worktree_protocol>
⚠ **Task 3's instrument is the operator's running dev app (frontend :5173 + backend), against the
PRIMARY TREE** — exactly as `193-11` was. Do not dispatch Task 3 into a worktree, do not start or
restart the operator's services, and do not pull browser telemetry yourself: the operator drives the
browser. Tasks 1 and 2 may run anywhere; if in a worktree, `bash scripts/bootstrap-worktree.sh
"$(pwd)"` first and assert `git rev-parse HEAD` equals the dispatched base SHA.

`GSD_VITEST_MAX_WORKERS=2` on every vitest invocation.
</worktree_protocol>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/193.1-template-first-authoring/193.1-CONTEXT.md
@.planning/phases/193.1-template-first-authoring/193.1-VALIDATION.md
@.planning/phases/193.1-template-first-authoring/193.1-UAT.md
</context>

<interfaces>
<!-- Figures measured at plan time (HEAD 568afc7a). ALL OF THEM WILL BE STALE BY THIS PLAN'S OWN
     COMMITS — re-derive every one with the commands given, never quote these. -->

**The three rows this phase owes, and what each cell says today:**

| File | `CLAUDE.md` cell at plan time | Measured 2026-08-14 | Ruling recorded in CONTEXT |
|---|---|---|---|
| `frontend/src/pages/WorkflowBuilderPage.tsx` | 33 commits / 2055 L | **34 / 2073** | D-01 — G-5 fired, HONOURED by extraction (Plan 05), no override |
| `backend/app/api/workflows.py` | 32 commits / 1813 L | **33 / 1813** | D-03 — honoured BY CONSTRUCTION (a third door in the template block it already owns), obligation inherited forward |
| `frontend/src/components/workflows/PhaseFormPanel.tsx` | *"140 / 183 / 184 / 185 — 1095 L"* — **wrong in BOTH directions**: it names phase `140`, which the standard derivation does not return, and omits `103`, `155`, `189`, `193` | **15 commits · 7 phases (103 155 183 184 185 189 193) + quick `260814` · 1136 L** | D-22 — G-5 fires; honoured BY CONSTRUCTION (own component, one gated line at `:1086`), no override |

**Re-derive with (never re-read the cell):**
`git log --oneline -- <file> | wc -l` · `wc -l <file>` ·
`git log --format=%s -- <file> | sed -E 's/^[a-z]+\(([^)]+)\).*/\1/' | sed -E 's/-.*//' | sort -u`
⚠ That `sed` recipe returns QUICK-TASK buckets (`260809`, `260814`) alongside phases. Count phases
and state the quick tasks separately, exactly as the `WorkflowBuilderPage.tsx` row already does.

**The house habit for these cells, which this plan must follow rather than overwrite:** state the
correction BESIDE the previous value, with the command that produced it, so a reader can see what
changed. Four cells have gone stale about themselves this month and each says so.

**The four UAT rows** live in `193.1-UAT.md` (opened by Plan 04). U1 (SC#2) was driven in Wave 1.
U2, U3 and U4 are this plan's.

⚠ **`take_screenshot` times out on this setup — read DOM geometry via `evaluate_script`.**
⚠ **Do not drive any row with the operator's three SEED-prefixed templates** — they correctly render
*"could not read"*.

**Gate baselines to compare against:** `tsc -p tsconfig.app.json` → **33** (⚠ `--noEmit` WITHOUT
`-p tsconfig.app.json` checks ZERO files). Count gate at cap 2 before this phase: **exit 0 · total
3604 · failed 0 · 68/68 pinned**. `CLAUDE.md`'s rule table still says worker cap `4` and that value
has ROTTED — its own prose beneath documents the rot.

⚠ **Phase 193 is STILL NOT COMPLETE and still owes UAT rows U1/U2 of ITS OWN file
(`193-UAT.md`).** They are NOT this phase's rows and must not be driven, closed or referenced as
satisfied here. Say so in the SUMMARY so a reader cannot mistake this phase closing for that one.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Re-run every gate at HEAD and record the numbers</name>
  <files>scripts/vitest-count-gate.cjs</files>
  <read_first>
    - .planning/phases/193.1-template-first-authoring/193.1-VALIDATION.md § Sampling Rate and § Validation Sign-Off
    - scripts/vitest-count-gate.cjs — the `BASELINE` entries this phase touched
  </read_first>
  <action>
    Run all four gates at the phase's own HEAD, in a fresh run rather than quoting a wave merge:
    the frontend count gate at `GSD_VITEST_MAX_WORKERS=2`, `tsc --noEmit -p tsconfig.app.json`, the
    backend unit suite, and `npx eslint` over the directories this phase touched.

    Record, in the SUMMARY, a before/after table: count-gate `total`, `failed`, pinned-file count and
    pinned total; the tsc error count; the backend pass/fail counts against the figure Plan 02
    recorded; and the eslint count. **State every delta rather than only the passes.**

    Sweep the `BASELINE` object for any suite this phase touched whose pinned number no longer
    matches the script's own `actual` column, and re-pin it here. Also check for unpinned surplus in
    files this phase touched — the gate tolerates `total > pinned` and only fails on a per-file
    DECREASE, so a surplus is silent. ⚠ `PhaseTimeline.test.tsx` was recorded at 17 pinned / 21 actual
    before this phase; it is NOT this phase's file and NOT this phase's debt — do not re-pin it, but
    note whether the surplus is still there.

    If any gate is red, STOP and report the exact failure rather than adjusting a pin to make it green.
  </action>
  <acceptance_criteria>
    - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` exits 0 with `failed 0`, and the SUMMARY records `total`, `failed`, pinned files and pinned total.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports **33**.
    - `cd backend && venv/Scripts/python.exe -m pytest tests/unit -q` shows no new failure vs Plan 02's recorded baseline.
    - The SUMMARY carries a before/after table with every delta named.
  </acceptance_criteria>
  <verify>
    <automated>GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>Every gate is green at HEAD and its numbers are recorded rather than inherited.</done>
</task>

<task type="auto">
  <name>Task 2: Correct all three hot-file ledger rows, with figures re-derived at close</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md § "Workflow guardrails (MANDATORY)" — the hot-file ledger table, and specifically the `WorkflowsPage.tsx`, `WorkflowCard.tsx` and `WorkflowDoorSwitch.tsx` rows, which are the house model for how a correction is stated beside its previous value
    - .planning/phases/193.1-template-first-authoring/193.1-CONTEXT.md D-01, D-03 and the D-22 amendment
  </read_first>
  <action>
    Re-derive all three files' commit counts, phase lists and line counts with the commands in
    `<interfaces>`, at this phase's HEAD, and update their cells.

    For `frontend/src/pages/WorkflowBuilderPage.tsx`: record that G-5 FIRED and was HONOURED BY
    EXTRACTION rather than waived — the cut shipped in Wave 2, BEFORE the feature it made room for
    (the 192.1 D-01 order) — name the module it went to and the measured subtree growth (the page's
    own before/after plus every module this phase created), and state the dominant term rather than
    smoothing it. Name what the page still hosts and what the next natural seam is, so the next phase
    inherits a measured count instead of re-deriving one.

    For `backend/app/api/workflows.py`: record that G-5 FIRES ON THE COUNT and was deliberately NOT
    taken, with the measured reason — the module gained a THIRD door in the template block it already
    owns, twenty lines from its two siblings, not a second concern. **State that the obligation is
    INHERITED FORWARD, not discharged.**

    For `frontend/src/components/workflows/PhaseFormPanel.tsx`: the cell is currently wrong in both
    directions and this phase is the first to say so. Replace it with the re-derived figures, record
    that G-5 fires at 7+ phases, and record that it was honoured BY CONSTRUCTION under that row's own
    standing order — own component, one gated line at the `pt === "llm_emit"` mount — which Phase 185
    honoured, Phase 193 honoured again, and this phase honoured a third time.

    In all three cells: **state the previous value beside the corrected one** and paste the command
    that produced the new one. ⚠ Note in the `WorkflowBuilderPage.tsx` cell that any figure written at
    a phase's close goes stale on that phase's own next commit — this table has documented that about
    itself four times, and Task 2's own commit will touch `CLAUDE.md` but not the measured files.

    **Record explicitly, in all three cells, that NO guardrail override exists for Phase 193.1** — a
    waiver was offered and DECLINED (D-01), so `.planning/STATE.md` carries none, and that absence is
    a measurement rather than an omission.

    ⚠ Do NOT edit the vitest-worker-cap rule table in the same commit — its rot is real and already
    documented in its own prose; changing it is a separate decision with its own measurement, and this
    phase re-measured only that cap 2 holds at 3604 cases.
  </action>
  <acceptance_criteria>
    - All three rows contain a re-derived commit count, phase list and line count, each with the command that produced it.
    - Each of the three cells contains the string "no guardrail override" (or an equivalent explicit statement) for Phase 193.1.
    - `grep -c "PhaseFormPanel.tsx" CLAUDE.md` >= 1 and its row no longer reads `1095 L` or names phase `140` as its whole list.
    - `git diff --stat CLAUDE.md` shows the ledger table changed and no other section (in particular, the `GSD_VITEST_MAX_WORKERS` rule row is untouched).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; for f in frontend/src/pages/WorkflowBuilderPage.tsx backend/app/api/workflows.py frontend/src/components/workflows/PhaseFormPanel.tsx; do echo "$f commits=$(git log --oneline -- $f | wc -l) lines=$(wc -l < $f)"; done; grep -c "PhaseFormPanel.tsx" CLAUDE.md'</automated>
  </verify>
  <done>Three ledger rows carry measured figures, so the next phase's G-5 audit can see the files this phase made hot.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: The operator drives U2, U3 and U4 — the three rows the code cannot answer</name>
  <files>.planning/phases/193.1-template-first-authoring/193.1-UAT.md</files>
  <read_first>
    - .planning/phases/193.1-template-first-authoring/193.1-UAT.md (the four rows and the SC#2 artifact already recorded)
    - .planning/phases/193.1-template-first-authoring/193.1-VALIDATION.md § Manual-Only Verifications
  </read_first>
  <what-built>
    A pre-draft template row on BOTH describe screens (the fast door and the govern door), with a
    five-arm footing line, a CTA that refuses while the read is in flight, `template_placeholders` on
    the real `/generate` call, an auto-bind on first save with its own failure line, and a three-bucket
    name check on the step rail for a template attached to an existing draft.
  </what-built>
  <how-to-verify>
    Drive all three rows against your running dev app. **Do not use the three SEED-prefixed templates**
    — they correctly render "could not read".

    **U2 — the two-templates collision (G-4).** Open the Builder's describe screen with the canvas flag
    ON, so the shipped starter picker is present. Read the screen and say, UNPROMPTED, which control
    does what. ⚠ The attach button deliberately reuses the shipped `TEMPLATE_ATTACH_LABEL`
    (*"Attach a template"*) so two doors onto one act do not get two names — while roughly two inches
    away sits the shipped `STARTER_DOOR_LINE` (*"Not sure where to start? Start from a template."*),
    which seeds the describe box from a curated starter WORKFLOW. **Silence or a wrong answer is a
    FAIL**, and the fix is copy, not code.

    **U3 — the disabled CTA reads "not ready", not "broken" (G-4, D-07 + D-09).** Attach a large real
    `.docx` and press the draft CTA immediately. The button must be disabled AND the footing line must
    say why. Then let the read finish and confirm the button re-enables and the fields list appears.
    Repeat once on the FAST door.

    **U4 — the describe-column scroll.** With the row present at a real laptop viewport, read the DOM
    geometry via Chrome MCP `evaluate_script` (⚠ `take_screenshot` times out on this setup). Sketch
    time measured `scrollHeight 761` vs `clientHeight 614` at the 660 px stage; report both numbers and
    whether the switch-to-govern strip falls below the fold.

    Also worth exercising while you are there, and recorded as evidence either way: attach a template,
    press draft, and confirm the resulting workflow's step rail shows the template as bound (the D-06
    auto-bind) — and that if the bind fails, exactly one honest line appears and the save does NOT
    report an error.
  </how-to-verify>
  <action>
    Record each row's verdict with its evidence: the operator's own words for U2, the observed button
    state and footing sentence for U3, the two geometry numbers for U4. A row may be recorded ⛔ with
    its reason and a blocking issue id, but **never silently omitted** — a scoreboard listing only what
    passed is not a scoreboard.

    For any FAIL, size it before routing it: ≤ 1 file and ≤ 10 lines with no schema or API surface is
    `/gsd:fast` under G-3, not a gap-closure round. Record the size and the route beside the failure.

    ⚠ In the SUMMARY, state plainly that **Phase 193's own U1/U2 rows in `193-UAT.md` remain owed** and
    are NOT discharged by this phase.
  </action>
  <resume-signal>Report each row as pass / fail / blocked, with the evidence named above.</resume-signal>
  <verify>
    <human-check>All four rows in `193.1-UAT.md` carry a verdict and its evidence; none is blank and none was removed.</human-check>
  </verify>
  <done>The lived-experience rows are driven and recorded, with failures sized and routed rather than deferred silently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none introduced | Documentation, gate re-runs and human verification. No route, no input, no render path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193.1-10-01 | Repudiation | a stale ledger cell | mitigate | All three rows re-derived with the commands recorded inline, and the previous value stated beside the new one. A guardrail cannot see what is absent from its list — and cannot see through a wrong figure either. |
| T-193.1-10-02 | Repudiation | a scoreboard listing only passes | mitigate | Every row carries a verdict; ⛔ is permitted with a reason and a blocking id; omission is not. Phase 193's own owed rows are named as still owed. |
| T-193.1-10-03 | Tampering | a pin adjusted to make a gate green | mitigate | Task 1 stops and reports on any red rather than re-pinning; re-pins are permitted only where the script's own `actual` column moved for a suite this phase edited. |
| T-193.1-10-04 | Information disclosure | UAT evidence | mitigate | Record observed UI text and DOM geometry only. No bearer token, no API key, no `Authorization` header in the artifact. |
| T-193.1-10-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed.** |
</threat_model>

<verification>
- All four gates green at HEAD with recorded numbers.
- Three ledger rows corrected, each with its derivation command and its previous value.
- `193.1-UAT.md` carries four rows with verdicts and evidence.
- `.planning/STATE.md` carries NO guardrail override for Phase 193.1 (verify by reading; do not add one).
- ⚠ **No migration was added by this phase, and none was expected** (RESEARCH § Runtime State Inventory).
</verification>

<success_criteria>
The phase closes with measured figures rather than inherited ones: three hot-file ledger rows
corrected, four UAT rows driven and recorded with evidence, every gate re-run at HEAD, and no
guardrail override anywhere in the phase.
</success_criteria>

<output>
Create `.planning/phases/193.1-template-first-authoring/193.1-10-SUMMARY.md` when done.
</output>
