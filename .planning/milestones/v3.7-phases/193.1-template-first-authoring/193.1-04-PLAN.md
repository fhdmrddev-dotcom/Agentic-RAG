---
phase: 193.1-template-first-authoring
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/193.1-template-first-authoring/193.1-UAT.md
autonomous: false
requirements: [AUTH-03]

must_haves:
  truths:
    - "D-23: SC#2's real `/generate` call is driven EARLY, as its own checkpoint, not discovered at the phase gate"
    - "D-19: the returned definition is inspected for an `llm_emit` phase with `emitter: \"render_template\"` FIRST (binary, mechanical) and for placeholder-name overlap SECOND (the model's judgement)"
    - "The raw returned definition is recorded as an artifact — a verdict without the artifact is not evidence"
    - "Which model and which provider served the call is recorded (the cross-provider roster ruling in VALIDATION.md is carried, not re-opened)"
    - "D-20: the 167-C heading's claim about the ORDER OF EVENTS is measured against the definition schema before any copy ships"
  artifacts:
    - path: ".planning/phases/193.1-template-first-authoring/193.1-UAT.md"
      provides: "the four manual rows, the SC#2 artifact, and the 167-C copy ruling"
      contains: "SC#2"
  key_links:
    - from: "the operator's running backend"
      to: "POST /workflows/generate"
      via: "a live call carrying template_placeholders"
      pattern: "template_placeholders"
---

<objective>
Prove SC#2 on a REAL `/generate` call with a REAL template's real placeholder keys, **before** the
phase builds the UI that will depend on the answer — and settle one copy question that a measurement
has just invalidated.

Purpose (D-23): the 101.1 precedent — run `7fa36d2a` failed because a model that had never seen the
placeholders invented its own key names. Whether a template-aware draft actually *names* the fields
is the model's judgement and cannot be settled by reading. RESEARCH Open Question 2 records that if
the model returns a `render_template` phase whose steps still name nothing, **the reconcile's design
assumptions change** — so this must be known before Plan 09 is written, not after.

⚠ This plan needs NO code from this phase. `POST /workflows/generate` has accepted
`template_placeholders` since Phase 103; the eight real names of `pm-weekly-status-report.docx` were
parsed during quick task `260814-q5r`. That is why it sits in Wave 1 rather than at the gate.

Output: `193.1-UAT.md`, carrying the raw definition and two operator rulings.
</objective>

<worktree_protocol>
⚠ **THIS PLAN'S INSTRUMENT IS THE OPERATOR'S RUNNING DEV BACKEND, WHICH RUNS AGAINST THE PRIMARY
TREE** — exactly as `193-11` did. Do NOT dispatch it into a worktree, and do not restart or
reconfigure the operator's backend. If a worktree is used anyway, `bash scripts/bootstrap-worktree.sh
"$(pwd)"` first and assert `git rev-parse HEAD` equals the dispatched base SHA; but every command
below is issued against the primary tree's running services.

The operator starts and owns the backend. Do not start it.
</worktree_protocol>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/193.1-template-first-authoring/193.1-CONTEXT.md
@.planning/phases/193.1-template-first-authoring/193.1-RESEARCH.md
@.planning/phases/193.1-template-first-authoring/193.1-VALIDATION.md
@.planning/sketches/167-the-template-arrives-late/BUILD-CONTRACT.generated.md
</context>

<interfaces>
<!-- Measured at HEAD 568afc7a and against the live local Supabase. -->

**The eight real placeholder names**, parsed from `pm-weekly-status-report.docx`'s real
`TemplateAttachSection` render during `260814-q5r` and re-emitted by sketch 165's build contract —
**never re-typed by hand**:
`accomplishments, milestones, overall_rag_status, planned_next, project_name, reporting_period,
risks_blockers, summary`.

**The model resolution (D-23, measured at plan time):** `resolve_authoring_model` needs NO settings
row — with `harness_authoring_model` unset it falls through to the registry default
`claude-opus-4-8` (provider `anthropic`, `forced_emission: True`,
`backend/app/services/workflow_authoring.py:126-129`), and `ANTHROPIC_API_KEY` is present in
`backend/.env`. RESEARCH left "is a working authoring model configured at all?" as BLOCKING; it is
settled. Re-derive with `resolve_authoring_model(SimpleNamespace())`.

**The cross-provider roster ruling — ALREADY DECIDED in `193.1-VALIDATION.md`; carry it, do not
re-open it.** `POST /workflows/generate` has NO per-request `model`/`provider` override
(`GenerateRequest`, `workflows.py:1539-1547`), so the recipe's cheap per-request scoreboard method
does not apply and driving eight providers would mean mutating a global setting eight times — which
the recipe explicitly avoids. **Drive SC#2 on the configured authoring model and RECORD which model
and provider served it.** Roster coverage of `harness_authoring_model` is a separate scoreboard.

**⚠ Test-data warning:** three of the operator's own workflows bind **SEED-prefixed** templates and
correctly render *"could not read"*. Do NOT drive any row with them.

**⚠ THE MEASUREMENT THAT INVALIDATES A SKETCH STRING (new at plan time, measured against the live
DB — 223 `workflow_definitions`):** sketch 167's `reconcile.heading` is
*"This draft was written before the template arrived"*, which asserts an ORDER OF EVENTS. Measured,
**the app cannot know it**. `EmitPhaseConfig.emitter` DEFAULTS to `"render_template"`
(`backend/app/models/harness.py:156`), and across the live corpus every `llm_emit` phase carries it
explicitly: `render_template` **78**, `render_docx` **1**, absent **0** — and on the **74**
template-binding definitions the split is `render_template` **73**. So a template-FIRST draft and a
blind draft are indistinguishable by the definition, and the heading would be FALSE on precisely the
drafts this phase creates. Re-derive by reading `definition->phases[*].config.emitter` over
`workflow_definitions` with `_coerce_definition`'s posture (⚠ `definition` is a jsonb STRING scalar
on 194 of 223 rows — raw `definition->'phases'` silently returns zero rows).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Confirm the instrument, and open `193.1-UAT.md` with its four rows</name>
  <files>.planning/phases/193.1-template-first-authoring/193.1-UAT.md</files>
  <read_first>
    - .planning/phases/193.1-template-first-authoring/193.1-VALIDATION.md § Manual-Only Verifications and § Cross-provider roster ruling
    - .planning/phases/193-authoring-doors-template-placement/193-UAT.md (the shipped row format, including how a ⛔ row records its reason)
    - backend/app/services/workflow_authoring.py lines 110-135 and 250-275 (`resolve_authoring_model` and its `no_authoring_model` refusal)
  </read_first>
  <action>
    Re-derive the model resolution rather than trusting this plan's own claim: call
    `resolve_authoring_model` with an empty settings object and print the resolved model id and its
    provider. Record both values verbatim in the SUMMARY. If it refuses with `no_authoring_model`,
    STOP and report — SC#2 has no instrument and the phase's headline claim is unprovable.

    Create `193.1-UAT.md` with the four rows VALIDATION.md declares, each with an explicit verdict
    column that starts `⬜ pending`:
    U1 — SC#2, a real `/generate` against a real template (this plan, Task 2).
    U2 — G-4, the two-templates collision: the operator reads the flag-ON describe screen and says,
    **unprompted**, which control does what. Silence or a wrong answer is a FAIL.
    U3 — G-4, the disabled CTA reads *"not ready"*, not *"broken"*: attach a large template and press
    the CTA immediately; the footing line must say why.
    U4 — the describe-column scroll at a real viewport, read via Chrome MCP `evaluate_script` DOM
    geometry (⚠ `take_screenshot` times out on this setup). CONTEXT measured `scrollHeight 761` vs
    `clientHeight 614` at the 660 px stage; check whether the switch-to-govern strip falls below the
    fold.

    Carry VALIDATION.md's roster ruling into the file verbatim, so a later reader can see the axis was
    DECIDED rather than skipped. Carry the SEED-prefixed test-data warning too.

    Write the file's own rule at the top: a row may be recorded ⛔ with its reason and its blocking
    issue id, but **never silently omitted**. A scoreboard listing only what passed is not a
    scoreboard.
  </action>
  <acceptance_criteria>
    - `cd backend && venv/Scripts/python.exe -c "from types import SimpleNamespace; from app.services.workflow_authoring import resolve_authoring_model; m=resolve_authoring_model(SimpleNamespace()); print(m)"` exits 0 and prints a model id (record it).
    - `193.1-UAT.md` exists with exactly four rows U1-U4, each carrying an instruction and a verdict cell.
    - The file contains the roster ruling and the SEED-prefixed test-data warning.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -c "from types import SimpleNamespace; from app.services.workflow_authoring import resolve_authoring_model; print(resolve_authoring_model(SimpleNamespace()))"</automated>
  </verify>
  <done>The instrument is confirmed to exist and the scoreboard is open with four rows.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Drive the real `/generate` with the eight real keys, and record the raw definition</name>
  <files>.planning/phases/193.1-template-first-authoring/193.1-UAT.md</files>
  <read_first>
    - backend/app/api/workflows.py lines 1539-1586 (`GenerateRequest` and the `/generate` route — the body shape and the fields it accepts)
    - .planning/phases/193.1-template-first-authoring/193.1-RESEARCH.md § B (what the field does to the prompt, and why the `render_template` assertion comes FIRST)
  </read_first>
  <what-built>
    Nothing in code. This checkpoint drives the SHIPPED `/generate` route with a field the frontend
    has never sent (`SEED-157`), to answer the one question that cannot be settled by reading:
    does a template-aware draft actually come back aimed at the template?
  </what-built>
  <how-to-verify>
    1. Confirm the backend is running (the operator starts it; do not start it).
    2. Obtain a bearer token for your own user from the running frontend session.
    3. Issue TWO calls to `POST /workflows/generate`, identical except for one key, so the difference
       is attributable:
       - **Call A (control)** — body: a `describe` string asking for a weekly project-status report
         built from the knowledge base, and NOTHING else.
       - **Call B (the phase's claim)** — the SAME `describe`, plus `template_placeholders` set to
         the eight names listed in `<interfaces>`, copied from there and not re-typed.
    4. Paste BOTH raw JSON responses into `193.1-UAT.md` under row U1, in full. **A verdict without
       the artifact is not evidence.**
    5. Record which model id and which provider served them (from Task 1's resolution, plus any
       provider field the response or the backend log carries).
  </how-to-verify>
  <action>
    Once the operator returns the two responses, assert against them IN THIS ORDER and record each
    result separately — the first is binary and mechanical, the second is the model's judgement and
    must not be allowed to stand in for it:

    (a) **Call B contains an `llm_emit` phase whose `config.emitter` is `"render_template"`.**
        This is the DELIVERABLE RULE's branch flipping. PASS/FAIL on its own line.
    (b) **Call A does NOT** — i.e. the control ends in a plain-text deliverable. If Call A ALSO
        carries a `render_template` emit phase, say so plainly: the branch is not the discriminator
        RESEARCH §B traced, and record it as a finding rather than smoothing it.
    (c) **The overlap** between Call B's phase slugs / phase names / prompts and the eight keys.
        Record the count and the actual overlapping tokens. RESEARCH measured that across all 74
        live template-binding definitions the slug↔placeholder overlap is **ZERO** (real slugs are
        verbs — `retrieve` ×70, `emit` ×69 — real placeholders are nouns), so any overlap at all is
        the new information.
    (d) Whether Call B's definition declares a non-empty `inputs[]`, and with which keys. Only 3 of
        223 live definitions do; this is the second of the reconcile's two real bucket sources.

    Then write ONE explicit consequence line for Plan 09: given (c) and (d), does the three-bucket
    name check have any non-degenerate input on a template-aware draft, or does it still resolve to
    `produced 0 · run-input 0 · named-nowhere ALL`? Plan 09's fixture and copy are built on that
    answer.
  </action>
  <acceptance_criteria>
    - `193.1-UAT.md` row U1 carries BOTH raw JSON responses in full, the model id and the provider.
    - Four separate recorded results (a)-(d), each PASS/FAIL/OBSERVED with its number.
    - One explicit consequence sentence addressed to Plan 09.
    - The verdict cell for U1 is `✅`, `❌` or `⛔` with a reason — never blank, never removed.
  </acceptance_criteria>
  <resume-signal>Paste both raw JSON responses (or type "blocked: &lt;reason&gt;" to record U1 as ⛔).</resume-signal>
  <verify>
    <human-check>The operator confirms the two responses in `193.1-UAT.md` are the ones their backend returned.</human-check>
  </verify>
  <done>SC#2 has a dated artifact and a recorded verdict, and Plan 09 has the measurement it needs.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Rule on the two 167-C strings a measurement has just invalidated</name>
  <files>.planning/phases/193.1-template-first-authoring/193.1-UAT.md</files>
  <read_first>
    - .planning/sketches/167-the-template-arrives-late/BUILD-CONTRACT.generated.md (the six proposed strings and the rule behind each)
    - .planning/phases/193.1-template-first-authoring/193.1-CONTEXT.md D-10, D-12, D-20, D-21 (the two-meanings-one-word precedent and the reword-the-NEW-string ruling)
  </read_first>
  <decision>
    Sketch 167-C's `reconcile.heading` — *"This draft was written before the template arrived"* — and
    `reconcile.action` — *"Reconcile the steps with this template"* — both assert something the app
    cannot know or does not do. Should they be reworded, or shipped as approved?
  </decision>
  <context>
    ⚠ **A G-2 acceptance-bar gap, found at plan time by measurement — the same class as D-21's, and
    stated rather than absorbed.**

    **`reconcile.heading` asserts an ORDER OF EVENTS the definition cannot carry.** Measured over the
    live corpus (223 rows): `EmitPhaseConfig.emitter` DEFAULTS to `"render_template"`
    (`backend/app/models/harness.py:156`), every live `llm_emit` phase names it explicitly
    (`render_template` **78** · `render_docx` **1** · absent **0**), and on the **74**
    template-binding definitions it is `render_template` **73**. There is therefore **no field that
    distinguishes a draft written before the template from one written to it**. Under D-19 this phase
    starts producing template-FIRST drafts — on which the heading would be flatly false. That is the
    class of dishonesty D-10 calls a red line, one screen earlier than `SEED-159`.

    **`reconcile.action` names an act variant C does not perform.** 167-B (ask the model to reconcile)
    was rejected on SIZE, and no seed was planted. In C the check is a pure client-side name
    comparison with no model call and no write (D-12), so "Reconcile the steps with this template"
    promises a reconciliation that never happens.

    **Why rewording is cheap here:** both strings are UNSHIPPED, UNGOVERNED and carry no byte-exact
    pin — the identical argument D-21 used to reword `ctrl.label` rather than reopen the shipped
    `STARTER_DOOR_LINE`. Nothing in the codebase renders either string today.

    **What must survive whichever option is chosen:** `reconcile.disclaim` verbatim (D-10 calls it the
    single most important string on the surface), three buckets never two, and an empty bucket
    rendering no claim.
  </context>
  <options>
    <option id="reword-both">
      <name>Reword both — the D-21 precedent</name>
      <pros>The surface stops claiming what it cannot compute. Cheapest possible change: two unshipped strings with no pin. Consistent with D-21, decided four hours earlier on the same phase for the same reason.</pros>
      <cons>Departs from an approved sketch variant, so the G-2 acceptance bar for these two strings becomes this ruling rather than the mockup. The replacement wording is new and unsketched.</cons>
    </option>
    <option id="ship-as-approved">
      <name>Ship both as approved, and accept the claim</name>
      <pros>The mockup is the acceptance bar and stays untouched; no new copy to review.</pros>
      <cons>The heading is FALSE on every template-first draft — i.e. on exactly the drafts this phase exists to create. A control labelled "Reconcile" that reconciles nothing trains the author to distrust the panel, which is the failure D-10 spent its whole reasoning avoiding.</cons>
    </option>
    <option id="reword-heading-only">
      <name>Reword the heading; keep `reconcile.action` as the disclosure control</name>
      <pros>Removes the false claim while keeping the sketch's explicit-act shape (D-12): the author presses to see the check, nothing is computed or written until they do.</pros>
      <cons>"Reconcile" still overstates a pure name comparison, though less severely than the heading overstates the order of events.</cons>
    </option>
  </options>
  <action>
    Record the ruling in `193.1-UAT.md` under a `## 167-C copy ruling` heading, with the measurement
    above beside it so a later reader sees what forced the question. If a rewording is chosen, record
    the EXACT replacement strings — Plan 06 puts them in `templateFirstVocabulary.ts` verbatim and
    Plan 09 renders them; neither plan may invent copy.

    ⚠ Whichever option is chosen, note the residual that stays open either way and route it to row U2:
    165's build contract makes `ctrl.button` **deliberately** the shipped `TEMPLATE_ATTACH_LABEL`,
    *"Attach a template"* — character for character, so two doors onto one act do not get two names.
    That string contains the word *template* and will render on the Builder's describe screen roughly
    two inches from the shipped `STARTER_DOOR_LINE` (*"Not sure where to start? Start from a
    template."*). D-21 removed the collision from the LABEL; it does not remove it from the BUTTON,
    and row U2 is the instrument for what is left.
  </action>
  <resume-signal>Select: reword-both, ship-as-approved, or reword-heading-only — and supply the exact replacement string(s) if rewording.</resume-signal>
  <verify>
    <human-check>The operator's ruling and any exact replacement strings are recorded verbatim in `193.1-UAT.md`.</human-check>
  </verify>
  <done>Plan 09 has settled copy, and the button-level residual is routed to a UAT row rather than left to be discovered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator's bearer token → this plan's record | A live authenticated call is driven and its response recorded into a planning artifact. |
| document-derived names → LLM prompt | The eight placeholder names are supplied to a real provider call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193.1-04-01 | Information disclosure | the recorded artifact | mitigate | Record the request bodies and the returned definitions ONLY. The bearer token, the `ANTHROPIC_API_KEY` and any `Authorization` header must never appear in `193.1-UAT.md` — redact before pasting. |
| T-193.1-04-02 | Tampering | the operator's environment | mitigate | The call carries a per-request body only. **No global setting is mutated** — that is precisely why VALIDATION.md's roster ruling declines the eight-provider sweep. Do not set `harness_authoring_model`. |
| T-193.1-04-03 | Repudiation | a verdict without evidence | mitigate | Both raw responses are pasted in full; a row may be ⛔ with a reason but never blank and never removed. |
| T-193.1-04-04 | Tampering | prompt injection via placeholder names | **accept (bounded)** | The eight names are from a known-good document. The general case is PRE-EXISTING since Phase 103 and bounded by `_check_grounding_fidelity` (`workflow_authoring.py:203-230`) plus `extra="forbid"`; Plan 03 pins it. |
| T-193.1-04-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed.** |
</threat_model>

<verification>
- `193.1-UAT.md` exists with four rows and a recorded verdict on each (⬜ only where a later plan owns it).
- Row U1 carries two raw JSON responses, the model id and the provider.
- The 167-C ruling is recorded with its measurement.
- No secret appears anywhere in the artifact.
</verification>

<success_criteria>
SC#2's real call has been driven and its artifact recorded, before any UI depends on the answer;
Plan 09 knows whether the three-bucket check has non-degenerate input; and the two 167-C strings a
measurement invalidated have an operator ruling instead of a silent ship.
</success_criteria>

<output>
Create `.planning/phases/193.1-template-first-authoring/193.1-04-SUMMARY.md` when done.
</output>
