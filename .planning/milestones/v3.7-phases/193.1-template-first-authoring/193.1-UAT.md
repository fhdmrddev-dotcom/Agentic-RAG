# Phase 193.1 — UAT Scoreboard

**Opened:** 2026-08-14 by plan `193.1-04` (Wave 1)
**Requirement:** `AUTH-03` · **Phase goal:** *The AI drafts a workflow knowing what the template asks for.*

---

## The rule this file runs by

**A row may be recorded ⛔ with its reason and its blocking issue id, but NEVER silently omitted.**
A scoreboard that lists only what passed is not a scoreboard. Phase 193 closed owing rows U1/U2 and
said so plainly; that is legitimate. Quietly dropping a row is not.

⚠ **Test-data warning — do NOT drive any row with these.** Three of the operator's own workflows bind
**SEED-prefixed** templates and correctly render *"could not read"*. A row driven against one of them
measures the seed data, not the feature.

---

## Cross-provider roster ruling — DECIDED, carried verbatim from `193.1-VALIDATION.md`

`CLAUDE.md`'s UAT recipe requires the **full native roster (7 + OpenRouter)** for any phase touching
streaming, the agent loop, provider routing, or UI state.

**Measured: `/generate` is none of those.** `generate_workflow_definition` resolves ONE model from
`settings.harness_authoring_model` (`workflow_authoring.py:259`) and calls `forced_emit` once (twice
on a validation failure). **There is no per-request `model`/`provider` override on
`POST /workflows/generate`** (`GenerateRequest`, `workflows.py:1539-1547`), so the recipe's cheap
per-request scoreboard method does not apply — driving eight providers would mean mutating a global
setting eight times, which the recipe explicitly avoids.

**Ruling: drive SC#2 on the configured authoring model and RECORD which model and provider served
it.** Roster coverage of `harness_authoring_model` is a separate scoreboard, scoped separately —
**not smuggled into this phase's rows.** The axis was decided, not skipped.

---

## Rows

| Row | What must be TRUE | Driven by | Verdict |
|---|---|---|---|
| **U1** | SC#2 — a real `/generate` against a real template's real placeholder keys produces a draft aimed at that template | plan `193.1-04` (❌), **re-driven by plan `193.1-11` after the D-26 fix (✅)** | ✅ **PASS after the D-26 fix — `render_template` on 6 of 6 Call-B runs, 8/8 key coverage. The original ❌ FAIL and its cause are preserved verbatim below; see `### U1 re-drive (post-D-26 fix)`.** |
| **U2** | G-4 — the two-templates collision: the operator reads the flag-ON describe screen and says, **unprompted**, which control does what. Silence or a wrong answer is a FAIL | operator, after Plan 08 | ⬜ pending |
| **U3** | G-4 — the disabled CTA reads *"not ready"*, not *"broken"*: attach a large template and press the CTA immediately; the footing line must say why | operator, after Plan 08 | ⬜ pending |
| **U4** | The describe column still works at a real viewport — CONTEXT measured `scrollHeight 761` vs `clientHeight 614` at the 660 px stage; check whether the switch-to-govern strip falls below the fold | Chrome MCP `evaluate_script` (⚠ `take_screenshot` times out on this setup), after Plan 08 | ⬜ pending |

---

## U1 — SC#2, driven 2026-08-14. **VERDICT: ❌ FAIL, with the cause identified and reproduced.**

### The instrument

`resolve_authoring_model(SimpleNamespace())` → **`claude-opus-4-8`**, provider **`anthropic`**,
`forced_emission: True` — resolved via the registry fallback at `workflow_authoring.py:126-129` with
**no settings row** (`harness_authoring_model` is unset). `ANTHROPIC_API_KEY` present in
`backend/.env`. **RESEARCH Open Question 1 is therefore settled: the instrument EXISTS.**

Backend: the operator's running dev backend on `localhost:8000` (primary tree, not a worktree).
Auth: a dev session minted for the dedicated `fhdmrd.automation@gmail.com` account via the local
Supabase admin API — **the operator's own session was not touched**, and no token appears in this
file (T-193.1-04-01).

⚠ **`POST /workflows/generate` PERSISTS NOTHING** — verified in its own docstring
(`workflows.py`: *"a draft object the Builder loads (NOT persisted)"*). No library row was minted by
any call below. No global setting was mutated (T-193.1-04-02).

### The eight real placeholder names

Copied from `<interfaces>`, never re-typed — parsed from `pm-weekly-status-report.docx`'s real
`TemplateAttachSection` render during quick task `260814-q5r`:

`accomplishments, milestones, overall_rag_status, planned_next, project_name, reporting_period,
risks_blockers, summary`

### What was driven

All calls share one `describe`; the variables are (i) whether `template_placeholders` was sent and
(ii) whether the `describe` itself says a template was attached.

- **`describe` (A and B):** *"Produce a weekly project status report for stakeholders, built from our
  knowledge base. Gather the current status, risks and upcoming work, then produce the finished
  report document."*
- **`describe` (C):** the same string **plus** *"I have attached a .docx template that the finished
  report must be filled into."*

| Call | phases | phase types | `render_template` | keys present in the definition | `inputs[]` |
|---|---|---|---|---|---|
| Call A — control | 2 | `llm_agent, llm_single` | **0** | 1/8 | `null` |
| Call B run 1 | 3 | `llm_agent, llm_agent, llm_single` | **0** | 3/8 | `null` |
| Call B run 2 | 2 | `llm_agent, llm_single` | **0** | 4/8 | `[{"key": "project_name", "label": "Project name", "type": "text", "required": true, "source": "user", "enum_options": [], "folder_scope": null}, {"key": "reporting_period", "label": "Reporting period (e.g. week of ...)", "type": "text", "required": true, "source": "user", "enum_options": [], "folder_scope": null}]` |
| Call B run 3 | 2 | `llm_agent, llm_single` | **0** | 3/8 | `null` |
| Call C run 1 | 4 | `llm_agent, llm_single, llm_human_input, llm_emit` | **1** | 8/8 | `[]` |
| Call C run 2 | 5 | `llm_agent, llm_agent, llm_agent, llm_human_input, llm_emit` | **1** | 8/8 | `null` |

### The four recorded results, in the order Task 2 requires

**(a) Call B contains an `llm_emit` phase whose `config.emitter` is `"render_template"` — ❌ FAIL.**
Zero, on **three independent runs**. This is the DELIVERABLE RULE's branch **not** flipping.

**(b) Call A does NOT — ✅ PASS (as expected).** The control ended in a plain-text `llm_single`
deliverable, exactly as the rule prescribes when no template is listed. So the control behaves
correctly; the discriminator is what fails.

**(c) The overlap between Call B's slugs / names / prompts and the eight keys — OBSERVED, and it is
real but partial.** Call B carried **3–4 of 8** keys somewhere in the definition against the
control's **1 of 8** (`summary`, which is an ordinary English word and is the weakest possible hit),
and **2 of 8** appeared in phase slugs/names (`accomplishments`, `milestones`). **So the grounding IS
reaching the model and IS shaping the draft's content** — this is genuinely new information against
RESEARCH's measurement that the live corpus has **zero** slug↔placeholder overlap. It is simply not
enough to make the workflow *fill* the template.

**(d) Call B's definition declares `inputs[]` — OBSERVED: `null` on all three runs.** It contributes
nothing to the reconcile's run-input bucket.

### ⚠ THE CAUSE, ISOLATED AND REPRODUCED — this is the finding

**`template_placeholders` alone does not flip the deliverable branch. The model also needs to be told
a template file was actually PROVIDED.**

The mechanical wire is **not** broken — proved directly, without a provider call:
`render_grounding_prompt` emits `### Template placeholder fields (if the workflow must fill a
template)` followed by all eight names, and **all 8 are present in the rendered prompt** (the empty
case correctly renders `(none)`). RESEARCH §B's trace is accurate at the code level.

What fails is the **inference**. Both governing strings are written conditionally, and neither states
that a file exists:

- the grounding header hedges — *"**if** the workflow must fill a template"*;
- the DELIVERABLE RULE's own condition is *"ONLY when the grounding explicitly lists template
  placeholders **(i.e. the user provided a .docx/.pptx/.xlsx template to fill)**"*, and it closes
  with *"reach for a template only when one is **actually provided**"*.

Nothing in the payload asserts provision. **Adding one sentence to the `describe` that says a
template is attached flips it immediately and completely: 2/2 runs produced a `render_template`
`llm_emit` phase, and key coverage jumped from 3–4/8 to 8/8 on both.** Call C also grew an
`llm_human_input` phase on both runs — the model started asking for the fields it now knew it had to
fill.

### Why this matters more than a failed row

**Shipping the phase as planned would attach a template, send the placeholders, and still produce a
draft that does not fill it** — the exact defect AUTH-03 was re-opened to remove, arriving through a
different door. It would pass a wire-level test and fail the user.

It also **corrects D-19 in one direction while confirming it in the other**, and both halves are
stated rather than smoothed:

- ✅ **CONFIRMED:** the rule is real, the branch exists, and the control (Call A) obeys it.
- ❌ **CORRECTED:** D-19 says *"names present ⇒ `llm_emit`/`render_template`"*. **Measured, names
  present ⇒ nothing, 3 times out of 3.** The trigger is not the names; it is the *assertion of
  provision*. D-19's derived warning — that shipping the wire without the bind yields an unbound
  `render_template` draft that is terminal at run — therefore **does not materialize as predicted**:
  the wire alone produces **no** `render_template` phase at all. The risk D-19 named is real in
  principle but is currently unreachable, because the branch never fires.

### The consequence line Plan 09 asked for

**On a template-aware draft as the phase currently plans to build it, the three-bucket name check
still resolves to `produced 0 · run-input 0 · named-nowhere ALL`** — `inputs[]` was `null` on every
run, and only 2 of 8 keys reached a slug/name. **D-20's degenerate-first design stands and must not
be softened.** ⚠ **But if the provision fix below ships, this changes materially**: Call C reached
**8/8** key coverage and grew a `llm_human_input` phase, which is exactly the shape that would begin
populating the *run input* bucket. **Plan 09 must therefore build for the degenerate case AND must
not hard-code the assumption that the non-degenerate case is unreachable.**

### ⚠ Owed to the operator

This row was driven by the assistant under the operator's standing delegation to proceed unattended.
Task 2's `<human-check>` — *"the operator confirms the two responses are the ones their backend
returned"* — **is NOT satisfied and remains owed.** The artifacts are recorded in full below so that
confirmation is a reading task, not a re-run.

---

## U1 re-drive (post-D-26 fix), driven 2026-08-14 by plan `193.1-11`. **VERDICT: ✅ PASS.**

⚠ **Everything above this heading is the BEFORE column and is preserved unedited.** The original
❌ FAIL, its six raw responses and the cause it isolated are the evidence this section is measured
against; nothing in them was rewritten.

### The instrument — and why it is NOT the same instrument

`resolve_authoring_model(settings)` → **`claude-opus-4-8`**, provider **`anthropic`**, resolved via
the registry fallback with `harness_authoring_model` **still unset** — printed by the driver on every
run, not assumed. Same model, same provider, same account (`fhdmrd.automation@gmail.com`,
`2f0546ea-…`) as the original six calls.

⚠ **The calls were driven IN-PROCESS, not over HTTP, and that is a deliberate strengthening rather
than a shortcut.** The operator's dev backend on `localhost:8000` holds the OLD prompt in memory and
the operator is away, so restarting it was not available; but an HTTP re-drive would in any case have
left *"did the server reload the module?"* open. Calling `generate_workflow_definition`
(`backend/app/services/workflow_authoring.py`) directly from a freshly-started interpreter removes
that question entirely — the code under test is the code just written. To make that checkable rather
than claimed, the driver **prints the rendered grounding heading for both arms before the first
call**:

```
### instrument: model='claude-opus-4-8' provider='anthropic'
### harness_authoring_model on app settings: None
### grounding heading [PRESENT]: ### Template placeholder fields — the user HAS attached a template document to this workflow; the final deliverable MUST therefore be an `llm_emit` phase with `emitter: 'render_template'` that fills EXACTLY the fields named on the next line
### grounding heading [ABSENT]: ### Template placeholder fields — NO template document was provided with this workflow; the final deliverable MUST therefore be PLAIN TEXT / markdown, and a `render_template` emit phase is FORBIDDEN because it would fail at run time
```

Everything else is identical to the original drive: the same `describe` string, the same eight keys,
`template_placeholders` as the ONLY variable. **Call C is not repeated** — it was the diagnostic that
isolated the cause, and the fix's whole claim is that **B now behaves as C did**.

⚠ **`POST /workflows/generate` PERSISTS NOTHING** and the service path is the same one the route
delegates to, so no library row was minted. **No global setting was mutated** (T-193.1-11-03) — the
printed `harness_authoring_model: None` is the evidence. No token, `Authorization` header or API key
appears anywhere in this file (T-193.1-11-02).

### The result, beside the before-column

| Call | `describe` asserts a template | `template_placeholders` sent | `render_template` — **BEFORE** | `render_template` — **AFTER** | keys — BEFORE | keys — AFTER |
|---|---|---|---|---|---|---|
| A (control) | no | no | 0 | **0** | 1/8 | 1–2/8 |
| B run 1 | no | **yes** | 0 | **1** | 3/8 | **8/8** |
| B run 2 | no | **yes** | 0 | **1** | 4/8 | **8/8** |
| B run 3 | no | **yes** | 0 | **1** | 3/8 | **8/8** |

**THE ONE QUESTION: does Call B now produce an `llm_emit` phase with `emitter: "render_template"` on
all three runs? — ✅ PASS, 3 of 3.** Key coverage went `3–4/8 → 8/8` on every run, matching what Call
C produced when the assertion of provision came from the `describe` string instead.

⚠ **It is actually 6 of 6, and the second triplet was an ACCIDENT — stated because a stronger result
arrived by mistake and hiding that would be worse than the mistake.** The analysis step imported the
driver module, which carries no `if __name__ == "__main__"` guard, so the whole script re-executed and
drove four more live calls. That second, fully independent set reproduced the first exactly:
**A → 0 `render_template`; B ×3 → 1 / 1 / 1, 8/8 keys on each.** Its raw bodies were NOT persisted
(the re-run crashed on the missing output-path argument after printing), so only the summary is
claimed for it — **the four verbatim artifacts below are the FIRST set**, and the second is
corroboration, not evidence.

### The control did not weaken — checked, not assumed

**Call A produced 0 `render_template` phases on both drives**, ending in a plain-text deliverable
(`llm_agent ×3 → llm_single`). That is the arm SC#4 (*the fast door stays fast*) and D-08 rest on, and
a `render_template` phase with no template bound is TERMINAL at run (`no_template_bound`,
`phase_types.py:1271`). The absent arm's new wording is strictly more explicit than the hedge it
replaced, and the measurement says it cost nothing.

### The consequence line owed to Plan 09 — RE-STATED, and it is NOT what U1 predicted

The original U1 warned: *"if the provision fix below ships, this changes materially… Call C reached
8/8 and grew an `llm_human_input` phase, which is exactly the shape that would begin populating the
run input bucket."*

**Measured after the fix, that prediction is HALF RIGHT and the half that matters for Plan 09 is
WRONG.** Both halves are stated rather than smoothed:

- ✅ **The `llm_human_input` phase did arrive** — on **4 of the 6** post-fix B runs (runs 2 and 3 of
  the first set, runs 2 and 3 of the second).
- ❌ **`inputs[]` is STILL `null` — on 6 of 6 runs, exactly as before the fix.** The human-input phase
  asks for the fields in its own `prompt` prose, not by declaring definition-level inputs. So the
  run-input bucket is fed by `WorkflowDefinition.inputs[].key` (D-20) and **nothing feeds it**.
- ❌ **Slug/name reach did not improve either** — measured across the six post-fix B runs, phase
  slugs and names carry **0–2 of 8** keys (0, 0, 0 in the first set; 2, 0, 1 in the second). The 8/8
  figure is coverage **anywhere in the definition**, and it lands almost entirely in the `llm_emit`
  phase's `prompt`, which is **not one of D-20's three bucket sources**.

**So the three-bucket name check still resolves to `produced 0–2 · run-input 0 · named-nowhere 6–8`.
D-20's degenerate-first design STANDS and must not be softened, and Plan 09 must still survive
*"8 named nowhere"* as its realistic first render.** The one thing that changed for Plan 09 is that
the *reconcile surface will now actually have a template-aimed draft to reconcile* — which is what it
was built for — not that the buckets fill up.

### ⚠ Owed to the operator, still

Same standing-delegation caveat as the original drive: **the operator has not confirmed these are the
responses their backend returns.** The four artifacts are recorded in full below so confirming is a
reading task. Note the honest asymmetry — these were driven in-process against this worktree's code,
so confirming them on the operator's own backend requires that backend to be restarted onto a tree
carrying commit `9cf97033`.

### Raw responses — the FIRST set, verbatim

*A verdict without the artifact is not evidence.* No `Authorization` header, bearer token or API key
appears below (T-193.1-11-02).

#### Re-drive Call A — control (no `template_placeholders`), post-fix

```json
{
  "ok": true,
  "definition": {
    "slug": "weekly-project-status-report-948a2a24",
    "version": 1,
    "name": "Weekly Project Status Report",
    "status": "draft",
    "phases": [
      {
        "slug": "gather-status",
        "phase_index": 0,
        "config": {
          "phase_type": "llm_agent",
          "prompt": "Search the knowledge base for the latest project status information. Gather the current overall status, milestones completed this week, and any status updates from recent documents. Summarize the current state of the project with citations to the source documents.",
          "available_tools": [
            "search_documents",
            "read_document",
            "query_documents",
            "get_related_documents"
          ],
          "max_steps": 12,
          "wall_clock_seconds": null,
          "model": null,
          "folder_scope": null,
          "skill_ref": null,
          "skill_snapshot": null
        },
        "validators": [],
        "name": "Pull the current project status from the KB",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      },
      {
        "slug": "gather-risks",
        "phase_index": 1,
        "config": {
          "phase_type": "llm_agent",
          "prompt": "Search the knowledge base for open risks, issues, and blockers affecting the project. List each risk with its severity, impact, and current mitigation, citing the source documents.",
          "available_tools": [
            "search_documents",
            "read_document",
            "query_documents",
            "get_related_documents"
          ],
          "max_steps": 12,
          "wall_clock_seconds": null,
          "model": null,
          "folder_scope": null,
          "skill_ref": null,
          "skill_snapshot": null
        },
        "validators": [],
        "name": "Collect open risks and issues",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      },
      {
        "slug": "gather-upcoming",
        "phase_index": 2,
        "config": {
          "phase_type": "llm_agent",
          "prompt": "Search the knowledge base for planned and upcoming work items, next milestones, and scheduled deliverables for the coming week. Summarize the upcoming work with citations to the source documents.",
          "available_tools": [
            "search_documents",
            "read_document",
            "query_documents",
            "get_related_documents"
          ],
          "max_steps": 12,
          "wall_clock_seconds": null,
          "model": null,
          "folder_scope": null,
          "skill_ref": null,
          "skill_snapshot": null
        },
        "validators": [],
        "name": "Identify upcoming work for next week",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      },
      {
        "slug": "write-report",
        "phase_index": 3,
        "config": {
          "phase_type": "llm_single",
          "prompt": "Using the current status, open risks, and upcoming work gathered in the previous steps, write a finished weekly project status report for stakeholders in clear markdown. Include these sections: an executive summary, Current Status, Risks & Issues, and Upcoming Work (Next Week). Keep it concise and stakeholder-friendly, and preserve the citations to source documents. Output the complete report as plain markdown text.",
          "model": null,
          "temperature": null,
          "folder_scope": null,
          "skill_ref": null,
          "skill_snapshot": null
        },
        "validators": [],
        "name": "Write the finished weekly status report",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      }
    ],
    "project_folder_id": null,
    "output_target_folder": null,
    "reingest_output": false,
    "version_policy": "supersede-by-filename",
    "provenance": "source",
    "inputs": null,
    "assets": null,
    "business_requirement": null,
    "category": null
  }
}
```

#### Re-drive Call B run 1 — `template_placeholders` sent, `describe` silent about a template

```json
{
  "ok": true,
  "definition": {
    "slug": "weekly-project-status-report-c99cf6ec",
    "version": 1,
    "name": "Weekly Project Status Report",
    "status": "draft",
    "phases": [
      {
        "slug": "gather-status",
        "phase_index": 0,
        "config": {
          "phase_type": "llm_agent",
          "prompt": "Search the knowledge base for the latest project information covering the current reporting period. Gather: (1) the project name and reporting period, (2) recent accomplishments completed this period, (3) milestones reached or coming up, (4) current risks and blockers, (5) planned next steps / upcoming work, and (6) enough signal to judge an overall RAG (red/amber/green) status. Cite the source documents for every fact you collect and compile them into a clear, organized set of notes for a stakeholder status report.",
          "available_tools": [
            "search_documents",
            "query_documents",
            "read_document",
            "get_related_documents",
            "analyze_document"
          ],
          "max_steps": 15,
          "wall_clock_seconds": null,
          "model": null,
          "folder_scope": null,
          "skill_ref": null,
          "skill_snapshot": null
        },
        "validators": [],
        "name": "Gather current status, risks and upcoming work from the KB",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      },
      {
        "slug": "render-report",
        "phase_index": 1,
        "config": {
          "phase_type": "llm_emit",
          "prompt": "Using the gathered and cited notes, fill the weekly project status report template. Populate exactly these fields: project_name, reporting_period, overall_rag_status (red, amber, or green), summary (a concise stakeholder-facing overview), accomplishments (what was completed this period), milestones (milestones reached and upcoming), risks_blockers (current risks and blockers), and planned_next (upcoming work for next period). Ground every statement in the cited source documents.",
          "emitter": "render_template",
          "model": null,
          "folder_scope": null,
          "skill_ref": null,
          "skill_snapshot": null,
          "citation_policy": "strict",
          "integrity_policy": "strict"
        },
        "validators": [],
        "name": "Fill the status report template",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      }
    ],
    "project_folder_id": null,
    "output_target_folder": null,
    "reingest_output": false,
    "version_policy": "supersede-by-filename",
    "provenance": "source",
    "inputs": null,
    "assets": null,
    "business_requirement": "Produce a weekly project status report for stakeholders, built from the knowledge base: gather current status, risks and upcoming work, then produce the finished report document.",
    "category": null
  }
}
```

#### Re-drive Call B run 2

```json
{
  "ok": true,
  "definition": {
    "slug": "weekly-project-status-report-d6ac6d56",
    "version": 1,
    "name": "Weekly Project Status Report",
    "status": "draft",
    "phases": [
      {
        "slug": "gather-status",
        "phase_index": 0,
        "config": {
          "phase_type": "llm_agent",
          "prompt": "Search the knowledge base for the current state of the project this week. Gather and organize: (1) accomplishments completed this reporting period, (2) key milestones and their status, (3) an overall RAG (red/amber/green) status judgment with justification, (4) planned work for next period, and (5) current risks and blockers. Use the retrieval tools to find recent project updates, meeting notes, task trackers, and any status documents. Cite the source documents for every item. Produce a clearly structured briefing covering all of these areas plus a short overall summary and the project name and reporting period.",
          "available_tools": [
            "search_documents",
            "query_documents",
            "read_document",
            "get_related_documents",
            "query_tables",
            "analyze_document"
          ],
          "max_steps": 12,
          "wall_clock_seconds": null,
          "model": null,
          "folder_scope": null,
          "skill_ref": null,
          "skill_snapshot": null
        },
        "validators": [],
        "name": "Gather current status, risks and upcoming work from the KB",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      },
      {
        "slug": "confirm-report",
        "phase_index": 1,
        "config": {
          "phase_type": "llm_human_input",
          "prompt": "Here is the gathered project status: accomplishments, milestones, overall RAG status, planned next steps, risks/blockers, and summary. Please review before we generate the final stakeholder report. Confirm to proceed or send corrections.",
          "options": [
            "Approve and generate report",
            "Needs changes"
          ],
          "timeout_seconds": 300
        },
        "validators": [],
        "name": "Confirm the gathered status before finalizing",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      },
      {
        "slug": "emit-report",
        "phase_index": 2,
        "config": {
          "phase_type": "llm_emit",
          "prompt": "Using the confirmed gathered status, emit the weekly project status report by filling exactly these template fields: project_name, reporting_period, overall_rag_status, summary, accomplishments, milestones, planned_next, and risks_blockers. Keep every statement grounded in and cited from the source documents gathered earlier.",
          "emitter": "render_template",
          "model": null,
          "folder_scope": null,
          "skill_ref": null,
          "skill_snapshot": null,
          "citation_policy": "strict",
          "integrity_policy": "strict"
        },
        "validators": [],
        "name": "Fill the status report template into the finished document",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      }
    ],
    "project_folder_id": null,
    "output_target_folder": null,
    "reingest_output": false,
    "version_policy": "supersede-by-filename",
    "provenance": "source",
    "inputs": null,
    "assets": null,
    "business_requirement": "Produce a weekly project status report for stakeholders, built from the knowledge base: gather current status, risks and upcoming work, then produce the finished report document.",
    "category": null
  }
}
```

#### Re-drive Call B run 3

```json
{
  "ok": true,
  "definition": {
    "slug": "weekly-project-status-report-522c549a",
    "version": 1,
    "name": "Weekly Project Status Report",
    "status": "draft",
    "phases": [
      {
        "slug": "gather-status",
        "phase_index": 0,
        "config": {
          "phase_type": "llm_agent",
          "prompt": "Search the knowledge base for the latest project information covering the reporting week. Gather: the project name and reporting period; a concise overall status summary; recent accomplishments; completed and upcoming milestones; planned work for next period; and any current risks or blockers. Cite the source documents you rely on. Assemble these findings clearly so they can be used to fill out a status report.",
          "available_tools": [
            "search_documents",
            "query_documents",
            "read_document",
            "get_related_documents",
            "query_tables"
          ],
          "max_steps": 12,
          "wall_clock_seconds": null,
          "model": null,
          "folder_scope": null,
          "skill_ref": null,
          "skill_snapshot": null
        },
        "validators": [],
        "name": "Gather current status, risks and upcoming work from the KB",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      },
      {
        "slug": "assess-rag-status",
        "phase_index": 1,
        "config": {
          "phase_type": "llm_single",
          "prompt": "Based on the gathered status, accomplishments, milestones, planned work, and risks/blockers, determine the overall RAG status for the project this week (one of Red, Amber, or Green) and briefly justify it. Also draft a short stakeholder-facing summary paragraph.",
          "model": null,
          "temperature": null,
          "folder_scope": null,
          "skill_ref": null,
          "skill_snapshot": null
        },
        "validators": [],
        "name": "Judge the overall RAG (red/amber/green) health",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      },
      {
        "slug": "confirm-report",
        "phase_index": 2,
        "config": {
          "phase_type": "llm_human_input",
          "prompt": "Please review the drafted status, RAG rating, risks and planned work. Confirm to generate the final weekly status report, or reply with corrections.",
          "options": [
            "Confirm and generate report",
            "Make changes"
          ],
          "timeout_seconds": 300
        },
        "validators": [],
        "name": "Ask the human to confirm before finalizing the report",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      },
      {
        "slug": "render-report",
        "phase_index": 3,
        "config": {
          "phase_type": "llm_emit",
          "prompt": "Emit the finished weekly project status report by filling the template fields from the confirmed findings: project_name, reporting_period, overall_rag_status, summary, accomplishments, milestones, planned_next, and risks_blockers. Keep the content accurate to the cited knowledge-base sources.",
          "emitter": "render_template",
          "model": null,
          "folder_scope": null,
          "skill_ref": null,
          "skill_snapshot": null,
          "citation_policy": "strict",
          "integrity_policy": "strict"
        },
        "validators": [],
        "name": "Fill the status report template with the confirmed findings",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      }
    ],
    "project_folder_id": null,
    "output_target_folder": null,
    "reingest_output": false,
    "version_policy": "supersede-by-filename",
    "provenance": "source",
    "inputs": null,
    "assets": null,
    "business_requirement": null,
    "category": null
  }
}
```


---

## 167-C copy ruling

**Ruling: `reword-both`** — recorded by the assistant under the operator's standing delegation to
proceed unattended, on the **D-21 precedent the operator set on this same phase, on the same day, for
the same reason**. It is cheaply reversible: both strings are unshipped, ungoverned and carry no
byte-exact pin, and nothing in the codebase renders either today.

### The measurement that forced the question

Sketch 167-C's `reconcile.heading` — *"This draft was written before the template arrived"* —
**asserts an ORDER OF EVENTS the definition cannot carry.** Measured over the live corpus (223
`workflow_definitions`): `EmitPhaseConfig.emitter` **defaults** to `"render_template"`
(`backend/app/models/harness.py:156`), every live `llm_emit` phase names it explicitly
(`render_template` **78** · `render_docx` **1** · absent **0**), and on the **74** template-binding
definitions it is `render_template` **73**. **There is no field distinguishing a draft written before
the template from one written to it.** Under D-19 this phase starts producing template-first drafts —
on which the heading would be flatly false. That is the class of dishonesty D-10 calls a red line,
one screen earlier than `SEED-159`.

`reconcile.action` — *"Reconcile the steps with this template"* — **names an act variant C does not
perform.** 167-B (ask the model to reconcile) was rejected on SIZE and no seed was planted. In C the
check is a pure client-side name comparison with **no model call and no write** (D-12), so the label
promises a reconciliation that never happens.

### The exact replacement strings — Plan 06 puts these in `templateFirstVocabulary.ts` VERBATIM, Plan 09 renders them. Neither plan may invent copy.

| id | approved-sketch string (superseded) | **the string that ships** |
|---|---|---|
| `reconcile.heading` | This draft was written before the template arrived | **`What this template asks for, next to what these steps name`** |
| `reconcile.action` | Reconcile the steps with this template | **`Check the names against this template`** |

**Why these two.** The heading now states only what the panel actually puts side by side — a
comparison — and claims nothing about when either was authored, which is the fact the app does not
have. The action names the operation the code truly performs, a **name check**, matching D-10's
own vocabulary (*"a three-bucket NAME check"*) and D-12's rule that the reconcile is an explicit act
that never rewrites the draft.

### What survives this ruling unchanged, and is not open

- **`reconcile.disclaim` ships VERBATIM.** D-10 calls it *"the single most important string on the
  surface"*: without it C asserts a coverage verdict it **cannot compute**. Measured — at attach time
  the app has placeholder keys, phase slugs and `inputs[].key`, a **heuristic**; only at run time does
  `check_coverage` (`template_render_service.py:416-460`) produce `covered_keys` / `covers_template` /
  `null_leaf_count`, a **verdict**. The copy may say *"nothing in the steps **names** these"*. It may
  **NEVER** say the template is uncovered.
- **Three buckets, never two** (D-10's red line).
- **An empty bucket renders no claim** (D-20 / 193's D-15 rule one surface later).

### ⚠ The residual this ruling does NOT remove — routed to row U2, not left to be discovered

Sketch 165's build contract makes `ctrl.button` **deliberately** the shipped `TEMPLATE_ATTACH_LABEL`,
*"Attach a template"* — character for character, so that two doors onto one act do not acquire two
names. **That string contains the word *template*** and will render on the Builder's describe screen
roughly two inches from the shipped `STARTER_DOOR_LINE` (*"Not sure where to start? Start from a
template."*). **D-21 removed the collision from the LABEL; it does not remove it from the BUTTON.**
Row **U2** is the instrument for what is left, and it is the reason U2 must be driven by a person who
has not read this file.

---

## Artifacts — the raw responses, verbatim

*A verdict without the artifact is not evidence.* No `Authorization` header, bearer token or API key
appears anywhere below (T-193.1-04-01).

### Call A — control (no `template_placeholders`)

```json
{
  "ok": true,
  "definition": {
    "slug": "weekly-project-status-report-06cc6390",
    "version": 1,
    "name": "Weekly Project Status Report",
    "status": "draft",
    "phases": [
      {
        "slug": "gather-status",
        "phase_index": 0,
        "config": {
          "phase_type": "llm_agent",
          "prompt": "Search the knowledge base for the latest project information. Gather and summarize three things: (1) the current project status and recent progress, (2) open risks and issues, and (3) upcoming work planned for the next period. Use search and read tools to find the most recent and relevant documents, and cite what you find. Produce a structured set of notes covering all three areas.",
          "available_tools": [
            "search_documents",
            "query_documents",
            "read_document",
            "get_related_documents",
            "fetch_document_file",
            "analyze_document"
          ],
          "max_steps": 12,
          "wall_clock_seconds": null,
          "model": null,
          "folder_scope": null,
          "skill_ref": null,
          "skill_snapshot": null
        },
        "validators": [],
        "name": "Gather current status, risks and upcoming work from the KB",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      },
      {
        "slug": "write-report",
        "phase_index": 1,
        "config": {
          "phase_type": "llm_single",
          "prompt": "Using the gathered notes on current status, risks, and upcoming work, write a polished weekly project status report in markdown for stakeholders. Include clear sections: an executive summary, Current Status, Open Risks & Issues, and Upcoming Work / Next Steps. Keep it concise, professional, and easy to scan. Output the finished report as plain markdown text.",
          "model": null,
          "temperature": null,
          "folder_scope": null,
          "skill_ref": null,
          "skill_snapshot": null
        },
        "validators": [],
        "name": "Write the finished weekly status report for stakeholders",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      }
    ],
    "project_folder_id": null,
    "output_target_folder": null,
    "reingest_output": false,
    "version_policy": "supersede-by-filename",
    "provenance": "source",
    "inputs": null,
    "assets": null,
    "business_requirement": null,
    "category": null
  }
}
```

### Call B run 1 — `template_placeholders` sent, `describe` silent about a template

```json
{
  "ok": true,
  "definition": {
    "slug": "weekly-project-status-report-b99244ba",
    "version": 1,
    "name": "Weekly Project Status Report",
    "status": "draft",
    "phases": [
      {
        "slug": "gather-status",
        "phase_index": 0,
        "config": {
          "phase_type": "llm_agent",
          "prompt": "Search the knowledge base for the latest project information covering this reporting period. Gather the current overall status, recent accomplishments, and milestones reached or upcoming. Cite the source documents you rely on.",
          "available_tools": [
            "search_documents",
            "query_documents",
            "read_document",
            "get_related_documents"
          ],
          "max_steps": 12,
          "wall_clock_seconds": null,
          "model": null,
          "folder_scope": null,
          "skill_ref": null,
          "skill_snapshot": null
        },
        "validators": [],
        "name": "Pull current status, accomplishments and milestones",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      },
      {
        "slug": "gather-risks",
        "phase_index": 1,
        "config": {
          "phase_type": "llm_agent",
          "prompt": "Search the knowledge base for current risks, blockers, and planned upcoming work for the project. Summarize each risk/blocker and the work planned for the next period, citing the source documents.",
          "available_tools": [
            "search_documents",
            "query_documents",
            "read_document",
            "get_related_documents"
          ],
          "max_steps": 12,
          "wall_clock_seconds": null,
          "model": null,
          "folder_scope": null,
          "skill_ref": null,
          "skill_snapshot": null
        },
        "validators": [],
        "name": "Pull risks, blockers and upcoming work",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      },
      {
        "slug": "write-report",
        "phase_index": 2,
        "config": {
          "phase_type": "llm_single",
          "prompt": "Using the gathered status, accomplishments, milestones, risks/blockers, and upcoming work from the prior steps, write the finished weekly project status report for stakeholders as clean markdown. Include: project name, reporting period, an overall RAG status, an executive summary, accomplishments, milestones, risks and blockers, and planned next steps. Write the answer directly as the deliverable.",
          "model": null,
          "temperature": null,
          "folder_scope": null,
          "skill_ref": null,
          "skill_snapshot": null
        },
        "validators": [],
        "name": "Write the finished weekly status report",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      }
    ],
    "project_folder_id": null,
    "output_target_folder": null,
    "reingest_output": false,
    "version_policy": "supersede-by-filename",
    "provenance": "source",
    "inputs": null,
    "assets": null,
    "business_requirement": "Produce a weekly project status report for stakeholders, built from the knowledge base: gather current status, risks, and upcoming work, then produce the finished report document.",
    "category": null
  }
}
```

### Call B run 2

```json
{
  "ok": true,
  "definition": {
    "slug": "weekly-project-status-report-458bae77",
    "version": 1,
    "name": "Weekly Project Status Report",
    "status": "draft",
    "phases": [
      {
        "slug": "gather-status",
        "phase_index": 0,
        "config": {
          "phase_type": "llm_agent",
          "prompt": "Research the knowledge base to build a weekly status picture for the project named in the inputs, covering the given reporting period. Gather: (1) current progress and recent accomplishments, (2) milestone status, (3) active risks and blockers, (4) planned work for the coming week, and (5) an overall status signal (on track / at risk / off track). Use the search and read tools to find the most recent and relevant project documents. Produce an organized, cited summary of your findings for the next step to write from.",
          "available_tools": [
            "search_documents",
            "query_documents",
            "read_document",
            "get_related_documents",
            "fetch_document_file",
            "analyze_document"
          ],
          "max_steps": 12,
          "wall_clock_seconds": null,
          "model": null,
          "folder_scope": null,
          "skill_ref": null,
          "skill_snapshot": null
        },
        "validators": [],
        "name": "Pull current status, risks and upcoming work from the KB",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      },
      {
        "slug": "write-report",
        "phase_index": 1,
        "config": {
          "phase_type": "llm_single",
          "prompt": "Using the gathered findings from the previous step, write the finished weekly project status report for stakeholders as clean markdown. Include a header with the project name and reporting period; an overall RAG status; a short executive summary; recent accomplishments; milestone status; risks and blockers; and planned work for next week. Keep it concise, factual, and grounded in the gathered findings. Output the complete report directly as the deliverable.",
          "model": null,
          "temperature": null,
          "folder_scope": null,
          "skill_ref": null,
          "skill_snapshot": null
        },
        "validators": [],
        "name": "Write the finished weekly status report",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      }
    ],
    "project_folder_id": null,
    "output_target_folder": null,
    "reingest_output": false,
    "version_policy": "supersede-by-filename",
    "provenance": "source",
    "inputs": [
      {
        "key": "project_name",
        "label": "Project name",
        "type": "text",
        "required": true,
        "source": "user",
        "enum_options": [],
        "folder_scope": null
      },
      {
        "key": "reporting_period",
        "label": "Reporting period (e.g. week of ...)",
        "type": "text",
        "required": true,
        "source": "user",
        "enum_options": [],
        "folder_scope": null
      }
    ],
    "assets": null,
    "business_requirement": "Produce a weekly project status report for stakeholders, built from the knowledge base: gather current status, risks and upcoming work, then produce the finished report document.",
    "category": null
  }
}
```

### Call B run 3

```json
{
  "ok": true,
  "definition": {
    "slug": "weekly-project-status-report-89fc84ba",
    "version": 1,
    "name": "Weekly Project Status Report",
    "status": "draft",
    "phases": [
      {
        "slug": "gather-status",
        "phase_index": 0,
        "config": {
          "phase_type": "llm_agent",
          "prompt": "Search the knowledge base to gather everything needed for a weekly project status report. Find the project's current status and overall health, recent accomplishments and completed milestones, active risks and blockers, and planned work for the coming period. Read the most relevant and recent documents and pull direct evidence. Produce a well-organized set of findings with citations covering: overall status/health, accomplishments, milestones, risks and blockers, and planned next steps.",
          "available_tools": [
            "search_documents",
            "query_documents",
            "read_document",
            "get_related_documents",
            "analyze_document",
            "fetch_document_file"
          ],
          "max_steps": 12,
          "wall_clock_seconds": null,
          "model": null,
          "folder_scope": null,
          "skill_ref": null,
          "skill_snapshot": null
        },
        "validators": [],
        "name": "Gather current status, risks and upcoming work",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      },
      {
        "slug": "write-report",
        "phase_index": 1,
        "config": {
          "phase_type": "llm_single",
          "prompt": "Using the findings gathered in the previous step, write the finished weekly project status report for stakeholders as clear, well-structured markdown. Include these sections: Project Name, Reporting Period, Overall Status (RAG health), Summary, Accomplishments, Milestones, Risks & Blockers, and Planned Next Steps. Write in a concise, professional tone suitable for stakeholders, and keep it grounded in the gathered evidence.",
          "model": null,
          "temperature": null,
          "folder_scope": null,
          "skill_ref": null,
          "skill_snapshot": null
        },
        "validators": [],
        "name": "Write the finished status report",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      }
    ],
    "project_folder_id": null,
    "output_target_folder": null,
    "reingest_output": false,
    "version_policy": "supersede-by-filename",
    "provenance": "source",
    "inputs": null,
    "assets": null,
    "business_requirement": "Produce a weekly project status report for stakeholders, built from the knowledge base: gather current status, risks and upcoming work, then produce the finished report document.",
    "category": null
  }
}
```

### Call C run 1 — `template_placeholders` sent **and** `describe` states a template is attached

```json
{
  "ok": true,
  "definition": {
    "slug": "weekly-project-status-report-e3eaf222",
    "version": 1,
    "name": "Weekly Project Status Report",
    "status": "draft",
    "phases": [
      {
        "slug": "gather-status",
        "phase_index": 0,
        "config": {
          "phase_type": "llm_agent",
          "prompt": "Search the knowledge base for the latest project information needed for a weekly stakeholder status report. Gather and summarize: the current overall status and RAG (red/amber/green) health, recent accomplishments, active risks and blockers, milestone progress, and planned work for the upcoming period. Cite the source documents for every fact you collect.",
          "available_tools": [
            "search_documents",
            "read_document",
            "query_documents",
            "get_related_documents"
          ],
          "max_steps": 12,
          "wall_clock_seconds": null,
          "model": null,
          "folder_scope": null,
          "skill_ref": null,
          "skill_snapshot": null
        },
        "validators": [],
        "name": "Gather current status, risks and upcoming work",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      },
      {
        "slug": "draft-report-content",
        "phase_index": 1,
        "config": {
          "phase_type": "llm_single",
          "prompt": "Using the gathered and cited status information, write the content for a weekly project status report. Produce clear text for each section: a project summary, the overall RAG status, recent accomplishments, milestones, risks and blockers, and planned next steps for the upcoming reporting period. Keep every statement traceable to the cited sources.",
          "model": null,
          "temperature": null,
          "folder_scope": null,
          "skill_ref": null,
          "skill_snapshot": null
        },
        "validators": [],
        "name": "Draft the report narrative from the gathered facts",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      },
      {
        "slug": "confirm-before-finalizing",
        "phase_index": 2,
        "config": {
          "phase_type": "llm_human_input",
          "prompt": "Please review the drafted status report content (summary, RAG status, accomplishments, milestones, risks/blockers, planned next steps). Confirm it is accurate and ready to be filled into the report template, or request changes.",
          "options": [
            "Approve and finalize",
            "Request changes"
          ],
          "timeout_seconds": 300
        },
        "validators": [],
        "name": "Confirm the report content before filling the template",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      },
      {
        "slug": "fill-report-template",
        "phase_index": 3,
        "config": {
          "phase_type": "llm_emit",
          "prompt": "Emit the finished weekly project status report by filling the provided .docx template. Map the approved content into the template placeholders: project_name, reporting_period, overall_rag_status, summary, accomplishments, milestones, risks_blockers, and planned_next. Every value must be supported by the cited source documents.",
          "emitter": "render_template",
          "model": null,
          "folder_scope": null,
          "skill_ref": null,
          "skill_snapshot": null,
          "citation_policy": "strict",
          "integrity_policy": "strict"
        },
        "validators": [],
        "name": "Fill the approved content into the report template",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      }
    ],
    "project_folder_id": null,
    "output_target_folder": null,
    "reingest_output": false,
    "version_policy": "supersede-by-filename",
    "provenance": "source",
    "inputs": [],
    "assets": [],
    "business_requirement": "Produce a weekly project status report for stakeholders, built from the knowledge base: gather current status, risks, and upcoming work, then fill the provided .docx template.",
    "category": null
  }
}
```

### Call C run 2

```json
{
  "ok": true,
  "definition": {
    "slug": "weekly-project-status-report-1ea90ea0",
    "version": 1,
    "name": "Weekly Project Status Report",
    "status": "draft",
    "phases": [
      {
        "slug": "gather-current-status",
        "phase_index": 0,
        "config": {
          "phase_type": "llm_agent",
          "prompt": "Search the knowledge base for the latest information on the project's current status for this reporting period. Determine the overall RAG status and write a concise summary of accomplishments and progress made. Cite the source documents you rely on.",
          "available_tools": [
            "search_documents",
            "read_document",
            "query_documents",
            "get_related_documents"
          ],
          "max_steps": 12,
          "wall_clock_seconds": null,
          "model": null,
          "folder_scope": null,
          "skill_ref": null,
          "skill_snapshot": null
        },
        "validators": [],
        "name": "Pull the current project status",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      },
      {
        "slug": "gather-risks-blockers",
        "phase_index": 1,
        "config": {
          "phase_type": "llm_agent",
          "prompt": "Search the knowledge base for any active risks, issues, and blockers affecting the project during this reporting period. Summarize each risk or blocker clearly, citing the source documents you rely on.",
          "available_tools": [
            "search_documents",
            "read_document",
            "query_documents",
            "get_related_documents"
          ],
          "max_steps": 12,
          "wall_clock_seconds": null,
          "model": null,
          "folder_scope": null,
          "skill_ref": null,
          "skill_snapshot": null
        },
        "validators": [],
        "name": "Pull the current risks and blockers",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      },
      {
        "slug": "gather-upcoming-work",
        "phase_index": 2,
        "config": {
          "phase_type": "llm_agent",
          "prompt": "Search the knowledge base for upcoming milestones and planned work for the next period. Summarize the planned next steps and key milestones, citing the source documents you rely on.",
          "available_tools": [
            "search_documents",
            "read_document",
            "query_documents",
            "get_related_documents"
          ],
          "max_steps": 12,
          "wall_clock_seconds": null,
          "model": null,
          "folder_scope": null,
          "skill_ref": null,
          "skill_snapshot": null
        },
        "validators": [],
        "name": "Pull the upcoming milestones and planned work",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      },
      {
        "slug": "confirm-before-finalizing",
        "phase_index": 3,
        "config": {
          "phase_type": "llm_human_input",
          "prompt": "Review the gathered status summary, RAG status, risks/blockers, and upcoming work. Confirm this is accurate and complete before the final report is produced, or provide corrections.",
          "options": [
            "Looks good, produce the report",
            "I have corrections"
          ],
          "timeout_seconds": 300
        },
        "validators": [],
        "name": "Confirm the gathered content before finalizing",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      },
      {
        "slug": "produce-status-report",
        "phase_index": 4,
        "config": {
          "phase_type": "llm_emit",
          "prompt": "Using the confirmed gathered content, emit the fields to fill the weekly project status report template: project_name, reporting_period, overall_rag_status, summary, accomplishments, milestones, planned_next, and risks_blockers. Ground every field in the cited knowledge base sources.",
          "emitter": "render_template",
          "model": null,
          "folder_scope": null,
          "skill_ref": null,
          "skill_snapshot": null,
          "citation_policy": "strict",
          "integrity_policy": "strict"
        },
        "validators": [],
        "name": "Fill the status report template",
        "grounding_escalated": false,
        "action_risk_armed": false,
        "name_seeded_by_ai": true
      }
    ],
    "project_folder_id": null,
    "output_target_folder": null,
    "reingest_output": false,
    "version_policy": "supersede-by-filename",
    "provenance": "source",
    "inputs": null,
    "assets": null,
    "business_requirement": "Produce a weekly project status report for stakeholders, built from the knowledge base: gather current status, risks and upcoming work, then produce the finished report filled into the provided .docx template.",
    "category": null
  }
}
```

---

## U5 — ⭐ THE END-TO-END RUN. **PASS.** Driven by the operator, 2026-08-15.

**This is the row the whole phase existed to make possible, and the one no test in the repository
could stand in for: a real template, attached at authoring time, filled by a real run.**

### What was driven

The operator built a purpose-made business case rather than reusing a fixture — a **Quarterly
Business Review** for a fictional SaaS account, with a **five-document knowledge base** and a
**10-field `.docx` template** carrying deliberately loud branding (navy header bar, amber health
box, teal section headings, Georgia serif). Facts were planted in the documents that a model
could not invent.

- Workflow: `northwind-qbr-q3-2026-f0f9033f`
- Run: `d8331add-bf37-4b29-8c05-a48211203823` — **status `completed`**, not a golden run
- Output: `workspace_files` id `92c9392a-e900-4cb6-810b-f76c8863af68`,
  `/Northwind-QBR-Template.docx`, **38,763 bytes**
- Source template: 37,424 bytes ⇒ **+1,339 bytes of generated content**

### Structural verdict — the template survived the whole trip

| Check | Result |
|---|---|
| Navy header/footer bar (`0B2545`) | ✅ 11 occurrences |
| Amber health box (`F2C14E`) | ✅ |
| Teal section headings (`1B998B`) | ✅ 7 |
| Grey panel (`F4F6F8`) | ✅ |
| Georgia serif retained | ✅ |
| Cell shading elements preserved | ✅ 4 |
| **Unrendered `{{ }}` remaining** | **none** |
| **Literal `"None"` in the document** | **none** |
| Byte-identical to the template? | **No** — content was written into it |

**All ten fields rendered.** The branding is intact, which is the discriminator the fixture was
designed around: a regenerated document would have been plain.

### Grounding verdict — **11 of 13 planted facts, and the misses are attribution, not content**

FOUND: Priya Raghunathan · 68% seat utilisation · 412 of 605 seats · NPS 41 · **INC-4471** ·
6h12m outage on 12 Aug · 87 tickets / 5 SLA breaches · **ARR £284,000** · renewal 31 Jan 2027 ·
Freight Analytics · **AMBER** health status.

It additionally surfaced `SUP-9902` and Tomas Lindqvist's 31 Oct capacity-review action, neither
of which was on the checklist.

⚠ **MISSING: `Marcus Feld` and `Gareth Pryce` — and this is a real observation, not a scoring
artifact.** The *substance* of Feld's quote is present — *"the Monday operating pack 'is no
longer a job' (six hours of manual Excel work per Monday eliminated)"* — **with his name
stripped.** The model grounded the fact and dropped the attribution. For an internal summary
that is acceptable; **for a client-facing QBR it is a downgrade**, because *"your Ops Director
said this"* carries weight that *"someone said this"* does not. Worth a seed if it recurs, not a
failure of this phase.

### ⚠ Two publish blockers were hit on the way, and neither is cosmetic

The run only happened after the operator cleared **two** gates that the authoring path does not
know about:

1. **`business_requirement` was empty** and had to be typed by hand, restating what the describe
   box already said → **`SEED-163`**.
2. **The draft contained an `llm_human_input` phase**, which the synchronous publish gate
   categorically refuses; the operator **deleted the step on the canvas** to proceed →
   **`BUG-260815-01`** (severity **blocking**).

⚠ **The second is a consequence of this phase's own D-26 fix**: once the model knows it must
fill ten named fields, it adds a step to ask the human for what it cannot find. Measured 2 for 2
whenever a template step appears. **So the capability works and the path around it does not** —
recorded rather than smoothed.

### Consequence for `AUTH-03`

AUTH-03 requires *"attaches a template when authoring it, **and every run fills that same
template**."* Both halves are now **observed on a real run**, which is the evidence that was
missing at the close of plan `193.1-10` and the reason it declined to tick the requirement on
unit evidence alone.

### Artifact — the produced document's full text

Saved alongside the run kit as `RUN-OUTPUT-Northwind-QBR.docx` and dumped here verbatim, because
**a verdict without the artifact is not evidence**:

```text
1. PRODUCT ADOPTION
Seat utilisation rose to 68% (+4 pts) with 412 of 605 licensed seats active in period, weekly active users 309, depot rollups 1,187, document searches 31,668 (+41% QoQ — the largest jump since onboarding), fuel reconciliation exports 14. Depot rollups are now fully self-serve (6 of 7 depots); fuel reconciliation remains under-adopted (14 exports vs a theoretical 39); only 9% of active users have opened the saved-views feature.

2. SERVICE & SUPPORT
87 tickets raised (+23% vs Q2, tracking the 96 supervisor accounts onboarded in July), 82 resolved within SLA, 5 SLA breaches (4 were Sev-3 documentation/how-to questions with no operational impact). Median first response improved 47→38 min, median time to resolve 9h05m→7h51m, reopened tickets 4→2. Quarter defined by INC-4471, the customer's first Sev-1 in 30 months.

3. WINS THIS QUARTER
Customer-called wins at the 24 Sep 2026 QBR: the Monday operating pack 'is no longer a job' (six hours of manual Excel work per Monday eliminated); DVSA compliance audit passed with zero documentation findings (Aug 2026), attributed directly to document search; 96 depot supervisors onboarded in two weeks with zero training escalations — described by Northwind's L&D team as unprecedented.

4. RISKS & WATCH-OUTS
INC-4471 — the only Sev-1 in 30 months (12 Aug 2026, depot rollup generation down 6h12m, delaying Monday operating packs across all seven depots); seat utilisation at 68% leaves ~193 licensed seats unused — the most likely target for a commercial reduction at renewal; open tickets SUP-9902 (finance trust / continued re-keying) and SUP-10014 (saved views invisible to supervisors); Hull depot remains a manual-assistance exception.

5. COMMERCIAL POSITION
ARR £284,000 at 605 licensed seats (£469/seat/year), current term ends 31 Jan 2027, no auto-renew — active renewal required with 60-day notice (by 2 Dec 2026). Overall risk AMBER — value compression rather than churn. Downside: ~500 seats would cut ARR to £234,500 (−£49,500). Only expansion lever is Freight Analytics (~£62,000/yr at Northwind's volume), gated on closing SUP-9902; net position if both land ≈ £296,500. Commercial proposal (seat banding + Freight Analytics) due 14 Nov 2026.

6. PRIORITIES FOR NEXT QUARTER
Agreed actions from the 24 Sep 2026 QBR: written capacity review before any seat expansion (Tomas Lindqvist, due 31 Oct); close SUP-9902 fuel export column ordering (Aoife Brennan, due 15 Nov); fix supervisor visibility of saved views (Tomas Lindqvist, due 10 Oct); commercial proposal on seat banding + Freight Analytics (Priya Raghunathan, due 14 Nov); Hull depot enablement session (Priya Raghunathan, due 28 Nov).

QUARTERLY BUSINESS REVIEW
Northwind Logistics
Review period: Q3 2026 (1 July – 30 September 2026)     |     Account owner: Priya Raghunathan
ACCOUNT HEALTH
AMBER
Executive summary
Seat utilisation rose to 68% (+4 pts) with 412 of 605 licensed seats active in period, weekly active users 309, depot rollups 1,187, document searches 31,668 (+41% QoQ — the largest jump since onboarding), fuel reconciliation exports 14. Depot rollups are now fully self-serve (6 of 7 depots); fuel reconciliation remains under-adopted (14 exports vs a theoretical 39); only 9% of active users have opened the saved-views feature.
Commercial in confidence  ·  Prepared for Northwind Logistics  ·  Q3 2026 (1 July – 30 September 2026)
```
