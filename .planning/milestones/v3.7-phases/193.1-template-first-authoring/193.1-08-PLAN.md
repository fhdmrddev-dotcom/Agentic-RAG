---
phase: 193.1-template-first-authoring
plan: 08
type: execute
wave: 5
depends_on: ["193.1-07"]
files_modified:
  - frontend/src/pages/WorkflowBuilderPage.tsx
  - frontend/src/components/workflows/WorkflowDoorSwitch.tsx
  - frontend/src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx
  - frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx
  - frontend/src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx
  - frontend/src/pages/WorkflowBuilderPage.describe.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "D-24: the row mounts on BOTH pre-draft describe screens — the fast door and the govern door — and every plan touching the splice names its file and asserts which component it landed in"
    - "D-24: the fast door's already-completed read crosses the handoff as props, on the SHIPPED `initialProjectFolderId` precedent, and is SEEDED rather than re-read"
    - "SC#1: a template can be supplied BEFORE describing, on the screen the approved mockup actually rendered"
    - "SC#4 / D-08: with no template supplied the describe screen is byte-identical and the CTA is never disabled"
    - "D-13: `FLAG_OFF_DESCRIBE_MARKUP` and the `/template|starter/i` CTA-region guard both stay green with ZERO edits — the splice is OUTSIDE the pinned group"
    - "D-02: the two baseline re-captures are DECLARED, dated and reasoned, labelled 1 of 1 each — never quietly absorbed"
  artifacts:
    - path: "frontend/src/components/workflows/WorkflowDoorSwitch.tsx"
      provides: "the fast door's mount + the two crossing props into the Builder"
    - path: "frontend/src/pages/WorkflowBuilderPage.tsx"
      provides: "the govern door's mount + the seed intake"
  key_links:
    - from: "frontend/src/components/workflows/WorkflowDoorSwitch.tsx"
      to: "frontend/src/pages/WorkflowBuilderPage.tsx"
      via: "initialTemplateFile + initialTemplateRead, in the 187-26 initialProjectFolderId shape"
      pattern: "initialTemplateFile"
---

<objective>
Mount the row on **both** pre-draft describe screens, and carry the fast door's read across the
handoff.

Purpose (D-24, operator ruling — the finding neither CONTEXT nor RESEARCH could see): **the approved
mockup and the phase's stated target file are two different components.** Sketch 165 rendered
`WorkflowDoorSwitch`'s describe door; CONTEXT's every pointer names the Builder. A fast-door user's
CTA is a HANDOFF that auto-fires `onDraft()` once, so they never touch the Builder's describe
screen — mounting only there means the primary describe path never gets the feature. A govern-door
user calls `/generate` directly — mounting only on the fast door leaves that path producing blind
drafts, which under D-19 are now actively steered AWAY from templates, i.e. broken on purpose.

⚠ **The splice anchor is NOT unique across the codebase.** `<div className="flex flex-col
items-center gap-3">` occurs at `WorkflowDoorSwitch.tsx:300` **AND** `WorkflowBuilderPage.tsx:1579`.
It was unique within the sketch's DUMP, which is all `build.cjs` ever asserted. A plan that splices
"at the CTA group" can land on either surface and both will look right.

Output: two mounts, two crossing props, two declared re-captures.
</objective>

<worktree_protocol>
1. FIRST action: `bash scripts/bootstrap-worktree.sh "$(pwd)"`.
2. THEN assert `git rev-parse HEAD` equals the dispatched base SHA. A mismatch is a STOP.
3. `GSD_VITEST_MAX_WORKERS=2` on EVERY vitest invocation; ≤ 2 concurrent test-running agents.
4. `cd frontend && npx tsc --noEmit -p tsconfig.app.json`, baseline **33**.
</worktree_protocol>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/193.1-template-first-authoring/193.1-CONTEXT.md
@.planning/phases/193.1-template-first-authoring/193.1-PATTERNS.md
@.planning/phases/193.1-template-first-authoring/193.1-06-SUMMARY.md
@.planning/phases/193.1-template-first-authoring/193.1-07-SUMMARY.md
@.planning/sketches/165-the-template-lands-first/BUILD-CONTRACT.generated.md
</context>

<interfaces>
<!-- Extracted at HEAD 568afc7a. Every line number VERIFIED. -->

**THE TWO SCREENS, side by side:**

| | `WorkflowDoorSwitch.tsx:227-380` — **door-describe** | `WorkflowBuilderPage.tsx:1533-1615` — **describeScreen** |
|---|---|---|
| Which door | LOOSE / fast (`DOOR_A_NAME`) | GOVERN (`DOOR_B_NAME`) |
| KB picker | `<DescribeKbPicker>` at **`:299`** | an INLINE `<select data-testid="project-folder-picker">` at **`:1562`**, gated on `folderOptions.length > 0` |
| CTA group | `<div className="flex flex-col items-center gap-3">` at **`:300`** | the SAME class string at **`:1579`** |
| What the CTA does | `onDescribeDraft?.(describe); setHandoffDraft(true); setDoor("govern")` (`:305-315`) — **a handoff, NO network call** | `onClick={onDraft}` (`:1583`) → `generateWorkflow` (`:1267`) — **the ONLY `/generate` caller** |
| Its own `canDraft` | `WorkflowDoorSwitch.tsx:150` — `describe.trim().length > 0` | the extracted hook's (Plan 05/07) |
| Starter picker | **absent** | `{canvasEnabled && <StarterTemplatePicker …/>}` at **`:1596`**, INSIDE the CTA group |

**Sketch 165 rendered the FIRST one** — `build.cjs:6-7` says so, `emit.test.tsx.src:55` imports
`WorkflowDoorSwitch` not the page, and its anchor assertions name `switch-strip`, which exists ONLY
at `WorkflowDoorSwitch.tsx:335`. **The splice is `dom.describe.replace(CTA_GROUP, block + CTA_GROUP)`
(`build.cjs:269`) — the block goes BEFORE the CTA group, never inside it.**

**THE CROSSING IS NOT A NEW MECHANISM.** `WorkflowDoorSwitch.tsx:202-209` already passes `initial`,
`initialDescribe={describe}`, `autoDraft={handoffDraft}` and **`initialProjectFolderId={kbFolderId}`**
into the Builder — added in `187-26` for the IDENTICAL problem: pre-draft state picked on the loose
door that only the Builder can spend. Its comment records that `""` (nobody picked) leaves the
Builder's own initializer at exactly today's value. **That is the shape for the two new props.**
⚠ The Builder auto-fires `onDraft()` once on handoff (`WorkflowBuilderPage.tsx:1303-1315`).

**THE PINS THAT MUST STAY GREEN WITH ZERO EDITS, and why the splice placement is what saves them:**
- `FLAG_OFF_DESCRIBE_MARKUP` — `WorkflowBuilderPage.describe.test.tsx:307-308`. Its
  `describeRegion()` walks UP from `describe-hint` (`:259-261`), so it covers **the CTA group only**.
  A row spliced BEFORE `:1579` is outside it. It is a FLAG-OFF pin, which is how `187-15` added
  `StarterTemplatePicker` inside that region without reddening it — **do not inherit that escape
  hatch; keep the new row outside the region and render it unconditionally.** (RESEARCH Open
  Question 6's recommendation: do NOT gate the row on `canvasEnabled` — the feature is AUTH-03, not a
  canvas feature.)
- ⚠ **A guard neither CONTEXT nor RESEARCH names:** `describe.test.tsx:324` asserts the CTA region
  says NOTHING matching `/template|starter/i`, with its plant at `:351-358`. It is scoped to
  `describeRegion()`, so a row outside the CTA group does not trip it — **but a mount INSIDE the CTA
  group trips TWO independent assertions, not one.**
- `FLAG_OFF_HEADER_MARKUP` — `WorkflowBuilderPage.header.test.tsx:382` (⚠ NOT `:304`, which is an
  ordinary presence assertion — anchor on the identifier, never the number). Orthogonal to this cut.

**THE RE-CAPTURE THIS PLAN OWES, and it is REQUIRED rather than avoidable.**
`WorkflowDoorSwitch.baseline.test.tsx`'s `DESCRIBE_STANDALONE` and `DESCRIBE_INLINE` rows are
whole-`container.innerHTML` captures of the describe door. Mounting a row on that door changes them.
Its own re-capture standard (`:46-49`) says a diff against these strings is a **BEHAVIOUR CHANGE TO
EXPLAIN, never a test to update**. Phase 193 handled the same situation by DECLARING each re-capture
in its own plan with a date and a reason (`193-08` words-only, `193-09` structure-only). Do the same.
The same obligation applies to Plan 01's six pre-draft captures.
⚠ The baseline suite's `GOVERN_*` rows must NOT move — nothing in this plan touches the govern
door's own markup.

**Sketch 165's row order under the KB picker** (from its build contract): the reworded prompt, the
sub-note, the attach button, the types note, then the spec block, then the one footing line — all of
which `DescribeTemplateRow.tsx` already renders (Plan 06). This plan mounts it; it authors no copy.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Mount on the GOVERN door's describe screen — `WorkflowBuilderPage.tsx`, outside the pinned group</name>
  <files>frontend/src/pages/WorkflowBuilderPage.tsx, frontend/src/pages/WorkflowBuilderPage.describe.test.tsx</files>
  <read_first>
    - frontend/src/pages/WorkflowBuilderPage.tsx lines 1533-1615 (`describeScreen`: the KB picker `</label>` at :1577 and the CTA group opening at :1579)
    - frontend/src/pages/WorkflowBuilderPage.describe.test.tsx lines 250-360 (`describeRegion()`, `FLAG_OFF_DESCRIBE_MARKUP` at :307, and the `/template|starter/i` guard at :324 with its plant at :351-358)
    - frontend/src/components/workflows/DescribeTemplateRow.tsx (its props, as Plan 06 shipped it)
  </read_first>
  <behavior>
    - With no file supplied, the describe screen renders the control row and nothing else new; the CTA
      is enabled exactly when it is today.
    - Picking a file issues one read; while it is outstanding the CTA is disabled AND the footing line
      says why (166-B's `loading` value).
    - When the read resolves to `fields`, the CTA re-enables and the spec block lists the names.
    - The row is present with the canvas flag OFF as well as ON — it is not gated on `canvasEnabled`.
  </behavior>
  <action>
    Splice `<DescribeTemplateRow …/>` into `describeScreen` **immediately after the KB picker's
    closing `</label>` (`:1577`) and BEFORE the CTA group `<div>` at `:1579`** — outside the region
    `describeRegion()` covers. Write the comment naming BOTH reasons: `FLAG_OFF_DESCRIBE_MARKUP`
    covers the CTA group only, and `describe.test.tsx:324`'s `/template|starter/i` guard is scoped to
    the same region, so a mount inside it would red two independent assertions.

    Render it **unconditionally** — NOT behind `canvasEnabled`. Write the reason beside it: gating it
    would inherit the flag-off escape hatch that let `StarterTemplatePicker` ship inside the pinned
    region, and AUTH-03 is not a canvas feature; the flag-off Spine is the surface most authors are
    actually on.

    Feed it from the hook: the read state, the held filename, `onPickFile`, `onClear`. The page
    computes nothing about templates — the panel-prop discipline (`PhaseFormPanel.tsx:155-169`),
    applied one screen up.

    Add cases to `WorkflowBuilderPage.describe.test.tsx` covering the four behaviours, and **name the
    file explicitly in each case's title** — every plan touching this splice must assert WHICH
    component it landed in, because the anchor is not unique. Assert at least one case through a
    query that can only match this page (e.g. the inline `project-folder-picker` `<select>` plus the
    absence of `switch-strip`, which exists only on the door switch).

    ⚠ Do NOT edit `FLAG_OFF_DESCRIBE_MARKUP` or the `/template|starter/i` guard. If either reds, the
    splice landed in the wrong place — move the splice, never the pin.
  </action>
  <acceptance_criteria>
    - `grep -n "DescribeTemplateRow" frontend/src/pages/WorkflowBuilderPage.tsx` shows exactly one mount, on a line BEFORE the `flex flex-col items-center gap-3` CTA group.
    - The mount line contains no `canvasEnabled`.
    - `git diff frontend/src/pages/WorkflowBuilderPage.describe.test.tsx` shows ADDED cases only — the `FLAG_OFF_DESCRIBE_MARKUP` literal and the `/template|starter/i` case are unchanged (paste the diff in the SUMMARY).
    - At least one new case asserts it is on the Builder's screen and not the door switch's.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowBuilderPage.describe.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowBuilderPage.describe.test.tsx</automated>
  </verify>
  <done>The govern door's describe screen can supply a template, with both CTA-region pins untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Mount on the FAST door — and carry the finished read across the handoff</name>
  <files>frontend/src/components/workflows/WorkflowDoorSwitch.tsx, frontend/src/pages/WorkflowBuilderPage.tsx, frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx</files>
  <read_first>
    - frontend/src/components/workflows/WorkflowDoorSwitch.tsx lines 95-215 (the props, the local state, `canDraft` at :150, and the Builder mount at :195-215 with `initialProjectFolderId` at :207) and 290-345 (the describe door's KB picker, CTA group and switch strip)
    - frontend/src/pages/WorkflowBuilderPage.tsx lines 540-610 (the props interface and the destructure) and 1303-1315 (the auto-draft one-shot)
    - frontend/src/components/workflows/useTemplateFirstDraft.ts (the exported `Answer` shape and the seed argument, as Plan 07 shipped them)
  </read_first>
  <behavior>
    - The fast door renders the same row, in the same position relative to its KB picker.
    - The fast door's `canDraft` gains the same single `loading` term, so the handoff cannot fire
      mid-read.
    - Pressing the fast door's CTA hands `initialTemplateFile` and `initialTemplateRead` to the
      Builder alongside the existing four props.
    - The Builder SEEDS its read with those values: the state resolves immediately, **zero requests
      are issued**, and the auto-fired `onDraft()` carries `template_placeholders` on the first call.
    - With no file supplied, both props are genuinely ABSENT from the element (the spread-conditional
      idiom this file already uses for `headerLead`/`headerTrail`), and the Builder's behaviour is
      exactly today's.
  </behavior>
  <action>
    Hold the picked `File` in `WorkflowDoorSwitch`'s local state beside `describe` and `kbFolderId`,
    and read it with the `useTemplateRead` hook Plan 07 exported. Mount `DescribeTemplateRow`
    immediately after `<DescribeKbPicker>` (`:299`) and BEFORE the CTA group `<div>` at `:300` — the
    sketch's own splice (`build.cjs:269`: the block goes BEFORE the CTA group).

    Add the `loading` term to this file's `canDraft` (`:150`) — one `&&`, the same shape as the
    Builder's, and for the same D-07/D-08 reason: without a file there is no read and therefore no
    gate.

    Pass `initialTemplateFile` and `initialTemplateRead` into `<WorkflowBuilderPage>` using the
    **spread-conditional** idiom this file already uses (`:213` for `headerLead`/`headerTrail`), so
    with nothing supplied the props are genuinely ABSENT rather than present-and-undefined. Write the
    comment naming `initialProjectFolderId` (187-26) as the precedent: pre-draft state picked on the
    loose door that only the Builder can spend.

    On the Builder side, accept the two props and pass them as the hook's seed. ⚠ **Seed, do not
    re-read.** A re-read would return the state to `loading`, and the auto-draft one-shot fires
    immediately on handoff — which is precisely the blind-draft race D-07 exists to make impossible.
    Assert the zero-request property in a test.

    Do NOT clear the held file in `goBoth` — `kbFolderId` is deliberately not cleared there either
    (`:160-166`: the hand-off is a one-shot and must not re-fire, but a considered pick is not undone
    by looking around). Follow the shipped rule rather than inventing a second one.

    Add cases to `WorkflowDoorSwitch.test.tsx`, each naming this file in its title and asserting the
    door-switch surface specifically (e.g. via `switch-strip`, which exists only here).
  </action>
  <acceptance_criteria>
    - `grep -n "DescribeTemplateRow" frontend/src/components/workflows/WorkflowDoorSwitch.tsx` shows one mount, on a line BEFORE the CTA group `<div>`.
    - `grep -c "initialTemplateFile" frontend/src/components/workflows/WorkflowDoorSwitch.tsx` >= 1 and it appears inside a spread-conditional, not as a bare always-present prop.
    - A test asserts that on handoff with a resolved read, the Builder issues **zero** calls to `readTemplatePlaceholdersFromFile` and the first `generateWorkflow` body carries `template_placeholders`.
    - A test asserts the fast door's CTA is disabled while the read is in flight and enabled with no file held.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/WorkflowDoorSwitch.test.tsx` exits 0.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports **33**.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/WorkflowDoorSwitch.test.tsx</automated>
  </verify>
  <done>Both describe screens carry the row, and the fast path drafts from the template on its very first generate call.</done>
</task>

<task type="auto">
  <name>Task 3: Declare both re-captures — dated, reasoned, labelled — and re-pin</name>
  <files>frontend/src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx, frontend/src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - frontend/src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx lines 1-110 (the docblock, the re-capture standard at :46-49, `CAPTURE_SHA` at :98) and 380-415 (the row table and the comparison)
    - frontend/src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx (as Plans 01 and 07 left it)
    - .planning/phases/193-authoring-doors-template-placement/193-08-SUMMARY.md and 193-09-SUMMARY.md (how Phase 193 declared its two re-captures, "1 of 2" / "2 of 2")
  </read_first>
  <action>
    Re-capture ONLY what the mounts actually changed, and DECLARE each one in its own dated block:

    (a) `WorkflowDoorSwitch.baseline.test.tsx` — the `DESCRIBE_STANDALONE` and `DESCRIBE_INLINE`
    rows. Label it **"re-capture 1 of 1 for Phase 193.1"**, dated, with the reason: the fast door
    gains the AUTH-03 pre-draft template row under its KB picker (D-24). ⚠ **The `GOVERN_*` rows must
    NOT move** — assert that in the SUMMARY by showing the diff touches only the two describe rows.

    (b) `WorkflowBuilderPage.preDraft.baseline.test.tsx` — the six pre-draft captures. Label it
    **"re-capture 1 of 1 for Phase 193.1"**, dated, with the reason: the govern door's describe screen
    gains the same row, spliced before the CTA group. State that Plans 05 and 07 held these captures
    with ZERO edits and that this is the FIRST and only movement — that is what makes the extraction's
    proof and the wire's proof independent of the feature's.

    In both files restate the standard rather than weakening it: a diff against these strings is a
    BEHAVIOUR CHANGE TO EXPLAIN, never a test to update. Do not raise or remove any absence
    assertion to make a capture pass.

    Then re-pin every suite whose count moved — `WorkflowDoorSwitch.test.tsx`,
    `WorkflowDoorSwitch.baseline.test.tsx`, `WorkflowBuilderPage.describe.test.tsx`,
    `WorkflowBuilderPage.preDraft.baseline.test.tsx` — taking each number from the script's own
    `actual` column, and run the full gate.

    Finally, record in the SUMMARY the two pins that did NOT move and were not edited:
    `FLAG_OFF_DESCRIBE_MARKUP` (`describe.test.tsx:307`) and `FLAG_OFF_HEADER_MARKUP`
    (`header.test.tsx:382`), each with a `git diff --numstat` line showing zero changes.
  </action>
  <acceptance_criteria>
    - Each re-capture block carries a date, a reason, and the label "1 of 1".
    - `git diff frontend/src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx` touches only the two `DESCRIBE_*` literals (show it).
    - `git diff --numstat frontend/src/pages/WorkflowBuilderPage.describe.test.tsx` shows no change to the `FLAG_OFF_DESCRIBE_MARKUP` literal or the `/template|starter/i` case; `git diff --numstat frontend/src/pages/WorkflowBuilderPage.header.test.tsx` is EMPTY.
    - No assertion is deleted or weakened in either baseline suite (line counts do not decrease — show `git diff --numstat`).
    - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` exits 0 with `failed 0`.
    - `cd backend && venv/Scripts/python.exe -m pytest tests/unit -q` shows no new failure (wave-merge sampling).
  </acceptance_criteria>
  <verify>
    <automated>GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>Both re-captures are declared rather than absorbed, both untouched pins are proved untouched, and the gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a picked `File` → the door handoff | A browser-held object crosses a component boundary as a prop and is later uploaded. |
| document text → DOM | Placeholder names render on two more surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193.1-08-01 | Tampering | a blind draft on the fast path | mitigate | The fast door's `canDraft` gains the same `loading` term, and the crossing SEEDS the Builder's read so the auto-fired `onDraft()` cannot run against a `loading` state. Asserted by a zero-request test. |
| T-193.1-08-02 | Tampering (XSS) | placeholder names on two more surfaces | mitigate | Both mounts render the SAME `DescribeTemplateRow`, whose plain-text-children property and injection case were proved in Plan 06. No second render path is introduced. |
| T-193.1-08-03 | Tampering | the wrong component receiving the splice | mitigate | The anchor `<div className="flex flex-col items-center gap-3">` occurs in BOTH files. Every new case names its file in its title and asserts a surface-unique node (`switch-strip` for the door, the inline `project-folder-picker` for the page). |
| T-193.1-08-04 | Repudiation | a quietly absorbed re-capture | mitigate | Both re-captures are DECLARED with a date, a reason and a "1 of 1" label; the diffs are pasted; the `GOVERN_*` rows and the two flag-off pins are shown untouched. |
| T-193.1-08-05 | Information disclosure | the held `File` crossing a prop boundary | accept | The object never leaves the browser until the author's own draft is created, and it is uploaded to `POST /workflows/{id}/template` under the user-keyed prefix that route already mints. No new exposure. |
| T-193.1-08-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed.** |
</threat_model>

<verification>
- Both mounts present, each asserted on its own surface.
- Both byte-exact CTA-region/header pins untouched; both baseline re-captures declared.
- Count gate green at cap 2; `tsc -p tsconfig.app.json` reports 33; backend unit suite unmoved.
</verification>

<success_criteria>
A person on either describe screen can supply a template before describing, is told what the draft
will be built from on every reading arm, cannot press the CTA into a blind draft, and — on the fast
path — reaches the Builder with the read already answered.
</success_criteria>

<output>
Create `.planning/phases/193.1-template-first-authoring/193.1-08-SUMMARY.md` when done.
</output>
