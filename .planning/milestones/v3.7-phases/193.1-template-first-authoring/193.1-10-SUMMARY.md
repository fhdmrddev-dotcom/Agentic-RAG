# Plan 193.1-10 — Summary (close-out)

**Plan:** `193.1-10` · **Wave:** 7 · **Requirement:** `AUTH-03`
**Executed:** 2026-08-15, inline by the orchestrator against the **primary tree**.
**Status:** ◐ **Tasks 1 and 2 COMPLETE. Task 3 is OWED — it is the operator's to drive.**

---

## Task 1 — every gate re-run at HEAD

All measured by the orchestrator on the merged tree, **not inherited from any executor's report**.

| Gate | Result |
|---|---|
| `tsc --noEmit -p tsconfig.app.json` | **33** — the baseline, **unmoved across all eleven plans** |
| count gate (`GSD_VITEST_MAX_WORKERS=2`) | **exit 0 · total 3892 · failed 0 · 75/75 pinned** |
| backend `pytest tests/unit` | **62 failed / 2092 passed** — 62 is the recorded **SEED-056 rot baseline**, failures unchanged; passes grown from 2060 by this phase's new cases |
| `scripts/check-deploy-drift.sh` | **PASS** — one-box deploy artifacts in sync (2 pre-existing non-blocking WARNs) |
| **G-7** | **CLEAR** — `plans: 11 total · 0 gap-closure`, exit 0. **Zero gap-closure rounds were opened.** |

**Count-gate arc across the phase:** 3604 (base) → 3620 → 3650 → 3717 → 3811 → 3829 → **3892**;
pinned files 68 → **75**. Every increase is growth; **no per-file decrease at any point.**

⚠ **`GSD_VITEST_MAX_WORKERS=2` IS NO LONGER DETERMINISTIC, and the cap-2 rule has begun rotting
exactly as cap-4 did before it.** `193.1-01` measured `failed 3, 0, 0, 0` on ONE identical tree at
3620 cases; the orchestrator measured `failed 1` then `failed 0` at 3717. Every failure carried the
`STACK_TRACE_ERROR` timeout signature and none reproduced. **2 remains the best value; it is no
longer a guarantee.** ⚠ **The orchestrator lost one red run's failing filenames by piping the gate
through `tail` — the precise mistake `193.1-01` said it would avoid — and records that as its own
error rather than as an unexplained flake.** Later runs redirected output to a file.

---

## Task 2 — the hot-file ledger, all rows re-derived at close

Five rows corrected in `CLAUDE.md`. **Every figure re-derived with `git log --oneline -- <file> |
wc -l` and `wc -l <file>` rather than read from the cell.**

| File | cell said | **measured at close** |
|---|---|---|
| `frontend/src/pages/WorkflowBuilderPage.tsx` | 33 / 10 / 2055 | **40 / 11 / 2252** |
| `backend/app/api/workflows.py` | 32 / 16 / 1813 | **34 / 17 / 1951** |
| `backend/app/services/harness/grounding.py` | 15 / 4 / 1186 | **18 / 5 / 1252** |
| `frontend/src/components/workflows/WorkflowDoorSwitch.tsx` | 11 / 7 / 426 | **12 / 8 / 522** |
| `frontend/src/components/workflows/PhaseFormPanel.tsx` | *140 / 183 / 184 / 185 — 1095 L* | **16 commits / 8 phases / 1167 L** |

⚠ **The `PhaseFormPanel.tsx` row was wrong in BOTH directions and is rewritten, not patched.** It
named a **phase 140 that does not appear in the file's history at all**, and omitted **five that do**
(103, 155, 189, 193, 193.1). *A row that names a phase which never touched the file, and omits five
that did, cannot be audited against* — the same class of failure as a hot file being **absent** from
the table, which `WorkflowsPage.tsx` suffered for ten phases and `WorkflowDoorSwitch.tsx` for six.

⚠ **Two figures written by THIS PHASE were already stale when written**, and both are corrected
beside their originals rather than over them: `193.1-CONTEXT.md` D-01's discuss-time re-measurement
(`33 / 1813` for `workflows.py`) and D-22's (`15 / 7 / 1136` for `PhaseFormPanel.tsx`). This table has
now documented a figure going stale about itself four separate times.

**ROADMAP updated:** plans `06`, `07`, `08`, `09` ticked; **`11` inserted and ticked** (it did not
exist at plan time); `10` left open; **the phase header left `- [ ]` — the phase is NOT complete.**
The stale G-5 figures quoted in the phase's own Flags block were corrected in place, including the
fact that **G-5 fired on a THIRD file neither the ROADMAP nor CONTEXT named.**

---

## Task 3 — ⛔ **OWED. Three UAT rows only a person can answer.**

**This is a DECISION recorded honestly, not a claim that everything ran.**

| Row | What must be TRUE | Status |
|---|---|---|
| **U1** | SC#2 — a real `/generate` produces a draft aimed at the template | ✅ **driven** (plan 04, re-driven post-fix by plan 11) — ⚠ **but the operator's confirmation that these were their backend's responses is still owed** |
| **U2** | The two-templates collision: the operator reads the flag-ON describe screen and says, **unprompted**, which control does what. Silence or a wrong answer is a **FAIL** | ⛔ **OWED** |
| **U3** | The disabled CTA reads *"not ready"*, not *"broken"* — attach a large template, press immediately, the footing line must say why | ⛔ **OWED** |
| **U4** | The describe column at a real viewport — CONTEXT measured `scrollHeight 761` vs `clientHeight 614`; check whether the switch-to-govern strip falls below the fold | ⛔ **OWED** |

**Why these are not the assistant's to drive.** U2 asks whether a person can tell two controls apart
**unprompted**; scoring it from an agent that has read every string on the screen is the
check-that-cannot-fail the UAT file exists to prevent. This is the same reason Phase 193 closed owing
its own U1/U2, and **those two remain owed as well — nothing in 193.1 discharges them.**

⚠ **U2 is the most important of the three**, and its target is known in advance: `ctrl.button`
deliberately ships the shipped `TEMPLATE_ATTACH_LABEL` *"Attach a template"* character for character
(so two doors onto one act do not acquire two names), **which means the word *template* is still on
that screen by design**, roughly two inches from `STARTER_DOOR_LINE`'s *"Not sure where to start?
Start from a template."* **D-21/D-28 removed the collision from the LABEL, not from the BUTTON.**
U2 is the instrument for the residual.

⚠ **Test-data warning:** three of the operator's own workflows bind **SEED-prefixed** templates and
correctly render *"could not read"*. **Do not drive any row with them.**

---

## AUTH-03 — deliberately NOT ticked

`REQUIREMENTS.md` records AUTH-03 as *"ANSWERED WRONGLY, not delivered"* and the coverage row may not
be ticked until a draft is grounded in a real template's real placeholder keys **and every run fills
that same template**.

**The first half is now measured true** (Call B: 3/3 `render_template`, 8/8 key coverage) and **the
bind shipped in the same commit as the wire** (`02606a08`), so the mechanism exists. **But the second
half — that a real run actually fills the template end-to-end — has not been observed**, and no test
in this phase observes it. **Ticking AUTH-03 is the operator's call after U2-U4**, and this file
declines to tick it on the strength of unit evidence alone. That is precisely the trap Phase 193 fell
into: built exactly to the written criteria, missed the requirement.

---

## Guardrail overrides: **NONE recorded for Phase 193.1**

G-5 fired on **three** files and all three were honoured:
- `WorkflowBuilderPage.tsx` — **extracted, in Wave 2, BEFORE the feature** (the 192.1 D-01 order);
- `backend/app/api/workflows.py` — a **third door in a template block it already owns**;
- `PhaseFormPanel.tsx` — **its own component and one gated line**, now *mechanically guarded* by a
  source fence rather than promised.

**A waiver was OFFERED AND DECLINED at discuss time (D-01). The absence of an override is a
measurement, not an omission.** G-7 clear, G-1 does not fire, G-2 satisfied by sketches 165/166/167.

---

## What the next reader should not re-derive

1. **The phase's central assumption was FALSE and Wave 1 was sequenced to find it.** Sending
   `template_placeholders` did not flip the deliverable branch; the model also had to be told a
   template was **provided**. Cost: one unplanned plan (`193.1-11`). ⇒ `D-26`.
2. **Four fences were found that could not fire** — the `degraded` pin credited in three places, a
   missing `SWEPT_SOURCES` entry (the *higher-risk* vocabulary module), plan 07's harness swallowing a
   callback's return value, and plan 08's capture harness leaking state between arms. **Every one was
   found by planting a failure, none by reading.** *A fence is only real once you have watched it
   fail.*
3. **The wrong-base bug is 9 for 9 on this phase** (instances 13-21 project-wide). Every worktree
   arrived at `fda79214`, the `master` merge tip; every one was caught by the
   `git merge-base --is-ancestor` assertion and reset. **Without it, plan 05's entire extraction proof
   would have been worthless.** Keep that assertion in every executor prompt.
4. **The GSD SDK `state.*` verbs corrupted `STATE.md` again** — deleting `stopped_at` (~9 KB of this
   phase's locked decisions) and `resume_file` **while reporting `"updated": true`**. Every STATE.md
   edit this phase was made by hand and the YAML re-parsed afterwards. **Do not call `state.*`.**
5. **A capture of a delegating component sees its delegate's markup** — the `GOVERN_*` baseline rows
   moved by exactly the same +1015 chars / +11 tags as the `DESCRIBE_*` ones, because the govern door
   *is* the Builder. An instruction that said they must not move was wrong.
6. **The jsonb string-scalar trap is still live** — `definition` is a JSON *string* scalar on 194 of
   223 rows, so a raw `definition->'phases'` query silently reads only 29. It caught plan 09's own
   re-measurement, which then corrected itself against the plan rather than the other way round.

---

## Files

- `CLAUDE.md` — five hot-file ledger rows corrected.
- `.planning/ROADMAP.md` — plans 06-09 and 11 ticked; 10 and the phase header left open; stale G-5
  figures corrected.
- `.planning/phases/193.1-template-first-authoring/193.1-UAT.md` — U1 driven and re-driven; U2/U3/U4
  ⛔ owed.
