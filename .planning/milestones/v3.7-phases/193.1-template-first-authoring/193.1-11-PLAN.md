---
phase: 193.1-template-first-authoring
plan: 11
type: execute
wave: 2
depends_on: ["193.1-02", "193.1-03", "193.1-04"]
files_modified:
  - backend/app/services/harness/grounding.py
  - backend/tests/unit/test_193_1_grounding_wire.py
  - .planning/phases/193.1-template-first-authoring/193.1-UAT.md
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "D-26: `template_placeholders` alone does NOT flip the deliverable branch — measured 0/3; the model must also be told a template file was PROVIDED, which flipped it 2/2"
    - "D-26: the fix asserts PROVISION in the grounding section, which is the authoring service's concern — composing a `describe` suffix in the frontend is REJECTED under the one-home-per-concern rule"
    - "D-19: the DELIVERABLE RULE is real and the control obeys it — the absent arm must keep steering to plain text, proven, not assumed"
    - "D-19: the wire-and-bind indivisibility guard STAYS. Its hazard was unreachable only because the branch never fired; this plan makes it fire"
    - "The fix is re-driven against U1's exact six-call shape — a fix believed rather than measured is what D-26 exists to prevent"
  artifacts:
    - path: "backend/app/services/harness/grounding.py"
      provides: "a grounding section that ASSERTS provision when placeholders are present"
      contains: "placeholders"
    - path: ".planning/phases/193.1-template-first-authoring/193.1-UAT.md"
      provides: "row U1 re-driven and its verdict updated with the post-fix measurement"
      contains: "U1"
  key_links:
    - from: "render_grounding_prompt"
      to: "AUTHORING_SYSTEM_PROMPT's DELIVERABLE RULE"
      via: "the grounding section the rule branches on"
      pattern: "Template placeholder fields"
---

<objective>
Make the grounding **assert that a template was provided** when placeholder names are present, so the
DELIVERABLE RULE's branch actually fires — and re-drive plan `193.1-04`'s six-call measurement to
prove it, rather than believing it.

**This plan exists because `193.1-04` measured the phase's central assumption FALSE.** It is an
amendment authored after Wave 1, deliberately not folded into `193.1-02` or `193.1-03`, because both
were concurrently editing the files this touches and a second writer mid-execution is how baselines
and fences get invalidated.
</objective>

<worktree_protocol>
**FIRST action, before anything else:**

```bash
bash scripts/bootstrap-worktree.sh "$(pwd)"
```

`git worktree add` checks out TRACKED files only; `backend/venv` (1.7 GB) is gitignored and junctioned
by this script. Without it there is no Python interpreter and every pytest command fails for reasons
that look like this plan's fault.

**THEN assert your base SHA** (the orchestrator supplies it in the dispatch prompt):

```bash
git rev-parse HEAD
git merge-base --is-ancestor <BASE_SHA> HEAD && echo "BASE OK" || echo "WRONG BASE"
```

⚠ **This is not ceremony — it has now caught the wrong base 14 times, including on BOTH Wave-1
worktrees of this phase** (each came up at `fda79214`, the `master` merge tip). If HEAD is not at or
descended from the dispatched SHA, `git reset --hard <BASE_SHA>` and say so in the SUMMARY.

⚠ **NEVER `rm -rf` this worktree.** A recursive delete FOLLOWS a junction and silently destroys the
operator's real 1.7 GB `venv`. Teardown is the orchestrator's job.

Python runs through the venv explicitly: `backend/venv/Scripts/python.exe -m pytest …`.
</worktree_protocol>

<context>
@.planning/phases/193.1-template-first-authoring/193.1-CONTEXT.md
@.planning/phases/193.1-template-first-authoring/193.1-UAT.md
@.planning/phases/193.1-template-first-authoring/193.1-03-SUMMARY.md
@.planning/phases/193.1-template-first-authoring/193.1-RESEARCH.md
</context>

<interfaces>
<!-- Measured at HEAD after 193.1-02 and 193.1-03 merged. -->

**The line to change** — `backend/app/services/harness/grounding.py:580`, inside
`render_grounding_prompt` (`:536`):

```
"### Template placeholder fields (if the workflow must fill a template)\n"
f"{', '.join(bundle.placeholders) if bundle.placeholders else '(none)'}\n"
```

**The rule that branches on it** — `AUTHORING_SYSTEM_PROMPT`,
`backend/app/services/workflow_authoring.py:82` onward. Its condition is *"ONLY when the grounding
explicitly lists template placeholders **(i.e. the user provided a .docx/.pptx/.xlsx template to
fill)**"*, closing *"reach for a template only when one is **actually provided**"*. **Nothing in the
payload asserts provision** — that is the whole defect.

**The pins that WILL red, and must be updated deliberately** — `193.1-03` (merged) holds:
- `_HEADING` at `backend/tests/unit/test_193_1_grounding_wire.py:81`, a byte-exact copy of the
  current header string;
- the *"grounding section heading must appear EXACTLY once"* assertion at `:125`.

⚠ **These two reds are EXPECTED and are the point.** They are the mechanism noticing a change to the
string it guards. Label them in the SUMMARY as declared re-captures ("1 of 1"), dated and reasoned —
never quietly absorbed. **Every OTHER assertion in that file must stay green with zero edits**, in
particular the four RED-driven fences and the `degraded` positive control; if any of them reds, stop
— that is a real regression, not a re-capture.

**The measured baseline this plan must beat** (`193.1-UAT.md` row U1, six real calls on
`claude-opus-4-8` / `anthropic`):

| Call | `describe` asserts a template | `template_placeholders` sent | `render_template` phase | keys in draft |
|---|---|---|---|---|
| A (control) | no | no | 0 | 1/8 |
| B ×3 | no | **yes** | **0 / 0 / 0** | 3–4/8 |
| C ×2 | **yes** | yes | **1 / 1** | **8/8** |

**⚠ `POST /workflows/generate` PERSISTS NOTHING** — verified in its own docstring (*"a draft object
the Builder loads (NOT persisted)"*). Re-driving mints no library row.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Make the grounding section ASSERT provision, on both arms</name>
  <files>backend/app/services/harness/grounding.py</files>
  <read_first>
    - backend/app/services/harness/grounding.py lines 536-600 (`render_grounding_prompt` in full — every other section's voice, so the new wording matches the file rather than importing a new style)
    - backend/app/services/workflow_authoring.py lines 75-100 (the DELIVERABLE RULE's exact condition — the new text must satisfy it on its own terms, not approximately)
    - .planning/phases/193.1-template-first-authoring/193.1-UAT.md § U1 (what Call C said that worked)
  </read_first>
  <action>
    Replace the single conditional header + value pair at `grounding.py:580-581` with a **two-arm
    branch on `bundle.placeholders`**, so the section states a fact instead of posing a condition.

    **Present arm** — must assert, in the grounding's own voice, all three of: that the user HAS
    attached a template document; that the final deliverable MUST be an `llm_emit` phase with
    `emitter: "render_template"`; and that it must fill exactly the listed fields. Then the names,
    comma-joined exactly as today. Keep the `### `-prefixed heading shape every other section in this
    function uses.

    **Absent arm** — must assert that NO template was provided and that the deliverable must therefore
    be plain text / markdown. ⚠ **Do not weaken this arm.** Call A proved the absent path already
    behaves correctly; this arm must stay at least as strong, because SC#4 (*the fast door stays
    fast*) and D-08 both rest on it.

    Add a docblock above the branch recording WHY the wording is assertive: the conditional phrasing
    was measured to leave the branch unfired 3 times out of 3 while an explicit statement of provision
    fired it 2 out of 2 (`193.1-UAT.md` row U1). **Cite D-26.**

    ⚠ **Do NOT edit `AUTHORING_SYSTEM_PROMPT`.** One home for one concern: the rule states the policy,
    the grounding states the facts the policy reads. Changing both at once would leave neither
    attributable when the re-drive is measured.
  </action>
  <acceptance_criteria>
    - `render_grounding_prompt` with a non-empty `placeholders` returns a string containing every supplied name AND an assertion of provision; with an empty `placeholders` it returns a string asserting no template was provided.
    - The two arms share NO sentence that would let one be mistaken for the other (assert they differ, not merely that each renders).
    - `grep -c "if the workflow must fill a template" backend/app/services/harness/grounding.py` == 0.
    - `grep -c "D-26" backend/app/services/harness/grounding.py` >= 1.
    - `backend/app/services/workflow_authoring.py` is byte-unchanged: `git diff --numstat backend/app/services/workflow_authoring.py` is EMPTY.
    - `cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_193_1_stateless_placeholders.py tests/unit/test_q5r_template_placeholders.py -q` exits 0 (35 passed — this change must not touch the read doors).
  </acceptance_criteria>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python.exe -m pytest tests/unit/test_193_1_stateless_placeholders.py tests/unit/test_q5r_template_placeholders.py -q</automated>
  </verify>
  <done>The grounding states a fact the rule can act on, and the absent arm is no weaker than before.</done>
</task>

<task type="auto">
  <name>Task 2: Re-pin `193.1-03`'s two heading assertions, and prove nothing else moved</name>
  <files>backend/tests/unit/test_193_1_grounding_wire.py</files>
  <read_first>
    - backend/tests/unit/test_193_1_grounding_wire.py (the whole file — you are editing two constants inside a suite whose other assertions must not move)
    - .planning/phases/193.1-template-first-authoring/193.1-03-SUMMARY.md (what each fence defends and the RED output each produced, so you can tell a re-capture from a regression)
  </read_first>
  <action>
    Run the suite FIRST, before editing it, and record which cases red. Expected: only the two tied to
    `_HEADING` (`:81`) and the exactly-once heading assertion (`:125`).

    Update those two to the new present-arm heading. **Then ADD a case the old suite could not have:
    assert the ABSENT arm renders its own distinct assertion and that the two arms share no
    sentence** — the D-26 defect was precisely that one string served both meanings, and a suite that
    only pins the present arm would let it regress back.

    ⚠ **Do not touch the four RED-driven fences or the `degraded` positive control.** If any of them
    reds, STOP and report — that is a regression, not a re-capture.

    Record in the SUMMARY, as a DECLARED re-capture "1 of 1", dated, with the reason: the guarded
    string changed by decision D-26, and the guard noticed. State the before and after values.
  </action>
  <acceptance_criteria>
    - The pre-edit run's failing case list is recorded verbatim in the SUMMARY, and contains ONLY heading-related cases.
    - `cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_193_1_grounding_wire.py -q` exits 0, with a case count strictly GREATER than 10 (the new absent-arm case).
    - The suite contains an assertion that the present and absent arms differ.
    - `git diff` on this file touches only the heading constants and adds the new case — no fence body is modified.
  </acceptance_criteria>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python.exe -m pytest tests/unit/test_193_1_grounding_wire.py -q</automated>
  </verify>
  <done>The guard tracks the new string, and now also guards the distinction the old one could not see.</done>
</task>

<task type="auto">
  <name>Task 3: Re-drive U1's six-call shape and record whether the fix actually worked</name>
  <files>.planning/phases/193.1-template-first-authoring/193.1-UAT.md</files>
  <read_first>
    - .planning/phases/193.1-template-first-authoring/193.1-UAT.md § U1 (the exact describe strings, the eight keys, the table shape to extend — copy them, do not re-type them)
    - .planning/phases/193.1-template-first-authoring/193.1-04-SUMMARY.md (how the calls were driven and what was deliberately NOT touched)
  </read_first>
  <action>
    ⚠ **The backend must be restarted for this task to mean anything** — the running dev server holds
    the OLD prompt in memory. If you cannot restart it (the operator owns it), say so plainly and
    record the re-drive as ⛔ OWED with that reason rather than reporting a result you did not
    observe. **A fix believed rather than measured is exactly what D-26 exists to prevent.**

    If the backend is restartable, re-drive the SAME shape: Call A (control, no placeholders) once,
    and **Call B three times** — same `describe` as U1, silent about any template, WITH
    `template_placeholders`. Call C is no longer needed: it was the diagnostic that isolated the
    cause, and the fix's whole claim is that **B now behaves as C did**.

    Record, in a new `### U1 re-drive (post-D-26 fix)` subsection beneath the existing U1 evidence
    (**do not delete or rewrite the original measurement — it is the before-column**):
    - the same table columns, so before and after are comparable at a glance;
    - all raw responses verbatim;
    - the model id and provider;
    - **an explicit PASS/FAIL on the one question: does Call B now produce an `llm_emit` phase with
      `emitter: "render_template"`, on all three runs?**

    Then update row U1's verdict cell: ✅ only if B produces `render_template` on **3 of 3** — a
    2-of-3 is a FAIL with the flakiness stated, not a pass with a caveat.

    Finally, re-state the consequence line owed to Plan 09: with the fix in, does a template-aware
    draft declare a non-empty `inputs[]` or reach more keys into slugs/names? **Plan 09's fixture and
    copy depend on this answer, and it is the second time this measurement has been the deciding
    input.**

    ⚠ No token, `Authorization` header or API key may appear in the artifact (T-193.1-04-01). Mutate
    NO global setting — in particular do not set `harness_authoring_model` (T-193.1-04-02).
  </action>
  <acceptance_criteria>
    - `193.1-UAT.md` contains a `### U1 re-drive (post-D-26 fix)` subsection, and the ORIGINAL U1 evidence is still present and unedited.
    - Row U1's verdict cell is ✅, ❌ or ⛔ with a reason — never blank, never removed.
    - If driven: four raw responses verbatim, plus a stated PASS/FAIL on the 3-of-3 question.
    - If not driven: ⛔ with the restart reason, and no invented result anywhere in the file.
    - `grep -ci "eyJhbGci" .planning/phases/193.1-template-first-authoring/193.1-UAT.md` == 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "U1 re-drive" .planning/phases/193.1-template-first-authoring/193.1-UAT.md</automated>
  </verify>
  <done>The fix is measured, not believed — or its measurement is honestly recorded as owed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document-derived placeholder names → LLM prompt | The names now arrive inside an ASSERTIVE instruction, which is a stronger channel than a hedged list. |
| the operator's running backend → this plan's record | A live authenticated call may be driven and its response recorded into a planning artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193.1-11-01 | Tampering | prompt injection via placeholder names | **accept (bounded)** — ⚠ **but the blast radius GROWS here and that is stated, not smoothed.** The names now sit inside a sentence instructing the model what it MUST do, so a crafted field name is quoted in a more authoritative position than before. It remains bounded by the SAME server-side controls, which are enforced AFTER the emit and are unaffected by prompt wording: `_check_grounding_fidelity` (`workflow_authoring.py:203-230`) — folder scopes ⊆ the project subtree, tools ∈ the real registry, `skill_ref` ∈ the enabled set — plus `extra="forbid"` rejecting invented keys. **No new capability is reachable through this change**; the worst case is a lower-quality draft, which the author reviews before saving. |
| T-193.1-11-02 | Information disclosure | the UAT artifact | mitigate | Record request bodies and returned definitions ONLY. No bearer token, `Authorization` header or API key. Asserted by a `grep` in Task 3's acceptance criteria. |
| T-193.1-11-03 | Tampering | the operator's environment | mitigate | No global setting is mutated; `harness_authoring_model` stays unset. `/generate` persists nothing, so no library row is minted. |
| T-193.1-11-04 | Denial of service | the absent arm | mitigate | The absent arm must stay at least as strong — a weakened plain-text instruction would push drafts toward `render_template` with no template, which is **terminal at run** (`no_template_bound`, `phase_types.py:1271`). Pinned by Task 2's new distinctness case. |
| T-193.1-11-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed. No migration is added.** |
</threat_model>

<verification>
- The present arm asserts provision; the absent arm asserts its absence; the two share no sentence.
- `workflow_authoring.py` is byte-unchanged.
- `test_193_1_grounding_wire.py` exits 0 with more cases than before; the four RED-driven fences and the `degraded` positive control are untouched and green.
- `test_193_1_stateless_placeholders.py` + `test_q5r_template_placeholders.py` exit 0 (35 passed).
- The full backend unit suite shows **62 failed** — the recorded SEED-056 rot baseline, unchanged — with passes grown, not shrunk.
- Row U1 carries a verdict and, if driven, a before/after table with all raw responses.
</verification>

<success_criteria>
The DELIVERABLE RULE's branch fires on `template_placeholders` alone, measured on a real call rather
than assumed — or the re-drive is honestly recorded as owed. The absent arm is provably no weaker, so
SC#4 and D-08 still hold by construction.
</success_criteria>

<output>
Create `.planning/phases/193.1-template-first-authoring/193.1-11-SUMMARY.md` when done.
</output>
