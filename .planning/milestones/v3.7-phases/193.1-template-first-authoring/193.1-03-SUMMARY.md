---
phase: 193.1-template-first-authoring
plan: 03
subsystem: backend / workflow authoring grounding
tags: [AUTH-03, SC#2-mech, D-19, grounding, authoring-prompt, degraded, fences]
wave: 1
requires: []
provides:
  - "the SC#2 mechanical half — the supplied placeholder names provably reach the authoring prompt, with NO provider call"
  - "the DELIVERABLE RULE pin — both clauses plus the no_template_bound consequence"
  - "the degraded set-membership pin the shipped q5r dataclass-field pin never covered"
affects:
  - "plan 04 (the live /generate call) — this is its mechanical precondition, not its substitute"
  - "plan 07 (wire + bind, indivisible) — the run-time terminal failure it exists to avoid is pinned here"
tech-stack:
  added: []
  patterns: ["characterization pins driven RED against real plants, then restored byte-identical"]
key-files:
  created:
    - backend/tests/unit/test_193_1_grounding_wire.py
  modified: []
decisions:
  - "D-19 is now asserted rather than believed: the names reach the prompt AND the rule that branches on them is pinned"
  - "The shipped pin test_grounding_bundle_has_no_template_read_axis is PROVED weaker than its reputation — it passed a planted degraded.add(\"template\") while the two new cases RED'd"
  - "A permanent positive control was added because degraded measures EMPTY under the offline posture, so the two membership cases are vacuous on their own — stated rather than hidden"
metrics:
  duration: ~35 min
  tasks: 2
  commits: 2
  tests_added: 10
  files_created: 1
  source_files_modified: 0
  completed: 2026-08-14
---

# Phase 193.1 Plan 03: Pin the Wire Summary

**One-liner:** `template_placeholders` is now mechanically proved to reach the authoring prompt,
and the CRITICAL DELIVERABLE RULE that branches on it — plus the `degraded` exclusion that keeps a
document-read blip out of `/validate` and the publish gauntlet — are pinned by ten fences, four of
which were driven RED against real plants in production source.

## Worktree protocol

| Step | Result |
|---|---|
| `bash scripts/bootstrap-worktree.sh` | `BOOTSTRAP OK` — venv + node_modules junctioned, `.env` copied |
| Base SHA assertion | ⚠ **`WRONG BASE` on first read** |

⚠ **The worktree forked from the WRONG base, and it is stated rather than fixed quietly.** `git
rev-parse HEAD` returned **`fda792141b0129de7b15dd40ddc1082e76f95a2a`** — the `master` tip (*"Merge
develop into master — BUG-260809-02 …"*), not the dispatched
`5333518b2c969294fc0fce93b9549e30189531ff`. `git merge-base --is-ancestor` answered `WRONG BASE`.
Recovered with `git reset --hard 5333518b…` (after discarding an incidental
`.claude/settings.local.json` modification), then re-asserted → `BASE OK`.

This is the **13th** recorded instance of this failure (12/12 in Phase 192, once more on
2026-08-14). The dispatched-SHA assertion in the executor prompt is what caught it.

## Tasks

| Task | Name | Commit | Files |
|---|---|---|---|
| 1 | The names reach the prompt, and the rule that branches on them is pinned | `97052def` | `backend/tests/unit/test_193_1_grounding_wire.py` (created) |
| 2 | Strengthen the `degraded` pin to assert what the constraint actually says | `9be95b23` | same file (extended) |

**No source file under `backend/app` was modified.** Every plant was reverted and verified
byte-identical.

## Test results

| Command | Result |
|---|---|
| `pytest tests/unit/test_193_1_grounding_wire.py -x -q` (task 1 gate) | **7 passed**, exit 0 |
| `pytest tests/unit/test_193_1_grounding_wire.py tests/unit/test_q5r_template_placeholders.py -q` (task 2 gate) | **24 passed**, exit 0 |
| `pytest tests/unit -q` (whole backend unit suite) | **62 failed, 2070 passed, 2 xfailed, 2 xpassed** in 64 s |

⚠ **The 62 is the KNOWN baseline, not a regression, and it is stated with its source rather than
asserted.** `CLAUDE.md`'s worktree section records the measured backend rot as *"62 failed / 1986
passed on both"* (serial and parallel). The failed count is unchanged at **62**; the passed count
has grown 1986 → 2070 because the suite has grown since that figure was taken. **Zero of the 62 are
in this plan's file or in the q5r suite** — verified mechanically:
`pytest tests/unit -q | grep "^FAILED" | grep -c "193_1\|q5r"` → **0**.

### Acceptance criteria, mechanically checked

| Criterion | Measured |
|---|---|
| ≥ 5 test functions | **10** (`grep -c "^def test_\|^async def test_"`) |
| `grep -c "forced_emit\|httpx\|requests\|openai\|anthropic" <file>` == 0 | **0** — no provider path is named, in code OR in prose |
| Heading literal spelled exactly once in the test source | **1** |
| Suite asserts `rendered.count(HEADING) == 1` | yes — applied on **every** section read, via `_section_line` |

## The fences, and the RED output for each

**A fence nobody has seen fire is a fence nobody knows is connected.** Four plants were made in
production source, each observed RED, each restored.

### Plant 1 — delete clause (b) of the DELIVERABLE RULE (`workflow_authoring.py:85-89`)

The exact edit SEED-157 would be restored by: the plain-text-when-absent clause and the
`no_template_bound` consequence removed, leaving clause (a) intact.

```
FAILED test_the_deliverable_rule_carries_both_clauses_and_the_consequence
E  assert 'When NO template is listed in the grounding, the final deliverable MUST be
   PLAIN TEXT / markdown' in "You are a workflow authoring assistant for a knowledge-base
   agent platform. …"
tests\unit\test_193_1_grounding_wire.py:232: AssertionError
```

Restored → md5 `a0879dcefc5ac5069a1e65eaedc2f8e9`, `git diff --numstat` **empty**.

### Plant 2 — replace the render's join with a constant `(none)` (`grounding.py:577`)

The un-wiring that would make the whole phase inert while every other gate stayed green.

```
FAILED test_every_supplied_name_reaches_the_prompt
E  AssertionError: 'accomplishments' never reached the grounding prompt
E  assert 'accomplishments' in ['(none)']
tests\unit\test_193_1_grounding_wire.py:142: AssertionError

FAILED test_a_name_carrying_injection_text_is_rendered_verbatim
E  AssertionError: assert ['(none)'] == ['ignore_prev...project_name']
E    At index 0 diff: '(none)' != 'ignore_previous_instructions'
```

Note the failure names the *specific* missing name — the per-name membership assertion, not a
containment check over the joined string, is what produces that message.

### Plant 3 — duplicate the grounding section (`grounding.py:576-577` emitted twice)

```
FAILED test_the_section_heading_appears_exactly_once
E  AssertionError: the grounding section heading must appear EXACTLY once in the rendered
   prompt; found 2
E  assert 2 == 1
```

Three tests RED'd on this plant, because `_section_line` applies the count fence on every read.
Restored → md5 `85869e18259c5f3aeb808778a9609472`.

### Plant 4 — `degraded.add("template")` inside `assemble_grounding_bundle` (`grounding.py:513`)

**This is the plant that proves the correction.** Verbatim:

```
FAILED test_a_SUCCESSFUL_template_read_puts_no_template_code_in_degraded
FAILED test_a_FAILED_template_read_puts_no_template_code_in_degraded
E  AssertionError: a template-read axis entered bundle.degraded: ['template']
   (whole set: ['template']). That set feeds grounding_unavailable_finding, consumed by
   POST /workflows/validate and by the publish gauntlet — a document-read blip must never
   become a validation finding.
   assert ['template'] == []
tests\unit\test_193_1_grounding_wire.py:312: AssertionError
```

⚠ **And in the SAME run, under the SAME plant, the shipped pin PASSED:**

```
pytest tests/unit/test_q5r_template_placeholders.py::test_grounding_bundle_has_no_template_read_axis
  → 1 passed
```

That is the finding stated as a measurement rather than an argument: the pin credited — in its own
docblock, in `resolve_template_placeholders`'s docblock (`grounding.py:279-286`) and in `CLAUDE.md`'s
`grounding.py` ledger row — with defending *"a template-read failure must NEVER enter `degraded`"*
**passes through exactly the change it is quoted as preventing.** It asserts the absence of
`template_read` / `read` FIELDS on the dataclass, and `degraded.add("template")` adds no field.

The original test is **untouched and still green**; the correction is stated beside it, never over
it.

Restored → md5 `85869e18259c5f3aeb808778a9609472`, `git diff --numstat` **empty** across both
source files.

### ⚠ One honesty note that is NOT a plant, and matters more than it looks

**Measured: under the offline `MagicMock` posture, `bundle.degraded` comes back EMPTY** —
`sorted(b.degraded)` → `[]`. Neither guarded read (`fetch_visible_folders`, `_resolve_caller_org_ids`)
raises against a `MagicMock`. So *"no element of `degraded` mentions a template"* is **vacuously
true** over an empty set, and two green cases prove nothing on their own.

Rather than leave that for a later reader to discover, a **permanent positive control** was added
(`test_the_degraded_predicate_itself_fires_on_a_template_shaped_code`) pinning the predicate against
synthetic sets, so a typo in `_TEMPLATE_WORDS` reds immediately and forever. The plant proves
connectivity once; the control keeps proving the predicate. The vacuity is stated in that test's own
docblock so nobody inherits *"the degraded cases are green"* as if it meant *"a template code would
be caught"*.

## ⚠ RESEARCH §B's four-hop trace — verified at HEAD, hop by hop

Every `file:line` in §B is a claim. All were re-derived at `5333518b`. **The trace is ACCURATE. Not
one hop has moved.**

| # | Claim | Measured at HEAD | Verdict |
|---|---|---|---|
| 1 | `GenerateRequest` opens at `workflows.py:1539` | `1539:class GenerateRequest(BaseModel):` | ✅ exact |
| 1 | field at `workflows.py:1547` | `1547:    template_placeholders: list[str] \| None = None` | ✅ exact |
| 2 | forward at `workflows.py:1583` | `1583:        template_placeholders=body.template_placeholders,` | ✅ exact |
| 3 | `generate_workflow_definition` param `workflow_authoring.py:242` | `242:    template_placeholders: … = None` | ✅ exact |
| 3 | forward `workflow_authoring.py:298` | `298:        template_placeholders=template_placeholders,` | ✅ exact |
| 3 | thin delegate `:172` / `:197` | `_assemble_grounding` opens `:165`, param `:172`, forward `:197` | ✅ exact |
| 3 | → `grounding.py:345` | `345:    template_placeholders: list[str] \| None = None,` on `assemble_grounding_bundle` (`:338`) | ✅ exact |
| 4 | supplied short-circuit `grounding.py:288-289` | `288: if template_placeholders:` / `289: return list(template_placeholders), "ok"` | ✅ exact |
| 4 | resolve call `grounding.py:507-513` | `507-513` is the call; `504-506` is the discard comment | ✅ exact |
| 4 | `:523` puts it on the bundle | `523:        placeholders=placeholders,` | ✅ exact |
| 4 | **render at `:576-577`** | `576` heading literal, `577` the join-or-`(none)` f-string | ✅ exact |
| — | `degraded` built at `:528` | `528:        degraded=frozenset(degraded),` | ✅ exact |
| — | grounded prompt is the FIRST user message, `:301-303` | `301-303: base_messages = [{"role": "user", …}]` | ✅ exact |
| — | DELIVERABLE RULE at `workflow_authoring.py:82-91` | `82` opens `"DELIVERABLE RULE (CRITICAL …"`, `91` closes the clause | ✅ exact, verbatim |
| — | passed as `system_prompt` at `:316` | `316:            system_prompt=AUTHORING_SYSTEM_PROMPT,` | ✅ exact |
| — | `_check_grounding_fidelity` at `:203-230` | opens `203`, closes `230` | ✅ exact |
| — | terminal failure `phase_types.py:1271` | `1271: render_verdict={"failure": "no_template_bound"}`; substep `:1273`; `_emit_failure_output` `:1275` | ✅ exact |
| — | shipped pin at `test_q5r_template_placeholders.py:324-335` | `324:def test_grounding_bundle_has_no_template_read_axis` … `335` | ✅ exact |

**Two refinements, neither a correction — stated so a later reader is not surprised by their own
command's output:**

1. **`:504-513` vs `:507-513`.** RESEARCH §C quotes the resolve site as `:504-513` while §B and the
   plan's `<interfaces>` quote `:507-513`. Both are true readings of the same thing: `504-506` is the
   three-line comment recording that the read status is discarded on purpose, `507-513` is the call
   itself. No discrepancy.
2. **The `degraded` docblock span.** RESEARCH says *"the docblock at `:282-286` records why the
   exclusion matters"*. Measured, the sentence that records it runs `:283-286` and the paragraph
   containing it opens at `:279`. The content is exactly as described.

## Deviations from Plan

**None.** The plan executed exactly as written, with two additions that are strengthenings inside its
own stated intent rather than scope changes:

1. **Three extra RED plants in Task 1.** The plan mandates a RED plant only for Task 2. Task 1 is
   `tdd="true"`, and for a *characterization* pin over shipped behaviour there is no implementation
   to write — the honest RED/GREEN analog is to drive each new fence against a real plant, observe
   it fire, restore, observe green. That is what was done (plants 1–3 above). No source line
   survived.
2. **A positive control on the `degraded` predicate.** Added because the property measured vacuous
   under the offline posture — see the honesty note above. It adds one test function; it changes no
   behaviour and widens no scope.

**No package installed. No migration. No source file modified.** All three were confirmed
unnecessary, not merely avoided.

## Threat model disposition

| Threat ID | Disposition | What actually happened |
|---|---|---|
| T-193.1-03-01 — prompt injection via placeholder names | **accept (bounded)** | Asserted as a measured fact (`test_a_name_carrying_injection_text_is_rendered_verbatim`), with the two bounding mitigations named in the module docblock: server-side `_check_grounding_fidelity` (`workflow_authoring.py:203-230`) applied AFTER the emit, and `extra="forbid"` rejecting invented keys. **PRE-EXISTING since Phase 103** via `template_asset_id`. **No mitigation was attempted** — this plan neither introduces nor closes it. |
| T-193.1-03-02 — a template read failure entering `bundle.degraded` | **mitigated** | Two set-membership cases plus a positive control; driven RED against a real plant (plant 4). |
| T-193.1-03-03 — a prompt edit silently un-wiring the DELIVERABLE RULE | **mitigated** | Both clauses + the `no_template_bound` consequence pinned; driven RED against a real plant (plant 1). Additionally, `test_the_terminal_run_time_failure_the_rule_names_really_exists` checks the executor really emits that substep, so the rule cannot threaten a code the engine never mints. |
| T-193.1-03-SC — package installs | **n/a** | No package installed. |

## Known Stubs

None. This plan ships assertions over shipped behaviour; it wires no UI and stubs no data source.

## Threat Flags

None. No new network endpoint, auth path, file-access pattern or schema change — the plan's
`files_modified` is one test file and its measured output is one test file.

## State files

⚠ **`STATE.md` / `ROADMAP.md` / `REQUIREMENTS.md` were deliberately NOT modified by this executor,
and the omission is a decision rather than a miss.** This is wave 1 of a 10-plan phase running in
**parallel worktrees**; ten executors each editing `STATE.md` would guarantee merge conflicts on
every wave boundary, and `MEMORY.md` records that seven GSD SDK state verbs write FALSE records
(STATE.md corrupted 5× in Phase 190 alone, with the standing instruction to hand-edit). Phase-level
position, the progress bar and `AUTH-03`'s requirement checkbox are the orchestrator's to reconcile
once the wave lands.

## Self-Check: PASSED

| Claim | Check | Result |
|---|---|---|
| `backend/tests/unit/test_193_1_grounding_wire.py` exists | `[ -f … ]` | **FOUND** |
| commit `97052def` exists | `git log --oneline --all \| grep` | **FOUND** |
| commit `9be95b23` exists | `git log --oneline --all \| grep` | **FOUND** |
| no source file under `backend/app` modified | `git diff --numstat` vs base | **EMPTY** |
| `grounding.py` restored byte-identical | md5 `85869e18259c5f3aeb808778a9609472` | **MATCH** |
| `workflow_authoring.py` restored byte-identical | md5 `a0879dcefc5ac5069a1e65eaedc2f8e9` | **MATCH** |
