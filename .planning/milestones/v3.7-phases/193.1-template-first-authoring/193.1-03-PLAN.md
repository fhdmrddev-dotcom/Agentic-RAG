---
phase: 193.1-template-first-authoring
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/test_193_1_grounding_wire.py
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "D-19: `template_placeholders` is proved to REACH the authoring prompt, and the DELIVERABLE RULE that branches on it is pinned so a prompt edit cannot silently un-wire the phase"
    - "A template-read failure can never enter `bundle.degraded` — the set that feeds /validate's grounding_unavailable_finding and the publish gauntlet"
    - "The shipped pin is proved WEAKER than the constraint it is credited with, and the stronger assertion is added rather than the claim repeated"
  artifacts:
    - path: "backend/tests/unit/test_193_1_grounding_wire.py"
      provides: "the SC#2 mechanical half (no provider call) + the degraded-exclusion pin + the DELIVERABLE RULE pin"
  key_links:
    - from: "backend/tests/unit/test_193_1_grounding_wire.py"
      to: "backend/app/services/harness/grounding.py"
      via: "render_grounding_prompt over a bundle carrying supplied placeholder names"
      pattern: "render_grounding_prompt"
    - from: "backend/tests/unit/test_193_1_grounding_wire.py"
      to: "backend/app/services/workflow_authoring.py"
      via: "AUTHORING_SYSTEM_PROMPT's DELIVERABLE RULE"
      pattern: "AUTHORING_SYSTEM_PROMPT"
---

<objective>
Pin the mechanism the whole phase rests on, mechanically and without a provider call.

Purpose: `template_placeholders` is **not a hint** (D-19). It is rendered into the authoring prompt
at `grounding.py:576-577`, and `AUTHORING_SYSTEM_PROMPT`'s *"DELIVERABLE RULE (CRITICAL)"* branches
on exactly that section: names present ⇒ `llm_emit` / `render_template`; absent ⇒ the deliverable
MUST be plain text and `render_template` is FORBIDDEN. Nothing in this repository currently
asserts either half. A prompt edit could un-wire the phase's headline claim with every gate green.

Output: one new backend suite. No source change.
</objective>

<worktree_protocol>
1. FIRST action: `bash scripts/bootstrap-worktree.sh "$(pwd)"`.
2. THEN assert `git rev-parse HEAD` equals the dispatched base SHA. A mismatch is a STOP.
3. `cd backend && venv/Scripts/python.exe -m pytest …` — never a bare `pytest`.
4. No DB writes; safe to run in parallel.
</worktree_protocol>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/193.1-template-first-authoring/193.1-CONTEXT.md
@.planning/phases/193.1-template-first-authoring/193.1-RESEARCH.md
@.planning/phases/193.1-template-first-authoring/193.1-VALIDATION.md
</context>

<interfaces>
<!-- Extracted at HEAD 568afc7a. -->

**The four hops the field travels (RESEARCH §B, each quoted with file:line):**
1. `GenerateRequest.template_placeholders: list[str] | None = None` — `workflows.py:1547`
   (the class opens at `:1539`).
2. route → service — `workflows.py:1583`, `template_placeholders=body.template_placeholders`.
3. `generate_workflow_definition` → `_assemble_grounding` → `assemble_grounding_bundle` —
   `workflow_authoring.py:242`, `:298`, `:172`/`:197` → `grounding.py:345`.
4. resolver → bundle → prompt — `grounding.py:507-513` resolves; `:288-289` returns a SUPPLIED list
   verbatim as `("ok")`; `:523` puts it on `GroundingBundle.placeholders`;
   **`render_grounding_prompt` at `:576-577` renders it.**

**The rendered section (`grounding.py:576-577`):** a literal heading
`### Template placeholder fields (if the workflow must fill a template)` followed by
`", ".join(bundle.placeholders)` — or the literal `(none)` when the list is empty.

**The branch (`workflow_authoring.py:82-91`), passed as `system_prompt` to `forced_emit` at `:316`:**
a rule labelled `DELIVERABLE RULE (CRITICAL — a wrong choice makes the workflow unpublishable)`
whose two clauses are (a) use `llm_emit` with `emitter: 'render_template'` ONLY when the grounding
explicitly lists template placeholders, and (b) when NO template is listed the deliverable MUST be
plain text and a `render_template` emit phase must NEVER be created — at run time it fails with
`no_template_bound` and the workflow can never publish.

**The run-time consequence (`phase_types.py:1255-1275`, VERIFIED):** no bytes ⇒ audit `emit_failed`,
substep `no_template_bound`, `_emit_failure_output("no_template_bound", …)`. Terminal.

**⚠ The pin that is WEAKER than its reputation (RESEARCH §C):**
`test_grounding_bundle_has_no_template_read_axis`
(`backend/tests/unit/test_q5r_template_placeholders.py:324-335`) asserts the ABSENCE of
`template_read` / `read` FIELDS on the `GroundingBundle` dataclass. **It does not assert that
`degraded` excludes a template code.** A future `degraded.add("template")` inside
`assemble_grounding_bundle` would leave it GREEN. `assemble_grounding_bundle` unpacks the status and
discards it at `:504-513`; `degraded` is built at `:528`; the docblock at `:282-286` records why the
exclusion matters — `degraded` feeds `/validate`'s `grounding_unavailable_finding` and the publish
gauntlet.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The names reach the prompt, and the rule that branches on them is pinned</name>
  <files>backend/tests/unit/test_193_1_grounding_wire.py</files>
  <read_first>
    - backend/app/services/harness/grounding.py lines 245-330 (`resolve_template_placeholders`) and 500-578 (`assemble_grounding_bundle`'s tail and `render_grounding_prompt`)
    - backend/app/services/workflow_authoring.py lines 57-105 (`AUTHORING_SYSTEM_PROMPT`) and 233-320 (`generate_workflow_definition`, `_assemble_grounding`, the `forced_emit` call)
    - backend/tests/unit/test_q5r_template_placeholders.py (how a `GroundingBundle` is constructed under test today)
  </read_first>
  <behavior>
    - Given a bundle whose `placeholders` carries eight names, `render_grounding_prompt` contains
      EACH name, and contains the heading literal exactly once.
    - Given a bundle with an empty `placeholders`, the rendered prompt contains the literal `(none)`
      in that section and NONE of the eight names.
    - `resolve_template_placeholders` returns a SUPPLIED list verbatim with `read == "ok"` and makes
      no Storage call — the supplied-list short circuit at `grounding.py:288-289`.
    - `AUTHORING_SYSTEM_PROMPT` contains both clauses of the DELIVERABLE RULE: the `render_template`
      ONLY-when-listed clause AND the plain-text-when-absent clause, plus the `no_template_bound`
      consequence.
    - A name carrying prompt-injection text (e.g. a placeholder literally spelled
      `ignore_previous_instructions`) is rendered VERBATIM into the section — asserted so the
      accepted, bounded risk is a measured fact in the suite rather than a sentence in a document.
  </behavior>
  <action>
    Write the suite as a set of independent assertions over the real functions — no provider call, no
    network, no mocked LLM.

    Assert the prompt cases with EXACT membership per name, never a single `toContain` over a joined
    string: eight names joined by `", "` would pass a containment check on the joined form even if
    the join changed shape.

    Assert the heading literal ONCE, spelled once in the test file, and assert its occurrence count
    in the rendered prompt is exactly 1 — a second grounding section would be a silent duplication.

    For the DELIVERABLE RULE, assert on the two clauses by their load-bearing fragments and add a
    docblock stating why the assertion exists: this is the ONLY branch that makes
    `template_placeholders` matter, nothing else in the repository asserts it, and a prompt edit
    that dropped clause (b) would silently restore SEED-157 with every gate green.

    For the injection case, assert the verbatim render AND write the bounded-risk statement beside
    it, naming the two mitigations that already bind: `_check_grounding_fidelity`
    (`workflow_authoring.py:203-230` — folder scopes ⊆ the project subtree, tools ∈ the real
    registry, `skill_ref` ∈ the enabled set) enforced server-side AFTER the emit, and `extra="forbid"`
    rejecting invented keys. State that the exposure is PRE-EXISTING since Phase 103 via
    `template_asset_id`, and that this plan neither introduces nor closes it.
  </action>
  <acceptance_criteria>
    - At least five test functions exist, one per behaviour above.
    - The heading literal appears exactly once in the test source and the suite asserts
      `rendered.count(HEADING) == 1`.
    - `grep -c "forced_emit\|httpx\|requests\|openai\|anthropic" backend/tests/unit/test_193_1_grounding_wire.py` == 0 (no provider path is touched).
    - `cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_193_1_grounding_wire.py -x -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_193_1_grounding_wire.py -x -q</automated>
  </verify>
  <done>SC#2's mechanical half is pinned, and so is the prompt rule the phase's headline claim depends on.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen the `degraded` pin to assert what the constraint actually says</name>
  <files>backend/tests/unit/test_193_1_grounding_wire.py</files>
  <read_first>
    - backend/tests/unit/test_q5r_template_placeholders.py lines 300-340 (`test_grounding_bundle_has_no_template_read_axis` — the pin that is weaker than its reputation)
    - backend/app/services/harness/grounding.py lines 275-300 (the docblock recording why `degraded` must exclude template codes) and 500-530 (where `degraded` is built)
  </read_first>
  <action>
    Add cases asserting the membership property the shipped pin does not cover: after
    `assemble_grounding_bundle` runs with a template whose read SUCCEEDS, and again with one whose
    read FAILS, `bundle.degraded` contains no element mentioning a template — assert the property as
    a set-membership check over the whole set, not as a check on one named code.

    Write the docblock as a CORRECTION stated beside the original rather than over it: the shipped
    pin asserts the ABSENCE OF FIELDS on the dataclass and would stay green through a
    `degraded.add("template")`; this case is the one that would red. Name the consumers that make it
    matter — `/validate`'s `grounding_unavailable_finding` and the publish gauntlet — so a future
    reader can see what a template code entering that set would actually do.

    Drive it RED once before trusting it: temporarily add a template code to the set in
    `assemble_grounding_bundle`, observe the new case fail, restore the file, and confirm the
    restored file is byte-identical (`git diff --numstat` empty). Record the observed failure message
    in the SUMMARY. A fence nobody has seen fire is a fence nobody knows is connected.
  </action>
  <acceptance_criteria>
    - Two new cases exist (successful read, failed read), each asserting over the whole `degraded` set.
    - The SUMMARY records the RED observation verbatim and shows `git diff --numstat` empty on
      `backend/app/services/harness/grounding.py` after restoration.
    - `cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_193_1_grounding_wire.py tests/unit/test_q5r_template_placeholders.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/unit/test_193_1_grounding_wire.py tests/unit/test_q5r_template_placeholders.py -q</automated>
  </verify>
  <done>A template-read failure is mechanically proved unable to reach `degraded`, and the fence has been seen to fire.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document text → LLM prompt | Placeholder names parsed from an attacker-supplied `.docx` are rendered verbatim into the authoring prompt at `grounding.py:577`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193.1-03-01 | Tampering | prompt injection via placeholder names | **accept (bounded)** | ⚠ **PRE-EXISTING since Phase 103** — the same path has been reachable via `template_asset_id`. Bounded by server-side grounding FIDELITY enforcement AFTER the emit (`_check_grounding_fidelity`, `workflow_authoring.py:203-230`) and by `extra="forbid"` rejecting invented keys. This plan neither introduces nor closes it; Task 1 records it as a measured fact so a later audit cannot "discover" it. |
| T-193.1-03-02 | Denial of service / integrity | a template read failure entering `bundle.degraded` | mitigate | Task 2's set-membership pin, driven RED. A template code in that set would become a `/validate` finding and change publish behaviour far outside this concern. |
| T-193.1-03-03 | Tampering | a prompt edit silently un-wiring the DELIVERABLE RULE | mitigate | Task 1 pins both clauses and the `no_template_bound` consequence. |
| T-193.1-03-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed.** No `## Package Legitimacy Audit` exists in RESEARCH because no external package is added. |
</threat_model>

<verification>
- Both new suites green; the shipped q5r suite unmoved.
- The `degraded` fence observed RED against a real plant, and the plant restored byte-identical.
- No source file under `backend/app` is modified by this plan (the RED plant is reverted).
</verification>

<success_criteria>
The mechanism D-19 rests on is asserted rather than believed: the names reach the prompt, the
DELIVERABLE RULE's two branches are pinned, and a template-read failure is proved unable to enter
the set that feeds `/validate` and the publish gauntlet.
</success_criteria>

<output>
Create `.planning/phases/193.1-template-first-authoring/193.1-03-SUMMARY.md` when done.
</output>
