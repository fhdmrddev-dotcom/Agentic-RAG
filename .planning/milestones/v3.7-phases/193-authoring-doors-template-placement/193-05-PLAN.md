---
phase: 193-authoring-doors-template-placement
plan: 05
type: execute
wave: 3
depends_on: ["193-01", "193-03"]
files_modified:
  - frontend/src/components/workflows/doorVocabulary.ts
  - frontend/src/components/workflows/doorVocabulary.test.ts
  - frontend/src/components/workflows/WorkflowDoorSwitch.tsx
  - frontend/src/components/workflows/DoorHeaderStrip.tsx
  - frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [AUTH-01]

must_haves:
  truths:
    - "D-10 holds — the module is `components/workflows/doorVocabulary.ts`, deliberately NOT under `library/`, because the doors are an authoring surface and `library/` carries Phase 192's fences"
    - "D-11 as superseded by D-23 holds — ALL 21 ids live in the module, including the two variant D inherits unchanged and the 21st the contract did not govern until 193-04"
    - "D-08's second half holds: this is still a PURE MOVE — every string keeps its SHIPPED value, so the 193-01 baselines and `WorkflowBuilderPage.header.test.tsx`'s band literal stay green with zero edits"
    - "D-12 holds — the describe hint stays THREE NAMED FRAGMENTS as plain strings; the component owns the `<b>` markup and the non-bold connectives"
    - "D-24(a) is fenced — no door COPY literal survives outside `doorVocabulary.ts`, with the needles READ OFF the module so the fence file spells zero literals and cannot red on itself"
  artifacts:
    - path: "frontend/src/components/workflows/doorVocabulary.ts"
      provides: "All 21 governed door strings as flat named exports, a `.ts` leaf with ZERO imports"
      min_lines: 90
      contains: "export const"
    - path: "frontend/src/components/workflows/doorVocabulary.test.ts"
      provides: "Whole-table properties: 21 ids present, non-empty, pairwise distinct with the declared exceptions"
      min_lines: 60
  key_links:
    - from: "frontend/src/components/workflows/WorkflowDoorSwitch.tsx"
      to: "frontend/src/components/workflows/doorVocabulary.ts"
      via: "a named-import list, never a namespace import"
      pattern: "from \"@/components/workflows/doorVocabulary\"|from \"\\./doorVocabulary\""
---

<objective>
Move all 21 governed door strings out of JSX into `doorVocabulary.ts` — **still a pure move, still
zero text changed**.

Purpose: the sketch's own build contract requires porting the COPY table into a vocabulary module
rather than re-typing strings into JSX, and G-5 wants an extraction before the feature. They are the
same act (D-08). Doing the move and the reword in one commit would leave the move unprovable — so the
strings arrive here at their SHIPPED values and `193-08` flips them to column D.

⚠ The move's proof is at the **DOM level**, not the source level: the source spells JSX entities
(`Author &amp; govern`) while a vocabulary module holds plain strings (`Author & govern`). The
rendered `textContent` is identical, the source bytes are not — so a source-level diff of the strings
would show a difference where the DOM shows none. `193-01`'s six captures and
`WorkflowBuilderPage.header.test.tsx`'s band literal are the right instruments.

Output: `doorVocabulary.ts` + `doorVocabulary.test.ts`, both consumers importing every string, the
D-24(a) copy fence in `WorkflowDoorSwitch.test.tsx`, and the new suite pinned.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<worktree_protocol>
1. `bash scripts/bootstrap-worktree.sh "$(pwd)"` — FIRST action.
2. Assert the dispatched base SHA; reset hard to it if the worktree forked elsewhere.
3. `GSD_VITEST_MAX_WORKERS=4` on every vitest run.
4. Never `rm -rf` a worktree — `bash scripts/teardown-worktree.sh <path>`.
</worktree_protocol>

<context>
@.planning/phases/193-authoring-doors-template-placement/193-CONTEXT.md
@.planning/phases/193-authoring-doors-template-placement/193-RESEARCH.md
@.planning/phases/193-authoring-doors-template-placement/193-PATTERNS.md
@.planning/phases/193-authoring-doors-template-placement/193-VALIDATION.md
@frontend/src/components/workflows/runVocabulary.ts
@frontend/src/components/workflows/library/libraryVocabulary.ts
@frontend/src/components/workflows/WorkflowDoorSwitch.tsx
@frontend/src/components/workflows/DoorHeaderStrip.tsx
</context>

<interfaces>
<!-- The 21 ids and their SHIPPED strings, measured at f2087341. Re-derive each by CONTENT
     (grep the string), never by line number. -->

| id | shipped string (the value this plan ports) | site |
|---|---|---|
| `chooser.h1` | `How do you want to build this?` | chooser `<h1>` |
| `chooser.sub` | `Pick the fast path or full control — nothing is locked, you can switch anytime.` | chooser sub-`<p>` |
| `doorA.tier` | `loose · fastest path` | `door-card-describe` tier |
| `doorA.name` | `Describe & run` (source: `Describe &amp; run`) | `door-card-describe` name |
| `doorA.desc` | `Say what recurring work this should do — the AI drafts the phases and sets the strictness.` | `door-card-describe` desc |
| `doorA.note` | `nothing locked — switch to Author & govern anytime` | `door-card-describe` footnote |
| `doorB.tier` | `power · full control` | `door-card-govern` tier |
| `doorB.name` | `Author & govern` | `door-card-govern` name |
| `doorB.desc` | `Open the full Builder — every advanced control, the live strictness tier, the locked judge.` | `door-card-govern` desc |
| `doorB.note` | `citation policy · gate set · per-phase scope & model` | `door-card-govern` footnote |
| `strip.back` | `‹ both doors` | `both-doors` — ⚠ **ONE id, TWO JSX sites** (`DoorHeaderStrip.tsx` and the describe band in `WorkflowDoorSwitch.tsx`) |
| `strip.label` | `⚡ Describe & run` | describe-door current-door label |
| `strip.labelGovern` | `🔧 Author & govern` | govern-door current-door label (now in `DoorHeaderStrip.tsx`) |
| `describe.h1` | `What recurring work should this automate?` | describe door `<h1>` |
| `describe.cta` | `Draft the workflow` | `describe-draft` |
| `hint.frag1` | `drafts the phases` | `describe-hint` `<b>` #1 |
| `hint.frag2` | `sets the strictness` | `describe-hint` `<b>` #2 |
| `hint.frag3` | `asks about anything it had to guess` | `describe-hint` `<b>` #3 |
| `switch.prompt` | `Need citation policy, gates, or per-phase scope?` | `switch-strip` prompt |
| `switch.cta` | `Author & govern ›` | `switch-to-govern` |
| `soul.label` | `This workflow's soul` | `describe-soul-preview` label |

**Strings the contract does NOT govern — do not move them:** `Open ›` (both door cards), the textarea
`aria-label="business requirement"`, its placeholder, the judge badge's `title` attribute,
`🔒 judge always-on`, and the hint's non-bold connective text.

The export forms shipped in this directory (`runVocabulary.ts`, `libraryVocabulary.ts`):
```ts
export const FORK_VERB = "Make my own copy"                              // flat, SCREAMING_SNAKE
export const RUN_READING_WORD: Record<CanvasReading, string> = { … }     // closed-set table
export const OWN_YOURS: (typeof CHIP_WORDS)["yours"]["label"] = "Yours"  // agreement fence
```

The `?raw` fence idiom already shipping in `WorkflowDoorSwitch.test.tsx`:
```ts
import workflowDoorSwitchSource from "./WorkflowDoorSwitch?raw"
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create `doorVocabulary.ts` — 21 shipped strings, a `.ts` leaf with zero imports (D-10 / D-11 / D-23)</name>
  <files>frontend/src/components/workflows/doorVocabulary.ts</files>
  <read_first>
    - `frontend/src/components/workflows/runVocabulary.ts` (482 L) — **the primary analog.** Same directory, same `.ts`-leaf shape, its own suite, and the file `libraryVocabulary.ts` itself cites as its precedent. Copy the header convention (what the file is, the rule it exists to keep, the "no React, no hooks, no JSX" paragraph) and the one-docblock-per-export habit.
    - `frontend/src/components/workflows/library/libraryVocabulary.ts` — the secondary analog for the flat `export const` shape, the **"lock-elsewhere / mirror-here"** paragraph, the **"PORTED FROM THE GENERATED BUILD CONTRACT, NOT TRANSCRIBED FROM PROSE"** docblock, and the **"EXACT-MATCH ASSERTIONS ONLY … a `toContain` on a fragment is a vacuous fence"** rule.
    - `frontend/src/components/workflows/WorkflowDoorSwitch.tsx` and `frontend/src/components/workflows/DoorHeaderStrip.tsx` — the 21 shipped strings, read out of the source rather than from any table
    - `193-CONTEXT.md` **D-10**, **D-11**, **D-12**, **D-23**, **D-02**, **D-08**
    - `193-PATTERNS.md` § `doorVocabulary.ts` (the header convention, the five export forms, the collision warning)
    - `193-RESEARCH.md` § B.3 / § B.4 / § B.5 / § H-2
  </read_first>
  <action>
Create `frontend/src/components/workflows/doorVocabulary.ts` holding all 21 governed strings **at
their SHIPPED values**, read out of the two component sources.

Shape (Claude's discretion per CONTEXT, within these constraints): flat `export const`,
SCREAMING_SNAKE, one docblock per export — the shipped habit in both analogs. Suggested names:
`CHOOSER_H1`, `CHOOSER_SUB`, `DOOR_A_TIER`, `DOOR_A_NAME`, `DOOR_A_DESC`, `DOOR_A_NOTE`,
`DOOR_B_TIER`, `DOOR_B_NAME`, `DOOR_B_DESC`, `DOOR_B_NOTE`, `STRIP_BACK`, `STRIP_LABEL`,
`STRIP_LABEL_GOVERN`, `DESCRIBE_H1`, `DESCRIBE_CTA`, `HINT_FRAG1`, `HINT_FRAG2`, `HINT_FRAG3`,
`SWITCH_PROMPT`, `SWITCH_CTA`, `SOUL_LABEL`.

⚠ **Values are the SHIPPED strings, not column D.** This plan is the move; `193-08` is the reword.
Write the JSX-entity conversion honestly: `&amp;` in JSX becomes a plain `&` in the module, the
rendered `textContent` is unchanged, and the docblock says so — that is exactly why the move-proof is
taken at the DOM level.

⚠ **ZERO imports.** `runVocabulary.ts` states "exactly TWO imports, both type-only"; this module goes
one better and imports nothing at all, which is what makes D-24(b)'s cycle risk one-directional and
is the mechanical reason D-10 puts the strings in a `.ts` leaf rather than in `DoorHeaderStrip.tsx`
(`react-refresh/only-export-components` is an ACTIVE error here).

Header obligations, retargeted from the analogs rather than invented:
- **The lock-elsewhere / mirror-here rule**, pointed at
  `.planning/sketches/164-telling-the-doors-apart/BUILD-CONTRACT.generated.md` instead of a CONTEXT
  file: a change is a decision taken in that generated contract and reflected here, never the other
  way round.
- **The port-not-transcribe paragraph**, naming D-02 and stating that the contract is emitted FROM
  the real component's rendered DOM, so the strings are what a human approved on screen rather than a
  re-typing of a README — and that the anti-drift mechanism only works if every consumer imports.
- **The exact-match rule**, and why it BINDS harder here than anywhere: after `193-08`,
  `DOOR_B_NAME` (`Build it myself`) is a strict PREFIX of `SWITCH_CTA` (`Build it myself ›`), and
  `STRIP_LABEL_GOVERN` is a third string built on the same words. A `toContain` on a fragment is
  vacuous by construction.
- **State plainly that the values here are the SHIPPED ones and that `193-08` replaces them with
  column D**, so a reader landing between the two waves is not misled.
- **D-12:** the three hint fragments are separate strings; the component owns the `<b>` markup and
  the non-bold connectives (`You describe the goal — the AI `, `, `, `, and `, `.`). No template
  string with `{1}`/`{2}`/`{3}` markers, no parser.
  </action>
  <acceptance_criteria>
    - `grep -c "^export const" frontend/src/components/workflows/doorVocabulary.ts` → **21**.
    - `grep -cE "^import |from \"" frontend/src/components/workflows/doorVocabulary.ts` → **0** — a true leaf, zero imports.
    - Each of the 21 values equals the corresponding SHIPPED string; verify by a script that greps each value's rendered form out of `git show <phase-base>:frontend/src/components/workflows/WorkflowDoorSwitch.tsx` (allowing `&`→`&amp;`), and paste the 21/21 result.
    - `grep -c "Open ›\|business requirement\|judge always-on" frontend/src/components/workflows/doorVocabulary.ts` → **0** — the un-governed strings did not travel.
    - `cd frontend && npx eslint src/components/workflows/doorVocabulary.ts` → 0 / 0.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` → **33**.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"</automated>
  </verify>
  <done>All 21 governed strings live in one home at their shipped values, in a zero-import `.ts` leaf whose header states the rule it exists to keep.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire both components to import every string, with the hint still composed in JSX (D-12)</name>
  <files>frontend/src/components/workflows/WorkflowDoorSwitch.tsx, frontend/src/components/workflows/DoorHeaderStrip.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/doorVocabulary.ts` — the module just written
    - `frontend/src/components/workflows/WorkflowDoorSwitch.tsx` — every governed string site, ESPECIALLY the **describe band's own `‹ both doors` button**, which is the SECOND JSX site of the single `strip.back` id
    - `frontend/src/components/workflows/DoorHeaderStrip.tsx` — the first `strip.back` site and the `strip.labelGovern` site
    - `frontend/src/components/workflows/library/WorkflowCard.tsx` — the shipped named-import-list convention (never a namespace import)
    - `193-RESEARCH.md` § H-1's last bullet: *"Missing the second `strip.back` site … a move that catches only one leaves a literal behind."*
  </read_first>
  <action>
Replace every one of the 21 governed literals in `WorkflowDoorSwitch.tsx` and `DoorHeaderStrip.tsx`
with the corresponding import from `./doorVocabulary`. Use a **named-import list**, never a namespace
import — the shipped convention in this directory.

⚠ **`strip.back` has TWO JSX sites.** `DoorHeaderStrip.tsx` holds one; the describe band inside
`WorkflowDoorSwitch.tsx` holds the other, with the same class list. Both become `{STRIP_BACK}`. That
duplication is precisely the drift the vocabulary module removes, and the D-24(a) fence in Task 3
will say so if either is missed.

⚠ **The hint stays composed in JSX (D-12).** Only the three fragments become data:
`<b …>{HINT_FRAG1}</b>`, `<b …>{HINT_FRAG2}</b>`, `<b …>{HINT_FRAG3}</b>`. The connective text
(`You describe the goal — the AI `, `, `, `, and `, `.`) and the `<b>` markup stay in the component.

⚠ **Change no class list, no testid, no attribute, no structure.** This is still a move. The only
edits are literal → `{IDENTIFIER}` and the added import line.

⚠ **Do not move the un-governed strings** (`Open ›`, the textarea `aria-label` and placeholder, the
judge badge's `title`, `🔒 judge always-on`, the hint connectives). Moving them is scope the
contract's audit cannot verify.

The proof this commit owes: `193-01`'s six captures and `WorkflowBuilderPage.header.test.tsx`'s band
literal must both stay green **with zero edits and zero deletions**. That is the whole point of doing
the move before the reword.
  </action>
  <acceptance_criteria>
    - `grep -c "‹ both doors" frontend/src/components/workflows/WorkflowDoorSwitch.tsx frontend/src/components/workflows/DoorHeaderStrip.tsx` → **0** in both files.
    - A script that greps each of the 21 shipped strings (in both plain and `&amp;`-escaped spelling) across both component files reports **0 hits** — paste the 21/21 zero result.
    - `grep -c "doorVocabulary" frontend/src/components/workflows/WorkflowDoorSwitch.tsx` → ≥ 1; same for `DoorHeaderStrip.tsx`.
    - Neither file uses `import * as` for the vocabulary module.
    - `grep -c "<b " frontend/src/components/workflows/WorkflowDoorSwitch.tsx` → **3** — the hint's markup is still owned by the component.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx src/pages/WorkflowBuilderPage.header.test.tsx src/pages/WorkflowBuilderPage.describe.test.tsx src/components/workflows/WorkflowDoorSwitch.test.tsx src/components/workflows/DoorHeaderStrip.test.tsx` → **all pass, 0 failed**.
    - `git diff --numstat -- frontend/src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx frontend/src/pages/WorkflowBuilderPage.header.test.tsx` → **both EMPTY**. Zero edits, zero re-capture.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` → **33**.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx src/pages/WorkflowBuilderPage.header.test.tsx src/pages/WorkflowBuilderPage.describe.test.tsx src/components/workflows/WorkflowDoorSwitch.test.tsx src/components/workflows/DoorHeaderStrip.test.tsx</automated>
  </verify>
  <done>Every governed string is imported, both `strip.back` sites are caught, the hint is still three fragments composed in JSX, and both pre-existing DOM baselines pass unedited.</done>
</task>

<task type="auto">
  <name>Task 3: The D-24(a) copy fence, the vocabulary suite, and the gate pin</name>
  <files>frontend/src/components/workflows/doorVocabulary.test.ts, frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/components/workflows/runVocabulary.test.ts` (420 L) — **the primary analog**: its four numbered header rules, its "the literal lives in the TEST, never in the source" falsification paragraph, its `ALL_READINGS = Object.keys(TABLE)` derive-the-list habit, and its whole-table property assertions (rule 4: *"a property stated only about the row being added is a property the next row can break in silence"*).
    - `frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx` — the shipped `?raw` import at the top and the existing source-grep case with its POSITIVE CONTROL block
    - `frontend/src/components/workflows/library/librarySubtree.fences.test.ts` — the **"sweeps no test file"** rule (`expect(SWEPT.filter((f) => /\.test\.tsx?$/.test(f.path))).toEqual([])`) and the non-vacuity guard
    - `193-CONTEXT.md` **D-24(a)**; `193-PATTERNS.md` § `WorkflowDoorSwitch.test.tsx` (the AST trap and the two shipped escapes — **use escape (a)**)
    - `scripts/vitest-count-gate.cjs` — `BASELINE`, and the read-the-number-off-the-gate rule
  </read_first>
  <action>
**(a) `doorVocabulary.test.ts` — whole-table properties only, this wave.**
Create it with the four-numbered-rule header shape from `runVocabulary.test.ts`, and assert:
- A `ALL_DOOR_WORDS` object built from the module's own exports (derive, never hand-list) has
  **exactly 21** entries.
- Every value is a non-empty string with no leading/trailing whitespace it did not ship with.
- The values are **pairwise distinct**, EXCEPT the equalities the contract deliberately declares —
  at this wave there are none, because `STRIP_LABEL_GOVERN` still carries its emoji-prefixed shipped
  value. Assert the exception SET is currently empty, and say in a comment that `193-08` will make it
  non-empty (`DOOR_B_NAME` ⊂ `SWITCH_CTA`; `STRIP_LABEL_GOVERN` ≡ `DOOR_B_NAME`) — so the next author
  extends a declared list rather than discovering a surprise.
- **No exact-match column-D literals here.** Those land in `193-08`, where the words are the subject.
  This wave's byte-level proof of the strings is `193-01`'s DOM captures, which is the right level
  given the `&amp;` entity conversion. State that reasoning in the header so the absence reads as a
  decision, not an omission.

**(b) The D-24(a) copy fence — in `WorkflowDoorSwitch.test.tsx`.**
Use PATTERNS' escape (a): **read the needles OFF the module at runtime** and sweep
`WorkflowDoorSwitch?raw` and `DoorHeaderStrip?raw` for each. The fence file then spells **zero** door
literals, so the raw-grep-reds-on-itself trap cannot fire.
- Sweep both component sources for each of the 21 values in BOTH spellings (plain and
  `&`→`&amp;`-escaped); each must be **absent**.
- ⚠ **Sweep no test file.** `doorVocabulary.test.ts` legitimately spells values; assert the swept
  path list contains no `*.test.ts(x)` path.
- Non-vacuity FIRST, per the 192.1 E-2 lesson: assert both `?raw` sources are non-empty
  (`length > 1000`) and that the needle list has 21 entries, before any negative assertion. **A fence
  swept against the empty string passes green while defending nothing.**
- POSITIVE CONTROL inline: build a fake source string containing one of the 21 values and assert the
  sweep DOES flag it.
- **Drive it RED against a real plant in a real file:** temporarily restore one literal (e.g. put
  `‹ both doors` back into the describe band in `WorkflowDoorSwitch.tsx`) and confirm the fence fails
  with a real `AssertionError` naming that string; restore and confirm md5-identical. Repeat with the
  `DoorHeaderStrip.tsx` site, so BOTH swept files are proved reachable. Record both md5 pairs.

**(c) Pin `doorVocabulary.test.ts`** in `scripts/vitest-count-gate.cjs` in this same commit — a
`BASELINE` entry for a path that does not yet exist makes the gate ERROR (exit 2), so it can only
land here. Read the number off the gate's own printed `actual` column; never hand-count. Touch no
other pin (`193-11` owns the sweep).
  </action>
  <acceptance_criteria>
    - `doorVocabulary.test.ts` asserts the table has exactly 21 entries, derived from the module's exports rather than hand-listed.
    - The pairwise-distinctness assertion names an explicit exception set, currently empty, with the `193-08` extension named in a comment.
    - The D-24(a) fence spells **zero** door literals: `grep -c "both doors\|Author & govern\|Describe & run" frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx` → **0**.
    - The fence asserts its swept path list excludes every `*.test.ts(x)` path, and asserts both `?raw` sources have `length > 1000` BEFORE any negative.
    - RED-first proof recorded **twice** (one plant per swept file), each a real `AssertionError` naming the restored string, each restored md5-identical. Paste both md5 pairs and both failure lines.
    - SCOPE proof recorded: state, with the command output, that both swept sources resolve non-empty and that the needle list is 21 long — *could it fire?*
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/doorVocabulary.test.ts src/components/workflows/WorkflowDoorSwitch.test.tsx` → all pass, 0 failed.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 node ../scripts/vitest-count-gate.cjs` → exit 0, no `[count-decrease]`, `doorVocabulary.test.ts` present in the pinned map.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/doorVocabulary.test.ts src/components/workflows/WorkflowDoorSwitch.test.tsx</automated>
  </verify>
  <done>The table's whole-table properties are pinned, the copy fence is proved able to fire against a real plant in EACH swept file, and the new suite is pinned from a read number.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated contract → vocabulary module | The module MIRRORS the contract; a value changed here without a contract change silently forks the acceptance bar. |
| vocabulary module → component JSX | The single channel for every governed word. A literal left in JSX is a second home and cannot be re-worded by a one-line diff. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193-17 | Tampering | A governed literal left behind in JSX (the second `strip.back` site) | mitigate | D-24(a) sweeps BOTH component sources for all 21 values in both spellings, with the needles read OFF the module so the fence spells zero literals. **Driven RED against a real plant in EACH swept file.** |
| T-193-18 | Spoofing (of fact) | A fence that passes because its subject resolved to the empty string | mitigate | Non-vacuity guards run FIRST: both `?raw` sources `length > 1000`, needle list length 21, plus an inline positive control. The 192.1 E-2 finding, applied. |
| T-193-19 | Tampering | A "pure move" that changed a rendered word | mitigate | `193-01`'s six DOM captures AND `WorkflowBuilderPage.header.test.tsx`'s nine-phase-old band literal must both pass with `git diff --numstat` EMPTY. Proof taken at the DOM level because the `&amp;` entity conversion makes source-level diffing meaningless for strings. |
| T-193-20 | Elevation of Privilege | A shared runtime value exported from a component module, defeating D-10 | mitigate | `doorVocabulary.ts` has ZERO imports and `DoorHeaderStrip.tsx` exports no runtime value; `react-refresh/only-export-components` is an ACTIVE eslint error and is part of the gate. |
| T-193-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero packages installed.** The `?raw` loader is Vite's and already used in this directory. |
</threat_model>

<verification>
- `cd frontend && GSD_VITEST_MAX_WORKERS=4 node ../scripts/vitest-count-gate.cjs` → exit 0, `failed 0`.
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` → **33**.
- `cd frontend && npx eslint src/components/workflows/` → ≤ 10, **0** in the three touched source files.
- `git diff --numstat -- frontend/src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx frontend/src/pages/WorkflowBuilderPage.header.test.tsx` → both EMPTY.
</verification>

<success_criteria>
- All 21 governed strings live in `doorVocabulary.ts` at their shipped values; zero governed literals remain in either component.
- The hint is still three fragments composed in JSX.
- Both pre-existing DOM baselines pass with zero edits — the move is proved at the level where the proof means something.
- The D-24(a) fence was driven RED in each swept file and its scope verified.
- `tsc` 33, count gate exit 0, the new suite pinned from a read number.
</success_criteria>

<output>
Create `.planning/phases/193-authoring-doors-template-placement/193-05-SUMMARY.md` when done.
Record: the 21/21 shipped-value verification output; the 21/21 zero-hit sweep output; both RED-plant
md5 pairs and failure lines; the `git diff --numstat` EMPTY results for both baseline files; the
gate's pin number; and the measured `components/workflows/` subtree growth, stated plainly.
</output>
