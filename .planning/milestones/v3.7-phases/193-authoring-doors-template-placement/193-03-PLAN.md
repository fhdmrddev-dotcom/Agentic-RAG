---
phase: 193-authoring-doors-template-placement
plan: 03
type: execute
wave: 2
depends_on: ["193-01"]
files_modified:
  - frontend/src/components/workflows/DoorHeaderStrip.tsx
  - frontend/src/components/workflows/DoorHeaderStrip.test.tsx
  - frontend/src/components/workflows/WorkflowDoorSwitch.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [AUTH-01]

must_haves:
  truths:
    - "G-5 is honoured BY CONSTRUCTION per D-08: the extraction ships in its own wave, BEFORE the feature it makes room for, and changes not one character of user-visible text"
    - "D-05 holds — ONE component serves both header variants, and the `ml-auto` conditional moves VERBATIM so the non-inline class list stays character-for-character what shipped"
    - "The move is proved MECHANICALLY, not asserted: the moved span sed'd out of the base blob diffs IDENTICAL against the new module, and `WorkflowBuilderPage.header.test.tsx`'s nine-phase-old byte-exact band literal stays green with ZERO edits"
    - "D-24(b) is fenced — `DoorHeaderStrip.tsx` may not name a `WorkflowDoorSwitch` specifier in ANY import form, and the fence is driven RED against a real back-import in a real file before it is trusted"
    - "D-06 stays rejected: the return control is NOT moved into `headerLead`, so the standalone band keeps a way back"
  artifacts:
    - path: "frontend/src/components/workflows/DoorHeaderStrip.tsx"
      provides: "The extracted door header strip: return control + door label + judge badge, serving both the standalone band and the inline `headerTrail`"
      min_lines: 60
      contains: "ml-auto"
    - path: "frontend/src/components/workflows/DoorHeaderStrip.test.tsx"
      provides: "D-05 `ml-auto` cases on both `inline` values + the D-24(b) ESM-cycle fence with four controls"
      min_lines: 90
  key_links:
    - from: "frontend/src/components/workflows/WorkflowDoorSwitch.tsx"
      to: "frontend/src/components/workflows/DoorHeaderStrip.tsx"
      via: "a module-scope value import rendered in the standalone band AND passed as `headerTrail`"
      pattern: "import \\{ DoorHeaderStrip \\} from"
---

<objective>
Cut the govern door's header strip out of `WorkflowDoorSwitch.tsx` into its own module — **a pure
structural move, zero text changed**.

Purpose: **G-5 fires on `WorkflowDoorSwitch.tsx`** (8 commits / 6 phases / 385 L, and the file was
ABSENT from the CLAUDE.md hot-file ledger — the identical failure to `WorkflowsPage.tsx` escaping
G-5 for ten phases). **D-09 rejected an override.** D-08 honours it by construction, in the 192.1
order: the extraction ships FIRST, in its own wave, before the copy and the restack land on it.

⚠ This plan runs BEFORE the vocabulary move (`193-05`) on purpose. The structural cut is provable by
`sed`+`diff` against the phase-base blob only while the strings are still literals — once the
vocabulary move rewrites them, that proof is no longer available in that form. The strings' own proof
is the DOM-level baseline from `193-01`, which is the right level for them anyway (the `&amp;`
JSX-entity trap makes a source-level diff of the 21 strings meaningless).

Output: `DoorHeaderStrip.tsx` + `DoorHeaderStrip.test.tsx`, `WorkflowDoorSwitch.tsx` rendering the
import in both variants, and the new suite pinned.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<worktree_protocol>
1. `bash scripts/bootstrap-worktree.sh "$(pwd)"` — FIRST action.
2. Assert the dispatched base SHA; `git reset --hard` to it if the worktree forked elsewhere. **This
   plan's whole proof is a diff against a named base blob — a wrong base makes the proof a fiction.**
3. `GSD_VITEST_MAX_WORKERS=4` on every vitest run.
4. Never `rm -rf` a worktree — `bash scripts/teardown-worktree.sh <path>`.
</worktree_protocol>

<context>
@.planning/phases/193-authoring-doors-template-placement/193-CONTEXT.md
@.planning/phases/193-authoring-doors-template-placement/193-RESEARCH.md
@.planning/phases/193-authoring-doors-template-placement/193-PATTERNS.md
@.planning/phases/193-authoring-doors-template-placement/193-VALIDATION.md
@frontend/src/components/workflows/WorkflowDoorSwitch.tsx
@frontend/src/components/workflows/PlaneEditingLayer.tsx
@frontend/src/components/workflows/WorkflowCanvas.test.tsx
@frontend/src/components/workflows/BuilderHeaderBar.tsx
</context>

<interfaces>
<!-- Re-derive every span BY CONTENT at execution HEAD. The numbers below were measured at
     f2087341 and this project has corrected line numbers in five of six recent waves. -->

The span to move — `WorkflowDoorSwitch.tsx`, inside `if (door === "govern") { … }`:
- the `doorGroup` **docblock** (the paragraph explaining `ml-auto`, currently ~`:145-155`)
- the `doorGroup` **fragment** (`const doorGroup = ( <> … </> )`, currently ~`:156-175`), containing
  the `data-testid="both-doors"` button, the `🔧 Author &amp; govern` `<span>`, and the
  `data-testid="judge-locked"` badge whose className is
  `` `${inline ? "" : "ml-auto "}inline-flex items-center gap-1 rounded-full border …` ``

The two render sites that must keep working, unchanged in shape:
```tsx
{!inline && (
  <div className="flex items-center gap-3 border-b border-border px-4 py-2">{doorGroup}</div>
)}
...
<WorkflowBuilderPage
  ...
  {...(inline ? { headerLead, headerTrail: doorGroup } : {})}
/>
```

The mechanical cause of the `ml-auto` conditional, one grep away and belonging in the moved docblock:
`BuilderHeaderBar.tsx` already wraps `trail` in
`<div className="ml-auto flex shrink-0 items-center gap-2">`.

The 188.1 cycle-fence regexes to port (from `WorkflowCanvas.test.tsx`), substituting the subject:
```ts
const IMPORT_FROM_DOORS  = /from\s+["'][^"']*WorkflowDoorSwitch(\.[jt]sx?)?["']/
const DYNAMIC_IMPORT_DOORS = /import\s*\(\s*["'][^"']*WorkflowDoorSwitch(\.[jt]sx?)?["']\s*\)/
```
⚠ `(\.[jt]sx?)?` is LOAD-BEARING: `frontend/tsconfig.app.json` sets `moduleResolution: "bundler"`
WITH `allowImportingTsExtensions: true`, so `from "./WorkflowDoorSwitch.tsx"` compiles, resolves, and
would build a real TDZ cycle under a naive fence that passes green.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Cut `doorGroup` into `DoorHeaderStrip.tsx` — verbatim, zero text changed (D-05)</name>
  <files>frontend/src/components/workflows/DoorHeaderStrip.tsx, frontend/src/components/workflows/WorkflowDoorSwitch.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/WorkflowDoorSwitch.tsx` (385 L — read whole; the govern branch, the `doorGroup` docblock and fragment, and both render sites)
    - `frontend/src/components/workflows/PlaneEditingLayer.tsx` — **the 188.1 verbatim-move product and the single best docblock template in this repo.** Copy all five paragraph KINDS (what it is + why it was liftable · STATE OF THE EXTRACTION with the pre-move `file:line` · the `:NNN`-refs-point-at-the-pre-move-file warning · the `react-refresh/only-export-components` rule · what keeps the paragraphs honest by machine).
    - `frontend/src/components/workflows/BuilderHeaderBar.tsx` — the `ml-auto` wrapper that is the mechanical cause of the conditional
    - `193-CONTEXT.md` **D-05** (one component, both variants; the `ml-auto` care), **D-06** (why the return control stays in the strip), **D-08** (wave order), **D-24(b)**
    - `193-PATTERNS.md` § `DoorHeaderStrip.tsx` and § S-2 / § S-7
    - `193-RESEARCH.md` § B.1 (the three bands), § B.2 (what `inline` actually changes), § H-2 (cycles + lint)
  </read_first>
  <action>
Create `frontend/src/components/workflows/DoorHeaderStrip.tsx` and cut the `doorGroup` docblock +
fragment into it as `export function DoorHeaderStrip(props: DoorHeaderStripProps)` returning the same
fragment.

Props: `onBack: () => void` (receives `goBoth`) and `inline?: boolean`. Nothing else — the component
owns no state, fetches nothing, and closes over nothing at module scope, which is the property that
made the fragment liftable and the property the next author has to keep true.

**The body moves byte-for-byte** with its docblock and every inline comment intact — nothing re-typed,
nothing tidied, nothing re-ordered, no Prettier pass over the moved span. The ONLY characters that may
be added are the `export ` keyword, the function wrapper, the props destructure, and the import lines.
⚠ Do **not** normalize `&amp;` to `&`, do not change `‹`, and do not touch any class list.

`DoorHeaderStrip.tsx` exports **the component and its props type and NO runtime value of any kind** —
`react-refresh/only-export-components` is an ACTIVE error in this repo (10 hits in
`components/workflows/`). This is the mechanical reason every string will go to a `.ts` leaf in
`193-05`.

Rewire `WorkflowDoorSwitch.tsx`: import `DoorHeaderStrip` at module scope and render
`<DoorHeaderStrip onBack={goBoth} inline={inline} />` at BOTH sites — inside the `{!inline && (<div
className="flex items-center gap-3 border-b border-border px-4 py-2">…</div>)}` band and as the
`headerTrail` value in the spread-conditional. **Keep the spread-conditional idiom exactly as
shipped**: without `inline` the two slots must be genuinely ABSENT from the element, not
present-and-undefined. `WorkflowDoorSwitch.tsx` must declare `doorGroup` no more and leave **no
re-export shim**.

⚠ **D-06 stays rejected.** Do not move the return control into `headerLead`; `headerLead` renders only
when `inline` is true, so the standalone band would lose its way back entirely. An author with no way
back is a worse defect than a busy strip.

Record the move proof IN THE SUMMARY, run rather than claimed: `git show` the phase-base blob of
`frontend/src/components/workflows/WorkflowDoorSwitch.tsx`, `sed -n` the moved span out of it into one
file; `sed -n` the corresponding span out of `DoorHeaderStrip.tsx` and pipe it through `sed 's/^export //'`
into a second; then `diff` the two — the output must be EMPTY. The span bounds are re-derived by content
at execution HEAD, never copied from any document.
and the diff-stat shape: insertions on the destination side exceeding deletions is EXPECTED (the new
module acquires a docblock and import lines) and is NOT evidence of drift — a diff against the
`193-01` baselines would be.
  </action>
  <acceptance_criteria>
    - `diff` of the sed'd base-blob span against the sed'd new-module span (with the one `export ` stripped) prints NOTHING — byte-identical. Paste the exact commands and the empty output into the SUMMARY.
    - `grep -c "const doorGroup = (" frontend/src/components/workflows/WorkflowDoorSwitch.tsx` → **0**.
    - `grep -c "DoorHeaderStrip" frontend/src/components/workflows/WorkflowDoorSwitch.tsx` → ≥ 3 (the import + both render sites).
    - `grep -cE "export \{[^}]*DoorHeaderStrip[^}]*\} from" frontend/src/components/workflows/WorkflowDoorSwitch.tsx` → **0** — no re-export shim.
    - `grep -c "ml-auto" frontend/src/components/workflows/DoorHeaderStrip.tsx` → ≥ 1, and the conditional is still spelled as a concatenation onto the class string (not two separate className branches).
    - `frontend/src/components/workflows/DoorHeaderStrip.tsx` exports exactly two symbols: the component and its props type. `grep -c "^export const\|^export let\|^export var" DoorHeaderStrip.tsx` → **0**.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/pages/WorkflowBuilderPage.header.test.tsx` → **all pass**, AND `git diff --numstat -- frontend/src/pages/WorkflowBuilderPage.header.test.tsx` is EMPTY (zero edits). This nine-phase-old byte-exact band literal passing unedited is the strongest available move-proof.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx` → all pass, AND `git diff --numstat` on that file shows **0 deletions**. Zero re-capture.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` → **33**.
    - `cd frontend && npx eslint src/components/workflows/DoorHeaderStrip.tsx src/components/workflows/WorkflowDoorSwitch.tsx` → 0 / 0 (no `react-refresh/only-export-components`).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/pages/WorkflowBuilderPage.header.test.tsx src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx src/components/workflows/WorkflowDoorSwitch.test.tsx</automated>
  </verify>
  <done>`doorGroup` lives in `DoorHeaderStrip.tsx`, serving both variants, proved byte-identical by `sed`+`diff` and by two independent baselines passing with zero edits.</done>
</task>

<task type="auto">
  <name>Task 2: `DoorHeaderStrip.test.tsx` — the D-05 `ml-auto` pair and the D-24(b) cycle fence</name>
  <files>frontend/src/components/workflows/DoorHeaderStrip.test.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/DoorHeaderStrip.tsx` — the module just written
    - `frontend/src/components/workflows/WorkflowCanvas.test.tsx` — **the ESM-cycle fence, the exact shape to port**: the rationale block, the two regexes with the load-bearing `(\.[jt]sx?)?` group, and all FOUR `it()`s (the matcher/positive-control one, the "neither module names the specifier" one, the LEAF one, the "the parent imports rather than declares, and left no shim" one). They are not interchangeable.
    - `frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx` — the `?raw` import idiom already shipping in this directory, and its POSITIVE CONTROL case
    - `frontend/src/components/workflows/library/librarySubtree.fences.test.ts` — the non-vacuity guard (`expect(subtreeSource.length).toBeGreaterThan(1000)`) and the four control kinds every fence in this repo carries
    - `193-CONTEXT.md` **D-05**, **D-24**
    - `193-PATTERNS.md` § `DoorHeaderStrip.test.tsx` (the full port map)
  </read_first>
  <action>
Create `frontend/src/components/workflows/DoorHeaderStrip.test.tsx` with two concerns.

**(1) D-05 — one component, both variants.** Render the strip standalone (`inline` absent) and inline
(`inline` set), and assert:
- the judge badge node carries the class token `ml-auto` when NOT inline and does **not** when inline;
- the standalone badge's full class string matches, character-for-character, the badge class substring
  held in `WorkflowBuilderPage.header.test.tsx`'s byte-exact band literal — read that substring out of
  the other suite's constant rather than re-typing it, so the two guards provably agree;
- the strip's children, asserted by **CHILD ORDER, never by class name** (the 192.1 rule): the return
  control precedes the door label, which precedes the judge badge, and the badge is the LAST child in
  both variants. Do the length check FIRST so an absent node reds as an `AssertionError` rather than a
  `getBy*` throw.
- `onBack` fires exactly once on a click of `both-doors`, in both variants.

**(2) D-24(b) — the ESM-cycle fence.** Port all four `it()`s from `WorkflowCanvas.test.tsx`,
substituting:
- `IMPORT_FROM_DOORS` / `DYNAMIC_IMPORT_DOORS` as given in `<interfaces>` — **keep the
  `(\.[jt]sx?)?` group**;
- `doorHeaderStripSource` from `./DoorHeaderStrip?raw`, `workflowDoorSwitchSource` from
  `./WorkflowDoorSwitch?raw`;
- POSITIVE CONTROLS inline and FIRST, one per import form and one per branch of the optional
  suffix group: static import, `import type`, re-export, and dynamic `import()`, each in both the
  bare and `.tsx`-suffixed spelling;
- NEGATIVE CONTROLS: `'import s from "./WorkflowDoorSwitch?raw"'` and
  `'import s from "./WorkflowDoorSwitch.tsx?raw"'` must NOT match either regex — that is this very
  file's own spelling;
- non-vacuity: `expect(doorHeaderStripSource.length).toBeGreaterThan(500)` before the negatives, so
  they are about a real file rather than an empty string;
- the one-direction test: `workflowDoorSwitchSource` MATCHES
  `/import \{ DoorHeaderStrip \} from ["'](\.\/|@\/components\/workflows\/)DoorHeaderStrip["']/`,
  does **not** contain `const doorGroup = (`, and leaves no re-export shim.

⚠ **Do NOT pre-name `doorVocabulary` in any sweep here.** It does not exist yet, and
`import.meta.glob` contributes the empty string for an absent path and never throws — the 192.1 E-2
finding, where a fence swept against the empty string passed green while defending nothing. Plan
`193-05` adds the vocabulary leg with its own non-vacuity guard.

**Drive the fence RED before trusting it.** Temporarily add
`import type { WorkflowDoorSwitchProps } from "./WorkflowDoorSwitch.tsx"` to `DoorHeaderStrip.tsx`,
run the suite, observe the "neither module names the specifier" case FAIL (a real `AssertionError`),
then remove it and confirm `DoorHeaderStrip.tsx` is md5-identical to its pre-plant bytes. Repeat with
the bare (`"./WorkflowDoorSwitch"`) spelling. Record both md5 pairs and both observed failures.
  </action>
  <acceptance_criteria>
    - The suite contains the four cycle-fence `it()`s AND the `ml-auto` pair AND the child-order case.
    - Both regexes carry the literal `(\.[jt]sx?)?` group; the suite asserts each regex against BOTH the bare and the `.tsx`-suffixed spelling of all four import forms.
    - `expect(doorHeaderStripSource.length).toBeGreaterThan(500)` is present and runs before the negative assertions.
    - RED-first proof recorded, twice: a `.tsx`-suffixed back-import plant and a bare back-import plant each reddened the fence with a real `AssertionError`; `DoorHeaderStrip.tsx` restored md5-identical after each. Paste both md5 pairs.
    - SCOPE proof recorded: state in the SUMMARY, with the command output, that the fence's subject files resolve non-empty — *could it fire?* — per the 192.1 E-2 lesson.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/DoorHeaderStrip.test.tsx` → all pass, 0 failed.
    - `cd frontend && npx eslint src/components/workflows/DoorHeaderStrip.test.tsx` → 0 / 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/DoorHeaderStrip.test.tsx</automated>
  </verify>
  <done>The strip's two variants are pinned by child order and by the `ml-auto` pair, and the cycle fence is proved able to fire against two real plants in a real file.</done>
</task>

<task type="auto">
  <name>Task 3: Pin the new suite in the count gate, in the commit that creates it</name>
  <files>scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `scripts/vitest-count-gate.cjs` — the `BASELINE` map, the `TARGETS` list (entry 0 is the directory `src/components/workflows`, which sweeps the new suite the moment it lands), and the two-knob comment block
    - `frontend/src/components/workflows/library/librarySubtree.fences.test.ts` — the house rule recorded five times: **the literal is READ OUT OF THE GATE'S OWN FAILING DIFF, never predicted, never hand-counted from `it(`**
    - `193-PATTERNS.md` § `scripts/vitest-count-gate.cjs`
  </read_first>
  <action>
Add `"DoorHeaderStrip.test.tsx": <n>` to `BASELINE` in `scripts/vitest-count-gate.cjs`, in THIS SAME
commit as the suite. A `BASELINE` entry pointing at a path that does not yet exist makes the gate
ERROR (exit 2), so it can only land here; and an unpinned covering suite is an UNGUARDED one, not a
lightly-guarded one.

Read `<n>` off the gate's own printed `actual` column, from a run at this HEAD. Do not hand-count.

Do NOT touch any other pin. `193-11` owns the phase-wide sweep; folding unrelated drift (the four
known pins outside this blast radius — `ExternalActionSection` +9, `PhaseTimeline` +4,
`WorkflowBuilderPage.canvas` +5, `builderStore` +6, **24 cases deletable with the gate green today**)
into this commit would grow a commit that did not cause it. Decline it explicitly in the SUMMARY, for
the tenth consecutive time.
  </action>
  <acceptance_criteria>
    - `grep -c "DoorHeaderStrip.test.tsx" scripts/vitest-count-gate.cjs` → 1.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 node ../scripts/vitest-count-gate.cjs` → **exit 0**, `failed 0`, no `[count-decrease]`, no `[total-below-baseline]`.
    - `git diff --stat scripts/vitest-count-gate.cjs` shows exactly one pin added (plus at most the stale-marker comment correction) — no other `BASELINE` value changed.
    - The SUMMARY quotes the gate's `total` and `pinned total` lines verbatim.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 node ../scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>The new suite is pinned from a read number in the commit that created it, and the four out-of-scope drifted pins were declined on the record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| module import graph | `WorkflowDoorSwitch` imports `DoorHeaderStrip`'s VALUE at module scope. A back-import would typecheck clean, lint clean, and fail only at RUNTIME with a TDZ `ReferenceError` in whichever module a caller reached first. |
| props → rendered class string | `inline` is the only branch that reaches a class list; it comes from the host page, not from user data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193-09 | Denial of Service | ESM cycle between the extracted strip and its parent | mitigate | The D-24(b) `?raw` fence forbids a `WorkflowDoorSwitch` specifier in ANY import form (static / type-only / re-export / dynamic), in both the bare and `.tsx`-suffixed spelling. **Driven RED against two real plants in a real file** and restored md5-identical. |
| T-193-10 | Tampering | A "verbatim" move that quietly changed markup | mitigate | `sed`-out-of-base-blob + `diff` → IDENTICAL, PLUS two independent DOM baselines (`193-01`'s six captures and `WorkflowBuilderPage.header.test.tsx`'s nine-phase-old band literal) green with **zero edits / zero deletions**. |
| T-193-11 | Spoofing (of fact) | The `ml-auto` regression D-05 warns about, hidden behind a passing suite | mitigate | The `ml-auto` pair is asserted on BOTH `inline` values from the rendered node, and the standalone class string is compared against the substring read out of the OTHER suite's committed literal — two guards that must agree. |
| T-193-12 | Elevation of Privilege | A shared runtime value exported from a component module | mitigate | `DoorHeaderStrip.tsx` exports the component and its props type only; `grep -c "^export const"` → 0, and `react-refresh/only-export-components` is an ACTIVE eslint error that would fire. |
| T-193-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero packages installed.** The `?raw` loader is Vite's, already used in this directory. |
</threat_model>

<verification>
- `cd frontend && GSD_VITEST_MAX_WORKERS=4 node ../scripts/vitest-count-gate.cjs` → exit 0, `failed 0`.
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` → **33**.
- `cd frontend && npx eslint src/components/workflows/` → ≤ 10, **0** in the two touched source files.
- `git diff --numstat -- frontend/src/pages/WorkflowBuilderPage.header.test.tsx` → EMPTY.
- `git diff --numstat -- frontend/src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx` → 0 deletions.
</verification>

<success_criteria>
- `DoorHeaderStrip.tsx` exists, serves both header variants, and its moved span diffs IDENTICAL against the base blob.
- `WorkflowDoorSwitch.tsx` declares `doorGroup` no more and leaves no re-export shim.
- The D-24(b) cycle fence was driven RED twice and its scope verified.
- Both pre-existing baselines pass with zero edits.
- `tsc` 33, count gate exit 0, the new suite pinned from a read number.
</success_criteria>

<output>
Create `.planning/phases/193-authoring-doors-template-placement/193-03-SUMMARY.md` when done.
Record: the exact `sed`/`diff` commands and their empty output; the `git diff --numstat` for both
baseline files; both RED-plant md5 pairs and the observed failures; the gate's pin number and
`total`/`pinned total` lines; and the explicit declining of the four out-of-scope drifted pins.
State the measured growth of the `components/workflows/` subtree (before → after line counts for
`WorkflowDoorSwitch.tsx` + `DoorHeaderStrip.tsx`) **plainly, not smoothed** — 188.2 measured +67.1 %,
192 +126.2 %, 192.1 +52.7 %, and this phase inherits that habit.
</output>
