---
phase: 193-authoring-doors-template-placement
plan: 08
type: execute
wave: 4
depends_on: ["193-04", "193-05"]
files_modified:
  - frontend/src/components/workflows/doorVocabulary.ts
  - frontend/src/components/workflows/doorVocabulary.test.ts
  - frontend/src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx
  - frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx
  - frontend/src/pages/WorkflowBuilderPage.header.test.tsx
  - frontend/src/pages/WorkflowBuilderPage.describe.test.tsx
  - frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx
autonomous: true
requirements: [AUTH-01]

must_haves:
  truths:
    - "D-01 ships — the door wording is VARIANT D, 'the mix': B's door NAMES with C's two uppercase TIER labels, everything else B verbatim"
    - "D-02 holds — every string is PORTED from the regenerated BUILD-CONTRACT's column D, never re-transcribed from CONTEXT.md or from any prose"
    - "D-23 ships — `strip.labelGovern` carries variant D's door-B name, and a literal-type agreement fence makes a rename on either side a TYPECHECK ERROR rather than two words that quietly disagree"
    - "D-11 as superseded by D-23 holds — all 21 governed ids now equal column D, including the two variant D inherits unchanged"
    - "The four consumer suites that assert door copy are re-captured ONCE, deliberately, each with its reason stated — never quietly absorbed"
  artifacts:
    - path: "frontend/src/components/workflows/doorVocabulary.ts"
      provides: "All 21 ids at their column-D values, with the D-23 agreement fence"
      contains: "Build it myself"
    - path: "frontend/src/components/workflows/doorVocabulary.test.ts"
      provides: "Exact-match column-D literals spelled once in the test, codepoint assertions on the five fragile characters, and the two declared equalities"
      min_lines: 120
  key_links:
    - from: "frontend/src/components/workflows/doorVocabulary.ts"
      to: ".planning/sketches/164-telling-the-doors-apart/BUILD-CONTRACT.generated.md"
      via: "column D of the regenerated COPY table, ported verbatim"
      pattern: "BUILD-CONTRACT"
---

<objective>
Ship variant D — the words the whole phase exists for.

Purpose: **AUTH-01.** SEED-147's operator could not name the second door. Variant D answers *who does
the work* in the door names (`Draft it for me` / `Build it myself`) and states *what it costs you* in
the two tier labels (`you write one paragraph` / `you decide every setting`), replacing benefit
language with the concrete price.

⚠ **This is the first commit in the phase that intentionally changes a rendered word.** Everything
before it was a move, proved by baselines that passed unedited. Those baselines now red — that is
correct, expected, and must be recorded as a deliberate one-time re-capture with its reason, never
absorbed in silence.

⚠ **Port, do not transcribe (D-02).** The strings come out of
`.planning/sketches/164-telling-the-doors-apart/BUILD-CONTRACT.generated.md`'s column D, regenerated
by `193-04`. That file is emitted FROM the real component's rendered DOM, which is what makes it an
acceptance bar rather than a document.

Output: 21 column-D values, the D-23 agreement fence, exact-match test literals with codepoint
assertions, and four re-captured consumer suites.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<worktree_protocol>
1. `bash scripts/bootstrap-worktree.sh "$(pwd)"` — FIRST action.
2. Assert the dispatched base SHA; reset hard to it if the worktree forked elsewhere.
3. `GSD_VITEST_MAX_WORKERS=4` on every vitest run.
4. Never `rm -rf` a worktree — `bash scripts/teardown-worktree.sh <path>`.
</worktree_protocol>

<context>
@.planning/phases/193-authoring-doors-template-placement/193-CONTEXT.md
@.planning/phases/193-authoring-doors-template-placement/193-RESEARCH.md
@.planning/phases/193-authoring-doors-template-placement/193-PATTERNS.md
@.planning/phases/193-authoring-doors-template-placement/193-VALIDATION.md
@.planning/sketches/164-telling-the-doors-apart/BUILD-CONTRACT.generated.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
@frontend/src/components/workflows/doorVocabulary.ts
@frontend/src/components/workflows/runVocabulary.test.ts
</context>

<interfaces>
<!-- Column D as it stands in the contract BEFORE 193-04 regenerates it. `193-04` adds ONE row
     (`strip.labelGovern`). READ THE REGENERATED FILE — this table is here to make the diff legible,
     not to be typed from. -->

| id | D · THE PICK |
|---|---|
| `chooser.h1` | `How do you want to start?` |
| `chooser.sub` | `Both end up in the same place. You can switch between them at any time.` |
| `doorA.tier` | `you write one paragraph` ⬅ from C |
| `doorA.name` | `Draft it for me` |
| `doorA.desc` | `Describe the recurring work in plain language. The AI writes the steps, sets how strict it is, and asks you about anything it had to guess.` |
| `doorA.note` | `you can open the full editor at any point — nothing is locked in` |
| `doorB.tier` | `you decide every setting` ⬅ from C |
| `doorB.name` | `Build it myself` |
| `doorB.desc` | `Open the editor and set each step yourself — what it must cite, which checks have to pass, and which model runs each step.` |
| `doorB.note` | `what it must cite · required checks · per-step sources & model` |
| `strip.back` | `‹ Change how I start` |
| `strip.label` | `⚡ Drafting it for you` |
| `strip.labelGovern` | `Build it myself` (added by `193-04` under D-23) |
| `describe.h1` | *(inherit — `What recurring work should this automate?`)* |
| `describe.cta` | `Write the first draft` |
| `hint.frag1` | `writes the steps` |
| `hint.frag2` | `sets how strict it is` |
| `hint.frag3` | *(inherit — `asks about anything it had to guess`)* |
| `switch.prompt` | `Need to set citations, checks, or per-step sources yourself?` |
| `switch.cta` | `Build it myself ›` |
| `soul.label` | `What this will do` |

Composed hint, variant D (the connectives stay in JSX per D-12):
> You describe the goal — the AI **writes the steps**, **sets how strict it is**, and **asks about
> anything it had to guess**.

The five characters that survive an editor badly and must be asserted BY CODEPOINT (the
`runVocabulary.test.ts:80-107` `D16_WORD` pattern):

| char | appears in | codepoint |
|---|---|---|
| `‹` | `strip.back` | `0x2039` |
| `›` | `switch.cta` | `0x203A` |
| `·` | `doorB.note` | `0x00B7` |
| `—` | `doorA.note`, `doorA.desc`… | `0x2014` |
| `⚡` | `strip.label` | `0x26A1` |

The agreement-fence form to use for D-23 (`libraryVocabulary.ts`'s shipped idiom):
```ts
export const STRIP_LABEL_GOVERN: typeof DOOR_B_NAME = "Build it myself"
```

The four consumer suites that assert door copy (three of them were NOT named in CONTEXT):
`components/workflows/WorkflowDoorSwitch.test.tsx` · `pages/WorkflowBuilderPage.describe.test.tsx` ·
`pages/WorkflowBuilderPage.canvas.test.tsx` (⚠ also the known-flaky suite, ~14 % — STATE.md wave 8) ·
`pages/WorkflowBuilderPage.header.test.tsx` (⚠ holds the byte-exact `innerHTML` band literal, plus
`getByRole("button", { name: "‹ both doors" })` and a `toHaveTextContent("🔧 Author & govern")`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Port column D into `doorVocabulary.ts`, with the D-23 agreement fence (D-01 / D-02 / D-23)</name>
  <files>frontend/src/components/workflows/doorVocabulary.ts</files>
  <read_first>
    - `.planning/sketches/164-telling-the-doors-apart/BUILD-CONTRACT.generated.md` — **the regenerated file, read at execution HEAD.** Column D is the source of every value. Do not use the table in this plan's `<interfaces>` as the source; it is a legibility aid.
    - `frontend/src/components/workflows/doorVocabulary.ts` — the module as `193-05` left it (21 shipped values)
    - `frontend/src/components/workflows/library/libraryVocabulary.ts` — the **agreement fence** idiom (`export const OWN_YOURS: (typeof CHIP_WORDS)["yours"]["label"] = "Yours"`) and its stated reasoning: *"THE TYPE ANNOTATION IS THE FENCE, and it costs nothing at runtime … a rename on either side becomes a TYPECHECK ERROR rather than two words that quietly disagree."*
    - `193-CONTEXT.md` **D-01**, **D-02**, **D-11**, **D-23**
    - `193-PATTERNS.md` § `doorVocabulary.ts` (form 5 is the one to reach for on D-23) and the collision warning
  </read_first>
  <action>
Replace all 21 values in `frontend/src/components/workflows/doorVocabulary.ts` with column D from the
regenerated `BUILD-CONTRACT.generated.md`.

**Port mechanically.** Extract the cells with a script (parse the markdown table, strip the `**…**`
emphasis and the `⬅ from C` annotation) and compare the result against what you wrote — do not
hand-type 21 strings and hope. Paste the 21/21 comparison result. Two ids inherit their shipped value
unchanged (`describe.h1`, `hint.frag3`); they are still governed ids and must still be present.

**D-23's agreement fence.** Declare `STRIP_LABEL_GOVERN` with the literal type annotation
`typeof DOOR_B_NAME`, so the govern strip's label and door B's card name cannot drift. Spell the
literal out anyway — the build contract declares it as its own key. Record in the docblock that this
closes the SEED-147 failure mode in the one place it would otherwise survive: after variant D ships,
`🔧 Author & govern` would have been **the only remaining instance in the product of the exact wording
the seed reports as illegible**, sitting on the door whose card now reads *Build it myself*.

⚠ **State the `🔧` consequence rather than smoothing it.** `strip.labelGovern`'s D value is
`Build it myself`, with no glyph, while `strip.label` keeps `⚡ Drafting it for you`. That asymmetry is
a direct consequence of D-23's wording, not a discretionary choice made here. Write it into the
docblock and route it to UAT row **U6**, so the operator rules on it by looking.

**Update the module header** written in `193-05`: the values are no longer the shipped ones. Replace
that paragraph with a statement that the module now mirrors column D of the regenerated contract, and
name the two declared equalities (`STRIP_LABEL_GOVERN` ≡ `DOOR_B_NAME`; `DOOR_B_NAME` is a strict
PREFIX of `SWITCH_CTA`) so the exact-match rule's exceptions are declared rather than discovered.

⚠ Still ZERO imports. Still a `.ts` leaf.
  </action>
  <acceptance_criteria>
    - A script that parses column D out of `BUILD-CONTRACT.generated.md` and compares it to the 21 module values reports **21/21 exact matches, 0 mismatches**. Paste the output.
    - `grep -c "^export const" frontend/src/components/workflows/doorVocabulary.ts` → **21**, unchanged.
    - `grep -cE "^import |from \"" frontend/src/components/workflows/doorVocabulary.ts` → **0**.
    - `STRIP_LABEL_GOVERN` carries the annotation `typeof DOOR_B_NAME` (or the equivalent literal-type form) — verify by planting a differing literal and observing `tsc` rise from 33 to 34, then restoring to 33. Paste both counts.
    - The docblock names the `🔧` asymmetry and routes it to U6.
    - `cd frontend && npx eslint src/components/workflows/doorVocabulary.ts` → 0 / 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"</automated>
  </verify>
  <done>All 21 values equal column D, ported by script rather than by hand, and the D-23 agreement is enforced by a typecheck proved to fire.</done>
</task>

<task type="auto">
  <name>Task 2: The exact-match suite — literals spelled once in the test, codepoints asserted, exceptions declared</name>
  <files>frontend/src/components/workflows/doorVocabulary.test.ts</files>
  <read_first>
    - `frontend/src/components/workflows/runVocabulary.test.ts` (420 L) — **the analog.** Its four numbered header rules, its `D16_WORD` falsification paragraph (*"It MUST be a literal here — that is what makes the first assertion a FALSIFICATION of the table rather than a copy of it"*), its em-dash-by-codepoint assertion, and its `ALL_READINGS = Object.keys(TABLE)` derive-the-list habit.
    - `frontend/src/components/workflows/doorVocabulary.ts` — the module just re-valued
    - `frontend/src/components/workflows/library/libraryVocabulary.ts` — the **"EXACT-MATCH ASSERTIONS ONLY … a `toContain` on a fragment is the vacuous fence"** rule, verbatim
    - `.planning/sketches/164-telling-the-doors-apart/BUILD-CONTRACT.generated.md` — the acceptance bar the literals falsify against
    - `193-PATTERNS.md` § `doorVocabulary.test.ts` (the five fragile characters, and the two declared equalities)
  </read_first>
  <action>
Extend `frontend/src/components/workflows/doorVocabulary.test.ts` from `193-05`'s whole-table
properties to the full falsification suite.

**(1) Exact-match literals, spelled ONCE, here.** Declare a `const COLUMN_D: Record<string, string>`
holding the 21 column-D strings as literals **in the test file** and assert each module export equals
it with `toBe`. The literal living in the TEST is what makes the assertion a falsification of the
table rather than a copy of it. ⚠ **Never `toContain`.** `DOOR_B_NAME` is a strict prefix of
`SWITCH_CTA` and identical to `STRIP_LABEL_GOVERN`, so a containment check on that fragment is true of
all three and proves nothing — a vacuous fence by construction, in this table more than in any other.

**(2) Codepoint assertions** on the five fragile characters, in the shape of `runVocabulary.test.ts`'s
em-dash case: extract the character from the shipped value and assert `codePointAt(0)` equals
`0x2039` (`‹`), `0x203A` (`›`), `0x00B7` (`·`), `0x2014` (`—`), `0x26A1` (`⚡`). Also assert the
em-dash strings do **not** contain `--` and the `·` strings do not contain a middle-dot lookalike
(`•` U+2022 / `.` U+002E as a separator). A hyphen-minus or an en dash looks nearly identical in a
diff and in most editors.

**(3) The declared exceptions.** The pairwise-distinctness property from `193-05` now has exactly two
legitimate exceptions — pin them as **EQUALITIES**, so they are declared rather than tolerated:
`STRIP_LABEL_GOVERN === DOOR_B_NAME`, and `SWITCH_CTA === DOOR_B_NAME + " ›"`. Every other pair
remains distinct, and no other id may be a prefix of another.

**(4) Whole-table properties, never row assertions** (`runVocabulary.test.ts` rule 4): derive the id
list from the module's own exports so a 22nd id inherits every guard without anyone remembering to
extend a list. Assert `Object.keys(COLUMN_D)` and the module's export set are the SAME set — so a
value added to one and not the other is a failure.

**(5) The contract-agreement check.** Read column D out of
`.planning/sketches/164-telling-the-doors-apart/BUILD-CONTRACT.generated.md` at test time and assert
it equals `COLUMN_D`. That is the mechanical statement of D-02: the acceptance bar and the module
cannot drift, and a copy change is a one-line diff in one file.

Drive the suite RED: change one module value by a single character (e.g. an en dash for the em dash in
`doorA.note`) and confirm BOTH the exact-match case and the codepoint case fail; restore
md5-identical.
  </action>
  <acceptance_criteria>
    - `COLUMN_D` holds 21 literal strings in the test file, and every module export is asserted with `toBe` against it.
    - `grep -c "toContain" frontend/src/components/workflows/doorVocabulary.test.ts` → **0**.
    - Five codepoint assertions exist, one per fragile character, each asserting an exact `codePointAt` value.
    - The two declared equalities are asserted, and the distinctness property excludes exactly those two pairs.
    - The contract-agreement case reads `BUILD-CONTRACT.generated.md` at test time and compares against `COLUMN_D`.
    - RED-first proof recorded: a one-character change (en dash for em dash) reddened both the exact-match and the codepoint case; source restored md5-identical. Paste the md5 pair and both failure lines.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/doorVocabulary.test.ts` → all pass, 0 failed.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/doorVocabulary.test.ts</automated>
  </verify>
  <done>The 21 words are falsified against literals spelled once in the test and against the generated contract itself, with the five fragile characters pinned by codepoint and both legitimate collisions declared.</done>
</task>

<task type="auto">
  <name>Task 3: The one-time re-capture of the four consumer suites, each with its reason stated</name>
  <files>frontend/src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx, frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx, frontend/src/pages/WorkflowBuilderPage.header.test.tsx, frontend/src/pages/WorkflowBuilderPage.describe.test.tsx, frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx` — the six captures from `193-01` and their `CAPTURE_SHA` rationale block
    - `frontend/src/pages/WorkflowBuilderPage.header.test.tsx` — the byte-exact band literal, its two consumption sites, the `getByRole("button", { name: "‹ both doors" })` query and the `toHaveTextContent("🔧 Author & govern")` assertion. **This literal predates the phase by nine phases; it has already done its job.**
    - `frontend/src/pages/WorkflowBuilderPage.describe.test.tsx` and `.canvas.test.tsx` — every door-copy assertion in them (find with a grep for the shipped strings, not by memory)
    - `frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx` — its own copy assertions and the D-24(a) fence added in `193-05`
    - `193-VALIDATION.md` § "The landmine CONTEXT.md's canonical_refs never named"
    - STATE.md wave 8 item 2 — `WorkflowBuilderPage.canvas.test.tsx`'s measured ~14 % flake, so a red there is diagnosed before it is attributed to this plan
  </read_first>
  <action>
Update every assertion in the four consumer suites that names a shipped door string, replacing the
expectation with the column-D value. **Import the value from `doorVocabulary` wherever the suite's
convention allows it**; where a suite deliberately spells a literal (the header suite's byte-exact
band), re-capture from the rendered tree rather than hand-editing the string.

Record the re-capture, in the file, beside each changed literal:
- **WHY it is legitimate now:** `193-03` and `193-05` both passed with these literals GREEN and
  UNEDITED (`git diff --numstat` EMPTY), which is the proof the two moves were verbatim. This plan is
  the first that intentionally changes a rendered word, so the literals have already discharged their
  purpose.
- **WHAT changed:** the words only. Class lists, testids, structure and the `ml-auto` conditional are
  unchanged in this plan — assert that by diffing the old and new band literals and confirming the
  ONLY differences are text nodes. Paste the diff.
- ⚠ **This is re-capture ONE of TWO.** `193-09`'s D-04/D-22 restack will red the same band literal
  again, on structure rather than words. Both are legitimate; both must be stated. Say so here so the
  next author is not surprised.

⚠ **`getByRole("button", { name: "‹ both doors" })` becomes `{ name: "‹ Change how I start" }`** — the
accessible name changed with the copy. Find every such query by grep, not by memory.
⚠ **`toHaveTextContent("🔧 Author & govern")` becomes the D-23 value** — and the `🔧` glyph is gone;
that is Task 1's stated consequence, and the assertion must reflect the shipped reality rather than be
softened into a `toContain`.

⚠ **If `WorkflowBuilderPage.canvas.test.tsx` reds on something that is NOT a door string**, check
STATE.md wave 8 item 2 first: that suite has a measured ~14 % flake in the
`184-11 … rails key (D-14)` case, seen in both the main tree and a worktree, and it is a Phase-184
suite. Do not attribute it to 193 without re-running.

Do not raise any count-gate pin here; `193-11` owns the sweep.
  </action>
  <acceptance_criteria>
    - `grep -rc "both doors\|Describe & run\|Author & govern\|Draft the workflow\|How do you want to build this\|This workflow's soul" frontend/src --include=*.test.tsx --include=*.test.ts` → **0** across all four suites (allowing prose in the re-capture rationale comments, which must therefore not spell the retired strings — assemble them from parts if a comment must reference one).
    - Each of the four suites carries a dated re-capture note naming this plan and stating that the change is words-only.
    - The band-literal diff is pasted and shows text-node differences ONLY — no class, testid or structural difference.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx src/components/workflows/WorkflowDoorSwitch.test.tsx src/components/workflows/DoorHeaderStrip.test.tsx src/pages/WorkflowBuilderPage.header.test.tsx src/pages/WorkflowBuilderPage.describe.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx` → all pass, 0 failed.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 node ../scripts/vitest-count-gate.cjs` → exit 0, **no `[count-decrease]`** — the re-capture changed expectations, it deleted no case.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` → **33**.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx src/components/workflows/WorkflowDoorSwitch.test.tsx src/pages/WorkflowBuilderPage.header.test.tsx src/pages/WorkflowBuilderPage.describe.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx</automated>
  </verify>
  <done>The four consumer suites speak variant D, the re-capture is dated and reasoned in each file, and the band-literal delta is proved to be words only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated contract → vocabulary module | The acceptance bar. A value that differs here from column D has forked the bar invisibly. |
| vocabulary module → four consumer suites | The suites are what stop a copy change becoming a structural one by accident. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193-31 | Tampering | A string typed from memory instead of ported (D-02) | mitigate | The values are extracted from `BUILD-CONTRACT.generated.md` by script and compared 21/21, AND a test case re-reads the contract at test time and asserts equality. A drift between bar and module is a red test, not a review question. |
| T-193-32 | Spoofing (of fact) | A vacuous fence passing on a shared prefix | mitigate | `toContain` is banned in the suite (`grep -c` → 0); every word is asserted with `toBe`; the two legitimate collisions are pinned as EQUALITIES so they are declared rather than tolerated. |
| T-193-33 | Tampering | A character that survives an editor badly (en dash for em dash, `•` for `·`) | mitigate | Five codepoint assertions plus the `--` and lookalike negatives, driven RED against a real one-character change. |
| T-193-34 | Repudiation | A re-capture that hides a structural change behind a copy change | mitigate | The band-literal diff is inspected and published; only text-node differences are permitted, and any class/testid/structure difference stops the plan. The count gate's `[count-decrease]` covers deletion. |
| T-193-35 | Denial of Service | Attributing the `WorkflowBuilderPage.canvas.test.tsx` ~14 % flake to this plan | accept | Named in advance with its evidence (STATE.md wave 8 item 2, a Phase-184 suite, seen in main tree and worktree). A red there is re-run and diagnosed before attribution. Not fixed here — that would fold unrelated drift into a commit that did not cause it. |
| T-193-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero packages installed.** The contract parser is a few lines of Node against a markdown table. |
</threat_model>

<verification>
- `cd frontend && GSD_VITEST_MAX_WORKERS=4 node ../scripts/vitest-count-gate.cjs` → exit 0, `failed 0`, no `[count-decrease]`.
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` → **33**.
- `cd frontend && npx eslint src/components/workflows/` → ≤ 10, **0** in `doorVocabulary.ts`.
- The 21/21 contract-comparison script output is pasted in the SUMMARY.
</verification>

<success_criteria>
- All 21 governed strings equal column D of the regenerated contract, verified by script AND by a test that re-reads the contract.
- `STRIP_LABEL_GOVERN` is type-fenced against `DOOR_B_NAME`, proved by a `tsc` 33 → 34 → 33 plant.
- The five fragile characters are pinned by codepoint.
- The four consumer suites speak variant D, with a dated, reasoned re-capture note in each.
- `tsc` 33, count gate exit 0, no case deleted.
</success_criteria>

<output>
Create `.planning/phases/193-authoring-doors-template-placement/193-08-SUMMARY.md` when done.
Record: the 21/21 port-comparison output; the `tsc` 33 → 34 → 33 agreement-fence proof; the RED-plant
md5 pair and failure lines; the band-literal diff proving words-only; the explicit statement that this
is **re-capture 1 of 2** and that `193-09` owns the second; and the `🔧` asymmetry routed to UAT
row **U6**.
</output>
