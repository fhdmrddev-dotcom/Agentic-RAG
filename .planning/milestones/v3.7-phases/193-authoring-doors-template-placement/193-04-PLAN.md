---
phase: 193-authoring-doors-template-placement
plan: 04
type: execute
wave: 2
depends_on: ["193-01"]
files_modified:
  - .planning/sketches/164-telling-the-doors-apart/build.cjs
  - .planning/sketches/164-telling-the-doors-apart/emit.test.tsx.src
  - .planning/sketches/164-telling-the-doors-apart/dom.generated.json
  - .planning/sketches/164-telling-the-doors-apart/BUILD-CONTRACT.generated.md
  - .planning/sketches/164-telling-the-doors-apart/body.generated.html
  - .planning/sketches/164-telling-the-doors-apart/index.html
  - .planning/sketches/164-telling-the-doors-apart/README.md
autonomous: true
requirements: [AUTH-01]

must_haves:
  truths:
    - "D-23 is executed through the GENERATOR, never by typing a string into JSX — `strip.labelGovern` becomes the 21st governed COPY id by being added to `build.cjs` and regenerated, which is what preserves D-02's derived-never-re-typed property"
    - "The govern door's DOM is captured for the first time, so `strip.labelGovern`'s substitution is verified against a real rendered node rather than declared and unchecked"
    - "D-02 holds: `D_FROM_C` still contains exactly the two tier ids, so the new id takes its B value like every other id and variant D remains derived rather than hand-written"
    - "The regenerated substitution audit's numbers are READ OUT of the regenerated contract, never predicted"
    - "The three surfaces the sketch cannot accept — the D-03/D-04 govern restack, the D-22 describe restack, and both AUTH-03 surfaces (D-13/D-15/D-17) — are named in the sketch's own README rather than left for a later reader to discover"
  artifacts:
    - path: ".planning/sketches/164-telling-the-doors-apart/BUILD-CONTRACT.generated.md"
      provides: "The regenerated COPY table with 21 substantive rows including `strip.labelGovern`, whose column D reads `Build it myself`"
      contains: "strip.labelGovern"
    - path: ".planning/sketches/164-telling-the-doors-apart/dom.generated.json"
      provides: "Four keys — chooser, describe, govern, runModal — the govern door captured for the first time"
      contains: "govern"
  key_links:
    - from: ".planning/sketches/164-telling-the-doors-apart/build.cjs"
      to: ".planning/sketches/164-telling-the-doors-apart/BUILD-CONTRACT.generated.md"
      via: "node build.cjs regenerating the contract from the COPY table and the captured DOM"
      pattern: "strip.labelGovern"
---

<objective>
Add the 21st governed COPY id — `strip.labelGovern` — to the sketch's generator and regenerate the
acceptance bar.

Purpose: **`🔧 Author & govern` (the govern strip's current-door label) was never in the COPY table
and `dom.generated.json` never captured it.** After variant D ships, it would become **the one
surviving instance in the whole product of the exact wording SEED-147 reports as illegible**, sitting
on the door whose card now reads *Build it myself* — the inverse of the AUTH-01 failure this phase
exists to fix. D-23 closes that, and it closes it **through `build.cjs`** so D-02's
derived-never-re-typed property survives: the planner and the executor both port a generated cell
rather than typing a string.

⚠ **This plan touches no application source at all.** `193-08` consumes its output.

⚠ **`depends_on: ["193-01"]` is a SCHEDULING dependency, not a content one — stated so it is not
mistaken for the latter.** This plan needs nothing `193-01` produces. It is held to wave 2 because it
runs the sketch emitter under vitest, and `GSD_VITEST_MAX_WORKERS=4` is calibrated for **two**
concurrent vitest runs (at three, `192-05` measured the count gate flipping `failed 6 → 0 → 1 → 3` on
a single commit). Wave 1 already dispatches two.

Output: a regenerated `BUILD-CONTRACT.generated.md` with 21 substantive rows, a `dom.generated.json`
that includes the govern door, and a re-run substitution audit whose numbers are read rather than
predicted.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<worktree_protocol>
1. `bash scripts/bootstrap-worktree.sh "$(pwd)"` — FIRST action. This plan runs vitest once (the
   emitter), so the venv/node_modules junctions are required.
2. Assert the dispatched base SHA; reset hard to it if the worktree forked elsewhere.
3. `GSD_VITEST_MAX_WORKERS=4` on the emitter run.
4. Never `rm -rf` a worktree — `bash scripts/teardown-worktree.sh <path>`.
</worktree_protocol>

<context>
@.planning/phases/193-authoring-doors-template-placement/193-CONTEXT.md
@.planning/phases/193-authoring-doors-template-placement/193-RESEARCH.md
@.planning/phases/193-authoring-doors-template-placement/193-PATTERNS.md
@.planning/sketches/164-telling-the-doors-apart/README.md
@.planning/sketches/164-telling-the-doors-apart/build.cjs
@.planning/sketches/164-telling-the-doors-apart/emit.test.tsx.src
@.planning/sketches/164-telling-the-doors-apart/BUILD-CONTRACT.generated.md
</context>

<interfaces>
<!-- Read at HEAD f2087341. Re-derive by content. -->

The `COPY[]` row shape in `build.cjs` (the neighbours `strip.labelGovern` sits between):
```js
{ id: "strip.back",  where: 'testid="both-doors" — the return control, BOTH open doors', shipped: "‹ both doors",     B: "‹ Change how I start", C: "‹ Back to both options" },
{ id: "strip.label", where: "describe-door header — the current-door label <span>",       shipped: "⚡ Describe & run", B: "⚡ Drafting it for you", C: "⚡ Describing it" },
```
⚠ `shipped` is what `applyVariant` SUBSTITUTES ON and is written with a PLAIN `&` — the emitter
escapes at substitution time (`const esc = (s) => s.replace(/&/g, "&amp;")`).

The derivation D-02 protects — **`strip.labelGovern` must NOT be added to `D_FROM_C`**:
```js
const D_FROM_C = new Set(["doorA.tier", "doorB.tier"])
for (const c of COPY) { const src = D_FROM_C.has(c.id) ? c.C : c.B; if (src) c.D = src }
```

The longest-first substitution guard, which is why D-23 is safe:
```js
const subs = COPY.filter((c) => c.shipped && c[key] && c[key] !== c.shipped)
  .sort((a, b) => b.shipped.length - a.shipped.length)
```
`🔧 Author & govern` is LONGER than `Author & govern`, so it sorts first and cannot be clobbered by
`doorB.name`'s substitution.

⚠ **`COPY[]` holds 21 ENTRIES today, not 20.** The 21st, `describe.hint`, carries `shipped: null` and
no B/C/D — a composite descriptor, not a substitutable string — and is filtered out of the generated
table (hence 20 table rows). **A planner or executor that ports `COPY.length` ports one entry too
many.** D-23 takes the SUBSTANTIVE count 20 → 21 and the array to 22 entries.

The emitter's dump keys today — `dom.generated.json` has exactly three:
`["chooser", "describe", "runModal"]`. The emitter's mock seam already mounts the real
`WorkflowBuilderPage`, so the govern door needs no additional mocks.

The regeneration chain, verbatim from `README.md`:
```bash
cp .planning/sketches/164-telling-the-doors-apart/emit.test.tsx.src \
   frontend/src/components/workflows/__emit164.test.tsx
cd frontend && npx vitest run src/components/workflows/__emit164.test.tsx && cd ..
rm frontend/src/components/workflows/__emit164.test.tsx
node .planning/sketches/164-telling-the-doors-apart/build.cjs
node .planning/sketches/164-telling-the-doors-apart/assemble.cjs
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Capture the govern door's real DOM for the first time</name>
  <files>.planning/sketches/164-telling-the-doors-apart/emit.test.tsx.src, .planning/sketches/164-telling-the-doors-apart/dom.generated.json</files>
  <read_first>
    - `.planning/sketches/164-telling-the-doors-apart/emit.test.tsx.src` (121 L — read whole: the `vi.hoisted` mock block, the `strictDef` fixture, the three existing dump blocks, and the `OUT` path resolution)
    - `.planning/sketches/164-telling-the-doors-apart/README.md` § "Reproduce the whole chain from a clean checkout" — the exact five commands
    - `frontend/src/components/workflows/WorkflowDoorSwitch.tsx` — the govern branch, so the render call passes the props that actually reach it (`initialDoor="govern"`)
    - `193-CONTEXT.md` **D-23**; `193-RESEARCH.md` § B.4 (the 21st string, and why the audit cannot verify it without this capture)
  </read_first>
  <action>
Add a fourth dump block to `.planning/sketches/164-telling-the-doors-apart/emit.test.tsx.src`, keyed
`govern`, rendering
`<WorkflowDoorSwitch def={strictDef} initialDoor="govern" onDescribeDraft={vi.fn()} />` and dumping
`container.innerHTML` — the same ten-line shape as the three existing blocks. Place it after the
`describe` block and before the `runModal` block. Reuse the existing `vi.hoisted` mock set unchanged;
it already covers what the govern door needs, because that door mounts the real
`WorkflowBuilderPage`.

⚠ **Capture the STANDALONE (non-`inline`) govern state.** That is the flag-OFF path, the one whose
bytes `WorkflowBuilderPage.header.test.tsx` already pins, and the one where the current-door label is
rendered inside this shell's own band.

Then run the regeneration chain exactly as `README.md` states, including the `rm` that keeps the app
tree clean. Record the emitter's `EMITTED …` console line verbatim (it prints each key's byte length),
so the new key's presence and size are evidence rather than a claim.

⚠ **The `__emit164.test.tsx` copy must not survive the commit.** `git status` must show no such file.

Update the `README.md` mechanism-table row that describes what the emitter captures, so the doc and
the artifact agree in the same commit.
  </action>
  <acceptance_criteria>
    - `node -e "console.log(Object.keys(require('./.planning/sketches/164-telling-the-doors-apart/dom.generated.json')).join(','))"` → prints exactly `chooser,describe,govern,runModal`.
    - The `govern` dump contains the substrings `both-doors`, `judge-locked`, `ml-auto` and `🔧 Author &amp; govern` — assert each with `grep -c` against the JSON file, all → ≥ 1.
    - `git status --porcelain frontend/src/components/workflows/__emit164.test.tsx` → EMPTY (the throwaway was removed).
    - `git diff --stat` for this task lists no path under `frontend/` and no path outside `.planning/sketches/164-telling-the-doors-apart/`.
    - The emitter's `EMITTED chooser=…B describe=…B govern=…B runModal=…B` line is pasted into the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const k=Object.keys(require('./.planning/sketches/164-telling-the-doors-apart/dom.generated.json'));if(!k.includes('govern'))process.exit(1);console.log(k.join(','))"</automated>
  </verify>
  <done>The govern door's real rendered DOM is captured, so the 21st id's substitution can be verified against a node that exists rather than declared and unchecked.</done>
</task>

<task type="auto">
  <name>Task 2: Add `strip.labelGovern` to the COPY table and regenerate (D-23)</name>
  <files>.planning/sketches/164-telling-the-doors-apart/build.cjs, .planning/sketches/164-telling-the-doors-apart/BUILD-CONTRACT.generated.md, .planning/sketches/164-telling-the-doors-apart/body.generated.html, .planning/sketches/164-telling-the-doors-apart/index.html</files>
  <read_first>
    - `.planning/sketches/164-telling-the-doors-apart/build.cjs` — the `COPY[]` array header comment, the `strip.back` / `strip.label` rows, the `describe.hint` composite entry, `D_FROM_C`, `esc`, and `applyVariant`'s longest-first sort with its `applied.push({id, ok})` audit
    - `frontend/src/components/workflows/WorkflowDoorSwitch.tsx` — the exact rendered text of the govern strip's label `<span>` (`🔧 Author &amp; govern` in source; the PLAIN form goes in `shipped`)
    - `.planning/sketches/164-telling-the-doors-apart/BUILD-CONTRACT.generated.md` — the current table, so the diff is legible
    - `193-CONTEXT.md` **D-23** (verbatim — it names the string), **D-02**, **D-11**
    - `193-PATTERNS.md` § `build.cjs` (the row shape, the `esc` rule, the longest-first guard, correction **P-5** on the entry count, and the "read the new numbers out of the regenerated contract, never predict them" rule)
  </read_first>
  <action>
Insert ONE row into `COPY[]` in `build.cjs`, immediately after the `strip.back` row and before
`strip.label`:

- `id`: `strip.labelGovern`
- `where`: `govern-door header — the current-door label <span>`
- `shipped`: `🔧 Author & govern` — the PLAIN-`&` form, because `applyVariant` escapes at
  substitution time
- `B`: `Build it myself` — **D-23 fixes this string; it is variant D's door name for door B.**
- `C`: omit. Leaving it absent makes the contract render *(inherit)* for column C, which is honest —
  the operator's ruling was about the shipped→D transition, and inventing a C value would be exactly
  the re-typing D-02 forbids.

⚠ **Do NOT add `strip.labelGovern` to `D_FROM_C`.** It takes its B value like every other id; that is
what keeps variant D derived.

⚠ **State the consequence rather than smoothing it:** under D-23 the govern strip label loses its
`🔧` glyph while the describe strip keeps `⚡` (`⚡ Drafting it for you`). That asymmetry is a direct
consequence of D-23's wording — *"its string becomes variant D's door name — `Build it myself`"* —
and is NOT a discretionary choice made here. Record it in the SUMMARY and flag it as an explicit
observation point for UAT row **U6**, so the operator rules on it by looking.

Then re-run `node build.cjs && node assemble.cjs` and READ the regenerated artifacts:
- the COPY table must now render **21** substantive rows. ⚠ The array itself holds **22** entries
  after this change, because `describe.hint` carries `shipped: null` and is filtered out — do not
  confuse `COPY.length` with the table's row count.
- the substitution audit's new per-variant numbers must be READ OUT of
  `BUILD-CONTRACT.generated.md`, never predicted. Quote them verbatim in the SUMMARY beside the
  previous values (B 18/0 · C 19/0 · D 18/0) so the delta is visible.
- ⚠ **If any variant reports a MISS, stop and report it** — a miss means the `shipped` string does
  not appear in the captured DOM, which would mean Task 1's capture or the `shipped` spelling is
  wrong. Do not "fix" it by editing the generated file; generated files are never hand-edited.
  </action>
  <acceptance_criteria>
    - `grep -c 'id: "strip.labelGovern"' .planning/sketches/164-telling-the-doors-apart/build.cjs` → 1.
    - `grep -c 'D_FROM_C = new Set(\["doorA.tier", "doorB.tier"\])' .planning/sketches/164-telling-the-doors-apart/build.cjs` → 1, unchanged — the new id is NOT in the derived-from-C set.
    - `grep -c '^| `' .planning/sketches/164-telling-the-doors-apart/BUILD-CONTRACT.generated.md` → **21** table rows.
    - The `strip.labelGovern` row's column D cell reads exactly `**Build it myself**`.
    - The substitution audit block reports **zero misses** for every variant. Paste the three lines verbatim.
    - `git status --porcelain` shows no modified file outside `.planning/sketches/164-telling-the-doors-apart/`.
    - No generated file was hand-edited: re-running `node build.cjs && node assemble.cjs` produces no further diff (`git diff --stat` empty after a second run).
  </acceptance_criteria>
  <verify>
    <automated>node .planning/sketches/164-telling-the-doors-apart/build.cjs && node .planning/sketches/164-telling-the-doors-apart/assemble.cjs && grep -c "strip.labelGovern" .planning/sketches/164-telling-the-doors-apart/BUILD-CONTRACT.generated.md</automated>
  </verify>
  <done>The contract governs 21 ids; `strip.labelGovern`'s column D reads `Build it myself`; the audit re-verified against the real govern DOM with zero misses; and variant D is still derived, not typed.</done>
</task>

<task type="auto">
  <name>Task 3: Record the regeneration in the sketch README so the artifact and its story agree</name>
  <files>.planning/sketches/164-telling-the-doors-apart/README.md</files>
  <read_first>
    - `.planning/sketches/164-telling-the-doors-apart/README.md` — the mechanism table, the "Three measured corrections to SEED-147" section, and the open-decision section that says the header-strip stacking is *"not addressed"*
    - `.planning/sketches/164-telling-the-doors-apart/BUILD-CONTRACT.generated.md` — the regenerated artifact
    - `193-CONTEXT.md` **D-23**, **D-03**, **D-04**, **D-22**, **D-13**, **D-15**, **D-17**
  </read_first>
  <action>
Append a short, dated section to `.planning/sketches/164-telling-the-doors-apart/README.md` recording
what changed and why, in the project's correct-in-the-open voice:

- **The 21st id.** `strip.labelGovern` did not exist when the operator picked variant D. It was found
  at plan time (`193-RESEARCH.md` § B.4): the govern strip's `🔧 Author & govern` label was never in
  the COPY table and the emitter never rendered the govern door, so `dom.generated.json` never held
  it. Under D-23 it is now governed, its B/D value is `Build it myself`, and the audit verifies it
  against a real captured node.
- **The emitter now dumps four surfaces, not three.** Name the new key and its byte length.
- **The corrected audit numbers**, quoted from the regenerated contract beside the previous ones.
- **The `🔧` asymmetry**, stated plainly (see Task 2) and routed to UAT row **U6**.
- **What the sketch still does NOT cover**, restated so a later reader does not over-trust it: the
  D-03/D-04 govern restack and the D-22 describe restack have **no mockup on either band**, and the
  AUTH-03 template panel is a **PROPOSAL** — `card.templateMark` (D-13/D-15), `run.templateLabel` and
  `run.templateAbsent` (D-17) have no visual acceptance bar of any kind. That is the SEED-155 exposure
  in its "draws none" form rather than its "draws a wrong one" form, and it is why UAT rows
  U3 / U3b / U4 / U5 must be driven hardest.

Do not restate the operator's variant-D decision; it is already recorded and unchanged.
  </action>
  <acceptance_criteria>
    - The README contains a dated section naming `strip.labelGovern`, the new `govern` dump key, the regenerated audit numbers, the `🔧` asymmetry, and the un-mockup'd surfaces.
    - `grep -c "strip.labelGovern" .planning/sketches/164-telling-the-doors-apart/README.md` → ≥ 1.
    - `grep -c "U6" .planning/sketches/164-telling-the-doors-apart/README.md` → ≥ 1 (the asymmetry is routed, not merely mentioned).
    - `grep -c "U3b\|U4\|U5" .planning/sketches/164-telling-the-doors-apart/README.md` → ≥ 1 (the un-mockup'd surfaces are routed too).
    - No generated file (`*.generated.*`, `index.html`) is edited by this task.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "strip.labelGovern" .planning/sketches/164-telling-the-doors-apart/README.md</automated>
  </verify>
  <done>The sketch's own story matches its regenerated artifacts, and the surfaces it cannot accept are named rather than implied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real component DOM → generated contract | The whole anti-drift mechanism: the contract is emitted FROM the build, so a string the component cannot render cannot enter the table silently. |
| generated artifact → downstream plan | `193-08` ports column D from `BUILD-CONTRACT.generated.md`. A hand-edited generated file would fork the acceptance bar invisibly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193-13 | Tampering | A hand-edited generated file forking the acceptance bar | mitigate | Acceptance criterion: re-running `node build.cjs && node assemble.cjs` after the commit must produce an EMPTY `git diff --stat`. A hand edit fails that check by construction. |
| T-193-14 | Spoofing (of fact) | An audit reporting `matched` for a string no rendered node contains | mitigate | Task 1 captures the govern DOM BEFORE the row is added, so `applyVariant` verifies the substitution against a real node; a miss is surfaced and stops the plan rather than being smoothed. |
| T-193-15 | Tampering | The throwaway emitter surviving in the app tree | mitigate | `git status --porcelain frontend/src/components/workflows/__emit164.test.tsx` must be EMPTY; the `rm` is part of the README chain and is an acceptance criterion. |
| T-193-16 | Elevation of Privilege | The emitter running with real `@/lib/api` bindings | accept | The emitter `vi.mock`s the entire `@/lib/api` surface it touches (`generateWorkflow`, `createWorkflowDraft`, `updateWorkflowDraft`, `listFolders`, `listSkills`); it runs under jsdom with no network. Accepted because the mock set is shipped and unchanged by this plan. |
| T-193-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero packages installed.** `build.cjs` and `assemble.cjs` are plain Node with no dependencies beyond the repo's existing tailwind invocation. |
</threat_model>

<verification>
- `node .planning/sketches/164-telling-the-doors-apart/build.cjs && node .planning/sketches/164-telling-the-doors-apart/assemble.cjs` → runs clean, and a SECOND run produces an empty `git diff --stat`.
- `dom.generated.json` has four keys including `govern`.
- The regenerated contract renders 21 table rows and reports zero substitution misses for every variant.
- `git status --porcelain` lists nothing under `frontend/`.
</verification>

<success_criteria>
- `strip.labelGovern` is a governed COPY id whose column D reads `Build it myself`, produced by the generator rather than typed.
- The govern door's DOM is captured, so the new id's substitution is verified rather than declared.
- `D_FROM_C` is unchanged — variant D is still derived.
- The `🔧` asymmetry and the un-mockup'd surfaces are recorded and routed to UAT.
</success_criteria>

<output>
Create `.planning/phases/193-authoring-doors-template-placement/193-04-SUMMARY.md` when done.
Record: the `EMITTED …` line verbatim, the three audit lines before and after, the table row count AND
the array entry count (they differ — correction P-5), the exact column-D cell for `strip.labelGovern`,
and the `🔧` asymmetry with its U6 routing.
</output>
