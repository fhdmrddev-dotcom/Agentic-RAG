---
phase: 193-authoring-doors-template-placement
plan: 09
type: execute
wave: 5
depends_on: ["193-08"]
files_modified:
  - frontend/src/components/workflows/DoorHeaderStrip.tsx
  - frontend/src/components/workflows/DoorHeaderStrip.test.tsx
  - frontend/src/components/workflows/WorkflowDoorSwitch.tsx
  - frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx
  - frontend/src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx
  - frontend/src/pages/WorkflowBuilderPage.header.test.tsx
autonomous: true
requirements: [AUTH-01]

must_haves:
  truths:
    - "D-03 holds — the header-strip restack ships; it is the half of SEED-147 suspect #2 that copy provably cannot reach"
    - "D-04 holds — the return control keeps its place but drops its border/box, becomes plain muted text, and gains a divider between it and the door label; the judge badge keeps its far-edge position UNCHANGED"
    - "D-22 holds — the describe band's return control is demoted TOO, with NO divider on that side, so the two doors do not disagree about their own escape hatch"
    - "D-05 survives the restack — one component still serves both header variants and the `ml-auto` conditional is untouched"
    - "D-06 stays rejected — the return control is still in the strip, not in `headerLead`, so the standalone band keeps a way back"
  artifacts:
    - path: "frontend/src/components/workflows/DoorHeaderStrip.tsx"
      provides: "The demoted return control, the borrowed divider, and the untouched judge badge"
      contains: "aria-hidden"
    - path: "frontend/src/components/workflows/DoorHeaderStrip.test.tsx"
      provides: "D-04's shape asserted by CHILD ORDER — return control, divider, door label, judge badge last — on both `inline` values"
  key_links:
    - from: "frontend/src/components/workflows/DoorHeaderStrip.tsx"
      to: "the shipped divider idiom in CanvasToolbar.tsx"
      via: "a decorative aria-hidden 1px rule, borrowed rather than invented"
      pattern: "w-px bg-border"
---

<objective>
Break the peer reading. Today the return control, the current-door label and the `🔒 judge always-on`
badge render as three visual peers in one strip — which is precisely why SEED-147's operator could not
name "the other one". Copy alone cannot reach that; **D-03 puts the restack in scope**.

Purpose: **AUTH-01's structural half.** D-04's shape:

```
‹ back  │  <current door>            [🔒 judge always-on]
└─ no border, muted   └─ primary   └─ far edge, unchanged
```

D-22 extends the demotion — and only the demotion — to the describe door's band, because demoting one
escape hatch and not the other manufactures a new inconsistency one click apart.

⚠ **The sketch draws NO mockup of this, on either band.** The acceptance bar is D-04 / D-05 / D-06 /
D-22 plus UAT rows **U3** and **U3b**, driven by looking, at both `inline` values. This is where
sketch→build drift survives in this phase; it is the part to drive hardest.

⚠ **This is re-capture 2 of 2.** `193-08` reddened the header band literal on WORDS; this plan reds it
on STRUCTURE. Both are legitimate; both must be stated, never quietly absorbed.

Output: the demoted controls, the divider, child-order assertions, and the second recorded re-capture.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<worktree_protocol>
1. `bash scripts/bootstrap-worktree.sh "$(pwd)"` — FIRST action.
2. Assert the dispatched base SHA; reset hard to it if the worktree forked elsewhere.
3. `GSD_VITEST_MAX_WORKERS=4` on every vitest run.
4. Never `rm -rf` a worktree — `bash scripts/teardown-worktree.sh <path>`.
</worktree_protocol>

<context>
@.planning/phases/193-authoring-doors-template-placement/193-CONTEXT.md
@.planning/phases/193-authoring-doors-template-placement/193-RESEARCH.md
@.planning/phases/193-authoring-doors-template-placement/193-PATTERNS.md
@.planning/phases/193-authoring-doors-template-placement/193-VALIDATION.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
@frontend/src/components/workflows/DoorHeaderStrip.tsx
@frontend/src/components/workflows/WorkflowDoorSwitch.tsx
@frontend/src/components/workflows/CanvasToolbar.tsx
</context>

<interfaces>
<!-- Measured at f2087341, PRE-extraction. RE-DERIVE BY CONTENT at execution HEAD: 193-03 moved the
     govern band into DoorHeaderStrip.tsx and 193-05 / 193-08 re-valued the strings. -->

The shipped return-control class list, identical on BOTH bands — this is what makes them peers:
```
rounded-md border border-border px-2.5 py-1 text-[13px] text-muted-foreground hover:text-foreground
```
`rounded-md border border-border` is the box D-04 drops.

The door-label span, which stays PRIMARY:
```tsx
<span className="text-[13px] font-medium text-foreground">{STRIP_LABEL_GOVERN}</span>
```

The judge badge — DO NOT TOUCH IT, including the `ml-auto` conditional and the `title` attribute:
```tsx
<span data-testid="judge-locked" title="The llm_judge_rubric …"
      className={`${inline ? "" : "ml-auto "}inline-flex items-center gap-1 rounded-full border …`}>
  <span aria-hidden="true">🔒</span> judge always-on
</span>
```

The describe band (D-22's subject) in `WorkflowDoorSwitch.tsx` — an identically-classed return control
and the current-door label, **and no judge badge**:
```tsx
<div className="flex items-center gap-3 border-b border-border px-4 py-2">
  {inline && headerLead}
  <button type="button" data-testid="both-doors" onClick={goBoth} className="rounded-md border …">{STRIP_BACK}</button>
  <span className="text-[13px] font-medium text-foreground">{STRIP_LABEL}</span>
</div>
```

**The divider — do not invent one.** The shipped idiom, `CanvasToolbar.tsx`:
```tsx
<span aria-hidden="true" className="mx-0.5 h-4 w-px bg-border" />
```
Already in the token vocabulary, already `aria-hidden`, already used in a toolbar strip.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Demote the return control and add the divider in `DoorHeaderStrip.tsx` (D-03 / D-04)</name>
  <files>frontend/src/components/workflows/DoorHeaderStrip.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/DoorHeaderStrip.tsx` — the whole file as `193-03` / `193-05` / `193-08` left it: the moved docblock, the return control, the door label, the judge badge and its `ml-auto` conditional
    - `frontend/src/components/workflows/CanvasToolbar.tsx` — **the shipped divider idiom.** Do not build a new border utility.
    - `.claude/skills/sketch-findings-agentic-rag/SKILL.md` — the workflow-surface findings; the demoted control must stay inside the shipped token vocabulary
    - `193-CONTEXT.md` **D-03**, **D-04** (its ASCII is the acceptance bar — the sketch draws none), **D-05**, **D-06**, and the "Claude's Discretion" entry that scopes the Tailwind choices
    - `193-RESEARCH.md` § B.1 (the three bands) and § "Don't Hand-Roll" (the divider row)
  </read_first>
  <action>
Restack the govern strip in `frontend/src/components/workflows/DoorHeaderStrip.tsx` per D-04.

**The return control:** drop `rounded-md border border-border` from its class list. It stays a real,
focusable `<button type="button" data-testid="both-doors" onClick={onBack}>` — only its BOX goes. Keep
it muted and small (`text-[13px] text-muted-foreground` plus the `hover:text-foreground` affordance)
and add whatever minimal padding or hover-underline treatment reads as a quiet escape, within the
shipped token vocabulary. Those pixel choices are Claude's discretion per CONTEXT; the sketch draws
none, so they are a human comparison at UAT rows U3 / U3b.

**The divider:** insert `<span aria-hidden="true" className="mx-0.5 h-4 w-px bg-border" />` — the
`CanvasToolbar.tsx` idiom, verbatim — BETWEEN the return control and the door label. Do not build a
new utility, do not use a text `│`, and give it no testid: it is decorative and `aria-hidden`, so it
must not reach the accessible name of anything.

**The door label stays PRIMARY** (`font-medium text-foreground`). The whole fix is that the three stop
reading as peers, and the current door is what a person should read first after the escape.

**The judge badge is UNCHANGED** — position, classes, `ml-auto` conditional, `title` and text. D-04 is
explicit: moving it is not needed and would spend change budget on the one element already correctly
positioned. ⚠ The `ml-auto` concatenation is D-05's whole risk and `193-03` proved it moved verbatim;
do not touch it.

**D-06 stays rejected:** the return control does NOT move into `headerLead`, which renders only when
`inline` is true. An author with no way back is a worse defect than a busy strip.

Record in the docblock: what changed, why (SEED-147 suspect #2's structural half, which copy cannot
reach), that the badge is deliberately untouched, and that this shape has **no mockup** so U3 is the
acceptance bar.
  </action>
  <acceptance_criteria>
    - `grep -c "rounded-md border border-border" frontend/src/components/workflows/DoorHeaderStrip.tsx` → **0**.
    - `grep -c "w-px bg-border" frontend/src/components/workflows/DoorHeaderStrip.tsx` → **1**, and that node carries `aria-hidden="true"`.
    - `git diff -U0 -- frontend/src/components/workflows/DoorHeaderStrip.tsx | grep -c "judge-locked\|ml-auto\|judge always-on"` → **0** — no line touching the badge was changed.
    - The return control is still `<button type="button">` with `data-testid="both-doors"` and an `onClick` — it did not become a `<span>`.
    - `grep -c "headerLead" frontend/src/components/workflows/DoorHeaderStrip.tsx` → **0** — D-06 was not quietly adopted.
    - `cd frontend && npx eslint src/components/workflows/DoorHeaderStrip.tsx` → 0 / 0; with `-c eslint.a11y.config.js` → 0.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` → **33**.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx eslint src/components/workflows/DoorHeaderStrip.tsx -c eslint.a11y.config.js</automated>
  </verify>
  <done>The govern strip reads escape · divider · current door · badge-at-the-far-edge, with the badge untouched and the divider borrowed rather than invented.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the same demotion — and only the demotion — to the describe band (D-22)</name>
  <files>frontend/src/components/workflows/WorkflowDoorSwitch.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/WorkflowDoorSwitch.tsx` — the describe band: `{inline && headerLead}`, the identically-classed `both-doors` button, and the current-door label span. Re-derive by content.
    - `frontend/src/components/workflows/DoorHeaderStrip.tsx` — the treatment just applied, so the two bands provably agree
    - `193-CONTEXT.md` **D-22** verbatim: *"the demotion is the border/box drop and the muted treatment, nothing else"* — and NO divider on that side, because no judge badge lives there
    - `193-RESEARCH.md` § B.1 band 5 — the finding that made D-22 necessary
  </read_first>
  <action>
Apply the **return-control demotion only** to the describe door's band in `WorkflowDoorSwitch.tsx`.
Drop `rounded-md border border-border`, keep the muted small treatment, and give it the identical
class string the govern strip's control now carries — read that string out of `DoorHeaderStrip.tsx`
rather than re-typing it.

⚠ **NO divider on this side.** D-22 is explicit: no judge badge lives in the describe band, so there
is no third peer to separate from and a divider would be a mark with nothing to divide.

⚠ Change nothing else in the band: `{inline && headerLead}` stays, the band's own
`border-b border-border px-4 py-2` wrapper stays, the label span stays primary, and `goBoth` stays the
handler.

⚠ **Do not extract this band into `DoorHeaderStrip`.** D-05's constraint is one component for the two
GOVERN header variants (standalone and inline); the describe band is a different band with no badge
and its own `headerLead` slot, and folding it in would be a structural change with neither a decision
nor a mockup behind it. Record that as a deliberate non-action rather than leaving the absence to be
read as an oversight.

Record in a comment: D-22, decided at plan time; the reason (demoting one and not the other makes the
two doors disagree about their own escape hatch); and that it has **no mockup on either side**, so
U3b is the acceptance bar.
  </action>
  <acceptance_criteria>
    - `grep -c "rounded-md border border-border" frontend/src/components/workflows/WorkflowDoorSwitch.tsx` → **0**.
    - `grep -c "w-px bg-border" frontend/src/components/workflows/WorkflowDoorSwitch.tsx` → **0** — no divider on the describe side.
    - `{inline && headerLead}` is still present in the describe band.
    - `grep -c "DoorHeaderStrip" frontend/src/components/workflows/WorkflowDoorSwitch.tsx` is unchanged from its `193-05` value — the describe band was NOT folded into the extracted component.
    - The two return-control class strings are proved EQUAL by an assertion in Task 3 that reads both `?raw` sources — not by eye.
    - `cd frontend && npx eslint src/components/workflows/WorkflowDoorSwitch.tsx -c eslint.a11y.config.js` → 0.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` → **33**.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx eslint src/components/workflows/WorkflowDoorSwitch.tsx -c eslint.a11y.config.js</automated>
  </verify>
  <done>Both doors demote their escape hatch identically, the describe side gains no divider it has nothing to divide, and the band was deliberately not folded into the extracted component.</done>
</task>

<task type="auto">
  <name>Task 3: Assert the restacked shape by child order, and record re-capture 2 of 2</name>
  <files>frontend/src/components/workflows/DoorHeaderStrip.test.tsx, frontend/src/components/workflows/WorkflowDoorSwitch.test.tsx, frontend/src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx, frontend/src/pages/WorkflowBuilderPage.header.test.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/DoorHeaderStrip.test.tsx` — the D-05 `ml-auto` pair and the cycle fence from `193-03`, and its existing child-order case
    - `frontend/src/components/workflows/library/WorkflowCard.test.tsx` — the **assert-by-child-order** pattern (`expect(column.children).toHaveLength(3)`, `expect(column.children[1]).toBe(line)`) and the "length check FIRST, so an absent node reds as an AssertionError rather than a `getBy*` throw" rule
    - `frontend/src/pages/WorkflowBuilderPage.header.test.tsx` — the byte-exact band literal, now reddened a second time, and its two consumption sites
    - `frontend/src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx` — the six captures; the two govern states and the two describe states red here
    - `193-PATTERNS.md` § S-4 (assert by child order, never by class name)
  </read_first>
  <action>
**(a) `DoorHeaderStrip.test.tsx` — D-04's shape by CHILD ORDER.** On BOTH `inline` values assert:
- the strip's children are, in order, `[return control] [divider] [door label] [judge badge]` — length
  check FIRST;
- the judge badge is the **LAST** child in both variants (D-04: far edge, unchanged);
- the divider node carries `aria-hidden="true"` and contributes nothing to the strip's accessible
  name — assert the strip's `textContent` gains no stray character;
- the return control's **own node** carries no `border-border` token — assert on that node, not on the
  strip: a class assertion on the wrong node passes for the wrong reason;
- the `ml-auto` pair from `193-03` still holds, unchanged. D-05 survived the restack.

**(b) `WorkflowDoorSwitch.test.tsx` — D-22.** Read both component sources via `?raw`, extract the two
return-control class strings, and assert they are EQUAL. Assert the describe band contains no
`w-px bg-border` node.

**(c) Re-capture 2 of 2.** Re-capture the affected `DOOR_HTML_BASELINE` states and the header suite's
band literal **from the rendered tree** — never hand-edit a capture string. Beside each, add a dated
note stating:
- this is the SECOND and final re-capture of the phase; `193-08` did the first, on WORDS; this one is
  on STRUCTURE (the D-04 / D-22 restack);
- what changed, precisely: the return control's class list on both bands, plus one inserted divider
  node in the govern strip only — proved by diffing the previous and new capture strings and
  confirming the deltas are exactly those two;
- the earlier capture SHAs stay in the files beside the new one. This repo corrects in the open.

⚠ **If any capture differs anywhere else** — a moved testid, a changed `ml-auto`, a lost badge — STOP
and report it. That is a regression, not a re-capture.

Do not raise any count-gate pin here; `193-11` owns the sweep.
  </action>
  <acceptance_criteria>
    - `DoorHeaderStrip.test.tsx` asserts the four-child order on BOTH `inline` values, with the length check first and the badge asserted LAST.
    - The divider is asserted `aria-hidden="true"` and proved not to alter the strip's `textContent`.
    - The return control's own node is asserted to carry no `border-border` token; the `193-03` `ml-auto` pair still passes unchanged.
    - `WorkflowDoorSwitch.test.tsx` asserts the two return-control class strings are EQUAL, read from `?raw` sources, and that the describe band has no divider node.
    - RED-first proof recorded, twice: (a) restore `rounded-md border border-border` on the govern control → the no-border case fails as a real `AssertionError`; (b) delete the divider node → the child-order case fails. Restore after each and confirm `DoorHeaderStrip.tsx` md5-identical. Paste both md5 pairs and both failure lines.
    - The per-capture diffs are pasted and show ONLY the class-list change and the one inserted divider node.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/DoorHeaderStrip.test.tsx src/components/workflows/WorkflowDoorSwitch.test.tsx src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx src/pages/WorkflowBuilderPage.header.test.tsx` → all pass, 0 failed.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 node ../scripts/vitest-count-gate.cjs` → exit 0, no `[count-decrease]`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/DoorHeaderStrip.test.tsx src/components/workflows/WorkflowDoorSwitch.test.tsx src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx src/pages/WorkflowBuilderPage.header.test.tsx</automated>
  </verify>
  <done>D-04's shape is pinned by child order on both variants, D-22's agreement is pinned by comparing the two class strings, and the second re-capture is dated, reasoned and proved to contain only the two intended deltas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host page → `inline` prop | The one branch that reaches a class list in this strip. It comes from the host's feature gate, never from user data. |
| decorative node → accessibility tree | A divider that is not `aria-hidden` becomes part of the strip's accessible name and is read aloud as noise. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193-36 | Denial of Service (of an escape route) | The demoted return control ceasing to be reachable | mitigate | It stays a real `<button type="button">` with its `onClick`; the child-order case asserts it is present and first in BOTH variants, and the a11y eslint config runs on both touched files. **D-06 stays rejected** so the standalone band never loses its way back. |
| T-193-37 | Information Disclosure | A decorative divider entering the accessibility tree | mitigate | The divider is `aria-hidden="true"` (the shipped `CanvasToolbar.tsx` idiom) and the suite asserts the strip's `textContent` gains no character from it. |
| T-193-38 | Tampering | The judge badge quietly moved or restyled under cover of the restack | mitigate | `git diff -U0` must touch no line naming `judge-locked`, `ml-auto` or the badge text; the child-order case asserts the badge is the LAST child in both variants; the `193-03` `ml-auto` pair must pass unchanged. |
| T-193-39 | Repudiation | A structural regression absorbed into a "legitimate re-capture" | mitigate | The per-capture diffs are inspected and published; only the class-list change and the one divider node are permitted, and any other difference stops the plan. `[count-decrease]` covers deletion. |
| T-193-40 | Spoofing (of fact) | The two bands claimed to agree while their class strings differ | mitigate | The agreement is asserted by extracting BOTH class strings from `?raw` sources and comparing them, never by eye and never by a duplicated literal. |
| T-193-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero packages installed.** The divider is a shipped Tailwind idiom; no icon library, no separator component. |
</threat_model>

<verification>
- `cd frontend && GSD_VITEST_MAX_WORKERS=4 node ../scripts/vitest-count-gate.cjs` → exit 0, `failed 0`, no `[count-decrease]`.
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` → **33**.
- `cd frontend && npx eslint src/components/workflows/` → ≤ 10, **0** in both touched source files; a11y config → 0.
- `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/pages/ src/components/workflows/` → all pass.
</verification>

<success_criteria>
- The govern strip renders escape · divider · current door · badge-at-the-far-edge, with the badge byte-untouched.
- The describe band's return control is demoted identically and carries no divider.
- D-05's `ml-auto` pair and D-06's rejection both survive.
- Both properties were driven RED against real plants and the source restored md5-identical.
- The second and final re-capture is dated, reasoned, and proved to contain only the two intended deltas.
</success_criteria>

<output>
Create `.planning/phases/193-authoring-doors-template-placement/193-09-SUMMARY.md` when done.
Record: both RED-plant md5 pairs and failure lines; the per-capture diffs; the explicit statement that
this is **re-capture 2 of 2** and that no further re-capture is expected in this phase; the exact
Tailwind classes chosen for the demoted control and the divider (they are discretionary and have no
mockup, so UAT rows **U3** and **U3b** are the acceptance bar and must name them); and the deliberate
non-action of folding the describe band into `DoorHeaderStrip`.
</output>
