---
phase: 193-authoring-doors-template-placement
plan: 10
type: execute
wave: 6
depends_on: ["193-05", "193-06", "193-07", "193-08", "193-09"]
files_modified:
  - scripts/vitest-count-gate.cjs
  - CLAUDE.md
  - .planning/ROADMAP.md
  - .planning/phases/193-authoring-doors-template-placement/193-UAT.md
  - .planning/phases/193-authoring-doors-template-placement/193-VALIDATION.md
autonomous: true
requirements: [AUTH-01, AUTH-03]

must_haves:
  truths:
    - "D-07 is discharged — `WorkflowDoorSwitch.tsx` finally has a hot-file ledger row with RE-DERIVABLE figures, so the next phase inherits a measured count instead of the invisibility that let six phases touch it without G-5 ever firing"
    - "D-09 holds and is recorded — NO G-5 override exists for this phase; the guardrail was honoured by construction, not waived"
    - "Every suite this phase changed is pinned from a number READ OFF THE GATE, never hand-counted, and no pin decreased"
    - "The eight G-4 rows are authored as a drivable artifact with their targets named — U4 and U5 against `ephemeral-template-fill-101uat`, the only published row that admits"
  artifacts:
    - path: ".planning/phases/193-authoring-doors-template-placement/193-UAT.md"
      provides: "The eight G-4 rows from VALIDATION.md, each with a driving recipe, a named target and an empty `result:` field"
      min_lines: 100
      contains: "ephemeral-template-fill-101uat"
    - path: "CLAUDE.md"
      provides: "A hot-file ledger row for `WorkflowDoorSwitch.tsx` with its re-derivation commands, plus the `WorkflowCard.tsx` row corrected on measurement"
      contains: "WorkflowDoorSwitch.tsx"
  key_links:
    - from: "CLAUDE.md hot-file ledger"
      to: "frontend/src/components/workflows/WorkflowDoorSwitch.tsx"
      via: "a row whose figures are re-derivable with the commands printed beside them"
      pattern: "WorkflowDoorSwitch.tsx"
---

<objective>
Close the phase's paperwork so the next one inherits measurements rather than re-deriving them.

Purpose: three of this project's most expensive recurring failures are bookkeeping, not code —
**a hot file absent from the ledger is permanently invisible to its own guardrail** (`WorkflowsPage.tsx`
escaped G-5 for ten phases; `WorkflowDoorSwitch.tsx` for six); **a phase absent from the ROADMAP
checklist is invisible to every audit that reads it** (188.2 at the v3.6 close, 192.1 at its own);
and **an unpinned suite is UNGUARDED, not lightly guarded**.

Output: the pin sweep, two ledger rows, the ROADMAP plan list, and `193-UAT.md` — the drivable
artifact `193-11` hands to the operator.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<worktree_protocol>
1. `bash scripts/bootstrap-worktree.sh "$(pwd)"` — FIRST action (the pin sweep runs the full gate).
2. Assert the dispatched base SHA; reset hard to it if the worktree forked elsewhere.
3. `GSD_VITEST_MAX_WORKERS=4` on every gate run. This plan runs alone in its wave, so the 2-concurrent
   cap is not approached.
4. Never `rm -rf` a worktree — `bash scripts/teardown-worktree.sh <path>`.
</worktree_protocol>

<context>
@.planning/phases/193-authoring-doors-template-placement/193-CONTEXT.md
@.planning/phases/193-authoring-doors-template-placement/193-VALIDATION.md
@.planning/phases/193-authoring-doors-template-placement/193-RESEARCH.md
@.planning/phases/193-authoring-doors-template-placement/193-PATTERNS.md
@CLAUDE.md
@.planning/ROADMAP.md
</context>

<interfaces>
<!-- Baselines measured at 58a402bc / cafc9389. RE-MEASURE at execution HEAD — every one of them
     moved during this phase. -->

Pins as they stood before this phase (the "from" column of the sweep):
`soulData.test.ts` 17 · `WorkflowDoorSwitch.test.tsx` 23 · `WorkflowCard.test.tsx` 80 ·
`librarySubtree.fences.test.ts` 118 · `RunModal.test.tsx` 32 · `RunModal.a11y.test.tsx` 16 ·
`WorkflowBuilderPage.header.test.tsx` 32 · `WorkflowBuilderPage.describe.test.tsx` 19 ·
`WorkflowBuilderPage.canvas.test.tsx` 128 · `WorkflowsPage.test.tsx` 52.
Pinned by this phase already: `WorkflowDoorSwitch.baseline.test.tsx` (`193-01`),
`DoorHeaderStrip.test.tsx` (`193-03`), `doorVocabulary.test.ts` (`193-05`).

The gate's failure conditions: `[count-decrease]` (a pinned file ran FEWER than its pin) and
`[total-below-baseline]`. A count INCREASE is drift, exit 0. A `BASELINE` entry for a path that does
not exist ERRORS (exit 2).

The `BASELINE_TOTAL` marker comment is stale — it reads `⚠ 3384 (192.1-06)` while the reduce computes
a different figure. Thirteenth recorded staleness event. **State the correction, do not overwrite it**
— the file's own habit.

The ledger figures to re-derive (`193-CONTEXT.md` D-07 measured them at discuss time):
`WorkflowDoorSwitch.tsx` — 8 commits / 6 phases (124, 155, 184, 184.1, 186, 187) / 385 L, **absent
from the ledger**. `WorkflowCard.tsx`'s shipped row reads `6 commits / 721 L`; measured before this
phase it was **7 / 747**, and 193 makes it the card's **3rd phase**, arming G-5 for the phase after.

Re-derivation commands (print them beside the figures so the row is auditable, not asserted):
```
git log --oneline -- <file> | wc -l
git log --format=%s -- <file> | sed -E 's/^[a-z]+\(([^)]+)\).*/\1/' | sed -E 's/-.*//' | sort -u
wc -l <file>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The pin sweep — every changed suite pinned from a number read off the gate</name>
  <files>scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `scripts/vitest-count-gate.cjs` — the `BASELINE` map, the `TARGETS` list, the two-knob comment block, and the `BASELINE_TOTAL` marker
    - `frontend/src/components/workflows/library/librarySubtree.fences.test.ts` — the house rule recorded five times: *"THE LITERAL IS READ OUT OF THIS ASSERTION'S OWN FAILING DIFF, never predicted"* and *"never hand-counted from `it(`"*
    - The SUMMARY of every prior plan in this phase, for the suites each touched
    - `193-RESEARCH.md` § E.1 / § E.2 (the measured baselines and the suite inventory)
  </read_first>
  <action>
Run the gate at this HEAD and raise every pin this phase moved, reading each number off the gate's own
printed `actual` column. **Hand-count nothing.**

Suites expected to have grown: `soulData.test.ts` (`193-02`), `WorkflowCard.test.tsx` (`193-06`),
`RunModal.test.tsx` (`193-01` + `193-07`), `RunModal.a11y.test.tsx` (`193-07`),
`doorVocabulary.test.ts` (`193-05` + `193-08`), `DoorHeaderStrip.test.tsx` (`193-03` + `193-09`),
`WorkflowDoorSwitch.test.tsx` (`193-05` + `193-08` + `193-09`),
`WorkflowDoorSwitch.baseline.test.tsx` (`193-01`), and possibly the three
`WorkflowBuilderPage.*.test.tsx` suites if `193-08`'s re-capture added cases. **Verify against the
gate's output rather than against this list** — the list is a prompt, the gate is the measurement.

⚠ **No pin may DECREASE.** If any does, stop: a case was deleted or skipped away, and that is a real
finding, not a pin to lower. Report it with the file and the delta.

Correct the stale `BASELINE_TOTAL` marker comment in the file's own habit: **state the correction
beside the old figure rather than overwriting it**, and name it as the thirteenth recorded staleness
event.

⚠ **Decline the four out-of-scope drifted pins** — `ExternalActionSection` +9, `PhaseTimeline` +4,
`WorkflowBuilderPage.canvas` +5, `builderStore` +6 (**24 cases deletable with the gate green today**).
They are outside this phase's blast radius, and re-pinning them here would fold unrelated drift into a
commit that did not cause it. Say so explicitly in the SUMMARY — this is the tenth consecutive plan to
decline them for the same reason, and stating it is what keeps the decline a decision rather than an
oversight.
  </action>
  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 node ../scripts/vitest-count-gate.cjs` → **exit 0**, `failed 0`, **no `[count-decrease]`**, no `[total-below-baseline]`.
    - Every pin this phase moved is raised to the number the gate printed in its `actual` column; the before → after table is pasted in the SUMMARY with the gate's own output beside it.
    - `git diff scripts/vitest-count-gate.cjs` touches no pin for `ExternalActionSection`, `PhaseTimeline`, `WorkflowBuilderPage.canvas.test.tsx` or `builderStore.test.ts`.
    - The `BASELINE_TOTAL` marker comment states the corrected figure beside the stale one rather than replacing it.
    - A second gate run at the same HEAD reproduces the same `total` and `failed 0` — recorded, because `GSD_VITEST_MAX_WORKERS=4` non-determinism is a measured risk in this repo.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 node ../scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>Every suite this phase touched is pinned from a read number, no pin decreased, and the four out-of-scope drifted pins were declined on the record.</done>
</task>

<task type="auto">
  <name>Task 2: The two hot-file ledger rows (D-07 / D-09) and the ROADMAP plan list</name>
  <files>CLAUDE.md, .planning/ROADMAP.md</files>
  <read_first>
    - `CLAUDE.md` § "Hot-file ledger" — the row format, and specifically the `WorkflowsPage.tsx` and `WorkflowCard.tsx` rows, which are the house examples of a row that **corrects itself on measurement** and prints its own re-derivation commands
    - `CLAUDE.md` § "Workflow guardrails" — G-5's wording and the orchestrator protocol for a fired guardrail
    - `.planning/ROADMAP.md` § "Phase 193" — the goal, the three success criteria, and the `**Plans:** TBD` placeholder
    - `193-CONTEXT.md` **D-07** (the measured figures and why the file was invisible), **D-08**, **D-09** (the override that was OFFERED AND DECLINED)
    - `.planning/STATE.md` § "Phase 193 — where it stands" — the ledger-drift note on `WorkflowCard.tsx`
  </read_first>
  <action>
**(a) Add a `WorkflowDoorSwitch.tsx` row to the CLAUDE.md hot-file ledger (D-07).** Re-derive every
figure at this HEAD with the commands in `<interfaces>` and print those commands inside the cell, the
habit the `WorkflowsPage.tsx` and `PhaseNodeCard.tsx` rows keep. The row must state:
- the touch list and counts, measured now (they grew during this phase);
- ⚠ **that the file was ABSENT from this table until Phase 193**, so six phases touched it without
  G-5 ever firing — the identical failure to `WorkflowsPage.tsx` escaping it for ten. The audit step
  scans `files_modified` **against this table**; a hot file missing from the table is permanently
  invisible to its own guardrail. **That sentence is the reason the row exists and must survive
  future edits.**
- **G-5 status: FIRED at 193 and HONOURED BY CONSTRUCTION, not waived** — the extraction
  (`DoorHeaderStrip.tsx` + `doorVocabulary.ts`) shipped in its own waves BEFORE the copy and the
  restack, in the 192.1 order (D-08). **D-09: an override was offered and DECLINED; no waiver is
  recorded for this phase.**
- the measured subtree growth, **stated plainly rather than smoothed** — the precedents are 188.2's
  +67.1 %, 192's +126.2 % and 192.1's +52.7 %, and this project publishes the figure even when it is
  large;
- what now binds the file: the D-24(a) copy fence (no governed literal outside `doorVocabulary.ts`),
  the D-24(b) cycle fence, D-05's `ml-auto` pair, and D-06's standing rejection;
- the next seam, named rather than implied, so a later phase inherits it.

**(b) Correct the `WorkflowCard.tsx` row on measurement.** It reads `6 commits / 721 L`; re-derive and
state the measured values, in the row's own correct-in-the-open voice. Record that **193 is the card's
3rd phase, which ARMS G-5 for the phase after this one** — per CONTEXT's `<deferred>`, a refactor
recommendation is owed by whoever touches it next. That is not 193's debt; it is recorded so it is
inherited rather than re-derived.

⚠ Do not create a row for `library/RunModal.tsx` or `soulData.ts` — neither is near the threshold and
padding the ledger devalues it. **Do** state that check in the SUMMARY so the absence reads as a
measurement rather than an omission.

**(c) ROADMAP — VERIFY, then tick.** `/gsd:plan-phase` already wrote the Phase 193 block: `**Plans**:
11 plans in 7 waves` plus an eleven-entry `- [ ]` checklist and the wave-parallelism note. **Do not
rewrite it.** Verify it lists exactly eleven entries whose filenames match the plans on disk, and tick
every plan that has shipped by this wave (01–09 and this one) to `- [x]`, leaving `193-11` unticked
because its checkpoint has not been driven.
⚠ The phase heading must stay a `#### Phase NNN:` heading — bold labels silently fail the phase
lookup and broke all of v3.7 once already.
⚠ If any checklist entry names a plan that does not exist on disk, or a plan on disk is missing from
the checklist, say so and fix it — **188.2 and 192.1 were each ABSENT from their own checklist until
the close, and a phase missing from its checklist is invisible to every audit that reads it.**
  </action>
  <acceptance_criteria>
    - `grep -c "WorkflowDoorSwitch.tsx" CLAUDE.md` → ≥ 1, in a ledger table row (not only in prose).
    - The new row contains the three re-derivation commands verbatim and the sentence naming the absent-from-the-table failure.
    - The row states `HONOURED BY CONSTRUCTION` and `no override / no waiver` — `grep -c "override" ` on the row's cell returns a hit, and `.planning/STATE.md` records no G-5 waiver for 193.
    - The `WorkflowCard.tsx` row's figures are re-derived at this HEAD and the correction is stated beside the previous value, not silently replacing it.
    - `.planning/ROADMAP.md` § Phase 193 lists **11** plan entries whose filenames all exist on disk, the heading is still `#### Phase 193:`, and plans 01–10 are ticked `- [x]` while `193-11` remains `- [ ]`.
    - `grep -c "^#### Phase 193:" .planning/ROADMAP.md` → 1.
    - No other ROADMAP phase block was edited: `git diff .planning/ROADMAP.md` touches only the Phase 193 block.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^#### Phase 193:" .planning/ROADMAP.md && grep -c "WorkflowDoorSwitch.tsx" CLAUDE.md</automated>
  </verify>
  <done>The file that escaped G-5 for six phases has a measured, re-derivable ledger row; the card's row is corrected; the ROADMAP lists all eleven plans; and D-09's declined override is on the record.</done>
</task>

<task type="auto">
  <name>Task 3: Author `193-UAT.md` — the eight G-4 rows, with their targets named</name>
  <files>.planning/phases/193-authoring-doors-template-placement/193-UAT.md, .planning/phases/193-authoring-doors-template-placement/193-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/193-authoring-doors-template-placement/193-VALIDATION.md` § "Manual-only — the G-4 rows this phase owes" — **the eight rows U1, U2, U3, U3b, U4, U5, U6, U7 are already authored there. Port them; do not invent new ones and do not renumber them.**
    - `.planning/phases/192.1-workflow-identity/192.1-UAT.md` — the shipped UAT artifact shape: per-row `what` / `why it cannot be automated` / driving recipe / `fail:` conditions / an empty `result:` field
    - Every prior SUMMARY in this phase — for the discretionary choices UAT must judge (the demoted control's exact Tailwind classes, the divider treatment, the `🔧` asymmetry)
    - `193-CONTEXT.md` **D-21** (the single admitting slug), **D-04**, **D-22**, **D-23**
    - `CLAUDE.md` § "UAT scoreboard recipe" — and the measured note that the 4-axis cross-provider board is **NOT** triggered by this phase
  </read_first>
  <action>
Create `.planning/phases/193-authoring-doors-template-placement/193-UAT.md` carrying the eight rows
from `193-VALIDATION.md` verbatim in scope, each expanded into a drivable recipe.

Per row: the exact steps, the target to drive it against, the observable that decides it, the
`fail:` conditions stated **before** the drive, and an empty `result:` field for the operator's words.

Row-specific obligations that must not be lost in the port:
- **U1** (SC#1) — ask what each door does **BEFORE** the click, and **record the answer verbatim**.
  This is the row the whole phase exists for and no assertion can measure it.
- **U2** (SC#2) — count the perceived choices on the chooser and inside each open door. A structural
  count cannot see perception.
- **U3** / **U3b** — drive the restack at **both** `inline` values (`visual_workflow_canvas` OFF for
  the standalone band, ON for the merged row) and on **both** bands. Name the exact Tailwind classes
  `193-09` chose, since they are discretionary and have no mockup.
- **U4** (SC#3) — ⚠ **drive against `ephemeral-template-fill-101uat`, the ONLY published row that
  admits under D-21**, and against a non-admitting row beside it. A row picked at random cannot see
  this feature at all. Judge both halves: is `Template to fill` findable without being told, and is
  the control's absence *unremarkable* rather than *broken-looking*?
- **U5** — same target. Does `· needs a template` read as *a requirement of you* or as *a description
  of the workflow*? If it reads as a description, the mark says nothing the shipped *Makes a file*
  chip does not.
- **U6** — the govern door's header label after variant D. Does it agree with the door card that
  opened it? ⚠ **Also judge the `🔧` asymmetry** D-23 produced: the govern label loses its glyph while
  the describe label keeps `⚡`. D-23 is what makes a PASS possible here at all.
- **U7** — trigger a launch failure on a workflow whose template block is hidden. **Is the error
  visible?** The `launchError` hazard, end-to-end.

Driving notes to carry over verbatim (do not re-derive): `take_screenshot` **times out** on this
setup — read DOM geometry via `evaluate_script`; `computer` clicks can deliver zero events while
`hover` / `left_click_drag` work; and **no row may locate its target by `getElementById` on a known
id** (D-27 — the rule Phase 192's re-drive broke and 192.1 restored).

State plainly at the top: **0 driven · 0 passed · 0 failed · 8 owed**, and that the 4-axis
cross-provider board is **not triggered** by this phase (it touches UI state but not streaming, the
agent loop, or provider routing) — recorded so its absence is distinguishable from an omission.

Then add a one-line pointer in `193-VALIDATION.md`'s "Manual-only" section to `193-UAT.md` as the
drivable artifact, and fill its `## Per-Task Verification Map` from the eleven plans' `<verify>`
commands.
  </action>
  <acceptance_criteria>
    - `193-UAT.md` contains exactly eight rows, ids `U1 U2 U3 U3b U4 U5 U6 U7`, matching `193-VALIDATION.md` — no row invented, none renumbered, none dropped.
    - `grep -c "ephemeral-template-fill-101uat" .planning/phases/193-authoring-doors-template-placement/193-UAT.md` → ≥ 2 (named in both U4 and U5).
    - Every row has a `fail:` block written BEFORE any drive and an empty `result:` field.
    - `grep -c "getElementById" .planning/phases/193-authoring-doors-template-placement/193-UAT.md` → ≥ 1, appearing only in the D-27 prohibition note.
    - The header states `0 driven · 0 passed · 0 failed · 8 owed` and records the 4-axis board as not triggered, with the reason.
    - `193-VALIDATION.md`'s `## Per-Task Verification Map` is filled with one line per plan, quoting each plan's `<automated>` command.
    - No PLAN.md file contains a UAT row as a task — `grep -lc "^| U[0-9]" .planning/phases/193-authoring-doors-template-placement/193-*-PLAN.md` returns nothing.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "ephemeral-template-fill-101uat" .planning/phases/193-authoring-doors-template-placement/193-UAT.md</automated>
  </verify>
  <done>The eight owed rows exist as a drivable artifact with their targets named, their failure conditions written before the drive, and their count honestly recorded as owed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured figure → ledger row | A figure written at a phase's close goes stale on the next commit that touches any file it counted. The row must carry its own re-derivation commands. |
| owed UAT rows → close claim | A scoreboard that lists only what passed is not a scoreboard; owed rows must be recorded as owed, never omitted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193-41 | Repudiation | A hot file invisible to its own guardrail | mitigate | The `WorkflowDoorSwitch.tsx` ledger row is added with re-derivation commands printed inside the cell, and the sentence naming the absent-from-the-table failure is required to survive future edits. `grep -c` on the row is an acceptance criterion. |
| T-193-42 | Spoofing (of fact) | A stale ledger or ROADMAP figure read later as current | mitigate | Every figure is re-derived at this HEAD with the printed commands; the `WorkflowCard.tsx` correction is stated BESIDE the previous value rather than replacing it, so a reader can see what was believed and what measurement changed it. |
| T-193-43 | Repudiation | A pin lowered to make a deletion green | mitigate | The gate's `[count-decrease]` is the mechanism; a decrease stops the plan and is reported with file and delta rather than absorbed. Every raised pin comes from the gate's printed `actual` column. |
| T-193-44 | Spoofing (of fact) | A phase closed while claiming UAT it did not run | mitigate | `193-UAT.md` opens with `0 driven · 0 passed · 0 failed · 8 owed`. Closing with rows owed is legitimate; claiming they ran is not. `193-11` is the drive. |
| T-193-45 | Tampering | A UAT row driven against a target that cannot see the feature | mitigate | U4 and U5 name `ephemeral-template-fill-101uat` explicitly — the ONLY published row that admits under D-21 — and the requirement is an acceptance criterion, not a note. |
| T-193-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero packages installed.** This plan writes documentation and one config map. |
</threat_model>

<verification>
- `cd frontend && GSD_VITEST_MAX_WORKERS=4 node ../scripts/vitest-count-gate.cjs` → exit 0, `failed 0`, no `[count-decrease]`, run TWICE with agreeing totals.
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` → **33**.
- `cd frontend && npx eslint src/components/workflows/` → ≤ 10, **0** in every file this phase touched.
- `grep -c "^#### Phase 193:" .planning/ROADMAP.md` → 1, with 11 plan entries beneath it.
- `node scripts/check-gap-closure-rounds.cjs 193` → exit 0 (`G-7 clear`) — recorded so the absence of a check is distinguishable from a skipped one.
</verification>

<success_criteria>
- Every suite this phase changed is pinned from a read number; no pin decreased; the four out-of-scope pins were declined on the record.
- `WorkflowDoorSwitch.tsx` has a re-derivable hot-file ledger row stating that G-5 fired and was honoured by construction, with D-09's declined override recorded.
- `WorkflowCard.tsx`'s row is corrected on measurement and its G-5 arming is inherited forward.
- The ROADMAP lists all eleven plans under a `#### Phase 193:` heading.
- `193-UAT.md` carries the eight owed rows with named targets and pre-written failure conditions.
</success_criteria>

<output>
Create `.planning/phases/193-authoring-doors-template-placement/193-10-SUMMARY.md` when done.
Record: the before → after pin table with the gate's own output; both gate runs' totals; the
re-derived ledger figures with their commands; the measured `components/workflows/` subtree growth
stated plainly; the explicit decline of the four out-of-scope pins; and the statement that eight G-4
rows are **owed, not run**, with `193-11` as the drive.
</output>
