---
phase: 193-authoring-doors-template-placement
plan: 06
type: execute
wave: 3
depends_on: ["193-02"]
files_modified:
  - frontend/src/components/workflows/library/WorkflowCard.tsx
  - frontend/src/components/workflows/library/WorkflowCard.test.tsx
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "D-13 holds — the mark is a PLAIN TEXT SEGMENT in the card's existing identity line: no chip, no badge, no new colour, no new component, and no plan claims a typecheck enforces it"
    - "D-14 holds — the mark sits AFTER provenance and before everything else in `identityParts`, asserted by CHILD ORDER and array index, never by class name, and the provenance node stays at DOM position 2"
    - "D-15 holds — the mark renders ONLY on `\"admits\"`; `\"does-not-admit\"` and `\"unknown\"` are deliberately INDISTINGUISHABLE, asserted with the same expected array so a divergence is a failure rather than an untested difference"
    - "The card needs no new prop and no `RowIdentity` field — `row.def` is already on the row type and `templateAdmission` is called in-card, the shipped practice `CHIP_PREDICATES.yours(row)` already establishes"
  artifacts:
    - path: "frontend/src/components/workflows/library/WorkflowCard.tsx"
      provides: "The template mark spliced at index 0 of `identityParts`, gated on a positive `admits` only"
      contains: "templateAdmission"
    - path: "frontend/src/components/workflows/library/WorkflowCard.test.tsx"
      provides: "Three-arm coverage with `does-not-admit` and `unknown` asserted identically, plus the extended grammar-in-order block"
  key_links:
    - from: "frontend/src/components/workflows/library/WorkflowCard.tsx"
      to: "frontend/src/components/workflows/soulData.ts"
      via: "templateAdmission(row.def) === \"admits\""
      pattern: "templateAdmission\\(row\\.def\\)"
---

<objective>
Give the library card the one word AUTH-03 owes it: `· needs a template`, on the rows that genuinely
need one.

Purpose: SC#3 — *a user with a template to fill can find where to supply it* — starts at the row, not
at the modal. The mark tells a person, before they open anything, that this workflow expects
something from them.

⚠ **The cost is stated, not smoothed:** under D-21 the mark is visible on **exactly ONE published row
in the local library** — `ephemeral-template-fill-101uat`. 110 of 145 published rows read `unknown`
(a `phases: []` stub nobody authored) and render nothing, which is D-15 working correctly. UAT row
**U5** must be driven against that slug or it cannot see the feature at all.

Output: one spliced segment in the card's identity line, and the cases that pin its slot and its
three-arm silence.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<worktree_protocol>
1. `bash scripts/bootstrap-worktree.sh "$(pwd)"` — FIRST action.
2. Assert the dispatched base SHA; reset hard to it if the worktree forked elsewhere.
3. `GSD_VITEST_MAX_WORKERS=4` on every vitest run.
4. Never `rm -rf` a worktree — `bash scripts/teardown-worktree.sh <path>`.
</worktree_protocol>

<context>
@.planning/phases/193-authoring-doors-template-placement/193-CONTEXT.md
@.planning/phases/193-authoring-doors-template-placement/193-RESEARCH.md
@.planning/phases/193-authoring-doors-template-placement/193-PATTERNS.md
@.planning/phases/193-authoring-doors-template-placement/193-VALIDATION.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
@frontend/src/components/workflows/library/WorkflowCard.tsx
@frontend/src/components/workflows/library/rowIdentity.ts
</context>

<interfaces>
<!-- Measured at f2087341. RE-DERIVE BY CONTENT — `identityParts()`'s helper was reported at :632 by
     both CONTEXT and RESEARCH and measures :154. -->

The composition site in `WorkflowCard.tsx`:
```tsx
const owned = CHIP_PREDICATES.yours(row)          // the in-card derivation precedent

const identityParts: string[] = [
  ...identity.segs,
  ...(identity.ofN === null ? [] : [identity.ofN]),
  ...(identity.when === null ? [] : [identity.when]),
]
```

The render site (`data-testid="row-identity"`), unchanged by this plan:
```tsx
<div data-testid="row-identity" className={IDENTITY_CLASSES}>
  <span className={owned ? IDENTITY_OWN_YOURS : IDENTITY_OWN_SHARED}>{identity.own}</span>
  {identityParts.map((part, index) => (
    <Fragment key={`${index}-${part}`}>
      <span aria-hidden="true">{IDENTITY_SEPARATOR}</span>
      <span>{part}</span>
    </Fragment>
  ))}
</div>
```
`IDENTITY_SEPARATOR = "·"`, spent BETWEEN present parts only; `null`s are DROPPED in `identityParts`
rather than rendered as blanks.

The predicate (from `193-02`):
```ts
export type TemplateAdmission = "admits" | "does-not-admit" | "unknown"
export function templateAdmission(def: DefShape | null | undefined): TemplateAdmission
```

The string (from `193-02`, in `library/libraryVocabulary.ts`):
`export const CARD_TEMPLATE_MARK = "needs a template"`

The suite's helper and the child-order pattern, in `WorkflowCard.test.tsx`:
```ts
const identityParts = (line: HTMLElement): string[] =>
  Array.from(line.children).map((c) => c.textContent ?? "").filter((t) => t !== IDENTITY_SEPARATOR)
```
```ts
expect(column.children).toHaveLength(3)
expect(column.children[0].textContent).toContain(row.name)   // the name/version row
expect(column.children[1]).toBe(line)                        // D-08's position — DOM position 2
expect(column.children[2].textContent).toContain("Risk & Compliance")
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Splice the mark at index 0 of `identityParts`, gated on a positive `admits` only (D-13 / D-14 / D-15)</name>
  <files>frontend/src/components/workflows/library/WorkflowCard.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/library/WorkflowCard.tsx` (747 L) — the `owned = CHIP_PREDICATES.yours(row)` precedent, the `identityParts` composition docblock, `IDENTITY_SEPARATOR`, and the `row-identity` render block. Re-derive every span by content.
    - `frontend/src/components/workflows/library/rowIdentity.ts` — the `RowIdentity` definition (*"Everything the card paints on the identity line, already resolved"*) and its `when: string | null` wire-absence rule. **Read it to understand why the mark must NOT go there.**
    - `frontend/src/components/workflows/soulData.ts` — `templateAdmission`'s signature and docblock
    - `frontend/src/components/workflows/library/libraryVocabulary.ts` — `CARD_TEMPLATE_MARK`
    - `.claude/skills/sketch-findings-agentic-rag/SKILL.md` — load the findings for the library card / identity-line surface before touching it
    - `193-CONTEXT.md` **D-13** (incl. its "measured FALSE" correction), **D-14**, **D-15**, **D-21**
    - `193-PATTERNS.md` § `library/WorkflowCard.tsx` (the exact splice, and the "do NOT add the mark to `RowIdentity`" reasoning)
  </read_first>
  <action>
In `frontend/src/components/workflows/library/WorkflowCard.tsx`:

1. Import `templateAdmission` from `@/components/workflows/soulData` and `CARD_TEMPLATE_MARK` from
   `./libraryVocabulary` — both are named additions to existing import lists.
2. Beside the shipped `const owned = CHIP_PREDICATES.yours(row)`, add
   `const templateMark = templateAdmission(row.def) === "admits" ? [CARD_TEMPLATE_MARK] : []`.
3. Splice it at **index 0 of `identityParts`**, before `identity.segs`.

Why index 0 is D-14's slot, stated in the docblock so it is not re-derived later: `identity.own` is
rendered SEPARATELY as the pill, and `identityParts` begins after it — so index 0 is *immediately
after provenance*, giving `whose it is · what it needs · which copy · when`, with recency still last
where 192.1 put it. Do NOT place the mark before `own`: 192.1 asserts the provenance node at **DOM
position 2 by child order**, and the whole point of asserting by child order was that it does not
move.

Docblock obligations:
- **D-13:** a plain text segment, muted by the line's existing classes, separated by the `·` the line
  already spends. No chip, no badge, no new colour, no new component. ⚠ **And the correction, stated
  rather than repeated:** the 188.2 two-badge ceiling (`@ts-expect-error`, `BadgeSlots`) guards the
  CANVAS `PhaseNodeCard`; there is **no badge ceiling of any kind under `library/`**. D-13 stands on
  SEED-155 / UAT U8 grounds — sketch 163 drew a chip this card could not render — which is the reason
  that actually applies. **Do not write, here or in a test, that a typecheck enforces this.**
- **D-15:** the comparison is `=== "admits"`, a positive yes. `"does-not-admit"` and `"unknown"` both
  render nothing and are deliberately indistinguishable, mirroring the `updated_at` rule this line
  already follows: `undefined` means *the wire did not say*, and the honest rendering of that is no
  segment. **Absence of the mark must never be readable as an assertion that no template is needed.**
- **No new prop, no `RowIdentity` field.** `row.def` is already on the row type and `CHIP_PREDICATES.yours(row)`
  is the shipped precedent for an in-card derivation. `RowIdentity`'s four fields are all properties
  of the row's PLACE IN THE LIST, resolved by an index built over `[rows]`; a definition-derived fact
  is a category error there and would force `rowIdentity.ts` to learn about definitions.
- **D-21's cost, in one line:** the mark is visible on exactly one published row locally
  (`ephemeral-template-fill-101uat`), because 16 of the 17 emit-phase published rows bind a library
  template and 110 read `unknown`.

⚠ **Fences that bind this file:** no `title=` attribute (F1, AST-parsed), no raw-HTML escape hatch
(T-192-04 is a RAW regex — do not spell that prop even in a comment), no `WorkflowsPage` specifier
(F4). Add no `key` scheme change: the existing `` `${index}-${part}` `` already handles two parts with
the same text.
  </action>
  <acceptance_criteria>
    - `grep -c "templateAdmission(row.def)" frontend/src/components/workflows/library/WorkflowCard.tsx` → 1, and the comparison operand is the literal `"admits"`.
    - `grep -c "needs a template" frontend/src/components/workflows/library/WorkflowCard.tsx` → **0** — the string comes from `libraryVocabulary.ts`, never inline.
    - The splice is at index 0 of `identityParts` (the spread of `templateMark` precedes `...identity.segs` in the array literal).
    - `grep -c "rowIdentity\|RowIdentity" frontend/src/components/workflows/library/rowIdentity.ts` unchanged — `rowIdentity.ts` is NOT in this plan's `files_modified` and must show no diff.
    - `grep -c "title=" frontend/src/components/workflows/library/WorkflowCard.tsx` → **0**; `grep -c "dangerously" frontend/src/components/workflows/library/WorkflowCard.tsx` → **0**.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/library/librarySubtree.fences.test.ts` → all pass (F1 / F4 / F5 / T-192-04 unmoved).
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` → **33**.
    - `cd frontend && npx eslint src/components/workflows/library/WorkflowCard.tsx` → 0 / 0, and with the a11y config → 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/library/librarySubtree.fences.test.ts</automated>
  </verify>
  <done>The card renders one extra plain text segment, only on a positive `admits`, at the slot D-14 names — with no new prop, no new node type, and no claim that a typecheck guards it.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the slot and the three-arm silence — including the arm a green suite would never enter</name>
  <files>frontend/src/components/workflows/library/WorkflowCard.test.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/library/WorkflowCard.test.tsx` (1132 L, pinned 80) — the `identityParts()` helper (**at `:154`, NOT `:632`** — re-derive with `grep -n "const identityParts"`), the `renderCard` builder and its "REQUIRED, never optional-with-a-default" convention, the child-index `it.each` block, and the `D-03 / D-06 — the line's grammar, in order` describe block whose `toEqual` arrays every new segment reds
    - `frontend/src/components/workflows/library/WorkflowCard.tsx` — the change just made
    - `frontend/src/components/workflows/soulData.test.ts` — the shapes that reach each of the three states, so the fixtures here match the predicate's real arms
    - `193-PATTERNS.md` § `WorkflowCard.test.tsx` (the child-order pattern, the grammar block, the three-arm requirement)
    - `193-VALIDATION.md` § "Phase Requirements → Test Map" — the D-14 / D-15 rows
  </read_first>
  <action>
Extend `frontend/src/components/workflows/library/WorkflowCard.test.tsx`.

**(1) The three arms, asserted at the surface.** Three cases driven by three real `row.def` shapes:
- **admits** — an `llm_emit` phase, no bound `assets[kind === "template"]` → `identityParts(line)`
  contains `CARD_TEMPLATE_MARK` at **array index 0**.
- **does-not-admit** — a definition with no emit phase.
- **unknown** — `{ phases: [] }`. ⚠ **This arm is the 192 CR-01 lesson applied**: *"both failure tests
  pinned the correct branch without ever entering the wrong one."* A suite that covers only `admits`
  and the trivial no-emit case never enters the arm 110 of 145 published rows actually take.

Assert the last two with the **SAME expected `toEqual` array**, so their indistinguishability (D-15) is
a mechanical property rather than an untested coincidence. Write that reason in the case's comment.

**(2) The slot, by child order.** Extend the shipped `D-03 / D-06 — the line's grammar, in order`
block with a colliding row that ALSO admits, asserting the full ordered array
`[OWN_*, CARD_TEMPLATE_MARK, "<seg>", STATE_DRAFT, "<n> share this name", "changed …"]`. **The mark's
position is asserted by its array index in `toEqual`, never by a class name** — a class assertion
passes on a node in the wrong column and fails on a Tailwind tidy-up.

**(3) The provenance node has not moved.** Re-run the shipped child-index `it.each`
(`column.children[1]` is the identity line, `children` length 3) on a row that admits — the mark must
not disturb DOM position 2. Do the length check FIRST so an absent line reds as an `AssertionError`
rather than a `getBy*` throw.

**(4) No new atom.** Assert the card renders **no new node type**: the count of direct children of
`row-identity` is exactly `1 + 2 × identityParts.length` on an admitting row (the own pill, plus a
separator + a text span per part), so the mark arrived as a part rather than as a chip. That is D-13
proved by structure rather than by intention.

Every case that already asserts a `toEqual` grammar array WILL red when the mark lands on a fixture
that admits — that is correct, and this block is the right place to pin the new order. Where a shipped
fixture does not admit, its array must be UNCHANGED; a case whose expectation you had to edit on a
non-admitting row means the predicate is wrong, not the test.

Do not raise the count-gate pin here; `193-11` owns the sweep.
  </action>
  <acceptance_criteria>
    - Three cases exist reaching `admits`, `does-not-admit` and `unknown`, and the latter two use the identical expected array (assert the two expectations are the same value in code, e.g. a shared `const SILENT_LINE`).
    - The grammar case asserts `CARD_TEMPLATE_MARK` at array index 0 of `identityParts(line)`.
    - The child-index case asserts `column.children` has length 3 and `column.children[1]` is the identity line, on an ADMITTING row.
    - The structural D-13 case asserts `row-identity`'s direct child count equals `1 + 2 × parts.length`.
    - RED-first proof recorded, twice: (a) change the card's comparison to `!== "does-not-admit"` and observe the `unknown` case FAIL as a real `AssertionError`; (b) move the splice from index 0 to the end of `identityParts` and observe the grammar case FAIL. Restore after each and confirm `WorkflowCard.tsx` md5-identical. Paste both md5 pairs and both failure lines.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/library/WorkflowCard.test.tsx` → all pass, 0 failed, count ≥ 80.
    - No expectation on a NON-admitting shipped fixture was edited: `git diff -U0 -- frontend/src/components/workflows/library/WorkflowCard.test.tsx` shows edits only inside the new cases and the grammar block's admitting row.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/library/WorkflowCard.test.tsx</automated>
  </verify>
  <done>The mark's slot and its three-arm silence are pinned by child order and array index, the `unknown` arm is genuinely entered, and both properties were driven RED against real plants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server `definition` → rendered identity line | The mark is derived from server JSON. It renders as a JSX text node; no definition content is interpolated into it. |
| predicate silence → user reading | Absence of the mark is read by a person. It must never be readable as a positive claim that no template is needed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193-21 | Spoofing (of fact) | D-15's silence read as *"no template needed"* | accept | **Decided, not defaulted (D-15).** On the card, silence costs nothing and a fabricated claim costs trust; `"does-not-admit"` and `"unknown"` are deliberately indistinguishable. The named check is UAT row **U5**, driven against `ephemeral-template-fill-101uat`, and the acceptance is recorded in the card's own docblock. |
| T-193-22 | Tampering | XSS via definition content reaching the line | mitigate | The mark is the constant `CARD_TEMPLATE_MARK`; no definition value is interpolated. The line renders JSX text nodes (React escapes), and the raw-HTML escape hatch is fenced across the subtree by T-192-04. |
| T-193-23 | Information Disclosure | A hover-only tooltip carrying more than the line shows | mitigate | F1 sweeps this file for a `title` JSX attribute (AST-parsed since `192-05`); `grep -c "title="` → 0 is an acceptance criterion. Touch has no hover. |
| T-193-24 | Denial of Service | A per-render definition walk on a 200-row library | mitigate | `templateAdmission` is two `some()` calls over arrays measured ≤ 12 elements; it runs where `CHIP_PREDICATES.yours(row)` already runs, at the same cadence. No new memo, no new index. |
| T-193-25 | Repudiation | A claim that a typecheck forbids a third badge here | mitigate | The correction is written into the card's docblock and into `193-02`'s vocabulary docblock: the `@ts-expect-error` control guards `PhaseNodeCard`, not this card. D-13 is enforced structurally instead — by the child-count assertion in Task 2. |
| T-193-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero packages installed.** No badge component, no tooltip library, no icon. |
</threat_model>

<verification>
- `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/library/` → all pass, 0 failed.
- `cd frontend && GSD_VITEST_MAX_WORKERS=4 node ../scripts/vitest-count-gate.cjs` → exit 0, `failed 0`, no `[count-decrease]`.
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "error TS"` → **33**.
- `cd frontend && npx eslint src/components/workflows/library/WorkflowCard.tsx` → 0 / 0, a11y config → 0.
</verification>

<success_criteria>
- `· needs a template` renders on rows that positively admit, at index 0 of `identityParts`, and nowhere else.
- `does-not-admit` and `unknown` are proved indistinguishable by a shared expectation.
- The provenance node is still at DOM position 2 and the line gained no new node type.
- Both properties were driven RED against real plants and the source restored md5-identical.
- `tsc` 33, subtree fences green.
</success_criteria>

<output>
Create `.planning/phases/193-authoring-doors-template-placement/193-06-SUMMARY.md` when done.
Record: both RED-plant md5 pairs and failure lines; the case count `WorkflowCard.test.tsx` now
reports; and a plain restatement of D-21's cost — the mark is visible on exactly ONE published row
locally, which is why UAT row **U5 must be driven against `ephemeral-template-fill-101uat`** and
against a non-admitting row beside it.
</output>
