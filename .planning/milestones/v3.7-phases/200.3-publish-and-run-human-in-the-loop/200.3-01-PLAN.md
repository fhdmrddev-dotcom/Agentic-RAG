---
phase: 200.3-publish-and-run-human-in-the-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/harness/human_input.py
  - backend/app/services/harness/publish_service.py
  - backend/tests/unit/test_publish_service.py
  - backend/tests/unit/test_198_node_vocabulary.py
autonomous: true
requirements: [SEED-164]
tags: [SEED-164, backend, publish-gauntlet, golden-run, human-input]
must_haves:
  truths:
    - "When validating a workflow during the background golden run (is_golden_run=True), human input steps auto-continue rather than hanging or timing out"
    - "The publish gauntlet permits publishing workflows containing llm_human_input nodes"
    - "Live production runs continue to strictly pause and require interactive human input"
  artifacts:
    - path: backend/app/services/harness/human_input.py
      provides: "Golden run mock bypass for human input"
    - path: backend/app/services/harness/publish_service.py
      provides: "Updated publish gauntlet permitting human approval workflows"
---

<objective>
Enable publishing for workflows containing human approval nodes (SEED-164):
1. In `human_input.py`, when `ctx.is_golden_run` is True, auto-continue with `options[0]` (or `"Approved"`) and record `_golden_run_mock: True`.
2. In `publish_service.py`, pass `is_golden_run=True` in `ctx`, and update `_interactive_phase_failures` so `llm_human_input` is no longer blocked from publishing.
3. Update unit tests in `test_publish_service.py` and `test_198_node_vocabulary.py`.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Golden Run Bypass in human_input.py</name>
  <files>backend/app/services/harness/human_input.py</files>
  <action>
    In `_exec_llm_human_input`, check `if getattr(ctx, "is_golden_run", False):`. If true, log that the golden run is auto-continuing, and return immediately with:
    `{"text": prompt, "answer": options[0] if options else "Approved", "tool_call_id": tool_call_id, "_golden_run_mock": True}`.
  </action>
  <verify>
    <automated>venv\Scripts\python -m pytest tests/unit/test_198_node_vocabulary.py</automated>
  </verify>
  <done>Human input step auto-continues immediately during golden validation runs.</done>
</task>

<task type="auto">
  <name>Task 2: Update Publish Gauntlet in publish_service.py</name>
  <files>backend/app/services/harness/publish_service.py</files>
  <action>
    1. In `_validate_with_golden_run`, add `is_golden_run=True` to the `ctx = SimpleNamespace(...)` definition.
    2. In `_interactive_phase_failures`, remove `config.phase_type == "llm_human_input"` from the blocking check (or keep only `ask_user_validator` if needed), allowing human-in-the-loop workflows to proceed to golden run validation.
  </action>
  <verify>
    <automated>venv\Scripts\python -m pytest tests/unit/test_publish_service.py</automated>
  </verify>
  <done>Workflows with human input nodes pass pre-run lint and succeed in golden run validation.</done>
</task>

<task type="auto">
  <name>Task 3: Unit Tests for SEED-164</name>
  <files>backend/tests/unit/test_publish_service.py, backend/tests/unit/test_198_node_vocabulary.py</files>
  <action>
    Add test cases asserting:
    1. An `llm_human_input` definition is not refused by `_interactive_phase_failures`.
    2. `_exec_llm_human_input` with `ctx.is_golden_run = True` immediately returns the mock answer with `_golden_run_mock: True` and no asyncio wait.
  </action>
  <verify>
    <automated>venv\Scripts\python -m pytest tests/unit/test_publish_service.py tests/unit/test_198_node_vocabulary.py</automated>
  </verify>
  <done>All unit tests pass cleanly.</done>
</task>

</tasks>

<success_criteria>
- Human input workflows pass the publish gauntlet.
- All backend unit tests pass.
</success_criteria>
