---
phase: 200.3-publish-and-run-human-in-the-loop
plan: 02
type: execute
wave: 2
depends_on: [200.3-01]
files_modified:
  - frontend/src/pages/WorkflowBuilderPage.tsx
  - frontend/src/components/workflows/BuilderSaveRegion.tsx
  - frontend/src/pages/WorkflowBuilderPage.header.test.tsx
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md
autonomous: true
requirements: [SEED-164]
tags: [SEED-164, frontend, builder, test-run, human-input]
must_haves:
  truths:
    - "The Workflow Studio / Builder header offers a Test Run button for draft workflows"
    - "Clicking Test Run launches an interactive run of the draft definition and opens the Run view"
    - "Vitest count gate and tsc pass cleanly with zero regressions"
  artifacts:
    - path: frontend/src/pages/WorkflowBuilderPage.tsx
      provides: "Test Run handler and launch flow in Builder"
---

<objective>
Add a direct "Test Run" action to the Workflow Studio / Builder header:
1. In `WorkflowBuilderPage.tsx`, wire `onTestRun` that ensures the draft is persisted and triggers a run launch to `/workflows/runs/:run_id`.
2. In `BuilderSaveRegion.tsx` (or `BuilderHeaderBar.tsx`), render the `▶ Test Run` button beside Publish.
3. Update header tests and sync hot file ledgers.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add Test Run Action to Builder Header</name>
  <files>frontend/src/pages/WorkflowBuilderPage.tsx, frontend/src/components/workflows/BuilderSaveRegion.tsx</files>
  <action>
    1. In `WorkflowBuilderPage.tsx`, implement `handleTestRun` which saves the current working draft if dirty, calls `api.createWorkflowRun` or opens the launch flow for the draft `definition_id`, and routes to `/workflows/runs/${run.id}`.
    2. In `BuilderSaveRegion.tsx`, add a `Test Run` button with `data-testid="builder-test-run"`.
  </action>
  <verify>
    <automated>npx vitest run src/pages/WorkflowBuilderPage.header.test.tsx</automated>
  </verify>
  <done>The Test Run button is mounted and triggers run execution for the draft.</done>
</task>

<task type="auto">
  <name>Task 2: Full Suite Verification & Count Gate</name>
  <files>frontend/src/pages/WorkflowBuilderPage.header.test.tsx</files>
  <action>
    Run vitest count gate across all 110 pinned files.
  </action>
  <verify>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>Count gate OK with 0 failures.</done>
</task>

<task type="auto">
  <name>Task 3: Ledger & CLAUDE.md Sync</name>
  <files>docs/HOT-FILE-LEDGER.md, CLAUDE.md</files>
  <action>
    Record Phase 200.3 disposition in `docs/HOT-FILE-LEDGER.md` and verify `node scripts/check-claude-md-size.cjs`.
  </action>
  <verify>
    <automated>node scripts/check-claude-md-size.cjs</automated>
  </verify>
  <done>CLAUDE.md size gate OK.</done>
</task>

</tasks>

<success_criteria>
- Authors can test run any draft workflow from the Builder header.
- All frontend vitest suites and count gate pass.
- Hot file ledger and CLAUDE.md are synchronized.
</success_criteria>
