---
phase: 196-registry-backed-model-picker-canvas
plan: 04
type: execute
wave: 2
depends_on: ["196-01"]
files_modified:
  - backend/app/services/model_registry.py
  - backend/app/api/model_registry.py
  - backend/app/api/admin.py
  - backend/app/main.py
  - backend/tests/test_196_model_registry_route.py
  - frontend/src/lib/api.ts
autonomous: true
requirements: [AUTH-04]

must_haves:
  truths:
    - "D-01: a workflow author can read the LIVE model registry — the union of the code registry and the DB overlay — from a route that does not require operator privilege."
    - "D-02: the union logic is COPIED, not invented — `_registry_row` and the DEF-plus-DB-only loop move verbatim into a leaf service and BOTH callers use the one function."
    - "D-02: `GET /admin/models` is NOT widened. The router-level `require_operator` gate is untouched and still returns a byte-identical 404 to a non-operator — proven by contrast, in the same test file, with the same identity."
    - "D-03: the union size is RE-DERIVED at execution time, never pinned to the 69 measured on 2026-08-17."
    - "D-13: `emit_tier` travels to the client on every union row — the first time this value has ever been on the wire."
    - "The author payload is an explicit ALLOWLIST projection, not a drop-list: `deprecated_reason`, `overridden_fields`, `is_locked`, the timeouts and the window sizes do not travel."
    - "Open Q1 and Q4 are RESOLVED here: the route returns `run_default_model`, computed server-side through the SAME chain a run uses — never `_registry_row.is_default`, which answers a different question, and never a hardcoded `gpt-5.4`."
    - "Open Q3 is RESOLVED: no pagination. 69 rows, fetched once at the Builder page level; revisit at ~500."
    - "The `enabled` semantics are `_registry_row`'s (an absent override row means enabled), stated explicitly so a reviewer comparing this route's count against `/me/preferences`' 34 reads a decision rather than a bug."
  artifacts:
    - path: "backend/app/services/model_registry.py"
      provides: "the union composition leaf plus the allowlist projection"
      exports: ["build_model_registry_rows", "to_author_row"]
      min_lines: 90
    - path: "backend/app/api/model_registry.py"
      provides: "the non-operator top-level GET route"
      min_lines: 40
    - path: "backend/tests/test_196_model_registry_route.py"
      provides: "union size, non-operator 200, admin-still-404, field allowlist, emit_tier on the wire"
      min_lines: 100
  key_links:
    - from: "backend/app/api/model_registry.py"
      to: "backend/app/services/model_registry.py"
      via: "build_model_registry_rows + to_author_row"
      pattern: "build_model_registry_rows"
    - from: "backend/app/api/admin.py"
      to: "backend/app/services/model_registry.py"
      via: "a function-local import inside get_model_registry"
      pattern: "from app.services.model_registry import"
    - from: "backend/app/main.py"
      to: "backend/app/api/model_registry.py"
      via: "app.include_router(model_registry.router)"
      pattern: "model_registry.router"
    - from: "frontend/src/lib/api.ts"
      to: "GET /models/registry"
      via: "getAuthorModelRegistry, unwrapping the envelope in the client"
      pattern: "models/registry"
---

<objective>
Ship the one function that answers *"what models exist"*, and open a non-operator door onto it.

Purpose: **D-01 is LOAD-BEARING and was measured, not reasoned.** Neither shipped list is the live
registry:

| Source | ids | What it misses |
|---|---|---|
| `MODEL_CAPABILITIES` (code) -> `GET /settings` `verified_models` | 61 | all 8 DB-only ids |
| enabled `model_capabilities_overrides` -> `GET /me/preferences` `allowed_models` | 34 | 35 code-registry ids |
| overlap | 26 | — |

`allowed_models` alone cannot offer `gpt-5.5` — **the model SEED-135 measured as the working judge**.
`verified_models` alone cannot offer `gemini-3.6-flash` — **the exact id SEED-135 measured silently
degrading a run to `coerce`** — nor any of the three local LM Studio rows. A picker blind to the model
that caused the seed is not a fix.

⚠ **`GET /admin/models` cannot simply be reused.** It sits behind a router-level `require_operator`
default-deny that returns a byte-identical 404 to a normal author (SC#4 / D-149-09), with **no RLS
backstop** — the test is the only wall. That gate is correct and is not widened; a second, narrower route
is added beside `/features` and `/me/preferences`.

Output: a leaf service owning the union, a non-operator route serving a six-field allowlist projection,
`admin.py` reduced to a thin call, and the typed client.

**Depends on 196-01** for two mechanical reasons, both real: `_registry_row` reads `_MODEL_CAP_COLUMNS`
from `admin.py` module scope and that constant is exactly what D-14 edits; and `emit_tier` must exist as
an overlay-copied value before the union can put it on the wire.

**G-5 / D-22 note:** `backend/app/api/admin.py` is ABSENT from the hot-file ledger at 30 commits / 11
phases — this plan moves code OUT of it, which is the right direction, and plan 196-09 gives it the row
and detail section D-23 now owes.

**Parallel-safety:** every test in this plan patches `app.dependencies._pg_pool` and mutates zero
database rows (the `test_149_registry_read.py` family posture, verified parallel-safe). CLAUDE.md rule 4
does not bind.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/196-registry-backed-model-picker-canvas/196-CONTEXT.md
@.planning/phases/196-registry-backed-model-picker-canvas/196-RESEARCH.md
@.planning/phases/196-registry-backed-model-picker-canvas/196-PATTERNS.md
@.planning/phases/196-registry-backed-model-picker-canvas/196-VALIDATION.md
@.planning/phases/196-registry-backed-model-picker-canvas/196-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Extracted this session. Re-open every file — line numbers rot. -->

backend/app/api/admin.py:1054-1103 — `_registry_row(model_id, cap, ovr, default_model, model_locked)`
  returns THIRTEEN fields:
    model_id · provider · capability_source ("registry"|"db_override") · enabled · deprecated ·
    deprecated_reason · context_window_tokens · max_output_tokens · native_tools ·
    llm_call_timeout_seconds · is_default · is_locked · overridden_fields
  ⚠ It reads TWO module-scope names from admin.py: `_infer_provider_for` and `_MODEL_CAP_COLUMNS`.
  ⚠ WR-04 rule inside it: an absent numeric returns the RAW effective value or None, never a false 0.

backend/app/api/admin.py:1106-1149 — `get_model_registry`: DEF loop over MODEL_CAPABILITIES overlaid
  with overrides.get(id), then a DB-only loop for ids not `seen`. Reads `load_all_model_overrides()`
  (the ALL-ROWS cache — disabled rows INCLUDED) plus `_load_settings_from_db()` for llm_model /
  llm_model_locked. Returns {"models": rows}.
backend/app/api/admin.py:1122-1123 — the Pitfall-4 discipline every admin.py import follows:
  # Function-local imports (Pitfall 4 — keep the settings module off admin's load path).
backend/app/api/admin.py:152-157 — the gate that must NOT be widened:
  router = APIRouter(prefix="/admin", tags=["admin"], dependencies=[Depends(require_operator)])

backend/app/api/me_preferences.py — the non-operator precedent, read in full:
  :36  router = APIRouter(tags=["preferences"])          # no prefix, no router-level gate
  :44-67  @router.get(...) + Depends(get_current_user) + a FLAT server-derived dict
  :87-96  the 400-on-out-of-set refusal shape
  :120  the write returns the FRESH read, so the client is never optimistic
backend/app/main.py — `features.router` and `me_preferences.router` are registered on adjacent lines,
  EACH with a trailing comment recording the gate posture. The api import is ONE flat
  `from app.api import ...` list — add the name to it, never a second import statement.

The Open-Q1/Q4 chain (measured this planning session — this is what a RUN inherits):
  app/models/user_settings.py:947   load_app_settings_async() -> UserEffectiveSettings
  app/services/run_model_resolution.py:~185  apply_user_model_default(user_settings=..., current_user=...)
  app/services/sub_agent_models.py:231       resolve_workflow_ctx_model(user_settings) -> str ("" if None)
  app/services/workflow_kickoff.py:485       model=resolve_workflow_ctx_model(user_settings)   <- ctx.model
  ⚠ `_registry_row.is_default` is stamped from app_settings.llm_model (a DIFFERENT function), which is
    exactly the Open-Q4 mismatch. Do not use it for D-06's clause.

frontend/src/lib/api.ts — the envelope rule to mirror, and the doc-comment that is a RULE:
  getModelRegistry unwraps `.models` IN THE CLIENT; casting the raw object would ship a {models}
  object into list state and crash the next .map (CR-01 precedent).
  ModelRegistryRow.deprecated_reason: "operator context, never shown to end users."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the union composition into a leaf service and repoint admin.py</name>
  <files>backend/app/services/model_registry.py, backend/app/api/admin.py</files>
  <read_first>
    - backend/app/services/run_model_resolution.py (FULL — this is the template. Its module docstring's `PATCH SURFACE` section is the acceptance bar; note `from __future__ import annotations`, the module `logger`, the explicit `__all__` at the bottom, and the late-import discipline)
    - backend/app/api/admin.py:115-165 (the column constants, `_infer_provider_for`, the router gate)
    - backend/app/api/admin.py:1040-1160 (`_registry_row` and `get_model_registry` — the code being moved)
    - backend/app/models/user_settings.py:560-610, :940-960 (`load_all_model_overrides`, `load_app_settings_async`)
    - .planning/phases/196-registry-backed-model-picker-canvas/196-PATTERNS.md §`backend/app/services/model_registry.py` (the verbatim excerpts of what is being moved — do not retype the body from memory)
  </read_first>
  <action>
Create `backend/app/services/model_registry.py` as a leaf: no route, no auth, no FastAPI import.

**Move `_registry_row` VERBATIM** from `admin.py:1054-1103`. Do not retype it and do not make
"while-I'm-in-here" edits — the run_model_resolution extraction's stated discipline is that bodies are
copied unchanged, bugs included. The only addition is the D-14 field: `"emit_tier": _eff("emit_tier")`,
following the WR-04 rule the function already documents — return the RAW effective value or `None`, never
a coalesced false default. The read-time `coerce` default is applied by the CONSUMER, mirroring
`forced_emit.py:376`; do not bake it into the row.

⚠ **Two module-scope names travel with it**: `_infer_provider_for` and `_MODEL_CAP_COLUMNS`. Decide
deliberately whether each MOVES to the leaf or is IMPORTED by it, and record which and why in the
docstring. `_MODEL_CAP_COLUMNS` is the PATCH allowlist that plan 196-01 just extended to eight members
and that `set_model_capability` still needs, so it most likely stays in `admin.py` and the leaf imports
it — but whichever way it goes, the two copies must never diverge, so there must be exactly one
definition.

**Move the union loop VERBATIM** from `admin.py:1129-1143` into
`async def build_model_registry_rows() -> list[dict]`. Import `load_all_model_overrides` and
`_load_settings_from_db` from `app.models.user_settings` — which is what `admin.py:1123` already does.
⚠ Do **not** import them off `app.api.threads` the way `run_model_resolution.py` does; that indirection
exists solely to preserve `test_149`'s patch surface and is a legacy constraint, not the pattern.

Add `def to_author_row(row: dict) -> dict` — the NARROWED projection. Build it as an explicit **allowlist
of the six keys** (`model_id`, `provider`, `capability_source`, `enabled`, `deprecated`, `emit_tier`),
in the `_MODEL_CAP_COLUMNS` habit — **never as a `del` of unwanted keys**. A drop-list silently ships any
field added to `_registry_row` later; that is precisely how migration 116's RLS copy leaked a secret
column in Phase 190 (CR-01). Give it a docstring naming each omitted field and why:
`deprecated_reason` is documented in `api.ts` as *"operator context, never shown to end users"*;
`overridden_fields` and `is_locked` are operator-editor internals; the timeouts and window sizes have no
consumer on this surface and SEED-172 records that a stale `context_window_tokens` on a local row is a
real hazard, so shipping a number nobody validates invites it to be trusted; `is_default` is omitted
because Task 2's `run_default_model` answers the question honestly and `is_default` answers a different
one (Open Q4).

Write the module docstring in the `run_model_resolution.py` register, including a **`PATCH SURFACE`**
section. Before writing it, run `grep -rn "_registry_row\|get_model_registry\|_infer_provider_for" backend/tests/`
— measured at plan time this returns **no importing test**, so the patch surface is currently empty; if
that has changed, re-import the moved name at module scope back into `admin.py` and say so in the
docstring. Add an explicit `__all__`.

Then reduce `admin.py`'s `get_model_registry` to a thin call: keep `request.state.audit_is_write = False`,
keep the route decorator and the response envelope `{"models": ...}` byte-identical, and delegate the
composition through a **function-local** import (Pitfall 4 — keep the settings module off admin's load
path). `GET /admin/models`'s response must be byte-identical to today apart from the new `emit_tier`
field.

⚠ **Do not touch `admin.py:152-157`.** The router-level `require_operator` gate stays exactly as it is.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; ./venv/Scripts/python.exe -m pytest tests/test_149_registry_read.py tests/test_149_model_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/services/model_registry.py` exists, contains `from __future__ import annotations`, a module `logger`, a `PATCH SURFACE` section in its docstring, and an explicit `__all__`.
    - `grep -c 'fastapi\|APIRouter\|Depends' backend/app/services/model_registry.py` is 0 — the leaf imports no web framework.
    - `grep -c 'from app.api' backend/app/services/model_registry.py` is 0 EXCEPT for at most one line importing `_MODEL_CAP_COLUMNS` / `_infer_provider_for` from `app.api.admin`, and if present that line is function-local and its reason is stated in the docstring.
    - `grep -rn '_registry_row\|get_model_registry\|_infer_provider_for' backend/tests/` is run and its output pasted into the summary; if any importing test exists, the re-import is present in `admin.py` and named in the `PATCH SURFACE` section.
    - There is exactly ONE definition of `_registry_row` in the repo (`grep -rn 'def _registry_row' backend/app | wc -l` == 1) and exactly one of `_MODEL_CAP_COLUMNS`.
    - `git diff backend/app/api/admin.py` shows NO change to the `APIRouter(prefix="/admin", ...)` construction.
    - `tests/test_149_registry_read.py` and `tests/test_149_model_gate.py` both pass unchanged — the extraction is behaviour-preserving.
    - `cd backend && ./venv/Scripts/python.exe -m pytest tests/ -q` shows no NEW failures against the pre-plan baseline.
  </acceptance_criteria>
  <done>One function computes the union; `admin.py` calls it and its gate is untouched; the shipped registry tests pass unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add the non-operator route with the allowlist projection and the honest run-default</name>
  <files>backend/app/api/model_registry.py, backend/app/main.py, backend/app/services/model_registry.py, backend/tests/test_196_model_registry_route.py</files>
  <read_first>
    - backend/app/api/me_preferences.py (FULL — the router construction with no prefix and no router-level gate, `Depends(get_current_user)`, the flat server-derived response, and the docstring convention of enumerating endpoints at the bottom)
    - backend/app/main.py (the `features.router` / `me_preferences.router` registration lines WITH their trailing gate-posture comments, and the single flat `from app.api import ...` list)
    - backend/tests/conftest.py:218-277 (`client`, `auth_headers`, `mock_asyncpg_pool`, and the ⚠ note that overriding `require_operator` bypasses `request.state.operator` — drive the identity through the POOL, not through a dependency override)
    - backend/tests/test_149_model_gate.py:24-31 (the byte-identical-404 non-operator assertion, verbatim)
    - backend/tests/test_149_registry_read.py:34-38, :89-140 (`_reset_override_caches` — load-bearing — and `_prime_endpoint`, plus the union assertions to clone)
    - backend/app/services/sub_agent_models.py:231-263 and backend/app/services/run_model_resolution.py:180-215 (the run-default chain)
  </read_first>
  <action>
Create `backend/app/api/model_registry.py`. Construct the router with **no prefix and no router-level
dependency**, mirroring `me_preferences.py:36`. Add a single endpoint `GET /models/registry` guarded only
by `Depends(get_current_user)` — the shipped JWT path. Set `request.state.audit_is_write = False` as the
sibling reads do. Write the module docstring in the house form, enumerating the endpoint and its
response at the bottom.

**Response.** Return a Pydantic response model (not a bare dict) shaped
`{"models": [AuthorModelRow, ...], "run_default_model": str | None}`. The `{"models": [...]}` envelope
deliberately mirrors `/admin/models` so the client's existing *"unwrap `.models` HERE"* lesson transfers
unchanged. Each row is `to_author_row(...)` applied to a `build_model_registry_rows()` row — six fields,
allowlisted.

**`run_default_model` — this is where Open Questions 1 and 4 are answered, and the answer is the point of
the field.** D-06 forbids asserting a fixed default the code does not implement, and a hardcoded
`gpt-5.4` would re-create exactly the lie this phase removes: `app_settings.llm_model` measures
`deepseek-v4-flash` today, the sub-agent default is `gpt-5.4-mini`, and `gpt-5.4` is merely what 18
stored phases happen to carry. So compute the value the RUN would actually inherit, through the same
chain `workflow_kickoff.py:485` uses: load the effective settings with `load_app_settings_async()`,
overlay the caller's own preference with `apply_user_model_default(user_settings=..., current_user=...)`,
then hand the result to `resolve_workflow_ctx_model(...)`. Return `None` when that yields an empty
string. **Do NOT use `_registry_row`'s `is_default`** — it is stamped from `app_settings.llm_model`, a
different function from the one the run uses, which is Open Q4's mismatch stated as a measurement.

Import the three collaborators function-locally and wrap the whole run-default resolution in a
`try/except` that logs and returns `None` on any failure: an honest absence degrades the picker's label
to the bare sentence *"Use the run's model"*, which D-06 explicitly permits, whereas a 500 here would
take the whole picker down over a cosmetic clause.

Register the router in `backend/app/main.py` beside `features.router` and `me_preferences.router`, adding
the name to the existing single flat `from app.api import ...` list — a second import statement would be
a diff no other row has. Give the `include_router` line the same kind of trailing comment its two
siblings carry, recording the gate posture explicitly: Phase 196 / D-01 — the non-operator model-registry
union; NOT operator-gated and NOT under `/admin`, because an author must reach it to pick a model, and
`/admin/models` stays default-deny.

**State the `enabled` semantics in the leaf's docstring.** Measured: two incompatible semantics ship
today. `_registry_row` treats an absent override row as `enabled: true` (66 of 69 rows enabled);
`enabled_model_allowed_set()` treats it as not offerable (34). `_resolve_enabled_model` — the runtime
enforcement — agrees with `_registry_row`. This route adopts `_registry_row`'s semantics so the picker and
the harness cannot disagree about the same model; a picker narrower than the engine is a *different* lie
from the one being removed. ⚠ Say this in the docstring, because a reviewer comparing this route's
enabled count against `/me/preferences`' 34 will otherwise read it as a bug. `enabled_model_allowed_set`
must NOT appear anywhere in this phase's diff outside `me_preferences.py`.

**No pagination and no new cache** (Open Q3). `build_model_registry_rows()` already reads the 30-second
TTL override cache; a second cache layer would create a staleness window nobody can reason about. The
client fetches once at the Builder page level. Record the revisit threshold (~500 rows) in the docstring.

Create `backend/tests/test_196_model_registry_route.py`, cloning the `test_149` fixtures and the
`_prime_endpoint` / `_reset_override_caches` seeding so no case mutates a row. Cover:
- **union size** — assert the response row count EQUALS a value RE-DERIVED in the test from
  `len(MODEL_CAPABILITIES)` plus the seeded DB-only ids. ⚠ Do NOT hardcode 69: it moves whenever an
  operator adds a registry row, and D-03 says re-derive.
- **T-196-AC1** — a plain authenticated non-operator identity gets **200**.
- **T-196-AC2** — the SAME identity, in the SAME file, gets a byte-identical **404** from
  `GET /admin/models`, with `{"detail": "Not Found"}` and a JSON content-type. Proving it by contrast in
  one file is what stops the two claims from drifting apart. There is no RLS backstop on that gate, so
  this test is the only wall.
- **T-196-LEAK** — every row's key set EQUALS the six-key allowlist exactly. Assert on the SET, not with
  individual `not in` checks, so a field added to `_registry_row` next year fails this test instead of
  travelling silently. Additionally assert the serialized response body contains none of the substrings
  `deprecated_reason`, `overridden_fields`, `is_locked`, `llm_call_timeout_seconds`,
  `context_window_tokens`, `max_output_tokens`, `api_key`, `base_url`.
- **D-13 on the wire** — every row carries an `emit_tier` key (value may be `null`), and a seeded DB
  override value wins over the code value.
- **`run_default_model`** — present in the response, and when the settings chain resolves to empty the
  field is `null` rather than a guessed id.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; ./venv/Scripts/python.exe -m pytest tests/test_196_model_registry_route.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/api/model_registry.py` exists; its `APIRouter(...)` call contains no `prefix=` and no `dependencies=`.
    - `grep -c 'require_operator' backend/app/api/model_registry.py` is 0.
    - `backend/app/main.py` contains `model_registry.router` on an `include_router` line with a trailing comment naming the gate posture, and `model_registry` appears inside the existing single `from app.api import ...` list (`grep -c '^from app.api import' backend/app/main.py` is unchanged).
    - `grep -rn 'enabled_model_allowed_set' backend/app/services/model_registry.py backend/app/api/model_registry.py` returns nothing.
    - `grep -c 'is_default' backend/app/services/model_registry.py` shows `is_default` is NOT among `to_author_row`'s allowlisted keys.
    - `grep -c 'resolve_workflow_ctx_model' backend/app/api/model_registry.py` is at least 1; `grep -c "gpt-5.4" backend/app/api/model_registry.py` is 0.
    - The test file contains at least six cases; the union-size case computes its expected number from `MODEL_CAPABILITIES` rather than from a literal, and `grep -c '== 69\|69 ==' backend/tests/test_196_model_registry_route.py` is 0.
    - The AC2 case asserts `res.json() == {"detail": "Not Found"}` AND a JSON content-type, using the same seeded identity as the AC1 case in the same file.
    - The LEAK case asserts key-set EQUALITY against the six-key allowlist.
    - `grep -cE 'INSERT INTO|UPDATE model_capabilities' backend/tests/test_196_model_registry_route.py` is 0.
    - `cd backend && ./venv/Scripts/python.exe -m pytest tests/ -q` shows no NEW failures against the pre-plan baseline.
  </acceptance_criteria>
  <done>An authenticated non-operator can read the union in six allowlisted fields plus an honestly-computed run default; the operator gate is proven un-widened by contrast in the same file.</done>
</task>

<task type="auto">
  <name>Task 3: Type and fetch the author registry in the client</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts (the `ModelRegistryRow` interface, `getModelRegistry` and its envelope-unwrapping doc-comment, and the two doc-comments that are RULES: `deprecated_reason` "never shown to end users" and WR-04's null-not-zero)
    - backend/app/api/model_registry.py (the response model authored in Task 2 — the TS type must match it field for field)
  </read_first>
  <action>
Add a **separate, standalone interface** `AuthorModelRow` to `frontend/src/lib/api.ts`, carrying exactly
six fields: `model_id: string`, `provider: string`, `capability_source: string`, `enabled: boolean`,
`deprecated: boolean`, and `emit_tier: "force_strict" | "force" | "coerce" | null`.

⚠ **It must NOT be `Pick<ModelRegistryRow, ...>` and must NOT extend it.** `ModelRegistryRow` is the
OPERATOR row and carries `deprecated_reason`, which its own doc-comment declares is *"operator context,
never shown to end users"*. A structural allowlist is what stops a field added to the operator row later
from travelling to authors by inheritance. Put that reason in the new interface's doc-comment so the
separation reads as a rule rather than as duplication.

Add `getAuthorModelRegistry()` returning
`Promise<{ models: AuthorModelRow[]; run_default_model: string | null }>`. **Unwrap the envelope in the
client, never in the component** — the shipped `getModelRegistry` doc-comment records why (casting the
raw object would ship a `{models}` object into list state and crash the next `.map`, the CR-01
precedent). Default `models` to `[]` and `run_default_model` to `null` on an absent field. Throw
`ApiError` on a non-ok response, matching the sibling.

Give the function a doc-comment stating what makes it different from `getModelRegistry`: this one is
**not** operator-gated, it returns the six-field author projection, and `run_default_model` is the value
a run would actually inherit — resolved server-side — not `app_settings.llm_model` and not a constant.

⚠ Do not modify `getModelRegistry` or `ModelRegistryRow` in this task; plan 196-01 already added
`emit_tier` there.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0 (the `-p` is load-bearing).
    - `grep -c 'interface AuthorModelRow' frontend/src/lib/api.ts` is 1, and `grep -c 'Pick<ModelRegistryRow\|extends ModelRegistryRow' frontend/src/lib/api.ts` is 0.
    - `AuthorModelRow` has exactly six members; `grep -c 'deprecated_reason' ` within its declaration block is 0.
    - `getAuthorModelRegistry` exists, targets the path `/models/registry`, and returns the unwrapped `{ models, run_default_model }` shape with `?? []` / `?? null` defaults.
    - `git diff frontend/src/lib/api.ts` shows no change to `getModelRegistry`'s body.
  </acceptance_criteria>
  <done>A typed, non-operator client for the union exists, structurally unable to inherit an operator-only field.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated non-operator browser → `GET /models/registry` | data that was operator-only crosses to every author |
| non-operator browser → `GET /admin/models` | must remain a byte-identical 404 with no RLS backstop behind it |
| leaf service → `model_capabilities_overrides` | a global table with no `org_id` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-196-AC1 | Elevation of Privilege / Access Control (ASVS V4) | `GET /models/registry` | mitigate | `Depends(get_current_user)` only, on a router with no prefix and no router-level dependency — the shipped `/features` and `/me/preferences` posture. Tested: an authenticated non-operator receives 200 |
| **T-196-AC2** | Elevation of Privilege (ASVS V4) | `admin.py`'s router-level `require_operator` | mitigate | The gate is NOT widened; a second, narrower route is added. Tested by CONTRAST in the same file with the same seeded identity: 200 on the new route, byte-identical `{"detail": "Not Found"}` 404 on `/admin/models`. ⚠ There is **no RLS backstop** on that gate (SC#4 / D-149-09) — this test is the only wall |
| **T-196-LEAK** | Information Disclosure (ASVS V4) | `to_author_row` | mitigate | An explicit six-key ALLOWLIST, never a drop-list. Asserted as key-set EQUALITY so a field added to `_registry_row` later fails the test rather than travelling. `deprecated_reason` ("never shown to end users"), `overridden_fields`, `is_locked`, timeouts and window sizes are all omitted, each with a stated reason. Migration 116's RLS copy leaked a secret column in Phase 190 (CR-01) precisely because it inherited rather than allowlisted |
| T-196-LEAK2 | Information Disclosure | provider endpoint / key material | accept (verified absent) | All thirteen `_registry_row` fields were enumerated: none carries a base URL, endpoint or key. SEED-172's `_PROVIDER_BASE_URLS` (11, routing) vs `PROVIDER_ENDPOINTS` (8, SSRF allowlist) conflation is untouched by this phase; the allowlist makes an accidental future addition structurally impossible |
| T-196-TENANCY | Information Disclosure | `model_capabilities_overrides` RLS | accept | Measured: RLS is ENABLED and the sole policy is `model_overrides_read_all FOR SELECT TO authenticated USING (true)`; there is no `org_id` column. Every authenticated user can already SELECT every row at the RLS layer. The route exposes nothing RLS forbids; the genuine delta is the COMPOSITION, contained by the projection. No `org_id` filter is needed and a reviewer expecting one should be pointed at this measurement |
| T-196-DOS | Denial of Service | the unpaginated read | accept | 69 rows, served from the existing 30 s TTL cache; `/admin/models` is already documented as a floor-exempt poll-style read. Revisit at ~500 rows (Open Q3) |
| T-196-SC | Tampering | npm/pip/cargo installs | accept | No packages installed by this phase |
</threat_model>

<verification>
- `cd backend && ./venv/Scripts/python.exe -m pytest tests/test_196_model_registry_route.py tests/test_149_registry_read.py tests/test_149_model_gate.py -q`
- `cd backend && ./venv/Scripts/python.exe -m pytest tests/ -q` — no new failures vs the recorded baseline
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json`
- Re-derive and RECORD the live union size against the local DB using the RESEARCH.md M-1 command; state it as a reading, not a target, and note whether it still equals the 69 measured on 2026-08-17
</verification>

<success_criteria>
- One function composes the union; `/admin/models` and `/models/registry` both call it
- An authenticated non-operator reads the union; the same identity still gets a byte-identical 404 from `/admin/models`
- The author payload is exactly six allowlisted fields, asserted by set equality
- `emit_tier` is on the wire for the first time
- `run_default_model` is computed through the run's own chain and is `null` rather than guessed when it cannot resolve
- No pagination, no second cache, no `enabled_model_allowed_set` anywhere in the diff
</success_criteria>

<output>
Create `.planning/phases/196-registry-backed-model-picker-canvas/196-04-SUMMARY.md` when done.
Record verbatim: the `grep` output for existing importers of `_registry_row` / `get_model_registry`, the
decision on where `_MODEL_CAP_COLUMNS` and `_infer_provider_for` live and why, the re-derived union size
with its command, the `enabled`-semantics statement, and the backend baseline and post numbers.
</output>
