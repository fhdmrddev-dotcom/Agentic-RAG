---
phase: 196-registry-backed-model-picker-canvas
plan: 07
type: execute
wave: 4
depends_on: ["196-04"]
files_modified:
  - backend/app/api/settings.py
  - backend/tests/test_196_providers_disabled_models.py
  - frontend/src/lib/api.ts
  - frontend/src/hooks/useComposerModel.ts
  - frontend/src/hooks/__tests__/useComposerModel.test.ts
  - frontend/src/components/chat/ChatArea.tsx
  - frontend/src/components/chat/__tests__/ChatArea.model.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [BUG-260718-04]

must_haves:
  truths:
    - "D-18: a thread restores its own last-used model — derived from its most recent run-backed message, with no new storage, no schema change and no migration."
    - "D-18: `undefined` (user-role messages, which carry no run row) and the literal `'unknown'` (121 live `runs` rows) are BOTH skipped when deriving the last-used model."
    - "D-18 / D-07: a last-used model that is no longer enabled falls back — reusing D-07's disabled rule rather than inventing a second one."
    - "D-18: `provider` is restored BEFORE `model`, or `handleProviderChange` clobbers the restore and the bug appears fixed on mount and broken after any provider interaction."
    - "The restore is best-effort BY DESIGN: a thread whose last message predates run-backed attribution shows the global default, and that is not a failure."
    - "G-5 on `ChatArea.tsx` is honoured by REDUCTION, not by argument: `useState` goes 7 -> 2 and `useEffect` 4 -> 3, measured before and after. A naive implementation would have taken `useEffect` 4 -> 5 and failed the ledger's own recorded 194.1 test."
    - "The ledger's hard fence on `ChatArea.tsx` survives: `grep -c onStop` stays 0."
    - "A failed derivation resolves to a distinct reading, never to an empty success — the shipped leaf-hook rule."
  artifacts:
    - path: "frontend/src/hooks/useComposerModel.ts"
      provides: "the composer's provider/model state machine, lifted out of ChatArea, with the restore rung prepended"
      min_lines: 80
    - path: "frontend/src/components/chat/__tests__/ChatArea.model.test.tsx"
      provides: "the integration half — the composer reflects the restored model"
      min_lines: 40
  key_links:
    - from: "frontend/src/components/chat/ChatArea.tsx"
      to: "frontend/src/hooks/useComposerModel.ts"
      via: "one destructure replacing five useState and one useEffect"
      pattern: "useComposerModel"
    - from: "frontend/src/hooks/useComposerModel.ts"
      to: "GET /providers disabled_models"
      via: "the enabled check that reuses D-07's rule"
      pattern: "disabled"
---

<objective>
Make a chat thread remember the model it was actually using.

Purpose: `BUG-260718-04` (`severity: major`, folded into this phase as **D-18**). Today `selectedModel`
is ephemeral state seeded from the global default (`ChatArea.tsx:63`, `:154-156`) and reset on provider
change (`:201`); the gap is purely restore-on-load. This is the third face of the same defect: a control
that lies about what will actually run.

⚠ **D-18's premise sentence is imprecise in a way that changes the implementation, and the correction is
measured.** `public.messages` has **no `model` column**. The model lives on `public.runs.model` and is
JOIN-stamped onto assistant messages as `MessageResponse.model` (Phase 095.1-03), which the frontend
`Message` type already declares and `api.ts` already maps. The decision stands and is still
zero-migration — but the derivation must skip `undefined` (user-role messages have no run row) **and**
the **121 live rows** reading `'unknown'`, and it must restore `provider` FIRST or `ChatArea.tsx:201`
clobbers it.

⚠ **This is the one place in this phase where G-5 genuinely bites.** `ChatArea.tsx` measures
**61 commits / 29 phases / 587 L**; the hot-file ledger calls it *"the strongest extraction case on the
frontend side"* and records Phase 194.1's honouring as a measured test: *"`useState[(<]` 7 → 7 ·
`useEffect(` 4 → 4 · props 8 → 8 — every one unmoved."* **A naive D-18 adds a `useEffect` and fails that
exact test (4 → 5).** So this plan takes a named narrow seam instead and honours G-5 by REDUCING the
counts: `useState` **7 → 2**, `useEffect` **4 → 3**. A file that loses five state hooks has honoured G-5
by any reading. Baselines re-measured at plan time: `useState` **7**, `useEffect` **4**, `onStop` **0**,
**587** lines.

Output: one additive wire field, one new leaf hook owning the composer's model/provider machine, a
shrunken `ChatArea.tsx`, and two suites brought inside the count gate.

⚠ **File collision, flagged:** `frontend/src/lib/api.ts` is touched by plans 196-01, 196-04 and this one.
Worktrees isolate files, not merges — the three are deliberately in three different waves for that reason.
Confirm both prior versions are present at HEAD before editing.

⚠ **SC#3 fence:** this plan touches the chat composer, which is in scope as surface 3. It must NOT touch
`frontend/src/pages/SettingsPage.tsx` or `frontend/src/components/settings/ModelPillRow.tsx`, and it must
not change `verified_models` or `deprecated_models`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/196-registry-backed-model-picker-canvas/196-CONTEXT.md
@.planning/phases/196-registry-backed-model-picker-canvas/196-RESEARCH.md
@.planning/phases/196-registry-backed-model-picker-canvas/196-PATTERNS.md
@.planning/phases/196-registry-backed-model-picker-canvas/196-VALIDATION.md
@.planning/reported-bugs/BUG-260718-04.md
@docs/HOT-FILE-LEDGER.md
@CLAUDE.md

<interfaces>
<!-- Extracted this session. Re-open every file — line numbers rot. -->

backend/app/api/settings.py:579-601 — GET /providers, the handler this plan extends by ONE field.
  It already does the identical read one line away:
    from app.models.user_settings import load_all_model_overrides   # function-local (Pitfall 4)
    _overrides = await load_all_model_overrides()
    deprecated_models = sorted(mid for mid, cap in _overrides.items() if cap.get("deprecated"))
    return {"active": ..., "active_model": s.llm_model, "providers": configured,
            "deprecated_models": deprecated_models}
  The `configured` comprehension filters only on `if p.api_key` — a provider's `models` list is the
  OPERATOR's configured list and is NOT filtered by registry `enabled`. That is why the enabled check
  needs a wire field rather than a membership test.

frontend/src/components/chat/ChatArea.tsx — the five useState being moved (:59-65):
  providers, selectedProvider, models, selectedModel, deprecatedModels
The load effect (:142-157) — it ALREADY contains the rule D-18 prepends to:
  const preferred = active_model AND activeProvider.models.includes(active_model)
                      ? active_model : (activeProvider.models[0] ?? "")
The clobberer (:198-205) — Pitfall 6 in source form:
  const handleProviderChange = (providerId) => {
    setSelectedProvider(providerId)
    const p = providers.find(x => x.id === providerId)
    if (p) { setModels(p.models); setSelectedModel(p.models[0] ?? "") }
  }
ChatArea.tsx:52-58 already consumes useMessages(); :320 sends selectedModel per message.

frontend/src/types/index.ts:178 — Message.model?: string   (already on the type)
frontend/src/lib/api.ts:219-220 — model: model ?? undefined, provider: provider ?? undefined

The two partial analogs for the hook:
  frontend/src/hooks/useFollowScroll.ts — state lifted out of a hot chat component; an EXPORTED NAMED
    return type, and a docblock spelling the state machine as bullets before any code.
  frontend/src/hooks/useTemplatePlaceholders.ts:1-18 — "a failed read resolves to a distinct union
    member rather than to an empty success."

Measured baselines for ChatArea.tsx at plan time (re-derive before and after):
  grep -c 'useState[(<]'  -> 7        grep -c 'useEffect('  -> 4
  grep -c 'onStop'        -> 0        wc -l                 -> 587
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship the disabled-model set on the providers payload</name>
  <files>backend/app/api/settings.py, backend/tests/test_196_providers_disabled_models.py, frontend/src/lib/api.ts</files>
  <read_first>
    - backend/app/api/settings.py:575-605 (the whole `get_providers` handler and the `deprecated_models` comprehension one line above the return)
    - backend/app/models/user_settings.py:560-605 (`load_all_model_overrides` — the ALL-ROWS cache including disabled rows, and its never-raises property)
    - frontend/src/lib/api.ts (the `getProviders` return type and its mapping)
    - backend/tests/test_149_registry_read.py:34-38, :89-102 (the zero-mutation seeding)
  </read_first>
  <action>
Add one field, `disabled_models`, to the `GET /providers` response in `backend/app/api/settings.py`. Build
it with the same comprehension shape as the `deprecated_models` line directly above it, over the same
already-fetched `_overrides` dict, selecting ids whose `enabled` is explicitly `False`, sorted. No new
read, no new import, no new cache.

⚠ **Why a wire field rather than a client-side membership test — a real decision, stated.** A provider's
`models` list in this payload is the operator's configured list and is **not** filtered by registry
`enabled`, so a disabled model can appear there. D-18 requires the restore to fall back when the last-used
model is no longer enabled, and D-18 says to reuse D-07's rule rather than invent a second one — which
means the client needs the same `enabled is False` set the registry uses. This is the cheapest honest
source, in the handler that already performs exactly that read.

⚠ **Semantics must match `_registry_row`'s, not `enabled_model_allowed_set`'s.** Only an override row whose
`enabled` is explicitly `False` counts as disabled; an ABSENT override row is enabled.
`enabled_model_allowed_set` must not appear in this plan's diff.

⚠ Do NOT change `deprecated_models`, `active`, `active_model` or `providers`. Deprecated is not disabled —
a deprecated model stays selectable (D-149-05) — and `verified_models` on the `/settings` route is out of
scope entirely (SC#3). This plan makes the composer restore honest; it does not filter the composer's
offered list, which belongs to the deferred app-wide sweep.

Add `backend/tests/test_196_providers_disabled_models.py` asserting: the field is present and sorted; a row
with `enabled=False` appears in it; a row with `enabled=True` does not; a model with no override row does
not; and `deprecated_models` is byte-unchanged for the same seeded input. Seed through the patched pool —
mutate no rows.

Mirror the field on the client: add `disabled_models` to `getProviders`'s return type in
`frontend/src/lib/api.ts`, defaulting to `[]` when absent — exactly as `deprecated_models` already does.
The defensive-absent comment already in `ChatArea.tsx` records why: an older backend or a read blip must
degrade to no badge, never to a crash.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; ./venv/Scripts/python.exe -m pytest tests/test_196_providers_disabled_models.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'disabled_models' backend/app/api/settings.py` is at least 2 (the comprehension and the return key).
    - `git diff backend/app/api/settings.py` contains no change to the `deprecated_models` line, no change to the `configured` comprehension, and no change anywhere in the `/settings` route — `verified_models` is untouched.
    - `grep -c 'enabled_model_allowed_set' backend/app/api/settings.py` equals its pre-plan value.
    - The new backend test contains at least five cases including the "absent override row is NOT disabled" case, and `grep -cE 'INSERT INTO|UPDATE model_capabilities' ` on it is 0.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` exits 0, and `disabled_models` is on `getProviders`'s return type with a `?? []` default.
    - `git diff --name-only` contains neither `SettingsPage.tsx` nor `ModelPillRow.tsx`.
  </acceptance_criteria>
  <done>The client can tell which models an operator has disabled, from the handler that was already reading the overrides.</done>
</task>

<task type="auto">
  <name>Task 2: Extract the composer's model/provider machine into a leaf hook — behaviour-preserving</name>
  <files>frontend/src/hooks/useComposerModel.ts, frontend/src/components/chat/ChatArea.tsx</files>
  <read_first>
    - frontend/src/components/chat/ChatArea.tsx:50-80 (the five useState), :135-165 (the provider load effect), :190-215 (`handleProviderChange`), :310-330 (the per-message send that reads `selectedModel`)
    - frontend/src/hooks/useFollowScroll.ts:1-22, :40-105 (the exported named return type and the state-machine-as-bullets docblock)
    - frontend/src/hooks/useTemplatePlaceholders.ts:1-40 (the leaf-hook discipline and the failed-read-is-a-distinct-member rule)
    - docs/HOT-FILE-LEDGER.md#frontendsrccomponentschatchatareatsx (the recorded 194.1 measurement and the `onStop` fence)
  </read_first>
  <action>
**Measure first.** Record `grep -c 'useState[(<]'`, `grep -c 'useEffect('`, `grep -c 'onStop'` and
`wc -l` on `ChatArea.tsx` BEFORE any edit and paste them into the summary. Plan-time readings were
7 / 4 / 0 / 587; re-derive rather than trusting them.

Create `frontend/src/hooks/useComposerModel.ts`. Move — verbatim, no "while-I'm-in-here" edits — the five
`useState` declarations (`providers`, `selectedProvider`, `models`, `selectedModel`, `deprecatedModels`),
the provider-load `useEffect`, and `handleProviderChange`. Add `disabledModels` alongside
`deprecatedModels`, sourced from Task 1's field with the same defensive `?? []`.

Give the hook an **exported named return type** (the `useFollowScroll` habit) rather than an inline object
type, and a docblock spelling the state machine as bullets before any code. Those bullets are the D-18
fallback chain, written down so the next reader does not reconstruct it:
last-used enabled model -> the existing `active_model` global-default seed -> `activeProvider.models[0]`
-> `""`. **That is the shipped chain with one rung prepended** — say so, because the existing `preferred`
ternary at `ChatArea.tsx:151-153` already implements *"use the stored value iff it is in the current
provider's list, else fall back"*, which is the same shape D-07's enabled check needs. Prepend a rung; do
not author a second rule.

**This task is behaviour-preserving. Do NOT add the restore yet** — that is Task 3. Shipping the extraction
and the feature in one step makes any regression un-attributable, which is the whole reason the two are
separate tasks.

In `ChatArea.tsx`, replace the moved code with **one destructure** of the hook's return. Change nothing
else. In particular touch nothing named `onStop`: the ledger records a hard fence that
`grep -c "onStop" ChatArea.tsx` must stay **0**. This extraction touches nothing named that — confirm it,
do not assume it.

**Measure again** and record the after values. The bar is `useState` **7 -> 2** and `useEffect` **4 -> 3**,
with `onStop` still **0**. If the numbers do not land there, STOP and report rather than adjusting the
target: the point of taking this seam is that G-5 is honoured by arithmetic, not by argument.

Run the existing chat suites to prove nothing moved behaviourally.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit -p tsconfig.app.json &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/chat</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'useState[(<]' frontend/src/components/chat/ChatArea.tsx` is **2**, and `grep -c 'useEffect(' ` is **3** — both recorded beside their pre-edit values.
    - `grep -c 'onStop' frontend/src/components/chat/ChatArea.tsx` is **0**.
    - `grep -c 'useComposerModel' frontend/src/components/chat/ChatArea.tsx` is exactly 2 (the import and the one destructure).
    - `frontend/src/hooks/useComposerModel.ts` exports a NAMED return type (not an inline object type) and its docblock lists the fallback chain as bullets.
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0.
    - `GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/chat` passes with **no decrease** in any per-file case count versus the pre-edit run — both runs recorded.
    - `git diff frontend/src/components/chat/ChatArea.tsx` shows no behavioural edit beyond the removal and the destructure (no changed conditional, no changed prop).
  </acceptance_criteria>
  <done>Five state hooks and one effect live in a leaf hook; `ChatArea.tsx` measures 2 and 3 with `onStop` still 0; behaviour is unchanged and proven so.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the restore rung, its tests, and the two gate adoptions</name>
  <files>frontend/src/hooks/useComposerModel.ts, frontend/src/hooks/__tests__/useComposerModel.test.ts, frontend/src/components/chat/__tests__/ChatArea.model.test.tsx, scripts/vitest-count-gate.cjs</files>
  <behavior>
    - Given a thread whose most recent run-backed message carries `model: "gpt-5.5"` and `provider: "openai"`, and `gpt-5.5` is not in `disabledModels`, the composer's selected model is `gpt-5.5` and the selected provider is `openai`.
    - ⚠ `provider` is applied BEFORE `model` — asserted by ordering, not only by the end state, so a future refactor that reverses them fails here rather than in production.
    - Messages whose `model` is `undefined` (user-role rows) are skipped; the derivation walks backwards past them.
    - Messages whose `model` is the literal `"unknown"` are skipped — 121 live `runs` rows carry it.
    - A last-used model present in `disabledModels` falls back to the next rung and does NOT become the selection.
    - A thread with no run-backed message at all falls back to the existing `active_model` seed — and that is a SUCCESS, not a failure: the restore is best-effort by design.
    - A failed providers read resolves to a distinct reading, never to an empty success that looks like a correct render.
    - Switching provider after a restore still clobbers the model to that provider's first — the shipped `handleProviderChange` behaviour is unchanged, so a restore does not silently make provider switching stop working.
  </behavior>
  <read_first>
    - frontend/src/hooks/useComposerModel.ts (as extracted in Task 2)
    - frontend/src/hooks/useMessages.ts and frontend/src/types/index.ts:165-215 (`Message.model` / `Message.provider`)
    - frontend/src/hooks/__tests__/ (any existing hook test in this directory, for the local harness conventions)
    - frontend/src/components/chat/ChatArea.tsx:50-70 (how `useMessages` and the hook now sit together)
    - scripts/vitest-count-gate.cjs:2280-2400 and the `:2660-2670` comment recording that `src/components/chat` has NO bare-directory entry
  </read_first>
  <action>
Add the restore rung to `useComposerModel.ts`. The hook takes the thread's messages (already held by
`useMessages`, already consumed by `ChatArea`) and derives:

> the LAST message in thread order whose `model` is truthy AND is not the literal `"unknown"`

then applies `provider` first and `model` second. ⚠ **Order is load-bearing.** `handleProviderChange` does
`setSelectedModel(p.models[0] ?? "")`, so restoring the model without the provider is clobbered on the next
provider read — the bug then appears fixed on mount and broken after any provider interaction (Pitfall 6).

Apply the enabled check using Task 1's `disabledModels`, reusing D-07's rule: a model whose id is in that
set is not restored and the chain falls through to the next rung. Do NOT invent a second availability
notion, and do NOT use `allowed_models` semantics.

⚠ **Model the failure honestly.** Follow `useTemplatePlaceholders`'s rule — a failed providers read resolves
to a distinct member, never to an empty success. An empty model list from a failed fetch is otherwise
indistinguishable from a correctly-rendered empty composer.

⚠ **Record the known blind spot in the docblock, named in the bug's own re-open trigger:** a thread whose
last message predates run-backed attribution has no model, the fallback chain handles it, and a thread
showing the global default is **not necessarily a failure**. The restore is best-effort by design. Saying
so in the code is what stops a future reader from "fixing" it with storage the decision deliberately
declined (option (b) of the report).

Write `frontend/src/hooks/__tests__/useComposerModel.test.ts` covering every bullet in `<behavior>`, driven
RED first. Mock `@/lib/api`'s `getProviders`; feed messages as a plain array. The ordering assertion for
provider-before-model should observe the sequence of state transitions, not merely the final values.

Write `frontend/src/components/chat/__tests__/ChatArea.model.test.tsx` for the integration half: rendering
`ChatArea` for a thread whose last run-backed message names a non-default model shows that model as the
composer's selection. Keep it thin — `ChatArea` is expensive to render, which is one of the reasons the
logic lives in a hook. The hook suite is where the branch coverage belongs.

Finally, adopt both suites into the count gate. Measured: `TARGETS` has **no** bare-directory entry for
`src/components/chat` (the file's own comment at ~`:2664` records this) and **no** entry for `src/hooks`
beyond what plan 196-05 added. So both of these suites are **UNGATED** until added. Add each to `TARGETS`
**and** pin each in `BASELINE`, in this same commit — a `BASELINE` key naming a path that does not yet
exist makes the gate ERROR, so the pin belongs in the creating commit and nowhere else. Write the stated
reason in the script's own comment register for each: these two suites are the only guards on D-18's
derivation, and an unpinned file is not a lightly-guarded one, it is an unguarded one.

⚠ **Set every pin from the gate's own printed `actual` column, never from a number quoted in any document**
— including this one.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit -p tsconfig.app.json &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/hooks/__tests__/useComposerModel.test.ts src/components/chat/__tests__/ChatArea.model.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The hook test contains at least eight cases, one per `<behavior>` bullet, and the provider-before-model case asserts an ORDER, not only an end state.
    - `grep -c "unknown" frontend/src/hooks/useComposerModel.ts` is at least 1 and the literal is compared against, not merely mentioned in a comment.
    - `grep -c 'disabled' frontend/src/hooks/useComposerModel.ts` is at least 1 and the check consults the set from `getProviders`, not `allowed_models`.
    - The hook's docblock contains the best-effort-by-design statement and names the blind spot.
    - `grep -c 'useState[(<]' frontend/src/components/chat/ChatArea.tsx` is still **2** and `grep -c 'useEffect(' ` still **3** after this task — the restore added no hook to the guarded file.
    - `grep -c 'onStop' frontend/src/components/chat/ChatArea.tsx` is still **0**.
    - `scripts/vitest-count-gate.cjs` contains `src/hooks/__tests__/useComposerModel.test.ts` and `src/components/chat/__tests__/ChatArea.model.test.tsx` in BOTH `TARGETS` and `BASELINE`, each with an adjacent stated reason.
    - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` is run and its verdict line pasted verbatim into the summary, paired with the per-file deltas for the two new suites. ⚠ Do NOT make "the gate is green" the bar on its own. If it reds: capture the failing filenames from the gate's persisted JSON BEFORE re-running, check `git diff --numstat` and `git status --short`, and if the file is byte-unchanged and one of SEED-171's three (`src/pages/WorkflowsPage.test.tsx`, `src/components/workflows/library/WorkflowCard.test.tsx`, `src/pages/WorkflowBuilderPage.session.test.tsx`) record it as an observation and write "provably unmodified", never "fine". ⚠ Do NOT adjust `GSD_VITEST_MAX_WORKERS` — SEED-171 measures that adjusting the cap does not fix these.
    - `git diff --name-only` contains neither `SettingsPage.tsx` nor `ModelPillRow.tsx`.
  </acceptance_criteria>
  <done>A thread restores its last-used enabled model, provider first, skipping `undefined` and `"unknown"`; the guarded file's hook counts are unmoved by the feature; both suites are inside the gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `GET /providers` payload → composer state | server-derived model ids and the new disabled set cross into a control that selects which provider receives a prompt |
| thread message history → composer selection | a value derived from prior runs pre-selects the model for the next send |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-196-CMP1 | Spoofing (a control that lies) | `selectedModel` on thread load | mitigate | The composer restores the model the thread actually used, derived from run-backed attribution, with `provider` applied first so the restore is not clobbered. Asserted by an ORDER, not by an end state |
| T-196-CMP2 | Tampering | restoring a disabled model | mitigate | The enabled check reuses D-07's rule against the new `disabled_models` set — `_registry_row` semantics, not `allowed_models` semantics, so the composer cannot disagree with the runtime |
| T-196-CMP3 | Information Disclosure | the new `disabled_models` field | accept | It exposes which model ids an operator has disabled. `deprecated_models` already ships the analogous set to every chat user on the same payload, and `/me/preferences` already exposes an enabled set. No new class of information; no key, endpoint or reason string travels |
| T-196-CMP4 | Denial of Service (availability) | a failed providers read | mitigate | Resolves to a distinct reading rather than to an empty success, so a read failure cannot masquerade as a correctly-rendered empty composer |
| T-196-CMP5 | Tampering | `ChatArea.tsx` extraction | mitigate | Task 2 is behaviour-preserving and separate from Task 3, so any regression is attributable; the existing chat suites must show no per-file case decrease, and the `onStop` fence is re-asserted after both tasks |
| T-196-SC | Tampering | npm/pip/cargo installs | accept | No packages installed by this phase |
</threat_model>

<verification>
- `cd backend && ./venv/Scripts/python.exe -m pytest tests/test_196_providers_disabled_models.py -q`
- `cd backend && ./venv/Scripts/python.exe -m pytest tests/ -q` — no new failures vs the recorded baseline
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — the `-p` is load-bearing
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/hooks/__tests__/useComposerModel.test.ts src/components/chat` — the deterministic in-scope suites
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` — verdict line recorded verbatim with per-file deltas; SEED-171 triage protocol applies if it reds
- The `ChatArea.tsx` triple (`useState` / `useEffect` / `onStop`) recorded before Task 2, after Task 2 and after Task 3
- ⚠ NOT provable here: *"a thread restores its model across REFRESH"*. jsdom has no page reload; a hook test proves the derivation, not the lifecycle. That is G-4 row **U-C1** in `196-VALIDATION.md`, driven by Chrome MCP at phase verification
</verification>

<success_criteria>
- A thread restores its last-used enabled model, provider first
- `undefined` and `"unknown"` are skipped; a disabled last-used model falls back
- `ChatArea.tsx` measures `useState` 2 and `useEffect` 3 with `onStop` 0 — G-5 honoured by reduction, arithmetic recorded
- The extraction is behaviour-preserving and separately verified from the feature
- Both new suites are inside the count gate with pins read from the gate's own output
- No settings surface is touched; `verified_models` and `deprecated_models` are unchanged
</success_criteria>

<output>
Create `.planning/phases/196-registry-backed-model-picker-canvas/196-07-SUMMARY.md` when done.
Record verbatim: the `ChatArea.tsx` triple at three points (pre-Task-2, post-Task-2, post-Task-3), the
Task 3 RED output, each suite's case count, the gate verdict line with per-file deltas, any SEED-171
triage observations stated as "provably unmodified", and the backend baseline and post numbers.
</output>
