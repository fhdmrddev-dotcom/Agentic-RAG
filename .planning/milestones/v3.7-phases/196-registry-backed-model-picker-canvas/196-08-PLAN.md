---
phase: 196-registry-backed-model-picker-canvas
plan: 08
type: execute
wave: 5
depends_on: ["196-05"]
files_modified:
  - frontend/src/pages/WorkflowBuilderPage.tsx
  - frontend/src/components/workflows/PhaseFormPanel.tsx
  - frontend/src/components/workflows/PhaseFormPanel.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [AUTH-04]

must_haves:
  truths:
    - "AUTH-04 is TRUE on the canvas: all four `AI model` free-text inputs are gone, replaced by registry-backed pickers — there is no path through the step form that accepts a typed model name."
    - "D-20: the panel mounts the picker as EXACTLY FOUR one-line gated mounts, one per AI phase type, each forwarding a single spread prop whole — the shape the file's standing order has demanded since Phase 185 and 193.1 made mechanical."
    - "D-20: the panel COMPUTES NOTHING for the picker. Its body contains zero `useMemo`, zero `useState` and zero `useEffect` — an ABSOLUTE zero, since all three measure 0 today, not merely a non-increase."
    - "The 193.1 fence is scoped to `<TemplateNameCheck` and will NOT fire on a `ModelField` mount — so this phase writes its OWN fence, in the same shape, expecting four mounts, with a positive control and a non-vacuity floor."
    - "The registry is fetched ONCE, at the panel's owner, through the leaf hook — never four times inside the panel."
    - "`SelectField` is NOT widened to take option groups; widening it would put picker logic back inside the guarded file."
  artifacts:
    - path: "frontend/src/components/workflows/PhaseFormPanel.test.tsx"
      provides: "the NEW four-mount source fence scoped to <ModelField, plus the absolute zero-compute assertion"
      contains: "<ModelField"
  key_links:
    - from: "frontend/src/pages/WorkflowBuilderPage.tsx"
      to: "frontend/src/hooks/useModelRegistry.ts"
      via: "one call line, matching useTemplatePlaceholders and useGroundingBundle"
      pattern: "useModelRegistry"
    - from: "frontend/src/components/workflows/PhaseFormPanel.tsx"
      to: "frontend/src/components/workflows/ModelField.tsx"
      via: "four one-line gated mounts forwarding the prop whole via spread"
      pattern: "<ModelField"
---

<objective>
Mount the picker — four lines, and not one line more.

Purpose: this is the plan that makes AUTH-04 visibly true. The four free-text `AI model` `TextField`s at
`PhaseFormPanel.tsx:877`, `:913`, `:965` and `:1067` become four registry-backed `ModelField` mounts.

⚠ **The panel is under a standing order and a mechanical fence, and this plan's whole difficulty is
respecting them.** `PhaseFormPanel.tsx` measures **16 commits / 8 phases / 1167 L** — G-5 fires — and its
ledger row closes with an instruction rather than a status: *"the next surface that needs the panel gets
its own component and one gated line."* It has been honoured three times running (185 / 193 / 193.1), the
last **mechanically**, by a source fence that splits the panel's `?raw` text and asserts exactly one mount
line carrying both the `pt ===` guard and the spread prop. This phase REPLACES an existing field rather
than adding a second concern, and follows the same shape.

⚠ **The shipped fence will not protect this phase.** It is scoped to `<TemplateNameCheck`. A `ModelField`
mount passes through it invisibly, and the panel could grow a `useMemo` to build the option list with the
existing fence noticing nothing. **This phase owes its own fence**, expecting **four** mounts.

⚠ **Measured at plan time, and this makes the criterion stronger than a delta:** `PhaseFormPanel.tsx`
contains **zero** `useMemo(`, **zero** `useState(` and **zero** `useEffect(` today. So the acceptance bar
is an ABSOLUTE zero after this plan, not a non-increase.

Output: one hook call and one prop pass at the owner, one prop + one destructure + four gated lines in the
panel, and the new fence.

⚠ **File collision, flagged:** `scripts/vitest-count-gate.cjs` is touched by plans 196-03, 196-05, 196-07
and this one. Worktrees isolate files, not merges — the four sit in four different waves for exactly that
reason. Confirm the prior versions are present at HEAD before editing, and append in this file's own
region rather than resolving a merge.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/196-registry-backed-model-picker-canvas/196-CONTEXT.md
@.planning/phases/196-registry-backed-model-picker-canvas/196-RESEARCH.md
@.planning/phases/196-registry-backed-model-picker-canvas/196-PATTERNS.md
@.planning/phases/196-registry-backed-model-picker-canvas/196-VALIDATION.md
@.planning/phases/196-registry-backed-model-picker-canvas/196-05-SUMMARY.md
@docs/HOT-FILE-LEDGER.md
@CLAUDE.md

**Project skill:** `Skill("sketch-findings-agentic-rag")` — the 024-A picker idiom and SEED-085's
two-audience vocabulary. ⚠ G-2 fired and was DECLINED on a reason (D-21): the idiom ships twice already,
so the acceptance bar is the shipped components, not a mockup.

<interfaces>
<!-- Extracted this session. Re-open every file — line numbers rot. -->

The SHIPPED example the fence rewards — PhaseFormPanel.tsx:1116:
  {nameCheck AND pt === "llm_emit" AND <TemplateNameCheck {...nameCheck} />}
  (written here with AND standing in for the JSX logical-and operator)

The shipped fence — PhaseFormPanel.test.tsx:480-488, with its ?raw import at :22-24:
  import phaseFormPanelSource from "./PhaseFormPanel?raw"
  it("SOURCE — the mount really is ONE gated line and the panel computes nothing for it", () => {
    const mounts = phaseFormPanelSource.split("\n").filter(l => l.includes("<TemplateNameCheck"))
    expect(mounts).toHaveLength(1)
    expect(mounts[0]).toContain('pt === "llm_emit"')
    expect(mounts[0]).toContain("{...nameCheck}")
  })

The one-new-prop threading, all three touch points — the WHOLE cost the fence's comment names
("an import, a prop, a destructure and one line"):
  PhaseFormPanel.tsx:202-204   the prop, on PhaseFormPanelProps
  PhaseFormPanel.tsx:760       the destructure, one identifier in the existing list
  PhaseFormPanel.tsx:1116      the mount

The four fields being replaced — PhaseFormPanel.tsx:876-884 is the llm_single one; the other three are
byte-similar at :912-920, :964-972, :1066-1074:
  <TextField label="AI model" qualifier="(optional — uses the default if blank)"
             hint="model — pick a specific model, or leave blank to use the workspace default."
             help="Leave blank to use the workspace default."
             value={asStr(cfg.model)} onChange={set("model")} onPersist={onPersist} />

The two sibling leaf hooks already consumed at the owner, ONE call line each:
  frontend/src/pages/WorkflowBuilderPage.tsx:982   const templateFields = useTemplatePlaceholders(...)
  frontend/src/pages/WorkflowBuilderPage.tsx:1142  const bundle = useGroundingBundle(canvasEnabled)

Two sibling source assertions to re-read before adding any TEXT to the panel:
  PhaseFormPanel.rails.test.tsx:485-497  capability names are ABSENT from the panel source
  PhaseFormPanel.rails.test.tsx:500      `<ToolsField` appears exactly TWICE
  Neither is affected by a ModelField mount, but a new LABEL STRING might trip the first.

Measured baselines for PhaseFormPanel.tsx at plan time (re-derive before and after):
  useMemo( -> 0     useState[(<] -> 0     useEffect( -> 0     wc -l -> 1167
  label="AI model" -> 4   (at :877, :913, :965, :1067)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Own the registry fetch at the panel's owner and thread one prop</name>
  <files>frontend/src/pages/WorkflowBuilderPage.tsx, frontend/src/components/workflows/PhaseFormPanel.tsx</files>
  <read_first>
    - frontend/src/pages/WorkflowBuilderPage.tsx:160-210 (the import block), :970-995 and :1135-1150 (how the two existing leaf hooks are each consumed in ONE call line), and every `<PhaseFormPanel` mount site in the file
    - frontend/src/components/workflows/PhaseFormPanel.tsx:150-215 (the props interface and the four caller-owned props before this one, each with a docblock explaining WHY it is caller-owned), :750-775 (the destructure list)
    - frontend/src/hooks/useModelRegistry.ts (as created by plan 196-05)
    - frontend/src/components/workflows/ModelField.tsx (its exact prop names — the spread must match)
  </read_first>
  <action>
**Measure first.** Record `grep -c 'useMemo('`, `grep -c 'useState[(<]'`, `grep -c 'useEffect('`,
`grep -c '\.filter('`, `grep -c '\.map('` and `wc -l` on `PhaseFormPanel.tsx` BEFORE any edit and paste
them into the summary. Plan-time readings for the first three were all **0**.

In `frontend/src/pages/WorkflowBuilderPage.tsx`, call `useModelRegistry()` in ONE line, sited beside the
existing `useTemplatePlaceholders` and `useGroundingBundle` calls — the file's own established shape for a
leaf hook. This is where the fetch belongs: `ModelField` mounts four times inside one panel, so four
fetches per open would be wrong, and siting the fetch here is precisely what makes the D-20 fence
satisfiable.

Pass its result into every `<PhaseFormPanel ...>` mount in the file as a single new prop carrying
`models`, `runDefaultModel` and `showTechnical`, assembled inline from the hook's return and the panel's
existing technical-names flag. One object, forwarded whole.

In `frontend/src/components/workflows/PhaseFormPanel.tsx`, add exactly two things: the optional prop on
`PhaseFormPanelProps`, and one identifier in the existing destructure list. Give the prop a docblock in
the register the four caller-owned props before it already use — state that it is caller-owned for the
FIFTH time in this file's history and for the same reason the others are (the answer is derived above the
panel, while this panel's only write seam patches `config`), that ABSENT means the mounts render nothing
so every other mount of this panel stays byte-identical by construction, and restate the ONE GATED LINE
standing order with this phase named as the fourth honouring.

⚠ **Add no logic.** No `useMemo` to shape the rows, no `.filter(` to drop disabled ones, no `.map(` to
build options — all of that lives in `ModelField` and `modelFitness.ts` already. The panel receives a
finished answer and forwards it.

⚠ **Do NOT widen `SelectField`** (`PhaseFormPanel.tsx:336-376`) to take option groups. It takes
`options: readonly string[]` and cannot express a grouped select; widening it would put picker-shaped
logic back inside the file the fence guards. `ModelField` renders its own select and reuses `FieldLabel`.

Do not touch the four `TextField` mounts in this task — that is Task 2, so a typecheck failure here is
attributable to the plumbing and not to the swap.
  </action>
  <verify>
    <automated>cd frontend; npx tsc --noEmit -p tsconfig.app.json; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/PhaseFormPanel.test.tsx src/components/workflows/PhaseFormPanel.rails.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'useModelRegistry' frontend/src/pages/WorkflowBuilderPage.tsx` is exactly 2 (the import and one call line).
    - The new prop appears on every `<PhaseFormPanel` mount in `WorkflowBuilderPage.tsx` — count the mounts and count the prop occurrences and state both; none is missed.
    - `grep -cE 'useMemo\(|useState[(<]|useEffect\(' frontend/src/components/workflows/PhaseFormPanel.tsx` is **0**, recorded beside the pre-edit reading.
    - `git diff -U0 -- frontend/src/components/workflows/PhaseFormPanel.tsx | grep '^+' | grep -cE 'useMemo|useState|useEffect|\.filter\(|\.map\('` is **0**.
    - `git diff frontend/src/components/workflows/PhaseFormPanel.tsx` touches only the props interface and the destructure list — no change inside `SelectField`, no change to any render body.
    - The new prop carries a docblock naming the ONE GATED LINE standing order and this phase.
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0 (the `-p` is load-bearing — a bare `tsc --noEmit` checks ZERO files).
    - `PhaseFormPanel.test.tsx` and `PhaseFormPanel.rails.test.tsx` both pass with no decrease in case count — in particular the shipped `<TemplateNameCheck` fence and the `<ToolsField`-appears-twice assertion are untouched.
  </acceptance_criteria>
  <done>The owner fetches once and hands the panel a finished answer; the panel gained a prop and a destructure and nothing else.</done>
</task>

<task type="auto">
  <name>Task 2: Swap the four free-text fields for four gated mounts, and write the fence that guards them</name>
  <files>frontend/src/components/workflows/PhaseFormPanel.tsx, frontend/src/components/workflows/PhaseFormPanel.test.tsx, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - frontend/src/components/workflows/PhaseFormPanel.tsx:868-925, :955-980, :1058-1080 (all four `label="AI model"` mounts and their surrounding conditionals — re-open them, the line numbers will have moved)
    - frontend/src/components/workflows/PhaseFormPanel.tsx:1110-1120 (the shipped one-gated-line example)
    - frontend/src/components/workflows/PhaseFormPanel.test.tsx:20-30 (the `?raw` import) and :470-500 (the shipped fence, verbatim — the new one mirrors its shape exactly)
    - frontend/src/components/workflows/PhaseFormPanel.rails.test.tsx:478-505 (the two sibling source assertions)
    - frontend/src/components/workflows/ModelField.tsx (the exact prop names and the fitness flag)
    - scripts/vitest-count-gate.cjs (the BASELINE entry for `PhaseFormPanel.test.tsx`, whose pin rises with the new fence)
  </read_first>
  <action>
Replace each of the four `label="AI model"` `TextField` mounts with a `ModelField` mount that is **exactly
one line**, gated on its phase type, and forwarding the new prop **whole** via spread — matching the
`{...nameCheck}` shape the existing fence explicitly rewards. One mount per phase type: `llm_single`,
`llm_agent`, `llm_batch_agents`, `llm_emit`. The `llm_emit` mount — and only that one — additionally
passes the fitness flag (D-12: the tier predicts nothing on the other three, and a warning that predicts
nothing trains people to ignore the ones that do).

Each mount passes `value={asStr(cfg.model)}`, `onChange={set("model")}` and `onPersist={onPersist}` in the
same forms the `TextField` mounts used, so the panel's save-on-blur discipline is unchanged and no third
state discipline is introduced.

⚠ **Delete the four `TextField` mounts entirely.** Leaving one behind on any phase type leaves a free-text
path open and AUTH-04 unmet — `grep -c 'label="AI model"'` must go 4 to 0.

⚠ Re-read `PhaseFormPanel.rails.test.tsx:485-500` before finalising the label text: it asserts capability
names are ABSENT from the panel source and that `<ToolsField` appears exactly twice. A `ModelField` mount
affects neither, but a new label string could trip the first — check rather than assume.

**Write the new fence** in `PhaseFormPanel.test.tsx`, mirroring the shipped one at `:480-488` in shape and
sited beside it. Scoped to `<ModelField`, it asserts: exactly **four** mount lines; the captured
`pt === "..."` guards, sorted, equal `["llm_agent", "llm_batch_agents", "llm_emit", "llm_single"]`; every
mount line contains the spread of the new prop; and the `llm_emit` mount — and only it — carries the
fitness flag. Add a NON-VACUITY assertion (the source is non-empty and contains `<ModelField`) so a
selector that silently matched nothing cannot pass forever, and a POSITIVE CONTROL running the same
line-splitting extractor over an inline fixture string known to be wrong (two mounts, or a mount with no
`pt ===` guard) so the fence has been observed red before it is trusted.

Add a second source assertion in the same file: the panel source contains **zero** occurrences of
`useMemo(`, `useState(` and `useEffect(`. That is the absolute bar, since all three measure 0 today, and
it converts D-20's *"the panel computes nothing for it"* from a promise into an invariant that survives
the next phase — which is exactly what 193.1 contributed and what this phase inherits.

Give the new fence a comment in the shipped one's voice, recording what it exists to prevent: the shipped
fence watches only `<TemplateNameCheck`, so without this one the panel could grow a `useMemo` to build the
option list and the guardrail would notice nothing.

Finally, reconcile `PhaseFormPanel.test.tsx`'s `BASELINE` pin in `scripts/vitest-count-gate.cjs` — the
suite gains cases, so the pin rises. ⚠ Take the number from the gate's own printed `actual` column, never
from a document, and record the raise beside the additions that caused it.
  </action>
  <verify>
    <automated>cd frontend; npx tsc --noEmit -p tsconfig.app.json; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/PhaseFormPanel.test.tsx src/components/workflows/PhaseFormPanel.rails.test.tsx src/components/workflows/ModelField.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'label="AI model"' frontend/src/components/workflows/PhaseFormPanel.tsx` is **0** — all four free-text fields are gone.
    - `grep -c '<ModelField' frontend/src/components/workflows/PhaseFormPanel.tsx` is **4**, and each of those four lines contains a `pt === "..."` guard and the spread of the new prop.
    - Exactly one of those four lines contains the fitness flag, and it is the `llm_emit` one.
    - `grep -cE 'useMemo\(|useState[(<]|useEffect\(' frontend/src/components/workflows/PhaseFormPanel.tsx` is **0**.
    - `git diff -U0 -- frontend/src/components/workflows/PhaseFormPanel.tsx | grep '^+' | grep -cE 'useMemo|useState|useEffect|\.filter\(|\.map\('` is **0**.
    - `PhaseFormPanel.test.tsx` contains the new `<ModelField` fence WITH a positive control and a non-vacuity assertion, plus the absolute zero-compute source assertion; the shipped `<TemplateNameCheck` fence is byte-unchanged.
    - `PhaseFormPanel.rails.test.tsx` passes unchanged — no capability-name leak into the panel source, `<ToolsField` still exactly twice.
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0 and all three named suites pass; each case count is recorded.
    - `scripts/vitest-count-gate.cjs`'s `PhaseFormPanel.test.tsx` pin equals the gate's own printed `actual`, with the raise recorded beside its cause.
    - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` is run and its verdict line pasted verbatim into the summary, paired with the per-file delta for `PhaseFormPanel.test.tsx`. ⚠ Do NOT make "the gate is green" the bar on its own. If it reds: capture the failing filenames from the gate's persisted JSON BEFORE re-running, check `git diff --numstat` and `git status --short`, and if the file is byte-unchanged and one of SEED-171's three (`src/pages/WorkflowsPage.test.tsx`, `src/components/workflows/library/WorkflowCard.test.tsx`, `src/pages/WorkflowBuilderPage.session.test.tsx`) record it as an observation and write "provably unmodified", never "fine". ⚠ Do NOT adjust `GSD_VITEST_MAX_WORKERS` — SEED-171 measures that adjusting the cap does not fix these, and all three run on every gate run for this phase because one of them lives inside the bare `src/components/workflows` target.
    - `git diff --name-only` contains neither `SettingsPage.tsx` nor `ModelPillRow.tsx` (SC#3 fence).
  </acceptance_criteria>
  <done>Four free-text inputs are gone, four one-line gated mounts are in, and a fence with an observed positive control guards both the mount shape and the panel's zero-compute property.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author gesture → stored workflow definition | the step form is the primary path by which `config.model` is set |
| registry payload → panel props | server-derived rows cross into a control rendered four times per open |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-196-UI1 | Tampering | the free-text `AI model` inputs | mitigate | All four are deleted. `grep -c 'label="AI model"'` going 4 to 0 is the proof that no typed path remains through the form. The server refusal (plan 196-06) remains the wall for anything bypassing the form |
| T-196-UI2 | Tampering | panel-level computation creeping in | mitigate | A source fence asserting exactly four gated mounts, each forwarding the prop whole, plus an ABSOLUTE zero assertion on `useMemo` / `useState` / `useEffect`. Both carry a positive control and a non-vacuity floor — a fence never observed red is not evidence |
| T-196-UI3 | Denial of Service (client) | four mounts each fetching | mitigate | The fetch is owned once at `WorkflowBuilderPage.tsx` through the leaf hook; `ModelField` has no fetch and no effect (fenced in plan 196-05) |
| T-196-UI4 | Information Disclosure | panel props | accept | The prop carries the six-field author projection only; `deprecated_reason` is structurally unreachable from `AuthorModelRow` |
| T-196-SC | Tampering | npm/pip/cargo installs | accept | No packages installed by this phase |
</threat_model>

<verification>
- `cd frontend; npx tsc --noEmit -p tsconfig.app.json` — the `-p` is load-bearing
- `cd frontend; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows` — the deterministic in-scope directory
- `cd frontend; GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` — verdict line recorded verbatim with per-file deltas; SEED-171 triage protocol applies if it reds
- The `PhaseFormPanel.tsx` compute counts recorded before Task 1 and after Task 2
- ⚠ NOT provable here: the G-4 lived-experience rows. **U-A1** (open a saved workflow with an `llm_emit` step, touch nothing, close it, and verify VIA DB that `config.model` is byte-unchanged) and **U-A2** (pick a `coerce` model and see it distinguished before committing, in user words) are ratified operator rows in `196-VALIDATION.md`, driven by Chrome MCP at phase verification. U-A1's proof is a DB read, not a UI observation — the failure it catches is invisible on screen
</verification>

<success_criteria>
- No free-text model input remains anywhere in the step form
- Exactly four one-line gated `ModelField` mounts, each forwarding the prop whole, with fitness on `llm_emit` only
- The panel's `useMemo` / `useState` / `useEffect` counts are all zero, asserted on source and by diff
- A new fence guards the mount shape and the zero-compute property, with its positive control observed
- The shipped `<TemplateNameCheck` fence and the `rails` sibling assertions are untouched and green
</success_criteria>

<output>
Create `.planning/phases/196-registry-backed-model-picker-canvas/196-08-SUMMARY.md` when done.
Record verbatim: the `PhaseFormPanel.tsx` compute counts before and after, the `label="AI model"` count
going 4 to 0, the new fence's positive-control output, the `PhaseFormPanel.test.tsx` case count and its
new pin taken from the gate's `actual`, the gate verdict line, and any SEED-171 triage observations
stated as "provably unmodified".
</output>
