---
phase: 196-registry-backed-model-picker-canvas
plan: 09
type: execute
wave: 6
depends_on: ["196-01", "196-02", "196-03", "196-04", "196-05", "196-06", "196-07", "196-08"]
files_modified:
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
  - .planning/reported-bugs/BUG-260731-01-judge-model-knob-may-be-inert-env-singleton.md
  - .planning/reported-bugs/BUG-260718-04.md
  - .planning/seeds/SEED-173-authoring-model-knob-inert-by-absence.md
  - .planning/seeds/SEED-174-forced-emission-emit-tier-latent-drift.md
  - .planning/seeds/SEED-175-publish-path-accepts-a-stored-unregistered-model.md
  - backend/tests/unit/test_196_forced_emission_drift_trigger.py
autonomous: true
requirements: [AUTH-04]

must_haves:
  truths:
    - "D-23: every file this phase ACTUALLY MODIFIED has a hot-file ledger row AND a detail section, both written in the SAME commit — and every figure was RE-DERIVED at close with CLAUDE.md's recipe, never copied from D-22 or from RESEARCH.md."
    - "D-22's finding is discharged for the touched subset: `backend/app/config.py` — the second-hottest file measured anywhere in this project, at 41 phases, structurally invisible to its own guardrail for the project's entire life — now has a row."
    - "D-22's four other named-but-untouched files stay named-only, by decision, with `SettingsPage.tsx` explicitly NOT modified (SC#3)."
    - "D-21 is recorded: G-2 fired and was declined on a reason — the picker idiom ships twice, so the acceptance bar was the shipped components rather than a drawing."
    - "D-16 is honoured: no document produced by this phase states the `$defs`/`$ref` schema-stripping hypothesis as fact. Judge FITNESS stayed out, and the reason recorded is the measurement gap, not scope tidiness."
    - "D-19 is honoured: `BUG-260809-01` is still `status: open` and its frontmatter is byte-unchanged."
    - "Both folded bug reports are flipped only on evidence — each names the plan and the test that closes it."
    - "SC#3 is proven by a negative fence over the phase's whole diff, not asserted."
    - "The latent `forced_emission` / `emit_tier` drift is converted into a gate that fires the day it stops being latent, instead of being edited inside a critical-bug plan."
  artifacts:
    - path: "docs/HOT-FILE-LEDGER.md"
      provides: "a detail section per modified file, matching its CLAUDE.md row"
    - path: "backend/tests/unit/test_196_forced_emission_drift_trigger.py"
      provides: "the mechanical re-open trigger for the D-122-04 latent drift"
      min_lines: 30
  key_links:
    - from: "CLAUDE.md hot-file ledger table"
      to: "docs/HOT-FILE-LEDGER.md sections"
      via: "the SAME-COMMIT SYNC RULE — a row without a section, or a section without a row, is drift"
      pattern: "HOT-FILE-LEDGER"
---

<objective>
Close the phase's books: the ledger debt, the folded bugs, the seeds, and the negative fences.

Purpose: three of this phase's decisions are records rather than code, and a record nobody writes is a
finding nobody keeps. **D-22 is a real finding, not bookkeeping** — five files this phase touches were
absent from the hot-file ledger, so G-5 had never fired on any of them, and `backend/app/config.py` at
**41 phases** is the second-hottest file measured anywhere in this project after `api/threads.py` (76).
Its own guardrail has been structurally blind to it for the project's entire life. That is the identical
invisibility failure `WorkflowsPage.tsx` (ten phases), `WorkflowDoorSwitch.tsx` (six) and `db/workflows.py`
(seventeen) each suffered.

⚠ **A row that is present and WRONG is worse than an absent row** — it answers the auditor with
`satisfied` and stops the audit. A full re-derive on 2026-08-17 found FIVE rows stale and THREE of them
reading `satisfied`, one wrong by 67 phases. So **re-derive every figure here in one batch pass** with
CLAUDE.md's own recipe, subtracting six-digit dated-quick-task buckets, and never copy a number from
D-22, from RESEARCH.md §K.30, or from this plan.

Output: synchronised ledger rows and detail sections, two bug reports flipped on evidence, three seeds
planted with mechanical re-open triggers, one drift-trigger test, and the SC#3 negative fence proven.

⚠ **SAME-COMMIT SYNC RULE.** The row lives in `CLAUDE.md`'s table and the narrative lives in
`docs/HOT-FILE-LEDGER.md`. They ship together. A row without a section, or a section without a row, is
drift — and this ledger's own most repeated finding is that a figure written at a phase's close goes
stale on the next commit, sometimes the same afternoon.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/196-registry-backed-model-picker-canvas/196-CONTEXT.md
@.planning/phases/196-registry-backed-model-picker-canvas/196-RESEARCH.md
@.planning/phases/196-registry-backed-model-picker-canvas/196-VALIDATION.md
@docs/HOT-FILE-LEDGER.md
@CLAUDE.md

<interfaces>
<!-- The re-derivation recipe, quoted from CLAUDE.md so it is not paraphrased. -->

  git log --oneline -- <file> | wc -l                                    # commits
  git log --format=%s -- <file> | sed -E 's/^[a-z]+\(([^)]+)\).*/\1/' \
    | sed -E 's/-.*//' | grep -E '^[0-9]+(\.[0-9]+)?$' | sort -u | wc -l  # phases
  wc -l <file>                                                           # lines

⚠ Six-digit buckets (260814, 260809, 260529) are DATED QUICK TASKS, not phases — SUBTRACT them.
  Non-numeric buckets (quick, chat, streaming, untagged subjects) are likewise not phases.
⚠ G-5 fires at 3 phases. The table deliberately also lists sub-threshold young files, because
  `WorkflowsPage.tsx` escaped G-5 for ten phases purely by not being written down.

Files ALREADY in the ledger that this phase modified — row + section owed, re-derived:
  backend/app/api/workflows.py                     (row read 35 / 17 / 1962)
  backend/app/services/harness/phase_types.py      (row read 38 / 15 / 2393)
  backend/app/services/harness/publish_service.py  (row read 19 / 7 / 1243)
  frontend/src/components/workflows/PhaseFormPanel.tsx (row read 16 / 8 / 1167)
  frontend/src/components/chat/ChatArea.tsx        (row read 61 / 29 / 587)
  frontend/src/pages/WorkflowBuilderPage.tsx       (row read 41 / 12 / 2348)

Files ABSENT from the ledger that this phase modified — D-23 moves each from "named" to "owed":
  backend/app/config.py                            (D-22 read 70 / 41 / 1275 — the headline)
  backend/app/api/admin.py                         (D-22 read 30 / 11 / 1718)
  backend/app/services/harness/validator_kinds.py  (D-22 read 11 / 4 / 744)
  frontend/src/components/admin/ModelRegistryTab.tsx (D-22 read 9 / 3 / 1083)
  backend/app/api/settings.py · backend/app/services/eval_runner_service.py ·
  frontend/src/components/panel/PhaseCard.tsx · frontend/src/types/index.ts ·
  frontend/src/lib/api.ts · scripts/vitest-count-gate.cjs      (never measured — measure them)

NEW files this phase created — each owes a young-file row:
  backend/app/services/model_registry.py · backend/app/api/model_registry.py ·
  frontend/src/components/workflows/ModelField.tsx ·
  frontend/src/components/workflows/modelFitness.ts ·
  frontend/src/hooks/useComposerModel.ts · frontend/src/hooks/useModelRegistry.ts

⚠ NOT modified, and must stay that way (SC#3): frontend/src/pages/SettingsPage.tsx ·
  frontend/src/components/settings/ModelPillRow.tsx. `verified_models` must be unchanged.

The M-9 drift command that becomes the trigger test:
  bad = [m for m,c in MODEL_CAPABILITIES.items()
         if bool(c.get('forced_emission')) != (c.get('emit_tier') in ('force','force_strict'))]
  measured 2026-08-17 -> 0 of 61. LATENT, not live.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-derive every triple in one batch pass and synchronise rows with sections</name>
  <files>CLAUDE.md, docs/HOT-FILE-LEDGER.md</files>
  <read_first>
    - CLAUDE.md §"Workflow guardrails (MANDATORY)" — the whole hot-file ledger table, its re-derivation recipe, the SAME-COMMIT SYNC RULE, and the staleness warning naming the five rows found wrong on 2026-08-17
    - docs/HOT-FILE-LEDGER.md — the sections for `workflows.py`, `phase_types.py`, `publish_service.py`, `PhaseFormPanel.tsx`, `ChatArea.tsx` and `WorkflowBuilderPage.tsx`, so the new prose matches their voice and each names a seam rather than a status
    - .planning/phases/196-registry-backed-model-picker-canvas/196-01-SUMMARY.md through 196-08-SUMMARY.md (the actual files each plan touched — the ledger scope is what was MODIFIED, not what was planned)
  </read_first>
  <action>
**Derive the file list from git, not from this plan.** Run `git diff --name-only <phase-base-sha>..HEAD`
to get the real modified set, and reconcile it against the list in `<interfaces>`. If a file was touched
that no plan predicted, it still owes a row — say so rather than trimming the list to match the plan.

**Re-derive all three figures for every file in one batch pass**, using CLAUDE.md's recipe verbatim.
Subtract six-digit dated-quick-task buckets from the phase count; they are not phases. Do this as a single
scripted sweep and paste the raw output into the summary, so the numbers are auditable rather than
asserted. ⚠ Do NOT copy a figure from D-22, from RESEARCH.md §K.30, or from this plan's `<interfaces>`
block — those are plan-time readings and this project's most repeated finding is that they rot, sometimes
within the same afternoon.

Then, in the SAME commit, for every modified file:

**Already in the ledger** — update its `commits / phases / lines` cell, its G-5 verdict and its
disposition, and refresh its `docs/HOT-FILE-LEDGER.md` section with what Phase 196 did to it and how G-5
was honoured. Each of these six was honoured by construction and each plan said so on the record; carry
that reasoning across rather than restating it as an assumption:
- `workflows.py` — two call lines plus an import; the extraction was NOT taken, and the named five-way
  seam still stands for the next phase that adds a genuinely second concern
- `phase_types.py` — one function plus five one-word call-site edits; the 190 precedent applied verbatim
- `publish_service.py` — one consumer rewired for D-17; the 8-stage gauntlet was deliberately not extended
- `PhaseFormPanel.tsx` — the fourth honouring of the standing one-gated-line order, and **record the new
  `<ModelField` fence**, noting that the 193.1 fence is scoped to `<TemplateNameCheck` and could not have
  covered it
- `ChatArea.tsx` — **record the `useComposerModel` extraction and the NEW counts**; this is the one file
  in the phase where G-5 was honoured by REDUCTION rather than by argument, and the arithmetic belongs in
  the section
- `WorkflowBuilderPage.tsx` — one hook call and one prop, matching the two leaf hooks it already consumes

**Absent from the ledger** — add a row and a detail section. `backend/app/config.py` is the headline and
its section should say so plainly: it is the second-hottest file measured anywhere in this project and its
own guardrail was structurally blind to it until this phase, which is the identical invisibility failure
three other files each suffered. Each new section names the file's concerns, the invariants it carries,
and a candidate seam for the next phase that needs one — a section that only reports a status is not what
these sections are for.

For a file whose re-derived phase count is **below** the G-5 threshold, still add a row (the table
deliberately lists sub-threshold young files, because `WorkflowsPage.tsx` escaped G-5 for ten phases purely
by not being written down) with the young-file disposition the table already uses: *owes a detail section
the moment it reaches a 3rd phase*.

**New files created by this phase** — add a young-file row each, with the one-line disposition the table's
existing young rows use, naming what each is the ONE home for: the union composition, the non-operator
registry route, the picker, the tier vocabulary, the composer's model machine, and the registry fetch.

**D-22's four untouched files stay named-only, by decision** — `SettingsPage.tsx`, and any of `admin.py` /
`validator_kinds.py` / `ModelRegistryTab.tsx` that turn out not to have been modified after all. Writing
detail sections about code this phase does not touch would turn a model-picker phase into a documentation
phase; the deferred idea already records the re-open trigger (*the next phase whose `files_modified` names
any of them*). State the decision rather than leaving the omission silent.

⚠ **`SettingsPage.tsx` must NOT appear in the modified set.** If it does, SC#3 was violated and this task
stops and reports rather than documenting the violation into acceptance.
  </action>
  <verify>
    <automated>bash -c 'set -e; for f in $(git diff --name-only $(git merge-base HEAD origin/develop)..HEAD | grep -E "\.(py|ts|tsx|cjs)$"); do grep -q "$(basename $f)" docs/HOT-FILE-LEDGER.md || { echo "MISSING SECTION: $f"; exit 1; }; done; echo "every modified source file has a ledger mention"'</automated>
  </verify>
  <acceptance_criteria>
    - The raw batch re-derivation output (commits / phases / lines for every modified file) is pasted into the summary, with the command that produced it.
    - Every file in `git diff --name-only <base>..HEAD` with a `.py` / `.ts` / `.tsx` / `.cjs` extension appears BOTH as a row in CLAUDE.md's table AND as a section (or a stated young-file row) in `docs/HOT-FILE-LEDGER.md` — the same-commit sync rule, checked mechanically by the verify command.
    - `backend/app/config.py` has a row and a section, and the section states its phase count and the invisibility finding.
    - `frontend/src/components/chat/ChatArea.tsx`'s section records the `useState` and `useEffect` counts before and after the `useComposerModel` extraction.
    - `frontend/src/components/workflows/PhaseFormPanel.tsx`'s section records the new `<ModelField` fence and states why the 193.1 fence could not have covered it.
    - The six new files each have a young-file row naming what they are the one home for.
    - No figure in either file matches D-22's or RESEARCH §K.30's reading unless the re-derivation independently produced it — any coincidence is stated as a coincidence.
    - `git diff --name-only` does NOT contain `frontend/src/pages/SettingsPage.tsx` or `frontend/src/components/settings/ModelPillRow.tsx`.
  </acceptance_criteria>
  <done>Every file this phase modified carries a re-derived row and a matching section, written in one commit, with `config.py`'s decades-long invisibility recorded rather than inherited.</done>
</task>

<task type="auto">
  <name>Task 2: Flip the folded bugs on evidence and plant the seeds with mechanical triggers</name>
  <files>.planning/reported-bugs/BUG-260731-01-judge-model-knob-may-be-inert-env-singleton.md, .planning/reported-bugs/BUG-260718-04.md, .planning/seeds/SEED-173-authoring-model-knob-inert-by-absence.md, .planning/seeds/SEED-174-forced-emission-emit-tier-latent-drift.md, .planning/seeds/SEED-175-publish-path-accepts-a-stored-unregistered-model.md, backend/tests/unit/test_196_forced_emission_drift_trigger.py</files>
  <read_first>
    - .planning/reported-bugs/TEMPLATE.md (the frontmatter contract)
    - .planning/reported-bugs/BUG-260731-01-judge-model-knob-may-be-inert-env-singleton.md (its `re_open_trigger` states the BINDING condition for flipping to `closed`)
    - .planning/reported-bugs/BUG-260718-04.md (its own condition: restores across navigate AND refresh)
    - .planning/reported-bugs/BUG-260809-01-cloud-eval-engine-sweep-all-providers-fail.md (⚠ read to confirm it is untouched — D-19)
    - .planning/seeds/SEED-135-per-role-model-fitness-visible-at-pick-time.md and .planning/seeds/SEED-172-local-llm-providers-cannot-be-added-through-the-registry.md (the seed-file house format, and SEED-135's own rule forbidding any document from stating the `$defs`/`$ref` cause as fact)
    - .planning/phases/196-registry-backed-model-picker-canvas/196-02-SUMMARY.md and 196-06-SUMMARY.md and 196-07-SUMMARY.md
    - backend/tests/unit/test_audit_event_registration.py:1-31 (the non-vacuity + positive-control habit for a structural guard)
  </read_first>
  <action>
**`BUG-260731-01` (D-17).** Update the frontmatter honestly. The report's own `re_open_trigger` states the
binding condition: flip to `closed` ONLY when the test exists AND the three-row measurement re-runs with
`resolve_judge_model` returning the operator's value. Plan 196-02 shipped both. Set `status` accordingly,
set `verified_closed_by` to the plan id, and add a short body note recording: the four consumers rewired,
the test file name, and the LIVE consequence — the publish-gauntlet judge on the operator's box is now
`deepseek-v4-pro`, not `claude-opus-4-8`. ⚠ If plan 196-02's summary does not contain the post-fix
three-row measurement, do NOT flip it: leave `status: folded`, and record what is still owed. A status
flipped without its evidence is exactly the failure the memory note *"a bug's `status:` frontmatter IS the
index"* records — prose saying "still open" inside a `folded` record is invisible to the routing scan.

**`BUG-260718-04` (D-18).** Its condition is a thread demonstrably restoring its last-used model across
navigate AND refresh. The unit half shipped in plan 196-07; the refresh half is **G-4 row U-C1**, which is
driven by Chrome MCP at phase verification and may not have run yet. ⚠ **Do not flip to `closed` on the
unit tests alone.** If U-C1 has not been driven, leave `status: folded`, set `verified_closed_by: null`,
and write the owed row into `re_open_trigger` naming U-C1 explicitly. If it HAS been driven and passed,
flip and cite it.

**`BUG-260809-01` (D-19).** Confirm by reading that it is still `status: open` and byte-unchanged. It is
the cloud eval engine, not a model control, and belongs to the SEED-040/088 sweep this phase fences out.
Record the confirmation; change nothing.

**Plant three seeds**, each in the house format with a mechanical re-open trigger:

1. `SEED-173` — **`resolve_authoring_model` is a FIFTH instance of the D-17 defect class, and a different
   sub-species.** Measured: `workflow_authoring.py:185-206` reads
   `getattr(settings, "harness_authoring_model", None)` off the same env singleton the four judge
   consumers read. It is **not** one of D-17's four, because there is no `app_settings` column and no UI
   knob for it — so it is inert-by-**absence**, not inert-by-**wiring**, and nothing lies to an operator
   because nothing offers the control. That is why it was out of scope. **Re-open trigger:** the first
   time an authoring-model knob is added to the Settings UI. Record that the fix is then mechanically
   identical to D-17's.
2. `SEED-174` — **the D-122-04 latent drift.** `resolve_judge_model`'s fallback at
   `validator_kinds.py:83-86` reads the DEPRECATED-UNREAD `forced_emission` bool while the emission ladder
   reads `emit_tier`. Measured 2026-08-17: **0 of 61** rows disagree, and both fallback candidates resolve
   identically under either flag — so the drift is LATENT, not live. Record why it was deliberately NOT
   fixed inside plan 196-02: it is a different function from the one D-17 changes, it is measurably inert,
   and changing inert code inside a critical-bug plan would have made that plan's binding test harder to
   attribute if it went red — the *"bug-fix plans smuggle in cleanups"* generalisation of G-7.
   **Re-open trigger:** the trigger test below going red.
3. `SEED-175` — **the publish path accepts a stored unregistered model.** Publish reads the STORED
   definition, so a definition saved before this phase can carry an unregistered model and publish without
   ever meeting the save-path refusal. Measured population at plan time: **zero**. Record why no eighth
   gauntlet stage was added (a genuine second concern in a doubly-documented 8-stage ordering, to catch an
   empty population, when D-10's run-time check already covers the dangerous half for every path).
   **Re-open trigger:** the first definition observed carrying an unregistered `config.model` — and quote
   the query that detects it, taken from plan 196-06's summary.

**Write the trigger test for SEED-174**, as a NEW file
`backend/tests/unit/test_196_forced_emission_drift_trigger.py` — **not** a modification to any judge test,
so a red result names the drift and nothing else. It asserts that for every row in `MODEL_CAPABILITIES`,
`bool(forced_emission)` equals `emit_tier in {"force", "force_strict"}`, with a failure message naming
SEED-174 and telling the reader what to do. Add a NON-VACUITY assertion (the registry is non-empty) and a
POSITIVE CONTROL running the same predicate over an inline fixture known to disagree — a guard whose
control was never observed red is not evidence. This converts a latent drift into a gate that fires the
day it stops being latent, which is a strictly better use of this phase than editing the line.

⚠ **D-16 is binding on every document written in this task.** SEED-135 rates its own `$defs`/`$ref`
schema-stripping hypothesis MEDIUM and states it has NEVER been live-verified, forbidding any document
from citing the cause as fact until someone dumps the sanitized Google tool payload. Judge FITNESS stayed
OUT of this phase for that reason — a measurement gap, not scope tidiness. If any seed or note written
here touches the `gemini-3.5-flash` judge failure, it states the hypothesis AS a hypothesis with its
confidence, or does not mention it.
  </action>
  <verify>
    <automated>cd backend; ./venv/Scripts/python.exe -m pytest tests/unit/test_196_forced_emission_drift_trigger.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Both folded reports' frontmatter is updated, and each `status` value is justified in the summary by naming the evidence (a test file, a measurement, or a driven UAT row). Neither is flipped to `closed` without it.
    - `.planning/reported-bugs/BUG-260809-01-cloud-eval-engine-sweep-all-providers-fail.md` is byte-unchanged (`git diff --name-only` does not list it) and still reads `status: open`.
    - The three seed files exist, each with a re-open trigger that is MECHANICAL (a command, a query or a named event), not a vibe.
    - `backend/tests/unit/test_196_forced_emission_drift_trigger.py` exists, passes, contains a non-vacuity assertion and a positive control, and its failure message names SEED-174.
    - `grep -rn 'defs\|\$ref' .planning/seeds/SEED-17*.md .planning/reported-bugs/BUG-2607*.md` returns nothing, OR every hit is worded as an unverified hypothesis with its confidence stated (D-16).
    - `grep -c 'SimpleNamespace' backend/tests/unit/test_196_forced_emission_drift_trigger.py` is 0.
  </acceptance_criteria>
  <done>The two folded bugs carry an honest status backed by named evidence, the untouched one is confirmed untouched, and three seeds are planted with triggers a machine can evaluate.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the negative fences and the deployment-artifact parity</name>
  <files>docs/HOT-FILE-LEDGER.md</files>
  <read_first>
    - .planning/ROADMAP.md §"#### Phase 196: Registry-Backed Model Picker (canvas)" (SC#1, SC#2 and especially SC#3's scope fence)
    - .planning/phases/196-registry-backed-model-picker-canvas/196-VALIDATION.md (the Per-Task Verification Map's SC#3 row and the sign-off checklist)
    - scripts/check-deploy-drift.sh (what it enforces and its `OMITTED_FROM_ONEBOX` list)
    - CLAUDE.md §"Deployment (cloud) — operator-gated" (the same-commit deployment-artifact parity rule)
  </read_first>
  <action>
**SC#3, proven rather than asserted.** Run the negative fence over the phase's entire diff:
`git diff --name-only <phase-base-sha>..HEAD` must contain neither
`frontend/src/pages/SettingsPage.tsx` nor `frontend/src/components/settings/ModelPillRow.tsx`. Separately
confirm the `verified_models` payload is unchanged — `git diff` on `backend/app/api/settings.py` must show
no edit to the `verified_models` line or to the `/settings` response construction (plan 196-07 added
`disabled_models` to the `/providers` handler only). Paste both results into the summary as measurements.

⚠ If either fence fails, STOP and report. A scope fence documented into acceptance is not a fence.

**Deployment-artifact parity (RESEARCH assumption A3).** Run `bash scripts/check-deploy-drift.sh` and paste
its output. The expectation is that migration 120 adds no env var and no seed row, so
`deploy/onebox.env.example`, `docs/OPERATOR.md` Step 3 and `docker-compose.prod.yml` are unaffected — but
A3 is logged as an ASSUMPTION with the mitigation *"run the script; it is cheap and decisive"*, so run it
rather than reasoning about it. If it reds, fix the drift in this plan (that is what the same-commit rule
requires) and record what was missing.

**Record the owed operator-gated cloud step.** Migration 120 must be pasted into the CLOUD Supabase SQL
editor in the same operation as any deploy of this code. Confirm plan 196-01's summary recorded it as
owed-and-not-performed, and restate it in this plan's summary so it survives into the phase record.
⚠ Do NOT perform any cloud action.

**Record the two remaining phase-level decisions in `docs/HOT-FILE-LEDGER.md`'s Phase 196 material** (or in
the summary if the ledger is not the right home — say which and why):
- **D-21** — G-2 fired and was DECLINED on a reason: the picker idiom ships twice already
  (`JudgeModelPicker.tsx`, Phase 137.1 / sketch 024-A; `ModelDefaultPreference.tsx`, Phase 167), so the
  acceptance bar existed in code rather than in a drawing, and re-drawing an atom that already ships is
  precisely the structural sketch-to-build drift SEED-155 records. The operator was offered a sketch and
  did not take it. No override is owed and none is recorded.
- **D-16** — judge FITNESS stayed OUT because of a measurement gap, not scope tidiness:
  `gemini-3.5-flash` is `emit_tier: force` and registry-known and STILL returned a null verdict, so
  `emit_tier` is necessary but not sufficient for the judge role. Building a fitness facet on an
  unverified hypothesis was refused. What this phase fixed instead is the judge WIRING (D-17).

**Confirm the guardrail status line for the phase**: G-1, G-3 and G-7 did not fire (196 is not an insert,
the scope is far past `/gsd:fast`, and no gap-closure round has run). G-2 fired and was declined (D-21).
G-5 fired on six ledger files plus several newly-measured ones and was honoured by construction in every
case, with the arithmetic recorded in Task 1. G-4's four rows are ratified in `196-VALIDATION.md` and are
driven at phase verification, not here.

⚠ **Do NOT rewrite `196-VALIDATION.md`.** Its UAT scoreboard, its ratified G-4 rows and its Per-Task
Verification Map are the phase's validation contract; the verifier fills its statuses. This task only
confirms that the plans satisfy its map, and records any row this phase could not satisfy as OWED rather
than as done.
  </action>
  <verify>
    <automated>bash -c 'B=$(git merge-base HEAD origin/develop); D=$(git diff --name-only $B..HEAD); echo "$D" | grep -q "frontend/src/pages/SettingsPage.tsx" && { echo "SC#3 VIOLATED: SettingsPage.tsx"; exit 1; }; echo "$D" | grep -q "frontend/src/components/settings/ModelPillRow.tsx" && { echo "SC#3 VIOLATED: ModelPillRow.tsx"; exit 1; }; bash scripts/check-deploy-drift.sh; echo "SC#3 fence clear and deploy artifacts checked"'</automated>
  </verify>
  <acceptance_criteria>
    - The SC#3 negative fence runs over the phase's real diff and passes, with the command and its output pasted into the summary.
    - `git diff` on `backend/app/api/settings.py` shows no change to `verified_models` or to the `/settings` response — quoted in the summary.
    - `bash scripts/check-deploy-drift.sh` is run and its output pasted; if it reds, the drift is fixed in this plan and what was missing is recorded.
    - The owed cloud-parity step for migration 120 is restated in this plan's summary as operator-gated and NOT performed.
    - D-21's decline-on-a-reason and D-16's measurement-gap reason are both recorded in the phase's durable record, and neither states the `$defs`/`$ref` hypothesis as fact.
    - The guardrail status line for the phase (G-1 / G-2 / G-3 / G-4 / G-5 / G-7) is stated with each verdict and its reason.
    - `196-VALIDATION.md` is byte-unchanged by this plan (`git diff --name-only` does not list it).
  </acceptance_criteria>
  <done>SC#3 is proven by a fence over the real diff, the deploy artifacts are checked rather than assumed, and the phase's declined and deferred decisions are on the record with their reasons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo documentation → future planning decisions | a ledger row or a bug status is read as authority by a later phase's guardrail scan |
| repo → cloud deployment | a schema migration must be applied to a second environment by a human |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-196-DOC1 | Spoofing (a record that lies) | the hot-file ledger rows | mitigate | Every figure is RE-DERIVED in one batch pass with the recipe, never copied. A present-but-wrong row answers the auditor with `satisfied` and stops the audit — a full re-derive on 2026-08-17 found five wrong and three reading `satisfied`, one by 67 phases |
| T-196-DOC2 | Spoofing (a record that lies) | reported-bug `status` frontmatter | mitigate | A status is flipped only when its own stated condition is met, and the evidence is named. Prose saying "still open" inside a `folded` record is invisible to the routing scan — the frontmatter IS the index |
| T-196-DOC3 | Repudiation | drift between the CLAUDE.md row and the detail section | mitigate | The SAME-COMMIT SYNC RULE, checked mechanically by a verify command that fails when a modified source file has no ledger mention |
| T-196-DOC4 | Tampering (silent scope creep) | SC#3 | mitigate | A negative fence over the phase's real diff, not an assertion. It fails the task rather than being documented into acceptance |
| T-196-DEPLOY | Tampering | cloud schema parity | accept, RECORDED | Migration 120 is owed to the cloud SQL editor at promotion. This plan records it as operator-gated and performs no cloud action. `check-deploy-drift.sh` covers the artifact half |
| T-196-SC | Tampering | npm/pip/cargo installs | accept | No packages installed by this phase |
</threat_model>

<verification>
- The batch re-derivation output for every modified file, with its command
- The mechanical same-commit sync check (every modified source file has a ledger mention)
- `cd backend; ./venv/Scripts/python.exe -m pytest tests/unit/test_196_forced_emission_drift_trigger.py -q`
- The SC#3 negative fence over the phase's real diff
- `bash scripts/check-deploy-drift.sh`
- Confirmation that `BUG-260809-01` and `196-VALIDATION.md` are both byte-unchanged
</verification>

<success_criteria>
- Every file this phase modified has a re-derived ledger row AND a matching detail section, in one commit
- `config.py`'s structural invisibility to its own guardrail is recorded, not inherited
- Both folded bugs carry a status backed by named evidence; the un-folded one is untouched
- Three seeds exist with mechanical re-open triggers, and the latent flag drift is now a gate
- SC#3 is proven by a fence, deployment artifacts are checked, and the cloud step is recorded as owed
- D-16, D-19, D-21, D-22 and D-23 are all discharged on the record with their reasons
</success_criteria>

<output>
Create `.planning/phases/196-registry-backed-model-picker-canvas/196-09-SUMMARY.md` when done.
Record verbatim: the batch re-derivation output, the before/after cell for each already-listed row, the
justification for each bug status, the three seed ids with their triggers, the SC#3 fence output, the
`check-deploy-drift.sh` output, the owed cloud-parity step, and the phase's guardrail status line.
</output>
