---
phase: 194.1-make-the-stop-visible
plan: 03
type: execute
wave: 2
depends_on: ["194.1-01"]
files_modified:
  - frontend/src/stores/streamsStore.ts
  - frontend/src/providers/StreamsProvider.tsx
  - frontend/src/__tests__/providers/StreamsProvider.stopping.test.ts
autonomous: true
requirements: [RUN-01]

must_haves:
  truths:
    - "A press is recorded as stopping in the same synchronous tick, before any network work"
    - "A stop that never resolves climbs down at 8 seconds and says it was not confirmed"
    - "The stopping state clears on EVERY route a run can end by, not only the one handler"
    - "A Stop on a thread never opened this session resolves a real run id and issues a real cancel"
    - "No console line claims a cause that was not established"
    - "A harness kickoff inserts no assistant node, and the administrator refusal and the network failure both still reach the screen"
  artifacts:
    - path: "frontend/src/stores/streamsStore.ts"
      provides: "stoppingThreads / stopNotConfirmed / harnessKickoffThreads — the pure-data mirrors"
      contains: "stoppingThreads"
    - path: "frontend/src/providers/StreamsProvider.tsx"
      provides: "The 8s provider-owned timer, the slice-transition clear, both repaired resolvers, the kickoff gate and its two repairs"
      contains: "stopTimersRef"
    - path: "frontend/src/__tests__/providers/StreamsProvider.stopping.test.ts"
      provides: "R2's three timing points, all four clear routes, R6 both resolvers, R5's three arms, the L-01 boundary"
  key_links:
    - from: "frontend/src/providers/StreamsProvider.tsx"
      to: "useStreamsStore.subscribe(s => s.streamingThreads, …)"
      via: "the ONE clear point that covers all four terminal routes"
      pattern: "subscribe\\("
    - from: "frontend/src/providers/StreamsProvider.tsx"
      to: "getThreadWorkflow"
      via: "R6's run-id fallback when the chat bucket is empty"
      pattern: "getThreadWorkflow\\("
---

<objective>
The whole provider/store change set, in one commit-set, because four of its parts are one concern and
two of them **must not be separated**.

Purpose:
- **R1/R2** — the pressed state and its 8-second climb-down live in the store beside `stopThread`,
  not in the mounts (D-05/D-06). Component-local state would leave the panel's Stop pressable after
  the composer's was pressed for the same thread, and would die on unmount mid-stop — a new lie in a
  phase whose subject is an honest Stop.
- **R6** — both resolvers gain a frame-read fallback, and **both** copies of the false-cause string go.
  ⚠ D-22: the string exists **TWICE** (`:2394` and `:2457`), so the SPEC's `grep == 0` acceptance is
  **not satisfiable by editing `stopThread` alone**.
- **R5** — the harness kickoff placeholder is not inserted, and the two shipped honesty paths that
  currently ride on it are repaired in the same change (D-23). Shipping the gate without the repair
  silences the administrator kill-switch on the **only** path that raises it.
- **D-14 is satisfied BY CONSTRUCTION**: R6's fallback and R5's gate land together, so `stopThread`
  never spends a moment with no run-id source at all.

Output: three store Sets, three selectors, one provider ref, one subscription, two repaired resolvers,
one gated insert, two repaired error arms, and the suite that proves each.

⚠ **The L-01 boundary is a STATEMENT, never a fix.** At `WORKER_COUNT=2` roughly half of stops end
with the run reporting `completed` (`db/workflows.py:1458-1463` — `finish_run` has no terminal guard;
Phase 194 SC#2, FAILED). With the clear keyed on the slice transition, the surface will honestly show
`⊘ Stopping this run…` then `✓ Complete`. **That is correct behaviour for this phase.** No task here
may special-case, delay or suppress the terminal reading, and a fence exists to red against exactly
that well-meaning change.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/194.1-make-the-stop-visible/194.1-SPEC.md
@.planning/phases/194.1-make-the-stop-visible/194.1-CONTEXT.md
@.planning/phases/194.1-make-the-stop-visible/194.1-RESEARCH.md
@.planning/phases/194.1-make-the-stop-visible/194.1-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Extracted at base SHA f9e55b6d. Every line number was measured there. -->

`frontend/src/stores/streamsStore.ts`
  :22-27  THE RULE FOR NON-SERIALIZABLE HANDLES, verbatim: "AbortController must NOT live in Zustand
          state (not serializable, identity churn breaks selector memoization). The provider holds the
          actual Map<runId, AbortController> in a ref; this Set is the pure-data mirror."
          The shipped pair is `subscriptionsRef` (StreamsProvider.tsx:1178) ↔ `subscriptionsByThread`
          (streamsStore.ts:136). **Timer handles obey the same rule.**
  :115    `streamingThreads: Set<string>` — the slice to MIRROR, declared
  :167-174 `workflowLockByThread: Map<string, WorkflowLock>` + the invariant: "A thread holds a lock
          IFF a non-terminal Harness workflow run owns its active_workflow_run_id anchor."
  :222    `stopStream: () => Promise<void>`   :227 `stopThread: (threadId: string) => Promise<void>`
  :330    `create<StreamsState>()(subscribeWithSelector(() => ({…})))` — subscribeWithSelector IS on
  :345    `streamingThreads: new Set<string>()` — the defaults block; each default carries a
          `// Type: …` comment mirroring the interface line (a shipped grep convention — keep it)

`frontend/src/providers/StreamsProvider.tsx`
  :1178   `subscriptionsRef`  ·  :1214 `lastEventAtRef` — where the new timer ref belongs
  :1358   the mount-time `useStreamsStore.setState` that registers the real action bodies
  :1925-1941  the optimistic assistant placeholder (`runStatus:"streaming"`, no `runId`)
  :1943-1945  `streamingThreads: new Set(s.streamingThreads).add(threadId)` — SYNCHRONOUS at kickoff
  :1981   `if (opts?.workflowDefinitionId && run_id)` — the harness signal ALREADY branched on
  :2248-2265  the ApiError 403 arm — `blockedNotice` mapped onto `m.id === assistantId`
  :2270-2285  the generic ApiError rollback — routes to reconcileErrors, UNAFFECTED
  :2286-2293  the network-failure arm — `runStatus:"failed"` mapped onto `m.id === assistantId`
  :2295-2368  the `finally` chain — harmless over an absent id (every write is `m.id === assistantId`)
  :2304-2311  "the AUTHORITATIVE streaming-end write" — `streamingThreads` delete #1
  :2373-2404  `stopStream`  (false string :2394)   :2411-2474 `stopThread` (false string :2457)
  :3544   `usePhases(threadId)`  ·  :3564 `useViewingThread()`
  :3605-3606 `useStreamingForThread`  ·  :3614-3615 `useStreamingThreadIds`
  :3610-3613 the MEASURED re-render property: "Selecting streamingThreads directly is
          reference-stable across token deltas (the Set is reassigned ONLY on stream start/stop —
          tokens never touch it)."   :3293 records the fresh-object selector churn hazard.
  :3659   `useWorkflowLockForThread`
  App.tsx:253-313 — `<StreamsProvider>` wraps everything ABOVE the page switch, so a provider ref and
          a store slice both survive any mount unmounting. That is D-06's argument, made structural.

The FOUR `streamingThreads` delete sites (a clear keyed on `onTerminal` alone MISSES two of them —
Phase 145 exists for exactly those):
  #1 :2304-2311 sendMessage finally  ·  #2 :1599-1602 reconcile-DERIVE (snapshot.active_runs)
  #3 :3007-3010 inactivity watchdog silent-finalize  ·  #4 :3222-3225 org teardown

`frontend/src/lib/api.ts:1392` — `getThreadWorkflow(threadId, signal?)`, owner-scoped, NOT
canvas-gated. Returns `active_workflow_run_id` and `last_workflow_run_id` (a `workflow_runs.id`).
`api.ts::cancelRun` deliberately SWALLOWS 404.

`backend/app/api/runs.py:1211-1253` — `DELETE /runs/{id}`'s Step-1b dual-id fallback ALREADY accepts a
`workflow_runs.id`. Its clause (c) requires the thread anchor to still point at the run — true for
every LIVE run R6 serves. An already-stopped run 404s and `cancelRun` swallows it: correct, and
exactly why R1's no-button-while-stopping is load-bearing.
⚠ `ComposerStopHarness.test.tsx:10-12` says `DELETE /runs/{id}` "accepts the PRODUCER runs.run_id and
nothing else." **That was made FALSE by plan 194-11 in the same phase. Do not quote it.**
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Three pure-data Sets, one provider ref, three primitive selectors</name>
  <files>frontend/src/stores/streamsStore.ts, frontend/src/providers/StreamsProvider.tsx</files>
  <read_first>
    - `frontend/src/stores/streamsStore.ts` lines 1-60, 96-260, 325-400
    - `frontend/src/providers/StreamsProvider.tsx` lines 1170-1230 (the refs block) and 3595-3665 (the selector block and its alphabetical convention at :3601-3603)
    - `.planning/phases/194.1-make-the-stop-visible/194.1-RESEARCH.md` § Q1
  </read_first>
  <action>
FIRST ACTION IN THE WORKTREE, before reading anything else: `bash scripts/bootstrap-worktree.sh "$(pwd)"`.

**Then ASSERT THE BASE, and assert the right property.** Every `file:line` in this phase's artifacts was
measured at `f9e55b6db33606fca77ee84ce8ef3da3153d09e2`. What matters is NOT that HEAD equals that SHA — the plan set's own commit already
moved HEAD past it — but that the worktree descends from it and that **no source file this plan quotes
has moved since**:

    git merge-base --is-ancestor f9e55b6db33606fca77ee84ce8ef3da3153d09e2 HEAD    # must exit 0
    git diff --name-only f9e55b6db33606fca77ee84ce8ef3da3153d09e2 HEAD -- frontend/src backend/app scripts supabase

If the ancestor check fails the worktree forked from the WRONG base — **STOP and report.** This project
has measured that happening repeatedly (12/12 worktrees in Phase 192, and again on a single worktree on
2026-08-14). If the diff is non-empty, RE-DERIVE every line number this plan quotes **before** using it
and record the re-derivation in the SUMMARY — an earlier plan in this phase legitimately moves these
files, and a rotted line number quoted as fact is how a plan edits the wrong place.

**In `streamsStore.ts`**, declare three `Set<string>` fields beside `streamingThreads` (:115) and seed
three empty Sets in the defaults block beside `:345`, keeping the shipped `// Type: …` comment
convention on each default (it is what the 075.4-01 acceptance greps find):
  `stoppingThreads`      — threads whose Stop has been pressed and not yet resolved
  `stopNotConfirmed`     — threads whose stop passed the 8s window without a terminal
  `harnessKickoffThreads`— threads whose harness kickoff is in flight (R5's synchronous liveness mark)

Write ONE docblock over the three recording, in order:
 - they are the SIBLING of `streamingThreads`, not its rival: the same concern (per-thread run
   liveness) in a second state. That sentence is D-02's whole no-second-concern argument for this file
   and it must survive future edits.
 - **immutable replace only** — `new Set(s.X).add(t)` / copy-then-`delete`, never `.add` in place, so
   `:3610-3613`'s measured reference-stability property is inherited by construction. They are
   reassigned ONLY on press / clear / timeout — never on a token.
 - **two Sets, not one Map.** A `Map<string,"stopping"|"unconfirmed">` collapses "cleared entirely"
   and "unconfirmed" onto one key, and R2's acceptance needs them distinguishable. Two Sets also keep
   both selectors PRIMITIVE-returning, which is what makes the default `Object.is` equality re-render
   only the mount whose value flipped (`:3293` records the fresh-object churn hazard). Recorded as a
   rejected alternative rather than omitted; CONTEXT puts the shape in Claude's discretion and this is
   the exercise of it.
 - ⚠ **NO TIMER HANDLE MAY EVER BE PUT HERE.** Quote `:22-27`'s rule and name the shipped
   `subscriptionsRef ↔ subscriptionsByThread` pair as the pattern being followed.

**In `StreamsProvider.tsx`**, add `stopTimersRef = useRef<Map<string, number>>(new Map())` beside
`lastEventAtRef` (:1214), holding `window.setTimeout` handles keyed by thread id. Add a module-level
`const STOP_TIMEOUT_MS = 8000` with a one-line comment naming R2 as its source.

Add three selectors in the selector block beside `:3605`, honouring the alphabetical convention at
`:3601-3603`. Each returns a PRIMITIVE:
  `useStoppingForThread(threadId: string | null): boolean`
  `useStopNotConfirmedForThread(threadId: string | null): boolean`
  `useHarnessLiveForThread(threadId: string | null): boolean` — true when
    `harnessKickoffThreads.has(t) || workflowLockByThread.has(t)`. ⚠ It must return a BOOLEAN, never
    the lock object; and it must read BOTH, because the kickoff Set covers the round trip before the
    lock is seeded (`:1981`) and the lock covers a run rejoined by reconcile after navigation.

Nothing else in either file changes in this task.
  </action>
  <acceptance_criteria>
    - `grep -c "stoppingThreads" frontend/src/stores/streamsStore.ts` ≥ 2 (interface + default, per the shipped convention)
    - Same ≥ 2 for `stopNotConfirmed` and `harnessKickoffThreads`
    - `grep -c "setTimeout\|setInterval" frontend/src/stores/streamsStore.ts` is **0** — no timer handle in the store
    - `grep -n "STOP_TIMEOUT_MS = 8000" frontend/src/providers/StreamsProvider.tsx` returns exactly 1 hit
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — error count unmoved from `194.1-BASELINE.md` §11
    - The three selectors' return types are `boolean` — assert from source, and assert `useHarnessLiveForThread`'s body names BOTH `harnessKickoffThreads` and `workflowLockByThread`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <done>Three Sets exist as pure-data mirrors, the timer handle lives in a provider ref, and three primitive-returning selectors are declared.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The press, the 8s climb-down, and the ONE clear that covers all four routes</name>
  <files>frontend/src/providers/StreamsProvider.tsx, frontend/src/__tests__/providers/StreamsProvider.stopping.test.ts</files>
  <read_first>
    - `frontend/src/providers/StreamsProvider.tsx` lines 2373-2480 (both resolvers) and 1340-1370 (the mount-time setState that registers them)
    - `frontend/src/providers/StreamsProvider.tsx` lines 1590-1605, 2300-2315, 3000-3015, 3218-3228 (the FOUR `streamingThreads` delete sites)
    - `frontend/src/__tests__/providers/StreamsProvider.watchdog.test.ts` lines 1-45 (the provider-owned-timer fake-timer recipe)
    - `frontend/src/__tests__/providers/StreamsProvider.stopping.baseline.test.ts` (plan 01's captures — the before)
  </read_first>
  <behavior>
    - Pressing Stop marks the thread stopping SYNCHRONOUSLY, before any await
    - At t+7000ms the stopping reading is still on; at t+8000ms it is off and not-confirmed is on
    - A cancel that resolves terminally at t+2000ms never raises not-confirmed, even after t+8000ms
    - The stopping state and its timer clear when the thread leaves `streamingThreads` by ANY of the four routes — proved through reconcile-derive, which `onTerminal` alone cannot see
    - A second press while stopping is a no-op that does not restart the timer
  </behavior>
  <action>
In BOTH `stopStream` and `stopThread`, add a shared press-recording step as the FIRST statement of the
body (after the falsy-thread-id early return): synchronously `stoppingThreads.add(threadId)`,
`stopNotConfirmed.delete(threadId)`, and arm a `window.setTimeout` of `STOP_TIMEOUT_MS` recorded in
`stopTimersRef`. If a timer already exists for that thread, **leave it** and do not re-arm — a second
press must not extend the window.

On expiry (**D-07** — the climb-down is a store-mirrored timer keyed BY THREAD, not by run, so a
thread with no resolvable run id still climbs down): `stoppingThreads.delete(threadId)`,
`stopNotConfirmed.add(threadId)`, and delete the ref entry. Nothing else. The timer is the ONLY route back to a pressable Stop — sketch 168-B's losing arm
is load-bearing, not a nicety, and this is where that lives.

Factor the press-record and the expiry into two small named helpers inside the provider so both
resolvers call the same code. They are byte-mirror resolvers (`:2387-2389` says so on purpose) and
must not drift.

Register, in a mount-time `useEffect` in the provider, **ONE**
`useStreamsStore.subscribe((s) => s.streamingThreads, (next) => …)`. For every thread present in
`stoppingThreads` but **absent** from `next`: clear `stoppingThreads`, clear `stopNotConfirmed`, clear
`harnessKickoffThreads`, and `clearTimeout` its `stopTimersRef` handle. Return a cleanup that
unsubscribes AND clears every outstanding timer.

Write ONE comment above the subscription recording why it is keyed on the SLICE TRANSITION and not on
`onTerminal`: `onTerminal` is wrapped twice (`:1662-1663` reconcile path, `:2057-2058` send path) and
covers neither the reconcile-DERIVE delete (`:1599`) nor the inactivity watchdog (`:3007`) — i.e.
exactly the missed-terminal cases Phase 145 exists to close. Name all four delete sites by line.

⚠ **THE L-01 BOUNDARY, in this comment and nowhere weaker:** on roughly half of stops at the shipped
`WORKER_COUNT=2` a producer on the other worker writes `completed` over the cancel
(`db/workflows.py:1458-1463`, no terminal guard). With the clear keyed here, the surface will show
`⊘ Stopping this run…` and then `✓ Complete`. **That is correct and must NOT be special-cased.**
RUN-01 stays UNTICKED until the separate L-01 phase ships.

**The named plants, each observed RED before the fix is claimed green:**
 - P1: set `STOP_TIMEOUT_MS` to 3000 → the **t+7000 still-stopping** case reds. (A single t+8s case
   passes a 1 ms timeout — all three of R2's points are needed, not one.)
 - P2: clear the flag on expiry but do NOT `clearTimeout` on the terminal route → the "2s cancel never
   shows not-confirmed" case reds. This is the ONLY case that proves `clearTimeout` runs.
 - P3: key the clear on `onTerminal` only → the reconcile-derive case reds.
 - P4 (**the L-01 boundary fence**): suppress the terminal reading when the thread was stopping → the
   boundary case reds. This fence protects the honesty CONTEXT requires, in the direction a
   well-meaning plan would break it.
  </action>
  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/__tests__/providers/StreamsProvider.stopping.test.ts` exits 0
    - Cases exist and pass for: synchronous mark; t+7000 still stopping; t+8000 climbed down; 2s-cancel-never-unconfirmed; reconcile-derive clear; double-press does not re-arm
    - The L-01 boundary case asserts stopping-then-`✓ Complete` renders BOTH readings in order and that the terminal one is NOT suppressed
    - `grep -c "setTimeout" frontend/src/stores/streamsStore.ts` is still 0
    - P1-P4 all recorded in the SUMMARY as observed RED, each naming the case that fired
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/__tests__/providers/StreamsProvider.stopping.test.ts</automated>
  </verify>
  <done>The press is synchronous, the climb-down is exactly 8s, the clear covers all four routes through one subscription, and the L-01 terminal reading is fenced against suppression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: R6 — resolve the run id without the chat bucket, on BOTH resolvers</name>
  <files>frontend/src/providers/StreamsProvider.tsx, frontend/src/__tests__/providers/StreamsProvider.stopping.test.ts</files>
  <read_first>
    - `frontend/src/providers/StreamsProvider.tsx` lines 2373-2480 (as edited by task 2)
    - `frontend/src/lib/api.ts` lines 1290-1340 and 1388-1402 (`ThreadWorkflowState`, `getThreadWorkflow`)
    - `frontend/src/components/chat/__tests__/ComposerStopHarness.test.tsx` lines 1-215 (the mock bundle, `makeControllableRun`, and its two standing rules at :16-19 and :92-97)
    - `backend/app/api/runs.py` lines 1205-1260 (the Step-1b dual-id fallback and its clause (c))
  </read_first>
  <behavior>
    - Bucket populated → `cancelRun` is called with the BUCKET's producer run id (Deep unchanged)
    - Bucket EMPTY, frame returns `active_workflow_run_id` → `cancelRun` is called with THAT value
    - Bucket EMPTY, frame returns only `last_workflow_run_id` (anchor NULLed) → `cancelRun` is called with THAT value
    - Bucket empty AND the frame names no run → `cancelRun` is NOT called, stopping is cleared, not-confirmed is raised, and the console line claims no cause
    - `stoppedByUserRef` is set only on a path that actually issues a cancel
  </behavior>
  <action>
In **both** resolvers, keep the bucket scan FIRST and add the frame read as a **fallback only**:
when `runId` is falsy, `await getThreadWorkflow(threadId)` and take
`frame.active_workflow_run_id ?? frame.last_workflow_run_id ?? undefined`.

⚠ **D-12 — a store field stamped at kickoff to serve R6 was REJECTED, and the rejection is recorded
here so it is not re-proposed as a "cheaper" fix**: it would be a live-only reading of a durable fact,
and a live-only reading is precisely why `BUG-260610-01` is two months old (174 D1). Two sources of
truth for one fact is the defect, not the fix. The frame read is the durable one.

⚠ **Do not reverse the order.** The bucket path yields the producer `runs.run_id`; the frame path
yields a `workflow_runs.id` and takes the slower Step-1b branch. Reversing them would change the id
type on every Deep stop — a needless behaviour change on a surface this phase has no business on.

Both resolvers are already `async` and already `await cancelRun`, so an awaited fetch is not a new
shape. And because task 2's press-record is synchronous and sits ABOVE this, the fetch latency is
invisible to the user — that is the whole reason R1 and R6 belong in one provider change.

Wrap the frame read in its own `try/catch`; a failed frame read must fall through to the
no-id-resolved arm, never throw out of a click handler.

**DELETE BOTH copies of the false-cause string.** Replace each with a line that states ONLY what has
been established — that no run id could be resolved from either the live message bucket or the
thread's workflow frame, and that nothing was cancelled. It must name the thread id and the condition
and NOTHING ELSE (T-194-08-04: no message content, no auth header, no run output). It must NOT
suggest pressing Stop again, must NOT name the pre-stamp window as the cause, and must be worded
distinctly from the `"Stop failed:"` catch so the two conditions stay separable in a log.

On that arm, also clear `stoppingThreads`, raise `stopNotConfirmed`, and clear the timer — a press
that provably cancelled nothing must not sit showing `⊘ Stopping this run…` for eight seconds.

Keep `stoppedByUserRef.current = true` set only BELOW the resolution, on the path that actually calls
`cancelRun`. `:2461-2468`'s shipped comment explains why that ordering is load-bearing; preserve both
the ordering and the comment, updating only what the resolution now is.

**Discharge the shipped re-open trigger.** `:2443-2452` records *"a second sighting of a Stop lost in
the pre-stamp window"* as the trigger for disabling the control. R6 + R1 together discharge it: the
control is now REMOVED while stopping (not disabled), and the id is resolvable without the bucket.
Rewrite that block to record the trigger as **DISCHARGED by 194.1 R1+R6**, keeping the original
reasoning visible under a `SUPERSEDED` marker rather than deleting it — a deferral that lives only in
a deleted comment is exactly as invisible as one never written (193.2 WR-05).

**The named plants, each observed RED:**
 - P5: assert `expect(cancelRun).toHaveBeenCalled()` alone instead of the VALUE → passes under a wrong
   id. Show that it does, then use the VALUE assertion. (`ComposerStopHarness.test.tsx:16-19`.)
 - P6: reorder the resolvers (frame first) → the "bucket path still preferred" case reds.
 - P7: restore the false string in `stopStream` **only** → the source fence reds. ⚠ This is exactly
   the miss the SPEC's single-resolver framing invites, and it is the reason the fence must sweep the
   whole of `frontend/src` and not one function.

The source fence must print the swept file-list length and assert it is non-empty before asserting
the count is 0 (the 192.1 lesson).
  </action>
  <acceptance_criteria>
    - `grep -rc "press Stop again in a moment" frontend/src --include=*.ts --include=*.tsx | grep -v ':0$'` returns **nothing** (0 hits in production source AND in tests, except where a test quotes it as a plant — if a test must quote it, the fence excludes test files and says so in one line)
    - Five behaviour cases green, each asserting the VALUE handed to `cancelRun` (or its absence)
    - `getThreadWorkflow` is added to the suite's `vi.hoisted` `@/lib/api` bundle alongside `getSnapshot`
    - `:2443-2452`'s re-open trigger reads DISCHARGED with the original reasoning kept under a `SUPERSEDED` marker
    - P5, P6, P7 recorded in the SUMMARY as observed RED
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/__tests__/providers/StreamsProvider.stopping.test.ts src/components/chat/__tests__/ComposerStopHarness.test.tsx</automated>
  </verify>
  <done>Both resolvers resolve without the chat bucket, both false strings are gone with a source fence that can fire, and the shipped re-open trigger is recorded as discharged rather than deleted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: R5 — no assistant node at harness kickoff, and Deep byte-identical</name>
  <files>frontend/src/providers/StreamsProvider.tsx, frontend/src/__tests__/providers/StreamsProvider.stopping.test.ts</files>
  <read_first>
    - `frontend/src/providers/StreamsProvider.tsx` lines 1900-1990 (the optimistic inserts, the streamingThreads add, the `workflowDefinitionId` branch at :1981)
    - `frontend/src/stores/streamsStore.ts` (the `sendMessage` opts JSDoc — "Omitted on a Deep send (byte-identical)")
    - `.planning/sketches/171-one-run-one-slot/README.md` (winner C amended — what is and is NOT guaranteed)
    - `frontend/src/__tests__/providers/StreamsProvider.stopping.baseline.test.ts` cases 4 and 5 (plan 01's before)
  </read_first>
  <behavior>
    - A harness kickoff (`workflowDefinitionId` set) inserts ZERO assistant nodes into the chat bucket
    - A harness kickoff adds the thread to `harnessKickoffThreads` in the SAME synchronous tick as the user message
    - A Deep send (no `workflowDefinitionId`) still inserts exactly ONE assistant node — byte-identical
    - `harnessKickoffThreads` is cleared by the same slice-transition subscription task 2 registered
  </behavior>
  <action>
Gate the optimistic assistant placeholder at `:1925-1941` on `if (!opts?.workflowDefinitionId)`. Deep
never sets that opt (the store's own JSDoc: *"Omitted on a Deep send (byte-identical)"*), so Deep
enters the same branch it enters today and 174 D-14's byte-identity holds **by construction**, not by
test. In the `else` arm, add the thread to `harnessKickoffThreads` — synchronously, in the same tick
as the user-message insert and the `streamingThreads` add at `:1943-1945`, so the run's presence is
readable with **zero** round trips. That synchronicity is what R5's *"no interval showing a prompt
with no run instrument"* acceptance rests on; a fetch-driven liveness signal would fail it by one
kickoff RTT.

`assistantId` stays declared and stays the identity every later arm keys on — the `finally` chain
(`:2295-2368`) is a no-op over an absent id and must not be touched.

**REJECTED, recorded so it is not re-litigated:** the render-level rule (*"the assistant renders only
when it has content"*) is closer to R5's literal wording and is **rejected** — it changes what Deep
renders during its own first-token window. Hiding the node with CSS is **rejected** — R5's acceptance
is *zero assistant nodes in the transcript*, which a hidden node does not satisfy. Put both in one
comment above the gate.

**The named plants, each observed RED:**
 - P8: hide the node with CSS instead of skipping the insert → the node-COUNT assertion reds. (This is
   why R5's acceptance counts nodes rather than visibility.)
 - P9: gate on `agentMode` instead of `workflowDefinitionId` → the Deep control case reds.
  </action>
  <acceptance_criteria>
    - A harness-kickoff case asserts the chat bucket contains **zero** rows with `role === "assistant"`
    - A Deep-send case asserts exactly ONE assistant row, and plan 01's baseline case 5 is still green untouched
    - A case asserts `harnessKickoffThreads.has(threadId)` is true SYNCHRONOUSLY after `sendMessage` is invoked, before any promise resolves
    - A case asserts the thread leaves `harnessKickoffThreads` when it leaves `streamingThreads`
    - P8 and P9 recorded in the SUMMARY as observed RED
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/__tests__/providers/StreamsProvider.stopping.test.ts src/__tests__/providers/StreamsProvider.stopping.baseline.test.ts</automated>
  </verify>
  <done>A harness kickoff inserts no assistant node, marks liveness synchronously, and Deep's insert is byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: R5's two honesty repairs — the 403 kill-switch and the network failure</name>
  <files>frontend/src/providers/StreamsProvider.tsx, frontend/src/__tests__/providers/StreamsProvider.stopping.test.ts</files>
  <read_first>
    - `frontend/src/providers/StreamsProvider.tsx` lines 2236-2300 (all four error arms, in order)
    - `frontend/src/__tests__/providers/streamsProvider_state01b_403.test.tsx` (the shipped amber-notice suite — do not break it)
    - `backend/app/api/workflow_kickoff.py` around line 200 (the kill-switch refusal text and the fact that this is the ONLY path raising it)
    - `.planning/phases/194.1-make-the-stop-visible/194.1-CONTEXT.md` § D-23
  </read_first>
  <behavior>
    - A harness kickoff refused with `ApiError(403)` still puts the administrator's message on screen
    - A harness kickoff that fails the network still produces a visible failed reading — it is not silent
    - Deep's behaviour on both arms is unchanged
    - The 409 lock-refusal and the generic `ApiError` rollback arms are byte-untouched
  </behavior>
  <action>
⚠ **This task is the reason R5 is not a one-line change, and skipping it ships a NEW silence.** The
placeholder task 4 stopped inserting is the **sole carrier** for two shipped honesty paths:
 - `:2248-2265` — the **403 kill-switch amber notice**. `workflow_kickoff.py:200` (*"Workflows are
   currently disabled by the administrator"*) is raised on the **harness path and only the harness
   path**, so silencing it here silences it everywhere. Phase 174 D-04/D-05, sketch 129-C amber tier.
 - `:2286-2293` — the **network-failure `runStatus:"failed"` flip**. Without it a harness kickoff that
   fails the network is completely silent.

Repair both by INSERTING the notice-bearing assistant node on those two arms when the placeholder was
skipped, instead of `map`-ing onto an absent one. Use the same `assistantId` so identity is
unchanged for anything downstream. **A refused kickoff is not a running kickoff**, and a node that
exists only to carry a refusal does not reinstate the surface the duplicate avatar was drawn on —
state that in one comment on each arm, because it is the argument that keeps R5's acceptance true.

Keep the arms' ORDER exactly as shipped (409 → 403 → generic `ApiError` → else), because
`:2248`'s comment records that the 403 is placed BEFORE the generic branch on purpose so the 400/409
rollback paths stay byte-identical (D-05).

⚠ The 409 lock-refusal (`:2239-2247`) and the generic `ApiError` rollback (`:2270-2285`) route to
`reconcileErrors` / the banner and are **UNAFFECTED**. `git diff -U0` must show no line changed inside
either.

**The named plant, observed RED:** P10 — ship task 4's gate WITHOUT this repair. Both the 403 case and
the network case red. Run that plant explicitly and record both failures; it is the single most
important RED in this plan.
  </action>
  <acceptance_criteria>
    - A case drives `postMessage` rejecting `new ApiError(msg, 403)` with `workflowDefinitionId` set and asserts a node bearing `blockedNotice.message === msg` reaches the bucket
    - A case drives `postMessage` rejecting a plain `Error` with `workflowDefinitionId` set and asserts a node with `runStatus === "failed"` reaches the bucket
    - `frontend/src/__tests__/providers/streamsProvider_state01b_403.test.tsx` is still green and byte-unedited (`git diff --numstat` on it is EMPTY)
    - `git diff -U0` shows no changed line inside `:2239-2247` or `:2270-2285`
    - P10 recorded in the SUMMARY with BOTH failing case names
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/__tests__/providers/StreamsProvider.stopping.test.ts src/__tests__/providers/streamsProvider_state01b_403.test.tsx</automated>
  </verify>
  <done>Both honesty arms reach the screen on a harness kickoff with no placeholder, the two unaffected arms are provably byte-untouched, and P10 was observed RED.</done>
</task>

<task type="auto">
  <name>Task 6: Gate, typecheck, and publish the D-03 counts for this file</name>
  <files>frontend/src/providers/StreamsProvider.tsx, frontend/src/stores/streamsStore.ts</files>
  <read_first>
    - `.planning/phases/194.1-make-the-stop-visible/194.1-BASELINE.md` §3 and §11
    - `CLAUDE.md` § *Parallel execution* (the `GSD_VITEST_MAX_WORKERS=2` rule and its rot history)
  </read_first>
  <action>
Publish the D-03 stop-condition counts for the two files this plan touched, BEFORE and AFTER, from
`194.1-BASELINE.md` §3 and a fresh measurement:
`grep -c 'useState[(<]'`, `grep -c 'useEffect('` and `wc -l` on `StreamsProvider.tsx` and
`streamsStore.ts`. ⚠ `useEffect(` is EXPECTED to move by exactly +1 here (task 2's subscription) and
that is the one increment this plan is allowed — state it as such rather than letting it read as
drift. `useState(` must be unmoved: the stopping state is store-owned by D-05, and a `useState`
appearing in this file for it would be the same second-concern failure D-03 forbids in the mounts.

Run `cd frontend && npx tsc --noEmit -p tsconfig.app.json` (⚠ the bare `--noEmit` form checks ZERO
files) and record the error count against §11.

Run the full gate: `cd frontend && GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs`. Read
the **verdict line**. ⚠ If `failed` is non-zero, CAPTURE THE FAILING FILENAMES BEFORE RE-RUNNING
ANYTHING — cap 2 is not deterministic and a plan that re-ran first cannot afterwards prove its own
cases were innocent (193.2-02). Record every reading, not the best one.

Also re-run plan 01's four baseline suites and record which of their assertions this plan legitimately
INVERTED (the kickoff-placeholder captures, cases 4 and the two resolver cases). An inverted baseline
must be UPDATED IN PLACE with the original assertion quoted under a `SUPERSEDED` marker and the reason
— never deleted, never silently rewritten.
  </action>
  <acceptance_criteria>
    - The SUMMARY publishes before/after `useState(` / `useEffect(` / `wc -l` for both files, with `useState(` unmoved
    - `tsc` error count recorded and unmoved from §11
    - Count gate verdict `OK`, `failed 0` — or every failing filename captured verbatim before any re-run
    - Every inverted baseline assertion is updated in place with its original quoted under `SUPERSEDED`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>The gate is green, the D-03 counts are published with `useState(` unmoved, and every superseded baseline carries its original.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click → `stopThread` → `DELETE /runs/{id}` | A user-triggered destructive action crosses to the API. The API's own owner-scoping (`api/runs.py:1211-1253`, three clauses) is the gate and is **byte-untouched** by this plan. |
| browser → `GET /threads/{id}/workflow` | A new client call site for a shipped, owner-scoped, ungated read. |
| console output | A log line written by the client. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-194.1-03-01 | Information disclosure | the replacement console line | mitigate | It names the thread id and the condition and NOTHING ELSE — no message content, no auth header, no run output (T-194-08-04, inherited verbatim). Asserted by the source fence in task 3 |
| T-194.1-03-02 | Elevation of privilege | cancelling a run the user does not own via the frame-resolved id | transfer | The id comes from an OWNER-SCOPED read (`GET /threads/{id}/workflow`, ownership-gated 404) and is spent on an OWNER-SCOPED route whose Step-1b fallback carries three independent clauses. This plan adds no clause and relaxes none |
| T-194.1-03-03 | Denial of service | a leaked `setTimeout` per press | mitigate | Timers live in one ref, are cleared on the slice transition, on the no-id arm, and in the effect's cleanup; a second press does not re-arm. P2 is the fence that proves the clear runs |
| T-194.1-03-04 | Repudiation | the administrator's kill-switch refusal silently vanishing | mitigate | Task 5 exists solely for this. P10 (ship the gate without the repair) must be observed RED with both case names recorded |
| T-194.1-03-05 | Tampering | a well-meaning suppression of the terminal reading to hide L-01 | mitigate | P4 — a dedicated fence that reds against exactly that change. The L-01 lie is a BOUNDARY to state, never a defect to hide |
| T-194.1-03-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed.** RESEARCH § *Environment Availability*: no new dependency is required by any recommendation. A task reaching for one stops and escalates to a blocking human checkpoint |
</threat_model>

<verification>
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` → `OK`, `failed 0`
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` → unmoved from `194.1-BASELINE.md` §11
- `grep -rn "press Stop again in a moment" frontend/src` → no production hit
- `grep -c "setTimeout\|setInterval" frontend/src/stores/streamsStore.ts` → 0
- `git diff --numstat frontend/src/__tests__/providers/streamsProvider_state01b_403.test.tsx` → EMPTY
</verification>

<success_criteria>
- Three Sets, one ref, three primitive selectors; no timer handle in the store
- The press is synchronous; the climb-down is exactly 8s; the clear covers all four `streamingThreads`
  delete routes through ONE subscription
- Both resolvers resolve without the chat bucket; both false strings gone; the fence sweeps a
  provably non-empty file list
- A harness kickoff inserts zero assistant nodes; Deep is byte-identical; both honesty arms still reach
  the screen
- P1-P10 each observed RED before its fix, each recorded with the case that fired
- The L-01 boundary is fenced against suppression, and RUN-01 is NOT claimed ticked
</success_criteria>

<output>
Create `.planning/phases/194.1-make-the-stop-visible/194.1-03-SUMMARY.md` when done.
</output>
