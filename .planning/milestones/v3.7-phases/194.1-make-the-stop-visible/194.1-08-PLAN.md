---
phase: 194.1-make-the-stop-visible
plan: 08
type: execute
wave: 6
depends_on: ["194.1-01", "194.1-02", "194.1-03", "194.1-04", "194.1-05", "194.1-06", "194.1-07"]
files_modified:
  - CLAUDE.md
  - .planning/phases/194.1-make-the-stop-visible/194.1-VALIDATION.md
  - .planning/ROADMAP.md
  - .planning/phases/194.1-make-the-stop-visible/194.1-DEFERRED.md
autonomous: false
requirements: [RUN-01]

must_haves:
  truths:
    - "Every hot file this phase touched is VISIBLE to the G-5 audit, because a guardrail cannot see what is absent from its list"
    - "Every fence in this phase names the plant that was observed RED, in one filled table"
    - "The L-01 boundary is recorded as a statement, and RUN-01 is NOT ticked"
    - "Every deferral carries a re-open trigger, and no deferral lives only in a deleted line"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Six hot-file ledger rows — three ADDED, two UPDATED, one CORRECTED beside its old value"
      contains: "MessageList.tsx"
    - path: ".planning/phases/194.1-make-the-stop-visible/194.1-VALIDATION.md"
      provides: "The filled Per-Task Verification Map incl. the Negative-control-observed-RED column"
    - path: ".planning/phases/194.1-make-the-stop-visible/194.1-DEFERRED.md"
      provides: "Every deferral this phase created or inherited, each with a re-open trigger"
  key_links:
    - from: "CLAUDE.md"
      to: "frontend/src/providers/StreamsProvider.tsx"
      via: "the stale row CORRECTED beside its old value, never over it"
      pattern: "StreamsProvider.tsx"
---

<objective>
Close the phase: fix the G-5 ledger so the next audit can SEE these files, fill the validation
contract, record every deferral with a trigger, and state the L-01 boundary rather than let it be
discovered at UAT.

Purpose — and it is the sharpest lesson this project keeps re-learning: **a guardrail cannot see what
is absent from its list.** G-5 never fired once on `WorkflowsPage.tsx` across ten phases, on
`WorkflowDoorSwitch.tsx` across six, on `WorkflowBuilderPage.tsx` across ten, or on `db/workflows.py`
across seventeen — every time because the discuss-phase audit scans PLAN.md `files_modified` **against
the ledger table**, and a hot file missing from the table is permanently invisible to its own
guardrail. ⚠ **And a row that is PRESENT and WRONG is worse than an absent one**: `StreamsProvider.tsx`
currently reads *"5+ phases · satisfied (075.7)"* while measuring **77 / 33 / 3660** — an auditor
reading that row concludes the guardrail was honoured 28 phases ago and **stops looking**.

Output: six ledger rows, one filled verification map, one deferral register, one ROADMAP update, one
final gate reading.

⚠ **`autonomous: false`** — this plan ends at a blocking human checkpoint. The three G-4 rows are
operator-driven by definition (CLAUDE.md G-4: wire format and screenshots are **insufficient**), and
RUN-01's tick is an operator decision this plan explicitly declines to make.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/194.1-make-the-stop-visible/194.1-SPEC.md
@.planning/phases/194.1-make-the-stop-visible/194.1-CONTEXT.md
@.planning/phases/194.1-make-the-stop-visible/194.1-RESEARCH.md
@.planning/phases/194.1-make-the-stop-visible/194.1-VALIDATION.md
@.planning/ROADMAP.md
@CLAUDE.md

<interfaces>
<!-- What plan 08 must reconcile -->

`CLAUDE.md` § *Hot-file ledger* — the table's own conventions, which this plan must follow exactly:
  · a corrected figure is recorded **BESIDE** the previous value, **never over it**
  · each cell carries the COMMANDS that re-derive its triple
  · the raw `sed` bucket recipe's non-phase buckets are named and subtracted explicitly — "both are
    recorded, the loser beside the winner"
  · a row states plainly whether G-5 FIRES, and if it fires and was not honoured by extraction, it
    states the measured reason it was honoured BY CONSTRUCTION
  · a file the phase did NOT touch records "checked, unmoved" rather than nothing — so a reader can
    tell *checked* from *nobody checked*

The re-derivation recipe (per CLAUDE.md):
  commits : `git log --oneline -- <f> | wc -l`
  buckets : `git log --format=%s -- <f> | sed -E 's/^[a-z]+\(([^)]+)\).*/\1/' | sed -E 's/-.*//' | sort -u`
  lines   : `wc -l <f>`

WAVE-1 readings from `194.1-BASELINE.md` §7 (⚠ these are the BEFORE; every one of them has moved for
the five files this phase edited, and re-deriving them is this plan's job — this table has gone stale
on its own afternoon more than once):
  StreamsProvider.tsx  77 / 33 / 3660   ← ledger row present and WRONG ("5+ phases · satisfied")
  MessageInput.tsx     24 / 12 / 446    ← ABSENT from the ledger
  MessageList.tsx      18 /  7 / 234    ← ABSENT from the ledger
  RunCard.tsx          21 /  9 / 608    ← present; this phase expects it UNTOUCHED
  WorkspacePanel.tsx   14 /  9 / 580    ← present
  WorkflowRunPage.tsx  10 /  2 / 1046   ← ABSENT; G-5 does NOT fire (2 < 3)

⚠ Non-phase buckets the raw recipe returns, measured at base — name them, subtract them, record both:
  StreamsProvider.tsx : `260529`, `chat`, `SEED`, `streaming`
  MessageInput.tsx    : `backend`, plus three prose subjects (`feat: …`, `Fix LangSmith …`, `Redesign …`), `07`, `12`
  MessageList.tsx     : `chat`, `32`, `phase`, plus prose subjects
  RunCard.tsx         : `streaming` (the untagged 075.x fix/revert pair)

The four folded bug reports — **frontmatter ALREADY flipped at discuss-phase (D-21); this plan
VERIFIES rather than performs**:
  `BUG-260816-01` folded/194.1 (whole) · `BUG-260816-02` folded/194.1 (whole)
  `BUG-260709-01` folded/194.1 — ⚠ **PARTIAL: Direction A only**
  `BUG-260610-01` folded/194.1 — ⚠ **PARTIAL: the duplicate-avatar half only, and claimed as SURFACE
     REMOVAL, not repair**

⚠ **CONTEXT corrects the SPEC on `BUG-260710-01` and the correction must survive into the deferral
register:** the SPEC says *"no report with that id exists"*. It does —
`.planning/reported-bugs/cancelled-run-stop-indicator-lost-on-navigation.md` carries
`id: BUG-260710-01`. **The filename is not the id**, which is why a filename grep missed it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The six ledger rows — three added, two updated, one corrected BESIDE its old value</name>
  <files>CLAUDE.md</files>
  <read_first>
    - `CLAUDE.md` § *Workflow guardrails (MANDATORY)* and the whole hot-file ledger table (read at least three existing cells in full — the `WorkflowsPage.tsx`, `db/workflows.py` and `RunCard.tsx` rows — to absorb the format before writing)
    - `.planning/phases/194.1-make-the-stop-visible/194.1-BASELINE.md` §7
    - Every `194.1-0N-SUMMARY.md` from plans 02-07 (the D-03 counts and the measured no-second-concern arguments each published)
  </read_first>
  <action>
**D-01 measured G-5 firing on FIVE files, not the four the SPEC named — and the fifth
(`StreamsProvider.tsx`) is the worst, because its row is present and WRONG.** This task is where that
measurement becomes visible to the audit.

FIRST ACTION IN THE WORKTREE: `bash scripts/bootstrap-worktree.sh "$(pwd)"`.

RE-DERIVE all six triples **at THIS commit** with the CLAUDE.md recipe. ⚠ This plan deliberately does
NOT assert the phase base SHA the way plans 01-07 do: by now every earlier plan has moved these files,
and the whole point of this task is to measure them where they NOW are. Do not copy `194.1-BASELINE.md`
§7 — those are wave-1 readings and five of the six files have moved since. For each, record the raw
bucket list AND the numeric phase count, naming the non-phase buckets explicitly and subtracting them:
*both are recorded, the loser beside the winner.*

Then edit `CLAUDE.md`'s hot-file ledger:

**ADD** `frontend/src/components/chat/MessageInput.tsx` — G-5 **FIRES** (12 phases at base). State
plainly that it was **ABSENT from this table** until Phase 194.1 and that the audit could therefore
never see it; that sentence is the reason the row exists and must survive future edits. Honoured **by
construction**: the file **swapped one control for one shared component and owns ZERO new state** —
publish plan 04's before/after `useState(` / `useEffect(` / props counts as the measured proof, and
note the props count went DOWN by one (`onStop` retired). ⚠ Also name the raw recipe's non-phase
buckets so the next reader is not surprised by their own command's output.

**ADD** `frontend/src/components/chat/MessageList.tsx` — G-5 **FIRES** (7 phases at base). ⚠ Correct
the SPEC on measurement: it says *"≥6 phases"*; the numeric bucket count is **7**. Same
absent-from-the-table sentence. Honoured by construction: **one list-level mount and one prop**, with
plan 06's counts as proof (`useState(` 0 → 0, `useEffect(` 1 → 1). ⚠ Record that `ThreadRunLine`
legitimately owns its own `useState`/`useEffect` and that this is **not** a D-03 violation — D-03
forbids the **pressed state** living inside a **mount component**, and this is neither.

**ADD** `frontend/src/pages/WorkflowRunPage.tsx` — **G-5 does NOT fire (2 phases, below the threshold
of 3)**, stated plainly. It goes on the ledger anyway, exactly as `194-05` did for `RunCard.tsx`: at
**1046 lines** it is worth being visible **before** it reaches three phases, and a young file put on
the ledger now inherits a measured triple instead of forcing a re-derivation later. Note that this
phase's two edits (plan 06's verbatim `fmtElapsed` hoist; plan 07's single gated mount) move the count.

**CORRECT** `frontend/src/providers/StreamsProvider.tsx` — the row currently reads
`068 / 075 / 075.4 / 075.6 / 075.7 (5+)` · *"satisfied (075.7 — 2026-05-24)"*. ⚠ **Write the corrected
triple BESIDE the old value, never over it**, and say plainly that the row was **stale by 28 phases**
and that *a row that is present and WRONG answers the auditor with `satisfied` and stops the audit* —
a worse failure than absence. It is the **largest frontend file in the tree**. G-5 **FIRES HARDEST**.
Honoured by construction: the file already owned `sendMessage`'s optimistic placeholder (R5's edit) and
`stopThread`'s id resolution (R6's edit), and the stopping slice sits **beside** the shipped
`streamingThreads` slice — the same concern (per-thread run liveness) in a second state. Publish plan
03's counts, including the ONE allowed `useEffect(` increment (the slice-transition subscription) and
`useState(` unmoved.

**UPDATE** (never duplicate) `frontend/src/components/panel/WorkspacePanel.tsx` — inherits
`14 / 9 / 580`; re-derive and record the new triple beside it. G-5 fires; honoured by construction —
**two consumers of one boolean became two booleans, and `showTimeline` is byte-unchanged** (D-25).
Record the invariant that now binds the file: the Stop row gates on `isHarness`, whose lock invariant
(`streamsStore.ts:161-174`) is cleared on every terminal route, while `showTimeline`'s
`|| phases.length > 0` disjunct stays because it is what keeps a **finished** run's timeline visible
(Phase 098 UAT run-honesty fix B).

**UPDATE** `frontend/src/components/chat/RunCard.tsx` — inherits `21 / 9 / 608`. ⚠ **This phase expects
it UNTOUCHED.** Re-derive and record either *"checked, unmoved"* with the commands, or — if any plan
did edit it — the new triple with the reason. *A reader who finds no 194.1 entry here must be able to
tell "checked, unmoved" from "nobody checked".* Note the file's inherited G-5 obligation passes forward
undischarged if it was not touched.

⚠ **Record the G-5 override posture once, at the top of the phase's rows:** an override was
**OFFERED AND DECLINED** (D-02), so `.planning/STATE.md` must record **NO** guardrail override for
Phase 194.1 — the **FIFTH consecutive phase** to honour G-5 by construction (193, 193.1, 193.2, 194,
194.1). **VERIFY that absence rather than assuming it** (`grep -n "Guardrail overrides" .planning/STATE.md`)
so it is a measurement and not an omission.

⚠ Every row must state that its figures go stale on the next commit touching the file, and carry the
re-derive commands. This table has been corrected four times about `WorkflowsPage.tsx`, twice within a
single phase about `db/workflows.py`, and once *on the same afternoon it was written*.
  </action>
  <acceptance_criteria>
    - `grep -c "MessageInput.tsx" CLAUDE.md`, `grep -c "MessageList.tsx" CLAUDE.md` and `grep -c "WorkflowRunPage.tsx" CLAUDE.md` each ≥ 1, and each occurrence is a TABLE ROW, not prose
    - The `StreamsProvider.tsx` row contains BOTH the string `5+` (the old value, preserved) and the corrected triple
    - Exactly ONE row exists per file — `grep -c "^| \`frontend/src/components/panel/WorkspacePanel.tsx\`" CLAUDE.md` is 1 (no duplicate row was added)
    - Every one of the six rows contains the three re-derive commands
    - The `RunCard.tsx` row states either "checked, unmoved" with commands, or a new triple with a reason
    - `grep -n "Guardrail overrides" .planning/STATE.md` was run and its output recorded in the SUMMARY, showing no Phase-194.1 entry
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)" && for f in MessageInput.tsx MessageList.tsx WorkflowRunPage.tsx StreamsProvider.tsx WorkspacePanel.tsx RunCard.tsx; do n=$(grep -c "$f" CLAUDE.md); echo "$f=$n"; test "$n" -ge 1 || exit 1; done && grep -q "5+" CLAUDE.md && echo LEDGER_OK'</automated>
  </verify>
  <done>All six rows exist with re-derivable triples, the stale row is corrected beside its old value, and the declined-override absence is a verified measurement.</done>
</task>

<task type="auto">
  <name>Task 2: Fill the Per-Task Verification Map, including the RED column</name>
  <files>.planning/phases/194.1-make-the-stop-visible/194.1-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/194.1-make-the-stop-visible/194.1-VALIDATION.md` (the whole contract — the map's column set and the sign-off list)
    - `.planning/phases/194.1-make-the-stop-visible/194.1-RESEARCH.md` § *Validation Architecture* (the authoritative fence set — do NOT duplicate it into VALIDATION.md)
    - Every `194.1-0N-SUMMARY.md` from plans 01-07 (the plants each recorded as observed RED)
  </read_first>
  <action>
Fill `194.1-VALIDATION.md`'s **Per-Task Verification Map** with one row per task across all eight
plans: Task ID · Plan · Wave · Requirement · Threat Ref · Secure Behavior · Test Type · Automated
Command · **Negative control observed RED** · Status.

⚠ **A row whose "negative control" cell is blank is not a validated task.** Every plant this phase
named (plan 02's P1-P3, plan 03's P1-P10, plan 04's P1-P6, plan 05's P1-P6, plan 06's P1-P10, plan
07's P1-P3 plus F1/F2) must appear in that column, sourced from the SUMMARY that recorded the
observation — **not re-derived from the plan text**. A plan that reported a green fence without having
seen it red has not validated anything, and if any SUMMARY is silent on a plant, write `NOT OBSERVED`
in that cell and list it under the sign-off as an outstanding item. Do not fill a cell you cannot
source.

Tick the **Validation Sign-Off** list, and for each item either mark it done with its evidence or
leave it unticked with the reason. In particular:
 - *"Every source fence prints its swept file-list length and asserts it is non-empty"* — enumerate the
   source fences in this phase and confirm each. The 192.1 lesson: a renamed module was swept against
   the empty string and passed green.
 - *"Both count-gate pins bumped in the commit that adds their cases"* — cite plan 05's and plan 07's
   commits by SHA.
 - *"The three G-4 rows driven by the operator"* — leave UNTICKED here; task 5's checkpoint is where
   they are driven, and ⛔ rows are recorded with a reason, **never silently omitted**.
 - Set `nyquist_compliant: true` in the frontmatter ONLY if every task row carries an `<automated>`
   command; otherwise leave it false and name the tasks that do not.

Record, under the cross-provider section, that the 8-row native roster was **not** re-driven and that
this is a **decision**: the phase adds no provider-facing behaviour — no prompt, no tool call, no
emission path, no model routing — and the durable half it renders was scored on seven live runs in
Phase 194. ⚠ Verify that claim against this phase's actual `files_modified` before restating it; if
any plan ended up touching a provider service, `MODEL_CAPABILITIES` or the emission path, the full
roster is owed before the phase closes.
  </action>
  <acceptance_criteria>
    - The map has one row per task across all eight plans, and no row has an empty Requirement, Automated Command or Negative-control cell (`NOT OBSERVED` is an acceptable value; blank is not)
    - Every plant id named in plans 02-07 appears in the map, sourced from a SUMMARY
    - The sign-off list is fully addressed — each item either ticked with evidence or unticked with a reason
    - The cross-provider decision is restated only after `files_modified` was checked, and the check is recorded
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)" && f=.planning/phases/194.1-make-the-stop-visible/194.1-VALIDATION.md && ! grep -q "planner fills" "$f" && grep -c "^|" "$f" && echo MAP_FILLED'</automated>
  </verify>
  <done>The verification map is filled from the SUMMARYs, every plant is accounted for, and nothing green is claimed that was never seen red.</done>
</task>

<task type="auto">
  <name>Task 3: The deferral register — every deferral with a trigger, none living in a deleted line</name>
  <files>.planning/phases/194.1-make-the-stop-visible/194.1-DEFERRED.md</files>
  <read_first>
    - `.planning/phases/194.1-make-the-stop-visible/194.1-CONTEXT.md` § *Deferred Ideas* (the whole section)
    - `.planning/phases/194.1-make-the-stop-visible/194.1-RESEARCH.md` § *Open Questions* (items 3 and 4)
    - `.planning/reported-bugs/BUG-260816-01-*.md`, `BUG-260816-02-*.md`, `BUG-260709-01-*.md`, `BUG-260610-01-*.md` (frontmatter only)
    - `.planning/reported-bugs/cancelled-run-stop-indicator-lost-on-navigation.md` (frontmatter — the report whose id is `BUG-260710-01`)
  </read_first>
  <action>
Create `.planning/phases/194.1-make-the-stop-visible/194.1-DEFERRED.md`. One entry per deferral, each
with **a concrete re-open trigger** — a deferral without a trigger is an omission wearing a decision's
clothes, and one that lives only in a deleted comment is exactly as invisible as one never written
(193.2 WR-05).

Carry forward, at minimum:
 - **L-01 / CR-04** — Phase 194's SC#2 is FAILED and still has no roadmap home. Restate the mechanism
   (`RUN_TASKS` is per-process; a producer on the other worker calls `finish_run(run_id,'completed')`
   over the cancel; `finish_run` is an unconditional UPDATE with **no terminal guard**,
   `db/workflows.py:1458-1463`) and the operator decision of 2026-08-16: **state it as a boundary in
   194.1, insert a phase for it AFTER 194.1 lands.** ⚠ Record the interaction explicitly: **this
   phase's honest UI makes the L-01 lie MORE visible, not less** — the surface will truthfully show
   `⊘ Stopping this run…` then `✓ Complete`, and a fence exists (plan 03's P4) specifically to red
   against a well-meaning suppression. Carry the cheaper repairs named at 194's verification for
   whoever plans it: an in-loop `workflow_runs.status` re-read before `mark_phase_active` raising
   `CancelledError` when terminal; a terminal guard on `finish_run`; a Redis cancel channel as fallback.
 - **A-1** — the client-side twin (`api/threads.py:475-482` selects active runs with
   `status='streaming'` and no `parent_run_id IS NULL`). Not claimed here; carry it into the L-01 phase.
 - **`stopStream` is now dead code** (plan 04 retired its last consumer). **NOT deleted** — it is
   exported through `useMessages`' action surface and its removal is a Deep-path change.
   *Re-open trigger: a phase that touches `useMessages`' action surface.*
 - **The failed-run reading on the run line** (D-16) — absent by decision. Trigger: an operator
   observation that a failed workflow thread is as silent as a stopped one was.
 - **Run history in the thread** (D-10) — the read returns the latest run only.
 - **The run line on `WorkflowRunPage`** (RESEARCH open question 3) — out of scope; the run surface has
   its own band and elapsed. Trigger: *"an operator observation that the run surface and the thread
   disagree about the same stop."*
 - **The double-mount race itself** — three candidate mechanisms in `194-MEASUREMENTS.md`, no verdict;
   R5 removes the surface it drew on and does **not** repair it. Trigger: the artefact reaching a
   content-bearing message, or being seen on Deep.
 - **The Deep / plain-chat path**, **the timer half of `BUG-260610-01`**, **Direction B of
   `BUG-260709-01`**, **`BUG-260816-03`** — each with its existing trigger.
 - ⚠ **`BUG-260710-01` DOES exist** — `.planning/reported-bugs/cancelled-run-stop-indicator-lost-on-navigation.md`.
   **The filename is not the id**, which is why the SPEC's filename grep concluded it did not.
   The stopped-badge-on-Deep-chat family therefore has a real record to be deferred against and
   nothing needs filing. Record the correction; a deferral pointing at an id someone believes does not
   exist is invisible twice over.
 - **The composer's stopping-state WIDTH** — jsdom cannot measure it (D-24) and the fence is class-token
   identity on a fixed-size wrapper. Trigger: G-4 row 1 judging the row twitchy; the fix is a clamp on
   the composer variant's reading, one line.

Then **VERIFY** (do not re-perform) the four folded bug reports: each carries `status: folded` and
`folded_into: "194.1"` in its **frontmatter**. ⚠ `BUG-260709-01` is **Direction A only** and
`BUG-260610-01` is **the duplicate-avatar half only, claimed as SURFACE REMOVAL not repair** — confirm
both partials are recorded in the frontmatter's own comment and in the `re_open_trigger`, not merely
in prose. `status:` **is** the index a routing scan reads; a prose claim the frontmatter does not
record is worse than no claim, and that is exactly how `BUG-260610-01` stayed invisible for two months.
  </action>
  <acceptance_criteria>
    - `194.1-DEFERRED.md` exists with one entry per deferral, and EVERY entry has a `Re-open trigger:` line
    - The L-01 entry names `finish_run`'s missing terminal guard, the operator decision, and plan 03's suppression fence
    - The `BUG-260710-01` correction is recorded with the real filename
    - All four folded reports verified: `grep -l 'folded_into: "194.1"' .planning/reported-bugs/*.md` returns 4 files
    - The two PARTIAL folds are confirmed as recorded in frontmatter, not only in prose
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)" && n=$(grep -l "folded_into: \"194.1\"" .planning/reported-bugs/*.md | wc -l) && echo "folded=$n" && test "$n" -eq 4 && grep -c "Re-open trigger" .planning/phases/194.1-make-the-stop-visible/194.1-DEFERRED.md && echo DEFERRED_OK'</automated>
  </verify>
  <done>Every deferral has a trigger, the four folds are verified in frontmatter with their partials named, and the nonexistent-id claim is corrected.</done>
</task>

<task type="auto">
  <name>Task 4: ROADMAP, the final gate, and the plan-count statement</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` lines 552-568 (the Phase 194.1 block and its three success criteria)
    - `.planning/phases/194.1-make-the-stop-visible/194.1-BASELINE.md` §11
    - `.planning/REQUIREMENTS.md` line 70 (RUN-01) and its phase-mapping table
  </read_first>
  <action>
Update `.planning/ROADMAP.md`'s Phase 194.1 block: replace the `**Plans**: TBD` line and the
`Plans:` checklist with the eight real plan entries, one line each with its objective, and mark each
`[x]`. Leave the three success criteria as they are — verification, not this plan, judges them.

⚠ **RUN-01 STAYS UNTICKED**, and say why in one line on the ROADMAP row: the L-01 half is unshipped,
`WORKER_COUNT=2` still lets roughly half of stops report `completed`, and the requirement is *"a user
can stop a running workflow at any point, **and the run reports honestly that it was stopped**"* —
this phase delivers the first clause and makes the second clause's failure visible. Do not touch
`REQUIREMENTS.md`'s tick state.

Record the **plan-count decision** in the SUMMARY, because the operator set it as a gate: the phase
shipped in **eight** plans against a directive of *at most 8, fewer if honest.* Seven was evaluated —
merging the panel plan (05) and the run-surface plan (07), which would also have removed the
count-gate-pin serialization by construction — and **rejected on context cost**, not on difficulty: a
merged plan's `<read_first>` spans `WorkflowRunPage.tsx` (1046 L), `WorkspacePanel.tsx` (580 L),
`PendingAskCard.tsx` (486 L) and `ActiveRunsTray.tsx` (141 L) **plus** their four suites, one of which
carries 89 cases. That exceeds a single agent's context budget, which is one of the three legitimate
reasons to split. The two levers the directive named were both taken: **R4 and R6 share one read**
(plan 02 serves both; R6 needed no backend work at all) and **R4 and R5 are one component in two
states** (plan 06). RESEARCH's Q8 proposed the same eight and its plan-01 placement of the ledger rows
was **moved to plan 08** — a ledger row must POSTDATE the change to be accurate, while a
characterization baseline must PREDATE it. Both are recorded, the correction beside the original.

Final gate: `cd frontend && npx tsc --noEmit -p tsconfig.app.json` and
`cd frontend && GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs`, plus
`cd backend && venv/Scripts/python -m pytest tests/unit -q`. Read the count gate's **verdict line**;
record `total` / `failed` / `pinned total` / verdict against §11. ⚠ A grand total larger than any
figure quoted in CLAUDE.md is **the gate working** — its contract is *no per-file DECREASE* and *zero
failing*, never a fixed total.

Also verify the deployment-artifact parity rule does not bind: this phase adds **no env var, no
seed-bearing migration, no bundled service and no sandbox image tag change**, so
`scripts/check-deploy-drift.sh` has nothing to say. Run it and record the result rather than asserting
the exemption.
  </action>
  <acceptance_criteria>
    - `.planning/ROADMAP.md`'s Phase 194.1 block lists all eight plans with objectives, `**Plans**: TBD` gone
    - The ROADMAP block states RUN-01 stays UNTICKED with the L-01 reason
    - `.planning/REQUIREMENTS.md` is byte-unchanged (`git diff --numstat` EMPTY on it)
    - Count gate `OK`, `failed 0`, no per-file DECREASE; `tsc` unmoved from §11; backend unit suite no new failures
    - `bash scripts/check-deploy-drift.sh` run and its output recorded
    - The plan-count decision recorded in the SUMMARY with the rejected 7-plan option and its measured reason
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>The ROADMAP names all eight plans, RUN-01 is explicitly not ticked with its reason, and the whole tree is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: The three G-4 rows — operator-driven, and the L-01 boundary stated up front</name>
  <files>(none — operator-driven checkpoint; no file is written by this task)</files>
  <read_first>
    - `.planning/phases/194.1-make-the-stop-visible/194.1-VALIDATION.md` § *Manual-Only Verifications*
    - `.planning/phases/194.1-make-the-stop-visible/194.1-SPEC.md` (the three G-4 rows and their named failure shapes)
  </read_first>
  <action>
STOP HERE and hand the three G-4 rows to the operator. Present the `<what-built>` summary and the
`<how-to-verify>` briefing below **verbatim** — in particular the L-01 paragraph, BEFORE row 1, so a
`⊘ Stopping this run…` that lands on `✓ Complete` is read as the known boundary it is and not filed as
a defect of this phase. Confirm `visual_workflow_canvas` is ON before row 2 or the row measures the
gate rather than the feature. Record each row's outcome as PASS / FAIL / ⛔-with-reason in
`194.1-VALIDATION.md` § *Manual-Only Verifications*, then tick the corresponding sign-off item. Do NOT
tick RUN-01 in `REQUIREMENTS.md` under any outcome — that decision is the operator's and this plan
declines to make it.
  </action>
  <what-built>
Pressing Stop is now legible on all four surfaces:
 · the composer, the workspace panel, the active-runs tray and — new — the workflow run page's title row
 · on press the control **leaves its slot** and `⊘ Stopping this run…` takes it; there is no button to
   press twice
 · a stop that never resolves returns a pressable control after 8 seconds, saying it was not confirmed
 · a completed run no longer offers a dead Stop in the panel, and its timeline still shows
 · an approval waiting when you stopped stays on screen, visibly retired, and cannot be submitted
 · returning to a stopped workflow thread shows `⊘ Stopped by you · N of M steps · <elapsed>`
 · at workflow kickoff there is no empty assistant bubble — the run line carries the run instead
  </what-built>
  <how-to-verify>
⚠ **BEFORE ROW 2: turn `visual_workflow_canvas` ON in Settings.** It ships **OFF**
(`models/user_settings.py:1197-1203`), and driving row 2 with it off measures the gate, not the feature.

⚠ **READ THIS BEFORE ROW 1, so it is not reported as a defect of this phase.** At the shipped
`WORKER_COUNT=2`, roughly **half of all stops end with the run reporting `completed`** — Phase 194's
SC#2, FAILED, and unfixed here by decision. So on about half of your stops the surface will honestly
show `⊘ Stopping this run…` and then land on **`✓ Complete`**. **That is the L-01 lie becoming
visible, which is progress, not a regression** — and no plan was permitted to hide it. What IS a
defect: the reading never appearing, the button sitting inert, or the row twitching.

**Row 1 — Press.** Start a workflow run, then press Stop in the chat composer.
   *Fails if the button sits inert, or the composer row twitches.*
**Row 2 — Canvas.** Launch via `▶ Run workflow`, and stop the run from the run surface **without
   navigating away**. Then watch what the control does once the run is terminal.
   *Fails if there is no control, or the control persists after the run is terminal.*
**Row 3 — Return.** Stop a run, navigate away, come back to the thread.
   *Fails if the thread ends at the original prompt.*

Record each row PASS / FAIL / ⛔ with what you saw. ⛔ rows are recorded **with a reason** — a
scoreboard that lists only what passed is not a scoreboard.

Then two decisions this plan will not make for you:
 (a) **RUN-01 stays UNTICKED** unless you say otherwise — the L-01 phase is not yet inserted.
 (b) **The composer width** (D-24): jsdom cannot measure it, so row 1 is where it is judged. If the
     row twitches, say so and it becomes a one-line clamp, not a re-plan.
  </how-to-verify>
  <resume-signal>Type "approved" with the three row outcomes, or describe what failed.</resume-signal>
  <verify>
    <human-check>Three G-4 rows driven and recorded with outcomes; the RUN-01 tick decision stated</human-check>
  </verify>
  <done>All three G-4 rows recorded with outcomes (PASS / FAIL / ⛔ with reason), and the RUN-01 decision is the operator's, on the record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed by code | This plan edits documentation only. Its risk is entirely in what it FAILS to record. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-194.1-08-01 | Repudiation | a hot file staying invisible to G-5 | mitigate | Three rows ADDED for files that were absent; the mechanical check is `grep -c "<file>" CLAUDE.md ≥ 1` with each occurrence being a table row, not prose |
| T-194.1-08-02 | Spoofing | a stale ledger row answering the auditor with `satisfied` | mitigate | `StreamsProvider.tsx`'s row corrected BESIDE its old value, with the 28-phase staleness stated plainly so a reader sees the failure rather than a clean number |
| T-194.1-08-03 | Repudiation | a green fence claimed that was never seen red | mitigate | The verification map's Negative-control column is sourced from SUMMARYs, and `NOT OBSERVED` is a permitted value while blank is not |
| T-194.1-08-04 | Repudiation | a deferral surviving only in a deleted line | mitigate | `194.1-DEFERRED.md`, one entry per deferral, each with a `Re-open trigger:` line, mechanically counted |
| T-194.1-08-05 | Spoofing | RUN-01 read as satisfied while half of stops still report `completed` | mitigate | The ROADMAP row states the untick and its reason; the checkpoint hands the tick decision to the operator; the G-4 briefing names the L-01 behaviour up front so it is not mis-filed as a defect |
| T-194.1-08-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed by this plan.** It edits documentation only |
</threat_model>

<verification>
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` → `OK`, `failed 0`, no per-file DECREASE
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` → unmoved from `194.1-BASELINE.md` §11
- `cd backend && venv/Scripts/python -m pytest tests/unit -q` → no new failures
- `bash scripts/check-deploy-drift.sh` → run and recorded
- `grep -l 'folded_into: "194.1"' .planning/reported-bugs/*.md | wc -l` → 4
- `grep -n "Guardrail overrides" .planning/STATE.md` → no Phase-194.1 entry
</verification>

<success_criteria>
- Six ledger rows: three added, two updated in place, one corrected beside its old value; every row
  carries its re-derive commands and states whether G-5 fires
- The Per-Task Verification Map is filled with a non-blank Negative-control cell on every row
- `194.1-DEFERRED.md` exists with a re-open trigger on every entry, incl. the `BUG-260710-01` correction
- ROADMAP lists all eight plans; RUN-01 explicitly stays UNTICKED with its reason; `REQUIREMENTS.md` untouched
- The three G-4 rows are driven by the operator and recorded with outcomes
- The plan-count decision (8, with 7 evaluated and rejected on context cost) is on the record
</success_criteria>

<output>
Create `.planning/phases/194.1-make-the-stop-visible/194.1-08-SUMMARY.md` when done.
</output>
