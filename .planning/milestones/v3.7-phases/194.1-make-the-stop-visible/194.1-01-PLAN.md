---
phase: 194.1-make-the-stop-visible
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/chat/__tests__/StopControl.baseline.test.tsx
  - frontend/src/__tests__/providers/StreamsProvider.stopping.baseline.test.ts
  - frontend/src/__tests__/components/chat/MessageList.runline.baseline.test.tsx
  - frontend/src/components/panel/__tests__/PendingAskCard.retired.baseline.test.tsx
  - .planning/phases/194.1-make-the-stop-visible/194.1-BASELINE.md
autonomous: true
requirements: [RUN-01]

must_haves:
  truths:
    - "Every characterization baseline in this phase was captured on a tree where no source file had moved"
    - "The four shipped Stop mounts are recorded byte-exactly as they render TODAY, before any of them changes"
    - "The two count-gate pins and the current per-file test counts are recorded as numbers, not as claims"
  artifacts:
    - path: ".planning/phases/194.1-make-the-stop-visible/194.1-BASELINE.md"
      provides: "Every pre-change measurement this phase will be judged against"
      contains: "base SHA"
    - path: "frontend/src/components/chat/__tests__/StopControl.baseline.test.tsx"
      provides: "The four mounts' shipped Stop chrome, captured before StopControl exists"
    - path: "frontend/src/__tests__/providers/StreamsProvider.stopping.baseline.test.ts"
      provides: "The shipped resolver behaviour + the shipped kickoff placeholder, captured before either changes"
  key_links:
    - from: "194.1-BASELINE.md"
      to: "git rev-parse HEAD"
      via: "the recorded base SHA the whole phase asserts against"
      pattern: "f9e55b6d"
---

<objective>
Capture every pre-change measurement this phase will be judged against, on the **UNMOVED** tree, in
commits that touch **no source file**.

Purpose: a characterization baseline only proves something if it **predates** the change. This is the
188.1 lesson, re-proved in 193 and 193.1. Phase 194.1 rewrites four Stop mounts, two resolvers, the
kickoff insert and one render gate — every one of those is a place where a post-hoc "baseline" would
be a recording of the bug, not of the shipped behaviour.

Output: one `194.1-BASELINE.md` carrying the base SHA and every number the later plans quote, plus
four baseline test files that pin what the tree does today.

⚠ **This plan writes ZERO production source.** If `git diff --name-only` at the end of any task names
a file outside `frontend/src/**/__tests__/**`, `frontend/src/__tests__/**` or `.planning/**`, the task
has failed its own contract.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/194.1-make-the-stop-visible/194.1-SPEC.md
@.planning/phases/194.1-make-the-stop-visible/194.1-CONTEXT.md
@.planning/phases/194.1-make-the-stop-visible/194.1-RESEARCH.md
@.planning/phases/194.1-make-the-stop-visible/194.1-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Extracted at base SHA f9e55b6d. The executor consumes these directly; no codebase hunt. -->

The four shipped Stop mounts, verbatim at base:

  frontend/src/components/chat/MessageInput.tsx:409-420
    disabled ? <Button onClick={onStop} size="icon" variant="outline"
        aria-label="Stop generation" data-testid="composer-stop"
        className="h-8 w-8 rounded-lg shrink-0 transition-all border-destructive/40 text-destructive hover:bg-destructive/10 hover:border-destructive">
        <Square className="h-3.5 w-3.5 fill-current" /></Button> : <send button>
    ⚠ the "Enter ↵ · Shift+Enter for newline" hint at :404-408 is gated on `!disabled`, so it is
    ALREADY absent whenever the Stop button renders. Record that — it is why the stopping reading
    cannot push it.

  frontend/src/components/panel/WorkspacePanel.tsx:352-354  (the GATE)
    const workflowLock = useWorkflowLockForThread(threadId)
    const isHarness = workflowLock != null
    const showTimeline = isHarness || phases.length > 0
  frontend/src/components/panel/WorkspacePanel.tsx:453-470  (the MOUNT)
    {showTimeline && threadId && (<div …><span>{PANEL_STOP_LEAD}</span>
      <button data-testid="panel-stop-run" onClick={() => void streamActions.stopThread(threadId)}
        aria-label={PANEL_STOP_ARIA} …><Square …/> {PANEL_STOP_LABEL}</button></div>)}

  frontend/src/components/chat/ActiveRunsTray.tsx:126-134
    <button onClick={() => void streamActions.stopThread(id)} aria-label={`Stop run on ${titleFor(id)}`}
      …><Square className="h-2.5 w-2.5 fill-current" aria-hidden="true" /> Stop</button>
    plus the `Stop all` bulk button at :105-112 (2+ runs).

  frontend/src/pages/WorkflowRunPage.tsx — NO Stop control exists.

The two resolvers (byte-mirrors), and the false string that exists TWICE:
  frontend/src/providers/StreamsProvider.tsx:2373-2404  stopStream   (false string at :2394)
  frontend/src/providers/StreamsProvider.tsx:2411-2474  stopThread   (false string at :2457)

The kickoff placeholder R5 deletes, and its three consumers:
  StreamsProvider.tsx:1925-1941  the optimistic assistant insert (runStatus:"streaming", no runId)
  StreamsProvider.tsx:2248-2265  the ApiError 403 arm → blockedNotice ON THAT NODE
  StreamsProvider.tsx:2286-2293  the network-failure arm → runStatus:"failed" ON THAT NODE
  StreamsProvider.tsx:2295-2368  the finally chain (harmless: every write is `m.id === assistantId`)

Test-harness idioms to reuse, never invent:
  frontend/src/__tests__/components/chat/MessageList.test.tsx:20-23  renderML() — bare TooltipProvider,
    NO StreamsProvider, so "the streaming store is empty" is STRUCTURAL, not arranged.
  frontend/src/components/chat/__tests__/ComposerStopHarness.test.tsx:78-112  the vi.hoisted @/lib/api
    mock bundle. ⚠ :92-97 records that leaving `getSnapshot` unmocked kills reconcile SILENTLY.
    ⚠ :16-19: assert the VALUE handed to cancelRun, never `toHaveBeenCalled()` alone.
  frontend/src/__tests__/providers/StreamsProvider.watchdog.test.ts:29-30  the provider-owned-timer
    fake-timer recipe. Use `await vi.advanceTimersByTimeAsync(ms)` inside `act`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assert the dispatched base and open the baseline record</name>
  <files>.planning/phases/194.1-make-the-stop-visible/194.1-BASELINE.md</files>
  <read_first>
    - `.planning/phases/194.1-make-the-stop-visible/194.1-RESEARCH.md` (§ Q7, § Validation Architecture)
    - `CLAUDE.md` § *Parallel execution — worktrees are ENABLED*
    - `scripts/vitest-count-gate.cjs` lines 895-920 (the BASELINE map entries for the two pins)
  </read_first>
  <action>
FIRST ACTION IN THE WORKTREE, before reading anything else: `bash scripts/bootstrap-worktree.sh "$(pwd)"`.

**Then ASSERT THE BASE, and assert the right property.** Every `file:line` in this phase's artifacts was
measured at `f9e55b6db33606fca77ee84ce8ef3da3153d09e2`. What matters is NOT that HEAD equals that SHA — the plan set's own commit already
moved HEAD past it — but that the worktree descends from it and that **no source file this plan quotes
has moved since**:

    git merge-base --is-ancestor f9e55b6db33606fca77ee84ce8ef3da3153d09e2 HEAD    # must exit 0
    git diff --name-only f9e55b6db33606fca77ee84ce8ef3da3153d09e2 HEAD -- frontend/src backend/app scripts supabase

If the ancestor check fails the worktree forked from the WRONG base — **STOP and report.** This project
has measured that happening repeatedly (12/12 worktrees in Phase 192, and again on a single worktree on
2026-08-14). If the diff is non-empty, RE-DERIVE every line number this plan quotes **before** using it
and record the re-derivation in the SUMMARY — an earlier plan in this phase legitimately moves these
files, and a rotted line number quoted as fact is how a plan edits the wrong place.

Create `.planning/phases/194.1-make-the-stop-visible/194.1-BASELINE.md` with the base SHA at the top
and these measured sections, each carrying the command that produced it so the next reader
re-derives rather than trusts:

 §1 Count-gate pins — `grep -n '"WorkflowRunPage.test.tsx"\|"WorkspacePanel.test.tsx"' scripts/vitest-count-gate.cjs`
    (expect 89 and 41). Record BOTH the pin and the file's path entry in TARGETS.
 §2 Which suites are UNGATED — record that `src/components/chat` is absent from TARGETS, so
    `MessageList.test.tsx`, `MessageInput*`, `ActiveRunsTray.test.tsx`, `ComposerStopHarness.test.tsx`
    and the `__tests__/providers` suites carry no pin. Name them; a later plan must not assume a pin
    it does not have.
 §3 Hook/state counts on every mount this phase touches, BEFORE (the D-03 stop condition's baseline):
    for each of `components/chat/MessageInput.tsx`, `components/panel/WorkspacePanel.tsx`,
    `components/chat/ActiveRunsTray.tsx`, `pages/WorkflowRunPage.tsx`, `components/chat/MessageList.tsx`,
    `components/chat/ChatArea.tsx`, `components/panel/PendingAskCard.tsx` record
    `grep -c 'useState[(<]' <f>`, `grep -c 'useEffect(' <f>`, the count of members on its props
    interface, and `wc -l`. Expected at base: MessageInput 1/3/446, WorkspacePanel 4/3/580,
    ActiveRunsTray 2/3/141, WorkflowRunPage 6/7/1046, MessageList 0/1/234, ChatArea 7/4/563,
    PendingAskCard 9/2/486. Record what the commands actually print — if a figure differs, the
    measurement wins and the expectation is recorded beside it.
 §4 The four `grep -cE "workflowLock\??\.runId|cancelRun\("` readings on the four mount components
    (expect 0 on MessageInput / ActiveRunsTray / WorkflowRunPage; record WorkspacePanel's actual —
    its `:161-165` comment names `workflowLock.runId` in PROSE, so the raw count may be non-zero and
    the later fence must be scoped accordingly rather than assumed).
 §5 `grep -rn "press Stop again in a moment" frontend/src --include=*.ts --include=*.tsx` — record
    BOTH hits with their line numbers. The SPEC's `== 0` acceptance is a two-site fence.
 §6 `grep -ric "stopping" frontend/src --include=*.ts --include=*.tsx | grep -v ':0$'` — record every
    hit. ⚠ The SPEC already corrects sketch 168's claim of `0`; it is `1`
    (`useDraftPersistence.ts:463`, a docblock on an unrelated subject). Confirm or correct AGAIN.
 §7 The six G-5 ledger triples, re-derived at THIS commit with the CLAUDE.md recipe
    (`git log --oneline -- <f> | wc -l`; the `sed` bucket recipe; `wc -l`), for
    `StreamsProvider.tsx`, `MessageInput.tsx`, `MessageList.tsx`, `RunCard.tsx`, `WorkspacePanel.tsx`,
    `WorkflowRunPage.tsx`. ⚠ Record the RAW bucket list AND the numeric phase count separately —
    `MessageInput.tsx`, `MessageList.tsx`, `StreamsProvider.tsx` and `RunCard.tsx` all return
    non-phase buckets (`chat`, `backend`, `streaming`, `SEED`, prose subjects). The loser goes beside
    the winner, per this table's habit. ⚠ State plainly that these figures are a WAVE-1 reading and
    that plan 08 must RE-DERIVE them at the commit that writes the ledger rows — this table has gone
    stale on its own afternoon more than once.
  </action>
  <acceptance_criteria>
    - `git rev-parse HEAD` printed and equal to `f9e55b6db33606fca77ee84ce8ef3da3153d09e2`
    - `194.1-BASELINE.md` exists and contains all seven numbered sections, each with the command text
    - §5 records exactly two line numbers for `press Stop again in a moment`
    - §7 records, for each of the six files, three numbers AND the raw bucket list
    - `git diff --name-only` names only `.planning/**`
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)" && test "$(git rev-parse HEAD)" = "f9e55b6db33606fca77ee84ce8ef3da3153d09e2" && grep -qc "press Stop again in a moment" .planning/phases/194.1-make-the-stop-visible/194.1-BASELINE.md && echo BASELINE_OK'</automated>
  </verify>
  <done>The base SHA is asserted and `194.1-BASELINE.md` carries all seven sections with re-derivable commands.</done>
</task>

<task type="auto">
  <name>Task 2: Characterize the four Stop mounts as they render TODAY</name>
  <files>frontend/src/components/chat/__tests__/StopControl.baseline.test.tsx</files>
  <read_first>
    - `frontend/src/components/chat/MessageInput.tsx` (lines 27-56 for Props, 400-436 for the right cluster)
    - `frontend/src/components/panel/WorkspacePanel.tsx` (lines 296-310 for the PANEL_STOP_* constants, 345-360, 445-472)
    - `frontend/src/components/chat/ActiveRunsTray.tsx` (whole file — 141 lines)
    - `frontend/src/pages/WorkflowRunPage.tsx` (lines 855-895 — the header/title row)
    - `frontend/src/components/chat/__tests__/ActiveRunsTray.test.tsx` (the existing render harness for the tray)
    - `frontend/src/components/panel/__tests__/WorkspacePanel.test.tsx` (the existing render harness)
  </read_first>
  <action>
Create `frontend/src/components/chat/__tests__/StopControl.baseline.test.tsx`. It is named for the
component that does NOT exist yet on purpose: it is the characterization of what that component must
preserve.

Capture, for each of the three mounts that HAVE a Stop today:
 - the exact `className` STRING of the shipped control, read out of the rendered DOM and asserted as
   a literal (not a token subset) — composer `data-testid="composer-stop"`, panel
   `data-testid="panel-stop-run"`, tray the `aria-label={`Stop run on …`}` button;
 - its `aria-label` literal, its `data-testid`, and its rendered text content;
 - the shipped `Square` icon's own `className` per mount (`h-3.5 w-3.5 fill-current` composer,
   `h-2.5 w-2.5 fill-current` panel and tray).

And capture, for `WorkflowRunPage.tsx`, the ABSENCE that R3 removes: a source fence asserting
`grep -cE "onStop|stopThread|cancelRun|Stop"` over that file reads **0** at base. ⚠ Print the swept
file's byte length and assert it is > 0 before asserting the count — the 192.1 lesson is that a
fence swept against an empty string passes green and proves nothing.

Add one ABSENCE capture per mount for the state this phase introduces: assert that the string
`Stopping this run` does NOT appear in any of the three rendered DOMs at base. These three
assertions are what will be INVERTED by plan 04/05/06 and they are the proof the reading is new.

⚠ Do NOT add these files to `scripts/vitest-count-gate.cjs`. `src/components/chat` is not in TARGETS
(§2 of the baseline record) and adding a target is a gate-scope change this phase has not scoped.
  </action>
  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/chat/__tests__/StopControl.baseline.test.tsx` exits 0
    - The suite contains at least one literal-`className` assertion per shipped mount (3 total)
    - The `WorkflowRunPage` source fence prints the swept file length and asserts it non-zero before asserting `0` matches
    - Three "`Stopping this run` is absent" assertions are present and green
    - `git diff --name-only` names only files under `frontend/src/**/__tests__/**` and `.planning/**`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/chat/__tests__/StopControl.baseline.test.tsx</automated>
  </verify>
  <done>The three shipped Stop controls are pinned byte-exactly, the run surface's absence is pinned with a non-empty-sweep guard, and the new reading is proved absent everywhere.</done>
</task>

<task type="auto">
  <name>Task 3: Characterize the two resolvers and the kickoff placeholder</name>
  <files>frontend/src/__tests__/providers/StreamsProvider.stopping.baseline.test.ts</files>
  <read_first>
    - `frontend/src/providers/StreamsProvider.tsx` lines 1900-1950, 2236-2300, 2370-2480
    - `frontend/src/components/chat/__tests__/ComposerStopHarness.test.tsx` (whole — the mock bundle and `makeControllableRun`)
    - `frontend/src/__tests__/providers/StreamsProvider.watchdog.test.ts` lines 1-45 (the hoisted mock list and the fake-timer recipe)
    - `frontend/src/__tests__/providers/streamsProvider_state01b_403.test.tsx` (the shipped 403 amber-notice suite — this is the honesty path R5 must not silence)
  </read_first>
  <action>
Create `frontend/src/__tests__/providers/StreamsProvider.stopping.baseline.test.ts` capturing what the
provider does TODAY, before plan 03 touches it. Reuse `ComposerStopHarness`'s `vi.hoisted` bundle
shape over `@/lib/api` — and include `getSnapshot` AND `getThreadWorkflow` in it even though the
baseline does not exercise the latter (`ComposerStopHarness.test.tsx:92-97` records that an unmocked
`getSnapshot` kills reconcile SILENTLY; `getThreadWorkflow` is what plan 03 will add).

Capture five behaviours, each as its own `it()` so a later regression names itself:

 1. **The bucket-scan resolution works today** — seed the chat bucket with a streaming assistant row
    carrying `runId`, call `stopThread(threadId)`, assert `cancelRun` was called **with that exact
    VALUE**. Never `toHaveBeenCalled()` alone (`ComposerStopHarness.test.tsx:16-19`).
 2. **The silent no-op is real** — empty bucket, call `stopThread`, assert `cancelRun` was NOT called
    at all. This is the defect R6 repairs; pinning it is what makes plan 03's fix observable.
 3. **`stopStream` is the byte-mirror** — same two cases through `stopStream()` with
    `activeThreadIdRef` pointing at the thread. Same outcomes.
 4. **The harness kickoff inserts an assistant placeholder today** — drive `sendMessage` with
    `workflowDefinitionId` set and assert the chat bucket ends with exactly ONE assistant row whose
    `runStatus === "streaming"` and whose `runId` is undefined. This is the node R5 deletes.
 5. **A Deep send inserts the same placeholder** — same drive WITHOUT `workflowDefinitionId`, same
    assertion. This is 174 D-14's byte-identity baseline and plan 03 must leave it green untouched.

Also capture the two honesty paths R5 endangers, as their own `it()`s:
 6. **403 kill-switch** — `postMessage` rejects `new ApiError(msg, 403)` with `workflowDefinitionId`
    set; assert an assistant node bearing `blockedNotice.message === msg` is present in the bucket.
 7. **Network failure** — `postMessage` rejects a plain `Error` with `workflowDefinitionId` set;
    assert an assistant node with `runStatus === "failed"` is present.

⚠ Cases 6 and 7 are the ones plan 03 is REQUIRED to keep green while deleting the node they currently
ride on (D-23). If either cannot be made to fire at base, say so in the SUMMARY and in
`194.1-BASELINE.md` §8 rather than writing a case that passes vacuously — a fence that cannot fire is
worse than no fence.
  </action>
  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/__tests__/providers/StreamsProvider.stopping.baseline.test.ts` exits 0 with 7 passing cases
    - Case 1 and case 3 assert the VALUE passed to `cancelRun`, not a call count
    - Case 2 asserts `cancelRun` was not called (the shipped silent no-op, pinned)
    - Cases 6 and 7 each assert a concrete property of a concrete node — or are recorded as un-fireable in `194.1-BASELINE.md` §8 with the reason
    - `git diff --name-only` names only test files and `.planning/**`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/__tests__/providers/StreamsProvider.stopping.baseline.test.ts</automated>
  </verify>
  <done>Both resolvers, the kickoff placeholder on both paths, and the two honesty arms that ride on it are pinned before any of them moves.</done>
</task>

<task type="auto">
  <name>Task 4: Characterize the transcript's silence and the approval card's survival</name>
  <files>frontend/src/__tests__/components/chat/MessageList.runline.baseline.test.tsx, frontend/src/components/panel/__tests__/PendingAskCard.retired.baseline.test.tsx</files>
  <read_first>
    - `frontend/src/__tests__/components/chat/MessageList.test.tsx` lines 1-60 and 94-97
    - `frontend/src/components/chat/MessageList.tsx` (whole — 234 lines)
    - `frontend/src/components/panel/PendingAskCard.tsx` lines 240-330 and 420-486
    - `frontend/src/components/panel/__tests__/PendingAskCard.test.tsx` (the existing harness, incl. its fake-timer usage at ~:398-415)
  </read_first>
  <action>
Create `frontend/src/__tests__/components/chat/MessageList.runline.baseline.test.tsx`, using
`MessageList.test.tsx`'s `renderML()` shape (bare `TooltipProvider`, **no `StreamsProvider`**, so the
streaming store is empty STRUCTURALLY rather than by arrangement — this is the property R4's central
fence depends on, and pinning it here is what stops a later plan from quietly mounting a provider).
Capture:
 - rendering a transcript of exactly one USER message produces a DOM containing **no** occurrence of
   `Stopped by you`, **no** `⊘`, and **no** element with `data-testid="thread-run-line"`. That is
   `BUG-260816-02` as a measurement: the thread ends at the prompt.
 - `MessageList`'s Props interface today carries **no** `threadId` member — assert it from the
   component's `?raw` source (a source fence, with the swept length printed and asserted non-zero).
   Plan 07 adds that prop and this is the before.

Create `frontend/src/components/panel/__tests__/PendingAskCard.retired.baseline.test.tsx`. Capture:
 - a `pending` card renders its submit button with `disabled={false}` and `aria-disabled="false"`
   when an answer is present — i.e. it IS dispatchable today;
 - the three retirement sentences that exist today are captured as literals read out of the source
   (`?raw`), so plan 05's pairwise-distinct fence has a real, non-invented set to compare against:
   (a) the 404-expiry literal at ~`:280`, (b) the countdown-timeout literal at ~`:317`, (c) the
   no-deadline fallback at ~`:318`. Record all three verbatim in `194.1-BASELINE.md` §9.
   ⚠ Read them, do not re-type them — a fence built on a re-typed literal tests the typing.
  </action>
  <acceptance_criteria>
    - Both new suites exit 0 under `GSD_VITEST_MAX_WORKERS=2`
    - The `MessageList` suite renders WITHOUT `StreamsProvider` and asserts the three absences
    - The `MessageList` source fence prints the swept source length and asserts it non-zero
    - `194.1-BASELINE.md` §9 contains the three PendingAskCard retirement literals, quoted verbatim from source
    - `git diff --name-only` names only test files and `.planning/**`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/__tests__/components/chat/MessageList.runline.baseline.test.tsx src/components/panel/__tests__/PendingAskCard.retired.baseline.test.tsx</automated>
  </verify>
  <done>The transcript's silence after a stop and the approval card's present dispatchability are both pinned, and the three shipped retirement sentences are recorded verbatim.</done>
</task>

<task type="auto">
  <name>Task 5: Prove the tree is unmoved, run the full gate, and commit</name>
  <files>.planning/phases/194.1-make-the-stop-visible/194.1-BASELINE.md</files>
  <read_first>
    - `scripts/vitest-count-gate.cjs` lines 1-60 (the gate's own rules) and 895-920
    - `.planning/phases/194.1-make-the-stop-visible/194.1-BASELINE.md` (as written by tasks 1-4)
  </read_first>
  <action>
Run `git diff --numstat` across everything this plan wrote and assert that **no path outside**
`frontend/src/**/__tests__/**`, `frontend/src/__tests__/**` and `.planning/**` appears. Record the
numstat output verbatim in `194.1-BASELINE.md` §10 under the heading *"The tree was unmoved when these
baselines were taken"* — that sentence is the whole reason this plan exists as its own wave and it
must be evidenced, not asserted.

Run the typecheck (`cd frontend && npx tsc --noEmit -p tsconfig.app.json` — ⚠ the bare `--noEmit`
form checks ZERO files) and record the error count. Then run the full gate:
`cd frontend && GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs`. Read the **verdict
line**, never a summary; record `total`, `failed`, `pinned total` and the OK/FAIL verdict in §11.
⚠ The gated grand total is EXPECTED to be larger than any figure quoted in CLAUDE.md — a growing
number is the gate working. Its contract is *no per-file DECREASE* and *zero failing*.

⚠ If `failed` is non-zero, CAPTURE THE FAILING FILENAMES BEFORE RE-RUNNING ANYTHING (the 193.2-02
lesson — a plan that re-ran first could not afterwards prove its own cases were innocent). Cap 2 is
mandatory and is NOT deterministic; record every reading.

Commit with `docs(194.1): wave-0 baselines on the unmoved tree + the phase's measured record`.
  </action>
  <acceptance_criteria>
    - `194.1-BASELINE.md` §10 contains the verbatim `git diff --numstat` and no source path appears in it
    - §11 records `tsc` error count, and the gate's `total` / `failed` / `pinned total` / verdict
    - Count gate reports `failed 0` (or every failing filename is captured verbatim in §11 before any re-run)
    - One commit exists whose changed-file list contains no production source file
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>The baselines are committed, evidenced as having been taken on an unmoved tree, and the gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | This plan writes only test files and planning documents. No route, no query, no client-visible behaviour changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-194.1-01-01 | Tampering | the baseline record itself | mitigate | Every figure carries the command that produced it, so a wrong number is re-derivable rather than authoritative; the base SHA is asserted mechanically in task 1 |
| T-194.1-01-02 | Repudiation | "the tree was unmoved" | mitigate | Task 5 records the verbatim `git diff --numstat` rather than claiming the property (188.1 lesson) |
| T-194.1-01-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed by this plan.** RESEARCH § *Environment Availability*: "No new dependency is required by any recommendation in this file." A task that reaches for one must stop and escalate. |
</threat_model>

<verification>
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` → verdict `OK`, `failed 0`
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` → error count recorded (baseline is 33 at the tree's last reading; record the actual)
- `git diff --numstat` over the plan's commits names no file under `frontend/src` outside a `__tests__` directory
</verification>

<success_criteria>
- Base SHA `f9e55b6d…` asserted before any file was written
- `194.1-BASELINE.md` exists with §§1-11, every figure re-derivable from a recorded command
- Four baseline suites green; the three shipped Stop mounts pinned byte-exactly; the run surface's
  absence pinned with a non-empty-sweep guard
- The two honesty arms (403, network) captured green at base, or recorded as un-fireable with a reason
- The six G-5 triples recorded as a WAVE-1 reading, with the explicit note that plan 08 re-derives them
</success_criteria>

<output>
Create `.planning/phases/194.1-make-the-stop-visible/194.1-01-SUMMARY.md` when done.
</output>

