---
phase: 194.1-make-the-stop-visible
plan: 05
subsystem: workspace-panel
tags: [stop, run-honesty, dead-control, retirement, shared-component, cross-mount, tdd]
requires:
  - "194.1-01 (StopControl.baseline.test.tsx + PendingAskCard.retired.baseline.test.tsx — captured on an unmoved tree)"
  - "194.1-03 (the stopping slice: two Sets keyed by thread, the 8s timer, the primitive selectors)"
  - "194.1-04 (StopControl.tsx — the shared component, its four variants and its shipped-chrome table)"
provides:
  - "showStopRow — the SECOND boolean, so a finished run loses its Stop and keeps its timeline"
  - "the panel and tray mounts of the shared StopControl (three of four mounts now live)"
  - "RUN_STOPPED_RETIREMENT_LINE + the derived `retired` arm on PendingAskCard"
  - "the composer+panel cross-mount case — D-06's only real proof"
  - "the WorkspacePanel.test.tsx count-gate pin, re-derived 41 → 58"
affects:
  - "the panel Stop (no longer offered on a finished run); the tray Stop (now pressed-state aware)"
  - "PendingAskCard — one new OPTIONAL prop; PendingAskStack — two new reads"
  - "ActiveRunsTray — one fewer stopThread call site; Stop all byte-untouched"
tech-stack:
  added: []
  patterns:
    - "two booleans where one had two meanings, rather than narrowing the shared one"
    - "a derived render arm short-circuiting ABOVE the gate it must not perturb"
    - "backing a module mock's selectors with the REAL store, so two mounts share one source"
key-files:
  created: []
  modified:
    - frontend/src/components/panel/WorkspacePanel.tsx
    - frontend/src/components/chat/ActiveRunsTray.tsx
    - frontend/src/components/panel/PendingAskCard.tsx
    - frontend/src/components/panel/__tests__/WorkspacePanel.test.tsx
    - frontend/src/components/panel/__tests__/PendingAskCard.test.tsx
    - frontend/src/components/panel/__tests__/PendingAskCard.retired.baseline.test.tsx
    - frontend/src/components/chat/__tests__/ActiveRunsTray.test.tsx
    - frontend/src/components/chat/__tests__/StopControl.baseline.test.tsx
    - scripts/vitest-count-gate.cjs
decisions:
  - "D-25 honoured literally: showTimeline and showBatchResults are byte-unchanged; the Stop row gets its own boolean"
  - "Stop all stays byte-untouched — a bulk control over N threads is not a per-thread mount, and the rows answer for it"
  - "The retired arm RENDERS the submit control visibly dead rather than deleting it, for the same reason the card stays"
  - "The cross-mount case lives in WorkspacePanel.test.tsx because a fourth file would land UNGATED"
metrics:
  duration: "~2h"
  completed: "2026-08-16"
  tasks: 5
  commits: 7
---

# Phase 194.1 Plan 05: The Dead Control, the Shared One, and the Retired Approval — Summary

A finished run no longer offers a Stop that does nothing, and still shows the timeline the user came
to look at; the panel and the tray now render the same `StopControl` the composer does, so pressing
either retires both for one thread; and an approval waiting when the run was stopped stays on screen,
visibly retired, saying something the three shipped expiries do not.

---

## What shipped, task by task

| Task | Commit | What |
|---|---|---|
| 1 | `3b0e253e` | **RED** — a completed run must lose its Stop and KEEP its timeline |
| 1 | `a5ec0f57` | **GREEN** — `showStopRow = isHarness && threadId != null`, beside an untouched `showTimeline` |
| 2 | `6f33d9a2` | The shared `StopControl` mounted on the panel and the tray |
| 3 | `e3cc74b3` | **RED** — the retired approval: nine cases + the four-way distinctness fence |
| 3 | `5aa9314e` | **GREEN** — the derived `retired` arm and `RUN_STOPPED_RETIREMENT_LINE` |
| 4 | `c62ce86d` | The REAL cross-mount case — composer + panel, one thread |
| 5 | `717b5b92` | The pin bump `41 → 58`, in the commit with its cases |

---

## P1–P6 — every plant observed RED, with the case that fired

All six were driven against **real plants in production source**; every file was restored and
**proved** restored by `md5sum`, not merely edited back.

| # | The plant | What fired | Reading |
|---|---|---|---|
| **P1** | narrow `showTimeline` itself (`= isHarness`) | *a COMPLETED run … and its timeline STILL renders* + one shipped Phase-094 case | **2 failed.** ⚠ **The no-Stop clause of the SAME case stayed GREEN** — the mechanical proof that the one-assertion version would have shipped the Phase-098 regression. |
| **P2** | gate the row on the literal shipped `showTimeline && threadId` | *a COMPLETED run …* | **1 failed, and only it**, with `expected <button> to be null`. |
| **P3** | return `null` when the run is over | *the card is STILL IN THE DOM* (+4 riders) | **5 failed.** |
| **P4** | retire it visually but leave `aria-disabled={false}` and drop `disabled` | *a chosen answer becomes UNDISPATCHABLE* | **1 failed, and only it.** |
| **P5** | reuse `:280`'s shipped 404 sentence as the retired one | the four-way set-size clause | **2 failed** — `expected 3 to be 4`. |
| **P5b** | a sentence that is UNEQUAL to `:280` but reuses its second half | the shared-complete-sentence clause | **1 failed** — *"sentences 0 and 3 share a complete sentence: `the run is no longer active`"*. ⚠ **The set-size clause stayed GREEN at 4.** This is the 193.2 shape reproduced exactly, and it is the only evidence the stronger clause is not redundant. |
| **P6** | move the flag into a `useState` inside `MessageInput` | *pressing the PANEL's Stop removes the COMPOSER's control too* | **1 failed, and only it.** See below — two things stayed green and both matter. |

### ⚠ P6's honest reading — what did NOT fire is the argument for the case existing

Two green results under P6 are recorded because a reader would otherwise assume the cross-mount case
was redundant:

1. **The SYMMETRIC case stayed GREEN.** Pressing the composer and asserting the panel's control
   vanished passed under the plant — because a component-local flag also hides the composer's *own*
   button. **So even a two-mount test is blind if it only presses the mount it then inspects.** The
   direction that fires is panel-press → composer-absent.
2. **Plan 04's two-instance ANCESTOR stayed GREEN — 31 passed.** The one case that fired in
   `StopControl.test.tsx` was its **source fence** (the `<StopControl` mount count), which noticed
   that someone had stopped mounting the shared component. That catches a **shape**, not the
   property: a source fence observing an import is not any test observing that two live mounts share
   one source. This plan's case is the only thing in the phase that measures the property.

---

## Deviations from Plan

### 1. [Rule 3 — blocking] The worktree forked from the WRONG base — now **5/5** for this phase

HEAD arrived at `fda79214` (a `master` merge). `git merge-base HEAD 7c79aa12` returned `3781a3fe`,
i.e. **no descent from the dispatched base at all** — the identical failure the prompt predicted and
that plans 01-04 each recorded. Corrected with `git reset --hard 7c79aa12`, then re-asserted:
`git merge-base --is-ancestor f9e55b6d HEAD` exits 0.

⚠ The source-drift diff was **non-empty** (18 files — waves 1-3), so re-derivation was owed. The
result is worth recording: **`WorkspacePanel.tsx`, `ActiveRunsTray.tsx`, `PendingAskCard.tsx` and
`vitest-count-gate.cjs` are NOT in the drift list**, so every `file:line` this plan quotes about them
was valid as measured. `streamsStore.ts` and `StreamsProvider.tsx` ARE in it, so the plan's
`streamsStore.ts:161-174` citation was treated as approximate and the invariant was re-located rather
than quoted by line.

### 2. [Rule 1] ⚠ MY OWN DISPATCHABILITY ASSERTION PASSED VACUOUSLY, and only the RED gate caught it

The first draft of *"its submit control is not dispatchable"* rendered a retired card cold and
asserted `disabled === true`. **That proves nothing**: `canSubmit` requires `hasAnswer`, so a plain
`pending` card with no option chosen is **already** disabled on both axes — the assertion was GREEN
against a component with no retirement arm at all, and would have shipped a fence **P4 could not
red**.

Rewritten to drive the lived sequence on ONE instance: pick an answer → assert genuinely enabled →
*then* end the run → assert disabled on both axes. The RED gate's failure count went **5 → 6**, which
is the evidence the fence became real. This is the same family as `194.1-BASELINE.md` §8's
requirement that a fence be shown to fire, applied to a fence I wrote myself.

### 3. [Rule 1] Two acceptance greps read high — **on my own prose**, exactly plan 04's deviation 5

`grep -c "<StopControl"` read **3** on the panel and **2** on the tray; `grep -c "stopThread("` read
**2** on the tray. Every excess was a docblock discussing the thing it counts. **The PROSE was fixed,
never the needle** — the bare identifier in prose, the JSX-open token reserved as the needle, and the
resolver written without its parenthesis. Both files now say so in the place a later editor would be
tempted to tidy, per the `run_lifecycle.py` app-shutdown-gate discipline.

⚠ Related: `WorkspacePanel.tsx`'s import block now records that **neither the retired import's name
nor the resolver's name is spelled anywhere in the file, including in the sentence explaining their
absence** — because `WorkspacePanel.test.tsx`'s own F-1/V-05 fence sweeps that file's RAW source
un-stripped, and plan 04 already tripped it once.

### 4. [Rule 1] Two `tsc` excursions, both mine, both recorded rather than smoothed

- **33 → 35 after Task 1.** My fixture wrote `status: "completed"`. `Phase.status`'s terminal is
  **`"done"`**; `"completed"` belongs to `Message.runStatus`. ⚠ **This is the mirror image of the
  mistake `194.1-BASELINE.md` §11 records against itself** (`runStatus: "done"`, the SSE terminal
  *kind* written where the persisted *status* belonged). Two vocabularies for one moment, and the
  compiler is the only thing that tells them apart.
- **33 → 34 after Task 3.** A missing `beforeEach` import in the test file.

Both corrected; the reasoning for the first is recorded in the case itself.

### 5. [Scope] Two files outside the frontmatter `files_modified` were edited — both forced supersedings

`StopControl.baseline.test.tsx` and `PendingAskCard.retired.baseline.test.tsx` are named in the
tasks' own `<files>` / `<action>` bodies (Task 2 mandates the first, Task 3's fence reads the second),
but not in the frontmatter list. Both were **superseded in place with their originals quoted**, never
rewritten wholesale. Recorded here so the diff's nine files can be reconciled against the
frontmatter's seven.

---

## Superseded assertions — four, every original quoted verbatim

| File | What changed | Why |
|---|---|---|
| `WorkspacePanel.test.tsx` — *"mounts for a phases-exist thread with no lock"* | **inverted** | ⚠ **That assertion PINNED THE DEFECT.** It asserted `panel-stop-run` present on exactly the completed-run shape `BUG-260816-01` is about. Now asserts the absence **and** the timeline's survival. |
| `StopControl.baseline.test.tsx` — *"the lead sits in a sibling node"* | re-scoped to the slot's parent | ⚠ **PREDICTED BY NAME in `194.1-04-SUMMARY.md` before the causing code existed.** The mount wraps the button in a slot `<div>`, so `btn.parentElement` read `' Stop'`. The load-bearing clause (the lead is never inside the button) is **unweakened**. |
| `ActiveRunsTray.test.tsx` — `stopThread(` count | **2 → 1** | The per-row dispatch moved into the shared component. ⚠ **Kept EXACT rather than relaxed to `toBeGreaterThan(0)`** — an exact count is the only thing that catches a second cancel path returning. A new `<StopControl` count clause was added so the lower number cannot read as the Stop having been deleted. |
| `PendingAskCard.retired.baseline.test.tsx` — the `?raw` line pin | **487 → 630** | Task 3 grew the file. Kept exact with the re-derivation recipe recorded; its job is to prove the `?raw` import is reading the module it thinks it is. |

---

## Measurements

### D-03 stop condition — `useState` and `useEffect` UNMOVED on all three files

| File | `useState[(<]` | `useEffect(` | props | `wc -l` |
|---|---|---|---|---|
| `panel/WorkspacePanel.tsx` | **4 → 4** ✅ | **3 → 3** ✅ | **5 → 5** ✅ | 580 → 646 |
| `chat/ActiveRunsTray.tsx` | **2 → 2** ✅ | **3 → 3** ✅ | 1 → 1 | 141 → 164 |
| `panel/PendingAskCard.tsx` | **9 → 9** ✅ | **2 → 2** ✅ | 2 → **3** (one OPTIONAL prop) | 486 → 629 |

Every BEFORE column reproduces `194.1-BASELINE.md` §3 exactly. **`useState` unmoved on all three is
what discharges D-03**: the pressed state stayed store-owned, the retirement is derived rather than
stored, and **no G-5 refactor recommendation is owed by this plan.** `WorkspacePanel.tsx`'s ledger
row records `useState 4→4 / useEffect 3→3 / fetch 0→0 / props 5→5` as Phase 194's
honoured-by-construction test; all four are unmoved here too.

⚠ **The line growth is PROSE, not logic** — the fifth consecutive measurement of that pattern in this
project. The panel's whole change is one boolean and one JSX element; the card's is one constant, one
prop, one derived expression and one render arm.

### The acceptance greps, in the plan's literal RAW form

| Grep | Expected | Actual |
|---|---|---|
| `grep -c "<StopControl" WorkspacePanel.tsx` | 1 | **1** ✅ |
| `grep -c "<StopControl" ActiveRunsTray.tsx` | 1 | **1** ✅ |
| `grep -c "stopThread(" WorkspacePanel.tsx` | **0** | **0** ✅ |
| `grep -c "stopThread(" ActiveRunsTray.tsx` | 1 (`Stop all` only) | **1** ✅ |
| `grep -cE "workflowLock\??\.runId\|cancelRun\(" WorkspacePanel.tsx` | 0 | **0** ✅ |
| `grep -cE "workflowLock\??\.runId\|cancelRun\(" ActiveRunsTray.tsx` | 0 | **0** ✅ |
| `grep -c "useState[(<]" PendingAskCard.tsx` | 9 | **9** ✅ |
| `grep -n "098" WorkspacePanel.tsx` | one line, also naming `showTimeline` | **`:358`, one line** ✅ |

⚠ **ONE PLAN INSTRUCTION WAS MEASURED UNNECESSARY AND IS CORRECTED RATHER THAN FOLLOWED.** The plan
says the `workflowLock?.runId|cancelRun(` fence *"may be non-zero because `WorkspacePanel.tsx`'s
`:161-165` comment names `workflowLock.runId` in PROSE — scope the fence to code lines (strip comments
before counting)."* **The RAW count is 0 on all three files, before and after**, so no stripping was
applied and none is warranted. `194.1-BASELINE.md` §4 measured the same and explained why: the comment
says *"the lock's id field"* and *"the panel's workflow lock"* — it never spells the token. §4's
standing conclusion holds and is stronger than the plan anticipated. *A prediction about a grep is not
a grep.*

### `showTimeline` is byte-identical (D-25's core requirement)

```
:380  const showTimeline = isHarness || phases.length > 0
:381  const showBatchResults = showTimeline && tasks.length > 0
:407  const showStopRow = isHarness && threadId != null
```

`showTimeline` still gates the run soul (`:459`), the run receipt (`:470`), the timeline section and
`showBatchResults`. Only the control moved.

### Typecheck

`cd frontend && npx tsc --noEmit -p tsconfig.app.json` → **33**, the `194.1-BASELINE.md` §11 figure,
measured after **every** task. Transiently 35 and 34; both excursions are mine and are named above.
⚠ The bare `--noEmit` form checks ZERO files in this repo.

### Count gate — every reading, not the best one

```
cd frontend && GSD_VITEST_MAX_WORKERS=2 node ../scripts/vitest-count-gate.cjs
```

| Run | verdict | total | failed | pinned | present |
|---|---|---|---|---|---|
| 1 (pre-bump) | FAIL `[failing-tests]` | 3959 | **8** | 3868 | 75/75 |
| 2 (pre-bump) | FAIL `[failing-tests]` | 3959 | **9** | 3868 | 75/75 |
| 3 (pre-bump) | FAIL `[failing-tests]` | 3954 | **5** | 3868 | 75/75 |
| 4 (pre-bump) | FAIL `[failing-tests]` | 3959 | **5** | 3868 | 75/75 |
| **A (post-bump)** | **`count gate OK`** | **3959** | **0** | **3885** | **75/75** |
| **B (post-bump)** | **`count gate OK`** | **3959** | **0** | **3885** | **75/75** |

⚠ **THE CAPTURE-BEFORE-RERUN RULE WAS KEPT.** The failing tests were named from the gate's own JSON
reports **before** anything was changed, and all three reports agree:

- Every failure is `Error: STACK_TRACE_ERROR` — **the oversubscription signature `CLAUDE.md` names
  explicitly**, never an assertion failure.
- Every failure is in **`WorkflowsPage.test.tsx`** or **`WorkflowCard.test.tsx`**. **Neither file is
  in this plan's `files_modified`, and neither appears in its diff.**
- **Four of the five were identical across all three runs and the fifth rotated** — the signature of a
  timeout budget, not a defect.
- **Both suites pass in ISOLATION** (`WorkflowsPage.test.tsx` → 55/55). That is the clean proof.
- A **sibling executor (plan 06) was running concurrently**, which `CLAUDE.md` documents as pushing the
  gate non-deterministic regardless of cap.

⚠ **A separate reading is published so nobody repeats the experiment:** running the gate's 21 TARGETS
in ONE `npx vitest` process (rather than through the gate) reported **178 failed**. That number is
meaningless — a single process over the whole target set oversubscribes massively — and it is recorded
only so a future reader does not mistake it for a real regression.

⚠ **`3959` is larger than the `3954` plan 04 measured and the `3918` `CLAUDE.md` quotes. That is the
gate WORKING**: its contract is *no per-file DECREASE* and *zero failing*, never a fixed grand total.

### The pin — re-derived, not trusted

| | pin before | actual | pin after |
|---|---|---|---|
| `WorkspacePanel.test.tsx` | **41** | **58** (two agreeing runs, gate's own `actual` column) | **58** |
| `WorkflowRunPage.test.tsx` | 89 | 89 | **89 — byte-unchanged** (plan 06 owns it) |

⚠ **THE `41` WAS ALREADY STALE BY TWELVE BEFORE THIS PLAN ADDED A CASE**, exactly as the prompt warned
and as `194.1-BASELINE.md` §11 measured (actual 53 vs pin 41 at Wave 1). The gate was satisfied the
whole time, because its contract is *no DECREASE*, not equality. **So the `+17` is 12 INHERITED + 5
mine**, and a reader who takes `58 − 41` as this plan's case count is wrong by more than double. *A pin
is a floor, never a census.* The entry in the gate says so, with the five broken out.

`git diff -U0 scripts/vitest-count-gate.cjs` filtered to non-comment lines returns **exactly one**:
`"WorkspacePanel.test.tsx": 41,` → `58,`. No `TARGETS` entry was added (`src/components/chat` stays
deliberately absent per BASELINE §2).

### Suites

| Suite | result |
|---|---|
| `WorkspacePanel.test.tsx` | **58 passed** (was 53 actual / 41 pinned) |
| `PendingAskCard.test.tsx` | **41 passed** |
| `src/components/panel/__tests__` + `src/components/chat/__tests__` | **29 files / 397 passed** |

---

## Pre-existing failures outside this plan's scope — NOT fixed, and why

A widened sweep including `src/__tests__` reports **20 failures**: `IngestionPage`, `MessageItem`,
`Plan04.frontend`, `useMessages`, `StreamsProvider.dedup`, `streamsProvider` and siblings. **None is
in a file this plan touches**, all are in the ungated `src/__tests__` tree (BASELINE §2), and they are
the documented **SEED-056** rot set. `useMessages.test.ts` specifically is already logged in this
phase's `deferred-items.md` with import-containment evidence and the prompt states it is not mine.
No `deferred-items.md` entry was added, because these are already recorded.

---

## Known Stubs

None. The fourth `StopControl` variant (`page`, for `WorkflowRunPage.tsx`) remains defined and
unmounted — that is **plan 06's declared scope**, not a stub: plan 04's suite exercises it, and
`WorkflowRunPage.tsx` having no Stop control at all is `BUG-260816-01`'s second half.

---

## Threat Flags

None. No new network surface, no new route, no relaxed typing, **no package installed**
(T-194.1-05-SC holds).

- **T-194.1-05-01** (a Stop offered on a finished run) — **mitigated and PROVED**: the row gates on
  `isHarness`, whose invariant is cleared on every terminal route; P2 drove it RED against the literal
  shipped gate.
- **T-194.1-05-02** (narrowing `showTimeline`, regressing the Phase-098 finished-run timeline) —
  **mitigated**: `showTimeline` is byte-identical, and P1 demonstrated that the single-assertion form
  of the case would have let the regression through.
- **T-194.1-05-03** (submitting an answer into a cancelled run) — **mitigated**: the retired arm
  short-circuits above `canSubmit`, both disabled axes are hard-`true`, there is no `onClick`, and P4
  reds. ⚠ **The server's 404 remains the authority** — this is defence in depth, not a replacement.
- **T-194.1-05-04** (a card that vanishes mid-read) — **mitigated**: P3 reds the still-in-DOM
  assertion.
- **T-194.1-05-05** (retirements wearing one coat) — **mitigated over FOUR sentences and all SIX
  pairs**, with the three shipped ones read from source. P5 reds a whole reuse and **P5b reds a
  partial one that the set-size clause cannot see**.

---

## RUN-01 is NOT claimed ticked

Inherited from plans 03 and 04 and unchanged. The **L-01 boundary is a statement, not a fix**: at the
shipped `WORKER_COUNT=2` roughly half of stops still end with the run reporting `completed`, because a
producer on the other worker writes the terminal over the cancel (Phase 194 SC#2, FAILED). What this
plan adds is that the surface stops **offering** actions it cannot perform and stops **hiding** the
consequences of ones it did. **No requirement was marked complete, and no `gsd-sdk query state.*`,
`requirements.mark-complete` or `roadmap.update-plan-progress` verb was invoked.** `STATE.md` and
`ROADMAP.md` were never opened for writing.

⚠ **Two G-4 rows are OWED and this plan cannot discharge them**, because jsdom returns 0 for all
layout: whether the panel's Stop row **twitches** when the control yields to the reading, and whether
the retired approval card reads as *calm* rather than *broken*. Both are operator judgements.

---

## G-5 hot-file ledger

**`frontend/src/components/panel/WorkspacePanel.tsx` is a G-5-FIRING row (9 phases).** No row is
updated by this plan and that is deliberate: `194.1-CONTEXT.md` D-01 assigns all ledger-row work to
**plan 08**, so it finds one home rather than several. The obligation is **honoured by construction**
on the same measured test the row itself records — `useState 4→4`, `useEffect 3→3`, `fetch 0→0`,
`props 5→5`. **The panel gained a CONTROL and a boolean, not state ownership.**

**What plan 08 should carry forward from here (re-derive rather than trust — these staled on this
plan's own commits):**

| File | on the ledger? | after this plan |
|---|---|---|
| `panel/WorkspacePanel.tsx` | ✅ present, inherits `14 / 9 / 580` | **646 L** — the triple needs re-deriving |
| `chat/ActiveRunsTray.tsx` | **ABSENT** | **164 L** — worth a row on the `WorkflowCard.tsx` precedent |
| `panel/PendingAskCard.tsx` | **ABSENT** | **629 L** — its `useState` count of 9 is the number a future D-03 check reads |

---

## Self-Check: PASSED

All nine modified paths exist on disk. All seven commits resolve in `git log --all`: `3b0e253e`,
`a5ec0f57`, `6f33d9a2`, `e3cc74b3`, `5aa9314e`, `c62ce86d`, `717b5b92` → **7/7**.
`git diff --diff-filter=D` against the base names **no deleted file**. `git diff --name-only` against
the base names **exactly the nine files above and nothing else**. `git status --short` is clean apart
from this SUMMARY. `STATE.md` and `ROADMAP.md` were never opened for writing.

⚠ One working-tree entry was investigated rather than committed:
`frontend/src/components/workflows/__snapshots__/canvasModel.fixtures.test.ts.snap` appeared modified
after a test run, but `git diff --numstat` reported **zero insertions and zero deletions** — a pure
CRLF/LF artifact of the snapshot being rewritten with identical content. It was restored with
`git checkout -- <file>` so it cannot pollute the merge.
