---
phase: 192-workflow-library-ia
plan: 16
type: execute
wave: 4
gap_closure: true
gap_closure_round: 1
depends_on: [192-13, 192-14, 192-15]
files_modified:
  - scripts/vitest-count-gate.cjs
  - .planning/ROADMAP.md
  - CLAUDE.md
  - .planning/phases/192-workflow-library-ia/192-UAT.md
autonomous: false
requirements: [LIB-01, LIB-02, LIB-03, LIB-04]

must_haves:
  truths:
    - "Every suite this round grew is pinned at a number READ from the gate, so a later deletion of one of these regression tests reds the gate instead of passing quietly"
    - "The five negative fences and both characterization baselines still hold — this round changed behaviour, not rendering contracts"
    - "The hot-file ledger and the ROADMAP record what this round did, with re-derivable figures"
    - "The row that found the blocker is re-driven by a human, or its owing is recorded as a dated DECISION"
  artifacts:
    - path: "scripts/vitest-count-gate.cjs"
      provides: "raised pins for WorkflowsPage.test.tsx and WorkflowCard.test.tsx"
    - path: ".planning/ROADMAP.md"
      provides: "the Phase 192 entry carries the gap-closure round and its four plans"
    - path: "CLAUDE.md"
      provides: "the WorkflowsPage.tsx ledger row updated on measurement"
  key_links:
    - from: "scripts/vitest-count-gate.cjs BASELINE"
      to: "the four new regression tests"
      via: "raised pins in the same commit as the measurement"
      pattern: "WorkflowsPage.test.tsx"
---

<objective>
Make this round's regression tests un-deletable, prove the round changed behaviour and not rendering
contracts, and record what happened where the next reader will look.

Purpose: the whole reason U5 escaped is that **a passing suite is not the same as a guarded one**.
`192-VALIDATION.md` recorded the eighth occurrence of the two-knob trap in this repo; an unpinned
suite is an unguarded one. The four tests this round adds are the only mechanical memory of a live
blocker, and until they are pinned, deleting them is free.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/192-workflow-library-ia/192-UAT.md
@.planning/phases/192-workflow-library-ia/192-VALIDATION.md
@.planning/ROADMAP.md

@scripts/vitest-count-gate.cjs
</context>

<constraints_inherited>
- **Read the pin, never compute it.** `192-VALIDATION.md` § S-8 and the gate's own adoption comment:
  a pin is transcribed from the gate's `actual` column across runs that AGREE, never derived by adding
  the number of `it()`s you think you wrote.
- **A count change rides in the same commit as its cause** — here the cause landed in waves 1–3, so
  this commit carries the RAISE and says so. (Only DECREASES red the gate, which is why the raise was
  deferrable to one plan instead of forcing three serial gate edits.)
- **`GSD_VITEST_MAX_WORKERS=4`** is calibrated for TWO concurrent runs; at three the gate goes
  non-deterministic. This plan runs alone.
- **The known non-192 flake:** `WorkflowBuilderPage.canvas.test.tsx` fails at roughly 2 of 14 gate runs
  (the Phase-184 D-14-11 positive control). If the gate reds on ONLY that file, re-run it in isolation
  and record both results — do not attribute it to this round, and do not "fix" it here.
- **G-7 — gap-closure round 1.** No new capability. This plan writes no product code at all.
</constraints_inherited>

<tasks>

<task type="auto">
  <name>Task 1: Pin every suite this round grew, at numbers read from the gate</name>
  <files>scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `scripts/vitest-count-gate.cjs` — the `BASELINE` map (note `"WorkflowsPage.test.tsx": 40` and
      `"WorkflowCard.test.tsx": 35`), the adoption-comment convention above it, the failure codes
      (`[failing-tests]`, `[total-below-baseline]`, `[missing-file]`, `[count-decrease]`), and the note
      that `BASELINE_TOTAL` is a `reduce` over `Object.values` and is therefore NOT hand-edited.
    - `.planning/phases/192-workflow-library-ia/192-VALIDATION.md` § "The count gate does NOT protect
      this phase's own file" and § S-8.
  </read_first>
  <action>
    Run `node scripts/vitest-count-gate.cjs` TWICE and read the `actual` column for
    `WorkflowsPage.test.tsx` and `WorkflowCard.test.tsx`. The two runs must AGREE; if they do not,
    re-run and record every value observed rather than picking one.

    Raise both pins to the READ numbers. Expected from planning arithmetic — `WorkflowsPage.test.tsx`
    40 → 48 and `WorkflowCard.test.tsx` 35 → 39 — but **the expectation is not the source**: write what
    the gate printed, and if it differs from the expectation, say so in the SUMMARY and explain the
    difference rather than reconciling it silently.

    Add one adoption comment in the file's existing register, naming the cause: the four
    `192-14` cases (the fork-collision branch 3176 passing tests never entered) and the four `192-15`
    cases (WR-03's failure visibility) plus `192-13`'s four card-sentence cases. State plainly that
    these pins exist so that deleting a regression test for a LIVE BLOCKER reds the gate with
    `[count-decrease]` naming the file.

    Do not hand-edit `BASELINE_TOTAL`. Do not adopt or decline any other file.
  </action>
  <verify>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - The gate exits **0**, with `failed 0`.
    - `grep -n '"WorkflowsPage.test.tsx"' scripts/vitest-count-gate.cjs` shows the READ number, and the SUMMARY quotes the gate output line it came from.
    - Same for `"WorkflowCard.test.tsx"`.
    - Two agreeing gate runs are recorded in the SUMMARY (both totals quoted).
    - A deletion of any one new `it()` would now red the gate — demonstrate it: delete one new case, run the gate, capture `[count-decrease]` naming the file, restore, and record `md5sum` before/after as identical. This is a positive control, and without it the pin is a claim rather than a guard.
  </acceptance_criteria>
  <done>Both pins raised from measurement, gate exit 0, and the guard demonstrated RED against a real deletion.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the round changed behaviour, not contracts — then record it</name>
  <files>.planning/ROADMAP.md, CLAUDE.md</files>
  <read_first>
    - `.planning/ROADMAP.md` § `#### Phase 192: Workflow Library IA` — the Plans list and the ⛔ OWED
      note about the eleven G-4 rows.
    - `CLAUDE.md` § "Hot-file ledger" — the `frontend/src/pages/WorkflowsPage.tsx` row, and the
      neighbouring rows' habit of CORRECTING their own figures on measurement rather than inheriting
      them.
    - `.planning/phases/192-workflow-library-ia/192-VALIDATION.md` § "Negative fences".
  </read_first>
  <action>
    (a) Re-run the contract evidence and record the measured numbers, not the expected ones:
    the five negative fences (`librarySubtree.fences.test.ts`), both characterization suites
    (`src/pages/__tests__/RunModal.test.tsx`, `RunModal.a11y.test.tsx`, `PublishedCardDelete.test.tsx`),
    `npx tsc -p tsconfig.app.json --noEmit`, and `npx eslint` on every file this round touched plus the
    a11y config. Every one is expected UNMOVED — this round changed what a click does and what the page
    says, not how anything renders. **If a fence or baseline DID move, stop and report it; do not
    re-capture a baseline to make it agree.**

    (b) Add four checked plan lines to the ROADMAP's Phase 192 entry, marked as gap-closure round 1,
    with a one-line statement of what the round closed: the U5 blocker — a dead fork verb that 409'd
    twice in silence — and what it did NOT close (U5-b card density, routed to a sketch under G-2; and
    the 2-of-18 `published + published` slugs whose fork still refuses, now visibly). Update the
    phase's status line to say the phase re-opened for one gap-closure round and why: SC#3 was found
    UNMET in lived experience by a human on the first real click.

    (c) Update the `WorkflowsPage.tsx` hot-file ledger cell in `CLAUDE.md` on MEASUREMENT. Re-derive,
    do not inherit: `git log --oneline -- frontend/src/pages/WorkflowsPage.tsx | wc -l` and
    `wc -l frontend/src/pages/WorkflowsPage.tsx`. Add the gap round to the touch list and state the
    G-5 position plainly — this round added no second concern (the two fork handlers and the failure
    banners were already there and are named in the cell as remaining), so the row stays *satisfied*,
    and the next phase that adds a concern here still owes a refactor recommendation first.

    Write the numbers you measured. If any figure in the existing cell is wrong, correct it and say
    that you corrected it — the two rows above it in that table already do exactly this.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages src/components/workflows/library</automated>
  </verify>
  <acceptance_criteria>
    - `librarySubtree.fences.test.ts` → **64** passed, 0 failed (unmoved).
    - `RunModal.test.tsx` **11**, `RunModal.a11y.test.tsx` **8**, `PublishedCardDelete.test.tsx` **7** — all passing with ZERO re-capture of any baseline.
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` → **33**.
    - `cd frontend && npx eslint src/pages/WorkflowsPage.tsx src/components/workflows/library` → no output; same with `-c eslint.a11y.config.js`.
    - The ROADMAP Phase 192 entry lists `192-13` … `192-16` as gap-closure round 1 and names both exclusions (U5-b; the 2 residual slugs).
    - The CLAUDE.md ledger cell quotes freshly-run `git log … | wc -l` and `wc -l` output, and the SUMMARY shows those two commands' actual output.
  </acceptance_criteria>
  <done>Contracts proved unmoved by measurement; ROADMAP and ledger record the round with re-derivable figures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Re-drive U5 — the row a human had to find</name>
  <files>.planning/phases/192-workflow-library-ia/192-UAT.md</files>
  <read_first>
    - `.planning/phases/192-workflow-library-ia/192-UAT.md` test 11 (U5) and its `root_cause` block.
    - `.planning/phases/192-workflow-library-ia/192-VALIDATION.md` § "Manual-Only Verifications" —
      the driving notes (`take_screenshot` times out; read DOM geometry via `evaluate_script`;
      `computer` clicks can deliver zero events; a row that cannot be driven is recorded ⛔ with its
      reason, never omitted).
  </read_first>
  <action>
    Start the app locally and drive the U5 row below against the live surface — the row a human found
    the blocker on, re-driven against the repair. Record the outcome (pass, fail, or ⛔ with a named
    reason) into `192-UAT.md` test 11 and into the SUMMARY. **A UAT fix needs its own UAT** — this
    project has already shipped a fix that introduced the next defect, so do not mark this row from the
    unit suite's evidence: 3176 passing tests are exactly what missed it the first time.
  </action>
  <what-built>
    Forking a published workflow you have ALREADY forked now opens the copy you started, instead of
    trying to create a colliding version and failing with HTTP 409. The card says so before you click:
    on such a row the consequence sentence reads *"You already have your own copy of this. Opens the
    copy you started. The published version stays live and unchanged."* — and on every other row it
    reads exactly what it read before.

    Any fork click that still fails — including the two measured slugs that carry two PUBLISHED
    versions and therefore have no draft to open — now renders a visible message naming the workflow
    and stating that nothing was created and nothing was changed. Both fork paths do this; the
    starter fork keeps its single retry.

    ⚠ Stated rather than smoothed: **this does not make every fork succeed.** Two of the eighteen
    multi-version slugs measured in the live database (`meridian-risk-summary-good-07aedc33`,
    `readonly_refusal_098uat`) still refuse — the change is that they refuse OUT LOUD.
  </what-built>
  <how-to-verify>
    1. Start the app (frontend `http://localhost:5173`, backend `http://localhost:8000/health`).
    2. Open the Workflows page and find **Compliance Gap Report** — the exact row from the original
       report. The DB shows it as `v1 published + v2 draft`, so it is the repaired case.
    3. **Before clicking anything**, open that row's `⋯` and READ the sentence under the menu. State
       out loud what you expect the fork verb to do.
    4. Click `Make my own copy`. Expected: you land in the Builder on the copy you already started
       (the breadcrumb names it), and NOTHING new is created. Confirm in the DB if you want the hard
       proof: `select count(*) from workflow_definitions` must be unchanged.
    5. Now find a row you have NEVER forked and click the same verb. Expected: a NEW copy is made and
       opens — the first fork must still work exactly as it did.
    6. Optional, for the residual case: fork `meridian-risk-summary-good-07aedc33` or
       `readonly_refusal_098uat`. Expected: a visible message on the page naming the workflow and
       saying nothing was changed. **Silence is a FAIL.**

    Quote the exact rendered sentence in your answer — the same standard U6 is held to.
  </how-to-verify>
  <acceptance_criteria>
    - Step 3's stated expectation MATCHES step 4's outcome — that is SC#3, verbatim.
    - Step 4 creates nothing (row count unchanged) and navigates somewhere.
    - Step 5 still creates a new copy.
    - Step 6, if driven, shows a visible message; if not driven, it is recorded ⛔ OWED with its reason.
    - A row that cannot be driven is recorded with its reason and blocking id — never silently omitted.
  </acceptance_criteria>
  <resume-signal>
    Type "approved" (optionally with the quoted sentence), or describe what actually happened.
    If you prefer to close this round with the row owed — legitimate under CLAUDE.md, and the same
    call made on 2026-08-11 for the other eleven — say so and it will be recorded as a dated DECISION
    in STATE.md and the ROADMAP, never as a claim that it ran.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → future edits | The only thing preventing a silent re-arming of this blocker is a pin the gate reads |
| documented figures → the next reader | An inherited number is a claim; a re-derived one is evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192-44 | Tampering | the four new regression tests | mitigate | Pinned at READ numbers, and the guard itself driven RED by deleting a case and observing `[count-decrease]` |
| T-192-45 | Repudiation | this round's own claims | mitigate | Every figure in the SUMMARY, ROADMAP and ledger is quoted from a command run in this plan; no number is inherited from another document |
| T-192-46 | Spoofing (of coverage) | fences + characterization baselines | mitigate | Re-run and required UNMOVED with zero re-capture; a moved baseline is a stop condition, not something to re-capture into agreement |
| T-192-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this round |
</threat_model>

<verification>
- `node scripts/vitest-count-gate.cjs` → exit 0, failed 0, both raised pins present.
- The `[count-decrease]` positive control captured and the file restored md5-identical.
- Fences 64 · RunModal 11 + 8 · PublishedCardDelete 7 · tsc 33 · eslint 0 (+a11y 0) — all measured, all unmoved.
- U5 re-driven by the operator, or recorded ⛔ OWED as a dated decision.
</verification>

<success_criteria>
The regression tests for a live blocker cannot be deleted quietly. Nothing this round did moved a
rendering contract. The ROADMAP and the hot-file ledger say what happened, in numbers anyone can
re-derive. The human who found the defect has either confirmed the fix or explicitly deferred saying so.
</success_criteria>

<output>
Create `.planning/phases/192-workflow-library-ia/192-16-SUMMARY.md` when done.
</output>
