---
phase: 192-workflow-library-ia
plan: 05
type: execute
wave: 2
depends_on: ["192-02"]
files_modified:
  - frontend/src/components/workflows/library/libraryRow.ts
  - frontend/src/components/workflows/library/libraryVocabulary.ts
  - frontend/src/components/workflows/library/libraryFilter.ts
  - frontend/src/components/workflows/library/libraryFilter.test.ts
  - frontend/src/components/workflows/library/librarySubtree.fences.test.ts
autonomous: true
requirements: [LIB-01, LIB-02, LIB-03]

must_haves:
  truths:
    - "D-16: merging the three feeds produces one flat list with no duplicate `id`, and the dedupe key is `id` — never `slug`, because `onTweak` deliberately mints a second row with the same slug."
    - "D-03: every chip's count is computed over the SAME row set the list renders, so a chip can never promise results it cannot deliver."
    - "D-07 / D-08: search matches workflow name and the `business_requirement` purpose sentence by SUBSTRING; the paraphrase \"the thing that checks vendors\" returns zero rows, and no string anywhere under `library/` implies meaning-based search."
    - "D-12: each merged row carries an explicit `provenance` of `starter` | `published` | `draft`, assigned from feed origin, so the fork branch never has to guess which handler a row belongs to."
    - "D-13: the fork consequence sentence exists in exactly one place and names BOTH halves — what you get, and what stays true."
    - "The filter module is pure: it renders nothing, imports nothing from the API client at runtime, and is testable with zero rendering."
  artifacts:
    - path: "frontend/src/components/workflows/library/libraryRow.ts"
      provides: "the types-only leaf — LibraryRow, Provenance, ChipId"
      min_lines: 40
    - path: "frontend/src/components/workflows/library/libraryVocabulary.ts"
      provides: "every user-facing string on the library surface, in ONE home"
      contains: "FORK_CONSEQUENCE"
    - path: "frontend/src/components/workflows/library/libraryFilter.ts"
      provides: "normalize · merge+dedupe by id · six chip predicates · counts · substring search"
      exports: ["mergeLibrary", "filterLibrary", "chipCounts"]
    - path: "frontend/src/components/workflows/library/libraryFilter.test.ts"
      provides: "pure unit coverage + the ?raw purity fence"
    - path: "frontend/src/components/workflows/library/librarySubtree.fences.test.ts"
      provides: "source fences F1 (no title=), F4 (no ESM cycle), F5 (no meaning-search word), and the threadGroups byte-unchanged fence"
  key_links:
    - from: "library/libraryFilter.ts"
      to: "components/workflows/soulData.ts + deriveTier.ts"
      via: "tierForDefinition / soulDeliverable — the ONLY legal sources for the 🔒 Strict and Makes-a-file chips"
      pattern: "tierForDefinition|soulDeliverable"
    - from: "library/libraryVocabulary.ts"
      to: "deriveTier.ts TIERS.STRICT.glyph"
      via: "the 🔒 mark is READ from shipped vocabulary, never typed as a literal"
      pattern: "TIERS\\.STRICT\\.glyph"
---

<objective>
Create the four leaf modules the whole library surface composes over — the types leaf, the vocabulary
table, the pure filter/merge module — plus the source-fence suite that guards the subtree.

Purpose: every "derive a chip" problem in this phase already has a shipped, tested, single-source
helper. The chips are *composition over `soulData` + `deriveTier`*, not new logic — RESEARCH is explicit
that any task writing a new predicate over `definition` should be challenged. And PATTERNS found the
*mechanical* reason these must be separate files rather than exports of the components:
`react-refresh/only-export-components` errors when a component module exports a shared runtime value
(`PlaneEditingLayer.tsx:23-31`). That is not a taste call.

Output: `libraryRow.ts` · `libraryVocabulary.ts` · `libraryFilter.ts` · `libraryFilter.test.ts` ·
`librarySubtree.fences.test.ts`. No component, no page change, no rendering.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/192-workflow-library-ia/192-RESEARCH.md
@.planning/phases/192-workflow-library-ia/192-PATTERNS.md
@.planning/phases/192-workflow-library-ia/192-CONTEXT.md

<interfaces>
The two row types that must be normalized into one, read at HEAD:

```ts
// frontend/src/lib/api.ts:1349 (+ 192-02's two additive optional fields)
export interface PublishedWorkflow {
  id: string; slug: string; name: string
  definition?: WorkflowDefinitionJSON | null
  is_mine?: boolean; is_system_global?: boolean
}
// frontend/src/lib/api.ts:3311
export interface WorkflowDraftRow {
  id: string; slug: string; version: number; name: string | null
  definition?: WorkflowDefinitionJSON | null; token: string
}
```

The derivation helpers — the ONLY legal sources for two of the six chips:
```ts
// frontend/src/components/workflows/soulData.ts
export function tierForDefinition(def: DefShape | undefined): Tier      // :111
export function soulDeliverable(def: DefShape | undefined): { kind: "file" | "chat"; label: string }  // :161
export interface DefShape { /* … */ [k: string]: unknown }              // :68 — index signature
// frontend/src/components/workflows/deriveTier.ts
export const TIERS = { STRICT: { id: "STRICT", glyph: "🔒", label: "Strict", … }, … } as const  // :57-79
```

The highlight component D-07 reuses (imported by the toolbar/card in later waves, named here so the
vocabulary module does not duplicate its job):
```tsx
// frontend/src/lib/threadGroups.tsx:137 — thread-agnostic, importable with zero edits
export function HighlightTitle({ title, query }: { title: string; query: string })
```
</interfaces>

**Wave note:** runs in parallel with `192-03` and `192-04`; disjoint `files_modified`. It depends on
`192-02` for one narrow reason worth stating rather than leaving as a surprise typecheck error: the
*Yours* chip predicate reads `PublishedWorkflow.is_mine`, and that optional field does not exist on the
TypeScript type until `192-02` Task 3 lands.

**Gate note:** this plan creates two new suites under `src/components/workflows/`, which the count
gate's `TARGETS` directory entry already **runs**. They are deliberately left **unpinned while their
counts are still growing** — the 188-12 precedent for `WorkflowBuilderPage.header.test.tsx`, quoted in
the script itself. `192-12` pins every new library suite once they stop growing. An unpinned suite is an
unguarded one, so **the pin is owed, not waived**, and `192-12` is where it comes due.
</context>

<tasks>

<task type="auto">
  <name>Task 1: The types leaf and the one vocabulary home</name>
  <files>frontend/src/components/workflows/library/libraryRow.ts, frontend/src/components/workflows/library/libraryVocabulary.ts</files>
  <read_first>
    - `frontend/src/components/workflows/phaseNodeCardContract.ts:18-30` — the repo's types-only-leaf
      precedent: it emits no runtime code, so it cannot participate in a value-level module cycle at all.
      Copy its docblock claim and `deriveTier.ts:24-50`'s member ordering (exported aliases first, then
      the exported interface, one docblock per member).
    - `frontend/src/components/workflows/runVocabulary.ts:1-60` — the locked-words module. Two habits to
      copy verbatim: the words are locked by CONTEXT.md and MIRRORED here, never authored here; and
      **exact-match assertions only** when testing the table, because overlapping stems make `toContain`
      a vacuous fence.
    - `frontend/src/components/workflows/deriveTier.ts:57-79` — the `as const satisfies Record<K,V>`
      table idiom, so a seventh chip becomes a typecheck error rather than a silently missing count.
    - `.planning/phases/192-workflow-library-ia/192-PATTERNS.md` § S-9 — the glyph ruling: **five of the
      six chips get NO glyph** (they are word-badges; the WORD carries the meaning), and 🔒 Strict reads
      its mark from `TIERS.STRICT.glyph`, never as a literal.
    - `192-CONTEXT.md` D-03, D-08, D-13, D-17 — the locked words.
  </read_first>
  <action>
    Create `libraryRow.ts` as a **types-only leaf**: `Provenance = "starter" | "published" | "draft"`,
    `ChipId` (the six ids), and `LibraryRow` carrying `id`, `slug`, `name` (draft `null` falls back to
    `slug`, mirroring `WorkflowsPage.tsx:706`), `version` (published reads `definition.version`; draft
    reads the row's own `version`), `def`, `provenance`, and `source` — the ORIGINAL row object, kept
    whole. State in the docblock why `source` exists: `onOpenDraft` needs `token`, `onLaunch` needs the
    real `PublishedWorkflow`, and a hand-rebuilt object that forgets a field is the D-186-07 silent
    clobber, which is a *behaviour* bug and not a type error. Emit **no runtime value** from this file.

    Create `libraryVocabulary.ts` holding every user-facing string on the library surface, as an
    `as const satisfies` table where a keyed table is appropriate:
    - the six D-03 chip labels, in the locked wording: *Ready to run · Yours · Still building ·
      Starters · Makes a file · Strict*. The shelf names are RE-HOMED, not dropped: "Published" →
      *Ready to run*, "Drafts & seeds" → *Still building*.
    - `FORK_VERB` — the single fork word D-12 puts on the card face.
    - `FORK_CONSEQUENCE` — D-13's one sentence. It MUST name **both** halves: what you get (a new
      private copy, open for editing) **and** what stays true (the published version stays live and
      unchanged). Naming only the first half reproduces the exact surprise LIB-03 exists to end.
    - the search field's label and placeholder (D-06 always-on), the D-17 project note
      (*"Starters aren't tied to a project."*), the four honest empty/loading states, and the
      *Clear search & filters* control's label.

    ⚠ **D-08 is a word-class rule and it binds this file**: no string here may imply meaning-based
    search. The placeholder describes substring matching in plain words.

    Glyphs: five chips carry **no glyph**. The Strict chip's mark is READ from `TIERS.STRICT.glyph`, so
    the chip and the card's tier atom are provably the same mark. Do not invent a mark for any chip and
    do not repurpose a phase-type glyph as a category icon — the vocabulary is closed
    (`icon-convention.md` §4), and workflow identity is already carried by the glyph-dot phase spine.

    Write the module's docblock in `runVocabulary.ts`'s shape: the words are locked in CONTEXT.md and
    mirrored here; a change is a decision taken there and reflected here, never the other way round.
  </action>
  <acceptance_criteria>
    - `frontend/src/components/workflows/library/libraryRow.ts` exists and
      `grep -c "^export \(const\|function\|class\|let\|var\)" ` on it equals **0** (types only).
    - `grep -c "FORK_CONSEQUENCE" libraryVocabulary.ts` ≥ 1, and its value contains both a phrase about a
      new private copy and a phrase about the published version staying live/unchanged.
    - `grep -c "TIERS" libraryVocabulary.ts` ≥ 1 and `grep -c "🔒" libraryVocabulary.ts` equals **0**
      (the mark is read, not typed).
    - `grep -Eic "semantic|meaning|similar|AI-powered|understands|natural language" libraryVocabulary.ts`
      equals **0**.
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` equals **33**.
    - `cd frontend && npx eslint src/components/workflows/library` reports 0 errors, and
      `npx eslint src/components/workflows/library -c eslint.a11y.config.js` reports 0 errors.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"</automated>
  </verify>
  <done>A types-only leaf and a single vocabulary home exist; the glyph ruling and D-08's word class are honored mechanically.</done>
</task>

<task type="auto">
  <name>Task 2: The pure merge + filter module and its unit suite</name>
  <files>frontend/src/components/workflows/library/libraryFilter.ts, frontend/src/components/workflows/library/libraryFilter.test.ts</files>
  <read_first>
    - `frontend/src/components/workflows/soulData.ts` in full (172 L) and `deriveTier.ts` in full (129 L)
      — the repo's two named pure-derivation modules and the direct dependencies of two chips. Note
      `soulData.ts:13-15` and `deriveTier.ts:12-13`: *"This module imports NOTHING from the API client."*
      Copy that claim, and note PATTERNS' nuance — a **type-only** import of `PublishedWorkflow` /
      `WorkflowDraftRow` keeps it true at runtime; a value import breaks it. `runVocabulary.ts:34-36` is
      the shipped wording for that distinction.
    - `frontend/src/components/workflows/deriveTier.ts:119-127` — the exhaustiveness guard with a runtime
      fallback (`const _never: never = x`), the shape to copy for any `provenance` switch.
    - `frontend/src/components/workflows/soulData.test.ts:1-30` — it imports its own `?raw` source to
      prove purity. That is the mechanical form of the claim.
    - `frontend/src/pages/WorkflowsPage.tsx:220-291` — `onTweak` (same slug, version N+1) and
      `onUseStarter` (new suffixed slug, version 1, 409 retry once), and the shipped comment at `:250-257`
      explaining that `UNIQUE(slug, version)` is GLOBAL across all users.
    - `frontend/src/pages/WorkflowsPage.tsx:706` (draft name→slug fallback), `:762` (published version
      read from `definition.version`), `:67` + `:172-173` (the `UNBOUND` sentinel and its client narrow).
    - `.planning/phases/192-workflow-library-ia/192-RESEARCH.md` § "The D-16 merge" — the proof that the
      feeds cannot collide today, and why the `Map` guard must exist anyway.
  </read_first>
  <action>
    Create `libraryFilter.ts` exporting:

    - `mergeLibrary(published, starters, drafts)` → `LibraryRow[]`. Dedupe with a single `Map` keyed on
      **`id`**. Do not key on `slug` or `slug+version`: `onTweak` deliberately creates a second row with
      the same slug, so a slug-keyed dedupe swallows a user's own fork the moment it appears in the
      drafts feed alongside the published original. Assign `provenance` from **feed origin** — that is
      exact today given `?scope=mine`, and it is the signal the D-12 fork branch reads. Record in a
      comment that the no-collision property holds *because* the page passes `?scope=mine`, and that the
      `Map` is the guard that survives a future change to that parameter.
    - Six chip predicates in one `as const satisfies Record<ChipId, …>` table:
      *Ready to run* = provenance is `published` or `starter`; *Yours* = `is_mine` when the field is
      present, falling back to `provenance !== "starter"` when it is `undefined` (the D-04 degraded-state
      rule, so a frontend ahead of the backend is *correct*, not merely non-fatal); *Still building* =
      provenance `draft`; *Starters* = provenance `starter`; *Makes a file* = `soulDeliverable(def)`
      reports a file; *Strict* = `tierForDefinition(def)` reports the STRICT tier.
      Do **not** write a new predicate over `definition` for either of the last two —
      `tierForDefinition` takes the STRICTEST policy across multiple emit phases deterministically and a
      naive `citation_policy === "strict"` check is order-dependent, with an existing regression test.
    - `matchesQuery(row, query)` — SUBSTRING, case-insensitive, over the workflow **name** and the
      `business_requirement` purpose sentence (D-07). Nothing else. No stemming, no token expansion, no
      fuzzy distance: D-08 says it is substring matching and the product must say so.
    - `filterLibrary(rows, { query, chips, projectId })` → the rendered row set, applying D-17's project
      semantics **client-side for drafts only**: published rows are already narrowed server-side, drafts
      narrow on `definition.project_folder_id`, and **starters are held OUT of the project filter and
      stay visible**. Reuse the shipped `UNBOUND` sentinel rather than inventing one.
    - `chipCounts(rows, query, projectId)` → counts computed over **the same row set the list renders**,
      so `chipCount(c) === renderedRows.filter(pred(c)).length` holds at every render. Never zero a count
      during a load and never pre-compute for rows that have not arrived.

    Import `PublishedWorkflow` and `WorkflowDraftRow` **type-only**. No React, no JSX, no DOM.

    Create `libraryFilter.test.ts` covering, at minimum: dedupe by `id` when both feeds are stubbed to
    return the same row; a same-slug v(N+1) draft **surviving** the merge alongside its published
    original (the slug-dedupe trap, as a positive case); each chip's count equalling the rows its
    predicate produces under a live query; name-only and purpose-only search hits; the paraphrase
    *"the thing that checks vendors"* returning **zero** rows; starters surviving a project selection
    while drafts and published narrow; and the `is_mine`-absent fallback agreeing with feed-derived
    provenance. Add the `?raw` purity fence in `soulData.test.ts`'s shape: the module's own source
    contains no runtime import from `@/lib/api`.
  </action>
  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/components/workflows/library/libraryFilter.test.ts`
      passes with 0 failures and reports ≥ 10 tests.
    - `grep -c "from \"@/lib/api\"" src/components/workflows/library/libraryFilter.ts` — every occurrence
      is on an `import type` line; `grep -c "^import type .*@/lib/api" ` ≥ 1 and
      `grep -Ec "^import \{[^}]*\} from \"@/lib/api\"" ` equals **0**.
    - `grep -Ec "citation_policy\s*===" src/components/workflows/library/libraryFilter.ts` equals **0**
      (the tier is derived through `tierForDefinition`, never re-implemented).
    - `grep -c "byId" src/components/workflows/library/libraryFilter.ts` ≥ 1 and the dedupe key is `id`:
      `grep -Ec "\.set\((r|row)\.slug" ` equals **0**.
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` equals **33**.
    - `cd frontend && npx eslint src/components/workflows/library` reports 0 errors.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/components/workflows/library/libraryFilter.test.ts</automated>
  </verify>
  <done>Merge, chips, search and project semantics are one pure, unit-tested function set with no runtime API-client import.</done>
</task>

<task type="auto">
  <name>Task 3: The subtree source fences (F1, F4, F5) and the threadGroups byte-unchanged fence</name>
  <files>frontend/src/components/workflows/library/librarySubtree.fences.test.ts</files>
  <read_first>
    - `frontend/src/components/workflows/WorkflowCanvas.test.tsx:55-83` — the `?raw` subtree fence with an
      **explicit path list**, and its comment explaining why: `import.meta.glob` expands at build time
      over files that EXIST, so a listed path with no file contributes the empty string and never throws;
      a bare directory glob silently covers whatever happens to be there, and an invariance fence says
      nothing about what it no longer covers.
    - `frontend/src/components/workflows/WorkflowCanvas.test.tsx:669-735` — the ESM-cycle fence. The
      `(\.[jt]sx?)?` group is **load-bearing**: `tsconfig.app.json:13-14` sets `moduleResolution:
      "bundler"` with `allowImportingTsExtensions: true`, so `from "./X.tsx"` compiles, resolves, and
      would build a real TDZ cycle under a fence anchored without it. Keep the five positive controls and
      the two `?raw` negative controls at `:698-724`.
    - `frontend/src/components/workflows/governanceVocabulary.test.ts:20-199` — the word-class fence.
      Three scoping decisions this task needs for the same reasons: test files are excluded from the
      sweep (or this file is its own first offender); the match is scoped to **string literals and JSX
      text** via the TypeScript parser, never to comments; and the searched tokens are **assembled from
      parts** (`tok("mean","ing")`) so a future whole-tree grep does not trip on the guard itself. Reuse
      its `userVisibleTextOf` extractor (`:95-118`) and its explicit-lookaround `wordRegExp` (`:166-167`)
      — `\b` is not sufficient.
    - `.planning/phases/192-workflow-library-ia/192-VALIDATION.md` § "Negative fences" — F1–F5 and their
      plants. Note correction C-3: **F5's scope is the whole `library/**` subtree**, not
      `libraryVocabulary.ts` alone, because its own plant (a search `placeholder`) is authored in the
      toolbar module.
    - `frontend/src/lib/threadGroups.tsx:126-151` — `HighlightTitle` and its T-156-01 XSS control.
  </read_first>
  <action>
    Create `librarySubtree.fences.test.ts` with an **explicit six-path list** naming every module of the
    subtree — `libraryRow.ts`, `libraryVocabulary.ts`, `libraryFilter.ts`, `RunModal.tsx`,
    `WorkflowDeleteSheet.tsx`, `WorkflowCard.tsx`, `LibraryToolbar.tsx` — written now, in this commit,
    **before four of them exist**. That is the point of the explicit list: a fence written after the
    files land will only ever cover what happened to be there.

    Implement:
    - **F1** — no `title=` attribute anywhere in the subtree source (D-14: touch has no hover).
    - **F4** — no module under `library/` names a `WorkflowsPage` specifier in ANY import form. Port the
      canvas fence's two regexes including the `(\.[jt]sx?)?` group and the dynamic-`import()` form. Keep
      the `?raw` negative controls, because this test file will itself reference the page's path.
    - **F5** — no user-visible string anywhere in the subtree contains a meaning-search word
      (`semantic`, `meaning`, `similar`, `AI-powered`, `smart`, `understands`, `natural language`),
      matched over string literals and JSX text only, with the tokens assembled from parts.
    - **The threadGroups byte-unchanged fence** — pin a content hash of `@/lib/threadGroups?raw` computed
      at this commit. This converts 158-B's re-open trigger #3 (*"any phase touches …
      `lib/threadGroups.ts` for another reason"*) from an ambiguity resolved in prose into a mechanical
      fact: 192 IMPORTS `HighlightTitle` and does not modify it. Record in the comment that the file is
      `.tsx`, not `.ts` as the trigger's wording says, and that `matchesTitle` (`:74`) is deliberately NOT
      reused — its signature is `(t: Thread, q: string)` and widening it *would* be touching the shared
      engine, which is exactly what B's deferral was costed against.

    Add **synthetic positive controls** for every fence now, in this commit: run the real detector over
    an in-test synthetic source string containing the violation and assert it is caught, plus a
    comment-exemption control and a non-vacuity guard on the number of swept files. That makes each fence
    non-vacuous the day it is written. The separate obligation to drive each fence RED against a **real
    plant in production source**, with md5 restore, belongs to `192-12` and must not be simulated here.
  </action>
  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/components/workflows/library/librarySubtree.fences.test.ts`
      passes with 0 failures.
    - The file lists all seven subtree paths explicitly: `grep -c "WorkflowDeleteSheet.tsx" ` ≥ 1,
      `grep -c "LibraryToolbar.tsx" ` ≥ 1, `grep -c "WorkflowCard.tsx" ` ≥ 1, `grep -c "RunModal.tsx" ` ≥ 1
      — while `ls src/components/workflows/library/` shows those four files do NOT exist yet.
    - `grep -c "\\[jt\\]sx" src/components/workflows/library/librarySubtree.fences.test.ts` ≥ 1 (the
      load-bearing extension group is present).
    - `grep -ci "positive control" src/components/workflows/library/librarySubtree.fences.test.ts` ≥ 3.
    - The word-class fence contains no banned word as a contiguous literal:
      `grep -Ec "\"(semantic|meaning|natural language)\"" ` equals **0**.
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` equals **33**.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/components/workflows/library/librarySubtree.fences.test.ts src/components/workflows/library/libraryFilter.test.ts</automated>
  </verify>
  <done>Four fences exist over an explicitly-named subtree, each with a synthetic positive control, written before four of the seven files exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-authored `business_requirement` → rendered library row | D-07 now highlights this text. It is user-controlled input on a rendering path. |
| module graph inside `components/workflows/library/` | An import back to `@/pages/WorkflowsPage` typechecks clean, lints clean, and fails only at runtime. |
| copy authored anywhere in the subtree → the user's expectation of what search does | D-08 is a promise about capability; a word that overstates it is a correctness defect, not a style one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192-04 | Tampering (XSS) | the search highlight over `business_requirement` | mitigate | Do not re-implement highlighting in this module or anywhere else. `HighlightTitle` (`threadGroups.tsx:137`) renders segments as JSX text nodes so React escapes them; its docblock records that the sketch's `innerHTML` highlight was deliberately not ported because it is an XSS vector on user-controlled text. The threat here is *re-implementation*, and the fence is the byte-unchanged hash plus the ban on `dangerouslySetInnerHTML` in the subtree. |
| T-192-13 | Tampering | ESM cycle under `library/` | mitigate | F4, ported from `WorkflowCanvas.test.tsx:669-735` with the `(\.[jt]sx?)?` group and the dynamic-import form, plus `?raw` negative controls. |
| T-192-14 | Spoofing (of capability) | library copy | mitigate | F5 over the whole subtree, TypeScript-parser scoped to user-visible text, tokens assembled from parts. D-08: no copy, placeholder, tooltip, commit message or SUMMARY may imply semantic search. |
| T-192-15 | Information Disclosure | `is_mine` fallback | accept | When the field is absent the client falls back to `provenance !== "starter"`, which discloses nothing new — provenance is already derivable from feed origin and from `definition.category`. |
| T-192-SC | Tampering | package installs | accept | None. Zero new dependencies; `cmdk` is deliberately NOT added (158-B's deferral was costed against exactly that). |
</threat_model>

<verification>
- Both new suites green with `GSD_VITEST_MAX_WORKERS=4`.
- `npx tsc -p tsconfig.app.json --noEmit` → **33** errors.
- `npx eslint src/components/workflows/library` → 0 errors; `-c eslint.a11y.config.js` → 0 errors.
- `git diff --name-only` lists only files under `frontend/src/components/workflows/library/`.
- `node scripts/vitest-count-gate.cjs` exits 0 (the two new suites print as `new`, which is informational).
</verification>

<success_criteria>
- `mergeLibrary` dedupes by `id` and a same-slug fork survives the merge.
- Every chip count equals the rows its predicate produces over the rendered set.
- Search is substring over name + purpose; the paraphrase returns zero.
- Starters are held out of the project filter by construction, not by accident.
- Four fences exist over an explicitly-named subtree, each with a synthetic positive control.
</success_criteria>

<output>
Create `.planning/phases/192-workflow-library-ia/192-05-SUMMARY.md` when done. Record the measured line
count of each new module (these feed `192-12`'s subtree-delta measurement), the test count of both new
suites, and the fact that both are deliberately unpinned pending `192-12`.
</output>
