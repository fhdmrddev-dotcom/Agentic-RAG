---
phase: 192-workflow-library-ia
plan: 04
type: execute
wave: 2
depends_on: ["192-01"]
files_modified:
  - frontend/src/pages/__tests__/PublishedCardDelete.test.tsx
autonomous: true
requirements: [LIB-03]

must_haves:
  truths:
    - "D-01: the WFIN-03 delete Sheet's characterization baseline exists in a commit where `frontend/src/components/workflows/library/WorkflowDeleteSheet.tsx` provably does not exist."
    - "Seven render states of the Sheet are captured as whole-`innerHTML` literals, each observed twice on the unchanged tree with the two runs agreeing byte for byte."
    - "The two graded-guard invariants are pinned as behaviour, separately from the byte captures: the Sheet never dismisses mid-delete, and the card leaves the list only on server confirmation."
    - "The plan records that the Sheet is NOT a self-contained JSX block — its real extent is ≈169 lines including state that lives 100 lines above it inside a component D-01 deletes."
  artifacts:
    - path: "frontend/src/pages/__tests__/PublishedCardDelete.test.tsx"
      provides: "the seven-state whole-innerHTML capture + the two invariant assertions"
      contains: "DELETE_SHEET_HTML_BASELINE"
  key_links:
    - from: "the capture commit"
      to: "frontend/src/components/workflows/library/WorkflowDeleteSheet.tsx"
      via: "git show <capture-sha>:<path> exits non-zero"
      pattern: "DELETE_SHEET_HTML_BASELINE"
---

<objective>
Capture the WFIN-03 victim-naming delete Sheet's characterization baseline **before** D-01 moves it,
and record the measured extent of what actually has to move.

Purpose: this is D-01's second verbatim move, and it is the one most likely to be done wrong. CONTEXT.md
scopes it at `:872-1003` (132 lines of JSX) — RESEARCH measured that the block **closes over**
`type DeletePhase` at `:743`, five `useState` hooks plus `descId` at `:768-772`, `openDeleteSheet` at
`:774-786` and `handleDelete` at `:788-799`, all of them inside `PublishedCard`, which D-01 deletes.
Real extent ≈ **169 lines**. A cut that takes only the JSX reads self-contained and is not.

Output: seven captured render states plus the two graded-guard invariants, committed while
`library/WorkflowDeleteSheet.tsx` does not exist. **No source file is modified by this plan.**
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/192-workflow-library-ia/192-RESEARCH.md
@.planning/phases/192-workflow-library-ia/192-PATTERNS.md

**Wave note:** runs in parallel with `192-03` and `192-05`; disjoint `files_modified`; no local-Postgres
mutation.

**Gate note:** same as `192-03` — this plan adds tests to a suite `192-01` already pinned, does not touch
`scripts/vitest-count-gate.cjs` (growth is informational; only a decrease fails), and the pin RAISE is
owed to `192-12` under the 188-12 precedent.

Worktree rules: bootstrap FIRST; `GSD_VITEST_MAX_WORKERS=4` on every vitest run.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture seven whole-innerHTML render states of the delete Sheet</name>
  <files>frontend/src/pages/__tests__/PublishedCardDelete.test.tsx</files>
  <read_first>
    - `frontend/src/pages/__tests__/PublishedCardDelete.test.tsx` in full — its docblock (`:1-20`) already
      enumerates the five shipped behaviours, and its live-page render is at `:107`, opening the ⋯ menu
      off a real `PublishedCard`. Its `vi.mock("@/lib/api")` factory is exhaustive; keep it so.
    - `frontend/src/pages/WorkflowsPage.tsx:743` (`type DeletePhase`), `:768-772` (the five `useState`
      hooks + `descId`), `:774-786` (`openDeleteSheet`, which fetches `getWorkflowDeletePreview`),
      `:788-799` (`handleDelete`, which calls `deleteWorkflowCascade` then `onDeleted()`), and
      `:872-1003` (the `<Sheet>` JSX, comment from `:872`, element from `:877`).
    - `frontend/src/components/workflows/PhaseNodeCard.test.tsx:2555-2683` — the capture block to copy
      (rows declared once · one shared helper · literal labelled a CAPTURE and observed twice ·
      non-vacuity + marker rows).
    - `.planning/phases/192-workflow-library-ia/192-PATTERNS.md` § 2 and § S-3.
  </read_first>
  <action>
    Add one `describe` block with a **single** shared helper that renders the live page, opens the ⋯ menu,
    clicks `published-delete`, reads the `SheetContent`'s `innerHTML`, unmounts and returns the string.
    Capture and assertion must call the same helper.

    Declare the seven state rows once and capture: (a) loading, `preview === null`; (b) loaded with
    `in_flight = 0` — including the always-rendered zero-threads variant *"No chat threads to keep."*;
    (c) loaded with `in_flight > 0`, the amber cancel-first banner; (d) `deleting`; (e) `deleted`;
    (f) `error`; (g) `previewError`.

    Read each literal OUT of the rendered DOM, run the capture twice on the unchanged tree, and confirm
    byte agreement before committing. Label each with `⚠ THIS LITERAL IS A CAPTURE, NOT AN EXPECTATION`
    and the sentence that a diff against it is a behaviour change to explain, not a test to update.

    Add non-vacuity guards (`.length > 0`) plus separate `it`s asserting what each capture must contain —
    in particular that the loaded states render the **exact mocked counts**, since victim-naming with
    real counts is what makes this the heaviest of the three shipped guard grades and is the property
    D-15 and D-18 both lean on to mean anything.

    In the block's header comment, record the measured extent for the move that follows: the Sheet is
    **not** self-contained; `:743` + `:768-799` + `:877-1003` move as ONE unit, ≈169 lines, and the pieces
    at `:768-799` live *inside* the `PublishedCard` that D-01 deletes. Also record PATTERNS § 2's rule for
    this shape: the moved bodies are **JSX fragments**, so they must acquire a component wrapper, a props
    destructure and a props type — that wrapper is the ACCEPTED delta, and byte-identity is chased for
    the rendered DOM and the JSX element bodies, never for the wrapper.

    Modify no source file.
  </action>
  <acceptance_criteria>
    - `grep -c "DELETE_SHEET_HTML_BASELINE" frontend/src/pages/__tests__/PublishedCardDelete.test.tsx` ≥ 2.
    - The capture table has exactly **7** keys.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages/__tests__/PublishedCardDelete.test.tsx`
      passes with 0 failures on **two** runs whose reported counts agree.
    - `grep -c "toBeGreaterThan(0)"` ≥ 1, and the literal carries `CAPTURE, NOT AN EXPECTATION`.
    - The header comment contains the strings `:743`, `:768`, `:877` and `169` (the measured extent).
    - `git diff --name-only` lists exactly one file.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages/__tests__/PublishedCardDelete.test.tsx</automated>
  </verify>
  <done>Seven states captured from the live tree, observed twice, labelled, non-vacuous; the real move extent recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the two graded-guard invariants and prove the capture predates the move</name>
  <files>frontend/src/pages/__tests__/PublishedCardDelete.test.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowsPage.tsx:877-885` — `onOpenChange` refuses to close while
      `deletePhase === "deleting"`, with the shipped comment explaining why.
    - `frontend/src/pages/WorkflowsPage.tsx:788-799` — `handleDelete`: `onDeleted()` runs only after the
      server confirms, and `:593-596` shows `onDeleted` re-fetching rather than filtering the list
      locally (D-LOCK-04 — no optimistic vanish).
    - `.planning/phases/192-workflow-library-ia/192-PATTERNS.md` § S-5 — the three shipped guard grades.
      This Sheet is the heaviest; D-18's draft delete must be demonstrably lighter, and D-15's fork must
      be a direct flip. Both arguments depend on this one still meaning what it means.
  </read_first>
  <action>
    Add two behavioural assertions, marked in a comment as **invariants, not captures** — they answer a
    different question from the `innerHTML` literals and must not be re-derived from them:

    1. **The Sheet never dismisses mid-delete.** Drive it into `deleting`, then attempt to dismiss
       (Escape and/or an `onOpenChange(false)` path), and assert the Sheet is still in the document.
    2. **No optimistic vanish.** Assert the card leaves the list only after the server confirms — i.e.
       `deleteWorkflowCascade` resolves and `onDeleted` triggers a re-fetch — and that a *rejected*
       cascade leaves the card present with the error state rendered.

    Then record the predates-the-move proof: `git rev-parse HEAD` for the capture SHA, and
    `git show <capture-sha>:frontend/src/components/workflows/library/WorkflowDeleteSheet.tsx`, which
    must exit **non-zero**. Paste command and verbatim output into the SUMMARY.
  </action>
  <acceptance_criteria>
    - `grep -ci "mid-delete\|never dismiss" frontend/src/pages/__tests__/PublishedCardDelete.test.tsx` ≥ 1.
    - `grep -ci "optimistic" frontend/src/pages/__tests__/PublishedCardDelete.test.tsx` ≥ 1.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages/__tests__/PublishedCardDelete.test.tsx`
      passes with 0 failures.
    - `git show HEAD:frontend/src/components/workflows/library/WorkflowDeleteSheet.tsx` exits non-zero,
      and the command plus verbatim output appear in the SUMMARY.
    - `node scripts/vitest-count-gate.cjs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages/__tests__/PublishedCardDelete.test.tsx</automated>
  </verify>
  <done>Both invariants pinned as behaviour; the capture commit provably predates the destination module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-move tree → post-move tree | A 169-line move whose closure is invisible from the JSX range. The baseline is the only mechanism that notices a piece left behind. |
| user → destructive action | The victim-naming Sheet with exact server counts is the heaviest shipped guard grade; its behaviour is the reference point for D-15 (fork = direct flip) and D-18 (draft delete = lighter). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192-08 | Repudiation | the characterization baseline | mitigate | `git show <capture-sha>:.../library/WorkflowDeleteSheet.tsx` must exit non-zero, output pasted. |
| T-192-11 | Tampering | the graded-guard vocabulary | mitigate | Pin *never dismisses mid-delete* and *no optimistic vanish* as behaviour, separately from the byte captures — these are the properties D-15 and D-18 argue against, and a guard grade that quietly weakens makes both arguments false. |
| T-192-12 | Tampering | the "verbatim" move | mitigate | Record the measured extent (`:743` + `:768-799` + `:877-1003`, ≈169 L) inside the test file, so the executor of `192-07` inherits a measurement instead of CONTEXT.md's 132-line JSX range. |
| T-192-SC | Tampering | package installs | accept | None. Zero new dependencies in Phase 192. |
</threat_model>

<verification>
- `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages/__tests__/PublishedCardDelete.test.tsx` — 0 failures, twice.
- `node scripts/vitest-count-gate.cjs` exits 0.
- `git diff --name-only HEAD~2` lists only `frontend/src/pages/__tests__/PublishedCardDelete.test.tsx`.
- `git show HEAD:frontend/src/components/workflows/library/WorkflowDeleteSheet.tsx` exits non-zero.
</verification>

<success_criteria>
- Seven captures, observed twice in agreement, labelled as captures, non-vacuous, with exact counts asserted.
- Both graded-guard invariants pinned as behaviour.
- The real ≈169-line extent recorded in the file the next executor reads.
</success_criteria>

<output>
Create `.planning/phases/192-workflow-library-ia/192-04-SUMMARY.md` when done. Record the capture SHA,
the verbatim `git show` failure output, the two agreeing run reports, and the pin RAISE owed to `192-12`.
</output>
