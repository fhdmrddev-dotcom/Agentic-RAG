---
phase: 192-workflow-library-ia
plan: 11
type: execute
wave: 7
depends_on: ["192-10"]
files_modified:
  - frontend/src/pages/WorkflowsPage.test.tsx
autonomous: true
requirements: [LIB-01, LIB-02, LIB-03, LIB-04]

must_haves:
  truths:
    - "LIB-01 / SC#1: typing part of a workflow's name narrows the rendered list to it, and typing a word that appears only in the purpose sentence finds the row too (D-07)."
    - "D-08: the paraphrase \"the thing that checks vendors\" returns zero rows, and the page says so honestly with a one-click way out."
    - "LIB-02 / SC#2: every chip's number equals the number of rows its predicate produces over the rendered set, at every render, under the live search."
    - "The RUN CARVE-OUT survives the merge: a rejected `/workflows/drafts` fetch still renders published and starter rows."
    - "D-17: selecting a project keeps starters visible and states why, while published and draft rows narrow."
    - "LIB-04 / SC#4: the create affordance precedes every row in DOM order, proved by `compareDocumentPosition` at 200 workflows."
    - "D-10: no element with the text `Publish…` renders on any row, proved with a positive control that finds one when planted."
    - "D-11: no user-visible node on the library surface renders a `GET /workflows/...` string."
    - "D-18: deleting a draft calls `deleteWorkflowDraft` and never `deleteWorkflowCascade`."
    - "D-04: `is_mine` agrees with feed-derived provenance over the merged list, and the list is still correct when the field is absent."
  artifacts:
    - path: "frontend/src/pages/WorkflowsPage.test.tsx"
      provides: "the LIB-01…04 behaviour suite at 200 rows, plus the F2/F3 DOM fences and the D-04 cross-check"
  key_links:
    - from: "the rendered chip labels"
      to: "the rendered row count"
      via: "chipCount(c) === renderedRows.filter(pred(c)).length asserted at page level"
      pattern: "chip"
    - from: "the create affordance"
      to: "the first row"
      via: "compareDocumentPosition"
      pattern: "compareDocumentPosition"
---

<objective>
Prove the four requirements at the surface a user actually touches, at the scale the operator chose —
**200 workflows, never 12**.

Purpose: `192-10` kept the shipped contracts green through the restructure. It did not prove the new
ones. This plan is where SC#1–SC#4 acquire mechanical evidence, and where the two DOM-level negative
fences (F2, F3) are written.

Output: one enlarged page suite. No source change.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/192-workflow-library-ia/192-RESEARCH.md
@.planning/phases/192-workflow-library-ia/192-PATTERNS.md
@.planning/phases/192-workflow-library-ia/192-VALIDATION.md

**Judge every density and findability decision at 200 workflows.** That is the 045 real-scale lesson,
and it is why all three sketches ship a 12/54/200 scale selector. A suite that only ever renders four
fixtures proves the feature works on a demo.

**Gate note:** this plan only ADDS tests to a pinned file. It does **not** touch
`scripts/vitest-count-gate.cjs`; the RAISE is `192-12`'s, read from one gate run over the settled tree.
</context>

<tasks>

<task type="auto">
  <name>Task 1: A 200-row fixture generator, and the search + chip-count evidence (LIB-01, LIB-02)</name>
  <files>frontend/src/pages/WorkflowsPage.test.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowsPage.test.tsx:21-57` (the `vi.hoisted` + `vi.mock` seam) and `:75-152`
      (the four literal fixtures) — the house idiom. `SettingsPage.test.tsx:14` records that it *"mirrors
      the WorkflowsPage.test.tsx api-mock idiom"*, so this is the pattern, not an accident.
    - `frontend/src/components/panel/__tests__/PhaseReconcile.test.tsx:117` — the shipped bulk-generation
      precedent: an in-file typed generator via `Array.from({length: n}, (_, i) => …)`. There is **no
      factory module and no MSW** in this repo — re-verified — so a local generator is consistent with
      the house and there is no third pattern to avoid inventing.
    - `frontend/src/components/workflows/library/libraryFilter.ts` — `chipCounts` and `filterLibrary`;
      the page-level assertions here must agree with the pure unit tests in `192-05`, not restate them.
    - `frontend/src/lib/threadGroups.tsx:126-151` — `HighlightTitle` highlights the **first** match only.
      That is the shipped behaviour and matching it is correct; a test asserting "every occurrence is
      marked" would fail.
  </read_first>
  <action>
    Add `makePublishedRow(i, overrides)`, `makeStarterRow(i, overrides)` and `makeDraftRow(i, overrides)`
    **inside this test file** — not a shared fixtures module, because nothing else needs them and a shared
    one becomes this phase's rot to own. Generate 200 rows with `Array.from`, varying
    `business_requirement`, `citation_policy`, `phase_type` and `project_folder_id` so all six chips and
    the search have real spread rather than a uniform block that any predicate satisfies.

    Then assert, at 200 rows:
    - **Search by name (SC#1):** typing three characters of one workflow's name leaves that row rendered
      and the non-matching rows not rendered.
    - **Search by purpose (D-07):** a word that appears **only** in one row's `business_requirement` finds
      that row — the case that makes D-07 wider than SC#1's literal bar and measurably more useful.
    - **The hit is highlighted:** a `<mark>` wraps the matched slice. Assert the first match only.
    - **D-08, substring not meaning:** the paraphrase *"the thing that checks vendors"* renders **zero**
      rows, the honest empty state appears, and the *Clear search & filters* control is present and works.
      Name the test so the intent survives reading: it asserts a limitation, deliberately.
    - **Chip honesty (D-03) at page level:** for each of the six chips, under at least two different live
      queries, the number rendered on the chip **equals** the number of rows rendered when that chip is
      active. Compute the expected value from the DOM, not from the fixture array — the rule is
      `chipCount(c) === renderedRows.filter(pred(c)).length`, and a chip that promises results it cannot
      deliver is exactly what LIB-02 exists to prevent.
    - **A zero-count chip still renders** (the honest-empty-state rule: the atom is always rendered, never
      hidden, never fabricated).
  </action>
  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages/WorkflowsPage.test.tsx`
      passes with 0 failures.
    - `grep -c "length: 200" frontend/src/pages/WorkflowsPage.test.tsx` ≥ 1.
    - `grep -c "makePublishedRow" ` ≥ 2 and `grep -c "makeDraftRow" ` ≥ 2.
    - `grep -c "the thing that checks vendors" ` ≥ 1.
    - `grep -c "<mark>\|querySelector(\"mark\")\|getElementsByTagName(\"mark\")" ` ≥ 1.
    - At least six chip-count assertions exist — one per `ChipId`.
    - Runtime of this suite stays under 60 s; if it does not, reduce to the smallest N that still exposes
      density behaviour and **record the number and the reason**, rather than silently testing at 12.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages/WorkflowsPage.test.tsx</automated>
  </verify>
  <done>Search finds by name and by purpose at 200 rows, the paraphrase honestly returns zero, and every chip's number equals the rows it produces.</done>
</task>

<task type="auto">
  <name>Task 2: The carve-out, D-17's project semantics, and the D-04 provenance cross-check</name>
  <files>frontend/src/pages/WorkflowsPage.test.tsx</files>
  <read_first>
    - `.planning/phases/192-workflow-library-ia/192-RESEARCH.md` § "The RUN CARVE-OUT — what the merge must
      not do" (risk #1: a gated `/drafts` 403 emptying the library) and § "The unnamed IA defect".
    - `backend/app/api/workflows.py:171-174` — the carve-out comment, and `:1012` — the gate on `/drafts`.
    - `frontend/src/pages/WorkflowsPage.tsx` after `192-10` — the `allSettled` block and the D-17 narrow.
    - `.planning/phases/192-workflow-library-ia/192-RESEARCH.md` § "D-12 — the fork branch, and whether it
      blocks on D-04": provenance is available three ways, and `is_mine` acts as a **cross-check**, not as
      the source of truth.
  </read_first>
  <action>
    Add:
    - **The carve-out test.** Stub `listDraftWorkflows` to **reject**, leave the other two resolving, and
      assert that published and starter rows still render and that the failure is disclosed as a
      per-source failure rather than as an empty library. This is the single highest-risk defect the merge
      can ship, and it is invisible to any test that only stubs happy paths.
    - **D-17.** Select a specific project; assert published rows narrow (the re-query fired with that
      `project_folder_id`), draft rows narrow client-side on `definition.project_folder_id`, and **starter
      rows remain rendered**, with the toolbar's stated reason present as **text**. Add the negative case:
      with "All projects" selected, the note is absent. **Silence is a FAIL** — this is the structural half
      of UAT row U6.
    - **The in-flight count rule.** With a project re-query pending, assert that previously-committed rows
      are still rendered, that the chip counts still describe those rendered rows, that no count has been
      zeroed, and that the `data-state` updating marker is present. Then assert the marker is absent once
      the re-query settles — a marker that is always present proves nothing.
    - **The D-04 cross-check.** Over the merged list, assert `is_mine === (provenance !== "starter")` for
      every row when the backend supplies the field. Then run the same fixture with both fields **absent**
      (a frontend deployed ahead of the backend) and assert the *Yours* chip still produces the correct
      set from feed-derived provenance — the degraded state must be *correct*, not merely non-fatal.
  </action>
  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages/WorkflowsPage.test.tsx`
      passes with 0 failures.
    - `grep -c "mockRejectedValue\|mockRejectedValueOnce" frontend/src/pages/WorkflowsPage.test.tsx` ≥ 1.
    - `grep -ci "carve-out\|carve out" ` ≥ 1 (the test names its reason).
    - `grep -c "is_mine" ` ≥ 2 (present and absent cases).
    - `grep -c "data-state" ` ≥ 1.
    - The D-17 assertions check the note's **text**, not merely element presence — a blank node must not pass.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages/WorkflowsPage.test.tsx</automated>
  </verify>
  <done>A gated feed cannot empty the library, starters survive the project filter with a stated reason, counts stay honest mid-re-query, and is_mine agrees with provenance in both the present and absent cases.</done>
</task>

<task type="auto">
  <name>Task 3: The DOM fences (F2, F3) with a positive control, create-first order, and the draft endpoint</name>
  <files>frontend/src/pages/WorkflowsPage.test.tsx</files>
  <read_first>
    - `.planning/phases/192-workflow-library-ia/192-VALIDATION.md` § "Negative fences" — F2 and F3 and their
      plants. F1, F4 and F5 are **source** fences and live in `library/librarySubtree.fences.test.ts`;
      F2 and F3 are **DOM** fences and belong here.
    - `frontend/src/components/workflows/GovernanceSection.test.tsx:104-142` — the positive-control shape.
      The Phase 187 lesson is that an absence assertion with no positive control proves nothing.
    - `frontend/src/pages/WorkflowsPage.tsx:722-735` at the pre-192 tree (now deleted) — the `Publish…`
      button F3 forbids, and the fact that it shared `onClick={onOpen}` with `✎ Open`.
    - `frontend/src/lib/api.ts:3525` — `deleteWorkflowDraft`; and the cascade pair, which resolves a slug.
  </read_first>
  <action>
    Add:
    - **F2 (D-11).** Assert the rendered library view contains **no** node whose text matches
      `/GET \/workflows\//`. Scope the query to the library surface so the assertion says what it means.
    - **F3 (D-10) with its positive control.** Assert no element with text matching `/^Publish…?$/`
      renders on any row state — draft, published and starter, including inside each opened `⋯` menu. Then
      the control: render a locally-planted `Publish…` button in the same test and show the SAME query
      finds it. Without that, F3 also passes on a query that matches nothing anywhere.
    - **LIB-04 / SC#4.** At 200 rows, assert the create affordance precedes every rendered row in DOM
      order using `compareDocumentPosition`, and that it is the first interactive element on the library
      surface. Assert order, not coordinates — geometry belongs to UAT row U3, and asserting both in the
      same place is how a structural test starts failing for layout reasons.
    - **D-18 endpoint.** Drive a draft row's `⋯` → `Delete` through its arm step and assert
      `deleteWorkflowDraft` was called with that draft's id **and** that `deleteWorkflowCascade` and
      `getWorkflowDeletePreview` were **not** called. Add the counterpart on a published row: the
      victim-naming Sheet appears and the cascade path is used — so the two grades are proved distinct at
      the page level, not just asserted to be.
  </action>
  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages/WorkflowsPage.test.tsx`
      passes with 0 failures.
    - `grep -c "compareDocumentPosition" frontend/src/pages/WorkflowsPage.test.tsx` ≥ 1.
    - `grep -ci "positive control" ` ≥ 1.
    - `grep -c "GET \\\\/workflows\\\\/\|GET /workflows/" ` ≥ 1 (F2's pattern is present in the test).
    - `grep -c "deleteWorkflowCascade" ` ≥ 1, appearing in a `not.toHaveBeenCalled` assertion for the draft path.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages src/components/workflows/library`
      passes with 0 failures.
    - `node scripts/vitest-count-gate.cjs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages src/components/workflows/library</automated>
  </verify>
  <done>F2 and F3 exist at DOM level with a working positive control, create-first is proved by document order at 200 rows, and the two delete grades are proved distinct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a gated feed → an ungated surface | The merge's composition decides whether a 403 on `/drafts` degrades one source or the whole library. Only a rejecting stub can tell. |
| an absence assertion → the thing it claims to prove | A selector that matches nothing anywhere passes every absence test ever written against it. |
| a demo-scale suite → an org-scale product | Findability and density defects appear at 200 rows and are invisible at 12. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192-03 | Denial of Service (availability) | the merged feed under a gated `/drafts` | mitigate | A test that stubs `listDraftWorkflows` to reject and requires published + starter rows to still render. |
| T-192-29 | Repudiation | the F3 absence assertion | mitigate | A positive control in the same test proving the selector finds a planted `Publish…`. The Phase 187 lesson, applied where it binds. |
| T-192-23 | Elevation of Privilege / destructive scope | the draft delete path | mitigate | Assert `deleteWorkflowDraft` called with the draft id AND the cascade pair not called; assert the published path still uses the cascade behind its Sheet, so the grades are proved distinct rather than described. |
| T-192-27 | Spoofing (of filter behaviour) | project filter vs starters | mitigate | Assert the starters note's **text**, both present and absent cases. |
| T-192-19 | Spoofing (of results) | chip counts mid-re-query | mitigate | Assert counts describe the rendered rows while a re-query is pending, that none is zeroed, and that the `data-state` marker appears and then disappears. |
| T-192-SC | Tampering | package installs | accept | None. Zero new dependencies. |
</threat_model>

<verification>
- `GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages src/components/workflows/library` — 0 failures.
- `node scripts/vitest-count-gate.cjs` exits 0.
- `git diff --name-only` lists exactly `frontend/src/pages/WorkflowsPage.test.tsx` — this plan changes no source.
- Suite runtime recorded; if it exceeded 60 s and N was reduced below 200, the number and reason are stated.
</verification>

<success_criteria>
- SC#1, SC#2 and SC#4 each have mechanical evidence at 200 rows.
- D-08's limitation is asserted deliberately, by name.
- The carve-out, D-17 and the D-04 cross-check are covered, including their negative cases.
- F2 and F3 exist at DOM level, F3 with a positive control.
</success_criteria>

<output>
Create `.planning/phases/192-workflow-library-ia/192-11-SUMMARY.md` when done. Record the suite's new test
count and runtime, the N actually used for the scale tests (and the reason if it is not 200), and the pin
RAISE owed to `192-12`.
</output>
