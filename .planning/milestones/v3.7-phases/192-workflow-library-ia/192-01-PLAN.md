---
phase: 192-workflow-library-ia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [LIB-01, LIB-02, LIB-03, LIB-04]
gate_owner: true

must_haves:
  truths:
    - "Deleting an `it()` from any suite that renders the Workflows library fails `node scripts/vitest-count-gate.cjs` with `[count-decrease]`, instead of leaving it green."
    - "The count-gate adoption is the FIRST commit of Phase 192 — every later commit of this phase is measured against a stick that already existed (D-01's restructure has a safety net before it starts)."
    - "The adopt/decline decision for `WorkflowBuilderPage.session.test.tsx` and `ChatLayoutLaunch.test.tsx` is recorded with its reason, not left implicit."
  artifacts:
    - path: "scripts/vitest-count-gate.cjs"
      provides: "TARGETS entries + BASELINE pins for the five previously-unguarded suites covering WorkflowsPage"
      contains: "src/pages/WorkflowsPage.test.tsx"
  key_links:
    - from: "scripts/vitest-count-gate.cjs TARGETS"
      to: "src/pages/WorkflowsPage.test.tsx"
      via: "file-level TARGETS entry (the directory entries cover neither src/pages nor src/pages/__tests__)"
      pattern: "src/pages/WorkflowsPage\\.test\\.tsx"
    - from: "scripts/vitest-count-gate.cjs BASELINE"
      to: "the four adopted suites"
      via: "pinned count read from the gate's own `actual` column across two agreeing runs"
      pattern: "\"WorkflowsPage\\.test\\.tsx\": [0-9]+"
---

<objective>
Adopt the five suites that render `WorkflowsPage` into the vitest count gate — TARGETS (what RUNS)
and BASELINE (what is PINNED) — before any source change in this phase.

Purpose: RESEARCH measured that **74 of the 123 tests covering the surface this phase rewrites are
invisible to the count gate**. `src/pages/WorkflowsPage.test.tsx` is in neither knob, and neither are
the three `src/pages/__tests__/` suites. A plan that drops an `it()` during D-01's restructure would
leave the gate green. This is the eighth recorded occurrence of the two-knob trap and it is on this
phase's own file.

Output: one commit — the first of the phase — that changes no source, only the gate's coverage.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/192-workflow-library-ia/192-RESEARCH.md
@.planning/phases/192-workflow-library-ia/192-VALIDATION.md
@.planning/phases/192-workflow-library-ia/192-PATTERNS.md

Worktree rules (CLAUDE.md § "Parallel execution"): bootstrap FIRST with
`bash scripts/bootstrap-worktree.sh "$(pwd)"`; set `GSD_VITEST_MAX_WORKERS=4` on every vitest run;
never `rm -rf` a worktree.

**This plan runs in parallel with `192-02` (backend D-04).** Their `files_modified` are disjoint and
neither mutates the local Postgres.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adopt the four library-covering suites into TARGETS + BASELINE</name>
  <files>scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `scripts/vitest-count-gate.cjs` — the header (`:1-90`, the "READ FROM THIS SCRIPT'S OWN `actual`
      COLUMN" rule), the `BASELINE` map, `BASELINE_TOTAL` (`:989`), the `TARGETS` array (`:992+`), and
      the comparison block (`:1300-1380`) which shows that a **decrease** fails and growth is only
      informational.
    - `scripts/vitest-count-gate.cjs:1005-1011` and `:968-974` — the two prior adoptions that state the
      two-knob rule and the "pinned in the commit that creates it" habit. `:968-974`
      (`ConnectionsTab.test.tsx`) is the ONLY prior entry that needed BOTH knobs; copy its comment shape.
    - `.planning/phases/192-workflow-library-ia/192-PATTERNS.md` § S-8 — the adoption comment convention.
    - `.planning/phases/192-workflow-library-ia/192-RESEARCH.md` § "THE eighth two-knob trap".
  </read_first>
  <action>
    Add **file-level** `TARGETS` entries for `src/pages/WorkflowsPage.test.tsx`,
    `src/pages/__tests__/RunModal.test.tsx`, `src/pages/__tests__/RunModal.a11y.test.tsx` and
    `src/pages/__tests__/PublishedCardDelete.test.tsx`. File-level, not a bare `src/pages/__tests__`
    directory entry — the directory holds suites this phase does not read, which is the same reasoning
    the script already records for `src/components/panel/__tests__`, `src/lib`, `src/pages` and
    `src/components/layout`.

    Add `BASELINE` pins keyed by **bare filename** (`bareName`) for the same four files. Verified this
    pass: none of these four bare names collides with another suite in the tree, so a keyed pin is
    unambiguous.

    ⚠ **Read every pinned number from this script's own printed `actual` column, across two agreeing
    runs.** Do not hand-count `it(` literals and do not copy the numbers from any planning document.
    RESEARCH measured 23 / 11 / 8 / 7 on 2026-08-10 — treat those as an *expectation to falsify*, not a
    value to type. If a run disagrees with the expectation, pin what the gate printed and say so in the
    SUMMARY.

    Write the adoption comment in the `:968-974` shape: state **which existing entry failed to cover
    the file** (the directory entries reach only `src/components/workflows`; `src/pages` is reached by
    five named files; `src/pages/__tests__` is reached by nothing at all), and state that the number was
    READ, not computed.

    Update the trailing `BASELINE_TOTAL` marker comment (currently `⚠ 2775 (190-15)`) to the new
    `pinned total` the script prints — read, never computed by addition.

    A `TARGETS` entry pointing at a non-existent path makes the gate ERROR (exit 2), not fail. All four
    paths exist at HEAD; confirm each with `ls` before adding its entry.
  </action>
  <acceptance_criteria>
    - `grep -c "src/pages/WorkflowsPage.test.tsx" scripts/vitest-count-gate.cjs` ≥ 1.
    - `grep -c "src/pages/__tests__/RunModal.test.tsx" scripts/vitest-count-gate.cjs` ≥ 1, likewise for
      `RunModal.a11y.test.tsx` and `PublishedCardDelete.test.tsx`.
    - The `BASELINE` map contains keys `"WorkflowsPage.test.tsx"`, `"RunModal.test.tsx"`,
      `"RunModal.a11y.test.tsx"`, `"PublishedCardDelete.test.tsx"`.
    - `node scripts/vitest-count-gate.cjs` exits 0 and prints `delta 0` for all four new rows, on
      **two** consecutive runs whose printed per-file numbers agree exactly.
    - The printed `pinned total` matches the value written into the trailing marker comment.
    - No file outside `scripts/vitest-count-gate.cjs` is modified: `git diff --name-only` returns
      exactly that one path.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>The gate runs and pins the four previously-invisible suites; two consecutive runs agree; exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Decide and record the two open adoption questions (OQ3)</name>
  <files>scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `.planning/phases/192-workflow-library-ia/192-RESEARCH.md` § "Open Questions" item 3 and
      § "Assumptions Log" A6 (the `WorkflowBuilderPage.session.test.tsx` "pane click" timeout measured
      at 5060 ms against a 5000 ms limit under `--maxWorkers=4`, passing on re-run).
    - `scripts/vitest-count-gate.cjs` header — the adoption rule requires a stated reason either way.
    - `frontend/src/pages/WorkflowBuilderPage.session.test.tsx:482`, `:510`, `:768` — the three live
      `WorkflowsPage` renders that make it a covering suite.
    - `frontend/src/components/layout/__tests__/ChatLayoutLaunch.test.tsx` — 2 tests, reached through
      `ChatLayout`.
  </read_first>
  <action>
    **Adopt** `src/pages/WorkflowBuilderPage.session.test.tsx` into TARGETS and BASELINE. It renders the
    live `WorkflowsPage` three times and is currently unguarded; an unpinned covering suite is an
    unguarded one, not a lightly-guarded one (the 188-12 statement).

    Record RESEARCH assumption **A6 in the adoption comment before the pin**: its "pane click" case was
    measured as a parallel-load flake (5060 ms vs a 5000 ms limit at `--maxWorkers=4`), not latent rot.
    State that the adoption is therefore expected to import zero rot, and that if this case reds
    consistently during Phase 192 it is a pre-existing flake to re-run, not a regression to chase.
    Confirm before pinning by running the suite twice with `GSD_VITEST_MAX_WORKERS=4`.

    **Decline** `src/components/layout/__tests__/ChatLayoutLaunch.test.tsx`, and write the reason into
    the same comment block: 2 tests owned by the layout concern, reaching the page only transitively
    through `ChatLayout`, whose own launch contract is already pinned via
    `ChatLayout.launch.test.tsx` (17). A decline with no recorded reason is indistinguishable from an
    oversight, which is what the script's own adoption rule exists to prevent.

    Both numbers again read from the printed `actual` column across two agreeing runs.
  </action>
  <acceptance_criteria>
    - `grep -c "src/pages/WorkflowBuilderPage.session.test.tsx" scripts/vitest-count-gate.cjs` ≥ 1 and
      the `BASELINE` map contains key `"WorkflowBuilderPage.session.test.tsx"`.
    - `grep -c "ChatLayoutLaunch" scripts/vitest-count-gate.cjs` ≥ 1 — present as a **declined** entry
      in a comment, and absent from both `TARGETS` and the `BASELINE` map keys.
    - The comment block names A6 explicitly (the string `A6` or `5060` appears within it).
    - `node scripts/vitest-count-gate.cjs` exits 0 on two consecutive runs with identical per-file
      numbers.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages/WorkflowBuilderPage.session.test.tsx`
      reports 0 failures on at least one of two runs; if run 1 reds only on "pane click", record both
      outcomes in the SUMMARY rather than silently re-running.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>Five suites adopted, one decline recorded with its reason, gate green twice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → CI verification | The count gate is the only mechanism that notices a silently deleted test. A gate that does not cover a file cannot report on it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192-06 | Repudiation | `scripts/vitest-count-gate.cjs` coverage | mitigate | Adopt the five covering suites into BOTH knobs before any source change, so a deletion during D-01's restructure produces `[count-decrease]` rather than a green run. |
| T-192-07 | Tampering | pinned counts | mitigate | Every pin is READ from the gate's own `actual` column across two agreeing runs; the trailing total marker is read, never computed. This is the recorded failure mode — that note has gone stale eight times by being computed. |
| T-192-SC | Tampering | package installs | accept | **No packages are installed by Phase 192** (RESEARCH § "Package Legitimacy Audit": zero new dependencies, verified against `frontend/package.json`). The Package Legitimacy Gate is not applicable; if any plan proposes an install, that is a scope change to surface, not execute. |
</threat_model>

<verification>
- `node scripts/vitest-count-gate.cjs` exits 0, twice, with identical per-file output.
- `git diff --name-only HEAD~1` lists exactly `scripts/vitest-count-gate.cjs` — this commit changes no
  source, which is the property that makes it a valid "stick that already existed".
</verification>

<success_criteria>
- Five suites (`WorkflowsPage.test.tsx`, `RunModal.test.tsx`, `RunModal.a11y.test.tsx`,
  `PublishedCardDelete.test.tsx`, `WorkflowBuilderPage.session.test.tsx`) appear in TARGETS and in the
  BASELINE pin map.
- `ChatLayoutLaunch.test.tsx` is declined **with a written reason**.
- The gate is green and this is the first commit of Phase 192.
</success_criteria>

<output>
Create `.planning/phases/192-workflow-library-ia/192-01-SUMMARY.md` when done. Record the printed
per-file `actual` numbers from BOTH runs, the new `pinned total`, and — if any number differs from
RESEARCH's measured expectation (23 / 11 / 8 / 7 / 23) — say so plainly rather than smoothing it.
</output>
