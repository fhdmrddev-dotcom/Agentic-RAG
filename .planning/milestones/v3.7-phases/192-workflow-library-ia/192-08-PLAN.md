---
phase: 192-workflow-library-ia
plan: 08
type: execute
wave: 4
depends_on: ["192-04", "192-06"]
files_modified:
  - frontend/src/components/workflows/library/WorkflowDeleteSheet.tsx
  - frontend/src/pages/WorkflowsPage.tsx
  - frontend/src/pages/__tests__/PublishedCardDelete.test.tsx
autonomous: true
requirements: [LIB-03]

must_haves:
  truths:
    - "D-01: the WFIN-03 victim-naming delete Sheet lives in its own module, owning BOTH its state and its JSX, and `WorkflowsPage.tsx` declares neither — a HARD CUT with no re-export shim."
    - "The Sheet renders byte-identically after the move across all seven captured states, proved with ZERO re-capture, on the SAME render path the baselines were captured from."
    - "Both graded-guard invariants survive: the Sheet never dismisses mid-delete, and the card leaves the list only on server confirmation — never optimistically."
    - "The move takes all four pieces as ONE unit — the `DeletePhase` type, the five hooks plus `descId`, the two handlers, and the JSX — because the block that reads self-contained is not."
  artifacts:
    - path: "frontend/src/components/workflows/library/WorkflowDeleteSheet.tsx"
      provides: "the victim-naming delete Sheet, state + JSX as one component"
      min_lines: 190
      exports: ["WorkflowDeleteSheet"]
  key_links:
    - from: "frontend/src/pages/WorkflowsPage.tsx"
      to: "frontend/src/components/workflows/library/WorkflowDeleteSheet.tsx"
      via: "named import; the page declares neither the type nor the component"
      pattern: "from \"@/components/workflows/library/WorkflowDeleteSheet\""
    - from: "library/WorkflowDeleteSheet.tsx"
      to: "getWorkflowDeletePreview + deleteWorkflowCascade"
      via: "server-confirmed destructive flow with exact counts"
      pattern: "deleteWorkflowCascade"
---

<objective>
Execute D-01's second verbatim move: the WFIN-03 victim-naming delete Sheet out of `PublishedCard` and
into `components/workflows/library/WorkflowDeleteSheet.tsx` — **create additively, then cut** — proving
the seven `192-04` baselines hold with zero re-capture.

Purpose: this move is the one most likely to be done wrong. CONTEXT.md scopes it at the JSX range
`:872-1003`; the block **closes over** `type DeletePhase` (`:743`), five `useState` hooks plus `descId`
(`:768-772`), `openDeleteSheet` (`:774-786`) and `handleDelete` (`:788-799`) — all inside the
`PublishedCard` that D-01 deletes. Real extent ≈ **169 lines**. RESEARCH names the failure mode
precisely: *the block reads self-contained; the closure lives 100 lines above it inside a component
being deleted.*

The cut happens **here rather than in the page-restructure plan** for one measured reason: the seven
baselines were captured by rendering the LIVE page through `PublishedCard`, so cutting the Sheet while
`PublishedCard` still exists proves byte-identity on the *same render path* the capture used. Doing it
later, inside the card rewrite, would change the render path and the move at the same time, and neither
half could then be blamed for a difference.

Output: a self-contained Sheet component; a `PublishedCard` that mounts it; seven baselines unmoved.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/192-workflow-library-ia/192-RESEARCH.md
@.planning/phases/192-workflow-library-ia/192-PATTERNS.md
@.planning/phases/192-workflow-library-ia/192-04-SUMMARY.md

<interfaces>
The four pieces that move as ONE unit, and everything they need — re-verified at HEAD:

| Piece | Page line | Note |
|---|---|---|
| `type DeletePhase = "idle" \| "deleting" \| "deleted" \| "error"` | `:743` | module-level, declared for this Sheet only |
| `sheetOpen` · `preview` · `previewError` · `deletePhase` · `descId` | `:768-772` | **inside `PublishedCard`** |
| `openDeleteSheet` | `:774-786` | calls `getWorkflowDeletePreview` |
| `handleDelete` | `:788-799` | calls `deleteWorkflowCascade`, then `onDeleted()` |
| the `<Sheet>` JSX | `:877-1003` (comment from `:872`) | |

Imports it needs: `getWorkflowDeletePreview`, `deleteWorkflowCascade`, `WorkflowDeletePreview` from
`@/lib/api` (page `:43-44`, `:48`); `Sheet`, `SheetContent`, `SheetHeader`, `SheetTitle` from
`@/components/ui/sheet` (page `:37`); `Check`, `Loader2`, `AlertTriangle` from `lucide-react` (page `:29`).

`descId` is `` `wf-delete-${wf.id}` `` (`:772`), wired as `<SheetContent side="bottom"
aria-describedby={descId}>` (`:886`). RESEARCH measured this as **the only `aria-describedby` on the
page today** — so it is the page's own precedent for D-14, and the id-derived-from-row-id shape is what
`WorkflowCard`'s fork consequence copies in `192-09`.
</interfaces>

**Wave note:** `192-09` (`WorkflowCard`) depends on this module and is therefore in the next wave. No
other plan in wave 4 touches `WorkflowsPage.tsx`.

**Gate note:** does not touch `scripts/vitest-count-gate.cjs`; pin raises are owed to `192-12`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create library/WorkflowDeleteSheet.tsx additively — state and JSX as ONE component</name>
  <files>frontend/src/components/workflows/library/WorkflowDeleteSheet.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowsPage.tsx:743`, `:768-799`, `:872-1003` — all four pieces, read as one
      unit. Note `:877-885`'s `onOpenChange` refusal and its shipped comment, and `:788-799`'s
      server-confirmed `onDeleted()` with its D-LOCK-04 comment.
    - `frontend/src/components/workflows/PlaneEditingLayer.tsx:1-52` — the docblock shape (name what it
      closes over · HARD CUT, no shim · leave moved `:NNN` references pointing at the pre-move page).
    - `frontend/src/components/workflows/PhaseNodeCard.test.tsx:2653-2664` — the rule for moving a **JSX
      fragment** rather than a whole component: the rendered DOM must be byte-identical, the moved source
      will not be, because the fragment must acquire a component wrapper, a props destructure and a props
      type. **The wrapper is the ACCEPTED delta**; byte-identity is chased for the JSX element bodies and
      their comments.
    - `.planning/phases/192-workflow-library-ia/192-PATTERNS.md` § 2 and § S-5 (the three shipped guard
      grades — this Sheet is the heaviest, and D-15 and D-18 both argue by reference to it).
  </read_first>
  <action>
    Create `WorkflowDeleteSheet.tsx` as **one component that owns both the state and the JSX**. Do not
    split state into the caller and JSX into the module — PATTERNS names that explicitly as *the version
    of this move that looks verbatim and is not*.

    Expose opening either through a controlled `open`/`onOpenChange` pair or through an imperative
    `useWorkflowDelete(wf)` hook returning `{ openDeleteSheet, sheet }`. Pick one, and state which and why
    in the docblock. ⚠ If you export a hook, note that `react-refresh/only-export-components` constrains
    what a component module may export — if lint objects, the hook moves to its own `.ts` leaf, the same
    rule that put `libraryFilter.ts` and `libraryVocabulary.ts` in their own files.

    Move the JSX element bodies and every inline comment **byte-for-byte**, including the `onOpenChange`
    refusal comment and `handleDelete`'s D-LOCK-04 comment — those two comments are the written record of
    the invariants, and re-typing them is how a guard quietly loses its reason.

    Take `type DeletePhase` with it. Keep `descId` derived from the row id exactly as shipped.

    Delete nothing from `WorkflowsPage.tsx` in this task. Both declarations coexist for one commit.

    Write the docblock in the `PlaneEditingLayer` shape, and add one paragraph stating the measured extent
    the move covers (`:743` + `:768-799` + `:877-1003`, ≈169 L, **not** the 132 L the JSX range suggests)
    and the reason: the pieces at `:768-799` lived inside a component D-01 deletes.
  </action>
  <acceptance_criteria>
    - `frontend/src/components/workflows/library/WorkflowDeleteSheet.tsx` exists, `wc -l` ≥ 190.
    - `grep -c "deleteWorkflowCascade" ` on it equals ≥ 1 and `grep -c "getWorkflowDeletePreview" ` ≥ 1.
    - `grep -c "DeletePhase" ` on it ≥ 1.
    - `grep -Ec "from \"[^\"]*WorkflowsPage(\.[jt]sx?)?\"" ` on it equals **0**.
    - `grep -c "title=" ` on it equals **0**.
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` equals **33**.
    - `cd frontend && npx eslint src/components/workflows/library/WorkflowDeleteSheet.tsx` → 0 errors;
      `-c eslint.a11y.config.js` → 0 errors.
    - `git diff --name-only` shows `frontend/src/pages/WorkflowsPage.tsx` NOT modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx eslint src/components/workflows/library/WorkflowDeleteSheet.tsx -c eslint.a11y.config.js</automated>
  </verify>
  <done>A self-contained Sheet module exists with state, JSX and both invariant comments intact; the page is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Cut the Sheet out of PublishedCard and prove zero re-capture on the same render path</name>
  <files>frontend/src/pages/WorkflowsPage.tsx, frontend/src/pages/__tests__/PublishedCardDelete.test.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowsPage.tsx:745-1003` — `PublishedCard` in full, so the deletion is scoped
      precisely; and `:820-845` — the `⋯` `DropdownMenu` host whose `published-delete` item calls
      `openDeleteSheet`. That testid is what `PublishedCardDelete.test.tsx:107` drives; keep it.
    - `frontend/src/pages/WorkflowsPage.tsx:586-598` — `onDeleted` re-fetching the shelf rather than
      filtering the list locally (D-LOCK-04). The prop contract does not change.
    - `.planning/phases/192-workflow-library-ia/192-04-SUMMARY.md` — the seven captured states, the capture
      SHA and the verbatim `git show` proof.
    - `frontend/src/components/workflows/WorkflowCanvas.test.tsx:756-763` — the one-direction assertion.
  </read_first>
  <action>
    Delete the four moved pieces from `WorkflowsPage.tsx` — `:743`, `:768-772`, `:774-786`, `:788-799` and
    the `<Sheet>` JSX at `:872-1003` — and have `PublishedCard` mount `WorkflowDeleteSheet` instead,
    keeping the `published-delete` menu item and every prop contract (`wf`, `onDeleted`) unchanged.

    Remove whatever the deletion orphans. `noUnusedLocals` is on, so an orphaned import is a **type
    error**: check `Sheet`, `SheetContent`, `SheetHeader`, `SheetTitle`, `Check`, `Loader2`,
    `AlertTriangle`, `getWorkflowDeletePreview`, `deleteWorkflowCascade` and `WorkflowDeletePreview`
    against the post-cut file. Read the answer; do not assume it.

    Run the `192-04` baselines. All seven must pass **with zero re-capture**. This is the check the
    ordering of this plan exists to make meaningful: the baselines render the live page through
    `PublishedCard`, which still exists, so a difference can only come from the move. If a baseline
    differs, the move was not verbatim — fix the move, never the literal.

    Add the one-direction assertion to `PublishedCardDelete.test.tsx` over `?raw` sources: the page
    imports `WorkflowDeleteSheet` from the library path; the page's source contains neither
    `type DeletePhase` nor the Sheet's JSX marker; and no re-export shim was left behind.

    Measure and record `wc -l frontend/src/pages/WorkflowsPage.tsx` after this second cut, and the
    `git diff --numstat` for both commits.
  </action>
  <acceptance_criteria>
    - `grep -c "type DeletePhase" frontend/src/pages/WorkflowsPage.tsx` equals **0**.
    - `grep -c "deleteWorkflowCascade" frontend/src/pages/WorkflowsPage.tsx` equals **0**.
    - `grep -c "from \"@/components/workflows/library/WorkflowDeleteSheet\"" frontend/src/pages/WorkflowsPage.tsx` equals **1**.
    - `grep -c "export { WorkflowDeleteSheet" frontend/src/pages/WorkflowsPage.tsx` equals **0**.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages/__tests__/PublishedCardDelete.test.tsx src/pages/WorkflowsPage.test.tsx src/pages/__tests__/RunModal.test.tsx src/pages/WorkflowBuilderPage.header.test.tsx`
      passes with **0 failures**, and `git diff` shows **no change to any `*_HTML_BASELINE` literal**.
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` equals **33**.
    - `cd frontend && npx eslint src/pages/WorkflowsPage.tsx` → 0 errors.
    - `node scripts/vitest-count-gate.cjs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages/__tests__/PublishedCardDelete.test.tsx src/pages/WorkflowsPage.test.tsx src/pages/__tests__/RunModal.test.tsx</automated>
  </verify>
  <done>The page mounts the Sheet and declares none of its four pieces; all seven baselines hold un-re-captured on the original render path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → an irreversible destructive action | `deleteWorkflowCascade` resolves a **slug** and destroys every version under it. The victim-naming Sheet with exact server counts is the guard, and it is the heaviest of three shipped grades. |
| pre-move behaviour → post-move behaviour | A 169-line move whose closure is invisible from the JSX range; a piece left behind is a runtime bug no typecheck sees. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192-11 | Tampering | the graded-guard vocabulary | mitigate | The `onOpenChange` refusal and the D-LOCK-04 server-confirmed `onDeleted()` move byte-for-byte **with their comments**; `192-04`'s two behavioural invariant tests must stay green. D-15 (fork = direct flip) and D-18 (draft delete = lighter) are both arguments *by reference to this guard*; weakening it silently makes both false. |
| T-192-12 | Tampering | the "verbatim" claim | mitigate | Create-then-cut in two commits; the cut runs while `PublishedCard` still exists so byte-identity is proved on the **same render path** the baselines used; zero re-capture; measured `--numstat` recorded. |
| T-192-22 | Elevation of Privilege | the destructive call from a new module | accept | The endpoint is unchanged and remains owner-gated server-side. The module calls the same two client functions with the same arguments; no new call site and no new capability is introduced here. (D-18's *draft* delete is a different endpoint and belongs to `192-09`.) |
| T-192-13 | Tampering | ESM cycle | mitigate | F4 covers the new module; acceptance asserts no `WorkflowsPage` specifier; `192-12` drives the fence RED against a real plant. |
| T-192-SC | Tampering | package installs | accept | None. Zero new dependencies. |
</threat_model>

<verification>
- All seven `192-04` baselines green with **zero** re-capture.
- `npx tsc -p tsconfig.app.json --noEmit` → **33**; `npx eslint src/pages/WorkflowsPage.tsx` → 0.
- `node scripts/vitest-count-gate.cjs` exits 0; `WorkflowBuilderPage.header.test.tsx` unmoved at 32.
- `grep -c "type DeletePhase" frontend/src/pages/WorkflowsPage.tsx` → 0.
</verification>

<success_criteria>
- The Sheet is one self-contained module owning state and JSX; the page declares none of its four pieces.
- Seven baselines hold, un-re-captured, on the original render path.
- Both graded-guard invariants still pass.
- The post-cut page line count and the move's `--numstat` are MEASURED and recorded.
</success_criteria>

<output>
Create `.planning/phases/192-workflow-library-ia/192-08-SUMMARY.md` when done. Record the measured
post-cut `wc -l` of `WorkflowsPage.tsx`, the new module's line count, the `--numstat` for both commits,
which opening mechanism was chosen (controlled pair vs hook) and why, and an explicit statement that no
baseline literal was edited.
</output>
