---
phase: 192-workflow-library-ia
plan: 03
type: execute
wave: 2
depends_on: ["192-01"]
files_modified:
  - frontend/src/pages/__tests__/RunModal.test.tsx
  - frontend/src/pages/__tests__/RunModal.a11y.test.tsx
autonomous: true
requirements: [LIB-03]

must_haves:
  truths:
    - "D-01: the `RunModal` characterization baseline exists in a commit where `frontend/src/components/workflows/library/RunModal.tsx` provably does not exist — so it predates the move it is meant to prove, which is the only condition under which it proves anything."
    - "Six render states of the Run modal are captured as whole-`innerHTML` literals, each observed twice on the unchanged tree with the two runs agreeing byte for byte."
    - "Each captured literal is labelled a CAPTURE, not an EXPECTATION: a later diff against it is a behaviour change to explain, never a test to update."
    - "The captures are non-vacuous — every baseline string is non-empty and separate assertions name what each one must contain."
  artifacts:
    - path: "frontend/src/pages/__tests__/RunModal.test.tsx"
      provides: "the six-state whole-innerHTML capture + the shared render→innerHTML→unmount helper"
      contains: "RUN_MODAL_HTML_BASELINE"
    - path: "frontend/src/pages/__tests__/RunModal.a11y.test.tsx"
      provides: "the focus/a11y contract capture (role, aria-modal, initial focus, Escape, Tab containment)"
  key_links:
    - from: "the capture commit"
      to: "frontend/src/components/workflows/library/RunModal.tsx"
      via: "git show <capture-sha>:<path> exits non-zero — the 188.2 proof method"
      pattern: "RUN_MODAL_HTML_BASELINE"
---

<objective>
Capture the `RunModal` characterization baseline **before** D-01 moves it, and prove the capture
predates the move.

Purpose: 188.1's lesson, paid for in that phase and binding here — *a characterization baseline only
proves something if it PREDATES the change.* 188.2 proved its own by showing every destination module
answered *"No such file or directory"* at the capture commit, and then held six baselines green through
the cut with **zero re-capture**. `RunModal` (`WorkflowsPage.tsx:1054-1405`, 352 lines) is one of D-01's
two verbatim moves; this plan is the stick the move is measured against.

Output: six whole-`innerHTML` captures plus the modal's focus/a11y contract, committed while
`library/RunModal.tsx` does not exist. **No source file is modified by this plan.**
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/192-workflow-library-ia/192-RESEARCH.md
@.planning/phases/192-workflow-library-ia/192-PATTERNS.md

**Wave note:** this plan runs in parallel with `192-04` (delete-Sheet baseline) and `192-05` (the pure
library modules). All three have disjoint `files_modified`. None mutates the local Postgres.

**Gate note:** this plan ADDS tests to a suite `192-01` already pinned. The count gate fails only on a
**decrease** (`scripts/vitest-count-gate.cjs:1343`) — growth prints yellow and is informational — so
this plan deliberately does **not** touch `scripts/vitest-count-gate.cjs`. That avoids a write conflict
with the two plans running beside it. The pin is RAISED to the settled `actual` in `192-12`, following
the 188-12 precedent (`WorkflowBuilderPage.header.test.tsx` was deliberately left unpinned *while its
count was still growing*, then pinned once it stopped). The pin is **owed**, not waived.

Worktree rules: bootstrap FIRST with `bash scripts/bootstrap-worktree.sh "$(pwd)"`; every vitest run
carries `GSD_VITEST_MAX_WORKERS=4`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture six whole-innerHTML render states of the Run modal</name>
  <files>frontend/src/pages/__tests__/RunModal.test.tsx</files>
  <read_first>
    - `frontend/src/pages/__tests__/RunModal.test.tsx` in full — its docblock (`:1-17`), the `vi.hoisted`
      + `vi.mock("@/lib/api")` seam (`:22-40`), and the live-page render at `:162`. The `vi.mock` factory
      is **exhaustive**: every api symbol the page (or its hosted Builder or the Gauntlet) imports must be
      listed or the mount throws.
    - `frontend/src/components/workflows/PhaseNodeCard.test.tsx:2555-2683` — the capture block this task
      copies. Four mechanisms, all load-bearing: rows declared **once** so capture props and assertion
      props cannot drift (`:2597-2616`); **one shared** render→`innerHTML`→unmount helper used by both
      capture and assertion (`:2618-2625`); the literal labelled a CAPTURE and observed twice
      (`:2628-2651`); non-vacuity + marker assertions (`:2675-2712`).
    - `frontend/src/pages/WorkflowsPage.tsx:1054-1405` — `RunModal` itself. Note the states that matter:
      the `:1250` guard that hides the KB-scope select when `folders` is empty, the `:1369`
      `run-destination` copy branch on `useCanvasGate`, and the `:1111-1124` read of
      `def.phases[].config.folder_scope`.
    - `.planning/phases/192-workflow-library-ia/192-PATTERNS.md` § S-3.
  </read_first>
  <action>
    Add one `describe` block holding a **single** shared helper that renders the live `WorkflowsPage`,
    opens the Run modal off a published card, reads `getByTestId("run-modal").innerHTML` (confirm the
    testid by reading the component; if the modal's root carries a different testid, use the real one and
    record which), unmounts, and returns the string. Both the capture and the later assertion must call
    that same helper — a second helper is how capture and assertion drift apart.

    Declare the six row/prop sets **once**, as a keyed table, and capture these states:
    (a) a bound workflow with folders; (b) an unbound workflow (no `project_folder_id`);
    (c) `folders=[]`, which exercises the `:1250` guard that hides the scope select;
    (d) a staged template file; (e) `launchError` set; (f) `submitting` true.

    Additionally capture the `run-destination` copy under **both** `useCanvasGate` values (the `:1369`
    branch), mocking the gate rather than flipping any setting.

    Produce each literal by READING IT OUT of the rendered DOM — print it, paste it — never by writing
    what you expect. Run the capture **twice** on the unchanged tree and confirm the two runs agree byte
    for byte before committing. Label the literals with the exact discipline
    `PhaseNodeCard.test.tsx:2628-2651` uses: ⚠ THIS LITERAL IS A CAPTURE, NOT AN EXPECTATION — a diff
    against it is a behaviour change to explain, and NOT a test to update.

    Add the non-vacuity guard: every baseline string's `.length` is greater than 0, plus separate `it`s
    asserting what each capture must CONTAIN (read from the committed string), because byte-identity
    alone is satisfied by a state that rendered nothing.

    Modify **no source file**. If a capture is impossible without a testid that does not exist, record
    that as a finding in the SUMMARY and capture against a stable structural selector instead — do not
    add a testid to `WorkflowsPage.tsx` in this plan, because a source edit destroys the "the capture
    predates the change" property this whole plan exists to establish.
  </action>
  <acceptance_criteria>
    - `grep -c "RUN_MODAL_HTML_BASELINE" frontend/src/pages/__tests__/RunModal.test.tsx` ≥ 2 (the table
      and at least one assertion).
    - The capture table has exactly **6** keys, and `git diff --stat` shows only this test file changed.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages/__tests__/RunModal.test.tsx`
      passes with 0 failures, on **two** runs whose reported test counts agree.
    - Every baseline entry asserts `.length > 0` — `grep -c "toBeGreaterThan(0)"` ≥ 1.
    - The literal carries the words `CAPTURE, NOT AN EXPECTATION`.
    - `git diff --name-only` lists no file under `frontend/src/pages/WorkflowsPage.tsx` or
      `frontend/src/components/`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages/__tests__/RunModal.test.tsx</automated>
  </verify>
  <done>Six states captured from the live tree, observed twice in agreement, labelled as captures, non-vacuous.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the modal's focus/a11y contract and prove the capture predates the move</name>
  <files>frontend/src/pages/__tests__/RunModal.a11y.test.tsx</files>
  <read_first>
    - `frontend/src/pages/__tests__/RunModal.a11y.test.tsx` in full — its docblock (`:1-25`) already
      enumerates the shipped contract: the hidden `<input type="file">` with
      `aria-label="Upload template file"` + `tabIndex={-1}` (Tab passes THROUGH — not a trap), the
      `aria-label="Remove template"` ✕, the KB-scope control reachable by role + name, the
      `role="alert"` launch error. Its live-page renders are at `:117` and `:203`.
    - `frontend/src/pages/WorkflowsPage.tsx:1190-1229` — the deliberate minimal focus trap
      (*"no heavy dep / no shadcn Dialog rewrite"*). RESEARCH § "Don't Hand-Roll" is explicit: replacing
      this during a "verbatim move" is not a verbatim move.
  </read_first>
  <action>
    Add assertions that pin the focus/a11y contract as a **contract**, so the move in `192-06` cannot
    quietly substitute a different dialog implementation: the modal root has `role="dialog"` and
    `aria-modal`; initial focus lands on the kickoff textarea; `Escape` closes; `Tab` from the last
    focusable element returns into the dialog rather than leaving it.

    These are behavioural assertions, not `innerHTML` captures — state that distinction in the block's
    comment, because the two kinds of evidence answer different questions and conflating them is how a
    reviewer ends up re-capturing a baseline.

    Then record the proof that this capture predates the move. Run:
    `git rev-parse HEAD` for the capture SHA, and
    `git show <capture-sha>:frontend/src/components/workflows/library/RunModal.tsx` — which **must exit
    non-zero** (`fatal: path ... does not exist`). Paste both the command and its exact output into the
    SUMMARY. This is 188.2's method verbatim and it is auditable after the fact; a sentence claiming the
    capture came first is not.
  </action>
  <acceptance_criteria>
    - `grep -c "aria-modal" frontend/src/pages/__tests__/RunModal.a11y.test.tsx` ≥ 1.
    - `grep -ci "escape" frontend/src/pages/__tests__/RunModal.a11y.test.tsx` ≥ 1.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages/__tests__/RunModal.a11y.test.tsx`
      passes with 0 failures.
    - `git show HEAD:frontend/src/components/workflows/library/RunModal.tsx` exits **non-zero**, and the
      command plus its verbatim output appear in the SUMMARY.
    - `node scripts/vitest-count-gate.cjs` exits 0 (the two touched suites are pinned by `192-01`; their
      counts have only grown, which the gate prints yellow and never fails).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages/__tests__/RunModal.a11y.test.tsx src/pages/__tests__/RunModal.test.tsx</automated>
  </verify>
  <done>The a11y contract is pinned and the capture commit provably predates the destination module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-move tree → post-move tree | The only evidence that a 352-line verbatim move preserved behaviour is a baseline captured before it. A baseline captured after is evidence of nothing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192-08 | Repudiation | the characterization baseline | mitigate | Prove the capture predates the move mechanically — `git show <capture-sha>:.../library/RunModal.tsx` must exit non-zero — and paste the output. 188.2's method, auditable after the fact. |
| T-192-09 | Tampering | the captured literals | mitigate | Literals are READ OUT of the DOM and observed twice in agreement; labelled `CAPTURE, NOT AN EXPECTATION`; guarded for non-vacuity so a state that rendered nothing cannot pass as byte-identical. |
| T-192-10 | Tampering | the tree under capture | mitigate | This plan modifies no source file. `git diff --name-only` must list only the two test files — a source edit inside the capture commit would destroy the property being established. |
| T-192-SC | Tampering | package installs | accept | None. Zero new dependencies in Phase 192. |
</threat_model>

<verification>
- Both suites green with `GSD_VITEST_MAX_WORKERS=4`.
- `node scripts/vitest-count-gate.cjs` exits 0.
- `git diff --name-only HEAD~2` lists only `frontend/src/pages/__tests__/RunModal.test.tsx` and
  `frontend/src/pages/__tests__/RunModal.a11y.test.tsx`.
- `git show HEAD:frontend/src/components/workflows/library/RunModal.tsx` exits non-zero.
</verification>

<success_criteria>
- Six whole-`innerHTML` captures + the canvas-gate `run-destination` branch, observed twice in agreement.
- The focus/a11y contract pinned behaviourally and distinguished from the captures in writing.
- The predates-the-move proof recorded verbatim in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/192-workflow-library-ia/192-03-SUMMARY.md` when done. Record the capture SHA,
the verbatim `git show` failure output, the two agreeing run reports, and the pin RAISE owed to `192-12`
for `RunModal.test.tsx` and `RunModal.a11y.test.tsx`.
</output>
