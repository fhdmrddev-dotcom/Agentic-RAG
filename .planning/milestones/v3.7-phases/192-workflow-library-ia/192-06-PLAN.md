---
phase: 192-workflow-library-ia
plan: 06
type: execute
wave: 3
depends_on: ["192-03"]
files_modified:
  - frontend/src/components/workflows/library/RunModal.tsx
  - frontend/src/pages/WorkflowsPage.tsx
  - frontend/src/pages/__tests__/RunModal.test.tsx
autonomous: true
requirements: [LIB-03]

must_haves:
  truths:
    - "D-01: `RunModal` lives in its own module and `WorkflowsPage.tsx` no longer declares it — a HARD CUT with no re-export shim left behind."
    - "The Run modal renders byte-identically after the move across all six captured states, proved with ZERO re-capture of any baseline."
    - "`export default WorkflowsPage` and the page's export signature line are unchanged — a pinned suite asserts both by byte-exact source text."
    - "The cut has one direction: the page imports the module and the module names no `WorkflowsPage` specifier in any import form."
  artifacts:
    - path: "frontend/src/components/workflows/library/RunModal.tsx"
      provides: "the Run modal, moved verbatim from WorkflowsPage.tsx:1054-1405"
      min_lines: 360
      exports: ["RunModal"]
  key_links:
    - from: "frontend/src/pages/WorkflowsPage.tsx"
      to: "frontend/src/components/workflows/library/RunModal.tsx"
      via: "named import; the page declares the component nowhere"
      pattern: "import \\{ RunModal \\} from \"@/components/workflows/library/RunModal\""
---

<objective>
Execute D-01's first verbatim move: `RunModal` out of `WorkflowsPage.tsx` and into
`components/workflows/library/RunModal.tsx`, in two steps — **create additively, then cut** — with the
six baselines captured in `192-03` held green without a single re-capture.

Purpose: 188.1's second lesson, after "capture first": split an extraction into *create additively* then
*cut*, so the diff a reviewer reads is a MOVE rather than a rewrite they have to re-derive. 188.2's proof
of a clean extraction was **zero re-capture** — any need to "adjust" a captured baseline means the move
was not verbatim.

Output: a 352-line component in its own module, a page that imports it and declares it nowhere, and a
one-direction import edge.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/192-workflow-library-ia/192-RESEARCH.md
@.planning/phases/192-workflow-library-ia/192-PATTERNS.md
@.planning/phases/192-workflow-library-ia/192-03-SUMMARY.md

<interfaces>
Everything `RunModal` closes over — re-verified at HEAD. It is already a top-level function taking eight
props and closes over **no page state**, which is the property that makes it liftable at all:

| Needs | From | Page line |
|---|---|---|
| `useState`, `useEffect`, `useMemo`, `useRef` | `react` | `:28` |
| `Upload`, `Check`, `X` | `lucide-react` | `:29` |
| `cn` | `@/lib/utils` | `:30` |
| `entryInputKeys`, `DefShape` | `@/components/workflows/soulData` | `:58` |
| `useCanvasGate` | `@/pages/WorkflowBuilderPage` | `:53`, used at `:1083` |
| `PublishedWorkflow` (type) | `@/lib/api` | `:45` |
| `Folder` (type) | `@/types` | `:64` |

It also reads `def.phases[].config.folder_scope` (`:1111-1124`) — inside `DefShape`'s index signature, so
no new type is needed.
</interfaces>

**Wave note:** runs in parallel with `192-07` (`LibraryToolbar`), whose `files_modified` are entirely
under `library/` and do not include this page. The delete-Sheet extraction is `192-08` and is
deliberately placed in the NEXT wave, because it also has to touch `WorkflowsPage.tsx` — two plans
cutting the same 1407-line page concurrently is the one thing `files_modified` ownership exists to
prevent.

**Gate note:** this plan does not touch `scripts/vitest-count-gate.cjs`; `RunModal.test.tsx`'s pin RAISE
is owed to `192-12`.

**Extent, corrected — do not use CONTEXT.md's range.** `function RunModal(` is at `:1054`; its closing
`}` is at **`:1405`**. `:1406` is blank and **`:1407` is `export default WorkflowsPage`**, which must not
move. Extent = 352 lines.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create library/RunModal.tsx additively — the body moves byte-for-byte</name>
  <files>frontend/src/components/workflows/library/RunModal.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowsPage.tsx:1054-1405` — the whole component, including
      `:1190-1229` (the deliberate minimal focus trap — *"no heavy dep / no shadcn Dialog rewrite"*),
      `:1250` (the guard that hides the KB-scope select when `folders` is empty) and `:1369` (the
      `run-destination` copy branch on `useCanvasGate`).
    - `frontend/src/components/workflows/PlaneEditingLayer.tsx:1-52` — the docblock pattern to copy, four
      paragraphs in order. Three habits are load-bearing: **name what it closes over** in the first
      paragraph (that is the property the move depends on); state **"HARD CUT … NO re-export shim"** (a
      shim is what makes a later `?raw` fence vacuous); and **leave the moved `:NNN` references pointing
      at the pre-move page**, saying so, rather than rewriting them — rewriting them is an edit, and this
      must read as a move.
    - `frontend/src/components/workflows/PlaneEditingLayer.tsx:23-31` — the rule that decides what may
      live here: **a component module may not export a shared runtime value**
      (`react-refresh/only-export-components`). This file exports the component and its props type and
      **no runtime value of any kind**.
    - `.planning/phases/192-workflow-library-ia/192-PATTERNS.md` § 1 and § S-2.
  </read_first>
  <action>
    Create `frontend/src/components/workflows/library/RunModal.tsx` containing the component body from
    `:1054-1405` **byte-for-byte** — nothing re-typed, nothing tidied, nothing re-ordered, every inline
    comment intact — plus the imports it needs (listed in this plan's `<interfaces>` table) and its props
    type. Export `RunModal` and its props type; export no runtime value.

    Do **not** delete anything from `WorkflowsPage.tsx` in this task. Both declarations coexist for one
    commit; that is the point of "create additively", and it is what makes the following commit read as a
    cut rather than as a rewrite under `git diff --numstat`.

    Write the docblock in the `PlaneEditingLayer.tsx:1-52` shape, naming Phase 192-06 / D-01, stating
    what the component closes over (nothing from the page — eight props and two module-level helpers),
    stating that the next commit is a HARD CUT with no re-export shim, and warning that the `:NNN`
    references inside the moved comments point at the **pre-move** `WorkflowsPage.tsx` (measured 1407 L),
    kept as shipped.

    Record the layering note rather than fixing it: `useCanvasGate` lives in a *page* module and is
    imported by a *component* (`WorkflowsPage.tsx:53`); this module inherits that import. It is **not** a
    cycle — `WorkflowBuilderPage.tsx` references `WorkflowsPage` only in prose comments — and 192 did not
    introduce the smell. Widening scope to fix it here would be scope creep; leaving it unremarked would
    let a reviewer read it as introduced here.
  </action>
  <acceptance_criteria>
    - `frontend/src/components/workflows/library/RunModal.tsx` exists and `wc -l` reports ≥ 360.
    - `grep -c "^export function RunModal(\|^export const RunModal" ` on it equals **1**.
    - The module exports no runtime value beyond the component:
      `grep -Ec "^export (const|let|var|function) (?!RunModal)" ` — inspect and confirm the only exports
      are `RunModal` and its props type.
    - `grep -Ec "from \"[^\"]*WorkflowsPage(\.[jt]sx?)?\"" ` on the new file equals **0**.
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` equals **33**.
    - `cd frontend && npx eslint src/components/workflows/library/RunModal.tsx` reports 0 errors, and
      `-c eslint.a11y.config.js` reports 0 errors.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/components/workflows/library/librarySubtree.fences.test.ts`
      passes — F1 and F4 now cover a file that actually exists.
    - `git diff --name-only` shows `frontend/src/pages/WorkflowsPage.tsx` is **not** modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"</automated>
  </verify>
  <done>The module exists with the body moved verbatim; the page is untouched; the subtree fences still pass over a real file.</done>
</task>

<task type="auto">
  <name>Task 2: Cut RunModal out of the page and prove zero re-capture</name>
  <files>frontend/src/pages/WorkflowsPage.tsx, frontend/src/pages/__tests__/RunModal.test.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowsPage.tsx:1054-1407` — confirm with `sed -n '1405,1407p'` that `:1405`
      is the component's closing `}`, `:1406` is blank and `:1407` is `export default WorkflowsPage`
      **before** deleting anything. A range that includes `:1407` moves the default export out of the page.
    - `frontend/src/pages/WorkflowsPage.tsx:639-670` — the single `<RunModal …/>` render site with its
      `key={runFor.id}` and eight props; the import replaces the declaration, the render site does not change.
    - `frontend/src/pages/WorkflowBuilderPage.header.test.tsx:119` and `:842-843` — this suite imports
      `./WorkflowsPage?raw` and holds a **byte-exact source string** of the page's export line,
      `export function WorkflowsPage({ folders, onLaunch }: WorkflowsPageProps) {`, as a positive-control
      plant target. It is pinned at 32. **Do not change that line.**
    - `frontend/src/components/workflows/WorkflowCanvas.test.tsx:756-763` — the one-direction assertion
      to copy: the consumer imports the extracted module, declares it nowhere, and no re-export shim was
      left behind. Without that third clause the two negatives are also satisfied by two modules nobody uses.
    - `.planning/phases/192-workflow-library-ia/192-03-SUMMARY.md` — the six captured states and the
      capture SHA.
  </read_first>
  <action>
    Delete `:1054-1405` from `WorkflowsPage.tsx` and add a named import of `RunModal` from
    `@/components/workflows/library/RunModal`. Leave `export default WorkflowsPage` and the
    `export function WorkflowsPage(...)` signature line untouched.

    Remove any import that the deletion orphans — `noUnusedLocals` and `noUnusedParameters` are both on in
    `tsconfig.app.json`, so an orphaned import is a **type error**, not a lint warning. Check `Upload`,
    `Check`, `X`, `useCanvasGate`, `entryInputKeys` and `Folder` specifically: some are still used by the
    page's other code and some are not, and the answer must be read from the post-cut file, not assumed.

    Run the `192-03` baselines. They must pass **with zero re-capture**. If any baseline differs, the move
    was not verbatim — restore, find the difference, and fix the move. Do **not** update a baseline: the
    literal is a capture, and a diff against it is a behaviour change to explain.

    Add the one-direction assertion to `RunModal.test.tsx` in `WorkflowCanvas.test.tsx:756-763`'s shape,
    over `?raw` sources: the page's source **matches** an import of `RunModal` from the library path; the
    page's source does **not** contain `function RunModal(`; and the page leaves **no re-export shim**
    (no `export { RunModal }` and no `export * from` pointing at the module).

    Finally, measure and record — do not estimate — `wc -l frontend/src/pages/WorkflowsPage.tsx` after the
    cut, and the `git diff --numstat` insertions/deletions for the two-commit move. 188.1 reported its
    equivalent as 22 insertions / 323 deletions; the shape of a clean move is a large deletion against a
    small insertion.
  </action>
  <acceptance_criteria>
    - `grep -c "function RunModal(" frontend/src/pages/WorkflowsPage.tsx` equals **0**.
    - `grep -c "from \"@/components/workflows/library/RunModal\"" frontend/src/pages/WorkflowsPage.tsx` equals **1**.
    - `grep -c "export { RunModal" frontend/src/pages/WorkflowsPage.tsx` equals **0** and
      `grep -c "export \* from" frontend/src/pages/WorkflowsPage.tsx` equals **0**.
    - `tail -1 frontend/src/pages/WorkflowsPage.tsx` is `export default WorkflowsPage`.
    - `grep -c "export function WorkflowsPage({ folders, onLaunch }: WorkflowsPageProps) {" frontend/src/pages/WorkflowsPage.tsx` equals **1**.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages/__tests__/RunModal.test.tsx src/pages/__tests__/RunModal.a11y.test.tsx src/pages/WorkflowsPage.test.tsx src/pages/WorkflowBuilderPage.header.test.tsx`
      passes with **0 failures**, and `git diff` shows **no change to any `*_HTML_BASELINE` literal**.
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` equals **33**.
    - `cd frontend && npx eslint src/pages/WorkflowsPage.tsx` reports 0 errors.
    - `node scripts/vitest-count-gate.cjs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run --maxWorkers=4 src/pages/__tests__/RunModal.test.tsx src/pages/__tests__/RunModal.a11y.test.tsx src/pages/WorkflowsPage.test.tsx src/pages/WorkflowBuilderPage.header.test.tsx</automated>
  </verify>
  <done>The page imports the modal and declares it nowhere; all six baselines hold with zero re-capture; the pinned Builder header suite is unmoved at 32.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| module graph | `library/RunModal.tsx` imports `useCanvasGate` from `@/pages/WorkflowBuilderPage`. An import back to `@/pages/WorkflowsPage` from anywhere under `library/` typechecks clean, lints clean, and fails only at runtime. |
| pre-move behaviour → post-move behaviour | The modal owns the run launch path into a real thread. A silently altered focus trap or destination copy is a user-facing defect no typecheck can see. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192-13 | Tampering | ESM cycle under `library/` | mitigate | F4 from `192-05` now covers a real file; the acceptance criteria assert no `WorkflowsPage` specifier in the new module, and `192-12` drives the fence RED against a real planted import — the one fence that must exist precisely because the violation compiles and lints clean. |
| T-192-16 | Tampering | the "verbatim" claim | mitigate | Two-commit create-then-cut, byte-for-byte body, zero re-capture of any of the six baselines, and a measured `git diff --numstat` recorded in the SUMMARY rather than an assertion that the move was clean. |
| T-192-17 | Denial of Service | the Run launch path | mitigate | `onLaunch` (`ChatLayout.doRun`) is a prop and is NOT changed by this plan; the render site keeps its `key={runFor.id}` and all eight props. `ChatLayout.launch.test.tsx` stays pinned at 17. |
| T-192-18 | Tampering | a pinned cross-phase assertion | mitigate | `WorkflowBuilderPage.header.test.tsx` holds the page's export line as byte-exact source text and is pinned at 32. The export signature line and `export default WorkflowsPage` are asserted unchanged. |
| T-192-SC | Tampering | package installs | accept | None. Zero new dependencies. |
</threat_model>

<verification>
- All six `192-03` baselines green with **zero** re-capture (`git diff` touches no baseline literal).
- `npx tsc -p tsconfig.app.json --noEmit` → **33**; `npx eslint src/pages/WorkflowsPage.tsx` → 0.
- `node scripts/vitest-count-gate.cjs` exits 0; `WorkflowBuilderPage.header.test.tsx` unmoved at 32.
- `grep -c "function RunModal(" frontend/src/pages/WorkflowsPage.tsx` → 0.
</verification>

<success_criteria>
- `library/RunModal.tsx` holds the component; the page imports it and declares it nowhere; no shim.
- Six characterization baselines hold, un-re-captured.
- The post-cut page line count and the move's `--numstat` are MEASURED and recorded.
</success_criteria>

<output>
Create `.planning/phases/192-workflow-library-ia/192-06-SUMMARY.md` when done. Record the measured
post-cut `wc -l` of `WorkflowsPage.tsx`, the `git diff --numstat` for both commits, the new module's line
count, and an explicit statement that no baseline literal was edited.
</output>
