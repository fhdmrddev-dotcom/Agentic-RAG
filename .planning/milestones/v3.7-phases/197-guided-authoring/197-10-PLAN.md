---
phase: 197-guided-authoring
plan: 10
type: execute
wave: 6
depends_on: [197-09]
files_modified:
  - frontend/src/pages/WorkflowBuilderPage.tsx
  - frontend/src/pages/WorkflowBuilderPage.header.test.tsx
autonomous: true
requirements: [AUTH-02]

must_haves:
  truths:
    - "The drafted header shows the workflow's NAME when it has one, so the name the author edits is the name they see"
    - "The slug is still the identity — it is never written, and it is still what shows when there is no name"
    - "The card and the header never give two answers to one question"
  artifacts:
    - path: "frontend/src/pages/WorkflowBuilderPage.tsx"
      provides: "The one identity expression — meta.name when present, else the slug"
      contains: "identityGroup"
    - path: "frontend/src/pages/WorkflowBuilderPage.header.test.tsx"
      provides: "Either a DECLARED third re-capture of FLAG_OFF_HEADER_MARKUP with its date, reason and delta table — or the recorded proof that none was forced — plus the name/slug cases"
  key_links:
    - from: "frontend/src/pages/WorkflowBuilderPage.tsx"
      to: "row 4 of the arrival card"
      via: "both render meta.name, so the header agrees with the row instantly and by construction"
      pattern: "meta.name"
---

<objective>
Make the workflow's name visible where the author looks for it — the drafted header's identity slot —
so the name row edits something the screen actually shows.

Purpose: D-19, the operator's pick at plan-phase. Measured: `WorkflowBuilderPage.tsx:2203` renders
`{meta.slug ?? "Untitled workflow"}`, and `grep -n "meta.name"` returns nothing in a render position
anywhere on the page. **The workflow's name is displayed nowhere.** D-15 accepted in words that
*"name and slug can disagree"* — but it was written before anyone had measured that the name appears
nowhere at all, so what it accepted was a **latent** disagreement. Leaving the header alone ships a
**rendered** one: `northwind-qbr-fa65a43c` in the header against `Northwind QBR` in the row, on one
screen. That is the exact defect sketch 174 shipped and caught only by looking, and it is G-4 row
**U5**.

Output: one expression, and the header suite's honest disposition.

⚠ **`slug` is NEVER written.** This plan changes what is DISPLAYED. `setName` writes `meta.name`
only (plan `197-05`), and `WorkflowDefinition.slug` is minted once at generation
(`workflow_authoring.py:571`) and is the key identity, forks and versioning use.

⚠ **THIS PLAN IS SEPARATE FROM `197-09` ON PURPOSE.** The identity `<span>` sits inside
`FLAG_OFF_HEADER_MARKUP` **band 3** — a literal that stood unedited for **nine phases** before Phase
193 re-captured it twice, and whose own note (`WorkflowBuilderPage.header.test.tsx:378-382`) says
*"THIS IS RE-CAPTURE TWO OF TWO, AND THE PHASE EXPECTS NO THIRD."* Isolating the change means a
header byte-pin failure can never be confused with a mount failure, and the re-capture — if it is
forced at all — is declared in its own plan with a date and a reason, exactly as the file's own rule
demands.

⚠ **`193.2-09`'s escape hatch does NOT apply here, and that was measured.** That plan shipped the
`AI-proposed` mark for free by putting it *"ONE gated sibling INSIDE this affordance, never a new
node in `identityGroup`"*, so the flag-off header could not see it. `:2203` **is** the identity
`<span>` itself, outside any `canvasEnabled` ternary, and band 3's captured literal contains the
rendered slug verbatim: `<span class="min-w-0 truncate text-[14px] font-semibold text-foreground">vendor-brief</span>`.
There is no clever placement that avoids this one.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/197-guided-authoring/197-CONTEXT.md
@.planning/phases/197-guided-authoring/197-PATTERNS.md
</context>

<interfaces>
`WorkflowBuilderPage.tsx:2200-2211` — the block to change, quoted verbatim:
```tsx
  const identityGroup = (
    <>
      <span className="min-w-0 truncate text-[14px] font-semibold text-foreground">
        {meta.slug ?? "Untitled workflow"}
      </span>
      <span className="shrink-0 rounded bg-muted px-1.5 py-0.5 font-mono text-[11px] text-muted-foreground">
        draft
      </span>
      {kbAffordance}
      {requirementAffordance}
    </>
  )
```

`WorkflowBuilderPage.header.test.tsx` band 3, the third entry of `FLAG_OFF_HEADER_MARKUP`, opening:
```
<header class="flex items-center justify-between border-b border-border px-4 py-2.5"><div class="flex min-w-0 items-center gap-2"><span class="min-w-0 truncate text-[14px] font-semibold text-foreground">vendor-brief</span>…
```
⚠ **`vendor-brief` is the fixture's SLUG.** Whether this literal moves depends entirely on whether
the fixture definition also carries a `name` — see Task 2's decision rule.

The two assertions that consume the literal (`:392-408`) — note the second is an equality against the
SAME literal, deliberately, so the operator/non-operator pair cannot drift:
```tsx
  it("matches the captured flag-off header byte for byte", async () => {
    const { container } = await openDraftBuilder(OFF_VARIANTS[0].value)
    expect(headerMarkup(screen.getByTestId("builder-grid"), container)).toBe(FLAG_OFF_HEADER_MARKUP)
  })
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The one identity expression — the name when there is one, the slug when there is not</name>

  <read_first>
    - `frontend/src/pages/WorkflowBuilderPage.tsx:2196-2215` — `identityGroup` in full.
    - `frontend/src/pages/WorkflowBuilderPage.tsx:2158` — the shipped narrowing idiom
      `typeof meta.business_requirement === "string" ? meta.business_requirement : ""`, which this
      expression mirrors because `meta.name` also lands under `BuilderDefinition`'s index signature
      and types as `unknown` (plan `197-05`'s decision 2).
    - `frontend/src/pages/WorkflowBuilderPage.tsx:540-548` — `BuilderDefinition`, confirming `name`
      is **not** declared and is therefore `unknown`.
    - `.planning/phases/197-guided-authoring/197-CONTEXT.md` — **D-15** and **D-19** in full.
  </read_first>

  <files>frontend/src/pages/WorkflowBuilderPage.tsx</files>

  <action>
    Replace the identity `<span>`'s child expression so it renders, in order of preference: the
    workflow's `name` when it is a non-empty string; else `meta.slug`; else the shipped
    `"Untitled workflow"` fallback. Narrow with `typeof meta.name === "string"` — the shipped idiom
    forty-five lines above.

    ⚠ **A named deviation from D-19's literal `meta.name ?? meta.slug`, recorded rather than
    silent:** the non-empty check is included because `setName` deliberately does **not** trim and
    does **not** reject an empty string (the server owns emptiness — plan `197-05`), so an author who
    clears the field would otherwise be shown a blank identity slot. This is a **display fallback**,
    not a validation rule: no store action, no request and no predicate learns anything from it, and
    nothing downstream reads it. Record the deviation and its reason in the SUMMARY.

    Change **nothing else** in `identityGroup` — not the class list, not the `draft` badge, not the
    two affordance slots. The delta this plan is allowed to produce in the flag-off header is
    **exactly one text node in band 3, and nothing else.**

    Extract the expression to a `const` above `identityGroup` if that reads better, but keep the
    rendered output identical: the JSX must still produce a single text child in that `<span>`, with
    no wrapper element, or the delta stops being one text node.
  </action>

  <acceptance_criteria>
    - `grep -c "meta.name" frontend/src/pages/WorkflowBuilderPage.tsx` returns at least `1`.
    - `git diff <base-sha> HEAD -- frontend/src/pages/WorkflowBuilderPage.tsx` shows the identity
      `<span>`'s class list byte-unchanged and the `draft` badge byte-unchanged.
    - No new element node is introduced inside the identity `<span>` — its rendered content is still
      a single text child.
    - `grep -Ec "setName\(|meta\.slug *=" ` over the changed block returns `0` — this plan displays,
      it never writes.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` produces no NEW error against plan
      `197-01`'s recorded baseline.
  </acceptance_criteria>

  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>

  <done>The header renders the name when there is one and the slug when there is not, with the empty-name deviation recorded as a display fallback.</done>
</task>

<task type="auto">
  <name>Task 2: The header suite's honest disposition — a DECLARED third re-capture, or the recorded proof that none was forced</name>

  <read_first>
    - `frontend/src/pages/WorkflowBuilderPage.header.test.tsx:307-384` — the whole re-capture note
      block. ⚠ **This is the procedure. Follow it; do not invent one.** Its four load-bearing parts:
      the encoder is **VALIDATED FIRST** (re-encoding all three OLD bands must reproduce this file's
      own bytes before one new byte is written — nothing is hand-edited); the diff is characterised
      as a **TABLE** (per band: tag deltas vs non-empty text nodes); text nodes are compared as an
      **ORDERED SEQUENCE, never by position** (a naive `split(/<[^>]*>/)` reports 8 false "changed"
      nodes when one node is inserted); and the capture is **driven twice and must agree byte for
      byte**.
    - `frontend/src/pages/WorkflowBuilderPage.header.test.tsx:378-382` — the note that must now be
      answered: *"THIS IS RE-CAPTURE TWO OF TWO, AND THE PHASE EXPECTS NO THIRD … any further
      re-capture is a behaviour change to explain in its own plan, not a test to update."*
    - `frontend/src/pages/WorkflowBuilderPage.header.test.tsx` — the `openDraftBuilder` helper and
      the fixture definition it drives. ⚠ **Read what that fixture's definition actually contains.**
  </read_first>

  <files>frontend/src/pages/WorkflowBuilderPage.header.test.tsx</files>

  <action>
    **Run the suite FIRST and let it tell you which branch you are on.** The outcome depends on a
    fact nobody has measured: whether the header suite's fixture definition carries a `name`.

    **Branch A — band 3 is byte-identical (the suite is green).** The fixture carries no `name`, so
    `meta.slug` is still what renders and the literal never moved. **Then there is NO third
    re-capture, and that is the better outcome — record it prominently:** band 3 stands for a tenth
    phase, and the note at `:378-382` is vindicated rather than superseded. Do **not** touch the
    literal. Add the new cases below and, in the note block, append a short dated entry saying that
    Phase 197 changed the expression at the identity slot and band 3 did NOT move, with the reason
    (the fixture binds no name) — so the next author knows the pin was tested against this change
    rather than merely left alone.

    **Branch B — band 3 reds.** The fixture carries a `name`, so the rendered text node changed.
    Perform the re-capture following the file's own four-part procedure exactly:
    - Validate the encoder first against all three OLD bands.
    - Drive the capture twice; the two must agree byte for byte.
    - Substitute band 3 only. `git diff --numstat` on this file must show **one changed line**, not
      three.
    - Write the note as **RE-CAPTURE THREE**, dated 2026-08-18, naming **D-19** as the reason, with
      the delta table showing: bands 1 and 2 byte-identical; band 3 tag deltas **NONE**; band 3
      non-empty text nodes 4 → 4, with **exactly one** differing element, compared as an ordered
      sequence. ⚠ **If the diff shows a TAG difference, STOP** — the file's own rule is that any
      re-capture whose diff shows an unnamed tag difference is a behaviour change to explain, not a
      test to update, and Task 1's criteria forbid one.
    - State plainly that the note at `:378-382` predicted no third and that this IS the third, so the
      prediction is recorded as superseded rather than quietly overwritten.

    **In BOTH branches, add these cases additively:**
    - Flag-off, a definition carrying a `name` ⇒ the identity slot renders the **name**, not the slug.
    - Flag-off, a definition with **no** `name` ⇒ the identity slot renders the **slug**.
    - Flag-off, a definition whose `name` is an empty string ⇒ the identity slot renders the
      **slug**, pinning Task 1's declared display fallback.
    - A definition with a `name` ⇒ the rendered header contains the name and the `slug` is **still
      present in the definition** — read it off the store, proving D-15's "the slug is untouched"
      at the same beat the display changes.

    ⚠ Record which branch occurred, verbatim, in the SUMMARY. The `git diff --numstat` for this file
    is the evidence: `0` deletions means branch A.
  </action>

  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowBuilderPage.header.test.tsx` passes.
    - The SUMMARY names the branch (A or B) and quotes
      `git diff --numstat <base-sha> HEAD -- frontend/src/pages/WorkflowBuilderPage.header.test.tsx`.
    - **Branch B only:** the numstat shows exactly **1** deleted line and the note block contains a
      dated `RE-CAPTURE THREE` entry naming `D-19` and carrying a per-band delta table.
    - **Branch A only:** the numstat shows **0** deleted lines, and the note block gained a dated
      entry recording that the pin was tested against this change and did not move.
    - Four new cases exist covering: name present, name absent, name empty, and slug-still-present.
    - The suite's test count is at least **4** greater than its value at the phase base SHA (pin
      **27** at 196's close — re-derive, do not inherit).
    - `git diff --numstat <base-sha> HEAD -- frontend/src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx frontend/src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx` shows `0` deletions — the D-05 fence is on the PRE-DRAFT screen and is untouched by a DRAFTED header change. ⚠ Two different literals, two different dispositions; do not confuse them.
  </acceptance_criteria>

  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowBuilderPage.header.test.tsx src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx</automated>
  </verify>

  <done>The header pin's disposition is recorded honestly — either a fully declared third re-capture with its delta table, or the proof that none was forced — and four cases pin the name/slug precedence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model/author string → header DOM | The workflow name is model-authored on a fresh draft and author-authored after row 4 is used; it now crosses into the header's identity slot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-197-19 | Tampering | XSS via the name in the header | mitigate | The name renders as a plain React text child in the existing `<span>`; no raw-HTML prop is introduced, and Task 1's criteria forbid any new element node inside that span. React escapes text children |
| T-197-14 | Tampering | the slug written or overwritten | mitigate | This plan only DISPLAYS. A grep criterion forbids any write in the changed block, and a case asserts the slug is still present in the store's definition at the same beat the display changes |
| T-197-29 | Repudiation | a byte pin re-captured to make a red run green | mitigate | The re-capture, if forced, follows the file's own four-part procedure: encoder validated first, capture driven twice, one changed line, and a dated note naming D-19 with a per-band delta table. A TAG difference is an explicit STOP. If no re-capture is forced, the fact is recorded rather than left silent |
| T-197-30 | Repudiation | the pre-draft D-05 fence confused with the drafted header pin | mitigate | The acceptance criteria assert `0` deletions on BOTH baseline files in the same run that permits a deletion on the header file. Two literals, two dispositions, both stated |
| T-197-21 | Tampering | the card and the header giving two answers | mitigate | Both render `meta.name` off the same live store value, so they agree by construction rather than by synchronisation. G-4 rows **U5** and **U6** are the appearance proof and are owed |
| T-197-SC | Tampering | npm installs | accept | No package is installed by this plan |
</threat_model>

<verification>
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowBuilderPage.header.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx src/pages/WorkflowBuilderPage.describe.test.tsx src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx` green.
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — no new errors against `197-01`'s baseline.
- The three untouchable-file criteria (`SeedReceipt.tsx`, `publish_service.py`, both baselines) all
  still pass. **The header file is the phase's ONE declared exclusion** and its numstat is reported
  rather than pinned at zero.
</verification>

<success_criteria>
- The drafted header shows the name when there is one, the slug when there is not, and never a blank.
- The slug is provably never written.
- The header pin's disposition is declared, dated and reasoned — in whichever direction the
  measurement went.
- G-4 rows U5 and U6 are recorded as owed, to be driven after this plan and not before.
</success_criteria>

<output>
Create `.planning/phases/197-guided-authoring/197-10-SUMMARY.md` when done.
</output>
