---
phase: 197-guided-authoring
plan: 04
type: execute
wave: 2
depends_on: [197-01]
files_modified:
  - frontend/src/components/workflows/soulData.ts
  - frontend/src/components/workflows/soulData.test.ts
autonomous: true
requirements: [AUTH-02]

must_haves:
  truths:
    - "The card can name WHICH step produces the deliverable, not merely whether one exists"
    - "A draft with two emit steps resolves to the terminal one, deterministically, whatever order the phases arrive in"
    - "A draft with no emit step resolves to nothing, and the caller can tell that apart from a malformed definition"
  artifacts:
    - path: "frontend/src/components/workflows/soulData.ts"
      provides: "terminalEmitSlug — the one derivation naming the step that produces the file"
      exports: ["terminalEmitSlug"]
    - path: "frontend/src/components/workflows/soulData.test.ts"
      provides: "RED-first cases for the tie-break, the absence and the malformed-shape ladder"
  key_links:
    - from: "frontend/src/components/workflows/soulData.ts"
      to: "rows 2 and 5 of the decisions list"
      via: "the slug handed to the page's shipped jumpToStep seam"
      pattern: "terminalEmitSlug"
---

<objective>
Add the one pure derivation the arrival card needs and the module does not yet have:
**which step produces the deliverable.**

Purpose: D-18 routes rows 2 and 5 to the terminal `llm_emit` step — row 2 because
`TemplateAttachSection` is already mounted there (`PhaseFormPanel.tsx:1166`) and owns its own server
read, row 5 because that step's *Instructions* field is what actually decides what is produced. Both
rows call the page's shipped `jumpToStep(slug)` seam, so both need a slug. Nothing in the tree
computes one: `grep -rn "terminalEmitSlug" frontend/src` returns zero.

**This makes rows 2 and 5 ONE mechanism**, which is the simplification the plan takes deliberately
rather than discovering.

Output: one exported function beside `soulDeliverable`, and its cases.

⚠ **It must NOT re-answer a question this module already owns.** `soulData.ts:236-240` refuses that
by name: *"Nothing here re-implements a derivation `soulDeliverable` or `tierForDefinition` already
owns."* `soulDeliverable` answers *"does this make a file?"* with an order-independent
`phases.some(...)`. `terminalEmitSlug` answers *"WHICH step"* — a genuinely new fact, because
`.some()` structurally cannot tell you which one and there are drafts with more than one.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/197-guided-authoring/197-CONTEXT.md
@.planning/phases/197-guided-authoring/197-PATTERNS.md
</context>

<interfaces>
The two neighbours this export sits between, quoted so nothing needs re-deriving.

`soulData.ts:172-183` — the SIMPLE derivation, and the one it must not duplicate:
```ts
export function soulDeliverable(def: DefShape | null | undefined): SoulDeliverable {
  const phases = def?.phases ?? []
  const hasEmit = phases.some((p) => p.config?.phase_type === "llm_emit")
  if (!hasEmit) return { kind: "chat" }
  const name = def?.name?.trim()
  const label = name ? `${name} · file` : "file deliverable"
  return { kind: "file", label }
}
```
⚠ Two facts a plan must carry: it is `phases.some(...)`, **order-independent**; and its label is the
workflow **NAME** plus `· file`, so rows 4 and 5 are coupled today. A row 5 written as if it were
independent of row 4 will change when the author edits the name and be unable to say why.

`soulData.ts:262-276` — the DEFENSIVE-SHAPE ladder to copy, arm for arm, each with its reason:
```ts
export function templateAdmission(def: DefShape | null | undefined): TemplateAdmission {
  // (1) The wire did not say. Nullish, or a `phases` the server never sent as an array.
  if (!def || !Array.isArray(def.phases)) return "unknown"
  // (2) An empty `phases` is a stub nobody authored — 110 of 145 published rows.
  if (def.phases.length === 0) return "unknown"
  // (2b) ⚠ A PHASE LIST THAT DECLARES NO `config` AT ALL DESCRIBES NOTHING ABOUT PHASE TYPE
  if (def.phases.every((p) => p?.config?.phase_type === undefined)) return "unknown"
```

The new export's signature:
```ts
/** WHICH step produces the deliverable — `null` when none does. */
export function terminalEmitSlug(def: DefShape | null | undefined): string | null
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add terminalEmitSlug beside soulDeliverable, with the module's own defensive ladder</name>

  <read_first>
    - `frontend/src/components/workflows/soulData.ts:122-200` — `soulDeliverable`, the
      `SoulDeliverable` type and the docblock at `:184-196` stating why three states are load-bearing
      and why `soulDeliverable`'s `def?.phases ?? []` collapse is the CAUTIONARY precedent.
    - `frontend/src/components/workflows/soulData.ts:227-276` — the jsonb string-scalar note
      (194 of 223 rows store the definition as a jsonb STRING SCALAR) and the five-arm
      `templateAdmission` ladder.
    - `frontend/src/components/workflows/soulData.ts:236-240` — the no-second-derivation rule,
      verbatim. Quote it in the new export's docblock.
    - `.planning/phases/197-guided-authoring/197-CONTEXT.md` **D-18** — the operator's pick, and the
      refuted premise recorded beside it.
  </read_first>

  <files>frontend/src/components/workflows/soulData.ts</files>

  <action>
    Add `terminalEmitSlug(def)` immediately beside `soulDeliverable`, returning `string | null`.

    **Selection rule:** among phases whose `config?.phase_type === "llm_emit"`, return the slug of
    the one with the greatest `phase_index`. When two or more tie, or when `phase_index` is missing
    or non-numeric on the candidates, fall back to the **last such phase in array order** — a
    deterministic tie-break, stated in the docblock, never left to `sort` stability.

    **Defensive ladder, copying `templateAdmission`'s arm-per-reason shape:**
    - `!def || !Array.isArray(def.phases)` → `null` (the wire did not say)
    - no phase with `phase_type === "llm_emit"` → `null`
    - a candidate with no usable `slug` (absent, non-string, empty after trim) is **not** a
      candidate — the value's only purpose is to be handed to `jumpToStep`, and a slug that selects
      nothing is worse than an honest `null`.

    **Do NOT re-answer "does this make a file".** The docblock states, quoting `soulData.ts:236-240`,
    that this export's question is *which step*, that `soulDeliverable` remains the one owner of
    *whether*, and that a caller wanting both asks both. Do not refactor `soulDeliverable` to use
    this; its order-independent `.some()` is correct for its own question and is under a shipped
    suite.

    ⚠ **The docblock must also record the row-4/row-5 coupling** so a later reader does not
    rediscover it: `soulDeliverable`'s label interpolates the workflow NAME, so the deliverable
    row's *label* changes when the name row is edited. `terminalEmitSlug` is name-independent by
    construction, which is why the card reads the two separately.
  </action>

  <acceptance_criteria>
    - `grep -c "export function terminalEmitSlug" frontend/src/components/workflows/soulData.ts` returns `1`.
    - The function's body contains no call to `soulDeliverable` and no re-implementation of its
      `{kind:"chat"}` / `{kind:"file"}` shape — verified by reading: it returns only `string | null`.
    - `git diff <base-sha> HEAD -- frontend/src/components/workflows/soulData.ts` shows **no
      deletions** — this is a pure addition; `soulDeliverable` and `templateAdmission` are untouched.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` produces no NEW error against plan
      `197-01`'s recorded baseline.
  </acceptance_criteria>

  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>

  <done>`terminalEmitSlug` exists as a pure addition beside its two neighbours, with the ladder and the no-second-derivation rule in its docblock.</done>
</task>

<task type="auto">
  <name>Task 2: RED-first cases — the tie-break, the absence, and the shapes the wire really sends</name>

  <read_first>
    - `frontend/src/components/workflows/soulData.test.ts` — read the existing
      `soulDeliverable`/`templateAdmission` describes for the fixture-building idiom and the naming
      convention. The suite's gate pin is **36** at the phase base SHA; a growing number is the gate
      working.
    - `.planning/phases/197-guided-authoring/197-PATTERNS.md` §`soulData.ts` — the "no shipped
      derivation orders phases by `phase_index` for a *pick*" note. This is net-new logic and must be
      RED-first against ≥ 2 emit phases.
  </read_first>

  <files>frontend/src/components/workflows/soulData.test.ts</files>

  <action>
    Add a `terminalEmitSlug` describe covering, at minimum:

    1. **Two emit phases, `phase_index` ascending** → the greater index's slug.
    2. **Two emit phases with `phase_index` DESCENDING in array order** → still the greater index's
       slug. ⚠ This is the case that distinguishes the new logic from `soulDeliverable`'s
       order-independent `.some()`; write it first and confirm it goes RED before the implementation
       is trusted, then record that in the SUMMARY.
    3. **Two emit phases with equal or missing `phase_index`** → the LAST in array order, per the
       declared tie-break.
    4. **No emit phase** → `null`.
    5. **`null` / `undefined` definition** → `null`.
    6. **`phases` not an array** (the wire did not say) → `null`.
    7. **An emit phase whose `slug` is absent, non-string or empty** → that phase is not a candidate;
       with no other candidate the answer is `null`.
    8. **A phase list declaring no `config` at all** → `null`.
    9. **Mixed types** — one `llm_emit` among several non-emit phases → the emit phase's slug, so the
       filter is proven to filter rather than to pick position 0 or the last element blindly.

    Add one **coupling case** proving the two derivations stay separate: on a definition whose `name`
    is edited, `terminalEmitSlug` returns the same slug while `soulDeliverable`'s label changes. That
    is the row-4/row-5 coupling made checkable instead of remembered.
  </action>

  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/soulData.test.ts` passes.
    - The suite's test count is at least **10** greater than its value at the phase base SHA
      (re-derive that value; do not inherit `36`).
    - The descending-`phase_index` case exists and is named so it is findable — its title contains
      the token `phase_index`.
    - The SUMMARY quotes the RED output of the descending-order case taken before the implementation
      satisfied it.
    - `git diff --numstat <base-sha> HEAD -- frontend/src/components/workflows/soulData.test.ts`
      shows **zero deletions** — this is an additive describe; no existing case is edited.
  </acceptance_criteria>

  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/soulData.test.ts</automated>
  </verify>

  <done>Ten-plus additive cases pin the tie-break, the absence ladder and the row-4/row-5 separation, with the RED-first evidence recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server JSONB → client derivation | `WorkflowDefinition` arrives as author-supplied JSONB; 194 of 223 stored rows are jsonb STRING SCALARS, so a phase list may be absent, empty, or shaped unlike the model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-197-12 | Tampering | `terminalEmitSlug` fed a malformed definition | mitigate | The `templateAdmission` defensive ladder is copied arm for arm; every non-conforming shape resolves to `null` rather than throwing or returning a slug that selects nothing. Cases 5-8 drive each arm |
| T-197-13 | Repudiation | a slug that names no step | mitigate | A candidate with an absent, non-string or empty slug is excluded, so the value handed to `jumpToStep` either selects a real step or is `null`. Case 7 drives it |
| T-197-SC | Tampering | npm installs | accept | No package is installed by this plan |
</threat_model>

<verification>
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/soulData.test.ts` green.
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — no new errors against `197-01`'s baseline.
- Zero deletions in both modified files.

⚠ **Wave-2 note:** this plan shares no file with `197-02`, `197-03` or `197-05` and mutates no
database, so all four run in parallel safely under CLAUDE.md's rule 4. Bootstrap the worktree first:
`bash scripts/bootstrap-worktree.sh "$(pwd)"`.
</verification>

<success_criteria>
- `terminalEmitSlug` exists, is pure, is `null`-safe, and answers *which step* without re-answering
  *whether there is one*.
- The deterministic tie-break is declared in the docblock and driven by a case.
- Rows 2 and 5 can now be built as one mechanism in `197-06`.
</success_criteria>

<output>
Create `.planning/phases/197-guided-authoring/197-04-SUMMARY.md` when done.
</output>
