---
phase: 197-guided-authoring
plan: 11
type: execute
wave: 7
depends_on: [197-10]
files_modified:
  - scripts/vitest-count-gate.cjs
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
autonomous: true
requirements: [AUTH-02]

must_haves:
  truths:
    - "The three new suites are gated, so a future edit cannot silently delete their cases"
    - "Every file this phase touched has a ledger row that was RE-DERIVED, not copied forward"
    - "Every decision this phase declined carries a written re-open trigger, and every criterion it declared was run one last time"
  artifacts:
    - path: "scripts/vitest-count-gate.cjs"
      provides: "BASELINE pins for the three new component/module suites"
      contains: "DecisionsList.test.tsx"
    - path: "CLAUDE.md"
      provides: "Re-derived hot-file rows for the six G-5 files plus young rows for the three new files"
    - path: "docs/HOT-FILE-LEDGER.md"
      provides: "The matching detail sections — the same-commit sync rule"
  key_links:
    - from: "CLAUDE.md hot-file table"
      to: "docs/HOT-FILE-LEDGER.md sections"
      via: "the same-commit sync rule — a row without a section, or a section without a row, is drift"
      pattern: "HOT-FILE-LEDGER"
---

<objective>
Close the phase's mechanical obligations: gate the new suites, re-derive the ledger, and run every
criterion the phase declared one last time.

Purpose: two of this project's standing lessons converge here. **A hot file missing from the scan
list is permanently invisible to its own guardrail** — eleven files were found absent at Phase 196,
one of them (`backend/app/config.py`) for the project's entire life. And **a row that is present and
WRONG answers the auditor with `satisfied` and stops the audit, which is worse than an absent row** —
five rows were found stale on 2026-08-17 and three of them read `satisfied`. So this plan re-derives
rather than edits, in one batch pass.

Output: three gate pins, nine ledger rows, and the phase's close-out record.

⚠ **This plan is deliberately LAST and deliberately alone in its wave.** The gate pins must be taken
against the FINAL suite counts; a pin written before plan `197-10` landed would be wrong by the time
it shipped.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/197-guided-authoring/197-CONTEXT.md
@.planning/phases/197-guided-authoring/197-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the three new suites in the count gate, and record the final verdict</name>

  <read_first>
    - `scripts/vitest-count-gate.cjs` — the `BASELINE` map (see the shipped rows `"SeedReceipt.test.tsx": 68`
      at `:242` and `"soulData.test.ts": 36` at `:517`) and the `TARGETS` array. ⚠ Re-derive both line
      numbers; this file measures **100 commits / 16 phases / 3215 lines** and its own ledger row
      warns that it was absent from the scan list for sixteen phases **on the file that ENFORCES the
      guardrails**.
    - `scripts/vitest-count-gate.cjs:129`, `:454`, `:655`, `:731` — the one-knob-vs-two-knob comments:
      *"`src/components/workflows` is already a DIRECTORY entry in the `TARGETS` array below, so [the
      file] RAN the moment it landed."*
    - `CLAUDE.md` §"Parallel execution" — a GROWING total is the gate WORKING; its contract is *no
      per-file DECREASE* and *zero failing*, never a fixed grand total.
    - `.planning/phases/197-guided-authoring/197-01-SUMMARY.md` — the gate baseline this phase started
      from.
  </read_first>

  <files>scripts/vitest-count-gate.cjs</files>

  <action>
    Add BASELINE pins for the three suites this phase created:
    `decisionsVocabulary.test.ts`, `DecisionsList.test.tsx`, `DraftArrivalCard.test.tsx`.

    **Derive each count by running the suite and reading its verdict**, never by counting `it(` calls
    in the source. Pin the number the gate itself reports.

    ⚠ **All three live under `frontend/src/components/workflows/`, which is already a DIRECTORY entry
    in `TARGETS`.** That is one knob, not two — a BASELINE pin is all that is owed. This placement was
    chosen at plan time precisely to avoid the two-knob path `src/pages` requires. **Do not add
    `TARGETS` entries for them.**

    Run the full gate and record the verdict line **verbatim**, beside `197-01`'s recorded starting
    figure, so the phase's delta is visible: total, failed, pinned total, pinned-file fraction.

    ⚠ **If the gate reds, follow VALIDATION.md's non-negotiable procedure.** Filenames from the
    gate's **own persisted JSON before any re-run**; each checked against
    `git diff --numstat <base-sha> HEAD` and `git status --short`; **cap untouched**.
    ⚠ `WorkflowBuilderPage.canvas.test.tsx` is BOTH one of SEED-171's five flaky suites AND a suite
    this phase edited — it WILL be in the diff, so it is **not** provably a flake. Say *"provably
    unmodified"*, never *"fine"*. And remember a red gate is sometimes REAL: `196-08` hit 249 genuine
    failures from missing `@/lib/api` mock exports. Plan `197-06` added a TYPE, not a runtime export,
    so that trigger should not fire — if it does, that is a finding.

    ⚠ **Raising or adding a pin necessarily changes lines in this file**, so a
    `grep -c '^-[^-]'` expecting `0` on `scripts/vitest-count-gate.cjs` is wrong. It is one of the
    phase's two declared numstat EXCLUSIONS (plan `197-01`).
  </action>

  <acceptance_criteria>
    - `grep -c "decisionsVocabulary.test.ts\|DecisionsList.test.tsx\|DraftArrivalCard.test.tsx" scripts/vitest-count-gate.cjs` returns `3`.
    - No new `TARGETS` entry was added for any of the three — `git diff` shows the additions inside
      the BASELINE map only.
    - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` exits `0` and its verdict line is
      quoted verbatim in the SUMMARY beside `197-01`'s starting figure.
    - The SUMMARY states the pinned-file fraction went UP by exactly 3 and explains that a growing
      total is the gate working.
  </acceptance_criteria>

  <verify>
    <automated>GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs</automated>
  </verify>

  <done>Three suites pinned with counts the gate itself reported, the full gate green, and the phase's delta recorded against its own starting figure.</done>
</task>

<task type="auto">
  <name>Task 2: Re-derive the hot-file ledger in one batch pass, and sync the detail file in the same commit</name>

  <read_first>
    - `CLAUDE.md` §"Hot-file ledger (update as phases ship)" — the three-command re-derivation
      recipe, the rule that six-digit buckets (`260814`, `260809`, `260529`) are DATED QUICK TASKS and
      are subtracted, and ⚠ the **SAME-COMMIT SYNC RULE**: a row here and its section in
      `docs/HOT-FILE-LEDGER.md` are updated in the same commit; a row without a section, or a section
      without a row, is drift.
    - `docs/HOT-FILE-LEDGER.md` — the sections for the six files this phase's plans named, for the
      format each row's narrative takes.
    - `.planning/phases/197-guided-authoring/197-01-SUMMARY.md` — the six triples measured at the
      phase base SHA, for the before/after delta.
    - `CLAUDE.md` §"CLAUDE.md context budget" — the 150,000-char hard limit and the 120,000 warn
      band. ⚠ Measure with the gate, never with `wc`.
  </read_first>

  <files>CLAUDE.md, docs/HOT-FILE-LEDGER.md</files>

  <action>
    Re-derive, in ONE batch pass, the `commits / phases / lines` triple for every file this phase
    touched or named, using the ledger's own three commands. **Do not trust a cell; re-derive it.**

    **The six G-5 rows**, with their dispositions updated to what this phase actually did:
    - `frontend/src/lib/api.ts` — gained **one type-union arm and nothing else**. The seam is
      **DECLINED as a named decision** (plan `197-06`), with its re-open trigger: the next phase that
      adds a RUNTIME export or a second concern to that file.
    - `frontend/src/pages/WorkflowBuilderPage.tsx` — **honoured by construction**: the new surface,
      its list and its vocabulary are NEW files (the 193.1 precedent); the page gained a mount, a
      snapshot capture, two focus refs and one identity expression.
    - `backend/app/api/workflows.py` — ⚠ **NOT MODIFIED.** Measured: the route declares no
      `response_model` and passes the service dict through, so the phase's wire change needed zero
      change here. Record that as a *measured non-touch*, which is a stronger statement than silence.
    - `backend/app/services/harness/grounding.py` — ⚠ **NOT MODIFIED.** The predicate already lived
      in exactly one place; this phase added a second CONSUMER, not a second copy.
    - `frontend/src/components/workflows/builderStore.ts` — gained `setName` only.
    - `backend/app/services/workflow_authoring.py` — gained one key on one success return.

    **Three young rows for the files this phase created**, placed in the below-threshold section with
    their binding invariants stated, because `WorkflowsPage.tsx` escaped G-5 for ten phases purely by
    not being written down:
    - `DraftArrivalCard.tsx` — ⚠ **the suppression list is load-bearing**: it must name the receipt's
      four frame/duplicate handles and NONE of its four content handles, or the fold silently blanks.
    - `DecisionsList.tsx` — ⚠ **three arms, never two**: an absent readiness renders nothing
      affirmative, identically to a green one.
    - `decisionsVocabulary.ts` — ⚠ **a true leaf**: it imports nothing, and no row's copy may claim a
      publish requirement (only row 3 may, and its sentence arrives on the wire).

    Add or update the matching sections in `docs/HOT-FILE-LEDGER.md` **in the same commit**, each
    naming the seam the next refactor should take and the invariants the file carries.

    ⚠ **Where a re-derived figure contradicts what the table said, record BOTH** — the measured one
    and the one it replaces — rather than overwriting. That is this ledger's own convention and the
    only reason its rot is auditable at all.

    Run the size gate afterwards. If the file crosses the 120,000-char warn band, **schedule** the
    split by function per CLAUDE.md's own rule (verdict/index/scan-list stays; narrative moves) rather
    than deleting a row — the table's completeness is what makes it work.
  </action>

  <acceptance_criteria>
    - `node scripts/check-claude-md-size.cjs` exits `0`.
    - `CLAUDE.md`'s hot-file table contains a row for all nine files (six G-5 + three new), and each
      of the six carries a re-derived triple.
    - Every row added or changed in `CLAUDE.md` has a matching section in `docs/HOT-FILE-LEDGER.md`
      in the same commit — verified by grepping each file path in both.
    - The two `NOT MODIFIED` rows say so explicitly, with the measured reason.
    - Any contradicted figure is recorded beside its replacement, not in place of it.
    - `git diff --stat` shows `CLAUDE.md` and `docs/HOT-FILE-LEDGER.md` changed together.
  </acceptance_criteria>

  <verify>
    <automated>node scripts/check-claude-md-size.cjs</automated>
  </verify>

  <done>Nine ledger rows re-derived in one batch pass, the detail file synced in the same commit, and the size gate clear.</done>
</task>

<task type="auto">
  <name>Task 3: Run every declared criterion one last time and write the phase's close-out record</name>

  <read_first>
    - `.planning/phases/197-guided-authoring/197-01-SUMMARY.md` — the phase base SHA, the four numstat
      criteria and the two declared exclusions.
    - `.planning/phases/197-guided-authoring/197-VALIDATION.md` — the nine G-4 rows (U1–U9) and the
      four-axis scoreboard obligation. ⚠ **The UAT rows live there and are driven by the operator;
      this task RECORDS which are owed, it does not author or drive them.**
    - `CLAUDE.md` §"Reported bugs cross-check" — the `/gsd:plan-phase` touchpoint: verify every report
      with `folded_into: 197` is covered by a plan.
    - `.planning/phases/197-guided-authoring/197-CONTEXT.md` §"THREE CLAIMS … MEASURED AS FALSE" —
      C-1, C-2, C-3.
  </read_first>

  <files>none — this task runs the criteria and records their results</files>

  <action>
    Run and quote, verbatim:

    1. The four numstat deletion criteria from `197-01`, against the phase base SHA:
       both pre-draft baselines (**0 deletions**), `SeedReceipt.tsx` (**`0 0`**),
       `publish_service.py` (**`0 0`**).
    2. The two declared EXCLUSIONS, reported rather than pinned:
       `WorkflowBuilderPage.header.test.tsx` (whatever plan `197-10`'s branch produced) and
       `scripts/vitest-count-gate.cjs`.
    3. `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — against `197-01`'s recorded baseline.
    4. `backend/venv/Scripts/python.exe -m pytest backend/tests/unit -q` — against `197-01`'s recorded
       baseline; report the delta, not an absolute judgement.
    5. `bash scripts/check-deploy-drift.sh` — expected clear. This phase adds no env var, no seeded
       migration, no bundled service and no sandbox image tag change. Record the result rather than
       assuming it.
    6. `ls supabase/migrations/` compared against the base SHA — **no migration is expected.** If one
       appeared, the phase has drifted from additive-optional and that is a finding, not a footnote.

    Then write the close-out record into the SUMMARY:

    **Reported-bugs touchpoint.** Scan `.planning/reported-bugs/*.md` frontmatter for
    `folded_into: 197`. Research measured this as **empty**, and C-2/C-3 explain why two reports that
    read as owed are not: `BUG-260809-02` is `status: closed`, `folded_into: quick-260809-klo`; and
    `BUG-260815-01` is `status: closed`, `folded_into: "193.2"`. **Record this as a checked-and-empty
    result, never as an unrun check** — and if the scan finds anything, name it and say which plan
    covers it or why it does not.

    **Every decision this phase DECLINED, each with its re-open trigger** — take it or name it, never
    neither:
    - **C-1 / the AI mark on row 4** — declined; trigger: any phase that adds a definition-level
      provenance field for another reason.
    - **The `api.ts` seam** — declined; trigger: the next phase adding a runtime export or a second
      concern there.
    - **Moving `REQUIREMENT_INVITATION` / `REQUIREMENT_AI_MARK_*` out of the page** — declined;
      trigger: the next phase touching the header affordance copy.
    - **`template_asset_id`** — allowlisted, not wired; trigger: the next phase that takes up template
      binding (`AUTH-03`'s surface).
    - **D-04 dismissal remembered across a reload** — declined; trigger: an author reporting the card
      returns after a reload they dismissed it on.

    **The owed G-4 rows.** Name U1–U9 as owed to the operator, and state the two that this phase
    cannot honestly close without them: **U1** (the appearance of the one-card composition — jsdom
    applies no CSS, so the suppression is proved by source assertion only) and **U5/U6** (the row and
    the header agreeing, and the name's first honest display). ⚠ Also record the **four-axis
    obligation** verbatim from VALIDATION.md: **U4 needs ≥ 2 providers — anthropic and openai**,
    because that is where the 0/5-vs-5/5 contrast was measured; multi-tool and parallel-thread are
    ⛔ **not applicable with the reason recorded, never silently omitted**; long-message is
    **applicable and worth one row**. Derive the roster from `MODEL_CAPABILITIES`; never re-type it.

    ⚠ **Do NOT author UAT rows as tasks and do not mark any row driven.** They live in
    `197-VALIDATION.md` and the operator drives them.
  </action>

  <acceptance_criteria>
    - The SUMMARY quotes all four numstat criteria showing `0` in the deletions column (and `0 0` for
      the two byte-frozen files).
    - The SUMMARY quotes the two exclusions' numstat with their dispositions.
    - `bash scripts/check-deploy-drift.sh` result is recorded.
    - `git diff --name-only <base-sha> HEAD -- supabase/migrations/` is empty, and that is stated.
    - The reported-bugs scan result is recorded as **checked-and-empty** with the C-2/C-3 reasons.
    - All five declined decisions are listed, each with a written re-open trigger.
    - U1–U9 are named as owed, with U1, U4, U5 and U6 flagged as the ones the phase cannot close
      without, and the four-axis obligation restated including the ⛔ rows and their reasons.
  </acceptance_criteria>

  <verify>
    <automated>git diff --numstat HEAD -- frontend/src/pages/WorkflowBuilderPage.preDraft.baseline.test.tsx frontend/src/components/workflows/WorkflowDoorSwitch.baseline.test.tsx frontend/src/components/workflows/SeedReceipt.tsx backend/app/services/harness/publish_service.py</automated>
  </verify>

  <done>Every criterion the phase declared has been run and quoted, every declined decision carries a trigger, and the owed UAT rows are named rather than assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the phase's own evidence → the next auditor | A ledger row and a close-out record are what a future phase's guardrail scan reads; a wrong one answers `satisfied` and stops the audit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-197-31 | Repudiation | a stale or absent ledger row | mitigate | All nine rows are RE-DERIVED with the ledger's own recipe in one batch pass, contradicted figures are recorded beside their replacements, and the detail file is synced in the same commit. Three new files get rows immediately rather than at their third phase, because `WorkflowsPage.tsx` escaped G-5 for ten phases purely by not being written down |
| T-197-32 | Repudiation | an unrun check recorded as a passing one | mitigate | Every criterion is quoted verbatim from its own command output, including the reported-bugs scan, which is recorded as checked-and-empty with the two C-corrections that explain why two closed reports read as owed |
| T-197-33 | Tampering | an ungated new suite silently losing cases | mitigate | Three BASELINE pins, each derived from the gate's own reported count rather than from a source grep. The one-knob placement under `src/components/workflows` was chosen at plan time for this |
| T-197-34 | Repudiation | a declined decision evaporating | mitigate | Five declines, each with a written re-open trigger, recorded in the SUMMARY. This project's own standing lesson is that a deferral without a trigger is a deletion |
| T-197-SC | Tampering | npm/pip installs | accept | No package is installed by this phase; the Package Legitimacy Gate is vacuous-with-reason and recorded as skipped rather than run against an empty set |
</threat_model>

<verification>
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` exits `0`; verdict quoted.
- `node scripts/check-claude-md-size.cjs` exits `0`.
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` at `197-01`'s baseline.
- `backend/venv/Scripts/python.exe -m pytest backend/tests/unit -q` — delta reported against
  `197-01`'s baseline.
- The four numstat criteria and the two exclusions all recorded.
- `bash scripts/check-deploy-drift.sh` recorded; no migration added.
</verification>

<success_criteria>
- Three new suites gated with counts the gate itself reported; no `TARGETS` entry added.
- Nine ledger rows re-derived, the detail file synced in the same commit, size gate clear.
- Every phase-declared criterion run and quoted; every decline carries a trigger.
- U1–U9 named as owed to the operator, with the four-axis obligation restated including its ⛔ rows.
</success_criteria>

<output>
Create `.planning/phases/197-guided-authoring/197-11-SUMMARY.md` when done.
</output>
