---
phase: 197-guided-authoring
plan: 03
type: execute
wave: 2
depends_on: [197-01]
files_modified:
  - frontend/src/components/workflows/decisionsVocabulary.ts
  - frontend/src/components/workflows/decisionsVocabulary.test.ts
autonomous: true
requirements: [AUTH-02]

must_haves:
  truths:
    - "Every sentence the arrival card shows an author is an imported identifier, so drift is assertable by character-identity"
    - "The five decisions have ONE declared order, and it is data rather than JSX sequence"
    - "No row except the requirement row claims anything about what publishing requires"
  artifacts:
    - path: "frontend/src/components/workflows/decisionsVocabulary.ts"
      provides: "The one copy home for the arrival card's decisions half — five row labels, the fold summaries, the answers-absent constants, and the declared row order"
      exports: ["DECISION_ROW_ORDER", "decisionRowLabel", "decisionsFoldSummary", "groundingFoldSummary"]
      min_lines: 120
    - path: "frontend/src/components/workflows/decisionsVocabulary.test.ts"
      provides: "Character-identity, totality, exhaustiveness, the leaf assertion and the D-20 no-false-publish-claim fence — each with a positive control"
  key_links:
    - from: "frontend/src/components/workflows/decisionsVocabulary.ts"
      to: "nothing"
      via: "it is a TRUE LEAF — it imports nothing, and that is asserted rather than documented"
      pattern: "^import "
---

<objective>
Author the one copy home for the arrival card's decisions half (D-09, D-10), and fence it.

Purpose: *"a sentence that lives inside a component is a sentence nobody can test for drift"*
(`SeedReceipt.tsx:14-15`). Four shipped precedents on this exact surface put every user-visible
string in a module and assert it by character-identity. This plan supplies the fifth, and it also
supplies the thing a component cannot: **the declared five-row order**, exported as data so
*"always five, always the same order"* (D-07) is checkable at the module level rather than by
counting JSX.

Output: `decisionsVocabulary.ts` and its suite. No component imports it yet — plans `197-06` and
`197-07` do.

## D-10 is answered: a NEW module, and the reason is mechanical, not taste

`templateFirstVocabulary.ts:5-21` records why `doorVocabulary.ts` **structurally cannot** hold a
message FUNCTION: the D-24(a) copy fence builds its needles by coercing every export of that module
through a string `.replace`, so a function export throws inside the needle builder — the fence does
not merely fail, it errors before it can sweep anything. Several of the five sentences interpolate
the current answer or a count, so they are functions. A NEW module is therefore the safe pick and
extending `doorVocabulary.ts` is forbidden by measurement.

## Two named seams, DECLINED with reasons — take it or name it, never neither

1. **`REQUIREMENT_INVITATION` (`WorkflowBuilderPage.tsx:388`), `REQUIREMENT_AI_MARK_LABEL` (`:428`)
   and `REQUIREMENT_AI_MARK_EXPLANATION` (`:432`) stay where they are.** Moving them into this
   module is a real G-5-honouring extraction and it is **declined for this phase**: this module is a
   TRUE LEAF that imports nothing (the `templateFirstVocabulary.ts:34-40` property), and a
   vocabulary module importing from a *page* inverts the dependency direction the whole
   `components/workflows` tree depends on. **Re-declaring them here is forbidden** — *"two spellings
   of a locked string is how a locked string stops being locked"* (`builderStore.ts:147`).
   **Re-open trigger:** the next phase that touches the header affordance copy.
2. **This module authors NO sentence for row 3's publish verdict.** Per D-12 and the wire posture
   locked at plan-phase, that sentence is the server's `BUSINESS_REQUIREMENT_MISSING_MESSAGE` and it
   TRAVELS on the D-13 payload, rendered verbatim (`grounding.py:995-997` — *"this string IS the UI
   copy"*). There is no mechanism in this repository that generates TS copy from a Python constant,
   and a test asserting the two match is precisely what D-12 rejected.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/197-guided-authoring/197-CONTEXT.md
@.planning/phases/197-guided-authoring/197-PATTERNS.md
@.planning/sketches/174-the-line-that-opens/README.md
</context>

<interfaces>
The contract this module must export, so plans `197-06` and `197-07` can be written against it
without exploring. Names are binding; the STRINGS are the executor's to author under the rules in
Task 1.

```ts
export type DecisionRowKey =
  | "knowledge-base" | "template" | "requirement" | "name" | "deliverable"

/** D-07's fixed order, as DATA. Five entries, this sequence, always. */
export const DECISION_ROW_ORDER: readonly DecisionRowKey[]

/** Total over DecisionRowKey, with a `never` binding in the default arm. */
export function decisionRowLabel(key: DecisionRowKey): string

/** The two fold summary lines on the card. Both return "" at zero, the shipped
 *  `seedReceiptGroundingLead` idiom — one conditional shape per line. */
export function groundingFoldSummary(count: number): string
export function decisionsFoldSummary(count: number): string
export const GROUNDING_FOLD_ACTION: string   // the sketch's "why"
export const DECISIONS_FOLD_ACTION: string   // the sketch's "review"

/** What a row shows when the AI's answer is an ABSENCE. Each states the absence
 *  honestly; none of them says a decision is fine. */
export const DECISION_KB_NONE: string
export const DECISION_TEMPLATE_NONE: string
export const DECISION_REQUIREMENT_NONE: string
export const DECISION_NAME_NONE: string

/** Row 5's two honest answers — derived by the caller from `terminalEmitSlug`. */
export const DECISION_DELIVERABLE_FILE: string
export const DECISION_DELIVERABLE_CHAT: string

/** Row actions. `DECISION_CHANGE_ACTION` hands the author to the control already on
 *  the screen; `DECISION_OPEN_STEP_ACTION` hands them to a step. */
export const DECISION_CHANGE_ACTION: string
export const DECISION_OPEN_STEP_ACTION: string
export const DECISION_NAME_FIELD_LABEL: string   // the inline field's aria-label (D-17)

/** D-03's stated limit, ONE line, shown inside the opened decisions fold. */
export const DECISION_EDIT_LIMIT_NOTE: string
```

Shipped idioms to copy, quoted so nothing needs re-deriving:

`templateFirstVocabulary.ts:134-138` — the caller passes DATA, never a sentence:
```ts
export function footingFields(count: number): string {
  return count === 1
    ? "Your draft will be built to fill this 1 field."
    : `Your draft will be built to fill these ${count} fields.`
}
```

`definitionOps.ts` — the totality guard every count formatter takes:
```ts
function wholeCount(value: number): number {
  if (!Number.isFinite(value)) return 0
  const whole = Math.trunc(value)
  return whole > 0 ? whole : 0
}
```

`definitionOps.ts:788-812` — the exhaustiveness binding a sixth row must trip at TYPECHECK, never
render as a blank line:
```ts
    default: {
      const _never: never = cause
      void _never
      return ""
    }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author decisionsVocabulary.ts as a true leaf, with the five-row order as exported data</name>

  <read_first>
    - `frontend/src/components/workflows/templateFirstVocabulary.ts` — read `:1-45` (the module
      docblock: the `doorVocabulary` impossibility, the *"a caller may pass a number; a caller may
      never assemble a sentence"* rule, and the TRUE LEAF property asserted rather than documented)
      and `:100-140` (the constant-with-its-reason idiom and the function idiom).
    - `frontend/src/components/workflows/definitionOps.ts:600-830` — `SeedReceipt`'s actual copy
      home. This is the register the new sentences must match: past-tense, first-person, plain.
      Note `seedReceiptHeading`, `seedReceiptGroundingLead`, `SEED_RECEIPT_NOTHING_COMMITTED`.
    - `frontend/src/pages/WorkflowBuilderPage.tsx:388`, `:428-435` — `REQUIREMENT_INVITATION` and the
      two AI-mark constants. Read for VOICE only. **Do not move them and do not re-declare them.**
    - `.planning/phases/197-guided-authoring/197-CONTEXT.md` — **D-16** (the mark means *"a model
      wrote this"*, never *"this is good"* or *"this is durable"*) and **D-20** (rows 1, 2, 4 and 5
      must NOT say *"before publishing"*, because no gauntlet stage refuses on any of them).
    - `.planning/sketches/174-the-line-that-opens/README.md` — the four-line card and its two fold
      lines. The acceptance bar for the two summary sentences.
  </read_first>

  <files>frontend/src/components/workflows/decisionsVocabulary.ts</files>

  <action>
    Create the module with exactly the exports named in `<interfaces>`. It **imports nothing** — a
    true leaf, asserted in Task 2 rather than documented here.

    Author the strings under five binding rules, stated in the module docblock by ROLE (see the
    prose-trap warning below):

    1. **D-20 — only the requirement row may reference publishing, and this module carries no
       sentence for it.** No exported string may claim, imply or hint that the knowledge base, the
       template, the name or the deliverable is required to publish. Measured: nothing in the
       gauntlet refuses on any of them.
    2. **D-16 — the card says a model made a choice; it never says the choice is good, durable or
       correct.** No string may read as an endorsement or a warning about quality.
    3. **D-03's limit is stated, not hidden.** `DECISION_EDIT_LIMIT_NOTE` says in one line that
       changing an answer updates the workflow but does not re-write the steps that were already
       built around the previous answer. Taken deliberately as the discretion CONTEXT.md grants,
       shown only inside the opened fold so it costs nothing at arrival (sketch 172 measured it at
       30 px).
    4. **Absence is stated as absence.** `DECISION_KB_NONE` / `DECISION_TEMPLATE_NONE` /
       `DECISION_REQUIREMENT_NONE` / `DECISION_NAME_NONE` each say plainly that there is none. None
       of them may read as "that is fine" — `templateAdmission`'s three-arm lesson
       (`soulData.ts:184-196`).
    5. **A caller may pass a number or an answer; a caller may never assemble a sentence**
       (`templateFirstVocabulary.ts:25-26`).

    Both fold formatters take `wholeCount`'s totality guard and **return `""` at zero**, matching
    `seedReceiptGroundingLead`'s shipped shape — the card then renders one conditional line rather
    than a sentence claiming zero of something.

    `decisionRowLabel` switches over `DecisionRowKey` with the `never` binding in the default arm, so
    a sixth row is a typecheck error rather than a blank line on the card.

    `DECISION_ROW_ORDER` is a `readonly` tuple in D-07's order:
    knowledge-base, template, requirement, name, deliverable.

    ⚠ **The prose trap.** Suites on this surface sweep RAW source, comments included, and
    `builderStore.ts:186-194` records a docblock that quoted the import form it was explaining and
    turned its own fence red. **Name identifiers, never banned words, in every comment in this
    file** — in particular do not spell the publish-claim phrase Task 2's fence searches for, not
    even to explain that it is banned.
  </action>

  <acceptance_criteria>
    - `frontend/src/components/workflows/decisionsVocabulary.ts` exists and exports every identifier
      listed in `<interfaces>`, spelled exactly.
    - `grep -c "^import " frontend/src/components/workflows/decisionsVocabulary.ts` returns `0`.
    - `DECISION_ROW_ORDER` has length 5 and its members are, in order:
      `knowledge-base`, `template`, `requirement`, `name`, `deliverable`.
    - `decisionRowLabel` contains a `const _never: never =` binding in its default arm.
    - `groundingFoldSummary(0) === ""` and `decisionsFoldSummary(0) === ""`.
    - Every exported string constant is non-empty and equals its own `.trim()`.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` produces no NEW error against plan
      `197-01`'s recorded baseline.
  </acceptance_criteria>

  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>

  <done>The module exists as a zero-import leaf exporting the declared contract, and typecheck is clean against the recorded baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Fence the module — totality, exhaustiveness, pairwise distinctness, the leaf property and D-20, each with a positive control</name>

  <read_first>
    - `frontend/src/components/workflows/SeedReceipt.test.tsx:1024-1240` — the whole `?raw` fence
      block. Copy three techniques verbatim: needles ASSEMBLED FROM PARTS (`:1028-1052`), the
      fence-and-its-positive-control PAIR (`:1113-1131`), and the property-not-deny-list assertion
      (`:1195-1222`). ⚠ Also read its recorded REJECTIONS — two needles were tried and deleted
      because they fired on the correct code they sat beside; *"a fence that does that gets deleted,
      not fixed."*
    - `frontend/src/components/workflows/governanceVocabulary.test.ts:24-45` and `:63-68` — the three
      scoping decisions a corpus sweep must inherit, and the house `import.meta.glob` + `?raw`
      idiom. ⚠ Note this file is a cross-cutting SWEEP with no module of its own — `governanceVocabulary.ts`
      does not exist, despite being named as a precedent upstream.
    - `frontend/src/components/workflows/templateFirstVocabulary.ts:100-117` — the *"MUST NOT MERGE"*
      pairwise-distinctness idiom.
  </read_first>

  <files>frontend/src/components/workflows/decisionsVocabulary.test.ts</files>

  <action>
    Write the suite. Every fence gets a positive control in the same file — a fence swept against an
    empty set passes green while defending nothing, and this project has shipped that twice.

    **Cases to write:**

    1. **Row order is data and it is five.** `DECISION_ROW_ORDER` deep-equals the declared tuple; its
       length is 5; its members are unique.
    2. **`decisionRowLabel` is TOTAL over `DECISION_ROW_ORDER`** — every key yields a non-empty
       trimmed string, and the five labels are **pairwise distinct** (the `FOOTING_UNAVAILABLE`
       "MUST NOT MERGE" rule: two rows sharing a label is two rows nobody can tell apart).
    3. **Count formatters are total.** Drive both fold formatters over `NaN`, `Infinity`, `-1`, `0`,
       `1`, `2`, `1.5`. Zero and every non-positive/non-finite input yields `""`; `1` and `2` yield
       distinct non-empty strings (so the singular/plural arms are exercised, not assumed).
    4. **Absence constants are pairwise distinct and non-empty**, and none of the four equals any
       other exported constant in the module.
    5. **D-20 fence — no exported string claims a publish requirement.** Assemble the searched token
       at runtime from parts (never as a contiguous literal in this file), lower-case every exported
       string value, and assert none contains it. **Positive control, in the same block:** run the
       identical detector over a planted local string that DOES contain the assembled token and
       assert it is reported. Without that half, the fence proves only that the detector was never
       exercised.
    6. **The leaf property, asserted not documented.** Import the module's own source with `?raw` and
       assert it contains no `import` statement. **Positive control:** assert the same raw source DOES
       contain the exported identifier names, so the fence cannot pass because the file failed to
       load or was empty.
    7. **No banned re-declaration.** Assert the raw source does not declare `REQUIREMENT_INVITATION`,
       `REQUIREMENT_AI_MARK_LABEL` or `REQUIREMENT_AI_MARK_EXPLANATION` — the two-spellings failure
       this plan's objective declines by name. Build those needles from parts too, and pair the
       assertion with a positive control proving the declaration-detector fires on a planted
       declaration.

    ⚠ Strip comments before any source sweep, using the shipped helper shape at
    `SeedReceipt.test.tsx:1091-1105`, so a commented-out line cannot satisfy a sweep and a prose
    mention cannot break one.
  </action>

  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/decisionsVocabulary.test.ts` passes with **at least 7** tests.
    - Every `expect(...).not.` assertion in the file has a sibling assertion in the same `describe`
      that proves the same detector fires on a plant — checkable by reading: each fence case is
      immediately followed by a case whose name contains `POSITIVE CONTROL`.
    - `grep -c "POSITIVE CONTROL" frontend/src/components/workflows/decisionsVocabulary.test.ts` is at least `3`.
    - The D-20 searched token does not appear as a contiguous string literal anywhere in the test
      file: it is assembled from parts.
    - The totality case drives at least the seven listed numeric inputs.
  </acceptance_criteria>

  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/decisionsVocabulary.test.ts</automated>
  </verify>

  <done>The copy home is fenced on order, totality, exhaustiveness, distinctness, leafness and D-20 — and every fence has been shown to fire.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | This module is a pure zero-import leaf. It reads no input, opens no request and renders nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-197-09 | Repudiation | the five sentences | mitigate | D-20's fence: no exported string may claim a publish requirement, so the surface cannot make four false claims about what the gate does. Asserted with a positive control |
| T-197-10 | Tampering | the locked header strings | mitigate | A source fence asserts this module re-declares none of the three page-owned constants — the "two spellings of a locked string" failure `builderStore.ts:147` names. Positive control included |
| T-197-11 | Repudiation | a sixth row added later | mitigate | `decisionRowLabel`'s `never` binding makes an unhandled key a TYPECHECK error rather than a blank line on the card |
| T-197-SC | Tampering | npm installs | accept | No package is installed by this plan |
</threat_model>

<verification>
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/decisionsVocabulary.test.ts` green.
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — no new errors against `197-01`'s baseline.
- `grep -c "^import " frontend/src/components/workflows/decisionsVocabulary.ts` returns `0`.
- The two declined seams are recorded in the SUMMARY with their re-open triggers.
</verification>

<success_criteria>
- `decisionsVocabulary.ts` exists, exports the declared contract, imports nothing.
- `DECISION_ROW_ORDER` makes D-07's *"always five, always the same order"* a module-level fact.
- No exported string claims a publish requirement; the fence proving it has a positive control.
- The gate pin for this new suite is **not** added here — it lands with the other two in `197-11`,
  so no two plans contend for `scripts/vitest-count-gate.cjs`.
</success_criteria>

<output>
Create `.planning/phases/197-guided-authoring/197-03-SUMMARY.md` when done.
</output>
