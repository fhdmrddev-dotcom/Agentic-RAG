---
phase: 199-the-component-map
plan: 05
type: execute
wave: 2
depends_on: ["199-01"]
files_modified:
  - frontend/src/components/workflows/WorkflowCanvas.tsx
  - frontend/src/components/workflows/FlowEdge.tsx
  - frontend/src/components/workflows/CanvasToolbar.tsx
  - frontend/src/components/workflows/PlaneEditingLayer.tsx
  - frontend/src/components/workflows/FlowEdge.test.tsx
  - frontend/src/components/workflows/CanvasToolbar.test.tsx
  - frontend/src/components/workflows/WorkflowCanvas.test.tsx
  - frontend/src/components/workflows/WorkflowCanvas.composition.test.tsx
autonomous: true
requirements: [DES-01]

must_haves:
  truths:
    - "Every connection state the canvas can express is distinguishable WITHOUT colour — line weight, dash or shape carries it, and each carries its word where a word exists"
    - "The plane has a committed ground quiet enough that a step at rest is the loudest thing on it"
    - "The sheet's payload-bearing connection labels are REPORTED as cannot-express, with the missing value named — they are not faked and not approximated"
    - "The plane does not overflow and clip its outcomes — the sheet's own recorded flaw is not inherited"
  artifacts:
    - path: "frontend/src/components/workflows/FlowEdge.tsx"
      provides: "The connection states, re-presented, non-colour-only"
    - path: "frontend/src/components/workflows/WorkflowCanvas.tsx"
      provides: "The plane ground, the empty state and the read-only state, re-presented"
  key_links:
    - from: "frontend/src/components/workflows/WorkflowCanvas.tsx"
      to: "frontend/src/components/workflows/FlowEdge.tsx"
      via: "the edgeTypes 'flow' entry — EVERY connector on the canvas renders through it"
      pattern: "edgeTypes"
    - from: "frontend/src/components/workflows/PlaneEditingLayer.tsx"
      to: "the lane, never the card"
      via: "the ＋ insert and ✕ remove affordances"
      pattern: "canvas-insert-|canvas-remove-"
---

<objective>
Re-present the canvas plane and its connections in the adopted design language, against sheet
`c1-canvas-plane` — the RE-RUN sheet, not the superseded v1.

Purpose: DES-01 on the surface a business reader looks at to understand their own process. Sheet
c1's re-run is the sheet the README calls the one that answers the note, and its connection-state
vocabulary is the strongest genuinely-buildable idea in the whole exploration.

Output: connection states each distinguishable without colour, a committed plane ground, and the
phase's single most important CANNOT-EXPRESS report — the payload-bearing connection label.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/sketches/178-stitch-component-map/README.md
@.planning/sketches/178-stitch-component-map/sheets/c1-canvas-plane.html
@.claude/skills/sketch-findings-agentic-rag/references/canvas-frame-and-node-anatomy.md
@.claude/skills/sketch-findings-agentic-rag/references/canvas-editing-and-authoring.md
@.claude/skills/sketch-findings-agentic-rag/references/icon-convention.md
@docs/HOT-FILE-LEDGER.md
</context>

<scope_fence>
**PRESENTATION ONLY.** No new data, no endpoint, no migration, no wire-model change, no new
user-facing capability. `git diff` over `backend/` and `supabase/` must be EMPTY at every task.
A task that discovers it needs a backend change **STOPS and reports**.

**THE CANNOT-EXPRESS RULE (`SEED-155`).** If the shipped component cannot express the sheet, report
it — what the sheet asks for, what the component can do, and the gap. Never fake it, never
approximate it silently, never grow a backend change to make it possible.
</scope_fence>

<known_sheet_flaws_do_not_inherit>
Recorded in sketch 178's own README as defects in the SHEET:
1. ⚠ **The plane OVERFLOWS ON THE RIGHT**, clipping the two branch outcomes at its viewport. Do not
   inherit the overflow. A re-presentation that clips content is worse than the surface it replaced.
2. ⚠ **The fork node carries a small `Fork Logic` label** — machine vocabulary that slipped past the
   never-name-the-mechanism rule. Do NOT copy it. Any word on a fork comes from the shipped
   vocabulary module or does not appear.
3. ⚠ Use `c1-canvas-plane.html`. **`c1-canvas-plane.SUPERSEDED-v1.html` is superseded** and must not
   be opened as a target.
</known_sheet_flaws_do_not_inherit>

<tasks>

<task type="auto">
  <name>Task 1: Inventory the plane and reconcile it against sheet c1</name>
  <files>frontend/src/components/workflows/FlowEdge.test.tsx, frontend/src/components/workflows/WorkflowCanvas.test.tsx</files>
  <read_first>
    - `.planning/sketches/178-stitch-component-map/sheets/c1-canvas-plane.html` — §1 the plane, §2 the five connection states, §3 the floating controls, §4 the empty plane, §5 the read-only plane
    - `.planning/sketches/178-stitch-component-map/README.md` § "Sheet 1 · The canvas plane" — including the two remaining flaws
    - `frontend/src/components/workflows/WorkflowCanvas.tsx` (1301 L) — the read-only / editable split, the two `nodeTypes` tables, the empty-draft invitation, the "No steps yet" read-only state
    - `frontend/src/components/workflows/FlowEdge.tsx` — ⚠ EVERY flow edge on the canvas renders through this component; the baseline was TRANSCRIBED from the installed `@xyflow/react` v12 source, so a careless change alters the appearance of every connector in the app
    - `frontend/src/components/workflows/PlaneEditingLayer.tsx` — the ＋ / ✕ affordances live on the LANE, never on the card
    - `frontend/src/components/workflows/CanvasToolbar.tsx` — the shipped ↶ / ↷ marks
    - `icon-convention.md` §4 — the canvas glyph vocabulary. **Read it before drawing any canvas mark.** A sketch that invents a glyph teaches the wrong vocabulary to whoever builds from it.
  </read_first>
  <action>
Measure before changing anything. No source byte outside the two named test files may move.

Inventory the plane's RESTING atoms as literal strings — the populated plane, the empty plane and
the read-only plane — pinned PRESENT so a later removal is proved by INVERSION, never by deletion.

Then reconcile against the sheet, one row per element, with a verdict:
  - ⚠ **THE PAYLOAD-BEARING CONNECTION LABEL** (`312 contracts` → `48 extracted` → `12 flagged`).
    The README calls this *"the idea worth keeping from the whole exploration"*. **Nothing in this
    system emits a per-edge count.** This is the phase's flagship CANNOT-EXPRESS: record it in the
    three required parts, naming precisely which value is missing and where it would have to come
    from, and route it to a named future requirement with a re-open trigger. **Do not build it. Do
    not approximate it with a phase count or a step index — an approximation here is a fabricated
    business figure on the surface a business reader trusts most.**
  - **The five connection states** — at rest (thin solid) · hovered (brighter) · selected (indigo,
    weighted) · flowing (dashed, alive, indeterminate) · not taken (faint dotted, still legible).
    Determine which of the five the shipped canvas can express from state it already has: the
    dashed `skip_to_phase` branch already exists, hover and selection are library state, and a
    "flowing" reading exists only where a run reading is supplied. Record each as BUILT or
    CANNOT-EXPRESS. ⚠ **Each must be distinguishable WITHOUT colour** — weight, dash or shape, and
    a WORD wherever the shipped vocabulary has one. A skipped path is information, not an absence.
  - **The ground** — the sheet commits to a quiet dot grid. Record what the plane renders as its
    ground today and whether a step at rest is already the loudest thing on it.
  - **The floating controls** — zoom out / percentage / zoom in / fit / lock. Record which exist.
  - **The empty plane and the read-only plane** — the shipped invitation and the shipped
    "No steps yet"/read-only states. ⚠ Do NOT add a second empty-state affordance; one already
    ships and a ghost node on a read-only canvas implies an affordance that does not exist.
  - **The fork** — record `Fork Logic` as a REFUSED sheet element with its reason.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/workflows/FlowEdge.test.tsx src/components/workflows/WorkflowCanvas.test.tsx src/components/workflows/WorkflowCanvas.composition.test.tsx src/components/workflows/canvasModel.purity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The inventory pins pass and name atoms as literals.
    - The payload-label CANNOT-EXPRESS report exists in all three parts, with a re-open trigger.
    - `Fork Logic` is recorded as REFUSED with its reason.
    - `git status --short` shows exactly the two named test files. `git diff --stat -- backend supabase` is EMPTY.
  </acceptance_criteria>
  <done>Every bullet in <acceptance_criteria> above holds, measured rather than asserted.</done>
</task>

<task type="auto">
  <name>Task 2: Re-present the connection states</name>
  <files>frontend/src/components/workflows/FlowEdge.tsx, frontend/src/components/workflows/FlowEdge.test.tsx</files>
  <read_first>
    - Task 1's connection-state rows — the work list
    - `FlowEdge.tsx`'s docblock — the transcribed `@xyflow/react` v12 baseline and why getting the baseline half wrong changes every connector in the app
  </read_first>
  <action>
Apply ONLY the connection-state rows Task 1 marked BUILT.

Binding constraints:
  - **Non-colour-only is the acceptance bar.** Assert distinguishability by stripping every `class`
    attribute off the rendered DOM and re-asserting the states remain mutually distinct — the
    `192.2-05` method for proving colour is never the sole carrier. An assertion that only reads
    class names proves nothing about what a colour-blind reader sees.
  - **The transcribed bezier baseline is load-bearing.** Any geometry change must be measured
    against the pre-change path, not eyeballed.
  - **The armed action-risk detour is a shipped semantic**, not decoration. Do not flatten it into a
    plain connector while re-presenting.
  - **Verify every colour token the sheet asks for RESOLVES** in `frontend/tailwind.config.js`
    before use — `bg-warning` compiled to nothing in this repo until Phase 192.2 added the key, and
    an unpainted class is indistinguishable from a deliberately unpainted arm.
  - Do not inherit the sheet's right-overflow: assert the plane's content is not clipped at the
    narrow viewport the canvas already supports.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/workflows/FlowEdge.test.tsx src/components/workflows/WorkflowCanvas.test.tsx && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - Every BUILT connection state is mutually distinct with all `class` attributes stripped.
    - No geometry change lands without a measured before/after.
    - `npx tsc --noEmit -p tsconfig.app.json` reports 33, ZERO under `components/workflows/`.
    - `git diff --stat -- backend supabase` is EMPTY.
  </acceptance_criteria>
  <done>Every bullet in <acceptance_criteria> above holds, measured rather than asserted.</done>
</task>

<task type="auto">
  <name>Task 3: Re-present the plane ground, the floating controls and the two empty planes</name>
  <files>frontend/src/components/workflows/WorkflowCanvas.tsx, frontend/src/components/workflows/CanvasToolbar.tsx, frontend/src/components/workflows/PlaneEditingLayer.tsx, frontend/src/components/workflows/CanvasToolbar.test.tsx, frontend/src/components/workflows/WorkflowCanvas.test.tsx, frontend/src/components/workflows/WorkflowCanvas.composition.test.tsx</files>
  <read_first>
    - Task 1's ground / controls / empty-plane rows
    - `icon-convention.md` §4 — every canvas mark must trace to a shipped row or be explicitly flagged as a proposal. There is NO category-icon vocabulary; do not invent one.
  </read_first>
  <action>
Apply ONLY the BUILT rows for the ground, the floating controls and the two empty planes.

Binding constraints:
  - **The ground must stay quieter than a step at rest.** Assert the ordering rather than asserting
    a colour value: a ground that competes with a node has inverted the sheet's own claim.
  - **The ＋ and ✕ stay on the LANE, never on the card** — one tab stop per node is a canvas-level
    invariant, and the shipped `data-testid`s are the handles.
  - **No net-new canvas mark.** Every glyph traces to a row in `icon-convention.md` §4, or it does
    not ship. A phase-type glyph may NEVER be used as a category icon.
  - **One empty-state affordance only.** The shipped invitation and the shipped read-only state each
    have exactly one home; do not add a second.
  - The read-only plane's words come from the shipped vocabulary; nothing is re-spelled here.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/workflows/WorkflowCanvas.test.tsx src/components/workflows/WorkflowCanvas.composition.test.tsx src/components/workflows/WorkflowCanvas.editing.test.tsx src/components/workflows/CanvasToolbar.test.tsx && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - The plane renders no MORE at rest than the Task-1 inventory recorded; removals proved by inversion, zero assertions deleted.
    - Every canvas mark traces to `icon-convention.md` §4.
    - `node scripts/vitest-count-gate.cjs` from the REPO ROOT with `GSD_VITEST_MAX_WORKERS=2` reports `failed 0` and no per-file decrease.
    - `git diff --stat -- backend supabase` is EMPTY.
  </acceptance_criteria>
  <done>Every bullet in <acceptance_criteria> above holds, measured rather than asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored definition → plane | step names, branch labels and edge semantics are user/LLM-authored and rendered as the reader's picture of their own process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-199-05-01 | Spoofing | connection labels | mitigate | The payload-bearing label is REPORTED, never approximated. A fabricated business figure on the canvas is the highest-consequence lie this phase could ship. |
| T-199-05-02 | Tampering | `FlowEdge.tsx` | mitigate | Authored strings stay plain React text children; the transcribed bezier baseline is changed only with a measured before/after. |
| T-199-05-03 | Elevation of privilege | `WorkflowCanvas.tsx` | mitigate | The read-only plane gains no affordance; the ＋/✕ stay on the lane and the read-only opt-out block stays off in both modes. |

No package-manager install task exists in this plan, so the Package Legitimacy Gate does not apply.
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` → 33, unmoved
- `node scripts/vitest-count-gate.cjs` from the REPO ROOT with `GSD_VITEST_MAX_WORKERS=2` → `failed 0`, no per-file decrease
- `git diff --stat -- backend supabase` → empty
- ⚠ `docs/HOT-FILE-LEDGER.md` records `WorkflowCanvas.tsx` at 23 / 6 / 1301 — G-5 FIRES, disposition `satisfied (188.1)`. Re-derive the triple before assuming the cell is current; five rows were found stale on 2026-08-17 and three of them read `satisfied`.
</verification>

<success_criteria>
- Every element of sheet c1 carries a verdict; none is silently dropped (SC#1).
- No backend, migration, endpoint or wire model modified (SC#2).
- The plane renders no more at rest than before, proved against a pre-change inventory (SC#3).
- The mechanism is not printed to the user — `Fork Logic` refused (SC#4).
- Gates hold (SC#5).
</success_criteria>

<output>
Create `.planning/phases/199-the-component-map/199-05-SUMMARY.md` when done, carrying the
reconciliation table, the payload-label CANNOT-EXPRESS report in full with its re-open trigger, and
the two refused sheet elements.
</output>
