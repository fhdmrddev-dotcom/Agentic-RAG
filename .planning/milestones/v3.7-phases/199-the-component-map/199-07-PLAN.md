---
phase: 199-the-component-map
plan: 07
type: execute
wave: 2
depends_on: ["199-02"]
files_modified:
  - frontend/src/pages/WorkflowRunPage.tsx
  - frontend/src/components/panel/WorkspacePanel.tsx
  - frontend/src/components/panel/FilesSection.tsx
  - frontend/src/components/panel/PendingAskCard.tsx
  - frontend/src/components/panel/PanelEmpty.tsx
  - frontend/src/pages/WorkflowRunPage.test.tsx
  - frontend/src/components/panel/__tests__/WorkspacePanel.test.tsx
  - frontend/src/components/panel/__tests__/FilesSection.test.tsx
  - frontend/src/components/panel/__tests__/PendingAskCard.test.tsx
autonomous: true
requirements: [DES-01]

must_haves:
  truths:
    - "The panel keeps discipline at 380px and an unknown file time SAYS so rather than blanking"
    - "The ask card reads as a question with its answer, and its resolved state is a receipt — not a form that stayed on screen"
    - "The panel change is judged against the CHAT surface first, because ChatLayout is its only mount and DocumentDetailPanel reuses its shape"
    - "The run surface renders no MORE at rest than before"
  artifacts:
    - path: "frontend/src/components/panel/WorkspacePanel.tsx"
      provides: "The cross-surface panel shell, re-presented"
    - path: "frontend/src/pages/WorkflowRunPage.tsx"
      provides: "The workflow run surface, re-presented"
  key_links:
    - from: "frontend/src/components/layout/ChatLayout.tsx"
      to: "frontend/src/components/panel/WorkspacePanel.tsx"
      via: "the ONLY mount of the panel in the application"
      pattern: "WorkspacePanel"
    - from: "frontend/src/components/panel/FilesSection.tsx"
      to: "frontend/src/components/files/fileRowUtils.ts"
      via: "the ONE formatBytes / baseName / byNewestFirst"
      pattern: "fileRowUtils|formatBytes"
---

<objective>
Re-present the live run panel and the workflow run surface in the adopted design language, against
sheet `c8-run-panel`.

Purpose: DES-01 on the surface a person watches while their work is being done. Sheet c8's own
finding is that the panel *"keeps discipline at 380px, and an unknown file time says so instead of
blanking"* — honesty under density, which is this project's house rule expressed as layout.

Output: a re-presented panel and run surface, a pre-change inventory proving the subtraction, and an
explicit statement of where the change lands FIRST.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/sketches/178-stitch-component-map/README.md
@.planning/sketches/178-stitch-component-map/sheets/c8-run-panel.html
@.claude/skills/sketch-findings-agentic-rag/references/panel-shell.md
@.claude/skills/sketch-findings-agentic-rag/references/workflow-run-surface.md
@.claude/skills/sketch-findings-agentic-rag/references/pending-question.md
@.claude/skills/sketch-findings-agentic-rag/references/file-browser-and-diff.md
@docs/HOT-FILE-LEDGER.md
</context>

<wave_rationale>
This plan depends on `199-02` for a REAL reason: `WorkspacePanel` mounts `PhaseTimeline`, which
mounts `PhaseCard` — both of which `199-02` re-presents. The spine lands first so this plan measures
the panel against its final contents rather than against a moving target. No file is shared.
</wave_rationale>

<cross_surface_warning>
⚠ **THE PANEL IS NOT A WORKFLOW COMPONENT, AND SHEET C8'S OWN TITLE IS WRONG IN A WAY THAT MATTERS.**
Sketch 178's README records the correction, checked against the code and found STRONGER than stated:

- `WorkspacePanel` is mounted by **`components/layout/ChatLayout.tsx:673`** and has **no mount in any
  workflow page**.
- `PausedRunCue` is rendered by **`components/chat/MessageItem.tsx`**.
- `panel/panelOpenSignal.ts` exists ONLY so chat-side affordances can open the panel.
- `components/metadata/DocumentDetailPanel.tsx` **reuses the WorkspacePanel sheet shape**, so the
  shell already spans a THIRD surface.

**Consequences this plan MUST honour:**
1. **A change to the panel lands in CHAT first.** The plan's SUMMARY must say so plainly, and the
   panel suites that must stay green include the chat-side ones, not only the workflow ones.
2. **Any UAT that only exercises the workflow surface will miss it.** Record that in the SUMMARY as
   an owed verification, with the chat surface named as the place to look.
3. **Do not modify `ChatLayout.tsx`, `MessageItem.tsx` or `DocumentDetailPanel.tsx`.** They are
   outside this phase's ten sheets. If the re-presentation cannot land without touching one of them,
   the task **STOPS and reports**.
</cross_surface_warning>

<scope_fence>
**PRESENTATION ONLY.** No new data, no endpoint, no migration, no wire-model change, no new
user-facing capability. `git diff` over `backend/` and `supabase/` must be EMPTY at every task.
A task that discovers it needs a backend change **STOPS and reports**.

**THE CANNOT-EXPRESS RULE (`SEED-155`).** If the shipped component cannot express the sheet, report
it — what the sheet asks for, what the component can do, and the gap. Never fake it.
</scope_fence>

<tasks>

<task type="auto">
  <name>Task 1: Inventory the panel and the run surface, and reconcile them against sheet c8</name>
  <files>frontend/src/components/panel/__tests__/WorkspacePanel.test.tsx, frontend/src/pages/WorkflowRunPage.test.tsx</files>
  <read_first>
    - `.planning/sketches/178-stitch-component-map/sheets/c8-run-panel.html` — the ask card (open and resolved), the paused cue, the files section, the file preview (csv and json), the version diff, the empty panel
    - `.planning/sketches/178-stitch-component-map/README.md` § "Sheets 4-9" and § "The panel is NOT a workflow component"
    - `frontend/src/components/panel/WorkspacePanel.tsx` (646 L) — the shell, its collapse and its mobile behaviour
    - `frontend/src/components/panel/PendingAskCard.tsx` — the mid-run interrupt; ⚠ its suite is NOT pinned in the count gate's TARGETS, so a decrease there is invisible to the gate. State that in the SUMMARY.
    - `frontend/src/components/panel/FilesSection.tsx` and `frontend/src/components/files/fileRowUtils.ts` — ⚠ a MISSING `created_at` must sort FIRST, not last; `formatBytes` / `baseName` / `byNewestFirst` have exactly ONE home
    - `frontend/src/components/files/FileRow.tsx` — ⚠ it must forward its ref to the caller-supplied child or the panel's roving focus dies silently
    - `frontend/src/pages/WorkflowRunPage.tsx` (1156 L) — the run surface; one of its five named seams was taken in Phase 195
    - `frontend/src/lib/fileIcon.tsx` — the ONE per-extension icon path
  </read_first>
  <action>
Measure before changing anything. No source byte outside the two named test files may move.

Inventory the RESTING atoms of both surfaces as literal strings — the panel empty, the panel with
files, the ask card open and resolved, the paused cue, and the run surface at rest — pinned PRESENT
so a later removal is proved by INVERSION, never by deletion.

Then reconcile against the sheet, with a verdict per element:
  - **The ask card**, open and resolved. The sheet draws a resolved state as a RECEIPT carrying its
    answer and its time, not a form left on screen. Record what the shipped card does at resolution.
  - **The paused cue.** ⚠ It renders in `MessageItem` (chat), which is out of scope. Record it as
    OUT-OF-SCOPE-BY-SURFACE rather than building it here.
  - **The files section.** ⚠ *"an unknown file time says so instead of blanking"* — the sheet draws
    `--:--`. Check what the shipped section renders for a missing time and whether a MISSING
    `created_at` still sorts FIRST. This is a genuine honesty row.
  - **The file preview**, csv and json, each with its honest end-of-preview line.
  - **The version diff.**
  - **The empty panel line.**
  - **380px discipline** — record the panel's actual width contract and whether any element the
    sheet draws would overflow it.

⚠ For every row, first determine whether the atom lives in a file this plan OWNS. Anything living in
`ChatLayout.tsx`, `MessageItem.tsx` or `DocumentDetailPanel.tsx` is OUT OF SCOPE and is recorded, not
built.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/panel/__tests__/WorkspacePanel.test.tsx src/components/panel/__tests__/WorkspacePanel.derived.test.tsx src/components/panel/__tests__/FilesSection.test.tsx src/components/panel/__tests__/PendingAskCard.test.tsx src/pages/WorkflowRunPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The inventory pins pass and name atoms as literals.
    - Every sheet element carries a verdict, including OUT-OF-SCOPE-BY-SURFACE where it applies.
    - The SUMMARY states that a panel change lands in CHAT first, and names the owed chat-surface verification.
    - `git status --short` shows exactly the two named test files. `git diff --stat -- backend supabase` is EMPTY.
  </acceptance_criteria>
  <done>Every bullet in <acceptance_criteria> above holds, measured rather than asserted.</done>
</task>

<task type="auto">
  <name>Task 2: Re-present the panel shell, the files section and the ask card</name>
  <files>frontend/src/components/panel/WorkspacePanel.tsx, frontend/src/components/panel/FilesSection.tsx, frontend/src/components/panel/PendingAskCard.tsx, frontend/src/components/panel/PanelEmpty.tsx, frontend/src/components/panel/__tests__/WorkspacePanel.test.tsx, frontend/src/components/panel/__tests__/FilesSection.test.tsx, frontend/src/components/panel/__tests__/PendingAskCard.test.tsx</files>
  <read_first>
    - Task 1's reconciliation table — the work list; anything marked OUT-OF-SCOPE-BY-SURFACE is not in it
    - `panel-shell.md` — the shipped shell/collapse/mobile contract
    - `pending-question.md` — the shipped ask/interrupt contract
  </read_first>
  <action>
Apply ONLY the BUILT rows. Hierarchy, typography, spacing, tone and removal.

Binding constraints:
  - **An unknown value SAYS it is unknown.** Never blank, never zero, never a plausible-looking
    default. This is the same three-arm discipline the library card had to learn: absent, known-none
    and known-value are three readings, not two.
  - **A MISSING `created_at` sorts FIRST**, not last — the shipped rule, and inverting it silently
    hides the newest deliverable.
  - **`FileRow` must forward its ref to the caller-supplied child**, or the panel's roving focus
    dies silently and every geometry assertion stays green.
  - **`formatBytes` / `baseName` / `byNewestFirst` and the per-extension icon path each have ONE
    home.** Do not re-spell any of them here.
  - **The ask card's resolved state must remain unambiguously resolved** — a receipt, never a live
    form. Assert the interactive controls are gone at resolution, not merely disabled.
  - **380px discipline** — assert no element the sheet adds overflows the panel width contract.
  - **Verify every colour token the sheet asks for RESOLVES** in `frontend/tailwind.config.js`.

Where the sheet removes an atom, invert the Task-1 assertion; delete none. Never re-baseline a
characterization pin to turn red green.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/panel/__tests__/ src/components/panel/PhaseCard.test.tsx src/components/panel/FilePreview.test.tsx && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - The panel renders no MORE at rest than the Task-1 inventory recorded; removals proved by inversion, zero assertions deleted.
    - An unknown file time renders a stated unknown, asserted distinct from both a known time and an absent file.
    - No file under `components/layout/`, `components/chat/` or `components/metadata/` is modified.
    - `npx tsc --noEmit -p tsconfig.app.json` reports 33, ZERO under `components/panel/`.
    - `git diff --stat -- backend supabase` is EMPTY.
  </acceptance_criteria>
  <done>Every bullet in <acceptance_criteria> above holds, measured rather than asserted.</done>
</task>

<task type="auto">
  <name>Task 3: Re-present the workflow run surface</name>
  <files>frontend/src/pages/WorkflowRunPage.tsx, frontend/src/pages/WorkflowRunPage.test.tsx</files>
  <read_first>
    - Task 1's run-surface rows
    - `workflow-run-surface.md` — the shipped contract that the PANEL owns the meaningful phase spine while chat carries a thin run receipt
    - `docs/HOT-FILE-LEDGER.md` → `frontend/src/pages/WorkflowRunPage.tsx` — one of five named seams was TAKEN in Phase 195; four remain named. Read them before adding anything to this file.
  </read_first>
  <action>
Apply ONLY the run-surface rows Task 1 marked BUILT. The direction is subtraction: the run surface's
job is to let a person watch work happen, and the shipped contract already puts the meaningful spine
in the panel.

Binding constraints:
  - **Do not duplicate the panel's spine on the page.** One home per concern; the shipped split is
    deliberate.
  - **No fabricated progress** — no percentage, no determinate step count that the run does not emit.
  - **Nothing that a person needs in order to act may move behind a click** — in particular any stop
    or interrupt affordance stays where it is and stays legible.
  - ⚠ `WorkflowRunPage.tsx` is one of `SEED-171`'s named flaky suites. If its gate run reds, capture
    the failing filenames from the gate's own persisted JSON BEFORE re-running, check the named file
    against `git diff --numstat`, and if it is byte-unchanged record it as an observation. **Do NOT
    adjust the worker cap** — that remedy is REFUTED by measurement. One green sample is not proof of
    innocence; say "provably unmodified", never "fine".
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/pages/WorkflowRunPage.test.tsx && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - The run surface renders no MORE at rest than the Task-1 inventory recorded.
    - No second phase spine exists on the page.
    - `node scripts/vitest-count-gate.cjs` from the REPO ROOT with `GSD_VITEST_MAX_WORKERS=2` reports `failed 0` and no per-file decrease.
    - Any red suite is triaged from the gate's persisted JSON with the cap untouched, and the triage is recorded.
    - `git diff --stat -- backend supabase` is EMPTY.
  </acceptance_criteria>
  <done>Every bullet in <acceptance_criteria> above holds, measured rather than asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run output files → panel | filenames, sizes and previews are produced by sandboxed execution and rendered to the user |
| mid-run ask → panel | the interrupt's question and options are model-authored and drive a human decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-199-07-01 | Spoofing | `PendingAskCard.tsx` | mitigate | The resolved state asserts its interactive controls are GONE, not merely disabled, so a resolved ask can never be re-answered or read as still open. |
| T-199-07-02 | Tampering | `FilePreview.tsx` consumers | mitigate | Preview content stays rendered as text, never as markup; no `dangerouslySetInnerHTML` is introduced into the panel subtree. |
| T-199-07-03 | Information disclosure | cross-surface shell | mitigate | The panel change is explicitly bounded to files this plan owns; `ChatLayout`, `MessageItem` and `DocumentDetailPanel` are asserted unmodified so the blast radius is stated rather than discovered. |

No package-manager install task exists in this plan, so the Package Legitimacy Gate does not apply.
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` → 33, unmoved
- `node scripts/vitest-count-gate.cjs` from the REPO ROOT with `GSD_VITEST_MAX_WORKERS=2` → `failed 0`, no per-file decrease
- `git diff --stat -- backend supabase` → empty
- `git status --short` shows no file under `components/layout/`, `components/chat/` or `components/metadata/`
- ⚠ `src/components/panel/__tests__/PendingAskCard.test.tsx` is NOT in the count gate's TARGETS — the gate cannot see a decrease there. Run it explicitly and record its count in the SUMMARY.
</verification>

<success_criteria>
- Every element of sheet c8 carries a verdict, including the out-of-scope-by-surface ones (SC#1).
- No backend, migration, endpoint or wire model modified (SC#2).
- Neither surface renders more at rest than before, proved against a pre-change inventory (SC#3).
- The mechanism is not printed to the user; unknown values say they are unknown (SC#4).
- Gates hold (SC#5).
</success_criteria>

<output>
Create `.planning/phases/199-the-component-map/199-07-SUMMARY.md` when done, carrying the
reconciliation table, the explicit statement that a panel change lands in CHAT first, the owed
chat-surface verification, and every CANNOT-EXPRESS report.
</output>
