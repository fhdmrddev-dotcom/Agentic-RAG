---
phase: 199-the-component-map
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/workflows/PhaseNodeCard.tsx
  - frontend/src/components/workflows/PhaseNode.tsx
  - frontend/src/components/workflows/NodeRunOverlay.tsx
  - frontend/src/components/workflows/NodeCornerMarks.tsx
  - frontend/src/components/workflows/NodeIconWell.tsx
  - frontend/src/components/workflows/nodePresentation.ts
  - frontend/src/components/workflows/PhaseNodeCard.test.tsx
  - frontend/src/components/workflows/PhaseNode.test.tsx
autonomous: true
requirements: [DES-01]

must_haves:
  truths:
    - "Sheet c2's six phase types, three name-length cases, six interaction states, six run states, the router node and the three zoom sizes are each recorded as BUILT, ALREADY-SHIPPED or CANNOT-EXPRESS-WITH-REASON — none is silently dropped"
    - "The node face never prints the mechanism: no rendered string names a fallback rule, a ladder rung, a phase type identifier or a gate name"
    - "The node renders no MORE at rest than it did before this plan"
    - "No file under backend/ or supabase/ is touched"
  artifacts:
    - path: "frontend/src/components/workflows/PhaseNodeCard.test.tsx"
      provides: "Pre-change resting-atom inventory as literal strings, plus the mechanism-absence sweep"
      contains: "data-testid=\"canvas-node-"
    - path: "frontend/src/components/workflows/PhaseNodeCard.tsx"
      provides: "The re-presented 137-B card face"
  key_links:
    - from: "frontend/src/components/workflows/PhaseNode.tsx"
      to: "frontend/src/components/workflows/PhaseNodeCard.tsx"
      via: "slot contract in phaseNodeCardContract.ts"
      pattern: "PhaseNodeCard"
    - from: "frontend/src/components/workflows/NodeIconWell.tsx"
      to: "frontend/src/components/workflows/soulData.ts PHASE_GLYPHS"
      via: "the ONE shared 3D phase-type glyph map"
      pattern: "phaseGlyph|PHASE_GLYPHS"
---

<objective>
Re-present the canvas phase node in the adopted design language, against sheet `c2-phase-node` —
sketch 178's own best artifact.

Purpose: DES-01 on the atom every other canvas surface is built out of. The node is where the
"never print the mechanism" rule was first broken (sketch 177 printed *"Author name" / "Derived
name"* to the user) and where 178 fixed it; this plan makes the shipped card agree with the fixed
reading and reports every state the sheet asks for that the shipped card structurally cannot render.

Output: a re-presented `PhaseNodeCard` face, a characterization pin that inventories its resting
atoms as literal strings, and a written CAN-EXPRESS / CANNOT-EXPRESS reconciliation for all six
sections of sheet c2.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/sketches/178-stitch-component-map/README.md
@.planning/sketches/178-stitch-component-map/sheets/c2-phase-node.html
@.claude/skills/sketch-findings-agentic-rag/references/canvas-frame-and-node-anatomy.md
@.claude/skills/sketch-findings-agentic-rag/references/node-vocabulary-and-reveal.md
@.claude/skills/sketch-findings-agentic-rag/references/icon-convention.md
@frontend/src/components/workflows/PhaseNodeCard.tsx
@frontend/src/components/workflows/phaseNodeCardContract.ts
</context>

<scope_fence>
**PRESENTATION ONLY.** No new data, no endpoint, no migration, no wire-model change, no new
user-facing capability. `git diff` over `backend/` and `supabase/` must be EMPTY at every task.
A task that discovers it needs a backend change **STOPS and reports** — it does not grow one.

**THE CANNOT-EXPRESS RULE (replaces the skipped step-3 sketch — `SEED-155`).** Sheet 178 renders
ZERO shipped components; every sheet is drawn from scratch. **If the shipped component cannot
express the sheet, report it** — what the sheet asks for, what the component can do, and the gap.
Never fake it, never approximate it silently, never grow a backend change to make it possible.
</scope_fence>

<tasks>

<task type="auto">
  <name>Task 1: Inventory the resting node and reconcile it against every section of sheet c2</name>
  <files>frontend/src/components/workflows/PhaseNodeCard.test.tsx</files>
  <read_first>
    - `.planning/sketches/178-stitch-component-map/sheets/c2-phase-node.html` — all six sections
    - `.planning/sketches/178-stitch-component-map/README.md` § "Sheet 2 · The phase node" — the honest read, including WHY the invisible name ladder is the correct reading
    - `frontend/src/components/workflows/PhaseNodeCard.tsx` (280 L) and `phaseNodeCardContract.ts` — the slot contract and the max-2 badge tuple
    - `frontend/src/components/workflows/PhaseNode.tsx` — the graph-library adapter that builds the slots
    - `frontend/src/components/workflows/NodeRunOverlay.tsx`, `NodeCornerMarks.tsx`, `NodeIconWell.tsx`
    - `frontend/src/components/workflows/runVocabulary.ts` — the reading set and `runReadingLabel`
    - `frontend/src/components/workflows/phaseVocabulary.ts` — `nodeTitle` ladder and `PHASE_TYPE_SENTENCES`
    - the skill references `canvas-frame-and-node-anatomy.md` and `node-vocabulary-and-reveal.md` — the 137-B geometry, the one-tab-stop-per-node invariant, the "never take overflow:hidden" rule
  </read_first>
  <action>
Measure before changing anything. No source byte under `src/` outside the named test file may move
in this task.

First, enumerate the node's RESTING atoms — every string, mark and element the card paints when a
Builder renders it with no reading supplied — and pin each as a literal in `PhaseNodeCard.test.tsx`.
Follow the `192.2-05` method exactly: an atom is asserted PRESENT now so that a later removal can be
proved by INVERTING the assertion to ABSENT, never by deleting it. Do the same for the run-mode face
at every reading `runVocabulary` defines.

Second, add a mechanism-absence sweep over the rendered card: assert that no rendered text matches
the mechanism vocabulary the sheet's own honest read names as the failure — the words for a fallback
rule, a ladder rung, a derived-name explanation, a raw phase-type discriminator or a gate identifier.
Drive it RED once against a deliberate plant, then restore, so the sweep is falsifiable rather than
vacuous. This project has measured three fences that swept the empty string and passed green
defending nothing; assert non-vacuity (that the swept text is non-empty) BEFORE asserting contents.

Third, write the reconciliation table into the plan's SUMMARY, one row per sheet section, each row
carrying a verdict of BUILT-IN-TASK-2 / ALREADY-SHIPPED / CANNOT-EXPRESS with its reason:
  - §1 the six types — check all six `PHASE_GLYPHS` entries resolve to a real mark; the icon
    convention forbids re-declaring the map and forbids a phase-type glyph used as a category icon.
  - §2 the three name-length cases — the ladder must stay INVISIBLE. Record whether the shipped
    `nodeTitle` ladder already satisfies this (it is expected to; do not rebuild it).
  - §3 the six interaction states — rest, hovered, selected, dragging, configuration problem,
    locked. Record which of these the shipped card and its adapter can already express from data
    that already reaches them. ⚠ "configuration problem" and "locked" are likely to require state
    that does not exist; if so they are CANNOT-EXPRESS, not a licence to invent a permission model.
  - §4 the six run states — map each to the shipped `runVocabulary` reading. ⚠ The sheet's
    succeeded node carries a kept fact (*"Found 12 items"*); nothing emits a per-step result count,
    so that is CANNOT-EXPRESS — record it, do not build it.
  - §5 the hexagonal router node — the spine is LINEAR by construction (no `depends_on`, no
    parallel lanes, no free DAG). A router node type does not exist. Record CANNOT-EXPRESS naming
    the linear commitment as the reason.
  - §6 the three zoom sizes — record whether the card stays readable at the compact and micro
    zoom levels the canvas already offers. Do NOT edit `CanvasToolbar.tsx`; it belongs to plan 05.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/workflows/PhaseNodeCard.test.tsx src/components/workflows/PhaseNode.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The inventory pin passes and names every resting atom as a literal, not a shape.
    - The mechanism-absence sweep has been driven RED against a plant and restored; the plant and its
      restore are recorded in the SUMMARY.
    - `git status --short` shows exactly one modified file, `PhaseNodeCard.test.tsx`.
    - `git diff --stat -- backend supabase` is EMPTY.
    - The six-row reconciliation table exists with a verdict and a reason on every row.
  </acceptance_criteria>
  <done>Every bullet in <acceptance_criteria> above holds, measured rather than asserted.</done>
</task>

<task type="auto">
  <name>Task 2: Re-present the node face — apply only the deltas the shipped card can express</name>
  <files>frontend/src/components/workflows/PhaseNodeCard.tsx, frontend/src/components/workflows/PhaseNode.tsx, frontend/src/components/workflows/NodeRunOverlay.tsx, frontend/src/components/workflows/NodeCornerMarks.tsx, frontend/src/components/workflows/NodeIconWell.tsx, frontend/src/components/workflows/nodePresentation.ts, frontend/src/components/workflows/PhaseNodeCard.test.tsx, frontend/src/components/workflows/PhaseNode.test.tsx</files>
  <read_first>
    - Task 1's reconciliation table — it is the work list; nothing outside it may be built
    - `PhaseNodeCard.tsx`'s docblock — the four-branch border precedence (run state > selected >
      grounded > default), the mutually-exclusive-BY-SURFACE argument, and the "one tab stop per
      node" invariant
    - `canvas-frame-and-node-anatomy.md` § "What to Avoid" — never wash the card in type colour,
      never add a third badge, never take `overflow: hidden`, never key motion off selection
  </read_first>
  <action>
Apply ONLY the rows Task 1 marked BUILT-IN-TASK-2. Composition, typography, spacing, tone and
removal — nothing else. Where the sheet REMOVES an atom, invert its Task-1 assertion from present to
absent; never delete an assertion, and never re-baseline a pin to make red go green.

Binding constraints, all of which are shipped invariants rather than preferences:
  - The badge budget is FULL (max-2 tuple union). A third badge is a typecheck error; do not try.
  - No focusable control may enter the card. One tab stop per node is a canvas-level invariant and
    is asserted at two levels.
  - Type colour is a tint behind the icon well ONLY, never a card wash — the strong colours belong
    to run status.
  - Motion keys off RUN STATE, never off selection.
  - Nothing in the node subtree may take `overflow: hidden` — the 3D mark and the verdict both rely
    on upward overflow.
  - Any colour token the sheet asks for must be verified to RESOLVE in `frontend/tailwind.config.js`
    before it is used. This project measured `bg-warning` compiling to nothing because the config
    had no `warning` key; a class that compiles to nothing renders identically to the arm that is
    deliberately unpainted.
  - Business words are IMPORTED from `runVocabulary.ts` / `phaseVocabulary.ts`, never re-spelled
    here. A second copy of a word is the defect, not the fix.

For every CANNOT-EXPRESS row, write the report into the SUMMARY in the required three parts — what
the sheet asks for, what the component can do, and the gap. Do not leave a row implied.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/workflows/PhaseNodeCard.test.tsx src/components/workflows/PhaseNode.test.tsx src/components/workflows/WorkflowCanvas.test.tsx && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - Every BUILT row is applied; every CANNOT-EXPRESS row is reported with all three parts.
    - Any removed atom is proved by an INVERTED Task-1 assertion; the count of deleted assertions is ZERO.
    - The card renders no MORE at rest than the Task-1 inventory recorded.
    - `npx tsc --noEmit -p tsconfig.app.json` reports 33 errors, the pre-existing baseline, with ZERO under `components/workflows/`.
    - `node scripts/vitest-count-gate.cjs` (from the REPO ROOT, with `GSD_VITEST_MAX_WORKERS=2`) reports `failed 0` and no per-file DECREASE.
    - `git diff --stat -- backend supabase` is EMPTY.
  </acceptance_criteria>
  <done>Every bullet in <acceptance_criteria> above holds, measured rather than asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored workflow content → node face | phase names, subtitles and badge labels are user-authored strings rendered on the canvas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-199-01-01 | Tampering | `PhaseNodeCard.tsx` | mitigate | Every authored string stays a plain React text child; no `dangerouslySetInnerHTML` is introduced. The shipped grep guard is anchored on the JSX PROP FORM and must still pass. |
| T-199-01-02 | Information disclosure | node face | mitigate | The mechanism-absence sweep (Task 1) doubles as the guard that no internal identifier, gate name or rule name reaches the face. |
| T-199-01-03 | Elevation of privilege | scope fence | mitigate | `git diff --stat -- backend supabase` asserted EMPTY at every task; a presentation plan that grows a data axis has left its envelope. |

No package-manager install task exists in this plan, so the Package Legitimacy Gate does not apply.
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` → 33 errors, zero under `components/workflows/`
- `node scripts/vitest-count-gate.cjs` from the REPO ROOT with `GSD_VITEST_MAX_WORKERS=2` → `failed 0`, no per-file decrease. ⚠ Never run it as `cd frontend && … | tail`; that form makes `MODULE_NOT_FOUND` exit 0 and a broken invocation reads as a green gate.
- `git diff --stat -- backend supabase` → empty
- If a gate run reds, capture the failing filenames from the gate's own persisted JSON BEFORE re-running, check each against `git diff --numstat`, and do NOT adjust the worker cap (SEED-171 refutes the cap as the remedy).
</verification>

<success_criteria>
- All six sections of sheet c2 carry a verdict; none is silently dropped (ROADMAP SC#1).
- No backend, migration, endpoint or wire model modified (ROADMAP SC#2).
- The node renders no more at rest than before, proved against the Task-1 inventory (ROADMAP SC#3).
- The mechanism is not printed to the user (ROADMAP SC#4), proved by a sweep that has been driven RED.
- Gates hold: `tsc` unmoved, count gate `failed 0`, touched suites green (ROADMAP SC#5).
</success_criteria>

<output>
Create `.planning/phases/199-the-component-map/199-01-SUMMARY.md` when done, carrying the six-row
reconciliation table and every CANNOT-EXPRESS report in full.
</output>
