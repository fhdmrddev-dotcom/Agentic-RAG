---
phase: 199-the-component-map
plan: 06
type: execute
wave: 2
depends_on: ["199-01"]
files_modified:
  - frontend/src/components/workflows/PhaseFormPanel.tsx
  - frontend/src/components/workflows/GovernanceSection.tsx
  - frontend/src/components/workflows/ExternalActionSection.tsx
  - frontend/src/components/workflows/TemplateAttachSection.tsx
  - frontend/src/components/workflows/ModelField.tsx
  - frontend/src/components/workflows/PhaseFormPanel.test.tsx
  - frontend/src/components/workflows/PhaseFormPanel.rails.test.tsx
  - frontend/src/components/workflows/ModelField.test.tsx
  - frontend/src/components/workflows/GovernanceSection.test.tsx
autonomous: true
requirements: [DES-01]

must_haves:
  truths:
    - "The panel has a density ceiling: a collapsed-to-essentials reading and a fully-open one, with the always-visible per-field helper prose no longer printed under every label at once"
    - "The grounding control still visibly REFUSES rather than silently disabling — the detected-and-one-way dial is untouched in behaviour"
    - "The model picker can say it could not read the registry instead of showing an empty dropdown"
    - "Nothing a person needs in order to decide moved behind a click they have no reason to make"
  artifacts:
    - path: "frontend/src/components/workflows/PhaseFormPanel.tsx"
      provides: "The 400px form panel with a density ceiling"
    - path: "frontend/src/components/workflows/ModelField.tsx"
      provides: "The registry-backed picker, now able to express a failed read"
  key_links:
    - from: "frontend/src/components/workflows/ModelField.tsx"
      to: "frontend/src/hooks/useModelRegistry.ts"
      via: "the author-registry fetch — a failed read is status 'failed', NEVER an empty success"
      pattern: "useModelRegistry|failed"
    - from: "frontend/src/components/workflows/PhaseFormPanel.tsx"
      to: "frontend/src/components/workflows/GovernanceSection.tsx"
      via: "the graded-governance dial section"
      pattern: "GovernanceSection"
---

<objective>
Re-present the phase form panel in the adopted design language, against sheet `c4-phase-form-panel`.

Purpose: DES-01's clearest win. Sheet c4 *"holds a real density ceiling: collapsed-to-essentials and
fully-open"*, and this panel is where `SEED-184`'s complaint is most literally true — the shipped
form renders *"a muted, always-visible one-line plain-English helper sentence directly under EVERY
field label"*, on top of a hover ⓘ that already carries the technical term. Everything at one
visual level, all expanded, all at once.

Output: a panel with a density ceiling, a model picker that can say it could not read the registry,
and a pre-change inventory that proves the subtraction rather than asserting it.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/sketches/178-stitch-component-map/README.md
@.planning/sketches/178-stitch-component-map/sheets/c4-phase-form-panel.html
@.planning/seeds/SEED-184-information-is-dumped-as-text-not-presented.md
@.claude/skills/sketch-findings-agentic-rag/references/graded-governance.md
@.claude/skills/sketch-findings-agentic-rag/references/model-registry-and-discovery.md
@.claude/skills/sketch-findings-agentic-rag/references/workflow-authoring.md
@docs/HOT-FILE-LEDGER.md
</context>

<wave_rationale>
This plan depends on `199-01` for an ORDERING reason, not a code one: **the panel is where you SET
and the canvas is where you SEE.** The node's governance seal and this panel's grounding dial are two
renderings of one fact, so the node lands first and this plan is judged against it. No file is shared.
</wave_rationale>

<scope_fence>
**PRESENTATION ONLY.** No new data, no endpoint, no migration, no wire-model change, no new
user-facing capability. `git diff` over `backend/` and `supabase/` must be EMPTY at every task.
A task that discovers it needs a backend change **STOPS and reports**.

**THE CANNOT-EXPRESS RULE (`SEED-155`).** If the shipped component cannot express the sheet, report
it — what the sheet asks for, what the component can do, and the gap. Never fake it.
</scope_fence>

<tasks>

<task type="auto">
  <name>Task 1: Inventory the panel at full density and reconcile it against sheet c4</name>
  <files>frontend/src/components/workflows/PhaseFormPanel.test.tsx</files>
  <read_first>
    - `.planning/sketches/178-stitch-component-map/sheets/c4-phase-form-panel.html` — COLLAPSED TO ESSENTIALS vs FULLY EXPANDED / HIGH DENSITY, the grounding states, the armed action, the templates block and the issues summary
    - `.planning/sketches/178-stitch-component-map/README.md` § "Sheets 4-9" — *"a real density ceiling … the grounding control drawn as something that visibly refuses, and a model picker that can say 'I could not read the registry' rather than showing an empty dropdown"*
    - `frontend/src/components/workflows/PhaseFormPanel.tsx` (1216 L) — ⚠ the always-visible helper sentence under EVERY field label, the hover ⓘ carrying the technical term, the six-form per-phase-type conditioning and its locked invariants, the 400px grid track that PUSHES and never overlays, and the 44px rail at rest
    - `frontend/src/components/workflows/GovernanceSection.tsx` — the detected-and-one-way grounding dial: the switch that visibly refuses, and *"you can only undo a lock you created"*
    - `frontend/src/components/workflows/ModelField.tsx` — ⚠ the ledger records that it CANNOT express *"I could not read the registry"* today
    - `frontend/src/hooks/useModelRegistry.ts` — ⚠ a failed read is `status: "failed"`, NEVER an empty success. The data already exists; only its rendering is missing.
    - `frontend/src/components/workflows/modelFitness.ts` — own-property lookup, **never `?? floor`** (a coalesce returns `constructor`)
    - `.planning/seeds/SEED-184-*.md` § "Constraints a sketch must honour" — rule 3
  </read_first>
  <action>
Measure before changing anything. No source byte outside the named test file may move.

Inventory the panel's RESTING atoms as literal strings, per phase type, at the panel's current full
density — every label, every always-visible helper sentence, every hint, every section heading —
pinned PRESENT so a later removal is proved by INVERSION, never by deletion. Record the rendered
HEIGHT of the panel body for at least two representative phase types, so "the panel got quieter" is
a measured delta rather than a feeling.

Then reconcile against the sheet, with a verdict per element:
  - **The density ceiling.** Identify exactly which atoms belong to COLLAPSED-TO-ESSENTIALS and
    which to FULLY-OPEN. The always-visible helper sentence is the primary candidate for the open
    reading, because the hover ⓘ already carries the technical term and the label already carries
    the plain name — a label and a helper saying the same thing is precisely the "text is noise"
    case. ⚠ **Rule 3 fence:** the governance state, the strict/loose door, the armed-action state
    and the deliverable may NEVER move behind a click. Enumerate the atoms that are FENCED IN before
    deciding what moves.
  - **The grounding control that visibly refuses** — record whether the shipped dial already does
    this (it is expected to; do not rebuild it) and whether the refusal is legible at the collapsed
    reading.
  - **The model picker's failed-read arm** — confirm `useModelRegistry` already returns a failed
    status and that `ModelField` currently has no arm for it. This is a BUILT row: rendering an
    existing status is presentation, not new data.
  - **The armed action's consequence line** (*"Will overwrite 1,200 records"*) — ⚠ nothing emits a
    record count. CANNOT-EXPRESS: record it in three parts. The armed state itself already ships and
    stays.
  - **The templates block** — none attached / attached / corrupt. Record which arms exist today.
  - **The issues summary** (*"2 ISSUES DETECTED · Select a model · Connect a knowledge source"*) —
    check whether the shipped problems surface already carries this and, if so, record it as
    ALREADY-SHIPPED-ELSEWHERE so this panel does not grow a second copy. ⚠ The problems tray belongs
    to plan 199-09; do not build a duplicate here.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/workflows/PhaseFormPanel.test.tsx src/components/workflows/PhaseFormPanel.rails.test.tsx src/components/workflows/ModelField.test.tsx src/components/workflows/GovernanceSection.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The inventory pin passes, names atoms as literals per phase type, and records two panel-body heights as numbers.
    - The FENCED-IN atom list is written down BEFORE any decision about what moves behind a fold.
    - The armed-consequence CANNOT-EXPRESS report exists in all three parts.
    - `git status --short` shows exactly the one named test file. `git diff --stat -- backend supabase` is EMPTY.
  </acceptance_criteria>
  <done>Every bullet in <acceptance_criteria> above holds, measured rather than asserted.</done>
</task>

<task type="auto">
  <name>Task 2: Give the panel its density ceiling</name>
  <files>frontend/src/components/workflows/PhaseFormPanel.tsx, frontend/src/components/workflows/TemplateAttachSection.tsx, frontend/src/components/workflows/ExternalActionSection.tsx, frontend/src/components/workflows/PhaseFormPanel.test.tsx, frontend/src/components/workflows/PhaseFormPanel.rails.test.tsx</files>
  <read_first>
    - Task 1's FENCED-IN atom list and its collapsed/open split — the work list
    - `PhaseFormPanel.tsx`'s locked invariants — the six-form conditioning, and that `programmatic` / `llm_human_input` show NO model, tools or scope
  </read_first>
  <action>
Apply the collapsed/open split. This is composition and disclosure — no field is removed from the
form, no field changes what it writes, and no phase-type conditioning changes.

Binding constraints:
  - **Every FENCED-IN atom stays at the collapsed reading.** Governance state, strict/loose door,
    armed-action state and the deliverable are decisions; a person cannot be asked to click to find
    out whether their step is governed.
  - **The panel stays a grid track that PUSHES, never an overlay**, and stays layout-neutral about
    the mobile bottom-sheet the parent owns.
  - **The 44px rail at rest is unchanged.**
  - **The hover ⓘ technical term stays reachable** — it is the ⌥-style power-user affordance, not
    the thing being cut. What is being cut is the DUPLICATION between a plain label and a helper
    sentence restating it.
  - **Nothing is re-spelled.** All copy comes from its existing home.
  - **Verify every colour token the sheet asks for RESOLVES** in `frontend/tailwind.config.js`.

Prove the subtraction: invert each removed atom's Task-1 assertion from present to absent, delete
none, and publish the panel-body height delta for both representative phase types.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/workflows/PhaseFormPanel.test.tsx src/components/workflows/PhaseFormPanel.rails.test.tsx src/components/workflows/TemplateAttachSection.test.tsx src/components/workflows/ExternalActionSection.test.tsx && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - Every FENCED-IN atom is asserted PRESENT at the collapsed reading.
    - Every removed atom is proved by an INVERTED assertion; zero assertions deleted.
    - The panel-body height delta is published for both phase types and is negative.
    - No field's write path, and no phase-type conditioning, changed.
    - `npx tsc --noEmit -p tsconfig.app.json` reports 33, ZERO under `components/workflows/`.
    - `git diff --stat -- backend supabase` is EMPTY.
  </acceptance_criteria>
  <done>Every bullet in <acceptance_criteria> above holds, measured rather than asserted.</done>
</task>

<task type="auto">
  <name>Task 3: Let the model picker say it could not read the registry</name>
  <files>frontend/src/components/workflows/ModelField.tsx, frontend/src/components/workflows/GovernanceSection.tsx, frontend/src/components/workflows/ModelField.test.tsx, frontend/src/components/workflows/GovernanceSection.test.tsx</files>
  <read_first>
    - `frontend/src/hooks/useModelRegistry.ts` — the `failed` status and what it carries
    - `frontend/src/components/workflows/ModelField.tsx` — ⚠ it is mounted FOUR times; a change here lands in four places
    - `frontend/src/components/workflows/modelFitness.ts` — the tier→words vocabulary; own-property lookup, never `?? floor`
  </read_first>
  <action>
Render the registry's already-shipped `failed` status as a legible refusal instead of an empty
dropdown. The sheet's words are *"Failed to read registry"*; the shipped words must come from a
vocabulary module, not be typed into the component — if no home exists, put them in the module the
picker's other words already come from rather than creating a fourth vocabulary.

⚠ **An empty success and a failed read must be DIFFERENT readings.** A registry that returned zero
models is not a registry that could not be read, and collapsing them re-ships the exact class of lie
this project has recorded three times (`?? false`, `TABLE[key] ?? fallback`, the two-armed unknown).
Compare against the explicit status, never against truthiness or emptiness.

Then confirm the grounding dial's REFUSAL is still legible after Task 2's density change: a control
that visibly refuses must not have become a control that is merely disabled. Assert the refusal
sentence is present and reachable at the collapsed reading.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/workflows/ModelField.test.tsx src/components/workflows/modelFitness.test.ts src/components/workflows/GovernanceSection.test.tsx src/components/workflows/PhaseFormPanel.test.tsx && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - A failed registry read and an empty successful read render DIFFERENT readings, asserted against the explicit status by a matrix that reaches both.
    - The picker's words are imported, not re-spelled; no fourth vocabulary module is created.
    - The grounding refusal is asserted present and reachable at the collapsed reading.
    - `node scripts/vitest-count-gate.cjs` from the REPO ROOT with `GSD_VITEST_MAX_WORKERS=2` reports `failed 0` and no per-file decrease.
    - `git diff --stat -- backend supabase` is EMPTY.
  </acceptance_criteria>
  <done>Every bullet in <acceptance_criteria> above holds, measured rather than asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model registry (server) → picker | the offerable model set arrives from the registry endpoint and drives an authoring choice |
| authored config → panel | prompts, folder scopes and template names are user-authored and rendered back |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-199-06-01 | Spoofing | `ModelField.tsx` | mitigate | A failed registry read may never render as an empty-but-fine picker; the two readings are asserted distinct against the explicit status. |
| T-199-06-02 | Elevation of privilege | `GovernanceSection.tsx` | mitigate | The density change may not weaken the one-way grounding dial; the visible refusal is asserted at the collapsed reading. A refusal that degrades to a disabled control is a governance regression. |
| T-199-06-03 | Information disclosure | disclosure split | mitigate | The FENCED-IN atom list is written before anything moves, so no governance or deliverable state can end up behind a fold. |

No package-manager install task exists in this plan, so the Package Legitimacy Gate does not apply.
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` → 33, unmoved
- `node scripts/vitest-count-gate.cjs` from the REPO ROOT with `GSD_VITEST_MAX_WORKERS=2` → `failed 0`, no per-file decrease
- `git diff --stat -- backend supabase` → empty
- ⚠ `docs/HOT-FILE-LEDGER.md` records `PhaseFormPanel.tsx` at 19 / 9 / 1216 — G-5 FIRES, honoured by construction ×3. Re-derive the triple before trusting the cell.
</verification>

<success_criteria>
- Every element of sheet c4 carries a verdict; none is silently dropped (SC#1).
- No backend, migration, endpoint or wire model modified (SC#2).
- The panel renders LESS at rest than before, proved by a measured height delta (SC#3).
- The mechanism is not printed to the user; the ⓘ technical term stays an opt-in, not a default (SC#4).
- Gates hold (SC#5).
</success_criteria>

<output>
Create `.planning/phases/199-the-component-map/199-06-SUMMARY.md` when done, carrying the
reconciliation table, the FENCED-IN atom list, the measured height deltas, and every
CANNOT-EXPRESS report.
</output>
