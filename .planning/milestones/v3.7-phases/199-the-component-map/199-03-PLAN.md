---
phase: 199-the-component-map
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/workflows/PublishGauntlet.tsx
  - frontend/src/components/workflows/WorkflowSoul.tsx
  - frontend/src/components/workflows/PhaseSpine.tsx
  - frontend/src/components/workflows/soulData.ts
  - frontend/src/components/workflows/PublishGauntlet.test.tsx
  - frontend/src/components/workflows/WorkflowSoul.test.tsx
  - frontend/src/components/workflows/PhaseSpine.test.tsx
autonomous: true
requirements: [DES-01]

must_haves:
  truths:
    - "The gauntlet reads as ONE compact strip with five distinguishable outcomes: at rest, in progress (indeterminate), success, judge failure (fatal), mechanical failure (recoverable)"
    - "The judge wall carries NO override control anywhere, because there is no override"
    - "The soul's three strictness arms keep their SHIPPED derived meaning; the sheet's `UNDETERMINED` is not adopted as a re-label for a real MIDDLE band"
    - "Neither the gauntlet nor the soul renders MORE at rest than before"
  artifacts:
    - path: "frontend/src/components/workflows/PublishGauntlet.tsx"
      provides: "The publish gauntlet, re-presented as one compact strip"
    - path: "frontend/src/components/workflows/WorkflowSoul.tsx"
      provides: "The five-atom soul at run and pub scale, re-presented"
  key_links:
    - from: "frontend/src/components/workflows/WorkflowSoul.tsx"
      to: "frontend/src/components/workflows/deriveTier.ts"
      via: "tierForDefinition — the ONE strictness derivation, computed every render, never stored"
      pattern: "tierForDefinition|deriveTier"
    - from: "frontend/src/components/workflows/WorkflowSoul.tsx"
      to: "frontend/src/components/workflows/PhaseSpine.tsx"
      via: "atom 3, the glyph-dot spine"
      pattern: "PhaseSpine"
---

<objective>
Re-present the publish gauntlet and the workflow soul in the adopted design language, against sheet
`c7-gauntlet-soul`.

Purpose: DES-01 on the two surfaces that state a workflow's IDENTITY and its RIGHT TO SHIP. The
gauntlet is the project's hardest honesty surface — its judge wall has no override and must not
grow one — and the soul is the atom every other workflow surface quotes.

Output: a gauntlet re-presented as one compact strip with a worded verdict and raw detail on demand,
a soul whose three strictness arms keep their shipped derived meaning, and a pre-change inventory
proving whatever the re-presentation subtracts.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/sketches/178-stitch-component-map/README.md
@.planning/sketches/178-stitch-component-map/sheets/c7-gauntlet-soul.html
@.claude/skills/sketch-findings-agentic-rag/references/publish-gauntlet.md
@.claude/skills/sketch-findings-agentic-rag/references/workflow-soul-and-two-doors.md
@.claude/skills/sketch-findings-agentic-rag/references/workflow-energized-reskin.md
@frontend/src/components/workflows/WorkflowSoul.tsx
</context>

<planner_correction>
⚠ **`PhaseSpine.tsx` belongs to THIS plan, not to plan 02.** The ROADMAP maps it to the `c3` spine
sheet; it is in fact the SOUL's horizontal glyph-dot spine — atom 3 of `WorkflowSoul` — and its own
docblock carries a `G-5 RED LINE` forbidding it from touching the run-surface timeline. It is
grouped here so the soul and its spine change in ONE plan and can never disagree.

⚠ **`PhaseSpine` no longer renders at `scale="card"` on the library card** — Phase 192.2 removed
`<WorkflowSoul scale="card" />` from `WorkflowCard.tsx` as its D-03 subtraction. Its live consumers
are `scale="run"` and `scale="pub"` only. Verify this before assuming a card regression is possible.
</planner_correction>

<scope_fence>
**PRESENTATION ONLY.** No new data, no endpoint, no migration, no wire-model change, no new
user-facing capability. `git diff` over `backend/` and `supabase/` must be EMPTY at every task.
A task that discovers it needs a backend change **STOPS and reports**.

**THE CANNOT-EXPRESS RULE (`SEED-155`).** If the shipped component cannot express the sheet, report
it — what the sheet asks for, what the component can do, and the gap. Never fake it, never
approximate it silently.
</scope_fence>

<tasks>

<task type="auto">
  <name>Task 1: Inventory the gauntlet and the soul, and reconcile them against sheet c7</name>
  <files>frontend/src/components/workflows/PublishGauntlet.test.tsx, frontend/src/components/workflows/WorkflowSoul.test.tsx</files>
  <read_first>
    - `.planning/sketches/178-stitch-component-map/sheets/c7-gauntlet-soul.html` — the five gauntlet states and the three soul arms
    - `.planning/sketches/178-stitch-component-map/README.md` § "Sheets 4-9" — *"the gauntlet is one compact strip, indeterminate while running, with the judge's wall carrying NO override control anywhere because there is no override"*
    - `frontend/src/components/workflows/PublishGauntlet.tsx` (1000 L) — the shipped energized pip/energy-spine, the worded verdict, the raw-on-demand disclosure and the modal shell contract it shares with `RunModal`
    - `frontend/src/components/workflows/WorkflowSoul.tsx` — the LOCKED 046-A five-atom order with purpose as the hero at every size, and the two honest empty-states
    - `frontend/src/components/workflows/deriveTier.ts` — ⚠ `TierId` is `STRICT | MIDDLE | LOOSE`, derived on EVERY render from real enums; there is no stored strictness label
    - `frontend/src/components/workflows/soulData.ts` and `PhaseSpine.tsx`
    - the skill reference `publish-gauntlet.md` — the judge hard-wall and the shipped stage vocabulary
  </read_first>
  <action>
Measure before changing anything. No source byte outside the two named test files may move.

Inventory the RESTING atoms of both surfaces as literal strings — the gauntlet in each of its five
states, and the soul at `run` and `pub` scale including both honest empty-states — pinned PRESENT so
a later removal is proved by INVERSION (the `192.2-05` method), never by deleting an assertion.

Then reconcile against the sheet, with a verdict per element:
  - **The gauntlet is ONE compact strip.** Measure what the shipped gauntlet renders at rest today
    and record whether it is already one strip or several bands. The direction is subtraction.
  - **In progress is INDETERMINATE.** Assert no determinate percentage or stage count is rendered
    while running. If one is, that is a BUILT row in the removal direction.
  - **The two failure kinds are distinguishable**: judge failure is FATAL, mechanical failure is
    RECOVERABLE. Check the shipped verdict vocabulary already separates them in WORDS, not only in
    tone. Words are IMPORTED from the shipped vocabulary module, never re-spelled.
  - ⚠ **The judge wall carries NO override control.** Assert the absence over the whole rendered
    judge-failure state: no button, no link, no menu item offering to proceed. Drive this assertion
    RED once against a planted control, then restore, so it is falsifiable rather than vacuous.
  - **Raw detail on demand** — the sheet's *View Details*. Record whether the shipped raw-on-demand
    disclosure already satisfies it; do not build a second one.
  - ⚠ **The soul's third arm.** The sheet draws `STRICT / LOOSE / UNDETERMINED`. The shipped third
    arm is `MIDDLE` — a REAL derived band, not an unknown. Record this as a sheet-vs-code semantic
    disagreement: adopting `UNDETERMINED` would print a false claim (*we could not tell*) about a
    value the code CAN tell. **Do not adopt the sheet's word.** Record the decision.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/workflows/PublishGauntlet.test.tsx src/components/workflows/WorkflowSoul.test.tsx src/components/workflows/PhaseSpine.test.tsx src/components/workflows/deriveTier.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both inventories pass and name atoms as literals.
    - The no-override assertion has been driven RED against a plant and restored; both recorded.
    - The `UNDETERMINED`-vs-`MIDDLE` disagreement is recorded as a decision with its reason.
    - `git status --short` shows exactly the two named test files. `git diff --stat -- backend supabase` is EMPTY.
  </acceptance_criteria>
  <done>Every bullet in <acceptance_criteria> above holds, measured rather than asserted.</done>
</task>

<task type="auto">
  <name>Task 2: Re-present the gauntlet strip and the soul</name>
  <files>frontend/src/components/workflows/PublishGauntlet.tsx, frontend/src/components/workflows/WorkflowSoul.tsx, frontend/src/components/workflows/PhaseSpine.tsx, frontend/src/components/workflows/soulData.ts, frontend/src/components/workflows/PublishGauntlet.test.tsx, frontend/src/components/workflows/WorkflowSoul.test.tsx, frontend/src/components/workflows/PhaseSpine.test.tsx</files>
  <read_first>
    - Task 1's reconciliation table — the work list; nothing outside it may be built
    - `WorkflowSoul.tsx`'s docblock — the LOCKED 046-A atom ORDER and the rule that `scale` tunes layout and typography ONLY, so the three sizes can never render disagreeing tiers, glyphs or outputs
  </read_first>
  <action>
Apply ONLY the BUILT rows. Hierarchy, typography, spacing, tone and removal.

Binding constraints:
  - **The soul's five atoms and their ORDER are locked**, with PURPOSE as the hero at every size.
    `scale` tunes layout and typography only; the DATA and the DERIVATION stay identical across
    sizes, so `run` and `pub` can never disagree.
  - **Both honest empty-states stay rendered** — the atom is always present, never hidden, never a
    fabricated value. A re-presentation that hides an empty-state trades one bad experience for a
    worse one.
  - **The strictness tier is DERIVED every render** from real enums. Do not store it, do not
    re-label it, do not add a fourth arm.
  - **The judge wall gains no override**, in any state, at any breakpoint.
  - **No determinate progress while running.**
  - **The phase-type glyphs come from the ONE shared 3D `PHASE_GLYPHS` map.** Do not re-declare it,
    and never use a phase-type glyph as a category icon — `icon-convention.md` §4 forbids it by name.
  - **Verify every colour token the sheet asks for RESOLVES** in `frontend/tailwind.config.js`. A
    class that compiles to nothing renders identically to a deliberately unpainted arm.
  - Business words are IMPORTED from the shipped vocabulary modules, never re-spelled here.

Where the sheet removes an atom, invert the Task-1 assertion from present to absent; delete none.
Never re-baseline a characterization pin to turn red green.

For every CANNOT-EXPRESS row, write the three-part report into the SUMMARY.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/workflows/PublishGauntlet.test.tsx src/components/workflows/WorkflowSoul.test.tsx src/components/workflows/PhaseSpine.test.tsx src/components/workflows/soulData.test.ts && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - Neither surface renders MORE at rest than the Task-1 inventory recorded.
    - Zero assertions deleted; every removal proved by inversion.
    - The no-override assertion still passes over the judge-failure state.
    - The soul's atom order and both empty-states are unchanged in KIND.
    - `npx tsc --noEmit -p tsconfig.app.json` reports 33, the baseline, ZERO under `components/workflows/`.
    - `git diff --stat -- backend supabase` is EMPTY.
  </acceptance_criteria>
  <done>Every bullet in <acceptance_criteria> above holds, measured rather than asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| publish verdict (server) → gauntlet | gate outcomes and reasons arrive from the publish gate and are rendered as a shipping decision |
| authored definition → soul | `business_requirement`, input keys and deliverable labels are user/LLM-authored |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-199-03-01 | Elevation of privilege | `PublishGauntlet.tsx` | mitigate | The no-override assertion over the whole judge-failure state, driven RED against a planted control. A re-presentation must not introduce a path past a hard wall. |
| T-199-03-02 | Tampering | `WorkflowSoul.tsx` | mitigate | Authored strings stay plain React text children; no `dangerouslySetInnerHTML` (T-124-01 is a shipped clause). |
| T-199-03-03 | Repudiation | strictness tier | mitigate | The tier stays derived-per-render from real enums; no stored or re-labelled strictness value is introduced. |

No package-manager install task exists in this plan, so the Package Legitimacy Gate does not apply.
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` → 33, unmoved
- `node scripts/vitest-count-gate.cjs` from the REPO ROOT with `GSD_VITEST_MAX_WORKERS=2` → `failed 0`, no per-file decrease
- `git diff --stat -- backend supabase` → empty
</verification>

<success_criteria>
- Every element of sheet c7 carries a verdict; none is silently dropped (SC#1).
- No backend, migration, endpoint or wire model modified (SC#2).
- Neither surface renders more at rest than before, proved against a pre-change inventory (SC#3).
- The mechanism is not printed to the user (SC#4).
- Gates hold (SC#5).
</success_criteria>

<output>
Create `.planning/phases/199-the-component-map/199-03-SUMMARY.md` when done, carrying the
reconciliation table, the `UNDETERMINED`-vs-`MIDDLE` decision, and every CANNOT-EXPRESS report.
</output>
