---
phase: 199-the-component-map
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/workflows/DraftArrivalCard.tsx
  - frontend/src/components/workflows/DecisionsList.tsx
  - frontend/src/components/workflows/decisionsVocabulary.ts
  - frontend/src/components/workflows/DraftArrivalCard.test.tsx
  - frontend/src/components/workflows/DecisionsList.test.tsx
  - frontend/src/components/workflows/decisionsVocabulary.test.ts
autonomous: true
requirements: [DES-01]

must_haves:
  truths:
    - "The arrival moment stays ONE card — a fourth child of the page's three-row graph column strands the graph at 0 px"
    - "Every decisions row keeps THREE arms; an ABSENT readiness renders nothing affirmative, and a boolean would re-introduce 'unknown reads as satisfied'"
    - "Only the grounding row claims a publish requirement; the other four would be claiming a gate nothing enforces"
    - "The receipt suppression list still hides exactly the frame/duplicate handles and NO content handle — a blank fold is green in every geometry assertion"
  artifacts:
    - path: "frontend/src/components/workflows/DraftArrivalCard.tsx"
      provides: "The one-card arrival moment, re-presented"
    - path: "frontend/src/components/workflows/DecisionsList.tsx"
      provides: "The five-row decisions list, three arms per row"
  key_links:
    - from: "frontend/src/components/workflows/DecisionsList.tsx"
      to: "frontend/src/components/workflows/decisionsVocabulary.ts"
      via: "every user-visible word, plus the declared five-row ORDER as data"
      pattern: "decisionsVocabulary"
    - from: "frontend/src/components/workflows/DraftArrivalCard.tsx"
      to: "frontend/src/components/workflows/SeedReceipt.tsx"
      via: "composed inside the first fold, its own frame neutralised by the suppression class"
      pattern: "SeedReceipt|RECEIPT_SUPPRESSION_CLASS"
---

<objective>
Re-present the draft-arrival cluster in the adopted design language, against sheet `c5-draft-arrival`.

Purpose: DES-01 on the moment a drafted workflow lands — the surface `SEED-184` names by example
(*"the arrival card is 147 px collapsed and 398 px with both folds open. The folds are the one place
in this surface where progressive disclosure IS already used"*). The folds are prior art in the
right direction and are the starting point, not a thing to invent.

Output: a re-presented arrival card and decisions list, with a pre-change inventory proving the
subtraction and every shipped 197 invariant intact.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/sketches/178-stitch-component-map/README.md
@.planning/sketches/178-stitch-component-map/sheets/c5-draft-arrival.html
@.planning/seeds/SEED-184-information-is-dumped-as-text-not-presented.md
@.claude/skills/sketch-findings-agentic-rag/references/ai-seed-and-templates.md
@docs/HOT-FILE-LEDGER.md
</context>

<planner_correction>
⚠ **Sheet `c5-draft-arrival` is ABSENT from the ROADMAP's wave table.** The scope table lists TEN
sheets; the wave table lists NINE surfaces across three waves, and `c5` is the missing one. It is
placed in Wave 1 here, with its own plan, because it is an atom (a card and its list) and because
its consumer — `WorkflowBuilderPage.tsx` — is owned by plan 199-09 in Wave 3, so the atom must land
first. The omission is recorded rather than quietly absorbed.

⚠ **`SeedReceipt.tsx` is deliberately NOT in `files_modified`.** Phase 197 placed it under a
zero-insertion / zero-deletion diff criterion and wrote `DraftArrivalCard.tsx` so that criterion
never comes under pressure. This plan inherits that fence: the receipt is COMPOSED, never edited.
</planner_correction>

<scope_fence>
**PRESENTATION ONLY.** No new data, no endpoint, no migration, no wire-model change, no new
user-facing capability. `git diff` over `backend/` and `supabase/` must be EMPTY at every task.
A task that discovers it needs a backend change **STOPS and reports**.

**THE CANNOT-EXPRESS RULE (`SEED-155`).** If the shipped component cannot express the sheet, report
it — what the sheet asks for, what the component can do, and the gap. Never fake it.
</scope_fence>

<tasks>

<task type="auto">
  <name>Task 1: Inventory the arrival cluster and reconcile it against sheet c5</name>
  <files>frontend/src/components/workflows/DraftArrivalCard.test.tsx, frontend/src/components/workflows/DecisionsList.test.tsx</files>
  <read_first>
    - `.planning/sketches/178-stitch-component-map/sheets/c5-draft-arrival.html` — the two card states (freshly arrived / returned-to), the five decisions rows, and the seed receipt
    - `.planning/sketches/178-stitch-component-map/README.md` § "Sheets 4-9" — *"the draft arrival cluster reproduces our shipped `decisionsVocabulary` contract: three arms per row, an explicit not reported, and only the grounding row claiming a publish requirement"*
    - `frontend/src/components/workflows/DraftArrivalCard.tsx` — ⚠ the ONE-CARD constraint (a FOURTH child strands the page's graph column at 0 px), and ⚠ `RECEIPT_SUPPRESSION_CLASS`, which is LOAD-BEARING: four frame/duplicate handles and NO content handle, or the fold silently BLANKS — and a blank fold is green in every geometry assertion
    - `frontend/src/components/workflows/DecisionsList.tsx` — ⚠ THREE ARMS, NEVER TWO: an ABSENT readiness renders nothing affirmative, which is not a green one
    - `frontend/src/components/workflows/decisionsVocabulary.ts` — the five-row order as DATA, the true-leaf property (it imports NOTHING), and ⚠ D-20: ONLY ROW 3 MAY CLAIM A PUBLISH REQUIREMENT
    - `.planning/seeds/SEED-184-*.md` § "Constraints a sketch must honour" — rule 3: nothing may be hidden that a person needs in order to decide
  </read_first>
  <action>
Measure before changing anything. No source byte outside the two named test files may move.

Inventory the RESTING atoms of the arrival cluster as literal strings — the card COLLAPSED, the card
with each fold open, and every decisions row in each of its three arms — pinned PRESENT so a later
removal is proved by INVERSION (the `192.2-05` method), never by deleting an assertion. Record the
collapsed and fully-open HEIGHTS as the `SEED-184` measurement did, so "the card got quieter" is
falsifiable rather than a feeling.

Then reconcile against the sheet, one row per element, with a verdict:
  - **The two card states** — freshly arrived and returned-to. Record what differs today.
  - **The five decisions rows and their three arms** — satisfied / needs-you / not-reported. The
    sheet reproduces this contract; confirm it matches and record any drift IN THE SHEET rather than
    changing the code to match a drawing.
  - **Only the grounding row claims a publish requirement** — assert it over all five rows, with the
    other four asserted NOT to claim one. Drive this RED once against a plant, then restore.
  - **The seed receipt is absent when there is nothing to report** — assert the absence.
  - **Progressive disclosure** — per `SEED-184`, identify what is currently at one visual level and
    all expanded at once, and which of it can go behind the EXISTING folds rather than a new
    affordance. ⚠ Rule 3: the governance state, the strict/loose door and the deliverable may NEVER
    go behind a click the reader has no reason to make.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/workflows/DraftArrivalCard.test.tsx src/components/workflows/DecisionsList.test.tsx src/components/workflows/decisionsVocabulary.test.ts src/components/workflows/SeedReceipt.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The inventory pins pass and record collapsed and fully-open heights as numbers.
    - The only-row-3-claims-a-requirement assertion has been driven RED against a plant and restored.
    - `git status --short` shows exactly the two named test files. `git diff --stat -- backend supabase` is EMPTY.
    - `SeedReceipt.tsx` shows a `git diff --numstat` of exactly nothing.
  </acceptance_criteria>
  <done>Every bullet in <acceptance_criteria> above holds, measured rather than asserted.</done>
</task>

<task type="auto">
  <name>Task 2: Re-present the arrival card and the decisions list</name>
  <files>frontend/src/components/workflows/DraftArrivalCard.tsx, frontend/src/components/workflows/DecisionsList.tsx, frontend/src/components/workflows/decisionsVocabulary.ts, frontend/src/components/workflows/DraftArrivalCard.test.tsx, frontend/src/components/workflows/DecisionsList.test.tsx, frontend/src/components/workflows/decisionsVocabulary.test.ts</files>
  <read_first>
    - Task 1's reconciliation table — the work list; nothing outside it may be built
    - `DraftArrivalCard.tsx`'s docblock — the two consequences 197 accepted deliberately (`display` not opacity, so no hidden focusable control; the receipt's section losing its accessible name)
  </read_first>
  <action>
Apply ONLY the BUILT rows. Hierarchy, typography, spacing, tone, fold placement and removal.

Binding constraints, all shipped invariants:
  - **ONE CARD.** A fourth child of the page's three-row graph column strands the graph at 0 px.
  - **`SeedReceipt.tsx` may not be edited** — zero insertions, zero deletions. It is composed.
  - **The suppression list stays frame-only.** Adding a CONTENT handle silently blanks the fold, and
    a blank fold passes every geometry assertion. If a suppression entry changes, assert the fold's
    body is NON-EMPTY before asserting anything about its shape.
  - **THREE ARMS, NEVER TWO.** An absent readiness renders nothing affirmative. A boolean
    re-introduces "unknown reads as satisfied".
  - **ONLY ROW 3 MAY CLAIM A PUBLISH REQUIREMENT** — the other four would be claiming a gate nothing
    enforces (D-20, enumerated from source).
  - **`decisionsVocabulary.ts` stays a TRUE LEAF** — it imports NOTHING, and that property is
    asserted. Do not reach for `GOVERNANCE_SEAL_LABEL` or any other locked string from it; a
    near-spelling is still a second spelling.
  - **Every user-visible word lives in the vocabulary module**, asserted by character identity. No
    sentence may be authored inside a component.
  - **Nothing a person needs in order to decide may move behind a fold** (`SEED-184` rule 3).
  - **Verify every colour token the sheet asks for RESOLVES** in `frontend/tailwind.config.js`.

Where the sheet removes an atom, invert the Task-1 assertion; delete none. Never re-baseline a
characterization pin to turn red green. Re-record the collapsed and fully-open heights after the
change and publish the delta.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/workflows/DraftArrivalCard.test.tsx src/components/workflows/DecisionsList.test.tsx src/components/workflows/decisionsVocabulary.test.ts src/components/workflows/SeedReceipt.test.tsx && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - The cluster renders no MORE at rest than the Task-1 inventory recorded; the height delta is published.
    - Zero assertions deleted; every removal proved by inversion.
    - `SeedReceipt.tsx` `git diff --numstat` is still exactly nothing.
    - Every fold's body asserted NON-EMPTY before any assertion about its shape.
    - `npx tsc --noEmit -p tsconfig.app.json` reports 33, the baseline, ZERO under `components/workflows/`.
    - `git diff --stat -- backend supabase` is EMPTY.
  </acceptance_criteria>
  <done>Every bullet in <acceptance_criteria> above holds, measured rather than asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM-authored draft → arrival card | the describe text, the drafted name and the decision values are model output rendered to the author |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-199-04-01 | Spoofing | `DecisionsList.tsx` | mitigate | Three-arm rendering enforced; an absent readiness may never render an affirmative arm, so the surface cannot claim a decision the generator did not make. |
| T-199-04-02 | Repudiation | `decisionsVocabulary.ts` | mitigate | D-20 held by assertion — only row 3 claims a publish requirement; the other four are asserted NOT to, driven RED against a plant. |
| T-199-04-03 | Tampering | `DraftArrivalCard.tsx` | mitigate | Model-authored strings stay plain React text children; the receipt stays byte-unmodified so its own locked-string audit is untouched. |

No package-manager install task exists in this plan, so the Package Legitimacy Gate does not apply.
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` → 33, unmoved
- `node scripts/vitest-count-gate.cjs` from the REPO ROOT with `GSD_VITEST_MAX_WORKERS=2` → `failed 0`, no per-file decrease
- `git diff --stat -- backend supabase` → empty
- `git diff --numstat -- frontend/src/components/workflows/SeedReceipt.tsx` → empty
</verification>

<success_criteria>
- Every element of sheet c5 carries a verdict; none is silently dropped (SC#1).
- No backend, migration, endpoint or wire model modified (SC#2).
- The cluster renders no more at rest than before, proved against a pre-change inventory including measured heights (SC#3).
- The mechanism is not printed to the user (SC#4).
- Gates hold (SC#5).
</success_criteria>

<output>
Create `.planning/phases/199-the-component-map/199-04-SUMMARY.md` when done, carrying the
reconciliation table, the measured collapsed/open height delta, and every CANNOT-EXPRESS report.
</output>
