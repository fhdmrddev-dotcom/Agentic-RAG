---
phase: 199-the-component-map
plan: 09
type: execute
wave: 3
depends_on: ["199-04", "199-06", "199-08"]
files_modified:
  - frontend/src/pages/WorkflowBuilderPage.tsx
  - frontend/src/components/workflows/BuilderHeaderBar.tsx
  - frontend/src/components/workflows/BuilderSaveRegion.tsx
  - frontend/src/components/workflows/ProblemsTray.tsx
  - frontend/src/components/workflows/StepTypePicker.tsx
  - frontend/src/pages/WorkflowBuilderPage.header.test.tsx
  - frontend/src/components/workflows/BuilderSaveRegion.test.tsx
  - frontend/src/components/workflows/ProblemsTray.test.tsx
  - frontend/src/components/workflows/StepTypePicker.test.tsx
autonomous: true
requirements: [DES-01]

must_haves:
  truths:
    - "The header headline is the workflow's NAME, degrading to a dimmed fallback rather than to blank, and a long name truncates without pushing the controls off the row"
    - "The save region's failure arm is never confusable with its saved arm — proved with every `class` attribute stripped"
    - "Publish states its refusal rather than presenting a dead control"
    - "The problems tray stays business-plain BY DEFAULT, with the raw code only behind the ⌥ reveal; no friendly-message map is re-introduced"
    - "The step picker's rows still resolve their titles through THE one title resolver, never through a second sentence map"
  artifacts:
    - path: "frontend/src/pages/WorkflowBuilderPage.tsx"
      provides: "The builder chrome, re-presented"
    - path: "frontend/src/components/workflows/BuilderSaveRegion.tsx"
      provides: "The four-arm save reading"
  key_links:
    - from: "frontend/src/pages/WorkflowBuilderPage.tsx"
      to: "frontend/src/components/workflows/BuilderHeaderBar.tsx"
      via: "lead / trail / identity / actions slots — the bar owns LAYOUT and nothing else"
      pattern: "BuilderHeaderBar"
    - from: "frontend/src/components/workflows/StepTypePicker.tsx"
      to: "frontend/src/components/workflows/phaseVocabulary.ts nodeTitle"
      via: "each row is a PREVIEW OF THE CARD, resolved by THE one title resolver"
      pattern: "nodeTitle"
---

<objective>
Re-present the builder chrome in the adopted design language, against sheet `c10-builder-chrome` —
the sheet that took three attempts and completed twenty minutes late.

Purpose: DES-01 on the frame an author works inside all day. Sheet c10's headline claim is that the
header carries the workflow's NAME as the headline, never a slug — a defect that was on the board.

Output: a re-presented header, save region, publish gate, problems tray and step picker; and the
phase's second flagship report, because the sheet's problems tray is where the business-language rule
measurably failed and where the fix is NOT ours to make inside this fence.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/sketches/178-stitch-component-map/README.md
@.planning/sketches/178-stitch-component-map/sheets/c10-builder-chrome.html
@.claude/skills/sketch-findings-agentic-rag/references/workflow-builder.md
@.claude/skills/sketch-findings-agentic-rag/references/node-vocabulary-and-reveal.md
@docs/HOT-FILE-LEDGER.md
</context>

<already_shipped_do_not_rebuild>
⚠ **THE SHEET'S HEADLINE CLAIM IS ALREADY SHIPPED, AND REBUILDING IT WOULD BE THE WASTE THIS PHASE
EXISTS TO AVOID.** Phase 197 plan `197-10` (D-19) landed `identityLabel` in
`WorkflowBuilderPage.tsx`: the workflow's NAME when it has one, the slug when it does not, and the
shipped `"Untitled workflow"` fallback when it has neither. Its docblock records exactly the defect
sheet c10 designs out — *"before this, the header rendered the slug and `meta.name` appeared in no
render position anywhere on this page."*

So the header's NAME row is **ALREADY-SHIPPED**. What remains from sheet c10's five header cases is
the **empty-name** presentation (the sheet degrades to a DIMMED fallback, never blank) and the
**long-name truncation** (which must not push the controls off the row — `min-w-0 truncate` is
already in the identity group; verify rather than assume).

⚠ **`identityLabel`'s non-empty check is a DISPLAY fallback, not a validation rule.** No store
action, no request and no predicate learns anything from it. Keep it that way.
</already_shipped_do_not_rebuild>

<scope_fence>
**PRESENTATION ONLY.** No new data, no endpoint, no migration, no wire-model change, no new
user-facing capability. `git diff` over `backend/` and `supabase/` must be EMPTY at every task.
A task that discovers it needs a backend change **STOPS and reports**.

**THE CANNOT-EXPRESS RULE (`SEED-155`).** If the shipped component cannot express the sheet, report
it — what the sheet asks for, what the component can do, and the gap. Never fake it.
</scope_fence>

<known_sheet_flaw_do_not_inherit>
⚠ **THE PROBLEMS TRAY IS WHERE THE BUSINESS-LANGUAGE RULE MEASURABLY FAILED IN THE SHEET.** Sketch
178's README names it: *"Phase 5: Output schema invalid / JSON schema definition contains syntax
errors"*, *"Retrieve step requires at least one connected datastore"* — *"error codes wearing a
sentence… the business-language rule holds on labels and titles, and fails on error text."*

**DO NOT COPY THAT LANGUAGE INTO THE BUILD.** And note why the obvious fix is forbidden here:
`ProblemsTray.tsx` renders the server's own `message` VERBATIM by decision (D-182-06) — *"there is
no code table here and no friendly-message map"* — because Phase 185 ships new findings as new
identifiers in the module that owns them, and a client-side rewrite map is exactly what that
decision removed. Rewriting the sentences at their source is a **backend change**, which this
phase's fence forbids.

**Therefore this is a REPORT, not a build.** Task 3 writes it, with a routing recommendation.
</known_sheet_flaw_do_not_inherit>

<tasks>

<task type="auto">
  <name>Task 1: Inventory the builder chrome and reconcile it against sheet c10</name>
  <files>frontend/src/pages/WorkflowBuilderPage.header.test.tsx</files>
  <read_first>
    - `.planning/sketches/178-stitch-component-map/sheets/c10-builder-chrome.html` — §1 the header bar (five cases), §2 the save region (four arms), §3 the publish button (four states), §4 the problems tray, §5 the step picker
    - `.planning/sketches/178-stitch-component-map/README.md` § "Sheet 10 · The builder chrome"
    - `frontend/src/pages/WorkflowBuilderPage.tsx` — the `identityLabel` block and its D-19 docblock; the `identityGroup` and `actionGroup` composition; the `canvasEnabled` header gate and its FLAG-OFF branch
    - ⚠ `frontend/src/pages/WorkflowBuilderPage.header.test.tsx` — it holds `FLAG_OFF_HEADER_MARKUP`, a LITERAL markup pin over the three drafted header bands. Its own note records that the phase expects NO third re-capture. **Any header markup change reds it.**
    - `frontend/src/components/workflows/BuilderHeaderBar.tsx` — it owns LAYOUT AND NOTHING ELSE; `lead` and `trail` arrive as opaque nodes from ancestor band owners and keep their original owner, handler and state
    - `frontend/src/components/workflows/BuilderSaveRegion.tsx` — the state→sentence mapping and why the quiet line is flag-gated while the conflict banner is not
    - `frontend/src/components/workflows/ProblemsTray.tsx` — ⚠ the two-count summary IN THE CLOSED STATE, the `phase: null` section, the verbatim server message, and the raw `code` only behind the ⌥ reveal
    - `frontend/src/components/workflows/StepTypePicker.tsx` — ⚠ each row is a PREVIEW OF THE CARD resolved through `nodeTitle`; the file is fenced at ZERO sentence-map reads, because the previous second answer produced *"Check with you"* in the menu and *"Wait for your approval"* on the card, one click apart
  </read_first>
  <action>
Measure before changing anything. No source byte outside the named test file may move.

Inventory the RESTING atoms of the builder chrome as literal strings — the header in each of the
five sheet cases, the save region in each of its four arms, the publish control in each of its four
states, the tray closed and open, and the step picker — pinned PRESENT so a later removal is proved
by INVERSION, never by deletion.

Then reconcile against the sheet, with a verdict per element:
  - **Header §1.** Record the NAME row as ALREADY-SHIPPED (197 / D-19) with its commit reference.
    Then measure the two remaining cases: **empty name** (does it degrade to a DIMMED fallback or to
    blank?) and **long name** (does truncation push the controls off the row at a narrow window?).
  - **Save region §2 — four arms.** ⚠ *"Save failed — Check connection"* must be UNMISTAKABLE and
    never confusable with saved. Record how the four arms differ today.
  - **Publish §3 — four states**, with the refusal STATED (*"Needs 2 grounded phases"*). The shipped
    `renderPublish` seam already carries a `blockedReason`; record whether the refusal is rendered
    or only disables the control.
  - **Problems tray §4.** Record the tray's shipped two-count closed summary as ALREADY-SHIPPED, and
    record the sheet's engineer-language rows as REFUSED, with Task 3 owning the report.
  - **Step picker §5.** The sheet's verbs (*Find documents · Pull out specific details · Weigh
    against policy · Decide which way to go · Write a document · Send or file result*) are a DRAWING
    of the vocabulary, not a source for it. Assert the picker still resolves every row through
    `nodeTitle` and reads ZERO sentence maps. Do not transcribe the sheet's words.
  - ⚠ **The second describe box** (`WorkflowBuilderPage.tsx:1917`, `data-testid="describe-hint"`) —
    plan `199-08` landed the door refusal constant in wave 2. Import it here; do not re-spell it.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/pages/WorkflowBuilderPage.header.test.tsx src/components/workflows/BuilderSaveRegion.test.tsx src/components/workflows/ProblemsTray.test.tsx src/components/workflows/StepTypePicker.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The inventory pin passes and names atoms as literals across all five header cases and all four save arms.
    - The NAME row is recorded ALREADY-SHIPPED with its source reference, and is NOT rebuilt.
    - The sheet's engineer-language tray rows are recorded REFUSED.
    - `git status --short` shows exactly the one named test file. `git diff --stat -- backend supabase` is EMPTY.
  </acceptance_criteria>
  <done>Every bullet in <acceptance_criteria> above holds, measured rather than asserted.</done>
</task>

<task type="auto">
  <name>Task 2: Re-present the header, the save region and the publish gate</name>
  <files>frontend/src/pages/WorkflowBuilderPage.tsx, frontend/src/components/workflows/BuilderHeaderBar.tsx, frontend/src/components/workflows/BuilderSaveRegion.tsx, frontend/src/pages/WorkflowBuilderPage.header.test.tsx, frontend/src/components/workflows/BuilderSaveRegion.test.tsx</files>
  <read_first>
    - Task 1's reconciliation table — the work list
    - `WorkflowBuilderPage.tsx`'s D-06 note on where a gated sibling may live INSIDE an existing affordance, so band 3 of `FLAG_OFF_HEADER_MARKUP` stays unmovable BY CONSTRUCTION
    - `docs/HOT-FILE-LEDGER.md` → `frontend/src/pages/WorkflowBuilderPage.tsx` — 47 / 14 / 2656, G-5 FIRES, honoured by construction ×3. Read the named seam before adding anything.
  </read_first>
  <action>
Apply ONLY the BUILT header, save and publish rows. Hierarchy, typography, spacing, tone and removal.

Binding constraints:
  - ⚠ **`FLAG_OFF_HEADER_MARKUP` is a literal markup pin.** If a header change reds it, that is a
    DELIBERATE re-baseline: state it, scope it to exactly the bands that moved, and prove it by
    diffing the captured markup rather than re-capturing blind. **Never re-baseline a pin to make
    red go green.** If the change can be made without touching the pinned bands, prefer that.
  - **`BuilderHeaderBar` owns LAYOUT AND NOTHING ELSE.** `lead` / `trail` / `identity` / `actions`
    stay opaque nodes; do not move ownership of a control into the bar.
  - **NOTHING IS REMOVED from the header's control set.** Every control, badge and label keeps its
    accessible name. This is a re-presentation of what is drawn, not of what is owned.
  - **It WRAPS rather than truncates.** The trailing group falls to a second line on a narrow window
    rather than eliding a control — that is the shipped answer to the operator's original complaint.
  - **The empty-name case degrades to a DIMMED fallback, never blank**, and the fallback stays a
    DISPLAY fallback with no validation semantics.
  - **The save failure arm is asserted distinct from the saved arm with every `class` attribute
    stripped** off the rendered DOM. Colour alone is not the carrier.
  - **The publish refusal is rendered as words**, sourced from the shipped `blockedReason` seam; no
    new refusal predicate and no new reason string is authored here.
  - **Verify every colour token the sheet asks for RESOLVES** in `frontend/tailwind.config.js`.

Where the sheet removes an atom, invert the Task-1 assertion; delete none.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/pages/WorkflowBuilderPage.header.test.tsx src/pages/WorkflowBuilderPage.test.tsx src/pages/WorkflowBuilderPage.canvas.test.tsx src/components/workflows/BuilderSaveRegion.test.tsx && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - The header renders no MORE at rest than the Task-1 inventory recorded, and no control lost its accessible name.
    - The save failure and saved arms are mutually distinct with every `class` attribute stripped.
    - `FLAG_OFF_HEADER_MARKUP` is either byte-unchanged, or its re-baseline is stated, scoped to the moved bands, and proved by a markup diff.
    - `npx tsc --noEmit -p tsconfig.app.json` reports 33, ZERO under `pages/` or `components/workflows/`.
    - `git diff --stat -- backend supabase` is EMPTY.
  </acceptance_criteria>
  <done>Every bullet in <acceptance_criteria> above holds, measured rather than asserted.</done>
</task>

<task type="auto">
  <name>Task 3: Re-present the step picker, and REPORT the problems-tray language gap</name>
  <files>frontend/src/components/workflows/StepTypePicker.tsx, frontend/src/components/workflows/ProblemsTray.tsx, frontend/src/components/workflows/StepTypePicker.test.tsx, frontend/src/components/workflows/ProblemsTray.test.tsx</files>
  <read_first>
    - Task 1's picker and tray rows
    - `ProblemsTray.tsx`'s D-182-06 block — no code table, no friendly-message map, the server's `message` verbatim
    - `StepTypePicker.tsx`'s WR-03 block — the semantic drift that survived every `?raw` fence because both strings were imported identifiers
  </read_first>
  <action>
**The picker (a build).** Apply the BUILT presentation rows: hierarchy, spacing, the mark, and the
supporting line. ⚠ Every row title MUST continue to resolve through `nodeTitle` over the very phase
`minimalPhaseFor` will build. Do NOT transcribe the sheet's six verbs into this file — a second
answer to a question with one home is the exact defect WR-03 fixed, and it survived every lexical
fence because the drift was SEMANTIC. The file's zero-sentence-map fence must still pass.

**The tray (a report, plus a verification).** Verify — do not rebuild — that the tray is business-
plain BY DEFAULT: the two-count summary renders in the CLOSED state, the `phase: null` section
renders even when it is the only thing there, and the raw `code` appears ONLY behind the ⌥
Technical-names reveal, read through the shipped fail-closed accessor.

Then write the CANNOT-EXPRESS report in the three required parts:
  - **What the sheet asks for:** findings phrased in a colleague's language rather than as error
    codes wearing a sentence.
  - **What the component can do:** nothing — by decision. `ProblemsTray.tsx` renders the server's
    `message` verbatim (D-182-06); a client-side friendly-message map is exactly what that decision
    removed, and re-introducing one would put a second home under every finding identifier.
  - **The gap:** the sentences are authored server-side, in the module that owns the finding
    identifiers. Fixing them is a BACKEND change and is outside this phase's fence.

Recommend the routing explicitly and give it a re-open trigger, so the deferral has a home rather
than being forgotten. **Do not build a rewrite map. Do not touch `backend/`.**
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/workflows/StepTypePicker.test.tsx src/components/workflows/ProblemsTray.test.tsx src/components/workflows/phaseVocabulary.test.ts && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - The picker's zero-sentence-map fence still passes, and every row title is asserted against the RESOLVER, not against a string.
    - No friendly-message map exists anywhere in `ProblemsTray.tsx` — asserted, with the assertion driven RED against a planted map and restored.
    - The tray's raw `code` is asserted absent by default and present only under the ⌥ reveal.
    - The three-part report exists with a routing recommendation and a re-open trigger.
    - `node scripts/vitest-count-gate.cjs` from the REPO ROOT with `GSD_VITEST_MAX_WORKERS=2` reports `failed 0` and no per-file decrease.
    - `git diff --stat -- backend supabase` is EMPTY.
  </acceptance_criteria>
  <done>Every bullet in <acceptance_criteria> above holds, measured rather than asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server validation findings → problems tray | finding messages and codes arrive from the validation endpoint and are rendered verbatim |
| authored name → header | `meta.name` is user-authored and becomes the page headline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-199-09-01 | Tampering | `ProblemsTray.tsx` | mitigate | Server messages stay plain React text children rendered verbatim; no rewrite map and no `dangerouslySetInnerHTML`. The no-map assertion is driven RED against a plant. |
| T-199-09-02 | Spoofing | `BuilderSaveRegion.tsx` | mitigate | The failure arm is asserted distinct from the saved arm with classes stripped, so an unsaved draft can never read as saved. |
| T-199-09-03 | Elevation of privilege | publish gate | mitigate | The refusal is rendered from the shipped `blockedReason`; no new predicate is authored and no gate is loosened by a presentation change. |

No package-manager install task exists in this plan, so the Package Legitimacy Gate does not apply.
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` → 33, unmoved
- `node scripts/vitest-count-gate.cjs` from the REPO ROOT with `GSD_VITEST_MAX_WORKERS=2` → `failed 0`, no per-file decrease
- `git diff --stat -- backend supabase` → empty
- ⚠ `src/pages/WorkflowBuilderPage.canvas.test.tsx` and `src/pages/WorkflowBuilderPage.session.test.tsx` are named `SEED-171` flaky suites. If either reds, capture the failing filenames from the gate's persisted JSON BEFORE re-running, check each against `git diff --numstat`, and do NOT adjust the worker cap — that remedy is REFUTED. Record byte-unchanged suites as "provably unmodified", never as "fine".
- ⚠ A red gate is sometimes REAL: `196-08` measured `failed 249`, all genuine, from mock factories missing a newly-added export. The procedure, not the colour, is what separates the two cases.
</verification>

<success_criteria>
- Every element of sheet c10 carries a verdict; none is silently dropped (SC#1).
- No backend, migration, endpoint or wire model modified (SC#2).
- The chrome renders no more at rest than before, proved against a pre-change inventory (SC#3).
- The mechanism is not printed to the user; the engineer-language tray rows are refused and reported (SC#4).
- Gates hold (SC#5).
</success_criteria>

<output>
Create `.planning/phases/199-the-component-map/199-09-SUMMARY.md` when done, carrying the
reconciliation table, the ALREADY-SHIPPED NAME row with its reference, the problems-tray
CANNOT-EXPRESS report with its routing and re-open trigger, and any `FLAG_OFF_HEADER_MARKUP`
re-baseline stated, scoped and proved.
</output>
