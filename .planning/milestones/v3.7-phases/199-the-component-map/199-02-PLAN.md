---
phase: 199-the-component-map
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/workflows/PhaseSpineGraph.tsx
  - frontend/src/components/panel/PhaseTimeline.tsx
  - frontend/src/components/panel/PhaseCard.tsx
  - frontend/src/components/panel/phaseStatusMeta.ts
  - frontend/src/components/workflows/PhaseSpineGraph.test.tsx
  - frontend/src/components/panel/PhaseCard.test.tsx
  - frontend/src/components/panel/__tests__/PhaseTimeline.test.tsx
autonomous: true
requirements: [DES-01]

must_haves:
  truths:
    - "The authoring spine, the live panel spine and the chat-sized receipt each carry a verdict against sheet c3 — BUILT, ALREADY-SHIPPED or CANNOT-EXPRESS with its reason"
    - "No determinate mid-phase progress is rendered anywhere — the sheet's own `(4/12)` is a recorded sheet flaw and is NOT inherited"
    - "A branch outcome reads as a WORD, never as colour alone"
    - "The authoring spine renders no MORE at rest than before — the sheet's two-line-description drift is inverted, not copied"
  artifacts:
    - path: "frontend/src/components/workflows/PhaseSpineGraph.tsx"
      provides: "The read-only vertical authoring spine, re-presented"
    - path: "frontend/src/components/panel/PhaseCard.tsx"
      provides: "One live phase row, re-presented, with SUBSTEP_META still TOTAL over EmitSubStep"
  key_links:
    - from: "frontend/src/components/panel/PhaseTimeline.tsx"
      to: "frontend/src/components/panel/PhaseCard.tsx"
      via: "one card per phase row"
      pattern: "PhaseCard"
    - from: "frontend/src/components/workflows/PhaseSpineGraph.tsx"
      to: "frontend/src/components/workflows/phaseVocabulary.ts"
      via: "the ONE vocabulary + skip-target parse home"
      pattern: "phaseVocabulary|parseSkipTarget"
---

<objective>
Re-present the phase spine in the adopted design language, against sheet `c3-phase-spine`, across
its three real columns: the authoring spine, the live panel spine, and the chat-sized receipt.

Purpose: DES-01 on the surface that tells a person what a workflow IS and what it is DOING. Sheet
c3's unification works, and two of its columns carry recorded flaws that this plan must invert
rather than inherit.

Output: a re-presented vertical authoring spine and live panel spine, a pre-change inventory that
proves the subtraction, and a written report for the receipt column.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/sketches/178-stitch-component-map/README.md
@.planning/sketches/178-stitch-component-map/sheets/c3-phase-spine.html
@.claude/skills/sketch-findings-agentic-rag/references/harness-phase-timeline.md
@.claude/skills/sketch-findings-agentic-rag/references/run-honesty.md
@docs/HOT-FILE-LEDGER.md
</context>

<planner_correction>
⚠ **THE ROADMAP'S FILE MAPPING FOR THIS SHEET IS WRONG, AND IT WAS MEASURED.** The ROADMAP maps
`c3-phase-spine` to `PhaseSpine.tsx` (104 L) + `PhaseCard.tsx`. **`PhaseSpine.tsx` is the SOUL's
horizontal glyph-dot spine**, and its own docblock carries a `G-5 RED LINE: this is a sibling
component; it MUST NOT import or touch the run-surface live phase-timeline / phase-card.` It is
NOT sheet c3's subject and it belongs to plan **199-03** (the soul), where the soul's other half
already lives.

Sheet c3's three columns map to:
| Sheet column | Ships against |
|---|---|
| Col 1 · the authoring spine | `frontend/src/components/workflows/PhaseSpineGraph.tsx` (264 L) — the read-only VERTICAL builder spine |
| Col 2 · the panel spine (live run) | `frontend/src/components/panel/PhaseTimeline.tsx` + `frontend/src/components/panel/PhaseCard.tsx` (522 L) |
| Col 3 · the receipt spine (chat-sized) | the chat run surface — see Task 3, which is a REPORT, not a build |

The correction is recorded here rather than applied silently, because a wrong `files_modified` was
found in seven of Phase 192.2's eight plans and the rate is the finding.
</planner_correction>

<scope_fence>
**PRESENTATION ONLY.** No new data, no endpoint, no migration, no wire-model change, no new
user-facing capability. `git diff` over `backend/` and `supabase/` must be EMPTY at every task.
A task that discovers it needs a backend change **STOPS and reports**.

**THE CANNOT-EXPRESS RULE (`SEED-155`).** If the shipped component cannot express the sheet, report
it — what the sheet asks for, what the component can do, and the gap. Never fake it, never
approximate it silently, never grow a backend change to make it possible.
</scope_fence>

<known_sheet_flaws_do_not_inherit>
From sketch 178's own README, these are defects in the SHEET, not in our code:
1. ⚠ **`Processing liability caps section (4/12)`** — a determinate mid-phase count. The README
   names it as *"exactly the class of fabricated precision this project forbids"*: nothing in our
   system emits it. `PhaseTimeline.tsx`'s shipped `SUPPRESS-DON'T-FAKE (D-03)` rule already
   forbids per-phase counts. **DO NOT BUILD IT. Record it as a refused sheet element.**
2. ⚠ **The authoring column over-writes** — every phase carries a two-line description under its
   name, which the README calls *"the 'text is noise' rule drifting on the widest column."* The
   correct read is the OPPOSITE of the drawing: cut, do not add.
</known_sheet_flaws_do_not_inherit>

<tasks>

<task type="auto">
  <name>Task 1: Inventory both spines and reconcile them against sheet c3</name>
  <files>frontend/src/components/workflows/PhaseSpineGraph.test.tsx, frontend/src/components/panel/PhaseCard.test.tsx</files>
  <read_first>
    - `.planning/sketches/178-stitch-component-map/sheets/c3-phase-spine.html` — all three columns and the failure variant
    - `.planning/sketches/178-stitch-component-map/README.md` § "Sheet 3 · The spine" — the two named flaws
    - `frontend/src/components/workflows/PhaseSpineGraph.tsx` — the read-only invariant, the single dashed `skip_to_phase` branch, and the six things 183-04 DELETED from it so nothing is re-declared
    - `frontend/src/components/panel/PhaseTimeline.tsx` — the reconcile-floor skeleton, the forward-only counter, and the `SUPPRESS-DON'T-FAKE (D-03)` rule
    - `frontend/src/components/panel/PhaseCard.tsx` — the APG accordion, the non-colour-only status atom (SC 1.4.1), the closed-taxonomy failure copy, and the separate `role="alert"`
    - `frontend/src/components/panel/phaseStatusMeta.ts` — ⚠ `SUBSTEP_META` must stay TOTAL over `EmitSubStep` (hot-file ledger invariant)
  </read_first>
  <action>
Measure before changing anything. No source byte outside the two named test files may move.

Inventory the RESTING atoms of both spines as literal strings — the authoring spine's per-node
render at rest, and the panel spine's per-phase row in each status the closed taxonomy defines —
and pin each as PRESENT, so a later removal is proved by INVERTING the assertion rather than by
deleting it (the `192.2-05` method).

Then reconcile against the sheet, one row per element, each with a verdict:
  - **Col 1 authoring** — the phase name, its supporting line, the type marks, the branch. ⚠ The
    sheet's two-line descriptions are a recorded drift: record what the shipped spine renders at
    rest today and whether any of it is a restatement that the "text is noise" rule should cut.
  - **The branch reads as WORDS** — `TRAVERSED` / `SKIPPED`, not colour alone. Check whether the
    shipped dashed `skip_to_phase` branch carries a word today; if it carries only a line style,
    that is a genuine BUILT row (a word is presentation over data that already exists).
  - **Col 2 live panel** — waiting / running / succeeded / failed / skipped / paused-with-Approve-
    Reject. Map each to the shipped status taxonomy. ⚠ Record `(4/12)` as REFUSED with its reason.
  - **The failure variant** — the sheet carries a genuine error sentence. Check it against the
    shipped closed-taxonomy failure copy and the `reason_unknown` sentinel; the taxonomy is the
    single source and no sentence may be re-spelled here.
  - **Col 3 receipt** — mono per-step durations and a total runtime, chat-sized. Determine whether
    per-phase timing already reaches the frontend from data that already ships. If it does not,
    this is CANNOT-EXPRESS (it would need new data) and belongs in Task 3's report, NOT in a build.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/workflows/PhaseSpineGraph.test.tsx src/components/panel/PhaseCard.test.tsx src/components/panel/__tests__/PhaseTimeline.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both inventories pass and name atoms as literals, not shapes.
    - `git status --short` shows exactly the two named test files.
    - `git diff --stat -- backend supabase` is EMPTY.
    - Every sheet element has a verdict; `(4/12)` is recorded as REFUSED with its reason.
  </acceptance_criteria>
  <done>Every bullet in <acceptance_criteria> above holds, measured rather than asserted.</done>
</task>

<task type="auto">
  <name>Task 2: Re-present the authoring spine and the live panel spine</name>
  <files>frontend/src/components/workflows/PhaseSpineGraph.tsx, frontend/src/components/panel/PhaseTimeline.tsx, frontend/src/components/panel/PhaseCard.tsx, frontend/src/components/panel/phaseStatusMeta.ts, frontend/src/components/workflows/PhaseSpineGraph.test.tsx, frontend/src/components/panel/PhaseCard.test.tsx, frontend/src/components/panel/__tests__/PhaseTimeline.test.tsx</files>
  <read_first>
    - Task 1's reconciliation table — the work list; nothing outside it may be built
    - `docs/HOT-FILE-LEDGER.md` → `frontend/src/components/panel/PhaseCard.tsx` — ⚠ `SUBSTEP_META` must stay TOTAL over `EmitSubStep`
  </read_first>
  <action>
Apply ONLY the BUILT rows. Hierarchy, typography, spacing, tone and removal.

Binding constraints:
  - **No determinate mid-phase progress, ever.** `SUPPRESS-DON'T-FAKE (D-03)` is shipped policy; the
    sheet's `(4/12)` is refused.
  - **Status must never be carried by colour alone** (SC 1.4.1). Every status atom keeps a real
    visible text label. A branch outcome must read as a word.
  - **`SUBSTEP_META` stays TOTAL over `EmitSubStep`** — an unrecognised substep must still resolve;
    use an own-property lookup, never `TABLE[key] ?? fallback`, which this repo has measured
    returning an inherited property.
  - The authoring spine stays READ-ONLY: no `draggable`, no drag handler, no connection handle, no
    add-node control.
  - The vertical spine must not re-declare any vocabulary — the glyph map, the type labels, the
    on-fail parse and the title resolver each have exactly one home and it is not this file.
  - Failure copy comes from the shipped closed taxonomy; nothing is re-spelled.
  - Verify every colour token the sheet asks for RESOLVES in `frontend/tailwind.config.js` before
    using it — a class that compiles to nothing renders identically to an unpainted arm.

Where the sheet removes an atom, invert the Task-1 assertion from present to absent. Never delete an
assertion. Never re-baseline a characterization pin to turn red green.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/workflows/PhaseSpineGraph.test.tsx src/components/panel/PhaseCard.test.tsx src/components/panel/__tests__/PhaseTimeline.test.tsx src/components/panel/__tests__/PhaseReconcile.test.tsx && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - Neither spine renders MORE at rest than the Task-1 inventory recorded.
    - Zero assertions deleted; every removal proved by inversion.
    - No rendered string contains a determinate mid-phase count.
    - `npx tsc --noEmit -p tsconfig.app.json` reports 33, the baseline, with ZERO under `components/panel/` or `components/workflows/`.
    - `git diff --stat -- backend supabase` is EMPTY.
  </acceptance_criteria>
  <done>Every bullet in <acceptance_criteria> above holds, measured rather than asserted.</done>
</task>

<task type="auto">
  <name>Task 3: Report the receipt column rather than building it</name>
  <files>.planning/phases/199-the-component-map/199-02-SUMMARY.md</files>
  <read_first>
    - Task 1's Col-3 finding
    - `frontend/src/components/chat/RunCard.tsx` and `frontend/src/components/chat/ThreadRunLine.tsx` — the chat-sized run surfaces sheet c3's third column depicts
  </read_first>
  <action>
Write the CANNOT-EXPRESS report for sheet c3's third column in the required three parts: what the
sheet asks for (a chat-sized execution trace with mono per-step durations and a total runtime), what
the shipped chat run surface can do today, and the gap — naming precisely which value is missing and
where it would have to come from.

⚠ **This task builds nothing.** If Task 1 found that per-phase timing already reaches the frontend
from shipped data, say so plainly and route the build to a named future phase with a re-open
trigger — do NOT expand this plan to absorb it, and do NOT touch any chat-surface file. The chat
surface is outside this phase's ten sheets, and CLAUDE.md records that changes there land in the
chat product first.
  </action>
  <verify>
    <automated>test -f .planning/phases/199-the-component-map/199-02-SUMMARY.md && grep -qi "cannot-express" .planning/phases/199-the-component-map/199-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - The report names the missing value, its source, and the gap — all three parts present.
    - `git status --short` shows NO file under `frontend/src/components/chat/`.
    - A re-open trigger is written down, so the deferral has a home rather than being forgotten.
  </acceptance_criteria>
  <done>Every bullet in <acceptance_criteria> above holds, measured rather than asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live run stream → panel spine | phase names, failure reasons and substep labels arrive from the run stream and are rendered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-199-02-01 | Tampering | `PhaseCard.tsx` | mitigate | Failure copy stays sourced from the shipped closed taxonomy; no run-supplied string is rendered as markup and no `dangerouslySetInnerHTML` is introduced. |
| T-199-02-02 | Repudiation | live spine | mitigate | The forward-only counter and the reconcile floor are untouched; a re-presentation must not let the surface claim progress the run did not report. |
| T-199-02-03 | Elevation of privilege | scope fence | mitigate | `git diff --stat -- backend supabase` asserted EMPTY at every task; Task 3 is a report by construction. |

No package-manager install task exists in this plan, so the Package Legitimacy Gate does not apply.
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` → 33, unmoved
- `node scripts/vitest-count-gate.cjs` from the REPO ROOT with `GSD_VITEST_MAX_WORKERS=2` → `failed 0`, no per-file decrease
- `git diff --stat -- backend supabase` → empty
- ⚠ `src/components/panel/PhaseCard.test.tsx` and `src/components/panel/__tests__/PhaseTimeline.test.tsx` are both PINNED in the count gate's TARGETS. A decrease in either fails the gate; growth is the gate working.
</verification>

<success_criteria>
- All three sheet columns carry a verdict; none is silently dropped (SC#1).
- No backend, migration, endpoint or wire model modified (SC#2).
- Neither spine renders more at rest than before, proved against a pre-change inventory (SC#3).
- No determinate mid-phase count and no mechanism reaches the user (SC#4).
- Gates hold (SC#5).
</success_criteria>

<output>
Create `.planning/phases/199-the-component-map/199-02-SUMMARY.md` when done, carrying the
reconciliation table, the two refused sheet elements with their reasons, and the Col-3 report.
</output>
