---
phase: 195-show-the-deliverable
plan: 06
type: execute
wave: 3
depends_on: ["195-01", "195-02", "195-03"]
files_modified:
  - frontend/src/pages/WorkflowRunPage.tsx
  - frontend/src/pages/WorkflowRunPage.test.tsx
autonomous: true
requirements: [RUN-02, RUN-03]

must_haves:
  truths:
    - "The run surface's deliverable rows are the shared row, and the region keeps its heading, its list semantics, its one control per row and its section-level error"
    - "The region's heading no longer claims the run produced what it lists — it names the run's workspace"
    - "The three-way empty state still says three different true things, including saying NOTHING while the first read is in flight"
    - "The newest file is first, including the just-produced one that arrives over SSE with no created_at"
    - "The run page still names neither the panel's file section, nor the previewer, nor the viewed-thread hook"
    - "The id-less row is no longer a silent dead row — it carries the shipped 'Download unavailable' affordance and still contains zero buttons"
  artifacts:
    - path: "frontend/src/pages/WorkflowRunPage.tsx"
      provides: "The run adapter: the region, the heading, the three-way empty state, the ul/li, onDownload and the section error stay; the row markup and the three locals go"
      contains: "FileRow"
    - path: "frontend/src/pages/WorkflowRunPage.test.tsx"
      provides: "The F1 fence inverted in place with the original quoted; the D-02 heading cases updated; the F8 capture inverted; the D-12 ordering cases"
      min_lines: 1490
  key_links:
    - from: "frontend/src/pages/WorkflowRunPage.tsx"
      to: "frontend/src/components/files/FileRow.tsx"
      via: "asChild — the page supplies its own <button> and its own <div> for the id-less row"
      pattern: "FileRow"
    - from: "frontend/src/pages/WorkflowRunPage.tsx"
      to: "frontend/src/components/files/fileRowUtils.ts"
      via: "byNewestFirst applied client-side to the region's list, and baseName/formatBytes imported not re-derived"
      pattern: "byNewestFirst"
---

<objective>
Convert the run surface's deliverable region onto the shared row, relabel it honestly, order it
newest-first, and rewrite in place the two shipped fences that assert the duplication this phase
removes.

Purpose: this region is the surface RUN-02 is scoped to and it is the third of the three presentations
D-07 collapses. It is also where every one of this phase's mechanical constraints lands: two fences
INVERT, four raw-source predicates must keep holding, the most-seen state on the surface (161 of 222
runs) must survive byte-identical, and the sort key D-12 names is **absent on exactly the file D-12
exists to surface**.
Output: one converted page and its suite, with every fence either still green or inverted in place with
the original quoted.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@.planning/phases/195-show-the-deliverable/195-CONTEXT.md
@.planning/phases/195-show-the-deliverable/195-RESEARCH.md
@.planning/phases/195-show-the-deliverable/195-PATTERNS.md
@.planning/phases/195-show-the-deliverable/195-VALIDATION.md
@.planning/phases/195-show-the-deliverable/195-BASELINE.md
@.planning/phases/195-show-the-deliverable/195-02-SUMMARY.md
@.planning/phases/195-show-the-deliverable/195-03-SUMMARY.md

@frontend/src/pages/WorkflowRunPage.tsx
@frontend/src/pages/WorkflowRunPage.test.tsx
@frontend/src/components/files/FileRow.tsx
@frontend/src/components/files/fileRowUtils.ts

<interfaces>
<!-- Every predicate this plan must satisfy or deliberately invert, with its line and its verdict.
     Do not discover these at implementation time. -->

**FOUR RAW-SOURCE PREDICATES — comment-blind, so PROSE CAN BREAK THEM.** A docblock in
`WorkflowRunPage.tsx` explaining this extraction **must not write these tokens**; name them in WORDS:
  - `StopControl.baseline.test.tsx:595-599` — `(src.match(/<StopControl/g) ?? []).length` must be
    exactly **1**. Writing `<StopControl` in a comment makes it 2. ⚠ That suite was UNGATED until plan
    02 adopted it; before that, nothing would have told anyone.
  - `WorkflowRunPage.test.tsx:1463` — the token `FilesSection` must be ABSENT from raw page source
  - `:1464` — the token `useViewingThread` must be ABSENT
  - `:1478` — the token `FilePreview` must be ABSENT

**TWO ARMS THAT INVERT** — `WorkflowRunPage.test.tsx:1467-1468`, inside the `it("neither mounts the
panel's file list nor names it …")` case, verbatim today:

    expect(codeOf(pageSource)).toMatch(/function formatBytes/)
    expect(codeOf(pageSource)).toMatch(/function iconFor/)

⚠ **The suite is pinned EXACT with zero slack. DELETING the case forces a pin LOWERING, which needs
plan authorisation this plan does not grant.** Invert both arms **inside the same `it()`** so the count
does not fall.

**TWO CASES THAT MUST BE UPDATED FOR NEW COPY** — the literal `What this run produced` appears at
`WorkflowRunPage.tsx:122` **and** `WorkflowRunPage.test.tsx:897` **and `:1023`**. ⚠ `:1023` sits inside
the *"claims NEITHER while the first read is still in flight"* case, which is D-15's third arm — which
is why the D-02 relabel and the D-15 pin are ONE plan.

**PREDICATES THAT MUST KEEP HOLDING (do not invert, do not weaken):**
| Line | Predicate | What it constrains |
|---|---|---|
| `:1450-1455` | `useWorkspaceFiles(` ×1 · `useWorkspaceFiles(run?.thread_id` · `downloadWorkspaceFile(` ×1 · `const runThreadId = run?.thread_id` · `downloadWorkspaceFile(runThreadId` | ⚠ **A TENANCY PROPERTY, not a style rule** — the page downloads from the RUN's thread, never the viewed one. ⇒ **`onDownload` and its `downloadWorkspaceFile` call STAY IN THE PAGE.** Do not route them through a shared dispatcher. |
| `:1489-1490` | each empty-state copy appears **exactly once** in `codeOf(pageSource)` | the empty state stays in the PAGE — the shared unit is a ROW, not a list |
| `:950-953` | exactly **1** `<button>` per row, `aria-label` exactly `Download renewal-letter.docx (18.4 KB)` | one control per row, that aria-label format verbatim |
| `:968-971` | `[role="list"]` non-null, `li` count === 2 | keep `<ul role="list"><li>` |
| `:991` | the id-less row → `region.querySelectorAll("button")` length **0** | ⚠ survives ONLY because the shared row's dead affordance is a `<span aria-disabled>`, **not** a `<button>` |
| `:918` | `row.getAttribute("title")` === the full path | `title` = full path, on the control |
| `:945-949` | no `iframe`/`embed`/`object`/`[data-testid*='preview']` in the region | D-09 |
| `:1428-1429` | no `muted-foreground-dim`, no `--panel-` in page source | the row must not bring panel-scoped tokens onto the page |

**WHAT STAYS IN THE PAGE:** `useWorkspaceFiles(run?.thread_id ?? null)` (`:428-432`) and the comment
at `:415-427` recording that the reconcile is keyed on the OWNING thread; `onDownload` +
`runThreadId` + `downloadError` (`:549-561`); the `<h2>`; the three-way empty state (`:1030-1039`);
the `<ul role="list">`/`<li>`; the section-level `data-testid="run-download-error"` paragraph, which
does **not** auto-clear (unlike chat's 3 s in-row error — the asymmetry is deliberate).

**WHAT THIS PLAN DELETES:** `function formatBytes` (`:153-157`), `function baseName` (`:160-162`),
`function iconFor` (`:170-188`), the three `OOXML_*` consts (`:164-166`), the lucide file-glyph
imports, and the interior spans at `:1057-1064` and `:1074-1080`. `baseName` is still needed — import
it from the shared utils; `onDownload` calls it (`:556`).

**⚠ D-12's SORT KEY IS ABSENT ON EXACTLY THE FILE D-12 EXISTS TO SURFACE.** The SSE payload carries
`id/path/version/size_bytes/mime_type` only — **no `created_at`** (`lib/api.ts:845-851`) — and the store
**APPENDS** it (`StreamsProvider.tsx:2947-2954`). The list route orders by `path` and only then supplies
`created_at`. ⇒ a naive `created_at DESC` puts the just-produced deliverable **LAST**. Use
`byNewestFirst` from the shared utils, which sorts a **missing key FIRST** — and **do NOT fix this by
teaching the SSE to stamp `created_at`**: that edits `StreamsProvider.tsx` (33 phases, 4119 lines).
</interfaces>

⚠ **D-02 CHANGES ONLY THE HEADING.** `COPY_NO_FILES_TERMINAL = "This run produced no files."` looks
like the same overclaim and is **not**: a run is a subset of its thread, so an empty thread-scoped list
entails the run produced nothing. The overclaim only bites in the NON-empty direction. **D-15 says both
empty strings ship byte-identical**, and `:1489-1490` pins each at exactly one occurrence. A plan that
also rewords the empty copy breaks D-15 in D-02's name.

⚠ **The sort applies to the RUN PAGE ONLY.** D-12 sits under the run surface's many-files reading. The
panel's shipped `path` ordering is unchanged and no decision authorises changing it — say so in the
SUMMARY so a reviewer can tell "scoped" from "forgotten".

⚠ **`scripts/vitest-count-gate.cjs` is NOT in this plan's `files_modified`.** Two other plans run in
this wave; the gate file is owned by plan 195-07 in wave 4. Record this suite's new `actual` for its
pin raise. The gate stays green in the meantime — an increase is permitted, only a DECREASE reds.

⚠ **Worktree:** `bash scripts/bootstrap-worktree.sh "$(pwd)"` FIRST, then assert the dispatched base
SHA. `GSD_VITEST_MAX_WORKERS=2` on every vitest run.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert the deliverable region onto the shared row and order it newest-first (D-12, F8)</name>
  <files>frontend/src/pages/WorkflowRunPage.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowRunPage.tsx` :108-190 (the copy consts, the region docblock, the three locals), :415-432 (the files read and its recorded reconcile reasoning), :547-561 (`onDownload`), :1020-1093 (the region)
    - `frontend/src/components/files/FileRow.tsx` — the prop list, `DENSITY.run` and its empty `deadOverrides`, and the `dead` affordance's element
    - `frontend/src/components/files/fileRowUtils.ts` — `formatBytes`, `baseName`, `byNewestFirst` and its two-regime contract
    - `frontend/src/components/workflows/canvasModel.ts` :368 — the `[...list].sort()` purity rule; the provider hands out a stable ref, so an in-place sort mutates store state
    - `.planning/phases/195-show-the-deliverable/195-RESEARCH.md` F7, F8 and § "The recommended shared row" (the `run` adapter row)
    - `.planning/phases/195-show-the-deliverable/195-PATTERNS.md` § 6 — including the docblock WORDS rule and the RAW-vs-`codeOf` asymmetry
  </read_first>
  <action>
**Order the list.** Derive the rendered list with a `useMemo` over `[...files].sort(byNewestFirst)` —
never a bare `.sort()`, because the provider hands out a stable array ref and an in-place sort mutates
store state (the rule recorded at `canvasModel.ts:368`). Render from that derived list; keep
`files.length === 0` and `filesLoading` driving the empty state exactly as they do today.

**Convert each row.** For a file WITH an `id`, render a `FileRow` with `asChild`, `density="run"`,
`name={baseName(file.path)}`, `mimeType={file.mime_type}`, `sizeBytes={file.size_bytes}` and
`trailing="download"`, whose child is today's `<button type="button">` carrying `onClick={() => onDownload(file)}`,
`title={file.path}`, `aria-label={`Download ${name} (${size})`}` and its own skin classes
(`w-full text-left transition-colors hover:bg-accent focus:outline-none focus-visible:ring-1 focus-visible:ring-ring`
plus `rounded-md px-2 py-2`). ⚠ The `aria-label` must still compute to exactly
`Download renewal-letter.docx (18.4 KB)` for the shipped fixture — derive `size` from the shared
`formatBytes` so the string cannot drift.

For a file with **no `id`**, render the same `FileRow` with `trailing="dead"` and a child that is
today's plain `<div title={file.path}>`. ⚠ **This CHANGES run-page behaviour, and the change is an
improvement that must be stated rather than slipped in:** today the id-less row is a **silent**
non-interactive `<div>` with no cue — the exact ❌ pattern the chat card's dead state was built to
remove (`BUG-260523-03`). It now carries the shipped *"Download unavailable — this file has no link"*
affordance. Nothing else about that row changes: the run density's `deadOverrides` is empty, so the
name and icon keep their live colours. ⚠ **`:991`'s `toHaveLength(0)` survives ONLY because that
affordance is a `<span aria-disabled>` and not a `<button>`** — write that reason into the code, in
words, so it does not read as luck.

Delete `formatBytes`, `baseName`, `iconFor` and the three `OOXML_*` consts, and import `formatBytes`,
`baseName` and `byNewestFirst` from the shared utils. Remove every lucide file-glyph import that becomes
unused; keep whatever the rest of the page still uses. ⚠ `baseName` is still called by `onDownload`
(`:556`) — the import must cover that call site too.

**Rewrite the region's docblock** (`:127-149`). It currently explains why the page mirrors the panel's
icon mapping and byte formatter; that reason is now gone and the correction must sit **beside** the
original, not over it. State: the mapping and the formatter are no longer copied here, they are shared;
the panel's list is still NOT mounted here and the reason still holds (it resolves its thread from a
global selector rather than a prop, so mounting it would write chat's viewed thread as a side effect of
opening a run); the rows are still downloads with no preview and none is promised; and the id-less row
gained the dead affordance. ⚠ **Name every component in WORDS.** Writing `FilesSection`, `FilePreview`,
`useViewingThread` or `<StopControl` in this file reds four raw-source predicates — one of them in a
suite that has nothing to do with files.

⚠ **Assert `StopControl.baseline.test.tsx` green after EVERY edit to this file** (P9). It greps raw page
source and counts `/<StopControl/g` expecting exactly 1. It is the fence most likely to break for a
reason unrelated to its subject.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/chat/__tests__/StopControl.baseline.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function formatBytes\|function baseName\|function iconFor\|OOXML_DOCX" frontend/src/pages/WorkflowRunPage.tsx` returns **0**
    - `grep -c "FileRow\|byNewestFirst" frontend/src/pages/WorkflowRunPage.tsx` returns ≥ 2 for each token
    - `grep -cE "FilesSection|FilePreview|useViewingThread" frontend/src/pages/WorkflowRunPage.tsx` returns **0**
    - `grep -c "<StopControl" frontend/src/pages/WorkflowRunPage.tsx` returns exactly **1**
    - `grep -c "downloadWorkspaceFile(runThreadId" frontend/src/pages/WorkflowRunPage.tsx` returns exactly **1**, and `grep -c "useWorkspaceFiles(" ` returns exactly **1**
    - `grep -cE "muted-foreground-dim|--panel-" frontend/src/pages/WorkflowRunPage.tsx` returns **0**
    - `grep -c "\[\.\.\.files\]\.sort\|\[\.\.\.\(files\)\]" frontend/src/pages/WorkflowRunPage.tsx` shows the sort is over a COPY; a bare `files.sort(` appears **0** times
    - `COPY_NO_FILES_LIVE` and `COPY_NO_FILES_TERMINAL` string literals are byte-unchanged (`git diff` shows no change to either line)
    - `StopControl.baseline.test.tsx` reports 15 passed / 0 failing
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` equals the `195-BASELINE.md` figure
  </acceptance_criteria>
  <done>The region's rows are the shared row, its list is newest-first with the missing-key regime handled, the three locals are gone, and the four raw-source predicates still hold.</done>
</task>

<task type="auto">
  <name>Task 2: Relabel the region honestly (D-02, three locations) and pin the three-way empty state (D-15, P1, P5)</name>
  <files>frontend/src/pages/WorkflowRunPage.tsx, frontend/src/pages/WorkflowRunPage.test.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowRunPage.tsx` :117-125 (the copy consts, including the comment that already calls the heading *"the deliverable region's identity. Not a claim about contents"*) and :1029-1039
    - `frontend/src/pages/WorkflowRunPage.test.tsx` :893-1035 — both heading assertions (`:897`, `:1023`) and the three empty-state cases at :1000-1032
    - `.planning/phases/195-show-the-deliverable/195-CONTEXT.md` D-01, D-02, D-15 and the Claude's-Discretion note on the exact wording
    - `.planning/phases/195-show-the-deliverable/195-RESEARCH.md` F3, F4 · `195-VALIDATION.md` P1, P5 · Pitfall 7
  </read_first>
  <action>
**Change `COPY_DELIVERABLE_HEADING` and nothing else about the copy.** The heading must name the run's
**workspace** rather than claim authorship of every file listed — D-01 keeps the read thread-scoped, so
the list can legitimately contain files this run did not write (measured: exact for 60 of 61
file-bearing runs, visibly wrong for one). The exact wording is Claude's discretion; it must (a) not
assert the run produced the listed files, and (b) remain a short region identity, not a sentence.
Update the constant at `:122` **and both test assertions at `:897` and `:1023`** — three locations, one
commit. Extend the existing comment above the constant to record D-02's reasoning and to quote the old
string, beside the original rather than over it.

⚠ **DO NOT TOUCH `COPY_NO_FILES_LIVE` or `COPY_NO_FILES_TERMINAL`.** *"This run produced no files."*
reads like the same overclaim and is not: a run is a subset of its thread, so an EMPTY thread-scoped
list entails the run produced nothing. D-15 ships both strings byte-identical, and `:1489-1490` pins
each at exactly one occurrence in page source. Record that reasoning in the plan's SUMMARY so the next
reader does not "fix" it.

**P5 — the label fence, two plants plus a word-level sweep.** A `toBe(newCopy)` equality passes on any
wrong copy that happens to be the new one, so add
`expect(heading).not.toMatch(/this run (produced|made|created)/i)` alongside the equality, and drive it
against **three** different overclaiming strings. ⚠ **Scope the sweep to the `<h2>`, never to the
region's whole `textContent`** — the terminal empty copy legitimately contains *"This run produced no
files."*, so a region-wide sweep **reds on correct code**. Write that reason in a comment.
Plants: (a) revert the const to `"What this run produced"` → the equality case must red; (b) three
overclaiming variants → the word-level case must red on each.

**P1 — the three-way empty state, THREE plants because the arms are mutually exclusive.** The three
shipped cases at `:1000-1032` must stay green through the conversion. Drive:
(a) force the ternary at `:1037` to always-terminal → the LIVE case must red;
(b) force always-live → the TERMINAL case must red;
(c) **delete the `filesLoading ? null :` guard at `:1035`** → the *"claims NEITHER while the first read
is in flight"* case must red.
⚠ **(c) is the one most likely to be inert**, because its assertion is a `not.toContain` that passes
trivially if the fixture's loading flag is unset. **Before trusting it, verify the fixture actually
sets the loading flag** (the `setFiles([], true)`-shaped call around `:1025`). If it does not, fix the
fixture first and say so — an inert plant is a FINDING, not a formality.

Restore every plant md5-identical and record before/after sums, the failing case NAMES and the RED
output line.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/pages/WorkflowRunPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "What this run produced" frontend/src/pages/WorkflowRunPage.tsx frontend/src/pages/WorkflowRunPage.test.tsx` returns **0** for both files, except inside a quoted historical comment (which must be present — `grep -c` on the comment block returns ≥ 1)
    - The new heading string appears exactly once in the page source and exactly twice in the suite
    - `git diff -- frontend/src/pages/WorkflowRunPage.tsx` shows **no change** to the `COPY_NO_FILES_LIVE` or `COPY_NO_FILES_TERMINAL` lines
    - The suite contains a `not.toMatch(/this run (produced|made|created)/i)` assertion scoped to the `h2` element, not to the region
    - Five plants recorded (P5 ×2 including the three overclaiming variants, P1 ×3) each with the failing case NAMES, the RED output and md5 before == after
    - The P1(c) record states explicitly whether the loading fixture already set the flag, and what was done if it did not
    - `WorkflowRunPage.test.tsx` green, 0 failing
  </acceptance_criteria>
  <done>The region's label no longer claims a scope the thread-scoped read cannot deliver, the two empty-state strings are provably untouched, and all three arms of the most-seen state on this surface have been shown to be able to fail.</done>
</task>

<task type="auto">
  <name>Task 3: Invert the two fences that assert the duplication, invert the F8 capture, and add the D-12 ordering cases (F1, P4, P8)</name>
  <files>frontend/src/pages/WorkflowRunPage.test.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowRunPage.test.tsx` :1052-1063 (`codeOf` and its self-proving stripper case), :1425-1492 (the whole source-fence describe, including the two inverting arms and the existing positive controls at :1470-1471 / :1482)
    - `frontend/src/components/chat/__tests__/StopControl.baseline.test.tsx` :513-605 — the 194.1-07 invert-in-place precedent: the ⚠ SUPERSEDED banner, the verbatim quotation of the retired assertions, and the planted positive control
    - `.planning/phases/195-show-the-deliverable/195-02-SUMMARY.md` — the F8 pre-change capture block this task inverts
    - `.planning/phases/195-show-the-deliverable/195-VALIDATION.md` § "The fence that INVERTS (F1)", rows P4 and P8
    - `.planning/phases/195-show-the-deliverable/195-RESEARCH.md` § "`WorkflowRunPage.test.tsx` — 102 cases", and Pitfall 1
  </read_first>
  <action>
**(1) Invert F1 in place.** In the `it("neither mounts the panel's file list nor names it …")` case,
replace the two `toMatch(/function formatBytes/)` / `toMatch(/function iconFor/)` arms with their
negations over `codeOf(pageSource)`, and add the mirrored positive arms proving the shared module now
holds them (a `?raw` import of `fileRowUtils.ts` / `fileIcon.tsx` with a length AND identity guard —
192.1 measured a renamed module swept against the empty string, passing green).
⚠ **Do NOT delete the case and do NOT delete an `it(`.** The suite is pinned EXACT; a deletion trips
`[count-decrease]` and needs a pin lowering this plan does not authorise. Inverting inside the same
`it()` keeps the count where it is.
Apply the 194.1-07 banner shape: a ⚠ SUPERSEDED-IN-PLACE block quoting **both retired assertions
verbatim**, naming Phase 195 and the reason (the duplication they asserted is what RUN-03 removed), and
stating that a capture deleted the moment it inverts leaves no record that the duplication was ever
real (193.2 WR-05).
⚠ **Keep the four absence arms and their existing positive controls at `:1470-1471` / `:1482` exactly as
they are** — the needles are assembled from parts (`["Files","Section"].join("")`) precisely so the test
file's own source cannot satisfy them.

**(2) Invert plan 02's F8 capture in place**, using the same banner shape. The id-less row now carries
the shipped *"Download unavailable — this file has no link"* affordance, so:
  - the "no copy" case becomes a "HAS the copy" case, with the original quoted verbatim;
  - the "zero buttons" case **stays as it is** — the affordance is a `<span aria-disabled>`, and this is
    the assertion that proves it. Add a comment saying that in words, because a reader who does not know
    it will read `toHaveLength(0)` next to a visible download affordance as a bug.
Record the delta in one sentence: *the run page's id-less row went from a silent fact to a legible
unavailable one, and gained nothing else.*

**(3) P8 — three plants, each shown to red SEPARATELY.** Into `frontend/src/pages/WorkflowRunPage.tsx`,
one at a time, restoring md5-identical each time: (a) `import { FilesSection } …`, (b) `const t = useViewingThread()`,
(c) `import { FilePreview } …`. Each must red its own arm. They are distinct `expect`s, so one plant
cannot exercise all three.

**(4) P4 in situ — the ordering the region now applies.** The shared comparator has unit coverage from
plan 03; this suite must prove the REGION renders in that order. Add:
  - a two-row fixture with **differing** `created_at` values whose unsorted order is oldest-first →
    assert the rendered `li` order is newest-first (⚠ the positive control: without a fixture whose two
    orders differ, the case passes on a one-row list forever — the `PendingAskCard.test.tsx:205-222`
    shape);
  - ⚠ a two-row fixture where the **newest row has `created_at: undefined`** — the live-SSE regime, which
    is the regime the just-produced deliverable actually arrives in — asserting it renders FIRST.
Plants: remove the `.sort` from the page → both cases red; invert the comparator to ASC → the first case
reds; make the missing-key arm sort `undefined` LAST → **the second case must red**. That third plant is
what F7 exists for.

⚠ Then run the count gate and the ungated command, and **record `WorkflowRunPage.test.tsx`'s new
`actual`** for plan 195-07's pin raise. Do not edit `scripts/vitest-count-gate.cjs` here.
⚠ **Assert `StopControl.baseline.test.tsx` green again** — this task plants into the page four more
times.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/pages/WorkflowRunPage.test.tsx src/components/chat/__tests__/StopControl.baseline.test.tsx src/components/files/__tests__/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "toMatch(/function formatBytes/)\|toMatch(/function iconFor/)" frontend/src/pages/WorkflowRunPage.test.tsx` returns **0** outside the quoted historical block, and the quoted block containing both original lines verbatim is present (`grep -c` on it returns ≥ 2)
    - `git diff --numstat -- frontend/src/pages/WorkflowRunPage.test.tsx` shows a **net increase** in `it(` count; `grep -c "  it(" ` is greater than the value plan 02 recorded
    - Two ⚠ SUPERSEDED-IN-PLACE banner blocks exist, each naming Phase 195 and quoting its retired assertions verbatim
    - The four absence arms and the two existing positive controls at `:1470-1471` / `:1482` are byte-unchanged
    - The suite contains a case whose fixture's newest row has `created_at: undefined` and asserts it renders at `li` index 0
    - The suite contains a two-row fixture whose unsorted and rendered orders differ
    - Every `?raw` import added carries a `.length` assertion AND a `toContain` identity assertion
    - Seven plants recorded (P8 ×3, P4 ×3, plus the F8 affordance plant) with failing case NAMES, RED output and md5 before == after
    - `WorkflowRunPage.test.tsx` green, 0 failing; `StopControl.baseline.test.tsx` green at 15
    - Count gate VERDICT line quoted: `OK`, `failed 0`, **no `[count-decrease]`**; the new `WorkflowRunPage.test.tsx` `actual` recorded for plan 07
    - `scripts/vitest-count-gate.cjs` byte-unchanged by this plan (`git diff --numstat` EMPTY)
  </acceptance_criteria>
  <done>The two fences that asserted the duplication now assert its removal with the originals preserved, the pre-change dead-row capture is inverted with the improvement recorded as a delta, and the ordering is pinned in the regime the deliverable actually arrives in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `run.thread_id` → `useWorkspaceFiles` → `GET /threads/{tid}/workspace/files` | ⚠ **the security-relevant boundary of this plan.** Two independent ownership gates stand in front of it: the run read that yielded the thread id, and the route's own `_verify_thread_ownership` plus RLS |
| `runThreadId` + `file.id` → `downloadWorkspaceFile` → `/threads/{tid}/workspace/files/{fid}/raw` | bearer-authed; 401 / 404 copy identical to the sandbox seam; 404 deliberately collapses missing-and-IDOR |
| model/agent-derived `file.path` → the label, the `title` and the `aria-label` | text/attribute sinks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-195-06-01 | Information Disclosure | which thread's files this page lists and downloads | **mitigate** | The page must keep reading `run?.thread_id` and must keep passing `runThreadId` into `downloadWorkspaceFile`. This is exactly what the shipped fence at `:1450-1455` pins (`useWorkspaceFiles(run?.thread_id`, `downloadWorkspaceFile(runThreadId`, each at exactly one occurrence), and it is why this plan refuses to route the download through a shared dispatcher: doing so would drop both needles to zero and force inverting a fence that guards a tenancy property. Verified by the two `grep -c … == 1` acceptance criteria. |
| T-195-06-02 | Information Disclosure | the shared row acquiring a thread resolver | **mitigate** | `FileRow` receives no thread id and reads no hook (`grep -c useViewingThread\|useWorkspaceFiles` on `FileRow.tsx` → 0, plan 03). If the row could resolve the globally-viewed thread, opening a run would list and offer for download a DIFFERENT thread's files — the hazard `WorkflowRunPage.tsx:146-149` already documents. Pinned twice: at the row, and by `:1464` over this page's raw source. |
| T-195-06-03 | Spoofing / Elevation of Privilege | the id-less row's new affordance | **mitigate** | The affordance is a `<span aria-disabled="true">`, so it is not focusable and not clickable; there is no handler behind it. The authorization gate remains the code-level `if (!runThreadId \|\| !fileId) return` inside `onDownload` (`:554`), **not** the visual state. This plan must not turn the dead affordance into a `<button>`, and `:991`'s zero-button assertion is the mechanical check. |
| T-195-06-04 | Tampering (XSS) | `file.path` in the label, `title` and `aria-label` | mitigate | All three are React text/attribute values, never markup; `WorkflowRunPage.tsx` must contain zero `dangerouslySetInnerHTML` (assert `grep -c` → 0). The `aria-label` is template-interpolated from `baseName` and `formatBytes` output only. |
| T-195-06-05 | Repudiation | the region's honesty | **mitigate** | D-02 is a security-adjacent honesty fix rather than copywriting: a heading claiming the run produced files it did not produce misattributes provenance on the one surface a reviewer uses to hand work over. The word-level fence in task 2 is the mitigation, and its scope is deliberately the `<h2>` so it cannot red on the legitimately-worded empty state. |
| T-195-06-SC | Tampering | package installs | n/a | **No package installed.** `frontend/package.json` and `package-lock.json` untouched — assert `git diff --numstat` EMPTY. No `slopcheck` owed. |

**Honest scale statement:** this plan adds no route, no fetch and no persistence — but it is the one
plan in the phase that touches a real tenancy boundary, and the honest security content is a set of
properties that must **not** move. They are pinned by shipped fences plus `grep` criteria rather than
asserted in prose.
</threat_model>

<verification>
- `WorkflowRunPage.test.tsx` green with a net `it(` increase; both inverting arms rewritten in place with the originals quoted; the four absence arms untouched
- `StopControl.baseline.test.tsx` green at 15 after every page edit (P9)
- `formatBytes`, `baseName`, `iconFor` and the `OOXML_*` consts gone from the page; `FileRow` and `byNewestFirst` present
- `useWorkspaceFiles(` and `downloadWorkspaceFile(runThreadId` each appear exactly once in page source
- Both empty-state literals byte-unchanged; the heading changed in all three locations
- The ordering is pinned in BOTH regimes, including `created_at: undefined` first
- All plants across the three tasks observed RED with failing case names and md5 before == after
- Count gate `OK` / `failed 0` / no `[count-decrease]`; ungated command green; `tsc` at the baseline figure; the gate script untouched
</verification>

<success_criteria>
A completed run's deliverable is listed, newest first, on the shared row, from a region whose label is
honest about what it lists — and every fence the change touched is either still green or inverted in
place with its original preserved.
</success_criteria>

<output>
Create `.planning/phases/195-show-the-deliverable/195-06-SUMMARY.md` when done.
</output>
