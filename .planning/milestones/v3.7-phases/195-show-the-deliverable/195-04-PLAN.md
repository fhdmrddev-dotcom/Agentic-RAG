---
phase: 195-show-the-deliverable
plan: 04
type: execute
wave: 3
depends_on: ["195-03"]
files_modified:
  - frontend/src/components/chat/OutputFileCard.tsx
autonomous: true
requirements: [RUN-03]

must_haves:
  truths:
    - "The chat output-file card renders from the shared row, and nothing about it looks or behaves differently"
    - "OutputFileCard's public prop shape is byte-identical, so both of its call sites are never opened"
    - "The dead-link state and the supersedes subline still render in both branches, proved by fences authored before the change"
    - "Chat gains NO new file affordance — the presentation converted, the capability did not"
  artifacts:
    - path: "frontend/src/components/chat/OutputFileCard.tsx"
      provides: "The chat adapter: unchanged public props, unchanged download + error behaviour, body re-implemented on FileRow"
      contains: "FileRow"
  key_links:
    - from: "frontend/src/components/chat/OutputFileCard.tsx"
      to: "frontend/src/components/files/FileRow.tsx"
      via: "asChild — the card supplies its own <a href download> and its own <div data-dead> wrapper"
      pattern: "FileRow"
    - from: "frontend/src/components/chat/OutputFileCard.tsx"
      to: "frontend/src/lib/api.ts downloadSandboxOutput"
      via: "the download call and its in-row auto-clearing error stay in this component"
      pattern: "downloadSandboxOutput"
---

<objective>
Re-implement `OutputFileCard`'s body on the shared row with **zero behaviour change in chat** and a
**byte-identical public prop shape**.

Purpose: D-07 says all three surfaces convert in this phase — leaving two presentations is what SC#2
forbids. D-13 says chat gains no NEW capability. ⚠ **These are compatible and must not be conflated:
presentation converts, capability does not.** And keeping the prop shape byte-identical is what keeps
`MessageItem.tsx` (29 phases, extraction due, obligation UNDISCHARGED) and
`tool-bodies/ExecuteCodeBody.tsx` shut — D-14's decline honoured **structurally** rather than by
discipline.
Output: one converted file. `files_modified` is deliberately a single entry.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@.planning/phases/195-show-the-deliverable/195-CONTEXT.md
@.planning/phases/195-show-the-deliverable/195-RESEARCH.md
@.planning/phases/195-show-the-deliverable/195-PATTERNS.md
@.planning/phases/195-show-the-deliverable/195-VALIDATION.md
@.planning/phases/195-show-the-deliverable/195-BASELINE.md
@.planning/phases/195-show-the-deliverable/195-02-SUMMARY.md
@.planning/phases/195-show-the-deliverable/195-03-SUMMARY.md

@frontend/src/components/chat/OutputFileCard.tsx
@frontend/src/components/files/FileRow.tsx
@frontend/src/components/files/fileRowUtils.ts

<interfaces>
<!-- What must NOT move, stated as a contract. -->

**The public prop shape — BYTE-IDENTICAL, and `OutputFileCardProps` stays UNEXPORTED**
(`OutputFileCard.tsx:39-64`): `file: { filename; url?; size?; supersedes?; is_hero? }`, `variant?:
"hero" | "working"` defaulting to `"working"`. **Do not add a prop. Do not remove one. Do not export
the interface** — exporting it is itself a public-surface change, and both call sites pass only
`file`, so widening buys nothing and opens a 29-phase G-5 file.

**The two call sites, which must end this plan BYTE-UNCHANGED:**
`frontend/src/components/chat/MessageItem.tsx:158` and
`frontend/src/components/chat/tool-bodies/ExecuteCodeBody.tsx:358`

**What stays in this component, verbatim:**
  - `API_BASE` read from `import.meta.env.VITE_API_BASE_URL` (`:13`) and `resolveOutputUrl` (`:15-23`)
    — same env var, same value, no `.env` and no `deploy/onebox.env.example` edit owed
  - `downloading` / `downloadError` state, `handleClick`'s `e.preventDefault()` plus
    `downloadSandboxOutput`, the `DownloadError` catch with status-specific copy, and the **3 s
    auto-clear** (`:122-141`)
  - the static `<a href download target="_blank" rel="noopener noreferrer">` — preserved so right-click
    *"Save link as"* has a real target, a recorded D-067.3-R2-03 trade-off
  - `aria-disabled={downloading}` and `data-variant={variant}` on the live root; `data-variant` plus
    `data-dead="true"` on the dead root
  - the inert `variant` and the unread `is_hero`, both untouched (D-095.1-06)

**What this plan DELETES:** the local `function formatBytes` (`:25-29`) — the row formats the size cell
from `sizeBytes` — and the hand-written interior spans (`:98-119`, `:161-184`), which become
`FileRow`'s children.
</interfaces>

⚠ **Two suites guard this change and their assertions MUST NOT be edited by this plan:**
`frontend/src/components/chat/__tests__/OutputFileCard.baseline.test.tsx` (wave 1) and
`frontend/src/__tests__/components/MessageItem.finalOutputs.test.tsx` (shipped). If a case needs
changing to go green, the conversion changed behaviour and the conversion is wrong. `files_modified`
lists neither file on purpose.

⚠ **Worktree:** `bash scripts/bootstrap-worktree.sh "$(pwd)"` FIRST, then assert the dispatched base
SHA before reading the plan. `GSD_VITEST_MAX_WORKERS=2` on every vitest run.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-implement both branches on FileRow, public props untouched</name>
  <files>frontend/src/components/chat/OutputFileCard.tsx</files>
  <read_first>
    - `frontend/src/components/chat/OutputFileCard.tsx` (all 187 lines)
    - `frontend/src/components/files/FileRow.tsx` — the prop list, the `DENSITY` record's `chat` entry and its `deadOverrides`, and the slot order
    - `frontend/src/components/files/fileRowUtils.ts` — the `FileSource` type
    - `frontend/src/components/chat/__tests__/OutputFileCard.baseline.test.tsx` — the contract this conversion must satisfy without editing it
    - `frontend/src/components/ui/button.tsx` :42-53 — how a caller supplies an element to an `asChild` component
    - `.planning/phases/195-show-the-deliverable/195-PATTERNS.md` § 4
  </read_first>
  <action>
Convert the body, keeping the two branches as two branches.

**Live branch** (`file.url` present): render a `FileRow` with `asChild`, `density="chat"`,
`name={file.filename}`, `sizeBytes={file.size}`, `supersedes={file.supersedes}`,
`errorText={downloadError?.message ?? null}` and `trailing={downloading ? "spinner" : "download"}`,
whose single child is today's anchor — `href={resolveOutputUrl(file.url)}`, `download={file.filename}`,
`target="_blank"`, `rel="noopener noreferrer"`, `onClick={handleClick}`,
`aria-disabled={downloading}`, `data-variant={variant}`.

Split today's single `cn(...)` between the two. `FileRow`'s `chat` density already owns
`flex items-center gap-2.5`, so the anchor keeps everything else it has today:
`px-3 py-2 rounded-md text-xs transition-colors group ghost-border`, the
`downloading ? "opacity-60 cursor-not-allowed" : "bg-muted/30 hover:bg-accent/40"` pair, and the
`downloadError ? "border border-red-500/40" : ""` arm. ⚠ **Radix `mergeProps` JOINS `className`; it
does not twMerge** — verify no class left on the anchor competes with a class the density owns. If one
does, that is a finding to record and route, **not** a licence to edit `FileRow.tsx`: this plan runs in
the same wave as two others and must not touch a wave-2 file.

**Dead branch** (`!file.url`): the same `FileRow` with `density="chat"`, `trailing="dead"`, `sizeBytes`
omitted (today's dead branch renders no size) and `supersedes` passed, whose child is today's
`<div data-variant={variant} data-dead="true">` carrying its own
`ghost-border text-xs px-3 py-2 bg-muted/30` skin. The affordance's element, its `aria-disabled` and
its exact `title` and copy now come from `FileRow`'s `dead` arm and must render identically — which is
exactly what the wave-1 fence checks.

Delete the local `formatBytes` (`:25-29`) and do **not** import it here: a stray reference would leave
a second `formatBytes` token in a file plan 07's sweep reads. Remove any lucide import that becomes
unused so `tsc` stays at the baseline; keep every import the retained logic still needs.

Update the docblock: state that the interior now comes from the shared row, that the public prop shape
is unchanged **and why** (both call sites stay shut; `MessageItem.tsx` is 29 phases with an
undischarged extraction obligation), and that D-13 holds — **no new affordance was added, only the
markup's home changed.** ⚠ Keep the recorded reasons for `resolveOutputUrl`'s local `API_BASE` read and
for the preserved static `href`. If a sentence would now be wrong, correct it **beside** the original
rather than over it (the house pattern, three shipped instances).
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/chat/__tests__/OutputFileCard.baseline.test.tsx src/__tests__/components/MessageItem.finalOutputs.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `OutputFileCard.baseline.test.tsx` passes at the exact count plan 02 recorded, 0 failing, **with zero edits to that file** (`git diff --numstat -- frontend/src/components/chat/__tests__/OutputFileCard.baseline.test.tsx` EMPTY)
    - `MessageItem.finalOutputs.test.tsx` passes at 11, 0 failing, unedited
    - `grep -c "formatBytes" frontend/src/components/chat/OutputFileCard.tsx` returns **0**
    - `grep -c "FileRow" frontend/src/components/chat/OutputFileCard.tsx` returns ≥ 2 (import plus at least one use)
    - `git diff -- frontend/src/components/chat/OutputFileCard.tsx` shows **no changed line** containing any of `filename`, `url?`, `size?`, `supersedes?`, `is_hero?`, `variant?: "hero" | "working"` inside the props interface
    - `grep -c "export interface OutputFileCardProps\|export type OutputFileCardProps" frontend/src/components/chat/OutputFileCard.tsx` returns 0
    - All five of these tokens are still present in the file: `downloadSandboxOutput`, `resolveOutputUrl`, `VITE_API_BASE_URL`, `data-dead`, `aria-disabled`
    - `grep -c "setTimeout" frontend/src/components/chat/OutputFileCard.tsx` returns ≥ 1 (the 3 s auto-clear survived)
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` equals the `195-BASELINE.md` figure
  </acceptance_criteria>
  <done>The chat card's interior is the shared row; its props, its download, its error timing and its two roots are unchanged, and the fences written before the change pass untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the invariants — call sites byte-unchanged against a NAMED base, and re-drive the D-08 plants against the converted body</name>
  <files>frontend/src/components/chat/OutputFileCard.tsx</files>
  <read_first>
    - `.planning/phases/195-show-the-deliverable/195-BASELINE.md` — the `BASE_SHA` and the two recorded md5 sums
    - `.planning/phases/195-show-the-deliverable/195-02-SUMMARY.md` — the six plants and their RED output, to reproduce against the new body
    - `.planning/phases/195-show-the-deliverable/195-VALIDATION.md` rows P2, P3 and P7
  </read_first>
  <action>
Two proofs, both against the converted file.

**(1) The call sites are byte-unchanged, against a NAMED base.** Read `BASE_SHA` from
`195-BASELINE.md` — never `HEAD~n`, never the worktree's assumed fork point. Run
`git diff --numstat <BASE_SHA> HEAD -- frontend/src/components/chat/MessageItem.tsx frontend/src/components/chat/tool-bodies/ExecuteCodeBody.tsx`
and require EMPTY output. ⚠ **This check passes trivially when the base SHA is wrong** — 7/7 worktrees
forked from the wrong base in Phase 194 and 8/8 in 194.1, so a wrong base is DEFAULT behaviour here,
not a curiosity. Therefore also assert `git merge-base --is-ancestor <BASE_SHA> HEAD` succeeds, and
compare both files' current `md5sum` against the two values `195-BASELINE.md` recorded. Then **plant a
one-character edit into `MessageItem.tsx`** and prove the numstat check FAILS — that is P7(a)'s plant,
and without it the check is a formality — then restore md5-identical.

**(2) Re-drive the D-08 plants against the converted body.** A fence can go inert when the code it
guards moves; that is exactly how 5 inert fences shipped in 193.2 and 4 in 193.1. Reproduce each of
the six plants plan 02 drove and confirm each still reds the same NAMED case:
  - the live-branch `supersedes` path → the live case reds, the dead case stays green
  - the dead-branch `supersedes` path → the dead case reds, the live case stays green
  - the `Replaces: ` literal → the literal case reds
  - `data-dead="true"` removed → the attribute case reds
  - the `Download unavailable` copy changed → the copy case reds
  - `variant` made to change a class → the inert-variant case reds

⚠ Several of these now live in `FileRow.tsx` rather than in `OutputFileCard.tsx`. **Plant where the
code now is**, name the file in the record, and list which plants MOVED. That migration IS this phase's
structural claim, and recording it is how a reviewer can see the fences followed the code. Restore
every file md5-identical and record before and after sums.

Also assert **P7(c)** — no new interactive element appeared in a chat row: from the baseline suite's
rendered output, the live row's root is the `<a>` and the row contains zero `<button>` elements, and the
dead row contains zero `<button>` elements.

⚠ **A plant that produces a SKIP has proven nothing** (194-12). ⚠ **Read WHICH cases failed, never
just how many** (194-06). ⚠ **A fence that fails to red is a FINDING** — record it and fix the fence
rather than smoothing it over.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/chat/__tests__/OutputFileCard.baseline.test.tsx src/__tests__/components/MessageItem.finalOutputs.test.tsx src/components/files/__tests__/</automated>
  </verify>
  <acceptance_criteria>
    - `git merge-base --is-ancestor <BASE_SHA> HEAD` exits 0, and the BASE_SHA used is quoted in the SUMMARY
    - `git diff --numstat <BASE_SHA> HEAD -- MessageItem.tsx ExecuteCodeBody.tsx` output is EMPTY, and both files' md5 match `195-BASELINE.md`
    - The P7(a) plant is recorded: the one-character edit, the non-empty numstat output it produced, and md5 restored
    - All six D-08 plants recorded with the file planted, the failing case NAMES, the RED output line, and md5 before == after
    - The record explicitly lists which plants moved from `OutputFileCard.tsx` to `FileRow.tsx`
    - `src/components/files/__tests__/` still green at the counts plan 03 recorded
    - Count gate run and its VERDICT line quoted: `OK`, `failed 0`, no `[count-decrease]`
    - The explicit ungated command from `195-VALIDATION.md` green
  </acceptance_criteria>
  <done>The two call sites are proved byte-unchanged against a base that was itself proved, and every D-08 fence has been shown to still fire against the code in its new home.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `file.url` (backend `harvest_output_files`) → the anchor's `href` | the one boundary where a backend-supplied string becomes a navigable URL |
| model/sandbox-derived `filename` / `supersedes` → the render layer | text sinks inside the shared row |
| bearer-authed `downloadSandboxOutput` → `GET /sandbox-outputs/...` | unchanged, shipped; 404 deliberately collapses missing-and-IDOR (`sandbox_outputs.py:48-67`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-195-04-01 | Tampering (XSS) | `filename` / `supersedes` rendering | mitigate | Both remain React text children via the shared row; `OutputFileCard.tsx` must contain zero `dangerouslySetInnerHTML` (assert `grep -c` → 0). The wave-1 baseline suite's markup-as-text case covers the sink. |
| T-195-04-02 | Tampering (XSS via URL scheme) | `resolveOutputUrl(file.url)` → `href` | **accept** | ⚠ Stated rather than glossed: `resolveOutputUrl` passes through any `url` not starting with `/`, so a `javascript:`-scheme value would be a real sink. It is **pre-existing, unchanged by this plan, and out of scope by D-08** — this phase refactors presentation and is forbidden from re-deciding behaviour. `file.url` is emitted by the backend's `harvest_output_files`, not by a user. **Accepted with a named re-open trigger and a seed filed in plan 195-08: any change that lets a user or a connector supply `OutputFile.url`, or any phase that opens `resolveOutputUrl` for another reason, adds a scheme allow-list in the same commit.** |
| T-195-04-03 | Elevation of Privilege | the `dead` affordance | mitigate | `aria-disabled` is presentational. The real gate is the `if (!file.url)` branch itself — a url-less file renders the dead root and **no anchor exists to click**. That structural property is what the wave-1 fence pins, and this plan must not replace it with a styled-but-clickable control. |
| T-195-04-04 | Information Disclosure | the shared row acquiring data access | mitigate | `FileRow` receives no thread id, no file id and no URL — verified by plan 03's `grep` for `useWorkspaceFiles`/`useViewingThread` returning 0 in that file. The download call stays here, so there is no path by which chat's row could resolve or fetch another thread's file. |
| T-195-04-SC | Tampering | package installs | n/a | **No package installed.** `frontend/package.json` and `package-lock.json` untouched; assert `git diff --numstat` EMPTY for both. No `slopcheck` owed. |

**Honest scale statement:** one file changes and it changes only where its markup lives. The single
finding worth writing down is T-195-04-02, and it is recorded as an ACCEPTED pre-existing risk with a
trigger — not mitigated here, because D-08 forbids changing behaviour in this phase and a silent
mitigation would be exactly the kind of scope creep the phase is guarded against.
</threat_model>

<verification>
- Both guarding suites green and **unedited**
- `formatBytes` gone from `OutputFileCard.tsx`; `FileRow` present; props byte-identical; props still unexported
- `MessageItem.tsx` and `ExecuteCodeBody.tsx` byte-unchanged against the named `BASE_SHA`, with the P7(a) plant driven
- All six D-08 plants re-driven RED against the converted code, with the migration recorded
- Count gate `OK` / `failed 0`; ungated command green; `tsc` at the baseline figure
</verification>

<success_criteria>
Chat renders the shared row and a user cannot tell — same shape, same download, same 3 s error, same
dead state, same `Replaces:` subline — and the two files that consume this component were never opened.
</success_criteria>

<output>
Create `.planning/phases/195-show-the-deliverable/195-04-SUMMARY.md` when done.
</output>
