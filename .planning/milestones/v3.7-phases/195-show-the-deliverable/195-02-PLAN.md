---
phase: 195-show-the-deliverable
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/chat/__tests__/OutputFileCard.baseline.test.tsx
  - frontend/src/pages/WorkflowRunPage.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [RUN-02, RUN-03]

must_haves:
  truths:
    - "OutputFileCard's `supersedes` subline is covered by a test in BOTH the live branch and the dead branch, for the first time in the repository"
    - "OutputFileCard's dead-link state is proved to red on the attribute clause AND on the copy clause separately"
    - "The run page's id-less row is characterized as it ships today — no control and no copy — so the improvement plan 06 makes is a measured delta"
    - "Four suites that guard this phase's files are inside the count gate, or their exclusion is recorded with a reason"
  artifacts:
    - path: "frontend/src/components/chat/__tests__/OutputFileCard.baseline.test.tsx"
      provides: "First direct coverage of OutputFileCard — supersedes (both branches), the dead-link pair, the inert data-variant"
      contains: "Replaces:"
    - path: "scripts/vitest-count-gate.cjs"
      provides: "File-level TARGETS + BASELINE entries adopting five suites, with the adoption reasoning recorded in comments"
      contains: "OutputFileCard.baseline.test.tsx"
  key_links:
    - from: "frontend/src/components/chat/__tests__/OutputFileCard.baseline.test.tsx"
      to: "frontend/src/components/chat/OutputFileCard.tsx"
      via: "direct render of the component, url-bearing and url-less fixtures"
      pattern: "OutputFileCard"
    - from: "scripts/vitest-count-gate.cjs"
      to: "frontend/src/components/chat/__tests__/StopControl.baseline.test.tsx"
      via: "a file-level TARGETS entry, so P9's fence stops being invisible to CI"
      pattern: "StopControl.baseline.test.tsx"
---

<objective>
Author the fences this phase's byte-identity claims rest on, against the **UNMOVED** tree, and drive
each one RED against a real edit to production source.

Purpose: two of the states D-08 declares must survive have **no coverage anywhere in the repository**
(`supersedes`, `data-variant` — F5), and the run page's id-less row is a **silent dead row** whose
behaviour plan 06 deliberately improves (F8). Neither claim can be checked after the fact. And four of
this phase's five suites sit **outside the count gate**, so a green gate would say nothing about them.
Output: one new gated suite, one pre-change characterization block in the run page's suite, and the
gate-adoption decision made and recorded rather than left implicit.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@.planning/phases/195-show-the-deliverable/195-CONTEXT.md
@.planning/phases/195-show-the-deliverable/195-RESEARCH.md
@.planning/phases/195-show-the-deliverable/195-PATTERNS.md
@.planning/phases/195-show-the-deliverable/195-VALIDATION.md

@frontend/src/components/chat/OutputFileCard.tsx
@frontend/src/__tests__/components/MessageItem.finalOutputs.test.tsx
@frontend/src/pages/WorkflowRunPage.test.tsx

<interfaces>
<!-- The component under characterization. Its props are NOT exported, so a fixture must be an
     inline literal and any prop-set fence must be a SOURCE sweep. Extracted so the executor does
     not need to explore. -->

frontend/src/components/chat/OutputFileCard.tsx:39 — **`interface OutputFileCardProps` is NOT
exported.** The shape (do not widen it, do not export it — that is itself a public-surface change):

    file: {
      filename: string
      url?: string
      size?: number
      supersedes?: string
      is_hero?: boolean
    }
    variant?: "hero" | "working"

Two branches, and `supersedes` is written in BOTH:
  - dead branch (`!file.url`), `:92-119` — root `<div data-variant={variant} data-dead="true">`,
    icon wrapper `opacity-70`, name `text-foreground/60`, subline at `:101-105`, and the affordance
    at `:107-117` which is a **`<span aria-disabled="true" title="Download unavailable — this file
    has no link">` containing the text `Download unavailable` — NOT a `<button>`**
  - live branch, `:143-185` — root `<a href download target="_blank" data-variant={variant}>`,
    subline at `:168-172`, in-row error at `:173-175`, size only when `file.size != null`

Exported symbol: `export function OutputFileCard({ file, variant = "working" })` (`:74`)

frontend/src/pages/WorkflowRunPage.tsx:1066-1082 — the id-less row TODAY: a plain
`<div title={file.path}>` with an icon, the name, the size, **no control and no copy**.
</interfaces>

⚠ **THE ORDER MATTERS AND IT IS THE WHOLE POINT.** This plan runs in wave 1, against the tree
`195-01` measured. A characterization written after the conversion characterizes the conversion.

⚠ **Worktree:** `bash scripts/bootstrap-worktree.sh "$(pwd)"` is the FIRST command, before anything
else. `GSD_VITEST_MAX_WORKERS=2` on every vitest invocation.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author OutputFileCard.baseline.test.tsx — the first direct coverage (P3, P2)</name>
  <files>frontend/src/components/chat/__tests__/OutputFileCard.baseline.test.tsx</files>
  <read_first>
    - `frontend/src/components/chat/OutputFileCard.tsx` (all 187 lines — both branches, and the exact copy strings)
    - `frontend/src/__tests__/components/MessageItem.finalOutputs.test.tsx` :30-48 (the `make…(overrides: Partial<T> = {})` fixture idiom and `renderWithTooltip`) and :111-125 (the two-sided affordance+copy discipline)
    - `frontend/src/components/chat/__tests__/StopControl.baseline.test.tsx` :505-615 — the `*.baseline.test.tsx` naming idiom and the ⚠ SUPERSEDED-banner style this file must be ready to receive
    - `.planning/phases/195-show-the-deliverable/195-VALIDATION.md` § "The Plants" rows P2 and P3
    - `.planning/phases/195-show-the-deliverable/195-PATTERNS.md` § 8 — the three gotchas, incl. `OutputFileCardProps` not being exported
  </read_first>
  <action>
Create `frontend/src/components/chat/__tests__/OutputFileCard.baseline.test.tsx`. Name it with the
repo's `.baseline.test.tsx` idiom so a later reader knows it exists to pin pre-refactor behaviour.
Render `OutputFileCard` **directly** (not through `MessageItem`) inside a `TooltipProvider` via a
local `renderWithTooltip` helper, and build fixtures as inline literals — `OutputFileCardProps` is
not exported and **must not be exported by this plan**.

Author a docblock stating: what the file pins, that D-08 requires these states to survive the Phase
195 extraction byte-identical, and that plan 195-04 converts the component's internals while this
suite's assertions stay unchanged.

Cases, at minimum:

1. **`supersedes` in the LIVE branch** — a fixture with `url` and `supersedes: "draft-v1.docx"`
   renders the text `Replaces: draft-v1.docx`.
2. **`supersedes` in the DEAD branch** — a fixture with **no `url`** and `supersedes: "draft-v1.docx"`
   renders the same text. ⚠ This is the arm F5 says nothing in the tree can see today, and P3(b) says
   a fence rendering only a url-bearing fixture **structurally cannot** reach it.
3. **The literal** — assert the exact prefix `Replaces: ` (with its trailing space semantics as
   rendered) rather than a loose `/replaces/i`, so a copy change reds.
4. **`supersedes` absent** → no `Replaces:` text in either branch (the negative control).
5. **The dead-link pair, as TWO cases, not two `expect`s in one** — because the first assertion
   short-circuits the second (P2): one case asserting `[data-dead="true"]` is present, one asserting
   the text `Download unavailable` is present. Also assert the affordance is a `<span>` carrying
   `aria-disabled="true"` and that the dead row contains **zero** `<button>` elements — that last
   property is what keeps `WorkflowRunPage.test.tsx:991` green after plan 06 unifies (F8), so it is
   pinned HERE, at its source, rather than left as luck.
6. **The inert `data-variant`** — `variant="hero"` and `variant="working"` both render the SAME row
   shape (same root tag, same class list) and differ ONLY in the `data-variant` attribute value.
   Cite D-095.1-06: the hero/working split was REVERSED; this case exists so a future reader cannot
   mistake the surviving attribute for a surviving behaviour.
7. **Size omitted** — a fixture with no `size` renders no size cell; with `size` it renders
   `formatBytes`'s output (e.g. `18.4 KB`). This is the `file.size != null` branch at `:177`.

⚠ **Do NOT add a case for the 3 s auto-clearing in-row download error.** It needs fake timers and it
is not one of the two states D-08 names; adding it here buys future flake for no D-08 coverage. Record
that decision in the docblock so the omission reads as a choice.

**Then drive the plants.** Six plants minimum, each a real edit to
`frontend/src/components/chat/OutputFileCard.tsx`, run, observed RED, and the file restored
**md5-identical** (capture `md5sum` before and after and record both):
  - P3(a) delete the live-branch subline block (`:168-172`) → case 1 must red, case 2 must STAY GREEN
  - P3(b) delete the dead-branch subline block (`:101-105`) → case 2 must red, case 1 must STAY GREEN
  - P3(c) change the literal `Replaces: ` → case 3 must red
  - P2(a) remove `data-dead="true"` (`:96`) → case 5's attribute case must red
  - P2(b) change the copy `Download unavailable` (`:116`) → case 5's copy case must red
  - the variant plant: make `variant === "hero"` change any class → case 6 must red

⚠ **A plant that produces a SKIP rather than a RED has proven nothing** (194-12). ⚠ **Read WHICH
cases failed, never just how many** (194-06). ⚠ **A fence that fails to red is a FINDING** — record
it in the SUMMARY and fix the fence, do not smooth it over.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/chat/__tests__/OutputFileCard.baseline.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The suite exists and passes with 0 failing; the exact passed-count is recorded (it becomes this file's BASELINE pin in task 3)
    - `grep -c "Replaces:" frontend/src/components/chat/__tests__/OutputFileCard.baseline.test.tsx` returns ≥ 3
    - The suite contains at least one case rendered from a fixture with NO `url` key AND a `supersedes` key
    - The dead-branch case asserts `querySelectorAll("button")` has length 0 on the rendered row
    - Six plants recorded in the SUMMARY, each with: the exact edit, the failing case NAMES (not just a count), the RED output line, and `md5sum` before == after
    - `grep -c "export interface OutputFileCardProps\|export type OutputFileCardProps" frontend/src/components/chat/OutputFileCard.tsx` returns 0 (the props stayed unexported)
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` reports the baseline count from `195-BASELINE.md` (expected 33)
  </acceptance_criteria>
  <done>`supersedes` and `data-variant` have coverage for the first time, in both of the component's two branches, and every clause has been shown to be able to fail.</done>
</task>

<task type="auto">
  <name>Task 2: Characterize the run page's SILENT dead row before plan 06 improves it (F8)</name>
  <files>frontend/src/pages/WorkflowRunPage.test.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowRunPage.test.tsx` :893-1035 (the whole deliverable-region describe, especially the id-less-row case at ~:975-995) and :1052-1063 (`codeOf` and its self-proving stripper case)
    - `frontend/src/pages/WorkflowRunPage.tsx` :1066-1082 — the id-less `<div>` as it ships
    - `.planning/phases/195-show-the-deliverable/195-RESEARCH.md` F8, and § "`WorkflowRunPage.test.tsx` — 102 cases" for the full predicate table
    - `frontend/src/components/chat/__tests__/StopControl.baseline.test.tsx` :513-573 — the ⚠ SUPERSEDED-IN-PLACE banner shape plan 06 will apply to this block
  </read_first>
  <action>
Add ONE new `describe` block to `frontend/src/pages/WorkflowRunPage.test.tsx`, titled so its purpose
and its expiry date are both legible — e.g.
`describe("195-02 — the id-less deliverable row TODAY: a fact with no control and NO copy (pre-change capture)")`.

Its docblock must state, in words: this block captures behaviour that **plan 195-06 deliberately
changes** — the id-less row gains the shipped *"Download unavailable — this file has no link"*
affordance when the region adopts the shared row — and that when it inverts, the original assertions
are to be **quoted verbatim in place, never deleted**, because the suite is pinned EXACT and because
a capture deleted the moment it inverts leaves no record that the old behaviour was ever real
(193.2 WR-05).

Cases (2-3, no more — this is a capture, not a feature suite):
1. The id-less row renders the name, the size and the `title` full path, and the region contains
   **zero** `<button>` elements (mirrors the shipped `:991` assertion, stated from this block's own
   fixture so the delta has a home).
2. The id-less row contains **no** occurrence of the string `Download unavailable`. ⚠ Scope the query
   to the ROW, not to the region's whole `textContent` — and state why in a comment: the region's
   terminal empty copy legitimately contains other prose, and a region-wide sweep is the failure mode
   P5 names.
3. A positive control proving case 2 can fail: assert that the same needle IS found in a string that
   contains it (the shape at `:1470-1471` / `:1482`), so a mis-scoped query cannot pass vacuously.

**Plants.** Drive case 1 and case 2 RED against real edits to
`frontend/src/pages/WorkflowRunPage.tsx`, restoring md5-identical each time:
  - insert a `<button>` into the id-less branch → case 1 must red
  - insert the literal `Download unavailable` into the id-less branch → case 2 must red
    (this plant is the *preview* of plan 06's real change — record its output, because plan 06's
    inversion is expected to reproduce exactly it)

⚠ **Then assert `StopControl.baseline.test.tsx` green** (P9). It greps **raw**
`WorkflowRunPage.tsx?raw` and counts `/<StopControl/g` expecting exactly **1**. This task edits only
the test file, so it should be untouched — assert it anyway, because that suite is the one that breaks
for reasons unrelated to its subject and (until task 3) the gate never runs it.

⚠ **This block ADDS cases, which the gate permits (`[count-decrease]` fires only on a DECREASE) — but
raise the pin in task 3 to the new measured `actual` so a future deletion cannot pass silently.**
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/pages/WorkflowRunPage.test.tsx src/components/chat/__tests__/StopControl.baseline.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `WorkflowRunPage.test.tsx` passes with 0 failing and its passed-count is **greater than 102**; the exact new number is recorded
    - `StopControl.baseline.test.tsx` reports 15 passed / 0 failing
    - The new describe title contains the string `195-02` and the word `pre-change`
    - The new block contains a comment naming plan `195-06` as the inversion point
    - Both plants recorded with the failing case NAMES, the RED output, and `md5sum` before == after on `WorkflowRunPage.tsx`
    - `git diff --numstat` shows `frontend/src/pages/WorkflowRunPage.tsx` with **0 changed lines** in this plan's commits
  </acceptance_criteria>
  <done>Today's silent dead row is on record with its own passing fence, so plan 06's improvement is a measured delta rather than an unrecorded change.</done>
</task>

<task type="auto">
  <name>Task 3: Make the gate cover this phase — adopt five suites, file-level, and record the reasoning</name>
  <files>scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `scripts/vitest-count-gate.cjs` :1-120 (the contract, the `[count-decrease]` rule, the "read from this script's own `actual` column" rule), :919 (the existing `WorkflowRunPage.test.tsx` pin), :2113-2220 (`BASELINE_TOTAL`, `TARGETS`, the two-knobs + timing rules), :2360-2400 (the `ls`-before-writing timing note)
    - `scripts/vitest-count-gate.cjs` :2168-2175 — ⚠ **the script names `FilesSection` itself** as a suite *"a later phase … should adopt deliberately, with its own measured number"*, and records the FILE-LEVEL-never-bare-directory house rule
    - `.planning/phases/195-show-the-deliverable/195-PATTERNS.md` § 9 — the entry shape to copy, and the ⚠ global bare-filename key space
    - `.planning/phases/195-show-the-deliverable/195-VALIDATION.md` P10 and § Wave 0 "Gate adoption decision"
  </read_first>
  <action>
**Phase 195 IS the later phase the script's own comment at `:2168-2175` names.** Adopt, with
**FILE-LEVEL** entries only — never a bare directory, per the house rule stated three times in that
file:

  1. `src/components/chat/__tests__/OutputFileCard.baseline.test.tsx` — created by task 1
  2. `src/components/panel/__tests__/FilesSection.test.tsx` — this phase CONVERTS `FilesSection.tsx`,
     so its 11 cases are load-bearing protection
  3. `src/lib/__tests__/fileIcon.test.tsx` — this phase WIDENS `fileIcon.tsx`
  4. `src/components/chat/__tests__/StopControl.baseline.test.tsx` — ⚠ P9's fence, which greps raw
     `WorkflowRunPage.tsx`; leaving it out is precisely *"a pin in an ungated suite is a pin nothing
     checks"*, the lesson `docs/HOT-FILE-LEDGER.md` records against this very page
  5. `src/__tests__/components/MessageItem.finalOutputs.test.tsx` — the only shipped coverage of
     `OutputFileCard`'s dead-link state, and the suite that would notice if plan 04 accidentally
     opened `MessageItem.tsx`

Also **RAISE the existing `"WorkflowRunPage.test.tsx"` pin** from 102 to the number task 2 measured.

For every number: **read it from this script's own printed `actual` column, across TWO agreeing runs.**
Never hand-count `it(` literals — the rule the script states about itself. Record both runs' output in
the SUMMARY.

⚠ **TIMING IS NOT COSMETIC.** A `TARGETS` path that does not exist makes the gate **ERROR (exit 2),
not fail**. Confirm all five paths with `ls` before writing the entries, and note in the comment that
you did — the `:2370-2373` precedent.

⚠ **The BASELINE key space is GLOBAL (bare filename).** Confirm `OutputFileCard.baseline.test.tsx` is
unique tree-wide (`git ls-files | grep -c OutputFileCard.baseline.test.tsx`) before adding its pin,
or two suites collide silently on one number.

Write a comment block above the new entries carrying: (1) the plan that added them (`195-02`),
(2) that they land in the same commit as the suite they cover, (3) **what would be unguarded without
each one** — name it per suite, and (4) that they are file-level and why the bare directories
`src/components/chat`, `src/components/panel/__tests__`, `src/lib/__tests__` and `src/__tests__` were
each declined (adopting suites this phase does not read makes this phase the owner of their future
rot — the script's own argument).

**Record the decision, including any decline, with its reason.** *A decline with no recorded reason is
indistinguishable from an oversight.* If a suite is NOT adopted, say which and why, in the comment
block and in the SUMMARY.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 node ../scripts/vitest-count-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - The gate's VERDICT line reads `OK` with `failed 0`; the line is quoted verbatim in the SUMMARY, from TWO runs that agree
    - `grep -c "OutputFileCard.baseline.test.tsx" scripts/vitest-count-gate.cjs` returns ≥ 2 (one TARGETS, one BASELINE)
    - The same holds for `FilesSection.test.tsx`, `fileIcon.test.tsx`, `StopControl.baseline.test.tsx`, `MessageItem.finalOutputs.test.tsx`
    - `grep -n '"src/components/chat"' scripts/vitest-count-gate.cjs` returns nothing (no bare-directory entry was added)
    - The `WorkflowRunPage.test.tsx` pin equals the number task 2 measured, and both the old value (102) and the new one appear in the comment
    - The gate's printed `pinned total` and grand `total` both increased relative to `195-BASELINE.md`; both figures recorded
    - The explicit ungated command from `195-VALIDATION.md` still passes (run it too — the two knobs are separate)
  </acceptance_criteria>
  <done>Four suites that guard this phase's files, plus the one it just created, are inside the gate with numbers read from the gate itself, and every adoption and decline carries its reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model/sandbox-derived filename → the render layer | `file.filename` and `file.supersedes` are backend/model-derived strings that cross into JSX in the component under characterization |
| none new | this plan writes tests and a CI config; it adds no route, no fetch, no persistence and no dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-195-02-01 | Tampering (XSS) | `OutputFileCard`'s `filename` / `supersedes` rendering | mitigate | Task 1 asserts the strings render as **text children** (`getByText`), which is only satisfiable if React escapes them. Add one assertion that the rendered row contains no `innerHTML`-injected markup for a fixture whose `supersedes` is `<img src=x onerror=1>` — it must appear as literal text, proving the sink is text-only. No `dangerouslySetInnerHTML` may be introduced. |
| T-195-02-02 | Repudiation | the gate's own numbers | mitigate | Every pin is read from the gate's printed `actual` across two agreeing runs and both runs are quoted in the SUMMARY. A hand-counted pin is unverifiable after the fact — the script states this about itself. |
| T-195-02-03 | Denial of Service (CI) | `scripts/vitest-count-gate.cjs` | mitigate | A `TARGETS` path that does not exist makes the gate exit 2 for every subsequent plan in the phase. Mitigated by `ls`-confirming all five paths in the same commit and by running the gate as this task's verify. |
| T-195-02-SC | Tampering | package installs | n/a | **No package installed.** `frontend/package.json` and `frontend/package-lock.json` are untouched; assert with `git diff --numstat` being EMPTY for both. No `slopcheck` owed. |

**Honest scale statement:** this is a test-and-CI plan. The one genuine security-relevant assertion it
adds is that a model-derived filename is a text sink, not a markup sink — which was true before and is
now pinned.
</threat_model>

<verification>
- `OutputFileCard.baseline.test.tsx` exists, is green, and covers `supersedes` in BOTH branches
- All six task-1 plants and both task-2 plants observed RED, with failing case names and md5 before/after
- `WorkflowRunPage.test.tsx` green, count > 102, pin raised to the measured value
- `StopControl.baseline.test.tsx` green at 15 and now inside `TARGETS`
- Count gate `OK`, `failed 0`, on two agreeing runs; ungated command also green
- `tsc -p tsconfig.app.json` unchanged from `195-BASELINE.md`
- `frontend/src/components/chat/OutputFileCard.tsx` and `frontend/src/pages/WorkflowRunPage.tsx` are byte-unchanged in this plan's commits (`git diff --numstat` empty for both)
</verification>

<success_criteria>
Every byte-identity claim this phase will make about `OutputFileCard` and the run page's id-less row
now has a fence that has been PROVED able to fail, authored against the unmoved tree — and the gate
runs four suites it previously could not see.
</success_criteria>

<output>
Create `.planning/phases/195-show-the-deliverable/195-02-SUMMARY.md` when done.
</output>
