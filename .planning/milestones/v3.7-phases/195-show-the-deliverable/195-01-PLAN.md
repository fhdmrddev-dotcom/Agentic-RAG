---
phase: 195-show-the-deliverable
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/195-show-the-deliverable/195-BASELINE.md
autonomous: false
requirements: [RUN-02]

must_haves:
  truths:
    - "SC#1 is proved on a LIVE workflow run, in a commit that predates every source change in this phase"
    - "The deliverable that the run wrote is downloadable from the run surface and the downloaded file OPENS in a .docx reader"
    - "The phase's base SHA is recorded as an artifact, so later plans can assert byte-identity against a named base instead of an assumed one"
    - "Whether a browser and a .docx reader are actually reachable is MEASURED at wave 1, not discovered at phase close"
  artifacts:
    - path: ".planning/phases/195-show-the-deliverable/195-BASELINE.md"
      provides: "The pre-change live baseline: DB pre-state, gate/tsc/suite figures, the driven terminal + live arms, the three-surface side-by-side, the named BASE SHA"
      contains: "BASE_SHA"
  key_links:
    - from: ".planning/phases/195-show-the-deliverable/195-BASELINE.md"
      to: "195-07-PLAN.md P7(a) git byte-identity assertion"
      via: "the recorded BASE_SHA"
      pattern: "BASE_SHA"
---

<objective>
Prove SC#1 — *a completed workflow that produced a file shows that file from the run surface* — on a
LIVE run, **before this phase changes a single line of source**, and record the figures every later
plan compares against.

Purpose: D-16 is the one deliverable in this phase whose value is **destroyed by doing it later**
(188.1: *a baseline only proves something if it PREDATES the change*). CONTEXT records SC#1 as
measured-already-satisfied from source; this plan turns that into an observation. If the live run
refutes it, scope grows and that is the honest order.
Output: `.planning/phases/195-show-the-deliverable/195-BASELINE.md` — the pre-state, the driven
evidence, the named base SHA, and an explicit refutation verdict.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@.planning/phases/195-show-the-deliverable/195-CONTEXT.md
@.planning/phases/195-show-the-deliverable/195-RESEARCH.md
@.planning/phases/195-show-the-deliverable/195-VALIDATION.md

⚠ **THIS PLAN IS `autonomous: false` AND THAT IS STRUCTURAL, NOT CAUTION.** Task 2 needs a browser.
Worktree executors have no browser tool — Chrome MCP lives with the **orchestrator**. The executor
does task 1 and task 3's write-up; **task 2 is a blocking checkpoint the orchestrator/operator
drives**, and its evidence is pasted back for task 3.

⚠ **NO SOURCE FILE MAY BE EDITED BY THIS PLAN.** `files_modified` is one planning document. If this
plan's commit touches anything under `frontend/` or `backend/`, the baseline it exists to create has
already been contaminated.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the pre-change state — DB, tree, gate, tsc, suites, and the BASE SHA</name>
  <files>.planning/phases/195-show-the-deliverable/195-BASELINE.md</files>
  <read_first>
    - `.planning/phases/195-show-the-deliverable/195-RESEARCH.md` § "The D-16 live baseline (R4)" — the target workflow, the pre-existing read-only run row, and the exact psycopg2 command
    - `.planning/phases/195-show-the-deliverable/195-VALIDATION.md` § "THE COUNT GATE DOES NOT COVER THIS PHASE" — the two commands and the five suite figures
    - `./CLAUDE.md` § "Parallel execution" — `GSD_VITEST_MAX_WORKERS=2` is mandatory on a SINGLE run too; `bash scripts/bootstrap-worktree.sh "$(pwd)"` FIRST in a worktree
    - `./CLAUDE.md` § "Local dev infrastructure" — ⚠ if `127.0.0.1:54322` refuses, check the Windows port-reservation trap BEFORE suspecting Supabase
  </read_first>
  <action>
Record, in a new `195-BASELINE.md`, the state of the tree and the database **at this commit**, each
figure beside the command that produced it.

1. **BASE SHA.** `git rev-parse HEAD` and `git log -1 --format=%s`. Write it into the file under a
   heading literally containing the token `BASE_SHA`, e.g. `BASE_SHA = <sha>`. Plan 07's P7(a)
   `git diff --numstat` assertion reads this value — an assumed base makes that check pass trivially
   (7/7 worktrees forked from the wrong base in Phase 194, 8/8 in 194.1). The expected value at
   planning time was `8b71e9fc7710238e119b40143653fd453525d34c`; if HEAD differs, record BOTH and say
   which one the phase is built on.
2. **DB pre-state**, via the venv interpreter only (`backend/venv/Scripts/python.exe` + `psycopg2` —
   a bare `python -c "import psycopg2"` fails here): total `workspace_files` row count (was 86);
   the row count for thread `1189a1a3-b836-4e7a-8aca-a021f1175e4a`; and the full row for
   `workspace_files.id = d4598e01-a45b-430a-b30b-05ef7e9a3852` (`path`, `size_bytes`, `mime_type`,
   `created_at`) which is the pre-existing terminal-arm evidence.
3. **Frontend baselines**, re-derived not quoted: `cd frontend && npx tsc --noEmit -p tsconfig.app.json`
   (expected 33); `cd frontend && GSD_VITEST_MAX_WORKERS=2 node ../scripts/vitest-count-gate.cjs` —
   record the VERDICT line verbatim (expected `OK`, total ~3972, failed 0, pinned ~3898, 75/75), never
   a summary; and the five in-scope suites run individually at `--maxWorkers=2`:
   `src/pages/WorkflowRunPage.test.tsx` (expected 102), `src/components/panel/__tests__/FilesSection.test.tsx`
   (11), `src/__tests__/components/MessageItem.finalOutputs.test.tsx` (11),
   `src/lib/__tests__/fileIcon.test.tsx` (11),
   `src/components/chat/__tests__/StopControl.baseline.test.tsx` (15).
   ⚠ A growing gate total is the gate WORKING — its contract is *no per-file DECREASE* + *zero
   failing*, never a fixed grand total. If any figure differs from the expected value above, record
   the measured one and mark the expectation stale beside it, never over it.
4. **Hot-file triples, re-derived with the `CLAUDE.md` recipe** (subtract six-digit dated quick-task
   buckets) for the five files this phase touches or is measured against:
   `frontend/src/pages/WorkflowRunPage.tsx`, `frontend/src/components/chat/OutputFileCard.tsx`,
   `frontend/src/components/panel/FilesSection.tsx`, `frontend/src/lib/fileIcon.tsx`,
   `frontend/src/components/chat/MessageItem.tsx`. Research measured zero drift on 2026-08-17; commits
   land daily, so re-derive rather than copy. Plan 08 consumes these.
5. **The byte-identity pre-state for P7(a):** `git rev-parse HEAD` plus
   `md5sum frontend/src/components/chat/MessageItem.tsx frontend/src/components/chat/tool-bodies/ExecuteCodeBody.tsx`.

Leave a `## Driven evidence` section with the three arm headings and the word `PENDING` — task 2
fills it.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | tail -3 && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/pages/WorkflowRunPage.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/195-show-the-deliverable/195-BASELINE.md` exists and `grep -c BASE_SHA` on it returns ≥ 1
    - The file contains a 40-character git SHA and the subject line of that commit
    - The file records a `workspace_files` total row count and the four fields of row `d4598e01-a45b-430a-b30b-05ef7e9a3852`
    - The file records the count-gate VERDICT line verbatim (the line beginning `count gate`), not a paraphrase
    - The file records five suite counts, each with the command that produced it
    - The file records five `commits / phases / lines` triples with the three commands used
    - `git diff --numstat HEAD~1 HEAD -- frontend backend` is EMPTY for this plan's commit (no source touched)
  </acceptance_criteria>
  <done>The pre-change tree and DB state are recorded as commands-and-outputs, and the phase's base SHA is an artifact rather than an assumption.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Drive the live SC#1 baseline — terminal arm, live arm, and open the .docx</name>
  <files>.planning/phases/195-show-the-deliverable/195-BASELINE.md</files>
  <read_first>
    - `.planning/phases/195-show-the-deliverable/195-RESEARCH.md` § "The D-16 live baseline (R4)" — the procedure, the DOM readback snippet, and the refutation table
    - `.planning/phases/195-show-the-deliverable/195-VALIDATION.md` § "Manual-Only Verifications"
    - `.planning/phases/195-show-the-deliverable/195-CONTEXT.md` D-16, D-20
  </read_first>
  <what-built>
Nothing was built. This checkpoint MEASURES the shipped surface (Phase 188-10) before Phase 195
touches it. `WorkflowRunPage.tsx` has listed and downloaded a run's workspace files since `783daab5`;
this proves it on a real run rather than inheriting the claim.
  </what-built>
  <action>
Halt and hand control to the orchestrator, which owns the browser. Drive the four arms in
`<how-to-verify>` in order — probe the environment first, then the read-only terminal arm, then the live
arm, then the three-surface capture — and hand the evidence back for task 3 to write up. **Change no
source file and no planning file in this task**; its only product is measurement.
  </action>
  <how-to-verify>
⚠ **Frontend is at `http://localhost:5173`. Vite binds `[::1]` only on this box — an IPv4 probe
reports it down while it is running.** `take_screenshot` times out here; read DOM geometry via script
evaluation. ⚠ **Do NOT locate the row by `getElementById`** (192.1 D-27) — a machine check that
bypasses the human's task has not verified the human's task.

**Arm 0 — probe the environment (do this FIRST, not at phase close).** Confirm a browser is
connected (`list_connected_browsers`) and that a `.docx` reader exists on this machine. Phase 194's
eight UAT rows went entirely undriven because this was checked at the end. If either is absent, this
checkpoint returns ⛔ **with the reason recorded** — never silently omitted — and the phase's close
plan (195-08) inherits the owed row.

**Arm 1 — terminal, read-only, zero risk.** Open the existing completed run
`833e8e85-92a2-451b-9af7-7cd085355ff7` (thread `1189a1a3-b836-4e7a-8aca-a021f1175e4a`, definition
`northwind-qbr-fa65a43c` v1) from the Workflows library run receipt or the thread's run line. Confirm:
  1. the deliverable region renders,
  2. the row reads `Northwind-QBR-Template.docx` and `38.7 KB`,
  3. clicking it downloads a file, and
  4. **that file OPENS in Word (or any .docx reader).** Record the downloaded file's on-disk byte
     size — it must equal **39660**.

**Arm 2 — live.** Launch `northwind-qbr-fa65a43c` v1 from the Workflows page, stay on the run
surface, and watch the region go from `No files yet — this run hasn't written anything.` to a
populated row **mid-run**. Then download and **open** it. ⚠ **AVOID `compliance-gap-report`,
`pm-risk-register` and `risk-register`** — zero of their runs has ever produced a file.

**Arm 3 — the three-surface side-by-side (D-20's second half, and the F10 evidence).** Same thread,
three places: the chat message's output-file card, the workspace panel's file list, and the run page's
deliverable row. Capture each row's rendered text AND the shape of its icon element (glyph size,
whether a `.EXT` ribbon is present, the computed colour). This is the pre-change record that plan 03's
one-icon-path decision is measured against.

**DOM readback for arm 1/2 (query the way a person finds it, not by id):**
`document.querySelector('[data-testid="run-deliverables"]')`, then from it: the `h2` textContent, the
`li` count, and the first `button`'s `aria-label`, `title` and `getBoundingClientRect()`.

**Evidence to hand back:** the new `run_id` + `thread_id`; the new `workspace_files` row
(`id`/`path`/`size_bytes`/`created_at`); the DOM readback objects; the downloaded byte sizes; whether
Word opened each file; and the three-surface capture.

**Read the refutation table in RESEARCH § "What a REFUTATION looks like" before declaring a pass.**
A row that renders only after the run goes terminal does **not** refute SC#1 (RUN-02 says *"when the
run finishes"*) — log it. A download that 404s or yields 0 bytes, or a file that opens corrupt, is a
**backend** defect and breaks this phase's "no Python" premise — escalate rather than proceed.
  </how-to-verify>
  <acceptance_criteria>
    - `list_connected_browsers` result recorded (non-empty, or ⛔ with the reason)
    - Arm 1: the run id, the region's `h2` text, the `li` count, the button's exact `aria-label`, the downloaded file's on-disk byte size (must be 39660), and "opened in Word: yes/no"
    - Arm 2: a NEW `run_id` and `thread_id`, the new `workspace_files` row's four fields, whether the row appeared mid-run or only at terminal, and "opened in Word: yes/no"
    - Arm 3: three rows' rendered text plus three icon-element descriptions (size, ribbon present y/n, colour)
    - An explicit verdict line: `SC#1 CONFIRMED` or `SC#1 REFUTED — <observation> — <scope consequence from the RESEARCH refutation table>`
    - Any arm that could not be driven is recorded ⛔ with its reason and blocking condition — never omitted
  </acceptance_criteria>
  <done>Arms 0-3 are driven and their evidence handed back, including the downloaded file's on-disk
byte size and whether a .docx reader opened it — or each undriven arm is recorded as blocked with its
reason.</done>
  <resume-signal>Paste the evidence for arms 0-3 plus the verdict line, or describe what blocked each arm.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write the baseline record and its refutation verdict</name>
  <files>.planning/phases/195-show-the-deliverable/195-BASELINE.md</files>
  <read_first>
    - `.planning/phases/195-show-the-deliverable/195-BASELINE.md` — task 1's output, with the `PENDING` driven-evidence section
    - `.planning/phases/195-show-the-deliverable/195-RESEARCH.md` § "What a REFUTATION looks like, and what it would cost"
  </read_first>
  <action>
Replace the `PENDING` markers with task 2's evidence, verbatim rather than summarised. Add:

1. **The verdict**, as its own heading: `SC#1 CONFIRMED (live, pre-change)` or
   `SC#1 REFUTED` with the observation and the scope consequence copied from RESEARCH's refutation
   table.
2. **A `## What this baseline does NOT claim` section.** Name at least: whether any of the 161
   file-less runs had an emit phase (D-04's ACCEPTED BLIND SPOT — not checked here); whether the
   region ever shows a file from a PRIOR run on the same thread (D-01's known blind spot — if
   observed, record it, and note that it strengthens D-02's relabel rather than refuting SC#1); and
   any ⛔ arm.
3. **A `## Consumed by` section** listing which later plan reads which figure: 195-07 reads
   `BASE_SHA` and the two md5s; 195-08 reads the hot-file triples; every plan reads the gate/tsc
   figures as its non-regression floor.

⚠ If arm 2 or 3 returned ⛔, say so in the verdict line itself. **A baseline that lists only what
passed is not a baseline** — the same rule this project applies to UAT scoreboards.
  </action>
  <verify>
    <automated>grep -c "SC#1 \(CONFIRMED\|REFUTED\)" .planning/phases/195-show-the-deliverable/195-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c PENDING` on `195-BASELINE.md` returns 0
    - The file contains exactly one line matching `SC#1 \(CONFIRMED\|REFUTED\)`
    - Sections `## What this baseline does NOT claim` and `## Consumed by` both present
    - `## Consumed by` names `195-07` and `195-08` by plan number
    - `git diff --numstat` for this plan's commits touches no path under `frontend/` or `backend/`
  </acceptance_criteria>
  <done>`195-BASELINE.md` is a complete, dated, command-backed record whose verdict on SC#1 is a measurement and whose blind spots are named.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → frontend → `GET /threads/{tid}/workspace/files` | the only boundary this plan crosses, and it crosses it READ-ONLY through the shipped, RLS-guarded route |
| local Postgres @ `127.0.0.1:54322` | read-only `SELECT`s from the venv interpreter; no writes, no DDL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-195-01-01 | Information Disclosure | `195-BASELINE.md` | mitigate | The record carries run ids, thread ids and file ids — all already present in `.planning/` artifacts across this repo. It MUST NOT carry a bearer token, a Supabase key, a signed URL or any `.env` value. The write-up quotes DOM readback and DB columns only. |
| T-195-01-02 | Tampering | local database | mitigate | Arm 1 is read-only by construction. Arm 2 launches a workflow through the shipped UI — the same action any user takes — and writes nothing by hand. **No `UPDATE`, `INSERT` or migration is run by this plan.** |
| T-195-01-SC | Tampering | package installs | n/a | **No package is installed.** `package.json`, `package-lock.json` and every `requirements*.txt` are untouched — assert with `git diff --numstat HEAD~1 HEAD -- frontend/package.json frontend/package-lock.json` being EMPTY. No `slopcheck` is owed. |

**Honest scale statement:** this plan reads. It has no new network surface, no new route, no new
persistence and no new dependency. Inventing a high-severity threat here would be padding.
</threat_model>

<verification>
- `195-BASELINE.md` exists, contains `BASE_SHA`, and carries no `PENDING`
- The count-gate VERDICT line and the `tsc` figure are recorded verbatim as this phase's floor
- No file under `frontend/` or `backend/` is modified by any commit in this plan
- The SC#1 verdict is present and is either CONFIRMED or REFUTED with a scope consequence
</verification>

<success_criteria>
SC#1 is a MEASUREMENT taken before the phase changed anything, the phase's base SHA is an artifact,
and the browser + `.docx`-reader availability that D-20's close depends on is known at wave 1.
</success_criteria>

<output>
Create `.planning/phases/195-show-the-deliverable/195-01-SUMMARY.md` when done.
</output>
