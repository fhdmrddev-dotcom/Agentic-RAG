---
phase: 195-show-the-deliverable
plan: 03
type: execute
wave: 2
depends_on: ["195-01", "195-02"]
files_modified:
  - frontend/src/lib/fileIcon.tsx
  - frontend/src/lib/__tests__/fileIcon.test.tsx
  - frontend/src/components/files/fileRowUtils.ts
  - frontend/src/components/files/FileRow.tsx
  - frontend/src/components/files/__tests__/fileRowUtils.test.ts
  - frontend/src/components/files/__tests__/FileRow.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [RUN-03]

must_haves:
  truths:
    - "One presentational row component exists that can render the chat card's, the panel row's and the run row's shipped shapes without any of them changing visually"
    - "One byte formatter and one path-basename helper exist, exported, and no surface re-derives them"
    - "One icon module resolves every extension the four surfaces used, mime-first where the panel was mime-first, with no extension regressing to a worse glyph"
    - "A file with no `created_at` — the just-produced SSE deliverable — sorts FIRST, and both data regimes have their own pinned case"
    - "The row reads no hook and names no previewer, so the run page's two shipped absence fences stay satisfiable"
  artifacts:
    - path: "frontend/src/components/files/FileRow.tsx"
      provides: "The ONE row markup and the three shipped surface densities, with a caller-supplied wrapper element via asChild"
      min_lines: 90
      exports: ["FileRow"]
    - path: "frontend/src/components/files/fileRowUtils.ts"
      provides: "formatBytes, baseName, the FileSource discriminated identity, byNewestFirst"
      exports: ["formatBytes", "baseName", "byNewestFirst"]
    - path: "frontend/src/lib/fileIcon.tsx"
      provides: "The ONE icon path, widened with ribbon/tone/className/mimeType options"
      contains: "mimeType"
  key_links:
    - from: "frontend/src/components/files/FileRow.tsx"
      to: "frontend/src/lib/fileIcon.tsx"
      via: "the single fileIcon() call, parameterised by density"
      pattern: "fileIcon\\("
    - from: "frontend/src/components/files/FileRow.tsx"
      to: "@radix-ui/react-slot"
      via: "asChild — Slot + Slottable, so the caller supplies the wrapper element"
      pattern: "Slottable"
---

<objective>
Ship the shared presentational row and its pure helpers — the structural first move that IS RUN-03 —
without converting any surface yet.

Purpose: four surfaces hand-write the same file row today, with three independent icon mappings and
three copies of `formatBytes`, one of them commented *"copied verbatim … keep byte-for-byte
identical"*. This plan creates the ONE of each. It converts nothing, so a failure here is contained.
Output: `frontend/src/components/files/` (row + utils + two suites, both inside the gate) and a
`fileIcon.tsx` widened just enough that the panel and the run page can adopt it **without changing
what they look like**.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@.planning/phases/195-show-the-deliverable/195-CONTEXT.md
@.planning/phases/195-show-the-deliverable/195-RESEARCH.md
@.planning/phases/195-show-the-deliverable/195-PATTERNS.md
@.planning/phases/195-show-the-deliverable/195-VALIDATION.md
@.planning/phases/195-show-the-deliverable/195-BASELINE.md

@frontend/src/lib/fileIcon.tsx
@frontend/src/components/ui/button.tsx
@frontend/src/components/ingestion/NavRow.tsx
@frontend/src/components/chat/OutputFileCard.tsx
@frontend/src/components/panel/FilesSection.tsx

<interfaces>
<!-- The contracts this plan must produce and the three shipped shapes it must be able to reproduce.
     Extracted so the executor writes against a contract rather than exploring. -->

**Already installed, verified in the dist:** `@radix-ui/react-slot@^1.2.4` exports both `Slot` and
`Slottable`. `Slot` finds the child marked by `Slottable`, clones it, and injects the Slot's *other*
children as that element's children. `mergeProps` **joins** `className`
(`[slotProps, childProps].filter(Boolean).join(" ")`) — it does **not** twMerge — so the row's classes
and the caller's classes must not conflict on the same CSS property.

**The one first-party `asChild` in this tree** — `components/ui/button.tsx:42-53`: `forwardRef`,
`const Comp = asChild ? Slot : "button"`, `className={cn(...)}`, `displayName`. Copy the mechanics
verbatim; change the fallback to `"div"` and the ref generic to `HTMLElement` (the row is handed an
`<a>`, a `<div role="option">` and a `<button>`).

**Current signature to widen** — `frontend/src/lib/fileIcon.tsx:80`:

    export function fileIcon(filename: string, sizePx: number = 24): JSX.Element

**The three shipped skins the row must reproduce exactly:**

| | chat (`OutputFileCard.tsx:143-185`) | panel (`FilesSection.tsx:219-269`) | run (`WorkflowRunPage.tsx:1049-1082`) |
|---|---|---|---|
| row classes (caller-supplied) | `flex items-center gap-2.5 px-3 py-2 rounded-md text-xs transition-colors group ghost-border` + state classes | `flex cursor-pointer items-center gap-2 rounded-md border border-transparent px-2.5 py-2 transition-colors` + hover/ring/flash | `flex w-full items-center gap-2 rounded-md px-2 py-2 text-left transition-colors` + hover/ring |
| icon | `fileIcon(file.filename, 30)` — coloured glyph + `.EXT` ribbon | `<Icon className="h-4 w-4 flex-shrink-0 text-panel-muted-foreground" aria-hidden>` | `<Icon className="h-4 w-4 shrink-0 text-muted-foreground" aria-hidden>` |
| name span | `font-mono truncate text-foreground/80` inside `flex-1 min-w-0 flex flex-col` | `min-w-0 flex-1 truncate font-mono text-[13px] text-foreground/90` | `min-w-0 flex-1 truncate font-mono text-[11px] text-foreground/90` |
| size span | `text-muted-foreground flex-shrink-0`, only when `size != null` | `flex-shrink-0 font-mono text-[10px] text-panel-muted-foreground`, always, `+ " · v{version}"` | `shrink-0 font-mono text-[11px] text-muted-foreground`, always |
| trailing | `<Download className="w-3.5 h-3.5 text-primary flex-shrink-0">` or `<Loader2 … animate-spin>` | none | `<Download className="h-3.5 w-3.5 shrink-0 text-muted-foreground" aria-hidden>` |
| dead skin (chat only) | icon wrapper `flex-shrink-0 opacity-70`; name `font-mono text-foreground/60 truncate`; affordance = `<span aria-disabled="true" title="Download unavailable — this file has no link">` | n/a | n/a today — plan 06 gives the run row the same `<span>` affordance and NOTHING else |

**Slot order, identical in all three shipped surfaces:**
icon · (name + supersedes subline + error line) · trailingSlot · size · trailing affordance.

**The live-SSE regime that has no sort key** — `frontend/src/lib/api.ts:845-851` builds a workspace
file from `id/path/version/size_bytes/mime_type` only (**no `created_at`, no `kind`**), and
`StreamsProvider.tsx:2947-2954` **appends** it (`[...prev, merged]`). The list route orders by `path`
and only then supplies `created_at` (`backend/app/api/workspace.py:326-330`).
</interfaces>

⚠ **THE ARCHITECTURE IS MECHANICALLY CONSTRAINED AND THE CONSTRAINT IS SHIPPED.**
`WorkflowRunPage.test.tsx:1464` forbids the token `useViewingThread` and `:1478` forbids `FilePreview`
in **raw** page source. ⇒ **`FileRow` must be purely presentational**: it may not call a hook that
resolves a thread and it may not import or name the previewer. **D-09's "parameterise the activation"
is a CALLBACK PROP and a caller-supplied wrapper element — never a preview import.**

⚠ **NO NEW CARD.** SEED-148's adopted rule: *"Any plan that proposes a new card component should be
challenged."* This is an EXTRACTION of markup that already ships. Copy `NavRow.tsx`'s **discipline**
(pure, `ReactNode` slots, callbacks, a docblock recording the debt fixed while extracting) and
`OutputFileCard.tsx:161-184`'s **markup**. Do **not** copy NavRow's feature surface (inline rename,
tooltips, hover reveal, indent guides) and do **not** build on `components/ui/card.tsx`.

⚠ **NO `cva` VARIANTS BLOCK.** Phase 095.1 reversed the hero/working split; a `cva` variants block
would re-introduce it as a code shape. The `density` record below is per-**surface**, not per-file-
importance, and the docblock must say so explicitly so a reviewer cannot read it as the retired split.

⚠ **Worktree:** `bash scripts/bootstrap-worktree.sh "$(pwd)"` FIRST. `GSD_VITEST_MAX_WORKERS=2` on
every vitest run.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen fileIcon() into the ONE icon path — options, the mime-first branches, the missing extensions</name>
  <files>frontend/src/lib/fileIcon.tsx, frontend/src/lib/__tests__/fileIcon.test.tsx</files>
  <read_first>
    - `frontend/src/lib/fileIcon.tsx` (all 105 lines — `EXT_MAP`, `DEFAULT_SPEC`, the `hasDot` guard, the ribbon, the T-095-01-01 security note, and the stale `:21` docblock line)
    - `frontend/src/components/panel/FilesSection.tsx` :44-71 — `iconFor`, which is **MIME-FIRST** with a 14-entry `codeExts` list; this is the behaviour that must not regress
    - `frontend/src/pages/WorkflowRunPage.tsx` :164-188 — the run page's copy of the same mapping
    - `frontend/src/lib/__tests__/fileIcon.test.tsx` (all 11 cases — they must stay byte-unchanged and green)
    - `frontend/src/lib/phaseGlyph.tsx` :86-96 and :107-122 — the split-brain guard idiom and the recorded object-literal-prototype crash
    - `.planning/phases/195-show-the-deliverable/195-RESEARCH.md` F10 · `195-PATTERNS.md` § 3 and § 5's MIME-vs-extension paragraph
  </read_first>
  <action>
Widen `fileIcon` to a **third positional parameter with a default-`{}` options object** so every
existing call site stays byte-unchanged (`fileIcon(file.filename, 30)` at `OutputFileCard.tsx:83` and
all 11 shipped cases):

    export function fileIcon(
      filename: string,
      sizePx: number = 24,
      opts: { ribbon?: boolean; tone?: "category" | "inherit"; className?: string; mimeType?: string } = {},
    ): JSX.Element

Semantics, each defaulting to today's behaviour so the chat surface cannot move:
  - `ribbon` default **true** — the stacked mono `.EXT` label. `false` omits that span entirely.
  - `tone` default **"category"** — the inline `style={{ color }}`. **"inherit"** omits the inline
    colour on BOTH the wrapper and the ribbon so the caller's `className` token supplies it. This is
    what lets the panel keep `text-panel-muted-foreground` (a deliberate Phase 088-05 AA decision) and
    the run page keep `text-muted-foreground` **without the page ever naming a panel-scoped token** —
    `WorkflowRunPage.test.tsx:1428-1429` forbids `--panel-` in page source and the intent behind it is
    exactly this.
  - `className` merged onto the wrapper span with `cn` from `@/lib/utils`.
  - `mimeType` — when supplied, resolve **mime-first**, mirroring the panel's shipped order.

**Close the MIME/extension gap so nothing regresses** (this is measured, not assumed):
  1. Extend `EXT_MAP` with the extensions the panel's `codeExts` covers and `EXT_MAP` omits — `sh`,
     `bash`, `sql`, `yml`, `yaml`, `css`, `jsx`, `mjs`, `tsx` — each mapping to the existing code
     category (`Code` glyph, `#0c8599`). Without this, every one of them falls to `DEFAULT_SPEC`
     (`FileText`) and the panel visibly regresses on files the 11 shipped cases never exercise.
  2. Add the mime branches the panel had and extension-only resolution cannot express, checked BEFORE
     the extension lookup only when `opts.mimeType` is present: the three OOXML constants (docx →
     document, xlsx → table, pptx → slides), `text/markdown`, `text/csv`, `image/*`,
     `application/json`, `text/x-*` → code, and a bare `text/` → document fallthrough. Keep the OOXML
     mime constants as named consts, as both existing copies do.
  3. ⚠ **Harden the `EXT_MAP` lookup with `Object.prototype.hasOwnProperty.call`.** `EXT_MAP` is a
     plain object literal, so `EXT_MAP["constructor"]` is the `Object` FUNCTION — never nullish, so
     `?? DEFAULT_SPEC` does not fire and `const { color, Glyph } = Object` destructures to
     `undefined`, which would throw on render. `phaseGlyph.tsx:107-122` records this exact crash as a
     shipped scar. Two lines, with a precedent; take them and say why in the comment.

**Record the GLYPH DELTA rather than hiding it.** `fileIcon`'s map and the panel's differ on four
categories, and this plan keeps `fileIcon`'s (it is the DECLARED one shared icon module and its 11
cases stay green). State in the docblock, as a measured list: tables `FileSpreadsheet → Table`, code
`FileCode → Code`, images `FileImage → Image`, unknown `File → FileText`. Same category, different
lucide glyph. It is a **visible** change to the panel and the run page, it is the price of one icon
path, and D-18 declined a sketch for a surface that is already design-reviewed — so it is recorded
here as a decision, not discovered at UAT.

**Correct the two stale notes BESIDE the original, never over it** (a house pattern with three shipped
instances — `phaseGlyph.tsx:107-119`, `StopControl.baseline.test.tsx:517-520`,
`rowIdentity.test.ts:89-98`):
  - `fileIcon.tsx:21` reads *"NOT yet consumed — OutputFileCard imports this in Plan 05."* Quote it,
    then state that it now has four consumers via the shared row.
  - `195-CONTEXT.md`'s `<code_context>` says this module *"needs consumers, not changes"*. Note in the
    docblock that Phase 195 changed it, additively, and why (F10). Plan 08 records the same correction
    in the CONTEXT file itself.

**Extend `frontend/src/lib/__tests__/fileIcon.test.tsx`** — add cases, change none:
  - `ribbon: false` renders no `.EXT` text; default renders it (positive control)
  - `tone: "inherit"` renders no inline `color` style on wrapper or ribbon; default renders one
  - `className` reaches the wrapper span
  - each of the nine added extensions resolves to the `Code` glyph (`lucide-code`), and a control
    proving one of them resolved to `lucide-file-text` before (assert the DEFAULT_SPEC glyph for an
    extension still absent from the map, e.g. `.zzz`)
  - `mimeType` mime-first: `{ mimeType: "text/csv" }` on a name with **no** extension resolves to the
    table glyph; `{ mimeType: "image/png" }` on an extensionless name resolves to the image glyph
  - the prototype-pollution guard: a filename of `x.constructor` renders without throwing and yields
    the default glyph

**Plants** — real edits to `frontend/src/lib/fileIcon.tsx`, restored md5-identical, each observed RED
with the failing case NAMES recorded: remove the `ribbon` branch; remove the `tone === "inherit"`
branch; drop the nine `EXT_MAP` additions; drop the mime-first block; remove the `hasOwnProperty`
guard (must red the `x.constructor` case — ⚠ if it does not, the guard's case is inert and that is a
FINDING to fix, not to smooth over).
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/lib/__tests__/fileIcon.test.tsx && npx tsc --noEmit -p tsconfig.app.json 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function fileIcon(filename: string, sizePx: number = 24" frontend/src/lib/fileIcon.tsx` returns 1 — the first two parameters are unchanged
    - `git diff` on `frontend/src/lib/fileIcon.tsx` shows **no change** to the existing `EXT_MAP` rows or to the ribbon markup's default path
    - `frontend/src/lib/__tests__/fileIcon.test.tsx` reports **more than 11** passed, 0 failing; the 11 original case titles are all still present (`git diff` shows additions only)
    - `grep -c "hasOwnProperty" frontend/src/lib/fileIcon.tsx` returns ≥ 1
    - `grep -cE "sh:|bash:|sql:|yml:|yaml:|css:|jsx:|mjs:|tsx:" frontend/src/lib/fileIcon.tsx` returns ≥ 9
    - `grep -c "mimeType" frontend/src/lib/fileIcon.tsx` returns ≥ 2
    - The docblock contains the four-item glyph-delta list and both quoted stale notes
    - Five plants recorded with failing case NAMES, RED output, and md5 before == after
    - `tsc -p tsconfig.app.json` equals the `195-BASELINE.md` figure
  </acceptance_criteria>
  <done>One icon module resolves everything the four surfaces resolved, mime-first where the panel was, with no extension regressing — and the visible glyph delta is on record as a decision.</done>
</task>

<task type="auto">
  <name>Task 2: fileRowUtils.ts — the ONE formatter, the ONE basename, the D-05 source type, and the two-regime comparator (P4)</name>
  <files>frontend/src/components/files/fileRowUtils.ts, frontend/src/components/files/__tests__/fileRowUtils.test.ts</files>
  <read_first>
    - `frontend/src/components/chat/OutputFileCard.tsx` :25-29 — the origin `formatBytes`, to hoist character-for-character
    - `frontend/src/components/panel/FilesSection.tsx` :36-42 — the copy AND its own confession comment (which plan 05 deletes with it)
    - `frontend/src/pages/WorkflowRunPage.tsx` :151-162 — the third copy plus `baseName`
    - `frontend/src/components/panel/PendingAskCard.tsx` :608-614 — the tree's ONLY key-absence-aware comparator, its two-regime comment, and its `return 0` resolution
    - `frontend/src/components/workflows/canvasModel.ts` :368 — the `[...list].sort()` purity rule
    - `frontend/src/components/panel/__tests__/PendingAskCard.test.tsx` :205-222 — the two-row differing-order positive control to copy
    - `.planning/phases/195-show-the-deliverable/195-RESEARCH.md` F7 and the `fileRowUtils.ts` sketch · `195-PATTERNS.md` § 2 and A5/A6/A7
  </read_first>
  <action>
Create `frontend/src/components/files/fileRowUtils.ts` — pure, no React import.

  - `export function formatBytes(bytes: number): string` — **hoisted character-for-character** from
    `OutputFileCard.tsx:25-29`. Three branches, identical. This is a hoist, never a re-derivation:
    all three shipped copies are already identical, which is why one of them carries the comment
    *"Keep byte-for-byte identical."*
  - `export function baseName(path: string): string` — hoisted from `WorkflowRunPage.tsx:160-162`,
    including the `|| "download"` fallback.
  - `export type FileSource = { kind: "sandbox"; url: string } | { kind: "workspace"; threadId: string; fileId: string } | { kind: "unavailable" }`
    — D-05's discriminated source, with the third arm made explicit because both live surfaces already
    have it (chat when `url` is absent, the run page when `id` is absent) and CONTEXT's two-arm
    wording omits it.
    ⚠ **State the recorded DEVIATION in the docblock, with its reason.** RESEARCH sketched a shared
    `downloadFrom(src, filename)` dispatcher; this plan does **not** create one, and the reason is
    measured: `WorkflowRunPage.test.tsx:1450-1455` pins `downloadWorkspaceFile(` at **exactly one**
    occurrence in page source **and** pins `downloadWorkspaceFile(runThreadId` — a tenancy property
    (the page must download from the RUN's thread, never the viewed one), not a style rule. Routing the
    call through a util would drop that to zero and force inverting a fence that guards a real
    security property. So `FileSource` is the shared **vocabulary** each surface maps to its own
    live-vs-dead presentation, and the two shipped download calls stay where they are — along with
    their two deliberately different error placements (chat in-row, auto-clearing after 3 s; the run
    page section-level with `data-testid="run-download-error"` and no auto-clear). RUN-03 asks for one
    **presentation**, and that is what ships.
  - `export function byNewestFirst(a: { created_at?: string }, b: { created_at?: string }): number`
    — D-12's comparator, and **NEW GROUND: no comparator in this repository sorts a missing key
    FIRST.** Copy `PendingAskCard`'s guard *shape* and its habit of naming both data regimes in the
    comment; do **not** copy its `return 0`, which yields insertion order and therefore places the
    just-appended live deliverable **LAST** — the exact inverse of D-12's intent.
    Contract: both keys present → `b.created_at.localeCompare(a.created_at)` (the house DESC idiom,
    three shipped call sites); `a` missing and `b` present → `a` first; `b` missing and `a` present →
    `b` first; both missing → `0` (stable, insertion order preserved). The comment must state WHY a
    missing key is newest: the SSE payload carries no `created_at` (`lib/api.ts:845-851`) and the store
    appends, so an absent key means *"arrived during this session, after everything the GET returned."*

Author `frontend/src/components/files/__tests__/fileRowUtils.test.ts`:
  - `formatBytes` at each branch boundary (`0`, `1023`, `1024`, `1048575`, `1048576`) — the same values
    the shipped surfaces render, so a drift from the hoisted original is visible
  - a **byte-identity** case: the hoisted body matches the pre-change original. Capture the original
    with `git show <BASE_SHA>:frontend/src/components/chat/OutputFileCard.tsx`, extract the function,
    and compare against the new module's source via `?raw`. ⚠ Guard the `?raw` import with a length
    AND an identity assertion (`toContain("export function byNewestFirst")`) — 192.1 measured a
    renamed module swept against the **empty string**, passing green. Read `BASE_SHA` from
    `195-BASELINE.md`; do not assume `HEAD~n`.
  - `baseName` for a nested path, a bare name, a trailing slash, and the empty string (→ `download`)
  - `byNewestFirst`: both-keys-present DESC; **`a` missing sorts first**; **`b` missing sorts first**;
    both missing → stable order; and applied through `[...list].sort(byNewestFirst)` on a **two-row
    fixture whose two orders differ** (the `PendingAskCard.test.tsx:205-222` positive control — without
    it the fence passes on a one-row list forever)
  - a purity case: the input array is not mutated (`expect(input).toEqual(inputCopy)`)

**Plants** — real edits to `fileRowUtils.ts`, restored md5-identical, each RED with case names:
  P4(a) delete the `.sort` guard so the comparator always returns 0; P4(b) invert to ASC;
  P4(c) ⚠ **change the missing-key arms to sort `undefined` LAST** — the fixture whose NEWEST row has
  `created_at: undefined` must red. **(c) is what F7 exists for**: without it the fence proves only the
  reconciled regime, which is not the one the deliverable arrives in. Plus one plant per `formatBytes`
  branch boundary and one on `baseName`'s fallback.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/files/__tests__/fileRowUtils.test.ts && npx tsc --noEmit -p tsconfig.app.json 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/files/fileRowUtils.ts` exports `formatBytes`, `baseName`, `byNewestFirst` and the type `FileSource`; the file imports nothing from `react`
    - `diff <(git show $BASE_SHA:frontend/src/components/chat/OutputFileCard.tsx | sed -n '25,29p') <(sed -n '/^export function formatBytes/,/^}/p' frontend/src/components/files/fileRowUtils.ts | sed 's/^export //')` shows only the `export` keyword differing
    - The suite has at least one case where the row with `created_at: undefined` is asserted to be at index 0 of the sorted output
    - The suite has a two-row fixture whose unsorted and sorted orders differ (the positive control)
    - The `?raw` import carries both a `.length` assertion and a `toContain` identity assertion
    - The docblock contains the `downloadFrom` deviation paragraph naming `WorkflowRunPage.test.tsx:1450-1455`
    - All plants recorded with failing case NAMES, RED output, and md5 before == after — **including P4(c)**
    - `tsc -p tsconfig.app.json` equals the `195-BASELINE.md` figure
  </acceptance_criteria>
  <done>One formatter proved byte-identical to its origin, one basename, D-05's discriminated source, and a comparator whose missing-key arm — the arm the live deliverable actually takes — has its own pinned, RED-driven case.</done>
</task>

<task type="auto">
  <name>Task 3: FileRow.tsx — the ONE row markup, asChild, three shipped densities; then adopt both suites into the gate</name>
  <files>frontend/src/components/files/FileRow.tsx, frontend/src/components/files/__tests__/FileRow.test.tsx, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/components/ui/button.tsx` :36-56 — the ONLY first-party `asChild` in the tree; copy `forwardRef` + `const Comp = asChild ? Slot : "div"` + `cn` + `displayName` verbatim
    - `frontend/src/components/ingestion/NavRow.tsx` :1-66 — the docblock template and the prop discipline (values, `ReactNode` slots, callbacks; **no** hook, **no** stateful child)
    - `frontend/src/components/chat/OutputFileCard.tsx` :92-119 and :161-184 — the markup to copy, both branches
    - `frontend/src/components/panel/FilesSection.tsx` :219-269 and `frontend/src/pages/WorkflowRunPage.tsx` :1049-1082 — the other two shipped skins
    - `frontend/src/lib/utils.ts` :4-6 — `cn` is `twMerge(clsx(...))`
    - `frontend/src/__tests__/components/NavRow.test.tsx` :1-30 — a shared-row primitive's own suite, and `renderWithTooltip`
    - `scripts/vitest-count-gate.cjs` :2113-2220 and :2360-2400 — the two knobs, the same-commit timing rule, the file-level house rule
    - `.planning/phases/195-show-the-deliverable/195-RESEARCH.md` § "The recommended shared row" · `195-PATTERNS.md` § 1 and § 9
  </read_first>
  <action>
Create `frontend/src/components/files/FileRow.tsx`. This is **new ground twice over** and the docblock
must say so, so a reviewer does not read either as a house idiom being followed: it is the tree's
**second** first-party `asChild` component (the only other is `ui/button.tsx`), and
`components/files/` is the tree's **first** shared non-primitive component directory (17 `components/*`
dirs are all feature-scoped, `ui/` is reserved for 16 shadcn primitives, and `panel/PanelSection.tsx`
being imported from `metadata/DocumentDetailPanel.tsx` is the shipped instance of exactly the
mis-homing D-06 forbids).

Props:

    asChild?: boolean            // default false; when true the caller's element becomes the wrapper
    children?: ReactNode         // the caller's wrapper element, marked with <Slottable>
    density: "chat" | "panel" | "run"
    name: string                 // the visible label; the caller derives it
    mimeType?: string            // forwarded to fileIcon for mime-first resolution
    sizeBytes?: number           // OMITTED renders no size cell (OutputFileCard's `size?` case)
    metaSuffix?: string          // appended inside the size cell, e.g. " · v2" (panel)
    supersedes?: string          // D-08's "Replaces: …" subline
    errorText?: string | null    // chat's in-row error line
    trailing?: "download" | "spinner" | "dead" | "none"
    trailingSlot?: ReactNode     // rendered BEFORE the size cell — the panel's Template badge group
    className?: string

Body: `forwardRef<HTMLElement, FileRowProps>`, `const Comp = asChild ? Slot : "div"`, and under `Comp`
render `<Slottable>{children}</Slottable>` followed by the row's own children in this order — which is
the order **all three** shipped surfaces already use:

  1. the icon wrapper span → exactly ONE `fileIcon(name, …)` call, parameterised by density
  2. `flex-1 min-w-0 flex flex-col` containing the name span, then the `supersedes` subline when
     present (`Replaces: {supersedes}`), then the `errorText` line when present
  3. `trailingSlot`
  4. the size cell — rendered only when `sizeBytes != null` — as `formatBytes(sizeBytes)` followed by
     `metaSuffix ?? ""`
  5. the trailing affordance per `trailing`: `download` → the `Download` glyph; `spinner` → `Loader2`
     with `animate-spin`; `dead` → the D-08 **`<span aria-disabled="true" title="Download unavailable
     — this file has no link">` containing the `Download` glyph and the text `Download unavailable`**;
     `none` → nothing.
     ⚠ **The dead affordance MUST be a `<span>`, never a `<button>`.** That is the only reason
     `WorkflowRunPage.test.tsx:991`'s `expect(region.querySelectorAll("button")).toHaveLength(0)`
     survives plan 06's unification. Write that reason in the code, in words — it must not read as luck.

Encode the three shipped skins in ONE `const DENSITY: Record<"chat"|"panel"|"run", {...}>` record in
this file, each entry carrying a comment citing the origin `file:line` from the table in `<interfaces>`
above, and each supplying: `iconPx`, `iconRibbon`, `iconTone`, `iconClass`, `nameClass`, `sizeClass`,
`rowClass` (the layout-only part the row owns: `flex items-center` plus the surface's `gap-*`), plus a
`deadOverrides` slot. `deadOverrides` is populated **only for `chat`** (icon wrapper `opacity-70`,
name `text-foreground/60`) and is empty for `panel` and `run` — so the run page's id-less row gains the
affordance and **nothing else changes about it**, which is the whole of the delta plan 06 ships.

⚠ **`DENSITY` is per-SURFACE, not per-file-importance.** State that in the docblock and cite
D-095.1-06: the hero/working split was reversed and `variant` is inert. Anything shaped like a
per-file variants block re-introduces the retired pattern.

⚠ **`forwardRef` is load-bearing, not house style** — `FilesSection.tsx:109` and `:221-223` store row
elements in a `Map<string, HTMLDivElement | null>` for roving arrow-key focus, so a row that swallows
the ref breaks keyboard navigation. Generic is `HTMLElement`, because the row is handed an `<a>`, a
`<div role="option">` and a `<button>`.

⚠ **Radix `mergeProps` JOINS `className`, it does not twMerge.** So `rowClass` must carry layout only
and must not compete with any caller class on the same property. `padding`, `border`, `background`,
`hover`, `ring`, `text-align` and `width` stay with the caller — that is why the three shipped rows
survive unchanged.

Write the NavRow-style **"the debt fixed HERE, while extracting"** section with exactly one entry:
the run page's id-less row was a silent non-interactive `<div>` with no cue — the ❌ pattern
`BUG-260523-03` closed on the chat side — and it gains the shipped *"Download unavailable"* affordance.
And write the NavRow-style **"it holds NO business logic"** sentence naming what stays in the caller:
**the preview activation, the viewed-thread read, the download call and the section-level error.** That
sentence is where this phase's mechanical constraint gets recorded in the code itself.

Author `frontend/src/components/files/__tests__/FileRow.test.tsx`:
  - each density renders the icon, the name and (when given) the size; the size cell is ABSENT when
    `sizeBytes` is omitted
  - `metaSuffix` appends inside the size cell (`376 B · v2`)
  - `supersedes` renders `Replaces: …`; absent → no such text
  - `errorText` renders; `null` → absent
  - each `trailing` value: `download` renders a glyph and **zero** `<button>`; `spinner` renders the
    spinning glyph; **`dead` renders a `<span aria-disabled="true">` with the exact title and copy and
    ZERO `<button>` elements**; `none` renders neither
  - `asChild` with an `<a href download>` child → the rendered root is an `<a>` carrying BOTH the
    caller's `href`/`download` and the row's classes; with a `<button type="button">` child → root is
    a `<button>`; with a `<div role="option" tabIndex={0}>` child → root keeps `role="option"` and
    `tabIndex`
  - **ref forwarding**: a `ref` passed to `FileRow` with `asChild` resolves to the caller's element
    (`expect(ref.current?.tagName).toBe("BUTTON")`) — this is the roving-focus contract
  - **className merge order**: the caller's class is present AND the row's layout class is present on
    the same element (proving the join, and catching a future twMerge assumption)
  - the chat dead skin: `trailing="dead"` with `density="chat"` dims the icon wrapper and the name;
    with `density="run"` it does **not** (the two-arm assertion that pins `deadOverrides`)
  - the icon path: exactly one glyph element inside the icon wrapper, and for `density="chat"` a
    `.EXT` ribbon is present while for `panel`/`run` it is absent

**Plants** — real edits to `FileRow.tsx`, restored md5-identical, each RED with case names: change the
dead affordance from `<span>` to `<button>` (must red the zero-button case); drop the
`aria-disabled`; change the dead title/copy; drop `forwardRef` (must red the ref case — ⚠ if it does
not, the ref case is inert and that is a FINDING); reorder `trailingSlot` after the size cell; drop
`Slottable` so the caller's element nests instead of merging; populate `deadOverrides` for `run`.

**Then adopt both new suites into the gate**, FILE-LEVEL, in this same commit:
`src/components/files/__tests__/fileRowUtils.test.ts` and
`src/components/files/__tests__/FileRow.test.tsx`.
⚠ **NOT the bare directory `src/components/files`** — it would silently adopt every future suite in
this phase's own new directory, the ownership trap the script argues against three times.
⚠ Confirm both paths with `ls` before writing the entries — a `TARGETS` path that does not exist makes
the gate **ERROR (exit 2)**, not fail. ⚠ Read both BASELINE numbers from the gate's own printed
`actual` column across **two agreeing runs**; never hand-count `it(` literals. ⚠ Confirm both bare
filenames are unique tree-wide (`git ls-files | grep -c`) — the BASELINE key space is global.
`FileRow.sweep.test.ts` does **not** exist yet and its entry belongs to plan 195-07, not here.
  </action>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run --maxWorkers=2 src/components/files/__tests__/ && GSD_VITEST_MAX_WORKERS=2 node ../scripts/vitest-count-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/files/FileRow.tsx` exists, exports `FileRow`, sets `FileRow.displayName`, and is ≥ 90 lines
    - `grep -c "useViewingThread\|FilePreview\|useWorkspaceFiles" frontend/src/components/files/FileRow.tsx` returns **0** — the row reads no hook and names no previewer
    - `grep -c "cva\|buttonVariants" frontend/src/components/files/FileRow.tsx` returns 0 — no variants block
    - `grep -c "fileIcon(" frontend/src/components/files/FileRow.tsx` returns exactly **1**
    - `grep -c "Slottable" frontend/src/components/files/FileRow.tsx` returns ≥ 1
    - `grep -c "aria-disabled" frontend/src/components/files/FileRow.tsx` returns ≥ 1 and the dead affordance's element is a `<span` (assert by reading the JSX, and by the suite's zero-button case)
    - The docblock contains: the "second first-party asChild" statement, the "first shared non-primitive directory" statement, the "no business logic — the preview activation, the viewed-thread read, the download call and the section-level error live in the caller" sentence, the "debt fixed HERE" entry naming the run page's silent dead row, and the "DENSITY is per-surface, not per-file-importance" statement citing D-095.1-06
    - `src/components/files/__tests__/` reports 0 failing; both suites' exact counts recorded from the gate's `actual`
    - `grep -c "src/components/files/__tests__/FileRow.test.tsx" scripts/vitest-count-gate.cjs` ≥ 1 and the same for `fileRowUtils.test.ts`; `grep -n '"src/components/files"' scripts/vitest-count-gate.cjs` returns nothing
    - Count gate VERDICT line `OK`, `failed 0`, on two agreeing runs, quoted verbatim
    - The ungated command from `195-VALIDATION.md` green
    - `tsc -p tsconfig.app.json` equals the `195-BASELINE.md` figure
    - No file under `frontend/src/components/chat/`, `frontend/src/components/panel/` or `frontend/src/pages/` is modified by this plan (`git diff --numstat` empty for all three)
  </acceptance_criteria>
  <done>One row component can render all three shipped skins from one markup, its wrapper is supplied by the caller, it reads no hook and names no previewer, and both of its suites are inside the gate with numbers the gate itself printed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model/sandbox/user-derived filename → the render layer | `name`, `supersedes` and `metaSuffix` are backend-derived strings that cross into JSX inside `FileRow`, and `name` is additionally parsed for an extension by `fileIcon` |
| **none new at the network layer** | this plan adds no fetch, no route, no hook, no persistence and no dependency (`@radix-ui/react-slot` is already in `package.json:27`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-195-03-01 | Tampering (XSS) | `FileRow`'s `name` / `supersedes` / `metaSuffix` | mitigate | Rendered as React **text children** only. `FileRow.tsx` must contain zero occurrences of `dangerouslySetInnerHTML` (assert with `grep -c` → 0), and one suite case must render `name` and `supersedes` set to `<img src=x onerror=alert(1)>` and assert they appear as literal text with no `img` element in the container. |
| T-195-03-02 | Tampering (XSS) | `fileIcon`'s ribbon label | mitigate | The ONLY filename-derived value reaching the DOM is the PARSED extension, uppercased — `fileIcon.tsx`'s existing T-095-01-01 note. The widening must not add a path that renders the raw filename; assert with a case whose filename is `<b>x</b>.docx` rendering the ribbon as `.DOCX` and no `b` element. |
| T-195-03-03 | Denial of Service (render crash) | `EXT_MAP` lookup | mitigate | `EXT_MAP` is a plain object literal, so `EXT_MAP["constructor"]` returns the `Object` function and `?? DEFAULT_SPEC` never fires — `phaseGlyph.tsx:107-122` records this as a real shipped render crash. Task 1 adds an own-property guard and a case for a file named `x.constructor`. |
| T-195-03-04 | Elevation of Privilege | the `dead` affordance | mitigate | `aria-disabled` is a **presentational** guard, never an authorization one. `FileRow` performs no download and receives no thread id or file id, so it cannot be induced to fetch across a thread or tenant boundary — the download call and its `if (!runThreadId \|\| !fileId) return` code-level gate stay in the caller (plan 06), behind the route's own `_verify_thread_ownership` + RLS. Pinned by the `grep` for `useWorkspaceFiles`/`useViewingThread` returning 0. |
| T-195-03-SC | Tampering | package installs | n/a | **No package installed.** `frontend/package.json` and `package-lock.json` untouched — assert `git diff --numstat` EMPTY for both. `@radix-ui/react-slot` is a shipped dependency already imported by `ui/button.tsx`. No `slopcheck` owed. |

**Honest scale statement:** the only real attack surface here is a string sink and an object-literal
lookup, and both are pinned by cases rather than asserted in prose. There is no new network surface,
no new route and no new persistence, and inventing a high-severity threat to look thorough would be
worse than saying so.
</threat_model>

<verification>
- `frontend/src/components/files/` contains the row, the utils and two suites; all green
- `fileIcon.test.tsx` count increased, 0 failing, all 11 original titles intact
- Every plant across the three tasks observed RED with failing case names and md5 before == after
- Count gate `OK` / `failed 0` on two agreeing runs; ungated command green; `tsc` at the baseline figure
- **No surface converted:** `git diff --numstat` is EMPTY for `frontend/src/components/chat/OutputFileCard.tsx`, `frontend/src/components/panel/FilesSection.tsx`, `frontend/src/pages/WorkflowRunPage.tsx`, `frontend/src/components/chat/MessageItem.tsx` and `frontend/src/components/chat/tool-bodies/ExecuteCodeBody.tsx`
</verification>

<success_criteria>
There is exactly ONE row markup, ONE byte formatter, ONE basename and ONE icon path in the tree, each
with its own RED-driven coverage inside the count gate — and nothing has been converted yet, so the
three surface plans in wave 3 are pure adoption.
</success_criteria>

<output>
Create `.planning/phases/195-show-the-deliverable/195-03-SUMMARY.md` when done.
</output>
