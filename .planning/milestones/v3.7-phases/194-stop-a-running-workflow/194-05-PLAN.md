---
phase: 194-stop-a-running-workflow
plan: 05
type: execute
wave: 3
depends_on: ["194-01"]
files_modified:
  - CLAUDE.md
  - .claude/skills/sketch-findings-agentic-rag/references/workflow-run-surface.md
  - frontend/src/stores/streamsStore.ts
  - .planning/reported-bugs/BUG-260815-07-cannot-delete-a-workflow.md
  - .planning/reported-bugs/chat-stuck-on-starting-workflow-with-duplicate-icon.md
  - .planning/reported-bugs/BUG-260808-02-approval-hands-off-to-chat.md
  - .planning/reported-bugs/workflow-run-history-not-reachable-from-canvas.md
autonomous: true
requirements: [RUN-01]
validation_rows: []
must_haves:
  truths:
    - "RunCard.tsx and WorkspacePanel.tsx are ROWS in the CLAUDE.md hot-file ledger, so G-5 can see them in the next phase's audit."
    - "The sentence explaining that each file was ABSENT from the table survives, because that absence is why eight phases of G-5 never fired."
    - "Every claim this phase measured FALSE is corrected BESIDE its original, never over it — including two claims inside this phase's own CONTEXT."
    - "The validated sketch's cancel copy no longer says partial work was discarded, and the amendment sits beside the operator-approved original."
  artifacts:
    - path: "CLAUDE.md"
      provides: "Two new hot-file ledger rows in the db/workflows.py + publish_service.py shape"
      contains: "RunCard.tsx"
    - path: "frontend/src/stores/streamsStore.ts"
      provides: "The corrected WorkflowLock.runId JSDoc — false at two of its five write sites"
      contains: "CORRECTED"
    - path: ".claude/skills/sketch-findings-agentic-rag/references/workflow-run-surface.md"
      provides: "The amended cancel copy, recorded beside the original"
      contains: "AMENDED"
  key_links:
    - from: "CLAUDE.md hot-file ledger"
      to: "the discuss-phase G-5 audit"
      via: "the audit scans PLAN.md files_modified AGAINST this table"
      pattern: "WorkspacePanel.tsx"
---

<objective>
D-02's phase deliverable, plus every correct-beside-not-over debt this phase incurred.

Purpose: **a guardrail cannot see what is absent from its list.** The discuss-phase G-5 audit scans
PLAN.md `files_modified` *against the `CLAUDE.md` hot-file ledger*, so a hot file missing from the
table is permanently invisible to its own guardrail. `WorkflowsPage.tsx` escaped G-5 for ten
consecutive phases that way, `WorkflowDoorSwitch.tsx` for six, `WorkflowBuilderPage.tsx` for ten and
`db/workflows.py` for seventeen. `RunCard.tsx` (8 phases) and `WorkspacePanel.tsx` (8 phases) are
the next two, and **neither filename appears in `CLAUDE.md` at all** — measured, not assumed.

Output: two ledger rows, three correct-beside corrections, and a verified reported-bug routing.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/194-stop-a-running-workflow/194-RESEARCH.md
@.planning/phases/194-stop-a-running-workflow/194-PATTERNS.md
@.planning/phases/194-stop-a-running-workflow/194-CONTEXT.md
@.planning/phases/194-stop-a-running-workflow/194-BASELINE.md
@.planning/phases/194-stop-a-running-workflow/194-MEASUREMENTS.md
@CLAUDE.md

**Base commit — ASSERT FIRST.** Dispatched against `d2a3b51b` on `develop`. In a worktree, run
`bash scripts/bootstrap-worktree.sh "$(pwd)"` FIRST, then assert `d2a3b51b` is the base ancestor.
Tear down only with `bash scripts/teardown-worktree.sh <path>`; never `rm -rf`.

**Serialization:** docs-only — parallel-safe. It runs no test suite that mutates the database.
</context>

<non_goals>
- ⛔ **No behaviour change anywhere.** `streamsStore.ts` receives a COMMENT-only edit; a `git diff`
  showing any non-comment line in that file is a failure of this plan.
- ⛔ **Do not delete, overwrite or "tidy" any existing ledger cell, sketch line, docblock or bug
  frontmatter clause.** Corrections go BESIDE. *A deferral that lives only in a deleted comment is
  exactly as invisible as one that was never written* (193.2 WR-05).
- ⛔ Do not add a ledger row for any file this phase does not touch, and do not re-derive other
  rows' figures — they are their own phases' debts.
- ⛔ Do not change `BUG-260815-03`'s routing. It is DEFERRED to Phase 195 with `folded_into: null`
  (D-15); marking it folded would put a bug in the "claimed" state against a phase that
  structurally cannot close it.
- ⛔ Do not close `BUG-260815-07`'s non-reproducible delete-failure half. Only the reproducible
  stuck-active-runs clause is folded.
</non_goals>

<tasks>

<task type="auto">
  <name>Task 1: Add the two hot-file ledger rows to CLAUDE.md (D-02)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - `CLAUDE.md` § *Workflow guardrails (MANDATORY)* — G-5's wording and the sentence *"the orchestrator must scan PLAN.md `files_modified` against this ledger"*
    - `CLAUDE.md` hot-file ledger rows for `backend/app/db/workflows.py` and `backend/app/services/harness/publish_service.py` — **the two shape analogs**; read both cells in full
    - `.planning/phases/194-stop-a-running-workflow/194-PATTERNS.md` § 12 — the seven structural elements extracted from those rows, and the two extra habits both keep
    - `.planning/phases/194-stop-a-running-workflow/194-RESEARCH.md:1043-1105` — the DRAFT row content (measured at `05f664a0`)
    - `.planning/phases/194-stop-a-running-workflow/194-BASELINE.md` — the figures re-derived at THIS commit, which override the draft
    - `.planning/phases/194-stop-a-running-workflow/194-CONTEXT.md` D-01 and D-02
  </read_first>
  <action>
Add two rows to the `CLAUDE.md` hot-file ledger table, one for
`frontend/src/components/chat/RunCard.tsx` and one for
`frontend/src/components/panel/WorkspacePanel.tsx`, using the `db/workflows.py` /
`publish_service.py` cell shape. RESEARCH drafted the content; **re-derive every figure from
`194-BASELINE.md` (measured at this phase's own commit) and use those** — a figure written at a
plan's close goes stale on the next commit that touches the file, and this table has documented that
about itself four times.

Each row carries the seven structural elements PATTERNS extracted:
1. **Touch list** — the phase buckets, with this phase's own landing plan + sha.
2. **The triple** — `N commits across M phases`, `before → after L`.
3. **The quick-task / untagged-bucket subtraction, stated explicitly and NAMED.** ⚠ For
   `RunCard.tsx` the raw recipe returns **nine** buckets and the ninth is `streaming` — an untagged
   `fix(streaming)` + `revert(streaming)` 075.x follow-up pair, **not a phase** — so the phase count
   is **8**. Record both, the loser beside the winner, exactly as the `WorkflowBuilderPage.tsx` row
   already documents about `260809` / `260814`. For `WorkspacePanel.tsx` state that there are **ZERO**
   non-phase buckets and all eight are real phases.
4. **⚠ The ABSENT sentence, which MUST survive future edits:** *this file was ABSENT from this table
   until Phase 194; eight phases touched it and G-5 never fired once, because the discuss-phase
   audit scans PLAN.md `files_modified` against this table — a hot file missing from the table is
   permanently invisible to its own guardrail.* Name the four prior victims (`WorkflowsPage.tsx`
   ten phases, `WorkflowDoorSwitch.tsx` six, `WorkflowBuilderPage.tsx` ten, `db/workflows.py`
   seventeen) so the pattern is legible rather than anecdotal.
5. **Re-derive commands, inline** — the `git log --oneline … | wc -l`, the standard `sed` bucket
   recipe, and `wc -l`, each with its answer at this commit.
6. **The G-5 verdict + the MEASURED reason it was honoured by construction.** For `WorkspacePanel.tsx`
   the construction is **CONDITIONAL and the condition is load-bearing**: the Stop mounts as an
   additive sibling inside the shipped `showTimeline` gate calling `stopThread(threadId)` with a
   thread id the file already holds, through a ninth hook from a module it already imports eight
   from — **no new fetch, no new prop, no new store slice, no new state**. Had it instead needed a
   producer run id of its own, that would have been a SECOND concern and the refactor recommendation
   would have been owed FIRST. Quote plan `194-03`'s measured before → after `useState(` count as
   the evidence. For `RunCard.tsx`, the reason is that its `cancelled` vocabulary already ships and
   reads only the already-persisted `message.runStatus`, so an extension reads existing state.
7. **The binding invariants + the NAMED next seam + `It inherits N / M / L`.**
   - `WorkspacePanel.tsx`: an additive sibling may NOT read `PhaseCard` / `PhaseTimeline` internals
     or add a prop to either (the shipped red line, `:85-93` and `:182-190`); ⚠ **no control here
     may resolve a cancel through `workflowLock.runId`** — the file's own `RunSeam` docblock
     (`:160-164`) records that the lock's id is *"overwritten with a PRODUCER run id at kickoff"*
     and that *"both ids are bare uuids, so a swap typechecks and then resolves nothing"*, and
     plan 194-03's F-1 fence now enforces it mechanically across three directories. Next seam: the
     five data-section mounts versus the panel shell/collapse state machine versus the
     `RunSeam` / soul receipts.
   - `RunCard.tsx`: the `cancelled` glyph/word pair is the SHIPPED starting point and may be
     extended but not replaced without a stated reason (D-14); ⚠ **`■` is NOT in
     `icon-convention.md` §4's canvas glyph table and neither is the sketch's `⏹`**; the
     terminal-state tiering is Phase 174's and is not re-litigated. ⚠ **There is no badge ceiling
     of any kind on this file** — the 188.2 two-badge `@ts-expect-error` control guards the CANVAS
     `PhaseNodeCard`, not this one. Next seam: the run-identity header versus the status/terminal
     vocabulary versus the elapsed-timer machinery.

**Both extra habits both analog rows keep, and 194's rows must keep:**
- **A correction on measurement is recorded BESIDE, never over.** 194 owes exactly two, both against
  its own CONTEXT: D-01 says `RunCard.tsx` is *"20 commits / ~9 buckets"* — the commits and buckets
  are exact, but the PHASE count is 8; and D-01 says both filenames *"occur only inside other rows'
  prose"* — measured, `grep -o "RunCard.tsx" CLAUDE.md | wc -l` and
  `grep -o "WorkspacePanel.tsx" CLAUDE.md | wc -l` both returned **0** before these rows existed.
  **Neither filename appeared in `CLAUDE.md` at all.** The conclusion is unchanged and strengthened;
  record it that way.
- **A declined override is recorded as a measurement.** A G-5 override was OFFERED AND DECLINED
  (D-01, the fourth consecutive phase to decline one), so `.planning/STATE.md` records NO guardrail
  override for Phase 194 — **that absence is a measurement, not an omission.** Verify it before
  writing it.

⚠ **Write any grep-able sentence on ONE line.** 193.2-08 measured a rule written WRAPPED across two
lines failing its own literal `grep -q` and reading as *"already fixed"*.
  </action>
  <verify>
    <automated>test $(grep -oc "frontend/src/components/chat/RunCard.tsx" CLAUDE.md) -ge 1 &amp;&amp; test $(grep -oc "frontend/src/components/panel/WorkspacePanel.tsx" CLAUDE.md) -ge 1 &amp;&amp; grep -q "ABSENT FROM THIS TABLE UNTIL PHASE 194" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -o "RunCard.tsx" CLAUDE.md | wc -l` and `grep -o "WorkspacePanel.tsx" CLAUDE.md | wc -l`
      both now return ≥ 1, and each filename appears as the **first cell of a table row**, not only
      inside prose.
    - Both rows carry all seven structural elements; a reader can re-derive every figure from the
      commands quoted inside the row.
    - The ABSENT sentence appears for **both** files and is on ONE line each (verified with a literal
      `grep -q` of the sentence, not by eye).
    - Both CONTEXT corrections are present and recorded beside the original wording (the `~9 buckets`
      → 8 phases correction, and the "occurs only in prose" → **0 occurrences** correction).
    - The declined-override sentence is present AND verified: `grep -ci "guardrail override" .planning/STATE.md`
      shows no Phase-194 entry. If one exists, do not write the sentence — report the discrepancy.
    - Each row ends with an explicit `It inherits N / M / L`.
    - No existing ledger row was edited — `git diff -- CLAUDE.md` shows added lines only
      (`git diff --numstat -- CLAUDE.md` deletions column is 0, or every deletion is inside the two
      new rows).
  </acceptance_criteria>
  <done>Both files are ROWS in the ledger, with re-derivable figures, the G-5 verdict, the binding invariants, the named next seam, the two CONTEXT corrections beside their originals, and the ABSENT sentence that must survive future edits.</done>
</task>

<task type="auto">
  <name>Task 2: The correct-beside-not-over corrections — the lock's JSDoc and the sketch's cancel copy</name>
  <files>frontend/src/stores/streamsStore.ts, .claude/skills/sketch-findings-agentic-rag/references/workflow-run-surface.md</files>
  <read_first>
    - `frontend/src/stores/streamsStore.ts:45-65` — the `WorkflowLock` interface and its `runId` JSDoc
    - `.planning/phases/194-stop-a-running-workflow/194-PATTERNS.md` § *THE LANDMINE* — all five write sites with their exact assignment lines, and the measurement that `workflowLock.runId` has exactly ONE production read in the whole frontend
    - `frontend/src/components/panel/WorkspacePanel.tsx:160-181` — the `⚠ CORRECTED (CR-03)` block: the shipped SHAPE for a correction recorded beside its original
    - `.claude/skills/sketch-findings-agentic-rag/references/workflow-run-surface.md` — lines 25, 135 and 227, the cancel copy and its `⏹`
    - `.planning/phases/194-stop-a-running-workflow/194-CONTEXT.md` D-13 and D-14
    - `.claude/skills/sketch-findings-agentic-rag/references/icon-convention.md` §4
  </read_first>
  <action>
**(a) `streamsStore.ts` — COMMENT ONLY.** The JSDoc reads *"The workflow_runs.id
(active_workflow_run_id) that owns the lock."* Measured, that is **false at two of its five write
sites**: the mount reconcile (`StreamsProvider.tsx:1840`) and the banner path (`ChatArea.tsx:176`)
store a `workflow_runs.id`, while the kickoff seed (`:1983`) and the Continue re-subscribe (`:3034`)
store a **producer `runs.run_id`**, and the `cap_paused` SSE (`:965`) inherits whichever the SSE
carried. Correct it in the shipped `⚠ CORRECTED` shape: keep the original sentence visible, state
that it is false and at which sites, and state the consequence — **both ids are bare uuids, so a
swap typechecks and then resolves nothing**, `DELETE /runs/{id}` accepts only the producer id, and
`cancelRun` **swallows 404**, so a Stop wired to this field silently succeeds while doing nothing.
Point at the one legitimate production read (`MessageItem.tsx:596`/`:599` → `continueRun`, correct
because `/continue` has the dual-id fallback) and at plan 194-03's F-1 fence which now enforces this
mechanically. ⚠ Note that the project **already recorded this once** — `WorkspacePanel.tsx:160-164`,
Phase 188 — **and it was not carried forward**; that is why it is being recorded at the type's own
declaration this time.

**(b) The sketch's cancel copy.** `workflow-run-surface.md:25` specifies
*"⏹ Run cancelled — no deliverable produced · partial work discarded"*. ⚠ **"partial work
discarded" is exactly the sentence D-13 forbids**: a stopped run KEEPS its completed phases, and
collapsing to a bare `cancelled` that hides partial work was explicitly REJECTED because it
discards evidence the database still holds. Amend the copy under D-14 (*may extend; may not replace
without stating why*) and **record the amendment BESIDE the operator-approved original**, marked
`⚠ AMENDED (Phase 194, D-13/D-14)`, with the reason. Do the same at `:135` and `:227` if they carry
the same sentence. ⚠ Also flag the `⏹` in the same amendment: it is **absent from
`icon-convention.md` §4's canvas glyph table**, so it is a net-new mark that owes a FLAGGED
PROPOSAL, and 194 did not ship it — the Stop **control** reuses the shipped lucide `Square`
(`MessageInput.tsx:420`, `ActiveRunsTray.tsx:132`) and the stopped **state** uses the mark plan
194-04 chose by measurement. ⚠ **Record, beside RESEARCH's three-mark table, the FOURTH shipped
mark it missed:** `WorkflowRunPage.tsx:299` renders *"⊘ Cancelled"* as the run band's cancelled
sentence.

Neither edit changes any behaviour.
  </action>
  <verify>
    <automated>grep -q "CORRECTED" frontend/src/stores/streamsStore.ts &amp;&amp; grep -q "AMENDED" .claude/skills/sketch-findings-agentic-rag/references/workflow-run-surface.md &amp;&amp; cd frontend &amp;&amp; npx tsc -p tsconfig.app.json --noEmit 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `git diff -- frontend/src/stores/streamsStore.ts` contains **only comment lines** — every added
      and removed line begins with `*`, `/*`, `//` or is blank. Any other line is a failure.
    - `npx tsc -p tsconfig.app.json --noEmit` matches `194-BASELINE.md` exactly (expected 33).
    - The corrected JSDoc names **all five** write sites with their id types and states the
      404-swallow consequence, and notes that Phase 188 already recorded this once and it was not
      carried forward.
    - The sketch retains its ORIGINAL cancel sentence verbatim, with the amendment beside it under
      an `⚠ AMENDED` marker naming D-13/D-14 — `grep -c "partial work discarded"` is unchanged
      (the original survives) AND the amended sentence is present.
    - The `⏹` flag and the fourth-mark correction (`⊘` at `WorkflowRunPage.tsx:299`) are both recorded.
    - The frontend count gate is unaffected: `count gate OK`, `failed 0`, total ≥ baseline.
  </acceptance_criteria>
  <done>The lock's JSDoc tells the truth about both id types with the consequence stated, and the sketch's cancel copy is amended beside its original rather than diverged from silently.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the reported-bug routing and correct the two claims this phase measured FALSE</name>
  <files>.planning/reported-bugs/BUG-260815-07-cannot-delete-a-workflow.md, .planning/reported-bugs/chat-stuck-on-starting-workflow-with-duplicate-icon.md, .planning/reported-bugs/BUG-260808-02-approval-hands-off-to-chat.md, .planning/reported-bugs/workflow-run-history-not-reachable-from-canvas.md</files>
  <read_first>
    - All four reported-bug files, frontmatter FIRST
    - `CLAUDE.md` § *Reported bugs cross-check (MANDATORY)* — the `plan-phase` touchpoint rule: *"Verify every report with `folded_into: NNN` is actually addressed by at least one plan task. If a folded report isn't covered, either add a task or revert its status."*
    - `.planning/phases/194-stop-a-running-workflow/194-RESEARCH.md` § *CONTEXT.md claims measured FALSE or STALE*, rows 2 and 8
    - `.planning/phases/194-stop-a-running-workflow/194-MEASUREMENTS.md` — the duplicate-icon verdict from plan 194-01
    - All twelve `194-*-PLAN.md` files in this phase directory (to perform the coverage check)
  </read_first>
  <action>
**(a) The coverage check the `plan-phase` touchpoint requires.** For each report with
`folded_into: 194`, name the plan and task that addresses it, and record the mapping in the summary:
- `BUG-260815-07` (reproducible half — stuck-active runs) → plans **194-09** (the Step 3b workflow
  co-write) and **194-13** (the four-row heal receipt).
- `BUG-260815-04` (banner-advance half) → plan **194-07**. Its duplicate-icon half is routed by
  `194-MEASUREMENTS.md`.
- `BUG-260808-02` (stopping a run waiting at an approval) → plans **194-09** and **194-10** (the
  ask_user cancel sentinel ordering, F-12, and the engine's shielded expiry).
If any folded report turns out NOT to be covered by a task, **say so and either add a task or revert
its status** — never leave it silently claimed.

**(b) `BUG-260815-07` — correct the delete-blocker claim BESIDE the original.** Its `re_open_trigger`
asserts the two stuck rows are *"a permanent delete blocker"*. ⚠ **Measured FALSE:**
`delete_workflow_cascade` (`api/workflows.py:1481-1518`) cancel-firsts **and** calls
`finish_run(wf_id, "cancelled")` **unconditionally** for every in-flight row, including when
`producer_id IS NULL`; and both definitions are `is_system_global = False` with zero foreign runs,
so the 409 guard cannot fire either. Append the correction with the measurement and the file:line,
keeping the original clause visible. ⚠ **The correction does not remove the reason to heal the
rows** — they lie about being live, two threads still hold anchors, and one is an abandoned golden
run. Restate the honest justification so a later reader does not read the correction as a
de-scoping. ⚠ **The non-reproducible delete-failure half stays OPEN** — do not touch that clause.

**(c) `BUG-260815-04` — correct the stale test pointer BESIDE the original.** Its trigger cites
*"pinned by `toolMeta.test.ts:30`"*. Measured: the file is
`frontend/src/lib/__tests__/toolMeta.test.ts` and the byte-exact assertion is at **`:31`**
(`toolMeta.ts:92` is exact). Append the correction. ⚠ Also fold in `194-MEASUREMENTS.md`'s
duplicate-icon verdict: if the duplicate icon is deferred, the report's own
`re_open_trigger` already anticipates it (*"Re-open if 194 closes only the banner-advance half"*) —
make that explicit with the measured root cause named, rather than leaving it implied. And record
that the report's named prior art (`toolcallpanel-dedup-duplicates-tool-card.md`, BUG-260521-01) is
the **WRONG one** — a transient tool-card duplicate, minor, folded into 075.2 — with the correct
prior art named beside it.

**(d) `BUG-260808-02` and `BUG-260815-03`** — verify their frontmatter already matches the D-08 /
D-15 routing (`folded` with the scope fence; `deferred` with `folded_into: null` and the trigger).
If it does, record that as a measurement and change nothing. **Do not re-route `BUG-260815-03`.**
  </action>
  <verify>
    <automated>grep -q "folded_into: 194" .planning/reported-bugs/BUG-260815-07-cannot-delete-a-workflow.md &amp;&amp; grep -q "folded_into: 194" .planning/reported-bugs/chat-stuck-on-starting-workflow-with-duplicate-icon.md &amp;&amp; grep -q "folded_into: 194" .planning/reported-bugs/BUG-260808-02-approval-hands-off-to-chat.md &amp;&amp; grep -q "folded_into: null" .planning/reported-bugs/workflow-run-history-not-reachable-from-canvas.md</automated>
  </verify>
  <acceptance_criteria>
    - The plan/task coverage mapping for all three `folded_into: 194` reports is in the summary, and
      every one names a real plan number and task that exists in this phase directory.
    - `BUG-260815-07` carries the delete-blocker correction with its file:line measurement, the
      original clause still present, AND a restatement of the honest reason the rows are still healed.
    - `BUG-260815-07`'s non-reproducible half is untouched — `grep -c "NOT reproducible"` unchanged.
    - `BUG-260815-04` carries the `:30` → `:31` pointer correction, the duplicate-icon routing from
      `194-MEASUREMENTS.md`, and the wrong-prior-art correction, all beside their originals.
    - `BUG-260815-03` frontmatter is byte-unchanged (`git diff --numstat` on that file is empty) or,
      if a measurement note was added, `status: deferred` and `folded_into: null` are unchanged.
    - No report's `status` was advanced to `closed` — none of these can be verified closed until the
      phase ships and the bug no longer reproduces.
  </acceptance_criteria>
  <done>Every folded report is provably covered by a named plan task, and the two claims this phase measured false are corrected beside their originals without weakening the reason the work still ships.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Documentation and comment-only edits; no runtime path, no data, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-194-05-01 | Repudiation | the hot-file ledger | mitigate | The ABSENT sentence and the re-derive commands are required content, so a future reader can audit rather than trust. A row without them is the invisibility this plan exists to fix. |
| T-194-05-02 | Tampering | `frontend/src/stores/streamsStore.ts` | mitigate | Comment-only edit, asserted mechanically (every diff line must start with a comment token) and re-verified by a typecheck matching the baseline exactly. |
| T-194-05-03 | Information Disclosure | reported-bug files | accept | These files already ship in-repo; no new secret, credential, id or user content is added. |
| T-194-05-SC | Tampering | package installs | mitigate | None owed — no package is added. |
</threat_model>

<verification>
- `npx tsc -p tsconfig.app.json --noEmit` matches `194-BASELINE.md`; the count gate is unaffected.
- `git diff -- frontend/src/stores/streamsStore.ts` is comment-only.
- `git diff --numstat -- CLAUDE.md` shows the two rows added and no other row edited.
- Both G-5 filenames are now greppable as ledger row cells in `CLAUDE.md`.
</verification>

<success_criteria>
- The two ledger rows exist, in the analog shape, with re-derivable figures and the ABSENT sentence.
- The lock's JSDoc, the sketch's cancel copy and two reported-bug claims are corrected BESIDE their
  originals.
- Every report folded into 194 maps to a named plan task.
</success_criteria>

<output>
Create `.planning/phases/194-stop-a-running-workflow/194-05-SUMMARY.md` when done.
</output>
