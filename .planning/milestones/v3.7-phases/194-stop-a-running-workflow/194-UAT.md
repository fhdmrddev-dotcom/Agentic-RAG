---
phase: 194-stop-a-running-workflow
plan: 01
task: 3
artifact: uat-scoreboard
requirement: RUN-01
derived_at: 743965a1
branch: develop
authored_on: 2026-08-16
rows_total: 15
rows_driven: 7
driven_at: 2026-08-16
driven_at_head: 045a83dc
driven_by: Claude (verify-work, Chrome MCP against the live local stack)
worker_count_observed: 1
status: partial — 7 driven (4 pass, 3 fail), 8 blocked ⛔
---

# Phase 194 — UAT Scoreboard

> **RUN-01** — *A user can stop a running workflow at any point, and the run reports honestly that
> it was stopped.*

---

## ⚠ THESE ROWS ARE NOT TASKS IN ANY `PLAN.md`, AND A LATER READER MUST NOT GO LOOKING FOR THEM THERE

`CLAUDE.md` § *UAT scoreboard recipe (MANDATORY)* is explicit: **"UAT rows MUST be authored under
VALIDATION.md, NOT in PLAN.md tasks."** They are **authored here, in Wave 0**, and **driven at phase
verification** — after the code exists. No plan in Phase 194 contains a task that drives one of
these rows, and that is correct rather than an omission. A reader searching `194-*-PLAN.md` for
"OpenRouter" or "long-message" will find nothing; **this file is where they live.**

Consequently every `verdict` cell below reads **⬜ pending** at authoring time. The phase is not
verified until they carry a real mark.

> ⚠ **UPDATED 2026-08-16 — the rows have now been DRIVEN.** The verdict cells below carry real
> marks; the full evidence is in **§ DRIVEN RESULTS** at the foot of this file. 7 driven (4 pass,
> 3 fail), 8 blocked ⛔. **Read the single-worker caveat there before quoting any ✅ against SC#2.**

---

## Why SC#10 fires on this phase at all

`CLAUDE.md` ROADMAP SC#10, verbatim:

> Any phase touching streaming, agent loop, provider routing, or UI state MUST include UAT rows for
> cross-provider × multi-tool × parallel-thread × long-message scenarios.

Phase 194 touches **streaming** (the SSE producer's `CancelledError` arm), **the agent loop**
(`_cancel_run_internals` → `task.cancel()`), and **UI state** (four Stop mounts, `workflowLock`,
`streamingThreads`, the chat banner). **Three of the four triggers fire.** The full-bandwidth rule
applies without discount.

---

## The roster — DERIVED, never re-typed

### The command, EXECUTED at HEAD `743965a1` (2026-08-16)

```bash
cd backend && venv/Scripts/python.exe -c "
from app.config import MODEL_CAPABILITIES
import collections
g=collections.defaultdict(list)
for k,v in MODEL_CAPABILITIES.items():
    g[v.get('provider') if isinstance(v,dict) else getattr(v,'provider',None)].append(k)
for p,ms in sorted(g.items()): print(p, len(ms), ms[-1])"
```

### Raw output, pasted verbatim — this is the proof of derivation

```
anthropic 7 claude-haiku-4-5-20251001
deepseek 2 deepseek-v4-pro
google 7 gemini-3.1-flash-lite
minimax 8 MiniMax-M3
moonshot 3 moonshot-v1-8k
openai 17 o1
openrouter 9 deepseek/deepseek-v4-pro
zhipu 8 glm-5.2
```

**8 groups: `anthropic · deepseek · google · minimax · moonshot · openai · openrouter · zhipu`** —
exactly the roster `CLAUDE.md` names, arrived at by execution rather than transcription. The roster
is derived because the standing rule says the hand-typed list *"will rot the moment a provider is
added."*

### ⚠ A CORRECTION TO THE DERIVATION COMMAND ITSELF, MEASURED HERE AND RECORDED BESIDE IT

The command's third column is **`ms[-1]` — the LAST id in dict-insertion order. That is NOT the
NEWEST id, and on three of the eight groups it is markedly OLD.** The rule the roster must satisfy
is *"prefer the **newest**, registry-backed id per provider"*, so `ms[-1]` cannot be used as the
row's model without checking. Measured:

| Provider | `ms[-1]` says | Actually newest in the registry | Verdict on `ms[-1]` |
|---|---|---|---|
| openai | `o1` | `gpt-5.6-sol` / `-terra` / `-luna` | ⚠ **badly stale** — `o1` is a legacy reasoning model, 16 ids newer sit in the same group |
| moonshot | `moonshot-v1-8k` | `kimi-k2.6` | ⚠ **badly stale** — `moonshot-v1-8k` is the oldest of the three |
| anthropic | `claude-haiku-4-5-20251001` | `claude-sonnet-5` | ⚠ stale, and it is also the *weakest* tier in the group |
| google | `gemini-3.1-flash-lite` | `gemini-3.5-flash` | ⚠ stale by a minor version |
| deepseek | `deepseek-v4-pro` | `deepseek-v4-pro` | ✅ agrees |
| minimax | `MiniMax-M3` | `MiniMax-M3` | ✅ agrees |
| zhipu | `glm-5.2` | `glm-5.2` | ✅ agrees |
| openrouter | `deepseek/deepseek-v4-pro` | `deepseek/deepseek-v4-pro` | ✅ agrees |

**The command is still the right way to derive the GROUPS — its `ms[-1]` column is a
*representative*, not a recommendation.** The per-group id below was selected from the full group
dump (second command, § *Registry evidence*), which is likewise executed, not transcribed.

⚠ **Why an id absent from `MODEL_CAPABILITIES` may NEVER be substituted (SEED-040 §2026-07-31,
SEED-135):** an unregistered id resolves `capability_source=inferred` and **silently loses
`emit_tier`**. A row driven on such an id measures a **weaker configuration than the one that
ships** — the scoreboard would then be honest about a product we do not sell. Every id below was
read out of the registry.

### Registry evidence — `native_tools` and `emit_tier` per chosen id

```bash
cd backend && venv/Scripts/python.exe -c "
from app.config import MODEL_CAPABILITIES
import collections
g=collections.defaultdict(list)
for k,v in MODEL_CAPABILITIES.items():
    d = v if isinstance(v,dict) else getattr(v,'__dict__',{})
    g[d.get('provider')].append((k,d.get('native_tools'),d.get('emit_tier')))
for p,ms in sorted(g.items()):
    print('==',p,len(ms))
    for m in ms: print('   ',m)"
```

Excerpt of the raw output for the eight selected ids (full dump ran over all 61 registry entries):

```
anthropic    ('claude-sonnet-5', True, 'force')
deepseek     ('deepseek-v4-pro', True, 'force')
google       ('gemini-3.5-flash', True, 'force')
minimax      ('MiniMax-M3', True, 'force')
moonshot     ('kimi-k2.6', True, 'coerce')
openai       ('gpt-5.6-sol', True, 'force_strict')
openrouter   ('deepseek/deepseek-v4-pro', False, 'force')
zhipu        ('glm-5.2', True, 'force')
```

⚠ **One selection is arbitrary and is declared rather than hidden:** OpenAI ships **three co-newest
`gpt-5.6` variants** (`-sol`, `-terra`, `-luna`), all `native_tools True` / `emit_tier
force_strict`. `gpt-5.6-sol` was taken as the group's representative; the choice among the three is
not evidence-backed, and any of the three is an equally valid row. Swapping it does not change what
the row measures.

---

## The 8-row cross-provider scoreboard

**Every row stops a real, live workflow (harness) run.** One row per provider group — the FULL
native roster **plus** OpenRouter, not the four providers a scoreboard reaches for first. The rule
this obeys, verbatim from `CLAUDE.md`: *"testing four and calling it 'cross-provider' tests the four
we happen to think of first."*

| Provider | Model id | `native_tools` | `emit_tier` | Stop mount driven | Verdict | Evidence to capture |
|---|---|---|---|---|---|---|
| anthropic | `claude-sonnet-5` | True | `force` | **1 — panel phase spine** | ⛔ **blocked** | `workflow_runs.status='cancelled'` · `threads.active_workflow_run_id IS NULL` · the interrupted `workflow_phases` row `='cancelled'` · completed phases untouched |
| openai | `gpt-5.6-sol` | True | `force_strict` | **2 — composer Stop** | ⛔ **blocked** | same three tables + `runs.status='cancelled'` for the producer row |
| google | `gemini-3.5-flash` | True | `force` | **3 — `ActiveRunsTray`** | ⛔ **blocked** | same, driven from a DIFFERENT thread than the run's own |
| deepseek | `deepseek-v4-pro` | True | `force` | **1 — panel phase spine** | ⛔ **blocked** | same three tables · ⚠ `strict_json_schema` is **inert** on DeepSeek (no `/beta` base_url, D-122-04) — do not read a strict-mode signal off this row |
| zhipu | `glm-5.2` | True | `force` | **2 — composer Stop** | ⛔ **blocked** | same three tables |
| minimax | `MiniMax-M3` | True | `force` | **3 — `ActiveRunsTray`** | ⛔ **blocked** | same three tables |
| moonshot | `kimi-k2.6` | True | **`coerce`** | **1 — panel phase spine** | ⛔ **blocked** | same three tables · ⚠ **the ONLY `emit_tier: coerce` NATIVE row** — the weakest emission guarantee in the registry, so this is the row most likely to expose a cancel that races a malformed emit |
| openrouter | `deepseek/deepseek-v4-pro` | **False** | `force` | **2 — composer Stop** | ⛔ **blocked** | same three tables · ⚠ **the non-native tool path**, not a ninth flavour of the native one — every OpenRouter row is `native_tools: False` |

### ⚠ A BLOCKED ROW IS RECORDED ⛔ WITH ITS REASON AND ITS BLOCKING ISSUE ID. IT IS NEVER DROPPED.

**A scoreboard that lists only what passed is not a scoreboard.** A provider with no funded key, or
one blocked by a known defect, gets a **⛔** in the `Verdict` column, the reason in plain words, and
the id of the issue blocking it. It keeps its row.

**Key availability, measured at authoring time** (names only — no value was read, printed or
stored, per T-194-01-01):

```
OPENAI_API_KEY PRESENT      ANTHROPIC_API_KEY PRESENT    GOOGLE_API_KEY PRESENT
DEEPSEEK_API_KEY PRESENT    ZHIPU_API_KEY PRESENT        MINIMAX_API_KEY PRESENT
MOONSHOT_API_KEY PRESENT    OPENROUTER_API_KEY PRESENT
```

**All eight provider keys are configured**, so no row is ⛔ on *absence* grounds today. ⚠ **A
present key is not a funded key.** RESEARCH assumption **A3** rates this MEDIUM risk with a named
precedent: **Phase 193.2 lost three publishes to an exhausted OpenAI balance.** A row that fails on
billing is ⛔ *"blocked — provider balance exhausted"*, never a silent omission and never a ✅
borrowed from a neighbouring row.

⚠ **A PASS FROM ANOTHER ROW'S EVIDENCE IS NOT A DRIVEN ROW.** Each of the eight must be stopped on
its own run and read from its own DB rows.

---

## The remaining three axes

`CLAUDE.md`'s 4-axis bandwidth requires cross-provider **plus** three more. Each gets at least one
row of its own.

| # | Axis | Scenario | Verdict | Evidence to capture |
|---|---|---|---|---|
| A-1 | **Multi-tool** | Start a workflow whose phase invokes **two tools in one prompt** — `search_documents` **and** `execute_code`. Press Stop **mid-`execute_code`**, while the sandbox container is live. | ⚠ **partially driven — the SUB-AGENT hazard was probed with two `streaming` rows live on one thread; the parent producer id was selected and the run cancelled correctly. NOT reproduced, NOT refuted.** | Run terminal in `workflow_runs` **and** `runs`; the interrupted `workflow_phases` row `='cancelled'`; **no orphaned sandbox session left streaming**; no surface says failed or complete |
| A-2 | **Multi-tool (second shape)** | Same, but Stop **mid-`search_documents`** (a fast tool — the Stop is far more likely to land between tool calls rather than inside one). | ⛔ **blocked — the multi-tool workflow is a DRAFT, not publishable from the library** | Same, plus: whichever of the two arms is taken (`task_cancelled` vs `terminal_noop`) is recorded, since the outcome discriminator's honesty rule (D-11) binds the copy |
| A-3 | **Parallel-thread** | **Thread A** runs a workflow and is streaming. **Thread B** accepts a new prompt at the same time. Stop **A only**. | ❌ **FAIL (blocker) — B was correctly untouched, but A WAS NEVER STOPPED: no `DELETE` was issued and A ran to `completed`. → UAT-01** | A terminal; **B unaffected and still streaming**; ⚠ **B's composer was NEVER locked** at any point; `threads.active_workflow_run_id` cleared for A and untouched for B; `streamingThreads` still contains B |
| A-4 | **Long-message** | A thread with **≥ 50 prior messages** (or a **≥ 5 KB** user prompt) launches a workflow; Stop it mid-phase. | ⛔ **blocked — not driven (time). ⚠ NOW HIGHER value than when authored: UAT-01 proves the bucket scan is the weak link and this row stresses exactly that scan.** | Terminal states as above, **and** that the Stop control resolved a run id at all — `stopThread` scans the chat bucket for an assistant row with `runStatus === "streaming"`, and a long bucket is where that scan is most likely to be slow or to pick the wrong row |

⚠ **A-3 is the row that proves the phase did not break the thing it was not asked to touch.** Deep
runs and unrelated threads are on the deferred list as *"untouched — any change to it is a
regression, not a deliverable."*

---

## The four G-4 lived-experience rows

`CLAUDE.md` **G-4** requires operator-defined *"I'd recognize failure here"* scenarios, defined at
scope time rather than post-hoc, driven at verification. These four come from `194-RESEARCH.md`
§ *UAT scoreboard — SC#10 fires*.

⚠ **Drive with Chrome MCP. `take_screenshot` TIMES OUT on this box — read DOM geometry via
`evaluate_script` instead.** A wire-format assertion and a DB row are *not* a lived-experience
verdict; G-4 exists because structural UAT missed lived defects across the whole 075.x cascade.

| # | Lived-experience row | Verdict | What "I'd recognize failure" looks like |
|---|---|---|---|
| G-1 | ✅ **PASS — driven 2026-08-16** (see *Driven results*). **Stop from the panel, mid-phase.** Watch the phase spine while a run is on phase 2 of 4; press the panel Stop. | ✅ **pass** | ✅ The interrupted phase reads **stopped**, the phases that genuinely finished are **still there and still marked complete**, and the run is terminal. ❌ Failure: the spine blanks; or the interrupted phase reads **failed** or **skipped** (both dishonest — the phase did not fail and was not skipped, it ran and was interrupted, D-04); or a completed phase loses its output |
| G-2 | ✅ **PASS — driven, BUT ONLY BECAUSE THE THREAD HAD BEEN OPENED FIRST; the same control on a never-opened thread is a silent no-op → UAT-01.** **Stop from ANOTHER thread**, via `ActiveRunsTray`, without navigating into the run's own thread. | ✅ pass (qualified) | ✅ The **originating thread's composer unlocks** — go back to it and type. ❌ Failure: the composer stays locked, i.e. the anchor was not cleared and the thread is wedged |
| G-3 | ❌ **FAIL (major) — driven. DB correct; the LIVE screen keeps the zombie approval card and reads "Running" for a `cancelled` run until a reload. → UAT-02.** **Stop a run that is WAITING AT AN APPROVAL.** Let a run reach an `ask_user` / approval checkpoint, leave it pending, then Stop. | ❌ fail | ✅ The pending approval card **resolves and does not strand**; nothing sits forever awaiting an answer that can never come. ❌ Failure: a zombie approval card. *(This is `BUG-260808-02`'s folded half — 194 covers **stopping a run waiting at an approval**, NOT relocating where approval lives.)* |
| G-4 | ✅ **PASS — DRIVEN TWICE. The landmine did not fire: both Stops sent the PRODUCER `runs.run_id`.** ⚠ **THE NEGATIVE ROW — the one that would have caught the id landmine. RELOAD the page mid-run, THEN press Stop.** The reload makes the mount reconcile overwrite `workflowLock.runId` with `wf.active_workflow_run_id` — a **`workflow_runs.id`**, not a producer `runs.run_id`. | ✅ **pass ×2** | ✅ **After** the fix: the run stops. ❌ **Before** the fix: a **SILENT NO-OP** — `DELETE /runs/{id}` 404s on the wrong id type, `cancelRun` **swallows 404 as success** (`api.ts:1265-1267`), the UI reports nothing, and **the run keeps running.** ⚠ **This row must be driven, not reasoned about: a silent success is worse than a visible failure in a phase whose entire requirement is honesty, and it is invisible to every wire-format and DB assertion in this file** |

⚠ **G-4's row is the whole reason a negative row exists at all.** Both ids are bare uuids, so a swap
**typechecks** and then resolves nothing (`WorkspacePanel.tsx:164`). No test that asserts a
successful response can see this; only a person watching a run refuse to stop can.

---

## How every row is driven — the cheapest honest method

**Proven in Phase 185 and re-recommended by RESEARCH.** Drive each row as a **real run** with a
**per-request `model` + `provider`** on:

```
POST /threads/{id}/messages     body: { ..., "model": "<id>", "provider": "<provider>" }
```

then press Stop on the row's assigned mount, then read the verdict from the database:

| Table | What to read |
|---|---|
| `workflow_runs` | `status` — must be `cancelled`, never left `active` |
| `threads` | `active_workflow_run_id` — must be `NULL` after the stop |
| `workflow_phases` | the interrupted row `= 'cancelled'`; **completed rows UNTOUCHED** (D-07) |
| `runs` | the producer row `status = 'cancelled'` |
| `harness_audit` | the recorded outcome verb — ⚠ a `"zombie_healed"` outcome renders **"recovered a stuck run"**, **NEVER "killed"** (064-B / D-11) |

### ⚠ WHY PER-REQUEST AND NOT A SETTINGS CHANGE — this is a mitigation, not a convenience

Threat **T-194-01-02** (Tampering): switching the global model/provider in Settings between rows
would (a) mutate the operator's own environment and (b) let rows contaminate each other, so a later
row could inherit an earlier row's configuration and a failure could not be attributed. **A
per-request override mutates NO global setting**, so the operator's environment is untouched and
each row is independently attributable.

---

## Sign-off

| Requirement | Satisfied by |
|---|---|
| Cross-provider — **8 rows**, full native roster + OpenRouter | the scoreboard above, roster **derived** by executing the command |
| Multi-tool ≥ 1 row | **A-1**, **A-2** |
| Parallel-thread ≥ 1 row | **A-3** |
| Long-message ≥ 1 row | **A-4** |
| G-4 lived-experience, operator-defined at scope time | **G-1 – G-4**, including the negative row |
| Blocked rows recorded ⛔, never omitted | stated and bound above |
| No global setting mutated | the per-request driving method |
| Rows are not `PLAN.md` tasks | stated at the top of this file |

**Total rows: 15 (8 provider + 4 axis + 4 G-4, with G-1 double-counted against no axis).**
~~**Driven: 0. Every verdict reads ⬜ pending — this file is authored in Wave 0 and driven at phase
verification.**~~ ⚠ **SUPERSEDED 2026-08-16 at HEAD `045a83dc`, kept visible rather than deleted:
DRIVEN 7 — 4 pass, 3 fail — and 8 ⛔ blocked. See § DRIVEN RESULTS below.**

⚠ **Closing a phase with owed manual UAT rows is legitimate, but it must be stated as a DECISION —
never as a claim that everything ran.** If Phase 194 closes with rows still ⬜, the owed rows go in
the ROADMAP progress row and `STATE.md`, naming which row to run first.

---
---

# DRIVEN RESULTS — 2026-08-16, HEAD `045a83dc`

Driven with Chrome MCP against the live local stack (frontend `:5173`, backend `:8000`, Supabase
`:54322`). Every verdict below is backed by a DB read of `workflow_runs` / `workflow_phases` /
`threads.active_workflow_run_id` / `runs`, plus the actual `DELETE /runs/{id}` status read off the
`Response` object via an in-page `fetch` interceptor.

## ⚠ THE ONE CAVEAT THAT BINDS EVERY ROW BELOW: THIS WAS A SINGLE-WORKER BACKEND

Measured, not assumed — `Get-CimInstance Win32_Process` at drive time:

```
36440 :: python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
33192 :: multiprocessing spawn_main(parent_pid=36440)     <- the ONE app process
```

`--reload` runs **one** app worker. **`WORKER_COUNT=2` — the shipped default in
`backend/Dockerfile:43`, `backend/.env.example`, `deploy/onebox.env.example` and
`scripts/restart-backend.ps1:94` — was NOT the configuration under test.**

⇒ **L-01 / CR-04 (the cross-worker `completed`-overwrites-`cancelled` race) COULD NOT FIRE HERE and
is neither confirmed nor refuted by any row below.** `VERIFICATION.md` predicted exactly this: *"a
UAT session would be more likely than not to observe a passing Stop, since a single-worker dev
process … produce[s] the honest path."* **No ✅ in this file may be read as evidence about SC#2 at
the default worker count.** The rows that DID fail below fail for reasons independent of L-01.

## Summary

| Verdict | Rows |
|---|---|
| ✅ pass | 4 — G-1, G-4 (×2 runs), A-1(partial: the sub-agent probe), plus A-3's "thread B unaffected" half |
| ❌ fail | 3 — **A-3**, **G-3**, **WR-03 row** |
| ⛔ blocked | 8 provider rows + A-2 + A-4 |

**7 live workflow runs were started and stopped for this session.** No global setting was mutated;
no auth token was minted or forged; all driving went through the shipped UI.

---

## ❌ THE HEADLINE FINDING — **UAT-01: the cross-thread Stop is a silent no-op, and the run then reports `completed`**

**Severity: blocker.** This is a SECOND "silently complete" path, it is **client-side**, it fired on a
**single-worker** backend, and **it is not L-01.**

### What was driven

1. Started the 5-phase *Quarterly Business Review — Northwind Logistics* workflow from the Workflows
   page → run `833e8e85-92a2-451b-9af7-7cd085355ff7`, thread `1189a1a3-b836-4e7a-8aca-a021f1175e4a`.
   **Its chat thread was never opened.**
2. Switched to a different thread, sent a normal prompt there (the A-3 parallel-thread condition).
3. Opened `ActiveRunsTray` and clicked **"Stop run on Quarterly Business Review — Northwind Logistics"**.

### What happened

| Instrument | Reading |
|---|---|
| `fetch` interceptor | **NO `DELETE /runs/{id}` was ever issued.** |
| Screen | **Nothing.** No error, no toast, no state change. |
| Console | `Stop did nothing: no run id yet for thread 1189a1a3-… — the run had not finished registering (the pre-stamp window). Nothing was cancelled; press Stop again in a moment.` |
| DB, afterwards | `workflow_runs 833e8e85 = **completed**` · all five phases `completed` · producer run `completed` |

**The user pressed Stop; the run ran to completion and reports `completed`.** That is the outcome
SC#2 forbids in the exact word it forbids it in.

### The mechanism — read out of source, not inferred

`StreamsProvider.tsx:2411-2418`:

```js
stopThread: async (threadId: string) => {
  const bucket = useStreamsStore.getState().bucketsBySurface.get("chat")?.get(threadId) ?? []
  const streamingMsg = [...bucket].reverse()
    .find((m) => m.role === "assistant" && m.runStatus === "streaming")
  const runId = streamingMsg?.runId
  if (!runId) { console.warn("Stop did nothing: …pre-stamp window…"); return }
```

`stopThread` resolves the run id **exclusively from the client's per-thread chat MESSAGE BUCKET**.
That bucket is hydrated only when the thread's messages are fetched — i.e. **when you open the
thread**. The tray's run list comes from a *different* store slice (`streamingThreads`, derived from
`snapshot.active_runs` at `StreamsProvider.tsx:1587`).

⇒ **Two slices, two population events. The tray can LIST a run it structurally CANNOT STOP.**

### ⚠ Three consequences, each stated separately because they are separate claims

1. **`stopThread`'s own docblock is falsified by this row.** `ActiveRunsTray.tsx:12-17` and
   `StreamsProvider.tsx:2406-2409` both promise the tray "can cancel a backgrounded run **without
   navigating into it**". Measured: it cannot, unless the bucket happens to already be hydrated.
2. **`VERIFICATION.md`'s SC#1 reasoning is inverted by this row.** It reads: *"Does SC#1 silently
   depend on mount 4? No — confirmed, not accepted. The property mount 4 was reached for ('stoppable
   from outside its thread without navigating in') is delivered by mount 3."* **Mount 3 does not
   deliver that property.** The descope of mount 4 rests on a premise this row measures as false.
   (This does not make the descope *wrong* — it makes its stated justification wrong.)
3. **The warn's TEXT is itself dishonest**, on a phase whose subject is honesty. It asserts *"the run
   had not finished registering (the pre-stamp window)"* and advises *"press Stop again in a
   moment."* Measured: the run had been registered for ~90 seconds and was on **phase 4 of 5**. The
   advice will never work — re-pressing re-runs the same failing bucket scan. The one diagnostic
   channel that exists states a false cause and gives useless advice.

### ⚠ And this is the RE-OPEN TRIGGER the code itself asked for

`StreamsProvider.tsx:2443-2452` records a rejected alternative and its trigger verbatim:

> RE-OPEN TRIGGER: **a second sighting of a Stop lost in the pre-stamp window (a UAT row**, a bug
> report, or this warn appearing in a real log capture). At that point disabling the control — or
> queueing the intent until the stamp lands — becomes the proportionate fix and this comment is its
> starting point.

**That trigger is now met by a UAT row, exactly as written** — with the correction that the sighting
is **not** the pre-stamp window at all. The pre-stamp window is a one-RTT transient; **this is a
permanent condition for every thread the user has not opened in the current page session.** The fix
the comment proposes (disable / queue the intent) does not address this cause. The cause is the
bucket-scan resolution strategy.

### The contrast case that isolates it — same control, same action, opposite outcome

| | thread opened this session? | tray Stop result |
|---|---|---|
| **G-2** (below) | **yes** — I had opened it, then navigated away | `DELETE` issued → **204** → run cancelled ✅ |
| **UAT-01** | **no** — started from Workflows page, never opened | **no DELETE** → silent no-op → run `completed` ❌ |

### ⚠ A SECOND, STRICTLY WORSE VARIANT — after a page reload there is NO Stop affordance at all

Driven separately with run `607a325d-6fd7-48aa-b74c-ce0429318b92` (thread
`645b0d61-…`, never opened): started the run, **reloaded the page**, opened a different thread, and
looked for the tray.

- **The `● N running` tray button did not render.** Zero entries.
- Confirmed live at that exact moment by DB: `workflow_runs = active`, producer run `= streaming`,
  `threads.active_workflow_run_id` set.

Because `streamingThreads` is populated per-thread from `snapshot.active_runs`, a fresh page load
knows about **no** runs until a thread is opened. **From a cold load, a user has no cross-thread Stop
for a live workflow run — the affordance is absent, not merely broken.** Opening that thread and
pressing the panel Stop then worked (204, cancelled correctly).

---

## ❌ **UAT-02 (G-3) — Stop at an approval: the DATABASE is honest, the LIVE SCREEN is not**

Driven on *Slack Connector UAT (Phase 190)*, whose `act` phase carries `action_risk_armed: true`.
Run `4a8b7528-3d49-455b-8724-793cf4515574`, thread `92d61412-…`. Let it park at the armed
checkpoint (the card reading **"Approve this step / Do not run it / OR / Type an answer"**), then
pressed the panel Stop.

| Half | Verdict | Evidence |
|---|---|---|
| `DELETE /runs/{id}` | ✅ | **204** on producer `c3d16b2c-…` |
| DB | ✅ **fully correct** | `workflow_runs = cancelled` · `act = cancelled` · `retrieve = completed` **untouched** · `emit` still `pending` (never falsely terminalized) · anchor `NULL` · producer `cancelled` |
| **Live screen, +5s** | ❌ | approval card **still rendered**; spine still `act ● Running`; badge still `Working` |
| **Live screen, +13s** | ❌ | **unchanged.** `Approve this step` still present. The word **"Stopped" appears NOWHERE on the surface** (`stoppedWords: []`) while the run is `cancelled` in Postgres |
| **After a reload** | ✅ | `act ■ Stopped` · `Phase 2 of 3, act, stopped` · `retrieve ✓ Complete` · `emit ○ Locked` · approval card **gone** |

**So this is a LIVE-UPDATE failure, not a persisted lie — and the distinction is stated rather than
smoothed, because it changes the severity.** The stored truth is right and a reload shows it. But
G-3's own ❌ condition is *"a zombie approval card"*, and for as long as the user stays on the page
that is exactly what is on screen: a run that Postgres says is cancelled, rendered as **Running**,
offering an approval that can never be honoured. Severity: **major** (not blocker — no wrong state
persists).

---

## ❌ **UAT-03 (WR-03) — the finished-run Stop, driven: it is worse than "does nothing silently"**

`VERIFICATION.md` A-2 / WR-03 records that a finished run still shows "This run — Stop" and that
pressing it "emits a console.warn and nothing reaches the screen". Driven on the completed G-2 run:

| Instrument | Reading |
|---|---|
| Network | **no request at all** |
| Screen | `document.body.innerText` **byte-identical** before and after (`diffLen: 0`) |
| Console | `Stop did nothing: no run id yet for thread ae2f654b-… — the run had not finished registering (the pre-stamp window). Nothing was cancelled; press Stop again in a moment.` |

**The recorded description is confirmed AND extended: the warn's TEXT is false for this case too.**
The run is finished, not "not yet registering", and "press Stop again in a moment" is advice that can
never succeed. One warn string is currently serving three different conditions — genuine pre-stamp,
unopened-thread (UAT-01), and already-finished — and it is **wrong for two of the three**. It is the
same class of false statement the phase exists to remove, in the only channel that reports at all.

---

## ✅ The rows that PASSED, with their evidence

### ✅ G-1 — Stop from the panel, mid-phase

Run `271a510c-…`, stopped while phase 2 of 5 (`gather-support`) was `active`.

- `DELETE /runs/1e31376c-…` → **204** (the **producer** id, correctly resolved)
- `workflow_runs = cancelled`
- `gather-usage = completed` **untouched** · `gather-support = cancelled` · `gather-commercial`,
  `synthesize`, `emit-qbr` **still `pending`** — never falsely marked `failed` or `skipped` (D-04)
- `threads.active_workflow_run_id = NULL` · producer `cancelled` · the three sub-agent runs
  `completed`, untouched

### ✅ G-4 — the negative row: RELOAD mid-run, THEN Stop — **driven twice, passed twice**

The row that exists to catch the two-id landmine. Both times, after a full `location.reload()`
mid-run and re-entering the thread:

| Run | id sent to `DELETE` | Which id is that? | Status | Result |
|---|---|---|---|---|
| `472ff318-…` (phase 5/5 active) | `8d2d6c10-…` | **producer `runs.run_id`** | see note | `cancelled` · `emit-qbr = cancelled` · 4 completed phases untouched · anchor `NULL` |
| `ee717838-…` (phase 3/5 active) | `a032259d-…` | **producer `runs.run_id`** | **204** | `cancelled` · `gather-commercial = cancelled` · 2 completed untouched · `synthesize`/`emit-qbr` still `pending` · anchor `NULL` |

**The landmine did not fire.** In neither case was a `workflow_runs.id` or a sub-agent id sent. The
mount-reconcile stamp on `workflowLock.runId` is genuinely bypassed by the panel Stop delegating to
`stopThread(threadId)`, as `194-03` claimed.

⚠ **Note on run 1's status — an unresolved single observation, recorded rather than dropped.** The
Chrome network log reported that first `DELETE` as **503**, not 204. It has **not been reproduced**:
the two subsequent post-reload Stops both read **204**, and *four* further Stops in this session all
read 204. No code path explains it — `cancel_run` (`api/runs.py:1181-1430`) can only return
204/404/500; `MaintenanceMiddleware` explicitly **allowlists** `DELETE /runs/` (`maintenance.py:74-76`)
and maintenance was `false`; `SetupMiddleware` is a no-op on a configured box. ⚠ **The instruments
differ and that asymmetry is the most likely explanation:** the 503 came from the *extension's*
network log, while every 204 was read off the actual `Response` object. **Recorded as UNCONFIRMED —
possible instrument artifact — not as a defect.** It matters only because `api.ts::cancelRun` swallows
**404 only** and *throws* on 503, so if it is real, a Stop that worked would report an error. Re-open
trigger: a second sighting of a non-204/404 on this route.

### ✅ G-2 — Stop from ANOTHER thread via `ActiveRunsTray`; the originating composer unlocks

Run `110992b0-…`, thread `ae2f654b-…`. **The thread WAS opened before navigating away** — see
UAT-01 for why that turns out to be load-bearing.

- Before: thread A's composer **locked** — `disabled: true`, placeholder **"Workflow running — Cancel
  to switch back"**
- Stopped from a *different* thread's tray → `DELETE /runs/1c8a1f3b-…` → **204**
- DB: `workflow_runs = cancelled` · `gather-commercial = cancelled` · two completed phases untouched
  · anchor `NULL` · producer `cancelled`
- **Back in thread A: composer UNLOCKED** — `disabled: false`, placeholder **"Ask anything…"**, and it
  **accepted typed text** (asserted on `value`, not on appearance). **The thread is not wedged.**
- ⚠ Minor: the tray still read **"1 running"** ~3.5 s after the 204; it cleared by ~10 s. A lag, not
  a wedge — recorded, not filed.

### ✅ A-1 (the sub-agent hazard) — probed WITH the precondition present; it did not fire

`VERIFICATION.md`'s Anti-Pattern A-1 warns that the snapshot's `active_runs` SELECT filters
`status='streaming'` with **no `parent_run_id IS NULL`**, so a **sub-agent** id could reach
`DELETE /runs/{id}`. Whether it fires "depends on placeholder ordering, which only a live run
exercises."

**The precondition was captured live** on run `607a325d-…` — DB at that instant showed **two
`streaming` rows on one thread**: producer `0a26c472-…` (`parent_run_id NULL`) **and** sub-agent
`c0520e0d-…` (`parent_run_id = 0a26c472-…`). Opened the thread and pressed the panel Stop.

- `DELETE /runs/0a26c472-…` → **204** — **the PARENT producer id, not the sub-agent's**
- `workflow_runs = cancelled` · `gather-commercial = cancelled` · two completed untouched · anchor
  `NULL` · all sub-agent rows ended `completed`, none cancelled

⚠ **This is ONE observation with the precondition genuinely present, not a proof.** A-1 is **not
reproduced** and is **not refuted**; the reverse bucket scan picked the parent here. Recorded as
evidence, not as closure.

### ✅ A-3, the half that passes — thread B was unaffected

While thread A's workflow ran, thread B's composer was **never locked** (placeholder "Ask anything…",
`disabled: false`), accepted a prompt, streamed, and **completed normally** (`runs` row `completed`,
`parent_run_id NULL`) after thread A's Stop was pressed. **Stopping A did not touch B.** The
"untouched surfaces stay untouched" property holds. A-3's *other* half is UAT-01 and it fails.

---

## ⛔ BLOCKED ROWS — recorded with a reason, never dropped

**A scoreboard that lists only what passed is not a scoreboard.**

### ⛔ All 8 cross-provider rows — blocked, structural

| Provider | Model id | Verdict | Reason |
|---|---|---|---|
| anthropic | `claude-sonnet-5` | ⛔ | ↓ |
| openai | `gpt-5.6-sol` | ⛔ | ↓ |
| google | `gemini-3.5-flash` | ⛔ | ↓ |
| deepseek | `deepseek-v4-pro` | ⛔ | ↓ |
| zhipu | `glm-5.2` | ⛔ | ↓ |
| minimax | `MiniMax-M3` | ⛔ | ↓ |
| moonshot | `kimi-k2.6` | ⛔ | ↓ |
| openrouter | `deepseek/deepseek-v4-pro` | ⛔ | ↓ |

**Reason (one cause, all eight): there is no per-request model/provider override on the workflow-run
path, and the two available workarounds are both forbidden by this file's own rules.**

- The rows require *"a real, live **workflow** (harness) run"*. A workflow run's model comes from the
  **definition** — measured: every run driven above recorded `workflow_runs.model = deepseek-v4-flash`
  without being asked. `RunModal` exposes only **scope / kickoff / destination** (testids
  `run-scope-select`, `run-kickoff`, `run-destination`, `run-confirm`) — **no model picker.**
- The prescribed method (`POST /threads/{id}/messages` with per-request `model` + `provider`) is a
  **Deep-chat** dispatch, and driving it directly needs a live session token. The one in
  `localStorage` is **expired** (measured: `exp 2026-02-08`, six months stale). **No token was minted
  or forged** — the same line `194-13` drew.
- **Editing the 8 definitions' `model` field would mutate the operator's own workflow data**, and
  **changing the Settings default model is forbidden outright** by threat **T-194-01-02** (global
  mutation + cross-row contamination), which is the very reason this file mandates per-request.

⚠ **This is a gap in the driving METHOD this file prescribes, not merely an untested row** — the file
specifies a per-request override for a surface that has none. **Blocking issue: `UAT-04` (below).**

### ⛔ A-2 — multi-tool, Stop mid-`search_documents`

Not driven. The published *"Multi-tool scope (098 UAT — search + execute_code)"* workflow is a
**DRAFT**, not published, so it cannot be launched from the library. Landing a Stop *inside* a
sub-second tool call also needs timing control the UI does not offer. **A-1's sibling observation
above partially covers the multi-tool axis** (three sub-agent runs per QBR run, one `streaming` at
Stop time) but it is **not** the A-2 row and is not claimed as one.

### ⛔ A-4 — long-message (≥ 50 prior messages or ≥ 5 KB prompt)

Not driven — time. ⚠ **This row is now MORE valuable than when it was authored, not less:** UAT-01
proves `stopThread`'s bucket scan is the weak link, and A-4 is the row that stresses *that exact
scan* over a long bucket. **Run A-4 immediately after any fix to UAT-01.**

---

## What this session changes about the phase's own record

| Claim on record | Status after driving |
|---|---|
| `VERIFICATION.md`: SC#1 ✓, mount-4 descope is safe because mount 3 delivers "stoppable from outside its thread without navigating in" | ⚠ **premise measured FALSE** (UAT-01). The conclusion may still hold; the stated reason does not. |
| `VERIFICATION.md`: SC#2 ✗ FAILED, cause = L-01 cross-worker overwrite | ⚠ **still ✗, and now for a SECOND independent reason.** UAT-01 produces `completed` after a Stop **client-side, on ONE worker**. Fixing L-01 would not fix UAT-01. |
| `VERIFICATION.md`: SC#3 ✓ safe mid-phase, completed phases untouched | ✅ **held on every one of the 7 live runs.** No completed phase was ever touched; no pending phase was falsely terminalized; the interrupted phase read `cancelled` every time, never `failed`/`skipped`. |
| `194-03`: the panel Stop resolves via `stopThread(threadId)`, never `workflowLock.runId` | ✅ **confirmed live**, including twice after a reload (G-4). |
| A-1 (sub-agent id can reach the cancel route) | ⚠ **probed with the precondition present; did not fire.** Not refuted. |
| WR-03 (finished-run Stop does nothing, silently) | ❌ **confirmed and extended** — the warn's text is false for this case (UAT-03). |
| L-01 / CR-04 at `WORKER_COUNT=2` | ⛔ **NOT PROBED — single-worker backend.** Nothing here speaks to it in either direction. |

---

## Gaps (structured — for whoever routes this next)

⚠ **G-7 was run mechanically, not eyeballed:** `node scripts/check-gap-closure-rounds.cjs 194` →
`plans: 13 total · 0 gap-closure` · **`G-7 clear`, exit 0**. So G-7 does **not** block a round on
the count. **A round is nevertheless the wrong route, and the reason is the DATING below, not the
count** — per the G-7 protocol's own step 2.

### Dating the offending code (`git log -S`, not read off a comment)

| Finding | Offending code | First introduced | Whose debt |
|---|---|---|---|
| **UAT-01** | `stopThread`'s bucket-scan run-id resolution | `8ccc31f8 feat(SEED-064): cross-thread run visibility + Stop` | ⚠ **INHERITED — predates Phase 194 entirely** |
| **UAT-03** | the warn TEXT asserting "the pre-stamp window" | `241d9cd5 fix(194-08): make the pre-stamp silent no-op Stop OBSERVABLE` | **Phase 194's own** |
| **UAT-02** | live SSE/reconcile not clearing the approval card on cancel | not in 194's diff | inherited |

```yaml
- id: UAT-01
  truth: "A user can stop a running workflow at any point."
  status: failed
  severity: blocker
  test: A-3 / G-2-negative
  reason: >-
    stopThread() resolves the run id ONLY from the client's per-thread chat message bucket
    (StreamsProvider.tsx:2411-2418). That bucket is hydrated only when the thread is OPENED.
    For a run on a thread not opened in the current page session the scan finds nothing, no
    DELETE is issued, nothing reaches the screen, and the run continues to `completed`.
    Driven: workflow_run 833e8e85 pressed-Stop -> ran to completion, all 5 phases `completed`.
    Second, worse variant: after a page RELOAD the ActiveRunsTray does not render at all
    (streamingThreads is per-thread-reconcile-populated), so there is no cross-thread Stop
    affordance whatsoever from a cold load.
  artifacts:
    - path: "frontend/src/providers/StreamsProvider.tsx:2411-2418"
      issue: "run-id resolution reads bucketsBySurface('chat')(threadId) — empty for an unopened thread"
    - path: "frontend/src/components/chat/ActiveRunsTray.tsx:41-44"
      issue: "run LIST comes from streamingThreads (a different slice) — the tray can list a run it cannot stop"
    - path: "frontend/src/providers/StreamsProvider.tsx:2406-2409"
      issue: "docblock promises cancel 'without navigating into it' — measured false"
  missing:
    - "A run-id resolution that does not depend on bucket hydration (e.g. carry the run id on the streamingThreads entry / snapshot.active_runs, which the tray ALREADY reads to decide the run exists)."
    - "OR: hydrate the bucket / fetch the snapshot for the target thread inside stopThread before the scan."
  routing: >-
    NOT a gap-closure round. The defective resolution strategy is INHERITED from SEED-064 and
    changing it is a new mechanism, which G-7 forbids inside a closure round. Route to a phase
    or a seed. NOTE this also invalidates the STATED justification for descoping Stop mount 4
    (VERIFICATION.md SC#1), which rests on mount 3 delivering exactly the property that fails here.

- id: UAT-02
  truth: "Stopping a run waiting at an approval resolves the pending approval; nothing strands."
  status: failed
  severity: major
  test: G-3
  reason: >-
    DB is fully correct (workflow_runs cancelled, act cancelled, retrieve completed untouched,
    emit still pending, anchor NULL, producer cancelled) and a RELOAD renders it correctly
    ("act ■ Stopped"). But the LIVE surface keeps the actionable "Approve this step / Do not run
    it" card and keeps reading "act ● Running" / "Working" indefinitely; the word "Stopped"
    appears NOWHERE on the surface while the run is cancelled in Postgres.
    Live-update failure, not a persisted lie — stated as such because it changes severity.
  missing:
    - "The cancel terminal must clear the pending ask_user/approval card and flip the live phase reading, not only the reloaded one."

- id: UAT-03
  truth: "The one channel that reports a failed Stop tells the truth about why."
  status: failed
  severity: minor
  test: WR-03 / G-4-adjacent
  reason: >-
    One warn string serves three different conditions — genuine pre-stamp, unopened-thread
    (UAT-01) and already-finished-run — and it is WRONG for two of the three. It asserts
    "the run had not finished registering (the pre-stamp window)" and advises "press Stop again
    in a moment", advice that can never succeed in either wrong case. Driven on a finished run:
    no request, zero on-screen change (innerText byte-identical), only this false warn.
  artifacts:
    - path: "frontend/src/providers/StreamsProvider.tsx:2453-2459"
      issue: "the warn asserts a cause it has not established"
  routing: >-
    G-3 fast-fix (1 file, a few lines, no schema/API surface) — and it IS this phase's own code
    (241d9cd5, plan 194-08), which is the one finding here that a fast-fix legitimately covers.
    Fix = state the OBSERVED fact ("no streaming run id is loaded for thread X in this session")
    rather than an unestablished cause, and drop the "press Stop again" advice.

- id: UAT-04
  truth: "The 8-row cross-provider scoreboard can be driven at all."
  status: blocked
  severity: major
  test: the 8 provider rows
  reason: >-
    The rows require a live WORKFLOW run per provider, but a workflow run takes its model from
    the DEFINITION and RunModal exposes no model picker (scope / kickoff / destination only).
    The per-request method this file prescribes (POST /threads/{id}/messages with model+provider)
    is a Deep-chat dispatch and needs a live session token; the localStorage one is 6 months
    expired and no token was minted or forged. The two workarounds are both forbidden here:
    editing 8 definitions mutates operator data, and changing the Settings default model is
    forbidden outright by T-194-01-02.
  missing:
    - "Either a per-request model/provider override on the workflow-run launch path, or a documented operator-run recipe for the 8 rows that does not mutate global settings."
