---
phase: 194-stop-a-running-workflow
plan: 13
type: execute
wave: 8
depends_on: ["194-12"]
files_modified:
  - .planning/phases/194-stop-a-running-workflow/194-HEAL-RECEIPT.md
  - CLAUDE.md
autonomous: false
requirements: [RUN-01]
validation_rows: [V-20]
must_haves:
  truths:
    - "No workflow_runs row still reads active while lying about being live, and no workflow_phases row still reads active under a terminal run."
    - "Every healed row has a recorded before and after — a data repair with no receipt is indistinguishable from a claim."
    - "The two run rows were healed BY RUNNING THE PATH THIS PHASE BUILT, so the receipt evidences SC#2 rather than an operator's SQL."
    - "Every hot file this phase touched is a ROW in the CLAUDE.md ledger with figures re-derived at the phase's final commit."
  artifacts:
    - path: ".planning/phases/194-stop-a-running-workflow/194-HEAL-RECEIPT.md"
      provides: "Row ids, thread_id, created_at and status BEFORE and AFTER for all four rows, plus the method used for each"
      contains: "is_golden_run"
    - path: "CLAUDE.md"
      provides: "The hot-file ledger cells re-derived at the phase's final commit, corrections recorded beside"
      contains: "It inherits"
  key_links:
    - from: "DELETE /runs/{workflow_run_id}"
      to: "the two historically stuck workflow_runs rows"
      via: "the dual-id fallback → no live producer → the exported composition"
      pattern: "cancelled"
---

<objective>
D-12 and D-17: heal the four rows that are SC#2's and SC#3's failure modes **already sitting in the
live data**, and close the phase's ledger debt.

Purpose, stated honestly. RESEARCH measured **two `workflow_runs` rows stuck `active` since
2026-06-14 and 2026-08-01**, and — beyond D-12 — **two `workflow_phases` rows stuck `active` under
runs that have already `failed`** (both `confirm` phases, stamped 2026-07-18). Leaving a known
orphan `active` phase row unhealed while shipping the fix that prevents new ones is precisely the
inconsistency this phase exists to remove.

⚠ **`BUG-260815-07`'s "permanent delete blocker" justification was measured FALSE and is NOT the
reason this ships.** `delete_workflow_cascade` already cancel-firsts **and** calls
`finish_run(wf_id, "cancelled")` unconditionally for every in-flight row, and both definitions are
`is_system_global = False` with zero foreign runs, so the 409 guard cannot fire either. **The honest
reasons stand and are the ones to record:** two rows lie about being live, two threads still hold
anchors, two phase rows present partial writes as still running, and one run is an abandoned golden
run that nothing will ever pick up again.

⚠ **SERIALIZED — this plan runs ALONE in its wave.** It mutates four real, irreplaceable rows.

Output: a committed four-row receipt, and every hot file this phase touched carrying a re-derived
ledger cell.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/194-stop-a-running-workflow/194-RESEARCH.md
@.planning/phases/194-stop-a-running-workflow/194-CONTEXT.md
@.planning/phases/194-stop-a-running-workflow/194-BASELINE.md
@.planning/phases/194-stop-a-running-workflow/194-MIGRATION-RECEIPT.md
@CLAUDE.md

**Base commit — ASSERT FIRST.** In a worktree, run `bash scripts/bootstrap-worktree.sh "$(pwd)"`
FIRST. Tear down only with `bash scripts/teardown-worktree.sh <path>`; never `rm -rf`.

<interfaces>
The four rows, as measured on **2026-08-16**. ⚠ **RE-DERIVE ALL OF THEM before touching anything** —
these ids and timestamps are from a prior commit and this tree moves fast.

Two `workflow_runs` rows stuck `active`:
| # | `workflow_runs.id` | `thread_id` | created_at | `is_golden_run` | anchor points here? |
|---|---|---|---|---|---|
| 1 | `fde3bbe3-02c2-4664-902f-921411bb1fe7` | `4bf89cbc-5630-4fb3-982e-5fd179450003` | 2026-06-14 21:15:56Z | `False` | ✅ yes |
| 2 | `4b0feda7-c524-4a18-8ea0-4c6fc9705868` | `e52a19f7-52cc-45ba-bedf-03247a8c4133` | 2026-08-01 17:04:34Z | ⚠ **`True`** | ✅ yes |

- **#1's producer `runs` row is ALREADY `cancelled`** (`652ec9bb-…`, completed 84 ms after it
  started). ⇒ **This row is the historical proof of the G-B gap in its purest form: the `runs` half
  was cancelled and the `workflow_runs` half was never told.**
- **#2 is a GOLDEN RUN** — a publish-validation artifact, not a user run. Since Phase 190 (A4),
  `find_resumable_runs` excludes `is_golden_run = true` outright, so nothing will ever pick it up.
  Its thread carries five `runs` rows, all `failed` with `error='resume re-drive failed'`.

Two orphan `workflow_phases` rows stuck `active` under **terminally `failed`** runs:
| phase id | run | run status | slug | updated_at |
|---|---|---|---|---|
| `961c290d-8ec7-46c2-b6d9-58923f8612cc` | `5aa42b6b-…` | `failed` | `confirm` | 2026-07-18 20:43:42Z |
| `9e6acf54-8c10-46d8-8351-5d31dc3946af` | `e0d1f740-…` | `failed` | `confirm` | 2026-07-18 20:43:42Z |

⚠ **Zero `runs` rows were `streaming` across the whole DB at measurement time**, which is why the
new `DELETE` fallback resolves `producer_id IS NULL` for both run rows and takes the
no-live-producer arm.
</interfaces>
</context>

<non_goals>
- ⛔ **Do NOT re-anchor, resume or re-drive any healed run.** ⚠ Phase 190's A4 gate exists so a
  resumed golden run cannot perform an external action; **cancel it, never re-anchor it.**
- ⛔ **Do NOT introduce a new status literal** to distinguish a healed golden run. If it needs
  distinguishing, that is an `error` / audit note — a second migration for a one-off is not
  acceptable (D-17).
- ⛔ **Do NOT claim `BUG-260815-07`'s delete-blocker justification.** It was measured FALSE; use the
  honest reasons.
- ⛔ **Do NOT close `BUG-260815-07`'s non-reproducible delete-failure half.**
- ⛔ **Do NOT touch any row not in the four listed** — no bulk UPDATE, no `WHERE status='active'`
  sweep across the table.
- ⛔ **Do NOT apply anything to the CLOUD database.**
- ⛔ Do not add a ledger row for a file this phase did not touch.
</non_goals>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Re-derive the four rows and capture the BEFORE receipt (V-20, first half)</name>
  <files>.planning/phases/194-stop-a-running-workflow/194-HEAL-RECEIPT.md</files>
  <read_first>
    - `.planning/phases/194-stop-a-running-workflow/194-RESEARCH.md` § *Runtime State Inventory* — the two stuck-row table, the 2-orphan-phase-row table and the supporting measurements
    - `.planning/phases/194-stop-a-running-workflow/194-CONTEXT.md` D-12 and D-17
    - `.planning/phases/194-stop-a-running-workflow/194-MIGRATION-RECEIPT.md` — the applied constraint, so a `cancelled` phase write is now legal
  </read_first>
  <what-built>
Nothing yet. This task establishes the **before** column, because *a data repair with no receipt is
indistinguishable from a claim* (D-12) — and because RESEARCH's ids were measured at a prior commit
and may have moved.
  </what-built>
  <how-to-verify>
Run against the local DB (`postgresql://postgres:postgres@127.0.0.1:54322/postgres`) and paste every
output verbatim:
1. `SELECT id, thread_id, status, created_at, updated_at, is_golden_run FROM workflow_runs WHERE status IN ('active','paused','cap_paused') ORDER BY created_at;`
2. `SELECT wp.id, wp.workflow_run_id, wp.slug, wp.phase_index, wp.status, wp.updated_at, wr.status AS run_status FROM workflow_phases wp JOIN workflow_runs wr ON wr.id = wp.workflow_run_id WHERE wp.status = 'active' ORDER BY wp.updated_at;`
3. `SELECT id, active_workflow_run_id FROM threads WHERE active_workflow_run_id IS NOT NULL;`
4. `SELECT status, count(*) FROM runs GROUP BY status ORDER BY status;` — confirm **zero**
   `streaming` rows, which is what makes the `producer_id IS NULL` arm the one that will run.
5. `SELECT status, count(*) FROM workflow_runs GROUP BY status; SELECT status, count(*) FROM workflow_phases GROUP BY status;` — the histograms, so the after column can prove exactly four rows moved.

⚠ **If the live set differs from RESEARCH's table** — a row healed itself, a new one appeared, an id
changed — **say so and stop.** Heal what is actually there, recorded as it actually is; do not force
the measured data to match a document.
  </how-to-verify>
  <action>
Do not write to the database in this task. Present the five queries to the operator, then record
every pasted output verbatim in `194-HEAL-RECEIPT.md` under a `## BEFORE` section, dated, with the
local DSN. Build the per-row table D-12 requires — id, `thread_id`, `created_at`, `updated_at`,
status, and `is_golden_run` for the run rows. Diff the live set against RESEARCH's table and record
any divergence BESIDE RESEARCH's figures with the measurement date. If the live set is empty (the
rows healed themselves), record that as the measurement and STOP — there is nothing to repair, and a
receipt for a repair that did not happen is the exact false claim D-12 exists to prevent.
  </action>
  <acceptance_criteria>
    - `194-HEAL-RECEIPT.md` exists with a BEFORE section containing all five query outputs verbatim.
    - Every row to be healed is listed with its **id, `thread_id`, `created_at`, `updated_at` and
      status** — D-12 requires all of them.
    - Any divergence from RESEARCH's table is recorded **beside** RESEARCH's figures with the
      measurement date, never over them.
    - The `runs` status histogram is captured and the `streaming` count is stated explicitly.
    - The `is_golden_run` flag is captured per run row.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "is_golden_run" .planning/phases/194-stop-a-running-workflow/194-HEAL-RECEIPT.md &amp;&amp; grep -qi "BEFORE" .planning/phases/194-stop-a-running-workflow/194-HEAL-RECEIPT.md</automated>
  </verify>
  <done>Every row that will be repaired is recorded with its id, `thread_id`, `created_at`, `updated_at`, status and `is_golden_run`, re-derived at this commit, with any divergence from RESEARCH noted beside it.</done>
  <resume-signal>Paste all five query outputs, then type "before captured".</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Heal all four rows — two by RUNNING THE NEW PATH, two by a one-off recorded write (V-20, second half)</name>
  <files>.planning/phases/194-stop-a-running-workflow/194-HEAL-RECEIPT.md</files>
  <read_first>
    - `backend/app/api/runs.py` — the dual-id fallback from plan 194-11 and its no-live-producer arm
    - `backend/app/services/run_lifecycle.py` — the exported composition from plan 194-09
    - `backend/app/db/workflows.py` — `finish_run` and `cancel_active_phases`
    - `.planning/phases/194-stop-a-running-workflow/194-RESEARCH.md` § *Recommendation on HOW to heal (D-12's open question)* — the four reasons, in order, and the golden-run caveat
  </read_first>
  <what-built>
The complete cancel path now ships: `DELETE /runs/{id}` accepts a `workflow_runs.id`, owner-scoped
and anchor-confirmed; it resolves forward to a live producer and, finding none, calls the exported
composition (`finish_run` + `cancel_active_phases`); and migration 119 makes the phase write legal.
  </what-built>
  <how-to-verify>
**⚠ THE METHOD IS PART OF THE DELIVERABLE, AND THE TWO GROUPS ARE HEALED DIFFERENTLY ON PURPOSE.
State this in the receipt rather than leaving it inferable.**

**Group A — the two `workflow_runs` rows: heal BY RUNNING THE NEW PATH.** A one-off `UPDATE` would
prove the rows changed; running the path proves **the thing this phase built** changes them, so the
receipt evidences SC#2 rather than an operator's SQL. Both threads still hold their anchors, so the
new fallback resolves each `workflow_runs.id` → owner-scoped → anchor-confirmed → no live `runs` row
→ the exported composition. For each row, as the OWNING user:
`DELETE /runs/{workflow_runs.id}` — expect **204**.
⚠ Row #2 is a **GOLDEN RUN**. Cancelling it is honest (it is abandoned and permanently unresumable
since Phase 190's A4 gate), **but a future reader finding a `cancelled` golden run needs to know it
was healed rather than user-stopped.** Record that explicitly in the receipt, and if a
distinguishing marker is written, it is an `error` / audit note — **never a new status literal.**

**Group B — the two orphan `workflow_phases` rows: a one-off recorded write, and here is WHY it
cannot be the path.** Their parent runs are already terminally `failed`, so `DELETE /runs/{id}`
takes **Step 2 `terminal_noop`** — no writes at all — and the cancel path can never reach them. A
one-off write is therefore the only instrument, and stating that reason is the difference between a
documented exception and an unexplained `UPDATE`. Write each row **individually by id** —
`UPDATE workflow_phases SET status='cancelled', updated_at=now() WHERE id = '<the exact id>'` — two
statements, two ids. ⚠ **No `WHERE status='active'` sweep**: a predicate write here could catch a
genuinely live phase and there is no reason to take that risk for two known rows.

**Then capture the AFTER column** by re-running **all five** queries from Task 1, and paste them.
  </how-to-verify>
  <action>
Do not write to the database yourself. Hand the operator the exact `DELETE /runs/{id}` calls (Group
A) and the two id-scoped `UPDATE` statements (Group B), then record, in `194-HEAL-RECEIPT.md` under
an `## AFTER` section: the two 204 responses, all five re-run query outputs verbatim, and a per-row
BEFORE→AFTER table. Add a `## Method` section stating, per group, which instrument was used and
**why** — Group A by running the path this phase built, so the receipt evidences SC#2 rather than an
operator's SQL; Group B by a one-off id-scoped write, because their parent runs are already
terminally `failed`, so the cancel path takes Step 2 `terminal_noop` and can never reach them. Add a
`## Caveats` section carrying the golden-run note and the measured falsification of
`BUG-260815-07`'s delete-blocker justification, with the honest reasons the heal shipped anyway. If
the histograms show any row moving that is not one of the four, STOP and report.
  </action>
  <acceptance_criteria>
    - Both `DELETE /runs/{id}` calls returned **204**, and the request/response evidence is in the
      receipt.
    - AFTER: `SELECT ... FROM workflow_runs WHERE status IN ('active','paused','cap_paused')` returns
      **zero rows**, and `SELECT ... FROM threads WHERE active_workflow_run_id IS NOT NULL` returns
      **zero rows** — the anchors are cleared by `finish_run`'s own transaction.
    - AFTER: the `workflow_phases` `status='active'` query returns **zero rows**.
    - The histograms show **exactly four** rows moved — two `workflow_runs` and two
      `workflow_phases` — and no other row changed status.
    - The receipt states, per group, **which method was used and why**, including the measured reason
      Group B could not use the path (`terminal_noop`).
    - The golden-run caveat is recorded for row #2, with the marker used (if any) named as an
      `error`/audit note and **not** a status literal.
    - The receipt states plainly that `BUG-260815-07`'s delete-blocker justification was measured
      FALSE, and gives the honest reasons the heal shipped anyway.
    - No row outside the four was written — proved by the histograms, not asserted.
  </acceptance_criteria>
  <verify>
    <automated>grep -qi "AFTER" .planning/phases/194-stop-a-running-workflow/194-HEAL-RECEIPT.md &amp;&amp; grep -qi "terminal_noop" .planning/phases/194-stop-a-running-workflow/194-HEAL-RECEIPT.md</automated>
  </verify>
  <done>All four rows are terminal, the receipt carries a per-row BEFORE→AFTER table plus the method and reason per group, and the histograms prove exactly four rows moved.</done>
  <resume-signal>Paste the two 204 responses and all five AFTER query outputs, then type "healed".</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Close the ledger debt — re-derive every hot-file cell at the phase's final commit</name>
  <files>CLAUDE.md, .planning/phases/194-stop-a-running-workflow/194-HEAL-RECEIPT.md</files>
  <read_first>
    - `CLAUDE.md` § *Workflow guardrails* + the whole hot-file ledger table, including the two rows plan 194-05 added
    - `.planning/phases/194-stop-a-running-workflow/194-BASELINE.md` — the figures measured at `d2a3b51b`
    - The `194-09`, `194-10` and `194-11` summaries — each hands over a measured triple for a backend file not currently on the ledger
    - `.planning/phases/194-stop-a-running-workflow/194-PATTERNS.md` § 12 — the seven structural elements
  </read_first>
  <action>
**(a) RE-DERIVE the three ledger cells this phase touched**, at the phase's FINAL commit:
`frontend/src/components/chat/RunCard.tsx`, `frontend/src/components/panel/WorkspacePanel.tsx` and
`backend/app/db/workflows.py`. For each: `git log --oneline -- <file> | wc -l`, the standard `sed`
bucket recipe with non-phase buckets subtracted and **NAMED**, and `wc -l`.

⚠ **Every figure plan 194-05 wrote is expected to have MOVED, and the correction is recorded BESIDE
it, never over it.** That is not a defect — it is this table's documented behaviour about itself:
the `WorkflowsPage.tsx` cell has gone stale **four consecutive times and once within a single day**,
and *a figure written at a plan's close goes stale on the next commit that touches the file*. Say so
in each corrected cell, and end each with `It inherits N / M / L`.

**(b) MEASURE the three backend files this phase touched that are NOT ledger rows** —
`backend/app/services/run_lifecycle.py`, `backend/app/api/runs.py` and
`backend/app/services/harness_engine.py` — using the triples handed over by plans 194-09, 194-10 and
194-11, **re-derived at the final commit**. For each, state the phase count and whether **G-5 fires**
(≥ 3 phases).

⚠ **For every one that fires, ADD A ROW — even though this phase is closing.** *A hot file missing
from the table is permanently invisible to its own guardrail*, and this project has now watched that
happen to `WorkflowsPage.tsx` for ten phases, `WorkflowDoorSwitch.tsx` for six,
`WorkflowBuilderPage.tsx` for ten, `db/workflows.py` for seventeen, and to both files this very
phase had to add. The whole point of adding a row is that the NEXT phase inherits a measured count
instead of re-deriving one it does not know it owes. Each new row carries the same seven elements,
including the ABSENT sentence and the named next seam. For each, state whether G-5 was honoured **by
construction** with the measured reason (a test, not an argument) — e.g. `run_lifecycle.py` gained
one arm inside a concern it already owns; `api/runs.py` gained the third dual-id fallback on a
prefix where two already ship; `harness_engine.py` gained one write inside a cleanup arm it already
owns, beside a `fail_run` arm doing the same thing three lines below.

**(c) Record the declined override.** A G-5 override was OFFERED AND DECLINED (D-01) — the fourth
consecutive phase. **Verify** that `.planning/STATE.md` records NO guardrail override for Phase 194
before writing that sentence; **that absence is a measurement, not an omission.** If one exists, do
not write the sentence — report the discrepancy.

**(d)** Append a `## Ledger` section to `194-HEAL-RECEIPT.md` listing every re-derived triple with
its command, so the phase's final figures are auditable in one place.

⚠ **Write any grep-able sentence on ONE line** — 193.2-08 measured a rule written WRAPPED across two
lines failing its own literal `grep -q` and reading as *"already fixed"*.
  </action>
  <verify>
    <automated>grep -c "It inherits" CLAUDE.md &amp;&amp; grep -q "ABSENT FROM THIS TABLE UNTIL PHASE 194" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - All three touched cells carry figures re-derived at the final commit, with every moved figure
      recorded **beside** plan 194-05's value and the earlier commit named.
    - Each of the three backend files has a stated phase count and an explicit **G-5 fires / does not
      fire** verdict, with the derivation command shown.
    - Every backend file whose G-5 fires has a ROW, with the ABSENT sentence, the re-derive commands,
      the measured honoured-by-construction reason, the binding invariants, the named next seam and
      `It inherits N / M / L`.
    - The declined-override sentence is present **and** verified against `.planning/STATE.md`.
    - No pre-existing ledger row other than the three touched cells was edited.
    - `194-HEAL-RECEIPT.md` carries the `## Ledger` section with every triple and its command.
  </acceptance_criteria>
  <done>Every hot file this phase touched is a ledger row with figures measured at the final commit, corrections recorded beside their originals, and the next phase inherits numbers instead of a re-derivation it does not know it owes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → four real, irreplaceable database rows | ⚠ V8 Data Protection: this mutates real user data |
| the healed golden run → any future resume path | ⚠ A resumed golden run could perform an external action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-194-13-01 | Tampering | live user data | mitigate | Operator-gated `checkpoint:human-verify`; four rows written **individually by id**, never by predicate sweep; before/after histograms prove exactly four rows moved. |
| T-194-13-02 | Elevation of Privilege | a resumed golden run performing an external action | mitigate | **Already mitigated** by Phase 190's A4 gate (`find_resumable_runs` excludes `is_golden_run = true`). ⚠ The heal **cancels, never re-anchors** — an explicit non-goal, so no arm here can reintroduce the row into a resumable state. |
| T-194-13-03 | Repudiation | the repair itself | mitigate | *A data repair with no receipt is indistinguishable from a claim* (D-12). Row ids, `thread_id`, `created_at`, status before and after, the method per group and the reason are all committed. |
| T-194-13-04 | Elevation of Privilege | the two `DELETE /runs/{id}` calls | mitigate | Driven **as the owning user**, through the owner-scoped, anchor-confirmed fallback — the same boundary V-02 and V-03 fence. Not through an operator override. |
| T-194-13-05 | Information Disclosure | the committed receipt | mitigate | Row ids, thread ids, statuses and timestamps only. No prompt, no phase `output`, no provider payload, no auth header. |
| T-194-13-SC | Tampering | pip/npm installs | mitigate | None owed — no package is added. |
</threat_model>

<verification>
- `194-HEAL-RECEIPT.md` is committed with a BEFORE column, an AFTER column, the method and reason per
  group, the golden-run caveat, and the `## Ledger` section.
- The live DB has **zero** `workflow_runs` rows in `('active','paused','cap_paused')`, **zero**
  `workflow_phases` rows in `active`, and **zero** threads holding a non-null
  `active_workflow_run_id`.
- The histograms prove exactly four rows moved.
- `CLAUDE.md` carries a re-derived cell for every hot file this phase touched.
- `pytest backend/tests/unit -q` and the frontend count gate are both compared to
  `194-BASELINE.md` one final time — no NEW backend failure BY NAME, `count gate OK`, `failed 0`,
  total ≥ baseline.
</verification>

<success_criteria>
- V-20: all four rows terminal, with a committed before/after receipt naming the method and the
  reason for each group.
- The healed golden run is distinguishable as healed, without a new status literal and without being
  re-anchored.
- Every hot file this phase touched is a ledger row with figures measured at the final commit.
</success_criteria>

<output>
Create `.planning/phases/194-stop-a-running-workflow/194-13-SUMMARY.md` when done.
</output>
