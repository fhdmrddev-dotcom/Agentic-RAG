---
phase: 194-stop-a-running-workflow
plan: 07
type: execute
wave: 4
depends_on: ["194-01"]
files_modified:
  - frontend/src/lib/toolMeta.ts
  - frontend/src/lib/__tests__/toolMeta.test.ts
  - frontend/src/components/chat/MessageItem.tsx
  - frontend/src/components/chat/RunCard.tsx
  - frontend/src/components/chat/__tests__/MessageItem.harnessBanner.test.tsx
autonomous: true
requirements: [RUN-01]
validation_rows: [V-07]
must_haves:
  truths:
    - "During a workflow run the chat surface says something true about progress after phase 1 — it no longer holds one pre-tools sentence from kickoff to terminal."
    - "Before phase 1 the banner still reads exactly 'Starting workflow…' — the nine-phase-old byte pin is unmoved."
    - "A Deep run's banner is byte-identical: the additive default keeps every existing caller unchanged."
    - "RunCard's shipped cancelled vocabulary is the starting point and was not replaced."
  artifacts:
    - path: "frontend/src/lib/toolMeta.ts"
      provides: "outerBannerLabel gains an additive-default progress input; the pinned string stays as the pre-phase-1 value"
      contains: "Starting workflow"
    - path: "frontend/src/components/chat/MessageItem.tsx"
      provides: "The harness progress read, beside the workflow-lock read the component already performs"
      contains: "usePhases"
    - path: "frontend/src/components/chat/__tests__/MessageItem.harnessBanner.test.tsx"
      provides: "The advance case, the before-phase-1 case, the Deep byte-identity case, and the measured re-render count"
      contains: "Starting workflow"
  key_links:
    - from: "StreamsProvider harness phase demux (phasesByThread)"
      to: "frontend/src/components/chat/MessageItem.tsx"
      via: "usePhases(message.thread_id), beside the shipped useWorkflowLockForThread(message.thread_id)"
      pattern: "usePhases\\(message\\.thread_id"
    - from: "frontend/src/components/chat/MessageItem.tsx"
      to: "outerBannerLabel"
      via: "the additive progress argument"
      pattern: "outerBannerLabel\\("
---

<objective>
SC#1's legibility half — `BUG-260815-04`, folded. If the chat surface never leaves its pre-tools
state, a user cannot tell the run is running, let alone reach a Stop.

Purpose, and it is a MECHANISM, not a copy bug: `hasAnyTools = (message.tool_calls?.length ?? 0) > 0`
(`MessageItem.tsx:329`), and **a harness run writes NO `tool_calls`** — its progress lives in
`workflow_phases` rows and in `phase_started` / `phase_completed` SSE. So `hasAnyTools` is `false`
for the entire run, `isPlanning` is false, and the branch at `MessageItem.tsx:635` holds from
kickoff to terminal. **The banner is structurally incapable of advancing.** It is not a
subscription gap, not a propagation gap, and not a copy bug.

⚠⚠ **THE STRING IS DELIBERATE AND BYTE-PINNED. DO NOT REWORD IT.** `toolMeta.ts:92` returns
`"Starting workflow…"` when the thread is harness-locked and pre-tools, pinned byte-exact at
`frontend/src/lib/__tests__/toolMeta.test.ts:31` as a D-14 decision. **A plan that "fixes" this by
editing the copy has fixed nothing and broken a nine-phase-old pin.** The fix is the ADVANCING
MECHANISM.

Output: an additive-default progress input on `outerBannerLabel`, its one consumer in `MessageItem`,
the `RunCard` vocabulary check under D-14, and V-07's two-directional test.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/194-stop-a-running-workflow/194-PATTERNS.md
@.planning/phases/194-stop-a-running-workflow/194-RESEARCH.md
@.planning/phases/194-stop-a-running-workflow/194-CONTEXT.md
@.planning/phases/194-stop-a-running-workflow/194-BASELINE.md
@.planning/phases/194-stop-a-running-workflow/194-MEASUREMENTS.md
@.claude/skills/sketch-findings-agentic-rag/references/icon-convention.md

**Base commit — ASSERT FIRST.** Dispatched against `d2a3b51b` on `develop`. In a worktree, run
`bash scripts/bootstrap-worktree.sh "$(pwd)"` FIRST, then assert `d2a3b51b` is the base ancestor.
Tear down only with `bash scripts/teardown-worktree.sh <path>`; never `rm -rf`.
Run vitest with `GSD_VITEST_MAX_WORKERS=2`; never as a third concurrent test-running agent.

<interfaces>
`frontend/src/lib/toolMeta.ts:87-93` — the pre-tools branch:
`if (!hasAnyTools && !isPlanning) { if (reasoningActive) return "Reasoning…"; return isHarness ? "Starting workflow…" : "Setting up agent…" }`.
⚠ `toolMeta.ts:78` and `:88` both already use the **additive-default parameter** shape, and both say
why: *"Mirrors the isHarness additive-default shape above — default false keeps every existing
caller + Deep Mode byte-identical (D-14)."* That is the shape to copy.

`frontend/src/components/chat/MessageItem.tsx`:
- `:300-303` — `const workflowLock = useWorkflowLockForThread(message.thread_id ?? null)`, added by
  Phase 092 (CONT-01 / D-07). **A chat component has consumed a harness-demux slice since Phase 092.**
- `:329` — `const hasAnyTools = (message.tool_calls?.length ?? 0) > 0`
- `:635` — the branch `) : isStreaming && !hasAnyTools ? (`
- `:644` — the call `outerBannerLabel(null, false, message.isPlanning ?? false, workflowLock != null, !message.content && !!message.reasoningContent)`.
  It passes the lock's EXISTENCE, never its progress.
- `:651-659` — the shipped *"cancelled — no output yet"* honest empty state, styled Square + muted italic.

`frontend/src/providers/StreamsProvider.tsx:971-978` — PANEL-09's rule, read at source:
*"They write `phasesByThread` ONLY — never `bucketsBySurface` (PANEL-09: the chat selector
`useThreadMessages` reads `bucketsBySurface` exclusively → zero chat re-renders)."*
⇒ **PANEL-09 is a rule about what the demux WRITES, not about what a component may READ.**
`usePhases(threadId)` is the shipped selector over `phasesByThread`.

`frontend/src/components/chat/RunCard.tsx:529-550` — `statusGlyph` maps `cancelled → "■"` (`:534`)
and `statusWord` maps `cancelled → "cancelled"` (`:548`), both read from the already-persisted
`message.runStatus`.
</interfaces>
</context>

<non_goals>
- ⛔⛔ **The `"Starting workflow…"` string is NOT edited.** Not reworded, not re-cased, not
  re-punctuated, not moved. `toolMeta.test.ts:31`'s byte pin must stay green **and unedited**.
- ⛔ `"Setting up agent…"` and `"Reasoning…"` are not touched — the Deep path must be byte-identical.
- ⛔ **No backend change.** The advancing data is already in the client store; this is a consumption
  gap, not a propagation gap.
- ⛔ **Do not modify the demux.** Adding a read does not change one line of
  `StreamsProvider.tsx:963-1100`, and this plan does not touch that file at all (it belongs to plan
  194-08 in a later wave).
- ⛔ **Do not fix the duplicate assistant icon here.** It is a separate symptom routed by
  `194-MEASUREMENTS.md` (plan 194-01). If that verdict was "defer", it stays deferred with its trigger.
- ⛔ **Do not replace `RunCard`'s `cancelled` glyph/word pair** (D-14: may extend, may not replace
  without stating why). Do not introduce `⏹` anywhere — net-new, absent from `icon-convention.md`
  §4's table, owes a flagged proposal.
</non_goals>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Give outerBannerLabel an additive-default progress input, with the byte pin unmoved (V-07, F-9)</name>
  <files>frontend/src/lib/toolMeta.ts, frontend/src/lib/__tests__/toolMeta.test.ts</files>
  <behavior>
    - **Before phase 1** (harness-locked, no phase progress): returns exactly `"Starting workflow…"`.
      The shipped byte pin at `toolMeta.test.ts:31` stays green **and unedited**.
    - **After a phase completes** (harness-locked, progress supplied): returns a string that is NOT
      `"Starting workflow…"` and that carries the progress truthfully.
    - **Deep run** (`isHarness` false, no progress argument passed): returns exactly
      `"Setting up agent…"` — byte-identical to today.
    - **Reasoning takes precedence** unchanged: `reasoningActive` still returns `"Reasoning…"` ahead
      of the harness branch.
    - Every existing caller that passes no new argument behaves byte-identically.
  </behavior>
  <read_first>
    - `frontend/src/lib/toolMeta.ts:60-110` — `outerBannerLabel` whole, and the two shipped additive-default parameters at `:78` and `:88` with their stated reason
    - `frontend/src/lib/__tests__/toolMeta.test.ts` — **all of it**, and `:31` in particular (the byte-exact assertion). ⚠ CONTEXT cites `toolMeta.test.ts:30`; that path/line is STALE — the file is under `__tests__/` and the assertion is at `:31`.
    - `.planning/phases/194-stop-a-running-workflow/194-RESEARCH.md` § *The fences* row **F-9**
    - `.planning/phases/194-stop-a-running-workflow/194-CONTEXT.md` D-13 and D-18
  </read_first>
  <action>
Add ONE additive-default parameter to `outerBannerLabel` carrying harness progress — the current
phase ordinal, the total, and the phase slug or label the panel already renders. Default it so that
**every existing caller and the whole Deep path stay byte-identical**, which is exactly what
`toolMeta.ts:78` and `:88` already do and already explain.

Inside the `!hasAnyTools && !isPlanning` branch, keep the ordering: `reasoningActive` first
(unchanged), then the harness arm. In the harness arm: **when no progress is supplied, return the
pinned `"Starting workflow…"` string, byte-unchanged**; when progress IS supplied, return a
sentence built from it.

⚠ **The pinned string is the PRE-PHASE-1 VALUE, not the only value.** Say that in the code comment,
because the next reader's instinct will be to delete it.

The progress sentence must be TRUE of what it reads: it may state which step is running and where
in the sequence, and it may not claim completion, a deliverable, or a step count the data does not
support. Keep it in the chat surface's register — chat carries a **thin run receipt**; the panel
owns the meaningful spine (the Phase 094/103 split). Do not rebuild the spine in the banner.

**⚠ DRIVE F-9 RED.** Plant, in production source: make the harness arm return the phase label
**unconditionally** (i.e. delete the no-progress guard) → `toolMeta.test.ts:31` must RED → revert →
assert the file restores **md5-identical**. Record the planted line, the failing assertion output
and the post-revert md5. ⚠ **`toolMeta.test.ts:31` must not be edited by this plan.** If it needs
editing to accommodate the new parameter, the parameter is not additive — redesign it.

Add the four `<behavior>` cases to `toolMeta.test.ts` **alongside** the shipped ones.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/lib/__tests__/toolMeta.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The suite passes, and `git diff -- frontend/src/lib/__tests__/toolMeta.test.ts` shows
      **additions only** — no shipped assertion, including `:31`, was modified or moved.
    - `grep -c "Starting workflow…" frontend/src/lib/toolMeta.ts` returns exactly 1 and the byte
      sequence is unchanged (verify with `git diff` showing no `-` line containing it).
    - `grep -c "Setting up agent…" frontend/src/lib/toolMeta.ts` and the `"Reasoning…"` line are
      unchanged.
    - The new parameter has a default; a call with the shipped argument list compiles and returns
      the shipped values (asserted by the Deep byte-identity case).
    - **The F-9 RED observation is recorded**: the planted line, the exact failing assertion output
      from `toolMeta.test.ts:31`, and the post-revert md5 of `toolMeta.ts`.
    - `npx tsc -p tsconfig.app.json --noEmit` matches `194-BASELINE.md`.
  </acceptance_criteria>
  <done>The banner can advance, the nine-phase-old byte pin is green and unedited, the Deep path is byte-identical, and F-9 has been watched to FAIL.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consume the progress in MessageItem — the PANEL-09 crossing, with the cost MEASURED and stated beside the original reasoning (D-18)</name>
  <files>frontend/src/components/chat/MessageItem.tsx, frontend/src/components/chat/__tests__/MessageItem.harnessBanner.test.tsx</files>
  <behavior>
    - A streaming harness message with **zero** completed phases renders exactly
      `"Starting workflow…"`.
    - The same message with **one or more** completed phases renders a different, progress-bearing
      sentence.
    - A streaming **Deep** message (no workflow lock) renders exactly `"Setting up agent…"` — the
      shipped value, unchanged.
    - A message with tool calls is unaffected: the `hasAnyTools` branch still wins.
    - The component re-renders on a PHASE transition and **not** on a token delta — asserted by a
      render counter across a simulated token stream versus a simulated phase transition.
  </behavior>
  <read_first>
    - `frontend/src/components/chat/MessageItem.tsx` — `:298-335` (the shipped `useWorkflowLockForThread` read and `hasAnyTools`), `:630-660` (the branch, the `outerBannerLabel` call and the shipped cancelled empty state)
    - `frontend/src/providers/StreamsProvider.tsx:963-1100` — the harness phase demux and **PANEL-08 / PANEL-09's comment, read at source**
    - `.planning/phases/194-stop-a-running-workflow/194-PATTERNS.md` § 10 — ⭐ the analog is in the same component, one hook over, and it materially reframes D-18's cost
    - `.planning/phases/194-stop-a-running-workflow/194-CONTEXT.md` **D-18** — the ruling, and the requirement to record the accepted cost AND the measured re-render count **beside** PANEL-09's original reasoning, not over it
    - `frontend/src/components/chat/__tests__/MessageItem.test.tsx` — the existing harness for this component
  </read_first>
  <action>
Read the harness progress in `MessageItem` **beside** the workflow-lock read it already performs at
`:303`, using the shipped `usePhases(message.thread_id ?? null)` selector, and pass it into
`outerBannerLabel` at `:644` as the new additive argument. Nothing else in the component changes.

⚠ **RECORD THE PANEL-09 CROSSING HONESTLY, IN A COMMENT BESIDE THE READ.** Three things must be
written, and the third is the one that is usually skipped:
1. **PANEL-09's original reasoning, quoted, not paraphrased** — the demux writes `phasesByThread`
   ONLY, never `bucketsBySurface`, so panel events cost zero chat re-renders. That was a considered
   performance decision.
2. **What is and is not being changed** — PANEL-09 is a rule about what the demux WRITES; this adds
   a READ and **does not change one line of the demux**, exactly as `useWorkflowLockForThread` has
   done in this same component since Phase 092. `bucketsBySurface` is untouched.
3. **The ACCEPTED COST, with the MEASURED number** — the re-render is **per PHASE TRANSITION, not
   per token**; a workflow run has a handful of phases, so the cost PANEL-09 was protecting against
   (token-rate re-renders) is **not the cost being incurred**. ⚠ D-18 requires a MEASURED re-render
   count, not an assertion: instrument a render counter in the test, drive a simulated token stream
   and a simulated phase sequence, and record both counts. If the token-stream count moves at all,
   the read is wired to the wrong slice — stop and report.

⚠ **The claim `usePhases` is reference-stable across token deltas must be PROVED, not assumed.**
`ActiveRunsTray.tsx:20` documents that property for a sibling selector; this plan measures it for
this one. That measurement is the whole justification D-18 rests on.

Write the five `<behavior>` cases into a new
`frontend/src/components/chat/__tests__/MessageItem.harnessBanner.test.tsx` — a new file so the
shipped `MessageItem.test.tsx` stays byte-unedited.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/chat/__tests__/MessageItem.harnessBanner.test.tsx src/components/chat/__tests__/MessageItem.test.tsx src/components/chat/__tests__/MessageItem.blockedNotice.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All three suites pass; `git diff --numstat -- frontend/src/components/chat/__tests__/MessageItem.test.tsx`
      is **empty** (the shipped suite is byte-unedited).
    - `grep -c "usePhases(message.thread_id" frontend/src/components/chat/MessageItem.tsx` returns
      exactly 1, and it sits beside the shipped `useWorkflowLockForThread` read.
    - `git diff --numstat -- frontend/src/providers/StreamsProvider.tsx` is **empty** — the demux is
      untouched.
    - **Two measured re-render counts are recorded in the summary**: renders across a simulated
      token stream (must be **unmoved** from the pre-change baseline) and renders across a simulated
      phase sequence (expected to equal the number of phase transitions). A moved token-stream count
      is a STOP condition.
    - The comment beside the read quotes PANEL-09's original reasoning verbatim, states what is not
      being changed, and records the accepted cost with the measured numbers — **beside** the
      original, never over it.
    - `npx tsc -p tsconfig.app.json --noEmit` matches `194-BASELINE.md`.
  </acceptance_criteria>
  <done>The chat banner advances per phase, the demux is untouched, and D-18's accepted cost is recorded with two measured re-render counts rather than an assurance.</done>
</task>

<task type="auto">
  <name>Task 3: RunCard's cancelled vocabulary under D-14 — extend or leave, and state which</name>
  <files>frontend/src/components/chat/RunCard.tsx</files>
  <read_first>
    - `frontend/src/components/chat/RunCard.tsx:520-560` — `statusGlyph` and `statusWord`, and the UI-SPEC §8.2 copy-contract comment above them
    - `frontend/src/components/chat/MessageItem.tsx:651-659` — the shipped *"cancelled — no output yet"* honest empty state (lucide Square + muted italic), i.e. the chat surface has already chosen lucide `Square` over `■` for the control-adjacent case
    - `.claude/skills/sketch-findings-agentic-rag/references/icon-convention.md` §4
    - `.planning/phases/194-stop-a-running-workflow/194-CONTEXT.md` D-13 and D-14
    - `.planning/phases/194-stop-a-running-workflow/194-BASELINE.md` — the `RunCard.tsx` G-5 figures
  </read_first>
  <action>
`RunCard`'s vocabulary already maps `cancelled → "■"` / `"cancelled"`, read from the
already-persisted `message.runStatus`. D-14 says it is the **starting point, not a rewrite**:
planning may extend it; it may not replace it without stating why.

Decide, and **state the decision either way** — a plan that silently leaves a file untouched and a
plan that considered it and declined are two different facts, and only one of them is auditable:
- If the receipt needs no change, add a comment recording that D-14 was applied and the shipped pair
  was KEPT, with the reason: a stopped run's chat receipt is a thin run receipt (the 094/103 split),
  the panel owns the meaningful spine, and the persisted `runStatus` already drives both.
- If it needs an extension (e.g. the receipt should not read as though nothing survived), extend it
  **additively** and state why — and ⚠ the copy must not claim partial work was discarded. D-13
  keeps completed phases and explicitly rejects *"collapsing to a bare `cancelled` that hides
  partial work … it discards evidence the database still holds."*

⛔ Do not replace `■`. ⛔ Do not introduce `⏹`. ⚠ Record, as a correction beside RESEARCH's
three-mark table, the **FOURTH shipped mark for this concept** that RESEARCH missed:
`WorkflowRunPage.tsx:299` renders `"⊘ Cancelled"` as the run band's cancelled sentence.

**G-5 statement (D-01, 8 phases):** report the measured evidence that this file gained no state —
`useState(` count before → after, new `fetch(` count, new prop count, all expected 0/unmoved. ⚠ If
any moved, D-01 fires: that is a SECOND concern and the refactor recommendation is owed FIRST.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/chat/__tests__/ &amp;&amp; npx tsc -p tsconfig.app.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The chat suites pass and `npx tsc -p tsconfig.app.json --noEmit` matches `194-BASELINE.md`.
    - `grep -c '"■"' frontend/src/components/chat/RunCard.tsx` is unchanged and
      `grep -c "⏹" frontend/src/components/chat/RunCard.tsx` returns 0.
    - The D-14 decision is recorded in the file as a comment AND in the summary — "kept, and here is
      why" or "extended, and here is why". Silence is not acceptable.
    - Any new copy contains no claim that partial or completed work was discarded (asserted by a
      negative match on the forbidden phrasing).
    - The G-5 evidence is reported as measured before → after figures, not as a claim.
    - The fourth-mark correction (`⊘` at `WorkflowRunPage.tsx:299`) is recorded.
  </acceptance_criteria>
  <done>`RunCard`'s cancelled vocabulary has an auditable D-14 decision behind it, `■` is intact, `⏹` is absent, and G-5 is honoured with measured evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store slice → rendered chat copy | Server-derived phase data reaches a sentence a user reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-194-07-01 | Spoofing (of progress) | the harness banner | mitigate | The sentence may state only what the phase slice supports. It may not claim completion, a deliverable, or a step count the data does not carry — and the pre-phase-1 value stays the pinned string, so an absent slice degrades to the shipped truth rather than to an invented one. |
| T-194-07-02 | Information Disclosure | banner copy | mitigate | The banner renders the phase ordinal / total / slug only. No phase `output`, no run error text, no user content enters the sentence. |
| T-194-07-03 | Denial of Service | chat render loop | mitigate | The read is on `phasesByThread`, reassigned on phase transitions, never on tokens. The token-stream re-render count is MEASURED and must be unmoved; a moved count is a stop condition, not a note. |
| T-194-07-04 | Repudiation | the byte-pinned string | mitigate | `toolMeta.test.ts:31` stays green **and unedited**, and F-9 is driven RED against a real production-source plant to prove the pin can still fire. |
| T-194-07-SC | Tampering | npm installs | mitigate | None owed — no package is added. |
</threat_model>

<verification>
- `toolMeta.test.ts`, `MessageItem.harnessBanner.test.tsx`, `MessageItem.test.tsx` and the chat
  suites all green; `MessageItem.test.tsx` byte-unedited.
- `npx tsc -p tsconfig.app.json --noEmit` matches `194-BASELINE.md`.
- Count gate: `count gate OK`, `failed 0`, total ≥ baseline, all pinned files present.
- `git diff --numstat -- frontend/src/providers/StreamsProvider.tsx` is empty.
- `git diff` on `toolMeta.ts` contains no `-` line carrying `Starting workflow…`.
</verification>

<success_criteria>
- V-07 both directions: the banner advances after a phase completes AND still reads
  `"Starting workflow…"` before phase 1, with `toolMeta.test.ts:31` green and unedited.
- F-9 driven RED against a real production-source plant, with the md5-identical revert recorded.
- D-18's accepted cost is recorded with two measured re-render counts, beside PANEL-09's original
  reasoning.
- `RunCard`'s D-14 decision is stated and auditable; G-5 honoured with measured evidence.
</success_criteria>

<output>
Create `.planning/phases/194-stop-a-running-workflow/194-07-SUMMARY.md` when done.
</output>
