---
phase: 194-stop-a-running-workflow
plan: 03
type: execute
wave: 2
depends_on: ["194-01"]
files_modified:
  - frontend/src/components/panel/WorkspacePanel.tsx
  - frontend/src/components/panel/__tests__/WorkspacePanel.test.tsx
autonomous: true
requirements: [RUN-01]
validation_rows: [V-04, V-05]
must_haves:
  truths:
    - "A user watching a workflow run in the panel can stop it from the panel, without navigating anywhere."
    - "The panel's Stop resolves its run through stopThread(threadId) — never through workflowLock.runId, which is the wrong id type half the time and 404s silently."
    - "A Deep thread and a no-run thread see nothing new — the control lives inside the existing harness gate."
  artifacts:
    - path: "frontend/src/components/panel/WorkspacePanel.tsx"
      provides: "The primary Stop mount: one useStreamActions import + one additive sibling inside the shipped showTimeline gate"
      contains: "panel-stop-run"
    - path: "frontend/src/components/panel/__tests__/WorkspacePanel.test.tsx"
      provides: "V-04 behaviour test + the V-05/F-1 union-scoped source fence, each driven RED against a real production-source plant"
      contains: "panel-stop-run"
  key_links:
    - from: "frontend/src/components/panel/WorkspacePanel.tsx"
      to: "useStreamActions().stopThread"
      via: "the click handler, passing the THREAD id"
      pattern: "stopThread\\("
    - from: "frontend/src/components/panel/__tests__/WorkspacePanel.test.tsx"
      to: "@/providers/StreamsProvider mock"
      via: "a NINTH mock key — the mock is an explicit object literal, not a passthrough"
      pattern: "useStreamActions"
---

<objective>
SC#1. Mount the **primary** Stop control on the workflow run surface the user actually watches:
`WorkspacePanel`, which today has **no Stop control of any kind** (`grep` returns only local
`let cancelled` effect flags). This is where a user watches a workflow run, per the Phase 094/103
design decision that **the panel owns the meaningful phase spine and chat carries a thin run
receipt**.

Purpose: G-A, the SC#1 gap. Every shipped Stop keys off a streaming assistant message in the chat
bucket; there is no Stop on the panel at all.

Output: one `useStreamActions` import and one additive-sibling control inside the shipped
`showTimeline` gate, plus the V-04 behaviour test and the V-05 / F-1 source fence.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/194-stop-a-running-workflow/194-PATTERNS.md
@.planning/phases/194-stop-a-running-workflow/194-RESEARCH.md
@.planning/phases/194-stop-a-running-workflow/194-CONTEXT.md
@.planning/phases/194-stop-a-running-workflow/194-BASELINE.md
@frontend/src/components/chat/ActiveRunsTray.tsx
@.claude/skills/sketch-findings-agentic-rag/references/icon-convention.md

**Base commit — ASSERT FIRST.** Dispatched against `d2a3b51b` on `develop`. In a worktree, run
`bash scripts/bootstrap-worktree.sh "$(pwd)"` FIRST, then assert `d2a3b51b` is the base ancestor
(worktrees forked from the WRONG base 12/12 in Phase 192). Tear down only with
`bash scripts/teardown-worktree.sh <path>`; never `rm -rf` — it follows the junction and destroys
the operator's real 1.7 GB `venv`.

**Concurrency:** run vitest with `GSD_VITEST_MAX_WORKERS=2`, and never as a third concurrent
test-running agent. Capture failing FILENAMES before re-running anything — cap 2 is not
deterministic.

<interfaces>
The contracts this plan consumes. The executor should NOT go hunting for these.

`frontend/src/components/panel/WorkspacePanel.tsx` already holds everything the mount needs:
- `const threadId = useViewingThread()` at `:271`
- an **8-key** import block from `@/providers/StreamsProvider` at `:34-43`
- `const workflowLock = useWorkflowLockForThread(threadId)` at `:291`
- `const isHarness = workflowLock != null` at `:292`
- `const showTimeline = isHarness || phases.length > 0` at **`:293`** (⚠ RESEARCH said `:292`;
  PATTERNS corrected it — `:292` is the `isHarness` line)
- the mount region at `:371-391`: `{showTimeline && (<PanelSection title="This workflow"><RunSoul …/></PanelSection>)}`,
  then `{showTimeline && <RunSeam threadId={threadId} onOpenRun={onOpenRun} />}`,
  then `{showTimeline && (<PanelSection title="Workflow" count={…}><PhaseTimeline threadId={threadId}/></PanelSection>)}`

`frontend/src/providers/StreamsProvider.tsx` exports `useStreamActions()`, whose
`stopThread(threadId: string): Promise<void>` (`:2400-2415`) scans the chat bucket for the latest
assistant message with `runStatus === "streaming"`, reads its `runId` — the **producer**
`runs.run_id`, the correct type — and calls `cancelRun(runId)`. It early-returns on a falsy
`runId` (`:2408`).

`frontend/src/components/chat/ActiveRunsTray.tsx:127-133` is the shipped per-run Stop control:
a `<button onClick={() => void streamActions.stopThread(id)}>` with destructive border/bg/text
tokens, an `aria-label` of the form `Stop run on {title}`, and a lucide `<Square className="h-2.5 w-2.5 fill-current" aria-hidden="true" />`
followed by the word `Stop`.
</interfaces>
</context>

<non_goals>
- ⛔ **The control MUST NOT acquire a run id of its own.** `RunSeam`'s `useState<string | null>` fed
  from `getThreadWorkflow` (`:201`, `:219`) holds the **`workflow_runs.id`** — the wrong type for
  `cancelRun`. Copying that id-acquisition is NEW STATE OWNERSHIP ⇒ a SECOND concern ⇒ **D-01 fires
  and the G-5 refactor recommendation is owed FIRST**, before this feature. If the mount cannot be
  built with `stopThread(threadId)` alone, STOP and report; do not plumb an id.
- ⛔ No `PhaseCard` / `PhaseTimeline` internal may be read, and no prop may be added to either — the
  G-5 red line written into this file at `:85-93` and restated at `:182-190`.
- ⛔ No new fetch, no new prop on `WorkspacePanel`, no new store slice, no new `useState`.
- ⛔ Mount 4 is DESCOPED (D-16). Nothing under `frontend/src/pages/WorkflowsPage.tsx` or
  `frontend/src/components/workflows/library/` may be touched.
- ⛔ No net-new canvas/spine GLYPH is invented. `⏹` is absent from `icon-convention.md` §4's table
  and owes a flagged proposal; do not use it.
- ⛔ No new npm package (no toast library — Phase 192's gap round refused `sonner` for this reason).
</non_goals>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mount the panel Stop as an additive sibling, and add the NINTH mock key in the same commit (V-04)</name>
  <files>frontend/src/components/panel/WorkspacePanel.tsx, frontend/src/components/panel/__tests__/WorkspacePanel.test.tsx</files>
  <behavior>
    - With a harness thread (a non-null workflow lock), the panel renders a control with
      `data-testid="panel-stop-run"`.
    - Clicking it calls `stopThread` **exactly once**, with the **thread id** — not a run id, not a
      lock id, not `undefined`.
    - With a Deep / no-run thread (`workflowLock == null` and zero phases, so `showTimeline` is
      false), the control does **not** render — asserted by `queryByTestId(...)` being null.
    - Every pre-existing case in `WorkspacePanel.test.tsx` and `WorkspacePanel.derived.test.tsx`
      stays green with **no edit to its assertions**.
  </behavior>
  <read_first>
    - `frontend/src/components/panel/WorkspacePanel.tsx` — **the whole file** (493 L). In particular `:85-93` (the `RunSoul` additive-sibling G-5 red line), `:160-190` (the `RunSeam` docblock, which already records the id-type landmine: *"the lock's id field is documented as the anchor but is overwritten with a PRODUCER run id at kickoff … Both ids are bare uuids, so a swap typechecks and then resolves nothing"*), `:271`, `:291-293`, `:371-391`
    - `frontend/src/components/panel/__tests__/WorkspacePanel.test.tsx` — **`:71-81` first**: the `@/providers/StreamsProvider` mock is an **EXPLICIT object literal with exactly eight keys and NO `useStreamActions`**. And `:97-100`, where this exact failure mode is already documented from Phase 100.
    - `frontend/src/components/chat/ActiveRunsTray.tsx:114-136` — the shipped control to copy (tokens, `aria-label` shape, `void` on the promise) and its docblock rule at `:25-26`: *"Every Stop routes through the one durable cancel path … nothing here invents new backend behavior."*
    - `frontend/src/components/panel/PanelSection.tsx` — the section shell
    - `.planning/phases/194-stop-a-running-workflow/194-PATTERNS.md` § 8 and § 9
  </read_first>
  <action>
Add `useStreamActions` to the existing 8-key import block from `@/providers/StreamsProvider` — a
**ninth import from a module this file already imports eight from**. Call it once in the component
body beside the existing hook calls.

Render the control **inside the existing `showTimeline` gate**, as an additive sibling of the
`PhaseTimeline` `PanelSection` — the same shape `RunSoul` and `RunSeam` already use. It must
satisfy all four clauses of the shipped G-5 red line: reads no `PhaseCard`/`PhaseTimeline`
internal; adds a prop to neither; sits inside the SAME harness gate so a Deep / no-run thread sees
nothing new; and renders nothing when its data is absent, so every existing caller and every
existing panel test is byte-unchanged.

The handler is `() => void streamActions.stopThread(threadId)`. **`threadId`, never a run id.**

Copy `ActiveRunsTray.tsx:127-133`'s control vocabulary byte-consistently: the destructive
border/background/text tokens, the lucide `<Square className="… fill-current" aria-hidden="true" />`
plus the word `Stop`, and an `aria-label` naming what is being stopped (e.g. `Stop this workflow
run`). ⚠ **This is a REUSE, not a choice**: lucide `Square` is the shipped Stop-**control** mark at
two sites already, so the panel becomes the third occurrence of one mark for one concept. Do **not**
use `⏹` (net-new, absent from `icon-convention.md` §4's table, owes a flagged proposal) and do not
use `■` (that is `RunCard`'s cancelled-**state** glyph, a different concept — a state, not a
control).

Give it `data-testid="panel-stop-run"`.

The control reads as stopping **the RUN**, and must not be confusable with skipping a phase
(Claude's-discretion constraint from CONTEXT): mount it at run level, not inside or beside any
individual phase row, and word it accordingly.

⚠ **THE NINTH MOCK KEY IS OWED IN THIS SAME COMMIT.** `WorkspacePanel.test.tsx:71-81` mocks
`@/providers/StreamsProvider` as an explicit eight-key object; a component reading a hook the mock
omits throws. The file already documents this exact failure from Phase 100 (`:97-100`), where the
chosen fix was to sentinel-mock the CHILD. **194 cannot do that, because the caller IS the panel.**
Add `useStreamActions` as the ninth key, wired to a `vi.fn()`-backed stub exposing `stopThread`, in
the same commit as the component change. Do the same for
`WorkspacePanel.derived.test.tsx` if it carries its own copy of the mock.

Write the three behaviour cases from `<behavior>` above.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/panel/__tests__/WorkspacePanel.test.tsx src/components/panel/__tests__/WorkspacePanel.derived.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The two suites above pass, and **no pre-existing assertion in either file was edited** —
      `git diff` on those files shows additions plus the single mock-key line, and no modification
      to an existing `expect(`.
    - `grep -c "data-testid=\"panel-stop-run\"" frontend/src/components/panel/WorkspacePanel.tsx`
      returns exactly 1.
    - `grep -c "stopThread(threadId)" frontend/src/components/panel/WorkspacePanel.tsx` returns
      exactly 1, and `grep -cE "workflowLock\??\.runId|cancelRun\(" frontend/src/components/panel/WorkspacePanel.tsx`
      returns **0**.
    - `grep -cE "useState|useEffect\(.*getThreadWorkflow|fetch\(" ` on the diff hunks returns 0 —
      **no new state, no new fetch**. State this as a measured figure in the summary
      (`useState(` count before → after must be unchanged).
    - The control is inside the `showTimeline` gate: the negative case (Deep / no-run thread)
      asserts `queryByTestId("panel-stop-run")` is null.
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit` reports the SAME error count as
      `194-BASELINE.md` (expected 33). ⚠ A bare `--noEmit` without `-p` checks zero files.
    - The `StreamsProvider` mock in `WorkspacePanel.test.tsx` has **nine** keys.
  </acceptance_criteria>
  <done>The panel renders a run-level Stop inside the shipped harness gate, calls `stopThread(threadId)` exactly once, renders nothing on a Deep thread, and the panel suites are green with no pre-existing assertion edited.</done>
</task>

<task type="auto">
  <name>Task 2: The F-1 fence — no mount resolves a cancel through the workflow lock's id (V-05), driven RED with a plant in EACH mount directory</name>
  <files>frontend/src/components/panel/__tests__/WorkspacePanel.test.tsx</files>
  <read_first>
    - `.planning/phases/194-stop-a-running-workflow/194-RESEARCH.md` § *The fences* row **F-1**, and the scope-trap warning immediately below the table
    - `.planning/phases/194-stop-a-running-workflow/194-PATTERNS.md` § *THE LANDMINE* (all five `WorkflowLock.runId` write sites, and the measurement that `workflowLock.runId` has exactly ONE production read in the whole frontend — `MessageItem.tsx:596,:599`, which calls `/continue`, the one route that already tolerates both ids)
    - `frontend/src/components/panel/__tests__/WorkspacePanel.test.tsx:31` — the shipped `?raw` source-fence import shape
    - `frontend/src/lib/api.ts:1259-1269` — `cancelRun`'s **deliberate 404 swallow**, which is why a wrong-id Stop is a SILENT success
    - `frontend/src/stores/streamsStore.ts:51-59` — the JSDoc that asserts only one of the two id types
  </read_first>
  <action>
Add a source fence asserting that **no module in any Stop-mount directory passes the workflow
lock's `runId` (in any spelling) to `cancelRun`, or calls `cancelRun` at all.**

**Scope it over the UNION of the mount directories, not over `components/panel/` alone.** RESEARCH
names this trap explicitly and this repository has been bitten by it twice: Phase 192.1 shipped a
fence that swept a **renamed module against the empty string and passed green**. The union is
`frontend/src/components/panel/`, `frontend/src/components/chat/` and
`frontend/src/components/workflows/`. Use the shipped `?raw` import shape
(`WorkspacePanel.test.tsx:31`) or an equivalent directory read; if the fence reads files from disk,
it must **assert a non-zero file count first**, so a wrong glob cannot report green about files it
cannot see.

The fence has two clauses:
(a) no `cancelRun(` call site appears anywhere in the union — the only two production call sites
    are `StreamsProvider.tsx:2389` (`stopStream`) and `:2411` (`stopThread`), and every new mount
    routes through those (D-08: four mounts, ONE mechanism, no second cancel path);
(b) no `workflowLock.runId` / `workflowLock?.runId` / `lock.runId` value is passed to any cancel
    call. ⚠ **Carve out the ONE legitimate shipped read** — `MessageItem.tsx:596` / `:599` reads
    `workflowLock.runId` and hands it to `continueRun`, which is correct because `/continue` has
    the dual-id fallback. The fence must permit `continueRun` and forbid `cancelRun`; a fence that
    forbids the identifier outright would red on shipped, correct code and would be rewritten to
    uselessness on its first run.

**⚠ DRIVE IT RED. A fence is only real once you have watched it FAIL.** This project shipped five
inert fences in Phase 193.2, four in 193.1, three in 192.1 and five in 190 — **every one caught by
planting, none by reading.** Required plants, each in **production source** (never in the test),
each observed RED, each reverted with the file restored **md5-identical**:

1. In `WorkspacePanel.tsx`, replace `stopThread(threadId)` with a `cancelRun(workflowLock.runId)`
   call → observe RED → revert → assert md5 identical.
2. The **`?.` spelling, separately**: plant `cancelRun(workflowLock?.runId)` → observe RED → revert.
   RESEARCH requires this as a distinct plant; an assertion matching only the bare form is half a
   fence.
3. **One plant per remaining mount directory**, so the union scope is proven rather than assumed:
   a `cancelRun(` call planted in a real file under `components/chat/`, and one under
   `components/workflows/` → observe RED for each → revert → md5 identical.

Record each plant's file, the exact line planted, the RED output, and the post-revert md5 in the
plan summary. A fence with no recorded RED observation is a claim.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/panel/__tests__/WorkspacePanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The fence passes on the unmodified tree.
    - **Four RED observations are recorded**, one per plant (bare `.runId`, `?.runId`,
      `components/chat/`, `components/workflows/`), each with the planted line, the failing
      assertion message, and the post-revert md5 proving the production file was restored byte-identical.
    - The fence asserts a non-zero swept-file count before asserting absence, so an empty sweep
      cannot pass — demonstrate this by temporarily narrowing the glob to a non-existent directory
      and observing the count assertion RED (this is the 192.1 empty-string lesson, mechanised).
    - `continueRun(workflowLock.runId)` at `MessageItem.tsx:596-599` remains GREEN under the fence —
      the carve-out is proven, not assumed.
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit` matches `194-BASELINE.md`.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 node ../scripts/vitest-count-gate.cjs` reads
      `count gate OK`, `failed 0`, and a total **≥** the baseline (a growing total is the gate
      WORKING; the contract is no per-file decrease and zero failing).
  </acceptance_criteria>
  <done>The V-05 / F-1 fence sweeps all three mount directories, has been driven RED four times against real production-source plants plus once against an empty sweep, and permits the one legitimate shipped `continueRun` read.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user gesture → the durable cancel path | The panel Stop is a destructive action on a running workflow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-194-03-01 | Spoofing (of success) | the Stop control | mitigate | The mount resolves via `stopThread(threadId)`, never `workflowLock.runId`. A wrong-id Stop 404s and `cancelRun` **swallows 404** (`api.ts:1265-1267`) — a silent success is worse than a visible failure in a phase about honesty. Fenced by F-1 with four real plants. |
| T-194-03-02 | Elevation of Privilege | `DELETE /runs/{id}` | transfer | The client sends only a thread-scoped resolution; ownership is enforced server-side by the Step-1 `runs` SELECT `.eq("user_id", …)` and, from plan 194-11, by the owner-scoped + anchor-confirmed fallback. **404 never 403.** |
| T-194-03-03 | Denial of Service | a stray Stop on a Deep thread | mitigate | The control lives inside the existing `showTimeline` harness gate; the negative case asserts it does not render on a Deep / no-run thread. |
| T-194-03-SC | Tampering | npm installs | mitigate | None owed — no package is added. A toast library is explicitly out of scope (Phase 192's gap round refused `sonner`). |
</threat_model>

<verification>
- `WorkspacePanel.test.tsx` and `WorkspacePanel.derived.test.tsx` green; no pre-existing assertion edited.
- `npx tsc -p tsconfig.app.json --noEmit` unchanged against `194-BASELINE.md`.
- Count gate: `count gate OK`, `failed 0`, total ≥ baseline, all pinned files present.
- `git diff --numstat` touches only the two files in `files_modified`.
- **G-5 (D-01) is honoured BY CONSTRUCTION and the evidence is stated, not asserted:** report the
  measured before → after `useState(` count in `WorkspacePanel.tsx` (must be unchanged), the count
  of new `fetch(` calls (0), and the count of new props on `WorkspacePanel` (0). If any of those
  moved, the mount acquired state ownership, D-01 fires, and the refactor recommendation is owed
  FIRST — stop and report rather than proceeding.
</verification>

<success_criteria>
- V-04: the panel renders a Stop under the harness gate and calls `stopThread` with the thread id.
- V-05 / F-1: the union-scoped fence exists, is driven RED four times plus once on an empty sweep,
  and permits the shipped `continueRun` read.
- `WorkspacePanel.tsx` gained a control and no state — G-5 honoured by construction, no override.
</success_criteria>

<output>
Create `.planning/phases/194-stop-a-running-workflow/194-03-SUMMARY.md` when done.
</output>
