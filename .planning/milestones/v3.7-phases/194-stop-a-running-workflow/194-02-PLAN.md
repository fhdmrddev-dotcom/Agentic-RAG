---
phase: 194-stop-a-running-workflow
plan: 02
type: execute
wave: 2
depends_on: ["194-01"]
files_modified:
  - supabase/migrations/119_workflow_phases_cancelled.sql
  - backend/tests/test_migration_119.py
autonomous: true
requirements: [RUN-01]
validation_rows: [V-14, V-15]
must_haves:
  truths:
    - "Migration 119 exists as a file and widens workflow_phases_status_check from 6 literals to 7 by ADDING 'cancelled', with all six shipped literals re-added verbatim."
    - "The live-DB gate for migration 119 green-skips cleanly until the migration is applied, and its positive control loops PER LITERAL rather than asserting them in one collapsed expression."
    - "The negative control proves a display sentence is still refused with SQLSTATE 23514 naming workflow_phases_status_check — the column stores the SLUG."
  artifacts:
    - path: "supabase/migrations/119_workflow_phases_cancelled.sql"
      provides: "The CHECK widening, in the mig-115 shape"
      contains: "workflow_phases_status_check"
    - path: "backend/tests/test_migration_119.py"
      provides: "The live-DB gate: seven-literal positive control (looped) + the 23514 negative control"
      contains: "23514"
  key_links:
    - from: "supabase/migrations/119_workflow_phases_cancelled.sql"
      to: "public.workflow_phases"
      via: "DROP CONSTRAINT IF EXISTS then ADD CONSTRAINT inside BEGIN/COMMIT"
      pattern: "ADD CONSTRAINT workflow_phases_status_check"
    - from: "backend/tests/test_migration_119.py"
      to: "pg_constraint"
      via: "the applied-probe reads pg_get_constraintdef, never an INSERT probe"
      pattern: "pg_get_constraintdef"
---

<objective>
Author migration 119 and its live-DB gate. **This plan AUTHORS files and applies NOTHING** — the
apply is an operator action in plan 194-12 (D-06: paste into the Supabase SQL editor, never
`db push` / `db reset`).

Purpose: SC#3. `workflow_phases_status_check` (migration 115) admits exactly six literals —
`pending / active / completed / failed / skipped / recorded_not_sent` — and there is no
`cancelled`. Reusing `failed` or `skipped` was OFFERED AND REJECTED as dishonest (D-04): the phase
did not fail and it was not skipped; it ran and was interrupted. Without this literal, the
in-flight `workflow_phases` row on a stopped run stays `active` forever, which is SC#3's failure
mode in the schema itself.

Output: `supabase/migrations/119_workflow_phases_cancelled.sql` and
`backend/tests/test_migration_119.py`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/194-stop-a-running-workflow/194-PATTERNS.md
@.planning/phases/194-stop-a-running-workflow/194-RESEARCH.md
@.planning/phases/194-stop-a-running-workflow/194-CONTEXT.md
@.planning/phases/194-stop-a-running-workflow/194-BASELINE.md
@supabase/migrations/115_workflow_phases_recorded_not_sent.sql
@backend/tests/test_migration_115.py

**Base commit — ASSERT FIRST.** Dispatched against `d2a3b51b` on `develop`. In a worktree, run
`bash scripts/bootstrap-worktree.sh "$(pwd)"` FIRST, then assert `d2a3b51b` is the base ancestor.
Tear down only with `bash scripts/teardown-worktree.sh <path>`; never `rm -rf`.

**Serialization:** this plan is parallel-safe — it authors a file and applies nothing. The apply,
and every test that seeds `workflow_runs` / `workflow_phases`, are SERIALIZED in later waves
(CLAUDE.md § *Parallel execution* rule 4; the `DROP+ADD CONSTRAINT` takes a brief ACCESS EXCLUSIVE
lock on `workflow_phases`).
</context>

<non_goals>
- ⛔ **Do not apply the migration.** No `supabase db push`, no `db reset`, no psql apply, no
  `regenerate-full-schema.sh` in this plan. The apply is operator-gated (194-12).
- ⛔ Do not hand-edit `supabase/full-schema.sql`. It is regenerated, never edited.
- ⛔ Do not touch `workflow_runs_status_check` — it already admits `cancelled` (mig 057:19,
  re-asserted mig 063:55-57). No migration is owed on that side.
- ⛔ No table, column, index, grant or RLS change. The migration alters ONE constraint.
- ⛔ Do not apply this migration to the CLOUD database in this phase. It belongs in the standing
  migration-parity window (mig 115's own precedent).
</non_goals>

<tasks>

<task type="auto">
  <name>Task 1: Author supabase/migrations/119_workflow_phases_cancelled.sql</name>
  <files>supabase/migrations/119_workflow_phases_cancelled.sql</files>
  <read_first>
    - `supabase/migrations/115_workflow_phases_recorded_not_sent.sql` — **the whole file, header included (lines 1-122)**. It is the ONLY migration that has ever touched this constraint, and its header states the four rules 119 must copy and why each exists.
    - `.planning/phases/194-stop-a-running-workflow/194-PATTERNS.md` § 5 (the block to copy and the one line NOT to copy)
    - `supabase/migrations/063_dual_mode_continue.sql:50-57` and `057_workflow_runs.sql:19` — the `workflow_runs` CHECK, to confirm no migration is owed there
    - `backend/app/db/workflows.py` around `mark_phase_active` / `complete_phase` / `fail_phase` / `skip_phase` / `record_phase_not_sent` — the five shipped phase writers, so the header's pointer is written from measurement
  </read_first>
  <action>
Create `supabase/migrations/119_workflow_phases_cancelled.sql`, following migration 115's shape
**exactly**. Confirm the number first: `ls supabase/migrations/ | wc -l` and the highest existing
number (RESEARCH measured 112 files, highest 118 —
`118_connector_secret_column_privilege.sql`). ⚠ The filename must match `<digits>_name.sql`; a
letter suffix like `119b` is silently SKIPPED by the Supabase CLI.

**The DDL body**, copied from mig 115 `:110-122` with exactly one element added:
`BEGIN;` then `ALTER TABLE public.workflow_phases DROP CONSTRAINT IF EXISTS workflow_phases_status_check;`
then `ALTER TABLE public.workflow_phases ADD CONSTRAINT workflow_phases_status_check CHECK (status = ANY (ARRAY[...]))`
with **all six shipped literals re-added verbatim** — `'pending'::text`, `'active'::text`,
`'completed'::text`, `'failed'::text`, `'skipped'::text`, `'recorded_not_sent'::text` — plus
`'cancelled'::text`, then `COMMIT;`.

**The four rules, each with mig 115's own stated reason written into the header:**
1. `BEGIN` / `COMMIT` (WR-01, 2026-08-07) — without it, a dropped session between the two ALTERs
   leaves the table with **NO CHECK at all**: the closed vocabulary the migration exists to
   preserve, gone, **fail-open, and silently**.
2. `DROP CONSTRAINT IF EXISTS` — makes a re-paste safe (house style: migs 048, 063).
3. `= ANY (ARRAY[...])` with `::text` casts, **NOT `IN (...)`** — it is the form `pg_dump`
   regenerates, so the regenerated `full-schema.sql` diff stays roughly one line instead of a
   whole-constraint reformat.
4. **Re-add every shipped literal verbatim.** A re-typed `ARRAY[...]` is precisely where a shipped
   literal gets silently dropped, which orphans every existing row using it.

**The header must also carry**, each written from measurement rather than transcribed:
- **WHAT THE STATUS MEANS**, in mig 115's voice: `cancelled` is the phase that was RUNNING when the
  user stopped the run. It did not fail (nothing was wrong) and it was not skipped (it was never
  routed around) — none of the six shipped statuses is true of that outcome. This is D-04's
  rejection of `failed`/`skipped`, recorded in the schema rather than only in CONTEXT.
- **⚠ THE COLUMN STORES THE SLUG (D-17, inherited from mig 115).** `cancelled` is the constraint
  literal. The sentence a person reads is RENDERED by the client's vocabulary layer and appears in
  no query and no constraint.
- **⚠ COMPLETED PHASES ARE UNTOUCHED (D-07/D-13).** A stopped run keeps its completed phases; only
  the single `active` row moves. The migration admits a literal — it does not authorise a bulk
  rewrite.
- **The apply story (D-06), verbatim project rule:** paste into the Supabase SQL editor; **never**
  `db push` / `db reset`; then `bash scripts/regenerate-full-schema.sh` with **no** `--reset`;
  never hand-edit `full-schema.sql`. And explicitly: *"This plan ONLY AUTHORS the file — it is NOT
  applied here."*
- **The ACCESS EXCLUSIVE note:** the `DROP+ADD CONSTRAINT` takes a brief ACCESS EXCLUSIVE lock on
  `workflow_phases`; on cloud this belongs in the standing migration-parity window, and it must
  **NOT** be applied to the cloud database during Phase 194.
- **The phase writers pointer, RE-DERIVED not copied.** ⚠ Mig 115's header says the phase-status
  literals live in *"the four UPDATEs in `backend/app/db/workflows.py` (lines 965, 979, 1001,
  1013)"* — **measured at HEAD those lines are inside `count_foreign_runs_on_global` /
  `load_run_phases`; the pointer is stale by ~240 lines and there are FIVE writers, not four.**
  Write the corrected pointer by grepping for the writer function names at this commit, and record
  the correction **beside** the note that 115's pointer was stale — never silently.
  </action>
  <verify>
    <automated>test -f supabase/migrations/119_workflow_phases_cancelled.sql &amp;&amp; grep -q "BEGIN;" supabase/migrations/119_workflow_phases_cancelled.sql &amp;&amp; grep -q "COMMIT;" supabase/migrations/119_workflow_phases_cancelled.sql &amp;&amp; test $(grep -oE "'(pending|active|completed|failed|skipped|recorded_not_sent|cancelled)'::text" supabase/migrations/119_workflow_phases_cancelled.sql | sort -u | wc -l) -eq 7</automated>
  </verify>
  <acceptance_criteria>
    - The file is named `119_workflow_phases_cancelled.sql` and matches `^[0-9]+_[a-z0-9_]+\.sql$`;
      `ls supabase/migrations/ | grep -c "^119_"` returns 1 and no `119[a-z]` file exists.
    - The automated check above passes: **exactly 7 distinct `'<literal>'::text` values** appear,
      and all six shipped ones are present.
    - `grep -c "IN (" supabase/migrations/119_workflow_phases_cancelled.sql` on the DDL body returns
      0 — the constraint uses `= ANY (ARRAY[...])`.
    - `DROP CONSTRAINT IF EXISTS` and `ADD CONSTRAINT` both appear, both inside the single
      `BEGIN;` / `COMMIT;` pair.
    - `grep -cE "ALTER TABLE" supabase/migrations/119_workflow_phases_cancelled.sql` returns exactly
      2, and neither adds a table, column, index, grant or RLS policy
      (`grep -cE "CREATE (TABLE|INDEX|POLICY)|GRANT|ENABLE ROW LEVEL" ` returns 0).
    - The header states the SLUG rule, the completed-phases-untouched rule, the apply story, the
      ACCESS EXCLUSIVE / no-cloud note, and the sentence that this plan authors but does not apply.
    - The header's `db/workflows.py` writer pointer is derived at this commit and names **five**
      writers, with mig 115's stale four-line pointer recorded beside it as stale.
    - `git diff --numstat` shows changes to this file only.
  </acceptance_criteria>
  <done>Migration 119 exists, widens the CHECK 6 → 7 in the mig-115 shape, carries its reasons in its header, and has been applied nowhere.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author backend/tests/test_migration_119.py — the live-DB gate (V-14, V-15)</name>
  <files>backend/tests/test_migration_119.py</files>
  <behavior>
    - **V-14 (positive, LOOPED):** with migration 119 applied, an INSERT into `workflow_phases`
      succeeds for **each** of the seven literals, asserted **one literal per iteration**.
    - **V-15 (negative):** an INSERT of the display sentence `Run cancelled — no deliverable produced`
      raises `asyncpg.PostgresError` with `sqlstate == "23514"` and a constraint name containing
      `workflow_phases_status_check`.
    - **Skip 1:** local Postgres unreachable at `POSTGRES_DSN` → clean skip with the DSN in the reason.
    - **Skip 2:** migration 119 not yet applied → clean **green-skip** with a reason naming the
      operator apply step. Once applied, the file MUST pass.
    - Every write rides a transaction that ROLLS BACK; the deliberate failure rides a nested savepoint.
  </behavior>
  <read_first>
    - `backend/tests/test_migration_115.py` — **the whole file**. Copy its scaffold: the two clean skips, the `pg_constraint` applied-probe, the FK seed chain, the rollback transaction and the nested savepoint.
    - `.planning/phases/194-stop-a-running-workflow/194-PATTERNS.md` § 6 (the excerpts, and the ONE thing to change)
    - `.planning/phases/194-stop-a-running-workflow/194-RESEARCH.md` § *The fences* rows **F-7** and **F-8**
    - `supabase/migrations/119_workflow_phases_cancelled.sql` (Task 1's output — the literal list this test pins)
  </read_first>
  <action>
Create `backend/tests/test_migration_119.py` by copying `test_migration_115.py`'s scaffold whole.

**Keep verbatim:** the `_POSTGRES_TEST_DSN` default (`postgresql://postgres:postgres@127.0.0.1:54322/postgres`),
`_pg_reachable` / `_check_pg_available_sync` / `PG_AVAILABLE` / the module-level `pytestmark`
skipif; the FK seed chain `auth.users` → `threads` → `workflow_definitions` → `workflow_runs`
inside the rolled-back transaction (`org_id` is NOT NULL with **no FK**, so a synthetic uuid is
correct); the outer `tx.rollback()` in a `finally` so live dev data is NEVER mutated; the nested
`conn.transaction()` savepoint around the deliberate failure, rolled back so the seeded outer
transaction survives.

**The applied-probe reads `pg_constraint`, never an INSERT probe:** a `_migration_119_applied(conn)`
that runs `SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname = 'workflow_phases_status_check'`
and returns whether the definition contains the `cancelled` slug. The unapplied skip reason must
name the operator step explicitly — SQL-editor paste, then `regenerate-full-schema.sh` with no
`--reset`, never `db push` / `db reset` — and state that **once applied this file MUST pass**.

**⚠ The one thing to change from mig 115's test, and F-7 is right about why: KEEP THE PER-LITERAL
LOOP.** The shipped positive control iterates `for idx, status in enumerate(SHIPPED_STATUSES)` and
that loop is load-bearing: a single collapsed `assert all(...)` short-circuits and proves only the
first literal (the 193.2 clause-by-clause lesson — a pytest run proves only that the FIRST clause
fails, because `assert` short-circuits). 119's tuple becomes **seven**: the six shipped plus
`cancelled`. Each iteration must carry the literal in its assertion message so a failure NAMES the
literal that was dropped.

**F-8's negative control** uses this phase's own display sentence — `Run cancelled — no deliverable
produced` — mirroring 115's `NOT_SENT_DISPLAY_SENTENCE = "Not sent — recorded"`. Assert both
`exc.value.sqlstate == "23514"` **and** that `workflow_phases_status_check` appears in
`getattr(exc.value, "constraint_name", "") or str(exc.value)`. The sqlstate alone is not enough:
`23514` is every CHECK on the table, and the point of the assertion is that it is **this** one.

⚠ **The RED observation for F-7 and F-8 is NOT owed by this plan** — the gate green-skips until the
migration is applied, so it cannot be driven RED here. Plan **194-12** drives both plants against
the live DB inside a rolled-back DDL transaction and records the RED. State that in this file's
module docstring so nobody later mistakes the green-skip for a passing fence.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python.exe -m pytest tests/test_migration_119.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_migration_119.py -q` exits 0. Because migration 119 is not applied, the
      expected result is a **clean green-SKIP**, and the printed skip reason names the operator
      apply step. A FAIL or an ERROR is not acceptable; a skip for the wrong reason (e.g. an import
      error) is not acceptable.
    - The applied-probe queries `pg_constraint` / `pg_get_constraintdef` —
      `grep -c "pg_get_constraintdef" backend/tests/test_migration_119.py` ≥ 1 and the file contains
      no INSERT-based applied-probe.
    - The positive control is a **loop over seven literals**, not a collapsed assertion:
      `grep -cE "for .* in .*STATUSES" backend/tests/test_migration_119.py` ≥ 1, and
      `grep -c "assert all(" backend/tests/test_migration_119.py` returns 0.
    - The literal tuple has exactly 7 entries including `cancelled` and all six shipped ones.
    - The negative control asserts BOTH `23514` AND the constraint name — both
      `grep -c '23514'` ≥ 1 and `grep -c 'workflow_phases_status_check'` ≥ 1 inside the negative test.
    - Exactly two skip conditions exist (unreachable DSN, unapplied migration) —
      `grep -cE "pytest.skip|skipif"` corresponds to those two and no third.
    - Every write path is inside a transaction with a `finally: await tx.rollback()`.
    - The module docstring states that F-7/F-8's RED observations are owed by plan 194-12, not here.
    - Backend `pytest tests/unit -q` shows **no NEW failure by NAME** against `194-BASELINE.md`.
  </acceptance_criteria>
  <done>The live-DB gate exists, green-skips cleanly with the operator apply step named, loops per literal, and pins the negative control on both the SQLSTATE and the constraint name.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file → live Postgres schema | DDL that defines a closed vocabulary; a dropped literal orphans existing rows |
| test process → live local Postgres | The gate connects to the real dev database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-194-02-01 | Tampering | `workflow_phases_status_check` | mitigate | All six shipped literals re-added VERBATIM and pinned by a per-literal loop; a dropped literal orphans every existing row using it (mig 115's own stated trap). |
| T-194-02-02 | Denial of Service | `public.workflow_phases` | mitigate | Single `BEGIN`/`COMMIT` pair — a dropped session cannot leave the table with NO CHECK (fail-open). ACCESS EXCLUSIVE lock is brief and the migration is NOT applied to cloud in this phase. |
| T-194-02-03 | Tampering | live dev data | mitigate | Every test write rides a transaction rolled back in a `finally`; the deliberate failure rides a nested savepoint. No INSERT-based applied-probe. |
| T-194-02-04 | Information Disclosure | skip messages | accept | Skip reasons contain the local DSN with the well-known local dev credentials only; no secret is read or printed. |
| T-194-02-SC | Tampering | package installs | mitigate | None owed — this plan installs nothing. `asyncpg` and `pytest` already ship. |
</threat_model>

<verification>
- `ls supabase/migrations/ | grep "^119"` returns exactly one file.
- `pytest backend/tests/test_migration_119.py -q` exits 0 with a clean green-skip and the documented
  reason.
- `git status --porcelain` shows only the two files in `files_modified`.
- `supabase/full-schema.sql` is **unchanged** (`git diff --numstat -- supabase/full-schema.sql` is
  empty) — it is regenerated in 194-12, never here and never by hand.
</verification>

<success_criteria>
- Migration 119 authored in the mig-115 shape, 6 → 7 literals, all six shipped ones verbatim, with
  its reasons and its apply story in the header (V-14 / V-15 prerequisites satisfied).
- The live-DB gate exists, green-skips cleanly, loops per literal, and pins the negative control on
  the SQLSTATE **and** the constraint name.
- Nothing has been applied to any database.
</success_criteria>

<output>
Create `.planning/phases/194-stop-a-running-workflow/194-02-SUMMARY.md` when done.
</output>

