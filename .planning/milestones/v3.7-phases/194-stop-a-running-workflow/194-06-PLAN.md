---
phase: 194-stop-a-running-workflow
plan: 06
type: execute
wave: 4
depends_on: ["194-01", "194-02"]
files_modified:
  - backend/app/db/workflows.py
  - backend/tests/test_workflow_phase_cancel.py
autonomous: true
requirements: [RUN-01]
validation_rows: []
must_haves:
  truths:
    - "There is exactly one way to write 'cancelled' onto a workflow_phases row from a known phase id, and exactly one way to do it from a run id when no engine is present."
    - "Neither writer can touch a completed, failed, skipped or recorded_not_sent phase row."
    - "finish_run's docstring no longer claims it is narrower than it is."
  artifacts:
    - path: "backend/app/db/workflows.py"
      provides: "cancel_phase (PHASE-KEYED, for the engine arm) + cancel_active_phases (RUN-KEYED set-predicate, for the zombie/no-producer arm), plus the corrected finish_run docstring"
      contains: "cancel_active_phases"
    - path: "backend/tests/test_workflow_phase_cancel.py"
      provides: "Writer-level unit cover: the SQL predicate, the bind style, and the completed-untouched property"
      contains: "workflow_run_id"
  key_links:
    - from: "backend/app/db/workflows.py::cancel_active_phases"
      to: "public.workflow_phases"
      via: "a set-predicate UPDATE keyed on workflow_run_id AND status='active', $N binds only"
      pattern: "WHERE workflow_run_id = \\$1 AND status = 'active'"
    - from: "supabase/migrations/119_workflow_phases_cancelled.sql"
      to: "backend/app/db/workflows.py"
      via: "the sixth phase writer for the seventh CHECK literal"
      pattern: "cancelled"
---

<objective>
SC#3's data-access half. Migration 119 admits the literal; these two writers are the only things
that write it.

Purpose: `finish_run` writes the run row + the thread anchor and **nothing else**, so the in-flight
`workflow_phases` row on a stopped run stays `active` **forever**. Two homes are needed and the
reason is structural, not belt-and-braces: the **engine** cancel arm knows exactly WHICH phase was
interrupted (`phase_id` is bound in the same loop iteration), and the **zombie / no-producer** path
has no engine, no loop and no `phase_id` at all — there, the row must be FOUND.

Output: `cancel_phase` (PHASE-KEYED) and `cancel_active_phases` (RUN-KEYED), plus the corrected
`finish_run` docstring. **No caller is wired in this plan** — plans 194-09 and 194-10 call them.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/194-stop-a-running-workflow/194-PATTERNS.md
@.planning/phases/194-stop-a-running-workflow/194-RESEARCH.md
@.planning/phases/194-stop-a-running-workflow/194-CONTEXT.md
@.planning/phases/194-stop-a-running-workflow/194-BASELINE.md
@supabase/migrations/119_workflow_phases_cancelled.sql
@CLAUDE.md

**Base commit — ASSERT FIRST.** Dispatched against `d2a3b51b` on `develop`. In a worktree, run
`bash scripts/bootstrap-worktree.sh "$(pwd)"` FIRST, then assert `d2a3b51b` is the base ancestor.
Tear down only with `bash scripts/teardown-worktree.sh <path>`; never `rm -rf`.

**Serialization:** this plan's tests mock `asyncpg.Pool` and seed NOTHING in Postgres, so it is
parallel-safe under CLAUDE.md rule 4. ⚠ If any test in this plan ends up seeding `workflow_runs` or
`workflow_phases` against the live DB, it becomes SERIALIZED — do not write such a test here; the
live-DB gate is `test_migration_119.py` (plan 194-02) and it runs in the serialized wave.

<interfaces>
`backend/app/db/workflows.py` — the five shipped phase writers, all PHASE-KEYED (`WHERE id=$1`):
`mark_phase_active` `:1202` · `complete_phase` `:1213` · `fail_phase` `:1228` · `skip_phase` `:1250` ·
`record_phase_not_sent` `:1261`. **This plan adds the sixth and a run-keyed sibling.**

`record_phase_not_sent(pool: asyncpg.Pool, phase_id: UUID, output: dict) -> None` is the newest and
the closest analog (Phase 189, the same phase that wrote migration 115). Its docstring carries the
sections to copy: **WHAT THIS STATUS MEANS**, **⚠ THE COLUMN STORES THE SLUG (D-17)**, and the
`PHASE-KEYED write → WHERE id=$1` note.

`get_active_phase(pool: asyncpg.Pool, run_id: UUID) -> dict | None` `:1088-1105` — the read whose
predicate the run-keyed writer applies as a WRITE:
`WHERE workflow_run_id = $1 AND status = 'active' ORDER BY phase_index LIMIT 1`. ⚠ Its own docstring
records the trap: **RUN-KEYED on `workflow_run_id`, NOT `run_id` — that column does not exist on
`workflow_phases` and a bare `wp.run_id` raises Postgres 42703.**

`finish_run(pool: asyncpg.Pool, run_id: UUID, status: str) -> None` `:1308-1337` — one transaction:
`UPDATE workflow_runs SET status = $2 WHERE id = $1` then
`UPDATE threads SET active_workflow_run_id = NULL WHERE active_workflow_run_id = $1`. Idempotent;
the anchor clear finds 0 rows on a re-run.

`harness_engine.py:1646-1651`, the `fail_run` arm — the file's own shipped "terminalize the phase,
then the run" composition, carrying D-07 verbatim: *"Completed phases' outputs are ALREADY durable —
`finish_run` does NOT touch them (D-07). The run flips to `failed`; nothing silently dropped."*
</interfaces>
</context>

<non_goals>
- ⛔ **Do NOT put the phase write inside `finish_run`.** `finish_run` is called on the `completed`
  path (`harness_engine.py`'s success arm) and by `delete_workflow_cascade`; a phase write there
  changes behaviour on paths this phase must not touch.
- ⛔ **Do NOT write a second `workflow_runs` status writer** (D-10). `finish_run` already pairs the
  status write and the anchor clear in ONE transaction, is idempotent, and is already called from a
  cancel context by `delete_workflow_cascade`.
- ⛔ **Do NOT terminalize phases in bulk.** D-07/D-13 are inherited verbatim: completed phases'
  outputs are already durable and are LEFT UNTOUCHED. Only rows currently `active` move.
- ⛔ **Do NOT change any of the five shipped phase writers**, their SQL, their docstrings or their
  key style.
- ⛔ **Do NOT wire a caller here.** No import of these functions is added to `run_lifecycle.py`,
  `harness_engine.py`, `api/runs.py` or `run_producer.py` in this plan.
- ⛔ No f-string may reach any SQL value. `$N` binds only (T-152-05-05, T-091-03).
- ⛔ No new package.
</non_goals>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cancel_phase (PHASE-KEYED) and cancel_active_phases (RUN-KEYED set-predicate)</name>
  <files>backend/app/db/workflows.py, backend/tests/test_workflow_phase_cancel.py</files>
  <behavior>
    - `cancel_phase(pool, phase_id)` issues ONE `UPDATE workflow_phases` setting `status='cancelled'`
      and `updated_at=now()`, keyed `WHERE id = $1`, with `phase_id` passed as a bind.
    - `cancel_active_phases(pool, workflow_run_id)` issues ONE `UPDATE workflow_phases` setting
      `status='cancelled'` and `updated_at=now()`, keyed `WHERE workflow_run_id = $1 AND status = 'active'`,
      with the run id passed as a bind.
    - Neither writer's SQL contains the substrings `failed`, `skipped`, `completed` or
      `recorded_not_sent` as a written VALUE.
    - `cancel_active_phases` is correct for 0, 1 or N matching rows — it needs no prior read and
      raises on none of the three.
    - Neither function contains an f-string, `%` format or `.format(` reaching the SQL.
  </behavior>
  <read_first>
    - `backend/app/db/workflows.py:1080-1300` — `get_active_phase` and all five shipped phase writers, with their docstrings
    - `backend/app/db/workflows.py:1300-1340` — `finish_run`
    - `.planning/phases/194-stop-a-running-workflow/194-PATTERNS.md` § 3 — the analog, its excerpts, and **THE ONE DELIBERATE DEPARTURE** (the zombie path is RUN-KEYED because it has no `phase_id`)
    - `.planning/phases/194-stop-a-running-workflow/194-RESEARCH.md` § *G-C: the interrupted phase row — where the terminalize belongs* (the A/B/C table and the "A *and* B, structural not belt-and-braces" recommendation), and § *Is there exactly one `active` phase row per run?*
    - `supabase/migrations/119_workflow_phases_cancelled.sql` — the exact slug
    - `backend/tests/test_062_cancel_run.py:60-79` — `_chainable` and the mock helpers, for house style
  </read_first>
  <action>
Add two functions to `backend/app/db/workflows.py`, placed with the other phase writers, and add
both to `__all__` if that module maintains one.

**(a) `cancel_phase(pool: asyncpg.Pool, phase_id: UUID) -> None`** — the sixth PHASE-KEYED writer,
shaped on `record_phase_not_sent`. One `UPDATE`, `WHERE id = $1`, setting `status='cancelled'` and
`updated_at=now()`. It is for the **engine** cancel arm, which is the only home that knows WHICH
phase the user interrupted without a query, and the only home that can distinguish *"the phase the
user interrupted"* from *"some phase row that happens to be `active`."*

**(b) `cancel_active_phases(pool: asyncpg.Pool, workflow_run_id: UUID) -> None`** — the RUN-KEYED
sibling, for the zombie / no-producer path where there is **no engine, no loop and no `phase_id`**.
Apply `get_active_phase`'s predicate as a WRITE: `WHERE workflow_run_id = $1 AND status = 'active'`.
⚠ **RUN-KEYED means `workflow_run_id`** — a bare `run_id` column does not exist on
`workflow_phases` and would raise Postgres **42703**; `get_active_phase`'s own docstring records
that trap. ⚠ **Write it as a SET-PREDICATE, not as a read-then-update-by-id.** RESEARCH measured
that exactly one `active` row per run is *typical, not guaranteed* (3 runs have exactly 1, 0 runs
have more; `get_active_phase` itself hedges with `ORDER BY phase_index LIMIT 1`), so a predicate
UPDATE is correct for 0, 1 or N and needs no read.

**Docstrings, in `record_phase_not_sent`'s voice**, each section for the reason 189 gives:
- **WHAT THIS STATUS MEANS** — the phase that was RUNNING when the user stopped the run. It did not
  `fail` (nothing went wrong), was not `skipped` (it was never routed around) and is not `completed`.
  Reusing `failed` or `skipped` was OFFERED AND REJECTED (D-04) in a phase whose entire requirement
  is honesty.
- **⚠ THE COLUMN STORES THE SLUG (D-17).** `cancelled` is the literal in
  `workflow_phases_status_check` (migration 119). The sentence a person reads is RENDERED by the
  client's vocabulary layer (plan 194-04) and appears in no query and no constraint.
- **⚠ COMPLETED PHASES ARE UNTOUCHED (D-07/D-13, inherited verbatim).** State that `cancel_active_phases`'
  `status = 'active'` clause is the mechanism, not a convention — and that widening it to a set
  would be a bulk terminalize, which D-07 forbids.
- The key style: `PHASE-KEYED write → WHERE id=$1` for (a); `RUN-KEYED write → WHERE workflow_run_id=$1`
  for (b), with the 42703 trap named.
- ⚠ **Where each is called from, and why there are two.** A docstring that does not say which arm it
  serves invites the next reader to collapse them.

Create `backend/tests/test_workflow_phase_cancel.py` with the `<behavior>` cases, mocking the pool
the way `test_062_cancel_run.py` does. Assert on the **composed SQL string and the bind arguments**
handed to the mock — not on a live database.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python.exe -m pytest tests/test_workflow_phase_cancel.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_workflow_phase_cancel.py -q` passes.
    - `grep -c "WHERE workflow_run_id = \$1 AND status = 'active'" backend/app/db/workflows.py`
      returns ≥ 1, and the string `wp.run_id` / a bare `run_id = $1` predicate against
      `workflow_phases` appears **nowhere** in the new code.
    - Both functions issue exactly ONE `UPDATE` each (asserted on the mock's call count).
    - No f-string, `%`-format or `.format(` reaches SQL in either function — asserted by a source
      check over the two function bodies.
    - The five shipped phase writers are byte-unchanged: `git diff -U0 -- backend/app/db/workflows.py`
      contains no hunk inside `mark_phase_active`, `complete_phase`, `fail_phase`, `skip_phase` or
      `record_phase_not_sent`.
    - The slug written by both functions is byte-identical to the new literal in
      `supabase/migrations/119_workflow_phases_cancelled.sql` — asserted by comparing greps of the
      two files, never by eye.
    - `pytest backend/tests/unit -q` shows **no NEW failure BY NAME** against `194-BASELINE.md`.
    - **G-5 statement (this file fires hardest but one — 17 phases):** report the measured reason it
      is honoured BY CONSTRUCTION, as a test rather than an argument — the file already owns FIVE
      phase writers, and a sixth plus its run-keyed sibling is the SAME concern. Report the complete
      non-comment diff (`git diff -U0 -- backend/app/db/workflows.py | grep '^[-+]' | grep -v '^[-+][-+]' | grep -vE "^[-+]\s*#"`)
      and state the insertions/deletions split. **If the diff shows a second concern entering this
      file, STOP: the G-5 refactor recommendation is owed FIRST.**
  </acceptance_criteria>
  <done>Two writers exist — one phase-keyed for the engine, one run-keyed set-predicate for the engineless path — with the SLUG rule, the completed-untouched rule and the 42703 trap in their docstrings, and no shipped writer moved.</done>
</task>

<task type="auto">
  <name>Task 2: Correct finish_run's docstring BESIDE, not over</name>
  <files>backend/app/db/workflows.py</files>
  <read_first>
    - `backend/app/db/workflows.py:1308-1337` — `finish_run`, whose first docstring line reads *"Terminal run status write (``completed`` / ``failed``) + lock-clear (SC#2)"*
    - `backend/app/services/run_producer.py:262-277` — the F2 caller, which has passed `"cancelled"` since v2.8
    - `backend/app/api/workflows.py:1494-1518` — `delete_workflow_cascade`, which also passes `"cancelled"`
    - `supabase/migrations/057_workflow_runs.sql:19` and `063_dual_mode_continue.sql:50-57` — the `workflow_runs` CHECK, which has admitted `cancelled` since mig 057
    - `frontend/src/components/panel/WorkspacePanel.tsx:166-181` — the shipped `⚠ CORRECTED (CR-03)` SHAPE for a correction recorded beside its original
    - `backend/app/services/harness/publish_service.py` — the `SUPERSEDED` marker habit
  </read_first>
  <action>
`finish_run`'s docstring is **narrower than the function**: it says the status is `completed` or
`failed`, while the F2 caller has passed `"cancelled"` since v2.8 and `delete_workflow_cascade`
passes it too, and the `workflow_runs` CHECK has admitted it since migration 057. A reader trusting
that first line concludes `cancelled` is unsupported — which is exactly the wrong conclusion for
every caller this phase adds.

**Correct it BESIDE, not over** (CONTEXT's explicit instruction and this project's standing habit):
keep the original sentence visible under a `⚠ CORRECTED (Phase 194)` marker, state that it was
narrower than the function, name the two callers with their file:line and the migration that
admitted the literal, and state the corrected contract — a terminal run status write for
`completed` / `failed` / `cancelled`, plus the lock clear, in ONE transaction, idempotent on a
re-run.

⚠ **Also record, in the same block, the RACE that RESEARCH named rather than guarded:** a Stop
landing on worker A (Step 3b → `finish_run`) while worker B's producer is mid-F2 can interleave two
identical `UPDATE … SET status='cancelled'` writes. Both write the SAME VALUE, row-level locking
serialises them, and the anchor clear is idempotent — **benign by value-identity, not by
exclusion.** State the one thing a caller must never do: **make the two writes disagree** (e.g. one
passing `'failed'`). That sentence is the whole guard, and it belongs where a caller will read it.

⚠ **Write any grep-able sentence on ONE line** — 193.2-08 measured a rule written WRAPPED across two
lines failing its own literal `grep -q` and reading as *"already fixed"*.

This is a COMMENT-ONLY edit. `finish_run`'s body does not change.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python.exe -m pytest tests/test_062_cancel_run.py tests/test_cancel_run.py tests/test_run_lifecycle.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `git diff -U0 -- backend/app/db/workflows.py` shows **no non-comment, non-docstring change
      inside `finish_run`** — the two `con.execute(` lines and the `async with` block are byte-identical.
    - The original first docstring line survives verbatim inside the corrected block —
      `grep -c 'Terminal run status write' backend/app/db/workflows.py` is ≥ 1.
    - The corrected block names both callers with file:line, and names migration 057.
    - The interleaving race is stated with the words that make it safe (**same value**, row-level
      locking, idempotent anchor clear) AND with the prohibition (**the two writes must never
      disagree**), on one line each for the grep-able sentences.
    - The three cancel-path suites pass, unchanged.
  </acceptance_criteria>
  <done>`finish_run`'s docstring states its true contract with the original preserved beside it, and the cross-worker interleaving race is documented where a caller will read it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API/service layer → `public.workflow_phases` | ⚠ The workflow cluster reads through a **service-role pool that BYPASSES RLS**, so a query's `WHERE` clause **is** the access boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-194-06-01 | Tampering | `cancel_active_phases` | mitigate | `$N` binds only — never an f-string on any value (T-152-05-05, T-091-03). Asserted by a source check over the function body. |
| T-194-06-02 | Tampering | completed phase rows | mitigate | The `AND status = 'active'` clause is the mechanism that leaves completed / failed / skipped / recorded_not_sent rows untouched (D-07). Fenced at the call site by V-18 in plan 194-09, driven RED against a widened predicate. |
| T-194-06-03 | Elevation of Privilege | cross-run phase writes | mitigate | Both writers are keyed (`id` / `workflow_run_id`) and take no user-supplied filter. Ownership is enforced by the CALLER — plan 194-10's owner-scoped, anchor-confirmed route — and both writers document that they perform no ownership check, in the style `_cancel_run_internals` already uses (T-147-06). |
| T-194-06-04 | Repudiation | the interrupted phase row | mitigate | `updated_at=now()` is written with the status so the moment of the stop is recorded, matching the five shipped writers. |
| T-194-06-SC | Tampering | pip installs | mitigate | None owed — no package is added. |
</threat_model>

<verification>
- `pytest tests/test_workflow_phase_cancel.py tests/test_062_cancel_run.py tests/test_cancel_run.py tests/test_run_lifecycle.py -q` passes.
- `pytest backend/tests/unit -q` — no NEW failure BY NAME against `194-BASELINE.md`.
- `git diff --numstat` touches only the two files in `files_modified`.
- No caller was wired: `grep -rn "cancel_active_phases\|cancel_phase(" backend/app --include=*.py`
  returns hits in `db/workflows.py` only.
- **G-5 on `backend/app/db/workflows.py` (17 phases — the second-hottest backend file):** the
  measured non-comment diff is reported, and it shows one concern (a sixth phase writer and its
  run-keyed sibling), not a second.
</verification>

<success_criteria>
- The only two ways to write `cancelled` onto a `workflow_phases` row exist, are keyed correctly,
  and cannot touch a non-`active` row.
- `finish_run`'s docstring tells the truth, with its original beside it, and names the benign
  cross-worker interleave plus the one thing that would make it unsafe.
- No shipped writer, no caller and no behaviour moved.
</success_criteria>

<output>
Create `.planning/phases/194-stop-a-running-workflow/194-06-SUMMARY.md` when done.
</output>
