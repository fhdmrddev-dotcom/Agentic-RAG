---
phase: 194-stop-a-running-workflow
plan: 12
type: execute
wave: 7
depends_on: ["194-02", "194-06", "194-09", "194-10", "194-11"]
files_modified:
  - supabase/full-schema.sql
  - .planning/phases/194-stop-a-running-workflow/194-MIGRATION-RECEIPT.md
autonomous: false
requirements: [RUN-01]
validation_rows: [V-14, V-15]
must_haves:
  truths:
    - "workflow_phases_status_check admits seven literals on the live local database, and the six shipped ones survived the re-type."
    - "A display sentence is still refused by the constraint — the column stores the SLUG."
    - "The regenerated full-schema.sql reflects the applied constraint and was not hand-edited."
  artifacts:
    - path: ".planning/phases/194-stop-a-running-workflow/194-MIGRATION-RECEIPT.md"
      provides: "The applied constraint definition read from pg_constraint before and after, and both fence RED observations"
      contains: "pg_get_constraintdef"
    - path: "supabase/full-schema.sql"
      provides: "The regenerated deploy artifact"
      contains: "workflow_phases_status_check"
  key_links:
    - from: "supabase/migrations/119_workflow_phases_cancelled.sql"
      to: "the live local Postgres"
      via: "an operator paste into the Supabase SQL editor — never db push / db reset"
      pattern: "workflow_phases_status_check"
---

<objective>
Apply migration 119 to the live local database, prove the constraint on the real DB, and regenerate
the single-file deploy artifact.

⚠ **SERIALIZED — this plan runs ALONE in its wave.** CLAUDE.md § *Parallel execution* rule 4:
*"Serialize any plan whose tests MUTATE the local database. Worktrees isolate files, not Postgres."*
Its `DROP+ADD CONSTRAINT` takes a brief **ACCESS EXCLUSIVE** lock on `workflow_phases`, so any
concurrent suite writing that table will block. No other plan may be dispatched while this one runs.

Output: the applied constraint, V-14 and V-15 green on the live DB with F-7 and F-8 driven RED, a
regenerated `full-schema.sql`, and a committed receipt.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/194-stop-a-running-workflow/194-PATTERNS.md
@.planning/phases/194-stop-a-running-workflow/194-RESEARCH.md
@.planning/phases/194-stop-a-running-workflow/194-CONTEXT.md
@supabase/migrations/119_workflow_phases_cancelled.sql
@backend/tests/test_migration_119.py
@supabase/SETUP.md
@CLAUDE.md

**Base commit — ASSERT FIRST.** In a worktree, run `bash scripts/bootstrap-worktree.sh "$(pwd)"`
FIRST. Tear down only with `bash scripts/teardown-worktree.sh <path>`; never `rm -rf`.

⚠ **If local Postgres on `127.0.0.1:54322` refuses connections, read CLAUDE.md § *THE WINDOWS
PORT-RESERVATION TRAP* before touching Docker.** The symptom is a container reporting `Up (healthy)`
whose port accepts nothing. Diagnose in ONE step with
`netsh int ipv4 show excludedportrange protocol=tcp` and look for an UNMARKED range straddling
54322 — ⚠ **the trailing `*` is load-bearing: a marked range is our own administered exclusion and
is NOT the fault.** Bring infra up + verify with
`powershell -ExecutionPolicy Bypass -File scripts/start-local-infra.ps1`.
</context>

<non_goals>
- ⛔ **NEVER `supabase db push` or `supabase db reset`.** Both destroy or rebuild local dev data.
  D-06 is a project rule, not a preference: paste into the Supabase SQL editor.
- ⛔ **NEVER hand-edit `supabase/full-schema.sql`.** It is regenerated by the script, always.
- ⛔ **Do NOT pass `--reset` to `scripts/regenerate-full-schema.sh`.** `--reset` wipes the local DB;
  it is for CI / release verification only.
- ⛔ **Do NOT apply this migration to the CLOUD database.** It belongs in the standing
  migration-parity window (mig 115's own precedent). Live deploys are operator-triggered.
- ⛔ **Do NOT heal any data row here.** The four-row heal is plan 194-13, in its own serialized wave.
- ⛔ Do not modify the migration file. If it is wrong, stop and report — a migration edited after a
  partial apply is how a constraint ends up in a state no file describes.
</non_goals>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: OPERATOR — paste migration 119 into the Supabase SQL editor</name>
  <files>supabase/migrations/119_workflow_phases_cancelled.sql</files>
  <read_first>
    - `supabase/migrations/119_workflow_phases_cancelled.sql` — the whole file, header included
    - `supabase/SETUP.md` — the migration application story
    - `CLAUDE.md` § *Local dev infrastructure* — the SQL-editor rule and the port-reservation trap
  </read_first>
  <what-built>
Plan 194-02 authored `supabase/migrations/119_workflow_phases_cancelled.sql`, which widens
`workflow_phases_status_check` from six literals to seven by ADDING `cancelled`, re-adding all six
shipped literals verbatim, inside a single `BEGIN` / `COMMIT` pair with `DROP CONSTRAINT IF EXISTS`.
Nothing has been applied. `backend/tests/test_migration_119.py` currently GREEN-SKIPS with a reason
naming this step.
  </what-built>
  <how-to-verify>
1. **Capture the BEFORE state first**, so the receipt has a real before column. Run against the
   local DB (`postgresql://postgres:postgres@127.0.0.1:54322/postgres`):
   `SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname = 'workflow_phases_status_check';`
   and `SELECT status, count(*) FROM workflow_phases GROUP BY status ORDER BY status;`
   Paste both outputs back.
2. Open the local Supabase Studio SQL editor (`http://127.0.0.1:54323`).
3. Paste the **entire contents** of `supabase/migrations/119_workflow_phases_cancelled.sql` and run
   it. ⚠ **Do not run `supabase db push` or `supabase db reset`.**
4. Re-run the two queries from step 1 and paste both outputs back.
   - The constraint definition must now list **seven** literals, including all six from the before
     column plus `cancelled`.
   - The per-status counts must be **UNCHANGED** — a widening ALTER touches no row, and a changed
     count means something other than this migration ran.
  </how-to-verify>
  <action>
Do not apply anything yourself. Present the migration file's contents and the two BEFORE queries to
the operator, wait for the pasted BEFORE and AFTER outputs, then record all four outputs verbatim in
`194-MIGRATION-RECEIPT.md` under an `## Apply` section, with the date and the local DSN. Compare the
AFTER definition against the BEFORE **literal by literal**, not by counting, and state the result.
If any shipped literal is missing from the AFTER definition, or if a per-status row count moved,
STOP and report — do not proceed to Task 2.
  </action>
  <acceptance_criteria>
    - The BEFORE and AFTER `pg_get_constraintdef` outputs are both pasted, verbatim.
    - The AFTER definition contains all six shipped literals **and** `cancelled` — verified literal
      by literal against the before column, not by counting.
    - The `workflow_phases` per-status row counts are byte-identical before and after.
    - The operator confirms neither `db push` nor `db reset` was run.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "pg_get_constraintdef" .planning/phases/194-stop-a-running-workflow/194-MIGRATION-RECEIPT.md &amp;&amp; grep -q "cancelled" .planning/phases/194-stop-a-running-workflow/194-MIGRATION-RECEIPT.md</automated>
  </verify>
  <done>`workflow_phases_status_check` admits seven literals on the live local database, all six shipped ones verified individually, with no `workflow_phases` row moved and the before/after recorded.</done>
  <resume-signal>Paste the BEFORE and AFTER constraint definitions and both status-count outputs, then type "applied".</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: V-14 and V-15 on the live DB, and drive F-7 and F-8 RED inside a rolled-back DDL transaction</name>
  <files>.planning/phases/194-stop-a-running-workflow/194-MIGRATION-RECEIPT.md</files>
  <behavior>
    - `pytest backend/tests/test_migration_119.py -x` now **PASSES** — it must no longer skip.
    - **V-14:** each of the seven literals is accepted, asserted **one per loop iteration**.
    - **V-15:** the display sentence `Run cancelled — no deliverable produced` is refused with
      SQLSTATE `23514` and a constraint name containing `workflow_phases_status_check`.
    - Every write rides a transaction that rolls back; live dev data is unchanged afterwards.
  </behavior>
  <read_first>
    - `backend/tests/test_migration_119.py` — the whole file, especially the `pg_constraint` applied-probe and the two skip reasons
    - `backend/tests/test_migration_115.py` — the scaffold it copies, and its nested-savepoint shape
    - `.planning/phases/194-stop-a-running-workflow/194-RESEARCH.md` § *The fences* rows **F-7** and **F-8**
    - `CLAUDE.md` § *Parallel execution* rule 4
  </read_first>
  <action>
Run `cd backend && venv/Scripts/python.exe -m pytest tests/test_migration_119.py -x -q`. It must
**PASS**. A skip here means the migration did not apply and Task 1's verification was wrong — stop
and report rather than proceeding.

**⚠ NOW DRIVE F-7 AND F-8 RED. A fence is only real once you have watched it FAIL, and a green-skip
is not a pass.** Both plants are DDL, so both are driven **inside a transaction that is rolled
back** — Postgres DDL is transactional, so this is safe and leaves the database byte-unchanged:

- **F-7 (positive control):** in a single connection, `BEGIN`, drop the constraint and re-add it
  **omitting `'skipped'::text`**, then run V-14's per-literal assertion body against that connection
  → the `skipped` iteration must FAIL, **naming that literal specifically** → `ROLLBACK`. ⚠ **The
  per-literal loop is what makes this observable**: a single collapsed `assert all(...)`
  short-circuits and proves only the first literal (the 193.2 clause-by-clause lesson). If the
  failure message does not name `skipped`, the loop is not per-literal — fix the test, then re-drive.
- **F-8 (negative control):** confirm the display-sentence INSERT raises `23514`. Then, in a rolled-
  back transaction, DROP the constraint entirely and re-run the same INSERT → it must now SUCCEED,
  proving the assertion is testing the constraint and not something incidental → `ROLLBACK`.
  ⚠ Assert on **both** the SQLSTATE and the constraint NAME: `23514` is every CHECK on the table,
  and the point of the assertion is that it is **this** one.

**After every plant**, re-read `pg_get_constraintdef` and confirm it is byte-identical to Task 1's
AFTER definition, and re-run the per-status counts. **Record that confirmation** — a rolled-back
DDL transaction is safe by design, and the receipt is what proves it was actually rolled back.

Write `194-MIGRATION-RECEIPT.md` containing: Task 1's before/after constraint definitions and status
counts; the passing `pytest` output; both RED observations with their exact failure messages; and
the post-plant constraint definition proving the rollback.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python.exe -m pytest tests/test_migration_119.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_migration_119.py -x -q` **passes** — zero skips, zero failures.
    - **Two RED observations recorded** with their exact failure output: F-7's must NAME the
      `skipped` literal; F-8's must show the INSERT succeeding once the constraint is dropped.
    - After both plants, `pg_get_constraintdef` is byte-identical to Task 1's AFTER definition, and
      the per-status counts are unchanged — both re-read and recorded.
    - `194-MIGRATION-RECEIPT.md` contains all five evidence blocks listed above.
    - `pytest backend/tests/unit -q` — no NEW failure BY NAME against `194-BASELINE.md`.
  </acceptance_criteria>
  <done>The seven-literal constraint is proved on the live database, both fences have been watched to FAIL against real DDL plants, and the database is provably unchanged by the plants.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Regenerate full-schema.sql with NO --reset, and confirm the diff is the constraint alone</name>
  <files>supabase/full-schema.sql</files>
  <read_first>
    - `scripts/regenerate-full-schema.sh` — its default behaviour and what `--reset` does
    - `supabase/SETUP.md`
    - `CLAUDE.md` § *Stack* — the migration rules paragraph
    - `supabase/migrations/115_workflow_phases_recorded_not_sent.sql` header — the `= ANY (ARRAY[…])` rule and its stated reason: it matches the form `pg_dump` regenerates, so the diff stays ~one line
  </read_first>
  <what-built>
Migration 119 is applied and proved. `supabase/full-schema.sql` — the single-file deploy artifact
for greenfield environments — is now stale.
  </what-built>
  <how-to-verify>
1. Run `bash scripts/regenerate-full-schema.sh` — **with NO `--reset`**. The default dumps the live
   DB schema without a reset, preserving dev data. ⚠ `--reset` is destructive and is for CI /
   release verification only.
2. Inspect `git diff -- supabase/full-schema.sql`.
3. Confirm the diff is **the `workflow_phases_status_check` definition and essentially nothing
   else**. That is the whole reason migration 119 uses `= ANY (ARRAY[…])` with `::text` casts
   instead of `IN (…)`: it matches the form `pg_dump` regenerates. **A sprawling diff means either
   the migration used the wrong form, or something else changed the live schema — stop and report
   in either case.**
4. Paste the diff stat and the changed constraint hunk back.
  </how-to-verify>
  <action>
Do not run the regeneration script without the operator's confirmation that no `--reset` is wanted;
that flag is destructive. Once it has run, record the `git diff --numstat`, the changed constraint
hunk and the `scripts/check-deploy-drift.sh` result in `194-MIGRATION-RECEIPT.md` under a
`## Regeneration` section. Account for **every** changed hunk — if any hunk is not explained by the
constraint widening, STOP and report rather than committing a schema artifact nobody can account for.
  </action>
  <acceptance_criteria>
    - The script was run with **no** `--reset` — confirmed by the operator and by the local
      `workflow_phases` row counts being unchanged from Task 1's AFTER capture.
    - `git diff --numstat -- supabase/full-schema.sql` is small, and every changed hunk is
      accounted for; the `workflow_phases_status_check` hunk lists seven literals.
    - `supabase/full-schema.sql` was **not** hand-edited — the only writer is the script.
    - The receipt records the diff stat and the constraint hunk.
    - ⚠ **Deployment-artifact parity is checked, not assumed:** run
      `bash scripts/check-deploy-drift.sh` and record its result. No env var, bundled service or
      sandbox tag changed in this phase, so it should be unaffected — record the confirmation rather
      than the expectation.
  </acceptance_criteria>
  <verify>
    <automated>git diff --numstat -- supabase/full-schema.sql &amp;&amp; grep -q "workflow_phases_status_check" supabase/full-schema.sql</automated>
  </verify>
  <done>`supabase/full-schema.sql` is regenerated from the live schema with no `--reset`, its diff is accounted for hunk by hunk, and deploy-artifact drift is checked rather than assumed.</done>
  <resume-signal>Paste the `git diff --numstat` for `supabase/full-schema.sql`, the changed constraint hunk, and the `check-deploy-drift.sh` result, then type "regenerated".</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator SQL editor → live local Postgres | DDL applied by hand to a database holding real dev data |
| regenerated artifact → future greenfield environments | `full-schema.sql` is the single-file deploy artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-194-12-01 | Tampering | live dev data | mitigate | SQL-editor paste only — **never** `db push` / `db reset`; the regeneration runs with **no** `--reset`. Per-status row counts captured before and after and asserted unchanged. |
| T-194-12-02 | Denial of Service | `public.workflow_phases` | mitigate | The `DROP+ADD CONSTRAINT` takes a brief ACCESS EXCLUSIVE lock; this plan runs ALONE in its wave (rule 4) so no concurrent suite blocks on it. |
| T-194-12-03 | Tampering | the closed status vocabulary | mitigate | The single `BEGIN`/`COMMIT` pair means a dropped session cannot leave the table with NO CHECK — fail-open, silently (WR-01, 2026-08-07). F-7 proves all seven literals survive, per literal. |
| T-194-12-04 | Tampering | `supabase/full-schema.sql` | mitigate | Regenerated by the script only, never hand-edited; the diff is inspected and must be confined to the constraint. |
| T-194-12-05 | Tampering | the CLOUD database | accept | Explicitly out of scope — the migration joins the standing parity window; live deploys are operator-triggered and this phase does not push to `master` or `production`. |
| T-194-12-SC | Tampering | pip installs | mitigate | None owed — no package is added. |
</threat_model>

<verification>
- `pytest backend/tests/test_migration_119.py -x -q` passes on the live DB.
- `pg_get_constraintdef` lists seven literals, and the six shipped ones are verified individually.
- `supabase/full-schema.sql` regenerated with no `--reset`; the diff is confined to the constraint.
- `194-MIGRATION-RECEIPT.md` is committed with every evidence block.
- `pytest backend/tests/unit -q` — no NEW failure BY NAME against `194-BASELINE.md`.
</verification>

<success_criteria>
- V-14 and V-15 green on the live local database, with F-7 and F-8 each watched to FAIL against a
  rolled-back DDL plant.
- The deploy artifact is regenerated correctly and drift-checked.
- The database is provably unchanged apart from the one constraint.
</success_criteria>

<output>
Create `.planning/phases/194-stop-a-running-workflow/194-12-SUMMARY.md` when done.
</output>
