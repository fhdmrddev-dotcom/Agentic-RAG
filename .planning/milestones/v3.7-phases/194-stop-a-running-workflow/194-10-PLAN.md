---
phase: 194-stop-a-running-workflow
plan: 10
type: execute
wave: 6
depends_on: ["194-01", "194-06"]
files_modified:
  - backend/app/services/harness_engine.py
  - backend/tests/test_harness_engine.py
autonomous: true
requirements: [RUN-01]
validation_rows: [V-16]
must_haves:
  truths:
    - "When a live producer is cancelled mid-phase, the phase the user actually interrupted is the one marked cancelled — identified without a query."
    - "The 096-09 graceful-shutdown gate on the ask_user expiry is unmoved, and the CancelledError still re-raises last."
    - "Completed phases are untouched on the engine cancel arm, exactly as they are on the fail_run arm."
  artifacts:
    - path: "backend/app/services/harness_engine.py"
      provides: "The in-flight phase terminalize inside the shielded cancel/escape cleanup, before the re-raise"
      contains: "cancel_phase"
  key_links:
    - from: "backend/app/services/harness_engine.py cancel arm"
      to: "backend/app/db/workflows.py::cancel_phase"
      via: "the phase_id already bound in the same loop iteration — no query"
      pattern: "cancel_phase\\(pool, phase_id"
---

<objective>
SC#3's happy-path half. When the producer task is alive, `DELETE /runs/{id}` takes Step 3a
(`task.cancel()`), the producer raises `CancelledError`, and the **engine's cancel arm** is the only
place in the system that knows **WHICH** phase the user interrupted — `phase_id` is bound at
`:1572`, inside the same `while` iteration, before the `try` — without asking the database.

Purpose: RESEARCH weighed three homes against four criteria and recommended **A *and* B, for a
structural reason rather than belt-and-braces**. B (the run-keyed terminalize) is the only home the
**zombie** path reaches, because that path has no engine, no loop and no `phase_id` — plan 194-09
shipped it. **A is the only home that can distinguish "the phase the user interrupted" from "some
phase row that happens to be `active`"** — this plan ships it.

Output: one shielded write inside the shipped cleanup, and V-16.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/194-stop-a-running-workflow/194-PATTERNS.md
@.planning/phases/194-stop-a-running-workflow/194-RESEARCH.md
@.planning/phases/194-stop-a-running-workflow/194-CONTEXT.md
@.planning/phases/194-stop-a-running-workflow/194-BASELINE.md

**Base commit — ASSERT FIRST.** Dispatched against `d2a3b51b` on `develop`. In a worktree, run
`bash scripts/bootstrap-worktree.sh "$(pwd)"` FIRST, then assert `d2a3b51b` is the base ancestor.
Tear down only with `bash scripts/teardown-worktree.sh <path>`; never `rm -rf`.

**Serialization:** the tests here mock the pool and seed nothing in Postgres — parallel-safe under
CLAUDE.md rule 4.

<interfaces>
`backend/app/services/harness_engine.py`:
- `:1572` — `phase_id = row["id"]`, bound inside the `while` iteration, **before** the `try` at
  `:1597`, so it is in scope inside the `except`.
- `:1578` — `mark_phase_active`, one per loop iteration (which is why more than one `active` row per
  run is unreachable in normal operation).
- `:1613-1644` — **the arm to edit**: `except BaseException:` — the cancel/escape path
  (D-06 / BUG-260605-01). It currently: checks `if not is_app_shutting_down():` (⚠ **096-09, UAT
  Test 2** — on a GRACEFUL shutdown the pending prompt must NOT be expired, so the run stays
  resumable), then `await asyncio.shield(_expire_pending_ask_user(pool, ctx.thread_id, run_id, org_id=…))`
  inside its own `try/except BaseException` + `logger.exception`, then **`raise`**.
- `:1646-1651` — **the analog, three lines below**: the `fail_run` arm, the file's own shipped
  "terminalize the phase, then the run" composition, carrying D-07 verbatim: `await fail_phase(pool, phase_id, outcome.reason)`
  then *"Completed phases' outputs are ALREADY durable — `finish_run` does NOT touch them (D-07).
  The run flips to `failed`; nothing silently dropped."* then `await finish_run(pool, run_id, "failed")`.
- `:92-94` — `is_app_shutting_down()`.

`backend/app/db/workflows.py::cancel_phase(pool, phase_id)` — the PHASE-KEYED writer from plan 194-06.
</interfaces>
</context>

<non_goals>
- ⛔ **Do NOT move, duplicate or remove the `is_app_shutting_down()` gate at `:1630`.** It is Phase
  096-09 / UAT Test 2. Its scope is the **ask_user expiry**, and this plan must not silently widen or
  narrow it.
- ⛔ **The `raise` stays LAST.** Cancellation is already in flight; anything added must not swallow
  or delay it beyond a shielded write.
- ⛔ **Do NOT call `finish_run` here.** The producer's F2 block already writes
  `workflow_runs.status='cancelled'` on this path, and adding a second call would put two writers on
  one arm. This plan writes the PHASE row only.
- ⛔ **Do NOT touch the `fail_run` arm or any other outcome arm.** They are the analog, not the target.
- ⛔ **Do NOT touch `run_producer.py`.**
- ⛔ **Do NOT terminalize in bulk.** `cancel_phase` is PHASE-KEYED on the id already in scope; a
  run-keyed write here would defeat the whole reason home A exists.
- ⛔ No new package.
</non_goals>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Terminalize the interrupted phase inside the shielded cancel/escape cleanup (V-16)</name>
  <files>backend/app/services/harness_engine.py, backend/tests/test_harness_engine.py</files>
  <behavior>
    - On `CancelledError` mid-phase, `cancel_phase(pool, phase_id)` is awaited **once**, with the
      `phase_id` bound in the current loop iteration.
    - The `CancelledError` still propagates — the arm re-raises, and the caller still sees it.
    - `_expire_pending_ask_user` is still shielded and still gated by `is_app_shutting_down()`; on a
      graceful shutdown it is NOT called.
    - If `cancel_phase` itself raises, the arm logs and **still re-raises** the original exception —
      a cleanup failure must not swallow the cancellation.
    - Deep runs are unaffected: this arm belongs to the harness engine, which a Deep run never enters.
  </behavior>
  <read_first>
    - `backend/app/services/harness_engine.py:1560-1660` — the loop, `phase_id`'s binding at `:1572`, `mark_phase_active` at `:1578`, the `try` at `:1597`, the cancel arm `:1613-1644` **in full**, and the `fail_run` analog `:1646-1651`
    - `backend/app/db/workflows.py::cancel_phase` (plan 194-06) and its docstring
    - `.planning/phases/194-stop-a-running-workflow/194-PATTERNS.md` § 4 — the analog, the two things that must survive byte-identical, and the shield/try-except discipline
    - `.planning/phases/194-stop-a-running-workflow/194-RESEARCH.md` § *G-C: the interrupted phase row — where the terminalize belongs* (the A/B/C table)
    - `.planning/phases/194-stop-a-running-workflow/194-CONTEXT.md` D-07 and D-13
    - `backend/tests/test_harness_engine.py` — the house fixtures and how the loop is driven
  </read_first>
  <action>
Inside the existing `except BaseException:` cancel/escape arm, add ONE await of
`cancel_phase(pool, phase_id)`, in the shape the `fail_run` arm three lines below already uses
(`fail_phase(pool, phase_id, …)` then the D-07 sentence then `finish_run`) — but writing the PHASE
row only.

**Three properties that must hold, and each is a mechanism rather than a convention:**
1. **`asyncio.shield` it and give it its OWN `try/except` + `logger.exception`** — cancellation is
   already in flight, which is exactly why the shipped `_expire_pending_ask_user` call is shielded
   and wrapped. An unshielded await here would be cancelled before it wrote.
2. **The `raise` stays LAST**, and a failure of the new write must not swallow it.
3. **The 096-09 gate is NOT extended over the new write.** Its scope is the ask_user expiry.
   ⚠ Decide explicitly and STATE the decision in the comment: does the phase terminalize run on a
   graceful shutdown? RESEARCH's answer for **Step 3b** was a definite no-gate, on three reasons
   that do not transfer here — this arm DOES run on shutdown-induced cancellation, and a phase row
   left `active` on a run the boot sweep will RESUME is correct, because that phase really is still
   pending work. ⇒ **Place the new write inside the same `if not is_app_shutting_down():` scope as
   the expiry**, so a graceful shutdown leaves both the prompt and the phase row resumable, and
   record that reasoning in the comment beside the gate. If the executor's reading of the code
   contradicts this, STOP and report — do not silently pick the other arm.

⚠ **Add the D-07 sentence beside the write, in the `fail_run` arm's own words:** *completed phases'
outputs are ALREADY durable and are not touched; only the phase that was running moves.* That
sentence is inherited verbatim, not re-litigated.

Extend `backend/tests/test_harness_engine.py` with the five `<behavior>` cases.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python.exe -m pytest tests/test_harness_engine.py -q -k "cancel or phase"</automated>
  </verify>
  <acceptance_criteria>
    - All five cases pass and every shipped case in `test_harness_engine.py` is green with **no
      assertion edited**.
    - `grep -c "cancel_phase(pool, phase_id" backend/app/services/harness_engine.py` returns exactly 1.
    - `grep -c "is_app_shutting_down" backend/app/services/harness_engine.py` is **unchanged** from
      the pre-change count — the gate was neither duplicated nor removed.
    - The `raise` is still the LAST statement of the arm (asserted by a source read of the arm's
      final line, and behaviourally by the case that proves `CancelledError` still propagates).
    - The new await is `asyncio.shield`-ed and inside its own `try/except`.
    - `git diff -U0 -- backend/app/services/harness_engine.py` shows **no hunk** inside the
      `fail_run` arm or any other outcome arm.
    - `git diff --numstat -- backend/app/services/run_producer.py` is **empty**.
    - The graceful-shutdown decision is STATED in the comment with its reasoning.
    - `pytest backend/tests/unit -q` — no NEW failure BY NAME against `194-BASELINE.md`.
  </acceptance_criteria>
  <done>The phase the user actually interrupted is marked on the live-cancel path, without a query, shielded, with the re-raise last and the 096-09 gate's scope unmoved and its treatment of the new write stated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fence the arm — completed phases untouched, the re-raise preserved, and drive both RED</name>
  <files>backend/tests/test_harness_engine.py</files>
  <behavior>
    - Given a run whose phases are `[completed, active, pending]` and a mid-phase cancel, exactly ONE
      phase write is issued and it targets the `active` row's id — the completed row's id appears in
      no write.
    - The cancel arm never composes a phase status of `failed` or `skipped` — the only value written
      is migration 119's slug.
    - A `cancel_phase` that raises does not prevent the `CancelledError` from propagating.
  </behavior>
  <read_first>
    - `backend/app/services/harness_engine.py:1613-1651` — the edited arm and the `fail_run` analog
    - `backend/tests/test_harness_engine.py` — the fixtures added in Task 1
    - `.planning/phases/194-stop-a-running-workflow/194-RESEARCH.md` § *The fences* rows **F-5** and **F-6**, and F-6's scoping warning
    - `.planning/phases/194-stop-a-running-workflow/194-CONTEXT.md` D-04, D-07, D-13
    - `supabase/migrations/119_workflow_phases_cancelled.sql`
  </read_first>
  <action>
Add the three `<behavior>` fences.

⚠ **Scope the vocabulary fence over the composed VALUE, never the module source** — `harness_engine.py`
legitimately names `fail_phase`, `skip_phase` and their statuses three lines below the arm being
edited, so a raw source sweep would red on shipped, correct code (the 193.2 F-3 lesson).

**⚠ DRIVE BOTH RED, with plants in production source, each reverted md5-identical:**
1. **Completed-untouched plant:** in `harness_engine.py`, change the cancel arm's write from the
   phase-keyed `cancel_phase(pool, phase_id)` to a run-keyed terminalize over every phase of the run
   → the completed-untouched case must RED → revert → md5-identical. ⚠ This plant is the reason home
   A exists at all: a run-keyed write here would mark whatever happens to be `active`, and on a
   race could repaint work the user completed.
2. **Re-raise plant:** make the new `cancel_phase` await unshielded and swallow the exception (i.e.
   `return` instead of `raise` at the arm's end) → the propagation case must RED → revert →
   md5-identical.

Record each plant's file, planted line, failing output and post-revert md5. **A fence with no
recorded RED observation is a claim.** This project shipped five inert fences in Phase 193.2, four
in 193.1, three in 192.1 and five in 190 — **every one caught by planting, none by reading.**
  </action>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python.exe -m pytest tests/test_harness_engine.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All three fences pass on the unmodified tree, and the whole `test_harness_engine.py` suite is
      green.
    - **Two RED observations recorded**, each with the planted line, the failing assertion output and
      the post-revert md5.
    - The vocabulary fence is scoped over the composed value — confirmed by observing it stays GREEN
      with the file's shipped `fail_phase` / `skip_phase` references in place, and stating that this
      was checked rather than assumed.
    - The written slug is compared byte-for-byte against migration 119's literal by grepping both
      files.
    - `pytest backend/tests/unit -q` — no NEW failure BY NAME against `194-BASELINE.md`.
  </acceptance_criteria>
  <done>The engine cancel arm is fenced on both the phase-keying and the re-raise, and each fence has been watched to FAIL against a real production-source plant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine cleanup → `public.workflow_phases` | A write performed while cancellation is already in flight |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-194-10-01 | Tampering | completed phase rows | mitigate | The write is PHASE-KEYED on the `phase_id` bound in this loop iteration — it cannot reach a completed row by construction, and the fence is driven RED against a run-keyed plant. |
| T-194-10-02 | Repudiation | restart-resumability | mitigate | The 096-09 gate's occurrence count is asserted unchanged, and the new write's placement relative to that gate is a STATED decision with its reasoning, not an accident. A phase row left `active` on a run the boot sweep will resume is correct. |
| T-194-10-03 | Denial of Service | the cancellation itself | mitigate | The new await is `asyncio.shield`-ed inside its own `try/except`, and the `raise` stays last — a cleanup failure logs and still propagates. Fenced by the re-raise plant. |
| T-194-10-04 | Tampering | a paused approval | mitigate | `_expire_pending_ask_user` remains shielded and gated, unmoved, so a paused prompt cannot strand as a submittable-but-dead card (`BUG-260808-02`'s folded half). |
| T-194-10-05 | Information Disclosure | error paths | mitigate | `logger.exception` only; no phase `output`, prompt or provider payload is logged. |
| T-194-10-SC | Tampering | pip installs | mitigate | None owed — no package is added. |
</threat_model>

<verification>
- `pytest tests/test_harness_engine.py -q` passes in full.
- `pytest backend/tests/unit -q` — no NEW failure BY NAME against `194-BASELINE.md`.
- `git diff --numstat -- backend/app/services/run_producer.py backend/app/services/run_lifecycle.py backend/app/api/runs.py`
  is **empty**.
- `grep -c "is_app_shutting_down" backend/app/services/harness_engine.py` unchanged.
- **Two RED observations recorded**, each with a post-revert md5.
- ⚠ **Measure this file for the hot-file ledger.** `harness_engine.py` is not a ledger row; derive
  `git log --oneline -- backend/app/services/harness_engine.py | wc -l`, the bucket recipe with
  non-phase buckets subtracted and NAMED, and `wc -l`, and hand the triple to plan 194-13.
</verification>

<success_criteria>
- V-16: the in-flight phase becomes `cancelled` on the engine cancel arm, identified without a query.
- The 096-09 gate, the shield discipline and the trailing `raise` are all provably intact.
- Both fences driven RED against real production-source plants, reverted md5-identical.
</success_criteria>

<output>
Create `.planning/phases/194-stop-a-running-workflow/194-10-SUMMARY.md` when done.
</output>
