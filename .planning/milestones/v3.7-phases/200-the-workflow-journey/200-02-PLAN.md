---
phase: 200-the-workflow-journey
plan: 02
type: execute
wave: 2
depends_on: ["200-01"]
files_modified:
  - supabase/migrations/121_workflow_phases_timings.sql
  - supabase/full-schema.sql
  - backend/tests/test_migration_121.py
  - backend/app/db/workflows.py
  - backend/app/services/harness/phase_types.py
  - backend/app/services/harness_engine.py
  - backend/app/api/workflow_runs.py
  - backend/app/api/threads.py
  - backend/app/models/thread.py
  - backend/tests/test_188_workflow_run_read.py
  - backend/tests/test_200_phase_counts.py
  - frontend/src/lib/api.ts
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
autonomous: false
requirements: [DES-02]

must_haves:
  truths:
    - "D-05 — `workflow_phases` carries nullable `started_at` and `completed_at`, written at ALL SEVEN status-write sites, and a per-step duration is derivable for the first time"
    - "D-06 — NO BACKFILL: every pre-200 row keeps both timestamps NULL, so `never ran` and `time not recorded` stay structurally distinct; the migration test asserts this as a NEGATIVE control"
    - "D-07 — a per-step count is DECLARED by the phase type and only where a count is already a fact in its own output; three of the seven types emit no key at all, and `source_refs: []` emits a real `count: 0`"
    - "D-15 — the `api.ts` Phase-197 decline HOLDS: this plan's whole `api.ts` diff is type-only, proved by grep over the real diff, so `196-08`'s mock-factory failure mode stays unreachable"
    - "D-16 — `backend/app/api/workflow_runs.py` gains its hot-file-ledger row in CLAUDE.md AND its section in docs/HOT-FILE-LEDGER.md in the SAME COMMIT that modifies it"
    - "All FOUR transports carry the new fields — the two SSE emits, `GET /threads/{id}/workflow` and `GET /workflow-runs/{id}` — so the run page and the chat panel cannot disagree"
    - "The route's projection, its Pydantic model and its serializer widen in LOCKSTEP, and the test fake can now SEE a forgotten projection"
    - "The migration applies by SQL-editor paste, never `db push` / `db reset`, and `full-schema.sql` is regenerated with no `--reset`"
  artifacts:
    - path: "supabase/migrations/121_workflow_phases_timings.sql"
      provides: "Two nullable timestamptz columns on workflow_phases, no default, no backfill"
      contains: "ADD COLUMN IF NOT EXISTS"
    - path: "backend/tests/test_migration_121.py"
      provides: "Live-DB gate: columns exist, are nullable, and NO row was backfilled"
      contains: "is_nullable"
    - path: "backend/tests/test_200_phase_counts.py"
      provides: "The seven-row per-executor count table test"
    - path: "backend/app/db/workflows.py"
      provides: "Seven timestamp write sites and two widened SELECT lists"
      contains: "started_at"
  key_links:
    - from: "backend/app/api/workflow_runs.py"
      to: "workflow_phases.started_at"
      via: "the PostgREST .select() projection, widened in lockstep with the model and the serializer"
      pattern: "select\\(.*started_at"
    - from: "backend/app/services/harness/phase_types.py"
      to: "workflow_phases.output"
      via: "_persist_output writes the executor dict full-inline; the declared measure rides in it"
      pattern: "_measure"
---

<objective>
Put the measurable facts on the wire: two nullable timestamp columns written at **all seven**
phase-status write sites, a per-step count **declared** by the phase type wherever a count is already
a fact in its own output, and **all four transports** widened so the run page and the chat panel
cannot disagree.

Purpose: measured, not assumed — `workflow_phases` batch-INSERTs every row at run creation
(`db/workflows.py:334`) and overwrites `updated_at` on every transition, so a per-step duration is
genuinely underivable today. No presentation-only phase can close DES-02. This is the slice that
clears four of the sketch's seven amber rows.

Output: migration 121, seven write sites, seven executors surveyed (four declare, three stay silent),
four transports, a type-only `api.ts` mirror, and the hot-file-ledger row `api/workflow_runs.py` owes.

⚠ **This plan runs ALONE in its wave.** `test_migration_121.py` connects to real Postgres `:54322`,
and CLAUDE.md's parallel-execution rule 4 forbids concurrent plans whose tests mutate the local
database. Worktrees isolate files; they do not isolate Postgres.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⚠ **WORKTREE BOOTSTRAP IS YOUR FIRST ACTION, BEFORE ANYTHING ELSE:**
```bash
bash scripts/bootstrap-worktree.sh "$(pwd)"
```
`workflow.use_worktrees` is **true**, so this plan runs in an isolated worktree. `git worktree add`
checks out TRACKED files only, and `backend/venv` (1.7 GB), `frontend/node_modules` (541 MB),
`backend/.env` and `frontend/.env.local` are all gitignored. The bootstrap junctions the two large
trees and copies the two env files. It is idempotent and fails loudly. **A worktree that skipped it
reports green typechecks and red tests for reasons that look like this plan's fault** — and every
`<verify>` block below invokes `backend/venv/Scripts/python.exe -m pytest`, `npx vitest run` or
`npx tsc -p tsconfig.app.json --noEmit`, so a skipped bootstrap masks exactly the defects this plan
exists to catch.

⚠ **NEVER `rm -rf` a worktree.** A recursive delete FOLLOWS the junction and destroys the operator's
real 1.7 GB `venv`, silently. Tear down only with `bash scripts/teardown-worktree.sh <path>`.

⚠ **Assert your base SHA before reading the plan.** This phase was planned at
`fe40ce1ce6af06ae3735aa98a539f0f4463abbdf`. Run `git rev-parse HEAD` as your second action and record
it in the SUMMARY. Twelve of twelve worktrees in Phase 192 forked from a `master` merge commit
rather than the orchestrator's HEAD.

⚠ **`GSD_VITEST_MAX_WORKERS=2` on every vitest invocation** — in the prose of an acceptance
criterion as well as in the `<verify>` command. The cap read `4` until 2026-08-14 and **ROTTED**;
at the current gated total, cap 4 measured `failed` 17 / 4 / 3 on one identical commit while cap 2
measured 0. ⚠ **If a gate run reds, do NOT reach for the cap** — capture the failing filenames from
the gate's own persisted JSON BEFORE re-running anything, check each against
`git diff --numstat fe40ce1c HEAD`, and only then judge. Red is not always flake:
`196-08` hit `failed 249` and every one was real.
</execution_context>

<context>
@.planning/phases/200-the-workflow-journey/200-CHECKLIST.md
@.planning/phases/200-the-workflow-journey/200-RESEARCH.md
@.planning/phases/200-the-workflow-journey/200-PATTERNS.md
@.planning/phases/200-the-workflow-journey/200-VALIDATION.md
@CLAUDE.md
@supabase/migrations/119_workflow_phases_cancelled.sql
@supabase/migrations/058_workflow_phases.sql
@backend/tests/test_migration_119.py

<interfaces>
<!-- The wire contract this plan establishes. Executor: implement against THESE names. -->

Python — `backend/app/api/workflow_runs.py` (`WorkflowRunPhaseRead`, extended):
  slug: str
  phase_index: int
  status: str
  phase_type: str | None = None
  started_at: datetime | None = None      # NEW
  completed_at: datetime | None = None    # NEW
  step_count: int | None = None           # NEW — a real integer, INCLUDING 0; None means "this type declares no count"
  step_noun: str | None = None            # NEW — the step's own noun; None iff step_count is None

Python — `backend/app/models/thread.py` (`WorkflowPhaseState`, extended): the SAME four new fields.
  ⚠ This is a SECOND, INDEPENDENT wire model for the SAME rows. It feeds the workspace panel's
  PhaseTimeline/PhaseCard. CONTEXT names only `WorkflowRunPhaseRead`; widening only that one ships a
  run page with durations and a chat panel without them.

Python — the executor-declared measure, carried inside the existing `workflow_phases.output` jsonb
(`harness_engine.py:142` `_persist_output` stores each executor's dict full and inline):
  {"_measure": {"count": int, "noun": str}}      # present ONLY when the type has a real number
  # A type with no real number emits NO `_measure` KEY AT ALL — not 0, not null, not a dash.

TypeScript — `frontend/src/lib/api.ts` (`WorkflowRunPhase`, extended — TYPE ONLY, zero runtime exports):
  started_at?: string | null
  completed_at?: string | null
  step_count?: number | null
  step_noun?: string | null
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 121 and its live-DB gate (Wave 0) — nullable, no default, NO BACKFILL</name>
  <read_first>
    - `supabase/migrations/119_workflow_phases_cancelled.sql` (the analog — the house style and its four header rules)
    - `supabase/migrations/058_workflow_phases.sql` (the table, its four RLS policies at `:38-77`, and the `updated_at` trigger)
    - `backend/tests/test_migration_119.py` (`:97-160` the two clean skips; `:161-250` the rolled-back transaction + `_seed_phase_parents` FK chain)
    - `.planning/phases/200-the-workflow-journey/200-PATTERNS.md` § 1 and § 3 (the seven binding invariants and the six test invariants)
    - `.planning/phases/200-the-workflow-journey/200-CHECKLIST.md` §6 (the migration number this phase claims, re-derived)
  </read_first>
  <files>supabase/migrations/121_workflow_phases_timings.sql, backend/tests/test_migration_121.py</files>
  <action>
    **First re-check the number:** `ls supabase/migrations/ | sort -V | tail -1`. RESEARCH measured
    `120_model_capabilities_overrides_emit_tier.sql` as the highest, so `121` is free. The filename must
    match `<digits>_name.sql` — ⚠ a letter suffix like `121b` is **silently skipped** by the Supabase CLI.

    Write `121_workflow_phases_timings.sql` in 119's house style: `BEGIN;` … `COMMIT;` wrapping a single
    `ALTER TABLE public.workflow_phases ADD COLUMN IF NOT EXISTS started_at timestamptz, ADD COLUMN IF
    NOT EXISTS completed_at timestamptz;`, followed by a `COMMENT ON COLUMN` for each naming Phase 200 /
    DES-02 / D-05, the write site, and — for `started_at` — the explicit **NO BACKFILL** decision and its
    reason (a backfill from `updated_at` is right for some rows and silently wrong for others, and
    nothing on the row would say which). `completed_at`'s comment names the seventh site,
    `cancel_active_phases` at `db/workflows.py:1649`.

    Binding invariants, each of which makes the migration WRONG if violated:
    1. **Both columns NULLABLE, no `DEFAULT`, no backfill.** A `DEFAULT now()` backfills on ALTER and
       destroys D-06's "time not recorded" arm before it exists.
    2. **Do NOT touch `workflow_phases_status_check`.** Phase 200 adds no phase status — D-10 pauses the
       RUN, not the phase. 119's *"re-add every shipped literal VERBATIM"* rule does not apply here
       because 121 rewrites no CHECK.
    3. **No RLS work is owed.** `058:38-77`'s four policies share one predicate
       (`auth.uid() = (SELECT t.user_id FROM threads t JOIN workflow_runs wr ON wr.thread_id = t.id WHERE wr.id = workflow_run_id)`)
       and **none names a column list**, so `ADD COLUMN` touches no policy. Write this in the migration's
       header comment — a reviewer will ask.
    4. **The `updated_at` trigger is inert here.** `set_updated_at()` writes only `NEW.updated_at`, is
       `BEFORE UPDATE` (not INSERT), and fires on every UPDATE regardless of which column moved — so the
       seven write sites **keep their explicit `updated_at=now()`** unchanged and no diff is owed there.
    5. ⚠ **Do NOT take `SEED-143` (a CHECK constraining `slug`) in this migration.** Record the decline
       in the header with its trigger: *"the next phase that opens a `workflow_phases` migration for
       another reason."* It is a schema + API-surface change on a phase already carrying a behaviour
       change, and neither CONTEXT nor the ROADMAP scoped it.

    Then author `backend/tests/test_migration_121.py` by copying `test_migration_119.py`'s shape:
    - **SKIP 1 of 2** — `_pg_reachable()` against `POSTGRES_DSN` (default
      `postgresql://postgres:postgres@127.0.0.1:54322/postgres`) with a 2 s timeout, via `pytestmark`.
    - **SKIP 2 of 2** — `_migration_121_applied(conn)` reads `information_schema.columns` for
      `table_name='workflow_phases' AND column_name IN ('started_at','completed_at')`. ⚠ **Read the
      catalog; never probe with an INSERT.** A probe writes to the operator's live dev DB and conflates
      *"the column is absent"* with *"something else rejected the row."*
    - Every write inside a transaction that **ROLLS BACK** — the operator's local DB is the working
      environment, and `_seed_phase_parents` seeds `auth.users → threads → workflow_definitions →
      workflow_runs` inside that same rolled-back transaction.
    - **Assert nullability, not just existence:** `is_nullable = 'YES'` on both. A `NOT NULL` column would
      have required a backfill, so its absence is part of the proof that none happened.
    - ⚠ **THE NO-BACKFILL NEGATIVE CONTROL is as load-bearing as the positive one.** Read a real
      pre-existing `workflow_phases` row (or seed one at `status='completed'` and assert it was not
      touched) and assert `started_at IS NULL AND completed_at IS NULL`. A test that only asserts the
      columns exist cannot tell a no-backfill migration from a backfilled one.
    - Add the docblock 119 carries: ⚠ **A GREEN SKIP IS NOT A PASSING FENCE.** Until the migration is
      applied this file has demonstrated only that it skips — the RED-then-GREEN observation is owed by
      Task 2, not by this one.
  </action>
  <acceptance_criteria>
    - `ls supabase/migrations/121_*.sql` resolves to exactly one file matching `^[0-9]+_[a-z0-9_]+\.sql$`.
    - The SQL contains `BEGIN;`, `COMMIT;`, two `ADD COLUMN IF NOT EXISTS`, and **zero** occurrences of `DEFAULT` and of `workflow_phases_status_check`.
    - `grep -c 'NOT NULL' supabase/migrations/121_workflow_phases_timings.sql` returns 0.
    - The header comment contains both `NO BACKFILL` and `SEED-143`.
    - `backend/venv/Scripts/python.exe -m pytest backend/tests/test_migration_121.py -q` exits 0 — either all-pass (DB reachable AND applied) or a clean skip with the reason printed; a hard error is a failure.
    - The test file contains `is_nullable`, `information_schema.columns`, `rollback` and a case whose name contains `backfill`.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; m=supabase/migrations/121_workflow_phases_timings.sql; grep -q "BEGIN;" $m; grep -q "COMMIT;" $m; test $(grep -c "ADD COLUMN IF NOT EXISTS" $m) -eq 2; test $(grep -c "DEFAULT" $m) -eq 0; test $(grep -c "NOT NULL" $m) -eq 0; grep -q "NO BACKFILL" $m; grep -q "SEED-143" $m; t=backend/tests/test_migration_121.py; for k in is_nullable information_schema.columns rollback backfill; do grep -q "$k" $t; done; cd backend && venv/Scripts/python.exe -m pytest tests/test_migration_121.py -q --no-header'</automated>
  </verify>
  <done>Migration 121 exists in house style with no default and no backfill; its gate asserts existence, nullability and the no-backfill negative control, and skips cleanly when `:54322` is unreachable or the migration is unapplied.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Operator applies migration 121 via the Supabase SQL editor, then the schema artifact is regenerated</name>
  <read_first>
    - `supabase/migrations/121_workflow_phases_timings.sql` (Task 1's output — the exact text to paste)
    - `CLAUDE.md` § *Local dev infrastructure* (the SQL-editor rule, `scripts/start-local-infra.ps1`, and ⚠ the Windows port-reservation trap: an UNMARKED `netsh` range straddling 54322 is the fault; a trailing `*` means the administered exclusion is already applied)
    - `supabase/SETUP.md` and `scripts/regenerate-full-schema.sh` (⚠ **no `--reset`** — that wipes the local DB; never hand-edit `full-schema.sql`)
    - `scripts/check-deploy-drift.sh` (the Phase-157 deployment-artifact parity gate)
  </read_first>
  <action>
    **[BLOCKING — operator action, `autonomous: false`].** Halt here. The migration is applied by a
    human pasting it into the Supabase SQL editor, then the schema artifact is regenerated by script.
    ⚠ **NEVER `supabase db push` / `supabase db reset`** — CLAUDE.md's rule, and it is what preserves
    the operator's dev data. Present the numbered steps below, wait for the resume signal, and do not
    proceed to Task 3 until `test_migration_121.py` reports PASSES rather than SKIPS.
  </action>
  <files>supabase/full-schema.sql</files>
  <instructions>
    `supabase/migrations/121_workflow_phases_timings.sql` (two nullable `timestamptz` columns on
    `workflow_phases`, no default, no backfill) and `backend/tests/test_migration_121.py`, which
    currently **skips** with *"the migration this file gates has not been applied yet."*
  </instructions>
  <verification>
    ⚠ **NEVER `supabase db push` or `supabase db reset`** — CLAUDE.md's rule, and it is what preserves
    your dev data. This step is an operator action by design.

    1. If local Supabase is not up, run:
       `powershell -ExecutionPolicy Bypass -File scripts/start-local-infra.ps1`
       (idempotent; it verifies that `54322` / `6379` / `54321` accept a real TCP connection, not merely
       that a container reports `Up`). If it reports a Windows port reservation, follow CLAUDE.md's
       one-step diagnosis: `netsh int ipv4 show excludedportrange protocol=tcp` — an **UNMARKED** range
       straddling 54322 is the fault; a trailing `*` means the administered exclusion is already in place.
    2. Open the Supabase SQL editor (Studio, `http://127.0.0.1:54323`).
    3. Paste the **entire contents** of `supabase/migrations/121_workflow_phases_timings.sql` and run it.
       It is `BEGIN`/`COMMIT`-wrapped and `IF NOT EXISTS`-guarded, so a re-paste is safe.
    4. Confirm it applied:
       `backend/venv/Scripts/python.exe -m pytest backend/tests/test_migration_121.py -q`
       — the two SKIPs must now be **PASSES**, including the no-backfill negative control.
       ⚠ **A green skip is not a passing fence.** The RED→GREEN observation is owed HERE.
    5. Regenerate the deploy artifact: `bash scripts/regenerate-full-schema.sh`
       — **no `--reset`** (that wipes the local DB). Never hand-edit `supabase/full-schema.sql`.
       Confirm the diff on that file shows only the two new columns and their comments.
    6. **Deployment-artifact parity (same-commit rule).** This change adds no env var, no bundled
       service, no seed row and does not move the sandbox image tag — so no Phase-157 artifact is owed.
       Confirm by running `bash scripts/check-deploy-drift.sh` and pasting its verdict.
    7. Note for the eventual production promotion (NOT now — deploys are operator-triggered):
       migration 121 must be pasted into the **cloud** Supabase SQL editor in the same operation that
       deploys this backend, or the widened `.select()` reads a column the cloud DB does not have.
  </verification>
  <resume-signal>Type "applied" once step 4 shows passes rather than skips and step 5's diff is limited to the two columns, or describe what happened.</resume-signal>
  <acceptance_criteria>
    - `backend/venv/Scripts/python.exe -m pytest backend/tests/test_migration_121.py -q` reports passes, not skips.
    - `git diff --stat supabase/full-schema.sql` shows a change, and `git diff supabase/full-schema.sql` mentions `started_at` and `completed_at` and nothing unrelated.
    - `bash scripts/check-deploy-drift.sh` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd backend && venv/Scripts/python.exe -m pytest tests/test_migration_121.py -q --no-header 2>&1 | tee /dev/stderr | grep -qE "[0-9]+ passed" && ! (cd .. && git diff supabase/full-schema.sql | grep -q "^+.*started_at") && echo "WARN: full-schema not regenerated" && exit 1 || true'</automated>
  </verify>
  <done>Migration 121 is applied to the live local DB, its gate passes rather than skips, and `full-schema.sql` is regenerated by script with no `--reset`.</done>
</task>

<task type="auto">
  <name>Task 3: Write the timestamps at all SEVEN sites and declare the per-step count across the SEVEN phase types</name>
  <read_first>
    - `backend/app/db/workflows.py` — the seven `UPDATE workflow_phases SET status=…` writers at `:1441`, `:1483`, `:1505`, `:1517`, `:1548`, `:1597` and ⚠ **`:1649` (`cancel_active_phases`, the run-keyed engineless-Stop arm)**; the two SELECT lists `load_run_phases` (`:1237-1245`) and `get_active_phase` (`:1329-1337`); the batch INSERT at `:334`
    - `backend/app/services/harness/phase_types.py` — `PHASE_TYPE_REGISTRY_ENTRIES` (`:2398-2410`, seven keys) and the seven executors' return dicts
    - `backend/app/services/harness_engine.py:142` (`_persist_output` — stores each executor dict full and inline)
    - `.planning/phases/200-the-workflow-journey/200-PATTERNS.md` § 11 (the writer excerpts + the `IS DISTINCT FROM` rule) and § 14 (the seven types and their real count facts)
    - `.planning/phases/200-the-workflow-journey/200-RESEARCH.md` §B4, §B7, Pitfall 5, Pitfall 8, IDIOM-3
    - `backend/tests/test_workflow_phase_cancel.py`, `backend/tests/test_l01_finish_run_terminal_guard.py` (the existing writer coverage), `backend/tests/test_harness_engine.py`
  </read_first>
  <files>backend/app/db/workflows.py, backend/app/services/harness/phase_types.py, backend/tests/test_200_phase_counts.py</files>
  <action>
    **A — the seven write sites.** Add one column assignment per site, inside the existing single-line SQL
    string, leaving every `WHERE` clause and the explicit `updated_at=now()` untouched:

    | # | Function | UPDATE line | Add |
    |---|---|---|---|
    | 1 | `mark_phase_active` | 1441 | `started_at = now()` |
    | 2 | `complete_phase` | 1483 | `completed_at = now()` |
    | 3 | `fail_phase` | 1505 | `completed_at = now()` |
    | 4 | `skip_phase` | 1517 | ⚠ **NEITHER** — a skipped phase never ran; D-06 calls that *correct silence* |
    | 5 | `record_phase_not_sent` | 1548 | `completed_at = now()` |
    | 6 | `cancel_phase` | 1597 | `completed_at = now()` |
    | 7 | **`cancel_active_phases`** | **1649** | `completed_at = now()` |

    ⚠ **SEVEN, not six.** CONTEXT D-05 enumerates six and every one of its line numbers is exact — the
    enumeration is one short. Site 7 is the arm 194 built for a Stop landing with **no producer running**;
    its `WHERE … AND status = 'active'` means it only moves rows that already carry a `started_at`, so
    `completed_at = now()` is correct there. A plan that writes six leaves a hole on exactly that path,
    and D-06's `cancelled → ran 8.1s, interrupted` arm then renders *"time not recorded"* on it.

    ⚠ **`cancel_active_phases`' predicate is written on ONE source line ON PURPOSE** (193.2-08: a rule
    written WRAPPED failed its own literal `grep -q` and read as *"already fixed"*). Keep it on one line.
    ⚠ **Do not widen `AND status = 'active'`** — it is the only thing between that statement and a bulk
    terminalize. ⚠ **The batch INSERT at `:334` must NOT learn a timestamp**: `started_at` is precisely
    the thing `created_at` cannot be, which is D-05's entire argument.

    Widen the two SELECT lists (`load_run_phases`, `get_active_phase`) to add `started_at, completed_at`.
    These feeds run on a pool that **BYPASSES RLS**, so the `WHERE` clause IS the access boundary —
    adding a column to a `SET` or a `SELECT` moves no `WHERE`. State that in the docstring.
    `$N` placeholders only; no string interpolation anywhere in the slice.

    **B — the declared count, across SEVEN phase types (not eight).** ⚠ `PHASE_TYPE_REGISTRY_ENTRIES`
    has seven keys and its own comment says *"The 7 executors"*; `llm_judge_rubric` is a
    `ValidatorSpec.kind`, not a phase type (see the checklist's X-2 refutation). Add **one additive line
    per declaring executor** — that is what keeps this from being a second concern under G-5:

    | # | type | declares | noun |
    |---|---|---|---|
    | 1 | `programmatic` | **NO — deliberately not this phase** | — |
    | 2 | `llm_single` | NO | — |
    | 3 | `llm_agent` | `len(source_refs)` | `sources` |
    | 4 | `llm_batch_agents` | `len(sub_run_ids)` | `agents` |
    | 5 | `llm_human_input` | NO | — |
    | 6 | `llm_emit` | `len(field_map)` | `fields` |
    | 7 | `external_action` | NO — one send is not a count | — |

    The declaration rides in the returned dict as `{"_measure": {"count": N, "noun": "…"}}`, which
    `_persist_output` already stores full and inline in `workflow_phases.output` — no column, no
    migration. Rules:
    - **Types 1, 2, 5 and 7 emit NO `_measure` KEY AT ALL.** Not `0`, not `null`-with-a-noun, not a dash.
      The absence must be an absent KEY so the client's arm is `hasOwnProperty`-shaped rather than
      `?? 0`-shaped.
    - ⚠ **`source_refs: []` ⇒ `count: 0` IS EMITTED.** A step that searched and found nothing is a REAL
      fact and is not the same as "this type doesn't count things" (SEED-159 / IDIOM-3).
    - **`programmatic` must NOT declare a count here.** Its registry members are pluggable and their
      shapes differ per `fn`; `len(whatever_list_is_there)` is exactly the structural derivation D-07
      rejected. A per-`fn` declaration would be a fifth site and a **second** concern — G-5 fires.
    - ⚠ **The noun is the STEP's own, never the contract's.** `312 docs matched` / `48 fields extracted`
      on the sheet are DOMAIN sentences; having a model author the number would ship the fabricated
      business figure `199-05` named *"the highest-consequence lie this phase could ship."* The three
      nouns above are domain-neutral by construction (SEED-168).
    - ⚠ `_build_phase_tool_context` is `(phase, ctx, *, model=None)` — any new stub lambda must be
      `lambda phase, ctx, **_: …` or it raises `TypeError`.

    **C — `backend/tests/test_200_phase_counts.py`**, a table test with **seven** rows: four asserting
    the declaring types emit `_measure` with the right count and noun, **three asserting no `_measure`
    key is present at all** (`assert "_measure" not in out`), and one extra case driving `source_refs: []`
    through `llm_agent` and asserting `out["_measure"]["count"] == 0` — the count-side twin of D-06.
  </action>
  <acceptance_criteria>
    - `grep -c "started_at\|completed_at" backend/app/db/workflows.py` ≥ 8 (seven writes minus the deliberate `skip_phase` abstention, plus the two SELECT lists).
    - `grep -n "UPDATE workflow_phases SET status='cancelled', updated_at=now(), completed_at = now() WHERE workflow_run_id" backend/app/db/workflows.py` returns exactly one line (the seventh site, still on ONE source line).
    - `grep -n "SET status='skipped'" backend/app/db/workflows.py` returns a line containing **neither** `started_at` nor `completed_at`.
    - `grep -n "INSERT INTO workflow_phases" backend/app/db/workflows.py` returns a statement naming neither new column.
    - `grep -c "_measure" backend/app/services/harness/phase_types.py` is between 3 and 6 — the declaring executors only.
    - `backend/venv/Scripts/python.exe -m pytest tests/test_200_phase_counts.py -q` passes with ≥ 8 cases, including three `"_measure" not in` assertions and one `count == 0` assertion.
    - `backend/venv/Scripts/python.exe -m pytest tests/test_workflow_phase_cancel.py tests/test_l01_finish_run_terminal_guard.py tests/test_harness_engine.py -q` shows no NEW failures against `200-CHECKLIST.md` §6's recorded baseline.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=backend/app/db/workflows.py; grep -n "SET status=.skipped." $f | grep -qv "started_at\|completed_at"; grep -q "AND status = .active." $f; cd backend && venv/Scripts/python.exe -m pytest tests/test_200_phase_counts.py tests/test_workflow_phase_cancel.py tests/test_l01_finish_run_terminal_guard.py tests/test_harness_engine.py -q --no-header'</automated>
  </verify>
  <done>All seven sites write the right timestamp (and `skip_phase` deliberately writes neither), four of seven executors declare a measure, three emit no key at all, and `count: 0` is proved emittable.</done>
</task>

<task type="auto">
  <name>Task 4: Widen ALL FOUR transports in lockstep, fix the blind test fake, mirror the types, and pay the D-16 ledger row</name>
  <read_first>
    - `backend/app/api/workflow_runs.py` — `:24-47` the IDOR posture, `:76-103` `WorkflowRunPhaseRead`, `:173` `response_model=WorkflowRunRead`, **`:230` the PostgREST `.select("slug, phase_index, status")`**, `:237-245` the serializer
    - `backend/app/api/threads.py:1191-1194` (the raw SQL through `_rls_fetch`) and `backend/app/models/thread.py:48-60` (`WorkflowPhaseState` — the SECOND wire model)
    - `backend/app/services/harness_engine.py:1580-1594` (`phase_started`, and the WRITE-before-EMIT ordering) and `:1950-1975` (`phase_completed`, `phase_recorded_not_sent`, `phase_transition`)
    - `backend/tests/test_188_workflow_run_read.py` — **`:121-122` `def select(self, *_columns, **_kwargs): return self`**, and tests 1, 2 and 6 which pin the IDOR posture
    - `frontend/src/lib/api.ts:4172-4235` (`WorkflowRunPhase`, `WorkflowRunRead` and its `:4200-4204` docblock)
    - `.planning/phases/200-the-workflow-journey/200-PATTERNS.md` § 12, § 13, § 15, § 17, § 18, § 20
    - `docs/HOT-FILE-LEDGER.md` (any `##` section — the shape to copy) and `CLAUDE.md`'s hot-file scan-list table
  </read_first>
  <files>backend/app/api/workflow_runs.py, backend/app/api/threads.py, backend/app/models/thread.py, backend/app/services/harness_engine.py, backend/tests/test_188_workflow_run_read.py, frontend/src/lib/api.ts, CLAUDE.md, docs/HOT-FILE-LEDGER.md</files>
  <action>
    **A — `GET /workflow-runs/{id}`, three places in LOCKSTEP.** Widening two of three produces a green
    model and an empty field, because the route declares `response_model=WorkflowRunRead` at `:173` and
    that **drops undeclared keys silently** (192.2's measured lesson on `api/workflows.py`: a green db
    test beside an unchanged UI). Widen:
    (a) the `.select()` at `:230` → add `started_at, completed_at, output`;
    (b) `WorkflowRunPhaseRead` → add the four fields from `<interfaces>`;
    (c) the serializer at `:237-245` → pass them through.

    ⚠ **Do NOT put the raw `output` jsonb on the wire.** `_persist_output` stores each executor's dict
    full and inline, so `output` carries `field_map`, `citations` and prompts. **The projection selects
    `output`; the SERIALIZER extracts only `_measure.count` / `_measure.noun` into `step_count` /
    `step_noun`, and the model declares no `output` field** — so `response_model`'s drop behaviour is
    what keeps the payload narrow. This resolves RESEARCH's open question A7 without a second migration:
    the carrier stays the jsonb CONTEXT chose, the exposure stays bounded, and the client never sees a
    prompt. Read `_measure` with `isinstance(out, dict)` + `.get`, never `out["_measure"]`.
    ⚠ A plain `.select("*")` would also "work" and **weakens the read** — reject it.

    ⚠ **V4 ACCESS CONTROL — the module's primary deliverable (`:24-47`).** `id` AND `user_id` filtered on
    the SAME select, `.maybe_single()`, **404 never 403**, `Depends(require_canvas())` standing ALONE.
    The widening **must not add a second query, must not add a dependency, and must not change any 404
    body.** The widened `.select()` adds columns to a query already scoped by
    `.eq("workflow_run_id", run["id"])` where `run` came from the ownership select — **no scope widens.**
    Write that sentence in the code comment; a reviewer will ask.

    **B — `GET /threads/{id}/workflow`, the SECOND transport CONTEXT does not name.** Widen the raw SQL at
    `threads.py:1191` (`SELECT slug, phase_index, status …` → add `started_at, completed_at, output`) and
    `WorkflowPhaseState` (`models/thread.py:48`) with the same four fields, extracting the measure the
    same way. This is the CHAT-surface reconcile that feeds `PhaseTimeline`/`PhaseCard`. **Widening only
    `WorkflowRunPhaseRead` ships a run page with durations and a chat panel without them.**
    ⚠ `test_thread_workflow_endpoint.py::test_thread_workflow_state_shape` is **ALREADY RED at HEAD**
    (`assert body["locked"] is True`) and it asserts the very shape this widens. It is recorded as a
    RED-before baseline in `200-CHECKLIST.md` §6 — **do not fix it silently and do not let it read as a
    regression**; re-state its status in the plan summary.

    **C — the two SSE emits.** `_emit(…, "phase_started", …)` at `harness_engine.py:1589-1594` gains
    `started_at` (the live tick's anchor, without a fetch); `_emit(…, "phase_completed", …)` at `:1962`
    gains `completed_at` and the declared `{count, noun}`. ⚠ **WRITE-before-EMIT everywhere** — never
    emit a fact the DB does not yet carry; the write sites from Task 3 already precede these calls.
    **BOTH transports, with FETCH AUTHORITATIVE** — this is the answer to CONTEXT's Claude's-Discretion
    question, and its four reasons are measured: CLAUDE.md's D-v2.5-03; `196`'s live-SSE-lock failure
    that hid a shipped control; `PhaseTimeline`'s reconcile floor exists precisely because a terminal run
    has no stream; and a live tick needs `started_at` at the instant the step starts, which only the SSE
    emit delivers without polling.

    **D — make the test fake able to SEE a forgotten projection.** `_FakeQuery.select` at
    `test_188_workflow_run_read.py:121-122` is a **no-op that discards its column list**, so a test that
    seeds `started_at` reads it back even if `:230` was never widened — the exact opposite of PostgREST.
    Record the requested columns and have `execute()` **project** to them, then add a **positive control
    asserting an UNSELECTED key is ABSENT** from the returned row. Also add a source fence over
    `api/workflow_runs.py` asserting the `.select(` string names each new column. Warning sign to state
    in the test's docblock: *a test that would still pass if you deleted the `.select()` line.*
    Then add wire cases: a phase with both timestamps, a historic phase with both NULL, a phase whose
    type declared no count (`step_count is None`), and a phase whose count is `0` (`step_count == 0`,
    **not** None) — the four renders D-06 and D-07 depend on.

    **E — `frontend/src/lib/api.ts`, TYPE ONLY.** Add the four optional fields to `WorkflowRunPhase`.
    ⚠ **D-15 — verify the 197 decline by GREP OVER THE REAL DIFF, never by quoting the decision:**
    `git diff -U0 -- frontend/src/lib/api.ts | grep -E '^\+' | grep -E '^\+\s*export\s+(const|function|class|let|var|enum)\b'`
    **must be EMPTY.** A non-empty result means `196-08`'s mock-factory failure mode (nine suites throwing
    at mount because a `vi.mock("@/lib/api")` factory did not declare a newly-added export) becomes
    reachable and the re-open trigger fires. ⚠ **The `WorkflowRunRead` docblock at `:4200-4204` goes
    STALE in this commit and must be corrected in it** — it says *"`claimed_at` is the ONLY honest elapsed
    anchor"*, which stays true of `workflow_runs` but is now superseded for a run SPAN by
    `min(started_at) → max(completed_at)`. Measured why it matters: `claimed_at` is null on **100% of
    completed runs** (149 rows, 0 with `claimed_at` — `WorkflowRunPage.tsx:849-851`).
    ⚠ **Do NOT grow a second DB-status→client-union mapping** — `@/lib/phaseState`'s `DB_PHASE_STATUS`
    + `phaseStatusFromDb` is the one, and `api.ts:4172`'s docblock says so outright.

    **F — D-16, the same-commit ledger obligation.** In the SAME commit that modifies
    `backend/app/api/workflow_runs.py`, add its row to CLAUDE.md's hot-file scan-list table AND its
    `##` section to `docs/HOT-FILE-LEDGER.md`. Use the triple re-derived in `200-CHECKLIST.md` §6
    (RESEARCH measured `3 / 3 / 260` — **exactly at the G-5 threshold**, the `libraryRow.ts` /
    `doorVocabulary.ts` state where a missing row costs most). The section records: the three-place
    lockstep widening as the file's binding invariant; the `response_model` silent-drop hazard; the IDOR
    posture as the module's primary deliverable; and the named next seam. *A row without a section, or a
    section without a row, is drift.* Then run `node scripts/check-claude-md-size.cjs` — the 150,000-char
    gate — and record its verdict.
  </action>
  <acceptance_criteria>
    - `grep -n '\.select(' backend/app/api/workflow_runs.py` shows the phase projection naming `started_at`, `completed_at` and `output`.
    - `grep -c 'output' backend/app/api/workflow_runs.py` shows `output` used in the projection and the serializer, and `grep -n 'output' backend/app/api/workflow_runs.py | grep -c 'class WorkflowRunPhaseRead'` is 0 — **the raw jsonb is never a declared wire field**.
    - `backend/app/models/thread.py`'s `WorkflowPhaseState` and `workflow_runs.py`'s `WorkflowRunPhaseRead` each declare all four new fields (`started_at`, `completed_at`, `step_count`, `step_noun`).
    - `grep -c 'started_at' backend/app/services/harness_engine.py` ≥ 1 and `grep -c 'completed_at' backend/app/services/harness_engine.py` ≥ 1, both inside `_emit(` calls.
    - `test_188_workflow_run_read.py` contains a case asserting an **unselected** key is absent, and a case asserting `step_count == 0` distinct from a case asserting `step_count is None`.
    - `backend/venv/Scripts/python.exe -m pytest tests/test_188_workflow_run_read.py tests/test_thread_workflow_endpoint.py -q` shows exactly the one pre-existing failure recorded in `200-CHECKLIST.md` §6 and no other.
    - **D-15 proof:** `git diff -U0 -- frontend/src/lib/api.ts | grep -E '^\+' | grep -E '^\+\s*export\s+(const|function|class|let|var|enum)\b'` prints nothing.
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit` exits 0.
    - `docs/HOT-FILE-LEDGER.md` contains a `## \`backend/app/api/workflow_runs.py\`` section AND CLAUDE.md's scan-list table contains a row naming the same path, both in this commit.
    - `node scripts/check-claude-md-size.cjs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; grep -q "started_at" backend/app/api/workflow_runs.py; grep -q "step_count" backend/app/models/thread.py; test -z "$(git diff -U0 -- frontend/src/lib/api.ts | grep -E "^\+" | grep -E "^\+\s*export\s+(const|function|class|let|var|enum)\b")"; grep -q "backend/app/api/workflow_runs.py" docs/HOT-FILE-LEDGER.md; grep -q "backend/app/api/workflow_runs.py" CLAUDE.md; node scripts/check-claude-md-size.cjs; (cd frontend && npx tsc -p tsconfig.app.json --noEmit); (cd backend && venv/Scripts/python.exe -m pytest tests/test_188_workflow_run_read.py tests/test_thread_workflow_endpoint.py -q --no-header)'</automated>
  </verify>
  <done>All four transports carry the new fields, the test fake can now fail on a forgotten projection, the `api.ts` diff is provably type-only, and `api/workflow_runs.py`'s ledger row and section landed together.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `GET /workflow-runs/{id}` | A guessable run uuid arrives from an authenticated but possibly non-owning user |
| browser → `GET /threads/{id}/workflow` | Same rows, second transport, user-JWT client backed by RLS |
| executor output → `workflow_phases.output` jsonb → wire | Model-influenced content crosses into a payload the browser renders |
| operator → Supabase SQL editor | DDL applied by hand against a live dev database holding real work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-200-02-01 | Information Disclosure | `api/workflow_runs.py:230` widened `.select()` | mitigate | Columns are added to a query already scoped by `.eq("workflow_run_id", run["id"])` where `run` came from the ownership select; **no second query, no new dependency, no changed 404 body**. Tests 1, 2 and 6 of `test_188_workflow_run_read.py` re-run unchanged |
| T-200-02-02 | Information Disclosure | the raw `output` jsonb | mitigate | `output` is selected server-side but **never declared on any wire model**; the serializer extracts only `_measure.count` / `_measure.noun`. `response_model=WorkflowRunRead`'s drop behaviour is the enforcement, and a positive control asserts an unselected/undeclared key is absent |
| T-200-02-03 | Elevation of Privilege | `workflow_phases` RLS after `ADD COLUMN` | mitigate | `058:38-77`'s four policies share one predicate and **name no column list**, so new columns inherit the 2-hop FK chain unchanged. The migration header states this; `test_migration_121.py` reads `information_schema` rather than probing |
| T-200-02-04 | Tampering | `db/workflows.py` SQL | mitigate | `$N` placeholders only; no string interpolation. `cancel_active_phases`' `AND status = 'active'` is NOT widened — it is the only thing between that statement and a bulk terminalize |
| T-200-02-05 | Repudiation | a fabricated per-step number | mitigate | D-07: the count is DECLARED from a fact already in the executor's own return dict (`len(source_refs)` / `len(sub_run_ids)` / `len(field_map)`); no model authors it, and `programmatic` deliberately declares nothing rather than counting whichever key happens to be a list |
| T-200-02-06 | Denial of Service | payload growth | accept | The four added fields are two timestamps, an int and a short noun per phase row; a run carries single-digit phases. The raw `output` jsonb, which WOULD be a size concern, is deliberately not on the wire (T-200-02-02) |
| T-200-02-SC | Tampering | npm/pip/cargo installs | mitigate | **No package installs in this plan** — no `package.json`, `requirements.txt`, lockfile or `Dockerfile.sandbox` is in `files_modified`. If an install becomes necessary, RESEARCH carries no `## Package Legitimacy Audit`, so every package is `[ASSUMED]` and needs a blocking human checkpoint before install |
</threat_model>

<verification>
- `backend/venv/Scripts/python.exe -m pytest tests/test_migration_121.py tests/test_200_phase_counts.py tests/test_188_workflow_run_read.py tests/test_workflow_phase_cancel.py tests/test_l01_finish_run_terminal_guard.py tests/test_harness_engine.py tests/test_harness_resume.py -q` — compared **per file** against `200-CHECKLIST.md` §6, with `test_thread_workflow_state_shape` named as the one pre-existing RED.
- `backend/venv/Scripts/python.exe -m pytest tests/unit -q` — no NEW failures against the §6 baseline.
- `cd frontend && npx tsc -p tsconfig.app.json --noEmit` exits 0. (⚠ a bare `tsc --noEmit` checks ZERO files.)
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root — **no per-file decrease** against §6 and `failed 0`. ⚠ If it reds: capture failing filenames from the gate's own persisted JSON **before re-running anything**, check each against `git diff --numstat`, and **never reach for the cap** (SEED-171). Red is sometimes REAL — `196-08` hit `failed 249` and every one was genuine.
- `git show --stat` on the ledger commit shows `api/workflow_runs.py`, `CLAUDE.md` and `docs/HOT-FILE-LEDGER.md` together.
</verification>

<success_criteria>
- ROADMAP SC#2 (wire half): `workflow_phases` carries `started_at` + `completed_at`, written at **all seven** transition sites, with `skip_phase` deliberately writing neither.
- ROADMAP SC#3 (wire half): a count is emitted **only** where the phase type declared one from a fact in its own output; three of seven types emit no key at all, and `count: 0` is proved emittable and distinct from absence.
- ROADMAP SC#5 (migration half): the migration applied via the SQL editor, `full-schema.sql` regenerated by script with no `--reset`, and `check-deploy-drift.sh` clean.
- The four transports agree; the run page and the chat panel read the same facts.
</success_criteria>

<output>
Create `.planning/phases/200-the-workflow-journey/200-02-SUMMARY.md` when done.
</output>
