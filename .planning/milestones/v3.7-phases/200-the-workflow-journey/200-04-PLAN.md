---
phase: 200-the-workflow-journey
plan: 04
type: execute
wave: 4
depends_on: ["200-01", "200-03"]
files_modified:
  - frontend/src/components/workflows/toolNames.ts
  - frontend/src/components/workflows/PhaseFormPanel.tsx
  - frontend/src/components/workflows/PhaseFormPanel.test.tsx
  - frontend/src/components/workflows/PhaseFormPanel.rails.test.tsx
  - frontend/src/components/workflows/StepCardSection.tsx
  - frontend/src/components/workflows/stepCardSectionContext.ts
  - frontend/src/components/workflows/toolNames.test.ts
  - scripts/vitest-count-gate.cjs
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
autonomous: true
requirements: [DES-02]

must_haves:
  truths:
    - "D-01 — the `step-panel` screen reports N/N atoms against `200-CHECKLIST.md` §1 or NAMES the miss; the MUST NOT RENDER half is checked as strictly as the MUST RENDER half"
    - "D-02 — SP-1 and SP-2 are BUILT (blue), SP-3 is VERIFIED not rebuilt (green), SP-4 and SP-5 are REPORTED with named re-open triggers (amber outside the slice)"
    - "D-11 — `PhaseFormPanel.test.tsx`'s ABSOLUTE-ZERO hook pin is honoured by EXTRACTION and passes UNEDITED; any state this screen needs lives in a new leaf, and leaf sprawl is the accepted, stated cost"
    - "D-12 — `WorkflowDoorSwitch.baseline.test.tsx` is NOT in this phase's blast radius: the doors screen is deferred, so its byte-for-byte pin is never approached and never re-baselined"
    - "The EIGHTH live WR-04 prototype-key sink — `friendlyToolName` in this plan's own primary file — is closed RED-first through `own()`"
    - "No user-visible tool id renders as raw snake_case, and the SP-1 coverage figure is the one MEASURED in `200-CHECKLIST.md`, never the stale `3 of 27`"
  artifacts:
    - path: "frontend/src/components/workflows/toolNames.ts"
      provides: "The ONE tool-id → human-phrase home, read through own()"
      contains: "own("
    - path: "frontend/src/components/workflows/toolNames.test.ts"
      provides: "The RED-first prototype-key case and the coverage assertion against rails.toolOptions"
  key_links:
    - from: "frontend/src/components/workflows/PhaseFormPanel.tsx"
      to: "frontend/src/components/workflows/toolNames.ts"
      via: "import — the panel spells no tool phrase inline"
      pattern: "from \"./toolNames\""
    - from: "frontend/src/components/workflows/toolNames.ts"
      to: "frontend/src/components/workflows/ownProperty.ts"
      via: "own(), never TABLE[key] ?? fallback"
      pattern: "own\\("
---

<objective>
Build the `step-panel` screen to `200-CHECKLIST.md` §1: human-named tool phrases instead of raw
`snake_case` ids, and sheet c4's `MODEL` / `WHAT IT CAN REACH` / `WHAT IT CHANGES OUTSIDE THIS
WORKFLOW` card sections composed from parts that already ship.

Purpose: the panel's ingredients exist — `modelFitness.ts`, the governance dial and the readiness
contract all ship — and were never composed into the sheet's shape. SP-1's inconsistency *"reads worse
than the noise"*: a handful of named tools beside a list of raw ids looks half-finished.

Output: a tool-phrase vocabulary leaf that closes the **eighth** live prototype-key sink in this tree,
the composed card sections, and a `MUST NOT RENDER` fence built to actually fire.

⚠ **`depends_on` includes `200-03` as a DISPATCH constraint, not a code dependency.** This screen is
authoring-side and needs nothing from the wire slice; it is sequenced after `200-03` because `200-02`
and `200-03` must each run alone (a DB-mutating migration test, and a shared `phase_types.py`).
This plan is the ROADMAP's `200-03`, shifted by one — see `200-03`'s recorded deviation.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⚠ **WORKTREE BOOTSTRAP IS YOUR FIRST ACTION, BEFORE ANYTHING ELSE:**
```bash
bash scripts/bootstrap-worktree.sh "$(pwd)"
```
`workflow.use_worktrees` is **true**, so this plan runs in an isolated worktree. `git worktree add`
checks out TRACKED files only, and `backend/venv` (1.7 GB), `frontend/node_modules` (541 MB),
`backend/.env` and `frontend/.env.local` are all gitignored. The bootstrap junctions the two large
trees and copies the two env files. It is idempotent and fails loudly. **A worktree that skipped it
reports green typechecks and red tests for reasons that look like this plan's fault** — and every
`<verify>` block below invokes `backend/venv/Scripts/python.exe -m pytest`, `npx vitest run` or
`npx tsc -p tsconfig.app.json --noEmit`, so a skipped bootstrap masks exactly the defects this plan
exists to catch.

⚠ **NEVER `rm -rf` a worktree.** A recursive delete FOLLOWS the junction and destroys the operator's
real 1.7 GB `venv`, silently. Tear down only with `bash scripts/teardown-worktree.sh <path>`.

⚠ **Assert your base SHA before reading the plan.** This phase was planned at
`fe40ce1ce6af06ae3735aa98a539f0f4463abbdf`. Run `git rev-parse HEAD` as your second action and record
it in the SUMMARY. Twelve of twelve worktrees in Phase 192 forked from a `master` merge commit
rather than the orchestrator's HEAD.

⚠ **`GSD_VITEST_MAX_WORKERS=2` on every vitest invocation** — in the prose of an acceptance
criterion as well as in the `<verify>` command. The cap read `4` until 2026-08-14 and **ROTTED**;
at the current gated total, cap 4 measured `failed` 17 / 4 / 3 on one identical commit while cap 2
measured 0. ⚠ **If a gate run reds, do NOT reach for the cap** — capture the failing filenames from
the gate's own persisted JSON BEFORE re-running anything, check each against
`git diff --numstat fe40ce1c HEAD`, and only then judge. Red is not always flake:
`196-08` hit `failed 249` and every one was real.
</execution_context>

<context>
@.planning/phases/200-the-workflow-journey/200-CHECKLIST.md
@.planning/phases/200-the-workflow-journey/200-PATTERNS.md
@.planning/phases/200-the-workflow-journey/200-RESEARCH.md
@.claude/skills/sketch-findings-agentic-rag/SKILL.md
@.claude/skills/sketch-findings-agentic-rag/references/icon-convention.md
@.planning/sketches/200-journey-interactive/screens/step-panel.html
@frontend/src/components/workflows/FieldGuidance.tsx
@frontend/src/components/workflows/fieldGuidanceContext.ts
@frontend/src/components/workflows/ownProperty.ts

<interfaces>
<!-- The guard leaf every keyed lookup on this screen must route through. Zero imports BY CONTRACT. -->
<!-- frontend/src/components/workflows/ownProperty.ts -->
export function own<T>(table: Record<string, T>, key: string): T | undefined {
  return Object.prototype.hasOwnProperty.call(table, key)
    ? (table as Record<string, T>)[key]
    : undefined
}

<!-- THE LIVE DEFECT this plan closes — frontend/src/components/workflows/PhaseFormPanel.tsx:873-883 -->
<!-- map["constructor"] resolves the inherited Object.prototype.constructor: a FUNCTION, never nullish, -->
<!-- so `??` never fires and a FUNCTION is returned into JSX at :638, :733 and :791. -->
function friendlyToolName(id: string): string {
  const map: Record<string, string> = { search_documents: "Search documents", /* …4 more */ }
  return map[id] ?? id
}

<!-- The disclosure-leaf split (`199-06`'s precedent) — a component file may not export a shared HOOK. -->
<!-- FieldGuidance.tsx + fieldGuidanceContext.ts: 27 files do this; ZERO eslint disables exist. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SP-1 — one home for the tool phrases, and close the EIGHTH WR-04 sink RED-first</name>
  <read_first>
    - `frontend/src/components/workflows/PhaseFormPanel.tsx:873-883` (`friendlyToolName` — the live defect) and its three call sites at `:638`, `:733`, `:791`
    - `frontend/src/components/workflows/ownProperty.ts` (the guard, zero imports by contract) and its shipped call sites `WorkflowCanvas.tsx:699,738` / `editAffordance.ts:540,543`
    - `frontend/src/components/workflows/PhaseFormPanel.rails.test.tsx:90` (the non-rails **free-text comma field** the author types tool ids into — this is how an arbitrary key reaches the map), `:484` (`toolOptions` was NOT widened — the three capabilities are absent from the source), `:526-540` (the `import.meta.glob` scan: **no module other than `PhaseFormPanel.tsx` may export a phase-config form component** — read it before naming the new leaf)
    - `frontend/src/components/workflows/doorVocabulary.ts` (the one-string-home module shape)
    - `.planning/phases/200-the-workflow-journey/200-CHECKLIST.md` §0 (X-13 — the MEASURED coverage pair) and §1 (the SP-1 atom rows)
    - `.planning/phases/200-the-workflow-journey/200-PATTERNS.md` IDIOM-1, § 19
  </read_first>
  <behavior>
    - `toolName("search_documents")` returns the human phrase, not the id.
    - ⚠ **RED-FIRST:** `toolName("constructor") === "constructor"` — today's `map[id] ?? id` returns a FUNCTION here, so this case must be written and observed RED before the fix.
    - `toolName("__proto__")` returns `"__proto__"`.
    - `toolName("a_tool_nobody_named")` returns the id unchanged (the honest fallback stays).
    - Every id offered by `rails.toolOptions` resolves to a phrase — coverage asserted as a set, not a count.
  </behavior>
  <files>frontend/src/components/workflows/toolNames.ts, frontend/src/components/workflows/toolNames.test.ts, frontend/src/components/workflows/PhaseFormPanel.tsx</files>
  <action>
    Create `toolNames.ts` as a pure leaf (type-only imports, no React) holding the whole tool-id →
    human-phrase table and a `toolName(id: string): string` reader that routes through `own()` from
    `./ownProperty` — **never `TABLE[key] ?? fallback`**, because `map["constructor"]` resolves the
    inherited `Object.prototype.constructor`, which is a function and therefore never nullish, so the
    coalesce never fires and a **function is returned into JSX** at three call sites today.

    Write the failing cases first (see `<behavior>`), observe the `constructor` case RED against the
    shipped `friendlyToolName`, then move the map out of `PhaseFormPanel.tsx`, delete
    `friendlyToolName`, and repoint all three call sites to `toolName`.

    **Widen the coverage, and measure rather than inherit.** SP-1's ledger note says *"24 raw
    `snake_case` tool ids against 3 human-named ones"* — ⚠ **that number is stale (X-13): the shipped map
    has FIVE entries.** `200-CHECKLIST.md` §0 carries the re-measured pair. Derive the target set from
    `rails.toolOptions` (the ids the panel actually offers) and add a phrase for every one, using the
    sketch's own wording where it exists (`step-panel.html` draws twelve human-named tool phrases plus
    *"Pick from the tools this workspace allows — you cannot add one by typing."*). Assert coverage as a
    **set difference against `toolOptions`**, so adding an option later fails the test rather than
    silently regressing the screen.

    ⚠ **Naming constraint:** `PhaseFormPanel.rails.test.tsx:526` runs an `import.meta.glob` scan
    asserting *no module other than `PhaseFormPanel.tsx` exports a phase-config form component*. A
    vocabulary leaf is not one — but read `:526-540` and confirm the predicate before choosing the file
    name and its exports.
    ⚠ **Do not spell `useState(` / `useMemo(` / `useEffect(` in any prose you add to
    `PhaseFormPanel.tsx`.** Its pin is a **substring scan over the whole file, comments included**, and
    that trap has already caught three authors in this exact file. Build tokens, never spell them.
  </action>
  <acceptance_criteria>
    - `grep -c "friendlyToolName" frontend/src/components/workflows/PhaseFormPanel.tsx` returns 0.
    - `grep -c "own(" frontend/src/components/workflows/toolNames.ts` ≥ 1 and `grep -cE '\]\s*\?\?' frontend/src/components/workflows/toolNames.ts` returns 0.
    - `toolNames.test.ts` contains a case asserting `toolName("constructor") === "constructor"` and one asserting `toolName("__proto__") === "__proto__"`, **and the plan summary records the RED observation of the first against the pre-fix implementation**.
    - A coverage case asserts `toolOptions.filter(id => toolName(id) === id)` is empty — a set assertion, not a count.
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/toolNames.test.ts src/components/workflows/PhaseFormPanel.test.tsx src/components/workflows/PhaseFormPanel.rails.test.tsx --reporter=basic` is green, **and `PhaseFormPanel.test.tsx`'s ABSOLUTE-ZERO hook pin passes UNEDITED** (`git diff --numstat` on that file shows no change to the pin block).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/toolNames.test.ts src/components/workflows/PhaseFormPanel.test.tsx src/components/workflows/PhaseFormPanel.rails.test.tsx --reporter=basic</automated>
  </verify>
  <done>Tool phrases have one home read through `own()`, the eighth WR-04 sink is closed with an observed RED, and every offered tool id resolves to a phrase.</done>
</task>

<task type="auto">
  <name>Task 2: SP-2 — compose sheet c4's card sections, honouring the ABSOLUTE-ZERO pin by EXTRACTION</name>
  <read_first>
    - `.planning/sketches/200-journey-interactive/screens/step-panel.html` (the drawn sections: `What it does` · `Model` + `Strong for judging` + `We couldn't load the list of models.` · `What it can reach` + the lock line · `What this step can do` · `What it changes outside this workflow` (`Writes to your database` `NEEDS ARMING`, `Sends a Slack message`) · `Files it starts from` · `How strictly it is held` (`Loose`/`Strict`) · `2 things still missing`)
    - `frontend/src/components/workflows/PhaseFormPanel.tsx` (the shipped form; the four `<ModelField` mounts and the one gated `<TemplateNameCheck` mount)
    - `frontend/src/components/workflows/PhaseFormPanel.test.tsx:626-631`, `:676-677`, `:693-721` (the mount fences, the non-vacuity floor, the ABSOLUTE-ZERO pin **and its positive control**) and its docblock at `:634-661`
    - `frontend/src/components/workflows/FieldGuidance.tsx` + `fieldGuidanceContext.ts` (`199-06`'s precedent, created for exactly this, including the `onMouseDown` suppression and the default-degrades-to-shipped rule)
    - `frontend/src/components/workflows/modelFitness.ts` (own-property lookup, **never `?? floor`**) and the shipped governance dial / readiness contract
    - `.planning/phases/200-the-workflow-journey/200-CHECKLIST.md` §1 (`SP-MR-*` rows) and §0 (N-6 — the three stale model literals are placeholder text, not atoms)
    - `.planning/phases/200-the-workflow-journey/200-PATTERNS.md` § 10, § 19
  </read_first>
  <files>frontend/src/components/workflows/StepCardSection.tsx, frontend/src/components/workflows/stepCardSectionContext.ts, frontend/src/components/workflows/PhaseFormPanel.tsx, frontend/src/components/workflows/PhaseFormPanel.test.tsx</files>
  <action>
    Compose the sheet's card sections from parts that already ship — `modelFitness.ts`, the governance
    dial, the readiness contract, `ModelField`. **The sheet asks for them in a card SHAPE, not for new
    capability.** Do not invent a control that has no backing fact.

    ⚠ **D-11 — the hook pin is honoured by EXTRACTION, never re-baselined.**
    `PhaseFormPanel.test.tsx:711-716` asserts an **ABSOLUTE ZERO** of `useMemo(` / `useState(` /
    `useEffect(` over the panel's own `?raw` source — *"not 'no increase' — zero, because all three
    measured zero before this phase, and a non-decrease criterion on a file that already reads 0 is a
    criterion that permits the first one."* So any state a card section needs (a disclosure switch, a
    collapsed/expanded card) lives in a new leaf — `StepCardSection.tsx` for the component and
    `stepCardSectionContext.ts` for the context + the hook + its two control words, because **a
    component file may not export a shared HOOK** (`react-refresh/only-export-components`; 27 files in
    this tree answer that with a leaf module and **zero eslint disables exist**).

    ⚠ **The honest cost, stated rather than discovered: leaf sprawl.** 199 already added one leaf; if all
    three card sections need disclosure state, that is up to three more. Accepted deliberately — the
    alternative removes the only guard keeping a 1290-line panel that fires G-5 at 11 phases from
    absorbing state.

    Binding invariants:
    - **`onMouseDown={(e) => e.preventDefault()}` on every new control inside the panel.** The panel's
      persist seam is a field's `onBlur`; a press moves focus off the focused field and PATCHes a
      version. This is the 184-11 trap and it has already bitten once.
    - **The default must degrade to the SHIPPED surface, not to an emptier one.** `FieldGuidance`'s
      default is `true` for exactly this reason: a consumer rendered outside a provider must behave as it
      did before the module existed, so a missing wrapper cannot look like a successful subtraction.
    - **Keep the shipped mount fences intact**: exactly ONE `<TemplateNameCheck` line gated
      `pt === "llm_emit"`; exactly FOUR `<ModelField` mounts compared as a sorted SET over
      `["llm_agent","llm_batch_agents","llm_emit","llm_single"]`, each forwarding `{...modelPicker}` and
      `onPersist`; `showFitness` on the `llm_emit` mount and only it; `programmatic` and
      `llm_human_input` still showing no model, no tools and no folder scope.
    - **SP-3 is a green row — VERIFY, do not rebuild.** `199-06` already ships the absent-registry arm
      (*"We couldn't load the list of models."*). Assert it still renders; **emphatically do not add a
      free-text box** when the registry is absent (AUTH-04, pinned at `PhaseFormPanel.test.tsx:723+`).
    - **N-6:** the sheet's `GPT-4o` / `Claude 3.5 Sonnet` / `Llama 3 Instruct` are placeholder text.
      Models come from the registry via `useModelRegistry` / `ModelField` — **no model literal is a
      shippable atom.**
    - **N-5 / the icon convention:** no Material Symbols ligature name (`description`, `bolt`, `lock`,
      `shield`, …) is a shippable atom. `icon-convention.md` §1/§2/§4 is the authority; a net-new mark is
      **flagged as a proposal**, never passed off as shipped vocabulary.
    - ⚠ **Any keyed lookup you add** (fitness tier → words, gate kind → label, capability → sentence)
      **routes through `own()` or `hasOwnProperty.call`.** `modelFitness.ts`'s own rule: never `?? floor`,
      because a coalesce returns `constructor`.
    - ⚠ Say `use` + `State(` nowhere in this file's prose.
    - **D-12:** `WorkflowDoorSwitch.tsx` and `WorkflowDoorSwitch.baseline.test.tsx` are **not** in this
      plan's blast radius — the doors screen is deferred, its byte-for-byte pin is never approached, and
      nothing here may re-baseline it. Assert it: `git diff --numstat` names neither file.
  </action>
  <acceptance_criteria>
    - `PhaseFormPanel.test.tsx`'s ABSOLUTE-ZERO pin passes and **its assertion block is byte-unchanged**: `git diff -U0 -- frontend/src/components/workflows/PhaseFormPanel.test.tsx | grep -c '^-.*ABSOLUTE zero'` returns 0.
    - `grep -cE 'use(State|Memo|Effect)\(' frontend/src/components/workflows/PhaseFormPanel.tsx` returns 0.
    - Every new control inside the panel carries `onMouseDown` with `preventDefault`.
    - The four `<ModelField` mounts, the one gated `<TemplateNameCheck` mount and the `showFitness` placement are all unchanged — `GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/PhaseFormPanel.test.tsx src/components/workflows/PhaseFormPanel.rails.test.tsx` is green.
    - A case asserts the absent-registry arm still renders its shipped sentence **and that no free-text tool/model input appears** (SP-3 verify + AUTH-04).
    - `git diff --numstat` for this plan names neither `WorkflowDoorSwitch.tsx` nor `WorkflowDoorSwitch.baseline.test.tsx` (D-12).
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; test $(grep -cE "use(State|Memo|Effect)\(" frontend/src/components/workflows/PhaseFormPanel.tsx) -eq 0; test -z "$(git diff --numstat | grep WorkflowDoorSwitch || true)"; cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/PhaseFormPanel.test.tsx src/components/workflows/PhaseFormPanel.rails.test.tsx --reporter=basic && npx tsc -p tsconfig.app.json --noEmit'</automated>
  </verify>
  <done>The c4 card sections render from shipped parts, all panel state lives in leaves, the ABSOLUTE-ZERO pin passes unedited, and the doors pin was never approached.</done>
</task>

<task type="auto">
  <name>Task 3: The `step-panel` MUST NOT RENDER fence — built to fire — plus the REPORT rows and the gate pins</name>
  <read_first>
    - `frontend/src/components/workflows/PublishGauntlet.test.tsx:1095-1334` — the **role-SET scan** with its planted positive controls; ⚠ the only fence in this tree **proved able to fire against a live planted violation**
    - `frontend/src/components/workflows/library/librarySubtree.fences.test.ts:82-210` — the complementary half: an explicit path list with `toHaveLength`, and the **non-vacuity-before-contents** rule
    - `.planning/phases/200-the-workflow-journey/200-CHECKLIST.md` §1 (the `SP-MNR-*` rows) and §5 (SP-4 / SP-5 and their triggers)
    - `scripts/vitest-count-gate.cjs:2540-2546` (TARGETS and BASELINE are TWO knobs) and `200-CHECKLIST.md` §6 (this phase's baseline)
    - `docs/HOT-FILE-LEDGER.md` → `frontend/src/components/workflows/PhaseFormPanel.tsx` (its existing section) and CLAUDE.md's row for the same file
    - `.planning/phases/200-the-workflow-journey/200-PATTERNS.md` § 9, § 20
  </read_first>
  <files>frontend/src/components/workflows/PhaseFormPanel.test.tsx, scripts/vitest-count-gate.cjs, CLAUDE.md, docs/HOT-FILE-LEDGER.md</files>
  <action>
    **A — the fence.** Add a `MUST NOT RENDER` block covering `200-CHECKLIST.md` §1's `SP-MNR-*` rows:
    no raw `snake_case` tool id in the rendered tool list; none of N-6's three model literals; no Material
    Symbols ligature name as visible text.

    ⚠ **Build it to FIRE.** `199-03` measured a `?raw` source regex **AND** a
    `queryAllByRole("button")` filter **both passing GREEN** against a live planted violation
    (`<a href="/publish?force=1">Proceed to publish anyway</a>`) — a source regex cannot see a control
    composed from a variable, and a button scan cannot see a link. **Only a role-SET scan went red.**
    So: scan the **rendered DOM** over a role SET (`button, a[href], [role="button"], [role="link"],
    [role="menuitem"], input[type="submit"], input[type="button"]`) plus the rendered text nodes, and
    ship **two permanent non-vacuity controls** in the `PublishGauntlet` shape:
    1. a **planted-violation** case that renders a raw `snake_case` id, a model literal and a ligature
       name, and asserts the predicate finds **all three** — this is what keeps the absence assertion
       falsifiable rather than vacuous, and it is committed permanently;
    2. a **deliberate-absence** case proving the predicate does NOT fire on the honest shipped copy, so
       nobody has to delete a true sentence to go green.
    Assert non-vacuity **before** contents (source length > 1000, the export name present), or every
    negative below passes against the empty string.
    ⚠ Do not spell the forbidden tokens in this file's own prose in a way a `?raw` scan would count —
    build them.

    **B — the REPORT rows.** Add two cases that *document* rather than build, so nothing is silently
    dropped: **SP-4** (`Locked by Alex M.`) — lock-holder attribution is not on the wire, and per §0's N-7
    the sketch's own screen draws the person-less form, which already ships; assert the shipped
    person-less lock sentence renders and reference the trigger *a phase that scopes lock ownership*.
    **SP-5** (`Will overwrite 1,200 records`) — no row count is computed anywhere; assert that **no
    fabricated row count renders**, with the trigger *the connections / approval milestone (`SEED-146`)*.
    ⚠ A preflight count is a capability, not a label — inventing one here would be exactly the fabricated
    business figure `199-05` refused.

    **C — the gate pins.** Add a `BASELINE` entry for every file this plan created
    (`toolNames.test.ts`, and any disclosure-leaf suite) **in this same commit** — *"an unpinned file is
    not a lightly-guarded one, it is an unguarded one."* Check **both knobs**: files under
    `src/components/workflows` are already RUN by the directory entry in `TARGETS`, so what is owed is
    the `BASELINE` entry; anything outside that directory needs both. Raise `PhaseFormPanel.test.tsx`'s
    pin to its new actual. ⚠ **Note the asymmetry: ADDING a pin is `+n / -0`; RAISING one necessarily
    DELETES a line**, so a `grep -c '^-[^-]'` expecting 0 is wrong for a raise and right for an add.

    **D — the ledger.** `PhaseFormPanel.tsx` already has a row and a section; refresh both with the
    triple re-derived in `200-CHECKLIST.md` §6 (RESEARCH measured `22 / 11 / 1290`; ⚠ CONTEXT's D-14 says
    `10` phases — X-7), record this phase's edit as *honoured by construction + D-11*, and state the next
    seam. ⚠ **A row that is present and WRONG answers the auditor with `satisfied` and stops the audit,
    which is worse than an absent row.** Then run `node scripts/check-claude-md-size.cjs`.

    Finally, run the phase gate and compare **per file** against `200-CHECKLIST.md` §6. ⚠ **If it reds,
    do NOT reach for the cap.** Capture failing filenames from the gate's own persisted JSON **before
    re-running anything**, check each against `git diff --numstat <base> HEAD` and `git status --short`,
    and if a named file is byte-unchanged and one of SEED-171's five, record it as an observation and say
    *"provably unmodified"*, never *"fine"*. ⚠ **Red is sometimes REAL** — `196-08` hit `failed 249` and
    every one was genuine.
  </action>
  <acceptance_criteria>
    - The fence block contains a role-SET selector string listing at least the seven roles, and **two** permanent non-vacuity cases (a planted violation that goes red, and a deliberate absence that stays green).
    - The planted-violation case asserts a count ≥ 3 (one plant per forbidden class), so a selector typo fails THIS case first.
    - Cases exist for SP-4 and SP-5, each naming its re-open trigger in the case name or an inline comment.
    - `scripts/vitest-count-gate.cjs` gains a `BASELINE` entry for every file this plan created; `git diff -U0 -- scripts/vitest-count-gate.cjs | grep -c '^-[^-]'` equals the number of pins RAISED (0 if only additions).
    - `docs/HOT-FILE-LEDGER.md`'s `PhaseFormPanel.tsx` section and CLAUDE.md's row both carry the re-derived triple, in this commit; `node scripts/check-claude-md-size.cjs` exits 0.
    - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root: **no per-file decrease** against `200-CHECKLIST.md` §6, and any failing file is triaged by the procedure above rather than by the cap.
    - The plan summary reports `step-panel: N/N atoms` against §1, or **names the miss**.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; node scripts/check-claude-md-size.cjs; grep -q "PhaseFormPanel" docs/HOT-FILE-LEDGER.md; (cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/PhaseFormPanel.test.tsx --reporter=basic); GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs'</automated>
  </verify>
  <done>The step-panel fence can fire, both REPORT rows are recorded with triggers, every new file is pinned in its creating commit, and the screen reports N/N or names the miss.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author-typed tool ids → an object-literal index | The non-rails variant offers a **free-text comma field** (`rails.test.tsx:90`), so an arbitrary string reaches a keyed lookup |
| workflow definition → panel render | `workflow_phases.slug` and `available_tools` are unconstrained `text` (`058:18`; `harness.py:202`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-200-04-01 | Tampering | `friendlyToolName` → `toolNames.ts` | mitigate | **The EIGHTH live WR-04 sink.** `map[id] ?? id` returns `Object.prototype.constructor` — a function — into JSX at three call sites. Route through `own()`, proved by a RED-first case asserting `toolName("constructor") === "constructor"` and a second for `__proto__` |
| T-200-04-02 | Tampering | any new keyed lookup on this screen (fitness tier, gate kind, capability) | mitigate | `own()` / `hasOwnProperty.call` only; **never `?? fallback`** — `modelFitness.ts`'s own recorded rule |
| T-200-04-03 | Spoofing | fabricated consequence copy | mitigate | SP-5 is REPORTED, not built: **no row count is computed anywhere**, and a fabricated *"Will overwrite 1,200 records"* is the class of lie `199-05` refused. A fence asserts no such figure renders |
| T-200-04-04 | Elevation of Privilege | the absent-registry arm | mitigate | AUTH-04: an unreadable registry yields **no control at all — emphatically no free-text box**; pinned at `PhaseFormPanel.test.tsx:723+` and re-asserted here |
| T-200-04-05 | Repudiation | a re-baselined pin | mitigate | D-11: the ABSOLUTE-ZERO pin passes **unedited**; `git diff` on its assertion block must be empty. *A pin relaxed to make red go green is a pin that will never fail again* |
| T-200-04-SC | Tampering | npm/pip/cargo installs | mitigate | **No package installs.** No `package.json` or lockfile in `files_modified`; every atom composes shipped modules. RESEARCH carries no `## Package Legitimacy Audit`, so any package would be `[ASSUMED]` and need a blocking human checkpoint first |
</threat_model>

<verification>
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/toolNames.test.ts src/components/workflows/PhaseFormPanel.test.tsx src/components/workflows/PhaseFormPanel.rails.test.tsx --reporter=basic` — green, with the ABSOLUTE-ZERO pin **unedited**.
- `cd frontend && npx tsc -p tsconfig.app.json --noEmit` exits 0.
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root — per-file deltas compared against `200-CHECKLIST.md` §6; SEED-171 triage applied if red, never the cap.
- `node scripts/check-claude-md-size.cjs` exits 0.
- The summary reports `step-panel: N/N atoms` or names the miss, and records the RED observation for the `constructor` case.
</verification>

<success_criteria>
- ROADMAP SC#1 for `step-panel`: N/N atoms against `200-CHECKLIST.md` §1, or a named miss; the `MUST NOT RENDER` half checked as strictly as the `MUST RENDER` half, with a fence proved able to fire.
- SP-1 and SP-2 BUILT; SP-3 VERIFIED not rebuilt; SP-4 and SP-5 REPORTED with triggers.
- The eighth WR-04 sink is closed and the class guard (`own()`) is now the panel's only lookup idiom.
</success_criteria>

<output>
Create `.planning/phases/200-the-workflow-journey/200-04-SUMMARY.md` when done.
</output>
