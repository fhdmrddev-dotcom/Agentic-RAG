---
phase: 192.1-workflow-identity
plan: 07
type: execute
wave: 6
depends_on: ["192.1-06"]
files_modified:
  - frontend/src/components/workflows/library/ForkNameDialog.tsx
  - frontend/src/components/workflows/library/ForkNameDialog.test.tsx
  - frontend/src/components/workflows/library/useWorkflowFork.ts
  - frontend/src/components/workflows/library/WorkflowCard.tsx
  - frontend/src/components/workflows/library/librarySubtree.fences.test.ts
  - frontend/src/pages/WorkflowsPage.tsx
  - frontend/src/pages/WorkflowsPage.test.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [LIB-05]
gap_closure: false

must_haves:
  truths:
    - "Forking a workflow asks for a name before creating anything, and the copy arrives under that name"
    - "Typing a name you already have WARNS and still lets you proceed; leaving it empty is the only thing that blocks"
    - "Forking a row you have already forked shows NO prompt and opens the existing draft, exactly as it did before"
    - "The prompt is an INPUT, not a guard — its primary button says 'Create my copy', never 'Confirm', and it wears no destructive styling"
  artifacts:
    - path: "frontend/src/components/workflows/library/ForkNameDialog.tsx"
      provides: "162-B's name prompt on the shadcn Dialog primitive, with a described input and a warn-never-block hint"
      contains: "FORK_DIALOG_OK"
    - path: "frontend/src/components/workflows/library/WorkflowCard.tsx"
      provides: "192's D-15 direct-flip docblock AMENDED, in the same commit as the dialog"
      contains: "input"
  key_links:
    - from: "frontend/src/components/workflows/library/useWorkflowFork.ts"
      to: "frontend/src/components/workflows/library/ForkNameDialog.tsx"
      via: "the hook holds the pending fork and the caller's own names; the dialog collects the name and hands it back"
      pattern: "ForkNameDialog"
    - from: "frontend/src/components/workflows/library/ForkNameDialog.tsx"
      to: "frontend/src/components/workflows/library/libraryVocabulary.ts"
      via: "every string imported — FORK_DIALOG_*, FORK_HINT_*, FORK_CONSEQUENCE"
      pattern: "from \"\\./libraryVocabulary\""
---

<objective>
Ship **162-B — the name prompt at the fork** — on the seam Plan 03 extracted, and **amend 192's D-15
in the same commit** so the guard-vocabulary argument is never left silently contradicted.

Purpose: 43 of the operator's 104 workflows are called "Compliance Gap Report" because every fork
inherits its parent's name. The read-side line (Plans 04-06) tells them apart; this is the write-side
answer that stops the pile growing. **D-19's whole argument is that this is an INPUT, not a guard —
it collects something the system cannot know — and the design carries that claim mechanically rather
than in prose.**
Output: `library/ForkNameDialog.tsx` + suite, the hook wired to hold a pending fork, and the card's
D-15 docblock amended.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⚠ **WORKTREE BOOTSTRAP IS YOUR FIRST ACTION:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`.
**NEVER `rm -rf` a worktree** — `bash scripts/teardown-worktree.sh <path>`.
⚠ Assert the dispatched base SHA and record it. ⚠ `GSD_VITEST_MAX_WORKERS=4` on every vitest run.

⚠ **Load the project design skill:** `Skill("sketch-findings-agentic-rag")`. Any approval /
confirm-sheet / action-guard surface is covered by it, and the graded-guard ladder (146-148) is the
vocabulary this dialog must deliberately stay OUTSIDE of.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/192.1-workflow-identity/192.1-CONTEXT.md
@.planning/phases/192.1-workflow-identity/192.1-RESEARCH.md
@.planning/phases/192.1-workflow-identity/192.1-PATTERNS.md
@.planning/phases/192.1-workflow-identity/192.1-VALIDATION.md
@.planning/sketches/163-the-assembled-card/BUILD-CONTRACT.generated.md
@.planning/sketches/163-the-assembled-card/README.md

<interfaces>
<!-- The strings — ALL already in libraryVocabulary.ts after Plan 04. IMPORT them. -->
  FORK_DIALOG_TITLE  = "Name your copy"
  forkDialogSub(src) = "You are copying “<src>”. Give your copy a name you will recognise later — you can change it in the Builder."
  FORK_DIALOG_OK     = "Create my copy"        ← asserting it is NOT "Confirm" is what makes D-19 mechanical
  FORK_DIALOG_CANCEL = "Cancel"
  FORK_HINT_EMPTY    = "Give it a name."       ← the ONLY hard gate
  FORK_HINT_FREE     = "No other workflow of yours has this name."
  FORK_HINT_CLASH    = "You already have one called this. Allowed — but you will not be able to tell them apart."
  FORK_CONSEQUENCE   = the SHIPPED sentence, rendered verbatim in the dialog — imported, never retyped

<!-- The sketch's exact behaviour, 163/index.html:432-471 -->
  open      → the field is set to "" (never prefilled), then focused; the hint is computed immediately
  validate  → v = value.trim();  if (!v) { hint = FORK_HINT_EMPTY; ok.disabled = true; return }
              clash = allRows().some(r => r.isMine && r.name === v)
              hint = clash ? FORK_HINT_CLASH : FORK_HINT_FREE;  ok.disabled = FALSE   ← warns, never blocks
  submit    → creates under the typed name; Escape closes without creating

<!-- The primitive: ui/dialog.tsx (Radix @radix-ui/react-dialog ^1.1.15), exports at :109-120 —
     Dialog, DialogTrigger, DialogPortal, DialogClose, DialogOverlay, DialogContent, DialogHeader,
     DialogFooter, DialogTitle, DialogDescription.
     NOT AlertDialog: its three shipped consumers are all CONFIRMATIONS, it traps focus on a
     confirm/cancel pair, and reaching for the *alert* primitive would contradict D-19 in the
     component tree while the prose said otherwise. -->

<!-- The hook contract Plan 03 created, which this plan extends -->
  export interface WorkflowFork {
    forkFailed: { name: string; conflict: boolean } | null
    draftBySlug: Map<string, WorkflowDraftRow>
    onForkNewVersion: (row: LibraryRow) => void
    onForkStarter: (row: LibraryRow) => void
  }

<!-- The docblock D-19 AMENDS — WorkflowCard.tsx:79-87, verbatim at HEAD -->
 *   fork           DIRECT FLIP. It is non-destructive and reversible — the live version stays
 *                  live and unchanged — so a sheet on it would spend the guard vocabulary the
 *                  delete relies on to mean anything (`CapabilityGrid.tsx:11-13`). The sketch
 *                  attached a confirm to 159-C; it is deliberately not shipped.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ForkNameDialog.tsx — an input, not a guard (D-19, D-20, D-36)</name>
  <files>frontend/src/components/workflows/library/ForkNameDialog.tsx, frontend/src/components/workflows/library/ForkNameDialog.test.tsx, frontend/src/components/workflows/library/librarySubtree.fences.test.ts, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/components/org/InviteMemberDialog.tsx` lines 25-40 (imports), 85-100 (reset-on-open), 130-150 (shell + header), 190-215 (label ↔ input, static id), 250-280 (the error `role="alert"` and the footer) — ⚠ AND lines 155-165 and 225-232, which carry the two `title=` attributes that must NOT be copied
    - `frontend/src/components/ui/dialog.tsx` lines 100-125 (the export list)
    - `frontend/src/components/workflows/library/RunModal.tsx` lines 285-305 (the label-wraps-control idiom and the one-testid-per-interactive-node habit)
    - `frontend/src/pages/__tests__/RunModal.a11y.test.tsx` lines 20-35 (the axe import), 95-140 (the four Radix jsdom shims and the axe posture), 205-225 and 285-300 (role + accessible-name assertions), 380-400 (the POSITIVE CONTROL habit)
    - `frontend/src/components/workflows/library/LibraryToolbar.tsx` lines 218-230 (the in-subtree `aria-label` + `aria-describedby` wiring on an `<input>` — the closest analog for the name field)
    - `frontend/src/components/workflows/library/libraryVocabulary.ts` (the strings added in Plan 04)
    - `192.1-CONTEXT.md` D-19, D-20, D-21, D-36 · `192.1-RESEARCH.md` § 6a-6d · `192.1-PATTERNS.md` § 4a-4c
  </read_first>
  <behavior>
    - Opening the dialog sets the field to `""` — never prefilled with the source name — and focuses it
    - With an empty (or whitespace-only) field the hint reads `FORK_HINT_EMPTY` and the primary button is DISABLED
    - With a name that no row of the caller's own carries, the hint reads `FORK_HINT_FREE` and the primary is ENABLED
    - With a colliding name the hint reads `FORK_HINT_CLASH` and the primary is **still ENABLED** — warns, never blocks
    - ⚠ POSITIVE CONTROL: submitting THROUGH the clash actually fires the create callback with the typed name — asserting only "the hint is present" is equally satisfied by a dialog that blocks
    - The primary button's accessible name is `Create my copy` and is **NOT** `/confirm/i`
    - The dialog renders `FORK_CONSEQUENCE` verbatim (imported), and exactly ONE consequence sentence
    - The dialog has an accessible name and an accessible description, and `axe()` reports no violations
    - Escape closes the dialog and fires NO create callback
    - Re-opening after a previous use shows an empty field again (reset-on-open)
    - The clash hint is NOT `role="alert"` — it is DOM text wired by `aria-describedby` on the input
  </behavior>
  <action>
Create `frontend/src/components/workflows/library/ForkNameDialog.tsx` on **`ui/dialog.tsx`'s `Dialog`,
not `AlertDialog`** (RESEARCH §6a): `AlertDialog`'s three shipped consumers are all confirmations, it
traps focus on a confirm/cancel pair which is the wrong role for a text input, and — the mechanical
reason — **D-19's whole argument is that this is an INPUT, not a guard.** Reaching for the *alert*
primitive would contradict the decision in the component tree while the prose said otherwise.

Props: `{ open: boolean; sourceName: string; onCancel: () => void; onCreate: (name: string) => void; isClash: (name: string) => boolean }`.

⚠ **D-36 — do NOT clone `InviteMemberDialog.tsx` wholesale.** It carries the F1-forbidden attribute
TWICE (`:159` `title={sentLink}`, `:228` `title={opt.hint}`), and inside `library/` either line reds F1
**by construction** the instant this file joins `LIBRARY_SUBTREE_PATHS`. `<DialogTitle>` is an ELEMENT
and is safe; a `title` **prop** is not, and F1's scoping control (`:195-200`) covers only an object
*property* — which is exactly how the D-07 highlight got deferred. **Copy its Dialog/label/footer
wiring and its reset-on-open effect; copy neither tooltip.**

Close the gap PATTERNS names rather than cloning it: **use `DialogDescription` for
`forkDialogSub(sourceName)`.** Neither `InviteMemberDialog` nor `CreateLinkDialog` uses it, and Radix
warns when `DialogContent` has no accessible description; six other files in the repo do use it.

Label ↔ input with a static id (a singleton dialog), the `InviteMemberDialog.tsx:192-211` shape.
Wire the hint by `aria-describedby` on the input — the in-subtree analog is
`LibraryToolbar.tsx:223-224` and the shipped round-trip is the card's own
`consequenceId = \`wf-fork-${row.id}\`` (`WorkflowCard.tsx:319-325, 412, 421, 485`).
⚠ **The clash hint is NOT `role="alert"`** (D-20 warns and never blocks); `InviteMemberDialog.tsx:255`
is correct THERE because a 422 is a refusal.

**D-20 — the ONLY hard gate is emptiness.** Disable the primary only when `value.trim() === ""`. *A
name you chose is a name you are allowed to have; blocking would make the library's problem the user's
fault.*

**D-19's four mechanical clauses, each of which is a test:** it asks for a NAME, never a confirmation;
it wears **no destructive styling** — no `DESTRUCTIVE_ITEM_CLASSES`, no red, no victim naming; its
primary button is **`Create my copy`** and asserting *"the primary is not `Confirm`"* is what makes the
distinction mechanical; and it **warns and never blocks**.

Render `FORK_CONSEQUENCE` verbatim inside the dialog (imported from `./libraryVocabulary` — never
retyped, never reworded, and **no second consequence sentence** anywhere on the surface).

Give it one `data-testid` per interactive node, in `RunModal.tsx`'s habit (e.g. `fork-name-dialog`,
`fork-name-input`, `fork-name-hint`, `fork-name-create`, `fork-name-cancel`). ⚠ These are NET-NEW
hooks on a NEW component and do not touch D-11's "exactly one net-new hook", which is about the CARD's
`row-identity`. **Do not remove or rename any shipped testid.**

Write `ForkNameDialog.test.tsx` FIRST, RED, covering every behavior above, in
`RunModal.a11y.test.tsx`'s posture: the four Radix jsdom shims (`hasPointerCapture`,
`setPointerCapture`, `releasePointerCapture`, `scrollIntoView` — T-5), `axe()` on the open dialog, and
role + accessible-name assertions. Copy the POSITIVE-CONTROL habit at `:383-397` explicitly for the
warn-never-block pair.

Add `"./ForkNameDialog.tsx"` to `LIBRARY_SUBTREE_PATHS`, update `toHaveLength` to the array's own new
length (D-33/D-35 — read it from the failing assertion, and note this is the FIFTH and final source
module, so the corpus should now be 12 and the suite 79; **verify both from the gate's `actual`
column rather than from that arithmetic**). Raise the fences and `ForkNameDialog.test.tsx` pins in the
same commit from two agreeing gate runs.
  </action>
  <acceptance_criteria>
    - `grep -n "title=" frontend/src/components/workflows/library/ForkNameDialog.tsx` returns NOTHING
    - `grep -n "AlertDialog" frontend/src/components/workflows/library/ForkNameDialog.tsx` returns NOTHING
    - `grep -c "DialogDescription" frontend/src/components/workflows/library/ForkNameDialog.tsx` is ≥ 1
    - `grep -n "role=\"alert\"" frontend/src/components/workflows/library/ForkNameDialog.tsx` returns NOTHING
    - `grep -niE "\"Confirm\"|>Confirm<|DESTRUCTIVE_ITEM_CLASSES|text-red|bg-red|destructive" frontend/src/components/workflows/library/ForkNameDialog.tsx` returns NOTHING
    - Every user-visible string in the file is an imported identifier, not a literal — verified by reading, and by `grep` showing no new sentence-shaped string literal
    - A test asserts the primary button's accessible name is `Create my copy` AND that `getByRole("button", { name: /confirm/i })` finds nothing, paired with a positive control
    - A test submits THROUGH a clash and asserts `onCreate` fired with the typed name
    - An `axe()` case reports no violations on the open dialog
    - `LIBRARY_SUBTREE_PATHS` contains `"./ForkNameDialog.tsx"` and `toHaveLength` matches
    - The RED was observed as a real `AssertionError` and both outputs are quoted in the SUMMARY
    - `cd frontend && npm run lint:a11y` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/library/ForkNameDialog.test.tsx src/components/workflows/library/librarySubtree.fences.test.ts</automated>
  </verify>
  <done>A shadcn-`Dialog` name prompt exists that asks rather than confirms, warns without blocking, carries an accessible description, wears no destructive styling, imports every string, has zero `title=` attributes, and is proved RED-first including an axe case and a submit-through-the-clash positive control.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the prompt into the fork seam without regressing 192-14 (D-21, D-22, D-23, D-24)</name>
  <files>frontend/src/components/workflows/library/useWorkflowFork.ts, frontend/src/pages/WorkflowsPage.tsx, frontend/src/pages/WorkflowsPage.test.tsx, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/components/workflows/library/useWorkflowFork.ts` (whole file as Plan 03 left it — the two handlers, the D-23 early-return branch, the D-24 retry loop, `onForked`)
    - `frontend/src/pages/WorkflowsPage.test.tsx` lines 820-1160 (the twelve fork-arc cases, especially `:886`'s 192-14 block and `:1081`'s T-192-42 two-call pin)
    - `frontend/src/components/workflows/library/libraryFilter.ts` lines 143-166 (`CHIP_PREDICATES.yours` — the ownership predicate the pre-flight must reuse)
    - `192.1-CONTEXT.md` D-21, D-22, D-23, D-24 · `192.1-RESEARCH.md` § 6c, § 1b · `192.1-PATTERNS.md` § 3
  </read_first>
  <action>
Extend `useWorkflowFork` so the two CREATE paths ask for a name before writing:

1. Hold a `pendingFork: { row: LibraryRow; kind: "version" | "starter" } | null` state.
2. `onForkNewVersion(row)` — **first** run 192-14's existing-draft branch. If
   `draftBySlug.get(row.slug)` hits, call `onOpenDraft(existing)` and **return: no prompt, nothing
   created** (**D-23**). Only when there is no existing draft does it set `pendingFork`.
3. `onForkStarter(row)` sets `pendingFork` directly (a starter fork always creates).
4. `confirmFork(name)` runs the existing create path with the typed name applied to the forked
   definition's `name` key, and to the Builder `label`. `cancelFork()` clears `pendingFork` and writes
   nothing.
5. Return `pendingFork`, `confirmFork`, `cancelFork` and `isClash` so the page can mount the dialog —
   or expose a ready-made `forkDialog` element; either is acceptable provided the page gains no fork
   logic beyond the mount.

**⚠ D-23 IS THE BLOCKER THAT MUST NOT COME BACK.** On a published row the caller has already forked,
the verb still OPENS the existing draft and creates nothing — so **no name prompt appears on that
path, because nothing is being named.** The card's sentence already says so
(`FORK_CONSEQUENCE_EXISTING`, selected by `hasExistingFork`). Regressing this re-opens the U5 blocker
that cost Phase 192 a round. **`WorkflowsPage.test.tsx:886`'s three cases must stay green**, and add
one NEW case asserting that on that path the dialog never mounts.

**⚠ D-24 — `onUseStarter`'s single 409 retry is preserved**, pinned from the inside at exactly two
`createWorkflowDraft` calls (T-192-42). `WorkflowsPage.test.tsx:1081`'s case must stay green. Do not
restructure the loop under cover of the prompt.

**⚠ D-21 — the pre-flight collision check is CLIENT-SIDE, over the caller's OWN rows in the merged
feed, and it checks the DISPLAY NAME.** It is free because the page already holds the list. Implement
`isClash(name)` as `rows.some(r => CHIP_PREDICATES.yours(r) && r.name === name.trim())` — **reusing
the shipped predicate, never a second copy of it** (D-09 / 192's D-03). **It is not, and must not be
presented as, a slug check.**

**⚠ D-22 — WR-08 IS NOT RETIRED HERE, and that is recorded rather than half-done.** Sketch 163 floats
the name field as a natural home for the 409; it is not. The 409 is a `UNIQUE(slug, version)`
violation and the pre-flight reads NAMES. The two residual failing rows are `published + published`
slugs with no draft (`meridian-risk-summary-good-07aedc33`, `readonly_refusal_098uat`) and no name
field prevents them. `isForkConflict` keeps its substring test and its docblock, and WR-08's re-open
trigger — *the next phase that touches `createWorkflowDraft`'s throw site replaces this substring test
with the typed error's own discriminator, in the same commit* — stays stated. **This phase does not
touch that throw site** (scope fence).

On the page: mount the dialog and pass `rows` (the full merged list) into the hook so `isClash` can
read it. **No fork logic on the page.** ⚠ Mind the TDZ order again — `rows` is declared at `:449`, so
the hook call must remain BELOW it as well as below `onOpenDraft`.

Add the new `WorkflowsPage.test.tsx` cases RED-first. ⚠ **T-4 — `asyncUtilTimeout` is 15 s in this
file, longer than vitest's 5 s per-test budget, so a bare `findByTestId` on an absent node blows the
TEST timeout and yields an uninformative RED. Wait on a call count first, then assert under an
explicit short budget.** Raise the `WorkflowsPage.test.tsx` pin in the same commit from two agreeing
gate runs.
  </action>
  <acceptance_criteria>
    - A new `WorkflowsPage.test.tsx` case asserts that forking a published row WITH an existing draft mounts NO dialog and calls `createWorkflowDraft` zero times, paired with a positive control on the no-existing-draft path where the dialog DOES mount
    - The three 192-14 cases at `:886` and the T-192-42 case at `:1081` are green and their assertions are unchanged
    - `grep -c "createWorkflowDraft(" frontend/src/components/workflows/library/useWorkflowFork.ts` still equals **2**
    - `grep -c "CHIP_PREDICATES.yours" frontend/src/components/workflows/library/useWorkflowFork.ts` equals **1**, and no hand-written `isMine ??` expression appears
    - `grep -n "slug" frontend/src/components/workflows/library/useWorkflowFork.ts` shows `isClash` comparing NAMES, not slugs
    - `git diff frontend/src/components/workflows/library/libraryFork.ts` is EMPTY (WR-08 untouched, D-22)
    - `git diff frontend/src/lib/api.ts` is EMPTY (`createWorkflowDraft`'s throw site untouched)
    - The `useWorkflowFork(` call site remains BELOW both `rows`' and `onOpenDraft`'s declarations
    - A test asserts the created draft's definition carries the TYPED name
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` prints **33**
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/pages/WorkflowsPage.test.tsx src/components/workflows/library/</automated>
  </verify>
  <done>Both create paths ask for a name and create under it; the already-forked path still opens the draft with no prompt; the starter retry is still exactly two calls; the pre-flight reads names through the one shipped ownership predicate; WR-08 and the throw site are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Amend 192's D-15 in the card's own docblock, in this same commit (D-19)</name>
  <files>frontend/src/components/workflows/library/WorkflowCard.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/library/WorkflowCard.tsx` lines 60-100 (the action-grade docblock: the sheet, the arm-to-confirm and the fork's DIRECT FLIP paragraph at :79-87)
    - `.planning/sketches/163-the-assembled-card/README.md` § "⚠ 162-B reopens D-15 — picking it is a recorded decision"
    - `192.1-CONTEXT.md` D-19 (the four mechanical clauses) · `192.1-PATTERNS.md` § 8 (the docblock verbatim)
  </read_first>
  <action>
**192's D-15 is REOPENED by 162-B, and it is AMENDED IN THE SAME COMMIT AS THE DIALOG — never left
silently contradicted.** `WorkflowCard.tsx:79-87` currently records the fork as a DIRECT FLIP on the
argument that *"a confirm on a non-destructive, reversible action would spend the guard vocabulary the
delete relies on to mean anything (`CapabilityGrid.tsx:11-13`). The sketch attached a confirm to
159-C; it is deliberately not shipped."*

Rewrite that paragraph to say what is true afterwards, carrying the rationale that makes the prompt
legal — **and it is mechanical rather than asserted: the prompt collects something the system cannot
know, so it is an INPUT, not a guard, and inputs do not spend guard vocabulary.** Name the four clauses
that make the claim testable rather than rhetorical:
- it asks for a **name**, never for a confirmation;
- it wears **no destructive styling** — no `DESTRUCTIVE_ITEM_CLASSES`, no red, no victim naming;
- its primary button is **`Create my copy`**, and asserting *"the primary button is not `Confirm`"* is
  what makes the distinction mechanical;
- it **warns and never blocks** on a colliding name (D-20).

State explicitly that **the graded-guard ladder (146-148) is UNTOUCHED**: a sheet for the live delete,
arm-to-confirm for the draft delete, and the fork remains OUTSIDE the ladder entirely — it is now an
input step, not a lighter guard. Name D-19 and this phase so the amendment is traceable, and leave
192's original reasoning VISIBLE rather than deleted — that is this project's habit.

⚠ Do NOT change any rendered string, any testid, or any behaviour in this task. It is a docblock edit,
and it must land in the SAME commit as the dialog (D-19). If the dialog is already committed, amend
that commit or say plainly in the SUMMARY that it did not, and why.
  </action>
  <acceptance_criteria>
    - `WorkflowCard.tsx:79-87`'s paragraph no longer asserts unqualified that a confirm "is deliberately not shipped" without naming 162-B
    - The amended text contains the words "input" and "guard" and names D-19 and phase 192.1
    - The amended text names all four mechanical clauses and states that the 146-148 ladder is untouched
    - 192's original DIRECT FLIP reasoning is still visible in the file (not deleted)
    - `git diff frontend/src/components/workflows/library/WorkflowCard.tsx` in this task contains ONLY comment lines
    - `git log --oneline -1 -- frontend/src/components/workflows/library/ForkNameDialog.tsx` and the docblock edit share a commit — or the SUMMARY states plainly that they do not, and why
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/library/WorkflowCard.test.tsx` is green at its pinned count
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/library/ && GSD_VITEST_MAX_WORKERS=4 node ../scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>The card's own docblock records the amendment with its four mechanical clauses, leaves 192's reasoning visible, states that the graded-guard ladder is untouched, and changes nothing but comments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keystrokes → new workflow name | Free text supplied by the caller, rendered back into the DOM and sent to `POST /workflows`. |
| client pre-flight → server constraint | The name check is advisory; the DB's `UNIQUE(slug, version)` is the real constraint and it is on SLUGS, not names. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192.1-19 | Tampering | XSS via the typed fork name | mitigate | Rendered as a JSX text node (React escapes); `dangerouslySetInnerHTML` is fenced across the subtree by T-192-04. The name also travels to the server as JSON, never as markup. |
| T-192.1-20 | Spoofing (of guarantee) | Presenting the client pre-flight as a collision guarantee | mitigate | D-21: it is a NAME check over the caller's own rows, explicitly not a slug check, and it WARNS rather than blocks. D-22 records that WR-08's 409 is NOT retired by it and names the two residual slugs. |
| T-192.1-21 | Elevation of Privilege (vocabulary) | Spending the graded-guard vocabulary on a non-destructive action | mitigate | D-19's four clauses are each a test: no `Confirm`, no destructive styling, warns-never-blocks, asks for a name. Grep-asserted, and the card's docblock records the amendment in the same commit. |
| T-192.1-22 | Denial of Service | A blocking name gate turning a library problem into a user problem | accept | D-20: emptiness is the only hard gate. A name you chose is a name you are allowed to have. |
| T-192.1-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero packages installed.** `@radix-ui/react-dialog` ^1.1.15, `eslint-plugin-jsx-a11y` 6.10.2 and `vitest-axe` ^0.1.0 are ALL already present — verified in `package.json`. **A new dependency is not discretionary** (D-19's discretion note; 192-15 declined a toast library for the same reason). If one seems needed, STOP and report. |
</threat_model>

<verification>
- `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` → **33**
- `cd frontend && npx eslint src/components/workflows/library/ src/pages/WorkflowsPage.tsx` → exit 0
- `cd frontend && npm run lint:a11y` → exit 0 (the CI a11y gate; a new dialog's SOURCE is linted, its test is not)
- The eight-suite scoped run → 0 failed, with `WorkflowsPage.test.tsx` grown and its 192-14 / T-192-42 cases unchanged
- `GSD_VITEST_MAX_WORKERS=4 node scripts/vitest-count-gate.cjs` → exit 0
</verification>

<success_criteria>
- A fork asks for a name and creates under it, on both create paths
- An already-forked row opens its draft with NO prompt (D-23 not regressed)
- A colliding name warns and still proceeds; only emptiness blocks
- The primary button is `Create my copy` and there is no `Confirm` anywhere in the dialog
- 192's D-15 is amended in the same commit, with the four mechanical clauses recorded
- WR-08, `libraryFork.ts` and `createWorkflowDraft`'s throw site are all untouched
</success_criteria>

<output>
Create `.planning/phases/192.1-workflow-identity/192.1-07-SUMMARY.md` when done.
Record: the base SHA; the RED/GREEN pair for the warn-never-block positive control; the axe result;
confirmation that the 192-14 and T-192-42 cases are byte-identical and green; the final
`LIBRARY_SUBTREE_PATHS` length and the fences suite count read from the gate's `actual` column; and
whether the D-19 docblock amendment shares the dialog's commit.
</output>
