---
phase: 192.1-workflow-identity
plan: 04
type: execute
wave: 3
depends_on: ["192.1-01", "192.1-03"]
files_modified:
  - frontend/src/components/workflows/library/libraryVocabulary.ts
  - frontend/src/components/workflows/library/relativeChanged.ts
  - frontend/src/components/workflows/library/relativeChanged.test.ts
  - frontend/src/components/workflows/library/__fixtures__/libraryScale.ts
  - frontend/src/components/workflows/library/librarySubtree.fences.test.ts
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [LIB-05]
gap_closure: false

must_haves:
  truths:
    - "Every word the identity line and the fork prompt will render is declared in one file and imported, so a copy change is a one-line diff"
    - "A timestamp becomes a long-form English phrase across nine bands, and an absent timestamp becomes silence rather than a fabricated time"
    - "A test can build a 100+ row library whose duplicate names FALL OUT of the real fork rules, and which carries orphan slugs no generator can produce"
  artifacts:
    - path: "frontend/src/components/workflows/library/libraryVocabulary.ts"
      provides: "the ported sketch COPY table — identity segments, the changed prefix, the 1-of-N label and the fork dialog strings"
      contains: "CHANGED_PREFIX"
    - path: "frontend/src/components/workflows/library/relativeChanged.ts"
      provides: "the nine-band long-form relative-time formatter with now-as-parameter"
      contains: "export function relativeChanged"
    - path: "frontend/src/components/workflows/library/__fixtures__/libraryScale.ts"
      provides: "makeLibraryFixture — the ported generator carrying the duplicate shape AND the orphan shape"
      contains: "compliance-gap-report"
  key_links:
    - from: "frontend/src/components/workflows/library/relativeChanged.ts"
      to: "frontend/src/components/workflows/library/libraryVocabulary.ts"
      via: "CHANGED_PREFIX imported, never retyped"
      pattern: "CHANGED_PREFIX"
    - from: "frontend/src/components/workflows/library/__fixtures__/libraryScale.ts"
      to: "frontend/src/components/workflows/library/libraryRow.ts"
      via: "the generator emits LibraryRow[] including updatedAt"
      pattern: "LibraryRow"
---

<objective>
Land the phase's three supporting pure assets: **the ported COPY table** (D-14), **the nine-band
relative-time formatter** (D-18), and **the scale fixture carrying the real duplicate AND orphan
shapes** (D-25, D-26, D-30).

Purpose: the resolver (Plan 05) and the card (Plan 06) must import their words and their clock, not
retype them — that is the anti-drift mechanism sketch 163 exists to provide. And SC#1 is judged on a
fixture whose SHAPE is realistic, not merely its SIZE: *"volume was real, shape was not"* is the
recorded reason Phase 192's gates all passed while the operator could not tell 43 rows apart.
Output: `libraryVocabulary.ts` widened, `relativeChanged.ts` + suite, `__fixtures__/libraryScale.ts`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⚠ **WORKTREE BOOTSTRAP IS YOUR FIRST ACTION:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`.
**NEVER `rm -rf` a worktree** — `bash scripts/teardown-worktree.sh <path>`.
⚠ Assert the dispatched base SHA and record it. ⚠ `GSD_VITEST_MAX_WORKERS=4` on every vitest run.

⚠ **Load the project design skill before writing any user-visible string:**
`Skill("sketch-findings-agentic-rag")`. This surface is covered by it.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/192.1-workflow-identity/192.1-CONTEXT.md
@.planning/phases/192.1-workflow-identity/192.1-RESEARCH.md
@.planning/phases/192.1-workflow-identity/192.1-PATTERNS.md
@.planning/phases/192.1-workflow-identity/192.1-VALIDATION.md
@.planning/sketches/163-the-assembled-card/BUILD-CONTRACT.generated.md
@.planning/sketches/163-the-assembled-card/README.md

<interfaces>
<!-- THE EXACT STRINGS, from the GENERATED build contract. Port these; do not retype from prose. -->

| Key | Value |
|---|---|
| OWN_YOURS            | "Yours" |
| OWN_SHARED           | "Shared" |
| LINEAGE_ORIGINAL     | "Original" |
| LINEAGE_COPY_OF      | "Copy of "  (note the trailing space) |
| LINEAGE_STARTER_SUF  | " starter"  (note the leading space) |
| STATE_RUNNABLE       | "Ready to run" |
| STATE_DRAFT          | "Still building" |
| STATE_STARTER        | "Shared starter" |
| NO_PROJECT           | "No project" |
| CHANGED_PREFIX       | "changed "  (note the trailing space) |
| FORK_DIALOG_TITLE    | "Name your copy" |
| FORK_DIALOG_OK       | "Create my copy" |
| FORK_DIALOG_CANCEL   | "Cancel" |
| FORK_HINT_EMPTY      | "Give it a name." |
| FORK_HINT_FREE       | "No other workflow of yours has this name." |
| FORK_HINT_CLASH      | "You already have one called this. Allowed — but you will not be able to tell them apart." |

Composed (ship as FUNCTIONS, per libraryVocabulary.ts:145-153's `forkFailedMessage` idiom):
  lineageVersionOf(mine, parent)  →  "v3 of v2"
  oneOfLabel(n)                   →  "1 of 43"        ← "1 of " + n, VERBATIM (D-07)
  forkDialogSub(sourceName)       →  "You are copying “Compliance Gap Report”. Give your copy a name
                                      you will recognise later — you can change it in the Builder."

ALREADY SHIPPED — imported, NEVER retyped (libraryVocabulary.ts:78, :91):
  FORK_VERB = "Make my own copy"
  FORK_CONSEQUENCE = "Opens a new private copy you can edit. The published version stays live and unchanged."

NOT PORTED — the five card labels (RUN_LABEL, OPEN_LABEL, DELETE_WORKFLOW, DELETE_DRAFT, MENU_ARIA)
already live module-private in WorkflowCard.tsx:105-109. Their re-home debt is recorded there and is
NOT this phase's. Do not port them, do not re-home them, and do not add a sixth escapee.

The nine bands, verbatim from .planning/sketches/themes/library-fixture-192-1.js:352-365 — note it
ALREADY takes `now`, so no redesign is needed and Math.round (not floor) is load-bearing at every
boundary:
  d = now - ms; mins = round(d/60000)
  mins < 60            →  mins <= 1 ? "just now" : mins + " min ago"
  hrs = round(mins/60); hrs < 24  →  hrs + (hrs === 1 ? " hour ago" : " hours ago")
  days = round(hrs/24); days === 1 → "yesterday"
  days < 7             →  days + " days ago"
  wks = round(days/7);  wks < 5   →  wks === 1 ? "last week" : wks + " weeks ago"
  mos = round(days/30)            →  mos === 1 ? "last month" : mos + " months ago"

The better analog for the signature — settings/connectionsCopy.ts:280-288 :
  export function credentialLabel(lastCheckedAt: string | null | undefined, now: number = Date.now()): string

The fixture factory shape already shipped one directory up — workflows/__fixtures__/shapeGenerator.ts:147 :
  export function generatedShapes(): CanvasFixture[]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the sketch's COPY table into libraryVocabulary.ts (D-14)</name>
  <files>frontend/src/components/workflows/library/libraryVocabulary.ts</files>
  <read_first>
    - `frontend/src/components/workflows/library/libraryVocabulary.ts` (whole file — 223 L; the header's one-home rule at :1-31, `CHIP_WORDS` at :52-59, `FORK_VERB` at :78, `FORK_CONSEQUENCE` at :80-92, `FORK_CONSEQUENCE_EXISTING` at :124, `forkFailedMessage` at :145-153, `LIBRARY_STATES` at :203-208, `PROVENANCE_WORDS` at :214-218)
    - `.planning/sketches/163-the-assembled-card/BUILD-CONTRACT.generated.md` (the generated table — THE source, never the prose)
    - `.planning/sketches/163-the-assembled-card/index.html` lines 222-260 (the COPY object, for the composed-string bodies)
    - `frontend/src/components/workflows/library/librarySubtree.fences.test.ts` lines 280-300 (F5's seven forbidden words and the `tok("sem","antic")` assembly trick)
    - `192.1-CONTEXT.md` D-07, D-14 · `192.1-PATTERNS.md` § 9, § S-1
  </read_first>
  <action>
Add to `libraryVocabulary.ts` every string in the `<interfaces>` table above, in the module's own two
established shapes: a plain `export const` for a fixed string, and an **exported FUNCTION** for an
interpolated one (`forkFailedMessage`'s idiom at `:145-153`, whose docblock records the reason — *"the
interpolated half is data, and a template assembled at the call site is a string that leaks out of
this module's fences"*).

Group them under two headed sections with docblocks stating what they are for and which decision
locked them: the identity-line vocabulary (D-03 / D-06 / D-18) and the 162-B fork-prompt vocabulary
(D-19 / D-20).

⚠ **`oneOfLabel(n)` ships as `"1 of " + n`, VERBATIM (D-07).** The leading `1` is a constant, not an
index, and it *can* be misread as one. The operator-approved mockup is the acceptance bar and changing
a bar string at plan time is exactly the drift sketch 163 exists to prevent — so it ships as approved,
and **UAT row U4 asks a human whether it reads as an index**. Put that sentence in the function's
docblock so the next reader does not "fix" it silently: if the answer is yes, it is a one-line change
in this one file, by construction.

⚠ **Import, never retype: `FORK_VERB` (`:78`) and `FORK_CONSEQUENCE` (`:91`) already live here.** The
sketch itself reads them from the fixture rather than re-typing them — that is the proof the mechanism
works, since a drifted copy of either would have failed the drive. Add NOTHING that re-states them, and
do not reword `FORK_CONSEQUENCE` or `FORK_CONSEQUENCE_EXISTING` (scope fence).

⚠ **F5 sweeps this file.** The forbidden word class is
`semantic / meaning / similar / AI-powered / smart / understands / natural language`. RESEARCH audited
the whole COPY table against it with **zero hits** — so **port the table and do not improvise
microcopy.** Any executor-invented phrase can red F5.

⚠ Do not port the five card labels named in `<interfaces>`, and do not re-home them.
  </action>
  <acceptance_criteria>
    - Every key in the `<interfaces>` table exists as an export, with the value byte-identical to the generated build contract (including the trailing space in `"Copy of "` and `"changed "`, and the leading space in `" starter"`)
    - `lineageVersionOf`, `oneOfLabel` and `forkDialogSub` are exported FUNCTIONS, not template literals assembled at a call site
    - `oneOfLabel`'s body is `"1 of " + n` (or the template equivalent) and its docblock names D-07 and UAT row U4
    - `grep -c "Make my own copy" frontend/src/components/workflows/library/libraryVocabulary.ts` equals **1** (no second copy of `FORK_VERB`)
    - `git diff frontend/src/components/workflows/library/libraryVocabulary.ts` shows NO change to the `FORK_CONSEQUENCE` or `FORK_CONSEQUENCE_EXISTING` string bodies
    - `grep -niE "semantic|meaning|similar|AI-powered|smart|understands|natural language" frontend/src/components/workflows/library/libraryVocabulary.ts` returns no NEW user-visible string (pre-existing prose hits, if any, unchanged)
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` prints **33**
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/library/librarySubtree.fences.test.ts src/components/workflows/library/libraryFilter.test.ts</automated>
  </verify>
  <done>Every identity and fork-prompt string is declared once in `libraryVocabulary.ts`, ported from the generated contract rather than prose, with the three composed strings as functions; F5 is green and the shipped fork strings are untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: relativeChanged.ts — nine bands, now-as-parameter, silence on absence (D-18)</name>
  <files>frontend/src/components/workflows/library/relativeChanged.ts, frontend/src/components/workflows/library/relativeChanged.test.ts, frontend/src/components/workflows/library/librarySubtree.fences.test.ts, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/components/settings/connectionsCopy.ts` lines 258-290 (`relativeTime` + `credentialLabel` — the `now`-as-parameter shape and the honest-alternative rule)
    - `frontend/src/components/admin/UsersAndAccess.tsx` lines 85-100 (the COMPACT operator-audience formatter — precedent, NOT a dependency)
    - `.planning/sketches/themes/library-fixture-192-1.js` lines 350-368 (`relTime` — the nine bands, verbatim)
    - `frontend/src/components/workflows/library/libraryFilter.ts` lines 1-30 (the purity-claim header) and `libraryFilter.test.ts` lines 1-40
    - `frontend/src/components/workflows/library/librarySubtree.fences.test.ts` lines 60-110
    - `192.1-CONTEXT.md` D-18 · `192.1-RESEARCH.md` § P-1 · `192.1-PATTERNS.md` § 5, § S-8
  </read_first>
  <behavior>
    - `relativeChanged(t, now)` where `now - t` is 0 ms → `"changed just now"`; 60 s → `"changed just now"` (mins ≤ 1)
    - 2 min → `"changed 2 min ago"`; 59 min → `"changed 59 min ago"`
    - 1 h → `"changed 1 hour ago"`; 5 h → `"changed 5 hours ago"`; 23 h → `"changed 23 hours ago"`
    - 1 day → `"changed yesterday"`; 3 days → `"changed 3 days ago"`; 6 days → `"changed 6 days ago"`
    - 7 days → `"changed last week"`; 21 days → `"changed 3 weeks ago"`
    - 30 days → `"changed last month"`; 60 days → `"changed 2 months ago"`
    - BOTH SIDES of each of the eight thresholds are exercised — `Math.round` (not `floor`) is load-bearing at every boundary
    - `relativeChanged(undefined, now)` → `null`; `relativeChanged(null, now)` → `null`; `relativeChanged("not-a-date", now)` → `null`
    - a POSITIVE CONTROL sits beside each `null` assertion proving a valid input DOES produce a string
    - the function is deterministic: called twice with the same `(value, now)` it returns the same string
  </behavior>
  <action>
Create `frontend/src/components/workflows/library/relativeChanged.ts` exporting

```
export function relativeChanged(updatedAt: string | null | undefined, now?: number): string | null
```

with `now` defaulting to `Date.now()` — **the last parameter with a default**, copied exactly from
`connectionsCopy.ts:280-288`'s `credentialLabel(lastCheckedAt, now = Date.now())`. That signature is
P-1's whole fix: the page hoists ONE `const now = Date.now()` per render so every card in a render
agrees, and the test injects a fixed instant with no clock mock.

Body: parse with `Date.parse`; if the value is absent or `!Number.isFinite(t)`, **return `null`** —
the honest alternative. `undefined` means the wire did not say (S-8, `libraryFilter.ts:153-158`'s
"never treat `undefined` as `false`"), and a fabricated time is the `068-A` honest-last-active rule's
exact prohibition. On success return `CHANGED_PREFIX + <band>`, importing `CHANGED_PREFIX` from
`./libraryVocabulary` (D-14) rather than typing `"changed "`.

Port the nine bands verbatim from the fixture. **`Math.round`, not `Math.floor`, everywhere.**

⚠ **The module's docblock must SAY this is the THIRD spelling of a relative-time formatter in this
repo, and why the two existing ones cannot serve.** `relativeTime` exists byte-identically at
`admin/UsersAndAccess.tsx:87-97` (module-private) and `settings/connectionsCopy.ts:265-275` (exported,
and claiming to be "reused rather than re-invented" while in fact being a copy). Both are COMPACT
(`4mo ago`) for an operator audience; this surface is LONG-FORM with nine bands, not five. Without
that sentence a reviewer reads the new module as the drift `libraryFilter.ts:18-27` forbids by name.

⚠ **NO DATE LIBRARY.** `frontend/package.json` carries none and adding one is not discretionary (S-5).

⚠ **Do NOT add a `setInterval` or any live ticker.** 188's `WorkflowRunPage` tick-gate bug
(`vitest-count-gate.cjs:497-504`) is this project's recorded lesson that a live clock is its own defect
class. The value refreshes when something else re-renders; that is accepted and must be stated in the
docblock rather than shipped around.

Give the module the `libraryFilter.ts:1-27` purity-claim header (S-9) and assert it in the suite with
a `?raw` self-import.

Write `relativeChanged.test.ts` FIRST, RED, in the `libraryFilter.test.ts` / `deriveTier.test.ts`
posture — a pure `describe` per rule, no `render`, no clock mock. Cover every behavior above.

Add `"./relativeChanged.ts"` to `LIBRARY_SUBTREE_PATHS` and update `toHaveLength` to the array's own
new length **read from the failing assertion's diff** (D-33/D-35). Raise the
`librarySubtree.fences.test.ts` and `relativeChanged.test.ts` pins in `scripts/vitest-count-gate.cjs`
in the SAME commit, from the gate's own `actual` column across two agreeing runs.
  </action>
  <acceptance_criteria>
    - `relativeChanged.ts` exports exactly one function with the signature `(updatedAt: string | null | undefined, now?: number) => string | null`
    - `grep -c "Math.floor" frontend/src/components/workflows/library/relativeChanged.ts` equals **0**
    - `grep -n "setInterval\|setTimeout" frontend/src/components/workflows/library/relativeChanged.ts` returns NOTHING
    - `grep -n "\"changed \|'changed " frontend/src/components/workflows/library/relativeChanged.ts` returns NOTHING (the prefix is imported)
    - The docblock contains the word "third" and names both `admin/UsersAndAccess.tsx` and `settings/connectionsCopy.ts` with the reason they cannot serve
    - `relativeChanged.test.ts` exercises both sides of all eight thresholds and pairs every `null` assertion with a positive control
    - The RED was observed as a real `AssertionError` and both RED and GREEN outputs are quoted in the SUMMARY
    - `LIBRARY_SUBTREE_PATHS` contains `"./relativeChanged.ts"`; `toHaveLength` matches the array
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` prints **33**
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/library/relativeChanged.test.ts src/components/workflows/library/librarySubtree.fences.test.ts</automated>
  </verify>
  <done>One long-form nine-band formatter exists, takes `now` as a parameter, returns `null` rather than a fabricated time, imports its prefix, admits it is the third spelling, and is proved RED-first across every boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Port the scale fixture with the duplicate shape AND the orphan shape (D-25, D-26, D-30)</name>
  <files>frontend/src/components/workflows/library/__fixtures__/libraryScale.ts, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `.planning/sketches/themes/library-fixture-192-1.js` (whole file — 419 L; note `mkRng`, `hash6`, `nameCounts`, `lineageOf` at :334-350 and its fixture-only `parentSlug` short-circuit at :335, `seedRecent` at :367-374)
    - `frontend/src/components/workflows/__fixtures__/shapeGenerator.ts` lines 1-40 and 140-160 (the header contract and the `generatedShapes(): CanvasFixture[]` factory export)
    - `frontend/src/components/workflows/__fixtures__/canvasFixtures.ts` lines 1-25 (the "two live reads disagreed, so pin the artifact and cite it" honesty clause)
    - `frontend/src/components/workflows/library/WorkflowCard.test.tsx` lines 80-100 (the house `rowOf` builder with its `...over` spread and `as unknown as` casts)
    - `frontend/src/components/workflows/library/libraryRow.ts` (the `LibraryRow` shape the generator must emit, now including `updatedAt`)
    - `frontend/src/components/workflows/library/librarySubtree.fences.test.ts` lines 70-120 (the glob at `:77` is `./*.{ts,tsx}` and is NON-RECURSIVE)
    - `192.1-CONTEXT.md` D-25, D-26, D-30 · `192.1-RESEARCH.md` § 5b, § 9 · `192.1-PATTERNS.md` § 6
  </read_first>
  <action>
Create `frontend/src/components/workflows/library/__fixtures__/libraryScale.ts` exporting a FACTORY,
not a constant:

```
export function makeLibraryFixture(opts?: { rows?: number; orphans?: boolean; now?: number }): LibraryRow[]
```

Fresh objects per call (so two callers can never alias each other's rows), deterministic (port the
sketch's seeded `mkRng` PRNG and `hash6` — **no `Math.random`**), and **zero new dependency** (no
property-testing library, no generator library). Copy `shapeGenerator.ts:1-33`'s header contract
nearly clause-for-clause.

**⚠ D-25 — the duplicates must FALL OUT of the real fork rules, not be authored.** Port the
generator's rule application so the duplicate names are a consequence of `onTweak` minting
`<same name> v(N+1)` and `onUseStarter` minting `<slug>-<hash6>` at v1 — which is exactly why the
shape is realistic. *"A corpus where every row answers every predicate the same way makes each
assertion pass for the wrong reason"* (`libraryFilter.test.ts:34-38`) — and one level deeper, a corpus
whose axes all genuinely vary hides D-31's flagship failure.

**⚠ D-30 — pin TODAY's re-measured shape and cite BOTH readings.** Target:
**43/41 · 11/10 · 9/6 · 8/4 · 14 duplicated names · 106 merged rows** (measured 2026-08-12), and record
in the header that the ROADMAP's **43/41 · 10/9 · 8/5 · 8/4 · 107** was the earlier reading of the same
day. Use `canvasFixtures.ts:10-18`'s words: two reads disagreed, so the artifact is pinned and cited
rather than read live. A fixture asserting numbers the DB no longer shows invites a future "correct"
edit that silently weakens the shape.

**⚠ D-26 — the generator CANNOT produce the orphan shape, so append it explicitly.** The sketch's
`lineageOf` short-circuits on a fixture-only `parentSlug` field (`library-fixture-192-1.js:335`), so no
generated slug is ever an orphan. Production has no `parentSlug` — it derives from the slug alone —
and that is precisely what admits slugs whose last segment happens to be six base-36 characters.
Append rows whose slug matches `^(.*)-[a-z0-9]{6}$` with **no parent present**, using the REAL measured
ones:
  - **`compliance-gap-report`** — ⚠ **required, and it is the important one (C-3 / D-30).** It is the
    base slug of the 43-row family, so the family's TRUE ORIGINAL renders **no lineage segment** while
    its 43 copies say *"Copy of Compliance Gap Report"*. That is correct under D-13 and it will read as
    a bug at UAT unless it is written down — write it down, in the fixture's own header.
  - `pm-weekly-status-report` (`report` is six legal lowercase alphanumerics)
  - `risk-register-fill-101uat` (`101uat` likewise)

**Do NOT emit `parentSlug` or `forkKind` on the produced rows.** They are fixture-only fields with no
`LibraryRow` equivalent and no wire equivalent; carrying them would let the resolver's tests pass
against data production never sees. The generator's own internal bookkeeping may use them; the exported
rows must not.

**⚠ Keep `seedRecent`** (`:367-374`), which nudges four rows to `just now` / hours / `yesterday` /
2 days. Without it the newest row is three weeks old and every relative-time band above "weeks" is
untestable (SC#3). Drive it off the `opts.now` parameter so the fixture is time-injectable.

Expose an override seam matching the house `rowOf` posture (`WorkflowCard.test.tsx:84-95`) — a
`...over` spread and `as unknown as` casts for `def` / `source` — so a single row can be injected
without a second fixture.

**Placement is load-bearing:** `__fixtures__/` is a SUBDIRECTORY and the fences corpus glob (`:77`) is
`./*.{ts,tsx}`, **non-recursive** — so the fixture is provably outside the sweep and must NOT be added
to `LIBRARY_SUBTREE_PATHS`. `TARGETS` reaches the directory anyway via `"src/components/workflows"`, and
a non-`.test` module is not run as a suite, so **no `TARGETS` edit and no new pin key for the fixture
itself**.

⚠ Two sketch numbers that are not what they look like: `BUILD-CONTRACT.generated.md` says
`cards rendered: 108` (the 162-B drive really created a row) — **do not port 108 as the fixture size**;
and `lines carrying '1 of N': 94` was measured at 108 cards — **re-derive it from the ported fixture in
Plan 05 rather than pinning 94**.
  </action>
  <acceptance_criteria>
    - `makeLibraryFixture()` returns ≥ 100 `LibraryRow` objects and two successive calls return structurally equal but non-aliased arrays
    - The default output carries **14 duplicated names** and family sizes matching D-30's pinned shape; the header records BOTH the pinned reading and the ROADMAP's earlier one
    - `grep -c "Math.random" frontend/src/components/workflows/library/__fixtures__/libraryScale.ts` equals **0**
    - `grep -n "parentSlug\|forkKind" frontend/src/components/workflows/library/__fixtures__/libraryScale.ts` shows neither field on any EXPORTED row object
    - `grep -c "compliance-gap-report" frontend/src/components/workflows/library/__fixtures__/libraryScale.ts` is ≥ 1, and the header explains why the 43-family's own base slug renders no lineage segment
    - At least four rows carry an `updatedAt` inside the sub-day bands (the `seedRecent` port), driven off `opts.now`
    - `LIBRARY_SUBTREE_PATHS` does NOT contain the fixture, and the SUMMARY states the non-recursive-glob reason
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` prints **33**
    - `GSD_VITEST_MAX_WORKERS=4 node scripts/vitest-count-gate.cjs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/library/ && GSD_VITEST_MAX_WORKERS=4 node ../scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>A deterministic, dependency-free factory produces a 100+ row library whose duplicate names fall out of the real fork rules, carrying D-30's pinned shape and D-26's three orphan slugs including `compliance-gap-report`, with the recency seed intact and the fixture provably outside the fences sweep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vocabulary → rendered DOM | Every string in `libraryVocabulary.ts` reaches a user's screen as a JSX text node. |
| (no data boundary) | `relativeChanged` and the fixture are pure; neither reads a network, a datastore or `window`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192.1-07 | Spoofing (of fact) | `relativeChanged` on an absent/unparseable timestamp | mitigate | Returns `null`, never a fabricated time. The `068-A` honest-last-active rule, applied to a row's recency. Asserted with a positive control so the `null` case cannot pass for the wrong reason (S-6). |
| T-192.1-08 | Information Disclosure | New user-visible copy overstating what the surface does | mitigate | F5 sweeps the subtree for `semantic / meaning / similar / AI-powered / smart / understands / natural language`. The whole ported table is pre-audited clean; the control is "port the table, do not improvise". |
| T-192.1-09 | Tampering | A fixture that hides the failure it exists to expose | mitigate | D-26's orphan rows and D-30's pinned family shape are both grep-asserted, and the header cites both live readings rather than reading live. |
| T-192.1-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero packages installed.** No date library, no generator library, no property-testing library. Adding one is forbidden by the scope fence, not merely discouraged. |
</threat_model>

<verification>
- `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` → **33**
- `cd frontend && npx eslint src/components/workflows/library/ src/pages/WorkflowsPage.tsx` → exit 0
- `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/pages/WorkflowsPage.test.tsx src/components/workflows/library/ src/pages/__tests__/PublishedCardDelete.test.tsx src/pages/__tests__/RunModal.test.tsx src/pages/__tests__/RunModal.a11y.test.tsx` → 0 failed
- `GSD_VITEST_MAX_WORKERS=4 node scripts/vitest-count-gate.cjs` → exit 0
</verification>

<success_criteria>
- Every identity and fork-prompt string is declared once, ported from the GENERATED contract, and importable
- One long-form nine-band formatter exists with `now` as a parameter and `null` on absence
- A deterministic factory produces the real duplicate shape AND the orphan shape, including `compliance-gap-report`
- No new dependency; tsc 33; eslint 0; gate exit 0
</success_criteria>

<output>
Create `.planning/phases/192.1-workflow-identity/192.1-04-SUMMARY.md` when done.
Record: the base SHA; the RED and GREEN outputs for `relativeChanged.test.ts`; the fixture's MEASURED
family shape and duplicated-name count (from a script run over its output, not from D-30's text); the
new `toHaveLength` value and where it was read from; and both raised pins with their two agreeing runs.
</output>
