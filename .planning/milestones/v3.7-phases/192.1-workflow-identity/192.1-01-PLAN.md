---
phase: 192.1-workflow-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/db/workflows.py
  - backend/app/api/workflows.py
  - backend/tests/unit/test_workflows_routes.py
  - frontend/src/lib/api.ts
  - frontend/src/components/workflows/library/libraryRow.ts
  - frontend/src/components/workflows/library/libraryFilter.ts
  - frontend/src/components/workflows/library/libraryFilter.test.ts
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [LIB-05]
gap_closure: false

must_haves:
  truths:
    - "A published, a starter and a draft row all arrive at the browser carrying the timestamp of when they last changed"
    - "The drafts feed's opaque concurrency token is untouched and is never the source of that timestamp"
    - "Every owner-scoping predicate on the three feeds is byte-identical to what shipped"
  artifacts:
    - path: "backend/app/db/workflows.py"
      provides: "updated_at in all FOUR SELECT lists across list_published_workflows / list_starter_workflows / list_draft_workflows"
      contains: "updated_at"
    - path: "backend/app/api/workflows.py"
      provides: "updated_at on PublishedWorkflow and DraftRow, serialized by all three builders"
      contains: "updated_at"
    - path: "frontend/src/components/workflows/library/libraryRow.ts"
      provides: "LibraryRow.updatedAt"
      contains: "updatedAt"
  key_links:
    - from: "backend/app/db/workflows.py"
      to: "backend/app/api/workflows.py"
      via: "r[\"updated_at\"] read in all three response builders"
      pattern: "updated_at"
    - from: "frontend/src/lib/api.ts"
      to: "frontend/src/components/workflows/library/libraryFilter.ts"
      via: "fromPublished / fromDraft lift row.updated_at into LibraryRow.updatedAt"
      pattern: "updatedAt: row\\.updated_at"
---

<objective>
Ship `updated_at` end to end as an ADDITIVE projection — four SQL SELECT lists, two Pydantic models,
three response builders, two wire types, one `LibraryRow` field, two normalizers — following Phase
192's own `is_mine` precedent (its D-04) verbatim.

Purpose: SC#3 ("a user can tell which workflow changed most recently") needs a timestamp the client
can read. It is the only net-new wire cost in the phase, and it must widen the projection without
touching a single predicate.
Output: every row on all three feeds carries `updated_at`; `LibraryRow.updatedAt` exists for the
identity resolver (Plan 04) and the card (Plan 05) to consume.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⚠ **WORKTREE BOOTSTRAP IS YOUR FIRST ACTION, BEFORE ANYTHING ELSE:**
```bash
bash scripts/bootstrap-worktree.sh "$(pwd)"
```
It is idempotent and fails loudly. A worktree that skipped it reports green typechecks and red tests
for reasons that look like this plan's fault. **NEVER `rm -rf` a worktree** — use
`bash scripts/teardown-worktree.sh <path>`.

⚠ **Assert your base SHA before reading the plan.** Twelve of twelve worktrees in Phase 192 forked
from a `master` merge commit rather than the orchestrator's HEAD. Run `git rev-parse HEAD` and record
it in the SUMMARY.

⚠ **`GSD_VITEST_MAX_WORKERS=4` on every vitest invocation.**
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/192.1-workflow-identity/192.1-CONTEXT.md
@.planning/phases/192.1-workflow-identity/192.1-RESEARCH.md
@.planning/phases/192.1-workflow-identity/192.1-PATTERNS.md
@.planning/phases/192.1-workflow-identity/192.1-VALIDATION.md

<interfaces>
<!-- The contracts this plan edits. Extracted at HEAD; the executor does not need to hunt. -->

backend/app/api/workflows.py:115 — ONE model, TWO feeds (`/published` at :219 AND `/starters` at :273):
  class PublishedWorkflow(BaseModel):
      id: UUID
      slug: str
      name: str
      definition: dict | None = None
      is_mine: bool = False
      is_system_global: bool = False

backend/app/api/workflows.py:195 — serves `/drafts` (route :1067, handler :1072):
  class DraftRow(BaseModel):
      id: UUID
      slug: str
      version: int
      name: str | None = None
      definition: dict | None = None
      token: str

frontend/src/lib/api.ts:1364 / :3328 — the two wire types:
  export interface PublishedWorkflow { id; slug; name; definition?; is_mine?; is_system_global? }
  export interface WorkflowDraftRow  { id; slug; version; name; definition?; token }

frontend/src/components/workflows/library/libraryRow.ts:52-100 — the one normalized shape:
  export interface LibraryRow { id; slug; name; version?; def?; provenance; isMine: boolean | undefined; source }

frontend/src/components/workflows/library/libraryFilter.ts:68 / :94 — the two normalizers:
  export function fromPublished(row: PublishedWorkflow, provenance): LibraryRow
  export function fromDraft(row: WorkflowDraftRow): LibraryRow
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project updated_at from all four SELECT lists and serialize it on both models (D-15, D-16, D-17)</name>
  <files>backend/app/db/workflows.py, backend/app/api/workflows.py</files>
  <read_first>
    - `backend/app/db/workflows.py` lines 85-100 (`CONCURRENCY_TOKEN_SQL`), 228-342 (`list_published_workflows` + `list_starter_workflows`), 495-529 (`list_draft_workflows`)
    - `backend/app/api/workflows.py` lines 108-217 (both models + the BINDING RULE docblock at :134-142 and the token warning at :183-189), 246-320 (the published + starter builders), 1063-1102 (`list_drafts`)
    - `supabase/full-schema.sql` lines 1935-1945 (`updated_at … NOT NULL`), 3760-3775 (the two triggers)
    - `192.1-CONTEXT.md` D-15, D-16, D-17 · `192.1-PATTERNS.md` § 7 (the `is_mine` hop-by-hop template)
  </read_first>
  <action>
Per **D-15**, widen FOUR SELECT lists and never a predicate:
`list_published_workflows` (`:231`) has TWO SELECT lists — the `owned_only` branch at `:282` and the
default branch at `:297`; `list_starter_workflows` (`:309`) at `:331`; `list_draft_workflows` (`:502`)
at `:517`. Append `updated_at` to each column list. Do not alter any `WHERE`, any `ORDER BY`, or any
bind parameter. Echo the shipped comment form used at `:277-281` ("The projection widens; the
predicate does NOT.") on each new edit, naming `192.1 (LIB-05 / D-15)`.

⚠ **D-16 is a fence, not advice.** `CONCURRENCY_TOKEN_SQL` (`:93-95`) is already
`to_char(updated_at AT TIME ZONE 'UTC', …)` — the drafts feed ships `updated_at` in disguise as
`token`. Add a SEPARATE `updated_at` column to `list_draft_workflows`' SELECT. Do not re-purpose,
re-parse or alias `token`.

On the API layer add `updated_at: str | None = None` to BOTH `PublishedWorkflow` (`:115`) and
`DraftRow` (`:195`) — optional-and-defaulted, following the `is_mine` precedent stated at `:125-131`
(a frontend deployed AHEAD of this backend degrades rather than crashing). **Type it `str`, not
`datetime`, and record the reason in the field's docblock:** `DraftCreateResponse.token`'s binding
warning (`:184-188`) records that a `datetime`-typed field re-serialized through Pydantic drops the
fractional part when microseconds are 0; typing it `str` and serializing with `.isoformat()` in the
builder makes the wire string exactly what the DB returned. Write in the same docblock that
`updated_at` PASSES the `PublishedWorkflow` BINDING RULE (`:134-142`) for the rule's own stated
reason — it "describes the ROW, never a person" — so a reviewer does not read the new field as the
mig-116 / CR-01 information-disclosure shape.

Serialize it in all THREE builders: the `/published` comprehension (`:256-270`), the `/starters`
comprehension (`:292-308`) and `list_drafts` (`:1084-1094`). The column is `NOT NULL` and always
projected, so use `r["updated_at"]` (the house `r[...]` vs `r.get(...)` split recorded in PATTERNS
§7 hop 4), guarded as `r["updated_at"].isoformat() if r["updated_at"] else None`.

Add or extend a backend unit case asserting `updated_at` is present on all three feed responses AND
that the existing owner-scope cases stay green. ⚠ **`grep` `backend/tests/` for existing
`list_published_workflows` / `list_drafts` assertions FIRST** — RESEARCH A5 records that the test
scope was not enumerated; do not assume the file path.

Per **D-17**, add one comment at the published builder recording that a published row's `updated_at`
is frozen at the publish flip by `workflow_definitions_block_published` (`full-schema.sql:3764`), so
"changed &lt;rel&gt;" on a published row honestly means "when it was published". Do not add a second
field to "fix" it.
  </action>
  <acceptance_criteria>
    - `grep -c "updated_at" backend/app/db/workflows.py` increases by at least 4 over its pre-change value, and every new occurrence is inside a SELECT column list
    - `git diff backend/app/db/workflows.py` contains ZERO changes to any line containing `WHERE`, `ORDER BY`, or `$1`
    - `grep -n "token" backend/app/db/workflows.py` shows `CONCURRENCY_TOKEN_SQL` unchanged (byte-identical to HEAD)
    - `backend/app/api/workflows.py` declares `updated_at: str | None = None` on both `PublishedWorkflow` and `DraftRow`
    - All three builders pass `updated_at=` explicitly; no builder hard-codes a value
    - The `updated_at` field docblock names the BINDING RULE at `:134-142` and states the "describes the ROW, never a person" reason
    - `cd backend && venv/Scripts/python -m pytest tests/unit/ -k workflow` exits 0 with no NEW failures against the pre-change reading (record both readings in the SUMMARY)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/unit/ -k workflow -q</automated>
  </verify>
  <done>All four SELECT lists project `updated_at`; both Pydantic models carry it as `str | None = None`; all three builders serialize it; the pytest scope is green with no new failures; no predicate and no `token` expression changed.</done>
</task>

<task type="auto">
  <name>Task 2: Carry updated_at through the two wire types, LibraryRow and both normalizers (D-15)</name>
  <files>frontend/src/lib/api.ts, frontend/src/components/workflows/library/libraryRow.ts, frontend/src/components/workflows/library/libraryFilter.ts, frontend/src/components/workflows/library/libraryFilter.test.ts, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/lib/api.ts` lines 1355-1372 (`PublishedWorkflow` + its optional-on-purpose docblock) and 3323-3350 (`WorkflowDraftRow` + the token do-not-parse docblock) and 5585-5592 (the `ConnectorConnection.updated_at?: string | null` spelling to copy)
    - `frontend/src/components/workflows/library/libraryRow.ts` (whole file — 100 L; the `isMine` field docblock at :83-93 is the shape to copy)
    - `frontend/src/components/workflows/library/libraryFilter.ts` lines 60-140 (`fromPublished`, `fromDraft`, `mergeLibrary`)
    - `frontend/src/components/workflows/library/libraryFilter.test.ts` lines 1-60 (header + fixture posture)
    - `scripts/vitest-count-gate.cjs` lines 900-975 (the pin block and its "read from this script's own `actual` column" rule)
    - `192.1-PATTERNS.md` § S-8 and § 7 hops 5-7
  </read_first>
  <action>
Add `updated_at?: string | null` to BOTH `PublishedWorkflow` (`api.ts:1364`) and `WorkflowDraftRow`
(`api.ts:3328`). Use exactly the optional+nullable spelling already shipped at `api.ts:5589`, which
is the form `PublishedWorkflow`'s own docblock (`:1357-1363`) makes binding for a frontend deployed
ahead of its backend. On `WorkflowDraftRow`, place the new field ADJACENT to `token` and add one line
to its docblock saying a separate field exists precisely because `token` must never be parsed as a
date (D-16) — that is where a reader will look when they wonder why there are two.

Add `updatedAt: string | undefined` to `LibraryRow` (`libraryRow.ts:52-100`). **camelCase is the
house habit here** — the type already renames `is_mine` → `isMine`. Copy the `isMine` docblock's
shape (`:83-93`): state that `undefined` means the wire did not say, and that the honest reading is
to render no `changed` segment rather than a fabricated time.

Lift the field in BOTH normalizers: `fromPublished` (`libraryFilter.ts:68`) reads
`row.updated_at ?? undefined`; `fromDraft` (`:94`) reads `row.updated_at ?? undefined`. **Never
`row.token`.** `mergeLibrary` (`:127`) needs no edit — it runs over whatever arrived, so the
`source-failed` partial path carries the field automatically.

Extend `libraryFilter.test.ts` with cases proving (a) `fromPublished` and `fromDraft` both lift
`updated_at` into `updatedAt`, (b) an absent wire field yields `undefined` rather than a fabricated
value, and (c) a POSITIVE CONTROL — a row WITH the field yields the string — so the absence assertion
in (b) cannot pass for the wrong reason (S-6).

Raise `libraryFilter.test.ts`'s pin in `scripts/vitest-count-gate.cjs` (currently `36` at `:955`) IN
THIS SAME COMMIT, **read from the gate's own `actual` column across two agreeing runs** — never
hand-counted from `it(` literals, never taken from a SUMMARY. `BASELINE_TOTAL` is derived
(`:1248`); do not hand-edit it. ⚠ `3164` is the PIN and `3188` is the MEASURED total (correction
C-5) — a plan that "re-pins to 3188" is wrong.
  </action>
  <acceptance_criteria>
    - `grep -n "updated_at?: string | null" frontend/src/lib/api.ts` returns at least the two new occurrences on `PublishedWorkflow` and `WorkflowDraftRow`
    - `grep -n "updatedAt" frontend/src/components/workflows/library/libraryRow.ts` returns the new field with a docblock naming the `undefined` = "the wire did not say" rule
    - `grep -n "token" frontend/src/components/workflows/library/libraryFilter.ts` returns NO occurrence inside `fromDraft`'s `updatedAt` assignment
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` prints **33** (unchanged from the measured baseline)
    - `cd frontend && npx eslint src/components/workflows/library/ src/pages/WorkflowsPage.tsx` exits 0 with no output
    - `libraryFilter.test.ts` contains a positive control paired with the absence assertion
    - The `libraryFilter.test.ts` pin in `scripts/vitest-count-gate.cjs` equals the number printed in the gate's `actual` column, and the SUMMARY quotes both runs
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/library/ src/pages/WorkflowsPage.test.tsx src/pages/__tests__/PublishedCardDelete.test.tsx src/pages/__tests__/RunModal.test.tsx src/pages/__tests__/RunModal.a11y.test.tsx</automated>
  </verify>
  <done>Both wire types, `LibraryRow` and both normalizers carry the field; `tsc` is unmoved at 33; eslint is 0; the eight-suite run is green with only `libraryFilter.test.ts` grown; the pin is raised from the gate's own output.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the projection did not widen access, and reconcile the gate (D-15)</name>
  <files>backend/tests/unit/test_workflows_routes.py, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `backend/app/db/workflows.py` lines 270-300 (the RLS-bypass note the owner-scoping depends on)
    - `backend/app/api/workflows.py` lines 134-150 (the BINDING RULE)
    - `192.1-RESEARCH.md` § 13 Security Domain (V4 Access Control and the mig-116 / CR-01 threat row)
    - the pre-change `git diff` of this plan's own two backend files
  </read_first>
  <action>
Prove the access boundary did not move, rather than asserting it. Run
`git diff HEAD -- backend/app/db/workflows.py` and confirm by inspection that every changed hunk is
inside a SELECT column list; record the hunk count and the confirmation verbatim in the SUMMARY.

Add (or extend) a backend case that a NON-owner caller still receives only `is_system_global` rows
from `/published` after the widening — the existing owner-scope case is the one that must stay green,
so if it already exists, name it in the SUMMARY rather than duplicating it.

Run the full count gate and reconcile: `GSD_VITEST_MAX_WORKERS=4 node scripts/vitest-count-gate.cjs`
must exit 0. Record `total`, `failed` and `pinned total` from its own output. ⚠ Two suites already
report ABOVE their pins on a clean tree (`ExternalActionSection.test.tsx` 25 → 34,
`PhaseTimeline.test.tsx` 17 → 21) — pre-existing slack, **not this plan's, and not to be "fixed" here**.

Confirm `bash scripts/check-deploy-drift.sh` stays silent — CONTEXT says confirm rather than assume.
This plan adds no env var, no migration, no bundled service and no sandbox tag.
  </action>
  <acceptance_criteria>
    - The SUMMARY quotes the `git diff --stat` for both backend files and states that every hunk is a SELECT column list
    - A named backend case proves a non-owner caller sees only `is_system_global` rows from `/published`, and it is green
    - `GSD_VITEST_MAX_WORKERS=4 node scripts/vitest-count-gate.cjs` exits 0; the SUMMARY quotes its `total · failed · pinned total` line verbatim
    - `bash scripts/check-deploy-drift.sh` exits 0 with no drift reported, and the SUMMARY says so
  </acceptance_criteria>
  <verify>
    <automated>GSD_VITEST_MAX_WORKERS=4 node scripts/vitest-count-gate.cjs && bash scripts/check-deploy-drift.sh</automated>
  </verify>
  <done>The projection is proved additive, the owner-scope case is green and named, the count gate exits 0 with its numbers recorded, and the deploy-drift check is silent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| asyncpg pool → API layer | The pool BYPASSES RLS (`db/workflows.py:277-281`). Whatever the SELECT projects and the API serializes is what the caller gets. |
| API layer → browser | Every field on `PublishedWorkflow` / `DraftRow` is public to the authenticated caller. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192.1-01 | Information Disclosure | `list_published_workflows` / `list_starter_workflows` SELECT widening | mitigate | Predicate is untouched — asserted by inspecting every diff hunk (Task 3) and by the owner-scope backend case staying green. This is the mig-116 / CR-01 shape Phase 190's review caught; `PublishedWorkflow`'s BINDING RULE (`:134-142`) is the standing control. |
| T-192.1-02 | Information Disclosure | `updated_at` as a new public field | accept | The BINDING RULE's own criterion: it "describes the ROW, never a person." It discloses when a row the caller can already see last changed. Recorded in the field docblock so a reviewer does not re-litigate it. |
| T-192.1-03 | Tampering | `CONCURRENCY_TOKEN_SQL` | mitigate | The token expression is byte-identical to HEAD (grep-asserted). A parsed-and-re-rendered token matches zero rows and makes every later save refuse as stale (D-16, probed live 2026-08-01). |
| T-192.1-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero packages installed by this plan** — no `pip install`, no `npm install`. If an install becomes necessary, STOP: the phase's scope fence forbids a new frontend dependency outright. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/unit/ -k workflow -q` — green, no new failures
- `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` → **33**
- `cd frontend && npx eslint src/components/workflows/library/ src/pages/WorkflowsPage.tsx` → exit 0, no output
- `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/pages/WorkflowsPage.test.tsx src/components/workflows/library/ src/pages/__tests__/PublishedCardDelete.test.tsx src/pages/__tests__/RunModal.test.tsx src/pages/__tests__/RunModal.a11y.test.tsx` — 8 files, 0 failed, only `libraryFilter.test.ts` above its pre-change count
- `GSD_VITEST_MAX_WORKERS=4 node scripts/vitest-count-gate.cjs` → exit 0
- `bash scripts/check-deploy-drift.sh` → silent
</verification>

<success_criteria>
- `updated_at` reaches the browser on all three feeds (published, starters, drafts)
- `LibraryRow.updatedAt` exists and is lifted by both normalizers
- Zero SQL predicate changes; zero changes to `CONCURRENCY_TOKEN_SQL`
- `tsc` unmoved at 33; eslint 0; count gate exit 0
- The `libraryFilter.test.ts` pin is raised from the gate's own `actual` column across two agreeing runs
</success_criteria>

<output>
Create `.planning/phases/192.1-workflow-identity/192.1-01-SUMMARY.md` when done.
Record: the base SHA asserted, both `pytest` readings (before/after), the `tsc`/eslint numbers, the
gate's `total · failed · pinned total` line verbatim, the raised pin and the two agreeing runs it came
from, and the confirmation that every backend diff hunk is a SELECT column list.
</output>
