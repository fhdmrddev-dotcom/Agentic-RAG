---
phase: 192.1-workflow-identity
plan: 08
status: CHECKPOINT-PENDING
subsystem: workflows-library
tags: [LIB-05, D-02, D-07, D-13, D-27, D-28, D-31, S-10, G-4, G-5, hot-file-ledger, uat]
requires:
  - "192.1-01…07 — every figure published here is measured at their close, not predicted"
  - "192.1-02 — the line classifier and its 95a4c915 known-good"
provides:
  - "CLAUDE.md: WorkflowsPage.tsx ledger row re-measured (33 commits / 12 phases / 1160 L)"
  - "CLAUDE.md: a NEW WorkflowCard.tsx ledger row at 6 commits / 2 phases / 721 L — G-5 does NOT fire, and it is on the list anyway"
  - ".planning/ROADMAP.md: the false G-5 flag corrected, struck-but-readable"
  - "WorkflowCard.tsx:154-198 — the stale subtree figures replaced on measurement (comment-only)"
  - "192.1-UAT.md — the nine G-4 rows, authored, none locating its target by id"
affects:
  - "Phase 193/194 — both ledger rows now carry a measured count instead of forcing a re-derivation"
tech-stack:
  added: []
  patterns:
    - "a measured figure is published WITH the command that re-derives it, in the cell"
    - "a wrong claim is struck and left readable beside its correction, never deleted"
    - "writing a figure into a file changes that file's count — correct the DIGITS in place to a fixed point"
key-files:
  created:
    - .planning/phases/192.1-workflow-identity/192.1-UAT.md
  modified:
    - CLAUDE.md
    - .planning/ROADMAP.md
    - frontend/src/components/workflows/library/WorkflowCard.tsx
decisions:
  - "Executed on the MAIN WORKING TREE, deliberately — a worktree run silently skips shared-file updates, which would have dropped the ROADMAP correction without reporting it"
  - "Both subtree 'before' readings published (3438 the phase base, 3469 the honest pre-cut base) so neither can be quoted as the other"
  - "Task 3 NOT executed — it is a blocking operator checkpoint and is recorded as OWED"
metrics:
  duration: ~40m
  completed: 2026-08-13
  tasks_completed: 2
  tasks_total: 3
---

# Phase 192.1 Plan 08: the ledger, the flag, the docblock and the nine rows — Summary

**⚠ THIS PLAN IS NOT COMPLETE. Tasks 1 and 2 are done and committed; Task 3 is a blocking
operator checkpoint and has NOT been executed.** It is recorded as OWED at the bottom of this file.

Two hot-file ledger rows now carry re-derived, command-backed figures — and `WorkflowCard.tsx` is on
the ledger **for the first time**, at a count that does not fire G-5, precisely because *a guardrail
cannot see what is absent from its list*. The ROADMAP's claim that it was already there was **false**
and is corrected with the wrong sentence left readable. The card's own docblock, stale for two
phases, is replaced on measurement. And the nine G-4 rows exist, none of which may find its target
the way a program would.

## Base

```
git rev-parse HEAD              -> ad360211b7c5ed21d1fbac851e1817945fc5bb9d   ✅ as dispatched
git rev-parse --abbrev-ref HEAD -> develop                                    ✅
```

**Run on the MAIN WORKING TREE, deliberately — no worktree, no `bootstrap-worktree.sh`.** This plan
edits `.planning/ROADMAP.md`, and the worktree path silently skips shared-file updates: the ROADMAP
correction, which is half the plan, would have been dropped **without being reported**. Recorded as a
decision rather than an omission. `git stash`, `git reset --hard`, `git clean` and `rm -rf` were not
used at any point.

## Commits

| # | Hash | Task | What |
|---|---|---|---|
| 1 | `1ae9e4d2` | 1 | both ledger rows, the ROADMAP flag, the card docblock (3 files, 57 ins / 30 del) |
| 2 | `c0dcb523` | 2 | `192.1-UAT.md` — 457 lines, nine rows |

**Files changed by this plan — exactly four**, `git diff --name-only ad360211..HEAD`:
`CLAUDE.md` · `.planning/ROADMAP.md` · `frontend/src/components/workflows/library/WorkflowCard.tsx` ·
`.planning/phases/192.1-workflow-identity/192.1-UAT.md`. **`.planning/STATE.md` was NOT modified.**
Zero deletions in either commit (`git diff --diff-filter=D` empty on both). Zero packages installed.

---

## Task 1 — the measurements

### The classifier was RE-VALIDATED before one figure was trusted

```
git show 95a4c915:frontend/src/components/workflows/PhaseNodeCard.tsx | node classify.cjs
  ->  797   518   249   30        (188.2's published known-good: 797 / 518 / 249 / 30)   ✅ exact
  wc -l cross-check                                                       -> 797         ✅
```

Reproduced exactly, first attempt, no correction needed. An inherited classifier is an inherited
claim until it agrees with the known-good again.

### Hot-file counts — re-derived, with the command beside each

```bash
git log --oneline -- frontend/src/pages/WorkflowsPage.tsx | wc -l
git log --format=%s -- <file> | sed -E 's/^[a-z]+\(([^)]+)\).*/\1/' | sed -E 's/-.*//' | sort -u
wc -l <file>
```

| File | Commits | Phases | Lines | Ledger read before | Verdict |
|---|---|---|---|---|---|
| `WorkflowsPage.tsx` | **33** | **12** — 103 124 143 152 155 165 184 184.1 186 188 192 **192.1** | **1160** | 30 / 11 / 1180 | **stale in all three** |
| `WorkflowCard.tsx` | **6** | **2** — 192, 192.1 | **721** | *absent from the ledger* | **added** |

192.1's own commits: page ×3 (`790ce24d` the cut, `38cb3cab`, `d5b788f6`), card ×2 (`59c7a698`,
`b351627e`).

### ⚠ The page's single line count hides its shape — the arc is published instead

`1180 → 1054` at the D-01 fork cut (**−126**) → `1113` (the identity memo) → `1160` (the dialog
mount). **NET −20 across a phase whose headline act was an extraction.**

The plan's task text predicted *"it should: ~225 L moved out"*. Measured, **both halves are true and
neither is the other**: ~225 lines of fork concern left the page and the page fell only 126, because
the cut leaves receipt prose at each emptied site (Wave 3 measured that gap independently); then
Waves 6 and 7 added 106 back for the identity memo and the dialog mount. An extraction that removes
126 lines and a phase that nets −20 are two facts. Quoting one as the other is how the next estimate
goes wrong.

### The subtree — SIX numbers, not four, and all six are published

```bash
node classify.cjs frontend/src/pages/WorkflowsPage.tsx \
  $(ls frontend/src/components/workflows/library/*.ts \
       frontend/src/components/workflows/library/*.tsx | grep -v '\.test\.')
```
Test files and `__fixtures__/` excluded, as 188.2 and Phase 192 both excluded theirs — **stated,
not assumed**.

| Reading | TOTAL | COMMENT | CODE | BLANK |
|---|---|---|---|---|
| pre-192 page alone (`6bdc4684`) | 1407 | 334 | 1021 | 52 |
| **phase base** `8fc9bd74`, page + 7 modules | **3438** | 1725 | **1547** | 166 |
| **honest pre-CUT base** (after Wave 1's `updatedAt`: filter 284→294, row 100→121, **+31**) | **3469** | — | **1550** | — |
| **NOW**, page + **12** modules | **5323** | **2964** | **2095** | **264** |

**⚠ BASELINE §8b's `3438` was ALREADY STALE at Wave 3's own base**, by +31, and Wave 3 said so. Both
"before" readings are therefore defensible and **both are published** in the ledger cell and in the
docblock, so neither can be quoted as the other:

```
PAGE      1407 → 1160   (−17.6 %)    CODE 1021 →  462  (−54.8 %)
SUBTREE   1407 → 5323   (+278.3 %)   CODE 1021 → 2095  (+105.2 %)
192.1 vs the phase BASE:      3438 → 5323  (+54.8 %)   CODE 1547 → 2095  (+35.4 %)
192.1 vs the honest PRE-CUT:  3469 → 5323  (+53.4 %)   CODE 1550 → 2095  (+35.2 %)
```

**The growth is attributed, not merely admitted.** Nine per-file deltas against the phase base:
page −20 · `libraryFilter` +10 · `libraryRow` +21 · `libraryVocabulary` +216 · `WorkflowCard` +180 ·
`LibraryToolbar` 0 · `RunModal` 0 · `WorkflowDeleteSheet` 0 · five NEW modules +1478
(`useWorkflowFork` 517, `rowIdentity` 509, `ForkNameDialog` 279, `relativeChanged` 109,
`libraryFork` 64). **They sum to exactly +1885, which is `5323 − 3438` — no unattributed residual.**

**PROSE is the dominant term for the third cut running:** COMMENT `1725 → 2964` = **+1239 L, 65.7 %
of the growth**, against CODE +548 (29.1 %) and BLANK +98 (5.2 %).

### ⚠ Writing the figures CHANGED them — and my own prediction was wrong by 8 lines

Wave 3 hit this loop and resolved it by correcting digits in place; the same happened here and is
recorded rather than smoothed. I estimated the docblock at **+18 lines**; measured, it was **+26**
(card 721 → **747**, subtree 5297 → **5323**, comment 2938 → **2964**). Every digit correction was
chosen to be the **same character width**, and the fix was verified mechanically:

```
chars before / after the digit correction:  42365 / 42365   (equal -> the line count cannot move)
re-run classifier                        ->  747  /  5323 / 2964   ✅ fixed point
```

### The ROADMAP flag (D-02)

Corrected in place, **struck but readable** — this project's habit is to leave the wrong claim beside
its correction. It said *"G-5: `WorkflowCard.tsx` and `WorkflowsPage.tsx` are both on the hot-file
ledger."* The flag is not deleted; it is made true, and it now records that G-5 fired on
`WorkflowsPage.tsx` alone, was HONOURED in Wave 3 (the extraction shipped **before** the feature it
made room for), and that `WorkflowCard.tsx` is on the ledger **now**.

### The card docblock (S-10 / Addendum-2 finding 6)

`git diff --numstat` → **54 insertions / 28 deletions, and every changed line is a comment** —
verified by filtering the diff for lines not beginning `+ *` / `- *`; the filter returned **nothing**.
No behaviour, no string, no testid, no import moved. The stale `PAGE 1407 → 1007` / `SUBTREE 1407 →
3182` pair is **left readable under a `WAS (192-12):` label** beside the measured replacement, with
BASELINE §8c's correction B-6 cited (which found the CODE columns stale too, not just the totals).

---

## Task 2 — `192.1-UAT.md`

457 lines. Nine rows `U1…U9`, each carrying **`do:` / `see:` / `fail:` / `result:` / `saw:`**, with
`result:` deliberately blank for the operator's drive. `U4` carries `(a)`/`(b)` splits with a
required written `answer` on each.

**D-27 is the file's header, in the required words**, with Phase 192's failure named as the reason:
*"A machine check that bypasses the human's task does not verify the human's task."*

**The prohibition is mechanically auditable, and the file says how.** Case-insensitive
`grep -ni "getElementById\|by id"` returns **exactly 4 lines**:

| Line | Where |
|---|---|
| 25, 30, 42 | inside `## ⚠ THE RULE THAT MAKES THIS FILE NECESSARY` (starts `:20`) |
| 450 | inside `## Sign-off` (starts `:444`) |

`## Tests` starts at `:166`. **Zero hits between 166 and 444** — no row instructs anyone to locate
anything that way. DevTools is sanctioned in exactly one place (U7) and there it **blocks a request**;
the file says so explicitly so the exception cannot be read as a loophole.

**The two residuals are written IN, not left to be discovered:**

- **U4 / D-31.** On the real 43-name *Compliance Gap Report* family the ranker narrows the largest
  byte-identical block **41 → 39 and no further**, guarded by a `>= 35` **floor** test that fails if
  a future change makes the number look good by inventing a technical discriminator. `1 of N` is then
  a **true warning**, and the product's answer is the 162-B rename (which U5 drives). SC#1 is judged
  on the realistic fixture — **75 distinct renderings across 106 rows**, 24 distinct discriminator
  strips across the 93 colliding rows, largest identical block 5 — and the dev-data residual is
  reported **beside** it, never instead of it.
- **The `compliance-gap-report` orphan (D-13 / D-30).** The base slug of the 43-row family is itself
  orphan-shaped (`report` is six legal lowercase alphanumerics), so **the family's TRUE ORIGINAL
  renders no lineage segment while its 43 copies read `Copy of Compliance Gap Report starter`.**
  Correct under D-13, stated with the measured trade **57 resolve · 12 go quiet · 0 lie**, and
  flagged as *"this will look like a bug unless you have read this paragraph."*

Also in the file: sketch `163-the-assembled-card/index.html` named as the visual reference **with the
five things no automated check covers listed by name** (spacing, Tailwind class choices, wrap,
truncation, hover/focus); the **cross-provider roster STATED as N/A** with its reason rather than
omitted; the known pre-existing REDs table; and a sign-off checklist requiring `⛔ + reason` for any
blocked row — *a scoreboard that lists only what passed is not a scoreboard.*

---

## Measured gate results

| Gate | Base (`ad360211`) | After | Verdict |
|---|---|---|---|
| `tsc -p tsconfig.app.json --noEmit` | **33** | **33** | ✅ unmoved |
| `eslint src/components/workflows/library/ src/pages/WorkflowsPage.tsx` | 0 | **exit 0**, no output | ✅ |
| library subtree scoped run | — | **8 files · 419 tests · 0 failed** | ✅ |
| count gate | `total 3436 · failed 0 · pinned total 3412`, 64/64 | **identical**, exit 0 | ✅ unmoved |
| `npm run lint:a11y` | 4 errors, exit 1 | **the same 4**, exit 1 | ⚠ pre-existing (see below) |
| `bash scripts/check-deploy-drift.sh` | — | **`RESULT: PASS`, exit 0** | ⚠ not "silent" (see below) |

`WorkflowCard.test.tsx` is inside the 419 and unmoved at its pinned 80; the gate's own report shows
**no per-file decrease** across all 64 pinned files.

---

## Deviations from Plan

### 1. [Measurement] D-02's *"the SINGLE occurrence"* — two readings, both true, both published

D-02 says *"The **single** occurrence of that filename in `CLAUDE.md` sits inside the
`WorkflowsPage.tsx` row's prose."* Measured at the base commit:

| Command | Before | After |
|---|---|---|
| `grep -c "WorkflowCard.tsx" CLAUDE.md` (**lines**) | **1** | **2** |
| `grep -o "WorkflowCard.tsx" CLAUDE.md \| wc -l` (**occurrences**) | **2** | **7** |

D-02 is **exactly right at the line level and wrong at the occurrence level** — both prose mentions
sit on the *same* line, because the `WorkflowsPage.tsx` row is one enormous line. The conclusion is
untouched (neither was a row). Both readings are published in the ROADMAP correction and the new
ledger cell. ⚠ The plan's acceptance criterion *"`grep -c … ` is now ≥ 2"* is satisfied (**2**), but
it is a **line** count — worth knowing before someone reads it as an occurrence count.

### 2. [Measurement] The plan predicted the docblock delta; the prediction was wrong

Estimated +18 lines, measured **+26**. Resolved to a fixed point by correcting digits in place at
identical character width, verified by an equal before/after character count and a re-run classifier.
Recorded because the plan's own warning (*"expect it"*) proved correct in the direction of a **larger**
delta than a careful estimate produced.

### 3. [Measurement] `check-deploy-drift.sh` is NOT "silent"

The plan's verification says it *"should stay silent"*. Measured: it prints a four-section report and
**two non-blocking WARNs**, then `RESULT: PASS` at **exit 0**. Neither WARN is this plan's — one lists
seed-bearing migrations above #089 for human review, the other says `docker compose` is unavailable in
this shell so the structural fallback ran. This plan adds no env var, no migration, no bundled service
and no sandbox tag, so it could not have caused either. **PASS is the real bar; "silent" was never
true of this script.**

### 4. [Scope] Executed on the main working tree rather than a worktree

Directed by the orchestrator and recorded as a decision: the worktree path silently skips shared-file
updates, so the `.planning/ROADMAP.md` correction — half of D-02 — would have been dropped **without
being reported**. Nothing destructive was run; hooks ran normally on both commits.

---

## Known pre-existing REDs, untouched and out of scope

- `test_dual_mode_wiring.py::test_published_workflows_list_endpoint` (**DEF-192.1-01**) — backend, not
  run here.
- **4 `npm run lint:a11y` errors**, unchanged and identical to the phase base:
  `setup/PresetPickerStep.tsx:104` · `setup/SetupTokenGate.tsx:163` · `setup/SmokeChecklist.tsx:320` ·
  `pages/WorkflowRunPage.tsx:986`. None is in the library subtree.
- **3 inherited `title=` attributes** from `WorkflowSoul.tsx:99` and `PhaseSpine.tsx:77` — Phase
  124/187's, carved out by D-01.
- Unpinned count-gate slack, exactly +24 and fully attributed at the base: `ExternalActionSection` +9,
  `builderStore` +6, `WorkflowBuilderPage.canvas` +5, `PhaseTimeline` +4.
- **`SEED-154`** — definitions written double-encoded; its consequence for this phase (an empty
  *project* discriminator on most dev rows) is recorded in the UAT file.

## Known Stubs

None. This plan wrote documentation and one comment-only source edit; every figure it publishes is
measured at HEAD and carries its command.

## Threat Flags

None. No network endpoint, no auth path, no file access, no schema change, no runtime code path
touched. Register dispositions:

| Threat | Disposition | Evidence |
|---|---|---|
| T-192.1-23 — a ledger figure asserted rather than derived | **mitigated** | every number in both rows and the docblock carries its command in the cell; the classifier re-validated at `95a4c915` first; both `3438` and `3469` published |
| T-192.1-24 — a UAT drive that bypasses the human's task | **mitigated** | D-27 verbatim in the file's header; 4 audited phrase hits, all outside the Tests section; `⛔`-not-omission required per row |
| T-192.1-25 — information disclosure | **n/a** | the only source edit is comment-only (54/28, filter for non-comment changed lines returned nothing) |
| T-192.1-SC — package installs | **mitigated** | **zero packages installed** |

---

## ⛔ TASK 3 — NOT EXECUTED. A BLOCKING OPERATOR CHECKPOINT, OWED.

`<task type="checkpoint:human-verify" gate="blocking">` — **Task 3: Operator drives the G-4 rows
against the real library.** It was deliberately not executed and was not simulated. Per D-27, a
headless or Chrome-MCP drive is **not** a substitute for the human's reading; substituting one would
reproduce the exact failure this phase exists to correct.

**What the operator owes:**

1. Start the app at `http://localhost:5173/` and open Workflows against the **real** library.
2. Work `U1 → U9` in order, from `.planning/phases/192.1-workflow-identity/192.1-UAT.md`. **Drive by
   looking** — search, read the identity line, click what you pointed at.
3. **U1 first** — it is SC#1 and it is the row Phase 192 failed.
4. **U8** needs `.planning/sketches/163-the-assembled-card/index.html` open side by side, plus a
   375 px viewport check.
5. **U7** needs DevTools request-blocking on `/workflows/drafts`, then a reload.
6. Record `pass` / `fail` / `⛔ + reason` in every row's `result:`, with what was seen in `saw:`.
7. **U4 asks two design questions and both need a written answer** — does `1 of 43` read as an index
   (a one-line fix in one file if so), and does the line honestly present itself as a warning on rows
   it cannot disambiguate?

**Read before driving:** the file's two residual sections — the D-31 residual (`41 → 39`, floor 35)
and the `compliance-gap-report` orphan (silent original, `Copy of …` children). Both **will** look
like defects on screen and neither is one.

**Routing after the drive:** any `fail` is SIZED first — ≤ 1 file / ≤ 10 lines with no schema or API
surface is `/gsd:fast` under **G-3**, never a gap-closure round (**G-7**).

---

## Self-Check: PASSED

Files:
- FOUND: `.planning/phases/192.1-workflow-identity/192.1-UAT.md` (457 L)
- FOUND: `CLAUDE.md` — `grep -c '^| \`frontend/src/components/workflows/library/WorkflowCard.tsx\`'` → **1** (a table ROW, not prose)
- FOUND: `.planning/ROADMAP.md` — the struck flag + its correction
- FOUND: `frontend/src/components/workflows/library/WorkflowCard.tsx` (747 L)

Commits: `1ae9e4d2` FOUND · `c0dcb523` FOUND.

`.planning/STATE.md` NOT modified. Task 3 NOT executed.
