---
phase: 192.1-workflow-identity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/192.1-workflow-identity/192.1-BASELINE.md
autonomous: true
requirements: [LIB-05]
gap_closure: false

must_haves:
  truths:
    - "Every number the D-01 extraction will be judged against was measured on the UNMOVED page and is timestamped to a named commit"
    - "The line classifier used to publish the extraction's growth was validated against a known-good before any 192.1 number was trusted"
    - "The twelve fork-arc characterization cases are recorded green BEFORE one line moves"
  artifacts:
    - path: ".planning/phases/192.1-workflow-identity/192.1-BASELINE.md"
      provides: "The pre-move reading: eight suite counts, tsc, eslint, the count gate, the WorkflowsPage.tsx source digest, and the blank/comment/code classification of the page and the subtree"
      min_lines: 60
      contains: "95a4c915"
  key_links:
    - from: ".planning/phases/192.1-workflow-identity/192.1-BASELINE.md"
      to: "frontend/src/pages/WorkflowsPage.tsx"
      via: "an FNV-1a digest of the file's exact pre-move source"
      pattern: "WORKFLOWS_PAGE_DIGEST"
---

<objective>
Capture every baseline the D-01 extraction will be measured against, on the UNMOVED tree, and record
them in one dated artifact.

Purpose: **the 188.1 lesson — a characterization baseline only proves something if it PREDATES the
change.** 188.1 measured nine inherited claims FALSE across five plans for exactly this reason, and
D-01 makes the pre-move capture a requirement rather than a courtesy. This plan writes no production
code, deliberately: it exists so Plan 03 cannot estimate what it should measure.
Output: `192.1-BASELINE.md` — the stick every later plan in this phase measures against.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⚠ **WORKTREE BOOTSTRAP IS YOUR FIRST ACTION:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`.
It is idempotent and fails loudly. **NEVER `rm -rf` a worktree** — `bash scripts/teardown-worktree.sh <path>`.

⚠ **Assert your base SHA (`git rev-parse HEAD`) and put it in the artifact's header.** A baseline
without a commit is a number without a stick. Twelve of twelve Phase 192 worktrees forked from the
wrong base; if your HEAD is not the SHA the orchestrator dispatched, STOP and report.

⚠ **`GSD_VITEST_MAX_WORKERS=4` on every vitest invocation.** This plan runs concurrently with Plan 01
and the cap is calibrated for exactly TWO concurrent runs.

⚠ **CHANGE NO SOURCE FILE.** If you find yourself editing anything under `frontend/src`,
`backend/app` or `scripts/`, you have left the plan.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/192.1-workflow-identity/192.1-CONTEXT.md
@.planning/phases/192.1-workflow-identity/192.1-RESEARCH.md
@.planning/phases/192.1-workflow-identity/192.1-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure the eight suites, the two typecheck knobs, the lint scope and the count gate on the unmoved tree</name>
  <files>.planning/phases/192.1-workflow-identity/192.1-BASELINE.md</files>
  <read_first>
    - `192.1-VALIDATION.md` § "Measured baselines at HEAD = 92eb251d" (the table this task re-derives) and § "The gate's two knobs"
    - `192.1-RESEARCH.md` § 2a, § 2b, § 2c, § 8a-8e
    - `scripts/vitest-count-gate.cjs` lines 905-975 (the pin block) and 1245-1260 (`BASELINE_TOTAL`, `TARGETS`)
    - `frontend/src/pages/WorkflowsPage.test.tsx` lines 1-110 (the `asyncUtilTimeout: 15000` setting at :63 — trap T-4)
  </read_first>
  <action>
Create `.planning/phases/192.1-workflow-identity/192.1-BASELINE.md` with a header stating the
measured commit SHA, the date, and the sentence that this artifact exists because a baseline only
proves something if it PREDATES the change (D-01, the 188.1 lesson).

Run and record, each with the command that produced it:

1. The scoped eight-suite run —
   `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/pages/WorkflowsPage.test.tsx src/components/workflows/library/ src/pages/__tests__/PublishedCardDelete.test.tsx src/pages/__tests__/RunModal.test.tsx src/pages/__tests__/RunModal.a11y.test.tsx`
   Record the per-file counts for all eight suites and the total. RESEARCH read 48 / 39 / 64 / 36 /
   36 / 32 / 16 / 32 = **303**; re-derive, do not copy. ⚠ `--reporter=basic` was REMOVED in vitest 4
   (`ERR_LOAD_URL`) — use the default reporter or `--reporter=json`.
2. `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` — RESEARCH read
   **33**. ⚠ Also run bare `npx tsc --noEmit` once and record that it checks ZERO files, so the
   two-knob fact is captured in this phase's own artifact rather than inherited (T-2).
3. `cd frontend && npx eslint src/components/workflows/library/ src/pages/WorkflowsPage.tsx` —
   RESEARCH read exit 0 / no output. Record that this is a SCOPED claim: `eslint.a11y.config.js`'s
   own header mentions ~160 pre-existing non-a11y errors app-wide.
4. `cd frontend && npm run lint:a11y` — the CI gate. Record its exit code.
5. `GSD_VITEST_MAX_WORKERS=4 node scripts/vitest-count-gate.cjs` — record the final
   `total N · failed N · pinned total N` line VERBATIM and the pinned-file count. ⚠ Record explicitly
   that `BASELINE_TOTAL` (the PIN) and the MEASURED total are different numbers (correction C-5), and
   name the two suites already reporting above their pins on a clean tree
   (`ExternalActionSection.test.tsx`, `PhaseTimeline.test.tsx`) so a later plan does not read them as
   its own regression.

Run the count gate TWICE and record both readings. `GSD_VITEST_MAX_WORKERS=4` REDUCES the `failed`
flake but does not eliminate it (D-192-DEF-01, measured `0 / 0 / 1` across three capped runs on one
tree) — **the COUNT columns are the regression backstop; the `failed` line, on this machine, is not.**
Say that in the artifact.
  </action>
  <acceptance_criteria>
    - `.planning/phases/192.1-workflow-identity/192.1-BASELINE.md` exists, opens with the measured SHA and the date, and every number is followed by the exact command that produced it
    - All eight suite counts are individually listed plus a total
    - The `tsc -p tsconfig.app.json` count and the bare `tsc --noEmit` zero-file observation are both recorded
    - The count gate's `total · failed · pinned total` line appears verbatim, TWICE (two runs), with any disagreement stated rather than smoothed
    - The artifact names the two pre-existing above-pin suites and says they are not this phase's
    - `git status --porcelain -- frontend/ backend/ scripts/` prints NOTHING
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/192.1-workflow-identity/192.1-BASELINE.md && grep -q "pinned total" .planning/phases/192.1-workflow-identity/192.1-BASELINE.md && test -z "$(git status --porcelain -- frontend/ backend/ scripts/)"</automated>
  </verify>
  <done>The artifact carries eight suite counts, both typecheck readings, both lint readings and two count-gate readings, each with its command, and no source file was touched.</done>
</task>

<task type="auto">
  <name>Task 2: Capture the fork-arc characterization reading, the page source digest, and the validated line classification (D-01)</name>
  <files>.planning/phases/192.1-workflow-identity/192.1-BASELINE.md</files>
  <read_first>
    - `frontend/src/pages/WorkflowsPage.test.tsx` lines 820-1160 (the four fork-arc describes at :823, :886, :1017, :1119 — twelve cases)
    - `frontend/src/components/workflows/library/librarySubtree.fences.test.ts` lines 414-446 (the in-test FNV-1a `digest` helper and the `THREAD_GROUPS_DIGEST` shape)
    - `frontend/src/components/workflows/library/WorkflowCard.tsx` lines 105-145 (the docblock that publishes 188.2/192's measured growth — the template, and the stale `:119-120` figures)
    - `192.1-RESEARCH.md` § 2c, § 2d · `192.1-PATTERNS.md` § S-10
  </read_first>
  <action>
**(a) The twelve fork-arc cases.** Enumerate them by line and title from
`WorkflowsPage.test.tsx` — the four describes at `:823` (Tweak forks a v(N+1) draft, 3 cases), `:886`
(192-14 the U5 blocker, 3 cases), `:1017` (192-15 WR-03 a fork click that fails says so, 4 cases,
including `:1081`'s T-192-42 exactly-two-`createWorkflowDraft`-calls pin), `:1119` (the starter row and
its fresh-copy fork, 2 cases). Record all twelve titles and that they are GREEN at this SHA. This is
the characterization set D-01 requires green with **ZERO re-capture** across the move — the artifact
is the timestamped record that they passed before anything moved.

**(b) The page source digest.** Compute an FNV-1a digest of `frontend/src/pages/WorkflowsPage.tsx`'s
exact bytes at this commit using the in-test helper's algorithm (`librarySubtree.fences.test.ts:433-442`)
— **NOT `node:crypto`**, which adds a 34th `TS2307` because `tsconfig.app.json` carries no Node type
declarations (T-6). A throwaway node script outside the app's tsconfig is fine; do not add a file to
`frontend/src`. Record the digest as `WORKFLOWS_PAGE_DIGEST` together with `wc -l` (expected **1180**).
State plainly that this is NOT a fence — the page must change — and exists only so Plan 03's SUMMARY
can state the exact delta rather than estimate it.

**(c) The line classifier, VALIDATED FIRST.** Write a named blank/comment/code line classifier
(counting `{/* … */}` JSX comment blocks as comment, matching 188.2's and Phase 192's) and
**validate it against 188.2's published known-good before trusting any 192.1 number**:
`git show 95a4c915:frontend/src/components/workflows/library/WorkflowCard.tsx` must classify to
**797 total / 518 non-code / 249 code / 30 blank**. If it does not reproduce exactly, fix the
classifier and say so — do not proceed to (d) with an unvalidated tool.

**(d) Classify the pre-move tree.** Run the validated classifier over
`frontend/src/pages/WorkflowsPage.tsx` and over each of the seven current `library/` source modules
(`libraryFilter.ts`, `libraryRow.ts`, `LibraryToolbar.tsx`, `libraryVocabulary.ts`, `RunModal.tsx`,
`WorkflowCard.tsx`, `WorkflowDeleteSheet.tsx` — test files EXCLUDED, as 188.2 and 192 both excluded
them). Record per-file and summed totals plus the subtree total (page + seven). RESEARCH §2d measured
the subtree at **3438** and the page at **1180**; re-derive rather than copy, and if your reading
differs, publish yours with the derivation. Note in the artifact that `WorkflowCard.tsx:119-120`
currently states `PAGE 1407 → 1007` and `SUBTREE 1407 → 3182`, both stale, and that Plan 07 owes the
correction on measurement.

⚠ **Do not predict the phase's growth.** This artifact records only what is true now. 188.2 measured
+67.1 % and Phase 192 measured +126.2 % against its own +5…+16 % estimate — an ~8× miss. The number
is measured at the end, never forecast at the start.
  </action>
  <acceptance_criteria>
    - All twelve fork-arc case titles are listed with their line numbers and recorded green
    - `WORKFLOWS_PAGE_DIGEST` is present as a hex string, with the algorithm named as the in-test FNV-1a helper and `node:crypto` explicitly ruled out
    - The classifier's validation run against `95a4c915:WorkflowCard.tsx` is recorded and equals `797 / 518 / 249 / 30`; if it did not on the first attempt, the artifact says so
    - Per-file and summed blank/comment/code figures exist for the page and all seven library modules, with the total
    - The artifact states that the stale `WorkflowCard.tsx:119-120` figures are owed a correction in Plan 07
    - The artifact contains NO predicted growth number
    - `git status --porcelain -- frontend/ backend/ scripts/` prints NOTHING
  </acceptance_criteria>
  <verify>
    <automated>grep -q "95a4c915" .planning/phases/192.1-workflow-identity/192.1-BASELINE.md && grep -q "797" .planning/phases/192.1-workflow-identity/192.1-BASELINE.md && grep -q "WORKFLOWS_PAGE_DIGEST" .planning/phases/192.1-workflow-identity/192.1-BASELINE.md && test -z "$(git status --porcelain -- frontend/ backend/ scripts/)"</automated>
  </verify>
  <done>The twelve characterization cases, the page digest and a KNOWN-GOOD-VALIDATED line classification of the pre-move page and subtree are all recorded, and no source file was touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed) | This plan reads the repository and writes one planning document. No runtime surface, no data, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192.1-02a | Tampering | The measurement itself — an inherited or predicted number recorded as measured | mitigate | Every figure is followed by the command that produced it, and the classifier is validated against 188.2's published known-good (`95a4c915` = `797/518/249/30`) before any 192.1 number is trusted. |
| T-192.1-02b | Repudiation | A baseline with no commit is unfalsifiable | mitigate | The artifact opens with `git rev-parse HEAD` and the executor asserts the dispatched base SHA. |
| T-192.1-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero packages installed.** The throwaway digest script uses plain Node builtins outside the app tsconfig; no dependency is added and none may be. |
</threat_model>

<verification>
- `test -f .planning/phases/192.1-workflow-identity/192.1-BASELINE.md`
- `git status --porcelain -- frontend/ backend/ scripts/` → empty (no source touched)
- The artifact contains: the SHA, 8 suite counts, `tsc` 2 readings, eslint 2 readings, 2 count-gate lines, 12 case titles, `WORKFLOWS_PAGE_DIGEST`, and the validated classifier's `797/518/249/30` reproduction
</verification>

<success_criteria>
- One dated artifact carries every stick Plans 03-07 will be measured against
- Every number has its command; no number is inherited from RESEARCH or CONTEXT without re-derivation
- The line classifier is proved correct against a published known-good before use
- No production file changed
</success_criteria>

<output>
Create `.planning/phases/192.1-workflow-identity/192.1-02-SUMMARY.md` when done.
Record: the asserted base SHA, any figure that DISAGREED with RESEARCH's reading (with both numbers),
and the classifier's validation result.
</output>
