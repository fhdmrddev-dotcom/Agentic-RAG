---
phase: 192.1-workflow-identity
plan: 05
type: execute
wave: 4
depends_on: ["192.1-04"]
files_modified:
  - frontend/src/components/workflows/library/rowIdentity.ts
  - frontend/src/components/workflows/library/rowIdentity.test.ts
  - frontend/src/components/workflows/library/librarySubtree.fences.test.ts
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [LIB-05]
gap_closure: false

must_haves:
  truths:
    - "For a row whose name collides, the resolver produces at least one fact that actually narrows the set of namesakes, and never more than two"
    - "A row whose slug looks like a copy but whose parent is absent produces NO lineage claim at all — silence, not 'Original'"
    - "The number in '1 of N' counts the whole merged library, so typing in search never changes it"
    - "The index is built once per rows change, not once per row per render"
  artifacts:
    - path: "frontend/src/components/workflows/library/rowIdentity.ts"
      provides: "buildIdentityIndex + resolveIdentity — the O(n) precomputed index, the four-state lineage union and the D-31 discrimination ranker"
      contains: "export function buildIdentityIndex"
    - path: "frontend/src/components/workflows/library/rowIdentity.test.ts"
      provides: "the D-31 ranker proof, the three-state lineage proof, the >20-distinct-renderings property and the O(n) call-count property"
      contains: "compliance-gap-report"
  key_links:
    - from: "frontend/src/components/workflows/library/rowIdentity.ts"
      to: "frontend/src/components/workflows/library/libraryVocabulary.ts"
      via: "every rendered word imported, never typed"
      pattern: "from \"\\./libraryVocabulary\""
    - from: "frontend/src/components/workflows/library/rowIdentity.ts"
      to: "frontend/src/components/workflows/library/libraryFilter.ts"
      via: "CHIP_PREDICATES.yours — the ONE ownership predicate, never a second copy"
      pattern: "CHIP_PREDICATES\\.yours"
---

<objective>
Build the identity resolver: a **precomputed O(n) index** over the merged library, a **four-state
lineage union** whose fourth state is silence, and the **D-31 discrimination ranker** that replaces
the approved sketch's `varies()` because a measurement contradicted it on the exact cluster this
phase was inserted for.

Purpose: SC#1 and SC#2. The line must say something that actually tells this row apart from its
namesakes — and where it cannot, it must say so honestly rather than pretend.
Output: `library/rowIdentity.ts` + its suite, plus the two NEW subtree fences (no `token` parsed as a
date, no owner display name).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⚠ **WORKTREE BOOTSTRAP IS YOUR FIRST ACTION:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`.
**NEVER `rm -rf` a worktree** — `bash scripts/teardown-worktree.sh <path>`.
⚠ Assert the dispatched base SHA and record it. ⚠ `GSD_VITEST_MAX_WORKERS=4` on every vitest run.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/192.1-workflow-identity/192.1-CONTEXT.md
@.planning/phases/192.1-workflow-identity/192.1-RESEARCH.md
@.planning/phases/192.1-workflow-identity/192.1-PATTERNS.md
@.planning/phases/192.1-workflow-identity/192.1-VALIDATION.md
@.planning/sketches/163-the-assembled-card/BUILD-CONTRACT.generated.md

<interfaces>
<!-- The contract this plan creates. Downstream (Plan 06) implements against exactly this. -->

// library/rowIdentity.ts — PURE, no React, unit-proved without a DOM
export type Lineage =
  | { kind: "original" }
  | { kind: "copy"; ofName: string; ofIsStarter: boolean }
  | { kind: "version"; mine: number; parent: number }
  | { kind: "unknown" }                       // ← D-13's THIRD state: renders NOTHING

export type IdentityAxis = "lineage" | "project" | "state" | "version"

export interface RowIdentity {
  own: string                 // OWN_YOURS | OWN_SHARED  — from CHIP_PREDICATES.yours
  segs: readonly string[]     // MAX 2 (D-04), possibly empty
  ofN: string | null          // oneOfLabel(n) — ONLY when the name collides
  when: string | null         // relativeChanged(row.updatedAt, now) — null when the wire did not say
}

export interface IdentityIndex { /* opaque to callers */ }
export function buildIdentityIndex(rows: readonly LibraryRow[]): IdentityIndex
export function resolveIdentity(index: IdentityIndex, row: LibraryRow, now: number): RowIdentity

The rendered grammar (BUILD-CONTRACT.generated.md):
  [own pill] [computed seg] · [computed seg] [1 of N] · changed <rel>

The rendered lines per row shape, from the generated contract (separators inserted by the card):
  Access Review Attestation  →  Yours · Original · Ready to run · 1 of 2 · changed 5 months ago
  Access Review Attestation  →  Yours · v2 of v1 · Still building · 1 of 2 · changed 2 weeks ago
  Audit Trail Extract        →  Shared · changed 5 months ago
  Compliance Gap Report      →  Shared · No project · Shared starter · 1 of 43 · changed 2 months ago
  Q3 EU gap review           →  Yours · Copy of Access Review Attestation · changed just now

The SHIPPED ownership predicate — libraryFilter.ts:159, consumed at WorkflowCard.tsx:361 :
  yours: (row: LibraryRow) => row.isMine ?? row.provenance !== "starter"
  ⚠ NEVER treat `undefined` as `false`; NEVER write a second copy of this test.

The sketch's O(n³) resolver being replaced — 163/index.html:297-320 — and the fixture's
`lineageOf` at library-fixture-192-1.js:334-350, whose `if (!row.parentSlug) return null;`
short-circuit is a FIXTURE-ONLY field with no production equivalent. Production must delete that line,
and deleting it is exactly what admits the orphans.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildIdentityIndex — three linear passes, Map/Set only, four-state lineage (D-12, D-13, D-32, D-34)</name>
  <files>frontend/src/components/workflows/library/rowIdentity.ts, frontend/src/components/workflows/library/rowIdentity.test.ts</files>
  <read_first>
    - `frontend/src/components/workflows/library/libraryFilter.ts` lines 1-30 (the purity header), 60-140 (`fromPublished` / `fromDraft` / `mergeLibrary` and the `Map`-as-invariant argument at :119-138), 143-166 (`CHIP_PREDICATES` and the `as const satisfies Record<…>` idiom), 220-235 (the runtime exhaustiveness guard)
    - `frontend/src/components/workflows/library/libraryRow.ts` (whole file — the shape, including `updatedAt` and `isMine`)
    - `frontend/src/components/workflows/deriveTier.ts` lines 55-130 (the table idiom + its exhaustiveness guard)
    - `frontend/src/components/workflows/ownProperty.ts` (whole file — 86 L, the ONE shipped own-guard spelling; the preferred outcome is that you never need it)
    - `frontend/src/components/workflows/phaseVocabulary.test.ts` lines 750-762 (the WR-04 inherited-key probe)
    - `frontend/src/pages/WorkflowsPage.tsx` lines 515-540 (`draftBySlug` — the shipped `useMemo`-builds-a-`Map` precedent)
    - `192.1-CONTEXT.md` D-03, D-04, D-05, D-06, D-12, D-13, D-32, D-34 · `192.1-RESEARCH.md` § 4a-4d, § 5b · `192.1-PATTERNS.md` § 1, § S-2
  </read_first>
  <behavior>
    - `buildIdentityIndex(rows)` runs three passes and returns in O(n); calling `resolveIdentity` once per row is O(1) per call
    - a row whose slug is `<base>-<6 base-36 chars>` AND whose `<base>` slug is present → `{ kind: "copy", ofName, ofIsStarter }`
    - a row sharing a slug with a LOWER-versioned row in the list → `{ kind: "version", mine, parent }`, and the version branch is checked BEFORE the copy branch
    - a row whose slug has no fork shape → `{ kind: "original" }`
    - a row whose slug HAS the fork shape but whose parent is ABSENT → `{ kind: "unknown" }` and renders nothing
    - `compliance-gap-report` (present in the fixture, no `compliance-gap` parent) resolves `unknown`; its `…-<hash>` children resolve `copy` with `ofName === "Compliance Gap Report"`
    - a row whose slug is `constructor`, `__proto__`, `toString` or `hasOwnProperty` cannot reach a prototype member — the lookup returns "absent", not a function
    - `own` equals `OWN_YOURS` exactly when `CHIP_PREDICATES.yours(row)` is true, including when `row.isMine` is `undefined`
    - `when` is `relativeChanged(row.updatedAt, now)` and is `null` when `updatedAt` is absent
  </behavior>
  <action>
Create `frontend/src/components/workflows/library/rowIdentity.ts` with exactly the exported contract
in `<interfaces>`. Give it the `libraryFilter.ts:1-27` purity-claim header (imports nothing from the
API client at runtime; type-only where a wire type is needed) so a fence can bind to the claim (S-9).

**⚠ D-34 — a PRECOMPUTED INDEX, never a per-row scan.** RESEARCH measured the sketch's
`identity(row, rows)` as worst-case **O(n³)** — `lineagePhrase` is called per sibling and each call
scans the whole list — at ≈ **230,000 inner-loop iterations per render** on the operator's shape,
twice per render, on every keystroke. Build three linear passes instead:

| Pass | Builds | Cost |
|---|---|---|
| 1 | `slugSet: Set<string>`, `bySlug: Map<string, LibraryRow[]>`, `byName: Map<string, LibraryRow[]>`, `maxLowerVersionBySlug: Map<string, number>` | O(n) |
| 2 | `lineageByRowId: Map<string, Lineage>` — each row resolved ONCE using pass-1 maps | O(n) |
| 3 | `narrowedByName: Map<string, Map<IdentityAxis, Map<string, number>>>` — per-family value histograms computed ONCE PER FAMILY, not once per row | O(n) total |

`resolveIdentity` is then O(1).

**⚠ D-32 — `Map` / `Set` throughout, never object literals.** Two prototype-pollution bugs of exactly
this class are OPEN in this repo (`BUG-260807-01`, `BUG-260808-01`). `libraryFilter.ts:119-126` records
the house argument verbatim: *"The `Map` is what turns an invariant held by a call site into one held
by code."* Include a `constructor`-slug case in the suite, in `phaseVocabulary.test.ts:754-758`'s probe
shape. If any lookup ends up on a plain object anyway, use the ONE shipped spelling —
`workflows/ownProperty.ts`'s `own<T>()` — never a second spelling. The preferred outcome is that you
never need it.

**⚠ D-13 — FOUR lineage states, and the fourth is SILENCE. A `Lineage | null` return type CANNOT
carry this and would silently re-introduce a fabricated claim** (RESEARCH §4c calls this the single
most important type-level decision in the resolver). Measured against the live DB: **57 resolve · 12
go quiet · 0 lie.** The parent-exists guard is load-bearing and the charset cannot be narrowed to
rescue it — `freshHash()` is genuinely `[a-z0-9]{6}`, so `report` is a legal hash and
`pm-weekly-status-report` matches the regex for that reason alone.

Copy `libraryFilter.ts:224-232`'s runtime exhaustiveness guard onto the `Lineage` union's switch: a
`const _never: never = …` compile guard PLUS a runtime default that does something honest rather than
dropping the row silently.

**⚠ Derive the branch, do not read a fixture field.** The sketch selects the version branch via
`row.forkKind === "version"`, which does not exist in production. Derive it: same slug with a lower
version present in the merged list → version fork; else regex `^(.*)-[a-z0-9]{6}$` **and** the base
slug present → copy fork; else `original`; else `unknown`. **Order matters and must match the
sketch's — version first.**

**⚠ D-09 — ownership comes from `CHIP_PREDICATES.yours` (`libraryFilter.ts:159`), imported, ONE call.**
A second copy of the predicate is the drift 192's D-03 forbids by name. Never treat `undefined` as
`false`. **No owner display name anywhere** — `Yours` / `Shared` is the honest ceiling, and it is a
fence (Task 3), not a preference.

**⚠ D-14 — every rendered word is imported from `./libraryVocabulary`.** No string literal that
reaches the DOM may be typed in this module.

**⚠ D-16 is a fence, not advice.** An executor implementing recency on a DRAFT row will notice
`row.source.token` already looks like a timestamp and is right there, needing no backend change. That
shortcut IS the defect: the token is microsecond-precision, a JS `Date` truncates to milliseconds, and
a parsed-and-re-rendered token matches ZERO rows so every later save refuses as stale (probed live
2026-08-01). It would also work only on drafts and silently render nothing on the other two feeds.
**This module reads `row.updatedAt` and nothing else.**

Write `rowIdentity.test.ts` FIRST, RED. Cover every behavior above using small hand-built rows (the
`WorkflowCard.test.tsx:84-95` `rowOf` posture); the fixture-scale properties come in Task 2.
  </action>
  <acceptance_criteria>
    - `rowIdentity.ts` exports `buildIdentityIndex`, `resolveIdentity`, `Lineage`, `IdentityAxis`, `RowIdentity` with exactly the `<interfaces>` signatures
    - `grep -n "= {}\|Object.create(null)\|\[key\] =" frontend/src/components/workflows/library/rowIdentity.ts` shows NO slug- or name-keyed object literal used as a lookup table; `grep -c "new Map(\|new Set(" …/rowIdentity.ts` is ≥ 4
    - `grep -n "token" frontend/src/components/workflows/library/rowIdentity.ts` returns NOTHING
    - `grep -n "forkKind\|parentSlug" frontend/src/components/workflows/library/rowIdentity.ts` returns NOTHING
    - `grep -c "CHIP_PREDICATES.yours" frontend/src/components/workflows/library/rowIdentity.ts` equals **1**
    - The `Lineage` union has FOUR members and `"unknown"` is one of them; no function in the module returns `Lineage | null`
    - `rowIdentity.test.ts` includes the `constructor` / `__proto__` / `toString` / `hasOwnProperty` inherited-key probe
    - The RED was observed as a real `AssertionError` (not a timeout) and both RED and GREEN outputs are quoted in the SUMMARY
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` prints **33**
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/library/rowIdentity.test.ts</automated>
  </verify>
  <done>An O(n) three-pass index exists with a four-state lineage union whose fourth state is silence, built entirely on `Map`/`Set`, reading ownership through the one shipped predicate and recency from `updatedAt` alone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The D-31 discrimination ranker, and the properties it must not break (D-03, D-04, D-05, D-06, D-07, D-31)</name>
  <files>frontend/src/components/workflows/library/rowIdentity.ts, frontend/src/components/workflows/library/rowIdentity.test.ts</files>
  <read_first>
    - `frontend/src/components/workflows/library/__fixtures__/libraryScale.ts` (created in Plan 04 — the shape, the orphans, `seedRecent`)
    - `.planning/sketches/163-the-assembled-card/index.html` lines 276-330 (the sketch's `lineagePhrase` + `identity`, i.e. the `varies()` being replaced)
    - `.planning/sketches/163-the-assembled-card/BUILD-CONTRACT.generated.md` (the five rendered lines and the measured invariants)
    - `frontend/src/components/workflows/library/libraryFilter.test.ts` lines 30-45 (*"a corpus where every row answers every predicate the same way makes each assertion pass for the wrong reason"*)
    - `192.1-CONTEXT.md` D-03, D-04, D-05, D-06, D-07, D-28, D-31 · `192.1-VALIDATION.md` § Per-Task Verification Map (the four D-31 rows)
  </read_first>
  <behavior>
    - For each candidate axis A, `narrowed(A)` = how many siblings share THIS row's own value for A. Any axis with `narrowed(A) === N` is DROPPED (it excludes nobody). The rest are ranked by `narrowed(A)` ASCENDING, tie-broken by the D-04 priority order `lineage → project → state → version`. At most 2 are spent.
    - On the D-30 fixture the ranker produces **> 20 distinct rendered identity lines** (the property from `160/drive.cjs` must SURVIVE the change)
    - No row, ever, on any fixture, carries more than **2** computed segments
    - `1 of N` is present exactly on rows whose name collides, and absent on every unique-name row
    - `N` is counted over the FULL row list handed to `buildIdentityIndex`; the same row resolved against a FILTERED list yields a different `N`, proving the count is a property of the list — which is why Plan 06 must key the memo on `[rows]`
    - A unique-name row that IS a fork carries its lineage segment; a unique-name row that is NOT a fork carries no computed segment at all (D-06 `quiet`)
    - ⚠ THE FLAGSHIP REGRESSION: on a 40+ member family whose lineage is shared by all-but-one and whose state is shared by nearly all, the produced lines are NOT 40+ byte-identical strings — the ranker drops the axis that excludes nobody
    - ⚠ THE RESIDUAL, PROVED NOT PAPERED OVER: on rows identical in every recorded fact, the best pair still cannot separate them, and the row still renders `1 of N` — a true warning, asserted as such
  </behavior>
  <action>
Implement the D-31 ranker inside `resolveIdentity`, replacing the sketch's `varies()`.

**⚠ Why the approved sketch is being changed, in one paragraph the code's docblock must carry.**
Measured over the 44 rows named *Compliance Gap Report* (live local DB, 2026-08-12): lineage is
`varies() === true` because ONE outlier differs, so slot 1 goes to a phrase **43 of 44** rows share;
slot 2 then goes to `Still building`, which **41** share. **The flagship family would render 40+
byte-identical lines.** The generated fixture could not surface it because its rows carry real
definitions, so its projects and tiers genuinely vary — *the same shape of miss the phase was created
to fix, one level deeper.* The fix is faithful to 160-B's own stated principle: *"compute what actually
differs among the rows sharing its name, and print only that."*

**D-04's order is PRESERVED as the tie-break**, so on the realistic fixture — where the axes genuinely
differ — the output is unchanged and the sketch's driven assertions still hold. **Prove that**: the
`> 20 distinct renderings` property must survive, asserted on the D-30 fixture.

**⚠ AND THE RESIDUAL IS REPORTED, NOT PAPERED OVER.** No ranker can separate rows identical in every
fact the product holds; on that family the best available pair narrows 44 → ~40. `1 of 43` then reads
as exactly what it is — a true warning that this row is not distinguishable — and **the product's
answer is to give it a name, which is what 162-B ships. Do NOT invent a technical discriminator (a
slug hash, an id fragment, a created-at) to force the number down: that is the vocabulary drift the
187 node-face ladder forbids.** Assert the residual as a test, and state it in the SUMMARY.

Model the four axes as an `as const satisfies Record<IdentityAxis, …>` table
(`deriveTier.ts:57-79`'s idiom, cited by `libraryFilter.ts:143-166`), so a fifth axis added to the
union without a value extractor is a **typecheck error** rather than an axis that silently never fires
— the only mechanical guard on D-04's priority order being total.

Axis value extractors:
- **lineage** — the rendered lineage phrase, or a distinct sentinel for `unknown` (silence is a value
  a row can share)
- **project** — `row.def?.project_folder_id`'s resolved name, or `NO_PROJECT`. ⚠ **P-7: `LibraryRow.def`
  is `undefined` on most dev rows (SEED-154's double-encoded `definition`), so this axis is simply
  ABSENT for them. Do NOT "fix" that here** — a segment that varies for a reason the resolver cannot
  see is a MISSING segment, not a wrong one, and the max-2 rule falls through to the next axis. An
  executor adding a `def ?? fetchDefinition()` path has left the plan.
- **state** — `STATE_RUNNABLE` / `STATE_DRAFT` / `STATE_STARTER` by `provenance`
- **version** — `"v" + row.version`

**⚠ D-05 — `N` counts the FULL merged library, BEFORE search / chip / project filtering.** That is
both the honest reading (the discriminators answer *what tells this row apart from its namesakes*,
a property of the library) and the reason Plan 06 keys its memo on `[rows]` alone. A count that shrank
as you typed would both flicker and misdescribe the library.

**⚠ D-28 — `1 of N` is NOT suppressed when a feed fails.** The `source-failed` banner is the
disclosure, spent in ONE place rather than 106 times, and drafts are 72 % of the operator's merged
rows so a drafts 403 genuinely collapses `N`. The resolver takes no `failedSources` input. UAT row U7
covers it.

**⚠ D-07 — `oneOfLabel` is imported and rendered verbatim.** Do not rewrite `"1 of N"`.

**⚠ T-7 / P-4 — DO NOT SORT.** `163/index.html:557` sorts by name; the shipped page does not
(`grep -n "sort" libraryFilter.ts` → no hits) and `mergeLibrary` concatenates in feed order. Porting
the sketch's sort violates the scope fence twice over and re-litigates 160-C, which LOST.

Extend `rowIdentity.test.ts` RED-first with every behavior above, driven against
`makeLibraryFixture()`. Pair each absence assertion with a POSITIVE CONTROL (S-6) — *"an assertion
that a selector finds NOTHING passes identically when the selector is broken, when the component never
had the thing, and when the component did not render at all."* Include the **D-34 call-count
property**: spy on the pass-builders (or export a build counter) and assert the index is built ONCE per
`buildIdentityIndex` call and that `resolveIdentity` performs no list scan — e.g. resolving all N rows
costs O(N) extractor calls, not O(N²).
  </action>
  <acceptance_criteria>
    - A test asserts the ranker DROPS an axis whose `narrowed(A) === N`, and a sibling test asserts ascending rank with the D-04 tie-break
    - A test on a purpose-built 40+ family (lineage shared by all-but-one, state shared by nearly all) asserts the rendered lines are NOT all identical, and names it as the flagship regression
    - A test asserts `new Set(allRenderedLines).size > 20` on `makeLibraryFixture()`
    - A test asserts `segs.length <= 2` for EVERY row of the fixture
    - A test asserts `ofN !== null` iff the name collides, over the whole fixture
    - A test resolves one row against the full list and against a filtered list and asserts the two `ofN` values differ — the D-05 scope proof
    - A test asserts the D-31 residual explicitly: on an all-facts-identical family the pair cannot separate them and `ofN` is still rendered as the true warning
    - `grep -n "\.sort(" frontend/src/components/workflows/library/rowIdentity.ts` returns NOTHING that reorders the ROW list (an internal axis ranking sort is fine and must be commented as such)
    - `grep -n "fetchDefinition\|await " frontend/src/components/workflows/library/rowIdentity.ts` returns NOTHING
    - Every RED was observed as a real `AssertionError` and the RED/GREEN outputs are quoted in the SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/library/rowIdentity.test.ts</automated>
  </verify>
  <done>The ranker drops axes that exclude nobody, ranks by narrowing with D-04 as tie-break, spends at most two, preserves the >20-distinct-renderings property on the realistic fixture, and REPORTS its residual rather than inventing a technical discriminator.</done>
</task>

<task type="auto">
  <name>Task 3: The two NEW subtree fences, each driven RED against a real plant (D-09, D-16, D-33, D-35)</name>
  <files>frontend/src/components/workflows/library/librarySubtree.fences.test.ts, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/components/workflows/library/librarySubtree.fences.test.ts` lines 60-120 (corpus + `toHaveLength` + the no-`.test.`-in-sweep assertion), 130-200 (F1's TS-AST `jsxAttributeNamesOf` and the reason a raw grep had to be replaced), 230-300 (F4's three-part shape — POSITIVE CONTROL / NEGATIVE CONTROL / sweep, plus the re-homed-sentinel non-vacuity case), 280-300 (F5's `tok("sem","antic")` assembly trick), 410-450 (the in-test FNV-1a `digest`), 490-510 (the `HighlightTitle` fence written raw first and observed RED against its own comment)
    - `frontend/src/components/workflows/library/libraryRow.ts` lines 15-25 (⚠ the `token` needle already appears in prose HERE)
    - `192.1-CONTEXT.md` D-09, D-16, D-33, D-35 and Addendum-2 finding 3 · `192.1-VALIDATION.md` § Negative fences · `192.1-PATTERNS.md` § 10, § S-6, § S-7
  </read_first>
  <action>
Add `"./rowIdentity.ts"` to `LIBRARY_SUBTREE_PATHS` and update `toHaveLength` to the array's own new
length, **read from the failing assertion's diff** (D-33). ⚠ D-35: the corpus reaches **12** and the
suite **79** only once ALL five source modules have landed — write the number YOUR module list
produces, and re-derive the suite total from the gate's `actual` column, never from arithmetic in a
document.

**Fence A — D-16: no module under `library/` parses `token` as a date.**

⚠⚠ **A raw `token` + `Date` regex REDS ON A CLEAN TREE**, because the needle already appears in
`libraryRow.ts:19` prose and in `WorkflowCard.tsx` prose. The fences file records this exact trap
TWICE (`:186-193`, `:497-506`) — F1 had to become AST-parsed for it, and the `HighlightTitle` fence was
*"written raw first and observed RED against exactly that comment."* **Detect via the TS AST or scope
the regex to code (strip comments first), and drive it RED against a real plant either way.**

Follow the file's three-part shape:
1. **POSITIVE CONTROL** — the detector matches every shape it forbids: `Date.parse(token)`,
   `new Date(row.source.token)`, `new Date(t.token)`.
2. **SCOPING CONTROL** — the detector does NOT match the existing prose occurrences, proved against
   the real `libraryRow.ts` source.
3. **The sweep** over `LIBRARY_SUBTREE_PATHS`.
4. **A NON-VACUITY case** (F4's re-homed-sentinel pattern at `:277`): assert the subtree DOES carry a
   relative-time formatter — e.g. `LIBRARY_MODULES["./relativeChanged.ts"]` contains
   `export function relativeChanged` — so *"no module parses `token` as a date"* is narrower than *"no
   module does anything with time"*.

**Fence B — D-09: no owner display name anywhere in the subtree.** CONTEXT: *"that is a fence, not a
preference."* Assert the only owner words the subtree renders are `OWN_YOURS` / `OWN_SHARED`, i.e. no
module reads a `created_by`, `owner_name`, `user_name`, `display_name`, `email` or `full_name`
identifier. Same three-part shape, with a POSITIVE CONTROL naming each forbidden identifier.

**⚠ S-7 — each new fence MUST be driven RED against a real plant in a REAL PRODUCTION FILE, then the
file restored md5-identical** (the 192-12 practice; a fence never observed RED is a fence that may not
fire). For Fence A, plant `const _x = new Date(row.source.token)` inside `rowIdentity.ts`; for Fence B,
plant a `created_by` read. Record for each: the md5 before, the RED output, the md5 after restore, and
that they match.

⚠ If either forbidden identifier must appear as a literal in the fence file itself, use F5's
`tok("sem","antic")` assembly trick (`:282-294`) so the fence file does not trip its own sweep.

Raise the `librarySubtree.fences.test.ts` and `rowIdentity.test.ts` pins in
`scripts/vitest-count-gate.cjs` in the SAME commit, from the gate's own `actual` column across two
agreeing runs. ⚠ `3164` is the PIN and the measured total is higher (C-5) — `BASELINE_TOTAL` is derived
at `:1248` and is not hand-edited. ⚠ Do NOT "fix" `ExternalActionSection.test.tsx` (+9) or
`PhaseTimeline.test.tsx` (+4) — pre-existing slack, not this phase's (T-9).
  </action>
  <acceptance_criteria>
    - Both new fences exist with a POSITIVE CONTROL, a SCOPING CONTROL and an `it.each(LIBRARY_SUBTREE_PATHS)` sweep
    - Fence A carries a non-vacuity case proving the subtree DOES contain a relative-time formatter
    - The SUMMARY records, for EACH fence: the md5 of the plantee file before, the RED output naming the planted line, the md5 after restore, and that the two md5s match
    - `LIBRARY_SUBTREE_PATHS` contains `"./rowIdentity.ts"` and `toHaveLength` equals the array's length
    - `cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/library/librarySubtree.fences.test.ts` passes on the clean tree with zero pre-existing-prose false positives
    - `GSD_VITEST_MAX_WORKERS=4 node scripts/vitest-count-gate.cjs` exits 0
    - `git status --porcelain` shows no leftover planted file
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/library/ && GSD_VITEST_MAX_WORKERS=4 node ../scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>Two new subtree fences exist, each observed RED against a real plant in a real production file and each file restored md5-identical; the corpus and the pins are raised in the same commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wire row → derived claim | The resolver turns slugs and versions into a sentence a person will believe. A wrong claim here is indistinguishable from a right one at a glance. |
| slug string → lookup key | User-influenced strings index into the identity maps. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192.1-10 | Spoofing (of provenance) | `lineageOf`'s third state | mitigate | A four-member discriminated union with an explicit `"unknown"`; a `Lineage \| null` return is forbidden and grep-asserted. Measured trade: **57 resolve · 12 go quiet · 0 lie.** |
| T-192.1-11 | Tampering | Prototype pollution via a slug named `constructor` / `__proto__` | mitigate | `Map` / `Set` throughout (grep-asserted; ≥ 4 constructions, no slug-keyed object literal) plus the `phaseVocabulary.test.ts:754-758` inherited-key probe. Two bugs of this exact class are OPEN in this repo. |
| T-192.1-12 | Information Disclosure | An owner's display name reaching the identity line | mitigate | **Fence B**, driven RED against a planted `created_by` read. `Yours` / `Shared` is the honest ceiling (D-09) and a users join is out of scope. |
| T-192.1-13 | Tampering | `token` re-purposed as the recency source | mitigate | **Fence A**, AST- or comment-stripped detection with a scoping control (a raw regex reds on the clean tree), driven RED against a planted `new Date(row.source.token)`. A parsed-and-re-rendered token matches ZERO rows and makes every later save refuse as stale. |
| T-192.1-14 | Denial of Service | O(n³) resolution on every keystroke | mitigate | Three linear passes; a call-count property test; and Plan 06's `[rows]`-only memo key. Measured worst case on the operator's shape: ≈ 230 k inner-loop iterations per render, twice per render. |
| T-192.1-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero packages installed.** No property-testing library for the O(n) property — a call counter suffices. |
</threat_model>

<verification>
- `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` → **33**
- `cd frontend && npx eslint src/components/workflows/library/ src/pages/WorkflowsPage.tsx` → exit 0
- The eight-suite scoped run → 0 failed
- `GSD_VITEST_MAX_WORKERS=4 node scripts/vitest-count-gate.cjs` → exit 0
- Both new fences observed RED against a real plant, with md5-before == md5-after recorded
</verification>

<success_criteria>
- The resolver is O(n), Map/Set-only, and reads ownership through the one shipped predicate
- Lineage has four states and the fourth is silence — `compliance-gap-report` proves it on the fixture
- The D-31 ranker drops axes that exclude nobody, preserves >20 distinct renderings, never spends more than 2 segments, and its residual is asserted rather than hidden
- `N` is a property of the full library, proved by resolving against a filtered list
- Two new fences exist and were each observed RED
</success_criteria>

<output>
Create `.planning/phases/192.1-workflow-identity/192.1-05-SUMMARY.md` when done.
Record: the base SHA; every RED/GREEN pair; the MEASURED distinct-renderings count on the fixture (not
the sketch's 94, which was measured at 108 cards); the D-31 residual as measured on the all-facts-
identical family; and for each fence the md5-before / RED / md5-after triple.
</output>
