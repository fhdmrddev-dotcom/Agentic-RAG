---
phase: 192.1-workflow-identity
plan: 03
type: execute
wave: 2
depends_on: ["192.1-02"]
files_modified:
  - frontend/src/components/workflows/library/libraryFork.ts
  - frontend/src/components/workflows/library/libraryFork.test.ts
  - frontend/src/components/workflows/library/useWorkflowFork.ts
  - frontend/src/components/workflows/library/librarySubtree.fences.test.ts
  - frontend/src/pages/WorkflowsPage.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [LIB-05]
gap_closure: false

must_haves:
  truths:
    - "Forking a published row still creates a v(N+1) draft and opens it; forking a row you already have a draft of still opens that draft and creates nothing"
    - "A starter fork that 409s is still retried exactly once, and a terminal failure still says so out loud"
    - "The fork concern no longer lives on WorkflowsPage.tsx, and nothing mechanically permits a second copy of it there"
  artifacts:
    - path: "frontend/src/components/workflows/library/libraryFork.ts"
      provides: "freshHash + isForkConflict, moved verbatim with their docblocks"
      contains: "export function freshHash"
    - path: "frontend/src/components/workflows/library/useWorkflowFork.ts"
      provides: "the fork concern — forkFailed state, draftBySlug, the two handlers, the two LibraryRow adapters, the failure notice"
      contains: "export function useWorkflowFork"
    - path: "frontend/src/components/workflows/library/librarySubtree.fences.test.ts"
      provides: "the corpus widened to include both new modules, and PAGE_MUST_NOT_DECLARE extended so the page cannot keep a second copy"
      contains: "function freshHash("
  key_links:
    - from: "frontend/src/pages/WorkflowsPage.tsx"
      to: "frontend/src/components/workflows/library/useWorkflowFork.ts"
      via: "one useWorkflowFork({ drafts, refetchDrafts, onOpenDraft, onForked }) call declared BELOW onOpenDraft"
      pattern: "useWorkflowFork\\("
    - from: "frontend/src/components/workflows/library/useWorkflowFork.ts"
      to: "frontend/src/pages/WorkflowsPage.tsx"
      via: "the onForked callback — the page's ONLY setBuilderInitial + setPageView call site for a fork"
      pattern: "onForked"
---

<objective>
Honor **G-5 on `frontend/src/pages/WorkflowsPage.tsx`** (30 commits / 11 phases / 1180 L) by
extracting the fork/create concern into `library/` — **before** 162-B's dialog is built on top of it
(**D-01**).

Purpose: the ledger row for this file named this exact seam and named WR-08 as the trigger that would
force someone into it. 162-B puts a modal, a name field, a pre-flight check and new state between the
click and the POST — a second concern landing on the named seam. The recommendation is produced as
the first option and taken.
Output: `library/libraryFork.ts` (pure) + `library/useWorkflowFork.ts` (the hook), the page reduced to
one hook call and one `onForked`, the fences widened so the page cannot keep a second copy, and the
measured growth published rather than predicted.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⚠ **WORKTREE BOOTSTRAP IS YOUR FIRST ACTION:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`.
**NEVER `rm -rf` a worktree** — `bash scripts/teardown-worktree.sh <path>`.
⚠ Assert the dispatched base SHA with `git rev-parse HEAD` and record it.
⚠ `GSD_VITEST_MAX_WORKERS=4` on every vitest invocation.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/192.1-workflow-identity/192.1-CONTEXT.md
@.planning/phases/192.1-workflow-identity/192.1-RESEARCH.md
@.planning/phases/192.1-workflow-identity/192.1-PATTERNS.md
@.planning/phases/192.1-workflow-identity/192.1-VALIDATION.md
@.planning/phases/192.1-workflow-identity/192.1-BASELINE.md

<interfaces>
<!-- The exact spans that move, re-verified at HEAD. "Span" INCLUDES each symbol's docblock,
     because those carry the recorded reasoning D-01 requires be moved with the code. -->

WorkflowsPage.tsx — MOVES:
  freshHash              docblock 137-140  decl 141-145   → libraryFork.ts   (closes over NOTHING)
  isForkConflict         docblock 147-164  decl 165-167   → libraryFork.ts   (closes over NOTHING)
  forkFailed state       docblock 280-288  decl 289       → useWorkflowFork.ts
  draftBySlug            docblock 518-530  decl 531-538   → useWorkflowFork.ts
  onTweak                docblock 562-571  decl 572-636   → useWorkflowFork.ts
  onUseStarter           docblock 638-645  decl 646-694   → useWorkflowFork.ts
  handleForkNewVersion   (shared 704-718)  decl 729-732   → useWorkflowFork.ts   (D-29)
  handleForkStarter      (shared 704-718)  decl 734-737   → useWorkflowFork.ts   (D-29)
  library-fork-failed notice  998-1012     decl 1013-1021 → rendered from the hook's state

WorkflowsPage.tsx — STAYS (D-01, correction C-1):
  onOpenDraft            docblock 540-547  decl 548-560   ← the drafts-navigation concern

Closures the moved code reads from component scope (READ OUT OF THE BODIES, not the dep arrays —
React setters are stable and legitimately omitted from deps, so they are invisible to a deps-only
reading):
  onTweak       deps: refetchDrafts, draftBySlug, onOpenDraft
                NOT in deps: setForkFailed (:611), setBuilderInitial (:616), setPageView (:623)
  onUseStarter  deps: refetchDrafts
                NOT in deps: setForkFailed (:652), setBuilderInitial (:665), setPageView (:672)

The seed `setBuilderInitial` receives today (both handlers), which `onForked` must carry:
  { definition: WorkflowDefinitionJSON, draftId: string, label: string, token: string }

The shipped analog for the hook's posture — frontend/src/hooks/useDraftPersistence.ts:472 :
  export function useDraftPersistence(args: DraftPersistenceArgs): DraftPersistence {
    const { definition, enabled, initialDraftId, initialToken, store,
            publishInFlight, validationCause, onDraftCreated } = args
  — one args object IN, a CALLBACK out, so the caller keeps ownership of what happens next.

The fences corpus that must grow — librarySubtree.fences.test.ts:66-74, :93 :
  const LIBRARY_SUBTREE_PATHS = [ "./libraryRow.ts", "./libraryVocabulary.ts", "./libraryFilter.ts",
    "./RunModal.tsx", "./WorkflowDeleteSheet.tsx", "./WorkflowCard.tsx", "./LibraryToolbar.tsx" ] as const
  expect(LIBRARY_SUBTREE_PATHS).toHaveLength(7)

The OD fence — librarySubtree.fences.test.ts:535-552 :
  const PAGE_IMPORTED_MODULES = ["RunModal","libraryFilter","libraryRow","LibraryToolbar","WorkflowCard","libraryVocabulary"]
  const PAGE_MUST_NOT_DECLARE = ["function RunModal(","type DeletePhase","function FilterItem(",
    "function PublishedCard(","function DraftCard(","function StarterCard("]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move the two pure leaves into library/libraryFork.ts and prove them as arithmetic (D-01, D-22, D-24)</name>
  <files>frontend/src/components/workflows/library/libraryFork.ts, frontend/src/components/workflows/library/libraryFork.test.ts, frontend/src/components/workflows/library/librarySubtree.fences.test.ts, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/pages/WorkflowsPage.tsx` lines 130-175 (both symbols WITH their docblocks — `freshHash` 137-145, `isForkConflict` 147-167)
    - `frontend/src/components/workflows/library/libraryFilter.ts` lines 1-30 (the purity-claim header contract to copy) and 143-166 (the table idiom)
    - `frontend/src/components/workflows/library/libraryFilter.test.ts` lines 1-40 (the `?raw` self-import at :19 and the corpus-shape warning at :34-38)
    - `frontend/src/components/workflows/library/librarySubtree.fences.test.ts` lines 60-110 (corpus + `toHaveLength`) and 230-300 (F4's three-part shape: POSITIVE CONTROL, NEGATIVE CONTROL, sweep)
    - `scripts/vitest-count-gate.cjs` lines 900-975 (the pin block and the "read from this script's own `actual` column" rule)
    - `192.1-CONTEXT.md` D-01, D-22, D-24, D-33, D-35 · `192.1-PATTERNS.md` § 3 and § 10
  </read_first>
  <behavior>
    - `freshHash()` returns a 6-character string matching `/^[a-z0-9]{6}$/` on every call, and two calls differ
    - `isForkConflict(new Error("... status 409"))` is `true`
    - `isForkConflict(new Error("... status 500"))` is `false`
    - `isForkConflict(null)` and `isForkConflict(undefined)` are `false` and do not throw
    - the fences suite's `LIBRARY_SUBTREE_PATHS` includes `./libraryFork.ts` and its `toHaveLength` assertion equals the corpus's own new length
  </behavior>
  <action>
Create `frontend/src/components/workflows/library/libraryFork.ts` and move `freshHash`
(`WorkflowsPage.tsx:137-145`) and `isForkConflict` (`:147-167`) into it **verbatim, docblocks
included**. Both are module-scope and close over NOTHING, so the move is genuinely byte-preserving —
say so in the new module's header and state that it was verified rather than assumed.

⚠ **D-22: `isForkConflict` is NOT upgraded here.** It classifies by `String(e).includes("409")`, and
its own docblock records WR-08's re-open trigger — *the next phase that touches `createWorkflowDraft`'s
throw site replaces this substring test with the typed error's own discriminator, in the same commit.*
This phase does not touch that throw site (it is an `api.ts` change with callers outside this page,
notably `useDraftPersistence`). **Move the predicate and its docblock intact; restate WR-08's trigger
verbatim in the new module so it survives the move.**

⚠ **T-8: `freshHash` must NOT be "upgraded" to `crypto.randomUUID()`.** `WorkflowsPage.tsx:137-140`
records that its 6-char `[a-z0-9]` shape is what the fork/collision contract expects — and D-13's
copy-fork regex `^(.*)-[a-z0-9]{6}$` depends on it. It is a uniqueness suffix behind a
`UNIQUE(slug, version)` constraint with a 409 retry, not a security token.

Give the module the `libraryFilter.ts:1-27` header contract: state the PURE + CLIENT-SIDE claim
(imports nothing from the API client at runtime; `import type` only if a wire type is needed) so the
claim is something a fence can bind to (S-9).

Write `libraryFork.test.ts` FIRST, RED, then move: the five behaviors above, in the
`libraryFilter.test.ts` posture (a pure `describe` per rule, no `render`, no DOM). Include a
`?raw` self-import asserting the module names no `@/lib/api` runtime import — the shipped purity
idiom at `libraryFilter.test.ts:19`.

**⚠ At this point the page STILL declares both functions — do NOT delete them yet.** Keep the page
importing from `./…/libraryFork` OR leave the page untouched; either way the CUT is Task 3's job and
the OD fence entries land there with it. Adding `./libraryFork.ts` to `LIBRARY_SUBTREE_PATHS` is
required now (D-33: the fences suite is widened in the SAME commit that adds a module — a module not
on the list is swept by NOTHING, which is the *"a guardrail cannot see what is absent from its list"*
lesson one layer below G-5).

Update `librarySubtree.fences.test.ts:93`'s `toHaveLength(7)` to the corpus's own new length **read
from the failing assertion's diff**, not predicted. ⚠ D-35 corrects the arithmetic circulating in
RESEARCH and VALIDATION: FIVE source modules land under `library/` across this phase, so the final
corpus is 12 and the suite ends at 79 — but **this plan adds only its own**, and the number it writes
is the number ITS module list produces. Raise the `librarySubtree.fences.test.ts` and
`libraryFork.test.ts` pins in `scripts/vitest-count-gate.cjs` in the same commit, **read from the
gate's own `actual` column across two agreeing runs**. `BASELINE_TOTAL` is derived (`:1248`) — do not
hand-edit it. ⚠ No `TARGETS` edit is needed: `"src/components/workflows"` (`:1252`) is a DIRECTORY
entry, and a `TARGETS` path that does not resolve makes the gate exit 2 rather than fail.
  </action>
  <acceptance_criteria>
    - `frontend/src/components/workflows/library/libraryFork.ts` exports `freshHash` and `isForkConflict`, each with its original docblock text present
    - `grep -c "WR-08" frontend/src/components/workflows/library/libraryFork.ts` is ≥ 1 and the re-open trigger sentence is present verbatim
    - `grep -n "randomUUID\|crypto\." frontend/src/components/workflows/library/libraryFork.ts` returns NOTHING
    - `libraryFork.test.ts` covers all five behaviors and includes a `?raw` purity assertion
    - The RED was observed as a real `AssertionError` (not a timeout) and both the RED and GREEN outputs are quoted in the SUMMARY
    - `LIBRARY_SUBTREE_PATHS` contains `"./libraryFork.ts"` and the `toHaveLength` literal equals the array's length
    - The two raised pins equal the gate's own `actual` column across two agreeing runs
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` prints the BASELINE value (33)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/library/libraryFork.test.ts src/components/workflows/library/librarySubtree.fences.test.ts</automated>
  </verify>
  <done>Both pure leaves live in `libraryFork.ts` with their docblocks and WR-08's trigger intact, are proved as arithmetic RED-first, are inside the fences corpus, and the pins are raised from the gate's own output.</done>
</task>

<task type="auto">
  <name>Task 2: Create useWorkflowFork.ts ADDITIVELY — build it before cutting anything (D-01, D-29)</name>
  <files>frontend/src/components/workflows/library/useWorkflowFork.ts, frontend/src/components/workflows/library/librarySubtree.fences.test.ts, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/pages/WorkflowsPage.tsx` lines 275-300 (`forkFailed` state + docblock), 375-400 (`refetchDrafts` — NOT moved), 515-560 (`draftBySlug` + `onOpenDraft` and its TDZ warning at :543-547), 560-700 (both handlers in full), 700-760 (the five-adapter block), 995-1025 (the notice)
    - `frontend/src/hooks/useDraftPersistence.ts` lines 420-560 (the args-object-in / callback-out posture at :472, the callback-mirroring ref at :552-556, the "one home per concern" paragraph at :495-521)
    - `frontend/src/components/workflows/library/librarySubtree.fences.test.ts` lines 230-280 (F4's regexes at :235-236)
    - `192.1-CONTEXT.md` D-01, D-22, D-23, D-24, D-29 · `192.1-RESEARCH.md` § 1b, § 1c, § 1d, § 1e · `192.1-PATTERNS.md` § 3, § S-4
  </read_first>
  <action>
**Split the extraction into *create additively* then *cut*** — the 188.1 lesson, recorded verbatim in
this project's memory. This task creates the module; the page is NOT touched.

Create `frontend/src/components/workflows/library/useWorkflowFork.ts` exporting:

```
export interface ForkSeed { definition: WorkflowDefinitionJSON; draftId: string; label: string; token: string }
export interface WorkflowForkArgs {
  drafts: WorkflowDraftRow[]
  refetchDrafts: () => Promise<unknown> | void
  onOpenDraft: (draft: WorkflowDraftRow) => void
  onForked: (seed: ForkSeed) => void
}
export interface WorkflowFork {
  forkFailed: { name: string; conflict: boolean } | null
  draftBySlug: Map<string, WorkflowDraftRow>
  onForkNewVersion: (row: LibraryRow) => void
  onForkStarter: (row: LibraryRow) => void
}
export function useWorkflowFork(args: WorkflowForkArgs): WorkflowFork
```

Move into it, verbatim where the code survives, WITH their docblocks: the `forkFailed` state
(`:280-289`), `draftBySlug` (`:518-538`, now built from the `drafts` argument), `onTweak`
(`:562-636`), `onUseStarter` (`:638-694`), and — per **D-29** — the two `LibraryRow → wire` adapters
`handleForkNewVersion` (`:729-732`) and `handleForkStarter` (`:734-737`), which exist solely to feed
the two fork handlers. Expose the adapters as `onForkNewVersion` / `onForkStarter`; the raw
`onTweak` / `onUseStarter` stay module-internal.

**⚠ The three Builder-handoff setters must NOT move.** `setForkFailed` is the hook's own; but
`setBuilderInitial` and `setPageView` are the page's Builder host. Replace both call sites in each
handler with ONE `onForked(seed)` call carrying `{ definition, draftId, label, token }`. The reason is
recorded in shipped source (`WorkflowsPage.tsx:574-578`): 192-14 reused `onOpenDraft` inside `onTweak`
*"rather than a third `setBuilderInitial` call site"*, because that **threads 186-07's opaque
concurrency token by construction instead of by remembering to — a dropped token is a silent-clobber
bug that typechecks.** One callback preserves that; taking `setBuilderInitial` directly would re-open
two call sites. Carry that paragraph into the hook.

**⚠ `refetchDrafts` is passed IN, never moved.** It is a `useCallback` at `WorkflowsPage.tsx:379-395`
owning `draftsSeqRef` (a latest-wins ticket), `setDrafts` and `markSource("draft", …)` — the
fetch-orchestration concern, which the CLAUDE.md ledger row lists as still living on the page.

**⚠ D-23 must survive by construction.** `onTweak`'s FIRST branch — `draftBySlug.get(wf.slug)` →
`onOpenDraft(existing)` → `return`, creating nothing — is 192-14's U5-blocker fix. Move it with its
docblock unchanged. Regressing it re-opens a shipped blocker.

**⚠ D-24: `onUseStarter`'s single 409 retry is preserved**, pinned from the inside at exactly two
`createWorkflowDraft` calls (T-192-42). Do not restructure the `for (let attempt = 0; attempt < 2; …)`
loop, and do not "simplify" the `if (attempt === 0 && isForkConflict(e)) continue` line.

**⚠ Preserve the failure shape (T-192-40):** `forkFailed` holds `{ name, conflict }` — *"a shape that
cannot hold server prose, a status code or an id"* (`WorkflowsPage.tsx:283-285`). Do not widen it.

**⚠ S-4 / F4: the hook must not import the page, and must not import `BuilderInitial`.** F4's regex
does NOT match `WorkflowBuilderPage` ("WorkflowsPage" is not a substring of it) — so this is a
judgement constraint, not a fenced one. `ForkSeed` is declared LOCALLY and is structurally identical
to what `setBuilderInitial` accepts; the page adapts it in `onForked`. Imports are `@/lib/api`
(`createWorkflowDraft` runtime; `PublishedWorkflow` / `WorkflowDraftRow` / `WorkflowDefinitionJSON`
as types), `./libraryFork`, `./libraryRow`, and React.

Add `"./useWorkflowFork.ts"` to `LIBRARY_SUBTREE_PATHS` and raise `toHaveLength` to the array's own
new length (D-33/D-35), plus the fences pin in the same commit from the gate's `actual` column.

**Do NOT touch `frontend/src/pages/WorkflowsPage.tsx` in this task.** The module exists and
typechecks; nothing consumes it yet. `noUnusedLocals` is ON app-wide but applies to locals, not to
exported module members — if `tsc` disagrees, read the error rather than predicting it (T-3).
  </action>
  <acceptance_criteria>
    - `frontend/src/components/workflows/library/useWorkflowFork.ts` exists and exports `useWorkflowFork`, `WorkflowForkArgs`, `WorkflowFork`, `ForkSeed`
    - `grep -n "setBuilderInitial\|setPageView" frontend/src/components/workflows/library/useWorkflowFork.ts` returns NOTHING
    - `grep -n "WorkflowsPage\|WorkflowBuilderPage\|BuilderInitial" frontend/src/components/workflows/library/useWorkflowFork.ts` returns NOTHING
    - `grep -c "createWorkflowDraft(" frontend/src/components/workflows/library/useWorkflowFork.ts` equals **2** (the Tweak path and the starter loop's single site) — the T-192-42 shape preserved
    - The `draftBySlug.get(wf.slug)` early-return branch is present with its 192-14 docblock
    - `forkFailed`'s type is exactly `{ name: string; conflict: boolean } | null`
    - `git diff --stat frontend/src/pages/WorkflowsPage.tsx` shows NO change in this task's commit
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` prints the BASELINE value (33)
    - `LIBRARY_SUBTREE_PATHS` now contains both `"./libraryFork.ts"` and `"./useWorkflowFork.ts"`, and `toHaveLength` matches
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS" && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/library/librarySubtree.fences.test.ts</automated>
  </verify>
  <done>The hook exists, typechecks, is inside the fences corpus, contains neither Builder setter nor any page import, preserves the D-23 branch and the D-24 two-call retry shape — and the page is still byte-identical.</done>
</task>

<task type="auto">
  <name>Task 3: CUT the page over, extend the OD fence, and publish the measured growth (D-01, D-29, D-33, D-37)</name>
  <files>frontend/src/pages/WorkflowsPage.tsx, frontend/src/components/workflows/library/librarySubtree.fences.test.ts, frontend/src/components/workflows/library/useWorkflowFork.ts, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/pages/WorkflowsPage.tsx` lines 240-300, 370-400, 440-480, 515-560, 560-760, 990-1030, 1060-1100 (everything that moves, everything that stays, and the TDZ warning at :543-547)
    - `frontend/src/components/workflows/library/librarySubtree.fences.test.ts` lines 530-602 (the whole OD block)
    - `frontend/src/components/workflows/library/WorkflowCard.tsx` lines 105-145 (the docblock template for publishing measured growth)
    - `.planning/phases/192.1-workflow-identity/192.1-BASELINE.md` (the pre-move classification, the page digest, the twelve fork-arc case titles)
    - `192.1-CONTEXT.md` D-01, D-29, D-33, D-37 · `192.1-RESEARCH.md` § 1c (the TDZ trap), § 7c (the OD extension) · `192.1-PATTERNS.md` § P-3, § S-10
  </read_first>
  <action>
**(a) Extend the OD fence FIRST, and take the RED for free.** Add to `PAGE_MUST_NOT_DECLARE`
(`librarySubtree.fences.test.ts:545-552`): `"function freshHash("`, `"function isForkConflict("`,
`"const onTweak = useCallback"`, `"const onUseStarter = useCallback"`,
`"const [forkFailed, setForkFailed]"`, `"const draftBySlug = useMemo"`. Add
`"useWorkflowFork"` and `"libraryFork"` to `PAGE_IMPORTED_MODULES` (`:535-542`).

⚠ The existing `"function …("` pattern matches the first two exactly (both are module-scope `function`
declarations); it will NOT match the four component-scope `const`s — hence their declaration
spellings. **Run the fences suite BEFORE deleting anything from the page and record the RED naming
all six entries.** This is a stronger drive than 192-12's plant-and-restore: the "plant" is the
actual shipped declaration, so no file is mutated and none needs restoring. Quote the RED output in
the SUMMARY, then cut, then quote the GREEN.

**(b) Cut the page.** Delete `freshHash`, `isForkConflict`, the `forkFailed` state, `draftBySlug`,
`onTweak`, `onUseStarter`, `handleForkNewVersion` and `handleForkStarter`, and replace them with ONE
`useWorkflowFork({ drafts, refetchDrafts, onOpenDraft, onForked })` call. `onForked` is the page's
only fork-time `setBuilderInitial` + `setPageView` site:
`(seed) => { setBuilderInitial(seed); setPageView("builder") }`.

**⚠⚠ THE HIGHEST-RISK MECHANICAL DEFECT IN THIS PHASE, AND IT TYPECHECKS CLEAN.** A dep array is
evaluated at RENDER time, so a `const` declared further down the component body is a temporal-dead-zone
`ReferenceError` on every render — *a crash, not a lint warning* (the shipped warning at
`WorkflowsPage.tsx:543-547`, recorded by 192-14). The hook call reads `onOpenDraft` (`:548`),
`refetchDrafts` (`:379`) and `drafts` (`:254`), so **it MUST be placed BELOW `onOpenDraft`'s
declaration.** Placing it where `draftBySlug` sits today (`:531`) crashes on first render. Verify by
running the page suites, not by reading.

Render the `library-fork-failed` notice (`:998-1021`) from the hook's returned `forkFailed`, keeping
its `role="status"` shape and its `forkFailedMessage(name, conflict)` call. ⚠ There is **NO toast
library in this repo** and one may not be added (S-5, verified: zero `sonner` imports, no `ui/toast`).

Pass `onForkNewVersion` / `onForkStarter` to `WorkflowCard` at `:1082`'s call site, and keep
`hasExistingFork={row.provenance === "published" && draftBySlug.has(row.slug)}` working off the hook's
returned `draftBySlug`. **The two fork testids stay distinct** — D-12's only mechanical evidence.

⚠ `noUnusedLocals` / `noUnusedParameters` are ON (`WorkflowsPage.tsx:1172-1176`), so an import
orphaned by the cut is a **TYPE ERROR, not a lint warning**. **Read the orphan list out of `tsc`; do
not predict it** (T-3 — 192-06/08/10 each did exactly this).

**(c) D-37 — rewrite the five-adapter docblock.** `WorkflowsPage.tsx:704-718` is one block headed
*"THE CARD'S FIVE CALLBACKS"* which also carries the D-12 never-merged paragraph. Only TWO of the five
move (the other three feed `onRun` / `onOpen` / `onDeleted`). Rewrite the heading and body to say what
is true afterwards, and **move the D-12 never-merged paragraph into `useWorkflowFork.ts` beside the
two adapters it now governs**, leaving a one-line pointer on the page. A docblock asserting five where
three remain is the wrong-pointer-in-prose failure Phase 189 banked — nothing typechecks prose.

**(d) Prove zero re-capture.** Run the eight-suite scoped command. **The twelve fork-arc cases in
`WorkflowsPage.test.tsx` must be GREEN with the test file BYTE-IDENTICAL to its pre-move state** —
`git diff --stat frontend/src/pages/WorkflowsPage.test.tsx` must be empty, and the suite must still
report its baseline count. That is what "a baseline that predates the change" buys, and it is the
whole proof of the move.

**(e) Publish the MEASURED growth, never a prediction.** Re-run Plan 02's validated line classifier
over the post-cut page and over the (now nine) `library/` source modules. Publish the numbers in
`useWorkflowFork.ts`'s own docblock in the `WorkflowCard.tsx:111-140` shape: PAGE before → after with
the percentage, SUBTREE before → after with the percentage, and CODE for each. ⚠ **State the growth
rather than smoothing it** — 188.2 measured +67.1 % and Phase 192 measured **+126.2 %** against its
own +5…+16 % estimate, an ~8× miss. Cite the classifier's known-good validation
(`95a4c915:PhaseNodeCard.tsx = 797/518/249/30`) and Plan 02's pre-move reading as the "before" column.
Also record the page's `wc -l` delta against `WORKFLOWS_PAGE_DIGEST`'s commit.

Raise every grown pin in `scripts/vitest-count-gate.cjs` from the gate's own `actual` column across
two agreeing runs, in the same commit.
  </action>
  <acceptance_criteria>
    - The fences RED was observed BEFORE the cut and its output — naming all six new `PAGE_MUST_NOT_DECLARE` entries — is quoted in the SUMMARY, followed by the post-cut GREEN
    - `grep -n "function freshHash(\|function isForkConflict(\|const onTweak = useCallback\|const onUseStarter = useCallback\|const \[forkFailed, setForkFailed\]\|const draftBySlug = useMemo" frontend/src/pages/WorkflowsPage.tsx` returns NOTHING
    - `grep -c "useWorkflowFork(" frontend/src/pages/WorkflowsPage.tsx` equals **1**
    - `grep -n "setBuilderInitial" frontend/src/pages/WorkflowsPage.tsx` shows the fork path reaching it ONLY through `onForked`
    - The `useWorkflowFork(` call site's line number is GREATER than `onOpenDraft`'s declaration line (assert by `grep -n` on both and comparing)
    - `git diff --stat frontend/src/pages/WorkflowsPage.test.tsx` is EMPTY, and `WorkflowsPage.test.tsx` reports its baseline count with 0 failed
    - `WorkflowsPage.tsx:704-718`'s docblock no longer says "FIVE CALLBACKS", and the D-12 never-merged paragraph is present in `useWorkflowFork.ts`
    - `useWorkflowFork.ts`'s docblock carries PAGE and SUBTREE before → after figures with percentages and cites `95a4c915` as the classifier's known-good
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` prints **33**
    - `cd frontend && npx eslint src/components/workflows/library/ src/pages/WorkflowsPage.tsx` exits 0
    - `GSD_VITEST_MAX_WORKERS=4 node scripts/vitest-count-gate.cjs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/pages/WorkflowsPage.test.tsx src/components/workflows/library/ src/pages/__tests__/PublishedCardDelete.test.tsx src/pages/__tests__/RunModal.test.tsx src/pages/__tests__/RunModal.a11y.test.tsx</automated>
  </verify>
  <done>The page holds one hook call and one `onForked`; six OD entries were driven RED pre-cut and are GREEN post-cut; the twelve fork-arc cases pass with the test file byte-identical; the D-37 docblock tells the truth; the growth is measured and published; tsc 33, eslint 0, gate exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API (`POST /workflows`) | The fork write. Unchanged by this plan — the same `createWorkflowDraft` call, the same body. |
| server error → user-visible notice | `forkFailed` is the only path from a thrown error to the screen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192.1-03 | Information Disclosure | The fork failure notice | mitigate | `forkFailed` keeps the shipped `{ name: string; conflict: boolean }` shape — *"a shape that cannot hold server prose, a status code or an id"* (T-192-40, `WorkflowsPage.tsx:283-285`). Grep-asserted; the widening is what would leak. |
| T-192.1-04 | Denial of Service | The `onUseStarter` 409 retry loop | mitigate | Exactly two `createWorkflowDraft` calls, pinned from the inside by T-192-42 and grep-asserted at 2 occurrences in the new module (D-24). A restructured loop is how an unbounded retry gets introduced. |
| T-192.1-05 | Tampering | `freshHash` mistaken for a security token | accept | It is a uniqueness suffix behind a `UNIQUE(slug, version)` constraint with a 409 retry, not a secret. "Upgrading" it to `crypto.randomUUID()` BREAKS D-13's copy-fork regex and the collision contract (T-8) — the fence is the grep for `randomUUID`. |
| T-192.1-06 | Tampering | ESM cycle / page↔library coupling | mitigate | F4 sweeps every corpus module for a `WorkflowsPage` specifier. ⚠ It does NOT match `WorkflowBuilderPage`, so `BuilderInitial` is avoided on judgement and grep-asserted absent. |
| T-192.1-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero packages installed.** No toast library, no date library, no test-harness library. The scope fence forbids a new frontend dependency outright; if one appears necessary, STOP and report. |
</threat_model>

<verification>
- `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` → **33**
- `cd frontend && npx eslint src/components/workflows/library/ src/pages/WorkflowsPage.tsx` → exit 0
- `cd frontend && npm run lint:a11y` → exit 0
- The eight-suite scoped run → 0 failed; `WorkflowsPage.test.tsx` at its baseline count with the file byte-identical
- `GSD_VITEST_MAX_WORKERS=4 node scripts/vitest-count-gate.cjs` → exit 0
- The fences RED (pre-cut, six entries) and GREEN (post-cut) are both quoted in the SUMMARY
</verification>

<success_criteria>
- G-5 is honored on `WorkflowsPage.tsx`: the fork concern lives in `library/`, and the OD fence mechanically prevents a second copy on the page
- The twelve fork-arc characterization cases are green with ZERO re-capture
- D-23's open-the-existing-draft branch and D-24's two-call retry survive, grep-asserted
- The extraction's growth is MEASURED and published in the destination docblock, not predicted
- The D-37 docblock says what is true afterwards
</success_criteria>

<output>
Create `.planning/phases/192.1-workflow-identity/192.1-03-SUMMARY.md` when done.
Record: the base SHA; the fences RED output (pre-cut) and GREEN output (post-cut) verbatim; the
`tsc` orphan-import list as `tsc` printed it (not as predicted); the measured PAGE and SUBTREE
before → after figures with percentages; and the confirmation that `WorkflowsPage.test.tsx` is
byte-identical.
</output>
