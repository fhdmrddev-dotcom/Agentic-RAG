---
phase: 192.1-workflow-identity
plan: 06
type: execute
wave: 5
depends_on: ["192.1-05"]
files_modified:
  - frontend/src/components/workflows/library/WorkflowCard.tsx
  - frontend/src/components/workflows/library/WorkflowCard.test.tsx
  - frontend/src/pages/WorkflowsPage.tsx
  - scripts/vitest-count-gate.cjs
autonomous: true
requirements: [LIB-05]
gap_closure: false

must_haves:
  truths:
    - "Every card in the library shows a second line, directly under its name, saying whose the row is and when it last changed"
    - "A draft — which today shows no sentence at all — now shows its lineage for the first time"
    - "Typing in the search box does not change the number in '1 of N', and does not make the line flicker"
    - "The card computes nothing about slugs or versions; it renders a value handed down"
  artifacts:
    - path: "frontend/src/components/workflows/library/WorkflowCard.tsx"
      provides: "the 14th atom — one data-testid=\"row-identity\" node at DOM position 2 inside the header's min-w-0 column"
      contains: "row-identity"
    - path: "frontend/src/pages/WorkflowsPage.tsx"
      provides: "the identity index useMemo keyed on [rows] alone, and the one hoisted const now"
      contains: "buildIdentityIndex"
  key_links:
    - from: "frontend/src/pages/WorkflowsPage.tsx"
      to: "frontend/src/components/workflows/library/rowIdentity.ts"
      via: "useMemo(() => buildIdentityIndex(rows), [rows]) — NOT [visibleRows], NOT [query]"
      pattern: "buildIdentityIndex\\(rows\\), \\[rows\\]"
    - from: "frontend/src/pages/WorkflowsPage.tsx"
      to: "frontend/src/components/workflows/library/WorkflowCard.tsx"
      via: "a REQUIRED identity prop, exactly as hasExistingFork is passed at :1082"
      pattern: "identity=\\{"
---

<objective>
Render the identity line: **a 14th atom at DOM position 2** on every card (161-A's placement, 160-B's
content, D-06's quiet resolution), fed by **one `useMemo` on the page keyed on `[rows]` alone** and
**one hoisted `const now`**.

Purpose: this is where SC#1, SC#2 and SC#3 become visible. Everything before it was plumbing.
Output: `WorkflowCard.tsx` grows one node and one required prop; `WorkflowsPage.tsx` grows one memo,
one `now` and one prop hand-down.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

⚠ **WORKTREE BOOTSTRAP IS YOUR FIRST ACTION:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`.
**NEVER `rm -rf` a worktree** — `bash scripts/teardown-worktree.sh <path>`.
⚠ Assert the dispatched base SHA and record it. ⚠ `GSD_VITEST_MAX_WORKERS=4` on every vitest run.

⚠ **Load the project design skill before touching the card:** `Skill("sketch-findings-agentic-rag")`.
The card, the library and the canvas glyph vocabulary are all covered by it, and
`.planning/sketches/163-the-assembled-card/index.html` is the acceptance bar for this task.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/192.1-workflow-identity/192.1-CONTEXT.md
@.planning/phases/192.1-workflow-identity/192.1-RESEARCH.md
@.planning/phases/192.1-workflow-identity/192.1-PATTERNS.md
@.planning/phases/192.1-workflow-identity/192.1-VALIDATION.md
@.planning/sketches/163-the-assembled-card/BUILD-CONTRACT.generated.md
@.planning/sketches/163-the-assembled-card/README.md

<interfaces>
<!-- THE EXACT INSERTION POINT — verified at HEAD. D-08 places the line BETWEEN :382 and :383. -->

WorkflowCard.tsx:376-388
  {/* ── Card chrome (NOT a soul atom): name/version header, folder chip, ⋯ + pill ── */}
  <div className="flex items-start justify-between gap-2">
    <div className="min-w-0">
      <div className="flex items-center gap-2">              ← :378  the name row OPENS
        <span aria-hidden="true">{face.mark}</span>
        <span className={NAME_CLASSES}>{row.name}</span>
        {row.version !== undefined && <span className={VERSION_CLASSES}>v{row.version}</span>}
      </div>                                                  ← :382  the name row CLOSES
      ◀── THE IDENTITY LINE GOES HERE (DOM position 2 inside min-w-0) ──▶
      {folderName && (                                        ← :383  the folder chip
        <span className="mt-0.5 inline-block text-[11px] text-muted-foreground">
          📁 {folderName}
        </span>
      )}
    </div>

The shared class block — WorkflowCard.tsx:202-206 (add IDENTITY_CLASSES here, not inline):
  const NAME_CLASSES    = "truncate text-[14px] font-medium text-foreground"
  const VERSION_CLASSES = "font-mono text-[11px] text-muted-foreground"
  const NOTE_CLASSES    = "text-[11.5px] leading-snug text-muted-foreground"

The sentence gate D-10 relies on — WorkflowCard.tsx:484-488, with runnable = row.provenance !== "draft" (:316):
  {runnable && (
    <p id={consequenceId} data-testid="fork-consequence" className={NOTE_CLASSES}>
      {hasExistingFork ? FORK_CONSEQUENCE_EXISTING : FORK_CONSEQUENCE}
    </p>
  )}
  → every DRAFT renders NO sentence today. The identity line is UNCONDITIONAL and sits OUTSIDE this guard.

The prop-passing precedent — WorkflowsPage.tsx:1066-1084:
  <WorkflowCard key={row.id} row={row} folderName={folderName(row.def?.project_folder_id)} …
                hasExistingFork={row.provenance === "published" && draftBySlug.has(row.slug)} />

⚠ The three sibling memos and the key TRAP — WorkflowsPage.tsx:449 / :461 / :472:
  rows        = useMemo(… mergeLibrary(published, starters, drafts),  [published, starters, drafts])
  visibleRows = useMemo(… filterLibrary(rows, {query, chips, projectId}), [rows, query, activeChips, selectedProjectId])
  counts      = useMemo(… chipCounts(rows, query, selectedProjectId),  [rows, query, selectedProjectId])
  `counts` is the CLOSEST key precedent AND the CLOSEST trap — it is keyed on query BECAUSE a chip's
  number describes the FILTERED view. The identity index is the OPPOSITE (D-05). Do NOT pattern-match
  on the memo three lines above it.

The rendered lines the build must reproduce (BUILD-CONTRACT.generated.md, separators by the card):
  Yours · Original · Ready to run · 1 of 2 · changed 5 months ago
  Yours · v2 of v1 · Still building · 1 of 2 · changed 2 weeks ago
  Shared · changed 5 months ago
  Shared · No project · Shared starter · 1 of 43 · changed 2 months ago
  Yours · Copy of Access Review Attestation · changed just now
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The 14th atom — one row-identity node at DOM position 2 (D-08, D-09, D-10, D-11)</name>
  <files>frontend/src/components/workflows/library/WorkflowCard.tsx, frontend/src/components/workflows/library/WorkflowCard.test.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/library/WorkflowCard.tsx` lines 200-210 (the shared class block), 270-300 (`hasExistingFork`'s prop docblock — the shape to copy, and the trick that does NOT transfer), 310-330 (`runnable`, `consequenceId`), 350-400 (the ownership read at :361 and the insertion point at :376-388, plus the testid-as-literal habit at :396-399), 480-495 (the sentence gate), 540-560 (the `role="status"` precedent at :549)
    - `frontend/src/components/workflows/library/WorkflowCard.test.tsx` lines 1-160 (the header's absence-needs-a-positive-control rule at :10-17, the `rowOf` builder at :84-95, the Radix jsdom shims at :143-148)
    - `frontend/src/components/workflows/library/rowIdentity.ts` (the `RowIdentity` contract from Plan 05)
    - `.planning/sketches/163-the-assembled-card/index.html` lines 330-400 (how the sketch composes the line and its separators)
    - `192.1-CONTEXT.md` D-06, D-08, D-09, D-10, D-11 · `192.1-PATTERNS.md` § 8, § S-3, § S-6
  </read_first>
  <behavior>
    - Every card — published, starter AND draft — renders exactly ONE `data-testid="row-identity"` node
    - Asserted by CHILD ORDER, never by a class name: inside the header's `min-w-0` column the identity node is child index **1**, after the name row and BEFORE the folder chip
    - A colliding row renders `own`, its ≤ 2 computed segments, its `1 of N` and its `changed <rel>`, in that order
    - A unique-name non-fork row renders `own` and `changed <rel>` only — no computed segment, no `1 of N` (D-06 `quiet`)
    - A unique-name FORK row renders its lineage segment (identity, not disambiguation)
    - A DRAFT row renders the identity line AND no `fork-consequence` node — the two are independent (D-10)
    - When `identity.when` is `null` the line still renders and simply carries no `changed` text — with a POSITIVE CONTROL proving a present value DOES render
    - The three root testids (`published-card` / `starter-card` / `draft-card`), the two distinct fork testids, `fork-consequence`, `published-run`, `published-delete`, `draft-delete` and `draft-open` all still resolve, verbatim
    - No `title` attribute appears anywhere in the rendered card
  </behavior>
  <action>
Add a REQUIRED prop to `WorkflowCard`:

```
identity: RowIdentity
```

**⚠ REQUIRED, not optional-with-a-default.** `hasExistingFork`'s optional trick
(`WorkflowCard.tsx:274-288`) exists so the 35 shipped card cases stay byte-identical; it does **not**
transfer. The identity line is UNCONDITIONAL (D-06), so an optional prop defaulting to "no line" would
make every un-updated call site render a card the phase says cannot exist. **Make it required and let
`tsc` enumerate the call sites** (T-3: read the list out of `tsc`, do not predict it).

Copy `hasExistingFork`'s prop-docblock SHAPE, because it states the discipline the identity line
inherits: *"⚠ ONLY THE PAGE CAN KNOW THIS … The card computes NOTHING about slugs or versions — the
same D-12 discipline that keeps it out of the two fork handlers' slug/version mechanics."* (S-3.)

Insert the node **between `:382` and `:383`** — inside the header's `min-w-0` column, immediately after
the name/version row and BEFORE the folder chip (**D-08**). Add an `IDENTITY_CLASSES` const to the
shared block at `:202-206` (that block is the file's established home for chrome classes); the
house-wide secondary-line idiom is `text-[11px] text-muted-foreground` (folder chip, `PhaseSpine.tsx:67`)
or `text-[11.5px] leading-snug text-muted-foreground` (`NOTE_CLASSES`, `DescribeKbPicker.tsx:187`) —
choose one, do not invent a third size.

`data-testid="row-identity"` ships as a LITERAL with a comment saying it is part of the shipped test
surface — the habit at `:396-399` (**D-11**). It is the phase's ONLY net-new test hook.

⚠ **Do not remove or rename ANY shipped testid** (scope fence). ⚠ **Do not add a `title=` attribute
to anything** — F1 sweeps this file, and F1's object-property scoping control does NOT cover a
component prop, which is exactly how the D-07 highlight got deferred. ⚠ **Do not add a second
consequence sentence and do not reword `FORK_CONSEQUENCE` / `FORK_CONSEQUENCE_EXISTING`.**

Composition is at your discretion (D-06 note): one `<p>` with separators or a `<span>` list, provided
there is exactly ONE `row-identity` node per card and child order satisfies D-08. Render the separator
`·` between present parts only — never a dangling separator when a part is absent.

⚠ **D-09 — the owner word is `identity.own`, already resolved by the page through
`CHIP_PREDICATES.yours`.** The card must NOT call the predicate a second time; there is exactly ONE
call in the tree, and Plan 05's Fence B guards the "no owner display name" half.

Extend `WorkflowCard.test.tsx` RED-first with every behavior above. **Assert placement by child
order** — read the `min-w-0` column's `children` and assert index 1 is the identity node — never by a
class name (D-08). Pair every absence assertion with a POSITIVE CONTROL (`WorkflowCard.test.tsx:10-17`:
*"an assertion that a selector finds NOTHING passes identically when the selector is broken, when the
component never had the thing, and when the component did not render at all"*). Keep the four Radix
jsdom shims (`:143-148`) — T-5.
  </action>
  <acceptance_criteria>
    - `grep -c "row-identity" frontend/src/components/workflows/library/WorkflowCard.tsx` equals **1**, and the literal carries the shipped-test-surface comment
    - `identity` is declared REQUIRED on the props interface (no `?`, no default)
    - The identity JSX sits between the name row's closing tag and the `{folderName && (` block, INSIDE the `min-w-0` div — verified by reading the file, and asserted by a child-order test
    - `grep -n "title=" frontend/src/components/workflows/library/WorkflowCard.tsx` returns NOTHING
    - `grep -c "CHIP_PREDICATES.yours" frontend/src/components/workflows/library/WorkflowCard.tsx` is unchanged from HEAD (the existing single call for `owned`; no second call for the line)
    - `git diff frontend/src/components/workflows/library/WorkflowCard.tsx` shows NO change to any existing `data-testid` literal and no change to the `FORK_CONSEQUENCE` render
    - A draft-provenance test asserts BOTH that `row-identity` is present AND that `fork-consequence` is absent, each with a positive control
    - The RED was observed as a real `AssertionError` and both RED and GREEN outputs are quoted in the SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/components/workflows/library/WorkflowCard.test.tsx src/components/workflows/library/librarySubtree.fences.test.ts</automated>
  </verify>
  <done>One `row-identity` node per card at DOM position 2, required prop, unconditional (drafts included), all shipped testids intact, no `title=` anywhere, proved RED-first by child order.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the page — one memo keyed on [rows], one hoisted now (D-05, D-34, P-1, P-2)</name>
  <files>frontend/src/pages/WorkflowsPage.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowsPage.tsx` lines 440-500 (the three sibling memos at :449/:461/:472 and `failedSources` at :489-492), 1060-1100 (the `WorkflowCard` call site)
    - `frontend/src/components/workflows/library/rowIdentity.ts` (the two exported functions)
    - `192.1-CONTEXT.md` D-05, D-28, D-34 and Addendum-2 finding 1 · `192.1-RESEARCH.md` § 4b, § P-1, § P-2, § P-3 · `192.1-PATTERNS.md` § 2
  </read_first>
  <action>
Add ONE memo to the page:

```
const identityIndex = useMemo(() => buildIdentityIndex(rows), [rows])
```

**⚠ `[rows]` ALONE. NOT `[visibleRows]`, NOT `[query]`, NOT `[rows, query, selectedProjectId]`.** Two
independent reasons, both requirements rather than optimisations:
1. **D-05 demands it.** `N` is counted over the FULL merged library before filtering. A count that
   shrank as you typed would both flicker and misdescribe the library.
2. **It makes typing free.** With `[rows]`, a keystroke re-runs `filterLibrary` only — the sketch's
   resolver cost ≈ 230 k inner-loop iterations per render on the operator's shape, twice per render.

⚠ **The trap is three lines above.** `counts` (`:472`) is keyed `[rows, query, selectedProjectId]`
because a chip's number must describe the FILTERED view. Pattern-matching on it silently violates D-05.
Put that sentence in the new memo's docblock so the next reader does not "harmonise" the keys.

Add ONE hoisted clock at the top of the component body:

```
const now = Date.now()
```

**⚠ P-1 — split the memo from the clock, and there is no free option.** If `Date.now()` is called per
card, two cards in one render can disagree across a band boundary. If it is captured INSIDE the
`[rows]`-keyed memo, the whole line FREEZES — a card can still read `just now` an hour later. So:
identity + lineage + `1 of N` are keyed on `[rows]` (they do not depend on time), and
`changed <rel>` is computed during render from ONE `now` so every card in a render agrees. **Accept
that the value only refreshes when something else re-renders, and SAY SO in the docblock rather than
shipping a ticker.** **Do NOT add a `setInterval`** — 188's `WorkflowRunPage` tick-gate bug
(`vitest-count-gate.cjs:497-504`) is this project's recorded lesson that a live clock is its own defect
class.

Pass the resolved value down at the `WorkflowCard` call site (`:1082`'s neighbourhood), exactly as
`hasExistingFork` is passed: `identity={resolveIdentity(identityIndex, row, now)}`.

**⚠ D-28 — do NOT thread `failedSources` into the identity path.** `1 of N` is NOT suppressed when a
feed fails; the `source-failed` banner is the disclosure and it is spent in ONE place rather than 106
times. Drafts are 72 % of the operator's merged rows, so a drafts 403 genuinely collapses `N` — and
suppressing the count would remove a discriminator at exactly the moment the list is hardest to read.
Record that as a comment beside the memo, and note that UAT row **U7** is the named check.

⚠ **T-7 / P-4 — do NOT add a sort.** The sketch sorts by name (`163/index.html:557`); the shipped page
does not, and `mergeLibrary` concatenates in feed order with each feed arriving server-`ORDER BY name`.
A sort here violates the scope fence twice over and re-litigates 160-C, which LOST.

⚠ `noUnusedLocals` is ON — read any orphan out of `tsc`, do not predict it.
  </action>
  <acceptance_criteria>
    - `grep -n "buildIdentityIndex" frontend/src/pages/WorkflowsPage.tsx` shows exactly ONE `useMemo` whose dependency array is literally `[rows]`
    - `grep -c "resolveIdentity" frontend/src/pages/WorkflowsPage.tsx` equals **1**, at the card call site
    - `grep -n "Date.now()" frontend/src/pages/WorkflowsPage.tsx` shows the hoisted `now` OUTSIDE any `useMemo` callback
    - `grep -n "setInterval" frontend/src/pages/WorkflowsPage.tsx` returns NOTHING
    - `grep -n "failedSources" frontend/src/pages/WorkflowsPage.tsx` shows it reaching ONLY the existing banner region, never the identity path
    - `grep -n "\.sort(" frontend/src/pages/WorkflowsPage.tsx` shows no new call over the row list
    - The new memo's docblock names `counts` at `:472` as the trap it is deliberately not copying
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` prints **33**
    - `cd frontend && npx eslint src/components/workflows/library/ src/pages/WorkflowsPage.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/pages/WorkflowsPage.test.tsx src/pages/__tests__/PublishedCardDelete.test.tsx src/pages/__tests__/RunModal.test.tsx src/pages/__tests__/RunModal.a11y.test.tsx</automated>
  </verify>
  <done>The page holds one `[rows]`-keyed index memo, one hoisted `now`, and one resolved prop per card; no ticker, no sort, no `failedSources` in the identity path; tsc 33 and eslint 0.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the line at scale against the D-30 fixture, and reconcile the gate (SC#1, SC#2, SC#3, D-05)</name>
  <files>frontend/src/components/workflows/library/WorkflowCard.test.tsx, scripts/vitest-count-gate.cjs</files>
  <read_first>
    - `frontend/src/components/workflows/library/__fixtures__/libraryScale.ts` (the factory and its orphan rows)
    - `frontend/src/components/workflows/library/rowIdentity.test.ts` (what is already proved at the resolver level — do not duplicate it here; this task proves the RENDERED result)
    - `.planning/sketches/163-the-assembled-card/BUILD-CONTRACT.generated.md` § "Measured invariants at 107 rows"
    - `scripts/vitest-count-gate.cjs` lines 900-975
    - `192.1-VALIDATION.md` § Per-Task Verification Map · `192.1-RESEARCH.md` § 9e
  </read_first>
  <action>
Add scale cases to `WorkflowCard.test.tsx` that render cards from `makeLibraryFixture()` and assert the
sketch's measured invariants **against the real component**, per the README's sketch-assertion →
build-assertion mapping table:

| Sketch assertion | Build assertion |
|---|---|
| identity lines on every card (quiet mode) | one `row-identity` node per rendered card, count == card count |
| never more than 2 computed segments | over EVERY rendered line |
| `1 of N` only on colliding names | over EVERY rendered line |
| identity line at DOM position 2, above the folder chip | by child order |
| all 5 soul atoms survive on every card | already shipped — keep it green |
| exactly 1 sentence node per runnable card, 0 on drafts | already shipped — keep it green |
| the sentence text is unchanged from shipped | `FORK_CONSEQUENCE` imported, never retyped |
| the two fork testids stay distinct | already shipped — D-12's only mechanical evidence |
| no owner display name anywhere | Plan 05's Fence B — assert here that only `Yours`/`Shared` appear as owner words |
| zero hover-only tooltip attributes | already fenced by F1 |

Add the three SC assertions explicitly, each named with its criterion:
- **SC#1** — at the fixture's 100+ rows with 14 duplicated names, every colliding row's line carries
  ≥ 1 discriminator AND `1 of N`
- **SC#2** — a fork row names what it came from; an original says `Original`; the orphan
  `compliance-gap-report` row renders NO lineage segment (with a positive control proving a resolving
  sibling DOES)
- **SC#3** — `changed <rel>` renders on all three provenances

⚠ **RE-DERIVE, DO NOT PIN THE SKETCH'S NUMBERS.** `BUILD-CONTRACT.generated.md` says
`cards rendered: 108` — 108, not 107, because the 162-B drive really created a row (the README says so
honestly) — and `lines carrying '1 of N': 94` was measured at those 108 cards. **Compute both from the
ported fixture and pin YOUR numbers**, quoting the derivation.

⚠ **T-4 — the RED-first trap recorded in this very tree.** `WorkflowsPage.test.tsx`'s `asyncUtilTimeout`
is 15 s, longer than vitest's 5 s per-test budget, so a plain `findByTestId` on an absent node blows the
TEST timeout and yields an uninformative RED rather than an `AssertionError`. **Wait on a call count
first, then assert under an explicit short budget.** Apply the same discipline in `WorkflowCard.test.tsx`.

Raise every grown pin in `scripts/vitest-count-gate.cjs` in the same commit, read from the gate's own
`actual` column across two agreeing runs. ⚠ Do not "fix" the two pre-existing above-pin suites (T-9).
Run the full gate and record its `total · failed · pinned total` line verbatim.
  </action>
  <acceptance_criteria>
    - Scale cases render ≥ 100 fixture rows and assert: one `row-identity` per card; `segs.length <= 2` over every line; `1 of N` present iff colliding
    - Three cases are named for SC#1, SC#2 and SC#3 respectively and each maps to its criterion in a comment
    - The orphan `compliance-gap-report` case asserts NO lineage segment and is paired with a positive control on a resolving sibling
    - The rendered card count and the `1 of N` line count are DERIVED from the fixture and quoted in the SUMMARY with their derivation — the sketch's 108 / 94 are not pinned
    - Every async assertion waits on a call count before asserting, under an explicit short budget (no bare `findBy*` on an expected-absent node)
    - `GSD_VITEST_MAX_WORKERS=4 node scripts/vitest-count-gate.cjs` exits 0 and its summary line is quoted verbatim
    - `cd frontend && npm run lint:a11y` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && GSD_VITEST_MAX_WORKERS=4 npx vitest run src/pages/WorkflowsPage.test.tsx src/components/workflows/library/ src/pages/__tests__/PublishedCardDelete.test.tsx src/pages/__tests__/RunModal.test.tsx src/pages/__tests__/RunModal.a11y.test.tsx</automated>
  </verify>
  <done>The rendered line is proved at the realistic 100+ row duplicate shape against the real component, the three success criteria have named cases, the numbers are re-derived rather than inherited, and the gate exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resolved value → DOM | The identity line is rendered as JSX text nodes; React escapes them. |
| page state → per-card render | The memo key decides both correctness (D-05) and cost. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192.1-15 | Spoofing (of fact) | `1 of N` under a failed feed | accept | **D-28**, decided rather than defaulted: the `LIBRARY_STATES["source-failed"]` banner is the disclosure and is spent in ONE place. Drafts are 72 % of the operator's rows, so suppressing the count would remove a discriminator exactly when the list is hardest to read. UAT row **U7** is the named check, and the acceptance is recorded in code beside the memo. |
| T-192.1-16 | Denial of Service | Per-keystroke re-resolution | mitigate | The memo is keyed on `[rows]` alone (grep-asserted on the literal dependency array). The sketch's shape cost ≈ 230 k inner-loop iterations per render, twice per render, on every keystroke. |
| T-192.1-17 | Information Disclosure | Hover-only tooltip carrying more than the line shows | mitigate | F1 sweeps the card for a `title` JSX attribute; grep-asserted absent. Touch has no hover, so a `title=` is a disclosure that some users can never reach. |
| T-192.1-18 | Tampering | XSS via a row name rendered into the line | mitigate | Rendered as a JSX text node (React escapes); `dangerouslySetInnerHTML` is fenced by T-192-04 across the subtree. |
| T-192.1-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero packages installed.** No date library, no tooltip library, no virtualiser. |
</threat_model>

<verification>
- `cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS"` → **33**
- `cd frontend && npx eslint src/components/workflows/library/ src/pages/WorkflowsPage.tsx` → exit 0
- `cd frontend && npm run lint:a11y` → exit 0
- The eight-suite scoped run → 0 failed
- `GSD_VITEST_MAX_WORKERS=4 node scripts/vitest-count-gate.cjs` → exit 0
</verification>

<success_criteria>
- Every card carries exactly one `row-identity` node at DOM position 2, drafts included
- The identity index is built once per `rows` change and the clock is hoisted once per render
- `1 of N` does not change when the search box is typed into
- SC#1, SC#2 and SC#3 each have a named passing case at the realistic 100+ row duplicate shape
- No shipped testid removed or renamed; no `title=` anywhere; no sort added
</success_criteria>

<output>
Create `.planning/phases/192.1-workflow-identity/192.1-06-SUMMARY.md` when done.
Record: the base SHA; the RED/GREEN pair for the child-order assertion; the DERIVED card count and
`1 of N` line count with their derivation (and how they differ from the sketch's 108 / 94 and why);
the `tsc` call-site list the required prop produced; and the gate's summary line verbatim.
</output>
