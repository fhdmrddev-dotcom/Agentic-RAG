---
phase: 193.2-from-authored-to-runnable
plan: 02
type: execute
wave: 2
depends_on: ["193.2-01"]
files_modified:
  - .planning/phases/193.2-from-authored-to-runnable/193.2-D10-D19-CONFIRMATION.md
  - frontend/src/pages/WorkflowsPage.test.tsx
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "D-10 — the phase records, from re-run evidence rather than from RESEARCH's say-so, that the shipped author-time interactive gate DID fire and the publish endpoint never refused anything; the escape hatch (`if the gate is silently not firing, rewriting the string fixes nothing`) is explicitly recorded as NOT triggered"
    - "D-10 — the consequence is recorded: the surface is the Builder header, so the refusal's action is a PRECISE INSTRUCTION and never a live control"
    - "D-19 — the post-publish Run CTA is measured before anything is built beside it, and the honest verdict is recorded: it fires and names the workflow, it is NOT a hand-off to a location, and it had ZERO automated coverage"
    - "The post-publish Run CTA has its first automated pin ever: absent before the published refetch lands, present and naming the workflow after"
    - "No navigate-to-row behaviour is added to `WorkflowsPage.tsx` — D-19 and the file's inherited G-5 obligation both forbid it"
  artifacts:
    - path: ".planning/phases/193.2-from-authored-to-runnable/193.2-D10-D19-CONFIRMATION.md"
      provides: "The D-10 and D-19 confirmations recorded as artifacts, with the commands and their output"
      contains: "publish_blocked"
      min_lines: 50
    - path: "frontend/src/pages/WorkflowsPage.test.tsx"
      provides: "The first automated coverage the post-publish Run CTA has ever had"
      contains: "run-cta"
  key_links:
    - from: "frontend/src/pages/WorkflowsPage.test.tsx"
      to: "frontend/src/pages/WorkflowsPage.tsx"
      via: "a held-promise published refetch driving onGauntletPublished → runCta → the run-cta banner"
      pattern: "run-cta"
---

<objective>
Wave 1 of the RESEARCH wave shape. **Two shipped surfaces are treated as suspects, not scenery**
(D-10, D-19). Both should already have covered half of this phase and apparently did not. Nothing may
be built beside either until it is observed.

RESEARCH already answered D-10 with HIGH confidence across nine hops plus the live audit ledger, so
**this is a confirmation-and-record wave, not a discovery wave — plan and execute it as the cheaper
thing it is.** Its two jobs: (1) re-run the evidence at HEAD and write the answer down as a durable
artifact so no later plan re-derives or mis-quotes it, and (2) close the one measured gap RESEARCH
found — the post-publish Run CTA has **zero** automated coverage, and WR-04 already fixed one silent
drop on it that no test would have caught.

Purpose: 193.1's Wave 1 was sequenced exactly this way and found that phase's CENTRAL assumption
FALSE. At the gate, that would have cost the phase.

Output: a confirmation artifact and the CTA's first pin.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

**Worktree bootstrap FIRST:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`.
**Assert the dispatched base** before reading anything else — `git rev-parse HEAD` must match the
`base_sha` recorded in `193.2-BASELINE.md` (or its descendant on this phase's branch). A worktree
forked from the wrong base reports green typechecks and red tests for reasons that look like the
plan's fault.
**Every vitest invocation carries `GSD_VITEST_MAX_WORKERS=2`.**
</execution_context>

<context>
@.planning/phases/193.2-from-authored-to-runnable/193.2-CONTEXT.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-RESEARCH.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-PATTERNS.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-BASELINE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run the D-10 and D-19 evidence at HEAD and write the confirmation artifact</name>
  <files>.planning/phases/193.2-from-authored-to-runnable/193.2-D10-D19-CONFIRMATION.md</files>
  <read_first>
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-RESEARCH.md` — §R1 in full (the audit-ledger query, the nine-hop table, the three candidate causes, the two consequences) and §R3 in full (the CTA's wiring, the benign race, what it genuinely does not do)
    - `backend/app/services/harness/publish_service.py:48-52` (the `publish_attempted` vs `publish_blocked` keying rule), `:190-208` (stage 2.5), `:434-468` (`_block`), `:471+` (`_safe_audit` swallows write failures — the caveat)
    - `backend/app/api/workflows.py:650-709` (the verdict-taxonomy docblock, `_ROUTE_ASSIGNED_CODES`, `_INCOMPLETE_CODES`), `:884-950` (the finding append order and `ok=(len(verdicts) == 0)`)
    - `frontend/src/pages/WorkflowBuilderPage.tsx:1338-1349` (`blockedReason`, the verbatim-message rule, the fail-closed `isCheckOutstanding` branch), `:988` (`useLiveValidation`)
    - `frontend/src/components/workflows/PublishGauntlet.tsx:846, 921, 932-939` (`blocked`, `disabled={blocked}`, `publish-blocked-reason`)
    - `frontend/src/pages/WorkflowsPage.tsx:721-729, 806-839, 861-864, 897-917` (`onGauntletPublished`, the `renderPublish` mount, the `runCtaRow` lookup, the banner)
    - `frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx:3017-3046` — the shipped pin *"187-28 — a route-assigned verdict GATES the Publish control"*
  </read_first>
  <action>
Create `.planning/phases/193.2-from-authored-to-runnable/193.2-D10-D19-CONFIRMATION.md`. Frontmatter:
`phase: 193.2`, `decisions: [D-10, D-19]`, `confirmed: 2026-08-15`, `head_sha:` from `git rev-parse HEAD`.

**§1 — D-10, the ledger re-run.** Re-run RESEARCH's decisive query against the live local Postgres at
`127.0.0.1:54322` using `backend/venv/Scripts/python.exe` with `psycopg2` (the evidence-tools recipe
this repo already uses), and paste both results verbatim:
  - `select event_type, count(*), min(created_at), max(created_at) from harness_audit where created_at >= '2026-08-15' group by event_type`
  - `select created_at, metadata->>'blocked_stage' from harness_audit where event_type='publish_blocked' order by created_at desc limit 25`

Then state the conclusion in the artifact's own words: `publish_attempted` is written only at stage 3
and every pre-run gate (stages 0/1/2/**2.5**/2.6 — 2.5 IS the interactive block) writes
`publish_blocked`; therefore **the publish endpoint never refused anything on the operator's session.**
⚠ Record RESEARCH's caveat honestly: `_safe_audit` swallows write failures, so "zero `publish_blocked`
rows" is strong evidence rather than proof — and name the independent positive half (exactly one
`publish_attempted` + one `publish_succeeded`, with the `publish_succeeded` timestamp matching the QBR
row's `updated_at`).

**§2 — D-10, the ruling.** Write the three consequences as decisions the rest of the phase inherits:
  1. **D-10's escape hatch does NOT trigger.** The gate is not silently broken; rewriting the string
     IS the fix, and D-11 + D-13 are the whole of it. Quote D-10's own sentence and mark it
     not-triggered rather than deleting it.
  2. **The surface is the Builder header** — `publish-blocked-reason` is a `text-[12px] leading-snug`
     span in a `shrink-0` header slot beside the disabled `◆ Publish…` button, with the offending
     step visible on the canvas and its `✕` (`PlaneEditingLayer.tsx:256`,
     `aria-label={`Remove step ${position + 1}`}`) one click away. ⇒ **D-13's "offers an action" is a
     PRECISE INSTRUCTION, not a live control** — a control would exist on only one of the two
     surfaces the single string feeds, which D-12 forbids, and would be dead weight on the publish
     refusal render.
  3. **The length ceiling is a hard UI constraint.** Today's message is 105 chars;
     `BUSINESS_REQUIREMENT_MISSING_MESSAGE` is 99. RE-DERIVE both by importing the constants and
     `len()`-ing them rather than trusting the numbers, and record what you measured. A rewritten
     message carrying a step label should aim at the same order of magnitude.

**§3 — D-10, the nine hops re-verified mechanically where a test already owns them.** Run
`cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_187_route_assigned_reach.py -q`
and `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowBuilderPage.canvas.test.tsx`
and record that the shipped pin *"187-28 — a route-assigned verdict GATES the Publish control"*
passes at HEAD. For the hops no test owns, record the `file:line` and the one-line reading, and
**correct RESEARCH's own off-by-one beside it, never over it** (CONTEXT says `workflows.py:895-903`;
measured it is `:894-902`).

**§4 — D-19, the CTA verdict.** Record RESEARCH R3's measured findings and re-verify the two that are
cheap to re-check: `grep -rn "run-cta\|runCta\|Ready to run it" frontend/src --include=*.test.tsx`
(expected: no matches BEFORE task 2) and the `runCtaRow` lookup running against `rows` (unfiltered),
not `visibleRows`. Then write the honest verdict against D-19's two acceptable outcomes: **neither
branches cleanly.** The CTA is wired and should fire; it is ephemeral, self-dismissing, and **not a
location** — it solves *"run it right now"* and does not solve *"find it again"*, which is the second
half of SC#3. ⇒ **D-19 resolves as: it fires, SC#3 is met by the sort (plan 193.2-03) plus one
automated pin so its next silent drop is caught. NO navigate-to-row behaviour is built** —
`WorkflowsPage.tsx` carries an inherited G-5 obligation (re-derive its counts from
`193.2-BASELINE.md` §5) and CONTEXT names navigate-to-row a genuine SECOND concern.

**§5 — What is routed to UAT rather than automated.** OQ-1 (did the operator SEE the greyed control)
and OQ-2 (does the CTA appear, and after how long) require a driven browser cycle. Record that both
are authored as rows in `193.2-UAT.md` by plan 193.2-10, with the Chrome MCP recipe
(⚠ `take_screenshot` times out on this box — read DOM geometry via `evaluate_script`), and state
plainly that this plan does NOT claim to have observed pixels.

Commit `docs(193.2-02): D-10 and D-19 confirmed and recorded at HEAD`.
  </action>
  <acceptance_criteria>
    - `193.2-D10-D19-CONFIRMATION.md` exists with frontmatter carrying `decisions: [D-10, D-19]` and a `head_sha`
    - §1 contains BOTH query results pasted verbatim, and the sentence that the publish endpoint never refused anything, and the `_safe_audit` caveat named as a caveat
    - §2 contains the literal token `D-10` and states, in words, that the escape hatch did NOT trigger; and contains the literal token `D-13` with the ruling `precise instruction` (not a live control)
    - §2 records RE-DERIVED `len()` values for the current interactive message and `BUSINESS_REQUIREMENT_MISSING_MESSAGE`, not copied numbers
    - §3 records `test_187_route_assigned_reach.py` passing and the `187-28` canvas pin passing, plus the `:894-902` correction stated BESIDE the CONTEXT value
    - §4 contains the literal token `D-19` and the sentence that no navigate-to-row behaviour is built, with the G-5 reason
    - §5 names OQ-1 and OQ-2 as UAT rows owned by plan 193.2-10 and states this plan observed no pixels
    - `git show --stat` for this commit lists exactly one file, under `.planning/`
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && grep -q "publish_blocked" .planning/phases/193.2-from-authored-to-runnable/193.2-D10-D19-CONFIRMATION.md && grep -q "D-10" .planning/phases/193.2-from-authored-to-runnable/193.2-D10-D19-CONFIRMATION.md && grep -q "D-19" .planning/phases/193.2-from-authored-to-runnable/193.2-D10-D19-CONFIRMATION.md && cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_187_route_assigned_reach.py -q</automated>
  </verify>
  <done>Both suspect surfaces have been re-measured at HEAD and their verdicts written down as durable artifacts, including the two rulings (precise instruction, no navigate-to-row) that bind plans 193.2-06 and 193.2-10.</done>
</task>

<task type="auto">
  <name>Task 2: The post-publish Run CTA's first automated pin</name>
  <files>frontend/src/pages/WorkflowsPage.test.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowsPage.tsx:721-729` (`onGauntletPublished` — `setPageView("library")`, `setRunCta({slug, version})`, both refetches), `:806-839` (the `renderPublish` mount and the WR-04 `_def.slug` with its `builderInitial` fallback), `:861-864` (`runCtaRow = rows.find(row => row.slug === runCta.slug && row.provenance !== "draft")`), `:897-917` (the `data-testid="run-cta"` banner and its `▶ Run now` button calling `setRunCta(null)`)
    - `frontend/src/pages/WorkflowsPage.test.tsx:449-548` — the standing `beforeEach` that resolves all three feeds plus the hosted Builder's `listFolders` / `listSkills`
    - `frontend/src/pages/WorkflowsPage.test.tsx:1889-1905` — **Analog A**, the mount + `mockClear` + refetch-assertion shape
    - `frontend/src/pages/WorkflowsPage.test.tsx:1589-1636` — **Analog B**, the held-promise pattern (`let release`, `mockResolvedValueOnce(...)` then `mockReturnValueOnce(held)`, IN FLIGHT / SETTLED)
    - `frontend/src/pages/WorkflowsPage.test.tsx` header docblock — the **T-4** rule: ⚠ NO `findBy` ON AN ABSENT NODE. This file sets `asyncUtilTimeout: 15000`, longer than vitest's 5 s per-test budget, so a `findByTestId` for a node that never mounts blows the TEST timeout and yields a RED indistinguishable from a real failure. Cases asserting ABSENCE must wait on a node that IS present on their own path first, then query synchronously
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-PATTERNS.md` — §"`frontend/src/pages/WorkflowsPage.test.tsx` (extend)" and §"THE ONE PATTERN EVERY PLAN MUST COPY — the positive control"
  </read_first>
  <action>
Add ONE new `describe` block to `frontend/src/pages/WorkflowsPage.test.tsx` — do not restructure the
file, do not touch any existing case. Name it so its purpose is legible, e.g.
`describe("193.2 / D-19 — the post-publish Run CTA, its first automated coverage")`.

Compose Analog A + Analog B. The shape:

1. **The race case, which is the whole point.** Hold the published refetch open. Enter the hosted
   Builder, drive the gauntlet to PASS so `onGauntletPublished(version, slug)` fires (mock the
   publish call the way the suite's existing gauntlet-adjacent mocks do — wire `mockPublish` in this
   describe's own setup rather than editing the shared `beforeEach`). Assert:
   - **IN FLIGHT:** `screen.queryByTestId("run-cta")` is `null`. ⚠ T-4 applies — first `await` a node
     that IS present on this path (the library toolbar), then query for `run-cta` **synchronously**.
     Do not use `findByTestId` here.
   - **SETTLED:** `release([...withTheNewlyPublishedRow])`, then
     `await screen.findByTestId("run-cta")` — the safe direction, the node is expected present.
   - The banner **names the workflow**: assert its text contains the published row's `name` and the
     version, read off the fixture object rather than re-typed.
2. **The positive control, in the same file.** A sibling case proving the absence assertion in (1)
   can actually fail: drive the identical path but release a published list that does **not** contain
   the new slug, and assert `run-cta` stays absent — then assert, in the same test, that releasing a
   list that DOES contain it makes the node appear. An absence assertion whose corpus is always empty
   is the commonest way a fence stops seeing (`rowIdentity.test.ts:167-179`, part 2 of the
   four-part template).
3. **The draft-exclusion pin.** `runCtaRow` requires `row.provenance !== "draft"`. Assert that a
   published list containing the slug produces the banner while a drafts-only list carrying the same
   slug does not — this is the WR-04 class of silent drop, and it is exactly what has never been
   covered.

⚠ **Do NOT add navigate-to-row, scroll-to-row or highlight behaviour, and do NOT modify
`WorkflowsPage.tsx`.** This plan's `files_modified` deliberately contains only the test file. D-19
and the file's inherited G-5 obligation both forbid a second hand-off surface; the ruling is recorded
in `193.2-D10-D19-CONFIRMATION.md` §4.

⚠ **The count gate is pinned per file.** After adding cases, re-run
`GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs`; if the gate pins this file's case
count, update the pin in the same commit and say so in the summary — a gate that silently absorbs a
count change is not a gate.

Commit `test(193.2-02): the post-publish Run CTA gets its first automated pin (D-19)`.
  </action>
  <acceptance_criteria>
    - `grep -c "run-cta" frontend/src/pages/WorkflowsPage.test.tsx` returns ≥ 3 (it returned 0 before this task — record the before value in the summary)
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowsPage.test.tsx` reports 0 failed, and the new describe's cases appear in the output
    - The new describe contains at least one `queryByTestId("run-cta")` absence assertion and at least one `findByTestId("run-cta")` presence assertion, and no `findByTestId` is used on a node expected to be absent
    - The positive-control case is present: releasing a list WITHOUT the slug keeps the node absent, releasing one WITH it makes the node appear, both asserted in the suite
    - `git diff --numstat` shows `frontend/src/pages/WorkflowsPage.tsx` UNCHANGED (0 lines) — the plan adds coverage, not behaviour
    - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` prints `count gate OK`; if the pinned total moved, the pin update is in this same commit
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` is at the baseline error count recorded in `193.2-BASELINE.md` §3
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowsPage.test.tsx</automated>
  </verify>
  <done>The post-publish Run CTA has automated coverage for the first time: the refetch race, the naming, the draft exclusion, and a positive control proving the absence assertion can fail. `WorkflowsPage.tsx` itself is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local Postgres → the confirmation artifact | An audit-ledger read whose output is pasted into a planning document |
| none in shipped code | This plan modifies one test file and one planning artifact; no runtime code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193.2-02 | Information disclosure | `harness_audit` rows pasted into `193.2-D10-D19-CONFIRMATION.md` | mitigate | Paste only `event_type`, `count`, `created_at` and `metadata->>'blocked_stage'`. Never paste `metadata` wholesale, never a user id, never an email — the artifact is committed to the repository |
| T-193.2-01 | Repudiation | The D-10 ruling itself | mitigate | The conclusion is recorded with its command output and its caveat (`_safe_audit` swallows write failures), so a later reader can re-derive rather than trust. RESEARCH's `:895-903` → `:894-902` correction is recorded BESIDE the original |
| T-193.2-SC | Tampering | npm/pip/cargo installs | accept | No package is installed. The CTA pin composes two analogs already in the suite; `psycopg2` is already in `backend/venv` |
</threat_model>

<verification>
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowsPage.test.tsx` → 0 failed
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` → `count gate OK`
- `git diff --numstat HEAD~2 -- frontend/src/pages/WorkflowsPage.tsx` → EMPTY
- `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_187_route_assigned_reach.py -q` → 0 failed
</verification>

<success_criteria>
- `193.2-D10-D19-CONFIRMATION.md` records both confirmations with their commands and output, and both rulings (precise instruction; no navigate-to-row)
- The Run CTA has ≥ 3 new assertions where it had zero, including a positive control
- `WorkflowsPage.tsx` is byte-unchanged
</success_criteria>

<output>
Create `.planning/phases/193.2-from-authored-to-runnable/193.2-02-SUMMARY.md` when done.
</output>
