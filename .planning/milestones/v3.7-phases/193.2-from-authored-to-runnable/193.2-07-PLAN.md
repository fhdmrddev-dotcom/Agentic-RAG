---
phase: 193.2-from-authored-to-runnable
plan: 07
type: execute
wave: 4
depends_on: ["193.2-05"]
files_modified:
  - backend/app/models/harness.py
  - backend/app/services/workflow_authoring.py
  - backend/tests/unit/test_workflow_authoring_requirement.py
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "D-06 — the requirement arrives PRE-FILLED and carries a DURABLE provenance mark that the AI wrote it; the shipped `name_seeded_by_ai` pattern is reused, not reinvented, and the mark survives a reload because it is a model field rather than page state"
    - "D-06 — a value that trims empty is never stamped: provenance for a requirement that does not exist would make the demote-on-edit rule read a lie"
    - "D-07 made mechanical — the stamp additionally refuses a value that is a byte-identical copy of the describe text; a durable requirement is a harder bar than one run's task instruction, and that is part of the T-193.2-03 mitigation"
    - "D-08 — the fallback when the model emits nothing is TODAY'S EXACT BEHAVIOUR: blank field plus the shipped `REQUIREMENT_INVITATION`. No new failure mode, no new string, no second derive call, and the field stays schema-optional"
    - "D-09 — the publish gate is NOT changed; stage 1's `business_requirement_missing` predicate only checks non-emptiness, an AI-seeded value passes it untouched, and that is accepted because pressing Publish IS the consent"
    - "D-02 — `models/harness.py` gains no SECOND concern: the model already carries `business_requirement` AND already carries a `name_seeded_by_ai` provenance boolean; a sibling provenance boolean beside the field it describes is the same concern"
    - "F-6 — the stamp is never true for an empty, whitespace-only or absent value, and it ignores any provenance claim the model puts in its own emitted payload"
  artifacts:
    - path: "backend/app/models/harness.py"
      provides: "The additive-optional provenance field on WorkflowDefinition, zero-migration, beside the field it describes"
      contains: "business_requirement"
    - path: "backend/app/services/workflow_authoring.py"
      provides: "The server-side provenance stamp on the single success path, beside the shipped name_seeded_by_ai stamp"
      contains: "model_copy"
    - path: "backend/tests/unit/test_workflow_authoring_requirement.py"
      provides: "F-6 plus the adversarial payload-claim case and the describe-copy refusal"
      contains: "seeded"
  key_links:
    - from: "backend/app/services/workflow_authoring.py"
      to: "backend/app/models/harness.py"
      via: "a sibling model_copy stamping the new WorkflowDefinition provenance field on the single success path"
      pattern: "model_copy"
---

<objective>
D-06's durable half. The requirement field arrives pre-filled **and visibly marked as an AI
proposal** — the visible mark is plan 193.2-09; this plan builds the thing it renders.

⚠ **A field the CONTEXT did not anticipate, and it is load-bearing.** D-06 asks for a durable mark
and names the shipped pattern — a persisted boolean (`PhaseSpec.name_seeded_by_ai`). But
`WorkflowDefinition` extends `_StrictBase` with `model_config = ConfigDict(extra="forbid")`
(**MEASURED**), so an undeclared key **422s the autosave PATCH**. A durable mark therefore requires
an additive field on `WorkflowDefinition` in `backend/app/models/harness.py` — a **seventh** hot file,
**16 commits / 15 phases / 578 L, also ABSENT from the ledger.** G-5 fires on it.

**The decision this plan takes, stated rather than assumed:** add the field. The D-02 no-second-concern
argument is easy and honest here — the model already carries `business_requirement` and already
carries `name_seeded_by_ai`; a sibling provenance boolean beside the field it describes is the same
concern. **The alternative — deriving the mark from page state with no model change — loses the mark
on reload or re-open**, and D-06 says in as many words: *"Reuse the shipped provenance pattern, do not
invent one."*

Purpose: without a persisted flag, the visible mark in plan 193.2-09 would be a lie the moment the
author refreshed the page.

Output: one additive-optional field (zero migration), one sibling `model_copy` stamp, and F-6.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

**Worktree bootstrap FIRST:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`, then assert the
dispatched base SHA — this plan runs **after** 193.2-05 and shares two files with it, so the base
must already contain 193.2-05's commits.
</execution_context>

<context>
@.planning/phases/193.2-from-authored-to-runnable/193.2-CONTEXT.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-RESEARCH.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-PATTERNS.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The additive-optional provenance field on WorkflowDefinition</name>
  <files>backend/app/models/harness.py</files>
  <read_first>
    - `backend/app/models/harness.py:38-41` — `_StrictBase` with `model_config = ConfigDict(extra="forbid")`, **the reason a page-state-only mark cannot work**
    - `backend/app/models/harness.py:531-548` — `business_requirement: str | None = None` with its *"the ONE business requirement the QUAL-01 judge anchors to … Additive-optional (old rows model_validate() to None). OPTIONAL on a draft; the publish ENDPOINT, NOT this schema, enforces required-at-publish"* comment, and `category: str | None = None` immediately after with **the comment to copy** — it names the failure the field prevents (`extra="forbid"` would 422 on the key) and states the zero-migration argument in full
    - `backend/app/models/harness.py:435` — `name_seeded_by_ai: bool = False` with its trailing-comment idiom (correction C-5: the declaration is the single line `:435`; `:420-434` is the docblock)
    - `backend/app/models/harness.py:420-434` — the docblock recording that the node FACE is computed AT RENDER and NEVER stored, because the save path persists `model_dump(mode="json")` and a derivation living in the model would be BAKED into the JSONB
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-PATTERNS.md` §"`backend/app/models/harness.py` — the additive provenance field" and §"Shared Patterns / 5. Additive-optional, zero-migration schema growth"
  </read_first>
  <action>
Add ONE field to `WorkflowDefinition`, immediately beside `business_requirement` — the field it
describes. Name it in the shipped idiom, e.g. `business_requirement_seeded_by_ai: bool = False`, and
match `name_seeded_by_ai`'s declaration shape exactly (a plain `bool` defaulting to `False`, never an
`Optional[bool]` — a tri-state would make "absent" and "hand-typed" indistinguishable).

Its comment must reproduce **both** halves of `category`'s argument, because `category`'s comment is
the one that names the failure the field prevents:
  - **Additive-optional, zero-migration:** the column is JSONB, old rows `model_validate()` to the
    default. **No file under `supabase/migrations/` is added by this phase** — say so.
  - **Load-bearing:** `extra="forbid"` on `_StrictBase` means an undeclared key **422s the autosave
    PATCH**, which is exactly why D-06's durable mark cannot be a page-state invention. State this,
    and state that the field **does NOT relax `extra="forbid"`** — every other unknown key still
    raises `ValidationError`.
  - Name the phase (193.2), the decision (`D-06`) and the seed (`SEED-163`), and point at
    `PhaseSpec.name_seeded_by_ai` as the pattern being reused.
  - ⚠ **State the one rule the pattern carries that a reader could otherwise get wrong:** the marker
    is **stamped server-side after validation and never read off the emitted payload**, so a model
    cannot claim its own text was hand-typed. That rule lives in `workflow_authoring.py` (task 2);
    the model comment points at it so the two halves are findable from either end.

⚠ **Do NOT add a model validator, a computed field, or any derivation.** The save path persists
`model_dump(mode="json")`; a derivation living in the model would be baked into the JSONB and a stale
row could then lie about itself. `harness.py:420-434` records this by name.

⚠ **Do NOT change `required`, `extra`, `_StrictBase`, or any other field.**

**D-02, proved not asserted:** state in the summary that `models/harness.py` already carries
`business_requirement` and already carries `name_seeded_by_ai`; this is a sibling provenance boolean
beside the field it describes, not a second concern. If you find otherwise, STOP — D-01 is void for
this file and a refactor recommendation is owed FIRST. **The ledger row for this file is a deliverable
of plan 193.2-10 whether or not this plan edits it; this plan DOES edit it, so the row must say so.**

Commit `feat(193.2-07): an additive-optional provenance flag beside the requirement it describes (D-06)`.
  </action>
  <acceptance_criteria>
    - `WorkflowDefinition` carries exactly one new boolean field, declared adjacent to `business_requirement`, defaulting to `False`, typed `bool` (not `bool | None`)
    - `backend/venv/Scripts/python.exe -c "from app.models.harness import WorkflowDefinition as W; print(W.model_validate({'slug':'s','version':1,'name':'n','phases':[]}))"` succeeds and the new field reads `False` — old rows validate clean
    - Round-trip: a dict containing the new key validates, and `model_dump(mode="json")` emits it
    - An unknown key still raises `ValidationError` — assert `extra="forbid"` was NOT relaxed
    - The field's comment contains the literal tokens `D-06`, `SEED-163`, `extra="forbid"` and a `no migration` statement
    - `git diff --numstat` shows no change to `_StrictBase`, to any `required` list, or to any other field in the file
    - `ls supabase/migrations/ | wc -l` unchanged from HEAD
    - `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit -q -k "harness or 103 or 187"` → no new failures vs `193.2-BASELINE.md` §2
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && ./venv/Scripts/python.exe -m pytest tests/unit -q -k "harness or 103 or 187"</automated>
  </verify>
  <done>The provenance mark has a durable home beside the field it describes, with the zero-migration and `extra="forbid"` arguments recorded in the comment, and nothing else in the model moved.</done>
</task>

<task type="auto">
  <name>Task 2: The server-side stamp, on the single success path, beside the shipped one</name>
  <files>backend/app/services/workflow_authoring.py</files>
  <read_first>
    - `backend/app/services/workflow_authoring.py:353-379` in full — the `name_seeded_by_ai` stamp AND its whole six-paragraph comment. **Five of those paragraphs state rules the new stamp inherits unchanged**: stamped server-side, after validation, unconditionally, on the SINGLE success path so first-emit and retry-emit are identical; never read off the emitted payload; an empty value is never stamped, because *"provenance for a name that does not exist would make that rule read a lie"*; `model_copy` and never a mutation in place; never a model-level validator hook, because the save path persists `model_dump(mode="json")`
    - `backend/app/services/workflow_authoring.py:387` — the slug mint, which the new stamp must land BEFORE
    - `backend/app/services/workflow_authoring.py` — where `describe` is available on this path, so the D-07 byte-identity comparison can be made
    - `backend/app/services/harness/grounding.py:1000-1007` — `BUSINESS_REQUIREMENT_MISSING_MESSAGE` and `business_requirement_missing`, publish stage 1's predicate: **it only checks non-emptiness, so an AI-seeded value passes it untouched, and D-09 accepts that**
    - `frontend/src/pages/WorkflowBuilderPage.tsx:383` — the shipped `REQUIREMENT_INVITATION`, which is D-08's unchanged fallback
    - `backend/app/services/harness/publish_service.py:966-972` and `backend/app/services/harness/validator_kinds.py:142-149` — `JUDGE_RUBRIC_CORE.format(business_requirement=...)` and `answers_business_requirement`, the self-grading loop this plan's D-07 control mitigates
  </read_first>
  <action>
Add ONE sibling `model_copy` stamp in `generate_workflow_definition`, immediately beside (or folded
into) the shipped `name_seeded_by_ai` stamp, on the same single success path, **before** the slug
mint. Set the new `WorkflowDefinition` provenance field.

**The predicate.** Start from the shipped rule verbatim — `bool(x and x.strip())` — and **widen it in
the SAME expression** with D-07 made mechanical: refuse to stamp when the emitted
`business_requirement`, normalised (strip + whitespace collapse + casefold), is **byte-identical to
the normalised `describe` text**. Keep this inside one `bool(...)`; it is a widening of the existing
"empty was not seeded" predicate on the same expression, not a new concern.

Copy the whole six-paragraph comment shape, adapted, so the new stamp carries its rules where a
reader lands on them. It must state:
  - **Stamped server-side, after validation, on the single success path** — a first-emit result and a
    retry-emit result are stamped identically.
  - **Never read off the emitted payload** — a model that emits the provenance flag itself does not
    get to launder its own text into looking hand-typed.
  - **Empty is never stamped**, with the shipped reason quoted: provenance for a value that does not
    exist would make the demote-on-edit rule read a lie.
  - **`model_copy`, never a mutation in place, never a model-level validator hook** — the JSONB-baking
    trap.
  - **The D-07 widening, with its reason (T-193.2-03):** after this phase a model authors the
    criterion a model later grades the output against (`JUDGE_RUBRIC_CORE` →
    `answers_business_requirement`). **Format-string injection is NOT possible** — the value is
    substituted *into* `.format()`, never itself formatted. The residual is a **weakened gate**, which
    **D-09 accepts**; refusing to stamp a byte-identical copy of `describe` is the mechanical part of
    the mitigation, because a durable requirement is a HARDER bar than a one-run task instruction.

⚠ **D-08 — the fallback is TODAY'S EXACT BEHAVIOUR and nothing else.** When the model emits nothing,
the field stays blank and the shipped `REQUIREMENT_INVITATION` shows. **No second derive call** (a new
provider call and a new failure surface on every miss). **No schema-required field** (a hard-required
field can fail an otherwise-valid emit and the change ripples to every other emit path). **No new
string.**

⚠ **D-09 — the publish gate is NOT changed.** Do not add a check that distinguishes seeded from
author-confirmed; that would add a genuine second concern to `publish_service.py` and void D-01 for
that file. Pressing Publish is the consent.

⚠ **Do not touch `AUTHORING_SYSTEM_PROMPT` in this plan** — plan 193.2-05 owns it and has already
landed. Do not touch `WF_SCHEMA` or `EMIT_TOOL`.

Commit `feat(193.2-07): stamp the requirement's provenance server-side, refusing empty and refusing a copy of describe (D-06/D-07)`.
  </action>
  <acceptance_criteria>
    - The stamp is a `model_copy` on the single success path, placed before the slug mint; `git diff` shows no mutation-in-place and no model validator added
    - The predicate refuses: empty string, whitespace-only, absent, and a normalised byte-identical copy of `describe`
    - The stamp ignores a provenance claim present in the emitted payload — a model emitting the flag itself cannot set it
    - The comment beside the stamp contains the literal tokens `D-06`, `D-07`, `T-193.2-03`, and a sentence stating format-string injection is not possible and the residual is an accepted weakened gate
    - `git diff --numstat` shows `AUTHORING_SYSTEM_PROMPT`, `WF_SCHEMA` and `EMIT_TOOL` unchanged in this commit, and `backend/app/services/harness/grounding.py` and `publish_service.py` unchanged entirely
    - `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_187_authoring_step_names.py -q` → 0 failed (the shipped stamp is unaffected)
    - The call budget is unchanged: the case added in 193.2-05 asserting exactly one `forced_emit` call still passes
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && ./venv/Scripts/python.exe -m pytest tests/unit/test_187_authoring_step_names.py tests/unit/test_workflow_authoring_requirement.py -q</automated>
  </verify>
  <done>The provenance stamp sits beside its shipped sibling on the single success path, refuses empty and refuses a laundered describe copy, and cannot be set by the model itself.</done>
</task>

<task type="auto">
  <name>Task 3: F-6 and its adversarial half, driven RED against a real plant</name>
  <files>backend/tests/unit/test_workflow_authoring_requirement.py</files>
  <read_first>
    - `backend/tests/unit/test_187_authoring_step_names.py:241-292` — **the exact analog pair**, shipped and green: `test_named_phases_are_stamped_as_ai_seeded` (named → True; blank and absent → False; then the rule restated as a property over the whole definition) and `test_the_stamp_ignores_a_provenance_claim_in_the_emitted_payload` (T-187-02-02, the adversarial half)
    - `backend/tests/unit/test_187_authoring_step_names.py:80-107` — `_partially_named_definition_dict`, the mixed-case fixture idiom producing named / whitespace-only / absent in ONE definition
    - `backend/tests/unit/test_workflow_authoring_requirement.py` as plan 193.2-05 left it — the mock helpers, `STUB_PROVIDER`, and the module docstring's D-08 prohibition
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-VALIDATION.md` §"Fences" row F-6
  </read_first>
  <action>
Extend `backend/tests/unit/test_workflow_authoring_requirement.py` (created by plan 193.2-05) with
the stamp's cases. Model them line for line on the shipped `name_seeded_by_ai` pair.

**F-6 — the empty rule.** One fixture, several stubbed emits: a real non-empty requirement stamps
`True`; `""`, `"   "` (whitespace only) and an ABSENT key each stamp `False`. Then restate the rule
as a property over the definition, exactly as the analog does at its closing assertion — a property
restatement is what catches a branch the enumerated cases missed.

**F-6's positive control, driven RED for real.** Plant `bool(True)` unconditionally into the stamp
predicate, run the file, observe the whitespace case fail, restore with `git checkout --`, re-run
green, record the command, the failing case name, the failure output and an empty
`git diff --numstat` in the summary.

**The adversarial half (T-187-02-02's shape).** Stub an emit whose payload carries the provenance
flag set to `False` alongside a real non-empty requirement, and assert the server stamps `True`
anyway. Then the mirror: a payload claiming `True` with an EMPTY requirement must still stamp `False`.
A model does not get to launder its own text in either direction.

**The D-07 case.** Stub an emit whose `business_requirement` is a byte-identical copy of the
`describe` text passed in, and assert the flag is `False` — with a sibling case proving a
*genuinely different, durable* requirement on the same describe DOES stamp `True`, so the refusal
cannot pass for the wrong reason. Add a normalisation case: a copy differing only by leading and
trailing whitespace or case is still refused.

**D-08, restated where it binds.** Add a case asserting the fallback is byte-identical to today: when
the emit carries no `business_requirement`, the resulting definition's field is `None`/absent and the
flag is `False` — **no substitute string is written server-side**, because the shipped
`REQUIREMENT_INVITATION` is a client placeholder and this path must not grow a second copy of it.
⚠ **Do not write a case asserting the field is always populated** — the module docstring already
forbids it, and the frequency claim belongs to plan 193.2-08 as k/N.

Commit `test(193.2-07): F-6 with its adversarial half and the D-07 describe-copy refusal, driven RED`.
  </action>
  <acceptance_criteria>
    - `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_workflow_authoring_requirement.py -q` → 0 failed
    - Cases exist for: non-empty → True; empty string → False; whitespace-only → False; absent key → False; plus a property restatement over the definition
    - Two adversarial cases exist: a payload claiming `False` with a real value still stamps `True`; a payload claiming `True` with an empty value still stamps `False`
    - Three D-07 cases exist: exact describe copy → False; whitespace/case-differing copy → False; genuinely different durable requirement → True
    - A D-08 case asserts no substitute string is written server-side when the emit carries no requirement
    - The summary records the real `bool(True)` plant driven RED: command, failing case, output, restore, empty `git diff --numstat`
    - `grep -i "always"` finds no assertion in this file claiming the field is always populated
    - `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_187_authoring_step_names.py -q` → 0 failed
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && ./venv/Scripts/python.exe -m pytest tests/unit/test_workflow_authoring_requirement.py tests/unit/test_187_authoring_step_names.py -q</automated>
  </verify>
  <done>The stamp cannot be true for an empty value, cannot be claimed by the model, and cannot be earned by echoing the describe text back — and each of those has been shown to fail against a real plant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| **LLM-emitted payload → `WorkflowDefinition`** | Pydantic `extra="forbid"` is the gate; the provenance flag must NOT be readable off the payload |
| **model-authored `business_requirement` → the judge's rubric** | After this phase a model authors the criterion a model later grades the output against |
| autosave PATCH → the JSONB definition | An undeclared key 422s the PATCH; this is why the mark must be a declared field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| **T-193.2-03** | **Elevation of privilege (weakened gate)** | `publish_service.py:966-972` `JUDGE_RUBRIC_CORE.format(business_requirement=...)` → `validator_kinds.py` `answers_business_requirement` | **accept — RECORDED, not implicit (D-09)** | **Format-string injection is NOT possible** (MEASURED: the value is substituted *into* `.format()`, never itself formatted, so braces are inert). The residual is a **weakened gate**: a trivially-satisfiable AI-authored criterion makes the judge stage easier to pass. **D-09 accepts it** — the author still presses Publish, which `SEED-163` names as the human checkpoint, and D-06's visible mark (plan 193.2-09) means they can see and overrule it. **The mechanical control lands in THIS plan:** the stamp refuses a value that is a normalised byte-identical copy of `describe` (D-07 made mechanical), because a durable requirement is a HARDER bar than a one-run instruction |
| T-193.2-03b | Spoofing | a model emitting the provenance flag in its own payload | mitigate | Stamped server-side after validation, unconditionally, never read off the payload. Two adversarial test cases assert both directions |
| T-193.2-03c | Tampering | the JSONB definition and old rows | mitigate | Additive-optional with a `False` default: old rows `model_validate()` clean. `extra="forbid"` is NOT relaxed — an unknown key still raises. No model validator and no computed field, so nothing is baked into the JSONB |
| T-193.2-01 | Information disclosure | the requirement text itself | accept | Already controlled and unchanged: `business_requirement` renders as text (T-124-01) and the library search highlights it through `HighlightTitle`, which emits JSX text nodes and carries the T-156-01 control. No new render path is added by this plan |
| T-193.2-SC | Tampering | npm/pip/cargo installs | accept | No package installed, no migration added |
</threat_model>

<verification>
- `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_workflow_authoring_requirement.py tests/unit/test_187_authoring_step_names.py -q` → 0 failed
- The six workflow backend suites → 0 failed
- `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit -q` → no new failures vs `193.2-BASELINE.md` §2 (62 failed baseline)
- `ls supabase/migrations/ | wc -l` unchanged
- `git diff --numstat` shows `grounding.py` and `publish_service.py` untouched by this plan
</verification>

<success_criteria>
- One additive-optional provenance boolean beside `business_requirement`, zero migration, `extra="forbid"` intact
- One sibling `model_copy` stamp on the single success path, refusing empty and refusing a normalised describe copy
- F-6 green with its adversarial half and a recorded real-plant RED
- The publish gate, the schema `required` list and the fallback behaviour are all byte-unchanged
</success_criteria>

<output>
Create `.planning/phases/193.2-from-authored-to-runnable/193.2-07-SUMMARY.md` when done.
</output>
