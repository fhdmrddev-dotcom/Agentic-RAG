---
phase: 193.2-from-authored-to-runnable
plan: 01
subsystem: planning-artifacts
tags: [baseline, fences, guardrails, measurement, wave-0]
requires: []
provides:
  - "193.2-BASELINE.md — every pre-change figure the remaining nine plans are judged against"
  - "F-2 RED observation (backend predicate byte-identity fence)"
  - "F-7 RED observation (frontend no-client-sort fence)"
  - "The base SHA plan 193.2-03 runs F-1 against"
  - "The D-01 no-override record and the D-04 WorkflowCard scope fence"
affects:
  - "193.2-03 — F-1 must be scoped to non-comment CODE lines (CORRECTION C-2)"
  - "193.2-03/04/05/06 — the authoritative gate set, and the full-tree figure that is NOT a gate"
  - "D-03 ledger work — FOUR rows owed, not three (CORRECTION C-4)"
tech-stack:
  added: []
  patterns:
    - "plant → observe RED → restore → re-run → observe GREEN → assert empty diff"
    - "corrections recorded BESIDE the prior value, never over it"
key-files:
  created:
    - .planning/phases/193.2-from-authored-to-runnable/193.2-BASELINE.md
  modified: []
decisions:
  - "D-01 honoured: NO guardrail override recorded for Phase 193.2 — third consecutive phase to decline one"
  - "D-04 honoured: WorkflowCard.tsx is READ-only this phase; its G-5 obligation passes forward untouched"
  - "The full frontend tree is NOT a baseline — it is non-deterministic at cap 2 and cannot pass or fail a plan"
metrics:
  duration: ~35 min
  tasks_completed: 2
  commits: 2
  source_files_modified: 0
  completed: 2026-08-15
---

# Phase 193.2 Plan 01: Wave-0 Baselines and Fence Probes Summary

Measured every pre-change figure the remaining nine plans will be judged against, and proved the two
inherited fences this phase's correctness rests on can actually fire — both driven RED against real
plants on the unmoved tree, in commits touching **zero** source files.

## What Was Built

`.planning/phases/193.2-from-authored-to-runnable/193.2-BASELINE.md` (580 lines, seven sections):
base commit + F-1 input, backend baselines, frontend baselines, the two corrections a later plan
must not mistake for its own breakage, the D-01/D-04 guardrail record with seven re-derived
hot-file triples, the reported-bugs cross-check, and the two probe records.

## Task Commits

| Task | Name | Commit | Files |
|---|---|---|---|
| 1 | Measure every baseline at HEAD | `e8b8a0eb` | `193.2-BASELINE.md` (417 lines) |
| 2 | Drive F-2 and F-7 RED, restore, record | `f44d29ec` | `193.2-BASELINE.md` (+163 lines, §7) |

Base SHA: **`c1a6c12261377f2dd6b5c08d3d765111a7e53b59`**. Branch `worktree-agent-a07cc84b1d9cb8ac2`.

## The Two Fence Probes — DRIVEN, not asserted

| Fence | Plant | RED observed | After restore |
|---|---|---|---|
| **F-2** | `created_by = $1` → `created_by= $1` in `list_published_workflows` (`db/workflows.py:290`) | `test_published_predicates_are_byte_identical_to_what_shipped[status = 'published' AND created_by = $1]` at `test_workflows_updated_at.py:295` — **1 failed / 22 passed** | **23 passed**, diff EMPTY |
| **F-7** | `const shown = rows.sort(...)` planted as a non-comment CODE line in `rowIdentity.ts:370` | `T-7 / P-4 — the ROW LIST is never re-ordered` at `rowIdentity.test.ts:175` — **1 failed / 71 passed** | **72 passed**, diff EMPTY |

Both fences can fire. No test file was edited and neither fence was weakened.

## Measured Baselines

| Baseline | Value |
|---|---|
| `backend/tests/unit` full | 62 failed · 2092 passed · 2 xfailed · 2 xpassed (36.84 s) |
| Six backend workflow suites | **0 failed** · 81 passed |
| Five frontend workflow suites | **0 failed** · 337 passed |
| Count gate | **`count gate OK`** · total 3892 · failed 0 · pinned 3868 · 75/75 |
| `tsc -p tsconfig.app.json` | **33 errors** |
| `db/workflows.py` at HEAD | 67205 bytes |

## Deviations from Plan

### Auto-fixed / corrections recorded

**1. [Rule 1 — measurement disagrees with the plan] CORRECTION C-2: `grep -c "ORDER BY name"` is 4, not 3**
- **Found during:** Task 1, §1
- **Issue:** The plan's acceptance criterion asserts the count is `3`. Measured at HEAD it is `4`.
- **Resolution:** Both recorded. `3` is the number of **call sites** (`:316`, `:352`, `:553`) and is
  correct for what the phase is about; `4` is what the raw `grep -c` returns because a **docblock at
  `:344` quotes the clause**. This is the **187-24 trap on its seventh recorded instance** — a module
  that DOCUMENTS a clause reds a raw grep for it.
- **⚠ Consequence for plan 193.2-03:** F-1 is specified in VALIDATION as *"published + draft sources
  contain `ORDER BY updated_at DESC` and **not** `ORDER BY name`"*, and `_starter_source()` is the
  accessor whose docblock carries the prose hit. **F-1 must be verified over non-comment CODE lines**
  (the `rowIdentity.test.ts:99` / `libraryFilter.test.ts:461-468` pattern) or it will fail against a
  comment rather than a clause.

**2. [Rule 3 — blocking discrepancy, resolved by measurement] CORRECTION C-1: the base SHA is not the one the plan asserts**
- **Found during:** the HEAD assertion, before any measurement
- **Issue:** the plan's `<execution_context>` says HEAD MUST report `3cfafa0d` and to STOP otherwise.
  The orchestrator dispatched **`c1a6c122`**.
- **Resolution:** reported rather than absorbed, then **shown to be inert rather than argued to be**.
  The two intervening commits are docs-only, and the one thing that depends on base identity —
  `193.2-03`'s F-1 input — is md5-identical at both SHAs (`2373680e88132362a0a2ffe665b8ac05`).
  `193.2-03` may use either SHA and get the same source.

**3. [Finding] CORRECTION C-4: there are SEVEN hot files, not six — the seventh confirmed**
- CONTEXT D-01's table lists six. `backend/app/models/harness.py` measures **16 commits / 15 phases /
  578 lines** and is absent from BOTH that table and the `CLAUDE.md` ledger. RESEARCH found it and
  `STATE.md:8` records it; this baseline **confirms the figure exactly**. **D-03 owes FOUR ledger
  rows, not three.**

**4. [Finding] CORRECTION C-5: CONTEXT D-01 and D-04 are in tension over `WorkflowCard.tsx`**
- D-01's table counts it as its sixth in-scope file; D-04 rules it out of scope. **D-04 is operative**
  because it is the constraint that binds `files_modified`. Its measurement (`8 / 3 / 818`) is
  recorded under D-04 rather than dropped.

**5. [Finding] CORRECTION C-6: the F-2 test name in VALIDATION matches only two of three cases**
- VALIDATION's F-2 row and fence table say *"the three `*_predicate_is_byte_identical_*` cases"*. The
  published one is **plural and parametrized** — `test_published_predicates_are_byte_identical_to_what_shipped`
  (two arms). **Confirmed by the observed failure id**, not by reading the file.

**6. [Finding — the most consequential] VALIDATION's frontend full-tree figure of 26 is wrong, and the metric itself is not a baseline**
- **Measured: 49 failed (run 1), then 46 failed (run 2), on ONE identical commit with no source file
  touched between them.** VALIDATION quotes 26. The backend 62 reproduces exactly; the frontend 26
  does not.
- **⚠ The failing set includes suites that PASS in isolation.** `WorkflowsPage.test.tsx` shows 12
  failing cases in the full tree, yet it is one of the five named workflow suites that measured
  **337 passed / 0 failed** minutes earlier, and `WorkflowCard.test.tsx` is inside the count gate,
  which measured **`failed 0`**.
- **⇒ The full-tree number cannot pass or fail a plan.** Failing filenames were captured before the
  second run per the project rule, and are recorded in §4. Every plan in this phase gates on: count
  gate `failed 0` · the five frontend suites `0 failed` · the six backend workflow suites `0 failed`
  · `tsc -p tsconfig.app.json` = 33.

### Structural note

Both tasks write the same artifact, so Task 1 committed §1–§6 and Task 2 committed §7. The **probes
physically ran on the unmoved tree before the artifact was authored** — the ordering the plan requires
— and the commit sequence reflects the plan's task decomposition, not the work order.

### The wrong-base assertion fired again

This worktree arrived at `fda79214`, not the dispatched base, and was corrected by the
`<worktree_branch_check>` reset **before any measurement was taken**. That is the 19th occurrence
project-wide. Load-bearing here specifically: this plan's entire output is baselines, and a baseline
captured on the wrong tree proves nothing.

## Guardrail Record

- **D-01 — NO guardrail override recorded for Phase 193.2.** Verified at `STATE.md:51`. Third
  consecutive phase (193, 193.1, 193.2) offered one and declining it. The absence is a measurement.
- **D-04 — `WorkflowCard.tsx` appears in NO plan's `files_modified` this phase.** G-5 fires on it
  (3 phases ≥ 3) and its inherited obligation **passes forward untouched**; the next phase still
  inherits `8 / 3 / 818` and still owes the refactor recommendation FIRST.
- **Five of CONTEXT D-01's six figures reproduce EXACTLY.** The sixth (`WorkflowsPage.tsx`) confirms
  D-01's own finding that the `CLAUDE.md` cell is stale for the fourth time (`33/12/1160` →
  `34/12/1176`).

## Reported-Bugs Cross-Check

| Bug | folded_into | Covered by |
|---|---|---|
| `BUG-260815-01` (blocking) | `"193.2"` ✅ | `193.2-05` (prompt clause) + `193.2-06` (refusal rewrite) |
| `BUG-260815-02` (blocking) | `"193.2"` ✅ | `193.2-03` (server `ORDER BY`) + `193.2-04` (rendered position) |

Both confirmed by `grep -n folded_into` and by reading each covering plan's objective.

## Known Stubs

None. This plan produces a documentation artifact and modifies no source.

## Threat Flags

None. `T-193.2-01-A` (the temporary plants) was mitigated as the register requires: every plant
restored with `git checkout -- <named file>`, `git diff --numstat HEAD -- backend frontend` EMPTY,
and both commits contain exactly one file under `.planning/`. **No blanket `git clean`,
`git reset --hard` or `git checkout -- .` was used** — each restore named its single file.

## Verification

| Check | Result |
|---|---|
| `git diff --numstat HEAD -- backend frontend` | **EMPTY** |
| `git status --porcelain` | **EMPTY** |
| `git show --stat` both commits | one file each, both `.planning/…/193.2-BASELINE.md` |
| `pytest test_workflows_updated_at.py test_publish_service.py -q` | **51 passed · 0 failed** |
| `node scripts/vitest-count-gate.cjs` | **`count gate OK`** · total 3892 · failed 0 |

## Self-Check: PASSED

- `.planning/phases/193.2-from-authored-to-runnable/193.2-BASELINE.md` — FOUND (580 lines)
- Commit `e8b8a0eb` — FOUND
- Commit `f44d29ec` — FOUND
- `NO guardrail override` literal in §5 — present
- `D-01` and `D-04` literal tokens in §5 — present
- No modifications to `STATE.md` or `ROADMAP.md` — confirmed
