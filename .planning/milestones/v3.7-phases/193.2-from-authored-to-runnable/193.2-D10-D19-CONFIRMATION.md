---
phase: 193.2
plan: 02
decisions: [D-10, D-19]
confirmed: 2026-08-15
head_sha: fa83585e15091404b80874db3d8e1960bf002084
head_sha_short: fa83585e
tree_state: UNMOVED at capture (zero source files modified by this task)
---

# Phase 193.2 — D-10 and D-19 Confirmed at HEAD

> **This is a confirmation-and-record wave, not a discovery wave.** RESEARCH answered D-10 with HIGH
> confidence across nine hops plus the live audit ledger. This artifact's job is to (1) **re-run** that
> evidence at HEAD so no later plan re-derives or mis-quotes it, and (2) write the two rulings that
> bind plans `193.2-06` and `193.2-10` down as durable text.
>
> **Where a measurement disagrees with RESEARCH or CONTEXT, the corrected value is recorded BESIDE
> the previous one, never over it** — this repository's standing habit, and the habit that let
> `193.2-01` catch six corrections rather than inherit them.
>
> ⚠ **This plan observed NO PIXELS.** Everything below is a database read, a source read, or a test
> run. What the operator actually SAW is `OQ-1`/`OQ-2` and is owed to `193.2-10` as driven UAT — see §5.

---

## §1 — D-10, the ledger re-run

Re-run against the live local Postgres at `127.0.0.1:54322` using
`backend/venv/Scripts/python.exe` + `psycopg2` (the evidence-tools recipe this repo already uses).

### Query 1 — every `event_type` on 2026-08-15

```sql
select event_type, count(*), min(created_at), max(created_at)
from harness_audit where created_at >= '2026-08-15' group by event_type
```

Result, **verbatim**:

```
('emit_forced',       2, 2026-08-15 02:05:56.368554+00:00, 2026-08-15 02:10:48.642797+00:00)
('emit_rendered',     2, 2026-08-15 02:06:41.663481+00:00, 2026-08-15 02:11:20.791284+00:00)
('emit_validated',    2, 2026-08-15 02:06:22.151663+00:00, 2026-08-15 02:11:09.600501+00:00)
('gate_failed',       2, 2026-08-15 02:09:21.424236+00:00, 2026-08-15 02:09:47.142392+00:00)
('gate_passed',       6, 2026-08-15 02:04:58.704514+00:00, 2026-08-15 02:10:48.575655+00:00)
('judge_verdict',     1, 2026-08-15 02:07:02.335213+00:00, 2026-08-15 02:07:02.335213+00:00)
('phase_completed',   8, 2026-08-15 02:04:58.727405+00:00, 2026-08-15 02:11:20.804099+00:00)
('phase_started',     8, 2026-08-15 02:04:30.298230+00:00, 2026-08-15 02:10:48.600989+00:00)
('phase_transition',  6, 2026-08-15 02:04:58.735753+00:00, 2026-08-15 02:10:48.593397+00:00)
('publish_attempted', 1, 2026-08-15 02:06:41.880888+00:00, 2026-08-15 02:06:41.880888+00:00)
('publish_succeeded', 1, 2026-08-15 02:07:02.351311+00:00, 2026-08-15 02:07:02.351311+00:00)
('run_completed',     2, 2026-08-15 02:06:41.700090+00:00, 2026-08-15 02:11:20.818201+00:00)
```

| Event on 2026-08-15 | Count |
|---|---|
| `publish_attempted` | **1** |
| `publish_succeeded` | **1** |
| **`publish_blocked`** | **0 — the row is ABSENT from the result set entirely** |

✅ Reproduces RESEARCH R1 exactly.

### Query 2 — every `publish_blocked` row, most recent 25

```sql
select created_at, metadata->>'blocked_stage' from harness_audit
where event_type='publish_blocked' order by created_at desc limit 25
```

Result, **verbatim** — and ⚠ **the second column is `None` on every single row**:

```
(2026-08-07 19:39:11.385404+00:00, None)
(2026-08-07 19:34:52.244134+00:00, None)
(2026-08-07 19:29:41.820577+00:00, None)
(2026-08-01 17:28:18.707135+00:00, None)
(2026-08-01 17:06:16.326261+00:00, None)
(2026-08-01 16:59:52.370147+00:00, None)
(2026-07-31 14:35:01.281247+00:00, None)
(2026-07-31 13:57:44.527572+00:00, None)
(2026-07-30 20:06:02.267426+00:00, None)
(2026-07-30 19:59:58.768775+00:00, None)
(2026-07-30 19:48:08.657314+00:00, None)
(2026-07-30 13:55:15.872949+00:00, None)
(2026-06-27 20:58:07.382495+00:00, None)
(2026-06-27 17:55:18.475569+00:00, None)
(2026-06-27 17:48:54.766090+00:00, None)
(2026-06-27 17:44:40.480476+00:00, None)
(2026-06-27 17:44:11.443773+00:00, None)
(2026-06-27 17:43:57.693162+00:00, None)
(2026-06-27 17:43:48.595268+00:00, None)
(2026-06-17 21:08:23.853893+00:00, None)
(2026-06-14 21:43:41.175847+00:00, None)
(2026-06-14 17:00:23.630788+00:00, None)
(2026-06-14 16:31:38.670829+00:00, None)
(2026-06-14 15:15:29.169462+00:00, None)
(2026-06-14 14:17:43.853578+00:00, None)
```

**Most recent `publish_blocked` anywhere in the database: `2026-08-07 19:39:11.385404+00:00`.**
`select count(*) … where event_type='publish_blocked' and created_at >= '2026-08-15'` → **0**.
`select count(*) … where event_type='publish_blocked'` → **32** (all 32 attributable to the operator —
see the correction below).

---

### ⚠ CORRECTION C-7 — `metadata` is a DOUBLE-ENCODED jsonb STRING, so `metadata->>'blocked_stage'` returns NULL on all 32 rows

RESEARCH R1 states the second query returns *"the most recent `publish_blocked` anywhere in the
database is 2026-08-07 19:39:11 (`blocked_stage: structural_gate`)"*.

**The VALUE is correct. The COMMAND credited with producing it cannot produce it**, and both facts
are recorded rather than one overwriting the other:

```sql
select jsonb_typeof(metadata), count(*) from harness_audit
where event_type='publish_blocked' group by 1
→  ('string', 32)          -- NOT 'object'. Every row.

select distinct jsonb_object_keys(metadata) from harness_audit where event_type='publish_blocked'
→  psycopg2.errors.InvalidParameterValue: cannot call jsonb_object_keys on a scalar
```

`metadata` holds a jsonb **string scalar whose text is itself a JSON document**. `->>'key'` on a
scalar is NULL by definition, so the accessor silently answers `None` for a key that is demonstrably
present in the payload. **The working accessor is `metadata #>> '{}'` followed by a JSON parse:**

```python
json.loads(row_text)["blocked_stage"]   # after: select metadata #>> '{}' from harness_audit …
```

⚠ **This is the jsonb string-scalar trap on a new surface.** This project already records it for
`definition->'phases'` (which "silently returns 0 rows"); here it silently returns NULL for every
key on every `publish_blocked` receipt. **A NULL that reads like "the field is empty" is
indistinguishable from "you asked the wrong way"** — which is precisely the class of failure this
phase exists to fix in user-facing copy, appearing in our own diagnostics.

**Re-run through the working accessor** — `created_at` + `blocked_stage` only (T-193.2-02: no
`definition_id`, no `golden_run_id`, no `named_failures`, no user id, no email; the raw payloads
contain multi-kilobyte judge prose and UUIDs and are deliberately NOT reproduced here):

| # | `created_at` | `blocked_stage` |
|---|---|---|
| 1 | 2026-08-07T19:39:11.385404+00:00 | **`structural_gate`** |
| 2 | 2026-08-07T19:34:52.244134+00:00 | `judge` |
| 3 | 2026-08-07T19:29:41.820577+00:00 | `judge` |
| 4 | 2026-08-01T17:28:18.707135+00:00 | `draft_changed` |
| 5 | 2026-08-01T17:06:16.326261+00:00 | `golden_run_error` |
| 6 | 2026-08-01T16:59:52.370147+00:00 | `structural_gate` |
| 7 | 2026-07-31T14:35:01.281247+00:00 | `judge` |
| 8 | 2026-07-31T13:57:44.527572+00:00 | `judge` |
| 9 | 2026-07-30T20:06:02.267426+00:00 | `judge` |
| 10 | 2026-07-30T19:59:58.768775+00:00 | `structural_gate` |
| 11 | 2026-07-30T19:48:08.657314+00:00 | `structural_gate` |
| 12 | 2026-07-30T13:55:15.872949+00:00 | `structural_gate` |
| 13 | 2026-06-27T20:58:07.382495+00:00 | `judge` |
| 14–19 | 2026-06-27T17:43:48 … 17:55:18 | `business_requirement` (×6) |
| 20 | 2026-06-17T21:08:23.853893+00:00 | `judge` |
| 21 | 2026-06-14T21:43:41.175847+00:00 | `judge` |
| 22–24 | 2026-06-14T15:15:29 … 17:00:23 | `structural_gate` (×3) |
| 25 | 2026-06-14T14:17:43.853578+00:00 | `business_requirement` |

**Stage tally over all 32 rows:**
`structural_gate` 11 · `judge` 10 · `business_requirement` 7 · `golden_run_error` 2 ·
`draft_changed` 1 · `already_published` 1.

⇒ **RESEARCH's `structural_gate` reading for the 2026-08-07 row is CONFIRMED.** Only the accessor in
its stated command is wrong, and it is corrected here so the next reader who runs that exact SQL is
not told `None` and does not conclude the field was never written.

### ⚠ CORRECTION C-8 — the "twelve rows for this same user" figure is 32, not 12

RESEARCH's `_safe_audit` caveat argues the writer works because *"twelve `publish_blocked` rows exist
for this same user on earlier dates"*. Measured:

```sql
select count(*) from harness_audit ha join auth.users u on u.id = ha.user_id
where ha.event_type='publish_blocked' and u.email='<the operator>'
→ 32          -- i.e. ALL 32 publish_blocked rows in the database are the operator's
… and ha.created_at >= '2026-08-15'
→ 0
```

The correction runs **in the same direction as the argument and strengthens it**: the receipt writer
is demonstrably functional for this exact user, on this exact event type, **32 times**, across six
distinct dates and six distinct `blocked_stage` values — and it wrote **nothing** on 2026-08-15.

---

### The conclusion, in this artifact's own words

`publish_attempted` is written **only at stage 3**, after the golden-run row exists so the receipt
carries a real `run_id`. **Every pre-run gate — stages 0, 1, 2, 2.5 and 2.6 — writes
`publish_blocked`** keyed to a NULL `run_id`. Verified at HEAD in the module's own docblock
(`publish_service.py:48-50`) and in the two writers (`:305` `event_type="publish_attempted"`,
`:455` `event_type="publish_blocked"` inside `_block`, `:444`). **Stage 2.5 IS the interactive
block** (`publish_service.py:190-208`).

⇒ **THE PUBLISH ENDPOINT NEVER REFUSED ANYTHING ON THE OPERATOR'S SESSION.** It was called exactly
once and it succeeded.

⚠ **The caveat, stated as a caveat rather than buried.** `_safe_audit` (`publish_service.py:471+`)
**swallows write failures**. So "zero `publish_blocked` rows" is **strong evidence, not proof** — a
receipt that failed to write leaves no trace by design.

**The independent positive half does not depend on `_safe_audit` at all:**

| Fact | Value |
|---|---|
| `publish_attempted` rows on 2026-08-15 | exactly **1**, at `02:06:41.880888` |
| `publish_succeeded` rows on 2026-08-15 | exactly **1**, at `02:07:02.351311` |
| Newest `published` row's `updated_at` | `2026-08-15T02:07:02.343741` |
| Gap between them | **7.57 ms** — the same act, not two |

A blocked publish writes no `publish_succeeded` and mints no published row. **A published row exists,
dated to the millisecond of the success receipt.** That is a positive observation of the write path,
and it holds even if one imagines every `publish_blocked` receipt silently failing.

---

## §2 — D-10, the ruling

Three consequences the rest of the phase inherits.

### (1) **D-10's escape hatch does NOT trigger**

D-10's own sentence, quoted verbatim and marked rather than deleted:

> ⚠ **If the gate is silently not firing, that is the real defect and rewriting the string fixes
> nothing.**

**NOT TRIGGERED.** The gate is not silently broken — it fired, greyed `publish-trigger`, and printed
the server's literal beside it. The premise that was false is neither of the two CONTEXT facts: it is
the unstated third premise that **a 400 was received at all**. D-10 frames the question as *"one of
those two facts is false on the live path"*; measured, **both are TRUE** (§3), and the 400 never
happened.

⇒ **Rewriting the string IS the fix, and `D-11` + `D-13` are the whole of it.** The defect
`BUG-260815-01` reports is real and unchanged in severity; it is simply **located on the canvas**,
not behind a publish click.

### (2) **The surface is the Builder header ⇒ D-13's action is a PRECISE INSTRUCTION, never a live control**

Measured at HEAD:

| Element | Location | Measured |
|---|---|---|
| The message node | `PublishGauntlet.tsx:932-940` | `<span id={blockedReasonId} data-testid="publish-blocked-reason" className="ml-2 text-[12px] leading-snug text-muted-foreground">` |
| Its host slot | `WorkflowBuilderPage.tsx:2147` | `<div className="flex shrink-0 items-center gap-2">{actionGroup}</div>` |
| The control it sits beside | `PublishGauntlet.tsx:917-931` | `data-testid="publish-trigger"`, `disabled={blocked}`, label `◆ Publish…` |
| The removal affordance that ALREADY ships | `PlaneEditingLayer.tsx:256` | `aria-label={\`Remove step ${position + 1}\`}` |

⇒ **`D-13`'s "offers an action" ships as a `precise instruction`, NOT a live control.** Three reasons,
each mechanical rather than stylistic:

1. **A control would exist on only ONE of the two surfaces the single string feeds**, which `D-12`
   forbids outright ("any plan that writes a second string for the canvas is wrong"). The literal is
   consumed verbatim by the publish refusal AND by `/validate` → `blockedReason` → the canvas.
2. **It would be dead weight on the publish-refusal render**, which has no canvas beneath it.
3. **The canvas already ships the affordance one click away**, with an inline Undo
   (`WorkflowBuilderPage.tsx:1275-1287`). A second remove button in the header would be a canvas
   affordance rendered outside the canvas.

**This settles the CONTEXT "Claude's Discretion" item** — *"whether the removal action in D-13 is a
live control or a precise instruction — decide from what D-10 measures about which surface the author
is actually looking at"*. It is measured, and the answer is **precise instruction**. Plan `193.2-06`
inherits this as a constraint, not a suggestion.

### (3) **The length ceiling is a hard UI constraint — RE-DERIVED, not copied**

Both numbers were obtained by **importing the constants and calling `len()`**, never by trusting
RESEARCH's figures:

```python
from app.services.harness.grounding import BUSINESS_REQUIREMENT_MISSING_MESSAGE
from app.services.harness import publish_service as ps
msg = ps._interactive_phase_failures(<one llm_human_input phase>)[0]["message"]
```

| Constant | Measured `len()` | RESEARCH's figure | Agreement |
|---|---|---|---|
| The interactive message | **105** | 105 | ✅ exact |
| `BUSINESS_REQUIREMENT_MISSING_MESSAGE` | **99** | 99 | ✅ exact |

Verbatim, as measured:

```
'interactive phases (llm_human_input / ask_user dispositions) cannot be validated in a synchronous publish'
'Add the Business requirement — one line saying what this workflow must deliver — before publishing.'
```

Both fit a `text-[12px] leading-snug` span in a `shrink-0` slot. **A rewritten message carrying a step
label should aim at the same order of magnitude** — a 250-character sentence in that slot is a layout
regression this phase would be shipping blind.

⚠ Also measured, and it bounds `193.2-06`'s implementation: `_interactive_phase_failures` returns
findings whose keys are exactly `['message', 'phase']`, **one finding per phase**
(`publish_service.py:531` and `:541` — `continue` / `break`, each commented *"one finding per phase is
enough"*). `phase` carries the **slug**. The human-readable `name` is on the phase object being
iterated (`publish_service.py:520-522` reads `slug` and `config` off it) — so `D-13`'s label is
reachable without changing the function's signature, and `D-13`'s slug-leak guard is a real hazard
because **the slug is the only identifier the finding carries today.**

---

## §3 — D-10, the nine hops re-verified at HEAD

### Mechanically, where a test already owns them

```
backend/venv/Scripts/python.exe -m pytest backend/tests/unit/test_187_route_assigned_reach.py -q
→ 7 passed, 1 warning in 0.22s
```

```
cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowBuilderPage.canvas.test.tsx
→ Test Files 1 passed (1) · Tests 133 passed (133) · Duration 46.32s
```

The shipped pin, **by name**, isolated with `-t "187-28" --reporter=verbose`:

```
✓ WorkflowBuilderPage 187-28 — a route-assigned verdict GATES the Publish control
    > an ok:false whose SOLE verdict is an INCOMPLETE route-assigned code disables Publish   620ms
    > CONTROL A — a MIXED list still leads with the error, so incompletes are not merely 'first'  589ms
    > CONTROL B — ok:true with ZERO verdicts leaves Publish ENABLED on the very same mount    568ms
    > the SERVER gate is a different surface — the client shows a reason, it does not judge     1ms
  Tests 4 passed | 129 skipped (133)
```

✅ **The pin passes at HEAD, and it ships with its own two controls** (A: a mixed list still leads
with the error; B: `ok:true` leaves Publish ENABLED on the same mount). A gating assertion whose
sibling proves the control is enabled in the negative case cannot pass vacuously.

### The hops no test owns — `file:line` and the one-line reading

| # | Hop | `file:line` at HEAD | Reading | Verdict |
|---|-----|---|---|---|
| 1 | `/validate` mints `interactive_phase` | `backend/app/api/workflows.py:894-902` | `findings.extend({"code": "interactive_phase", …} for f in publish_service._interactive_phase_failures(body))` | **TRUE** |
| 2 | `ok` is false for *any* verdict | `workflows.py:950` | `return ValidateResponse(ok=(len(verdicts) == 0), verdicts=verdicts)` | **TRUE** |
| 3 | `interactive_phase` ∈ `_ROUTE_ASSIGNED_CODES` | `workflows.py:689` | listed | **TRUE** |
| 4 | `interactive_phase` ∈ `_INCOMPLETE_CODES` | `workflows.py:700` | listed | **TRUE** |
| 5 | The Builder runs the loop on a fresh draft | `WorkflowBuilderPage.tsx:988` | `useLiveValidation(definition, hasEdited \|\| (canvasEnabled && phases.length > 0))` — a generated draft has `phases > 0`, so the loop is enabled on OPEN without an edit | **TRUE** |
| 6 | `canvasEnabled` is on in the operator's env | `WorkflowBuilderPage.tsx:266-273` + live `app_settings.feature_visibility` | code default is `"off"`, live flag is `everyone` — RESEARCH queried it rather than assuming | **TRUE (inherited from RESEARCH; not re-queried here)** |
| 7 | `blockedReason` returns the server message verbatim | `WorkflowBuilderPage.tsx:1338` (`useMemo`), `:1343` (`isCheckOutstanding` → `DEGRADED_SENTENCE["not-run"]`), `:1347` (`.find(v => v.severity !== "incomplete") ?? validation.verdicts[0]`) | the `??` half is how an incomplete-only list reaches the header | **TRUE** |
| 8 | The message greys the control | `PublishGauntlet.tsx:846` (`const blocked = typeof blockedReason === "string" && blockedReason.trim().length > 0`), `:921` (`disabled={blocked}`), `:935` (`data-testid="publish-blocked-reason"`) | | **TRUE** |
| 9 | A pinned test proves the incomplete-only shape | `WorkflowBuilderPage.canvas.test.tsx` — the `187-28` describe | 4 cases, all green above | **TRUE** |

### ⚠ CORRECTION C-9 — the `/validate` mint block is `:894-902`, stated BESIDE CONTEXT's `:895-903`

`193.2-CONTEXT.md:158` says the call sits at `backend/app/api/workflows.py:895-903`. **Measured at
HEAD it is `:894-902`** — one line earlier. Both values are recorded; RESEARCH already flagged the
same off-by-one and this measurement **confirms RESEARCH rather than correcting it**. The derivation:

```
894:    # (4) the WR-04 interactive-phase pre-run block — reused verbatim (one finding per phase).
895:    findings.extend(
…
901:        for f in publish_service._interactive_phase_failures(body)
902:    )
```

⚠ Two more line references are recorded as measured rather than quoted:
`publish-blocked-reason`'s `data-testid` is at **`:935`** (RESEARCH gives the range `:932-939`, which
contains it — no conflict), and `blockedReason`'s `useMemo` opens at **`:1338`** with the verbatim-
message expression at **`:1347`** (CONTEXT cites `:1341`; the `useMemo` spans both, so CONTEXT points
inside the right block at the wrong line).

---

## §4 — D-19, the CTA verdict

### What was re-verified, cheaply

**(a) The coverage measurement — RE-RUN BEFORE this plan's Task 2 wrote a line.**

```bash
grep -rn "run-cta\|runCta\|Ready to run it" frontend/src --include=*.test.tsx --include=*.test.ts
→ (no output)
grep -c "run-cta" frontend/src/pages/WorkflowsPage.test.tsx
→ 0
```

✅ **ZERO. Confirmed at HEAD.** The one surface `D-19` treats as a suspect is defended by nothing.
WR-04 already fixed one silent drop on it and **no test would have caught the next one.**

**(b) The lookup runs against `rows` (unfiltered), not `visibleRows`.**

`WorkflowsPage.tsx:861-863`, verbatim:

```ts
const runCtaRow = runCta
  ? rows.find((row) => row.slug === runCta.slug && row.provenance !== "draft")
  : undefined
```

✅ Confirmed. **No chip, no search string and no project selection can hide the banner** — it reads
the merged list, not the filtered one. The file's own comment at `:858-860` records why the
`provenance !== "draft"` half is load-bearing: *"a Tweak fork carries the SAME slug as the published
row it forked, so a slug match alone can land on a draft — and a CTA that offers to Run a draft
offers something the product refuses by design."*

### The honest verdict against D-19's two acceptable outcomes

`D-19` offers two branches: *"it fires reliably and SC#3 is met by the sort alone"*, or *"it does not
and that is a named defect this phase fixes"*.

⚠ **NEITHER BRANCHES CLEANLY, and that is stated rather than forced into one of them.**

| Property | Measured | Where |
|---|---|---|
| **It is wired on the operator's path** | `WorkflowsPage` HOSTS the Builder (`if (pageView === "builder")`); `PublishGauntlet`'s `onPublished` is `onGauntletPublished` | `:721-729`, `:806-839` |
| **It names the workflow** | *"Published **{name}** v{version}. Ready to run it."* | `:903` |
| **WR-04's fix is present** | `_def.slug` with a `builderInitial` fallback, not the old hardcoded `"workflow"` | `:830-834` |
| **It is EPHEMERAL** | `runCta` is plain `useState` on the page; navigating to another home unmounts it for good | `:282` |
| **It is SELF-DISMISSING** | its own `▶ Run now` calls `setRunCta(null)` | `:910` |
| **It is NOT A LOCATION** | it does not scroll to, highlight, or navigate to the row | `:897-917` — no such call exists |
| **It is DELAYED, not lost** | before `refetchPublished()` lands the row is still in `drafts` and excluded, so `runCtaRow` is `undefined`; `runCta` is not cleared by the refetch | `:861-863` |

⇒ **The CTA solves *"run it right now"*. It does not solve *"find it again"*, which is the second
half of SC#3 and precisely what the operator asked for.** Two different jobs; only the first was
built.

### ⇒ THE RULING

**`D-19` resolves as: it fires. SC#3 is met by the sort (plan `193.2-03`) plus ONE automated pin so
its next silent drop is caught (plan `193.2-02` Task 2).**

⚠ **NO NAVIGATE-TO-ROW BEHAVIOUR IS BUILT. `WorkflowsPage.tsx` IS NOT MODIFIED BY THIS PLAN.**

The reason is G-5, and the counts are **re-derived at HEAD** rather than read off the ledger (which
`193.2-BASELINE.md` §5 records as stale for the fourth time):

```
git log --oneline -- frontend/src/pages/WorkflowsPage.tsx | wc -l   → 34
wc -l frontend/src/pages/WorkflowsPage.tsx                          → 1176
```

| Measure | Value | Source |
|---|---|---|
| commits | **34** | re-derived at HEAD `fa83585e` |
| phases | **12** | `193.2-BASELINE.md` §5, reproduced exactly |
| lines | **1176** | re-derived at HEAD |
| `CLAUDE.md` ledger cell | `33 / 12 / 1160` — **STALE, 4th time** | `193.2-BASELINE.md` CORRECTION C-3 |

**G-5 fires on this file and CONTEXT `D-19` names navigate-to-row a genuine SECOND concern.** Under
`D-02` ("the no-second-concern test must be PROVED per file, not asserted"), adding a scroll-to /
highlight / navigate behaviour would void `D-01` for this file and owe a refactor recommendation
FIRST. **This plan therefore adds coverage and not behaviour** — its `files_modified` contains the
test file and this artifact, and `git diff` on `WorkflowsPage.tsx` must be EMPTY.

Two supporting measurements make the "do nothing more" call cheap rather than merely disciplined:
after `193.2-03` the newest published row lands at **rendered position 4 of 109** (RESEARCH R2), and
**no filter has to be cleared to see it** — `query` defaults `""`, `activeChips` defaults `[]`,
`selectedProjectId` defaults `null`.

---

## §5 — What is routed to UAT rather than automated

⚠ **THIS PLAN OBSERVED NO PIXELS, AND SAYS SO PLAINLY.** Everything in §1–§4 is a database read, a
source read, or a test run. The audit ledger proves the **publish endpoint** never refused; it
**cannot prove what was on screen**, because `/validate` writes no audit row at all.

| ID | Question | Why it cannot be automated here | Owner |
|---|---|---|---|
| **OQ-1** | Did the operator actually SEE the greyed control and the message, or did they see the message somewhere else entirely? | Requires a driven browser cycle against a real describe→generate→canvas path. No audit row exists for `/validate` | **`193.2-10`**, UAT row **U2** |
| **OQ-2** | Does the post-publish CTA appear at all, and after how long? | The refetch race is source-visible; its **latency** is not. Zero automated coverage existed before Task 2, and a jsdom test cannot measure wall-clock hand-off | **`193.2-10`**, UAT row **U3** |

**The Chrome MCP recipe both rows must use** (recorded here so `193.2-10` inherits it rather than
rediscovering it):

- ⚠ **`take_screenshot` TIMES OUT on this box.** Read DOM state via `evaluate_script` instead.
- **OQ-1:** capture `document.querySelector('[data-testid="publish-blocked-reason"]').textContent`
  and `document.querySelector('[data-testid="publish-trigger"]').disabled`. Then re-run
  `select count(*) from harness_audit where event_type='publish_blocked' and created_at > now() - interval '10 minutes'`
  and confirm it stays **0** — which is the half that closes the loop back to §1.
- **OQ-2:** timestamp the click and the first appearance of `[data-testid="run-cta"]`. Appearing in
  < 1 s confirms the ruling in §4; never appearing would be a named defect.

⚠ **When `193.2-10` runs that `publish_blocked` re-check, it must use `metadata #>> '{}'` + a JSON
parse if it inspects `blocked_stage` — see CORRECTION C-7.** The `count(*)` half is safe either way
because it keys on `event_type`, a real column.

---

## Summary — what a later plan may rely on

| Claim | Value | §  |
|---|---|---|
| `publish_blocked` rows on 2026-08-15 | **0** | §1 |
| `publish_attempted` / `publish_succeeded` on 2026-08-15 | exactly **1** each, 7.57 ms before the published row's `updated_at` | §1 |
| Most recent `publish_blocked` anywhere | **2026-08-07 19:39:11**, `structural_gate` | §1 |
| The publish endpoint refused anything that day | **NO** | §1 |
| `_safe_audit` caveat | **named** — evidence, not proof; the positive half is independent | §1 |
| **D-10's escape hatch** | **NOT TRIGGERED** — rewriting the string IS the fix | §2 |
| **D-13's action** | a **precise instruction**, never a live control | §2 |
| Interactive message length | **105** (re-derived by `len()`) | §2 |
| `BUSINESS_REQUIREMENT_MISSING_MESSAGE` length | **99** (re-derived by `len()`) | §2 |
| `test_187_route_assigned_reach.py` | **7 passed** | §3 |
| The `187-28` canvas pin | **4 passed**, with its own two controls | §3 |
| The `/validate` mint block | **`:894-902`** (CONTEXT says `:895-903`) | §3 |
| CTA test coverage BEFORE Task 2 | **0 matches** | §4 |
| `runCtaRow` reads | **`rows`** (unfiltered), not `visibleRows` | §4 |
| **D-19** | it fires; SC#3 = the sort + one pin. **NO navigate-to-row** | §4 |
| `WorkflowsPage.tsx` G-5 | **34 / 12 / 1176** re-derived; fires; **not modified by this plan** | §4 |
| Pixels observed by this plan | **NONE** — OQ-1/OQ-2 owed to `193.2-10` | §5 |

**New corrections raised by this plan:** **C-7** (the double-encoded jsonb `metadata`), **C-8** (32
operator `publish_blocked` rows, not 12), **C-9** (`:894-902`, stated beside CONTEXT's `:895-903`).

*Confirmed 2026-08-15 · Phase 193.2 Plan 02 Task 1 · HEAD `fa83585e` · zero source files modified*
