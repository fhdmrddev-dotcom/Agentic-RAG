---
phase: 193.2-from-authored-to-runnable
plan: 03
type: execute
wave: 3
depends_on: ["193.2-01", "193.2-02"]
files_modified:
  - backend/app/db/workflows.py
  - backend/tests/unit/test_workflows_updated_at.py
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "D-15 — `list_published_workflows` and `list_draft_workflows` order by `updated_at DESC`; the column is already on the wire and already consumed by `relativeChanged`'s nine bands, so the card's 'changed <rel>' text and the list order agree by construction rather than by discipline"
    - "D-16 — starters STAY `ORDER BY name`; the divergence from `BUG-260815-02`'s change-them-together warning is deliberate, and it is written into the code at ALL THREE sites so a later reader finds a recorded decision rather than an inconsistency"
    - "D-02 — `db/workflows.py` gains no SECOND concern: three list feeds that already ORDER their own rows get a different ORDER BY on two of them. An ordering change to an ordering clause"
    - "F-1 — the shipped 192.1 scope fence `test_no_feed_orders_by_updated_at` is REWRITTEN IN PLACE as the D-16 divergence fence, never deleted, with the old reasoning recorded BESIDE the new; it is observed RED against the pre-change source before the change lands"
    - "F-2 — every WHERE predicate is byte-identical to what shipped; on a service-role pool that bypasses RLS the predicate IS the security boundary, and no predicate is tidied while a sort key changes"
    - "The shared-consumer consequence is stated: `list_published_workflows` also feeds the composer Harness picker, WorkspacePanel's run-soul and threads.py kickoff, and all three move from alphabetical to recency"
  artifacts:
    - path: "backend/app/db/workflows.py"
      provides: "Recency ordering on the two feeds holding the author's own work; the D-16 divergence recorded at all three sites"
      contains: "ORDER BY updated_at DESC"
    - path: "backend/tests/unit/test_workflows_updated_at.py"
      provides: "F-1, the rewritten divergence fence, with its positive control and the superseded reasoning kept beside"
      contains: "D-16"
  key_links:
    - from: "backend/app/db/workflows.py"
      to: "frontend/src/components/workflows/library/relativeChanged.ts"
      via: "the same `updated_at` column ordering the list and painting the card's 'changed <rel>' segment"
      pattern: "updated_at"
    - from: "backend/tests/unit/test_workflows_updated_at.py"
      to: "backend/app/db/workflows.py"
      via: "inspect.getsource source fences over all three feed functions"
      pattern: "ORDER BY updated_at DESC"
---

<objective>
`BUG-260815-02` (blocking): a just-published workflow is unfindable. The AI names it, three feeds sort
`ORDER BY name`, and nothing anywhere sorts by recency. Change the two feeds that hold the author's
own work to `ORDER BY updated_at DESC`; leave the curated starters shelf alphabetical.

Purpose: SC#3's server half. Fully independent of the prompt and refusal work, smallest blast radius
in the phase, and it unblocks the measurable half of findability immediately.

⚠ **The single largest planning hazard in this phase lives here.** A shipped, green,
positive-controlled test — `test_no_feed_orders_by_updated_at` — **explicitly forbids what D-15/D-16
mandate.** It is a Phase 192.1 SCOPE fence, not a correctness rule, and `BUG-260815-02` is the
blocking report that supersedes it. It is **rewritten in place, never deleted**, and driven RED
against the pre-change source before the change lands.

Output: two SQL clauses, three recorded-decision comments, one rewritten fence.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

**Worktree bootstrap FIRST:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`, then assert the
dispatched base SHA against `193.2-BASELINE.md`. **Record the base SHA before your first edit — F-1's
RED observation is taken against `git show <base>:backend/app/db/workflows.py`.**

**This plan runs in parallel with 193.2-04, 193.2-05 and 193.2-06.** Its tests are pure
`inspect.getsource` source assertions and mutate no database, so it is safe to co-schedule (cap 2).
</execution_context>

<context>
@.planning/phases/193.2-from-authored-to-runnable/193.2-CONTEXT.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-RESEARCH.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-PATTERNS.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-BASELINE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Observe F-1 RED against the pre-change source, then rewrite the 192.1 scope fence in place</name>
  <files>backend/tests/unit/test_workflows_updated_at.py</files>
  <read_first>
    - `backend/tests/unit/test_workflows_updated_at.py` in full — especially `:232-247` (the `_published_source()` / `_starter_source()` / `_draft_source()` `inspect.getsource` accessors), `:286-307` (the three predicate byte-identity cases — ⚠ **the published one is PLURAL and PARAMETRIZED**: `test_published_predicates_are_byte_identical_to_what_shipped`, correction C-1; a plan grepping the singular form finds 1 of 3), `:310-318` (`test_no_feed_orders_by_updated_at`, the fence being rewritten — correction C-2: the range is `:310-318`, not `:310-320`), `:183-201` (the Python positive-control idiom), `:422-434` (`test_d17_is_recorded_at_the_published_builder` — the precedent for pinning PROSE because the alternative failure is a design regression no behavioural test would catch)
    - `backend/app/db/workflows.py:250` (the *"NO expression index, ZERO migration"* precedent), `:261-270` (the SCOPED NARROWING docblock naming the other consumers), `:282-287` and `:305-308` (the 192.1 additive-comment shape), `:312-317` (site 1), `:348-353` (site 2), `:539-549` (the strongest in-file "a decision recorded where a reader will land on it" comment), `:550-553` (site 3)
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-VALIDATION.md` — §"Fences", rows F-1 and F-2
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-BASELINE.md` — §1 (the base SHA and the `grep -c "ORDER BY name"` = 3 reading) and §7 (the recorded F-2 RED observation)
  </read_first>
  <action>
**Order matters and is not stylistic: the fence is rewritten and observed RED BEFORE the SQL changes.**

**(a) Rewrite `test_no_feed_orders_by_updated_at` IN PLACE.** Keep the function name (it is the
fence's identity across git history) or rename it to something that names the decision it now guards
— if you rename it, the old name must be quoted in the new docstring so `git log -S` still finds the
thread. The rewritten body asserts:
  - `_published_source()` contains `ORDER BY updated_at DESC` and does **not** contain `ORDER BY name`
  - `_draft_source()` contains `ORDER BY updated_at DESC` and does **not** contain `ORDER BY name`
  - `_starter_source()` contains `ORDER BY name` and does **not** contain `updated_at DESC`
    (⚠ scope the starter needle to the ORDER BY, not to the bare token `updated_at` — the starter
    feed legitimately PROJECTS `updated_at` in its SELECT list, so a bare `not in` on `updated_at`
    would be red on a correct tree. This is the same scoping error the 187-24 trap keeps producing)
  - **Non-vacuity:** each of the three sources is asserted non-empty and to contain its own function
    name, so a broken `inspect.getsource` cannot make the fence pass by returning nothing.

**(b) The superseded reasoning is recorded BESIDE the new, never over it.** The new docstring keeps
192.1's original sentence verbatim — *"CONTEXT is explicit that 160-C (recency ORDERING) LOST.
`ORDER BY name` stands. SC#3 is met by the 'changed <rel>' segment on the card, never by re-ordering
the list"* — marked as SUPERSEDED, dated, with the superseding authority named (`BUG-260815-02`,
blocking; Phase 193.2 D-15/D-16) and the reason: it was a Phase 192.1 **scope** fence stopping that
phase widening into an ordering change, not a correctness rule. This is the repository's standing
habit and it is what makes the fence auditable instead of merely current.

**(c) Add the D-16 divergence assertion.** A sibling case pinning that the divergence is written into
the code at ALL THREE sites: assert each of the three sources contains the literal token `D-16`.
Model it on `test_d17_is_recorded_at_the_published_builder` (`:422-434`) and copy its reasoning
verbatim in spirit — this is pinned as source prose *because the alternative failure is a later
reader "tidying" the inconsistency, which no behavioural test would catch.*

**(d) OBSERVE F-1 RED — mandatory, and the observation is recorded, not asserted.** Before touching
`backend/app/db/workflows.py`, run the rewritten fence against the pre-change source. Two honest
mechanisms, pick one and record which:
  - `git stash` any `db/workflows.py` edit and run
    `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_workflows_updated_at.py -q`, or
  - write a throwaway probe that asserts against `git show <base_sha>:backend/app/db/workflows.py`.
It **MUST fail on all three clauses** (published missing `updated_at DESC`, draft missing it, and
both still carrying `ORDER BY name`). Paste the failure output into the plan summary. ⚠ Per PATTERNS
§"No Analog Found", asserting against a previous commit has no in-repo precedent — treat it as a
**plan-time probe with a recorded observation**, not a permanent artifact. The permanent artifact is
the rewritten fence plus its non-vacuity guard.

⚠ **DO NOT touch the three `*_predicate*_byte_identical*` cases.** They are F-2 and they must stay
green and unedited; on a service-role pool that bypasses RLS, the predicate IS the boundary.
`193.2-BASELINE.md` §7 already records them driven RED against a one-character plant.

Commit `test(193.2-03): the 192.1 scope fence rewritten in place as the D-16 divergence fence, observed RED`.
  </action>
  <acceptance_criteria>
    - `test_no_feed_orders_by_updated_at` (or its renamed successor, with the old name quoted in the docstring) exists in `backend/tests/unit/test_workflows_updated_at.py` — the function was NOT deleted; `git diff` shows a rewrite, not a removal
    - Its docstring contains the literal token `SUPERSEDED` and quotes 192.1's original reasoning verbatim, with `BUG-260815-02` named as the superseding authority
    - The rewritten body asserts `ORDER BY updated_at DESC` present in the published and draft sources, `ORDER BY name` absent from both, `ORDER BY name` present in the starter source, and carries a non-vacuity guard per source
    - The starter absence needle is scoped to the ORDER BY clause, NOT to the bare token `updated_at` (the starter feed projects that column)
    - A sibling case asserts the literal token `D-16` appears in all three feed function sources
    - The plan summary records the F-1 RED observation verbatim: the mechanism used, the three failing assertions, and the command
    - `git diff --numstat` shows the three `*_predicate*_byte_identical*` cases UNCHANGED
    - At this task's commit the fence is RED (the SQL has not changed yet) — record that; it goes green in task 2
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && ./venv/Scripts/python.exe -m pytest tests/unit/test_workflows_updated_at.py -q -k "predicate" </automated>
  </verify>
  <done>The 192.1 scope fence has been rewritten in place as the D-16 divergence fence, its superseded reasoning is recorded beside the new, and it has been OBSERVED RED against the pre-change source — so a later green reading proves something.</done>
</task>

<task type="auto">
  <name>Task 2: Change the two ORDER BY clauses and record the D-16 divergence at all three sites</name>
  <files>backend/app/db/workflows.py</files>
  <read_first>
    - `backend/app/db/workflows.py` — `list_published_workflows` (both predicate branches and the `sql +=` chain ending at the `ORDER BY name` append), `list_starter_workflows`, `list_draft_workflows`; plus `:250`, `:261-270`, `:282-287`, `:539-549` for the in-file comment shapes this task copies
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-RESEARCH.md` — §R4.3 in full (the three sites verbatim, the migration-not-needed evidence, the two `updated_at` trigger facts, and the shared-consumer scope consequence)
    - `frontend/src/components/workflows/library/libraryFilter.ts:86, 112` — where `updated_at` reaches `relativeChanged`
    - `backend/app/api/workflows.py:330-343` — the `owned_only` mapping and the *"ANY other value keeps `owned_only=False`"* note; `backend/app/api/threads.py:97` — the kickoff import
  </read_first>
  <action>
**Two clause changes, three comments. Nothing else in this file moves.**

**(a) Site 1 — `list_published_workflows`.** The `sql += " ORDER BY name"` append becomes
`sql += " ORDER BY updated_at DESC"`. It stays appended AFTER the `$N` project-filter append, exactly
as today. **Not one byte of the two `WHERE` branches, the `params` list or the `$N` binding changes.**

**(b) Site 3 — `list_draft_workflows`.** The `f"ORDER BY name"` line in the `pool.fetch` f-string
becomes `f"ORDER BY updated_at DESC"`. The `WHERE status = 'draft' AND created_by = $1 ` clause and
the `CONCURRENCY_TOKEN_SQL` projection are untouched.

**(c) Site 2 — `list_starter_workflows` — LEAVE THE CLAUSE ALONE.** It keeps `ORDER BY name`.

**(d) The D-16 divergence comment, at ALL THREE sites.** Copy the shape at `:282-287` (which names
the phase, the decision id, the property preserved and the evidence) and the fence register at
`:539-549` (*"⚠ D-XX IS A FENCE, NOT ADVICE, AND THIS LINE IS WHERE IT BINDS"*). Each comment must
carry, in the module's own voice:
  - the literal token `D-16` and the phase (`193.2`) and the bug (`BUG-260815-02`)
  - **the divergence stated as a decision, not an inconsistency:** the two feeds holding the author's
    OWN work get recency; the curated starters catalogue the author did not write stays alphabetical,
    because *"most recently updated starter"* is meaningless to someone browsing. `BUG-260815-02`
    warns *"change them together or the feeds disagree"* and **this decision deliberately does not** —
    accepted with eyes open, on the recorded condition that the divergence lives in the code
  - **the property that is PRESERVED:** the projection and the predicate are untouched; on a
    service-role pool that bypasses RLS the `WHERE` is the boundary, and `ORDER BY` is appended after
    it and after the `$N` project filter, so it cannot widen visibility
  - **the D-15 column choice, with its measured strength:** `updated_at` is already on the wire and
    already consumed by `relativeChanged`'s nine bands (`libraryFilter.ts:86`, `:112`), so the card's
    *"changed <rel>"* text and the list order agree by construction. And on the PUBLISHED feed
    specifically, `ORDER BY updated_at DESC` **IS** `ORDER BY publish-time DESC`: the publish flip is
    an `UPDATE` and `workflow_definitions_set_updated_at` is an unconditional `BEFORE UPDATE` trigger,
    while `workflow_definitions_block_published` then freezes the row. So D-15's rejection of
    `created_at DESC` costs nothing on that feed
  - **NO MIGRATION, stated plainly with its evidence:** there is no index on `name` either — the
    shipped sort was already unindexed — the table is 225 rows and the largest feed 118, and
    `db/workflows.py:250` already records the precedent. **No file under `supabase/migrations/` is
    added by this phase.**

**(e) Site 1's comment carries one extra paragraph the others do not — the SHARED-CONSUMER
consequence.** `list_published_workflows` is shared: with `owned_only=False` (the default) it also
feeds the **composer's Harness workflow picker**, **`WorkspacePanel`'s run-soul** and
**`threads.py`'s kickoff**. All three move from alphabetical to recency. That is defensible and it is
a user-visible change OUTSIDE the library, so it is stated here rather than discovered later. Note
also, measured, that **no test and no frontend module asserts alphabetical order for those surfaces**.

⚠ **Do not "simplify" the two `WHERE` branches into one.** `?scope=mine` is a query parameter, not a
schema constraint, and the `owned_only=False` branch is what every other consumer relies on.

**(f) D-02, proved not asserted.** In the plan summary, state the no-second-concern argument for this
file with the measurement behind it: the file already owns three list-feed functions that ORDER their
own rows; this phase changes the ORDER BY on two of them. An ordering change to an ordering clause is
not a second concern. If while working you find you are adding one, **STOP** — D-01 is void for this
file and a refactor recommendation is owed FIRST.

Run the rewritten fence: it must now be GREEN, and F-2's three predicate cases must still be green.

Commit `fix(193.2-03): published and draft feeds order by recency; starters stay alphabetical (D-15/D-16)`.
  </action>
  <acceptance_criteria>
    - `grep -c "ORDER BY updated_at DESC" backend/app/db/workflows.py` returns exactly `2`
    - `grep -c "ORDER BY name" backend/app/db/workflows.py` returns exactly `1`, and it is inside `list_starter_workflows`
    - `python -c "import inspect,sys; sys.path.insert(0,'backend'); ..."` — or equivalently the rewritten fence — confirms `D-16` appears in the source of all three feed functions
    - `git diff` on `backend/app/db/workflows.py` shows ZERO changes to any `WHERE` clause, any `params` append, any `$N` binding, or the `SELECT` projection lists
    - `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_workflows_updated_at.py -q` → 0 failed (F-1 green, F-2 green)
    - `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_workflows_updated_at.py tests/unit/test_publish_service.py tests/unit/test_starter_workflows.py tests/unit/test_103_draft_crud.py tests/unit/test_published_workflow_ownership.py tests/unit/test_187_route_assigned_reach.py -q` → 0 failed, matching `193.2-BASELINE.md` §2
    - Site 1's comment contains the words naming the composer picker, the run-soul and the kickoff as re-ordered consumers
    - Every site's comment contains the literal token `D-16`; site 1's and site 3's additionally contain `D-15`
    - `ls supabase/migrations/ | wc -l` is unchanged from HEAD — no migration was added
    - The summary states the D-02 no-second-concern argument for `backend/app/db/workflows.py` with the measured reason
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && ./venv/Scripts/python.exe -m pytest tests/unit/test_workflows_updated_at.py tests/unit/test_starter_workflows.py tests/unit/test_103_draft_crud.py tests/unit/test_published_workflow_ownership.py -q</automated>
  </verify>
  <done>Two feeds order by recency, the curated shelf stays alphabetical, the divergence is recorded in the code at all three sites with the no-migration and shared-consumer facts, and every predicate is byte-identical to what shipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| **service-role asyncpg pool → `workflow_definitions`** | ⚠ **The service role BYPASSES RLS. The `WHERE` clause IS the access-control boundary** (`db/workflows.py` states this in-source at the draft feed and at the published feed). This is the only real boundary this plan comes near |
| HTTP query param → `owned_only` | `?scope=mine` is a query parameter, not a schema constraint; the two branches select different predicates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193.2-02 | **Information disclosure** | `list_published_workflows` / `list_draft_workflows` `WHERE` predicates | **mitigate** | The `ORDER BY` is appended AFTER the predicate and after the `$N` project-filter append, so it cannot widen visibility. Guarded mechanically by the three shipped `*_predicate*_byte_identical*` cases (F-2), which `193.2-BASELINE.md` §7 records driven RED against a one-character `WHERE` plant. **Acceptance criteria forbid any diff to a `WHERE` clause, a `params` append or a `$N` binding.** No plan may tidy a predicate while changing a sort key |
| T-193.2-02b | Tampering (SQL injection) | the two changed clauses | mitigate | Both new clauses are **static string literals with no interpolation** — `" ORDER BY updated_at DESC"` and the f-string's literal segment. No user input reaches the ORDER BY; the `$N` binding for the project filter is untouched |
| T-193.2-02c | Denial of service | unindexed sort on 225 rows | accept | There is no index on `name` either — the shipped sort was already unindexed. Largest measured feed is 118 rows. `db/workflows.py:250` records the same precedent. Re-open if the table passes ~10k rows |
| T-193.2-SC | Tampering | npm/pip/cargo installs | accept | No package installed; no migration added. RESEARCH §"Package Legitimacy Audit" records the phase installs nothing |
</threat_model>

<verification>
- `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_workflows_updated_at.py -q` → 0 failed
- The six workflow backend suites → 0 failed, matching `193.2-BASELINE.md` §2
- `git diff HEAD~2 -- backend/app/db/workflows.py` → only the two `ORDER BY` lines plus three comment blocks; zero predicate lines
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` → `count gate OK` (this plan touches no frontend file; the gate is the no-collateral check)
</verification>

<success_criteria>
- Exactly two `ORDER BY updated_at DESC` and exactly one `ORDER BY name` in `backend/app/db/workflows.py`
- F-1 exists as a rewritten-in-place fence with its superseded reasoning beside it, and was observed RED against the pre-change source
- F-2's three cases are unedited and green
- `D-16` appears in the source of all three feed functions
- No migration file is added; the summary says so with the measured reason
</success_criteria>

<output>
Create `.planning/phases/193.2-from-authored-to-runnable/193.2-03-SUMMARY.md` when done.
</output>
