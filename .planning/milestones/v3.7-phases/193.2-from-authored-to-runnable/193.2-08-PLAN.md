---
phase: 193.2-from-authored-to-runnable
plan: 08
type: execute
wave: 5
depends_on: ["193.2-05", "193.2-07"]
files_modified:
  - backend/tests/integration/test_193_2_authoring_frequency.py
  - .planning/phases/193.2-from-authored-to-runnable/193.2-FREQUENCY.md
autonomous: false
requirements: [AUTH-03]

must_haves:
  truths:
    - "D-08 — the fix is explicitly NON-DETERMINISTIC and the phase claims only a MEASURED REDUCTION in frequency, never an absence; every claim in the artifact is k/N with the rule stated verbatim, and no case in the harness asserts absence"
    - "D-20 — the cross-provider roster is narrowed to the two RESOLVABLE authoring models, and the other six are recorded N/A WITH THE REASON, never dropped; a scoreboard that lists only what passed is not a scoreboard"
    - "D-20 — the narrowing is justified from `resolve_authoring_model`, which is explicitly NOT the composer's selected model; scoring six providers on a path they can never take would measure a configuration that does not ship"
    - "SC#4's automated control holds: `render_template` presence must NOT fall against 193.1's measured 3/3 — a prompt change that reduced `llm_human_input` by breaking the template branch would be a regression, not a fix"
    - "ZERO GLOBAL MUTATION — the model is passed as a per-row settings stub, so the operator's environment is untouched and rows cannot contaminate each other"
  artifacts:
    - path: "backend/tests/integration/test_193_2_authoring_frequency.py"
      provides: "The opt-in k/N harness: two driven rows, six recorded N/A rows, record-before-verdict, always-emit-the-full-table"
      contains: "SimpleNamespace"
    - path: ".planning/phases/193.2-from-authored-to-runnable/193.2-FREQUENCY.md"
      provides: "The measured k/N scoreboard with the reduction-not-absence rule stated verbatim and all eight roster rows"
      contains: "k/N"
  key_links:
    - from: "backend/tests/integration/test_193_2_authoring_frequency.py"
      to: "backend/app/services/workflow_authoring.py"
      via: "generate_workflow_definition driven with a per-row settings stub and a bound template"
      pattern: "generate_workflow_definition"
---

<objective>
The only instrument that can say whether plans 193.2-05 and 193.2-07 actually worked — and the only
honest shape for saying it.

D-08 makes this fix explicitly non-deterministic: when the model emits nothing, the fallback is
today's exact behaviour, so **the phase may claim a measured reduction in frequency and never an
absence.** Any success criterion or test written as *"the field is always populated"* is wrong. The
right instrument is the shipped Phase 187 authoring roster harness — opt-in, real paid calls, a
per-row settings stub so **zero global state is mutated**, every row recorded **before** it can
pass/fail/block, and the full table always emitted including ⛔ rows.

D-20 narrows the roster, and the narrowing is a measurement rather than a shortcut:
`resolve_authoring_model` returns `settings.harness_authoring_model`, else the first of
`("claude-opus-4-8", "gpt-5.5")` whose registry entry has a truthy `forced_emission` — **explicitly
not the composer's selected model.** `harness_authoring_model` has no `app_settings` column and no
Settings UI. Scoring eight providers on a path six of them can never take would measure a
configuration that does not ship — the exact error the roster rule itself warns about.

⚠ **SERIALIZE this plan.** It drives real, paid provider calls and reads the local Supabase for
grounding assembly. It must not be co-scheduled with any other database-touching plan.

Output: one new integration harness (modelled on, never modifying, the 187 file) and one measured
scoreboard artifact.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

**Worktree bootstrap FIRST:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`, then assert the
dispatched base SHA — this plan must run on a base containing 193.2-05 AND 193.2-07, or it measures
the old prompt and the answer is worthless.

⚠ **This plan is NOT autonomous.** Task 2 is a blocking checkpoint: the run costs money and needs
provider keys the operator controls.
</execution_context>

<context>
@.planning/phases/193.2-from-authored-to-runnable/193.2-CONTEXT.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-RESEARCH.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-PATTERNS.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-VALIDATION.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-05-SUMMARY.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the k/N harness, modelled on the 187 roster file</name>
  <files>backend/tests/integration/test_193_2_authoring_frequency.py</files>
  <read_first>
    - `backend/tests/integration/test_187_authoring_roster.py` in FULL — **model on it, do NOT modify it.** Specifically: `:66-73` (the SKIP DESIGN paragraph — the opt-in gate is applied **per row INSIDE the test**, never as a module `pytestmark`, and the recorded reason must not be re-litigated: a collection-time skip cannot let a blocked row RECORD itself ⛔ first), `:99-113` (the `.env` read, because `tests/conftest.py` setdefaults `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` to sentinels before any app import), `:212-213` (**secrets are a BOOLEAN, never a value** — `key_configured=bool(getattr(settings, f"{provider}_api_key", ""))`), `:228-286` (`RowResult`, `_RESULTS`, `_record`, `_block`, the `_scoreboard_writer` module-teardown fixture, and ⚠ **the Windows-console note: printing a verdict glyph on a cp1252 console raises `UnicodeEncodeError` IN TEARDOWN, which pytest reports as an ERROR on the last row — "the scoreboard would corrupt the very board it prints"**), `:365-378` (`test_roster_is_derived_from_the_live_registry`), `:406-418` (**the per-row `SimpleNamespace(harness_authoring_model=…)` stub — `settings` is a PARAMETER of the service, so nothing global is touched**)
    - `backend/app/services/workflow_authoring.py:108-133` — `resolve_authoring_model`, the reason D-20 narrows
    - `backend/app/services/harness/grounding.py:579-635` — the D-26 template arms, so the driven describe + bound template reproduces the condition `BUG-260815-01` measured 2-for-2
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-VALIDATION.md` §"The non-determinism rule (D-08)" and §"D-20 — the narrowed roster"
    - `.planning/phases/193.1-*/193.1-UAT.md` — the 193.1 UAT kit's 10-field `.docx` at `C:\Users\fhdmr\Desktop\uat-193.1-qbr\`, reused so SC#2 and SC#4 measure the same artefact
  </read_first>
  <action>
Create `backend/tests/integration/test_193_2_authoring_frequency.py`.

**Copy verbatim from the analog** (these are the properties that make the file trustworthy, and
re-deriving them is how they get lost): `RowResult`, `_RESULTS`, `_record`, `_block`, the
`_scoreboard_writer` module-teardown fixture **including its `sys.stdout.encoding` guard**, the
`.env`-reading block, the per-row `SimpleNamespace` settings stub, and the boolean-only key-presence
check.

**What must DIFFER, per D-20:** the 187 file DERIVES its roster from `MODEL_CAPABILITIES` across
eight provider groups. This file's roster is **2 DRIVE rows + 6 pre-seeded ⛔ N/A rows**:
  - DRIVE: `anthropic` / `claude-opus-4-8` (what this environment actually resolves) and
    `openai` / `gpt-5.5` (the declared fallback, reachable only via an explicit settings stub)
  - N/A, each carrying its reason as a string in `_RESULTS` at module import so it appears in the
    table even if the file is collected and skipped entirely: google, deepseek, zhipu/GLM, minimax,
    moonshot/kimi, openrouter. **Record all eight; never drop six.** For moonshot, additionally
    record the `test_187_authoring_roster.py:44-52` finding — an explicitly-set moonshot model hands
    an **unforceable** model to `forced_emit` — and mark it as a real finding that is **not this
    phase's**.
  - Keep the *spirit* of `test_roster_is_derived_from_the_live_registry` as a guard that runs
    **without** the opt-in: assert both DRIVE rows resolve `capability_source == "registry"`, because
    an id the shipping lookup misses resolves `inferred`, silently loses `emit_tier`, and would make
    the row measure a weaker configuration than the one that ships.

**The drive.** Per DRIVE row, call `generate_workflow_definition` **N ≥ 5** times with:
  - a describe text plus **a bound template** — the condition `BUG-260815-01` measured 2-for-2 —
    reusing the 193.1 UAT kit's 10-field `.docx` so SC#2 and SC#4 measure the same artefact;
  - the per-row `SimpleNamespace(harness_authoring_model=row.model_id)` stub — **zero global
    mutation**, so the operator's environment is untouched and the rows cannot contaminate each other;
  - `asyncio.wait_for` with a timeout, as the analog does.

**What to count, per row — three k/N figures and nothing else:**
  1. `business_requirement` non-empty — **k/N** (SC#1)
  2. an `llm_human_input` phase present — **k/N** (SC#2)
  3. a `render_template` phase present — **k/N**, **the SC#4 no-regression control**. 193.1 measured
     **3/3** and it must not fall. ⚠ A prompt change that reduced `llm_human_input` by breaking the
     template branch would be a regression dressed as a fix, and this is the only figure that can
     see it.
  Additionally record the seeded-provenance flag's value per call, so plan 193.2-09's mark is
  measured against something real rather than assumed.

**Opt-in env name:** `RUN_193_2_AUTHORING`. Gate per row, inside the test, exactly as the analog does.

⚠ **NO CASE IN THIS FILE MAY ASSERT ABSENCE.** No `assert llm_human_input not in ...`, no
`assert business_requirement is not None` over a real call. The deterministic properties are already
pinned by `test_workflow_authoring_requirement.py`. **Frequency is an artifact, not an assertion.**
The only hard assertion permitted over driven output is the SC#4 control expressed as a floor with
its baseline named (and even that must be phrased against 193.1's measured 3/3, not against 100%).

⚠ **Do NOT modify `backend/tests/integration/test_187_authoring_roster.py`.**

Commit `test(193.2-08): the k/N authoring-frequency harness, two driven rows and six recorded N/A (D-08/D-20)`.
  </action>
  <acceptance_criteria>
    - `backend/tests/integration/test_193_2_authoring_frequency.py` exists
    - WITHOUT the opt-in env set, `cd backend && ./venv/Scripts/python.exe -m pytest tests/integration/test_193_2_authoring_frequency.py -q` collects, runs the registry guard, skips the driven rows, and still emits the full 8-row scoreboard — 0 failed
    - `_RESULTS` contains 8 entries after a no-opt-in run; the six N/A rows each carry a non-empty reason string
    - The registry guard asserts `capability_source == "registry"` for both DRIVE rows and runs WITHOUT the opt-in
    - `grep -c "SimpleNamespace" backend/tests/integration/test_193_2_authoring_frequency.py` returns at least 1, and `grep` finds no assignment to a global settings object
    - No API key VALUE appears anywhere in the file; key presence is a `bool(...)` expression only
    - The `_scoreboard_writer` teardown carries the `sys.stdout.encoding` guard (the cp1252 `UnicodeEncodeError` trap)
    - `grep -n "not in\|is None\|assert not" ` over the driven-row body finds no absence assertion over model output
    - `git diff --numstat` shows `backend/tests/integration/test_187_authoring_roster.py` UNCHANGED
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && ./venv/Scripts/python.exe -m pytest tests/integration/test_193_2_authoring_frequency.py -q</automated>
  </verify>
  <done>The harness exists, runs green with no keys and no opt-in while still emitting all eight rows, mutates nothing global, and contains no absence assertion.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Authorize the paid measurement run</name>
  <files>(none — blocking checkpoint; no file is written by this task)</files>
  <action>
PAUSE. Present the summary below to the operator and wait for an explicit approval before any
provider call is made. Do NOT set `RUN_193_2_AUTHORING` and do NOT run task 3 until the operator has
answered. If the operator declines, record every driven row as OWED with the reason in the plan
summary and in `193.2-FREQUENCY.md`, and let the phase close with the frequency measurement OWED
rather than fabricated — closing with an owed row is a legitimate DECISION, but it must be stated as
one and never as a claim that everything ran.
  </action>
  <what-built>
    An opt-in integration harness at `backend/tests/integration/test_193_2_authoring_frequency.py`.
    It is green today WITHOUT any provider call: the driven rows are skipped and recorded ⛔ with
    their reason, and the registry guard still runs. Nothing global is mutated — the model is passed
    as a per-row `SimpleNamespace` stub, so your Settings and your `app_settings` rows are untouched.
  </what-built>
  <how-to-verify>
    This run makes **real, paid provider calls** — at least 5 per driven row, 2 rows, so ~10+ authoring
    generations, each with a bound `.docx` template. It also reads your local Supabase for grounding.

    1. Confirm the backend `.env` carries the keys you want exercised. The harness records key presence
       as a boolean only and never reads a key value into the artifact.
    2. Confirm `anthropic` is the row you care about most — `resolve_authoring_model` returns
       `claude-opus-4-8` in this environment because `harness_authoring_model` has no `app_settings`
       column and no Settings UI. `gpt-5.5` is reachable only through the harness's explicit stub.
    3. Decide N. The plan asks for **N ≥ 5** per row; more N buys a tighter k/N and costs proportionally.
    4. Confirm the local Supabase is up (`supabase status`), or every row will correctly record
       ⛔ `supabase-unreachable` and the phase will have no frequency measurement.

    Reply with **approved** (and any change to N), or describe what you want different.
  </how-to-verify>
  <resume-signal>Type "approved" (optionally "approved, N=8") or describe the change you want</resume-signal>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && ./venv/Scripts/python.exe -m pytest tests/integration/test_193_2_authoring_frequency.py -q</automated>
  </verify>
  <done>The operator has explicitly approved (or declined) the paid run and set N; the decision is recorded in the plan summary either way.</done>
</task>

<task type="auto">
  <name>Task 3: Drive the run and write the k/N scoreboard with the reduction-not-absence rule verbatim</name>
  <files>.planning/phases/193.2-from-authored-to-runnable/193.2-FREQUENCY.md</files>
  <read_first>
    - `backend/tests/integration/test_193_2_authoring_frequency.py` as task 1 left it
    - `.planning/reported-bugs/template-first-drafts-cannot-be-published.md` — the 2-for-2 measurement table, the pre-fix baseline this artifact compares against
    - `.planning/phases/193.1-*/193.1-11-SUMMARY.md` — the independent post-D-26 finding (`llm_human_input` in **4 of 6** runs) and the `render_template` **3/3** figure that is SC#4's control
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-VALIDATION.md` §"The non-determinism rule (D-08)" — **the sentence the artifact must state verbatim**
    - `CLAUDE.md` §"UAT scoreboard recipe" — the roster rule D-20 narrows, and the *"blocked, never silently omitted"* rule
  </read_first>
  <action>
Run the harness with `RUN_193_2_AUTHORING=1` and the operator-approved N, **serially — no other
database-touching plan may be executing.** Capture the emitted scoreboard.

Write `.planning/phases/193.2-from-authored-to-runnable/193.2-FREQUENCY.md` containing:

**§1 The rule, stated before any number.** Verbatim, in the artifact's own words:
> *"Baseline (pre-fix, from `BUG-260815-01` + `193.1-11-SUMMARY.md`): `business_requirement` 0/N;
> `llm_human_input` 2/2 with a template, 4/6 overall. Post-fix measured: k/N. The claim is a
> reduction, not an absence — the publish gate stays because a prompt cannot guarantee absence."*

**§2 The full eight-row roster table**, DRIVE rows with their k/N figures and ⛔ rows with their
reasons. State the roster rule, state why it is narrowed here (`resolve_authoring_model` is not the
composer's model; `harness_authoring_model` is env-only with no UI and needs a restart), and name the
two rows driven. ⚠ **Never drop the six.** A scoreboard that lists only what passed is not a
scoreboard, and one that silently omits rows is worse.

**§3 The three figures per driven row**, with N stated:
  - `business_requirement` non-empty — k/N (SC#1)
  - `llm_human_input` present — k/N (SC#2), compared explicitly against the 2-for-2 / 4-of-6 pre-fix
    baseline
  - `render_template` present — k/N, **the SC#4 control**, compared against 193.1's measured **3/3**.
    ⚠ **If this figure FELL, say so loudly and treat it as a finding that blocks the phase's SC#4
    claim** — a prompt change that reduced `llm_human_input` by breaking the template branch is a
    regression dressed as a fix, and the D-26 arm must NOT be weakened to buy it.
  - the provenance flag's value per call, so plan 193.2-09's visible mark is measured rather than
    assumed.

**§4 Attribution, stated honestly (OQ-4).** Plan 193.2-05 made TWO edits in one literal — the
suppression clause AND the shortened bullet — so a single measurement **cannot attribute** the effect
to either. If budget allowed a third arm (the clause with the ORIGINAL 121-character bullet), report
it. **If it did not, say so plainly**: the phase claims a combined effect and attributes nothing,
which is honest and cheap. `grounding.py:605-607` records exactly this attribution lesson.

**§5 What this does NOT prove.** k/N over N ≥ 5 is a frequency, not a guarantee. The gate stays. The
field can still arrive blank, and when it does the fallback is today's exact behaviour (D-08).

⚠ **Do not write any sentence claiming the field is always populated or the step never appears.**
Reject such a sentence at review.

Commit `docs(193.2-08): the measured k/N scoreboard — a reduction, never an absence (D-08/D-20)`.
  </action>
  <acceptance_criteria>
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-FREQUENCY.md` exists and §1 contains the reduction-not-absence sentence VERBATIM, including the words `The claim is a reduction, not an absence`
    - §2 contains a table with exactly 8 provider rows; the 6 N/A rows each carry a reason; the moonshot row additionally carries the unforceable-model finding marked as not-this-phase's
    - §3 states N and gives three k/N figures per driven row, each compared against its named pre-fix baseline
    - The `render_template` figure is compared explicitly against 193.1's 3/3; if it fell, the artifact says so and flags SC#4
    - §4 states whether the third attribution arm was driven, and if not, says the phase claims a combined effect and attributes nothing
    - `grep -i "always populated\|never appears\|guaranteed"` finds no CLAIM in the artifact (the §5 prohibition text is expected and correct)
    - No API key value appears anywhere in the artifact
    - The harness still passes WITHOUT the opt-in after the run: `pytest tests/integration/test_193_2_authoring_frequency.py -q` → 0 failed
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && grep -q "The claim is a reduction, not an absence" .planning/phases/193.2-from-authored-to-runnable/193.2-FREQUENCY.md && cd backend && ./venv/Scripts/python.exe -m pytest tests/integration/test_193_2_authoring_frequency.py -q</automated>
  </verify>
  <done>The phase has a measured frequency for both SC#1 and SC#2, an SC#4 control that is either held or loudly flagged, all eight roster rows recorded, and a stated rule that the claim is a reduction rather than an absence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| **backend `.env` provider keys → the test process** | Real credentials are read to make paid calls |
| **the harness → external provider APIs** | Live outbound network, real spend |
| **the harness → local Supabase** | Grounding assembly reads the operator's real local data |
| **measured output → a committed planning artifact** | Whatever the model emitted gets written into the repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193.2-08-A | **Information disclosure** | provider keys and grounded KB content reaching a committed artifact | **mitigate** | Key presence is recorded as `bool(getattr(settings, f"{provider}_api_key", ""))` — **the value never leaves that expression** (the analog's T-187-07-01 rule, copied verbatim). The scoreboard records counts and verdicts, not emitted prose; acceptance criteria forbid any key value in the artifact. Review the emitted definitions before pasting any excerpt |
| T-193.2-08-B | Tampering | the operator's environment / global settings | **mitigate** | The model is passed as a **per-row `SimpleNamespace(harness_authoring_model=…)` stub** — `settings` is a PARAMETER of the service, so nothing global is touched, no `app_settings` row is written, and the rows are independent of each other and of the operator's environment. Acceptance criteria forbid any global settings assignment |
| T-193.2-08-C | Denial of service / cost | real paid provider calls | **mitigate** | Opt-in behind `RUN_193_2_AUTHORING`, gated **per row inside the test** so a blocked row still records ⛔ before skipping; and a **blocking human checkpoint** (task 2) authorizes the spend and sets N |
| T-193.2-08-D | Repudiation | a frequency claim that overstates | **mitigate** | D-08 is enforced mechanically at review: no absence assertion is permitted in the harness, and the artifact must state the reduction-not-absence rule verbatim before any number. The six N/A rows are recorded rather than dropped (D-20) |
| T-193.2-03 | Elevation of privilege (weakened gate) | AI-authored `business_requirement` reaching the judge rubric | accept | Unchanged from plan 193.2-07's register: format-string injection is not possible; the residual is an accepted weakened gate under D-09, mitigated by D-07's describe-copy refusal. This plan MEASURES the field's arrival; it does not change the path |
| T-193.2-SC | Tampering | npm/pip/cargo installs | accept | No package installed. `python-dotenv` and `pytest-asyncio` are already in `backend/venv` and used by the analog |
</threat_model>

<verification>
- Without the opt-in: `cd backend && ./venv/Scripts/python.exe -m pytest tests/integration/test_193_2_authoring_frequency.py -q` → 0 failed, 8 rows in the emitted table
- `git diff --numstat` shows `backend/tests/integration/test_187_authoring_roster.py` UNCHANGED
- `193.2-FREQUENCY.md` contains the reduction-not-absence sentence verbatim and 8 roster rows
- No API key value appears in either file
</verification>

<success_criteria>
- The harness is opt-in, mutates nothing global, records before it can pass/fail/block, and emits all eight rows even when every driven row is skipped
- The measured scoreboard reports three k/N figures per driven row against named pre-fix baselines
- The SC#4 `render_template` control is reported and compared against 193.1's 3/3
- No sentence anywhere claims absence or always-population
</success_criteria>

<output>
Create `.planning/phases/193.2-from-authored-to-runnable/193.2-08-SUMMARY.md` when done.
</output>
