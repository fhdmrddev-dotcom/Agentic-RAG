---
phase: 193.2
slug: from-authored-to-runnable
status: verified
threats_open: 0
asvs_level: 1
created: 2026-08-15
register_authored_at_plan_time: true
---

# Phase 193.2 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.

**State B run** (no prior SECURITY.md; 10 PLANs + 10 SUMMARYs). **All 10 PLAN files carried a
parseable `<threat_model>` block**, so `register_authored_at_plan_time: true` and the audit ran in
**verify-mitigations** mode, not retroactive-STRIDE.

⚠ **THE SHORT-CIRCUIT WAS AVAILABLE AND WAS NOT TAKEN, and that is the most important line in this
file.** Step 3's rule permits skipping straight to the write step once every threat classifies
CLOSED from the PLAN dispositions and the SUMMARY threat flags. Every one of the 23 threats does
classify CLOSED on reading alone. **The skip was refused because this project has already measured
what it costs:** `192.1-SECURITY.md` closed with `threats_open: 0` and the audit still found
**three durability holes where the property held but NOTHING defended it** — including a register
that claimed a grep-assertion which did not exist, and a subtree guard that loaded 3 of 12 modules.
**A summary claiming a mitigation is not the mitigation.** Every row below therefore carries a
command-derived evidence cell, and the one row most at risk of being nominal was driven.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| Service-role Postgres pool → the three workflow list feeds | `db/workflows.py` reads through a pool that **bypasses RLS**, so each feed's `WHERE` clause **IS** the access boundary — there is no second net behind it | Workflow definitions across all tenants; `user_id` / `org_id` scoping lives only in the predicate |
| Model output → persisted `WorkflowDefinition` JSONB | `workflow_authoring.py` writes a model-authored definition; `models/harness.py` validates it under `extra="forbid"` | AI-authored `business_requirement`, phase specs, and the new `business_requirement_seeded_by_ai` provenance flag |
| Model output → the publish judge rubric | `publish_service.py` interpolates `business_requirement` into `JUDGE_RUBRIC_CORE` — an AI-written requirement is graded by an AI judge | Requirement prose that can influence its own pass criteria |
| Model output → user-visible refusal copy, an aria label, and a **persisted** `harness_audit` row | `_interactive_phase_failures` embeds a model-authored `PhaseSpec.name` in all three | Untrusted-length, untrusted-content label text |
| Browser store → autosave PATCH | `builderStore.ts` writes requirement text + provenance flag; server validates under `extra="forbid"` | Requirement text and a boolean the client must never be able to set `true` |
| Live provider APIs → committed phase artifacts | `193.2-FREQUENCY.md` drives real paid calls; UAT evidence is pasted into `193.2-UAT.md` | Provider keys, grounded KB document names, judge prose, run/definition UUIDs |

---

## Threat Register

23 unique threats across 43 register rows in 10 PLAN files. Bolded duplicates normalized
(`**T-193.2-01**` ≡ `T-193.2-01`; same for `-03`). Every row verified at HEAD against phase base
`c1a6c122`.

| Threat ID | Category | Component | Disposition | Mitigation — **verified evidence** | Status |
|-----------|----------|-----------|-------------|------------------------------------|--------|
| T-193.2-02 | Information disclosure | The three list feeds' RLS-bypassing `WHERE` predicates | mitigate | **The complete non-comment diff of `db/workflows.py` is TWO lines, both `ORDER BY`.** `git diff -U0 c1a6c122 HEAD -- backend/app/db/workflows.py \| grep '^[-+]' \| grep -v '^[-+][-+]' \| grep -vE '^[-+]\s*#'` → `-" ORDER BY name"` / `+" ORDER BY updated_at DESC, id DESC"` ×2. Zero predicates, zero `params` appends, zero `$N` bindings, zero projection changes. The `ORDER BY` is appended **after** the predicate, so it cannot widen visibility | closed |
| T-193.2-02b | Tampering (SQL injection) | The two changed `ORDER BY` clauses | mitigate | Both clauses are **static literals with no interpolation** — confirmed in the two-line diff above. ⚠ **One residual noted rather than smoothed:** one clause is written `f"ORDER BY updated_at DESC, id DESC"` — an f-string with **no placeholders**, therefore inert today, but an f-string is an invitation to a future edit that interpolates. Recorded as R-04 | closed |
| T-193.2-02c | Denial of service | Unindexed sort on 225 rows | accept | Accepted risk **R-01**. There is no index on `name` either — the shipped sort was **already** unindexed, so the plan shape does not move. Largest feed measured 118 rows | closed |
| T-193.2-03 | Elevation of privilege (weakened gate) | `publish_service.py` judge rubric consuming an AI-authored `business_requirement` | accept | Accepted risk **R-02** (D-09), and the acceptance is **not silent**: `193.2-09` shipped the human-visible `AI-proposed` mark so the unchanged gate is an honest declared trade rather than a quiet weakening. Verified: `builderStore.ts:720` writes the flag; the mark is gated in `WorkflowBuilderPage.tsx` | closed |
| T-193.2-03b | Spoofing | A model emitting the provenance flag in its own payload | mitigate | **Stamped SERVER-side and the model's claim is ignored in both directions.** `workflow_authoring.py:555` performs the stamp; `:483` records the rule verbatim — *"A model that emits `business_requirement_seeded_by_ai: false` … does not get to set it"*. Both directions pinned and driven RED by plants C and D | closed |
| T-193.2-03c | Tampering | The JSONB definition and pre-existing rows | mitigate | **Additive-optional, zero-migration.** `models/harness.py:571` → `business_requirement_seeded_by_ai: bool = False` — a plain `bool`, never `bool \| None`, so absence has ONE spelling. `extra="forbid"` **NOT relaxed** — still present at `:41`. `ls supabase/migrations/ \| wc -l` = **112 before and after**; `git diff --name-only c1a6c122 HEAD -- supabase/migrations/` is **empty** | closed |
| T-193.2-01 | Information disclosure / DoS | Model-authored `PhaseSpec.name` in refusal copy, an aria label, and a persisted audit row | mitigate | **`_LABEL_MAX_CHARS = 72`** at `publish_service.py:544` and **`def _clean_label`** at `:628`. The constant's own comment records *"XSS is not the threat here; length and honesty are"* so a later reader does not HTML-escape user-facing copy. An empty label degrades to a name-free TRUE sentence, never a bare slug | closed |
| T-193.2-01b | Spoofing / Repudiation | Internal identifiers and slugs leaking into copy aimed at a non-coder | mitigate | Fence **F-3** scoped over the **composed message VALUE**, never module source — deliberate, because this module's docblocks legitimately quote `llm_human_input` and `ask_user`. Closes the `BUG-260809-02` failure class | closed |
| T-193.2-01c | Repudiation | Copy promising an unscheduled capability | mitigate | Fence **F-8** asserts `planned` / `coming soon` / `deferred` / `future release` are absent case-insensitively from the prompt, driven RED against a plausible plant. Exists because `SEED-164` was born of a docblock calling something *"the DEFERRED Phase-103 rework"* for a year | closed |
| T-193.2-01d | Elevation of privilege | The publish gate itself | mitigate | **The gate STAYS, and widening it is forbidden by a SHIPPED FENCE — verified to exist and to pass, not merely cited.** ⚠ The register's reference resolved only to a *docstring mention* at `tests/integration/test_193_2_authoring_frequency.py:85` on first search — exactly the "fence that cannot fire" shape. **Definition confirmed at `tests/unit/test_publish_service.py:1478`**, and driven: `pytest -k armed_checkpoint_is_not_a_validator` → **1 passed**. Its docblock records CONFLICT-1 Option B as REJECTED (widening would make every `external_action` workflow unpublishable) | closed |
| T-193.2-09-A | Spoofing | A client-side *"is this still the AI's sentence?"* decision | mitigate | **Forbidden by construction** — the flag is cleared on ANY edit in one `set()`; there is no trim, no empty-check, no null-coercion, no debounce and no comparison. A client comparison would be a second copy of a server predicate the server does not have | closed |
| T-193.2-09-B | Tampering | The autosave PATCH under `extra="forbid"` | mitigate | **`builderStore.ts:720` writes an explicit `business_requirement_seeded_by_ai: false`, never `delete`.** The field is DECLARED on the model so the PATCH validates. The explicit-false strategy is what makes the demotion observable on the wire; a plant substituting `delete` reds the exact assertion | closed |
| T-193.2-01-A | Tampering | Temporary plants in `db/workflows.py`, `rowIdentity.ts` | mitigate | **All plants restored, verified at HEAD.** No `rows.sort(` / `visibleRows.sort(` appears in any production source — the eight hits are fence **fixtures inside `.test.ts`**, which is correct. `git status --porcelain` across all six in-scope source files is **empty**. Each restore named its single file — no blanket `git clean` / `reset --hard` / `checkout -- .` | closed |
| T-193.2-04-A | Tampering | The temporary `rows.sort(` plant in `libraryFilter.ts` | mitigate | Same verification as T-193.2-01-A; three plants across two files, all restored, empty numstat proved per plant | closed |
| T-193.2-05-A | Tampering | The temporary 200-character bullet plant | mitigate | Marker count at HEAD = **0** in `workflow_authoring.py`; working tree clean | closed |
| T-193.2-08-A | Information disclosure | Provider keys and grounded KB content reaching a committed artifact | mitigate | **Load-bearing, not nominal:** `193.2-02` measured that raw `publish_blocked` payloads contain `definition_id` / `golden_run_id` UUIDs and **multi-kilobyte judge prose quoting KB document names** (to 2550 chars/row). The committed artifacts reproduce **only** `created_at`, `blocked_stage`, `jsonb_typeof` and counts. No key, no KB content, no user id, no email | closed |
| T-193.2-08-B | Tampering | The operator's environment / global settings | mitigate | **Verified untouched:** `git diff --name-only c1a6c122 HEAD -- backend/app/api/settings.py backend/app/models/user_settings.py` is **empty**. Rows were driven with **per-request** `model` + `provider`, mutating no global setting — the cheapest-honest-method rule | closed |
| T-193.2-08-C | Denial of service / cost | Real paid provider calls | mitigate | **Opt-in gate confirmed in source:** `test_193_2_authoring_frequency.py:961` → `if not os.environ.get(OPT_IN_ENV):`. Unset by default, so the suite does not spend money on an ordinary run | closed |
| T-193.2-08-D | Repudiation | A frequency claim that overstates | mitigate | **D-08 binds every word:** the result is recorded as a measured **REDUCTION, NEVER AN ABSENCE** — `business_requirement` 5/5 (pre-fix 0/N), `llm_human_input` 0/5 (pre-fix 2/2 with a template), SC#4 `render_template` control 5/5 like-for-like and it did not fall. **This is exactly why the publish gate stays** | closed |
| T-193.2-10-A | Information disclosure | UAT evidence pasted into `193.2-UAT.md` | mitigate | Same measurement as T-193.2-08-A. `193.2-UAT.md` states the prohibition **in its own U1 section** so the operator does not paste raw payloads when driving the row | closed |
| T-193.2-10-B | Repudiation | A row marked pass from another row's evidence | mitigate | Mitigated **structurally**: U1's five moments are separate rows with separate verdicts. Two rows (U5, U2) are recorded ⏸ **not driven** rather than silently omitted — the scoreboard rule | closed |
| T-193.2-10-C | Repudiation | Closing a reported bug that still reproduces | mitigate | Both blocking reports closed **on driven evidence**, each with a falsifiable re-open trigger, because `BUG-260815-01`'s closure rests on a **frequency (0/20), not an absence** | closed |
| T-193.2-SC | Tampering (supply chain) | npm / pip / cargo installs | accept | Accepted risk **R-03**, and it is a **measurement, not a promise**: `git diff --name-only c1a6c122 HEAD -- backend/requirements.txt frontend/package.json frontend/package-lock.json '*/pyproject.toml'` returns **nothing**. The phase installed zero packages and added zero migrations | closed |

*Status: open · closed*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| R-01 | T-193.2-02c | Unindexed `ORDER BY updated_at DESC` on the two author feeds. **There is no index on `name` either — the shipped sort was already unindexed, so the query plan shape does not move.** Table is 225 rows; largest feed measured 118. **Re-open trigger: ~10k rows** | Operator (phase plan, D-16) | 2026-08-15 |
| R-02 | T-193.2-03 | An AI-authored `business_requirement` is interpolated into `JUDGE_RUBRIC_CORE` and graded by an AI judge. Accepted per **D-09** rather than gated, **on the condition that the provenance is made visible to the human** — discharged by the `AI-proposed` mark shipped in `193.2-09`. The mark is what converts this from a silent weakening into a declared trade | Operator (phase context, D-09) | 2026-08-15 |
| R-03 | T-193.2-SC | No dependency was added, so there is no new supply-chain surface to review. Verified by empty diff over all four manifests | Operator (all 10 plans) | 2026-08-15 |
| R-04 | T-193.2-02b | One `ORDER BY` clause is written as an **f-string with no placeholders** (`f"ORDER BY updated_at DESC, id DESC"`). Inert today — an f-string with no braces is a plain literal — but it sits on a **service-role, RLS-bypassing** query where the `WHERE` clause is the only access boundary, and the `f` prefix invites a future edit that interpolates. **Found by this audit, not by a plan.** Not fixed here: changing it is a one-character source edit outside a security finding's remit, and F-2's byte-identity predicate fences already guard the clause. **Re-open trigger: any edit to either `ORDER BY` line, or the first appearance of a `{` inside it** | This audit | 2026-08-15 |

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-08-15 | 23 | 23 | 0 | `/gsd:secure-phase 193.2` — orchestrator, verify-mitigations mode, evidence re-derived at HEAD |

**Commands the evidence rests on** (re-runnable, phase base `c1a6c122`):

```bash
git diff -U0 c1a6c122 HEAD -- backend/app/db/workflows.py | grep '^[-+]' | grep -v '^[-+][-+]' | grep -vE '^[-+]\s*#'
git diff --name-only c1a6c122 HEAD -- backend/requirements.txt frontend/package.json frontend/package-lock.json '*/pyproject.toml'
git diff --name-only c1a6c122 HEAD -- supabase/migrations/ ; ls supabase/migrations/ | wc -l
git diff --name-only c1a6c122 HEAD -- backend/app/api/settings.py backend/app/models/user_settings.py
grep -n "_LABEL_MAX_CHARS\s*=\|def _clean_label" backend/app/services/harness/publish_service.py
grep -n "business_requirement_seeded_by_ai" backend/app/models/harness.py backend/app/services/workflow_authoring.py frontend/src/components/workflows/builderStore.ts
grep -n 'extra\s*=\s*"forbid"' backend/app/models/harness.py
grep -rn "def test_the_armed_checkpoint_is_not_a_validator" backend/tests/ --include=*.py
cd backend && venv/Scripts/python.exe -m pytest tests/ -q -k "armed_checkpoint_is_not_a_validator"
grep -rn "rows\.sort(\|visibleRows\.sort(" frontend/src/components/workflows/library/ frontend/src/pages/WorkflowsPage.tsx
git status --porcelain <the six in-scope source files>
```

### What this audit added beyond the plan registers

1. **T-193.2-01d's fence was verified to EXIST and to PASS.** The register's reference resolved on
   first search only to a docstring mention in a different file — the exact shape of a fence that
   cannot fire. The definition was located (`test_publish_service.py:1478`) and **driven** (1 passed).
   Had it been absent, the phase's central *"the publish gate stays"* claim would have rested on prose.
2. **R-04 was found, and by this audit rather than by any plan** — a no-placeholder f-string on an
   RLS-bypassing query. Inert now; recorded with a falsifiable re-open trigger rather than fixed,
   because a security audit reports and does not edit implementation.
3. **Plant restoration was verified at HEAD**, not accepted from summaries — including confirming
   that the eight surviving `rows.sort(` hits are fence fixtures inside `.test.ts` and not leftovers.

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log (R-01 … R-04)
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-08-15
