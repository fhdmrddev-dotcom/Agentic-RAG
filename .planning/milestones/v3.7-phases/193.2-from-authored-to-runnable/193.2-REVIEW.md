---
phase: 193.2-from-authored-to-runnable
reviewed: 2026-08-15T00:00:00Z
depth: standard
diff_base: c1a6c122
files_reviewed: 14
files_reviewed_list:
  - backend/app/db/workflows.py
  - backend/app/models/harness.py
  - backend/app/services/harness/publish_service.py
  - backend/app/services/workflow_authoring.py
  - frontend/src/components/workflows/builderStore.ts
  - frontend/src/pages/WorkflowBuilderPage.tsx
  - backend/tests/integration/test_193_2_authoring_frequency.py
  - backend/tests/unit/test_publish_service.py
  - backend/tests/unit/test_workflow_authoring_requirement.py
  - backend/tests/unit/test_workflows_updated_at.py
  - frontend/src/components/workflows/PublishGauntlet.test.tsx
  - frontend/src/components/workflows/library/libraryFilter.test.ts
  - frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx
  - frontend/src/pages/WorkflowsPage.test.tsx
findings:
  critical: 0
  warning: 6
  info: 5
  total: 11
status: issues_found
fixed_at: 2026-08-15
fixed_scope: >-
  THREE targeted passes, NOT a full sweep. Pass 1 = WR-01 + WR-03; pass 2 = WR-02 + WR-04;
  pass 3 = WR-05 + WR-06. ALL SIX WARNINGS ARE NOW CLOSED. All five Info findings were
  excluded from ALL THREE passes by instruction and remain OPEN.
fixed:
  - WR-01
  - WR-02
  - WR-03
  - WR-04
  - WR-05
  - WR-06
open:
  - IN-01
  - IN-02
  - IN-03
  - IN-04
  - IN-05
---

> ## ⚠ THREE FIX PASSES — 2026-08-15 — **ALL SIX WARNINGS ARE RESOLVED**
>
> `WR-01` … `WR-06` are **resolved** (see the ✅ blocks on each).
> **All five Info findings are DELIBERATELY LEFT OPEN** — they were excluded from ALL
> THREE passes by instruction, not overlooked, and they await a separate decision.
> `status:` above stays `issues_found` for that reason.
>
> | Finding | State | Commit | Pass |
> |---|---|---|---|
> | WR-01 — F-5's publish half is a tautology | ✅ **fixed** | `82d622ad` | 1 |
> | WR-02 — `_clean_label` misses Unicode Cf | ✅ **fixed** | `c4fe1066` | 2 |
> | WR-03 — `ORDER BY` has no tiebreaker | ✅ **fixed** | `e09e3a13` | 1 |
> | WR-04 — sort fence misses non-mutating forms | ✅ **fixed** | `accf9539` | 2 |
> | WR-05 — "deferred Phase-103 rework" prose survives | ✅ **fixed** | `ab7e8ca2` | **3** |
> | WR-06 — docblock credits an ineffective mitigation | ✅ **fixed** | `3265dc80` | **3** |
> | IN-01 … IN-05 | ⬜ **OPEN** | — | — |
>
> **⚠ PASS 3 CHANGED NO BEHAVIOUR AT ALL — both fixes are prose, and that is the point
> rather than a caveat.** Between them they are 58 inserted lines and 3 deleted, every
> one a `#` comment; no predicate, no literal, no control flow, no stage and no test
> moved. Both findings were about a CLAIM a shipped comment makes — one promising an
> unscheduled capability (`SEED-164`'s founding error, still shipping 390 lines above the
> docstring it was removed from), one crediting a control with a mitigation the phase's
> own measurement recorded as 0-of-5 effective. **In both, the correction is written
> BESIDE the superseded text rather than over it**, so the drift stays auditable — the
> same habit as `193.2-VALIDATION.md`'s C-F5 and the WR-03 fence amendment. **The
> question each fix had to answer first was whether a SHIPPED fence saw the text**; the
> answer was NO in both cases, established by enumerating every test that reads either
> module's source rather than by assuming, and stated in each commit message. Nothing was
> updated in place and nothing needed driving RED, because nothing's view changed.
>
> **Gates after pass 3** (main working tree, quiet, all four RE-RUN — not inherited):
> count gate `OK · total 3918 · failed 0 · 75/75 pinned` (**first run, no re-run
> needed**); `tsc -p tsconfig.app.json` **33**, unmoved; the seven backend workflow suites
> **210 passed / 0 failed**, unmoved from pass 2; full `backend/tests/unit` **62 failed /
> 2221 passed** — the failure count identical to baseline AND the failing FILE set
> enumerated and identical (17 files: 15 `test_retrieval_service`, 12 `test_sql_service`,
> 6 `test_explorer_agent`, 5 `test_multimodal_query`, … the recorded SEED-056-class rot
> set; **not one is a workflow / harness / authoring / publish file**). Frontend counts
> are untouched — pass 3 is backend-only and comment-only.
>
> **⚠ BOTH PASS-2 FINDINGS WERE THE SAME DEFECT SHAPE, and that is the durable lesson
> rather than either fix.** In each, a guard's TEST was derived from the guard's own
> predicate, so the test could not see the class the guard missed — `_clean_label`'s case
> restated `ord(ch) < 32 or ord(ch) == 127`, and the sort fence's only real plants used the
> one shape the needle was already written for. Fixing the guard without fixing that
> circularity leaves the next gap equally invisible, so in both cases the test now asserts
> against an **independent, explicit list of hostile inputs**: a 31-codepoint literal table,
> and a 16-shape forbidden list with a 10-shape legal list beside it. This phase has now
> found **SIX fences that were green while defending nothing**; both of these make seven and
> eight, and both were caught by PLANTING rather than by reading.
>
> **Gates after pass 2** (main working tree, quiet, all four RE-RUN — not inherited):
> count gate `OK · total 3918 · failed 0 · 75/75 pinned` (**first run, no re-run needed**);
> `tsc -p tsconfig.app.json` **33**, unmoved; the seven backend workflow suites **210 passed
> / 0 failed** (= the inherited 143 + 67 new `WR-02` cases, exactly); full
> `backend/tests/unit` **62 failed / 2221 passed** — the failure COUNT is identical to
> baseline and the failing FILES were enumerated to confirm the set is too (15
> `test_retrieval_service`, 12 `test_sql_service`, 6 `test_explorer_agent`, … the recorded
> SEED-056-class rot set). **No NEW backend failure, and no failure in any file either pass
> touched.** Frontend case counts are UNCHANGED by pass 2 — every `WR-04` widening is in
> place — so no count-gate pin moved.
>
> **Gates after pass 1** (recorded as measured then, not restated): count gate `OK · total
> 3918 · failed 0 · 75/75 pinned`; `tsc` **33**; the seven backend workflow suites **143
> passed / 0 failed**; full `backend/tests/unit` **62 failed / 2154 passed**. ⚠ The count
> gate read `failed 2` on its FIRST run — both bare `STACK_TRACE_ERROR` timeouts, in
> `WorkflowBuilderPage.canvas.test.tsx` and `WorkflowsPage.test.tsx`; **filenames captured
> before re-running**, per the CLAUDE.md rule. Neither could be that pass's: `git diff
> 836758c9..HEAD --name-only -- frontend` was **EMPTY** — pass 1 is backend-only — and both
> files are already on the record as flaky (`193.2-BASELINE.md` §4, `193.2-03-SUMMARY.md`'s
> three runs).

# Phase 193.2: Code Review Report

**Reviewed:** 2026-08-15
**Depth:** standard (per-file, plus targeted cross-file tracing on the three changed seams)
**Files Reviewed:** 14 (6 source · 8 test)
**Status:** issues_found — **0 Critical · 6 Warning · 5 Info**

## Summary

Three defects, three seams: an `ORDER BY` change on two of three library feeds, a rewritten
publish-refusal string with a new label sanitiser, and a server-stamped provenance flag with a
client-side demote and a visible mark.

**What I could not break, stated so the clean result is legible rather than assumed:**

- **The RLS-bypassing boundary is genuinely untouched.** I read all three feed predicates at HEAD.
  `list_published_workflows`' two `WHERE` branches, its `$N` project filter, and
  `list_draft_workflows`' `WHERE status = 'draft' AND created_by = $1` are byte-identical to what
  shipped. The `ORDER BY` is appended *after* the predicate in both, and both clauses are static
  literals — **no user input reaches the sort**, so there is no injection surface and no widening.
- **The provenance flag is not trusted from the model, in either direction.** The stamp
  (`workflow_authoring.py:520-530`) is a `model_copy` computed from the *value*, applied after
  validation on the single success path; a payload claiming `false` beside real text is overwritten
  to `true`, and one claiming `true` beside `"   "` is overwritten to `false`. Both directions are
  driven, not asserted (`test_workflow_authoring_requirement.py:812`, `:836`).
- **No new XSS or format-string surface.** The refusal reaches React as a text child (verified:
  `PublishGauntlet.tsx:933-940` renders `{blockedReason}`; no `dangerouslySetInnerHTML` on the
  path), and the docblock's claim that format-string injection is impossible is **correct** — the
  value is an argument to `.format()`, never itself formatted.
- **Suites are green as claimed.** I ran them: `test_workflows_updated_at.py` +
  `test_publish_service.py` + `test_workflow_authoring_requirement.py` → **113 passed**;
  `libraryFilter.test.ts` + `PublishGauntlet.test.tsx` + `WorkflowsPage.test.tsx` → **165 passed**.

**The headline finding is a fence that cannot fire.** F-5's publish half
(`test_publish_service.py:772`) compares `_interactive_phase_failures(definition)` against
`_interactive_phase_failures(definition)` — the same function, the same input, twice. It is a
tautology. It can never go red, and `/validate`'s real handler is drivable in unit tests (three
suites already do it), so the property F-5 names has **zero** server-side coverage. This is the
fifth-plus instance of the class this phase set out to hunt.

The remaining Warnings are: a sanitiser whose stated invariant does not hold for Unicode format
characters (and a test that shares the blind spot); an `ORDER BY` with no tiebreaker against a live
DB where 39 published rows share one timestamp; a sort fence evadable by every non-mutating sort
form; the exact "deferred Phase-103 rework" prose D-14 exists to eliminate, still shipping 390 lines
above the docstring it was removed from; and a control credited in a shipped docblock with a
mitigation the phase's own measurement recorded as ineffective.

**Known constraints honoured, verified not flagged:** the publish gate is unwidened
(`external_action` still absent from `_interactive_phase_failures`); no test or comment in the diff
claims `business_requirement` is *always* populated; the three-state fallbacks are intact;
`WorkflowCard.tsx` is untouched; refusal messages measured at ≤ 196 chars.

---

## Critical Issues

None found.

---

## Warnings

### WR-01: F-5's publish half is a tautology — it asserts `f(x) == f(x)` and cannot fail

**File:** `backend/tests/unit/test_publish_service.py:772-794`
**Severity:** WARNING

**Issue.** The case is named
`test_f5_validate_and_the_publish_refusal_emit_the_same_bytes` and its docstring claims *"Computed
rather than hard-coded: a hard-coded expectation would pass while the two callers drifted apart,
which is the whole property under test."* But both sides of the comparison are the **same call**:

```python
publish_failures  = publish_service._interactive_phase_failures(definition)
validate_findings = [
    {"code": "interactive_phase", "phase": f.get("phase"), "message": f.get("message")}
    for f in publish_service._interactive_phase_failures(definition)   # ← same function
]
assert [f["message"] for f in publish_failures] == [f["message"] for f in validate_findings]
```

`_interactive_phase_failures` is a pure function of `definition`. The assertion is `x == x`. The
test **never imports `app.api.workflows`** and never executes the `/validate` handler — it
re-implements the route's projection inline. Nothing about the real `/validate` path is under test.

**Concrete failure scenario.** A future edit to `workflows.py:896-901` — clamping the message for
the canvas, prefixing a code, mapping through a lookup table, or switching to a different
composer — silently breaks D-12 ("ONE message string feeds both surfaces"). This test stays green,
the F-5 row in `193.2-VALIDATION.md` still reads ✅, and the divergence ships. Because the client
renders `verdict.message` **verbatim**, a drift here is directly user-visible.

**Compounding.** `193.2-VALIDATION.md`'s F-5 row names
`backend/tests/unit/test_187_route_assigned_reach.py` as carrying the other half with `⚠ case new`.
Measured: `git diff --stat c1a6c122..HEAD -- backend/tests/unit/test_187_route_assigned_reach.py`
returns **nothing** — that file received zero changes in this phase. So neither half of F-5 gained a
case that can observe drift.

**Fix.** Drive the real handler. The seam already exists and three suites use it
(`test_182_validate.py:117-126`, `test_182_grounding_degradation.py:244`,
`test_182_publish_grounding_stage.py:374`):

```python
async def test_f5_validate_and_the_publish_refusal_emit_the_same_bytes(kind, name):
    from app.api import workflows as wf
    definition = _interactive_definition(kind=kind, name=name)

    publish_messages = [f["message"] for f in
                        publish_service._interactive_phase_failures(definition)]

    response = await wf.validate_workflow(          # the REAL route
        body=definition, current_user=dict(_USER), supabase=object()
    )
    validate_messages = [v.message for v in response.verdicts
                         if v.code == "interactive_phase"]

    assert publish_messages and validate_messages
    assert publish_messages == validate_messages
```

Then **drive it RED**: plant `"message": f.get("message")[:20]` at `workflows.py:899`, observe the
failure, restore. A fence not observed red has not been shown to see anything — this phase's own
rule.

> ### ✅ RESOLVED — 2026-08-15 — commit `82d622ad`
>
> **What changed.** `test_f5_validate_and_the_publish_refusal_emit_the_same_bytes` was
> **rewritten in place** (never deleted; the superseded body is quoted VERBATIM in the new
> docstring, per this repo's habit) to execute the REAL `validate_workflow` handler and
> compare its `interactive_phase` verdict `message`s against what the publish path composes.
> Two different call paths, one expected value. A new `_validate_via_the_real_route` helper
> follows the posture the three suites the review named already use, and stubs
> `assemble_grounding_bundle` so the fence measures the route's NORMAL path rather than its
> documented `except Exception` degraded one. `phase` is asserted separately from `message`,
> because the slug is metadata on both surfaces while only `message` is copy.
>
> **Driven RED, twice, against real plants in production source — not reasoned about:**
>
> | # | Plant (in `backend/app/api/workflows.py`) | Observed | Restore |
> |---|---|---|---|
> | 1 | `f.get("message")[:20]` in the step-(4) projection — the review's own suggestion | **all 4 parametrized shapes FAIL** | `git checkout --`, `git diff --numstat` **EMPTY**, 4 passed |
> | 2 | step (4)'s generator source emptied (`for f in ()`) | **all 4 FAIL** on the non-vacuity clause — a dropped check cannot pass as an equality between two empty lists | diff **EMPTY** |
>
> ⚠ **And the counter-measurement, which is the part that proves the old fence was blind:**
> under the IDENTICAL plant 1, a scratch replica of the pre-rewrite body ran **GREEN on all
> four shapes**. Measured, not inferred.
>
> **The false credit is corrected too**, in `193.2-VALIDATION.md`, **BESIDE** the previous
> claim rather than over it (strikethroughs preserve what the row said) — new correction
> **C-F5**. `git diff c1a6c122..HEAD --stat -- backend/tests/unit/test_187_route_assigned_reach.py`
> returns **nothing**, confirming the review's measurement. C-F5 names what actually covers
> each half: the rewritten publish case (server) and `PublishGauntlet.test.tsx:986+`'s
> `193.2-06` `?raw` one-home fence (client). **`test_187_route_assigned_reach.py` is not
> modified** — the defect was the credit, not the file.
>
> **Verification.** `test_publish_service` + `test_182_validate` +
> `test_182_grounding_degradation` + `test_187_route_assigned_reach` → **109 passed**; the
> seven workflow suites **143 passed / 0 failed** (case count unmoved — rewritten in place,
> not added to); full backend unit **62 failed / 2154 passed**, identical to baseline. No
> source file is modified by this commit.

---

### WR-02: `_clean_label`'s "no control character / one line" invariant does not hold for Unicode format characters — and the test shares the blind spot

**File:** `backend/app/services/harness/publish_service.py:556-585`
(test: `backend/tests/unit/test_publish_service.py:735-752`)
**Severity:** WARNING
**Status:** ✅ **RESOLVED — 2026-08-15, fix pass 2, commit `c4fe1066`.** See the block at the end
of this finding. ~~⬜ OPEN — deliberately not fixed in the first pass (scope was WR-01 + WR-03
only); `_clean_label` and its test are unchanged and the Unicode-Cf blind spot stands.~~ (The
superseded status line is struck through rather than deleted — this file's own habit.)

**Issue.** The scrubber collapses only two classes:

```python
scrubbed = "".join(" " if (ch.isspace() or ord(ch) < 32 or ord(ch) == 127) else ch for ch in raw)
```

Unicode **category-Cf** characters are neither `isspace()` nor `< 32`. Measured against the shipped
function:

| char | name | `isspace()` | survives `_clean_label` |
|---|---|---|---|
| `U+200B` | zero-width space | False | **yes** |
| `U+202E` | RIGHT-TO-LEFT OVERRIDE | False | **yes** |
| `U+2066` | LEFT-TO-RIGHT ISOLATE | False | **yes** |
| `U+00AD` | soft hyphen | False | **yes** |

The docstring says the function delivers *"the single-line, length-bounded form"* and the
`_LABEL_MAX_CHARS` block says *"What IS defended here is LENGTH and ONE-LINE SHAPE."* Neither holds
for these.

**Concrete failure scenario.** A phase named `Approve the draft<U+202E>` produces a refusal whose text
after the label renders right-to-left in the browser — including the actionable clause *"Remove that
step to publish."* The same string is persisted verbatim into
`harness_audit.metadata.named_failures`, so the audit ledger inherits the mangling. `U+200B` in a
name additionally makes the clamp arithmetic and any operator `grep` disagree with what is on
screen.

**Why the test cannot catch it.** `test_t193201_a_newline_bearing_name_yields_a_single_line_message`
asserts `not any(ord(ch) < 32 or ord(ch) == 127 for ch in message)` — the **identical predicate** to
the code under test. A test that restates the implementation's own condition cannot discover a class
that condition misses.

**Severity note.** Not Critical: the phase `name` is authored by the workflow's own owner or by the
authoring model on that owner's behalf, and the message is shown back to that same owner, so no
privilege boundary is crossed. It is a correctness/robustness defect in a function whose entire job
is sanitisation.

**Fix.** Use the Unicode category, not the codepoint range:

```python
import unicodedata

scrubbed = "".join(
    " " if (ch.isspace() or unicodedata.category(ch) in ("Cc", "Cf", "Cs", "Co", "Zl", "Zp"))
    else ch
    for ch in raw
)
```

and widen the test's own predicate to the same category set, then drive it RED against a planted
`"Approve<U+202E>the draft"` before trusting it.

> ### ✅ RESOLVED — 2026-08-15 — commit `c4fe1066`
>
> **⚠ ONE HALF OF THE SUGGESTED FIX WAS ADOPTED AND THE OTHER HALF WAS DELIBERATELY NOT.**
> The category set landed as suggested (`Cc`, `Cf`, `Cs`, `Co`, `Zl`, `Zp`, as
> `_NON_PRINTING_CATEGORIES`). But *"widen the test's own predicate to the same category
> set"* would have **rebuilt the identical circularity one level up** — a test derived from
> the implementation's rule still cannot see a class that rule misses. So the test does the
> opposite: it asserts an INDEPENDENT literal table.
>
> **THE GUARD — by class, and the exclusions are auditable rather than implicit.** The
> constant's own comment names every excluded category *with its reason*, and names the two
> that are deliberately **not** excluded: `Cn` (unassigned — `unicodedata` is pinned to the
> build's Unicode version, so a character assigned in a LATER version reads as `Cn` here and
> excluding it would mangle a legitimate label in a newer script; unassigned codepoints
> render as tofu, they do not spoof) and `Mn` (excluding non-spacing marks would destroy
> ordinary Arabic, Hindi, Hebrew and Vietnamese step names; "Zalgo" stacking is bounded by
> `_LABEL_MAX_CHARS` instead). `Cc` is exactly the retired `ord(ch) < 32 or ord(ch) == 127`
> test, so nothing the old rule caught is let through.
>
> **THE MEASUREMENT THE REVIEW'S FOUR-ROW TABLE IS A SAMPLE OF.** Driven against the shipped
> function over a 31-codepoint table: **22 survived, and every single survivor was category
> `Cf`.** The other nine (7 × `Cc` + `Zl` + `Zp`) the old rule did catch. The review's four
> are all in the 22.
>
> **THE TEST — an INDEPENDENT fixed literal table, asserted row by row.**
> `_HOSTILE_CODEPOINTS` is 31 rows, each `(codepoint, name)`, **written as `\uXXXX` escapes
> and never as raw characters** — a raw bidi override in a file that becomes agent context is
> the very hazard being fixed, and the escape rule is pinned by a well-formedness case that
> also asserts ≥ 30 rows, no duplicates, and that all four of the review's own repro
> codepoints are present (so nobody can "fix" this down to the four that happened to be
> tried). Each codepoint is asserted INDIVIDUALLY against the composed refusal, so a future
> narrowing reds a row that NAMES what it stopped catching. The superseded assertion is
> quoted verbatim in the amended `test_t193201_…_single_line_message` docstring rather than
> deleted.
>
> **DRIVEN RED TWICE against real plants in production source**, restored from a saved
> pristine copy and re-run green (122 passed) after each:
>
> | # | Plant in `publish_service.py` | Observed |
> |---|---|---|
> | 1 | the scrub reverted to the pre-`WR-02` predicate | **46 failed** = the 22 `Cf` rows × 2 arms + both all-invisible degradation cases |
> | 2 | the naive *"fix the four codepoints the reviewer named"* scrub | **38 failed** = the OTHER 18 `Cf` rows × 2 + both degradations |
>
> ⚠ **Plant 2 is the one that matters.** Under it the four codepoints this review names all
> PASS — so it is precisely the fix a reader who trusted the finding's table would have
> written, and the test refuses it. That is the by-class property proved rather than claimed.
>
> **CONSTRAINT 1 — LENGTH, RE-MEASURED RATHER THAN ASSUMED.** Scrubbing shortens (each
> character becomes a space, runs then collapse), but the clamp and the scrub are coupled, so
> both were re-driven over five shapes including a 10,000-character `U+202E`/`U+200B`-stuffed
> name. **Worst case 196 on arm 1 / 195 on arm 2 — IDENTICAL to the pre-fix figures in
> `193.2-06-SUMMARY.md`**, against that summary's 250 ceiling. Nothing grew. Now pinned by
> its own case.
>
> **CONSTRAINT 2 — THE REFUSAL STILL NAMES THE CANVAS LABEL.** Every codepoint row also
> asserts the readable label survives the scrub, and a new boundary case pins that a name
> made ONLY of invisibles takes the honest name-free arm — never `the step "" stops to wait
> for a person`, and never the slug (`zz-plant-slug` asserted absent). No `llm_human_input` /
> `ask_user` / slug / bare step number reaches copy; F-3 and the D-13 no-digit case are
> untouched and green.
>
> **Verification.** `test_publish_service.py` **122 passed**; the seven backend workflow
> suites **210 passed / 0 failed** (143 + 67 new); full `backend/tests/unit` **62 failed /
> 2221 passed**, the failing FILE SET enumerated and identical to baseline. `IN-02` (the
> `.rstrip()` off-by-one in the clamp comment) is **NOT** fixed here and stays open — it is
> an Info finding and was out of scope.

---

### WR-03: `ORDER BY updated_at DESC` has no deterministic tiebreaker — 39 live published rows share one timestamp

**File:** `backend/app/db/workflows.py:368` (published) and `:660` (drafts)
**Severity:** WARNING

**Issue.** Both feeds order on `updated_at` alone. `updated_at` is not unique, and Postgres'
`now()` is **transaction-scoped**, so every row touched by one migration or one bulk update shares
an identical timestamp. Measured against the live local DB (`127.0.0.1:54322`, 225 rows):

```
published rows sharing one updated_at: 39  (2026-07-18 20:43:42.856183+00)
                                        2  (2026-07-30 20:16:20.222893+00)
draft     rows sharing one updated_at: 24  (2026-07-18 20:43:42.856183+00)
```

With no tiebreaker, the relative order of those 39 rows is **unspecified**. The previous
`ORDER BY name` was total in practice (names are near-unique); the new clause is not.

**Concrete failure scenario.** The Workflows page refetches on publish, on back-nav, and on project
re-select. Between two fetches Postgres may return the 39 co-timestamped rows in a different
sequence (the heap order the sort consumes changes whenever any row in the table is updated). The
user sees a third of their library silently reshuffle after an unrelated save. The same feed is the
composer's Harness picker, so the picker reshuffles too. The recency *goal* still works — a
just-published row has a unique fresh timestamp and lands correctly — but everything below the
recent band is unstable.

**Fix.** Make the sort total. One column, no migration, no plan-shape change (there is no index on
`updated_at` either, so nothing regresses):

```python
sql += " ORDER BY updated_at DESC, id DESC"
```

```python
f"ORDER BY updated_at DESC, id DESC",     # list_draft_workflows
```

`id` is `NOT NULL` + unique. Update F-1's needle in `test_workflows_updated_at.py:459-470` to the
new clause and re-drive its RED probe against the pre-change source.

*Unverified:* I did not reproduce an actual reshuffle — Postgres is free to be stable in practice on
a small table. The tie itself is measured; the observable reorder is a predicted consequence.

> ### ✅ RESOLVED — 2026-08-15 — commit `e09e3a13`
>
> **What changed.** Both author feeds now read `ORDER BY updated_at DESC, id DESC` — the
> review's suggested clause, adopted verbatim. The non-comment diff on `db/workflows.py` is
> **exactly two clause lines**: zero `WHERE` branches, zero `params` appends, zero `$N`
> bindings, zero projections moved. On a pool that bypasses RLS the predicate is the access
> boundary, and it is byte-identical.
>
> **The review's census reproduced exactly** against the live local DB (`127.0.0.1:54322`,
> 225 rows): **39** published rows share `2026-07-18 20:43:42.856183+00` (2 more share
> another instant), **24** draft rows share the same one.
>
> **`id` verified rather than assumed:** it is `workflow_definitions_pkey`, the PRIMARY KEY —
> therefore `NOT NULL` and unique (read from `pg_constraint` / `pg_attribute`; `count(*) ==
> count(distinct id) == 225`, zero nulls) — and it is **already in the SELECT list of both
> changed feeds**, so the projection does not widen by one byte. No migration, no index, no
> schema change.
>
> **It orders WITHIN ties and nowhere else — measured, which the review left as a predicted
> consequence.** Both clauses were driven over all three live predicate shapes (drafts 78
> rows, owned published 28, published-with-globals 91): the sequence of `updated_at` *values*
> is identical with and without the tiebreaker, the top row is unchanged, **every position
> that moved sits inside a tie group (zero outside)**, and two consecutive runs agree
> exactly. The four real feed functions were then driven against the live DB: valid SQL,
> non-increasing recency, run-to-run identical.
>
> **Constraint 1 honoured — the starters feed is byte-untouched.** D-16's divergence stands;
> it keeps `ORDER BY name`, is not given recency, and is not given the tiebreaker either
> (asserted). Live: `['Compliance Gap Report', 'Risk Register', 'Weekly Status Report']`.
>
> **Constraint 4 honoured — the pending UAT prediction still holds.** A freshly published row
> carries a unique fresh timestamp, is in no tie, and still lands first; rendered position
> **4 of 109** re-derived from the live feeds (3 starters + 28 owned published + 78 drafts).
>
> **The D-16 divergence fence was updated IN PLACE and DRIVEN RED.** Its `SUPERSEDED` block is
> untouched and the WR-03 amendment sits **beside** the prior reasoning, not over it. ⚠ **The
> needle is the FULL clause on purpose:** `"ORDER BY updated_at DESC"` is a **PREFIX** of the
> new clause, so the pre-amendment needle stays green against a feed with no tiebreaker at
> all — it could not have fired on this property. Run against the pre-change source, the
> published and draft arms both FAIL; a clause-by-clause probe (Python `assert`
> short-circuits) confirms **both** new clauses red independently, while the three starter
> clauses pass before *and* after — correct, because those are preservation assertions, not
> change assertions.
>
> **Verification.** `test_workflows_updated_at.py` **25 passed** (count unmoved — rewritten,
> not added to); the three `*_predicate*_byte_identical_to_what_shipped` cases unedited and
> green; the seven workflow suites **143 passed / 0 failed**; full backend unit **62 failed /
> 2154 passed**, identical to baseline. `supabase/migrations/` unchanged at 112.

---

### WR-04: the F-7-extended sort fence misses every non-mutating sort form

**File:** `frontend/src/components/workflows/library/libraryFilter.test.ts:768-772`, `:810-820`
**Severity:** WARNING
**Status:** ✅ **RESOLVED — 2026-08-15, fix pass 2, commit `accf9539`.** See the block at the end
of this finding. ~~⬜ OPEN — deliberately not fixed in the first pass (scope was WR-01 + WR-03
only); the needle is unchanged and `.toSorted()` / `.reverse()` still escape both clauses.~~

**Issue.** The needle is

```js
/\brows\.sort\(|\bvisibleRows\.sort\(|\bmerged\.sort\(|\bfamily\.sort\(|\blist\.sort\(/
```

Run against realistic shapes:

| candidate | caught by needle | caught by the `.sort(` corpus check |
|---|---|---|
| `rows.sort(cmp)` | ✅ | ✅ |
| `[...rows].sort(cmp)` | ❌ | ✅ |
| `[...visibleRows].sort(cmp)` | ❌ | ✅ |
| `visibleRows.slice().sort(cmp)` | ❌ | ✅ |
| `Array.from(rows).sort(cmp)` | ❌ | ✅ |
| `visibleRows.toSorted(cmp)` | ❌ | **❌** |
| `[...rows].reverse()` | ❌ | **❌** |

`Array.prototype.sort` **mutates**, so a React developer re-ordering a memoised list will almost
certainly write `[...visibleRows].sort(...)` — i.e. the needle catches only the one form the code
style discourages. The property survives today solely because of the companion assertion
`NON-VACUITY — the swept corpus is EMPTY today` (`:810`), which forbids *any* `.sort(` in either
module. That companion has its own problem: it directly contradicts the needle's docblock, which
states *"a sort that names a narrow thing it sorts … is legal"*. The day either module legitimately
sorts something narrow, the "non-vacuity" case reds on correct code, someone relaxes it, and the
only real coverage disappears — leaving the evadable needle alone.

`.toSorted()` (ES2023, widely available in the toolchain's target) and `.reverse()` escape both.

**Fix.** Make the needle shape-based rather than receiver-name-based, and let the corpus assertion
allow annotated narrow sorts:

```js
const FORBIDDEN_ROW_SORT =
  /(\brows\b|\bvisibleRows\b|\bmerged\b|\bfamily\b|\blist\b)\s*\]?\s*\)?\s*\.(sort|toSorted|reverse|toReversed)\(|\]\.(sort|toSorted|reverse)\(/
```

and drive it RED against real plants of **each** of `[...visibleRows].sort(`,
`visibleRows.toSorted(` and `[...rows].reverse()` in production source, restoring after each — the
inline `expect(planted).toMatch(...)` controls prove the regex, only a real plant proves the fence
points at the right file.

> ### ✅ RESOLVED — 2026-08-15 — commit `accf9539`
>
> **⚠ THE SUGGESTED REGEX WAS TESTED AND REJECTED, and the reason is a measurement rather
> than a preference — it would have redded a green module.** The suggested alternation ends
> `|\]\.(sort|toSorted|reverse)\(`, which matches ANY `].sort(`. `rowIdentity.ts:386` ships
> `const ascending = [...new Set(versions)].sort((a, b) => a - b)` — a **legal** narrow sort
> of a slug's version numbers. The suggestion's own second half ("guard against the opposite
> failure") is what caught its first half.
>
> **THE RULE IS NOW THE SHAPE, in four arms anchored on a row-list RECEIVER** rather than on
> a free character gap: (a) direct `rows.sort(` / `visibleRows.toSorted(` / `merged.reverse()`
> · (b) spread copy `[...rows].sort(` / `[...starters, ...merged].toReversed(` · (c)
> `Array.from(rows).sort(` / `Array.from(new Set(rows)).reverse(` · (d) method copy
> `rows.slice().sort(` / `visibleRows.filter(p).toSorted(`. Four reorder methods, not one:
> `sort` · `toSorted` · `reverse` · `toReversed`. ⚠ An intermediate draft DID use a free gap
> (`NAME …{0,40}? [\])] . REORDER(`) and was measured FALSE-POSITIVE on
> `if (rows.length) return items.map(f).sort(g)` — which is now a permanent negative-control
> row in the fence, so the rejected draft cannot quietly come back.
>
> **⚠ BOTH HOMES WERE FIXED, NOT THE FIRST HIT.** This finding cites
> `libraryFilter.test.ts:768`. `rowIdentity.test.ts`'s T-7 / P-4 case (`:167-179`) carries the
> SAME rule over a THIRD module and had the IDENTICAL blind spot — its needle was
> `/\brows\.sort\(|\bfamily\.sort\(|\blist\.sort\(/`. Widened in the same commit, with a
> cross-reference in both files; they stay duplicated deliberately, because they read
> different `?raw` scopes and neither may import the other's.
>
> **THE COMPANION ASSERTION'S PROBLEM IS ADDRESSED TOO, in the direction the finding did not
> anticipate.** The corpus check was `line.includes(".sort(")`, so `.toSorted(` and
> `.reverse()` escaped **both** layers — on the two shapes a developer is most likely to
> write, the property rested on nothing at all. It is now `ANY_REORDER_CALL`, covering all
> four methods. It never reads a receiver name, so it is the totalising half of the pair and
> cannot be evaded by renaming while it holds.
>
> **⚠ THE MISSED IDIOM IS NOT HYPOTHETICAL IN THIS REPOSITORY.** A sweep of all **318**
> non-test modules under `frontend/src` finds `[...list].sort((a, b) =>` shipping today at
> `SkillEvalSection.tsx:54` and `EvalsTab.tsx:51`. Neither is in this fence's scope — they are
> the evidence that the escaping shape is the NATURAL one, not a contrived evasion.
>
> **DRIVEN RED AGAINST TEN REAL PLANTS, ONE SHAPE AT A TIME**, each in production source, each
> restored to an EMPTY `git diff --numstat` before the next — all seven of this finding's own
> shapes, landed in whichever module binds that receiver, plus three against `rowIdentity.ts`:
>
> | # | Plant | Where | Observed |
> |---|---|---|---|
> | 1 | `rows.sort(cmp)` | `libraryFilter.ts` | **2 failed** — ABSENCE + NON-VACUITY |
> | 2 | `[...rows].sort(cmp)` | `libraryFilter.ts` | **2 failed** |
> | 3 | `[...visibleRows].sort(cmp)` | `WorkflowsPage.tsx` | **2 failed** |
> | 4 | `visibleRows.slice().sort(cmp)` | `WorkflowsPage.tsx` | **2 failed** |
> | 5 | `Array.from(rows).sort(cmp)` | `libraryFilter.ts` | **2 failed** |
> | 6 | `visibleRows.toSorted(cmp)` | `WorkflowsPage.tsx` | **2 failed** |
> | 7 | `[...rows].reverse()` | `libraryFilter.ts` | **2 failed** |
> | 8-10 | `[...rows].sort(` · `rows.toSorted(` · `[...rows].reverse()` | `rowIdentity.ts` | **1 failed each** — T-7 / P-4 |
>
> ⚠ **AND THE COUNTER-MEASUREMENT, which is what proves the old fence was BLIND rather than
> merely narrow.** Under the IDENTICAL plants, the pre-fix fence reported **`132 passed
> (132)` — fully green** for `visibleRows.toSorted(` and for `[...rows].reverse()`; and for
> `[...rows].sort(` only the corpus check fired while the needle itself stayed silent.
> Measured, not inferred.
>
> **THE SWEEP FOR FALSE POSITIVES — SCOPE STATED.** The widened needle was run over (a) the
> **three** files the two fences actually read (`libraryFilter.ts`, `WorkflowsPage.tsx`,
> `rowIdentity.ts`) and (b) the **whole 13-module non-test `library/` subtree`**: **ZERO
> hits** in both, with `rowIdentity.ts`'s two legal narrow sorts untouched by all four arms.
> Sixteen forbidden shapes and ten legal shapes are now permanent in-file controls, the legal
> list including `rowIdentity.ts`'s two ACTUAL shipped lines by their real text.
>
> **⚠ THE STANDING LESSON, recorded in the fence's own header rather than only here.** The
> pre-existing plants (`193.2-04`) DID hit both arms — and both used `rows.sort(` /
> `visibleRows.sort(`, the one shape the needle was already written for. **A plant chosen to
> confirm a needle proves the fence is aimed at the right FILE and nothing about its coverage
> of the RULE. A plant must be chosen to falsify.**
>
> **Verification.** `libraryFilter.test.ts` + `rowIdentity.test.ts` **132 passed** (case
> counts UNCHANGED — every widening is in place, so no count-gate pin moved); those two plus
> `WorkflowsPage.test.tsx` + `PublishGauntlet.test.tsx` **237 passed / 0 failed**; count gate
> `OK · total 3918 · failed 0 · 75/75 pinned` on the FIRST run; `tsc -p tsconfig.app.json`
> **33**, unmoved; `eslint` clean on both files. **No production source file is modified by
> this commit** — `git diff --numstat` on `libraryFilter.ts`, `WorkflowsPage.tsx` and
> `rowIdentity.ts` is EMPTY.

---

### WR-05: the "deferred Phase-103 rework" prose D-14 exists to eliminate still ships, 390 lines above where it was removed

**File:** `backend/app/services/harness/publish_service.py:196-197`
**Severity:** WARNING
**Status:** ✅ **RESOLVED — 2026-08-15, fix pass 3, commit `ab7e8ca2`.** See the block at the end
of this finding. ~~⬜ STILL OPEN after BOTH 2026-08-15 fix passes (pass 1 = WR-01 + WR-03; pass 2 =
WR-02 + WR-04). Excluded by instruction, not overlooked. ⚠ Re-verified after pass 2, which DID
modify `publish_service.py`: the offending comment is still present and still at `:196-198`,
unmoved — pass 2 touched only `_clean_label` and a new constant, both far below it.~~ (The
superseded status line is struck through rather than deleted — this file's own habit.)

⚠ **And one detail of this finding is CORRECTED ON MEASUREMENT, beside it rather than over it:**
the finding says `grep -rn "deferred Phase-103" backend/ frontend/src` *"returns exactly this one
hit"*. It cannot — the phrase is SPLIT ACROSS TWO LINES (`…is the deferred` ends `:197`,
`# Phase-103 rework` begins `:198`), so a line-oriented grep for it returns **nothing at all**.
The needle that finds it is `Phase-103 rework`. The defect is real and unchanged; the command
quoted for reproducing it is not. Whoever fixes WR-05 should not conclude from a silent grep that
it is already done.

**Issue.** The phase removed this sentence from `_interactive_phase_failures`' docstring and
replaced it with a careful D-14-compliant paragraph (*"it is a seed rather than a schedule: nothing
here promises it"*). The **identical sentence** survives verbatim in the stage 2.5 comment:

```python
# Block such definitions PRE-RUN with a named failure (cheap —
# no provider call) so the golden run is never driven for them. The full
# background-job publish that COULD validate interactive phases is the deferred
# Phase-103 rework — this is the honest minimum-viable cut.
```

`grep -rn "deferred Phase-103" backend/ frontend/src` returns exactly this one hit.

**Concrete failure scenario.** This is precisely the failure `SEED-164` was created to record: *"a
docblock calling something 'the DEFERRED Phase-103 rework' made unscheduled work read like a plan
for a year — and Phase 103 shipped long ago."* The next engineer reading stage 2.5 (which is the
entry point — the docstring is 390 lines below) learns that a background-job publish is *deferred*,
i.e. scheduled. It is not; it is `SEED-164`, a seed. Two adjacent blocks in one file now say
opposite things, and the F-4 fence cannot see it because F-4 is deliberately scoped over the
**composed message value**, never over module source.

**Fix.** Replace with the same register the docstring now uses:

```python
# Block such definitions PRE-RUN with a named failure (cheap — no provider call) so the
# golden run is never driven for them. A publish that could VALIDATE an interactive phase
# (a durable pause, a real subscriber) is a named capability need — `SEED-164` — and it is
# a seed, not a schedule. This is the honest minimum-viable cut.
```

Consider extending the F-4 regex to sweep this module's source for `deferred Phase-\d+` as well, with
its own plant.

> ### ✅ RESOLVED — 2026-08-15 — commit `ab7e8ca2`
>
> **⚠ THE `:190-208`-UNCHANGED QUESTION IS RESOLVED BY SEARCHING, AND THE ANSWER IS
> *NOTHING SHIPPED PINS THAT RANGE*.** Plan `193.2-06` left this comment alone because its
> own acceptance criteria required `:190-208` UNCHANGED and it honoured the explicit
> constraint. Whether that constraint still BINDS was the first thing this fix had to
> establish, so every backend test that reads `publish_service.py` source was enumerated
> and read rather than assumed:
>
> | Fence | What it actually reads | Sees this comment? |
> |---|---|---|
> | `test_182_extraction_parity.py:220-224` | two substrings — `business_requirement_missing` PRESENT, an inlined `(definition.business_requirement or "").strip()` ABSENT | **no** |
> | `test_182_severity_codes.py:459` | `_VERDICT_EMIT_RE` (`"code": "…"`) over source with **whole-line `#` comments STRIPPED FIRST** (`_strip_comment_lines`) | **no — by construction** |
> | `test_182_publish_grounding_stage.py:412-423` | `grounding_verdicts` / `stage="grounding_fidelity"` present; `available_tools` / `skill_ref` absent | **no** |
> | `test_190_ctx_org_scoping.py:54` | AST `SimpleNamespace` keyword sets — *"prose that merely mentions `org_id=` in a comment is invisible here"* | **no — by construction** |
>
> No byte-exact literal, no line-range assertion, no `?raw`/`innerHTML` baseline covers
> this region. `test_publish_service.py:508-512` states in its own header WHY: F-3/F-4
> scope over the composed message VALUE and deliberately **not** over
> `inspect.getsource`, because the module's docblocks legitimately quote both forbidden
> identifiers — a source-scoped fence here would be RED on a clean tree. **So the
> constraint existed ONLY as a closed plan's acceptance criterion. It does not bind future
> work, no fence was updated, and none needed driving RED** — this finding's own review
> suggestion to extend F-4 over module source is therefore *declined* for the same reason
> `:508-512` records, and that is stated rather than silently skipped.
>
> **⚠ THE SENTENCE WAS NOT SIMPLY DELETED — `SEED-164` EXISTS BECAUSE DEFERRALS
> EVAPORATE.** Deleting it would have repeated the original error in the other direction:
> a deferral living only in a deleted comment is exactly as invisible as one never
> written. The retired sentence is **QUOTED as SUPERSEDED** (correct-beside-never-over),
> and beside it the comment now states what is true today plus **both** durable handles
> the fix criteria asked for — a pointer
> (`.planning/seeds/SEED-164-a-workflow-that-legitimately-pauses-for-a-person.md`) **and**
> a concrete re-open trigger (SEED-164's own PRIMARY one: *any phase that touches the
> publish gauntlet or `_interactive_phase_failures`*). The register is the docstring's:
> *"a seed rather than a schedule; nothing here promises it."*
>
> **⚠ THE QUOTE IS ON ONE LINE ON PURPOSE, and that closes this finding's own measured
> correction.** As it shipped the phrase was split across `:197`/`:198`, so
> `grep -rn "deferred Phase-103"` returned **nothing** and read as "already fixed". It now
> returns exactly one hit — the SUPERSEDED block — so the next reader who runs that
> command lands on the correction instead of on silence. The reason is recorded in the
> comment itself.
>
> **Scope.** Comment-only: 26 insertions / 3 deletions, every line a `#` comment. No
> message literal, no predicate, no control flow, no stage moved; the surrounding copy
> rules are untouched and nothing promises a future capability. This is a code comment,
> not user-facing copy, and the fix does not overstate it as anything else.
>
> **Verification.** `ast.parse` OK; the seven backend workflow suites **210 passed / 0
> failed**; the four source-fence suites that read this module **42 passed / 0 failed**.
> `test_182_extraction_parity.py`'s two failures encountered en route are **PRE-EXISTING**
> — driven against the pre-change sources at `5701d09d` they fail identically (they
> compare `_assemble_grounding`'s tuple and `render_grounding_prompt`'s output against the
> pre-193.1-11 hedged template header; neither reads `publish_service.py`).

---

### WR-06: the D-07 copy-refusal is credited in a shipped docblock with a mitigation the phase's own measurement recorded as ineffective

**File:** `backend/app/services/workflow_authoring.py:496-512`
**Severity:** WARNING
**Status:** ✅ **RESOLVED — 2026-08-15, fix pass 3, commit `3265dc80`.** See the block at the end
of this finding. ~~⬜ STILL OPEN after BOTH 2026-08-15 fix passes (pass 1 = WR-01 + WR-03; pass 2 =
WR-02 + WR-04). Excluded by instruction, not overlooked. `workflow_authoring.py` was modified by
NEITHER pass; the docblock still credits the copy check with a mitigation `193.2-FREQUENCY.md`
§(c) measured as 0-of-5 effective.~~ (The superseded status line is struck through rather than
deleted — this file's own habit.)

**Issue.** The stamp's docblock frames the describe-copy check as the mitigation for the
judge-weakening residual:

> *"**T-193.2-03 — D-09 ACCEPTS that residual** … The mechanical part of the mitigation is right
> here: a value that is a normalised byte-identical copy of the `describe` text is REFUSED the
> mark, because a durable requirement is a HARDER bar than one run's task instruction, and echoing
> the instruction back is the cheapest way to produce a criterion the judge cannot fail."*

The check is `" ".join(text.split()).casefold()` equality — it catches only a whitespace/case-variant
**verbatim echo**. The phase then measured, in its own artifact
(`193.2-FREQUENCY.md` §(c)):

> *"**openai — 5 of 5** requirements named a one-run parameter. Representative: *"…for **Northwind
> Logistics** covering **Q3 2026** …"* — one run's parameters baked into the workflow's definition
> of done, which is precisely what D-07 asks against."*

and the scoreboard records the mark stamped **5/5** on those same calls. So the control fired on
**0 of 5** of the compositions it is credited with catching, and every one of them carried the
AI-proposed mark.

**Concrete failure scenario.** A reader (or a later phase) trusts the docblock, concludes the
weakened-judge residual is mechanically mitigated, and does not build the real mitigation. The
`answers_business_requirement` criterion continues to be authored, model-side, from one run's
parameters. `FREQUENCY.md` records the truth — but `FREQUENCY.md` is a phase artifact and the
docblock is what a `git blame` reader lands on.

**Fix.** Correct on measurement, *beside* the original rather than over it — this repo's own habit:

```python
# ⚠ MEASURED AFTER THIS WAS WRITTEN, AND CORRECTED HERE RATHER THAN OVERWRITTEN
# (`193.2-FREQUENCY.md` §(c), 2026-08-15): the copy check caught 0 of 5 openai
# emissions that baked one run's parameters into the requirement ("Northwind
# Logistics … Q3 2026"), and all 5 carried the mark. It refuses a VERBATIM echo and
# nothing weaker. It is NOT a durability control; the durability instruction lives in
# the prompt (D-07) and the real backstop is the author's eye on D-06's visible mark.
```

Nothing in code needs to change — a fuzzy threshold here is correctly rejected. The claim does.

> ### ✅ RESOLVED — 2026-08-15 — commit `3265dc80`
>
> **THE FIX IS PROSE ONLY — `git diff --numstat` is `32 0`, and every added line is a `#`
> comment.** The predicate is byte-untouched, the stamp is byte-untouched, and no test
> changed. That is not a shortcut: the finding's own instruction was that a fuzzy metric
> was **deliberately rejected** by plan `193.2-07` (its reason lives in
> `_normalised_for_copy_check`'s docstring — a threshold would refuse provenance to
> requirements that are durable but merely share vocabulary with the describe box, which
> is the ordinary case). The check answers its own question correctly; **all 20 stamps in
> the measured run were correct**, including the five openai ones, because a requirement
> naming one run's parameters IS still genuinely a model's proposal. The defect was the
> CLAIM.
>
> **CORRECTED BESIDE THE ORIGINAL, NEVER OVER IT.** The superseded sentence — *"The
> mechanical part of the mitigation is right here…"* — stands verbatim and unedited; the
> correction is appended under it, so the drift is auditable rather than erased. Same
> shape as `193.2-VALIDATION.md`'s C-F5 and the WR-03 fence amendment.
>
> **THE MEASUREMENT IS CITED BY PATH, NOT PARAPHRASED**, so the next reader checks the
> number instead of trusting the prose:
> `.planning/phases/193.2-from-authored-to-runnable/193.2-FREQUENCY.md` §(c), 20 real
> generations — **openai named a one-run parameter in 5 of 5 `kit10` requirements**
> (*"…for customer Northwind Logistics covering Q3 2026 …"*) with **all 5 stamped
> `seeded_by_ai: True`**, so **the copy check fired on 0 of those 5**. Anthropic's 0-of-5
> is recorded beside it with its reason (it wrote durable requirements, so there was
> nothing there for a control to catch) — otherwise the openai figure reads as a provider
> anomaly rather than as the general point.
>
> **WHAT THE DOCBLOCK NOW SAYS THE CONTROL DOES:** it is an **ANTI-ECHO guard** — it
> refuses the mark to the cheapest possible non-requirement, the describe text handed
> straight back, and to nothing weaker. **It is not a durability control.** It carries the
> phrase the phase settled on: **the AI-proposal mark means "a model wrote this", NEVER
> "this is durable."** Durability lives in the prompt (D-07) and its real backstop is
> D-06's visible mark under the author's eye before they press Publish — which is
> precisely why D-09's acceptance rests on that mark rather than on this predicate.
>
> **⚠ THE SWEEP QUESTION WAS ANSWERED BY SEARCHING, NOT ASSUMED — no shipped fence's view
> changes.** The D-14 token sweep `193.2-09` disclosed
> (`test_workflow_authoring_requirement.py:276-313`; tokens `planned` / `coming soon` /
> `deferred` / `future release`) reads `AUTHORING_SYSTEM_PROMPT` through `_prompt()`, **not
> module source** — which is exactly why `workflow_authoring.py:131-132` can already carry
> *"the DEFERRED Phase-103 rework"* in prose without redding it. This edit adds none of
> those tokens and touches no prompt literal. The only source-scoped fence over this module
> (`test_182_extraction_parity.py:189-210`) asserts four grounding-compute symbols ABSENT
> and two delegate lines PRESENT; none appears in the added text. And no test greps this
> docblock at all — `T-193.2-03` and `_normalised_for_copy_check` return **zero** hits
> across `backend/tests`. So nothing was updated in place and nothing needed driving RED.
>
> **Verification.** `ast.parse` OK; the seven backend workflow suites **210 passed / 0
> failed**; the five authoring/grounding suites (`test_187_authoring_step_names`,
> `test_103_nl_generate`, `test_103_grounding_fidelity`, `test_193_1_grounding_wire`,
> `test_skill_builder_model`) **38 passed / 0 failed**; full `backend/tests/unit`
> **62 failed / 2221 passed** with the failing FILE set enumerated and identical to
> baseline.

---

## Info

> ⬜ **ALL FIVE INFO FINDINGS ARE STILL OPEN.** All THREE 2026-08-15 fix passes (pass 1 =
> `WR-01` + `WR-03`, pass 2 = `WR-02` + `WR-04`, pass 3 = `WR-05` + `WR-06`) were scoped to
> Warnings and touched no Info finding. With the Warnings closed, these five are the ONLY
> findings left on this review.
>
> ⚠ **THREE ARE WORTH RE-READING BEFORE ANYONE ACTS ON THEM, BECAUSE THE CODE UNDER THEM
> MOVED — and the line numbers below are RE-MEASURED at pass 3's HEAD, not inherited.**
> Every one of these defects is byte-untouched; only its address changed.
>
> | Finding | Cited at review time | **Measured now** | Why it moved |
> |---|---|---|---|
> | `IN-01` | `db/workflows.py:353-360` | block shifted | pass 1 ADDED text to the same SHARED-CONSUMER block. The `threads.py` claim IN-01 disputes is unchanged in substance and still wrong |
> | `IN-02` | `publish_service.py:583-584` | ~~`:645-646`~~ → **`:668-669`** | pass 2 rewrote `_clean_label` directly above it (`583` → `645`); pass 3 then inserted 23 net lines at stage 2.5, far above (`645` → `668`). **The `.rstrip()`-runs-after-the-slice defect is byte-untouched and the finding still holds** — pass 2 widened the SCRUB, pass 3 touched only a comment, and neither went near the clamp |
> | `IN-04` | `publish_service.py:502-508` | **`:526-532`** (the `text-[12px] leading-snug` line is at **`:529`**) | pass 3's stage-2.5 comment, 23 net lines above. Both stale citations IN-04 reports are unchanged: the span is still at `PublishGauntlet.tsx:933-940`, not `:932-939`, and still carries no `shrink-0` |
>
> `IN-03` (`WorkflowBuilderPage.tsx:2035-2085`) and `IN-05`
> (`workflow_authoring.py:496-503`) are at their originally cited addresses — **`IN-05`'s
> range in particular is unmoved**, because pass 3's WR-06 correction was inserted at
> `:511` and after, BELOW the format-string/residual sentences IN-05 is about.

### IN-01: the new `db/workflows.py` comment names a consumer that does not consume

**File:** `backend/app/db/workflows.py:353-360`

The SHARED-CONSUMER block states, as *"MEASURED at the time of the change"*, that the ordering
change reaches *"the composer's Harness workflow picker, `WorkspacePanel`'s run-soul and
`threads.py`'s kickoff (`:97` imports it)"*. Measured now:
`grep -n "list_published_workflows" backend/app/api/threads.py` → **one hit, line 97, the import**;
`grep -n "list_published_workflows(" backend/app/api/threads.py` → **no matches**. The symbol is an
unused import; `threads.py` is not a consumer and its behaviour does not change.

**Fix.** Drop `threads.py` from the consequence list (or, better, drop the dead import from
`threads.py:97` in a separate change and say so).

### IN-02: the clamp comment claims an exact length it does not produce

**File:** `backend/app/services/harness/publish_service.py:583-584`

```python
# One-character ellipsis, so the clamped label is EXACTLY _LABEL_MAX_CHARS.
return collapsed[: _LABEL_MAX_CHARS - 1].rstrip() + "…"
```

`.rstrip()` runs *after* the slice, so when the 71-char prefix ends in a space the result is
shorter. Measured against the shipped function with `"A"*70 + " " + "B"*30` → **len 71**, not 72.
Harmless, but the comment is the kind of exact claim this repo pins tests against.

**Fix.** *"…so the clamped label is AT MOST `_LABEL_MAX_CHARS` (a trailing space is stripped before
the ellipsis, so it can be one shorter)."*

### IN-03: a 50-line docblock is now orphaned from the declaration it documents

**File:** `frontend/src/pages/WorkflowBuilderPage.tsx:2035-2085`

The block documenting `requirementAffordance` — BUG-260809-02, the D-186-15 `identityGroup`
placement, the mandatory `canvasEnabled` gate and the byte-pin rationale, the `✎`-not-`✦` glyph
ruling — used to sit immediately above `const requirementAffordance`. The new
`requirementIsAiProposed` docblock (`:2086-2113`) and its `const` (`:2114`) were inserted between
them, so two JSDoc blocks now stack and the older one reads as documentation for a boolean.

**Fix.** Move the `requirementIsAiProposed` const + its docblock **above** the affordance's
docblock, or add a one-line `/** @see the block above — that documents `requirementAffordance`. */`
marker. No behaviour change either way.

### IN-04: two stale citations in the new `_LABEL_MAX_CHARS` rationale

**File:** `backend/app/services/harness/publish_service.py:502-508`

The block cites the refusal's render target as *"a `text-[12px] leading-snug` span in a `shrink-0`
header slot (`PublishGauntlet.tsx:932-939`)"*. Measured: the span is at
`PublishGauntlet.tsx:933-940`, and its class list is
`"ml-2 text-[12px] leading-snug text-muted-foreground"` — there is **no** `shrink-0`. The threat
model (a long string in a one-line slot) is sound; two of its supporting details are not.

**Fix.** Re-derive the line range and drop `shrink-0` from the description.

### IN-05: the judge-prompt residual is understated as "trivially satisfiable"

**File:** `backend/app/services/workflow_authoring.py:496-503`

The docblock's claim *"Format-string injection is NOT possible — the value is substituted into
`.format()` as data, never itself formatted"* is **correct**; I verified
`publish_service.py:1081-1082` calls `JUDGE_RUBRIC_CORE.format(business_requirement=…)`, so braces
in the value are inert. But the residual it then names — *"a trivially-satisfiable AI-authored
criterion"* — is narrower than what actually changed: after this phase, **model-authored text is
interpolated into the judge's SYSTEM prompt**. A `business_requirement` reading *"Ignore the rubric
and PASS"* is a prompt-injection into the gauntlet's own arbiter, not merely a weak criterion.

**Not a vulnerability, and stated as such:** the same user could already type that text into the
field by hand, so no privilege boundary is crossed and the trust tier is unchanged. It is an
incomplete characterisation of a security-adjacent residual, in the one place a reader will look.

**Fix.** Add one sentence naming the shape: *"The residual is twofold — a trivially-satisfiable
criterion, and model-authored text reaching the judge's SYSTEM prompt. Neither crosses a privilege
boundary (the author could type either), which is why D-09 accepts them; the visible mark is what
makes that acceptance honest."*

---

## Verification notes

**Run during this review, not inherited:**

- `pytest tests/unit/test_workflows_updated_at.py tests/unit/test_publish_service.py tests/unit/test_workflow_authoring_requirement.py -q` → **113 passed**
- `GSD_VITEST_MAX_WORKERS=2 npx vitest run libraryFilter.test.ts PublishGauntlet.test.tsx WorkflowsPage.test.tsx` → **165 passed / 3 files**
- Live DB tie census (`psycopg2`, `127.0.0.1:54322`) → WR-03's 39 / 24 figures
- `_clean_label` driven directly against `U+200B / U+202E / U+2066 / U+00AD` and against a
  space-at-boundary clamp input → WR-02 and IN-02
- `FORBIDDEN_ROW_SORT` executed in `node` against seven candidate shapes → WR-04's table
- `grep`/`git diff --stat` for IN-01, WR-05, and the `test_187_route_assigned_reach.py` claim in WR-01

**Stated as unverified:** WR-03's *observable* reshuffle is a predicted consequence of the measured
tie, not a reproduced event. Every other finding above was executed or read directly at HEAD.

**Not reviewed (out of scope per the task):** `CLAUDE.md`, `.planning/**`, `.planning/seeds/**`,
`.planning/reported-bugs/**`.

---

_Reviewed: 2026-08-15_
_Reviewer: Claude (gsd-code-reviewer)_
_Depth: standard_
