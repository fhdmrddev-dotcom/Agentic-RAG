---
phase: 193.2-from-authored-to-runnable
plan: 06
type: execute
wave: 3
depends_on: ["193.2-01", "193.2-02"]
files_modified:
  - backend/app/services/harness/publish_service.py
  - backend/tests/unit/test_publish_service.py
  - frontend/src/components/workflows/PublishGauntlet.test.tsx
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "D-13 — the refusal names the offending step by the plain-language `name` a non-coder reads on the canvas, and offers removal as an ACTION; it no longer names two internal phase-type identifiers at a person who chose neither and can see neither word"
    - "D-13 — an empty or missing `name` degrades to a name-free but TRUE sentence, never to a bare slug and never to a step number; the backend must not grow a second copy of the canvas title ladder"
    - "D-12 — ONE message string, rewritten once, feeds BOTH surfaces: the publish refusal and `/validate` to `blockedReason` to the greyed Publish control. No second string is written for the canvas"
    - "D-14 — the message promises nothing unscheduled: no `planned`, `coming`, `soon`, `deferred`, `future release`. The function's docstring loses `the DEFERRED Phase-103 rework` for the same reason"
    - "D-25 — nothing shipped implies this phase delivers a workflow that deliberately pauses for a person; it suppresses an unwanted step and refuses honestly"
    - "The publish gate STAYS — no check is removed, no stage is added, no control flow moves. An unsubscribed `ask_user` can wedge a publish indefinitely and that is why the gate is deliberate"
    - "D-02 — `publish_service.py` gains no SECOND concern: `_interactive_phase_failures` already composes this exact message; this changes the message's WORDS"
    - "T-193.2-01 — the model-authored phase `name` now enters user-visible copy, an aria label and a PERSISTED audit row, so it is length-clamped and whitespace-collapsed before it is embedded"
  artifacts:
    - path: "backend/app/services/harness/publish_service.py"
      provides: "The rewritten two-arm refusal with an honest empty-name degradation and a clamped label"
      contains: "D-13"
    - path: "backend/tests/unit/test_publish_service.py"
      provides: "F-3, F-4, the publish-half of F-5, both message arms, and the empty-name degradation"
      contains: "zz-plant-slug"
    - path: "frontend/src/components/workflows/PublishGauntlet.test.tsx"
      provides: "The client half of F-5: a ?raw sweep proving no copy of the refusal literal lives in the client"
      contains: "?raw"
  key_links:
    - from: "backend/app/services/harness/publish_service.py"
      to: "backend/app/api/workflows.py"
      via: "/validate calls the same _interactive_phase_failures helper, so one rewrite changes both surfaces"
      pattern: "_interactive_phase_failures"
    - from: "backend/app/services/harness/publish_service.py"
      to: "frontend/src/components/workflows/PublishGauntlet.tsx"
      via: "blockedReason renders the first verdict's message VERBATIM into publish-blocked-reason"
      pattern: "publish-blocked-reason"
---

<objective>
`BUG-260815-01` (blocking), the half that ships as words. Today the refusal reads *"interactive
phases (llm_human_input / ask_user dispositions) cannot be validated in a synchronous publish"* — two
internal identifiers, aimed at a person who chose neither and can see neither word anywhere on the
canvas. **This is the same failure class as `BUG-260809-02`, whose message named an internal
snake_case field.**

Plan 193.2-02 established, from the audit ledger, that the gate **did** fire and the publish endpoint
never refused anything — so the surface the author is looking at is the **Builder header**, and the
fix is exactly this rewrite. It also established the ruling this plan inherits: **D-13's "offers an
action" is a PRECISE INSTRUCTION, not a live control** — a control would exist on only one of the two
surfaces the one string feeds, which D-12 forbids, and would be dead weight on the publish refusal
render, which has no canvas beneath it. The canvas already ships the affordance
(`PlaneEditingLayer.tsx:256`, a `✕` with `aria-label={` + "`Remove step ${position + 1}`" + `}`, with
an inline Undo).

Purpose: the label the author needs is **already in scope** at both append sites — the loop variable
is the whole `PhaseSpec`, not a slug — and the current message throws it away.

Output: a two-arm rewrite of one function's message composition. **No signature change, no new
parameter, no stage change, no control-flow change.**
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

**Worktree bootstrap FIRST:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`, then assert the
dispatched base SHA against `193.2-BASELINE.md`.
**Every vitest invocation carries `GSD_VITEST_MAX_WORKERS=2`.**

**Runs in parallel with 193.2-03, 193.2-04 and 193.2-05** — disjoint files, no database mutation.
</execution_context>

<context>
@.planning/phases/193.2-from-authored-to-runnable/193.2-CONTEXT.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-RESEARCH.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-PATTERNS.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-D10-D19-CONFIRMATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the refusal — two arms that share no sentence, an honest empty-name degradation, a clamped label</name>
  <files>backend/app/services/harness/publish_service.py</files>
  <read_first>
    - `backend/app/services/harness/publish_service.py:502-542` in full — `_interactive_phase_failures`, its docstring, ARM 1 (`config.phase_type == "llm_human_input"`, then `continue`) and ARM 2 (a validator whose `on_failure == "ask_user"`, then `break`), and the `getattr` defensive-read idiom; plus `:190-208` (the stage-2.5 call site, **unchanged by this plan**) and `:434-468` (`_block`, which persists `named_failures` into `harness_audit.metadata`)
    - `backend/app/models/harness.py:360-364` — `PhaseSpec.slug: str` and `PhaseSpec.name: str | None = None` (**`name` is genuinely optional and genuinely nullable — D-13's guard is the declared type, not defensive coding**); and `:420-435`, the docblock recording that the node FACE ladder is computed AT RENDER and NEVER stored, and why a derivation living in the model would be baked into the JSONB
    - `frontend/src/components/workflows/phaseVocabulary.ts:231-240` — `nodeTitle()`, the name → config-derived → type-sentence ladder. ⚠ **The server does not have this and must not grow one**
    - `frontend/src/components/workflows/definitionOps.ts:381-397` — `canRemovePhase`, **the copy register to match**: a shipped refusal that names a step by its visible label and says what to do next; and `:357-363`, the `RemovalOutcome` docblock (*"a bare boolean is FORBIDDEN … the refusing branch physically carries the sentence the surface renders"*)
    - `frontend/src/components/workflows/PlaneEditingLayer.tsx:256` — the `✕` control and its aria label, the action the message points at
    - `backend/app/services/harness/grounding.py:579-635` and `:616-617` — 193.1's D-26 **two assertive arms**, and the rule *"The two arms share no sentence"*, pinned there by a case driven RED against a plant that collapsed them back into one
    - `backend/app/services/harness/grounding.py:1000-1007` — `BUSINESS_REQUIREMENT_MISSING_MESSAGE`, the sibling literal whose voice and length this one should sit beside
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-D10-D19-CONFIRMATION.md` §2 — the ruling (precise instruction, not a live control) and the RE-DERIVED length ceiling
    - `.planning/seeds/SEED-164-a-workflow-that-legitimately-pauses-for-a-person.md` — read before writing a word of copy
  </read_first>
  <action>
Rewrite the message composition inside `_interactive_phase_failures`. **The loop shape is preserved
exactly** — the `getattr` defensive reads, the `continue` after arm 1, the `break` after arm 2, one
finding per phase, the returned `{"phase": <slug>, "message": ...}` dict shape and the `phase` key
still carrying the slug (it is machine-readable metadata, not copy).

**(a) TWO messages, not one. The arms share no sentence.** They describe different things and one
literal cannot honestly serve both:
  - **Arm 1** — the phase ITSELF is an `llm_human_input`: the thing to remove is the step.
  - **Arm 2** — this phase carries a validator whose failure route asks a human: the thing to change
    is the fallback route on that step, not the step.
Writing one new string for both repeats precisely the defect 193.1 just fixed in `grounding.py`.
Each message must: name the offending step by its visible `name`, state what is true now (a publish
runs the workflow end to end and cannot wait for a person), and give the ACTION — for arm 1, remove
that step on the canvas; for arm 2, change that step's failure route so it does not ask a person.

**(b) The label, sanitised — T-193.2-01.** `phase.name` is model- or author-authored free text and it
now enters (i) user-visible copy in a `text-[12px]` `shrink-0` header span, (ii) an
`aria-describedby` label, and (iii) `harness_audit.metadata.named_failures`, which is **persisted**.
`PhaseSpec.name` is unbounded `str | None`. Before embedding it:
  - **collapse whitespace and control characters** to single spaces — a newline in a name breaks the
    one-line shape on both surfaces;
  - **clamp to roughly 60–80 characters with an ellipsis** — a 10 KB name would otherwise become a
    10 KB refusal and a 10 KB audit row. Pick a value in that band and state it as a named module
    constant with a comment giving the reason, so the number is auditable rather than magic.
  - ⚠ **Rendering is already safe** — `blockedReason` reaches React as a text child and there is no
    `dangerouslySetInnerHTML` on this path. **XSS is not the threat; length and honesty are.** Say so
    in the comment so a later reader does not "harden" this into HTML-escaping and break the copy.

**(c) The empty-name degradation — a name-free but TRUE sentence.** When `name` is missing or trims
empty, the message must still be honest and still be about a step the author can find. **Never the
slug** (it reintroduces the exact defect being fixed) and **never a step number** — `PhaseNodeCard`
records *"no gate identifiers, no phase_index on the face"*, so *"Step 3"* names something the author
cannot read on the card. Write one degraded literal per arm. ⚠ **Do NOT port `nodeTitle()` to
Python.** That ladder is computed at render and never stored, and a second implementation of a
derivation is forbidden by name in this repo.

**(d) Length.** `193.2-D10-D19-CONFIRMATION.md` §2 records the re-derived ceiling (today's message
measures 105 characters and `BUSINESS_REQUIREMENT_MISSING_MESSAGE` 99; both fit the header span). A
label-carrying message will be longer — aim at the same order of magnitude, not at 250 characters.
Record the measured `len()` of every new literal at its maximum clamped label in the summary.

**(e) D-14 — and the docstring goes with it.** No `planned`, `coming`, `soon`, `deferred`,
`future release`. **And delete or rewrite the docstring's last paragraph**, which today reads *"The
full background-job publish that COULD validate interactive phases … is the DEFERRED Phase-103
rework — out of scope here."* That is the literal sentence `SEED-164` exists because of: unscheduled
work reading like a plan for a year, and Phase 103 shipped long ago. It is a docstring rather than
user copy, but a plan rewriting this function that leaves it in has missed the point of D-14. Replace
it with what is true now, naming nothing unscheduled.

**(f) One risk to think about for one sentence — removal can itself be refused.** `canRemovePhase`
returns `{ok:false}` when another step routes its failures to the target. Rare for an AI-added
`llm_human_input`, but the arm-1 message should not read as a promise that the `✕` will always
succeed. Prefer *"remove that step"* over *"you can always remove that step"*.

**Nothing else changes.** No stage is added or removed, no check is weakened, `_block` is untouched,
the stage-2.5 call site is untouched. **The gate STAYS.**

**D-02, proved not asserted:** state in the summary that `publish_service.py` already composes this
exact message and this plan changes its WORDS — no new check, no new stage, no control-flow change.
If you find otherwise, STOP; D-01 is void for this file and a refactor recommendation is owed FIRST.

Commit `fix(193.2-06): the publish refusal names the step a person can see, and says what to do (D-12/D-13/D-14)`.
  </action>
  <acceptance_criteria>
    - `_interactive_phase_failures` composes TWO distinct message literals; a source read confirms arm 1 and arm 2 share no complete sentence
    - Each arm has a named-label form and a degraded name-free form — four literals total, or a compositional equivalent whose branches are individually reachable
    - A named module constant holds the label clamp length, in the 60–80 band, with a comment giving the reason; the whitespace and control-character collapse is applied before embedding
    - No message literal, in any branch, contains the tokens `llm_human_input`, `ask_user`, or any interpolation of `slug`
    - No message literal matches the case-insensitive regex `plann?ed|coming|soon|deferred|future release`
    - The docstring no longer contains the string `DEFERRED Phase-103 rework`
    - `git diff` shows the loop structure unchanged: the `getattr` reads, the `continue`, the `break`, and the returned dict keys (`phase`, `message`) are the same; `:190-208` (stage 2.5) is UNCHANGED; `_block` is UNCHANGED
    - The summary records `len()` for each new literal computed at the maximum clamped label, and compares them against the re-derived 105 / 99 baseline from `193.2-D10-D19-CONFIRMATION.md` §2
    - The summary states the D-02 no-second-concern argument for `backend/app/services/harness/publish_service.py`
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && ./venv/Scripts/python.exe -m pytest tests/unit/test_publish_service.py tests/unit/test_187_route_assigned_reach.py -q</automated>
  </verify>
  <done>The refusal names the step by the label on the canvas, tells the author what to do, degrades honestly when there is no name, clamps a model-authored string before it reaches copy and a persisted audit row, and promises nothing unscheduled — in one function, with the gate untouched.</done>
</task>

<task type="auto">
  <name>Task 2: F-3, F-4 and the publish half of F-5, each driven RED against a real plant</name>
  <files>backend/tests/unit/test_publish_service.py</files>
  <read_first>
    - `backend/tests/unit/test_publish_service.py:388-496` — the shipped WR-04 interactive block: `_interactive_definition_row(*, kind)` at `:389-428` (⚠ **neither shipped fixture phase carries a `name`** — the empty-name degradation case is that fixture as it stands, and every label case must ADD one), the parametrized both-arms case at `:431-451`, and the helper-level unit at `:478-495`
    - `backend/tests/unit/test_publish_service.py:898-938` — **the strongest in-file positive-control precedent**: a case that asserts an absence and then drives a control to prove the detector still fires
    - `backend/tests/unit/test_187_route_assigned_reach.py` header — the two-halves discipline (*"THE CORRECTED SENTENCE HAS TWO HALVES, AND THEY ARE MEASURED IN TWO PLACES"*), which this task imitates rather than duplicates
    - `backend/app/api/workflows.py:894-902` — `/validate`'s `interactive_phase` mint, the second consumer of the one string
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-PATTERNS.md` — §"THE ONE PATTERN EVERY PLAN MUST COPY" (four parts) and ⚠ **the F-3 scoping inversion**: `publish_service.py`'s own docblock legitimately QUOTES both forbidden identifiers, so **F-3 must assert over the COMPOSED MESSAGE VALUE, never over the module source** — a source-scoped fence would be RED on a clean tree
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-VALIDATION.md` §"Fences" rows F-3, F-4, F-5
  </read_first>
  <action>
Extend `backend/tests/unit/test_publish_service.py`. Join the shipped WR-04 block; do not restructure
the file and do not edit an existing case.

**Fixtures first.** Add to (or parametrize) `_interactive_definition_row` so it can produce: a phase
with a distinctive `name`, a phase with `name=None`, a phase with a whitespace-only `name`, and — for
F-3 — a phase whose `slug` is the distinctive token **`zz-plant-slug`**.

**F-3 — no internal identifier and no slug reaches the copy.** ⚠ **Scope over the composed message
value, not the module source.** Build a definition whose phase carries `slug="zz-plant-slug"` and
`name=None`, call `publish_service._interactive_phase_failures(definition)`, and assert that for
every returned finding, `finding["message"]` contains none of `llm_human_input`, `ask_user`,
`zz-plant-slug`. Add **non-vacuity**: assert the returned list is non-empty (an absence assertion over
zero findings is the commonest way a fence stops seeing). Add the **inline plant**: assert a literal
string that DOES contain `zz-plant-slug` matches the same predicate the fence applies. Then **drive
it RED for real once**: plant an `f"{slug}"` interpolation into the composer, run the file, observe
the failure, restore, re-run green, and record all four outputs in the summary.

**F-4 — nothing unscheduled is promised.** Assert every composed `message` matches none of
`/plann?ed|coming|soon|deferred|future release/i`, across **both arms and both the named and
degraded forms** — parametrize so all four shapes are covered. Positive control: assert the same
regex DOES match a planted string containing `planned`. Then drive it RED for real by planting the
word into one literal, observe, restore, record.

**The label and degradation cases (D-13).** Assert: a phase with a `name` produces a message
containing that name; a phase with `name=None` and a phase with a whitespace-only `name` produce the
degraded literal, which contains neither the slug nor a digit-based step reference; the two arms
produce **different** messages for the same phase name — assert inequality directly, and add a
positive control driven RED against a plant that collapses both arms back onto one literal (this is
the 193.1 D-26 shape, and it is the only thing that can catch a later "simplification").

**T-193.2-01 — the clamp.** Assert that a phase whose `name` is a 10,000-character string produces a
message whose length stays under a stated bound, and that a `name` containing newlines and control
characters produces a single-line message. Assert the sanitised label is what lands in the finding,
not the raw one.

**F-5, the publish half.** Assert that the value `/validate` would emit and the value the publish
refusal would emit are **byte-identical for the same definition** — call the shared helper the way
each caller does and compare, rather than asserting a hard-coded string. Reference
`test_187_route_assigned_reach.py` in the docstring as the co-owner of the other half, exactly as
that file's header asks.

**Do NOT weaken or edit the shipped WR-04 cases**, including
`test_interactive_phase_blocks_publish_before_golden_run` and its `drive.assert_not_awaited()` — that
assertion IS the proof that the gate stays and the golden run is never driven.

Commit `test(193.2-06): F-3, F-4 and the publish half of F-5, each driven RED against a real plant`.
  </action>
  <acceptance_criteria>
    - `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_publish_service.py -q` → 0 failed
    - `grep -c "zz-plant-slug" backend/tests/unit/test_publish_service.py` returns at least 1
    - F-3 asserts over `finding["message"]` values and NOT over `inspect.getsource` of the module — a source-scoped F-3 is an automatic reject (the module docblock quotes both identifiers)
    - F-3 carries a non-vacuity assertion that the findings list is non-empty, and an inline plant assertion
    - F-4 is parametrized across both arms and both the named and degraded forms, with a positive control
    - A case asserts the two arms produce DIFFERENT messages for the same phase, with a positive control driven RED against a collapse-the-arms plant
    - A case asserts a 10,000-character `name` yields a bounded message, and a newline-bearing `name` yields a single-line message
    - A case asserts the `/validate` value and the publish-refusal value are byte-identical, computed rather than hard-coded
    - The summary records FOUR real-plant RED observations (F-3 slug interpolation, F-4 `planned`, the arm-collapse, and one clamp bypass), each with its command, failing case name, failure output, restore, and empty `git diff --numstat`
    - `git diff --numstat` shows the shipped WR-04 cases unchanged
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && ./venv/Scripts/python.exe -m pytest tests/unit/test_publish_service.py -q</automated>
  </verify>
  <done>The refusal's honesty properties are mechanical: no internal identifier, no slug, no unscheduled promise, two arms that cannot be collapsed, a bounded single-line label — and every one of them has been shown to fail against a real plant.</done>
</task>

<task type="auto">
  <name>Task 3: F-5's client half — a ?raw sweep proving the message has exactly one home</name>
  <files>frontend/src/components/workflows/PublishGauntlet.test.tsx</files>
  <read_first>
    - `frontend/src/components/workflows/PublishGauntlet.test.tsx` — the existing structure and its `blockedReason` cases; find where a new `describe` fits without restructuring
    - `frontend/src/components/workflows/PublishGauntlet.tsx:846, 921, 932-939` — `blocked`, `disabled={blocked}`, and the `data-testid="publish-blocked-reason"` span that renders the server's message as a TEXT CHILD
    - `frontend/src/pages/WorkflowBuilderPage.tsx:1338-1349` — `blockedReason`, the five branches, the `?? validation.verdicts[0]` fallback, and the rule that the client renders the first verdict's `message` VERBATIM and never rewrites or maps it
    - `frontend/src/pages/WorkflowBuilderPage.header.test.tsx:120-130` — **the `?raw` import idiom this repo uses to make a scope fence machine-checkable rather than merely intended**
    - `frontend/src/components/workflows/library/rowIdentity.test.ts:89-115` — the comment stripper and its non-vacuity guard
  </read_first>
  <action>
Add ONE new `describe` to `frontend/src/components/workflows/PublishGauntlet.test.tsx` proving D-12's
client half: **the refusal message has exactly one home, and it is the server.**

Import via Vite's `?raw` loader: `@/components/workflows/PublishGauntlet?raw` and
`@/pages/WorkflowBuilderPage?raw`. Strip comments (the 187-24 trap), then pair the stripper with its
own non-vacuity guard — assert a known identifier survives in `code` and a known prose token present
in `source` is absent from `code`.

Assert, over the stripped sources:
  - **No client copy of the refusal.** No distinctive phrase from the new server literals appears in
    either module. ⚠ **Do NOT hand-type the server's sentence into this test** — that would itself be
    a second home and would go stale silently at the next reword. Instead assert the *structural*
    property that makes a copy impossible: the client renders `verdict.message` and constructs no
    interactive-refusal sentence of its own. Concretely: assert neither stripped source contains the
    tokens `llm_human_input` or `ask_user` in a string-literal position, and assert
    `publish-blocked-reason`'s rendered value comes from a variable rather than a literal in the JSX.
  - **The inline plant, permanently in the file:** assert that a literal client-side string
    containing an interactive-refusal sentence DOES match the needle the fence applies.
  - Then **drive it RED for real once**: plant such a literal into `PublishGauntlet.tsx`, run the
    suite, observe the failure, restore with `git checkout --`, re-run green, and record the
    observation plus an empty `git diff --numstat` in the summary.

⚠ **`PublishGauntlet.tsx` itself is NOT modified by this plan** — it is in `read_first` and in the
plant/restore probe only. `files_modified` names the test file, not the component.

⚠ **Do not touch `WorkflowBuilderPage.header.test.tsx`.** It owns `FLAG_OFF_HEADER_MARKUP`, a
nine-phase-old byte pin whose own re-capture protocol says the phase expects **no third re-capture**.
This plan must leave it byte-identical.

Commit `test(193.2-06): F-5 client half — the refusal has exactly one home, driven RED against a plant (D-12)`.
  </action>
  <acceptance_criteria>
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/PublishGauntlet.test.tsx` → 0 failed
    - The new describe imports both modules via `?raw`, strips comments, and carries a stripper non-vacuity guard
    - The fence does NOT hand-type any server sentence; it asserts structural properties (no `llm_human_input` / `ask_user` string literals in the client; the rendered value comes from a variable)
    - An inline plant assertion is present permanently in the file
    - The summary records the real-plant RED against `PublishGauntlet.tsx`, its restore, and an empty `git diff --numstat` for that file
    - `git diff --numstat` shows `frontend/src/components/workflows/PublishGauntlet.tsx`, `frontend/src/pages/WorkflowBuilderPage.tsx` and `frontend/src/pages/WorkflowBuilderPage.header.test.tsx` ALL UNCHANGED
    - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` prints `count gate OK`; if the pinned total moved, the pin update lands in this same commit
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/PublishGauntlet.test.tsx</automated>
  </verify>
  <done>D-12 is mechanical on both sides: the server owns the sentence, the client renders it verbatim, and a planted client copy goes red.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| **model- or author-authored `PhaseSpec.name` → user-visible copy** | Unbounded `str \| None` free text entering a `text-[12px]` header span |
| **`PhaseSpec.name` → `harness_audit.metadata.named_failures`** | The same free text entering a PERSISTED audit row via `_block` |
| **`PhaseSpec.name` → an `aria-describedby` label** | Assistive-technology surface |
| caller-owned definition → the helper | `_interactive_phase_failures(definition)` iterates only the definition the caller already owns; `get_definition` is owner-scoped and `/validate` receives the caller's own body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| **T-193.2-01** | **Information disclosure / Denial of service** | the phase `name` embedded in the refusal, the aria label and the persisted audit row | **mitigate** | (1) **Length clamp** to a named constant in the 60–80 band with an ellipsis — `PhaseSpec.name` is unbounded, and a 10 KB name would become a 10 KB refusal in a `shrink-0` header span AND a 10 KB audit row; asserted by a 10,000-character case. (2) **Whitespace and control-character collapse** — a newline breaks the one-line shape on both surfaces; asserted by a newline-bearing case. (3) **Cross-user disclosure: not possible** — the helper iterates only the caller's own definition. (4) ⚠ **Rendering is already safe** (a React text child, no `dangerouslySetInnerHTML` on this path) — **XSS is not the threat; length and honesty are**, and the code comment must say so, so a later reader does not HTML-escape the copy |
| T-193.2-01b | Spoofing / Repudiation | internal identifiers and slugs leaking into copy aimed at a non-coder | mitigate | F-3, asserted over the COMPOSED MESSAGE VALUE rather than the module source (the docblock legitimately quotes both identifiers), with non-vacuity, an inline plant, and a recorded real-plant RED |
| T-193.2-01c | Repudiation | copy that promises an unscheduled capability | mitigate | F-4 across all four message shapes with a positive control, plus the docstring's `DEFERRED Phase-103 rework` sentence removed — the literal sentence `SEED-164` exists because of |
| T-193.2-01d | Elevation of privilege | the publish gate itself | **mitigate by not touching it** | No check removed, no stage added, no control flow moved; `_block` and the stage-2.5 call site are asserted UNCHANGED, and the shipped `drive.assert_not_awaited()` case stays green. An unsubscribed `ask_user` can wedge a publish indefinitely — the gate is deliberate |
| T-193.2-SC | Tampering | npm/pip/cargo installs | accept | No package installed; the `?raw` loader is Vite's, already used by three shipped suites |
</threat_model>

<verification>
- `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_publish_service.py tests/unit/test_187_route_assigned_reach.py -q` → 0 failed
- The six workflow backend suites → 0 failed
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/PublishGauntlet.test.tsx` → 0 failed
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` → `count gate OK`
- `git diff --numstat HEAD~3 -- frontend/src/pages/WorkflowBuilderPage.header.test.tsx frontend/src/components/workflows/PublishGauntlet.tsx` → EMPTY
</verification>

<success_criteria>
- The refusal names the visible step label, gives an action, and degrades to a true name-free sentence — never a slug, never a step number
- Two arms, no shared sentence, provably uncollapsible
- No internal identifier, no slug, no unscheduled promise in any branch
- The label is clamped and single-lined before it reaches copy or the persisted audit row
- One string still feeds both surfaces, proved from both sides
- The gate is untouched
</success_criteria>

<output>
Create `.planning/phases/193.2-from-authored-to-runnable/193.2-06-SUMMARY.md` when done.
</output>
