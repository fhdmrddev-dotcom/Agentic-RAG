---
phase: 193.2-from-authored-to-runnable
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/193.2-from-authored-to-runnable/193.2-BASELINE.md
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "D-01 — the baseline artifact records that a G-5 override was OFFERED AND DECLINED for the third consecutive phase, and that this phase carries NO guardrail override; the absence is written down as a measurement"
    - "D-04 — the baseline artifact records the standing scope fence: `WorkflowCard.tsx` appears in NO plan's files_modified, and its inherited G-5 obligation passes forward untouched"
    - "Every number a later plan could mistake for its own breakage is measured at HEAD and written down BEFORE any source changes"
    - "F-2 (the three predicate byte-identity cases) has been driven RED against a real one-character WHERE plant on the UNMOVED tree and restored"
    - "F-7 (rowIdentity T-7/P-4, no client sort) has been driven RED against a real planted `rows.sort(` on the UNMOVED tree and restored"
    - "The base commit SHA is recorded so plan 193.2-03 can run F-1 against the pre-change `db/workflows.py` source"
  artifacts:
    - path: ".planning/phases/193.2-from-authored-to-runnable/193.2-BASELINE.md"
      provides: "Measured baselines at HEAD, the two shipped-fence RED observations, the base SHA, and the D-01/D-04 scope record"
      contains: "count gate"
      min_lines: 60
  key_links:
    - from: "193.2-BASELINE.md"
      to: "backend/tests/unit/test_workflows_updated_at.py"
      via: "the recorded F-2 RED observation and the recorded base SHA that 193.2-03 asserts against"
      pattern: "test_published_predicates_are_byte_identical_to_what_shipped"
    - from: "193.2-BASELINE.md"
      to: "frontend/src/components/workflows/library/rowIdentity.test.ts"
      via: "the recorded F-7 RED observation"
      pattern: "T-7 / P-4"
---

<objective>
Wave 0 of the RESEARCH wave shape, executed as GSD wave 1. Measure and record every baseline this
phase will later be judged against, and prove that the two SHIPPED fences this phase leans on can
actually fire — **on the unmoved tree, in a commit that touches no source file.**

Purpose: *a baseline only proves something if it PREDATES the change* (the 188.1 lesson, re-proved in
193.1). Nine plans downstream will assert "unchanged", "still green", "no regression" — none of those
words mean anything without a dated, measured, pre-change record. And 193.1 found FOUR fences that
could not fire, all caught by planting and none by reading: F-2 and F-7 are inherited fences this
phase's correctness rests on, so they get planted here, before there is any change to blame.

Output: `.planning/phases/193.2-from-authored-to-runnable/193.2-BASELINE.md`. **No source file is
modified by this plan.** The two plants are temporary probes that are restored byte-identically
before the commit.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

**Worktree bootstrap is MANDATORY and is your FIRST action, before the HEAD assertion and before
reading the plan:**
```bash
bash scripts/bootstrap-worktree.sh "$(pwd)"
```
**Assert the dispatched base:** `git rev-parse HEAD` MUST report
`3cfafa0d4ae444c4e9a438fa97ee07657fa6316e`. Worktrees have forked from the wrong base 12/12 times in
Phase 192 and again on a single worktree on 2026-08-14 — if the SHA differs, STOP and report.

**Every vitest invocation carries `GSD_VITEST_MAX_WORKERS=2`.** The `4` in `CLAUDE.md` has rotted;
at 3892 gated cases, cap 4 measured `failed` 17/4/3 on one identical commit and cap 2 measured 0/0.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-CONTEXT.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure every baseline at HEAD and write 193.2-BASELINE.md</name>
  <files>.planning/phases/193.2-from-authored-to-runnable/193.2-BASELINE.md</files>
  <read_first>
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-VALIDATION.md` — §"Measured baselines at HEAD", §"Test Infrastructure" (the exact commands), §"Fences"
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-CONTEXT.md` — D-01 (the declined override), D-04 (the WorkflowCard scope fence)
    - `CLAUDE.md` — §"Parallel execution" (the `GSD_VITEST_MAX_WORKERS` rule and why 4 rotted), §"Workflow guardrails" hot-file ledger
    - `.planning/phases/193.1-template-first-authoring/193.1-BASELINE.md` if present — the shape this artifact copies
  </read_first>
  <action>
Create `.planning/phases/193.2-from-authored-to-runnable/193.2-BASELINE.md` with YAML frontmatter
(`phase: 193.2`, `captured: 2026-08-15`, `base_sha:` the output of `git rev-parse HEAD`, `tree_state: UNMOVED`)
and the following measured sections. RUN every command; do not copy a number from RESEARCH or
VALIDATION without re-running it — RESEARCH itself records that the count-gate total rotted from
3604 to 3892 in one day.

**§1 Base commit.** `git rev-parse HEAD` and `git rev-parse --short HEAD`. Also record
`git status --porcelain` (must be empty of source changes) and, as the F-1 input for plan 193.2-03,
the byte length of `git show HEAD:backend/app/db/workflows.py | wc -c` plus a grep proving all three
`ORDER BY name` clauses are present at HEAD:
`git show HEAD:backend/app/db/workflows.py | grep -c "ORDER BY name"` → record the number.

**§2 Backend baselines.** From `backend/`, using `./venv/Scripts/python.exe -m pytest`:
  - full `tests/unit -q` — record `failed/passed/xfailed/xpassed` and wall time
  - the six workflow suites named in VALIDATION §"Test Infrastructure" (`test_workflows_updated_at.py`,
    `test_publish_service.py`, `test_starter_workflows.py`, `test_103_draft_crud.py`,
    `test_published_workflow_ownership.py`, `test_187_route_assigned_reach.py`) — record passed/failed

**§3 Frontend baselines.**
  - the five workflow suites (`src/pages/WorkflowsPage.test.tsx`,
    `src/pages/WorkflowBuilderPage.canvas.test.tsx`, `src/pages/WorkflowBuilderPage.header.test.tsx`,
    `src/components/workflows/library/libraryFilter.test.ts`,
    `src/components/workflows/library/rowIdentity.test.ts`) with `GSD_VITEST_MAX_WORKERS=2 npx vitest run <files>`
  - the count gate from the repo root: `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs`
    — record `total`, `failed`, `pinned total`, files present, and the verdict line
  - typecheck: `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — record the error count.
    ⚠ State in the artifact that a BARE `tsc --noEmit` checks ZERO files and is not the gate.

**§4 The two corrections a later plan must not mistake for its own breakage.** State both plainly:
(a) `CLAUDE.md` records the count gate at 3604; measured today it is whatever §3 found — it moved
because the pinned set grew, not because anything broke; (b) the full frontend tree carries 26
pre-existing failures OUTSIDE the gate (the SEED-056 rot set) and the backend full unit suite carries
62 — **no plan in this phase touches any of them**, and every plan gates on the count gate plus the
named suites, never on a bare full-suite run.

**§5 Guardrail record (D-01 / D-04).** Write, as a measurement rather than an aside:
  - **D-01** — a G-5 override was OFFERED AND DECLINED for Phase 193.2, the third consecutive phase
    (193, 193.1, 193.2) to decline one. This phase records **NO** guardrail override. Include the
    re-derived hot-file counts for the seven files in scope using
    `git log --oneline -- <file> | wc -l`, `wc -l <file>`, and
    `git log --format=%s -- <file> | sed -E 's/^[a-z]+\(([^)]+)\).*/\1/' | sed -E 's/-.*//' | sort -u`
    (⚠ that last recipe returns quick-task buckets like `quick` / `260814` alongside real phases —
    count them OUT and say so). The seven: `backend/app/db/workflows.py`,
    `backend/app/services/harness/publish_service.py`, `backend/app/services/workflow_authoring.py`,
    `backend/app/models/harness.py`, `backend/app/api/workflows.py`,
    `frontend/src/pages/WorkflowsPage.tsx`, `frontend/src/pages/WorkflowBuilderPage.tsx`.
    ⚠ Where a measured figure disagrees with CONTEXT D-01 or RESEARCH, record the corrected value
    **BESIDE** the previous one, never over it — this repository's standing habit.
  - **D-04** — `frontend/src/components/workflows/library/WorkflowCard.tsx` is measured
    (`git log --oneline -- <file> | wc -l`, `wc -l`) and recorded as **out of scope**: it appears in
    NO plan's `files_modified` in this phase, its inherited G-5 obligation passes forward untouched,
    and it is a file to READ only.

**§6 Reported-bugs cross-check.** Record that `BUG-260815-01` and `BUG-260815-02` both carry
`folded_into: "193.2"` at HEAD (`grep -n folded_into` on both files), and name the plans that will
cover them (193.2-05/06 for -01; 193.2-03/04 for -02). This is the `plan-phase` touchpoint the
project rules require.

Commit with `docs(193.2-01): baselines measured at HEAD on the unmoved tree`. The commit must contain
ONLY `.planning/...193.2-BASELINE.md`.
  </action>
  <acceptance_criteria>
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-BASELINE.md` exists with frontmatter carrying a `base_sha` equal to the output of `git rev-parse HEAD`
    - `git show --stat HEAD` for this task's commit lists exactly one file, and it is under `.planning/`
    - §1 records `git show HEAD:backend/app/db/workflows.py | grep -c "ORDER BY name"` and the number is `3`
    - §2 records a `failed`/`passed` pair for the full backend unit suite AND a `0 failed` line for the six workflow suites
    - §3 records the count-gate verdict line verbatim, including the `total` and `failed` numbers, and the `tsc -p tsconfig.app.json` error count
    - §4 states both corrections in words, naming 3604 → the measured total, and naming the 26/62 pre-existing failures as OUT of every plan's gate
    - §5 contains the literal string `NO guardrail override` and the seven re-derived `commits / phases / lines` triples, with any disagreement against CONTEXT D-01 recorded BESIDE the previous value
    - §5 contains the literal token `D-01` and §5's WorkflowCard paragraph contains the literal token `D-04`
    - §6 shows `folded_into: "193.2"` for both `BUG-260815-01` and `BUG-260815-02`
    - No file under `backend/` or `frontend/` differs from HEAD: `git diff --numstat HEAD -- backend frontend` produces EMPTY output
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && test -f .planning/phases/193.2-from-authored-to-runnable/193.2-BASELINE.md && grep -q "NO guardrail override" .planning/phases/193.2-from-authored-to-runnable/193.2-BASELINE.md && grep -q "count gate" .planning/phases/193.2-from-authored-to-runnable/193.2-BASELINE.md && test -z "$(git diff --numstat HEAD -- backend frontend)"</automated>
  </verify>
  <done>The baseline artifact exists, carries the base SHA, every measured suite figure, both baseline corrections, the D-01 no-override record and the D-04 scope fence — and no source file was touched.</done>
</task>

<task type="auto">
  <name>Task 2: Drive F-2 and F-7 RED against real plants on the unmoved tree, restore, and record</name>
  <files>.planning/phases/193.2-from-authored-to-runnable/193.2-BASELINE.md</files>
  <read_first>
    - `backend/tests/unit/test_workflows_updated_at.py` — the three `*_predicate*_byte_identical*` cases (⚠ the published one is **plural and parametrized**: `test_published_predicates_are_byte_identical_to_what_shipped`, correction C-1) and the `_published_source()` / `_starter_source()` / `_draft_source()` `inspect.getsource` accessors
    - `backend/app/db/workflows.py` — `list_published_workflows` (both predicate branches), `list_starter_workflows`, `list_draft_workflows`
    - `frontend/src/components/workflows/library/rowIdentity.test.ts:89-115, 167-179` — the comment stripper, its non-vacuity guard, and the T-7 / P-4 fence with its inline plant
    - `frontend/src/components/workflows/library/libraryFilter.ts:137-149` — `mergeLibrary`
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-VALIDATION.md` — §"Fences", rows F-2 and F-7
  </read_first>
  <action>
Two probes. Each is: plant → run → observe RED → restore → re-run → observe GREEN → `git diff` empty.
Record every command and its verbatim tail in a new §7 of `193.2-BASELINE.md`.

**Probe A — F-2 can fire.** In `backend/app/db/workflows.py`, change exactly one character in a
`WHERE` clause of `list_published_workflows`: `created_by = $1` → `created_by= $1` (remove the space
before `=`). Run
`cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_workflows_updated_at.py -q`.
The parametrized `test_published_predicates_are_byte_identical_to_what_shipped` MUST fail. Record the
failing test id and the assertion line. Then `git checkout -- backend/app/db/workflows.py`, re-run,
and record `0 failed`. Confirm `git diff --numstat -- backend/app/db/workflows.py` is EMPTY.

**Probe B — F-7 can fire.** In `frontend/src/components/workflows/library/rowIdentity.ts`, plant a
forbidden shape the fence's needle is written to catch — a line containing `rows.sort(` inside a
non-comment code line (e.g. a dead local `const shown = rows.sort((a, b) => a.name.localeCompare(b.name))`).
Run `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/library/rowIdentity.test.ts`.
The `T-7 / P-4` case MUST fail. Record the failing test name and the assertion output. Then
`git checkout -- frontend/src/components/workflows/library/rowIdentity.ts`, re-run, record green,
and confirm `git diff --numstat` is EMPTY for that file.

⚠ **Both probes are DELIBERATELY on the unmoved tree.** The point is that these fences are shown to
see something BEFORE this phase changes anything they watch — so a later green reading cannot be
confused with a fence that was silently blind. Write that sentence into §7.

⚠ **Do not weaken either fence.** No test file is edited by this plan. If a plant does NOT go red,
STOP and report it as a finding: that would be a fifth fence that cannot fire, and it changes what
plans 193.2-03 and 193.2-04 must build.

Commit with `docs(193.2-01): F-2 and F-7 driven RED against real plants, restored, recorded`.
The commit must contain ONLY the baseline artifact.
  </action>
  <acceptance_criteria>
    - §7 of `193.2-BASELINE.md` names Probe A's failing test id as `test_published_predicates_are_byte_identical_to_what_shipped` (plural, parametrized) and quotes the assertion failure
    - §7 names Probe B's failing case as the `T-7 / P-4` test in `rowIdentity.test.ts` and quotes its failure
    - §7 records, for each probe, the post-restore GREEN run and the empty `git diff --numstat` for the planted file
    - §7 contains a sentence stating the probes were run on the UNMOVED tree and why that ordering is load-bearing
    - `git diff --numstat HEAD -- backend frontend` is EMPTY after this task
    - `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_workflows_updated_at.py -q` reports 0 failed
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/library/rowIdentity.test.ts` reports 0 failed
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && ./venv/Scripts/python.exe -m pytest tests/unit/test_workflows_updated_at.py -q && cd "C:/Vibe Apps/Agentic RAG" && test -z "$(git diff --numstat HEAD -- backend frontend)"</automated>
  </verify>
  <done>Both inherited fences have been observed RED against a real plant and GREEN after restore, the observations are recorded permanently in the baseline artifact, and the tree is byte-identical to HEAD.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local working tree → git history | This plan plants forbidden code temporarily; an unrestored plant would ship a defect |
| none other | This plan changes no runtime code, no schema, no endpoint, no dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193.2-01-A | Tampering | `backend/app/db/workflows.py`, `frontend/.../rowIdentity.ts` (temporary plants) | mitigate | Every plant is restored with `git checkout -- <file>`; the acceptance criteria require `git diff --numstat HEAD -- backend frontend` to be EMPTY, and the commit is asserted to contain exactly one file under `.planning/` |
| T-193.2-02 | Information disclosure | the three list feeds' RLS-bypassing `WHERE` predicates | mitigate | This plan does not change any predicate. Probe A exists precisely to prove the predicate guard can fire before any later plan touches the same file |
| T-193.2-SC | Tampering | npm/pip/cargo installs | accept | **No package is installed by this phase.** RESEARCH §"Package Legitimacy Audit" records this explicitly. If any plan proposes a dependency, D-02's no-second-concern argument is void for that file and the Package Legitimacy Gate must run before the install |
</threat_model>

<verification>
- `git diff --numstat HEAD -- backend frontend` → EMPTY
- `git show --stat` for both commits → only `.planning/phases/193.2-from-authored-to-runnable/193.2-BASELINE.md`
- `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_workflows_updated_at.py tests/unit/test_publish_service.py -q` → 0 failed
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` → `count gate OK`
</verification>

<success_criteria>
- `193.2-BASELINE.md` exists with seven sections, a `base_sha`, and every figure RE-RUN rather than copied
- The literal strings `NO guardrail override`, `D-01` and `D-04` appear in §5
- F-2 and F-7 each have a recorded RED observation against a named plant and a recorded GREEN after restore
- No source file differs from `3cfafa0d4ae444c4e9a438fa97ee07657fa6316e`
</success_criteria>

<output>
Create `.planning/phases/193.2-from-authored-to-runnable/193.2-01-SUMMARY.md` when done.
</output>

