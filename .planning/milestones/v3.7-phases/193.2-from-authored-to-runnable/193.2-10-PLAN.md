---
phase: 193.2-from-authored-to-runnable
plan: 10
type: execute
wave: 6
depends_on: ["193.2-03", "193.2-04", "193.2-06", "193.2-08", "193.2-09"]
files_modified:
  - CLAUDE.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/193.2-from-authored-to-runnable/193.2-UAT.md
  - .planning/reported-bugs/template-first-drafts-cannot-be-published.md
  - .planning/reported-bugs/cannot-find-a-just-published-workflow.md
  - .planning/seeds/SEED-163-authoring-does-not-propose-the-business-requirement.md
autonomous: false
requirements: [AUTH-03]

must_haves:
  truths:
    - "D-03 — FOUR hot-file ledger rows are a DELIVERABLE of this phase, not a nicety: `db/workflows.py`, `publish_service.py`, `workflow_authoring.py` and `models/harness.py`, each carrying measured counts, re-derive commands, the G-5 verdict with its measured reason, the invariants that now bind, and the named next seam — so Phase 194 inherits a measurement instead of re-deriving it"
    - "D-03 — the `WorkflowsPage.tsx` row's stale figures are corrected BESIDE the previous value, never over it; this is that cell's FOURTH consecutive staling and the row must say so"
    - "D-16 — the published/drafts-versus-starters divergence is written into the `db/workflows.py` ledger row, which is D-16's own stated condition"
    - "D-01 — `.planning/STATE.md` records NO guardrail override for Phase 193.2; the absence is a measurement, and it is the third consecutive phase to decline one"
    - "D-04 — `WorkflowCard.tsx` was touched by no plan; its inherited G-5 obligation passes forward untouched and its row records that"
    - "D-05 — G-2 did not fire on anything in scope and G-1 did not fire; a third `193.x` would trip G-1, and that is recorded for whoever comes next"
    - "D-18 — the deferred user-facing sort control keeps a concrete re-open trigger; a deferral living in one phase's context file is exactly as invisible as a hot file missing from the ledger"
    - "D-21 — SC#4 is ONE real operator-driven end-to-end run at phase close, the only row that exercises all three fixes together in the order a person meets them"
    - "D-22 — the three-instance pattern (`SEED-157`, `SEED-163`, the AI-chosen name) is recorded for 197 as a measured pattern rather than left for 197 to re-derive"
    - "D-24 and D-25 are recorded as NOT delivered here, each with its re-open trigger: naming and renaming go to 197 / AUTH-02, and a workflow that deliberately pauses for a person stays with `SEED-164`"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Four new hot-file ledger rows and the corrected figures for the existing rows"
      contains: "workflow_authoring.py"
    - path: ".planning/phases/193.2-from-authored-to-runnable/193.2-UAT.md"
      provides: "The UAT rows including D-21's single operator-driven run, the OQ-1/OQ-2/OQ-5/OQ-6 driven observations, and the D-20 roster statement"
      contains: "8+ planted facts"
  key_links:
    - from: "CLAUDE.md"
      to: "backend/app/db/workflows.py"
      via: "the ledger row that makes this file visible to its own guardrail for the first time"
      pattern: "db/workflows.py"
    - from: ".planning/STATE.md"
      to: ".planning/phases/193.2-from-authored-to-runnable/193.2-UAT.md"
      via: "the recorded no-override measurement and the owed versus driven UAT rows"
      pattern: "193.2"
---

<objective>
Close-out. Three jobs, and the first is not paperwork.

**The ledger rows are this phase's most durable deliverable.** Three of the six files G-5 fires on
were **ABSENT from the `CLAUDE.md` hot-file ledger**, so G-5 has never fired on them once — the
identical invisibility failure `WorkflowsPage.tsx` suffered for ten phases, `WorkflowDoorSwitch.tsx`
for six and `WorkflowBuilderPage.tsx` for ten. **A hot file missing from the table is permanently
invisible to its own guardrail**, because the discuss-phase audit scans PLAN.md `files_modified`
*against this table*. Research found a **fourth**: `backend/app/models/harness.py`, 16 commits across
15 phases, also absent. Writing these four rows is how Phase 194 inherits a measurement instead of
re-deriving one.

**The second job is the honest record.** A G-5 override was OFFERED AND DECLINED for the third
consecutive phase, so `STATE.md` must record **no override for 193.2** — and that absence is a
measurement, not an omission.

**The third is SC#4.** One real operator-driven end-to-end run, re-running the 193.1 UAT kit. It is
the ONLY row that exercises all three fixes together, in the order a person meets them: describe →
pre-filled requirement → publish **without deleting a step on the canvas** → find it at the top of
the library. 193.1 measured that a 3,600-case suite could not see the assumption that mattered, and a
`render_template` pin does not prove a template gets FILLED.

Output: four ledger rows, corrected figures beside the stale ones, `193.2-UAT.md`, updated
reported-bug and seed frontmatter, STATE and ROADMAP, and one driven run.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

**Worktree bootstrap FIRST:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`, then assert the base SHA
contains every prior plan's commits — this plan re-runs every gate at HEAD and its numbers are
worthless on a partial base.

⚠ **This plan is NOT autonomous.** Task 3 is a blocking checkpoint requiring the operator to drive a
real end-to-end run.

⚠ **Edit `.planning/STATE.md` BY HAND.** The GSD SDK's `state.*` verbs have written false records and
corrupted `STATE.md` repeatedly, including during Phase 193.1.
</execution_context>

<context>
@.planning/phases/193.2-from-authored-to-runnable/193.2-CONTEXT.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-RESEARCH.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-PATTERNS.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-BASELINE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The four hot-file ledger rows, and the corrections recorded beside the stale values</name>
  <files>CLAUDE.md</files>
  <read_first>
    - `CLAUDE.md` §"Workflow guardrails (MANDATORY)" — the whole hot-file ledger table, and in particular the `backend/app/services/harness/grounding.py` row, which is **the closest analog in size, habit and verdict shape**; plus the `frontend/src/pages/WorkflowsPage.tsx` row (the ten-phase invisibility story and its three self-corrections), the `WorkflowDoorSwitch.tsx` row (whose invisibility sentence explicitly asks to *"survive future edits"*), and the `WorkflowBuilderPage.tsx` row (the quick-task-bucket caveat on the `sed` recipe)
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-PATTERNS.md` §"`CLAUDE.md` hot-file ledger — four new rows" — **the seven required components in order**, and the two habits: correct BESIDE never over, and the self-staling note
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-RESEARCH.md` §"The four ledger rows (D-03)" — the measured table and the corrections list
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-BASELINE.md` §5 — the counts re-derived at the phase's OPEN
    - Every `193.2-*-SUMMARY.md` produced so far — for each file's D-02 no-second-concern argument as it was actually proved, and the invariants each plan established
  </read_first>
  <action>
**RE-DERIVE every figure at HEAD before writing it.** Do not copy from RESEARCH, from CONTEXT, or
from `193.2-BASELINE.md` §5 — this phase's own commits have moved several of them, and this table has
now documented itself going stale four separate times. Use:
`git log --oneline -- <file> | wc -l`, `wc -l <file>`, and
`git log --format=%s -- <file> | sed -E 's/^[a-z]+\(([^)]+)\).*/\1/' | sed -E 's/-.*//' | sort -u`
(⚠ that last recipe returns quick-task buckets like `quick` and `260814` alongside real phases —
**count them out and say so in the row**, as the `WorkflowBuilderPage.tsx` row already documents).

**Add FOUR rows**, each carrying the analog's seven components in order:
  1. touch list by phase, with quick-task buckets counted OUT
  2. the three measured figures — commits, phases, lines
  3. ⚠ **why the row exists, who added it and when** — and for the three that were missing, **the
     invisibility sentence, which is the reason they exist and must survive future edits:** *"THIS
     FILE WAS ABSENT FROM THIS TABLE UNTIL PHASE 193.2. N phases touched it and G-5 never fired once,
     because the discuss-phase audit scans PLAN.md `files_modified` against this table. A hot file
     missing from the table is permanently invisible to its own guardrail."*
  4. the re-derive commands, inline, with their measured outputs
  5. the G-5 verdict **with its measured reason** — the D-02 no-second-concern argument as the plans
     actually proved it, not as it was predicted
  6. the invariant or invariants that now bind the file
  7. the named next seam and the inherited obligation, ending with the triple the next phase inherits

Per-file specifics:
  - **`backend/app/db/workflows.py`** — say plainly that at its measured phase count it is the
    **second-hottest backend file in the tree**. ⚠ **D-16's divergence MUST be written into this row**
    — that is D-16's own stated condition, and the row is half of it, the code being the other half.
    Also record: on the published feed, `ORDER BY updated_at DESC` **IS** `ORDER BY publish-time DESC`
    (the publish flip is an UPDATE, `workflow_definitions_set_updated_at` is an unconditional
    `BEFORE UPDATE` trigger, and `workflow_definitions_block_published` then freezes the row); **no
    migration was needed, and why** (there is no index on `name` either, so the shipped sort was
    already unindexed; the table is 225 rows); and the shared-consumer consequence — the composer
    Harness picker, `WorkspacePanel`'s run-soul and `threads.py`'s kickoff all move from alphabetical
    to recency. Named next seam: the three list feeds versus the single-definition read and
    publish-flip versus the run CRUD.
  - **`backend/app/services/harness/publish_service.py`** — invariants: the refusal's two arms share
    no sentence and cannot be collapsed; no internal identifier, slug or step number reaches the copy;
    the phase label is clamped and single-lined before it enters user copy, an aria label and a
    PERSISTED audit row; **the gate STAYS**. Named next seam: the eight gauntlet stages versus the
    judge invocation versus the receipt writers.
  - **`backend/app/services/workflow_authoring.py`** — invariants: the prompt-bullet length discipline
    is now MECHANICAL (F-8) where it had been prose since Phase 189 and guarded by nothing; the
    provenance stamp is server-side, after validation, on the single success path, refuses empty and
    refuses a normalised copy of `describe`; and `grounding.py` was deliberately NOT edited (one home
    per concern, so the fix stays attributable when re-driven). Named next seam: the prompt literal
    versus the emit orchestration versus the post-validate stamps.
  - **`backend/app/models/harness.py`** — ⚠ **discovered by research, not by the table.** Invariants:
    additive-optional zero-migration growth only; `extra="forbid"` is NOT relaxed, so every other
    unknown key still raises; **no derivation may live in this model**, because the save path persists
    `model_dump(mode="json")` and a derivation would be baked into the JSONB where a stale row could
    lie about itself. Named next seam: the phase-spec union versus the definition schema versus the
    run and receipt models.

**Corrections, recorded BESIDE and never over** — the table's standing habit, which three rows already
demonstrate about themselves:
  - `frontend/src/pages/WorkflowsPage.tsx` — stale for the **FOURTH** consecutive time. Record the
    corrected triple beside the previous value and say plainly that it is the fourth staling.
  - `frontend/src/pages/WorkflowBuilderPage.tsx` — re-derive at HEAD (plan 193.2-09 touched it) and
    record beside, with the D-02 argument that plan proved: one gated sibling inside an existing
    affordance.
  - `backend/app/api/workflows.py` — re-derive and record; **this phase did not touch it**, so its
    obligation passes forward unchanged.
  - `frontend/src/components/workflows/library/WorkflowCard.tsx` — re-derive and record that **D-04
    kept it out of scope: it appears in NO plan's `files_modified` in this phase**, so its inherited
    G-5 obligation passes forward untouched. A measurement about a file nobody edited belongs in the
    table for the same reason the absences do.
  - ⚠ **Add the self-staling note where it applies:** a row whose line count is written in a commit
    that edits that same file goes stale on itself. `CLAUDE.md` is not one of the counted files, so
    this bites only if a later task in this plan edits a counted file — say which rows are safe and
    which are not.

⚠ **Also correct `CLAUDE.md`'s count-gate figure if it is still stale.** `193.2-BASELINE.md` §3/§4
measured a total that differs from the `3604` recorded in `CLAUDE.md`. Record the new figure BESIDE
the old with its re-derive command, exactly as the ledger rows do — a constant that rots silently is
the failure that rule already documents about itself, in the `GSD_VITEST_MAX_WORKERS` paragraph.

Commit `docs(193.2-10): four hot-file ledger rows, and the stale figures corrected beside (D-03)`.
  </action>
  <acceptance_criteria>
    - `CLAUDE.md`'s hot-file ledger contains four NEW rows naming `backend/app/db/workflows.py`, `backend/app/services/harness/publish_service.py`, `backend/app/services/workflow_authoring.py` and `backend/app/models/harness.py`
    - Each of the three previously-absent rows contains the invisibility sentence naming Phase 193.2 and the words `permanently invisible to its own guardrail`
    - Each new row contains its re-derive commands with measured outputs, a G-5 verdict with a measured reason, at least one binding invariant, and a named next seam ending with the inherited triple
    - The `db/workflows.py` row contains the literal token `D-16`, states the divergence, the no-migration reason, and the shared-consumer consequence
    - The `WorkflowsPage.tsx` cell carries a new correction recorded BESIDE the prior value and states it is the FOURTH staling
    - The `WorkflowCard.tsx` cell records that D-04 kept it out of scope and that its obligation passes forward untouched
    - Every figure written was RE-DERIVED at HEAD in this task; the summary lists each command and its output
    - `CLAUDE.md`'s count-gate figure is corrected beside the stale `3604` with its re-derive command
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && grep -q "backend/app/services/workflow_authoring.py" CLAUDE.md && grep -q "backend/app/models/harness.py" CLAUDE.md && grep -q "permanently invisible to its own guardrail" CLAUDE.md</automated>
  </verify>
  <done>Four hot files that could not be seen by their own guardrail are on the table with measured, re-derivable figures, and every stale cell this phase touched is corrected beside its previous value.</done>
</task>

<task type="auto">
  <name>Task 2: 193.2-UAT.md, the bug and seed frontmatter, STATE.md, ROADMAP, and every gate re-run at HEAD</name>
  <files>.planning/phases/193.2-from-authored-to-runnable/193.2-UAT.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/reported-bugs/template-first-drafts-cannot-be-published.md, .planning/reported-bugs/cannot-find-a-just-published-workflow.md, .planning/seeds/SEED-163-authoring-does-not-propose-the-business-requirement.md</files>
  <read_first>
    - `.planning/phases/193.1-*/193.1-UAT.md` — the shape this file copies, and the U5 row SC#4 re-runs
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-VALIDATION.md` §"Manual-Only Verifications" and §"D-20 — the narrowed roster"
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-RESEARCH.md` §"Open Questions" — OQ-1, OQ-2, OQ-5, OQ-6, each with its exact experiment
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-FREQUENCY.md` — the measured k/N figures this file CITES rather than re-derives
    - `.planning/reported-bugs/template-first-drafts-cannot-be-published.md`, `.planning/reported-bugs/cannot-find-a-just-published-workflow.md`, `.planning/seeds/SEED-163-authoring-does-not-propose-the-business-requirement.md` — the frontmatter fields (`status`, `folded_into`, `verified_closed_by`, `re_open_trigger`) and the project's status lifecycle
    - `.planning/STATE.md` §"Phase 193.2" and its guardrail-overrides section; `.planning/ROADMAP.md` §"Phase 193.2: From Authored to Runnable"
    - `CLAUDE.md` §"UAT scoreboard recipe" — the roster rule and the *"blocked, never silently omitted"* rule
  </read_first>
  <action>
**(a) Author `.planning/phases/193.2-from-authored-to-runnable/193.2-UAT.md`.** UAT rows live here,
**never as PLAN.md tasks**. Rows:
  - **U1 — D-21, the single end-to-end run.** Re-run the 193.1 UAT kit at
    `C:\Users\fhdmr\Desktop\uat-193.1-qbr\`, scoring rule **8+ planted facts**. The order a person
    meets it: describe, then **the requirement arrives pre-filled and visibly marked**, then publish
    **without deleting a step on the canvas**, then **find it near the top of the library**. Record
    each of the four moments separately so a partial pass is legible rather than averaged away.
  - **U2 — OQ-1, the rendered refusal.** Open the Builder on a draft carrying an interactive step;
    capture the `publish-blocked-reason` `textContent` and the `publish-trigger` `disabled` state via
    Chrome MCP `evaluate_script` (⚠ `take_screenshot` times out on this box — read DOM geometry
    instead), and re-run the `publish_blocked` count query to confirm it stays 0. The row records
    whether the message names the step the operator can actually see.
  - **U3 — OQ-2, the CTA latency.** Publish from the Builder; timestamp the click and the first
    appearance of `run-cta`.
  - **U4 — OQ-5, the rendered position.** After publish, observe the library **without searching**;
    record the row's rendered index. The measured prediction is **4 of 109** — position 4 is not
    position 1, and this row asks the operator's judgement rather than asserting a pass.
  - **U5 — OQ-6, the composer picker.** Open the composer's Harness workflow picker and confirm the
    recency ordering reads sensibly. It is the one surface outside the library the
    `list_published_workflows` change reaches.
  - **The D-20 roster statement.** State the roster rule, state why it is narrowed here
    (`resolve_authoring_model` is explicitly not the composer's model; `harness_authoring_model` is
    env-only, has no `app_settings` column and no UI, and needs a restart), name the two driven rows,
    and **carry all six N/A rows with their reasons**. Cite `193.2-FREQUENCY.md` for the measured k/N
    rather than re-deriving it. ⚠ *A scoreboard that lists only what passed is not a scoreboard.*
  - ⚠ **Any row not driven is recorded as OWED with its reason and named as owed in STATE.md** —
    never silently omitted, and never marked pass from another row's evidence.

**(b) Reported-bug and seed frontmatter.** Both bugs and the seed carry `folded_into: "193.2"`. Update
each per the project's status lifecycle: flip to `closed` with `verified_closed_by` **only if U1
verifiably closes it**; otherwise leave `status: folded` with a note, and give any residual a concrete
`re_open_trigger`. ⚠ `BUG-260815-01` is prompt-mitigated, not eliminated — D-08 makes it a
**reduction**, so it may only close if the operator's driven run publishes without a canvas edit, and
the frontmatter must say the claim is a frequency reduction, citing the k/N.

**(c) `.planning/STATE.md` — BY HAND.** Record the phase's position and next action; the plans
shipped; the four ledger rows; the measured k/N; and which UAT rows were driven versus OWED, naming
the first one to run. Then:
  - **`Guardrail overrides: NONE for Phase 193.2`** — a G-5 override was OFFERED AND DECLINED, the
    third consecutive phase to decline one, **and that absence is a measurement.**
  - **D-22** — the three-instance pattern is named for **197**: `SEED-157` (`/generate` accepted
    `template_placeholders` for five phases and the frontend never sent it), `SEED-163`
    (`business_requirement` — the emit tool advertised the field and the prompt never asked for it),
    and the AI-chosen **name** the author never gets to set. **197 inherits it as a measured pattern
    rather than re-deriving it.**
  - **D-24** — letting the author name or rename the workflow, routed to **197 / AUTH-02**. Re-open
    trigger: 197, or a report of a wrong AI-chosen name reaching a client.
  - **D-25** — a workflow that deliberately pauses for a person and can still be published, routed to
    `SEED-164`. ⚠ **193.2 SUPPRESSED the unwanted step; it did NOT deliver this**, and nothing shipped
    may imply otherwise.
  - **D-18** — the user-facing recency versus A-to-Z sort control, deferred to the library sketch so
    it is designed once. Re-open trigger: that sketch, or a second report of wanting alphabetical
    back. Alongside it, the **card density / list-vs-card layout** deferral, where **G-2 fires** and
    `SEED-155` binds.
  - **D-05** — G-2 did not fire on anything in scope and G-1 did not fire; ⚠ **a third `193.x` would
    trip G-1.** Record it for whoever comes next.

**(d) `.planning/ROADMAP.md`.** Replace the phase's `**Plans**: TBD` line with the ten plans and their
one-line objectives, and update the progress row. **Do not mark a success criterion satisfied that U1
has not driven.**

**(e) Re-run every gate at HEAD** and record the figures beside their `193.2-BASELINE.md` values:
backend full `tests/unit`, the six backend workflow suites, the five frontend workflow suites, the
count gate, and `tsc --noEmit -p tsconfig.app.json`. ⚠ **Gate on the count gate plus the named
suites, never on a bare full-suite run** — the 26 frontend and 62 backend pre-existing failures are
the SEED-056 rot set and no plan in this phase touched any of them. Any NEW failure is this phase's
and blocks close.

Commit `docs(193.2-10): UAT rows, bug and seed frontmatter, STATE and ROADMAP, gates re-run at HEAD`.
  </action>
  <acceptance_criteria>
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-UAT.md` exists with rows U1 through U5, the D-20 roster statement, and all eight roster rows (2 driven, 6 N/A with reasons)
    - U1 contains the literal string `8+ planted facts` and lists the four moments separately
    - U2 names `publish-blocked-reason` and `publish-trigger` and the `evaluate_script` method, with the `take_screenshot` caveat stated
    - U4 states the measured prediction `4 of 109` and asks the operator's judgement rather than asserting a pass
    - Both reported-bug files and `SEED-163` have updated frontmatter; any file left `folded` carries a note, and any residual carries a concrete `re_open_trigger`
    - `.planning/STATE.md` contains the literal string `NONE for Phase 193.2` and records D-22, D-24, D-25, D-18 and D-05 with their re-open triggers or forward routing
    - `.planning/ROADMAP.md` no longer contains `**Plans**: TBD` for Phase 193.2 and lists ten plans
    - The summary records every gate figure re-run at HEAD beside its `193.2-BASELINE.md` value and states plainly whether any NEW failure appeared
    - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` prints `count gate OK`
    - The summary confirms `.planning/STATE.md` was edited BY HAND and no `gsd-sdk state.*` verb was used
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG" && grep -q "NONE for Phase 193.2" .planning/STATE.md && grep -q "8+ planted facts" .planning/phases/193.2-from-authored-to-runnable/193.2-UAT.md && GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs</automated>
  </verify>
  <done>The UAT contract is authored where UAT rows belong, every deferral and inherited pattern has a written home with a trigger, the no-override measurement is recorded, and every gate has been re-run at HEAD against its pre-change baseline.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: D-21 — the one real operator-driven end-to-end run</name>
  <files>.planning/phases/193.2-from-authored-to-runnable/193.2-UAT.md</files>
  <action>
PAUSE. Present the summary below to the operator and wait for the driven result. This task performs
no automated work: SC#4 is the one row no test in this repository can stand in for. When the operator
answers, record each of the five moments separately in `193.2-UAT.md`, update the reported-bug and
seed frontmatter to match what was actually observed, and name any undriven row as OWED in
`.planning/STATE.md`. Do NOT mark a ROADMAP success criterion satisfied that this run did not drive.
  </action>
  <what-built>
    All three fixes are in and each is pinned by automated tests, but **no test in this repository can
    stand in for this row.** 193.1 measured that a 3,600-case suite could not see the assumption that
    mattered, and a `render_template` pin does not prove a template gets FILLED.

    What changed, in the order you will meet it:
    1. **The business requirement arrives pre-filled and visibly marked as an AI proposal** — you
       should not be asked to restate what you already typed into the describe box. It is editable,
       and editing it removes the mark.
    2. **The draft should publish without you deleting a step on the canvas.** If a step genuinely
       cannot be validated, the refusal now names that step **by the label you can read on the
       canvas** and tells you what to do — it no longer names `llm_human_input` or `ask_user`.
       ⚠ **This is a measured frequency reduction, not a guarantee** (see `193.2-FREQUENCY.md`); the
       publish gate deliberately stays, because an unsubscribed `ask_user` can wedge a publish
       indefinitely.
    3. **The library now orders your published work and your drafts by recency.** The curated starters
       shelf stays alphabetical, deliberately. Measured prediction: your new workflow lands at
       rendered position **4 of 109** — behind the three starters, first among your own.
  </what-built>
  <how-to-verify>
    Re-run the Phase 193.1 UAT kit at `C:\Users\fhdmr\Desktop\uat-193.1-qbr\`. Scoring rule:
    **8+ planted facts**.

    1. Upload the kit's knowledge base and its 10-field `.docx` template; describe the QBR workflow
       exactly as in 193.1's U5.
    2. **Before doing anything else, look at the business requirement field.** Is it filled? Is the
       AI-proposal mark visible? Record what it says.
    3. **Press Publish without touching the canvas.** Did it publish? If it refused, **copy the
       refusal text verbatim** — does it name a step you can see on the canvas, and does it tell you
       what to do?
    4. **Go to the library and find the workflow without searching and without knowing its name.**
       Record its rendered position and whether the post-publish Run CTA appeared.
    5. **Run it.** Score the produced document against the kit's planted facts and confirm the
       template fields are filled and the branding is intact — this is SC#4, and it is the row that
       proves nothing regressed `AUTH-03`.
    6. Optional, 30 seconds: open the composer's Harness workflow picker and say whether the new
       recency ordering reads sensibly there (U5 / OQ-6).

    Record the outcome of each of the five moments separately in `193.2-UAT.md`. **A partial pass is a
    legitimate outcome and must be stated as one** — closing the phase with owed rows is fine, but it
    is a DECISION, never a claim that everything ran.
  </how-to-verify>
  <resume-signal>Type "approved" with the scores and any refusal text you saw, or describe what failed</resume-signal>
  <verify>
    <human-check>The operator reports the score against the kit's planted facts (rule: 8+), whether the requirement arrived pre-filled and marked, whether the draft published without a canvas edit, and the workflow's rendered position in the library.</human-check>
  </verify>
  <done>SC#4 has been driven end to end by the operator, each of the five moments is recorded separately in `193.2-UAT.md`, and any owed row is named as owed in STATE.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| **the operator's live environment during U1** | A real describe, a real publish, a real run against the operator's own local Supabase and real provider keys |
| **UAT observations to a committed artifact** | Whatever the run produced gets written into the repository |
| documentation to future guardrails | The ledger rows are what makes G-5 able to fire on four files at all |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193.2-10-A | **Information disclosure** | UAT evidence pasted into `193.2-UAT.md` | **mitigate** | Record verdicts, positions, timings and the refusal text. **Do NOT paste knowledge-base content, provider keys, or the produced document's body.** The kit's planted facts are synthetic; the surrounding KB may not be |
| T-193.2-10-B | Repudiation | a UAT row marked pass from another row's evidence | **mitigate** | Each of the five moments is recorded separately; a partial pass is stated as a partial pass and any undriven row is recorded OWED with its reason in both `193.2-UAT.md` and `STATE.md`. *A scoreboard that lists only what passed is not a scoreboard* |
| T-193.2-10-C | Repudiation | closing a reported bug that still reproduces | mitigate | `BUG-260815-01` may close only if U1 publishes without a canvas edit, and its frontmatter must state the claim as a frequency **reduction** with the k/N cited — D-08 forbids an absence claim |
| T-193.2-02 | Information disclosure | the RLS-bypassing feed predicates | accept | Unchanged by this plan; the predicate guards (F-2) are green and re-run at HEAD in task 2 |
| T-193.2-SC | Tampering | npm/pip/cargo installs | accept | No package installed by this phase at all |
</threat_model>

<verification>
- `grep -c "permanently invisible to its own guardrail" CLAUDE.md` ≥ 3 (the three previously-absent rows)
- `grep -q "NONE for Phase 193.2" .planning/STATE.md`
- `.planning/ROADMAP.md` lists ten plans for Phase 193.2 and no `**Plans**: TBD`
- Backend full `tests/unit`, the six backend workflow suites, the five frontend workflow suites, the count gate and `tsc -p tsconfig.app.json` all re-run at HEAD with no NEW failure against `193.2-BASELINE.md`
- `193.2-UAT.md` carries U1–U5 and all eight roster rows
</verification>

<success_criteria>
- Four ledger rows exist with re-derived figures, the invisibility sentence, a measured G-5 verdict, binding invariants and a named next seam
- Every stale cell this phase touched is corrected BESIDE its previous value
- `STATE.md` records the declined override as a measurement, and every deferral and inherited pattern has a written home with a trigger
- SC#4 has been driven by the operator, with each of the five moments recorded separately and any owed row named as owed
</success_criteria>

<output>
Create `.planning/phases/193.2-from-authored-to-runnable/193.2-10-SUMMARY.md` when done.
</output>
