# Phase 193.2: From Authored to Runnable - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-15
**Phase:** 193.2-from-authored-to-runnable
**Areas discussed:** Guardrails (G-5), The proposed business requirement, The interactive step, Findability, Naming, Verification shape

---

## Guardrails (G-5)

Presented after a `git log` measurement rather than a ledger read — three of the six files this
phase must touch are **absent** from `CLAUDE.md`'s hot-file ledger and have therefore never
triggered their own guardrail (`db/workflows.py` 30 commits / 16 phases; `publish_service.py`
16 / 6; `workflow_authoring.py` 9 / 5).

| Option | Description | Selected |
|--------|-------------|----------|
| Honour by construction + add ledger rows | No extraction; argue the measured "no second concern" test per file; write the three missing ledger rows so 194 inherits a measurement | ✓ |
| Extract first, feature second | Cut a seam (likely the three list-feed functions) before the feature, per the 192.1 / 193.1 order | |
| Log a formal override | Record a guardrail override in STATE.md and proceed to the feature | |

**User's choice:** Honour by construction + add ledger rows
**Notes:** Third consecutive phase (193, 193.1, 193.2) to be offered an override and decline it —
so `STATE.md` recording no override for 193.2 is a measurement, not an omission. The `WorkflowsPage.tsx`
ledger cell was measured stale for the fourth time (`33 / 12 / 1160` → `34 / 12 / 1176`).

---

## The proposed business requirement (SEED-163)

### How the proposal arrives

| Option | Description | Selected |
|--------|-------------|----------|
| Pre-filled + visibly marked as an AI proposal | Populated, editable, visibly AI-authored — reusing the shipped `name_seeded_by_ai` provenance pattern | ✓ |
| Pre-filled silently, indistinguishable from typed | Cheapest; the field is simply no longer blank | |
| Shown as a suggestion the author clicks to accept | Field stays empty, proposal sits beside it with a "Use this" action | |

**User's choice:** Pre-filled + visibly marked
**Notes:** A silent pre-fill would repeat this phase's own root failure in miniature — a decision
the author is never shown.

### Fallback when the model emits nothing

| Option | Description | Selected |
|--------|-------------|----------|
| Blank + the shipped `REQUIREMENT_INVITATION` | Falls back to today's exact behaviour; no new failure mode | ✓ |
| Server-side derive as a second attempt | A cheap second LLM call when the emit omits it | |
| Make it required on the emit schema | Pydantic rejects a definition without it | |

**User's choice:** Blank + the shipped invitation string
**Notes:** Makes the fix explicitly non-deterministic — the phase may claim a measured frequency
reduction, never absence.

### Whether an AI-seeded value should satisfy the publish gate

| Option | Description | Selected |
|--------|-------------|----------|
| Passing is fine — pressing Publish IS the consent | No gate change; the visible AI mark plus the Publish press is the checkpoint | ✓ |
| Seeded requirements must be acknowledged before publish | The gate distinguishes seeded from author-confirmed | |
| You decide | | |

**User's choice:** Passing is fine
**Notes:** The rejected option would add a genuine second concern to `publish_service.py` and void
the D-01 "no second concern" argument for that file.

---

## The interactive step (BUG-260815-01)

Scouting reframed this area before the questions were asked: `/validate` **already** mints an
`interactive_phase` verdict on every canvas edit, it is registered in both `_ROUTE_ASSIGNED_CODES`
and `_INCOMPLETE_CODES`, it **already** greys the Publish control, the server's message is rendered
verbatim, and there is a pinned test (*"187-28 — a route-assigned verdict GATES the Publish
control"*). That contradicts the operator receiving a 400 from a click.

### Whether to measure why the shipped gate did not fire

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — measure it in Wave 1, before building anything | Reproduce the exact path; record whether `/validate` ran, what it returned, whether the trigger was disabled | ✓ |
| No — rewrite the string and suppress the step; assume the gate works | Cheaper and faster | |
| You decide | | |

**User's choice:** Measure it in Wave 1
**Notes:** Same sequencing 193.1 used, which found that phase's central assumption FALSE. If the
gate is silently not firing, that is the real defect and a better message fixes nothing.

### Suppression strength

| Option | Description | Selected |
|--------|-------------|----------|
| Prompt clause only, in the DELIVERABLE RULE's own voice | Symmetric with the D-26 fix that caused this; non-deterministic, so the gate stays | ✓ |
| Prompt clause + server-side strip of the emitted phase | Deterministic removal before the draft returns | |
| Prompt clause + emit it but pre-flag it on the canvas | The step survives, marked as the blocker | |

**User's choice:** Prompt clause only
**Notes:** A server-side strip silently deletes a step the model judged necessary — the exact root
this phase exists to fix — and forecloses SEED-164's capability at the wrong layer.

### What the rewritten refusal should do

| Option | Description | Selected |
|--------|-------------|----------|
| Name the step by its visible label + offer to remove it | The plain-language name a non-coder reads on the canvas, plus an action | ✓ |
| Name it, point at it, no action offered | Same honesty, no destructive control | |
| Name it + say the capability is coming (SEED-164) | Adds a forward promise | |

**User's choice:** Name by label + offer removal
**Notes:** The third option was rejected on the grounds SEED-164 itself records — unscheduled work
that reads like a plan is precisely the error that seed exists to correct, and repeating it in
shipped user-facing copy has a wider audience.

---

## Findability (BUG-260815-02)

A shipped positive-controlled fence was confirmed during scouting —
`rowIdentity.test.ts:167`, *"T-7 / P-4 — the ROW LIST is never re-ordered"* — so server `ORDER BY`
is the sole authority on library order.

### Recency key

| Option | Description | Selected |
|--------|-------------|----------|
| `updated_at DESC` | Already on the wire and consumed by `relativeChanged`'s nine bands, so card text and list order agree by construction | ✓ |
| `created_at DESC` | Strictly the publish moment | |
| You decide | | |

**User's choice:** `updated_at DESC`

### Which feeds change

| Option | Description | Selected |
|--------|-------------|----------|
| Published + drafts by recency; starters stay alphabetical | The two feeds holding the author's own work get recency; a curated catalogue stays A–Z | ✓ |
| All three by recency — one rule, no exceptions | Satisfies the bug's "change them together" warning literally | |
| All three, but starters by their own curated order | Adds an ordering column | |

**User's choice:** Published + drafts only
**Notes:** Deliberately diverges from the bug's own "change them together" warning; accepted on
condition the divergence is written into the ledger row and the code.

### Fixed default vs a sort control

| Option | Description | Selected |
|--------|-------------|----------|
| Fixed default — no new control | Changing the default is a defect fix and ships regardless; a sort dropdown is G-2 territory | ✓ |
| Recency default + an A–Z toggle in the toolbar | Preserves alphabetical for anyone who wants it | |

**User's choice:** Fixed default
**Notes:** A control built now risks being relocated or redesigned by the pending library sketch.

### What SC#3 still needs, given the post-publish CTA already exists

| Option | Description | Selected |
|--------|-------------|----------|
| Recency sort + establish why the CTA didn't save you | Measure the shipped surface before building beside it | ✓ |
| Recency sort + navigate straight to the row on publish | The bug's candidate fix 2 | |
| Recency sort alone — the CTA is enough once the row is at the top | Minimal | |

**User's choice:** Sort + measure the CTA
**Notes:** Same discipline as the interactive gate. `WorkflowsPage.tsx` carries an inherited G-5
obligation, so a navigate-to-row behaviour would be a genuine second concern.

---

## Naming

| Option | Description | Selected |
|--------|-------------|----------|
| Out of scope — defer to 197 / AUTH-02 with a re-open trigger | No success criterion mentions naming; a rename on a published row touches the slug identity/forks/versioning key off | ✓ |
| In scope — same shape as the business requirement | Editable, AI-marked name before publish | |
| In scope, but read-only — surface the name earlier | Show it in the Builder, no editing | |

**User's choice:** Out of scope, deferred to 197

### Whether to record the three-instance pattern

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — name it as a standing finding for 197 | SEED-157, SEED-163 and the AI-chosen name are three instances of one shape | ✓ |
| No — leave it to whoever hits the third | Keeps the paperwork tight | |

**User's choice:** Record it
**Notes:** SEED-163 itself says that at a third instance *"the pattern is the phase, not the field"*.
A deferral living in one file is invisible to the next phase — documented four times in this project.

---

## Verification shape

### Cross-provider roster

| Option | Description | Selected |
|--------|-------------|----------|
| No full roster — test the resolvable authoring models only | Authoring runs on a fixed `forced_emission`-capable model, not the composer's selection | ✓ |
| Full 8-row roster anyway | Maximum coverage | |
| You decide | | |

**User's choice:** Resolvable authoring models only
**Notes:** The other six rows are recorded N/A with the reason, never dropped — a scoreboard that
silently omits rows is worse than one listing only passes.

### SC#4 regression proof

| Option | Description | Selected |
|--------|-------------|----------|
| One real end-to-end operator-driven run at phase close | Re-run the 193.1 UAT kit; the only row exercising all three fixes together | ✓ |
| Automated regression only | Pin the grounding / deliverable behaviour in tests | |
| Both | Belt and braces | |

**User's choice:** One real run at close
**Notes:** 193.1 measured that a 3,600-case suite could not see the assumption that mattered, and a
`render_template` pin does not prove a template gets filled.

---

## Claude's Discretion

- Exact wording of the AI-proposal mark and the rewritten refusal message (bounded by D-13, D-14 and
  the shipped `REQUIREMENT_INVITATION` voice).
- Whether the removal action in D-13 is a live control or a precise instruction — to be decided from
  what D-10 measures about which surface the author is actually looking at.
- Wave / plan decomposition, subject to D-10 and D-19 landing before anything they could invalidate.

## Deferred Ideas

- Card density / list-vs-card library layout → `/gsd:sketch` under G-2, bound by SEED-155.
- A user-facing sort control (recency ⇄ A–Z) → designed once, in that same sketch.
- Letting the author name or rename the workflow → 197 / AUTH-02 (`ForkNameDialog.tsx` is the asset).
- A workflow that deliberately pauses for a person and can still be published → `SEED-164`.
- The stripped-attribution quality observation from 193.1 → held until it recurs.
