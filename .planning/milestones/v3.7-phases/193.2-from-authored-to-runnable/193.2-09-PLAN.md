---
phase: 193.2-from-authored-to-runnable
plan: 09
type: execute
wave: 5
depends_on: ["193.2-07"]
files_modified:
  - frontend/src/pages/WorkflowBuilderPage.tsx
  - frontend/src/components/workflows/builderStore.ts
  - frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "D-06 — the pre-filled requirement carries a VISIBLE mark that the AI wrote it; a silent pre-fill indistinguishable from typed text is rejected, because that repeats this phase's own root failure in miniature — a decision the author is never shown"
    - "D-06 — the mark demotes on edit, clearing the flag in the SAME `set()` that writes the text, mirroring the shipped `definitionOps.ts` rule that clears value and marker together so neither can describe the other falsely"
    - "D-08 — when the model emits nothing the surface is byte-identical to today: a blank field showing the shipped `REQUIREMENT_INVITATION`, no mark, no new string"
    - "D-02 — `WorkflowBuilderPage.tsx` gains no SECOND concern: the mark is one gated sibling inside the existing `requirementAffordance`, which is the shape that file's ledger row already asks for"
    - "The nine-phase-old `FLAG_OFF_HEADER_MARKUP` byte pin is UNMOVED — the mark lives inside the same `canvasEnabled` ternary as `requirementAffordance`, so band 3 cannot change; `git diff --numstat` on `WorkflowBuilderPage.header.test.tsx` must be EMPTY"
  artifacts:
    - path: "frontend/src/pages/WorkflowBuilderPage.tsx"
      provides: "The visible AI-proposal mark, mounted as a sibling inside the existing requirement affordance under the same canvasEnabled gate"
      contains: "requirementAffordance"
    - path: "frontend/src/components/workflows/builderStore.ts"
      provides: "Demote-on-edit: the provenance flag cleared in the same set() that writes the text"
      contains: "setBusinessRequirement"
    - path: "frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx"
      provides: "The mark's behaviour cases, asserted on the PATCH body for the wire half and on the DOM for the render half"
      contains: "business-requirement"
  key_links:
    - from: "frontend/src/pages/WorkflowBuilderPage.tsx"
      to: "backend/app/models/harness.py"
      via: "the durable provenance field stamped server-side in plan 193.2-07, read off the definition and rendered as a mark"
      pattern: "seeded"
    - from: "frontend/src/components/workflows/builderStore.ts"
      to: "frontend/src/components/workflows/definitionOps.ts"
      via: "the shipped demote-on-edit rule that clears value and marker together"
      pattern: "setBusinessRequirement"
---

<objective>
D-06's visible half. The requirement arrives pre-filled — plan 193.2-07 made that durable — and it
must arrive **visibly marked as an AI proposal.**

The rejected alternatives are recorded and are not re-litigable: a **silent pre-fill**
indistinguishable from typed text repeats this phase's own root failure in miniature (a decision the
author is never shown), and the project already judged that shape wrong once; an **empty field with a
click-to-accept proposal** is still one extra act before publish, which is the friction `SEED-163`
exists to remove.

Purpose: the mark is what makes D-09 defensible. The publish gate is unchanged and an AI-seeded value
passes stage 1 untouched — that is accepted **because the author can see the value is the AI's and
overrule it**, and pressing Publish is the consent. Without the mark, D-09 would be a silent
weakening of the judge's own criterion.

⚠ **Two traps this plan must not spring.** (1) The mark must live inside the **same `canvasEnabled`
ternary** as `requirementAffordance`, because `FLAG_OFF_HEADER_MARKUP` band 3 is the `<header>`
hosting `identityGroup` and that literal *"had stood unedited for NINE phases"* — its own re-capture
protocol says the phase expects **no third re-capture**. (2) The mark's behaviour cases go in
`WorkflowBuilderPage.canvas.test.tsx`, **not** `header.test.tsx` — all eight shipped
requirement-affordance cases live there, and RESEARCH routes them to the wrong file. **This plan
corrects that routing, and says so.**

Output: one gated sibling node, one flag cleared in an existing `set()`, and their cases next door to
the fixture that already exists.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

**Worktree bootstrap FIRST:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`, then assert the
dispatched base SHA — this plan runs **after** 193.2-07, whose model field it renders.
**Every vitest invocation carries `GSD_VITEST_MAX_WORKERS=2`.**
</execution_context>

<context>
@.planning/phases/193.2-from-authored-to-runnable/193.2-CONTEXT.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-RESEARCH.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-PATTERNS.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The visible mark, one gated sibling inside the existing affordance</name>
  <files>frontend/src/pages/WorkflowBuilderPage.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowBuilderPage.tsx:2030-2093` — `requirementAffordance` in full and its docblock, plus `identityGroup` and its sibling `kbAffordance`. Note **all four constraints the mark inherits**: (1) the same `canvasEnabled` gate and the same ternary; (2) the glyph convention — `✎` is this page's own describe glyph and **`✦` is REFUSED**, because it is the shipped Working badge and the icon convention keeps it off canvas-adjacent surfaces since it sits on the verdict mark's coordinates; (3) **no page-level `useState` mirror** — in the drafted view the definition is the single source of truth and a second copy is the drift D-14 forbids; (4) the mark mounts as a sibling inside this `<span>`, which mounts in `identityGroup`
    - `frontend/src/pages/WorkflowBuilderPage.tsx:383` — `REQUIREMENT_INVITATION`, D-08's unchanged fallback placeholder
    - `frontend/src/pages/WorkflowBuilderPage.header.test.tsx:316-405` — **READ, DO NOT EDIT.** The re-capture protocol and `FLAG_OFF_HEADER_MARKUP`; band 3 is the `<header>` hosting `identityGroup`, and with the flag OFF `requirementAffordance` is `null`, which is exactly why band 3's literal shows only `vendor-brief` + `draft`
    - `backend/app/models/harness.py` — the provenance field plan 193.2-07 added, for its exact name
    - `.claude/skills/sketch-findings-agentic-rag/` — **load the findings skill** if the icon convention or a canvas-adjacent glyph decision is in question; `references/icon-convention.md` §4 is the canvas glyph vocabulary
  </read_first>
  <action>
Add ONE node to `requirementAffordance` in `frontend/src/pages/WorkflowBuilderPage.tsx`, rendered
only when the definition's provenance flag is true AND the requirement text is non-empty.

**Placement — non-negotiable.** It is a sibling INSIDE the existing
`data-testid="builder-business-requirement"` span, inside the SAME `canvasEnabled ? (...) : null`
ternary. Do not add a new top-level node to `identityGroup`, do not add a second ternary, do not
change the `<header>`'s class list. Give it its own `data-testid` (e.g.
`business-requirement-ai-mark`) so its presence is assertable by testid rather than by class name.

**Copy and glyph — Claude's discretion, bounded.** The wording is bounded by the shipped
`REQUIREMENT_INVITATION` voice, by D-13's register (say what is true, plainly) and by **D-14** (no
`planned` / `coming` / `soon` / `deferred` / `future release`). It must read as *the AI proposed this;
it is yours to change* — not as an endorsement and not as a warning. Prefer a short label plus a
`title`/`aria-label` carrying the fuller sentence, matching the affordance's existing
`aria-label` idiom. **⚠ `✦` is refused** — reuse `✎` or pick a third glyph; do not reach for the
Working badge's mark.

**Reading the flag.** Read it off `meta` / the definition exactly as the input reads
`meta.business_requirement` — **no page-level `useState` mirror.** `BuilderDefinition` carries an
index signature, so no exported type needs widening; if you find one does, say so in the summary
rather than widening a type quietly.

**D-08's fallback stays byte-identical.** When the requirement is empty or absent: no mark, blank
input, the shipped `REQUIREMENT_INVITATION` placeholder. No new string, no alternative empty-state
copy. A mark on an empty value would make the demote rule read a lie, which is the same rule the
server-side stamp obeys.

⚠ **`WorkflowBuilderPage.header.test.tsx` must end this plan byte-identical.** Run it and confirm
green with `git diff --numstat` EMPTY for that file. If band 3 reds, the mark is mounted outside the
`canvasEnabled` gate — fix the placement, **do not re-capture the literal.** That literal has stood
nine phases and was re-captured exactly twice in Phase 193, each time declared and dated; a third
re-capture is *"a behaviour change to explain in its own plan, not a test to update."*

**D-02, proved not asserted:** state in the summary that the mark is one gated sibling inside an
existing affordance — the "no second concern" shape `WorkflowBuilderPage.tsx`'s ledger row already
asks for. If you find otherwise, STOP; D-01 is void for this file and a refactor recommendation is
owed FIRST.

Commit `feat(193.2-09): the requirement arrives visibly marked as an AI proposal (D-06)`.
  </action>
  <acceptance_criteria>
    - The new node renders inside the `data-testid="builder-business-requirement"` span and inside the SAME `canvasEnabled` ternary — verified by reading the diff, and by the flag-off case in task 2
    - The node has its own `data-testid` and an accessible name (`aria-label` or `title`)
    - The mark's copy matches none of the case-insensitive tokens `planned`, `coming`, `soon`, `deferred`, `future release`
    - The glyph is not `✦` — `grep` the added lines to confirm
    - No `useState` is added to `WorkflowBuilderPage.tsx` for this feature; the flag is read off the definition
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowBuilderPage.header.test.tsx` → 0 failed, AND `git diff --numstat -- frontend/src/pages/WorkflowBuilderPage.header.test.tsx` is EMPTY
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` is at the `193.2-BASELINE.md` §3 baseline error count
    - The summary states the D-02 no-second-concern argument for `frontend/src/pages/WorkflowBuilderPage.tsx`
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowBuilderPage.header.test.tsx</automated>
  </verify>
  <done>The AI proposal is visible where the author is already looking, inside the existing affordance and the existing flag gate, and the nine-phase-old header pin has not moved.</done>
</task>

<task type="auto">
  <name>Task 2: Demote on edit — the flag cleared in the same set() that writes the text</name>
  <files>frontend/src/components/workflows/builderStore.ts</files>
  <read_first>
    - `frontend/src/components/workflows/builderStore.ts:655-672` — `setBusinessRequirement` and its docblock, especially **WHY THERE IS NO TRIM AND NO EMPTY-CHECK**: whitespace is written THROUGH, the server owns the emptiness rule (`grounding.py` is literally `not (definition.business_requirement or "").strip()`), and a client that trimmed or nulled here would be a second copy of a server predicate, which D-182-06 forbids
    - `frontend/src/components/workflows/builderStore.ts:695+` — `setTemplateAsset`, the third structural mirror of the one-`set()` shape
    - `frontend/src/components/workflows/definitionOps.ts:283-320` — **the shipped demote-on-edit rule to mirror**: it clears BOTH `name` and `name_seeded_by_ai`, because *"Clearing only the name would leave a provenance marker describing a name that no longer exists"*; a HAND-TYPED value is never cleared, and *"that asymmetry is the entire reason the marker exists"*; and the keys are `delete`d rather than set to `undefined`, because a phase that carried no name must come back with **no key**, not an explicit `undefined` the round trip would serialize into a backend union with `extra="forbid"`
  </read_first>
  <action>
Extend `setBusinessRequirement` in `frontend/src/components/workflows/builderStore.ts` so the
provenance flag is cleared **in the same `set()` that writes the text**. One `set()`, `dirty` in the
same write — the shape the store already keeps three times.

**The rule, and why it is this rule.** The flag is cleared on **ANY** edit. Do **not** try to decide
client-side whether the text is "still the AI's sentence" (comparing against the seeded value,
diffing, debouncing) — that would be exactly the second copy of a server predicate the function's own
docblock forbids by name.

**Key handling.** Mirror `definitionOps.ts:283-320`: the flag and the value travel together. Since
the backend field defaults to `False` rather than being absent, either `delete` the key or set it to
`false` — **pick one, and state the reason in a comment**, checking the round-trip against the
backend union (`extra="forbid"` accepts the declared key; an explicit `undefined` serialised into
JSON is the trap `definitionOps` avoided by deleting).

**Whitespace stays written through.** Do not add a trim, an empty-check or a null. The server owns
emptiness. ⚠ Note the honest consequence and put it in the comment: editing a seeded value down to
whitespace clears the flag AND leaves a whitespace value — which is correct, because the server's
stamp would also refuse to mark it, and the shipped `REQUIREMENT_INVITATION` placeholder does not
show for a whitespace value. That is today's behaviour and this plan does not change it.

Commit `feat(193.2-09): the AI mark demotes on any edit, in the same set() that writes the text (D-06)`.
  </action>
  <acceptance_criteria>
    - `setBusinessRequirement` writes the text and clears the provenance flag in ONE `set()` call, with `dirty: true` in the same write — verified by reading the diff
    - No trim, no empty-check, no null-coercion is added; `grep` the changed lines for `.trim()` finds none
    - No comparison against a previously-seeded value is performed client-side
    - A comment states which key strategy was chosen (delete vs `false`) and why, referencing the backend union's `extra="forbid"`
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/builderStore.test.ts` → 0 failed
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` at baseline
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/components/workflows/builderStore.test.ts</automated>
  </verify>
  <done>A single keystroke on the requirement demotes the mark, in the same write, with no client-side copy of a server predicate.</done>
</task>

<task type="auto">
  <name>Task 3: The mark's cases — in canvas.test.tsx, where the fixture already lives</name>
  <files>frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx</files>
  <read_first>
    - `frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx:3195-3320` — **all eight shipped requirement-affordance cases**, including `withoutRequirement()` and `renderRequirement(features, def)` at `:3196-3212` (the mount helpers to REUSE), the flag-gate case at `:3287-3300` (**the positive-control template for a UI absence**: *"a `queryByTestId(...) === null` assertion alone passes just as happily against a control that was never built"* — so the negative and its positive control live in ONE test), the behaviour-guard rule at `:3214-3228` (**asserted on the RECORDED CALL ARGUMENT, never on a DOM value — a controlled input echoing its own prop would pass a DOM read while the payload stayed empty**), and the reload round-trip case at `:3230-3238`
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-PATTERNS.md` §"Corrections to upstream artifacts" **C-4** — RESEARCH and VALIDATION both route these cases to `header.test.tsx`; measured, `grep -rn "business-requirement" frontend/src --include=*.test.tsx` returns **8 hits, all in `canvas.test.tsx`, zero in `header.test.tsx`**
  </read_first>
  <action>
⚠ **State the correction explicitly in the summary before writing a case.** `193.2-RESEARCH.md`'s
Wave-0 gap list and `193.2-VALIDATION.md`'s SC#1 row both name
`frontend/src/pages/WorkflowBuilderPage.header.test.tsx` as the home for the AI-mark cases. That is
**wrong and it is being corrected here**: `header.test.tsx`'s relevance is the OPPOSITE one — it owns
`FLAG_OFF_HEADER_MARKUP`, the pin the mark must not move. Routing new cases there would create a
second home for a fixture that already exists next door. **This plan corrects the routing and says
that it is correcting it.**

Add the cases to the shipped requirement-affordance describe in `WorkflowBuilderPage.canvas.test.tsx`,
reusing `renderRequirement` and `withoutRequirement`:

1. **Seeded → the mark renders.** A definition carrying a non-empty requirement AND the provenance
   flag true renders `business-requirement-ai-mark` with its accessible name.
2. **Hand-typed → no mark, WITH its positive control in the same test.** A definition carrying the
   same non-empty requirement and the flag false/absent renders NO mark — then, in the same `it()`,
   `cleanup()` and render the seeded variant and assert the mark IS present. Copy the `:3287-3300`
   shape exactly: an absence assertion alone passes just as happily against a control that was never
   built.
3. **Empty → no mark (D-08's fallback, byte-identical to today).** An empty or absent requirement
   renders no mark, and the input still shows the shipped `REQUIREMENT_INVITATION` placeholder —
   assert the placeholder by reading the exported constant, **not by re-typing the sentence**.
4. **Flag OFF → nothing at all.** With `canvasEnabled` false, both the affordance and the mark are
   absent — this is the case that proves the mark cannot reach the flag-off header and therefore
   cannot move band 3.
5. **Demote on edit, asserted ON THE PATCH BODY.** Type into the input on a seeded definition and
   assert the **recorded update call argument** carries the demoted flag — **never a DOM read.** A
   controlled input echoing its own prop would pass a DOM check while the payload stayed wrong; the
   suite records this rule at `:3214-3228` and this is exactly the case it was written for. Assert
   the mark also disappears from the DOM, as a second, weaker check.
6. **The reload round trip.** Model on `:3230-3238`: a definition re-opened from a persisted payload
   carrying the flag still renders the mark. **This is the case that justifies the model field in
   plan 193.2-07** — page state alone would fail it — so name that in the docstring.

⚠ **Do not add cases to `header.test.tsx`.** Run it, confirm green, confirm `git diff --numstat`
EMPTY.

⚠ **The count gate is pinned per file.** Re-run it; if the pinned total moved, update the pin in the
same commit and say so.

Commit `test(193.2-09): the AI-proposal mark's cases, routed to canvas.test.tsx where the fixture lives (C-4)`.
  </action>
  <acceptance_criteria>
    - All six cases exist in `frontend/src/pages/WorkflowBuilderPage.canvas.test.tsx`; `grep -c "business-requirement-ai-mark"` returns at least 5
    - `grep -c "business-requirement" frontend/src/pages/WorkflowBuilderPage.header.test.tsx` returns `0`, and `git diff --numstat -- frontend/src/pages/WorkflowBuilderPage.header.test.tsx` is EMPTY
    - The hand-typed case contains BOTH the absence assertion and its positive control in the same `it()`
    - The demote case asserts on the recorded update call argument (`mockUpdate.mock.calls`), not on a DOM value — verified by reading the case
    - The empty-state case reads `REQUIREMENT_INVITATION` from its export rather than re-typing the sentence
    - The reload case's docstring names the model field as the reason page state would not suffice
    - `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowBuilderPage.canvas.test.tsx src/pages/WorkflowBuilderPage.header.test.tsx` → 0 failed
    - `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` → `count gate OK`
    - The summary states, in words, that RESEARCH's and VALIDATION's routing to `header.test.tsx` was wrong and has been corrected
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/frontend" && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowBuilderPage.canvas.test.tsx src/pages/WorkflowBuilderPage.header.test.tsx</automated>
  </verify>
  <done>The mark renders when seeded, vanishes when hand-typed or empty, survives a reload, demotes on the wire and not merely in the DOM — and the byte pin next door never moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| **persisted definition → the Builder render** | The provenance flag and the requirement text come from a JSONB row the author owns |
| **the requirement text → the rendered mark's neighbourhood** | Model-authored free text already rendered by this affordance today |
| client → autosave PATCH | The demoted flag must reach the wire, not just the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| **T-193.2-03** | **Elevation of privilege (weakened gate)** | an AI-authored `business_requirement` that the judge later grades against | **mitigate — this plan IS the human-visible half of the mitigation** | D-09 accepts the unchanged gate **because the author can see the value is the AI's and overrule it**, and pressing Publish is the consent. The visible mark is what makes that acceptance honest rather than silent. Without it, D-09 would be an invisible weakening of `answers_business_requirement`. Server-side controls (empty refusal, describe-copy refusal) landed in plan 193.2-07 |
| T-193.2-01 | Information disclosure | the requirement text rendered beside the mark | accept | Unchanged: `business_requirement` already renders here as a controlled input value and elsewhere as text (T-124-01); no new render path, no `dangerouslySetInnerHTML`, no HTML string construction |
| T-193.2-09-A | Spoofing | a client-side "is this still the AI's sentence?" decision | mitigate | Forbidden by construction: the flag is cleared on ANY edit in the same `set()`. A client-side comparison would be a second copy of a server predicate, which `builderStore.ts`'s own docblock forbids by name (D-182-06) |
| T-193.2-09-B | Tampering | the autosave PATCH under `extra="forbid"` | mitigate | The demoted flag is a DECLARED field (plan 193.2-07), so the PATCH validates. The key strategy (delete vs `false`) is chosen deliberately and commented, and the demote case asserts on the recorded call argument so a DOM-only pass cannot hide a wrong payload |
| T-193.2-SC | Tampering | npm/pip/cargo installs | accept | No package installed; no new dependency, no new component library |
</threat_model>

<verification>
- `cd frontend && GSD_VITEST_MAX_WORKERS=2 npx vitest run src/pages/WorkflowBuilderPage.canvas.test.tsx src/pages/WorkflowBuilderPage.header.test.tsx src/components/workflows/builderStore.test.ts` → 0 failed
- `git diff --numstat HEAD~3 -- frontend/src/pages/WorkflowBuilderPage.header.test.tsx` → EMPTY
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` → `count gate OK`
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` → baseline error count
</verification>

<success_criteria>
- The mark renders only when the value is non-empty AND seeded, inside the existing affordance and the existing `canvasEnabled` gate
- It demotes on any edit, in the same `set()`, and the demotion is proved on the PATCH body
- It survives a reload — the case that justifies the model field
- `FLAG_OFF_HEADER_MARKUP` is byte-unmoved and `header.test.tsx` has zero new cases
- The C-4 routing correction is stated
</success_criteria>

<output>
Create `.planning/phases/193.2-from-authored-to-runnable/193.2-09-SUMMARY.md` when done.
</output>
