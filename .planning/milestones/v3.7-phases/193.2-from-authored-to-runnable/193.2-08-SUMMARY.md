---
phase: 193.2-from-authored-to-runnable
plan: 08
subsystem: workflow authoring (the k/N frequency measurement)
tags: [AUTH-03, SC#1, SC#2, SC#4, D-08, D-20, OQ-4, frequency, roster, k/N]
base_sha: 39970e6f09a7505509c5de903bdc20297fbb25d0
base_sha_short: 39970e6f
requires:
  - "backend/app/services/workflow_authoring.py — the 193.2-05 prompt change, MEASURED not modified"
  - "backend/app/models/harness.py — business_requirement_seeded_by_ai (193.2-07), read"
  - "backend/tests/integration/test_187_authoring_roster.py — the analog, modelled on, NEVER modified"
  - "backend/app/services/template_render_service.py — parse_docx_template_variables, the shipped parser"
provides:
  - "backend/tests/integration/test_193_2_authoring_frequency.py — the opt-in k/N harness, 2 rows x 2 arms"
  - "193.2-FREQUENCY.md — the measured scoreboard, all eight roster rows, the D-08 rule verbatim"
  - "the ONLY artifact permitted to state whether 193.2-05 and 193.2-07 work in practice"
affects:
  - "plan 193.2-09 — its three-bucket assumption and its mark's COPY both change on findings below"
  - "plan 193.2-10 — inherits the roster narrowing and the three corrections recorded here"
tech-stack:
  added: []
  patterns:
    - "opt-in gated PER ROW inside the test, so a blocked row RECORDS itself before it skips"
    - "per-row SimpleNamespace settings stub — zero global mutation on a paid cross-provider drive"
    - "counters PLANTED before their zeroes are published (a blind zero and an earned zero are indistinguishable)"
    - "a stochastic control asserted as a FLOOR, with the smaller drop carried by a reported FLAG"
key-files:
  created:
    - backend/tests/integration/test_193_2_authoring_frequency.py
    - .planning/phases/193.2-from-authored-to-runnable/193.2-FREQUENCY.md
  modified: []
decisions:
  - "D-08 honoured: no case asserts absence over model output; the rule is stated verbatim in the artifact"
  - "D-20 honoured: all eight rows recorded, six N/A with reasons, none dropped"
  - "the external_action displacement counter ADDED beyond the plan (193.2-06's finding), reported never as a publish failure"
  - "a SECOND like-for-like arm driven on operator authorization, because the plan's same-artefact premise measured FALSE"
  - "the attribution arm was NOT driven — the phase claims a combined effect and attributes nothing (OQ-4)"
metrics:
  duration: ~2h (incl. a 411 s paid run and a blocking checkpoint)
  completed: 2026-08-15
  tasks: 3
  commits: 4
  files_changed: 2
  insertions: 1383
  deletions: 0
  live_provider_calls: 20
---

# Phase 193.2 Plan 08: The k/N Authoring-Frequency Measurement — Summary

**One-liner:** Twenty real paid generations across two providers and two arms measured
`business_requirement` **20/20** against a pre-fix 0/N, `llm_human_input` **0/20** against 2/2-with-a-
template, and the SC#4 control **5/5 on both providers on a like-for-like arm the plan did not ask
for** — plus three corrections to the phase's own documents, one of which changes what plan 09 may
assume.

---

## Base commit — the wrong-base assertion did NOT fire (the first time this phase)

| Property | Value |
|---|---|
| Dispatched base | `39970e6f` |
| `git rev-parse HEAD` at start | **`39970e6f09a7505509c5de903bdc20297fbb25d0`** — correct |
| Branch | `develop` — **the MAIN WORKING TREE, no worktree** |
| Why no worktree | This plan drives real provider calls and reads the operator's local Supabase. Worktrees isolate files, not Postgres (CLAUDE.md parallel-execution rule 4) |

⚠ Recorded because the running count is the evidence, and a NON-fire is as much a data point as a
fire: the 21 prior instances were all worktree dispatches. This plan ran serially on the main tree by
design, and arrived where it was sent.

**The run measured the FIXED code, proved rather than assumed.** All five commits of plans 05 and 07
were asserted ancestors of the drive commit (`git merge-base --is-ancestor` → exit 0 for `0e203d64`,
`82b1324b`, `efab299c`, `0cb3faf4`, `54651a93`), and the two literals were greped present. A
frequency measured against the old prompt would have been worthless.

---

## What shipped

| Task | Commit | What |
|---|---|---|
| 1 | `555a6c4d` | the k/N harness — 2 DRIVE rows, 6 recorded N/A, opt-in, zero global mutation |
| 2 | `a154b056` | the `uat8` like-for-like arm (the checkpoint's outcome) |
| — | `c87e137d` | the positive control that makes a zero readable — driven RED |
| 3 | `253a8014` | `193.2-FREQUENCY.md`, the measured scoreboard |

`git diff --numstat 39970e6f HEAD` → **exactly two files, 1383 insertions / 0 deletions.**
`backend/app/`, `frontend/src/` and `supabase/` are **absent from the diff entirely** — this plan
measures the fix, it does not touch it.

---

## THE MEASUREMENT — and the only shape it may be stated in

**20 real paid generations · 20 successes · 0 failed calls · 410.9 s wall clock.**

| Figure | anth `kit10` | anth `uat8` | oai `kit10` | oai `uat8` | Pre-fix baseline |
|---|---|---|---|---|---|
| **`business_requirement` non-empty (SC#1)** | **5/5** | **5/5** | **5/5** | **5/5** | **0/N** |
| — stamped `business_requirement_seeded_by_ai` | 5/5 | 5/5 | 5/5 | 5/5 | field is new |
| **`llm_human_input` present (SC#2)** | **0/5** | **0/5** | **0/5** | **0/5** | **2/2** w/ template; **4/6** overall |
| — `ask_user` validator (the gate's 2nd shape) | 0/5 | 0/5 | 0/5 | 0/5 | not measured |
| **`render_template` present (SC#4 CONTROL)** | **5/5** | **5/5** | **5/5** | **5/5** | 193.1's **3/3** |
| **`external_action` present (DISPLACEMENT)** | **0/5** | **0/5** | **0/5** | **0/5** | not measured |

⚠ **THE CLAIM IS A REDUCTION, NEVER AN ABSENCE.** `0/20` is a frequency over a sample, not a
guarantee; one more call could produce an interactive step, and when it does the fallback is today's
exact behaviour (D-08). **The publish gate stays, and nothing downstream may be relaxed on the
strength of these numbers.** The harness enforces this mechanically — **no case in it asserts absence
over model output**, and the artifact states the rule verbatim before any number.

**SC#4 did NOT fall, and it is now provable rather than arguable.** On the like-for-like arm the
control measured **5/5 on both providers against 193.1's 3/3** — same keys, same describe, same 100%
rate. No `SC#4-DEGRADED` flag on any row. **The two arms AGREE** (5/5 everywhere), so the
disagreement-is-the-finding case the operator named does not arise, and that is stated as a
measurement rather than assumed away.

---

## ⚠ THE PLAN'S CENTRAL PREMISE WAS FALSE, AND THE CHECKPOINT IS WHY IT WAS CAUGHT

`193.2-08-PLAN.md` says to reuse the kit's ten-field `.docx` *"so SC#2 and SC#4 measure the same
artefact"*. **Measured before spending anything: FALSE.** 193.1's `render_template` **3/3** was driven
on a **different** placeholder set — the eight-key weekly-project-status list with its own describe
(`193.1-UAT.md` rows A/B/C). Same kit folder, different artefact. A ten-field arm could only have been
compared to 3/3 as a floor, and the plan's own SC#4 language would have been a like-for-like claim
resting on two different things.

Surfaced at the Task-2 checkpoint rather than discovered afterwards. The operator authorized a
**second arm** on that basis, so the control is now a genuine re-drive of 193.1's exact shape. **This
is what the blocking checkpoint bought** — the alternative was a phase claiming an SC#4 comparison it
could not support.

---

## ⚠ THE COUNTERS WERE PLANTED BEFORE THEIR ZEROES WERE PUBLISHED

Three of four headline figures are **zeroes**. *A zero from a blind counter and a zero the model
earned are indistinguishable in an artifact* — and this phase's standing lesson is that a fence never
driven RED has never been shown to see anything (193.1 found four such; plan 03 found a clause already
green for an unrelated reason; plan 06 found a fence that passed its plant; plan 09 found a
nine-phase-old pin green under the exact plant it was credited with catching).

So `_count` was driven against a definition planting **every** shape it claims to see, and the control
now **ships permanently** in the harness:

| Shape planted | Read | Proves |
|---|---|---|
| an `llm_human_input` phase | **1** | the SC#2 counter can fire |
| `llm_emit` + `emitter: render_template` | **1** | the SC#4 control can fire |
| an `external_action` phase | **1** | the displacement counter can fire |
| a validator with `on_failure: ask_user` | **1** | the second-shape counter can fire |
| `business_requirement: "   "` | **absent, 0 chars** | a blank cannot count as an SC#1 success |
| `llm_emit` + `emitter: generic_docx` | **0** | the control keys on the EMITTER, not on `llm_emit` |

**And the control itself was driven RED against a real plant** — widening the predicate to count any
`llm_emit`:

```
E       assert 1 == 0
tests\integration\test_193_2_authoring_frequency.py:943: AssertionError
FAILED …::test_positive_control_every_counter_can_actually_fire
1 failed, 4 passed, 10 skipped
```

Restored from a pre-plant copy held **outside the watched tree**; `git diff --numstat` → **109
insertions / 0 deletions** (the control test only), **5 passed** on re-run. No `git clean`, no
`git checkout -- .`, no `git reset --hard` at any point.

---

## THREE CORRECTIONS TO THE PHASE'S OWN DOCUMENTS — one of them binds plan 09

Recorded in `193.2-FREQUENCY.md` §6 as well, because a correction living only in a run log is
invisible.

### (a) The plan's same-artefact premise — covered above.

### (b) ⚠ `193.1-11-SUMMARY.md`'s `inputs[]` conclusion is TRUE OF ANTHROPIC AND FALSE AS A GENERAL CLAIM

193.1-11 measured `inputs[] is null` on **6 of 6** and told plan 09 that D-20's run-input bucket *"is
fed by nothing"*, so its degenerate-first design *"STANDS and must not be softened"*. Measured across
20 calls:

| Row | `inputs[]` declared | keys, when declared |
|---|---|---|
| anthropic `kit10` | **0/5** | — |
| anthropic `uat8` | **0/5** | — |
| **openai `kit10`** | **2/5** | `client_name`, `review_period` |
| **openai `uat8`** | **5/5** | `project_name`, `reporting_period`, `stakeholder_audience` |

**Anthropic reproduces 193.1 exactly (0/10). `gpt-5.5` feeds the bucket on 7 of 10, with keys that ARE
placeholder names.** ⚠ **This is a PROVIDER difference, not an effect of this phase** — 193.1 never
drove `gpt-5.5`, so nothing here attributes it to the prompt change, and it is stated as a provider
observation rather than as a fix. **Plan 09 must still survive the degenerate case** — it is what the
model that actually resolves produces every time — but it should stop believing the bucket can never
be fed.

### (c) ⚠ D-07 durability is only partly obeyed, and the provenance mark CANNOT see that

On the `kit10` arm, anthropic named a one-run parameter in **0 of 5** requirements; **openai in 5 of
5** (*"…for customer Northwind Logistics covering Q3 2026…"* — one run's parameters baked into the
workflow's definition of done, precisely what D-07 asks against). **All 20 were still stamped
`seeded_by_ai: True`, and that is CORRECT rather than a bug:** the stamp's question is *"is this a
normalised copy of the describe text?"*, and `193.2-07` explicitly rejected a fuzzy similarity metric
with its reason recorded in `_normalised_for_copy_check`'s docstring.

**So the finding is about READING the mark, not fixing it: the AI-proposal mark means "a model wrote
this", never "this is durable".** Plan `193.2-09`'s copy must not imply the latter.

---

## The roster — all eight recorded, and the six are blocked by the RESOLVER

D-20 honoured: **2 DRIVE + 6 recorded ⛔ N/A**, each carrying its reason, **seeded into `_RESULTS` at
module import** so the full table is emitted even if no test executes. The narrowing is a measurement,
not a shortcut — `resolve_authoring_model` is explicitly *"NOT the composer's selected model"* and
`harness_authoring_model` measured `None` (no `app_settings` column, no UI, needs a restart), so only
two ids are reachable in a shipping backend.

⚠ **Every one of the eight provider keys is configured in this environment.** The six are therefore
blocked by the RESOLVER, not by a missing key — the stronger and more honest reason, stated so the
rows cannot later read as an environment gap. The moonshot row additionally carries the
unforceable-model finding (`test_187_authoring_roster.py:44-52`, adjacent to `BUG-260731-01`),
**marked NOT this phase's**, and a free test asserts both tokens are present so it cannot be quietly
dropped.

---

## The `external_action` displacement counter — added beyond the plan, and reported correctly

Not in the plan; added on `193.2-06`'s finding. **Measured 0 of 20 — no displacement occurred.** Had
it been non-zero it would still **not** be a publish failure: the armed checkpoint is auto-continued
on a golden run (`harness_engine.py:837`) and the send is skipped at `phase_types.py` GATE 1.
**Widening the gate is FORBIDDEN** — `test_the_armed_checkpoint_is_not_a_validator` pins that
emptiness deliberately (D-19 CONFLICT-1 Option B, REJECTED). ⚠ `SEED-164`'s *"always asks approval"*
phrase is true of a LIVE run and misleading as a publish-path claim; both readings are recorded.

---

## Attribution — NOT resolved, and it says so (OQ-4)

Plan `193.2-05` made **two** edits in one literal (the suppression clause AND the bullet shortened
121 → 74). **A single measurement cannot attribute figure 2 to either.** The third arm — the clause
with the ORIGINAL 121-character bullet — would have needed a temporary edit to production source and
twenty more paid calls to answer a question no success criterion asks; the operator's authorization
was explicitly for the **control**. **It was not driven, and the artifact says so plainly**: the phase
claims a combined effect and attributes nothing.

That figure 3 is a valid control at all is a consequence of the same discipline: `193.2-05` left
`grounding.py` byte-untouched, so the D-26 arms are on an unchanged input and a fall could only have
come from the prompt.

---

## Verification

| Gate | Baseline | Measured | Verdict |
|---|---|---|---|
| the harness WITHOUT the opt-in | — | **5 passed · 10 skipped · 0 failed**, all 8 rows emitted, zero network | ✅ |
| the plan's Task-3 verify, verbatim | — | `grep -q "The claim is a reduction, not an absence"` **&&** pytest → **5 passed** | ✅ |
| `pytest tests/unit -k "authoring or publish_service or 187 or 193"` | 0 failed | **185 passed · 0 failed** | ✅ |
| **full `pytest tests/unit`** | **62 failed / 2154 passed** | **62 failed · 2154 passed · 2 xfailed · 2 xpassed** | ✅ **exactly the baseline** |
| `git diff --numstat` on `test_187_authoring_roster.py` | must be EMPTY | **EMPTY** | ✅ |
| production source touched | must be none | `backend/app/`, `frontend/src/`, `supabase/` **absent from the diff** | ✅ |
| no vitest / tsc run | — | not run — this plan owns no frontend file | n/a |

⚠ **The frontend count gate was NOT run**, deliberately: this plan owns no frontend file, the
inherited baseline warns the full-tree figure is not reproducible, and a concurrent gate run is how
that gate goes non-deterministic.

### Acceptance criteria

| Criterion | Result |
|---|---|
| harness exists; runs green with no keys and no opt-in; emits all 8 rows | ✅ 5 passed · 10 skipped |
| `_RESULTS` has 8 entries after a no-opt-in run; 6 N/A carry reasons | ✅ **8** and **6**, seeded at import |
| registry guard asserts `capability_source == "registry"` and runs without the opt-in | ✅ plus a `forced_emission` assertion |
| `grep -c SimpleNamespace` ≥ 1; no global settings assignment | ✅ **3**; zero assignments |
| no API key VALUE anywhere; presence is a `bool(...)` only | ✅ one `bool(getattr(...))` expression |
| `_scoreboard_writer` carries the `sys.stdout.encoding` guard | ✅ — and **observed working**: the console echo degraded to `?` while the UTF-8 file kept `⛔` |
| no absence assertion in the driven-row body | ✅ **0 hits** for `assert not` / `not in` / ` is None` across its 167 lines |
| `test_187_authoring_roster.py` UNCHANGED | ✅ EMPTY numstat |
| artifact contains the reduction-not-absence sentence VERBATIM | ✅ (kept on ONE line on purpose — see below) |
| artifact has exactly 8 provider rows; 6 with reasons; moonshot marked not-this-phase's | ✅ **8 / 6 / 1** |
| §3 states N and three k/N figures per driven row vs named baselines | ✅ four figures, per arm |
| `render_template` compared explicitly against 193.1's 3/3; flagged if it fell | ✅ **5/5**, like-for-like; it did not fall |
| §4 states whether the third attribution arm was driven | ✅ NOT driven, said plainly |
| `grep -i "always populated\|never appears\|guaranteed"` finds no CLAIM | ✅ one hit — the §1 **prohibition** prose |
| harness still passes without the opt-in after the run | ✅ |

⚠ **One acceptance criterion caught a real defect in my own artifact.** The verbatim sentence was
first written WRAPPED across two lines, so the plan's literal `grep -q` returned **0** — a copy a
machine cannot find is not a copy. Reflowed onto one line, with a note in the artifact saying why it
must stay there.

---

## Deviations from Plan

**Two, both authorized at the checkpoint; plus two strengthenings.**

1. **A SECOND arm (`uat8`) was driven** — 20 generations instead of 10. Authorized by the operator
   after the plan's same-artefact premise measured FALSE. It is the reason figure 3 is a like-for-like
   comparison rather than a floor.
2. **The `external_action` displacement counter was added**, beyond the plan's three figures, on
   `193.2-06`'s explicit flag. Reported as a composition shift, never as a publish failure.
3. **A permanent positive control was added and driven RED** (not required). Without it, three of the
   four headline figures would be unfalsifiable zeroes.
4. **Two extra free guards**: every arm's describe is swept for the suppressing sentence (the kit
   README's *"Do not add a step that asks me for input."*, dropped deliberately — including it would
   have made SC#2 a measurement of the instrument), and the two arms are proved to differ in **both**
   variables with 193.1's key count pinned at 8.

**No deviation rules 1–4 fired.** No bug in production code, no missing critical functionality, no
blocker, no architectural question. **No package installed** (T-193.2-SC). No migration. No
`STATE.md` / `ROADMAP.md` write — plan `193.2-10` owns both.

---

## Threats — all four dispositions discharged

| Threat | Disposition |
|---|---|
| **T-193.2-08-A** information disclosure | Key presence is one `bool(getattr(settings, f"{provider}_api_key", ""))`; the value never leaves it. `CallOutcome` carries the requirement's **length**, never its text, so KB-grounded prose cannot reach a committed artifact through the scoreboard. The raw definitions went to the session scratchpad **outside the watched tree** and are not committed; the two requirement excerpts quoted in §6(c) were read and chosen deliberately (both are the model's own summary of the task, carrying no KB content) |
| **T-193.2-08-B** tampering with the operator's environment | Per-row `SimpleNamespace` stub only — **3 occurrences, zero global assignments**. No `app_settings` write. `harness_authoring_model` measured `None` before and is untouched |
| **T-193.2-08-C** cost | Opt-in, gated per row inside the test, behind a blocking checkpoint. Cap authorized at 20 generations; **exactly 20 driven**, and the cap is recorded in the harness's own docstring |
| **T-193.2-08-D** an overstated claim | D-08 stated verbatim before any number; no absence assertion; six N/A rows recorded rather than dropped; the un-driven attribution arm declared |
| **T-193.2-03 / T-193.2-SC** | Unchanged / nothing installed. This plan measures the path; it does not change it |

---

## Known Stubs

**None.** The `_MOONSHOT_EXTRA` string and `PLACEHOLDERS_UAT8` tuple are asserted-against constants,
not placeholders.

## Threat Flags

**None.** No network endpoint, auth path, file access pattern or schema change was added. The harness
makes outbound provider calls, but only behind an opt-in env var that is unset by default.

---

## What a later plan may rely on

| Claim | Value |
|---|---|
| `business_requirement` arrives non-empty | **20/20** measured — a REDUCTION in blank frequency, never a guarantee |
| `llm_human_input` composed | **0/20** measured — a reduction; the publish gate STAYS |
| SC#4 `render_template` control | **5/5 both providers, like-for-like against 193.1's 3/3** — did not fall |
| `external_action` displacement | **0/20**, and it could not wedge a publish even if non-zero |
| `business_requirement_seeded_by_ai` stamped | **20/20** — plan 09's mark has real data on every draft |
| `inputs[]` fed | anthropic **0/10** · gpt-5.5 **7/10** — a PROVIDER difference, not this phase's |
| The AI-proposal mark means | *"a model wrote this"* — **never** *"this is durable"* |
| Attribution between the clause and the bullet | **UNRESOLVED and undriven** — a combined effect is claimed |

---

## Self-Check: PASSED

| Claim | Command | Result |
|---|---|---|
| `backend/tests/integration/test_193_2_authoring_frequency.py` exists | `ls -1` | **FOUND** |
| `.planning/…/193.2-FREQUENCY.md` exists | `ls -1` | **FOUND** |
| `.planning/…/193.2-08-SUMMARY.md` exists | `ls -1` | **FOUND** |
| commit `555a6c4d` exists | `git log --oneline` | **FOUND** |
| commit `a154b056` exists | `git log --oneline` | **FOUND** |
| commit `c87e137d` exists | `git log --oneline` | **FOUND** |
| commit `253a8014` exists | `git log --oneline` | **FOUND** |

No missing items.
