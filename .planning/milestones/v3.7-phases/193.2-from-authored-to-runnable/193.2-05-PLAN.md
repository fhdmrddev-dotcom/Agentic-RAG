---
phase: 193.2-from-authored-to-runnable
plan: 05
type: execute
wave: 3
depends_on: ["193.2-01", "193.2-02"]
files_modified:
  - backend/app/services/workflow_authoring.py
  - backend/tests/unit/test_workflow_authoring_requirement.py
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "D-11 — suppression of the unwanted interactive step is a PROMPT CLAUSE ONLY, in the DELIVERABLE RULE's own voice; no server-side strip of the emitted phase, no pre-flagged canvas affordance, and the publish gate STAYS"
    - "D-11 — the over-long `llm_human_input` bullet is shortened toward the module's own measured median; a bullet 45 percent above the median that ends with an active invitation is a nudge toward the one phase type the publish gate categorically refuses"
    - "D-07 — the prompt asks for the DURABLE requirement (what the workflow must deliver for ANY run), never a copy of the describe text, which is one run's task instruction"
    - "D-08 — the fix is explicitly NON-DETERMINISTIC: nothing here asserts the field is always populated; frequency is measured as k/N in plan 193.2-08 and the claim is a reduction, never an absence"
    - "D-02 — `workflow_authoring.py` gains no SECOND concern: `AUTHORING_SYSTEM_PROMPT` already carries a DELIVERABLE RULE about publishability and already names the definition-level fields to set; this adds one clause to that rule and one field to that sentence"
    - "F-8 — the prompt-bullet length discipline, documented in prose since Phase 189 and guarded by nothing, becomes mechanical: the lengths are RE-DERIVED from the literal, never hand-counted"
    - "D-14 — no shipped prompt or copy text promises an unscheduled capability"
  artifacts:
    - path: "backend/app/services/workflow_authoring.py"
      provides: "The DELIVERABLE-RULE suppression clause, the shortened llm_human_input bullet, and the business_requirement instruction"
      contains: "business_requirement"
    - path: "backend/tests/unit/test_workflow_authoring_requirement.py"
      provides: "F-8 plus the deterministic prompt and schema assertions for SC#1 and SC#2"
      contains: "AUTHORING_SYSTEM_PROMPT"
  key_links:
    - from: "backend/app/services/workflow_authoring.py"
      to: "backend/app/models/harness.py"
      via: "WF_SCHEMA is WorkflowDefinition.model_json_schema() and already advertises business_requirement — the prompt is the only gap"
      pattern: "WF_SCHEMA"
---

<objective>
Two of this phase's three findings are one Python string literal.

`SEED-163`: the emit tool **already advertises** `business_requirement` (`WF_SCHEMA` is
`WorkflowDefinition.model_json_schema()` and the field is on the model), yet
`grep -c business_requirement backend/app/services/workflow_authoring.py` returns **0**. Nothing in
the schema or the prompt says what the field is for, so the model never fills it and the author is
asked to restate, by hand, what they already typed into the describe box.

`BUG-260815-01` (blocking): 193.1's own D-26 fix told the model it MUST fill exactly N named template
fields; when the grounding cannot supply them all, the reasonable composition is *"add a step that
asks the human"* — an `llm_human_input` phase the synchronous publish gate categorically refuses.
Measured 2 for 2 with a template. And, unanticipated by the CONTEXT: the `llm_human_input` bullet is
the **longest of the seven at 121 characters against a verified median of exactly 83.5**, and it ends
with an active invitation. **By the module's own recorded rule, the prompt is currently nudging the
model toward the one type the gate refuses.**

Purpose: the cheapest and most on-rule half of both fixes is prompt text, in one home. The
suppression clause belongs HERE and not in `grounding.py` — the code already states why: *"One home
per concern: the rule states the policy, the grounding states the FACTS the policy reads."* And the
D-26 arm must NOT be weakened to reduce `llm_human_input` frequency: softening it trades a blocked
publish for a broken run (`no_template_bound` is terminal at run time) and would regress SC#4.

Output: three edits inside one literal, plus a new deterministic test file. **The provenance stamp
and the additive model field are plan 193.2-07**, which depends on this one.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md

**Worktree bootstrap FIRST:** `bash scripts/bootstrap-worktree.sh "$(pwd)"`, then assert the
dispatched base SHA against `193.2-BASELINE.md`.

**Provider-docs-first applies:** `AUTHORING_SYSTEM_PROMPT` is provider-facing work. The resolvable
authoring models are **anthropic `claude-opus-4-8`** (what this environment actually resolves) and
**openai `gpt-5.5`**. **Do NOT claim any behavioural effect of this prompt change in this plan** —
prompt-behaviour claims come from driven calls, and those are plan 193.2-08's k/N run.

**Runs in parallel with 193.2-03, 193.2-04 and 193.2-06** — disjoint files, no database mutation.
</execution_context>

<context>
@.planning/phases/193.2-from-authored-to-runnable/193.2-CONTEXT.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-RESEARCH.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-PATTERNS.md
@.planning/phases/193.2-from-authored-to-runnable/193.2-BASELINE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Three edits inside AUTHORING_SYSTEM_PROMPT — the shortened bullet, the suppression clause, the requirement instruction</name>
  <files>backend/app/services/workflow_authoring.py</files>
  <read_first>
    - `backend/app/services/workflow_authoring.py` — `:20-31` (the one-home-per-concern header), `:56-105` (`AUTHORING_SYSTEM_PROMPT`: the seven phase-type bullets, the DELIVERABLE RULE labelled *"CRITICAL — a wrong choice makes the workflow unpublishable"*, the HARD-constraints rules, and the closing *"Set the definition `slug`, `version` (1), `name`, and `status` ('draft')"* sentence), `:67-68` (the 121-char `llm_human_input` bullet being shortened), `:70-81` (the `external_action` bullet and **the only place in this repo where the length rule is written down**), `:108-133` (`resolve_authoring_model`), `:148-163` (`WF_SCHEMA` / `EMIT_TOOL`)
    - `backend/app/models/harness.py:531-548` — `business_requirement: str | None = None` and its *"the ONE business requirement the QUAL-01 judge anchors to"* comment
    - `backend/app/services/harness/grounding.py:579-635` — the 193.1 D-26 two assertive arms that CAUSED this bug; `:605-607` (**the one-home-per-concern rule that puts the suppression clause here and NOT there**); `:609-614` (why the absent arm is deliberately stronger and must not be softened)
    - `backend/app/services/harness/publish_service.py:502-542` and `:190-208` — what the gate actually refuses, so the clause can state the CONSEQUENCE truthfully
    - `.planning/seeds/SEED-163-authoring-does-not-propose-the-business-requirement.md` — the *"propose, don't decide"* shape and its three **What NOT to do** rules
    - `.planning/seeds/SEED-164-a-workflow-that-legitimately-pauses-for-a-person.md` — read before writing a single word: it exists because *"the DEFERRED Phase-103 rework"* made unscheduled work read like a plan for a year
  </read_first>
  <action>
Three edits, all inside the one `AUTHORING_SYSTEM_PROMPT` literal. No function signature changes, no
new module, no control-flow change, no schema change.

**(a) Shorten the `llm_human_input` bullet.** It measures 121 characters — the longest of the seven,
45 percent above the verified 83.5 median. Keep it truthful about what the type IS (it pauses and
asks the human; it takes a `prompt` and optional `options`) and **drop the active invitation** — the
*"use for any 'confirm before finalizing' step"* tail is the part doing the nudging. Target the
median band; the `external_action` bullet at 104 characters is the module's own recorded upper
precedent. Attach a comment beside the edit copying the shape of the `:70-81` block: name the phase
(193.2), the decision (`D-11`), the MEASURED before and after character counts, and the rule being
obeyed.

**(b) Add ONE short clause to the DELIVERABLE RULE.** That rule is already labelled *"CRITICAL — a
wrong choice makes the workflow unpublishable"*, so the interactive case joins it in the rule's own
voice. Constraints on the clause, each a requirement rather than a preference:
  - **State the CONSEQUENCE, which is true today** — a workflow containing a step that pauses to ask
    a human cannot be published — rather than an unexplained prohibition. An unexplained prohibition
    in a generator prompt is a rule the model can trade away; a stated consequence is not.
  - **Tell the model what to do INSTEAD**, in the same register as the existing template clause
    (write the answer directly; gather with tools first if it needs to). **It must NOT tell the model
    to invent data.**
  - **D-14** — it may not promise the capability. No *"planned"*, *"coming"*, *"deferred"*,
    *"soon"*, *"future release"*. State what is true now; name nothing that is not on the roadmap.
  - **D-11 proportionality** — the DELIVERABLE RULE block is roughly 769 characters and already about
    eight times the median bullet, so a SHORT clause is proportionate to *this block*; the 83.5
    median belongs to the type-bullet list, not to this block. Say which you measured against, in the
    comment beside the edit.
  - **D-25** — nothing here may imply this phase delivers a deliberate human-pause workflow. It does
    not. It suppresses an unwanted step.

**(c) Extend the closing "Set the definition ..." sentence to name `business_requirement`.** The same
sentence, not a new paragraph — the module already tells the model to set `slug`, `version`, `name`
and `status` there. The added clause carries **D-07's distinction in roughly one clause**: the
business requirement is what the workflow must deliver **for any run** (durable), not what THIS run
was asked for (the describe text is a task instruction). A verbatim copy of the describe text bakes
one run's parameters into the workflow's definition of done, and the publish gauntlet's later stages
— including the judge's `answers_business_requirement` criterion — read this field.

**Do NOT touch `grounding.py`.** The D-26 arms stay exactly as they are. Changing both at once would
leave neither attributable when the fix is re-driven; `grounding.py:605-607` records exactly this
lesson and OQ-4 rests on it.

**Do NOT change `WF_SCHEMA`, `EMIT_TOOL`, the `required` list, or any model.** D-09: no schema change
is needed — the field is already advertised, with no description and no requirement. The prompt is
the whole gap.

**D-02, proved not asserted.** In the summary, state the no-second-concern argument for this file
with its measurement: the prompt literal already carries a DELIVERABLE RULE about publishability and
already names the definition-level fields to set; this adds one clause to that rule and one field to
that sentence. If you find you are adding a genuinely second concern, **STOP** — D-01 is void for
this file and a refactor recommendation is owed FIRST.

Commit `feat(193.2-05): the DELIVERABLE RULE learns what publish refuses, and asks for the durable requirement (D-07/D-11)`.
  </action>
  <acceptance_criteria>
    - `grep -c business_requirement backend/app/services/workflow_authoring.py` returns at least 1; the summary records that it returned **0** at HEAD
    - The `llm_human_input` bullet length, RE-DERIVED from the literal at runtime, is at most 104 and strictly less than 121; both numbers appear in the comment beside the edit
    - `AUTHORING_SYSTEM_PROMPT` matches none of the case-insensitive tokens `planned`, `coming soon`, `deferred`, `future release`
    - The DELIVERABLE RULE block still contains its `CRITICAL` label and its existing template clause byte-unchanged: `git diff` shows an ADDITION to that block, not a rewrite of it
    - The `business_requirement` instruction lives inside the SAME sentence that names `slug`, `version`, `name` and `status` — verified by locating that sentence in the literal and finding the field named within it
    - `git diff --numstat` shows `backend/app/services/harness/grounding.py` UNCHANGED
    - `git diff` shows `WF_SCHEMA`, `EMIT_TOOL` and the schema `required` list unchanged in this file
    - `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit -q -k "authoring or 187"` produces no NEW failures against `193.2-BASELINE.md` §2
    - The summary states the D-02 no-second-concern argument for `backend/app/services/workflow_authoring.py`
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && ./venv/Scripts/python.exe -m pytest tests/unit -q -k "authoring or 187"</automated>
  </verify>
  <done>The prompt no longer nudges toward the type the gate refuses, tells the model the consequence and the alternative, and asks for the durable business requirement — all inside one literal, with grounding.py untouched.</done>
</task>

<task type="auto">
  <name>Task 2: The new deterministic test file — F-8 plus the prompt and schema assertions</name>
  <files>backend/tests/unit/test_workflow_authoring_requirement.py</files>
  <read_first>
    - `backend/tests/unit/test_187_authoring_step_names.py` in full — **the structural analog**, especially `:80-107` (the `_partially_named_definition_dict` mixed-case fixture: one named, one BLANK, one ABSENT), `:116-186` (the five mock helpers `_patch_grounding`, `_patch_provider`, `_patch_user_settings`, `_patch_emit`, `_generate`, and the deliberately-fake `STUB_PROVIDER` so *"this file cannot reach a network"* is self-evident rather than a docstring claim), `:212-235` (**the "prompt asks for X" assertion shape — asserted as a source-level PROPERTY of the constant, never as a hand-typed copy of the prose**, because the module header licenses the copy as Claude's discretion), `:298-358` (the call-budget guard `assert len(calls) == 1`)
    - `backend/app/services/workflow_authoring.py:56-105` (the literal under test), `:148-163` (`WF_SCHEMA`, `EMIT_TOOL`)
    - `backend/tests/unit/test_workflows_updated_at.py:183-201` — the Python positive-control idiom (an absence case that names the positive control directly above it)
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-PATTERNS.md` §"No Analog Found" — **F-8 has NO analog and is written from scratch**; nothing in this repository mechanically guards a prompt-literal length
    - `.planning/phases/193.2-from-authored-to-runnable/193.2-VALIDATION.md` §"The non-determinism rule (D-08)" — the split between (a) deterministic asserts and (b) frequency artifacts
  </read_first>
  <action>
Create `backend/tests/unit/test_workflow_authoring_requirement.py`. Copy the analog's mock helpers
wholesale (they are the reason the file cannot reach a network) and copy its assertion PHILOSOPHY
verbatim: **assert source-level PROPERTIES of the prompt constant, never a hand-typed copy of the
prose** — the module header licenses the wording as Claude's discretion, so a reword must not turn
this file red.

Write these cases:

**1. F-8 — the bullet-length fence, from scratch.** Split `AUTHORING_SYSTEM_PROMPT` on newlines, take
the lines beginning with `"- "` that sit inside the phase-type bullet block, and assert every one is
at most **104** characters (the `external_action` precedent). **Do NOT pin a hand-counted number for
any individual bullet** — derive the set from the literal, exactly as RESEARCH did, or the fence rots
the way `GSD_VITEST_MAX_WORKERS=4` did. Add:
  - **non-vacuity**: assert the collected bullet list has at least 7 entries, so the fence cannot pass
    over an empty selection;
  - **the inline plant**, permanently in the file: assert that a literal 200-character bullet string
    DOES violate the same predicate the fence applies, so the predicate is shown to see the shape;
  - **and drive it RED for real once**: temporarily plant a 200-character bullet into
    `AUTHORING_SYSTEM_PROMPT`, run the file, observe the fence fail, restore with `git checkout --`,
    re-run green, and record both outputs plus an empty `git diff --numstat` in the summary. An
    inline needle assertion proves the predicate sees the shape; only a real plant proves the fence is
    pointed at the right constant.
  - Also assert the `llm_human_input` bullet specifically is **strictly shorter than 121** characters,
    with a docstring recording that 121 was its measured value at HEAD and why length is a
    composition nudge rather than a style note.

**2. F-4's sibling on the prompt (D-14).** Assert `AUTHORING_SYSTEM_PROMPT` matches none of the
case-insensitive tokens `planned`, `coming soon`, `deferred`, `future release`, with a positive
control asserting the same regex DOES match a planted string containing one of them.

**3. The DELIVERABLE RULE mentions the interactive case (D-11), asserted as a property.** Find the
DELIVERABLE RULE block by its `CRITICAL` label, and assert within that block that the phase-type
token `llm_human_input` (or the plain-language phrase the clause uses — pick ONE and state in the
docstring which property is being asserted and why) is present. **Assert a property, not a sentence.**
Add a non-vacuity guard that the located block is non-empty and contains its `CRITICAL` label.

**4. The requirement instruction is scoped to the definition (D-07), asserted as a property.** Copy
the `:212-235` shape exactly: split the prompt into sentences, keep those naming
`business_requirement`, and assert at least one of them also names the definition-level setting
context — i.e. that the model can tell the definition-level requirement from a per-phase prompt. Add
the D-07 half as a property too: assert at least one such sentence carries a durability signal
distinguishing it from the describe text. **State in the docstring which property is asserted and
that the WORDING is Claude's discretion.**

**5. The schema is unchanged (D-09).** Import `WF_SCHEMA` and assert `business_requirement` is still
advertised in `properties` AND is still **not** in `required`; assert
`WF_SCHEMA["required"] == ["slug", "version", "name", "phases"]`. A hard-required field can fail an
otherwise-valid emit and the change would ripple to every other emit path — this case is the fence
against a later "obvious improvement".

**6. The call budget is unchanged.** Copy the analog's guard: a prompt-text change must not buy an
extra provider shot. Drive `_generate` with a single stubbed emit and assert exactly one recorded
call.

**D-08 — the shape of what is NOT here, stated in the module docstring.** Write, in the file's own
header: this file asserts only DETERMINISTIC properties. **No case in this file may assert that
`business_requirement` is always populated, or that `llm_human_input` never appears** — those are
frequency claims, they are measured as k/N by
`backend/tests/integration/test_193_2_authoring_frequency.py` (plan 193.2-08), and the phase's claim
is a **reduction, never an absence**, because the publish gate stays precisely because a prompt
cannot guarantee absence.

⚠ **F-6 and the provenance stamp are NOT in this task.** They arrive in plan 193.2-07, which extends
this same file. Do not stub them here.

Commit `test(193.2-05): F-8 written from scratch plus the deterministic prompt and schema properties (D-08/D-11/D-14)`.
  </action>
  <acceptance_criteria>
    - `backend/tests/unit/test_workflow_authoring_requirement.py` exists and `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_workflow_authoring_requirement.py -q` reports 0 failed
    - The F-8 case derives bullet lengths from `AUTHORING_SYSTEM_PROMPT` at runtime; `grep` finds no hard-coded per-bullet length literal other than the 104 ceiling and the 121 historical value inside a docstring or comparison
    - The F-8 case carries a non-vacuity assertion of at least 7 collected bullets AND an inline 200-character plant assertion
    - The summary records the REAL 200-character plant driven RED: the command, the failing case name, the failure output, the post-restore green run, and an empty `git diff --numstat` for `workflow_authoring.py`
    - A D-14 case asserts the four forbidden tokens are absent, with a positive control asserting the same regex matches a planted string
    - A case asserts `WF_SCHEMA["required"] == ["slug", "version", "name", "phases"]` and that `business_requirement` is present in `properties`
    - A call-budget case asserts exactly one recorded `forced_emit` call
    - The file's module docstring contains the literal token `D-08` and states that no case here asserts always-populated or never-appears
    - `grep -i "always populated"` and `grep -i "never appears"` find no ASSERTION in this file (mentions inside the docstring explaining the prohibition are expected and correct)
    - The file makes no network call: `STUB_PROVIDER`-style fake provider naming is used and `forced_emit` is patched out
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Vibe Apps/Agentic RAG/backend" && ./venv/Scripts/python.exe -m pytest tests/unit/test_workflow_authoring_requirement.py -q</automated>
  </verify>
  <done>The prompt's length discipline is mechanical for the first time, the D-14 and D-09 properties are pinned, and the file states in its own header why it contains no frequency assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| **`describe` text (untrusted user input) to the authoring system prompt** | A hostile describe string is already an instruction-override surface; this plan adds one more instruction it could try to override |
| **LLM emit to `WorkflowDefinition`** | Pydantic with `extra="forbid"` is the gate; unchanged by this plan |
| model-authored `business_requirement` to the judge rubric | After this phase a model authors the criterion a model later grades against |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193.2-03 | **Elevation of privilege (weakened gate)** | `publish_service.py` judge rubric consuming an AI-authored `business_requirement`; `validator_kinds.py` `answers_business_requirement` | **accept — recorded, not implicit (D-09)** | **Format-string injection is NOT possible** (MEASURED: the value is substituted *into* `.format()`, never itself formatted, so braces are inert). The residual is a **weakened gate**: a trivially-satisfiable AI-authored requirement makes the judge stage easier to pass. D-09 accepts this — the author still presses Publish, which `SEED-163` names as the human checkpoint, and D-06's visible mark means they can see and overrule it. **D-07 is part of the mitigation, not decoration:** a durable requirement is a HARDER bar than a one-run instruction. The mechanical control (refuse to stamp provenance on a byte-identical copy of `describe`) lands in plan 193.2-07 |
| T-193.2-03b | Tampering | prompt injection via `describe` or KB content | mitigate | Already controlled and unchanged: the grounded folder/tool/skill sets are computed **server-side** so KB content cannot whitelist itself, and `_check_grounding_fidelity` re-checks membership after the emit. This plan adds no new data path. It adds one more instruction a hostile describe could try to override — which is exactly why the publish gate STAYS (D-11) and why the fix is measured as a frequency reduction (D-08) |
| T-193.2-05-A | Tampering | the temporary 200-character bullet plant | mitigate | Restored with `git checkout --`; acceptance criteria require an empty `git diff --numstat` and a post-restore GREEN run |
| T-193.2-SC | Tampering | npm/pip/cargo installs | accept | No package installed. RESEARCH §"Package Legitimacy Audit" records the phase installs nothing; if any plan proposes a dependency, D-02 is void for that file and the Package Legitimacy Gate runs first |
</threat_model>

<verification>
- `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit/test_workflow_authoring_requirement.py -q` → 0 failed
- `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit -q -k "authoring or 187"` → no new failures vs `193.2-BASELINE.md` §2
- The six workflow backend suites → 0 failed
- `git diff --numstat HEAD~2 -- backend/app/services/harness/grounding.py backend/app/models/harness.py` → EMPTY
</verification>

<success_criteria>
- `business_requirement` appears in `workflow_authoring.py` where it appeared zero times before
- The `llm_human_input` bullet is measurably shorter, within the module's own 104-character precedent
- The DELIVERABLE RULE carries a suppression clause that states a consequence and an alternative, and promises nothing unscheduled
- F-8 exists, derives its numbers, carries an inline plant, and was driven RED for real
- Nothing in this plan asserts a frequency; the file says so in its own header
</success_criteria>

<output>
Create `.planning/phases/193.2-from-authored-to-runnable/193.2-05-SUMMARY.md` when done.
</output>
