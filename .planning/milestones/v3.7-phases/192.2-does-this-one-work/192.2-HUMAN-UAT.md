---
status: partial
phase: 192.2-does-this-one-work
source: [192.2-VERIFICATION.md, 192.2-VALIDATION.md]
started: 2026-08-19
updated: 2026-08-19
rows: 10
passed: 0
issues: 0
pending: 10
skipped: 0
blocked: 0
---

## ⚠ This file is a POINTER, not a copy — the rows live in `192.2-VALIDATION.md`

**It exists so ten owed rows are visible to `/gsd:progress` and `/gsd:audit-uat`, which look for a
`-UAT.md` file. It deliberately does NOT restate the row scripts.**

`192.2-VALIDATION.md` already carries all ten rows in full — each with a concrete *how to drive* and
*fail if* script, authored **before** the phase claimed completion (`authored_at_sha: 23334f0d`) and
amended by `192.2-11` when the fourth arm landed. Copying them here would create a second source of
truth that drifts from the first, which is this project's most repeated documentation failure. **Drive
the rows in `192.2-VALIDATION.md` and record results there; update the counters in both frontmatters.**

## Current Test

[awaiting human testing — 0 of 10 driven]

## Why these are owed rather than skipped

Phase 192.2 verified **6/6 mechanically-checkable truths** (`192.2-VERIFICATION.md`, `human_needed`).
The blocker CR-01 is closed and independently re-verified — the suites were run live (279/279 frontend,
78/78 backend), not taken on report. **What no machine has checked is the phase's actual claim.** LIB-06
says a user *can tell which workflows work*; the goal's second clause says the card *reads quieter*.
Both are statements about a person looking at a screen. No plan can close them, and a plan that added
tests claiming to would substitute an automated proxy for the lived evidence G-4 exists to require.

## Tests

Drive in this order. The first three are the ones most likely to be skipped and most load-bearing.

### 1. U10 — ⚠ THE FOURTH ARM. A workflow somebody else ran must not read *Never run*
expected: on the Starters shelf, the row somebody has run reads *Run by someone else*, never *Never run*, and is not painted as a success. This is the defect the whole gap round existed to end — measured at **1 of 3 starter rows and 5 of 92 published rows** before the fix.
result: [pending]

### 2. U4 — ⚠ THE UNKNOWN ARM. Frontend against a backend that does not carry the new keys
expected: every row reads *Not recorded* — never *Never run*, never green. ⚠ **DELETE the keys from the payload; do not null them** — `null` is a different arm, and conflating them is the bug this row guards. ⚠ Its override-key list was **wrong until `192.2-11`**: it named two keys where the payload now carries three, and because rule 1 short-circuits before the bit is read, **it would have passed while blind to the omission.**
result: [pending]

### 3. U5 — The resting card reads QUIETER than it did this morning
expected: the felt "quieter, not louder" claim. Verification bounded this mechanically — nothing round 1 added paints at rest, and `WorkflowCard.baseline.test.tsx` is byte-unchanged across all eleven plans — but **mechanical bounding is not the same as a person finding it calmer.** This is a judgement only you can make.
result: [pending]

### 4. U1 — Pick out the one that last worked, from a shelf of rows sharing a name
expected: the phase's own stated thesis — *of these 43 things called Compliance Gap Report, which one is worth running?* ⚠ Judge by **reading the screen, never by `getElementById`** (D-27).
result: [pending]

### 5. U2 — Find one that FAILED, and say so without hovering or clicking
result: [pending]

### 6. U3 — A never-run draft reads as never-run and is NOT mistaken for broken
result: [pending]

### 7. U6 — Colour-blind safety: every state legible with colour ignored
expected: relevant because WR-01 shipped a fix making the `stopped` gutter paint at all — before it, `bg-warning` compiled to nothing and that arm was indistinguishable from the deliberately-unpainted one.
result: [pending]

### 8. U9 — A run happening RIGHT NOW reads *Not recorded*, and that is deliberate
result: [pending]

### 9. U7 — ⚠ CARRIED FINDING: the state word renders TWICE on a name-colliding row
expected: `BUG-260819-01`, `status: open`. Not fixed this phase — the fix is one decision in `rowIdentity.ts`, not in the card. If this row fails it should fold into the next library phase.
result: [pending]

### 10. U8 — ⚠ CARRIED TRADE-OFF: a search hit is no longer self-explaining
expected: WR-04 added a match-reason line for facts the card cannot show. ⚠ The round-1 review found the original recording **understated** it — `makes-a-file` and `strict` chips select on the deliverable and tier atoms D-03 cut, not only `purpose`. Judge whether the explanation line actually resolves it.
result: [pending]

## Summary

total: 10
passed: 0
issues: 0
pending: 10
skipped: 0
blocked: 0

## Gaps

<!-- Populated when rows are driven. A row that cannot be run is recorded with its reason and a
     blocking id — never dropped. A scoreboard that lists only what passed is not a scoreboard. -->
