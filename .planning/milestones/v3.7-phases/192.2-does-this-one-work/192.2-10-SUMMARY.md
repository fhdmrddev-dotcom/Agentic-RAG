---
phase: 192.2-does-this-one-work
plan: 10
gap_closure: true
gap_closure_round: 1
wave: 2
subsystem: ui / library wire→row→word seam
tags: [LIB-06, CR-01, WR-02, typescript, wire-contract, vocabulary, fences]

# Dependency graph
requires:
  - "192.2-08 — has_any_run: bool | None on PublishedWorkflow + DraftRow, all four library projections"
  - "192.2-09 — libraryFilter.ts's matchReasons + libraryVocabulary.ts's MATCH_REASON_* (both modified under this plan)"
  - "192.2-04 — the wire→row normalizer pattern in libraryFilter.ts this plan extends"
provides:
  - "frontend/src/lib/api.ts::has_any_run?: boolean | null on PublishedWorkflow and WorkflowDraftRow"
  - "frontend/src/components/workflows/library/libraryRow.ts::LibraryRow.hasAnyRun — three-valued"
  - "libraryFilter.ts — both normalizers carry the bit VERBATIM (no ??, no Boolean, no default)"
  - "libraryVocabulary.ts::RUN_NOT_BY_YOU = 'Run by someone else' — the fourth arm's word, one home"
  - "frontend/src/lib/apiRunFields.fences.test.ts — the WR-02 lookalike-field fence"
affects:
  - "192.2-11, which adds the RunFact union member, wires runFacts.ts and lands the two colour maps"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "CORRECT-BESIDE-NEVER-OVER: a falsified docblock sentence is preserved verbatim and superseded in place"
    - "PRECONDITION-NOT-REWORDING: a word made true again by narrowing the arm that fires it, leaving the literal untouched"
    - "MARKER-BOUND CROSS-REFERENCE: N lookalike declarations each carry one fixed token, counted line-anchored so the prose that documents the rule cannot satisfy the fence"

key-files:
  created:
    - frontend/src/lib/apiRunFields.fences.test.ts
  modified:
    - frontend/src/lib/api.ts
    - frontend/src/components/workflows/library/libraryRow.ts
    - frontend/src/components/workflows/library/libraryFilter.ts
    - frontend/src/components/workflows/library/libraryVocabulary.ts
    - frontend/src/components/workflows/library/libraryFilter.test.ts
    - frontend/src/components/workflows/library/runFacts.test.ts
    - frontend/src/components/workflows/library/cardFace.test.ts
    - frontend/src/components/workflows/library/rowIdentity.test.ts
    - frontend/src/components/workflows/library/WorkflowCard.test.tsx
    - frontend/src/components/workflows/library/__fixtures__/libraryScale.ts
    - scripts/vitest-count-gate.cjs

key-decisions:
  - "DEC-10-A honoured verbatim: hasAnyRun passes through with all three states; no coalesce exists in code, proved over comment-stripped source with a positive control"
  - "DEC-10-B honoured and PROVED rather than asserted: api.ts's change is type-and-comment only; Phase 197's seam-decline re-open trigger did NOT fire"
  - "DEC-10-C honoured: RUN_NOT_BY_YOU is authored in libraryVocabulary.ts and appears in exactly one other place — its own test"
  - "[plan latitude] the new word is documented as the FOURTH arm, not 'arm 3 as amended' — RUN_UNKNOWN already ships as D-08 arm 3 and renumbering a shipped vocabulary would have been the larger change"
  - "[Rule 2] the fence was wired into the count gate (TARGETS + BASELINE) in its creating commit — src/lib has no directory entry, so it would never have executed"

requirements-completed: [LIB-06]

# Metrics
duration: ~70 min
completed: 2026-08-19
tasks: 3
commits: 3
files: 12
---

# Phase 192.2 Plan 10: The Wire→Row→Word Seam, and WR-02 — Summary

**`has_any_run` now reaches `LibraryRow.hasAnyRun` with all three states distinguishable, the library
owns the word for *somebody ran this and it was not you*, the three docblocks that asserted a
caller-scoped `null` as a row-level fact stop doing so — and `api.ts`'s three identically-typed
`last_run_status` fields now name each other, guarded by a fence whose controls were driven RED
against real plants.**

## Performance

- **Duration:** ~70 min
- **Tasks:** 3 of 3
- **Commits:** 3 task commits + this metadata commit
- **Files:** 11 modified, 1 created

| # | Commit | What |
|---|---|---|
| 1 | `ff351da1` | the row-level run bit reaches `LibraryRow`, three-valued (CR-01) |
| 2 | `75952617` | the three lookalike `last_run_status` fields learn about each other (WR-02) |
| 3 | `d568f434` | `RUN_NOT_BY_YOU` — the word for the fourth arm (CR-01) |

## 1. What shipped

### Task 1 — the bit crosses the wire and lands on the row (`ff351da1`)

| Artifact | What it is |
|---|---|
| `api.ts` — `PublishedWorkflow.has_any_run` | `has_any_run?: boolean | null`, with the three states, the meaningful `true`-with-`last_run_at: null` pair, and the existence-only disclosure budget written on the member itself |
| `api.ts` — `WorkflowDraftRow.has_any_run` | the mirror, stating **why** it is mirrored: `/drafts` is `created_by = $1`, so the two facts agree *on this feed* — a property of the feed, never an invariant of the pair |
| `api.ts:last_run_at` docblock | the CR-01 sentence **corrected beside its original, never over it** |
| `libraryRow.ts` — `hasAnyRun` | `boolean | null | undefined`, the same three states its two neighbours carry, plus the amended `lastRunAt` docblock |
| `libraryFilter.ts` | `hasAnyRun: row.has_any_run` in **both** normalizers, bare |
| `libraryFilter.test.ts` | **+12 cases** (64 → 84 → **96**) |

The correction reads, in `api.ts`:

> ⚠ CORRECTED BY 192.2-10 (CR-01) — THE PARAGRAPH ABOVE IS PRESERVED VERBATIM AND IS SUPERSEDED.
> Its sentence *"`null` means the backend looked and there is NO RUN"* is FALSE AS A ROW-LEVEL
> CLAIM. […] So `null` means *the backend looked and **YOU** have no run of this row*.

Both texts are greppable, which is the acceptance criterion: the original sentence
`` `null` means the backend looked and there is NO RUN `` is still present (quoted inside the
correction), and the corrected claim sits beside it.

### Task 2 — WR-02 (`75952617`)

Each of the **three** `last_run_status?: string | null` declarations gained a cross-reference block
naming the other two **by TYPE NAME** — never by line number, which rots on the next edit to a
6,357-line file — and stating three things: that a swap between any two TYPECHECKS; which table and
scope each one describes; and that **`last_run_created_at` and `last_run_at` are the same column**
(`workflow_runs.created_at`) under two wire names, DECLINED for rename as DEC-10-B with the reason
written where the next reader is, not only in the plan.

`WorkflowDraftRow.last_run_status` had **no docblock of its own** before this plan — it shared the
one above `last_run_at`. It has one now.

`frontend/src/lib/apiRunFields.fences.test.ts` — **12 cases**, six of them synthetic positive
controls. It pins `3` declarations and `3` markers as **numbers**, asserts each marked block names
all three types and states the one-column-two-names fact, and asserts non-vacuity of the `?raw`
import **first**.

### Task 3 — the word (`d568f434`)

```ts
export const RUN_NOT_BY_YOU = "Run by someone else"
```

Authored in `libraryVocabulary.ts` and nowhere else. `RUN_NEVER`'s docblock is amended beside its
original; **its literal `"Never run"` is byte-unchanged**, because the word was always right — it was
the arm's precondition that was missing:

> ⚠ WHAT MAKES IT TRUE AGAIN IS A PRECONDITION, NOT A REWORDING. As of `192.2-08` the wire carries
> `has_any_run`, and this arm fires ONLY when the wire AFFIRMS `has_any_run === false`.

**+5 cases** in `runFacts.test.ts`, including the exact-match literal, the six-distinct-and-none-
contains-another property, and a case recording that `runFacts.ts` **does not yet consume the word**
— stated as a property rather than left as an absence a reader must infer, because the union member
is what makes `runGutterOf` non-total and that is `192.2-11`'s atomic change.

## 2. The constraints this plan was given, answered on the record

### 2a. ⚠ The `api.ts` seam decline — PROVED THREE WAYS, not by the grep alone

The plan's criterion is `git diff -- frontend/src/lib/api.ts | grep -c '^+export '` → **0**. It
returns **0**. The brief correctly warns that this grep is *necessary but not sufficient* — it would
miss a runtime value added to an existing `export` statement, or an `export {}` block edit. So the
diff was read, and here is what was actually verified:

**(i) Every non-comment added line, enumerated.** Filtering the diff's `+` lines to those that are
not a `*` / `/**` / `//` line leaves exactly this, and nothing else:

```
      2 has_any_run?: boolean | null
```

Two interface member declarations. That is the complete non-comment content of a `+131 / −1` diff.

**(ii) Every removed line, enumerated.** The whole diff removes exactly ONE line:

```
-   *  disclosure. Timestamps are ISO strings on the wire. */
```

— a docblock terminator, re-emitted several lines lower when the block was extended. No declaration,
no export, no statement was deleted or replaced.

**(iii) A token scan over the added lines, INCLUDING inside comments.** Grepping every added line for
`export` / `function` / `const` / `let` / `var` / `class` / `=>` returns **nothing at all** — so the
change cannot contain a runtime export smuggled into an existing statement, a re-export block, or an
arrow function, and there is no false negative available to an anchored grep.

**Verdict: the change is TYPE-AND-COMMENT ONLY. Phase 197's recorded re-open trigger — *the next
phase adding a RUNTIME export or a second concern* — did NOT fire, and the decline HOLDS.** Stated
explicitly rather than left as a silent cell.

### 2b. The disclosure budget is preserved — no who / when / count / outcome anywhere

`LibraryRow` gained exactly one member and it is a `boolean | null | undefined`. There is no field,
no type and no word in this plan's diff naming a user, an id, an org, a timestamp, a count or an
outcome. The budget is written into **three** places a future editor will actually be standing in —
`PublishedWorkflow.has_any_run`'s docblock, `LibraryRow.hasAnyRun`'s docblock, and
`RUN_NOT_BY_YOU`'s docblock — each carrying `192.2-08` DEC-08-A's re-open trigger. It is also
**asserted**, not only documented: `runFacts.test.ts` pins that the word contains no digit and none
of `@` / `ago` / `times`, with a non-vacuity probe proving those tokens are matchable at all.

### 2c. `WorkflowCard.baseline.test.tsx` — BYTE-UNCHANGED

`git status --short` on it is empty after all three commits, and it does not appear in
`git diff --numstat` against the base. It ran green (29/29) inside every subtree run. No re-baseline
was needed, considered or performed.

### 2d. No file under `backend/` or `supabase/`

```
$ git diff --name-only <base> HEAD -- backend/ supabase/ | wc -l
0
```

And no commit in this plan deleted a tracked file (`--diff-filter=D` → 0 across all three).

### 2e. ⚠ The acceptance criterion's own grep is UNSATISFIABLE — recorded, not side-stepped

The plan requires:

> `grep -n "?? false\|Boolean(row.has_any_run)\|has_any_run ??" …/libraryFilter.ts` returns
> **nothing** (exit 1). Paste the exit code.

**It returns exit 0 with one hit, and that hit is my own comment forbidding the pattern:**

```
128:    // written a third time. ⚠ NO `??`, NO `Boolean(...)`, NO DEFAULT: a `?? false` here would
```

This is the 195-07 trap verbatim — *a criterion phrased as "this string is absent" is UNSATISFIABLE
the moment anything documents it* — and its sibling, the 187-24 trap. **The comment is load-bearing
and was NOT deleted to satisfy a grep.** The check that actually discriminates strips comments first:

```
CODE-ONLY hits: 0
has_any_run code lines: [(62, 'hasAnyRun: row.has_any_run,'), (95, 'hasAnyRun: row.has_any_run,')]
```

Exactly two assignments, both bare. That same stripped-source detector is now a **test case**, with a
synthetic control planting a `?? false` beside a comment-only mention, so the discrimination is
guarded rather than performed once by hand.

### 2f. Every fence was driven RED against a REAL plant, with an md5-verified restore

| Plant | In | Result | Restore |
|---|---|---|---|
| `hasAnyRun: row.has_any_run ?? false` at both normalizer sites | `libraryFilter.ts` | **6 failed / 90 passed** — including the source fence and the four-distinct-readings case | md5 `14a426e3…` identical, 96 passed |
| a FOURTH `last_run_status?: string \| null` on a new interface, no marker | `api.ts` | **3 failed / 9 passed** (`expected […(4)] to have a length of 3`) | md5 `7abf0e04…` identical |
| ONE real marker deleted from a shipped docblock | `api.ts` | **4 failed / 8 passed** | md5 `7abf0e04…` identical |
| `const RESPELLED = "Run by someone else"` | `runFacts.ts` | **1 failed / 56 passed** — the widened T-06 sweep | md5 `8608bc8e…` identical |

⚠ Note which cases did **not** fire on plant 1: `true` and `false` both survive a `?? false`
unchanged. That is exactly why the plan demanded `toBe` over truthiness — the two states a coalesce
destroys are `null` and `undefined`, and only assertions that distinguish them can see the damage.

### 2g. The self-satisfying-grep trap, MEASURED rather than warned about

The three new cross-reference docblocks discuss `last_run_status` by name. Measured on the shipped
file:

| detector | reads |
|---|---|
| naive whole-file count of the identifier | **18** |
| line-anchored declaration count | **3** |

A `grep -c` fence would have been counting the prose that documents the rule and would have satisfied
itself. That contrast is now a test case, so the gap stays visible instead of being re-discovered.

## 3. Verification

`GSD_VITEST_MAX_WORKERS=2` on every vitest run; **the cap was never adjusted**, and no run reddened
for any reason other than a deliberate plant.

**Count gate — from the REPO ROOT** (`GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs`),
verdict line verbatim, exit 0:

```
  apiRunFields.fences.test.ts                  12      12       0
  -------------------------------------------------------------
  total                                      4340    4675    +335
  total 4675  ·  failed 0  ·  pinned total 4340
count gate OK — 93/93 pinned files present, no per-file decrease, 0 failing.
```

| | at base | at this tip | delta |
|---|---|---|---|
| total | 4646 | **4675** | **+29** |
| pinned total | 4328 | **4340** | +12 |
| pinned files | 92/92 | **93/93** | +1 |
| failed | 0 | **0** | — |

**The `+29` is fully accounted for with no residual:** `libraryFilter.test.ts` +12,
`apiRunFields.fences.test.ts` +12 (new), `runFacts.test.ts` +5.

**Typecheck** (`npx tsc --noEmit -p tsconfig.app.json`, from `frontend/`): **33 errors**, matching
base exactly, measured after **every** task. `grep -c "library/\|WorkflowsPage\|apiRunFields"` → **0**.

**Suites:**

| Suite | base | after |
|---|---|---|
| `libraryFilter.test.ts` | 84 | **96** |
| `runFacts.test.ts` | 52 | **57** |
| `cardFace.test.ts` | 33 | **33** (sweep widened, count unchanged) |
| `WorkflowCard.baseline.test.tsx` | 29 | **29** (unedited, byte-unchanged) |
| library subtree (12 files) | 633 | **650**, 0 failed |
| `apiRunFields.fences.test.ts` | — | **12**, 0 failed |

**Single spelling of the new word**, `grep -rn "Run by someone else" frontend/src`, pasted verbatim:

```
frontend/src/components/workflows/library/libraryVocabulary.ts:496:export const RUN_NOT_BY_YOU = "Run by someone else"
frontend/src/components/workflows/library/runFacts.test.ts:421:  it("is EXACTLY `Run by someone else` — asserted by exact match, never by fragment", () => {
frontend/src/components/workflows/library/runFacts.test.ts:425:    expect(RUN_NOT_BY_YOU).toBe("Run by someone else")
```

The vocabulary and its test. **No second spelling anywhere.**

## 4. Deviations from Plan

### 1. [Rule 2 — missing critical functionality] The fence was wired into the count gate

- **Found during:** Task 2, before committing the new suite.
- **Issue:** `frontend/src/lib/apiRunFields.fences.test.ts` lives in `src/lib/`, which has **no
  bare-directory entry** in `TARGETS` — the gate reaches that directory through exactly two
  file-level lines (`src/lib/phaseState.test.ts`, `src/lib/__tests__/fileIcon.test.tsx`). Without
  its own entry the suite **would never have executed under the gate**, and a falsification that does
  not run has falsified nothing. This is the two-knob trap the script itself documents ten times.
- **Fix:** a `TARGETS` entry **and** a `BASELINE` pin at **12**, both in the same commit that creates
  the file, with the reason recorded once (at the pin) and pointed at from the other. `node --check`
  clean. ⚠ **`+42 / −0`** — *adding* a pin is not *raising* one, so the diff removes nothing.
- **Files modified:** `scripts/vitest-count-gate.cjs` (not in `files_modified`)
- **Commit:** `75952617`
- **Consequence for the ledger:** `192.2` was **absent from that file's phase-bucket list at base**
  and this commit adds it — see §5.

### 2. [Rule 3 — blocking] Three `LibraryRow` construction sites the plan did not name

- **Found during:** Task 1, on the first typecheck after adding `hasAnyRun`.
- **Issue:** `hasAnyRun` is REQUIRED (`boolean | null | undefined`), matching its two neighbours, so
  `tsc` enumerated every literal construction site: `__fixtures__/libraryScale.ts:756`,
  `rowIdentity.test.ts:61`, `WorkflowCard.test.tsx:148`. None is in `files_modified`. That
  requiredness is deliberate and is the point — `WorkflowCard.test.tsx`'s own comment already argues
  it ("`tsc` enumerates every construction site rather than letting one silently default").
- **Fix:** each set to `undefined`, **the *wire did not say* arm** — matching the shipped precedent
  beside it, and each with a comment stating why `false` would be wrong: `false` is the affirmative
  claim *nobody has ever run this*, a fact those corpora do not hold. (The fixture's own docblock
  already records the same reasoning for `lastRunAt`, having declined `null` there for 107 rows.)
- **Files modified:** the three above
- **Commit:** `ff351da1`

### 3. [Rule 3 — blocking] Task 3's acceptance needs test files Task 3 does not list

- **Issue:** Task 3's `<files>` is `libraryVocabulary.ts` alone, but its acceptance requires an
  EXACT-match assertion on the new literal, a `RUN_NEVER`-unchanged assertion, and a grep proving no
  second spelling — none of which can exist without a test file. There is **no** `libraryVocabulary.test.ts`
  in this repo; the D-08 arm words are tested in `runFacts.test.ts` and swept in `cardFace.test.ts`.
- **Fix:** the five new cases went to `runFacts.test.ts` (which already imports all five run words
  and is the module `192.2-11` will wire), and both files' T-06 no-re-spelling sweeps were widened
  from five words to six **in the commit that births the word** — the only moment at which the
  wiring plan could re-spell it undetected.
- **Files modified:** `runFacts.test.ts`, `cardFace.test.ts`
- **Commit:** `d568f434`

### 4. [Plan latitude] The word is documented as the FOURTH arm, not "arm 3 as amended"

- **Issue:** Task 3's `<action>` says to document the new word as *"D-08 arm 3 as amended by CR-01"*.
  But `RUN_UNKNOWN` **already ships as D-08 arm 3** (`libraryVocabulary.ts`, and `runFacts.ts`'s
  three-armed union), and `RUN_NEVER` as arm 2. Taking the plan's phrasing literally would have
  renumbered a shipped vocabulary in a gap-closure round, or produced two constants both calling
  themselves arm 3.
- **Fix:** the docblock reads *"THE FOURTH ARM — added by CR-01 beside D-08's original three;
  `RUN_UNKNOWN` below keeps its own name as arm 3"*. This matches the plan's own `<objective>`, which
  calls it *"the fourth arm"*, and matches `192.2-CONTEXT.md`'s locked decision
  (`ran` / `never` / **`not-by-you` (NEW)** / `unknown`).

### 5. [Plan latitude] `libraryFilter.test.ts`'s coalesce fence reads STRIPPED source

- The plan's acceptance asks for a raw grep returning nothing. As §2e records, that criterion is
  unsatisfiable against a module whose comment forbids the pattern by name. The property is asserted
  over comment-stripped code instead, with a non-vacuity check (**exactly 2 assignments must survive
  the stripper**) placed *before* the four negatives, and a synthetic positive control. The raw-grep
  result and its cause are recorded above rather than the comment being quietly removed.

### 6. [Rule 1 - bug] STATE.md's plan counter was THREE BEHIND, not one

- **Found during:** the state update, while bumping `completed_plans`.
- **Issue:** the frontmatter read `completed_plans: 110`, the value written at `ca2fd53b` when
  `192.2-06` closed the phase's original six plans. **Wave 1 (`07` + `08` + `09`) merged without
  bumping it** — traced through every STATE.md commit since (`bf637b62` 109 → `ca2fd53b` 110 →
  `bba4ce9a` 110 → `d622165b` 110), while ROADMAP.md correctly advanced to `9 / 11`. The two
  artifacts disagreed by three.
- **Fix:** set to **114** = 110 + 3 (wave 1) + 1 (this plan), so the bump repairs the drift rather
  than carrying it forward. ROADMAP's `9 / 11 → 10 / 11` needed no repair.
- **Also recorded:** `gsd-sdk query roadmap.update-plan-progress 192.2` returned
  `{ "updated": true, "plan_count": 11, "summary_count": 10 }` and wrote **ZERO bytes**
  (`git diff --numstat` on ROADMAP.md empty immediately after). Its READ was correct; its WRITE did
  not happen. Both files were hand-edited, per CLAUDE.md's standing rule for STATE.md.

### `files_modified` accuracy — the rate is still the finding

⚠ The brief warns that a plan's `files_modified` has been wrong in **seven of this phase's eight**
plans. **This plan's six listed paths were all correct** — every one exists and contains what the
plan claimed, and the plan's quoted line numbers were accurate (`api.ts:1414`'s sentence, the
`libraryRow.ts` three-state docblock, both normalizers' *"the absent `?? undefined` is the whole
point"* comment, `libraryVocabulary.ts`'s `RUN_NEVER` block). **But it was INCOMPLETE by five files**:
three `tsc`-forced construction sites, two test files Task 3's own acceptance requires, and the count
gate. **Eight of nine plans, counting incompleteness as wrongness.**

Two specific traps in the brief were checked and both held: the normalizers really are in
`libraryFilter.ts` and not `libraryRow.ts`, and `192.2-09`'s wave-1 changes to `libraryFilter.ts` /
`libraryVocabulary.ts` were read as they now are — **`matchReasons`, the `MATCH_REASON_*` words and
the re-pointed scope anchor at `libraryFilter.test.ts:934` are all untouched by this plan** (my
additions are appended after the file's last describe; `git diff` shows `+125 / −0`, i.e. **not one
existing line of that file was modified**).

## 5. Hot-file ledger — all five triples RE-DERIVED, not inherited

Per DEC-10-B's *"re-derive and record all four"*, plus the count gate as a fifth (Deviation 1).
CLAUDE.md's recipe, six-digit dated-quick-task buckets excluded.

| File | CLAUDE.md row reads | measured **at base** | measured **at this tip** | this plan |
|---|---|---|---|---|
| `frontend/src/lib/api.ts` | `171 / 98 / 6174` | `172 / 99 / 6227` | **`174 / 99 / 6357`** | `+131 / −1`, type-and-comment only |
| `library/libraryVocabulary.ts` | `9 / 4 / 677` (added 192.2-06) | `9 / 4 / 677` | **`10 / 4 / 727`** | `+50 / −0` — one word, one amendment |
| `library/libraryFilter.ts` | `5 / 4 / 424` | `5 / 4 / 424` | **`6 / 4 / 435`** | `+11 / −0` — one line per normalizer |
| `library/libraryRow.ts` | `3 / 3 / 162` | `3 / 3 / 162` | **`4 / 3 / 201`** | `+39 / −0` — one field |
| `scripts/vitest-count-gate.cjs` | `101 / 17 / 3259` | `101 / 17 / 3259` | **`102 / 18 / 3301`** | `+42 / −0` — one entry, one pin |

**G-5 verdicts, each stated rather than assumed:**

- **`api.ts` FIRES HARDEST** (`174 / 99`) — the hottest file in the repository. **The seam stays
  DECLINED**, on the recorded 197 basis, and §2a proves the change is type-and-comment only three
  independent ways. The re-open trigger did **not** fire.
- **`libraryVocabulary.ts` / `libraryFilter.ts` / `libraryRow.ts`** all fire. Each gained **its own
  concern** — a word, one normalizer line, one field — so all three are **honoured by construction**.
  No phase count moved (all three already carried `192.2`), so **no new G-5 obligation arises**.
- ⚠ **`scripts/vitest-count-gate.cjs` moved from 17 phases to 18 — `192.2` was NOT in its bucket list
  at base.** Its row already reads ⚠ FIRES, and the verdict does not change; the file gained one
  `TARGETS` entry and one `BASELINE` pin and no second concern, so it too is honoured by
  construction. Its CLAUDE.md row (`100 / 16 / 3215`) was already stale at base by one commit / one
  phase / 44 lines, and is now stale by two / two / 86.

⚠ **The ledger rows themselves are deliberately NOT edited in this commit.** `CLAUDE.md` and
`docs/HOT-FILE-LEDGER.md` are shared artifacts and this is a multi-wave gap-closure round with
`192.2-11` still to land on `libraryVocabulary.ts`'s neighbours; editing them now would go stale
inside the hour and would conflict with a sibling. This follows `192.2-09`'s and `197-10`'s recorded
precedent of **flagging rather than editing a shared artifact mid-round**. **Owed to the
orchestrator at the round's close:** the five tip triples above, joining the seven cells `192.2-09`
already recorded as owed.

## 6. Threat register disposition

| Threat | Disposition | Evidence |
|---|---|---|
| **T-192.2-43** — a false claim manufactured by the normalizers | **MITIGATED** | Verbatim pass-through; 8 `toBe` state assertions (4 × 2 normalizers) plus a four-distinct-readings set case; a comment-stripped source fence with a synthetic control; and a real `?? false` plant driven RED (6 failures) with an md5 restore. |
| **T-192.2-44** — the word grows toward who / when / outcome | **MITIGATED** | The budget is written on `RUN_NOT_BY_YOU`'s docblock *and* on both wire members *and* on `LibraryRow.hasAnyRun`, each with `192.2-08`'s re-open trigger — and it is **asserted**: no digit, and none of `@` / `ago` / `times`, with a non-vacuity probe. |
| **T-192.2-45** — a runtime export on the hottest file in the repo | **MITIGATED, PROVED THREE WAYS** | §2a: the complete non-comment added content is two field declarations; the only removed line is a docblock terminator; a token scan over every added line (comments included) for `export`/`function`/`const`/`let`/`var`/`class`/`=>` returns nothing. |
| **T-192.2-46** — a self-satisfying count fence | **MITIGATED** | Line-anchored declaration matching (naive **18** vs true **3**, measured and pinned as a case), plus six synthetic controls, plus two real plants in `api.ts` driven RED (a fourth declaration; a deleted marker). |
| **T-192.2-SC** — package installs | **N/A** | No package installed. `package.json` untouched. |

## Threat Flags

None. No new network endpoint, no auth path, no file access, no schema change. The one new field on
the client is a boolean already on the wire, and its trust boundary — the normalizers — is the one
this plan fences.

## Known Stubs

**One, and it is the plan's own design rather than an omission.** `LibraryRow.hasAnyRun` is populated
by both normalizers but is **not yet read by any renderer**: `runFacts.ts` still resolves three arms
and the card still prints `RUN_NEVER` on a CR-01 row. `RUN_NOT_BY_YOU` is exported and unconsumed.

This is deliberate and is stated in the plan's `<objective>`: adding the fourth member to `RunFact`
makes `runGutterOf` in `WorkflowCard.tsx` **non-total, which is a typecheck error by design**.
Splitting the field and the word out keeps every commit here green and lets **`192.2-11`** land the
union member and its two colour maps in one atomic change. A test case records the property
(`runFacts.ts` contains neither `RUN_NOT_BY_YOU` nor `not-by-you`) so the gap is guarded, not merely
described. **CR-01 is not closed on screen until `192.2-11` ships.**

## 7. What the next plan inherits

1. **`LibraryRow.hasAnyRun` is `boolean | null | undefined` and all three states arrive intact.** A
   `?? false` in `runFacts.ts` re-creates CR-01 one layer further down; the four-state assertions
   here will not catch that, because they stop at the normalizer.
2. **The arm's precondition is now written into `RUN_NEVER`'s docblock as a contract:** `never` fires
   **only** when `hasAnyRun === false`. An `undefined` must NOT reach `never` — it belongs to
   `unknown`, and `true` with no caller-scoped run belongs to `RUN_NOT_BY_YOU`.
3. **The word is `Run by someone else`, imported — never re-spelled.** Both T-06 sweeps are already
   widened to six words, so a literal typed into `runFacts.ts` or `cardFace.ts` reds immediately.
4. **`WorkflowCard.baseline.test.tsx` will legitimately need attention in `192.2-11`, not here** —
   it is byte-unchanged by this plan and its 29 cases pass unedited.

## Self-Check

Files claimed created — verified present:

- `frontend/src/lib/apiRunFields.fences.test.ts` — FOUND, 12 passing cases, pinned in the gate ✔
- `.planning/phases/192.2-does-this-one-work/192.2-10-SUMMARY.md` — FOUND ✔

Files claimed modified — all eleven verified present and containing the claimed artifacts:

- `frontend/src/lib/api.ts` — `has_any_run` ×2, marker ×3 ✔
- `library/libraryRow.ts` — `hasAnyRun` ✔
- `library/libraryFilter.ts` — `hasAnyRun: row.has_any_run` ×2, zero coalesce in code ✔
- `library/libraryVocabulary.ts` — `RUN_NOT_BY_YOU`, `RUN_NEVER` still `"Never run"` ✔
- `library/libraryFilter.test.ts` · `runFacts.test.ts` · `cardFace.test.ts` · `rowIdentity.test.ts` ·
  `WorkflowCard.test.tsx` · `__fixtures__/libraryScale.ts` ✔
- `scripts/vitest-count-gate.cjs` — `node --check` clean, gate exit 0 at 93/93 ✔

Commits verified present in `git log`:

- `ff351da1` feat(192.2-10): the row-level run bit reaches LibraryRow, three-valued (CR-01)
- `75952617` feat(192.2-10): the three lookalike last_run_status fields learn about each other (WR-02)
- `d568f434` feat(192.2-10): RUN_NOT_BY_YOU — the word for the fourth arm (CR-01)

No commit in this plan deleted a tracked file (`git diff --diff-filter=D` empty across all three).

## Self-Check: PASSED
