---
phase: 192.2-does-this-one-work
verified: 2026-08-19T09:00:00Z
status: human_needed
score: 6/6 truths mechanically verified — 10 lived-experience rows still owed to a human
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 4/6
  gaps_closed:
    - "Truth 2 (CR-01) — 'never run' must be a TRUE fact about the ROW, not merely about the caller. Closed by 192.2-08 (has_any_run EXISTS bit, all four projection sites) + 192.2-10/192.2-11 (fourth RunFact arm `not-by-you`, RUN_NEVER re-preconditioned on an AFFIRMED has_any_run === false). Re-verified independently below — mechanically closed, not relocated."
  gaps_remaining:
    - "The lived-experience UAT rows (192.2-VALIDATION.md) are STILL undriven. This is unchanged from round 0 in kind, but the row count grew 9 → 10 (U10 added for the fourth arm) and U4's override script was corrected mid-round (it would have passed blind against a 3-key payload). 0/10 driven, driven: false, verified by grep this round."
  regressions: []
human_verification:
  - test: "U1 — Pick out the one that last worked, from the 43-member 'Compliance Gap Report' family, by reading the screen only"
    expected: "The operator can name the row that most recently ran successfully without sorting, hovering, or opening the version number to break ties"
    why_human: "Salience/triage is a felt property of the design; unit tests prove the resolver, not the eye"
  - test: "U2 — Find a failed run without hovering or clicking, scanning once"
    expected: "A failed row's outcome is noticed peripherally via the gutter + line 2, not hunted for"
    why_human: "192.1 U7 already proved geometry can pass while 'I'd miss it' fails on this exact card family"
  - test: "U3 — A never-run draft reads as neutral, not as an accusation, distinct from a failed card read aloud"
    expected: "The two do not feel like the same negative signal"
    why_human: "Tone is not machine-checkable"
  - test: "U4 — Override the /workflows/published response so it carries no `has_any_run`/`last_run_at`/`last_run_status` keys at all, reload, and read every card"
    expected: "Every card renders 'Not recorded', no gutter colour, no tick, no fabricated time, and critically never the words 'Never run' or 'Run by someone else'"
    why_human: "Proves the deployed frontend/backend pairing, not just the unit-tested resolver in isolation. NOTE: round 1 corrected this row's override key list — it now names three keys, not two, because the payload grew a third key (`has_any_run`) this round; the row would have passed blind under the old two-key script"
  - test: "U5 — Look at the real, full-scale library for 30 seconds and compare against the pre-phase card (sketch 179 variant A or `git show 82dd2efd`)"
    expected: "The shelf reads quieter, not emptier; no specific cut atom is missed, and the fourth run-arm / RUN_TONE sixth tone / matchReasons addition made this round do not make the resting card feel busier"
    why_human: "'Quieter, not louder' is the second half of the phase goal and is explicitly a felt claim, not a mechanical one. Mechanically checked this round (see 'Quieter, not louder — mechanical re-check' below) but the felt claim is still owed"
  - test: "U6 — Emulate protanopia/deuteranopia/achromatopsia in DevTools and repeat U1/U2, now including the sixth `not-by-you` gutter tone"
    expected: "No state becomes identifiable by colour alone; U1/U2 do not get appreciably harder"
    why_human: "Colour-blind legibility of a live visual surface needs a human eye, not a class-attribute strip"
  - test: "U7 — Read one row of the 43-member family top-to-bottom and say whether the state word's double appearance (line 2 + identity line) reads as a stutter"
    expected: "Recorded as a known, deliberately-not-fixed finding (BUG-260819-01); confirm severity stays 'minor' or escalate"
    why_human: "Whether a repeated word 'reads badly' is a perception judgment"
  - test: "U8 — Search a word known to appear only in one workflow's purpose text and confirm the row returns with no visible reason why"
    expected: "Recorded trade-off (purpose hero cut, filter still matches on purpose); confirm it doesn't feel broken in practice"
    why_human: "Self-explanatoriness of a search hit is a UX judgment, not a boolean"
  - test: "U9 — Start a workflow run and, in another tab, read that workflow's card while the run is still in flight"
    expected: "The card reads 'Not recorded' with no gutter colour — honest but quiet; confirm this doesn't read as broken to a person watching their own live run"
    why_human: "Whether quiet-but-honest silence reads as a bug during a live run is a perception call"
  - test: "U10 — On a workflow the operator has NOT personally run but knows a colleague/another tenant has (or simulate via the has_any_run override), confirm the card says 'Run by someone else', never 'Never run'"
    expected: "The fourth arm renders correctly on the real shelf, not just in the unit suite. Honest limitation recorded in the row: the operator is the one user who has run everything on this box today, so the override script is the practical instrument for driving this row"
    why_human: "CR-01's fix is unproven on the real deployed pairing until a human reads it; the operator is also the only account that can exercise this arm without the override"
---

# Phase 192.2: Does This One Work? Verification Report

**Phase Goal:** A user scanning the library can tell which workflows actually work — what ran, when, and whether it succeeded — and the card gets quieter, not louder, while saying it.
**Verified:** 2026-08-19T09:00:00Z (round 1 re-verification)
**Status:** human_needed
**Re-verification:** Yes — after gap-closure round 1 (`192.2-07`…`192.2-11`)

---

## Round 1 Re-Verification (this pass)

Round 0 (below, preserved unedited) scored 4/6 with two gaps: CR-01 (FAILED, blocker) and the
owed UAT rows (UNCERTAIN). Round 1 shipped five plans closing CR-01 with an operator-chosen fix
(a non-identifying `has_any_run` EXISTS bit + a fourth `RunFact` arm) rather than the review's
suggested reword. This section re-adjudicates both gaps against the codebase as it stands now,
not against the SUMMARYs or the review that already said so.

### Gap 1 (CR-01) — re-adjudicated: ✓ VERIFIED CLOSED

Independently re-read, not taken from `192.2-REVIEW-R1.md`'s verdict:

- **The row-level bit exists and is correctly scoped.** `backend/app/db/workflows.py:211-212`
  — `_HAS_ANY_RUN_SQL = "EXISTS (SELECT 1 FROM workflow_runs r2 WHERE r2.definition_id = wd.id) AS has_any_run"`.
  Unscoped to `user_id` (deliberately — it answers "has ANYBODY run this", the lateral above it
  still answers "have YOU"). Confirmed present at all **four** projection sites by grep:
  `list_published_workflows` (both branches, lines 426/466), `list_starter_workflows` (line 652),
  `list_draft_workflows` (line 933) — matching the review's claim, not merely repeating it.
- **The bit reaches the wire un-coerced.** `backend/app/api/workflows.py` — three serializer
  sites, all `has_any_run=r.get("has_any_run")` (never `bool(...)`), confirmed at lines 503, 571,
  1379. Two model fields, `has_any_run: bool | None = None` (lines 269, 409).
- **`api.ts` stays type-only.** `has_any_run?: boolean | null` on two interfaces
  (`lib/api.ts:1528`, `:3590`) — no runtime export, matching the Phase-197 seam-decline.
- **The resolver is total and correctly ordered.** Read `runFacts.ts` in full (not summarized):
  rule 1 (`status === undefined` → `unknown`) short-circuits BEFORE `hasAnyRun` is ever read,
  so the stale-deploy case cannot reach the new logic. Inside `status === null`, three
  strict-equality branches (`=== true` → `not-by-you`, `=== false` → `never`, anything else →
  `unknown`) — never a truthiness test, which the file's own comment (correctly) notes would
  fold `false`/`null`/`undefined` into one answer and re-ship CR-01 for the absent-bit case.
- **The vocabulary matches the resolver.** `RUN_NEVER = "Never run"` in
  `libraryVocabulary.ts:456` is now documented and gated on an AFFIRMED `has_any_run === false`;
  `RUN_NOT_BY_YOU = "Run by someone else"` (line 486) is the new fourth arm, disclosure-budgeted
  to say only "somebody," never who/when/how many.
- **Live-run confirmation, not just static reading.** Ran the actual test files rather than
  trusting the SUMMARY or the round-1 review's count:
  ```
  WorkflowCard.test.tsx + .baseline.test.tsx + runFacts.test.ts + cardFace.test.ts +
  gutterTokens.fences.test.ts + apiRunFields.fences.test.ts → 6 files, 279 tests, 0 failed
  backend: test_library_run_facts.py + test_published_workflow_ownership.py → 78 passed, 0 failed
  ```
  These include a case asserting all four arms (`["worked", "failed", "never", "not-by-you"]`),
  a case for an affirmed-never row, a case for the `not-by-you` gutter (`data-run="not-by-you"`),
  and a case for an ABSENT `hasAnyRun` resolving to `unknown` never `never` (DEC-11-B).
- **`WorkflowCard.baseline.test.tsx` is provably byte-unchanged across this round**:
  `git diff d622165b HEAD --numstat -- .../WorkflowCard.baseline.test.tsx` → empty. This is the
  mechanical proof that closing CR-01 did not reopen or touch SC#4's subtraction pin.

**Verdict: CR-01 is genuinely closed, independently confirmed against source and live test runs,
not merely against the round-1 review's own narration of itself.** The fix is the operator's
chosen shape (a fourth arm + a disclosure-budgeted existence bit) rather than the review's
originally-suggested wording change, and it holds under an adversarial re-read: I could not find
a code path where an affirmed `has_any_run === true` row can still print "Never run," nor one
where the absent-bit case (a stale-deploy frontend) reaches the affirmative arm.

**Residual (non-blocking) findings, inherited from `192.2-REVIEW-R1.md` and spot-checked here,
not re-litigated:** WR-01 (three of this round's own regression suites sit 22/38/7 cases above
their count-gate pin — deletable with a green gate), WR-02 (`gutterTokens.fences.test.ts` has no
pin at all), WR-03 (a backend source-count fence is satisfied by docstring prose, not just code —
the behavior it guards is independently covered by three passing serializer tests, so this is a
weak fence, not an uncovered defect), WR-04 (`EMPTY_MATCH_REASONS`'s "frozen/shared" docblock
claim is false — every render mints a fresh array — but the render guard `matchReasons.length > 0`
at `WorkflowCard.tsx:1014` still means nothing extra paints at rest; verified by reading that
guard directly), WR-05 (two docblocks still say "three arms," one line short of the code). None
of these touch what the phase goal or SC#1-5 require; they are code-hygiene debt for the next
touch of this subtree.

### Gap 2 (owed UAT) — re-adjudicated: still ✗ UNCERTAIN, unchanged in kind, grown in scope

Nothing in round 1 could have closed this — it requires a human at a screen — and nothing did.
Verified directly against the file, not against the SUMMARY claiming it exists:

```
.planning/phases/192.2-does-this-one-work/192.2-VALIDATION.md frontmatter:
  driven: false
  rows: 10        (was 9 at round 0 — U10 added for the fourth arm)
  driven_rows: 0
  passed: 0
  failed: 0
  owed: 10
```

Grep-confirmed: all ten `- **result:**` fields in the body are empty. U4's override script was
also corrected this round (the key list grew from 2 to 3 keys to match the new `has_any_run`
field — the old 2-key version would have "passed" the row while still leaking `has_any_run` in
the overridden payload, a blind pass). **Do not score this closed. Do not accept round 1's code
as evidence for it** — per the orchestrator's explicit instruction, and because it is true: unit
tests prove the resolver is correct in isolation, not that a person scanning the real 117-row
shelf can actually triage it, or that the shelf reads quieter to an eye rather than merely to a
byte-diff.

### "Quieter, not louder" — mechanical re-check of round 1's additions

Round 1 added three things to the card's surface area: a fourth `RunFact` arm, a `matchReasons`
line, and a sixth `RUN_TONE`/`GUTTER_TONE` entry. Checked each for whether it can paint anything
at rest that round 0's resting card did not:

1. **Fourth run arm (`not-by-you`).** Renders in the SAME existing slot line 2 already used for
   the other three arms (`face.runWord`, one `<span>`) — it is a new WORD reachable in an
   existing slot, not a new DOM node. `GUTTER_TONE["not-by-you"] = "bg-muted-foreground"` and
   `RUN_TONE["not-by-you"] = "text-muted-foreground"` — read directly from `WorkflowCard.tsx`,
   both are the same quiet grey already used for `never`/`unknown` (the file's own comment notes
   this is deliberate: "the three quiet arms are told apart by their WORDS, not by three shades
   of grey"). Not a new colour, not a new visual weight.
2. **`matchReasons`.** Guarded by `matchReasons.length > 0 && (...)` at `WorkflowCard.tsx:1014`.
   `matchReasons(row, selection)` returns `[]` when no chip is pressed and no search is typed
   (the resting state) — confirmed by reading `libraryFilter.ts`'s function, not assumed. WR-04's
   finding (the "frozen shared singleton" docblock claim is false — a fresh array is minted every
   render) is a perf/prose-accuracy issue, not a rendering one: the `.length > 0` guard still
   prevents anything from painting in the resting case.
3. **Sixth gutter tone.** Read `GUTTER_TONE`/`RUN_TONE` directly — six keys now instead of five,
   `satisfies Record<RunGutter, string>` makes a missing arm a typecheck error, but the sixth
   entry's actual class values are quiet greys reusing existing Tailwind tokens, not a new colour
   introduced to the palette.
4. **`WorkflowCard.baseline.test.tsx`** — the characterization pin asserting all six D-03-cut
   atoms stay absent across every provenance face — is `git diff --numstat` empty against the
   pre-round-1 SHA. A pin whose whole purpose is to catch anything re-added to the resting card
   did not move.

**Mechanical conclusion: no state reachable by a normal (non-override) user renders more DOM, a
new colour, or a new visual weight on the resting card than existed at the end of round 0.** This
is NOT the same as confirming the felt "quieter, not louder" claim — that is U5, still owed to a
human, and a byte-diff cannot substitute for an eye on the real 117-row shelf.

### Updated Observable Truths

| # | Truth | Round 0 | Round 1 | Evidence |
|---|---|---|---|---|
| 1 | Card states when it last ran + outcome, in words + colour, never blank | ✓ VERIFIED | ✓ VERIFIED (unchanged) | `runFacts.ts` total, 4-armed now; `cardFace.ts` → `WorkflowCard.tsx:928`; 279/279 live |
| 2 | The "never run" claim is a TRUE fact about the ROW, not merely the caller (CR-01) | ✗ FAILED | ✓ **VERIFIED — CLOSED** | `has_any_run` EXISTS bit at all 4 sites, 4th `RunFact` arm, 78+279 live tests pass, `git diff` proof the fix touched only the 4 flagged docblocks + resolver/vocabulary, not the owner-scoped lateral |
| 3 | Run facts reach the card feed-level only, degrade to "unknown" when absent | ✓ VERIFIED (mech.) | ✓ VERIFIED (mech., unchanged) | Rule 1 short-circuit confirmed by direct read; U4 (live-deploy case) still human-owed |
| 4 | Lead/defer decision lives in one presentation module; G-5 discharged | ✓ VERIFIED | ✓ VERIFIED (unchanged) | `cardFace.ts` sole caller, confirmed |
| 5 | Resting card measurably quieter — six named atoms gone | ✓ VERIFIED | ✓ VERIFIED, plus round-1 additions checked for silence-at-rest (see above) | `WorkflowCard.baseline.test.tsx` byte-unchanged since `98b47c19` through all of round 1 |
| 6 | No emoji, no system vocabulary reaches the user | ✓ VERIFIED | ✓ VERIFIED (unchanged) | unchanged in round 1's diff |
| — | The phase's central lived-experience claims are confirmed by a human | ? UNCERTAIN | ? **STILL UNCERTAIN — grown to 10 rows, 0 driven** | `192.2-VALIDATION.md` frontmatter, grep-verified |

**Score: 6/6 mechanically-checkable truths now verified** (was 4/6). **The 7th item — human
confirmation of the felt claims — remains open and is not counted as a truth failure; it routes
to `human_verification` per the decision tree** (no truth is FAILED, so `gaps_found` does not
apply; human items are non-empty, so `passed` does not apply either — the correct status is
`human_needed`).

### Requirements Coverage (re-checked)

| Requirement | Status | Evidence |
|---|---|---|
| LIB-06 | **Correctly left unchecked in REQUIREMENTS.md** | `.planning/REQUIREMENTS.md:24` — its own note says exactly what this verification found: "The CODE half is done ... but the requirement is a claim about what a user can tell, and the ten G-4 lived-experience rows ... are all still owed ... Check this when the owed UAT is driven and `/gsd:verify-work 192.2` passes." This is the right call — REQUIREMENTS.md is not lagging behind the code here, it is correctly gating on the human evidence this report also finds missing. |

### Anti-Patterns (round 1 diff only)

No `TBD`/`FIXME`/`XXX` markers in any file touched by round 1 (`git diff d622165b HEAD` swept).
5 warnings / 6 info carried from `192.2-REVIEW-R1.md`, none blocking (see Gap 1 residual findings
above) — all are fence-strength or docblock-accuracy issues in a subtree whose stated method is
that the docblock IS part of the contract; worth a follow-up pass, not a phase blocker.

### Recommendation

**Do not run a round 2.** G-7's own check (`check-gap-closure-rounds.cjs 192.2` → `G-7 clear`,
already measured) agrees, and there is nothing left in this phase's own remit that another round
of code could close — the sole remaining gap requires a human to read the real library, which is
categorically outside what a plan can execute. Close this phase's engineering work here; route
the 10 owed rows to the operator, starting with **U4 and U10** (both guard the CR-01 honesty fix
from opposite sides: U4 proves the honest-unknown floor under deploy skew, U10 proves the new
`not-by-you` arm actually renders on the real shelf) and **U5** (the phase's second goal clause,
"quieter not louder," which this report mechanically bounded but cannot itself confirm). Only
after those are driven does LIB-06 earn its checkbox.

---

## Round 0 — Original Verification (preserved, unedited below)

**Phase Goal:** A user scanning the library can tell which workflows actually work — what ran, when, and whether it succeeded — and the card gets quieter, not louder, while saying it.
**Verified:** 2026-08-19T05:00:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification

### Goal Achievement

#### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | A published workflow's card states when it last ran and whether it succeeded, in words plus colour, never blank | ✓ VERIFIED | `runFacts.ts` is total over its input, three-armed (`ran`/`never`/`unknown`), imported into `cardFace.ts` and rendered at `WorkflowCard.tsx:850` (`face.runWord`) with a 3px gutter (`WorkflowCard.tsx:820`). 219/219 tests pass across `WorkflowCard.test.tsx` + `.baseline.test.tsx` + `cardFace.test.ts` + `runFacts.test.ts`, run live in this verification. |
| 2 | ...but the "never run" claim must be a TRUE fact about the row, not merely about the caller | ✗ **FAILED** | **CR-01.** The lateral is owner-scoped (`r.user_id = $1`, `db/workflows.py:165`) — correct for security — but the rendered word ("Never run", `libraryVocabulary.ts:442`) is written and documented as a row-level fact everywhere downstream. On the world-readable Starters shelf (and any `is_system_global` published row), every caller but the run's owner sees "Never run" on a workflow that has actually run — measured at 20/15/11/7/1 real runs on 5 such rows. Unfixed in the codebase as of the phase tip. |
| 3 | The run facts reach the card from the feed only — no per-card fetch — and degrade to "unknown" (never a tick, never a fabricated time) when the backend doesn't carry them | ✓ VERIFIED (mechanically) / human row owed | Backend joins via `LEFT JOIN LATERAL ... LIMIT 1` at the feed level (`db/workflows.py:161-169`), one call per shelf load, not per card. `runFacts.ts` resolves `undefined` (key absent) to `unknown` BEFORE reading any timestamp (line 161). Unit-proven; the live deploy-skew scenario (U4) is not yet human-driven. |
| 4 | The lead/defer decision lives in one presentation module, shared by all library surfaces, and G-5 on `WorkflowCard.tsx` is discharged | ✓ VERIFIED | `cardFace.ts` exists, is a pure leaf (no React/DOM/store import), is the sole caller in `WorkflowCard.tsx:663` (`cardFace(row, now)`), and `WorkflowCard` is the only card component imported by `WorkflowsPage.tsx` (one import, used across Published/Draft/Starter shelves) — one component, one presentation module, not three copies. |
| 5 | The resting card is measurably quieter: the six named atoms (purpose sentence, `needs kickoff_prompt`, phase glyphs, `STRICT`, `produces: ... file`, fork-consequence paragraph) are gone from it | ✓ VERIFIED | `grep` for `kickoff_prompt`, `STRICT`, `produces:`, `phaseGlyph`/`PHASE_GLYPHS` usage, and emoji glyphs (`📄 ✨ 📝`) in `WorkflowCard.tsx` returns zero rendering hits (only historical comments). The inverted characterization pin (`WorkflowCard.baseline.test.tsx`) asserts all six absent across all three provenance faces and, in `WorkflowCard.test.tsx`, absent across all 106 rendered rows of the scale fixture. |
| 6 | No emoji and no system vocabulary (`published`/`draft`) reach the user on the card | ✓ VERIFIED | `cardFace.ts` returns business words imported from `libraryVocabulary.ts` (`STATE_RUNNABLE`/`STATE_DRAFT`/`STATE_STARTER`); `WorkflowCard.tsx:862` renders `face.state`, not the raw lifecycle word. `mark` is a plain key (`ready`/`starter`/`building`), mapped to an icon component (`MARK_ICON`), never an emoji. Zero emoji glyphs found in the rendering path. |

**Score:** 4/6 truths cleanly verified. Truth 2 is a genuine FAILED blocker (CR-01, unresolved in code). Truth 3 is mechanically verified but its live-deploy human check is unrun — folded into the human-verification list rather than double-counted as a separate truth.

#### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `frontend/src/components/workflows/library/cardFace.ts` | The one lead/defer + face-assembly module | ✓ VERIFIED | Exists, pure, exports `cardFace()`, wired as sole caller in `WorkflowCard.tsx` |
| `frontend/src/components/workflows/library/runFacts.ts` | The one wire→words run-truth resolver, three arms | ✓ VERIFIED, correctness caveat | Exists, pure, total, exports `runFacts()`; resolution logic is correct, but the `never` arm's underlying database fact is caller-scoped while the module documents it as row-scoped (CR-01) |
| `backend/app/db/workflows.py` (`_LAST_RUN_LATERAL_SQL`) | Feed-level JOIN, no migration, owner-scoped | ✓ VERIFIED (SQL correctness), ⚠ (semantic honesty) | `LEFT JOIN LATERAL ... LIMIT 1`, single constant reused by 3 feeds, no new placeholder, `git diff` confirms zero migration files touched. SQL is sound; the security scoping is sound; what it *means* downstream is where CR-01 lives |
| `frontend/src/components/workflows/library/WorkflowCard.tsx` | Variant-C card face, gutter + subtracted atoms | ✓ VERIFIED | 1104 lines, contains `cardFace(`, `gutter`, six CUT atoms absent, no emoji, business state words |
| `frontend/src/dev/SketchLibraryCard.tsx` + `/sketch-card` route | Torn down | ✓ VERIFIED | `frontend/src/dev` directory does not exist; `grep sketch-card frontend/src/main.tsx` returns nothing |
| `.planning/phases/192.2-does-this-one-work/192.2-VALIDATION.md` | 9 authored G-4 rows | ✓ VERIFIED (exists, well-formed) / ⚠ (0/9 driven) | File present, 9 rows each with concrete drive/fail scripts, `driven: false` |
| `.planning/reported-bugs/library-card-state-word-renders-twice-on-colliding-row.md` | BUG-260819-01 filed | ✓ VERIFIED | Present, `status: open`, `surface: Agentic-RAG`, correctly routable at next touchpoint |

#### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `WorkflowCard.tsx` | `cardFace.ts` | `cardFace(row, now)` call at line 663 | ✓ WIRED | Confirmed by grep + passing tests exercising the actual render path |
| `cardFace.ts` | `runFacts.ts` | `runFacts(row, now)` call, `cardFace.ts:189` | ✓ WIRED | Confirmed; face.run/face.runWord both sourced from the resolver, not re-derived |
| `db/workflows.py` (3 feeds) | `workflow_runs` | `_LAST_RUN_LATERAL_SQL` shared constant | ✓ WIRED | One constant, three call sites (`list_published_workflows` ×2 branches, `list_starter_workflows`, `list_draft_workflows` per SUMMARY), live HTTP responses in `192.2-03-SUMMARY.md` §2a show both keys on real payloads |
| `api/workflows.py` response models | frontend `api.ts` types | `last_run_at` / `last_run_status` fields | ✓ WIRED, ⚠ naming collision (WR-02, non-blocking) | Fields present on `PublishedWorkflow` and `WorkflowDraftRow`; WR-02 notes a same-column/different-name collision with an existing `ThreadWorkflowState.last_run_created_at` field — a maintainability risk, not a functional break |

#### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| LIB-06 | 01, 02, 03, 04, 05, 06 (all six) | "A user can tell from the library whether a workflow actually works ... so a shelf of identically-named workflows can be triaged" | ✗ **BLOCKED** | Mechanically 90% built and well-tested, but CR-01 means the "actually works" claim is FALSE for shared/global workflows viewed by anyone but the run owner — precisely the "identically-named workflows shelf" scenario the requirement names (the 43-member `Compliance Gap Report` family and the Starters shelf are the two places this bites hardest). REQUIREMENTS.md correctly leaves the checkbox unticked; no plan or SUMMARY claims it complete. |

No orphaned requirements — LIB-06 is the phase's sole requirement and is claimed by all six plans.

#### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `backend/app/db/workflows.py` / `libraryVocabulary.ts` / `runFacts.ts` / `api.ts` | multiple (see gap 1) | Docblocks assert a row-level fact that the code now produces only at caller-level | 🛑 Blocker | This is CR-01 — a truthfulness defect, not a code-smell; see gap above |
| `frontend/tailwind.config.js` / `WorkflowCard.tsx:494,503` | — | `bg-warning`/`text-warning` used for the `stopped` gutter/word but `warning` is not a Tailwind color key and `--warning` is defined only inside `.dark`, not `:root` | ⚠ Warning | The `cancelled`-outcome gutter renders with no colour at all (word still shows, so not blank/wrong — just uncoloured, contrary to the docblock's own "Deep Midnight tokens only, no new hex" claim). Repo-wide latent bug (also affects `ModelDiscoveryPanel.tsx` etc.), not newly introduced but newly load-bearing here (WR-01 in the review) |
| `frontend/src/components/workflows/library/WorkflowCard.tsx:1012-1019` | — | Fork-consequence `<p>` rendered as a direct, non-`menuitem` child of Radix `role="menu"` | ⚠ Warning | ARIA `aria-required-children` violation, no axe assertion catches it (WR-03) |
| `frontend/src/components/workflows/library/libraryFilter.ts:220-222,246-247` | — | Two filter chips (`makes-a-file`, `strict`) and the search's `purpose` arm select on card atoms D-03 removed from the resting card | ⚠ Warning | A filtered/searched row can carry no visible evidence of the property that selected it (WR-04) — recorded trade-off (U8), broader than the phase's own record acknowledged |

No `TBD`/`FIXME`/`XXX` unresolved-debt markers found in phase-modified files.

#### Gaps Summary (round 0, historical — superseded by the round 1 section above)

Two things stood between this phase and a clean pass, and they were different in kind.

**The blocker (CR-01)** was a genuine truth failure, not a nitpick. The phase set out to answer "does this one work?" honestly, went out of its way to design a three-armed resolver specifically so a missing fact could never be mistaken for a negative one (D-08), and the code review — correctly, and independently confirmed here against the live source — found that the SQL scoping decision (right for security) quietly turned the "never run" arm into a per-caller answer while every doc-comment, vocabulary string and type comment still asserted it was a per-row answer. **This is now CLOSED — see the round 1 section above.**

**The uncertainty (owed UAT)** is the lived-experience side of the same goal — still open, grown from 9 to 10 rows, 0/10 driven. See round 1 section above for the current state.

---

_Round 1 re-verification: 2026-08-19T09:00:00Z_
_Round 0 (original): 2026-08-19T05:00:00Z_
_Verifier: Claude (gsd-verifier)_
