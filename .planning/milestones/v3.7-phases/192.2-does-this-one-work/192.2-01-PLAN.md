---
phase: 192.2-does-this-one-work
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [LIB-06]

must_haves:
  truths:
    - "The card's CURRENT rendered atoms are written down before a single source byte changes, so Wave 4's subtraction can be proved rather than asserted"
    - "A later wave can tell a real regression from a SEED-171 flake, because the pre-change gate verdicts are recorded verbatim"
    - "The six atoms D-03 removes are named as literal on-screen strings, not as a description"
  artifacts:
    - path: ".planning/phases/192.2-does-this-one-work/192.2-01-SUMMARY.md"
      provides: "The phase base SHA, the card's current atom inventory, four gate baselines, and the re-derived hot-file triples"
      contains: "phase base SHA"
  key_links:
    - from: "192.2-01-SUMMARY.md"
      to: "192.2-05's subtraction proof"
      via: "the literal atom inventory taken before any change"
      pattern: "atom inventory"
---

<objective>
Record what the library card renders **today**, before a single source byte changes.

Purpose: 188.1's binding lesson — *a characterization baseline only proves something if it
PREDATES the change*. This phase's headline claim (D-03) is that the resting card loses six
atoms. That claim is unprovable unless the nine it currently carries are written down first,
as **literal strings**, at a known SHA.

Output: `192.2-01-SUMMARY.md` carrying the phase base SHA, the atom inventory, four gate
baselines and three re-derived ledger triples.

⚠ **THIS PLAN MODIFIES NO SOURCE FILE.** `files_modified` is empty by design. A task here that
edits source has misread the plan.
</objective>

<execution_context>
Read `.planning/phases/192.2-does-this-one-work/CONTEXT.md` in full before starting. Its
`<measurements>` block is the phase's evidence base and must not be re-derived by guesswork.
</execution_context>

<context>
Sketch 179 mounted the real `WorkflowCard` at the dev route `/sketch-card` and found the shipped
card is **nine information rows deep** — the opposite of what four prior sketches assumed. The
phase is therefore subtraction, and subtraction needs a before-picture.

⚠ The dev route is a *sketch surface*, not the product. This baseline must be taken from the
**real library page**, because that is the surface Wave 4 changes.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the phase base SHA and the four gate baselines</name>

  <read_first>
    - `CLAUDE.md` §"Parallel execution — worktrees are ENABLED" — the cap rule **and its 2026-08-17
      correction**. ⚠ The correction is load-bearing: a red gate must NOT be met by reaching for
      the cap. Capture failing filenames from the gate's own persisted JSON BEFORE any re-run.
    - `.planning/phases/197-guided-authoring/197-01-PLAN.md` — the house precedent for this plan
      shape. Follow it; do not invent a second convention.
  </read_first>

  <files>none — this task runs commands and records their output</files>

  <action>
    Record `git rev-parse HEAD` as the **phase base SHA**, written into the SUMMARY as a literal.

    Run and record verbatim verdict lines (not summaries) for:
    1. `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — record the error COUNT.
       ⚠ `tsc --noEmit` without `-p tsconfig.app.json` checks ZERO files. The count is expected to
       be non-zero and pre-existing; what matters is that it does not MOVE.
    2. `cd frontend && GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` — record the
       full verdict line (total · failed · pinned total · pinned files).
       ⚠ Its contract is *no per-file DECREASE* and *zero failing* — never a fixed grand total. A
       larger number than any figure quoted in CLAUDE.md is the gate WORKING.
    3. The library suites specifically, in one invocation:
       `src/components/workflows/library/*.test.tsx` and `src/pages/WorkflowsPage.test.tsx`.
    4. `cd backend && ./venv/Scripts/python.exe -m pytest tests/unit -q` — record failed/passed.
       ⚠ A non-zero failure count is the known SEED-056 rot set, not this phase's doing.
  </action>

  <verify>Every recorded figure is a verbatim line from the tool, and the SUMMARY says which command produced it.</verify>
</task>

<task type="auto">
  <name>Task 2: Inventory what the card renders TODAY, as literal strings</name>

  <read_first>
    - `frontend/src/components/workflows/library/WorkflowCard.tsx` — the whole render body.
      ⚠ Note `ROW_FACE`'s `mark` values: they are **emoji** (`📄 ✨ 📝`). Record them as a defect
      (D-06), do not fix them here.
    - `frontend/src/components/workflows/library/rowIdentity.ts` — `resolveIdentity`'s four fields
      (`own`, `segs`, `ofN`, `when`). This is what already answers LIB-05.
    - `frontend/src/components/workflows/WorkflowSoul.tsx` — rendered by the card at `scale="card"`.
  </read_first>

  <files>none — reading and recording only</files>

  <action>
    Produce an **atom inventory**: every distinct piece of information the resting published card
    renders, each as the literal string or literal token that appears on screen, in DOM order.
    Sketch 179's captured render is the reference; re-derive it from the source so the list is
    complete rather than screenshot-limited.

    Mark each atom with its disposition under D-03: **KEEP** or **CUT**. The six CUT atoms are the
    purpose sentence, `needs kickoff_prompt`, the phase glyphs, `STRICT`, `produces: … · file`, and
    the fork-consequence paragraph.

    ⚠ Record explicitly that `3 share this name · changed 2 days ago` is **already rendered today**
    (D-04). LIB-05 stays complete; a later wave that "adds" it has misread the phase.
  </action>

  <verify>The inventory names ≥ 9 atoms, each with a KEEP/CUT disposition, and the six CUT atoms match D-03 exactly.</verify>
</task>

<task type="auto">
  <name>Task 3: Re-derive the hot-file ledger triples this phase will touch</name>

  <read_first>
    - `CLAUDE.md` §"Hot-file ledger" — the re-derivation recipe and the ⚠ that six-digit buckets
      (`260814`, `260809`) are DATED QUICK TASKS, not phases, and must be subtracted.
    - `docs/HOT-FILE-LEDGER.md` — the detail sections for the files below.
  </read_first>

  <files>none — measurement only</files>

  <action>
    Re-derive `commits / phases / lines` with the CLAUDE.md recipe for:
    `frontend/src/components/workflows/library/WorkflowCard.tsx`,
    `frontend/src/lib/api.ts`, and `backend/app/api/workflows.py`.

    Compare each against its ledger row and record any staleness as a delta. ⚠ Do **not** edit the
    ledger here — Wave 5 owns the same-commit sync. Recording the delta now means Wave 5 cannot
    silently inherit a stale cell.
  </action>

  <verify>Three triples recorded, each with its ledger-row delta stated (or "row matches").</verify>
</task>

</tasks>

<threat_model>
- **T-01 — The baseline postdates the change.** Mitigated by `files_modified: []` and by recording
  the SHA before any other plan runs. A baseline taken in Wave 2 proves nothing.
- **T-02 — A red gate is misread as this phase's fault.** Mitigated by recording all four verdicts
  now, and by the CLAUDE.md 2026-08-17 correction: capture failing filenames from the gate's own
  JSON before re-running anything, and check each against `git diff --numstat`.
- **T-03 — The atom inventory is a description, not evidence.** Mitigated by requiring literal
  strings in DOM order.
</threat_model>

<verification>
- `git status --short` shows no modified source file attributable to this plan.
- The SUMMARY contains a literal 40-char SHA, four verbatim gate verdict lines, an atom inventory
  of ≥ 9 entries, and three ledger triples.
</verification>

<success_criteria>
- [ ] Phase base SHA recorded as a literal
- [ ] Four gate baselines recorded verbatim, each attributed to its command
- [ ] Atom inventory complete, in DOM order, each atom KEEP/CUT
- [ ] `3 share this name` recorded as ALREADY SHIPPING (D-04)
- [ ] Three hot-file triples re-derived with their ledger deltas
- [ ] No source file modified
</success_criteria>

<output>
`.planning/phases/192.2-does-this-one-work/192.2-01-SUMMARY.md`
</output>
