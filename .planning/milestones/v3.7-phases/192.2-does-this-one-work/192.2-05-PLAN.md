---
phase: 192.2-does-this-one-work
plan: 05
type: execute
wave: 4
depends_on: [04]
files_modified:
  - frontend/src/components/workflows/library/WorkflowCard.tsx
  - frontend/src/components/workflows/library/cardFace.ts
  - frontend/src/components/workflows/library/WorkflowCard.baseline.test.tsx
  - frontend/src/components/workflows/library/WorkflowCard.test.tsx
  - frontend/src/lib/phaseGlyph.tsx
autonomous: true
requirements: [LIB-06]

must_haves:
  truths:
    - "The resting card is measurably quieter than before — the six named atoms are gone from it"
    - "The name KEEPS the lead; the outcome arrives in the gutter, peripherally"
    - "No emoji and no system vocabulary reach the user on the card"
  artifacts:
    - path: "frontend/src/components/workflows/library/WorkflowCard.tsx"
      provides: "The variant-C card face"
      contains: "cardFace("
  key_links:
    - from: "WorkflowCard.tsx"
      to: "sketch 179 variant C"
      via: "the four-slot language table in CONTEXT D-01"
      pattern: "gutter"
---

<objective>
Land sketch 179's variant C: the card says whether the workflow **works**, and says six fewer
things while doing it.

⚠ **THE SUBTRACTION IS THE FEATURE.** A plan that adds the gutter and keeps the nine existing rows
has delivered the opposite of what was approved.
</objective>

<execution_context>
Read `CONTEXT.md` D-01, D-02, D-03 and D-06, plus
`.planning/sketches/179-what-the-eye-lands-on-honestly/README.md` (winner C, and its verdict
section). The rendered reference is the dev route `/sketch-card`, tab C — ⚠ **still running only
until 192.2-06 tears it down, so capture what you need from it in this plan.**
</execution_context>

<context>
⚠ **D-02 is the instruction most likely to be misread.** Sketches 177 and 178 argued *lead with
state, the name cannot be the differentiator*. **The operator chose C, which keeps the name as the
lead.** What moved is the ENCODING: outcome to a 3px gutter, arriving peripherally. A plan that
demotes the name to line two has built variant B, which was not chosen.

The four slots, from CONTEXT D-01:

| Slot | Carries |
|---|---|
| gutter, 3px | last-run outcome — worked / failed / never run. Colour, **and never colour alone** |
| line 1 | the **name**, dim mono version right-aligned |
| line 2 | the run truth in words, then the state in **business** words |
| everything else | **cut** from the resting card |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-baseline the characterization pin, deliberately and visibly</name>

  <read_first>
    - `frontend/src/components/workflows/library/WorkflowCard.baseline.test.tsx` — the pin authored
      in 192.2-02, which has passed unedited through two waves.
    - `192.2-01-SUMMARY.md` — the atom inventory with its KEEP/CUT dispositions.
  </read_first>

  <files>frontend/src/components/workflows/library/WorkflowCard.baseline.test.tsx</files>

  <action>
    This is the ONE plan permitted to edit the pin, and the edit must be **inverting**, not
    loosening: each of the six CUT atoms flips from *asserted present* to **asserted ABSENT** from
    the resting card. The KEEP atoms stay asserted present.

    ⚠ Do not delete the assertions. An atom that merely stops being checked can come back silently;
    an atom asserted absent cannot.

    Record in the SUMMARY, as a diff of counts: atoms asserted present before → after.
  </action>

  <verify>The pin asserts absence for exactly the six CUT atoms and presence for every KEEP atom.</verify>
</task>

<task type="auto">
  <name>Task 2: Draw the variant-C face</name>

  <read_first>
    - `frontend/src/components/workflows/library/cardFace.ts` — the resolved shape from waves 2–3.
      Everything the card draws comes from here; the card should contain no new conditional logic.
    - `.planning/sketches/themes/default.css` — the tokens. ⚠ **No new hex.**
  </read_first>

  <files>frontend/src/components/workflows/library/WorkflowCard.tsx</files>

  <action>
    Render the four slots. Cut from the resting card: the purpose sentence, `needs kickoff_prompt`,
    the phase glyphs, `STRICT`, `produces: … · file`, and the fork-consequence paragraph.

    ⚠ **Cut from the RESTING card — not deleted from the product.** The fork-consequence sentence in
    particular explains a real consequence before a destructive-ish act; it moves to the moment it
    is needed (the overflow action / the fork dialog), it does not vanish. Record where each cut
    atom went.

    ⚠ **The gutter is colour PLUS a word.** The word lives on line 2. A gutter alone fails the
    colour-blind reader and fails this project's standing rule.

    ⚠ **Every branch must be total.** `never` and `unknown` both render, and `unknown` must not look
    like either success or never.
  </action>

  <verify>Rendered output matches the four-slot table for all three run arms and all three provenances.</verify>
</task>

<task type="auto">
  <name>Task 3: Remove the emoji and the system vocabulary (D-06)</name>

  <read_first>
    - `frontend/src/components/workflows/library/WorkflowCard.tsx` — `ROW_FACE`'s `mark` values
      (`📄 ✨ 📝`) and `pill` values (`published` / `Starter` / `draft`).
    - `frontend/src/lib/phaseGlyph.tsx` and `frontend/src/components/workflows/soulData.ts`
      (`PHASE_GLYPHS`) — the single-source icon convention.
    - The `sketch-findings-agentic-rag` skill → `references/icon-convention.md` §4.
  </read_first>

  <files>
    frontend/src/components/workflows/library/cardFace.ts
    frontend/src/lib/phaseGlyph.tsx
  </files>

  <action>
    Replace the emoji marks with the shared glyph path, mapped from the token `cardFace` already
    returns. Replace the pill's system words with the business words `cardFace` already returns.

    ⚠ The card must not spell `published` or `draft` to the user anywhere. Those remain the
    system's words, in the system's data.
  </action>

  <verify>`grep -P "[\x{1F300}-\x{1FAFF}]" WorkflowCard.tsx cardFace.ts` returns nothing; no user-visible `published`/`draft` string remains in the card's render.</verify>
</task>

<task type="auto">
  <name>Task 4: Prove it at real scale, on the real page</name>

  <read_first>
    - `frontend/src/components/workflows/library/__fixtures__/libraryScale.ts` — `makeLibraryFixture`
      for the 100+ row case.
    - `frontend/src/pages/WorkflowsPage.test.tsx`
  </read_first>

  <files>frontend/src/components/workflows/library/WorkflowCard.test.tsx</files>

  <action>
    Extend the suite for: three identically-named rows differing only by run outcome (the measured
    worst case — 41 slugs share one name); an empty-name row; a never-run draft; an unknown-arm row.

    ⚠ Assert by **visible text**, never by `getElementById` (D-27 — the rule Phase 192's own re-drive
    broke, which is why its UAT proved the code and not the task).
  </action>

  <verify>Three same-named rows are distinguishable by asserted visible text alone.</verify>
</task>

</tasks>

<threat_model>
- **T-18 — The gutter is added and nothing is cut.** The card gets louder, not quieter — the exact
  opposite of the approved direction. Mitigated by Task 1's inverted pin, which fails if any CUT
  atom is still rendered.
- **T-19 — The name gets demoted.** That is variant B, which was not chosen. Mitigated by the D-02
  callout and by a pin assertion that the name is the first text node in the card body.
- **T-20 — Colour becomes the only carrier.** Mitigated by requiring the word on line 2 and by a
  test that asserts the word, not the class.
- **T-21 — A cut atom is deleted from the product rather than relocated.** The fork-consequence
  sentence warns about a real consequence. Mitigated by requiring Task 2 to record where each cut
  atom went.
- **T-22 — `unknown` renders as `never`.** Mitigated by a dedicated assertion; see also T-13.
</threat_model>

<verification>
- The inverted pin passes: six atoms absent, all KEEP atoms present.
- Three same-named rows distinguishable by visible text.
- No emoji, no `published`/`draft` in user-visible card output.
- `tsc` count and the vitest count gate unmoved-or-higher versus the 192.2-01 baseline; ⚠ a HIGHER
  count-gate total is the gate working, not drift.
</verification>

<success_criteria>
- [ ] The four slots render per D-01, for all three run arms and all three provenances
- [ ] Six atoms gone from the resting card, each with its new home recorded
- [ ] The name keeps the lead (D-02)
- [ ] Colour is never the only carrier
- [ ] No emoji; no system vocabulary reaches the user
- [ ] Three identically-named rows are distinguishable by visible text alone
</success_criteria>

<output>
`.planning/phases/192.2-does-this-one-work/192.2-05-SUMMARY.md`
</output>
