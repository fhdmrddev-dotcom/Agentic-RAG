---
phase: 192.2-does-this-one-work
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - frontend/src/components/workflows/library/cardFace.ts
  - frontend/src/components/workflows/library/cardFace.test.ts
  - frontend/src/components/workflows/library/WorkflowCard.tsx
  - frontend/src/components/workflows/library/WorkflowCard.baseline.test.tsx
autonomous: true
requirements: [LIB-06]

must_haves:
  truths:
    - "There is exactly ONE module that decides what a library row leads with and what defers"
    - "WorkflowCard renders BYTE-IDENTICAL output after the extraction — the seam ships before the feature it makes room for"
    - "G-5 on WorkflowCard.tsx is DISCHARGED, and discharged FIRST, not alongside the feature"
  artifacts:
    - path: "frontend/src/components/workflows/library/cardFace.ts"
      provides: "The one presentation layer: given a row, what leads and what defers"
      contains: "export function cardFace"
  key_links:
    - from: "WorkflowCard.tsx"
      to: "cardFace.ts"
      via: "a single call that resolves the face"
      pattern: "cardFace("
---

<objective>
Discharge the **G-5 obligation on `WorkflowCard.tsx`** by extracting the lead/defer decision into
one presentation module — and ship that extraction with the card's rendered output **unchanged**.

Purpose: the obligation has been carried since sketch 175 and is undischarged. Phase 192.1's D-01
established the order this project requires: *the seam ships BEFORE the feature it makes room for*.
Wave 4 then changes the language in one file instead of threading conditionals through 818 lines.

⚠ **THIS PLAN CHANGES NO PIXEL.** It is a pure refactor, proved by a characterization test. A task
here that alters what the user sees has misread the plan — the language change is Wave 4.
</objective>

<execution_context>
Read `CONTEXT.md` D-05 in full. ⚠ It records a fact confirmed against the file: the state pill sits
in a **different flex child** from the name (`flex flex-none items-center gap-1` on the right vs
`min-w-0` on the left). The extraction must make that boundary crossable by data, not by JSX.
</execution_context>

<context>
`WorkflowCard.tsx` is 818 lines across 8 commits / 3 phases. The same language is needed by three
library surfaces; today each would need its own copy. `cardFace.ts` becomes the single vocabulary,
which is what makes Wave 4 a small diff instead of a large one.

The house precedent for a types-and-pure-functions leaf in this directory is `libraryRow.ts` and
`deriveTier.ts` — exported aliases first, then the exported interface, one docblock per member.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the current render with a characterization test (RED-first, and it must predate the extraction)</name>

  <read_first>
    - `192.2-01-SUMMARY.md` — the atom inventory. This test pins exactly that list.
    - `frontend/src/components/workflows/library/__fixtures__/libraryScale.ts` — `libraryRowOf` and
      `buildIdentityIndex`/`resolveIdentity` are the real fixtures; use them, do not hand-build rows.
    - `frontend/src/components/workflows/library/WorkflowCard.test.tsx` — the existing suite's
      conventions. ⚠ Do not locate rows by `getElementById` (D-27).
  </read_first>

  <files>frontend/src/components/workflows/library/WorkflowCard.baseline.test.tsx</files>

  <action>
    Author a characterization test that renders `WorkflowCard` for four rows — a published row, a
    draft, a starter, and a row with an EMPTY name — and asserts the presence of every atom in the
    Task-2 inventory, by visible text.

    ⚠ **It must pass BEFORE the extraction.** Run it and record the verdict in the SUMMARY. A
    characterization test authored after the refactor pins the refactor's output, not the original,
    which is the exact trap 188.1 names.

    ⚠ Include the empty-name row deliberately: `WR-01` was a shipped bug where an empty name blanked
    the library title. The pin must prove it still degrades readably.
  </action>

  <verify>The new suite is green at the phase base SHA, and the verdict is recorded verbatim.</verify>
</task>

<task type="auto">
  <name>Task 2: Extract cardFace.ts — the ONE lead/defer decision</name>

  <read_first>
    - `frontend/src/components/workflows/library/libraryRow.ts` — the input type. `cardFace` takes a
      `LibraryRow` and nothing else, so it cannot reach for data the feed does not carry.
    - `frontend/src/components/workflows/library/deriveTier.ts` — the house ordering to copy.
  </read_first>

  <files>
    frontend/src/components/workflows/library/cardFace.ts
    frontend/src/components/workflows/library/cardFace.test.ts
  </files>

  <action>
    Author `cardFace(row: LibraryRow): CardFace` — a **pure, value-only leaf**: no React import, no
    runtime side effects, no API import. It returns the resolved shape the card renders:
    what leads, what defers, the state word, and the mark.

    ⚠ **Return WORDS, not system tokens.** The face's state vocabulary is the business one
    (`Ready to run` / `Still building` / `Starter`). The system spellings (`published` / `draft`)
    are an INPUT to this module and must not be an output. This is where D-06's vocabulary defect
    is structurally prevented from recurring, even though Wave 4 is what makes it visible.

    ⚠ **No emoji in the returned mark.** Return a *token* the card maps to a glyph, so the icon
    convention has one enforcement point. Do not import an icon component here — that would make
    this leaf non-pure.

    Unit-test `cardFace` directly against all three provenances, an empty name, and a missing
    `updatedAt`. ⚠ A missing value must resolve to an explicit unknown, never to a blank string.
  </action>

  <verify>`cardFace.ts` imports no React and no `@/lib/api`; its unit suite covers all three provenances plus the two absent-value cases.</verify>
</task>

<task type="auto">
  <name>Task 3: Route WorkflowCard through cardFace with output unchanged</name>

  <read_first>
    - `frontend/src/components/workflows/library/WorkflowCard.tsx` — the header block where the name
      and the pill live in different flex children (CONTEXT D-05).
  </read_first>

  <files>frontend/src/components/workflows/library/WorkflowCard.tsx</files>

  <action>
    Replace the card's inline face decisions with a single `cardFace(row)` call, mapping the returned
    mark token to the emoji the card renders **today** so output is unchanged.

    ⚠ **Keep the emoji at this step, deliberately.** Removing them here would change pixels and
    break the characterization pin, mixing a refactor with a feature. D-06's fix lands in Wave 4,
    where the pin is intentionally re-baselined.

    Re-run the Task-1 characterization suite. It must pass **without edits to the test**. ⚠ If the
    test needs changing to pass, the refactor changed behaviour — fix the source, not the test.
  </action>

  <verify>Task-1 suite green with zero diff to the test file; `git diff --numstat` on the test shows `0 0` or the file untouched.</verify>
</task>

</tasks>

<threat_model>
- **T-04 — The refactor silently changes the render.** Mitigated by the Task-1 pin authored and
  proven green BEFORE the extraction, and by the rule that it may not be edited to pass.
- **T-05 — The seam leaks impurity.** `cardFace.ts` importing React or `@/lib/api` would make it
  unreusable by the other two surfaces and re-create the copies this phase exists to prevent.
  Mitigated by an explicit import assertion in Task 2's verify.
- **T-06 — A second copy of the vocabulary appears anyway.** Mitigated by making the business words
  an OUTPUT of `cardFace` only; any other module spelling `Ready to run` is drift.
- **T-07 — G-5 is claimed rather than discharged.** Mitigated by requiring the extraction to ship in
  this wave, before Wave 4 touches the language.
</threat_model>

<verification>
- The Task-1 characterization suite passes at the base SHA and again after Task 3, unedited.
- `grep -c "from \"react\"" cardFace.ts` is 0.
- `tsc -p tsconfig.app.json` error count is unmoved from the 192.2-01 baseline.
</verification>

<success_criteria>
- [ ] `cardFace.ts` exists, is pure, and is the only place the business state words are spelled
- [ ] `WorkflowCard.tsx` resolves its face through one call
- [ ] The characterization suite passes before AND after, with zero edits to the test
- [ ] Rendered output is unchanged — no pixel moves in this plan
- [ ] G-5 on `WorkflowCard.tsx` is discharged, and discharged before the feature
</success_criteria>

<output>
`.planning/phases/192.2-does-this-one-work/192.2-02-SUMMARY.md`
</output>
