---
phase: 192.2-does-this-one-work
plan: 06
type: execute
wave: 5
depends_on: [05]
files_modified:
  - frontend/src/main.tsx
  - frontend/src/dev/SketchLibraryCard.tsx
  - docs/HOT-FILE-LEDGER.md
  - CLAUDE.md
  - .planning/phases/192.2-does-this-one-work/192.2-VALIDATION.md
autonomous: true
requirements: [LIB-06]

must_haves:
  truths:
    - "The dev-only sketch surface is gone, and its removal is proved rather than asserted"
    - "Every hot-file row this phase touched is re-derived in the SAME commit as the section it points at"
    - "The G-4 lived-experience rows exist as authored rows before anyone claims the phase is done"
  artifacts:
    - path: ".planning/phases/192.2-does-this-one-work/192.2-VALIDATION.md"
      provides: "The G-4 UAT rows, authored at scope-time rather than post-hoc"
      contains: "getElementById"
  key_links:
    - from: "docs/HOT-FILE-LEDGER.md"
      to: "CLAUDE.md hot-file table"
      via: "the same-commit sync rule"
      pattern: "same-commit"
---

<objective>
Close the phase's own loose ends: delete the sketch surface, sync the ledger, and author the
lived-experience rows **before** anyone claims this shipped.

Purpose: three things this project has been burned by, each now a task. A dev route that outlives
its sketch becomes production surface by accident. A ledger row that goes stale answers the next
auditor with `satisfied` and stops the audit. And a G-4 row written after the fact is written to
match what was built.
</objective>

<execution_context>
Read `CLAUDE.md` §"Hot-file ledger" (the re-derivation recipe **and** the ⚠ same-commit sync rule)
and §"Workflow guardrails" G-4. Read `192.2-01-SUMMARY.md` Task 3 for the triples measured at the
phase base, so this plan's re-derivation shows a delta rather than a fresh assertion.
</execution_context>

<context>
This phase's three loose ends each have a named precedent in this repository:

- **The dev route.** `main.tsx` carries a guarded `/sketch-card` branch and a `src/dev/` component,
  added under explicit operator authorisation for sketch 179 and scoped from the start as
  throwaway. It renders fixture data with no auth gate — acceptable for a sketch, not for a product.
- **The ledger.** The 2026-08-17 full re-derive found **five rows stale and three of them reading
  `satisfied`** — `threads.py` read `9+` while measuring 76 phases. A wrong row is worse than an
  absent one, because it answers the auditor and stops the audit.
- **The UAT rows.** G-4 requires operator-defined *"I'd recognise failure here"* scenarios **at
  scope-time, not post-hoc**. Phase 192 closed with eleven rows owed; Phase 193.1 with three. Those
  were decisions, not oversights — but the rows must at least EXIST before the claim.

</context>

<tasks>

<task type="auto">
  <name>Task 1: Tear down the sketch surface, and prove it is gone</name>

  <read_first>
    - `frontend/src/main.tsx` — the `/sketch-card` branch and its ⚠ teardown docblock.
    - `frontend/src/dev/SketchLibraryCard.tsx`
    - `.planning/sketches/179-what-the-eye-lands-on-honestly/README.md` § Teardown
  </read_first>

  <files>
    frontend/src/main.tsx
    frontend/src/dev/SketchLibraryCard.tsx
  </files>

  <action>
    Delete `src/dev/SketchLibraryCard.tsx` and the `/sketch-card` branch in `main.tsx`, **in this
    one commit**, returning `main.tsx` to its pre-sketch shape.

    ⚠ Prove it rather than asserting it: `grep -rn "sketch-card\|SketchLibraryCard" frontend/src`
    must return **nothing**, and `ls frontend/src/dev` must be absent or empty. Record both outputs.

    ⚠ Do NOT delete `.planning/sketches/179-…/` — the sketch is the phase's G-2 evidence and its
    README carries the verdict. Only the *source* surface goes.

    Update the sketch README's teardown section to record that it was discharged, with the commit.
  </action>

  <verify>Both greps empty; `git show --stat` shows `main.tsx` reverted to its pre-sketch line count.</verify>
</task>

<task type="auto">
  <name>Task 2: Re-derive and sync every hot-file row this phase touched</name>

  <read_first>
    - `CLAUDE.md` §"Hot-file ledger" — the recipe, and ⚠ the rule that six-digit buckets
      (`260814`, `260809`) are DATED QUICK TASKS and must be subtracted from the phase count.
    - `docs/HOT-FILE-LEDGER.md` — the sections for the files below.
  </read_first>

  <files>
    docs/HOT-FILE-LEDGER.md
    CLAUDE.md
  </files>

  <action>
    Re-derive `commits / phases / lines` for every file this phase modified, and update **both** the
    CLAUDE.md scan-list row and the `docs/HOT-FILE-LEDGER.md` section **in this same commit** — the
    sync rule. A row without a section, or a section without a row, is drift.

    Files that need rows or updates: `WorkflowCard.tsx`, `lib/api.ts`, `libraryRow.ts`,
    `backend/app/db/workflows.py`, `backend/app/api/workflows.py`, plus **new young rows** for
    `cardFace.ts` and `runFacts.ts` (listed deliberately *below* the G-5 threshold — `WorkflowsPage.tsx`
    escaped G-5 for ten phases purely by not being written down).

    ⚠ Record for `WorkflowCard.tsx` that **G-5 was DISCHARGED in Wave 2 by the `cardFace.ts`
    extraction**, and name the next seam rather than marking it `satisfied` with no successor —
    five rows were found stale in the 2026-08-17 re-derive and **three of them read `satisfied`.**

    ⚠ For `lib/api.ts`: this phase added a TYPE only, so the declined-seam decision from Phase 197
    still holds. Record that explicitly, with its standing re-open trigger.

    ⚠ Run `node scripts/check-claude-md-size.cjs` after editing CLAUDE.md and record the verdict.
    The 150,000-character gate has already tripped once and was over for three commits unnoticed.
  </action>

  <verify>Every touched file has a CLAUDE.md row AND a matching detail section, updated in one commit; the size gate exits 0.</verify>
</task>

<task type="auto">
  <name>Task 3: Author the G-4 lived-experience rows</name>

  <read_first>
    - `CLAUDE.md` §"Workflow guardrails" G-4 and the UAT scoreboard recipe.
    - `.planning/phases/192.1-workflow-identity/192.1-UAT.md` — the house row format, and the D-27
      rule its rows honour.
  </read_first>

  <files>.planning/phases/192.2-does-this-one-work/192.2-VALIDATION.md</files>

  <action>
    Author the rows **now**, before execution claims anything. At minimum:

    - **U1** — On the real library, with the operator's own data, pick out the workflow that last
      worked from a shelf of identically-named rows. ⚠ **By reading the screen, never by
      `getElementById`** — the machine check Phase 192's re-drive used proved the code and could not
      prove the row was findable.
    - **U2** — Find a workflow that **failed**, and say so without hovering or clicking.
    - **U3** — A never-run draft reads as never-run, and is not mistaken for broken.
    - **U4** — ⚠ **The unknown arm.** Run the frontend against a backend WITHOUT the new keys
      (deploy-skew) and confirm the card says it does not know — never a tick, never a blank.
      This is the row most likely to be skipped and it guards the phase's central honesty claim.
    - **U5** — The resting card reads as *quieter* than before. Subjective by design: the operator's
      eye is the instrument, and D-03's whole claim is a felt one.
    - **U6** — Colour-blind safety: every state is legible with colour ignored.

    Each row gets a `result:` field, left empty. ⚠ A scoreboard that lists only what passed is not a
    scoreboard — a row that cannot be run is recorded ⛔ with its reason, never dropped.
  </action>

  <verify>Six rows exist, each with an empty `result:`; U4 names the deploy-skew setup concretely.</verify>
</task>

</tasks>

<threat_model>
- **T-23 — The dev route outlives the sketch.** An unauthenticated pathname branch rendering fixture
  data in production. Mitigated by the grep-must-be-empty proof.
- **T-24 — A ledger row is updated and its section is not.** Mitigated by the same-commit rule and
  by verifying both sides.
- **T-25 — `WorkflowCard.tsx` is marked `satisfied` with no successor seam.** That is the exact state
  three rows were found in on 2026-08-17, each wrong by dozens of phases. Mitigated by requiring the
  next seam to be named.
- **T-26 — CLAUDE.md crosses 150,000 characters.** Every instruction in it silently stops applying,
  to every agent. Mitigated by running the size gate in-task.
- **T-27 — UAT rows are written to match what was built.** Mitigated by authoring them in this plan
  with empty results, and by U4/U5 being rows the implementation cannot trivially satisfy.
</threat_model>

<verification>
- `grep -rn "sketch-card\|SketchLibraryCard" frontend/src` → empty.
- `node scripts/check-claude-md-size.cjs` → exit 0, verdict recorded.
- Every modified file has both a CLAUDE.md row and a HOT-FILE-LEDGER section, same commit.
- `192.2-VALIDATION.md` has six rows, all `result:` empty.
</verification>

<success_criteria>
- [ ] Dev route and sketch component deleted, proved by grep
- [ ] Sketch README records the teardown as discharged
- [ ] All touched files: ledger row + detail section synced in one commit
- [ ] `WorkflowCard.tsx` records G-5 as discharged in Wave 2, with the NEXT seam named
- [ ] CLAUDE.md size gate exits 0
- [ ] Six G-4 rows authored with empty results, including the deploy-skew unknown-arm row
</success_criteria>

<output>
`.planning/phases/192.2-does-this-one-work/192.2-06-SUMMARY.md`
</output>
