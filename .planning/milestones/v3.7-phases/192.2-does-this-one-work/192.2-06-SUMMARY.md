---
phase: 192.2-does-this-one-work
plan: 06
subsystem: docs
tags: [teardown, hot-file-ledger, same-commit-sync, g-4-uat, count-gate-rot, phase-close]

# Dependency graph
requires:
  - phase: 192.2
    plan: 01
    provides: "The three triples measured at the phase base, so this plan's re-derivation shows a DELTA against a verified before-figure rather than a fresh assertion"
  - phase: 192.2
    plan: 05
    provides: "The two recorded-not-fixed findings this plan carries into VALIDATION.md with concrete re-open triggers, and the shipped card the G-4 rows are written against"
provides:
  - "The `/sketch-card` dev surface removed, proved by grep rather than asserted"
  - "Twelve hot-file rows synced table-side and section-side in the SAME commit — five corrected, three added because they were ABSENT, four young"
  - "`WorkflowCard.tsx`'s four-phase-old `obligation UNDISCHARGED` cell corrected, with the NEXT seam named"
  - "CLAUDE.md's count-gate constants corrected BESIDE their originals — the fourth rot, in two days"
  - "`192.2-VALIDATION.md` — nine G-4 rows with empty results, authored before the phase claimed anything"
  - "`BUG-260819-01` — the state-word duplication, filed where the routing scan can see it"
affects: [192.2-verification]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Prove a deletion with a grep whose exit code is recorded, plus a diff against the last PRE-feature commit — 'byte-identical to its pre-sketch shape' is checkable; 'reverted' is not"
    - "When a file is deleted, amend the docblocks that POINT at it in the same commit — nothing typechecks prose"
    - "Find ledger debt by diffing the phase's REAL diff against the plan's file list, never by trusting the list"

key-files:
  created:
    - .planning/phases/192.2-does-this-one-work/192.2-VALIDATION.md
    - .planning/reported-bugs/library-card-state-word-renders-twice-on-colliding-row.md
    - .planning/phases/192.2-does-this-one-work/192.2-06-SUMMARY.md
  modified:
    - frontend/src/main.tsx
    - frontend/src/components/workflows/library/runFacts.ts
    - frontend/src/components/workflows/library/WorkflowCard.tsx
    - .planning/sketches/179-what-the-eye-lands-on-honestly/README.md
    - docs/HOT-FILE-LEDGER.md
    - CLAUDE.md
    - .planning/STATE.md
    - .planning/ROADMAP.md
  deleted:
    - frontend/src/dev/SketchLibraryCard.tsx

key-decisions:
  - "The plan's verification demands `grep -rn 'sketch-card|SketchLibraryCard' frontend/src` return NOTHING, and TWO shipped docblocks matched it. They were AMENDED, not deleted — a dangling pointer is the class of wrong reference that survives every gate, and `runFacts.ts` is now recorded as the only home of the three-armed logic the sketch got wrong."
  - "THREE ledger rows were added that no plan named. The plan listed seven files; the phase's real diff names twelve, and `libraryFilter.ts` (4 phases) and `libraryRow.ts` (exactly 3) were at/over G-5 with no row at all."
  - "`libraryRow.ts` gets a row despite sitting EXACTLY at the threshold — that is the most expensive state for a missing row, because the next phase to touch it would have fired G-5 against nothing."
  - "`WorkflowCard.tsx` is recorded DISCHARGED with a NAMED successor seam (the ⋯ overflow menu + its armed-delete state machine) rather than `satisfied` — three of the five rows found stale on 2026-08-17 read `satisfied`."
  - "`api.ts`'s 197 seam-decline is re-affirmed EXPLICITLY rather than left as a silently unchanged cell, because its re-open trigger is worded and a reader is entitled to know it was tested, not merely not mentioned."
  - "The state-word duplication is filed as a BUG REPORT, not only as a VALIDATION row — a bug's `status:` frontmatter IS the routing index, and prose inside a phase summary is invisible to every touchpoint that scans."
  - "The search-hit trade-off is NOT filed as a bug: it is a knowingly-accepted D-03 consequence with a measured basis (14% population), so it is a deferral with a two-armed re-open trigger."
  - "LIB-06 is NOT ticked in REQUIREMENTS.md. The traceability table's own rule is 'marked here only after verification, never per-plan', and verification has not run. Recorded so the omission cannot read as an oversight."
  - "A misfiled ROADMAP record was MOVED under Rule 1: Wave 4's entire 4,267-character block had been appended to Phase 198's bullet."

patterns-established:
  - "Close a phase by diffing its REAL commit range against every plan's `files_modified`, and treat the gap as the finding rather than as bookkeeping"
  - "A deliberate deletion must state which side of the count gate the file was on — silence reads as 'unchanged' and means 'unwatched'"

requirements-completed: []

# Metrics
duration: 38min
completed: 2026-08-19
---

# Phase 192.2 Plan 06: The Teardown, The Ledger, And The Rows Written Before The Claim Summary

**The dev route is gone and the grep that proves it exits 1; twelve hot-file rows are synced on both sides in the same commits — five of them stale, and THREE added because they were ABSENT rather than new, two of which no plan in this phase ever named; and the nine G-4 rows exist with empty results, authored before anything claimed to have shipped.**

## Performance

- **Duration:** ~38 min
- **Tasks:** 3 of 3
- **Commits:** 4 task commits + this docs commit
- **Diff vs plan base:** `+818 / −291` across 9 files, of which **265 deleted lines are the sketch component**
- **No file under `backend/` or `supabase/` touched** — proved: `git diff --stat bf637b62 HEAD -- backend supabase` is **empty**

---

## 1. What shipped

| Commit | Task | Files |
|---|---|---|
| `b38764ca` | 1 — the teardown | `main.tsx` · `dev/SketchLibraryCard.tsx` (**deleted**) · `runFacts.ts` · `WorkflowCard.tsx` · sketch README |
| `23334f0d` | 2 — the ledger sync | `CLAUDE.md` (`+12 / −5`) · `docs/HOT-FILE-LEDGER.md` (`+308 / −0`) |
| `079cca07` | 3 — the G-4 rows | `192.2-VALIDATION.md` (new) · `BUG-260819-01` (new) |
| `eb279c08` | 2b — the count-gate constants | `CLAUDE.md` (`+50 / −0`) |

---

## 2. Task 1 — the sketch surface is gone, and it is PROVED

The plan's own words: *"prove it rather than asserting it."* Three independent proofs, each with its output recorded:

| Claim | Command | Result |
|---|---|---|
| the route is gone | `grep -rn "sketch-card\|SketchLibraryCard" frontend/src` | **no match, exit 1** |
| the component is gone | `ls frontend/src/dev` | **`No such file or directory`** — the directory held only that one file |
| `main.tsx` is back to its pre-sketch shape | `git diff 17a508d7 -- frontend/src/main.tsx` | **empty** |

⚠ **The third one is the load-bearing proof and it is stronger than "I reverted it."** `17a508d7` is the last commit on `main.tsx` *before* the sketch. An empty diff against it is a **byte-identity claim** a reader can re-run; *"restored"* is a claim nobody can check.

### ⚠ TWO SHIPPED DOCBLOCKS STILL POINTED AT THE DELETED PATH

The plan's verification requires the grep to return **nothing**, and it did not — **two production files matched**, neither of them the sketch:

- `library/runFacts.ts:23` — the paragraph explaining that the approved mockup carried the T-13 bug.
- `library/WorkflowCard.tsx:152` — the paragraph explaining that rendering the real component is what measured the card at nine information rows.

Both were **amended, not deleted.** The reasoning is this repository's own recorded lesson: **nothing here typechecks prose, so a wrong pointer survives every gate.** Deleting the sentences would have lost two real findings; leaving them would have left two paths that resolve to nothing.

⚠ **`runFacts.ts` now states that it is the ONLY home of the three-armed resolution** — because the sketch's `runWords` claimed *"its own three-armed unknown"* in its comment above a function with **two arms and a `Never run` catch-all**. Deleting the sketch removed that bug from the tree; **it also removed the only place a future reader could have gone looking for the version the mockup drew.** The docblock and the sketch README now both say where the correct logic lives.

### What was deliberately NOT deleted

`.planning/sketches/179-what-the-eye-lands-on-honestly/` **stays.** It is the phase's G-2 evidence and it carries the operator's verdict. Only the *source* surface went. Its teardown section now records the discharge **with the original wording preserved verbatim above it** — a teardown note quietly rewritten into the past tense cannot be audited against what it actually promised.

---

## 3. Task 2 — the ledger, and the debt that was larger than the plan thought

**Every triple was RE-DERIVED with the CLAUDE.md recipe in one batch pass. None was copied.** Six-digit dated quick-task buckets subtracted throughout.

### ⚠ The plan named SEVEN files. The phase's real diff names TWELVE.

Found by running `git diff --numstat 82dd2efd HEAD` and comparing against the plan's list — **the only method that finds this class of miss.** Three of the extra five sit at or over the G-5 threshold **with no row at all**, so the guardrail could never have fired on them at any count.

### The five that existed and were WRONG

| File | Row said | **Re-derived** | Delta |
|---|---|---|---|
| `library/WorkflowCard.tsx` | `8 / 3 / 818` | **`12 / 4 / 1105`** | +4 / +1 / +287 |
| `frontend/src/lib/api.ts` | `171 / 98 / 6174` | **`172 / 99 / 6227`** | +1 / +1 / +53 |
| `backend/app/db/workflows.py` | `36 / 18 / 1656` | **`37 / 19 / 1811`** | +1 / +1 / +155 |
| `backend/app/api/workflows.py` | `36 / 18 / 1984` | **`37 / 19 / 2073`** | +1 / +1 / +89 |
| `frontend/src/pages/WorkflowsPage.tsx` | `34 / 12 / 1176` | **`36 / 14 / 1186`** | +2 / +2 / +10 |

⚠ **`WorkflowsPage.tsx`'s row is its FIFTH consecutive staling, and the delta is NOT all this phase's.** Two phases landed on it since the last re-derivation (`197` and `192.2`); **192.2's own share is `+7 / −0`.** A reader attributing the whole delta to this phase would be wrong, so it is written down.

⚠ **`WorkflowCard.tsx` re-derives to `12 / 4 / 1105` where Wave 4 predicted `11 / 4 / 1104`** — because **this plan's own Task 1 commit touched the file.** That is the self-staling the ledger documents about itself, demonstrated one more time inside the plan whose job was to fix it.

### The three that were ABSENT — this is the finding

| File | **Re-derived** | G-5 | Named by any plan? |
|---|---|---|---|
| `library/libraryVocabulary.ts` | **`8 / 4 / 584`** | **FIRES** (4 vs 3) | flagged by `192.2-04` |
| `library/libraryFilter.ts` | **`4 / 4 / 341`** | **FIRES** (4 vs 3) | ⚠ **NO** |
| `library/libraryRow.ts` | **`3 / 3 / 162`** | **FIRES — exactly at threshold** | ⚠ **NO** |

⚠ **All three are additionally named IN PROSE inside the `WorkflowsPage.tsx` section**, which is precisely the state a row-scanning audit cannot see — the condition `MessageInput.tsx` sat in for twelve phases. **Being mentioned is not being listed.**

⚠ **`libraryFilter.ts`'s shape is worth carrying forward: 4 commits across 4 DIFFERENT phases.** Every single commit came from a different phase, which means the file never looks hot in any one plan's diff — exactly the profile a `files_modified` scan under-weights.

⚠ **`libraryRow.ts` sits EXACTLY on the boundary, which is the most expensive place for a missing row**: the very next phase to touch it would have fired G-5 against nothing.

### ✅ `WorkflowCard.tsx`'s obligation: DISCHARGED, with a successor

The cell read *"⚠ obligation UNDISCHARGED — next phase touching it owes a refactor rec FIRST"* for four phases. **It is discharged** — Wave 2 extracted the lead/defer decision to `cardFace.ts`, **before** the feature it made room for, and the proof is an **ordering**: the characterization pin was committed at `b1017d20`, one commit before `cardFace.ts` existed, and passed afterwards with a `numstat` of nothing.

⚠ **It is NOT marked `satisfied` with no successor.** The next seam is named — **the `⋯` overflow menu and its armed-delete state machine**, the largest self-contained concern left and the only part of the card that is not a pure function of the row. **Three of the five rows found stale on 2026-08-17 read `satisfied`**, each wrong by dozens of phases; a row that answers the auditor and stops the audit is worse than an absent one.

### ✅ `api.ts`'s 197 decline HOLDS, stated rather than left silent

Its re-open trigger is *worded*: **the next phase that adds a RUNTIME export, or a second concern.** 192.2's whole change there is `+53 / −0` and is **a type and nothing else** — zero new runtime exports, measured by grep over the diff, not assumed. So `196-08`'s mock-factory failure mode (249 real failures from one missing mock export) **measurably could not fire**, and the gate reads `failed 0`. **The trigger did not fire and the decline stands.** A reader is entitled to know a worded trigger was *tested*, not merely not mentioned.

### Four young rows, below the threshold on purpose

`cardFace.ts` (2/1/200) · `runFacts.ts` (2/1/176) · `relativeChanged.ts` (2/2/137) · `main.tsx` (3/1/10). The `fileIcon.tsx` precedent: **`WorkflowsPage.tsx` escaped G-5 for ten phases purely by not being written down**, and the cheapest moment to write a row is before the guardrail needs it.

⚠ **`main.tsx` is listed for almost the opposite reason to the others:** its entire 192.2 history is an **ADD then a REMOVE**, and it is now byte-identical to its pre-sketch shape. The row exists so the next person needing a throwaway surface finds **the precedent and its teardown together**.

### The same-commit sync rule, verified both directions

All twelve files have a CLAUDE.md row **and** a `docs/HOT-FILE-LEDGER.md` section, and the table and the sections moved **in one commit** (`23334f0d`). A row without a section, or a section without a row, is drift.

### The size gate

```
  CLAUDE.md    74297 chars   49.5% of limit  headroom  75703  [OK]
claude-md size gate OK — every CLAUDE.md loads, all under 120000 chars.
```
Exit `0`. After the count-gate correction (`eb279c08`) it re-reads **`77661 chars · 51.8 % · headroom 72339`, exit 0.**

---

## 4. The count-gate constants — the FOURTH rot, in two days

CLAUDE.md carried `4170 / 4096 / 83/83`, measured 2026-08-17. Re-derived at this HEAD from the **repo root**, verdict line read **verbatim**:

```
  total                                      4328    4594    +266
  total 4594  ·  failed 0  ·  pinned total 4328
count gate OK — 92/92 pinned files present, no per-file decrease, 0 failing.
```

| | CLAUDE.md said (2026-08-17) | **measured 2026-08-19** |
|---|---|---|
| grand total | 4170 | **4594** |
| pinned total | 4096 | **4328** |
| pinned files | 83/83 | **92/92** |

**Recorded BESIDE the originals, never over them** — the *rate* of rot is that section's own finding, and all three prior figure-sets remain readable. **`+424` cases in two days.**

192.2's own arc, every increment attributed with **no residual**: `4455` (wave-1 baseline; pinned already 4328, so the `+139` is entirely new **cases**, not new pins) → `4506` (+24 pin, +22 `cardFace`, +5 sweeps) → `4574` (+52 `runFacts`, +11 `cardFace`, +5 sweeps) → **`4594`** (+15 `WorkflowCard.test.tsx`, +5 the pin's re-baseline).

### ⚠ The deletion did NOT decrease the total, and that is the finding

A deliberate file deletion is the one thing that can legitimately reduce a gated count. **No pinned file decreased and 92/92 still resolve** — because **no gated suite ever covered `src/dev/` at all.**

So the gate could never have told anyone the dev surface had outlived its sketch. **That obligation was carried by a note in a sketch README and by nothing executable.** A plan that deletes a source file must say which side of the gate it was on: silence reads as *"unchanged"* and means *"unwatched"*.

⚠ **The cap held at `2` on every run of every plan in this phase and was never adjusted.** Nothing red appeared at any point, so SEED-171's triage procedure was never entered — **recorded as an observation, not as proof of innocence.** Two of SEED-171's five named flaky suites sat inside this phase's blast radius and were edited by it; both were green on the first run of every invocation.

---

## 5. Task 3 — the G-4 rows, authored before the claim

**Nine rows in `192.2-VALIDATION.md`, every `result:` empty.** The plan required six; three more were added rather than dropping real content into a summary nobody re-reads.

| Row | What it asks | Why it cannot be scripted |
|---|---|---|
| **U1** | pick the one that last worked, off a 43-member same-name shelf | the phase's thesis; a machine can find a row it was handed the id of |
| **U2** | find a **failed** one without hovering or clicking | salience. Geometry can prove position; only a person reports not seeing it |
| **U3** | a never-run draft reads never-run, not broken | tone, on 69 % of the shelf |
| **U4** | ⚠ **the unknown arm** — the frontend against a backend without the keys | the unit suite proves the resolver, never the deployed pair |
| **U5** | the resting card reads **quieter** | D-03's whole claim is a felt one |
| **U6** | colour-blind safety | *"the words differ"* is proved; *"a person can still triage"* is not |
| **U7** | the carried state-word duplication | needs a human to say whether the stutter reads badly |
| **U8** | the carried non-self-explaining search hit | needs a human to say whether an unexplained match is confusing |
| **U9** | an in-flight run reads *Not recorded* | the substitute for the parallel-thread axis |

⚠ **U4 and U5 were chosen because the implementation cannot trivially satisfy them.** U4 names **two concrete setups** (a DevTools response override, or a real backend at the phase base in a throwaway worktree) and states the trap explicitly: **delete the KEYS, do not null them** — `null` is a different arm.

**No row may locate its target by `getElementById`, by UUID, by `data-testid` or by the element picker**, and every row is driven on the **real** library — not the fixture, and not the dev route, which no longer exists.

### The 4-axis bandwidth — stated for every axis, including the three that do not apply

⚠ **A scoreboard that lists only what passed is not a scoreboard**, and an axis that does not apply is recorded with its reason rather than omitted:

- **Cross-provider (the 8-row roster): DOES NOT APPLY.** No streaming, no agent loop, no provider routing, no provider-dependent UI state. The card renders `workflow_runs.status`, joined by SQL and rendered by a pure function. ⚠ **The caveat that would make it apply is written down:** the run *was* executed by some provider, but this surface neither reads nor branches on `provider`, and `grep` finds no provider identifier under `library/`. **If a future phase puts a provider mark on a library row, that cell is wrong and the roster becomes binding.**
- **Multi-tool: does not apply** — nothing on the shelf dispatches a tool.
- **Parallel-thread: does not apply — and a substitute is driven instead of the axis being dropped.** The shelf holds no stream, but the nearest real question is reachable: what does a card say about a run happening right now? **That is U9.**
- **Long-message: does not apply — the analogue is SCALE, and it IS exercised.** 117 real rows including a 43-member same-name family; U1, U5 and U6 all run at that scale.

### Both carried findings reached VALIDATION.md with concrete triggers

1. **The state-word duplication (U7)** — re-open trigger: *an operator reads the stutter on the real shelf*, i.e. U7 failing. **The fix is in `rowIdentity.ts`, not the card:** the ranker should stop spending the state axis now that the state is unconditional on every card. ⚠ **Also FILED as `BUG-260819-01`** (`status: open`, `surface: Agentic-RAG`) — a record living only in a phase summary is invisible to every touchpoint that scans for open bugs, which is how a live bug hid for two months in this project.
2. **The non-self-explaining search hit (U8)** — **not** filed as a bug, because it is a knowingly-accepted D-03 consequence with a measured basis (14 % population). Re-open trigger has **two independent arms**: an operator reports an unaccountable result, **or** `business_requirement` population passes ~70 %.

---

## 6. Verification — every criterion with its command

| Criterion | Command | Verdict |
|---|---|---|
| the route is gone | `grep -rn "sketch-card\|SketchLibraryCard" frontend/src` | **exit 1, no match** |
| the component is gone | `ls frontend/src/dev` | **absent** |
| `main.tsx` reverted | `git diff 17a508d7 -- frontend/src/main.tsx` | **empty** |
| the size gate | `node scripts/check-claude-md-size.cjs` | **exit 0** — 77661 chars, 51.8 %, headroom 72339 |
| the count gate | `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` (repo root) | **`OK — 92/92`, total 4594, failed 0** |
| tsc unmoved | `npx tsc --noEmit -p tsconfig.app.json` | **33**, and **0** under `library/` |
| the library subtree | `vitest run src/components/workflows/library src/pages/WorkflowsPage.test.tsx` | **12 files / 640 tests / 0 failed** |
| no backend file | `git diff --stat bf637b62 HEAD -- backend supabase` | **empty** |
| both ledger sides synced | row + section presence for all 12 files | **12/12 OK** |
| nine rows, empty results | `grep -c "^- \*\*result:\*\*$"` | **9** |

⚠ **The gate was run from the REPO ROOT and its verdict line read verbatim, never piped through `tail`** — the `192.2-01` correction, which makes a `MODULE_NOT_FOUND` read as a green gate.

---

## 7. Deviations from Plan

### **1. [Rule 3 — Blocking] The plan's grep criterion could not pass without editing two files it did not name**

- **Found during:** Task 1. `grep -rn "sketch-card\|SketchLibraryCard" frontend/src` matched `library/runFacts.ts:23` and `library/WorkflowCard.tsx:152` — two **production** docblocks, neither in `files_modified`.
- **Fix:** both amended in the same commit, neither deleted. Recorded because *"the grep returns nothing"* is only meaningful if what it stopped matching is stated.

### **2. [Rule 2 — Missing critical] THREE ledger rows the plan did not name, two of which nobody named**

- **Found during:** Task 2, by diffing the phase's real range against the plan's seven-file list.
- **Issue:** `libraryFilter.ts` (**4 / 4 / 341**) and `libraryRow.ts` (**3 / 3 / 162**) are at/over G-5 **with no row at all**, so G-5 could never have fired on them. This is the `soulData.ts` failure (197) repeated one phase later.
- **Fix:** rows and sections added for all three, plus a Phase 192.2 section stating how they were found.

### **3. [Rule 3 — Blocking] `relativeBand` is not a module — the FIFTH wrong path in this phase**

- **The prompt said:** *"the `relativeBand` module `192.2-04` split out of `relativeChanged.ts`"*.
- **Measured:** `grep -rn "export function relativeBand" frontend/src` returns **one** hit, at **`relativeChanged.ts:103`**. There is no `relativeBand.ts`. The owed row is `relativeChanged.ts`'s (**2 / 2 / 137**).
- ⚠ **Collected with the other four** (`ROW_FACE` · `models/harness.py` · `libraryRow.ts` · `phaseGlyph.tsx`) in one table in `docs/HOT-FILE-LEDGER.md`, **because the RATE is the finding**: `files_modified` was wrong in **five of six plans**. A plan's file list is a claim, not a fact — and it is itself a scan list that can be incomplete in exactly the way the ledger table can.

### **4. [Rule 1 — Bug] A misfiled ROADMAP record, moved**

- **Found during:** the state update. **Wave 4's entire block — 4,267 characters of Phase 192.2's record — had been appended to Phase 198's bullet**, where it was both invisible where it belonged and wrong where it sat.
- **Fix:** moved onto the 192.2 progress line; Phase 198's bullet restored to its original 165 characters.

### **5. [Decision] `roadmap.update-plan-progress` was run, and was again measured to write nothing**

`gsd-sdk query roadmap.update-plan-progress 192.2` returned `{"updated": true, "plan_count": 6, "summary_count": 5}` and `git diff --numstat .planning/ROADMAP.md` was **empty**. **Second confirmation this phase.** Both ROADMAP.md and STATE.md were hand-edited; STATE.md's frontmatter was re-parsed as YAML afterwards to prove it was not corrupted.

### **6. [Decision] LIB-06 is NOT ticked in `REQUIREMENTS.md`**

The capability ships. The traceability table's own rule is *"Marked here only after verification, never per-plan"*, and phase verification has not run. `requirements-completed` is empty here for the same reason it was in `192.2-01` and `192.2-05`. **Recorded as a decision so the omission cannot read as an oversight.**

### No checkpoints, no auth gates

No `checkpoint:*` task was reached, no authentication gate was hit, no package was installed, and no Rule 4 situation arose. ⚠ **No `git stash` and no destructive git operation was run.**

---

## 8. Threat model — dispositions

| Threat | Disposition |
|---|---|
| **T-23** — the dev route outlives the sketch | **MITIGATED, three ways.** Grep exits 1; `frontend/src/dev/` does not exist; `main.tsx` is byte-identical to its pre-sketch commit. ⚠ **And the residual risk is named:** the gate could not have caught this, because `src/dev/` was never inside it. |
| **T-24** — a row is updated and its section is not | **MITIGATED.** Both sides moved in `23334f0d`, and presence was verified for all twelve files in both directions rather than assumed. |
| **T-25** — `WorkflowCard.tsx` marked `satisfied` with no successor | **MITIGATED.** Recorded as **DISCHARGED** with the mechanism (Wave 2, before the feature), the proof (the pin's ordering), and the **named** next seam. |
| **T-26** — CLAUDE.md crosses 150,000 characters | **MITIGATED, and measured twice.** `74297` after the table sync, `77661` after the constants correction — 51.8 % of limit, exit 0 both times. Measured with the gate, never with `wc -c`. |
| **T-27** — UAT rows written to match what was built | **MITIGATED BY ORDERING AND BY CONTENT.** Authored in this plan with empty results; U4 (deploy-skew) and U5 (*quieter*) cannot be satisfied by inspecting the diff, and U7/U8 are rows the implementation is already known to fail or trade against. |

---

## Known Stubs

None. This plan wrote no product code — it deleted a dev-only surface, amended two docblocks, and wrote documentation. No UI path renders an invented value as a result of it.

## Threat Flags

None. No network endpoint, no auth path, no file-access pattern and no schema change. The one file deleted was dev-only, unauthenticated, and rendered fixture data — **removing it narrows the surface rather than widening it.**

---

## 9. What the phase's verification inherits

| Owed | Detail |
|---|---|
| **Nine G-4 rows** | `192.2-VALIDATION.md`, all `result:` empty. **Run U4 first** (deploy-skew), **then U1** (the thesis). Real library, read the screen, never `getElementById`. |
| **`BUG-260819-01`** | `status: open`. If U7 fails, severity rises and it folds into the next library-touching phase; if U7 passes, it stays `minor`. |
| **LIB-06's checkbox** | Not ticked. The verifier owns it. |
| **The named next seam** | `WorkflowCard.tsx`'s `⋯` overflow menu + armed-delete state machine — the next phase adding a second concern there owes a refactor rec first. |
| **Three rows that just became visible** | `libraryVocabulary.ts`, `libraryFilter.ts`, `libraryRow.ts` now fire G-5. The next phase naming any of them in `files_modified` owes a refactor recommendation as its first option — **which was not possible before this plan.** |

---

## Self-Check: PASSED

- `.planning/phases/192.2-does-this-one-work/192.2-06-SUMMARY.md` — FOUND (this file)
- `.planning/phases/192.2-does-this-one-work/192.2-VALIDATION.md` — FOUND (9 rows, 9 empty `result:` fields, contains `getElementById` ×3)
- `.planning/reported-bugs/library-card-state-word-renders-twice-on-colliding-row.md` — FOUND (`BUG-260819-01`, `status: open`, `surface: Agentic-RAG`)
- `docs/HOT-FILE-LEDGER.md` — FOUND, `+308` lines, 3 new `###` sections + 5 corrected headers
- `CLAUDE.md` — FOUND, size gate exit 0 (`77661` chars, 51.8 %)
- `frontend/src/dev` — **ABSENT (correct)**
- Commits `b38764ca` · `23334f0d` · `079cca07` · `eb279c08` — all four present in `git log`
- The only file deleted in this plan is `frontend/src/dev/SketchLibraryCard.tsx`, and the deletion is the plan's Task 1
- `git diff --stat bf637b62 HEAD -- backend supabase` — **empty** (no backend file touched)
- `.planning/STATE.md` frontmatter re-parsed as YAML after hand-editing — **valid**, 7 top-level keys, no corruption
- The pre-existing uncommitted `.claude/` framework changes were left untouched and were never staged
