---
phase: 200.1-the-run-says-what-it-produced-insert
verified: 2026-08-21T02:10:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
---

# Phase 200.1: The Run Says What It Produced — Verification Report

**Phase Goal:** A person who opens a finished run is told what it produced — whichever KIND of
thing that was — and while a run is going the live line looks live.
**Verified:** 2026-08-21
**Status:** passed
**Re-verification:** No — initial verification

## Method

This verification does not trust the three SUMMARY.md files' claims. Every claim below was
independently re-derived against the merged tree at HEAD (`a38cb81f`, with the phase's own
merge commits `b99018b1` / `a7dccc82` / `145cf1ff` present in `git log`):

- Read the actual source of `backend/app/models/thread.py`, `backend/app/db/workflows.py`,
  `backend/app/api/workflow_runs.py`, `frontend/src/pages/WorkflowRunPage.tsx`,
  `frontend/src/lib/api.ts`, `frontend/src/components/workflows/RunTranscript.tsx`.
- Re-ran the new backend test files directly (not trusting the SUMMARY's pytest transcript):
  `test_200_1_phase_output_shape.py` + `test_200_1_deliverable_text.py` → **70 passed**.
- Re-ran the full `backend/tests/unit` suite → **62 failed, 2420 passed** (matches the
  orchestrator's measured baseline exactly).
- Re-ran `backend/tests/test_harness_engine.py` (outside the plan's declared `tests/unit`
  scope, where plan 01's deviation patched three sites) → **61 passed**.
- Re-ran `backend/tests/unit/test_publish_service.py` (the fourth deviation-patched site) →
  **125 passed**.
- Re-ran the frontend count gate from the repo root (`GSD_VITEST_MAX_WORKERS=2 node
  scripts/vitest-count-gate.cjs`) → **count gate OK — 110/110 pinned files present, no
  per-file decrease, 0 failing. total 5395 · pinned total 5004** — matches the orchestrator's
  measured baseline exactly.
- Re-ran `tsc -p tsconfig.app.json --noEmit` → **33 errors / 19 files**, matches baseline.
- Re-ran `RunTranscript.test.tsx` + `WorkflowRunPage.test.tsx` directly → **206 passed** (2
  files).
- Re-ran `node scripts/check-claude-md-size.cjs` → exit 0, 116,387 chars / 77.6%.
- Confirmed no migration was applied that this phase didn't intend (`ls supabase/migrations`
  = 117, migration 123 present but unapplied, consistent with plan 01's stated decision).
- Scanned all ten phase-touched source/test files for `TBD`/`FIXME`/`XXX` — zero hits.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | A phase row with a jsonb string-scalar `output` still yields its declared `_measure`/text | ✓ VERIFIED | `phase_output_object()` (`backend/app/models/thread.py:49`) unwraps both dict and `json.loads`-able string shapes; `declared_phase_measure` calls it at `:126`; 70 new tests pass, including the string-scalar case |
| 2 | A per-step count declared by an executor actually renders on a real row, in the browser | ✓ VERIFIED | Plan 01's blocking checkpoint was discharged by the operator on TWO runs (`bef870ba…` and a fresh post-merge run) — 5/5 and a second independent match, both read from DB and screen together. Code path confirmed live in `phase_output_object` → `declared_phase_measure` → `WorkflowRunPhaseRead.step_count`/`step_noun` |
| 3 | A finished run whose deliverable was TEXT says so and shows it; file / both / neither each render distinctly | ✓ VERIFIED | `WorkflowRunPage.tsx` four-arm logic (`runAnswer` memo `:834-848`, render gate `:1683-1799`) confirmed in source; 4 dedicated test cases (`FILE ONLY` / `TEXT ONLY` / `BOTH` / `GENUINELY NOTHING`, `WorkflowRunPage.test.tsx:1633-1687`) plus a set-size collapse-detector test (`:1689-1706`) all pass; operator drove 3 of 4 arms live in Chrome post-merge, 4th proven to have zero live instances by the UI's own predicate |
| 4 | Only `text` reaches the wire — no `citations`/`source_refs`/`field_map`/`tool_call_id`/`sub_run_id`/prompts | ✓ VERIFIED | `WorkflowRunPhaseRead.deliverable_text` (`workflow_runs.py:175`) is the only new field; serializer reads `obj.get("text")` by name only (`:748`); leak fence (`test_200_1_deliverable_text.py`) asserts SET EQUALITY over the real response body against `_EXPECTED_PHASE_KEYS` (9 keys) with 14 planted internal keys incl. all of `citations`/`source_refs`/`similarity_scores`/`sub_run_id`/`field_map`/`tool_call_id`, plus a prompt-sentinel absence check over the WHOLE response text, plus a never-before-seen planted key as positive control — all pass |
| 5 | The run log's live line is visibly live (indigo pulsing clock + spinner); both shipped refusals hold | ✓ VERIFIED | `RunTranscript.tsx` live-row block (`:504-560`) confirmed: `text-primary` + `animate-pulse` on the clock, a CSS ring `animate-spin` beside the line, scoped to `isLive`. Operator-measured live `color: rgb(163, 165, 255)` matches exactly; `[data-live="true"]` count = 1; live line NOT dimmed (brightest in column, confirmed against completed/not-started rows); no invented narration (only step name + one state word) |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `backend/app/models/thread.py::phase_output_object` | ONE read-side unwrap | ✓ VERIFIED | Present, called by `declared_phase_measure` and by `workflow_runs.py`'s serializer; never raises (guarded `isinstance` + try/except, no `pytest.raises` anywhere in its test file) |
| `backend/app/db/workflows.py` (3 terminal writers) | stop double-encoding | ✓ VERIFIED | `complete_phase`, `fail_phase`, `record_phase_not_sent` all bind plain dicts to `$2::jsonb`; SQL literals unchanged (asserted byte-identical by the plan's own AST fence) |
| `backend/tests/unit/test_200_1_phase_output_shape.py` | contract + counterfactual + writer fence | ✓ VERIFIED | 554 lines (min 120); re-run independently, all pass |
| `backend/app/api/workflow_runs.py::WorkflowRunPhaseRead.deliverable_text` | ONE narrow declared field | ✓ VERIFIED | Present at `:175`; allow-list read confirmed, not a filtered copy |
| `backend/tests/unit/test_200_1_deliverable_text.py` | set-equality leak fence + planted positive control | ✓ VERIFIED | present; re-run independently, all pass; set-equality + per-key diagnostic + whole-response-text sentinel check confirmed in source |
| `frontend/src/pages/WorkflowRunPage.tsx` (four renders) | `COPY_ANSWER_HEADING` present, four distinct arms | ✓ VERIFIED | `COPY_ANSWER_HEADING = "The answer this run wrote"` at `:249`; four-arm gate logic confirmed; `COPY_NO_FILES_TERMINAL` re-worded to cover both kinds |
| `frontend/src/components/workflows/RunTranscript.tsx` (live treatment) | `animate-pulse` present | ✓ VERIFIED | present at `:512`; gutter `w-16`/12px/500/1.4 confirmed at `:511`; spinner at `:560` |
| `frontend/src/components/workflows/RunTranscript.test.tsx` | ≥560 lines, both refusals asserted | ✓ VERIFIED | 845 lines; re-run, 44/44 pass |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `declared_phase_measure` | `phase_output_object` | direct call | ✓ WIRED | confirmed at source line 126 area, calls `phase_output_object(raw)` as first statement |
| `complete_phase` | asyncpg pool jsonb codec | plain dict to `$2::jsonb` | ✓ WIRED | confirmed, `output` (plain dict) is the bound parameter, not `json.dumps(output)` |
| `workflow_runs.py` phase serializer loop | `phase_output_object` | one parse per row, feeds both measure + text read | ✓ WIRED | confirmed at `:731` (`obj = phase_output_object(row.get("output"))`), then both `declared_phase_measure(obj)` and `obj.get("text")` reuse `obj` |
| `WorkflowRunPage.tsx` deliverable region | `deliverable_text` on the run read | last server-ordered row with non-empty text | ✓ WIRED | `runAnswer` memo confirmed, iterates `run.phases` backwards, returns first non-empty `deliverable_text` |
| `RunTranscript.tsx` live row | existing `isLive` derivation | same boolean drives `data-live` and the new treatment | ✓ WIRED | `data-live={isLive ? "true" : undefined}` at `:424`; live styling gated on the same `isLive` at `:512` |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| RUN-04 | 200.1-01, 200.1-02, 200.1-03 (all three) | A person who opens a finished run is told what it produced (any kind), and the live line looks live while running | ✓ SATISFIED | All 5 observable truths above verified against source, independently-run tests, and operator-witnessed live checkpoints. No orphaned requirements — RUN-04 is the only ID in scope and is claimed by all three plans consistently with the ROADMAP entry. |

No orphaned requirements: `.planning/REQUIREMENTS.md` names RUN-04 only for this phase, and all three plans declare it.

### Anti-Patterns Found

None. Zero `TBD`/`FIXME`/`XXX` markers across all ten phase-touched source and test files. No stub returns, no empty handlers, no hardcoded-empty props feeding a render path.

### Deviations Reviewed (not gaps — recorded and correctly implemented)

- **Plan 01** fixed four shipped test sites (one in `tests/unit/test_publish_service.py`,
  three in `tests/test_harness_engine.py`, the latter outside the plan's declared
  `tests/unit` verification scope) that were asserting the old double-encoded shape via
  `json.loads` on the writer's bound argument. Verified: all four now route through the
  shipped `phase_output_object` via a documented `_phase_output_arg` helper
  (`test_harness_engine.py:845`), and both files pass in full (`test_harness_engine.py`
  61/61, `test_publish_service.py` 125/125).
- **Plan 01's checkpoint discharge ran on a different run** than the plan named
  (`bef870ba-dbdc-4442-ab5c-b0153d30bdb9`, then a fresh post-merge run, rather than
  `f4c8d111-…`). Judged adequate: the alternate run carries genuinely different counts
  (20/20 vs the plan's predicted 10/10), so the discharge could not have been a memorized
  match, and a second independent run after merge strengthens rather than weakens the
  discharge.
- **`frontend/src/lib/api.ts`'s claimed staleness was refuted by plan 02's own
  re-measurement** (`177/100/6521` was current, not stale — the `102` figure came from not
  subtracting six-digit dated quick-task buckets). Recorded correctly in both CLAUDE.md and
  the SUMMARY rather than silently "corrected" to a wrong value.
- **A third stale ledger row was found and fixed** (`phaseDuration.ts`, `3/1/493` →
  `5/1/525`) that no plan named — confirmed present in CLAUDE.md's current text.

### Human Verification Required

None outstanding. Both plans with blocking `checkpoint:human-verify` gates (200.1-01 task 3,
200.1-03 task 3) were resolved by the operator on 2026-08-21, with verbatim readings recorded
in their SUMMARY.md files (DB-vs-screen count table for plan 01; computed CSS `color` /
`animation-name` / `[data-live]` count for plan 03). Plan 02's four-arm browser walk, initially
recorded as owed, was subsequently performed by the orchestrator with the operator watching
(commit `a7dccc82`) — three arms driven live, the fourth proven unreachable in this database
by the UI's own thread-scoped predicate (not merely asserted absent).

### Gaps Summary

No gaps. All 5 ROADMAP success criteria are independently verified against the codebase, not
against SUMMARY.md claims: the read-defect repair is real and covers both the string-scalar
and object shapes; the deliverable-by-type region renders four genuinely distinct arms with a
collapse-detecting test; the wire exposure is bounded by an allow-list proved as a set
equality with planted-key positive controls (confirmed all six named-risk keys plus eight
more are excluded); the run log's live-row treatment matches the sketch's markup with both
prior refusals (full-strength live line, no invented narration) still holding; and all
project gates (backend unit suite, frontend count gate, typecheck, CLAUDE.md size gate) hold
at their pre-existing baselines with growth attributable entirely to this phase's own new
test cases.

**One informational note, not a gap:** `.planning/REQUIREMENTS.md`'s RUN-04 checkbox is still
unchecked (`- [ ]`) at time of verification — this is expected to be flipped by the
orchestrator at phase close, not by the executor, and does not affect the correctness finding
above.

**One flagged risk, carried from the ROADMAP's own note:** `CLAUDE.md` sits at 116,387 chars
/ 77.6%, only 3,613 below the 120,000 warn band. The next phase that writes ledger rows at
this phase's scale will trip the warn band per CLAUDE.md's own scheduled-split rule.

---

_Verified: 2026-08-21_
_Verifier: Claude (gsd-verifier)_
