---
phase: 200.1-the-run-says-what-it-produced-insert
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/workflows/RunTranscript.tsx
  - frontend/src/components/workflows/RunTranscript.test.tsx
autonomous: false
requirements: [RUN-04]

must_haves:
  truths:
    - "While a run is going, the run log's clock on the LIVE row is visibly live — the sheet's indigo, pulsing."
    - "A spinner sits beside the live line, saying `this is happening now` without saying anything in words."
    - "The gutter carries the sheet's width and type scale, so the log column reads as the sheet draws it."
    - "The live line is STILL full strength — it is not dimmed."
    - "No authored per-line narration was invented; the log still spells nothing of its own."
  artifacts:
    - path: "frontend/src/components/workflows/RunTranscript.tsx"
      provides: "the ported gutter and the live-row treatment"
      contains: "animate-pulse"
    - path: "frontend/src/components/workflows/RunTranscript.test.tsx"
      provides: "the ported values pinned, and BOTH shipped refusals asserted as still holding"
      min_lines: 560
  key_links:
    - from: "frontend/src/components/workflows/RunTranscript.tsx (the live row)"
      to: "the component's existing `isLive` derivation"
      via: "the same boolean that already drives `data-live`"
      pattern: "data-live=\\{isLive"
    - from: "the live clock's colour"
      to: "the `--primary` design token"
      via: "the `text-primary` utility — the shipped equivalent of the sheet's `.text-indigo`"
      pattern: "text-primary"
---

<objective>
Port the run log's left column from the sketch's OWN MARKUP — the acceptance bar, not a change-log and
not a derivation. The sheet gives the settled gutter `w-16` at 12px/500 and gives the LIVE row an
indigo pulsing clock plus a spinning glyph beside its line. The shipped column has `w-14` at 11px/400
and **no live treatment at all**.

Purpose: the live-row treatment is the substance of this item — a run that is going should look like
it is going. The width and the size are the smaller half and come along because the sheet says so.

Output: a log column whose live row is unmistakable, with both of `RunTranscript`'s shipped refusals
asserted as still holding rather than merely left alone.

⚠ **This plan is wave 1 and runs in PARALLEL with `200.1-01`. It therefore touches NEITHER `CLAUDE.md`
NOR `docs/HOT-FILE-LEDGER.md`** — two concurrent worktrees editing the 150,000-character gate file is
a race on the one file whose silent breakage disables every project rule at once. The hot-file ledger
obligation for `RunTranscript.tsx` is discharged by `200.1-02` in wave 2, which is also the only point
at which this file's line count is final. That is a deliberate deviation from doing it here, and it is
recorded rather than left implicit.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@CLAUDE.md

<the_acceptance_bar>
`.planning/sketches/200-journey-interactive/screens/run-surface.html` IS the design contract. Open it
in a browser beside the running app. **Do not derive the target from this plan's prose, from a
change-log, or from the ROADMAP's summary table** — deriving the bar from a secondary source instead of
from the sheet is the instrument failure that cost Phase 200 its first pass, and it is named in that
phase's own artifacts.

The relevant markup, verbatim (`run-surface.html`, the Transcript Region):

  · SETTLED row      : outer `flex space-x-4`
                       gutter `w-16 font-data-sm text-data-sm flex-shrink-0 text-[#464651]`
  · ACTIVE row (:230): outer `flex space-x-4 items-center`
                       gutter `w-16 font-data-sm text-data-sm flex-shrink-0 text-indigo animate-pulse`
                       line   `flex-1 text-[#F4F6FE] flex items-center space-x-2`
                       glyph  `material-symbols-outlined text-[16px] text-indigo animate-spin`

The sheet's own token definitions, from its inline config and `<style>` block:

  · `text-data-sm` / `font-data-sm` = **12px · line-height 1.4 · font-weight 500 · JetBrains Mono**
  · `.text-indigo` = **#A3A5FF**
  · `#464651` = the sheet's `outline-variant`
  · `body { background-color: #060A0F }`
</the_acceptance_bar>

<token_mapping>
<!-- Derived by the planner by converting the sheet's literals against `frontend/src/index.css`.
     VERIFY each in the browser's computed styles; do not trust this block on its own. -->

  · **The grounds are the SAME colour.** This tree's dark `--background: 216 45% 4%` converts to
    **#060A0F** — byte-for-byte the sheet's `body` background. So every contrast figure below is
    measured against one shared ground and the two drawings are directly comparable.

  · **The sheet's indigo IS this tree's `--primary`.** Dark `--primary: 239 100% 82%` converts to
    **#A3A5FF**, which is exactly `.text-indigo`. So `text-primary` is a token match, not an
    approximation — and at **8.87:1** on the shared ground it clears AA comfortably.

  · **The sheet's `#464651` is NOT adopted, and this is the one deliberate deviation.** Measured on
    the shared ground it is **2.16:1** — below even the 3:1 graphical floor. The shipped
    `text-muted-foreground/60` composites to about **#5D6572** = **3.43:1**, still short of 4.5:1 but
    materially better, and it is the value this tree's accessibility notes already audited. Keeping it
    is the decision (see Task 1). ⚠ Neither figure clears AA for 12px body text; that is an
    OBSERVATION about the shipped tone, recorded with a trigger, not something this plan fixes —
    raising it would re-tone every muted reading in the app.
</token_mapping>

<interfaces>
From frontend/src/components/workflows/RunTranscript.tsx (the row body, ~:405-500):
  - the `<li>` carries `data-testid={`transcript-row-${row.slug}`}`, `data-outcome`, `data-reading`,
    `data-source-conflict`, and `data-live={isLive ? "true" : undefined}` — **`isLive` already exists
    and is already correct**; this plan hangs the treatment off it and derives nothing new.
  - the gutter is `<span data-testid="transcript-clock" className="w-14 shrink-0 font-mono text-[11px] tabular-nums text-muted-foreground/60">`
  - the title is `<span data-testid="transcript-title" …>` and the state word
    `<span data-testid="transcript-state" …>`; a trailing `<span aria-hidden="true" className="min-w-0 flex-1" />`
    takes the slack.
  - the `<li>` className is `"flex min-w-0 items-baseline gap-4 text-sm leading-relaxed"`
    (`gap-4` is already the sheet's `space-x-4`).

The shipped spinner precedent — a CSS ring, NO icon import, spells nothing:
  frontend/src/components/workflows/BuilderSaveRegion.tsx:175
    `inline-block h-3 w-3 animate-spin rounded-full border-2 border-muted-foreground border-t-transparent`

The shipped live-mark precedent on the sibling column:
  frontend/src/components/workflows/RunSpine.tsx:302  → `isRunning && "animate-pulse"`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The gutter — the sheet's width and type scale, and the colour decision taken in writing</name>

  <read_first>
    - `.planning/sketches/200-journey-interactive/screens/run-surface.html` — the Transcript Region
      (≈:193-236) AND the inline `tailwind.config` `fontSize`/`fontFamily` blocks AND the `<style>`
      block. Read the sheet, not this plan's quotation of it.
    - `frontend/src/components/workflows/RunTranscript.tsx` — the whole docblock and the row body. The
      docblock records why the two-line-per-step rhythm was BUILT AND WITHDRAWN and why the running
      line is bright; both are load-bearing context for what you may and may not change.
    - `frontend/src/components/workflows/RunTranscript.test.tsx` — in full. Note especially
      `it("carries no multi-word string literal outside the imports")` (~:397) and the whole
      `describe("RunTranscript — a step that has not run is QUIETER than one that has")` block.
    - `frontend/src/index.css` lines 105-200 — the dark token block, so you can verify the mapping
      above rather than trust it.
  </read_first>

  <files>frontend/src/components/workflows/RunTranscript.tsx, frontend/src/components/workflows/RunTranscript.test.tsx</files>

  <action>
Port the SETTLED gutter to the sheet's measurements. On the `transcript-clock` span: `w-14` becomes
`w-16`; `text-[11px]` becomes the sheet's data-sm scale — 12px at weight 500 with line-height 1.4,
expressed with the tree's own utilities (`text-[12px] font-medium leading-[1.4]`). Keep `shrink-0`,
keep `font-mono`, keep `tabular-nums` — `font-mono` is this tree's analogue of the sheet's JetBrains
Mono data family, and `tabular-nums` is what makes the column align at all.

**KEEP `text-muted-foreground/60` and record the decision as `D-200.1-03-A` in a docblock on the
gutter span**, in this file's existing voice. The reason is measured rather than argued: the sheet's
`#464651` reads **2.16:1** against the ground both drawings share (`#060A0F`), which is below even the
3:1 graphical floor, while the shipped value composites to about **3.43:1**. Adopting the sheet's
literal here would make the clock column harder to read while looking like fidelity. State plainly
that this is the ONE place the port deviates, quote the sheet's literal so a future reader can see
what was declined, and record the observation that **neither figure clears AA 4.5:1** with its
re-open trigger: *the next phase that re-tones this tree's muted readings.*

Do NOT change the `<li>` className on settled rows — `items-baseline` and `gap-4` are already the
sheet's rhythm and must stay. Do NOT touch the title span, the state span, the slack span, the
ordering, `runClock`, or any of the tense-rule logic. Do NOT add a string to
`transcriptVocabulary.ts`; this task spells nothing.

Pin the ported values in `RunTranscript.test.tsx` as their own cases: the clock span's className
contains `w-16` and does NOT contain `w-14`; it contains the 12px scale and does NOT contain
`text-[11px]`; it still contains `font-mono`, `tabular-nums` and `text-muted-foreground/60`. ⚠ Add a
case asserting the settled row's `<li>` className is UNCHANGED from the shipped string, so a future
edit cannot quietly re-flow the settled rows while claiming to have touched only the gutter.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/workflows/RunTranscript.test.tsx --reporter=basic</automated>
  </verify>

  <acceptance_criteria>
    - BEHAVIOR: rendering a settled run, `screen.getAllByTestId("transcript-clock")[0].className`
      contains `w-16` and `text-[12px]` and `font-medium`; it contains neither `w-14` nor `text-[11px]`.
    - BEHAVIOR: that same className still contains `font-mono`, `tabular-nums` and
      `text-muted-foreground/60` — the deviation is a KEPT value, asserted, not a silent omission.
    - BEHAVIOR: the settled `<li>` className equals the shipped string
      `"flex min-w-0 items-baseline gap-4 text-sm leading-relaxed"` exactly.
    - SOURCE: the gutter's docblock contains the literal `#464651`, the literal `2.16` and the literal
      `3.43`, so the declined value and both measurements are findable by grep.
    - SOURCE FENCE (unchanged, must still pass UNEDITED):
      `it("carries no multi-word string literal outside the imports")` passes with no edit to its body.
      ⚠ If it reds, the cause is almost certainly a className literal you introduced whose first two
      space-separated tokens are pure letters — fix the class list, do not relax the fence.
    - SOURCE: `git diff --numstat <base> HEAD -- frontend/src/components/workflows/transcriptVocabulary.ts`
      is EMPTY. This task adds no word to the product.
    - CLI: `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the REPO ROOT — read the
      verdict line VERBATIM into the SUMMARY and attribute every increment. Baseline at `e0c57ef4`:
      total **5365** · pinned **5004** · **110/110** · failed **0**. The contract is no per-file
      DECREASE and zero failing, never a fixed total.
  </acceptance_criteria>

  <done>
The settled gutter is the sheet's width and type scale; the one declined literal is declined in
writing with both measured ratios; the settled row flow is byte-identical and asserted so; the
vocabulary is untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: The live row — the pulsing indigo clock and the spinner, with BOTH refusals asserted</name>

  <read_first>
    - `.planning/sketches/200-journey-interactive/screens/run-surface.html` ≈:229-236 — the `<!-- Active Log -->`
      block. This is four lines of markup and it is the whole specification for this task.
    - `frontend/src/components/workflows/RunTranscript.tsx` — the `isLive` / `attention` / `unrun`
      derivations and the comment block above the title span, which states the muting refusal in the
      component's own words. You are adding a treatment the refusal explicitly leaves room for: *"the
      sheet's own current line is bright with a spinner, not dim."*
    - `frontend/src/components/workflows/RunTranscript.test.tsx` — the three tests
      `it("the RUNNING step is full strength — the muting refusal, asserted")`,
      `it("a step the run never reached is dimmed")` and
      `it("a run where EVERY step finished has no alternation, and that is correct")`. These must pass
      UNEDITED after your change; a pin that holds against a test nobody re-typed is evidence, a
      re-baselined pin is only a record of what the code now does.
    - `frontend/src/components/workflows/BuilderSaveRegion.tsx` ≈:170-180 — the shipped CSS-ring
      spinner. Use this shape; it imports nothing and spells nothing.
    - `frontend/src/components/workflows/RunSpine.tsx` ≈:295-305 — `isRunning && "animate-pulse"`, the
      sibling column's live mark, so the two columns are consistent about what "live" looks like.
  </read_first>

  <files>frontend/src/components/workflows/RunTranscript.tsx, frontend/src/components/workflows/RunTranscript.test.tsx</files>

  <action>
Give the LIVE row the sheet's treatment, hung off the component's existing `isLive` boolean — the same
one that already drives `data-live`. Derive nothing new.

**The clock.** When `isLive`, the `transcript-clock` span additionally carries `text-primary` and
`animate-pulse`, and DROPS `text-muted-foreground/60` (compose with `cn` so the two tones cannot both
land — a class list carrying both is a coin flip on emission order). `text-primary` resolves to
`#A3A5FF`, which is byte-identical to the sheet's `.text-indigo`; verify that in the browser's
computed styles at the Task 3 checkpoint rather than trusting the conversion.

**The spinner.** When `isLive`, render a 16px CSS ring immediately before the title span, inside the
row: `aria-hidden="true"`, `data-testid="transcript-spinner"`, sized `h-4 w-4`, `shrink-0`,
`animate-spin`, `rounded-full`, `border-2`, `border-primary`, `border-t-transparent`. It is a `span`
with **no children and no text**. Do not import an icon: the sheet's `material-symbols-outlined sync`
is not this tree's glyph vocabulary, this component currently renders zero glyphs, and a CSS ring is
the shipped precedent that keeps that true.

**The row's cross-axis alignment.** The sheet's active row is `items-center` where its settled rows are
not. Apply `items-center` to the `<li>` **only when `isLive`**, leaving the settled rows' className
byte-identical (Task 1 pinned it). A 16px ring on a baseline-aligned row sits visibly low.

**BOTH SHIPPED REFUSALS MUST BE ASSERTED AS STILL HOLDING, not merely left alone:**

  (i) *The live line stays FULL STRENGTH.* The existing muting-refusal test must pass with its body
      UNEDITED. Add one further case: on a live row the title span carries `text-foreground` and
      carries neither `text-muted-foreground` nor any opacity modifier, **and the spinner's presence is
      asserted in the same case** — so nobody can later dim the row and call the spinner the
      compensation.

  (ii) *No authored per-line narration is invented.* A spinner is not narration — it states
      *this is happening now*, which is a fact this product HAS. Assert it: the live row's total
      rendered `textContent` equals exactly the step's title concatenated with the state word the
      component already computes, and NOTHING else. Drive it with a POSITIVE CONTROL — a locally
      constructed row with a narration sentence appended must fail the same assertion — so the check
      is proved capable of firing rather than merely green.

Add a docblock above the live-row treatment recording that the sheet's per-line narration
(*"Connecting to Northwind CRM instance…"*) is STILL refused and why, quoting the component's existing
reasoning rather than restating it loosely, so the next reader does not read the spinner as the
narration having been reopened.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/workflows/RunTranscript.test.tsx --reporter=basic</automated>
  </verify>

  <acceptance_criteria>
    - BEHAVIOR: rendering with a live step (a row `status: "active"` plus a `liveOf` reading of
      `running`), the live row's `transcript-clock` className contains `text-primary` AND
      `animate-pulse`, and does NOT contain `text-muted-foreground/60`.
    - BEHAVIOR: every SETTLED row's clock in that same render still contains `text-muted-foreground/60`
      and contains neither `text-primary` nor `animate-pulse` — the treatment is scoped to one row.
    - BEHAVIOR: `within(liveRow).getByTestId("transcript-spinner")` exists, carries
      `aria-hidden="true"`, its className contains `animate-spin` and `border-primary`, and its
      `textContent` is the empty string.
    - BEHAVIOR: `screen.queryAllByTestId("transcript-spinner")` has length `0` on a run with no live
      row — including a TERMINAL run and a run with a `not-started` step. A spinner on a finished run
      is a lie, and the negative is asserted rather than assumed.
    - BEHAVIOR (refusal i): the shipped `it("the RUNNING step is full strength — the muting refusal, asserted")`
      passes with a `git diff` of ZERO lines inside its body, verified with
      `git diff <base> HEAD -- frontend/src/components/workflows/RunTranscript.test.tsx` and reading the
      hunks. Plus the new case asserting `text-foreground` on the live title AND the spinner's presence
      together.
    - BEHAVIOR (refusal ii): the live row's `textContent` equals `titleOf(slug) + stateWord` exactly,
      with a POSITIVE CONTROL proving the assertion fires when a narration sentence is present.
    - BEHAVIOR: the live row's `<li>` className contains `items-center`; a settled row's does not.
    - SOURCE: `grep -c "lucide-react\|material-symbols" frontend/src/components/workflows/RunTranscript.tsx`
      returns `0` — no glyph was imported.
    - SOURCE: `git diff --numstat <base> HEAD -- frontend/src/components/workflows/transcriptVocabulary.ts`
      is EMPTY.
    - CLI: `npx tsc -p tsconfig.app.json --noEmit` from `frontend/` — at or better than the 33-error /
      19-file pre-existing baseline. ⚠ A bare `tsc --noEmit` checks ZERO files; the `-p` is load-bearing.
    - CLI: `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the REPO ROOT, verdict line
      read VERBATIM, every increment attributed. ⚠ If it reds, capture the failing FILENAMES from the
      gate's own persisted JSON report BEFORE re-running anything and check each against
      `git diff --numstat`; do not reach for the cap. SEED-171's five named suites are the triage list.
  </acceptance_criteria>

  <done>
The live row's clock is indigo and pulsing, a textless CSS spinner sits beside its line, the treatment
is provably scoped to that one row, and both of `RunTranscript`'s shipped refusals are asserted as
still holding — one against a test nobody re-typed, one with a driven positive control.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Sheet and browser, side by side</name>

  <read_first>
    - `.planning/sketches/200-journey-interactive/screens/run-surface.html` — open it in a browser tab.
    - `.planning/ROADMAP.md` § Phase 200.1 success criterion 4.
  </read_first>

  <action>
Pause and hand the run log to the operator. Run NOTHING further in this plan until the checkpoint
resolves. Before pausing, state in the transcript exactly which values were ported (`w-16`, 12px,
weight 500), which single value was DECLINED and why (the sheet's `#464651` at 2.16:1 against the
shared `#060A0F` ground, against the shipped `text-muted-foreground/60` at 3.43:1), and the file path
of the sheet to open — `.planning/sketches/200-journey-interactive/screens/run-surface.html`.
  </action>

  <what-built>
The run log's left column, ported from the sheet's own markup: the settled gutter at `w-16` / 12px /
weight 500, and the live row carrying an indigo pulsing clock plus a 16px spinning ring beside its
line. `RunTranscript`'s two shipped refusals are asserted as still holding — the live line is not
dimmed, and no narration was invented.

One deviation, taken in writing: the sheet's `#464651` settled colour was declined at **2.16:1**
against the shared `#060A0F` ground, in favour of the shipped `text-muted-foreground/60` at **3.43:1**.
  </what-built>

  <how-to-verify>
1. Open `.planning/sketches/200-journey-interactive/screens/run-surface.html` in one browser tab and
   the running app at `http://localhost:5173` in another. **Both open at once — this comparison is the
   whole point of the checkpoint, and Phase 200's first pass failed precisely because a secondary
   source stood in for the sheet.**
2. Start a workflow run so the run surface has a LIVE row. (Any published workflow will do; the log is
   the centre column of the run surface.)
3. Compare the two logs directly:
   · the gutter column width and the clock's size/weight read the same in both;
   · the live row's clock is indigo and pulsing in both;
   · a spinner turns beside the live line in both.
4. Confirm the live line is NOT dimmed — it is the brightest line in the column, which is the shipped
   refusal this port must not have quietly overturned.
5. Confirm no line has gained a narration sentence: every line still reads as the step's own name plus
   at most one state word.
6. In DevTools, read the computed `color` on the live clock. It must be `rgb(163, 165, 255)` —
   `#A3A5FF`, byte-identical to the sheet's `.text-indigo`. Paste the computed value.
7. **Say whether the declined `#464651` was the right call.** The settled gutter is deliberately
   lighter than the sheet draws it. If you would rather have the sheet's darker literal, say so and
   the deviation is reversed with the contrast observation recorded as accepted.

⚠ If `take_screenshot` times out (a known failure mode on this box), read the DOM geometry and computed
styles via `evaluate_script` and paste the extracted values.
  </how-to-verify>

  <done>The sheet and the running app were compared side by side on a live run; the gutter and the live treatment match, the live line is the brightest in the column, no line gained narration, and the operator has ruled on the one declined literal.</done>
  <resume-signal>Type "approved", or name what differs from the sheet — and answer question 7 either way.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-supplied phase rows → `RunTranscript` render | `titleOf(row.slug)` and the state word originate in server data and definition JSON; both already flow through this component today. |
| the live SLICE (SSE) → the rendered word | Two sources feed one line; the component's shipped tense rule governs which wins. This plan adds a TREATMENT, never a new word. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-200.1-05 | Spoofing | the live treatment on `RunTranscript` | mitigate | A spinner on a row that is not live is a false claim that work is in progress. It is bound to the component's EXISTING `isLive` — which is already gated by the shipped `pageAgreesWithWire` tense rule, so a stale SSE slice cannot animate a finished step — and the negative is asserted directly: zero spinners on a terminal run and zero on a `not-started` step. |
| T-200.1-06 | Tampering | rendered text on the live row | mitigate | The spinner is a `span` with NO children, `aria-hidden="true"`, and an asserted empty `textContent`; the live row's total `textContent` is pinned to `title + stateWord` with a positive control. No server-supplied string gains a new render path, and `dangerouslySetInnerHTML` appears nowhere in this file. |
| T-200.1-07 | Information Disclosure | this plan's scope | accept | Zero backend surface. `git diff --numstat` over `backend/` is empty for this plan; no route, no model, no query and no field changes. Nothing new crosses any boundary. |
| T-200.1-08 | Denial of Service | two CSS animations per live row | accept | At most ONE row is live per run and the log renders one line per step (5-8 typical, 27 maximum across all local definitions). `animate-pulse` is already shipped on the sibling `RunSpine` mark for the same row. Re-open trigger: a surface that renders many runs' logs at once. |
| T-200.1-SC | Tampering | npm/pip/cargo installs | mitigate | **This plan installs no package and imports no icon library.** `frontend/package.json` and `package-lock.json` are asserted byte-unchanged by `git diff --numstat`, and the spinner is a CSS ring precisely so no dependency is added. The Package Legitimacy Gate does not apply and no `[ASSUMED]`/`[SUS]` checkpoint is owed. |
</threat_model>

<verification>
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the REPO ROOT — verdict line read
  VERBATIM, no per-file decrease, `failed 0`, every increment attributed. Baseline at `e0c57ef4`:
  total 5365 · pinned 5004 · 110/110.
- `cd frontend && npx tsc -p tsconfig.app.json --noEmit` — at or better than 33 errors / 19 files.
- `git diff --numstat <base> HEAD -- backend/ supabase/ frontend/package.json` is EMPTY.
- `git diff --numstat <base> HEAD -- CLAUDE.md docs/HOT-FILE-LEDGER.md` is EMPTY — this plan is wave 1
  and deliberately does not touch the gate file (the ledger is `200.1-02`'s, in wave 2).
- The Task 3 checkpoint is approved with the computed `color` pasted and question 7 answered.

## Owed to `200.1-02`
Report the FINAL `wc -l frontend/src/components/workflows/RunTranscript.tsx` in this plan's SUMMARY.
`200.1-02` derives that file's ledger triple and cannot do it correctly until this plan has landed.
</verification>

<success_criteria>
- The settled gutter carries the sheet's `w-16` and 12px/500 scale; the settled row flow is unchanged
  and asserted so.
- The live row's clock is `text-primary` + `animate-pulse` and a textless 16px CSS ring spins beside
  its line; both are provably scoped to the one live row, with the negative asserted.
- The muting refusal test passes with its body UNEDITED.
- The no-narration refusal is asserted with a driven positive control.
- No glyph library, no vocabulary string, no backend byte, no package.
- Sheet and browser were open side by side and the operator confirmed, including the `#464651` call.
</success_criteria>

<output>
Create `.planning/phases/200.1-the-run-says-what-it-produced-insert/200.1-03-SUMMARY.md` when done.
Record VERBATIM: the count-gate verdict line with every increment attributed, the `tsc` figure, the
computed `color` from the checkpoint, the operator's answer on `#464651`, and the final `wc -l` of
`RunTranscript.tsx` for `200.1-02`'s ledger task.
</output>
</content>
