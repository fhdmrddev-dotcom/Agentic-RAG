---
phase: 200.1-the-run-says-what-it-produced-insert
plan: 02
type: execute
wave: 2
depends_on: [200.1-01, 200.1-03]
files_modified:
  - backend/app/api/workflow_runs.py
  - backend/app/models/thread.py
  - backend/tests/unit/test_200_1_deliverable_text.py
  - frontend/src/lib/api.ts
  - frontend/src/pages/WorkflowRunPage.tsx
  - frontend/src/pages/WorkflowRunPage.test.tsx
  - CLAUDE.md
  - docs/HOT-FILE-LEDGER.md
autonomous: true
requirements: [RUN-04]

must_haves:
  truths:
    - "A finished run whose deliverable was a TEXT answer says so and shows it."
    - "A run that produced a file still shows the file, unchanged."
    - "A run that produced both shows both, with the file leading."
    - "A run that produced neither says THAT — in words that are no longer about files alone."
    - "Only `text` reaches the wire: no `citations`, no `source_refs`, no `field_map`, no `tool_call_id`, no `sub_run_id`, no prompt, and no key invented after this plan shipped."
    - "Every hot file this phase touched has a CLAUDE.md row and a docs/HOT-FILE-LEDGER.md section, re-derived, updated in ONE commit."
  artifacts:
    - path: "backend/app/api/workflow_runs.py"
      provides: "`deliverable_text` — ONE narrow declared field on `WorkflowRunPhaseRead`"
      contains: "deliverable_text"
    - path: "backend/tests/unit/test_200_1_deliverable_text.py"
      provides: "the SET-EQUALITY leak fence over the real response body, with a planted-key positive control"
      min_lines: 140
    - path: "frontend/src/pages/WorkflowRunPage.tsx"
      provides: "the four distinct deliverable renders"
      contains: "COPY_ANSWER_HEADING"
  key_links:
    - from: "backend/app/api/workflow_runs.py (the phase serializer loop)"
      to: "backend/app/models/thread.py::phase_output_object"
      via: "one parse per row, feeding BOTH the measure read and the text read"
      pattern: "phase_output_object\\("
    - from: "frontend/src/pages/WorkflowRunPage.tsx (the deliverable region)"
      to: "WorkflowRunPhase.deliverable_text on the run read"
      via: "the last server-ordered row carrying a non-empty deliverable_text"
      pattern: "deliverable_text"
---

<objective>
Make the run surface name its deliverable BY TYPE. Today the region is file-only: it heads
`Files in this run's workspace` and, on a terminal empty run, says `This run produced no files.` That
sentence is TRUE and USELESS about a run whose deliverable was a text answer — and **that is the
common case: 479 phase rows carry `output.text` against 60 carrying a file, 8×.**

Purpose: this is the requirement RUN-02 never named. The data is already there and the route already
SELECTs it; the serializer keeps only `_measure` because `WorkflowRunPhaseRead` declares no field for
it. So the backend cost is ONE narrow declared field and no migration.

Output: four distinct renders, none folded into another; the answer bounded by an allow-list proved
over the real response body; and the phase's hot-file ledger obligation discharged.

⚠ **Context estimate is ~60%, above this repo's ~50% target, and it is stated rather than hidden.** The
ledger task is phase-wide by necessity — a row and its section must move in ONE commit, and no TWO
plans may touch `CLAUDE.md` concurrently without racing on the 150,000-character gate file. It lands
here because this is the only plan in the last wave, so every source edit in the phase has landed and
every triple is derivable. ⚠ **CORRECTED 2026-08-20 at the plan-check**: this paragraph originally
claimed *"neither wave-1 plan may touch `CLAUDE.md`"*, which was FALSE — `200.1-01` writes BOTH
`CLAUDE.md` and `docs/HOT-FILE-LEDGER.md` (it gives `backend/app/models/thread.py` its first-ever row
and section) and its `files_modified` omitted them; both are now declared. **No race exists** because
`200.1-01` is ALONE among the wave-1 plans in touching them — `200.1-03` touches neither — and this
plan runs in a later wave. Task 3 CONFIRMS `models/thread.py`'s row rather than creating it. Task 3 is largely mechanical (a scripted
re-derivation loop plus two document edits); if the executor is degrading, stop after Task 2, commit,
and run Task 3 in a fresh context.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/200.1-the-run-says-what-it-produced-insert/200.1-01-SUMMARY.md
@.planning/phases/200.1-the-run-says-what-it-produced-insert/200.1-03-SUMMARY.md

<measurements>
<!-- Planner, against the live local DB (127.0.0.1:54322), 2026-08-20. Re-derive; do not quote. -->

THE KEY CENSUS over all 588 non-null `output` values, read through the unwrap. ⚠ **The ROADMAP's list
of six is NOT exhaustive** — this is the whole argument for an allow-list:

    text 479 · source_refs 314 · citations 314 · similarity_scores 254 · sub_run_id 239 ·
    field_map 66 · output_file 60 · path 60 · retrieved_ids 52 · placeholder_keys 52 ·
    _failure_reason 38 · answer 34 · tool_call_id 34 · sub_questions 15 · sub_run_ids 15 ·
    failure 15 · _measure 13 · _surfaced 5 · recorded_intent 5

`answer`, `retrieved_ids`, `placeholder_keys`, `sub_questions`, `sub_run_ids`, `failure`,
`recorded_intent` and `_surfaced` appear nowhere in the ROADMAP's list and are live on real rows today.
**A deny-list of the six named keys is already incomplete, and it cannot be made fail-closed** — this
project recorded exactly that at Phase 185's security audit. Read exactly one key by name.

`output.text` LENGTH: max **38,935** · mean **3,871** · p95 **15,431** characters.

ALL FOUR ARMS EXIST ON REAL LOCAL ROWS — this is fixture-free end to end. Counted over terminal runs:
    both 63 · text-only 120 · file-only 3 · genuinely-nothing 46

Named runs to drive each arm:
  · both              `f4c8d111-e2fa-4f00-9936-7d249d396993`  (61-char text · 1 workspace file)
  · text only         `13691156-773e-4c41-b301-f25369b770d7`  (517-char text · 0 files)
                      `57b0b648-aab5-495f-943f-ac78f39989bb`  (1,492-char text · 0 files)
  · genuinely nothing `c353ff78-1b7e-45c2-a5f8-2b10d9c3b77c`  (cancelled · no text · 0 files)
  · file only         3 runs exist; re-derive with the arm query — the render is TODAY'S, unchanged.
</measurements>

<interfaces>
From backend/app/api/workflow_runs.py:
  class WorkflowRunPhaseRead(BaseModel)
    slug: str · phase_index: int · status: str · phase_type: str | None
    started_at / completed_at: datetime | None
    step_count: int | None · step_noun: str | None
    ⚠ its own comment: "THESE FOUR FIELDS, THE `.select()` PROJECTION BELOW AND THE SERIALIZER MOVE
      IN LOCKSTEP … FastAPI DROPS UNDECLARED KEYS SILENTLY". The projection ALREADY selects `output`
      (`:636`); only the model and the serializer move here.
  ⚠ the route's standing argument, `:627-633`: "`output` IS SELECTED BUT NEVER PUT ON THE WIRE …
      this jsonb carries field_map, citations and prompts … the client never sees a prompt."

From backend/app/models/thread.py (created by 200.1-01):
  def phase_output_object(raw: object) -> dict | None      # the ONE read-side unwrap
  def declared_phase_measure(raw: object) -> tuple[int | None, str | None]
  class WorkflowPhaseState(BaseModel)                       # the SECOND wire model for these rows

From frontend/src/lib/api.ts:
  export interface WorkflowRunPhase { … step_count?: number | null; step_noun?: string | null }  (~:4206)
  export interface WorkflowRunRead { … phases: WorkflowRunPhase[] }                              (~:4308)

From frontend/src/pages/WorkflowRunPage.tsx:
  COPY_DELIVERABLE_HEADING = "Files in this run's workspace"        (:216)
  COPY_NO_FILES_LIVE       = "No files yet — this run hasn't written anything."   (:217)
  COPY_NO_FILES_TERMINAL   = "This run produced no files."                        (:218)
  the region: `<section data-testid="run-deliverables">` (~:1578) with
    `data-testid="run-deliverables-empty"` on the empty paragraph
  `files` / `filesLoading` / `orderedFiles` from `useWorkspaceFiles(run?.thread_id ?? null)`
  `wireRows` — the durable phase rows in server order; `isTerminal` — the run's terminal bit
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ONE narrow declared field, bounded by an allow-list, proved over the real response body</name>

  <read_first>
    - `backend/app/api/workflow_runs.py` — `WorkflowRunPhaseRead` in full (:80-150), the `:627-633`
      standing-argument comment, and the phase serializer loop (:636-660). The lockstep rule and the
      `.select("*")` refusal both live in those comments and both bind you.
    - `backend/app/models/thread.py` — `phase_output_object` and `declared_phase_measure` as
      `200.1-01` left them, and `WorkflowPhaseState`'s comment beginning "THIS IS A SECOND,
      INDEPENDENT WIRE MODEL FOR THE SAME `workflow_phases` ROWS" — that comment states a lockstep
      obligation you are about to NOT discharge, so you owe a written reason.
    - `backend/tests/unit/test_library_run_facts.py` — the whole file. It is this repo's model for a
      wire-exposure fence: per-handler assertions, `response_model`-drop awareness (its T-11), and
      fences over the SERIALIZED body rather than over source.
    - `backend/tests/unit/test_182_canvas_auth.py` ≈:130-160 — the `app.dependency_overrides` pattern
      for `get_user_supabase_client` / `get_current_user` / `require_canvas` on this router.
    - `backend/tests/unit/test_112_response_model_preserves_metadata.py` — the shipped precedent for
      asserting what `response_model` does and does not drop.
  </read_first>

  <files>backend/app/api/workflow_runs.py, backend/app/models/thread.py, backend/tests/unit/test_200_1_deliverable_text.py</files>

  <action>
Declare ONE field on `WorkflowRunPhaseRead`: `deliverable_text: str | None = Field(default=None, …)`.
Its description states the semantics in the file's existing voice — it is the phase's own
`output["text"]`, the answer the step produced; `None` means this step wrote no text; an empty string
is never shipped (an empty answer and no answer are not two facts worth distinguishing on a surface
whose job is to say what was produced). Name it `deliverable_text` and not `output_text` on purpose:
the name itself argues against a future `output_*` family.

In the serializer loop, parse ONCE per row: `obj = phase_output_object(row.get("output"))`, then feed
that same `obj` to `declared_phase_measure` (a dict passes straight through its unwrap) and read the
text as `obj.get("text") if isinstance(obj, dict) else None`, shipping it only when it is a non-empty
`str`. One parse, one home, both facts.

⚠ **READ EXACTLY ONE KEY BY NAME. Never iterate `obj`'s keys, never build a filtered copy, never
`.select("*")`, never put `output` on the model.** The bound is the ALLOW-LIST, and the measured reason
is in `<measurements>`: eight live keys are absent from the ROADMAP's list, so a deny-list is already
incomplete on today's data and — per this project's own Phase 185 finding — cannot be made fail-closed.
Leave the `.select()` projection at :636 byte-unchanged; it already selects `output` and nothing new is
needed.

**RECORD THE ASYMMETRY IN WRITING — it is decision `D-200.1-02-A`.** `WorkflowPhaseState` (the CHAT
panel's wire model) is DELIBERATELY NOT widened, which departs from the lockstep rule stated in
`models/thread.py`. Write the reason in BOTH places — beside the new field here, and as a short note
inside that lockstep comment in `models/thread.py` so a reader of the rule finds the exception where
the rule is: the lockstep rule's own stated purpose is *"the same facts, two surfaces, silently
disagreeing"*, and that does not apply, because **the chat surface already renders this text — it is
the assistant's message.** Adding it there would be a SECOND rendering of the same words on the same
screen, which is the exact duplication `RunTranscript`'s reason 2 removed from the run page. Timestamps
and counts were different: neither surface had them. Re-open trigger, named: *a chat-surface affordance
that needs the deliverable independently of the message stream.*

Also record the payload decision `D-200.1-02-B` beside the field: the text ships WHOLE, on every row
that has one, unclamped. Measured max 38,935 chars per row, mean 3,871, p95 15,431 — a 5-step run is
typically ~19 KB and worst-case ~195 KB, the same order as the `definition` this route already ships
whole. The alternative (populate only the final row) is a serializer rule INVISIBLE on the wire, which
is the "green DB test beside an unchanged UI" shape this route's own comment warns about; a clamp
without a second signalling field would be a silent truncation, and this surface does not lie by
omission. Re-open trigger: *the first surface that reads this field for a LIST of runs rather than one.*

Write `backend/tests/unit/test_200_1_deliverable_text.py` driving `GET /workflow-runs/{id}` through a
real `TestClient` with `app.dependency_overrides` for the auth/canvas/supabase seams and a fake
Supabase whose single phase row's `output` carries `text` PLUS planted `citations`, `source_refs`,
`field_map`, `tool_call_id`, `sub_run_id`, a prompt-shaped key, and a key name that exists nowhere in
the product today. Assert on the JSON BODY, never on the model and never on the serializer source.
  </action>

  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python.exe -m pytest tests/unit/test_200_1_deliverable_text.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - BEHAVIOR (the field reaches the client): the TestClient response body's `phases[0]` has
      `deliverable_text` equal to the planted text. ⚠ Asserted on `response.json()`, because
      `response_model` drops undeclared keys SILENTLY and only the body proves the three-place lockstep
      closed.
    - BEHAVIOR (**the leak fence, as a SET EQUALITY not a per-key absence**):
      `set(body["phases"][0].keys())` equals exactly
      `{slug, phase_index, status, phase_type, started_at, completed_at, step_count, step_noun, deliverable_text}`.
      A set equality fails on a key invented tomorrow; a list of `assert "citations" not in …` does not.
      Include per-key absence assertions for the six ROADMAP-named keys as well, so a failure NAMES the
      leaked key rather than only reporting a set mismatch.
    - BEHAVIOR (**POSITIVE CONTROL — non-vacuity**): the same test asserts the planted keys WERE present
      on the fake row before serialization (`"citations" in fake_row["output"]`, etc.). Without this the
      fence passes just as happily against a row that never carried them.
    - BEHAVIOR (the string-scalar shape is served too): a second case whose `output` is
      `json.dumps({...})` — the shape 484 of 484 `completed` rows actually have — yields the SAME
      `deliverable_text`. This is what proves 200.1-01's door is the one this plan reads through.
    - BEHAVIOR (three arms of absence, none folded): `output` absent → `deliverable_text` is `null`;
      `output` present with no `text` key → `null`; `output.text` an empty string → `null`. Never `""`,
      never a default sentence.
    - BEHAVIOR (`_measure` still works beside it): the same response carries the row's `step_count` /
      `step_noun`, so the one-parse refactor did not regress Phase 200's field pair.
    - SOURCE FENCE: over `inspect.getsource` of the phase serializer loop with comments stripped, the
      only string subscript taken on the unwrapped object is `"text"`. Include a POSITIVE CONTROL
      proving the stripper and the matcher still find a planted second key. ⚠ Strip comments first —
      this file's docblocks legitimately name `citations`, `field_map` and `prompts`, and counting them
      is the 187-24 trap this repo has hit repeatedly.
    - SOURCE FENCE: `grep -c 'select("\*")' backend/app/api/workflow_runs.py` returns `0`, and the
      `.select(` argument at the phases query is byte-identical to the base commit.
    - SOURCE: the ownership select (`.eq("id", …).eq("user_id", …)` on `workflow_runs`) is asserted
      BYTE-IDENTICAL to the base commit. No new query, no changed 404 body, no widened scope.
    - SOURCE: `D-200.1-02-A` and `D-200.1-02-B` are both greppable in `backend/app/api/workflow_runs.py`,
      and `D-200.1-02-A` is also greppable in `backend/app/models/thread.py`.
    - CLI: `cd backend && venv/Scripts/python.exe -m pytest tests/unit -q` — summary line read VERBATIM,
      no new failure against the `62 failed / 2350 passed` baseline.
  </acceptance_criteria>

  <done>
One declared field carries the answer; the exposure is bounded by reading a single named key and proved
by a set-equality assertion over the real response body with a planted-key positive control; the
string-scalar shape serves correctly; and the deliberate non-widening of the second wire model is
written down in both places with a re-open trigger.
  </done>
</task>

<task type="auto">
  <name>Task 2: Four distinct renders on the run surface, each pinned as its own case</name>

  <read_first>
    - `frontend/src/pages/WorkflowRunPage.tsx` — the whole copy block (:160-232) and the whole
      deliverable region (:1570-1660). ⚠ Read the docblock above `COPY_DELIVERABLE_HEADING` in full:
      it records that *"What this run produced"* was REPLACED because the list is THREAD-scoped and
      cannot support an authorship claim, and it records why the two empty strings are deliberately
      unchanged. You are about to change one of them; that docblock is the argument you must answer.
    - `frontend/src/pages/WorkflowRunPage.test.tsx` — the FOUR sites that reference the terminal empty
      copy: `:540` (the `COPY_EMPTY_TERMINAL` const), `:1140` (a comment), `:1441` (a comment) and
      **`:2047`, the source fence `expect(code.match(/This run produced no files\./g) ?? []).toHaveLength(1)`.**
      All four move together or the suite reds.
    - `frontend/src/lib/api.ts` ≈:4200-4310 — `WorkflowRunPhase` and `WorkflowRunRead`.
    - `docs/HOT-FILE-LEDGER.md` → `frontend/src/pages/WorkflowRunPage.tsx`, and the
      `frontend/src/lib/api.ts` sub-section recording the 197 DECLINE and its re-open trigger.
  </read_first>

  <files>frontend/src/lib/api.ts, frontend/src/pages/WorkflowRunPage.tsx, frontend/src/pages/WorkflowRunPage.test.tsx</files>

  <action>
Mirror the field on the client: add `deliverable_text?: string | null` to `WorkflowRunPhase` in
`frontend/src/lib/api.ts`, with a doc comment carrying the same semantics as the server description.
⚠ **A TYPE ONLY — no runtime export.** That is what keeps the 197 decline on this file intact and what
makes `196-08`'s mock-factory failure mode (suites throwing at mount because a `vi.mock("@/lib/api")`
factory did not declare a new export) structurally unable to fire. Prove it by grep over the real diff,
not by quoting this sentence.

In `WorkflowRunPage.tsx`, derive the run's answer as **the LAST row in server order carrying a
non-empty `deliverable_text`** — not strictly the final row. Record the reason in a docblock: a run
whose closing step emits a file while the step before it wrote the prose would otherwise be reported as
having produced no answer. It is presented as THE RUN's answer, never as any step's — consistent with
this region's shipped refusal to pass a `deliverableOf` and claim which step made what.

Restructure the region into FOUR renders. **None may be folded into another** — this is the
`runFacts` / `DecisionsList` / `phaseDuration` finding for the fifth time, and folding *"no files"*
into *"nothing at all"* prints *"nothing happened"* about a run that produced an answer:

  · **file(s)** — files present, no answer → TODAY'S render, byte-identical: the existing heading and
    the existing `FileRow` list. Assert the byte-identity rather than eyeballing it.
  · **text only** — no files, an answer → a new `COPY_ANSWER_HEADING` naming the answer as the
    deliverable, and the text beneath it. ⚠ The file heading and the *"produced no files"* sentence
    must NOT render in this arm; that sentence is true and useless here, and rendering it beside an
    answer is the fold this criterion forbids.
  · **both** — files AND an answer → both regions, **the file list FIRST** (it is the artefact),
    the answer beneath it.
  · **genuinely nothing** — terminal, neither → a RE-WORDED sentence. It is no longer the only
    absence, so it must stop being about files alone: it should say the run finished without producing
    a file OR a written answer, in this file's existing plain voice.

Keep `COPY_NO_FILES_LIVE` for the live-and-still-empty case and say in a comment that it was
deliberately left: the ROADMAP names only the terminal arm, a live run may still write either kind of
thing, and re-wording it is a change nobody asked for. Keep the `filesLoading ? null` guard — claiming
an absence before the first read lands is a lie with a short lifetime.

⚠ **RENDER THE ANSWER AS A REACT TEXT NODE.** It is model-authored content. `dangerouslySetInnerHTML`
appears nowhere; the block uses `whitespace-pre-wrap break-words` so a long answer reads and wraps, and
the region's existing `md:max-h-[220px] overflow-auto` already scrolls it.

⚠ **THE OLD SENTENCE IS SUPERSEDED, NEVER DELETED.** Rewrite the `:2047` source fence IN PLACE to pin
the NEW sentence at exactly one occurrence, keep the old string quoted verbatim inside the test's
docstring under an explicit `SUPERSEDED` marker with the reason (so `git log -S` still finds the
thread), and update `:540`, `:1140` and `:1441` in the same commit. Do the same in
`WorkflowRunPage.tsx`'s own copy docblock at :212, which quotes the old sentence. ⚠ Check whether the
`:2047` fence reads comment-stripped source or raw `?raw` before you change any prose — if it is raw,
the docblock quotation you are adding will itself change the count, which is the trap that has fired
twice in this tree in one phase.

Pin each of the four arms as its OWN case in `WorkflowRunPage.test.tsx`.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/pages/WorkflowRunPage.test.tsx --reporter=basic</automated>
    <human-check>
Walk the four arms in a browser at `http://localhost:5173`, one run each, and confirm the four renders
differ and none reads as another:
  · both              `f4c8d111-e2fa-4f00-9936-7d249d396993`  — file list, then the answer beneath it
  · text only         `13691156-773e-4c41-b301-f25369b770d7`  — the answer, and NO "produced no files"
  · genuinely nothing `c353ff78-1b7e-45c2-a5f8-2b10d9c3b77c`  — the re-worded sentence
  · file only         re-derive one with the arm query — must look exactly as it does today
Paste the sentence each arm rendered. ⚠ Re-derive the run ids first; these were measured 2026-08-20 and
this repo's own repeated finding is that a figure written at a plan's close goes stale on the next
commit.
    </human-check>
  </verify>

  <acceptance_criteria>
    - BEHAVIOR (text only): with files `[]`, a terminal run, and a phase carrying `deliverable_text`,
      the region renders the answer text and `COPY_ANSWER_HEADING`; `screen.queryByText("This run produced no files.")`
      (the SUPERSEDED string) is `null` AND the new genuinely-nothing sentence is `null`.
    - BEHAVIOR (file only): with one file and no `deliverable_text`, the rendered region's `innerHTML`
      is asserted equal to the base-commit render for the same fixture. Today's arm is UNCHANGED and is
      proved so, not assumed.
    - BEHAVIOR (both): with one file AND an answer, both are present, and the file list's DOM position
      precedes the answer block's — asserted with `compareDocumentPosition` or an index comparison over
      the region's children, never by reading the source order.
    - BEHAVIOR (genuinely nothing): terminal, no files, no answer → the NEW sentence renders, and it
      mentions neither only-files nor only-text. The other three arms' distinguishing text is absent.
    - BEHAVIOR (four renders, none folded): a single test asserts the four arms produce four DISTINCT
      rendered `textContent` values — `new Set([...]).size === 4`. This is the criterion that catches a
      fold directly rather than by inference.
    - BEHAVIOR (live and empty is unchanged): non-terminal, no files, no answer → still
      `COPY_NO_FILES_LIVE`; and with `filesLoading` true, the region claims NEITHER absence.
    - BEHAVIOR (the answer's derivation): a run whose LAST row has no text but whose second-to-last
      does renders that answer — the case the "last non-empty" rule exists for.
    - SOURCE: `grep -c "dangerouslySetInnerHTML" frontend/src/pages/WorkflowRunPage.tsx` returns `0`.
    - SOURCE (the 197 decline holds): `git diff <base> HEAD -- frontend/src/lib/api.ts` adds ZERO lines
      matching `^\+export (const|function|class|let|var) ` — measured over the real diff. If the count
      is non-zero the decline's trigger has FIRED and it must be recorded in Task 3's ledger edit.
    - SOURCE (the superseded string is findable): the old sentence appears exactly once in
      `WorkflowRunPage.test.tsx`, inside a docstring under a `SUPERSEDED` marker, and the rewritten
      `:2047` fence pins the NEW sentence at exactly one occurrence.
    - CLI: `cd frontend && npx tsc -p tsconfig.app.json --noEmit` — at or better than the 33-error /
      19-file baseline. ⚠ The `-p` is load-bearing; a bare `--noEmit` checks ZERO files.
    - CLI: `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the REPO ROOT — verdict
      line read VERBATIM, no per-file decrease, `failed 0`, every increment attributed. ⚠ If it reds,
      capture failing FILENAMES from the gate's persisted JSON BEFORE re-running and check each against
      `git diff --numstat`; do not touch the cap.
  </acceptance_criteria>

  <done>
Four renders exist and are proved distinct by a set-size assertion; the file-only arm is proved
byte-identical to today; the superseded sentence is quoted rather than erased and all four of its
reference sites moved together; the client field is a type with no runtime export.
  </done>
</task>

<task type="auto">
  <name>Task 3: The phase's hot-file ledger, re-derived — and the closing gate sweep</name>

  <read_first>
    - `CLAUDE.md` § the hot-file ledger — the re-derivation recipe, the SAME-COMMIT SYNC RULE, and the
      note recording that FIVE rows were found stale in one pass with THREE of them reading `satisfied`.
      A row that is present and WRONG answers the auditor with `satisfied` and STOPS the audit, which is
      worse than an absent row.
    - `docs/HOT-FILE-LEDGER.md` — the existing sections for `backend/app/api/workflow_runs.py`,
      `backend/app/db/workflows.py`, `frontend/src/pages/WorkflowRunPage.tsx` and the
      `frontend/src/lib/api.ts` sub-section with its 197 decline and re-open trigger.
    - `.planning/phases/200.1-the-run-says-what-it-produced-insert/200.1-01-SUMMARY.md` and
      `200.1-03-SUMMARY.md` — the triples those plans already derived and the final `wc -l` of
      `RunTranscript.tsx`. Confirm by re-deriving; do not inherit.
  </read_first>

  <files>CLAUDE.md, docs/HOT-FILE-LEDGER.md</files>

  <action>
Re-derive the ledger triple for EVERY non-test source file this phase touched, in ONE scripted pass
using `CLAUDE.md`'s own recipe (`git log --oneline | wc -l` · the phase-bucket `sed` pipeline, with
six-digit dated quick-task buckets subtracted · `wc -l`). Do not read the values off any table, and do
not inherit a figure from a SUMMARY. The eight files:

    backend/app/models/thread.py                              (row + section CREATED by 200.1-01 — confirm)
    backend/app/db/workflows.py                               (updated by 200.1-01 — confirm)
    backend/app/api/workflow_runs.py                          (this plan moved it)
    frontend/src/lib/api.ts                                   (this plan moved it)
    frontend/src/pages/WorkflowRunPage.tsx                    (this plan moved it)
    frontend/src/components/workflows/RunTranscript.tsx       (200.1-03 moved it)
    frontend/src/components/workflows/transcriptVocabulary.ts (untouched by this phase — see below)
    frontend/src/components/workflows/phaseDuration.ts        (only if this phase touched it; skip if not)

⚠ **TWO ROWS WERE ALREADY STALE AT THIS PHASE'S BASE COMMIT and the staleness is the finding, not a
tidy-up.** The planner measured at `e0c57ef4`: `frontend/src/lib/api.ts` at **177 / 102 / 6521** while
its row reads `177 / 100 / 6521` — **the PHASE count was wrong by two before this phase started**; and
`transcriptVocabulary.ts` at **3 / 1 / 131** while its row reads `1 / 1 / 105`. Correct both and record
BESIDE the old figures, never over them — that is this ledger's own habit and the only way the drift
stays visible.

For each file, update the CLAUDE.md table row AND the `docs/HOT-FILE-LEDGER.md` section **in this one
commit** (the same-commit sync rule is row↔section). A file whose triple fires G-5 (≥3 phases) and has
no section gets one. State the G-5 verdict EXPLICITLY for every firing file — either take a named seam
or write *honoured by construction* WITH ITS MEASURED REASON. Do not leave a verdict implied:

  · `api/workflow_runs.py` — honoured by construction: **the three-place lockstep** (projection · model ·
    serializer) is this row's named invariant and this phase moved the model and the serializer of a
    projection that already selected the column. Record the NEW invariant it now carries: **read exactly
    one key by name, never a filtered copy and never a deny-list** — with the measured reason that
    eight live `output` keys are absent from the ROADMAP's list.
  · `WorkflowRunPage.tsx` — the FOUR seams named at Phase 195 remain and none was taken here; say so
    rather than letting `honoured` imply otherwise. Carry the re-open trigger forward VERBATIM.
  · `lib/api.ts` — state whether the 197 decline's trigger FIRED, using Task 2's measured grep over the
    real diff. A type-only addition does not fire it; a runtime export does. Say which happened.
  · `RunTranscript.tsx` — record the ported gutter, the live treatment, and that BOTH shipped refusals
    were asserted as still holding, one against a test that passed UNEDITED.
  · `models/thread.py` — confirm 200.1-01's new row and section, and add this plan's own note: it is
    now the home of the ONE read-side unwrap AND of the recorded exception to its own lockstep rule.

Finish with the CLAUDE.md size gate and the phase's closing gate sweep.
  </action>

  <verify>
    <automated>node scripts/check-claude-md-size.cjs</automated>
  </verify>

  <acceptance_criteria>
    - CLI: every touched source file has a row in `CLAUDE.md`'s hot-file table — verified by a loop that
      greps each path and asserts at least one hit. Zero hits for any of them is a FAILURE, not a note.
    - CLI: every G-5-firing touched file has a `## ` or `### ` section in `docs/HOT-FILE-LEDGER.md` —
      verified the same way.
    - CLI: `git show --stat HEAD` for this commit lists BOTH `CLAUDE.md` and `docs/HOT-FILE-LEDGER.md`.
      A commit carrying only one of them is drift by this rule's own definition.
    - SOURCE: for `lib/api.ts` and `transcriptVocabulary.ts`, BOTH the previous figure and the
      re-derived one are present in the section text — recorded beside, never over.
    - SOURCE: every G-5-firing touched file's row carries an explicit verdict string (a named seam, or
      `honoured by construction` followed by a reason). Assert none reads bare `satisfied` with no
      successor named.
    - CLI: `node scripts/check-claude-md-size.cjs` exits `0`. Record the character count in the SUMMARY
      against the 120,000 warn band.
    - CLI (phase close, all read VERBATIM, none summarised):
      · `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the REPO ROOT — no per-file
        decrease, `failed 0`. Baseline at `e0c57ef4`: total 5365 · pinned 5004 · 110/110.
      · `cd frontend && npx tsc -p tsconfig.app.json --noEmit` — at or better than 33 errors / 19 files.
      · `cd backend && venv/Scripts/python.exe -m pytest tests/unit -q` — no new failure against
        `62 failed / 2350 passed`.
      · `git diff --stat <phase base> HEAD -- supabase/migrations` is EMPTY and
        `ls supabase/migrations | wc -l` is unchanged — **the phase chose no migration and proves it.**
        ⚠ Migration `123_repair_definition_snapshot_string_scalars.sql` is recorded as WRITTEN BUT NOT
        APPLIED; this phase neither applies nor renumbers it. Say so in the SUMMARY so the next reader
        does not mistake the silence for it having been handled.
  </acceptance_criteria>

  <done>
Every source file the phase touched has a current, re-derived row AND section landed in one commit;
the two rows that were stale before the phase began are corrected beside their old figures; every
firing file carries an explicit verdict; and all four phase gates are read verbatim into the SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `workflow_phases.output` jsonb → HTTP response body | **The new boundary.** A column that has never been on the wire now has one key on it. `_persist_output` stores each executor's dict FULL AND INLINE, so this jsonb holds retrieval citations, internal sub-run ids, field maps and prompts. |
| authenticated caller → another tenant's run | These run feeds are ownership-gated in the route; `db/workflows.py`'s library laterals run on a pool that BYPASSES RLS, where the predicate IS the access boundary. |
| model-authored `output.text` → the browser DOM | The answer is LLM-generated content rendered on a page the run's owner is reading. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-200.1-09 | Information Disclosure | `WorkflowRunPhaseRead` / the phase serializer | mitigate | **Allow-list, not deny-list.** Read exactly `obj.get("text")` by name; never iterate keys, never build a filtered copy, never `.select("*")`, never declare `output`. Proved by a SET-EQUALITY assertion over the real `response.json()` phase object with a planted `citations` / `source_refs` / `field_map` / `tool_call_id` / `sub_run_id` / prompt-shaped key / never-before-seen key, plus a non-vacuity positive control asserting those keys WERE on the row. A set equality also fails on a key invented after this plan ships; a per-key deny-list does not. **The deny-list is already incomplete on live data** — eight keys present on real rows today are absent from the ROADMAP's list of six. |
| T-200.1-10 | Information Disclosure | the phases query in `get_workflow_run` | mitigate | No new query and no widened scope. The `.select()` projection at `:636` is byte-unchanged (it already selected `output`), and the ownership select — `.eq("id", …).eq("user_id", …)` on the SAME statement, which is what makes a foreign id indistinguishable from a nonexistent one — is asserted BYTE-IDENTICAL to the base commit. The phases query is already `.eq("workflow_run_id", run["id"])` where `run` came from that ownership select. |
| T-200.1-11 | Information Disclosure | the CHAT surface's `WorkflowPhaseState` | mitigate | Deliberately NOT widened (`D-200.1-02-A`). `GET /threads/{id}/workflow` is the UNGATED route of the two — it carries no `require_canvas`. Declining to add the field there means this phase's new exposure exists only behind the canvas-gated run read, which is strictly the narrower door. Asserted: `git diff` adds no field to `WorkflowPhaseState`. |
| T-200.1-12 | Tampering | the answer rendered in `WorkflowRunPage.tsx` | mitigate | Rendered as a React text node with `whitespace-pre-wrap break-words`; `dangerouslySetInnerHTML` asserted absent from the file by grep. Model-authored markup cannot become DOM. |
| T-200.1-13 | Denial of Service | run-read payload size | accept | Measured: max 38,935 chars per row, mean 3,871, p95 15,431; a typical 5-step run ships ~19 KB and a worst case ~195 KB — the same order as the `definition` this route already ships whole (`D-200.1-02-B`). Accepted with a NAMED trigger: the first surface reading this field for a LIST of runs rather than one. |
| T-200.1-14 | Repudiation | the four-arm render | accept | The region makes no authorship claim about WHICH step produced what — the answer is presented as the RUN's, matching this region's shipped refusal to pass a `deliverableOf`. No audit record is added or needed; nothing here is an action. |
| T-200.1-SC | Tampering | npm/pip/cargo installs | mitigate | **This plan installs no package.** `frontend/package.json`, `package-lock.json` and `backend/requirements*.txt` are asserted byte-unchanged by `git diff --numstat`. The Package Legitimacy Gate does not apply and no `[ASSUMED]`/`[SUS]` blocking checkpoint is owed. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python.exe -m pytest tests/unit -q` — no new failure vs `62 failed / 2350 passed`.
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the REPO ROOT — verdict line
  verbatim, no per-file decrease, `failed 0`, every increment attributed.
- `cd frontend && npx tsc -p tsconfig.app.json --noEmit` — at or better than 33 errors / 19 files.
- `git diff --stat <phase base> HEAD -- supabase/migrations` EMPTY; `ls supabase/migrations | wc -l`
  unchanged.
- `node scripts/check-claude-md-size.cjs` exits `0`.
- The Task 2 `<human-check>` four-arm walk is completed and the four rendered sentences are pasted.
</verification>

<success_criteria>
- Four distinct renders exist, are pinned individually, and are proved distinct by a set-size assertion.
- The file-only arm is proved byte-identical to today's render.
- The answer reaches the client through ONE declared field, with the leak fence a set equality over the
  real response body and a planted-key positive control.
- The string-scalar row shape serves the answer correctly — the door `200.1-01` repaired is the one this
  plan reads through, asserted.
- `D-200.1-02-A` (the second wire model deliberately not widened) and `D-200.1-02-B` (unclamped payload)
  are written in the source with reasons and triggers.
- Every touched source file has a re-derived CLAUDE.md row AND a `docs/HOT-FILE-LEDGER.md` section,
  landed in ONE commit, with both pre-existing stale rows corrected beside their old figures.
- No migration; the CLAUDE.md gate is clear.
</success_criteria>

<output>
Create `.planning/phases/200.1-the-run-says-what-it-produced-insert/200.1-02-SUMMARY.md` when done.
Record VERBATIM: the four gate readings, the four rendered arm sentences from the human check, the eight
re-derived ledger triples with their previous values beside them, whether `lib/api.ts`'s 197-decline
trigger fired, and the CLAUDE.md character count against the 120,000 warn band.
</output>
</content>
