---
phase: 200.2-the-run-column-stops-repeating-itself-insert
plan: 03
type: execute
wave: 2
depends_on: ["200.2-01"]
files_modified:
  - frontend/src/components/workflows/RunHero.tsx
  - frontend/src/components/workflows/RunHero.test.tsx
  - frontend/src/components/workflows/runColumnVocabulary.ts
  - .planning/reported-bugs/BUG-260823-04-run-answer-rule-picks-llm-emit-status-line.md
autonomous: true
requirements: [RUN-05]
tags: [RUN-05, SC1, D-01, D-02, D-03, D-04, D-13, D-14, D-15, D-16, D-17, A-07, R-4, operator-decision-2, type-totality, superseded-not-deleted]
must_haves:
  truths:
    - "The hero renders the deliverable by TYPE — file / answer / both / neither — re-presenting the four 200.1-02 arms at the TOP of the centre column (D-01, SC#1)"
    - "While the run is LIVE the hero slot renders nothing at all — no card, no placeholder, no reserved height (D-03)"
    - "The empty hero states the run's own terminal fact, SPLIT by outcome: a failed run names the step it stopped at; a completed-but-empty run says it finished without writing; a cancelled run is never told it 'finished' (D-13 / D-16)"
    - "The empty-hero switch is TOTAL over every terminal run status with a type-level exhaustiveness check — an unknown status never reads as success (D-16)"
    - "The hero's heading is TYPED by deliverable kind, so it never claims authorship of a thread-scoped file (A-07 over D-14)"
    - "The file arm heroes the NEWEST file only; the rest stay a plain list beneath it (D-15)"
    - "The answer arm reuses the existing runAnswer memo's result and the shipped MarkdownRenderer — it does NOT re-derive the selection per-step and does NOT repair the D-17 rule (operator decision 2: the rule stays; the defect is RECORDED as a reported bug)"
    - "The hero is not a coloured band — prominence comes from weight, size, spacing and the card (R-4)"
    - "COPY_NO_FILES_LIVE is retired for the live arm, superseded-not-deleted: its one-occurrence source fence moves to the new live-absence reading (D-03)"
  artifacts:
    - path: frontend/src/components/workflows/RunHero.tsx
      provides: "the hero region — one slot the run fills by deliverable type"
      contains: "RunHero"
    - path: frontend/src/components/workflows/RunHero.test.tsx
      provides: "the four arms' distinct textContents, the D-16 totality cases, the live-absence case, the heading-typing cases"
    - path: .planning/reported-bugs/BUG-260823-04-run-answer-rule-picks-llm-emit-status-line.md
      provides: "the D-17 defect recorded per the operator's decision — the shipped rule stays untouched"
    - path: frontend/src/components/workflows/runColumnVocabulary.ts
      provides: "the hero's typed headings and the D-13/D-16 terminal sentences — ONE home, extended in the same commit (plan 02 creates the file; this plan may add entries)"
  key_links:
    - from: frontend/src/components/workflows/RunHero.tsx
      to: frontend/src/components/workflows/phaseDuration.ts
      via: "runSpan + runClock for the completed-in meta line (the ONE duration home — never a subtraction here)"
      pattern: "runSpan"
    - from: frontend/src/components/workflows/RunHero.tsx
      to: frontend/src/components/files/FileRow.tsx
      via: "the file arm's rows — RUN-03 forbids rebuilding output-file UI"
      pattern: "FileRow"
    - from: frontend/src/components/workflows/RunHero.tsx
      to: frontend/src/components/chat/MarkdownRenderer.tsx
      via: "the answer body — the shipped DOMPurify path, max-w-[72ch] break-words"
      pattern: "MarkdownRenderer"
---

<objective>
The hero region — the thing the run produced, leading the centre column.

Purpose: the run surface's centre column leads with the deliverable, chosen by what the run
actually produced (SC#1). The four arms' SELECTION LOGIC already ships (200.1-02); this plan
re-presents them in a hero card at the top of the column, adds the D-13/D-16 split terminal
sentences, retires the live-arm placeholder, and records the D-17 answer-rule defect as a
reported bug per the operator's plan-time decision.

Output: `RunHero.tsx` (new), `RunHero.test.tsx` (new), vocabulary entries, the bug report.
NOT mounted by this plan — plan 04 wires it (WorkflowRunPage.tsx belongs to plan 04; two plans
editing the page in one wave is a file conflict).
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-CONTEXT.md
@.planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-RESEARCH.md
@.planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-VALIDATION.md
@.planning/sketches/202-the-run-column-rendered/README.md
@frontend/sketch/SketchRunColumn.tsx
@.planning/phases/200.1-the-run-says-what-it-produced-insert/200.1-02-SUMMARY.md
@docs/HOT-FILE-LEDGER.md

⚠ **Worktree rules:** bootstrap first, `GSD_VITEST_MAX_WORKERS=2`, never `rm -rf`.

⚠ **THIS PLAN DOES NOT EDIT WorkflowRunPage.tsx.** The hero is built as a standalone component
with its own suite; plan 04 mounts it, moves the deliverable region, and owns every page-level
fence edit (the superseded-heading zero-occurrence fence, COPY_NO_FILES_LIVE's retirement).
What THIS plan owns is the component's own logic and the bug report.

<interfaces>
The hero's inputs (all already derivable at the call site plan 04 will write):
```typescript
// From WorkflowRunPage.tsx (plan 04 passes them in — this component owns the join of NOTHING):
run: WorkflowRunRead          // .status, .phases (full WorkflowRunPhase[]), .updated_at
answer: string | null         // the EXISTING runAnswer memo's RESULT — passed in, never re-derived
                            // (D-17's defect stays in the page's rule per operator decision 2)
files: WorkspaceFile[]        // the thread-scoped list, ALREADY sorted newest-first by the page
filesLoading: boolean         // while the first read is in flight we claim NEITHER (the shipped guard)
isTerminal: boolean           // TERMINAL_RUN_STATUSES.has(run.status)
failedStepTitle: string | null // the page's existing memo — names the failing step by TITLE
onDownload: (file: WorkspaceFile) => void
titleOf: (slug: string) => string   // for the failed-step sentence when the page memo is absent
now?: number
```

From phaseDuration.ts (the ONE duration home — never subtract here):
```typescript
export function runSpan(phases: readonly PhaseTimingRow[]): { ms: number } | null  // null until something completed
export function runClock(ms: number): string
```

From the shipped file arm (RUN-03 — compose, never rebuild):
```typescript
// frontend/src/components/files/FileRow.tsx — asChild, density="run", trailing="download"|"dead"
// frontend/src/components/files/fileRowUtils.ts — baseName(file.path), formatBytes(file.size_bytes)
// The page's existing rows (WorkflowRunPage.tsx:1750-1790) are the shape to copy: an <li> per
// file, FileRow asChild wrapping a button (id-bearing) or a div (id-less, the aria-disabled span
// affordance — a shipped fence asserts ZERO buttons on that arm).
```

From the shipped answer arm: `MarkdownRenderer` with `className="max-w-[72ch] break-words"` —
the 8da83fe9 shape. ⚠ In prose: describe React's raw-HTML escape hatch, never spell the prop.

The sketch's hero (SketchRunColumn.tsx:190-315) is the acceptance bar: a rounded card,
a heading typed by kind, a meta row (the completed-in clock from runSpan+runClock), the answer
behind a left rule, the file row(s), the empty sentence. ⚠ The sketch's "Open full text" button
and its lucide Clock/FileText/ExternalLink glyphs are SKETCH CHROME and a glyph budget question
respectively — see the action's glyph rules.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The D-17 bug report — record the answer-rule defect, leave the rule</name>
  <files>.planning/reported-bugs/BUG-260823-04-run-answer-rule-picks-llm-emit-status-line.md</files>
  <read_first>
    - .planning/reported-bugs/TEMPLATE.md (the frontmatter contract — status IS the index)
    - .planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-CONTEXT.md (A-04 / D-17, verbatim)
    - frontend/src/pages/WorkflowRunPage.tsx:840-867 (runAnswer — the shipped rule, which this report records as defective and this phase does NOT touch)
  </read_first>
  <action>
    Create the report from the TEMPLATE with: `id: BUG-260823-04`, `surface: Agentic-RAG`,
    `severity: minor`, `status: open`, `affected_areas: [frontend/workflows]`,
    `folded_into: null` (⚠ NOT folded — the fix is out of scope by the operator's decision),
    `related_seeds: [SEED-191]`, and `reproduces_on` pointing at the current develop commit.

    Body, from A-04's measured facts: the shipped `runAnswer` rule (last server-ordered row with
    non-empty deliverable_text, skipping llm_human_input) picks `emit-qbr` on the real QBR run
    (`bef870ba`) — an `llm_emit` step whose entire text is the 61-character status line
    `Produced the filled deliverable: /Northwind-QBR-Template.docx` — while the sibling
    `synthesize` step holds 6,133 characters of the actual narrative. The surface then renders
    the filename announcement under "The answer this run wrote". Same class of defect as the
    llm_human_input skip, one phase type over. Hypothesized fix direction (NOT applied): keying
    the skip on `phase_type === "llm_emit"` is NOT obviously correct — an emit step whose text
    IS prose would then be wrongly skipped — so a fix requires a measured cross-row
    justification. Re-open trigger: the next phase touching the run surface's answer selection,
    or an operator report of the wrong-answer hero on another workflow.
  </action>
  <acceptance_criteria>
    - The file exists with frontmatter matching the TEMPLATE's field set exactly; `status: open` and `folded_into: null`.
    - The body quotes the 61-character status line verbatim and names both the run id and the two step texts' measured lengths.
    - The report states the fix is deliberately NOT taken in 200.2 and names the measured-justification requirement for any future fix.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "status: open" ".planning/reported-bugs/BUG-260823-04-run-answer-rule-picks-llm-emit-status-line.md"</automated>
  </verify>
  <done>The D-17 defect is recorded, routable by the reported-bugs scan, and the shipped rule is provably untouched (this task edits no source).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RunHero — the four arms, the D-16 totality, the typed heading, the live absence</name>
  <files>frontend/src/components/workflows/RunHero.tsx, frontend/src/components/workflows/RunHero.test.tsx, frontend/src/components/workflows/runColumnVocabulary.ts</files>
  <read_first>
    - frontend/sketch/SketchRunColumn.tsx:106-315 (heroFor + Hero — the acceptance bar; note isGateStep keys on phase_type from the wire)
    - frontend/src/pages/WorkflowRunPage.tsx:153-271 (the copy constants and their superseded-not-deleted fences — COPY_DELIVERABLE_HEADING, COPY_ANSWER_HEADING, COPY_NO_FILES_LIVE, COPY_NO_FILES_TERMINAL) and :1672-1859 (the region this component re-presents)
    - frontend/src/components/workflows/phaseDuration.ts (runSpan, runClock)
    - frontend/src/components/workflows/runColumnVocabulary.ts (plan 02's leaf — extend it, do not create a second home)
    - frontend/src/lib/phaseState.ts (TERMINAL_RUN_STATUSES — completed, failed, cancelled, timed_out)
    - frontend/src/components/files/FileRow.tsx + fileRowUtils.ts (the file rows — RUN-03)
    - frontend/src/components/chat/MarkdownRenderer.tsx (the answer body)
    - .planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-CONTEXT.md (D-01..D-04, D-13..D-16, A-07, R-4)
  </read_first>
  <behavior>
    - THE FOUR ARMS render four DISTINCT textContents: files+no answer (the file hero + the rest list); no files+answer (the answer hero alone — no file heading, no "produced no…" sentence beside an answer); both (file leads, answer beneath); neither terminal (the empty sentence). Assert the set of four sentences has size 4, with a positive control proving the set-size assertion collapses when two readings coincide.
    - D-03 — a LIVE run renders NO hero at all: the component returns null (or the page-level mount guards it — assert whichever this component owns; the component-level contract is: `isTerminal === false` → null, no placeholder, no reserved height).
    - D-16 TOTALITY — the empty sentence over: completed → "finished without writing…" (never names a step); failed with a failedStepTitle → names the step it stopped at; failed without → the generic stop sentence; cancelled → "was stopped", NEVER "finished"; timed_out → its own arm or the never-success fallback (pick and pin one); an UNKNOWN status (a string in none of the sets) → the never-success fallback, asserted NOT to contain any success word. The switch is exhaustive at the type level: the default arm assigns the discriminated value to a `never`-checked variable, so adding a run status is a TYPECHECK ERROR (the phaseStatusMeta / SUBSTEP_META pattern).
    - A-07 TYPED HEADING — the heading is chosen by arm: an answer arm, a file arm, and the empty arm each get their OWN heading string from the vocabulary leaf; no heading claims the run made a thread-scoped file (the file heading names WHERE the files are — the D-02 rule preserved by typing).
    - D-15 — with 3 files, the FIRST (newest, the page sorts) renders in the hero slot and the other 2 render in the list beneath; with 1 file, no remainder list renders.
    - The files-loading guard: `filesLoading === true` renders NO empty-state sentence (asserting an absence before the answer arrives is a lie with a short lifetime — the shipped three-way rule).
    - The meta row renders the completed-in clock from runSpan+runClock when a span exists and the honest "runtime not recorded" reading when it does not — never `0s`.
    - R-4 — the hero's card classes spend no accent colour on the card itself: assert the rendered className set contains no `bg-primary` / accent classes on the section element (colour is for state, and the hero is not a state).
    - The XSS property: an answer containing `<img src=x onerror=alert(1)>` renders through MarkdownRenderer and arrives SANITISED (the two shipped guards' property — no script element, no handler attribute) — the component never bypasses MarkdownRenderer for the answer.
  </behavior>
  <action>
    Extend `runColumnVocabulary.ts` (plan 02's file — one home, do not create a sibling) with
    the hero's governed strings: the three typed headings (answer / file / empty), the D-16
    terminal sentences (completed-empty; failed-with-step; failed-without; cancelled; the
    never-success fallback — five entries minimum; add timed_out if you give it its own arm),
    the meta row's two readings, and the retired-live placeholder's replacement reading if the
    live absence needs an sr-only explanation (it should not — D-03 renders NOTHING; say so in
    the leaf's docblock rather than adding a string). Keep the leaf's whole-table claim true:
    update its docblock to cover the hero entries.

    Create `frontend/src/components/workflows/RunHero.tsx` exporting `RunHero` with the props
    in the interfaces block. The composition follows the sketch's Hero: a `section` card
    (`rounded-xl border border-border bg-card/40` — the sketch's neutral card, no accent
    background), the typed heading (`font-headline text-2xl font-semibold` per the sketch), the
    meta row (runSpan → `runClock(span.ms)`; NO sources chip — the A-09 refusal is recorded in
    the docblock with the sketch's own reasoning: the sum counts reads, not sources), the
    answer behind a left rule through MarkdownRenderer, the newest file in a bordered slot,
    the remainder list beneath, and the empty sentence for the neither arm. File rows compose
    FileRow exactly as the page's shipped rows do (asChild, density="run", the id-bearing
    button / id-less div split, `aria-label="Download …"` from baseName+formatBytes) — the
    download activation stays with the caller's `onDownload`.

    The failed-step naming (D-13): the component takes `failedStepTitle: string | null` from
    the page (its existing memo) and falls back to `titleOf` over the first failed phase row
    ONLY if the memo is absent — one sentence, naming the step by TITLE, never by slug or index.

    The docblock records: D-02 (renders in BOTH Log and Shape views — the hero sits above the
    switch point, which plan 04 implements; the component itself is view-agnostic), D-14's
    supersession (A-07: a heading TYPED by deliverable kind preserves the authorship constraint
    D-14 was protecting), and the two superseded constants' fate (COPY_DELIVERABLE_HEADING /
    COPY_ANSWER_HEADING are superseded by the typed headings, not deleted — plan 04 handles the
    page-side retirement; this component's vocabulary carries the new strings). ⚠ The
    superseded strings are QUOTED in prose only inside test files and the leaf's docblock where
    the reading mode has been checked — never casually in this component's comments, and never
    spell the raw-HTML escape hatch or the icon-library name.

    Create `frontend/src/components/workflows/RunHero.test.tsx` implementing the behavior block
    — the four-arm distinct-set assertion (with its collapse control), the totality cases
    including the unknown-status never-success arm, the live-null case, the typed-heading
    cases, the D-15 newest-first split, the loading guard, the sanitisation property, and the
    no-accent-on-the-card check.
  </action>
  <acceptance_criteria>
    - `frontend/src/components/workflows/RunHero.tsx` exists, exports `RunHero`, and `grep -c "lucide" frontend/src/components/workflows/RunHero.tsx` returns 0 (the meta clock, if marked, is a text/CSS mark, not an import — or omit the mark entirely and keep the meta row text-only, which is simpler and honest).
    - The totality switch ends with a `never`-typed exhaustiveness check; `cd frontend && npx tsc -p tsconfig.app.json --noEmit` baseline unmoved.
    - `grep -c "This run produced no file and no written answer." frontend/src/components/workflows/RunHero.tsx` returns 0 — the terminal-empty sentence lives in the vocabulary leaf, is IMPORTED, and its wording is the D-13 split family (the old single sentence is superseded, not copied).
    - `cd frontend && npx vitest run src/components/workflows/RunHero.test.tsx` exits 0 with every behavior-block case present (≥ 15 cases).
    - No file in this plan's `files_modified` edits `WorkflowRunPage.tsx`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/RunHero.test.tsx src/components/workflows/runColumnVocabulary.test.ts 2>/dev/null || cd frontend && npx vitest run src/components/workflows/RunHero.test.tsx</automated>
  </verify>
  <done>The hero component exists with its suite green, the vocabulary leaf extended in the same commit, the D-17 bug filed, and no page file touched. Note: if plan 02 has not yet created the leaf (parallel wave), create it here with BOTH plans' strings and record the sequencing in the SUMMARY — the leaf must exist exactly once.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the run's wire rows → the hero's answer | model-authored markdown crosses here; the shipped sanitisation path is mandatory |
| the thread-scoped file list → the hero's file arm | files this run did NOT write can appear (1-in-61 measured); the heading's typing is the guard against claiming them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-200.2-04 | Tampering (XSS) | the answer body | mitigate | MarkdownRenderer (DOMPurify) only; the suite plants a handler-bearing img and asserts sanitised output; no raw-HTML prop (described, never spelled, in prose) |
| T-200.2-05 | Tampering (of meaning) | the hero meta row | mitigate | A-09: no run-level sources aggregate (the sum counts reads, not sources) — asserted absent; runSpan+runClock is the ONE duration path, never a local subtraction |
| T-200.2-09 | Tampering (of meaning) | the empty-hero sentence | mitigate | D-16 type-level totality; a cancelled run never reads as finished; an unknown status hits the never-success fallback (asserted) |
| T-200.2-10 | Tampering (of meaning) | the file heading | mitigate | A-07 typing preserves D-02's authorship rule — the file heading names WHERE files are, never what the run made; the suite sweeps the heading against the shipped `/this run (produced|made|created)/i` pattern (the Phase-195 fence, re-aimed at the new heading home) |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows/RunHero.test.tsx` — green.
- `cd frontend && npx tsc -p tsconfig.app.json --noEmit` — baseline 33 / 19 unmoved.
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` — verdict line read verbatim; deltas attributed.
- The bug report exists and the reported-bugs scan will find it (frontmatter `status: open`).
</verification>

<success_criteria>
- The four arms render distinctly in the hero shape, the D-16 split terminal sentences exist with type-level totality, the live arm renders nothing, and D-17 is recorded as a bug with the rule untouched.
- No page file is edited by this plan.
</success_criteria>

<output>
Create `.planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-03-SUMMARY.md` when done.
</output>
