---
phase: 200.2-the-run-column-stops-repeating-itself-insert
plan: 03
type: execute
completed_at: 2026-08-23
files_modified:
  - frontend/src/components/workflows/RunHero.tsx
  - frontend/src/components/workflows/RunHero.test.tsx
  - frontend/src/components/workflows/runColumnVocabulary.ts
  - .planning/reported-bugs/BUG-260823-04-run-answer-rule-picks-llm-emit-status-line.md
requirements: [RUN-05]
tags: [RUN-05, SC1, D-01, D-02, D-03, D-04, D-13, D-14, D-15, D-16, D-17, A-07, R-4, operator-decision-2, type-totality, superseded-not-deleted]
---

# Phase 200.2 Plan 03 Summary: RunHero Component & D-17 Bug Report

## One-Liner
Built `RunHero.tsx` (top-positioned deliverable hero supporting file, answer, both, and D-16 split empty terminal states), filed bug `BUG-260823-04`, and created comprehensive unit tests.

---

## What Was Done

1. **D-17 Defect Filing (`.planning/reported-bugs/BUG-260823-04-run-answer-rule-picks-llm-emit-status-line.md`)**:
   - Recorded `BUG-260823-04` documenting that `runAnswer` picks the 61-character `llm_emit` status line over the 6,133-character substantive synthesis on run `bef870ba`.
   - Preserved existing rule in `WorkflowRunPage.tsx` without modification per operator decision 2.

2. **`RunHero.tsx` Component**:
   - Re-presents the four deliverable arms at the top of the centre column:
     - **File arm**: typed heading (`Generated file` / `Generated files`), newest file in hero slot (`files[0]`), remainder list beneath (D-15).
     - **Answer arm**: typed heading (`Generated answer`), Markdown body via `MarkdownRenderer`.
     - **Both arm**: typed heading (`Deliverables`), newest file + remainder list + Markdown body.
     - **Neither arm**: empty terminal state with D-16 totality over terminal run statuses (`completed`, `failed`, `cancelled`, `timed_out`, `never`-checked fallback).
   - Live runs (`!isTerminal`) and loading states (`filesLoading`) return `null` without reserving height (D-03).
   - Meta row formats run span via `fmtElapsed` or renders honest `Runtime not recorded` (never `0s`).
   - Neutral card styling without accent background classes (R-4).
   - Zero Lucide imports.

3. **Unit Test Suite (`RunHero.test.tsx`)**:
   - 16 test cases covering live run absence, 4 distinct deliverable arms (asserted set size = 4), D-16 totality across all terminal statuses, D-15 newest-first file ranking, loading guards, and source sweeps.

---

## Test Verification

- `npx vitest run src/components/workflows/RunHero.test.tsx src/components/workflows/RunStepList.test.tsx`: **33 passed (16 RunHero + 17 RunStepList)**.
- `WorkflowRunPage.tsx` remained strictly untouched during this plan.
