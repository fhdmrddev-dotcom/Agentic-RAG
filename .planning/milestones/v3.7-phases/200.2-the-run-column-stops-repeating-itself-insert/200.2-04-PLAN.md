---
phase: 200.2-the-run-column-stops-repeating-itself-insert
plan: 04
type: execute
wave: 3
depends_on: ["200.2-02", "200.2-03"]
files_modified:
  - frontend/src/pages/WorkflowRunPage.tsx
  - frontend/src/pages/WorkflowRunPage.test.tsx
  - frontend/src/components/workflows/RunSpine.tsx
  - frontend/src/components/workflows/RunSpine.test.tsx
autonomous: true
requirements: [RUN-05]
tags: [RUN-05, SC1, SC2, D-01, D-02, D-03, D-04, D-05, A-02, A-07, superseded-not-deleted, hot-file, G-5-by-construction]
must_haves:
  truths:
    - "The centre column's DOM order is hero → totals strip → step list, in BOTH the Log and the Shape views (D-01 / D-02 / D-04)"
    - "The old deliverable region at the page's foot is GONE, and COPY_NO_FILES_LIVE is retired for the live arm with its one-occurrence source fence re-aimed (D-03)"
    - "RunSpine's count sub-line is REMOVED — the spine renders name + status face + duration and nothing else (D-05 / A-02, load-bearing)"
    - "RunTranscript's mount is replaced by RunStepList's; the RunTranscript import is gone from the page"
    - "While the run is live the hero slot renders nothing at all (D-03)"
    - "COPY_DELIVERABLE_HEADING and COPY_ANSWER_HEADING are superseded by the typed headings, not deleted — each keeps a quotable thread (D-14 / A-07)"
  artifacts:
    - path: frontend/src/pages/WorkflowRunPage.tsx
      provides: "the re-composed centre column — the hero at the top, the strip, the step-card list"
    - path: frontend/src/components/workflows/RunSpine.tsx
      provides: "the spine without its count sub-line — the WHEN / HOW LONG column"
  key_links:
    - from: frontend/src/pages/WorkflowRunPage.tsx
      to: frontend/src/components/workflows/RunHero.tsx
      via: "the hero mount, above the centreView switch, in both views"
      pattern: "<RunHero"
    - from: frontend/src/pages/WorkflowRunPage.tsx
      to: frontend/src/components/workflows/RunStepList.tsx
      via: "the Log view's list, consuming run.phases (NOT wireRows)"
      pattern: "<RunStepList"
    - from: frontend/src/pages/WorkflowRunPage.tsx
      to: frontend/src/components/workflows/RunTranscript.tsx
      via: "REMOVED — the mount and the import are deleted"
      pattern: "RunTranscript"
---

<objective>
Wire the phase together on the page: hero at the top, step-card list in the Log view, the spine's
count sub-line removed, the old deliverable region retired.

Purpose: this is where the two columns stop repeating each other (SC#4's mechanical half — the
human judgement is plan 06). A-02 makes the spine-count removal LOAD-BEARING: the centre's yield
without the spine's count would leave the centre a strict subset of the spine — the original
defect in a new shape.

Output: the re-composed `WorkflowRunPage.tsx`, the thinned `RunSpine.tsx`, both test files updated.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-CONTEXT.md
@.planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-RESEARCH.md
@.planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-VALIDATION.md
@.planning/sketches/202-the-run-column-rendered/README.md
@frontend/sketch/SketchRunColumn.tsx
@docs/HOT-FILE-LEDGER.md

⚠ **Worktree rules:** bootstrap first, `GSD_VITEST_MAX_WORKERS=2`, never `rm -rf`.

⚠ **G-5 FIRES on `WorkflowRunPage.tsx`** (24 / 7 / 1830 at 200.1's close — re-derive before
editing; the ledger's own most-repeated finding is that the figure is stale by the time you read
it). The verdict this phase records: **honoured by construction** — the deliverable region MOVES
(the Phase-195 seam "the deliverable list" is being relocated, not grown), the new content lands
in new leaves (`RunHero.tsx`, `RunStepList.tsx`, `runColumnVocabulary.ts`), and the page's own
additions are the two mounts plus prop threading. The ledger row itself is updated by plan 05
(the phase's single writer for CLAUDE.md / docs/HOT-FILE-LEDGER.md). Do NOT edit those documents
in this plan.

⚠ **THE 187-24 TRAP, SEVEN FIRINGS, TWICE ON THIS FILE.** Before writing ANY new comment in
`WorkflowRunPage.tsx`, read each fence's source in `WorkflowRunPage.test.tsx` to learn its
reading mode: the empty-state fence reads comment-stripped `codeOf(pageSource)`, while the
raw-HTML-prop fence and the four-identifier fences read RAW `?raw` source — comments included.
Describe forbidden tokens; never spell them. The same discipline for `RunSpine.tsx` if its suite
carries sweeps (check before writing).

<interfaces>
The page facts this plan works with (all verified at the current tree):
- Region 3, the centre (`:1530-1577`): `centreView === "canvas"` → `WorkflowCanvas`; else → the `RunReceipt variant="summary"` strip then `RunTranscript`.
- Region 5, the deliverables section (`:1677-1859`): the four-arm region at the page's foot — MOVES OUT entirely.
- `runAnswer` memo (`:840-867`): UNTOUCHED (operator decision 2 / BUG-260823-04).
- `orderedFiles` (`:601`): already newest-first; the hero takes `orderedFiles` and derives `[first, ...rest]` — or RunHero takes the array and does it internally (check plan 03's prop contract and pass what it asks for).
- `wireRows` (`:807`): typed `PhaseTimingRow[]` — the strip and the spine keep consuming it; the NEW list consumes `run.phases` (the full model).
- `runState` / `runStateBySlug` (`:904-1005`): the already-worded live reading — `liveOf` for both the spine and the new list. ⚠ A-09 near-miss 2: the page ALREADY passes it to both shipped components; keep passing it (no `liveOf` → five `?` glyphs on a finished run).
- `isTerminal` (`:902`), `failedStepTitle` (`:1214-1217`), `titleOf` (`:1060-1063`), `nowMs` — all in scope; hand them to the hero.
- `COPY_NO_FILES_LIVE` (`:256`) — retired; `COPY_DELIVERABLE_HEADING` (`:235`) and `COPY_ANSWER_HEADING` (`:250`) — superseded.

The components being mounted (plans 02/03 — read their prop contracts in their source):
```typescript
// RunHero — plan 03:
<RunHero run={run} answer={runAnswer} files={orderedFiles} filesLoading={filesLoading}
         isTerminal={isTerminal} failedStepTitle={failedStepTitle}
         onDownload={onDownload} titleOf={titleOf} now={nowMs} />
// RunStepList — plan 02 (verify the exact prop names in the built component before wiring):
<RunStepList phases={run?.phases ?? []} titleOf={titleOf} runId={runId}
             runStatus={runStatus} liveOf={runState} now={nowMs} />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The spine loses its count sub-line (D-05 / A-02 — the load-bearing half)</name>
  <files>frontend/src/components/workflows/RunSpine.tsx, frontend/src/components/workflows/RunSpine.test.tsx</files>
  <read_first>
    - frontend/src/components/workflows/RunSpine.tsx (the whole file; the count sub-line at :354-363 and the docblock's no-count rule)
    - frontend/src/components/workflows/RunSpine.test.tsx (the spine-count pins at :76-91 — these UPDATE)
    - .planning/sketches/202-the-run-column-rendered/README.md (A-02 — why this is load-bearing, with the two-column table)
    - .planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-CONTEXT.md (D-05, A-02)
  </read_first>
  <action>
    Delete the `spine-count` block from `RunSpine.tsx` (the `{count && (...)}` span rendering
    `countDeclared(count.count, count.noun)`, :356-363) and the `declaredCount` import/call that
    fed it (keep `countDeclared` only if something else in the file still uses it — check). The
    sub-line's docblock rule ("a step that declared no count gets no sub-line at all") is
    AMENDED BESIDE the original, never overwritten: the whole sub-line is gone by D-05/A-02 —
    **the centre says WHAT each step yielded; the spine says WHEN and HOW LONG** — and the
    paragraph names the A-02 consequence: shipping the centre yield without this removal ships
    the centre as a strict subset of the spine, which is the defect the phase exists to fix.

    Update `RunSpine.test.tsx`: the cases pinning `spine-count` (three sites at :76-91) INVERT to
    assert the testid is ABSENT everywhere (a finished count-bearing row renders NO count
    sub-line — the strongest form asserts `queryByTestId("spine-count")` is null on a row whose
    `step_count` is 15). Update the suite's fixture-driven expectations for any row-height or
    text-content assertions the removal disturbs. Keep every other pin untouched — the ask-row,
    the ring faces, the durations, the rail.
  </action>
  <acceptance_criteria>
    - `grep -c "spine-count" frontend/src/components/workflows/RunSpine.tsx` returns 0.
    - `grep -c "declaredCount" frontend/src/components/workflows/RunSpine.tsx` returns 0 (no dead import — `noUnusedLocals` would fail otherwise).
    - `cd frontend && npx vitest run src/components/workflows/RunSpine.test.tsx` exits 0 with the inverted count assertions present.
    - The docblock amendment is BESIDE the original rule, quoting the division of labour verbatim.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/RunSpine.test.tsx</automated>
  </verify>
  <done>The spine renders name + status face + duration (+ the ask row) and nothing else; every other spine pin passes unedited.</done>
</task>

<task type="auto">
  <name>Task 2: The page — hero above the switch, RunStepList in the Log view, the foot region retired</name>
  <files>frontend/src/pages/WorkflowRunPage.tsx, frontend/src/pages/WorkflowRunPage.test.tsx</files>
  <read_first>
    - frontend/src/pages/WorkflowRunPage.tsx (the whole file — especially regions 1, 3, 5 and every copy constant's fence-bearing docblock)
    - frontend/src/pages/WorkflowRunPage.test.tsx (the fence sources: the superseded-string one-occurrence fences at :2500+, the empty-state codeOf fence, the raw-HTML-prop raw fence, the four-identifier fences, the deliverable-arm cases)
    - frontend/src/components/workflows/RunHero.tsx (plan 03 — its props)
    - frontend/src/components/workflows/RunStepList.tsx (plan 02 — its props)
    - .planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-CONTEXT.md (D-01..D-04, D-14)
  </read_first>
  <action>
    1. **The hero mount, above the switch point (D-01/D-02).** Inside the centre `section`
       (region 3), ABOVE the `centreView` conditional, mount `RunHero` gated on
       `isTerminal` (D-03: the live arm renders nothing — the gate can live at the mount or in
       the component; the mount gate is cheaper and keeps the component honest-by-construction,
       but MATCH whatever plan 03's suite asserts). The hero therefore renders in BOTH views.
       Wrap it in the same `mx-auto w-full max-w-*xl` rhythm the strip uses, per the sketch's
       `max-w-4xl gap-8` column. Pass the interfaces' props — `answer={runAnswer}` (the EXISTING
       memo's result; the memo itself is untouched), `files={orderedFiles}`, `filesLoading`,
       `failedStepTitle`, `onDownload`, `titleOf`, `now={nowMs}`, `run`.

    2. **The Log view's list (D-04 order: hero → strip → list).** Replace the `RunTranscript`
       mount with `RunStepList`, consuming `phases={run?.phases ?? []}` (the FULL wire model —
       NOT `wireRows`; the strip and the spine keep `wireRows`). Pass `runId`, `titleOf`,
       `runStatus={runStatus}`, `liveOf={runState}`, `now={nowMs}` (verify exact prop names
       against the built component). Delete the `RunTranscript` import.

    3. **Retire the foot region (region 5).** Delete the `run-deliverables` `section` and its
       four-arm gate wholesale — the arms now live in the hero. Keep `downloadError` rendering
       (move its `data-testid="run-download-error"` paragraph beside the hero mount — a download
       failure must still say so where the user clicked). Keep `onDownload`, `orderedFiles`,
       `filesNow` and the `useWorkspaceFiles` hook (the hero consumes them).

    4. **The copy constants.** `COPY_NO_FILES_LIVE`: retired for the live arm (D-03). Its
       docblock records the retirement BESIDE the original reasoning, and its one-occurrence
       source fence in the test file is RE-AIMED: the constant now has ZERO render sites (the
       live arm renders nothing) — delete the constant and keep the quoted string in the
       docblock + the test's SUPERSEDED block, which is what keeps `git log -S` able to find the
       thread (the superseded-not-deleted convention; 200.1-02's precedent moved four reference
       sites in one commit — do the same here). `COPY_DELIVERABLE_HEADING` and
       `COPY_ANSWER_HEADING`: likewise deleted from the page, their strings quoted in the
       docblock's superseded note and in the test's SUPERSEDED block, their fences retired with
       the strings' histories preserved. `COPY_NO_FILES_TERMINAL`: superseded by the D-13/D-16
       split family in the vocabulary leaf — same treatment. ⚠ The docblock comments carrying
       these histories are prose: check each affected fence's reading mode BEFORE writing them
       (the raw-mode fences cannot see comment-stripped prose, and vice versa).

    5. **The page's own docblock** gains the D-01..D-04 record: the hero leads, the log is the
       ledger, the strip keeps its position relative to the list it summarises, the hero sits
       above the switch so it renders in both views. Also correct the STALE canvas claim BESIDE
       the original (the CONTEXT's own instruction): `RunTranscript.tsx`'s docblock and the
       ledger say the canvas came OFF this page — it came back as a switch (`c5e3a352`); note
       that `RunTranscript.tsx`'s own docblock correction belongs to plan 05's ledger pass, and
       this plan's page-side comment must not contradict the switch's existence.

    6. **The test file.** Update `WorkflowRunPage.test.tsx`:
       - the deliverable-region cases (the four arms, the empty states, the file-only
         byte-identical baseline) MOVE to the hero's mount level — re-aim them at the hero's
         rendered output through the page (the page still mounts it; the assertions change
         anchor, not meaning);
       - the superseded-string one-occurrence fences RETIRE with re-aimed SUPERSEDED blocks
         (quote each old string once, assert it appears NOWHERE in the page source);
       - add: the hero renders in BOTH views (switch to canvas; the hero is still present);
       - add: the live arm renders no hero (a live run's centre starts with the strip);
       - add: the centre's DOM order is hero → strip → step list (assert by testid order);
       - add: `RunTranscript` appears nowhere in the page source (the import removal, asserted);
       - every existing fence whose subject moved (the raw-HTML-prop fence, the
         four-identifier fences, the Stop-control fence) re-runs GREEN — a page edit this large
         can red unrelated source fences, and the ledger's invariant #7 for this file says
         exactly that.
  </action>
  <acceptance_criteria>
    - `grep -c "RunTranscript" frontend/src/pages/WorkflowRunPage.tsx` returns 0.
    - `grep -c "run-deliverables" frontend/src/pages/WorkflowRunPage.tsx` returns 0.
    - `grep -c "No files yet" frontend/src/pages/WorkflowRunPage.tsx` returns at most 1 (the quoted superseded string in the docblock, if the fence's reading mode permits) and the test file's SUPERSEDED block quotes it.
    - `grep -c "<RunHero\|<RunStepList" frontend/src/pages/WorkflowRunPage.tsx` returns 2 (one mount each).
    - The hero mount sits ABOVE the `centreView` conditional in the source (DOM-order asserted in the suite).
    - `cd frontend && npx vitest run src/pages/WorkflowRunPage.test.tsx` exits 0.
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit` — baseline 33 / 19 unmoved (a removed import and removed constants must not orphan anything; `noUnusedLocals` will catch stragglers).
    - `grep -c "runAnswer" frontend/src/pages/WorkflowRunPage.tsx` — the memo is untouched (its docblock and body byte-unchanged; assert in the SUMMARY via `git diff` over the memo's lines).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/WorkflowRunPage.test.tsx src/components/workflows/RunSpine.test.tsx src/components/workflows/RunHero.test.tsx src/components/workflows/RunStepList.test.tsx</automated>
  </verify>
  <done>The page composes hero → strip → step list in both views, the foot region is gone, the spine has no count, every page fence is green, and the count gate closes with the verdict line read verbatim and every per-file delta attributed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the page → every source fence it carries | this is the largest edit this file has taken; raw-source fences cannot tell prose from code |
| the wire → the step list | the new list consumes the full model; no field is re-derived at the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-200.2-04 | Tampering (XSS) | the hero mount | mitigate | the hero renders the answer through MarkdownRenderer only; the page's raw-HTML-prop fence must stay green after the edit (re-verified, not assumed) |
| T-200.2-11 | Tampering (of meaning) | the retired strings | mitigate | superseded-not-deleted: every retired constant keeps a quoted thread in a docblock or the test's SUPERSEDED block, so `git log -S` still finds the history; the SUMMARY lists each |
| T-200.2-12 | Information Disclosure | the page's raw-source fences | mitigate | read each fence's reading mode before writing any new comment; describe forbidden tokens, never spell them; re-run every page fence after the edit |
| T-200.2-13 | Tampering (of meaning) | the A-02 dependency | mitigate | the spine-count removal and the centre yield land in the SAME plan — a wave that ships one without the other ships the strict-subset defect; the suite asserts both directions (a count-bearing row shows no spine count AND shows the centre yield) |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/pages/WorkflowRunPage.test.tsx src/components/workflows/RunSpine.test.tsx` — green.
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root — verdict line read verbatim; per-file deltas attributed (the gate's pin on `WorkflowRunPage.test.tsx` and `RunSpine.test.tsx` WILL move — record old → new for both).
- `cd frontend && npx tsc -p tsconfig.app.json --noEmit` — baseline unmoved.
- `git diff --numstat <base> HEAD -- supabase/migrations frontend/package.json` — EMPTY.
</verification>

<success_criteria>
- The two columns carry different payloads, mechanically: the centre yields (RunStepList), the spine times (no count sub-line) — asserted in both directions.
- The hero leads in both views; the live arm renders nothing at the hero slot.
- Every retired string is superseded-not-deleted with a findable thread.
</success_criteria>

<output>
Create `.planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-04-SUMMARY.md` when done.
Record in it: the G-5 verdict paragraph for WorkflowRunPage.tsx, the re-derived triple at the
plan's base and close, both moved gate pins (old → new), and the list of superseded strings with
where each thread now lives.
</output>
