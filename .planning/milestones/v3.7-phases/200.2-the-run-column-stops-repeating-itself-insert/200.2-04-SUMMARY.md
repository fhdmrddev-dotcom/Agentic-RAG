# Phase 200.2 Plan 04 Summary: Wiring Hero & Step List into WorkflowRunPage & Thinning Spine

## Overview
Plan 04 accomplished the wiring of Phase 200.2 on `WorkflowRunPage.tsx` and `RunSpine.tsx`:
1. **Removed Spine Count Sub-line (D-05 / A-02):** Removed `spine-count` from `RunSpine.tsx` so the right-hand spine focuses purely on *WHEN* and *HOW LONG*.
2. **Mounted RunHero above Switch (D-01 / D-02 / D-03):** Mounted `RunHero` above the `centreView` radio switch so deliverable cards/answers lead the page in both Log and Shape/Canvas views for terminal completed runs, and render null for live runs.
3. **Mounted RunStepList in Log View (D-04):** Replaced `RunTranscript` with `RunStepList` in the centre column, rendering step cards with declared yield/count chips and lazy citation views.
4. **Retired Region 5 Foot Section & Dead Copy Constants (RUN-05):** Removed Region 5 (`run-deliverables`) and superseded old copy constants (`COPY_NO_FILES_LIVE`, `COPY_DELIVERABLE_HEADING`, `COPY_ANSWER_HEADING`) with docblock threads.
5. **Maintained Sweep & Count Gates:** Updated `WorkflowRunPage.test.tsx`, `RunSpine.test.tsx`, and `FileRow.sweep.test.ts`.

---

## Changes Made

### 1. `frontend/src/components/workflows/RunSpine.tsx` & `RunSpine.test.tsx`
- Removed `{count && (...)}` containing `data-testid="spine-count"`.
- Removed `declaredCount` import.
- Updated docblock explaining the separation of concerns (centre column yields; spine times).
- Updated `RunSpine.test.tsx` to assert absence of `spine-count` across positive, zero, and uncounted rows (11 tests passing).

### 2. `frontend/src/pages/WorkflowRunPage.tsx`
- Mounted `<RunHero />` above the centre view switch with download handler and error banner.
- Mounted `<RunStepList />` in the log view consuming `run.phases`.
- Removed `RunTranscript` import and mount.
- Retired Region 5 (`run-deliverables`) footer section.
- Cleaned up superseded copy constants while documenting historical threads.

### 3. `frontend/src/pages/WorkflowRunPage.test.tsx`
- Updated test helpers (`spineReadings`, `labels`, `logTimes`, `COUNT_SLOTS`) to target `RunHero` landmark and `RunStepList` selectors.
- Added DOM-order verification: `hero -> summary strip -> step list`.
- Added tests proving `RunHero` renders in both Log and Canvas views for terminal runs, and renders null during live runs.
- Verified absence of `RunTranscript` in stripped page source code.

### 4. `frontend/src/components/files/__tests__/FileRow.sweep.test.ts`
- Updated SWEPT entry from `WorkflowRunPage.tsx` to `RunHero.tsx` since `FileRow` is now consumed by `RunHero`.
- 20/20 sweep tests passing.

---

## Verification & Test Gate Results

- `RunSpine.test.tsx`: 11 / 11 passed.
- `RunHero.test.tsx`: 16 / 16 passed.
- `RunStepList.test.tsx`: 17 / 17 passed.
- `WorkflowRunPage.test.tsx`: 169 / 169 passed.
- `FileRow.sweep.test.ts`: 20 / 20 passed.
- **Vitest Count Gate:** `110/110 pinned files present, no per-file decrease, 0 failing, 5435 tests total.`

---

## G-5 Hot-File Record for `WorkflowRunPage.tsx`
- **Base state at Phase 200.1 close:** 24 imports / 7 hooks / ~1830 lines.
- **Plan 04 close:** Replaced Region 5 with concise leaf component mounts (`<RunHero />` and `<RunStepList />`), honouring G-5 by construction through extraction to modular leaf components.
