---
phase: 200.2-the-run-column-stops-repeating-itself-insert
plan: 06
type: execute
wave: 4
depends_on: ["200.2-04", "200.2-05"]
files_modified:
  - .planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-UAT.md
autonomous: false
requirements: [RUN-05]
tags: [RUN-05, SC1, SC2, SC3, SC4, G-4, lived-experience, UAT, manual, long-message-partial]
must_haves:
  truths:
    - "SC#4 — a HUMAN reads the two columns on a real finished run and judges they no longer read as the same list (RUN-05's acceptance bar; no DOM assertion can tick it)"
    - "SC#1 — all four hero arms are driven in a browser on real runs, including 'neither', which today reads as a loading failure"
    - "SC#3 — the real citation passages are reachable from the step that read them, expanded inline, with no relevance score"
    - "The spine's ring faces render on a finished run with no '?' glyphs (A-09 near-miss 2)"
    - "The disclosure affordance appears only where it leads somewhere; an empty fetch degrades to plain text"
    - "Below 1024px the hero holds as the whole page (A-07 — every decision assumes two columns; this row proves the one-column case)"
    - "A long answer scrolls within the hero and does not blow the layout (the long-message axis, PARTIAL)"
  artifacts:
    - path: .planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-UAT.md
      provides: "the driven scoreboard — every row with its evidence, ⛔ rows named with their reason, never silently omitted"
  key_links:
    - from: .planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-UAT.md
      to: .planning/ROADMAP.md
      via: "the four success criteria's verification record — SC#4 is judged here or the phase does not close"
      pattern: "SC#4"
---

<objective>
The G-4 lived-experience UAT rows — the heart of the phase.

Purpose: RUN-05 is satisfied ONLY by a human reading the screen. Its load-bearing clause —
*the two columns no longer read as the same list* — is judged on screen, not by a DOM
assertion. The four-axis note: this phase touches UI state only; cross-provider, multi-tool and
parallel-thread axes DO NOT APPLY (stated explicitly in the UAT doc rather than silently
omitted); the long-message axis applies PARTIALLY (the long-answer row).

Output: `200.2-UAT.md` — the driven scoreboard with evidence per row.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-CONTEXT.md
@.planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-RESEARCH.md
@.planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-VALIDATION.md

**The standing project arrangement:** the operator starts the backend and drives the browser;
`take_screenshot` times out on this box — read the rendered `textContent` and DOM geometry via
`evaluate_script`. Chrome MCP can hang; the dropdown-wedge and HMR-staleness notes apply.
⚠ Every row needs its OWN evidence — a row without pasted output is an undriven row. UAT fixes
need their own UAT (re-drive the affected rows after any fix).

**Arm-defining runs go stale** — RE-DERIVE them at execution time against the live DB (the
200.1-02 lesson: its ids were measured a day earlier and the phase re-derived them at close).
The 200.1-02 table, to re-verify or replace:
- both: `bef870ba…` (5 steps, file + answer) — alternate `f4c8d111…` / `27cf1ab9…`
- answer only: `13691156…` (3 steps, 38 real citations) — alternate `d0ff51e0…`
- neither (cancelled): `c353ff78…` — alternates `cc32823d…`
- neither (failed): `d8f87920…` (sketch 202's failed arm)
- file only: ⛔ none found in this DB by either predicate (thread-scoped or output-key) — if
  still true at execution time, record the row as ⛔ with the reason, NEVER silently omitted.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: The UAT scoreboard — driven with the operator watching</name>
  <files>.planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-UAT.md</files>
  <action>
    Drive the eight G-4 lived-experience rows with the operator watching (backend live, browser
    on localhost:5173, verdicts read via `evaluate_script` — `take_screenshot` times out on this
    box), and author `.planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-UAT.md`
    as the driven scoreboard. The eight rows and their exact procedures are in
    `<how-to-verify>` below. This is MANUAL-ONLY work: SC#4 is a human reading the screen, and
    its verdict is recorded verbatim either way.
  </action>
  <what-built>
    The re-composed run surface: a hero leading the centre column (four arms by type), a
    step-card process trace with per-step yields and lazily-expanded citation passages, and a
    spine that carries only names, faces and durations.
  </what-built>
  <how-to-verify>
    Re-derive the arm-defining run ids against the live DB first (they go stale within days).
    Then drive each row at `http://localhost:5173` (backend live), reading rendered
    `textContent` via `evaluate_script`:

    1. **SC#4 — the load-bearing row.** Open a real finished run (the `both` arm's id). Read
       the two columns as a person would: the centre carries per-step YIELDS (15 sources / 2
       fields / wrote an answer), the spine carries WHEN and HOW LONG. The operator judges
       aloud whether the columns still read as the same list. Record the verdict VERBATIM —
       including a fail, which is a finding, not a re-roll.
    2. **SC#1 — the four hero arms.** One run per arm: both / answer / neither-failed /
       neither-cancelled. For each, paste the hero's rendered heading + first sentence. The
       `neither` arms must render TWO DIFFERENT SENTENCES (failed names its step; cancelled says
       stopped — never "finished without writing"). A file-only run: if none exists, record ⛔
       with the re-derived reason.
    3. **SC#2 — the yield on real rows.** On the `both` run: every retrieval step shows its
       declared count; the synthesize step shows `wrote an answer`; a step that produced
       nothing shows NOTHING (no 0, no dash). Confirm no step renders a character count and no
       run-level "N sources" aggregate appears anywhere.
    4. **SC#3 — the disclosure.** Expand a retrieval step: the passages render inline, each
       showing document/filename/chunk and the passage text, with NO numeric relevance score
       anywhere in the expanded region. Expand again / collapse: no duplicate fetch storm.
       A step whose expand returns nothing (find or force one): the row degrades to plain
       text, no chevron.
    5. **The spine's ring faces** (A-09 near-miss 2). On the finished run: every spine row
       shows its face (✓ etc.), ZERO `?` glyphs.
    6. **The narrow viewport** (A-07). Resize below 1024px: the spine is hidden, the hero is
       the whole page, and it holds (no overflow, no clipped card).
    7. **The long-answer row** (long-message axis, PARTIAL). Open the answer-arm run (4,411
       chars) or a longer one (max measured 38,935): the hero scrolls within its region and
       the layout does not blow.
    8. **The live arm** (D-03). If a run can be launched cheaply: while live, the centre starts
       with the strip and the log — NO hero placeholder, no reserved height; when the first
       deliverable lands, the hero pushes in. (If no run can be launched, record the row as
       owed-with-reason rather than claimed.)

    Write `.planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-UAT.md`
    with: the 4-axis statement (cross-provider / multi-tool / parallel-thread DO NOT APPLY and
    why — UI-state-only phase; long-message PARTIAL), every row above with its evidence, ⛔ rows
    with reasons, and the SC#1..SC#4 verdicts.
  </how-to-verify>
  <resume-signal>Operator confirms each row's verdict (or names failures to route)</resume-signal>
  <verify>
    <human-check>200.2-UAT.md exists with all eight rows driven or marked (⛔/owed with reasons), evidence pasted per row, the 4-axis statement, and the SC#1..SC#4 verdict table — reviewed by the operator.</human-check>
  </verify>
  <done>Every UAT row is driven or explicitly marked owed/⛔ with a reason; SC#4 carries the operator's verbatim on-screen judgement; no criterion reads verified without its driving row present.</done>
</task>

<task type="auto">
  <name>Task 2: Close the scoreboard — route failures, record owed rows</name>
  <files>.planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-UAT.md</files>
  <read_first>
    - .planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-UAT.md (Task 1's driven rows)
    - .planning/ROADMAP.md (the phase's four success criteria)
  </read_first>
  <action>
    After the operator's verdicts: for each failing row, record the finding VERBATIM and route
    it — a ≤1-file/≤10-line fix is a `/gsd:fast` candidate (G-3), anything larger is a bug
    report or a deferred row with a re-open trigger; never a silent re-roll. For each row that
    could not be driven, record it as OWED with the reason and the first row to run (the
    "closing a phase with owed manual UAT rows is legitimate, if stated as a decision"
    precedent). Produce the SC#1..SC#4 summary table at the foot of the UAT doc — which
    criteria are verified, which are owed, and what stands between the phase and close.
  </action>
  <acceptance_criteria>
    - Every row in the UAT doc carries evidence (pasted textContent / geometry) or a ⛔/owed marker with a reason.
    - The SC table names each criterion's status with no criterion marked verified without its driving row present.
    - Every failure is routed (bug report created or G-3 fix named) rather than left as prose.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "SC#4" ".planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-UAT.md"</automated>
  </verify>
  <done>The scoreboard is complete, honest about owed rows, and every criterion's status is stated as a decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the driven browser → the local app | real runs on the local DB; no global setting is mutated (navigate the run surface directly or drive per-request model/provider — never touch global config) |
| the UAT doc → the phase's close | a scoreboard that lists only what passed is not a scoreboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-200.2-17 | Repudiation | the UAT record | mitigate | every row carries its own pasted evidence; ⛔ rows named with reasons; owed rows stated as decisions — the feedback rows-need-their-own-evidence and UAT-fixes-need-their-own-UAT lessons applied |
| T-200.2-18 | Tampering (of meaning) | the SC#4 verdict | mitigate | the operator's judgement is recorded verbatim including a FAIL; a green suite cannot tick SC#4 and the doc says so in as many words |
</threat_model>

<verification>
- `200.2-UAT.md` exists with all eight driven-or-marked rows, the 4-axis statement, and the SC table.
- No criterion reads verified without its row.
</verification>

<success_criteria>
- SC#4 is judged by a human on screen, and the verdict (either way) is recorded.
- All four hero arms driven or ⛔-marked with re-derived reasons; the disclosure, the spine
  faces, the narrow viewport and the long answer all driven or owed-with-reason.
</success_criteria>

<output>
Create `.planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-06-SUMMARY.md` when done.
</output>
