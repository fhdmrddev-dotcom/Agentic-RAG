---
phase: 200.2-the-run-column-stops-repeating-itself-insert
plan: 02
type: execute
wave: 2
depends_on: ["200.2-01"]
files_modified:
  - frontend/src/components/workflows/RunStepList.tsx
  - frontend/src/components/workflows/RunStepList.test.tsx
  - frontend/src/components/workflows/runColumnVocabulary.ts
  - frontend/src/lib/api.ts
autonomous: true
requirements: [RUN-05]
tags: [RUN-05, SC2, SC3, D-05, D-06, D-07, D-11, D-12, A-01, A-02, A-03, A-08, R-2, R-3, zero-glyph, zero-spelled-strings, vocabulary-leaf]
must_haves:
  truths:
    - "Each step renders ONE line: status mark · name · the step's own yield — the variant-C acceptance bar (A-01)"
    - "The yield is a stored fact — the declared count via countDeclared, or 'wrote an answer' from a non-empty deliverable_text keyed on phase_type from the wire (A-03) — never a slug heuristic, never a character count"
    - "The yield REPLACES the outcome word 'finished' and only that word; every other outcome keeps its word and renders no yield; a step with neither count nor text renders nothing — no 0, no dash (D-07 / D-06)"
    - "step_count === 0 is a real measurement and renders its noun; only null is absent (D-06)"
    - "The yield IS the disclosure control: expanding a step lazy-fetches the citation route; a step whose fetch returns empty degrades to a plain-text yield with no affordance (D-12, operator decision 1)"
    - "The expanded passages region carries no total (D-11 / A-06) and no relevance score (D-10 / R-2)"
    - "No run-level aggregate of per-step counts ever renders (A-09)"
    - "The component renders zero glyphs and spells zero user-visible strings of its own — every word comes from the vocabulary leaf or an existing home (R-3 discipline)"
  artifacts:
    - path: frontend/src/components/workflows/RunStepList.tsx
      provides: "the variant-C process trace — one card per step, consuming the FULL WorkflowRunPhase wire model"
      contains: "RunStepList"
    - path: frontend/src/components/workflows/runColumnVocabulary.ts
      provides: "the ONE string home for the new column's own words — a TRUE leaf, zero imports, importing from neither receiptVocabulary nor transcriptVocabulary"
    - path: frontend/src/components/workflows/RunStepList.test.tsx
      provides: "the yield arms, the absent arm, the disclosure behavior, the phase_type gate, the zero-glyph and zero-spelled-strings sweeps"
    - path: frontend/src/lib/api.ts
      provides: "getWorkflowRunPhaseCitations — the client read for the new route"
      contains: "getWorkflowRunPhaseCitations"
  key_links:
    - from: frontend/src/components/workflows/RunStepList.tsx
      to: frontend/src/lib/api.ts
      via: "getWorkflowRunPhaseCitations on expand (D-12) — consuming WorkflowRunPhase[], never PhaseTimingRow"
      pattern: "getWorkflowRunPhaseCitations"
    - from: frontend/src/components/workflows/RunStepList.tsx
      to: frontend/src/components/workflows/phaseDuration.ts
      via: "phaseRunFacts + declaredCount over the full wire row (satisfies PhaseTimingRow structurally)"
      pattern: "phaseRunFacts"
    - from: frontend/src/components/workflows/RunStepList.tsx
      to: frontend/src/components/workflows/runColumnVocabulary.ts
      via: "every user-visible string this component renders"
      pattern: "runColumnVocabulary"
---

<objective>
The variant-C step-card list — the centre column's new "process trace".

Purpose: the centre column stops restating the right panel's list of step names in a second,
plainer typeface. Each step line reports WHAT that step yielded (SC#2), and the yield is the
door to the real citation passages (SC#3). This plan builds the component; plan 04 wires it into
the page and removes the old mount.

Output: `RunStepList.tsx` (new), `runColumnVocabulary.ts` (new leaf), the api client function,
and `RunStepList.test.tsx`.
</objective>

<execution_context>
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Vibe Apps/Agentic RAG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-CONTEXT.md
@.planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-RESEARCH.md
@.planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-VALIDATION.md
@.planning/sketches/202-the-run-column-rendered/README.md
@frontend/sketch/SketchRunColumn.tsx
@docs/HOT-FILE-LEDGER.md

⚠ **Worktree rules:** bootstrap first, `GSD_VITEST_MAX_WORKERS=2` on every vitest run, never
`rm -rf` a worktree.

⚠ **THE COMPONENT IS NOT MOUNTED BY THIS PLAN.** `WorkflowRunPage.tsx` belongs to plan 04 — two
plans editing that page in one wave is a file conflict. Building the component unmounted is
deliberate: it is testable in its own suite, and the A-10 sketch teardown (plan 05) fires only
when the phase has an executed plan anyway.

**The three plan-time decisions, RESOLVED by the operator (do NOT re-open):**
1. **Citation seam — fetch-on-expand with empty-degrade.** Every step's yield renders the same
   way; expanding fires the fetch; an empty response degrades the row to a plain-text yield (the
   affordance disappears, nothing else changes). Zero wire change to the existing serializer.
2. **D-17 — record a reported bug, leave the rule.** NOT this plan's concern directly, but the
   constraint it imposes IS: this component does NOT re-derive or repair the run-level answer
   selection. The bug report is plan 03's task.
3. **Vocabulary leaf — a NEW file** `runColumnVocabulary.ts`. Never import from
   `receiptVocabulary.ts` or `transcriptVocabulary.ts` (true-leaf contracts; importing across
   vocabularies breaks a contract rather than tripping a fence — only that module's own suite
   can catch it).

<interfaces>
From frontend/src/lib/api.ts (the wire model the component consumes — the FULL row, NOT PhaseTimingRow):
```typescript
export interface WorkflowRunPhase {
  slug: string
  phase_index: number
  status: string
  phase_type: string | null        // populated on every real row: llm_agent · llm_single · llm_emit · llm_human_input
  started_at?: string | null
  completed_at?: string | null
  step_count?: number | null       // 0 IS A REAL MEASUREMENT; null = this type declares no count
  step_noun?: string | null        // AUTHORED COPY — render verbatim, never substitute a domain word
  deliverable_text?: string | null // this step's own output["text"]
}
```

From frontend/src/components/workflows/phaseDuration.ts (the ONE wire-truth→fact resolver — WorkflowRunPhase satisfies PhaseTimingRow structurally, so NO widening, and phaseDuration.ts diffs to nothing):
```typescript
export interface PhaseTimingRow { slug: string; status: string; started_at?: string | null; completed_at?: string | null; step_count?: number | null; step_noun?: string | null }
export function phaseRunFacts(row: PhaseTimingRow, runStatus?: string | null, now?: number): PhaseRunFacts
  // → { timing: PhaseTiming, outcome: string, count: { count: number; noun: string } | null }
export function declaredCount(row: PhaseTimingRow): { count: number; noun: string } | null
```

From frontend/src/components/workflows/receiptVocabulary.ts (import these — they are the SHARED homes, not the new leaf's):
```typescript
export const OUTCOME_FINISHED = "finished"   // the ONE outcome word the yield replaces (D-07)
export function countDeclared(count: number, noun: string): string   // `${count} ${noun}`
```

From the sketch (the acceptance bar — SketchRunColumn.tsx's StepCard, :372-491):
- one card per step: mark · name · yield · chevron only where expansion leads somewhere
- `const Head = expandable ? "button" : "div"` — the yield IS the disclosure control
- `const word = facts.outcome === OUTCOME_FINISHED && chip ? null : facts.outcome`

The citation route this plan's fetch calls (plan 01's): `GET /workflow-runs/{run_id}/phases/{slug}/citations`
→ `[{ document_id, filename, chunk_index, passage }]` — similarity already stripped server-side.

The citation PRESENTATION to reuse UNCHANGED: `frontend/src/components/chat/CitationCard.tsx`
(takes `citation: Citation` — with `similarity` always null on this wire, its score branch
provably cannot fire; `n` optional; `useCitationNavOptional` degrades to null outside the
provider, which is mounted app-wide in `App.tsx:292`). ⚠ CitationCard itself imports lucide
glyphs — that is ITS shipped shape and not this surface's budget; the zero-glyph invariant
binds RunStepList's OWN marks, not the reused card's interior. The component's own chevron and
status marks must NOT be lucide imports (see action).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The vocabulary leaf + the api client function</name>
  <files>frontend/src/components/workflows/runColumnVocabulary.ts, frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/components/workflows/transcriptVocabulary.ts (the leaf SHAPE: whole-table claim, the four rules, zero imports, the not-spelled-neighbours discipline)
    - frontend/src/components/workflows/receiptVocabulary.ts (the sibling true leaf — read its rules, import NOTHING from it here beyond what RunStepList does; this file imports nothing at all)
    - frontend/src/lib/api.ts:4338-4379 (getWorkflowRun — the docblock + ApiError + encodeURIComponent convention the new function copies)
    - .planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-CONTEXT.md (D-06, D-12, A-08)
  </read_first>
  <action>
    1. Create `frontend/src/components/workflows/runColumnVocabulary.ts` — a TRUE leaf (zero
       imports, no React, no JSX), modelled on `transcriptVocabulary.ts`'s whole-table claim.
       It holds exactly these governed strings (final wording yours to settle, informed by the
       sketch's render; keep each a noun-phrase or sentence a person reads):
       - the region's landmark label (the accessible name for the process trace — e.g. the
         sketch's "Process trace" reading);
       - the `wrote an answer` yield string (D-06 — a FACT, not a figure; lowercase register
         matching `countDeclared`'s output so the two yields read as one kind of thing);
       - the region's two honest-absence headlines, which MUST be different sentences
         (the runFacts/DecisionsList/phaseDuration/transcriptVocabulary defect, caught a fifth
         time by D-13's reasoning): *no steps recorded* and *no times recorded* are two facts —
         fold them and the column prints "nothing happened" about a run that did plenty.
       - the disclosure's accessible name/prefix if one is needed for aria-labels.
       A docblock states the rules (exact-match assertions only; zero glyph strings; every
       consumer imports from here; no string may equal one exported by receiptVocabulary or
       transcriptVocabulary — asserted by RunStepList's suite, not here). ⚠ FORBIDDEN
       NEIGHBOUR MODULES ARE NEVER SPELLED IN THIS FILE'S PROSE — describe them, never name
       them (the 187-24 trap, five firings).

    2. In `frontend/src/lib/api.ts`, add `export interface RunStepCitation` (the four wire
       keys: `document_id: string`, `filename: string`, `chunk_index: number | null`,
       `passage: string | null` — deliberately NOT `Citation`: the narrower type makes the
       score's absence a compile-time fact rather than a runtime convention) and
       `export async function getWorkflowRunPhaseCitations(runId: string, phaseSlug: string, signal?: AbortSignal): Promise<RunStepCitation[]>`
       sited directly beneath `getWorkflowRun` (the sibling convention the 197-decline note
       already records for the run-log function). It copies `getWorkflowRun`'s shape exactly:
       `getAuthHeaders()`, `encodeURIComponent` on BOTH path params (WR-07 — a slug is an
       unconstrained string, so the encode is load-bearing here, not belt-and-braces), throws
       `ApiError` on `!res.ok`, returns the parsed list. ⚠ api.ts is the hottest file in the
       repo with a standing DECLINE whose trigger is "the next phase adding a RUNTIME export or
       a second concern here" — the SUMMARY must record the trigger FIRED and this function's
       placement beside its two siblings as the recorded reason it is not a second concern
       (one run-reads surface, one file), per the SEED-190 precedent written into that note.

       ⚠ In any new docblock comment in api.ts: describe React's raw-HTML escape hatch in
       words only — never spell the prop. `WorkflowRunPage.tsx` and this file both carry raw
       source fences, and the trap has fired seven times.
  </action>
  <acceptance_criteria>
    - `frontend/src/components/workflows/runColumnVocabulary.ts` exists; `grep -Ec "^import" frontend/src/components/workflows/runColumnVocabulary.ts` returns 0.
    - `grep -c "runColumnVocabulary" frontend/src/components/workflows/receiptVocabulary.ts frontend/src/components/workflows/transcriptVocabulary.ts` returns 0 on both (no cross-vocabulary import).
    - `RunStepCitation` declares exactly the four keys; `grep -n "similarity" frontend/src/lib/api.ts | grep -c "RunStepCitation"` — the type carries no score field.
    - `getWorkflowRunPhaseCitations` appears once in api.ts, beneath `getWorkflowRun`, with `encodeURIComponent` on both params.
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit` reports the baseline 33 errors / 19 files — no growth.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "error TS" || true</automated>
  </verify>
  <done>The leaf exists with zero imports and its whole-table claim; the client function sits beside its siblings; the typecheck baseline is unmoved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RunStepList — the variant-C card list, disclosure, and yield composition</name>
  <files>frontend/src/components/workflows/RunStepList.tsx, frontend/src/components/workflows/RunStepList.test.tsx</files>
  <read_first>
    - frontend/sketch/SketchRunColumn.tsx:370-491 (StepCard — the acceptance bar's composition)
    - frontend/src/components/workflows/RunTranscript.tsx (the file being replaced's discipline: zero-glyph invariant, the tense rule, the owns-no-words rule, pageAgreesWithWire)
    - frontend/src/components/workflows/RunSpine.tsx:106-175 (READING_FACE — the inherited glyph/ring vocabulary for the status mark)
    - frontend/src/components/workflows/phaseDuration.ts:246-340 (phaseTiming, declaredCount, phaseRunFacts)
    - frontend/src/components/workflows/receiptVocabulary.ts:149-199 (the nine outcome words)
    - frontend/src/components/workflows/runColumnVocabulary.ts (Task 1's leaf)
    - frontend/src/components/chat/CitationCard.tsx (the reused passage card — its Props and its null-score branch)
    - frontend/src/components/workflows/ownProperty.ts (own() — for any keyed read)
  </read_first>
  <behavior>
    - THE YIELD ARMS: a row with `{count: 15, noun: "sources"}` renders `15 sources`; a row with `{count: 0, noun: "sources"}` renders `0 sources` (0 is a fact); a row with null count AND non-empty deliverable_text renders `wrote an answer`; a row with neither renders NOTHING trailing — no `0`, no dash.
    - D-07 — the yield replaces ONLY `finished`: a finished row with a count renders the count and NOT the word; a finished row with no count and no text renders `finished`; a failed / skipped / cancelled / not-reached row keeps its outcome word and renders NO yield even when it carries text (nothing was produced).
    - A-03 — the `wrote an answer` arm is keyed on `phase_type` from the wire (rows typed llm_single / llm_human_input / external_action yield it); a slug containing "confirm" or "emit" must NOT change the arm (plant a slug-regex gate and watch it fail this test — the refuted heuristic, asserted against).
    - PRECEDENCE — a row with BOTH a count and text renders the count (the more specific), never both.
    - D-12 / operator decision 1 — fetch-on-expand with empty-degrade: every row's yield renders identically; clicking a row's head fires `getWorkflowRunPhaseCitations` ONCE for that slug (assert the call count — no refetch on re-collapse/re-expand of a fetched row is the cheap correct cache, a refetch each expand is also acceptable; assert whichever you build); a fetch that resolves `[]` degrades the row to a plain-text yield with NO chevron and NO expanded region (the affordance appears only where it leads somewhere); a fetch error leaves the row plain and does not throw.
    - D-10 / R-2 — the expanded region renders the passages through the reused CitationCard and asserts NO numeric score appears in the region's textContent.
    - D-11 / A-06 — the expanded region states NO total ("15 of 38" / "38 sources" nowhere).
    - A-09 — no run-level aggregate: the component renders no sum of counts anywhere (assert the region's textContent never contains the sum of two rows' counts when both are visible).
    - THE TENSE RULE (T-200.1-05) — the live reading (liveOf) is used ONLY while it agrees with the wire row's own status, exactly as RunTranscript's pageAgreesWithWire keeps it; a disagreeing live label falls back to the wire's outcome. The component takes `liveOf` as an optional prop in the same read-shape form (`{ reading, label }`) — NEVER a runtime import of the canvas vocabulary.
    - THE STATE MARK — one mark per row, inherited from RunSpine's READING_FACE glyph vocabulary (✓ ✕ ● ! ○ ⤳ ↛ ⏹ ?) via the liveOf reading with the same fallback; NO lucide import in this file.
    - THE ZERO-GLYPH / ZERO-STRING SWEEPS — the suite sweeps RunStepList's own `?raw` source: zero lucide imports, zero user-visible string literals outside the vocabulary imports (the RunTranscript sweep pattern).
    - Focus: expandable rows render a `button` head with `aria-expanded`; plain rows render no focusable head element. Tab order is DOM order.
  </behavior>
  <action>
    Create `frontend/src/components/workflows/RunStepList.tsx` exporting
    `RunStepList` with props: `phases: readonly WorkflowRunPhase[]` (⚠ the FULL wire model —
    imported from `@/lib/api` as a TYPE only, the `PhaseTimingRow` precedent; do NOT widen
    `PhaseTimingRow` and do NOT add fields to `phaseDuration.ts`), `titleOf: (slug: string) => string`
    (the page's, never re-derived), `runId: string` (for the citation fetch), `runStatus?: string | null`,
    `liveOf?: (slug: string) => { reading: string; label: string } | undefined` (the read shape,
    structurally satisfied), `now?: number`.

    Composition, per the sketch's StepCard: one card per step in SERVER order — a status mark
    (READING_FACE glyph + ring, inherited; own()-guarded), the step name (`titleOf`), the yield
    composed per the behavior block, and a chevron ONLY on rows whose citations have been
    fetched and are non-empty. The row head is a `button` when expandable, a plain `div`
    otherwise; the fetch fires on first expand; expanded region renders CitationCard per
    citation with the four-key objects (CitationCard's `Citation` prop accepts the narrower
    object structurally — `similarity` absent renders the score branch dead; `is_full_doc` may
    be passed `false`). Passage text is MODEL-AUTHORED — hand it to CitationCard, never through
    React's raw-HTML escape hatch.

    The yield resolution, once per row: `facts = phaseRunFacts(row, runStatus, now)` (the full
    row satisfies PhaseTimingRow structurally), `count = facts.count`, chip = count ?
    `countDeclared(count.count, count.noun)` : null, word = (outcome === OUTCOME_FINISHED &&
    chip) ? null : outcome, wroteAnswer = !chip && row.deliverable_text non-empty (a per-row
    fact — NEVER the run-level `runAnswer` derivation; that is the page's and stays). The yield
    lives INSIDE the phrase per D-05 (name then yield, one continuous line — no second line, no
    trailing atom, no clock gutter: A-07 deletes the centre's clock entirely).

    The component's docblock records: the A-02 division of labour (the centre says WHAT each
    step yielded; the spine says WHEN and HOW LONG — a plan that ships the centre yield without
    removing the spine's count sub-line has shipped the defect this phase exists to fix), the
    D-07 single-trailing-element rule, the R-3 boundary (the yield is a STORED FACT, not
    narration — that is what makes it admissible where a sentence is not), and the A-08 second
    bullet's recorded non-oversight (a step that produced the FILE cannot say so — nothing on
    workflow_phases attributes a file to a step; fabricating the join is the RunReceipt refusal).
    ⚠ In prose: never spell React's raw-HTML escape-hatch prop, the icon-library name, or the
    forbidden neighbour vocabulary modules. Describe them. The 187-24 trap has fired seven times,
    twice in files one directory over.

    Create `frontend/src/components/workflows/RunStepList.test.tsx` implementing every case in
    the behavior block. Mock `@/lib/api`'s `getWorkflowRunPhaseCitations` with a controllable
    deferred; seed full `WorkflowRunPhase[]` fixtures covering every arm (count / 0-count /
    text-only / neither / failed-with-text / gate-type / both). Include the two source sweeps
    (`?raw`) with non-vacuity checks. Run RED first where the behavior block names a plant
    (the slug-regex gate).
  </action>
  <acceptance_criteria>
    - `frontend/src/components/workflows/RunStepList.tsx` exists and exports `RunStepList`; `grep -c "lucide" frontend/src/components/workflows/RunStepList.tsx` returns 0.
    - `grep -c "PhaseTimingRow" frontend/src/components/workflows/phaseDuration.ts` is unchanged (the interface has no new members); `git diff --numstat <base> -- frontend/src/components/workflows/phaseDuration.ts` is EMPTY.
    - The component imports `WorkflowRunPhase` from `@/lib/api` as a type only (`import type`).
    - `cd frontend && npx vitest run src/components/workflows/RunStepList.test.tsx` exits 0, ≥ 20 cases.
    - The slug-regex counter-test exists and fails against a planted regex gate (recorded RED in the SUMMARY).
    - `cd frontend && npx tsc -p tsconfig.app.json --noEmit` baseline unmoved (33 / 19).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflows/RunStepList.test.tsx src/components/workflows/RunTranscript.test.tsx src/components/workflows/RunSpine.test.tsx</automated>
  </verify>
  <done>The component exists with its suite green, the shipped RunTranscript/RunSpine suites pass UNEDITED (nothing downstream of them changed yet), and the typecheck baseline is unmoved. ⚠ This plan must NOT edit WorkflowRunPage.tsx, RunSpine.tsx, RunTranscript.tsx, or phaseDuration.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the citation route → this component | model-authored passage text crosses here; it must never reach a raw-HTML sink |
| the wire → the yield | step_count/deliverable_text are model-influenced facts; null and 0 must never collapse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-200.2-04 | Tampering (XSS) | the expanded passages / any rendered model text | mitigate | passages render through the reused CitationCard as React children; no raw-HTML prop anywhere in this component (swept at zero occurrences); the suite plants a `<img src=x onerror>` passage and asserts it renders as text |
| T-200.2-05 | Tampering (of meaning) | the yield composition | mitigate | A-09 forbidden outright: no run-level sum of per-step counts (asserted); D-06 forbids character counts; D-11 forbids totals in the expansion |
| T-200.2-07 | Tampering (of meaning) | the 0-vs-null boundary | mitigate | declaredCount's typeof gate is reused, never re-implemented; the suite pins 0-count rendering its noun and null rendering nothing |
| T-200.2-08 | Information Disclosure | the citation fetch | mitigate | the fetch goes through the owner-scoped route (plan 01's posture); a failure degrades to plain text and never surfaces another user's data |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflows/RunStepList.test.tsx` — green.
- `cd frontend && npx vitest run src/components/workflows/RunTranscript.test.tsx src/components/workflows/RunSpine.test.tsx src/components/workflows/receiptVocabulary.test.ts src/components/workflows/transcriptVocabulary.test.ts` — all green UNEDITED (proof this plan's additions touched no shipped contract).
- `cd frontend && npx tsc -p tsconfig.app.json --noEmit` — baseline 33 / 19.
- `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` from the repo root — verdict line read verbatim; per-file deltas attributed in the SUMMARY.
</verification>

<success_criteria>
- The variant-C card list exists, consumes the full wire model, and its suite drives every yield arm, the disclosure seam, and both source sweeps.
- `phaseDuration.ts`, `RunTranscript.tsx`, `RunSpine.tsx`, `WorkflowRunPage.tsx` are byte-unchanged by this plan.
- The three operator decisions (fetch-on-expand; vocabulary leaf; no answer-rule repair here) are honored and recorded.
</success_criteria>

<output>
Create `.planning/phases/200.2-the-run-column-stops-repeating-itself-insert/200.2-02-SUMMARY.md` when done.
Record in it: the api.ts 197-decline trigger FIRED note, the slug-regex counter-test's RED observation, and the count-gate delta attribution.
</output>
