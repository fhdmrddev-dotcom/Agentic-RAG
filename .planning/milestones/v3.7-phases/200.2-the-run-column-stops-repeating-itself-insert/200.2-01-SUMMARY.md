---
phase: 200.2-the-run-column-stops-repeating-itself-insert
plan: 01
type: execute
completed_at: 2026-08-23
files_modified:
  - backend/app/api/workflow_runs.py
  - backend/app/middleware/canvas_gate.py
  - backend/tests/unit/test_200_2_citations.py
requirements: [RUN-05]
tags: [RUN-05, SC3, D-09, D-10, A-05, allow-list, set-equality-fence, string-scalar, idor, canvas-gate]
---

# Phase 200.2 Plan 01 Summary: Lazy Citation Passages Endpoint

## One-Liner
Mounted lazy endpoint `GET /workflow-runs/{workflow_run_id}/phases/{phase_slug}/citations` with four-field allow-list serialization and complete fence test suite.

---

## What Was Done

1. **`WorkflowRunCitationRead` Model (`backend/app/api/workflow_runs.py`)**:
   - Declares exactly four fields: `document_id: str`, `filename: str`, `chunk_index: int | None`, `passage: str | None`.
   - Strips `similarity`, `is_full_doc`, and arbitrary internal metadata by not copying them (allow-list design per A-05 / D-10).

2. **Lazy Read Route (`backend/app/api/workflow_runs.py`)**:
   - Registered `GET /{workflow_run_id}/phases/{phase_slug}/citations` under the existing `/workflow-runs` router.
   - Preserves uniform access posture: `Depends(require_canvas())` stands alone, consumes identity via `canvas_caller`, backed by user-JWT Supabase client.
   - Single query `id` + `user_id` ownership gate ensures uniform 404 response on miss (IDOR defense).
   - Unwraps `output` using `phase_output_object` to natively support both dict objects and jsonb string scalars.

3. **Canvas Gate Registration (`backend/app/middleware/canvas_gate.py`)**:
   - Added `"/workflow-runs/{workflow_run_id}/phases/{phase_slug}/citations"` to `CANVAS_GATED_PATHS` in the same commit.

4. **Fence Test Suite (`backend/tests/unit/test_200_2_citations.py`)**:
   - 9 comprehensive tests covering:
     - Set equality over `response.json()` keys.
     - `similarity` and sentinel leak absence (with non-vacuity check).
     - String scalar tolerance (the 484/484 completed DB shape).
     - Byte-identical 404 response between foreign and nonexistent runs.
     - Empty output / missing citations graceful degradation to `[]`.
     - 404 on missing phase slug.
     - Canvas flag cold-off byte-identical 404.
     - Path registration in `CANVAS_GATED_PATHS`.
     - AST walk ensuring zero `.select("*")` calls in `workflow_runs.py` (with positive control).
     - Model field totality.

---

## G-5 Verdict for `backend/app/api/workflow_runs.py`

**Honoured by construction:** `api/workflow_runs.py` previously held the single-run read (`read_workflow_run`) and the run log (`list_workflow_runs`). The new route `read_workflow_run_phase_citations` is a read-only sibling in the same run-reads family. Its access posture is copied verbatim from `read_workflow_run` (same gate, same dependencies, same 404 shape), and the named seam (*the read vs the access posture*) remains intact.

---

## Test Verification

- `tests/unit/test_200_2_citations.py` + `test_200_1_deliverable_text.py` + `test_188_workflow_run_read.py` + `test_seed190_run_log.py`: **65 passed, 0 failed in 5.02s**.
- Full backend unit suite (`tests/unit`): **2432 passed, 62 failed (matches exact pre-existing baseline), 2 xfailed, 2 xpassed in 39.54s**.
- Migrations diff (`git diff --numstat HEAD -- supabase/migrations`): **EMPTY** (migration 123 untouched).
- Package dependencies diff (`git diff --numstat HEAD -- frontend/package.json frontend/package-lock.json backend/requirements.txt`): **EMPTY**.
