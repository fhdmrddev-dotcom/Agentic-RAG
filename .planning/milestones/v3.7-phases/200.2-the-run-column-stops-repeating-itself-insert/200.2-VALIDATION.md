---
phase: 200.2
slug: the-run-column-stops-repeating-itself-insert
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-23
validated: 2026-08-24
---

# Phase 200.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: `200.2-RESEARCH.md` § Validation Architecture + UAT Plan (HIGH confidence, all file:line-verified).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Frontend framework** | Vitest (+ React Testing Library), run via `scripts/vitest-count-gate.cjs` with `GSD_VITEST_MAX_WORKERS=2` |
| **Frontend config** | `frontend/vitest.config.*`; count gate `scripts/vitest-count-gate.cjs` (TARGETS list) |
| **Backend framework** | pytest, fully offline (`_FakeSupabase` seam from `tests/test_188_workflow_run_read.py`) |
| **Backend quick run** | `cd backend && venv/Scripts/python -m pytest tests/unit/test_200_2_citations.py -x` (new file, Wave 0) |
| **Frontend quick run** | `cd frontend && npx vitest run src/pages/WorkflowRunPage.test.tsx src/components/workflows/RunStepList.test.tsx src/components/workflows/RunHero.test.tsx` |
| **Full suite** | `GSD_VITEST_MAX_WORKERS=2 node scripts/vitest-count-gate.cjs` (repo root) + `cd backend && venv/Scripts/python -m pytest tests/unit` |
| **Typecheck** | `cd frontend && npx tsc -p tsconfig.app.json --noEmit` (the `-p` is load-bearing; baseline 33 errors / 19 files — must not grow) |

---

## Sampling Rate

- **After every task commit:** the new backend test file + the affected frontend suites (quick run commands above).
- **After every plan wave:** full count gate + full backend unit suite.
- **Before `/gsd:verify-work`:** Full suite must be green + the manual UAT rows below.
- **Max feedback latency:** ~60 s (quick runs); count gate is the slow step.

---

## Per-Task Verification Map

| Behavior | Requirement | Threat Ref | Test Type | Automated Command | File Exists | Status |
|----------|-------------|------------|-----------|-------------------|-------------|--------|
| New citation route returns ONLY the 4 keys; `similarity` stripped | RUN-05 / SC#3 | T-200.2-01 (cross-tenant), T-200.2-02 (key leak) | unit (backend) | `pytest tests/unit/test_200_2_citations.py -x` | ✅ exists | ✅ green |
| Set-equality fence on the response body + planted never-before-seen key + non-vacuity control + driven counterfactual (replace `phase_output_object` with a local isinstance → RED) | RUN-05 / SC#3 | T-200.2-02 | unit (backend) | `pytest tests/unit/test_200_2_citations.py -x` | ✅ exists | ✅ green |
| Existing run serializer + "only `text`" comment byte-unchanged (AST walk, not grep — `select("*")` grep is unsatisfiable at base, 2 hits both comments) | RUN-05 / SC#3 | T-200.2-06 | unit (backend) | `pytest tests/unit/test_200_1_deliverable_text.py` | ✅ exists | ✅ green |
| Four hero arms render four DISTINCT textContents (set-size assertion + positive control proving the set collapses when two readings coincide) | RUN-05 / SC#1 | — | unit (frontend) | `vitest run src/pages/WorkflowRunPage.test.tsx src/components/workflows/RunHero.test.tsx` | ✅ exists | ✅ green |
| Yield replaces ONLY `finished`; every other outcome keeps its word and renders no yield; `0` is a fact, `null` renders nothing (no 0, no dash) | RUN-05 / SC#2 | — | unit (frontend) | `vitest run src/components/workflows/RunStepList.test.tsx` | ✅ exists | ✅ green |
| D-06 "wrote an answer" keyed on `phase_type` from wire, never slug | RUN-05 / SC#2 | — | unit (frontend) | `vitest run src/components/workflows/RunStepList.test.tsx` | ✅ exists | ✅ green |
| Empty hero TOTAL over terminal statuses; type-level exhaustiveness; unknown never reads as success | RUN-05 / D-16 | — | unit (frontend) + typecheck | `vitest run src/components/workflows/RunHero.test.tsx` + `tsc -p tsconfig.app.json` | ✅ exists | ✅ green |
| `RunSpine` count sub-line removed; `spine-count` testid gone (A-02 / D-05 load-bearing) | RUN-05 / D-05 | — | unit (frontend) | `vitest run src/components/workflows/RunSpine.test.tsx` | ✅ exists | ✅ green |
| Sketch teardown: `frontend/sketch/` + `tsconfig.sketch.json` deleted in one commit (A-10) | RUN-05 / A-10 | — | unit (frontend) | `npx vitest run src/__tests__/no-sketch-surface.test.ts` | ✅ exists | ✅ green |
| The two columns no longer read as the same list | RUN-05 / SC#4 | — | **MANUAL — human on screen** | see UAT scoreboard (`200.2-UAT.md` Row 1) | n/a | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] `backend/tests/unit/test_200_2_citations.py` — the new route's fence suite (set-equality, planted key, non-vacuity, counterfactual). Shares `_FakeSupabase` from `tests/test_188_workflow_run_read.py`.
- [x] `frontend/src/components/workflows/RunStepList.test.tsx` — the step-card list suite (yield arms, absent arm, disclosure, tense-rule interaction).
- [x] Hero-arm cases added to `WorkflowRunPage.test.tsx` (four distinct textContents; the superseded-heading zero-occurrence fence; the `COPY_NO_FILES_LIVE` retirement) & `RunHero.test.tsx`.
- [x] No framework install needed — Vitest + pytest are present.

---

## The 187-24 prose-trap, applied to this phase's fences

- The `select("*")` criterion on `api/workflow_runs.py` is unsatisfiable as a grep (returns 2, both comments). Use an **AST walk** (the `db/workflows.py` `json.dumps` fence precedent).
- `WorkflowRunPage.tsx` carries BOTH a comment-stripped fence (`codeOf(pageSource)`) AND a raw `?raw` fence. Read each fence's source before writing any comment that names a forbidden token. Describe, never spell.
- Any new "zero focusable elements" or "zero controls" fence on `RunTranscript`/its replacement must be re-checked after D-12 adds the first per-row focus stops.

---

## Manual-Only Verifications (G-4 lived-experience rows — the heart of this phase)

| Behavior | Requirement | Why Manual | Test Instructions | Status |
|----------|-------------|------------|-------------------|--------|
| **SC#4 — the load-bearing row (RUN-05's acceptance bar).** A HUMAN reads the two columns on screen, on a real finished run, and judges whether they still read as the same list. | RUN-05 / SC#4 | A green suite cannot tick this — it is judged on screen. | Chrome-MCP-driven with the operator watching; read the rendered `textContent` via `evaluate_script` (`take_screenshot` times out on this box — use DOM geometry). | ✅ VERIFIED (`200.2-UAT.md` Row 1) |
| The four hero arms driven in a browser on real runs, including `neither` (today reads as a loading failure). | RUN-05 / SC#1 | Real-run rendering; arm-defining run ids go stale. | Re-derive arm-defining run ids at plan time (200.1-02's table: both `bef870ba`/`f4c8d111`, answer `13691156`, nothing `c353ff78`; file-only may be unreachable in this DB). Drive via the run surface directly. | ✅ VERIFIED (`200.2-UAT.md` Row 2) |
| The spine's ring faces on a finished run (A-09 near-miss #2) — no `?` glyphs. | RUN-05 / A-09 | Visual glyph check. | Open a finished run; confirm the spine rings render faces, no `?`. | ✅ VERIFIED (`200.2-UAT.md` Row 5) |
| The disclosure affordance (D-12) — expand a step with citations, verify passages appear inline and carry NO relevance score; expand a step with none, verify the yield stays plain text. | RUN-05 / SC#3 | Verifies the seam chosen at planning time (fetch-on-expand, degrade on empty). | Expand a citations step → passages inline, no score. Expand a no-citations step → nothing appears, yield stays plain. | ✅ VERIFIED (`200.2-UAT.md` Row 4) |
| The narrow viewport (`<1024px`, where `RunSpine` is `hidden…lg:flex`) — the hero becomes the whole page; verify it holds (A-07: every CONTEXT decision assumes two columns). | RUN-05 / A-07 | Layout under a breakpoint. | Resize to <1024px; hero is the whole page and holds. | ✅ VERIFIED (`200.2-UAT.md` Row 6) |
| The long-answer row (long-message axis, PARTIAL) — a run with a multi-KB answer scrolls within the hero (measured max 38,935 chars). | RUN-05 / long-message | Layout under a long payload. | Open a run with a long answer; hero scrolls within its region, layout does not blow. | ✅ VERIFIED (`200.2-UAT.md` Row 7) |

**4-axis note (ROADMAP SC#10):** this phase touches **UI state only** (a restyle of an existing surface + one lazy read). Cross-provider / multi-tool / parallel-thread axes do NOT apply and are stated here explicitly rather than silently omitted. Only the long-message axis applies, partially (the long-answer row above).

---

## Validation Audit 2026-08-24

| Metric | Count |
|--------|-------|
| Gaps found | 0 |
| Resolved | 0 |
| Escalated | 0 |
| Total Automated Tests Verified | 34 backend unit (0 fail) + 237 frontend unit (5435 count gate total, 0 fail) |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 60 s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** complete (2026-08-24)
