# Resume here — 2026-08-25

Paste this whole file into a fresh session. Everything below is measured, not remembered.

## Where v3.8 stands

**201–205 complete and verified live. 206 is planned and pre-flighted, NOT executed.**

| Phase | State |
|---|---|
| 201, 202, 203 | ✅ shipped earlier |
| **204** Scheduled runs | ✅ complete, **driven live** — 60s cap tripped at 61.06s, exactly-once held 10/10 |
| **204.1** Card says what runs itself | ✅ complete, **seen in the browser** |
| **205** Stateful workflows | ✅ complete, **two-run UAT passed**: run 1 → `1. ALPHA`, run 2 → `1. ALPHA / 2. BRAVO` |
| **206** MCP connector client | 📋 planned by Gemini, pre-flighted, **one blocker open** |

## ⛔ THE ONE THING BLOCKING 206 — an operator decision, not a code change

`206-01-PLAN.md` task 5 adds **two runtime exports** to `frontend/src/lib/api.ts`
(`discoverConnectorTools`, `updateConnectorGrants`). That file's trigger, at
`docs/HOT-FILE-LEDGER.md:3676`, says verbatim:

> **The NEXT phase that adds a runtime export to `api.ts` TAKES the split, or escalates it to the
> operator as a phase of its own. It may NOT re-decline.**

206 is that phase. The two prior declines (197, 192.2) were type-only and measured; that defence is
gone. **Recommendation: escalate and defer** — let 206 add the exports, record it as a decision with
a fresh trigger, and keep the 6,580-line extraction out of a phase whose real work is an MCP client.

⚠ **Either way the mock budget ships in the same commit.** 7 suites `vi.mock("@/lib/api")` AND touch
206's surfaces — this is the exact shape that produced **249 red tests** at `196-08`:
`chat/__tests__/StopControl.test.tsx` · `workflows/ConnectionPicker.test.tsx` ·
`workflows/TemplateAttachSection.test.tsx` · `workflows/WorkflowDoorSwitch.test.tsx` ·
`hooks/useDraftPersistence.test.tsx` · `pages/WorkflowBuilderPage.canvas.test.tsx` ·
`pages/WorkflowBuilderPage.header.test.tsx`

Full review with 7 more findings: `.planning/phases/206-mcp-connector-client-workflow-scoped/206-PREFLIGHT.md`

## The working pipeline (keep it — it has paid for itself twice)

**Gemini plans + executes → Claude pre-flights before execution and verifies after.**
Recorded at `~/.claude/.../memory/feedback_gemini_plans_claude_reviews_pipeline.md`.

Its record so far:
- **205 pre-flight caught 2 blockers before a line was written.** The sharpest: scoping the prior run
  on `definition_id` would have **reset the living register on every republish** (measured —
  `pm-weekly-status-report` carries 4 definition ids for one slug). It would have looked like correct
  cold-start behaviour and no test could have caught it.
- **204 verification found 2 defects that 106 passing tests could not see**, both at the seam between
  two parallel waves, both found by **driving a real run**.

⚠ **The rule that came out of it: verify by DRIVING the feature, never by reading code.** Every
significant defect this week was invisible to a green suite.

## Baselines — measured 2026-08-25, use these to judge "new"

```
backend/tests/unit                     68 failing   (diff failing-ID SETS, never counts)
npx tsc --noEmit -p tsconfig.app.json  34 errors    (bare --noEmit checks ZERO files)
vitest count gate                      5444 · failed 0 · 110/110
connector_connections                  2 rows, capability CHECK = send_email|create_ticket|post_message
highest migration                      125 (applied) → 206 uses 126
```

## Environment state

- Migrations **124 + 125 applied locally**; `full-schema.sql` regenerated. **Neither is in cloud.**
- ⚠ **125 FAILS OPEN** — unapplied means `load_run_budget` returns empty and **every run is uncapped**
  while the UI shows a cap. Apply it before any cloud scheduled run exists.
- Scheduler is **ON** (`SCHEDULER_PROCESS_ENABLED=true`, `WORKER_COUNT=2`, 60s poll). 0 schedules now,
  so it is a no-op — but anything created from the UI genuinely fires.
- **The operator starts the backend and frontend**, never Claude. Backend `:8000`, frontend
  `localhost:5173` (IPv6 — a `127.0.0.1` check misses it).
- DB is clean: 0 schedules, 0 active runs, all UAT artifacts removed.

## What to do next, in order

1. **Answer the `api.ts` blocker** (above), then tell Gemini `/gsd:execute-phase 206`.
2. **Verify 206 in a fresh session** — drive a real MCP call against a public endpoint or a local
   stub. A mock asserting against itself does not count.
3. **Then deploy.** 201–205 is a substantial milestone sitting only on this laptop, and 204 only earns
   its keep on a machine that stays on. Ship with `SCHEDULER_PROCESS_ENABLED=false`, confirm the
   tables, then flip it.

## Parked, with pointers

- **Connections & Open Platform** milestone — `.planning/CONNECTIONS-MILESTONE-CANDIDATE.md`.
  Provider-shaped connections + OAuth + reads + chat surface + MCP both directions. 7 seeds routed.
- **SEED-198 Experts** — a domain bundle over skills + connections + KNOWLEDGE SCOPE, never a second
  runtime. The differentiator is the knowledge half; a plugin directory structurally cannot have it.
- **SEED-199 xyOps canvas grammar** — TWO node classes. Triggers, constraints and connectors become
  NODES rather than dialog fields. Cheapest first slice is connectors, at 206.
- **`task_service.py` fires G-5 at nine phases with NO ledger row** — sole home of
  `_stream_one_iteration` and `run_task_sub_agent`, so a change there lands in **chat** too.
- **`llm_emit` cannot measure its own token spend** (`forced_emit.py` has zero `usage` occurrences),
  so one of four phase types sits outside 204's cap.
